var sb=Object.defineProperty;var rb=(t,i,n)=>i in t?sb(t,i,{enumerable:!0,configurable:!0,writable:!0,value:n}):t[i]=n;var X=(t,i,n)=>rb(t,typeof i!="symbol"?i+"":i,n);function lb(t,i){for(var n=0;n<i.length;n++){const s=i[n];if(typeof s!="string"&&!Array.isArray(s)){for(const r in s)if(r!=="default"&&!(r in t)){const c=Object.getOwnPropertyDescriptor(s,r);c&&Object.defineProperty(t,r,c.get?c:{enumerable:!0,get:()=>s[r]})}}}return Object.freeze(Object.defineProperty(t,Symbol.toStringTag,{value:"Module"}))}(function(){const i=document.createElement("link").relList;if(i&&i.supports&&i.supports("modulepreload"))return;for(const r of document.querySelectorAll('link[rel="modulepreload"]'))s(r);new MutationObserver(r=>{for(const c of r)if(c.type==="childList")for(const h of c.addedNodes)h.tagName==="LINK"&&h.rel==="modulepreload"&&s(h)}).observe(document,{childList:!0,subtree:!0});function n(r){const c={};return r.integrity&&(c.integrity=r.integrity),r.referrerPolicy&&(c.referrerPolicy=r.referrerPolicy),r.crossOrigin==="use-credentials"?c.credentials="include":r.crossOrigin==="anonymous"?c.credentials="omit":c.credentials="same-origin",c}function s(r){if(r.ep)return;r.ep=!0;const c=n(r);fetch(r.href,c)}})();function Ff(t){return t&&t.__esModule&&Object.prototype.hasOwnProperty.call(t,"default")?t.default:t}var nc={exports:{}},Fn={},oc={exports:{}},le={};/**
 * @license React
 * react.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var qp;function cb(){if(qp)return le;qp=1;var t=Symbol.for("react.element"),i=Symbol.for("react.portal"),n=Symbol.for("react.fragment"),s=Symbol.for("react.strict_mode"),r=Symbol.for("react.profiler"),c=Symbol.for("react.provider"),h=Symbol.for("react.context"),p=Symbol.for("react.forward_ref"),m=Symbol.for("react.suspense"),f=Symbol.for("react.memo"),y=Symbol.for("react.lazy"),v=Symbol.iterator;function b(P){return P===null||typeof P!="object"?null:(P=v&&P[v]||P["@@iterator"],typeof P=="function"?P:null)}var A={isMounted:function(){return!1},enqueueForceUpdate:function(){},enqueueReplaceState:function(){},enqueueSetState:function(){}},T=Object.assign,I={};function M(P,F,se){this.props=P,this.context=F,this.refs=I,this.updater=se||A}M.prototype.isReactComponent={},M.prototype.setState=function(P,F){if(typeof P!="object"&&typeof P!="function"&&P!=null)throw Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");this.updater.enqueueSetState(this,P,F,"setState")},M.prototype.forceUpdate=function(P){this.updater.enqueueForceUpdate(this,P,"forceUpdate")};function R(){}R.prototype=M.prototype;function O(P,F,se){this.props=P,this.context=F,this.refs=I,this.updater=se||A}var q=O.prototype=new R;q.constructor=O,T(q,M.prototype),q.isPureReactComponent=!0;var z=Array.isArray,V=Object.prototype.hasOwnProperty,K={current:null},ie={key:!0,ref:!0,__self:!0,__source:!0};function J(P,F,se){var re,de={},he=null,ge=null;if(F!=null)for(re in F.ref!==void 0&&(ge=F.ref),F.key!==void 0&&(he=""+F.key),F)V.call(F,re)&&!ie.hasOwnProperty(re)&&(de[re]=F[re]);var pe=arguments.length-2;if(pe===1)de.children=se;else if(1<pe){for(var be=Array(pe),ve=0;ve<pe;ve++)be[ve]=arguments[ve+2];de.children=be}if(P&&P.defaultProps)for(re in pe=P.defaultProps,pe)de[re]===void 0&&(de[re]=pe[re]);return{$$typeof:t,type:P,key:he,ref:ge,props:de,_owner:K.current}}function ce(P,F){return{$$typeof:t,type:P.type,key:F,ref:P.ref,props:P.props,_owner:P._owner}}function we(P){return typeof P=="object"&&P!==null&&P.$$typeof===t}function Te(P){var F={"=":"=0",":":"=2"};return"$"+P.replace(/[=:]/g,function(se){return F[se]})}var Ke=/\/+/g;function je(P,F){return typeof P=="object"&&P!==null&&P.key!=null?Te(""+P.key):F.toString(36)}function Ve(P,F,se,re,de){var he=typeof P;(he==="undefined"||he==="boolean")&&(P=null);var ge=!1;if(P===null)ge=!0;else switch(he){case"string":case"number":ge=!0;break;case"object":switch(P.$$typeof){case t:case i:ge=!0}}if(ge)return ge=P,de=de(ge),P=re===""?"."+je(ge,0):re,z(de)?(se="",P!=null&&(se=P.replace(Ke,"$&/")+"/"),Ve(de,F,se,"",function(ve){return ve})):de!=null&&(we(de)&&(de=ce(de,se+(!de.key||ge&&ge.key===de.key?"":(""+de.key).replace(Ke,"$&/")+"/")+P)),F.push(de)),1;if(ge=0,re=re===""?".":re+":",z(P))for(var pe=0;pe<P.length;pe++){he=P[pe];var be=re+je(he,pe);ge+=Ve(he,F,se,be,de)}else if(be=b(P),typeof be=="function")for(P=be.call(P),pe=0;!(he=P.next()).done;)he=he.value,be=re+je(he,pe++),ge+=Ve(he,F,se,be,de);else if(he==="object")throw F=String(P),Error("Objects are not valid as a React child (found: "+(F==="[object Object]"?"object with keys {"+Object.keys(P).join(", ")+"}":F)+"). If you meant to render a collection of children, use an array instead.");return ge}function Ee(P,F,se){if(P==null)return P;var re=[],de=0;return Ve(P,re,"","",function(he){return F.call(se,he,de++)}),re}function _e(P){if(P._status===-1){var F=P._result;F=F(),F.then(function(se){(P._status===0||P._status===-1)&&(P._status=1,P._result=se)},function(se){(P._status===0||P._status===-1)&&(P._status=2,P._result=se)}),P._status===-1&&(P._status=0,P._result=F)}if(P._status===1)return P._result.default;throw P._result}var oe={current:null},W={transition:null},Z={ReactCurrentDispatcher:oe,ReactCurrentBatchConfig:W,ReactCurrentOwner:K};function H(){throw Error("act(...) is not supported in production builds of React.")}return le.Children={map:Ee,forEach:function(P,F,se){Ee(P,function(){F.apply(this,arguments)},se)},count:function(P){var F=0;return Ee(P,function(){F++}),F},toArray:function(P){return Ee(P,function(F){return F})||[]},only:function(P){if(!we(P))throw Error("React.Children.only expected to receive a single React element child.");return P}},le.Component=M,le.Fragment=n,le.Profiler=r,le.PureComponent=O,le.StrictMode=s,le.Suspense=m,le.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=Z,le.act=H,le.cloneElement=function(P,F,se){if(P==null)throw Error("React.cloneElement(...): The argument must be a React element, but you passed "+P+".");var re=T({},P.props),de=P.key,he=P.ref,ge=P._owner;if(F!=null){if(F.ref!==void 0&&(he=F.ref,ge=K.current),F.key!==void 0&&(de=""+F.key),P.type&&P.type.defaultProps)var pe=P.type.defaultProps;for(be in F)V.call(F,be)&&!ie.hasOwnProperty(be)&&(re[be]=F[be]===void 0&&pe!==void 0?pe[be]:F[be])}var be=arguments.length-2;if(be===1)re.children=se;else if(1<be){pe=Array(be);for(var ve=0;ve<be;ve++)pe[ve]=arguments[ve+2];re.children=pe}return{$$typeof:t,type:P.type,key:de,ref:he,props:re,_owner:ge}},le.createContext=function(P){return P={$$typeof:h,_currentValue:P,_currentValue2:P,_threadCount:0,Provider:null,Consumer:null,_defaultValue:null,_globalName:null},P.Provider={$$typeof:c,_context:P},P.Consumer=P},le.createElement=J,le.createFactory=function(P){var F=J.bind(null,P);return F.type=P,F},le.createRef=function(){return{current:null}},le.forwardRef=function(P){return{$$typeof:p,render:P}},le.isValidElement=we,le.lazy=function(P){return{$$typeof:y,_payload:{_status:-1,_result:P},_init:_e}},le.memo=function(P,F){return{$$typeof:f,type:P,compare:F===void 0?null:F}},le.startTransition=function(P){var F=W.transition;W.transition={};try{P()}finally{W.transition=F}},le.unstable_act=H,le.useCallback=function(P,F){return oe.current.useCallback(P,F)},le.useContext=function(P){return oe.current.useContext(P)},le.useDebugValue=function(){},le.useDeferredValue=function(P){return oe.current.useDeferredValue(P)},le.useEffect=function(P,F){return oe.current.useEffect(P,F)},le.useId=function(){return oe.current.useId()},le.useImperativeHandle=function(P,F,se){return oe.current.useImperativeHandle(P,F,se)},le.useInsertionEffect=function(P,F){return oe.current.useInsertionEffect(P,F)},le.useLayoutEffect=function(P,F){return oe.current.useLayoutEffect(P,F)},le.useMemo=function(P,F){return oe.current.useMemo(P,F)},le.useReducer=function(P,F,se){return oe.current.useReducer(P,F,se)},le.useRef=function(P){return oe.current.useRef(P)},le.useState=function(P){return oe.current.useState(P)},le.useSyncExternalStore=function(P,F,se){return oe.current.useSyncExternalStore(P,F,se)},le.useTransition=function(){return oe.current.useTransition()},le.version="18.3.1",le}var Op;function Xc(){return Op||(Op=1,oc.exports=cb()),oc.exports}/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Fp;function db(){if(Fp)return Fn;Fp=1;var t=Xc(),i=Symbol.for("react.element"),n=Symbol.for("react.fragment"),s=Object.prototype.hasOwnProperty,r=t.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner,c={key:!0,ref:!0,__self:!0,__source:!0};function h(p,m,f){var y,v={},b=null,A=null;f!==void 0&&(b=""+f),m.key!==void 0&&(b=""+m.key),m.ref!==void 0&&(A=m.ref);for(y in m)s.call(m,y)&&!c.hasOwnProperty(y)&&(v[y]=m[y]);if(p&&p.defaultProps)for(y in m=p.defaultProps,m)v[y]===void 0&&(v[y]=m[y]);return{$$typeof:i,type:p,key:b,ref:A,props:v,_owner:r.current}}return Fn.Fragment=n,Fn.jsx=h,Fn.jsxs=h,Fn}var Lp;function hb(){return Lp||(Lp=1,nc.exports=db()),nc.exports}var g=hb(),x=Xc();const Dt=Ff(x),iq=lb({__proto__:null,default:Dt},[x]);var Ss={},sc={exports:{}},pt={},rc={exports:{}},lc={};/**
 * @license React
 * scheduler.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var jp;function ub(){return jp||(jp=1,(function(t){function i(W,Z){var H=W.length;W.push(Z);e:for(;0<H;){var P=H-1>>>1,F=W[P];if(0<r(F,Z))W[P]=Z,W[H]=F,H=P;else break e}}function n(W){return W.length===0?null:W[0]}function s(W){if(W.length===0)return null;var Z=W[0],H=W.pop();if(H!==Z){W[0]=H;e:for(var P=0,F=W.length,se=F>>>1;P<se;){var re=2*(P+1)-1,de=W[re],he=re+1,ge=W[he];if(0>r(de,H))he<F&&0>r(ge,de)?(W[P]=ge,W[he]=H,P=he):(W[P]=de,W[re]=H,P=re);else if(he<F&&0>r(ge,H))W[P]=ge,W[he]=H,P=he;else break e}}return Z}function r(W,Z){var H=W.sortIndex-Z.sortIndex;return H!==0?H:W.id-Z.id}if(typeof performance=="object"&&typeof performance.now=="function"){var c=performance;t.unstable_now=function(){return c.now()}}else{var h=Date,p=h.now();t.unstable_now=function(){return h.now()-p}}var m=[],f=[],y=1,v=null,b=3,A=!1,T=!1,I=!1,M=typeof setTimeout=="function"?setTimeout:null,R=typeof clearTimeout=="function"?clearTimeout:null,O=typeof setImmediate<"u"?setImmediate:null;typeof navigator<"u"&&navigator.scheduling!==void 0&&navigator.scheduling.isInputPending!==void 0&&navigator.scheduling.isInputPending.bind(navigator.scheduling);function q(W){for(var Z=n(f);Z!==null;){if(Z.callback===null)s(f);else if(Z.startTime<=W)s(f),Z.sortIndex=Z.expirationTime,i(m,Z);else break;Z=n(f)}}function z(W){if(I=!1,q(W),!T)if(n(m)!==null)T=!0,_e(V);else{var Z=n(f);Z!==null&&oe(z,Z.startTime-W)}}function V(W,Z){T=!1,I&&(I=!1,R(J),J=-1),A=!0;var H=b;try{for(q(Z),v=n(m);v!==null&&(!(v.expirationTime>Z)||W&&!Te());){var P=v.callback;if(typeof P=="function"){v.callback=null,b=v.priorityLevel;var F=P(v.expirationTime<=Z);Z=t.unstable_now(),typeof F=="function"?v.callback=F:v===n(m)&&s(m),q(Z)}else s(m);v=n(m)}if(v!==null)var se=!0;else{var re=n(f);re!==null&&oe(z,re.startTime-Z),se=!1}return se}finally{v=null,b=H,A=!1}}var K=!1,ie=null,J=-1,ce=5,we=-1;function Te(){return!(t.unstable_now()-we<ce)}function Ke(){if(ie!==null){var W=t.unstable_now();we=W;var Z=!0;try{Z=ie(!0,W)}finally{Z?je():(K=!1,ie=null)}}else K=!1}var je;if(typeof O=="function")je=function(){O(Ke)};else if(typeof MessageChannel<"u"){var Ve=new MessageChannel,Ee=Ve.port2;Ve.port1.onmessage=Ke,je=function(){Ee.postMessage(null)}}else je=function(){M(Ke,0)};function _e(W){ie=W,K||(K=!0,je())}function oe(W,Z){J=M(function(){W(t.unstable_now())},Z)}t.unstable_IdlePriority=5,t.unstable_ImmediatePriority=1,t.unstable_LowPriority=4,t.unstable_NormalPriority=3,t.unstable_Profiling=null,t.unstable_UserBlockingPriority=2,t.unstable_cancelCallback=function(W){W.callback=null},t.unstable_continueExecution=function(){T||A||(T=!0,_e(V))},t.unstable_forceFrameRate=function(W){0>W||125<W?console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"):ce=0<W?Math.floor(1e3/W):5},t.unstable_getCurrentPriorityLevel=function(){return b},t.unstable_getFirstCallbackNode=function(){return n(m)},t.unstable_next=function(W){switch(b){case 1:case 2:case 3:var Z=3;break;default:Z=b}var H=b;b=Z;try{return W()}finally{b=H}},t.unstable_pauseExecution=function(){},t.unstable_requestPaint=function(){},t.unstable_runWithPriority=function(W,Z){switch(W){case 1:case 2:case 3:case 4:case 5:break;default:W=3}var H=b;b=W;try{return Z()}finally{b=H}},t.unstable_scheduleCallback=function(W,Z,H){var P=t.unstable_now();switch(typeof H=="object"&&H!==null?(H=H.delay,H=typeof H=="number"&&0<H?P+H:P):H=P,W){case 1:var F=-1;break;case 2:F=250;break;case 5:F=1073741823;break;case 4:F=1e4;break;default:F=5e3}return F=H+F,W={id:y++,callback:Z,priorityLevel:W,startTime:H,expirationTime:F,sortIndex:-1},H>P?(W.sortIndex=H,i(f,W),n(m)===null&&W===n(f)&&(I?(R(J),J=-1):I=!0,oe(z,H-P))):(W.sortIndex=F,i(m,W),T||A||(T=!0,_e(V))),W},t.unstable_shouldYield=Te,t.unstable_wrapCallback=function(W){var Z=b;return function(){var H=b;b=Z;try{return W.apply(this,arguments)}finally{b=H}}}})(lc)),lc}var Vp;function pb(){return Vp||(Vp=1,rc.exports=ub()),rc.exports}/**
 * @license React
 * react-dom.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Np;function mb(){if(Np)return pt;Np=1;var t=Xc(),i=pb();function n(e){for(var a="https://reactjs.org/docs/error-decoder.html?invariant="+e,o=1;o<arguments.length;o++)a+="&args[]="+encodeURIComponent(arguments[o]);return"Minified React error #"+e+"; visit "+a+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings."}var s=new Set,r={};function c(e,a){h(e,a),h(e+"Capture",a)}function h(e,a){for(r[e]=a,e=0;e<a.length;e++)s.add(a[e])}var p=!(typeof window>"u"||typeof window.document>"u"||typeof window.document.createElement>"u"),m=Object.prototype.hasOwnProperty,f=/^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/,y={},v={};function b(e){return m.call(v,e)?!0:m.call(y,e)?!1:f.test(e)?v[e]=!0:(y[e]=!0,!1)}function A(e,a,o,l){if(o!==null&&o.type===0)return!1;switch(typeof a){case"function":case"symbol":return!0;case"boolean":return l?!1:o!==null?!o.acceptsBooleans:(e=e.toLowerCase().slice(0,5),e!=="data-"&&e!=="aria-");default:return!1}}function T(e,a,o,l){if(a===null||typeof a>"u"||A(e,a,o,l))return!0;if(l)return!1;if(o!==null)switch(o.type){case 3:return!a;case 4:return a===!1;case 5:return isNaN(a);case 6:return isNaN(a)||1>a}return!1}function I(e,a,o,l,d,u,w){this.acceptsBooleans=a===2||a===3||a===4,this.attributeName=l,this.attributeNamespace=d,this.mustUseProperty=o,this.propertyName=e,this.type=a,this.sanitizeURL=u,this.removeEmptyString=w}var M={};"children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(e){M[e]=new I(e,0,!1,e,null,!1,!1)}),[["acceptCharset","accept-charset"],["className","class"],["htmlFor","for"],["httpEquiv","http-equiv"]].forEach(function(e){var a=e[0];M[a]=new I(a,1,!1,e[1],null,!1,!1)}),["contentEditable","draggable","spellCheck","value"].forEach(function(e){M[e]=new I(e,2,!1,e.toLowerCase(),null,!1,!1)}),["autoReverse","externalResourcesRequired","focusable","preserveAlpha"].forEach(function(e){M[e]=new I(e,2,!1,e,null,!1,!1)}),"allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(e){M[e]=new I(e,3,!1,e.toLowerCase(),null,!1,!1)}),["checked","multiple","muted","selected"].forEach(function(e){M[e]=new I(e,3,!0,e,null,!1,!1)}),["capture","download"].forEach(function(e){M[e]=new I(e,4,!1,e,null,!1,!1)}),["cols","rows","size","span"].forEach(function(e){M[e]=new I(e,6,!1,e,null,!1,!1)}),["rowSpan","start"].forEach(function(e){M[e]=new I(e,5,!1,e.toLowerCase(),null,!1,!1)});var R=/[\-:]([a-z])/g;function O(e){return e[1].toUpperCase()}"accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(e){var a=e.replace(R,O);M[a]=new I(a,1,!1,e,null,!1,!1)}),"xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(e){var a=e.replace(R,O);M[a]=new I(a,1,!1,e,"http://www.w3.org/1999/xlink",!1,!1)}),["xml:base","xml:lang","xml:space"].forEach(function(e){var a=e.replace(R,O);M[a]=new I(a,1,!1,e,"http://www.w3.org/XML/1998/namespace",!1,!1)}),["tabIndex","crossOrigin"].forEach(function(e){M[e]=new I(e,1,!1,e.toLowerCase(),null,!1,!1)}),M.xlinkHref=new I("xlinkHref",1,!1,"xlink:href","http://www.w3.org/1999/xlink",!0,!1),["src","href","action","formAction"].forEach(function(e){M[e]=new I(e,1,!1,e.toLowerCase(),null,!0,!0)});function q(e,a,o,l){var d=M.hasOwnProperty(a)?M[a]:null;(d!==null?d.type!==0:l||!(2<a.length)||a[0]!=="o"&&a[0]!=="O"||a[1]!=="n"&&a[1]!=="N")&&(T(a,o,d,l)&&(o=null),l||d===null?b(a)&&(o===null?e.removeAttribute(a):e.setAttribute(a,""+o)):d.mustUseProperty?e[d.propertyName]=o===null?d.type===3?!1:"":o:(a=d.attributeName,l=d.attributeNamespace,o===null?e.removeAttribute(a):(d=d.type,o=d===3||d===4&&o===!0?"":""+o,l?e.setAttributeNS(l,a,o):e.setAttribute(a,o))))}var z=t.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED,V=Symbol.for("react.element"),K=Symbol.for("react.portal"),ie=Symbol.for("react.fragment"),J=Symbol.for("react.strict_mode"),ce=Symbol.for("react.profiler"),we=Symbol.for("react.provider"),Te=Symbol.for("react.context"),Ke=Symbol.for("react.forward_ref"),je=Symbol.for("react.suspense"),Ve=Symbol.for("react.suspense_list"),Ee=Symbol.for("react.memo"),_e=Symbol.for("react.lazy"),oe=Symbol.for("react.offscreen"),W=Symbol.iterator;function Z(e){return e===null||typeof e!="object"?null:(e=W&&e[W]||e["@@iterator"],typeof e=="function"?e:null)}var H=Object.assign,P;function F(e){if(P===void 0)try{throw Error()}catch(o){var a=o.stack.trim().match(/\n( *(at )?)/);P=a&&a[1]||""}return`
`+P+e}var se=!1;function re(e,a){if(!e||se)return"";se=!0;var o=Error.prepareStackTrace;Error.prepareStackTrace=void 0;try{if(a)if(a=function(){throw Error()},Object.defineProperty(a.prototype,"props",{set:function(){throw Error()}}),typeof Reflect=="object"&&Reflect.construct){try{Reflect.construct(a,[])}catch(U){var l=U}Reflect.construct(e,[],a)}else{try{a.call()}catch(U){l=U}e.call(a.prototype)}else{try{throw Error()}catch(U){l=U}e()}}catch(U){if(U&&l&&typeof U.stack=="string"){for(var d=U.stack.split(`
`),u=l.stack.split(`
`),w=d.length-1,k=u.length-1;1<=w&&0<=k&&d[w]!==u[k];)k--;for(;1<=w&&0<=k;w--,k--)if(d[w]!==u[k]){if(w!==1||k!==1)do if(w--,k--,0>k||d[w]!==u[k]){var S=`
`+d[w].replace(" at new "," at ");return e.displayName&&S.includes("<anonymous>")&&(S=S.replace("<anonymous>",e.displayName)),S}while(1<=w&&0<=k);break}}}finally{se=!1,Error.prepareStackTrace=o}return(e=e?e.displayName||e.name:"")?F(e):""}function de(e){switch(e.tag){case 5:return F(e.type);case 16:return F("Lazy");case 13:return F("Suspense");case 19:return F("SuspenseList");case 0:case 2:case 15:return e=re(e.type,!1),e;case 11:return e=re(e.type.render,!1),e;case 1:return e=re(e.type,!0),e;default:return""}}function he(e){if(e==null)return null;if(typeof e=="function")return e.displayName||e.name||null;if(typeof e=="string")return e;switch(e){case ie:return"Fragment";case K:return"Portal";case ce:return"Profiler";case J:return"StrictMode";case je:return"Suspense";case Ve:return"SuspenseList"}if(typeof e=="object")switch(e.$$typeof){case Te:return(e.displayName||"Context")+".Consumer";case we:return(e._context.displayName||"Context")+".Provider";case Ke:var a=e.render;return e=e.displayName,e||(e=a.displayName||a.name||"",e=e!==""?"ForwardRef("+e+")":"ForwardRef"),e;case Ee:return a=e.displayName||null,a!==null?a:he(e.type)||"Memo";case _e:a=e._payload,e=e._init;try{return he(e(a))}catch{}}return null}function ge(e){var a=e.type;switch(e.tag){case 24:return"Cache";case 9:return(a.displayName||"Context")+".Consumer";case 10:return(a._context.displayName||"Context")+".Provider";case 18:return"DehydratedFragment";case 11:return e=a.render,e=e.displayName||e.name||"",a.displayName||(e!==""?"ForwardRef("+e+")":"ForwardRef");case 7:return"Fragment";case 5:return a;case 4:return"Portal";case 3:return"Root";case 6:return"Text";case 16:return he(a);case 8:return a===J?"StrictMode":"Mode";case 22:return"Offscreen";case 12:return"Profiler";case 21:return"Scope";case 13:return"Suspense";case 19:return"SuspenseList";case 25:return"TracingMarker";case 1:case 0:case 17:case 2:case 14:case 15:if(typeof a=="function")return a.displayName||a.name||null;if(typeof a=="string")return a}return null}function pe(e){switch(typeof e){case"boolean":case"number":case"string":case"undefined":return e;case"object":return e;default:return""}}function be(e){var a=e.type;return(e=e.nodeName)&&e.toLowerCase()==="input"&&(a==="checkbox"||a==="radio")}function ve(e){var a=be(e)?"checked":"value",o=Object.getOwnPropertyDescriptor(e.constructor.prototype,a),l=""+e[a];if(!e.hasOwnProperty(a)&&typeof o<"u"&&typeof o.get=="function"&&typeof o.set=="function"){var d=o.get,u=o.set;return Object.defineProperty(e,a,{configurable:!0,get:function(){return d.call(this)},set:function(w){l=""+w,u.call(this,w)}}),Object.defineProperty(e,a,{enumerable:o.enumerable}),{getValue:function(){return l},setValue:function(w){l=""+w},stopTracking:function(){e._valueTracker=null,delete e[a]}}}}function Ie(e){e._valueTracker||(e._valueTracker=ve(e))}function nt(e){if(!e)return!1;var a=e._valueTracker;if(!a)return!0;var o=a.getValue(),l="";return e&&(l=be(e)?e.checked?"true":"false":e.value),e=l,e!==o?(a.setValue(e),!0):!1}function Rt(e){if(e=e||(typeof document<"u"?document:void 0),typeof e>"u")return null;try{return e.activeElement||e.body}catch{return e.body}}function Ji(e,a){var o=a.checked;return H({},a,{defaultChecked:void 0,defaultValue:void 0,value:void 0,checked:o??e._wrapperState.initialChecked})}function Yi(e,a){var o=a.defaultValue==null?"":a.defaultValue,l=a.checked!=null?a.checked:a.defaultChecked;o=pe(a.value!=null?a.value:o),e._wrapperState={initialChecked:l,initialValue:o,controlled:a.type==="checkbox"||a.type==="radio"?a.checked!=null:a.value!=null}}function oi(e,a){a=a.checked,a!=null&&q(e,"checked",a,!1)}function ur(e,a){oi(e,a);var o=pe(a.value),l=a.type;if(o!=null)l==="number"?(o===0&&e.value===""||e.value!=o)&&(e.value=""+o):e.value!==""+o&&(e.value=""+o);else if(l==="submit"||l==="reset"){e.removeAttribute("value");return}a.hasOwnProperty("value")?pr(e,a.type,o):a.hasOwnProperty("defaultValue")&&pr(e,a.type,pe(a.defaultValue)),a.checked==null&&a.defaultChecked!=null&&(e.defaultChecked=!!a.defaultChecked)}function zd(e,a,o){if(a.hasOwnProperty("value")||a.hasOwnProperty("defaultValue")){var l=a.type;if(!(l!=="submit"&&l!=="reset"||a.value!==void 0&&a.value!==null))return;a=""+e._wrapperState.initialValue,o||a===e.value||(e.value=a),e.defaultValue=a}o=e.name,o!==""&&(e.name=""),e.defaultChecked=!!e._wrapperState.initialChecked,o!==""&&(e.name=o)}function pr(e,a,o){(a!=="number"||Rt(e.ownerDocument)!==e)&&(o==null?e.defaultValue=""+e._wrapperState.initialValue:e.defaultValue!==""+o&&(e.defaultValue=""+o))}var Qi=Array.isArray;function si(e,a,o,l){if(e=e.options,a){a={};for(var d=0;d<o.length;d++)a["$"+o[d]]=!0;for(o=0;o<e.length;o++)d=a.hasOwnProperty("$"+e[o].value),e[o].selected!==d&&(e[o].selected=d),d&&l&&(e[o].defaultSelected=!0)}else{for(o=""+pe(o),a=null,d=0;d<e.length;d++){if(e[d].value===o){e[d].selected=!0,l&&(e[d].defaultSelected=!0);return}a!==null||e[d].disabled||(a=e[d])}a!==null&&(a.selected=!0)}}function mr(e,a){if(a.dangerouslySetInnerHTML!=null)throw Error(n(91));return H({},a,{value:void 0,defaultValue:void 0,children:""+e._wrapperState.initialValue})}function Hd(e,a){var o=a.value;if(o==null){if(o=a.children,a=a.defaultValue,o!=null){if(a!=null)throw Error(n(92));if(Qi(o)){if(1<o.length)throw Error(n(93));o=o[0]}a=o}a==null&&(a=""),o=a}e._wrapperState={initialValue:pe(o)}}function Kd(e,a){var o=pe(a.value),l=pe(a.defaultValue);o!=null&&(o=""+o,o!==e.value&&(e.value=o),a.defaultValue==null&&e.defaultValue!==o&&(e.defaultValue=o)),l!=null&&(e.defaultValue=""+l)}function _d(e){var a=e.textContent;a===e._wrapperState.initialValue&&a!==""&&a!==null&&(e.value=a)}function Gd(e){switch(e){case"svg":return"http://www.w3.org/2000/svg";case"math":return"http://www.w3.org/1998/Math/MathML";default:return"http://www.w3.org/1999/xhtml"}}function fr(e,a){return e==null||e==="http://www.w3.org/1999/xhtml"?Gd(a):e==="http://www.w3.org/2000/svg"&&a==="foreignObject"?"http://www.w3.org/1999/xhtml":e}var fo,Jd=(function(e){return typeof MSApp<"u"&&MSApp.execUnsafeLocalFunction?function(a,o,l,d){MSApp.execUnsafeLocalFunction(function(){return e(a,o,l,d)})}:e})(function(e,a){if(e.namespaceURI!=="http://www.w3.org/2000/svg"||"innerHTML"in e)e.innerHTML=a;else{for(fo=fo||document.createElement("div"),fo.innerHTML="<svg>"+a.valueOf().toString()+"</svg>",a=fo.firstChild;e.firstChild;)e.removeChild(e.firstChild);for(;a.firstChild;)e.appendChild(a.firstChild)}});function $i(e,a){if(a){var o=e.firstChild;if(o&&o===e.lastChild&&o.nodeType===3){o.nodeValue=a;return}}e.textContent=a}var Xi={animationIterationCount:!0,aspectRatio:!0,borderImageOutset:!0,borderImageSlice:!0,borderImageWidth:!0,boxFlex:!0,boxFlexGroup:!0,boxOrdinalGroup:!0,columnCount:!0,columns:!0,flex:!0,flexGrow:!0,flexPositive:!0,flexShrink:!0,flexNegative:!0,flexOrder:!0,gridArea:!0,gridRow:!0,gridRowEnd:!0,gridRowSpan:!0,gridRowStart:!0,gridColumn:!0,gridColumnEnd:!0,gridColumnSpan:!0,gridColumnStart:!0,fontWeight:!0,lineClamp:!0,lineHeight:!0,opacity:!0,order:!0,orphans:!0,tabSize:!0,widows:!0,zIndex:!0,zoom:!0,fillOpacity:!0,floodOpacity:!0,stopOpacity:!0,strokeDasharray:!0,strokeDashoffset:!0,strokeMiterlimit:!0,strokeOpacity:!0,strokeWidth:!0},dw=["Webkit","ms","Moz","O"];Object.keys(Xi).forEach(function(e){dw.forEach(function(a){a=a+e.charAt(0).toUpperCase()+e.substring(1),Xi[a]=Xi[e]})});function Yd(e,a,o){return a==null||typeof a=="boolean"||a===""?"":o||typeof a!="number"||a===0||Xi.hasOwnProperty(e)&&Xi[e]?(""+a).trim():a+"px"}function Qd(e,a){e=e.style;for(var o in a)if(a.hasOwnProperty(o)){var l=o.indexOf("--")===0,d=Yd(o,a[o],l);o==="float"&&(o="cssFloat"),l?e.setProperty(o,d):e[o]=d}}var hw=H({menuitem:!0},{area:!0,base:!0,br:!0,col:!0,embed:!0,hr:!0,img:!0,input:!0,keygen:!0,link:!0,meta:!0,param:!0,source:!0,track:!0,wbr:!0});function gr(e,a){if(a){if(hw[e]&&(a.children!=null||a.dangerouslySetInnerHTML!=null))throw Error(n(137,e));if(a.dangerouslySetInnerHTML!=null){if(a.children!=null)throw Error(n(60));if(typeof a.dangerouslySetInnerHTML!="object"||!("__html"in a.dangerouslySetInnerHTML))throw Error(n(61))}if(a.style!=null&&typeof a.style!="object")throw Error(n(62))}}function yr(e,a){if(e.indexOf("-")===-1)return typeof a.is=="string";switch(e){case"annotation-xml":case"color-profile":case"font-face":case"font-face-src":case"font-face-uri":case"font-face-format":case"font-face-name":case"missing-glyph":return!1;default:return!0}}var wr=null;function vr(e){return e=e.target||e.srcElement||window,e.correspondingUseElement&&(e=e.correspondingUseElement),e.nodeType===3?e.parentNode:e}var br=null,ri=null,li=null;function $d(e){if(e=kn(e)){if(typeof br!="function")throw Error(n(280));var a=e.stateNode;a&&(a=jo(a),br(e.stateNode,e.type,a))}}function Xd(e){ri?li?li.push(e):li=[e]:ri=e}function Zd(){if(ri){var e=ri,a=li;if(li=ri=null,$d(e),a)for(e=0;e<a.length;e++)$d(a[e])}}function eh(e,a){return e(a)}function th(){}var kr=!1;function ah(e,a,o){if(kr)return e(a,o);kr=!0;try{return eh(e,a,o)}finally{kr=!1,(ri!==null||li!==null)&&(th(),Zd())}}function Zi(e,a){var o=e.stateNode;if(o===null)return null;var l=jo(o);if(l===null)return null;o=l[a];e:switch(a){case"onClick":case"onClickCapture":case"onDoubleClick":case"onDoubleClickCapture":case"onMouseDown":case"onMouseDownCapture":case"onMouseMove":case"onMouseMoveCapture":case"onMouseUp":case"onMouseUpCapture":case"onMouseEnter":(l=!l.disabled)||(e=e.type,l=!(e==="button"||e==="input"||e==="select"||e==="textarea")),e=!l;break e;default:e=!1}if(e)return null;if(o&&typeof o!="function")throw Error(n(231,a,typeof o));return o}var xr=!1;if(p)try{var en={};Object.defineProperty(en,"passive",{get:function(){xr=!0}}),window.addEventListener("test",en,en),window.removeEventListener("test",en,en)}catch{xr=!1}function uw(e,a,o,l,d,u,w,k,S){var U=Array.prototype.slice.call(arguments,3);try{a.apply(o,U)}catch(j){this.onError(j)}}var tn=!1,go=null,yo=!1,Sr=null,pw={onError:function(e){tn=!0,go=e}};function mw(e,a,o,l,d,u,w,k,S){tn=!1,go=null,uw.apply(pw,arguments)}function fw(e,a,o,l,d,u,w,k,S){if(mw.apply(this,arguments),tn){if(tn){var U=go;tn=!1,go=null}else throw Error(n(198));yo||(yo=!0,Sr=U)}}function La(e){var a=e,o=e;if(e.alternate)for(;a.return;)a=a.return;else{e=a;do a=e,(a.flags&4098)!==0&&(o=a.return),e=a.return;while(e)}return a.tag===3?o:null}function ih(e){if(e.tag===13){var a=e.memoizedState;if(a===null&&(e=e.alternate,e!==null&&(a=e.memoizedState)),a!==null)return a.dehydrated}return null}function nh(e){if(La(e)!==e)throw Error(n(188))}function gw(e){var a=e.alternate;if(!a){if(a=La(e),a===null)throw Error(n(188));return a!==e?null:e}for(var o=e,l=a;;){var d=o.return;if(d===null)break;var u=d.alternate;if(u===null){if(l=d.return,l!==null){o=l;continue}break}if(d.child===u.child){for(u=d.child;u;){if(u===o)return nh(d),e;if(u===l)return nh(d),a;u=u.sibling}throw Error(n(188))}if(o.return!==l.return)o=d,l=u;else{for(var w=!1,k=d.child;k;){if(k===o){w=!0,o=d,l=u;break}if(k===l){w=!0,l=d,o=u;break}k=k.sibling}if(!w){for(k=u.child;k;){if(k===o){w=!0,o=u,l=d;break}if(k===l){w=!0,l=u,o=d;break}k=k.sibling}if(!w)throw Error(n(189))}}if(o.alternate!==l)throw Error(n(190))}if(o.tag!==3)throw Error(n(188));return o.stateNode.current===o?e:a}function oh(e){return e=gw(e),e!==null?sh(e):null}function sh(e){if(e.tag===5||e.tag===6)return e;for(e=e.child;e!==null;){var a=sh(e);if(a!==null)return a;e=e.sibling}return null}var rh=i.unstable_scheduleCallback,lh=i.unstable_cancelCallback,yw=i.unstable_shouldYield,ww=i.unstable_requestPaint,Oe=i.unstable_now,vw=i.unstable_getCurrentPriorityLevel,Ar=i.unstable_ImmediatePriority,ch=i.unstable_UserBlockingPriority,wo=i.unstable_NormalPriority,bw=i.unstable_LowPriority,dh=i.unstable_IdlePriority,vo=null,zt=null;function kw(e){if(zt&&typeof zt.onCommitFiberRoot=="function")try{zt.onCommitFiberRoot(vo,e,void 0,(e.current.flags&128)===128)}catch{}}var Et=Math.clz32?Math.clz32:Aw,xw=Math.log,Sw=Math.LN2;function Aw(e){return e>>>=0,e===0?32:31-(xw(e)/Sw|0)|0}var bo=64,ko=4194304;function an(e){switch(e&-e){case 1:return 1;case 2:return 2;case 4:return 4;case 8:return 8;case 16:return 16;case 32:return 32;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return e&4194240;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return e&130023424;case 134217728:return 134217728;case 268435456:return 268435456;case 536870912:return 536870912;case 1073741824:return 1073741824;default:return e}}function xo(e,a){var o=e.pendingLanes;if(o===0)return 0;var l=0,d=e.suspendedLanes,u=e.pingedLanes,w=o&268435455;if(w!==0){var k=w&~d;k!==0?l=an(k):(u&=w,u!==0&&(l=an(u)))}else w=o&~d,w!==0?l=an(w):u!==0&&(l=an(u));if(l===0)return 0;if(a!==0&&a!==l&&(a&d)===0&&(d=l&-l,u=a&-a,d>=u||d===16&&(u&4194240)!==0))return a;if((l&4)!==0&&(l|=o&16),a=e.entangledLanes,a!==0)for(e=e.entanglements,a&=l;0<a;)o=31-Et(a),d=1<<o,l|=e[o],a&=~d;return l}function Cw(e,a){switch(e){case 1:case 2:case 4:return a+250;case 8:case 16:case 32:case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return a+5e3;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return-1;case 134217728:case 268435456:case 536870912:case 1073741824:return-1;default:return-1}}function Tw(e,a){for(var o=e.suspendedLanes,l=e.pingedLanes,d=e.expirationTimes,u=e.pendingLanes;0<u;){var w=31-Et(u),k=1<<w,S=d[w];S===-1?((k&o)===0||(k&l)!==0)&&(d[w]=Cw(k,a)):S<=a&&(e.expiredLanes|=k),u&=~k}}function Cr(e){return e=e.pendingLanes&-1073741825,e!==0?e:e&1073741824?1073741824:0}function hh(){var e=bo;return bo<<=1,(bo&4194240)===0&&(bo=64),e}function Tr(e){for(var a=[],o=0;31>o;o++)a.push(e);return a}function nn(e,a,o){e.pendingLanes|=a,a!==536870912&&(e.suspendedLanes=0,e.pingedLanes=0),e=e.eventTimes,a=31-Et(a),e[a]=o}function Iw(e,a){var o=e.pendingLanes&~a;e.pendingLanes=a,e.suspendedLanes=0,e.pingedLanes=0,e.expiredLanes&=a,e.mutableReadLanes&=a,e.entangledLanes&=a,a=e.entanglements;var l=e.eventTimes;for(e=e.expirationTimes;0<o;){var d=31-Et(o),u=1<<d;a[d]=0,l[d]=-1,e[d]=-1,o&=~u}}function Ir(e,a){var o=e.entangledLanes|=a;for(e=e.entanglements;o;){var l=31-Et(o),d=1<<l;d&a|e[l]&a&&(e[l]|=a),o&=~d}}var ye=0;function uh(e){return e&=-e,1<e?4<e?(e&268435455)!==0?16:536870912:4:1}var ph,Mr,mh,fh,gh,Pr=!1,So=[],ua=null,pa=null,ma=null,on=new Map,sn=new Map,fa=[],Mw="mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset submit".split(" ");function yh(e,a){switch(e){case"focusin":case"focusout":ua=null;break;case"dragenter":case"dragleave":pa=null;break;case"mouseover":case"mouseout":ma=null;break;case"pointerover":case"pointerout":on.delete(a.pointerId);break;case"gotpointercapture":case"lostpointercapture":sn.delete(a.pointerId)}}function rn(e,a,o,l,d,u){return e===null||e.nativeEvent!==u?(e={blockedOn:a,domEventName:o,eventSystemFlags:l,nativeEvent:u,targetContainers:[d]},a!==null&&(a=kn(a),a!==null&&Mr(a)),e):(e.eventSystemFlags|=l,a=e.targetContainers,d!==null&&a.indexOf(d)===-1&&a.push(d),e)}function Pw(e,a,o,l,d){switch(a){case"focusin":return ua=rn(ua,e,a,o,l,d),!0;case"dragenter":return pa=rn(pa,e,a,o,l,d),!0;case"mouseover":return ma=rn(ma,e,a,o,l,d),!0;case"pointerover":var u=d.pointerId;return on.set(u,rn(on.get(u)||null,e,a,o,l,d)),!0;case"gotpointercapture":return u=d.pointerId,sn.set(u,rn(sn.get(u)||null,e,a,o,l,d)),!0}return!1}function wh(e){var a=ja(e.target);if(a!==null){var o=La(a);if(o!==null){if(a=o.tag,a===13){if(a=ih(o),a!==null){e.blockedOn=a,gh(e.priority,function(){mh(o)});return}}else if(a===3&&o.stateNode.current.memoizedState.isDehydrated){e.blockedOn=o.tag===3?o.stateNode.containerInfo:null;return}}}e.blockedOn=null}function Ao(e){if(e.blockedOn!==null)return!1;for(var a=e.targetContainers;0<a.length;){var o=Rr(e.domEventName,e.eventSystemFlags,a[0],e.nativeEvent);if(o===null){o=e.nativeEvent;var l=new o.constructor(o.type,o);wr=l,o.target.dispatchEvent(l),wr=null}else return a=kn(o),a!==null&&Mr(a),e.blockedOn=o,!1;a.shift()}return!0}function vh(e,a,o){Ao(e)&&o.delete(a)}function Dw(){Pr=!1,ua!==null&&Ao(ua)&&(ua=null),pa!==null&&Ao(pa)&&(pa=null),ma!==null&&Ao(ma)&&(ma=null),on.forEach(vh),sn.forEach(vh)}function ln(e,a){e.blockedOn===a&&(e.blockedOn=null,Pr||(Pr=!0,i.unstable_scheduleCallback(i.unstable_NormalPriority,Dw)))}function cn(e){function a(d){return ln(d,e)}if(0<So.length){ln(So[0],e);for(var o=1;o<So.length;o++){var l=So[o];l.blockedOn===e&&(l.blockedOn=null)}}for(ua!==null&&ln(ua,e),pa!==null&&ln(pa,e),ma!==null&&ln(ma,e),on.forEach(a),sn.forEach(a),o=0;o<fa.length;o++)l=fa[o],l.blockedOn===e&&(l.blockedOn=null);for(;0<fa.length&&(o=fa[0],o.blockedOn===null);)wh(o),o.blockedOn===null&&fa.shift()}var ci=z.ReactCurrentBatchConfig,Co=!0;function Rw(e,a,o,l){var d=ye,u=ci.transition;ci.transition=null;try{ye=1,Dr(e,a,o,l)}finally{ye=d,ci.transition=u}}function Ew(e,a,o,l){var d=ye,u=ci.transition;ci.transition=null;try{ye=4,Dr(e,a,o,l)}finally{ye=d,ci.transition=u}}function Dr(e,a,o,l){if(Co){var d=Rr(e,a,o,l);if(d===null)Jr(e,a,l,To,o),yh(e,l);else if(Pw(d,e,a,o,l))l.stopPropagation();else if(yh(e,l),a&4&&-1<Mw.indexOf(e)){for(;d!==null;){var u=kn(d);if(u!==null&&ph(u),u=Rr(e,a,o,l),u===null&&Jr(e,a,l,To,o),u===d)break;d=u}d!==null&&l.stopPropagation()}else Jr(e,a,l,null,o)}}var To=null;function Rr(e,a,o,l){if(To=null,e=vr(l),e=ja(e),e!==null)if(a=La(e),a===null)e=null;else if(o=a.tag,o===13){if(e=ih(a),e!==null)return e;e=null}else if(o===3){if(a.stateNode.current.memoizedState.isDehydrated)return a.tag===3?a.stateNode.containerInfo:null;e=null}else a!==e&&(e=null);return To=e,null}function bh(e){switch(e){case"cancel":case"click":case"close":case"contextmenu":case"copy":case"cut":case"auxclick":case"dblclick":case"dragend":case"dragstart":case"drop":case"focusin":case"focusout":case"input":case"invalid":case"keydown":case"keypress":case"keyup":case"mousedown":case"mouseup":case"paste":case"pause":case"play":case"pointercancel":case"pointerdown":case"pointerup":case"ratechange":case"reset":case"resize":case"seeked":case"submit":case"touchcancel":case"touchend":case"touchstart":case"volumechange":case"change":case"selectionchange":case"textInput":case"compositionstart":case"compositionend":case"compositionupdate":case"beforeblur":case"afterblur":case"beforeinput":case"blur":case"fullscreenchange":case"focus":case"hashchange":case"popstate":case"select":case"selectstart":return 1;case"drag":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"mousemove":case"mouseout":case"mouseover":case"pointermove":case"pointerout":case"pointerover":case"scroll":case"toggle":case"touchmove":case"wheel":case"mouseenter":case"mouseleave":case"pointerenter":case"pointerleave":return 4;case"message":switch(vw()){case Ar:return 1;case ch:return 4;case wo:case bw:return 16;case dh:return 536870912;default:return 16}default:return 16}}var ga=null,Er=null,Io=null;function kh(){if(Io)return Io;var e,a=Er,o=a.length,l,d="value"in ga?ga.value:ga.textContent,u=d.length;for(e=0;e<o&&a[e]===d[e];e++);var w=o-e;for(l=1;l<=w&&a[o-l]===d[u-l];l++);return Io=d.slice(e,1<l?1-l:void 0)}function Mo(e){var a=e.keyCode;return"charCode"in e?(e=e.charCode,e===0&&a===13&&(e=13)):e=a,e===10&&(e=13),32<=e||e===13?e:0}function Po(){return!0}function xh(){return!1}function gt(e){function a(o,l,d,u,w){this._reactName=o,this._targetInst=d,this.type=l,this.nativeEvent=u,this.target=w,this.currentTarget=null;for(var k in e)e.hasOwnProperty(k)&&(o=e[k],this[k]=o?o(u):u[k]);return this.isDefaultPrevented=(u.defaultPrevented!=null?u.defaultPrevented:u.returnValue===!1)?Po:xh,this.isPropagationStopped=xh,this}return H(a.prototype,{preventDefault:function(){this.defaultPrevented=!0;var o=this.nativeEvent;o&&(o.preventDefault?o.preventDefault():typeof o.returnValue!="unknown"&&(o.returnValue=!1),this.isDefaultPrevented=Po)},stopPropagation:function(){var o=this.nativeEvent;o&&(o.stopPropagation?o.stopPropagation():typeof o.cancelBubble!="unknown"&&(o.cancelBubble=!0),this.isPropagationStopped=Po)},persist:function(){},isPersistent:Po}),a}var di={eventPhase:0,bubbles:0,cancelable:0,timeStamp:function(e){return e.timeStamp||Date.now()},defaultPrevented:0,isTrusted:0},Ur=gt(di),dn=H({},di,{view:0,detail:0}),Uw=gt(dn),qr,Or,hn,Do=H({},dn,{screenX:0,screenY:0,clientX:0,clientY:0,pageX:0,pageY:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,getModifierState:Lr,button:0,buttons:0,relatedTarget:function(e){return e.relatedTarget===void 0?e.fromElement===e.srcElement?e.toElement:e.fromElement:e.relatedTarget},movementX:function(e){return"movementX"in e?e.movementX:(e!==hn&&(hn&&e.type==="mousemove"?(qr=e.screenX-hn.screenX,Or=e.screenY-hn.screenY):Or=qr=0,hn=e),qr)},movementY:function(e){return"movementY"in e?e.movementY:Or}}),Sh=gt(Do),qw=H({},Do,{dataTransfer:0}),Ow=gt(qw),Fw=H({},dn,{relatedTarget:0}),Fr=gt(Fw),Lw=H({},di,{animationName:0,elapsedTime:0,pseudoElement:0}),jw=gt(Lw),Vw=H({},di,{clipboardData:function(e){return"clipboardData"in e?e.clipboardData:window.clipboardData}}),Nw=gt(Vw),Ww=H({},di,{data:0}),Ah=gt(Ww),Bw={Esc:"Escape",Spacebar:" ",Left:"ArrowLeft",Up:"ArrowUp",Right:"ArrowRight",Down:"ArrowDown",Del:"Delete",Win:"OS",Menu:"ContextMenu",Apps:"ContextMenu",Scroll:"ScrollLock",MozPrintableKey:"Unidentified"},zw={8:"Backspace",9:"Tab",12:"Clear",13:"Enter",16:"Shift",17:"Control",18:"Alt",19:"Pause",20:"CapsLock",27:"Escape",32:" ",33:"PageUp",34:"PageDown",35:"End",36:"Home",37:"ArrowLeft",38:"ArrowUp",39:"ArrowRight",40:"ArrowDown",45:"Insert",46:"Delete",112:"F1",113:"F2",114:"F3",115:"F4",116:"F5",117:"F6",118:"F7",119:"F8",120:"F9",121:"F10",122:"F11",123:"F12",144:"NumLock",145:"ScrollLock",224:"Meta"},Hw={Alt:"altKey",Control:"ctrlKey",Meta:"metaKey",Shift:"shiftKey"};function Kw(e){var a=this.nativeEvent;return a.getModifierState?a.getModifierState(e):(e=Hw[e])?!!a[e]:!1}function Lr(){return Kw}var _w=H({},dn,{key:function(e){if(e.key){var a=Bw[e.key]||e.key;if(a!=="Unidentified")return a}return e.type==="keypress"?(e=Mo(e),e===13?"Enter":String.fromCharCode(e)):e.type==="keydown"||e.type==="keyup"?zw[e.keyCode]||"Unidentified":""},code:0,location:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,repeat:0,locale:0,getModifierState:Lr,charCode:function(e){return e.type==="keypress"?Mo(e):0},keyCode:function(e){return e.type==="keydown"||e.type==="keyup"?e.keyCode:0},which:function(e){return e.type==="keypress"?Mo(e):e.type==="keydown"||e.type==="keyup"?e.keyCode:0}}),Gw=gt(_w),Jw=H({},Do,{pointerId:0,width:0,height:0,pressure:0,tangentialPressure:0,tiltX:0,tiltY:0,twist:0,pointerType:0,isPrimary:0}),Ch=gt(Jw),Yw=H({},dn,{touches:0,targetTouches:0,changedTouches:0,altKey:0,metaKey:0,ctrlKey:0,shiftKey:0,getModifierState:Lr}),Qw=gt(Yw),$w=H({},di,{propertyName:0,elapsedTime:0,pseudoElement:0}),Xw=gt($w),Zw=H({},Do,{deltaX:function(e){return"deltaX"in e?e.deltaX:"wheelDeltaX"in e?-e.wheelDeltaX:0},deltaY:function(e){return"deltaY"in e?e.deltaY:"wheelDeltaY"in e?-e.wheelDeltaY:"wheelDelta"in e?-e.wheelDelta:0},deltaZ:0,deltaMode:0}),ev=gt(Zw),tv=[9,13,27,32],jr=p&&"CompositionEvent"in window,un=null;p&&"documentMode"in document&&(un=document.documentMode);var av=p&&"TextEvent"in window&&!un,Th=p&&(!jr||un&&8<un&&11>=un),Ih=" ",Mh=!1;function Ph(e,a){switch(e){case"keyup":return tv.indexOf(a.keyCode)!==-1;case"keydown":return a.keyCode!==229;case"keypress":case"mousedown":case"focusout":return!0;default:return!1}}function Dh(e){return e=e.detail,typeof e=="object"&&"data"in e?e.data:null}var hi=!1;function iv(e,a){switch(e){case"compositionend":return Dh(a);case"keypress":return a.which!==32?null:(Mh=!0,Ih);case"textInput":return e=a.data,e===Ih&&Mh?null:e;default:return null}}function nv(e,a){if(hi)return e==="compositionend"||!jr&&Ph(e,a)?(e=kh(),Io=Er=ga=null,hi=!1,e):null;switch(e){case"paste":return null;case"keypress":if(!(a.ctrlKey||a.altKey||a.metaKey)||a.ctrlKey&&a.altKey){if(a.char&&1<a.char.length)return a.char;if(a.which)return String.fromCharCode(a.which)}return null;case"compositionend":return Th&&a.locale!=="ko"?null:a.data;default:return null}}var ov={color:!0,date:!0,datetime:!0,"datetime-local":!0,email:!0,month:!0,number:!0,password:!0,range:!0,search:!0,tel:!0,text:!0,time:!0,url:!0,week:!0};function Rh(e){var a=e&&e.nodeName&&e.nodeName.toLowerCase();return a==="input"?!!ov[e.type]:a==="textarea"}function Eh(e,a,o,l){Xd(l),a=Oo(a,"onChange"),0<a.length&&(o=new Ur("onChange","change",null,o,l),e.push({event:o,listeners:a}))}var pn=null,mn=null;function sv(e){Qh(e,0)}function Ro(e){var a=gi(e);if(nt(a))return e}function rv(e,a){if(e==="change")return a}var Uh=!1;if(p){var Vr;if(p){var Nr="oninput"in document;if(!Nr){var qh=document.createElement("div");qh.setAttribute("oninput","return;"),Nr=typeof qh.oninput=="function"}Vr=Nr}else Vr=!1;Uh=Vr&&(!document.documentMode||9<document.documentMode)}function Oh(){pn&&(pn.detachEvent("onpropertychange",Fh),mn=pn=null)}function Fh(e){if(e.propertyName==="value"&&Ro(mn)){var a=[];Eh(a,mn,e,vr(e)),ah(sv,a)}}function lv(e,a,o){e==="focusin"?(Oh(),pn=a,mn=o,pn.attachEvent("onpropertychange",Fh)):e==="focusout"&&Oh()}function cv(e){if(e==="selectionchange"||e==="keyup"||e==="keydown")return Ro(mn)}function dv(e,a){if(e==="click")return Ro(a)}function hv(e,a){if(e==="input"||e==="change")return Ro(a)}function uv(e,a){return e===a&&(e!==0||1/e===1/a)||e!==e&&a!==a}var Ut=typeof Object.is=="function"?Object.is:uv;function fn(e,a){if(Ut(e,a))return!0;if(typeof e!="object"||e===null||typeof a!="object"||a===null)return!1;var o=Object.keys(e),l=Object.keys(a);if(o.length!==l.length)return!1;for(l=0;l<o.length;l++){var d=o[l];if(!m.call(a,d)||!Ut(e[d],a[d]))return!1}return!0}function Lh(e){for(;e&&e.firstChild;)e=e.firstChild;return e}function jh(e,a){var o=Lh(e);e=0;for(var l;o;){if(o.nodeType===3){if(l=e+o.textContent.length,e<=a&&l>=a)return{node:o,offset:a-e};e=l}e:{for(;o;){if(o.nextSibling){o=o.nextSibling;break e}o=o.parentNode}o=void 0}o=Lh(o)}}function Vh(e,a){return e&&a?e===a?!0:e&&e.nodeType===3?!1:a&&a.nodeType===3?Vh(e,a.parentNode):"contains"in e?e.contains(a):e.compareDocumentPosition?!!(e.compareDocumentPosition(a)&16):!1:!1}function Nh(){for(var e=window,a=Rt();a instanceof e.HTMLIFrameElement;){try{var o=typeof a.contentWindow.location.href=="string"}catch{o=!1}if(o)e=a.contentWindow;else break;a=Rt(e.document)}return a}function Wr(e){var a=e&&e.nodeName&&e.nodeName.toLowerCase();return a&&(a==="input"&&(e.type==="text"||e.type==="search"||e.type==="tel"||e.type==="url"||e.type==="password")||a==="textarea"||e.contentEditable==="true")}function pv(e){var a=Nh(),o=e.focusedElem,l=e.selectionRange;if(a!==o&&o&&o.ownerDocument&&Vh(o.ownerDocument.documentElement,o)){if(l!==null&&Wr(o)){if(a=l.start,e=l.end,e===void 0&&(e=a),"selectionStart"in o)o.selectionStart=a,o.selectionEnd=Math.min(e,o.value.length);else if(e=(a=o.ownerDocument||document)&&a.defaultView||window,e.getSelection){e=e.getSelection();var d=o.textContent.length,u=Math.min(l.start,d);l=l.end===void 0?u:Math.min(l.end,d),!e.extend&&u>l&&(d=l,l=u,u=d),d=jh(o,u);var w=jh(o,l);d&&w&&(e.rangeCount!==1||e.anchorNode!==d.node||e.anchorOffset!==d.offset||e.focusNode!==w.node||e.focusOffset!==w.offset)&&(a=a.createRange(),a.setStart(d.node,d.offset),e.removeAllRanges(),u>l?(e.addRange(a),e.extend(w.node,w.offset)):(a.setEnd(w.node,w.offset),e.addRange(a)))}}for(a=[],e=o;e=e.parentNode;)e.nodeType===1&&a.push({element:e,left:e.scrollLeft,top:e.scrollTop});for(typeof o.focus=="function"&&o.focus(),o=0;o<a.length;o++)e=a[o],e.element.scrollLeft=e.left,e.element.scrollTop=e.top}}var mv=p&&"documentMode"in document&&11>=document.documentMode,ui=null,Br=null,gn=null,zr=!1;function Wh(e,a,o){var l=o.window===o?o.document:o.nodeType===9?o:o.ownerDocument;zr||ui==null||ui!==Rt(l)||(l=ui,"selectionStart"in l&&Wr(l)?l={start:l.selectionStart,end:l.selectionEnd}:(l=(l.ownerDocument&&l.ownerDocument.defaultView||window).getSelection(),l={anchorNode:l.anchorNode,anchorOffset:l.anchorOffset,focusNode:l.focusNode,focusOffset:l.focusOffset}),gn&&fn(gn,l)||(gn=l,l=Oo(Br,"onSelect"),0<l.length&&(a=new Ur("onSelect","select",null,a,o),e.push({event:a,listeners:l}),a.target=ui)))}function Eo(e,a){var o={};return o[e.toLowerCase()]=a.toLowerCase(),o["Webkit"+e]="webkit"+a,o["Moz"+e]="moz"+a,o}var pi={animationend:Eo("Animation","AnimationEnd"),animationiteration:Eo("Animation","AnimationIteration"),animationstart:Eo("Animation","AnimationStart"),transitionend:Eo("Transition","TransitionEnd")},Hr={},Bh={};p&&(Bh=document.createElement("div").style,"AnimationEvent"in window||(delete pi.animationend.animation,delete pi.animationiteration.animation,delete pi.animationstart.animation),"TransitionEvent"in window||delete pi.transitionend.transition);function Uo(e){if(Hr[e])return Hr[e];if(!pi[e])return e;var a=pi[e],o;for(o in a)if(a.hasOwnProperty(o)&&o in Bh)return Hr[e]=a[o];return e}var zh=Uo("animationend"),Hh=Uo("animationiteration"),Kh=Uo("animationstart"),_h=Uo("transitionend"),Gh=new Map,Jh="abort auxClick cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(" ");function ya(e,a){Gh.set(e,a),c(a,[e])}for(var Kr=0;Kr<Jh.length;Kr++){var _r=Jh[Kr],fv=_r.toLowerCase(),gv=_r[0].toUpperCase()+_r.slice(1);ya(fv,"on"+gv)}ya(zh,"onAnimationEnd"),ya(Hh,"onAnimationIteration"),ya(Kh,"onAnimationStart"),ya("dblclick","onDoubleClick"),ya("focusin","onFocus"),ya("focusout","onBlur"),ya(_h,"onTransitionEnd"),h("onMouseEnter",["mouseout","mouseover"]),h("onMouseLeave",["mouseout","mouseover"]),h("onPointerEnter",["pointerout","pointerover"]),h("onPointerLeave",["pointerout","pointerover"]),c("onChange","change click focusin focusout input keydown keyup selectionchange".split(" ")),c("onSelect","focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(" ")),c("onBeforeInput",["compositionend","keypress","textInput","paste"]),c("onCompositionEnd","compositionend focusout keydown keypress keyup mousedown".split(" ")),c("onCompositionStart","compositionstart focusout keydown keypress keyup mousedown".split(" ")),c("onCompositionUpdate","compositionupdate focusout keydown keypress keyup mousedown".split(" "));var yn="abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(" "),yv=new Set("cancel close invalid load scroll toggle".split(" ").concat(yn));function Yh(e,a,o){var l=e.type||"unknown-event";e.currentTarget=o,fw(l,a,void 0,e),e.currentTarget=null}function Qh(e,a){a=(a&4)!==0;for(var o=0;o<e.length;o++){var l=e[o],d=l.event;l=l.listeners;e:{var u=void 0;if(a)for(var w=l.length-1;0<=w;w--){var k=l[w],S=k.instance,U=k.currentTarget;if(k=k.listener,S!==u&&d.isPropagationStopped())break e;Yh(d,k,U),u=S}else for(w=0;w<l.length;w++){if(k=l[w],S=k.instance,U=k.currentTarget,k=k.listener,S!==u&&d.isPropagationStopped())break e;Yh(d,k,U),u=S}}}if(yo)throw e=Sr,yo=!1,Sr=null,e}function xe(e,a){var o=a[el];o===void 0&&(o=a[el]=new Set);var l=e+"__bubble";o.has(l)||($h(a,e,2,!1),o.add(l))}function Gr(e,a,o){var l=0;a&&(l|=4),$h(o,e,l,a)}var qo="_reactListening"+Math.random().toString(36).slice(2);function wn(e){if(!e[qo]){e[qo]=!0,s.forEach(function(o){o!=="selectionchange"&&(yv.has(o)||Gr(o,!1,e),Gr(o,!0,e))});var a=e.nodeType===9?e:e.ownerDocument;a===null||a[qo]||(a[qo]=!0,Gr("selectionchange",!1,a))}}function $h(e,a,o,l){switch(bh(a)){case 1:var d=Rw;break;case 4:d=Ew;break;default:d=Dr}o=d.bind(null,a,o,e),d=void 0,!xr||a!=="touchstart"&&a!=="touchmove"&&a!=="wheel"||(d=!0),l?d!==void 0?e.addEventListener(a,o,{capture:!0,passive:d}):e.addEventListener(a,o,!0):d!==void 0?e.addEventListener(a,o,{passive:d}):e.addEventListener(a,o,!1)}function Jr(e,a,o,l,d){var u=l;if((a&1)===0&&(a&2)===0&&l!==null)e:for(;;){if(l===null)return;var w=l.tag;if(w===3||w===4){var k=l.stateNode.containerInfo;if(k===d||k.nodeType===8&&k.parentNode===d)break;if(w===4)for(w=l.return;w!==null;){var S=w.tag;if((S===3||S===4)&&(S=w.stateNode.containerInfo,S===d||S.nodeType===8&&S.parentNode===d))return;w=w.return}for(;k!==null;){if(w=ja(k),w===null)return;if(S=w.tag,S===5||S===6){l=u=w;continue e}k=k.parentNode}}l=l.return}ah(function(){var U=u,j=vr(o),N=[];e:{var L=Gh.get(e);if(L!==void 0){var _=Ur,Y=e;switch(e){case"keypress":if(Mo(o)===0)break e;case"keydown":case"keyup":_=Gw;break;case"focusin":Y="focus",_=Fr;break;case"focusout":Y="blur",_=Fr;break;case"beforeblur":case"afterblur":_=Fr;break;case"click":if(o.button===2)break e;case"auxclick":case"dblclick":case"mousedown":case"mousemove":case"mouseup":case"mouseout":case"mouseover":case"contextmenu":_=Sh;break;case"drag":case"dragend":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"dragstart":case"drop":_=Ow;break;case"touchcancel":case"touchend":case"touchmove":case"touchstart":_=Qw;break;case zh:case Hh:case Kh:_=jw;break;case _h:_=Xw;break;case"scroll":_=Uw;break;case"wheel":_=ev;break;case"copy":case"cut":case"paste":_=Nw;break;case"gotpointercapture":case"lostpointercapture":case"pointercancel":case"pointerdown":case"pointermove":case"pointerout":case"pointerover":case"pointerup":_=Ch}var Q=(a&4)!==0,Fe=!Q&&e==="scroll",D=Q?L!==null?L+"Capture":null:L;Q=[];for(var C=U,E;C!==null;){E=C;var B=E.stateNode;if(E.tag===5&&B!==null&&(E=B,D!==null&&(B=Zi(C,D),B!=null&&Q.push(vn(C,B,E)))),Fe)break;C=C.return}0<Q.length&&(L=new _(L,Y,null,o,j),N.push({event:L,listeners:Q}))}}if((a&7)===0){e:{if(L=e==="mouseover"||e==="pointerover",_=e==="mouseout"||e==="pointerout",L&&o!==wr&&(Y=o.relatedTarget||o.fromElement)&&(ja(Y)||Y[ta]))break e;if((_||L)&&(L=j.window===j?j:(L=j.ownerDocument)?L.defaultView||L.parentWindow:window,_?(Y=o.relatedTarget||o.toElement,_=U,Y=Y?ja(Y):null,Y!==null&&(Fe=La(Y),Y!==Fe||Y.tag!==5&&Y.tag!==6)&&(Y=null)):(_=null,Y=U),_!==Y)){if(Q=Sh,B="onMouseLeave",D="onMouseEnter",C="mouse",(e==="pointerout"||e==="pointerover")&&(Q=Ch,B="onPointerLeave",D="onPointerEnter",C="pointer"),Fe=_==null?L:gi(_),E=Y==null?L:gi(Y),L=new Q(B,C+"leave",_,o,j),L.target=Fe,L.relatedTarget=E,B=null,ja(j)===U&&(Q=new Q(D,C+"enter",Y,o,j),Q.target=E,Q.relatedTarget=Fe,B=Q),Fe=B,_&&Y)t:{for(Q=_,D=Y,C=0,E=Q;E;E=mi(E))C++;for(E=0,B=D;B;B=mi(B))E++;for(;0<C-E;)Q=mi(Q),C--;for(;0<E-C;)D=mi(D),E--;for(;C--;){if(Q===D||D!==null&&Q===D.alternate)break t;Q=mi(Q),D=mi(D)}Q=null}else Q=null;_!==null&&Xh(N,L,_,Q,!1),Y!==null&&Fe!==null&&Xh(N,Fe,Y,Q,!0)}}e:{if(L=U?gi(U):window,_=L.nodeName&&L.nodeName.toLowerCase(),_==="select"||_==="input"&&L.type==="file")var $=rv;else if(Rh(L))if(Uh)$=hv;else{$=cv;var ee=lv}else(_=L.nodeName)&&_.toLowerCase()==="input"&&(L.type==="checkbox"||L.type==="radio")&&($=dv);if($&&($=$(e,U))){Eh(N,$,o,j);break e}ee&&ee(e,L,U),e==="focusout"&&(ee=L._wrapperState)&&ee.controlled&&L.type==="number"&&pr(L,"number",L.value)}switch(ee=U?gi(U):window,e){case"focusin":(Rh(ee)||ee.contentEditable==="true")&&(ui=ee,Br=U,gn=null);break;case"focusout":gn=Br=ui=null;break;case"mousedown":zr=!0;break;case"contextmenu":case"mouseup":case"dragend":zr=!1,Wh(N,o,j);break;case"selectionchange":if(mv)break;case"keydown":case"keyup":Wh(N,o,j)}var te;if(jr)e:{switch(e){case"compositionstart":var ne="onCompositionStart";break e;case"compositionend":ne="onCompositionEnd";break e;case"compositionupdate":ne="onCompositionUpdate";break e}ne=void 0}else hi?Ph(e,o)&&(ne="onCompositionEnd"):e==="keydown"&&o.keyCode===229&&(ne="onCompositionStart");ne&&(Th&&o.locale!=="ko"&&(hi||ne!=="onCompositionStart"?ne==="onCompositionEnd"&&hi&&(te=kh()):(ga=j,Er="value"in ga?ga.value:ga.textContent,hi=!0)),ee=Oo(U,ne),0<ee.length&&(ne=new Ah(ne,e,null,o,j),N.push({event:ne,listeners:ee}),te?ne.data=te:(te=Dh(o),te!==null&&(ne.data=te)))),(te=av?iv(e,o):nv(e,o))&&(U=Oo(U,"onBeforeInput"),0<U.length&&(j=new Ah("onBeforeInput","beforeinput",null,o,j),N.push({event:j,listeners:U}),j.data=te))}Qh(N,a)})}function vn(e,a,o){return{instance:e,listener:a,currentTarget:o}}function Oo(e,a){for(var o=a+"Capture",l=[];e!==null;){var d=e,u=d.stateNode;d.tag===5&&u!==null&&(d=u,u=Zi(e,o),u!=null&&l.unshift(vn(e,u,d)),u=Zi(e,a),u!=null&&l.push(vn(e,u,d))),e=e.return}return l}function mi(e){if(e===null)return null;do e=e.return;while(e&&e.tag!==5);return e||null}function Xh(e,a,o,l,d){for(var u=a._reactName,w=[];o!==null&&o!==l;){var k=o,S=k.alternate,U=k.stateNode;if(S!==null&&S===l)break;k.tag===5&&U!==null&&(k=U,d?(S=Zi(o,u),S!=null&&w.unshift(vn(o,S,k))):d||(S=Zi(o,u),S!=null&&w.push(vn(o,S,k)))),o=o.return}w.length!==0&&e.push({event:a,listeners:w})}var wv=/\r\n?/g,vv=/\u0000|\uFFFD/g;function Zh(e){return(typeof e=="string"?e:""+e).replace(wv,`
`).replace(vv,"")}function Fo(e,a,o){if(a=Zh(a),Zh(e)!==a&&o)throw Error(n(425))}function Lo(){}var Yr=null,Qr=null;function $r(e,a){return e==="textarea"||e==="noscript"||typeof a.children=="string"||typeof a.children=="number"||typeof a.dangerouslySetInnerHTML=="object"&&a.dangerouslySetInnerHTML!==null&&a.dangerouslySetInnerHTML.__html!=null}var Xr=typeof setTimeout=="function"?setTimeout:void 0,bv=typeof clearTimeout=="function"?clearTimeout:void 0,eu=typeof Promise=="function"?Promise:void 0,kv=typeof queueMicrotask=="function"?queueMicrotask:typeof eu<"u"?function(e){return eu.resolve(null).then(e).catch(xv)}:Xr;function xv(e){setTimeout(function(){throw e})}function Zr(e,a){var o=a,l=0;do{var d=o.nextSibling;if(e.removeChild(o),d&&d.nodeType===8)if(o=d.data,o==="/$"){if(l===0){e.removeChild(d),cn(a);return}l--}else o!=="$"&&o!=="$?"&&o!=="$!"||l++;o=d}while(o);cn(a)}function wa(e){for(;e!=null;e=e.nextSibling){var a=e.nodeType;if(a===1||a===3)break;if(a===8){if(a=e.data,a==="$"||a==="$!"||a==="$?")break;if(a==="/$")return null}}return e}function tu(e){e=e.previousSibling;for(var a=0;e;){if(e.nodeType===8){var o=e.data;if(o==="$"||o==="$!"||o==="$?"){if(a===0)return e;a--}else o==="/$"&&a++}e=e.previousSibling}return null}var fi=Math.random().toString(36).slice(2),Ht="__reactFiber$"+fi,bn="__reactProps$"+fi,ta="__reactContainer$"+fi,el="__reactEvents$"+fi,Sv="__reactListeners$"+fi,Av="__reactHandles$"+fi;function ja(e){var a=e[Ht];if(a)return a;for(var o=e.parentNode;o;){if(a=o[ta]||o[Ht]){if(o=a.alternate,a.child!==null||o!==null&&o.child!==null)for(e=tu(e);e!==null;){if(o=e[Ht])return o;e=tu(e)}return a}e=o,o=e.parentNode}return null}function kn(e){return e=e[Ht]||e[ta],!e||e.tag!==5&&e.tag!==6&&e.tag!==13&&e.tag!==3?null:e}function gi(e){if(e.tag===5||e.tag===6)return e.stateNode;throw Error(n(33))}function jo(e){return e[bn]||null}var tl=[],yi=-1;function va(e){return{current:e}}function Se(e){0>yi||(e.current=tl[yi],tl[yi]=null,yi--)}function ke(e,a){yi++,tl[yi]=e.current,e.current=a}var ba={},Ze=va(ba),lt=va(!1),Va=ba;function wi(e,a){var o=e.type.contextTypes;if(!o)return ba;var l=e.stateNode;if(l&&l.__reactInternalMemoizedUnmaskedChildContext===a)return l.__reactInternalMemoizedMaskedChildContext;var d={},u;for(u in o)d[u]=a[u];return l&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=a,e.__reactInternalMemoizedMaskedChildContext=d),d}function ct(e){return e=e.childContextTypes,e!=null}function Vo(){Se(lt),Se(Ze)}function au(e,a,o){if(Ze.current!==ba)throw Error(n(168));ke(Ze,a),ke(lt,o)}function iu(e,a,o){var l=e.stateNode;if(a=a.childContextTypes,typeof l.getChildContext!="function")return o;l=l.getChildContext();for(var d in l)if(!(d in a))throw Error(n(108,ge(e)||"Unknown",d));return H({},o,l)}function No(e){return e=(e=e.stateNode)&&e.__reactInternalMemoizedMergedChildContext||ba,Va=Ze.current,ke(Ze,e),ke(lt,lt.current),!0}function nu(e,a,o){var l=e.stateNode;if(!l)throw Error(n(169));o?(e=iu(e,a,Va),l.__reactInternalMemoizedMergedChildContext=e,Se(lt),Se(Ze),ke(Ze,e)):Se(lt),ke(lt,o)}var aa=null,Wo=!1,al=!1;function ou(e){aa===null?aa=[e]:aa.push(e)}function Cv(e){Wo=!0,ou(e)}function ka(){if(!al&&aa!==null){al=!0;var e=0,a=ye;try{var o=aa;for(ye=1;e<o.length;e++){var l=o[e];do l=l(!0);while(l!==null)}aa=null,Wo=!1}catch(d){throw aa!==null&&(aa=aa.slice(e+1)),rh(Ar,ka),d}finally{ye=a,al=!1}}return null}var vi=[],bi=0,Bo=null,zo=0,xt=[],St=0,Na=null,ia=1,na="";function Wa(e,a){vi[bi++]=zo,vi[bi++]=Bo,Bo=e,zo=a}function su(e,a,o){xt[St++]=ia,xt[St++]=na,xt[St++]=Na,Na=e;var l=ia;e=na;var d=32-Et(l)-1;l&=~(1<<d),o+=1;var u=32-Et(a)+d;if(30<u){var w=d-d%5;u=(l&(1<<w)-1).toString(32),l>>=w,d-=w,ia=1<<32-Et(a)+d|o<<d|l,na=u+e}else ia=1<<u|o<<d|l,na=e}function il(e){e.return!==null&&(Wa(e,1),su(e,1,0))}function nl(e){for(;e===Bo;)Bo=vi[--bi],vi[bi]=null,zo=vi[--bi],vi[bi]=null;for(;e===Na;)Na=xt[--St],xt[St]=null,na=xt[--St],xt[St]=null,ia=xt[--St],xt[St]=null}var yt=null,wt=null,Ae=!1,qt=null;function ru(e,a){var o=It(5,null,null,0);o.elementType="DELETED",o.stateNode=a,o.return=e,a=e.deletions,a===null?(e.deletions=[o],e.flags|=16):a.push(o)}function lu(e,a){switch(e.tag){case 5:var o=e.type;return a=a.nodeType!==1||o.toLowerCase()!==a.nodeName.toLowerCase()?null:a,a!==null?(e.stateNode=a,yt=e,wt=wa(a.firstChild),!0):!1;case 6:return a=e.pendingProps===""||a.nodeType!==3?null:a,a!==null?(e.stateNode=a,yt=e,wt=null,!0):!1;case 13:return a=a.nodeType!==8?null:a,a!==null?(o=Na!==null?{id:ia,overflow:na}:null,e.memoizedState={dehydrated:a,treeContext:o,retryLane:1073741824},o=It(18,null,null,0),o.stateNode=a,o.return=e,e.child=o,yt=e,wt=null,!0):!1;default:return!1}}function ol(e){return(e.mode&1)!==0&&(e.flags&128)===0}function sl(e){if(Ae){var a=wt;if(a){var o=a;if(!lu(e,a)){if(ol(e))throw Error(n(418));a=wa(o.nextSibling);var l=yt;a&&lu(e,a)?ru(l,o):(e.flags=e.flags&-4097|2,Ae=!1,yt=e)}}else{if(ol(e))throw Error(n(418));e.flags=e.flags&-4097|2,Ae=!1,yt=e}}}function cu(e){for(e=e.return;e!==null&&e.tag!==5&&e.tag!==3&&e.tag!==13;)e=e.return;yt=e}function Ho(e){if(e!==yt)return!1;if(!Ae)return cu(e),Ae=!0,!1;var a;if((a=e.tag!==3)&&!(a=e.tag!==5)&&(a=e.type,a=a!=="head"&&a!=="body"&&!$r(e.type,e.memoizedProps)),a&&(a=wt)){if(ol(e))throw du(),Error(n(418));for(;a;)ru(e,a),a=wa(a.nextSibling)}if(cu(e),e.tag===13){if(e=e.memoizedState,e=e!==null?e.dehydrated:null,!e)throw Error(n(317));e:{for(e=e.nextSibling,a=0;e;){if(e.nodeType===8){var o=e.data;if(o==="/$"){if(a===0){wt=wa(e.nextSibling);break e}a--}else o!=="$"&&o!=="$!"&&o!=="$?"||a++}e=e.nextSibling}wt=null}}else wt=yt?wa(e.stateNode.nextSibling):null;return!0}function du(){for(var e=wt;e;)e=wa(e.nextSibling)}function ki(){wt=yt=null,Ae=!1}function rl(e){qt===null?qt=[e]:qt.push(e)}var Tv=z.ReactCurrentBatchConfig;function xn(e,a,o){if(e=o.ref,e!==null&&typeof e!="function"&&typeof e!="object"){if(o._owner){if(o=o._owner,o){if(o.tag!==1)throw Error(n(309));var l=o.stateNode}if(!l)throw Error(n(147,e));var d=l,u=""+e;return a!==null&&a.ref!==null&&typeof a.ref=="function"&&a.ref._stringRef===u?a.ref:(a=function(w){var k=d.refs;w===null?delete k[u]:k[u]=w},a._stringRef=u,a)}if(typeof e!="string")throw Error(n(284));if(!o._owner)throw Error(n(290,e))}return e}function Ko(e,a){throw e=Object.prototype.toString.call(a),Error(n(31,e==="[object Object]"?"object with keys {"+Object.keys(a).join(", ")+"}":e))}function hu(e){var a=e._init;return a(e._payload)}function uu(e){function a(D,C){if(e){var E=D.deletions;E===null?(D.deletions=[C],D.flags|=16):E.push(C)}}function o(D,C){if(!e)return null;for(;C!==null;)a(D,C),C=C.sibling;return null}function l(D,C){for(D=new Map;C!==null;)C.key!==null?D.set(C.key,C):D.set(C.index,C),C=C.sibling;return D}function d(D,C){return D=Pa(D,C),D.index=0,D.sibling=null,D}function u(D,C,E){return D.index=E,e?(E=D.alternate,E!==null?(E=E.index,E<C?(D.flags|=2,C):E):(D.flags|=2,C)):(D.flags|=1048576,C)}function w(D){return e&&D.alternate===null&&(D.flags|=2),D}function k(D,C,E,B){return C===null||C.tag!==6?(C=Xl(E,D.mode,B),C.return=D,C):(C=d(C,E),C.return=D,C)}function S(D,C,E,B){var $=E.type;return $===ie?j(D,C,E.props.children,B,E.key):C!==null&&(C.elementType===$||typeof $=="object"&&$!==null&&$.$$typeof===_e&&hu($)===C.type)?(B=d(C,E.props),B.ref=xn(D,C,E),B.return=D,B):(B=fs(E.type,E.key,E.props,null,D.mode,B),B.ref=xn(D,C,E),B.return=D,B)}function U(D,C,E,B){return C===null||C.tag!==4||C.stateNode.containerInfo!==E.containerInfo||C.stateNode.implementation!==E.implementation?(C=Zl(E,D.mode,B),C.return=D,C):(C=d(C,E.children||[]),C.return=D,C)}function j(D,C,E,B,$){return C===null||C.tag!==7?(C=Ya(E,D.mode,B,$),C.return=D,C):(C=d(C,E),C.return=D,C)}function N(D,C,E){if(typeof C=="string"&&C!==""||typeof C=="number")return C=Xl(""+C,D.mode,E),C.return=D,C;if(typeof C=="object"&&C!==null){switch(C.$$typeof){case V:return E=fs(C.type,C.key,C.props,null,D.mode,E),E.ref=xn(D,null,C),E.return=D,E;case K:return C=Zl(C,D.mode,E),C.return=D,C;case _e:var B=C._init;return N(D,B(C._payload),E)}if(Qi(C)||Z(C))return C=Ya(C,D.mode,E,null),C.return=D,C;Ko(D,C)}return null}function L(D,C,E,B){var $=C!==null?C.key:null;if(typeof E=="string"&&E!==""||typeof E=="number")return $!==null?null:k(D,C,""+E,B);if(typeof E=="object"&&E!==null){switch(E.$$typeof){case V:return E.key===$?S(D,C,E,B):null;case K:return E.key===$?U(D,C,E,B):null;case _e:return $=E._init,L(D,C,$(E._payload),B)}if(Qi(E)||Z(E))return $!==null?null:j(D,C,E,B,null);Ko(D,E)}return null}function _(D,C,E,B,$){if(typeof B=="string"&&B!==""||typeof B=="number")return D=D.get(E)||null,k(C,D,""+B,$);if(typeof B=="object"&&B!==null){switch(B.$$typeof){case V:return D=D.get(B.key===null?E:B.key)||null,S(C,D,B,$);case K:return D=D.get(B.key===null?E:B.key)||null,U(C,D,B,$);case _e:var ee=B._init;return _(D,C,E,ee(B._payload),$)}if(Qi(B)||Z(B))return D=D.get(E)||null,j(C,D,B,$,null);Ko(C,B)}return null}function Y(D,C,E,B){for(var $=null,ee=null,te=C,ne=C=0,Ye=null;te!==null&&ne<E.length;ne++){te.index>ne?(Ye=te,te=null):Ye=te.sibling;var me=L(D,te,E[ne],B);if(me===null){te===null&&(te=Ye);break}e&&te&&me.alternate===null&&a(D,te),C=u(me,C,ne),ee===null?$=me:ee.sibling=me,ee=me,te=Ye}if(ne===E.length)return o(D,te),Ae&&Wa(D,ne),$;if(te===null){for(;ne<E.length;ne++)te=N(D,E[ne],B),te!==null&&(C=u(te,C,ne),ee===null?$=te:ee.sibling=te,ee=te);return Ae&&Wa(D,ne),$}for(te=l(D,te);ne<E.length;ne++)Ye=_(te,D,ne,E[ne],B),Ye!==null&&(e&&Ye.alternate!==null&&te.delete(Ye.key===null?ne:Ye.key),C=u(Ye,C,ne),ee===null?$=Ye:ee.sibling=Ye,ee=Ye);return e&&te.forEach(function(Da){return a(D,Da)}),Ae&&Wa(D,ne),$}function Q(D,C,E,B){var $=Z(E);if(typeof $!="function")throw Error(n(150));if(E=$.call(E),E==null)throw Error(n(151));for(var ee=$=null,te=C,ne=C=0,Ye=null,me=E.next();te!==null&&!me.done;ne++,me=E.next()){te.index>ne?(Ye=te,te=null):Ye=te.sibling;var Da=L(D,te,me.value,B);if(Da===null){te===null&&(te=Ye);break}e&&te&&Da.alternate===null&&a(D,te),C=u(Da,C,ne),ee===null?$=Da:ee.sibling=Da,ee=Da,te=Ye}if(me.done)return o(D,te),Ae&&Wa(D,ne),$;if(te===null){for(;!me.done;ne++,me=E.next())me=N(D,me.value,B),me!==null&&(C=u(me,C,ne),ee===null?$=me:ee.sibling=me,ee=me);return Ae&&Wa(D,ne),$}for(te=l(D,te);!me.done;ne++,me=E.next())me=_(te,D,ne,me.value,B),me!==null&&(e&&me.alternate!==null&&te.delete(me.key===null?ne:me.key),C=u(me,C,ne),ee===null?$=me:ee.sibling=me,ee=me);return e&&te.forEach(function(ob){return a(D,ob)}),Ae&&Wa(D,ne),$}function Fe(D,C,E,B){if(typeof E=="object"&&E!==null&&E.type===ie&&E.key===null&&(E=E.props.children),typeof E=="object"&&E!==null){switch(E.$$typeof){case V:e:{for(var $=E.key,ee=C;ee!==null;){if(ee.key===$){if($=E.type,$===ie){if(ee.tag===7){o(D,ee.sibling),C=d(ee,E.props.children),C.return=D,D=C;break e}}else if(ee.elementType===$||typeof $=="object"&&$!==null&&$.$$typeof===_e&&hu($)===ee.type){o(D,ee.sibling),C=d(ee,E.props),C.ref=xn(D,ee,E),C.return=D,D=C;break e}o(D,ee);break}else a(D,ee);ee=ee.sibling}E.type===ie?(C=Ya(E.props.children,D.mode,B,E.key),C.return=D,D=C):(B=fs(E.type,E.key,E.props,null,D.mode,B),B.ref=xn(D,C,E),B.return=D,D=B)}return w(D);case K:e:{for(ee=E.key;C!==null;){if(C.key===ee)if(C.tag===4&&C.stateNode.containerInfo===E.containerInfo&&C.stateNode.implementation===E.implementation){o(D,C.sibling),C=d(C,E.children||[]),C.return=D,D=C;break e}else{o(D,C);break}else a(D,C);C=C.sibling}C=Zl(E,D.mode,B),C.return=D,D=C}return w(D);case _e:return ee=E._init,Fe(D,C,ee(E._payload),B)}if(Qi(E))return Y(D,C,E,B);if(Z(E))return Q(D,C,E,B);Ko(D,E)}return typeof E=="string"&&E!==""||typeof E=="number"?(E=""+E,C!==null&&C.tag===6?(o(D,C.sibling),C=d(C,E),C.return=D,D=C):(o(D,C),C=Xl(E,D.mode,B),C.return=D,D=C),w(D)):o(D,C)}return Fe}var xi=uu(!0),pu=uu(!1),_o=va(null),Go=null,Si=null,ll=null;function cl(){ll=Si=Go=null}function dl(e){var a=_o.current;Se(_o),e._currentValue=a}function hl(e,a,o){for(;e!==null;){var l=e.alternate;if((e.childLanes&a)!==a?(e.childLanes|=a,l!==null&&(l.childLanes|=a)):l!==null&&(l.childLanes&a)!==a&&(l.childLanes|=a),e===o)break;e=e.return}}function Ai(e,a){Go=e,ll=Si=null,e=e.dependencies,e!==null&&e.firstContext!==null&&((e.lanes&a)!==0&&(dt=!0),e.firstContext=null)}function At(e){var a=e._currentValue;if(ll!==e)if(e={context:e,memoizedValue:a,next:null},Si===null){if(Go===null)throw Error(n(308));Si=e,Go.dependencies={lanes:0,firstContext:e}}else Si=Si.next=e;return a}var Ba=null;function ul(e){Ba===null?Ba=[e]:Ba.push(e)}function mu(e,a,o,l){var d=a.interleaved;return d===null?(o.next=o,ul(a)):(o.next=d.next,d.next=o),a.interleaved=o,oa(e,l)}function oa(e,a){e.lanes|=a;var o=e.alternate;for(o!==null&&(o.lanes|=a),o=e,e=e.return;e!==null;)e.childLanes|=a,o=e.alternate,o!==null&&(o.childLanes|=a),o=e,e=e.return;return o.tag===3?o.stateNode:null}var xa=!1;function pl(e){e.updateQueue={baseState:e.memoizedState,firstBaseUpdate:null,lastBaseUpdate:null,shared:{pending:null,interleaved:null,lanes:0},effects:null}}function fu(e,a){e=e.updateQueue,a.updateQueue===e&&(a.updateQueue={baseState:e.baseState,firstBaseUpdate:e.firstBaseUpdate,lastBaseUpdate:e.lastBaseUpdate,shared:e.shared,effects:e.effects})}function sa(e,a){return{eventTime:e,lane:a,tag:0,payload:null,callback:null,next:null}}function Sa(e,a,o){var l=e.updateQueue;if(l===null)return null;if(l=l.shared,(ue&2)!==0){var d=l.pending;return d===null?a.next=a:(a.next=d.next,d.next=a),l.pending=a,oa(e,o)}return d=l.interleaved,d===null?(a.next=a,ul(l)):(a.next=d.next,d.next=a),l.interleaved=a,oa(e,o)}function Jo(e,a,o){if(a=a.updateQueue,a!==null&&(a=a.shared,(o&4194240)!==0)){var l=a.lanes;l&=e.pendingLanes,o|=l,a.lanes=o,Ir(e,o)}}function gu(e,a){var o=e.updateQueue,l=e.alternate;if(l!==null&&(l=l.updateQueue,o===l)){var d=null,u=null;if(o=o.firstBaseUpdate,o!==null){do{var w={eventTime:o.eventTime,lane:o.lane,tag:o.tag,payload:o.payload,callback:o.callback,next:null};u===null?d=u=w:u=u.next=w,o=o.next}while(o!==null);u===null?d=u=a:u=u.next=a}else d=u=a;o={baseState:l.baseState,firstBaseUpdate:d,lastBaseUpdate:u,shared:l.shared,effects:l.effects},e.updateQueue=o;return}e=o.lastBaseUpdate,e===null?o.firstBaseUpdate=a:e.next=a,o.lastBaseUpdate=a}function Yo(e,a,o,l){var d=e.updateQueue;xa=!1;var u=d.firstBaseUpdate,w=d.lastBaseUpdate,k=d.shared.pending;if(k!==null){d.shared.pending=null;var S=k,U=S.next;S.next=null,w===null?u=U:w.next=U,w=S;var j=e.alternate;j!==null&&(j=j.updateQueue,k=j.lastBaseUpdate,k!==w&&(k===null?j.firstBaseUpdate=U:k.next=U,j.lastBaseUpdate=S))}if(u!==null){var N=d.baseState;w=0,j=U=S=null,k=u;do{var L=k.lane,_=k.eventTime;if((l&L)===L){j!==null&&(j=j.next={eventTime:_,lane:0,tag:k.tag,payload:k.payload,callback:k.callback,next:null});e:{var Y=e,Q=k;switch(L=a,_=o,Q.tag){case 1:if(Y=Q.payload,typeof Y=="function"){N=Y.call(_,N,L);break e}N=Y;break e;case 3:Y.flags=Y.flags&-65537|128;case 0:if(Y=Q.payload,L=typeof Y=="function"?Y.call(_,N,L):Y,L==null)break e;N=H({},N,L);break e;case 2:xa=!0}}k.callback!==null&&k.lane!==0&&(e.flags|=64,L=d.effects,L===null?d.effects=[k]:L.push(k))}else _={eventTime:_,lane:L,tag:k.tag,payload:k.payload,callback:k.callback,next:null},j===null?(U=j=_,S=N):j=j.next=_,w|=L;if(k=k.next,k===null){if(k=d.shared.pending,k===null)break;L=k,k=L.next,L.next=null,d.lastBaseUpdate=L,d.shared.pending=null}}while(!0);if(j===null&&(S=N),d.baseState=S,d.firstBaseUpdate=U,d.lastBaseUpdate=j,a=d.shared.interleaved,a!==null){d=a;do w|=d.lane,d=d.next;while(d!==a)}else u===null&&(d.shared.lanes=0);Ka|=w,e.lanes=w,e.memoizedState=N}}function yu(e,a,o){if(e=a.effects,a.effects=null,e!==null)for(a=0;a<e.length;a++){var l=e[a],d=l.callback;if(d!==null){if(l.callback=null,l=o,typeof d!="function")throw Error(n(191,d));d.call(l)}}}var Sn={},Kt=va(Sn),An=va(Sn),Cn=va(Sn);function za(e){if(e===Sn)throw Error(n(174));return e}function ml(e,a){switch(ke(Cn,a),ke(An,e),ke(Kt,Sn),e=a.nodeType,e){case 9:case 11:a=(a=a.documentElement)?a.namespaceURI:fr(null,"");break;default:e=e===8?a.parentNode:a,a=e.namespaceURI||null,e=e.tagName,a=fr(a,e)}Se(Kt),ke(Kt,a)}function Ci(){Se(Kt),Se(An),Se(Cn)}function wu(e){za(Cn.current);var a=za(Kt.current),o=fr(a,e.type);a!==o&&(ke(An,e),ke(Kt,o))}function fl(e){An.current===e&&(Se(Kt),Se(An))}var Me=va(0);function Qo(e){for(var a=e;a!==null;){if(a.tag===13){var o=a.memoizedState;if(o!==null&&(o=o.dehydrated,o===null||o.data==="$?"||o.data==="$!"))return a}else if(a.tag===19&&a.memoizedProps.revealOrder!==void 0){if((a.flags&128)!==0)return a}else if(a.child!==null){a.child.return=a,a=a.child;continue}if(a===e)break;for(;a.sibling===null;){if(a.return===null||a.return===e)return null;a=a.return}a.sibling.return=a.return,a=a.sibling}return null}var gl=[];function yl(){for(var e=0;e<gl.length;e++)gl[e]._workInProgressVersionPrimary=null;gl.length=0}var $o=z.ReactCurrentDispatcher,wl=z.ReactCurrentBatchConfig,Ha=0,Pe=null,We=null,Ge=null,Xo=!1,Tn=!1,In=0,Iv=0;function et(){throw Error(n(321))}function vl(e,a){if(a===null)return!1;for(var o=0;o<a.length&&o<e.length;o++)if(!Ut(e[o],a[o]))return!1;return!0}function bl(e,a,o,l,d,u){if(Ha=u,Pe=a,a.memoizedState=null,a.updateQueue=null,a.lanes=0,$o.current=e===null||e.memoizedState===null?Rv:Ev,e=o(l,d),Tn){u=0;do{if(Tn=!1,In=0,25<=u)throw Error(n(301));u+=1,Ge=We=null,a.updateQueue=null,$o.current=Uv,e=o(l,d)}while(Tn)}if($o.current=ts,a=We!==null&&We.next!==null,Ha=0,Ge=We=Pe=null,Xo=!1,a)throw Error(n(300));return e}function kl(){var e=In!==0;return In=0,e}function _t(){var e={memoizedState:null,baseState:null,baseQueue:null,queue:null,next:null};return Ge===null?Pe.memoizedState=Ge=e:Ge=Ge.next=e,Ge}function Ct(){if(We===null){var e=Pe.alternate;e=e!==null?e.memoizedState:null}else e=We.next;var a=Ge===null?Pe.memoizedState:Ge.next;if(a!==null)Ge=a,We=e;else{if(e===null)throw Error(n(310));We=e,e={memoizedState:We.memoizedState,baseState:We.baseState,baseQueue:We.baseQueue,queue:We.queue,next:null},Ge===null?Pe.memoizedState=Ge=e:Ge=Ge.next=e}return Ge}function Mn(e,a){return typeof a=="function"?a(e):a}function xl(e){var a=Ct(),o=a.queue;if(o===null)throw Error(n(311));o.lastRenderedReducer=e;var l=We,d=l.baseQueue,u=o.pending;if(u!==null){if(d!==null){var w=d.next;d.next=u.next,u.next=w}l.baseQueue=d=u,o.pending=null}if(d!==null){u=d.next,l=l.baseState;var k=w=null,S=null,U=u;do{var j=U.lane;if((Ha&j)===j)S!==null&&(S=S.next={lane:0,action:U.action,hasEagerState:U.hasEagerState,eagerState:U.eagerState,next:null}),l=U.hasEagerState?U.eagerState:e(l,U.action);else{var N={lane:j,action:U.action,hasEagerState:U.hasEagerState,eagerState:U.eagerState,next:null};S===null?(k=S=N,w=l):S=S.next=N,Pe.lanes|=j,Ka|=j}U=U.next}while(U!==null&&U!==u);S===null?w=l:S.next=k,Ut(l,a.memoizedState)||(dt=!0),a.memoizedState=l,a.baseState=w,a.baseQueue=S,o.lastRenderedState=l}if(e=o.interleaved,e!==null){d=e;do u=d.lane,Pe.lanes|=u,Ka|=u,d=d.next;while(d!==e)}else d===null&&(o.lanes=0);return[a.memoizedState,o.dispatch]}function Sl(e){var a=Ct(),o=a.queue;if(o===null)throw Error(n(311));o.lastRenderedReducer=e;var l=o.dispatch,d=o.pending,u=a.memoizedState;if(d!==null){o.pending=null;var w=d=d.next;do u=e(u,w.action),w=w.next;while(w!==d);Ut(u,a.memoizedState)||(dt=!0),a.memoizedState=u,a.baseQueue===null&&(a.baseState=u),o.lastRenderedState=u}return[u,l]}function vu(){}function bu(e,a){var o=Pe,l=Ct(),d=a(),u=!Ut(l.memoizedState,d);if(u&&(l.memoizedState=d,dt=!0),l=l.queue,Al(Su.bind(null,o,l,e),[e]),l.getSnapshot!==a||u||Ge!==null&&Ge.memoizedState.tag&1){if(o.flags|=2048,Pn(9,xu.bind(null,o,l,d,a),void 0,null),Je===null)throw Error(n(349));(Ha&30)!==0||ku(o,a,d)}return d}function ku(e,a,o){e.flags|=16384,e={getSnapshot:a,value:o},a=Pe.updateQueue,a===null?(a={lastEffect:null,stores:null},Pe.updateQueue=a,a.stores=[e]):(o=a.stores,o===null?a.stores=[e]:o.push(e))}function xu(e,a,o,l){a.value=o,a.getSnapshot=l,Au(a)&&Cu(e)}function Su(e,a,o){return o(function(){Au(a)&&Cu(e)})}function Au(e){var a=e.getSnapshot;e=e.value;try{var o=a();return!Ut(e,o)}catch{return!0}}function Cu(e){var a=oa(e,1);a!==null&&jt(a,e,1,-1)}function Tu(e){var a=_t();return typeof e=="function"&&(e=e()),a.memoizedState=a.baseState=e,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:Mn,lastRenderedState:e},a.queue=e,e=e.dispatch=Dv.bind(null,Pe,e),[a.memoizedState,e]}function Pn(e,a,o,l){return e={tag:e,create:a,destroy:o,deps:l,next:null},a=Pe.updateQueue,a===null?(a={lastEffect:null,stores:null},Pe.updateQueue=a,a.lastEffect=e.next=e):(o=a.lastEffect,o===null?a.lastEffect=e.next=e:(l=o.next,o.next=e,e.next=l,a.lastEffect=e)),e}function Iu(){return Ct().memoizedState}function Zo(e,a,o,l){var d=_t();Pe.flags|=e,d.memoizedState=Pn(1|a,o,void 0,l===void 0?null:l)}function es(e,a,o,l){var d=Ct();l=l===void 0?null:l;var u=void 0;if(We!==null){var w=We.memoizedState;if(u=w.destroy,l!==null&&vl(l,w.deps)){d.memoizedState=Pn(a,o,u,l);return}}Pe.flags|=e,d.memoizedState=Pn(1|a,o,u,l)}function Mu(e,a){return Zo(8390656,8,e,a)}function Al(e,a){return es(2048,8,e,a)}function Pu(e,a){return es(4,2,e,a)}function Du(e,a){return es(4,4,e,a)}function Ru(e,a){if(typeof a=="function")return e=e(),a(e),function(){a(null)};if(a!=null)return e=e(),a.current=e,function(){a.current=null}}function Eu(e,a,o){return o=o!=null?o.concat([e]):null,es(4,4,Ru.bind(null,a,e),o)}function Cl(){}function Uu(e,a){var o=Ct();a=a===void 0?null:a;var l=o.memoizedState;return l!==null&&a!==null&&vl(a,l[1])?l[0]:(o.memoizedState=[e,a],e)}function qu(e,a){var o=Ct();a=a===void 0?null:a;var l=o.memoizedState;return l!==null&&a!==null&&vl(a,l[1])?l[0]:(e=e(),o.memoizedState=[e,a],e)}function Ou(e,a,o){return(Ha&21)===0?(e.baseState&&(e.baseState=!1,dt=!0),e.memoizedState=o):(Ut(o,a)||(o=hh(),Pe.lanes|=o,Ka|=o,e.baseState=!0),a)}function Mv(e,a){var o=ye;ye=o!==0&&4>o?o:4,e(!0);var l=wl.transition;wl.transition={};try{e(!1),a()}finally{ye=o,wl.transition=l}}function Fu(){return Ct().memoizedState}function Pv(e,a,o){var l=Ia(e);if(o={lane:l,action:o,hasEagerState:!1,eagerState:null,next:null},Lu(e))ju(a,o);else if(o=mu(e,a,o,l),o!==null){var d=st();jt(o,e,l,d),Vu(o,a,l)}}function Dv(e,a,o){var l=Ia(e),d={lane:l,action:o,hasEagerState:!1,eagerState:null,next:null};if(Lu(e))ju(a,d);else{var u=e.alternate;if(e.lanes===0&&(u===null||u.lanes===0)&&(u=a.lastRenderedReducer,u!==null))try{var w=a.lastRenderedState,k=u(w,o);if(d.hasEagerState=!0,d.eagerState=k,Ut(k,w)){var S=a.interleaved;S===null?(d.next=d,ul(a)):(d.next=S.next,S.next=d),a.interleaved=d;return}}catch{}finally{}o=mu(e,a,d,l),o!==null&&(d=st(),jt(o,e,l,d),Vu(o,a,l))}}function Lu(e){var a=e.alternate;return e===Pe||a!==null&&a===Pe}function ju(e,a){Tn=Xo=!0;var o=e.pending;o===null?a.next=a:(a.next=o.next,o.next=a),e.pending=a}function Vu(e,a,o){if((o&4194240)!==0){var l=a.lanes;l&=e.pendingLanes,o|=l,a.lanes=o,Ir(e,o)}}var ts={readContext:At,useCallback:et,useContext:et,useEffect:et,useImperativeHandle:et,useInsertionEffect:et,useLayoutEffect:et,useMemo:et,useReducer:et,useRef:et,useState:et,useDebugValue:et,useDeferredValue:et,useTransition:et,useMutableSource:et,useSyncExternalStore:et,useId:et,unstable_isNewReconciler:!1},Rv={readContext:At,useCallback:function(e,a){return _t().memoizedState=[e,a===void 0?null:a],e},useContext:At,useEffect:Mu,useImperativeHandle:function(e,a,o){return o=o!=null?o.concat([e]):null,Zo(4194308,4,Ru.bind(null,a,e),o)},useLayoutEffect:function(e,a){return Zo(4194308,4,e,a)},useInsertionEffect:function(e,a){return Zo(4,2,e,a)},useMemo:function(e,a){var o=_t();return a=a===void 0?null:a,e=e(),o.memoizedState=[e,a],e},useReducer:function(e,a,o){var l=_t();return a=o!==void 0?o(a):a,l.memoizedState=l.baseState=a,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:e,lastRenderedState:a},l.queue=e,e=e.dispatch=Pv.bind(null,Pe,e),[l.memoizedState,e]},useRef:function(e){var a=_t();return e={current:e},a.memoizedState=e},useState:Tu,useDebugValue:Cl,useDeferredValue:function(e){return _t().memoizedState=e},useTransition:function(){var e=Tu(!1),a=e[0];return e=Mv.bind(null,e[1]),_t().memoizedState=e,[a,e]},useMutableSource:function(){},useSyncExternalStore:function(e,a,o){var l=Pe,d=_t();if(Ae){if(o===void 0)throw Error(n(407));o=o()}else{if(o=a(),Je===null)throw Error(n(349));(Ha&30)!==0||ku(l,a,o)}d.memoizedState=o;var u={value:o,getSnapshot:a};return d.queue=u,Mu(Su.bind(null,l,u,e),[e]),l.flags|=2048,Pn(9,xu.bind(null,l,u,o,a),void 0,null),o},useId:function(){var e=_t(),a=Je.identifierPrefix;if(Ae){var o=na,l=ia;o=(l&~(1<<32-Et(l)-1)).toString(32)+o,a=":"+a+"R"+o,o=In++,0<o&&(a+="H"+o.toString(32)),a+=":"}else o=Iv++,a=":"+a+"r"+o.toString(32)+":";return e.memoizedState=a},unstable_isNewReconciler:!1},Ev={readContext:At,useCallback:Uu,useContext:At,useEffect:Al,useImperativeHandle:Eu,useInsertionEffect:Pu,useLayoutEffect:Du,useMemo:qu,useReducer:xl,useRef:Iu,useState:function(){return xl(Mn)},useDebugValue:Cl,useDeferredValue:function(e){var a=Ct();return Ou(a,We.memoizedState,e)},useTransition:function(){var e=xl(Mn)[0],a=Ct().memoizedState;return[e,a]},useMutableSource:vu,useSyncExternalStore:bu,useId:Fu,unstable_isNewReconciler:!1},Uv={readContext:At,useCallback:Uu,useContext:At,useEffect:Al,useImperativeHandle:Eu,useInsertionEffect:Pu,useLayoutEffect:Du,useMemo:qu,useReducer:Sl,useRef:Iu,useState:function(){return Sl(Mn)},useDebugValue:Cl,useDeferredValue:function(e){var a=Ct();return We===null?a.memoizedState=e:Ou(a,We.memoizedState,e)},useTransition:function(){var e=Sl(Mn)[0],a=Ct().memoizedState;return[e,a]},useMutableSource:vu,useSyncExternalStore:bu,useId:Fu,unstable_isNewReconciler:!1};function Ot(e,a){if(e&&e.defaultProps){a=H({},a),e=e.defaultProps;for(var o in e)a[o]===void 0&&(a[o]=e[o]);return a}return a}function Tl(e,a,o,l){a=e.memoizedState,o=o(l,a),o=o==null?a:H({},a,o),e.memoizedState=o,e.lanes===0&&(e.updateQueue.baseState=o)}var as={isMounted:function(e){return(e=e._reactInternals)?La(e)===e:!1},enqueueSetState:function(e,a,o){e=e._reactInternals;var l=st(),d=Ia(e),u=sa(l,d);u.payload=a,o!=null&&(u.callback=o),a=Sa(e,u,d),a!==null&&(jt(a,e,d,l),Jo(a,e,d))},enqueueReplaceState:function(e,a,o){e=e._reactInternals;var l=st(),d=Ia(e),u=sa(l,d);u.tag=1,u.payload=a,o!=null&&(u.callback=o),a=Sa(e,u,d),a!==null&&(jt(a,e,d,l),Jo(a,e,d))},enqueueForceUpdate:function(e,a){e=e._reactInternals;var o=st(),l=Ia(e),d=sa(o,l);d.tag=2,a!=null&&(d.callback=a),a=Sa(e,d,l),a!==null&&(jt(a,e,l,o),Jo(a,e,l))}};function Nu(e,a,o,l,d,u,w){return e=e.stateNode,typeof e.shouldComponentUpdate=="function"?e.shouldComponentUpdate(l,u,w):a.prototype&&a.prototype.isPureReactComponent?!fn(o,l)||!fn(d,u):!0}function Wu(e,a,o){var l=!1,d=ba,u=a.contextType;return typeof u=="object"&&u!==null?u=At(u):(d=ct(a)?Va:Ze.current,l=a.contextTypes,u=(l=l!=null)?wi(e,d):ba),a=new a(o,u),e.memoizedState=a.state!==null&&a.state!==void 0?a.state:null,a.updater=as,e.stateNode=a,a._reactInternals=e,l&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=d,e.__reactInternalMemoizedMaskedChildContext=u),a}function Bu(e,a,o,l){e=a.state,typeof a.componentWillReceiveProps=="function"&&a.componentWillReceiveProps(o,l),typeof a.UNSAFE_componentWillReceiveProps=="function"&&a.UNSAFE_componentWillReceiveProps(o,l),a.state!==e&&as.enqueueReplaceState(a,a.state,null)}function Il(e,a,o,l){var d=e.stateNode;d.props=o,d.state=e.memoizedState,d.refs={},pl(e);var u=a.contextType;typeof u=="object"&&u!==null?d.context=At(u):(u=ct(a)?Va:Ze.current,d.context=wi(e,u)),d.state=e.memoizedState,u=a.getDerivedStateFromProps,typeof u=="function"&&(Tl(e,a,u,o),d.state=e.memoizedState),typeof a.getDerivedStateFromProps=="function"||typeof d.getSnapshotBeforeUpdate=="function"||typeof d.UNSAFE_componentWillMount!="function"&&typeof d.componentWillMount!="function"||(a=d.state,typeof d.componentWillMount=="function"&&d.componentWillMount(),typeof d.UNSAFE_componentWillMount=="function"&&d.UNSAFE_componentWillMount(),a!==d.state&&as.enqueueReplaceState(d,d.state,null),Yo(e,o,d,l),d.state=e.memoizedState),typeof d.componentDidMount=="function"&&(e.flags|=4194308)}function Ti(e,a){try{var o="",l=a;do o+=de(l),l=l.return;while(l);var d=o}catch(u){d=`
Error generating stack: `+u.message+`
`+u.stack}return{value:e,source:a,stack:d,digest:null}}function Ml(e,a,o){return{value:e,source:null,stack:o??null,digest:a??null}}function Pl(e,a){try{console.error(a.value)}catch(o){setTimeout(function(){throw o})}}var qv=typeof WeakMap=="function"?WeakMap:Map;function zu(e,a,o){o=sa(-1,o),o.tag=3,o.payload={element:null};var l=a.value;return o.callback=function(){cs||(cs=!0,Hl=l),Pl(e,a)},o}function Hu(e,a,o){o=sa(-1,o),o.tag=3;var l=e.type.getDerivedStateFromError;if(typeof l=="function"){var d=a.value;o.payload=function(){return l(d)},o.callback=function(){Pl(e,a)}}var u=e.stateNode;return u!==null&&typeof u.componentDidCatch=="function"&&(o.callback=function(){Pl(e,a),typeof l!="function"&&(Ca===null?Ca=new Set([this]):Ca.add(this));var w=a.stack;this.componentDidCatch(a.value,{componentStack:w!==null?w:""})}),o}function Ku(e,a,o){var l=e.pingCache;if(l===null){l=e.pingCache=new qv;var d=new Set;l.set(a,d)}else d=l.get(a),d===void 0&&(d=new Set,l.set(a,d));d.has(o)||(d.add(o),e=Jv.bind(null,e,a,o),a.then(e,e))}function _u(e){do{var a;if((a=e.tag===13)&&(a=e.memoizedState,a=a!==null?a.dehydrated!==null:!0),a)return e;e=e.return}while(e!==null);return null}function Gu(e,a,o,l,d){return(e.mode&1)===0?(e===a?e.flags|=65536:(e.flags|=128,o.flags|=131072,o.flags&=-52805,o.tag===1&&(o.alternate===null?o.tag=17:(a=sa(-1,1),a.tag=2,Sa(o,a,1))),o.lanes|=1),e):(e.flags|=65536,e.lanes=d,e)}var Ov=z.ReactCurrentOwner,dt=!1;function ot(e,a,o,l){a.child=e===null?pu(a,null,o,l):xi(a,e.child,o,l)}function Ju(e,a,o,l,d){o=o.render;var u=a.ref;return Ai(a,d),l=bl(e,a,o,l,u,d),o=kl(),e!==null&&!dt?(a.updateQueue=e.updateQueue,a.flags&=-2053,e.lanes&=~d,ra(e,a,d)):(Ae&&o&&il(a),a.flags|=1,ot(e,a,l,d),a.child)}function Yu(e,a,o,l,d){if(e===null){var u=o.type;return typeof u=="function"&&!$l(u)&&u.defaultProps===void 0&&o.compare===null&&o.defaultProps===void 0?(a.tag=15,a.type=u,Qu(e,a,u,l,d)):(e=fs(o.type,null,l,a,a.mode,d),e.ref=a.ref,e.return=a,a.child=e)}if(u=e.child,(e.lanes&d)===0){var w=u.memoizedProps;if(o=o.compare,o=o!==null?o:fn,o(w,l)&&e.ref===a.ref)return ra(e,a,d)}return a.flags|=1,e=Pa(u,l),e.ref=a.ref,e.return=a,a.child=e}function Qu(e,a,o,l,d){if(e!==null){var u=e.memoizedProps;if(fn(u,l)&&e.ref===a.ref)if(dt=!1,a.pendingProps=l=u,(e.lanes&d)!==0)(e.flags&131072)!==0&&(dt=!0);else return a.lanes=e.lanes,ra(e,a,d)}return Dl(e,a,o,l,d)}function $u(e,a,o){var l=a.pendingProps,d=l.children,u=e!==null?e.memoizedState:null;if(l.mode==="hidden")if((a.mode&1)===0)a.memoizedState={baseLanes:0,cachePool:null,transitions:null},ke(Mi,vt),vt|=o;else{if((o&1073741824)===0)return e=u!==null?u.baseLanes|o:o,a.lanes=a.childLanes=1073741824,a.memoizedState={baseLanes:e,cachePool:null,transitions:null},a.updateQueue=null,ke(Mi,vt),vt|=e,null;a.memoizedState={baseLanes:0,cachePool:null,transitions:null},l=u!==null?u.baseLanes:o,ke(Mi,vt),vt|=l}else u!==null?(l=u.baseLanes|o,a.memoizedState=null):l=o,ke(Mi,vt),vt|=l;return ot(e,a,d,o),a.child}function Xu(e,a){var o=a.ref;(e===null&&o!==null||e!==null&&e.ref!==o)&&(a.flags|=512,a.flags|=2097152)}function Dl(e,a,o,l,d){var u=ct(o)?Va:Ze.current;return u=wi(a,u),Ai(a,d),o=bl(e,a,o,l,u,d),l=kl(),e!==null&&!dt?(a.updateQueue=e.updateQueue,a.flags&=-2053,e.lanes&=~d,ra(e,a,d)):(Ae&&l&&il(a),a.flags|=1,ot(e,a,o,d),a.child)}function Zu(e,a,o,l,d){if(ct(o)){var u=!0;No(a)}else u=!1;if(Ai(a,d),a.stateNode===null)ns(e,a),Wu(a,o,l),Il(a,o,l,d),l=!0;else if(e===null){var w=a.stateNode,k=a.memoizedProps;w.props=k;var S=w.context,U=o.contextType;typeof U=="object"&&U!==null?U=At(U):(U=ct(o)?Va:Ze.current,U=wi(a,U));var j=o.getDerivedStateFromProps,N=typeof j=="function"||typeof w.getSnapshotBeforeUpdate=="function";N||typeof w.UNSAFE_componentWillReceiveProps!="function"&&typeof w.componentWillReceiveProps!="function"||(k!==l||S!==U)&&Bu(a,w,l,U),xa=!1;var L=a.memoizedState;w.state=L,Yo(a,l,w,d),S=a.memoizedState,k!==l||L!==S||lt.current||xa?(typeof j=="function"&&(Tl(a,o,j,l),S=a.memoizedState),(k=xa||Nu(a,o,k,l,L,S,U))?(N||typeof w.UNSAFE_componentWillMount!="function"&&typeof w.componentWillMount!="function"||(typeof w.componentWillMount=="function"&&w.componentWillMount(),typeof w.UNSAFE_componentWillMount=="function"&&w.UNSAFE_componentWillMount()),typeof w.componentDidMount=="function"&&(a.flags|=4194308)):(typeof w.componentDidMount=="function"&&(a.flags|=4194308),a.memoizedProps=l,a.memoizedState=S),w.props=l,w.state=S,w.context=U,l=k):(typeof w.componentDidMount=="function"&&(a.flags|=4194308),l=!1)}else{w=a.stateNode,fu(e,a),k=a.memoizedProps,U=a.type===a.elementType?k:Ot(a.type,k),w.props=U,N=a.pendingProps,L=w.context,S=o.contextType,typeof S=="object"&&S!==null?S=At(S):(S=ct(o)?Va:Ze.current,S=wi(a,S));var _=o.getDerivedStateFromProps;(j=typeof _=="function"||typeof w.getSnapshotBeforeUpdate=="function")||typeof w.UNSAFE_componentWillReceiveProps!="function"&&typeof w.componentWillReceiveProps!="function"||(k!==N||L!==S)&&Bu(a,w,l,S),xa=!1,L=a.memoizedState,w.state=L,Yo(a,l,w,d);var Y=a.memoizedState;k!==N||L!==Y||lt.current||xa?(typeof _=="function"&&(Tl(a,o,_,l),Y=a.memoizedState),(U=xa||Nu(a,o,U,l,L,Y,S)||!1)?(j||typeof w.UNSAFE_componentWillUpdate!="function"&&typeof w.componentWillUpdate!="function"||(typeof w.componentWillUpdate=="function"&&w.componentWillUpdate(l,Y,S),typeof w.UNSAFE_componentWillUpdate=="function"&&w.UNSAFE_componentWillUpdate(l,Y,S)),typeof w.componentDidUpdate=="function"&&(a.flags|=4),typeof w.getSnapshotBeforeUpdate=="function"&&(a.flags|=1024)):(typeof w.componentDidUpdate!="function"||k===e.memoizedProps&&L===e.memoizedState||(a.flags|=4),typeof w.getSnapshotBeforeUpdate!="function"||k===e.memoizedProps&&L===e.memoizedState||(a.flags|=1024),a.memoizedProps=l,a.memoizedState=Y),w.props=l,w.state=Y,w.context=S,l=U):(typeof w.componentDidUpdate!="function"||k===e.memoizedProps&&L===e.memoizedState||(a.flags|=4),typeof w.getSnapshotBeforeUpdate!="function"||k===e.memoizedProps&&L===e.memoizedState||(a.flags|=1024),l=!1)}return Rl(e,a,o,l,u,d)}function Rl(e,a,o,l,d,u){Xu(e,a);var w=(a.flags&128)!==0;if(!l&&!w)return d&&nu(a,o,!1),ra(e,a,u);l=a.stateNode,Ov.current=a;var k=w&&typeof o.getDerivedStateFromError!="function"?null:l.render();return a.flags|=1,e!==null&&w?(a.child=xi(a,e.child,null,u),a.child=xi(a,null,k,u)):ot(e,a,k,u),a.memoizedState=l.state,d&&nu(a,o,!0),a.child}function ep(e){var a=e.stateNode;a.pendingContext?au(e,a.pendingContext,a.pendingContext!==a.context):a.context&&au(e,a.context,!1),ml(e,a.containerInfo)}function tp(e,a,o,l,d){return ki(),rl(d),a.flags|=256,ot(e,a,o,l),a.child}var El={dehydrated:null,treeContext:null,retryLane:0};function Ul(e){return{baseLanes:e,cachePool:null,transitions:null}}function ap(e,a,o){var l=a.pendingProps,d=Me.current,u=!1,w=(a.flags&128)!==0,k;if((k=w)||(k=e!==null&&e.memoizedState===null?!1:(d&2)!==0),k?(u=!0,a.flags&=-129):(e===null||e.memoizedState!==null)&&(d|=1),ke(Me,d&1),e===null)return sl(a),e=a.memoizedState,e!==null&&(e=e.dehydrated,e!==null)?((a.mode&1)===0?a.lanes=1:e.data==="$!"?a.lanes=8:a.lanes=1073741824,null):(w=l.children,e=l.fallback,u?(l=a.mode,u=a.child,w={mode:"hidden",children:w},(l&1)===0&&u!==null?(u.childLanes=0,u.pendingProps=w):u=gs(w,l,0,null),e=Ya(e,l,o,null),u.return=a,e.return=a,u.sibling=e,a.child=u,a.child.memoizedState=Ul(o),a.memoizedState=El,e):ql(a,w));if(d=e.memoizedState,d!==null&&(k=d.dehydrated,k!==null))return Fv(e,a,w,l,k,d,o);if(u){u=l.fallback,w=a.mode,d=e.child,k=d.sibling;var S={mode:"hidden",children:l.children};return(w&1)===0&&a.child!==d?(l=a.child,l.childLanes=0,l.pendingProps=S,a.deletions=null):(l=Pa(d,S),l.subtreeFlags=d.subtreeFlags&14680064),k!==null?u=Pa(k,u):(u=Ya(u,w,o,null),u.flags|=2),u.return=a,l.return=a,l.sibling=u,a.child=l,l=u,u=a.child,w=e.child.memoizedState,w=w===null?Ul(o):{baseLanes:w.baseLanes|o,cachePool:null,transitions:w.transitions},u.memoizedState=w,u.childLanes=e.childLanes&~o,a.memoizedState=El,l}return u=e.child,e=u.sibling,l=Pa(u,{mode:"visible",children:l.children}),(a.mode&1)===0&&(l.lanes=o),l.return=a,l.sibling=null,e!==null&&(o=a.deletions,o===null?(a.deletions=[e],a.flags|=16):o.push(e)),a.child=l,a.memoizedState=null,l}function ql(e,a){return a=gs({mode:"visible",children:a},e.mode,0,null),a.return=e,e.child=a}function is(e,a,o,l){return l!==null&&rl(l),xi(a,e.child,null,o),e=ql(a,a.pendingProps.children),e.flags|=2,a.memoizedState=null,e}function Fv(e,a,o,l,d,u,w){if(o)return a.flags&256?(a.flags&=-257,l=Ml(Error(n(422))),is(e,a,w,l)):a.memoizedState!==null?(a.child=e.child,a.flags|=128,null):(u=l.fallback,d=a.mode,l=gs({mode:"visible",children:l.children},d,0,null),u=Ya(u,d,w,null),u.flags|=2,l.return=a,u.return=a,l.sibling=u,a.child=l,(a.mode&1)!==0&&xi(a,e.child,null,w),a.child.memoizedState=Ul(w),a.memoizedState=El,u);if((a.mode&1)===0)return is(e,a,w,null);if(d.data==="$!"){if(l=d.nextSibling&&d.nextSibling.dataset,l)var k=l.dgst;return l=k,u=Error(n(419)),l=Ml(u,l,void 0),is(e,a,w,l)}if(k=(w&e.childLanes)!==0,dt||k){if(l=Je,l!==null){switch(w&-w){case 4:d=2;break;case 16:d=8;break;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:d=32;break;case 536870912:d=268435456;break;default:d=0}d=(d&(l.suspendedLanes|w))!==0?0:d,d!==0&&d!==u.retryLane&&(u.retryLane=d,oa(e,d),jt(l,e,d,-1))}return Ql(),l=Ml(Error(n(421))),is(e,a,w,l)}return d.data==="$?"?(a.flags|=128,a.child=e.child,a=Yv.bind(null,e),d._reactRetry=a,null):(e=u.treeContext,wt=wa(d.nextSibling),yt=a,Ae=!0,qt=null,e!==null&&(xt[St++]=ia,xt[St++]=na,xt[St++]=Na,ia=e.id,na=e.overflow,Na=a),a=ql(a,l.children),a.flags|=4096,a)}function ip(e,a,o){e.lanes|=a;var l=e.alternate;l!==null&&(l.lanes|=a),hl(e.return,a,o)}function Ol(e,a,o,l,d){var u=e.memoizedState;u===null?e.memoizedState={isBackwards:a,rendering:null,renderingStartTime:0,last:l,tail:o,tailMode:d}:(u.isBackwards=a,u.rendering=null,u.renderingStartTime=0,u.last=l,u.tail=o,u.tailMode=d)}function np(e,a,o){var l=a.pendingProps,d=l.revealOrder,u=l.tail;if(ot(e,a,l.children,o),l=Me.current,(l&2)!==0)l=l&1|2,a.flags|=128;else{if(e!==null&&(e.flags&128)!==0)e:for(e=a.child;e!==null;){if(e.tag===13)e.memoizedState!==null&&ip(e,o,a);else if(e.tag===19)ip(e,o,a);else if(e.child!==null){e.child.return=e,e=e.child;continue}if(e===a)break e;for(;e.sibling===null;){if(e.return===null||e.return===a)break e;e=e.return}e.sibling.return=e.return,e=e.sibling}l&=1}if(ke(Me,l),(a.mode&1)===0)a.memoizedState=null;else switch(d){case"forwards":for(o=a.child,d=null;o!==null;)e=o.alternate,e!==null&&Qo(e)===null&&(d=o),o=o.sibling;o=d,o===null?(d=a.child,a.child=null):(d=o.sibling,o.sibling=null),Ol(a,!1,d,o,u);break;case"backwards":for(o=null,d=a.child,a.child=null;d!==null;){if(e=d.alternate,e!==null&&Qo(e)===null){a.child=d;break}e=d.sibling,d.sibling=o,o=d,d=e}Ol(a,!0,o,null,u);break;case"together":Ol(a,!1,null,null,void 0);break;default:a.memoizedState=null}return a.child}function ns(e,a){(a.mode&1)===0&&e!==null&&(e.alternate=null,a.alternate=null,a.flags|=2)}function ra(e,a,o){if(e!==null&&(a.dependencies=e.dependencies),Ka|=a.lanes,(o&a.childLanes)===0)return null;if(e!==null&&a.child!==e.child)throw Error(n(153));if(a.child!==null){for(e=a.child,o=Pa(e,e.pendingProps),a.child=o,o.return=a;e.sibling!==null;)e=e.sibling,o=o.sibling=Pa(e,e.pendingProps),o.return=a;o.sibling=null}return a.child}function Lv(e,a,o){switch(a.tag){case 3:ep(a),ki();break;case 5:wu(a);break;case 1:ct(a.type)&&No(a);break;case 4:ml(a,a.stateNode.containerInfo);break;case 10:var l=a.type._context,d=a.memoizedProps.value;ke(_o,l._currentValue),l._currentValue=d;break;case 13:if(l=a.memoizedState,l!==null)return l.dehydrated!==null?(ke(Me,Me.current&1),a.flags|=128,null):(o&a.child.childLanes)!==0?ap(e,a,o):(ke(Me,Me.current&1),e=ra(e,a,o),e!==null?e.sibling:null);ke(Me,Me.current&1);break;case 19:if(l=(o&a.childLanes)!==0,(e.flags&128)!==0){if(l)return np(e,a,o);a.flags|=128}if(d=a.memoizedState,d!==null&&(d.rendering=null,d.tail=null,d.lastEffect=null),ke(Me,Me.current),l)break;return null;case 22:case 23:return a.lanes=0,$u(e,a,o)}return ra(e,a,o)}var op,Fl,sp,rp;op=function(e,a){for(var o=a.child;o!==null;){if(o.tag===5||o.tag===6)e.appendChild(o.stateNode);else if(o.tag!==4&&o.child!==null){o.child.return=o,o=o.child;continue}if(o===a)break;for(;o.sibling===null;){if(o.return===null||o.return===a)return;o=o.return}o.sibling.return=o.return,o=o.sibling}},Fl=function(){},sp=function(e,a,o,l){var d=e.memoizedProps;if(d!==l){e=a.stateNode,za(Kt.current);var u=null;switch(o){case"input":d=Ji(e,d),l=Ji(e,l),u=[];break;case"select":d=H({},d,{value:void 0}),l=H({},l,{value:void 0}),u=[];break;case"textarea":d=mr(e,d),l=mr(e,l),u=[];break;default:typeof d.onClick!="function"&&typeof l.onClick=="function"&&(e.onclick=Lo)}gr(o,l);var w;o=null;for(U in d)if(!l.hasOwnProperty(U)&&d.hasOwnProperty(U)&&d[U]!=null)if(U==="style"){var k=d[U];for(w in k)k.hasOwnProperty(w)&&(o||(o={}),o[w]="")}else U!=="dangerouslySetInnerHTML"&&U!=="children"&&U!=="suppressContentEditableWarning"&&U!=="suppressHydrationWarning"&&U!=="autoFocus"&&(r.hasOwnProperty(U)?u||(u=[]):(u=u||[]).push(U,null));for(U in l){var S=l[U];if(k=d!=null?d[U]:void 0,l.hasOwnProperty(U)&&S!==k&&(S!=null||k!=null))if(U==="style")if(k){for(w in k)!k.hasOwnProperty(w)||S&&S.hasOwnProperty(w)||(o||(o={}),o[w]="");for(w in S)S.hasOwnProperty(w)&&k[w]!==S[w]&&(o||(o={}),o[w]=S[w])}else o||(u||(u=[]),u.push(U,o)),o=S;else U==="dangerouslySetInnerHTML"?(S=S?S.__html:void 0,k=k?k.__html:void 0,S!=null&&k!==S&&(u=u||[]).push(U,S)):U==="children"?typeof S!="string"&&typeof S!="number"||(u=u||[]).push(U,""+S):U!=="suppressContentEditableWarning"&&U!=="suppressHydrationWarning"&&(r.hasOwnProperty(U)?(S!=null&&U==="onScroll"&&xe("scroll",e),u||k===S||(u=[])):(u=u||[]).push(U,S))}o&&(u=u||[]).push("style",o);var U=u;(a.updateQueue=U)&&(a.flags|=4)}},rp=function(e,a,o,l){o!==l&&(a.flags|=4)};function Dn(e,a){if(!Ae)switch(e.tailMode){case"hidden":a=e.tail;for(var o=null;a!==null;)a.alternate!==null&&(o=a),a=a.sibling;o===null?e.tail=null:o.sibling=null;break;case"collapsed":o=e.tail;for(var l=null;o!==null;)o.alternate!==null&&(l=o),o=o.sibling;l===null?a||e.tail===null?e.tail=null:e.tail.sibling=null:l.sibling=null}}function tt(e){var a=e.alternate!==null&&e.alternate.child===e.child,o=0,l=0;if(a)for(var d=e.child;d!==null;)o|=d.lanes|d.childLanes,l|=d.subtreeFlags&14680064,l|=d.flags&14680064,d.return=e,d=d.sibling;else for(d=e.child;d!==null;)o|=d.lanes|d.childLanes,l|=d.subtreeFlags,l|=d.flags,d.return=e,d=d.sibling;return e.subtreeFlags|=l,e.childLanes=o,a}function jv(e,a,o){var l=a.pendingProps;switch(nl(a),a.tag){case 2:case 16:case 15:case 0:case 11:case 7:case 8:case 12:case 9:case 14:return tt(a),null;case 1:return ct(a.type)&&Vo(),tt(a),null;case 3:return l=a.stateNode,Ci(),Se(lt),Se(Ze),yl(),l.pendingContext&&(l.context=l.pendingContext,l.pendingContext=null),(e===null||e.child===null)&&(Ho(a)?a.flags|=4:e===null||e.memoizedState.isDehydrated&&(a.flags&256)===0||(a.flags|=1024,qt!==null&&(Gl(qt),qt=null))),Fl(e,a),tt(a),null;case 5:fl(a);var d=za(Cn.current);if(o=a.type,e!==null&&a.stateNode!=null)sp(e,a,o,l,d),e.ref!==a.ref&&(a.flags|=512,a.flags|=2097152);else{if(!l){if(a.stateNode===null)throw Error(n(166));return tt(a),null}if(e=za(Kt.current),Ho(a)){l=a.stateNode,o=a.type;var u=a.memoizedProps;switch(l[Ht]=a,l[bn]=u,e=(a.mode&1)!==0,o){case"dialog":xe("cancel",l),xe("close",l);break;case"iframe":case"object":case"embed":xe("load",l);break;case"video":case"audio":for(d=0;d<yn.length;d++)xe(yn[d],l);break;case"source":xe("error",l);break;case"img":case"image":case"link":xe("error",l),xe("load",l);break;case"details":xe("toggle",l);break;case"input":Yi(l,u),xe("invalid",l);break;case"select":l._wrapperState={wasMultiple:!!u.multiple},xe("invalid",l);break;case"textarea":Hd(l,u),xe("invalid",l)}gr(o,u),d=null;for(var w in u)if(u.hasOwnProperty(w)){var k=u[w];w==="children"?typeof k=="string"?l.textContent!==k&&(u.suppressHydrationWarning!==!0&&Fo(l.textContent,k,e),d=["children",k]):typeof k=="number"&&l.textContent!==""+k&&(u.suppressHydrationWarning!==!0&&Fo(l.textContent,k,e),d=["children",""+k]):r.hasOwnProperty(w)&&k!=null&&w==="onScroll"&&xe("scroll",l)}switch(o){case"input":Ie(l),zd(l,u,!0);break;case"textarea":Ie(l),_d(l);break;case"select":case"option":break;default:typeof u.onClick=="function"&&(l.onclick=Lo)}l=d,a.updateQueue=l,l!==null&&(a.flags|=4)}else{w=d.nodeType===9?d:d.ownerDocument,e==="http://www.w3.org/1999/xhtml"&&(e=Gd(o)),e==="http://www.w3.org/1999/xhtml"?o==="script"?(e=w.createElement("div"),e.innerHTML="<script><\/script>",e=e.removeChild(e.firstChild)):typeof l.is=="string"?e=w.createElement(o,{is:l.is}):(e=w.createElement(o),o==="select"&&(w=e,l.multiple?w.multiple=!0:l.size&&(w.size=l.size))):e=w.createElementNS(e,o),e[Ht]=a,e[bn]=l,op(e,a,!1,!1),a.stateNode=e;e:{switch(w=yr(o,l),o){case"dialog":xe("cancel",e),xe("close",e),d=l;break;case"iframe":case"object":case"embed":xe("load",e),d=l;break;case"video":case"audio":for(d=0;d<yn.length;d++)xe(yn[d],e);d=l;break;case"source":xe("error",e),d=l;break;case"img":case"image":case"link":xe("error",e),xe("load",e),d=l;break;case"details":xe("toggle",e),d=l;break;case"input":Yi(e,l),d=Ji(e,l),xe("invalid",e);break;case"option":d=l;break;case"select":e._wrapperState={wasMultiple:!!l.multiple},d=H({},l,{value:void 0}),xe("invalid",e);break;case"textarea":Hd(e,l),d=mr(e,l),xe("invalid",e);break;default:d=l}gr(o,d),k=d;for(u in k)if(k.hasOwnProperty(u)){var S=k[u];u==="style"?Qd(e,S):u==="dangerouslySetInnerHTML"?(S=S?S.__html:void 0,S!=null&&Jd(e,S)):u==="children"?typeof S=="string"?(o!=="textarea"||S!=="")&&$i(e,S):typeof S=="number"&&$i(e,""+S):u!=="suppressContentEditableWarning"&&u!=="suppressHydrationWarning"&&u!=="autoFocus"&&(r.hasOwnProperty(u)?S!=null&&u==="onScroll"&&xe("scroll",e):S!=null&&q(e,u,S,w))}switch(o){case"input":Ie(e),zd(e,l,!1);break;case"textarea":Ie(e),_d(e);break;case"option":l.value!=null&&e.setAttribute("value",""+pe(l.value));break;case"select":e.multiple=!!l.multiple,u=l.value,u!=null?si(e,!!l.multiple,u,!1):l.defaultValue!=null&&si(e,!!l.multiple,l.defaultValue,!0);break;default:typeof d.onClick=="function"&&(e.onclick=Lo)}switch(o){case"button":case"input":case"select":case"textarea":l=!!l.autoFocus;break e;case"img":l=!0;break e;default:l=!1}}l&&(a.flags|=4)}a.ref!==null&&(a.flags|=512,a.flags|=2097152)}return tt(a),null;case 6:if(e&&a.stateNode!=null)rp(e,a,e.memoizedProps,l);else{if(typeof l!="string"&&a.stateNode===null)throw Error(n(166));if(o=za(Cn.current),za(Kt.current),Ho(a)){if(l=a.stateNode,o=a.memoizedProps,l[Ht]=a,(u=l.nodeValue!==o)&&(e=yt,e!==null))switch(e.tag){case 3:Fo(l.nodeValue,o,(e.mode&1)!==0);break;case 5:e.memoizedProps.suppressHydrationWarning!==!0&&Fo(l.nodeValue,o,(e.mode&1)!==0)}u&&(a.flags|=4)}else l=(o.nodeType===9?o:o.ownerDocument).createTextNode(l),l[Ht]=a,a.stateNode=l}return tt(a),null;case 13:if(Se(Me),l=a.memoizedState,e===null||e.memoizedState!==null&&e.memoizedState.dehydrated!==null){if(Ae&&wt!==null&&(a.mode&1)!==0&&(a.flags&128)===0)du(),ki(),a.flags|=98560,u=!1;else if(u=Ho(a),l!==null&&l.dehydrated!==null){if(e===null){if(!u)throw Error(n(318));if(u=a.memoizedState,u=u!==null?u.dehydrated:null,!u)throw Error(n(317));u[Ht]=a}else ki(),(a.flags&128)===0&&(a.memoizedState=null),a.flags|=4;tt(a),u=!1}else qt!==null&&(Gl(qt),qt=null),u=!0;if(!u)return a.flags&65536?a:null}return(a.flags&128)!==0?(a.lanes=o,a):(l=l!==null,l!==(e!==null&&e.memoizedState!==null)&&l&&(a.child.flags|=8192,(a.mode&1)!==0&&(e===null||(Me.current&1)!==0?Be===0&&(Be=3):Ql())),a.updateQueue!==null&&(a.flags|=4),tt(a),null);case 4:return Ci(),Fl(e,a),e===null&&wn(a.stateNode.containerInfo),tt(a),null;case 10:return dl(a.type._context),tt(a),null;case 17:return ct(a.type)&&Vo(),tt(a),null;case 19:if(Se(Me),u=a.memoizedState,u===null)return tt(a),null;if(l=(a.flags&128)!==0,w=u.rendering,w===null)if(l)Dn(u,!1);else{if(Be!==0||e!==null&&(e.flags&128)!==0)for(e=a.child;e!==null;){if(w=Qo(e),w!==null){for(a.flags|=128,Dn(u,!1),l=w.updateQueue,l!==null&&(a.updateQueue=l,a.flags|=4),a.subtreeFlags=0,l=o,o=a.child;o!==null;)u=o,e=l,u.flags&=14680066,w=u.alternate,w===null?(u.childLanes=0,u.lanes=e,u.child=null,u.subtreeFlags=0,u.memoizedProps=null,u.memoizedState=null,u.updateQueue=null,u.dependencies=null,u.stateNode=null):(u.childLanes=w.childLanes,u.lanes=w.lanes,u.child=w.child,u.subtreeFlags=0,u.deletions=null,u.memoizedProps=w.memoizedProps,u.memoizedState=w.memoizedState,u.updateQueue=w.updateQueue,u.type=w.type,e=w.dependencies,u.dependencies=e===null?null:{lanes:e.lanes,firstContext:e.firstContext}),o=o.sibling;return ke(Me,Me.current&1|2),a.child}e=e.sibling}u.tail!==null&&Oe()>Pi&&(a.flags|=128,l=!0,Dn(u,!1),a.lanes=4194304)}else{if(!l)if(e=Qo(w),e!==null){if(a.flags|=128,l=!0,o=e.updateQueue,o!==null&&(a.updateQueue=o,a.flags|=4),Dn(u,!0),u.tail===null&&u.tailMode==="hidden"&&!w.alternate&&!Ae)return tt(a),null}else 2*Oe()-u.renderingStartTime>Pi&&o!==1073741824&&(a.flags|=128,l=!0,Dn(u,!1),a.lanes=4194304);u.isBackwards?(w.sibling=a.child,a.child=w):(o=u.last,o!==null?o.sibling=w:a.child=w,u.last=w)}return u.tail!==null?(a=u.tail,u.rendering=a,u.tail=a.sibling,u.renderingStartTime=Oe(),a.sibling=null,o=Me.current,ke(Me,l?o&1|2:o&1),a):(tt(a),null);case 22:case 23:return Yl(),l=a.memoizedState!==null,e!==null&&e.memoizedState!==null!==l&&(a.flags|=8192),l&&(a.mode&1)!==0?(vt&1073741824)!==0&&(tt(a),a.subtreeFlags&6&&(a.flags|=8192)):tt(a),null;case 24:return null;case 25:return null}throw Error(n(156,a.tag))}function Vv(e,a){switch(nl(a),a.tag){case 1:return ct(a.type)&&Vo(),e=a.flags,e&65536?(a.flags=e&-65537|128,a):null;case 3:return Ci(),Se(lt),Se(Ze),yl(),e=a.flags,(e&65536)!==0&&(e&128)===0?(a.flags=e&-65537|128,a):null;case 5:return fl(a),null;case 13:if(Se(Me),e=a.memoizedState,e!==null&&e.dehydrated!==null){if(a.alternate===null)throw Error(n(340));ki()}return e=a.flags,e&65536?(a.flags=e&-65537|128,a):null;case 19:return Se(Me),null;case 4:return Ci(),null;case 10:return dl(a.type._context),null;case 22:case 23:return Yl(),null;case 24:return null;default:return null}}var os=!1,at=!1,Nv=typeof WeakSet=="function"?WeakSet:Set,G=null;function Ii(e,a){var o=e.ref;if(o!==null)if(typeof o=="function")try{o(null)}catch(l){Ue(e,a,l)}else o.current=null}function Ll(e,a,o){try{o()}catch(l){Ue(e,a,l)}}var lp=!1;function Wv(e,a){if(Yr=Co,e=Nh(),Wr(e)){if("selectionStart"in e)var o={start:e.selectionStart,end:e.selectionEnd};else e:{o=(o=e.ownerDocument)&&o.defaultView||window;var l=o.getSelection&&o.getSelection();if(l&&l.rangeCount!==0){o=l.anchorNode;var d=l.anchorOffset,u=l.focusNode;l=l.focusOffset;try{o.nodeType,u.nodeType}catch{o=null;break e}var w=0,k=-1,S=-1,U=0,j=0,N=e,L=null;t:for(;;){for(var _;N!==o||d!==0&&N.nodeType!==3||(k=w+d),N!==u||l!==0&&N.nodeType!==3||(S=w+l),N.nodeType===3&&(w+=N.nodeValue.length),(_=N.firstChild)!==null;)L=N,N=_;for(;;){if(N===e)break t;if(L===o&&++U===d&&(k=w),L===u&&++j===l&&(S=w),(_=N.nextSibling)!==null)break;N=L,L=N.parentNode}N=_}o=k===-1||S===-1?null:{start:k,end:S}}else o=null}o=o||{start:0,end:0}}else o=null;for(Qr={focusedElem:e,selectionRange:o},Co=!1,G=a;G!==null;)if(a=G,e=a.child,(a.subtreeFlags&1028)!==0&&e!==null)e.return=a,G=e;else for(;G!==null;){a=G;try{var Y=a.alternate;if((a.flags&1024)!==0)switch(a.tag){case 0:case 11:case 15:break;case 1:if(Y!==null){var Q=Y.memoizedProps,Fe=Y.memoizedState,D=a.stateNode,C=D.getSnapshotBeforeUpdate(a.elementType===a.type?Q:Ot(a.type,Q),Fe);D.__reactInternalSnapshotBeforeUpdate=C}break;case 3:var E=a.stateNode.containerInfo;E.nodeType===1?E.textContent="":E.nodeType===9&&E.documentElement&&E.removeChild(E.documentElement);break;case 5:case 6:case 4:case 17:break;default:throw Error(n(163))}}catch(B){Ue(a,a.return,B)}if(e=a.sibling,e!==null){e.return=a.return,G=e;break}G=a.return}return Y=lp,lp=!1,Y}function Rn(e,a,o){var l=a.updateQueue;if(l=l!==null?l.lastEffect:null,l!==null){var d=l=l.next;do{if((d.tag&e)===e){var u=d.destroy;d.destroy=void 0,u!==void 0&&Ll(a,o,u)}d=d.next}while(d!==l)}}function ss(e,a){if(a=a.updateQueue,a=a!==null?a.lastEffect:null,a!==null){var o=a=a.next;do{if((o.tag&e)===e){var l=o.create;o.destroy=l()}o=o.next}while(o!==a)}}function jl(e){var a=e.ref;if(a!==null){var o=e.stateNode;switch(e.tag){case 5:e=o;break;default:e=o}typeof a=="function"?a(e):a.current=e}}function cp(e){var a=e.alternate;a!==null&&(e.alternate=null,cp(a)),e.child=null,e.deletions=null,e.sibling=null,e.tag===5&&(a=e.stateNode,a!==null&&(delete a[Ht],delete a[bn],delete a[el],delete a[Sv],delete a[Av])),e.stateNode=null,e.return=null,e.dependencies=null,e.memoizedProps=null,e.memoizedState=null,e.pendingProps=null,e.stateNode=null,e.updateQueue=null}function dp(e){return e.tag===5||e.tag===3||e.tag===4}function hp(e){e:for(;;){for(;e.sibling===null;){if(e.return===null||dp(e.return))return null;e=e.return}for(e.sibling.return=e.return,e=e.sibling;e.tag!==5&&e.tag!==6&&e.tag!==18;){if(e.flags&2||e.child===null||e.tag===4)continue e;e.child.return=e,e=e.child}if(!(e.flags&2))return e.stateNode}}function Vl(e,a,o){var l=e.tag;if(l===5||l===6)e=e.stateNode,a?o.nodeType===8?o.parentNode.insertBefore(e,a):o.insertBefore(e,a):(o.nodeType===8?(a=o.parentNode,a.insertBefore(e,o)):(a=o,a.appendChild(e)),o=o._reactRootContainer,o!=null||a.onclick!==null||(a.onclick=Lo));else if(l!==4&&(e=e.child,e!==null))for(Vl(e,a,o),e=e.sibling;e!==null;)Vl(e,a,o),e=e.sibling}function Nl(e,a,o){var l=e.tag;if(l===5||l===6)e=e.stateNode,a?o.insertBefore(e,a):o.appendChild(e);else if(l!==4&&(e=e.child,e!==null))for(Nl(e,a,o),e=e.sibling;e!==null;)Nl(e,a,o),e=e.sibling}var $e=null,Ft=!1;function Aa(e,a,o){for(o=o.child;o!==null;)up(e,a,o),o=o.sibling}function up(e,a,o){if(zt&&typeof zt.onCommitFiberUnmount=="function")try{zt.onCommitFiberUnmount(vo,o)}catch{}switch(o.tag){case 5:at||Ii(o,a);case 6:var l=$e,d=Ft;$e=null,Aa(e,a,o),$e=l,Ft=d,$e!==null&&(Ft?(e=$e,o=o.stateNode,e.nodeType===8?e.parentNode.removeChild(o):e.removeChild(o)):$e.removeChild(o.stateNode));break;case 18:$e!==null&&(Ft?(e=$e,o=o.stateNode,e.nodeType===8?Zr(e.parentNode,o):e.nodeType===1&&Zr(e,o),cn(e)):Zr($e,o.stateNode));break;case 4:l=$e,d=Ft,$e=o.stateNode.containerInfo,Ft=!0,Aa(e,a,o),$e=l,Ft=d;break;case 0:case 11:case 14:case 15:if(!at&&(l=o.updateQueue,l!==null&&(l=l.lastEffect,l!==null))){d=l=l.next;do{var u=d,w=u.destroy;u=u.tag,w!==void 0&&((u&2)!==0||(u&4)!==0)&&Ll(o,a,w),d=d.next}while(d!==l)}Aa(e,a,o);break;case 1:if(!at&&(Ii(o,a),l=o.stateNode,typeof l.componentWillUnmount=="function"))try{l.props=o.memoizedProps,l.state=o.memoizedState,l.componentWillUnmount()}catch(k){Ue(o,a,k)}Aa(e,a,o);break;case 21:Aa(e,a,o);break;case 22:o.mode&1?(at=(l=at)||o.memoizedState!==null,Aa(e,a,o),at=l):Aa(e,a,o);break;default:Aa(e,a,o)}}function pp(e){var a=e.updateQueue;if(a!==null){e.updateQueue=null;var o=e.stateNode;o===null&&(o=e.stateNode=new Nv),a.forEach(function(l){var d=Qv.bind(null,e,l);o.has(l)||(o.add(l),l.then(d,d))})}}function Lt(e,a){var o=a.deletions;if(o!==null)for(var l=0;l<o.length;l++){var d=o[l];try{var u=e,w=a,k=w;e:for(;k!==null;){switch(k.tag){case 5:$e=k.stateNode,Ft=!1;break e;case 3:$e=k.stateNode.containerInfo,Ft=!0;break e;case 4:$e=k.stateNode.containerInfo,Ft=!0;break e}k=k.return}if($e===null)throw Error(n(160));up(u,w,d),$e=null,Ft=!1;var S=d.alternate;S!==null&&(S.return=null),d.return=null}catch(U){Ue(d,a,U)}}if(a.subtreeFlags&12854)for(a=a.child;a!==null;)mp(a,e),a=a.sibling}function mp(e,a){var o=e.alternate,l=e.flags;switch(e.tag){case 0:case 11:case 14:case 15:if(Lt(a,e),Gt(e),l&4){try{Rn(3,e,e.return),ss(3,e)}catch(Q){Ue(e,e.return,Q)}try{Rn(5,e,e.return)}catch(Q){Ue(e,e.return,Q)}}break;case 1:Lt(a,e),Gt(e),l&512&&o!==null&&Ii(o,o.return);break;case 5:if(Lt(a,e),Gt(e),l&512&&o!==null&&Ii(o,o.return),e.flags&32){var d=e.stateNode;try{$i(d,"")}catch(Q){Ue(e,e.return,Q)}}if(l&4&&(d=e.stateNode,d!=null)){var u=e.memoizedProps,w=o!==null?o.memoizedProps:u,k=e.type,S=e.updateQueue;if(e.updateQueue=null,S!==null)try{k==="input"&&u.type==="radio"&&u.name!=null&&oi(d,u),yr(k,w);var U=yr(k,u);for(w=0;w<S.length;w+=2){var j=S[w],N=S[w+1];j==="style"?Qd(d,N):j==="dangerouslySetInnerHTML"?Jd(d,N):j==="children"?$i(d,N):q(d,j,N,U)}switch(k){case"input":ur(d,u);break;case"textarea":Kd(d,u);break;case"select":var L=d._wrapperState.wasMultiple;d._wrapperState.wasMultiple=!!u.multiple;var _=u.value;_!=null?si(d,!!u.multiple,_,!1):L!==!!u.multiple&&(u.defaultValue!=null?si(d,!!u.multiple,u.defaultValue,!0):si(d,!!u.multiple,u.multiple?[]:"",!1))}d[bn]=u}catch(Q){Ue(e,e.return,Q)}}break;case 6:if(Lt(a,e),Gt(e),l&4){if(e.stateNode===null)throw Error(n(162));d=e.stateNode,u=e.memoizedProps;try{d.nodeValue=u}catch(Q){Ue(e,e.return,Q)}}break;case 3:if(Lt(a,e),Gt(e),l&4&&o!==null&&o.memoizedState.isDehydrated)try{cn(a.containerInfo)}catch(Q){Ue(e,e.return,Q)}break;case 4:Lt(a,e),Gt(e);break;case 13:Lt(a,e),Gt(e),d=e.child,d.flags&8192&&(u=d.memoizedState!==null,d.stateNode.isHidden=u,!u||d.alternate!==null&&d.alternate.memoizedState!==null||(zl=Oe())),l&4&&pp(e);break;case 22:if(j=o!==null&&o.memoizedState!==null,e.mode&1?(at=(U=at)||j,Lt(a,e),at=U):Lt(a,e),Gt(e),l&8192){if(U=e.memoizedState!==null,(e.stateNode.isHidden=U)&&!j&&(e.mode&1)!==0)for(G=e,j=e.child;j!==null;){for(N=G=j;G!==null;){switch(L=G,_=L.child,L.tag){case 0:case 11:case 14:case 15:Rn(4,L,L.return);break;case 1:Ii(L,L.return);var Y=L.stateNode;if(typeof Y.componentWillUnmount=="function"){l=L,o=L.return;try{a=l,Y.props=a.memoizedProps,Y.state=a.memoizedState,Y.componentWillUnmount()}catch(Q){Ue(l,o,Q)}}break;case 5:Ii(L,L.return);break;case 22:if(L.memoizedState!==null){yp(N);continue}}_!==null?(_.return=L,G=_):yp(N)}j=j.sibling}e:for(j=null,N=e;;){if(N.tag===5){if(j===null){j=N;try{d=N.stateNode,U?(u=d.style,typeof u.setProperty=="function"?u.setProperty("display","none","important"):u.display="none"):(k=N.stateNode,S=N.memoizedProps.style,w=S!=null&&S.hasOwnProperty("display")?S.display:null,k.style.display=Yd("display",w))}catch(Q){Ue(e,e.return,Q)}}}else if(N.tag===6){if(j===null)try{N.stateNode.nodeValue=U?"":N.memoizedProps}catch(Q){Ue(e,e.return,Q)}}else if((N.tag!==22&&N.tag!==23||N.memoizedState===null||N===e)&&N.child!==null){N.child.return=N,N=N.child;continue}if(N===e)break e;for(;N.sibling===null;){if(N.return===null||N.return===e)break e;j===N&&(j=null),N=N.return}j===N&&(j=null),N.sibling.return=N.return,N=N.sibling}}break;case 19:Lt(a,e),Gt(e),l&4&&pp(e);break;case 21:break;default:Lt(a,e),Gt(e)}}function Gt(e){var a=e.flags;if(a&2){try{e:{for(var o=e.return;o!==null;){if(dp(o)){var l=o;break e}o=o.return}throw Error(n(160))}switch(l.tag){case 5:var d=l.stateNode;l.flags&32&&($i(d,""),l.flags&=-33);var u=hp(e);Nl(e,u,d);break;case 3:case 4:var w=l.stateNode.containerInfo,k=hp(e);Vl(e,k,w);break;default:throw Error(n(161))}}catch(S){Ue(e,e.return,S)}e.flags&=-3}a&4096&&(e.flags&=-4097)}function Bv(e,a,o){G=e,fp(e)}function fp(e,a,o){for(var l=(e.mode&1)!==0;G!==null;){var d=G,u=d.child;if(d.tag===22&&l){var w=d.memoizedState!==null||os;if(!w){var k=d.alternate,S=k!==null&&k.memoizedState!==null||at;k=os;var U=at;if(os=w,(at=S)&&!U)for(G=d;G!==null;)w=G,S=w.child,w.tag===22&&w.memoizedState!==null?wp(d):S!==null?(S.return=w,G=S):wp(d);for(;u!==null;)G=u,fp(u),u=u.sibling;G=d,os=k,at=U}gp(e)}else(d.subtreeFlags&8772)!==0&&u!==null?(u.return=d,G=u):gp(e)}}function gp(e){for(;G!==null;){var a=G;if((a.flags&8772)!==0){var o=a.alternate;try{if((a.flags&8772)!==0)switch(a.tag){case 0:case 11:case 15:at||ss(5,a);break;case 1:var l=a.stateNode;if(a.flags&4&&!at)if(o===null)l.componentDidMount();else{var d=a.elementType===a.type?o.memoizedProps:Ot(a.type,o.memoizedProps);l.componentDidUpdate(d,o.memoizedState,l.__reactInternalSnapshotBeforeUpdate)}var u=a.updateQueue;u!==null&&yu(a,u,l);break;case 3:var w=a.updateQueue;if(w!==null){if(o=null,a.child!==null)switch(a.child.tag){case 5:o=a.child.stateNode;break;case 1:o=a.child.stateNode}yu(a,w,o)}break;case 5:var k=a.stateNode;if(o===null&&a.flags&4){o=k;var S=a.memoizedProps;switch(a.type){case"button":case"input":case"select":case"textarea":S.autoFocus&&o.focus();break;case"img":S.src&&(o.src=S.src)}}break;case 6:break;case 4:break;case 12:break;case 13:if(a.memoizedState===null){var U=a.alternate;if(U!==null){var j=U.memoizedState;if(j!==null){var N=j.dehydrated;N!==null&&cn(N)}}}break;case 19:case 17:case 21:case 22:case 23:case 25:break;default:throw Error(n(163))}at||a.flags&512&&jl(a)}catch(L){Ue(a,a.return,L)}}if(a===e){G=null;break}if(o=a.sibling,o!==null){o.return=a.return,G=o;break}G=a.return}}function yp(e){for(;G!==null;){var a=G;if(a===e){G=null;break}var o=a.sibling;if(o!==null){o.return=a.return,G=o;break}G=a.return}}function wp(e){for(;G!==null;){var a=G;try{switch(a.tag){case 0:case 11:case 15:var o=a.return;try{ss(4,a)}catch(S){Ue(a,o,S)}break;case 1:var l=a.stateNode;if(typeof l.componentDidMount=="function"){var d=a.return;try{l.componentDidMount()}catch(S){Ue(a,d,S)}}var u=a.return;try{jl(a)}catch(S){Ue(a,u,S)}break;case 5:var w=a.return;try{jl(a)}catch(S){Ue(a,w,S)}}}catch(S){Ue(a,a.return,S)}if(a===e){G=null;break}var k=a.sibling;if(k!==null){k.return=a.return,G=k;break}G=a.return}}var zv=Math.ceil,rs=z.ReactCurrentDispatcher,Wl=z.ReactCurrentOwner,Tt=z.ReactCurrentBatchConfig,ue=0,Je=null,Ne=null,Xe=0,vt=0,Mi=va(0),Be=0,En=null,Ka=0,ls=0,Bl=0,Un=null,ht=null,zl=0,Pi=1/0,la=null,cs=!1,Hl=null,Ca=null,ds=!1,Ta=null,hs=0,qn=0,Kl=null,us=-1,ps=0;function st(){return(ue&6)!==0?Oe():us!==-1?us:us=Oe()}function Ia(e){return(e.mode&1)===0?1:(ue&2)!==0&&Xe!==0?Xe&-Xe:Tv.transition!==null?(ps===0&&(ps=hh()),ps):(e=ye,e!==0||(e=window.event,e=e===void 0?16:bh(e.type)),e)}function jt(e,a,o,l){if(50<qn)throw qn=0,Kl=null,Error(n(185));nn(e,o,l),((ue&2)===0||e!==Je)&&(e===Je&&((ue&2)===0&&(ls|=o),Be===4&&Ma(e,Xe)),ut(e,l),o===1&&ue===0&&(a.mode&1)===0&&(Pi=Oe()+500,Wo&&ka()))}function ut(e,a){var o=e.callbackNode;Tw(e,a);var l=xo(e,e===Je?Xe:0);if(l===0)o!==null&&lh(o),e.callbackNode=null,e.callbackPriority=0;else if(a=l&-l,e.callbackPriority!==a){if(o!=null&&lh(o),a===1)e.tag===0?Cv(bp.bind(null,e)):ou(bp.bind(null,e)),kv(function(){(ue&6)===0&&ka()}),o=null;else{switch(uh(l)){case 1:o=Ar;break;case 4:o=ch;break;case 16:o=wo;break;case 536870912:o=dh;break;default:o=wo}o=Mp(o,vp.bind(null,e))}e.callbackPriority=a,e.callbackNode=o}}function vp(e,a){if(us=-1,ps=0,(ue&6)!==0)throw Error(n(327));var o=e.callbackNode;if(Di()&&e.callbackNode!==o)return null;var l=xo(e,e===Je?Xe:0);if(l===0)return null;if((l&30)!==0||(l&e.expiredLanes)!==0||a)a=ms(e,l);else{a=l;var d=ue;ue|=2;var u=xp();(Je!==e||Xe!==a)&&(la=null,Pi=Oe()+500,Ga(e,a));do try{_v();break}catch(k){kp(e,k)}while(!0);cl(),rs.current=u,ue=d,Ne!==null?a=0:(Je=null,Xe=0,a=Be)}if(a!==0){if(a===2&&(d=Cr(e),d!==0&&(l=d,a=_l(e,d))),a===1)throw o=En,Ga(e,0),Ma(e,l),ut(e,Oe()),o;if(a===6)Ma(e,l);else{if(d=e.current.alternate,(l&30)===0&&!Hv(d)&&(a=ms(e,l),a===2&&(u=Cr(e),u!==0&&(l=u,a=_l(e,u))),a===1))throw o=En,Ga(e,0),Ma(e,l),ut(e,Oe()),o;switch(e.finishedWork=d,e.finishedLanes=l,a){case 0:case 1:throw Error(n(345));case 2:Ja(e,ht,la);break;case 3:if(Ma(e,l),(l&130023424)===l&&(a=zl+500-Oe(),10<a)){if(xo(e,0)!==0)break;if(d=e.suspendedLanes,(d&l)!==l){st(),e.pingedLanes|=e.suspendedLanes&d;break}e.timeoutHandle=Xr(Ja.bind(null,e,ht,la),a);break}Ja(e,ht,la);break;case 4:if(Ma(e,l),(l&4194240)===l)break;for(a=e.eventTimes,d=-1;0<l;){var w=31-Et(l);u=1<<w,w=a[w],w>d&&(d=w),l&=~u}if(l=d,l=Oe()-l,l=(120>l?120:480>l?480:1080>l?1080:1920>l?1920:3e3>l?3e3:4320>l?4320:1960*zv(l/1960))-l,10<l){e.timeoutHandle=Xr(Ja.bind(null,e,ht,la),l);break}Ja(e,ht,la);break;case 5:Ja(e,ht,la);break;default:throw Error(n(329))}}}return ut(e,Oe()),e.callbackNode===o?vp.bind(null,e):null}function _l(e,a){var o=Un;return e.current.memoizedState.isDehydrated&&(Ga(e,a).flags|=256),e=ms(e,a),e!==2&&(a=ht,ht=o,a!==null&&Gl(a)),e}function Gl(e){ht===null?ht=e:ht.push.apply(ht,e)}function Hv(e){for(var a=e;;){if(a.flags&16384){var o=a.updateQueue;if(o!==null&&(o=o.stores,o!==null))for(var l=0;l<o.length;l++){var d=o[l],u=d.getSnapshot;d=d.value;try{if(!Ut(u(),d))return!1}catch{return!1}}}if(o=a.child,a.subtreeFlags&16384&&o!==null)o.return=a,a=o;else{if(a===e)break;for(;a.sibling===null;){if(a.return===null||a.return===e)return!0;a=a.return}a.sibling.return=a.return,a=a.sibling}}return!0}function Ma(e,a){for(a&=~Bl,a&=~ls,e.suspendedLanes|=a,e.pingedLanes&=~a,e=e.expirationTimes;0<a;){var o=31-Et(a),l=1<<o;e[o]=-1,a&=~l}}function bp(e){if((ue&6)!==0)throw Error(n(327));Di();var a=xo(e,0);if((a&1)===0)return ut(e,Oe()),null;var o=ms(e,a);if(e.tag!==0&&o===2){var l=Cr(e);l!==0&&(a=l,o=_l(e,l))}if(o===1)throw o=En,Ga(e,0),Ma(e,a),ut(e,Oe()),o;if(o===6)throw Error(n(345));return e.finishedWork=e.current.alternate,e.finishedLanes=a,Ja(e,ht,la),ut(e,Oe()),null}function Jl(e,a){var o=ue;ue|=1;try{return e(a)}finally{ue=o,ue===0&&(Pi=Oe()+500,Wo&&ka())}}function _a(e){Ta!==null&&Ta.tag===0&&(ue&6)===0&&Di();var a=ue;ue|=1;var o=Tt.transition,l=ye;try{if(Tt.transition=null,ye=1,e)return e()}finally{ye=l,Tt.transition=o,ue=a,(ue&6)===0&&ka()}}function Yl(){vt=Mi.current,Se(Mi)}function Ga(e,a){e.finishedWork=null,e.finishedLanes=0;var o=e.timeoutHandle;if(o!==-1&&(e.timeoutHandle=-1,bv(o)),Ne!==null)for(o=Ne.return;o!==null;){var l=o;switch(nl(l),l.tag){case 1:l=l.type.childContextTypes,l!=null&&Vo();break;case 3:Ci(),Se(lt),Se(Ze),yl();break;case 5:fl(l);break;case 4:Ci();break;case 13:Se(Me);break;case 19:Se(Me);break;case 10:dl(l.type._context);break;case 22:case 23:Yl()}o=o.return}if(Je=e,Ne=e=Pa(e.current,null),Xe=vt=a,Be=0,En=null,Bl=ls=Ka=0,ht=Un=null,Ba!==null){for(a=0;a<Ba.length;a++)if(o=Ba[a],l=o.interleaved,l!==null){o.interleaved=null;var d=l.next,u=o.pending;if(u!==null){var w=u.next;u.next=d,l.next=w}o.pending=l}Ba=null}return e}function kp(e,a){do{var o=Ne;try{if(cl(),$o.current=ts,Xo){for(var l=Pe.memoizedState;l!==null;){var d=l.queue;d!==null&&(d.pending=null),l=l.next}Xo=!1}if(Ha=0,Ge=We=Pe=null,Tn=!1,In=0,Wl.current=null,o===null||o.return===null){Be=1,En=a,Ne=null;break}e:{var u=e,w=o.return,k=o,S=a;if(a=Xe,k.flags|=32768,S!==null&&typeof S=="object"&&typeof S.then=="function"){var U=S,j=k,N=j.tag;if((j.mode&1)===0&&(N===0||N===11||N===15)){var L=j.alternate;L?(j.updateQueue=L.updateQueue,j.memoizedState=L.memoizedState,j.lanes=L.lanes):(j.updateQueue=null,j.memoizedState=null)}var _=_u(w);if(_!==null){_.flags&=-257,Gu(_,w,k,u,a),_.mode&1&&Ku(u,U,a),a=_,S=U;var Y=a.updateQueue;if(Y===null){var Q=new Set;Q.add(S),a.updateQueue=Q}else Y.add(S);break e}else{if((a&1)===0){Ku(u,U,a),Ql();break e}S=Error(n(426))}}else if(Ae&&k.mode&1){var Fe=_u(w);if(Fe!==null){(Fe.flags&65536)===0&&(Fe.flags|=256),Gu(Fe,w,k,u,a),rl(Ti(S,k));break e}}u=S=Ti(S,k),Be!==4&&(Be=2),Un===null?Un=[u]:Un.push(u),u=w;do{switch(u.tag){case 3:u.flags|=65536,a&=-a,u.lanes|=a;var D=zu(u,S,a);gu(u,D);break e;case 1:k=S;var C=u.type,E=u.stateNode;if((u.flags&128)===0&&(typeof C.getDerivedStateFromError=="function"||E!==null&&typeof E.componentDidCatch=="function"&&(Ca===null||!Ca.has(E)))){u.flags|=65536,a&=-a,u.lanes|=a;var B=Hu(u,k,a);gu(u,B);break e}}u=u.return}while(u!==null)}Ap(o)}catch($){a=$,Ne===o&&o!==null&&(Ne=o=o.return);continue}break}while(!0)}function xp(){var e=rs.current;return rs.current=ts,e===null?ts:e}function Ql(){(Be===0||Be===3||Be===2)&&(Be=4),Je===null||(Ka&268435455)===0&&(ls&268435455)===0||Ma(Je,Xe)}function ms(e,a){var o=ue;ue|=2;var l=xp();(Je!==e||Xe!==a)&&(la=null,Ga(e,a));do try{Kv();break}catch(d){kp(e,d)}while(!0);if(cl(),ue=o,rs.current=l,Ne!==null)throw Error(n(261));return Je=null,Xe=0,Be}function Kv(){for(;Ne!==null;)Sp(Ne)}function _v(){for(;Ne!==null&&!yw();)Sp(Ne)}function Sp(e){var a=Ip(e.alternate,e,vt);e.memoizedProps=e.pendingProps,a===null?Ap(e):Ne=a,Wl.current=null}function Ap(e){var a=e;do{var o=a.alternate;if(e=a.return,(a.flags&32768)===0){if(o=jv(o,a,vt),o!==null){Ne=o;return}}else{if(o=Vv(o,a),o!==null){o.flags&=32767,Ne=o;return}if(e!==null)e.flags|=32768,e.subtreeFlags=0,e.deletions=null;else{Be=6,Ne=null;return}}if(a=a.sibling,a!==null){Ne=a;return}Ne=a=e}while(a!==null);Be===0&&(Be=5)}function Ja(e,a,o){var l=ye,d=Tt.transition;try{Tt.transition=null,ye=1,Gv(e,a,o,l)}finally{Tt.transition=d,ye=l}return null}function Gv(e,a,o,l){do Di();while(Ta!==null);if((ue&6)!==0)throw Error(n(327));o=e.finishedWork;var d=e.finishedLanes;if(o===null)return null;if(e.finishedWork=null,e.finishedLanes=0,o===e.current)throw Error(n(177));e.callbackNode=null,e.callbackPriority=0;var u=o.lanes|o.childLanes;if(Iw(e,u),e===Je&&(Ne=Je=null,Xe=0),(o.subtreeFlags&2064)===0&&(o.flags&2064)===0||ds||(ds=!0,Mp(wo,function(){return Di(),null})),u=(o.flags&15990)!==0,(o.subtreeFlags&15990)!==0||u){u=Tt.transition,Tt.transition=null;var w=ye;ye=1;var k=ue;ue|=4,Wl.current=null,Wv(e,o),mp(o,e),pv(Qr),Co=!!Yr,Qr=Yr=null,e.current=o,Bv(o),ww(),ue=k,ye=w,Tt.transition=u}else e.current=o;if(ds&&(ds=!1,Ta=e,hs=d),u=e.pendingLanes,u===0&&(Ca=null),kw(o.stateNode),ut(e,Oe()),a!==null)for(l=e.onRecoverableError,o=0;o<a.length;o++)d=a[o],l(d.value,{componentStack:d.stack,digest:d.digest});if(cs)throw cs=!1,e=Hl,Hl=null,e;return(hs&1)!==0&&e.tag!==0&&Di(),u=e.pendingLanes,(u&1)!==0?e===Kl?qn++:(qn=0,Kl=e):qn=0,ka(),null}function Di(){if(Ta!==null){var e=uh(hs),a=Tt.transition,o=ye;try{if(Tt.transition=null,ye=16>e?16:e,Ta===null)var l=!1;else{if(e=Ta,Ta=null,hs=0,(ue&6)!==0)throw Error(n(331));var d=ue;for(ue|=4,G=e.current;G!==null;){var u=G,w=u.child;if((G.flags&16)!==0){var k=u.deletions;if(k!==null){for(var S=0;S<k.length;S++){var U=k[S];for(G=U;G!==null;){var j=G;switch(j.tag){case 0:case 11:case 15:Rn(8,j,u)}var N=j.child;if(N!==null)N.return=j,G=N;else for(;G!==null;){j=G;var L=j.sibling,_=j.return;if(cp(j),j===U){G=null;break}if(L!==null){L.return=_,G=L;break}G=_}}}var Y=u.alternate;if(Y!==null){var Q=Y.child;if(Q!==null){Y.child=null;do{var Fe=Q.sibling;Q.sibling=null,Q=Fe}while(Q!==null)}}G=u}}if((u.subtreeFlags&2064)!==0&&w!==null)w.return=u,G=w;else e:for(;G!==null;){if(u=G,(u.flags&2048)!==0)switch(u.tag){case 0:case 11:case 15:Rn(9,u,u.return)}var D=u.sibling;if(D!==null){D.return=u.return,G=D;break e}G=u.return}}var C=e.current;for(G=C;G!==null;){w=G;var E=w.child;if((w.subtreeFlags&2064)!==0&&E!==null)E.return=w,G=E;else e:for(w=C;G!==null;){if(k=G,(k.flags&2048)!==0)try{switch(k.tag){case 0:case 11:case 15:ss(9,k)}}catch($){Ue(k,k.return,$)}if(k===w){G=null;break e}var B=k.sibling;if(B!==null){B.return=k.return,G=B;break e}G=k.return}}if(ue=d,ka(),zt&&typeof zt.onPostCommitFiberRoot=="function")try{zt.onPostCommitFiberRoot(vo,e)}catch{}l=!0}return l}finally{ye=o,Tt.transition=a}}return!1}function Cp(e,a,o){a=Ti(o,a),a=zu(e,a,1),e=Sa(e,a,1),a=st(),e!==null&&(nn(e,1,a),ut(e,a))}function Ue(e,a,o){if(e.tag===3)Cp(e,e,o);else for(;a!==null;){if(a.tag===3){Cp(a,e,o);break}else if(a.tag===1){var l=a.stateNode;if(typeof a.type.getDerivedStateFromError=="function"||typeof l.componentDidCatch=="function"&&(Ca===null||!Ca.has(l))){e=Ti(o,e),e=Hu(a,e,1),a=Sa(a,e,1),e=st(),a!==null&&(nn(a,1,e),ut(a,e));break}}a=a.return}}function Jv(e,a,o){var l=e.pingCache;l!==null&&l.delete(a),a=st(),e.pingedLanes|=e.suspendedLanes&o,Je===e&&(Xe&o)===o&&(Be===4||Be===3&&(Xe&130023424)===Xe&&500>Oe()-zl?Ga(e,0):Bl|=o),ut(e,a)}function Tp(e,a){a===0&&((e.mode&1)===0?a=1:(a=ko,ko<<=1,(ko&130023424)===0&&(ko=4194304)));var o=st();e=oa(e,a),e!==null&&(nn(e,a,o),ut(e,o))}function Yv(e){var a=e.memoizedState,o=0;a!==null&&(o=a.retryLane),Tp(e,o)}function Qv(e,a){var o=0;switch(e.tag){case 13:var l=e.stateNode,d=e.memoizedState;d!==null&&(o=d.retryLane);break;case 19:l=e.stateNode;break;default:throw Error(n(314))}l!==null&&l.delete(a),Tp(e,o)}var Ip;Ip=function(e,a,o){if(e!==null)if(e.memoizedProps!==a.pendingProps||lt.current)dt=!0;else{if((e.lanes&o)===0&&(a.flags&128)===0)return dt=!1,Lv(e,a,o);dt=(e.flags&131072)!==0}else dt=!1,Ae&&(a.flags&1048576)!==0&&su(a,zo,a.index);switch(a.lanes=0,a.tag){case 2:var l=a.type;ns(e,a),e=a.pendingProps;var d=wi(a,Ze.current);Ai(a,o),d=bl(null,a,l,e,d,o);var u=kl();return a.flags|=1,typeof d=="object"&&d!==null&&typeof d.render=="function"&&d.$$typeof===void 0?(a.tag=1,a.memoizedState=null,a.updateQueue=null,ct(l)?(u=!0,No(a)):u=!1,a.memoizedState=d.state!==null&&d.state!==void 0?d.state:null,pl(a),d.updater=as,a.stateNode=d,d._reactInternals=a,Il(a,l,e,o),a=Rl(null,a,l,!0,u,o)):(a.tag=0,Ae&&u&&il(a),ot(null,a,d,o),a=a.child),a;case 16:l=a.elementType;e:{switch(ns(e,a),e=a.pendingProps,d=l._init,l=d(l._payload),a.type=l,d=a.tag=Xv(l),e=Ot(l,e),d){case 0:a=Dl(null,a,l,e,o);break e;case 1:a=Zu(null,a,l,e,o);break e;case 11:a=Ju(null,a,l,e,o);break e;case 14:a=Yu(null,a,l,Ot(l.type,e),o);break e}throw Error(n(306,l,""))}return a;case 0:return l=a.type,d=a.pendingProps,d=a.elementType===l?d:Ot(l,d),Dl(e,a,l,d,o);case 1:return l=a.type,d=a.pendingProps,d=a.elementType===l?d:Ot(l,d),Zu(e,a,l,d,o);case 3:e:{if(ep(a),e===null)throw Error(n(387));l=a.pendingProps,u=a.memoizedState,d=u.element,fu(e,a),Yo(a,l,null,o);var w=a.memoizedState;if(l=w.element,u.isDehydrated)if(u={element:l,isDehydrated:!1,cache:w.cache,pendingSuspenseBoundaries:w.pendingSuspenseBoundaries,transitions:w.transitions},a.updateQueue.baseState=u,a.memoizedState=u,a.flags&256){d=Ti(Error(n(423)),a),a=tp(e,a,l,o,d);break e}else if(l!==d){d=Ti(Error(n(424)),a),a=tp(e,a,l,o,d);break e}else for(wt=wa(a.stateNode.containerInfo.firstChild),yt=a,Ae=!0,qt=null,o=pu(a,null,l,o),a.child=o;o;)o.flags=o.flags&-3|4096,o=o.sibling;else{if(ki(),l===d){a=ra(e,a,o);break e}ot(e,a,l,o)}a=a.child}return a;case 5:return wu(a),e===null&&sl(a),l=a.type,d=a.pendingProps,u=e!==null?e.memoizedProps:null,w=d.children,$r(l,d)?w=null:u!==null&&$r(l,u)&&(a.flags|=32),Xu(e,a),ot(e,a,w,o),a.child;case 6:return e===null&&sl(a),null;case 13:return ap(e,a,o);case 4:return ml(a,a.stateNode.containerInfo),l=a.pendingProps,e===null?a.child=xi(a,null,l,o):ot(e,a,l,o),a.child;case 11:return l=a.type,d=a.pendingProps,d=a.elementType===l?d:Ot(l,d),Ju(e,a,l,d,o);case 7:return ot(e,a,a.pendingProps,o),a.child;case 8:return ot(e,a,a.pendingProps.children,o),a.child;case 12:return ot(e,a,a.pendingProps.children,o),a.child;case 10:e:{if(l=a.type._context,d=a.pendingProps,u=a.memoizedProps,w=d.value,ke(_o,l._currentValue),l._currentValue=w,u!==null)if(Ut(u.value,w)){if(u.children===d.children&&!lt.current){a=ra(e,a,o);break e}}else for(u=a.child,u!==null&&(u.return=a);u!==null;){var k=u.dependencies;if(k!==null){w=u.child;for(var S=k.firstContext;S!==null;){if(S.context===l){if(u.tag===1){S=sa(-1,o&-o),S.tag=2;var U=u.updateQueue;if(U!==null){U=U.shared;var j=U.pending;j===null?S.next=S:(S.next=j.next,j.next=S),U.pending=S}}u.lanes|=o,S=u.alternate,S!==null&&(S.lanes|=o),hl(u.return,o,a),k.lanes|=o;break}S=S.next}}else if(u.tag===10)w=u.type===a.type?null:u.child;else if(u.tag===18){if(w=u.return,w===null)throw Error(n(341));w.lanes|=o,k=w.alternate,k!==null&&(k.lanes|=o),hl(w,o,a),w=u.sibling}else w=u.child;if(w!==null)w.return=u;else for(w=u;w!==null;){if(w===a){w=null;break}if(u=w.sibling,u!==null){u.return=w.return,w=u;break}w=w.return}u=w}ot(e,a,d.children,o),a=a.child}return a;case 9:return d=a.type,l=a.pendingProps.children,Ai(a,o),d=At(d),l=l(d),a.flags|=1,ot(e,a,l,o),a.child;case 14:return l=a.type,d=Ot(l,a.pendingProps),d=Ot(l.type,d),Yu(e,a,l,d,o);case 15:return Qu(e,a,a.type,a.pendingProps,o);case 17:return l=a.type,d=a.pendingProps,d=a.elementType===l?d:Ot(l,d),ns(e,a),a.tag=1,ct(l)?(e=!0,No(a)):e=!1,Ai(a,o),Wu(a,l,d),Il(a,l,d,o),Rl(null,a,l,!0,e,o);case 19:return np(e,a,o);case 22:return $u(e,a,o)}throw Error(n(156,a.tag))};function Mp(e,a){return rh(e,a)}function $v(e,a,o,l){this.tag=e,this.key=o,this.sibling=this.child=this.return=this.stateNode=this.type=this.elementType=null,this.index=0,this.ref=null,this.pendingProps=a,this.dependencies=this.memoizedState=this.updateQueue=this.memoizedProps=null,this.mode=l,this.subtreeFlags=this.flags=0,this.deletions=null,this.childLanes=this.lanes=0,this.alternate=null}function It(e,a,o,l){return new $v(e,a,o,l)}function $l(e){return e=e.prototype,!(!e||!e.isReactComponent)}function Xv(e){if(typeof e=="function")return $l(e)?1:0;if(e!=null){if(e=e.$$typeof,e===Ke)return 11;if(e===Ee)return 14}return 2}function Pa(e,a){var o=e.alternate;return o===null?(o=It(e.tag,a,e.key,e.mode),o.elementType=e.elementType,o.type=e.type,o.stateNode=e.stateNode,o.alternate=e,e.alternate=o):(o.pendingProps=a,o.type=e.type,o.flags=0,o.subtreeFlags=0,o.deletions=null),o.flags=e.flags&14680064,o.childLanes=e.childLanes,o.lanes=e.lanes,o.child=e.child,o.memoizedProps=e.memoizedProps,o.memoizedState=e.memoizedState,o.updateQueue=e.updateQueue,a=e.dependencies,o.dependencies=a===null?null:{lanes:a.lanes,firstContext:a.firstContext},o.sibling=e.sibling,o.index=e.index,o.ref=e.ref,o}function fs(e,a,o,l,d,u){var w=2;if(l=e,typeof e=="function")$l(e)&&(w=1);else if(typeof e=="string")w=5;else e:switch(e){case ie:return Ya(o.children,d,u,a);case J:w=8,d|=8;break;case ce:return e=It(12,o,a,d|2),e.elementType=ce,e.lanes=u,e;case je:return e=It(13,o,a,d),e.elementType=je,e.lanes=u,e;case Ve:return e=It(19,o,a,d),e.elementType=Ve,e.lanes=u,e;case oe:return gs(o,d,u,a);default:if(typeof e=="object"&&e!==null)switch(e.$$typeof){case we:w=10;break e;case Te:w=9;break e;case Ke:w=11;break e;case Ee:w=14;break e;case _e:w=16,l=null;break e}throw Error(n(130,e==null?e:typeof e,""))}return a=It(w,o,a,d),a.elementType=e,a.type=l,a.lanes=u,a}function Ya(e,a,o,l){return e=It(7,e,l,a),e.lanes=o,e}function gs(e,a,o,l){return e=It(22,e,l,a),e.elementType=oe,e.lanes=o,e.stateNode={isHidden:!1},e}function Xl(e,a,o){return e=It(6,e,null,a),e.lanes=o,e}function Zl(e,a,o){return a=It(4,e.children!==null?e.children:[],e.key,a),a.lanes=o,a.stateNode={containerInfo:e.containerInfo,pendingChildren:null,implementation:e.implementation},a}function Zv(e,a,o,l,d){this.tag=a,this.containerInfo=e,this.finishedWork=this.pingCache=this.current=this.pendingChildren=null,this.timeoutHandle=-1,this.callbackNode=this.pendingContext=this.context=null,this.callbackPriority=0,this.eventTimes=Tr(0),this.expirationTimes=Tr(-1),this.entangledLanes=this.finishedLanes=this.mutableReadLanes=this.expiredLanes=this.pingedLanes=this.suspendedLanes=this.pendingLanes=0,this.entanglements=Tr(0),this.identifierPrefix=l,this.onRecoverableError=d,this.mutableSourceEagerHydrationData=null}function ec(e,a,o,l,d,u,w,k,S){return e=new Zv(e,a,o,k,S),a===1?(a=1,u===!0&&(a|=8)):a=0,u=It(3,null,null,a),e.current=u,u.stateNode=e,u.memoizedState={element:l,isDehydrated:o,cache:null,transitions:null,pendingSuspenseBoundaries:null},pl(u),e}function eb(e,a,o){var l=3<arguments.length&&arguments[3]!==void 0?arguments[3]:null;return{$$typeof:K,key:l==null?null:""+l,children:e,containerInfo:a,implementation:o}}function Pp(e){if(!e)return ba;e=e._reactInternals;e:{if(La(e)!==e||e.tag!==1)throw Error(n(170));var a=e;do{switch(a.tag){case 3:a=a.stateNode.context;break e;case 1:if(ct(a.type)){a=a.stateNode.__reactInternalMemoizedMergedChildContext;break e}}a=a.return}while(a!==null);throw Error(n(171))}if(e.tag===1){var o=e.type;if(ct(o))return iu(e,o,a)}return a}function Dp(e,a,o,l,d,u,w,k,S){return e=ec(o,l,!0,e,d,u,w,k,S),e.context=Pp(null),o=e.current,l=st(),d=Ia(o),u=sa(l,d),u.callback=a??null,Sa(o,u,d),e.current.lanes=d,nn(e,d,l),ut(e,l),e}function ys(e,a,o,l){var d=a.current,u=st(),w=Ia(d);return o=Pp(o),a.context===null?a.context=o:a.pendingContext=o,a=sa(u,w),a.payload={element:e},l=l===void 0?null:l,l!==null&&(a.callback=l),e=Sa(d,a,w),e!==null&&(jt(e,d,w,u),Jo(e,d,w)),w}function ws(e){if(e=e.current,!e.child)return null;switch(e.child.tag){case 5:return e.child.stateNode;default:return e.child.stateNode}}function Rp(e,a){if(e=e.memoizedState,e!==null&&e.dehydrated!==null){var o=e.retryLane;e.retryLane=o!==0&&o<a?o:a}}function tc(e,a){Rp(e,a),(e=e.alternate)&&Rp(e,a)}function tb(){return null}var Ep=typeof reportError=="function"?reportError:function(e){console.error(e)};function ac(e){this._internalRoot=e}vs.prototype.render=ac.prototype.render=function(e){var a=this._internalRoot;if(a===null)throw Error(n(409));ys(e,a,null,null)},vs.prototype.unmount=ac.prototype.unmount=function(){var e=this._internalRoot;if(e!==null){this._internalRoot=null;var a=e.containerInfo;_a(function(){ys(null,e,null,null)}),a[ta]=null}};function vs(e){this._internalRoot=e}vs.prototype.unstable_scheduleHydration=function(e){if(e){var a=fh();e={blockedOn:null,target:e,priority:a};for(var o=0;o<fa.length&&a!==0&&a<fa[o].priority;o++);fa.splice(o,0,e),o===0&&wh(e)}};function ic(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11)}function bs(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11&&(e.nodeType!==8||e.nodeValue!==" react-mount-point-unstable "))}function Up(){}function ab(e,a,o,l,d){if(d){if(typeof l=="function"){var u=l;l=function(){var U=ws(w);u.call(U)}}var w=Dp(a,l,e,0,null,!1,!1,"",Up);return e._reactRootContainer=w,e[ta]=w.current,wn(e.nodeType===8?e.parentNode:e),_a(),w}for(;d=e.lastChild;)e.removeChild(d);if(typeof l=="function"){var k=l;l=function(){var U=ws(S);k.call(U)}}var S=ec(e,0,!1,null,null,!1,!1,"",Up);return e._reactRootContainer=S,e[ta]=S.current,wn(e.nodeType===8?e.parentNode:e),_a(function(){ys(a,S,o,l)}),S}function ks(e,a,o,l,d){var u=o._reactRootContainer;if(u){var w=u;if(typeof d=="function"){var k=d;d=function(){var S=ws(w);k.call(S)}}ys(a,w,e,d)}else w=ab(o,a,e,d,l);return ws(w)}ph=function(e){switch(e.tag){case 3:var a=e.stateNode;if(a.current.memoizedState.isDehydrated){var o=an(a.pendingLanes);o!==0&&(Ir(a,o|1),ut(a,Oe()),(ue&6)===0&&(Pi=Oe()+500,ka()))}break;case 13:_a(function(){var l=oa(e,1);if(l!==null){var d=st();jt(l,e,1,d)}}),tc(e,1)}},Mr=function(e){if(e.tag===13){var a=oa(e,134217728);if(a!==null){var o=st();jt(a,e,134217728,o)}tc(e,134217728)}},mh=function(e){if(e.tag===13){var a=Ia(e),o=oa(e,a);if(o!==null){var l=st();jt(o,e,a,l)}tc(e,a)}},fh=function(){return ye},gh=function(e,a){var o=ye;try{return ye=e,a()}finally{ye=o}},br=function(e,a,o){switch(a){case"input":if(ur(e,o),a=o.name,o.type==="radio"&&a!=null){for(o=e;o.parentNode;)o=o.parentNode;for(o=o.querySelectorAll("input[name="+JSON.stringify(""+a)+'][type="radio"]'),a=0;a<o.length;a++){var l=o[a];if(l!==e&&l.form===e.form){var d=jo(l);if(!d)throw Error(n(90));nt(l),ur(l,d)}}}break;case"textarea":Kd(e,o);break;case"select":a=o.value,a!=null&&si(e,!!o.multiple,a,!1)}},eh=Jl,th=_a;var ib={usingClientEntryPoint:!1,Events:[kn,gi,jo,Xd,Zd,Jl]},On={findFiberByHostInstance:ja,bundleType:0,version:"18.3.1",rendererPackageName:"react-dom"},nb={bundleType:On.bundleType,version:On.version,rendererPackageName:On.rendererPackageName,rendererConfig:On.rendererConfig,overrideHookState:null,overrideHookStateDeletePath:null,overrideHookStateRenamePath:null,overrideProps:null,overridePropsDeletePath:null,overridePropsRenamePath:null,setErrorHandler:null,setSuspenseHandler:null,scheduleUpdate:null,currentDispatcherRef:z.ReactCurrentDispatcher,findHostInstanceByFiber:function(e){return e=oh(e),e===null?null:e.stateNode},findFiberByHostInstance:On.findFiberByHostInstance||tb,findHostInstancesForRefresh:null,scheduleRefresh:null,scheduleRoot:null,setRefreshHandler:null,getCurrentFiber:null,reconcilerVersion:"18.3.1-next-f1338f8080-20240426"};if(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__<"u"){var xs=__REACT_DEVTOOLS_GLOBAL_HOOK__;if(!xs.isDisabled&&xs.supportsFiber)try{vo=xs.inject(nb),zt=xs}catch{}}return pt.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=ib,pt.createPortal=function(e,a){var o=2<arguments.length&&arguments[2]!==void 0?arguments[2]:null;if(!ic(a))throw Error(n(200));return eb(e,a,null,o)},pt.createRoot=function(e,a){if(!ic(e))throw Error(n(299));var o=!1,l="",d=Ep;return a!=null&&(a.unstable_strictMode===!0&&(o=!0),a.identifierPrefix!==void 0&&(l=a.identifierPrefix),a.onRecoverableError!==void 0&&(d=a.onRecoverableError)),a=ec(e,1,!1,null,null,o,!1,l,d),e[ta]=a.current,wn(e.nodeType===8?e.parentNode:e),new ac(a)},pt.findDOMNode=function(e){if(e==null)return null;if(e.nodeType===1)return e;var a=e._reactInternals;if(a===void 0)throw typeof e.render=="function"?Error(n(188)):(e=Object.keys(e).join(","),Error(n(268,e)));return e=oh(a),e=e===null?null:e.stateNode,e},pt.flushSync=function(e){return _a(e)},pt.hydrate=function(e,a,o){if(!bs(a))throw Error(n(200));return ks(null,e,a,!0,o)},pt.hydrateRoot=function(e,a,o){if(!ic(e))throw Error(n(405));var l=o!=null&&o.hydratedSources||null,d=!1,u="",w=Ep;if(o!=null&&(o.unstable_strictMode===!0&&(d=!0),o.identifierPrefix!==void 0&&(u=o.identifierPrefix),o.onRecoverableError!==void 0&&(w=o.onRecoverableError)),a=Dp(a,null,e,1,o??null,d,!1,u,w),e[ta]=a.current,wn(e),l)for(e=0;e<l.length;e++)o=l[e],d=o._getVersion,d=d(o._source),a.mutableSourceEagerHydrationData==null?a.mutableSourceEagerHydrationData=[o,d]:a.mutableSourceEagerHydrationData.push(o,d);return new vs(a)},pt.render=function(e,a,o){if(!bs(a))throw Error(n(200));return ks(null,e,a,!1,o)},pt.unmountComponentAtNode=function(e){if(!bs(e))throw Error(n(40));return e._reactRootContainer?(_a(function(){ks(null,null,e,!1,function(){e._reactRootContainer=null,e[ta]=null})}),!0):!1},pt.unstable_batchedUpdates=Jl,pt.unstable_renderSubtreeIntoContainer=function(e,a,o,l){if(!bs(o))throw Error(n(200));if(e==null||e._reactInternals===void 0)throw Error(n(38));return ks(e,a,o,!1,l)},pt.version="18.3.1-next-f1338f8080-20240426",pt}var Wp;function fb(){if(Wp)return sc.exports;Wp=1;function t(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>"u"||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!="function"))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(t)}catch(i){console.error(i)}}return t(),sc.exports=mb(),sc.exports}var Bp;function gb(){if(Bp)return Ss;Bp=1;var t=fb();return Ss.createRoot=t.createRoot,Ss.hydrateRoot=t.hydrateRoot,Ss}var yb=gb();const wb=Ff(yb);/**
 * react-router v7.18.1
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */var Zc=/^(?:[a-z][a-z0-9+.-]*:|[\\/]{2})/i,Lf=/^[\\/]{2}/;function vb(t,i){return i+t.replace(/\\/g,"/")}var zp="popstate";function Hp(t){return typeof t=="object"&&t!=null&&"pathname"in t&&"search"in t&&"hash"in t&&"state"in t&&"key"in t}function bb(t={}){function i(s,r){var f;let c=(f=r.state)==null?void 0:f.masked,{pathname:h,search:p,hash:m}=c||s.location;return Rc("",{pathname:h,search:p,hash:m},r.state&&r.state.usr||null,r.state&&r.state.key||"default",c?{pathname:s.location.pathname,search:s.location.search,hash:s.location.hash}:void 0)}function n(s,r){return typeof r=="string"?r:$n(r)}return xb(i,n,null,t)}function Ce(t,i){if(t===!1||t===null||typeof t>"u")throw new Error(i)}function Wt(t,i){if(!t){typeof console<"u"&&console.warn(i);try{throw new Error(i)}catch{}}}function kb(){return Math.random().toString(36).substring(2,10)}function Kp(t,i){return{usr:t.state,key:t.key,idx:i,masked:t.mask?{pathname:t.pathname,search:t.search,hash:t.hash}:void 0}}function Rc(t,i,n=null,s,r){return{pathname:typeof t=="string"?t:t.pathname,search:"",hash:"",...typeof i=="string"?Bi(i):i,state:n,key:i&&i.key||s||kb(),mask:r}}function $n({pathname:t="/",search:i="",hash:n=""}){return i&&i!=="?"&&(t+=i.charAt(0)==="?"?i:"?"+i),n&&n!=="#"&&(t+=n.charAt(0)==="#"?n:"#"+n),t}function Bi(t){let i={};if(t){let n=t.indexOf("#");n>=0&&(i.hash=t.substring(n),t=t.substring(0,n));let s=t.indexOf("?");s>=0&&(i.search=t.substring(s),t=t.substring(0,s)),t&&(i.pathname=t)}return i}function xb(t,i,n,s={}){let{window:r=document.defaultView,v5Compat:c=!1}=s,h=r.history,p="POP",m=null,f=y();f==null&&(f=0,h.replaceState({...h.state,idx:f},""));function y(){return(h.state||{idx:null}).idx}function v(){p="POP";let M=y(),R=M==null?null:M-f;f=M,m&&m({action:p,location:I.location,delta:R})}function b(M,R){p="PUSH";let O=Hp(M)?M:Rc(I.location,M,R);f=y()+1;let q=Kp(O,f),z=I.createHref(O.mask||O);try{h.pushState(q,"",z)}catch(V){if(V instanceof DOMException&&V.name==="DataCloneError")throw V;r.location.assign(z)}c&&m&&m({action:p,location:I.location,delta:1})}function A(M,R){p="REPLACE";let O=Hp(M)?M:Rc(I.location,M,R);f=y();let q=Kp(O,f),z=I.createHref(O.mask||O);h.replaceState(q,"",z),c&&m&&m({action:p,location:I.location,delta:0})}function T(M){return Sb(r,M)}let I={get action(){return p},get location(){return t(r,h)},listen(M){if(m)throw new Error("A history only accepts one active listener");return r.addEventListener(zp,v),m=M,()=>{r.removeEventListener(zp,v),m=null}},createHref(M){return i(r,M)},createURL:T,encodeLocation(M){let R=T(M);return{pathname:R.pathname,search:R.search,hash:R.hash}},push:b,replace:A,go(M){return h.go(M)}};return I}function Sb(t,i,n=!1){let s="http://localhost";t&&(s=t.location.origin!=="null"?t.location.origin:t.location.href),Ce(s,"No window.location.(origin|href) available to create URL");let r=typeof i=="string"?i:$n(i);return r=r.replace(/ $/,"%20"),!n&&Lf.test(r)&&(r=s+r),new URL(r,s)}function jf(t,i,n="/"){return Ab(t,i,n,!1)}function Ab(t,i,n,s,r){let c=typeof i=="string"?Bi(i):i,h=ha(c.pathname||"/",n);if(h==null)return null;let p=Cb(t),m=null,f=Fb(h);for(let y=0;m==null&&y<p.length;++y)m=Ob(p[y],f,s);return m}function Cb(t){let i=Vf(t);return Tb(i),i}function Vf(t,i=[],n=[],s="",r=!1){let c=(h,p,m=r,f)=>{let y={relativePath:f===void 0?h.path||"":f,caseSensitive:h.caseSensitive===!0,childrenIndex:p,route:h};if(y.relativePath.startsWith("/")){if(!y.relativePath.startsWith(s)&&m)return;Ce(y.relativePath.startsWith(s),`Absolute route path "${y.relativePath}" nested under path "${s}" is not valid. An absolute child route path must start with the combined path of all its parent routes.`),y.relativePath=y.relativePath.slice(s.length)}let v=Nt([s,y.relativePath]),b=n.concat(y);h.children&&h.children.length>0&&(Ce(h.index!==!0,`Index routes must not have child routes. Please remove all child routes from route path "${v}".`),Vf(h.children,i,b,v,m)),!(h.path==null&&!h.index)&&i.push({path:v,score:Ub(v,h.index),routesMeta:b.map((A,T)=>{let[I,M]=Bf(A.relativePath,A.caseSensitive,T===b.length-1);return{...A,matcher:I,compiledParams:M}})})};return t.forEach((h,p)=>{var m;if(h.path===""||!((m=h.path)!=null&&m.includes("?")))c(h,p);else for(let f of Nf(h.path))c(h,p,!0,f)}),i}function Nf(t){let i=t.split("/");if(i.length===0)return[];let[n,...s]=i,r=n.endsWith("?"),c=n.replace(/\?$/,"");if(s.length===0)return r?[c,""]:[c];let h=Nf(s.join("/")),p=[];return p.push(...h.map(m=>m===""?c:[c,m].join("/"))),r&&p.push(...h),p.map(m=>t.startsWith("/")&&m===""?"/":m)}function Tb(t){t.sort((i,n)=>i.score!==n.score?n.score-i.score:qb(i.routesMeta.map(s=>s.childrenIndex),n.routesMeta.map(s=>s.childrenIndex)))}var Ib=/^:[\w-]+$/,Mb=3,Pb=2,Db=1,Rb=10,Eb=-2,_p=t=>t==="*";function Ub(t,i){let n=t.split("/"),s=n.length;return n.some(_p)&&(s+=Eb),i&&(s+=Pb),n.filter(r=>!_p(r)).reduce((r,c)=>r+(Ib.test(c)?Mb:c===""?Db:Rb),s)}function qb(t,i){return t.length===i.length&&t.slice(0,-1).every((s,r)=>s===i[r])?t[t.length-1]-i[i.length-1]:0}function Ob(t,i,n=!1){let{routesMeta:s}=t,r={},c="/",h=[];for(let p=0;p<s.length;++p){let m=s[p],f=p===s.length-1,y=c==="/"?i:i.slice(c.length)||"/",v={path:m.relativePath,caseSensitive:m.caseSensitive,end:f},b=m.matcher&&m.compiledParams?Wf(v,y,m.matcher,m.compiledParams):Vs(v,y),A=m.route;if(!b&&f&&n&&!s[s.length-1].route.index&&(b=Vs({path:m.relativePath,caseSensitive:m.caseSensitive,end:!1},y)),!b)return null;Object.assign(r,b.params),h.push({params:r,pathname:Nt([c,b.pathname]),pathnameBase:Vb(Nt([c,b.pathnameBase])),route:A}),b.pathnameBase!=="/"&&(c=Nt([c,b.pathnameBase]))}return h}function Vs(t,i){typeof t=="string"&&(t={path:t,caseSensitive:!1,end:!0});let[n,s]=Bf(t.path,t.caseSensitive,t.end);return Wf(t,i,n,s)}function Wf(t,i,n,s){let r=i.match(n);if(!r)return null;let c=r[0],h=c.replace(/(.)\/+$/,"$1"),p=r.slice(1);return{params:s.reduce((f,{paramName:y,isOptional:v},b)=>{if(y==="*"){let T=p[b]||"";h=c.slice(0,c.length-T.length).replace(/(.)\/+$/,"$1")}const A=p[b];return v&&!A?f[y]=void 0:f[y]=(A||"").replace(/%2F/g,"/"),f},{}),pathname:c,pathnameBase:h,pattern:t}}function Bf(t,i=!1,n=!0){Wt(t==="*"||!t.endsWith("*")||t.endsWith("/*"),`Route path "${t}" will be treated as if it were "${t.replace(/\*$/,"/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${t.replace(/\*$/,"/*")}".`);let s=[],r="^"+t.replace(/\/*\*?$/,"").replace(/^\/*/,"/").replace(/[\\.*+^${}|()[\]]/g,"\\$&").replace(/\/:([\w-]+)(\?)?/g,(h,p,m,f,y)=>{if(s.push({paramName:p,isOptional:m!=null}),m){let v=y.charAt(f+h.length);return v&&v!=="/"?"/([^\\/]*)":"(?:/([^\\/]*))?"}return"/([^\\/]+)"}).replace(/\/([\w-]+)\?(\/|$)/g,"(/$1)?$2");return t.endsWith("*")?(s.push({paramName:"*"}),r+=t==="*"||t==="/*"?"(.*)$":"(?:\\/(.+)|\\/*)$"):n?r+="\\/*$":t!==""&&t!=="/"&&(r+="(?:(?=\\/|$))"),[new RegExp(r,i?void 0:"i"),s]}function Fb(t){try{return t.split("/").map(i=>decodeURIComponent(i).replace(/\//g,"%2F")).join("/")}catch(i){return Wt(!1,`The URL path "${t}" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (${i}).`),t}}function ha(t,i){if(i==="/")return t;if(!t.toLowerCase().startsWith(i.toLowerCase()))return null;let n=i.endsWith("/")?i.length-1:i.length,s=t.charAt(n);return s&&s!=="/"?null:t.slice(n)||"/"}function Lb(t,i="/"){let{pathname:n,search:s="",hash:r=""}=typeof t=="string"?Bi(t):t,c;return n?(n=zf(n),n.startsWith("/")?c=Gp(n.substring(1),"/"):c=Gp(n,i)):c=i,{pathname:c,search:Nb(s),hash:Wb(r)}}function Gp(t,i){let n=Ns(i).split("/");return t.split("/").forEach(r=>{r===".."?n.length>1&&n.pop():r!=="."&&n.push(r)}),n.length>1?n.join("/"):"/"}function cc(t,i,n,s){return`Cannot include a '${t}' character in a manually specified \`to.${i}\` field [${JSON.stringify(s)}].  Please separate it out to the \`to.${n}\` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.`}function jb(t){return t.filter((i,n)=>n===0||i.route.path&&i.route.path.length>0)}function ed(t){let i=jb(t);return i.map((n,s)=>s===i.length-1?n.pathname:n.pathnameBase)}function $s(t,i,n,s=!1){let r;typeof t=="string"?r=Bi(t):(r={...t},Ce(!r.pathname||!r.pathname.includes("?"),cc("?","pathname","search",r)),Ce(!r.pathname||!r.pathname.includes("#"),cc("#","pathname","hash",r)),Ce(!r.search||!r.search.includes("#"),cc("#","search","hash",r)));let c=t===""||r.pathname==="",h=c?"/":r.pathname,p;if(h==null)p=n;else{let v=i.length-1;if(!s&&h.startsWith("..")){let b=h.split("/");for(;b[0]==="..";)b.shift(),v-=1;r.pathname=b.join("/")}p=v>=0?i[v]:"/"}let m=Lb(r,p),f=h&&h!=="/"&&h.endsWith("/"),y=(c||h===".")&&n.endsWith("/");return!m.pathname.endsWith("/")&&(f||y)&&(m.pathname+="/"),m}var zf=t=>t.replace(/[\\/]{2,}/g,"/"),Nt=t=>zf(t.join("/")),Ns=t=>t.replace(/\/+$/,""),Vb=t=>Ns(t).replace(/^\/*/,"/"),Nb=t=>!t||t==="?"?"":t.startsWith("?")?t:"?"+t,Wb=t=>!t||t==="#"?"":t.startsWith("#")?t:"#"+t,Bb=class{constructor(t,i,n,s=!1){this.status=t,this.statusText=i||"",this.internal=s,n instanceof Error?(this.data=n.toString(),this.error=n):this.data=n}};function zb(t){return t!=null&&typeof t.status=="number"&&typeof t.statusText=="string"&&typeof t.internal=="boolean"&&"data"in t}function Hb(t){let i=t.map(n=>n.route.path).filter(Boolean);return Nt(i)||"/"}var Hf=typeof window<"u"&&typeof window.document<"u"&&typeof window.document.createElement<"u";function Kf(t,i){let n=t;if(typeof n!="string"||!Zc.test(n))return{absoluteURL:void 0,isExternal:!1,to:n};let s=n,r=!1;if(Hf)try{let c=new URL(window.location.href),h=Lf.test(n)?new URL(vb(n,c.protocol)):new URL(n),p=ha(h.pathname,i);h.origin===c.origin&&p!=null?n=p+h.search+h.hash:r=!0}catch{Wt(!1,`<Link to="${n}"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.`)}return{absoluteURL:s,isExternal:r,to:n}}Object.getOwnPropertyNames(Object.prototype).sort().join("\0");var _f=["POST","PUT","PATCH","DELETE"];new Set(_f);var Kb=["GET",..._f];new Set(Kb);var _b=["about:","blob:","chrome:","chrome-untrusted:","content:","data:","devtools:","file:","filesystem:","javascript:"];function Gb(t){try{return _b.includes(new URL(t).protocol)}catch{return!1}}var zi=x.createContext(null);zi.displayName="DataRouter";var Xs=x.createContext(null);Xs.displayName="DataRouterState";var Gf=x.createContext(!1);function Jb(){return x.useContext(Gf)}var Jf=x.createContext({isTransitioning:!1});Jf.displayName="ViewTransition";var Yb=x.createContext(new Map);Yb.displayName="Fetchers";var Qb=x.createContext(null);Qb.displayName="Await";var kt=x.createContext(null);kt.displayName="Navigation";var oo=x.createContext(null);oo.displayName="Location";var Bt=x.createContext({outlet:null,matches:[],isDataRoute:!1});Bt.displayName="Route";var td=x.createContext(null);td.displayName="RouteError";var Yf="REACT_ROUTER_ERROR",$b="REDIRECT",Xb="ROUTE_ERROR_RESPONSE";function Zb(t){if(t.startsWith(`${Yf}:${$b}:{`))try{let i=JSON.parse(t.slice(28));if(typeof i=="object"&&i&&typeof i.status=="number"&&typeof i.statusText=="string"&&typeof i.location=="string"&&typeof i.reloadDocument=="boolean"&&typeof i.replace=="boolean")return i}catch{}}function ek(t){if(t.startsWith(`${Yf}:${Xb}:{`))try{let i=JSON.parse(t.slice(40));if(typeof i=="object"&&i&&typeof i.status=="number"&&typeof i.statusText=="string")return new Bb(i.status,i.statusText,i.data)}catch{}}function tk(t,{relative:i}={}){Ce(Hi(),"useHref() may be used only in the context of a <Router> component.");let{basename:n,navigator:s}=x.useContext(kt),{hash:r,pathname:c,search:h}=so(t,{relative:i}),p=c;return n!=="/"&&(p=c==="/"?n:Nt([n,c])),s.createHref({pathname:p,search:h,hash:r})}function Hi(){return x.useContext(oo)!=null}function ea(){return Ce(Hi(),"useLocation() may be used only in the context of a <Router> component."),x.useContext(oo).location}var Qf="You should call navigate() in a React.useEffect(), not when your component is first rendered.";function $f(t){x.useContext(kt).static||x.useLayoutEffect(t)}function Xf(){let{isDataRoute:t}=x.useContext(Bt);return t?fk():ak()}function ak(){Ce(Hi(),"useNavigate() may be used only in the context of a <Router> component.");let t=x.useContext(zi),{basename:i,navigator:n}=x.useContext(kt),{matches:s}=x.useContext(Bt),{pathname:r}=ea(),c=JSON.stringify(ed(s)),h=x.useRef(!1);return $f(()=>{h.current=!0}),x.useCallback((m,f={})=>{if(Wt(h.current,Qf),!h.current)return;if(typeof m=="number"){n.go(m);return}let y=$s(m,JSON.parse(c),r,f.relative==="path");t==null&&i!=="/"&&(y.pathname=y.pathname==="/"?i:Nt([i,y.pathname])),(f.replace?n.replace:n.push)(y,f.state,f)},[i,n,c,r,t])}x.createContext(null);function ik(){let{matches:t}=x.useContext(Bt),i=t[t.length-1];return(i==null?void 0:i.params)??{}}function so(t,{relative:i}={}){let{matches:n}=x.useContext(Bt),{pathname:s}=ea(),r=JSON.stringify(ed(n));return x.useMemo(()=>$s(t,JSON.parse(r),s,i==="path"),[t,r,s,i])}function nk(t,i){return Zf(t,i)}function Zf(t,i,n){var M;Ce(Hi(),"useRoutes() may be used only in the context of a <Router> component.");let{navigator:s}=x.useContext(kt),{matches:r}=x.useContext(Bt),c=r[r.length-1],h=c?c.params:{},p=c?c.pathname:"/",m=c?c.pathnameBase:"/",f=c&&c.route;{let R=f&&f.path||"";tg(p,!f||R.endsWith("*")||R.endsWith("*?"),`You rendered descendant <Routes> (or called \`useRoutes()\`) at "${p}" (under <Route path="${R}">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won't match anymore and therefore the child routes will never render.

Please change the parent <Route path="${R}"> to <Route path="${R==="/"?"*":`${R}/*`}">.`)}let y=ea(),v;if(i){let R=typeof i=="string"?Bi(i):i;Ce(m==="/"||((M=R.pathname)==null?void 0:M.startsWith(m)),`When overriding the location using \`<Routes location>\` or \`useRoutes(routes, location)\`, the location pathname must begin with the portion of the URL pathname that was matched by all parent routes. The current pathname base is "${m}" but pathname "${R.pathname}" was given in the \`location\` prop.`),v=R}else v=y;let b=v.pathname||"/",A=b;if(m!=="/"){let R=m.replace(/^\//,"").split("/");A="/"+b.replace(/^\//,"").split("/").slice(R.length).join("/")}let T=n&&n.state.matches.length?n.state.matches.map(R=>Object.assign(R,{route:n.manifest[R.route.id]||R.route})):jf(t,{pathname:A});Wt(f||T!=null,`No routes matched location "${v.pathname}${v.search}${v.hash}" `),Wt(T==null||T[T.length-1].route.element!==void 0||T[T.length-1].route.Component!==void 0||T[T.length-1].route.lazy!==void 0,`Matched leaf route at location "${v.pathname}${v.search}${v.hash}" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.`);let I=ck(T&&T.map(R=>Object.assign({},R,{params:Object.assign({},h,R.params),pathname:Nt([m,s.encodeLocation?s.encodeLocation(R.pathname.replace(/%/g,"%25").replace(/\?/g,"%3F").replace(/#/g,"%23")).pathname:R.pathname]),pathnameBase:R.pathnameBase==="/"?m:Nt([m,s.encodeLocation?s.encodeLocation(R.pathnameBase.replace(/%/g,"%25").replace(/\?/g,"%3F").replace(/#/g,"%23")).pathname:R.pathnameBase])})),r,n);return i&&I?x.createElement(oo.Provider,{value:{location:{pathname:"/",search:"",hash:"",state:null,key:"default",mask:void 0,...v},navigationType:"POP"}},I):I}function ok(){let t=mk(),i=zb(t)?`${t.status} ${t.statusText}`:t instanceof Error?t.message:JSON.stringify(t),n=t instanceof Error?t.stack:null,s="rgba(200,200,200, 0.5)",r={padding:"0.5rem",backgroundColor:s},c={padding:"2px 4px",backgroundColor:s},h=null;return console.error("Error handled by React Router default ErrorBoundary:",t),h=x.createElement(x.Fragment,null,x.createElement("p",null,"💿 Hey developer 👋"),x.createElement("p",null,"You can provide a way better UX than this when your app throws errors by providing your own ",x.createElement("code",{style:c},"ErrorBoundary")," or"," ",x.createElement("code",{style:c},"errorElement")," prop on your route.")),x.createElement(x.Fragment,null,x.createElement("h2",null,"Unexpected Application Error!"),x.createElement("h3",{style:{fontStyle:"italic"}},i),n?x.createElement("pre",{style:r},n):null,h)}var sk=x.createElement(ok,null),eg=class extends x.Component{constructor(t){super(t),this.state={location:t.location,revalidation:t.revalidation,error:t.error}}static getDerivedStateFromError(t){return{error:t}}static getDerivedStateFromProps(t,i){return i.location!==t.location||i.revalidation!=="idle"&&t.revalidation==="idle"?{error:t.error,location:t.location,revalidation:t.revalidation}:{error:t.error!==void 0?t.error:i.error,location:i.location,revalidation:t.revalidation||i.revalidation}}componentDidCatch(t,i){this.props.onError?this.props.onError(t,i):console.error("React Router caught the following error during render",t)}render(){let t=this.state.error;if(this.context&&typeof t=="object"&&t&&"digest"in t&&typeof t.digest=="string"){const n=ek(t.digest);n&&(t=n)}let i=t!==void 0?x.createElement(Bt.Provider,{value:this.props.routeContext},x.createElement(td.Provider,{value:t,children:this.props.component})):this.props.children;return this.context?x.createElement(rk,{error:t},i):i}};eg.contextType=Gf;var dc=new WeakMap;function rk({children:t,error:i}){let{basename:n}=x.useContext(kt);if(typeof i=="object"&&i&&"digest"in i&&typeof i.digest=="string"){let s=Zb(i.digest);if(s){let r=dc.get(i);if(r)throw r;let c=Kf(s.location,n),h=c.absoluteURL||c.to;if(Gb(h))throw new Error("Invalid redirect location");if(Hf&&!dc.get(i))if(c.isExternal||s.reloadDocument)window.location.href=h;else{const p=Promise.resolve().then(()=>window.__reactRouterDataRouter.navigate(c.to,{replace:s.replace}));throw dc.set(i,p),p}return x.createElement("meta",{httpEquiv:"refresh",content:`0;url=${h}`})}}return t}function lk({routeContext:t,match:i,children:n}){let s=x.useContext(zi);return s&&s.static&&s.staticContext&&(i.route.errorElement||i.route.ErrorBoundary)&&(s.staticContext._deepestRenderedBoundaryId=i.route.id),x.createElement(Bt.Provider,{value:t},n)}function ck(t,i=[],n){let s=n==null?void 0:n.state;if(t==null){if(!s)return null;if(s.errors)t=s.matches;else if(i.length===0&&!s.initialized&&s.matches.length>0)t=s.matches;else return null}let r=t,c=s==null?void 0:s.errors;if(c!=null){let y=r.findIndex(v=>v.route.id&&(c==null?void 0:c[v.route.id])!==void 0);Ce(y>=0,`Could not find a matching route for errors on route IDs: ${Object.keys(c).join(",")}`),r=r.slice(0,Math.min(r.length,y+1))}let h=!1,p=-1;if(n&&s){h=s.renderFallback;for(let y=0;y<r.length;y++){let v=r[y];if((v.route.HydrateFallback||v.route.hydrateFallbackElement)&&(p=y),v.route.id){let{loaderData:b,errors:A}=s,T=v.route.loader&&!b.hasOwnProperty(v.route.id)&&(!A||A[v.route.id]===void 0);if(v.route.lazy||T){n.isStatic&&(h=!0),p>=0?r=r.slice(0,p+1):r=[r[0]];break}}}}let m=n==null?void 0:n.onError,f=s&&m?(y,v)=>{var b,A;m(y,{location:s.location,params:((A=(b=s.matches)==null?void 0:b[0])==null?void 0:A.params)??{},pattern:Hb(s.matches),errorInfo:v})}:void 0;return r.reduceRight((y,v,b)=>{let A,T=!1,I=null,M=null;s&&(A=c&&v.route.id?c[v.route.id]:void 0,I=v.route.errorElement||sk,h&&(p<0&&b===0?(tg("route-fallback",!1,"No `HydrateFallback` element provided to render during initial hydration"),T=!0,M=null):p===b&&(T=!0,M=v.route.hydrateFallbackElement||null)));let R=i.concat(r.slice(0,b+1)),O=()=>{let q;return A?q=I:T?q=M:v.route.Component?q=x.createElement(v.route.Component,null):v.route.element?q=v.route.element:q=y,x.createElement(lk,{match:v,routeContext:{outlet:y,matches:R,isDataRoute:s!=null},children:q})};return s&&(v.route.ErrorBoundary||v.route.errorElement||b===0)?x.createElement(eg,{location:s.location,revalidation:s.revalidation,component:I,error:A,children:O(),routeContext:{outlet:null,matches:R,isDataRoute:!0},onError:f}):O()},null)}function ad(t){return`${t} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`}function dk(t){let i=x.useContext(zi);return Ce(i,ad(t)),i}function hk(t){let i=x.useContext(Xs);return Ce(i,ad(t)),i}function uk(t){let i=x.useContext(Bt);return Ce(i,ad(t)),i}function id(t){let i=uk(t),n=i.matches[i.matches.length-1];return Ce(n.route.id,`${t} can only be used on routes that contain a unique "id"`),n.route.id}function pk(){return id("useRouteId")}function mk(){var s;let t=x.useContext(td),i=hk("useRouteError"),n=id("useRouteError");return t!==void 0?t:(s=i.errors)==null?void 0:s[n]}function fk(){let{router:t}=dk("useNavigate"),i=id("useNavigate"),n=x.useRef(!1);return $f(()=>{n.current=!0}),x.useCallback(async(r,c={})=>{Wt(n.current,Qf),n.current&&(typeof r=="number"?await t.navigate(r):await t.navigate(r,{fromRouteId:i,...c}))},[t,i])}var Jp={};function tg(t,i,n){!i&&!Jp[t]&&(Jp[t]=!0,Wt(!1,n))}x.memo(gk);function gk({routes:t,manifest:i,future:n,state:s,isStatic:r,onError:c}){return Zf(t,void 0,{manifest:i,state:s,isStatic:r,onError:c})}function ag({to:t,replace:i,state:n,relative:s}){Ce(Hi(),"<Navigate> may be used only in the context of a <Router> component.");let{static:r}=x.useContext(kt);Wt(!r,"<Navigate> must not be used on the initial render in a <StaticRouter>. This is a no-op, but you should modify your code so the <Navigate> is only ever rendered in response to some user interaction or state change.");let{matches:c}=x.useContext(Bt),{pathname:h}=ea(),p=Xf(),m=$s(t,ed(c),h,s==="path"),f=JSON.stringify(m);return x.useEffect(()=>{p(JSON.parse(f),{replace:i,state:n,relative:s})},[p,f,s,i,n]),null}function Bn(t){Ce(!1,"A <Route> is only ever to be used as the child of <Routes> element, never rendered directly. Please wrap your <Route> in a <Routes>.")}function yk({basename:t="/",children:i=null,location:n,navigationType:s="POP",navigator:r,static:c=!1,useTransitions:h}){Ce(!Hi(),"You cannot render a <Router> inside another <Router>. You should never have more than one in your app.");let p=t.replace(/^\/*/,"/"),m=x.useMemo(()=>({basename:p,navigator:r,static:c,useTransitions:h,future:{}}),[p,r,c,h]);typeof n=="string"&&(n=Bi(n));let{pathname:f="/",search:y="",hash:v="",state:b=null,key:A="default",mask:T}=n,I=x.useMemo(()=>{let M=ha(f,p);return M==null?null:{location:{pathname:M,search:y,hash:v,state:b,key:A,mask:T},navigationType:s}},[p,f,y,v,b,A,s,T]);return Wt(I!=null,`<Router basename="${p}"> is not able to match the URL "${f}${y}${v}" because it does not start with the basename, so the <Router> won't render anything.`),I==null?null:x.createElement(kt.Provider,{value:m},x.createElement(oo.Provider,{children:i,value:I}))}function wk({children:t,location:i}){return nk(Ec(t),i)}function Ec(t,i=[]){let n=[];return x.Children.forEach(t,(s,r)=>{if(!x.isValidElement(s))return;let c=[...i,r];if(s.type===x.Fragment){n.push.apply(n,Ec(s.props.children,c));return}Ce(s.type===Bn,`[${typeof s.type=="string"?s.type:s.type.name}] is not a <Route> component. All component children of <Routes> must be a <Route> or <React.Fragment>`),Ce(!s.props.index||!s.props.children,"An index route cannot have child routes.");let h={id:s.props.id||c.join("-"),caseSensitive:s.props.caseSensitive,element:s.props.element,Component:s.props.Component,index:s.props.index,path:s.props.path,middleware:s.props.middleware,loader:s.props.loader,action:s.props.action,hydrateFallbackElement:s.props.hydrateFallbackElement,HydrateFallback:s.props.HydrateFallback,errorElement:s.props.errorElement,ErrorBoundary:s.props.ErrorBoundary,hasErrorBoundary:s.props.hasErrorBoundary===!0||s.props.ErrorBoundary!=null||s.props.errorElement!=null,shouldRevalidate:s.props.shouldRevalidate,handle:s.props.handle,lazy:s.props.lazy};s.props.children&&(h.children=Ec(s.props.children,c)),n.push(h)}),n}var Es="get",Us="application/x-www-form-urlencoded";function Zs(t){return typeof HTMLElement<"u"&&t instanceof HTMLElement}function vk(t){return Zs(t)&&t.tagName.toLowerCase()==="button"}function bk(t){return Zs(t)&&t.tagName.toLowerCase()==="form"}function kk(t){return Zs(t)&&t.tagName.toLowerCase()==="input"}function xk(t){return!!(t.metaKey||t.altKey||t.ctrlKey||t.shiftKey)}function Sk(t,i){return t.button===0&&(!i||i==="_self")&&!xk(t)}var As=null;function Ak(){if(As===null)try{new FormData(document.createElement("form"),0),As=!1}catch{As=!0}return As}var Ck=new Set(["application/x-www-form-urlencoded","multipart/form-data","text/plain"]);function hc(t){return t!=null&&!Ck.has(t)?(Wt(!1,`"${t}" is not a valid \`encType\` for \`<Form>\`/\`<fetcher.Form>\` and will default to "${Us}"`),null):t}function Tk(t,i){let n,s,r,c,h;if(bk(t)){let p=t.getAttribute("action");s=p?ha(p,i):null,n=t.getAttribute("method")||Es,r=hc(t.getAttribute("enctype"))||Us,c=new FormData(t)}else if(vk(t)||kk(t)&&(t.type==="submit"||t.type==="image")){let p=t.form;if(p==null)throw new Error('Cannot submit a <button> or <input type="submit"> without a <form>');let m=t.getAttribute("formaction")||p.getAttribute("action");if(s=m?ha(m,i):null,n=t.getAttribute("formmethod")||p.getAttribute("method")||Es,r=hc(t.getAttribute("formenctype"))||hc(p.getAttribute("enctype"))||Us,c=new FormData(p,t),!Ak()){let{name:f,type:y,value:v}=t;if(y==="image"){let b=f?`${f}.`:"";c.append(`${b}x`,"0"),c.append(`${b}y`,"0")}else f&&c.append(f,v)}}else{if(Zs(t))throw new Error('Cannot submit element that is not <form>, <button>, or <input type="submit|image">');n=Es,s=null,r=Us,h=t}return c&&r==="text/plain"&&(h=c,c=void 0),{action:s,method:n.toLowerCase(),encType:r,formData:c,body:h}}Object.getOwnPropertyNames(Object.prototype).sort().join("\0");function nd(t,i){if(t===!1||t===null||typeof t>"u")throw new Error(i)}function ig(t,i,n,s){let r=typeof t=="string"?new URL(t,typeof window>"u"?"server://singlefetch/":window.location.origin):t;return n?r.pathname.endsWith("/")?r.pathname=`${r.pathname}_.${s}`:r.pathname=`${r.pathname}.${s}`:r.pathname==="/"?r.pathname=`_root.${s}`:i&&ha(r.pathname,i)==="/"?r.pathname=`${Ns(i)}/_root.${s}`:r.pathname=`${Ns(r.pathname)}.${s}`,r}async function Ik(t,i){if(t.id in i)return i[t.id];try{let n=await import(t.module);return i[t.id]=n,n}catch(n){return console.error(`Error loading route module \`${t.module}\`, reloading page...`),console.error(n),window.__reactRouterContext&&window.__reactRouterContext.isSpaMode,window.location.reload(),new Promise(()=>{})}}function Mk(t){return t==null?!1:t.href==null?t.rel==="preload"&&typeof t.imageSrcSet=="string"&&typeof t.imageSizes=="string":typeof t.rel=="string"&&typeof t.href=="string"}async function Pk(t,i,n){let s=await Promise.all(t.map(async r=>{let c=i.routes[r.route.id];if(c){let h=await Ik(c,n);return h.links?h.links():[]}return[]}));return Uk(s.flat(1).filter(Mk).filter(r=>r.rel==="stylesheet"||r.rel==="preload").map(r=>r.rel==="stylesheet"?{...r,rel:"prefetch",as:"style"}:{...r,rel:"prefetch"}))}function Yp(t,i,n,s,r,c){let h=(m,f)=>n[f]?m.route.id!==n[f].route.id:!0,p=(m,f)=>{var y;return n[f].pathname!==m.pathname||((y=n[f].route.path)==null?void 0:y.endsWith("*"))&&n[f].params["*"]!==m.params["*"]};return c==="assets"?i.filter((m,f)=>h(m,f)||p(m,f)):c==="data"?i.filter((m,f)=>{var v;let y=s.routes[m.route.id];if(!y||!y.hasLoader)return!1;if(h(m,f)||p(m,f))return!0;if(m.route.shouldRevalidate){let b=m.route.shouldRevalidate({currentUrl:new URL(r.pathname+r.search+r.hash,window.origin),currentParams:((v=n[0])==null?void 0:v.params)||{},nextUrl:new URL(t,window.origin),nextParams:m.params,defaultShouldRevalidate:!0});if(typeof b=="boolean")return b}return!0}):[]}function Dk(t,i,{includeHydrateFallback:n}={}){return Rk(t.map(s=>{let r=i.routes[s.route.id];if(!r)return[];let c=[r.module];return r.clientActionModule&&(c=c.concat(r.clientActionModule)),r.clientLoaderModule&&(c=c.concat(r.clientLoaderModule)),n&&r.hydrateFallbackModule&&(c=c.concat(r.hydrateFallbackModule)),r.imports&&(c=c.concat(r.imports)),c}).flat(1))}function Rk(t){return[...new Set(t)]}function Ek(t){let i={},n=Object.keys(t).sort();for(let s of n)i[s]=t[s];return i}function Uk(t,i){let n=new Set;return new Set(i),t.reduce((s,r)=>{let c=JSON.stringify(Ek(r));return n.has(c)||(n.add(c),s.push({key:c,link:r})),s},[])}function od(){let t=x.useContext(zi);return nd(t,"You must render this element inside a <DataRouterContext.Provider> element"),t}function qk(){let t=x.useContext(Xs);return nd(t,"You must render this element inside a <DataRouterStateContext.Provider> element"),t}var sd=x.createContext(void 0);sd.displayName="FrameworkContext";function er(){let t=x.useContext(sd);return nd(t,"You must render this element inside a <HydratedRouter> element"),t}function Ok(t,i){let n=x.useContext(sd),[s,r]=x.useState(!1),[c,h]=x.useState(!1),{onFocus:p,onBlur:m,onMouseEnter:f,onMouseLeave:y,onTouchStart:v}=i,b=x.useRef(null);x.useEffect(()=>{if(t==="render"&&h(!0),t==="viewport"){let I=R=>{R.forEach(O=>{h(O.isIntersecting)})},M=new IntersectionObserver(I,{threshold:.5});return b.current&&M.observe(b.current),()=>{M.disconnect()}}},[t]),x.useEffect(()=>{if(s){let I=setTimeout(()=>{h(!0)},100);return()=>{clearTimeout(I)}}},[s]);let A=()=>{r(!0)},T=()=>{r(!1),h(!1)};return n?t!=="intent"?[c,b,{}]:[c,b,{onFocus:Ln(p,A),onBlur:Ln(m,T),onMouseEnter:Ln(f,A),onMouseLeave:Ln(y,T),onTouchStart:Ln(v,A)}]:[!1,b,{}]}function Ln(t,i){return n=>{t&&t(n),n.defaultPrevented||i(n)}}function Fk({page:t,...i}){let n=Jb(),{nonce:s}=er(),{router:r}=od(),c=x.useMemo(()=>jf(r.routes,t,r.basename),[r.routes,t,r.basename]);return c?(i.nonce==null&&s&&(i={...i,nonce:s}),n?x.createElement(jk,{page:t,matches:c,...i}):x.createElement(Vk,{page:t,matches:c,...i})):null}function Lk(t){let{manifest:i,routeModules:n}=er(),[s,r]=x.useState([]);return x.useEffect(()=>{let c=!1;return Pk(t,i,n).then(h=>{c||r(h)}),()=>{c=!0}},[t,i,n]),s}function jk({page:t,matches:i,...n}){let s=ea(),{future:r}=er(),{basename:c}=od(),h=x.useMemo(()=>{if(t===s.pathname+s.search+s.hash)return[];let p=ig(t,c,r.v8_trailingSlashAwareDataRequests,"rsc"),m=!1,f=[];for(let y of i)typeof y.route.shouldRevalidate=="function"?m=!0:f.push(y.route.id);return m&&f.length>0&&p.searchParams.set("_routes",f.join(",")),[p.pathname+p.search]},[c,r.v8_trailingSlashAwareDataRequests,t,s,i]);return x.createElement(x.Fragment,null,h.map(p=>x.createElement("link",{key:p,rel:"prefetch",as:"fetch",href:p,...n})))}function Vk({page:t,matches:i,...n}){let s=ea(),{future:r,manifest:c,routeModules:h}=er(),{basename:p}=od(),{loaderData:m,matches:f}=qk(),y=x.useMemo(()=>Yp(t,i,f,c,s,"data"),[t,i,f,c,s]),v=x.useMemo(()=>Yp(t,i,f,c,s,"assets"),[t,i,f,c,s]),b=x.useMemo(()=>{if(t===s.pathname+s.search+s.hash)return[];let I=new Set,M=!1;if(i.forEach(O=>{var z;let q=c.routes[O.route.id];!q||!q.hasLoader||(!y.some(V=>V.route.id===O.route.id)&&O.route.id in m&&((z=h[O.route.id])!=null&&z.shouldRevalidate)||q.hasClientLoader?M=!0:I.add(O.route.id))}),I.size===0)return[];let R=ig(t,p,r.v8_trailingSlashAwareDataRequests,"data");return M&&I.size>0&&R.searchParams.set("_routes",i.filter(O=>I.has(O.route.id)).map(O=>O.route.id).join(",")),[R.pathname+R.search]},[p,r.v8_trailingSlashAwareDataRequests,m,s,c,y,i,t,h]),A=x.useMemo(()=>Dk(v,c),[v,c]),T=Lk(v);return x.createElement(x.Fragment,null,b.map(I=>x.createElement("link",{key:I,rel:"prefetch",as:"fetch",href:I,...n})),A.map(I=>x.createElement("link",{key:I,rel:"modulepreload",href:I,...n})),T.map(({key:I,link:M})=>x.createElement("link",{key:I,nonce:n.nonce,...M,crossOrigin:M.crossOrigin??n.crossOrigin})))}function Nk(...t){return i=>{t.forEach(n=>{typeof n=="function"?n(i):n!=null&&(n.current=i)})}}var Wk=typeof window<"u"&&typeof window.document<"u"&&typeof window.document.createElement<"u";try{Wk&&(window.__reactRouterVersion="7.18.1")}catch{}function Bk({basename:t,children:i,useTransitions:n,window:s}){let r=x.useRef();r.current==null&&(r.current=bb({window:s,v5Compat:!0}));let c=r.current,[h,p]=x.useState({action:c.action,location:c.location}),m=x.useCallback(f=>{n===!1?p(f):x.startTransition(()=>p(f))},[n]);return x.useLayoutEffect(()=>c.listen(m),[c,m]),x.createElement(yk,{basename:t,children:i,location:h.location,navigationType:h.action,navigator:c,useTransitions:n})}var Xn=x.forwardRef(function({onClick:i,discover:n="render",prefetch:s="none",relative:r,reloadDocument:c,replace:h,mask:p,state:m,target:f,to:y,preventScrollReset:v,viewTransition:b,defaultShouldRevalidate:A,...T},I){let{basename:M,navigator:R,useTransitions:O}=x.useContext(kt),q=typeof y=="string"&&Zc.test(y),z=Kf(y,M);y=z.to;let V=tk(y,{relative:r}),K=ea(),ie=null;if(p){let Ee=$s(p,[],K.mask?K.mask.pathname:"/",!0);M!=="/"&&(Ee.pathname=Ee.pathname==="/"?M:Nt([M,Ee.pathname])),ie=R.createHref(Ee)}let[J,ce,we]=Ok(s,T),Te=_k(y,{replace:h,mask:p,state:m,target:f,preventScrollReset:v,relative:r,viewTransition:b,defaultShouldRevalidate:A,useTransitions:O});function Ke(Ee){i&&i(Ee),Ee.defaultPrevented||Te(Ee)}let je=!(z.isExternal||c),Ve=x.createElement("a",{...T,...we,href:(je?ie:void 0)||z.absoluteURL||V,onClick:je?Ke:i,ref:Nk(I,ce),target:f,"data-discover":!q&&n==="render"?"true":void 0});return J&&!q?x.createElement(x.Fragment,null,Ve,x.createElement(Fk,{page:V})):Ve});Xn.displayName="Link";var zk=x.forwardRef(function({"aria-current":i="page",caseSensitive:n=!1,className:s="",end:r=!1,style:c,to:h,viewTransition:p,children:m,...f},y){let v=so(h,{relative:f.relative}),b=ea(),A=x.useContext(Xs),{navigator:T,basename:I}=x.useContext(kt),M=A!=null&&$k(v)&&p===!0,R=T.encodeLocation?T.encodeLocation(v).pathname:v.pathname,O=b.pathname,q=A&&A.navigation&&A.navigation.location?A.navigation.location.pathname:null;n||(O=O.toLowerCase(),q=q?q.toLowerCase():null,R=R.toLowerCase()),q&&I&&(q=ha(q,I)||q);const z=R!=="/"&&R.endsWith("/")?R.length-1:R.length;let V=O===R||!r&&O.startsWith(R)&&O.charAt(z)==="/",K=q!=null&&(q===R||!r&&q.startsWith(R)&&q.charAt(R.length)==="/"),ie={isActive:V,isPending:K,isTransitioning:M},J=V?i:void 0,ce;typeof s=="function"?ce=s(ie):ce=[s,V?"active":null,K?"pending":null,M?"transitioning":null].filter(Boolean).join(" ");let we=typeof c=="function"?c(ie):c;return x.createElement(Xn,{...f,"aria-current":J,className:ce,ref:y,style:we,to:h,viewTransition:p},typeof m=="function"?m(ie):m)});zk.displayName="NavLink";var Hk=x.forwardRef(({discover:t="render",fetcherKey:i,navigate:n,reloadDocument:s,replace:r,state:c,method:h=Es,action:p,onSubmit:m,relative:f,preventScrollReset:y,viewTransition:v,defaultShouldRevalidate:b,...A},T)=>{let{useTransitions:I}=x.useContext(kt),M=Yk(),R=Qk(p,{relative:f}),O=h.toLowerCase()==="get"?"get":"post",q=typeof p=="string"&&Zc.test(p),z=V=>{if(m&&m(V),V.defaultPrevented)return;V.preventDefault();let K=V.nativeEvent.submitter,ie=(K==null?void 0:K.getAttribute("formmethod"))||h,J=()=>M(K||V.currentTarget,{fetcherKey:i,method:ie,navigate:n,replace:r,state:c,relative:f,preventScrollReset:y,viewTransition:v,defaultShouldRevalidate:b});I&&n!==!1?x.startTransition(()=>J()):J()};return x.createElement("form",{ref:T,method:O,action:R,onSubmit:s?m:z,...A,"data-discover":!q&&t==="render"?"true":void 0})});Hk.displayName="Form";function Kk(t){return`${t} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`}function ng(t){let i=x.useContext(zi);return Ce(i,Kk(t)),i}function _k(t,{target:i,replace:n,mask:s,state:r,preventScrollReset:c,relative:h,viewTransition:p,defaultShouldRevalidate:m,useTransitions:f}={}){let y=Xf(),v=ea(),b=so(t,{relative:h});return x.useCallback(A=>{if(Sk(A,i)){A.preventDefault();let T=n!==void 0?n:$n(v)===$n(b),I=()=>y(t,{replace:T,mask:s,state:r,preventScrollReset:c,relative:h,viewTransition:p,defaultShouldRevalidate:m});f?x.startTransition(()=>I()):I()}},[v,y,b,n,s,r,i,t,c,h,p,m,f])}var Gk=0,Jk=()=>`__${String(++Gk)}__`;function Yk(){let{router:t}=ng("useSubmit"),{basename:i}=x.useContext(kt),n=pk(),s=t.fetch,r=t.navigate;return x.useCallback(async(c,h={})=>{let{action:p,method:m,encType:f,formData:y,body:v}=Tk(c,i);if(h.navigate===!1){let b=h.fetcherKey||Jk();await s(b,n,h.action||p,{defaultShouldRevalidate:h.defaultShouldRevalidate,preventScrollReset:h.preventScrollReset,formData:y,body:v,formMethod:h.method||m,formEncType:h.encType||f,flushSync:h.flushSync})}else await r(h.action||p,{defaultShouldRevalidate:h.defaultShouldRevalidate,preventScrollReset:h.preventScrollReset,formData:y,body:v,formMethod:h.method||m,formEncType:h.encType||f,replace:h.replace,state:h.state,fromRouteId:n,flushSync:h.flushSync,viewTransition:h.viewTransition})},[s,r,i,n])}function Qk(t,{relative:i}={}){let{basename:n}=x.useContext(kt),s=x.useContext(Bt);Ce(s,"useFormAction must be used inside a RouteContext");let[r]=s.matches.slice(-1),c={...so(t||".",{relative:i})},h=ea();if(t==null){c.search=h.search;let p=new URLSearchParams(c.search),m=p.getAll("index");if(m.some(y=>y==="")){p.delete("index"),m.filter(v=>v).forEach(v=>p.append("index",v));let y=p.toString();c.search=y?`?${y}`:""}}return(!t||t===".")&&r.route.index&&(c.search=c.search?c.search.replace(/^\?/,"?index&"):"?index"),n!=="/"&&(c.pathname=c.pathname==="/"?n:Nt([n,c.pathname])),$n(c)}function $k(t,{relative:i}={}){let n=x.useContext(Jf);Ce(n!=null,"`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?");let{basename:s}=ng("useViewTransitionState"),r=so(t,{relative:i});if(!n.isTransitioning)return!1;let c=ha(n.currentLocation.pathname,s)||n.currentLocation.pathname,h=ha(n.nextLocation.pathname,s)||n.nextLocation.pathname;return Vs(r.pathname,h)!=null||Vs(r.pathname,c)!=null}var Qp="1.3.25";function og(t,i,n){return Math.max(t,Math.min(i,n))}function Xk(t,i,n){return(1-n)*t+n*i}function Zk(t,i,n,s){return Xk(t,i,1-Math.exp(-n*s))}function ex(t,i){return(t%i+i)%i}var tx=class{constructor(){X(this,"isRunning",!1);X(this,"value",0);X(this,"from",0);X(this,"to",0);X(this,"currentTime",0);X(this,"lerp");X(this,"duration");X(this,"easing");X(this,"onUpdate")}advance(t){var n;if(!this.isRunning)return;let i=!1;if(this.duration&&this.easing){this.currentTime+=t;const s=og(0,this.currentTime/this.duration,1);i=s>=1;const r=i?1:this.easing(s);this.value=this.from+(this.to-this.from)*r}else this.lerp?(this.value=Zk(this.value,this.to,this.lerp*60,t),Math.round(this.value)===Math.round(this.to)&&(this.value=this.to,i=!0)):(this.value=this.to,i=!0);i&&this.stop(),(n=this.onUpdate)==null||n.call(this,this.value,i)}stop(){this.isRunning=!1}fromTo(t,i,{lerp:n,duration:s,easing:r,onStart:c,onUpdate:h}){this.from=this.value=t,this.to=i,this.lerp=n,this.duration=s,this.easing=r,this.currentTime=0,this.isRunning=!0,c==null||c(),this.onUpdate=h}};function ax(t,i){let n;return function(...s){clearTimeout(n),n=setTimeout(()=>{n=void 0,t.apply(this,s)},i)}}var ix=class{constructor(t,i,{autoResize:n=!0,debounce:s=250}={}){X(this,"width",0);X(this,"height",0);X(this,"scrollHeight",0);X(this,"scrollWidth",0);X(this,"debouncedResize");X(this,"wrapperResizeObserver");X(this,"contentResizeObserver");X(this,"resize",()=>{this.onWrapperResize(),this.onContentResize()});X(this,"onWrapperResize",()=>{this.wrapper instanceof Window?(this.width=window.innerWidth,this.height=window.innerHeight):(this.width=this.wrapper.clientWidth,this.height=this.wrapper.clientHeight)});X(this,"onContentResize",()=>{this.wrapper instanceof Window?(this.scrollHeight=this.content.scrollHeight,this.scrollWidth=this.content.scrollWidth):(this.scrollHeight=this.wrapper.scrollHeight,this.scrollWidth=this.wrapper.scrollWidth)});this.wrapper=t,this.content=i,n&&(this.debouncedResize=ax(this.resize,s),this.wrapper instanceof Window?window.addEventListener("resize",this.debouncedResize):(this.wrapperResizeObserver=new ResizeObserver(this.debouncedResize),this.wrapperResizeObserver.observe(this.wrapper)),this.contentResizeObserver=new ResizeObserver(this.debouncedResize),this.contentResizeObserver.observe(this.content)),this.resize()}destroy(){var t,i;(t=this.wrapperResizeObserver)==null||t.disconnect(),(i=this.contentResizeObserver)==null||i.disconnect(),this.wrapper===window&&this.debouncedResize&&window.removeEventListener("resize",this.debouncedResize)}get limit(){return{x:this.scrollWidth-this.width,y:this.scrollHeight-this.height}}},sg=class{constructor(){X(this,"events",{})}emit(t,...i){var s;const n=this.events[t]||[];for(let r=0,c=n.length;r<c;r++)(s=n[r])==null||s.call(n,...i)}on(t,i){return this.events[t]?this.events[t].push(i):this.events[t]=[i],()=>{var n;this.events[t]=(n=this.events[t])==null?void 0:n.filter(s=>i!==s)}}off(t,i){var n;this.events[t]=(n=this.events[t])==null?void 0:n.filter(s=>i!==s)}destroy(){this.events={}}};const nx=100/6,Ra={passive:!1};function $p(t,i){return t===1?nx:t===2?i:1}var ox=class{constructor(t,i={wheelMultiplier:1,touchMultiplier:1}){X(this,"touchStart",{x:0,y:0});X(this,"lastDelta",{x:0,y:0});X(this,"window",{width:0,height:0});X(this,"emitter",new sg);X(this,"onTouchStart",t=>{const{clientX:i,clientY:n}=t.targetTouches?t.targetTouches[0]:t;this.touchStart.x=i,this.touchStart.y=n,this.lastDelta={x:0,y:0},this.emitter.emit("scroll",{deltaX:0,deltaY:0,event:t})});X(this,"onTouchMove",t=>{const{clientX:i,clientY:n}=t.targetTouches?t.targetTouches[0]:t,s=-(i-this.touchStart.x)*this.options.touchMultiplier,r=-(n-this.touchStart.y)*this.options.touchMultiplier;this.touchStart.x=i,this.touchStart.y=n,this.lastDelta={x:s,y:r},this.emitter.emit("scroll",{deltaX:s,deltaY:r,event:t})});X(this,"onTouchEnd",t=>{this.emitter.emit("scroll",{deltaX:this.lastDelta.x,deltaY:this.lastDelta.y,event:t})});X(this,"onWheel",t=>{let{deltaX:i,deltaY:n,deltaMode:s}=t;const r=$p(s,this.window.width),c=$p(s,this.window.height);i*=r,n*=c,i*=this.options.wheelMultiplier,n*=this.options.wheelMultiplier,this.emitter.emit("scroll",{deltaX:i,deltaY:n,event:t})});X(this,"onWindowResize",()=>{this.window={width:window.innerWidth,height:window.innerHeight}});this.element=t,this.options=i,window.addEventListener("resize",this.onWindowResize),this.onWindowResize(),this.element.addEventListener("wheel",this.onWheel,Ra),this.element.addEventListener("touchstart",this.onTouchStart,Ra),this.element.addEventListener("touchmove",this.onTouchMove,Ra),this.element.addEventListener("touchend",this.onTouchEnd,Ra)}on(t,i){return this.emitter.on(t,i)}destroy(){this.emitter.destroy(),window.removeEventListener("resize",this.onWindowResize),this.element.removeEventListener("wheel",this.onWheel,Ra),this.element.removeEventListener("touchstart",this.onTouchStart,Ra),this.element.removeEventListener("touchmove",this.onTouchMove,Ra),this.element.removeEventListener("touchend",this.onTouchEnd,Ra)}};const Xp=t=>Math.min(1,1.001-2**(-10*t));var sx=class{constructor({wrapper:t=window,content:i=document.documentElement,eventsTarget:n=t,smoothWheel:s=!0,syncTouch:r=!1,syncTouchLerp:c=.075,touchInertiaExponent:h=1.7,duration:p,easing:m,lerp:f=.1,infinite:y=!1,orientation:v="vertical",gestureOrientation:b=v==="horizontal"?"both":"vertical",touchMultiplier:A=1,wheelMultiplier:T=1,autoResize:I=!0,prevent:M,virtualScroll:R,overscroll:O=!0,autoRaf:q=!1,anchors:z=!1,autoToggle:V=!1,allowNestedScroll:K=!1,__experimental__naiveDimensions:ie=!1,naiveDimensions:J=ie,stopInertiaOnNavigate:ce=!1}={}){X(this,"_isScrolling",!1);X(this,"_isStopped",!1);X(this,"_isLocked",!1);X(this,"_preventNextNativeScrollEvent",!1);X(this,"_resetVelocityTimeout",null);X(this,"_rafId",null);X(this,"_isDraggingSelection",!1);X(this,"isTouching");X(this,"isIos");X(this,"time",0);X(this,"userData",{});X(this,"lastVelocity",0);X(this,"velocity",0);X(this,"direction",0);X(this,"options");X(this,"targetScroll");X(this,"animatedScroll");X(this,"animate",new tx);X(this,"emitter",new sg);X(this,"dimensions");X(this,"virtualScroll");X(this,"onScrollEnd",t=>{t instanceof CustomEvent||(this.isScrolling==="smooth"||this.isScrolling===!1)&&t.stopPropagation()});X(this,"dispatchScrollendEvent",()=>{this.options.wrapper.dispatchEvent(new CustomEvent("scrollend",{bubbles:this.options.wrapper===window,detail:{lenisScrollEnd:!0}}))});X(this,"onTransitionEnd",t=>{var i;(i=t.propertyName)!=null&&i.includes("overflow")&&t.target===this.rootElement&&this.checkOverflow()});X(this,"onClick",t=>{const i=t.composedPath().filter(s=>s instanceof HTMLAnchorElement&&s.href).map(s=>new URL(s.href)),n=new URL(window.location.href);if(this.options.anchors){const s=i.find(r=>n.host===r.host&&n.pathname===r.pathname&&r.hash);if(s){const r=typeof this.options.anchors=="object"&&this.options.anchors?this.options.anchors:void 0,c=decodeURIComponent(s.hash);this.scrollTo(c,r);return}}if(this.options.stopInertiaOnNavigate&&i.some(s=>n.host===s.host&&n.pathname!==s.pathname)){this.reset();return}});X(this,"onPointerDown",t=>{t.button===1&&this.reset()});X(this,"onVirtualScroll",t=>{if(typeof this.options.virtualScroll=="function"&&this.options.virtualScroll(t)===!1)return;const{deltaX:i,deltaY:n,event:s}=t;if(this.emitter.emit("virtual-scroll",{deltaX:i,deltaY:n,event:s}),s.ctrlKey||s.lenisStopPropagation)return;const r=s.type.includes("touch"),c=s.type.includes("wheel");if(r&&this.isIos&&(s.type==="touchstart"&&(this._isDraggingSelection=this.isTouchOnSelectionHandle(s)),this._isDraggingSelection)){s.type==="touchend"&&(this._isDraggingSelection=!1);return}this.isTouching=s.type==="touchstart"||s.type==="touchmove";const h=i===0&&n===0;if(this.options.syncTouch&&r&&s.type==="touchstart"&&h&&!this.isStopped&&!this.isLocked){this.reset();return}const p=this.options.gestureOrientation==="vertical"&&n===0||this.options.gestureOrientation==="horizontal"&&i===0;if(h||p)return;let m=s.composedPath();m=m.slice(0,m.indexOf(this.rootElement));const f=this.options.prevent,y=Math.abs(i)>=Math.abs(n)?"horizontal":"vertical";if(m.find(T=>{var I,M,R,O,q;return T instanceof HTMLElement&&(typeof f=="function"&&(f==null?void 0:f(T))||((I=T.hasAttribute)==null?void 0:I.call(T,"data-lenis-prevent"))||y==="vertical"&&((M=T.hasAttribute)==null?void 0:M.call(T,"data-lenis-prevent-vertical"))||y==="horizontal"&&((R=T.hasAttribute)==null?void 0:R.call(T,"data-lenis-prevent-horizontal"))||r&&((O=T.hasAttribute)==null?void 0:O.call(T,"data-lenis-prevent-touch"))||c&&((q=T.hasAttribute)==null?void 0:q.call(T,"data-lenis-prevent-wheel"))||this.options.allowNestedScroll&&this.hasNestedScroll(T,{deltaX:i,deltaY:n}))}))return;if(this.isStopped||this.isLocked){s.cancelable&&s.preventDefault();return}if(!(this.options.syncTouch&&r||this.options.smoothWheel&&c)){this.isScrolling="native",this.animate.stop(),s.lenisStopPropagation=!0;return}let v=n;this.options.gestureOrientation==="both"?v=Math.abs(n)>Math.abs(i)?n:i:this.options.gestureOrientation==="horizontal"&&(v=i),(!this.options.overscroll||this.options.infinite||this.options.wrapper!==window&&this.limit>0&&(this.animatedScroll>0&&this.animatedScroll<this.limit||this.animatedScroll===0&&n>0||this.animatedScroll===this.limit&&n<0))&&(s.lenisStopPropagation=!0),s.cancelable&&s.preventDefault();const b=r&&this.options.syncTouch,A=r&&s.type==="touchend";A&&(v=Math.sign(v)*Math.abs(this.velocity)**this.options.touchInertiaExponent),this.scrollTo(this.targetScroll+v,{programmatic:!1,...b?{lerp:A?this.options.syncTouchLerp:1}:{lerp:this.options.lerp,duration:this.options.duration,easing:this.options.easing}})});X(this,"onNativeScroll",()=>{if(this._resetVelocityTimeout!==null&&(clearTimeout(this._resetVelocityTimeout),this._resetVelocityTimeout=null),this._preventNextNativeScrollEvent){this._preventNextNativeScrollEvent=!1;return}if(this.isScrolling===!1||this.isScrolling==="native"){const t=this.animatedScroll;this.animatedScroll=this.targetScroll=this.actualScroll,this.lastVelocity=this.velocity,this.velocity=this.animatedScroll-t,this.direction=Math.sign(this.animatedScroll-t),this.isStopped||(this.isScrolling="native"),this.emit(),this.velocity!==0&&(this._resetVelocityTimeout=setTimeout(()=>{this.lastVelocity=this.velocity,this.velocity=0,this.isScrolling=!1,this.emit()},400))}});X(this,"raf",t=>{const i=t-(this.time||t);this.time=t,this.animate.advance(i*.001),this.options.autoRaf&&(this._rafId=requestAnimationFrame(this.raf))});window.lenisVersion=Qp,window.lenis||(window.lenis={}),window.lenis.version=Qp,v==="horizontal"&&(window.lenis.horizontal=!0),r===!0&&(window.lenis.touch=!0),this.isIos=/(iPad|iPhone|iPod)/g.test(navigator.userAgent),(!t||t===document.documentElement)&&(t=window),typeof p=="number"&&typeof m!="function"?m=Xp:typeof m=="function"&&typeof p!="number"&&(p=1),this.options={wrapper:t,content:i,eventsTarget:n,smoothWheel:s,syncTouch:r,syncTouchLerp:c,touchInertiaExponent:h,duration:p,easing:m,lerp:f,infinite:y,gestureOrientation:b,orientation:v,touchMultiplier:A,wheelMultiplier:T,autoResize:I,prevent:M,virtualScroll:R,overscroll:O,autoRaf:q,anchors:z,autoToggle:V,allowNestedScroll:K,naiveDimensions:J,stopInertiaOnNavigate:ce},this.dimensions=new ix(t,i,{autoResize:I}),this.updateClassName(),this.targetScroll=this.animatedScroll=this.actualScroll,this.options.wrapper.addEventListener("scroll",this.onNativeScroll),this.options.wrapper.addEventListener("scrollend",this.onScrollEnd,{capture:!0}),(this.options.anchors||this.options.stopInertiaOnNavigate)&&this.options.wrapper.addEventListener("click",this.onClick),this.options.wrapper.addEventListener("pointerdown",this.onPointerDown),this.virtualScroll=new ox(n,{touchMultiplier:A,wheelMultiplier:T}),this.virtualScroll.on("scroll",this.onVirtualScroll),this.options.autoToggle&&(this.checkOverflow(),this.rootElement.addEventListener("transitionend",this.onTransitionEnd)),this.options.autoRaf&&(this._rafId=requestAnimationFrame(this.raf))}destroy(){this.emitter.destroy(),this.options.wrapper.removeEventListener("scroll",this.onNativeScroll),this.options.wrapper.removeEventListener("scrollend",this.onScrollEnd,{capture:!0}),this.options.wrapper.removeEventListener("pointerdown",this.onPointerDown),(this.options.anchors||this.options.stopInertiaOnNavigate)&&this.options.wrapper.removeEventListener("click",this.onClick),this.virtualScroll.destroy(),this.dimensions.destroy(),this.cleanUpClassName(),this._rafId&&cancelAnimationFrame(this._rafId)}on(t,i){return this.emitter.on(t,i)}off(t,i){return this.emitter.off(t,i)}get overflow(){const t=this.isHorizontal?"overflow-x":"overflow-y";return getComputedStyle(this.rootElement)[t]}checkOverflow(){["hidden","clip"].includes(this.overflow)?this.internalStop():this.internalStart()}setScroll(t){this.isHorizontal?this.options.wrapper.scrollTo({left:t,behavior:"instant"}):this.options.wrapper.scrollTo({top:t,behavior:"instant"})}isTouchOnSelectionHandle(t){const i=window.getSelection();if(!i||i.isCollapsed||i.rangeCount===0)return!1;const n=t.targetTouches[0]??t.changedTouches[0];if(!n)return!1;const s=i.getRangeAt(0).getClientRects();if(s.length===0)return!1;const r=s[0],c=s[s.length-1],h=40,p=Math.hypot(n.clientX-r.left,n.clientY-r.top)<=h,m=Math.hypot(n.clientX-c.right,n.clientY-c.bottom)<=h;return p||m}resize(){this.dimensions.resize(),this.animatedScroll=this.targetScroll=this.actualScroll,this.emit()}emit(){this.emitter.emit("scroll",this)}reset(){this.isLocked=!1,this.isScrolling=!1,this.animatedScroll=this.targetScroll=this.actualScroll,this.lastVelocity=this.velocity=0,this.animate.stop()}start(){if(this.isStopped){if(this.options.autoToggle){this.rootElement.style.removeProperty("overflow");return}this.internalStart()}}internalStart(){this.isStopped&&(this.reset(),this.isStopped=!1,this.emit())}stop(){if(!this.isStopped){if(this.options.autoToggle){this.rootElement.style.setProperty("overflow","clip");return}this.internalStop()}}internalStop(){this.isStopped||(this.reset(),this.isStopped=!0,this.emit())}scrollTo(t,{offset:i=0,immediate:n=!1,lock:s=!1,programmatic:r=!0,lerp:c=r?this.options.lerp:void 0,duration:h=r?this.options.duration:void 0,easing:p=r?this.options.easing:void 0,onStart:m,onComplete:f,force:y=!1,userData:v}={}){if((this.isStopped||this.isLocked)&&!y)return;let b=t,A=i;if(typeof b=="string"&&["top","left","start","#"].includes(b))b=0;else if(typeof b=="string"&&["bottom","right","end"].includes(b))b=this.limit;else{let T=null;if(typeof b=="string"?(T=b.startsWith("#")?document.getElementById(b.slice(1)):document.querySelector(b),T||(b==="#top"?b=0:console.warn("Lenis: Target not found",b))):b instanceof HTMLElement&&(b!=null&&b.nodeType)&&(T=b),T){if(this.options.wrapper!==window){const z=this.rootElement.getBoundingClientRect();A-=this.isHorizontal?z.left:z.top}const I=T.getBoundingClientRect(),M=getComputedStyle(T),R=this.isHorizontal?Number.parseFloat(M.scrollMarginLeft):Number.parseFloat(M.scrollMarginTop),O=getComputedStyle(this.rootElement),q=this.isHorizontal?Number.parseFloat(O.scrollPaddingLeft):Number.parseFloat(O.scrollPaddingTop);b=(this.isHorizontal?I.left:I.top)+this.animatedScroll-(Number.isNaN(R)?0:R)-(Number.isNaN(q)?0:q)}}if(typeof b=="number"){if(b+=A,this.options.infinite){if(r){this.targetScroll=this.animatedScroll=this.scroll;const T=b-this.animatedScroll;T>this.limit/2?b-=this.limit:T<-this.limit/2&&(b+=this.limit)}}else b=og(0,b,this.limit);if(b===this.targetScroll){m==null||m(this),f==null||f(this);return}if(this.userData=v??{},n){this.animatedScroll=this.targetScroll=b,this.setScroll(this.scroll),this.reset(),this.preventNextNativeScrollEvent(),this.emit(),f==null||f(this),this.userData={},requestAnimationFrame(()=>{this.dispatchScrollendEvent()});return}r||(this.targetScroll=b),typeof h=="number"&&typeof p!="function"?p=Xp:typeof p=="function"&&typeof h!="number"&&(h=1),this.animate.fromTo(this.animatedScroll,b,{duration:h,easing:p,lerp:c,onStart:()=>{s&&(this.isLocked=!0),this.isScrolling="smooth",m==null||m(this)},onUpdate:(T,I)=>{this.isScrolling="smooth",this.lastVelocity=this.velocity,this.velocity=T-this.animatedScroll,this.direction=Math.sign(this.velocity),this.animatedScroll=T,this.setScroll(this.scroll),r&&(this.targetScroll=T),I||this.emit(),I&&(this.reset(),this.emit(),f==null||f(this),this.userData={},requestAnimationFrame(()=>{this.dispatchScrollendEvent()}),this.preventNextNativeScrollEvent())}})}}preventNextNativeScrollEvent(){this._preventNextNativeScrollEvent=!0,requestAnimationFrame(()=>{this._preventNextNativeScrollEvent=!1})}hasNestedScroll(t,{deltaX:i,deltaY:n}){const s=Date.now();t._lenis||(t._lenis={});const r=t._lenis;let c,h,p,m,f,y,v,b,A,T;if(s-(r.time??0)>2e3){r.time=Date.now();const K=window.getComputedStyle(t);if(r.computedStyle=K,c=["auto","overlay","scroll"].includes(K.overflowX),h=["auto","overlay","scroll"].includes(K.overflowY),f=["auto"].includes(K.overscrollBehaviorX),y=["auto"].includes(K.overscrollBehaviorY),r.hasOverflowX=c,r.hasOverflowY=h,!(c||h))return!1;v=t.scrollWidth,b=t.scrollHeight,A=t.clientWidth,T=t.clientHeight,p=v>A,m=b>T,r.isScrollableX=p,r.isScrollableY=m,r.scrollWidth=v,r.scrollHeight=b,r.clientWidth=A,r.clientHeight=T,r.hasOverscrollBehaviorX=f,r.hasOverscrollBehaviorY=y}else p=r.isScrollableX,m=r.isScrollableY,c=r.hasOverflowX,h=r.hasOverflowY,v=r.scrollWidth,b=r.scrollHeight,A=r.clientWidth,T=r.clientHeight,f=r.hasOverscrollBehaviorX,y=r.hasOverscrollBehaviorY;if(!(c&&p||h&&m))return!1;const I=Math.abs(i)>=Math.abs(n)?"horizontal":"vertical";let M,R,O,q,z,V;if(I==="horizontal")M=Math.round(t.scrollLeft),R=v-A,O=i,q=c,z=p,V=f;else if(I==="vertical")M=Math.round(t.scrollTop),R=b-T,O=n,q=h,z=m,V=y;else return!1;return!V&&(M>=R||M<=0)?!0:(O>0?M<R:M>0)&&q&&z}get rootElement(){return this.options.wrapper===window?document.documentElement:this.options.wrapper}get limit(){return this.options.naiveDimensions?this.isHorizontal?this.rootElement.scrollWidth-this.rootElement.clientWidth:this.rootElement.scrollHeight-this.rootElement.clientHeight:this.dimensions.limit[this.isHorizontal?"x":"y"]}get isHorizontal(){return this.options.orientation==="horizontal"}get actualScroll(){const t=this.options.wrapper;return this.isHorizontal?t.scrollX??t.scrollLeft:t.scrollY??t.scrollTop}get scroll(){return this.options.infinite?ex(this.animatedScroll,this.limit):this.animatedScroll}get progress(){return this.limit===0?1:this.scroll/this.limit}get isScrolling(){return this._isScrolling}set isScrolling(t){this._isScrolling!==t&&(this._isScrolling=t,this.updateClassName())}get isStopped(){return this._isStopped}set isStopped(t){this._isStopped!==t&&(this._isStopped=t,this.updateClassName())}get isLocked(){return this._isLocked}set isLocked(t){this._isLocked!==t&&(this._isLocked=t,this.updateClassName())}get isSmooth(){return this.isScrolling==="smooth"}get className(){let t="lenis";return this.options.autoToggle&&(t+=" lenis-autoToggle"),this.isStopped&&(t+=" lenis-stopped"),this.isLocked&&(t+=" lenis-locked"),this.isScrolling&&(t+=" lenis-scrolling"),this.isScrolling==="smooth"&&(t+=" lenis-smooth"),t}updateClassName(){this.cleanUpClassName(),this.className.split(" ").forEach(t=>{this.rootElement.classList.add(t)})}cleanUpClassName(){for(const t of Array.from(this.rootElement.classList))(t==="lenis"||t.startsWith("lenis-"))&&this.rootElement.classList.remove(t)}};const rx=[{heading:"What is MVC and where it came from",blocks:[{t:"p",text:"**MVC (Model–View–Controller)** is the oldest UI architecture pattern — invented by Trygve Reenskaug at Xerox PARC in **1979** for Smalltalk. It splits an interactive application into three roles: the **Model** (data + business logic), the **View** (what the user sees), and the **Controller** (interprets user input and updates the Model)."},{t:"list",items:["**Model** — owns application data and rules; notifies observers when it changes. Knows nothing about View or Controller.","**View** — renders the Model. In *classic* MVC the View observes the Model directly and re-reads it on change notifications.","**Controller** — receives raw user input (clicks, keystrokes), translates it into Model operations. In classic MVC it does *not* update the View — the Model's change notification does."]},{t:"note",text:"Key structural fact interviewers probe: in classic MVC the **View talks to the Model directly** (reads it, observes it). That's the fundamental difference from MVP/MVVM, where the middle layer fully insulates the View from the Model."}]},{heading:"The variants that confuse everyone",blocks:[{t:"p",text:'"MVC" means different things in different ecosystems, and untangling them is a classic senior question:'},{t:"table",headers:["Variant","How it wires up","Where you see it"],rows:[["Classic (Smalltalk) MVC","View observes Model; Controller handles input only","original desktop GUIs; mostly historical"],["Web MVC (a.k.a. Model 2)","Controller receives the HTTP request, manipulates the Model, *selects and populates* the View (template); no live observation — one-shot request/response","Spring MVC, Rails, Django (MTV), ASP.NET MVC"],["Apple MVC (Cocoa)","Controller sits *between* Model and View, mediating both directions",'iOS UIKit — famously degrades into "Massive View Controller"'],['"Android MVC"',"Activity/Fragment plays View *and* Controller simultaneously; XML layout is the (dumb) View skeleton","pre-2017 Android codebases"]]},{t:"p",text:`So when an interviewer asks "is Android MVC?", the accurate answer: Android's framework never enforced any pattern; the *default* shape people wrote — everything in the Activity — is closest to Apple-style MVC where the controller absorbed everything, and that failure mode is exactly why the community moved on.`}]},{heading:"MVC on Android in practice — the God Activity",blocks:[{t:"p",text:`Here's what "MVC" Android code actually looked like — the Activity finds views, handles clicks, calls the network, parses responses, mutates widgets, and manages lifecycle, all in one class:`},{t:"code",title:"The classic God Activity (what NOT to do — but know how to critique it)",code:`class UserActivity : AppCompatActivity() {

    private lateinit var nameText: TextView
    private lateinit var progress: ProgressBar
    private var call: Call<UserDto>? = null

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_user)
        nameText = findViewById(R.id.name)
        progress = findViewById(R.id.progress)

        progress.visibility = View.VISIBLE
        call = api.getUser(intent.getStringExtra("id")!!)
        call!!.enqueue(object : Callback<UserDto> {
            override fun onResponse(c: Call<UserDto>, r: Response<UserDto>) {
                progress.visibility = View.GONE
                // business rule buried in the "view":
                val display = if (r.body()!!.isPremium) "⭐ " + r.body()!!.name
                              else r.body()!!.name
                nameText.text = display
            }
            override fun onFailure(c: Call<UserDto>, t: Throwable) {
                progress.visibility = View.GONE
                Toast.makeText(this@UserActivity, t.message, Toast.LENGTH_SHORT).show()
            }
        })
    }

    override fun onDestroy() {
        super.onDestroy()
        call?.cancel() // manual lifecycle management, easy to forget
    }
}`},{t:"list",items:["The Activity is simultaneously **View** (mutating widgets), **Controller** (handling input, orchestrating the call), and partly **Model** (the premium-star business rule).","Rotation destroys everything mid-flight: the request either dies or, worse, its callback touches a destroyed Activity — the historical source of countless `IllegalStateException`s and leaks.","Nothing is unit-testable: every line needs an Android device/emulator because it's welded to framework classes."]}]},{heading:"Why MVC breaks down on Android specifically",blocks:[{t:"list",items:["**No separate Controller slot in the framework**: Android hands you one entry-point class (Activity/Fragment) that already owns the view hierarchy and the lifecycle — the V and C roles collapse into it by gravity.","**Lifecycle hostility**: classic MVC assumes long-lived View/Controller objects; Android destroys and recreates them on rotation, so any in-controller state is lost and any Model→View observation must be re-wired constantly.",`**View–Model coupling**: even done "correctly", classic MVC lets the View read the Model — so Model refactors ripple into layouts/adapters, and you can't fake the Model easily for UI tests.`,"**Testability**: controllers welded to Activities need instrumented tests; JVM unit tests are impossible for the interesting logic.","**SRP violation at scale**: 2000-line Activities where a bug fix in networking risks breaking click handling — the maintenance cost that drove MVP adoption around 2014–2016."]},{t:"note",text:"Interview framing: don't just trash MVC. Say *why it made sense* (simple, no indirection, fine for tiny screens) and *what specifically about Android* (lifecycle + merged roles) made it fail — that shows architectural judgment rather than memorized dogma."}]},{heading:"MVC vs MVP vs MVVM — the exact deltas",blocks:[{t:"table",headers:["Question","MVC","MVP","MVVM"],rows:[["Does the View know the Model?","**Yes** — reads/observes it directly","No — Presenter mediates everything","No — ViewModel exposes prepared state"],["Does the middle layer know the View?","Controller: often yes (Android: they're the same object)","Yes — via a View **interface**","**No** — View observes the VM"],["Input handling","Controller","View forwards to Presenter","View calls VM functions"],["Testability of logic","poor (framework-welded)","good (mock View interface)","good (assert observable state)"],["Config-change story","none","manual (retain/detach)","built-in (Jetpack ViewModel)"]]},{t:"p",text:"Evolution in one sentence: MVC → MVP moved all View↔Model traffic through a testable Presenter with a View interface; MVP → MVVM replaced the Presenter→View interface calls with **observable state**, removing the attach/detach ceremony and gaining lifecycle survival."}]},{heading:"Migrating away from a God-Activity MVC codebase",blocks:[{t:"list",items:["**Extract the data layer first**: move API/database code behind repositories — highest value, lowest risk, benefits every later step.","**Then extract presentation logic per screen** into ViewModels (going straight to MVVM — migrating MVC→MVP in 2026 would be moving into another condemned building).","**Characterize before you cut**: God Activities hide implicit behavior (ordering, double-click guards, toast timing); pin it with UI tests (Espresso/Compose) before splitting.","**Kill silent couplings**: God Activities often communicate via static fields, singletons and sticky broadcasts — replace with explicit repository state as you go.","Strangler pattern, screen by screen; new features in the new architecture only."]}]}],lx=[{level:"junior",q:"What is MVC? Explain the three components.",a:[{t:"p",text:"MVC splits an app into **Model** (data + business logic, notifies observers on change), **View** (renders the model), and **Controller** (interprets user input and updates the model). It's the oldest UI pattern — Smalltalk, 1979. The defining wiring of classic MVC: the **View reads/observes the Model directly**, and the Controller handles input — it doesn't paint the screen."}]},{level:"junior",q:"In Android MVC, which component is the Activity?",a:[{t:"p",text:`Both View **and** Controller — and that's precisely the problem. Android gives you one class that owns the view hierarchy, receives input, and holds the lifecycle, so the roles merge into a "God Activity". The XML layout is only the static skeleton of the View, not the View role itself (it has no behavior). Because V and C are one object, you can't test controller logic without an emulator, and there's no seam to keep business rules out of UI code.`}]},{level:"junior",q:"What are the main problems with MVC on Android?",a:[{t:"list",items:["**God Activity** — UI mutation, input handling, networking and business rules in one class violating single-responsibility.","**Untestable** — logic welded to framework classes needs instrumented tests; no JVM unit tests.","**Lifecycle pain** — rotation destroys the Activity mid-operation: leaked callbacks, lost state, crashes touching dead views.","**View–Model coupling** — the View reading the Model directly means model refactors ripple into UI code."]}]},{level:"junior",q:"What is the difference between a Controller and a Presenter?",a:[{t:"p",text:"A **Controller** handles *input*: it translates user gestures into Model operations, and in classic MVC the View then updates itself by observing the Model — the Controller may never touch the View. A **Presenter** additionally owns *output*: all Model data reaches the View through it, and it actively drives the View via an interface (`view.showUser(...)`). So MVP fully insulates View from Model, while MVC does not. Mnemonic: Controller = input router; Presenter = input **and** presentation logic."}]},{level:"junior",q:"Is MVC ever an acceptable choice for an Android app?",a:[{t:"p",text:'For a genuinely trivial app — a one-screen utility, a prototype, a hackathon demo — putting logic straight into the Activity is honest pragmatism, and adding layers would be ceremony. The moment the app has real state, async work, or a second developer, the costs (untestability, lifecycle bugs, unbounded class growth) outweigh the simplicity. A good answer also notes that with modern tools the "simple" baseline has shifted: a minimal `ViewModel` + `StateFlow` costs almost nothing, so there\'s rarely a reason to start with God-Activity MVC even for small apps.'}]},{level:"junior",q:"In classic MVC, how does the View update when data changes?",a:[{t:"p",text:"Through the **observer pattern**: the View registers as an observer of the Model; when the Controller mutates the Model, the Model fires change notifications and the View re-reads the data it renders. This is worth knowing because it shows MVC already contained the seed of reactive UI — MVVM's `StateFlow` observation is the same idea with the crucial twist that the View observes *prepared UI state* rather than the raw domain model."}]},{level:"senior",q:"Compare classic MVC, Web MVC, and Apple's MVC. Why does 'MVC' mean such different things?",a:[{t:"list",items:["**Classic (Smalltalk)**: live objects; View observes Model directly; Controller strictly handles input devices. Designed for always-alive desktop GUIs.","**Web MVC (Model 2 — Rails/Spring/Django)**: request/response, not observation. The Controller receives an HTTP request, calls the Model, then *selects and fills a View template* and returns it. Nothing is long-lived; 'observing' is impossible across HTTP, so the Controller took over view population.","**Apple MVC**: the ViewController deliberately mediates both directions between Model and View — closer to MVP structurally, but because it also owns the view lifecycle it absorbs everything → 'Massive View Controller'."]},{t:"p",text:"The name survived while the wiring changed because each platform kept the *role names* and adapted the *communication paths* to its runtime model (live objects vs stateless requests vs framework-owned view controllers). That's the real lesson: patterns are shaped by platform constraints — the same reason Android 'MVC' degraded into the God Activity and the community eventually needed the Jetpack ViewModel to get a slot the framework didn't provide."}]},{level:"senior",q:"Why did MVC's failure on Android lead to MVP first rather than straight to MVVM?",a:[{t:"p",text:"Chronology and tooling. When teams hit the God-Activity wall (~2013–2015), Android had **no observability primitives** in the toolkit — no LiveData, no lifecycle-aware anything; RxJava existed but was a heavy adoption bet. MVP required nothing new: plain interfaces and classes, instantly unit-testable with JUnit + Mockito, which matched the era's testing push. MVVM only became the obvious default when Google shipped **Architecture Components (2017)** — a `ViewModel` that survives rotation plus `LiveData` for safe observation solved exactly the two things MVP handled awkwardly (config changes and lifecycle-safe view updates). So the sequence was driven by infrastructure availability, not pattern merit alone."}]},{level:"senior",q:"You inherit a 3000-line God Activity. Walk me through your migration plan and its risks.",a:[{t:"list",items:["**Stabilize first**: add characterization tests (Espresso) for the critical flows — God Activities are full of implicit behavior (ordering assumptions, double-tap guards) that naive extraction breaks.","**Extract the data layer**: all network/DB/prefs access behind repository classes. Mechanical, low-risk, immediately shrinks the Activity and creates the seam every later step needs.","**Extract state and presentation logic** into a ViewModel exposing a `UiState` — go directly to MVVM; MVP as a stepping stone doubles the work. Replace each 'mutate widget here' site with a state field + one render function.","**Untangle hidden channels**: statics, singletons-as-buses, `onActivityResult` chains, sticky intents — replace with repository state or explicit results; this is where the surprises live.","**Slice by responsibility, not by line count**: pull one feature (e.g. search) end-to-end at a time; ship after each slice."]},{t:"p",text:"Biggest risks: behavioral regressions from implicit ordering, team velocity stalling on a long-lived refactor branch (avoid — keep slices mergeable), and the half-migrated state becoming permanent — mitigate with an explicit definition of done per screen and architecture checks in review/CI."}]},{level:"senior",q:"Defend MVC: what did it get right that modern architectures still use?",a:[{t:"list",items:["**Separation of data from presentation** — the Model as an independent, observable source of truth is the direct ancestor of the repository + reactive stream stack.","**Observer-based UI updates** — 'the view re-renders when the model changes' *is* modern declarative UI; Compose recomposing on state change is classic-MVC observation industrialized.","**Role vocabulary** — Model/View/middle-thing framing is how every successor (MVP, MVVM, MVI) still explains itself; MVC defined the axis they all vary along: *who may talk to whom*.","**Minimal indirection** — for genuinely simple UIs, MVC's directness is a feature; part of why over-layered 'clean' codebases get criticized is that they forgot MVC's economy."]},{t:"p",text:"Sharp closing line: MVC didn't fail conceptually — it failed *operationally* on platforms that merged its roles into one framework class. The fix (MVP, then MVVM) was re-separating roles the platform had fused, not abandoning MVC's ideas."}]},{level:"senior",q:"Does Jetpack Compose bring Android back to MVC?",a:[{t:"p",text:"Interesting framing question — the honest answer is 'closer to classic MVC's *spirit*, but no'. Compose restores the classic-MVC loop: state changes → observing UI re-renders automatically; the framework View role (composables) is finally dumb and stateless like MVC intended. But two things differ fundamentally: the UI observes **prepared UI state from a ViewModel**, never the domain Model directly (that's the MVVM boundary MVC lacked), and input flows through **explicit event callbacks up to a state holder** (UDF), not a controller mutating shared objects. So the modern stack is best described as MVVM/UDF that *finally implements* MVC's original observe-and-render dream with the coupling problems fixed. Saying that shows you understand patterns as evolving wiring, not brand names."}]},{level:"senior",q:"Where does business logic actually live in Android 'MVC', and why is that the core architectural flaw?",a:[{t:"p",text:"In practice it lives **everywhere**: validation in click listeners, pricing rules in `onResponse` callbacks, formatting in adapters, caching in statics. Because the Activity is both V and C, there is no *structural* place that business logic belongs, so it accretes wherever the cursor happens to be. That's the core flaw — not that the Activity is 'too big', but that the architecture provides **no enforceable boundary** between decisions (business/presentation logic) and effects (widget mutation). Every successor pattern is essentially an answer to 'where do decisions live and how do we keep effects out of there': Presenter (MVP), ViewModel (MVVM), reducer (MVI). When asked this, land on the principle: an architecture is good exactly to the degree that logic has one obvious, testable home."}]},{level:"junior",q:"What does 'separation of concerns' mean, and how does MVC try to achieve it?",a:[{t:"p",text:"Separation of concerns is the idea that each part of a program should be responsible for one kind of thing, so you can understand, change, and test each part in isolation. When 'concerns' are mixed — say networking, business rules, and UI all in one class — a change to any one risks breaking the others, and nothing can be tested without dragging in everything else."},{t:"p",text:"MVC's answer is to split responsibilities into three concerns: the **Model** owns data and business rules, the **View** owns rendering, and the **Controller** owns input handling. The intent is that you could swap the View (a different UI) without touching business rules, or test the Model without any UI. That's the *promise* of MVC."},{t:"list",items:["**Model** — 'what the data is and the rules that govern it' (a user, a cart, pricing logic).","**View** — 'how it looks on screen' (widgets, layout).","**Controller** — 'what happens when the user does something' (translate a tap into a model change)."]},{t:"p",text:"The common mistake is thinking any code organized into three files 'is MVC'. Separation of concerns is about *dependencies and responsibilities*, not file names — if your Controller contains business rules and directly formats views, you've mixed concerns even with three files. On Android specifically, MVC's separation breaks because the framework fuses View and Controller into the Activity, so the concern-separation the pattern promises isn't actually enforceable."},{t:"note",text:"Frame separation of concerns as 'each part changes for one reason' (Single Responsibility) — it makes the answer land as a principle, not a file-layout convention."}]},{level:"junior",q:"What exactly are the Model's responsibilities in MVC?",a:[{t:"p",text:"The Model is the part of the app that has nothing to do with the screen. It holds the application's *data* and the *business logic* that operates on that data, and it knows nothing about how it's displayed — no reference to a View, a Controller, or any UI framework class."},{t:"list",items:["**Owns the data** — the domain state (the current user, the shopping cart, the list of messages).","**Owns the business rules** — validation, calculations, invariants ('a cart total can't be negative', 'a premium user gets 20% off').","**Notifies observers of changes** — in classic MVC the Model fires change events so the View can re-read it (the observer pattern).","**Independent of the UI** — no Android imports, so it can be unit-tested on the JVM with no device."]},{t:"code",title:"A framework-free Model",code:`// Pure Kotlin — no Android, no View reference
class CartModel {
    private val items = mutableListOf<Item>()
    private val observers = mutableListOf<() -> Unit>()

    fun addObserver(o: () -> Unit) { observers.add(o) }

    fun addItem(item: Item) {
        items.add(item)
        observers.forEach { it() }   // notify: "something changed, re-read me"
    }

    fun total(): Money = items.sumOf { it.price }   // business rule lives here
}`},{t:"p",text:"A frequent confusion is treating the Model as 'just the data classes' (the DTOs). In proper MVC the Model is the *whole* business layer — data *and* the logic. Reducing it to plain data objects and pushing the logic into the Controller/Activity is exactly how the God Activity forms."},{t:"note",text:"Say 'the Model is the part that would be identical if you rewrote the entire UI' — that captures its framework-independence crisply."}]},{level:"junior",q:"What is the Observer pattern, and why is it central to MVC?",a:[{t:"p",text:"The Observer pattern is a way for one object (the subject) to notify other objects (observers) when it changes, without knowing who they are. The subject keeps a list of observers and calls them on every change; observers register and unregister themselves. This decouples 'the thing that changed' from 'the things that react to it'."},{t:"p",text:"It's central to MVC because it's how the View stays in sync with the Model *without the Model knowing the View exists*. The View registers as an observer of the Model; when the Controller mutates the Model, the Model notifies its observers, and the View re-reads and re-renders. This one-way dependency (View knows Model, Model doesn't know View) is what keeps the Model framework-free."},{t:"code",title:"The observer wiring in MVC",code:`// View registers on the Model
model.addObserver { render(model) }   // "when you change, I'll re-read you"

// Controller mutates the Model (never touches the View directly)
fun onAddClicked() { model.addItem(item) }
// -> model notifies observers -> View's render() runs automatically`},{t:"p",text:"The reason this matters beyond MVC: modern reactive UI is the *same* idea industrialized. `LiveData`, `StateFlow`, and Compose's recomposition are all 'the UI observes state and re-renders on change' — the observer pattern with better lifecycle handling and observing *prepared UI state* instead of the raw Model. So understanding MVC's observer wiring is understanding the ancestor of everything reactive on Android."},{t:"note",text:"Connect it forward: 'StateFlow + collectAsState is the observer pattern with lifecycle safety' — it shows you see patterns as an evolving lineage."}]},{level:"junior",q:"How does data flow through classic MVC, step by step?",a:[{t:"p",text:"In classic MVC the flow is a loop: the user acts on the View, the Controller interprets that action and updates the Model, the Model notifies observers, and the View re-reads the Model and re-renders. The key is that the Controller never paints the screen and the Model never knows about the View — the View updates itself by observing."},{t:"code",title:"The classic MVC loop",code:`// 1. User taps a button in the View
// 2. Controller handles the input:
fun onAddToCartClicked(item: Item) {
    cartModel.addItem(item)        // Controller -> Model (update)
}
// 3. Model changes and notifies observers:
//    items.add(item); observers.forEach { it() }
// 4. View (an observer) re-reads the Model and re-renders:
//    render(cartModel.items, cartModel.total())`},{t:"list",items:["**View → Controller**: user input (taps, gestures).","**Controller → Model**: translate input into a state change.","**Model → View**: change notification (observer), View re-reads.","The Controller→View arrow does *not* exist in classic MVC — that's the difference from MVP/Apple-MVC."]},{t:"p",text:"On Android this pure loop collapses because the Activity is *both* the View and the Controller — so 'Controller updates Model, View observes' becomes 'Activity mutates data and also directly sets widget text', erasing the separation. That's why 'Android MVC' rarely matches the textbook loop."},{t:"note",text:"Draw the triangle and stress the missing Controller→View arrow in classic MVC — interviewers use that arrow to test whether you actually know the variants apart."}]},{level:"junior",q:"What is the difference between MVC, MVP, and MVVM at a glance?",a:[{t:"p",text:"All three separate UI from logic through a 'middle' component, and they differ mainly in *who talks to whom* — specifically whether the middle layer holds a reference to the View, and whether the View sees the Model directly."},{t:"table",headers:["","MVC","MVP","MVVM"],rows:[["Middle component","Controller","Presenter","ViewModel"],["View ↔ Model","View observes Model directly","fully separated (Presenter mediates)","fully separated (VM exposes state)"],["Middle → View link","Controller: usually none (View observes)","Presenter holds a View interface, pushes","VM holds no View ref; View observes"],["How View updates","re-reads Model on change","Presenter calls view.showX()","View observes VM state"],["Config-change survival","none","manual (retain hacks)","built-in (Jetpack ViewModel)"],["Testability","poor (framework-welded)","good (mock the View interface)","good (assert emitted state)"]]},{t:"p",text:"The one-sentence evolution: MVC → MVP moved all View↔Model traffic through a testable Presenter with a View interface; MVP → MVVM replaced the Presenter's imperative `view.showX()` calls with observable *state*, removing the attach/detach ceremony and gaining config-change survival. Each step reduces coupling between the middle layer and the View."},{t:"note",text:"If you can state the single differentiator — 'does the middle layer hold a View reference?' (MVC no/observer, MVP yes/interface, MVVM no/observation) — you've captured the whole comparison."}]},{level:"junior",q:"Show a typical Android MVC implementation and point out what's wrong with it.",a:[{t:"p",text:"'Android MVC' in practice means the Activity plays View and Controller, an XML layout is the passive View skeleton, and the Model is whatever data/logic the Activity touches. It looks organized but concentrates everything in one class."},{t:"code",title:"The classic God Activity",code:`class UserActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_user)

        val id = intent.getStringExtra("id")!!
        progress.isVisible = true
        api.getUser(id).enqueue(object : Callback<UserDto> {   // networking (data)
            override fun onResponse(c: Call<UserDto>, r: Response<UserDto>) {
                progress.isVisible = false
                val user = r.body()!!
                // business rule buried in the "view":
                nameText.text = if (user.isPremium) "⭐ " + user.name else user.name
            }
            override fun onFailure(c: Call<UserDto>, t: Throwable) {
                progress.isVisible = false
                Toast.makeText(this@UserActivity, t.message, Toast.LENGTH_SHORT).show()
            }
        })
    }
}`},{t:"list",items:["**Mixed concerns** — networking, a business rule (premium star), and view mutation are all in one method. Nothing is separable.","**Untestable** — every line needs the Android framework; there's no JVM-unit-testable seam.","**Lifecycle-unsafe** — rotate mid-request and the callback touches a destroyed Activity (crash or leak).","**No home for logic** — the 'premium' rule lives in a UI callback; the next rule will land wherever the cursor is."]},{t:"p",text:"The fix isn't 'make the Activity smaller' — it's to give logic a *structural home*: extract a repository (data), and move presentation logic + state into a ViewModel the UI observes. The Activity becomes a thin renderer."},{t:"note",text:"When shown MVC code, critique the *structure* ('there's no enforceable boundary between data, logic, and UI'), not just the size — that's the senior-level read."}]},{level:"junior",q:"How do you show a loading spinner in Android MVC, and why is it awkward?",a:[{t:"p",text:"In MVC you show a spinner by imperatively toggling the progress view: set it visible before the async call, hide it in the callback. It works, but it's awkward because the *state* ('we are loading') isn't represented anywhere — it's implied by scattered `visibility` mutations, so it's easy to get into inconsistent UI (spinner and error both showing, or spinner stuck on after rotation)."},{t:"code",title:"MVC: imperative visibility toggling",code:`progress.isVisible = true          // start loading
api.getUser(id).enqueue(object : Callback<UserDto> {
    override fun onResponse(...) {
        progress.isVisible = false     // stop loading
        // ... but did you also clear a previous error? easy to forget
    }
    override fun onFailure(...) {
        progress.isVisible = false     // must remember here TOO
        errorView.isVisible = true
    }
})`},{t:"p",text:"The awkwardness is the general problem with imperative UI: you must remember *every* transition (start → hide previous error → show spinner; success → hide spinner → hide error → show content). Miss one and you get stale UI. And on rotation, the in-flight `progress.isVisible = true` is lost, so the spinner may never come back or never go away."},{t:"p",text:"The modern fix is to model loading as *state*: a `UiState` (Loading / Content / Error) that the ViewModel emits and the UI renders wholesale. Then 'loading' is a single explicit value, transitions are impossible to half-apply, and it survives rotation. This contrast is exactly why MVC gave way to state-based patterns."},{t:"note",text:"Use the spinner example to explain 'forgotten transitions' — it's the concrete bug that motivates modeling UI as immutable state instead of imperative toggles."}]},{level:"senior",q:"How does MVC handle configuration changes like rotation, and what breaks?",a:[{t:"p",text:"MVC has *no* built-in answer to configuration changes. On rotation Android destroys and recreates the Activity — and since in Android MVC the Activity *is* the View and Controller (and often holds the Model references and in-flight work), everything is lost or corrupted unless you manually preserve it."},{t:"list",items:["**In-flight async work** — a network call started in `onCreate` either leaks (its callback holds the destroyed Activity) or its result touches a dead view and crashes (`IllegalStateException`, or a NPE on a recreated view).","**UI/loading state** — imperatively-set visibility (`progress.isVisible = true`) is gone; the spinner may vanish or stick.","**Model references** — if the Activity created the Model, a new Model is built on recreation, discarding the loaded data → the screen re-fetches or blanks.","**Manual workarounds** — retained fragments, `onSaveInstanceState`, or `android:configChanges` to suppress recreation — all hacks with their own bugs."]},{t:"code",title:"The rotation bug in MVC",code:`// onCreate: request started, holding 'this' (the Activity)
api.getUser(id).enqueue(callback)  // callback captures this@UserActivity
// user rotates -> Activity destroyed, new one created
// old request completes -> callback.onResponse runs on the DEAD Activity:
nameText.text = user.name          // NPE / IllegalStateException / leak`},{t:"p",text:"This is precisely the gap Jetpack ViewModel filled: a ViewModel *survives* configuration changes, so the in-flight work (in `viewModelScope`) and the state live across rotation, and the recreated Activity just re-observes. MVC predates that slot, so it fights the lifecycle by hand."},{t:"note",text:"The clean framing: 'MVC has no config-change story because the Activity is stateful and disposable — the ViewModel exists precisely to be the retained, framework-free home MVC lacked.'"}]},{level:"senior",q:"Which SOLID principles does the God-Activity form of MVC violate?",a:[{t:"p",text:"The God Activity violates most of SOLID, but the headline offenders are Single Responsibility and Dependency Inversion — and understanding *which* principle each successor pattern restores is a strong way to explain architecture evolution."},{t:"list",items:["**Single Responsibility (S)** — the most flagrant: the Activity handles input, networking, business rules, formatting, and view mutation. It changes for many unrelated reasons, so a networking change can break click handling.","**Dependency Inversion (D)** — high-level policy (business rules) is welded to low-level detail (Retrofit callbacks, Android views). There are no abstractions to depend on, so nothing is substitutable or testable.","**Open/Closed (O)** — adding a feature means editing the one giant class rather than extending; every change risks the whole file.","**Interface Segregation (I)** — less directly, but the Activity depends on a huge surface (the whole Android framework) rather than narrow abstractions."]},{t:"p",text:"The insight: each successor pattern is essentially a targeted SOLID fix. MVP restores testability by inverting the View dependency (depend on a View *interface*). MVVM restores SRP and DIP by giving presentation logic a dedicated, framework-free home (the ViewModel) that depends on abstractions (repositories). Clean Architecture generalizes DIP across the whole app."},{t:"note",text:"Answer with 'S and D are the core violations; every successor pattern is a SOLID fix' — mapping patterns to principles signals you understand *why* they exist, not just their shapes."}]},{level:"senior",q:"Can MVC coexist with Clean Architecture, or are they mutually exclusive?",a:[{t:"p",text:"They operate at different levels, so in principle they aren't mutually exclusive — MVC is a *presentation* pattern (how UI, input, and the presentation-facing model relate), while Clean Architecture is about *layering the whole app* (domain, data, presentation with dependencies pointing inward). You could have a Clean data/domain layer with an MVC presentation layer on top."},{t:"p",text:"In practice, though, they fit poorly on Android. Clean Architecture's whole point is testable, framework-independent business logic — but Android MVC's Controller *is* the Activity, which is framework-welded and untestable. So while the *data* and *domain* layers could be clean, the *presentation* would still concentrate logic in the Activity, undermining the benefits. That mismatch is why Clean Architecture on Android is paired with MVVM/MVI (which give the presentation layer a framework-free home), not MVC."},{t:"list",items:["**Compatible in theory** — Clean = layering; MVC = presentation wiring; different axes.","**Poor fit in practice** — MVC's Controller (Activity) can't be the clean, testable presentation layer Clean Architecture wants.","**The real pairing** — Clean Architecture + MVVM/MVI, where the ViewModel is the framework-free presentation layer that observes the domain."]},{t:"note",text:"Distinguish the axes explicitly: 'MVC is presentation wiring, Clean is app layering' — then note they *technically* compose but Android MVC's untestable Controller defeats the purpose."}]},{level:"senior",q:"If you were forced to use MVC on Android, how would you make it less terrible?",a:[{t:"p",text:"The core problem is that the Activity has no seam to keep logic out of it. So even within 'MVC', I'd introduce discipline that pulls concerns out of the Activity, effectively nudging toward MVP without a full rewrite. The goal is to make the Activity thin and give logic a testable home."},{t:"list",items:["**Extract a real Model layer** — all networking/DB/prefs behind repositories, and business rules in framework-free classes. This alone removes the biggest untestable chunk from the Activity and can be done incrementally.","**Keep the Activity as a dumb 'render + forward' shell** — it should only read model state to update widgets and forward input; no business rules, no direct networking.","**Use observer-based updates** — have the Activity observe the Model (via a listener or a small state holder) rather than imperatively mutating widgets from scattered callbacks, so UI updates flow from one place.","**Handle lifecycle deliberately** — cancel in-flight work when the Activity is destroyed; don't let callbacks touch dead views.","**Add characterization tests** — since you can't easily unit test, add Espresso tests around critical flows so you can refactor safely later."]},{t:"p",text:"Honestly, this migration path *is* the path to MVP/MVVM — extracting the Model and thinning the Activity is exactly step one of a proper migration. So 'make MVC less terrible' converges on 'start moving off MVC', which is the point to make: the disciplines that fix MVC are the disciplines of the patterns that replaced it."},{t:"note",text:"Turn the constraint into a migration: 'the way to survive MVC is to do the first steps of leaving it — extract the data layer and thin the Activity.' That reframes a bad-pattern question as an architectural-judgment answer."}]},{level:"senior",q:"A junior on your team puts all logic in the Activity. How do you review and guide them?",a:[{t:"p",text:"The goal of the review isn't just 'move this code' — it's to transfer the *principle* so they internalize why, and to do it without discouraging them. I'd anchor on a concrete, felt pain (testing) rather than abstract purity, because that's what makes the lesson stick."},{t:"list",items:["**Lead with a question, not a verdict** — 'How would you write a unit test for this pricing rule?' They'll discover it's impossible because it's tangled with the Activity — the lesson teaches itself.","**Name the principle simply** — 'Let's give this logic a home where it's not tied to the screen. The Activity should mostly show state and forward taps.'","**Show the target shape concretely** — pair on extracting one piece: the business rule into a testable class or the network call into a repository, then write the test that's now possible. One worked example beats a lecture.","**Explain the payoff in their terms** — 'When this crashes on rotation, this structure is why; moving state to a ViewModel fixes a whole class of bugs.' Tie it to bugs they'll actually hit.","**Keep it incremental and kind** — praise what's right, refactor one concern at a time, and point to the codebase's existing patterns so they have a template."]},{t:"p",text:"The meta-point: good architecture reviews teach *heuristics* ('can I test this in isolation?', 'does logic have one home?') rather than enforcing a specific pattern by fiat. A junior who leaves the review able to *ask themselves the right question* is a better outcome than one who just copied your diff."},{t:"note",text:"The reviewer move that lands: ask 'how would you test this?' — untestability reveals the design problem better than any style argument, and it teaches a durable heuristic."}]},{level:"junior",q:"Why can't you unit-test a God Activity?",a:[{t:"p",text:"A unit test runs on the JVM (your machine, no device) and tests one piece of logic in isolation with fake dependencies. A God Activity can't be unit-tested because its logic is welded to Android framework classes — `Context`, `View`, `Activity` lifecycle, static Android calls — none of which exist on the plain JVM, and none of which can be substituted with fakes because the logic never depended on abstractions."},{t:"list",items:["**No JVM without Android** — instantiating or running Activity code needs the Android framework (or Robolectric to simulate it), so tests are slow instrumented/Robolectric tests, not fast unit tests.","**No seams to inject fakes** — the logic calls Retrofit and touches views directly; there's no interface to swap a fake repository or fake view for, so you can't isolate the rule under test.","**Nothing is isolatable** — the pricing rule, the network call, and the widget update are in one method, so you can't test the rule without also running the network and UI."]},{t:"p",text:"Contrast: a ViewModel with an injected repository is a plain class — you construct it with a fake repository, call a method, and assert the emitted state, all on the JVM in milliseconds. The difference isn't 'ViewModels are magic'; it's that constructor injection + framework-free logic *create the seams* unit testing requires. So 'can't unit test' is really 'no seams and framework-welded' — a design symptom, not an Android limitation."},{t:"note",text:"'Untestable' = 'framework-welded + no injection seams'. Naming both causes (not just 'it's in the Activity') shows you understand testability as a design property."}]},{level:"senior",q:"What causes the 'Massive View Controller' problem, and how is it the same as the God Activity?",a:[{t:"p",text:"'Massive View Controller' is the iOS name for the same failure mode as Android's God Activity: the framework provides one class that owns the view lifecycle *and* is the natural place to handle input and coordinate the model, so all logic gravitates into it and it balloons. Both stem from the platform fusing MVC's roles into a single framework-owned object."},{t:"list",items:["**Apple MVC** intends the ViewController to *mediate* between Model and View (both directions) — structurally closer to MVP. But because it also owns the view lifecycle and is the OS entry point, everything accretes there.","**Android** gives you the Activity/Fragment as View+Controller in one object with the lifecycle — same gravity well.","**Root cause (identical)** — the platform provides no *other* structural home for presentation logic, so it lands in the one class the framework instantiates."]},{t:"p",text:"The shared lesson: the God Activity / Massive View Controller isn't caused by 'bad developers' — it's caused by the *architecture providing no enforceable boundary* between decisions and effects. That's why both ecosystems evolved patterns (MVVM, VIPER, unidirectional architectures) that *add* a home for logic the framework didn't give — and it's a great cross-platform observation to make in an interview."},{t:"note",text:"Naming the iOS parallel ('Massive View Controller') and the shared root cause (framework fuses the roles, no home for logic) demonstrates architecture understanding beyond one platform."}]},{level:"senior",q:"You're building a throwaway proof-of-concept in a day. Would you use MVC? Defend your answer.",a:[{t:"p",text:"For a genuine one-day throwaway POC, putting logic straight in the Activity (MVC-ish) can be a defensible, honest choice — the costs of MVC (untestability, lifecycle bugs, unbounded growth) are costs that accrue *over time and team size*, and a throwaway that will never be maintained doesn't pay them. Adding layers you'll delete tomorrow is ceremony."},{t:"p",text:"But I'd qualify it heavily. First, the modern 'simple baseline' has shifted: a minimal `ViewModel` + `StateFlow` costs almost nothing and immediately buys config-change survival — so even for a POC, MVVM-lite is barely more effort and avoids the rotation crashes that waste demo time. Second, POCs have a habit of becoming production ('temporary' is the most permanent state in software), so 'throwaway' is a risky assumption. Third, if I'm demoing to stakeholders, a crash on rotation looks bad — and MVC's classic failure is exactly that."},{t:"list",items:["**Defensible when** — truly throwaway, single screen, no async that outlives the screen, solo, deleted after the demo.","**Prefer MVVM-lite when** — any chance it survives, any async work, any stakeholder demo (rotation must not crash)."]},{t:"p",text:"So my honest answer: I *could* justify MVC for a true throwaway, but I'd probably still reach for a minimal ViewModel because the cost is now negligible and it dodges the demo-killing rotation bug. The senior signal is weighing the *context* (lifespan, audience, risk of surviving) rather than dogmatically saying 'always layer' or 'never bother'."},{t:"note",text:"Show judgment both ways: MVC is defensible for a true throwaway, but 'ViewModel-lite is nearly free and dodges rotation crashes' — deciding by context, not dogma, is the answer they want."}]},{level:"senior",q:"A legacy pure-MVC app crashes on rotation for some users. How do you triage it?",a:[{t:"p",text:"Rotation crashes in MVC almost always trace to the Activity being destroyed and recreated while something still references the old one or touches a recreated-but-not-ready view. I'd triage by confirming the pattern, finding the specific reference, and applying the smallest safe fix — then consider the structural fix."},{t:"list",items:["**1. Confirm it's rotation-related** — reproduce by rotating during the suspect flow (mid-load), and check the crash reports for `IllegalStateException`/NPE with a stack trace pointing at a view access or a callback. Correlate with 'happens after backgrounding/rotating'.","**2. Find the dangling reference** — the usual culprit is an async callback (network, timer) that captured the old Activity/View and fires after recreation, touching a dead view. Or `onSaveInstanceState` not preserving state the recreated Activity assumes exists.","**3. Smallest safe fix** — cancel in-flight work when the Activity is destroyed (so stale callbacks don't run), null-check/guard view access, and ensure any state the recreated Activity reads is restored. This stops the crash.","**4. Structural fix** — move the async work and state into a ViewModel (survives rotation) so the recreated Activity just re-observes. This eliminates the *class* of bug, not just the one crash. Do it for the affected screen first (incremental).","**5. Prevent regression** — add a test that rotates during the flow, and consider StrictMode/LeakCanary to catch the leaked-Activity pattern in debug."]},{t:"p",text:"The judgment call is 'stop the bleeding vs fix the disease': the smallest fix (cancel callbacks, guard views) ships fast and stops the crash; the structural fix (ViewModel) prevents recurrence but is a bigger change. I'd usually ship the guard first, then migrate the screen to a ViewModel to remove the root cause."},{t:"note",text:"Triage answer structure that impresses: confirm the pattern → find the exact dangling reference → smallest safe fix (cancel work) → structural fix (ViewModel) → regression test. It shows incident discipline plus root-cause thinking."}]},{level:"junior",q:"How does the Controller communicate with the View across the different MVC variants?",a:[{t:"p",text:"This is the detail that actually distinguishes the MVC variants, so it's worth being precise. The variants differ in whether the Controller (or its equivalent) *pushes* to the View or whether the View *pulls* from the Model by observing."},{t:"list",items:["**Classic (Smalltalk) MVC** — the Controller does *not* update the View. The Controller mutates the Model; the View observes the Model and re-renders itself. Controller→View communication essentially doesn't exist.","**Web MVC (Model 2)** — the Controller *does* select and populate the View: it handles the request, updates the Model, then picks a template and fills it, returning the rendered result. One-shot, no observation (there's no long-lived View to observe across HTTP).","**Apple MVC** — the ViewController mediates *both* directions: it reads the Model and pushes to the View, and handles View input to update the Model. This bidirectional mediation is why it's structurally close to MVP.","**Android 'MVC'** — the Activity (View+Controller fused) mutates widgets directly from input handlers, so 'Controller→View' is just 'the Activity sets its own views' — the separation is gone."]},{t:"p",text:"The reason 'MVC' is such a slippery term is exactly this: the *role names* stayed constant while the *communication paths* changed per platform's runtime model (live objects vs stateless HTTP vs framework-owned controllers). So 'do you know MVC?' really means 'do you know which variant, and how its View gets updated?'."},{t:"note",text:"The discriminator is 'push vs observe': classic = View observes Model (no Controller→View), Web/Apple = Controller pushes to View. Knowing that separates people who memorized 'MVC = three boxes' from people who understand it."}]},{level:"senior",q:"Explain why 'the Activity is both View and Controller' is the root architectural flaw, not just an inconvenience.",a:[{t:"p",text:"It's tempting to see the God Activity as merely 'a big class' — a code-hygiene issue. But the deeper problem is *structural*: because the Activity is simultaneously View and Controller (and framework-owned, with the lifecycle), the architecture provides no *enforceable boundary* between decisions (business/presentation logic) and effects (mutating widgets). Logic has no designated home, so it accretes wherever it's convenient."},{t:"list",items:["**No home for logic** — since V and C are one object, there's no slot that means 'this is where presentation logic lives, free of the framework'. Rules end up in click listeners, in `onResponse` callbacks, in adapters — anywhere.","**No boundary to enforce** — you can't lint or review your way to separation when the *architecture* offers no seam. Discipline is fighting the structure, not supported by it.","**Testability follows from the boundary** — with no framework-free home for decisions, there's nothing to unit-test in isolation. Testability is a *consequence* of the missing boundary, not a separate problem."]},{t:"p",text:"This reframes every successor pattern: MVP, MVVM, and MVI are each fundamentally an answer to 'where do decisions live, and how do we keep effects out of there?' — the Presenter, the ViewModel, the reducer are all *the home for decisions* that MVC's fused roles denied. So 'the Activity is View and Controller' isn't an inconvenience; it's the absence of the boundary that all of Android architecture has been trying to restore ever since."},{t:"note",text:"The senior framing: 'an architecture is good exactly to the degree that logic has one obvious, testable home' — MVC's flaw is that the fused Activity denies logic any home, and every successor pattern is a home for decisions."}]}],cx={id:"mvc",title:"MVC",tagline:"Model – View – Controller: the original UI pattern, its variants, why it collapsed into the God Activity on Android, and how to talk about it in interviews.",tags:["Android","History","God Activity","Patterns"],content:rx,qa:lx},dx=[{heading:"What is MVP",blocks:[{t:"p",text:"**MVP (Model–View–Presenter)** evolved from MVC (Taligent, 1990s; popularized on Android ~2014–2016) to fix MVC's two coupling problems: the View talking to the Model, and logic having no testable home. In MVP, **all** communication between View and Model flows through the **Presenter**."},{t:"list",items:["**Model** — data layer: repositories, data sources, business logic. Same as in MVVM.","**View** — Activity/Fragment implementing a **View interface**. Completely passive: forwards user input to the Presenter, exposes methods like `showLoading()`, `showUser(user)`, `showError(msg)` that just mutate widgets.","**Presenter** — plain Kotlin/Java class holding a reference to the View *interface*. Receives events (`onRefreshClicked()`), talks to the Model, and drives the View by calling its methods. Contains all presentation logic; zero Android imports."]},{t:"note",text:"The defining wiring: **the Presenter holds a View reference and pushes to it imperatively**. Compare MVVM, where the ViewModel holds no View reference and the View pulls/observes. Every MVP strength and weakness follows from that one arrow."}]},{heading:"The contract — canonical MVP code",blocks:[{t:"code",title:"Contract interface (the signature MVP artifact)",code:`interface UserContract {
    interface View {
        fun showLoading()
        fun hideLoading()
        fun showUser(name: String, isPremium: Boolean)
        fun showError(message: String)
    }
    interface Presenter {
        fun attach(view: View)
        fun detach()
        fun onLoadUser(id: String)
    }
}`},{t:"code",title:"Presenter — pure JVM, fully unit-testable",code:`class UserPresenter(
    private val repository: UserRepository,
    private val mainScheduler: Scheduler, // era-typical: RxJava
) : UserContract.Presenter {

    private var view: UserContract.View? = null
    private val disposables = CompositeDisposable()

    override fun attach(view: UserContract.View) { this.view = view }

    override fun onLoadUser(id: String) {
        view?.showLoading()
        disposables += repository.getUser(id)
            .observeOn(mainScheduler)
            .subscribe(
                { user ->
                    view?.hideLoading()
                    view?.showUser(user.name, user.isPremium)
                },
                { error ->
                    view?.hideLoading()
                    view?.showError(error.message ?: "Unknown error")
                }
            )
    }

    override fun detach() {
        disposables.clear()
        view = null // critical — otherwise the destroyed Activity leaks
    }
}`},{t:"code",title:"View — passive Activity",code:`class UserActivity : AppCompatActivity(), UserContract.View {

    private lateinit var presenter: UserContract.Presenter

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_user)
        presenter = UserPresenter(repository, AndroidSchedulers.mainThread())
        presenter.attach(this)
        refreshButton.setOnClickListener {
            presenter.onLoadUser(intent.getStringExtra("id")!!)
        }
    }

    override fun onDestroy() {
        presenter.detach()
        super.onDestroy()
    }

    override fun showLoading() { progress.isVisible = true }
    override fun hideLoading() { progress.isVisible = false }
    override fun showUser(name: String, isPremium: Boolean) {
        nameText.text = if (isPremium) "⭐ " + name else name
    }
    override fun showError(message: String) {
        Snackbar.make(root, message, Snackbar.LENGTH_SHORT).show()
    }
}`}]},{heading:"Passive View vs Supervising Controller",blocks:[{t:"p",text:"Martin Fowler split MVP into two flavors — a favorite senior distinction:"},{t:"list",items:["**Passive View** — the View is *completely* dumb: every label, every visibility flag is set through an explicit Presenter call. Maximum testability (the whole UI decision surface is presenter logic), maximum boilerplate. The Android-common flavor.","**Supervising Controller** — the View handles simple declarative binding from the Model itself (e.g. data binding for trivial fields), and the Presenter only handles complex logic and input. Less boilerplate, but logic starts seeping back into the View."]},{t:"p",text:"Worth saying in interviews: Supervising Controller with data binding is already halfway to MVVM — the history is a smooth slide, not a jump."}]},{heading:"Lifecycle & configuration changes — MVP's hardest problem",blocks:[{t:"p",text:"The Presenter holds a View reference, and Android destroys Views on rotation. Everything painful about MVP on Android radiates from this:"},{t:"list",items:["**attach/detach ceremony**: the View must attach in `onCreate`/`onStart` and detach in `onDestroy`/`onStop`; forgetting `detach()` (or nulling the reference) leaks the destroyed Activity through the Presenter.","**The null-view dance**: every async callback must guard `view?.` because the result may arrive while detached — and then the update is silently *dropped*: rotate during load and the data never shows unless you re-request or cache.","**Presenter survival options** (none great): recreate it every time and re-fetch (wasteful, flicker); retain it via a retained Fragment / static cache / Loader (pre-Jetpack hacks with their own leaks); libraries like Mosby and Nucleus existed specifically to manage keep-alive + view re-attachment + delivering pending results.","**State restoration**: a re-attached View is blank; the Presenter needs a 'replay current state onto the new view' step — which MVP has no standard mechanism for. (MVVM gets this for free: the new collector immediately receives the current `StateFlow` value.)"]},{t:"note",text:'This section is *the* answer to "why did MVVM win?": Jetpack ViewModel + observable state solves attach/detach, the null-view dance, presenter survival AND state replay in one stroke. Being able to enumerate the four MVP pains and map each to its MVVM fix is a strong senior answer.'}]},{heading:"Testing Presenters",blocks:[{t:"p",text:`MVP's headline feature in its era: the Presenter is a plain class, and the View is an interface you mock — so tests read as "event in → view calls out":`},{t:"code",title:"Classic presenter test (JUnit + Mockito)",code:`@Test
fun loadUser_success_showsUser() {
    val view: UserContract.View = mock()
    val repository: UserRepository = mock()
    whenever(repository.getUser("42"))
        .thenReturn(Single.just(User(name = "Revanth", isPremium = true)))

    val presenter = UserPresenter(repository, Schedulers.trampoline())
    presenter.attach(view)

    presenter.onLoadUser("42")

    inOrder(view) {
        verify(view).showLoading()
        verify(view).hideLoading()
        verify(view).showUser("Revanth", true)
    }
    verify(view, never()).showError(any())
}`},{t:"list",items:["Style difference from MVVM testing: MVP tests **verify interactions** (mock method calls, ordering); MVVM tests **assert emitted state values**. State assertion is generally more robust — interaction tests break when you rename or reorder view calls even if behavior is identical.","Threading in tests: replace `AndroidSchedulers.mainThread()` with `Schedulers.trampoline()` (Rx) — the era's equivalent of today's `Dispatchers.setMain(TestDispatcher)`.","Edge cases worth testing then and now: events arriving while detached, double-clicks (re-entrancy), error-then-retry sequences."]}]},{heading:"Why MVVM replaced MVP — and where you'll still meet MVP",blocks:[{t:"table",headers:["Pain in MVP","MVVM's answer"],rows:[["Presenter → View interface + attach/detach ceremony","No view reference at all — observation"],["Presenter dies on rotation / retain hacks","Jetpack ViewModel survives config changes natively"],["Dropped updates while detached; manual state replay","StateFlow/LiveData replays current state to every new observer"],["One View interface + implementation per screen (boilerplate)","One state class; the View renders it"],["Lifecycle-safety hand-rolled","repeatOnLifecycle / collectAsStateWithLifecycle"]]},{t:"list",items:["You will still meet MVP in: long-lived codebases started 2014–2017 (banking, e-commerce), interviews for teams maintaining them, and some TV/embedded projects.","MVP remains *conceptually* fine where the platform lacks lifecycle churn or an observation toolkit — but on Android/KMP today there is no reason to start a new MVP screen.","Migration MVP→MVVM is mechanical: each `view.showX(args)` call becomes a field on `UiState`; the Presenter's methods become ViewModel functions; the contract dies; tests convert from interaction-verification to state-assertion (and usually get simpler)."]}]}],hx=[{level:"junior",q:"What is MVP and how does data flow through it?",a:[{t:"p",text:"Model–View–Presenter. The **View** (Activity/Fragment implementing a View interface) is passive: it forwards user events to the **Presenter** and exposes methods like `showUser(...)` that only mutate widgets. The **Presenter** — a plain class with no Android imports — receives events, calls the **Model** (repositories), and pushes results back by calling View interface methods. Flow: user input → View → Presenter → Model → Presenter → View. Nothing goes View↔Model directly — that's the fix over MVC."}]},{level:"junior",q:"What is a 'contract' in MVP?",a:[{t:"p",text:"A container interface (usually `SomethingContract`) declaring the paired `View` and `Presenter` interfaces for one screen — the explicit API between the two. Benefits: the whole screen's interaction surface is readable in one place, the Presenter depends only on the abstract View (mockable in tests), and either side can be swapped. Cost: one more file of ceremony per screen — part of why MVP felt heavy."}]},{level:"junior",q:"Why is the Presenter easy to unit test?",a:[{t:"p",text:"Because it's a plain JVM class whose only 'UI' dependency is the View *interface* — in a test you mock the View and the repository, call presenter methods, and **verify** the expected View calls happened (`verify(view).showUser(...)`), including their order. No emulator, no Robolectric. The one thing to handle is threading: swap the main-thread scheduler/dispatcher for a synchronous test one."}]},{level:"junior",q:"Why must you call detach() (or null the view) in MVP, and what happens if you forget?",a:[{t:"p",text:"The Presenter holds a strong reference to the View, which on Android is the Activity/Fragment. When the Activity is destroyed (rotation, back), the Presenter's reference keeps the entire destroyed Activity — and its view hierarchy — reachable, so the GC can't reclaim it: a **memory leak**. Worse, an async result arriving after destruction would call methods on a dead Activity (crashes touching views, `IllegalStateException`s). Hence the ceremony: `attach(view)` in onCreate/onStart, `detach()` (nulling the reference and cancelling work) in onDestroy/onStop — and `view?.` guards on every async callback."}]},{level:"junior",q:"Presenter vs ViewModel — what's the core difference?",a:[{t:"p",text:"The direction of the arrow to the View. A **Presenter** holds a View reference and *pushes* imperatively: `view.showLoading()`. A **ViewModel** holds no View reference; it exposes observable state and the View *pulls/observes*. Consequences: the ViewModel needs no attach/detach and can't leak the Activity; it survives configuration changes (Jetpack); and a new observer automatically receives current state, whereas a re-attached MVP View is blank until the Presenter replays state manually."}]},{level:"junior",q:"What happens in MVP when the screen rotates during a network call?",a:[{t:"p",text:"The Activity detaches and is destroyed; the new Activity attaches (usually to a *new* Presenter). The in-flight result then hits `view?.showUser(...)` with a null or stale view — so the update is **silently dropped** and the new screen either re-fetches (flicker, wasted request) or shows nothing. Solving this properly required retaining the Presenter (retained fragment, static cache, or libraries like Mosby/Nucleus that also buffer results and replay them to the re-attached view) — exactly the machinery Jetpack ViewModel + StateFlow made obsolete."}]},{level:"junior",q:"Is the Activity the View or something else in MVP?",a:[{t:"p",text:"The Activity/Fragment **is the View** — it implements the View interface and does nothing but widget mutation and event forwarding. That's the philosophical shift from Android MVC: instead of fighting to make the Activity a controller, MVP demotes it to a dumb rendering shell and moves the brain into a framework-free Presenter. Follow-up worth volunteering: 'passive view' is the ideal — any `if` beyond trivial rendering that appears in the Activity is logic escaping the Presenter."}]},{level:"senior",q:"Explain Passive View vs Supervising Controller and the trade-off between them.",a:[{t:"p",text:"Fowler's two MVP flavors. **Passive View**: the View is entirely driven by explicit Presenter calls — every text, every visibility. All UI decisions become presenter logic, so test coverage of presentation is total; the price is an interface method for every pixel-level change and verbose presenters. **Supervising Controller**: trivial, declarative bindings go straight View↔Model (e.g. data binding for a name field), and the Presenter handles only complex logic and input — less boilerplate, but simple-binding decisions escape testability and logic gradually leaks viewward."},{t:"p",text:"Senior observation: the industry's path Passive View → Supervising Controller → MVVM is one continuous slide — each step replaces imperative view calls with declarative binding, and MVVM is the endpoint where *all* of it is binding/observation."}]},{level:"senior",q:"How did production MVP apps keep Presenters alive across configuration changes before Jetpack?",a:[{t:"list",items:["**Retained fragment** (`setRetainInstance(true)`) holding the Presenter — the sanctioned pre-Jetpack survival hack (deprecated since).","**Static/application-scoped presenter caches** keyed by screen id, with manual eviction on real finish — leak-prone and easy to get wrong on multi-window/multi-instance.","**Loaders** (`LoaderManager`) — framework-retained async, awkward API, sometimes abused as presenter hosts.","**Libraries**: Mosby (presenter retention + `ViewState` replayed onto re-attached views), Nucleus (retention + delivering cached Rx results on re-attach)."]},{t:"p",text:"Two follow-up points that score: all of these still lose the Presenter on **process death** — input state needed `onSaveInstanceState` handling separately (same split as ViewModel vs SavedStateHandle today); and the very existence of this hack zoo is the evidence that the pattern fought the platform — the Jetpack ViewModel is institutionally 'the retained presenter done right'."}]},{level:"senior",q:"MVP interaction-based tests vs MVVM state-based tests — which are better and why?",a:[{t:"p",text:"MVP tests **verify calls**: `verify(view).showUser(\"Revanth\", true)` with `inOrder` for sequencing. MVVM tests **assert values**: collect the state flow and check emitted `UiState`s. State-based is generally more robust: interaction tests are coupled to the *names and granularity* of view methods — split `showUser` into two calls and every test breaks with identical behavior; mock-verification also can't easily express 'the final state is X regardless of path'. Interaction tests do have one edge: they naturally catch *unwanted* calls (`verify(view, never()).showError(...)`) and exact ordering/timing of imperative effects. Net: assert state where possible, verify interactions only for genuine one-shot effects — which is precisely the shape modern MVVM testing (state assertions + event-channel assertions) converged on."}]},{level:"senior",q:"Sketch the mechanical recipe for migrating one MVP screen to MVVM.",a:[{t:"list",items:["**Inventory the View interface** — it's a specification of the screen's states: each `showX(args)` becomes a field (or variant) of a new `UiState` data class; overlapping calls (`showLoading` + stale list visible) tell you whether you need flags or a sealed hierarchy.","**Presenter → ViewModel**: same constructor deps (repositories); each event method stays; every `view?.callX(...)` becomes `_uiState.update { ... }`. One-shot calls (`navigateTo`, `showToast`) move to an event channel or consumed-state fields.","**Delete attach/detach**; `CompositeDisposable`/manual cancellation becomes `viewModelScope`; Rx chains become coroutines/Flow (or keep Rx with `asFlow` bridges initially — smaller diff).","**View side**: contract implementation replaced by one render function collecting state via `repeatOnLifecycle` (or the screen goes Compose in the same step if the team allows).","**Tests**: each `verify(view).x(...)` assertion becomes an `awaitItem()` state assertion — usually a line-for-line translation."]},{t:"p",text:"Sequencing advice: migrate the *most-edited* screens first (highest interest earned), keep both patterns behind the same repositories, and forbid new MVP screens from day one."}]},{level:"senior",q:"In MVP, where do one-shot actions like navigation and toasts go, and how does that compare to MVVM's event problem?",a:[{t:"p",text:"MVP handles them *trivially*: the Presenter just calls `view.navigateToDetails(id)` — imperative push makes one-shot actions natural, no stickiness possible. The catch is the detached window: fire it while the view is detached and it's dropped (rotate right after a save completes → the navigation never happens), so retained-presenter setups had to queue pending view commands and flush on re-attach — Nucleus/Mosby did exactly this. MVVM inverted the trade: state observation made *continuous* UI trivial but made *one-shot* delivery the hard problem (sticky state, dropped SharedFlow emissions, Channel semantics). It's a genuinely symmetric trade-off, and framing it that way — each pattern is awkward exactly where the other is natural — is a strong senior answer."}]},{level:"senior",q:"Could you use MVP in a KMP project? Why does the ecosystem not do it?",a:[{t:"p",text:"Technically yes — a Presenter is a plain class, and contract interfaces cross platforms fine; early KMM experiments did exactly this. The ecosystem rejected it for three reasons. First, the **lifecycle ceremony multiplies per platform**: every host (Activity, Fragment, UIViewController, SwiftUI view) must implement attach/detach correctly — the leak-prone part is now duplicated. Second, **imperative push doesn't fit SwiftUI/Compose**, which are state-driven by construction: a `view.showUser(...)` call has nowhere natural to land in declarative UI, whereas an observable `StateFlow<UiState>` maps directly onto both. Third, shared observable state via coroutines/Flow was the piece MVP existed to avoid needing — and in KMP it's already there. So shared code converged on ViewModel-shaped state holders (androidx KMP ViewModel, moko-mvvm, Decompose components) exposing StateFlow."}]},{level:"senior",q:"What are the failure modes of MVP at scale that you'd warn a team about?",a:[{t:"list",items:["**Contract explosion** — hundreds of near-identical View/Presenter interface pairs; renaming a UI behavior touches interface + implementation + presenter + tests.","**God Presenters** — the God Activity relocated: without a data/domain layer discipline, presenters absorb caching, mapping and business rules. MVP doesn't prevent it; it just moves the pile.","**View methods becoming a remote control** — dozens of granular `setXVisible(Boolean)` methods whose valid *combinations* are undocumented; the screen's actual state space lives implicitly in call ordering (MVVM's single UiState fixes this by making the state space explicit).","**Lifecycle bug taxes** — every new async source re-introduces the detached-window/null-view/leak class of bugs; code review must police it forever.","**Interaction-test brittleness** — large mock-verification suites that break on refactors, training the team to rubber-stamp test changes."]}]},{level:"junior",q:"What problem did MVP solve that MVC couldn't?",a:[{t:"p",text:"The core problem with Android MVC was that the Activity was both View and Controller, so presentation logic had no home outside the framework — making it untestable and tangled. MVP solved this by extracting all presentation logic into a **Presenter**, a plain class with no Android dependencies, leaving the Activity as a passive View."},{t:"list",items:["**Testability** — the Presenter is a pure Kotlin/Java class, so you can unit-test presentation logic on the JVM by mocking the View interface. In MVC that logic was welded to the Activity and needed an emulator.","**A home for logic** — MVC gave logic no structural place, so it accreted in the Activity. MVP designates the Presenter as that home.","**Decoupling View from Model** — in MVC the View reads the Model directly (coupling); in MVP the Presenter mediates all View↔Model traffic, so the View never touches the Model."]},{t:"p",text:"The key insight is *why* MVP came before MVVM historically: when teams hit the God-Activity wall (~2013–2015), Android had no observability primitives (no LiveData, no lifecycle-aware anything). MVP needed nothing new — just plain interfaces and classes, instantly testable with JUnit + Mockito. MVVM only became practical when Jetpack shipped the ViewModel and LiveData in 2017. So MVP was the pragmatic fix available at the time."},{t:"note",text:"MVP's one-line pitch: 'move presentation logic into a framework-free Presenter so it's testable' — and note it won because it needed no new tooling, unlike MVVM which waited for Architecture Components."}]},{level:"junior",q:"What are the Presenter's responsibilities?",a:[{t:"p",text:"The Presenter is the middle layer that holds all presentation logic. It receives user events forwarded from the View, talks to the Model (repositories/use cases) to get or change data, and then tells the View what to display by calling methods on a View interface. It contains zero Android imports — it's a plain class."},{t:"list",items:["**Handle user events** — the View forwards input (`onRefreshClicked()`); the Presenter decides what to do.","**Talk to the Model** — call repositories/use cases to fetch or mutate data.","**Drive the View via its interface** — `view.showLoading()`, `view.showUser(name)`, `view.showError(msg)` — the Presenter *pushes* to the View imperatively.","**Presentation logic** — formatting, deciding what's visible, mapping domain data to display form.","**Manage its View reference** — attach the View on start, detach (null it) on stop to avoid leaks."]},{t:"code",title:"A Presenter's shape",code:`class UserPresenter(private val repo: UserRepository) : UserContract.Presenter {
    private var view: UserContract.View? = null
    override fun attach(v: UserContract.View) { view = v }
    override fun detach() { view = null }   // critical: avoid leaking the Activity

    override fun onLoadUser(id: String) {
        view?.showLoading()
        repo.getUser(id,
            onSuccess = { user -> view?.showUser(user.name) },   // push to View
            onError   = { e -> view?.showError(e.message) })
    }
}`},{t:"p",text:"The distinguishing trait vs a ViewModel: the Presenter *holds a View reference and pushes to it imperatively* (`view.showX()`), whereas a ViewModel holds no View reference and exposes state the View observes. Every MVP strength (simple, testable) and weakness (attach/detach ceremony, leak risk) flows from that one arrow — the Presenter → View reference."},{t:"note",text:"The Presenter 'pushes' to a View interface (imperative); the ViewModel exposes state the View 'pulls' (observation). That single difference explains all of MVP's ceremony."}]},{level:"junior",q:"Show a complete MVP implementation — contract, presenter, and view.",a:[{t:"p",text:"The signature artifact of MVP is the **contract** — a single interface pairing the View and Presenter interfaces for one screen, so the whole interaction surface is readable in one place and both sides are mockable. Here's a full example."},{t:"code",title:"The contract",code:`interface UserContract {
    interface View {                       // what the Presenter can do TO the View
        fun showLoading()
        fun hideLoading()
        fun showUser(name: String)
        fun showError(message: String)
    }
    interface Presenter {                  // what the View can ask the Presenter
        fun attach(view: View)
        fun detach()
        fun onLoadUser(id: String)
    }
}`},{t:"code",title:"The Presenter (plain class — unit-testable)",code:`class UserPresenter(private val repo: UserRepository) : UserContract.Presenter {
    private var view: UserContract.View? = null
    override fun attach(view: UserContract.View) { this.view = view }
    override fun detach() { view = null }

    override fun onLoadUser(id: String) {
        view?.showLoading()
        repo.getUser(id) { result ->
            view?.hideLoading()
            result.onSuccess { view?.showUser(it.name) }
                  .onFailure { view?.showError(it.message ?: "Error") }
        }
    }
}`},{t:"code",title:"The View (a passive Activity)",code:`class UserActivity : AppCompatActivity(), UserContract.View {
    private lateinit var presenter: UserContract.Presenter
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_user)
        presenter = UserPresenter(repository)
        presenter.attach(this)                     // attach on start
        loadButton.setOnClickListener { presenter.onLoadUser("42") }
    }
    override fun onDestroy() { presenter.detach(); super.onDestroy() }  // detach on stop

    override fun showLoading() { progress.isVisible = true }
    override fun hideLoading() { progress.isVisible = false }
    override fun showUser(name: String) { nameText.text = name }
    override fun showError(message: String) { toast(message) }
}`},{t:"p",text:"Notice the ceremony: the contract file, the attach/detach lifecycle, and the `view?.` null-guard on every callback (the view may be detached when the async result arrives). This boilerplate — one View interface + implementation per screen, plus lifecycle plumbing — is exactly why MVVM's observation model eventually replaced it."},{t:"note",text:"The contract interface is MVP's signature — mention it names the whole screen's interaction surface and makes both sides mockable, then note the boilerplate it implies."}]},{level:"junior",q:"How do you handle loading and error states in MVP?",a:[{t:"p",text:"In MVP you handle loading/error by adding explicit methods to the View interface and having the Presenter call them imperatively at the right moments — `view.showLoading()` before an async call, `view.hideLoading()` and `view.showUser()` or `view.showError()` in the callback. The View just implements each method by mutating widgets."},{t:"code",title:"Loading/error as View-interface methods",code:`// Contract View methods:
fun showLoading(); fun hideLoading()
fun showContent(data: Data); fun showError(msg: String)

// Presenter drives them:
override fun onLoad() {
    view?.showLoading()
    repo.load { result ->
        view?.hideLoading()                    // must remember in BOTH branches
        result.onSuccess { view?.showContent(it) }
              .onFailure { view?.showError(it.message ?: "Error") }
    }
}`},{t:"p",text:"The awkwardness — and a good thing to point out — is that the *state* isn't represented as a single value; it's implied by which combination of `showX()` methods were last called. This is the imperative-UI problem: you must remember every transition (show loading → hide loading → hide error → show content), and forgetting one leaves stale UI (spinner stuck, error lingering under content). It's testable (you can `verify(view).showLoading()` then `showContent()`), but brittle to the exact call sequence."},{t:"p",text:"MVVM fixes this by modeling the screen as a single immutable `UiState` (Loading/Content/Error) that the ViewModel emits and the View renders wholesale — one value, no forgotten transitions. That contrast (imperative view-method calls vs a single observable state) is the crux of why MVP gave way to MVVM."},{t:"note",text:"In MVP loading/error are View-interface *methods* the Presenter calls; the weakness is state lives implicitly in the call sequence — MVVM's single UiState value is the fix."}]},{level:"junior",q:"How do you pass dependencies and arguments into a Presenter?",a:[{t:"p",text:"Because a Presenter is a plain class you construct yourself (unlike a ViewModel), you pass its dependencies through its constructor — repositories, use cases, schedulers — and runtime arguments (like an id) either through the constructor or through the method that starts the work. With a DI framework you inject the Presenter's dependencies via `@Inject constructor`."},{t:"code",title:"Constructor injection + runtime args",code:`// Dependencies via constructor (DI-friendly)
class UserPresenter @Inject constructor(
    private val repo: UserRepository,
    private val analytics: Analytics,
) : UserContract.Presenter {
    // Runtime arg passed to the method that starts work:
    override fun onLoadUser(id: String) { /* use id */ }
}

// The Activity creates/injects it and passes the id:
presenter.attach(this)
presenter.onLoadUser(intent.getStringExtra("id")!!)`},{t:"p",text:"This is actually *simpler* than ViewModel construction historically: a ViewModel with constructor arguments needed a hand-written `ViewModelProvider.Factory` (before Hilt generated it), whereas a Presenter is just `UserPresenter(repo)`. The trade-off is that the Presenter's *lifecycle* is now your responsibility — you construct it, attach the View, and must detach/recreate it correctly across configuration changes, which the ViewModel handles automatically."},{t:"p",text:"The gotcha: because you construct the Presenter in `onCreate`, a naive implementation creates a *new* Presenter on every rotation, losing in-flight work and state. Production MVP had to keep the Presenter alive across rotation (retained fragments, loaders, or libraries like Mosby/Nucleus) — the very machinery the Jetpack ViewModel made obsolete."},{t:"note",text:"Presenters take deps via constructor (easy, DI-friendly) — but you own their lifecycle, so surviving rotation needs retain hacks that ViewModel later eliminated."}]},{level:"junior",q:"What does 'Passive View' mean, and why is it the ideal in MVP?",a:[{t:"p",text:"A Passive View is a View that does *nothing on its own* — it has no logic, no decisions, no direct Model access. It only implements methods that mutate widgets (`showUser(name)` sets a text field) and forwards raw user input to the Presenter (`onClick { presenter.onSave() }`). Every decision about *what* to show lives in the Presenter."},{t:"p",text:"It's the ideal in MVP because it maximizes testability: if the View is truly passive, then *all* the interesting logic (what to show, when, in what format) is in the Presenter, which is unit-testable. Any `if` beyond trivial rendering that creeps into the View is logic escaping the Presenter — untestable and a design smell."},{t:"code",title:"Passive vs leaky View",code:`// PASSIVE (ideal): View just renders what it's told
override fun showUser(displayName: String) { nameText.text = displayName }
// Presenter decided the format: view.showUser(if (premium) "⭐ $name" else name)

// LEAKY (bad): logic in the View — now untestable
override fun showUser(user: User) {
    nameText.text = if (user.isPremium) "⭐ \${user.name}" else user.name  // decision in View!
}`},{t:"p",text:"The contrast is with 'Supervising Controller' (the other MVP flavor), where the View handles *simple* declarative binding itself and the Presenter only handles complex logic. Supervising Controller has less boilerplate but lets some logic leak into the View. Passive View is stricter (more boilerplate, total testability); Supervising Controller is pragmatic (less boilerplate, some untestable binding). The industry slide from Passive View → Supervising Controller → MVVM is one continuous move toward declarative binding."},{t:"note",text:"Passive View = the View makes zero decisions, so 100% of logic is in the testable Presenter. The litmus test: any `if` in the View (beyond trivial rendering) is logic that leaked out."}]},{level:"senior",q:"How exactly does unit-testing a Presenter work?",a:[{t:"p",text:"You unit-test a Presenter by mocking the View interface and the repository, calling the Presenter's event methods, and then *verifying* that the expected View methods were called (with the right arguments, in the right order). Because the Presenter's only 'UI' dependency is the View *interface*, you never need a real Activity or device."},{t:"code",title:"A Presenter test (JUnit + MockK)",code:`@Test fun loadUser_success_showsUser() {
    val view: UserContract.View = mockk(relaxed = true)
    val repo: UserRepository = mockk()
    every { repo.getUser("42", any()) } answers {
        secondArg<(Result<User>) -> Unit>()(Result.success(User("Revanth", premium = true)))
    }
    val presenter = UserPresenter(repo)
    presenter.attach(view)

    presenter.onLoadUser("42")

    verifyOrder {                                  // assert INTERACTIONS
        view.showLoading()
        view.hideLoading()
        view.showUser("⭐ Revanth")               // and the formatted output
    }
    verify(exactly = 0) { view.showError(any()) }  // and what did NOT happen
}`},{t:"list",items:["**Interaction-based** — you assert 'these View methods were called', not 'this state equals X'. This is the key stylistic difference from MVVM testing (which asserts emitted state).","**Mock the View interface** — the passive View is trivial to mock; you check what the Presenter told it to do.","**No device** — pure JVM, fast, deterministic."]},{t:"p",text:"The downside of interaction-based tests: they're coupled to the *exact* View method calls, so refactoring the Presenter's implementation (splitting `showUser` into two calls) breaks tests even when behavior is identical. MVVM's state assertions are more robust because they check the *outcome* (the final state) rather than *how* it was produced."},{t:"note",text:"MVP tests are *interaction-based* (`verify(view).showUser(...)`); MVVM tests are *state-based* (assert the emitted UiState). State-based is more refactor-proof — a good comparison to volunteer."}]},{level:"junior",q:"MVC vs MVP — what specifically changed between them?",a:[{t:"p",text:"The single structural change from MVC to MVP is *who the View talks to*. In MVC the View reads/observes the Model directly. In MVP the View is fully insulated from the Model — all traffic goes through the Presenter, which holds a reference to the View (via an interface) and pushes updates to it."},{t:"table",headers:["","MVC","MVP"],rows:[["View ↔ Model","View observes Model directly","fully separated — Presenter mediates"],["Middle → View","Controller usually doesn't touch View","Presenter holds a View interface, pushes to it"],["Where logic lives","Activity (View+Controller fused)","the Presenter (framework-free)"],["Testability","poor (welded to Android)","good (mock the View interface)"],["Boilerplate","low","higher (contract interfaces per screen)"]]},{t:"p",text:"The practical consequences: MVP made presentation logic *testable* (the Presenter is a plain class) and *decoupled* the View from the Model (so Model refactors don't ripple into the UI). The cost was boilerplate — a View interface and Presenter interface (the contract) per screen, plus attach/detach lifecycle management. MVC has less ceremony but no testable seam."},{t:"note",text:"The one-sentence delta: 'MVP inserts a testable Presenter between View and Model, so the View never touches the Model and logic leaves the Activity — at the cost of contract boilerplate.'"}]},{level:"senior",q:"For a brand-new Android project, would you choose MVP or MVVM? Defend it.",a:[{t:"p",text:"For any new Android project, I'd choose MVVM (with UDF and a single UiState), not MVP. There's essentially no scenario in modern Android where MVP is the better choice for new code — MVVM solves every MVP pain point using tooling that's now standard, and MVP's only historical advantage (no new tooling required) is irrelevant now."},{t:"list",items:["**Config-change survival** — the Jetpack ViewModel survives rotation for free; MVP needs retain hacks (retained fragments, loaders, Mosby/Nucleus).","**No attach/detach ceremony** — MVVM's observation model means no View reference to manage, no `view?.` null dance, no leak risk from a forgotten `detach()`.","**Less boilerplate** — one `UiState` class vs a View interface + implementation per screen.","**State replay for free** — a new observer of a `StateFlow` immediately gets the current state; a re-attached MVP View is blank until the Presenter replays.","**KMP-ready** — MVVM's state (StateFlow) works in shared code; MVP's imperative view-interface model doesn't fit SwiftUI/Compose declarative UI."]},{t:"p",text:"The honest caveat: MVP is *conceptually fine* and you'll meet it in codebases from 2014–2017. But 'would you *choose* it for new code?' — no. Every MVP pain (rotation, ceremony, boilerplate, state replay) maps to a MVVM feature that eliminates it, and MVP as a stepping stone to MVVM just doubles the work. The senior signal is being able to enumerate *pain → MVVM fix* rather than just saying 'MVVM is newer'."},{t:"note",text:"Answer decisively (MVVM) but justify with the pain→fix mapping: attach/detach→observation, retain hacks→retained ViewModel, view-interface boilerplate→single UiState, blank-on-reattach→state replay."}]},{level:"senior",q:"How does a Presenter leak the Activity, and how do you prevent it?",a:[{t:"p",text:"A Presenter leaks the Activity when it holds a reference to the View (which on Android is the Activity/Fragment) and *outlives* it without releasing the reference. If the Activity is destroyed but the Presenter still references it — because you forgot to `detach()`, or the Presenter is retained/static, or an async callback captured the old View — the garbage collector can't reclaim the Activity and its entire view hierarchy. That's the leak."},{t:"code",title:"The leak and the fix",code:`class UserPresenter : UserContract.Presenter {
    private var view: UserContract.View? = null
    override fun attach(v: UserContract.View) { view = v }   // holds the Activity

    override fun detach() { view = null }   // THE FIX: release on destroy

    override fun onLoad() {
        repo.getUser(id) { user ->
            // if the Activity was destroyed and 'view' NOT nulled,
            // this callback touches a dead Activity -> crash or leak
            view?.showUser(user.name)   // '?.' guards, but you must also detach
        }
    }
}
// Activity: override fun onDestroy() { presenter.detach(); super.onDestroy() }`},{t:"list",items:["**Cause 1 — forgotten detach()** — the Presenter keeps the View reference after the Activity is destroyed. Always `detach()` in `onDestroy`/`onStop`.","**Cause 2 — stale async callbacks** — a network callback captured the View; it fires after destruction and touches a dead view. Guard with `view?.` *and* cancel work on detach.","**Cause 3 — retained Presenter holding a live View** — if you retain the Presenter across rotation (to survive config change) but don't swap the View reference on re-attach, the old destroyed Activity is pinned."]},{t:"p",text:"The deeper point: this whole class of bug exists *because* the Presenter holds a View reference. The ViewModel eliminates it by design — it holds *no* View reference (the View observes the ViewModel), so there's nothing to leak and no attach/detach to forget. The MVP leak is the concrete cost of the Presenter→View arrow."},{t:"note",text:"The MVP leak = Presenter holds the Activity + outlives it. Fix: detach() + cancel work. But the real lesson is that the ViewModel's no-View-reference design makes this bug *structurally impossible*."}]},{level:"senior",q:"Why did MVVM ultimately replace MVP? Map each MVP pain to its MVVM fix.",a:[{t:"p",text:"MVVM replaced MVP because it eliminated every one of MVP's structural pain points — all of which radiate from a single root cause: the Presenter holds a reference to the View and pushes to it imperatively. MVVM's inversion (the View observes state the ViewModel exposes, no View reference) dissolves the whole class of problems."},{t:"table",headers:["MVP pain","Root cause","MVVM fix"],rows:[["Presenter dies on rotation / retain hacks","you own the Presenter's lifecycle","Jetpack ViewModel survives config changes natively"],["attach/detach ceremony + null-view dance","Presenter holds a View reference","no View reference — the View observes state"],["Leaks the Activity if detach forgotten","Presenter → View reference outlives Activity","no View reference to leak"],["Re-attached View is blank until replay","imperative push, no current value","StateFlow replays current state to new observers"],["View interface + impl per screen (boilerplate)","imperative view-method contract","one immutable UiState the View renders"],["Doesn't fit declarative/KMP UI","imperative view.showX() model","observable state maps to Compose/SwiftUI"]]},{t:"p",text:"The single sentence: MVP is 'the middle layer pushes to a View it holds'; MVVM is 'the middle layer exposes state a View observes'. Flipping that arrow removes the lifecycle ceremony, the leak risk, the boilerplate, and the blank-on-reattach problem simultaneously — plus it makes state work in KMP shared code. That's why the industry moved wholesale once Jetpack made it easy."},{t:"note",text:"Every MVP pain traces to one thing — the Presenter holds and pushes to the View. MVVM inverts it to observation, and *all* the pains vanish together. Delivering that unified explanation beats listing fixes."}]},{level:"senior",q:"Is MVP dead? Where, if anywhere, would you still use it?",a:[{t:"p",text:"MVP isn't 'dead' as a concept — it's conceptually sound and still runs in huge production codebases — but it's effectively *legacy* for new Android code: there's no scenario where you'd choose it over MVVM for a fresh Android project. So the honest answer is 'you'll maintain it, but you won't start with it'."},{t:"list",items:["**You'll meet it in** — codebases started 2014–2017 (banking, e-commerce, enterprise apps), and interviews for teams maintaining them. Knowing MVP is a maintenance skill.","**Conceptually still valid where** — a platform *lacks* an observation toolkit and lifecycle-aware state (MVP needs no special tooling). But modern Android/KMP has all of that, so the advantage is gone.","**Where you would NOT use it** — any new Android app (MVVM wins on every axis), and anything KMP (imperative view-interfaces don't fit shared code / declarative UI)."]},{t:"p",text:"The nuance worth adding: migrating MVP → MVVM is mechanical (each `view.showX(args)` call becomes a field on `UiState`; the Presenter's methods become ViewModel functions; the contract dies; interaction tests convert to state assertions, usually getting simpler). So even in a legacy MVP codebase, the path forward is well-understood — you don't rewrite, you migrate screen by screen. Recognizing MVP as 'maintain and migrate, don't start' is the mature position."},{t:"note",text:"'MVP is legacy, not dead' — you'll maintain and migrate it (migration is mechanical: view.showX → UiState field), but never choose it for new Android/KMP work."}]},{level:"senior",q:"How would you inject Presenters with Dagger/Hilt?",a:[{t:"p",text:"Since a Presenter is a plain class you construct, you inject its dependencies with `@Inject constructor` and let Dagger/Hilt build it — then the Activity/Fragment gets the Presenter injected as a field (because the framework instantiates Activities, so you can't constructor-inject them). The Presenter's *dependencies* are constructor-injected; the Presenter *itself* is field-injected into the View."},{t:"code",title:"Injecting a Presenter with Hilt",code:`// Presenter deps via constructor injection
class UserPresenter @Inject constructor(
    private val repo: UserRepository,
) : UserContract.Presenter { /* ... */ }

// Bind the interface to the impl
@Module @InstallIn(ActivityComponent::class)
abstract class PresenterModule {
    @Binds abstract fun bindUserPresenter(impl: UserPresenter): UserContract.Presenter
}

// Field-inject the Presenter into the (framework-created) Activity
@AndroidEntryPoint
class UserActivity : AppCompatActivity(), UserContract.View {
    @Inject lateinit var presenter: UserContract.Presenter
    override fun onCreate(s: Bundle?) {
        super.onCreate(s)
        presenter.attach(this)   // still must manage attach/detach yourself
    }
}`},{t:"p",text:"The catch DI *doesn't* solve: the Presenter's *lifecycle*. Even with Hilt injecting it, you still manage attach/detach and (harder) surviving configuration changes — Hilt scopes the Presenter to the Activity component, so it's recreated on rotation by default, losing state. To retain it you'd scope it more carefully or use `ActivityRetainedComponent`, which is fiddly. This is another place where the ViewModel wins: `@HiltViewModel` gives you injection *and* automatic config-change survival *and* no attach/detach — all the things you're hand-managing in injected MVP."},{t:"note",text:"Deps → constructor-inject; Presenter → field-inject into the Activity. But DI doesn't fix MVP's lifecycle — you still manage attach/detach and retention, which @HiltViewModel handles for free."}]},{level:"senior",q:"You inherit an MVP codebase full of God Presenters. How do you improve it?",a:[{t:"p",text:"A God Presenter is the same failure as a God Activity relocated one layer over: without a disciplined data/domain layer, the Presenter absorbs networking, caching, business rules, and mapping. So the fix is the same shape — give logic below the Presenter a home, and thin the Presenter to orchestration — whether or not you also migrate to MVVM."},{t:"list",items:["**Push logic down first** — extract networking/DB into repositories and business rules into use cases. This is mechanical, low-risk, and immediately shrinks Presenters (often by half). The Presenter should mostly wire events to use cases and format results.","**Extract mapping and formatting** — into dedicated mapper classes with their own tests, out of the Presenter.","**Split by responsibility** — if a Presenter handles several independent widgets, consider splitting it; a screen-level Presenter can coordinate smaller ones.","**Add characterization tests before big changes** — Presenters *are* testable (mock the View), so pin current behavior with interaction tests, then refactor safely.","**Then decide: migrate to MVVM or not** — if the team is investing long-term, migrate screen by screen (view.showX → UiState field, Presenter → ViewModel); the interaction tests convert to state tests. If not, at least the extraction above makes the MVP maintainable."]},{t:"p",text:"The guardrail I'd apply in review: if a Presenter imports a data-source library (Retrofit/Room types) or exceeds a few hundred lines, the layering has broken down below it — the same rule as for a bloated ViewModel. God Presenters aren't an MVP problem specifically; they're a 'no data/domain layer' problem that any pattern suffers without discipline."},{t:"note",text:"God Presenter = God Activity moved one layer over. Fix it the same way: extract repositories/use cases so logic has a home, thin the Presenter to orchestration — then optionally migrate to MVVM screen by screen."}]},{level:"junior",q:"What is the difference between Passive View and Supervising Controller in one practical example?",a:[{t:"p",text:"Both are MVP flavors; the difference is how much the View is allowed to do. In **Passive View**, the Presenter drives *every* pixel — even simple data binding goes through a `view.showX()` call. In **Supervising Controller**, the View handles *simple, declarative* binding itself (like data binding a name field directly), and the Presenter only steps in for *complex* logic."},{t:"code",title:"Same feature, two flavors",code:`// PASSIVE VIEW: Presenter formats and pushes everything
// Presenter:  view.showUserName(if (u.premium) "⭐ " + u.name else u.name)
// View:       fun showUserName(s: String) { nameText.text = s }

// SUPERVISING CONTROLLER: View binds the simple field itself...
// View (XML data binding): android:text="@{user.name}"
// ...and the Presenter only handles the complex 'premium badge' logic:
// Presenter:  view.setPremiumBadgeVisible(u.premium)`},{t:"list",items:["**Passive View** — maximum testability (all display decisions are in the testable Presenter), maximum boilerplate (a view method for everything).","**Supervising Controller** — less boilerplate (simple binding is declarative), but the simple-binding decisions escape testability, and logic can creep into the View."]},{t:"p",text:"The reason this matters historically: Supervising Controller with data binding is *already halfway to MVVM* — 'the View declaratively binds to data' is the MVVM idea. So the industry path Passive View → Supervising Controller → MVVM is one smooth slide, each step replacing imperative `view.showX()` calls with more declarative binding, until MVVM makes *all* of it observation."},{t:"note",text:"Passive View (Presenter drives everything, testable, verbose) vs Supervising Controller (View binds simple fields, less verbose, some logic leaks) — and note Supervising Controller is a step toward MVVM."}]},{level:"junior",q:"Why does every async callback in a Presenter need a `view?.` null check?",a:[{t:"p",text:"Because the View reference can be *null* when an async result arrives. The Presenter holds the View only between `attach()` and `detach()`. If the Activity is destroyed (rotation, back press) while a network call is in flight, `detach()` sets the reference to null — but the callback still fires later, and touching a null (or dead) View would crash. The `view?.` safe call makes the update a no-op when detached."},{t:"code",title:"The null-view dance",code:`override fun onLoad() {
    view?.showLoading()
    repo.getUser(id) { user ->
        // by now the user may have rotated -> detach() nulled 'view'
        view?.showUser(user.name)   // '?.' = do nothing if detached
    }
}`},{t:"p",text:"The subtle bug this *creates*: if the View was detached, the update is silently *dropped*. So if you rotate during a load, the data never shows on the new screen unless the Presenter re-requests or caches — the re-attached View is blank. This 'dropped update on re-attach' is one of MVP's real pain points, and it's why production MVP needed retained Presenters that replay state on re-attach (Mosby's ViewState). MVVM's StateFlow fixes it for free: a new observer immediately gets the current state."},{t:"note",text:"`view?.` guards against updating a detached View — but the update is *dropped*, so a re-attached View is blank. That dropped-update problem is exactly what StateFlow's state-replay solves in MVVM."}]},{level:"senior",q:"In MVP, how do you handle a result that arrives while the View is detached (e.g. rotation during a load)?",a:[{t:"p",text:"This is MVP's genuinely hard problem. When a result arrives while the View is detached, a naive `view?.showX()` simply drops it, so the re-attached View shows nothing. To handle it correctly, the Presenter must *survive* the rotation and *remember the last state* so it can replay it onto the new View when it re-attaches."},{t:"list",items:["**Retain the Presenter across rotation** — via a retained fragment, a Loader, a static cache, or a library (Mosby, Nucleus). Without this, a new Presenter is created and the in-flight work + result are lost entirely.","**Cache the last state in the Presenter** — the Presenter stores the latest result/loading/error (a 'ViewState'). When a result arrives while detached, it updates this cached state instead of (or in addition to) calling the View.","**Replay on re-attach** — in `attach()`, the Presenter pushes its cached state onto the fresh View, so the new screen shows the loaded data immediately. Mosby's `MvpViewStatePresenter` formalizes exactly this.","**Or buffer pending view commands** — queue the `showX()` calls that arrived while detached and flush them on re-attach (Nucleus's approach)."]},{t:"code",title:"Cache-and-replay pattern",code:`class UserPresenter {
    private var view: View? = null
    private var lastState: UiState = UiState.Idle   // remembered across detach

    override fun attach(v: View) { view = v; render(lastState) }  // replay!
    override fun detach() { view = null }

    override fun onLoad() {
        lastState = UiState.Loading; render(lastState)
        repo.getUser(id) { user ->
            lastState = UiState.Content(user)   // update state even if detached
            render(lastState)                   // no-op if view == null, but state is kept
        }
    }
    private fun render(s: UiState) { view?.let { /* apply s to it */ } }
}`},{t:"p",text:"The tell here is recognizing that this machinery — retain the Presenter, cache the state, replay on re-attach — is *exactly* what the Jetpack ViewModel + StateFlow give you automatically. The ViewModel survives rotation (no retain hack), holds the current state (no manual cache), and a re-collecting View instantly receives it (no manual replay). So MVP's hardest problem is MVVM's free lunch — which is the point to make."},{t:"note",text:"MVP's answer to detached-result: retain the Presenter + cache state + replay on re-attach (Mosby's ViewState). That whole dance is what ViewModel+StateFlow does for free — the clearest illustration of why MVVM won."}]}],ux={id:"mvp",title:"MVP",tagline:"Model – View – Presenter: contracts, passive views, the attach/detach lifecycle problem, presenter testing, and why MVVM replaced it.",tags:["Android","Contracts","Presenter","Legacy","Testing"],content:dx,qa:hx},px=[{heading:"What is MVVM and why it exists",blocks:[{t:"p",text:"**MVVM (Model–View–ViewModel)** is a UI architecture pattern that separates *what is shown* (View) from *the state and logic that decides what to show* (ViewModel) and from *where data comes from* (Model). It was introduced by Microsoft (WPF, 2005) and became the de-facto standard on Android after Google shipped Jetpack **Architecture Components** (2017) with a first-class `ViewModel` class."},{t:"list",items:["**Model** — the data layer: repositories, data sources (network, database, preferences), and domain/business logic. It knows nothing about UI.","**View** — Activity / Fragment / Composable. Renders state and forwards user input. It is as dumb as possible: no business logic, no data access.","**ViewModel** — holds UI state, exposes it as an observable stream, and contains presentation logic. It transforms Model data into something renderable and reacts to user events."]},{t:"p",text:"The defining rule is the **direction of references**: the View knows the ViewModel, the ViewModel knows the Model, but **never the other way around**. The ViewModel has *no reference to the View* — the View *observes* the ViewModel. This one-way dependency is what makes MVVM testable and lifecycle-safe, and it is the main difference from MVP (where the Presenter holds a View interface and calls it directly)."},{t:"note",text:'Interview one-liner: "MVVM separates UI from logic through **observable state** — the ViewModel exposes state, the View subscribes to it, and events flow the opposite way. The ViewModel never touches the View, which gives us testability, lifecycle safety and free survival of configuration changes."'}]},{heading:"The three layers on Android — minimal example",blocks:[{t:"p",text:"A canonical modern stack: Repository (Model) → ViewModel exposing `StateFlow<UiState>` → Compose UI collecting it. The same shape works with XML views observing via `repeatOnLifecycle`."},{t:"code",title:"Model — repository",code:`class UserRepository(
    private val api: UserApi,
    private val dao: UserDao,
) {
    fun observeUser(id: String): Flow<User> = dao.observeUser(id)

    suspend fun refreshUser(id: String) {
        val remote = api.fetchUser(id)
        dao.upsert(remote.toEntity())
    }
}`},{t:"code",title:"ViewModel — state + presentation logic",code:`data class UserUiState(
    val isLoading: Boolean = false,
    val user: User? = null,
    val error: String? = null,
)

class UserViewModel(
    private val repository: UserRepository,
    savedStateHandle: SavedStateHandle,
) : ViewModel() {

    private val userId: String = checkNotNull(savedStateHandle["userId"])

    private val _uiState = MutableStateFlow(UserUiState(isLoading = true))
    val uiState: StateFlow<UserUiState> = _uiState.asStateFlow()

    init {
        viewModelScope.launch {
            repository.observeUser(userId)
                .catch { e -> _uiState.update { it.copy(isLoading = false, error = e.message) } }
                .collect { user ->
                    _uiState.update { it.copy(isLoading = false, user = user, error = null) }
                }
        }
        refresh()
    }

    fun refresh() {
        viewModelScope.launch {
            _uiState.update { it.copy(isLoading = true) }
            runCatching { repository.refreshUser(userId) }
                .onFailure { e -> _uiState.update { it.copy(isLoading = false, error = e.message) } }
        }
    }
}`},{t:"code",title:"View — Compose",code:`@Composable
fun UserScreen(viewModel: UserViewModel = hiltViewModel()) {
    val state by viewModel.uiState.collectAsStateWithLifecycle()

    when {
        state.isLoading -> LoadingIndicator()
        state.error != null -> ErrorMessage(state.error, onRetry = viewModel::refresh)
        state.user != null -> UserContent(state.user)
    }
}`},{t:"list",items:["State flows **down** (ViewModel → View), events flow **up** (View → ViewModel) — this is **Unidirectional Data Flow (UDF)** and modern MVVM on Android is always described together with it.","The View never mutates state directly; it calls ViewModel functions (`viewModel.refresh()`), and the new state arrives through the observed stream.","The backing-property pattern (`_uiState` private mutable, `uiState` public read-only) guarantees only the ViewModel can change state."]}]},{heading:"Jetpack ViewModel deep dive — lifecycle & internals",blocks:[{t:"p",text:"Important distinction: **\"ViewModel\" the pattern role** vs **`androidx.lifecycle.ViewModel` the Jetpack class**. The class is Google's implementation of the role with two extra superpowers: it **survives configuration changes** and it gets a **cleanup callback** (`onCleared`). You could implement MVVM without it, but you'd lose both."},{t:"h3",text:"How it survives rotation (the internals interviewers love)"},{t:"list",items:["ViewModels are stored in a `ViewModelStore` — essentially a `Map<String, ViewModel>`.","Every `ViewModelStoreOwner` (Activity, Fragment, NavBackStackEntry) owns one store.","On a configuration change the Activity is destroyed and recreated, but the system retains its `NonConfigurationInstances` — the `ViewModelStore` rides along inside it. The recreated Activity gets the *same* store back, so `ViewModelProvider` returns the *same* ViewModel instance.","`onCleared()` is only called when the owner is destroyed **for good** — `Activity.isFinishing == true`, Fragment permanently removed, or back-stack entry popped. On rotation it is *not* called.","Consequence: a ViewModel **outlives** the Activity/Fragment instance — which is exactly why it must never reference one (that would leak the destroyed Activity until the ViewModel dies)."]},{t:"h3",text:"Creation: providers, factories, CreationExtras"},{t:"p",text:"You never call the constructor yourself in production code — you go through `ViewModelProvider` (or `by viewModels()`, `hiltViewModel()`, `koinViewModel()`), which checks the store first and only creates a new instance on a miss. Constructor arguments require a **Factory**; with Hilt, `@HiltViewModel` + `@Inject constructor` generates it for you, and `SavedStateHandle` can simply be added as a constructor parameter."},{t:"code",title:"Manual factory (what Hilt generates for you)",code:`class UserViewModelFactory(
    private val repository: UserRepository,
) : ViewModelProvider.Factory {
    override fun <T : ViewModel> create(modelClass: Class<T>, extras: CreationExtras): T {
        val handle = extras.createSavedStateHandle()
        @Suppress("UNCHECKED_CAST")
        return UserViewModel(repository, handle) as T
    }
}

// In the Fragment/Activity:
private val viewModel: UserViewModel by viewModels { UserViewModelFactory(repo) }`},{t:"h3",text:"viewModelScope"},{t:"list",items:["`viewModelScope` is a `CoroutineScope` bound to the ViewModel: `SupervisorJob() + Dispatchers.Main.immediate`.","All jobs launched in it are **cancelled automatically in `onCleared()`** — no manual cleanup, no leaked coroutines.","`SupervisorJob` means one failed child coroutine does not cancel the siblings.","`Main.immediate` executes without an extra dispatch if you're already on the main thread — avoids one-frame delays.","Never expose `viewModelScope` publicly or launch UI-lifetime work in it that must outlive the screen (e.g. an upload that should finish after the user leaves) — that belongs in an application-scoped scope or `WorkManager`."]},{t:"h3",text:"Scoping options (know all of them)"},{t:"table",headers:["Scope","How","Lives as long as"],rows:[["Activity","`by viewModels()` in Activity","the Activity (across rotations)"],["Fragment","`by viewModels()` in Fragment","the Fragment"],["Shared between fragments","`by activityViewModels()`","the host Activity — classic way to share state between sibling fragments"],["Navigation graph","`hiltViewModel(backStackEntry)` / `navGraphViewModels()`","the nav (sub)graph — great for multi-step flows like checkout or onboarding"],["Parent fragment","`by viewModels({ requireParentFragment() })`","the parent fragment"]]},{t:"note",text:"Edge case: `by viewModels()` is lazy — the ViewModel isn't created until first access. And two `by viewModels()` for the *same type with the same owner and key* return the same instance; pass a different `key` if you genuinely need two."}]},{heading:"Exposing state: LiveData vs StateFlow vs SharedFlow vs Compose State",blocks:[{t:"table",headers:["","LiveData","StateFlow","SharedFlow","Compose mutableStateOf"],rows:[["Initial value","optional","**required**","none","required"],["Lifecycle-aware out of the box","yes","no — use `repeatOnLifecycle` / `collectAsStateWithLifecycle`","no — same","n/a (Compose-managed)"],["Conflates equal values","no (re-emits same value)","**yes — skips `equals()` duplicates**","no (replay/buffer configurable)","skips duplicates (structural equality policy)"],["Operators","limited (`map`, `switchMap`)","full Flow API","full Flow API","n/a"],["KMP compatible","**no** (Android-only)","yes","yes","Compose Multiplatform only"],["Backing for one-shot events","bad (sticky)","bad (sticky, conflated)","possible (replay=0), with caveats","bad"]]},{t:"p",text:"Modern guidance: **`StateFlow` for state, and prefer state over events**. LiveData is in maintenance mode — fine in legacy code, but it's Android-only (a dealbreaker for KMP), has weak operators and no natural coroutine interop. Compose `mutableStateOf` inside a ViewModel is acceptable for Compose-only apps, but `StateFlow` keeps the ViewModel toolkit-agnostic and easier to test with Flow tooling."},{t:"code",title:"The canonical exposure pattern",code:`private val _uiState = MutableStateFlow(UiState())
val uiState: StateFlow<UiState> = _uiState.asStateFlow()

// Always mutate with update {} — it's atomic (compare-and-set loop),
// safe against concurrent updates from multiple coroutines:
fun onQueryChange(query: String) {
    _uiState.update { it.copy(query = query) }
}`},{t:"list",items:["**Why expose the read-only type?** Encapsulation: if the View could call `_uiState.value = ...`, state changes could originate anywhere and UDF collapses. The ViewModel must be the *single writer*.","**`update {}` vs `value =`**: `update` retries on CAS failure, so concurrent `copy()`-based mutations don't lose writes. `value = _uiState.value.copy(...)` from two threads can drop one update.","**Conflation edge case**: `StateFlow` never emits a value equal to the current one, and a slow collector only sees the *latest* value, not every intermediate one. If you must observe every emission (e.g. analytics of each step), StateFlow is the wrong tool — use `SharedFlow`.","**Deriving state**: use `map`/`combine` + `stateIn` instead of manually copying values between flows."]},{t:"code",title:"Derived state with combine + stateIn",code:`val uiState: StateFlow<SearchUiState> = combine(
    queryFlow,
    repository.observeResults(),
    filtersFlow,
) { query, results, filters ->
    SearchUiState(query = query, results = results.applyFilters(filters))
}.stateIn(
    scope = viewModelScope,
    started = SharingStarted.WhileSubscribed(5_000),
    initialValue = SearchUiState(),
)`}]},{heading:"Modeling UiState — one data class vs sealed hierarchy",blocks:[{t:"code",title:"Option A — single data class (flags)",code:`data class ScreenUiState(
    val isLoading: Boolean = false,
    val items: List<Item> = emptyList(),
    val error: String? = null,
)`},{t:"code",title:"Option B — sealed interface (mutually exclusive states)",code:`sealed interface ScreenUiState {
    data object Loading : ScreenUiState
    data class Success(val items: List<Item>) : ScreenUiState
    data class Error(val message: String) : ScreenUiState
}`},{t:"list",items:["**Sealed** shines when states are truly exclusive — the compiler forces exhaustive `when`, and impossible combinations (loading *and* error) can't be represented.","**Data class** shines when states overlap — e.g. showing cached items *while* refreshing *with* an error snackbar. With sealed classes that becomes state duplication.","Common hybrid: a data class whose fields include sealed sub-states (e.g. `val loadState: LoadState`).","Keep it **immutable** (val + immutable collections). Mutable state inside the object breaks Compose recomposition skipping and makes time-travel/debugging impossible.","Senior point: one `UiState` per screen is a guideline, not a law — very complex screens may expose a few cohesive streams instead of one giant object that recomposes everything."]},{t:"note",text:'Classic follow-up: "where do you transform domain models into UI models?" — in the ViewModel (or a dedicated mapper it calls). The View should receive data that is *ready to render* (formatted strings, resolved colors as semantic enums, stable list keys), so formatting logic is testable.'}]},{heading:"One-shot events — the classic hard problem",blocks:[{t:"p",text:"Navigation, snackbars, toasts — things that must happen **exactly once** — don't fit naturally into sticky state: put `showSnackbar = true` in a `StateFlow` and it re-fires after rotation. Every approach has trade-offs; interviewers want you to know all of them."},{t:"table",headers:["Approach","How","Problems"],rows:[["`SingleLiveEvent`","LiveData subclass that delivers once","legacy hack; only one observer; race-prone — avoid"],["`SharedFlow(replay = 0)`","emit events, collect in View","**events emitted while no collector is active are dropped** (e.g. during rotation or in background)"],["`Channel` + `receiveAsFlow()`","buffered; delivery is suspended until a collector exists","events survive gaps in collection, but delivery to *multiple* collectors is fan-out (each event to only one); still lost on process death"],["**State-based events (Google's recommendation)**","model the event as state; View performs it and calls back `eventConsumed()`","slightly more ceremony, but survives config change AND process death (if in SavedStateHandle); no delivery-guarantee ambiguity"]]},{t:"code",title:"Channel-based events (pragmatic, widely used)",code:`private val _events = Channel<UiEvent>(Channel.BUFFERED)
val events: Flow<UiEvent> = _events.receiveAsFlow()

fun onSaveClicked() {
    viewModelScope.launch {
        repository.save()
        _events.send(UiEvent.NavigateBack)
    }
}

// View:
LaunchedEffect(Unit) {
    viewModel.events.collect { event ->
        when (event) {
            UiEvent.NavigateBack -> navController.popBackStack()
            is UiEvent.ShowSnackbar -> snackbarHostState.showSnackbar(event.message)
        }
    }
}`},{t:"code",title:"State-based events (most robust)",code:`data class UiState(
    val savedMessage: String? = null, // non-null == "show it"
)

fun onMessageShown() {
    _uiState.update { it.copy(savedMessage = null) }
}

// View:
state.savedMessage?.let { message ->
    LaunchedEffect(message) {
        snackbarHostState.showSnackbar(message)
        viewModel.onMessageShown()
    }
}`},{t:"note",text:`Senior answer: "I default to modeling events as state per Google's guidance, because Channel/SharedFlow events can be dropped or double-handled around lifecycle gaps. Where true fire-and-forget is fine (analytics, haptics), a Channel is acceptable. And navigation-as-state also plays better with deep links and process death."`}]},{heading:"Configuration changes vs process death (know the difference cold)",blocks:[{t:"table",headers:["","Configuration change (rotation, locale, dark mode, resize)","System-initiated process death"],rows:[["Activity","destroyed & recreated","whole process killed"],["ViewModel","**survives** (same instance)","**destroyed** — recreated from scratch"],["In-memory state (StateFlow values)","survives","**lost**"],["SavedStateHandle / onSaveInstanceState","available (not needed)","**restored** — this is what it's for"],["How to test","rotate device",'background the app → "Terminate" in Android Studio, or `adb shell am kill <pkg>` — then relaunch from recents']]},{t:"p",text:"**`SavedStateHandle`** is a key-value map injected into the ViewModel that participates in the saved-instance-state mechanism. It also receives **navigation arguments** (Navigation component puts destination args into it). Constraint: values must be `Bundle`-compatible (primitives, `Parcelable`, `Serializable`) and **small** — the Binder transaction limit (~1 MB shared across the whole transaction) means you save *identifiers and input*, never lists of data. Restore heavy data from the repository/database using the saved id."},{t:"code",title:"SavedStateHandle + StateFlow integration",code:`class SearchViewModel(
    private val savedStateHandle: SavedStateHandle,
    repository: SearchRepository,
) : ViewModel() {

    // Survives config change AND process death; also writable:
    val query: StateFlow<String> = savedStateHandle.getStateFlow("query", "")

    fun onQueryChange(newQuery: String) {
        savedStateHandle["query"] = newQuery // updates the StateFlow too
    }

    @OptIn(ExperimentalCoroutinesApi::class)
    val results: StateFlow<List<Result>> = query
        .debounce(300)
        .flatMapLatest { q -> repository.search(q) }
        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), emptyList())
}`},{t:"note",text:'Interview trap: "does ViewModel survive process death?" — **No.** A surprising number of candidates say yes. ViewModel survives *configuration changes only*. Process death recovery = SavedStateHandle (for input/ids) + persistent layer (for data). A senior answer also mentions that over-relying on `savedInstanceState` for big data causes `TransactionTooLargeException` crashes.'}]},{heading:"Collecting safely: lifecycle-aware collection & WhileSubscribed(5000)",blocks:[{t:"list",items:["Flows are **not lifecycle-aware**; collecting in `lifecycleScope.launch { }` naively keeps collecting while the app is backgrounded — wasted work, possible crashes touching views after stop.","**XML views**: `viewLifecycleOwner.lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { flow.collect { ... } } }` — collection is **cancelled** at STOP and **restarted** at START.","**Why not `launchWhenStarted`?** It only *suspends* the collector while stopped — the upstream flow (e.g. a location listener) stays active, buffering. Deprecated for this reason. `repeatOnLifecycle` cancels the whole collection.","**Compose**: `collectAsStateWithLifecycle()` (lifecycle-runtime-compose) does the same; plain `collectAsState()` keeps collecting when the app is backgrounded.","Fragment gotcha: always use `viewLifecycleOwner`, not the Fragment's own lifecycle — the view can be destroyed and recreated (back stack) while the Fragment instance lives, leaking observers or touching a dead view."]},{t:"p",text:"**Why `SharingStarted.WhileSubscribed(5_000)`?** With `stateIn`, this stops the upstream flow 5 seconds after the last collector disappears. The magic number 5000 exists because a **configuration change** briefly drops all collectors — the timeout keeps the upstream (DB query, network observation) alive through rotation, but shuts it down for a real backgrounding (onStop → more than 5s). `Eagerly` never stops (fine for cheap, always-needed state); `Lazily` starts on first collector and never stops."}]},{heading:"MVVM with Clean Architecture — repositories, use cases, mapping",blocks:[{t:"p",text:"MVVM only prescribes the *presentation* boundary. In real apps it's combined with a layered data architecture. Google's recommended architecture: **UI layer (View + ViewModel) → optional Domain layer (use cases) → Data layer (repositories → data sources)**."},{t:"list",items:["**Repository**: single source of truth for one data type; decides between network/cache/DB; exposes `Flow` and suspend functions; hides data-source details from the ViewModel.","**Use case (interactor)**: a class encapsulating one business action (`GetUserProfileUseCase`), usually with a single `operator fun invoke()`. Add them when logic is *reused across ViewModels* or complex enough to isolate — a use case that only forwards to a repository is ceremony; be ready to defend either stance.","**Model mapping**: DTO (network) → entity (DB) → domain model → UI model. Each layer owns its model; mapping at boundaries prevents e.g. a server field rename from rippling into composables.","**Threading rule**: the ViewModel should be able to call the data layer from the main thread — repositories/use cases are *main-safe* and move themselves to the right dispatcher (`withContext(ioDispatcher)`) internally.","**Dependency rule**: dependencies point inward (UI → domain → data interfaces). The domain layer has no Android imports — which is exactly what makes it shareable in KMP."]}]},{heading:"MVVM in Kotlin Multiplatform",blocks:[{t:"p",text:"KMP is where MVVM choices really get stress-tested, because the ViewModel and Model can move into `commonMain` while each platform keeps its native (or Compose Multiplatform) UI."},{t:"list",items:["**`androidx.lifecycle:lifecycle-viewmodel` is multiplatform since 2.8** — `ViewModel`, `viewModelScope` and `onCleared` are usable in `commonMain`. `SavedStateHandle` support arrived for CMP in later releases. Before that, the community standard was **moko-mvvm** or hand-rolled `expect/actual` ViewModels.","**LiveData does not exist in KMP** — one of the strongest practical reasons the ecosystem standardized on `StateFlow`.","**Compose Multiplatform**: `viewModel()` / koin's `koinViewModel()` work in common code; ViewModel scoping ties into CMP navigation back-stack entries just like on Android.","**iOS with native SwiftUI**: Swift doesn't understand coroutines/Flow generics natively. Options: expose `StateFlow` and wrap it with a closure-based observer class in Kotlin; use **SKIE** (turns Flows into Swift `AsyncSequence` and adds proper enum bridging); or KMP-NativeCoroutines. In SwiftUI you typically wrap the shared ViewModel in an `ObservableObject`/`@Observable` adapter that subscribes and republishes state.",'**Lifecycle mismatch**: iOS has no Activity recreation, so "survives configuration change" is meaningless there — but `onCleared`-style cleanup still matters. The shared VM\'s lifetime is managed by whoever owns the adapter (e.g. deinit of the SwiftUI wrapper calls `clear()`).',"**Threading**: with the new K/N memory manager, freezing is gone; still, ensure state updates land on the main thread when driving SwiftUI (`Dispatchers.Main` in shared code)."]},{t:"code",title:"Shared ViewModel in commonMain (lifecycle 2.8+)",code:`// commonMain
class ProfileViewModel(
    private val repository: ProfileRepository,
) : ViewModel() {

    private val _uiState = MutableStateFlow<ProfileUiState>(ProfileUiState.Loading)
    val uiState: StateFlow<ProfileUiState> = _uiState.asStateFlow()

    init {
        viewModelScope.launch {
            repository.observeProfile()
                .catch { _uiState.value = ProfileUiState.Error(it.message ?: "Unknown") }
                .collect { _uiState.value = ProfileUiState.Content(it) }
        }
    }
}`},{t:"code",title:"iOS consumption (SwiftUI adapter, with SKIE)",code:`@MainActor
final class ProfileObservable: ObservableObject {
    @Published var state: ProfileUiState = ProfileUiState.Loading()
    private let viewModel: ProfileViewModel

    init(viewModel: ProfileViewModel) {
        self.viewModel = viewModel
        Task {
            // SKIE exposes StateFlow as an AsyncSequence
            for await state in viewModel.uiState {
                self.state = state
            }
        }
    }

    deinit { viewModel.clear() }
}`},{t:"note",text:'Strong senior talking point: "In KMP I keep the ViewModel in commonMain exposing StateFlow, which Android/CMP consume directly; for SwiftUI I bridge via SKIE into an ObservableObject adapter. The pattern stays MVVM everywhere — only the observation mechanism is platform-specific."'}]},{heading:"Testing ViewModels",blocks:[{t:"p",text:"Testability is MVVM's headline benefit — the ViewModel is a plain class with no Android UI dependencies, tested on the JVM with fake/mock repositories. Two pieces of infrastructure make coroutine ViewModels testable:"},{t:"code",title:"MainDispatcherRule — viewModelScope uses Dispatchers.Main",code:`class MainDispatcherRule(
    private val dispatcher: TestDispatcher = UnconfinedTestDispatcher(),
) : TestWatcher() {
    override fun starting(description: Description) = Dispatchers.setMain(dispatcher)
    override fun finished(description: Description) = Dispatchers.resetMain()
}`},{t:"code",title:"Testing state emissions with Turbine",code:`@get:Rule val mainDispatcherRule = MainDispatcherRule()

@Test
fun refresh_emitsLoadingThenContent() = runTest {
    val repository = FakeUserRepository(user = testUser)
    val viewModel = UserViewModel(repository, SavedStateHandle(mapOf("userId" to "42")))

    viewModel.uiState.test {
        assertEquals(true, awaitItem().isLoading)
        val loaded = awaitItem()
        assertEquals(testUser, loaded.user)
        assertEquals(false, loaded.isLoading)
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**`StandardTestDispatcher` vs `UnconfinedTestDispatcher`**: Standard queues coroutines until you `advanceUntilIdle()`/`runCurrent()` — precise control, good for testing intermediate states. Unconfined runs them eagerly — convenient, but you can miss transient states like `isLoading`.","**`stateIn(WhileSubscribed)` gotcha**: the flow does nothing until collected — a test asserting `viewModel.uiState.value` right after construction sees only `initialValue`. Collect it (Turbine, or `backgroundScope.launch { uiState.collect() }`) to start the upstream.","**Inject dispatchers** (`ioDispatcher: CoroutineDispatcher` constructor param) instead of hardcoding `Dispatchers.IO`, so tests can substitute the TestDispatcher and control virtual time (`advanceTimeBy` for `debounce`).","Prefer **fakes** (in-memory repository implementations) over mocks for the data layer — tests read better and don't break on refactors."]}]},{heading:"Anti-patterns & common mistakes (rapid-fire checklist)",blocks:[{t:"list",items:["**Holding a `Context`, View, Activity or Fragment reference in a ViewModel** — leaks the destroyed instance across rotation. If you truly need app context, use `AndroidViewModel`'s application (or better, inject what you need — a resource provider / use case).","**Exposing `MutableStateFlow`/`MutableLiveData` publicly** — breaks single-writer UDF.","**God ViewModel** — thousands of lines handling half the app. Fix: one ViewModel per screen, delegate logic to use cases, split independent widgets into their own state holders.","**Business logic in the View** — `if (user.age > 18)` in a composable/fragment means untestable logic. Views map state to pixels, nothing else.","**Doing data-layer work in the ViewModel** — Retrofit/Room calls directly in the VM couples presentation to data details; go through a repository.","**Observing with the wrong lifecycle owner** in fragments (`this` instead of `viewLifecycleOwner`).","**`GlobalScope` / hardcoded dispatchers** — untestable, leak-prone; use `viewModelScope` + injected dispatchers.","**Sticky-state events** — `showToast = true` in state without a consumed callback replays the toast on every rotation.","**Kicking off loads in `init` without thought** — it makes the VM eager and harder to test/paginate; `stateIn`-driven lazy starts or explicit triggers are often cleaner (know the trade-off; `init` is not *wrong*).","**Passing ViewModels down the composable tree** — pass state and lambdas instead; deep VM references kill previews and reusability.","**Storing big data in SavedStateHandle** — `TransactionTooLargeException`; save ids, reload data.","**Mutable collections in UiState** — breaks Compose stability/skipping and equality-based conflation."]}]},{heading:"MVVM vs MVC vs MVP vs MVI — the comparison question",blocks:[{t:"table",headers:["","MVC (classic Android)","MVP","MVVM","MVI"],rows:[["Who updates the UI","Activity does everything","Presenter calls View interface methods","View observes ViewModel state","View renders a single immutable state from a reducer loop"],["Middle layer → View reference","n/a (they're merged)","**yes** (interface)","**no** (observation)","no (observation)"],["Survives config change","no","no (needs retain hacks)","**yes** (Jetpack ViewModel)","yes (built on VM usually)"],["State shape","scattered","scattered across presenter calls","one or few observable streams","**single state object + explicit intents**"],["Testability","poor","good (mock the View interface)","good (assert emitted state)","excellent (pure reducer)"],["Boilerplate","low","high (View interfaces)","medium","medium-high"]]},{t:"list",items:["MVP died on Android largely because the Presenter↔View interface pair was boilerplate-heavy and lifecycle-blind; MVVM's observation model plus the Jetpack ViewModel's config-change survival won.",'MVI is best understood as **MVVM with stricter rules**: exactly one state object, events as explicit Intent objects, state changes only through a reducer. Modern "MVVM with UDF + single UiState" is already 80% of MVI.',`Good senior close: "I don't pick dogmatically — Compose + StateFlow + UDF naturally lands between MVVM and MVI, and that's what Google's architecture guidance recommends."`]}]}],mx=[{level:"junior",q:"What is MVVM? Explain each component.",a:[{t:"p",text:"MVVM is a UI architecture pattern with three roles. The **Model** is the data layer — repositories and data sources, plus business logic; it knows nothing about UI. The **View** (Activity, Fragment, or Composable) renders state and forwards user input. The **ViewModel** sits between them: it holds UI state, exposes it as an observable stream (`StateFlow`/`LiveData`), and contains presentation logic."},{t:"p",text:"The key rule is that references go one way: View → ViewModel → Model. The ViewModel **never references the View** — the View subscribes to the ViewModel. That makes the ViewModel testable on the JVM and safe across configuration changes."}]},{level:"junior",q:"How is MVVM different from MVP and MVC?",a:[{t:"p",text:"In classic Android **MVC**, the Activity ends up being controller *and* view — everything in one class, untestable. In **MVP**, a Presenter holds a reference to a View *interface* and calls methods on it (`view.showLoading()`); logic becomes testable by mocking that interface, but you write a lot of interface boilerplate and the Presenter must be manually attached/detached to avoid leaks."},{t:"p",text:"**MVVM inverts that last relationship**: the ViewModel exposes observable state and doesn't know who's watching. No View interface, no attach/detach, and with Jetpack's `ViewModel` you also get configuration-change survival for free — which Presenters never had. That's essentially why the Android community moved from MVP to MVVM after Architecture Components shipped."}]},{level:"junior",q:"Is the Jetpack ViewModel class the same thing as the ViewModel in MVVM?",a:[{t:"p",text:"Related but not identical. The MVVM ViewModel is a *pattern role* — any class holding UI state and presentation logic qualifies. `androidx.lifecycle.ViewModel` is Google's concrete implementation of that role with two extras: it **survives configuration changes** (stored in a `ViewModelStore` that's retained across recreation) and provides **`onCleared()`** for cleanup plus `viewModelScope` for coroutines. You could do MVVM without the Jetpack class, but you'd re-implement those features yourself."}]},{level:"junior",q:"How does a ViewModel survive screen rotation?",a:[{t:"p",text:"ViewModels live in a `ViewModelStore`, owned by a `ViewModelStoreOwner` (Activity, Fragment, or nav back-stack entry). On a configuration change the Activity instance is destroyed, but the system retains its non-configuration state — and the `ViewModelStore` is carried inside it. The recreated Activity receives the same store, so when it asks `ViewModelProvider` for the ViewModel, it gets the **same instance** back instead of a new one."},{t:"p",text:"`onCleared()` only fires when the owner goes away permanently — the Activity is finishing, the Fragment is removed for good, or the back-stack entry is popped. Important corollary: the ViewModel outlives the Activity instance, so it must never hold a reference to it."}]},{level:"junior",q:"What is viewModelScope and what happens to it in onCleared()?",a:[{t:"p",text:"`viewModelScope` is a `CoroutineScope` attached to the ViewModel, built from `SupervisorJob() + Dispatchers.Main.immediate`. Anything launched in it is **automatically cancelled when `onCleared()` runs**, so you never leak coroutines when the screen is gone. `SupervisorJob` means one failing child doesn't cancel its siblings, and `Main.immediate` avoids an extra dispatch when you're already on the main thread."},{t:"p",text:"Edge case worth adding: work that must **outlive the screen** (say, finishing an upload) doesn't belong in `viewModelScope` — use an application-scoped `CoroutineScope` injected into the repository, or `WorkManager` if it must survive process death."}]},{level:"junior",q:"Why must a ViewModel never hold a reference to an Activity, Fragment, View, or their Context?",a:[{t:"p",text:"Because the ViewModel outlives them. On rotation the Activity is destroyed and recreated, but the ViewModel instance stays — if it held the old Activity (or anything holding one, like a View or an adapter), the garbage collector can't reclaim it: a **memory leak** of the entire view hierarchy, until the ViewModel itself dies."},{t:"p",text:"If application-level context is genuinely needed (rare — usually a sign work belongs in the data layer), `AndroidViewModel` exposes the `Application`, which is a singleton and safe. The cleaner answer is to inject an abstraction — a resource provider or a use case — so the ViewModel stays free of Android classes and unit-testable."}]},{level:"junior",q:"LiveData vs StateFlow — what are the differences and which should you use?",a:[{t:"list",items:["`StateFlow` **requires an initial value**; LiveData can be empty.","LiveData is **lifecycle-aware by itself** (observers auto-pause/auto-remove); StateFlow needs `repeatOnLifecycle` or `collectAsStateWithLifecycle` to collect safely.","`StateFlow` **conflates equal values** — setting the same value twice emits once; LiveData re-notifies.","StateFlow has the **full Flow operator set** and clean coroutine interop; LiveData has only a handful of transformations.","LiveData is **Android-only**; StateFlow is pure Kotlin — the deciding factor for **KMP**."]},{t:"p",text:"Default today: **StateFlow**. LiveData is in maintenance mode and fine to keep in legacy code, but new code — especially anything that might share logic with KMP — should use StateFlow."}]},{level:"junior",q:"Why do we expose StateFlow instead of MutableStateFlow from the ViewModel?",a:[{t:"p",text:"Encapsulation and single-writer discipline. If the View got the mutable type, any code could set state, and you lose the unidirectional flow that makes the app predictable. The convention is a private `_uiState: MutableStateFlow` plus a public `uiState: StateFlow` via `asStateFlow()`, so the **ViewModel is the only writer** and the View can only read and observe."},{t:"p",text:"Detail: `asStateFlow()` is preferred over just upcasting, because upcast state can be cast back to mutable, while `asStateFlow()` returns a read-only wrapper."}]},{level:"junior",q:"How does the View observe the ViewModel in XML-based UIs vs Compose?",a:[{t:"code",title:"XML / Fragment",code:`viewLifecycleOwner.lifecycleScope.launch {
    viewLifecycleOwner.repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.uiState.collect { state -> render(state) }
    }
}`},{t:"code",title:"Compose",code:"val state by viewModel.uiState.collectAsStateWithLifecycle()"},{t:"p",text:"In both cases the point is **lifecycle-safe collection**: stop collecting when the UI isn't visible, resume when it is. In fragments, always use `viewLifecycleOwner` — the fragment instance can outlive its view on the back stack, and observing with `this` leaks or touches dead views."}]},{level:"junior",q:"How do you pass arguments (like an id) into a ViewModel?",a:[{t:"p",text:'Constructor injection through a **factory** — you never construct a ViewModel directly, so extra parameters must go through `ViewModelProvider.Factory`. With Hilt, `@HiltViewModel` with an `@Inject constructor` generates the factory, and runtime values like a user id are best passed via **`SavedStateHandle`**: the Navigation component automatically places destination arguments into it, so the ViewModel reads `savedStateHandle["userId"]` — and that value also survives process death.'},{t:"p",text:"For assisted injection of values not in navigation args, Hilt supports `@AssistedInject` ViewModel factories (or you write a small custom factory)."}]},{level:"junior",q:"Where should business logic live in MVVM? What belongs in the View?",a:[{t:"p",text:"**Business/domain logic** (rules, validation, calculations) belongs in the domain or data layer — use cases and repositories. **Presentation logic** (combining streams, mapping domain models to UI state, deciding what's visible) belongs in the ViewModel. The **View** only maps state to widgets and forwards events — if you see `if (user.balance < 0)` deciding colors in a Fragment, that condition should have produced a semantic field in UiState instead."},{t:"p",text:"Litmus test: could you unit-test the decision without instrumented tests? If not, it's in the wrong layer."}]},{level:"junior",q:"What is a Repository and why doesn't the ViewModel call Retrofit/Room directly?",a:[{t:"p",text:"A repository is the data layer's public API for one type of data — the **single source of truth**. It decides between cache, database, and network; exposes `Flow`s and suspend functions; and hides the data-source libraries entirely. The ViewModel calling Retrofit directly couples presentation to transport details, duplicates caching/merging logic across screens, and makes the ViewModel hard to test. With a repository interface, tests swap in an in-memory fake."},{t:"p",text:"Bonus rule: repositories should be **main-safe** — the ViewModel can call them from the main thread and they handle `withContext(ioDispatcher)` internally."}]},{level:"junior",q:"What is Unidirectional Data Flow (UDF)?",a:[{t:"p",text:"A cycle with one direction: **state flows down, events flow up**. The View sends user events to the ViewModel (`onQueryChange(...)`), the ViewModel updates state, and the new state flows back down through the observable stream to be rendered. The View never mutates state itself. Benefits: a single place where state changes (easy to debug), state that's a pure function of data (easy to reason about), and natural fit with Compose's declarative model. Modern MVVM on Android is always practiced together with UDF."}]},{level:"junior",q:"How do two Fragments share data using a ViewModel?",a:[{t:"p",text:"Scope one ViewModel to a **shared owner**. Sibling fragments in the same Activity use `by activityViewModels()` — both get the same instance because the Activity's `ViewModelStore` is shared. With the Navigation component, scoping to a **navigation graph** (`hiltViewModel(parentEntry)` / `navGraphViewModels(R.id.checkout_graph)`) is usually better: the ViewModel lives exactly as long as the flow (e.g. a checkout wizard) and is cleared when the user leaves it, instead of lingering for the whole Activity."}]},{level:"junior",q:"What happens to the ViewModel when the user presses back vs when they rotate the screen?",a:[{t:"p",text:"**Rotation**: configuration change — the Activity is recreated but `isFinishing` is false, the `ViewModelStore` is retained, the same ViewModel instance is reused, `onCleared()` is *not* called. **Back press**: the Activity is finishing — the store is cleared, **`onCleared()` runs**, `viewModelScope` is cancelled, and the ViewModel is gone. Same logic for fragments (permanent removal vs recreation) and nav back-stack entries (popped vs kept)."}]},{level:"junior",q:"How do you show a one-time message (Snackbar/Toast) from a ViewModel?",a:[{t:"p",text:"The ViewModel can't touch UI, so it signals the View. The naive way — a `showMessage: Boolean` in sticky state — replays after rotation. Standard options: a **`Channel` collected as a Flow** in the View (buffered, delivered once), or Google's recommended **state-based event**: put `message: String?` in UiState, the View shows it and calls `viewModel.onMessageShown()` which nulls it out. State-based is the most robust across config change and process death; Channel is pragmatic and common. What matters in the interview is naming the rotation-replay problem and one correct solution."}]},{level:"junior",q:"What is SavedStateHandle and when do you need it?",a:[{t:"p",text:"A key-value map handed to the ViewModel that plugs into the saved-instance-state system. Two jobs: it carries **navigation arguments**, and anything you write into it **survives system-initiated process death** (which the ViewModel itself does not). It exposes values as `getStateFlow(key, default)` / `getLiveData`. Constraints: values must be Bundle-compatible (primitives/Parcelable) and small — store *ids and user input*, then reload real data from the repository."}]},{level:"junior",q:"Is data binding required for MVVM on Android?",a:[{t:"p",text:'No. MVVM only requires that the View *observes* the ViewModel — the mechanism is an implementation detail. XML **DataBinding** (binding expressions in layouts) was one popular mechanism and is what people historically associated with MVVM, but it\'s effectively legacy now; most XML projects use ViewBinding plus manual `collect`, and Compose observes state natively. Saying "MVVM = data binding" is a red flag answer.'}]},{level:"junior",q:"How do you represent loading, success and error in the UI state?",a:[{t:"p",text:'Two idioms. A **single data class** with fields (`isLoading`, `data`, `error`) — flexible, allows overlapping states like "showing cached data while refreshing with an error banner". Or a **sealed interface** (`Loading / Success / Error`) — states are mutually exclusive and the compiler forces exhaustive handling. Choose sealed when states truly exclude each other; choose the data class when they can overlap. Either way the state object stays **immutable** and is replaced wholesale via `copy()`.'}]},{level:"junior",q:"Can you create a ViewModel with 'new'/direct constructor call? Why or why not?",a:[{t:"p",text:"You *can* (it's just a class — unit tests do exactly this), but in production you must go through `ViewModelProvider` / `by viewModels()` / `hiltViewModel()`. The provider first checks the owner's `ViewModelStore` and returns the existing instance if present — that lookup is the entire survival mechanism. A directly-constructed ViewModel is never stored, so you'd get a fresh object on every rotation and `onCleared` would never be managed for it."}]},{level:"senior",q:"Walk me through exactly how a ViewModel survives configuration change under the hood.",a:[{t:"p",text:"`ComponentActivity` implements `ViewModelStoreOwner` and owns a `ViewModelStore` — a map of key → ViewModel. When a config change begins, the framework calls `onRetainNonConfigurationInstance()`; ComponentActivity packs the `ViewModelStore` into its `NonConfigurationInstances` object, which the ActivityThread keeps in memory across the destroy/recreate (the process never dies). The new Activity instance retrieves it via `getLastNonConfigurationInstance()` and adopts the store. So `ViewModelProvider.get()` computes the same default key (`androidx.lifecycle.ViewModelProvider.DefaultKey:` + class name), finds the entry, and returns the same object."},{t:"p",text:"The cleanup path: ComponentActivity observes its own lifecycle; on `ON_DESTROY` it checks `isChangingConfigurations` — only when that's **false** does it call `viewModelStore.clear()`, which iterates ViewModels and invokes `onCleared()` (also closing `viewModelScope`). Fragments do the analogous dance through `FragmentManagerViewModel`, and Navigation scopes stores to `NavBackStackEntry`s cleared on pop."}]},{level:"senior",q:"Configuration change vs process death — what exactly survives each, and how do you build for both?",a:[{t:"list",items:["**Config change**: process lives. ViewModel instance and all in-memory state survive; the Activity/views are recreated. `SavedStateHandle` isn't needed but keeps working.","**Process death**: the OS kills the backgrounded process. ViewModel is gone; only the saved-instance-state Bundle survives (the system holds it) — restored into `SavedStateHandle` on relaunch. In-memory caches, singletons, StateFlows — all gone."]},{t:"p",text:"Strategy: treat `SavedStateHandle` as the place for **identity and transient input** (selected id, query text, scroll anchor), and the **persistent layer as the source of truth for data**. On recreation the ViewModel reads the id from the handle and re-subscribes to the repository — the same code path as first launch, so process-death recovery isn't a special case. Watch the Binder limit (~1 MB per transaction, shared): dumping lists into the handle causes `TransactionTooLargeException`. Test it honestly: background the app, kill via Android Studio's Terminate (or `adb shell am kill`), relaunch from Recents — rotation tests prove nothing about process death."}]},{level:"senior",q:"StateFlow conflation — when is it a problem and what do you do about it?",a:[{t:"p",text:"Two distinct behaviors. First, **equality skipping**: `StateFlow` won't emit a value `equals()` to the current one. That bites with mutable objects (mutate-and-reset looks unchanged) or when you *want* re-emission (retry the same 'scroll to top' command). Second, **conflation proper**: a slow collector only ever sees the latest value — intermediate states can be skipped entirely. For *state* this is exactly right (the screen only cares about now); for anything where **every emission matters** — analytics events, a queue of animations, commands — it's wrong."},{t:"p",text:"Fixes: keep state immutable data classes so equality is meaningful; for must-not-miss streams use `SharedFlow` with an explicit `replay`/`extraBufferCapacity`/`onBufferOverflow` policy or a `Channel`; and don't model events as state values (a `counter` field to force emission is a design smell — that's an event stream in disguise)."}]},{level:"senior",q:"Why exactly 5000 ms in SharingStarted.WhileSubscribed(5000)? Compare with Eagerly and Lazily.",a:[{t:"p",text:"`WhileSubscribed(5_000)` stops the upstream flow 5 s after the last collector unsubscribes and restarts it when someone re-subscribes. The 5 s exists for **configuration changes**: during rotation, collectors vanish for a few hundred milliseconds; without the timeout the upstream (a Room query, a socket subscription) would be torn down and cold-restarted — flicker, wasted IO, re-shown loading states. 5 s comfortably covers recreation while still stopping work shortly after a genuine backgrounding (ON_STOP with `repeatOnLifecycle(STARTED)`). It also pairs with `replay`/initial value so the returning collector gets the last state instantly."},{t:"list",items:["`Eagerly` — starts immediately with the scope, never stops until scope cancellation. Fine for cheap, always-relevant state; wasteful for expensive upstreams.","`Lazily` — starts on the first collector, then never stops. Rarely what you want for UI.","`WhileSubscribed(0)` — stops instantly on unsubscribe; restarts upstream on every rotation (the exact problem 5000 solves)."]}]},{level:"senior",q:"Why was launchWhenStarted deprecated in favor of repeatOnLifecycle?",a:[{t:"p",text:"`launchWhenStarted` only **suspends** the coroutine below STARTED — the upstream flow stays **active**. A location provider, a Room observer, a socket keeps producing while the app is backgrounded; emissions pile up (or hot hardware stays on), and work resumes with a burst on return. It also silently never cancels — the producer runs until the scope dies. `repeatOnLifecycle(STARTED)` instead **cancels the whole collection** at ON_STOP and **restarts the block** at ON_START — the upstream is genuinely stopped. That cancel/restart semantic is honest about lifecycle and is why the `launchWhenX` family was deprecated."},{t:"p",text:"Compose equivalent: `collectAsStateWithLifecycle()` (uses `repeatOnLifecycle` internally) vs plain `collectAsState()` which keeps collecting in background. Pairing detail: `repeatOnLifecycle` + `stateIn(WhileSubscribed(5000))` compose perfectly — background for >5 s and the upstream stops too."}]},{level:"senior",q:"Deep-dive the one-shot event problem: compare SingleLiveEvent, SharedFlow, Channel, and state-based events, with their failure modes.",a:[{t:"list",items:["**SingleLiveEvent** — LiveData hack with an 'handled' flag. Only one observer gets the event, racy with multiple, relies on LiveData; legacy — name it only to dismiss it.","**`MutableSharedFlow(replay = 0)`** — hot with no buffer for absent collectors: an event emitted while the View is between `repeatOnLifecycle` windows (rotation! backgrounded!) is **silently dropped**. `tryEmit` with no buffer simply fails. Setting `replay = 1` fixes dropping but re-introduces stickiness — replayed after rotation. Fundamentally awkward for events.","**`Channel(BUFFERED).receiveAsFlow()`** — `send` suspends/buffers until a collector receives, so events **wait through collection gaps** — the usual pragmatic winner. Caveats: fan-out (each event goes to exactly *one* collector — a bug if two collectors are accidentally active); buffered events are lost on process death; and an event can be received *just* before the UI stops, then the Snackbar never shows.","**State-based (Google's guidance)** — the 'event' becomes a state field; the View executes it and calls a `consumed()` callback to clear it. Survives config change, and process death too if mirrored in `SavedStateHandle`; no delivery ambiguity because state is always there to re-render. Cost: consumption ceremony and modeling multiple queued events takes a list."]},{t:"p",text:"My default: state-based for anything that changes what the user sees or where they are (navigation, dialogs); Channel for genuine fire-and-forget side effects. The senior signal is explaining the **dropped-event window** — most candidates don't know SharedFlow silently drops."}]},{level:"senior",q:"MVVM vs MVI — real differences, and when would you pick MVI?",a:[{t:"p",text:"MVI is MVVM with three extra constraints: **a single immutable state object** per screen (MVVM allows several streams), **reified user intents** (sealed `Intent`/`Action` objects through one entry point, vs plain method calls), and **state mutation only through a reducer** — a pure `(state, action) -> state` function. That gives you an auditable event log, trivially testable reducers, and time-travel debugging; it costs boilerplate, indirection for trivial screens, and a learning curve."},{t:"p",text:"When I'd pick MVI: screens with genuinely complex state machines (editors, players, multi-step flows), or a team that wants the discipline enforced rather than by convention. Otherwise, modern MVVM — single `UiState` + `update {}` + UDF — already captures most of the benefit; frameworks like Orbit or MVIKotlin (popular in KMP with Decompose) formalize it when needed. Honest close: the industry converged on a middle ground, and Google's guidance is exactly that hybrid."}]},{level:"senior",q:"Your ViewModel has grown to 1500 lines. How do you fix a God ViewModel?",a:[{t:"list",items:["**Push logic down**: business rules move into use cases; data orchestration into repositories. The VM should mostly wire streams and map models — extracting logic usually halves it.","**Split by feature, not screen**: independent widgets on one screen (search bar, feed, stories row) can have their own state holders or even ViewModels; a screen-level VM composes them.","**Extract plain state-holder classes** for reusable UI logic that doesn't need VM lifetime, hoisted in Compose (`remember { ... }`).","**Extract mapping** into dedicated mapper/formatter classes with their own tests.","**Split the UiState** if it has become a dumping ground — several cohesive streams beat one 40-field object recomposing the world."]},{t:"p",text:"Guardrail I apply in review: if a ViewModel imports a data-source library (Retrofit/Room types) or exceeds a few hundred lines, the layering has broken down somewhere below it."}]},{level:"senior",q:"How do you test a ViewModel that uses viewModelScope, debounce, and stateIn(WhileSubscribed)? Include the gotchas.",a:[{t:"p",text:"Infrastructure first: `viewModelScope` runs on `Dispatchers.Main`, which doesn't exist on the JVM — a `MainDispatcherRule` calls `Dispatchers.setMain(TestDispatcher)`/`resetMain()`. Tests run in `runTest`, which gives **virtual time**."},{t:"list",items:["**Dispatcher choice**: `StandardTestDispatcher` queues coroutines until `advanceUntilIdle()`/`runCurrent()` — lets you assert intermediate states (`isLoading`). `UnconfinedTestDispatcher` runs eagerly — simpler, but transient states may be skipped before you can observe them.","**`stateIn(WhileSubscribed)` gotcha**: cold until collected. Asserting `uiState.value` right after constructing the VM only ever shows `initialValue` — you must collect (Turbine's `uiState.test { }`, or `backgroundScope.launch { uiState.collect() }`) to trigger the upstream.","**`debounce` / delays**: virtual time — `advanceTimeBy(300)` then assert; no real sleeping. Requires the debounce to run on the test scheduler, which leads to:","**Inject dispatchers** — a hardcoded `Dispatchers.IO` inside the VM or repository escapes virtual time and makes tests flaky/slow. Constructor-inject `CoroutineDispatcher` (or a dispatcher provider) and pass the TestDispatcher in tests.","Use **fakes over mocks** for repositories — behavioral fidelity, refactor-friendly; and **Turbine** for asserting emission sequences (`awaitItem()`, `expectNoEvents()`)."]}]},{level:"senior",q:"How do you share a ViewModel (or its equivalent) between Android and iOS in KMP, and what are the pain points?",a:[{t:"p",text:"Since androidx lifecycle 2.8, `ViewModel` + `viewModelScope` are multiplatform — the VM moves to `commonMain` exposing `StateFlow<UiState>`. Android and Compose Multiplatform consume it natively (`collectAsStateWithLifecycle`, `koinViewModel`). The friction is **native iOS**: Objective-C interop erases Flow's generics and has no suspend-stream concept, so SwiftUI can't just subscribe."},{t:"list",items:["**Bridging**: SKIE (converts `Flow` to Swift `AsyncSequence`, sealed classes to Swift enums — nicest DX), KMP-NativeCoroutines, or a hand-written closure-based `watch { }` wrapper. On the Swift side, an `ObservableObject`/`@Observable` adapter subscribes and republishes into SwiftUI.","**Lifecycle**: iOS has no config changes, so 'VM survives rotation' is moot — but ownership is manual: the Swift adapter must call `clear()`/cancel on deinit or the VM's coroutines leak. moko-mvvm and Decompose formalize this; Decompose in particular replaces ViewModel with lifecycle-aware 'components' shared across platforms.","**Main-thread delivery**: state driving SwiftUI must land on the main thread — keep VM emissions on `Dispatchers.Main` in shared code or hop in the adapter.","**Errors**: Kotlin exceptions crossing to Swift need `@Throws` or Result-typed state — uncaught Kotlin exceptions crash the app on the Swift side."]}]},{level:"senior",q:"How does SavedStateHandle.getStateFlow work with the rest of your state, and what are its limits?",a:[{t:"p",text:"`getStateFlow(key, initial)` returns a `StateFlow` backed by the handle: writing `savedStateHandle[key] = value` emits to the flow *and* marks the value for saved-state persistence — so a field both drives UI reactively and survives process death with one line. Typical pattern: persist raw inputs (query, selected filters) via handle-backed flows, `combine` them with repository flows, and `stateIn` the result; the heavy derived state is rebuilt on restore instead of persisted."},{t:"list",items:["Values must be Bundle-able (primitives, Parcelable, Serializable) — arbitrary domain objects need mapping or `SavedStateHandle.setSavedStateProvider` for custom serialization.","Size: part of the Binder-limited saved state (~1 MB total) — ids and inputs only, `TransactionTooLargeException` otherwise.","Saving happens at `onSaveInstanceState` time — mutations after the Activity is stopped may not be captured if the process dies immediately.","It's per-owner: a nav-graph-scoped VM's handle restores with the graph entry; also note `SavedStateHandle` in Compose Multiplatform arrived later than the VM itself — check versions in KMP."]}]},{level:"senior",q:"Should the ViewModel know about navigation? Discuss the approaches.",a:[{t:"list",items:["**VM emits navigation events** (Channel/state), View executes with NavController — most common; keeps NavController (a UI-layer object) out of the VM; testable by asserting emitted events. Downside: event-delivery caveats.","**Navigation as state** — a `destination` field in UiState (or Compose Navigation 3-style back stack derived from state); most robust across process death and deep links, aligns with state-driven navigation direction the ecosystem is moving in.","**Injecting a Navigator abstraction** into the VM — an interface the VM calls (`navigator.goToDetails(id)`), implemented over NavController. Reads nicely and mocks easily, but the VM now triggers UI actions imperatively and the navigator implementation must handle lifecycle (not firing while stopped).","**NavController directly in the VM — never**: it's tied to views/lifecycle; holding it in a VM is the leak/lifecycle bug the pattern forbids."]},{t:"p",text:"My answer: decisions about *whether/where* to navigate are presentation logic and belong in the VM; the *mechanics* belong in the View. Events or navigation-state both satisfy that — I lean state-based for anything that must survive process death."}]},{level:"senior",q:"How do you implement debounced search properly in a ViewModel?",a:[{t:"code",title:"The canonical reactive pipeline",code:`private val query = savedStateHandle.getStateFlow("query", "")

@OptIn(ExperimentalCoroutinesApi::class, FlowPreview::class)
val results: StateFlow<SearchUiState> = query
    .debounce(300)
    .distinctUntilChanged()
    .flatMapLatest { q ->
        if (q.isBlank()) flowOf(SearchUiState.Idle)
        else repository.search(q)
            .map<List<Item>, SearchUiState> { SearchUiState.Results(it) }
            .onStart { emit(SearchUiState.Searching) }
            .catch { emit(SearchUiState.Error(it.message)) }
    }
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), SearchUiState.Idle)

fun onQueryChange(q: String) { savedStateHandle["query"] = q }`},{t:"list",items:["`debounce(300)` — wait for typing to pause; `distinctUntilChanged` — skip no-op changes (StateFlow gives this for the query itself, but it matters post-debounce).","**`flatMapLatest`** is the load-bearing operator: a new query **cancels** the in-flight search, killing the classic race where a slow old response overwrites fresh results. (`flatMapConcat` would queue, `flatMapMerge` would race.)","`catch` *inside* the inner flow: an error kills only that search, not the whole pipeline — a `catch` on the outer flow would end results forever.","SavedStateHandle-backed query → search state survives process death; test with virtual time (`advanceTimeBy(300)`)."]}]},{level:"senior",q:"Error handling strategy in a ViewModel: exceptions, CoroutineExceptionHandler, Result types?",a:[{t:"list",items:["**Prefer errors as data**: repositories return sealed results (`Result`/`Either`, or emit error states in flows) so the VM handles failure as an expected branch and maps it into UiState. Exceptions escaping into `viewModelScope` mean a crash (in a `launch`, an uncaught exception hits the thread's handler → app crash; `SupervisorJob` only protects *siblings*).","**`runCatching`/try-catch at the call site** for suspend calls; but never blanket-catch `CancellationException` — swallowing it breaks structured cancellation (rethrow it, or catch specific exceptions).","**Flows**: `catch` operator placed to scope the failure correctly (inner flow for per-operation errors); `retry`/`retryWhen` for transient network errors with backoff.","**CoroutineExceptionHandler** installed into the scope is a *last-resort* logger/crash-prevention net, not a control-flow mechanism — by the time it fires the coroutine is dead.","Map technical errors to **semantic UI errors** in the VM (offline vs server vs validation) — the View shouldn't parse exception types."]}]},{level:"senior",q:"What's wrong with starting data loads in a ViewModel's init block? Is there a better pattern?",a:[{t:"p",text:"It's common and works, but know the trade-offs. Problems: work starts at *construction* — in tests the coroutine races your setup (especially with `UnconfinedTestDispatcher`) before you can install fakes' behavior or attach Turbine; with lazy `by viewModels()` the start time is 'whenever first accessed', which is implicit; retry-on-failure needs a separate public function anyway; and `init` work runs even if the UI never actually collects."},{t:"p",text:"The alternative: build state as a **cold chain started by collection** — repository flow + `onStart`/`flatMapLatest` + `stateIn(WhileSubscribed(5000))`. Loading begins when the UI subscribes, stops when it leaves, restarts on return, and tests control it by choosing when to collect. Google's samples largely moved to this 'lazy observable state' shape. `init` remains fine for cheap wiring or fire-once triggers — the senior answer is knowing *why* you'd choose each."}]},{level:"senior",q:"Compose specifics: collectAsState vs collectAsStateWithLifecycle, and should you pass the ViewModel down the tree?",a:[{t:"p",text:"`collectAsState()` collects in the composition's scope — it keeps collecting while the app is backgrounded (composition isn't disposed on ON_STOP). `collectAsStateWithLifecycle()` wraps collection in `repeatOnLifecycle(STARTED)` — collection stops in background and, combined with `WhileSubscribed(5000)`, the upstream stops too. Default to the lifecycle variant on Android; plain `collectAsState` is acceptable in previews/desktop CMP where the lifecycle concept differs."},{t:"p",text:"ViewModel placement: inject it at the **screen-level composable only**; children receive **state and lambdas** (`state: UiState, onRetry: () -> Unit`). Passing VMs deep kills `@Preview`s (they can't build VMs), couples reusable components to one screen's VM, and makes recomposition reasoning harder. Also know the scoping subtlety: `viewModel()`/`hiltViewModel()` scope to the nearest `ViewModelStoreOwner` — the nav back-stack entry — so the same call in two destinations returns different instances."}]},{level:"senior",q:"Two screens need to observe and mutate the same data. Shared ViewModel or something else?",a:[{t:"p",text:"Reach for the **data layer first**: make the repository the single source of truth exposing a hot/observed `Flow` (Room queries, an in-memory `MutableStateFlow` cache); each screen's ViewModel observes it independently and mutations go through repository functions. This survives any navigation topology, process recreation per-screen, and keeps ViewModels screen-scoped. A **shared ViewModel** (activity- or nav-graph-scoped) is the right tool for *transient flow state* that has no business in the data layer — wizard steps, in-progress form input across a checkout graph — where the nav-graph scope also gives automatic cleanup on flow exit."},{t:"p",text:"Anti-pattern to call out: an activity-scoped God ViewModel used as a message bus between unrelated screens — outlives its usefulness, accumulates state, and hides data-flow direction."}]},{level:"senior",q:"Where do memory leaks still happen in MVVM apps, and how do you find them?",a:[{t:"list",items:["**VM → UI references**: holding Context/View/Fragment/listener in the VM (including via lambda captures — an `onClick` stored in the VM capturing the Fragment).","**Wrong lifecycle owner in fragments**: observing/collecting with the Fragment's lifecycle instead of `viewLifecycleOwner` — view recreated on back-stack return, old bindings/adapters leak; same for keeping a `_binding` not nulled in `onDestroyView`.","**Un-cancelled collections**: collecting in `GlobalScope` or an application scope from UI code; callbacks registered on singletons and never unregistered (VM registering a listener on a singleton repository without removing it in `onCleared`).","**Anonymous inner classes / handlers** capturing the Activity outliving it.","**Static/companion caches** of UI objects."]},{t:"p",text:"Detection: **LeakCanary** in debug builds (watches destroyed activities/fragments/view models automatically), Android Studio Memory Profiler heap dumps (search retained Activity instances, follow the shortest GC-root path), and StrictMode for some cases. Process discipline: `onCleared` must undo everything `init` registered."}]},{level:"senior",q:"Critique MVVM: what are its genuine weaknesses, and when would you not use it?",a:[{t:"list",items:["**Under-specified**: MVVM says nothing about state shape, events, or the data layer — two 'MVVM apps' can look wildly different; teams need added conventions (UDF, single UiState) to stay consistent — which is effectively drifting toward MVI.","**The event problem is unsolved by the pattern itself** — one-shot delivery needs bolt-on idioms, each with failure modes.","**ViewModel as god-object gravity**: the VM is the easiest place to dump code, and the pattern has no built-in pressure against it.","**Overkill for trivial UI**: a static screen or a tiny utility app doesn't need the ceremony; and heavily server-driven UIs may fit a different shape entirely.","**Debugging indirection**: with many combined streams, answering 'why did this render?' means tracing reactive graphs — worse without discipline around naming and derived state."]},{t:"p",text:"When not: throwaway prototypes, screens with no state (pure display), or when the team is committed to a stricter framework (MVI/Redux-style, or Decompose components in KMP) where adding classic MVVM alongside would fragment the codebase. The mature position: MVVM-with-UDF is the pragmatic default on Android/KMP, applied with judgment rather than dogma."}]},{level:"senior",q:"What is AndroidViewModel and why is it discouraged?",a:[{t:"p",text:"`AndroidViewModel` is a ViewModel subclass that receives the `Application` in its constructor — the sanctioned way to have a context in a VM, safe from leaks because Application is a process-lifetime singleton. It's discouraged because needing a context in the VM usually signals **misplaced responsibility**: resource strings belong in the View or come via a resource-provider abstraction; file/database/preferences access belongs in the data layer. It also drags an Android dependency into the class — killing plain-JVM unit tests (Robolectric or instrumentation needed) and making the VM unshareable in KMP `commonMain`. With DI you can inject `@ApplicationContext` anyway, so the base class adds little; prefer injecting narrow abstractions instead."}]},{level:"senior",q:"Explain cold vs hot flows in the context of a ViewModel, and where each appears.",a:[{t:"p",text:"**Cold** flows (`flow { }`, Room/Retrofit-adapter flows, repository chains) run their producer *per collector*, starting on collection — each collector gets its own execution. **Hot** flows (`StateFlow`, `SharedFlow`, channels) exist and emit independently of any particular collector, multicasting to all of them."},{t:"list",items:["The typical VM is a **cold-to-hot converter**: cold repository chain → operators → `stateIn`/`shareIn` → hot state the UI observes. Without `stateIn`, two collectors of the same chain (or recollection after rotation) would re-execute the query — duplicate work, duplicate network calls.","`stateIn` = hot + latest-value cache + initial value (state); `shareIn` = hot multicast with configurable replay (streams). Both take a `SharingStarted` policy.","Interview edge: `StateFlow.collect` never completes (hot flows don't end) — so code *after* `collect { }` in the same coroutine is unreachable; each hot collection needs its own `launch`. Also `Flow.asLiveData()` exists for legacy bridges."]}]},{level:"senior",q:"How would you migrate a legacy MVP (or God-Activity) codebase to MVVM incrementally?",a:[{t:"list",items:["**Strangler approach, screen by screen** — never a big-bang rewrite; both patterns coexist behind the same repository layer.","**Step 0: carve out the data layer** — if Presenters/Activities call APIs directly, extract repositories first; this benefits both old and new code and is where most risk hides.","**Mechanical MVP→MVVM per screen**: the Presenter's logic moves into a ViewModel; every `view.showX(...)` call becomes a field in a `UiState` emitted through StateFlow; the View interface is deleted and the Activity/Fragment becomes a renderer of state. Attach/detach lifecycle code disappears (viewModelScope + repeatOnLifecycle replace it).","**Characterization tests first** on the Presenter where coverage is thin — then port; the new VM tests assert emitted states instead of verified view-mock calls (usually *simpler*).","**Prioritize** screens that are actively changing or crash-prone; leave stable dead screens for last (or forever). New features: MVVM only.","**Team conventions before scale**: agree the UiState/event/DI idioms on the first migrated screen and document it — otherwise you get N dialects of MVVM."]}]}],fx={id:"mvvm",title:"MVVM",tagline:"Model – View – ViewModel: the default presentation architecture for Android & KMP — internals, state, events, testing, and every interview angle.",tags:["Android","KMP","Jetpack ViewModel","StateFlow","UDF","Testing"],content:px,qa:mx},gx=[{heading:"What is MVI",blocks:[{t:"p",text:"**MVI (Model–View–Intent)** is a reactive, unidirectional architecture inspired by Cycle.js and Redux, brought to Android by Hannes Dorfmann (~2016). Despite the name, it's less about three components and more about a **cycle**: the View emits **Intents** (user intentions, not Android `Intent`s), a processing loop reduces them into a new immutable **Model** (state), and the View **renders** that state. One loop, one direction, no side doors."},{t:"list",items:["**Intent** — a reified user action or trigger: `data class QueryChanged(val q: String)`, `object Refresh`, `object RetryClicked`. Everything that can happen is a value.","**Model (State)** — a single immutable object describing the *entire* screen at a moment. Not the domain model — the rendered state.","**View** — `render(state)`: a pure mapping from state to UI, plus a stream of intents. In Compose this is literally the composable signature.","**Reducer** — pure function `(currentState, change) -> newState`. The only place state is produced.","**Side effects** — async work (network, DB) triggered by intents, whose *results* are fed back into the reducer as changes."]},{t:"note",text:'One-line definition for interviews: "MVI is MVVM with the screws tightened: exactly one immutable state, all inputs reified as intent objects through a single entry point, and all state transitions through a pure reducer — giving you an auditable, replayable state machine per screen."'}]},{heading:"The cycle in code — a hand-rolled MVI ViewModel",blocks:[{t:"p",text:"MVI on Android is usually *hosted inside* a Jetpack ViewModel (for lifecycle survival) — the patterns are complementary, not competing:"},{t:"code",title:"Contract: state, intents, one-shot effects",code:`data class SearchState(
    val query: String = "",
    val results: List<Item> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null,
)

sealed interface SearchIntent {
    data class QueryChanged(val query: String) : SearchIntent
    data object Retry : SearchIntent
    data class ItemClicked(val id: String) : SearchIntent
}

sealed interface SearchEffect {
    data class NavigateToDetail(val id: String) : SearchEffect
    data class ShowSnackbar(val message: String) : SearchEffect
}`},{t:"code",title:"The loop: single entry point, reducer, effects",code:`class SearchViewModel(
    private val repository: SearchRepository,
) : ViewModel() {

    private val _state = MutableStateFlow(SearchState())
    val state: StateFlow<SearchState> = _state.asStateFlow()

    private val _effects = Channel<SearchEffect>(Channel.BUFFERED)
    val effects: Flow<SearchEffect> = _effects.receiveAsFlow()

    private var searchJob: Job? = null

    // Single entry point — every user action funnels through here
    fun onIntent(intent: SearchIntent) {
        when (intent) {
            is SearchIntent.QueryChanged -> {
                reduce { it.copy(query = intent.query, isLoading = true) }
                search(intent.query)
            }
            is SearchIntent.Retry -> search(state.value.query)
            is SearchIntent.ItemClicked ->
                viewModelScope.launch {
                    _effects.send(SearchEffect.NavigateToDetail(intent.id))
                }
        }
    }

    private fun search(query: String) {
        searchJob?.cancel()
        searchJob = viewModelScope.launch {
            runCatching { repository.search(query) }
                .onSuccess { items ->
                    reduce { it.copy(isLoading = false, results = items, error = null) }
                }
                .onFailure { e ->
                    reduce { it.copy(isLoading = false, error = e.message) }
                }
        }
    }

    // All mutations go through one reducer helper — greppable, loggable
    private fun reduce(reducer: (SearchState) -> SearchState) {
        _state.update(reducer)
    }
}`},{t:"code",title:"View — render + intent stream (Compose)",code:`@Composable
fun SearchScreen(viewModel: SearchViewModel) {
    val state by viewModel.state.collectAsStateWithLifecycle()

    LaunchedEffect(Unit) {
        viewModel.effects.collect { effect ->
            when (effect) {
                is SearchEffect.NavigateToDetail -> { /* nav */ }
                is SearchEffect.ShowSnackbar -> { /* snackbar */ }
            }
        }
    }

    SearchContent(
        state = state,
        onIntent = viewModel::onIntent, // one lambda, not N callbacks
    )
}`},{t:"list",items:["The **single `onIntent` entry point** is what enables MVI's superpowers: log every intent and every state and you have a complete, replayable history of the screen.","The reducer here is the `copy` lambdas — strict MVI extracts them into a standalone pure function `reduce(state, change): State` that's trivially unit-tested with no coroutines at all.","Note it still uses `StateFlow` + `viewModelScope` — MVI on Android is a *discipline layered on* the MVVM toolkit."]}]},{heading:"State machine thinking & modeling rules",blocks:[{t:"list",items:["**Make illegal states unrepresentable**: if loading and error can't coexist, encode that (sealed sub-states) rather than trusting flag discipline.","**State is render-ready**: formatted strings, sorted lists, resolved flags — the View must not compute.","**Reducers are pure and synchronous**: no IO, no clocks, no randomness inside — those enter as *inputs* (changes/results). Purity is what makes them exhaustively testable.","**Long-running work is cancelled by newer intents** where it matters (`searchJob?.cancel()` / `flatMapLatest`) — the reducer alone doesn't protect you from stale-result races; effect management does.","**Time-travel & debugging**: because every transition is (state, change) → state, you can log the full sequence, reproduce bugs by replaying intents, and snapshot state in bug reports — the operational argument for MVI in large teams."]},{t:"note",text:'Classic probing question: "where do side effects go if reducers must be pure?" Answer: an effect-handling layer *around* the reducer (the intent handler / middleware / `intent { }` block in Orbit, Executors in MVIKotlin) performs IO and feeds results back in as plain data. Redux calls the same slot middleware/thunks.'}]},{heading:"One-shot effects in MVI",blocks:[{t:"p",text:"Purist MVI says *everything* is state — but navigation and snackbars fit awkwardly (the sticky-state problem, same as MVVM). In practice every MVI codebase picks one of:"},{t:"list",items:["**A separate effect stream** (Channel/SharedFlow) beside the state — pragmatic standard, used by Orbit (`postSideEffect`) and MVIKotlin (Labels). Keeps state clean; inherits the usual event-delivery caveats.","**Effects-as-state with consumption** — `pendingNavigation: Destination?` cleared by a `NavigationConsumed` intent; purist, replay-safe, more ceremony.","**One-off state flags with IDs** — event objects carrying unique ids so re-renders can dedupe; seen in Redux-family ports."]}]},{heading:"The MVI library landscape",blocks:[{t:"table",headers:["Library","Shape","Notes"],rows:[["**Orbit MVI**","DSL inside a ViewModel: `intent { reduce { } postSideEffect() }`","Gentlest adoption path — 'MVVM+' feel, KMP support, minimal boilerplate"],["**MVIKotlin** (Arkadii Ivanov)","Store/Executor/Reducer, framework-agnostic","Strictest separation; pairs with **Decompose** for KMP navigation/lifecycle; popular in serious KMP apps"],["**Mavericks** (Airbnb)","ViewModel + `Async<T>` state wrappers + `setState/withState`","Battle-tested at scale in the RecyclerView/Epoxy era; less momentum in pure-Compose codebases"],["**Molecule** (Cash App)","Compose runtime to *produce* StateFlow from composable logic","Not an MVI framework per se — a way to write the state-production loop declaratively"],["**Circuit** (Slack)","Compose-driven Presenter+UI pairs, state classes with embedded event sinks","A modern rethink: UDF architecture built entirely on Compose, KMP-ready"],["Hand-rolled","StateFlow + sealed intents + reduce helper","Perfectly viable; most 'MVI' production code is exactly this"]]}]},{heading:"MVI vs MVVM — honest trade-offs",blocks:[{t:"table",headers:["","MVVM (typical)","MVI (strict)"],rows:[["State","one or several observable streams","exactly one immutable state object"],["User input","N public functions on the VM","one entry point taking sealed intents"],["Mutation","anywhere inside the VM via update{}","only the reducer"],["Auditability","log manually per mutation","free: intent log + state log = full history"],["Boilerplate","lower","higher (intent classes, changes, wiring)"],["Learning curve","low","moderate; reducer/effect discipline must be taught"],["Risk profile","logic scattering as VM grows","ceremony on trivial screens; giant state objects recomposing everything"]]},{t:"list",items:["**Choose MVI** when screens are genuinely stateful machines (editors, players, checkout flows), when the team is large enough that convention-by-discipline fails, or in KMP where MVIKotlin+Decompose is a mature stack.","**Stay MVVM** for CRUD-ish screens and small teams — modern MVVM with a single `UiState` + `update {}` already delivers ~80% of MVI's value at ~50% of the ceremony.","**Performance edge case**: a single giant state object means every change recomposes/diffs everything observing it — mitigate by splitting screen state into cohesive sub-objects, using `derivedStateOf`/field-level selectors, and keeping lists stable (immutable collections, stable keys)."]},{t:"note",text:"The mature closing take: MVVM and MVI have converged. Google's guidance (single UiState, UDF, events-as-state) is MVI in all but name; the remaining difference is whether intents are reified and reduction is centralized — a *team-scaling* decision, not a correctness one."}]},{heading:"MVI in KMP",blocks:[{t:"list",items:["MVI is arguably **more popular in KMP than on pure Android**: a framework-free state machine in `commonMain` is the perfect shared brain, with each platform just rendering state and sending intents.","**MVIKotlin + Decompose** is the flagship stack: Decompose provides lifecycle-aware, navigable 'components' (BLoC-style) shared across Android/iOS/desktop/web; MVIKotlin provides the Store loop inside them.","**Orbit** works in KMP too, hosted in the multiplatform androidx ViewModel.","iOS consumption is the same story as MVVM: bridge the state `StateFlow`/`Value` to SwiftUI via SKIE/adapters; intents are plain function calls into shared code, which Swift handles natively.","Sealed intent/state hierarchies crossing to Swift benefit hugely from **SKIE** (exhaustive Swift enums instead of class-check chains)."]}]}],yx=[{level:"junior",q:"What is MVI and how is the data flow different from MVVM?",a:[{t:"p",text:"MVI (Model–View–Intent) is a unidirectional cycle: the View emits **Intents** (reified user actions like `Refresh` or `QueryChanged`), a loop processes them and a pure **reducer** produces a new immutable **State**, and the View renders that state. Versus MVVM: instead of N public ViewModel functions there's **one entry point** taking sealed intent objects; instead of possibly several streams there's **exactly one state object**; and state may only change **through the reducer**. Same observation mechanics (StateFlow), stricter rules on top."}]},{level:"junior",q:"Is an MVI 'Intent' the same as an Android Intent?",a:[{t:"p",text:"No — unfortunate name collision. An MVI intent is a plain value expressing a *user's intention*: `data class ItemClicked(val id: String)`, `object Retry` — usually a sealed interface per screen. Android's `Intent` is the OS-level messaging object for launching activities/services. Zero relation; interviewers ask exactly to check you know the difference."}]},{level:"junior",q:"What is a reducer and why must it be pure?",a:[{t:"p",text:"A reducer is a function `(currentState, change) -> newState` — the only place a new state is produced. Pure means: no IO, no time, no randomness, no mutation — output depends only on inputs. Why it matters: pure reducers are **exhaustively unit-testable** (plain function calls, no coroutines or mocks), **deterministic** (same intent log always replays to the same state — the basis of time-travel debugging), and **safe to reason about** (all transition logic is in one greppable place). Async work happens *around* the reducer and feeds results back in as data."}]},{level:"junior",q:"What does 'single source of truth' mean in MVI?",a:[{t:"p",text:"The screen has exactly **one state object**, and everything the UI shows derives from it. There's no second place where the checkbox's value or the list's loading flag lives — no view-held state drifting out of sync, no 'the widget says X but the model says Y' bugs. Practical consequences: `render(state)` can redraw the whole screen from scratch at any time (which is literally what Compose does), state can be snapshotted for bug reports, and restoring the screen equals restoring one object."}]},{level:"junior",q:"Does MVI replace the Jetpack ViewModel?",a:[{t:"p",text:"No — they're complementary. On Android, the MVI loop (state, intents, reducer) almost always lives **inside** a Jetpack ViewModel, which contributes what MVI itself doesn't address: surviving configuration changes, `viewModelScope`, and `SavedStateHandle`. MVI is a *discipline about how state changes*; ViewModel is *infrastructure about where state lives*. (In KMP, alternatives like Decompose components can play the infrastructure role instead.)"}]},{level:"junior",q:"What are the practical benefits of funneling everything through one onIntent() entry point?",a:[{t:"list",items:["**Complete audit log**: log one function and you've captured every user action — attach the intent+state history to crash reports and bugs become replayable.","**One lambda down the Compose tree** (`onIntent = vm::onIntent`) instead of a growing bundle of callbacks.","**Cross-cutting policies in one place**: throttling double-clicks, analytics, permission gates — wrap the funnel once.","**Exhaustiveness**: a sealed intent + `when` forces handling every action; adding an intent without handling it fails compilation."]}]},{level:"junior",q:"How does the View send user actions in MVI, and what does render(state) mean?",a:[{t:"p",text:"The View converts raw UI events into intent objects and passes them to the single entry point — `onClick = { onIntent(SearchIntent.Retry) }`. `render(state)` is the other half of the contract: a function that maps the state object to the screen with **no decisions of its own** — every `if` in render should be reading a state field, not computing policy. In Compose, the composable *is* `render`: it takes `state` and `onIntent` as parameters, which also makes it trivially previewable with fake states."}]},{level:"senior",q:"If reducers must be pure, walk me through exactly how a network call flows through an MVI loop.",a:[{t:"list",items:["1. View emits `Intent.Refresh` → entry point.","2. The **effect/executor layer** (Orbit's `intent {}` block, MVIKotlin's Executor, hand-rolled handler) — *not* the reducer — receives it, immediately dispatches a synchronous change `Change.Loading` through the reducer (state now shows loading), and launches the coroutine.","3. The network call runs in the effect layer; its outcome is converted into plain data: `Change.Loaded(items)` or `Change.Failed(error)`.","4. That change is dispatched through the reducer → new state → View re-renders. The reducer only ever saw values."]},{t:"p",text:"Follow-ups to preempt: cancellation lives in the effect layer (`flatMapLatest` / job tracking per intent type — a new Refresh cancels the old one); stale-result races are prevented there too, or defensively in the reducer by ignoring results that don't match current input; and this layered shape is exactly Redux middleware / Elm commands — naming that shows you see the family resemblance."}]},{level:"senior",q:"How do you test an MVI screen? What becomes easier and what becomes harder than MVVM?",a:[{t:"list",items:["**Reducer tests** — the payoff: pure function, table-driven tests over (state, change) pairs, no coroutines/mocks/dispatchers. Edge transitions (error while loading, retry after error) become one-line cases.","**Loop/integration tests** — same machinery as MVVM: TestDispatcher + Turbine, send intents, assert the emitted state sequence; assert effects on the effect stream separately.","**Libraries ship test DSLs**: Orbit's test module asserts posted states/side effects; MVIKotlin stores are testable without any framework."]},{t:"p",text:"Easier: transition logic coverage, exhaustiveness (sealed classes force handling), reproducing bugs by replaying an intent sequence. Harder/more verbose: simple flows now need intent + change + reducer wiring for what MVVM did in one function; and asynchronous effect-ordering tests (two competing intents cancelling each other) remain exactly as tricky as in MVVM — MVI doesn't magically solve concurrency, it just localizes it."}]},{level:"senior",q:"A single giant state object recomposes the whole screen on every keystroke. How do you fix MVI's performance problem?",a:[{t:"list",items:["**Split the screen state into cohesive sub-objects** (`SearchBarState`, `ResultsState`) and pass only the slice each composable needs — Compose skips recomposition when an unchanged stable slice is passed.","**Keep the state stable in Compose's sense**: immutable collections (kotlinx.collections.immutable or `@Immutable` wrappers), no `List` that Compose must treat as unstable, stable data classes — otherwise skipping never happens regardless of splitting.","**Selectors / field-level flows**: expose `state.map { it.subState }.distinctUntilChanged()` (or `derivedStateOf` in the UI) so consumers observe only their slice.","**Don't put high-frequency transient values through the full loop**: text-field composition state or drag positions can stay local UI state, entering the loop debounced/committed — running an entire reduce cycle per keystroke is the actual smell.","**Measure first**: Layout Inspector recomposition counts / composition tracing — most 'MVI is slow' reports are unstable-collection bugs, not the architecture."]}]},{level:"senior",q:"Compare Orbit, MVIKotlin, and hand-rolled MVI — when would you pick each?",a:[{t:"list",items:["**Hand-rolled** (StateFlow + sealed intents + reduce helper): zero dependency, team learns the mechanics, fully sufficient for a handful of screens. Risk: N developers → N dialects; no test DSL; discipline decays without review pressure.","**Orbit**: thin DSL over exactly that (`intent { reduce { } postSideEffect() }`) hosted in a ViewModel — lowest-friction formalization, KMP-ready, good test support. Pick when you want MVVM-adjacent ergonomics with enforced structure.","**MVIKotlin**: full Store/Executor/Reducer separation, framework-independent, with time-travel tooling; pairs with Decompose for lifecycle/navigation. Pick for serious KMP (shared brain across Android/iOS/desktop) or when you want the strictest, most testable separation and accept the ceremony."]},{t:"p",text:"Also worth naming: **Mavericks** (mature, Epoxy-era, less Compose-native momentum) and **Circuit** (Slack's Compose-first rethink). Decision axis in one line: how much enforced structure does the team's size and platform spread justify?"}]},{level:"senior",q:"How does MVI handle process death? Does the intent log help?",a:[{t:"p",text:"The intent log is an in-memory debugging artifact — it dies with the process, and replaying a persisted log on restore is impractical (effects would re-fire network calls; the world has changed). Real answer: same discipline as MVVM, but MVI's single state object makes it cleaner to execute. Persist the **minimal seed** — ids, user input, scroll anchors — into `SavedStateHandle` (some teams make the state class itself contain a small `@Parcelize` 'persistable core'), and rebuild the rest by re-running the normal load path on restore. The reducer helps here: restoration is just an ordinary `Change.Restored(seed)` flowing through the same loop, not a special code path. What you must *not* do is parcel the whole state (TransactionTooLarge risk with lists) or treat replay-from-log as a persistence strategy."}]},{level:"senior",q:"Steelman the case AGAINST adopting MVI on a team currently happy with MVVM.",a:[{t:"list",items:["**The delta has shrunk**: MVVM done to Google's current guidance (single UiState, update{}, UDF, events-as-state) already yields most of MVI's benefits — the migration buys reified intents and a centralized reducer, which mainly pay off at large team scale.","**Ceremony tax on simple screens**: a settings toggle becomes intent class + change + reducer branch + wiring; developers respond by smuggling shortcuts, and inconsistent-MVI is worse than consistent-MVVM.","**Migration cost is real**: retraining, rewriting stable screens, a long bilingual period, and test-suite conversion — spent on screens that weren't causing bugs.","**It doesn't solve the hard problems**: one-shot events, process death, and async races persist in MVI (just relocated); anyone selling MVI as the fix for those is overpromising.","**Framework lock-in risk** if a library (with its DSL and test tooling) is adopted, versus discipline that's harder to enforce if hand-rolled — you pay one of those two costs."]},{t:"p",text:"Then the balanced close: I'd still reach for MVI selectively — the genuinely state-machine-like screens — and for KMP shared logic, where MVIKotlin/Decompose maturity changes the calculus. Showing you can argue both sides *is* the senior signal on this question."}]},{level:"senior",q:"Explain how MVI relates to Redux, Elm, and Flux — and why that lineage matters.",a:[{t:"p",text:"Direct family line: **Flux** (Facebook) introduced unidirectional dispatch; **Elm** formalized the pure `update : Msg -> Model -> Model` loop with effects as data (Commands); **Redux** popularized single-store + pure reducers + middleware in JS; **Cycle.js** framed UI as a stream cycle — and Hannes Dorfmann's MVI translated that stack of ideas to Android's Rx world. The mapping is one-to-one: Intent = Action/Msg, State = Store/Model, Reducer = reducer/update, effect layer = middleware/Commands, render = view function."},{t:"p",text:"Why it matters beyond trivia: the lineage tells you MVI's properties are *inherited theorems, not marketing* — determinism, replay, time-travel come from reducer purity (Elm proved the model); the middleware slot is where every family member handles IO, so you know where effects belong in any MVI library you meet; and the known failure modes (boilerplate fatigue, giant single stores) played out in the JS ecosystem first, so their mitigations (state slicing, selectors — reselect in Redux, field-flows in MVI) are already mapped. Recognizing a 'new' architecture as an instance of a family is exactly the judgment senior interviews probe for."}]},{level:"junior",q:"What does MVI stand for, and what are its three core principles?",a:[{t:"p",text:"MVI stands for **Model–View–Intent**, but the name undersells it — MVI is less about three boxes and more about a *cyclic, unidirectional* flow of data. The View emits Intents (user intentions), those are reduced into a new immutable Model (state), and the View renders that state. It grew out of Redux/Elm/Cycle.js and came to Android around 2016."},{t:"list",items:["**Single immutable state** — the entire screen is described by *one* immutable state object at any moment. Not several streams — one.","**Reified intents** — every user action is a value (`data class QueryChanged(val q: String)`), funneled through a single entry point, not scattered method calls.","**Pure reduction** — state only ever changes through a pure reducer function `(state, change) -> newState`; no other code mutates state."]},{t:"p",text:"These three constraints are what distinguish MVI from MVVM. MVVM allows several observable streams, N public functions for input, and mutation anywhere in the ViewModel. MVI tightens all three: one state, one input funnel, one place mutation happens. The payoff is predictability (state is a pure function of the intent history) and debuggability (log the intents and states and you can replay any bug)."},{t:"note",text:"MVI in one line: 'MVVM with the screws tightened — exactly one immutable state, all inputs as intent objects through one entry point, all changes through a pure reducer.'"}]},{level:"junior",q:"What is Unidirectional Data Flow (UDF), and why does MVI enforce it?",a:[{t:"p",text:"Unidirectional Data Flow means data moves in *one direction* around a cycle: state flows *down* to the UI, and events flow *up* from the UI. The UI never mutates state directly — it emits an event, something processes it into new state, and the new state flows back down to be rendered. There's no two-way binding, no UI reaching in to change data."},{t:"code",title:"The UDF cycle",code:`// State flows DOWN to the UI:
render(state)                          // UI is a pure function of state

// Events flow UP from the UI:
onClick = { store.onIntent(Intent.Refresh) }   // UI emits an intent, never mutates state

// The intent produces new state, which flows down again -> cycle`},{t:"p",text:"MVI enforces UDF strictly by making state immutable and changeable only through the reducer, and by making all input explicit intents. The benefit is that the UI can *always* be redrawn from scratch given the current state (which is exactly what Compose does), and there's a single, traceable path for every change. Bugs where 'the UI and the data disagree' become impossible because the UI is *derived* from a single state, not independently mutated."},{t:"p",text:"Worth noting: modern MVVM on Android *also* uses UDF (single UiState, state down / events up). So UDF isn't unique to MVI — it's the shared foundation. MVI just adds stricter rules (reified intents, a formal reducer) on top of the same UDF principle."},{t:"note",text:"UDF = state down, events up, UI never mutates state. It's shared by modern MVVM *and* MVI — MVI just formalizes it further with intents and a reducer."}]},{level:"junior",q:"Show a complete minimal MVI implementation.",a:[{t:"p",text:"A minimal MVI setup has four pieces: an immutable **State**, a sealed **Intent** type (all possible user actions), a single **entry point** (`onIntent`) that funnels actions, and a **reducer** that produces new state. On Android it's usually hosted inside a ViewModel for lifecycle survival."},{t:"code",title:"State + Intent + reducer loop",code:`data class SearchState(
    val query: String = "",
    val results: List<Item> = emptyList(),
    val isLoading: Boolean = false,
)

sealed interface SearchIntent {                     // every action is a value
    data class QueryChanged(val q: String) : SearchIntent
    data object Retry : SearchIntent
}

class SearchViewModel(private val repo: Repo) : ViewModel() {
    private val _state = MutableStateFlow(SearchState())
    val state: StateFlow<SearchState> = _state.asStateFlow()

    fun onIntent(intent: SearchIntent) {            // SINGLE entry point
        when (intent) {
            is SearchIntent.QueryChanged -> {
                reduce { it.copy(query = intent.q, isLoading = true) }  // pure update
                search(intent.q)
            }
            is SearchIntent.Retry -> search(state.value.query)
        }
    }

    private fun search(q: String) = viewModelScope.launch {
        val items = repo.search(q)                  // side effect (outside the reducer)
        reduce { it.copy(isLoading = false, results = items) }
    }
    private fun reduce(f: (SearchState) -> SearchState) { _state.update(f) }  // one mutation point
}`},{t:"code",title:"The View (render + intents)",code:`@Composable fun SearchScreen(vm: SearchViewModel) {
    val state by vm.state.collectAsStateWithLifecycle()
    SearchContent(state = state, onIntent = vm::onIntent)  // one lambda, not N callbacks
}`},{t:"p",text:"Notice this still uses `StateFlow` + `viewModelScope` — MVI on Android is a *discipline layered on* the MVVM toolkit, not a replacement for it. The strict version extracts the reducer into a standalone pure function; here the `copy` lambdas are the reducer inline."},{t:"note",text:"Point out that MVI runs *inside* a ViewModel using StateFlow — it's a discipline (single state, intent funnel, reducer), not a different runtime from MVVM."}]},{level:"junior",q:"What is the difference between State and Effect (side effect) in MVI?",a:[{t:"p",text:"In MVI, **State** is the *persistent* description of the screen — what should be shown right now (loading, the list, an error message). It survives and is re-rendered on every recomposition/rotation. An **Effect** (or side effect / one-shot event) is something that should happen *exactly once* and isn't part of the screen's persistent picture — navigating away, showing a snackbar, triggering a vibration."},{t:"list",items:["**State** — 'what the screen looks like'. Sticky, re-rendered, survives config change. Example: `isLoading = true`, `results = [...]`.","**Effect** — 'a one-time action'. Fire-and-forget, must not repeat on re-render. Example: navigate to detail, show a toast."]},{t:"code",title:"State vs Effect",code:`data class State(val results: List<Item>, val isLoading: Boolean)   // persistent

sealed interface Effect {                                          // one-shot
    data class NavigateTo(val id: String) : Effect
    data class ShowSnackbar(val msg: String) : Effect
}
// Effects go through a SEPARATE channel (SharedFlow/Channel), not the state:
private val _effects = Channel<Effect>()
val effects: Flow<Effect> = _effects.receiveAsFlow()`},{t:"p",text:"Why the distinction matters: if you put a one-shot action *in* state (e.g. `navigate = true`), it re-fires on every recomposition and after rotation — the sticky-state problem. So effects need a *separate*, non-replaying channel (a `Channel` or `SharedFlow`). Purist MVI tries to model even effects as consumable state, but most implementations use a dedicated effect stream (Orbit's `postSideEffect`, MVIKotlin's Labels) precisely because true one-shot events don't fit the sticky-state model."},{t:"note",text:"State = persistent 'what to show' (sticky, re-rendered); Effect = one-shot 'do this once' (navigation/snackbar) via a separate channel — putting effects in state causes the re-fire-on-rotation bug."}]},{level:"senior",q:"MVI vs MVVM — what are the honest trade-offs?",a:[{t:"p",text:"MVI is essentially MVVM with three extra constraints (single state, reified intents, pure reducer). Those constraints buy predictability and debuggability at the cost of boilerplate and ceremony. Whether it's worth it depends heavily on the screen's complexity and the team's size."},{t:"table",headers:["","MVVM (typical)","MVI (strict)"],rows:[["State","one or several observable streams","exactly one immutable state object"],["User input","N public functions on the VM","one entry point taking sealed intents"],["Mutation","anywhere in the VM via update{}","only the reducer"],["Auditability","log manually per mutation","free: intent log + state log = full history"],["Boilerplate","lower","higher (intent classes, changes, wiring)"],["Learning curve","low","moderate (reducer/effect discipline)"],["Best for","CRUD screens, small teams","complex state machines, large teams"]]},{t:"p",text:"The nuance that shows maturity: MVVM and MVI have *converged*. Google's recommended architecture — single UiState, `update {}`, UDF, events-as-state — is MVI in all but name. The remaining difference is whether intents are *reified* (sealed Intent objects vs plain method calls) and whether reduction is *centralized* (one reducer vs mutation scattered in the VM). That's a *team-scaling* decision, not a correctness one: reified intents + a central reducer give you an audit log and enforced discipline, which pays off on large teams and complex screens but is ceremony on a simple CRUD screen."},{t:"note",text:"MVVM-with-UDF and MVI have converged; the real delta is 'reified intents + central reducer' (audit log, enforced discipline) — worth it at team/complexity scale, ceremony otherwise."}]},{level:"senior",q:"When should you choose MVI over MVVM?",a:[{t:"p",text:"Choose MVI when the benefits of its stricter constraints (single state, reified intents, pure reducer) outweigh their ceremony — which is specifically for *complex, stateful screens* and *large teams*, or for *KMP shared logic*. For simple CRUD screens or small teams, modern MVVM (single UiState + UDF) already gives ~80% of the benefit at ~50% of the cost."},{t:"list",items:["**Choose MVI when** — screens are genuinely state-machine-like (editors, media players, multi-step wizards, checkout flows) where impossible states and complex transitions are a real risk; the team is large enough that *convention* (MVVM's 'please use a single UiState') fails and you want the *structure enforced*; you need auditability/time-travel for debugging complex flows; or you're in KMP where MVIKotlin+Decompose is a mature shared-logic stack.","**Stay with MVVM when** — screens are CRUD-ish (a list, a form, a detail), the team is small, and the ceremony (intent classes, changes, reducer wiring) would be overhead without payoff. Modern MVVM with a single UiState is already UDF and already 'MVI-lite'.","**Performance caveat either way** — a single giant state object recomposes everything observing it; mitigate by splitting state into cohesive sub-objects and keeping it stable, regardless of MVI or MVVM."]},{t:"p",text:"The mature close: I don't pick dogmatically. Compose + StateFlow + UDF naturally lands *between* MVVM and MVI, which is exactly what Google's guidance recommends. I'd reach for full MVI (with a library like Orbit or MVIKotlin) selectively — the genuinely complex screens — and use MVVM-with-UDF as the default, rather than imposing MVI's ceremony everywhere."},{t:"note",text:"MVI for complex state-machine screens + large teams + KMP; MVVM-with-UDF as the default for CRUD/small teams. Modern MVVM is already 'MVI-lite', so reserve full MVI for where its discipline actually pays."}]},{level:"senior",q:"How does MVI achieve 'make illegal states unrepresentable'?",a:[{t:"p",text:"'Make illegal states unrepresentable' means designing your state type so that combinations that should never happen *cannot even be expressed* — the compiler prevents them. MVI leans into this by modeling state carefully, often with sealed hierarchies, so that mutually-exclusive states can't coexist."},{t:"code",title:"Representable illegal states vs unrepresentable",code:`// BAD: flags allow impossible combinations
data class State(val isLoading: Boolean, val data: List<Item>?, val error: String?)
// isLoading=true AND error!=null AND data!=null — all three at once? Meaningless but possible.

// GOOD: sealed type — the states are mutually exclusive, illegal combos can't exist
sealed interface State {
    data object Loading : State
    data class Content(val data: List<Item>) : State
    data class Error(val message: String) : State
}
// You literally cannot be Loading AND Error at the same time.`},{t:"p",text:"The reducer then benefits: a pure `(state, change) -> newState` over a sealed state, handled with an exhaustive `when`, means the compiler forces you to handle every state, and you can't accidentally produce an impossible one. This is a big part of MVI's correctness argument — the *type system* encodes the valid states, and the reducer is the only place they change, so invalid states are eliminated by construction rather than guarded against at runtime."},{t:"p",text:"The caveat: sealed states are ideal for *mutually exclusive* states, but real screens often have *overlapping* concerns (showing cached data *while* refreshing *with* an error banner). Forcing those into a sealed hierarchy causes state duplication. So the pragmatic approach is a data class whose fields include sealed sub-states — you get 'illegal states unrepresentable' for the parts that are genuinely exclusive without contorting the parts that legitimately overlap."},{t:"note",text:"Model exclusive states as a sealed hierarchy so impossible combos can't compile; use a data class (possibly with sealed sub-fields) when states legitimately overlap. Illegal-states-unrepresentable is a design goal, not a mandate to sealed-everything."}]},{level:"senior",q:"How do you handle one-shot effects (navigation, snackbars) in MVI without the re-fire bug?",a:[{t:"p",text:"One-shot effects — navigation, snackbars, toasts — must happen *exactly once*, but MVI's state is sticky (re-rendered on every recomposition and after rotation). If you put an effect in state (`navigate = true`), it re-fires on the next render. So effects need a *separate* delivery mechanism from state, and there are a few approaches with different trade-offs."},{t:"list",items:["**Separate effect channel (most common)** — a `Channel(BUFFERED).receiveAsFlow()` or a `SharedFlow(replay=0)` beside the state, carrying `Effect` values the View collects once. Orbit's `postSideEffect` and MVIKotlin's Labels do this. Pragmatic and clean; inherits the usual event-delivery caveats (a SharedFlow can drop events emitted while no collector is active; a Channel suspends until delivered, which is safer).","**Effect-as-consumable-state (purist)** — a nullable field in state (`pendingNav: Destination?`) cleared by a `NavConsumed` intent after the View handles it. Survives config change and (if in SavedStateHandle) process death; replay-safe. More ceremony.","**One-off events with unique ids** — event objects carrying a unique id so re-renders dedupe; seen in Redux-family ports."]},{t:"code",title:"Effect channel + collection",code:`private val _effects = Channel<Effect>(Channel.BUFFERED)
val effects: Flow<Effect> = _effects.receiveAsFlow()

fun onIntent(i: Intent) { if (i is Intent.ItemClicked)
    viewModelScope.launch { _effects.send(Effect.NavigateTo(i.id)) } }

// View collects once:
LaunchedEffect(Unit) { vm.effects.collect { when (it) {
    is Effect.NavigateTo -> navController.navigate(it.id) } } }`},{t:"p",text:"This is the *same* one-shot event problem that MVVM has — MVI doesn't magically solve it; it just gives it a named home (the effect channel). My default is a `Channel` for genuine fire-and-forget (navigation, snackbars) because `send` suspends until delivered, and consumable-state for anything that must survive process death."},{t:"note",text:"Effects go through a *separate* channel (Channel/SharedFlow), never state — putting them in state causes re-fire on rotation. MVI doesn't solve the one-shot problem, it just names it (the effect channel)."}]},{level:"senior",q:"What are the real costs and downsides of adopting MVI?",a:[{t:"p",text:"MVI's benefits (predictability, auditability, enforced discipline) are real, but so are its costs — and a balanced answer names them, because uncritically advocating MVI is a red flag. The costs are mostly *ceremony* and the risk of over-applying it."},{t:"list",items:["**Boilerplate** — every action needs an intent class, often a 'change' type, and reducer wiring. A settings toggle that's one line in MVVM becomes intent + change + reducer branch + wiring in strict MVI.","**Ceremony tax on simple screens** — for CRUD screens, the machinery adds indirection without payoff, and developers respond by smuggling shortcuts, which makes *inconsistent* MVI (worse than consistent MVVM).","**Giant single-state performance** — one state object means every change potentially recomposes everything observing it; you must split state into cohesive slices and keep it stable, or scrolling/typing janks.","**Learning curve** — reducer purity, effect handling, and the intent/change/reducer separation must be taught; a team half-understanding MVI produces a mess.","**It doesn't solve the hard problems** — one-shot events, process death, and async races persist in MVI (just relocated); anyone selling MVI as the fix for those is overpromising.","**Framework lock-in risk** — adopting Orbit/MVIKotlin brings a DSL and test tooling you're now coupled to; hand-rolling avoids that but loses the enforcement."]},{t:"p",text:"The balanced position: MVI is excellent for genuinely complex, state-machine-like screens and large teams needing enforced structure, and increasingly for KMP. But for simple screens or small teams, modern MVVM-with-UDF captures most of the value at far less cost — so applying MVI everywhere is over-engineering. Being able to argue *against* MVI is itself the senior signal."},{t:"note",text:"Name MVI's costs honestly: boilerplate, ceremony on simple screens, giant-state recomposition, learning curve, and that it doesn't solve events/process-death/races. Being able to argue against MVI is the senior tell."}]},{level:"senior",q:"How does time-travel debugging work in MVI, and why is it possible?",a:[{t:"p",text:"Time-travel debugging means being able to step backward and forward through an app's states — replaying how the UI got to its current state, and even reproducing a bug by replaying the exact sequence of user actions. It's possible in MVI *because* of its constraints: state is immutable, changes only happen through a pure reducer, and every input is a reified intent — so the entire history is a deterministic function of a logged sequence."},{t:"list",items:["**Log the inputs and outputs** — since every action is an `Intent` value and every state is immutable, you can record the full sequence: intent → state → intent → state.","**Determinism from reducer purity** — because the reducer is pure `(state, change) -> newState`, replaying the same intents from the same start *always* produces the same states. No hidden mutation means no non-reproducibility.","**Replay a bug** — attach the intent log to a crash report; a developer replays the intents through the reducer and reproduces the exact state that caused the bug. No guessing 'what did the user do?'.","**Snapshot state** — because state is a single immutable object, you can snapshot it at any point (for bug reports) or diff two states."]},{t:"p",text:"This is inherited from MVI's ancestors — Elm formalized the pure `update` loop, and Redux DevTools popularized time-travel in JS. MVIKotlin provides time-travel tooling on Android for exactly this reason. The caveat: the *state* is replayable, but *effects* (network calls) are not re-run on replay (the world has changed), and the log is in-memory so it dies with the process — so time-travel is a *debugging* aid, not a persistence mechanism. Still, 'reproduce any bug by replaying its intents' is a genuine, MVI-specific superpower."},{t:"note",text:"Time-travel works because reducer purity + immutable state + reified intents make the whole history deterministic from a logged intent sequence — replay the intents, reproduce the bug. It's MVI's concrete debugging payoff."}]},{level:"senior",q:"Why is MVI especially popular in Kotlin Multiplatform?",a:[{t:"p",text:"MVI is arguably *more* popular in KMP than in pure Android, because a framework-free state machine (single state, reified intents, pure reducer) is the perfect *shared brain* — it lives entirely in `commonMain`, and each platform (Android Compose, iOS SwiftUI) just renders the state and sends intents. The strict, self-contained loop maps cleanly onto the 'share logic, keep native UI' KMP model."},{t:"list",items:["**A self-contained loop shares perfectly** — the State/Intent/reducer/effect loop has no platform dependencies, so it drops into `commonMain` and drives both platforms identically. Each platform observes the shared state and dispatches shared intents.","**MVIKotlin + Decompose is the flagship stack** — Decompose provides lifecycle-aware, navigable 'components' shared across Android/iOS/desktop/web; MVIKotlin provides the Store loop inside them. This is a mature, production KMP architecture.","**Sealed intents/states benefit from SKIE on iOS** — SKIE turns Kotlin sealed classes into exhaustive Swift enums, so the shared MVI state/intents are pleasant to consume from Swift (exhaustive `switch` instead of `is` checks).","**Uniform mental model** — one architecture (the reducer loop) works everywhere, so developers reason about the app the same way regardless of platform."]},{t:"p",text:"The deeper reason: MVI's constraints (framework-free logic, single state, explicit intents) are *exactly* what makes code shareable — the same discipline that makes it testable and predictable makes it portable. iOS consumes the shared state the same way it would a shared MVVM ViewModel's StateFlow (via SKIE/an adapter), and dispatches intents as plain function calls. So MVI's popularity in KMP is really MVI's framework-independence paying off doubly — for testability *and* for sharing."},{t:"note",text:"MVI's framework-free reducer loop is the perfect KMP 'shared brain' — MVIKotlin + Decompose is the flagship stack, and SKIE makes the sealed intents/states idiomatic in Swift. Framework-independence pays off for both testing and sharing."}]},{level:"junior",q:"What is the difference between an Intent, an Action/Change, and the State in MVI?",a:[{t:"p",text:"These three (sometimes four, with Effect) are the vocabulary of the MVI loop, and mixing them up is common. An **Intent** is what the *user wants* ('I clicked retry'). A **Change/Result/Action** is the internal 'thing that happened' that the reducer applies ('data loaded', 'loading started'). The **State** is the resulting immutable description of the screen."},{t:"list",items:["**Intent** — a *user intention*, emitted by the View. `RetryClicked`, `QueryChanged(text)`. It's the input to the system.","**Change / Result / Action** — an internal event the reducer consumes to produce new state. `Loading`, `DataLoaded(items)`, `Failed(error)`. Often produced by the side-effect layer after processing an intent.","**State** — the immutable output describing the whole screen. `State(isLoading = false, items = [...])`.","**(Effect)** — a one-shot side action (navigate, snackbar) delivered separately from state."]},{t:"code",title:"The full vocabulary in flow",code:`Intent.Retry
  -> (effect layer) emits Change.Loading -> reducer -> State(isLoading=true)
  -> (effect layer) does network, emits Change.DataLoaded(items)
  -> reducer -> State(isLoading=false, items=items)`},{t:"p",text:"Why the Intent/Change split exists: it keeps the reducer *pure*. The reducer only ever sees *Changes* (plain data describing what happened), never does the async work itself. The 'effect layer' (Orbit's `intent{}`, MVIKotlin's Executor) turns an Intent into async work and emits Changes as the work completes. Simple MVI often collapses Intent and Change into one type; strict MVI (and Redux/Elm) keeps them separate for reducer purity."},{t:"note",text:"Intent = user wants (input); Change/Result = what happened (reducer's input); State = the screen (output). The Intent→Change split keeps the reducer pure — async lives in the effect layer, the reducer only sees plain Changes."}]},{level:"senior",q:"Explain how MVI relates to Redux, Elm, and Flux, and why that lineage matters.",a:[{t:"p",text:"MVI is a direct descendant of a lineage of web architectures, and recognizing that isn't trivia — it tells you MVI's properties are *inherited theorems* (proven elsewhere), and where each concept comes from. The line runs Flux → Elm → Redux → Cycle.js → MVI."},{t:"list",items:["**Flux (Facebook)** — introduced *unidirectional dispatch*: actions flow one way through a dispatcher to stores. First to reject two-way binding.","**Elm** — formalized the pure `update : Msg -> Model -> Model` loop with *effects as data* (Commands). This is where reducer purity and 'effects are values the runtime executes' come from — and Elm *proved* the determinism/replay properties.","**Redux** — popularized *single store + pure reducers + middleware* in JS. The single-state-object and the middleware-for-side-effects slot come from here.","**Cycle.js** — framed UI as a *stream cycle* (the 'Intent' terminology and the reactive-loop framing).","**MVI (Hannes Dorfmann, ~2016)** — translated this whole stack to Android's RxJava/coroutines world."]},{t:"p",text:"Why it matters: the mapping is one-to-one — Intent = Action/Msg, State = Store/Model, Reducer = reducer/update, effect layer = middleware/Commands, render = view function. So MVI's guarantees (determinism, time-travel, replay) are *not* marketing — they're Elm's proven properties following from reducer purity. And knowing the middleware slot is where every family member handles side effects tells you *where effects belong* in any MVI library you meet. Recognizing a 'new' Android architecture as an instance of a well-understood family — including its known failure modes (boilerplate fatigue, giant stores, which the JS world hit first and solved with selectors/reselect) — is exactly the systems-level judgment senior interviews probe."},{t:"note",text:"Flux→Elm→Redux→Cycle→MVI, one-to-one: Intent=Action, State=Store, Reducer=update, effect layer=middleware. MVI's replay/time-travel are *Elm's proven theorems*, not marketing — and the JS ecosystem already mapped its failure modes and fixes."}]},{level:"junior",q:"How do you model a form with multiple input fields in MVI?",a:[{t:"p",text:"A form is a good MVI test case because it has many inputs and validation. You model the *whole form* as one state object (all fields + validation + submit status), each field edit as an intent, and validation as pure logic in the reducer. The View renders the single state and emits an intent per keystroke."},{t:"code",title:"A form in MVI",code:`data class SignupState(
    val email: String = "",
    val password: String = "",
    val emailError: String? = null,
    val isSubmitEnabled: Boolean = false,
    val isSubmitting: Boolean = false,
)

sealed interface SignupIntent {
    data class EmailChanged(val v: String) : SignupIntent
    data class PasswordChanged(val v: String) : SignupIntent
    data object Submit : SignupIntent
}

fun onIntent(i: SignupIntent) = when (i) {
    is SignupIntent.EmailChanged -> reduce {
        val err = if (i.v.contains("@")) null else "Invalid email"   // validation in reducer
        it.copy(email = i.v, emailError = err,
                isSubmitEnabled = err == null && it.password.length >= 8)
    }
    is SignupIntent.PasswordChanged -> reduce { it.copy(password = i.v,
        isSubmitEnabled = it.emailError == null && i.v.length >= 8) }
    SignupIntent.Submit -> submit()
}`},{t:"p",text:"The benefit: the *entire* form state (values, errors, whether submit is enabled) is one object derived by pure logic, so it's trivially testable ('given this email, emailError should be X and submit disabled') and always consistent — the submit button's enabled state can never disagree with the field validity because it's computed in the same reducer."},{t:"p",text:"The performance caveat: a single form state means every keystroke updates the whole state object, potentially recomposing every field. For a big form, split the state or use per-field `derivedStateOf`/stable slices so typing in one field doesn't recompose the others. Also, high-frequency text-field state is sometimes better kept as local UI state, entering the MVI loop debounced — running the full reduce cycle per keystroke can be overkill."},{t:"note",text:"Model the whole form as one state with validation in the reducer (submit-enabled can't disagree with field validity). Watch recomposition: split state or debounce input so every keystroke doesn't reduce+recompose the entire form."}]},{level:"senior",q:"How would you migrate an existing MVVM screen to MVI incrementally?",a:[{t:"p",text:"Migrating MVVM → MVI is usually *low-risk* because modern MVVM (single UiState + UDF) is already most of the way there — you're mainly *reifying inputs* (turning public functions into intent objects) and *centralizing mutation* (into a reducer). You can do it screen by screen without touching the data/domain layers."},{t:"list",items:["**Precondition check** — if the ViewModel already exposes a single `UiState` StateFlow and follows UDF, you're 80% done. If it exposes multiple streams, consolidate to one UiState first (a pure MVVM improvement).","**Reify the inputs** — replace the ViewModel's N public functions (`onQueryChange`, `onRetry`) with a single `onIntent(intent: Intent)` and a sealed `Intent` type. The View now sends intent objects through one entry point instead of calling named methods.","**Centralize mutation into a reducer** — replace the scattered `_state.update { ... }` calls with a single reducer function that all state changes flow through. Extract it as a pure `(state, change) -> state` if you want strict MVI (and testability of the reducer in isolation).","**Separate effects** — move one-shot events (navigation, snackbar) to a dedicated effect channel if not already.","**Adopt a library if desired** — Orbit is the gentlest (a DSL over the ViewModel you already have); MVIKotlin is stricter. But you don't *need* a library — hand-rolled MVI is just the discipline above."]},{t:"p",text:"The key judgment: migrate *selectively*, not wholesale. Move the genuinely complex, state-machine-like screens to MVI where the discipline pays; leave simple CRUD screens as MVVM-with-UDF where MVI would be ceremony. Because both share the same StateFlow/ViewModel foundation, the two can coexist during migration — there's no all-or-nothing rewrite. And convert the tests: MVVM's state-assertion tests mostly carry over (MVI is also state-based), plus you gain reducer unit tests."},{t:"note",text:"MVVM→MVI is mostly 'reify inputs (functions→intents) + centralize mutation (→reducer)' — low-risk since both share StateFlow/UDF. Migrate complex screens selectively; leave simple ones as MVVM. No wholesale rewrite needed."}]},{level:"senior",q:"How do you keep the reducer pure when you need randomness, timestamps, or IDs?",a:[{t:"p",text:"A pure reducer must produce the same output for the same input — so it can't call `System.currentTimeMillis()`, `Random`, or `UUID.randomUUID()` directly, because those return different values each call, breaking determinism (and time-travel replay). The rule is: *impure values enter the reducer as inputs (in the Change/Intent), never generated inside it*."},{t:"code",title:"Impure values come in as data, not generated inside",code:`// BAD: reducer generates a timestamp -> not deterministic, breaks replay
fun reduce(s: State, c: Change): State = when (c) {
    is Change.AddNote -> s.copy(notes = s.notes + Note(id = UUID.randomUUID(),  // impure!
                                                        createdAt = System.now())) // impure!
}

// GOOD: the effect layer generates them and passes them IN as part of the Change
// effect layer: emit Change.AddNote(id = uuid(), createdAt = clock.now(), text = ...)
fun reduce(s: State, c: Change): State = when (c) {
    is Change.AddNote -> s.copy(notes = s.notes + Note(c.id, c.createdAt, c.text))  // pure
}`},{t:"list",items:["**Generate impure values in the effect/middleware layer** — the layer that does async work (already impure) creates the id/timestamp/random value and includes it *in the Change* it emits.","**The reducer just uses the value** — it receives the id/timestamp as plain data and copies it into state, staying pure and deterministic.","**Inject clocks/random for testability** — the effect layer takes a `Clock` and random source as dependencies, so tests can supply deterministic ones."]},{t:"p",text:"This is the same pattern as 'where do side effects go if reducers are pure' — the reducer is a pure function of *values*, and *all* impurity (async, IO, clocks, randomness) lives in the effect layer *around* it, feeding results in as plain data. Keeping that boundary crisp is what preserves reducer purity and its downstream benefits (testability, determinism, replay)."},{t:"note",text:"Never generate timestamps/IDs/random *in* the reducer — the effect layer generates them and passes them into the Change as data. All impurity lives around the reducer, feeding it plain values, preserving purity and replay."}]}],wx={id:"mvi",title:"MVI",tagline:"Model – View – Intent: single immutable state, reified intents, pure reducers, side-effect handling, the library landscape, and when the discipline pays off.",tags:["Android","KMP","UDF","Reducer","Orbit","MVIKotlin"],content:gx,qa:yx},vx=[{heading:"The original Clean Architecture — circles and the Dependency Rule",blocks:[{t:"p",text:"**Clean Architecture** is Robert C. Martin's (Uncle Bob, 2012) synthesis of earlier layered ideas (Hexagonal/Ports & Adapters, Onion Architecture). Concentric circles: **Entities** (enterprise business rules) at the core, then **Use Cases** (application business rules), then **Interface Adapters** (presenters, controllers, gateways), then **Frameworks & Drivers** (UI, DB, network, devices) outermost."},{t:"p",text:"Everything hangs on one law — the **Dependency Rule**: *source-code dependencies may only point inward*. Inner circles know nothing about outer ones — not names, not types, not frameworks. The result: business rules that can be compiled, tested, and reasoned about with no UI, no database, no Android — and outer details (Room vs SQLDelight, Retrofit vs Ktor) that are swappable without touching the core."},{t:"list",items:["When the inner layer must *call outward* (a use case needs data), it defines an **interface (port)** that an outer layer implements (adapter) — that's **Dependency Inversion (the D in SOLID)** doing the load-bearing work.","Data crossing boundaries is converted at each boundary — inner layers never see outer-layer types (no Retrofit DTOs inside use cases).",'"Clean" is about **dependency direction**, not folder names — a project with domain/data/presentation folders where domain imports Retrofit is not Clean Architecture.']},{t:"note",text:'One-liner: "Clean Architecture = business logic at the center, frameworks at the edges, all dependencies pointing inward, with interfaces + dependency inversion wherever the flow of control must go outward."'}]},{heading:"The Android adaptation — three layers, and Google's pragmatic version",blocks:[{t:"p",text:"Android collapsed the circles into three layers. Know **both** the strict mapping and Google's official recommendation — and that they differ:"},{t:"list",items:["**Presentation (UI) layer** — Activities/Composables + ViewModels. Maps to Interface Adapters + Frameworks.","**Domain layer** — use cases + domain models + repository *interfaces*. Pure Kotlin, zero Android imports. Maps to Entities + Use Cases.","**Data layer** — repository *implementations* + data sources (Retrofit, Room, DataStore) + DTOs/entities + mappers. Outer circle."]},{t:"table",headers:["","Strict Clean (community)","Google's recommended architecture"],rows:[["Domain layer","mandatory core of the design","**optional** — 'add if complexity warrants'"],["Repository interface lives in","domain layer (dependency inversion: data implements domain's port)","data layer defines and exposes it"],["Use cases","every interaction goes through one","only when logic is complex or reused"],["Data flow","UI → UseCase → Repository","UI → Repository directly is fine"],["Philosophy","framework independence above all","pragmatism: SSOT, UDF, layered — without ceremony"]]},{t:"note",text:`High-signal interview move: explicitly name this divergence. "Strict Clean puts repository interfaces in domain and mandates use cases; Google's guide makes domain optional and lets ViewModels call repositories. I choose per project: shared-KMP or complex business rules → strict; CRUD app → Google-pragmatic."`}]},{heading:"Domain layer deep dive — entities, use cases, and when they're worth it",blocks:[{t:"h3",text:"Domain models (entities)"},{t:"list",items:["Plain immutable Kotlin data classes expressing business concepts — `Order`, `Money`, `Subscription` — with invariants enforced in construction (value classes for `UserId`, no raw strings for money).","No serialization annotations, no Room annotations, no framework anything — those belong to the layer-specific models (DTO/entity).","Business rules that belong to the concept live on it (`Order.canBeCancelled`), not in a ViewModel."]},{t:"h3",text:"Use cases (interactors)"},{t:"code",title:"Use case conventions",code:`// Naming: VerbNounUseCase; single public operator invoke
class GetPersonalizedFeedUseCase(
    private val feedRepository: FeedRepository,       // domain interfaces
    private val userRepository: UserRepository,
    private val defaultDispatcher: CoroutineDispatcher, // injected, main-safe
) {
    suspend operator fun invoke(): Result<Feed> = withContext(defaultDispatcher) {
        val user = userRepository.getUser()
        feedRepository.getFeed()
            .map { feed -> feed.rankFor(user.interests) } // the actual business value
    }
}

// Flow-returning variant for observation:
class ObserveCartTotalUseCase(
    private val cartRepository: CartRepository,
) {
    operator fun invoke(): Flow<Money> =
        cartRepository.observeItems()
            .map { items -> items.sumOf { it.price * it.quantity } }
}`},{t:"list",items:["**Earn their keep when**: logic combines multiple repositories, is reused by several ViewModels, or encodes real business policy (ranking, pricing, eligibility).","**Pure ceremony when**: `class GetUserUseCase(repo) { operator fun invoke(id) = repo.getUser(id) }` — a rename of the repository call. Teams mandating these for uniformity trade boilerplate for consistency; know the argument on both sides.","Use cases are **stateless** and reusable; they may call other use cases; they should be main-safe (own their `withContext`).","They do **not** know about UI state, navigation, or Android — a use case returning `UiState` is a layering bug."]}]},{heading:"Data layer deep dive — repositories, data sources, SSOT, offline-first",blocks:[{t:"list",items:["**Repository** = the public API of the data layer for one domain concept: hides *where* data comes from, exposes suspend functions + Flows of **domain models**, owns the merge strategy between sources.","**Data sources** = one class per origin (RemoteDataSource wrapping Retrofit/Ktor, LocalDataSource wrapping Room/SQLDelight DAO); repositories orchestrate them, never the reverse; data sources never know each other.","**Single Source of Truth**: pick one source (usually the DB) that the app *reads from*; network writes into it, UI observes it. Reads are then consistent, offline works, and the 'flash of stale then fresh' is handled uniformly.","**Offline-first**: reads always serve local; writes go local-first then sync (WorkManager, outbox pattern); conflict policy (last-write-wins vs merge) is a repository concern.","Repositories also own: in-memory caching, request de-duplication, pagination glue (Paging sources), and mapping DTO/entity → domain."]},{t:"code",title:"Repository implementing SSOT (network → DB → UI)",code:`class ArticleRepositoryImpl(
    private val remote: ArticleRemoteDataSource,
    private val local: ArticleLocalDataSource,
    private val ioDispatcher: CoroutineDispatcher,
) : ArticleRepository {

    // Reads: always from the local SSOT
    override fun observeArticles(): Flow<List<Article>> =
        local.observeAll().map { entities -> entities.map { it.toDomain() } }

    // Refresh: network writes INTO the SSOT; UI updates via the observation above
    override suspend fun refresh(): Result<Unit> = withContext(ioDispatcher) {
        runCatching {
            val dtos = remote.fetchArticles()
            local.replaceAll(dtos.map { it.toEntity() })
        }
    }
}`}]},{heading:"Model mapping — DTO vs entity vs domain vs UI model",blocks:[{t:"table",headers:["Model","Layer","Shaped by","Example concern"],rows:[["DTO","data (network)","the API contract",'`@SerialName("user_name")`, nullable everything'],["DB entity","data (local)","storage & queries","`@Entity`, indices, foreign keys"],["Domain model","domain","business meaning","non-null invariants, value classes, behavior"],["UI model / UiState","presentation","what the screen renders","formatted strings, resolved flags, stable keys"]]},{t:"list",items:["Mapping happens **at layer boundaries**: DTO→domain and entity→domain inside the data layer; domain→UI in the ViewModel (or a mapper it uses).","Why bother: a server field rename touches the DTO + one mapper — not forty composables. Nullability gets resolved *once*, at the boundary, so the domain stays honest.","Pragmatic seniority: for a tiny app, one model reused everywhere is a defensible YAGNI call — but say you'd split the moment API shape and UI needs diverge, or the DB schema needs to evolve independently.","Mappers are plain functions/extension functions (`fun ArticleDto.toDomain(): Article`) — trivially unit-tested, and the place where defaulting/validation of dirty server data lives."]}]},{heading:"A full vertical slice — every layer in one example",blocks:[{t:"code",title:"Domain layer (pure Kotlin — no Android, no libraries)",code:`// model
data class Article(val id: String, val title: String, val publishedAt: Instant)

// repository PORT — interface owned by domain (strict Clean)
interface ArticleRepository {
    fun observeArticles(): Flow<List<Article>>
    suspend fun refresh(): Result<Unit>
}

// use case
class ObserveSortedArticlesUseCase(
    private val repository: ArticleRepository,
) {
    operator fun invoke(): Flow<List<Article>> =
        repository.observeArticles()
            .map { articles -> articles.sortedByDescending { it.publishedAt } }
}`},{t:"code",title:"Data layer (implements the domain port)",code:`class ArticleRepositoryImpl(
    private val remote: ArticleRemoteDataSource,
    private val local: ArticleLocalDataSource,
    private val ioDispatcher: CoroutineDispatcher,
) : ArticleRepository { /* SSOT impl from the previous section */ }`},{t:"code",title:"Presentation layer",code:`class FeedViewModel(
    observeSortedArticles: ObserveSortedArticlesUseCase,
    private val refreshArticles: RefreshArticlesUseCase,
) : ViewModel() {

    val uiState: StateFlow<FeedUiState> = observeSortedArticles()
        .map { articles -> FeedUiState.Content(articles.map { it.toUiModel() }) }
        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), FeedUiState.Loading)

    fun onPullToRefresh() {
        viewModelScope.launch { refreshArticles() }
    }
}`},{t:"p",text:"Trace the Dependency Rule through it: presentation imports domain; data imports domain; **domain imports nothing** from either. Control flows UI→domain→data at runtime, but the *source dependency* from domain to data is inverted through the `ArticleRepository` interface — that inversion is the exam-question heart of Clean Architecture."}]},{heading:"Error handling across layers",blocks:[{t:"list",items:["**Data layer**: catch transport-level garbage (IOException, HTTP codes, serialization) at the boundary and translate to **typed domain errors** — `sealed interface DataError { NoConnection; NotFound; Unauthorized; Unknown }` — via `Result`/Either or error-emitting Flows. Never let Retrofit's `HttpException` leak upward.","**Domain layer**: business failures are *values*, not exceptions — `InsufficientFunds`, `CartExpired` as sealed results; exceptions are reserved for bugs.","**Presentation**: map typed errors to UX — retry affordance for connectivity, sign-in flow for auth, toast for the rest. The ViewModel `when`s over the sealed error, never string-matches messages.","**Flows**: remember `catch` only handles *upstream* exceptions, terminal failures kill the collection — repositories should catch inside and emit error values so long-lived observation streams survive individual failures.","**CancellationException must always be rethrown** — a blanket `runCatching` in every layer that swallows it silently breaks structured concurrency; use a helper that rethrows it."]}]},{heading:"Threading rules across layers",blocks:[{t:"list",items:["**Main-safety contract**: every suspend function in repositories/use cases must be safe to call from the main thread — the layer *doing* blocking work wraps itself (`withContext(ioDispatcher)`); callers never pre-emptively hop dispatchers.","**Inject dispatchers** everywhere (`ioDispatcher`, `defaultDispatcher` as constructor params or a DispatcherProvider) — testability + a single place to tune.","Room/Retrofit suspend calls are already main-safe (they manage their own threading) — wrapping them in `withContext(IO)` again is harmless but cargo-cult; know that nuance.","`Flow` work moves off-main with `flowOn` applied in the layer that owns the heavy operator, not by the collector.","CPU-heavy mapping of big lists → `defaultDispatcher`, IO → `ioDispatcher`; the UI layer stays on `Main.immediate` via `viewModelScope`."]}]},{heading:"SOLID mapped to Android Clean Architecture",blocks:[{t:"table",headers:["Principle","Where it shows up in this architecture"],rows:[["**S**ingle Responsibility","one use case = one business action; repository per concept; ViewModel per screen; the anti-God-class principle"],["**O**pen/Closed","sealed hierarchies for state/errors extend by adding variants; new data sources plug in behind the repository without touching consumers"],["**L**iskov Substitution","fake repositories in tests must honor the interface contract (same semantics for edge inputs) — if tests need to know which impl is behind the port, LSP is broken"],["**I**nterface Segregation","small per-concept repository interfaces instead of one `AppRepository` with 40 methods; ViewModels depend only on the slice they use"],["**D**ependency Inversion","the load-bearing one: domain defines repository ports, data implements them; high-level policy never depends on low-level detail"]]}]},{heading:"Clean Architecture in KMP",blocks:[{t:"list",items:["Clean's layering maps perfectly onto KMP: **domain + data in `commonMain`**, presentation shared (CMP ViewModels) or per-platform (SwiftUI adapters over shared state).","The domain layer's 'no framework imports' rule becomes *enforced by compilation*: `commonMain` literally cannot import Android classes — KMP is Clean Architecture's dependency rule with a compiler behind it.","Data layer in common code: **Ktor** (network), **SQLDelight/Room-KMP** (DB), **multiplatform DataStore** (prefs); platform specifics (file paths, drivers, engines) injected via `expect/actual` or DI.","The strict-Clean choice (repository interfaces in domain) pays off doubly here: platforms and tests substitute implementations without touching shared business logic.","Typical module shape: `:shared:domain`, `:shared:data`, `:shared:feature-x` consumed by `:androidApp` and the iOS framework — which is where Clean Architecture meets modularization."]}]},{heading:"Criticisms & pragmatism — the question that separates seniors",blocks:[{t:"list",items:["**Over-engineering small apps**: five modules, four model types and pass-through use cases for a two-screen app is architecture cosplay — the cost (navigation overhead reading code, mapper churn) is real and the payoff absent.","**Pass-through layers**: wrappers that only forward calls add nothing but stack frames; add a layer when it has *responsibility*, not for symmetry.","**Dogma vs delivery**: 'framework independence' is worth little if you'll never actually swap Room — the real, recurring payoff is **testability and team-scale boundaries**, so argue from those.","**Uncle Bob's version isn't gospel on mobile**: Google's guide deliberately deviates (optional domain, repo interfaces in data) — citing that shows you read primary sources, not blog cargo cult.","**When to invest**: multiple developers, KMP sharing, complex/regulated business rules, long lifespan → strict layering compounds. Hackathon/MVP → ViewModel + repository and ship."]},{t:"note",text:'Best closing sentence in a Clean Architecture interview: "Layers are a means; the ends are testable business logic, independent teams, and replaceable details. I add exactly as much architecture as buys those — and no more."'}]}],bx=[{level:"junior",q:"What is Clean Architecture and what is the Dependency Rule?",a:[{t:"p",text:"Clean Architecture (Uncle Bob, 2012) organizes code in concentric layers — business entities and use cases at the center, interface adapters around them, frameworks (UI, DB, network) at the edge. The **Dependency Rule** is its single law: source-code dependencies may only point **inward** — inner layers never know about outer ones. On Android that means the domain layer has zero Android/library imports, and things like Retrofit or Room are outer details the core never names. The payoff: business logic that's testable without an emulator and details you can swap without touching the core."}]},{level:"junior",q:"Describe the three layers of a typical Android Clean Architecture app.",a:[{t:"list",items:["**Presentation/UI**: Activities/Composables render state; ViewModels hold UI state and call the layer below.","**Domain**: pure Kotlin — domain models, use cases, and (in strict Clean) repository *interfaces*. No Android imports.","**Data**: repository *implementations*, remote/local data sources (Retrofit/Ktor, Room/SQLDelight), DTOs and DB entities, and mappers to domain models."]},{t:"p",text:"Dependencies: presentation → domain ← data. Both outer layers depend on domain; domain depends on nothing — the data layer's arrow points inward because it *implements* interfaces the domain defines."}]},{level:"junior",q:"What is a use case (interactor) and when should you create one?",a:[{t:"p",text:"A class encapsulating **one business action** — `GetPersonalizedFeedUseCase` — conventionally with a single `operator fun invoke()`, stateless, main-safe, depending on repository interfaces. Create one when logic **combines multiple repositories**, is **reused across ViewModels**, or encodes **real business policy** (ranking, eligibility, pricing). A use case that only forwards `repo.getUser(id)` adds nothing but a file — some teams still mandate them for uniformity and future-proofing; be able to argue both sides and note Google's guidance makes the whole domain layer optional."}]},{level:"junior",q:"What is the Repository pattern and what problems does it solve?",a:[{t:"p",text:"A repository is the single entry point for one kind of data — it hides *where* data lives (network, DB, cache), decides the merge/refresh strategy, and exposes clean domain types via suspend functions and Flows. Problems solved: ViewModels no longer know about Retrofit/Room (swap or add sources freely), caching and offline logic exist **once** instead of per-screen, tests substitute an in-memory fake behind the interface, and the app gets a **single source of truth** so two screens can't disagree about the same data."}]},{level:"junior",q:"Why does the domain layer have no Android dependencies?",a:[{t:"p",text:"Three concrete wins. **Testability**: pure Kotlin runs as plain JVM unit tests — fast, no Robolectric/emulator. **Portability**: an Android-free domain drops into KMP `commonMain` unchanged — the iOS app runs the same business rules. **Stability**: Android APIs, UI toolkits and libraries churn constantly; business rules shouldn't have to change when the framework does. The Dependency Rule is what protects all three — one `import android.*` in domain and you've lost them."}]},{level:"junior",q:"What is the difference between a DTO, a database entity, and a domain model? Why not one class for all?",a:[{t:"p",text:"A **DTO** mirrors the API contract (serialization names, everything nullable because servers lie); a **DB entity** mirrors storage (Room annotations, indices, foreign keys); a **domain model** mirrors business meaning (non-null invariants, value types, behavior). One class serving all three masters means a server field rename touches your database schema and your composables, Room annotations leak into business code, and nullability from the network infects every layer. Mapping at boundaries (`ArticleDto.toDomain()`) contains each concern. Honest caveat worth adding: in a tiny app, one model is a defensible YAGNI shortcut — split when shapes start diverging."}]},{level:"junior",q:"What does 'Single Source of Truth' mean in the data layer?",a:[{t:"p",text:"One designated place — usually the local database — is what the app *reads*; everything else feeds it. The network never returns data straight to the UI: a refresh **writes into the DB**, and the UI updates because it *observes* the DB (Room/SQLDelight Flows). Benefits: every screen shows consistent data, offline reads work automatically, and there's exactly one merge point for remote and local changes. This is the pattern behind 'observe local, refresh remote' repositories."}]},{level:"junior",q:"How does dependency injection relate to Clean Architecture?",a:[{t:"p",text:"Clean Architecture *requires* construction to happen somewhere outside the layers: the ViewModel needs a use case, the use case needs an `ArticleRepository`, and the actual `ArticleRepositoryImpl` lives in a layer the domain must not know. A DI container (Hilt/Koin) is that assembly point — the composition root binds interface → implementation (`@Binds ArticleRepositoryImpl into ArticleRepository`), so every layer receives its dependencies as abstractions through constructors. Side benefits: swapping fakes in tests is a binding change, and scoping (singleton repository, screen-scoped ViewModel) is declared in one place. DI isn't *part* of Clean Architecture — it's the mechanism that makes the Dependency Rule practical."}]},{level:"senior",q:"Where should repository interfaces live — domain or data layer? Argue it properly.",a:[{t:"p",text:"**Strict Clean: in domain.** The interface is the *port* the domain defines for what it needs; data implements it. This is dependency inversion doing its job — domain compiles standalone, use cases are testable against the port, and in KMP/multi-module setups the domain module genuinely doesn't depend on the data module. **Google's guide: in data.** Their reasoning: most apps don't swap implementations, the domain layer is optional in their architecture, and one interface + one impl in different modules is ceremony for typical apps — so the data layer just exposes its API."},{t:"p",text:"My resolution: it's decided by **who needs the seam**. If a domain layer exists and use cases must be testable/portable without data-layer code (KMP especially), the interface belongs to domain — that's the whole point of the port. If ViewModels call repositories directly and there's no domain module, an interface in data (or even a concrete class with a fake for tests) is honest. The failure mode to name: interface-in-domain but *shaped exactly like the impl* (leaking DTO-ish types, one method per endpoint) — that's inversion theater; the port should be expressed in domain vocabulary."}]},{level:"senior",q:"Your team writes a pass-through use case for every repository method 'for consistency'. Defend or attack this policy.",a:[{t:"p",text:"**Attack**: pass-throughs violate the reason use cases exist — they add a class, a file, DI wiring and a stack frame while encoding zero business knowledge. Cost is real: navigation overhead reading code, slower onboarding ('is there logic in here? no, never'), and diluted signal — when every call has a use case, the ones containing actual policy don't stand out. YAGNI says add the class when logic appears; refactoring a direct repository call into a use case later is a five-minute, tooling-assisted change."},{t:"p",text:"**Steelman before concluding**: uniformity has value at scale — 'ViewModels never touch repositories' is a rule a lint check can enforce without judgment calls, it pre-creates the seam so adding cross-cutting logic (analytics, authorization) later touches one layer, and juniors can't pick the wrong dependency. Verdict I'd give: for a large org with codegen/lint enforcement, mandatory use cases are a defensible *convention* tax; for a small team, rule-of-three pragmatism wins. What's non-negotiable either way is the direction: when a use case exists, the ViewModel must not *also* bypass it."}]},{level:"senior",q:"Design the error-handling strategy across all three layers. Where are exceptions caught, and what crosses each boundary?",a:[{t:"list",items:["**Data**: catch transport garbage at the source (IOException, HTTP codes, JSON errors) and translate into a **typed error vocabulary** — `sealed interface DataError` (NoConnection, Unauthorized, NotFound, Unknown(cause)). Nothing library-specific crosses upward: `HttpException` escaping the data layer is a layering bug.","**Domain**: business failures are **values in return types** (`Result<Order, OrderError>` / sealed results) — `InsufficientFunds` is an expected outcome, not an exception. Use cases may *translate* data errors into business terms (NotFound → `ProductDiscontinued`).","**Presentation**: exhaustive `when` over the sealed error → UX decision (retry UI for connectivity, auth flow for Unauthorized, generic toast + log for Unknown). Never string-match messages.","**Two global rules**: `CancellationException` is always rethrown, at every layer — a blanket `runCatching` that swallows it breaks structured concurrency (use a `runSuspendCatching` helper); and long-lived observation Flows must catch *inside* (emit error values) because a terminal exception kills the collection permanently."]},{t:"p",text:"The design principle to state: **each layer speaks errors in its own vocabulary**, translated at the same boundaries where models are mapped — errors *are* part of the model."}]},{level:"senior",q:"What does 'main-safe' mean and which layer owns dispatcher switching? What are the common mistakes?",a:[{t:"p",text:"Main-safe: any suspend function can be called from the main thread without blocking it — the function itself moves work where it belongs. **The layer doing the blocking work owns the switch**: a repository doing heavy mapping wraps in `withContext(ioDispatcher/defaultDispatcher)`; use cases wrap their own CPU work; the ViewModel then simply launches in `viewModelScope` with no dispatcher juggling."},{t:"list",items:["Mistake 1 — **caller-side hopping**: ViewModels wrapping every call in `withContext(IO)` 'just in case'; scatters threading policy everywhere and hides which layer actually blocks.","Mistake 2 — **hardcoding `Dispatchers.IO`**: kills virtual-time testing; inject dispatchers (constructor params / DispatcherProvider).","Mistake 3 — cargo-cult wrapping of **Room/Retrofit suspend calls** — they're already main-safe; the extra `withContext` is noise (harmless, but shows shallow understanding when probed).","Mistake 4 — collector-side `flowOn` confusion: `flowOn` affects *upstream* only; the layer owning the heavy operator applies it, not the UI."]}]},{level:"senior",q:"How do you structure Clean Architecture in a KMP project, and what changes versus Android-only?",a:[{t:"list",items:["**Domain + data move to `commonMain`**: models, use cases, repository ports and implementations shared; Ktor/SQLDelight (or Room KMP) as common data sources, with platform bits (DB driver, HTTP engine, file paths) injected via `expect/actual` or DI.","**The Dependency Rule becomes compiler-enforced**: `commonMain` physically cannot import Android — KMP turns Clean's discipline from convention into a build error, which is the strongest practical argument for strict layering there.","**Presentation splits by strategy**: Compose Multiplatform → shared ViewModels (androidx KMP ViewModel) all the way; native SwiftUI → shared state holders bridged via SKIE/adapters, with thin platform view layers.","**Strict-Clean choices start paying rent**: ports-in-domain, injected dispatchers, typed errors — all the things that felt ceremonial on Android-only become load-bearing when two platforms and their tests consume the same core.","What changes in judgment: the 'is a domain layer worth it?' question mostly disappears — sharing *is* the payoff; the new hard questions are where presentation logic lives (shared vs per-platform) and keeping the iOS bridge thin."]}]},{level:"senior",q:"How does Clean Architecture interact with Jetpack Paging, WorkManager, and other framework libraries that want to cross layers?",a:[{t:"p",text:"This is the honest pain point — several Jetpack libraries are architecturally invasive, and the senior answer is knowing where to bend. **Paging**: `PagingData`/`Flow<PagingData<T>>` is designed to flow from data layer to UI *as a type* — purists wrap it, but fighting it costs the library's main value (state handling, invalidation); the pragmatic standard is treating Paging types as an allowed cross-layer vocabulary while keeping the *items* as domain/UI models via `map`. **WorkManager**: workers are entry points like Activities — they should be thin, immediately delegating to injected use cases/repositories (HiltWorker), never containing business logic. **Room/DataStore observation**: expose as Flows of domain types at the repository boundary — that one's easy."},{t:"p",text:"The generalizable principle: framework types may act as **plumbing across layers** when a library's core value depends on it, but **decisions** stay in your layers, and the framework type never becomes a parameter of business logic. State it as a trade-off you make knowingly, not a rule you broke."}]},{level:"senior",q:"A pull-to-refresh must update data shown on three different screens simultaneously. Walk through how your architecture handles it.",a:[{t:"p",text:"This is the SSOT pattern's showcase. All three screens' ViewModels observe the same repository stream (`observeArticles(): Flow<List<Article>>`), which reads from the database. The refresh — triggered from any screen — calls `repository.refresh()`, which fetches from network and **writes into the database**. Room/SQLDelight invalidation re-emits the query, every observing Flow fires, and all three screens update in the same frame — no screen-to-screen communication, no event bus, no 'notify others' code at all."},{t:"list",items:["Loading/error state for the *gesture* is per-screen (the initiating ViewModel tracks its own `isRefreshing` around the suspend call) — shared data, local UI state: an important separation to name.","In-flight de-duplication (two screens refreshing at once) is a repository concern — a mutex or shared in-flight job, solved once.","Contrast the alternatives interviewers are fishing for you to reject: shared ViewModels stretched across unrelated screens, or broadcast/event buses — both reinvent SSOT badly."]}]},{level:"senior",q:"Critique Clean Architecture: when is it the wrong choice, and what do its critics get right?",a:[{t:"list",items:["**The ceremony critique is correct in the small**: for a 2-screen app, four model types, mandatory use cases and five modules are cost without benefit — you're maintaining translation machinery for translations that never vary.","**'Swap the framework' is mostly a myth**: nobody replaces Room with realm on a whim; arguing Clean from swappability is weak. The *real* recurring payoffs are unit-testable logic, parallel team workstreams, and KMP portability — argue from those.","**Indirection has a comprehension price**: following one button tap through VM → use case → port → impl → source is six files; without strong naming conventions this actively slows debugging.","**Dogma drift**: teams enforcing pattern compliance in review while shipping slowly have inverted means and ends; Google's own guide (optional domain layer) is the institutional acknowledgment.","**When I'd skip it**: prototypes/MVPs, single-developer utility apps, heavily server-driven UI where the client has little logic to protect."]},{t:"p",text:"Then land it: the judgment skill is *scaling architecture to stakes* — start with ViewModel + repository + SSOT (cheap, huge payoff), add domain/use cases when business logic or team size demands, add strict ports when KMP or module boundaries appear. Architecture is bought in increments, not installed as a religion."}]},{level:"senior",q:"Map each SOLID principle to a concrete decision in an Android Clean Architecture codebase.",a:[{t:"list",items:["**S** — one use case per business action; repository per domain concept; screen-scoped ViewModels. Violation smell: a 'UserManager' doing auth + profile + settings.","**O** — sealed error/state hierarchies grow by adding variants (compiler finds every `when` to update); new data source slots in behind the repository with zero consumer changes.","**L** — the test fake for `ArticleRepository` must honor the port's contract (same behavior for empty results, same error types) — if production code needs `is RoomArticleRepository` checks, substitution is broken.","**I** — per-concept repository interfaces, not a 40-method `AppRepository`; a ViewModel needing read-only data depends on an observe-only interface if you split reads/writes.","**D** — the architecture's spine: domain defines `ArticleRepository`, data implements it, DI binds them; high-level policy (use cases) never names low-level detail (Retrofit)."]},{t:"p",text:"Strong close: note that the Dependency Rule *is* DIP applied at architectural scale, and SRP at file level is what the layer boundaries are at system level — SOLID isn't a checklist beside the architecture, it's what the architecture is made of."}]},{level:"junior",q:"Who created Clean Architecture and what are the concentric circles?",a:[{t:"p",text:"Clean Architecture was described by Robert C. Martin ('Uncle Bob') in 2012 as a synthesis of earlier layered ideas — Hexagonal (Ports & Adapters), Onion Architecture, and others. It's usually drawn as concentric circles, with the most stable, abstract code at the center and the most volatile, concrete code (frameworks, UI, DB) at the edges."},{t:"list",items:["**Entities (innermost)** — enterprise-wide business rules and core domain objects. The most stable, least likely to change.","**Use Cases** — application-specific business rules (the operations your app performs).","**Interface Adapters** — presenters, controllers, gateways that convert data between the use cases and the outer world.","**Frameworks & Drivers (outermost)** — the UI, database, network, Android SDK — the volatile details."]},{t:"p",text:"The whole thing hangs on one rule — the **Dependency Rule**: source-code dependencies may only point *inward*. Inner circles know nothing about outer ones (not their names, not their types, not their frameworks). So the domain has no idea Retrofit or Room or Android even exist. On Android these circles collapse to three practical layers (domain, data, presentation), but the principle is the same: business logic at the center, frameworks at the edges, dependencies pointing in."},{t:"note",text:"Clean Architecture = Uncle Bob's synthesis of Hexagonal/Onion; concentric circles (Entities → Use Cases → Adapters → Frameworks) governed by one law: dependencies point *inward*, so the center never knows about frameworks."}]},{level:"junior",q:"What belongs in the Domain layer specifically?",a:[{t:"p",text:"The Domain layer is the core of the app — the part that would be identical regardless of what UI framework, database, or network library you use. It's pure Kotlin with *no Android imports*, containing your business concepts and rules. It's the innermost, most stable layer."},{t:"list",items:["**Domain models (entities)** — immutable business objects with invariants (`Order`, `Money`, `Subscription`), often using value classes for type safety (`UserId` not raw `String`).","**Use cases (interactors)** — one business operation each (`GetPersonalizedFeedUseCase`), encoding real business policy.","**Repository interfaces (in strict Clean)** — the *ports* the domain defines for what it needs; the data layer implements them.","**Business rules and logic** — validation, calculations, eligibility rules — expressed in framework-free code."]},{t:"code",title:"Domain layer — pure Kotlin, no framework",code:`// model with an invariant
data class Order(val id: OrderId, val items: List<Item>) {
    val total: Money = items.sumOf { it.price }        // business rule on the entity
    fun canBeCancelled(): Boolean = items.all { !it.shipped }
}
// repository PORT — the domain declares what it needs
interface OrderRepository {
    suspend fun getOrder(id: OrderId): Order
}
// use case — one business operation
class CancelOrderUseCase(private val repo: OrderRepository) {
    suspend operator fun invoke(id: OrderId): Result<Unit> {
        val order = repo.getOrder(id)
        return if (order.canBeCancelled()) repo.cancel(id) else Result.failure(...)
    }
}`},{t:"p",text:"Why it's framework-free: this gives you three things — it's unit-testable on the JVM (no device), it's stable (Android APIs churn, business rules don't), and it's *portable* (it drops straight into KMP `commonMain`). The common mistake is letting Android or library types leak in (`Context`, a Retrofit DTO, a Room `@Entity`) — the moment that happens, the domain is no longer the stable, portable core."},{t:"note",text:"The Domain layer is 'the part identical if you rewrote the entire UI and swapped the database' — pure Kotlin, no Android. Any framework import in it is a design bug."}]},{level:"junior",q:"What is the Data layer's responsibility?",a:[{t:"p",text:"The Data layer is where the app actually gets and stores data — it's the outer layer that deals with the concrete details (network, database, files) that the domain deliberately avoids. It implements the repository interfaces the domain declares, hiding *where* data comes from behind a clean API."},{t:"list",items:["**Repository implementations** — implement the domain's repository interfaces, deciding between network/cache/DB and exposing domain models.","**Data sources** — one class per origin: a `RemoteDataSource` wrapping Retrofit/Ktor, a `LocalDataSource` wrapping Room/DataStore.","**DTOs and entities** — the network models (DTOs) and database models (entities), shaped by the API and storage respectively.","**Mappers** — convert DTO/entity ↔ domain model at the boundary.","**The Single Source of Truth** — usually a local DB that the app reads from; network writes into it."]},{t:"code",title:"A repository (data layer) implementing a domain port",code:`class OrderRepositoryImpl(
    private val api: OrderApi,       // network detail
    private val dao: OrderDao,        // database detail
    private val io: CoroutineDispatcher,
) : OrderRepository {                  // implements the DOMAIN's interface
    override fun observeOrders(): Flow<List<Order>> =
        dao.observeAll().map { entities -> entities.map { it.toDomain() } }  // map at boundary
    override suspend fun refresh() = withContext(io) {    // main-safe
        dao.replaceAll(api.getOrders().map { it.toEntity() })  // network -> DB (SSOT)
    }
}`},{t:"p",text:"The key discipline: the data layer depends *inward* on the domain (it implements the domain's interfaces and returns domain models), but the domain depends on *nothing* from the data layer. The data layer absorbs all the volatile detail — Retrofit, Room, JSON, caching strategy — so the rest of the app never sees it. That's what makes the details swappable and the domain stable."},{t:"note",text:"The Data layer implements the domain's repository ports and absorbs all the volatile detail (network, DB, mapping, caching, SSOT). It depends inward on the domain; the domain never depends on it."}]},{level:"junior",q:"What is the Presentation layer in Clean Architecture?",a:[{t:"p",text:"The Presentation layer is the outer layer that handles the UI — it's where the user-facing screens and the logic that drives them live. In modern Android it's the Compose/View UI plus the ViewModels. It depends inward on the domain (calling use cases or repositories) and turns domain data into something ready to render."},{t:"list",items:["**UI** — Composables or Activities/Fragments that render state and forward user events. As dumb as possible.","**ViewModels** — hold UI state, call use cases/repositories, and map domain models into UI-ready state (formatted strings, resolved flags). Framework-free-ish (no View references).","**UI models / UiState** — the presentation-shaped data the UI renders (formatted, with stable keys), distinct from domain models."]},{t:"code",title:"Presentation layer — ViewModel driving UI from the domain",code:`class FeedViewModel(
    observeFeed: ObserveFeedUseCase,      // depends INWARD on domain
) : ViewModel() {
    val uiState: StateFlow<FeedUiState> = observeFeed()
        .map { articles -> FeedUiState.Content(articles.map { it.toUiModel() }) }  // domain -> UI
        .stateIn(viewModelScope, WhileSubscribed(5000), FeedUiState.Loading)
}`},{t:"p",text:"The boundary rule: the presentation layer knows the domain (it calls use cases), but the domain knows *nothing* about presentation — a use case returning a `UiState` would be a layering violation (the domain would depend outward on presentation concerns). The ViewModel is where domain→UI mapping happens, so the UI receives render-ready data and the domain stays presentation-agnostic. In practice this is where Clean Architecture and MVVM meet — the ViewModel is the presentation layer's core."},{t:"note",text:"Presentation = UI + ViewModels; it depends inward on the domain and maps domain models → UI state. A use case returning UiState is a layering bug — the domain must stay presentation-agnostic."}]},{level:"junior",q:"How does data flow from the network to the UI through the layers?",a:[{t:"p",text:"Data flows *inward* through the layers on the way in, and the dependencies point inward too — but it's worth tracing concretely because it shows how the mapping and boundaries work in practice. The network's raw data is transformed at each boundary until the UI gets exactly what it needs."},{t:"code",title:"Network → UI, with mapping at each boundary",code:`// 1. DATA layer: network returns a DTO (shaped by the API)
val dto: UserDto = api.getUser(id)          // { "user_name": "...", nullable fields }

// 2. DATA layer: map DTO -> domain model (resolve nullability, clean names)
val user: User = dto.toDomain()             // non-null, business-shaped

// 3. DATA layer: repository returns the DOMAIN model (not the DTO)
//    (optionally cached in Room as an entity first — the SSOT)

// 4. DOMAIN layer: a use case may apply business logic
val ranked: Feed = rankFeedUseCase(user)    // pure business rule

// 5. PRESENTATION: ViewModel maps domain -> UI model (formatted for display)
val uiState = FeedUiState.Content(ranked.items.map { it.toUiModel() })

// 6. UI: renders the UI model`},{t:"list",items:["**DTO → Domain** (in the data layer) — nullability resolved, API names normalized, mapped to business-shaped models.","**Domain → UI model** (in the ViewModel) — formatted for display (strings, resolved colors, stable keys).","**Each layer sees only its own models** — the UI never sees a `UserDto`; the domain never sees a UI model."]},{t:"p",text:"The reason for the mapping at each boundary is *isolation of change*: a server field rename touches only the DTO and one mapper, not the whole app; a UI formatting change touches only the presentation mapping, not the domain. Dependencies point inward (presentation→domain←data), but *data* flows through with transformations at each boundary so every layer works with models shaped for its concerns."},{t:"note",text:"Trace it as DTO → domain (data layer) → UI model (ViewModel), mapping at each boundary. The mapping isolates change: a server rename hits one mapper, not the whole app."}]},{level:"junior",q:"What is dependency inversion and how does Clean Architecture use it?",a:[{t:"p",text:"Dependency inversion (the 'D' in SOLID) says high-level modules should depend on *abstractions*, not on low-level concrete details. Normally, high-level code (business logic) would depend on low-level code (a database) — but that couples your important logic to a volatile detail. Inversion flips it: both depend on an abstraction (an interface), so the detail is swappable and the logic is stable."},{t:"code",title:"Inversion via an interface the domain owns",code:`// DOMAIN (high-level) defines and depends on the ABSTRACTION:
interface UserRepository { suspend fun getUser(id: String): User }
class GetUserUseCase(private val repo: UserRepository)   // depends on interface, not impl

// DATA (low-level) implements the abstraction:
class UserRepositoryImpl(private val api: Api) : UserRepository { ... }

// DI wires interface -> impl at the outermost layer.
// The domain NEVER names UserRepositoryImpl or Api.`},{t:"p",text:"Clean Architecture *is* dependency inversion applied at architectural scale. The Dependency Rule ('dependencies point inward') is literally DIP: the inner domain defines repository *ports* (interfaces), the outer data layer provides *adapters* (implementations), and the dependency arrow points from the concrete implementation *toward* the abstraction — inverted from the naive 'business logic uses the database' direction. This is what makes the whole app substitutable and testable: the domain depends on an interface, so tests inject a fake and production injects the real thing."},{t:"p",text:"The concrete payoff: because `GetUserUseCase` depends on the `UserRepository` *interface*, you can (a) unit-test it with a `FakeUserRepository`, (b) swap the data source (network → local → different backend) without touching the use case, and (c) keep the domain compilable without the data layer even existing (crucial in KMP). Dependency inversion is the *mechanism*; Clean Architecture's layering is the *structure* it produces."},{t:"note",text:"Clean Architecture is dependency inversion at architectural scale: the domain defines repository interfaces (ports), the data layer implements them (adapters), so the dependency arrow points inward toward the abstraction — making the app testable and details swappable."}]},{level:"junior",q:"Show a complete vertical slice through all three layers.",a:[{t:"p",text:"A vertical slice — one feature through domain, data, and presentation — is the clearest way to see Clean Architecture in action and how the Dependency Rule holds. Here's a 'load articles' feature."},{t:"code",title:"Domain (pure Kotlin, defines the port)",code:`data class Article(val id: String, val title: String, val publishedAt: Instant)

interface ArticleRepository {                        // PORT owned by domain
    fun observeArticles(): Flow<List<Article>>
    suspend fun refresh(): Result<Unit>
}
class ObserveSortedArticlesUseCase(private val repo: ArticleRepository) {
    operator fun invoke(): Flow<List<Article>> =
        repo.observeArticles().map { it.sortedByDescending { a -> a.publishedAt } }
}`},{t:"code",title:"Data (implements the port; absorbs Retrofit/Room)",code:`class ArticleRepositoryImpl(
    private val api: ArticleApi, private val dao: ArticleDao, private val io: CoroutineDispatcher,
) : ArticleRepository {
    override fun observeArticles() = dao.observeAll().map { it.map(Entity::toDomain) }
    override suspend fun refresh() = withContext(io) {
        runCatching { dao.replaceAll(api.getArticles().map { it.toEntity() }) }
    }
}`},{t:"code",title:"Presentation (depends inward on the domain)",code:`class FeedViewModel(observe: ObserveSortedArticlesUseCase, private val refresh: RefreshUseCase)
    : ViewModel() {
    val uiState = observe().map { FeedUiState.Content(it.map(Article::toUiModel)) }
        .stateIn(viewModelScope, WhileSubscribed(5000), FeedUiState.Loading)
    fun onRefresh() = viewModelScope.launch { refresh() }
}`},{t:"p",text:"Trace the Dependency Rule: presentation imports domain; data imports domain; **domain imports nothing** from either. At runtime control flows UI→domain→data, but the *source dependency* from data to domain is inverted through the `ArticleRepository` interface — the data layer depends on the domain's abstraction, not vice versa. DI (Hilt/Koin) wires `ArticleRepositoryImpl` to `ArticleRepository` at the outermost layer, which is the only place that knows all the concrete types."},{t:"note",text:"In a vertical slice, both presentation and data point *inward* to the domain, and the domain points nowhere. The inverted data→domain dependency (via the repository interface) is the heart of Clean Architecture."}]},{level:"senior",q:"Are Clean Architecture and MVVM competing or complementary?",a:[{t:"p",text:"They're complementary — they operate at different scopes. MVVM is a *presentation-layer* pattern (how the UI, ViewModel, and state relate). Clean Architecture is a *whole-app layering* strategy (domain, data, presentation with dependencies pointing inward). MVVM describes the *inside* of Clean Architecture's presentation layer."},{t:"list",items:["**Different scopes** — MVVM answers 'how does the UI get its state?' (ViewModel exposes state, View observes). Clean answers 'how is the whole app layered?' (domain at center, frameworks at edges).","**They compose** — a typical modern Android app is *both*: Clean Architecture's three layers, with MVVM inside the presentation layer (the ViewModel calls use cases in the domain, which call repositories in the data layer).","**Neither replaces the other** — you don't choose MVVM *or* Clean; you use MVVM as the presentation pattern within a Clean-layered app."]},{t:"code",title:"MVVM living inside Clean's presentation layer",code:`// PRESENTATION layer (MVVM):
class FeedViewModel(private val getFeed: GetFeedUseCase) : ViewModel() {  // MVVM VM
    val uiState = getFeed()...   // calls INTO the domain layer (Clean)
}
// DOMAIN layer: GetFeedUseCase -> FeedRepository (interface)
// DATA layer: FeedRepositoryImpl`},{t:"p",text:"The nuance worth adding: you can do MVVM *without* full Clean Architecture (a ViewModel calling a repository directly, no separate domain layer — Google's recommended architecture makes the domain optional). So MVVM is the smaller, always-useful pattern; Clean Architecture is the larger layering you *add* when the app's complexity justifies a domain layer. They're a small pattern and a big structure that fit together, not alternatives."},{t:"note",text:"MVVM is the presentation-layer pattern *inside* Clean Architecture's larger app layering — complementary, not competing. You can do MVVM without full Clean (domain optional), but not vice versa."}]},{level:"senior",q:"Is the domain layer always necessary? When can you skip it?",a:[{t:"p",text:"No — the domain layer (use cases + domain models + repository interfaces) is *optional*, and this is where strict Clean Architecture and Google's recommended architecture openly diverge. Google's guidance makes the domain layer optional: 'add it if complexity warrants'. So the answer is 'add the domain layer when it earns its keep, skip it when it's ceremony'."},{t:"list",items:["**Skip the domain layer when** — the app is simple CRUD: the ViewModel can call the repository directly, and use cases would just forward calls (pass-through use cases that add a file and a stack frame but no logic). A domain layer here is ceremony.","**Add the domain layer when** — business logic is *complex* (ranking, pricing, eligibility, multi-step rules), logic is *reused across multiple ViewModels* (so it needs a shared home), you're doing *KMP* (the domain must be framework-free to share, so a clean domain layer pays off doubly), or the app is large/long-lived and you want the extra boundary."]},{t:"code",title:"When a use case is pure ceremony vs when it earns its keep",code:`// CEREMONY: forwards to the repo, adds nothing -> skip it, call the repo directly
class GetUserUseCase(private val repo: Repo) { operator fun invoke(id: String) = repo.getUser(id) }

// EARNS IT: real business logic combining sources -> keep it
class GetPersonalizedFeedUseCase(private val feedRepo: FeedRepo, private val userRepo: UserRepo) {
    suspend operator fun invoke(): Feed {
        val user = userRepo.getUser()
        return feedRepo.getFeed().rankFor(user.interests)   // actual policy
    }
}`},{t:"p",text:"The judgment is per-project, and stating that shows maturity: strict Clean insists on always having the domain layer (and use cases for every interaction); Google's guidance is pragmatic (domain optional, ViewModels can call repositories). I'd add the domain layer when there's genuine business logic to house or KMP sharing to enable, and skip it for simple CRUD — rather than dogmatically layering everything or nothing."},{t:"note",text:"The domain layer is *optional* (Google's guidance) — add it for complex/reused business logic or KMP sharing; skip it for simple CRUD where use cases would just forward calls. Judgment per project, not dogma."}]},{level:"senior",q:"What are the signs that someone has applied Clean Architecture wrong (over-engineered it)?",a:[{t:"p",text:"Clean Architecture over-engineering happens when the *ceremony* is applied without the *benefit* — layers, abstractions, and mappings added for their own sake on an app that doesn't need them. The costs (indirection, boilerplate, slower navigation) are real, so misapplied Clean Architecture is genuinely worse than a simpler design."},{t:"list",items:["**Pass-through use cases everywhere** — a use case for every repository method that just forwards the call, adding files and stack frames but zero logic. Ceremony, not architecture.","**Four model types for trivial data** — DTO → entity → domain → UI model with identical fields and pure-copy mappers, for a two-field object. Mapping machinery for mappings that never vary.","**Interfaces with exactly one implementation, never faked** — a repository interface + impl where the interface exists 'for Clean Architecture' but is never substituted or tested against. Abstraction theater.","**Five modules for a two-screen app** — modularization overhead (navigation, build config) with no team-size or build-speed payoff.","**Debugging requires opening six files** — following one button tap through VM → use case → interface → impl → data source → mapper, when a direct repository call would do.","**Dogmatic reviews** — enforcing layer 'compliance' in code review while shipping slowly; means and ends inverted."]},{t:"p",text:"The tell of *good* judgment is scaling architecture to stakes: start with ViewModel + repository + SSOT (cheap, huge payoff), add a domain layer when business logic or KMP demands it, add modules when team size or build speed demands it. The critics of Clean Architecture are right that over-layered codebases forgot the economy of simpler patterns — so the mature position is 'add exactly as much architecture as buys testability/team-boundaries/portability, and no more'."},{t:"note",text:"Over-engineering signs: pass-through use cases, four identical model types, one-impl interfaces never faked, modules without payoff, six-file debugging. Good judgment scales architecture to stakes — add layers when they buy something, not for compliance."}]},{level:"senior",q:"How does Google's recommended architecture differ from strict Clean Architecture?",a:[{t:"p",text:"Google's recommended app architecture is heavily *inspired* by Clean Architecture but deliberately *relaxes* several of its stricter rules for pragmatism. Knowing the differences signals you've read the primary sources rather than following a blog's version of 'Clean Architecture on Android'."},{t:"table",headers:["","Strict Clean (Uncle Bob / community)","Google's recommended architecture"],rows:[["Domain layer","mandatory core","*optional* — 'add if complexity warrants'"],["Use cases","every interaction goes through one","only when logic is complex or reused"],["Repository interface lives in","domain (dependency inversion)","data layer defines and exposes it"],["Data flow","UI → UseCase → Repository","UI → Repository directly is fine"],["Guiding philosophy","framework independence above all","SSOT + UDF + layering, without ceremony"]]},{t:"p",text:"The biggest concrete divergences: Google makes the *domain layer optional* (ViewModels can call repositories directly), and puts *repository interfaces in the data layer* (whereas strict Clean puts them in the domain, for dependency inversion). Google's version keeps the *valuable* parts — single source of truth, unidirectional data flow, a clear data/UI split — while dropping the ceremony that doesn't pay for typical apps."},{t:"p",text:"The reconciliation I'd give: I choose per project. For a shared-KMP app or one with complex business rules, strict Clean's domain layer and domain-owned repository interfaces pay off (framework-free, portable, dependency-inverted). For a standard Android CRUD app, Google's pragmatic version is enough — SSOT + UDF + a repository, without a domain layer. The point is that 'Clean Architecture' isn't one fixed thing; even Google deviates from the strict form deliberately, so applying it well means picking the right *amount* for the context."},{t:"note",text:"Google's version relaxes strict Clean: domain layer *optional*, repository interfaces in the *data* layer, ViewModels may call repositories directly — keeping SSOT/UDF/layering while dropping ceremony. Citing this divergence shows you read primary sources."}]},{level:"senior",q:"How do you enforce the Dependency Rule in a real codebase so it doesn't erode?",a:[{t:"p",text:"The Dependency Rule ('dependencies point inward') erodes at the rate reviewers get tired — a convention that's only checked by humans in code review will drift. So durable enforcement means making violations *impossible to compile* or *caught by a machine*, not just discouraged."},{t:"list",items:["**Modularize by layer (strongest)** — put the domain in its own Gradle module that *has no dependency on* the data or presentation modules. Then a domain class *cannot* import a Retrofit DTO or an Android type — it's a *compile error*, not a review comment. The build system enforces the arrow.","**Architecture tests (Konsist / ArchUnit)** — executable rules run in CI: 'classes in ..domain.. must not import android.*', 'domain must not depend on data'. Turn architectural drift into a red build.","**Custom lint / detekt rules** — for idiom-level rules (no Android imports in domain packages, no repository *impl* types referenced from presentation).","**Module-graph checks** — plugins that validate the dependency DAG against a declared spec (no data→presentation edges, no upward dependencies).","**Structural friction** — `internal` visibility + api/impl module splits so consumers can't even *see* the classes they shouldn't depend on."]},{t:"p",text:"The principle to state: convention < detection < prevention. Convention (a style guide) decays. Detection (CI architecture tests) catches drift but after the fact. Prevention (module boundaries that make violations non-compilable) is best — which is exactly why modularizing by layer is the gold standard for enforcing Clean Architecture: the Dependency Rule becomes a *build constraint*, so the domain physically cannot depend on frameworks. This is also why KMP is such a good fit — `commonMain` literally cannot import Android, so the compiler enforces the rule for free."},{t:"note",text:"Enforce the Dependency Rule with *structure*, not vigilance: module boundaries make violations non-compilable (best), CI architecture tests (Konsist/ArchUnit) catch drift, lint for idioms. Convention < detection < prevention — modularizing by layer is the gold standard."}]},{level:"senior",q:"How do use cases combine data from multiple repositories?",a:[{t:"p",text:"One of the clearest justifications for a use case (over calling repositories directly from the ViewModel) is when a business operation needs data from *multiple* repositories combined by real logic. The use case orchestrates the repositories and encodes the business rule, keeping that logic out of both the ViewModel and any single repository."},{t:"code",title:"A use case combining repositories",code:`class GetPersonalizedFeedUseCase(
    private val feedRepo: FeedRepository,        // domain interfaces
    private val userRepo: UserRepository,
    private val settingsRepo: SettingsRepository,
    private val defaultDispatcher: CoroutineDispatcher,   // injected, main-safe
) {
    suspend operator fun invoke(): Result<Feed> = withContext(defaultDispatcher) {
        val user = userRepo.getUser()
        val settings = settingsRepo.getSettings()
        feedRepo.getFeed()
            .map { feed -> feed.rankFor(user.interests)          // business rule
                               .filterBy(settings.contentFilters) }  // more logic
    }
}`},{t:"code",title:"Reactive combination with Flow",code:`class ObserveCartSummaryUseCase(
    private val cartRepo: CartRepository, private val pricingRepo: PricingRepository,
) {
    operator fun invoke(): Flow<CartSummary> =
        combine(cartRepo.observeItems(), pricingRepo.observeDiscounts()) { items, discounts ->
            CartSummary(subtotal = items.sumOf { it.price },
                        discount = discounts.applyTo(items))   // combining logic in one place
        }
}`},{t:"list",items:["**Orchestration** — the use case calls multiple repositories and *combines* their results with business logic (ranking, filtering, pricing).","**Main-safety** — the use case wraps CPU-heavy combination in `withContext(defaultDispatcher)` so callers stay main-safe.","**Single home for the rule** — the combining logic lives in *one* place, so it's reused across ViewModels and unit-testable in isolation (inject fake repositories)."]},{t:"p",text:"This is the litmus test for 'does this use case earn its keep?': if it *combines multiple sources* or *encodes real policy*, yes — it's the right home for logic that belongs to no single repository and shouldn't be duplicated in every ViewModel. If it just forwards one repository call, it's pass-through ceremony. The multi-repository combination case is the archetypal 'earns its keep' use case."},{t:"note",text:"Use cases that *combine multiple repositories* with real logic (ranking, pricing, filtering) are the archetypal 'earns its keep' case — one testable home for logic that belongs to no single repository. Use `combine` for reactive sources."}]}],kx={id:"clean-architecture",title:"Clean Architecture",tagline:"The Dependency Rule, layers, use cases, repositories, SSOT, error & threading strategy, SOLID, KMP layering — and when it's all overkill.",tags:["Android","KMP","Domain Layer","Repository","SOLID","SSOT"],content:vx,qa:bx},xx=[{heading:"Why modularize — and when not to",blocks:[{t:"p",text:"**Modularization** splits one monolithic Gradle module into many smaller ones with explicit dependencies. It is *orthogonal* to MVVM/Clean — those say how code is layered; modularization decides how it's **physically partitioned and compiled**. The reasons that actually hold up:"},{t:"list",items:["**Enforced boundaries**: within one module, any class can reach any other — architecture rules are convention. Across modules, an illegal dependency is a **compile error**. Modularization is architecture with teeth.","**Build speed**: Gradle skips compiling modules whose inputs didn't change and builds independent modules **in parallel** — in large apps this cuts incremental builds from minutes to seconds.","**Team ownership**: feature teams own modules with clear APIs; merge conflicts drop; CODEOWNERS maps cleanly.","**Reuse & delivery**: shared `core` modules across apps (or KMP targets); **Play Feature Delivery** (on-demand dynamic features) requires modules.","**Testability & tooling**: per-module test suites run in isolation; demo/sandbox apps can assemble a single feature."]},{t:"note",text:"When **not** to: small apps and small teams. Modularization has fixed costs — Gradle config sprawl, slower *clean* builds (module graph overhead), cross-module refactors touching many build files. Under ~50–100k LOC and a handful of developers, a well-layered monolith with package discipline is usually faster to work in. Say this unprompted; it's the judgment interviewers probe."}]},{heading:"Module taxonomy — app, feature, core, and the api/impl split",blocks:[{t:"list",items:["**`:app`** — thin shell: application class, DI aggregation, navigation host, final dependency wiring. Depends on everything; contains almost nothing.","**`:feature:*`** (e.g. `:feature:checkout`, `:feature:profile`) — one user-facing feature each: its screens, ViewModels, feature-scoped DI. Feature modules **never depend on each other**.","**`:core:*`** (a.k.a. library modules) — shared infrastructure: `:core:designsystem`, `:core:network`, `:core:database`, `:core:common`, `:core:testing`. Depend only on other core modules.","**`:data:*` / `:domain:*`** — when layering is also modularized: `:core:data` repositories, `:core:domain`/`:domain:orders` models+use cases (this is where Clean Architecture's layers become physical)."]},{t:"h3",text:"The api/impl split"},{t:"p",text:"Mature graphs split modules into a tiny **`-api`** module (interfaces + models only) and an **`-impl`** module (implementation): consumers depend on `:feature:payments:api`, while only `:app` depends on `:feature:payments:impl` to wire DI. Effects: consumers **can't** reach implementation details (enforced encapsulation), and an impl change doesn't recompile consumers — only api changes do. Cost: double the module count; adopt it where the compile-avoidance and decoupling pay, not everywhere by default."}]},{heading:"Slicing strategies: by layer, by feature, or hybrid",blocks:[{t:"table",headers:["Strategy","Shape","Verdict"],rows:[["**By layer**","`:presentation`, `:domain`, `:data` — three fat horizontal modules","enforces Clean's arrows but scales terribly: every feature touches all three modules, teams collide, parallelism is capped at 3"],["**By feature**","`:feature:search`, `:feature:cart`, … + shared `:core:*`","matches team boundaries and build parallelism; layering *inside* a feature is back to convention"],["**Hybrid (industry standard)**","features sliced vertically, each internally layered (or split into `:feature:x` + `:feature:x:api`), on top of `:core:*` layer modules","what Google's Now in Android sample and most large apps do"]]},{t:"p",text:"The dependency graph you're aiming for is a **DAG with three tiers**: `:app` → features → core. Arrows only point downward; anything sideways (feature→feature) or upward is a design smell the build should reject."}]},{heading:"Gradle mechanics — api vs implementation and what they do to builds",blocks:[{t:"code",title:"The dependency configurations that matter",code:`// :feature:checkout/build.gradle.kts
dependencies {
    implementation(project(":core:designsystem")) // internal use only
    api(project(":core:model"))                    // leaked in this module's public API

    implementation(libs.androidx.lifecycle.viewmodel)
}`},{t:"list",items:["**`implementation`**: the dependency is invisible to consumers' compile classpaths. When it changes (ABI), *consumers of this module don't recompile*. Default choice — always.","**`api`**: the dependency is re-exported — consumers see its types and **recompile when it changes**. Use only when the module's own public signatures expose those types (e.g. a models module).","The build-speed rule: `api` where unavoidable, `implementation` everywhere else — accidental `api` chains are the classic reason 'we modularized but builds didn't improve'.","**ABI vs non-ABI changes**: Gradle's compile avoidance recompiles consumers only when a dependency's *public signatures* change — a method-body edit in `:core:network` shouldn't recompile features (unless leaked via `api`).","Circular module dependencies are a **hard Gradle error** — usually a hidden shared concept that wants extraction into a lower module."]}]},{heading:"Feature-to-feature navigation without dependencies",blocks:[{t:"p",text:"Checkout must open Profile, but features can't depend on each other. Every large app solves this with **inversion through a shared contract** — know several concrete shapes:"},{t:"list",items:["**Route/deep-link contracts in a shared module**: `:core:navigation` holds route constants or type-safe route classes (Navigation-Compose serializable routes); features navigate by *route*, and `:app`'s NavHost maps routes → screens. Features never see each other's composables.","**Navigator interfaces**: `:feature:checkout` declares `interface CheckoutNavigation { fun openProfile(userId: String) }`; `:app` implements it with the real NavController and provides it via DI — full inversion, easiest to fake in tests.","**Deep links proper** (URIs) — loosest coupling, also covers external entry; stringly-typed unless wrapped.","**Screen-factory registries** for non-Navigation stacks (or multi-module Fragments): features register `Route -> Screen` factories into a map the host consumes."]},{t:"code",title:"Contract + app-level wiring (Navigator-interface flavor)",code:`// :feature:checkout (declares what it needs)
interface CheckoutNavigator {
    fun openProfile(userId: String)
    fun openOrderConfirmation(orderId: String)
}

// :app (the only module that knows everyone)
class AppNavigator(private val navController: NavHostController) :
    CheckoutNavigator, ProfileNavigator {
    override fun openProfile(userId: String) =
        navController.navigate(ProfileRoute(userId))
    override fun openOrderConfirmation(orderId: String) =
        navController.navigate(OrderConfirmationRoute(orderId))
}`}]},{heading:"Dependency injection across modules",blocks:[{t:"list",items:["**Hilt**: each module declares its own `@Module @InstallIn(...)` bindings; the annotation processor **aggregates them automatically** when `:app` depends on the module — no manual registry. `@HiltViewModel`s in feature modules just work.","**Hilt across api/impl splits**: consumers inject the interface from `-api`; the `@Binds` lives in `-impl`; only `:app` depends on `-impl`, so aggregation still sees it. **EntryPoints** cover non-injectable entry places (dynamic-feature code, content providers).","**Dynamic Feature caveat**: Hilt can't aggregate from on-demand modules (they depend on `:app`, inverting the graph) — you use `@EntryPoint` accessors there; a classic gotcha question.","**Koin**: each Gradle module exposes a `val featureModule = module { ... }`; `:app` collects the list into `startKoin` — explicit, no codegen, and multiplatform (the common choice in KMP).","**Scoping discipline**: singletons (network, DB) live in core modules' bindings; feature bindings stay feature/ViewModel-scoped so features don't bloat the app-lifetime graph."]}]},{heading:"Build infrastructure: version catalogs & convention plugins",blocks:[{t:"p",text:"Thirty modules means thirty `build.gradle.kts` files — without shared infrastructure they drift. Two standard tools:"},{t:"code",title:"Version catalog — gradle/libs.versions.toml",code:`[versions]
kotlin = "2.1.0"
compose-bom = "2025.01.00"

[libraries]
androidx-lifecycle-viewmodel = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version = "2.8.7" }

[plugins]
android-application = { id = "com.android.application", version = "8.7.0" }`},{t:"code",title:"Convention plugin — build-logic/ (one line per module afterwards)",code:`// build-logic/convention/src/main/kotlin/AndroidFeatureConventionPlugin.kt
class AndroidFeatureConventionPlugin : Plugin<Project> {
    override fun apply(target: Project) = with(target) {
        pluginManager.apply("com.android.library")
        pluginManager.apply("org.jetbrains.kotlin.android")
        extensions.configure<LibraryExtension> {
            compileSdk = 35
            defaultConfig.minSdk = 24
        }
        dependencies {
            "implementation"(project(":core:designsystem"))
            "implementation"(project(":core:common"))
        }
    }
}

// a feature module's ENTIRE build.gradle.kts:
plugins { id("myapp.android.feature") }
dependencies { implementation(project(":core:data")) }`},{t:"list",items:["**Version catalog** = single source of truth for versions/coordinates, type-safe accessors (`libs.androidx.lifecycle.viewmodel`), one place for Renovate/Dependabot updates.","**Convention plugins** (in an included `build-logic` build) capture per-module-type configuration — `myapp.android.feature`, `myapp.android.library`, `myapp.kmp.library` — so module build files shrink to declarations. This is the Now-in-Android pattern and a strong senior signal to name.","Anti-pattern they replace: `subprojects { }` / `allprojects { }` blocks in the root build file — cross-project configuration that breaks configuration caching and project isolation."]}]},{heading:"Build performance — what modularization actually buys, and the rest of the toolbox",blocks:[{t:"list",items:["**Compile avoidance**: unchanged modules aren't recompiled; ABI-only recompilation limits blast radius of a change to its dependents — *if* the graph is wide and `api` leakage is controlled. A deep chain of modules serializes and helps nothing.","**Parallel execution**: `org.gradle.parallel=true` builds independent modules concurrently — payoff scales with graph *width*.","**Build cache** (local/remote): task outputs keyed by inputs — CI reuses results across machines; a fat monolith module can never cache-hit partially.","**Configuration cache**: skips the configuration phase entirely on repeat builds — the biggest recent win; requires plugins (and your build logic) to be compatible.","**KSP over KAPT** (Hilt/Room/Moshi): removes the stub-generation tax, roughly 2× annotation-processing speed.","Measure, don't guess: **build scans** (`--scan`), `gradle-profiler`, and the module dependency graph (e.g. `graphviz` task plugins) to find the critical path and `api` leaks."]}]},{heading:"Modularization in KMP",blocks:[{t:"list",items:["Same DAG philosophy, plus the platform dimension: shared KMP modules (`:shared:domain`, `:shared:data`, `:shared:feature:x`) consumed by `:androidApp`, with iOS consuming through a framework.","**The umbrella module**: iOS wants *one* framework, so multi-module KMP projects add a `:shared:umbrella` module that `export`s the others into a single XCFramework — a KMP-specific pattern worth naming (direct multi-framework consumption breaks type identity across frameworks).","Per-module `commonMain/androidMain/iosMain` source sets keep `expect/actual` local to the module that owns the abstraction (DB driver in `:shared:database`, not globally).","DI: Koin modules per Gradle module aggregate in shared init code called from both platforms; Hilt stays Android-side only.","Watch iOS build times: each shared module compiles to Kotlin/Native — the umbrella + exported set should be curated, not 'export everything'."]}]},{heading:"Pitfalls, smells, and the migration path",blocks:[{t:"list",items:["**The `:core:common` dumping ground** — 'utils' modules that every module depends on become a new monolith: one change recompiles the world. Keep leaf modules small and purposeful; split `common` the moment it accretes.","**Over-modularization** — 200 micro-modules where configuration time exceeds compile time; module count should follow team/feature boundaries, not class count.","**`api` chains** — transitive leakage quietly restoring monolith-like recompilation; audit with build scans.","**Sideways dependencies** smuggled through 'shared feature' modules — `:feature:a` and `:feature:b` both depending on `:feature:shared-ab` that contains half of each; extract genuine common *domain* downward instead.","**Circulars resolved by merging** — merging two modules to kill a cycle usually papers over a missing abstraction; extract the shared concept downward instead.","**Migration order (monolith → modules)**: 1) extract leaf utilities (`:core:model`, `:core:common`); 2) extract infrastructure (`:core:network`, `:core:database`, `:core:designsystem`); 3) carve features out one at a time, newest/most-active first; 4) thin `:app` to a shell. Enforce with lint/Konsist/module-graph checks so the monolith doesn't regrow."]},{t:"note",text:'Best interview close: "Modularization is the physical enforcement of whatever architecture you chose — layers become compile errors instead of review comments. Its ROI curve starts negative and grows with team size, so the skill is timing the investment, not maximizing module count."'}]}],Sx=[{level:"junior",q:"What is modularization and why do teams do it?",a:[{t:"p",text:"Splitting one monolithic Gradle module into multiple smaller modules with explicit dependencies between them. The four real reasons: **enforced boundaries** (an illegal dependency becomes a compile error instead of a review comment), **build speed** (unchanged modules aren't recompiled, independent ones build in parallel), **team ownership** (features map to modules with clear APIs), and **reuse/delivery** (shared core modules, Play on-demand feature delivery). It's orthogonal to MVVM/Clean — those decide layering, modularization decides physical partitioning."}]},{level:"junior",q:"Describe a typical module structure for a modularized Android app.",a:[{t:"list",items:["**`:app`** — thin shell: Application class, DI aggregation, the NavHost. Depends on everything, contains almost nothing.","**`:feature:search`, `:feature:cart`, …** — one user-facing feature each (screens + ViewModels + feature DI). Never depend on each other.","**`:core:designsystem`, `:core:network`, `:core:database`, `:core:model`, `:core:common`** — shared infrastructure that features build on."]},{t:"p",text:"The shape is a three-tier **DAG**: app → features → core, arrows only pointing downward. This hybrid (vertical feature slices over horizontal core layers) is what Google's Now in Android sample demonstrates."}]},{level:"junior",q:"What's the difference between api and implementation in Gradle dependencies?",a:[{t:"p",text:"`implementation` keeps the dependency **private**: consumers of your module don't see its types and — crucially — **don't recompile when it changes**. `api` **re-exports** it: the types appear in your module's public surface and every consumer recompiles on its changes. Rule: `implementation` always, `api` only when your own public signatures genuinely expose the dependency's types (e.g. a `:core:model` module). Accidental `api` chains are the top reason modularized builds stay slow."}]},{level:"junior",q:"Why shouldn't feature modules depend on each other?",a:[{t:"p",text:"Because feature-to-feature edges rebuild the monolith incrementally: they serialize builds (B waits for A), create merge coupling between teams, invite cycles (checkout→profile→checkout is a hard Gradle error), and make features impossible to build/test/deliver in isolation. When feature A needs something of feature B's, the *thing* is either shared domain (extract downward into a core/domain module) or navigation (invert through a route contract or navigator interface that `:app` wires). The graph stays a DAG with arrows pointing down."}]},{level:"junior",q:"How does one feature open a screen that belongs to another feature module?",a:[{t:"p",text:"Through an inverted contract, since a direct dependency is forbidden. Common shapes: **shared route definitions** in `:core:navigation` (feature navigates by type-safe route; `:app`'s NavHost maps routes to screens), a **navigator interface** the feature declares (`fun openProfile(userId)`) and `:app` implements with the real NavController via DI, or **deep links** for the loosest coupling. In all three, only `:app` knows every screen — features know destinations as abstract contracts."}]},{level:"junior",q:"What is a version catalog and what problem does it solve?",a:[{t:"p",text:"`gradle/libs.versions.toml` — a single declarative file listing dependency versions, library coordinates and plugins, exposed as type-safe accessors (`libs.androidx.lifecycle.viewmodel`) in every module. Problems solved: thirty build files no longer each hardcode versions (no drift, no '3 versions of OkHttp' surprises), upgrades are a one-line change, and bots (Renovate/Dependabot) have one file to update. It replaced the older `ext {}` / `Dependencies.kt` buildSrc conventions."}]},{level:"junior",q:"Does modularization automatically make builds faster?",a:[{t:"p",text:"No — it *enables* faster builds, and can even slow them down if done badly. Wins come from **compile avoidance** (unchanged modules skipped, only dependents of a changed module rebuild) and **parallelism** (independent modules concurrently) — both require a *wide* graph with `implementation` boundaries. A deep chain of modules, `api` leakage everywhere, or a `:core:common` that everything depends on gives you monolith rebuild behavior plus extra Gradle configuration overhead. Clean builds actually get slightly slower (per-module fixed costs). So: measure with build scans, control `api`, keep the graph wide and shallow."}]},{level:"senior",q:"Compare modularizing by layer vs by feature. Which do you choose and why?",a:[{t:"p",text:"**By layer** (`:presentation`/`:domain`/`:data`) physically enforces Clean's arrows, but scales badly: every feature change touches all three modules (so teams collide in the same modules), parallelism caps at the layer count, and each layer module grows into its own monolith. **By feature** matches team ownership and maximizes graph width, but layering *inside* each feature falls back to convention, and shared logic gets duplicated or dumped into common modules."},{t:"p",text:"I choose the **hybrid** every serious codebase converges on: vertical feature slices (`:feature:x` — optionally split into `:api`/`:impl`) sitting on horizontal core modules (`:core:data`, `:core:designsystem`, `:core:model`). Feature boundaries follow *team and product* seams; layer boundaries live in core and, where the stakes justify it, inside features via the api/impl split. The honest observation to add: pure by-layer is the textbook answer, hybrid is the shipped answer — NowInAndroid, and most large apps, are hybrids."}]},{level:"senior",q:"Explain the api/impl module split — what does it buy, what does it cost, and when is it worth it?",a:[{t:"list",items:["**Mechanics**: `:feature:payments:api` holds only interfaces + models; `:feature:payments:impl` holds the implementation and its `@Binds`. Consumers depend on `:api`; only `:app` depends on `:impl` for wiring.","**Buys — encapsulation with teeth**: consumers *cannot* reach implementation classes (not convention — the classpath doesn't contain them).","**Buys — compile-avoidance**: impl changes recompile only `:app`'s wiring, not the consumers; api modules are small and change rarely, so the recompilation blast radius of most edits collapses.","**Buys — swappability**: fake impls for demo apps and tests slot in per-module.","**Costs**: 2× module count, DI indirection for every binding, and 'where does this type go' friction; api modules that accrete logic defeat the purpose."]},{t:"p",text:"Worth it when: many consumers per module (core modules first — `:core:data:api` pays off fastest), large teams needing hard encapsulation, or build times dominated by wide recompilation. Not worth it as a blanket rule on a 10-module app — apply it to the hubs of the dependency graph, not the leaves."}]},{level:"senior",q:"How does Hilt work across modules, and what breaks with dynamic feature modules?",a:[{t:"p",text:"Standard multi-module Hilt is pleasantly automatic: each module declares `@Module @InstallIn(SingletonComponent::class)` (etc.) bindings; because `:app` (the `@HiltAndroidApp` root) transitively depends on every module, annotation processing **aggregates all bindings** into the app's component graph at compile time — no manual registry, and `@HiltViewModel`s in feature modules just work. With api/impl splits, the `@Binds` sits in `:impl`, consumers inject the `:api` interface, and aggregation still finds it since `:app` depends on `:impl`."},{t:"p",text:"**Dynamic features invert the graph** — the on-demand module depends *on* `:app`, not vice versa — so Hilt's aggregation can't see it: no `@AndroidEntryPoint` in dynamic modules. The workaround is **`@EntryPoint`**: define an interface listing what the dynamic module needs, install it in the app's component, and fetch it reflectively via `EntryPointAccessors.fromApplication(...)` from the dynamic module's screens. It's clunky by design — worth also saying that this friction (plus dagger's per-module strictness) is why some heavily-dynamic apps choose Koin, which resolves at runtime and doesn't care about compile-time graph direction."}]},{level:"senior",q:"What are convention plugins and why are they preferred over buildSrc and allprojects blocks?",a:[{t:"p",text:"Convention plugins are small Gradle plugins in an **included build** (`build-logic/`) that encode each module *type*'s full configuration — `myapp.android.feature` applies AGP + Kotlin, sets SDK versions, adds standard deps — so a feature module's build file shrinks to `plugins { id(\"myapp.android.feature\") }` plus its specific dependencies. Thirty modules stay consistent by construction, and changing minSdk is a one-file edit."},{t:"list",items:["**vs `allprojects`/`subprojects`**: cross-project configuration reaches into other projects' models — it breaks **configuration caching** and **project isolation**, and hides configuration far from the module; convention plugins keep configuration declarative and local.","**vs `buildSrc`**: a `buildSrc` change invalidates the build classpath of *every* module (full rebuild); an included `build-logic` build is a normal dependency with normal invalidation — that single caching difference is why the ecosystem (and NowInAndroid) migrated.","Bonus points: convention plugins are themselves testable, versionable, and shareable across repositories — build logic promoted to first-class code."]}]},{level:"senior",q:"Your 60-module app still has slow incremental builds. Walk through your diagnosis.",a:[{t:"list",items:["**Get data first**: `--scan` / gradle-profiler on a representative edit (one-line body change in a hot module). Look at what actually recompiled and the task critical path — guesses about Gradle are usually wrong.","**Check the blast radius**: if a body-only (non-ABI) change recompiled half the graph, hunt `api` leakage — audit hub modules' dependency declarations, demote to `implementation`.","**Check the hubs**: a `:core:common` dumping ground everything depends on makes every change global — split it; check the graph's *width vs depth* (deep chains serialize; parallelism needs width).","**Check the perennial taxes**: KAPT still present? (migrate to KSP — Hilt/Room/Glide all support it); configuration time high? (enable configuration cache, kill `allprojects` blocks and eager task creation); no remote build cache on CI?","**Check the non-Gradle suspects**: single massive module dominating the critical path (carve it), annotation processors on hot paths, unnecessary resource processing in library modules, outdated AGP/Kotlin (compiler perf improves every release)."]},{t:"p",text:"The framing that lands: treat build performance as an engineering problem with measurements and a critical path, not folklore — and note the fix is often *dependency hygiene* (api→implementation, splitting hubs), not more modules."}]},{level:"senior",q:"How do you structure modules in a KMP project, and what is the umbrella framework problem?",a:[{t:"p",text:"Same DAG discipline with a platform dimension: shared modules (`:shared:model`, `:shared:database`, `:shared:feature:x`) with per-module `commonMain`/`androidMain`/`iosMain` source sets — keeping each `expect/actual` inside the module that owns the abstraction. Android consumes shared modules as ordinary Gradle deps; DI is typically Koin (modules aggregated in shared init) since Hilt is Android-only."},{t:"p",text:"**The umbrella problem**: iOS consumes Kotlin as an Apple framework, and if each shared module produced its own framework, shared types would lose identity across them (the same Kotlin class becomes two incompatible Obj-C classes in two frameworks). So multi-module KMP projects create one **umbrella module** whose framework `export()`s the modules iOS needs, producing a single XCFramework. Follow-ups worth volunteering: curate the export list (every exported module grows the binary and compile time — Kotlin/Native linking is the slow path), non-exported transitive types surface as opaque `KotlinBase` — so design the iOS-facing API to live in exported modules; and SKIE runs on the umbrella to improve the Swift surface."}]},{level:"senior",q:"How do you enforce architectural boundaries beyond what Gradle gives you — and prevent the monolith from regrowing?",a:[{t:"list",items:["**Gradle gives the coarse walls** (missing dependency = compile error) — but within a module, and for rules like 'features may not depend on features', you need active enforcement.","**Konsist / ArchUnit tests**: executable architecture rules — 'classes in ..presentation.. may not import ..data.internal..', 'ViewModels live in feature modules', run in CI like any test.","**Custom lint checks / detekt rules** for idiom-level rules (no `android.*` imports in domain packages, no repository types in composables).","**Module-graph assertions**: plugins that validate the dependency DAG against a declared spec (no feature→feature edges, max depth, no new edges without review) — turning graph erosion into a red build.","**Structural friction**: `internal` visibility + api/impl splits make violations *impossible* rather than detectable; CODEOWNERS on build files makes new edges reviewable by the platform team."]},{t:"p",text:"The principle to close on: every architecture decays at the rate reviewers get tired — durable boundaries are the ones a machine checks. Convention < detection (CI rules) < prevention (visibility + graph structure)."}]},{level:"senior",q:"Walk through migrating a 400k-line monolith module to a modular structure while the team keeps shipping.",a:[{t:"list",items:["**Prep**: measure builds (baseline to prove ROI later); map the internal dependency structure (package-level analysis — jdeps/Sonargraph/IDE); set up version catalog + convention plugins *first* so every extracted module is one line of config.","**Extract leaves first**: `:core:model`, then infrastructure with few inward deps — `:core:designsystem`, `:core:network`, `:core:database`. Each extraction is mechanical (move + fix imports) and immediately parallelizes builds.","**Break the hard knots**: the monolith's remaining tangle is usually cyclic feature code coupled through statics/utils — introduce interfaces at the seams inside the monolith *before* physically moving code (decouple in place, then extract cheaply).","**Carve features by activity**: highest-churn features first (that's where isolation pays daily); each becomes `:feature:x` with a navigation contract; the monolith shrinks to a legacy module that new code may not depend on (enforced by a graph rule).","**Keep shipping**: every step lands on main behind normal review — no long-lived migration branch; the graph rules + CI checks prevent regrowth; declare victory not at 'zero legacy' but when the legacy module is frozen and shrinking."]},{t:"p",text:"Risks to name: import-fixing churn creating merge storms (coordinate extraction windows), hidden reflection/resource references breaking at runtime (R classes, ProGuard), and morale — pick early extractions that visibly improve build times so the effort funds its own credibility."}]},{level:"junior",q:"What is a Gradle module, and how is it different from a package?",a:[{t:"p",text:"A Gradle module is an independently-buildable unit of code with its own `build.gradle.kts`, its own dependencies, and its own compiled output. A package is just a namespace *within* a module — a way to organize files. The crucial difference: modules have *enforced* boundaries (a module can only use what it explicitly depends on), while packages have none (any class can reach any other class in the same module)."},{t:"list",items:["**Module** — a build unit: own build file, own dependency list, own compilation, produces its own artifact. Boundaries are enforced by the build system.","**Package** — a namespace inside a module; purely organizational. No dependency enforcement — everything in the module can access everything else."]},{t:"p",text:"This distinction is *the whole point* of modularization. Within a single module, 'the presentation layer must not import data-layer internals' is a convention that reviewers must police — and it erodes. Across modules, if `:feature:profile` doesn't declare a dependency on `:feature:payments:impl`, then a profile class *cannot* import a payment implementation class — it's a *compile error*. So modules turn architectural rules into build constraints. That's why 'organize into packages' and 'split into modules' are fundamentally different: only the latter *enforces* the boundaries."},{t:"note",text:"A module is a *buildable unit with enforced boundaries*; a package is just a namespace with none. Modularization matters because it turns 'please don't depend on that' into a compile error."}]},{level:"junior",q:"What are the main types of modules in a modularized app?",a:[{t:"p",text:"A well-modularized Android app typically has three kinds of modules arranged in tiers: a thin `:app` module at the top, `:feature:*` modules for user-facing features, and `:core:*` modules for shared infrastructure they all build on. The dependencies flow downward: app → features → core."},{t:"list",items:["**`:app`** — a thin shell: the Application class, DI aggregation, the navigation host, final wiring. Depends on everything, contains almost nothing.","**`:feature:*`** (e.g. `:feature:checkout`, `:feature:profile`) — one user-facing feature each: its screens, ViewModels, feature-scoped DI. Feature modules **never depend on each other**.","**`:core:*`** (e.g. `:core:designsystem`, `:core:network`, `:core:database`, `:core:common`) — shared infrastructure that features build on. Depend only on other core modules.","**`:data`/`:domain`** (optional) — if you also modularize by layer, the repositories and domain models get their own modules — where Clean Architecture's layers become physical."]},{t:"p",text:"The target shape is a **DAG (directed acyclic graph) with arrows pointing downward**: `:app` → features → core, and nothing sideways (feature→feature) or upward. This is the hybrid structure Google's Now in Android sample demonstrates and that most large apps converge on — vertical feature slices sitting on horizontal core modules. Understanding the app/feature/core taxonomy and the downward-only dependency rule is the foundation of modularization."},{t:"note",text:"Three tiers: thin `:app` → `:feature:*` (never depend on each other) → `:core:*` (shared infra). A downward-pointing DAG — no sideways or upward edges. That's the Now-in-Android shape."}]},{level:"junior",q:"What is the difference between multi-module and single-module, and when is each right?",a:[{t:"p",text:"A single-module app keeps all code in one Gradle module (`:app`); a multi-module app splits it into many. The trade-off is *enforced boundaries + build speed at scale* (multi-module) versus *simplicity and lower overhead* (single-module). The right choice depends heavily on app size and team size."},{t:"table",headers:["","Single-module","Multi-module"],rows:[["Boundaries","convention only (packages)","enforced by the build (compile errors)"],["Incremental build","recompiles everything on any change","recompiles only changed modules; parallel"],["Overhead","low (one build file)","higher (many build files, config)"],["Team scaling","merge conflicts, coupling","teams own modules with clear APIs"],["Best for","small apps, small teams, prototypes","large apps, multiple teams, long-lived"]]},{t:"p",text:"The honest guidance: modularization has *fixed costs* (Gradle config sprawl, slower clean builds due to module-graph overhead, cross-module refactors touching many build files). Under roughly 50–100k lines and a handful of developers, a well-layered single module with package discipline is usually faster to work in. Modularization pays off as team size and codebase grow — its ROI curve *starts negative* and rises with scale."},{t:"p",text:"So I don't reflexively modularize. For a small app or MVP, single-module is simpler and correct. For a large, multi-team, long-lived app, multi-module's enforced boundaries and build parallelism are worth the overhead. The skill is *timing the investment* — modularize when the pain (slow builds, merge conflicts, eroding boundaries) actually appears, not preemptively."},{t:"note",text:"Single-module for small apps/teams (simpler, lower overhead); multi-module for large/multi-team apps (enforced boundaries, build parallelism). Modularization's ROI starts negative and rises with scale — time the investment, don't do it preemptively."}]},{level:"senior",q:"How does modularization actually make builds faster? Explain the mechanism.",a:[{t:"p",text:"Modularization enables — but doesn't guarantee — faster builds through two mechanisms: **compile avoidance** (unchanged modules aren't recompiled) and **parallel execution** (independent modules build concurrently). Both require the module graph to be *wide* and dependencies to be properly scoped, or you get no benefit."},{t:"list",items:["**Compile avoidance** — Gradle skips recompiling a module whose inputs haven't changed. If you edit `:feature:profile`, only it (and its dependents) recompile — `:feature:checkout` and untouched core modules are reused from cache. In a single monolith, *any* change recompiles *everything*.","**ABI vs non-ABI changes** — even finer: Gradle recompiles a module's *consumers* only when the module's *public signatures* (ABI) change. A method-body edit in `:core:network` (non-ABI) doesn't recompile the features that use it — only an API change does. This dramatically limits the 'blast radius' of a change.","**Parallel execution** (`org.gradle.parallel=true`) — independent modules with no dependency between them build *concurrently* on multiple cores. The benefit scales with the graph's *width*."]},{t:"p",text:"The critical caveat: these benefits only materialize with discipline. If dependencies leak through `api` (instead of `implementation`), a change cascades recompilation across the whole graph — you 'modularized but builds didn't get faster'. If the graph is *deep* (a long chain of modules) rather than *wide*, there's no parallelism and every change ripples up the chain. And a `:core:common` that everything depends on becomes a new monolith — one change recompiles the world."},{t:"p",text:"So modularization *enables* fast builds; achieving them requires wide graphs, `implementation` boundaries (not `api`), and avoiding god-modules everyone depends on. The mechanism is compile avoidance + parallelism; the discipline is what makes it real."},{t:"note",text:"Faster builds come from compile avoidance (unchanged modules skipped; only ABI changes recompile consumers) + parallel execution (independent modules concurrently). But they require wide graphs, `implementation` not `api`, and no god-module — else you 'modularized but builds got no faster'."}]},{level:"junior",q:"What is a monolithic app module and why is it a problem at scale?",a:[{t:"p",text:"A monolithic app module is the default single `:app` module where *all* code lives — every feature, all the data and networking, all the UI. It's perfectly fine for a small app, but it becomes a problem as the app and team grow, because it has no enforced structure and no build parallelism."},{t:"list",items:["**No enforced boundaries** — any class can reach any other, so architectural layers (or feature separation) are convention-only and erode over time. The presentation code ends up reaching into data internals; features get tangled.","**Slow builds** — any change recompiles the *entire* module. A one-line edit means the whole app rebuilds, with no compile avoidance and no parallelism — build times grow with the codebase.","**Merge conflicts and team collisions** — everyone works in the same module, so multiple teams stepping on the same files causes constant conflicts.","**No independent testing/delivery** — you can't build or test one feature in isolation, and you can't do on-demand feature delivery (Play Feature Delivery needs modules)."]},{t:"p",text:"The core issue is that a monolith relies on *discipline* for structure, and discipline decays at the rate reviewers get tired. Modularization fixes each problem structurally: modules enforce boundaries (compile errors, not conventions), enable compile avoidance and parallelism (faster builds), let teams own modules (fewer conflicts), and allow independent testing/delivery. But — importantly — a monolith is *not* wrong for a small app; the problems are all scale problems. Modularizing a tiny app just adds overhead."},{t:"note",text:"A monolithic `:app` module is fine when small but breaks at scale: no enforced boundaries (erode), whole-app recompiles (slow), team collisions, no independent build/delivery. All *scale* problems — don't modularize a tiny app to 'fix' them."}]},{level:"senior",q:"How do you decide where module boundaries should go?",a:[{t:"p",text:"Module boundaries should follow *how the code changes and who owns it* — the goal is that a typical change touches *one* module, and that modules map to team ownership and to reuse. The dominant strategy is to slice by *feature* (vertical) on top of *core* infrastructure (horizontal), because that matches how work actually happens."},{t:"list",items:["**By feature (vertical) — the primary axis** — one module per user-facing feature (`:feature:checkout`, `:feature:profile`). This matches *team ownership* (a team owns a feature end-to-end) and *build parallelism* (independent features build concurrently), and a feature change touches one module.","**By layer for shared infra (horizontal)** — `:core:network`, `:core:database`, `:core:designsystem`, `:core:common` — infrastructure that features build on. These change independently of features and are reused by all.","**The hybrid (what most large apps use)** — features sliced vertically, sitting on core layer modules, sometimes with each feature internally split into `:api`/`:impl`. This is the Now-in-Android shape."]},{t:"p",text:"The anti-pattern is modularizing *purely by layer* (`:presentation`, `:domain`, `:data` as three fat modules): every feature touches all three, so teams collide in the same modules, parallelism caps at three, and each layer becomes its own monolith. Pure-by-layer is the textbook answer; hybrid (feature slices on core layers) is the shipped answer."},{t:"p",text:"The heuristics I apply: (1) does a typical change touch one module? (if a change always spans three modules, the boundary is wrong); (2) does the boundary match team ownership? (3) is there genuine reuse (core modules) vs feature-specific code? Module count should follow *team and feature boundaries*, not class count — you don't create a module per class, you create one per cohesive, independently-ownable unit of work."},{t:"note",text:"Boundaries follow change + ownership: slice by *feature* (vertical, matches teams and parallelism) on top of *core* infra (horizontal, reused). Avoid pure-by-layer (every change spans all layers). Test: does a typical change touch one module?"}]},{level:"senior",q:"How does modularization enable teams to work in parallel?",a:[{t:"p",text:"Modularization enables parallel teamwork by giving each team a module they *own* with a clear public API, so teams can work independently without stepping on each other — both at the code level (fewer merge conflicts) and the build level (faster feedback). It's often the *primary* motivation for modularization at large companies, ahead of even build speed."},{t:"list",items:["**Ownership with clear APIs** — a team owns `:feature:checkout`; other teams depend on its *public API* (ideally a `:feature:checkout:api` module of interfaces) without seeing or touching its internals. Teams contract through APIs, not through shared mutable code.","**Fewer merge conflicts** — because teams work in *separate* modules and files, they rarely edit the same code. In a monolith, everyone shares files, so conflicts are constant. Modules physically separate the work.","**Independent development and testing** — a team can build and test their module in isolation (or with a small demo/sandbox app that assembles just their feature), getting fast feedback without building the whole app.","**CODEOWNERS mapping** — modules map cleanly to code ownership, so reviews route to the right team automatically.","**Enforced contracts** — the api/impl split means a consuming team *cannot* accidentally depend on another team's implementation details (compile error), so teams can refactor their internals freely without breaking others."]},{t:"p",text:"The deeper point: this is really about *decoupling teams via decoupling code*. Conway's Law says software structure mirrors org structure — modularization deliberately shapes the code so that team boundaries and module boundaries align, letting teams move independently. This is why 'why modularize?' at scale is often answered 'team autonomy' first: it lets a 100-engineer org ship without constant coordination overhead, which is worth more than the build-speed win alone."},{t:"note",text:"Modules give teams ownership + clear APIs, so they work in separate code (fewer conflicts), build/test in isolation (fast feedback), and can't touch each other's internals (enforced contracts). Team autonomy is often the *primary* reason to modularize at scale — decoupling teams by decoupling code."}]},{level:"junior",q:"What goes in a :core module, and what is the :core:common dumping-ground problem?",a:[{t:"p",text:"`:core:*` modules hold *shared infrastructure* that features build on — things that aren't user-facing features but are needed across the app. The idea is to extract genuinely reusable, cohesive infrastructure into focused modules. The danger is a `:core:common` (or `:core:utils`) that becomes a dumping ground for everything, which recreates the monolith's problems."},{t:"list",items:["**Good core modules (focused)** — `:core:network` (the HTTP client, interceptors), `:core:database` (Room setup, DAOs), `:core:designsystem` (theme, reusable components), `:core:model` (shared domain models), `:core:datastore` (preferences).","**Each has a single, clear purpose** — you can name what it's *for*, and features depend only on the core modules they actually need."]},{t:"p",text:"The **`:core:common` dumping-ground problem**: a 'utils' or 'common' module that accumulates unrelated helpers (extensions, constants, base classes, a logger, date utils) and that *every other module depends on*. Because everything depends on it, any change to it recompiles the *entire* app — it becomes a new monolith bottleneck, defeating the point of modularization. And because it's a grab-bag, it has no coherent responsibility, so it grows unboundedly."},{t:"list",items:["**Why it's bad** — everything depends on it → every change to it recompiles the world (no compile avoidance); it violates single-responsibility (no clear purpose); it grows unboundedly.","**The fix** — keep core modules *small and purposeful*; the moment `:core:common` accretes unrelated things, split it (`:core:logging`, `:core:datetime`, `:core:ui-utils`) so changes are localized and dependencies are precise. Not everything needs to be in a shared module."]},{t:"note",text:"Core modules = focused shared infra (`:core:network`, `:core:designsystem`), each with one clear purpose. The trap is `:core:common` — a dumping ground everything depends on, so every change recompiles the world. Split it the moment it accretes unrelated things."}]},{level:"senior",q:"What causes circular module dependencies, and how do you fix them?",a:[{t:"p",text:"A circular dependency is when module A depends on module B and B (directly or transitively) depends on A — which Gradle rejects as a *hard error* (the module graph must be a DAG, acyclic). It usually signals a *missing abstraction* or a *misplaced piece of code*: two modules both need something, so they reach into each other."},{t:"list",items:["**Cause 1 — a shared concept in the wrong place** — `:feature:a` and `:feature:b` both use a `User` model, so each imports the other's version, creating a cycle. Fix: extract the shared concept *downward* into a lower module (`:core:model`) that both depend on. This is the most common cause.","**Cause 2 — feature-to-feature dependency** — `:feature:cart` needs to open `:feature:checkout`, and checkout needs something from cart. Fix: neither should depend on the other; use a *navigation contract* in a shared module (route definitions or a navigator interface that `:app` wires) so features depend on the abstraction, not each other.","**Cause 3 — bidirectional coupling** — module A calls B and B calls back into A. Fix: introduce an *interface* — A defines an interface, B depends on and implements it, and the dependency now points one way (dependency inversion)."]},{t:"code",title:"Breaking a cycle by extracting downward",code:`// CYCLE: :feature:a <-> :feature:b (both need SharedThing)
// FIX: extract SharedThing to a lower module both depend on
// :core:model  <-- :feature:a
//              <-- :feature:b
// Now the graph is acyclic: features -> core (downward only)`},{t:"p",text:"The anti-fix to avoid: *merging* the two modules to kill the cycle. That papers over the real problem (a missing shared abstraction) and re-creates a bigger monolith. The right fix is almost always to *extract the shared thing downward* into a lower module, or *invert a dependency* with an interface — so the cycle becomes a clean one-directional DAG. A circular dependency is a design signal: it's telling you a shared concept wants its own home lower in the graph."},{t:"note",text:"Circular deps = missing abstraction: two modules both need something. Fix by extracting the shared concept *downward* (to `:core:model`) or inverting a dependency with an interface — never by merging the modules (that rebuilds the monolith)."}]},{level:"senior",q:"What are the signs of over-modularization, and how do you avoid it?",a:[{t:"p",text:"Over-modularization is splitting into so many modules that the *overhead* (Gradle configuration, build-graph management, cross-module friction) exceeds the *benefit*. Like over-applying any architecture, it adds ceremony without payoff — modules are a tool with real fixed costs, not a free good."},{t:"list",items:["**Too many micro-modules** — 200 modules for a medium app where *configuration* time (Gradle evaluating the graph) exceeds actual *compile* time. Module count should follow team/feature boundaries, not class count.","**A module per class/tiny-thing** — modules with one or two files, whose build-file overhead and dependency-declaration ceremony dwarf the code they contain.","**Cross-module refactors constantly** — if a normal change routinely touches many modules and build files, the boundaries are wrong (too granular or misaligned).","**Slower *clean* builds** — modularization speeds *incremental* builds but adds per-module overhead to clean builds; too many modules makes clean builds (and CI cold builds) slower.","**Navigation overhead reading code** — following logic across a dozen tiny modules with api/impl splits for everything obscures more than it reveals."]},{t:"p",text:"How to avoid it: modularize based on *actual pain and actual boundaries*, not preemptively or maximally. Start with fewer, larger modules aligned to features and core infrastructure, and split further *only* when a module becomes too big to own, too slow to build, or shared by enough consumers to justify an api/impl split. The api/impl split in particular is worth it for the *hubs* of the dependency graph (core modules with many consumers), not every leaf. The mature stance: 'as many modules as buy team autonomy and build speed, and no more' — the same 'scale architecture to stakes' judgment as Clean Architecture."},{t:"note",text:"Over-modularization signs: micro-modules where config time > compile time, module-per-class, constant cross-module refactors, slower clean builds. Avoid it by modularizing on *actual pain and boundaries*, not maximally — apply api/impl to graph *hubs*, not every leaf."}]},{level:"junior",q:"How do you share a design system (theme, components) across feature modules?",a:[{t:"p",text:"You put the design system in a dedicated `:core:designsystem` module that every feature depends on. It holds the theme (colors, typography, shapes) and the reusable UI components (buttons, cards, text fields), so all features render consistently and none reimplement the basics. Feature modules depend *downward* on it (feature → core:designsystem)."},{t:"code",title:":core:designsystem structure",code:`// :core:designsystem module:
//   Theme.kt         -> MyAppTheme { MaterialTheme(colorScheme, typography, shapes, content) }
//   components/      -> AppButton, AppCard, AppTextField (reusable, styled)
//   tokens/          -> Spacing, Elevation (design tokens)

// A feature module depends on it:
// :feature:profile/build.gradle.kts
dependencies { implementation(project(":core:designsystem")) }

// and uses it:
@Composable fun ProfileScreen() {
    MyAppTheme {                      // shared theme
        AppButton(onClick = { }) { Text("Save") }   // shared component
    }
}`},{t:"list",items:["**Consistency** — one place defines the look, so every feature is visually consistent and a rebrand touches one module.","**No reimplementation** — features reuse `AppButton` etc. rather than each rolling their own styled button.","**Depend on `implementation`** — features use the design system internally; unless a feature *re-exposes* design-system types in its public API, use `implementation` (not `api`) so a design-system change doesn't cascade-recompile every feature.","**Screenshot-test the design system** — since it's shared and central, lock its components' appearance with screenshot tests (Paparazzi) so a change doesn't silently break every screen."]},{t:"p",text:"The design system is a canonical `:core` module — focused (one purpose: the visual language), reused by all features, and a great example of *good* modularization: extracting genuinely shared infrastructure so features don't duplicate it and stay consistent. It's also where a shared Compose theme naturally lives in a modularized app."},{t:"note",text:"Put the theme + reusable components in `:core:designsystem`, depended on by every feature (via `implementation`). One place for the visual language = consistency + no reimplementation. Screenshot-test it since it's central."}]},{level:"senior",q:"How do build cache and configuration cache help modular builds?",a:[{t:"p",text:"These two Gradle features are *essential* to realizing modularization's build-speed promise — modularization enables compile avoidance and parallelism, but the caches are what make repeated and CI builds fast. They cache *task outputs* and the *configuration phase* respectively."},{t:"list",items:["**Build cache** — caches the *outputs* of tasks (like compiling a module) keyed by their inputs. If a module's inputs haven't changed, Gradle reuses the cached output instead of rebuilding. Crucially, a **remote build cache** shares these outputs *across machines* — so on CI (or a teammate's machine), a module someone else already built is downloaded, not rebuilt. This compounds with modularization: unchanged modules are cache hits, so only genuinely-changed modules do work.","**Configuration cache** — caches the result of Gradle's *configuration phase* (evaluating all the build scripts and building the task graph), so repeat builds skip re-configuring the project entirely. This is a big win precisely *because* modularization *increases* configuration cost — many modules mean many build scripts to evaluate, so caching that phase matters more the more modules you have."]},{t:"p",text:"The interaction is important: modularization *adds* configuration overhead (more modules = more scripts to configure), which the configuration cache offsets; and modularization *enables* compile avoidance, which the build cache amplifies (especially remotely, across CI/team). Without the caches, a heavily-modularized project can actually feel *slower* on some builds (config overhead) — with them, you get the full benefit (skip config, reuse unchanged module outputs across machines)."},{t:"p",text:"The practical requirements: configuration cache demands that your build logic be compatible (no cross-project configuration like `subprojects {}`/`allprojects {}` blocks, which break it — this is a reason convention plugins are preferred). And remote build cache needs a cache server (CI infrastructure). Together with `org.gradle.parallel` and `implementation` boundaries, these caches are what turn 'we modularized' into 'our builds are actually fast'."},{t:"note",text:"Build cache reuses task outputs (remote = across CI/team machines — amplifies compile avoidance); configuration cache skips re-evaluating build scripts (offsets the config overhead many modules add). They're what makes modularization's build-speed promise real — and configuration cache is why you avoid `subprojects {}`."}]},{level:"senior",q:"Starting a brand-new project, how would you approach modularization?",a:[{t:"p",text:"For a brand-new project I would *not* over-modularize on day one — I'd start with a small, sensible structure and split further as the app and team grow, because modularization's ROI starts negative (fixed overhead) and rises with scale. Premature modularization adds ceremony before there's anything to gain from it."},{t:"list",items:["**Start small but with the right seams** — for a small new app, even a single module (or a handful) is fine. But I'd set up the *infrastructure* that makes later splitting cheap: a version catalog (`libs.versions.toml`) and convention plugins from the start, so adding a module later is a one-line build file.","**Establish core modules early if reuse is clear** — if I know there'll be a shared design system, network layer, and database, I might create `:core:designsystem`, `:core:network`, `:core:database` early, since those are stable, obviously-shared, and cheap to extract.","**Add feature modules as features grow** — start features in the app module or a single feature module, and split into `:feature:*` when a feature becomes substantial or gets its own team/owner. Let boundaries emerge from real features, not speculation.","**Adopt the hybrid target shape** — plan toward the Now-in-Android structure (feature slices on core layers, downward DAG), but *arrive* there incrementally rather than building 20 modules before writing a screen."]},{t:"p",text:"The judgment: I'd rather *under*-modularize early and split when pain appears (slow builds, a feature too big to own, team growth) than over-modularize and carry the overhead prematurely. But I'd invest early in the *cheap enablers* — version catalog, convention plugins, and the discipline of `implementation`-by-default and no feature-to-feature deps — so that when I *do* split, it's fast and the boundaries are already clean. The mistake I'd avoid is copying a big app's 30-module structure onto a 2-screen MVP: that's ceremony, and modularization should follow the project's actual scale and team."},{t:"note",text:"New project: start small (few modules), but set up the cheap enablers early (version catalog, convention plugins, `implementation`-by-default). Extract obvious core modules; add feature modules as features grow. Split on real pain, not speculation — don't copy a big app's 30-module structure onto an MVP."}]},{level:"senior",q:"How does modularization enable dynamic feature delivery, and what's the catch?",a:[{t:"p",text:"Modularization is a *prerequisite* for Play Feature Delivery — delivering parts of your app *on demand* rather than at install. A **Dynamic Feature Module** is a special module whose code and resources are downloaded only when the user needs the feature, keeping the initial install small. But it comes with a significant catch around dependency injection."},{t:"list",items:["**On-demand delivery** — a rarely-used feature (a complex editor, a one-time onboarding flow) can be a dynamic module the user downloads only when they open it, so the base install stays small. Also supports conditional delivery (by device capability) and instant delivery.","**Built on the App Bundle** — Play generates the dynamic feature's APK separately and delivers it when requested via the Play Core / Feature Delivery API.","**Requires modularization** — you can't have dynamic features without splitting the app into modules first; it's a payoff of the modular structure."]},{t:"p",text:"**The catch — the dependency graph inverts.** Normally modules flow `:app` → `:feature` (the app depends on features), so DI frameworks like Hilt can aggregate the features' bindings. But a *dynamic feature module depends on `:app`* (the reverse), so at compile time the app can't see the dynamic module's code — meaning Hilt's compile-time aggregation *can't reach it*, and you **can't use `@AndroidEntryPoint`/standard Hilt injection** in a dynamic feature module."},{t:"list",items:["**The workaround** — use Hilt's `@EntryPoint` + `EntryPointAccessors`: define an interface listing what the dynamic module needs, install it in the app's component, and fetch dependencies *reflectively at runtime* from the dynamic module. Clunky by design, because you're reaching into a graph the module can't participate in normally.","**Other frictions** — resource access, navigation into the dynamic feature, and testing all become more involved. Some heavily-dynamic apps prefer Koin (runtime DI) precisely because its resolution doesn't care about the compile-time graph direction."]},{t:"p",text:"So the honest summary: modularization *unlocks* on-demand delivery (a real benefit for install size), but dynamic feature modules invert the dependency graph in a way that breaks standard Hilt injection, forcing the `@EntryPoint` workaround. Knowing this trade-off (and *why* — the inverted graph) distinguishes someone who's shipped dynamic features from someone who's only read about them."},{t:"note",text:"Modularization unlocks on-demand delivery (Dynamic Feature Modules → smaller installs). The catch: dynamic modules depend *on* `:app` (inverted graph), so Hilt can't aggregate their bindings — you need `@EntryPoint` + `EntryPointAccessors`. Koin's runtime resolution sidesteps this."}]},{level:"junior",q:"Show a feature module's build.gradle.kts and explain each dependency.",a:[{t:"p",text:"A feature module's build file is where you declare what the module *is* (via a convention plugin) and what it *depends on*. Keeping it small and declarative is a sign of a well-set-up modular project — the shared configuration lives in the convention plugin, so the feature file is mostly its unique dependencies."},{t:"code",title:"A feature module build file",code:`// :feature:profile/build.gradle.kts
plugins {
    id("myapp.android.feature")     // convention plugin: applies AGP, Kotlin, Compose,
                                     // sets SDK versions, adds common deps (designsystem, etc.)
}
android { namespace = "com.myapp.feature.profile" }

dependencies {
    implementation(project(":core:data"))     // uses repositories — internal, won't leak
    implementation(project(":core:model"))     // domain models
    // designsystem + common come from the convention plugin, not repeated here

    implementation(libs.androidx.lifecycle.viewmodel.compose)   // from version catalog
    implementation(libs.hilt.android)
    ksp(libs.hilt.compiler)
}`},{t:"list",items:['**`plugins { id("myapp.android.feature") }`** — a convention plugin that captures *all* the shared config (compile SDK, Compose setup, common dependencies), so this file stays tiny and consistent with every other feature.',"**`implementation(project(\":core:data\"))`** — a project dependency using `implementation` (not `api`) so this module's *consumers* don't recompile when `:core:data`'s internals change, and can't see `:core:data`'s types.","**`libs.xxx`** — type-safe accessors from the version catalog (`libs.versions.toml`), so versions live in one place, not hardcoded here.","**`ksp(libs.hilt.compiler)`** — KSP (not KAPT) for Hilt's annotation processing — faster builds."]},{t:"p",text:"The takeaway: in a well-structured modular project, a feature's build file is *minimal* — a convention plugin line, a namespace, and a short list of `implementation(project(...))` and `libs.*` dependencies. If a feature build file is long and full of repeated config (compile SDK, Compose options, common libraries copy-pasted), that's a smell — that config belongs in a convention plugin. Small, declarative build files are a hallmark of good modularization."},{t:"note",text:"A well-set-up feature build file is tiny: a convention plugin line + namespace + a short list of `implementation(project(...))` and `libs.*` deps. Long build files with repeated config are a smell — that belongs in a convention plugin."}]},{level:"senior",q:"How do you keep the module dependency graph healthy over time?",a:[{t:"p",text:"A module graph degrades the same way any architecture does — through small, individually-reasonable additions that accumulate into tangle. Keeping it healthy means *enforcing* the graph's invariants automatically (not by review vigilance) and periodically inspecting it, because the graph *is* the architecture and a bad graph erases modularization's benefits."},{t:"list",items:["**Enforce the DAG rules with tooling** — no feature→feature edges, no upward dependencies, no cycles (Gradle catches cycles; you add checks for the rest). Module-graph-assertion plugins validate the actual dependency graph against a declared spec and *fail the build* on a forbidden edge — turning graph erosion into a red build.","**Default to `implementation`, audit `api`** — `api` leakage is the silent killer of build speed (it cascades recompilation); periodically audit with build scans and demote unnecessary `api` to `implementation`. This keeps compile avoidance working.","**Watch for god-modules** — a `:core:common` that everything depends on, or a module whose fan-in keeps growing, is becoming a bottleneck; split it before every change recompiles the world.","**Visualize the graph** — generate and review the module dependency graph (Graphviz/module-graph plugins) periodically to spot the DAG getting deeper (kills parallelism), sideways edges creeping in, or hubs forming.","**Structural prevention** — `internal` visibility + api/impl splits make forbidden dependencies *non-compilable* (can't import what you can't see), which is stronger than any CI check.","**CODEOWNERS on build files** — route new dependency edges to the platform team for review, so graph changes are deliberate."]},{t:"p",text:"The principle: every architecture decays at the rate reviewers get tired, so durable graph health comes from *machine-checked invariants* (module-graph assertions in CI, structural visibility) rather than convention. Modularization's benefits — enforced boundaries, build speed, team autonomy — *all* depend on the graph staying a clean, wide, acyclic DAG with `implementation` boundaries. Let the graph erode (sideways edges, `api` leakage, god-modules, deepening chains) and you get the overhead of modules with none of the payoff. So healthy-graph maintenance is the ongoing discipline that keeps modularization worth it."},{t:"note",text:"Keep the graph healthy with machine-checked invariants (module-graph assertions fail the build on forbidden edges), `implementation`-by-default with `api` audits, splitting god-modules, and periodic graph visualization. The graph *is* the architecture — let it erode and you get module overhead with no payoff."}]}],Ax={id:"modularization",title:"Modular Architecture",tagline:"Feature/core module graphs, api vs implementation, cross-module navigation & DI, convention plugins, build performance, and KMP module structure.",tags:["Android","KMP","Gradle","Build Speed","Hilt","Boundaries"],content:xx,qa:Sx},Cx=[{heading:"Null safety — nullability is part of the type",blocks:[{t:"p",text:"Kotlin's headline feature is **null safety**: the type system distinguishes a type that can hold null from one that can't. `String` is a non-null type — it can *never* be null, guaranteed by the compiler. `String?` is a nullable type — it might be null. This turns the billion-dollar `NullPointerException` from a runtime crash into a compile-time check: you literally cannot assign null to a non-null type, and you can't use a nullable value without handling the null case."},{t:"code",title:"The compiler enforces nullability",code:`var a: String = "hello"
a = null            // COMPILE ERROR — String is non-null

var b: String? = "hi"
b = null            // OK — String? is nullable

val len = b.length  // COMPILE ERROR — b might be null, must handle it first`},{t:"list",items:["The guarantee is *compile-time*: non-null types can't hold null, and nullable types force you to handle the null before use. NPEs become nearly impossible in pure Kotlin.","This shifts null handling from 'remember to check everywhere' (Java) to 'the compiler won't let you forget' — a systemic safety improvement."]}]},{heading:"The null-handling operators",blocks:[{t:"code",title:"Safe call, Elvis, not-null assertion",code:`val user: User? = getUser()

// ?. safe call — returns null if the receiver is null (short-circuits)
val name: String? = user?.name
val city: String? = user?.address?.city   // chains — null if any link is null

// ?: Elvis — provide a default when the left side is null
val displayName: String = user?.name ?: "Guest"

// ?.let — run a block only if non-null
user?.let { u -> println(u.name) }         // block skipped if user is null

// !! not-null assertion — assert non-null, THROW NPE if wrong (dangerous)
val forced: String = user!!.name           // crashes if user is null`},{t:"list",items:["**`?.` (safe call)** — access a member only if the receiver is non-null; the whole expression is null otherwise. Chainable (`a?.b?.c`), short-circuiting at the first null.",'**`?:` (Elvis operator)** — supply a fallback value (or throw / return) when the left side is null: `name ?: "default"`, `id ?: return`, `token ?: throw IllegalStateException()`.',"**`?.let { }`** — execute a block with the non-null value, skipped entirely if null — a common way to 'do something only if present'.","**`!!` (not-null assertion)** — force a nullable into non-null, throwing NPE if it's actually null. It reintroduces the exact crash null safety prevents, so it's a code smell — use only when you can *prove* non-null and the alternative is worse. Overusing `!!` is a red flag in review."]}]},{heading:"Smart casts — the compiler narrows types for you",blocks:[{t:"p",text:"After you *check* a value's type or nullability, Kotlin **smart-casts** it — automatically treating it as the narrower type within that scope, so you don't cast manually."},{t:"code",title:"Smart casts in action",code:`fun describe(x: Any) {
    if (x is String) {
        // x is smart-cast to String here — call String methods directly
        println(x.length)
    }
}

fun greet(name: String?) {
    if (name != null) {
        // name is smart-cast to non-null String inside this branch
        println(name.uppercase())
    }
}`},{t:"list",items:["After `if (x is Type)`, `x` is treated as `Type` in that branch. After `if (x != null)`, `x` is non-null there. Also works with `&&`, early returns, and `when` branches.","**Smart cast requires the value can't change between check and use** — it works on `val`s and local `var`s, but **not** on mutable properties that could be changed by another thread or a custom getter (a `var` property could return different values on each access). That's why you sometimes must copy a nullable property to a local val first: `val n = name; if (n != null) n.uppercase()`."]}]},{heading:"val vs var, and immutability",blocks:[{t:"list",items:["**`val`** — a read-only reference (assigned once, can't be reassigned). **`var`** — a mutable reference (can be reassigned). Prefer `val` by default; reach for `var` only when reassignment is genuinely needed. `val` makes code easier to reason about (the reference won't change under you).","**`val` is not deep immutability**: `val list = mutableListOf(1)` — you can't reassign `list`, but you *can* `list.add(2)` (the object it points to is mutable). Immutability of the *reference* is separate from immutability of the *object*. For an immutable collection, use `listOf()` (read-only) or a truly immutable type.","**`const val`** — a compile-time constant (only for top-level or `object` primitives/Strings); inlined at compile time. Different from a regular `val`, which is computed at runtime."]}]},{heading:"Kotlin type system essentials",blocks:[{t:"list",items:["**Everything is an object**: there are no Java-style primitives in the language — `Int`, `Boolean`, `Double` are classes. The compiler optimizes them to JVM primitives where possible (a non-null `Int` becomes `int`), but a nullable `Int?` becomes a boxed `Integer`.","**`Any`** — the root of all non-null types (like Java's `Object`). **`Any?`** — the root of *everything*, including nullables. **`Unit`** — the type of functions that return no meaningful value (like `void`, but an actual type/object). **`Nothing`** — the type that has no values; a function returning `Nothing` never returns normally (it always throws or loops forever) — used for `throw`, `TODO()`, and to make the compiler's flow analysis smarter.","**Type inference**: Kotlin infers types, so you rarely write them — `val x = 5` infers `Int`. You add explicit types for public API clarity or when inference is ambiguous."]}]},{heading:"Platform types — the Java interop gap",blocks:[{t:"p",text:"When Kotlin calls Java code, Java has *no* nullability information (any reference can be null in Java). Kotlin represents these as **platform types** (written `String!` in errors/docs), meaning 'nullability unknown'. The compiler *relaxes* null checks for them — it lets you treat a platform type as non-null, trusting you. The risk: if the Java method actually returns null and you treated it as non-null, you get an NPE at runtime — the one place Kotlin's null safety can be bypassed."},{t:"list",items:["**Why they exist**: forcing every Java call to be nullable would make interop unbearable (every result would need `?.`); treating every Java call as non-null would hide real nulls. Platform types are the pragmatic middle — you decide.","**Best practice**: when consuming Java APIs, *explicitly annotate* the type you assign to (`val name: String = javaObj.getName()` asserts non-null and will NPE early if wrong; `val name: String? = ...` treats it as nullable and safe). Don't let platform types flow deep into your code untyped.","**Nullability annotations help**: Java code annotated with `@Nullable`/`@NonNull` (JetBrains, AndroidX, JSpecify) *does* carry nullability into Kotlin, eliminating the platform-type gap — which is why AndroidX APIs interop cleanly."]},{t:"note",text:'Interview-ready: "Kotlin encodes nullability in the type system (`String` vs `String?`), enforced at compile time, handled with `?.`, `?:`, `?.let`, and (sparingly) `!!`. Smart casts narrow types after checks. The one hole is *platform types* from unannotated Java — nullability-unknown types the compiler trusts you on, which can still NPE; annotate them explicitly at the boundary."'}]}],Tx=[{level:"junior",q:"How does Kotlin's null safety work?",a:[{t:"p",text:"**The concept**: Kotlin makes nullability part of the type. `String` is a non-null type that can *never* hold null, and `String?` is a nullable type that might. The compiler enforces this: you can't assign null to a non-null type, and you can't call methods on a nullable value without first handling the possibility of null. This turns NullPointerExceptions from runtime crashes into compile-time errors."},{t:"code",title:"The compiler catches null mistakes",code:`var name: String = "Kotlin"
name = null           // compile error
var nick: String? = null   // fine — nullable
println(nick.length)       // compile error — must handle null first`},{t:"p",text:"You handle nullables with a small set of operators: `?.` (safe call — returns null instead of crashing), `?:` (Elvis — provide a default), `?.let { }` (run a block only if non-null), and `!!` (assert non-null, throwing if wrong — to be avoided). The big win is systemic: instead of *remembering* to null-check everywhere like in Java, the compiler *won't let you forget*. The only escape hatch is platform types from Java code, which the compiler can't verify."}]},{level:"junior",q:"What do the ?., ?:, and !! operators do?",a:[{t:"list",items:["**`?.` (safe call)**: accesses a member only if the receiver is non-null; otherwise the whole expression evaluates to null. `user?.name` is the name, or null if `user` is null. It chains and short-circuits: `user?.address?.city` is null if any link is null.","**`?:` (Elvis operator)**: provides a fallback when the left side is null. `user?.name ?: \"Guest\"` gives the name or 'Guest'. The right side can also be a `return` or `throw`: `val id = user?.id ?: return`.","**`!!` (not-null assertion)**: forces a nullable value to non-null, throwing a `NullPointerException` if it's actually null. `user!!.name` crashes if `user` is null."]},{t:"p",text:"The first two are the safe, idiomatic tools — `?.` to safely navigate and `?:` to supply defaults. `!!` is the dangerous one: it reintroduces the exact NPE crash that null safety exists to prevent, so it's considered a code smell. Use it only when you can genuinely *prove* the value is non-null and restructuring isn't practical. A method full of `!!` usually signals nullability that should have been handled properly upstream."}]},{level:"junior",q:"What is a smart cast?",a:[{t:"p",text:"**The concept**: after you check a value's type or nullability, Kotlin automatically treats it as the narrower type within that scope — you don't write an explicit cast. If you write `if (x is String)`, then inside that block `x` is already a `String`, so you can call `x.length` directly. Similarly, after `if (name != null)`, `name` is treated as non-null in that branch."},{t:"code",title:"No manual casting needed",code:`fun handle(value: Any?) {
    if (value is String) {
        println(value.uppercase())   // smart-cast to String
    }
    if (value != null) {
        println(value.toString())    // smart-cast to non-null
    }
}`},{t:"p",text:"It saves boilerplate and is safe because the compiler knows the check holds. The one limitation to know: smart casts require the value *can't change* between the check and the use. It works on `val`s and local `var`s, but **not** on mutable `var` properties (a custom getter or another thread could change the value between the check and the call). In those cases you copy to a local val first: `val n = name; if (n != null) n.uppercase()`. This 'why won't it smart-cast my property?' situation is a common Kotlin gotcha."}]},{level:"junior",q:"What is the difference between val and var?",a:[{t:"p",text:"**`val`** declares a read-only reference — it's assigned once and can't be reassigned. **`var`** declares a mutable reference — you can reassign it. The guidance is to prefer `val` by default and use `var` only when you genuinely need to reassign, because immutable references are easier to reason about (the value won't change out from under you)."},{t:"p",text:"The crucial subtlety: **`val` makes the *reference* immutable, not the *object* it points to.** `val list = mutableListOf(1, 2)` means you can't reassign `list` to a different list, but you *can* still mutate its contents with `list.add(3)`. If you want the contents immutable too, use a read-only type like `listOf()`. So `val` ≠ deep immutability — it's 'this variable always points to the same object', while that object may itself be mutable. There's also `const val` for compile-time constants (top-level or in an `object`, primitives/Strings only), which is inlined at compile time, unlike a regular `val` that's computed at runtime."}]},{level:"junior",q:"What are Unit and Nothing in Kotlin?",a:[{t:"p",text:"**`Unit`** is the type of a function that doesn't return a meaningful value — Kotlin's equivalent of `void`, except it's a real type with a single value (the `Unit` object). A function with no explicit return type returns `Unit`. It's a proper type, so you can use it as a generic argument (`Function<Unit>`), which `void` can't do."},{t:"p",text:"**`Nothing`** is the type that has *no values at all* — nothing can ever be an instance of it. A function declared to return `Nothing` *never returns normally*: it always throws an exception or loops forever. `throw`, `TODO()`, and `error()` all return `Nothing`. This is genuinely useful for the compiler's flow analysis: because a `Nothing`-returning expression can't complete, the compiler knows code after it is unreachable, and it lets `Nothing` fit anywhere in type inference — which is why `val x: String = someValue ?: throw Exception()` compiles (the `throw` branch has type `Nothing`, compatible with `String`). So `Unit` = 'returns, but nothing useful'; `Nothing` = 'never returns at all'."}]},{level:"senior",q:"What are platform types, and how do they create a hole in Kotlin's null safety?",a:[{t:"p",text:"**The concept**: when Kotlin calls into Java, Java carries no nullability information — any Java reference could be null, and the Java type system doesn't distinguish. Kotlin represents these Java-originated types as **platform types** (shown as `Type!` in errors, e.g. `String!`), meaning 'nullability is unknown'. For platform types, the compiler *relaxes* its null checks — it lets you treat the value as either nullable or non-null, trusting your choice, rather than forcing safe calls everywhere."},{t:"list",items:["**The hole**: if you treat a platform type as non-null (`val name: String = javaObj.getName()`) but the Java method actually returns null at runtime, you get a `NullPointerException` — the exact crash Kotlin's null safety normally prevents. It's the one place pure-Kotlin's guarantee can be silently bypassed.","**Why they exist rather than a stricter rule**: if every Java call returned a nullable type, interop would be miserable (safe calls on everything, even values that are never null); if every Java call returned non-null, real nulls would slip through as NPEs everywhere. Platform types are the pragmatic compromise: the compiler defers to you at the boundary.","**Where the NPE actually fires**: it can be immediate (if you dereference right away) or *delayed* — a null platform value assigned to a non-null Kotlin field can travel deep into your code and crash far from the Java call, making it hard to trace. This delayed failure is the dangerous case."]},{t:"list",items:["**Mitigation 1 — annotate at the boundary**: explicitly declare the type when consuming Java. `val name: String? = javaObj.getName()` treats it as nullable (safe); `val name: String = javaObj.getName()` asserts non-null and, if wrong, throws *immediately at the boundary* (an early, traceable crash) rather than deep later. Never let raw platform types flow untyped into your codebase.","**Mitigation 2 — nullability annotations**: Java code annotated with `@Nullable`/`@NonNull` (JetBrains, AndroidX) or JSpecify carries nullability *into* Kotlin, eliminating the platform type entirely — the type becomes a proper `String` or `String?`. This is why modern AndroidX APIs interop cleanly with full null safety, while old unannotated Java libraries are risky."]},{t:"p",text:"**The senior framing**: Kotlin's null safety is airtight *within* Kotlin, but Java interop is a trust boundary. Platform types make interop ergonomic at the cost of a null-safety gap; the discipline is to *pin down nullability explicitly at the Java boundary* so the uncertainty doesn't leak inward, and to prefer annotated Java libraries."}]},{level:"senior",q:"Why won't the compiler smart-cast a nullable var property, and what are the ways around it?",a:[{t:"p",text:"**The reason**: smart casting is only sound if the value provably *cannot change* between the null-check and the use. A mutable `var` property fails that condition for two reasons: (1) another thread could reassign it after your check but before your use (a data race), and (2) the property could be a *custom getter* that computes a new value on every access — so calling it twice might return non-null then null. The compiler can't prove stability, so it refuses to smart-cast, giving the error 'smart cast to X is impossible, because the property could have been changed'."},{t:"code",title:"The problem and the fixes",code:`class Foo {
    var name: String? = null
    fun p() {
        if (name != null) {
            // println(name.length)   // ERROR: name is a mutable property
        }
    }
}

// Fix 1: copy to a local val (most common)
fun p2(foo: Foo) {
    val n = foo.name
    if (n != null) println(n.length)   // n is a stable local val — smart-casts fine
}

// Fix 2: let / safe call
foo.name?.let { println(it.length) }

// Fix 3: Elvis to bail early
val n = foo.name ?: return`},{t:"list",items:["**Fix 1 — capture in a local `val`**: the most idiomatic. A local `val` is stable (can't be reassigned, no getter re-evaluation), so the compiler smart-casts it freely. This is the standard workaround.","**Fix 2 — `?.let { }`**: the safe call captures the value once and passes the non-null into the block — sidestepping the property re-read entirely.","**Fix 3 — Elvis with early return** (`val x = prop ?: return`): pull the value into a non-null local and bail if null.","**Why not just `!!`**: `foo.name!!` works but reintroduces NPE risk and doesn't fix the underlying race — if another thread nulls it between the `!!` evaluation... actually `!!` reads once so it's safe from re-read, but it throws instead of handling, so it's inferior to capturing a local val and handling the null case."]},{t:"p",text:"**The deeper point**: the restriction is protecting you from a real concurrency/getter hazard, not being pedantic. Capturing the value in a local val *snapshots* it, which is both what makes the smart cast sound and what makes the code correct under concurrency. Understanding *why* (races + custom getters) rather than just memorizing 'copy to a local' is the senior-level answer."}]},{level:"junior",q:"What is the difference between a nullable and a non-nullable type?",a:[{t:"p",text:"In Kotlin, `String` (non-nullable) can *never* hold `null`, while `String?` (nullable) can. This is enforced at *compile time*: assigning `null` to a non-nullable type, or calling a method on a nullable type without a null check, is a compile error. Nullability is part of the type system, so most NullPointerExceptions are caught before you run."},{t:"code",title:"Nullable vs non-nullable",code:`var a: String = "hi"
a = null            // compile error — String can't be null

var b: String? = "hi"
b = null            // OK — String? is nullable
b.length            // compile error — must handle null (b?.length)`},{t:"list",items:["**Non-nullable (`T`)** — guaranteed non-null; can be dereferenced freely.","**Nullable (`T?`)** — may be null; the compiler forces a null check before use.","**Compile-time enforcement** — nullability bugs surface at build time, not runtime.","**Default is non-nullable** — you opt into nullability with `?`."]},{t:"note",text:"T (non-nullable) can never hold null; T? (nullable) can. The compiler enforces this — assigning null to T or dereferencing T? without a check is a compile error. Nullability is in the type system, so most NPEs are caught at build time. Types are non-nullable by default; opt in with ?."}]},{level:"junior",q:"What is the difference between a safe cast (as?) and an unsafe cast (as)?",a:[{t:"p",text:"`as` is an *unsafe* cast — it throws `ClassCastException` if the object isn't the target type. `as?` is a *safe* cast — it returns `null` instead of throwing when the cast fails, giving a nullable result you handle. Use `as?` (often with `?:`) when the type isn't guaranteed."},{t:"code",title:"as vs as?",code:`val x: Any = "hello"
val s1 = x as String       // OK here, but throws ClassCastException if x weren't a String
val s2 = x as? String      // String? — null if not a String
val len = (x as? String)?.length ?: 0   // safe handling`},{t:"list",items:["**`as`** — throws `ClassCastException` on mismatch; use when the type is certain.","**`as?`** — returns `null` on mismatch; safe for uncertain types.","**Pair with `?:`** — provide a default for the null case.","**Smart casts** — often you can avoid casts entirely with `is` checks."]},{t:"note",text:"as is an unsafe cast (throws ClassCastException on mismatch — use when certain); as? is a safe cast (returns null on mismatch — use when uncertain, often with ?: for a default). Prefer is checks + smart casts to avoid explicit casts where possible."}]},{level:"junior",q:"What is the difference between == and === in Kotlin?",a:[{t:"p",text:"`==` checks *structural* equality — it calls `.equals()` (and is null-safe: `a == b` works even if `a` is null). `===` checks *referential* equality — whether two references point to the *same object* in memory. For data classes, `==` compares field values; `===` compares identity."},{t:"code",title:"Structural vs referential",code:`val a = User("Sam"); val b = User("Sam")   // data class
a == b       // true — same field values (equals)
a === b      // false — different objects
a === a      // true — same reference`},{t:"list",items:["**`==`** — structural; calls `equals()`; null-safe (translates to `a?.equals(b) ?: (b === null)`).","**`===`** — referential; same-object identity.","**Data classes** — `==` compares all properties; `===` is identity.","**Java note** — Kotlin's `==` is Java's `.equals()`, not Java's `==` (which is identity)."]},{t:"note",text:"== is structural equality (calls equals(), null-safe); === is referential (same object in memory). For data classes, == compares field values, === compares identity. Note: Kotlin's == is Java's .equals(), while === is Java's == — the opposite of what Java devs expect."}]},{level:"junior",q:"What do requireNotNull, checkNotNull, and error do?",a:[{t:"p",text:"These are precondition helpers that also *smart-cast* to non-null. `requireNotNull(x)` throws `IllegalArgumentException` if `x` is null (for validating function arguments); `checkNotNull(x)` throws `IllegalStateException` (for validating object/state); `error(msg)` throws `IllegalStateException` and returns `Nothing`. On success, `requireNotNull`/`checkNotNull` return the non-null value."},{t:"code",title:"Precondition + smart cast",code:`fun process(id: String?) {
    val validId = requireNotNull(id) { "id must not be null" }   // arg validation
    // validId is String (non-null) from here
}
fun useState() {
    checkNotNull(currentUser) { "must be logged in" }   // state validation
}
val x = map[key] ?: error("missing key")   // throw with Nothing return`},{t:"list",items:["**`requireNotNull`** — `IllegalArgumentException`; validate arguments; returns the non-null value.","**`checkNotNull`** — `IllegalStateException`; validate state; returns the non-null value.","**`error(msg)`** — throw `IllegalStateException`; returns `Nothing` (usable in `?:`).","**`require`/`check`** — boolean variants for general preconditions."]},{t:"note",text:"requireNotNull (IllegalArgumentException — args), checkNotNull (IllegalStateException — state) throw on null AND return the smart-cast non-null value; error(msg) throws IllegalStateException returning Nothing (great in ?:). Plus require/check for boolean preconditions. Cleaner and more intentional than !!."}]},{level:"junior",q:"How do you use the Elvis operator with return or throw?",a:[{t:"p",text:"Because `return` and `throw` are expressions of type `Nothing`, you can put them on the right side of the Elvis operator `?:` — so `val x = nullable ?: return` exits the function early when the value is null, and after that line `x` is smart-cast to non-null. This is a clean 'guard clause' pattern."},{t:"code",title:"Elvis guard clauses",code:`fun greet(user: User?) {
    val name = user?.name ?: return          // bail out if null
    println("Hello $name")                   // name is non-null here
}
fun require(id: String?): String = id ?: throw IllegalArgumentException("null id")`},{t:"list",items:["**`?: return`** — early-return when null; the rest of the function has a non-null value.","**`?: throw`** — fail fast when null.","**Works because `Nothing`** — `return`/`throw` produce `Nothing`, which fits any type.","**Guard clauses** — flatten nested null checks into linear code."]},{t:"note",text:"return/throw are Nothing-typed expressions, so they fit the right side of ?:. `val x = nullable ?: return` (or `?: throw ...`) exits early on null and smart-casts x to non-null afterward — clean guard clauses that flatten nested null checks."}]},{level:"junior",q:"How do you use let for null handling?",a:[{t:"p",text:"`?.let { }` runs a block *only if* the value is non-null, with the value as `it` (smart-cast to non-null inside). It's a concise way to execute code conditionally on non-null, and combined with `?:` you can handle both branches. It also scopes a temporary non-null copy, useful for nullable `var`s that can't be smart-cast."},{t:"code",title:"?.let for non-null blocks",code:`user?.let {
    println(it.name)       // runs only if user != null; it is non-null
    save(it)
}
// Both branches:
val result = value?.let { transform(it) } ?: default
// Smart-cast a nullable var safely:
nullableVar?.let { render(it) }   // it is a stable non-null copy`},{t:"list",items:["**`?.let { }`** — block runs only if non-null; `it` is the non-null value.","**With `?:`** — handle the null branch (`?.let { } ?: else`).","**Nullable `var`s** — `?.let` captures a stable copy the compiler can smart-cast.","**Don't overuse** — a plain `if (x != null)` is clearer for simple cases."]},{t:"note",text:"?.let { } runs its block only if the value is non-null (it = non-null value); pair with ?: for the null branch. It's especially useful to safely use a nullable var (captures a stable copy). Don't overuse — a plain if (x != null) is clearer for simple non-null checks."}]},{level:"senior",q:"When is !! acceptable, and how do you design it out?",a:[{t:"p",text:"`!!` asserts non-null and throws `NullPointerException` if wrong — it defeats null safety, so it's a code smell in most places. It's occasionally acceptable when you *know* a value is non-null but the compiler can't (e.g. after external validation), but you should prefer designing the nullability away: non-null types, `requireNotNull` with a message, `lateinit`, or restructuring so the value is never nullable."},{t:"list",items:["**`!!`** — assert non-null; throws NPE on failure; loses compile-time safety.","**Rarely acceptable** — when non-nullness is guaranteed but unprovable to the compiler.",'**Better** — `requireNotNull(x) { "why" }` (meaningful message + smart cast), `?.let`, `?:`.',"**Design out** — use non-nullable types, `lateinit` for late-init, or model state so it's never null.","**Never chain `!!`** — `a!!.b!!.c!!` hides which one is null and is a strong smell."]},{t:"note",text:"!! asserts non-null (NPE if wrong), defeating null safety — a smell. Rarely OK when non-nullness is guaranteed but unprovable. Prefer requireNotNull(x){msg} (message + smart cast), ?.let/?:, lateinit, or restructuring so the value is never nullable. Never chain a!!.b!!.c!! (hides which is null)."}]},{level:"junior",q:"What do takeIf and takeUnless do?",a:[{t:"p",text:"`takeIf { predicate }` returns the value if the predicate is true, else `null`; `takeUnless { predicate }` is the inverse (returns the value if the predicate is *false*). They turn a condition into a nullable result you can chain with `?.` and `?:` — handy for conditional pipelines."},{t:"code",title:"takeIf / takeUnless",code:`val validEmail = email.takeIf { it.contains("@") }        // email or null
val nonEmpty = input.takeUnless { it.isBlank() } ?: "default"
password.takeIf { it.length >= 8 }?.let { register(it) }  // proceed only if valid`},{t:"list",items:["**`takeIf { }`** — value if predicate true, else null.","**`takeUnless { }`** — value if predicate false, else null.","**Chainable** — combine with `?.let`/`?:` for conditional flows.","**Readable guards** — express 'use this only if valid' inline."]},{t:"note",text:`takeIf { p } returns the value if p is true else null; takeUnless { p } is the inverse. They convert a condition into a nullable, chainable with ?.let/?: for 'use this only if valid' pipelines (e.g. email.takeIf { it.contains("@") }).`}]},{level:"senior",q:"What's the difference between List<T>?, List<T?>, and List<T?>?",a:[{t:"p",text:"Nullability position matters. `List<T>?` is a *nullable list* of non-null items (the list itself may be null, but its elements aren't). `List<T?>` is a *non-null list* of nullable items (the list exists, but items may be null). `List<T?>?` is both. Reading the position of `?` tells you exactly what can be null."},{t:"code",title:"Where the ? is",code:`val a: List<String>?  = null            // the list may be null; items non-null
val b: List<String?>  = listOf("x", null)  // list non-null; items may be null
val c: List<String?>? = null            // both may be null

a?.forEach { it.length }                // safe call on the list
b.forEach { it?.length }                // safe call on each item`},{t:"list",items:["**`List<T>?`** — the list reference may be null; elements are non-null.","**`List<T?>`** — the list is non-null; elements may be null.","**`List<T?>?`** — both may be null.","**Read the `?` position** — it tells you precisely what's nullable."]},{t:"note",text:"Position of ? matters: List<T>? = nullable list of non-null items; List<T?> = non-null list of nullable items; List<T?>? = both. Read where the ? sits to know exactly what can be null — the list reference, the elements, or both."}]},{level:"junior",q:"How do Kotlin's numeric types work, and why is there no implicit conversion?",a:[{t:"p",text:"Kotlin has `Byte`, `Short`, `Int`, `Long`, `Float`, `Double` (and unsigned variants). Unlike Java, Kotlin does *not* implicitly widen numbers — you can't assign an `Int` to a `Long` variable without an explicit conversion (`toLong()`). This prevents subtle bugs from unexpected conversions; you convert explicitly with `toX()` functions."},{t:"code",title:"Explicit conversions",code:`val i: Int = 10
val l: Long = i          // compile error — no implicit widening
val l2: Long = i.toLong()   // explicit
val d: Double = i.toDouble()
// Literals: 10L (Long), 10.0 (Double), 10f (Float), 0xFF (hex), 1_000_000 (underscores)`},{t:"list",items:["**Types** — Byte/Short/Int/Long, Float/Double, and UByte/UShort/UInt/ULong.","**No implicit widening** — convert explicitly (`toLong()`, `toDouble()`).","**Literals** — `L` suffix for Long, `f` for Float, `0x`/`0b` prefixes, `_` separators.","**Why** — avoids accidental precision/type surprises common in Java."]},{t:"note",text:"Kotlin numeric types (Byte/Short/Int/Long, Float/Double, unsigned variants) require EXPLICIT conversion — no implicit widening (Int → Long needs toLong()), preventing accidental conversion bugs. Literals: 10L, 10.0, 10f, 0xFF, 1_000_000. Convert with toX() functions."}]},{level:"senior",q:"How does boxing work for nullable primitives (Int vs Int?)?",a:[{t:"p",text:"A non-nullable `Int` compiles to the JVM primitive `int` (no object, no allocation). But `Int?` must be able to hold `null`, which primitives can't, so it compiles to the boxed `java.lang.Integer` object. So nullable primitives (and generics like `List<Int>`) involve *boxing* — extra allocation and indirection, worth knowing for hot paths."},{t:"list",items:["**`Int`** → primitive `int` (no allocation).","**`Int?`** → boxed `Integer` (can hold null; allocates).","**Generics box** — `List<Int>` holds `Integer`s (type params are objects).","**Performance** — avoid unnecessary nullable primitives / boxing in tight loops; use `IntArray` over `List<Int>` for primitive arrays."]},{t:"code",title:"Boxing",code:`val a: Int = 5      // primitive int
val b: Int? = 5     // boxed Integer (nullable)
val arr = IntArray(100)     // primitive int[], no boxing
val list = List(100) { it } // List<Int> -> boxed Integers`},{t:"note",text:"Int compiles to primitive int (no allocation); Int? compiles to boxed Integer (to hold null). Generics also box (List<Int> holds Integers). So nullable primitives and generic collections allocate — in hot paths prefer non-null primitives and IntArray over List<Int> to avoid boxing."}]},{level:"senior",q:"How does Kotlin enforce null safety at compile time versus runtime?",a:[{t:"p",text:"Null safety is primarily a *compile-time* feature — the compiler tracks nullable vs non-nullable types and rejects unsafe dereferences, so there's little runtime overhead. At runtime, Kotlin adds *intrinsic null checks* at boundaries (e.g. non-null parameters of public functions, values crossing from Java) so a null sneaking in from Java throws a clear `NullPointerException` early rather than corrupting state."},{t:"list",items:["**Compile-time** — the type system prevents unsafe null access; most safety costs nothing at runtime.","**Runtime intrinsics** — `Intrinsics.checkNotNull` on non-null public API params and Java-boundary values.","**Java interop** — platform types can still bring null in; runtime checks catch it at the boundary.","**Result** — safety mostly free, with guard rails where the compiler can't see (Java)."]},{t:"note",text:"Null safety is mostly compile-time (the type system rejects unsafe dereferences — little runtime cost). At runtime, Kotlin inserts intrinsic null checks on non-null public-API parameters and Java-boundary values, so null sneaking in from Java fails fast with a clear NPE rather than corrupting state."}]},{level:"senior",q:"What is a definitely non-nullable type (T & Any)?",a:[{t:"p",text:"`T & Any` is a *definitely non-nullable* type, used mainly in generics to say 'this type parameter, but never nullable'. When a generic `T` could be a nullable type (e.g. `T = String?`), `T & Any` intersects it with `Any` (the non-nullable root) to guarantee non-null. It's most relevant when overriding Java generic methods that must return non-null."},{t:"code",title:"Definitely non-nullable",code:`fun <T> elvisLike(value: T?, default: T & Any): T & Any =
    value ?: default    // guarantees a non-null result even if T is nullable`},{t:"list",items:["**`T & Any`** — intersection making a possibly-nullable `T` non-nullable.","**Generics** — ensures a type parameter is treated as non-null.","**Java interop** — implement Java `@NotNull` generic methods precisely.","**Niche** — mostly library/interop code; rarely needed in app code."]},{t:"note",text:"T & Any is a definitely non-nullable type — it intersects a possibly-nullable generic T with Any (non-nullable root) to guarantee non-null. Mainly for generics and implementing Java @NotNull generic methods precisely. Niche; mostly library/interop, rarely needed in app code."}]},{level:"junior",q:"What is lateinit, and how does isInitialized work?",a:[{t:"p",text:"`lateinit var` lets you declare a non-null `var` without initializing it immediately, promising to set it before first use — useful for dependency injection or lifecycle-initialized fields. Accessing it before assignment throws `UninitializedPropertyAccessException`. You can check `::property.isInitialized` to see if it's been set."},{t:"code",title:"lateinit",code:`lateinit var adapter: MyAdapter    // no initial value, non-null

fun onCreate() {
    adapter = MyAdapter()          // initialized before use
}
fun safeUse() {
    if (::adapter.isInitialized) adapter.refresh()
}`},{t:"list",items:["**`lateinit var`** — non-null, initialized later; only for `var`, non-primitive, non-nullable types.","**Access before init** — throws `UninitializedPropertyAccessException`.","**`::prop.isInitialized`** — check whether it's been assigned.","**Use** — DI-injected fields, view binding, values set in lifecycle callbacks."]},{t:"note",text:"lateinit var declares a non-null var initialized later (DI, view binding, lifecycle) — accessing before assignment throws UninitializedPropertyAccessException. Check ::prop.isInitialized. Only for var, non-nullable, non-primitive types. Alternative to nullable + !! when init is guaranteed but deferred."}]},{level:"junior",q:"What are Any and Any?, and how does Kotlin's type hierarchy work?",a:[{t:"p",text:"`Any` is the root of all *non-nullable* types (like Java's `Object`, but without nullability) — every non-null class is an `Any`. `Any?` is the root of *all* types including nullable ones. At the bottom is `Nothing` (subtype of everything, no instances). So the hierarchy goes `Any?` at the top, `Any` below it (non-null), and `Nothing`/`Nothing?` at the bottom."},{t:"list",items:["**`Any`** — supertype of all non-nullable types (has `equals`/`hashCode`/`toString`).","**`Any?`** — supertype of everything, including nullable types.","**`Nothing`** — the bottom type; subtype of every type; no instances (for `throw`/infinite loops).","**Hierarchy** — `Any?` ⊇ `Any` ⊇ your types ⊇ `Nothing`."]},{t:"note",text:"Any = root of all non-nullable types (Java's Object minus nullability). Any? = root of everything (including nullable). Nothing = bottom type (subtype of all, no instances — for throw/infinite loops). Hierarchy: Any? ⊇ Any ⊇ your types ⊇ Nothing."}]},{level:"junior",q:"How do you invoke a nullable function type safely?",a:[{t:"p",text:"A nullable lambda/callback (`(() -> Unit)?`) can't be called directly — you invoke it with `?.invoke()`, which calls it only if non-null. This is common for optional callbacks (an `onClick` that might not be set)."},{t:"code",title:"Nullable callback",code:`var onComplete: (() -> Unit)? = null
fun finish() {
    onComplete?.invoke()          // calls only if set
}
var onResult: ((Int) -> Unit)? = null
onResult?.invoke(42)`},{t:"list",items:["**`callback?.invoke(args)`** — call only if non-null (can't use `callback()` on a nullable type).","**Optional callbacks** — a nullable function type models 'maybe set'.","**With `?:`** — provide a default action if unset.","**Alternative** — a non-null default (`{ }`) avoids the nullability entirely."]},{t:"note",text:"Invoke a nullable function type with block?.invoke(args) (you can't call block() on a nullable type) — runs only if set. Models optional callbacks. Or give it a non-null default no-op ({ }) to avoid nullability. Pair with ?: for a fallback action."}]},{level:"junior",q:"How does smart casting work with when and is?",a:[{t:"p",text:"After an `is` check, the compiler *smart-casts* the variable to that type within the scope where the check holds — no explicit cast needed. In a `when` on a sealed type, each `is` branch smart-casts to the specific subtype, and the compiler verifies exhaustiveness. This makes type-based branching clean and safe."},{t:"code",title:"Smart cast in when",code:`when (result) {
    is Success -> render(result.data)   // result smart-cast to Success
    is Error   -> show(result.message)  // smart-cast to Error
    Loading    -> showSpinner()
}   // exhaustive for a sealed type — no else needed`},{t:"list",items:["**`is` smart-cast** — after `if (x is T)`, `x` is `T` in that scope.","**`when` branches** — each `is` branch smart-casts to the subtype.","**Exhaustiveness** — a `when` over a sealed type needs no `else` if all cases are covered.","**Limitation** — smart cast requires the value can't change between check and use (e.g. `val`, local; not a mutable `var` property)."]},{t:"note",text:"After an is check, the compiler smart-casts the variable to that type in scope (no explicit cast). In a when over a sealed type, each is branch smart-casts to the subtype and the compiler enforces exhaustiveness (no else). Caveat: smart cast needs the value to be stable (val/local, not a mutable var property)."}]},{level:"junior",q:"When should you annotate types versus rely on inference?",a:[{t:"p",text:'Kotlin infers types from the initializer, so `val name = "Sam"` is `String` without annotation. Rely on inference for obvious locals; annotate explicitly for *public API* (return types, properties) for clarity and stability, when the inferred type would be too broad/narrow, or when initializing with `null`/an empty collection where inference can\'t determine the intended type.'},{t:"code",title:"Infer vs annotate",code:`val count = 0                 // inferred Int — fine
val items = mutableListOf<User>()   // annotate the element type
var result: Result<User>? = null    // annotate — null gives no info
fun getUser(): User = ...     // annotate public return type explicitly`},{t:"list",items:["**Infer** — obvious locals with a clear initializer.","**Annotate public API** — return types/properties for readability and API stability.","**Annotate when ambiguous** — `null` init, empty collections, or to widen/narrow.","**Explicit return types** — recommended for non-trivial public functions."]},{t:"note",text:'Rely on inference for obvious locals (val name = "Sam"). Annotate public API return types/properties (clarity + API stability), and when the initializer gives no/ambiguous type info (null, empty collections) or you need to widen/narrow. Explicit return types are recommended for public functions.'}]},{level:"senior",q:"How do you handle nullability at the Java interop boundary?",a:[{t:"p",text:"Java types come into Kotlin as *platform types* (`String!`) — the compiler doesn't know their nullability, so it won't force a check, but a null can sneak through and cause an NPE. Mitigate by: honoring Java nullability annotations (`@Nullable`/`@NonNull`, which Kotlin respects), explicitly typing platform values as nullable (`val s: String? = javaCall()`) to force handling, and validating at the boundary."},{t:"list",items:["**Platform types (`T!`)** — from Java; nullability unknown; the compiler is lenient (risk of NPE).","**Respect annotations** — `@Nullable`/`@NonNull`/JSpecify make Kotlin treat them as `T?`/`T`.","**Type explicitly** — assign a platform value to a `T?` to force null handling.","**Validate at the boundary** — `requireNotNull` for values you expect non-null from Java."]},{t:"note",text:"Java values enter as platform types (String!) — nullability unknown, so the compiler won't force a check and a null can cause NPE. Mitigate: honor Java @Nullable/@NonNull annotations (Kotlin respects them), explicitly type platform values as T? to force handling, and requireNotNull at the boundary for expected-non-null values."}]},{level:"junior",q:"What is the difference between an empty string and null, and what is orEmpty()?",a:[{t:"p",text:'`""` (empty string) is a valid, non-null value of length 0; `null` is the absence of a value. They\'re different: `"".length` is 0, but `null.length` won\'t compile. `orEmpty()` converts a nullable `String?` (or collection) to a non-null empty one when it\'s null — a concise way to collapse null and empty into a safe default.'},{t:"code",title:"orEmpty and null vs empty",code:`val s: String? = null
val safe = s.orEmpty()          // "" when null, else the string
val list: List<Int>? = null
val safeList = list.orEmpty()   // emptyList() when null
// isNullOrEmpty / isNullOrBlank check both conditions:
if (input.isNullOrBlank()) showError()`},{t:"list",items:['**Empty vs null** — `""` is a non-null value; `null` is absence.',"**`orEmpty()`** — nullable String/collection → non-null empty when null.","**`isNullOrEmpty()`/`isNullOrBlank()`** — check both null and empty/blank in one call.","**Simplifies** — avoids separate null and empty checks."]},{t:"note",text:'"" is a valid non-null length-0 value; null is absence — different things. orEmpty() turns a nullable String/collection into a non-null empty one when null. isNullOrEmpty()/isNullOrBlank() check both conditions at once. Collapses null-and-empty handling into concise, safe defaults.'}]},{level:"senior",q:"What is the Nothing type, and how is it used in practice?",a:[{t:"p",text:"`Nothing` is the type with *no values* — it represents a computation that never completes normally (always throws or loops forever). It's the return type of `throw` expressions and functions like `error()`/`TODO()`. Because `Nothing` is a subtype of *every* type, an expression of type `Nothing` fits anywhere, which is why `val x = nullable ?: throw ...` type-checks."},{t:"code",title:"Nothing in action",code:`fun fail(msg: String): Nothing = throw IllegalStateException(msg)
val user = findUser() ?: fail("not found")   // fail() returns Nothing, fits any type
fun notDone(): Int = TODO()                   // TODO() returns Nothing`},{t:"list",items:["**No instances** — represents 'never returns normally'.","**Subtype of everything** — a `Nothing` expression fits any expected type.","**`throw`/`error()`/`TODO()`** — all typed `Nothing`.","**Enables `?:` guards** — `x ?: throw` / `x ?: return` type-check because of `Nothing`."]},{t:"note",text:"Nothing has no values — it types computations that never return normally (throw, error(), TODO(), infinite loops). It's a subtype of every type, so a Nothing expression fits anywhere — which is why `x ?: throw ...` and `x ?: return` type-check. Use it for functions that always throw."}]},{level:"junior",q:"How do you chain multiple nullable calls safely?",a:[{t:"p",text:"Use the safe-call operator `?.` to chain — `a?.b?.c` returns `null` if *any* link in the chain is null, short-circuiting without an NPE. Combine with `?:` at the end to provide a default. This flattens what would be nested null checks in Java into one readable line."},{t:"code",title:"Safe-call chains",code:`val city = user?.address?.city ?: "Unknown"   // null if user or address is null
user?.address?.let { updateMap(it) }           // runs only if the whole chain is non-null`},{t:"list",items:["**`?.` chains** — short-circuits to `null` on the first null link.","**`?:` default** — provide a fallback for the null result.","**No nested ifs** — one line replaces multiple Java null checks.","**`?.let`** — run a block only if the whole chain resolves non-null."]},{t:"note",text:"Chain nullable calls with ?. — a?.b?.c short-circuits to null if any link is null (no NPE); end with ?: for a default. Replaces nested Java null checks with one readable line. Use ?.let to run a block only when the whole chain is non-null."}]},{level:"senior",q:"Why can't the compiler smart-cast an open or custom-getter property?",a:[{t:"p",text:"Smart casting requires the compiler to *guarantee* the value won't change between the null check and the use. It can't guarantee that for an `open`/overridable `val` (a subclass could override it with a custom getter returning different values), a `val` with a custom getter (which could return a different value each call), or a mutable `var` (another thread/code could change it). In those cases, capture it in a local `val` first."},{t:"code",title:"Capture to smart-cast",code:`class Box { val value: String? get() = compute() }   // custom getter

fun use(box: Box) {
    // if (box.value != null) box.value.length  // won't smart-cast
    val v = box.value                            // capture into a local val
    if (v != null) println(v.length)             // smart-casts fine
}`},{t:"list",items:["**Needs stability** — the value must be provably unchanged between check and use.","**Blocked by** — `open val`, custom getters, mutable `var` (esp. properties), delegated properties.","**Fix** — assign to a local `val` (immutable, stable) and check that.","**Or `?.let`** — captures a stable non-null copy for the block."]},{t:"note",text:"Smart cast needs the value provably unchanged between check and use — impossible for open vals, custom getters (could return different values each call), mutable vars, or delegated properties. Fix: capture into a local val and check that, or use ?.let (stable copy)."}]},{level:"junior",q:"What is the difference between ?: (Elvis) and ?. (safe call)?",a:[{t:"p",text:"They solve related but distinct problems. `?.` (safe call) accesses a member *only if* the receiver is non-null, yielding `null` otherwise. `?:` (Elvis) provides a *fallback value* when its left side is null. You often combine them: `?.` to safely navigate, `?:` to supply a default for the possibly-null result."},{t:"code",title:"Combining them",code:`val length = text?.length ?: 0     // safe-call gives Int?, Elvis defaults to 0
val name = user?.name ?: "Guest"   // navigate then default`},{t:"list",items:["**`?.`** — safe navigation: member access that returns null if the receiver is null.","**`?:`** — Elvis: supply a default when the left side is null.","**Together** — `a?.b ?: default` navigates safely then falls back.","**`!!`** — the opposite: assert non-null (throws), avoid it."]},{t:"note",text:"?. (safe call) accesses a member only if the receiver is non-null (else null); ?: (Elvis) supplies a fallback when the left side is null. Combine: a?.b ?: default navigates safely then defaults. !! is the unsafe opposite (assert non-null, throws)."}]}],Ix={id:"null-safety-types",title:"Null Safety & Types",tagline:"Nullable vs non-null types, ?./?:/!!/let, smart casts, val vs var & immutability, Unit/Nothing/Any, and platform types from Java interop.",tags:["Kotlin","Null Safety","Smart Cast","Platform Types","val/var"],content:Cx,qa:Tx},Mx=[{heading:"data class — value-holding classes with generated methods",blocks:[{t:"p",text:"A **`data class`** is for holding data. The compiler auto-generates the boilerplate you'd otherwise write by hand from the properties in the primary constructor: `equals()`/`hashCode()` (structural equality by content), `toString()` (readable), `copy()` (create a modified duplicate), and `componentN()` functions (for destructuring)."},{t:"code",title:"What a data class gives you",code:`data class User(val id: String, val name: String, val premium: Boolean = false)

val a = User("1", "Revanth")
val b = User("1", "Revanth")
a == b                       // true — structural equality (same content)

val updated = a.copy(premium = true)   // new instance, one field changed
val (id, name) = a                     // destructuring via componentN()
println(a)                             // User(id=1, name=Revanth, premium=false)`},{t:"list",items:["**`equals`/`hashCode` compare by content**, not reference — two data classes with equal fields are equal. This is why they're perfect for UI state (Compose skips recomposition when state is `equals`) and as map keys.","**`copy()`** is the idiomatic way to make a modified version of an immutable object — central to state management (`state.copy(loading = false)`).","**Rules/limits**: needs at least one primary-constructor parameter; only *primary-constructor properties* count toward the generated methods (a property in the body is excluded from equals/hashCode/toString); can't be abstract/open in the usual sense; components come from the constructor order.","**Immutability caveat**: `data class` doesn't force `val` — a `data class` with `var` or mutable collections is a mutable value holder, which breaks `equals`-based caching and is a common bug (prefer `val` + immutable collections)."]}]},{heading:"sealed class / sealed interface — restricted hierarchies",blocks:[{t:"p",text:"A **`sealed`** class or interface defines a *closed* set of subtypes — all direct subclasses must be declared in the same module/package, so the compiler knows *every* possible subtype. The payoff: a `when` over a sealed type can be **exhaustive** — the compiler verifies you've handled every case and doesn't require an `else`, and it *errors* if you add a new subtype without handling it."},{t:"code",title:"Sealed hierarchy + exhaustive when",code:`sealed interface UiState {
    data object Loading : UiState
    data class Success(val data: List<Item>) : UiState
    data class Error(val message: String) : UiState
}

fun render(state: UiState) = when (state) {   // no 'else' needed
    UiState.Loading -> showSpinner()
    is UiState.Success -> showList(state.data)   // smart-cast to Success
    is UiState.Error -> showError(state.message)
    // add a new subtype -> this 'when' fails to compile until handled
}`},{t:"list",items:["**Exhaustiveness is the killer feature**: model your states/results as a sealed type, and the compiler guarantees you handle every case — add a new state later and every `when` that isn't exhaustive breaks the build, pointing you to what to update. Huge for correctness.","**`sealed interface` vs `sealed class`**: interfaces are more flexible (a type can implement multiple sealed interfaces; no constructor). Prefer `sealed interface` unless you need shared state/constructor in the base.","**Ideal for**: UI states (Loading/Success/Error), navigation events, API results (Success/Failure), any 'one of a fixed set of shapes'. Combined with smart casts in `when` branches, you get typed access to each variant's data.","**vs enum**: an enum is a fixed set of *singleton instances* (no per-instance data beyond constructor values shared by all); a sealed type is a fixed set of *subtypes* that can each carry different data and have multiple instances. Use enum for simple constants, sealed for variants-with-data."]}]},{heading:"enum class — a fixed set of constants",blocks:[{t:"code",title:"Enums can hold data and methods",code:`enum class Status(val label: String) {
    ACTIVE("Active"),
    PAUSED("Paused"),
    ARCHIVED("Archived");

    fun isVisible() = this != ARCHIVED
}

Status.ACTIVE.label         // "Active"
Status.values()             // all constants (or entries in newer Kotlin)
Status.valueOf("PAUSED")    // lookup by name`},{t:"list",items:["An `enum` is a fixed set of named constant *instances*, each a singleton. They can have constructor parameters, properties, and methods (even per-constant overrides via anonymous bodies).","`when` over an enum is also exhaustive (no `else` needed if all constants are covered).","Use for a simple closed set of options where each option is just a labeled constant. When options need to carry *different* data shapes, use a sealed type instead."]}]},{heading:"value class (inline class) — zero-overhead wrappers",blocks:[{t:"p",text:"A **`value class`** (formerly `inline class`) wraps a single value to add type safety *without* the runtime cost of an object allocation. At runtime, the compiler **inlines** the wrapper away — a `value class UserId(val value: String)` is represented as just a `String` in most cases, so you get a distinct type at compile time with no boxing overhead."},{t:"code",title:"Type-safe wrappers with no allocation",code:`@JvmInline
value class UserId(val value: String)
@JvmInline
value class Email(val value: String)

fun sendEmail(to: Email, from: UserId) { }

// Compile-time safety — can't swap the arguments:
sendEmail(Email("a@b.com"), UserId("42"))   // OK
// sendEmail(UserId("42"), Email("a@b.com")) // COMPILE ERROR — types don't match
// At runtime, these are just Strings — no wrapper objects allocated`},{t:"list",items:["**The problem it solves**: passing raw `String`/`Int` everywhere loses meaning and lets you mix up arguments (a userId and an orderId are both Strings). Wrapping them in value classes makes the compiler enforce which is which — with *zero* runtime cost (no allocation).","**Requirements**: exactly one property (the wrapped value), `@JvmInline` annotation on the JVM. Can have methods and computed properties, but no `init` state beyond the single value.","**When it *does* box**: the inlining is dropped (a real object is allocated) in cases where a concrete type is needed — e.g. when used as a nullable (`UserId?`), in generics, or stored in a collection of `Any`. Mostly it's free, but not always."]}]},{heading:"object and companion object — singletons and statics",blocks:[{t:"code",title:"object declarations",code:`// object — a singleton (one instance, lazily created, thread-safe)
object Analytics {
    fun track(event: String) { }
}
Analytics.track("open")     // access directly, no instantiation

// companion object — 'static' members tied to a class
class User private constructor(val name: String) {
    companion object {
        fun create(name: String): User = User(name)   // factory
        const val MAX_NAME_LENGTH = 50
    }
}
User.create("Revanth")      // called on the class, like a static method`},{t:"list",items:["**`object`** — a **singleton**: declares a class *and* its single instance at once, created lazily on first access, thread-safely. Use for stateless helpers, singletons, and (Kotlin-idiomatically) things you'd make static utility classes for. `data object` adds a nice `toString`/`equals` (used for singleton sealed variants like `Loading`).","**`companion object`** — Kotlin has no `static`; instead, a class can have *one* companion object holding members accessible via the class name (`User.create()`). Used for factory methods, constants, and anything you'd make static in Java.","**Companion is a real object**: unlike Java statics, a companion object is an actual object instance — it can implement interfaces, be passed as a value, and have extension functions. This makes 'statics' first-class.","**Anonymous objects** (`object : Listener { }`) create one-off instances implementing an interface — Kotlin's version of anonymous classes."]},{t:"note",text:"Quick chooser: **data class** for value holders (auto equals/copy); **sealed** for a closed set of variants-with-data (exhaustive when); **enum** for a fixed set of simple constants; **value class** for zero-cost type-safe wrappers; **object** for singletons; **companion object** for statics/factories."}]}],Px=[{level:"junior",q:"What is a data class and what does it generate?",a:[{t:"p",text:"**The concept**: a `data class` is designed to hold data, and the compiler auto-generates the boilerplate you'd otherwise write from the properties declared in the primary constructor."},{t:"list",items:["**`equals()` / `hashCode()`** — structural equality: two data classes are equal if their contents match (not their references). This makes them safe as map keys and lets Compose skip recomposition when state is unchanged.","**`toString()`** — a readable representation like `User(id=1, name=Revanth)`.","**`copy()`** — create a new instance with some fields changed: `user.copy(premium = true)`. The idiomatic way to 'modify' an immutable object.","**`componentN()`** — enables destructuring: `val (id, name) = user`."]},{t:"p",text:"Data classes are everywhere in Android — UI state, domain models, API responses — because `equals`-by-content and `copy()` are exactly what immutable state management needs. One caveat: only *primary-constructor* properties count toward the generated methods (a property declared in the class body is excluded), and you should keep them `val` with immutable collections, since a data class with `var` fields breaks the equality-based caching that makes them useful."}]},{level:"junior",q:"What is a sealed class and why is it useful?",a:[{t:"p",text:"**The concept**: a `sealed` class or interface defines a *closed* set of subtypes — all direct subclasses must be in the same module, so the compiler knows every possible subtype at compile time. The main benefit is **exhaustive `when`**: a `when` over a sealed type doesn't need an `else`, the compiler verifies you handled every case, and if you add a new subtype later, every non-exhaustive `when` fails to compile until you handle it."},{t:"code",title:"Modeling UI state",code:`sealed interface Result {
    data class Success(val data: List<Item>) : Result
    data class Error(val message: String) : Result
    data object Loading : Result
}

when (result) {                          // exhaustive — no else
    is Result.Success -> show(result.data)   // smart-cast to Success
    is Result.Error -> showError(result.message)
    Result.Loading -> showSpinner()
}`},{t:"p",text:"It's ideal for anything that's 'one of a fixed set of shapes, each possibly carrying different data' — UI states (Loading/Success/Error), API results, navigation events. The exhaustiveness is a real correctness tool: add a new state and the compiler *points you to every place* that needs updating, instead of you discovering a missed case at runtime. Combined with smart casts, each `when` branch gives you typed access to that variant's data."}]},{level:"junior",q:"What is the difference between a sealed class and an enum?",a:[{t:"p",text:"**Both represent a fixed, closed set of options and both give exhaustive `when` — the difference is what each 'option' can be.** An `enum` is a fixed set of *singleton constants*: each value is one instance, and while an enum can have properties, *all* instances share the same shape (the same constructor parameters). A `sealed` type is a fixed set of *subtypes*: each variant can be a different class carrying *different data*, and you can have multiple instances of each."},{t:"list",items:["**Use an enum** for a simple set of constants where each is just a named value — a `Status` (ACTIVE/PAUSED/ARCHIVED), a day of the week. All the same shape.","**Use a sealed type** when the variants carry *different* data — `Loading` (no data), `Success(val data)`, `Error(val message)`. An enum can't do this because each constant would need the same fields."]},{t:"code",title:"The distinguishing case",code:`// enum — all constants share the same shape
enum class Direction { NORTH, SOUTH, EAST, WEST }

// sealed — each variant has its OWN data shape
sealed interface Payment {
    data class Card(val number: String) : Payment
    data class Cash(val amount: Int) : Payment
    data object Free : Payment
}`},{t:"p",text:"Rule of thumb: if the options differ only by name/label, enum; if they differ by *structure* (different data per option), sealed. Sealed is essentially 'an enum where each case can be a different type'."}]},{level:"junior",q:"What is a companion object and how does it relate to Java's static?",a:[{t:"p",text:"**The concept**: Kotlin has no `static` keyword. Instead, to have members accessible on the class itself (rather than an instance), you put them in a `companion object` — a single special object declared inside the class. You then call them via the class name, just like Java statics: `User.create()`, `User.MAX_LENGTH`."},{t:"code",title:"Factory + constant via companion",code:`class User private constructor(val name: String) {
    companion object {
        const val MAX_LENGTH = 50
        fun create(name: String) = User(name.take(MAX_LENGTH))
    }
}
User.create("Revanth")`},{t:"p",text:"The key difference from Java statics: a companion object is a **real object instance**, not just a namespace. That means it can implement interfaces, be passed around as a value, hold state, and receive extension functions — none of which Java statics can do. So 'statics' in Kotlin are first-class objects. Common uses: factory methods (often with a `private` constructor so the only way to create instances is through the factory), constants (`const val` for compile-time constants), and anything you'd have made static in Java. Each class can have at most one companion object."}]},{level:"junior",q:"What is the difference between a class and an object declaration?",a:[{t:"p",text:"**A `class` is a blueprint you instantiate (possibly many times with `new`/constructor calls); an `object` declaration is a singleton — it declares the class *and* creates its single instance at the same time.** You never instantiate an `object`; you just use it directly by name."},{t:"code",title:"class vs object",code:`class Counter { var count = 0 }         // make instances
val c1 = Counter(); val c2 = Counter()  // two separate instances

object Config {                          // exactly ONE instance, ever
    val baseUrl = "https://api.example.com"
}
Config.baseUrl                           // used directly, no instantiation`},{t:"list",items:["**`object`** is Kotlin's built-in singleton: created lazily on first access, thread-safely, with no boilerplate. Use for stateless utilities, single shared instances, and things you'd make a static utility class for.","**`data object`** adds a sensible `toString`/`equals` — used for singleton sealed variants like `Loading` where you want it to print nicely.","**Anonymous objects** (`object : SomeInterface { }`) are one-off instances, Kotlin's version of Java anonymous classes — common for one-shot listener implementations.","The caution with `object`: since it's a single shared instance, don't put mutable state in it carelessly — that state is global and shared across the whole app (and can leak or cause concurrency issues)."]}]},{level:"senior",q:"What is a value class (inline class) and when would you use one?",a:[{t:"p",text:"**The concept**: a `value class` (annotated `@JvmInline`) wraps a single value to create a *distinct type* at compile time, but the compiler **inlines the wrapper away at runtime** — so there's no object allocation, no boxing overhead. You get type safety for free. `@JvmInline value class UserId(val value: String)` is, at runtime, just a `String` in most cases."},{t:"code",title:"Preventing argument mix-ups with zero cost",code:`@JvmInline value class UserId(val value: String)
@JvmInline value class OrderId(val value: String)

fun cancel(order: OrderId, requestedBy: UserId) { }

cancel(OrderId("o-1"), UserId("u-9"))    // OK
// cancel(UserId("u-9"), OrderId("o-1")) // COMPILE ERROR — can't swap them`},{t:"list",items:["**The problem it solves**: when domain identifiers are all raw `String`/`Int`, you lose meaning and can accidentally pass a userId where an orderId belongs (both are Strings, so the compiler can't catch it). Wrapping each in a value class makes them distinct types the compiler enforces — a whole class of bugs eliminated at *zero runtime cost*.","**vs a regular (data) class wrapper**: a normal wrapper class would allocate an object for every id, adding memory and GC pressure — often unacceptable for pervasive types like ids used in tight loops or large collections. Value classes give the same type safety without the allocation.","**Requirements & limits**: exactly one property (the wrapped value); `@JvmInline` on the JVM. It can have methods and computed properties. The inlining is *dropped* (a real object is boxed) in specific situations — when used as a nullable (`UserId?`), as a generic type argument, or when stored as `Any`/in a collection of interface type — so it's 'usually free', not 'always free'.","**Other uses**: units of measure (`value class Meters(val value: Double)`), validated wrappers (an `Email` value class with validation in `init`), and making primitive-obsessed APIs type-safe."]},{t:"p",text:"**The senior framing**: value classes are how you get the safety and expressiveness of strong domain types (no more 'stringly-typed' code) without paying the traditional wrapper-object tax — the compiler gives you the type at compile time and erases it at runtime. They're underused and a good thing to bring up when discussing modeling domain primitives."}]},{level:"senior",q:"When would you choose a sealed interface over a sealed class, and what are the trade-offs?",a:[{t:"p",text:"**Both create a closed, exhaustive hierarchy — the choice mirrors the general interface-vs-abstract-class decision, plus a few sealed-specific points.** Prefer a **`sealed interface`** by default; use a **`sealed class`** when you need shared *state* or a common constructor in the base type."},{t:"list",items:["**Multiple inheritance of type**: a class can implement *several* sealed interfaces but can only extend *one* sealed class. So sealed interfaces compose — a type can belong to more than one closed hierarchy, which is impossible with sealed classes. This flexibility is the main reason to prefer them.","**No state/constructor in interfaces**: a `sealed interface` can't hold properties with backing fields or have a constructor. If every variant genuinely shares stored state (say a common `timestamp` field with a backing field, or init logic), a `sealed class` with that state in the base is cleaner than repeating it in each variant.","**A variant can be a `data class`, `data object`, or regular class under either** — that's independent of whether the parent is a sealed class or interface. So you don't lose data-class benefits either way.","**Interoperability & design**: interfaces impose fewer constraints on implementers (no forced single-inheritance slot), which keeps the hierarchy open to future composition. This is the same 'prefer interfaces for flexibility' principle from general OO design."]},{t:"code",title:"When shared state pushes you to a sealed class",code:`// sealed interface — no shared state needed, most flexible
sealed interface NetworkResult {
    data class Success(val body: String) : NetworkResult
    data class Failure(val code: Int) : NetworkResult
}

// sealed class — genuine shared state/behavior in the base
sealed class Screen(val route: String) {   // every screen HAS a route
    data object Home : Screen("home")
    data class Detail(val id: String) : Screen("detail/$id")
}`},{t:"p",text:"**Rule of thumb**: default to `sealed interface` (flexible, composable, no downsides for most modeling); switch to `sealed class` only when the variants share stored state or constructor logic that's cleaner to centralize in the base. The exhaustiveness benefit is identical for both — the choice is purely about whether you need base-level state and single vs multiple inheritance."}]},{level:"junior",q:"What is a primary constructor, and what does the init block do?",a:[{t:"p",text:"The primary constructor is declared in the class header (`class User(val name: String)`) — its parameters can directly declare properties with `val`/`var`. Since the primary constructor can't contain code, initialization logic goes in `init { }` blocks, which run when an instance is created, in the order they and property initializers appear."},{t:"code",title:"Primary constructor + init",code:`class User(val name: String, age: Int) {   // 'name' is a property; 'age' is just a param
    val isAdult: Boolean
    init {
        require(age >= 0) { "age must be non-negative" }   // validation
        isAdult = age >= 18                                 // init logic
    }
}`},{t:"list",items:["**Primary constructor** — in the header; `val`/`var` params become properties.","**`init { }`** — holds construction logic (validation, derived properties).","**Order** — property initializers and `init` blocks run top-to-bottom.","**Params without `val`/`var`** — usable in `init`/initializers but not stored as properties."]},{t:"note",text:"The primary constructor is in the class header; val/var params become properties. Since it holds no code, put initialization/validation in init { } blocks, which run at construction in declaration order alongside property initializers. Params without val/var are constructor-scoped, not stored."}]},{level:"junior",q:"What are secondary constructors, and when do you need them?",a:[{t:"p",text:"Secondary constructors (`constructor(...)`) provide alternative ways to construct an instance; they must delegate to the primary constructor with `this(...)`. In Kotlin you often don't need them because *default parameter values* on the primary constructor cover most 'overloaded constructor' cases more concisely."},{t:"code",title:"Secondary vs default params",code:`class User(val name: String, val role: String) {
    constructor(name: String) : this(name, "guest")   // delegates to primary
}
// Often better — default parameter:
class User2(val name: String, val role: String = "guest")   // no secondary needed`},{t:"list",items:["**Secondary** — `constructor(...) : this(...)`, delegates to the primary.",'**Prefer default params** — `role: String = "guest"` replaces most overloads.',"**When needed** — different init logic per constructor, or Java interop requiring specific constructors.","**`@JvmOverloads`** — generate overloads for Java callers from default params."]},{t:"note",text:"Secondary constructors (constructor(...) : this(...)) delegate to the primary. Usually unnecessary in Kotlin — default parameter values cover most overloaded-constructor cases more concisely. Use secondaries for distinct init logic or Java interop; @JvmOverloads generates Java-visible overloads from defaults."}]},{level:"junior",q:"What is a backing field, and how do custom getters/setters work?",a:[{t:"p",text:"A Kotlin property is really a getter (and setter for `var`) plus an optional *backing field* referenced as `field`. When you write a custom getter/setter, you use `field` to access the stored value (using the property name inside would recurse infinitely). A property with a custom getter that doesn't use `field` is *computed* — no backing field is generated."},{t:"code",title:"Custom accessors and backing field",code:`class Temperature {
    var celsius: Double = 0.0
        set(value) {
            require(value >= -273.15)
            field = value                 // 'field' is the backing field
        }
    val fahrenheit: Double                // computed — no backing field
        get() = celsius * 9 / 5 + 32
}`},{t:"list",items:["**Property = accessors** — get (and set for `var`), optionally over a backing field.","**`field`** — the backing field, used inside custom accessors to avoid recursion.","**Computed property** — a custom getter without `field`; derived each access, no storage.","**Validation/transformation** — custom setters enforce invariants."]},{t:"note",text:"A property is a getter (+ setter for var) over an optional backing field, accessed as `field` inside custom accessors (using the property name recurses). A custom getter without `field` = a computed property (derived each access, no storage). Custom setters enforce validation/invariants."}]},{level:"junior",q:"What is the difference between an abstract class and an interface?",a:[{t:"p",text:"Both define contracts with abstract members, but: a class can implement *many* interfaces but extend only *one* abstract class; interfaces *can't hold state* (no backing fields — only abstract or computed properties), while abstract classes can have state and constructors. Use an interface for a capability/role; an abstract class for a partial base implementation with shared state."},{t:"table",headers:["","Interface","Abstract class"],rows:[["Multiple","implement many","extend only one"],["State","no backing fields","can have fields/state"],["Constructor","no","yes"],["Default methods","yes","yes"]]},{t:"list",items:["**Interface** — multiple inheritance of behavior; no state; models a capability (`Comparable`, `Clickable`).","**Abstract class** — single inheritance; can hold state and a constructor; a partial base implementation.","**Choose interface** — when types share a role but not a hierarchy.","**Choose abstract class** — when subclasses share state/implementation and one 'is-a' relationship."]},{t:"note",text:"Interface: implement many, no backing-field state, no constructor — models a capability/role. Abstract class: extend one, can hold state + constructor — a partial base implementation. Both allow default methods. Prefer interfaces for shared roles; abstract classes for shared state/impl in a single hierarchy."}]},{level:"junior",q:"Why is everything final by default in Kotlin, and how do you allow inheritance?",a:[{t:"p",text:"Kotlin classes and members are `final` (non-inheritable/non-overridable) by default — you must mark them `open` to allow subclassing or overriding. This is deliberate: it prevents the 'fragile base class' problem (subclasses breaking when a base changes unexpectedly) and makes inheritance an intentional, documented choice rather than an accident."},{t:"code",title:"open for inheritance",code:`open class Animal {          // open — can be subclassed
    open fun sound() = "..."   // open — can be overridden
}
class Dog : Animal() {
    override fun sound() = "Woof"   // override required
}
// class Cat : Animal2()   // error if Animal2 isn't 'open'`},{t:"list",items:["**Final by default** — classes and members can't be extended/overridden unless `open`.","**`open`** — opt into inheritance/overriding explicitly.","**`override`** — required keyword when overriding (documents intent).","**Why** — avoids fragile base classes; favors composition over accidental inheritance."]},{t:"note",text:"Kotlin is final by default — classes/members need `open` to be subclassed/overridden, and overriding requires the `override` keyword. This prevents fragile-base-class bugs and makes inheritance intentional. It nudges toward composition; use open deliberately where extension is designed for."}]},{level:"junior",q:"What is the difference between a nested class and an inner class?",a:[{t:"p",text:"A *nested* class (default) does *not* hold a reference to the outer class — it's like a Java `static` nested class. An *inner* class (marked `inner`) *does* hold a reference to the outer instance and can access its members. Use nested for grouping without coupling; use `inner` only when you genuinely need the outer instance (and beware it can cause leaks)."},{t:"code",title:"nested vs inner",code:`class Outer {
    val x = 10
    class Nested {                 // no reference to Outer
        // fun f() = x            // error — can't access x
    }
    inner class Inner {            // holds Outer reference
        fun f() = x                // OK — accesses Outer.x
    }
}`},{t:"list",items:["**Nested (default)** — no outer reference; like Java `static` nested.","**`inner`** — holds the outer instance; can access outer members.","**Leak risk** — an `inner` class outliving its purpose retains the outer instance.","**Prefer nested** — unless you specifically need the outer reference."]},{t:"note",text:"Nested class (default) has NO outer reference (like Java static nested); inner class holds the outer instance and can access its members. Prefer nested to avoid coupling/leaks; use inner only when you truly need the outer instance (an inner class outliving its scope leaks the outer)."}]},{level:"junior",q:"What is an object expression (anonymous object)?",a:[{t:"p",text:"An object expression creates an *anonymous* object on the spot — often to implement an interface or extend a class without naming a new class, similar to a Java anonymous class. `object : Listener { ... }` produces a one-off instance implementing `Listener`. Unlike a lambda, it can implement multiple methods and hold state."},{t:"code",title:"Anonymous object",code:`val listener = object : ClickListener {
    override fun onClick() { }
    override fun onLongClick() { }   // multiple methods — a lambda can't do this
}
// Can also extend a class and add ad-hoc members:
val point = object { val x = 1; val y = 2 }`},{t:"list",items:["**`object : Type { }`** — a one-off instance implementing/extending a type.","**Multiple methods/state** — unlike a lambda (single method).","**Captures scope** — can use enclosing variables.","**vs SAM lambda** — a single-method interface can just be a lambda; use object expressions for multi-method ones."]},{t:"note",text:"An object expression (object : Type { }) creates an anonymous one-off instance implementing/extending a type — like a Java anonymous class, with multiple methods and state (unlike a lambda). For single-method (SAM) interfaces, prefer a lambda; use object expressions for multi-method implementations."}]},{level:"junior",q:"What can an enum class do beyond simple constants?",a:[{t:"p",text:"An `enum class` is a fixed set of named instances, but each constant can carry *properties*, the enum can have *methods*, and constants can *override* methods with per-constant behavior. Enums also get `values()`, `valueOf()`, `entries`, `ordinal`, and `name`, and work great in exhaustive `when` expressions."},{t:"code",title:"Rich enum",code:`enum class Planet(val mass: Double, val radius: Double) {
    EARTH(5.97e24, 6.37e6),
    MARS(6.42e23, 3.39e6) {
        override fun describe() = "The red planet"   // per-constant override
    };
    val gravity: Double get() = mass / (radius * radius)
    open fun describe() = name
}`},{t:"list",items:["**Constants with properties** — each entry carries data (`mass`, `radius`).","**Methods + per-constant overrides** — shared and specialized behavior.","**Built-ins** — `entries`/`values()`, `valueOf`, `ordinal`, `name`.","**Exhaustive `when`** — the compiler checks all constants are handled."]},{t:"note",text:"enum class = fixed named instances that can carry properties, have methods, and override methods per-constant. Built-ins: entries/values(), valueOf, ordinal, name. Great in exhaustive when. For richer hierarchies (varying data shapes per case), use a sealed class instead."}]},{level:"junior",q:"What is a data object, and when do you use it?",a:[{t:"p",text:"A `data object` (Kotlin 1.9+) is a singleton `object` that also gets a proper `toString()` (printing the object name) and structural `equals`/`hashCode`. It's mainly used for stateless singletons in sealed hierarchies — e.g. a `Loading` state — so it prints nicely (`Loading`) instead of the default `Object@hash`."},{t:"code",title:"data object in a sealed hierarchy",code:`sealed interface UiState {
    data object Loading : UiState           // toString() == "Loading"
    data class Content(val items: List<Item>) : UiState
    data object Empty : UiState
}`},{t:"list",items:["**`data object`** — singleton with generated `toString`/`equals`/`hashCode`.","**Sealed hierarchies** — stateless cases (`Loading`, `Empty`) that print their name.","**vs plain `object`** — plain `object` has an ugly default `toString`.","**Consistency** — pairs with `data class` cases for logging/debugging clarity."]},{t:"note",text:"data object (Kotlin 1.9+) is a singleton object with generated toString (prints the name), equals, and hashCode — for stateless cases in sealed hierarchies (Loading/Empty) that should print nicely instead of Object@hash. Pairs with data class cases for clean logging."}]},{level:"junior",q:"What is the difference between const val and val?",a:[{t:"p",text:"`val` is a read-only property assigned at *runtime* (once). `const val` is a *compile-time constant* — its value is inlined at compile time, so it must be a primitive or `String` known at compile time, and it can only be declared at the top level or in an `object`/`companion object`. Use `const` for true constants (config keys, magic numbers)."},{t:"code",title:"const vs val",code:`const val MAX_COUNT = 100          // compile-time, inlined
const val BASE_URL = "https://..."  // String OK
val runtimeValue = computeAtRuntime()   // val — runtime assignment

class C { companion object { const val KEY = "key" } }   // const in companion`},{t:"list",items:["**`val`** — read-only, assigned at runtime; any type.","**`const val`** — compile-time constant, inlined; only primitives/String; top-level or (companion) object.","**Use `const`** — for literal constants (keys, limits, URLs).","**Annotations** — `const` values can be used in annotations (compile-time required)."]},{t:"note",text:"val = read-only, assigned at runtime, any type. const val = compile-time constant (inlined at call sites), only primitives/String, only top-level or in (companion) object. Use const for literal constants (keys/limits/URLs) — and it's required for values used in annotations."}]},{level:"junior",q:"What are Kotlin's visibility modifiers, including internal?",a:[{t:"p",text:"Kotlin has four visibility modifiers: `public` (default — visible everywhere), `private` (visible in the file or class), `protected` (visible in the class and subclasses — not for top-level), and `internal` (visible within the same *module*). `internal` is unique to Kotlin — great for exposing something across a module's packages while hiding it from other modules/consumers."},{t:"table",headers:["Modifier","Visible in"],rows:[["public (default)","everywhere"],["private","the file (top-level) or the class"],["protected","the class and its subclasses"],["internal","the same module"]]},{t:"list",items:["**`public`** — default; everywhere.","**`private`** — file-scoped (top-level) or class-scoped.","**`protected`** — class + subclasses (not for top-level declarations).","**`internal`** — same module; ideal for library internals shared across packages but hidden from consumers."]},{t:"note",text:"public (default, everywhere), private (file or class), protected (class + subclasses, not top-level), internal (same module). internal is Kotlin-specific — perfect for sharing across a module's packages while hiding from other modules/library consumers."}]},{level:"junior",q:"What is destructuring, and how does componentN work?",a:[{t:"p",text:"Destructuring lets you unpack an object into multiple variables — `val (name, age) = user`. It works via `componentN()` operator functions: `component1()`, `component2()`, etc. Data classes generate these automatically for their primary-constructor properties (in order); you can also define them manually for any class."},{t:"code",title:"Destructuring",code:`data class User(val name: String, val age: Int)
val (name, age) = User("Sam", 30)     // name="Sam", age=30

for ((key, value) in map) { }          // Map.Entry has component1/component2
val (a, _, c) = triple                  // skip with _`},{t:"list",items:["**`val (a, b) = obj`** — unpacks via `component1()`, `component2()`.","**Data classes** — auto-generate `componentN` for primary-constructor props (positional).","**Maps/pairs** — destructure entries and pairs.","**`_`** — skip a component you don't need."]},{t:"note",text:"Destructuring (val (a, b) = obj) unpacks via componentN() operator functions. Data classes auto-generate them for primary-constructor properties positionally — so order matters, not names. Works for pairs/map entries; use _ to skip. You can define componentN manually for non-data classes."}]},{level:"junior",q:"What does copy() do on a data class, and why is it important?",a:[{t:"p",text:'`copy()` creates a new instance of a data class with the same values, optionally overriding some via named arguments — `user.copy(name = "New")`. It\'s the idiomatic way to produce a modified version of an *immutable* object without mutating it, which is central to state management (updating UiState, Redux-style updates).'},{t:"code",title:"copy for immutable updates",code:`data class UiState(val loading: Boolean, val items: List<Item>)
val next = state.copy(loading = false)         // only change loading
val updated = user.copy(name = "New", age = 31)  // change several fields`},{t:"list",items:["**`copy(overrides)`** — new instance with named fields changed.","**Immutable updates** — modify state without mutation (thread-safe, predictable).","**State management** — `_state.update { it.copy(...) }` is the core pattern.","**Shallow copy** — nested mutable objects are shared; copy those too if needed."]},{t:"note",text:"copy() makes a new data-class instance with named fields overridden — the idiomatic immutable update (state.copy(loading = false)), central to state management. It's a shallow copy (nested mutable objects are shared, so copy those too). Pairs with StateFlow.update { it.copy(...) }."}]},{level:"senior",q:"What is a fun interface (SAM interface), and how does it relate to lambdas?",a:[{t:"p",text:"A `fun interface` is a *functional interface* — an interface with exactly one abstract method (Single Abstract Method). Marking it `fun` lets you implement it with a *lambda* instead of an `object : Interface { }`, via SAM conversion. This is how you get concise lambda syntax for your own single-method interfaces."},{t:"code",title:"fun interface + SAM conversion",code:`fun interface Validator {
    fun validate(input: String): Boolean
}
val notEmpty = Validator { it.isNotEmpty() }   // lambda instead of object
notEmpty.validate("hi")   // true`},{t:"list",items:["**`fun interface`** — exactly one abstract method; enables SAM conversion.","**Lambda implementation** — `Validator { ... }` instead of `object : Validator { }`.","**Java SAM interop** — Java functional interfaces also support lambda conversion from Kotlin.","**vs function type** — a `(String) -> Boolean` type is often simpler; use `fun interface` when you want a named type or Java interop."]},{t:"note",text:"A fun interface is a functional (single-abstract-method) interface; marking it `fun` enables SAM conversion so you implement it with a lambda (Validator { it.isNotEmpty() }) instead of an object expression. Prefer a plain function type ((String)->Boolean) when you don't need a named type or Java interop."}]},{level:"senior",q:"How does operator overloading work in Kotlin?",a:[{t:"p",text:"You overload operators by defining functions with the `operator` modifier and specific names — `plus` for `+`, `get` for `[]`, `invoke` for `()`, `compareTo` for `<`/`>`, `equals` for `==`, etc. This lets your types use natural operator syntax (e.g. vector math, DSLs). Use it judiciously — only where the operator's meaning is intuitive."},{t:"code",title:"Overloading operators",code:`data class Vec(val x: Int, val y: Int) {
    operator fun plus(o: Vec) = Vec(x + o.x, y + o.y)   // enables v1 + v2
    operator fun get(i: Int) = if (i == 0) x else y      // enables v[0]
}
val sum = Vec(1, 2) + Vec(3, 4)   // Vec(4, 6)`},{t:"list",items:["**`operator fun`** — named conventionally (`plus`, `times`, `get`, `invoke`, `compareTo`, `contains`, `rangeTo`).","**Natural syntax** — `+`, `[]`, `()`, `in`, `..`, comparisons.","**Use judiciously** — only where the operator meaning is obvious (math, collections, DSLs).","**`invoke`** — makes an object callable like a function."]},{t:"note",text:"Overload operators with operator fun named by convention: plus (+), times (*), get ([]), invoke (()), compareTo (< >), contains (in), rangeTo (..), equals (==). Enables natural syntax for math/collections/DSLs. Use only where the operator's meaning is intuitive — don't surprise readers."}]},{level:"senior",q:"What does a data class generate, and what are its limitations?",a:[{t:"p",text:"A `data class` auto-generates `equals()`, `hashCode()`, `toString()`, `componentN()`, and `copy()` — based *only* on the *primary constructor* properties. Key limitations: properties declared in the *body* (not the primary constructor) are excluded from these; a data class can't be `abstract`/`open`/`sealed`/`inner`; and it needs at least one primary-constructor parameter."},{t:"code",title:"Only primary-constructor props count",code:`data class User(val id: Int, val name: String) {
    var lastSeen: Long = 0   // in the body — NOT part of equals/hashCode/toString/copy
}
val a = User(1, "Sam").apply { lastSeen = 100 }
val b = User(1, "Sam").apply { lastSeen = 999 }
a == b   // true! lastSeen is ignored`},{t:"list",items:["**Generated** — `equals`/`hashCode`/`toString`/`componentN`/`copy`.","**Only primary-constructor props** — body properties are excluded (a common gotcha for `equals`).","**Restrictions** — not `abstract`/`open`/`sealed`/`inner`; ≥1 primary param.","**Override** — you can provide custom `equals`/`hashCode` if needed."]},{t:"note",text:"data class generates equals/hashCode/toString/componentN/copy from PRIMARY-CONSTRUCTOR properties only — body properties are excluded (so two instances differing only in a body var compare equal — a classic gotcha). It can't be abstract/open/sealed/inner and needs ≥1 primary param."}]},{level:"senior",q:"What is the class initialization order in Kotlin?",a:[{t:"p",text:"When an instance is created, initialization runs in this order: the primary constructor's arguments are evaluated, then *property initializers and `init` blocks execute top-to-bottom in declaration order*, and finally the secondary constructor body (if used) runs. For inheritance, the base class is fully initialized before the derived class's initializers — which is why calling an `open` method from `init` is dangerous (the subclass override runs before the subclass is initialized)."},{t:"code",title:"Init order gotcha",code:`open class Base {
    init { greet() }              // calls overridden greet() — runs before Derived init!
    open fun greet() = println("base")
}
class Derived : Base() {
    val name = "Sam"
    override fun greet() = println(name)   // prints "null" — name not yet initialized
}`},{t:"list",items:["**Order** — constructor args → property initializers + `init` blocks (top-to-bottom) → secondary constructor body.","**Base before derived** — the superclass initializes fully first.","**Danger** — calling an `open` method from `init` runs the subclass override before the subclass is initialized (sees uninitialized state).","**Avoid** — don't call open/overridable members from constructors/`init`."]},{t:"note",text:"Init order: constructor args → property initializers + init blocks (declaration order) → secondary constructor body; base class fully before derived. Gotcha: calling an open method from init runs the subclass override before the subclass initializes (sees null/uninitialized state) — never call overridable members from constructors/init."}]},{level:"senior",q:"How do interface default methods work, and how are conflicts resolved?",a:[{t:"p",text:"Interfaces can provide *default implementations* for methods. If a class implements two interfaces that both provide the same method (a diamond conflict), the compiler *forces* you to override it and resolve the ambiguity explicitly, using `super<Interface>.method()` to pick which one(s) to call."},{t:"code",title:"Resolving a diamond conflict",code:`interface A { fun greet() = "A" }
interface B { fun greet() = "B" }
class C : A, B {
    override fun greet() = super<A>.greet() + super<B>.greet()   // must resolve
}`},{t:"list",items:["**Default methods** — interfaces can implement methods (not just declare).","**Conflict** — two defaults for the same signature forces an override.","**`super<Interface>.method()`** — explicitly call a specific interface's version.","**No state** — interface defaults can't rely on backing fields."]},{t:"note",text:"Interfaces can provide default method implementations. When two implemented interfaces both provide the same method (diamond), the compiler forces you to override it and disambiguate with super<Interface>.method(). Interface defaults can't use backing-field state."}]},{level:"junior",q:"How do you use a companion object as a factory or for constants?",a:[{t:"p",text:"A `companion object` holds members tied to the *class* rather than instances (like Java statics). Common uses: factory functions (`create()`), constants, and grouping class-level utilities. You call them via the class name (`User.create()`), and you can name the companion or have it implement interfaces."},{t:"code",title:"Factory + constants",code:`class User private constructor(val name: String) {
    companion object {
        const val MAX_NAME = 50
        fun create(name: String): User =
            User(name.take(MAX_NAME))   // factory; can access private constructor
    }
}
val u = User.create("Sam")`},{t:"list",items:["**Class-level members** — accessed via the class name, one per class.","**Factory pattern** — a `create()` in the companion can call a private constructor.","**Constants** — `const val` / grouped values.","**Named/interface** — `companion object Factory` or implement an interface for polymorphic factories."]},{t:"note",text:"A companion object holds class-level members (like Java statics), called via the class name. Uses: factory functions (create() — can access a private constructor to control instantiation), constants (const val), and class utilities. It can be named and implement interfaces for polymorphic factories."}]},{level:"junior",q:"How do you create a singleton, and is it thread-safe?",a:[{t:"p",text:"An `object` declaration creates a *singleton* — a single, lazily-initialized instance. Kotlin guarantees its initialization is *thread-safe* (the object is created safely on first access, like the initialization-on-demand holder idiom). Use it for stateless helpers, a single shared manager, or a registry."},{t:"code",title:"object singleton",code:`object Analytics {
    private val events = mutableListOf<String>()
    fun log(event: String) { events += event }
}
Analytics.log("open")   // single shared instance, thread-safe init`},{t:"list",items:["**`object`** — one lazily-created, thread-safe-initialized instance.","**Use** — stateless utilities, shared managers, registries, sealed-hierarchy singletons.","**Mutable state caveat** — thread-safe *init* doesn't make its mutable state thread-safe; guard shared mutable data.","**vs DI singleton** — for testability, prefer DI-scoped singletons over global `object`s holding state."]},{t:"note",text:"An object declaration is a singleton — one instance, lazily and thread-safely initialized. Use for stateless helpers/managers/registries. Caveat: safe INITIALIZATION doesn't make its mutable state thread-safe — guard shared mutable data. For testability, prefer DI-scoped singletons over global stateful objects."}]},{level:"senior",q:"How do you correctly override equals and hashCode for a non-data class?",a:[{t:"p",text:"If you override `equals`, you *must* override `hashCode` consistently — equal objects must have equal hash codes, or hash-based collections (`HashMap`/`HashSet`) break. Base both on the same fields. For most value types, a `data class` generates these correctly; override manually only for custom equality (e.g. comparing by a subset of fields)."},{t:"code",title:"Consistent equals/hashCode",code:`class Money(val amount: Int, val currency: String) {
    override fun equals(other: Any?): Boolean {
        if (this === other) return true
        if (other !is Money) return false
        return amount == other.amount && currency == other.currency
    }
    override fun hashCode(): Int = 31 * amount + currency.hashCode()
}`},{t:"list",items:["**Override both together** — equal objects must share a hash code.","**Same fields** — base `equals` and `hashCode` on the same properties.","**Contract** — reflexive, symmetric, transitive, consistent.","**Prefer `data class`** — it generates correct implementations; override only for custom semantics."]},{t:"note",text:"Override equals and hashCode together, based on the same fields — equal objects MUST have equal hash codes or HashMap/HashSet break. Follow the equals contract (reflexive/symmetric/transitive/consistent). Prefer a data class (generates them correctly); override manually only for custom equality semantics."}]},{level:"junior",q:"What is the difference between a class and an interface property?",a:[{t:"p",text:"A class property can have a *backing field* (stored state) with an initial value. An interface property *cannot* store state — it can only be abstract (no initializer) or provide a custom getter (computed each access). This is why interfaces model behavior/contracts, not state."},{t:"code",title:"Interface vs class properties",code:`interface Named {
    val name: String                 // abstract — no backing field
    val displayName: String get() = "Mr. $name"   // computed — allowed
    // val count: Int = 0            // ERROR — interfaces can't hold state
}
class Person(override val name: String) : Named   // class provides storage`},{t:"list",items:["**Class property** — can have a backing field (stored value).","**Interface property** — abstract or computed (custom getter); no state.","**Implementers store it** — a class implementing the interface provides the backing field.","**Why** — interfaces define contracts; state belongs to classes."]},{t:"note",text:"Class properties can have backing fields (stored state); interface properties can't — they're abstract or computed (custom getter) only. The implementing class provides the storage. This is why interfaces model contracts/behavior, not state."}]},{level:"senior",q:"What is the difference between an object declaration and a class with a private constructor?",a:[{t:"p",text:"An `object` declaration is a *language-level singleton* — one instance, thread-safe lazy init, no way to make more. A class with a `private constructor` restricts instantiation but can still create multiple instances internally (e.g. a factory that caches a few). Use `object` for a true single instance; use a private constructor when you want *controlled* instantiation (factories, a fixed set of instances, builders)."},{t:"list",items:["**`object`** — exactly one instance, guaranteed by the language.","**Private constructor** — controlled creation; a companion factory can make one or several instances.","**Use `object`** — global stateless singleton.","**Use private constructor** — factory patterns, instance caching, enforcing invariants at creation."]},{t:"code",title:"Controlled creation",code:`class Config private constructor(val env: String) {
    companion object {
        fun of(env: String) = Config(env)   // controlled instantiation
    }
}`},{t:"note",text:"object = a language-guaranteed singleton (one instance, thread-safe lazy init). A class with a private constructor restricts external creation but a companion factory can still make one or several instances (caching, fixed sets). object for a true singleton; private constructor for controlled instantiation/factories."}]},{level:"senior",q:"When should you use a sealed class versus an enum for representing states?",a:[{t:"p",text:"Use an `enum` when the cases are a *fixed set of constants* that all carry the *same shape* of data (or none) — like `Status.ACTIVE/INACTIVE`. Use a `sealed class/interface` when different cases carry *different data* — like `Result.Success(data)` vs `Result.Error(exception)`. Both give exhaustive `when`, but only sealed types let each case have its own properties."},{t:"code",title:"enum vs sealed",code:`enum class Status { LOADING, LOADED, FAILED }   // no per-case data

sealed interface Result {                        // per-case data
    data class Success(val data: List<Item>) : Result
    data class Error(val exception: Throwable) : Result
    data object Loading : Result
}`},{t:"list",items:["**enum** — fixed constants, uniform (or no) data; lightweight.","**sealed** — cases with *different* data shapes; richer state modeling.","**Both** — exhaustive `when` without `else`.","**Rule of thumb** — same data across cases → enum; different data per case → sealed."]},{t:"note",text:"enum for a fixed set of constants with uniform (or no) data (Status.ACTIVE/INACTIVE); sealed class/interface when cases carry DIFFERENT data (Success(data) vs Error(exception)). Both give exhaustive when. Same data shape → enum; different data per case → sealed."}]}],Dx={id:"classes",title:"Classes & Objects",tagline:"data classes, sealed classes/interfaces & exhaustive when, enums, value/inline classes, object singletons, and companion objects.",tags:["Kotlin","data class","sealed","value class","companion object"],content:Mx,qa:Px},Rx=[{heading:"Higher-order functions and lambdas",blocks:[{t:"p",text:"Kotlin treats functions as **first-class**: you can pass them as arguments, return them, and store them in variables. A **higher-order function** is one that takes or returns a function. A **lambda** is a function literal — an unnamed function you write inline, like `{ x -> x * 2 }`."},{t:"code",title:"Function types, lambdas, and passing them around",code:`// A function type: (Int) -> Int  means 'takes Int, returns Int'
fun transform(x: Int, op: (Int) -> Int): Int = op(x)

transform(5) { it * 2 }          // lambda; 'it' is the single implicit param -> 10
transform(5, ::square)            // pass a named function by reference

// Storing a lambda in a variable
val adder: (Int, Int) -> Int = { a, b -> a + b }
adder(2, 3)                       // 5`},{t:"list",items:["**Function type syntax**: `(A, B) -> R` — parameter types in parens, return type after the arrow. `() -> Unit` is a no-arg function returning nothing.","**`it`**: when a lambda has a single parameter, you can omit the declaration and refer to it as `it` (`list.map { it.name }`).","**Trailing lambda**: if the last parameter is a function, you can move the lambda *outside* the parentheses — `transform(5) { it * 2 }`. This is what makes DSLs and things like `launch { }` read naturally.","**Function references**: `::square` passes a named function; `String::length` a member reference; `user::save` a bound reference. Cleaner than wrapping in a lambda when you're just forwarding."]}]},{heading:"Extension functions — adding methods to existing types",blocks:[{t:"p",text:"An **extension function** lets you add a function to an existing type *without modifying it or subclassing it* — even to types you don't own (`String`, `View`, library classes). Inside the function, `this` refers to the receiver (the object it's called on)."},{t:"code",title:"Extending types you don't own",code:`fun String.isValidEmail(): Boolean =
    contains("@") && contains(".")

"a@b.com".isValidEmail()          // true — called like a member

fun View.hide() { visibility = View.GONE }   // extend Android's View
myButton.hide()`},{t:"list",items:["**How it works**: extensions are *statically resolved* — the compiler turns `\"x\".isValidEmail()` into a static call passing the string as the receiver. They don't actually modify the class or add anything to it at runtime; there's no dynamic dispatch.","**Consequence — no polymorphism**: because they're static, extensions are resolved by the *declared* type, not the runtime type. An extension on `Animal` won't be overridden by one on `Dog` based on the actual object — the *static* type at the call site decides which runs.","**Can't access private members** of the type they extend (they're outside the class), and **a member function wins** if there's a name clash (members take precedence over extensions).","**Uses**: utility functions that read naturally (`list.sumOf { }`), scoped helpers, making APIs fluent, and Kotlin's entire standard library (`map`, `filter`, `let` are all extension functions)."]}]},{heading:"inline functions — eliminating lambda overhead",blocks:[{t:"p",text:"Every lambda you pass is normally compiled into an **object** (a `Function` instance) — a small allocation per call. For higher-order functions called frequently, that adds up. The **`inline`** keyword tells the compiler to *copy the function's body (and the lambda's body) directly into the call site*, eliminating the function call and the lambda object entirely."},{t:"code",title:"inline removes the lambda allocation",code:`inline fun measure(block: () -> Unit) {
    val start = System.nanoTime()
    block()                        // body inlined at the call site — no lambda object
    println(System.nanoTime() - start)
}

// Standard library uses inline everywhere: let, run, with, apply, also,
// map, filter, forEach — so lambdas passed to them cost nothing.`},{t:"list",items:["**Benefit**: no lambda object allocated, no function-call overhead — the code is pasted inline. This is why `list.forEach { }` is as cheap as a `for` loop, and why scope functions (`let`, `apply`) are free.","**Enables non-local returns**: because the lambda body is inlined, a `return` inside a lambda passed to an inline function returns from the *enclosing* function — impossible with non-inline lambdas.","**Enables `reified` type parameters**: inline functions can use `reified T`, making the type available at runtime (see the generics topic) — e.g. `inline fun <reified T> Gson.fromJson(json: String): T`.","**Trade-offs / when NOT to inline**: inlining *duplicates* the body at every call site, so inlining a *large* function called in many places bloats the bytecode. Inline is for *small* higher-order functions where the lambda-overhead saving matters — not big functions. Use `noinline` (to exclude a specific lambda parameter) and `crossinline` (to forbid non-local returns from a lambda that's called from another context) for finer control."]}]},{heading:"The five scope functions: let, run, with, apply, also",blocks:[{t:"p",text:"Scope functions execute a block *in the context of an object*, differing in two dimensions: (1) how they refer to the object — as **`it`** (a lambda parameter) or **`this`** (the receiver), and (2) what they **return** — the object itself or the lambda's result. This table is worth memorizing:"},{t:"table",headers:["Function","Object referred to as","Returns","Typical use"],rows:[["`let`","`it`","lambda result","null-safe ops, transform, scoping a variable"],["`run`","`this`","lambda result","compute a result using the object; config + return"],["`with`","`this` (arg, not extension)","lambda result","call multiple methods on an object"],["`apply`","`this`","the object itself","configure an object then return it (builders)"],["`also`","`it`","the object itself","side effects (logging) in a chain, keep the object"]]},{t:"code",title:"Each scope function in its idiomatic use",code:`// let — null-safe transform; returns the result
val length = nullableStr?.let { it.trim().length }

// apply — configure and return the SAME object (builder-style)
val intent = Intent(this, MainActivity::class.java).apply {
    putExtra("id", 42)
    flags = Intent.FLAG_ACTIVITY_NEW_TASK
}   // returns the configured Intent

// also — side effect without breaking the chain; returns the object
val user = repository.load().also { Log.d("TAG", "loaded $it") }

// run — execute a block with 'this' and return the result
val area = rectangle.run { width * height }

// with — group calls on an object (not an extension; takes it as arg)
with(canvas) { drawRect(...); drawText(...) }`},{t:"list",items:["**Choose by return + reference**: need the *object back* → `apply` (this) or `also` (it). Need the *lambda's result* → `let`/`run` (this) or `with`. Doing null-safe work → `?.let`. Configuring a builder → `apply`. Logging in a chain → `also`.",'**`apply` vs `also`**: both return the object; `apply` uses `this` (good for setting properties: `apply { name = "x" }`), `also` uses `it` (good for side effects on the whole object: `also { log(it) }`).',"**`let` vs `run`**: both return the lambda result; `let` uses `it` (good with `?.` and for renaming/scoping), `run` uses `this` (good for calling the object's own methods).","**Don't overuse them**: nested scope functions (`apply` inside `let` inside `also`) become unreadable. Reach for a plain variable when clarity beats conciseness."]},{t:"note",text:"The memory hook: **`apply`/`also` return the object** (for chaining/config), **`let`/`run`/`with` return the result** (for computing). **`let`/`also` use `it`**, **`run`/`with`/`apply` use `this`**. All are `inline`, so they cost nothing at runtime."}]},{heading:"Default & named arguments, varargs, and more",blocks:[{t:"list",items:['**Default arguments**: parameters can have defaults (`fun greet(name: String = "Guest")`), removing the need for Java-style method overloads. Combined with **named arguments** (`greet(name = "Revanth")`), you get flexible, readable calls and can skip middle parameters.',"**`vararg`**: a variable number of arguments (`fun sum(vararg nums: Int)`); spread an existing array with `*` (`sum(*array)`).","**Infix functions**: `infix fun` allows calling without dot/parens (`1 to 2`, `x shl 3`) — for DSL-like readability.","**Local functions**: functions defined *inside* other functions, with access to the outer scope — for extracting repeated logic without polluting the class namespace.","**Single-expression functions**: `fun square(x: Int) = x * x` — omit braces and `return` for one-liners; type inferred.","**Operator overloading**: `operator fun plus(...)` lets `+`, `[]`, `in`, etc. work on your types (`operator fun get(i: Int)` enables `obj[i]`)."]}]}],Ex=[{level:"junior",q:"What is a higher-order function and a lambda?",a:[{t:"p",text:"**The concept**: Kotlin treats functions as first-class values — you can pass them as arguments, return them, and store them in variables. A **higher-order function** is one that takes a function as a parameter or returns one. A **lambda** is a function literal: an unnamed function written inline, like `{ x -> x * 2 }`."},{t:"code",title:"A higher-order function taking a lambda",code:`fun repeat(times: Int, action: (Int) -> Unit) {
    for (i in 0 until times) action(i)
}
repeat(3) { i -> println(i) }    // lambda passed as the action
repeat(3) { println(it) }         // single param can be 'it'`},{t:"p",text:"The function type `(Int) -> Unit` describes 'takes an Int, returns nothing'. When a lambda's last parameter is a function, you can write it *outside* the parentheses (a 'trailing lambda') — which is why `launch { }`, `forEach { }`, and DSLs read so naturally. Higher-order functions plus lambdas are the foundation of Kotlin's expressive standard library (`map`, `filter`, `let`) and of coroutine/Compose APIs."}]},{level:"junior",q:"What is an extension function and how does it work?",a:[{t:"p",text:"**The concept**: an extension function adds a function to an existing type *without modifying that type or subclassing it* — even for types you don't own like `String` or Android's `View`. You call it like a normal member, and inside it `this` refers to the object it was called on (the 'receiver')."},{t:"code",title:"Extending String",code:`fun String.toTitleCase(): String =
    split(" ").joinToString(" ") { it.replaceFirstChar(Char::uppercase) }

"hello world".toTitleCase()      // "Hello World"`},{t:"p",text:"**How it works under the hood**: extensions don't actually modify the class — they're *statically resolved*. The compiler turns `\"hi\".toTitleCase()` into a static function call passing the string as an argument. This has an important consequence: because it's static, an extension is dispatched by the *declared* type at the call site, not the runtime type — so extensions aren't polymorphic (an extension on `Animal` isn't 'overridden' by one on `Dog` based on the actual object). They also can't access private members of the type they extend, and if a real member function has the same name, the member wins. Despite these limits, they're everywhere in Kotlin — most of the standard library (`map`, `filter`, `let`) is extension functions."}]},{level:"junior",q:"What are the scope functions and how do you choose between them?",a:[{t:"p",text:"**The concept**: the five scope functions (`let`, `run`, `with`, `apply`, `also`) all execute a block *in the context of an object*. They differ in two ways: how they refer to the object (as `it` or as `this`), and what they return (the object itself, or the block's result)."},{t:"table",headers:["Function","Refers as","Returns"],rows:[["`let`","`it`","block result"],["`run`","`this`","block result"],["`apply`","`this`","the object"],["`also`","`it`","the object"],["`with`","`this`","block result"]]},{t:"list",items:["**`let`** — null-safe operations and transformations: `value?.let { transform(it) }`. Uses `it`, returns the result.","**`apply`** — configure an object and get it back (builder pattern): `Intent().apply { putExtra(...) }`. Uses `this`, returns the object.","**`also`** — a side effect (like logging) in a chain without breaking it: `load().also { log(it) }`. Uses `it`, returns the object.","**`run`** — compute a result using the object's members: `rect.run { width * height }`. Uses `this`, returns the result.","**`with`** — group multiple calls on an object: `with(canvas) { drawX(); drawY() }`. Takes the object as an argument, returns the result."]},{t:"p",text:"The decision: need the *object back* → `apply` (set properties via `this`) or `also` (side effect via `it`); need the *block's result* → `let`/`run`/`with`. They're all `inline`, so they compile away with zero runtime cost. The main pitfall is overusing them — deeply nested scope functions hurt readability, so a plain variable is sometimes clearer."}]},{level:"junior",q:"What is the difference between let and apply?",a:[{t:"p",text:"**Both run a block on an object, but they differ in what they return and how they reference the object.** `let` returns the *block's result* and refers to the object as `it`. `apply` returns *the object itself* and refers to it as `this`."},{t:"code",title:"The contrast",code:`// let — transform and return a NEW result; object is 'it'
val nameLength: Int? = user?.let { it.name.length }

// apply — configure the SAME object and return it; object is 'this'
val user = User().apply {
    name = "Revanth"        // 'this.name', can omit 'this'
    age = 25
}   // returns the configured User`},{t:"list",items:["**Use `let`** when you want to *do something with* an object and get a *different result* — especially null-safe transforms (`?.let { }`). The `it` reference makes it read like 'take this value and produce that'.","**Use `apply`** when you want to *configure* an object (set several properties) and get *the object back* — the builder pattern. The `this` reference lets you set properties directly (`name = ...` instead of `it.name = ...`)."]},{t:"p",text:"Mnemonic: `apply` = 'apply this configuration to the object and return the object'; `let` = 'let me compute something from this object and return that'. If you find yourself writing `.apply { }` but then using the block's result, you wanted `run`; if you write `.let { }` just to configure and return the object, you wanted `apply`."}]},{level:"senior",q:"What does the inline keyword do, and what are its benefits and costs?",a:[{t:"p",text:"**The concept**: normally, a lambda passed to a function is compiled into an *object* (a `Function` instance) — a small heap allocation per call, plus the overhead of the function call itself. Marking a higher-order function `inline` tells the compiler to *copy the function's body — and the bodies of its lambda parameters — directly into the call site*, eliminating both the function call and the lambda object."},{t:"list",items:["**Benefit 1 — no allocation/call overhead**: the lambda isn't turned into an object; the code is pasted inline. This is why `list.forEach { }` is as cheap as a `for` loop and why scope functions (`let`, `apply`) cost nothing. For higher-order functions called in hot paths, this is a real performance win.","**Benefit 2 — non-local returns**: because the lambda body is inlined into the caller, a `return` inside a lambda passed to an inline function returns from the *enclosing* function (not just the lambda). Non-inline lambdas can't do this.","**Benefit 3 — reified type parameters**: only inline functions can have `reified` type parameters, which makes the generic type available at runtime (e.g. `inline fun <reified T> parse(json: String): T` can call `T::class`). This is impossible without inlining because of type erasure."]},{t:"list",items:["**The cost — code bloat**: inlining *duplicates* the function body at every call site. Inline a *large* function called in *many* places and you multiply its bytecode everywhere, increasing method size and APK size. So `inline` is meant for *small* higher-order functions where the per-call lambda overhead actually matters — not big functions, and not functions without lambda parameters (the compiler even warns that inlining without a functional parameter is pointless).","**Fine-grained control**: `noinline` on a specific lambda parameter excludes it from inlining (e.g. when you need to store or pass that lambda elsewhere, which inlined lambdas can't be); `crossinline` marks a lambda that's inlined but must *not* do non-local returns (because it's invoked from a different execution context, like inside another lambda or object)."]},{t:"p",text:"**The senior framing**: `inline` trades bytecode size for eliminating lambda allocation and call overhead, and unlocks non-local returns and reified generics. Use it for small, frequently-called higher-order functions (which is exactly what the standard library does); avoid it for large functions where the duplication outweighs the savings. Blindly adding `inline` everywhere is an anti-pattern — it can *increase* size and even hurt performance via instruction-cache pressure."}]},{level:"senior",q:"Extension functions are statically resolved — what does that mean and what surprising behavior does it cause?",a:[{t:"p",text:"**Static resolution means the extension to call is decided at *compile time* based on the *declared* (static) type of the expression, not the *runtime* type of the object.** An extension function isn't really added to the class — the compiler rewrites `x.ext()` into a static call `ext(x)`, choosing which `ext` by the type it *thinks* `x` is at that line, not by what `x` actually is at runtime. This is the opposite of member functions, which are dispatched dynamically (virtually) by the runtime type."},{t:"code",title:"The surprising non-polymorphic behavior",code:`open class Animal
class Dog : Animal()

fun Animal.speak() = "generic sound"
fun Dog.speak() = "woof"

val pet: Animal = Dog()      // declared type Animal, runtime type Dog
println(pet.speak())         // prints "generic sound" — NOT "woof"!
// Resolved by the DECLARED type (Animal), ignoring the runtime Dog.`},{t:"list",items:["**The surprise**: even though `pet` is really a `Dog`, `pet.speak()` calls the `Animal` extension, because the compiler resolved it by `pet`'s declared type (`Animal`). If `speak()` were a *member* function overridden in `Dog`, it would print 'woof' (dynamic dispatch). Extensions have no polymorphism.","**Member wins over extension**: if the class already has a member function with the same signature, the member is always called — an extension can never override a member. So you can't use extensions to 'patch' existing behavior.","**No access to private members**: since extensions live outside the class, they can only use its public/internal API, not private fields.","**Practical implications**: don't rely on extensions for polymorphic behavior — if you need runtime dispatch, use a member function or an interface. Extensions are best for *stateless utility* on a type where the declared type is unambiguous (like extending `String` or `List`), not for behavior that should vary by subtype."]},{t:"p",text:"**Why it works this way**: extensions are a compile-time convenience (syntactic sugar over static helper functions), deliberately *not* modifying the class — so they can't participate in the virtual dispatch table. Knowing this prevents a genuinely confusing class of bugs where an extension 'should' be overridden but isn't, and it demonstrates you understand extensions are static helpers dressed up as members, not real methods."}]},{level:"junior",q:"What are default and named arguments, and how do they replace overloads?",a:[{t:"p",text:"Default arguments let a parameter have a value used when the caller omits it (`fun f(x: Int = 0)`), and named arguments let callers specify parameters by name in any order (`f(x = 5)`). Together they replace most method overloading — one function with defaults covers many call shapes, and named args make calls with many parameters readable."},{t:"code",title:"Defaults + named args",code:`fun createUser(name: String, role: String = "user", active: Boolean = true) { }

createUser("Sam")                          // uses defaults
createUser("Sam", active = false)          // named, skip role
createUser(name = "Sam", role = "admin")   // named for clarity`},{t:"list",items:["**Default arguments** — omit params that have defaults.","**Named arguments** — specify by name, any order; skip middle params.","**Replace overloads** — one function covers many signatures.","**`@JvmOverloads`** — generate overloads for Java callers (Java has no defaults)."]},{t:"note",text:"Default arguments (fun f(x = 0)) provide values when omitted; named arguments (f(x = 5)) specify by name in any order (skip middle params). Together they replace most overloading — one function covers many call shapes, and named args make many-param calls readable. @JvmOverloads exposes overloads to Java."}]},{level:"junior",q:"What is the difference between a function type and a lambda?",a:[{t:"p",text:"A *function type* like `(Int) -> String` is a *type* — the signature of a function value. A *lambda* `{ x: Int -> x.toString() }` is a *value* of that type — an anonymous function expression. So a function type describes what's expected (a parameter or variable type), and a lambda is one way to create such a value (function references are another)."},{t:"code",title:"Type vs value",code:`val transform: (Int) -> String = { it.toString() }   // type is (Int)->String; value is the lambda
fun process(op: (Int) -> Int) { }                    // parameter type
process { it * 2 }                                    // lambda argument
process(::square)                                     // function reference`},{t:"list",items:["**Function type** — `(A, B) -> R`; the type of a function value.","**Lambda** — `{ params -> body }`; an anonymous function value.","**Function reference** — `::name` / `obj::method`; another way to supply a function value.","**Trailing lambda** — a lambda as the last argument can go outside the parens."]},{t:"note",text:"A function type ((Int)->String) is a type (signature of a function value); a lambda ({ it.toString() }) is a value of that type (anonymous function). Function references (::name, obj::method) are another way to make such values. The last-argument lambda can go outside the parentheses (trailing lambda syntax)."}]},{level:"junior",q:"What is a trailing lambda, and how does the it parameter work?",a:[{t:"p",text:"If a function's *last* parameter is a function type, you can move the lambda *outside* the parentheses — `list.filter { it > 0 }`. And when a lambda has a *single* parameter, you can omit its declaration and refer to it as the implicit `it`. These two conventions make Kotlin's higher-order calls read cleanly."},{t:"code",title:"Trailing lambda + it",code:`list.filter { it > 0 }                     // trailing lambda + implicit it
list.fold(0) { acc, x -> acc + x }         // multiple params -> name them
run { println("hi") }                       // only-lambda call: no parens needed`},{t:"list",items:["**Trailing lambda** — a last function-type arg goes outside the parens.","**Only argument** — if the lambda is the sole argument, drop the parens entirely.","**`it`** — the implicit name for a single-parameter lambda.","**Multiple params** — must be named explicitly (no `it`)."]},{t:"note",text:"If the last parameter is a function type, the lambda goes outside the parentheses (trailing lambda); if it's the only argument, drop the parens entirely. A single-parameter lambda's parameter is the implicit `it`; multiple parameters must be named. These make higher-order calls read cleanly."}]},{level:"junior",q:"What is a function reference (::), and what kinds are there?",a:[{t:"p",text:"A function reference passes an existing function as a value instead of wrapping it in a lambda. `::topLevel` references a top-level/local function, `obj::method` a bound method (on a specific instance), `Class::method` an unbound one, and `::ClassName` a constructor reference. They're concise and often clearer than `{ x -> f(x) }`."},{t:"code",title:"Reference kinds",code:`list.map(::square)              // top-level function reference
list.forEach(viewModel::log)   // bound reference (on viewModel)
list.map(String::uppercase)    // unbound (receiver is the arg)
val users = names.map(::User)  // constructor reference`},{t:"list",items:["**`::function`** — top-level/local function.","**`obj::method`** — bound to a specific instance.","**`Class::method`** — unbound; the receiver becomes the first argument.","**`::ClassName`** — constructor reference."]},{t:"note",text:"Function references pass existing functions as values: ::topLevel, obj::method (bound), Class::method (unbound — receiver is the arg), ::ClassName (constructor). Cleaner than { x -> f(x) } and enable point-free style (list.map(::square))."}]},{level:"senior",q:"What is the difference between run, with, let, apply, and also?",a:[{t:"p",text:"The five scope functions differ on two axes: how they refer to the context object (`it` vs `this`) and what they return (the object vs the lambda result). `let`/`also` use `it`; `run`/`with`/`apply` use `this`. `apply`/`also` return the *object*; `let`/`run`/`with` return the *lambda result*."},{t:"table",headers:["Function","Refers as","Returns"],rows:[["let","it","lambda result"],["run","this","lambda result"],["with","this","lambda result"],["apply","this","the object"],["also","it","the object"]]},{t:"list",items:["**`let`** — transform/null-check; `it`; returns result. `x?.let { }`.","**`run`** — compute a result with `this`; also a non-extension `run { }` block.","**`with`** — like `run` but a regular function: `with(x) { }`.","**`apply`** — configure an object; `this`; returns the object. `Builder().apply { }`.","**`also`** — side effect; `it`; returns the object. `x.also { log(it) }`."]},{t:"note",text:"Scope functions differ by receiver (it: let/also; this: run/with/apply) and return (object: apply/also; result: let/run/with). let: transform/null-check; run/with: compute a result; apply: configure and return the object; also: side effect and return the object. Pick by whether you need the result or the object, and it vs this."}]},{level:"senior",q:"What is a receiver in a lambda (lambda with receiver), and how do DSLs use it?",a:[{t:"p",text:"A *lambda with receiver* has type `Receiver.() -> R` — inside it, `this` is the receiver, so you can call the receiver's members directly without qualification. This is the foundation of Kotlin DSLs (like Compose, Gradle Kotlin, `buildString`): the builder is the receiver, so the lambda body reads like a mini-language operating on it."},{t:"code",title:"Lambda with receiver",code:`fun buildUser(block: UserBuilder.() -> Unit): User =
    UserBuilder().apply(block).build()

val user = buildUser {
    name = "Sam"        // 'this' is UserBuilder — direct member access
    age = 30
}
// buildString { append("a"); append("b") }  // StringBuilder receiver`},{t:"list",items:["**`Receiver.() -> R`** — `this` inside the lambda is the receiver.","**Direct member access** — call the receiver's methods/properties unqualified.","**DSLs** — builders as receivers create readable domain-specific syntax (Compose, Gradle).","**`apply`/`with`/`buildString`** — standard examples of receiver lambdas."]},{t:"note",text:"A lambda with receiver (Receiver.() -> R) makes `this` the receiver inside, so you call its members directly without qualification — the basis of Kotlin DSLs (Compose, Gradle, buildString): the builder is the receiver, so the body reads like a domain language. apply/with use this pattern."}]},{level:"senior",q:"What does noinline do, and when do you need it?",a:[{t:"p",text:"In an `inline` function, all function-type parameters are inlined by default. `noinline` marks a specific lambda parameter to *not* be inlined — needed when you must treat that lambda as an object (store it, pass it to a non-inline function, or return it), which inlined lambdas can't be."},{t:"code",title:"noinline",code:`inline fun process(
    action: () -> Unit,             // inlined
    noinline callback: () -> Unit,  // kept as an object
) {
    action()
    registerLater(callback)          // needs a real object -> noinline
}`},{t:"list",items:["**Default** — inline function lambdas are inlined (no object created).","**`noinline`** — keep a lambda as an object; use when you store/pass/return it.","**Why** — inlined lambdas have no object identity to hold onto.","**Related** — `crossinline` for lambdas used in another execution context."]},{t:"note",text:"In an inline function, lambda params are inlined by default; noinline keeps a specific lambda as a real object — needed when you store it, pass it to a non-inline function, or return it (inlined lambdas have no object). crossinline is the related modifier for non-local-return control."}]},{level:"senior",q:"What is crossinline, and what problem does it solve?",a:[{t:"p",text:"`crossinline` marks an inline function's lambda parameter that will be called from a *different execution context* (e.g. inside another lambda, a `Runnable`, or a nested object) — where a non-local `return` from the lambda would be unsafe. It keeps the lambda inlined but *forbids non-local returns* from it, resolving the conflict."},{t:"code",title:"crossinline",code:`inline fun runOnUi(crossinline block: () -> Unit) {
    Handler(Looper.getMainLooper()).post {
        block()   // called inside another lambda -> crossinline required
    }
}
// crossinline forbids: return from block would be non-local and unsafe here`},{t:"list",items:["**`crossinline`** — inlined, but non-local `return` disallowed.","**Why** — the lambda runs in a nested context where a non-local return can't work.","**Still inlined** — no object created (unlike `noinline`).","**Use** — passing an inline lambda into a `post`/`Runnable`/nested lambda."]},{t:"note",text:"crossinline keeps a lambda inlined but forbids non-local returns — needed when the lambda is invoked from a different execution context (inside another lambda/Runnable/object) where a non-local return would be unsafe. Unlike noinline, it stays inlined (no object)."}]},{level:"senior",q:"What is a non-local return, and how does it relate to inline functions?",a:[{t:"p",text:"Normally you can't `return` from a lambda to exit the *enclosing* function. But in an *inline* function's lambda, a bare `return` *does* return from the enclosing function (a *non-local return*), because the lambda body is inlined into it. This is why `forEach { return }` can exit the outer function — `forEach` is inline. For labeled/local returns, use `return@forEach`."},{t:"code",title:"Non-local vs local return",code:`fun find(list: List<Int>): Int? {
    list.forEach { if (it > 10) return it }   // non-local: returns from find()
    return null
}
list.forEach { if (it > 10) return@forEach }  // local: continues the loop`},{t:"list",items:["**Non-local return** — `return` from an inline lambda exits the enclosing function.","**Enabled by inline** — the lambda is inlined, so the return targets the outer function.","**Local return** — `return@label` returns only from the lambda (like `continue`).","**Non-inline lambdas** — can't do non-local return (no enclosing frame to return to)."]},{t:"note",text:"In an inline function's lambda, a bare return exits the ENCLOSING function (non-local return) because the lambda is inlined into it — that's why forEach { return } exits the outer function. Use return@forEach for a local return (like continue). Non-inline lambdas can't do non-local returns."}]},{level:"senior",q:"What are reified type parameters, and why do they need inline?",a:[{t:"p",text:"Normally generic type arguments are *erased* at runtime, so you can't do `T::class` or `is T`. A `reified` type parameter (only allowed on `inline` functions) preserves the type at the call site — because the function is inlined, the concrete type is substituted in, making `T::class`, `is T`, and `as T` work. This powers APIs like `fromJson<User>()`."},{t:"code",title:"reified",code:`inline fun <reified T> Gson.fromJson(json: String): T =
    fromJson(json, T::class.java)                // T::class works because reified

val user = gson.fromJson<User>(jsonString)
inline fun <reified T> List<*>.filterByType() = filterIsInstance<T>()`},{t:"list",items:["**`reified`** — preserves the type argument at runtime; requires `inline`.","**Enables** — `T::class`, `is T`, `as T` inside the function.","**Why inline** — inlining substitutes the concrete type at the call site.","**Uses** — type-safe serialization, `filterIsInstance`, DI lookups, `getSystemService<T>()`."]},{t:"note",text:"A reified type parameter (only on inline functions) preserves the type argument at runtime — enabling T::class, is T, as T (normally impossible due to erasure) because inlining substitutes the concrete type at the call site. Powers fromJson<User>(), filterIsInstance<T>(), getSystemService<T>()."}]},{level:"junior",q:"What is an extension property, and how does it differ from an extension function?",a:[{t:"p",text:"An extension property adds a *property* (with a getter, and setter if `var`) to an existing type — `val String.firstWord: String get() = split(\" \").first()`. Like extension functions, it's resolved statically and can't add *state* (no backing field), so it must be computed. It's just syntactic sugar for a getter/setter over the receiver."},{t:"code",title:"Extension property",code:`val String.wordCount: Int
    get() = trim().split(Regex("\\\\s+")).size

"hello world".wordCount   // 2`},{t:"list",items:["**Extension property** — a computed property on an existing type (custom getter).","**No backing field** — can't store state; must be computed.","**Statically resolved** — like extension functions (based on declared type).","**Uses** — convenient derived values (`list.lastIndex`, custom accessors)."]},{t:"note",text:"An extension property adds a computed property (getter, and setter for var) to an existing type — no backing field (can't store state), statically resolved like extension functions. Sugar for a getter over the receiver; use for convenient derived values."}]},{level:"junior",q:"What is a vararg parameter, and how does the spread operator work?",a:[{t:"p",text:"`vararg` lets a function accept a variable number of arguments of a type — `fun sum(vararg nums: Int)`, callable as `sum(1, 2, 3)`. Inside, `nums` is an array. To pass an existing array *as* the varargs, use the spread operator `*`: `sum(*intArray)`."},{t:"code",title:"vararg + spread",code:`fun log(vararg messages: String) { messages.forEach { println(it) } }
log("a", "b", "c")

val arr = arrayOf("x", "y")
log(*arr)                 // spread: pass the array as varargs
log("first", *arr, "last")   // combine`},{t:"list",items:["**`vararg`** — variable number of args; treated as an array inside.","**Spread `*`** — pass an existing array as the varargs.","**Combine** — mix literal args and a spread array.","**One vararg per function** — usually the last parameter."]},{t:"note",text:'vararg accepts a variable number of args (an array inside the function): fun log(vararg m: String), called log("a", "b"). The spread operator * passes an existing array as varargs: log(*arr). One vararg per function (usually last); you can mix literals with a spread.'}]},{level:"senior",q:"What is an infix function, and when should you use one?",a:[{t:"p",text:'An `infix fun` can be called without the dot and parentheses — `a to b` instead of `a.to(b)`. It must be a member or extension function with a *single* parameter. Use it sparingly for readable, operator-like DSL syntax (`1 to "one"`, `x shl 2`, custom matchers) — only where it genuinely reads better.'},{t:"code",title:"infix",code:`infix fun Int.times(str: String) = str.repeat(this)
2 times "Bye "          // "Bye Bye "
val pair = "key" to 1   // 'to' is an infix function creating a Pair`},{t:"list",items:["**`infix fun`** — call as `a name b` (no dot/parens).","**Single parameter** — member or extension, exactly one arg.","**Readable DSLs** — `to`, bitwise (`shl`, `and`), test matchers.","**Use sparingly** — only where it clearly improves readability."]},{t:"note",text:"infix fun enables a name b syntax (no dot/parens) — must be a single-parameter member/extension. Examples: to (Pair), bitwise shl/and, test matchers. Use sparingly for operator-like readability where it genuinely reads better; overuse hurts clarity."}]},{level:"senior",q:"What is tail recursion (tailrec), and what problem does it solve?",a:[{t:"p",text:"`tailrec` marks a recursive function whose recursive call is the *last* operation, letting the compiler convert it into a *loop* — avoiding stack growth and `StackOverflowError` for deep recursion. The recursion must be in tail position (nothing done after the recursive call) for the compiler to optimize it."},{t:"code",title:"tailrec",code:`tailrec fun factorial(n: Int, acc: Long = 1): Long =
    if (n <= 1) acc else factorial(n - 1, acc * n)   // recursive call is last -> compiled to a loop`},{t:"list",items:["**`tailrec`** — compiler rewrites tail-recursive functions as loops.","**No stack growth** — avoids `StackOverflowError` for deep recursion.","**Tail position required** — the recursive call must be the last thing done.","**Accumulator pattern** — carry state in a parameter to keep the call in tail position."]},{t:"note",text:"tailrec converts a tail-recursive function (recursive call is the LAST operation) into a loop — no stack growth, no StackOverflowError for deep recursion. Requires true tail position (nothing after the recursive call); use an accumulator parameter to keep it in tail position."}]},{level:"senior",q:"What is a local function, and when is it useful?",a:[{t:"p",text:"A local function is a function *declared inside another function*. It can access the enclosing function's local variables and parameters (a closure), and it's not visible outside. Use it to factor out repeated logic within a function without polluting the class namespace or passing many parameters."},{t:"code",title:"Local function",code:`fun processOrder(order: Order): Result {
    fun validate(field: String) {          // local, sees 'order'
        require(field.isNotBlank()) { "invalid" }
    }
    validate(order.name)
    validate(order.address)
    return submit(order)
}`},{t:"list",items:["**Local function** — declared inside a function; not visible outside.","**Closure** — accesses enclosing locals/params without passing them.","**Encapsulation** — factor out repeated logic without a private method.","**Recursion** — can be recursive (and `tailrec`)."]},{t:"note",text:"A local function is declared inside another function — it closes over the enclosing locals/params (no need to pass them) and isn't visible outside. Use it to factor out repeated logic within a function without adding a private class method. Can be recursive/tailrec."}]},{level:"junior",q:"What is a single-expression function, and when do you use it?",a:[{t:"p",text:"When a function just returns one expression, you can write it with `=` instead of a block and `return` — `fun square(x: Int) = x * x`. The return type is inferred (though you can annotate it). It makes small functions concise; use a block body when there's more than a single expression."},{t:"code",title:"Expression body",code:`fun square(x: Int) = x * x                     // inferred Int
fun greet(name: String): String = "Hi $name"   // annotated return
fun statusOf(u: User) = when (u.state) {        // expression when
    Active -> "on"; else -> "off"
}`},{t:"list",items:["**`fun f() = expr`** — single-expression (expression body) function.","**Return type inferred** — or annotate for public API clarity.","**Concise** — for one-liners, `when`/`if` expressions.","**Block body** — use `{ return ... }` for multi-statement logic."]},{t:"note",text:"A single-expression function uses = instead of a block: fun square(x) = x * x (return type inferred; annotate for public API). Great for one-liners and expression when/if. Use a block body { return ... } when there's more than one expression."}]},{level:"senior",q:"How do inline functions affect lambda performance?",a:[{t:"p",text:"Normally a lambda passed to a function is compiled into an *object* (a `Function` instance), and capturing variables adds allocation. An `inline` function copies its body — and its lambda bodies — directly into the call site, so *no lambda object is created* and there's no call overhead. This is why the standard library's higher-order functions (`map`, `filter`, `forEach`) are inline: zero-overhead functional style."},{t:"list",items:["**Non-inline lambda** — allocates a `Function` object (and captures) per call.","**`inline`** — copies the body to the call site; no lambda object, no call overhead.","**Std lib** — `map`/`filter`/`let`/`run` are inline for zero-cost lambdas.","**Cost** — inlining increases bytecode size; use for small, lambda-taking functions, not large ones."]},{t:"note",text:"A non-inline lambda compiles to a Function object (allocation, call overhead); an inline function copies its body and lambdas into the call site — no lambda object, no call overhead. That's why map/filter/let/run are inline (zero-cost functional style). Trade-off: larger bytecode, so inline small lambda-taking functions, not big ones."}]},{level:"junior",q:"What is a closure, and what can a lambda capture?",a:[{t:"p",text:"A closure is a lambda (or local/anonymous function) that *captures* variables from its enclosing scope — it can read and, unlike Java, *modify* captured `var`s. The lambda keeps those variables alive as long as it exists. This is powerful but can cause leaks if a long-lived lambda captures a large object (or an Activity)."},{t:"code",title:"Capturing",code:`fun counter(): () -> Int {
    var count = 0
    return { ++count }     // captures and mutates 'count'
}
val next = counter()
next(); next()             // 1, 2 — 'count' persists in the closure`},{t:"list",items:["**Captures enclosing variables** — reads and (unlike Java) mutates `var`s.","**Keeps them alive** — captured variables live as long as the lambda.","**Leak risk** — a retained lambda capturing an Activity/large object leaks it.","**Kotlin vs Java** — Kotlin closures can modify captured variables (Java requires effectively-final)."]},{t:"note",text:"A closure is a lambda that captures variables from its enclosing scope — reading and (unlike Java's effectively-final) modifying captured vars, keeping them alive as long as the lambda. Powerful, but a retained lambda capturing an Activity/large object leaks it — be mindful in callbacks/coroutines."}]},{level:"senior",q:"What is the difference between a top-level function and a method?",a:[{t:"p",text:"Kotlin allows *top-level functions* — declared directly in a file, not inside a class. They're great for utilities and don't require a wrapper class (unlike Java's static methods in a `Utils` class). A *method* is a function that belongs to a class/object and has access to its state. Prefer top-level functions for stateless utilities; methods when behavior operates on instance state."},{t:"list",items:["**Top-level function** — file-level, no class; for stateless utilities.","**Method** — member of a class/object; accesses instance state.","**No Utils classes** — top-level replaces Java's static-method holders.","**JVM** — top-level functions compile to static methods in a `FileNameKt` class (for Java interop)."]},{t:"note",text:"Top-level functions are declared at file level (no class) — ideal for stateless utilities, replacing Java's static-method Utils classes. Methods belong to a class/object and access its state. On the JVM, top-level functions become static methods in a FileNameKt class for Java interop."}]},{level:"junior",q:"How do you choose between let, run, apply, also, and with in practice?",a:[{t:"p",text:"Pick by intent: `apply` to *configure* an object and return it (`Builder().apply { }`); `also` for a *side effect* that returns the object (`x.also { log(it) }`); `let` to *transform* or null-check (`x?.let { }`); `run`/`with` to *compute a result* from an object's members. Consistency and readability matter more than rigid rules."},{t:"list",items:["**Configure + return object** → `apply` (`this`).","**Side effect + return object** → `also` (`it`).","**Transform / null-check → result** → `let` (`it`).","**Compute result from members** → `run`/`with` (`this`)."]},{t:"code",title:"In practice",code:`val user = User().apply { name = "Sam"; age = 30 }   // configure
val len = text?.let { it.trim().length } ?: 0        // transform + null-check
logger.also { it.log("start") }                       // side effect
val area = rect.run { width * height }                // compute from members`},{t:"note",text:"Choose by intent: apply = configure and return the object (this); also = side effect returning the object (it); let = transform/null-check returning a result (it); run/with = compute a result from members (this). Consistency/readability beats rigid rules — many teams standardize on a subset."}]},{level:"junior",q:"What is a higher-order function, and why are they powerful?",a:[{t:"p",text:"A higher-order function *takes a function as a parameter and/or returns one*. This enables passing behavior as data — you parameterize *what* to do, not just *with what*. It's the basis of the collection operators (`map`, `filter`), callbacks, and building reusable abstractions (retry, measureTime, resource management)."},{t:"code",title:"Behavior as a parameter",code:`fun <T> retry(times: Int, block: () -> T): T {   // takes a function
    repeat(times - 1) { runCatching { return block() } }
    return block()
}
fun multiplier(factor: Int): (Int) -> Int = { it * factor }   // returns a function
retry(3) { api.load() }`},{t:"list",items:["**Takes/returns functions** — behavior passed as values.","**Parameterize behavior** — the caller supplies the 'what to do'.","**Reusable abstractions** — retry, timing, transactions, resource wrappers.","**Foundation** — collection ops, callbacks, DSLs."]},{t:"note",text:"A higher-order function takes and/or returns functions — parameterizing behavior (the 'what to do'), not just data. It's the basis of collection operators, callbacks, and reusable abstractions (retry, timing, resource management). Combined with inline, it's zero-overhead."}]},{level:"senior",q:"How does the use function work for resource management?",a:[{t:"p",text:"`use { }` is an extension on `Closeable`/`AutoCloseable` that runs a block and *guarantees the resource is closed* afterward — even on exception — returning the block's result. It's Kotlin's try-with-resources equivalent, and it correctly handles exceptions during close (adding them as suppressed)."},{t:"code",title:"use for auto-close",code:`val text = File("data.txt").bufferedReader().use { reader ->
    reader.readText()          // reader.close() called automatically, even on error
}
FileInputStream(path).use { stream -> stream.readBytes() }`},{t:"list",items:["**`use { }`** — runs the block, closes the resource in a `finally`.","**Exception-safe** — closes even if the block throws; close errors added as suppressed.","**Returns the block result** — assign it directly.","**Replaces** — manual try/finally close boilerplate."]},{t:"note",text:"use { } (on Closeable/AutoCloseable) runs a block and guarantees close() afterward — even on exception (close errors added as suppressed), returning the block's result. Kotlin's try-with-resources; replaces manual try/finally close boilerplate."}]},{level:"senior",q:"What is the difference between a lambda and an anonymous function?",a:[{t:"p",text:"A *lambda* (`{ x -> x + 1 }`) is concise but has restrictions: no explicit return type, and `return` means non-local return. An *anonymous function* (`fun(x: Int): Int { return x + 1 }`) looks like a regular function without a name — it lets you specify the return type explicitly and use a normal (local) `return`. Use anonymous functions when you need an explicit return type or a local return in the function value."},{t:"code",title:"Lambda vs anonymous function",code:`val a = { x: Int -> x + 1 }                       // lambda
val b = fun(x: Int): Int { return x + 1 }         // anonymous function (explicit return type)
list.filter(fun(x): Boolean {
    if (x < 0) return false                        // LOCAL return (returns from the anon fn)
    return x % 2 == 0
})`},{t:"list",items:["**Lambda** — concise; return type inferred; `return` is non-local (in inline contexts).","**Anonymous function** — explicit return type; `return` is local to it.","**Use anon fn** — when you need an explicit return type or local returns.","**Prefer lambdas** — for most cases (shorter, idiomatic)."]},{t:"note",text:"A lambda ({ x -> ... }) is concise (inferred return type; return is non-local). An anonymous function (fun(x): Int { return ... }) allows an explicit return type and normal LOCAL return. Use anonymous functions when you need an explicit return type or local returns; prefer lambdas otherwise."}]},{level:"junior",q:"How do labels work for returning from or continuing a lambda?",a:[{t:"p",text:"Labels let you control *where* a `return`/`break`/`continue` targets. `return@forEach` returns from just the lambda (like `continue` for the loop), while a bare `return` in an inline lambda returns from the enclosing function. You can also define custom labels (`loop@ for ... break@loop`) for nested loops."},{t:"code",title:"Labeled returns",code:`list.forEach {
    if (it == 0) return@forEach     // skip this element (like continue)
    process(it)
}
outer@ for (i in rows) {
    for (j in cols) { if (done) break@outer }   // break the outer loop
}`},{t:"list",items:["**`return@lambda`** — local return from the lambda (continue-like).","**Bare `return`** — non-local (exits the enclosing function) in inline lambdas.","**Custom labels** — `label@` on loops; `break@label`/`continue@label`.","**Implicit label** — the function name (`return@forEach`)."]},{t:"note",text:"Labels target control flow: return@forEach does a LOCAL return from the lambda (continue-like), while a bare return in an inline lambda is non-local (exits the enclosing function). Custom labels (outer@ for ... break@outer) control nested loops. The implicit lambda label is the function name."}]}],Ux={id:"functions-lambdas",title:"Functions, Lambdas & Scope Functions",tagline:"Higher-order functions & lambdas, extension functions (static resolution), inline/reified, the five scope functions, and default/named args.",tags:["Kotlin","Lambdas","Extensions","inline","Scope Functions"],content:Rx,qa:Ex},qx=[{heading:"Read-only vs mutable collections",blocks:[{t:"p",text:"Kotlin splits collections into **read-only** and **mutable** interfaces. `List<T>`, `Set<T>`, `Map<K,V>` are read-only (no add/remove methods). `MutableList<T>`, `MutableSet<T>`, `MutableMap<K,V>` add modification methods. You choose the type by whether callers should be able to modify it."},{t:"code",title:"Creating collections",code:`val readOnly = listOf(1, 2, 3)          // List<Int> — no add/remove
val mutable = mutableListOf(1, 2, 3)   // MutableList<Int> — can modify
mutable.add(4)

val map = mapOf("a" to 1, "b" to 2)    // read-only Map
val mmap = mutableMapOf<String, Int>() // mutable

val set = setOf(1, 2, 2, 3)            // Set — duplicates removed -> {1,2,3}`},{t:"list",items:["**Read-only is not immutable**: `List` is an *interface* without mutators, but the underlying object might still be a mutable list held elsewhere. A `val list: List<Int>` prevents *you* from modifying it through that reference, but doesn't guarantee the data can't change via another reference. For true immutability use `kotlinx.collections.immutable` (`persistentListOf`).","**Expose read-only, use mutable internally**: the idiom is to keep a `private val _items = mutableListOf()` and expose `val items: List<T> = _items` — so only the owner mutates. (Same principle as the StateFlow backing-property pattern.)","**Default to read-only**: prefer `listOf`/`mapOf` unless you specifically need mutation — it communicates intent and prevents accidental modification."]}]},{heading:"The functional operators — map, filter, and friends",blocks:[{t:"code",title:"The everyday transformations",code:`val users = listOf(User("A", 25), User("B", 17), User("C", 30))

users.map { it.name }                    // [A, B, C] — transform each
users.filter { it.age >= 18 }            // adults only
users.first { it.age > 20 }              // first match (throws if none)
users.firstOrNull { it.age > 100 }       // null if none
users.any { it.age < 18 }                // true — at least one matches
users.all { it.age > 0 }                 // true — every element matches
users.count { it.age >= 18 }             // 2
users.sortedBy { it.age }                // sorted copy
users.groupBy { it.age >= 18 }           // Map<Boolean, List<User>>
users.associateBy { it.name }            // Map<String, User> keyed by name
users.sumOf { it.age }                   // 72
users.maxByOrNull { it.age }             // User C
users.partition { it.age >= 18 }         // Pair<adults, minors>
users.flatMap { it.tags }                // flatten nested lists`},{t:"list",items:["**These are extension functions on `Iterable`** returning *new* collections — the originals are never modified (functional style). `map`/`filter` are the core; the rest cover common needs (grouping, aggregation, searching).","**`map` vs `flatMap`**: `map` is 1:1 (each element → one result); `flatMap` is 1:many flattened (each element → a list, all concatenated).","**`associate`/`associateBy`/`groupBy`**: build maps from lists — `associateBy { it.id }` makes a lookup map, `groupBy { it.category }` buckets elements.","**`first`/`find`/`single`**: `first { }` throws if no match, `firstOrNull`/`find` returns null, `single { }` requires exactly one."]}]},{heading:"The eager evaluation problem",blocks:[{t:"p",text:"Standard collection operators are **eager**: each one processes the *entire* collection and creates a *new intermediate collection* before the next operator runs. Chaining several means allocating a full list at each step and iterating multiple times — fine for small collections, wasteful for large ones."},{t:"code",title:"Eager: multiple passes and intermediate lists",code:`val result = hugeList
    .map { it * 2 }        // creates a full new list (pass 1)
    .filter { it > 10 }    // creates ANOTHER full list (pass 2)
    .take(5)               // creates ANOTHER list, then takes 5 (pass 3)
// Even though we only wanted 5 items, map ran over the ENTIRE list first.`},{t:"p",text:"In that example, `map` transforms all million elements, then `filter` scans all of them, then `take(5)` keeps just 5 — we did a million transforms and a million filters to get 5 results, plus two throwaway intermediate lists. That's the eager evaluation cost."}]},{heading:"Sequences — lazy evaluation",blocks:[{t:"p",text:"A **`Sequence`** evaluates **lazily**: operators aren't applied until a *terminal* operation pulls values, and elements flow through the *whole chain one at a time* rather than the whole collection through each operator. No intermediate collections, and short-circuiting operators like `take`/`first` stop early. It's the collections analog of Kotlin Flow (Sequence is to collections what Flow is to async streams)."},{t:"code",title:"Lazy: one element at a time, short-circuits",code:`val result = hugeList.asSequence()
    .map { it * 2 }        // lazy — not run yet
    .filter { it > 10 }    // lazy
    .take(5)               // lazy
    .toList()              // TERMINAL — now it runs

// Now each element flows through map->filter one at a time, and the
// whole thing STOPS as soon as 5 pass the filter. No intermediate lists.`},{t:"list",items:["**How it differs**: eager processes *operator by operator* (all elements through map, then all through filter); lazy processes *element by element* (element 1 through the whole chain, then element 2…), stopping when a terminal like `take(5)`/`first` is satisfied.","**Terminal vs intermediate**: sequence operators are intermediate (lazy) until a terminal operation (`toList`, `first`, `sum`, `count`) triggers evaluation — exactly like Flow.","**When sequences win**: large collections, long operator chains, and short-circuiting (`take`, `first`, `any`) where you don't need to process everything. They avoid intermediate allocations and can stop early.","**When they DON'T help (or hurt)**: small collections — the lazy machinery has per-element overhead that outweighs the benefit, so eager is actually faster for small data. Don't reflexively `.asSequence()` everything."]}]},{heading:"Collections vs Sequences — the decision",blocks:[{t:"table",headers:["","Collection (eager)","Sequence (lazy)"],rows:[["Evaluation","each operator processes all elements immediately","elements flow through the whole chain lazily"],["Intermediate collections","one per operator","none"],["Passes over data","one per operator","single pass, element by element"],["Short-circuits (take/first)","no — earlier operators already ran fully","yes — stops as soon as satisfied"],["Best for","small collections, few operators","large data, long chains, early termination"]]},{t:"note",text:"Rule of thumb: use plain collection operators by default (simpler, faster for small data). Switch to `.asSequence()` when the collection is large *and* you have multiple chained operations *or* can short-circuit (take/first/any) — that's when avoiding intermediate lists and early termination pays off. For small lists, sequences can be slower due to overhead."}]},{heading:"Other collection essentials",blocks:[{t:"list",items:["**`Array` vs `List`**: `Array<T>` is a fixed-size, mutable, JVM-array-backed structure (use for performance-critical fixed data or interop); `List` is the general-purpose interface. Prefer `List` unless you need array semantics. Specialized `IntArray`/`FloatArray` avoid boxing.","**Destructuring**: `for ((key, value) in map) { }`, `val (a, b) = pair` — works with data classes, pairs, and map entries via `componentN()`.","**`zip`/`unzip`**: `listA zip listB` pairs elements; useful for combining parallel lists.","**`fold`/`reduce`**: accumulate to a single value — `fold(0) { acc, x -> acc + x }` (with initial value), `reduce { acc, x -> ... }` (uses first element as initial, throws on empty).","**`windowed`/`chunked`**: `chunked(3)` splits into groups of 3; `windowed(2)` creates sliding windows — handy for batching and pairwise processing.","**Null handling in collections**: `filterNotNull()`, `mapNotNull { }` (map and drop nulls in one step), `firstNotNullOfOrNull { }`."]}]}],Ox=[{level:"junior",q:"What is the difference between List and MutableList?",a:[{t:"p",text:"**The concept**: Kotlin separates collections into read-only and mutable interfaces. `List<T>` is read-only — it has access methods (`get`, `size`, `contains`) but *no* methods to add or remove elements. `MutableList<T>` extends it with mutation methods (`add`, `remove`, `clear`). You pick the type based on whether the code holding it should be able to modify it."},{t:"code",title:"The two types",code:`val ro: List<Int> = listOf(1, 2, 3)          // no add/remove
val mut: MutableList<Int> = mutableListOf(1) // can modify
mut.add(2)`},{t:"p",text:"The important nuance: **read-only is not the same as immutable.** `List` is just an *interface* without mutators — but the actual object could be a `MutableList` referenced elsewhere and changed through that other reference. So `val list: List<Int>` prevents modification *through that variable*, not modification of the data entirely. The idiomatic pattern for safe encapsulation is to keep a private mutable collection and expose it as read-only (`private val _items = mutableListOf(); val items: List<T> = _items`), so only the owner can mutate. For genuine immutability you'd use `kotlinx.collections.immutable`. Default to read-only types unless you actually need mutation."}]},{level:"junior",q:"Explain map, filter, and a few other common collection operators.",a:[{t:"list",items:["**`map { }`** — transform each element into a new one, returning a new list: `users.map { it.name }` → list of names.","**`filter { }`** — keep only elements matching a predicate: `users.filter { it.age >= 18 }`.","**`first { }` / `firstOrNull { }` / `find { }`** — get the first matching element; `first` throws if none, `firstOrNull`/`find` return null.","**`any { }` / `all { }` / `none { }`** — boolean checks: at least one matches / all match / none match.","**`groupBy { }` / `associateBy { }`** — build maps: `groupBy` buckets elements by a key into lists; `associateBy` makes a lookup map keyed by a property.","**`sumOf { }` / `maxByOrNull { }` / `sortedBy { }`** — aggregate, find extremes, sort (returning a new sorted list)."]},{t:"p",text:"These are extension functions on `Iterable` that return *new* collections without modifying the original — functional style. They compose into readable pipelines: `users.filter { it.active }.sortedBy { it.name }.map { it.email }`. The one thing to keep in mind is that each operator on a regular collection is *eager* — it processes the whole collection and creates a new intermediate list — which matters for large data (where sequences help) but is fine for typical small lists."}]},{level:"junior",q:"What is the difference between map and flatMap?",a:[{t:"p",text:"**`map` is one-to-one; `flatMap` is one-to-many, flattened.** `map { }` transforms each element into exactly one result, giving a list of the same size. `flatMap { }` transforms each element into a *list* (or iterable), then concatenates all those lists into one flat result."},{t:"code",title:"The difference",code:`val users = listOf(
    User("A", tags = listOf("x", "y")),
    User("B", tags = listOf("z")),
)

users.map { it.tags }        // [[x, y], [z]]  — a list of lists
users.flatMap { it.tags }    // [x, y, z]       — flattened into one list`},{t:"p",text:"Use `map` when each element becomes one new element (names from users). Use `flatMap` when each element expands into *multiple* elements you want combined — like collecting all tags across all users into a single list, or all order-items across all orders. It's essentially `map` followed by `flatten`. This is the collections analog of `flatMapConcat` in Flow — the same 'each element produces a collection, merge them all' idea."}]},{level:"junior",q:"What is a Sequence and how is it different from a List?",a:[{t:"p",text:"**The concept**: a `List`'s operators are *eager* — each one (`map`, `filter`) processes the whole list immediately and produces a new intermediate list. A `Sequence`'s operators are *lazy* — nothing runs until a terminal operation (like `toList()` or `first()`), and then elements flow through the *entire chain one at a time* instead of the whole collection passing through each operator."},{t:"code",title:"Lazy vs eager",code:`// List (eager): map processes all, then filter processes all, then take
list.map { f(it) }.filter { g(it) }.take(5)

// Sequence (lazy): each element flows through map->filter->take;
// stops as soon as 5 elements pass
list.asSequence().map { f(it) }.filter { g(it) }.take(5).toList()`},{t:"p",text:"The practical difference: with a List, a chain of operators creates a new intermediate collection at each step and iterates multiple times. With a Sequence, there are no intermediate collections, it's a single pass, and short-circuiting operators like `take(5)`/`first` can *stop early* without processing the rest. So sequences shine for large collections with long operator chains or early termination. But for *small* collections, sequences are actually slower (the lazy machinery has per-element overhead), so you shouldn't convert everything to sequences reflexively. It's essentially the collections version of Kotlin Flow."}]},{level:"senior",q:"When should you use a Sequence over regular collection operators, and when would it hurt?",a:[{t:"p",text:"**The decision comes down to collection size, chain length, and whether you can short-circuit.** The costs sequences avoid — intermediate collection allocations and multiple passes — only matter when there's enough data and enough operators for those costs to be significant. Sequences also add a *fixed per-element overhead* (each element is pushed through the operator chain via iterator calls), which can dominate for small data."},{t:"list",items:["**Use a Sequence when**: (1) the collection is *large*, (2) you chain *multiple* operators (each intermediate list you avoid is a real saving), or (3) you can *short-circuit* — `take(n)`, `first { }`, `any { }` — so lazy evaluation stops early instead of processing everything. The classic win: `hugeList.asSequence().map { }.filter { }.first { }` processes only until the first match, whereas the eager version transforms and filters the *entire* list first.","**Sequences HURT when**: the collection is *small* (dozens of elements) — the lazy iterator overhead per element outweighs the avoided allocations, so plain operators are measurably faster. Also when you have a *single* operator (no intermediate list to avoid) or need the result materialized anyway.","**A subtle correctness point**: some operations (like `sorted`) are inherently *stateful* — they must see all elements, so within a sequence they materialize the whole thing internally, negating the laziness up to that point. Sequences help most with *stateless* operations (map/filter/take)."]},{t:"code",title:"The clearest win — early termination on large data",code:`// EAGER: transforms and filters ALL million items to find one
millionItems.map { expensive(it) }.filter { it.valid }.first()

// LAZY: stops at the first valid item — maybe processes only a handful
millionItems.asSequence().map { expensive(it) }.filter { it.valid }.first()`},{t:"p",text:"**The senior takeaway**: don't cargo-cult `.asSequence()`. The heuristic is 'large collection + multiple operators, or short-circuiting'. For a list of 20 UI items with two operators, plain collection functions are simpler and faster. For a million-element pipeline with a `first`/`take`, sequences can be dramatically faster. Measure if unsure — the crossover point depends on element count and operator cost."}]},{level:"senior",q:"'Read-only' collections aren't truly immutable. Explain the risk and how to get real immutability.",a:[{t:"p",text:"**The subtlety**: `List<T>` is a read-only *interface* — it lacks mutator methods, so you can't modify the collection *through a `List` reference*. But the underlying object at runtime might actually be a `MutableList`, and if another reference to it exists as `MutableList`, that reference can still change the data — and your `List` view reflects the change. Read-only means 'this view can't mutate', not 'this data can never change'."},{t:"code",title:"The read-only view can change under you",code:`val mutable = mutableListOf(1, 2, 3)
val readOnly: List<Int> = mutable          // a read-only VIEW of the same object

mutable.add(4)                              // mutate via the mutable reference
println(readOnly)                           // [1, 2, 3, 4] — the view changed!`},{t:"list",items:["**The risk in practice**: you expose a `List` from a class thinking it's safe, but you kept a `MutableList` internally and hand out the *same object* upcast to `List`. If the caller downcasts (`readOnly as MutableList`) or if the internal mutation happens while the caller iterates, you get surprises — including `ConcurrentModificationException` or, in Compose, stale UI because the 'immutable' state actually mutated in place (breaking equality-based skipping).","**Real immutability option 1 — defensive copy**: expose `_items.toList()`, which creates a genuinely separate list. Safe, but copies on every access (cost for large/frequent).","**Real immutability option 2 — `kotlinx.collections.immutable`**: `persistentListOf()` / `toImmutableList()` give truly immutable collections (`ImmutableList`) that *cannot* be mutated by anyone, with efficient structural sharing for 'modifications' (they return new versions). These are also what Compose recognizes as stable for recomposition skipping.","**Real immutability option 3 — don't share the mutable reference**: keep the `MutableList` strictly private and only ever expose copies or immutable types, never the same object upcast."]},{t:"p",text:"**Why it matters for Android specifically**: Compose stability relies on immutability — an `@Immutable`/`@Stable` data class holding a `List` is only safe if that list truly never mutates; a read-only view of a mutable list breaks that promise and causes stale UI. So for Compose state, prefer `ImmutableList` (compiler-recognized) or defensive copies over exposing read-only views of internally-mutable collections. The general principle: read-only is an *access restriction*, immutability is a *data guarantee* — know which one you actually have."}]},{level:"junior",q:"What are the main collection types, and when do you use each?",a:[{t:"p",text:"Kotlin's core collections are `List` (ordered, allows duplicates, indexed access), `Set` (unordered, no duplicates, fast membership), and `Map` (key→value lookups). Each has read-only and `Mutable` variants. Pick by access pattern: ordered/indexed → List; uniqueness/contains → Set; keyed lookup → Map."},{t:"list",items:["**`List`** — ordered, indexed, allows duplicates; the default sequence of items.","**`Set`** — no duplicates, fast `contains`; use for uniqueness/membership.","**`Map`** — key→value; fast lookup by key.","**Read-only vs `Mutable`** — `List` vs `MutableList`, etc.; expose read-only, mutate privately."]},{t:"code",title:"The three",code:`val list = listOf(1, 2, 2, 3)          // [1,2,2,3]
val set = setOf(1, 2, 2, 3)            // [1,2,3]
val map = mapOf("a" to 1, "b" to 2)    // key -> value`},{t:"note",text:"List (ordered, indexed, duplicates allowed), Set (unique, fast contains), Map (key→value lookup) — each with read-only and Mutable variants. Choose by access: ordered/indexed → List, uniqueness/membership → Set, keyed lookup → Map. Expose read-only, mutate privately."}]},{level:"junior",q:"What's the difference between listOf, mutableListOf, emptyList, and buildList?",a:[{t:"p",text:"`listOf(...)` creates a read-only list; `mutableListOf(...)` a mutable one; `emptyList()` a read-only empty list (shared singleton, allocation-free); `buildList { }` lets you construct a list imperatively (add in a lambda) and returns a read-only result — combining mutation during building with immutability after."},{t:"code",title:"Building lists",code:`val a = listOf(1, 2, 3)             // read-only
val b = mutableListOf(1, 2)         // mutable
b.add(3)
val c = emptyList<Int>()            // read-only empty
val d = buildList {                 // build then freeze
    add(1); if (cond) add(2)
}   // returns read-only List`},{t:"list",items:["**`listOf`** — read-only, fixed contents.","**`mutableListOf`** — mutable, add/remove.","**`emptyList()`** — read-only empty (shared, no allocation).","**`buildList { }`** — imperative construction, read-only result; cleaner than build-a-mutable-then-return."]},{t:"note",text:"listOf = read-only; mutableListOf = mutable; emptyList() = read-only empty (shared singleton, no allocation); buildList { add(...) } = imperative construction returning a read-only list (mutate while building, immutable after). Same family: buildSet/buildMap."}]},{level:"senior",q:"What is the difference between an Array and a List, and IntArray vs Array<Int>?",a:[{t:"p",text:"An `Array` is a fixed-size, mutable, JVM-backed array; a `List` is a richer collection interface (read-only or mutable, resizable for `MutableList`). For primitives, `IntArray`/`DoubleArray` map to JVM primitive arrays (`int[]`) with *no boxing*, while `Array<Int>` boxes each element (`Integer[]`). Prefer `List` for general use; use primitive arrays in performance-critical numeric code."},{t:"code",title:"Array vs List, boxing",code:`val arr = intArrayOf(1, 2, 3)     // IntArray -> int[], no boxing
val boxed = arrayOf(1, 2, 3)      // Array<Int> -> Integer[], boxed
val list = listOf(1, 2, 3)        // List<Int> -> boxed too, but richer API`},{t:"list",items:["**`Array`** — fixed size, mutable elements; lower-level.","**`List`** — collection interface, richer operators; `MutableList` is resizable.","**`IntArray` etc.** — primitive arrays, no boxing; use in hot numeric loops.","**`Array<Int>`/`List<Int>`** — box elements; fine for general code."]},{t:"note",text:"Array = fixed-size mutable JVM array; List = richer collection interface (MutableList resizable). Primitive arrays (IntArray→int[]) avoid boxing; Array<Int>/List<Int> box elements (Integer). Prefer List generally; use IntArray/DoubleArray in performance-critical numeric code."}]},{level:"junior",q:"What does groupBy do?",a:[{t:"p",text:"`groupBy { key }` transforms a collection into a `Map` where each key maps to the *list* of elements that produced it — grouping elements by a computed key. `groupBy(keySelector, valueTransform)` also transforms the grouped values. It's the go-to for bucketing data (by category, first letter, date)."},{t:"code",title:"groupBy",code:`val byFirst = words.groupBy { it.first() }        // Map<Char, List<String>>
val namesByAge = users.groupBy({ it.age }, { it.name })   // Map<Int, List<String>>
// For counts, groupingBy + eachCount:
val counts = words.groupingBy { it.first() }.eachCount()`},{t:"list",items:["**`groupBy { key }`** — `Map<Key, List<Element>>`.","**With value transform** — `groupBy(key, value)` maps to transformed values.","**`groupingBy { }.eachCount()`** — efficient counting per group.","**Uses** — bucketing by category, section headers, histograms."]},{t:"note",text:"groupBy { key } → Map<Key, List<Element>> (bucket elements by a computed key); groupBy(key, valueTransform) maps grouped values. For counts use groupingBy { }.eachCount(). Great for categories, section lists, histograms."}]},{level:"senior",q:"What is the difference between associate, associateBy, and associateWith?",a:[{t:"p",text:"These build a `Map` from a collection but differ in what becomes the key/value. `associateBy { key }` uses the element as the *value* and a selector as the *key* (index by id); `associateWith { value }` uses the element as the *key* and a selector as the *value*; `associate { key to value }` lets you produce both from each element."},{t:"code",title:"The three associates",code:`users.associateBy { it.id }              // Map<Id, User> — index by id
ids.associateWith { fetch(it) }          // Map<Id, Data> — key is the element
users.associate { it.id to it.name }     // Map<Id, String> — custom both`},{t:"list",items:["**`associateBy { key }`** — element as value, selector as key (common: index by id).","**`associateWith { value }`** — element as key, selector as value.","**`associate { k to v }`** — both from the element.","**Duplicate keys** — later entries overwrite earlier ones."]},{t:"note",text:"associateBy { key } → element as value, selector as key (index by id); associateWith { value } → element as key, selector as value; associate { k to v } → both from the element. Duplicate keys: later wins. associateBy is the usual 'index a list by id' tool."}]},{level:"junior",q:"What is the difference between fold and reduce for collections?",a:[{t:"p",text:"Both combine a collection into a single value. `reduce { acc, x -> }` starts with the *first element* as the accumulator (and throws on an empty collection). `fold(initial) { acc, x -> }` starts with an *explicit initial value*, so it handles empty collections and can produce a *different result type* than the elements."},{t:"code",title:"fold vs reduce",code:`listOf(1, 2, 3).reduce { acc, x -> acc + x }        // 6 (throws if empty)
listOf(1, 2, 3).fold(10) { acc, x -> acc + x }      // 16 (seed, safe on empty)
listOf("a", "b").fold(StringBuilder()) { sb, s -> sb.append(s) }  // different type`},{t:"list",items:["**`reduce`** — seed is the first element; throws on empty; result type = element type.","**`fold(initial)`** — explicit seed; safe on empty; result type can differ.","**`runningFold`/`runningReduce`** — produce intermediate accumulations.","**`foldRight`/`reduceRight`** — combine from the end."]},{t:"note",text:"reduce { acc, x } seeds with the first element (throws on empty, result = element type); fold(initial) { acc, x } uses an explicit seed (safe on empty, result type can differ). Use fold for empty-safety and type changes; runningFold/runningReduce for intermediate steps."}]},{level:"junior",q:"What does partition do, and how is it different from filter?",a:[{t:"p",text:"`partition { predicate }` splits a collection into *two* lists in one pass — a `Pair` of (matching, non-matching). `filter` gives only the matching elements (you'd need a second `filterNot` for the rest). Use `partition` when you need both groups."},{t:"code",title:"partition",code:`val (adults, minors) = people.partition { it.age >= 18 }
// vs two passes:
val adults2 = people.filter { it.age >= 18 }
val minors2 = people.filterNot { it.age >= 18 }`},{t:"list",items:["**`partition { }`** — `Pair<List, List>` of (true, false) in one pass, destructurable.","**`filter`** — only the matching elements.","**One pass** — `partition` avoids iterating twice.","**Destructure** — `val (yes, no) = ...`."]},{t:"note",text:"partition { } splits into a Pair of (matching, non-matching) lists in ONE pass (destructurable: val (yes, no) = ...); filter returns only matches (needs a second filterNot for the rest). Use partition when you need both groups."}]},{level:"junior",q:"What do zip and unzip do?",a:[{t:"p",text:"`zip` pairs elements of two collections positionally into a list of `Pair`s (stopping at the shorter one), optionally with a transform. `unzip` does the reverse — turns a list of pairs into a `Pair` of two lists. Useful for combining parallel lists (names + ages) or splitting them."},{t:"code",title:"zip / unzip",code:`val names = listOf("A", "B"); val ages = listOf(1, 2, 3)
names.zip(ages)                       // [(A,1), (B,2)] — stops at shorter
names.zip(ages) { n, a -> "$n:$a" }   // ["A:1", "B:2"]
listOf("A" to 1, "B" to 2).unzip()    // (["A","B"], [1,2])`},{t:"list",items:["**`zip`** — positional pairs; stops at the shorter collection.","**`zip { }`** — combine with a transform.","**`unzip`** — split a list of pairs into two lists.","**Uses** — combining/splitting parallel data."]},{t:"note",text:"zip pairs two collections positionally (stops at the shorter), optionally with a transform; unzip splits a list of pairs back into two lists. Use for combining or splitting parallel data (names + ages)."}]},{level:"junior",q:"How do you sort collections with multiple criteria?",a:[{t:"p",text:"`sortedBy { selector }` sorts by one key ascending; `sortedByDescending` descends. For *multiple* criteria, use `sortedWith(compareBy({ ... }).thenBy({ ... }))` — sort by the first key, breaking ties with the next. `compareByDescending`/`thenByDescending` handle descending keys."},{t:"code",title:"Multi-key sorting",code:`users.sortedBy { it.age }                       // single key
users.sortedWith(
    compareBy<User> { it.lastName }
        .thenBy { it.firstName }
        .thenByDescending { it.age }
)`},{t:"list",items:["**`sortedBy`/`sortedByDescending`** — single key.","**`sortedWith(comparator)`** — custom/multi-key ordering.","**`compareBy(...).thenBy(...)`** — primary then tie-breakers.","**Stable sort** — equal elements keep their relative order."]},{t:"note",text:"sortedBy/sortedByDescending for one key; sortedWith(compareBy { }.thenBy { }.thenByDescending { }) for multiple criteria (primary then tie-breakers). Kotlin's sort is stable (equal elements keep order). Returns a new sorted list; sortedArray/sort() for in-place on mutable."}]},{level:"junior",q:"What do distinct and distinctBy do?",a:[{t:"p",text:"`distinct()` removes duplicate elements (by `equals`), preserving first-occurrence order. `distinctBy { selector }` removes elements with duplicate *keys* — keeping the first element per key even if other fields differ. Useful for deduping by an id while keeping the rest of the object."},{t:"code",title:"distinct / distinctBy",code:`listOf(1, 2, 2, 3).distinct()                 // [1, 2, 3]
users.distinctBy { it.email }                 // one user per unique email`},{t:"list",items:["**`distinct()`** — remove `equals`-duplicates, keep first occurrence.","**`distinctBy { key }`** — dedupe by a selected key, keep first per key.","**Order preserved** — first occurrences retained.","**Alternative** — `toSet()` dedupes but loses order (for a `Set`)."]},{t:"note",text:"distinct() removes equals-duplicates (keeps first occurrence, order preserved); distinctBy { key } dedupes by a selected key (one per key, keeping the first). Use distinctBy to dedupe objects by id/email. toSet() also dedupes but loses order."}]},{level:"senior",q:"What are chunked and windowed?",a:[{t:"p",text:"`chunked(n)` splits a collection into consecutive *non-overlapping* sublists of size `n` (the last may be smaller). `windowed(size, step)` creates *sliding windows* of a given size, moving by `step` — overlapping by default. Use `chunked` for batching (e.g. paginate a list); `windowed` for moving averages or comparing neighbors."},{t:"code",title:"chunked / windowed",code:`(1..7).chunked(3)                      // [[1,2,3], [4,5,6], [7]]
(1..5).windowed(3, step = 1)           // [[1,2,3], [2,3,4], [3,4,5]]
(1..5).windowed(2).map { (a, b) -> b - a }   // pairwise diffs`},{t:"list",items:["**`chunked(n)`** — non-overlapping batches of size n.","**`windowed(size, step, partialWindows)`** — sliding windows.","**`chunked` uses** — batching API calls, grid rows.","**`windowed` uses** — moving averages, comparing adjacent elements."]},{t:"note",text:"chunked(n) = consecutive non-overlapping sublists of size n (last may be smaller — batching, grid rows). windowed(size, step) = sliding (overlapping) windows — moving averages, comparing neighbors. Both have transform overloads."}]},{level:"junior",q:"What do any, all, none, and count do?",a:[{t:"p",text:"These are predicate-testing terminal operations. `any { }` returns true if *at least one* element matches; `all { }` if *every* element matches; `none { }` if *no* element matches; `count { }` returns *how many* match. Without a predicate, `any()` checks non-emptiness, `count()` returns size."},{t:"code",title:"Predicate checks",code:`list.any { it > 10 }        // at least one > 10
list.all { it > 0 }         // all positive
list.none { it.isBlank() }  // none blank
list.count { it % 2 == 0 }  // how many even
list.any()                  // is not empty`},{t:"list",items:["**`any { }`** — at least one matches (short-circuits).","**`all { }`** — every element matches.","**`none { }`** — no element matches.","**`count { }`** — number matching; `count()` = size."]},{t:"note",text:"any { } (≥1 matches, short-circuits), all { } (every matches), none { } (zero match), count { } (how many). No-predicate: any() checks non-empty, count() = size. Prefer any()/isNotEmpty() over size > 0, and any { } over filter { }.isNotEmpty() (short-circuits)."}]},{level:"junior",q:"How do you find elements: first, firstOrNull, find, single?",a:[{t:"p",text:"`first { }` returns the first matching element (throws if none); `firstOrNull { }`/`find { }` return the match or `null` (safe). `single { }` expects *exactly one* match (throws if zero or more than one). Prefer the `OrNull`/`find` variants unless you're certain a match exists."},{t:"code",title:"Finding elements",code:`list.first { it > 5 }          // first > 5 (throws if none)
list.firstOrNull { it > 5 }    // or null
list.find { it.id == id }      // == firstOrNull, reads nicely
list.single { it.isDefault }   // exactly one expected`},{t:"list",items:["**`first { }`** — first match; throws `NoSuchElementException` if none.","**`firstOrNull { }`/`find { }`** — first match or null (same thing).","**`single { }`** — exactly one match; throws otherwise.","**`last`/`lastOrNull`** — from the end."]},{t:"note",text:"first { } (first match, throws if none), firstOrNull { }/find { } (match or null — same), single { } (exactly one, throws on 0 or >1), last/lastOrNull (from the end). Prefer firstOrNull/find unless a match is guaranteed; single when you truly expect one."}]},{level:"junior",q:"How do you aggregate numbers: sumOf, maxByOrNull, minByOrNull, average?",a:[{t:"p",text:"`sumOf { selector }` sums a numeric projection; `maxByOrNull { }`/`minByOrNull { }` return the element with the max/min selected value (null if empty); `maxOfOrNull { }`/`minOfOrNull { }` return the max/min *value*; `average()` computes the mean. The `OrNull` variants avoid exceptions on empty collections."},{t:"code",title:"Numeric aggregation",code:`orders.sumOf { it.total }              // sum of totals
users.maxByOrNull { it.age }           // the oldest user (or null)
prices.minOfOrNull { it }              // smallest price value
scores.average()                        // mean (Double)`},{t:"list",items:["**`sumOf { }`** — sum a numeric projection (typed Int/Long/Double).","**`maxByOrNull`/`minByOrNull`** — the *element* with the extreme key.","**`maxOfOrNull`/`minOfOrNull`** — the extreme *value*.","**`average()`** — mean as Double; empty → NaN."]},{t:"note",text:"sumOf { } sums a numeric projection; maxByOrNull/minByOrNull return the ELEMENT with the extreme key; maxOfOrNull/minOfOrNull return the extreme VALUE; average() the mean. Use OrNull variants for empty-safety (non-OrNull throw on empty)."}]},{level:"junior",q:"What do filterNotNull, filterIsInstance, and mapNotNull do?",a:[{t:"p",text:"`filterNotNull()` removes null elements (turning `List<T?>` into `List<T>`); `filterIsInstance<T>()` keeps only elements of a type (typed as `T`); `mapNotNull { }` maps and drops null results in one step. All three clean or narrow a collection concisely."},{t:"code",title:"Cleaning collections",code:`listOf(1, null, 2).filterNotNull()             // [1, 2]
items.filterIsInstance<Post>()                  // only Posts, typed
strings.mapNotNull { it.toIntOrNull() }         // parse, drop failures`},{t:"list",items:["**`filterNotNull()`** — drop nulls; `List<T?>` → `List<T>`.","**`filterIsInstance<T>()`** — keep one type, smart-typed.","**`mapNotNull { }`** — transform + drop nulls.","**Uses** — parsing (drop failures), narrowing heterogeneous lists."]},{t:"note",text:"filterNotNull() drops nulls (List<T?>→List<T>); filterIsInstance<T>() keeps one type (typed); mapNotNull { } maps and drops null results. Great for parsing (mapNotNull { it.toIntOrNull() }) and narrowing heterogeneous lists."}]},{level:"junior",q:"What is the difference between forEach and map?",a:[{t:"p",text:"`map` *transforms* each element and returns a *new collection* of results. `forEach` performs a *side effect* per element and returns `Unit` (nothing). Use `map` when you want a transformed collection; use `forEach` for effects (logging, mutation of external state). Using `map` and ignoring its result is a smell — use `forEach`."},{t:"code",title:"Transform vs side effect",code:`val names = users.map { it.name }       // new List<String>
users.forEach { log(it) }               // side effects, returns Unit
// smell: users.map { log(it) }         // ignores the created list`},{t:"list",items:["**`map`** — transform → new collection.","**`forEach`** — side effect → Unit.","**`onEach`** — side effect but returns the collection (chainable).","**Don't misuse** — `map` for effects wastes an allocated list."]},{t:"note",text:"map transforms each element → a new collection; forEach performs a side effect → Unit. Use map for transformations, forEach for effects (logging/mutation). onEach does a side effect but returns the collection (chainable). Don't use map for side effects (wastes the result list)."}]},{level:"junior",q:"What are the common Set operations?",a:[{t:"p",text:"Sets support mathematical operations: `union` (all elements of both), `intersect` (elements in both), and `subtract` (in the first but not the second). Sets also give fast `contains` and automatic deduplication. Use them for membership tests and comparing groups."},{t:"code",title:"Set operations",code:`val a = setOf(1, 2, 3); val b = setOf(2, 3, 4)
a union b        // [1, 2, 3, 4]
a intersect b    // [2, 3]
a subtract b     // [1]
2 in a           // true — fast membership`},{t:"list",items:["**`union`** — all elements of both (deduped).","**`intersect`** — common elements.","**`subtract`** — in the first, not the second.","**Fast `contains`** — O(1) membership (hash set)."]},{t:"note",text:"Set operations: union (both), intersect (common), subtract (first minus second) — plus O(1) contains and auto-dedup. Use for membership tests and comparing groups. Works on any collection (returns a Set)."}]},{level:"junior",q:"What are useful Map operations (getOrDefault, getOrPut, iteration)?",a:[{t:"p",text:"Maps offer safe access and lazy population: `get`/`[]` returns `null` for a missing key; `getOrDefault(key, default)` returns a default; `getOrElse(key) { }` computes one; `getOrPut(key) { }` (on a MutableMap) returns the existing value or *inserts and returns* a computed one. Iterate with `for ((k, v) in map)`."},{t:"code",title:"Map access patterns",code:`map["missing"]                       // null
map.getOrDefault("k", 0)             // default if absent
val cache = mutableMapOf<String, Data>()
cache.getOrPut("k") { expensiveLoad() }   // memoize
for ((key, value) in map) println("$key=$value")`},{t:"list",items:["**`[]`/`get`** — value or null.","**`getOrDefault`/`getOrElse { }`** — fallback for missing keys.","**`getOrPut { }`** — get-or-insert; great for memoization/caches.","**Iteration** — destructure entries `(k, v)`; `keys`, `values`, `entries`."]},{t:"note",text:"Map access: [] / get → value or null; getOrDefault/getOrElse { } → fallback; getOrPut { } (MutableMap) → get-or-insert (memoization/caches). Iterate with for ((k, v) in map); use keys/values/entries. mapValues/filterKeys transform maps."}]},{level:"junior",q:"How do you convert between collection types?",a:[{t:"p",text:"Use the `toX()` functions: `toList()`, `toMutableList()`, `toSet()`, `toMutableSet()`, `toTypedArray()`, `associate`/`toMap()`. These create a *new* collection of the target type. `toList()` also snapshots a mutable collection into a read-only copy."},{t:"code",title:"Conversions",code:`val set = list.toSet()                 // dedupe into a Set
val mutable = list.toMutableList()     // mutable copy
val array = list.toTypedArray()        // Array<T>
val map = pairs.toMap()                // List<Pair> -> Map
val snapshot = mutableList.toList()    // read-only copy (defensive)`},{t:"list",items:["**`toList`/`toMutableList`** — list copies (read-only / mutable).","**`toSet`/`toMutableSet`** — deduped set copies.","**`toTypedArray`/`toIntArray`** — arrays.","**`toMap`** — pairs to a map; defensive copies to avoid shared mutation."]},{t:"note",text:"toList/toMutableList (list copies), toSet/toMutableSet (deduped), toTypedArray/toIntArray (arrays), toMap (pairs→map) — each creates a NEW collection of the target type. toList() also makes a read-only defensive copy of a mutable collection."}]},{level:"senior",q:"Why can't you compare arrays with ==, and what do you use instead?",a:[{t:"p",text:"For arrays, `==` (and `.equals()`) checks *reference* identity, not contents — two arrays with the same elements are `!=`. Use `contentEquals` for a shallow content comparison, and `contentDeepEquals` for nested arrays. (Lists, by contrast, compare by content with `==`.) This is a common gotcha for Java developers."},{t:"code",title:"Array comparison",code:`val a = intArrayOf(1, 2, 3); val b = intArrayOf(1, 2, 3)
a == b                 // false — reference comparison
a.contentEquals(b)     // true — content
arrayOf(a).contentDeepEquals(arrayOf(b))   // nested arrays`},{t:"list",items:["**Array `==`** — reference identity (not content).","**`contentEquals`** — shallow content comparison.","**`contentDeepEquals`** — nested arrays.","**Lists differ** — `List` `==` compares content; prefer `List` when you want value equality."]},{t:"note",text:"Array == compares references, not contents (a common gotcha) — use contentEquals (shallow) or contentDeepEquals (nested). Lists, unlike arrays, compare by content with ==. Prefer List over Array when you want value equality (and data classes with arrays need custom equals)."}]},{level:"senior",q:"How do you decide between chained operators and a manual loop?",a:[{t:"p",text:"Chained operators (`map`/`filter`/`groupBy`) are declarative and readable, and are the default choice. But each intermediate operator on a `List` allocates a *new list*, so a long chain over a large collection can be wasteful — there, use a `Sequence` (lazy, no intermediate lists) or a single loop. For small collections, readability wins; for large/hot paths, measure and consider sequences or loops."},{t:"list",items:["**Operators** — readable, declarative; the default for clarity.","**Intermediate allocations** — each list operator creates a new list; long chains over big data add up.","**`Sequence`** — lazy evaluation, no intermediate lists, for large collections / long chains.","**Manual loop** — occasionally clearest/fastest for complex single-pass logic; don't prematurely optimize."]},{t:"note",text:"Prefer chained operators for readability (the default). But list operators allocate an intermediate list each step — for large collections / long chains, use a Sequence (lazy, no intermediates) or a single loop. Small data: readability wins. Large/hot paths: measure, then optimize with sequences/loops."}]},{level:"junior",q:"What is the difference between flatten and flatMap?",a:[{t:"p",text:"`flatten()` collapses a collection *of collections* into a single flat collection. `flatMap { }` does two steps at once — it maps each element to a collection and then flattens the results. So `flatMap` = `map { }` producing collections + `flatten()`."},{t:"code",title:"flatten vs flatMap",code:`listOf(listOf(1, 2), listOf(3, 4)).flatten()   // [1, 2, 3, 4]
users.flatMap { it.roles }                       // all roles across users, flattened
// equivalent: users.map { it.roles }.flatten()`},{t:"list",items:["**`flatten()`** — `List<List<T>>` → `List<T>`.","**`flatMap { }`** — map each element to a collection, then flatten.","**Equivalence** — `flatMap { f(it) }` == `map { f(it) }.flatten()`.","**Uses** — gathering nested data (all tags, all children) into one list."]},{t:"note",text:"flatten() collapses List<List<T>> → List<T>. flatMap { } maps each element to a collection then flattens (= map { }.flatten()). Use flatMap to gather nested data (all roles/tags/children) into one flat list."}]},{level:"junior",q:"How do you build a collection imperatively with buildList/buildMap?",a:[{t:"p",text:"`buildList { }`, `buildSet { }`, and `buildMap { }` give you a *mutable* builder inside the lambda and return an immutable (read-only) result. They're cleaner than creating a `mutableListOf`, populating it conditionally, and returning it — you get imperative construction with an immutable result in one expression."},{t:"code",title:"buildList",code:`val items = buildList {
    add(header)
    if (showAds) add(adItem)
    addAll(posts)
    if (hasMore) add(loadingItem)
}   // returns an immutable List
val config = buildMap { put("a", 1); if (cond) put("b", 2) }`},{t:"list",items:["**`buildList { }`** — mutable inside, read-only result.","**Conditional construction** — add items based on logic cleanly.","**`buildSet`/`buildMap`** — same for sets and maps.","**Better than** — a `mutableListOf` you populate and return (no leaked mutability)."]},{t:"note",text:"buildList/buildSet/buildMap { } give a mutable builder in the lambda and return an immutable result — clean imperative construction (conditional adds) without leaking a mutable list. Prefer over creating a mutableListOf, populating, and returning it."}]},{level:"senior",q:"What's the difference between count() and size, and any() and isNotEmpty()?",a:[{t:"p",text:"For a `Collection`, `size` is a property (O(1)) and `count()` (no predicate) just returns `size` — so use `size`. But on a `Sequence` or `Iterable` without a known size, `count()` iterates (O(n)). Similarly, `isNotEmpty()` is the clear way to check non-emptiness; `any()` (no predicate) also works. Use predicate versions (`count { }`, `any { }`) when filtering."},{t:"list",items:["**`size`** — O(1) property on collections; prefer it over `count()`.","**`count()`** — O(1) on collections, but O(n) (iterates) on sequences/iterables.","**`count { }`/`any { }`** — with a predicate, count/check matches (any short-circuits).","**`isNotEmpty()`/`isEmpty()`** — clearest emptiness checks; avoid `size > 0`."]},{t:"note",text:"For collections, size is an O(1) property and count() (no predicate) just returns it — use size. On sequences/iterables, count() iterates (O(n)). Use count { }/any { } with predicates (any short-circuits). Prefer isNotEmpty()/isEmpty() over size > 0/== 0."}]}],Fx={id:"collections-sequences",title:"Collections & Sequences",tagline:"Read-only vs mutable collections, the functional operators, eager vs lazy evaluation, Sequences and when they help (or hurt), and real immutability.",tags:["Kotlin","Collections","Sequences","map/filter","Immutability"],content:qx,qa:Ox},Lx=[{heading:"Generics — writing type-safe reusable code",blocks:[{t:"p",text:"**Generics** let you write code that works with *any* type while keeping full type safety. Instead of a `Box` that holds `Any` (and needs casting), a `Box<T>` holds a specific type `T` chosen at the use site — `Box<String>` holds strings, `Box<Int>` holds ints, and the compiler enforces it. The `T` is a **type parameter** — a placeholder filled in when the type is used."},{t:"code",title:"Generic class and function",code:`class Box<T>(val value: T) {
    fun get(): T = value
}
val stringBox = Box("hello")     // Box<String> — T inferred as String
val intBox = Box(42)             // Box<Int>

// Generic function
fun <T> firstOrNull(list: List<T>): T? = if (list.isEmpty()) null else list[0]

// Type constraints (upper bounds): T must be Comparable
fun <T : Comparable<T>> max(a: T, b: T): T = if (a > b) a else b`},{t:"list",items:["**Type safety without casting**: `stringBox.get()` returns a `String`, not `Any` — no cast, no `ClassCastException` risk.","**Type constraints (`T : Bound`)**: restrict `T` to subtypes of a bound so you can use that bound's methods — `<T : Comparable<T>>` lets you use `>`. Multiple bounds use a `where` clause.",'**Type inference**: you rarely write the type argument explicitly (`Box("hi")` infers `Box<String>`); specify it when inference can\'t (`emptyList<String>()`).']}]},{heading:"Type erasure — generics are compile-time only",blocks:[{t:"p",text:"On the JVM, generics use **type erasure**: the type argument exists at *compile time* for type-checking but is *erased* at runtime. At runtime, a `List<String>` and a `List<Int>` are both just `List` — the JVM doesn't know the element type. This is inherited from Java's generics and has real consequences."},{t:"code",title:"What erasure prevents",code:`// CAN'T check a generic type at runtime — the type arg is erased:
if (list is List<String>) { }    // ERROR: cannot check erased type
if (list is List<*>) { }          // OK: can only check the raw type (star)

// CAN'T create an array of a generic type directly:
// val arr = Array<T>(10) { }     // ERROR without a reified type

// Two functions differing only by generic type CLASH after erasure:
fun process(list: List<String>) {}
fun process(list: List<Int>) {}   // ERROR: same signature after erasure`},{t:"list",items:["**You can't do `x is List<String>`** — at runtime there's no `String` info, only `List`. You can check `is List<*>` (the raw type) but not the type argument.","**You can't overload on generic type alone** — `f(List<String>)` and `f(List<Int>)` have the same erased signature and clash.","**Workaround — `reified`**: an `inline fun <reified T>` keeps the type at runtime (because the function is inlined, the concrete type is pasted in). This is why `inline fun <reified T> Gson.fromJson(json)` works and a non-inline one can't. Reified is the escape hatch from erasure."]}]},{heading:"Variance — the core hard topic",blocks:[{t:"p",text:"**Variance** answers: if `Dog` is a subtype of `Animal`, is `Box<Dog>` a subtype of `Box<Animal>`? By default in Kotlin, **no** — generics are *invariant*: `Box<Dog>` and `Box<Animal>` are unrelated types, even though `Dog` and `Animal` are related. This is safe but restrictive. **Variance annotations** (`out` and `in`) let you relax it safely."},{t:"code",title:"Why invariance is the safe default",code:`// If MutableList<Dog> WERE a subtype of MutableList<Animal>, this would break:
val dogs: MutableList<Dog> = mutableListOf(Dog())
val animals: MutableList<Animal> = dogs   // (hypothetically allowed)
animals.add(Cat())                         // adding a Cat to a list of Dogs!
val dog: Dog = dogs[0]                      // now a Cat is in here — corruption
// So Kotlin makes MutableList INVARIANT: the assignment above is a compile error.`}]},{heading:"out — covariance (producers)",blocks:[{t:"p",text:"**`out T`** makes a type parameter **covariant**: `Box<out Animal>` accepts `Box<Dog>`. It's allowed when the type is only ever *produced* (returned/read), never *consumed* (taken as input). Marking `out` is a promise: 'this class only outputs `T`, never accepts it as a parameter'. Because it only produces, it's safe to treat a producer of `Dog` as a producer of `Animal` (a Dog is an Animal)."},{t:"code",title:"out enables covariance",code:`// List<out E> is declared covariant — that's why this works:
val dogs: List<Dog> = listOf(Dog())
val animals: List<Animal> = dogs   // OK! List only PRODUCES elements (get), never adds

// Declaration-site: the class promises to only produce T
interface Producer<out T> {
    fun produce(): T          // OK — T is in output position
    // fun consume(t: T) { }  // ERROR — can't have T in input position with 'out'
}`},{t:"list",items:["**Read-only `List<out E>` is covariant** (that's why `List<Dog>` is a `List<Animal>`), while `MutableList<E>` is invariant (it consumes elements via `add`, so covariance would be unsafe).","**The restriction**: with `out T`, `T` can only appear in *output* positions (return types), never *input* positions (parameters) — the compiler enforces this so the covariance is sound.","**Mnemonic**: `out` = 'produces/outputs T' = covariant = you can *up*cast the generic (Box<Dog> → Box<Animal>)."]}]},{heading:"in — contravariance (consumers)",blocks:[{t:"p",text:"**`in T`** makes a type parameter **contravariant**: `Box<in Dog>` accepts `Box<Animal>`. It's allowed when the type is only ever *consumed* (taken as input), never *produced*. If something can consume any `Animal`, it can certainly consume a `Dog` — so a consumer of `Animal` is usable as a consumer of `Dog`. The subtyping *reverses*."},{t:"code",title:"in enables contravariance",code:`// A Comparator<in T> that compares Animals can compare Dogs:
val animalComparator: Comparator<Animal> = Comparator { a, b -> a.age - b.age }
val dogComparator: Comparator<Dog> = animalComparator   // OK with 'in'

interface Consumer<in T> {
    fun consume(t: T)          // OK — T is in input position
    // fun produce(): T        // ERROR — can't have T in output position with 'in'
}`},{t:"list",items:["**The restriction**: with `in T`, `T` can only appear in *input* positions (parameters), never output — enforced by the compiler.","**Mnemonic**: `in` = 'consumes/inputs T' = contravariant = subtyping reverses (Consumer<Animal> is a Consumer<Dog>).","**The unifying rule — PECS / 'producer out, consumer in'**: if a type parameter is *produced* (output), mark it `out`; if *consumed* (input), mark it `in`. This is Kotlin's version of Java's PECS ('Producer Extends, Consumer Super')."]}]},{heading:"Declaration-site vs use-site variance, and star projection",blocks:[{t:"list",items:["**Declaration-site variance** (Kotlin's advantage): you declare `out`/`in` *once*, at the class/interface definition (`interface List<out E>`). Every use of `List` is then covariant automatically. Java can't do this — it only has use-site variance (wildcards).","**Use-site variance (type projection)**: you apply variance at a specific *use* instead — `fun copy(from: Array<out Any>, to: Array<Any>)`. Kotlin supports this too (it's how you handle invariant classes like `Array` at a call site). It's Kotlin's equivalent of Java's `? extends`/`? super` wildcards.","**Star projection `<*>`**: when you don't know or care about the type argument — `List<*>` means 'a List of *something*'. You can read elements as the upper bound (`Any?`) but can't add anything (except null-related edge cases), because the actual type is unknown. Useful for 'I just need to know it's a List' situations."]},{t:"note",text:"The variance summary to state: generics are *invariant* by default (safe). `out` = covariant = for producers (T in output positions) = you can upcast (List<Dog> is List<Animal>). `in` = contravariant = for consumers (T in input positions) = subtyping reverses (Consumer<Animal> is Consumer<Dog>). Rule: 'producer out, consumer in'. Kotlin allows declaration-site variance (declare once) which Java can't; `<*>` is star projection for unknown type arguments."}]}],jx=[{level:"junior",q:"What are generics and why are they useful?",a:[{t:"p",text:"**The concept**: generics let you write code that works with any type while keeping type safety. Instead of a container that holds `Any` (forcing casts and risking `ClassCastException`), you write `Box<T>` where `T` is a type parameter chosen at the use site — `Box<String>` holds strings, `Box<Int>` holds ints, and the compiler enforces the type throughout."},{t:"code",title:"Generic class and function",code:`class Box<T>(val value: T) { fun get(): T = value }
val b = Box("hi")            // Box<String>, get() returns String — no cast

fun <T> firstOrNull(list: List<T>): T? =
    if (list.isEmpty()) null else list[0]`},{t:"p",text:"The benefits: **type safety** (the compiler knows `box.get()` is a `String`, no cast needed), **reusability** (one `Box` class works for every type), and **clearer APIs** (a `List<User>` documents exactly what it holds). You can also constrain a type parameter with an upper bound — `fun <T : Comparable<T>> max(a: T, b: T)` requires `T` to be comparable so you can use `>` inside. The standard library is built on generics: `List<T>`, `Map<K,V>`, `Result<T>` all use them."}]},{level:"junior",q:"What is type erasure?",a:[{t:"p",text:"**The concept**: on the JVM, a generic type argument exists only at *compile time* — the compiler uses it to check your code, then *erases* it. At runtime, `List<String>` and `List<Int>` are both just `List`; the JVM has no idea what element type it holds. Kotlin inherits this from Java's generics."},{t:"list",items:["**You can't check a generic type at runtime**: `if (x is List<String>)` is a compile error because there's no `String` info at runtime. You can only check the raw type: `if (x is List<*>)`.","**You can't overload on generic type alone**: `fun f(x: List<String>)` and `fun f(x: List<Int>)` have the *same* signature after erasure, so they clash.","**You can't directly instantiate a generic type**: `Array<T>(n)` doesn't work without more info, because `T` isn't known at runtime."]},{t:"p",text:"The escape hatch is `reified` type parameters on `inline` functions: because an inline function's body (including its type argument) is pasted into each call site, the concrete type *is* available at runtime there. That's why `inline fun <reified T> fromJson(json: String): T` can do `T::class` while a non-inline version can't. So type erasure is the default limitation, and `reified` is how you work around it when you genuinely need the type at runtime."}]},{level:"junior",q:"What does the 'reified' keyword do?",a:[{t:"p",text:"**The concept**: normally a generic type parameter is erased at runtime, so you can't reference it (`T::class` doesn't compile, `x is T` doesn't work). Marking an `inline` function's type parameter `reified` keeps the type available at runtime. It works *because* the function is inlined — the compiler pastes the function body into each call site with the actual type substituted in, so at that pasted location the concrete type is known."},{t:"code",title:"reified makes the type usable at runtime",code:`inline fun <reified T> Gson.fromJson(json: String): T =
    fromJson(json, T::class.java)     // T::class works because reified

val user: User = gson.fromJson(jsonString)   // no need to pass User::class

// Also enables runtime type checks:
inline fun <reified T> List<*>.filterByType(): List<T> =
    filterIsInstance<T>()`},{t:"p",text:"The practical wins: you avoid passing `Class` objects around (`fromJson(json)` instead of `fromJson(json, User::class.java)`), you can do `x is T` and `T::class` inside the function, and it makes generic APIs much cleaner (Android's `viewModels<T>()`, `intent.getParcelableExtra` wrappers, DI's `get<T>()` all use it). The constraint: `reified` *requires* `inline`, so it only works on inline functions — and like all inlining, it duplicates code, so keep those functions small."}]},{level:"senior",q:"Explain variance, and why generics are invariant by default.",a:[{t:"p",text:"**Variance answers a subtyping question**: given that `Dog` is a subtype of `Animal`, what's the relationship between `Container<Dog>` and `Container<Animal>`? By default in Kotlin, generics are **invariant** — `Container<Dog>` and `Container<Animal>` are *unrelated* types, despite `Dog` and `Animal` being related. You can't assign one to the other."},{t:"p",text:"**Why invariance is the safe default** — because covariance-by-default would break type safety for mutable containers:"},{t:"code",title:"Why MutableList can't be covariant",code:`val dogs: MutableList<Dog> = mutableListOf(Dog())
// IF this were allowed (it's not — compile error):
val animals: MutableList<Animal> = dogs
animals.add(Cat())          // Cat added to a list that's really List<Dog>!
val d: Dog = dogs[0]         // ClassCastException waiting to happen`},{t:"list",items:["If `MutableList<Dog>` were a subtype of `MutableList<Animal>`, you could view a dog-list as an animal-list and `add(Cat())` to it — corrupting the dog-list with a cat. The next read of a `Dog` would blow up. So Kotlin makes `MutableList` invariant, making that assignment a compile error.","**The insight**: whether covariance is safe depends on whether the container *produces* or *consumes* the type. A container that only *produces* T (like read-only `List`, which you only read from) *can* safely be covariant. A container that *consumes* T (via `add`) cannot. Invariance is the default because it's safe for *both* cases; you opt into covariance (`out`) or contravariance (`in`) when you know the usage is one-directional."]},{t:"p",text:"This is exactly why `List<Dog>` *is* assignable to `List<Animal>` (List is declared covariant, `List<out E>`, because it only produces) while `MutableList<Dog>` is *not* assignable to `MutableList<Animal>` (invariant, because it consumes). Understanding that variance safety hinges on produce-vs-consume is the heart of the topic."}]},{level:"senior",q:"Explain the 'out' and 'in' variance modifiers with the producer/consumer rule.",a:[{t:"p",text:"**`out` and `in` relax invariance safely, based on whether the type parameter is produced or consumed.** The guiding rule is 'producer `out`, consumer `in`' (Kotlin's version of Java's PECS — Producer Extends, Consumer Super)."},{t:"list",items:["**`out T` — covariance, for producers**: mark a type parameter `out` when the class only ever *produces* (returns/outputs) `T`, never consumes it as a parameter. Then `Producer<Dog>` becomes a subtype of `Producer<Animal>` — subtyping goes the *same* direction. It's safe because if it only outputs Dogs, treating those outputs as Animals is fine (a Dog is an Animal). The compiler enforces that `T` appears only in *output* positions (return types).","**`in T` — contravariance, for consumers**: mark a type parameter `in` when the class only ever *consumes* (takes as input) `T`, never produces it. Then `Consumer<Animal>` becomes a subtype of `Consumer<Dog>` — subtyping *reverses*. It's safe because something that can consume any Animal can certainly consume a Dog. The compiler enforces that `T` appears only in *input* positions (parameters)."]},{t:"code",title:"Both in action",code:`interface Producer<out T> { fun next(): T }        // out -> covariant
val animalProducer: Producer<Animal> = dogProducer  // Producer<Dog> works

interface Consumer<in T> { fun accept(item: T) }   // in -> contravariant
val dogConsumer: Consumer<Dog> = animalConsumer     // Consumer<Animal> works

// Real examples: List<out E> (covariant), Comparator<in T> (contravariant)`},{t:"list",items:["**Mnemonic**: `out` = output = produce = covariant = upcast the generic (`List<Dog>` is a `List<Animal>`). `in` = input = consume = contravariant = reverse (`Comparator<Animal>` is a `Comparator<Dog>`).","**Kotlin's edge — declaration-site variance**: you write `out`/`in` *once* at the class declaration (`interface List<out E>`), and every use is variant automatically. Java can only do use-site variance (wildcards `? extends`/`? super` at each call). Kotlin supports use-site variance too (type projections like `Array<out Any>`), for cases like the inherently-invariant `Array`."]}]},{level:"senior",q:"What is star projection (<*>) and when do you use it?",a:[{t:"p",text:"**The concept**: `<*>` (star projection) is how you say 'a generic type with *some* type argument, but I don't know or care which'. `List<*>` means 'a List of something' — you know it's a List, but not its element type. It's a safe way to work with a generic type when the specific type parameter is unknown or irrelevant."},{t:"code",title:"Star projection",code:`fun printSize(list: List<*>) {          // any List, element type unknown
    println(list.size)                   // OK — size doesn't depend on T
    val item: Any? = list[0]             // reads come back as Any? (the upper bound)
    // list.add(...)                     // NOT allowed for MutableList<*> — can't
                                         // safely add when the real type is unknown
}`},{t:"list",items:["**What you can do with `<*>`**: read elements as the *upper bound* of the type parameter — for `List<*>` that's `Any?`, since any element is at least `Any?`. And use members that don't involve the type parameter (`size`, `isEmpty`).","**What you can't do**: *write* a value of the projected type into a consumer position, because the real type is unknown — adding to a `MutableList<*>` is disallowed (you might add the wrong type). The compiler treats the 'in' positions as unusable.","**How it relates to variance**: `<*>` is essentially projecting the type as `out (upperBound)` for reading and `in Nothing` for writing — 'I can get an `Any?` out, but I can't put anything in'. It's the Kotlin equivalent of Java's unbounded wildcard `<?>`.","**When to use it**: when you have a generic type but genuinely don't need its type argument — logging/inspecting a collection generically, storing heterogeneous generic instances (`List<Box<*>>`), or writing a function that only uses type-independent members. If you *do* need the type, use a generic function with a real type parameter instead."]},{t:"p",text:"**The distinction to make**: `<*>` (star projection) is 'unknown specific type' — safe, read-as-upper-bound. `<Any?>` is 'explicitly the Any? type'. And `<T>` (a real type parameter on a generic function) is 'I don't know it *here* but I want to *capture and use* it' — which is what you'd choose when you need to actually work with the element type rather than just inspect the container. Star projection is specifically for 'I don't care what T is'."}]},{level:"junior",q:"How do you write a generic function and a generic class?",a:[{t:"p",text:"You declare type parameters in angle brackets. For a function, they go before the function name — `fun <T> singletonList(item: T): List<T>`. For a class, after the class name — `class Box<T>(val value: T)`. The type parameter (`T`) then stands in for a concrete type the caller provides (or that's inferred)."},{t:"code",title:"Generic function and class",code:`fun <T> firstOrNull(list: List<T>): T? = if (list.isEmpty()) null else list[0]
class Box<T>(val value: T) { fun get(): T = value }

val b = Box("hi")          // T inferred as String
val n = firstOrNull(listOf(1, 2))   // T inferred as Int`},{t:"list",items:["**Function** — `fun <T> name(...)`; type params before the name.","**Class** — `class Name<T>(...)`; type params after the name.","**Inference** — the compiler infers `T` from arguments where possible.","**Multiple params** — `<K, V>` for maps, etc."]},{t:"note",text:"Declare type parameters in angle brackets: functions put them before the name (fun <T> f(...)), classes after (class Box<T>). T stands for a concrete type the caller provides or the compiler infers from arguments. Multiple params: <K, V>."}]},{level:"junior",q:"What is a generic constraint (upper bound)?",a:[{t:"p",text:"A constraint restricts what types a type parameter can be. `<T : Number>` means `T` must be `Number` or a subtype, so you can call `Number`'s methods on `T`. The default upper bound is `Any?` (any type, nullable). For multiple bounds, use a `where` clause."},{t:"code",title:"Upper bounds",code:`fun <T : Comparable<T>> max(a: T, b: T): T = if (a > b) a else b   // T is Comparable
fun <T> f(x: T) where T : CharSequence, T : Appendable { }         // multiple bounds`},{t:"list",items:["**`<T : Bound>`** — `T` must be `Bound` or a subtype; enables calling `Bound`'s members.","**Default** — `Any?` (any nullable type) when no bound is given.","**Non-null bound** — `<T : Any>` forces non-nullable `T`.","**`where`** — multiple constraints on the same parameter."]},{t:"note",text:"A constraint (upper bound) restricts a type parameter: <T : Number> means T is Number or a subtype (so you can use Number's members). Default bound is Any? (any nullable). <T : Any> forces non-null. Use a where clause for multiple bounds: where T : A, T : B."}]},{level:"senior",q:"Why are generics invariant by default, and what problem does that prevent?",a:[{t:"p",text:"Generics are *invariant*: `List<String>` is *not* a subtype of `List<Any>` even though `String` is a subtype of `Any`. This prevents type-safety holes — if `MutableList<String>` were a `MutableList<Any>`, you could add a non-`String` to it, corrupting the list. Variance (`out`/`in`) lets you safely relax this where it's provably safe."},{t:"code",title:"Why invariance is needed",code:`val strings: MutableList<String> = mutableListOf("a")
// val objs: MutableList<Any> = strings   // if allowed...
// objs.add(42)                            // ...you'd corrupt the String list
// val bad: String = strings[1]           // ClassCastException`},{t:"list",items:["**Invariant** — `List<String>` isn't a `List<Any>` by default.","**Prevents corruption** — you can't sneak a wrong type into a container.","**Type safety** — the compiler rejects unsafe subtyping.","**Relax with variance** — `out`/`in` when the usage is safe (produce-only / consume-only)."]},{t:"note",text:"Generics are invariant (List<String> isn't a List<Any>) to prevent type-safety holes — if it were, you could add a non-String into a String list and get a ClassCastException on read. Variance modifiers (out/in) safely relax this for produce-only/consume-only usage."}]},{level:"senior",q:"What is declaration-site vs use-site variance?",a:[{t:"p",text:"*Declaration-site* variance puts `out`/`in` on the type parameter *where the class is declared* (`interface Producer<out T>`), so *all* uses are covariant/contravariant — cleaner and the Kotlin-preferred approach. *Use-site* variance (type projection) applies variance at a specific *usage* (`Array<out Any>`), useful when a class is invariant but a particular function only produces or only consumes."},{t:"code",title:"Declaration-site vs use-site",code:`interface Source<out T> { fun next(): T }        // declaration-site: always covariant

fun copy(from: Array<out Any>, to: Array<Any>) { } // use-site: 'from' is covariant here`},{t:"list",items:["**Declaration-site** — `out`/`in` on the class's type param; applies everywhere. Kotlin-preferred.","**Use-site (projection)** — `Array<out T>` at a specific usage; for invariant classes used one-directionally.","**Java wildcards** — `? extends`/`? super` are use-site variance; Kotlin has both.","**Prefer declaration-site** — when the class is inherently producer/consumer."]},{t:"note",text:"Declaration-site variance: out/in on the class's type parameter (interface Source<out T>) — applies to all uses, Kotlin-preferred. Use-site variance (projection): out/in at a specific usage (Array<out Any>) for an invariant class used one-directionally. Java's ? extends/? super are use-site."}]},{level:"senior",q:"What is variance in function types?",a:[{t:"p",text:"Function types are naturally *contravariant in parameters* and *covariant in return types*. That is, `(Animal) -> Dog` is a subtype of `(Dog) -> Animal` — you can substitute a function that accepts a *broader* parameter and returns a *narrower* result. Kotlin models this because `Function1<in P, out R>` declares `in` on the parameter and `out` on the result."},{t:"code",title:"Function subtyping",code:`val broad: (Animal) -> Dog = { Dog() }
val narrow: (Dog) -> Animal = broad     // OK — accepts fewer, returns more specific
// Function1<in P, out R>: contravariant param, covariant return`},{t:"list",items:["**Parameters** — contravariant (`in`): accept a broader (super) type.","**Return** — covariant (`out`): return a narrower (sub) type.","**`Function1<in P, out R>`** — the declaration encoding this.","**Practical** — a more general function can stand in for a more specific one."]},{t:"note",text:"Function types are contravariant in parameters (in) and covariant in returns (out): (Animal)->Dog is a subtype of (Dog)->Animal. FunctionN<in P, out R> encodes this — a function accepting a broader param and returning a narrower result can substitute a more specific one."}]},{level:"senior",q:"What is the difference between a type parameter and a type argument?",a:[{t:"p",text:"A *type parameter* is the placeholder declared with the generic (`T` in `class Box<T>` or `fun <T> f()`). A *type argument* is the concrete type supplied when you *use* it (`String` in `Box<String>`). Parameter = the declaration side; argument = the call/usage side. It's the generic equivalent of parameter vs argument for values."},{t:"code",title:"Parameter vs argument",code:`class Box<T>(val value: T)     // T is a type PARAMETER (declaration)
val b = Box<String>("hi")      // String is a type ARGUMENT (usage)
fun <T> id(x: T): T = x        // T is a parameter
id<Int>(5)                     // Int is the argument (often inferred)`},{t:"list",items:["**Type parameter** — the placeholder in the declaration (`<T>`).","**Type argument** — the concrete type at the use site (`Box<String>`).","**Analogy** — like function parameter (declaration) vs argument (call).","**Inference** — arguments are often inferred, not written."]},{t:"note",text:"A type parameter is the placeholder in the declaration (T in class Box<T>); a type argument is the concrete type supplied at usage (String in Box<String>). Declaration side vs usage side — like function parameter vs argument. Arguments are often inferred."}]},{level:"senior",q:"How do you constrain a type parameter to be non-nullable?",a:[{t:"p",text:"The default upper bound is `Any?`, which *includes* nullable types — so a generic `T` could be `String?`. To forbid nullable arguments, bound it with `Any`: `<T : Any>`. Then `T` is guaranteed non-nullable, and you can't pass a nullable type or return null as `T`."},{t:"code",title:"Non-null bound",code:`fun <T> f(x: T) { }            // T can be String? (default Any?)
fun <T : Any> g(x: T) { }      // T must be non-null; g<String?>() is an error
fun <T : Any> firstNonNull(list: List<T?>): T? = list.firstOrNull { it != null }`},{t:"list",items:["**Default `Any?`** — allows nullable type arguments.","**`<T : Any>`** — forces non-nullable `T`.","**Use** — APIs that must not accept/return null for the generic type.","**`T & Any`** — definitely-non-nullable type for finer control in generic overrides."]},{t:"note",text:"The default upper bound Any? allows nullable type arguments (T could be String?). Bound with <T : Any> to force a non-nullable T (T=String? becomes an error). Use for APIs that must not accept/return null; T & Any is the definitely-non-nullable form for generic overrides."}]},{level:"senior",q:"What are common generics pitfalls caused by type erasure?",a:[{t:"p",text:"Erasure removes type arguments at runtime, causing several classic pitfalls: you can't overload two functions that differ only by generic type (`fun f(x: List<String>)` and `fun f(x: List<Int>)` clash — same erased signature); `is T` / `is List<String>` don't work (only `is List<*>`); and you can't create `Array<T>` directly. Solutions include `reified` inline functions and passing `Class<T>`."},{t:"list",items:["**Overload clash** — functions differing only by generic type have the same JVM signature (`@JvmName` can help, or restructure).","**`is` checks** — can't check the type argument; only `is List<*>`; use `reified` for `is T`.","**Generic arrays** — no `Array<T>(n)`; use `reified` or `List<T>`.","**Runtime type** — pass `Class<T>`/`KClass<T>` when you need the type at runtime without `reified`."]},{t:"note",text:"Erasure pitfalls: can't overload functions differing only by generic type (same erased signature — use @JvmName/restructure); can't do is List<String> (only is List<*> — use reified for is T); can't create Array<T> (use reified or List<T>). Pass Class<T>/KClass<T> for runtime type info without reified."}]},{level:"junior",q:"Why can't you create an array of a generic type (Array<T>)?",a:[{t:"p",text:"Because of type erasure, the runtime doesn't know `T`, and arrays are *reified* on the JVM (they carry their component type at runtime) — so `Array<T>(n)` can't be created directly. Workarounds: use a `reified` type parameter (`inline fun <reified T> makeArray()`), pass a factory/`Class<T>`, or use collections (`List<T>`) which don't have this problem."},{t:"code",title:"Generic array workarounds",code:`// fun <T> make(n: Int): Array<T> = Array(n) { ... }   // error
inline fun <reified T> make(n: Int, init: (Int) -> T): Array<T> = Array(n, init)   // OK

// Or just use a List:
fun <T> makeList(n: Int, init: (Int) -> T): List<T> = List(n, init)`},{t:"list",items:["**Arrays are reified** — they need the component type at runtime, which erasure removes.","**`reified` inline** — lets you create `Array<T>` (type known at call site).","**Pass `Class<T>`** — an alternative for reflection-based creation.","**Prefer `List<T>`** — collections avoid the reified-array problem."]},{t:"note",text:"Arrays are reified on the JVM (carry component type at runtime) but generics are erased, so Array<T>(n) can't be created directly. Fixes: inline fun <reified T> (type known at call site), pass a Class<T>, or use List<T> (collections don't have this problem)."}]},{level:"senior",q:"What is the difference between out T and in T in practice?",a:[{t:"p",text:"`out T` (covariant) makes `Container<Sub>` a subtype of `Container<Super>` — the container only *outputs* T (like a read-only `List<out E>`). `in T` (contravariant) makes `Container<Super>` a subtype of `Container<Sub>` — the container only *inputs* T (like a `Comparator<in T>`). They point subtyping in opposite directions based on producing vs consuming."},{t:"code",title:"out vs in subtyping",code:`val producers: List<out Animal> = listOf<Dog>()      // out: List<Dog> is a List<out Animal>
val comparator: Comparator<Dog> = Comparator<Animal> { a, b -> ... }  // in: Comparator<Animal> works for Dog`},{t:"list",items:["**`out T`** — covariant; `C<Sub>` ⊆ `C<Super>`; producer/output only.","**`in T`** — contravariant; `C<Super>` ⊆ `C<Sub>`; consumer/input only.","**`List<out E>`** — read-only, covariant (Kotlin's `List` is `out`).","**`Comparator<in T>`** — consumes T, contravariant."]},{t:"note",text:"out T (covariant): C<Sub> is a subtype of C<Super> — output-only (Kotlin's List<out E>). in T (contravariant): C<Super> is a subtype of C<Sub> — input-only (Comparator<in T>). They point subtyping opposite ways based on producing (out) vs consuming (in)."}]},{level:"junior",q:"How does the compiler infer generic types, and when must you specify them?",a:[{t:"p",text:"The compiler infers type arguments from the actual arguments and expected return type — `listOf(1, 2)` infers `List<Int>`, `emptyList<String>()` needs the explicit type because there's nothing to infer from. Specify explicitly when there are no arguments to infer from, when inference would pick a too-broad/narrow type, or to disambiguate."},{t:"code",title:"Inference vs explicit",code:`val a = listOf(1, 2)              // inferred List<Int>
val b = emptyList<String>()      // explicit — nothing to infer
val c = mutableListOf<User>()    // explicit element type for an empty list
val d: Box<Number> = Box(1)      // explicit to widen (else Box<Int>)`},{t:"list",items:["**Inferred** — from arguments/return context.","**Explicit needed** — no arguments (empty collections, `emptyList<T>()`).","**Widen/narrow** — annotate to get a broader/narrower type than inferred.","**Disambiguate** — when inference is ambiguous."]},{t:"note",text:"The compiler infers type arguments from arguments and expected return type (listOf(1,2) → List<Int>). Specify explicitly when there's nothing to infer from (emptyList<String>(), empty mutable collections), to widen/narrow the inferred type, or to disambiguate."}]},{level:"senior",q:"What is a recursive generic bound (like Comparable<T>)?",a:[{t:"p",text:"A recursive (F-bounded) generic bound references the type parameter itself — `<T : Comparable<T>>` means 'T is comparable to itself'. This is common for self-referential contracts like ordering, builders, or the Curiously Recurring Template Pattern, where a type's operations should return/accept its own type."},{t:"code",title:"Recursive bound",code:`fun <T : Comparable<T>> maxOf(a: T, b: T): T = if (a >= b) a else b
// T must implement Comparable<T> — comparable to its own type
maxOf("a", "b")     // String : Comparable<String>
maxOf(1, 2)         // Int : Comparable<Int>`},{t:"list",items:["**`<T : Comparable<T>>`** — T is bounded by a type parameterized on T itself.","**Self-referential contracts** — ordering, fluent builders returning `T`.","**Type safety** — ensures operations stay within the same type.","**Common** — sorting helpers, `max`/`min`/`coerceIn`."]},{t:"note",text:"A recursive (F-bounded) generic bound references the parameter itself: <T : Comparable<T>> ('T comparable to its own type'). Used for self-referential contracts — ordering (max/min), fluent builders returning T. Ensures operations stay within the same type."}]},{level:"senior",q:"How do Kotlin's variance modifiers map to Java wildcards?",a:[{t:"p",text:"Kotlin's declaration-site `out T` corresponds to Java's `? extends T` (covariant), and `in T` to `? super T` (contravariant). Kotlin's use-site projections `Foo<out T>`/`Foo<in T>` map directly to Java wildcards. When Kotlin code is called from Java, `out`/`in` generate the appropriate `extends`/`super` wildcards."},{t:"table",headers:["Kotlin","Java"],rows:[["`out T` / `Foo<out T>`","`? extends T`"],["`in T` / `Foo<in T>`","`? super T`"],["`Foo<*>`","`Foo<?>`"],["invariant `Foo<T>`","`Foo<T>`"]]},{t:"list",items:["**`out` ↔ `? extends`** — covariant/producer.","**`in` ↔ `? super`** — contravariant/consumer.","**`<*>` ↔ `<?>`** — unknown type.","**Interop** — Kotlin variance generates Java wildcards for Java callers."]},{t:"note",text:"Kotlin out T ↔ Java ? extends T (covariant); in T ↔ ? super T (contravariant); <*> ↔ <?>. Kotlin's declaration-site variance generates the right Java wildcards for Java callers. Same PECS concept, different syntax (Kotlin prefers declaration-site)."}]},{level:"junior",q:"What is a generic extension function?",a:[{t:"p",text:"A generic extension function adds a method to a generic type, with its own type parameters — `fun <T> List<T>.second(): T = this[1]`. Many standard-library functions (`map`, `filter`, `firstOrNull`) are exactly this: generic extensions on `Iterable<T>`/`List<T>`. They let you write reusable, type-safe utilities on generic receivers."},{t:"code",title:"Generic extension",code:`fun <T> List<T>.secondOrNull(): T? = getOrNull(1)
fun <T : Comparable<T>> List<T>.isSorted(): Boolean =
    zipWithNext().all { (a, b) -> a <= b }
listOf(1, 2, 3).isSorted()   // true`},{t:"list",items:["**`fun <T> Receiver<T>.ext()`** — extension with type parameters.","**Reusable utilities** — type-safe helpers on generic types.","**Constraints** — add bounds (`<T : Comparable<T>>`) for typed operations.","**Std lib** — most collection operators are generic extensions."]},{t:"note",text:"A generic extension function adds a type-safe method to a generic type (fun <T> List<T>.secondOrNull(): T?). It's how the standard library's map/filter/firstOrNull are built (generic extensions on Iterable<T>). Add bounds for typed operations (<T : Comparable<T>>)."}]},{level:"senior",q:"Why can't out and in types appear in the wrong position, and what does the compiler enforce?",a:[{t:"p",text:"Variance modifiers come with position rules the compiler enforces. An `out T` parameter can only appear in *output* positions (return types) — not as a function *parameter* (input), because that would let you pass a wrong type in and break covariance. An `in T` can only appear in *input* positions. This guarantees the variance is actually safe."},{t:"code",title:"Position enforcement",code:`interface Producer<out T> {
    fun get(): T           // OK — output position
    // fun set(item: T)    // ERROR — T in input position with 'out'
}
interface Consumer<in T> {
    fun consume(item: T)   // OK — input position
    // fun produce(): T    // ERROR — T in output position with 'in'
}`},{t:"list",items:["**`out T`** — output positions only (returns); not as a parameter.","**`in T`** — input positions only (parameters); not as a return type.","**Enforced** — the compiler rejects wrong-position usage.","**Why** — guarantees the covariance/contravariance is type-safe."]},{t:"note",text:"The compiler enforces variance positions: out T only in output positions (return types, not parameters — else you'd break covariance); in T only in input positions (parameters, not returns). This guarantees the declared variance is actually type-safe. @UnsafeVariance can override it (rarely, deliberately)."}]},{level:"senior",q:"What is a generic type alias, and when is it useful?",a:[{t:"p",text:"A `typealias` gives an existing (often generic or complex) type a shorter, meaningful name — `typealias UserCache = Map<String, User>` or `typealias Handler<T> = (T) -> Unit`. It doesn't create a new type (it's the same type), but improves readability for verbose generic/function types."},{t:"code",title:"typealias",code:`typealias UserMap = Map<String, User>
typealias ClickHandler = (View) -> Unit
typealias Result<T> = Either<Error, T>

fun process(handler: ClickHandler) { }   // clearer than (View) -> Unit`},{t:"list",items:["**`typealias`** — an alias for an existing type; not a new type.","**Readability** — name verbose generic/function types.","**Generic aliases** — `typealias Handler<T> = (T) -> Unit`.","**No new type** — fully interchangeable with the underlying type."]},{t:"note",text:"typealias gives an existing (often verbose generic/function) type a readable name — typealias ClickHandler = (View) -> Unit. It's NOT a new type (fully interchangeable with the original), just an alias for clarity. Can be generic: typealias Handler<T> = (T) -> Unit."}]},{level:"senior",q:"How do generic constraints enable calling methods on a type parameter?",a:[{t:"p",text:"Without a bound, a type parameter `T` is `Any?`, so you can only call `Any`'s methods (`toString`, `equals`). Adding a constraint (`<T : Number>`, `<T : Comparable<T>>`) tells the compiler `T` has that type's members, so you can call them. Constraints thus *unlock* the operations you need inside a generic function."},{t:"code",title:"Constraint unlocks methods",code:`fun <T> sumAll(items: List<T>): Double =
    items.sumOf { (it as Number).toDouble() }   // needs a cast — no bound

fun <T : Number> sumAll2(items: List<T>): Double =
    items.sumOf { it.toDouble() }               // toDouble() available — T is Number`},{t:"list",items:["**No bound** — `T` is `Any?`; only `Any`'s members.","**Bounded** — `<T : X>` lets you call `X`'s members on `T`.","**Avoids casts** — the constraint proves the operations are valid.","**Multiple bounds** — `where` for combining capabilities."]},{t:"note",text:"Without a bound, T is Any? (only Any's members). A constraint (<T : Number>) tells the compiler T has that type's members, so you can call them (it.toDouble()) without casts. Constraints unlock the operations you need inside a generic function; use where for multiple."}]},{level:"junior",q:"What is the difference between List<Any> and List<*>?",a:[{t:"p",text:"`List<Any>` is a list whose elements are declared to be `Any` — you know the type is `Any`. `List<*>` is a list of an *unknown but specific* type (could be `List<String>`, `List<Int>`, etc.) — you don't know which. You can pass a `List<String>` where `List<*>` is expected (unknown type), but *not* where `List<Any>` is expected unless it's covariant."},{t:"list",items:["**`List<Any>`** — elements known to be `Any`; accepts any element (since `List` is `out`, also accepts `List<String>`).","**`List<*>`** — unknown element type; read as `Any?`, can't write.","**Accepting any list** — `List<*>` is the way to accept 'some list of unknown type'.","**Kotlin `List` is covariant** — so `List<String>` *is* a `List<Any>` too (unlike `MutableList`)."]},{t:"note",text:"List<Any> = elements known to be Any. List<*> = unknown but specific element type (read as Any?, can't write). Use List<*> to accept 'any list'. Note Kotlin's List is covariant (out E), so List<String> IS a List<Any> — but MutableList (invariant) is not."}]},{level:"senior",q:"When would you use @UnsafeVariance or @JvmSuppressWildcards?",a:[{t:"p",text:"`@UnsafeVariance` lets you deliberately use a type parameter in a position variance rules forbid, when *you* guarantee it's safe (rare, e.g. `Comparable`'s definition uses it internally). `@JvmSuppressWildcards`/`@JvmWildcard` control the Java wildcard generation for Kotlin variance at the interop boundary — useful when Java callers need a specific wildcard form."},{t:"list",items:["**`@UnsafeVariance`** — override a variance position rule when you're sure it's safe; rare, deliberate.","**`@JvmSuppressWildcards`** — stop Kotlin from generating `? extends`/`? super` for Java callers.","**`@JvmWildcard`** — force wildcard generation at a specific spot.","**Interop-focused** — mostly for library APIs consumed from Java."]},{t:"note",text:"@UnsafeVariance deliberately uses a type param in a variance-forbidden position when you guarantee safety (rare — e.g. Comparable's internals). @JvmSuppressWildcards/@JvmWildcard control Java wildcard generation for Kotlin variance at the interop boundary (for Java callers needing a specific form). Library/interop concerns."}]},{level:"junior",q:"What is a generic interface, and how do you implement it?",a:[{t:"p",text:"A generic interface declares type parameters used by its methods — `interface Repository<T> { suspend fun get(id: String): T }`. Implementers either fix the type argument (`class UserRepo : Repository<User>`) or remain generic (`class MemoryRepo<T> : Repository<T>`). This is how you write reusable abstractions (repositories, mappers, data sources) parameterized by the entity type."},{t:"code",title:"Generic interface",code:`interface Repository<T> {
    suspend fun get(id: String): T?
    suspend fun save(item: T)
}
class UserRepository : Repository<User> {          // fixes T = User
    override suspend fun get(id: String): User? = ...
    override suspend fun save(item: User) { }
}`},{t:"list",items:["**`interface Foo<T>`** — type parameters used across its members.","**Fix the type** — `class Bar : Foo<Concrete>`.","**Stay generic** — `class Baz<T> : Foo<T>`.","**Uses** — repositories, mappers, `Comparable<T>`, data sources."]},{t:"note",text:"A generic interface parameterizes its methods (interface Repository<T> { get(): T }). Implementers fix the type (class UserRepo : Repository<User>) or stay generic (class MemoryRepo<T> : Repository<T>). It's how you build reusable abstractions (repositories, mappers) parameterized by the entity type."}]},{level:"senior",q:"How do you use Class<T> or KClass<T> when reified isn't available?",a:[{t:"p",text:"When you need the runtime type but can't use `reified` (e.g. a non-inline function, or storing it), pass the type explicitly as `Class<T>` (Java) or `KClass<T>` (Kotlin reflection). Libraries like Gson (`fromJson(json, User::class.java)`) and older DI frameworks use this. You can then call `clazz.newInstance()`, `clazz.isInstance(x)`, etc."},{t:"code",title:"Passing the type token",code:`fun <T> parse(json: String, clazz: Class<T>): T = gson.fromJson(json, clazz)
parse(json, User::class.java)

fun <T : Any> create(kClass: KClass<T>): T = kClass.constructors.first().call()`},{t:"list",items:["**`Class<T>`/`KClass<T>`** — pass the type as a value (token) when `reified` can't be used.","**Runtime operations** — `isInstance`, reflection, instantiation.","**`reified` wrapper** — often an `inline reified` function forwards to the `Class<T>` version.","**Uses** — serialization, DI, Room/Retrofit internals."]},{t:"note",text:"When reified isn't available (non-inline, or storing the type), pass Class<T> (Java) or KClass<T> (Kotlin) as a type token — enabling runtime type operations (isInstance, reflection, instantiation). Gson/DI use this (fromJson(json, User::class.java)). Often a reified inline wrapper forwards to the Class<T> version."}]},{level:"senior",q:"How do you build a type-safe builder or DSL with generics?",a:[{t:"p",text:"Combine generic type parameters, lambdas with receiver, and (optionally) `@DslMarker` to build fluent, type-safe DSLs. The builder holds typed state, methods return the builder or use receiver lambdas for nesting, and generics keep the result strongly typed — like `buildList<Int> { }` or a custom query builder."},{t:"code",title:"A tiny typed DSL",code:`@DslMarker annotation class QueryDsl

@QueryDsl
class QueryBuilder<T> {
    private val filters = mutableListOf<(T) -> Boolean>()
    fun where(predicate: (T) -> Boolean) { filters += predicate }
    fun build(): (T) -> Boolean = { item -> filters.all { it(item) } }
}
fun <T> query(block: QueryBuilder<T>.() -> Unit) = QueryBuilder<T>().apply(block).build()

val isAdult = query<User> { where { it.age >= 18 } }`},{t:"list",items:["**Generic builder** — `QueryBuilder<T>` keeps the DSL typed to `T`.","**Receiver lambda** — `block: Builder.() -> Unit` for the DSL body.","**`@DslMarker`** — prevents accidentally calling outer-scope methods in nested DSLs.","**Type-safe result** — generics carry the entity type through."]},{t:"note",text:"Type-safe DSLs combine generic type parameters + lambdas with receiver (block: Builder<T>.() -> Unit) + @DslMarker (blocks outer-scope calls in nested DSLs). The generic builder keeps the DSL and its result strongly typed to T — like buildList<Int> { } or a custom query builder."}]},{level:"senior",q:"How do you use where clauses for multiple type constraints?",a:[{t:"p",text:"When a type parameter needs *more than one* upper bound, list them in a `where` clause after the signature — `fun <T> copy(x: T) where T : CharSequence, T : Appendable`. This requires `T` to satisfy *all* the listed bounds, so you can use members from each. A single bound can go inline (`<T : Bound>`); multiple bounds need `where`."},{t:"code",title:"Multiple bounds",code:`fun <T> appendAll(target: T, items: List<T>)
    where T : CharSequence, T : Appendable {
    // can use both CharSequence and Appendable members on T
}`},{t:"list",items:["**`where T : A, T : B`** — `T` must satisfy all listed bounds.","**Single bound** — inline `<T : Bound>` suffices.","**Access all members** — from every bound.","**Multiple params** — `where` can constrain several parameters."]},{t:"note",text:"A where clause adds multiple upper bounds: fun <T> f(...) where T : CharSequence, T : Appendable — T must satisfy ALL bounds, so you can use members from each. A single bound goes inline (<T : Bound>); multiple bounds require where (which can also constrain several type parameters)."}]},{level:"senior",q:"Why is Kotlin's List covariant but MutableList invariant?",a:[{t:"p",text:"`List<out E>` is declared covariant because it's *read-only* — it only *produces* elements (via `get`, iteration), never accepts them, so it's safe for `List<String>` to be a `List<Any>`. `MutableList<E>` is *invariant* because it also *consumes* elements (`add`), and allowing `MutableList<String>` as `MutableList<Any>` would let you `add(42)` — corrupting the list. Read-only can be covariant; read-write can't."},{t:"code",title:"Covariant read-only, invariant mutable",code:`val strs: List<String> = listOf("a")
val anys: List<Any> = strs             // OK — List is covariant (out E), read-only

val mstrs: MutableList<String> = mutableListOf("a")
// val manys: MutableList<Any> = mstrs // ERROR — would allow manys.add(42)`},{t:"list",items:["**`List<out E>`** — produces only; covariant; `List<String>` is a `List<Any>`.","**`MutableList<E>`** — produces and consumes; invariant for safety.","**Why** — a covariant mutable container would let you insert a wrong type.","**General rule** — read-only → covariant; read-write → invariant."]},{t:"note",text:"List<out E> is covariant because it's read-only (only produces elements) — safe for List<String> to be List<Any>. MutableList is invariant because it also consumes (add) — covariance would let you add(42) to a String list. Read-only can be covariant; read-write must be invariant."}]}],Vx={id:"generics",title:"Generics & Variance",tagline:"Generic classes/functions, type constraints, type erasure & reified, invariance by default, out (covariance), in (contravariance), and star projection.",tags:["Kotlin","Generics","Variance","reified","out/in"],content:Lx,qa:jx},Nx=[{heading:"The 'by' keyword — delegation in one word",blocks:[{t:"p",text:"Kotlin's **`by`** keyword implements the **delegation pattern** — forwarding work to another object — at the language level. It comes in two flavors: **class delegation** (implement an interface by forwarding to another instance) and **property delegation** (let another object handle a property's get/set). Both replace boilerplate you'd otherwise write by hand."}]},{heading:"Class delegation — composition over inheritance",blocks:[{t:"p",text:"Class delegation lets a class implement an interface by **delegating all its methods to another object** that already implements it — without manually writing forwarding methods. `class MyList(list: List<T>) : List<T> by list` implements the entire `List` interface by forwarding to `list`. You can then override just the methods you want to customize."},{t:"code",title:"Implement an interface by delegation",code:`interface Repository { fun getAll(): List<Item>; fun save(item: Item) }

// Delegate everything to 'base', override only what you need:
class LoggingRepository(
    private val base: Repository,
) : Repository by base {          // forwards getAll() and save() to base
    override fun save(item: Item) {
        Log.d("Repo", "saving $item")
        base.save(item)           // add behavior, then delegate
    }
    // getAll() is auto-forwarded to base — no boilerplate
}`},{t:"list",items:["**Composition over inheritance**: instead of subclassing (tight coupling, fragile base class problem), you *compose* — wrap an object and forward to it, overriding selectively. This is the decorator pattern with zero boilerplate.","**The compiler generates the forwarding methods** for every interface member, delegating to the `by` object. You override only the ones you want to change.","**Uses**: decorators (logging, caching, validation wrappers around a repository), adapting one implementation, and avoiding deep inheritance hierarchies. It's why Kotlin favors 'prefer composition' — the language makes it easy."]}]},{heading:"Property delegation — by lazy, by Delegates, custom",blocks:[{t:"p",text:"Property delegation lets *another object* handle a property's get/set logic. `val x by SomeDelegate()` means reads and writes of `x` are routed to the delegate's `getValue`/`setValue`. The standard library provides ready-made delegates for common needs:"},{t:"code",title:"Standard property delegates",code:`// lazy — compute once, on first access, then cache; thread-safe by default
val config: Config by lazy {
    loadExpensiveConfig()          // runs only on first access to config
}

// Delegates.observable — run a callback whenever the property changes
var name: String by Delegates.observable("") { _, old, new ->
    println("name changed: $old -> $new")
}

// map-backed delegate — property reads from a map (common for JSON/config)
class User(map: Map<String, Any?>) {
    val id: String by map          // reads map["id"]
    val age: Int by map
}

// vetoable — validate/reject a change
var age: Int by Delegates.vetoable(0) { _, _, new -> new >= 0 }  // reject negatives`},{t:"list",items:["**`by lazy { }`** — the most common: computes the value *once*, lazily on first access, caches it, and is *thread-safe* by default (`LazyThreadSafetyMode.SYNCHRONIZED` — only one thread computes it). For single-threaded use, `lazy(LazyThreadSafetyMode.NONE)` is faster (no locking).","**`Delegates.observable`** — fires a callback on every change (old → new); great for reacting to property changes without manual setters.","**`Delegates.vetoable`** — like observable but can *reject* the change (return false).","**Map delegation** — a property backed by a map entry; handy for dynamic/config objects.","**Custom delegates** — implement `getValue`/`setValue` (or `ReadWriteProperty`) to encapsulate any get/set logic (e.g. a SharedPreferences-backed property)."]}]},{heading:"lateinit vs lazy — the classic comparison",blocks:[{t:"table",headers:["","lateinit var","by lazy { }"],rows:[["Keyword","`lateinit var`","`val ... by lazy { }`"],["Mutability","var (mutable, reassignable)","val (read-only)"],["Who initializes","you, explicitly, later","the lazy block, on first access"],["When initialized","whenever you assign it","automatically, on first read"],["If accessed before init","throws UninitializedPropertyAccessException","can't happen (initializes on access)"],["Nullable / primitives","non-null objects only (no Int/Boolean)","any type"],["Thread safety","none (you manage it)","thread-safe by default"]]},{t:"code",title:"When to use each",code:`// lateinit — value set by a framework/DI later, and it's mutable
class MyFragment : Fragment() {
    private lateinit var binding: FragmentBinding   // assigned in onCreateView
    // ::binding.isInitialized  can check if it's been set
}

// lazy — expensive read-only value computed on first use
class ViewModel {
    val repository: Repository by lazy { createRepository() }
}`},{t:"list",items:["**Use `lateinit`** for a non-null property that will be assigned *later by external code* (DI, a framework lifecycle callback like `onCreate`) and may be reassigned — you promise 'I'll set this before using it'. Accessing it before assignment throws (a clear error, better than a null).","**Use `lazy`** for a read-only property that's *expensive to compute* and should be created *once on first use* — the delegate handles initialization and caching. It's a `val`, so it can't be reassigned.","**`lateinit` can't be used with nullable types or primitives** (use a nullable `var` with null as the 'uninitialized' state instead); `lazy` works with anything.","**`::prop.isInitialized`** checks whether a lateinit has been assigned — useful to avoid the exception."]}]},{heading:"How property delegation works under the hood",blocks:[{t:"p",text:"`val x by delegate` compiles to calls on the delegate object: reading `x` calls `delegate.getValue(thisRef, property)`, and writing (for `var`) calls `delegate.setValue(thisRef, property, value)`. The delegate must provide these operator functions (or implement `ReadOnlyProperty`/`ReadWriteProperty`). This is why you can write your own delegates — it's just a convention of implementing two functions."},{t:"code",title:"A custom delegate — SharedPreferences-backed property",code:`class PrefDelegate(
    private val prefs: SharedPreferences,
    private val key: String,
    private val default: String,
) : ReadWriteProperty<Any?, String> {
    override fun getValue(thisRef: Any?, property: KProperty<*>): String =
        prefs.getString(key, default) ?: default
    override fun setValue(thisRef: Any?, property: KProperty<*>, value: String) {
        prefs.edit().putString(key, value).apply()
    }
}

// Usage — property reads/writes go straight to SharedPreferences:
var authToken: String by PrefDelegate(prefs, "token", "")`},{t:"note",text:"Delegation summary: **`by` (class delegation)** = implement an interface by forwarding to another object (composition over inheritance, zero boilerplate). **`by` (property delegation)** = another object handles a property's get/set. **`by lazy`** = compute-once, thread-safe, read-only. **`lateinit var`** = non-null, assigned-later, mutable, throws if accessed early. Custom delegates just implement `getValue`/`setValue`."}]}],Wx=[{level:"junior",q:"What is the difference between lateinit and lazy?",a:[{t:"p",text:"**Both let you delay initialization of a property, but they work differently and suit different situations.** `lateinit var` is a mutable property you promise to assign *yourself, later*, before using it — the initialization is your responsibility and can happen anytime (and be reassigned). `by lazy { }` is a read-only property whose value is computed *automatically on first access* by the lazy block, then cached."},{t:"list",items:["**`lateinit var`**: mutable (`var`), non-null object types only (no `Int`/`Boolean`), you assign it explicitly (often in a framework callback or via DI), and accessing it before assignment throws `UninitializedPropertyAccessException`. Use for values set later by external code — a Fragment's binding set in `onCreateView`, an injected dependency.","**`by lazy { }`**: read-only (`val`), works with any type, initializes itself on first read, caches the result, and is thread-safe by default. Use for expensive values that should be computed once, on demand — a lazily-created repository, a parsed config."]},{t:"code",title:"Each in its place",code:`private lateinit var binding: FragmentBinding    // assigned later in onCreateView
val db: Database by lazy { buildExpensiveDb() }  // computed once, on first use`},{t:"p",text:"Quick chooser: is it *mutable and set by someone else later*? → `lateinit`. Is it *read-only and expensive to compute once on demand*? → `lazy`. And remember `lateinit` can't hold primitives or nullables, while `lazy` can hold anything."}]},{level:"junior",q:"What does 'by lazy' do and is it thread-safe?",a:[{t:"p",text:"**The concept**: `val x by lazy { ... }` defers computing `x` until the *first time it's read*. The block runs once, on first access, and the result is cached — every subsequent read returns the cached value without re-running the block. If the property is never accessed, the block never runs."},{t:"p",text:"**Thread safety**: yes, by default. The default mode is `LazyThreadSafetyMode.SYNCHRONIZED`, which uses a lock to ensure that even if multiple threads access the property simultaneously on first use, the block runs *exactly once* and all threads see the same value. This makes it safe to use for shared lazy state. If you know the property is only accessed from a single thread, you can pass `LazyThreadSafetyMode.NONE` for a faster version that skips the locking (`by lazy(LazyThreadSafetyMode.NONE) { }`). There's also `PUBLICATION` mode (the block may run on multiple threads but only the first result is used). The default SYNCHRONIZED is the safe choice, and you only optimize to NONE when you're certain about single-threaded access and it's a hot path."}]},{level:"junior",q:"What is class delegation with the 'by' keyword?",a:[{t:"p",text:"**The concept**: class delegation lets a class implement an interface by *forwarding all its methods to another object* that already implements that interface — without you writing the forwarding boilerplate. `class Wrapper(base: Foo) : Foo by base` automatically implements every method of `Foo` by delegating to `base`. You then override only the methods you want to change."},{t:"code",title:"Decorator with almost no boilerplate",code:`interface Repository { fun getAll(): List<Item>; fun save(item: Item) }

class CachingRepository(private val base: Repository) : Repository by base {
    private val cache = mutableListOf<Item>()
    override fun getAll(): List<Item> = cache.ifEmpty { base.getAll().also { cache.addAll(it) } }
    // save() is auto-forwarded to base — no code needed
}`},{t:"p",text:"This is Kotlin's language-level support for **composition over inheritance** — the decorator pattern. Instead of subclassing (which couples you to the base class's implementation and risks the fragile-base-class problem), you *wrap* an object and forward to it, overriding selectively. The compiler generates all the forwarding methods, so adding logging, caching, or validation around an existing implementation takes almost no code. It's a big reason Kotlin can genuinely favor composition — the boilerplate that made composition tedious in Java is gone."}]},{level:"junior",q:"How does property delegation work?",a:[{t:"p",text:"**The concept**: `val x by delegate` routes the property's get (and set, for `var`) to another object — the delegate. Reading `x` doesn't read a field directly; it calls `delegate.getValue(...)`. Writing calls `delegate.setValue(...)`. The delegate encapsulates whatever logic backs the property."},{t:"code",title:"The compiler rewrites property access into delegate calls",code:`val name: String by SomeDelegate()
// reading 'name'  -> SomeDelegate.getValue(thisRef, property)
// (for var) writing -> SomeDelegate.setValue(thisRef, property, value)`},{t:"p",text:"This is how `by lazy`, `Delegates.observable`, and map-backed properties all work — they're just objects providing `getValue`/`setValue`. And because it's a simple convention, you can write *custom* delegates: implement `ReadOnlyProperty`/`ReadWriteProperty` (or the operator functions directly) to back a property with anything — SharedPreferences, a database, a network value, a computed cache. For example, a `PrefDelegate` can make `var token: String by PrefDelegate(prefs, \"token\")` read and write directly to SharedPreferences transparently. Property delegation is Kotlin's mechanism for factoring out reusable get/set behavior."}]},{level:"senior",q:"When would you write a custom property delegate, and what does it need to implement?",a:[{t:"p",text:"**You write a custom delegate when the same get/set *behavior* recurs across many properties and you want to factor it out cleanly.** The classic examples: properties backed by SharedPreferences/DataStore, properties that log or validate on change, properties computed from a resource, or properties with custom caching. Instead of writing a custom getter/setter on each one, you write the logic once in a delegate and apply it with `by`."},{t:"code",title:"A reusable SharedPreferences delegate",code:`class StringPref(
    private val prefs: SharedPreferences,
    private val key: String,
    private val default: String = "",
) : ReadWriteProperty<Any?, String> {
    override fun getValue(thisRef: Any?, property: KProperty<*>) =
        prefs.getString(key, default) ?: default
    override fun setValue(thisRef: Any?, property: KProperty<*>, value: String) =
        prefs.edit().putString(key, value).apply()
}

// Now every pref-backed property is one line:
class Settings(prefs: SharedPreferences) {
    var authToken: String by StringPref(prefs, "token")
    var username: String by StringPref(prefs, "username")
}`},{t:"list",items:["**What it must implement**: for a read-only (`val`) property, `getValue(thisRef, property)` — either directly as an `operator fun` or by implementing `ReadOnlyProperty<R, T>`. For a mutable (`var`) property, also `setValue(thisRef, property, value)` — via `ReadWriteProperty<R, T>`. The `property: KProperty<*>` parameter gives reflection info (like the property name), which is handy — e.g. you could default the preferences key to the property's own name.","**`provideDelegate`** (advanced): a delegate can also implement `provideDelegate` to run logic at the moment the delegate is *created and bound* to a property — useful for validation or registration at construction time (e.g. checking the property name, registering with a container).","**Why it's powerful**: it turns cross-cutting property behavior into a reusable, testable unit. The SharedPreferences example collapses dozens of boilerplate getter/setter pairs into one-liners, and you can unit-test the delegate once instead of every property. Libraries use this extensively — Koin/Hilt-style injection (`by inject()`), navigation args (`by navArgs()`), and `viewModels()` are all custom delegates."]}]},{level:"senior",q:"Why does Kotlin favor 'composition over inheritance', and how does class delegation support that?",a:[{t:"p",text:"**The principle 'composition over inheritance' says: to reuse and extend behavior, prefer *containing* another object and forwarding to it (composition) over *subclassing* it (inheritance).** Inheritance has well-known problems: it creates tight coupling to the base class's implementation details, suffers the 'fragile base class' problem (a base-class change silently breaks subclasses), forces a single-inheritance hierarchy, and exposes all of the base's protected members. Composition avoids all of these — you depend only on the *interface* of the object you wrap, not its internals."},{t:"list",items:["**The historical friction**: composition was always 'better' in theory, but in Java it meant writing tedious forwarding methods — to wrap a `List` and change one method, you had to manually implement all ~25 `List` methods to forward to the wrapped instance. That boilerplate pushed people toward inheritance out of laziness.","**How `by` removes the friction**: `class MyList(private val base: List<T>) : List<T> by base` generates *all* the forwarding methods automatically. Now wrapping-and-overriding-one-method is as little code as subclassing-and-overriding-one-method — so composition is finally as *convenient* as inheritance, not just as *correct*.","**Concrete benefits in practice**: you can decorate a repository with logging/caching/retry without subclassing it (and without the base needing to be `open`); you can compose multiple behaviors by wrapping wrappers; and you depend only on the interface, so the wrapped implementation can be swapped or mocked freely. It also plays well with Kotlin's default-`final` classes — you don't need to make classes `open` for inheritance, because you compose instead."]},{t:"code",title:"Stacking behaviors via composition",code:`val repo: Repository =
    LoggingRepository(
        CachingRepository(
            NetworkRepository()
        )
    )
// Each layer wraps the next via 'by', overriding only what it changes.
// Impossible to express this cleanly with single inheritance.`},{t:"p",text:"**The senior framing**: Kotlin's design nudges you toward composition in several ways — classes are `final` by default (inheritance requires explicit `open`), and `by` makes delegation nearly free. The language is deliberately making the *better* design (composition) also the *easier* one, correcting Java's accidental bias toward inheritance-via-boilerplate-avoidance. Class delegation is the concrete feature that makes 'prefer composition' practical rather than aspirational."}]},{level:"senior",q:"What are the thread-safety modes of lazy (SYNCHRONIZED, PUBLICATION, NONE)?",a:[{t:"p",text:"`lazy { }` takes a `LazyThreadSafetyMode`. `SYNCHRONIZED` (the default) locks so the initializer runs *once* even across threads. `PUBLICATION` allows multiple threads to run the initializer concurrently but the *first result* wins (all get the same value). `NONE` skips synchronization entirely — fastest, but only safe when accessed from a single thread."},{t:"code",title:"lazy modes",code:`val a by lazy { compute() }                                    // SYNCHRONIZED (default)
val b by lazy(LazyThreadSafetyMode.PUBLICATION) { compute() }  // first result wins
val c by lazy(LazyThreadSafetyMode.NONE) { compute() }         // single-thread only, fastest`},{t:"list",items:["**`SYNCHRONIZED` (default)** — thread-safe; initializer runs exactly once (locking).","**`PUBLICATION`** — concurrent init allowed, first result published to all.","**`NONE`** — no synchronization; fastest; only for single-threaded access (e.g. main-thread UI).","**Trade-off** — safety vs the small locking overhead; `NONE` when you know it's single-threaded."]},{t:"note",text:"lazy modes: SYNCHRONIZED (default — locks, runs once across threads), PUBLICATION (concurrent init, first result wins), NONE (no sync, fastest, single-thread only). Use the default for shared state; NONE for main-thread-only properties to skip locking overhead."}]},{level:"junior",q:"What is Delegates.observable, and how do you react to property changes?",a:[{t:"p",text:"`Delegates.observable(initial) { property, old, new -> }` is a built-in delegate whose callback fires *after* the property changes — giving you the old and new values. It's a clean way to run logic on assignment (logging, triggering updates) without a custom setter."},{t:"code",title:"observable",code:`var name: String by Delegates.observable("") { prop, old, new ->
    println("\${prop.name}: $old -> $new")
    if (old != new) onNameChanged(new)
}
name = "Sam"   // triggers the callback`},{t:"list",items:["**`Delegates.observable(init) { }`** — callback runs after each assignment.","**Old + new values** — react to the change.","**Uses** — logging, invalidating caches, notifying observers.","**vs custom setter** — no backing-field boilerplate."]},{t:"note",text:"Delegates.observable(initial) { prop, old, new -> } fires a callback AFTER each assignment with the old and new values — for logging, cache invalidation, or notifying observers, without writing a custom setter. Delegates.vetoable is the variant that can reject changes."}]},{level:"junior",q:"What is Delegates.vetoable, and how does it differ from observable?",a:[{t:"p",text:"`Delegates.vetoable(initial) { property, old, new -> Boolean }` runs *before* the change and can *reject* it — if the lambda returns `false`, the assignment is discarded and the property keeps its old value. `observable` fires *after* and can't prevent the change. Use `vetoable` for validation that should block invalid values."},{t:"code",title:"vetoable",code:`var age: Int by Delegates.vetoable(0) { _, old, new ->
    new >= 0        // reject negatives; property stays at old value
}
age = -5    // rejected; age stays 0
age = 30    // accepted`},{t:"list",items:["**`vetoable(init) { } : Boolean`** — return `false` to reject the assignment.","**Runs before** — validates before committing.","**vs `observable`** — observable runs after and can't reject.","**Uses** — enforce invariants/ranges on assignment."]},{t:"note",text:"Delegates.vetoable(initial) { _, old, new -> Boolean } runs BEFORE the change and rejects it if the lambda returns false (property keeps the old value) — for validation. observable runs AFTER and can't reject. Use vetoable to enforce invariants on assignment."}]},{level:"junior",q:"What is a map-backed property delegate (by map)?",a:[{t:"p",text:'A property can be delegated to a `Map` — `val name: String by map` reads the value from `map["name"]`. This is handy for dynamic data like parsing JSON into a typed object, or configuration. A `MutableMap` supports `var` properties (writes go back to the map).'},{t:"code",title:"by map",code:`class User(map: Map<String, Any?>) {
    val name: String by map        // reads map["name"]
    val age: Int by map            // reads map["age"]
}
val u = User(mapOf("name" to "Sam", "age" to 30))
u.name   // "Sam"`},{t:"list",items:["**`by map`** — property reads from the map by its name.","**`MutableMap`** — enables `var` (writes update the map).","**Uses** — dynamic/loosely-typed data (JSON, config) mapped to typed properties.","**Caveat** — missing keys / type mismatches fail at runtime."]},{t:"note",text:'val name: String by map reads the property from map["name"] (MutableMap enables var, writing back) — handy for mapping dynamic/loosely-typed data (JSON, config) to typed properties. Caveat: missing keys or type mismatches fail at runtime, not compile time.'}]},{level:"senior",q:"What is Delegates.notNull, and how does it compare to lateinit?",a:[{t:"p",text:"`Delegates.notNull<T>()` is a delegate for a non-null property initialized later — accessing it before assignment throws `IllegalStateException`. It's similar to `lateinit`, but works for *primitive types* (which `lateinit` doesn't support) at the cost of boxing. Prefer `lateinit` for object types; use `Delegates.notNull` when you need a late-initialized primitive."},{t:"code",title:"notNull for primitives",code:`var count: Int by Delegates.notNull()   // lateinit doesn't work for Int
fun init() { count = 5 }
// count before init() -> IllegalStateException`},{t:"list",items:["**`Delegates.notNull<T>()`** — late-initialized non-null property; throws if accessed before set.","**Works for primitives** — unlike `lateinit` (which needs object types).","**Boxing cost** — the delegate boxes the value.","**Prefer `lateinit`** — for non-primitive types (no delegate overhead)."]},{t:"note",text:"Delegates.notNull<T>() is a delegate for a late-initialized non-null property (throws if accessed before set) — works for PRIMITIVES (which lateinit can't do), at a boxing cost. Prefer lateinit for object types; use Delegates.notNull for a late-init primitive like Int."}]},{level:"senior",q:"How does a custom property delegate work (getValue/setValue)?",a:[{t:"p",text:"A delegate is any object providing `operator fun getValue(thisRef, property)` (and `setValue(...)` for `var`). Kotlin translates `val x by delegate` into calls to these. You can implement them directly, or implement `ReadOnlyProperty`/`ReadWriteProperty` interfaces. The `property` parameter (a `KProperty`) gives metadata like the property name."},{t:"code",title:"A custom delegate",code:`class Preference(private val key: String, private val default: String) {
    operator fun getValue(thisRef: Any?, property: KProperty<*>): String =
        prefs.getString(key, default)!!
    operator fun setValue(thisRef: Any?, property: KProperty<*>, value: String) {
        prefs.edit().putString(key, value).apply()
    }
}
var username: String by Preference("username", "")`},{t:"list",items:["**`getValue(thisRef, property)`** — returns the value (for `val`/`var`).","**`setValue(thisRef, property, value)`** — stores it (for `var`).","**`ReadWriteProperty<R, T>`** — an interface bundling both.","**`property.name`** — the delegated property's name (useful as a key)."]},{t:"note",text:"A custom delegate provides operator fun getValue(thisRef, property) (and setValue for var) — Kotlin translates `val x by delegate` into these calls. Implement directly or via ReadOnlyProperty/ReadWriteProperty. property.name gives the name (handy as a storage key, e.g. a SharedPreferences-backed property)."}]},{level:"senior",q:"Does class delegation forward calls to overridden methods?",a:[{t:"p",text:"This is a subtle gotcha. With `class C(b: Base) : Base by b`, calls to `C`'s inherited methods forward to the delegate `b`. But if `C` *overrides* a method, the delegate `b` doesn't know about that override — internal calls *within* `b` still call `b`'s own implementation, not `C`'s override. So class delegation doesn't give you the polymorphism you'd get from inheritance."},{t:"code",title:"Override isn't seen by the delegate",code:`interface Base { fun a(); fun b() }
class Impl : Base {
    override fun a() { println("Impl.a"); b() }   // calls Impl.b, not the override
    override fun b() { println("Impl.b") }
}
class Deco(base: Base) : Base by base {
    override fun b() { println("Deco.b") }
}
Deco(Impl()).a()   // prints "Impl.a" then "Impl.b" — Deco.b NOT called`},{t:"list",items:["**Forwarding** — inherited methods delegate to the wrapped object.","**Override gotcha** — the delegate's internal calls don't dispatch to your override.","**No inheritance polymorphism** — delegation composes, it doesn't subclass.","**Design around it** — don't expect overrides to intercept the delegate's internal calls."]},{t:"note",text:"Class delegation (C : Base by b) forwards inherited methods to b, but b's internal calls do NOT dispatch to C's overrides (b doesn't know about C). So overriding a method doesn't intercept the delegate's internal usage — delegation composes, it isn't inheritance polymorphism. Design around this."}]},{level:"junior",q:"How does 'by viewModels()' delegation work in Android?",a:[{t:"p",text:"`by viewModels()` is a property delegate (from the fragment/activity-ktx libraries) that lazily obtains a `ViewModel` scoped to the component. It handles the `ViewModelProvider` boilerplate and returns the same instance across configuration changes. Variants include `by activityViewModels()` (Activity-scoped, shared across fragments) and Hilt's `by hiltViewModel()` (in Compose)."},{t:"code",title:"viewModels delegate",code:`class MyFragment : Fragment() {
    private val viewModel: MyViewModel by viewModels()             // fragment-scoped
    private val shared: SharedViewModel by activityViewModels()    // activity-scoped
}`},{t:"list",items:["**`by viewModels()`** — lazy, component-scoped ViewModel; hides `ViewModelProvider` boilerplate.","**`by activityViewModels()`** — Activity-scoped, shared across the Activity's fragments.","**Survives config change** — returns the retained instance.","**Factory** — pass a factory lambda for constructor args (or use Hilt)."]},{t:"note",text:"by viewModels() is a lazy property delegate that obtains a component-scoped ViewModel (hiding ViewModelProvider boilerplate, returning the retained instance across config changes). by activityViewModels() scopes to the Activity (shared across fragments); pass a factory for constructor args, or use Hilt."}]},{level:"junior",q:"How does 'by remember' work as a delegate in Compose?",a:[{t:"p",text:"`var count by remember { mutableStateOf(0) }` uses property delegation: `MutableState<T>` provides `getValue`/`setValue` operators, so `by` unwraps `.value` automatically — reading `count` reads `state.value`, writing `count` writes `state.value`. This gives clean `count++` syntax instead of `count.value++`."},{t:"code",title:"by mutableStateOf",code:`var count by remember { mutableStateOf(0) }   // delegate unwraps .value
count++          // == count.value++
// vs without delegation:
val state = remember { mutableStateOf(0) }
state.value++`},{t:"list",items:["**`MutableState` is a delegate** — provides `getValue`/`setValue` for `.value`.","**`by`** — transparently reads/writes `.value`.","**Cleaner syntax** — `count++` instead of `count.value++`.","**Import** — needs `getValue`/`setValue` imports (usually auto-added)."]},{t:"note",text:"var x by remember { mutableStateOf(0) } uses property delegation — MutableState provides getValue/setValue, so `by` unwraps .value, giving clean x++/x = ... syntax instead of x.value. Needs the getValue/setValue imports (auto-added)."}]},{level:"senior",q:"When is lazy inappropriate, and what should you use instead?",a:[{t:"p",text:"`lazy` computes its value *once* and caches it forever — so it's wrong for values that need to *recompute* when dependencies change, or that depend on state not available at first access. For values derived from changing inputs, use a computed property (custom getter) or `derivedStateOf` (Compose); for late-but-settable values, use `lateinit`/`Delegates.notNull`."},{t:"list",items:["**`lazy`** — one-time, cached; for expensive values that never change once computed.","**Not for changing values** — it won't recompute; use a computed property (`get()`).","**Compose derivations** — `derivedStateOf` for values from changing state.","**Late-settable** — `lateinit`/`Delegates.notNull` for values assigned later (not computed once)."]},{t:"note",text:"lazy computes once and caches forever — wrong for values that must recompute when inputs change, or depend on not-yet-available state. Use a computed property (custom getter) or derivedStateOf (Compose) for changing values; lateinit/Delegates.notNull for late-but-settable values."}]},{level:"senior",q:"How do you compose behavior by delegating an interface to multiple objects?",a:[{t:"p",text:"You can implement several interfaces by delegating each to a different object — `class C : A by a, B by b` — composing behaviors from independent implementations without inheritance. This is powerful for mixing capabilities (e.g. a class that is both a `Logger` and a `Cache`) while keeping each concern in its own reusable object."},{t:"code",title:"Composing via delegation",code:`interface Logger { fun log(m: String) }
interface Cache { fun get(k: String): String? }
class Service(
    logger: Logger, cache: Cache,
) : Logger by logger, Cache by cache      // composes both behaviors`},{t:"list",items:["**Multiple `by`** — delegate each interface to a separate implementation.","**Composition** — mix capabilities without deep inheritance.","**Reusable parts** — each behavior lives in its own object.","**Override selectively** — provide your own version of specific methods."]},{t:"note",text:"class C : A by a, B by b composes behaviors by delegating each interface to a separate object — mixing capabilities (Logger + Cache) without inheritance, keeping each concern reusable. Override specific methods where you need custom behavior (mind the delegate-override gotcha)."}]},{level:"senior",q:"What is the overhead of property delegation?",a:[{t:"p",text:"Each delegated property involves an extra object (the delegate) and an indirection through `getValue`/`setValue` calls, and often stores a `KProperty` metadata reference. For most code this is negligible, but in *very* hot paths or when creating huge numbers of instances, the extra allocation/indirection can matter — a plain field or computed property may be leaner."},{t:"list",items:["**Extra object** — the delegate instance per property.","**Indirection** — `getValue`/`setValue` calls instead of direct field access.","**Metadata** — a `KProperty` reference is generated.","**Usually negligible** — matters only in hot paths / mass instantiation; then prefer plain fields."]},{t:"note",text:"Property delegation adds an extra delegate object, indirection through getValue/setValue, and a KProperty reference — negligible for most code, but in very hot paths or mass object creation the allocation/indirection can matter; a plain field or computed property is leaner there."}]},{level:"junior",q:"How do you delegate a property to another object's property?",a:[{t:"p",text:"Using the `::property` reference and the `getValue`/`setValue` extensions, you can delegate one property to another — `val x: Int by other::y` makes `x` read/write `other.y`. This is useful for renaming, forwarding, or exposing a nested property at a higher level."},{t:"code",title:"Delegating to another property",code:`class Wrapper(val inner: Inner) {
    var value: Int by inner::count    // reads/writes inner.count
}`},{t:"list",items:["**`by other::prop`** — delegate to another object's property.","**Forwarding** — expose or rename a nested property.","**`var` support** — writes forward if the target is mutable.","**Refactoring aid** — delegate a deprecated name to the new one."]},{t:"note",text:"val x by other::y delegates a property to another object's property (reads/writes forward). Useful for exposing/renaming a nested property or forwarding a deprecated name to a new one. var works if the target is mutable."}]},{level:"junior",q:"Is a lazy property computed once, and is the result cached?",a:[{t:"p",text:"Yes — `by lazy { }` runs its initializer on the *first* access and *caches* the result; every subsequent access returns the same cached value without re-running the initializer. This makes it ideal for expensive one-time initialization (parsing config, building a heavy object) that you want deferred until needed."},{t:"code",title:"lazy caches",code:`val config by lazy {
    println("computing")   // prints only on first access
    parseConfig()
}
config   // "computing" + value
config   // cached value, no re-computation`},{t:"list",items:["**First access** — runs the initializer.","**Cached** — subsequent accesses return the stored value.","**Deferred** — nothing happens until first use.","**One-time init** — expensive setup you may not always need."]},{t:"note",text:"Yes — by lazy { } runs its initializer on first access and caches the result; later accesses return the cached value without recomputing. Ideal for deferred, expensive one-time initialization (parsing config, building a heavy object) you may not always need."}]},{level:"junior",q:"What's the difference between a property delegate and a computed property?",a:[{t:"p",text:"A *computed property* (custom `get()`) recomputes its value on *every* access — no storage. A *delegated property* (`by`) routes access through a delegate object that decides how to store/compute/cache. So `by lazy` caches (unlike a computed getter that recomputes), and delegates can add behavior (observation, persistence) a plain getter can't."},{t:"list",items:["**Computed property** — `get()` recomputes each access; no state.","**Delegated property** — `by delegate`; delegate controls storage/behavior (caching, persistence, observation).","**`by lazy`** — caches (computed getter wouldn't).","**Reusable** — a delegate encapsulates behavior across many properties; a getter is per-property."]},{t:"note",text:"A computed property (custom get()) recomputes on every access (no storage); a delegated property (by) routes access through a delegate that controls storage/behavior — so by lazy caches, and delegates add reusable behavior (observation, persistence) a getter can't. Delegates are reusable across properties."}]},{level:"senior",q:"How would you build a SharedPreferences-backed property delegate?",a:[{t:"p",text:'Implement a delegate whose `getValue` reads from `SharedPreferences` and `setValue` writes to it, using the property name (or a passed key) as the preference key. This gives you clean, typed access to preferences as ordinary properties — `var darkMode: Boolean by BooleanPref("dark_mode", false)`.'},{t:"code",title:"Preference delegate",code:`class BooleanPref(
    private val prefs: SharedPreferences,
    private val key: String,
    private val default: Boolean,
) : ReadWriteProperty<Any?, Boolean> {
    override fun getValue(thisRef: Any?, property: KProperty<*>) =
        prefs.getBoolean(key, default)
    override fun setValue(thisRef: Any?, property: KProperty<*>, value: Boolean) =
        prefs.edit().putBoolean(key, value).apply()
}
var darkMode: Boolean by BooleanPref(prefs, "dark_mode", false)`},{t:"list",items:["**`ReadWriteProperty`** — implement `getValue`/`setValue`.","**Reads/writes prefs** — typed access, key from name or param.","**Reusable** — one delegate class for many preference properties.","**Modern note** — DataStore is preferred over SharedPreferences, but the delegate pattern is the same idea."]},{t:"note",text:"Implement a ReadWriteProperty delegate: getValue reads SharedPreferences, setValue writes it (key from property name or a param). Gives typed preference access as plain properties (var darkMode by BooleanPref(...)). Reusable across properties. (DataStore is the modern preference store, same delegate idea.)"}]},{level:"senior",q:"How does the 'by' keyword desugar for class versus property delegation?",a:[{t:"p",text:"For *class delegation* (`class C : Base by b`), the compiler generates forwarding methods for each `Base` member that call the delegate `b`. For *property delegation* (`val x by d`), the compiler stores the delegate and translates reads/writes of `x` into `d.getValue(...)`/`d.setValue(...)` calls. Both use `by`, but one forwards *interface methods* and the other routes *property accessors*."},{t:"list",items:["**Class delegation** — generates method forwarders to the delegate for each interface member.","**Property delegation** — routes get/set through the delegate's `getValue`/`setValue`.","**Shared keyword** — `by` in both, but different desugaring.","**Both** — reduce boilerplate (forwarding methods / accessor logic)."]},{t:"note",text:"by desugars differently: class delegation (C : Base by b) generates forwarding methods to b for each interface member; property delegation (val x by d) routes reads/writes through d.getValue/setValue. Same keyword, different mechanism — one forwards interface methods, the other routes property accessors."}]},{level:"junior",q:"What is lateinit good for, and what are its restrictions?",a:[{t:"p",text:"`lateinit var` declares a non-null property you'll initialize later — perfect for values set in lifecycle callbacks, DI, or `setUp()` in tests. Restrictions: it works only on `var` (not `val`), only non-nullable *object* types (not primitives), can't have a custom accessor, and throws `UninitializedPropertyAccessException` if read before assignment."},{t:"list",items:["**Use** — DI-injected fields, view binding, lifecycle-initialized values, test fixtures.","**`var` only** — not `val`.","**Non-null object types** — no primitives (use `Delegates.notNull`).","**Access before init** — throws `UninitializedPropertyAccessException`; check `::prop.isInitialized`."]},{t:"note",text:"lateinit var = non-null property initialized later (DI, view binding, lifecycle, test setup). Restrictions: var only, non-nullable object types only (not primitives — use Delegates.notNull), no custom accessor, throws UninitializedPropertyAccessException if read before set (check ::prop.isInitialized)."}]},{level:"senior",q:"What is the provideDelegate operator, and when is it useful?",a:[{t:"p",text:"`operator fun provideDelegate(thisRef, property)` lets a delegate *factory* run logic when the delegate is *created* (at property initialization), returning the actual delegate. It's used for validation (checking the property name/type up front) or registering the property — for example, verifying a key exists before the property is ever read."},{t:"code",title:"provideDelegate",code:`class PrefProvider(private val key: String) {
    operator fun provideDelegate(thisRef: Any?, property: KProperty<*>): ReadWriteProperty<Any?, String> {
        require(isValidKey(key)) { "invalid key" }   // validate at creation
        return PrefDelegate(key)
    }
}`},{t:"list",items:["**`provideDelegate`** — runs at delegate creation; returns the real delegate.","**Up-front logic** — validation, registration, resource acquisition.","**Access to metadata** — the `property` before any get/set.","**Advanced** — a library feature, rarely needed in app code."]},{t:"note",text:"provideDelegate(thisRef, property) runs when the delegate is created (property init) and returns the actual delegate — for up-front validation, registration, or resource acquisition using the property metadata before any get/set. Advanced/library feature; rare in app code."}]},{level:"junior",q:"When should you use delegation instead of a helper function or inheritance?",a:[{t:"p",text:"Use *class delegation* to compose reusable behavior (implement an interface by wrapping an existing implementation) — favoring composition over inheritance. Use *property delegation* to reuse property behavior (lazy, observable, persistence) across many properties. Use a plain *helper function* when there's no property/interface to reuse — don't over-engineer with delegates when a function suffices."},{t:"list",items:["**Class delegation** — compose/reuse behavior without subclassing (decorators, mixins).","**Property delegation** — reuse property logic (lazy/observable/persisted) across properties.","**Helper function** — simple, one-off logic with no reuse pattern.","**Avoid over-engineering** — delegates add indirection; use them when the reuse is real."]},{t:"note",text:"Class delegation to compose/reuse behavior without inheritance (decorators, mixins); property delegation to reuse property logic (lazy/observable/persisted) across properties; a plain helper function for one-off logic with no reuse. Don't over-engineer — delegates add indirection, so use them when the reuse pattern is real."}]},{level:"senior",q:"How does class delegation help you build the decorator pattern?",a:[{t:"p",text:"Class delegation makes decorators trivial: implement the interface `by` delegating to the wrapped object, then override *only* the methods you want to augment — the rest forward automatically. This gives you a decorator with minimal boilerplate (no manual forwarding of every method), wrapping and extending behavior."},{t:"code",title:"A logging decorator",code:`interface Repository { fun get(id: String): Data; fun save(d: Data) }

class LoggingRepository(private val delegate: Repository) : Repository by delegate {
    override fun save(d: Data) {          // augment only save
        log("saving $d")
        delegate.save(d)                  // still call the real one
    }
    // get() forwards to delegate automatically
}`},{t:"list",items:["**`by delegate`** — auto-forwards all interface methods.","**Override selectively** — only the methods you decorate.","**No boilerplate** — unforwarded methods delegate for free.","**Uses** — logging, caching, validation, metrics wrappers."]},{t:"note",text:"Class delegation makes decorators trivial: implement the interface by delegate (auto-forwards all methods), then override only the ones you augment (calling delegate for the real work). No manual forwarding boilerplate. Great for logging/caching/validation/metrics wrappers."}]},{level:"senior",q:"How do you unit-test a class that uses lazy or lateinit properties?",a:[{t:"p",text:"For `lateinit`, set the property in the test's `setUp()` (or via the constructor/DI) before exercising the class — accessing it unset throws. For `by lazy`, it initializes on first access, so tests just use the property normally; if the lazy computation has dependencies, inject them so the lazy block uses test doubles. Prefer constructor injection over `lateinit` for testability."},{t:"list",items:["**`lateinit`** — assign in `@Before`/`setUp()` or inject before use; unset access throws.","**`by lazy`** — initializes on first access; ensure its dependencies are injectable test doubles.","**Prefer constructor injection** — more testable than field `lateinit` (no hidden init order).","**`::prop.isInitialized`** — assert init state if relevant."]},{t:"note",text:"lateinit: assign in setUp()/inject before use (unset access throws). by lazy: initializes on first access — make its dependencies injectable test doubles. Prefer constructor injection over field lateinit for testability (no hidden init-order coupling). Check ::prop.isInitialized if needed."}]},{level:"junior",q:"What built-in property delegates does the Kotlin standard library provide?",a:[{t:"p",text:"The standard library ships several ready-made delegates so you rarely write your own: `lazy` (compute-once, cached), `Delegates.observable` (callback after change), `Delegates.vetoable` (reject changes), `Delegates.notNull` (late-init for any type), and map-backed properties (`by map`). Knowing these covers most delegation needs."},{t:"list",items:["**`lazy { }`** — lazily computed, cached read-only value.","**`Delegates.observable(init) { }`** — react after each assignment (old/new).","**`Delegates.vetoable(init) { }`** — validate/reject an assignment before it commits.","**`Delegates.notNull()`** — late-initialized non-null property (works for primitives).","**`by map`/`by mutableMap`** — read/write properties from a map."]},{t:"note",text:"Standard-library delegates: lazy (compute-once, cached), Delegates.observable (post-change callback), Delegates.vetoable (reject changes), Delegates.notNull (late-init, any type incl. primitives), and by map (map-backed properties). These cover most needs — write a custom delegate only for bespoke storage/behavior."}]},{level:"senior",q:"How do you delegate a ViewModel property to SavedStateHandle for process-death survival?",a:[{t:"p",text:"`SavedStateHandle` behaves like a map, so you can create a property delegate that reads/writes through it — persisting the value across process death. AndroidX provides `savedStateHandle.saveable { }` (Compose) and you can build a simple delegate wrapping `handle[key]`/`handle[key] = value` or expose a `StateFlow` via `getStateFlow`."},{t:"code",title:"SavedStateHandle-backed property",code:`class MyViewModel(private val handle: SavedStateHandle) : ViewModel() {
    var query: String
        get() = handle["query"] ?: ""
        set(value) { handle["query"] = value }   // survives process death

    val queryFlow = handle.getStateFlow("query", "")   // observable variant
}`},{t:"list",items:["**`SavedStateHandle` is map-like** — read/write by key survives process death.","**Custom getter/setter or delegate** — route the property through the handle.","**`getStateFlow(key, default)`** — an observable, saved StateFlow.","**`saveable { }`** — Compose-friendly saved state in a ViewModel."]},{t:"note",text:"SavedStateHandle is map-like, so route a ViewModel property through handle[key] (custom getter/setter or a delegate) to survive process death; getStateFlow(key, default) gives an observable saved StateFlow, and saveable { } is the Compose-friendly variant. It's the process-death companion to viewModelScope's config-change survival."}]},{level:"junior",q:"What does property delegation give you over a plain custom getter/setter?",a:[{t:"p",text:"A custom getter/setter is *per-property* boilerplate; a property delegate *encapsulates* the get/set logic in a reusable object you can apply to many properties with `by`. So instead of writing the same validation/persistence/observation accessors on every property, you write one delegate and reuse it — cleaner and DRY."},{t:"code",title:"Reusable vs repeated",code:`// Repeated custom accessors on each property:
var a: String get() = prefs.getString("a", "")!!; set(v) { prefs.edit()... }
var b: String get() = prefs.getString("b", "")!!; set(v) { prefs.edit()... }

// One reusable delegate:
var a: String by StringPref("a")
var b: String by StringPref("b")`},{t:"list",items:["**Custom accessor** — logic lives on each property (repeated).","**Delegate** — logic in one reusable object, applied via `by`.","**DRY** — validation/persistence/observation written once.","**Composable** — the same delegate across many properties/classes."]},{t:"note",text:"A custom getter/setter repeats accessor logic per property; a property delegate encapsulates that logic in one reusable object applied with `by` across many properties — DRY (validation/persistence/observation written once). Use a delegate when the same accessor behavior recurs; a plain getter for one-off logic."}]}],Bx={id:"delegation",title:"Delegation & Advanced",tagline:"The by keyword, class delegation (composition over inheritance), property delegation, by lazy vs lateinit, and writing custom delegates.",tags:["Kotlin","Delegation","by lazy","lateinit","Composition"],content:Nx,qa:Wx},zx={id:"kotlin",name:"Kotlin Language",description:"The language itself — null safety & types, classes (data/sealed/value/object), functions & scope functions, collections & sequences, generics & variance, and delegation.",topics:[Ix,Dx,Ux,Fx,Vx,Bx]},Hx=[{heading:"What Jetpack Compose is — and what 'declarative' actually means",blocks:[{t:"p",text:'**Jetpack Compose** is Android\'s modern UI toolkit where you build UI by writing **Kotlin functions** instead of XML layouts. Before Compose, Android UI was **imperative**: you inflated an XML layout into a tree of View objects, kept references to them (`findViewById`, ViewBinding), and *mutated* them step by step — `textView.text = "Hi"`, `progressBar.visibility = GONE`. Your code had to describe **how to change** the screen from its current state to the next one, for every possible transition.'},{t:"p",text:`Compose is **declarative**: you write a function that describes **what the UI looks like for a given state** — and that's all. You never mutate widgets. When state changes, Compose *re-runs* the relevant functions and figures out the difference itself. The mental model shifts from "a tree of objects I mutate" to "UI = f(state)": the screen is simply the result of calling your function with the current state.`},{t:"code",title:"Imperative (Views) vs declarative (Compose) — the same feature",code:`// IMPERATIVE: describe transitions, for every case, manually
fun render(user: User?) {
    if (user == null) {
        progressBar.visibility = View.VISIBLE
        nameText.visibility = View.GONE
    } else {
        progressBar.visibility = View.GONE
        nameText.visibility = View.VISIBLE
        nameText.text = user.name
    }
}

// DECLARATIVE: describe the result; Compose handles transitions
@Composable
fun UserScreen(user: User?) {
    if (user == null) {
        CircularProgressIndicator()
    } else {
        Text(text = user.name)
    }
}`},{t:"list",items:["Why this matters: imperative UI bugs are almost always **forgotten transitions** (you showed the spinner but forgot to hide the error from last time). Declarative UI can't have that class of bug — every run describes the *whole* result from scratch.",`"Re-runs the function" sounds wasteful — it isn't, because Compose is smart about re-running only what could have changed. That machinery (recomposition) is the heart of Compose and of most interview questions.`,"Compose is also **unbundled from the OS** (ships as a library, updates without OS updates) and is the foundation of **Compose Multiplatform** — the same model runs on iOS, desktop, and web."]}]},{heading:"@Composable functions — the rules of the game",blocks:[{t:"p",text:"A function annotated with `@Composable` is not a normal function. The annotation tells the **Compose compiler plugin** to rewrite it: behind the scenes, every composable secretly receives an extra parameter (a `Composer`) and gets calls injected that record what the function *emits* — text, layout nodes, images — into an in-memory structure. That's why the rules below exist:"},{t:"list",items:["**Composables can only be called from other composables** — because they need that hidden Composer parameter, which only the Compose runtime provides. Calling one from a normal function is a compile error.",'**They return Unit** (usually) — a composable doesn\'t *return* UI; it **emits** UI into the composition as a side effect the runtime tracks. `Text("Hi")` adds a text node; it hands you nothing back.',"**They can run in any order and in parallel** (in theory): Compose reserves the right to skip, reorder, or run composables optimistically. So they must be **fast and free of your own side effects** — no writing globals, no network calls, no mutating shared objects directly in the body. (Side effects have dedicated, controlled APIs — covered in the Side Effects topic.)","**They can re-run many times** (recomposition) — anything you do in the body must be safe to repeat. Creating an object per call is fine; incrementing a global counter is a bug.","**Naming convention**: composables that emit UI are `PascalCase` nouns (`ProfileCard`), because conceptually they *declare a thing*, not *do an action*."]},{t:"note",text:'Interview phrasing that shows understanding: "`@Composable` is a compiler contract, like `suspend`. Just as `suspend` functions can only be called from coroutines because the compiler threads a Continuation through them, composables can only be called from composables because the compiler threads a Composer through them." That parallel is exactly right and interviewers love it.'}]},{heading:"Composition and recomposition — the core concept",blocks:[{t:"p",text:"**Composition** is what Compose builds when it runs your composable functions the first time: a tree describing your UI, stored in an internal structure called the **slot table**. Think of it as Compose's memory of *what your functions emitted and with which inputs* — every composable call, its parameters, and its `remember`ed values get a slot."},{t:"p",text:'**Recomposition** is Compose re-running composable functions when the state they read has changed, and updating the composition to match. The critical word is **read**: during execution, Compose tracks exactly which state objects (`State<T>` instances) each composable *reads*. When one of those values changes, Compose knows precisely which functions depend on it — and re-runs **only those**, skipping everything else. This is why "the whole function re-runs" doesn\'t mean "the whole screen re-renders".'},{t:"code",title:"Recomposition scope in action",code:`@Composable
fun Counter() {
    var count by remember { mutableStateOf(0) }   // observable state

    Column {
        Header()                    // reads no state -> never recomposes
        Text("Count: " + count)     // reads count -> recomposes on change
        Button(onClick = { count++ }) {
            Text("Increment")       // reads no state -> skipped
        }
    }
}`},{t:"list",items:["When `count` changes: Compose invalidates the nearest **recomposition scope** that read it (here, the `Counter` body), re-runs it, and while re-running, **skips** child composables whose parameters are unchanged (`Header()`, the Button content). Only the `Text` showing count actually produces new output.","Recomposition is **triggered by state writes, scoped by state reads**. You don't subscribe manually to anything — reading `count` inside a composable *is* the subscription. This automatic tracking is done by the **snapshot state system** (detailed in the State topic).","Recomposition must be **restartable**: Compose may abandon a recomposition mid-way if the state changes again before it finishes (e.g. during an animation) and restart with fresh values. Another reason bodies must be side-effect-free.","Key vocab to use precisely: *composition* = the built tree; *initial composition* = first build; *recomposition* = incremental re-execution; *leaving the composition* = a composable's branch no longer being emitted (its `remember`ed state is forgotten)."]}]},{heading:"The three phases: Composition → Layout → Draw",blocks:[{t:"p",text:"Every frame that needs UI work goes through up to three phases — and understanding them separately is what unlocks both performance work and custom layouts:"},{t:"list",items:['**1. Composition — "what to show"**: runs your composable functions, producing/updating the tree of layout nodes. This is the only phase that executes *your* composable code.',`**2. Layout — "where to place it"**: walks the tree; each node measures its children and places them. Compose's layout is **single-pass**: a parent measures each child exactly once (children can't be re-measured in the same pass) — this is why deep nesting doesn't cause the exponential double-measurement View systems suffered from.`,'**3. Draw — "how to render it"**: nodes draw themselves into a Canvas, top-down.']},{t:"p",text:"The key performance insight: **the phases can run independently**. If only *where* something sits changes (a drag offset), Compose can skip composition entirely and just re-layout; if only *pixels* change (a color animation), it can skip straight to draw. You opt into this by **deferring state reads** to the latest possible phase — e.g. using the lambda version of `offset` or `graphicsLayer` so the animated value is read during layout/draw instead of composition:"},{t:"code",title:"Deferring a read to skip recomposition entirely",code:`// BAD: reads dragOffset during COMPOSITION -> recomposes every drag frame
Box(Modifier.offset(x = dragOffset, y = 0.dp))

// GOOD: lambda defers the read to the LAYOUT phase ->
// dragging re-runs layout only; composition never re-executes
Box(Modifier.offset { IntOffset(dragOffset.roundToPx(), 0) })

// Same idea for draw-phase-only changes:
Box(Modifier.graphicsLayer { alpha = animatedAlpha.value })`},{t:"note",text:'This is one of the highest-frequency Compose interview questions: "name the phases, and how do you use them to optimize?" Answer: composition/layout/draw; state read *location* decides which phase re-runs; defer reads (lambda modifiers, `graphicsLayer`) so high-frequency changes touch only layout or draw.'}]},{heading:"Under the hood: slot table, Composer, and positional memoization",blocks:[{t:"p",text:"You don't need internals to *use* Compose, but interviewers use them to separate depth from surface knowledge. The essentials, plainly:"},{t:"list",items:["**The slot table** is a gap-buffer data structure where the composition lives: every composable call gets a **group** identified by its *position in the code* (compiler-generated key), storing its inputs and `remember`ed slots. On recomposition, Compose walks the table, compares stored inputs to new ones, and re-executes only mismatching groups.","**Positional memoization**: identity comes from *call position*, not function name. The same `Text()` called at two different places in code is two different groups with independent state. Call it in a loop and each iteration's position is the same — which is why dynamic lists need `key()` to give items stable identities (detailed in Lazy Lists).","**The Composer** is the hidden parameter the compiler injects into every composable: it's the cursor into the slot table — `startGroup`/`endGroup` calls the compiler inserts around your code are it navigating and diffing the table.","**Donut-hole skipping**: because groups nest, Compose can re-run an outer function but skip inner groups whose inputs didn't change (the 'donut'), or re-run just an inner lambda while the outer stays cached (the 'hole') — this is what makes 'the whole function re-runs' cheap in practice.","**The Recomposer** is the scheduler: it collects invalidated scopes and drives recomposition each frame, synchronized with the display's frame clock (`withFrameNanos`)."]}]},{heading:"Where composition starts: setContent and the bridge to the OS",blocks:[{t:"code",title:"The entry point",code:`class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {                 // creates the composition
            MyAppTheme {
                AppNavHost()
            }
        }
    }
}`},{t:"list",items:["`setContent` installs a single **ComposeView** as the Activity's content — under Compose there is still exactly *one* Android View hosting the whole composition; inside it, Compose manages its own node tree (LayoutNodes), not Views.","That's why Compose interops cleanly both directions: a `ComposeView` can sit inside XML layouts, and `AndroidView { }` can host a classic View inside a composition (covered in the Interop topic).",'Frame flow end-to-end: state write → snapshot notifies → Recomposer schedules → next vsync: recompose invalid scopes → layout → draw → render thread. Knowing this pipeline lets you answer "what actually happens when I click a button?" precisely.']}]},{heading:"Compose vs the View system — the comparison question",blocks:[{t:"table",headers:["","View system (XML)","Compose"],rows:[["UI definition","XML inflated into View objects","Kotlin functions emitting nodes"],["Updates","imperative mutation of retained objects","re-execution + diffing (recomposition)"],["State handling","manual sync between data and widgets","UI derived from state; tracking automatic"],["Measurement","multiple measure passes possible (exponential in nesting)","single-pass; intrinsics for the exceptions"],["Reuse","custom Views, styles, fragments","plain functions — trivially composable and parameterizable"],["Tooling age","20 years of maturity, every edge case solved","modern preview/live-edit; some advanced cases still land first in Views"],["Interop","—","two-way (ComposeView / AndroidView)"]]},{t:"list",items:["Balanced take for interviews: Compose removes whole bug classes (state-sync, forgotten transitions) and makes custom UI dramatically cheaper; the costs are a learning curve (recomposition mental model), some interop seams, and needing performance discipline (stability) that XML never asked of you.","New development is Compose-first per Google; the View system remains supported and coexists via interop — 'rewrite everything' is never the right answer in an interview."]}]}],Kx=[{level:"junior",q:"What is Jetpack Compose and how is it different from the XML/View system?",a:[{t:"p",text:'**The concept**: there are two ways to build UI. The old Android way is *imperative* — you create widget objects (from XML), keep references to them, and mutate them over time: `textView.text = "Hi"`, `spinner.visibility = GONE`. Your code manages every transition between screen states by hand. Compose is *declarative* — you write a Kotlin function that describes what the screen looks like **for a given state**, and never touch widgets afterwards. When state changes, Compose re-runs your function and updates only what differs.'},{t:"p",text:"**Why that's a big deal**: imperative UI bugs are usually forgotten transitions — you showed the loading spinner but forgot to hide last time's error message. In Compose that bug can't exist, because each run describes the *entire* result from scratch; there is no stale leftover to forget. It also collapses the state-synchronization problem: data and UI can't drift apart when UI is literally computed from data (`UI = f(state)`)."},{t:"p",text:"**Concrete differences to name**: no XML — layout, styling and logic are all Kotlin; no `findViewById`/ViewBinding — there are no retained widget objects to reference; updates happen by *recomposition* (smart re-execution) instead of mutation; and it ships as a library independent of the OS version, which is also what enables Compose Multiplatform (same code on iOS/desktop)."}]},{level:"junior",q:"What is a @Composable function and what special rules apply to it?",a:[{t:"p",text:"**The concept**: `@Composable` marks a function that *describes UI*. It's not just documentation — the Compose **compiler plugin** rewrites the function: it secretly adds a `Composer` parameter and injects bookkeeping calls, so that when the function runs, everything it emits (text nodes, layouts, images) is recorded into Compose's internal tree. The function doesn't *return* UI — calling `Text(\"Hi\")` returns `Unit`; its effect is to **emit** a text node into the composition."},{t:"list",items:["**Only callable from other composables** — a normal function has no Composer to pass along, so the compiler rejects the call. (Same mechanism as `suspend` functions only being callable from coroutines — a hidden compiler-threaded parameter.)","**Must be fast and side-effect-free**: Compose may re-run your function many times (recomposition), skip it, or even abandon a run halfway and restart. Anything in the body must be safe to repeat — no network calls, no mutating globals. Side effects go through dedicated APIs (`LaunchedEffect` etc.).","**No guaranteed order**: sibling composables may execute in any order — never make one sibling depend on another having run first.","**Convention**: UI-emitting composables are named like nouns in PascalCase (`ProfileCard`), since they declare a thing rather than perform an action."]}]},{level:"junior",q:"Explain composition and recomposition. What triggers a recomposition?",a:[{t:"p",text:"**The concept**: when Compose runs your composable functions the first time, it builds the **composition** — an internal tree recording what every function emitted and with which inputs. This is Compose's memory of your UI. **Recomposition** is Compose updating that memory: re-running the affected functions when something they depend on changes."},{t:"p",text:'**What triggers it**: writing to an observable **state object** that some composable previously **read**. While your functions execute, Compose records every `State<T>` read (reading `count` inside `Text("Count: " + count)` silently subscribes that spot to `count`). When `count` is written, Compose looks up exactly which *recomposition scopes* read it and re-runs only those. So the rule is: **writes trigger, reads scope** — no manual subscriptions anywhere.'},{t:"p",text:"**The crucial refinement**: re-running a function doesn't mean re-rendering everything inside it. During the re-run, child composables whose parameters haven't changed are **skipped** entirely — Compose compares new inputs against what's stored in the composition. So a screen-level recomposition typically executes only a handful of small functions. Edge cases worth knowing: recomposition can be abandoned mid-way and restarted if state changes again quickly (why bodies must be repeat-safe), and plain variables (`var x = 0`) trigger nothing — only snapshot state types (`mutableStateOf`, `StateFlow` via `collectAsState`) are observable."}]},{level:"junior",q:"Why does a composable function return Unit? How does the UI actually get built?",a:[{t:"p",text:"**The concept**: in an object-oriented UI toolkit, a builder returns a widget you insert into a tree. Compose inverts this: a composable *emits* nodes into an implicit tree while it runs. The hidden `Composer` (injected by the compiler) is a cursor into an internal structure called the **slot table**; every composable call registers a group there containing its inputs and emitted nodes. So the 'return value' of your UI code is a side effect the runtime captures — the function itself has nothing useful to return, hence `Unit`."},{t:"p",text:"**Why design it that way**: it's what makes recomposition possible. Because the tree is keyed by *call position in code* (positional memoization), Compose can re-run your function later, walk the same positions, diff new inputs against stored ones, and update only mismatches — impossible if you handed it opaque widget objects. It also makes composition trivially compositional: any function can call any other, and the emitted trees nest automatically. One exception to mention: `remember { }` *does* return a value — but it's not emitting UI; it's reading/writing a slot in that same table."}]},{level:"junior",q:"What are the three phases of a Compose frame?",a:[{t:"p",text:"**The concept**: turning your code into pixels happens in three separable steps each frame — and Compose keeps them separable on purpose."},{t:"list",items:["**Composition** — *what to show*: your composable functions run (or re-run), producing the tree of layout nodes. The only phase executing your composable code.","**Layout** — *where to put it*: each node measures its children and places them. Single-pass by design: a parent measures each child exactly once, so deeply nested UIs don't explode the way nested `RelativeLayout`s did with double measurement.","**Draw** — *how it looks*: nodes render themselves to the canvas."]},{t:"p",text:"**Why you should care** (the follow-up they're fishing for): the phases can run independently. If a state change only affects position (a drag), Compose can skip composition and just re-layout; if it only affects pixels (alpha, color), just re-draw. *Where you read state decides which phases re-run* — reading a drag offset in composition recomposes every frame, while the lambda form `Modifier.offset { ... }` reads it during layout, and `graphicsLayer { alpha = ... }` reads during draw. Deferring reads like this is the standard fix for animation-driven recomposition storms."}]},{level:"junior",q:"Can you call a composable function from a normal function or a coroutine? Why not?",a:[{t:"p",text:"No — it's a compile-time error, and the reason is mechanical, not stylistic. The Compose compiler rewrites every composable to take a hidden `Composer` parameter (the cursor into the composition), plus bookkeeping calls around the body. A normal function has no Composer to supply, so there's nothing to pass — the call cannot be compiled. Only a composition context (started by `setContent`, or a parent composable) provides it."},{t:"p",text:"**The analogy that shows understanding**: it's exactly how `suspend` works — suspend functions need a hidden `Continuation`, so only coroutines can call them; composables need a hidden `Composer`, so only composables can call them. **Practical corollaries**: to run non-UI code *from* UI you go the other direction (event lambdas calling ViewModel functions); to start UI from a coroutine or callback you don't call composables — you change *state* the composition already observes. If you genuinely need composable logic outside `setContent` (e.g. producing state from Compose code), that's what specialized runtimes like Molecule do — a niche but impressive thing to name."}]},{level:"senior",q:"Explain the slot table and positional memoization. Why does call position matter?",a:[{t:"p",text:"**The concept**: the slot table is the data structure where the composition physically lives — a gap buffer (an array optimized for localized inserts, like text editors use) holding **groups**. Each composable call site becomes a group storing: a compiler-generated key for that *position in the source code*, the call's inputs as recorded last time, and slots for `remember`ed values. Recomposition is the Composer walking this table in step with re-executing your code, comparing stored inputs with fresh ones — equal inputs mean 'skip, reuse everything in this group'; changed inputs mean 're-execute this group, update its slots'."},{t:"p",text:"**Positional memoization** is the identity rule making that walk possible: a composable's identity is *where it was called from*, not what function it is. Two `Text()` calls at different source positions are two independent groups; state `remember`ed in one is invisible to the other. The same call inside an `if/else` gets *different* positions per branch — so switching branches destroys one group (its remembered state is forgotten) and creates the other."},{t:"p",text:"**Where it breaks and why `key()` exists**: in a loop, every iteration shares one source position, so Compose falls back to index-based identity. Insert an item at the top of a list and every item's index shifts — Compose thinks *every* item changed, recomposing all of them and, worse, migrating remembered state to the wrong items (item 0's expanded-flag now belongs to the new item 0). Wrapping items in `key(item.id) { }` (or the `key` parameter in lazy lists) replaces positional identity with your stable identity, so moved items keep their state and only genuinely new items compose. That failure story is the expected answer to 'why do lists need keys'."}]},{level:"senior",q:"What is a recomposition scope, and what exactly decides how much code re-runs when state changes?",a:[{t:"p",text:"**The concept**: Compose doesn't track state reads per-line — it tracks them per **recomposition scope**, which is (approximately) the body of each non-inline composable function and certain lambda arguments. While a scope executes, every snapshot-state read inside it is recorded against that scope. When the state is written, the runtime invalidates *the innermost scopes that read it* and schedules them for the next frame; re-running a scope re-evaluates its body, and unchanged-child skipping limits the damage below it."},{t:"p",text:"**The consequences that answer real interview probes**: (1) Reading state high up (in the screen composable) invalidates the big screen scope; passing values down as parameters — or better, passing *lambdas* that read the state where needed — keeps invalidation small. (2) **Inline composables like `Column`, `Row`, `Box` don't create their own scopes** — a read inside a `Column` body actually registers against the nearest non-inline parent, which is why 'I read state in the Column but the whole function recomposed' surprises people. (3) A scope that reads *no* state never recomposes — it exists in the table but is never invalidated. (4) `derivedStateOf` inserts an extra node so downstream scopes only invalidate when the *derived* value changes, not on every underlying write."},{t:"code",title:"Scope size in practice",code:`@Composable
fun Screen(vm: ViewModel) {
    val state by vm.uiState.collectAsStateWithLifecycle()
    // Read here -> the WHOLE Screen scope invalidates on any state change.

    Header(title = state.title)   // skipped if title unchanged
    Feed(items = state.items)     // skipped if items unchanged (and stable)
}
// Finer-grained alternative: pass () -> String and read inside Header,
// so only Header's scope tracks the read.`}]},{level:"senior",q:"Compose measurement is 'single-pass'. What does that mean, why does it matter, and how do intrinsics fit in?",a:[{t:"p",text:"**The problem it solves**: in the View system, a parent could measure children multiple times (e.g. `RelativeLayout` measuring twice to resolve constraints). Nest such parents and measurement work multiplies per level — 2^depth in the worst case — which is why 'flatten your hierarchy' was gospel and `ConstraintLayout` was invented. Compose forbids this: **during layout, a parent may measure each child exactly once** (the runtime literally throws if you measure twice). Result: measurement cost is linear in tree size, and deep composable nesting is free — a genuine architectural improvement worth stating as such."},{t:"p",text:"**How layout actually flows**: constraints (min/max width/height) travel *down* — parent tells child what sizes are acceptable; sizes travel *up* — child picks its size within them; then the parent *places* children. One pass, top-down then bottom-up."},{t:"p",text:"**Intrinsics are the escape hatch**: some layouts genuinely need to 'ask before measuring' — e.g. a Row where all children should match the tallest child's height (`Modifier.height(IntrinsicSize.Max)`). Intrinsic measurements let a parent query 'what height would you want at this width?' *without* it counting as the real measurement — under the hood the child's intrinsic functions compute estimates, then the single real measure still happens. They're more expensive (extra tree walk), so the guidance is: use intrinsics when needed, don't build everything on them. This — plus SubcomposeLayout for 'measure one child to decide another' — covers every 'but what if I need two passes?' follow-up."}]},{level:"senior",q:"Walk through what happens end-to-end from tapping a button to pixels changing on screen.",a:[{t:"list",items:["**1. Input**: the OS delivers the touch to the Activity's window → Compose's pointer-input system routes it through the node tree via hit testing → your `onClick` lambda runs (on the main thread).","**2. State write**: the lambda executes `count++` on a `MutableState`. The write goes through the **snapshot system** — Compose's MVCC-style state layer — which records that this state object changed and notifies observers when the snapshot is applied.","**3. Invalidation**: the runtime looks up which **recomposition scopes** read `count` during their last execution and marks them invalid. Nothing re-runs yet.","**4. Scheduling**: the **Recomposer** — a coroutine-based scheduler synced to the display via the frame clock (`withFrameNanos`) — picks up pending invalidations for the next vsync. Multiple writes in one frame coalesce into one recomposition.","**5. Recomposition**: on the frame, invalid scopes re-execute; the Composer diffs against the slot table, skipping children with unchanged inputs; the LayoutNode tree gets updated where output actually differed.","**6. Layout**: nodes whose measurements were invalidated re-measure (constraints down, sizes up, single pass) and re-place.","**7. Draw**: invalidated draw regions re-record their drawing; the render thread rasterizes to the screen buffer for that frame."]},{t:"p",text:"Points that earn the 'deep understanding' checkmark: state writes are **decoupled** from re-execution (write now, recompose at next frame — that's why incrementing state twice in one click yields one recomposition); each phase only touches what was invalidated at *its* level; and if the frame budget (~16ms at 60Hz) is blown by any of steps 5–7 on the main thread, that's your jank — profiling means finding *which* phase overran."}]},{level:"senior",q:"How does Compose exist as 'just a library' without OS support, and what are the architectural consequences?",a:[{t:"p",text:"**The design**: Compose bypasses the OS widget stack almost entirely. `setContent` installs exactly one Android View (`ComposeView` hosting an `AndroidComposeView`) into the Activity; from there down, Compose owns everything — its own node tree (LayoutNodes, not Views), its own layout engine, its own drawing (to the same low-level Canvas the framework uses), its own input dispatch and accessibility bridge (translating its semantics tree into the framework's accessibility APIs). The OS sees one big custom View drawing itself."},{t:"list",items:["**Ships and evolves independently**: new Compose features reach every supported API level via Gradle, not OS updates — contrast with View-system features gated on Android versions for two decades.","**The runtime/UI split**: `compose.runtime` (slot table, snapshots, Recomposer) is UI-agnostic tree management — `compose.ui` is just one client emitting LayoutNodes. That's why Compose Multiplatform exists (same runtime, different renderers per platform) and why Molecule can use the runtime with *no* UI at all.","**Interop is structural, not hacked**: since ComposeView is a View, it drops into XML layouts; since Compose can host arbitrary nodes, `AndroidView { }` embeds real Views (maps, WebViews) inside compositions.","**Costs to name for balance**: Compose re-implements what the OS gave Views for free — text editing, magnifier, autofill, a11y — each had to reach parity over time; and one giant custom View means View-based tooling (hierarchy viewers, some test frameworks) sees an opaque blob, which is why Compose has its own semantics-based testing/tooling stack."]}]},{level:"senior",q:'"Composable functions may run in any order, in parallel, be skipped, or run again." Explain each claim and what it forbids you from doing.',a:[{t:"list",items:["**Any order**: Compose only guarantees parent-before-child, not sibling order — e.g. all tabs of a screen may compose in whatever order serves priority. Forbidden pattern: sibling A writing something sibling B's body reads (a shared `var`, a list you append to) — B may run first, or A may be skipped.","**In parallel** (reserved, not currently default): the contract allows Compose to run composables on multiple threads to fill frames faster — one more reason bodies must touch no unsynchronized shared mutable state. Writing code as if this is already true keeps you future-proof and is the honest way to state it.","**Skipped**: if a composable's inputs are unchanged (and stable), Compose reuses its previous output without executing it. Forbidden pattern: relying on a composable's body executing for anything besides describing UI — logging, analytics, counters in bodies silently stop firing when skipping kicks in.","**Run again (restartable)**: recomposition re-executes bodies arbitrarily often, and can even *abandon* an in-progress recomposition and restart when state changes mid-frame (fast animations do this). Forbidden pattern: any non-idempotent work in the body — the same allocation twice is fine, the same POST request twice is a bug."]},{t:"p",text:"**The unifying rule to close with**: a composable body must be a **pure description** — same inputs, same emitted UI, no observable side effects. Everything effectful gets a controlled home instead: `remember` for surviving re-runs, `LaunchedEffect`/`DisposableEffect` for actual side effects keyed to lifecycle, state hoisting for shared data. The four claims aren't trivia — they're *why* those APIs exist."}]},{level:"junior",q:"What is declarative UI, and how is it different from imperative UI?",a:[{t:"p",text:"Declarative UI means you *describe what the UI should look like for a given state*, and the framework figures out how to make the screen match that description. Imperative UI means you *issue step-by-step commands to mutate the UI* — find a widget, set its text, change its visibility — and you're responsible for every transition between states."},{t:"code",title:"Imperative (Views) vs declarative (Compose)",code:`// IMPERATIVE: you command each change, and must handle every transition
if (isLoading) { progressBar.visibility = VISIBLE; textView.visibility = GONE }
else { progressBar.visibility = GONE; textView.visibility = VISIBLE; textView.text = name }

// DECLARATIVE: you describe the result; Compose computes the diff
@Composable fun Screen(isLoading: Boolean, name: String) {
    if (isLoading) CircularProgressIndicator() else Text(name)
}`},{t:"p",text:"The practical payoff: imperative UI bugs are almost always *forgotten transitions* — you showed the spinner but forgot to hide last time's error. Declarative UI can't have that class of bug, because each run describes the *whole* result from scratch; there's no stale leftover state to forget about. You go from 'a tree of objects I mutate' to 'UI = f(state)': the screen is simply the output of your function given the current state."},{t:"note",text:"Declarative = 'describe the UI for this state'; imperative = 'command each mutation'. The killer benefit: declarative UI eliminates the forgotten-transition bug class, because every render describes the whole screen fresh."}]},{level:"junior",q:"What is the Compose compiler plugin and what does it do?",a:[{t:"p",text:"The Compose compiler plugin is a Kotlin compiler plugin that *rewrites* your `@Composable` functions during compilation, transforming them from ordinary-looking Kotlin into code that participates in the Compose runtime. Without it, `@Composable` would just be a meaningless annotation — the plugin is what gives composables their special powers."},{t:"list",items:["**Injects the hidden `Composer` parameter** — every composable secretly receives a `$composer` argument that's the cursor into the composition (the slot table). This is why composables can only be called from other composables.","**Adds group start/end calls** — it wraps your code in `startRestartGroup`/`endRestartGroup` (and similar) calls that let the runtime track, skip, and restart pieces of your UI.","**Enables recomposition & skipping** — it generates the machinery that records which state each composable read and decides whether a composable can be skipped when its inputs are unchanged.","**Computes stability** — it analyzes types and marks composables `skippable`/`restartable` and parameters stable/unstable (feeding the performance model)."]},{t:"p",text:"The key insight is that Compose is *two* things: the compiler plugin (compile-time code transformation) and the runtime (the slot table, snapshots, Recomposer). The plugin is what makes `@Composable` a genuine language-level construct rather than a library convention — analogous to how the `suspend` keyword is compiler magic (threading a Continuation), the plugin threads a Composer."},{t:"note",text:"The Compose compiler plugin rewrites @Composable functions — injecting the hidden Composer, adding group calls, and enabling recomposition/skipping/stability. It's why @Composable is real language magic, not just an annotation (like `suspend`)."}]},{level:"junior",q:"What is the mental model 'UI = f(state)' and why does it matter?",a:[{t:"p",text:"'UI = f(state)' means the UI is a *pure function of state*: give the same state, get the same UI. Your composables take state as input and produce (emit) UI as output, with no hidden side effects. When the state changes, Compose re-runs the function with the new state and updates the screen to match — you never mutate the UI directly."},{t:"code",title:"The UI is derived, not mutated",code:`// State drives everything; the UI is just its rendering
var count by remember { mutableStateOf(0) }
Column {
    Text("Count: $count")               // derived from state
    Button(onClick = { count++ }) { }   // event changes state -> UI re-derives
}`},{t:"p",text:"Why it matters: it eliminates the *state-synchronization problem*. In imperative UI, data lives in your model *and* in the widgets, and they drift apart (the model says 5 but the label still shows 4 because you forgot to update it). When UI is a pure function of state, the widgets *can't* disagree with the model — they're computed from it. This makes UIs far easier to reason about (to know what's on screen, look at the state), test (assert on the state), and debug (there's one source of truth)."},{t:"note",text:"UI = f(state): the screen is a pure function of state, so widgets can never drift out of sync with your data (imperative UI's core bug). To know what's on screen, look at the state."}]},{level:"junior",q:"What does setContent do, and where does a Compose UI actually start?",a:[{t:"p",text:"`setContent { }` is the bridge from the Android world into Compose. Called in an Activity's `onCreate`, it installs a single `ComposeView` as the Activity's content and starts a *composition* — running the composable lambda you pass and building the initial UI tree. Everything Compose after that point is managed by Compose, not the Android View system."},{t:"code",title:"The entry point",code:`class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {              // creates the composition
            MyAppTheme {
                AppNavHost()      // your entire Compose UI tree
            }
        }
    }
}`},{t:"list",items:["**One ComposeView hosts everything** — under Compose there's still exactly *one* Android View (a `ComposeView`/`AndroidComposeView`); inside it, Compose manages its own tree of LayoutNodes, not Views.","**It starts the composition** — the runtime runs your composables, records the tree in the slot table, and drives recomposition from there.","**Interop both ways** — because a ComposeView is a normal View, you can also put Compose *inside* an XML layout, and put Views inside Compose via `AndroidView`."]},{t:"note",text:"`setContent` installs one ComposeView and starts the composition. There's a single hosting View; everything inside is Compose's own node tree. This is also why Compose↔View interop works both directions."}]},{level:"junior",q:"What is @Preview and why is it useful?",a:[{t:"p",text:"`@Preview` is an annotation you put on a composable function to render it directly in Android Studio's design pane — *without running the app on a device or emulator*. It lets you see and iterate on UI instantly, and preview multiple states/configurations side by side."},{t:"code",title:"Previewing states and themes",code:`@Preview(showBackground = true)
@Composable fun ProfileCardPreview() {
    MyAppTheme { ProfileCard(user = fakeUser) }   // pass fake data directly
}

@Preview(name = "Dark", uiMode = UI_MODE_NIGHT_YES)
@Preview(name = "Large font", fontScale = 2f)
@Composable fun ProfileCardVariants() { /* one function, many previews */ }`},{t:"list",items:["**Instant iteration** — no build-and-deploy cycle; see UI changes live.","**Preview many states** — loading/error/content, light/dark, different font scales and screen sizes, all at once.","**Requires stateless, data-driven composables** — you pass fake state directly, which is *why* state hoisting matters: a composable that fetches its own data or needs a real ViewModel can't be previewed easily. Previewability is a design pressure toward stateless UI."]},{t:"p",text:"The deeper point: `@Preview` rewards good architecture. A *stateless* composable (takes state + lambdas as parameters) previews trivially with fake data; a composable that reaches for a ViewModel or does I/O doesn't. So 'can I preview this?' is a useful design heuristic — it nudges you toward hoisted state and separating the stateful 'screen' composable from the stateless 'content' composable."},{t:"note",text:"@Preview renders composables in the IDE with fake data — instant iteration across states/themes/sizes. It only works well on *stateless* composables, so 'is it previewable?' is a design heuristic pushing you toward hoisted state."}]},{level:"junior",q:"Why can't you use a plain variable (var x = 0) to hold state in Compose?",a:[{t:"p",text:"A plain `var x = 0` doesn't work as Compose state for two independent reasons: it isn't *observable* (changing it doesn't tell Compose to recompose), and it doesn't *survive* recomposition (a new variable is created every time the function re-runs). You need both properties, which is why state must be `remember { mutableStateOf(...) }`."},{t:"code",title:"Why plain var fails",code:`@Composable fun Broken() {
    var count = 0                      // NOT observable, NOT remembered
    Button(onClick = { count++ }) {    // increments, but nothing recomposes...
        Text("Count: $count")          // ...and even if it did, count resets to 0
    }
}
// Correct:
var count by remember { mutableStateOf(0) }   // observable AND survives recomposition`},{t:"list",items:["**Not observable** — Compose only recomposes when a *snapshot state object* (`MutableState`) that a composable *read* changes. A plain `var` write is invisible to the runtime, so nothing re-renders.","**Not remembered** — even if it somehow triggered recomposition, the composable re-runs top to bottom, so `var count = 0` executes again and resets to 0. `remember` stores the value in the slot table so it persists across re-runs."]},{t:"note",text:"A plain `var` is neither observable (writes don't trigger recomposition) nor remembered (resets on every re-run). State needs both — `remember { mutableStateOf(...) }`: observable + survives recomposition."}]},{level:"junior",q:"How is Jetpack Compose different from Flutter and SwiftUI?",a:[{t:"p",text:"All three are modern *declarative* UI toolkits with the same core idea (describe UI as a function of state), so they feel similar to use. The differences are in *language, platform reach, and rendering*."},{t:"table",headers:["","Jetpack Compose","SwiftUI","Flutter"],rows:[["Language","Kotlin","Swift","Dart"],["Primary platform","Android (+ KMP: iOS/desktop/web)","Apple platforms","Cross-platform (one codebase)"],["Rendering","own rendering (Skia-based)","native UIKit/AppKit under the hood","own rendering (Skia/Impeller)"],["State model","snapshot state + recomposition","@State + view invalidation","setState / widgets rebuild"],["Interop","with Android Views (2-way)","with UIKit","platform channels"]]},{t:"p",text:"The conceptual overlap is huge — recomposition, `@State`/`mutableStateOf`, and `setState` are all 'the UI rebuilds when state changes'. For an Android engineer, the notable points are: Compose is *Kotlin* (so it composes with coroutines/Flow naturally), it interops both directions with the existing View system (crucial for incremental adoption), and via *Compose Multiplatform* it extends beyond Android to iOS/desktop/web — the same UI code running cross-platform, which is Compose's answer to Flutter's cross-platform pitch but using Kotlin."},{t:"note",text:"Compose, SwiftUI, and Flutter share the declarative 'UI = f(state)' model; they differ in language (Kotlin/Swift/Dart), platform reach, and rendering. Compose's edges: it's Kotlin (coroutines/Flow interop), 2-way View interop, and Compose Multiplatform for cross-platform."}]},{level:"junior",q:"Is recomposition expensive? What happens if you do heavy work in a composable body?",a:[{t:"p",text:"Recomposition itself is designed to be *cheap* — Compose skips composables whose inputs haven't changed, so a state change typically re-runs only a handful of small functions. But it's cheap *only if your composable bodies are cheap*. If you do heavy work directly in a composable body, that work re-runs on *every* recomposition, which can happen many times per second (during animations, scrolling, or rapid state changes) — turning cheap recomposition into a performance problem."},{t:"code",title:"Heavy work in the body vs cached",code:`@Composable fun Bad(items: List<Item>) {
    val sorted = items.sortedByDescending { it.score }   // re-sorts EVERY recomposition!
    LazyColumn { items(sorted) { ItemRow(it) } }
}
@Composable fun Good(items: List<Item>) {
    val sorted = remember(items) { items.sortedByDescending { it.score } }  // cached until items change
    LazyColumn { items(sorted) { ItemRow(it) } }
}`},{t:"list",items:["**Composable bodies must be fast and idempotent** — they can run many times per second, so expensive computation, allocations, or I/O in them is a bug.","**Cache derived work with `remember`** — `remember(key) { expensive() }` recomputes only when the key changes, not on every recomposition.","**Never do side effects in the body** — networking, logging, mutating shared state; those go in effect handlers (`LaunchedEffect`)."]},{t:"note",text:"Recomposition is cheap because of skipping — *if* your bodies are cheap. Heavy work (sorting, computation) in a body re-runs on every recomposition; cache it with `remember(key) { }`. Bodies must be fast, allocation-light, and side-effect-free."}]},{level:"senior",q:"What is the difference between the Compose runtime and Compose UI?",a:[{t:"p",text:"Compose is architecturally split into two independent parts. The **Compose runtime** (`androidx.compose.runtime`) is a *general-purpose tree-management engine* — it knows about the slot table, snapshot state, recomposition, and the Recomposer, but nothing about UI. **Compose UI** (`androidx.compose.ui`) is one *client* of that runtime that emits LayoutNodes, does measurement/layout/draw, and handles input. The runtime doesn't know what a `Text` or a pixel is."},{t:"list",items:["**Compose runtime** — the state/recomposition engine: slot table, `@Composable`, `remember`, snapshots, the Recomposer. UI-agnostic; it just manages a tree of 'things' efficiently.","**Compose UI** — the Android UI toolkit built on the runtime: LayoutNode, Modifier, measurement/layout/draw, pointer input, the Material components."]},{t:"p",text:"Why this separation matters: because the runtime is UI-agnostic, it can drive *non-UI* trees. **Compose Multiplatform** uses the same runtime with a different renderer to run on iOS/desktop/web. **Molecule** (Cash App) uses the runtime with *no UI at all* — to build `StateFlow`s from composable logic. So 'Compose' isn't one thing; it's a reusable reactive runtime plus a UI toolkit that happens to be its most famous client. Knowing this explains how Compose can exist beyond Android and beyond UI."},{t:"note",text:"Compose = runtime (UI-agnostic reactive tree engine: slot table, snapshots, recomposition) + Compose UI (one client emitting LayoutNodes). The split is why Compose Multiplatform (different renderer) and Molecule (no UI) exist."}]},{level:"senior",q:"What is the Composer, and what role does it play?",a:[{t:"p",text:"The `Composer` is the object the compiler plugin secretly passes into every composable function — it's the *cursor and controller* for building and updating the composition. When your composable runs, it's really calling methods on the Composer to record what it emits and remembers into the slot table, and to decide whether to execute or skip each piece."},{t:"list",items:["**It's the hidden parameter** — `@Composable fun Foo()` compiles to `fun Foo($composer: Composer)`. That's *why* composables can only be called from composables: they need a Composer, which only the runtime provides.","**It navigates the slot table** — the compiler inserts `startGroup`/`endGroup` calls around your code; those are the Composer moving through and diffing the slot table (positional memoization).","**It decides skip vs execute** — on recomposition, the Composer compares stored inputs against new ones and either re-executes a group or skips it (reusing the previous output).","**It manages `remember`** — `remember { }` is really the Composer reading or writing a slot in the table at the current position."]},{t:"p",text:"The analogy that lands: the Composer is to `@Composable` what the `Continuation` is to `suspend`. Both are hidden compiler-injected parameters that carry the machinery the feature needs — a Continuation threads the coroutine's resumption state; the Composer threads the composition's position and slot table. You never see it, but it's the reason the whole system works and the reason for the calling-context restrictions."},{t:"note",text:"The Composer is the hidden parameter (like suspend's Continuation) that's the cursor into the slot table — it navigates groups, decides skip-vs-execute, and manages remember. It's why composables need a composable calling context."}]},{level:"senior",q:"What is the Recomposer and how does it schedule recomposition?",a:[{t:"p",text:"The Recomposer is the *scheduler* that drives recomposition. When state changes, composables don't re-run immediately — instead, the invalidated recomposition scopes are collected, and the Recomposer schedules them to re-run on the *next frame*, synchronized with the display's refresh via the frame clock. It's the coroutine-based engine that turns 'state changed' into 'the right composables re-run at the right time'."},{t:"list",items:["**Collects invalidations** — when a snapshot state write happens, the runtime marks the scopes that read that state as invalid and hands them to the Recomposer.","**Batches per frame** — multiple state writes in one frame coalesce into a *single* recomposition pass, synchronized to `withFrameNanos` (the choreographer's frame clock). This is why incrementing state twice in one event produces one recomposition, not two.","**Runs on a coroutine** — the Recomposer is driven by a coroutine that awaits frame signals and applies pending invalidations, so recomposition is cooperative and frame-aligned rather than synchronous."]},{t:"p",text:"The practical consequences: state writes are *decoupled* from re-execution (write now, recompose next frame), which is why you can safely update state multiple times in an event handler and get one efficient recomposition. And because it's frame-synchronized, recomposition, layout, and draw all happen within the frame budget — the Recomposer is what keeps Compose's updates aligned with the display rather than firing chaotically on every state change."},{t:"note",text:"The Recomposer is the frame-synchronized scheduler: it collects invalidated scopes and re-runs them on the next frame (via the frame clock), batching multiple state writes into one recomposition. State writes are decoupled from re-execution — write now, recompose next frame."}]},{level:"senior",q:"How does Compose's tree diffing compare to React's virtual DOM?",a:[{t:"p",text:"Both solve 'efficiently update the UI when state changes', but with fundamentally different mechanisms. React builds a *virtual DOM* — a full in-memory tree — on each render, then *diffs* the new tree against the old to compute minimal real-DOM changes. Compose does *not* build and diff whole trees; instead it uses *positional memoization* via the slot table to update the existing tree in place, only re-running the composables whose inputs changed."},{t:"list",items:["**React** — re-render produces a new virtual tree; a diff algorithm compares old vs new; the reconciler applies the difference to the real DOM. Diffing is O(n) over the tree.","**Compose** — no separate virtual tree and no whole-tree diff. The slot table records each composable's inputs *by position*; on recomposition, Compose compares stored inputs to new ones at each position and *skips* unchanged subtrees, updating only the slots that changed. It's fine-grained invalidation, not tree diffing."]},{t:"p",text:"The consequence: Compose can skip re-running a composable *entirely* when its inputs are unchanged (it never even executes the function), whereas React re-runs the component function and relies on the diff to find nothing changed (mitigated by `memo`). Compose's model is closer to 'targeted invalidation' — the reads a composable made are its subscriptions, so only affected scopes re-run. This is why Compose emphasizes *stability* and *skipping* rather than a reconciliation/diff step: the efficiency comes from not re-running unchanged code in the first place, not from diffing the output afterward."},{t:"note",text:"React: build a virtual DOM and diff whole trees. Compose: no virtual tree/diff — positional memoization in the slot table skips unchanged composables entirely (they never re-run). Compose's efficiency is targeted invalidation, not output diffing."}]},{level:"senior",q:"When would you adopt Compose vs stay with the View system, and can they coexist?",a:[{t:"p",text:"Compose is Google's recommended toolkit for new UI, and it can coexist with Views both directions, so the realistic answer is almost never 'rewrite everything' — it's 'adopt Compose for new screens and incrementally, using interop, while Views remain fully supported'."},{t:"list",items:["**Use Compose for** — new features and screens, new apps, and places where custom UI is expensive in Views (Compose makes custom layouts/drawing far cheaper). It removes whole bug classes (state-sync, forgotten transitions) and is where the ecosystem is heading.","**Stay with Views when** — you have a large, stable View codebase that works (no need to rewrite), a team not yet ramped on Compose, or a specific edge case where a mature View library has no Compose equivalent yet.","**They coexist both ways** — `ComposeView` embeds Compose inside XML/Fragment screens (add Compose to an existing app screen by screen); `AndroidView` embeds a View (MapView, WebView, ad SDK) inside a Compose screen."]},{t:"p",text:"The honest trade-off: Compose eliminates state-sync bugs and makes custom UI dramatically cheaper, but has a learning curve (the recomposition mental model) and demands performance discipline (stability) that XML never asked of you. The migration strategy is incremental via interop — new screens in Compose, existing screens migrated when touched — not a big-bang rewrite. Saying 'rewrite everything in Compose' is the wrong answer; 'adopt incrementally using two-way interop, Views stay supported' is the right one."},{t:"note",text:"Adopt Compose for new UI and incrementally via interop (ComposeView in Views, AndroidView in Compose); never big-bang rewrite. Views stay fully supported. The trade-off: fewer bug classes + cheaper custom UI vs a learning curve + stability discipline."}]},{level:"senior",q:"What is a LayoutNode, and how does the composition relate to what's actually on screen?",a:[{t:"p",text:"There are *two* trees in Compose. The **composition** is the runtime's record of your composable calls (in the slot table) — it tracks what you emitted and what you remembered. The **LayoutNode tree** is the actual UI tree that gets measured, laid out, and drawn. Composables *emit* LayoutNodes into this second tree; the composition is the recipe, the LayoutNode tree is the rendered structure."},{t:"list",items:["**Composition (slot table)** — the runtime bookkeeping: which composables ran, their inputs, their `remember`ed values, recomposition scopes. Updated on recomposition.","**LayoutNode tree** — the concrete nodes that Compose UI measures (single-pass), places, and draws. When a composable like `Text` runs, it emits/updates a LayoutNode here.","**The link** — recomposition updates the composition, which in turn creates/updates/removes LayoutNodes; then the layout and draw phases operate on the LayoutNode tree."]},{t:"p",text:"This distinction explains the three phases cleanly: *composition* runs your code and updates the LayoutNode tree (what to show); *layout* measures and places those LayoutNodes (where); *draw* renders them (how). It also explains why not everything that recomposes causes layout — if a composable re-runs but emits the same LayoutNode structure, only the changed nodes re-measure/re-draw. Understanding that the composition (recipe) and the LayoutNode tree (rendered result) are distinct is key to reasoning about performance and the phase model."},{t:"note",text:"Two trees: the composition (slot table — the recipe of composable calls/state) and the LayoutNode tree (the concrete nodes measured/placed/drawn). Composables emit LayoutNodes; the three phases (composition→layout→draw) operate across these trees."}]},{level:"junior",q:"What is a stateful vs a stateless composable?",a:[{t:"p",text:"A *stateful* composable holds its own state internally (with `remember`), so callers just use it. A *stateless* composable holds no state — it receives its state as parameters and reports changes via callback lambdas, so the caller decides where state lives. The distinction is the foundation of state hoisting and reusable UI."},{t:"code",title:"Stateful vs stateless",code:`// STATEFUL: owns its state — convenient, but caller can't control/observe it
@Composable fun Counter() {
    var count by remember { mutableStateOf(0) }
    Button(onClick = { count++ }) { Text("$count") }
}
// STATELESS: state hoisted up — reusable, testable, previewable
@Composable fun Counter(count: Int, onIncrement: () -> Unit) {
    Button(onClick = onIncrement) { Text("$count") }
}`},{t:"list",items:["**Stateless is preferred for reusable components** — it's testable (pass any state), previewable (`@Preview` with fake data), and reusable in any context, because it makes no assumptions about where state lives.","**Stateful is fine for self-contained UI-only state** — an expandable card's open/closed flag that nobody else cares about can stay internal.","**The common pattern** — a stateful 'screen' composable that owns/collects state, wrapping a stateless 'content' composable that just renders it. Test and preview the content one."]},{t:"note",text:"Stateless composables (state via params, changes via callbacks) are reusable/testable/previewable; stateful ones own state internally (fine for private UI state). Idiom: a stateful screen composable wrapping a stateless content composable."}]},{level:"junior",q:"How do you organize a Compose screen — what's the typical structure?",a:[{t:"p",text:"The idiomatic structure splits a screen into a *stateful route composable* (gets the ViewModel, collects state, handles navigation) and a *stateless content composable* (takes state + lambdas and just renders). This separation makes the UI testable and previewable while keeping the ViewModel wiring in one thin place."},{t:"code",title:"The route + content pattern",code:`// STATEFUL route — the only place that touches the ViewModel
@Composable
fun ProfileRoute(viewModel: ProfileViewModel = hiltViewModel()) {
    val state by viewModel.uiState.collectAsStateWithLifecycle()
    ProfileScreen(
        state = state,
        onRefresh = viewModel::refresh,     // events forwarded up
        onEditClick = viewModel::onEdit,
    )
}

// STATELESS content — testable & previewable with fake data
@Composable
fun ProfileScreen(state: ProfileUiState, onRefresh: () -> Unit, onEditClick: () -> Unit) {
    when (state) {
        is ProfileUiState.Loading -> LoadingIndicator()
        is ProfileUiState.Content -> ProfileContent(state.user, onEditClick)
        is ProfileUiState.Error -> ErrorMessage(onRetry = onRefresh)
    }
}

@Preview @Composable
fun ProfileScreenPreview() { ProfileScreen(ProfileUiState.Content(fakeUser), {}, {}) }`},{t:"list",items:["**Route composable** — collects state (`collectAsStateWithLifecycle`), gets the ViewModel, handles navigation. Thin.","**Content composable** — stateless, takes `state` + event lambdas. This is what you preview and UI-test.","**State down, events up (UDF)** — state flows from ViewModel → route → content; events flow content → route → ViewModel."]},{t:"note",text:"Split each screen into a stateful *route* (ViewModel + state collection + nav) and a stateless *content* (state + lambdas). Test/preview the content; keep VM wiring in the thin route. This is UDF applied at the screen level."}]},{level:"senior",q:"What is 'donut-hole skipping' and why does 'the whole function re-runs' not mean slow?",a:[{t:"p",text:"'Donut-hole skipping' is Compose's ability to re-run an *outer* composable while *skipping* the inner composables whose inputs didn't change — and vice versa, to re-run just an inner lambda while the outer stays cached. The 'donut' is the part that recomposes; the 'hole' is the skipped part inside it. This is why 'the whole function re-runs on recomposition' doesn't mean the whole screen re-renders."},{t:"code",title:"The outer scope recomposes, inner children skip",code:`@Composable fun Screen(count: Int) {   // re-runs when count changes
    Header()                            // inputs unchanged -> SKIPPED (the hole)
    Text("Count: $count")               // reads count -> re-runs (the donut)
    ExpensiveList(items)                // items unchanged -> SKIPPED (the hole)
}`},{t:"list",items:["**Groups nest** — the slot table records composables as nested groups, so Compose can re-execute an outer group while skipping inner groups with unchanged, stable inputs.","**The inverse also happens** — `Column { }` content that reads changing state can recompose while the `Column` call itself doesn't; the reverse donut hole.","**Skipping requires stability** — a child is only skipped if its parameters are stable and unchanged (by value). Unstable params defeat the hole, forcing the child to re-run."]},{t:"p",text:"The practical takeaway: when someone worries 'my whole `Screen` function re-runs every time count changes', the answer is that re-running the *function* is cheap because its expensive children (`Header`, `ExpensiveList`) are *skipped* — only the small part that actually reads the changed state produces new output. This is precisely why *stability* matters so much: it's what lets Compose punch holes (skip children) rather than re-render everything. Break stability and the holes close, and now 'the whole function re-runs' *does* mean the whole subtree re-renders."},{t:"note",text:"Donut-hole skipping: an outer composable re-runs while its unchanged children are skipped (the holes). So 'the whole function re-runs' is cheap — the expensive children skip. This *depends on stability*; break it and the holes close."}]},{level:"junior",q:"How do you log or debug recompositions to see what's re-running?",a:[{t:"p",text:"You debug recompositions primarily with the **Layout Inspector** in Android Studio, which shows live *recomposition counts* and *skip counts* per composable as you interact with the app. A composable whose recomposition count climbs when it shouldn't is your problem; healthy composables show lots of skips and few recompositions."},{t:"list",items:["**Layout Inspector recomposition counts** — the primary tool: turn on recomposition counts, interact, and watch which composables re-run vs skip. High recompositions + low skips on something static = a stability/state-scoping problem.","**Compose Compiler Reports** — a compiler flag emits reports classifying each composable as `skippable`/`restartable` and each type as stable/unstable, *with reasons* — telling you *why* a composable isn't skipping.",'**A manual recomposition counter** — for a quick check, a `SideEffect { Log.d("recompose", "Foo ran") }` inside a composable logs each successful recomposition (useful for pinpointing one composable).',"**Composition tracing** — shows composable execution on the system trace timeline (Perfetto) with names, for seeing time spent composing during a janky frame."]},{t:"p",text:"The workflow: Layout Inspector tells you *which* composable recomposes too much; the Compiler Reports tell you *why* (which parameter type is unstable). Then you fix the root cause (stability, keys, deferred reads). And crucially — measure on a *release* build for real numbers, since debug builds recompose/perform differently."},{t:"note",text:"Debug recompositions with Layout Inspector (live recomposition/skip counts — find the culprit), Compose Compiler Reports (find *why* — which type is unstable), and a SideEffect log for a quick per-composable check. Workflow: which (Inspector) → why (Reports) → fix."}]}],_x={id:"fundamentals",title:"Compose Fundamentals",tagline:"What declarative UI really means, @Composable rules, composition & recomposition, the three phases, and the slot-table internals underneath it all.",tags:["Compose","Recomposition","Slot Table","Phases","Declarative UI"],content:Hx,qa:Kx},Gx=[{heading:"State in Compose — the starting point",blocks:[{t:"p",text:"In Compose, **state is any value that can change over time and that the UI should reflect**. Because Compose re-executes functions instead of mutating widgets, state must satisfy two needs at once: it must **survive re-execution** (a local `var` would reset every recomposition) and it must be **observable** (Compose must know when it changed so it can recompose). Two APIs split these jobs:"},{t:"list",items:["**`mutableStateOf(value)`** creates a `MutableState<T>` — an observable holder. Reading `.value` inside a composable silently subscribes that recomposition scope; writing `.value` triggers recomposition of subscribed scopes. This is the *observability* half.","**`remember { ... }`** stores a value in the composition (the slot table) so it survives recomposition — without it, `mutableStateOf` would create a *fresh* state object on every re-run and your UI would reset each frame. This is the *survival* half.","So the canonical local state line combines both: `var count by remember { mutableStateOf(0) }`."]},{t:"code",title:"Why both halves are needed",code:`@Composable
fun Counter() {
    // WRONG: no remember -> new state object every recomposition,
    // count visually stuck at 0 forever
    // var count by mutableStateOf(0)

    // WRONG: remember but not state -> survives recomposition,
    // but writing it notifies nobody; UI never updates
    // var count = remember { 0 }

    // RIGHT: survives + observable
    var count by remember { mutableStateOf(0) }

    Button(onClick = { count++ }) { Text("Clicked " + count) }
}`},{t:"list",items:["The `by` keyword is Kotlin **property delegation** — `by remember { mutableStateOf(0) }` unwraps `.value` reads/writes so `count` acts like a plain `Int`. Without `by`, you'd write `count.value` everywhere. Same object, nicer syntax.","`remember` keys: `remember(key1) { ... }` recalculates when the key changes — used for derived objects that must rebuild when an input changes.","`remember` is scoped to the **call site's position**: if the composable leaves the composition (its `if` branch stops being emitted), the remembered value is *forgotten*. State lifetime = presence in the tree."]}]},{heading:"rememberSaveable — surviving configuration change and process death",blocks:[{t:"p",text:"`remember` lives in the composition, and the composition lives inside the Activity — so **rotation destroys everything `remember`ed**. `rememberSaveable` fixes that: it additionally saves the value into the Activity's saved-instance-state `Bundle`, restoring it after configuration changes *and* system-initiated process death."},{t:"code",title:"rememberSaveable, with a custom Saver for non-Bundle types",code:`// primitives / Parcelable / Serializable: works out of the box
var query by rememberSaveable { mutableStateOf("") }

// custom types need a Saver — teach Compose how to (de)serialize:
data class Filter(val tag: String, val enabled: Boolean)

val FilterSaver = listSaver<Filter, Any>(
    save = { listOf(it.tag, it.enabled) },
    restore = { Filter(it[0] as String, it[1] as Boolean) }
)

var filter by rememberSaveable(stateSaver = FilterSaver) {
    mutableStateOf(Filter("all", true))
}`},{t:"list",items:["**Decision rule**: `remember` for transient visual state (animation scratch values, expanded flags you're fine losing); `rememberSaveable` for anything a user would be annoyed to lose on rotation (text input, selected tab, scroll — though scroll has its own `rememberLazyListState`, which is already saveable internally).","It uses the same `Bundle` machinery as `onSaveInstanceState` — so the same limits apply: small, Bundle-compatible data only. Screen *data* still belongs in the ViewModel/SavedStateHandle; `rememberSaveable` is for **UI-element state**.","Division of labor interviewers probe: **ViewModel** = screen state, survives config change in memory, needs SavedStateHandle for process death; **rememberSaveable** = widget-level state, survives both, but only Bundle-sized. They complement, not compete."]}]},{heading:"State hoisting — stateless vs stateful composables",blocks:[{t:"p",text:"**State hoisting** means moving state *up* out of a composable and passing it back down as parameters: instead of a composable owning `var text`, it receives `value: String` and `onValueChange: (String) -> Unit`. The composable becomes **stateless** — a pure function of its inputs — and the caller decides where the state actually lives."},{t:"code",title:"The hoisting pattern (this is exactly how TextField works)",code:`// STATEFUL: owns its state — convenient, but the parent can't
// read, control, or validate the text
@Composable
fun SearchBoxStateful() {
    var query by remember { mutableStateOf("") }
    TextField(value = query, onValueChange = { query = it })
}

// STATELESS (hoisted): state up, events up, value down
@Composable
fun SearchBox(
    query: String,
    onQueryChange: (String) -> Unit,
    modifier: Modifier = Modifier,
) {
    TextField(value = query, onValueChange = onQueryChange, modifier = modifier)
}

// Caller owns the state and can now share/validate/persist it:
@Composable
fun SearchScreen(viewModel: SearchViewModel) {
    val query by viewModel.query.collectAsStateWithLifecycle()
    SearchBox(query = query, onQueryChange = viewModel::onQueryChange)
}`},{t:"list",items:["**Why hoist**: single source of truth (no duplicated text living both in the widget and the ViewModel), reusability (a stateless composable works in any context), testability and previews (pass any state directly), and it's what makes UDF work end-to-end — state flows down, events flow up.","**How high to hoist**: to the **lowest common ancestor** of everyone who reads or writes it — no higher. UI-only state nobody else cares about (is this dropdown open?) can stay internal; state the screen logic needs goes to the ViewModel.","Compose's own components are all built hoisted (`TextField`, `Checkbox`, `Slider` — all take value + onChange). When you see `value`/`onValueChange` pairs, that's hoisting as an API convention.","**Plain state holder classes**: when a composable has several intertwined pieces of UI state + logic (e.g. scroll position + animation), bundle them in a `remember`ed class (`rememberScrollState()`, `rememberLazyListState()` are exactly this pattern) — a middle tier between loose `remember`s and the ViewModel."]}]},{heading:"derivedStateOf — computed state without extra recomposition",blocks:[{t:"p",text:`**The problem**: you have state that changes constantly (scroll position, every keystroke) but the UI only cares about a *derived* fact that changes rarely ("is the user past item 3?", "is the input valid?"). If you compute the fact inline, the scope recomposes on **every** underlying change even when the fact's answer is identical.`},{t:"code",title:"derivedStateOf: recompose only when the ANSWER changes",code:`val listState = rememberLazyListState()

// BAD: firstVisibleItemIndex changes every scroll frame ->
// this scope recomposes continuously while scrolling
val showButtonBad = listState.firstVisibleItemIndex > 3

// GOOD: derivedStateOf re-evaluates on every scroll, but only
// INVALIDATES readers when the Boolean result actually flips
val showButton by remember {
    derivedStateOf { listState.firstVisibleItemIndex > 3 }
}

AnimatedVisibility(visible = showButton) { ScrollToTopButton() }`},{t:"list",items:["Mechanics: `derivedStateOf` creates a state node that observes whatever snapshot state its lambda reads. The lambda re-runs on each underlying change, but downstream scopes are invalidated **only when the computed result differs** (by equality) from last time — it's a change-frequency filter.","**When to use**: input changes far more often than the output — scroll thresholds, validation flags, filtered snapshots of fast-changing sources. **When NOT to**: output changes as often as input (plain `remember(key)` is cheaper — derivedStateOf adds tracking overhead), or the inputs aren't snapshot state at all (it can't observe plain parameters — a classic misuse: `derivedStateOf` over a function parameter observes nothing).","Wrap it in `remember { }` — forgetting that recreates the derived node each recomposition, silently defeating it (and it's the most common derivedStateOf bug in review)."]}]},{heading:"Collecting external state: ViewModels, Flows, and collectAsStateWithLifecycle",blocks:[{t:"p",text:"Screen-level state usually lives in a ViewModel as `StateFlow`. Compose bridges reactive streams into snapshot state with collect-as-state adapters — each emission writes a `State<T>` that composables read like any other state:"},{t:"code",title:"The standard screen wiring",code:`@Composable
fun ProfileScreen(viewModel: ProfileViewModel = hiltViewModel()) {
    // Lifecycle-aware: collection stops at ON_STOP, restarts at ON_START
    val state by viewModel.uiState.collectAsStateWithLifecycle()

    ProfileContent(
        state = state,
        onRefresh = viewModel::refresh,
    )
}

@Composable // stateless, previewable, reusable
fun ProfileContent(state: ProfileUiState, onRefresh: () -> Unit) { /* ... */ }`},{t:"list",items:["**`collectAsStateWithLifecycle` vs `collectAsState`**: the lifecycle variant cancels collection when the app is backgrounded (via `repeatOnLifecycle(STARTED)`), pairing with `stateIn(WhileSubscribed(5000))` to stop upstream work too. Plain `collectAsState` keeps collecting in the background — use it only where lifecycle doesn't apply (previews, desktop).","Pattern: **route composable** (gets the ViewModel, collects state) + **content composable** (stateless, takes state + lambdas). Previews and tests target the content one.","You can also keep Compose state *in* the ViewModel (`var uiState by mutableStateOf(...)` with a private setter) — valid, Compose-runtime-dependent; `StateFlow` keeps the ViewModel toolkit-agnostic (and KMP/iOS-friendly). Know both, prefer StateFlow when sharing matters."]}]},{heading:"Under the hood: the snapshot system",blocks:[{t:"p",text:"How does reading `.value` *subscribe* a scope, and how are concurrent changes safe? The answer is Compose's **snapshot state system** — an MVCC (multi-version concurrency control) design, the same family of technique databases use:"},{t:"list",items:["Every snapshot-state object keeps a **linked list of value records**, each tagged with the snapshot id that wrote it. A **snapshot** is a consistent view of all state at a point in time — reading inside snapshot N sees the newest record valid for N, regardless of later writes elsewhere.","**Read tracking**: snapshots can carry read observers. During composition, Compose opens a snapshot whose observer records every state object read, per recomposition scope — that recording *is* the subscription. No listeners, no annotations: reading is subscribing.","**Write tracking & application**: writes inside a mutable snapshot create new records invisible to others until the snapshot is **applied** (atomically, like a commit). On apply, registered apply-observers receive the changed objects — the Recomposer intersects them with recorded reads and invalidates exactly the affected scopes.","**Why MVCC**: recomposition can run concurrently with new writes (even from other threads) without tearing — each computation sees an isolated, consistent world; conflicting parallel snapshots detect collision at apply time. This is also what makes `mutableStateOf` safe to write from any thread.","`snapshotFlow { }` bridges the reverse direction: it observes snapshot reads inside its lambda and emits into a cold Flow whenever they change — snapshot state → Flow, the mirror of `collectAsState`."]},{t:"note",text:`You rarely *say* 'MVCC' in an interview unprompted — but when asked "how does Compose know what to recompose?", the precise answer is: "state reads are recorded per scope by snapshot read-observers during composition; state writes notify apply-observers with the changed objects; the Recomposer intersects the two sets and invalidates only scopes that read what changed." That one sentence is the whole system.`}]},{heading:"State pitfalls checklist (the bugs interviewers describe and ask you to diagnose)",blocks:[{t:"list",items:["**`mutableStateOf` without `remember`** → state resets every recomposition; UI stuck at initial value. (Compiler lint usually flags it.)","**`remember` without `mutableStateOf`** → value survives but changes are invisible; UI never updates.","**Mutating a remembered mutable object** (`remember { mutableListOf() }`, then `list.add(x)`) → no recomposition: the *reference* never changed and the list isn't observable. Fix: `mutableStateListOf()` / `mutableStateMapOf()` (element-level observable) or immutable copies in a `MutableState`.","**Expecting recomposition from plain `var`s or normal Flows** — only snapshot state and collect-as-state adapters are observable.","**State captured in stale lambdas**: a long-lived lambda (callback registered once) capturing a state *value* sees the old value forever — capture the `State` object or use `rememberUpdatedState` (Side Effects topic).","**Remembered state keyed to nothing when it should follow an input**: `remember { mutableStateOf(item.isFavorite) }` keeps the *first* item's flag when `item` changes — needs `remember(item.id)`.","**Losing state on conditional structure**: moving a composable between `if` branches changes its slot-table position → remembered state is forgotten; use `key()` or restructure so identity is stable.","**Backwards writes** — writing state that the *same* composition pass already read (e.g. writing in the body based on a read above) → infinite recomposition loop; Compose logs a warning. Writes belong in event handlers/effects."]}]}],Jx=[{level:"junior",q:"What does remember do, and why do we need it if we already have mutableStateOf?",a:[{t:"p",text:"**The concept**: Compose re-executes your composable function on every recomposition — so any local variable created in the body is created *again*, from scratch, each time. `remember { ... }` opts a value out of that: it runs its lambda once, stores the result in the composition (the slot table), and on every later execution returns the stored value instead of recomputing."},{t:"p",text:"**Why both are needed**: `mutableStateOf` and `remember` solve two different problems. `mutableStateOf(0)` makes a value *observable* — Compose recomposes readers when it changes. But if you write `var count by mutableStateOf(0)` without `remember`, every recomposition creates a **brand-new state object reset to 0** — you increment it, recomposition happens, and the new execution builds a fresh zero. The UI appears frozen at the initial value. `remember` keeps the *same* state object alive across executions. Conversely, `remember { 0 }` alone survives but notifies nobody on change. So: **remember = survival, mutableStateOf = observability**, and UI state needs both: `remember { mutableStateOf(0) }`."},{t:"p",text:"**Edge cases worth adding**: `remember(key)` re-runs the lambda when the key changes (for state that must reset when an input changes); and remembered values die when the composable **leaves the composition** (its `if` branch stops being emitted, it's scrolled far away in a lazy list) — remember-lifetime equals presence in the tree, which is why rotation (which rebuilds the whole composition) also wipes it, leading to `rememberSaveable`."}]},{level:"junior",q:"What is the difference between remember and rememberSaveable?",a:[{t:"p",text:"**The concept first**: `remember` stores values in the composition — and the composition lives inside the Activity. A configuration change (rotation, dark mode, resize) destroys and recreates the Activity, taking the entire composition and everything remembered with it. So `remember` survives *recomposition*, but not *recreation*."},{t:"p",text:"`rememberSaveable` does everything `remember` does, **plus** writes the value into the saved-instance-state `Bundle` (the same mechanism as `onSaveInstanceState`). After rotation — and even after system-initiated **process death** — the value is restored. Requirements: the type must be Bundle-compatible (primitives, String, Parcelable, Serializable), or you supply a custom `Saver` telling Compose how to convert it to something saveable."},{t:"p",text:"**How to choose**: use `rememberSaveable` for anything a user would be annoyed to lose on rotating the phone — typed text, selected tab, checkbox choices. Use `remember` for transient/visual scratch state that's fine resetting. And know the boundary: screen *data* (the loaded list, the user object) belongs in the ViewModel, not in either — `rememberSaveable` is for small widget-level state, and stuffing big data into it risks `TransactionTooLargeException` like any Bundle abuse."}]},{level:"junior",q:"What is state hoisting and why is it recommended?",a:[{t:"p",text:"**The concept**: a composable can either *own* its state internally (`var text by remember {...}` inside it) or have the state **hoisted** — moved up to the caller, with the composable receiving the current `value` as a parameter and reporting changes through an `onValueChange` lambda. The composable becomes *stateless*: a pure function of its inputs that holds nothing itself. This is literally how Compose's own components work — `TextField(value, onValueChange)`, `Checkbox(checked, onCheckedChange)` — none of them store their own value."},{t:"list",items:["**Single source of truth**: without hoisting, the text lives both inside the widget and in your ViewModel, and they *will* drift apart. Hoisted, exactly one place owns it.","**The parent gains control**: it can validate input, transform it (force uppercase), share it between widgets, persist it — impossible when the state is locked inside a child.","**Reusability + previews + tests**: a stateless composable can be dropped into any screen, previewed with any state (`@Preview` can't provide ViewModels), and tested by passing values directly.","**It completes UDF**: state flows down as parameters, events flow up as lambdas — same pattern as ViewModel↔UI, applied recursively down the tree."]},{t:"p",text:`**The judgment part**: don't hoist dogmatically. Hoist to the **lowest common ancestor** of everything that reads or writes the state — and no higher. A dropdown's open/closed flag that nobody else cares about is fine staying internal; hoisting it to the ViewModel is noise. The interview-ready phrasing: "internal state for UI-only concerns, hoisted state the moment a second party needs it."`}]},{level:"junior",q:"Why doesn't the UI update when I add items to a remember { mutableListOf() }?",a:[{t:"p",text:"**The concept behind the bug**: Compose doesn't watch your objects' *contents* — it watches **snapshot state objects**. `remember { mutableListOf<String>() }` stores a plain `ArrayList` in the composition. When you call `list.add(item)`, the list mutates internally, but nothing observable happened from Compose's perspective: no `State` was written, so no scope is invalidated, so nothing recomposes. The data is there — the screen just never re-reads it."},{t:"code",title:"Three correct alternatives",code:`// 1) Observable collection — element operations trigger recomposition
val items = remember { mutableStateListOf<String>() }
items.add("new")            // recomposes readers

// 2) Immutable value in observable holder — replace, don't mutate
var items by remember { mutableStateOf(listOf<String>()) }
items = items + "new"       // new list assigned -> write -> recomposes

// 3) Real app: state lives in the ViewModel as StateFlow<List<T>>
//    and the UI just collects it`},{t:"p",text:"**The deeper lesson interviewers want**: mutation vs replacement. Compose's model favors **immutable values replaced through observable writes** — replacement is what the snapshot system can see, and equality of immutable values is what powers skipping. `mutableStateListOf` exists as the escape hatch for genuinely list-shaped local state, but if you find yourself mutating shared objects hoping the UI notices, the design is fighting the framework."}]},{level:"junior",q:"What does the 'by' keyword do in `var count by remember { mutableStateOf(0) }`?",a:[{t:"p",text:"**The concept**: `remember { mutableStateOf(0) }` gives you a `MutableState<Int>` — a *box* with a `.value` property. Without any sugar, you'd read `count.value` and write `count.value = 5` everywhere. Kotlin's **property delegation** (`by`) lets an object handle a property's get/set: `MutableState` provides `getValue`/`setValue` operator functions, so `var count by ...` makes plain-looking `count` reads and writes secretly go through `.value`."},{t:"p",text:"**What it changes — and doesn't**: purely syntax; the same state object is involved, the same read-tracking and recomposition happen (reading `count` still registers the read; assigning still triggers invalidation). Two practical notes: use `=` (not `by`) when you want to pass the `State` object itself around — `val countState = remember { mutableStateOf(0) }` — for example to hand a stable reference into a lambda; and the delegate imports (`androidx.compose.runtime.getValue/setValue`) are needed for `by` to compile, the source of a classic head-scratcher error for newcomers."}]},{level:"junior",q:"collectAsState vs collectAsStateWithLifecycle — which one and why?",a:[{t:"p",text:"**The concept**: a `StateFlow` in your ViewModel isn't something composables can read directly — Compose needs snapshot state. Both functions bridge this: they collect the flow inside the composition and mirror each emission into a `State<T>`, so your composable recomposes on new values. The difference is *when collection is active*."},{t:"p",text:"`collectAsState()` collects for as long as the composable is in the composition — **including while the app is in the background**, because backgrounding doesn't dispose the composition. `collectAsStateWithLifecycle()` additionally respects the lifecycle: it uses `repeatOnLifecycle(STARTED)` internally, so collection **stops at ON_STOP and restarts at ON_START**. Combined with the standard `stateIn(SharingStarted.WhileSubscribed(5000))` in the ViewModel, backgrounding the app stops not just the collection but the whole upstream chain (DB observation, location, sockets) — no wasted work while invisible."},{t:"p",text:'**Answer**: on Android, default to `collectAsStateWithLifecycle` (it\'s in `lifecycle-runtime-compose`). `collectAsState` remains for contexts without an Android lifecycle — previews, desktop/multiplatform targets. If asked "what actually goes wrong with plain collectAsState?": a flow of, say, GPS updates keeps the location pipeline alive with the app minimized — battery drain and pointless processing until the process is killed.'}]},{level:"junior",q:"Where should state live — the composable, a state holder, or the ViewModel?",a:[{t:"p",text:"**The concept**: Compose apps have a spectrum of state owners, and the skill is matching state to the cheapest owner that satisfies its consumers. The question to ask of every piece of state: *who needs to read or write this, and how long must it live?*"},{t:"list",items:["**Inside the composable** (`remember`/`rememberSaveable`): UI-element state nobody else cares about — is the dropdown expanded, current text-field selection, animation scratch. Dies with the composable, and that's correct.","**Plain state-holder class** (`remember`ed, like `rememberLazyListState()`): several intertwined pieces of *UI logic* — scroll position + 'show jump button', drag state + snap targets. Still UI-lifetime, but organized and reusable; this is Compose's own pattern for every `rememberXxxState` API.","**ViewModel**: *screen* state and anything involving business/data — loaded content, loading/error flags, user input that must survive configuration change or feed validation. Survives rotation, integrates SavedStateHandle for process death, testable on the JVM."]},{t:"p",text:"**The boundary rule**: business state never lives in `remember`; pure UI mechanics never need a ViewModel. And when one piece of state is needed at two levels (query text shown in the field *and* driving search), hoist it to the higher owner — the ViewModel — and pass it down; duplicating it at both levels is the drift bug hoisting exists to prevent."}]},{level:"senior",q:"Explain derivedStateOf: what problem it solves, how it works, and when it's the wrong tool.",a:[{t:"p",text:"**The problem**: recomposition frequency is driven by how often *read state changes* — not by how often the UI actually needs to look different. Scroll offset changes every frame while scrolling; if your composable reads `listState.firstVisibleItemIndex > 3`, the scope recomposes every frame even though the boolean answer flips maybe twice in the whole gesture. You're paying per-input-change for a per-output-change need."},{t:"p",text:"**How it works**: `derivedStateOf { ... }` creates an intermediate state node. Its lambda runs whenever any snapshot state it reads changes (cheap — just the lambda, not your UI), and the node compares the new result to the previous one by equality. **Only when the result differs** does it invalidate scopes that read the derived value. It's a change-frequency filter between fast inputs and slow outputs — conceptually the snapshot-world version of `distinctUntilChanged`."},{t:"list",items:["**Wrong tool #1 — output changes as often as input**: `derivedStateOf { a + b }` where the sum changes on every write adds tracking overhead and filters nothing; plain inline computation or `remember(a, b)` is cheaper.","**Wrong tool #2 — inputs aren't snapshot state**: it only observes snapshot reads. Deriving from a plain function parameter observes nothing and silently never updates — the parameter changing causes recomposition anyway, so use `remember(param) { compute(param) }` instead. This distinction (parameter changes → remember with keys; state-object changes at higher frequency → derivedStateOf) is the crispest way to show mastery.","**Bug to name**: forgetting to wrap it in `remember { derivedStateOf { ... } }` — recreated every recomposition, the comparison history is lost and the optimization silently vanishes."]}]},{level:"senior",q:"How does Compose's snapshot state system actually work? (How does reading state subscribe, and why is it thread-safe?)",a:[{t:"p",text:"**The design**: Compose state is built on **snapshots** — an MVCC (multi-version concurrency control) scheme, the same family databases use for isolation. Each `mutableStateOf` object internally keeps a *list of value records*, each stamped with the id of the snapshot that wrote it. Code always runs *inside* some snapshot, and reading a state object returns the newest record valid for that snapshot — so every computation sees one consistent version of the world, unaffected by writes happening elsewhere mid-flight."},{t:"p",text:"**How reading subscribes**: snapshots support **read observers**. When the Recomposer runs composition, it opens a snapshot whose read observer logs every state object read, attributed to the currently-executing recomposition scope. That log *is* the subscription table — no listeners registered by you, reading is subscribing. **How writing notifies**: writes in a mutable snapshot create new records invisible to others until the snapshot is atomically **applied** (a commit); apply-observers then receive the set of changed objects, and the Recomposer intersects 'objects changed' with 'scopes that read them' to invalidate exactly the right scopes for the next frame."},{t:"p",text:"**Why thread-safe**: because writes go into isolated snapshots and only become visible at atomic apply, a background thread writing `mutableStateOf` can't tear a half-finished composition — the composition's snapshot simply doesn't see the write until its next run. Concurrent conflicting snapshots detect collisions at apply time (optimistic concurrency). Practical corollaries worth naming: writing snapshot state from any thread is safe (unusual for UI frameworks); `Snapshot.withMutableSnapshot { }` batches multi-write consistency; and `snapshotFlow { }` is the same read-observer trick repackaged to emit into a Flow — the bridge *out* of the snapshot world, mirroring `collectAsState` bridging in."}]},{level:"senior",q:"A callback registered once keeps seeing stale state. Explain why lambdas capture stale values and the fixes.",a:[{t:"p",text:"**The concept**: every recomposition creates *new* lambda instances capturing the *current* values. That's fine for lambdas Compose re-reads each time (like `onClick` passed to Button — the newest lambda replaces the old in the composition). The trap is a lambda handed to something **long-lived that won't be replaced**: a broadcast receiver registered in a `DisposableEffect(Unit)`, a delayed coroutine in `LaunchedEffect(Unit)`, a listener on a singleton. That lambda was created during one particular composition and captured that moment's values — later recompositions create new lambdas, but the long-lived consumer still holds the original. It's not Compose magic misbehaving; it's ordinary closure capture meeting an unusual re-execution model."},{t:"code",title:"The bug and the idiomatic fix",code:`@Composable
fun Timer(onTimeout: () -> Unit) {
    // BUG: effect keyed to Unit runs once, capturing the FIRST onTimeout.
    // If the parent recomposes with a new handler, timeout calls the old one.
    LaunchedEffect(Unit) {
        delay(60_000)
        onTimeout()               // stale!
    }

    // FIX: rememberUpdatedState gives a stable State box whose
    // .value is refreshed every recomposition
    val currentOnTimeout by rememberUpdatedState(onTimeout)
    LaunchedEffect(Unit) {
        delay(60_000)
        currentOnTimeout()        // always the latest handler
    }
}`},{t:"list",items:["**Fix 1 — `rememberUpdatedState`**: capture a stable `State` wrapper instead of the value; the wrapper's `.value` is written on each recomposition, so the old closure dereferences fresh data. Use when the effect must *not* restart (a timer, a registered receiver).","**Fix 2 — key the effect on the value**: `LaunchedEffect(onTimeout)` cancels and relaunches with the new capture. Correct when restarting is acceptable/desired; wrong for the one-shot-timer case (it would reset the countdown).","**The decision rule to state**: 'should this ongoing work restart when the value changes?' Yes → put it in the keys. No → `rememberUpdatedState`. That sentence is the whole topic."]}]},{level:"senior",q:"What is a 'backwards write' and what happens if you write state during composition?",a:[{t:"p",text:"**The concept**: composition is supposed to be a *read-only derivation* — run functions, read state, emit UI. A **backwards write** breaks the direction: writing a state object during composition that composition (the same pass or an enclosing scope) has already read. The result is a self-invalidating loop: the pass reads X, then writes X → the write invalidates the scope that read it → next frame recomposes → reads and writes again → an **infinite recomposition loop**, burning a full extra frame of work forever. Compose detects simple cases and logs a 'backwards write' warning, but the loop still runs."},{t:"code",title:"Bug vs the correct homes for the logic",code:`@Composable
fun Bad(items: List<Item>) {
    var count by remember { mutableStateOf(0) }
    count = items.size          // WRITE during composition of state read below
    Text("Count: " + count)     // READ -> loop
}

// If it's derivable — don't store it at all:
@Composable
fun Good(items: List<Item>) {
    Text("Count: " + items.size)
}

// If it must react to a change — that's an effect, not composition:
LaunchedEffect(items) { analytics.log(items.size) }`},{t:"p",text:"**The rule and its exceptions**: writes belong in **event handlers** (clicks) and **effects** (`LaunchedEffect`, `SideEffect`) — never in the composable body. Two refinements that show depth: initializing state *inside* `remember { mutableStateOf(initial) }` is fine (it's construction, not a tracked write to observed state); and 'derive, don't sync' — most backwards writes are people caching something computable, and the real fix is deleting the stored state, not relocating the write. If genuine derived-state caching is needed, that's `remember(key)` or `derivedStateOf` doing it safely."}]},{level:"senior",q:"Compare holding screen state as StateFlow in the ViewModel vs mutableStateOf in the ViewModel. Trade-offs?",a:[{t:"p",text:"**Both are legitimate** — this is a real design choice, not a trick. `var uiState by mutableStateOf(UiState())` (private set) in a ViewModel works because snapshot state is observable from composables directly; Google's own samples have used it. `MutableStateFlow` + `asStateFlow` is the more traditional shape. What differs:"},{t:"list",items:["**Toolkit coupling**: `mutableStateOf` ties the ViewModel to the Compose *runtime* (a dependency, though not the UI toolkit). StateFlow is pure kotlinx — matters enormously in **KMP** (iOS consumes StateFlow via SKIE/adapters; snapshot state has no Swift story) and if any non-Compose consumer exists.","**Operators & derivation**: StateFlow plugs into the whole Flow world — `combine`, `debounce`, `flatMapLatest`, `stateIn` chains from repositories. Snapshot state has `snapshotFlow` as a bridge but no native operator algebra.","**Lifecycle-aware collection**: StateFlow + `collectAsStateWithLifecycle` + `WhileSubscribed` gives automatic stop-upstream-when-backgrounded. Compose state read directly has no such notion — the state just sits there (fine for cheap state, a non-feature for expensive upstreams).","**Ergonomics**: mutableStateOf wins on simplicity — no `.update {}`, no initial-value duplication, granular per-field states are trivial. Also no equality-conflation surprises (though StateFlow's skip-equal behavior is usually what you want for state).","**Testing**: StateFlow tests with Turbine/standard Flow tooling; Compose-state ViewModels test fine too but pull the Compose runtime into JVM tests."]},{t:"p",text:"**Recommendation to give**: StateFlow as the default — especially for KMP-bound code (which, for an Android/KMP engineer, is decisive) and screens with derived/combined streams; `mutableStateOf` in ViewModels is a pragmatic choice for Compose-only Android apps that value ergonomics. Either way, expose read-only, mutate only inside the ViewModel — the UDF contract is the invariant; the container is negotiable."}]},{level:"junior",q:"What is 'state' in Compose, fundamentally?",a:[{t:"p",text:"State in Compose is any value that can change over time and that the UI should reflect. Because Compose re-runs functions instead of mutating widgets, state must satisfy two needs at once: it must *survive* recomposition (a plain local variable would reset every re-run), and it must be *observable* (Compose must know when it changed so it can recompose the parts that read it)."},{t:"list",items:["**`mutableStateOf(value)`** provides the *observability* — it creates a `MutableState<T>` holder; reading `.value` subscribes the current composable, writing `.value` triggers recomposition of subscribers.","**`remember { }`** provides the *survival* — it stores the value in the slot table so it persists across recompositions.","**Together** — `var x by remember { mutableStateOf(0) }` gives you state that both survives re-runs and triggers recomposition on change."]},{t:"p",text:"The mental model 'UI = f(state)' makes state central: the whole screen is derived from state, so what state you have and where it lives *is* your app's design. Reading state inside a composable is how it 'subscribes' — there's no manual observer registration. Understanding that state needs both survival (`remember`) and observability (`mutableStateOf`) is the foundation of everything else in Compose state."},{t:"note",text:"State = a changing value the UI reflects, needing *survival* (remember) + *observability* (mutableStateOf). Reading it subscribes the composable; writing it recomposes subscribers. UI = f(state), so state is your app's design."}]},{level:"junior",q:"What is MutableState and the State<T> interface?",a:[{t:"p",text:"`State<T>` is a read-only holder of an observable value — reading its `.value` from a composable subscribes that composable to changes. `MutableState<T>` extends it with a settable `.value`, so you can write to it. `mutableStateOf()` returns a `MutableState`; several APIs (like `collectAsState`, `derivedStateOf`) return a read-only `State`."},{t:"code",title:"State vs MutableState",code:`val count: MutableState<Int> = mutableStateOf(0)   // read + write
count.value = 5                                     // write -> recomposes readers
val len: State<Int> = derivedStateOf { count.value * 2 }  // read-only

// The 'by' delegate unwraps .value:
var c by remember { mutableStateOf(0) }   // 'c' reads/writes .value transparently
val readOnly by remember { derivedStateOf { c * 2 } }`},{t:"list",items:["**`State<T>`** — read-only observable value (`collectAsState`, `derivedStateOf` return this). Reading `.value` subscribes.","**`MutableState<T>`** — read/write. `mutableStateOf` returns this.","**Exposing read-only** — you often store a `MutableState` privately but expose it as `State` so callers can read but not write (encapsulation, single-writer)."]},{t:"note",text:"State<T> = read-only observable value (reading .value subscribes); MutableState<T> adds a settable .value. mutableStateOf returns MutableState; derivedStateOf/collectAsState return read-only State. Expose State to prevent outside writes."}]},{level:"junior",q:"What are mutableStateListOf and mutableStateMapOf, and when do you use them?",a:[{t:"p",text:"`mutableStateListOf()` and `mutableStateMapOf()` are *observable collections* — like `MutableList`/`MutableMap`, but Compose recomposes readers when you add, remove, or change elements. They solve the problem that a plain `remember { mutableListOf() }` doesn't trigger recomposition when you mutate it (because the list reference never changed and the list isn't observable)."},{t:"code",title:"Observable collections vs the broken plain version",code:`// BROKEN: mutating a plain list doesn't recompose (reference unchanged, not observable)
val items = remember { mutableListOf<String>() }
items.add("x")   // UI does NOT update

// FIXED option 1: observable collection — element ops trigger recomposition
val items = remember { mutableStateListOf<String>() }
items.add("x")   // UI updates

// FIXED option 2: immutable list in a MutableState — replace, don't mutate
var items by remember { mutableStateOf(listOf<String>()) }
items = items + "x"   // new list assigned -> recomposes`},{t:"list",items:["**`mutableStateListOf`** — observable at the *element* level; `add`/`remove`/`set` each trigger recomposition. Good for a list you mutate frequently in place.","**`mutableStateMapOf`** — same for maps (put/remove trigger recomposition).","**The alternative** — an immutable `listOf()` inside a `mutableStateOf`, replaced with `copy`/`+`. This is often preferred for state that flows from a ViewModel (immutable, stable for skipping), while `mutableStateListOf` suits local, mutation-heavy UI state."]},{t:"note",text:"mutableStateListOf/mutableStateMapOf are observable collections — element operations trigger recomposition (a plain mutableListOf doesn't). Use them for local mutation-heavy state; prefer immutable list + mutableStateOf for VM-driven state (stable for skipping)."}]},{level:"junior",q:"What are the primitive state variants like mutableIntStateOf, and why do they exist?",a:[{t:"p",text:"`mutableIntStateOf`, `mutableLongStateOf`, `mutableFloatStateOf`, `mutableDoubleStateOf` are specialized state holders for primitive types that *avoid autoboxing*. A regular `mutableStateOf<Int>(0)` boxes the `Int` into an `Integer` object; the primitive variants store the primitive directly, saving allocations — which matters for state that changes very frequently (animations, scroll offsets, counters)."},{t:"code",title:"Boxed vs unboxed primitive state",code:`var count by remember { mutableStateOf(0) }        // boxes Int -> Integer on each set
var count by remember { mutableIntStateOf(0) }     // stores primitive int, no boxing

var offset by remember { mutableFloatStateOf(0f) } // ideal for frequently-changing values`},{t:"list",items:["**Avoids autoboxing** — the primitive variants store the value without wrapping it in an object, reducing allocations and GC pressure.","**Matters for high-frequency state** — a scroll offset or animation value that updates 60×/second benefits; a counter tapped occasionally doesn't really.","**Same API** — used exactly like `mutableStateOf` with `by`, just typed for the primitive."]},{t:"note",text:"mutableIntStateOf/FloatStateOf/etc. store primitives without autoboxing, cutting allocations — worth it for high-frequency state (animations, scroll offsets). Lint even suggests them. Same API as mutableStateOf."}]},{level:"junior",q:"How do you handle text input (TextField) state in Compose?",a:[{t:"p",text:"A `TextField` is stateless by design — it takes the current text `value` and an `onValueChange` callback, and holds *no* text state itself. You own the state (hoisted), update it in `onValueChange`, and pass it back down. This is state hoisting applied to text input, and it's what lets you validate, transform, or share the text."},{t:"code",title:"Hoisted text state",code:`var query by rememberSaveable { mutableStateOf("") }   // survives rotation
TextField(
    value = query,
    onValueChange = { newText ->
        query = newText.take(50)          // you control it: e.g. limit length, validate
    },
    label = { Text("Search") },
)
// Real screens hoist this to the ViewModel so it can drive search:
// TextField(value = uiState.query, onValueChange = viewModel::onQueryChange)`},{t:"list",items:["**TextField is stateless** — `value` + `onValueChange`, no internal text. *You* own the text.","**Use `rememberSaveable`** so the typed text survives rotation (or hoist to the ViewModel/SavedStateHandle for process-death survival).","**You control the input** — transform/validate in `onValueChange` (uppercase, length limits, digit-only). Because *you* hold the state, you can."]},{t:"p",text:"A performance note for very high-frequency typing: routing every keystroke through the full state loop (especially up to a ViewModel with a debounced search) is standard, but for the *raw text field* itself, some apps keep the immediate text as local `rememberSaveable` state and only push committed/debounced values up — to avoid a full recomposition cascade per keystroke. Compose also has newer `TextFieldState`-based APIs that manage text state more efficiently."},{t:"note",text:"TextField is stateless (value + onValueChange) — you hoist and own the text, letting you validate/transform it. Use rememberSaveable (rotation) or the ViewModel (process death). Newer TextFieldState APIs handle high-frequency text more efficiently."}]},{level:"junior",q:"What survives a configuration change vs process death for Compose state?",a:[{t:"p",text:"This is the crux of choosing `remember` vs `rememberSaveable` vs a ViewModel. Different mechanisms survive different events: `remember` survives recomposition but *not* configuration changes; `rememberSaveable` survives both configuration changes *and* process death (via the saved-instance Bundle); a ViewModel survives configuration changes but *not* process death."},{t:"table",headers:["","Recomposition","Config change (rotation)","Process death"],rows:[["`remember { }`","survives","lost","lost"],["`rememberSaveable { }`","survives","survives","survives (Bundle)"],["ViewModel state","survives","survives","lost (unless SavedStateHandle)"],["plain `var`","lost","lost","lost"]]},{t:"p",text:"So the decision: `remember` for transient UI state you're fine losing on rotation (animation scratch values); `rememberSaveable` for small UI state a user would be annoyed to lose (text input, selected tab); the ViewModel for screen data (survives config change in memory); and `SavedStateHandle` in the ViewModel for the *ids/inputs* needed to rebuild after process death. The robust pattern combines them: ViewModel (config change) + SavedStateHandle (process death) + rememberSaveable for widget-level state."},{t:"note",text:"remember: survives recomposition only. rememberSaveable: + config change + process death (Bundle). ViewModel: + config change, NOT process death (needs SavedStateHandle). Combine them: ViewModel for data, SavedStateHandle for process-death ids, rememberSaveable for widget state."}]},{level:"senior",q:"How do you write a custom Saver for rememberSaveable?",a:[{t:"p",text:"`rememberSaveable` can only save types the `Bundle` understands (primitives, String, Parcelable, Serializable). For a custom type, you provide a **`Saver`** — an object that tells Compose how to convert your type *to* something saveable and *back*. Then your custom type survives rotation and process death."},{t:"code",title:"A custom Saver",code:`data class Filter(val tag: String, val enabled: Boolean)

// listSaver: convert to/from a List of Bundle-able values
val FilterSaver = listSaver<Filter, Any>(
    save = { listOf(it.tag, it.enabled) },              // Filter -> saveable list
    restore = { Filter(it[0] as String, it[1] as Boolean) },  // list -> Filter
)

var filter by rememberSaveable(stateSaver = FilterSaver) {
    mutableStateOf(Filter("all", true))
}

// Or mapSaver for named fields; or @Parcelize on the data class (simplest):
@Parcelize data class Filter2(val tag: String, val enabled: Boolean) : Parcelable
var f by rememberSaveable { mutableStateOf(Filter2("all", true)) }  // no Saver needed`},{t:"list",items:["**`listSaver`** — save your object as a list of Bundle-able values, restore by reading positions.","**`mapSaver`** — same but with named keys (more readable for many fields).","**`Saver` object** — full control via `save`/`restore` for complex cases.","**Simplest — `@Parcelize`** — if the type is `Parcelable`, no Saver is needed; `rememberSaveable` handles it automatically. Prefer this when you control the type."]},{t:"note",text:"rememberSaveable only saves Bundle-able types; a custom Saver (listSaver/mapSaver/Saver) defines to-saveable and back. Simplest path: make the type @Parcelize Parcelable — then no Saver is needed at all."}]},{level:"junior",q:"Why should UiState be immutable, and what breaks if it isn't?",a:[{t:"p",text:"UiState should be an immutable data class (all `val`, immutable collections) that you replace wholesale via `copy()`. If it's mutable — a `var` field or a `mutableListOf` inside — you break two things Compose relies on: *recomposition triggering* (mutating in place doesn't change the reference, so Compose doesn't notice) and *skipping* (unstable types aren't skippable, and equality checks become meaningless)."},{t:"code",title:"Mutable UiState breaks updates and skipping",code:`// BAD: mutable — in-place changes don't recompose, and it's unstable
data class UiState(var items: MutableList<Item>, var isLoading: Boolean)
state.items.add(item)   // reference unchanged -> no recomposition

// GOOD: immutable — replace via copy, recomposes reliably, stable/skippable
data class UiState(val items: List<Item>, val isLoading: Boolean)
_state.update { it.copy(items = it.items + item) }   // new object -> recomposes`},{t:"list",items:["**Recomposition needs a new reference** — Compose (and StateFlow) detect change by equality/reference. Mutating a `var` or a list in place doesn't produce a new state object, so nothing recomposes.","**Stability for skipping** — a data class with `var` or a mutable collection is *unstable*, so composables taking it as a parameter can't be skipped, hurting performance.","**Predictability** — immutable state is a snapshot; you can't have code accidentally mutating it from elsewhere. It's also what makes 'time-travel' and equality-based diffing possible."]},{t:"note",text:"Immutable UiState (val + immutable collections, replaced via copy) is required so recomposition triggers (new reference), the type is stable/skippable, and state is a predictable snapshot. Mutating in place = no recomposition + unstable + unpredictable."}]},{level:"senior",q:"Should UiState be a single data class or a sealed hierarchy?",a:[{t:"p",text:"Both are valid; the choice depends on whether your screen's states are *mutually exclusive* or can *overlap*. A sealed interface (`Loading`/`Content`/`Error`) makes states exclusive and forces exhaustive handling; a single data class with flags allows overlapping states (showing cached content *while* refreshing *with* an error banner)."},{t:"code",title:"Sealed vs data class",code:`// SEALED: mutually exclusive states, exhaustive when, impossible combos can't exist
sealed interface UiState {
    data object Loading : UiState
    data class Content(val items: List<Item>) : UiState
    data class Error(val msg: String) : UiState
}
// DATA CLASS: overlapping states possible (content + refreshing + error together)
data class UiState(
    val items: List<Item> = emptyList(),
    val isRefreshing: Boolean = false,
    val error: String? = null,
)`},{t:"list",items:["**Sealed** — best when states truly exclude each other; the compiler forces an exhaustive `when` and impossible combinations (loading *and* error) can't be represented. Great for simple load flows.","**Data class** — best when states overlap: showing existing content while a refresh spins with a transient error. Sealed classes would force awkward state duplication here.","**Hybrid** — a data class whose fields include sealed sub-states (e.g. `val loadState: LoadState`) — exclusive where it should be, overlapping where it should be."]},{t:"p",text:"The judgment: a pure load screen (nothing to show until loaded) suits sealed; a screen that keeps content visible during refresh (most real feeds) suits a data class. 'One UiState per screen' is a guideline, not a law — the right shape is whichever makes illegal states unrepresentable *without* forcing duplication for states that legitimately coexist."},{t:"note",text:"Sealed UiState for mutually-exclusive states (exhaustive when, impossible combos can't exist); data class for overlapping states (content + refreshing + error together). Real feeds usually need the data class; pure load screens suit sealed. Hybrid is common."}]},{level:"junior",q:"How does collectAsState turn a Flow into Compose State?",a:[{t:"p",text:"`collectAsState()` (and `collectAsStateWithLifecycle()`) bridge a `Flow`/`StateFlow` into Compose's world by *collecting* the flow inside the composition and mirroring each emission into a `State<T>` that composables can read. Under the hood it launches a coroutine (in the composition's scope) that collects the flow and updates a backing `mutableStateOf` on each emission — so a new emission recomposes the readers."},{t:"code",title:"Flow -> State",code:`// StateFlow<UiState> in the ViewModel becomes State<UiState> the UI can read
val state: State<UiState> by viewModel.uiState.collectAsStateWithLifecycle()
// each emission from the flow -> updates the backing State -> recomposes readers`},{t:"list",items:["**It collects inside the composition** — launches a coroutine that collects the flow and writes each value into a `mutableStateOf`.","**`collectAsState` vs `collectAsStateWithLifecycle`** — the lifecycle variant stops collecting when the app is backgrounded (`repeatOnLifecycle`), pairing with `stateIn(WhileSubscribed)`; plain `collectAsState` keeps collecting in the background. Prefer the lifecycle variant on Android.","**Needs an initial value for a plain Flow** — `StateFlow` has one; a plain `Flow` requires you to pass an `initial`."]},{t:"note",text:"collectAsState launches a coroutine that collects the flow and mirrors emissions into a mutableStateOf, so new values recompose readers. Use collectAsStateWithLifecycle on Android (stops collecting when backgrounded, pairs with WhileSubscribed)."}]},{level:"junior",q:"How do you reset or recreate state when an input changes?",a:[{t:"p",text:"You pass a *key* to `remember` — `remember(key) { ... }` recomputes (and thus resets) its value whenever the key changes. This solves the common bug where remembered state 'sticks' to the first value and doesn't update when the relevant input changes."},{t:"code",title:"Keyed remember resets state on input change",code:`// BUG: keeps the FIRST item's expanded state even when 'item' changes
var expanded by remember { mutableStateOf(item.defaultExpanded) }

// FIX: keyed on item.id -> resets when a different item is passed
var expanded by remember(item.id) { mutableStateOf(item.defaultExpanded) }`},{t:"list",items:["**`remember(key)`** — re-runs the calculation when `key` changes, giving fresh state. Use when the remembered value *derives from* or *should reset with* an input.","**Multiple keys** — `remember(a, b) { }` recomputes if either changes.","**In lazy lists** — this is why item state needs care: reusing a composable for a different item must reset per-item state; `key()` and item keys handle identity so remembered state migrates correctly."]},{t:"note",text:"remember(key) { } recomputes when the key changes — the fix for 'remembered state sticks to the first value'. Key on the input the state should reset with (e.g. item.id). Without a key, remember keeps the first value regardless of input changes."}]},{level:"junior",q:"When does remembered state get forgotten?",a:[{t:"p",text:"Remembered state lives as long as its call site stays in the composition. It's forgotten — permanently discarded — when the composable *leaves the composition*: its `if` branch stops being emitted, it's removed from a lazy list far off-screen, or the whole composition is torn down (like a configuration change, since `remember` doesn't survive that). State lifetime equals presence in the tree."},{t:"list",items:["**Leaving a conditional branch** — `if (show) { val x = remember { ... } }` — when `show` becomes false, `x` is forgotten; when true again, it's re-initialized fresh.","**Scrolled out of a lazy list** — items scrolled far away are disposed, forgetting their remembered state (unless hoisted or made saveable).","**Composition teardown** — a configuration change rebuilds the whole composition, wiping all `remember`ed state (this is why `rememberSaveable` exists).","**Moving position** — moving a composable between branches changes its slot-table position, so its remembered state is forgotten (use `key()` to preserve identity)."]},{t:"note",text:"Remembered state is forgotten when the composable leaves the composition: a conditional branch closes, a lazy-list item scrolls far away, position changes, or the composition is torn down (config change). State lifetime = presence in the tree — use rememberSaveable/hoisting to outlive it."}]},{level:"junior",q:"How do you share state between two sibling composables?",a:[{t:"p",text:"You *hoist* the state to their nearest common ancestor and pass it down to both — the state lives in one place (the parent), and both children receive it as parameters plus callbacks to change it. Neither child owns the state; the parent does. This is state hoisting applied to sharing."},{t:"code",title:"Hoist to the common ancestor",code:`@Composable fun Parent() {
    var selectedTab by remember { mutableStateOf(0) }   // shared state lives here
    Column {
        TabRow(selected = selectedTab, onSelect = { selectedTab = it })   // child A writes
        TabContent(tab = selectedTab)                                     // child B reads
    }
}`},{t:"list",items:["**Hoist to the lowest common ancestor** — the state lives in the composable that's a parent of everyone who needs it, and no higher.","**Pass down state + events** — readers get the value; writers get a callback. State flows down, events flow up (UDF).","**For app-wide/screen-level sharing** — the ViewModel is the shared owner; both composables read its state and call its functions. For truly ambient values (theme, current user) across a deep tree, `CompositionLocal` is an option — but prefer explicit hoisting for normal data."]},{t:"note",text:"Hoist shared state to the siblings' lowest common ancestor; pass value down to readers and a callback to writers (UDF). For screen-wide sharing use the ViewModel; for ambient values across deep trees, CompositionLocal — but prefer explicit hoisting."}]},{level:"senior",q:"What is SnapshotMutationPolicy and when would you change it?",a:[{t:"p",text:"A `SnapshotMutationPolicy` tells Compose *how to decide whether a state value actually changed* when you write it — which determines whether a write triggers recomposition. The default is `structuralEqualityPolicy()` (uses `equals()`), so setting a value `equals` to the current one is a no-op (no recomposition). You can change it for special cases."},{t:"list",items:["**`structuralEqualityPolicy()` (default)** — compares with `equals()`. Writing an equal value doesn't recompose. Right for most immutable data.","**`referentialEqualityPolicy()`** — compares by reference (`===`). Even an `equals`-equal but different instance counts as a change. Useful when identity matters or `equals` is expensive/misleading.","**`neverEqualPolicy()`** — *every* write is treated as a change, always recomposing. Useful when you need to force re-emission of a value that's `equals` to the previous (e.g. a mutable object you mutated in place and want to signal changed)."]},{t:"code",title:"Choosing a policy",code:`// Force recomposition even when the new value equals the old:
var trigger by remember { mutableStateOf(Unit, neverEqualPolicy()) }
// Setting it re-triggers readers even though Unit == Unit`},{t:"p",text:"You rarely change it — the default structural equality is correct for the immutable-data model Compose encourages. But knowing it exists explains subtle behavior (why setting an `equals`-equal value doesn't recompose) and gives an escape hatch: `neverEqualPolicy` when you genuinely must re-emit an equal value, or `referentialEqualityPolicy` when identity is what matters. Reaching for these is usually a sign the state should be modeled differently (an equal value you must re-emit is often really an *event*, not state)."},{t:"note",text:"SnapshotMutationPolicy decides if a write counts as a change: structural (default, equals — equal writes don't recompose), referential (===), or neverEqual (always recompose). Rarely changed; needing neverEqual usually means the thing is an event, not state."}]},{level:"senior",q:"What is the checklist of reasons 'my Compose UI doesn't update when state changes'?",a:[{t:"p",text:"'The state changed but the UI didn't update' is the most common Compose bug, and it almost always comes down to the state not being observable, not being remembered, or being mutated in place. Here's the diagnostic checklist."},{t:"list",items:["**Not a snapshot state type** — you used a plain `var` or a normal `Flow` without `collectAsState`. Only `mutableStateOf`/`State` (and collected flows) are observable. Fix: wrap in `mutableStateOf`.","**Mutated in place** — you did `list.add(x)` on a `remember { mutableListOf() }`, or changed a `var` field of a data class. The reference didn't change, so no recomposition. Fix: `mutableStateListOf`, or replace with an immutable copy via `copy()`.","**`mutableStateOf` without `remember`** — state resets to the initial value every recomposition, so it looks 'stuck'. Fix: `remember { mutableStateOf(...) }`.","**Writing an `equals`-equal value** — structural equality means an equal value is a no-op. Fix: ensure the value genuinely differs, or reconsider (it may be an event).","**Reading a different instance than you're writing** — two separate state objects, or reading `.value` of one and writing another. Fix: single source.","**Collecting the wrong flow / not collecting** — a cold `stateIn(WhileSubscribed)` flow does nothing until collected; reading `.value` shows only the initial value. Fix: `collectAsStateWithLifecycle`.","**Reading state outside a composition** — reading in a callback that captured an old value, not the State object. Fix: read the `State`/`rememberUpdatedState`."]},{t:"note",text:"'UI won't update' checklist: (1) not a snapshot state type, (2) mutated in place (reference unchanged), (3) mutableStateOf without remember (resets), (4) writing an equal value (structural equality no-op), (5) reading a different instance, (6) an uncollected cold flow. Almost always #1–3."}]},{level:"senior",q:"How do you model one-off events (navigate, show snackbar) versus state?",a:[{t:"p",text:"State and events are different: *state* is a value that persists and can be re-read (the current list, isLoading); an *event* is a one-off instruction that should fire exactly once (navigate to detail, show a snackbar, play a sound). Modeling an event as state causes bugs — on recomposition or config change the UI re-reads the 'navigate' state and navigates again."},{t:"list",items:["**Don't put events in UiState as a flag** — `data class UiState(val navigateToDetail: Boolean)` re-triggers after rotation because the flag is still true. If you use a flag, you must immediately reset it after consuming (error-prone).","**Preferred: a `Channel` exposed as a `Flow`** — `Channel(Capacity.BUFFERED).receiveAsFlow()`; each event is delivered exactly once to a single collector. The UI collects it in a `LaunchedEffect`/`collectAsStateWithLifecycle`-driven block and acts.","**Alternative: `SharedFlow`** with `replay = 0` — also fires once, but be careful about drops when there's no collector; a `Channel` is safer for guaranteed-once delivery.","**The rule of thumb** — if re-reading the value after rotation should re-do the action, it's wrongly modeled. Navigation/snackbars are events, not state."]},{t:"code",title:"Events via a Channel",code:`// ViewModel
private val _events = Channel<UiEvent>(Channel.BUFFERED)
val events = _events.receiveAsFlow()
fun onSaved() { viewModelScope.launch { _events.send(UiEvent.NavigateBack) } }

// UI: collect once, act once
LaunchedEffect(Unit) {
    viewModel.events.collect { event -> when (event) { /* navigate, snackbar */ } }
}`},{t:"note",text:"State persists and is re-read; events fire once. Modeling navigation/snackbar as a UiState flag re-fires after rotation. Deliver one-off events via a Channel.receiveAsFlow() (guaranteed-once) collected in a LaunchedEffect — not as state."}]},{level:"junior",q:"How do you debounce a search field with Compose state and Flow?",a:[{t:"p",text:"You keep the text as Compose state (updates instantly as the user types) but push the *query* into a Flow so you can apply `debounce` before hitting the network — avoiding a request per keystroke. The typical shape holds the query in a StateFlow in the ViewModel and transforms it with `debounce` + `distinctUntilChanged` + `flatMapLatest`."},{t:"code",title:"Debounced search",code:`// ViewModel
private val query = MutableStateFlow("")
fun onQueryChange(q: String) { query.value = q }   // called from TextField.onValueChange

@OptIn(FlowPreview::class, ExperimentalCoroutinesApi::class)
val results = query
    .debounce(300)                 // wait for typing to pause
    .distinctUntilChanged()        // ignore no-op changes
    .flatMapLatest { q ->          // cancel the previous search when query changes
        if (q.isBlank()) flowOf(emptyList()) else repo.search(q)
    }
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), emptyList())`},{t:"list",items:["**Text is instant, search is debounced** — the TextField shows every keystroke (state), but the network query waits for a 300ms pause (Flow `debounce`).","**`flatMapLatest`** — cancels the in-flight search when a newer query arrives, so stale results never overwrite fresh ones.","**`distinctUntilChanged`** — skips redundant searches (e.g. same text after trimming)."]},{t:"note",text:"Hold the text as instant Compose state, push the query into a StateFlow, then debounce(300) + distinctUntilChanged + flatMapLatest (cancels stale searches) + stateIn. Text updates every keystroke; the network fires only after a typing pause."}]},{level:"senior",q:"When should ViewModel state be exposed as mutableStateOf vs StateFlow?",a:[{t:"p",text:"Both work for driving Compose UI, and the choice is mostly about ecosystem fit. `StateFlow` is the mainstream choice because it's Compose-agnostic (testable without Compose, usable in KMP, composes with other flows via operators), while Compose `mutableStateOf` in a ViewModel is simpler and skips the `collectAsState` bridge but couples the ViewModel to the Compose runtime."},{t:"table",headers:["","StateFlow","mutableStateOf in ViewModel"],rows:[["Compose dependency","none (pure Kotlin)","depends on Compose runtime"],["KMP-friendly","yes","only with Compose Multiplatform"],["Flow operators","yes (combine, debounce…)","no (not a flow)"],["UI usage","`collectAsStateWithLifecycle()`","read directly (no collect)"],["Lifecycle-aware collection","yes (WhileSubscribed)","always 'hot'"]]},{t:"list",items:["**Prefer `StateFlow`** for most apps — it's Compose-independent (easy unit tests, KMP shared ViewModels), composes with other flows, and `collectAsStateWithLifecycle()` + `WhileSubscribed` gives lifecycle-aware, backgrounding-safe collection.","**`mutableStateOf` in the ViewModel** is fine for small Compose-only apps — no `collectAsState` boilerplate — but ties the ViewModel to Compose and can't be combined with flow operators.","**Consistency matters** — pick one per codebase. Mixing both for the same screen's state is confusing."]},{t:"note",text:"StateFlow is the default: Compose-agnostic (testable, KMP), composable with flow operators, and collectAsStateWithLifecycle + WhileSubscribed is lifecycle-safe. mutableStateOf in the VM is simpler (no collect) but couples to Compose and can't use flow operators. Pick one per codebase."}]},{level:"junior",q:"Why must you never do side effects directly in a composable body when computing state?",a:[{t:"p",text:"A composable body can run *any number of times* (every recomposition), on *any thread*, in *any order*, and can be *skipped* or *abandoned*. So writing to a variable, launching a coroutine, or mutating external state directly in the body runs unpredictably — maybe 0 times, maybe 60×/second. Side effects belong in the effect APIs (`LaunchedEffect`, `SideEffect`, `DisposableEffect`) that give controlled, lifecycle-aware execution."},{t:"code",title:"Side effect in the body vs in an effect",code:`// BAD: runs on every recomposition, unpredictably
@Composable fun Screen(id: String) {
    viewModel.load(id)          // fires again on every recomposition!
    var x = mutableStateOf(0)   // and not remembered -> resets every time
}

// GOOD: effect runs once per key change, lifecycle-aware
@Composable fun Screen(id: String) {
    LaunchedEffect(id) { viewModel.load(id) }   // runs when id changes, cancels on leave
    var x by remember { mutableStateOf(0) }     // survives recomposition
}`},{t:"list",items:["**Composables are restartable and skippable** — the body isn't a one-time init; treating it like `onCreate` causes repeated/duplicate work.","**Use the right effect** — `LaunchedEffect` for coroutine work keyed to inputs, `DisposableEffect` for setup/teardown (listeners), `SideEffect` for publishing state to non-Compose code each successful composition.","**State must be `remember`ed** — declaring `mutableStateOf` in the body without `remember` resets it every recomposition (a side-effect-like bug)."]},{t:"note",text:"Composable bodies run any number of times, any thread, any order, and can be skipped — so side effects there run unpredictably (0 to 60×/sec). Put them in LaunchedEffect/DisposableEffect/SideEffect (controlled, lifecycle-aware) and always remember your state."}]}],Yx={id:"state",title:"State & Recomposition",tagline:"remember, rememberSaveable, state hoisting, derivedStateOf, collecting Flows, the snapshot system internals, and every classic state bug.",tags:["Compose","remember","Hoisting","derivedStateOf","Snapshots"],content:Gx,qa:Jx},Qx=[{heading:"Modifiers — what they are and how they really work",blocks:[{t:"p",text:"A **Modifier** is Compose's way of decorating or configuring a composable: sizing, padding, backgrounds, clicks, scrolling, drawing — all attach through the `modifier` parameter instead of dozens of constructor parameters or XML attributes. `Modifier.padding(16.dp).background(Blue)` builds an **ordered chain** of modifier elements; each element wraps the element after it, forming a pipeline the layout/draw/input systems walk from left to right."},{t:"list",items:["Modifiers are **immutable**: each call like `.padding(...)` returns a *new* chain — you can safely store, share and reuse them (`val cardModifier = Modifier...` reused across items is a real optimization).","**Order is semantics, not style** — the single most-asked modifier fact. Each element applies to everything to its *right*: `padding` then `background` pads first, then paints inside the padded area; reversed, the background paints the full size and then content is inset.","Under the hood (Modifier.Node system): the chain materializes into persistent node objects attached to the LayoutNode, each participating in the phases it cares about — layout modifiers adjust measurement, draw modifiers wrap drawing, pointer-input modifiers join hit testing. On recomposition the chain is *diffed* and nodes updated in place, which is why modifiers are cheap."]},{t:"code",title:"Order changes meaning — the classic quartet",code:`// 1) Background BEHIND the padding (paints full area, then insets content)
Modifier.background(Color.Blue).padding(16.dp)

// 2) Background INSIDE the padding (pads first, paints smaller area)
Modifier.padding(16.dp).background(Color.Blue)

// 3) Whole padded area clickable (ripple covers padding)
Modifier.clickable { }.padding(16.dp)

// 4) Only the inner content clickable (padding is dead space)
Modifier.padding(16.dp).clickable { }

// size constraints resolve left-to-right too:
Modifier.size(100.dp).size(50.dp)   // stays 100.dp — first size fixes
                                    // constraints; later ones can't override`},{t:"note",text:"Convention interviewers check: every reusable composable should take a `modifier: Modifier = Modifier` as its **first optional parameter** and pass it to its **outermost** layout — so callers can size/pad/position your component without you predicting their needs. Accepting-but-ignoring the modifier, or applying it to an inner child, are the review bugs to name."}]},{heading:"The layout model: constraints down, sizes up, placement",blocks:[{t:"p",text:'Compose layout is a single-pass negotiation in three steps. Understanding it precisely unlocks every sizing question ("why is fillMaxSize ignored here?") and custom layouts:'},{t:"list",items:['**1. Constraints go down**: a parent hands each child a `Constraints` object — `minWidth..maxWidth`, `minHeight..maxHeight` in pixels. `fillMaxWidth()` means "take maxWidth"; `wrapContentSize()` means "prefer min"; a fixed `size(50.dp)` *tries* to be 50 but is **coerced into the incoming constraints** — this coercion is the answer to most "my size modifier is ignored" mysteries.',"**2. Sizes go up**: each child measures itself within the constraints (measuring *its* children first, recursively) and reports one final size. A parent may measure each child **exactly once**.","**3. Placement**: knowing all children's sizes, the parent positions each one (`placeable.place(x, y)`). Alignment, arrangement and offset all happen here."]},{t:"code",title:"Reading a constraints bug",code:`// Why is the Box not 50.dp? Because fillMaxSize already fixed
// constraints to exactly the parent size, and size() cannot escape
// the incoming min == max constraints:
Box(Modifier.fillMaxSize()) {
    Box(Modifier.fillMaxSize().size(50.dp)) { }  // still full size!
}

// requiredSize() is the escape hatch — it IGNORES incoming constraints
Box(Modifier.fillMaxSize().requiredSize(50.dp)) { } // truly 50.dp
// (parent still lays it out as if it were the constrained size;
// content visually overflows centered)`}]},{heading:"The standard layouts: Row, Column, Box — and their alignment vocabulary",blocks:[{t:"code",title:"The three workhorses",code:`Column(
    verticalArrangement = Arrangement.spacedBy(8.dp),   // main axis
    horizontalAlignment = Alignment.CenterHorizontally, // cross axis
) {
    Text("one")
    Text("two")
    Spacer(Modifier.weight(1f))   // weight: share leftover space
    Text("pinned to bottom")
}

Row(verticalAlignment = Alignment.CenterVertically) {
    Icon(Icons.Default.Star, contentDescription = null)
    Text("title", Modifier.weight(1f))   // takes all remaining width
    Text("meta")
}

Box {                                    // stacking (like FrameLayout)
    Image(painter, contentDescription = null)
    Text("badge", Modifier.align(Alignment.TopEnd))
}`},{t:"list",items:["**Arrangement** = distribution along the **main axis** (Column: vertical; Row: horizontal): `spacedBy`, `SpaceBetween`, `SpaceEvenly`, `Center`… **Alignment** = positioning on the **cross axis**. Mixing these up is the #1 layout-API confusion.","**`weight(n)`** (Row/Column-scoped): children *without* weight measure first; leftover space is split among weighted children proportionally. `weight(1f, fill = false)` lets a child take *up to* its share. This replaces LinearLayout's layout_weight — and is also the idiomatic 'push to the end' tool (a weighted Spacer).","Scoped modifiers are typed: `align()` exists only inside Box/Column/Row scopes, `weight()` only in Row/Column — the compiler stops you using them elsewhere (a nicety over XML worth mentioning).","**Slot APIs**: composables taking composable lambdas as parameters (`TopAppBar(title = { ... }, actions = { ... })`, Scaffold's slots) — Compose's pattern for customizable containers; you'll be asked to *design* one for a component library question."]}]},{heading:"Custom layouts with Layout — when Row/Column aren't enough",blocks:[{t:"p",text:"Any layout you can describe, you can implement with the `Layout` composable — the same primitive Row/Column are built on. You receive the children as `measurables` and the incoming `constraints`, and you implement the measure-place contract yourself:"},{t:"code",title:"A real custom layout: simple flow/chip layout that wraps lines",code:`@Composable
fun FlowRow(
    modifier: Modifier = Modifier,
    spacing: Dp = 8.dp,
    content: @Composable () -> Unit,
) {
    Layout(content = content, modifier = modifier) { measurables, constraints ->
        val gap = spacing.roundToPx()
        // 1) MEASURE each child once, with loose constraints
        val placeables = measurables.map { it.measure(constraints.copy(minWidth = 0, minHeight = 0)) }

        // 2) Break into rows against maxWidth
        var x = 0; var y = 0; var rowHeight = 0
        val positions = placeables.map { p ->
            if (x + p.width > constraints.maxWidth && x > 0) {
                x = 0; y += rowHeight + gap; rowHeight = 0
            }
            val pos = IntOffset(x, y)
            x += p.width + gap
            rowHeight = maxOf(rowHeight, p.height)
            pos
        }
        val totalHeight = (y + rowHeight).coerceIn(constraints.minHeight, constraints.maxHeight)

        // 3) Report size, then PLACE
        layout(constraints.maxWidth, totalHeight) {
            placeables.forEachIndexed { i, p -> p.place(positions[i]) }
        }
    }
}`},{t:"list",items:["The contract in words: measure every child **once**, decide your own size within `constraints`, call `layout(w, h) { ... }` and place children. That's the entire layout system — no onMeasure/onLayout pairs, no MeasureSpec bit-packing.","**`SubcomposeLayout`**: for the rare case where composing one child depends on another's *measured size* (e.g. Scaffold measuring bars before content) — it defers *composition* of some content until measure time. Heavier; use only when measurement genuinely drives what to compose (this is also how LazyColumn composes only visible items).","**Intrinsic measurements** (`Modifier.height(IntrinsicSize.Max)` etc.): pre-query 'what size would you want?' without consuming the single measure — for match-the-tallest-sibling cases. Costs an extra tree walk; fine when localized.","`Modifier.layout { measurable, constraints -> ... }` is the single-child version — for tweaking one child's measurement/placement without a full Layout (e.g. custom baseline padding)."]}]},{heading:"Common modifier families you must be fluent in",blocks:[{t:"table",headers:["Family","Members","Notes / gotchas"],rows:[["Size","`size`, `fillMaxWidth/Height/Size`, `wrapContentSize`, `requiredSize`, `sizeIn`, `aspectRatio`","`size` obeys incoming constraints; `requiredSize` overrides them; `fillMaxWidth(0.5f)` takes a fraction"],["Spacing","`padding`","there is no margin — outer spacing is just padding placed earlier in the chain, or parent `spacedBy`"],["Drawing","`background`, `border`, `clip`, `shadow`, `alpha`, `drawBehind`, `drawWithContent`, `graphicsLayer`","`clip(RoundedCornerShape(...))` must come *before* `background` to round it; `graphicsLayer` changes render without re-layout"],["Interaction","`clickable`, `combinedClickable`, `toggleable`, `selectable`, `pointerInput`","`clickable` adds ripple + semantics + focus; raw `pointerInput` gives gesture detectors but no accessibility for free"],["Scroll","`verticalScroll(rememberScrollState())`, `horizontalScroll`","composes ALL children (unlike lazy lists); gives content infinite max height — nesting a LazyColumn inside is the classic crash"],["Position","`offset` (and its lambda form), `zIndex`, `align` (scoped)","`offset` moves placement without affecting measurement of siblings; lambda form defers the state read to layout phase"]]},{t:"note",text:"Two chain-reading rules that resolve most puzzles: (1) each modifier wraps what's to its right — read left = outermost; (2) constraints only tighten as they travel right — once fixed, later size modifiers can't loosen them (except the `required*` family)."}]},{heading:"ConstraintLayout in Compose — and why you rarely need it",blocks:[{t:"p",text:"`ConstraintLayout` exists in Compose (separate artifact) with references and constraint DSL. But its *original reason to exist* — avoiding nested-layout double measurement in the View system — is gone: Compose measures in a single pass, so **nesting Rows/Columns is cheap and idiomatic**. Reach for ConstraintLayout only for genuinely relational designs (align this to that's baseline across branches, barrier/chain behavior, guideline percentages), not as the default screen scaffold. Saying exactly this — with the *why* — is a well-known interview checkpoint."}]},{heading:"BoxWithConstraints and adaptive layouts",blocks:[{t:"code",title:"Responding to available space",code:`BoxWithConstraints {
    if (maxWidth < 600.dp) {
        CompactList()
    } else {
        Row {
            ListPane(Modifier.weight(1f))
            DetailPane(Modifier.weight(2f))
        }
    }
}`},{t:"list",items:["`BoxWithConstraints` exposes the incoming constraints *during composition* — letting you compose different trees per available size. Cost: it's built on SubcomposeLayout (composition deferred to measure), so don't sprinkle it everywhere; use it at adaptive-layout decision points.","For app-level adaptivity prefer **WindowSizeClass** (compact/medium/expanded buckets from Material3-adaptive) — decisions follow the window, not any one box, and it standardizes breakpoints for phones/foldables/tablets.",'Related: `Spacer` for pure space, `Surface` for themed containers (color/elevation/shape + content color propagation), `Scaffold` for screen chrome (app bars, FAB, snackbar host) with correct insets via its `paddingValues` — forgetting to apply Scaffold\'s padding to content is a favorite "spot the bug".']}]}],$x=[{level:"junior",q:"What is a Modifier and why does its order matter?",a:[{t:"p",text:"**The concept**: a Modifier is an ordered chain of decorations and behaviors attached to a composable — sizing, padding, background, clicks, scrolling. Instead of widgets exposing hundreds of properties, Compose factors all of that into one composable-agnostic chain: `Modifier.padding(16.dp).background(Blue).clickable { }`. Each call returns a new immutable chain with one more element."},{t:"p",text:"**Why order matters**: the chain isn't a bag of settings — it's a **pipeline** where each element wraps everything to its right. `background(Blue).padding(16.dp)` paints the background over the full area *then* insets the content — the padding is blue. `padding(16.dp).background(Blue)` insets *first*, so the background only covers the smaller inner area — the padding is transparent. Same two modifiers, visibly different UI. The same logic governs clickable areas (`clickable` before `padding` = padded area is tappable and rippled; after = only the content is) and size constraints (`size(100.dp).size(50.dp)` stays 100 — constraints fix left-to-right and later elements can't loosen them)."},{t:"p",text:"**How to never get confused**: read the chain left to right as \"outermost to innermost\". And there's no `margin` in Compose precisely because of this — outer spacing is just a `padding` placed *earlier* in the chain than the background/border, which is arguably cleaner than the View system's two separate concepts."}]},{level:"junior",q:"Explain how measurement works in Compose (constraints, sizes, placement).",a:[{t:"p",text:"**The model**: layout is a single-pass negotiation. First, **constraints travel down** — every parent gives each child a `Constraints` object: acceptable min/max width and height in pixels. The child doesn't get to demand a size; it gets a range. Second, **sizes travel up** — the child (after measuring its own children the same way) picks its final size *within* that range and reports it. Third, **placement**: the parent, now knowing every child's size, positions them with x/y coordinates. Each child is measured exactly once per pass — enforced by the runtime."},{t:"p",text:"**How modifiers plug in**: size modifiers just transform the constraints flowing through the chain. `fillMaxWidth()` = 'set min = max = the available maxWidth'; `wrapContentWidth()` = 'prefer the minimum'; `size(50.dp)` = 'try to fix constraints to 50, **coerced into what came in**'. That coercion answers the classic confusion: inside a parent that already fixed the size (`fillMaxSize` set min == max), a later `size(50.dp)` is silently overridden — the constraints leave it no room. The `requiredSize` family is the explicit escape hatch that ignores incoming constraints."},{t:"p",text:`**Why single-pass is a feature to name**: the View system allowed parents to measure children repeatedly, so nested weights/RelativeLayouts could go exponential with depth — the origin of "flatten your hierarchy". Compose's one-measure rule makes cost linear in tree size, so nesting Columns and Rows freely is idiomatic, not a smell.`}]},{level:"junior",q:"What's the difference between Arrangement and Alignment in Row/Column?",a:[{t:"p",text:"**The concept**: Row and Column each have a *main axis* (the direction they stack children — horizontal for Row, vertical for Column) and a *cross axis* (the perpendicular one). The two parameters control one axis each: **Arrangement** distributes children and leftover space **along the main axis**; **Alignment** positions each child **on the cross axis**."},{t:"p",text:"So in a `Column`: `verticalArrangement` decides vertical distribution (`Arrangement.spacedBy(8.dp)` for gaps, `SpaceBetween`, `Center`, `Bottom`…), while `horizontalAlignment` decides whether children sit left/center/right. In a `Row` it flips: `horizontalArrangement` + `verticalAlignment`. Two useful details: `Arrangement.spacedBy` is the idiomatic replacement for putting padding on every child; and individual children can override the cross-axis alignment with `Modifier.align(...)`, which only compiles inside the right scope — the type system prevents using `align` where it's meaningless. If you remember one sentence: **Arrangement = the stacking axis, Alignment = the other one.**"}]},{level:"junior",q:"How does Modifier.weight work, and what does it replace from the View system?",a:[{t:"p",text:"**The concept**: `weight` divides *leftover* space. Inside a Row or Column, measurement happens in two rounds: first, all children **without** weight measure normally and take what they need; then the remaining space is split among weighted children **proportionally to their weights** — `weight(1f)` and `weight(2f)` split leftovers 1:2. A weighted child's size is thus decided by the parent's arithmetic, not its content."},{t:"code",title:"The three everyday uses",code:`Row {
    Icon(Icons.Default.Star, null)
    Text("Title that truncates", Modifier.weight(1f), maxLines = 1)  // absorb space
    Text("3:45")
}

Column(Modifier.fillMaxHeight()) {
    Content()
    Spacer(Modifier.weight(1f))   // push the button to the bottom
    SubmitButton()
}

Row { // equal-width segmented buttons
    repeat(3) { OptionButton(Modifier.weight(1f)) }
}`},{t:"p",text:'It replaces LinearLayout\'s `layout_weight`, minus its famous pitfalls (no `0dp`-width trick needed, no double-measure cost). Details worth knowing: `weight(1f, fill = false)` means "take *up to* your share" (child can be smaller); weight only exists in Row/Column scope (compile-checked); and a weighted child receives its share as *fixed constraints* — so it will be exactly that size unless `fill = false`.'}]},{level:"junior",q:"Why should every reusable composable take a `modifier: Modifier = Modifier` parameter?",a:[{t:"p",text:"**The concept**: a component author cannot predict how callers will use the component — one screen wants it full-width, another wants it 100.dp with extra padding, a third wants it clickable and semi-transparent. Rather than adding parameters for each possibility, Compose's convention is: accept a single `modifier` parameter (first optional parameter, defaulting to `Modifier`) and apply it to your **outermost** layout. Callers then compose whatever sizing/spacing/behavior they need from the outside."},{t:"list",items:["**Apply it to the root, once**: applying the caller's modifier to an inner child means their `fillMaxWidth` sizes the wrong thing; applying it to multiple children duplicates paddings and clicks — both are real review bugs.","**Chain yours after theirs**: `modifier.then(internalStuff)` or simply `modifier.padding(...)` — caller's elements sit outermost, so their positioning wins, and your internal decorations stay inside.","**Don't accept-and-ignore**: silently dropping the parameter breaks every caller's layout expectations and is worse than not having it.","**Why a parameter and not hardcoding**: it keeps components context-free — the same `ProfileCard` works in a list, a grid, a dialog — which is the composability Compose is named after."]}]},{level:"junior",q:"There's no margin in Compose — how do you create spacing between elements?",a:[{t:"p",text:"**The concept**: the View system had two spacing ideas — padding (inside the border) and margin (outside it). Compose deliberately has only `padding`, because with ordered modifier chains one concept covers both: padding applied *before* the background/border in the chain acts like a margin (space outside the visible surface), padding applied *after* acts like classic padding (space inside it). `Modifier.padding(8.dp).background(Blue).padding(16.dp)` gives 8dp of 'margin' and 16dp of 'padding' with one API."},{t:"p",text:"**The idiomatic toolbox for spacing**: `Arrangement.spacedBy(8.dp)` on Row/Column/LazyColumn for uniform gaps between children (preferred over per-child padding — no doubled edges); `Spacer(Modifier.height(8.dp))` for one-off gaps; `contentPadding` on lazy lists for space at the edges *inside* the scrolling region (so content scrolls under the edge, unlike outer padding which clips the scroll area — a distinction interviewers like); and weighted Spacers to push things apart. If asked why this design is better: one concept + explicit order beats two concepts + implicit rules about how they interact."}]},{level:"senior",q:"Implement (or walk through) a custom layout with the Layout composable. What contract must you satisfy?",a:[{t:"p",text:"**The concept**: `Layout` is the primitive underneath Row, Column, Box — all of them are just measure policies. You provide the children (`content`) and a `MeasurePolicy` lambda receiving `measurables` (unmeasured children) and `constraints` (from your parent). The contract has three obligations: **measure each child at most once** (`measurable.measure(childConstraints)` → a `Placeable` with real width/height; measuring twice throws), **choose your own size within the incoming constraints** and report it via `layout(width, height) { ... }`, and inside that block **place every child** (`placeable.place(x, y)`) — unplaced children simply don't render."},{t:"code",title:"Skeleton every custom layout follows",code:`Layout(content = content, modifier = modifier) { measurables, constraints ->
    // 1. Measure children — you choose their constraints
    //    (often loosened: minWidth = 0 so children can be small)
    val placeables = measurables.map {
        it.measure(constraints.copy(minWidth = 0, minHeight = 0))
    }

    // 2. Compute your layout's geometry from the measured sizes
    val height = placeables.sumOf { it.height }.coerceIn(
        constraints.minHeight, constraints.maxHeight
    )

    // 3. Report size and place
    layout(constraints.maxWidth, height) {
        var y = 0
        placeables.forEach { p ->
            p.place(x = 0, y = y)
            y += p.height
        }
    }
}`},{t:"list",items:["**Decisions you own**: what constraints children get (pass yours through, loosen them, or fix them — this *is* your layout's behavior), and the placement math. Everything is plain Kotlin over `Placeable` sizes — no MeasureSpec bit-twiddling.","**Edge cases that show seniority**: respect min constraints when reporting size (`coerceIn`) or parents may reject it; use `placeRelative` for automatic RTL mirroring; density is in scope (`8.dp.roundToPx()`); and if your algorithm needs to know one child's size *before deciding what else to compose* — that's not Layout anymore, that's `SubcomposeLayout`.","**When asked 'why not just nest Rows/Columns?'**: custom Layout is for genuinely novel geometry (flow layouts, circular menus, staggered arrangements, text-baseline systems) — nesting stays the answer for anything expressible with the standard vocabulary, since single-pass measurement makes it free."]}]},{level:"senior",q:"What are intrinsic measurements, why do they exist if Compose is single-pass, and what do they cost?",a:[{t:"p",text:"**The problem they solve**: single-pass measurement means a parent can't measure a child, look at the result, and re-measure others accordingly. But some designs need exactly that shape of information — the classic: a Row of two texts separated by a divider, where the divider should match the *taller text's* height. The Row can't know that height until children measure, and can't re-measure the divider after. Deadlock with the one-measure rule."},{t:"p",text:"**The mechanism**: intrinsics are a separate, lightweight *query* pass that doesn't count as measurement. Every layout node can answer four questions — min/max intrinsic width for a given height, min/max intrinsic height for a given width — computed recursively from its children (`Text` answers from its text metrics, a Row sums children's widths, etc.). `Modifier.height(IntrinsicSize.Max)` on the Row asks children \"what's the largest height you'd want?\", takes the max, and then runs the *real* single measure pass with that height as a fixed constraint. One query walk + one measure walk — the invariant survives."},{t:"list",items:["**Cost**: an extra recursive tree walk under the intrinsic-modified node, potentially per layout pass. Fine localized (a card, a row); a smell if wrapping whole screens or appearing inside frequently re-laid-out ancestors.","**Accuracy caveat**: intrinsics are *estimates* computed outside real constraints — custom layouts that don't implement them sensibly (default falls back to measuring behavior approximations) can give wrong answers; if you write a serious custom Layout, consider overriding the intrinsic methods too.","**Alternatives to name**: restructure so the design doesn't need it (often possible); `SubcomposeLayout` when the *composition itself* should depend on a measured size (heavier); or fixed heights from the design system when honesty allows."]}]},{level:"senior",q:"SubcomposeLayout: what is it, where does Compose itself use it, and why should you be reluctant to use it?",a:[{t:"p",text:"**The concept**: normally, *composition* (running your composables) fully finishes before *layout* (measuring) begins — layout can only rearrange what composition already created. `SubcomposeLayout` breaks that ordering deliberately: it lets you delay composing part of your content until **measure time**, so you can measure something first and then decide *what to compose* — even feeding one child's measured size into another child's composition as a parameter."},{t:"list",items:["**Where the framework uses it**: `LazyColumn`/`LazyRow`/grids — they can't compose a million items, so at measure time (knowing the viewport size) they subcompose only the visible slice, item by item, until the viewport fills. `Scaffold` — measures top/bottom bars first, then subcomposes content with the bars' sizes available as `paddingValues`. `BoxWithConstraints` — your lambda literally runs at measure time, which is how it can hand you real constraints during composition.","**Why be reluctant**: cost and contract. Subcomposition runs composition machinery inside the measure phase — heavier than a normal measure, defeats some skipping optimizations, and content inside composes *later* than siblings (observable if you do effects at odd times). Most 'I need the size' impulses have cheaper answers: `BoxWithConstraints` gives available space; `onSizeChanged`/`onGloballyPositioned` report sizes after layout (fine for non-layout reactions); intrinsics handle match-the-sibling sizing.","**The interview litmus**: reach for SubcomposeLayout only when *what to compose* genuinely depends on a measurement — building a lazy container, a 'measure A, size B from it' widget pair. If the composition set is fixed and only geometry varies, it's a custom `Layout`."]}]},{level:"senior",q:"Explain the Modifier.Node system — what actually happens to a modifier chain at runtime, and why was the older composed {} approach replaced?",a:[{t:"p",text:"**The concept**: `Modifier.padding(8.dp).clickable{}.background(Blue)` is, at face value, an immutable linked description — cheap to build, but something must turn it into working behavior on the UI tree. The modern runtime does this with **Modifier.Node**: each modifier *element* in the chain materializes a persistent **node** object attached to the composable's LayoutNode. Nodes are typed by capability — `LayoutModifierNode` participates in measurement, `DrawModifierNode` wraps drawing, `PointerInputModifierNode` joins hit-testing, `SemanticsModifierNode` contributes accessibility — and the chain of nodes is what the layout/draw/input systems actually traverse."},{t:"p",text:"**The key property is persistence across recomposition**: when the chain 'changes', the runtime **diffs** the new element list against the existing nodes — unchanged elements keep their node (with all its internal state: gesture-in-progress, animation, focus), changed elements get an in-place `update()`, only added/removed elements create/destroy nodes. So recomposing with an equivalent chain costs near zero, and stateful modifiers don't lose state on recomposition."},{t:"p",text:"**Why `composed {}` was retired for framework use**: the old way to make a stateful modifier was `Modifier.composed { remember { ... } }` — a modifier factory invoking *composition* per use site. That meant every element of every chain ran its own little composition (slot table entries, remember lookups), materialized fresh on every recomposition, unskippable, and unresolvable at chain-construction time. Measurably slow at scale — clickable/padding on thousands of nodes added real frame cost. Modifier.Node moves the state *into the node object* (no composition involved), enabling the diff-and-reuse machinery above; framework modifiers were migrated and custom stateful modifiers should use the `ModifierNodeElement` API today. Knowing this migration story — and that `composed {}` still works but is the slow path — is a genuine depth signal."}]},{level:"senior",q:"A design needs text aligned to another element's baseline, and a divider matching the tallest sibling. Which tools solve relational layout in Compose, and when is ConstraintLayout actually justified?",a:[{t:"list",items:["**Baseline alignment**: Row's `verticalAlignment = Alignment.CenterVertically` is wrong for text pairs of different sizes — use `Modifier.alignByBaseline()` on the texts (Row scope), or `alignBy(FirstBaseline)`/`paddingFrom(FirstBaseline, ...)` for distance-from-baseline specs straight from redlines. Baselines propagate through the layout system as `AlignmentLine`s — custom layouts can expose their own.","**Match-the-tallest sibling**: `Modifier.height(IntrinsicSize.Max)` on the Row + `fillMaxHeight()` on the divider — the intrinsic query gets the max desired height, the real pass fixes everyone to it.","**Percentage/guideline positioning**: fractional sizes (`fillMaxWidth(0.4f)`), weights, or `BoxWithConstraints` arithmetic cover most 'guideline at 30%' cases without any library.","**Overlap and anchoring**: Box with `align` + `offset`; `zIndex` for stacking order."]},{t:"p",text:"**ConstraintLayout's honest niche**: in Views it existed to flatten hierarchies because nesting was expensive — that rationale is void in Compose (single-pass measurement). It remains justified when the design is *inherently relational in ways the above don't express*: barriers (position after the longest of several elements), chains with complex distribution, many elements constrained to each other where nesting would obscure the relationships, or porting an existing complex XML screen where the constraint graph is the clearest spec. Even then it's a normal library choice, not a performance one — and that reframing ('in Compose, ConstraintLayout is about expressiveness, never performance') is exactly the sentence interviewers wait for."}]},{level:"junior",q:"Which Modifiers should every Compose developer know by heart?",a:[{t:"p",text:"Modifiers decorate a composable — sizing, spacing, background, click handling, and more. A handful cover the vast majority of UI. Knowing them (and that *order matters*) lets you build most layouts without reaching for anything exotic."},{t:"list",items:["**Sizing** — `size`, `fillMaxWidth`/`fillMaxHeight`/`fillMaxSize`, `width`/`height`, `weight` (in Row/Column), `wrapContentSize`.","**Spacing** — `padding` (there's no margin; padding + a `Spacer` do it all).","**Appearance** — `background`, `clip`, `border`, `alpha`, `graphicsLayer`.","**Interaction** — `clickable`, `pointerInput`, `scrollable`/`verticalScroll`.","**Positioning** — `offset`, `align` (in a Box scope), `zIndex`.","**Measurement callbacks** — `onGloballyPositioned`, `onSizeChanged`."]},{t:"code",title:"A typical chain",code:`Text(
    "Hi",
    modifier = Modifier
        .fillMaxWidth()
        .padding(16.dp)          // outer space (order matters!)
        .background(Color.Blue)  // painted inside the padding
        .clip(RoundedCornerShape(8.dp))
        .clickable { onClick() }
        .padding(12.dp),         // inner space between bg edge and text
)`},{t:"note",text:"Core Modifiers: sizing (size/fillMax*/weight), spacing (padding + Spacer — no margins), appearance (background/clip/border), interaction (clickable/pointerInput), positioning (offset/align/zIndex). Order matters — each wraps the next."}]},{level:"junior",q:"What's the difference between size, requiredSize, fillMaxSize, and wrapContentSize?",a:[{t:"p",text:"These all affect sizing but interact with parent *constraints* differently. The key insight: `size` is a *preference* the parent can override via constraints, `requiredSize` *forces* a size ignoring parent constraints, `fillMaxSize` takes all available space, and `wrapContentSize` shrinks to content and can re-center within a larger bound."},{t:"list",items:["**`size(100.dp)`** — asks for 100dp, but the parent's constraints win; if the parent forces min 200dp, you get 200dp.","**`requiredSize(100.dp)`** — forces exactly 100dp even if it violates parent constraints (the child may then be clipped or overflow). Use sparingly.","**`fillMaxSize()`** — expand to the maximum the parent allows (needs a bounded parent; inside an infinite-height scroll it can crash/misbehave).","**`wrapContentSize()`** — measure the child at its content size within the incoming constraints, then position it (default center) in the leftover space — handy to center a small child inside a `fillMaxSize` parent."]},{t:"code",title:"size vs requiredSize under a constraining parent",code:`Box(Modifier.size(50.dp)) {                 // parent forces 50dp
    Box(Modifier.size(100.dp))              // -> becomes 50dp (constraint wins)
    Box(Modifier.requiredSize(100.dp))      // -> stays 100dp (overflows the 50dp box)
}`},{t:"note",text:"size = preferred (parent constraints can override); requiredSize = forced (ignores parent, may overflow); fillMaxSize = take all available (needs bounded parent); wrapContentSize = shrink to content and re-position in leftover space. 'size can be overridden' trips people up constantly."}]},{level:"junior",q:"How do Constraints work in Compose layout (min/max, bounded, infinity)?",a:[{t:"p",text:"`Constraints` are the bounds a parent passes to a child during measurement: `minWidth`, `maxWidth`, `minHeight`, `maxHeight`. The child must choose a size *within* these bounds. This is the core of Compose's single-pass layout: constraints flow *down*, chosen sizes flow *up*."},{t:"list",items:["**Bounded** — a finite max (e.g. maxWidth = screen width). `fillMaxWidth` picks maxWidth.","**Unbounded / infinity** — `Constraints.Infinity` max (e.g. the child of a vertically scrolling Column has infinite max height — it can be as tall as it wants). `fillMaxHeight` in an infinite-height parent is meaningless/crashes, which is why you can't put a `fillMaxSize` or nested `LazyColumn` directly in a `verticalScroll` Column.","**Exact (tight) constraints** — min == max, forcing a specific size (what `size()` produces for its child when it resolves).","**Modifiers reshape constraints** — `padding` shrinks the max passed down; `size` tightens them; `fillMax*` reads the max."]},{t:"code",title:"Reading constraints in a custom layout",code:`Layout(content) { measurables, constraints ->
    // constraints.maxWidth may be a finite number or Constraints.Infinity
    val childConstraints = constraints.copy(minWidth = 0)  // loosen min
    val placeables = measurables.map { it.measure(childConstraints) }
    layout(constraints.maxWidth, placeables.maxOf { it.height }) { /* place */ }
}`},{t:"note",text:"Constraints (min/max width & height) flow down; chosen sizes flow up — one pass. Bounded = finite max; unbounded = Constraints.Infinity (a scroll child's cross-axis). fillMax* in an infinite dimension is undefined — that's why fillMaxHeight/nested LazyColumn breaks inside verticalScroll."}]},{level:"junior",q:"When do you use Box vs Row vs Column?",a:[{t:"p",text:"These are the three fundamental layout composables. `Column` stacks children vertically, `Row` stacks them horizontally, and `Box` overlaps children on top of each other (z-stacked). Choosing among them is the first decision in any layout."},{t:"list",items:["**`Column`** — vertical stack. Use `verticalArrangement` (spacing/distribution along the main axis) and `horizontalAlignment` (cross-axis).","**`Row`** — horizontal stack. `horizontalArrangement` + `verticalAlignment`.","**`Box`** — overlap children; later children draw on top. Use `contentAlignment` and per-child `Modifier.align()` to position. Great for badges, overlays, backgrounds behind content, centering a single child."]},{t:"code",title:"Box for overlap, Column/Row for stacks",code:`Box(contentAlignment = Alignment.Center) {          // overlap + center
    Image(...)                                       // background layer
    CircularProgressIndicator()                      // on top, centered
    Text("NEW", Modifier.align(Alignment.TopEnd))    // badge in a corner
}
Column(verticalArrangement = Arrangement.spacedBy(8.dp)) { /* vertical list */ }
Row(horizontalArrangement = Arrangement.SpaceBetween) { /* horizontal bar */ }`},{t:"note",text:"Column = vertical stack, Row = horizontal stack, Box = overlapping z-stack (later children on top, align per child). Box is the go-to for overlays, badges, backgrounds, and centering a single child. They're the flex/frame equivalents of Compose."}]},{level:"junior",q:"How does Modifier.clickable work, and how do you customize or remove the ripple?",a:[{t:"p",text:"`Modifier.clickable` makes any composable respond to taps: it adds click handling, a ripple indication, accessibility semantics (role, focus), and keyboard/D-pad support. Because it's a modifier, you can make *anything* clickable — a Box, an Image, a Row — not just buttons."},{t:"code",title:"clickable and controlling the ripple",code:`// Default: includes ripple + a11y
Modifier.clickable { onClick() }

// With role/label for accessibility
Modifier.clickable(
    onClickLabel = "Open profile",
    role = Role.Button,
) { onClick() }

// Remove the ripple (e.g. for a custom-drawn press effect):
val interaction = remember { MutableInteractionSource() }
Modifier.clickable(
    interactionSource = interaction,
    indication = null,     // no ripple
) { onClick() }`},{t:"list",items:["**Adds more than onClick** — ripple `indication`, semantics (`Role.Button`), focus, and enabled state.","**Customize the ripple** — pass a custom `indication` (e.g. `ripple(bounded = false, color = ...)`), or `null` to disable.","**Use `combinedClickable`** for long-press/double-tap; use `pointerInput` + `detectTapGestures` for fully custom gesture handling.","**Accessibility caveat** — if you strip semantics or use raw `pointerInput`, you may lose the button role/label; add `Modifier.semantics` back."]},{t:"note",text:"Modifier.clickable adds tap handling + ripple + accessibility (role/focus/keyboard) to any composable. Customize via indication (custom ripple or null to remove) and an InteractionSource; combinedClickable for long-press/double-tap. Don't lose a11y semantics with raw pointerInput."}]},{level:"junior",q:"How do clip, background, and border interact, and why does their order matter?",a:[{t:"p",text:"`clip(shape)` restricts drawing (and touch) to a shape; `background(color, shape)` paints a shape; `border(width, color, shape)` draws an outline. Because modifiers wrap outward-in, the order determines whether the background/border respects the clip and where padding sits relative to them."},{t:"code",title:"Ordering shapes correctly",code:`// Rounded card: clip first so background AND content are rounded
Modifier
    .clip(RoundedCornerShape(12.dp))   // everything after is clipped to rounded
    .background(Color.White)
    .border(1.dp, Color.Gray, RoundedCornerShape(12.dp))
    .padding(16.dp)                    // inner content padding

// background(color, shape) is shorthand that paints a shape without clipping content
Modifier.background(Color.White, RoundedCornerShape(12.dp))`},{t:"list",items:["**`clip` before `background`/content** — so both the fill and the children are rounded. Clipping after background rounds nothing you already painted.","**`background(color, shape)`** paints a shape but does *not* clip child content — use `clip` if children (like an Image) must be rounded too.","**Padding position** — padding *before* background = space outside the colored area; padding *after* = space between the background edge and content."]},{t:"note",text:"clip restricts drawing to a shape; background paints one; border outlines one. Put clip first so background AND children are clipped to the shape. background(color, shape) paints but doesn't clip content — use clip for rounded images. Padding before vs after background changes what's spaced."}]},{level:"junior",q:"How do you build a responsive layout that adapts to available width?",a:[{t:"p",text:"Compose gives you the available space at layout time, and you adapt by reading it. The main tools are `BoxWithConstraints` (exposes the incoming constraints so you can branch on `maxWidth`), `WindowSizeClass` (standardized breakpoints for phone/tablet/desktop), and adaptive layout APIs. You branch on size to choose one-pane vs two-pane, grid columns, etc."},{t:"code",title:"BoxWithConstraints and WindowSizeClass",code:`BoxWithConstraints {
    if (maxWidth < 600.dp) {
        SinglePaneList()          // phone
    } else {
        Row { ListPane(); DetailPane() }   // tablet: two panes
    }
}

// App-level: WindowSizeClass gives semantic breakpoints
val widthClass = windowSizeClass.widthSizeClass
when (widthClass) {
    WindowWidthSizeClass.Compact -> CompactLayout()
    WindowWidthSizeClass.Medium, WindowWidthSizeClass.Expanded -> WideLayout()
}`},{t:"list",items:["**`BoxWithConstraints`** — local, gives `minWidth`/`maxWidth`/etc. of *this* slot; good for a single component's responsiveness. Note it uses SubcomposeLayout, so it's slightly heavier.","**`WindowSizeClass`** — app/screen-level standardized breakpoints (Compact/Medium/Expanded); better for top-level navigation decisions.","**Adaptive APIs** — `androidx.compose.material3.adaptive` (list-detail, supporting-pane scaffolds) for canonical responsive patterns."]},{t:"note",text:"Adapt to size with BoxWithConstraints (local maxWidth branching — uses SubcomposeLayout, slightly heavier) or WindowSizeClass (app-level Compact/Medium/Expanded breakpoints for navigation). Material3 adaptive scaffolds give canonical list-detail patterns. Branch on size, don't hardcode."}]},{level:"junior",q:"Why can't you read a composable's size during composition, and how do you get it?",a:[{t:"p",text:"Composition (building the tree) happens *before* layout (measuring/placing), so at composition time nothing has a size yet — the size doesn't exist until the layout phase runs. To get an actual pixel size you must wait for layout and read it via a callback modifier, then (if you need to react in UI) store it in state."},{t:"code",title:"onSizeChanged / onGloballyPositioned",code:`var sizePx by remember { mutableStateOf(IntSize.Zero) }
Box(
    Modifier.onSizeChanged { sizePx = it }   // called after layout, gives the measured size
) { /* content */ }

// onGloballyPositioned also gives position in window/root coordinates:
Modifier.onGloballyPositioned { coords -> val bounds = coords.boundsInWindow() }`},{t:"list",items:["**Phase ordering** — composition → layout → draw. Size is a layout-phase result, unknown during composition.","**`onSizeChanged`** — gives the measured size after layout; store it in state to use it in composition next frame.","**`onGloballyPositioned`** — gives size *and* position (coordinates), useful for anchoring popups or measuring relative positions.","**Beware feedback loops** — reading size into state that changes size can loop; and this adds a frame of latency. Prefer a custom `Layout` or intrinsics when you need size *during* layout, not after."]},{t:"note",text:"Size is a layout-phase result; composition runs first, so no size exists yet. Read it after layout via onSizeChanged (size) or onGloballyPositioned (size + position), storing in state. Watch for feedback loops and the one-frame lag; use a custom Layout/intrinsics when you need size during layout."}]},{level:"junior",q:"What does Modifier.graphicsLayer do, and when should you prefer it?",a:[{t:"p",text:"`Modifier.graphicsLayer` applies transformations (translation, scale, rotation, alpha, clip, shadow) by drawing the composable into a separate render layer. Crucially, these transforms happen in the *draw* phase without re-triggering layout — so animating them is cheap. It's the performant way to animate movement, fades, and scaling."},{t:"code",title:"graphicsLayer for cheap animation",code:`val scale by animateFloatAsState(if (pressed) 0.95f else 1f)
Box(
    Modifier.graphicsLayer {
        scaleX = scale; scaleY = scale     // draw-phase only — no relayout per frame
        alpha = 0.9f
        rotationZ = 10f
    }
)
// vs Modifier.offset/size in an animation -> re-runs layout every frame (more costly)`},{t:"list",items:["**Draw-phase transforms** — translation/scale/rotation/alpha skip layout, so per-frame animation is cheap (no remeasure).","**Lambda form** — `graphicsLayer { }` reads animated state *inside* the lambda, deferring the state read to the draw phase (avoids recomposition/relayout).","**Also does** — `clip = true` + `shape`, `shadowElevation`, `compositingStrategy` (offscreen buffer for alpha over overlapping content).","**Prefer over `offset`/`size` for animation** — animating `offset`/`size` re-runs layout each frame; `graphicsLayer.translationX`/`scale` doesn't."]},{t:"note",text:"graphicsLayer applies scale/rotation/translation/alpha/shadow in the DRAW phase — no relayout, so animating it is cheap. Use the lambda form to defer the state read to draw. Prefer graphicsLayer.translationX/scale over animating offset/size (which re-runs layout every frame)."}]},{level:"junior",q:"How do you conditionally apply modifiers, and what is Modifier.then?",a:[{t:"p",text:"Because a `Modifier` is an immutable, chainable value, you can build it conditionally like any expression. `Modifier.then(other)` concatenates two modifier chains. The clean pattern is to conditionally append a modifier (or `Modifier` — the no-op identity — when the condition is false)."},{t:"code",title:"Conditional modifiers",code:`Modifier
    .fillMaxWidth()
    .then(if (selected) Modifier.border(2.dp, Color.Blue) else Modifier)  // Modifier = no-op

// A common helper for readability:
fun Modifier.conditional(condition: Boolean, block: Modifier.() -> Modifier) =
    if (condition) this.then(block()) else this

Modifier.conditional(selected) { border(2.dp, Color.Blue) }`},{t:"list",items:["**`Modifier` (bare) is the identity** — a no-op you can substitute when a condition is false.","**`then`** concatenates chains; useful for merging a passed-in `modifier` parameter with local ones — always apply the caller's `modifier` at the right spot.","**Watch ordering** — where you insert the conditional modifier in the chain still matters (padding vs background etc.).","**Don't over-engineer** — a plain `if (x) Modifier.a() else Modifier` inside `.then()` is perfectly idiomatic."]},{t:"note",text:"Modifiers are immutable values, so build them conditionally: `.then(if (cond) Modifier.x() else Modifier)` — bare `Modifier` is the no-op identity. A `Modifier.conditional{}` extension reads nicely. Ordering within the chain still matters."}]},{level:"junior",q:"What's the difference between offset and padding for moving a composable?",a:[{t:"p",text:"Both can shift a composable, but they work in different phases and have different effects on layout. `padding` reserves space and pushes neighbors (it changes the element's laid-out bounds); `offset` moves the element *visually* during placement without affecting the space it reserved, so it can overlap neighbors."},{t:"list",items:["**`padding`** — layout-affecting; adds space that displaces siblings and shrinks the child's available area. Use for genuine spacing.","**`offset(x, y)`** — placement-phase shift; the element occupies its original slot but is drawn shifted, so it can overlap adjacent content. Use for nudging/overlap effects.","**`absoluteOffset`** — like offset but ignores layout direction (LTR/RTL).","**`graphicsLayer { translationX = }`** — draw-phase move, best for *animated* movement (cheapest, no relayout)."]},{t:"code",title:"offset overlaps; padding displaces",code:`Modifier.padding(start = 20.dp)   // pushes the element right, moves neighbors
Modifier.offset(x = 20.dp)        // draws it 20dp right, neighbors unaffected (may overlap)
Modifier.graphicsLayer { translationX = animatedX }  // animated move, no relayout`},{t:"note",text:"padding reserves space and displaces siblings (layout-affecting); offset shifts placement visually without changing reserved space (can overlap); graphicsLayer.translationX is the cheapest for animated movement (draw-phase). Pick by whether neighbors should move."}]},{level:"senior",q:"How do you create a reusable custom modifier, and why prefer Modifier.Node over composed{}?",a:[{t:"p",text:"A reusable modifier is just an extension function on `Modifier` that appends behavior. For *stateless* combinations, a plain factory function is enough. For modifiers that need their own state, coordinate access, or lifecycle, the modern approach is a `Modifier.Node` implementation, which replaced the older `composed { }` factory because `composed` was slow (it created a composition per usage and defeated skipping/reuse)."},{t:"code",title:"Simple factory vs Modifier.Node",code:`// Simple, stateless: just chain existing modifiers
fun Modifier.card() = this
    .clip(RoundedCornerShape(12.dp))
    .background(Color.White)
    .padding(16.dp)

// Stateful / needs draw or pointer access: Modifier.Node (modern, performant)
// class MyNode : Modifier.Node(), DrawModifierNode { override fun ContentDrawScope.draw() {...} }
// then a ModifierNodeElement to create/update it.`},{t:"list",items:["**Plain extension function** — for composing existing modifiers with no new state. Cheap, idiomatic.","**`Modifier.Node` + `ModifierNodeElement`** — for custom draw/layout/pointer/focus behavior or per-usage state. Nodes are allocated once and *updated* in place (not recreated), avoiding recomposition.","**Why not `composed { }`** — it ran a composable per modifier application, breaking modifier reuse/skipping and hurting performance. `Modifier.Node` is the official replacement."]},{t:"note",text:"Reusable modifier = extension fn on Modifier. Stateless combos: a plain factory. Stateful/custom draw-layout-pointer: Modifier.Node + ModifierNodeElement (allocated once, updated in place). Avoid the old composed{} — it spun up a composition per use and killed reuse/skipping."}]},{level:"junior",q:"What is Modifier.matchParentSize inside a Box, and how does it differ from fillMaxSize?",a:[{t:"p",text:"Inside a `Box`, `Modifier.matchParentSize()` sizes a child to match the Box's size *without influencing* that size. `fillMaxSize()` fills the max available and *does* participate in measuring. The difference matters when the Box sizes itself to its content: `matchParentSize` won't inflate the Box, `fillMaxSize` can."},{t:"code",title:"matchParentSize for backgrounds",code:`Box {
    // This child DECIDES the Box size (e.g. the content):
    Text("Some content of variable size")
    // A background that matches the Box without affecting its size:
    Box(Modifier.matchParentSize().background(Color.LightGray))   // sits behind, same size
    // fillMaxSize() here would try to fill max constraints and could blow up the Box
}`},{t:"list",items:["**`matchParentSize`** — Box-scope only; measured *after* the Box knows its size, so it matches without contributing to sizing. Perfect for a background layer behind content.","**`fillMaxSize`** — participates in measurement, filling the incoming max constraints; if it's the only child, the Box becomes max-sized.","**Draw order** — put the background child before content (earlier = behind), or it covers the content."]},{t:"note",text:"matchParentSize (Box-scope) sizes a child to the Box WITHOUT affecting the Box's size — ideal for a background behind variable content. fillMaxSize participates in measuring and fills max constraints (can inflate the Box). Order children so the background draws first."}]},{level:"junior",q:"How do you handle safe areas, insets, and the status/navigation bars in Compose?",a:[{t:"p",text:"With edge-to-edge display (default on modern Android), your content draws behind the system bars, so you must apply *window insets* to avoid content hiding under the status bar, navigation bar, or IME (keyboard). Compose exposes insets via the `WindowInsets` API and helper modifiers."},{t:"code",title:"Applying insets",code:`// Pad content away from system bars:
Modifier.windowInsetsPadding(WindowInsets.systemBars)
// Common shortcuts:
Modifier.statusBarsPadding()
Modifier.navigationBarsPadding()
Modifier.imePadding()              // move content above the keyboard
Modifier.safeDrawingPadding()      // all of the above combined

// Scaffold applies insets for you and passes contentPadding:
Scaffold { innerPadding -> Content(Modifier.padding(innerPadding)) }`},{t:"list",items:["**Edge-to-edge is the default** — `enableEdgeToEdge()`; content goes behind bars, so insets are your responsibility.","**Inset modifiers** — `statusBarsPadding`, `navigationBarsPadding`, `imePadding`, `safeDrawingPadding`, or the general `windowInsetsPadding(insets)`.","**`Scaffold` handles most of it** — apply the `innerPadding` it gives you; ignoring it causes content under the app bar.","**`consumeWindowInsets`** — prevents double-applying insets in nested scrollables."]},{t:"note",text:"Edge-to-edge draws behind system bars, so apply WindowInsets: statusBarsPadding/navigationBarsPadding/imePadding/safeDrawingPadding, or windowInsetsPadding(insets). Scaffold gives innerPadding — always apply it. Use consumeWindowInsets to avoid double-padding in nested scrolls."}]},{level:"junior",q:"What does Modifier.zIndex do, and how is draw order determined in Compose?",a:[{t:"p",text:"By default, Compose draws children in *declaration order* — later children (and later modifiers) draw on top. `Modifier.zIndex(z)` overrides this within a layout, letting a child draw above siblings regardless of its declaration order, without reordering the layout itself."},{t:"list",items:["**Default order = declaration order** — in a Box, the last-declared child is on top. Often the simplest fix is just reordering children.","**`Modifier.zIndex(z)`** — higher z draws later (on top); affects *drawing* within the same parent, not measurement/placement.","**Scope** — zIndex only compares siblings in the same layout; it doesn't let a child escape its parent's stacking.","**Elevation/shadow** — `graphicsLayer { shadowElevation = }` or Material elevation also creates visual depth (and can affect draw order via elevation overlays)."]},{t:"code",title:"zIndex to lift a child",code:`Box {
    Card(Modifier.zIndex(1f)) { /* draws above */ }
    Overlay()                       // declared later but drawn below due to zIndex above
}`},{t:"note",text:"Draw order defaults to declaration order (later = on top). Modifier.zIndex(z) overrides draw order among siblings (higher = on top) without reordering layout. It only compares siblings in the same parent. Often just reordering children is simpler than zIndex."}]},{level:"senior",q:"How do alignment lines (like baselines) work, and how do you use them for relational layout?",a:[{t:"p",text:"An *alignment line* is a horizontal or vertical line a composable exposes so parents can align other composables to it — the most common being text `FirstBaseline`/`LastBaseline`. Layouts read children's alignment lines during measurement and can position siblings relative to them, enabling things like aligning a label's baseline to a value's baseline."},{t:"code",title:"Aligning to a baseline",code:`Row {
    Text("42", fontSize = 32.sp, modifier = Modifier.alignByBaseline())
    Text("points", fontSize = 14.sp, modifier = Modifier.alignByBaseline())  // baselines line up
}

// Custom: read a child's baseline in a Layout
Layout(content) { measurables, constraints ->
    val p = measurables.first().measure(constraints)
    val baseline = p[FirstBaseline]        // access the alignment line value
    layout(p.width, p.height) { p.place(0, 0) }
}`},{t:"list",items:["**`alignByBaseline()`** in a Row aligns children's text baselines — crucial for mixed font sizes looking correct.","**`paddingFromBaseline`** sets distance from a baseline (Material spacing specs use this).","**Custom alignment lines** — you can define your own with `AlignmentLine` and expose them from a custom layout for advanced relational positioning.","**Reading them costs a measure** — accessing an alignment line forces measuring that child, so use judiciously."]},{t:"note",text:"Alignment lines (esp. text FirstBaseline/LastBaseline) let parents position siblings relative to a child's line. Use alignByBaseline() in a Row for mixed font sizes, paddingFromBaseline for spec spacing. You can define custom AlignmentLines. Reading one forces measuring that child."}]},{level:"junior",q:"What is Spacer, and when do you use it versus padding or Arrangement.spacedBy?",a:[{t:"p",text:"`Spacer` is an empty composable that just occupies space — you give it a size and it creates a gap. It's one of three ways to create spacing (alongside `padding` and `Arrangement.spacedBy`), each suited to a different situation."},{t:"list",items:["**`Spacer(Modifier.height(8.dp))`** — an explicit, one-off gap between two specific elements. Also `Spacer(Modifier.weight(1f))` to push elements apart (fill remaining space).","**`Arrangement.spacedBy(8.dp)`** — uniform gaps between *all* children of a Row/Column; cleaner than a Spacer between each item.","**`padding`** — space around a single element (inside or outside), independent of siblings."]},{t:"code",title:"Three spacing tools",code:`Column(verticalArrangement = Arrangement.spacedBy(8.dp)) {   // uniform gaps
    ItemA(); ItemB(); ItemC()
}
Row {
    Text("Left")
    Spacer(Modifier.weight(1f))   // push next item to the far right
    Text("Right")
}`},{t:"note",text:"Spacer is an empty sized composable for one-off gaps (or Spacer(Modifier.weight(1f)) to push items apart). Prefer Arrangement.spacedBy for uniform gaps between all children; padding for space around a single element. There are no margins — these three cover all spacing."}]},{level:"senior",q:"How does Modifier.layout work, and how is it different from the Layout composable?",a:[{t:"p",text:"`Modifier.layout { measurable, constraints -> }` lets you customize the measurement and placement of a *single* composable inline, without writing a whole custom `Layout`. You measure the (one) incoming measurable, then report a size and place it — adjusting position or reported size. Use it for one-off tweaks; use the `Layout` composable when you're arranging *multiple* children."},{t:"code",title:"Modifier.layout for a custom padding-like effect",code:`fun Modifier.firstBaselineToTop(top: Dp) = layout { measurable, constraints ->
    val placeable = measurable.measure(constraints)
    val baseline = placeable[FirstBaseline]
    val placeableY = top.roundToPx() - baseline
    val height = placeable.height + placeableY
    layout(placeable.width, height) { placeable.placeRelative(0, placeableY) }
}`},{t:"list",items:["**`Modifier.layout`** — one measurable in, one placeable out; customize a single element's size/position inline.","**`Layout` composable** — multiple children (`measurables`), you measure and place all of them; for building new layout containers (a custom flow row, a radial menu).","**Both follow the same contract** — measure children within constraints, call `layout(width, height) { place... }`."]},{t:"note",text:"Modifier.layout customizes ONE composable's measure/placement inline (one measurable → one placeable). The Layout composable arranges MULTIPLE children into a new container. Same contract (measure within constraints, then layout(w,h){ place }). Reach for Modifier.layout for one-off tweaks."}]},{level:"senior",q:"What is contentPadding in scrollable containers, and why not just use Modifier.padding?",a:[{t:"p",text:"Scrollable containers like `LazyColumn` take a `contentPadding` parameter that adds padding *inside* the scroll area — around the items — while still letting items scroll into that padded region. `Modifier.padding` on the LazyColumn instead pads the *whole viewport*, clipping items at the padded edge so they can't scroll through it."},{t:"list",items:["**`contentPadding`** — space at the start/end (and sides) of the scrollable content; the first/last items can scroll fully into view *past* the padding, and content is clipped at the true container edge (nice fade-at-edge behavior).","**`Modifier.padding`** — shrinks the scroll viewport itself; items are clipped at the padded boundary, so you lose the 'scroll under the edge' effect and the first item starts inset permanently.","**Common use** — `contentPadding = PaddingValues(vertical = 16.dp)` gives breathing room at top/bottom of a list without clipping mid-scroll; also used to offset for a floating app bar / FAB."]},{t:"code",title:"contentPadding vs Modifier.padding",code:`LazyColumn(
    contentPadding = PaddingValues(16.dp),   // items scroll through this space, clipped at edge
) { items(list) { Row(it) } }

// vs — pads the viewport, items clipped at 16dp inset, no scroll-under:
LazyColumn(Modifier.padding(16.dp)) { /* ... */ }`},{t:"note",text:"contentPadding pads INSIDE a scrollable — items scroll through it and clip at the true edge (breathing room top/bottom, offset for FAB/app bar). Modifier.padding shrinks the viewport, clipping items at the inset and killing the scroll-under effect. Use contentPadding for list edges."}]}],Xx={id:"modifiers-layout",title:"Modifiers & Layout",tagline:"Modifier chains and ordering, the constraints model, Row/Column/Box fluency, custom Layouts, intrinsics, SubcomposeLayout, and Modifier.Node internals.",tags:["Compose","Modifiers","Constraints","Custom Layout","Intrinsics"],content:Qx,qa:$x},Zx=[{heading:"Why lazy lists exist — the RecyclerView problem, solved differently",blocks:[{t:"p",text:"A list of 10,000 items can't create 10,000 UI elements — memory and time forbid it. The View system solved this with **RecyclerView**: a machine that *recycles* a small pool of ViewHolders, rebinding them as you scroll (with adapters, view types, DiffUtil — a lot of ceremony). Compose solves the same problem with **lazy composition**: `LazyColumn` composes **only the items currently visible** (plus a small buffer), and as you scroll, items leaving the viewport are *disposed* while entering ones are *composed*. No adapter, no ViewHolder, no DiffUtil — the item lambda is the whole API."},{t:"code",title:"The core API",code:`LazyColumn(
    state = rememberLazyListState(),
    contentPadding = PaddingValues(16.dp),
    verticalArrangement = Arrangement.spacedBy(8.dp),
) {
    item { Header() }                              // single item

    items(
        items = articles,
        key = { it.id },                           // STABLE identity — crucial
        contentType = { "article" },               // reuse hint
    ) { article ->
        ArticleCard(article)
    }

    item { Footer() }
}`},{t:"list",items:["**The DSL, not composables**: the `LazyColumn` block is a `LazyListScope` — `item { }` / `items(...)` are *descriptions* registered up front; the lambdas run only when their item scrolls into view. You cannot call arbitrary composables directly in the scope (compile error) — a common early confusion.","Under the hood it's built on **SubcomposeLayout**: at measure time, knowing the viewport, it subcomposes items one by one until the viewport (plus `beyondBoundsItemCount` buffer) is filled.","**Contrast with `Column + verticalScroll`**: that composes *every* child immediately — fine for a settings page of 15 rows, catastrophic for a feed. The decision rule: bounded, small, static content → scrollable Column; unbounded/large/dynamic → LazyColumn.","The family: `LazyRow`, `LazyVerticalGrid`/`LazyHorizontalGrid` (with `GridCells.Fixed(2)` / `Adaptive(120.dp)`), `LazyVerticalStaggeredGrid`, and `HorizontalPager`/`VerticalPager` (same lazy machinery, page-snapping behavior)."]}]},{heading:"Keys — the most important parameter you can pass",blocks:[{t:"p",text:"By default, an item's identity is its **position** (index). That's fine for a static list, and quietly wrong for anything that changes. Suppose item #0 is deleted: every remaining item shifts to a new index — to Compose, *every item changed*, so every visible item recomposes, and worse, **item state migrates to the wrong items**: the `remember`ed 'expanded' flag of old item 3 now belongs to whatever sits at index 3."},{t:"code",title:"What keys fix",code:`// WITHOUT key: delete one item ->
//  - every item after it recomposes (index identity shifted)
//  - remembered state (expanded flags, animations) attaches to WRONG items
//  - no sensible item animations possible

items(messages) { msg -> MessageRow(msg) }

// WITH key: identity follows the DATA ->
//  - unmoved items untouched, moved items keep their state
//  - animateItem() can animate reorders/insertions/deletions
items(messages, key = { it.id }) { msg ->
    MessageRow(msg, Modifier.animateItem())
}`},{t:"list",items:["**Key requirements**: stable (same item → same key across updates), unique within the list, and **saveable** (keys go into saved instance state so scroll position survives recreation — a data-class id is fine, an unparcelable object is not).","**Never use the index as the key** — that's just re-stating the default with extra steps.","Keys are also what `rememberLazyListState()` uses to keep **scroll position anchored to an item** when the list changes above the viewport: with keys, inserting items above doesn't visually jump the list.","`contentType` is the *other* hint: items of the same type can **reuse each other's node structure** when scrolled (Compose keeps a small reuse pool per type, RecyclerView-style but automatic) — meaningful for mixed feeds (ads vs posts); harmless to omit for uniform lists."]}]},{heading:"LazyListState — scroll position, programmatic control, derived facts",blocks:[{t:"code",title:"The state object and the standard patterns around it",code:`val listState = rememberLazyListState()
val scope = rememberCoroutineScope()

LazyColumn(state = listState) { /* ... */ }

// Programmatic scrolling (suspend — needs a coroutine)
Button(onClick = {
    scope.launch { listState.animateScrollToItem(0) }
}) { Text("Back to top") }

// Reacting to scroll WITHOUT recomposing every frame:
val showFab by remember {
    derivedStateOf { listState.firstVisibleItemIndex > 5 }
}

// Infinite scroll / load-more trigger:
val shouldLoadMore by remember {
    derivedStateOf {
        val last = listState.layoutInfo.visibleItemsInfo.lastOrNull()
        last != null && last.index >= listState.layoutInfo.totalItemsCount - 5
    }
}
LaunchedEffect(shouldLoadMore) {
    if (shouldLoadMore) viewModel.loadNextPage()
}`},{t:"list",items:["`rememberLazyListState()` survives recomposition *and* recreation (it's internally saveable — scroll position restores after rotation, keyed to item + offset).","`firstVisibleItemIndex` / `firstVisibleItemScrollOffset` change **every scroll frame** — reading them raw in composition recomposes continuously; wrap scroll-derived booleans in **`derivedStateOf`** (the canonical use case for it).","`layoutInfo` exposes the full visible-items snapshot (indices, offsets, sizes, viewport) — the basis for load-more triggers, scroll analytics, and 'current section' headers.","`scrollToItem` (instant) vs `animateScrollToItem` (animated) — both suspend functions, called from `rememberCoroutineScope()` in event handlers."]}]},{heading:"Performance rules for lazy lists",blocks:[{t:"list",items:["**Always provide keys** for changeable data — identity correctness *is* performance (prevents cascade recomposition on inserts/removals).","**Keep item composables cheap and skippable**: stable parameters (immutable item models), no heavy computation in item bodies — an item composes *during scroll*, on the frame deadline; expensive items = dropped frames at the scroll edge.","**Don't fetch/compute per item-composition**: item lambdas re-run whenever items scroll back into view — image loading must go through a caching loader (Coil handles this), formatting should live in the UI model, not the item body.",'**Never nest a lazy list inside a same-direction scrollable** (`LazyColumn` in `verticalScroll` Column) — infinite max-height constraints crash at runtime ("Vertically scrollable component was measured with an infinity maximum height"). Restructure: make everything items of ONE LazyColumn (`item { Header() }` + `items(feed)`), which is also the correct virtualization.',"**Cross-direction nesting is fine and idiomatic**: `LazyRow` inside `LazyColumn` items (Netflix-style carousels) — give the rows `contentType` and their own keys.","**Avoid 0-pixel initial layouts**: a LazyColumn inside a parent that measures it with zero height composes nothing, then everything when sized — a subtle jank source in dynamically-sized containers.","**Item animations**: `Modifier.animateItem()` (placement/appearance/disappearance) requires keys; without keys there is no 'same item moved' concept to animate.","In debug builds lazy scrolling is notably janky — **judge scroll performance only on release/R8 builds** (or at least with baseline profiles applied); this one sentence defuses a very common false alarm."]}]},{heading:"Sticky headers, multiple item types, and real feed structure",blocks:[{t:"code",title:"A realistic sectioned feed",code:`LazyColumn {
    contactsByLetter.forEach { (letter, contacts) ->
        stickyHeader(key = "header-" + letter) {   // pins until next header pushes it
            LetterHeader(letter)
        }
        items(contacts, key = { it.id }, contentType = { "contact" }) {
            ContactRow(it)
        }
    }

    item(contentType = "loader") {
        if (isLoadingMore) LoadingRow()
    }
}`},{t:"list",items:["**Heterogeneous lists need no view-type enums**: the item lambda is code — `when (item) { is Ad -> AdCard(item); is Post -> PostCard(item) }`; `contentType = { it::class }` keeps the reuse pool efficient per type.","**`stickyHeader`** pins the current section header to the top until the next one arrives — the RecyclerView ItemDecoration nightmare reduced to one DSL call.","Building sections by iterating a grouped map inside the DSL is normal — the DSL executes eagerly (it's cheap registration); only item *content* lambdas are lazy."]}]},{heading:"Paging 3 integration — infinite lists from the data layer",blocks:[{t:"p",text:"For genuinely unbounded data (network pages, DB + network), hand-rolling load-more triggers gets messy — **Paging 3** formalizes it: the data layer exposes `Flow<PagingData<T>>`, and Compose consumes it with `collectAsLazyPagingItems()`:"},{t:"code",title:"Paging 3 in Compose",code:`val pagingItems = viewModel.articles.collectAsLazyPagingItems()

LazyColumn {
    items(
        count = pagingItems.itemCount,
        key = pagingItems.itemKey { it.id },
        contentType = pagingItems.itemContentType { "article" },
    ) { index ->
        val article = pagingItems[index]           // triggers page loads
        if (article != null) ArticleCard(article)
        else ArticlePlaceholder()                  // placeholders enabled
    }

    when (pagingItems.loadState.append) {          // footer states
        is LoadState.Loading -> item { LoadingRow() }
        is LoadState.Error -> item { RetryRow { pagingItems.retry() } }
        else -> Unit
    }
}`},{t:"list",items:["Accessing `pagingItems[index]` near the loaded edge is what triggers fetching the next page — the 'load more' logic lives in the library, not your scroll math.","`loadState.refresh` / `append` / `prepend` give you first-load spinners, end-of-list errors and retry — model them as list items, not overlays, for correct UX.","Mention-worthy nuance: `PagingData` is a stream of *self-updating list snapshots* — it doesn't fit `stateIn`/normal Flow operators; it's cached with `cachedIn(viewModelScope)` (mandatory, or rotation re-fetches everything)."]}]}],eS=[{level:"junior",q:"What is the difference between LazyColumn and Column with verticalScroll?",a:[{t:"p",text:"**The concept**: both scroll vertically, but they compose their children completely differently. A `Column` with `Modifier.verticalScroll(...)` composes **every child immediately** — all of them exist in the composition at once, whether visible or not, and scrolling just moves the viewport over them. `LazyColumn` composes **only the visible items** (plus a small buffer); as you scroll, off-screen items are disposed and incoming ones composed on demand — the same virtualization idea as RecyclerView, but automatic."},{t:"p",text:"**How to choose**: use the scrollable Column for a *bounded, small* set of children — a settings screen with 15 rows, a form. Use LazyColumn for anything *large, unbounded, or dynamic* — a feed, search results, a chat. The failure mode that makes it a real bug, not just a preference: putting hundreds (or a network-paged list) of items in a scrollable Column composes them all up front — memory spikes and the first frame stalls. And you can't fix it by nesting — a LazyColumn inside a verticalScroll Column crashes, because the scroll container offers infinite height and the lazy list can't decide what's 'visible'."}]},{level:"junior",q:"Why should you provide a key in items()? What breaks without it?",a:[{t:"p",text:"**The concept**: Compose identifies each item so it knows, across list updates, which item is 'the same one'. By default that identity is the item's **index** (position). Index identity is fine while the list never changes — but the moment items are inserted, removed, or reordered, indices shift, and Compose can no longer tell 'item moved' from 'item changed'."},{t:"list",items:["**Cascade recomposition**: delete the first item and every following item now has a new index → Compose treats them all as changed → every visible item recomposes, even though their data is identical. With `key = { it.id }`, identity follows the data, so unmoved items are left completely alone.","**State attaches to the wrong item** — the worst bug: an item's `remember`ed state (an expanded/collapsed flag, a half-played animation, a text field's contents) is stored by identity. With index keys, after a deletion the state that belonged to old item #3 silently reattaches to whatever is now at index #3. Users see a checkbox 'jump' to a different row.","**No item animations**: `Modifier.animateItem()` needs stable keys to know an item *moved* rather than *appeared*; without keys, reorders can't be animated."]},{t:"p",text:"**Requirements for a good key**: stable, unique in the list, and saveable (a data-class `id`, not a random UUID generated in the item body — that changes every recomposition and defeats the purpose). And never pass the index as the key: that just reproduces the default."}]},{level:"junior",q:"How do you scroll a LazyColumn programmatically, e.g. a 'scroll to top' button?",a:[{t:"p",text:"**The concept**: scrolling is controlled through a `LazyListState` you create with `rememberLazyListState()` and pass to the list. The scroll functions are **suspend functions** (because scrolling can animate over time and must be cancellable), so you call them from a coroutine — in an event handler, that means a `rememberCoroutineScope()`."},{t:"code",title:"The standard pattern",code:`val listState = rememberLazyListState()
val scope = rememberCoroutineScope()

Scaffold(
    floatingActionButton = {
        FloatingActionButton(onClick = {
            scope.launch { listState.animateScrollToItem(index = 0) }
        }) { Icon(Icons.Default.KeyboardArrowUp, null) }
    }
) { padding ->
    LazyColumn(state = listState, contentPadding = padding) { /* items */ }
}`},{t:"p",text:"`animateScrollToItem(index)` animates smoothly; `scrollToItem(index)` jumps instantly (good for restoring a saved position). Both take an optional pixel offset. A subtle correctness point: launch from `rememberCoroutineScope`, **not** a `LaunchedEffect` triggered by a click flag, and never call the suspend function directly in the composable body — scrolling is an event-driven action, so it belongs to the event's coroutine."}]},{level:"junior",q:"How do you build a list with different item types (e.g. headers and rows, or ads and posts)?",a:[{t:"p",text:"**The concept**: RecyclerView made you enumerate 'view types', override `getItemViewType`, and switch in `onCreateViewHolder`. Compose needs none of that — the item content is *just code*, so you branch with an ordinary `when`."},{t:"code",title:"Heterogeneous list — plain Kotlin branching",code:`LazyColumn {
    items(
        items = feed,
        key = { it.id },
        contentType = { it::class },   // reuse hint: group by type
    ) { entry ->
        when (entry) {
            is FeedItem.Post -> PostCard(entry)
            is FeedItem.Ad   -> AdBanner(entry)
            is FeedItem.Divider -> Divider()
        }
    }

    // fixed-purpose items use item {} directly:
    item(contentType = "header") { FeedHeader() }
}`},{t:"p",text:"**The one thing to add for performance is `contentType`**: Compose keeps a small pool of disposed item node-structures to reuse as you scroll (like recycling), and it can only reuse a structure for an item of the *same* type. Passing `contentType` (e.g. `it::class`) tells it how to group the pool, so a post scrolling in reuses a post's structure instead of rebuilding from scratch. It's optional and harmless to omit on uniform lists, but for mixed feeds it's the difference between smooth and janky recycling."}]},{level:"junior",q:"What does rememberLazyListState() give you, and does scroll position survive rotation?",a:[{t:"p",text:"`rememberLazyListState()` creates the `LazyListState` that owns the list's scroll position and exposes scroll control and inspection. Because it's built with a saver internally, it survives **both** recomposition and configuration change / process recreation — so yes, scroll position is preserved across rotation automatically, you don't wire anything up. It restores by remembering the first visible item's *key* and pixel offset (which is another reason stable keys matter — with index keys, a list that changed above the viewport would restore to the wrong place)."},{t:"p",text:"What it exposes: `firstVisibleItemIndex` and `firstVisibleItemScrollOffset` (the current anchor), `layoutInfo` (a full snapshot of visible items — indices, sizes, offsets, viewport bounds), `isScrollInProgress`, and the suspend scroll functions. You only need to create and pass it when you want to *observe* or *control* scrolling; a plain `LazyColumn {}` without a state parameter gets an internal one and still preserves position."}]},{level:"senior",q:"How do you implement infinite scroll (load-more) efficiently in Compose without recomposing on every scroll frame?",a:[{t:"p",text:"**The trap first**: the naive approach reads `listState.firstVisibleItemIndex` (or `layoutInfo`) directly in the composable to decide when to load more. Those values change on *every scroll frame*, so any composable reading them recomposes continuously while scrolling — you've turned a scroll into a recomposition storm. The fix is to compute the *boolean decision* inside `derivedStateOf`, which only invalidates readers when the decision actually flips."},{t:"code",title:"Load-more with derivedStateOf + LaunchedEffect",code:`val shouldLoadMore by remember {
    derivedStateOf {
        val layoutInfo = listState.layoutInfo
        val lastVisible = layoutInfo.visibleItemsInfo.lastOrNull()?.index ?: 0
        // trigger when within 5 items of the end
        lastVisible >= layoutInfo.totalItemsCount - 5
    }
}

LaunchedEffect(shouldLoadMore) {
    if (shouldLoadMore) viewModel.loadNextPage()
}`},{t:"list",items:["`derivedStateOf` re-evaluates the threshold on every scroll (cheap — just arithmetic) but only *invalidates* the `LaunchedEffect` key when `shouldLoadMore` transitions false→true.","Keying the `LaunchedEffect` on the boolean means the load fires once per crossing, not repeatedly; the ViewModel should also guard against concurrent/duplicate page requests (an `isLoading` flag or a single in-flight job) since the flag can stay true across a few frames.","**The senior answer names the better alternative**: for real network pagination, hand-rolling this is fragile (dedup, error/retry, prepend, placeholders). **Paging 3** does it properly — accessing an item near the edge triggers the fetch inside the library, and `loadState` gives you loading/error/retry as list items. Roll your own only for simple bounded cases; reach for Paging for production infinite feeds."]}]},{level:"senior",q:"A LazyColumn scrolls smoothly in debug but users report jank. Walk through diagnosing lazy-list performance.",a:[{t:"list",items:["**First, rule out the false alarm**: debug builds are *expected* to be janky — no R8, extra Compose runtime checks, no baseline profile → JIT compiling on the fly during scroll. Always measure scroll performance on a **release build** (or at least with baseline profiles). Half of 'my list is janky' reports evaporate here; say this first.","**Check keys**: missing/unstable keys cause whole-list recomposition on any data change — data updating during scroll then compounds jank. Verify stable `key = { it.id }`.","**Check item skippability/stability**: if item parameters are unstable types (a plain `List`, a non-`@Immutable`/`@Stable` model), items recompose needlessly. Enable the Compose compiler metrics/strong-skipping and make item models stable — an unstable item model is the most common real cause.","**Check work done in the item body**: item lambdas run on the frame deadline as items scroll in. Anything heavy there — sorting, formatting, non-cached image decode, allocation — blows the budget. Move formatting into the UI model; ensure images go through a caching loader (Coil) with proper sizing.","**Use the tools**: Layout Inspector recomposition counts to spot items recomposing too often; the Android Studio profiler / `Macrobenchmark` with `FrameTimingMetric` to get real jank numbers; composition tracing to see time per item.","**Add a baseline profile**: for lists specifically, baseline profiles precompile the scroll/compose hot paths and are one of the biggest single wins — measurably fewer dropped frames on first scroll."]},{t:"p",text:"The framing that lands: treat it as a measurement problem — release build + profiler numbers first, then attack in order of likelihood (build type → keys → stability → item-body work → baseline profile). Guessing and sprinkling `remember` is the junior move."}]},{level:"senior",q:"Why does nesting a LazyColumn inside a vertically-scrolling Column crash, and how do you restructure it?",a:[{t:"p",text:"**The mechanism**: a `Modifier.verticalScroll` container measures its content with an **infinite maximum height** — that's how it lets content be taller than the screen and scroll. A `LazyColumn`'s entire job depends on knowing its viewport height so it can decide *which items are visible* and compose only those. Given infinite height, 'visible' would mean 'all of them', which defeats laziness and is undefined — so Compose throws: *\"Vertically scrollable component was measured with an infinity maximum height.\"* The two scrollables have incompatible contracts in the same axis."},{t:"p",text:"**The fix is to make everything one lazy list** rather than nesting. Whatever you were putting *around* the LazyColumn becomes items *of* it — headers, banners, and sections all move into the DSL:"},{t:"code",title:"Restructure: one LazyColumn owns the whole screen",code:`LazyColumn {
    item { ProfileHeader() }          // was above the list
    item { StatsRow() }               // was above the list
    items(posts, key = { it.id }) { PostCard(it) }
    item { Footer() }                 // was below the list
}`},{t:"list",items:["This is also *more* correct, not just a workaround: now the header/footer participate in virtualization too, and there's a single scroll container with one consistent scroll position.","**Cross-axis nesting is completely fine**: a `LazyRow` inside a `LazyColumn` item (horizontal carousels in a vertical feed) — different axes, no infinite-constraint conflict. Give the rows their own keys/contentType.","If you genuinely must nest same-axis (rare), you'd have to give the inner list a *fixed* height (`Modifier.height(300.dp)`) so it isn't measured with infinity — but that usually signals the one-LazyColumn restructure is what you actually want."]}]},{level:"senior",q:"Explain how LazyColumn is implemented under the hood, and how item reuse compares to RecyclerView.",a:[{t:"p",text:"**The foundation is SubcomposeLayout**: unlike normal composables (composed fully before layout), LazyColumn defers composing its items until *measure time*. At measure, it knows the viewport dimensions; starting from the current scroll anchor, it subcomposes items one at a time — measure, accumulate height — until the viewport plus a small buffer (`beyondBoundsItemCount`) is filled, then stops. Items scrolled fully out of that window are disposed (their composition removed from the slot table); items scrolling in are subcomposed fresh. So at any moment only ~viewport-worth of items exist in the composition, regardless of dataset size."},{t:"list",items:["**Reuse pool**: to avoid rebuilding node structures constantly, Compose keeps a small pool of *disposed but reusable* item layouts, grouped by `contentType`. When an item of a matching type scrolls in, it reuses a pooled structure and just updates the changed slots — conceptually RecyclerView's ViewHolder recycling, but the runtime does it for you and it operates on Compose nodes, not Views.","**vs RecyclerView — what's automatic**: no `Adapter`, no `ViewHolder` class, no `onCreateViewHolder`/`onBindViewHolder`, no `getItemViewType`, no `DiffUtil`/`notifyItemChanged`. The item lambda is the bind logic; keys replace DiffUtil (Compose diffs by key + parameter equality); `contentType` replaces view types.","**vs RecyclerView — the tradeoffs to be honest about**: RecyclerView's recycling is more aggressive/mature and its prefetch is very tuned, so extremely heavy lists can still favor it in edge cases; Compose leans on *stability + keys + baseline profiles* to hit smoothness instead. And RecyclerView had years of tooling; Compose lists need the stability discipline in exchange for far less boilerplate.","**Consequence for correctness**: because items are genuinely disposed and recomposed as you scroll, any `remember`ed item state is lost when the item leaves the pool unless it's hoisted or made saveable — a footgun that has no RecyclerView analogue (there, state was never in the item to begin with)."]}]},{level:"senior",q:"How does Paging 3 integrate with Compose, and what problems does it solve that a hand-rolled load-more doesn't?",a:[{t:"p",text:"**The integration**: the data layer exposes `Flow<PagingData<T>>` (from a `Pager` with a `PagingSource`, or `RemoteMediator` for network+DB). In the ViewModel it must be `cachedIn(viewModelScope)`. The UI calls `collectAsLazyPagingItems()` to get a `LazyPagingItems<T>`, and drives the list with `items(count = pagingItems.itemCount, key = pagingItems.itemKey { it.id }, contentType = ...)`, reading each element via `pagingItems[index]` — which is what triggers loading the next page as you approach the edge."},{t:"list",items:["**What it solves over hand-rolled**: automatic page-fetch triggering (no derivedStateOf edge math), request **de-duplication** and cancellation, a proper `loadState` machine (`refresh`/`append`/`prepend` each Loading/NotLoading/Error) so first-load spinners, end-of-list errors, retry, and empty states are modeled uniformly — as list items, not ad-hoc overlays. It also supports **placeholders** (show item count before data loads) and `RemoteMediator` for offline-first (DB is SSOT, network fills pages).","**cachedIn is mandatory, and interviewers check it**: without it, `PagingData` is re-fetched from scratch on every configuration change (rotation reloads page 1). `cachedIn` keeps the loaded pages alive in the ViewModel scope.","**Nuance about the type**: `PagingData` is a stream of self-updating list snapshots — it does *not* behave like a normal Flow of values, so you can't `stateIn`/`map` its contents with ordinary operators (there are dedicated `.map`/`.filter` on PagingData instead). That's why it has its own collection API rather than `collectAsStateWithLifecycle`.","**When NOT to use it**: bounded lists that fully fit in memory (a user's 20 settings, a fixed menu) — Paging's ceremony (PagingSource, load states, cachedIn) is overkill; a plain `items(list)` is correct there. Paging earns its complexity on genuinely unbounded, network-backed data."]}]},{level:"junior",q:"What's the difference between item {}, items(), and itemsIndexed() in a LazyListScope?",a:[{t:"p",text:"Inside a `LazyColumn`/`LazyRow` you don't write children directly — you describe them in a `LazyListScope` using DSL functions. `item { }` adds a single item, `items(list) { }` adds one per element, and `itemsIndexed(list) { index, element -> }` adds one per element with its index."},{t:"code",title:"The lazy DSL",code:`LazyColumn {
    item { Header() }                         // one fixed item (header)
    items(users, key = { it.id }) { user ->   // one per element
        UserRow(user)
    }
    itemsIndexed(users) { index, user ->      // element + index
        Text("\${index + 1}. \${user.name}")
    }
    item { Footer() }                         // one fixed footer
}`},{t:"list",items:["**`item { }`** — a single item; use for headers, footers, or a loading spinner at the end.","**`items(list) { }`** — the workhorse; pass `key` and `contentType` here for correctness and performance.","**`itemsIndexed(list) { i, e -> }`** — when you need the position (numbering, alternating backgrounds).","**Important** — these are *not* composables you loop over with a `for`; they're DSL calls that let the list compose only visible items. Don't call composables directly in the LazyColumn body outside these."]},{t:"note",text:"item{} adds one item (headers/footers/spinners); items(list){} one per element (put key + contentType here); itemsIndexed gives index too. They're LazyListScope DSL calls — the list uses them to compose only visible items, so don't hand-loop composables in the body."}]},{level:"junior",q:"What lazy containers exist besides LazyColumn (rows, grids, staggered)?",a:[{t:"p",text:"Compose has a family of lazy containers, all sharing the same key/contentType/state concepts but differing in axis and arrangement. Knowing which to pick avoids reinventing layouts."},{t:"list",items:["**`LazyColumn` / `LazyRow`** — vertical / horizontal lists.","**`LazyVerticalGrid` / `LazyHorizontalGrid`** — grids; you specify `columns`/`rows` as `GridCells.Fixed(n)` or `GridCells.Adaptive(minSize)`.","**`LazyVerticalStaggeredGrid` / `LazyHorizontalStaggeredGrid`** — Pinterest-style grids where items have varying heights and pack tightly.","**`FlowRow` / `FlowColumn`** — wrap content onto multiple lines (chips, tags); *not* lazy (composes all children), so only for small counts."]},{t:"code",title:"A grid",code:`LazyVerticalGrid(columns = GridCells.Adaptive(minSize = 128.dp)) {
    items(photos, key = { it.id }) { PhotoCell(it) }
    item(span = { GridItemSpan(maxLineSpan) }) { SectionHeader() }  // full-width row
}`},{t:"note",text:"LazyColumn/LazyRow (lists), LazyVerticalGrid/LazyHorizontalGrid (grids, Fixed(n) or Adaptive(minSize)), LazyStaggeredGrid (varying-height Pinterest style). FlowRow/FlowColumn wrap chips but are NOT lazy — small counts only."}]},{level:"junior",q:"How do you configure columns in LazyVerticalGrid, and make an item span the full width?",a:[{t:"p",text:"`LazyVerticalGrid` takes a `columns` parameter that decides how many columns and how they size. `GridCells.Fixed(n)` gives exactly n equal columns; `GridCells.Adaptive(minSize)` fits as many columns as possible at ≥ minSize each (responsive). To make an item (like a section header) span all columns, set its `span`."},{t:"code",title:"Fixed vs Adaptive, and spanning",code:`LazyVerticalGrid(
    columns = GridCells.Adaptive(minSize = 120.dp),   // responsive column count
    // columns = GridCells.Fixed(3),                  // always 3 columns
    horizontalArrangement = Arrangement.spacedBy(8.dp),
    verticalArrangement = Arrangement.spacedBy(8.dp),
) {
    item(span = { GridItemSpan(maxLineSpan) }) { Header() }   // spans all columns
    items(items, key = { it.id }) { Cell(it) }
}`},{t:"list",items:["**`GridCells.Fixed(n)`** — exactly n columns; simple, but not responsive across screen sizes.","**`GridCells.Adaptive(minSize)`** — as many columns as fit at ≥ minSize; adapts to phone/tablet automatically. Usually preferred.","**Spanning** — `item(span = { GridItemSpan(maxLineSpan) })` (or per-item in `items(span = ...)`) makes headers/full-width rows."]},{t:"note",text:"columns = GridCells.Fixed(n) for exactly n columns, or GridCells.Adaptive(minSize) for responsive (as many as fit ≥ minSize — prefer this). Span the full width with span = { GridItemSpan(maxLineSpan) } on an item — the standard way to add grid section headers."}]},{level:"junior",q:"How do you add sticky headers to a LazyColumn?",a:[{t:"p",text:"`stickyHeader { }` is a LazyListScope function that pins a header to the top of the viewport while its section scrolls beneath it, then pushes it up when the next section's header arrives — the classic contacts/section-list behavior. You typically group your data and emit a `stickyHeader` before each group's items."},{t:"code",title:"Sticky section headers",code:`LazyColumn {
    grouped.forEach { (letter, names) ->
        stickyHeader { SectionHeader(letter) }   // pinned while this section scrolls
        items(names, key = { it }) { Name(it) }
    }
}`},{t:"list",items:["**`stickyHeader`** — the header stays visible at the top while its section is on screen.","**Group first** — build a `Map<Key, List<Item>>` (e.g. by first letter) and iterate, emitting a sticky header per group.","**Keys still matter** — give items stable keys for correct scrolling/animation; headers can key by their group value.","**Caveat** — it was experimental for a while; ensure you're on a version where it's stable, and be mindful that heavy header content recomposes as it pins."]},{t:"note",text:"stickyHeader { } pins a header to the top while its section scrolls beneath, replaced by the next section's header. Group data (e.g. Map by first letter) and emit a stickyHeader before each group's items(). Keep stable keys; keep header content light."}]},{level:"senior",q:"What is contentType in items(), and why does it matter for performance?",a:[{t:"p",text:"`contentType` tells the lazy list what *kind* of item each entry is, so its item-reuse pool can recycle a scrolled-off composable of the same type for a new item — reusing the underlying composition/nodes instead of building fresh. In a list with multiple item types (header, ad, post), providing `contentType` dramatically improves scroll performance."},{t:"code",title:"contentType in a mixed list",code:`LazyColumn {
    items(
        feed,
        key = { it.id },
        contentType = { it.type },   // "post", "ad", "header" -> separate reuse pools
    ) { entry ->
        when (entry.type) { "post" -> Post(entry); "ad" -> Ad(entry); else -> Header(entry) }
    }
}`},{t:"list",items:["**Enables structural reuse** — Compose keeps reuse pools *per contentType*; scrolling reuses a matching composition, skipping re-creation of the node tree.","**Only helps with heterogeneous lists** — a single-type list already reuses fine; the win is when types differ (mixed feeds).","**Wrong/absent contentType** — mixing types in one pool means reuse can't happen (structures differ), so every item rebuilds — visible as jank.","**Pair with `key`** — `key` is for item identity (state/animation correctness); `contentType` is for reuse efficiency. Provide both in mixed lists."]},{t:"note",text:"contentType groups items into per-type reuse pools so scrolling reuses a matching composition instead of rebuilding — a big scroll-perf win for mixed feeds (post/ad/header). Single-type lists reuse fine already. key = identity (state/anim); contentType = reuse. Use both in heterogeneous lists."}]},{level:"senior",q:"How do you react to scroll state (e.g. first visible item) without recomposing every frame?",a:[{t:"p",text:"`LazyListState` exposes `firstVisibleItemIndex`, `firstVisibleItemScrollOffset`, and `layoutInfo` — but these change on *every* scroll frame. Reading them directly in composition recomposes 60×/second. The fix is `derivedStateOf`, which recomputes on every scroll but only *emits a new value* (triggering recomposition) when your derived boolean/coarse value actually changes."},{t:"code",title:"derivedStateOf to throttle scroll-driven recomposition",code:`val listState = rememberLazyListState()
val showScrollToTop by remember {
    derivedStateOf { listState.firstVisibleItemIndex > 3 }  // only true/false transitions recompose
}
if (showScrollToTop) ScrollToTopFab(onClick = { /* scroll to 0 */ })`},{t:"list",items:["**Direct read = per-frame recomposition** — `if (listState.firstVisibleItemIndex > 3)` re-reads the raw index every frame.","**`derivedStateOf`** — computes each frame internally but only recomposes readers when the *result* changes (crossing the threshold), collapsing 60 changes/sec into ~1.","**`snapshotFlow`** — alternatively, convert the state to a Flow for side effects (analytics, load-more) with `distinctUntilChanged`/`filter`."]},{t:"note",text:"Scroll state (firstVisibleItemIndex/offset) changes every frame — reading it directly recomposes 60×/sec. Wrap coarse derivations in derivedStateOf (recomputes each frame, recomposes only when the result changes). Use snapshotFlow for side effects like load-more/analytics."}]},{level:"junior",q:"How do you animate item insertions, removals, and reordering in a lazy list?",a:[{t:"p",text:"Use `Modifier.animateItem()` (formerly `animateItemPlacement()`) on the item content — the lazy list then animates items sliding to new positions on reorder, and (in newer versions) fading in/out on insert/remove. This *requires* stable `key`s, because animation is driven by tracking each keyed item's position between frames."},{t:"code",title:"Animated list changes",code:`LazyColumn {
    items(items, key = { it.id }) { item ->   // stable key is REQUIRED
        Row(Modifier.animateItem()) { ItemContent(item) }   // animates move/place, and fade on add/remove
    }
}`},{t:"list",items:["**Needs `key`** — without stable keys, the list can't tell which item moved, so nothing animates (and state gets misattributed).","**`animateItem()`** — animates placement changes (reorder) and, on recent Compose, appearance/disappearance (fade in/out) with configurable specs.","**Reorder** — when you emit the list in a new order, keyed items animate to their new slots automatically.","**Caveat** — animations only apply to items that stay in/near the viewport; items far off-screen just appear."]},{t:"note",text:"Modifier.animateItem() (was animateItemPlacement) animates reorder/insert/remove in lazy lists — but ONLY with stable keys, since animation tracks keyed positions between frames. Emit the list in the new order and keyed items slide to place. No keys = no animation + state bugs."}]},{level:"junior",q:"How do you implement pull-to-refresh with a lazy list?",a:[{t:"p",text:"Material3 provides `PullToRefreshBox` (or the `pullToRefresh` modifier + `PullToRefreshState`) that wraps your `LazyColumn`, shows a refresh indicator on over-scroll at the top, and calls your `onRefresh` when the user pulls far enough. You drive it with an `isRefreshing` boolean from your ViewModel."},{t:"code",title:"PullToRefreshBox",code:`val isRefreshing by viewModel.isRefreshing.collectAsStateWithLifecycle()
PullToRefreshBox(
    isRefreshing = isRefreshing,
    onRefresh = { viewModel.refresh() },
) {
    LazyColumn { items(data, key = { it.id }) { Row(it) } }
}
// ViewModel sets isRefreshing=true during the fetch, false when done`},{t:"list",items:["**Wrap the list** — `PullToRefreshBox` handles the gesture, indicator, and threshold.","**State-driven** — the indicator shows while your `isRefreshing` state is true; the ViewModel owns that flag around the refresh call.","**Don't reset scroll** — refreshing should update data in place (keyed items) so the user's scroll position and item animations are preserved."]},{t:"note",text:"Wrap the LazyColumn in Material3's PullToRefreshBox (or pullToRefresh modifier); it shows the indicator and calls onRefresh on pull. Drive the spinner with an isRefreshing StateFlow the ViewModel toggles around the fetch. Update data in place (keys) so scroll position is kept."}]},{level:"junior",q:"How do you handle empty, loading, and error states in a list screen?",a:[{t:"p",text:"A list screen has more states than 'has items' — it can be loading (first load), empty (loaded but zero items), error (load failed), or content. Model these explicitly in your UiState and branch in the UI so users never see a blank screen or a spinner forever."},{t:"code",title:"Branching on list state",code:`when (val s = uiState) {
    is UiState.Loading -> FullScreenSpinner()
    is UiState.Error   -> ErrorView(s.message, onRetry = viewModel::retry)
    is UiState.Empty   -> EmptyView("No items yet")           // distinct from loading!
    is UiState.Content -> LazyColumn { items(s.items, key = { it.id }) { Row(it) } }
}
// For paging: show inline append spinner/error at the list's end via item {}`},{t:"list",items:["**Empty ≠ loading** — a loaded-but-empty list needs an empty state (illustration + CTA), not a spinner. Distinguish them.","**Error with retry** — always give a way to recover; for refresh errors keep existing content and show a snackbar instead of replacing the whole screen.","**Paging states** — append/prepend loading and errors render as extra `item { }` entries at the list ends (Paging 3's `LoadState`).","**Skeletons** — for first load, placeholder skeleton items feel faster than a centered spinner."]},{t:"note",text:"Model list state as Loading/Empty/Error/Content and branch — empty is NOT the same as loading (show an empty-state CTA). Errors need retry; for refresh keep content + snackbar. Paging append/prepend states render as extra item{} at the ends. Skeletons beat a bare spinner."}]},{level:"junior",q:"How do you implement swipe-to-dismiss on list items?",a:[{t:"p",text:"Material3's `SwipeToDismissBox` wraps each item; the user swipes it away, revealing a background (e.g. a delete color/icon), and you handle the dismissed state to remove the item. It's driven by a `SwipeToDismissBoxState` per item."},{t:"code",title:"SwipeToDismissBox per item",code:`items(list, key = { it.id }) { item ->
    val state = rememberSwipeToDismissBoxState(
        confirmValueChange = { if (it == SwipeToDismissBoxValue.EndToStart) { viewModel.delete(item.id); true } else false }
    )
    SwipeToDismissBox(
        state = state,
        backgroundContent = { DeleteBackground() },
        modifier = Modifier.animateItem(),    // animate the collapse after removal
    ) { ItemRow(item) }
}`},{t:"list",items:["**Per-item state** — each row has its own `SwipeToDismissBoxState`; keyed items keep that state correct as the list changes.","**`confirmValueChange`** — decide whether the swipe commits (and trigger the delete) or snaps back.","**Combine with `animateItem()`** — so the list smoothly closes the gap after removal.","**Offer undo** — deleting on swipe pairs well with a snackbar 'Undo' rather than a confirm dialog."]},{t:"note",text:"Wrap rows in Material3 SwipeToDismissBox with a per-item rememberSwipeToDismissBoxState; confirmValueChange commits the delete or snaps back, backgroundContent shows the reveal. Combine with animateItem() to close the gap, and offer snackbar undo. Stable keys keep per-item state right."}]},{level:"senior",q:"How do you track item impressions/analytics using LazyListState and snapshotFlow?",a:[{t:"p",text:"To log which items became visible (impressions for analytics/ads), observe `LazyListState.layoutInfo.visibleItemsInfo` via `snapshotFlow`. `snapshotFlow` turns snapshot state into a cold Flow that emits when the observed state changes, letting you debounce and dedupe impression events off the UI thread of recomposition."},{t:"code",title:"Impression tracking",code:`val listState = rememberLazyListState()
LaunchedEffect(listState) {
    snapshotFlow { listState.layoutInfo.visibleItemsInfo.map { it.key } }
        .distinctUntilChanged()
        .collect { visibleKeys -> analytics.onItemsVisible(visibleKeys) }
}`},{t:"list",items:["**`snapshotFlow { }`** — reads snapshot state (the visible items) and emits when it changes; runs in the `LaunchedEffect` coroutine, not per-recomposition.","**Dedupe/debounce** — `distinctUntilChanged`, `debounce`, or a threshold (e.g. item ≥50% visible for ≥1s) to count a genuine impression, not a fast scroll-by.","**Use item `key`** — track by stable keys so impressions map to real items across recomposition.","**Why not recompose-based** — reading visibleItemsInfo in composition recomposes every frame; `snapshotFlow` is the correct side-effect channel."]},{t:"note",text:"Track impressions with snapshotFlow { listState.layoutInfo.visibleItemsInfo.map { it.key } } inside a LaunchedEffect, then distinctUntilChanged/debounce and a visibility-duration threshold for real impressions. It emits off the recomposition path — never read visibleItemsInfo directly in composition."}]},{level:"senior",q:"How does a lazy list decide what to compose, and what is item prefetching?",a:[{t:"p",text:"A lazy list composes only the items currently in (or just outside) the viewport, plus a *prefetch* buffer: as the user scrolls, it proactively composes and measures the next item(s) just beyond the visible edge on a background frame, so they're ready to display without a hitch. Items scrolled far away are disposed and their compositions recycled."},{t:"list",items:["**Viewport + buffer** — only visible items (and a small ahead-of-scroll prefetch) are composed; the rest exist only as a lightweight description in the `LazyListScope`.","**Prefetching** — the next item is composed/measured ahead of time during idle frame budget, reducing jank when it scrolls in. This is why smooth debug scrolling can still jank if items are expensive.","**Disposal + reuse** — items that scroll far off are disposed (their `remember`ed state forgotten unless hoisted); their compositions feed the reuse pool (keyed by `contentType`).","**Implications** — keep item composition cheap (heavy per-item work defeats prefetch), and never rely on off-screen items keeping state."]},{t:"note",text:"Lazy lists compose only visible items plus a prefetch buffer (next items composed ahead on idle frames for smooth scroll-in); far-off items are disposed and their compositions reused (by contentType). Keep item composition cheap or prefetch can't keep up — and don't rely on off-screen item state."}]},{level:"senior",q:"How do you preserve a list's scroll position across navigation and process death?",a:[{t:"p",text:"`rememberLazyListState()` survives recomposition and, because it's saveable, configuration changes (rotation) — but it's tied to the composable's lifetime. When you navigate away and back, the composable is disposed, so you need the state to be restored: `rememberLazyListState()` uses `rememberSaveable` under the hood, so it restores across config change and process death *as long as the composable is recreated at the same position with the same saver scope*."},{t:"list",items:["**Within a screen** — `rememberLazyListState()` already restores scroll position across rotation/process death (it's backed by a Saver).","**Across navigation** — Navigation Compose saves/restores the back stack entry's saved state; a list state remembered in the composable is restored when you return, provided the destination is recreated (not a fresh instance). If you pop and re-push, it resets.","**Data must match** — restored scroll index only makes sense if the list has the same items; restore data (e.g. from cache) before or alongside, or the index points at different content.","**For paged/remote lists** — you may need to re-fetch to the saved position; keying items stably lets the state re-anchor."]},{t:"note",text:"rememberLazyListState() is Saver-backed, so it restores scroll position across rotation and process death automatically, and across Navigation Compose back-stack restoration. Caveat: the list must have the same items on restore (load cache first), or the saved index points at different content."}]},{level:"junior",q:"How do you nest a horizontal list inside a vertical list (e.g. a 'shelf' UI)?",a:[{t:"p",text:"Nesting a `LazyRow` inside a `LazyColumn` is fully supported and common (Play Store / Netflix 'shelves') because the two scroll on *different axes* — there's no ambiguity about which handles a gesture. Each `LazyRow` is one item of the `LazyColumn`. (Contrast with nesting same-axis scrollables, which is what crashes.)"},{t:"code",title:"Vertical list of horizontal shelves",code:`LazyColumn {
    items(shelves, key = { it.id }) { shelf ->
        Text(shelf.title)
        LazyRow {                                    // horizontal — different axis, OK
            items(shelf.items, key = { it.id }) { Card(it) }
        }
    }
}`},{t:"list",items:["**Different axes = fine** — `LazyRow` in `LazyColumn` (or vice-versa) works; the outer scrolls vertically, inner horizontally.","**Give each `LazyRow` its own state** if you need to control/observe its scroll (`rememberLazyListState()` per row — key it by shelf if reused).","**Same axis = crash** — a `LazyColumn` directly inside a vertically-scrolling parent throws (infinite height constraints). Keep nesting cross-axis.","**Performance** — each row prefetches independently; keep card content light since many rows may be near the viewport."]},{t:"note",text:"LazyRow inside LazyColumn (shelves) is supported — different scroll axes, no gesture conflict. Each LazyRow is one LazyColumn item; give rows their own keyed state if you observe them. Only SAME-axis nesting (LazyColumn in a verticalScroll) crashes on infinite constraints."}]},{level:"junior",q:"How do you add uniform spacing between lazy list items?",a:[{t:"p",text:"Use the container's `verticalArrangement = Arrangement.spacedBy(dp)` (or `horizontalArrangement` for a LazyRow) to put a consistent gap between items — cleaner than adding padding to each item or emitting Spacers. Combine with `contentPadding` for space at the list's outer edges."},{t:"code",title:"Spacing done right",code:`LazyColumn(
    verticalArrangement = Arrangement.spacedBy(12.dp),   // gap BETWEEN items only
    contentPadding = PaddingValues(16.dp),               // space around the whole content
) {
    items(list, key = { it.id }) { Row(it) }
}`},{t:"list",items:["**`Arrangement.spacedBy`** — one gap between adjacent items, none at the ends (no leading/trailing extra space).","**`contentPadding`** — space at the top/bottom (and sides) of the scrollable content, scrolled-through and clipped at the edge.","**Avoid per-item padding for gaps** — it double-spaces adjacent items and adds padding at the very ends where you may not want it."]},{t:"note",text:"Use verticalArrangement/horizontalArrangement = Arrangement.spacedBy(dp) for uniform gaps BETWEEN items (none at the ends), plus contentPadding for outer edge space. Don't put padding on each item for gaps — it double-spaces neighbors and pads the ends."}]},{level:"senior",q:"What's your checklist for keeping a large/complex lazy list smooth?",a:[{t:"p",text:"Lazy-list jank almost always comes from expensive item composition, missing keys/contentType, or reading rapidly-changing state during composition. Here's the practical checklist to keep 60/120fps."},{t:"list",items:["**Provide stable `key`** — correct state/animation and better reuse.","**Provide `contentType`** for mixed lists — enables per-type composition reuse.","**Keep item content cheap** — no heavy work in the item body; precompute in the ViewModel, hoist derived values, avoid large `Modifier` chains and unnecessary nesting.","**Ensure items are stable/skippable** — pass immutable data; unstable params force item recomposition on unrelated changes.","**Don't read scroll state directly** — use `derivedStateOf`/`snapshotFlow` so you don't recompose items every frame.","**Defer state reads to draw** — use `graphicsLayer { }` lambda for scroll-driven visual effects instead of layout-affecting modifiers.","**Image loading** — use Coil with proper sizing/placeholders; oversized bitmaps cause GC jank.","**Generate a Baseline Profile** — precompiles Compose/list code so first scrolls aren't interpreted (huge real-device win).","**Profile on a release build** on a real mid-tier device — never judge on debug."]},{t:"note",text:"Smooth-list checklist: stable key + contentType, cheap/stable item content (immutable data), no direct scroll-state reads (derivedStateOf/snapshotFlow), graphicsLayer for scroll effects, right-sized image loading, a Baseline Profile, and profile on a RELEASE build on a real device. Jank = expensive items or missing keys."}]},{level:"junior",q:"What's the difference between scrollToItem and animateScrollToItem, and how do you use a scroll offset?",a:[{t:"p",text:"Both move a `LazyColumn` to a target item via its `LazyListState`, but `scrollToItem` jumps *instantly* while `animateScrollToItem` *smoothly animates* the scroll. Both are `suspend` functions (call them from a coroutine) and accept an optional pixel `scrollOffset` to fine-tune the final position."},{t:"code",title:"Programmatic scrolling",code:`val state = rememberLazyListState()
val scope = rememberCoroutineScope()

scope.launch { state.scrollToItem(index = 0) }               // instant jump to top
scope.launch { state.animateScrollToItem(index = 20) }       // smooth scroll to item 20
scope.launch { state.animateScrollToItem(index = 20, scrollOffset = -32) } // 32px above the top edge`},{t:"list",items:["**`scrollToItem`** — instant; use for 'jump to top' on tab reselect, or restoring a position without animation.","**`animateScrollToItem`** — animated; use for user-visible navigation to a section.","**`scrollOffset`** — pixels to offset the item from the viewport start (e.g. leave room for a sticky header).","**They're `suspend`** — launch from `rememberCoroutineScope()` (a click handler) or inside a `LaunchedEffect`."]},{t:"note",text:"scrollToItem = instant jump, animateScrollToItem = smooth animated; both suspend (launch from a scope) and take a pixel scrollOffset to fine-tune (e.g. leave room under a sticky header). Use instant for jump-to-top/restore, animated for user-facing navigation."}]},{level:"junior",q:"How do you build a chat-style list that starts at the bottom (reverseLayout)?",a:[{t:"p",text:"Set `reverseLayout = true` on the `LazyColumn` — items are laid out from the bottom up, so index 0 sits at the bottom and the list starts scrolled to the newest message. You typically also reverse (or emit newest-first) your data so the freshest item is at index 0."},{t:"code",title:"Chat list",code:`LazyColumn(
    reverseLayout = true,               // index 0 at the bottom; starts at newest
    verticalArrangement = Arrangement.spacedBy(4.dp),
) {
    items(messagesNewestFirst, key = { it.id }) { MessageBubble(it) }
}
// On a new message, scroll to keep the newest visible:
LaunchedEffect(messages.size) { if (atBottom) state.animateScrollToItem(0) }`},{t:"list",items:["**`reverseLayout = true`** — bottom-anchored; naturally starts at the newest message and grows upward.","**Data order** — provide messages newest-first (index 0 = newest) so the reversed layout shows them chronologically.","**Auto-scroll on new message** — only auto-scroll if the user is already at the bottom (check `firstVisibleItemIndex == 0`), so you don't yank them away while reading history.","**Load older on scroll up** — detect nearing the end (top, visually) to page in history."]},{t:"note",text:"reverseLayout = true anchors the list to the bottom (index 0 at bottom) — ideal for chat: starts at the newest, grows upward. Provide messages newest-first, auto-scroll to item 0 on new messages ONLY if the user is already at the bottom, and page history on scroll-up."}]},{level:"junior",q:"When should you NOT use a LazyColumn?",a:[{t:"p",text:"Lazy lists have real overhead (the lazy machinery, item reuse pools, scroll state). For a small, fixed number of items you know will all be shown, a plain `Column` (with `verticalScroll` if needed) is simpler and often faster — and it lets children measure normally without lazy constraints."},{t:"list",items:["**Small, fixed count** — a handful of items (a settings screen, a form): use `Column`; the lazy overhead isn't worth it.","**Non-scrolling content** — if it fits on screen, `Column` alone; no scroll container needed.","**All items always visible** — no benefit to lazy composition when nothing is off-screen.","**Use `LazyColumn` when** — the list is long, unbounded, or dynamically sized, so composing only visible items matters.","**Gotcha** — never put a `LazyColumn` inside a `verticalScroll` `Column`; if you need a scrolling screen with a list section, make the whole thing a `LazyColumn` and use `item { }` for the non-list parts."]},{t:"note",text:"Use a plain Column (± verticalScroll) for small, fixed, all-visible content (settings, forms) — LazyColumn's machinery is overkill and can't nest in a verticalScroll anyway. Reserve LazyColumn for long/unbounded/dynamic lists where composing only visible items pays off; wrap mixed screens as one LazyColumn with item{}."}]},{level:"junior",q:"How do you show a loading footer / 'load more' spinner at the end of a list?",a:[{t:"p",text:"Emit an extra `item { }` at the end of the `LazyColumn` that renders a spinner (or an error-with-retry row) based on your append/load-more state. Because it's a real lazy item, it only composes when the user scrolls to the bottom — which is also the natural trigger point for loading the next page."},{t:"code",title:"Append footer",code:`LazyColumn {
    items(items, key = { it.id }) { Row(it) }
    if (appendState == LoadState.Loading) {
        item { Box(Modifier.fillMaxWidth().padding(16.dp), Alignment.Center) { CircularProgressIndicator() } }
    }
    if (appendState is LoadState.Error) {
        item { RetryRow(onRetry = viewModel::retryAppend) }
    }
}`},{t:"list",items:["**Footer as an `item { }`** — composes only when scrolled into view; render spinner or error/retry from your append state.","**Paging 3** — exposes this as `loadState.append` (Loading/Error/NotLoading); wire the footer to it instead of hand-rolling.","**Distinct from refresh** — the *append* footer is separate from the top *refresh* indicator; model both states."]},{t:"note",text:"Add a trailing item { } that shows a spinner or retry row based on your append/loadState — it only composes when scrolled to the bottom. Paging 3 gives loadState.append (Loading/Error/NotLoading) to wire it directly. Keep append state separate from top-refresh state."}]},{level:"senior",q:"How do you scroll a specific item into view when it receives focus or on demand (BringIntoViewRequester)?",a:[{t:"p",text:"`BringIntoViewRequester` lets a child ask its scrollable ancestors to scroll it into the visible area — useful when a field gains focus (so the keyboard doesn't cover it) or when you programmatically need to reveal a nested element whose index you don't track. You attach a requester to the element and call `bringIntoView()` from a coroutine."},{t:"code",title:"BringIntoViewRequester",code:`val requester = remember { BringIntoViewRequester() }
val scope = rememberCoroutineScope()
TextField(
    modifier = Modifier
        .bringIntoViewRequester(requester)
        .onFocusEvent { if (it.isFocused) scope.launch { requester.bringIntoView() } },
    ...
)`},{t:"list",items:["**Element-driven scrolling** — unlike `animateScrollToItem` (which needs an index), this works from the child; the scroll parents cooperate to reveal it.","**Common for forms** — bring a focused `TextField` above the IME so it isn't hidden (pair with `imePadding`).","**Works across nested scrollables** — the request propagates up through scroll parents."]},{t:"note",text:"BringIntoViewRequester lets a child ask scroll ancestors to reveal it (call bringIntoView() in a coroutine) — no index needed, unlike animateScrollToItem. Classic use: scroll a focused TextField above the keyboard (with imePadding). It propagates through nested scrollables."}]}],tS={id:"lazy-lists",title:"Lazy Lists & Grids",tagline:"LazyColumn/Row/Grid, why keys matter, LazyListState & derivedStateOf, sticky headers, item types, Paging 3, and lazy-list performance.",tags:["Compose","LazyColumn","Keys","Paging 3","Performance"],content:Zx,qa:eS},aS=[{heading:"What a 'side effect' is and why Compose needs special APIs for them",blocks:[{t:"p",text:"A **side effect** is anything a composable does that reaches *outside* itself and isn't just describing UI: showing a snackbar, starting a coroutine, registering a listener, logging analytics, calling a callback, writing to a repository. The problem is that composable bodies are **not** a safe place for these — recall the rules: a composable can run many times per second (recomposition), be skipped, run in any order, or be abandoned mid-execution. If you fired a network call or logged an event directly in the body, it would fire an unpredictable number of times, or not at all."},{t:"p",text:'So Compose provides **effect APIs**: controlled entry points that run your side-effecting code at well-defined moments in the composition lifecycle — when a composable *enters* the composition, when it *leaves*, when specific *keys change* — instead of on every recomposition. Learning Compose side effects is really learning "which API runs my code at which moment, and how is it cleaned up".'},{t:"note",text:`Frame this in interviews as: "composition must stay a pure description of UI; anything that isn't pure UI goes through an effect handler that ties its execution and cleanup to the composition lifecycle." That one sentence explains why all these APIs exist.`}]},{heading:"LaunchedEffect — run suspend work tied to composition",blocks:[{t:"p",text:"`LaunchedEffect(keys)` launches a **coroutine** when the composable enters the composition, and **cancels it** when the composable leaves. If any key changes, it **cancels the old coroutine and starts a fresh one**. This is the primary way to run suspend work (load data on first show, animate, observe something) from the UI."},{t:"code",title:"The three canonical uses",code:`// 1) Run once when the screen appears (key = Unit / a constant)
LaunchedEffect(Unit) {
    viewModel.trackScreenView()
}

// 2) Re-run when an input changes (key = the input)
LaunchedEffect(userId) {
    viewModel.loadUser(userId)   // old load cancelled if userId changes
}

// 3) Consume a one-shot event stream (snackbars, navigation)
LaunchedEffect(Unit) {
    viewModel.events.collect { event ->
        when (event) {
            is UiEvent.ShowSnackbar -> snackbarHostState.showSnackbar(event.msg)
            is UiEvent.NavigateBack -> navController.popBackStack()
        }
    }
}`},{t:"list",items:["**The keys ARE the contract**: they answer 'when should this restart?'. `LaunchedEffect(userId)` = 'this work belongs to *this* userId; if it changes, abandon and redo'. `LaunchedEffect(Unit)` = 'run once for the composable's whole lifetime'.","The coroutine runs in a scope tied to the composition — leaving the composition cancels it automatically (no manual job management), which is why `LaunchedEffect` is the safe home for `collect`, `delay`, animations, and one-shot loads.","**Common bug — wrong key**: `LaunchedEffect(Unit) { load(userId) }` loads only the *first* userId and never reloads when it changes (Unit never changes). Put the real dependency in the key.","**Don't** use it for work that should outlive the screen (uploads that must finish) — that belongs in the ViewModel/repository with an app-scoped scope; `LaunchedEffect` is for UI-lifetime work."]}]},{heading:"rememberCoroutineScope — launch from event handlers, not composition",blocks:[{t:"p",text:"`LaunchedEffect` starts a coroutine *as part of composition*. But sometimes you need to start a coroutine in response to a **user event** (a click) — and you can't call a suspend function or `LaunchedEffect` from inside an `onClick` lambda. `rememberCoroutineScope()` gives you a `CoroutineScope` bound to the composition that you can `launch` from anywhere, including event handlers."},{t:"code",title:"The distinction that trips people up",code:`val scope = rememberCoroutineScope()
val listState = rememberLazyListState()

// Event-driven -> rememberCoroutineScope (NOT LaunchedEffect)
Button(onClick = {
    scope.launch { listState.animateScrollToItem(0) }
}) { Text("Top") }

// Composition-driven -> LaunchedEffect
LaunchedEffect(selectedTab) {
    pagerState.animateScrollToPage(selectedTab)
}`},{t:"list",items:["**Decision rule**: is the coroutine started *because the composition happened / a key changed* → `LaunchedEffect`. Started *because the user did something* → `rememberCoroutineScope().launch`.","Its scope is cancelled when the composable leaves the composition — same lifetime safety as LaunchedEffect.","Anti-pattern: creating a scope with `CoroutineScope(...)` inside a composable — it isn't lifecycle-aware and leaks; always use `rememberCoroutineScope()`."]}]},{heading:"DisposableEffect — effects that need cleanup",blocks:[{t:"p",text:"When a side effect **registers something that must be unregistered** — a listener, a callback, a broadcast receiver, a sensor — use `DisposableEffect`. It runs setup when the composable enters (or a key changes) and requires you to return an `onDispose { }` block that runs cleanup when it leaves (or before re-running on key change)."},{t:"code",title:"Registering and cleaning up a lifecycle observer",code:`DisposableEffect(lifecycleOwner) {
    val observer = LifecycleEventObserver { _, event ->
        if (event == Lifecycle.Event.ON_RESUME) viewModel.refresh()
    }
    lifecycleOwner.lifecycle.addObserver(observer)   // setup

    onDispose {
        lifecycleOwner.lifecycle.removeObserver(observer)  // cleanup — mandatory
    }
}`},{t:"list",items:["The mental model: `DisposableEffect` is 'setup + teardown' where the teardown is *required*; `LaunchedEffect` is 'a coroutine' where teardown is automatic cancellation. Use Disposable when there's no coroutine but there IS something to undo.","On key change, the *old* effect's `onDispose` runs **before** the new setup — so registrations always balance.","Forgetting `onDispose` (or registering in `LaunchedEffect` without cleanup) is the classic Compose leak — a receiver that outlives the screen."]}]},{heading:"SideEffect and rememberUpdatedState — the smaller, sharper tools",blocks:[{t:"p",text:"**`SideEffect { }`** runs *after every successful recomposition* — used to publish Compose state to a non-Compose object that isn't lifecycle-scoped. Rare, but the right tool when you must push the latest composed value out to imperative code (e.g. updating an analytics user property, configuring a third-party object) on each successful composition."},{t:"code",title:"SideEffect: publish composed state to external code",code:`SideEffect {
    analytics.setUserProperty("theme", if (isDark) "dark" else "light")
}`},{t:"p",text:"**`rememberUpdatedState(value)`** solves the *stale capture* problem: when a long-running effect (keyed to `Unit`, so it must NOT restart) needs to always see the *latest* value of something. It returns a `State` box refreshed every recomposition, so the effect's closure reads the current value instead of the one captured when it started."},{t:"code",title:"rememberUpdatedState: latest callback without restarting the effect",code:`@Composable
fun AutoLogout(onTimeout: () -> Unit) {
    val currentOnTimeout by rememberUpdatedState(onTimeout)
    LaunchedEffect(Unit) {          // must run exactly once — no restart
        delay(5.minutes)
        currentOnTimeout()          // always the latest handler, not the first
    }
}`},{t:"list",items:["The decision behind `rememberUpdatedState` vs keying the effect: 'should this ongoing work restart when the value changes?' No → `rememberUpdatedState`. Yes → put the value in the keys. Same one-liner from the State topic — it's the crux.","`SideEffect` vs `LaunchedEffect`: `SideEffect` is synchronous, runs after *every* recomposition, no coroutine — for pushing values out. `LaunchedEffect` is a coroutine keyed to restart conditions — for doing work."]}]},{heading:"produceState, snapshotFlow, and derivedStateOf — state-producing effects",blocks:[{t:"list",items:["**`produceState`** turns non-Compose sources (a Flow, a callback API, a suspend load) *into* Compose `State`. It launches a coroutine (like LaunchedEffect) and gives you a `value` to write; readers recompose on change. `val user by produceState<Result>(Loading, id) { value = repo.load(id) }` — handy for local, screen-scoped async state without a ViewModel.","**`snapshotFlow { }`** is the reverse bridge: it converts *Compose state reads* into a cold **Flow**, emitting when the read state changes. Use it to apply Flow operators (`debounce`, `distinctUntilChanged`, `filter`) to Compose state — e.g. react to scroll or text with debouncing.","**`derivedStateOf`** (covered fully in the State topic) is the 'compute state from other state, recompose only when the result changes' tool — technically a state object, not an effect, but it lives in the same toolbox."]},{t:"code",title:"snapshotFlow: debounce a Compose-state search query",code:`LaunchedEffect(Unit) {
    snapshotFlow { query }            // Compose state -> Flow
        .debounce(300)
        .distinctUntilChanged()
        .collectLatest { viewModel.search(it) }
}`}]},{heading:"The decision table — which effect API when",blocks:[{t:"table",headers:["Need","API"],rows:[["Run suspend work when composable appears / a key changes","`LaunchedEffect(keys)`"],["Launch a coroutine from a click / event handler","`rememberCoroutineScope().launch`"],["Register + unregister a listener/receiver/callback","`DisposableEffect(keys) { ...; onDispose { } }`"],["Publish composed state to non-Compose imperative code each recomposition","`SideEffect { }`"],["Keep a long-lived effect reading the latest value without restarting","`rememberUpdatedState(value)`"],["Turn a Flow/callback/suspend source into Compose State","`produceState(initial, keys) { }`"],["Turn Compose state into a Flow (to use Flow operators)","`snapshotFlow { }`"],["Compute state from state, recompose only when result changes","`derivedStateOf { }`"]]},{t:"note",text:"If you can articulate this table and the reasoning ('keys = restart contract', 'Disposable = has cleanup', 'rememberCoroutineScope = event-driven', 'rememberUpdatedState = don't restart but stay fresh'), you've covered the vast majority of Compose side-effect interview questions."}]}],iS=[{level:"junior",q:"What is a side effect in Compose and why can't you just write it in the composable body?",a:[{t:"p",text:"**The concept**: a side effect is anything a composable does beyond describing UI — starting a coroutine, showing a snackbar, registering a listener, logging analytics, calling a repository. A composable body is supposed to be a *pure description*: given the same inputs, it emits the same UI and does nothing else observable."},{t:"p",text:"**Why the body is unsafe for them**: composables don't run in a predictable, once-per-thing way. They recompose (possibly many times a second), can be **skipped** when inputs are unchanged, run in **any order**, and can even be **abandoned** mid-execution and restarted. So a network call written directly in the body might fire zero times (skipped), once, or dozens of times (every recomposition of an animation) — completely unpredictable. A snackbar would re-show on every recomposition."},{t:"p",text:"**The solution**: Compose provides *effect APIs* — `LaunchedEffect`, `DisposableEffect`, `SideEffect`, etc. — that run your effect at a *defined moment* in the composition lifecycle (enter, leave, key change) rather than 'whenever the body happens to run', and handle cleanup. Learning side effects is learning which API pins your code to which moment."}]},{level:"junior",q:"What does LaunchedEffect do, and what is the role of its keys?",a:[{t:"p",text:"**The concept**: `LaunchedEffect(keys) { }` launches a coroutine when the composable **enters the composition**, and automatically **cancels** it when the composable **leaves**. Because it's a coroutine, it's the home for suspend work driven by the UI: loading data when a screen appears, running an animation, or collecting a Flow of one-shot events."},{t:"p",text:"**The keys are the restart contract** — they answer the question 'when should this work be thrown away and redone?'. `LaunchedEffect(Unit)` (a constant that never changes) runs **once** for the composable's entire lifetime. `LaunchedEffect(userId)` runs now, and if `userId` changes it **cancels the running coroutine and starts a fresh one** — exactly what you want when the work belongs to a specific input."},{t:"code",title:"Right key vs wrong key",code:`// CORRECT: reloads whenever userId changes
LaunchedEffect(userId) { viewModel.loadUser(userId) }

// BUG: Unit never changes -> loads the FIRST userId, ignores later ones
LaunchedEffect(Unit) { viewModel.loadUser(userId) }`},{t:"p",text:"So the single most common LaunchedEffect bug is a **wrong key**: keying on `Unit` when the effect actually depends on a value means it never re-runs when that value changes. The rule: put every value the effect *depends on* into the keys."}]},{level:"junior",q:"When do you use rememberCoroutineScope instead of LaunchedEffect?",a:[{t:"p",text:"**The concept**: both launch coroutines tied to the composition's lifetime, but they answer different triggers. `LaunchedEffect` launches its coroutine *as part of composition* — it runs when the composable appears or a key changes. `rememberCoroutineScope()` gives you a `CoroutineScope` you can `launch` from *anywhere*, including places that aren't composition — most importantly **event handlers** like `onClick`."},{t:"p",text:"**Why you can't just use LaunchedEffect in a click**: an `onClick` lambda is ordinary (non-composable) code, so you can't call `LaunchedEffect` (a composable) or a suspend function inside it. `rememberCoroutineScope().launch { }` bridges that gap."},{t:"list",items:["**Decision rule**: coroutine started *because composition happened or a key changed* → `LaunchedEffect`. Started *because the user did something* → `rememberCoroutineScope().launch`.","Example: animating a scroll when a tab is *selected via state* → `LaunchedEffect(selectedTab)`. Animating a scroll when a *button is clicked* → `scope.launch { }` in the onClick.","Both scopes cancel when the composable leaves the composition. Never hand-roll `CoroutineScope(...)` in a composable — it's not lifecycle-aware and leaks."]}]},{level:"junior",q:"What is DisposableEffect and when do you need it instead of LaunchedEffect?",a:[{t:"p",text:"**The concept**: `DisposableEffect` is for side effects that **register something you must later unregister** — a listener, a callback, a `BroadcastReceiver`, a sensor subscription, a lifecycle observer. It runs a setup block when the composable enters (or a key changes) and *forces* you to return an `onDispose { }` block that runs when the composable leaves (or before the effect re-runs on a key change)."},{t:"p",text:"**Why not LaunchedEffect**: LaunchedEffect is for *coroutines*, and its cleanup is automatic cancellation of that coroutine. When there's no coroutine but there *is* a resource to release, LaunchedEffect gives you nowhere to put the cleanup — so you'd leak. DisposableEffect's whole reason to exist is the guaranteed teardown."},{t:"code",title:"The setup/teardown shape",code:`DisposableEffect(Unit) {
    val callback = SensorEventListener { /* ... */ }
    sensorManager.registerListener(callback, sensor, RATE)
    onDispose { sensorManager.unregisterListener(callback) }  // required
}`},{t:"p",text:"Mental model: **DisposableEffect = setup + mandatory teardown; LaunchedEffect = a coroutine with automatic teardown.** Forgetting `onDispose` (or worse, doing a registration inside LaunchedEffect with no cleanup) is the classic Compose memory leak — a receiver that outlives its screen."}]},{level:"junior",q:"How do you handle one-time events like navigation or showing a snackbar from a ViewModel in Compose?",a:[{t:"p",text:"**The concept and the trap**: navigation and snackbars must happen **exactly once**, but Compose state is *sticky* — if you put `showSnackbar = true` in state, it re-fires on every recomposition and after rotation. So one-shot events need a mechanism that delivers once and doesn't replay."},{t:"p",text:"**The common approach**: expose events from the ViewModel as a `Channel` (or `SharedFlow`) turned into a `Flow`, and collect it inside a `LaunchedEffect` in the UI. The LaunchedEffect runs the collection for the screen's lifetime and performs the action per event."},{t:"code",title:"Event channel + LaunchedEffect collection",code:`// ViewModel
private val _events = Channel<UiEvent>(Channel.BUFFERED)
val events = _events.receiveAsFlow()

// UI
LaunchedEffect(Unit) {
    viewModel.events.collect { event ->
        when (event) {
            is UiEvent.ShowSnackbar -> snackbarHostState.showSnackbar(event.message)
            is UiEvent.NavigateBack -> navController.popBackStack()
        }
    }
}`},{t:"p",text:"**The nuance to mention** (shows you know the debate): Google actually recommends modeling even these as *state* with a consumption callback, because Channel/SharedFlow events can be dropped or double-handled around lifecycle gaps — a snackbar message field cleared via `onMessageShown()` survives config change and process death. Both are acceptable; the interview signal is knowing *why* raw state is wrong for events and naming at least one correct solution."}]},{level:"senior",q:"Explain rememberUpdatedState. What exact problem does it solve, and how do you decide between it and keying the effect?",a:[{t:"p",text:"**The problem**: some effects must run **exactly once** and never restart — a 5-minute auto-logout timer, a one-time registration — so they're keyed on `Unit`. But such an effect may need to call a callback or read a value that the parent *updates over time*. Because the effect's coroutine closure captured the value at the moment it started (when `Unit` first ran), it will forever use the **stale** original — later recompositions create new callbacks, but the running effect never sees them."},{t:"p",text:"**How `rememberUpdatedState` fixes it**: instead of capturing the raw value, you capture a stable `State` box whose `.value` is **refreshed on every recomposition**. The effect's closure holds the box (which never changes identity, so no restart) but dereferences `.value` at call time, getting the latest. It's a controlled indirection: 'restart nothing, but always read fresh'."},{t:"code",title:"The pattern",code:`@Composable
fun LandingScreen(onTimeout: () -> Unit) {
    val currentOnTimeout by rememberUpdatedState(onTimeout)
    LaunchedEffect(Unit) {           // key = Unit: MUST NOT restart on new onTimeout
        delay(5_000)
        currentOnTimeout()           // latest handler, even if the parent recomposed
    }
}`},{t:"list",items:["**The decision rule** (the heart of the question): ask 'should this ongoing work *restart* when the value changes?' If **yes** → put the value in the effect's keys (`LaunchedEffect(value)`), which cancels and relaunches. If **no** → use `rememberUpdatedState`, which keeps the work running but feeds it the current value.","Concretely: the auto-logout timer must NOT reset just because the parent passed a new `onTimeout` lambda (that would delay logout forever) → `rememberUpdatedState`. But a data load that IS about `userId` *should* restart when `userId` changes → key it.","Same reasoning applies to any long-lived registration (DisposableEffect capturing a callback): if it shouldn't re-register, wrap the callback in `rememberUpdatedState`."]}]},{level:"senior",q:"Compare LaunchedEffect, SideEffect, and DisposableEffect. When is each correct?",a:[{t:"list",items:["**`LaunchedEffect(keys)`** — runs a **coroutine** on enter, cancels on leave, restarts on key change. For anything suspend/async or time-based: loading, collecting flows, animations, delays. Teardown is automatic (cancellation).","**`DisposableEffect(keys)`** — runs a **synchronous setup** on enter and a **required `onDispose`** on leave (and before each key-change re-run). For non-coroutine resources that must be released: listeners, receivers, observers, sensors, third-party subscriptions. Teardown is manual and mandatory.","**`SideEffect { }`** — runs **synchronously after every successful recomposition**, no keys, no cleanup. For pushing a freshly-composed value *out* to non-Compose imperative code that isn't lifecycle-scoped: updating an analytics user property, configuring an external object with the current state. It intentionally has no restart control because it runs every time."]},{t:"p",text:"**The distinguishing questions**: 'Is it a coroutine / async?' → LaunchedEffect. 'Does it register something needing cleanup?' → DisposableEffect. 'Do I just need to publish the latest composed value to imperative code each frame?' → SideEffect. A subtle SideEffect point that shows depth: it runs only on *successful* recomposition (not abandoned ones), which is precisely why it's the safe place to notify external systems — you won't publish a value from a composition that got rolled back."}]},{level:"senior",q:"What are snapshotFlow and produceState, and when would you reach for each?",a:[{t:"p",text:"**They bridge Compose state and the Flow world in opposite directions.** `produceState` goes *Flow/callback/suspend → Compose State*; `snapshotFlow` goes *Compose State → Flow*."},{t:"p",text:"**`produceState(initial, keys) { }`** launches a coroutine (LaunchedEffect under the hood) and gives you a mutable `value` to set; it returns a `State<T>` the UI reads. Use it to produce *local, screen-scoped* async state without spinning up a ViewModel — e.g. loading an image or a detail object for a small composable: `val result by produceState<Result>(Loading, id) { value = repository.load(id) }`. It also supports `awaitDispose` for cleanup if it wraps a callback source."},{t:"p",text:"**`snapshotFlow { }`** observes the Compose state read inside its lambda and emits a new value into a cold Flow whenever that state changes. You reach for it when you want to apply **Flow operators to Compose state** — the textbook case is debouncing: turn a `query` state or `listState.firstVisibleItemIndex` into a Flow and `.debounce().distinctUntilChanged().collect { }`. It only emits on actual changes (deduped), and being cold, it starts when collected (inside a LaunchedEffect)."},{t:"code",title:"snapshotFlow to react to scroll with operators",code:`LaunchedEffect(listState) {
    snapshotFlow { listState.firstVisibleItemIndex }
        .distinctUntilChanged()
        .filter { it > 0 }
        .collect { analytics.trackScrolled(it) }
}`},{t:"p",text:"**When NOT to use them**: `produceState` for anything that belongs to business logic or must survive rotation → that's a ViewModel/StateFlow, not a composable-scoped producer. `snapshotFlow` when a simple `derivedStateOf` suffices — snapshotFlow is for when you specifically need *Flow operators* (debounce, buffering, combine); for a plain 'compute boolean from scroll position', `derivedStateOf` is lighter."}]},{level:"senior",q:"A composable registers a BroadcastReceiver but it keeps leaking / firing after the screen is gone. Diagnose and fix.",a:[{t:"p",text:"**The likely cause**: the receiver was registered in a place with no cleanup — either directly in the composable body (so it re-registers on every recomposition, stacking up dozens of live receivers), or inside a `LaunchedEffect` (which has no teardown hook, so it's registered but never unregistered when the composable leaves). Both leave receivers alive past the screen's lifetime, firing into a dead UI and retaining it in memory."},{t:"code",title:"The correct pattern — DisposableEffect with balanced setup/teardown",code:`DisposableEffect(context) {
    val receiver = object : BroadcastReceiver() {
        override fun onReceive(c: Context?, intent: Intent?) {
            viewModel.onConnectivityChanged(/* ... */)
        }
    }
    val filter = IntentFilter(ConnectivityManager.CONNECTIVITY_ACTION)
    ContextCompat.registerReceiver(context, receiver, filter, ContextCompat.RECEIVER_NOT_EXPORTED)

    onDispose {
        context.unregisterReceiver(receiver)   // the fix: guaranteed cleanup
    }
}`},{t:"list",items:["**Why DisposableEffect**: it runs setup exactly on enter (and on key change), and its `onDispose` runs exactly on leave (and before re-running on key change) — so registrations and unregistrations always balance, one-to-one.","**Watch the keys**: keying on something that changes often (like a lambda) would re-register/unregister repeatedly — key on stable values (`context`), and if the receiver needs the latest callback without re-registering, wrap that callback in `rememberUpdatedState`.","**The deeper fix for many cases**: this kind of app-wide signal (connectivity, auth state) is usually better owned by a repository exposing a `Flow`, which the ViewModel observes — then the UI just collects state and no composable touches receivers at all. Mention this: the *most* robust answer moves the effect out of the UI layer entirely.","**Also consider lifecycle**: if the receiver should only be active while the screen is resumed, combine with a `LifecycleEventObserver` (also in a DisposableEffect) or use `Lifecycle.repeatOnLifecycle` semantics rather than composition presence."]}]},{level:"senior",q:"Why is it wrong to launch long-running work (like an upload that must complete) from LaunchedEffect, and where should it go?",a:[{t:"p",text:"**The concept**: `LaunchedEffect`'s coroutine is scoped to the *composable's presence in the composition*. The instant the composable leaves — the user navigates away, the screen is popped, a config change disposes and (for some hosts) recreates it — that coroutine is **cancelled**. That lifetime is correct for UI work (an animation, a screen-scoped load) but *wrong* for work that must finish regardless of what the UI does."},{t:"p",text:"**Concretely**: start a file upload in `LaunchedEffect` and the user backs out mid-upload → the upload is cancelled halfway. Even keeping the screen open, a rotation can interrupt it. The UI's lifetime is simply not the work's lifetime."},{t:"list",items:["**Screen-scoped-but-survives-config-change work** → the **ViewModel**, in `viewModelScope`. It survives rotation (the ViewModel outlives the composable) and is cancelled only when the ViewModel is cleared (screen truly gone). Most 'load data for this screen' work belongs here, not in LaunchedEffect — LaunchedEffect should usually just *trigger* a ViewModel function or collect its state.","**Work that must outlive even the screen** (uploads, syncs that should complete after the user leaves) → an **application-scoped `CoroutineScope`** injected into a repository, or an app-level component. It's not tied to any UI lifetime.","**Work that must survive process death / reboot / needs constraints** (guaranteed uploads, periodic sync) → **WorkManager**. It persists across process death and app restarts and enforces constraints (network, charging).","**So what IS LaunchedEffect for**: genuinely UI-lifetime, cancel-when-gone work — animations, collecting the ViewModel's event flow, one-shot 'on appear' triggers that call into the ViewModel. The rule: LaunchedEffect *initiates and observes*; it should rarely *own* important work."]}]},{level:"senior",q:"Should you load screen data in a LaunchedEffect or in the ViewModel? Why?",a:[{t:"p",text:"For most screen data, load it in the ViewModel (in `init` or lazily via a `StateFlow` with `WhileSubscribed`), not in a `LaunchedEffect`. The ViewModel survives configuration changes, so the data isn't re-fetched on rotation; a `LaunchedEffect` lives with the composition and is a UI concern. Reserve `LaunchedEffect` for effects that are genuinely tied to the composition's presence or to composable inputs."},{t:"code",title:"ViewModel-driven load vs LaunchedEffect",code:`// PREFERRED: ViewModel owns loading; survives rotation, no refetch
class ScreenVM(repo: Repo) : ViewModel() {
    val state = repo.observeData()
        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), Loading)
}

// LaunchedEffect is right when the effect depends on a composable input:
LaunchedEffect(itemId) { viewModel.load(itemId) }   // re-run when itemId changes`},{t:"list",items:["**ViewModel load** — survives config change (no refetch on rotation), testable without Compose, the single source of truth. Use `stateIn(WhileSubscribed)` so it starts when observed and stops when not.","**`LaunchedEffect`** — right when the trigger is a *composable input* (a passed `id` that changes) or a UI-lifetime concern; but note it *re-runs after process death recreation* and doesn't survive config change, so don't rely on it for expensive one-time fetches.","**Anti-pattern** — `LaunchedEffect(Unit) { viewModel.load() }` to load once: works, but rotation-safe loading belongs in the VM; use this only if the VM truly can't own it."]},{t:"note",text:"Load screen data in the ViewModel (init or stateIn(WhileSubscribed)) — it survives config change, so no refetch on rotation, and it's testable. Use LaunchedEffect when the trigger is a composable input that changes (LaunchedEffect(id)). Avoid LaunchedEffect(Unit) as a poor-man's one-time loader."}]},{level:"senior",q:"Does LaunchedEffect re-run on recomposition? On configuration change? When exactly does it restart or cancel?",a:[{t:"p",text:"A `LaunchedEffect` launches its coroutine when it enters the composition and *keeps it running across recompositions* as long as its keys are unchanged. It restarts (cancels the old coroutine, launches a new one) only when a key changes, and it cancels when the composable leaves the composition. It does *not* survive a configuration change (the composition is recreated, so the effect launches again)."},{t:"table",headers:["Event","LaunchedEffect behavior"],rows:[["Recomposition (same keys)","keeps running — does NOT restart"],["A key changes","cancels old coroutine, launches new one"],["Leaves composition (branch closes, disposed)","cancels the coroutine"],["Configuration change (rotation)","composition recreated → effect launches again"],["Process death & restore","recreated → effect launches again"]]},{t:"list",items:["**Recomposition alone doesn't restart it** — that's the whole point of keys; a re-run of the composable body doesn't relaunch the effect.","**Key change = restart** — the previous coroutine is cancelled (cooperatively) and a fresh one starts with the new key values.","**Leaving composition = cancel** — structured concurrency tears the coroutine down.","**Config change = relaunch** — because the composition is destroyed and rebuilt; this is why rotation-sensitive one-time work belongs in the ViewModel."]},{t:"note",text:"LaunchedEffect runs on enter, KEEPS running across recompositions (same keys), restarts on key change (cancels old + launches new), cancels on leaving composition, and relaunches on config change/process-death (composition rebuilt). Recomposition alone never restarts it — that's what keys are for."}]},{level:"junior",q:"How do you run code exactly once when a composable first appears, and what are the pitfalls?",a:[{t:"p",text:"Use `LaunchedEffect(Unit)` (or `LaunchedEffect(true)`) — a constant key means the effect launches once on enter and never restarts due to recomposition. But 'once' is scoped to *this composition instance*: it runs again if the composable leaves and re-enters, and after a configuration change or process-death recreation."},{t:"code",title:"Run-once-on-appear",code:`LaunchedEffect(Unit) {          // launches once when this composable enters
    analytics.logScreenView("Home")
}`},{t:"list",items:["**`LaunchedEffect(Unit)`** — one launch per composition entry; won't re-run on recomposition.","**Pitfall 1 — re-entry** — navigating away and back re-enters the composable, running it again. If you need truly-once (per app session/user), track it in the ViewModel.","**Pitfall 2 — config change** — rotation recreates the composition and re-runs the effect. Again, put session-once logic in the VM.","**Pitfall 3 — one-off events** — don't use `LaunchedEffect(Unit)` to consume a state flag as an event; model events as a Flow/Channel instead."]},{t:"note",text:"LaunchedEffect(Unit) runs once per composition entry (not per recomposition) — good for a screen-view log. But it re-runs on re-entry and config change/process death, so 'truly once per session' belongs in the ViewModel. Don't use it to consume event flags — use a Channel/Flow."}]},{level:"junior",q:"How do you collect a Flow in Compose safely with respect to lifecycle?",a:[{t:"p",text:"The safe way is `collectAsStateWithLifecycle()` (from lifecycle-runtime-compose), which collects only while the lifecycle is at least STARTED and stops when the app is backgrounded — avoiding wasted work and updates while off-screen. If you're collecting for a *side effect* (not state), use `LaunchedEffect` + `repeatOnLifecycle(STARTED)`."},{t:"code",title:"Two safe patterns",code:`// For STATE the UI reads:
val ui by viewModel.uiState.collectAsStateWithLifecycle()

// For SIDE EFFECTS (events) — collect only while STARTED:
LaunchedEffect(Unit) {
    lifecycleOwner.repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.events.collect { handle(it) }
    }
}`},{t:"list",items:["**`collectAsStateWithLifecycle`** — the default for turning a StateFlow into UI state; lifecycle-aware, pairs with `stateIn(WhileSubscribed)`.","**Plain `collectAsState`** — keeps collecting even in the background (multiplatform-safe but not Android-lifecycle-aware); avoid on Android when the source is hot/expensive.","**`repeatOnLifecycle` in a `LaunchedEffect`** — for collecting events/effects; it re-collects on each STARTED and cancels on STOP, preventing missed/duplicated handling."]},{t:"note",text:"Use collectAsStateWithLifecycle() for UI state (collects only while STARTED, stops when backgrounded, pairs with WhileSubscribed). For side-effect/event collection, LaunchedEffect + repeatOnLifecycle(STARTED). Plain collectAsState keeps running in the background — avoid for hot/expensive Android sources."}]},{level:"junior",q:"How do you show a snackbar in Compose, and why does it need a coroutine?",a:[{t:"p",text:"Snackbars are shown through a `SnackbarHostState`, whose `showSnackbar()` is a *suspend* function — it suspends until the snackbar is dismissed or its action is clicked, returning a `SnackbarResult`. So you call it from a coroutine (a `rememberCoroutineScope` for user actions, or a `LaunchedEffect` collecting events)."},{t:"code",title:"Snackbar wiring",code:`val snackbarHostState = remember { SnackbarHostState() }
Scaffold(snackbarHost = { SnackbarHost(snackbarHostState) }) { padding ->
    // Trigger from a collected event (recommended):
    LaunchedEffect(Unit) {
        viewModel.messages.collect { msg ->
            val result = snackbarHostState.showSnackbar(msg, actionLabel = "Undo")
            if (result == SnackbarResult.ActionPerformed) viewModel.undo()
        }
    }
}`},{t:"list",items:["**`showSnackbar` suspends** — it waits for dismissal/action, so it must run in a coroutine and it naturally queues (one at a time).","**Drive from events, not state** — showing a snackbar is a one-off event; collect it from a `Channel`/`SharedFlow` in a `LaunchedEffect` rather than reading a UiState flag (which re-shows on rotation).","**Return value** — `SnackbarResult.ActionPerformed` vs `Dismissed` lets you implement 'Undo'."]},{t:"note",text:"Show snackbars via SnackbarHostState.showSnackbar() — a suspend fn that waits for dismissal/action (queues automatically) and returns SnackbarResult (ActionPerformed → Undo). Drive it from an event Flow/Channel in a LaunchedEffect, not a UiState flag (which re-fires on rotation)."}]},{level:"senior",q:"How do you observe Android Lifecycle events (ON_RESUME, ON_PAUSE) from a composable?",a:[{t:"p",text:"Add a `LifecycleEventObserver` to the current `LifecycleOwner` inside a `DisposableEffect`, and remove it in `onDispose`. This lets a composable react to `ON_START`/`ON_RESUME`/`ON_PAUSE`/`ON_STOP` — for example, to pause a video, refresh on resume, or start/stop a sensor."},{t:"code",title:"Lifecycle observer in Compose",code:`val lifecycleOwner = LocalLifecycleOwner.current
DisposableEffect(lifecycleOwner) {
    val observer = LifecycleEventObserver { _, event ->
        when (event) {
            Lifecycle.Event.ON_RESUME -> player.play()
            Lifecycle.Event.ON_PAUSE  -> player.pause()
            else -> {}
        }
    }
    lifecycleOwner.lifecycle.addObserver(observer)
    onDispose { lifecycleOwner.lifecycle.removeObserver(observer) }  // cleanup!
}`},{t:"list",items:["**`DisposableEffect`** — needed because you must *remove* the observer when the composable leaves (else a leak).","**`LocalLifecycleOwner`** — gives the right lifecycle (Activity/Fragment/nav entry).","**Use cases** — pause/resume media, start/stop location or sensors, refresh-on-resume.","**Reusable helper** — many codebases wrap this in an `OnLifecycleEvent`/`LifecycleResumeEffect` composable (AndroidX now provides `LifecycleResumeEffect`/`LifecycleStartEffect`)."]},{t:"note",text:"Observe lifecycle via a LifecycleEventObserver added in DisposableEffect(lifecycleOwner) and removed in onDispose (or use AndroidX LifecycleResumeEffect/LifecycleStartEffect). Get the owner from LocalLifecycleOwner. Classic uses: pause/resume media, start/stop sensors, refresh on resume. DisposableEffect prevents the observer leak."}]},{level:"senior",q:"What is SideEffect and when is it the right choice?",a:[{t:"p",text:"`SideEffect { }` runs its block *after every successful recomposition* (i.e. when the composition commits). It's for publishing Compose state to *non-Compose* code that needs the latest value on each successful frame — not for coroutines (use `LaunchedEffect`) or setup/teardown (use `DisposableEffect`)."},{t:"code",title:"SideEffect publishing to non-Compose code",code:`SideEffect {
    // runs after each successful recomposition, with the latest values
    analytics.setCurrentScreen(screenName)          // update a non-Compose singleton
    thirdPartySdk.updateUser(userId)
}`},{t:"list",items:["**Runs on every successful recomposition** — not once; if that's not what you want, use `LaunchedEffect(key)`.","**No cleanup, no coroutine** — it's a synchronous block; for cleanup use `DisposableEffect`, for async use `LaunchedEffect`.","**Only on success** — unlike code in the composable body (which can run during abandoned/failed compositions), `SideEffect` runs only when the composition actually commits — safe for publishing state outward.","**Typical uses** — syncing state into an analytics SDK, a legacy controller, or `View`-world objects each frame."]},{t:"note",text:"SideEffect { } runs after every SUCCESSFUL recomposition (commit) — for pushing Compose state into non-Compose code (analytics SDK, legacy controller) that needs the latest value each frame. Not for coroutines (LaunchedEffect) or cleanup (DisposableEffect). Safer than the body: only runs on committed compositions."}]},{level:"senior",q:"What is produceState and when would you use it over LaunchedEffect + mutableStateOf?",a:[{t:"p",text:"`produceState` is a convenience that combines `remember { mutableStateOf(initial) }` with a `LaunchedEffect` that populates it — returning a `State<T>` you read directly. It's ideal for converting a non-Compose async source (a suspend call, a callback API) into observable state, with a clean initial value."},{t:"code",title:"produceState",code:`@Composable
fun loadUser(id: String): State<Result<User>> = produceState<Result<User>>(
    initialValue = Result.Loading,
    key1 = id,                         // re-launches when id changes
) {
    value = try { Result.Success(api.getUser(id)) } catch (e: Exception) { Result.Error(e) }
    // awaitDispose { } here if you registered a callback that needs cleanup
}`},{t:"list",items:["**= `mutableStateOf` + `LaunchedEffect`** — less boilerplate; you set `value` inside the producer.","**Keyed** — pass keys so it relaunches when inputs change (like LaunchedEffect keys).","**`awaitDispose`** — for callback sources, register in the block and clean up in `awaitDispose { }` (it suspends until the effect leaves).","**When to prefer explicit LaunchedEffect + state** — if you need more control, multiple state values, or to update a ViewModel instead of local state. For screen data, the ViewModel is still usually better; `produceState` shines for local, composable-scoped async values (e.g. loading a bitmap)."]},{t:"note",text:"produceState = remember { mutableStateOf(initial) } + LaunchedEffect that sets `value` — a tidy way to turn a suspend/callback source into observable State, keyed to inputs, with awaitDispose for cleanup. Great for composable-scoped async (load a bitmap); for screen data, prefer the ViewModel."}]},{level:"junior",q:"Why shouldn't you launch coroutines with GlobalScope or a raw CoroutineScope in a composable?",a:[{t:"p",text:"Coroutines started with `GlobalScope` (or a manually created scope) aren't tied to the composition or any lifecycle — they keep running after the composable leaves the screen, leaking work and capturing stale references. Compose gives you scoped alternatives (`LaunchedEffect`, `rememberCoroutineScope`) that cancel automatically when the composable is disposed."},{t:"list",items:["**`GlobalScope`** — app-lifetime; the coroutine outlives the screen, leaking and possibly updating gone UI. Almost never correct.","**Raw `CoroutineScope(...)` in the body** — created anew each recomposition (unless remembered) and never cancelled; leaks.","**Use `LaunchedEffect`** — for work tied to composition presence/keys; cancelled on leave.","**Use `rememberCoroutineScope()`** — for launching from callbacks (button clicks); its scope is cancelled when the composable leaves.","**Use `viewModelScope`** — for work that should survive config change (data loading)."]},{t:"note",text:"GlobalScope/raw scopes aren't lifecycle-tied — coroutines outlive the screen, leak, and touch dead UI. Use LaunchedEffect (composition-tied), rememberCoroutineScope() (launch from callbacks, cancels on leave), or viewModelScope (survives config change). Never GlobalScope in UI."}]},{level:"senior",q:"What are common mistakes with LaunchedEffect keys?",a:[{t:"p",text:"Keys control when a `LaunchedEffect` restarts, so getting them wrong causes either stale effects (missing keys) or thrashing (over-keying). The classic mistakes: omitting a key the effect depends on, passing an unstable/newly-created object as a key, or keying on something that changes every recomposition."},{t:"list",items:["**Missing a dependency** — `LaunchedEffect(Unit) { load(id) }` captures the *first* `id` and never reloads when `id` changes. Fix: `LaunchedEffect(id)`.","**Unstable/new key each recomposition** — passing a freshly-created lambda, list, or object as a key makes the effect restart every recomposition (it's never 'equal'). Fix: key on stable/primitive values, or use `rememberUpdatedState` for callbacks you want fresh *without* restarting.","**Over-keying** — keying on a whole object when only one field matters restarts on unrelated changes. Key on the specific field.","**Using a mutable object as key** — mutating it in place doesn't change equality, so the effect won't restart when you expect."]},{t:"code",title:"Right keys",code:`LaunchedEffect(userId) { load(userId) }   // restarts only when userId changes
// For a callback that must stay fresh but not restart the effect:
val current by rememberUpdatedState(onEvent)
LaunchedEffect(Unit) { delay(3000); current() }   // uses latest onEvent, no restart`},{t:"note",text:"Key mistakes: (1) missing a dependency → stale captured value (LaunchedEffect(Unit){load(id)}); (2) unstable/new key each recomposition (lambda/list/object) → constant restarts; (3) over-keying on a whole object; (4) mutable-object keys. Fix: key on the exact stable values; rememberUpdatedState for fresh callbacks without restart."}]},{level:"junior",q:"How do you trigger a refresh or retry as a side effect when some trigger changes?",a:[{t:"p",text:"Key a `LaunchedEffect` on a trigger value (a counter, a query, a retry token). Each time the trigger changes, the effect cancels and relaunches, running your refresh. This is a clean way to turn 'user tapped retry' or 'filter changed' into a re-fetch."},{t:"code",title:"Retry via a keyed effect",code:`var retryToken by remember { mutableStateOf(0) }
LaunchedEffect(query, retryToken) {     // relaunch when query OR retryToken changes
    result = repo.search(query)
}
Button(onClick = { retryToken++ }) { Text("Retry") }   // bump token -> effect reruns`},{t:"list",items:["**Key on the trigger** — changing the key (a token counter, or the query) restarts the effect, re-running the fetch.","**Retry token pattern** — increment an Int to force a re-run even when other inputs are unchanged.","**Often better in the VM** — for data, expose a `refresh()` on the ViewModel that re-emits; the UI stays declarative. Use the keyed-effect approach for composable-local async."]},{t:"note",text:"Key a LaunchedEffect on the trigger (query, or a retryToken Int you increment) — changing the key cancels + relaunches the effect, re-running the fetch. The retry-token bump forces a rerun with unchanged inputs. For screen data, a ViewModel refresh() is usually cleaner than a UI-side keyed effect."}]},{level:"junior",q:"How do you request a runtime permission or launch an activity-for-result from Compose?",a:[{t:"p",text:"Use `rememberLauncherForActivityResult` with an `ActivityResultContract`. It returns a launcher you call (from a click handler or an effect) to start the request; the result comes back in the callback. For permissions specifically, Accompanist/`rememberPermissionState` wraps this with permission status."},{t:"code",title:"Permission and activity result launchers",code:`val cameraPermission = rememberLauncherForActivityResult(
    ActivityResultContracts.RequestPermission()
) { granted -> if (granted) openCamera() else showRationale() }

Button(onClick = { cameraPermission.launch(Manifest.permission.CAMERA) }) { Text("Scan") }

// Pick an image:
val pickImage = rememberLauncherForActivityResult(
    ActivityResultContracts.GetContent()
) { uri -> uri?.let { viewModel.onImagePicked(it) } }
// pickImage.launch("image/*")`},{t:"list",items:["**`rememberLauncherForActivityResult`** — the Compose-friendly wrapper over the Activity Result API; survives recomposition.","**Launch from a callback/effect** — call `launcher.launch(input)` in `onClick` (or a `LaunchedEffect` for auto-request).","**Permissions** — `RequestPermission`/`RequestMultiplePermissions` contracts, or Accompanist's `rememberPermissionState`/`rememberMultiplePermissionsState` for status + rationale handling.","**Don't call `launch` during composition** — only from effects/callbacks; calling it in the body would fire every recomposition."]},{t:"note",text:"Use rememberLauncherForActivityResult(contract) { result -> } and call launcher.launch(input) from a click/effect — never in the composable body. Permissions: RequestPermission/RequestMultiplePermissions contracts, or Accompanist rememberPermissionState for status + rationale. It's the Compose wrapper over the Activity Result API."}]},{level:"senior",q:"What dispatcher does LaunchedEffect run on, and what is withFrameNanos?",a:[{t:"p",text:"A `LaunchedEffect` coroutine runs on the composition's `CoroutineContext`, whose dispatcher is `AndroidUiDispatcher.Main` — it dispatches work aligned with the Choreographer frame, on the main thread. So effect code runs on the main thread by default; move blocking/CPU work off it with `withContext(Dispatchers.IO/Default)`. `withFrameNanos` suspends until the next frame and gives you its timestamp — the basis of Compose animations."},{t:"list",items:["**Main-thread by default** — `LaunchedEffect` uses `AndroidUiDispatcher.Main`; safe to touch state/UI, but offload heavy work with `withContext`.","**Frame-aligned** — the UI dispatcher batches with the frame, so state written in effects is applied efficiently per frame.","**`withFrameNanos { frameTime -> }`** — suspends until the next display frame, returning its time in nanos; animation clocks (`Animatable`, `animate*AsState`) are built on it to advance per frame.","**Implication** — you can hand-roll frame-perfect animations with a `while (true) { withFrameNanos { ... } }` loop, though the animation APIs usually do this for you."]},{t:"note",text:"LaunchedEffect runs on AndroidUiDispatcher.Main (main thread, frame-aligned) — safe for state but offload heavy work with withContext(IO/Default). withFrameNanos suspends until the next frame and returns its timestamp; it's the primitive under Compose animations (per-frame advancement)."}]},{level:"junior",q:"Why is remember { } wrong for running work, and how does it differ from LaunchedEffect?",a:[{t:"p",text:"`remember { }` is for *caching a value* across recompositions — its block runs during composition to compute something, and it must be side-effect-free. `LaunchedEffect` is for *running side effects* (coroutines) tied to the composition lifecycle. Putting a network call or a coroutine launch in `remember` runs it during composition (which can happen unexpectedly, be abandoned, or run on the wrong thread) and isn't lifecycle-managed."},{t:"code",title:"Wrong vs right",code:`// WRONG: side effect during composition, not lifecycle-aware, may be abandoned
val data = remember { repo.blockingLoad() }     // runs in composition; blocks; no cancel

// RIGHT: value caching only
val formatter = remember { DateFormatter(locale) }   // pure computation, cached

// RIGHT: side effect
LaunchedEffect(id) { data = repo.load(id) }          // coroutine, cancellable, lifecycle-tied`},{t:"list",items:["**`remember`** — compute-and-cache a value; the block should be pure (no I/O, no launching coroutines).","**`LaunchedEffect`** — perform side effects; lifecycle-managed and cancellable.","**Why it matters** — composition can run, be skipped, or be abandoned; work in `remember` inherits that unpredictability and runs on the composition thread."]},{t:"note",text:"remember { } caches a PURE computed value across recompositions; its block must be side-effect-free. LaunchedEffect runs SIDE EFFECTS (coroutines), lifecycle-tied and cancellable. Doing I/O or launching in remember runs it during composition — unpredictable, uncancellable, wrong thread."}]},{level:"junior",q:"How do you handle the system back press from a composable?",a:[{t:"p",text:"Use the `BackHandler(enabled) { }` composable — it registers an `OnBackPressedCallback` tied to the composition, intercepting back while `enabled` is true. Use it to close a drawer, dismiss a custom sheet, confirm unsaved changes, or implement custom back navigation. It's effectively a lifecycle-scoped effect for the back button."},{t:"code",title:"BackHandler",code:`var showDialog by remember { mutableStateOf(false) }
BackHandler(enabled = hasUnsavedChanges) {
    showDialog = true        // intercept back to confirm, instead of leaving
}
// Or close a drawer on back:
BackHandler(enabled = drawerState.isOpen) { scope.launch { drawerState.close() } }`},{t:"list",items:["**`enabled` gates it** — when false, back behaves normally (navigates/exits). Toggle it based on state (drawer open, unsaved edits).","**Composition-scoped** — the callback is added/removed with the composable automatically (no manual cleanup).","**Order** — multiple enabled `BackHandler`s: the innermost (most recently composed) wins, like a stack.","**Predictive back** — for the newer predictive-back animations, use `PredictiveBackHandler` to react to the in-progress gesture."]},{t:"note",text:"BackHandler(enabled) { } intercepts the back press while enabled — for closing drawers/sheets, confirming unsaved changes, or custom back nav. It's composition-scoped (auto add/remove), innermost enabled handler wins. Use PredictiveBackHandler for predictive-back gesture animations."}]},{level:"junior",q:"How do you run periodic work like a ticking clock or countdown in Compose?",a:[{t:"p",text:"Put a loop with `delay` inside a `LaunchedEffect`, writing to state each tick. Because the effect is cancelled when the composable leaves, the loop stops automatically — no leak. Key the effect if the interval or target should reset it."},{t:"code",title:"A countdown timer",code:`var secondsLeft by remember { mutableIntStateOf(60) }
LaunchedEffect(Unit) {
    while (secondsLeft > 0) {
        delay(1000)
        secondsLeft--
    }
    onFinished()
}
Text("$secondsLeft s")`},{t:"list",items:["**`while + delay` in `LaunchedEffect`** — the coroutine ticks and updates state; cancelled on leave, so it stops cleanly.","**For a clock** — read `System.currentTimeMillis()` each tick, or use `withFrameNanos` for frame-accurate animation timing.","**Lifecycle-pause** — wrap in `repeatOnLifecycle(STARTED)` if you want the timer to pause when backgrounded.","**Prefer the VM for logic that must survive rotation** — a countdown that shouldn't reset on rotation belongs in the ViewModel; the UI just displays it."]},{t:"note",text:"Ticking work = while + delay inside LaunchedEffect, updating state each tick; it auto-cancels on leave (no leak). Use repeatOnLifecycle(STARTED) to pause when backgrounded, withFrameNanos for frame-accurate timing. If it must survive rotation, run it in the ViewModel and just display the state."}]},{level:"senior",q:"How do you bridge a callback-based API (like location updates) into Compose state?",a:[{t:"p",text:"You register the callback when the composable enters and unregister when it leaves — either with `DisposableEffect` (writing into `mutableStateOf`) or `produceState` (with `awaitDispose`). For a stream you'll transform, wrap the callback in a `callbackFlow` in the data layer and collect it; for a purely UI-scoped listener, `DisposableEffect`/`produceState` is fine."},{t:"code",title:"Callback → state with cleanup",code:`val location by produceState<Location?>(initialValue = null) {
    val listener = LocationListener { value = it }
    locationManager.requestUpdates(listener)
    awaitDispose { locationManager.removeUpdates(listener) }   // cleanup on leave
}

// Or DisposableEffect:
DisposableEffect(Unit) {
    val l = LocationListener { current = it }
    locationManager.requestUpdates(l)
    onDispose { locationManager.removeUpdates(l) }
}`},{t:"list",items:["**`produceState` + `awaitDispose`** — register in the block, write `value` from the callback, unregister in `awaitDispose`.","**`DisposableEffect` + `onDispose`** — same idea if you're writing to your own `mutableStateOf`.","**`callbackFlow`** — the cleaner data-layer approach: wrap the callback as a Flow (`awaitClose { }` for cleanup), expose it, and collect with `collectAsStateWithLifecycle`. Preferred when the source is shared or needs operators.","**Always unregister** — the whole point is that the effect's disposal removes the listener, preventing leaks and callbacks into dead UI."]},{t:"note",text:"Bridge callbacks via produceState + awaitDispose or DisposableEffect + onDispose — register on enter, write state from the callback, unregister on leave (no leak). For shared/streamed sources, wrap in a data-layer callbackFlow (awaitClose cleanup) and collectAsStateWithLifecycle. The disposal removing the listener is the key."}]},{level:"senior",q:"How do you decide what to pass as keys to LaunchedEffect or DisposableEffect?",a:[{t:"p",text:"Pass as keys every value the effect *depends on* whose change should restart the effect — and nothing that shouldn't. The mental test: 'if this value changes, do I want the effect to cancel and start over?' If yes, it's a key; if no (you just want the latest value without restarting), use `rememberUpdatedState` instead."},{t:"list",items:["**Include restart-worthy dependencies** — the `id` you're loading, the `query` you're searching, the interval a timer uses. Changing them should re-run the effect.","**Exclude 'latest value' callbacks** — an `onTimeout` lambda you want current but that shouldn't restart the timer: capture with `rememberUpdatedState`, key on `Unit`.","**Use stable/primitive keys** — Strings, Ints, stable data; avoid freshly-allocated lambdas/lists (never equal → constant restarts).","**`Unit`/`true`** — deliberately 'run once and never restart'; make sure that's really the intent (it captures initial values).","**Match to lifetime** — `DisposableEffect` keys decide when the listener is torn down and re-registered; over-keying causes churny register/unregister cycles."]},{t:"note",text:"Key on every dependency whose change SHOULD restart the effect ('if this changes, redo the work?') — id/query/interval. For 'want the latest value but don't restart' (callbacks), use rememberUpdatedState + Unit key. Keys must be stable/primitive (fresh lambdas/lists restart every recomposition). Unit = run-once, captures initial values."}]},{level:"senior",q:"How do you debounce fast-changing Compose state (like a text field) using snapshotFlow?",a:[{t:"p",text:"`snapshotFlow { }` converts snapshot state into a cold Flow that emits when the read state changes, so you can apply Flow operators like `debounce` to it inside a `LaunchedEffect` — debouncing a search on the UI side without pushing raw keystrokes anywhere. (In MVVM, you'd usually debounce in the ViewModel's Flow instead, but `snapshotFlow` is the composable-side tool.)"},{t:"code",title:"snapshotFlow debounce",code:`var text by remember { mutableStateOf("") }
LaunchedEffect(Unit) {
    snapshotFlow { text }              // emits on each text change
        .debounce(300)                 // wait for a typing pause
        .distinctUntilChanged()
        .collect { q -> onSearch(q) }  // fires only after the pause
}
TextField(value = text, onValueChange = { text = it })`},{t:"list",items:["**`snapshotFlow { text }`** — bridges Compose state → Flow; the block re-reads on change and emits distinct values.","**Apply operators** — `debounce`, `filter`, `mapLatest` work as on any Flow, inside the `LaunchedEffect`.","**Stays alive with the composable** — the collection is scoped to the effect, cancelled on leave.","**Prefer VM for real search** — a shared/testable search flow belongs in the ViewModel; use `snapshotFlow` when the state genuinely lives in the composable."]},{t:"note",text:"snapshotFlow { state } turns Compose state into a Flow so you can debounce/filter it inside a LaunchedEffect (scoped, cancels on leave). Good for composable-side debouncing of a text field. For shared/testable search logic, debounce in the ViewModel's Flow instead."}]},{level:"junior",q:"What's the difference between LaunchedEffect and rememberCoroutineScope in practice?",a:[{t:"p",text:"Both give you a composition-scoped coroutine, but the trigger differs: `LaunchedEffect` launches *automatically* when it enters composition (or a key changes), while `rememberCoroutineScope()` gives you a scope to launch from *imperatively* — inside a callback like `onClick`. Use `LaunchedEffect` for effects driven by state/entering; use `rememberCoroutineScope` for effects driven by user events."},{t:"code",title:"Event-driven vs entry-driven",code:`// Entry/state driven -> LaunchedEffect
LaunchedEffect(itemId) { data = repo.load(itemId) }

// User-event driven -> rememberCoroutineScope
val scope = rememberCoroutineScope()
Button(onClick = {
    scope.launch { snackbarHostState.showSnackbar("Saved") }   // launch on click
}) { Text("Save") }`},{t:"list",items:["**`LaunchedEffect`** — you can't call `launch` inside `onClick` from it; it's for automatic, keyed effects.","**`rememberCoroutineScope`** — returns a `CoroutineScope` tied to the composition; call `scope.launch { }` from callbacks. Cancelled when the composable leaves.","**Don't create your own scope** — `rememberCoroutineScope` gives the correctly-cancelled one."]},{t:"note",text:"LaunchedEffect launches automatically on enter/key-change (state-driven); rememberCoroutineScope() gives a composition-scoped CoroutineScope to launch from callbacks like onClick (event-driven). You can't launch from a click inside a LaunchedEffect — use the scope. Both cancel on leave."}]},{level:"senior",q:"What is the correct way to consume one-time events so they don't re-fire on rotation?",a:[{t:"p",text:"Deliver one-time events (navigate, snackbar, toast) through a `Channel` exposed as a `Flow` (or a `SharedFlow` with `replay = 0`), and collect them in a lifecycle-aware effect. Because a `Channel` delivers each item exactly once to a single collector, rotation (which re-collects) won't replay past events — unlike a state flag, which is re-read after recreation and re-fires."},{t:"code",title:"Channel-based events, collected safely",code:`// ViewModel
private val _events = Channel<UiEvent>()
val events = _events.receiveAsFlow()
fun onDone() { viewModelScope.launch { _events.send(UiEvent.NavigateBack) } }

// UI
LaunchedEffect(Unit) {
    lifecycleOwner.repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.events.collect { e -> when (e) { is NavigateBack -> nav.popBackStack() } }
    }
}`},{t:"list",items:["**Why not a state flag** — `uiState.navigate = true` is re-read after rotation/process death and navigates again; you'd have to reset it immediately, which is fragile.","**`Channel.receiveAsFlow()`** — exactly-once delivery to one collector; buffered so events aren't lost if briefly no collector.","**`SharedFlow(replay = 0)`** — an alternative, but events can drop if emitted with no active collector; a `Channel` is safer for guaranteed handling.","**Collect with `repeatOnLifecycle`/`collectAsStateWithLifecycle` semantics** — so you don't handle events while the UI is stopped."]},{t:"note",text:"Emit one-time events via a Channel.receiveAsFlow() (exactly-once, buffered) and collect with repeatOnLifecycle(STARTED) — rotation re-collects but won't replay past events. A UiState flag re-fires after recreation (must reset, fragile). SharedFlow(replay=0) works but can drop with no collector; Channel is safer."}]}],nS={id:"side-effects",title:"Side Effects",tagline:"LaunchedEffect, rememberCoroutineScope, DisposableEffect, SideEffect, rememberUpdatedState, produceState & snapshotFlow — which one, when, and why.",tags:["Compose","LaunchedEffect","DisposableEffect","Effects","Lifecycle"],content:aS,qa:iS},oS=[{heading:"The mental model: recomposition is cheap, but only if skipping works",blocks:[{t:"p",text:"Compose's performance story rests on **skipping**: when a composable is about to recompose, Compose checks each of its inputs — if they're all *equal* to last time, it **skips** executing that composable entirely and reuses its previous output. A well-behaved Compose UI recomposes a handful of small functions per state change because everything else is skipped. Performance problems are almost always **skipping failing** — composables re-executing when their data didn't meaningfully change. To make skipping work, Compose must be able to (a) *compare* inputs reliably and (b) *trust* that equal inputs mean unchanged behavior. Those two requirements are what **stability** is about."},{t:"note",text:"Reframe every 'my Compose screen is slow' question as: 'which composables are recomposing that shouldn't, and why is skipping not kicking in?' That reframing is itself a strong signal."}]},{heading:"Stable vs unstable types — the core concept",blocks:[{t:"p",text:"A type is **stable** (in Compose's sense) if Compose can rely on two things: its `equals()` is consistent with whether a recomposition-relevant change happened, and if any public property changes, Compose is *notified* (either it's immutable, or it uses snapshot state). If a composable's parameters are all stable, Compose marks the composable **skippable** — it can compare old vs new and skip. If any parameter is **unstable**, Compose *can't trust the comparison*, so it conservatively re-executes the composable **every time its parent recomposes**, even if the data is identical."},{t:"list",items:["**Stable by default**: all primitives (`Int`, `Boolean`, `Float`…), `String`, function types (lambdas), and any class the compiler infers as stable (immutable with stable properties). `MutableState` is stable (changes are observed).","**Unstable by default**: `List`, `Map`, `Set`, and other collection *interfaces* — because the interface could be backed by a mutable implementation; Compose can't prove immutability from the type. Also: any class with a `var` property, or a `val` of an unstable type, or classes from modules the compiler can't analyze (e.g. a `Date` from the stdlib, a model from a module without the Compose compiler).","**The subtle one**: `val items: List<Item>` is unstable *even though you never mutate it*, because the static type `List` doesn't guarantee immutability. This single fact causes most real-world unnecessary recomposition."]},{t:"code",title:"Why an unstable parameter breaks skipping",code:`// UNSTABLE: List is an interface -> could be a MutableList ->
// Compose can't prove equality is meaningful -> ItemRow NOT skippable ->
// recomposes on every parent recomposition even if 'items' is identical
@Composable fun ItemList(items: List<Item>) { /* ... */ }

// FIX 1: immutable collection type the compiler trusts
@Composable fun ItemList(items: ImmutableList<Item>) { /* skippable */ }

// FIX 2: annotate the holder as stable/immutable
@Immutable data class ItemsUi(val items: List<Item>)`}]},{heading:"@Stable and @Immutable — promising stability to the compiler",blocks:[{t:"p",text:"When you *know* a type is stable but the compiler can't prove it (e.g. it holds a `List` you promise never to mutate, or comes from a non-Compose module), you can annotate it — a **promise** the compiler trusts without verifying:"},{t:"list",items:["**`@Immutable`** — a strong promise: *all* public properties are `val` and never change after construction (deeply). Compose treats instances as fully interchangeable by `equals`. Use for read-only UI models.","**`@Stable`** — a weaker promise: the type may have mutable properties, *but* any change is observable via snapshot state, and `equals` is consistent. Use for observable holders.","**These are promises, not checks**: if you annotate `@Immutable` and then mutate the underlying list, Compose will skip recompositions it shouldn't and you get **stale UI** — a genuinely nasty, silent bug. Only annotate what's actually true.","**The cleaner alternative**: use `kotlinx.collections.immutable` (`ImmutableList`, `persistentListOf()`) — the compiler *knows* these are immutable, so no promise/annotation needed and no risk of lying."]}]},{heading:"Strong skipping mode — what changed in recent Compose",blocks:[{t:"p",text:"Recent Compose (compiler 2.x, on by default now) introduced **strong skipping mode**, which softens the stability rules significantly — worth knowing because it changes the 'right answer' to older interview questions:"},{t:"list",items:["With strong skipping, composables with **unstable parameters become skippable too** — Compose compares unstable params by **instance equality** (`===`) instead of giving up. So if the parent passes the *same list instance*, the child skips.","**Lambdas are auto-remembered**: previously, an unstable capture in a lambda made it a new instance each recomposition (breaking skipping); strong skipping auto-remembers lambdas, removing a whole class of manual `remember` workarounds.","**What this means practically**: much of the old 'wrap every List in ImmutableList or nothing skips' pain is reduced. But it's not a free pass — passing a *new* list instance each time (e.g. `items.filter { }` inline in the body) still fails `===` and still recomposes. Stability discipline still matters; strong skipping just raises the floor.","Interview-ready phrasing: 'Strong skipping makes unstable-parameter composables skippable via instance equality and auto-remembers lambdas — so the classic advice shifted from *make everything stable or nothing skips* to *avoid creating new instances of your data each recomposition*.'"]}]},{heading:"Diagnosing recomposition — the tools",blocks:[{t:"list",items:["**Layout Inspector (Android Studio)** — shows live **recomposition counts** and **skip counts** per composable while you interact. The first tool: find the composable whose count climbs when it shouldn't. High recompositions + low skips on a static element = a stability problem.","**Compose Compiler Metrics/Reports** — a compiler flag emits reports listing every composable as `skippable`/`restartable` and every class as `stable`/`unstable`, with *why*. This is how you find *which* type is unstable and dragging a composable down. Run it, grep for your slow screen's params.","**Composition tracing** — shows composable execution on the system trace timeline (Perfetto) with names, so you see time spent composing during a janky frame.","**Macrobenchmark + `FrameTimingMetric`** — measures real jank (frame durations) on release builds for scroll/startup; the objective number to optimize against.","**Recomposition highlighting** (developer tooling) — flashes recomposing regions so you can *see* over-recomposition visually."]},{t:"note",text:"Process to state in an interview: reproduce on a release build → Layout Inspector to find the over-recomposing composable → Compiler Reports to find *why* (which unstable type) → fix (stability/keys/deferred reads) → Macrobenchmark to confirm. Naming this workflow beats reciting fixes."}]},{heading:"The optimization toolkit — fixes in order of impact",blocks:[{t:"list",items:["**Fix stability first**: make list-typed params `ImmutableList`/`persistentList`, annotate genuinely-immutable models `@Immutable`, ensure UI models are immutable data classes. Biggest win, addresses the root cause.","**Provide keys in lazy lists** — identity correctness prevents cascade recomposition (see Lazy Lists topic).","**Defer state reads to the latest phase**: for high-frequency values (scroll, drag, animation), read in *lambda* modifiers (`Modifier.offset { }`), `graphicsLayer { }`, or `drawBehind { }` so only layout/draw re-runs, skipping composition entirely. This is the single biggest win for animations.","**Use `derivedStateOf`** to convert high-frequency inputs into low-frequency outputs (recompose only when the *result* changes).","**Keep lambdas stable**: method references (`viewModel::onClick`) or remembered lambdas so children stay skippable (largely automatic under strong skipping, but still relevant when capturing unstable values).","**Don't hoist state higher than needed**: reading state in a big parent invalidates the big scope; read it lower (pass lambdas that read where used) to shrink the recomposition scope.","**Defer reads via lambdas at API boundaries**: pass `() -> Int` instead of `Int` for frequently-changing values so the reading scope, not the caller, tracks the change."]}]},{heading:"Baseline profiles and startup — the production performance lever",blocks:[{t:"p",text:"Compose code ships as bytecode that must be JIT-compiled at runtime — and on first run (and first scroll of a list), that compilation happens *during* the user's interaction, causing startup lag and initial jank. **Baseline Profiles** fix this: they're a list of hot methods/classes precompiled to native code at install time (AOT), so the critical paths are already fast on first launch."},{t:"list",items:["You generate one with a **Macrobenchmark test** that exercises the critical journeys (startup, scroll the main feed); it records the hot paths into `baseline-prof.txt` shipped with the app.","Real-world impact is large: commonly **20–40% faster startup** and dramatically smoother first-scroll — one of the highest-ROI things you can do, and often the *correct* answer to 'my list is janky' when the code is already clean.","Compose libraries ship their own baseline profiles, but your *app's* screens need your own profile to benefit.","Related startup levers: `App Startup` library for initializer ordering, avoiding heavy work in `Application.onCreate`, and lazy-initializing expensive dependencies. Startup is measured as cold/warm/hot; baseline profiles primarily help cold start and first-use jank."]},{t:"note",text:"The crucial testing caveat that resolves most false alarms: **never judge Compose performance on a debug build.** Debug has no R8, extra runtime assertions, and no AOT/baseline compilation — it's *expected* to be janky. Always measure on release (minified) builds, ideally with baseline profiles applied."}]}],sS=[{level:"junior",q:"What does it mean for a composable to be 'skippable', and why does it matter for performance?",a:[{t:"p",text:"**The concept**: when Compose is about to recompose a composable, it can sometimes avoid running it at all. If the composable is **skippable**, Compose compares its current parameters to the values from last time — and if they're all equal, it **skips** executing the function entirely and reuses the previous UI. A skippable composable that gets the same inputs is essentially free."},{t:"p",text:"**Why it matters**: Compose's whole performance model depends on skipping. A state change high in the tree triggers recomposition, but the intent is that *most* of the tree skips — only the few composables whose data actually changed re-execute. When skipping works, a screen recomposes a handful of small functions per change. When skipping *fails* (a composable is not skippable, or its inputs look changed when they aren't), composables re-run needlessly, and that wasted work is what shows up as jank. So 'is this composable skippable, and are its inputs stable?' is the central performance question."}]},{level:"junior",q:"What makes a type 'stable' or 'unstable' in Compose, and why is List unstable?",a:[{t:"p",text:"**The concept**: for Compose to skip a composable, it must trust two things about each parameter's type. One: `equals()` reliably tells whether anything recomposition-relevant changed. Two: if a property *does* change, Compose is notified — meaning the type is either immutable, or backed by observable snapshot state. A type satisfying both is **stable**; Compose can compare it and skip. A type that might change without notice, or whose equality can't be trusted, is **unstable** — and Compose responds by *never* skipping composables that take it, re-running them on every parent recomposition to be safe."},{t:"p",text:"**Why `List` is unstable** (the classic gotcha): `List` is an *interface*. A value typed as `List<Item>` could actually be a `MutableList` underneath — the type gives Compose no guarantee it won't change. Since Compose can't prove immutability from the declared type, it conservatively marks `List` unstable. The surprising consequence: `fun Row(items: List<Item>)` recomposes even when you *never* mutate the list, purely because the *type* doesn't promise immutability."},{t:"list",items:["**Stable by default**: primitives, `String`, lambdas, `MutableState`, and classes the compiler infers immutable.","**Unstable by default**: `List`/`Map`/`Set` (interfaces), any class with a `var`, any class holding an unstable property, and types from modules without the Compose compiler (a `Date`, a model from a pure-Kotlin module).","**Fixes**: use `ImmutableList`/`persistentListOf()` from kotlinx-collections-immutable (compiler-verified immutable), or wrap in an `@Immutable` data class."]}]},{level:"junior",q:"What are @Stable and @Immutable annotations and when do you use them?",a:[{t:"p",text:"**The concept**: sometimes *you* know a type is stable but the *compiler can't prove it* — for example it holds a `List` you promise never to mutate, or it comes from a module the Compose compiler doesn't process. These annotations let you **promise** stability, which the compiler then trusts without verifying."},{t:"list",items:["**`@Immutable`** — the strong promise: every public property is a `val` and nothing changes after construction (all the way down). Use it on read-only UI models (`@Immutable data class ProfileUi(...)`). Instances become fully comparable by `equals` for skipping.","**`@Stable`** — the weaker promise: the object *may* have changing state, but every change is exposed through snapshot state (so Compose gets notified) and `equals` is consistent. Use it on observable holders."]},{t:"p",text:"**The critical caveat**: these are promises, not checks. If you annotate a class `@Immutable` and then actually mutate its list, Compose will trust the promise and skip recompositions it *shouldn't* — leaving **stale UI** that silently shows old data. That's a genuinely hard bug to track down. So only annotate what's truly immutable. The safer path when possible: use `ImmutableList`/`persistentListOf` so the compiler *knows* the truth and you never have to promise anything."}]},{level:"junior",q:"How do you find out which composables are recomposing too much?",a:[{t:"p",text:"**The primary tool is the Layout Inspector in Android Studio.** With the app running, it shows a live tree of composables with two counts per node: how many times each **recomposed** and how many times it **skipped**. You interact with the app and watch the numbers — a composable whose recomposition count climbs while you do something unrelated to it (a static header incrementing during scroll, say) is your culprit. Healthy nodes show lots of skips and few recompositions; a problem node shows the opposite."},{t:"p",text:"**Then, to find *why*, use the Compose Compiler Reports**: a compiler flag makes the build emit files that classify every composable as skippable/restartable and every class as stable/unstable — *with reasons*. So once Layout Inspector tells you *which* composable, the reports tell you *which parameter's type* is unstable and defeating skipping. For real jank numbers on release builds, `Macrobenchmark` with `FrameTimingMetric` measures actual frame durations. The workflow is: Layout Inspector (where) → Compiler Reports (why) → fix → Macrobenchmark (confirm)."}]},{level:"junior",q:"Why should you never judge Compose performance on a debug build?",a:[{t:"p",text:"**The concept**: a debug build is deliberately un-optimized. It has no R8/minification, it includes extra Compose runtime assertions and debugging hooks, and — most importantly — its code runs through the JIT compiler *as you use it* rather than being precompiled. There's no baseline profile applied. So scrolling a list in debug means the device is literally compiling the scroll code on the fly, mid-gesture."},{t:"p",text:"**The consequence**: debug builds are *expected* to be janky, sometimes dramatically so, even when the code is perfectly optimized. A huge fraction of 'my Compose list stutters' reports simply disappear when tested on a release build. So the rule is: always measure performance on a **release (minified) build**, ideally with baseline profiles, before concluding anything is wrong. Saying this first in an interview shows you know the difference between a real problem and a measurement artifact."}]},{level:"senior",q:"Walk through diagnosing and fixing a screen that recomposes excessively.",a:[{t:"list",items:["**0. Confirm it's real**: reproduce on a *release* build. Rule out the debug-JIT artifact before spending time.","**1. Locate (where)**: Layout Inspector recomposition/skip counts while interacting. Identify the composable(s) whose recomposition count grows disproportionately — e.g. a whole list item recomposing on every keystroke in an unrelated search box.","**2. Explain (why)**: enable Compose Compiler Reports. Look up the offending composable — is it marked `skippable`? If not, which parameter is `unstable`? Trace the unstable type (usually a `List`, a model with a `var`, or a class from a non-Compose module).","**3. Fix at the root — stability**: convert list params to `ImmutableList`/`persistentListOf`; make UI models immutable data classes; `@Immutable`-annotate models from modules the compiler can't see; replace `var`s with `val` + copy.","**4. Fix scope size**: if a big parent scope invalidates on a value only a child needs, read the value lower — pass a lambda (`() -> T`) that reads it where used, shrinking the invalidated scope. Don't hoist state higher than its consumers.","**5. Fix high-frequency reads**: for scroll/drag/animation, defer the read to layout/draw via lambda modifiers (`Modifier.offset { }`, `graphicsLayer { }`) or funnel through `derivedStateOf` so composition doesn't re-run per frame.","**6. Keys**: in lazy lists, ensure stable `key = { it.id }` to stop cascade recomposition on data changes.","**7. Verify**: re-check Layout Inspector counts and Macrobenchmark frame timings to confirm the fix, not just assume it."]},{t:"p",text:"The senior signal is the *ordered, measurement-driven* process — locate with tools, explain with reports, fix the root cause (usually stability), verify with numbers — rather than reflexively sprinkling `remember` and `derivedStateOf` hoping something helps."}]},{level:"senior",q:"What is 'strong skipping mode' and how did it change stability best practices?",a:[{t:"p",text:"**The background**: originally, if a composable had *any* unstable parameter, it was not skippable at all — Compose re-ran it on every parent recomposition. That made the classic advice near-absolute: wrap every `List` in `ImmutableList`, make every model stable, or nothing skips. It was a lot of ceremony, and unstable models from other modules were a constant headache."},{t:"p",text:"**What strong skipping changed** (Compose compiler 2.x, now the default): composables with unstable parameters become **skippable anyway**, by comparing those unstable parameters using **instance equality (`===`)** instead of giving up. So if the parent passes the *same instance* of an unstable list, the child now skips. Additionally, **lambdas are automatically remembered**, which eliminated a whole category of manual `remember { }` workarounds that existed because a fresh lambda instance each recomposition used to break a child's skippability."},{t:"list",items:["**The best-practice shift**: from *'make everything stable or nothing skips'* to *'don't create new instances of your data on every recomposition.'* Instance equality only helps if you pass the same instance.","**What still bites**: computing a new collection inline in the body — `items.filter { it.active }` passed to a child — produces a *new* list instance each recomposition, fails `===`, and still recomposes. Fix by remembering the derived list (`remember(items) { items.filter { } }`) or computing it in the ViewModel.","**Stability still matters** for correctness of `equals`-based skipping of *stable* types and for readability of compiler reports — strong skipping raises the floor, it doesn't make stability irrelevant. The mature answer notes both: less manual ceremony now, but 'same instance' discipline replaced 'stable type' discipline rather than removing it."]}]},{level:"senior",q:"How does deferring state reads across the three phases optimize performance? Give a concrete example.",a:[{t:"p",text:"**The concept**: a Compose frame has three phases — composition (run composables), layout (measure/place), draw (paint). Crucially, *the phase in which you read a state value determines which phases must re-run when that value changes*. Read a value during composition and a change forces composition → layout → draw (the full pipeline). Read the *same* value only during layout, and a change skips composition and re-runs just layout → draw. Read it only during draw, and only draw re-runs. For a value that changes 60 times a second (a drag or animation), reading it in composition means recomposing 60 times a second — usually the actual cause of animation jank."},{t:"code",title:"The same animation, three cost levels",code:`// WORST: reads animated offset in COMPOSITION -> recomposes every frame
Box(Modifier.offset(x = animatedX.value.dp))

// BETTER: lambda form reads during LAYOUT -> composition skipped
Box(Modifier.offset { IntOffset(animatedX.value.roundToPx(), 0) })

// For pure visual props (alpha, scale, rotation, color): DRAW only
Box(Modifier.graphicsLayer { alpha = animatedAlpha.value })`},{t:"list",items:["**The mechanism**: the *lambda* forms of `offset`, and `graphicsLayer`/`drawBehind`/`drawWithContent`, take a lambda that Compose invokes during the layout or draw phase. By reading the state *inside* that lambda, the read is recorded against the layout/draw phase, not composition — so the state change invalidates only that phase.","**Concrete win**: animating position with `Modifier.offset(x = state.dp)` recomposes the composable (and re-skips its children, re-diffs, etc.) every frame; switching to `Modifier.offset { }` drops composition work to zero and the animation just re-lays-out — often the difference between 60fps and dropped frames on a complex element.","**Rule of thumb**: any value that changes at animation/gesture frequency should be read as *late* as possible — draw if it's purely visual (alpha, scale, color), layout if it's positional, and only composition if it genuinely changes *what* is composed.","This pairs with `derivedStateOf` (reduce read *frequency*) — deferring reduces *phase cost per read*, derivedStateOf reduces *number of invalidations*; together they handle most high-frequency-state performance issues."]}]},{level:"senior",q:"What are Baseline Profiles, why do they matter for Compose specifically, and how do you create one?",a:[{t:"p",text:"**The problem they solve**: Android ships apps as DEX bytecode that must be compiled to native code to run fast. Without help, this happens *just-in-time* — the device compiles hot methods as they first execute. For Compose that's especially painful: the composition/layout machinery and your screen code all compile *during* the user's first launch and first scroll, causing cold-start lag and initial-scroll jank even in a clean codebase."},{t:"p",text:"**What a Baseline Profile is**: a shipped list of the app's hot classes and methods (the critical startup and scroll paths) that the Android runtime **ahead-of-time compiles at install time**. So on the very first launch, those paths are already native — no mid-interaction JIT. The measured impact is significant: commonly **20–40% faster cold startup** and markedly smoother first scroll of the main list."},{t:"list",items:["**How you create it**: write a `Macrobenchmark` test using `BaselineProfileRule` that drives the critical user journeys — launch the app, scroll the main feed, open key screens. Running it records the exercised methods into a `baseline-prof.txt` bundled with the app; the Baseline Profile Gradle plugin automates generating and packaging it.","**Compose-specific note**: Compose *libraries* ship their own baseline profiles, so the framework's internals are covered — but *your app's* screens aren't, so you must generate a profile for your own critical journeys to get the app-level benefit.","**Where it fits in the performance story**: when a list is janky and the *code is already clean* (stable types, keys, deferred reads), a baseline profile is often the remaining big win — and it's the correct answer to 'first scroll always stutters but subsequent scrolls are fine' (that pattern is classic JIT-on-first-use).","**Related lever**: `startup profiles` (a variant applied at build/dexlayout time) and keeping `Application.onCreate` lean; baseline profiles primarily target cold start and first-use compilation."]}]},{level:"senior",q:"A data class parameter to a composable is unstable because it has one property from a non-Compose module. What are your options?",a:[{t:"p",text:"**Why this happens**: stability is *transitive* — a data class is only inferred stable if *all* its properties are stable. If one property is a type from a module the Compose compiler doesn't process (say a `java.util.Date`, a protobuf-generated class, or a model from a pure-Kotlin `:core:model` module that doesn't apply the Compose compiler), the compiler can't prove that property stable, so it marks the whole enclosing data class unstable — and every composable taking it becomes non-skippable."},{t:"list",items:["**Option 1 — annotate the holder `@Immutable`/`@Stable`**: if the class is genuinely immutable, `@Immutable data class Foo(val date: Date)` promises stability and restores skippability. Cheap, but a *promise* — only valid if it's truly immutable, and it doesn't help other composables using `Date` directly.","**Option 2 — map to a UI model** at the ViewModel boundary: convert the foreign property into a stable type (e.g. `Date` → a preformatted `String` or an epoch `Long`) so the UI model is inferred stable with no annotation and no promise. Often the *best* answer because the UI should receive render-ready data anyway (formatting belongs in the ViewModel), so this fixes stability *and* layering at once.","**Option 3 — the `stability configuration file`**: the Compose compiler lets you list external classes to *treat as stable* project-wide (e.g. add `java.time.LocalDate` to `stability_config.conf`). Ideal for widely-used third-party types you can't annotate and know are immutable — one config entry fixes them everywhere.","**Option 4 — apply the Compose compiler to your own modules**: if the unstable type is *your* model in a `:core:model` module, applying the Compose compiler plugin there lets it infer stability normally (an immutable data class becomes stable automatically). Best for first-party code.","**Option 5 — rely on strong skipping**: with strong skipping on, if the caller passes the *same instance*, the composable skips via `===` even with the unstable param — sometimes 'good enough' without changing types, as long as you're not creating new instances each recomposition."]},{t:"p",text:"The senior instinct: prefer the fix that also improves the design — mapping to a render-ready UI model (Option 2) or compiling your own modules (Option 4) — over sprinkling `@Immutable` promises you have to keep true forever."}]},{level:"junior",q:"What actually happens during recomposition, and why is it usually cheap?",a:[{t:"p",text:"Recomposition re-executes the composable functions whose observed state changed, producing an updated description of the UI; Compose then diffs that against the current slot table and applies only the differences to the LayoutNode tree. It's usually cheap because Compose *skips* composables whose inputs didn't change (positional memoization) and only re-runs the small subtrees that actually depend on the changed state."},{t:"list",items:["**Only affected scopes re-run** — the 'restart scope' reading the changed state re-executes; unrelated composables are skipped.","**Skipping** — a composable with unchanged, stable inputs is skipped entirely (its previous output is reused).","**Donut-hole skipping** — a parent can recompose while its children are skipped (and vice-versa), so a state change high up doesn't force everything below to re-run.","**Then layout/draw** — recomposition only rebuilds the composition; layout and draw run only for nodes that changed, often skipped too."]},{t:"note",text:"Recomposition re-runs only the composables reading changed state, diffs the result, and applies minimal updates to the node tree. It's cheap because of skipping (unchanged stable inputs → skip) and donut-hole skipping (parent recomposes while children don't). Layout/draw then run only for changed nodes."}]},{level:"senior",q:"How does the Compose compiler determine stability, and what are the exact rules?",a:[{t:"p",text:"The Compose compiler infers whether each type is *stable* — meaning Compose can trust that `equals` reflects real change and that its public properties won't mutate without notice. Stability drives skipping: a composable is skippable only if all its parameters are stable. The compiler applies concrete rules and marks each type stable or unstable."},{t:"list",items:["**Primitives, String, function types** — stable.","**A class is stable if** all its public `val` properties are of stable types *and* it has no public `var` (mutable) properties. `data class User(val id: Int, val name: String)` → stable.","**`var` property → unstable** — a public `var` means it can mutate silently, so Compose can't trust `equals`.","**Interfaces/abstract types → unstable by default** — the compiler can't see the implementation (e.g. `List` is an interface; the runtime could be a mutable list), so `List`, `Map`, `Set` are treated unstable.","**Classes from other modules without the Compose compiler** — treated unstable (the compiler couldn't analyze them).","**`MutableState`, stable collections (`ImmutableList`), and `@Stable`/`@Immutable`-annotated types** — stable."]},{t:"note",text:"A type is stable if equals reflects real change and its public props can't mutate unnoticed: primitives/String/lambdas stable; a class is stable iff all public props are stable vals with no public var; interfaces (List/Map/Set) and other-module classes are unstable by default. Skippable requires ALL params stable."}]},{level:"senior",q:"How do you generate and inspect Compose compiler metrics/stability reports?",a:[{t:"p",text:"The Compose compiler can emit *stability reports* and *metrics* — text files listing which composables are skippable/restartable and which classes are stable/unstable, with the reason. You enable them via compiler options, then read the generated files to find the exact parameters hurting performance."},{t:"code",title:"Enabling compiler reports (Gradle)",code:`// build.gradle(.kts) — Compose compiler options
composeCompiler {
    reportsDestination = layout.buildDirectory.dir("compose_reports")
    metricsDestination = layout.buildDirectory.dir("compose_metrics")
}
// Older setups pass -P plugin:androidx.compose.compiler...reportsDestination=...
// Outputs: *-composables.txt (skippable/restartable), *-classes.txt (stable/unstable + why)`},{t:"list",items:["**`*-composables.txt`** — lists each composable as `restartable skippable` or not, and flags unstable params.","**`*-classes.txt`** — lists each class `stable`/`unstable` and *why* (e.g. 'unstable field items: List').","**Use it to target fixes** — instead of guessing, you see exactly which type is unstable and can annotate it, wrap it, or use an immutable collection.","**Also — Layout Inspector** shows live recomposition counts at runtime; the reports are the *static* view."]},{t:"note",text:"Enable composeCompiler { reportsDestination / metricsDestination } to emit stability reports: *-composables.txt (skippable/restartable + unstable params) and *-classes.txt (stable/unstable + reason). They tell you EXACTLY which type is unstable so you fix the right thing. Pair with Layout Inspector's live recomposition counts."}]},{level:"junior",q:"How do you make a List parameter stable so a composable can skip?",a:[{t:"p",text:"`List` is an interface, so the compiler treats it as unstable (the runtime could be a mutable list). To make a composable taking a list skippable, either use `kotlinx.collections.immutable`'s `ImmutableList`/`PersistentList` (recognized as stable), wrap it in a `@Immutable` holder, or configure a stability-config file to mark `List` stable if you guarantee immutability."},{t:"code",title:"Stable list options",code:`// Option 1: kotlinx.collections.immutable (recommended)
@Composable fun Items(list: ImmutableList<Item>) { /* now skippable */ }
val list = persistentListOf(a, b, c)

// Option 2: @Immutable wrapper
@Immutable data class ItemList(val items: List<Item>)

// Option 3: stability config file listing kotlin.collections.List as stable
// (stabilityConfigurationFile) — only if you guarantee you never mutate them`},{t:"list",items:["**`ImmutableList`/`persistentListOf`** — the idiomatic fix; the compiler knows these can't mutate, so it's stable and the composable skips.","**`@Immutable` wrapper** — annotate a holder class as a promise of immutability.","**Stability config file** — globally mark `List`/`Map` stable; convenient but a *promise* — if you ever pass a mutable list, you get stale UI.","**Strong skipping mode** changes this calculus (see the strong-skipping question) — it can skip even with unstable params, but stability still helps."]},{t:"note",text:"List is an interface → unstable. Fix with ImmutableList/persistentListOf (kotlinx.collections.immutable — compiler treats as stable), an @Immutable wrapper class, or a stability config marking List stable (a promise — mutating it later = stale UI). Strong skipping softens this but stability still helps."}]},{level:"senior",q:"Why can passing a lambda cause recomposition, and how do you keep lambdas stable?",a:[{t:"p",text:"A lambda that captures unstable values, or is re-created each recomposition, can be an unstable parameter that defeats skipping. The Compose compiler *memoizes* many lambdas automatically (so an unchanging lambda is stable across recompositions), but lambdas capturing changing state, or created in ways the compiler can't memoize, can still churn."},{t:"list",items:["**Compiler auto-memoization** — a lambda like `{ viewModel.onClick() }` with stable captures is remembered, so it's the same instance across recompositions → stable, doesn't break skipping.","**Method references** — `onClick = viewModel::onClick` are stable and cheap; prefer them.","**Capturing changing values** — `{ onSelect(index) }` where `index` changes creates a new lambda each time; that's often fine, but in hot paths hoist or key it.","**Explicit `remember`** — for an expensive-to-create callback, `val cb = remember { { ... } }`, but usually unnecessary given auto-memoization.","**Strong skipping mode** memoizes lambdas more aggressively, reducing this concern."]},{t:"note",text:"Lambdas can be unstable params if re-created each recomposition or capturing changing state — defeating skipping. The compiler auto-memoizes lambdas with stable captures (so most are fine); prefer method references (viewModel::onClick). Strong skipping memoizes lambdas more aggressively. Only hand-remember callbacks in proven hot paths."}]},{level:"senior",q:"Explain the three phases (composition, layout, draw) and how skipping works in each.",a:[{t:"p",text:"Each frame, Compose runs up to three phases: **composition** (run composables to build/update the tree of what to show), **layout** (measure and place each node), and **draw** (paint each node). Crucially, a state change only invalidates the phase(s) that read it — so changing a color skips composition/layout and only redraws, and changing an offset via `graphicsLayer` skips composition/layout too."},{t:"list",items:["**Composition** — 'what to show'; re-runs only invalidated composables (skipping the rest).","**Layout** — 'where/how big'; re-measures only nodes whose size/position inputs changed.","**Draw** — 'how it looks'; re-draws only nodes whose draw inputs changed.","**Phase-targeted invalidation** — reading state in a *later* phase avoids the earlier ones. `Modifier.offset { }`/`graphicsLayer { }` lambdas and `drawBehind { }` defer reads to layout/draw, so animating them skips recomposition entirely."]},{t:"code",title:"Deferring a read to the draw phase",code:`// Reads scrollState in COMPOSITION -> recomposes every scroll frame (bad)
Box(Modifier.offset(y = scrollState.value.dp))
// Reads in LAYOUT via lambda -> skips recomposition (good)
Box(Modifier.offset { IntOffset(0, scrollState.value) })`},{t:"note",text:"Frame phases: composition (what) → layout (where/size) → draw (paint). A state change invalidates only the phase(s) that read it. Defer reads to later phases (offset{}/graphicsLayer{} lambdas, drawBehind{}) so animating position/color skips recomposition and layout — the core Compose perf technique."}]},{level:"junior",q:"What is the difference between recomposition, relayout, and redraw for performance?",a:[{t:"p",text:"These are the three kinds of work Compose can redo, in increasing cheapness of avoidance: *recomposition* (re-run composables) is what people usually optimize, but *relayout* (re-measure/place) and *redraw* (re-paint) also cost — and an inefficient layout or overdraw can jank even with zero excess recomposition. Diagnosing jank means knowing which one is happening."},{t:"list",items:["**Recomposition** — re-executing composables; caused by state reads in composition. Reduce with stability/skipping and deferring reads.","**Relayout** — re-measuring; caused by size/position-affecting changes (animating `size`, `padding`, `offset(dp)`). Reduce by using `graphicsLayer`/`offset{}` for movement.","**Redraw** — re-painting; caused by color/alpha/drawing changes. Usually cheap, but *overdraw* (many layers painted over each other) is a separate cost.","**Why it matters** — 'too many recompositions' isn't the only cause of jank; a single composition that triggers expensive layout every frame janks too. Profile to see which phase is hot."]},{t:"note",text:"Three redo-costs: recomposition (re-run composables), relayout (re-measure/place), redraw (re-paint). Jank isn't only 'too many recompositions' — animating size/padding causes relayout, and overdraw costs draw. Use graphicsLayer/offset{} to move things without relayout, and profile to see which phase is hot."}]},{level:"junior",q:"How do you use the Layout Inspector to find recomposition counts?",a:[{t:"p",text:"Android Studio's **Layout Inspector** shows, for a running app, each composable with its **recomposition count** and **skip count** live. You interact with the app and watch which composables' counts climb unexpectedly — those are recomposing more than they should, pointing you to the state read or unstable param to fix."},{t:"list",items:["**Enable recomposition counts** — in Layout Inspector, turn on the recomposition-count column; interact and watch the numbers.","**High recomposition + low skip** — a composable that keeps recomposing and rarely skips is a suspect (likely an unstable param or a too-high state read).","**High skip count is good** — it means stability is working.","**Complements the static reports** — the compiler stability report tells you *what's unstable*; Layout Inspector tells you *what's actually recomposing at runtime*. Use both.","**Also useful** — Modifier.Node counts and the composition tree to see donut-hole skipping in action."]},{t:"note",text:"Layout Inspector shows live recomposition and skip counts per composable — interact and watch which counts climb (high recompose + low skip = suspect). It's the runtime complement to the static stability report (what's unstable). Reset counts, reproduce, and target the composable that's churning."}]},{level:"senior",q:"What is 'deferring reads' and why does reading state as late as possible help?",a:[{t:"p",text:"Deferring a state read means arranging for the value to be read in a *later phase* (layout or draw) or a *smaller scope* (a child, or a lambda) rather than high up in composition. The read location determines *what* invalidates when the state changes — reading it lower/later means a change re-runs less work."},{t:"list",items:["**Read location = invalidation scope** — the composable/phase that reads the state is what re-runs on change. Read it in the smallest scope that needs it.","**Lambda-based modifiers defer to layout/draw** — `offset { scroll.value }`, `graphicsLayer { alpha = anim }`, `drawBehind { }`: the value is read in layout/draw, skipping recomposition.","**Pass state down as a lambda, not a value** — passing `() -> Int` (a getter) instead of `Int` lets the child read it, so only the child recomposes, not the parent chain (common for scroll-driven effects).","**Concrete win** — a collapsing toolbar reading scroll offset: read it in a `graphicsLayer`/`offset{}` lambda so scrolling only re-lays-out/draws the toolbar, not the whole screen."]},{t:"code",title:"Defer via a lambda parameter",code:`// Parent reads value -> parent + child recompose each change
Header(offset = scroll.value)
// Parent passes a getter -> only Header reads it, only Header invalidates
Header(offset = { scroll.value })`},{t:"note",text:"Deferring reads = read state in the latest phase / smallest scope, because the reader is what invalidates. Use lambda modifiers (offset{}, graphicsLayer{}, drawBehind{}) to read in layout/draw (skip recomposition), and pass state as a getter lambda so only the child invalidates. Key technique for scroll/animation-driven UI."}]},{level:"junior",q:"Why is a debug build misleading for Compose performance, and what should you measure on?",a:[{t:"p",text:"Debug builds run Compose code *interpreted/unoptimized*: no R8 optimization, live-literals and debugging instrumentation enabled, and — critically — no ahead-of-time compilation of the Compose runtime, so it JIT-compiles as you scroll. This makes debug builds dramatically slower and janky in ways a release build isn't. Always measure on a release (or profileable) build."},{t:"list",items:["**Debug = interpreted + unoptimized** — R8 off, extra instrumentation, no baseline profile applied; scrolling triggers JIT.","**Measure on release/`profileable`** — a `profileable` build variant gives release performance with profiling enabled.","**On a real, mid-tier device** — not a flagship or emulator; users' devices are slower.","**Apply a Baseline Profile** — without it, even release has a 'first run' JIT penalty; the profile precompiles hot paths.","**Use Macrobenchmark** — for repeatable startup/scroll/jank metrics (frame timing), not eyeballing."]},{t:"note",text:"Debug builds run Compose interpreted/unoptimized (R8 off, extra instrumentation, JIT while scrolling) — far slower and janky unlike release. Measure on a release/profileable build, on a real mid-tier device, with a Baseline Profile applied, using Macrobenchmark for repeatable frame-timing numbers."}]},{level:"senior",q:"How do Baseline Profiles work, and how do you create one for a Compose app?",a:[{t:"p",text:"A Baseline Profile is a list of hot code paths (classes/methods) shipped with your app so ART can *ahead-of-time compile* them at install, instead of interpreting-then-JIT-ing on first use. Compose benefits enormously because its runtime and your UI code are otherwise JIT'd during the first scrolls/launches, causing first-run jank. You generate one with a Macrobenchmark test that exercises key journeys."},{t:"code",title:"Generating a Baseline Profile",code:`// A BaselineProfileGenerator test (macrobenchmark module)
@Test fun generate() = baselineProfileRule.collect(packageName = "com.app") {
    startActivityAndWait()
    // exercise critical journeys: scroll the feed, open detail, etc.
    device.findObject(By.res("feed")).fling(Direction.DOWN)
}
// Output: baseline-prof.txt bundled into the app; ART precompiles those paths at install`},{t:"list",items:["**AOT-compiles hot paths at install** — removes the interpret/JIT penalty on first launch and first scrolls (typical 20–40% faster startup, smoother initial scroll).","**Generated via Macrobenchmark** — `BaselineProfileRule` records the methods hit while you script the critical user journeys.","**Ship it** — the profile lives in `src/main/baseline-prof.txt` (the Baseline Profile Gradle plugin automates generation/inclusion).","**Compose especially** — its library code isn't in the platform's system profile, so without a baseline profile it's JIT'd; this is the single biggest easy Compose perf win."]},{t:"note",text:"A Baseline Profile lists hot classes/methods so ART AOT-compiles them at install, killing first-run JIT jank — huge for Compose (its runtime is otherwise interpreted then JIT'd). Generate with a Macrobenchmark BaselineProfileRule test scripting key journeys; ship baseline-prof.txt. Biggest easy Compose perf win."}]},{level:"junior",q:"What causes excessive recomposition most often, and how do you fix each cause?",a:[{t:"p",text:"Excessive recomposition almost always traces to a small set of causes: unstable parameters, reading rapidly-changing state too high in the tree, or creating new objects each composition. Knowing the usual suspects lets you fix jank quickly."},{t:"list",items:["**Unstable parameters** — `List`/`Map`, other-module classes, `var` fields. Fix: immutable collections, `@Immutable`/`@Stable`, stable data classes.","**Reading fast-changing state too high** — scroll offset/animation value read in a parent. Fix: `derivedStateOf`, lambda getters, `graphicsLayer`.","**Creating new objects in composition** — a new list/lambda/object each recomposition passed as a param. Fix: `remember` it, or hoist.","**Unstable state exposed from ViewModel** — a mutable state object. Fix: immutable UiState, updated via `copy`.","**Whole-screen state for a small change** — one big UiState so any field change recomposes everything reading it. Fix: split reads, pass narrow slices.","**Not using keys in lists** — causes wrong reuse/recomposition. Fix: stable `key`."]},{t:"note",text:"Excessive recomposition causes + fixes: unstable params (→ immutable/@Stable), reading fast-changing state too high (→ derivedStateOf/lambda/graphicsLayer), new objects each composition (→ remember), mutable VM state (→ immutable UiState), one giant UiState (→ narrow reads), missing list keys. Confirm with Layout Inspector + stability report."}]},{level:"junior",q:"What do @Stable and @Immutable promise, and what happens if you lie?",a:[{t:"p",text:"`@Immutable` promises the type's public properties will *never change* after construction (fully immutable). `@Stable` is a weaker promise: the type may change, but when it does it will *notify Compose* (via snapshot state), and `equals` is consistent. Both tell the compiler to treat the type as stable (enabling skipping) even when it couldn't infer that. If you lie, you get stale UI — Compose skips recomposition that should have happened."},{t:"list",items:["**`@Immutable`** — 'this never changes'. Use on data classes with `val`s of types the compiler can't verify (e.g. wrapping a `List`).","**`@Stable`** — 'this may change but will notify via snapshot state, and equals is stable'. Use on holder classes backed by `mutableStateOf`.","**The danger of lying** — if you annotate a type stable but then mutate it invisibly, composables reading it are skipped and show stale data — a nasty, hard-to-trace bug.","**Prefer real immutability** — annotations are promises the compiler can't check; genuine immutable data (val + immutable collections) is safer than an annotation over mutable internals."]},{t:"note",text:"@Immutable = 'never changes'; @Stable = 'may change but notifies Compose (snapshot state) and equals is consistent'. Both force stable treatment (skipping). Lying = stale UI (Compose skips recomposition it shouldn't). Prefer genuine immutability over annotating mutable internals — the compiler can't verify the promise."}]},{level:"senior",q:"What problems does strong skipping mode solve, and what changed for developers?",a:[{t:"p",text:"Strong skipping mode (default from Kotlin 2.0.20 / recent Compose) makes composables with *unstable* parameters *skippable* by comparing those params with *instance equality*, and it auto-remembers lambdas. Before it, one unstable parameter (a `List`, a class from another module) made the whole composable non-skippable, forcing lots of `@Immutable`/`ImmutableList` boilerplate. Strong skipping greatly reduces that burden."},{t:"list",items:["**Unstable params no longer block skipping** — Compose compares unstable params by instance (`===`); if the same instance is passed, it can still skip.","**Lambdas auto-remembered** — reduces lambda-instability churn without manual `remember`.","**Less boilerplate** — you need fewer `@Immutable` annotations and `ImmutableList` conversions just to get skipping.","**Stability still matters** — for *value* equality (a new equal `List` instance won't skip under instance comparison), immutable/stable types still help; and correctness (avoiding stale UI from mutation) still requires immutability.","**Caveat** — passing a *new* unstable instance each time (e.g. a freshly-built list) still recomposes; strong skipping helps when the same instance flows through."]},{t:"note",text:"Strong skipping (default in recent Compose) makes composables with unstable params skippable via instance (===) comparison and auto-remembers lambdas — cutting @Immutable/ImmutableList boilerplate. But a NEW unstable instance each recomposition still recomposes, so stable/immutable types still help value-equality skipping and prevent stale-UI bugs."}]},{level:"senior",q:"How do you handle an unstable type you can't change (from a library or another module)?",a:[{t:"p",text:"When a type is unstable because it's defined in a module without the Compose compiler (or is a library class you can't annotate), you have several options short of rewriting it: wrap it, annotate via a stability configuration file, or restructure so the unstable type doesn't cross a composable boundary."},{t:"list",items:["**Stability configuration file** — list the class's fully-qualified name in a `stabilityConfigurationFile`; the compiler then treats it as stable (a promise you won't mutate it). Good for third-party value types you know are immutable.","**Wrap in an `@Immutable`/`@Stable` holder** — a small class in your module that the compiler analyzes, wrapping the foreign type.","**Map to your own model** — convert the library type to a domain data class at the boundary; often the cleanest (also decouples you from the library).","**Rely on strong skipping** — with it enabled, passing the same instance still skips via `===`, reducing the need to fix stability at all.","**Enable the Compose compiler on your own multi-module code** — for your *own* modules, applying the Compose compiler plugin makes their data classes stable automatically."]},{t:"note",text:"For an unstable type you can't edit: add it to a stabilityConfigurationFile (promise it's immutable), wrap it in an @Immutable holder, or map it to your own domain data class at the boundary (cleanest). Strong skipping also helps (instance-equality skip). For your own modules, apply the Compose compiler so their classes are analyzed."}]},{level:"junior",q:"How does Compose avoid unnecessary work with positional memoization?",a:[{t:"p",text:"Positional memoization is how Compose remembers things *by their location in the call tree* rather than by a key you supply. Each call site gets a slot in the slot table, so `remember` at a given position retrieves the same stored value across recompositions, and Compose can match up composables between recompositions to know what changed, was added, or removed."},{t:"list",items:["**Slot table by position** — every composable call and `remember` is identified by its position in the composition, so state 'sticks' to its call site.","**Enables `remember`** — the value persists because the slot persists across recompositions.","**Enables diffing** — matching positions lets Compose reuse unchanged subtrees and identify moves/insertions.","**`key()` overrides position** — when items reorder (lists), `key()` gives identity independent of position so state migrates with the item, not the slot."]},{t:"note",text:"Positional memoization identifies each composable/remember by its position in the call tree (a slot in the slot table), so state sticks to its call site and Compose can diff between recompositions. key() overrides position for reordering lists so state follows the item, not the slot."}]},{level:"senior",q:"How do you profile and fix Compose jank on a real device end-to-end?",a:[{t:"p",text:"A systematic pass: reproduce on a release/profileable build on a real mid-tier device, measure with the right tool, identify whether it's composition/layout/draw, fix the specific cause, and verify the metric improved. Guessing wastes time; the tools tell you exactly where the frames go."},{t:"list",items:["**Reproduce correctly** — release/`profileable` build, real device, Baseline Profile applied.","**Measure** — Macrobenchmark (`FrameTimingMetric`) for jank/startup numbers; the system trace / Perfetto for per-frame breakdown; Layout Inspector for recomposition counts.","**Classify** — is the hot phase composition (excess recomposition), layout (expensive/relayout every frame), or draw (overdraw, large bitmaps)?","**Fix the cause** — stability/keys/`derivedStateOf` for composition; `graphicsLayer`/`offset{}`/simpler layout for layout; right-sized images/fewer layers for draw.","**Verify** — re-run the benchmark; confirm p50/p95 frame time and jank % dropped. Keep the benchmark to catch regressions."]},{t:"note",text:"End-to-end: reproduce on release/profileable + real device + baseline profile → measure (Macrobenchmark FrameTiming, Perfetto trace, Layout Inspector counts) → classify the hot phase (composition/layout/draw) → fix the specific cause → re-benchmark to verify p95/jank% dropped. Measure, don't guess; keep the benchmark for regressions."}]},{level:"junior",q:"Why should you avoid heavy computation directly in a composable, and where should it go?",a:[{t:"p",text:"A composable body can run every recomposition (many times per second), so any non-trivial computation there — sorting, filtering, formatting, parsing — repeats needlessly and janks the frame. Move it out: derive it once with `remember`, compute it in the ViewModel, or use `derivedStateOf` for state-dependent derivations."},{t:"code",title:"Move computation out of the body",code:`// BAD: sorts on every recomposition
@Composable fun List(items: List<Item>) {
    val sorted = items.sortedBy { it.name }   // runs each recomposition
}
// GOOD: cache with remember keyed on the input
val sorted = remember(items) { items.sortedBy { it.name } }
// BETTER: compute in the ViewModel (off the UI, testable, survives config change)`},{t:"list",items:["**`remember(key)`** — caches the result, recomputing only when the input changes.","**ViewModel** — best home for real business logic and expensive transforms; the UI just displays results (and it survives config change, off the main thread).","**`derivedStateOf`** — for values derived from *other snapshot state* that changes at a different rate than reads.","**Never in the raw body** — repeated per recomposition, on the main thread, blocking frames."]},{t:"note",text:"Composable bodies run every recomposition, so heavy work (sort/filter/format) there janks frames. Cache with remember(key) { }, or better compute in the ViewModel (off main thread, testable, survives config change); use derivedStateOf for state-derived values. Never leave expensive computation in the raw body."}]},{level:"senior",q:"What is overdraw, and how do you reduce it in Compose?",a:[{t:"p",text:"Overdraw is painting the same pixel multiple times in one frame (stacked backgrounds, opaque layers over opaque layers). Each extra layer costs GPU/draw time; heavy overdraw contributes to jank independent of recomposition. You reduce it by removing redundant backgrounds and unnecessary layers."},{t:"list",items:["**Redundant backgrounds** — a colored `Surface` inside another same-colored one paints twice; drop the inner background.","**Opaque over opaque** — full-screen backgrounds behind opaque content are wasted; clip or size them to only the visible area.","**`clipToBounds`/`clip`** — clipping can prevent drawing outside needed bounds.","**Diagnose** — 'Debug GPU Overdraw' in developer options colors pixels by overdraw count (blue=1×, red=4×+); aim to reduce red areas.","**`graphicsLayer` `compositingStrategy`** — for alpha over overlapping content, but note offscreen buffers have their own cost — measure."]},{t:"note",text:"Overdraw = painting the same pixel multiple times per frame (stacked backgrounds/opaque layers) — a draw-phase cost separate from recomposition. Reduce by removing redundant/nested backgrounds and clipping to visible bounds. Diagnose with 'Debug GPU Overdraw' (blue=1×…red=4×+); shrink the red."}]},{level:"junior",q:"How do you optimize image loading in Compose to avoid jank and memory issues?",a:[{t:"p",text:"Use an image library like Coil (`AsyncImage`), which decodes off the main thread, caches in memory and on disk, and — crucially — downsamples images to the size actually displayed. Loading a full-resolution bitmap into a small view wastes memory and causes GC jank; letting Coil size the request fixes it."},{t:"code",title:"Efficient AsyncImage",code:`AsyncImage(
    model = ImageRequest.Builder(LocalContext.current)
        .data(url)
        .crossfade(true)
        .build(),
    contentDescription = null,
    modifier = Modifier.size(64.dp),   // Coil downsamples the decode to this size
    placeholder = painterResource(R.drawable.placeholder),
)`},{t:"list",items:["**Right-size the decode** — a bounded `Modifier.size`/known target lets Coil decode to that size, not full-res; avoids huge bitmaps and OOM/GC jank.","**Memory + disk cache** — Coil handles both; repeated items don't re-decode.","**Placeholders/crossfade** — smoother perceived loading in lists.","**Avoid unbounded size in lists** — an image with no size constraint may decode at full resolution; give it a size or aspect ratio."]},{t:"note",text:"Use Coil AsyncImage: it decodes off-main-thread, caches (memory+disk), and downsamples to the displayed size — a bounded Modifier.size is what lets it avoid loading full-res bitmaps (the usual cause of list GC jank/OOM). Add placeholders/crossfade; never leave list images unbounded."}]},{level:"senior",q:"How does donut-hole skipping work, and how do you exploit it?",a:[{t:"p",text:"Donut-hole skipping is Compose's ability to recompose a *child* (the hole) while skipping its *parent* (the donut), or vice-versa — invalidation follows the actual state reads, not the parent-child hierarchy. You exploit it by reading changing state in the *deepest* composable that needs it, so a change re-runs only that small 'hole' and its ancestors are skipped."},{t:"code",title:"Push the state read into the hole",code:`// BAD: parent reads count -> parent + all siblings recompose
@Composable fun Screen(count: Int) {
    ExpensiveHeader()          // recomposes needlessly
    Text("Count: $count")
}
// GOOD: only the Text (the hole) reads it -> ExpensiveHeader is skipped
@Composable fun Screen(count: () -> Int) {
    ExpensiveHeader()
    Text("Count: \${count()}")   // read deferred into this leaf
}`},{t:"list",items:["**Invalidation is per read-site** — only composables that read the changed state re-run; siblings that don't are skipped.","**Read low** — pass state as a lambda getter so the leaf reads it, keeping expensive siblings/parents out of the invalidation.","**Content lambdas help** — passing a `@Composable` content slot lets the container recompose without re-running the content, and vice-versa.","**Combined with stability** — skippable siblings + deep reads means a frequent state change touches minimal work."]},{t:"note",text:"Donut-hole skipping: Compose recomposes only the composables that READ the changed state, skipping parents/siblings that don't — invalidation follows reads, not hierarchy. Exploit it by reading changing state in the deepest leaf (pass a getter lambda), so expensive siblings stay skipped. Pairs with stability for minimal per-change work."}]}],rS={id:"performance-stability",title:"Performance & Stability",tagline:"Skippability, stable vs unstable types, @Stable/@Immutable, strong skipping, diagnosing recomposition, deferring reads, and baseline profiles.",tags:["Compose","Stability","Recomposition","Baseline Profiles","Jank"],content:oS,qa:sS},lS=[{heading:"How theming works in Compose — CompositionLocal, not global styles",blocks:[{t:"p",text:"In the View system, themes were global XML attributes resolved through a `Context`. Compose does it differently: a theme is a set of values (colors, typography, shapes) **provided implicitly down the composition tree** via **CompositionLocal**, and composables read them where needed. `MaterialTheme` is just a composable that provides three CompositionLocals — `LocalColorScheme`, `LocalTypography`, `LocalShapes` — to everything inside it."},{t:"code",title:"A theme is a provider + accessor object",code:`@Composable
fun AppTheme(darkTheme: Boolean = isSystemInDarkTheme(), content: @Composable () -> Unit) {
    val colors = if (darkTheme) DarkColorScheme else LightColorScheme
    MaterialTheme(
        colorScheme = colors,
        typography = AppTypography,
        shapes = AppShapes,
        content = content,
    )
}

// Read anywhere below via the MaterialTheme accessor object:
Text(
    text = "Title",
    color = MaterialTheme.colorScheme.primary,     // reads LocalColorScheme
    style = MaterialTheme.typography.headlineMedium // reads LocalTypography
)`},{t:"list",items:["**Why CompositionLocal**: it lets a value be available to a whole subtree *without threading it through every function's parameters*. The theme is 'ambient' — any composable can read `MaterialTheme.colorScheme` without being passed it.","Because reading a CompositionLocal is a **state read**, switching the theme (light→dark) *recomposes exactly the composables that read the changed values* — theming is reactive for free.","Nesting themes is natural: wrap a subtree in another `MaterialTheme(...)` to override values just there (a red-accented section), because the inner provider shadows the outer for that subtree."]}]},{heading:"Material 3, color schemes, and dynamic color",blocks:[{t:"list",items:["**Material 3 (Material You)** is the current design system: `ColorScheme` has semantic roles — `primary`, `onPrimary`, `secondary`, `surface`, `onSurface`, `error`, `surfaceVariant`, etc. You style by *role* (`onSurface` = 'content on a surface'), never by raw hex, so light/dark and dynamic themes swap automatically.","**Light & dark** are two `ColorScheme` instances; you pick based on `isSystemInDarkTheme()`. Because components read roles, the same composables render correctly in both — no separate dark layouts.","**Dynamic color** (Android 12+): `dynamicDarkColorScheme(context)` / `dynamicLightColorScheme(context)` derive the scheme from the user's wallpaper — the 'Material You' personalization. Gate it on `Build.VERSION >= S` with a static fallback for older devices.","**Content color propagation**: `Surface` sets a `LocalContentColor` matching its background, so `Text`/`Icon` inside automatically use the correct 'on' color — a big reason to wrap regions in `Surface` rather than raw `Box` with a background."]},{t:"code",title:"Dynamic color with fallback",code:`val colorScheme = when {
    Build.VERSION.SDK_INT >= Build.VERSION_CODES.S ->
        if (darkTheme) dynamicDarkColorScheme(context) else dynamicLightColorScheme(context)
    darkTheme -> DarkColorScheme
    else -> LightColorScheme
}`}]},{heading:"CompositionLocal — the mechanism, and when to create your own",blocks:[{t:"p",text:"A **CompositionLocal** is a way to pass data implicitly through the composition tree. You *provide* a value at some level and *read* it anywhere below, without parameters in between. Compose ships many: `LocalContext`, `LocalDensity`, `LocalConfiguration`, `LocalLifecycleOwner`, `LocalContentColor`."},{t:"code",title:"Defining and providing a custom CompositionLocal",code:`// Two flavors:
val LocalSpacing = staticCompositionLocalOf { Spacing() }     // rarely changes
val LocalUser = compositionLocalOf<User?> { null }            // changes over time

// Provide:
CompositionLocalProvider(LocalSpacing provides Spacing(large = 24.dp)) {
    Screen()   // everything inside can read LocalSpacing.current
}

// Read:
val spacing = LocalSpacing.current`},{t:"list",items:["**`staticCompositionLocalOf` vs `compositionLocalOf`**: the *static* one doesn't track reads — changing it recomposes the **entire** provided subtree (cheap to read, expensive to change; use for values that essentially never change, like a design-system spacing object). The regular one tracks reads and recomposes only actual readers (use for values that change at runtime, like the current user).","**When to create one**: genuinely cross-cutting, ambient values many composables need — theme extensions (custom spacing/elevation tokens), the current logged-in user for deep trees, analytics loggers. ","**When NOT to** (the interview caveat): CompositionLocal is implicit — it hides dependencies, hurts testability and preview-ability, and makes data flow hard to follow. For normal data, **prefer explicit parameters**. Rule of thumb: use it only when passing the value as a parameter through many layers would be clearly worse, and the value is truly 'ambient'."]}]},{heading:"Animations — the high-level APIs (cover 90% of needs)",blocks:[{t:"p",text:"Compose animations are **state-driven**: you animate by changing state and letting an animation API interpolate toward the new value. The high-level APIs are named for what they animate:"},{t:"code",title:"The everyday animation toolkit",code:`// 1) animate*AsState — animate a single value toward a target
val color by animateColorAsState(if (selected) Blue else Gray, label = "color")
val size by animateDpAsState(if (expanded) 200.dp else 100.dp, label = "size")

// 2) AnimatedVisibility — animate appearance/disappearance
AnimatedVisibility(
    visible = showDetails,
    enter = fadeIn() + expandVertically(),
    exit = fadeOut() + shrinkVertically(),
) { DetailsPanel() }

// 3) AnimatedContent — animate BETWEEN different contents/states
AnimatedContent(targetState = count, label = "count") { value ->
    Text("$value")   // old value animates out, new animates in
}

// 4) Modifier.animateContentSize — animate size changes automatically
Column(Modifier.animateContentSize()) { /* grows/shrinks smoothly */ }

// 5) animateItem() — lazy-list item placement animations (needs keys)
items(list, key = { it.id }) { Row(Modifier.animateItem()) { /* ... */ } }`},{t:"list",items:["**`animate*AsState`** is the workhorse: give it a *target* value; it returns a `State` that smoothly moves there whenever the target changes. Variants for every type (`Float`, `Dp`, `Color`, `Offset`, `IntSize`…), plus `animateValueAsState` with a custom `TwoWayConverter` for your own types.","**`AnimatedVisibility`** animates a composable entering/leaving; enter/exit transitions compose with `+` (`fadeIn() + slideInHorizontally()`).","**`AnimatedContent`** cross-fades/transitions between *different content* for different states (a counter, a loading→loaded swap) with configurable `transitionSpec`.","**`Modifier.animateContentSize()`** smoothly animates a composable's own size changes — great for expandable cards."]}]},{heading:"AnimationSpec — controlling the motion",blocks:[{t:"p",text:"Every animation takes an **`AnimationSpec`** describing *how* the value moves over time. The two families:"},{t:"list",items:["**Duration-based — `tween`**: fixed duration + easing curve. `tween(300, easing = FastOutSlowInEasing)`. Predictable; use for deterministic transitions (a panel sliding in).","**Physics-based — `spring`**: no fixed duration; motion follows spring dynamics via `dampingRatio` (bounciness) and `stiffness` (speed). `spring(dampingRatio = Spring.DampingRatioMediumBouncy)`. Feels natural and, crucially, is **interruptible**: if the target changes mid-animation, a spring smoothly redirects from its current velocity rather than restarting — the right default for gesture-driven and frequently-changing values.","**`keyframes`**: specify values at specific timestamps for complex multi-stage motion.","**`repeatable`/`infiniteRepeatable`**: loop an animation (pulsing, loading shimmer) — used with `rememberInfiniteTransition`."]},{t:"code",title:"Specs and an infinite animation",code:`val offset by animateDpAsState(
    targetValue = target,
    animationSpec = spring(stiffness = Spring.StiffnessLow),
    label = "offset",
)

// Infinite pulse:
val infinite = rememberInfiniteTransition(label = "pulse")
val scale by infinite.animateFloat(
    initialValue = 1f, targetValue = 1.2f,
    animationSpec = infiniteRepeatable(tween(600), RepeatMode.Reverse),
    label = "scale",
)`}]},{heading:"Lower-level animation: Animatable, Transition, and performance",blocks:[{t:"list",items:["**`Animatable`** — the imperative, coroutine-based primitive underneath `animate*AsState`. You `animateTo()`/`snapTo()` from a coroutine and can read velocity, cancel, and chain — needed for **gesture-driven** animation (swipe-to-dismiss, draggable with fling) where you drive the value manually. `val offset = remember { Animatable(0f) }` then `offset.animateTo(target)`.","**`Transition` (`updateTransition`)** — coordinates **multiple animations off one state change**, all staying in sync (e.g. a selection toggling color + size + elevation together). Also gives named child animations for tooling.","**`rememberInfiniteTransition`** — for endless animations (shimmer, pulse) decoupled from state.","**Performance rule** (ties to the Performance topic): high-frequency animation values should be read in **layout/draw**, not composition — `Modifier.offset { }`, `Modifier.graphicsLayer { }` — so each frame re-lays-out or re-draws instead of recomposing. Reading `animatedValue.value` directly in a modifier's non-lambda argument recomposes every frame; the lambda form defers it. This single point is the intersection of the animation and performance topics interviewers love to probe.","Always pass a **`label`** to animations — it names them in the Animation Preview/inspector tooling (and is required-ish in newer APIs)."]}]}],cS=[{level:"junior",q:"How does theming work in Jetpack Compose?",a:[{t:"p",text:"**The concept**: instead of global XML style attributes resolved through a `Context`, Compose provides theme values *implicitly down the composition tree* using **CompositionLocal**. `MaterialTheme` is a composable that wraps your app and provides three things to everything inside it — a `ColorScheme`, a `Typography`, and `Shapes`. Any composable below can read them through the `MaterialTheme` accessor: `MaterialTheme.colorScheme.primary`, `MaterialTheme.typography.bodyLarge`."},{t:"p",text:"**Why this is nice**: the theme is 'ambient' — a `Text` deep in the tree reads the current color without anyone passing it down. And because reading a theme value is a *state read*, toggling light↔dark automatically recomposes exactly the composables that used the changed values — reactive theming with no manual work. You also style by *semantic role* (`onSurface` = content on a surface) rather than raw colors, so the same components render correctly across light, dark, and dynamic themes without separate layouts."}]},{level:"junior",q:"What is CompositionLocal and when should you use it?",a:[{t:"p",text:"**The concept**: a CompositionLocal is a mechanism to pass data implicitly through the composition — you *provide* a value at one level and *read* it (`.current`) anywhere below, without threading it through every function's parameters in between. Compose uses it for ambient things every composable might need: `LocalContext`, `LocalDensity`, `LocalConfiguration`, `LocalContentColor`, and the theme locals."},{t:"code",title:"Provide and read",code:`val LocalSpacing = staticCompositionLocalOf { Spacing() }

CompositionLocalProvider(LocalSpacing provides Spacing(large = 24.dp)) {
    MyScreen()   // anything inside:
}
// val spacing = LocalSpacing.current`},{t:"p",text:"**When to use it**: genuinely cross-cutting, ambient values that a wide subtree needs — theme extensions (custom spacing/elevation tokens beyond Material's), the current user for a deep screen, an analytics logger. **When NOT to** (the important half): it makes data flow *implicit*, which hurts readability, testability, and previews — you can't tell a composable's dependencies from its signature. So for ordinary data, **prefer explicit parameters**. The rule: reach for CompositionLocal only when passing the value as a parameter through many layers would clearly be worse, *and* the value is truly ambient."}]},{level:"junior",q:"How do you support light and dark themes, and what is dynamic color?",a:[{t:"p",text:"**Light/dark**: define two `ColorScheme` instances (a light one and a dark one), then pick between them with `isSystemInDarkTheme()` and pass the choice to `MaterialTheme`. That's it — because all your components read *semantic color roles* (`primary`, `surface`, `onSurface`) rather than hardcoded colors, the exact same composables render correctly in both modes. There's no separate dark layout; the role values differ, the UI code doesn't."},{t:"p",text:"**Dynamic color (Material You, Android 12+)**: instead of your hand-picked palette, the color scheme is *generated from the user's wallpaper* via `dynamicLightColorScheme(context)` / `dynamicDarkColorScheme(context)`. It personalizes the app to the device. You gate it on `Build.VERSION.SDK_INT >= S` and fall back to your static schemes on older devices. A related detail worth mentioning: wrapping regions in `Surface` propagates the correct content color automatically (`LocalContentColor`), so text/icons inside pick the right 'on' color for the surface — which is why `Surface` is preferred over a raw `Box` with a background."}]},{level:"junior",q:"How do you animate a simple value change, like a color or size, in Compose?",a:[{t:"p",text:"**The concept**: Compose animation is state-driven — you don't imperatively run an animation, you change a *target* value and an animation API smoothly interpolates toward it. The simplest tool is the `animate*AsState` family: you give it a target, it returns a `State` that moves there whenever the target changes."},{t:"code",title:"animate*AsState — declarative single-value animation",code:`val bgColor by animateColorAsState(
    targetValue = if (selected) Color.Blue else Color.Gray,
    animationSpec = tween(300),
    label = "bgColor",
)
val padding by animateDpAsState(if (expanded) 24.dp else 8.dp, label = "padding")

Box(Modifier.background(bgColor).padding(padding)) { /* ... */ }`},{t:"p",text:"There's a typed variant for each common type — `animateColorAsState`, `animateDpAsState`, `animateFloatAsState`, `animateOffsetAsState`, and so on. You just read the returned value like any state; when the target flips (e.g. `selected` changes), the value animates rather than jumping. For appearing/disappearing content there's `AnimatedVisibility`, and for animating *between different contents* there's `AnimatedContent` — but `animate*AsState` covers the everyday 'this value should change smoothly' case."}]},{level:"junior",q:"What is the difference between tween and spring animation specs?",a:[{t:"p",text:"**The concept**: an `AnimationSpec` describes *how* a value travels to its target over time, and there are two philosophies. **`tween`** is duration-based: you specify a fixed duration and an easing curve (`tween(300, easing = FastOutSlowInEasing)`), and the value follows that curve for exactly that long. **`spring`** is physics-based: there's no fixed duration — the value moves as if pulled by a spring, controlled by `dampingRatio` (how bouncy) and `stiffness` (how fast), and it settles naturally."},{t:"list",items:["**Predictability vs naturalness**: `tween` is deterministic — good when you need a transition to take a known time (a scripted onboarding step). `spring` feels more organic and matches how physical objects move.","**The decisive difference — interruptibility**: if the target changes *mid-animation*, a spring smoothly redirects from its current position *and velocity* toward the new target. A tween, by contrast, tends to restart. So for anything gesture-driven or frequently-changing (a value that can be re-targeted while animating), spring feels right and tween looks janky. That's why spring is the default for many Compose animations.","Other specs to know: `keyframes` (values at specific timestamps for multi-stage motion) and `infiniteRepeatable` (looping animations like a pulse or shimmer)."]}]},{level:"senior",q:"Explain staticCompositionLocalOf vs compositionLocalOf. What's the performance implication?",a:[{t:"p",text:"**The concept**: both create a CompositionLocal, but they differ in whether Compose *tracks reads* of the value — and that changes what happens when the value changes."},{t:"list",items:["**`compositionLocalOf`** — tracks reads. Compose records which composables read `.current`, so when the provided value changes, it recomposes **only those readers**. Slightly more bookkeeping per read, but changes are surgical. Use for values that **change at runtime** and where only some composables care — e.g. the current user, a runtime setting.","**`staticCompositionLocalOf`** — does *not* track reads. Reading `.current` is a plain lookup with zero tracking overhead. The catch: because Compose doesn't know who read it, when the value changes it must recompose the **entire subtree** under the provider. Cheap to read, expensive to change. Use for values that essentially **never change** after being provided — a design-system spacing object, a fixed logger, the theme's static tokens."]},{t:"p",text:"**The performance decision**: match the tool to the change frequency. A theme's spacing tokens that are set once at app start → `staticCompositionLocalOf` (fast reads, and the 'recompose everything' cost never triggers because it never changes). A value that flips during use → `compositionLocalOf` (you *want* read tracking so a change doesn't blow away the whole subtree). Getting this backwards is a real, subtle performance bug: a frequently-changing `staticCompositionLocalOf` recomposes enormous subtrees on every change."}]},{level:"senior",q:"When would you use Animatable or Transition instead of animate*AsState?",a:[{t:"p",text:"**The tiers**: `animate*AsState` is the declarative, fire-and-forget top layer — perfect when a value should smoothly follow a target and you don't need fine control. You drop to lower-level APIs when you need *control* or *coordination* that the declarative form can't express."},{t:"list",items:["**`Animatable`** — the imperative, coroutine-driven primitive underneath `animate*AsState`. You call `animateTo()` / `snapTo()` from a coroutine, and you can read current **velocity**, **cancel** mid-flight, and chain animations. This is what you need for **gesture-driven** animation: a swipe-to-dismiss or draggable card where the user's finger drives the value directly (`snapTo` during drag) and then you `animateTo` a settle target with the drag's velocity for a natural fling. The declarative API can't do velocity handoff from a gesture — Animatable can.","**`Transition` (`updateTransition`)** — for coordinating **multiple animations that must stay in sync off one state change**. If selecting an item should animate color *and* size *and* elevation together, driving three separate `animate*AsState` risks them drifting; a `Transition` runs them as one synchronized unit keyed to the state, and exposes named child animations that show up nicely in the Animation Preview tooling.","**`rememberInfiniteTransition`** — for endless, state-independent animations (shimmer loading, pulsing indicators)."]},{t:"p",text:"**Rule of thumb**: `animate*AsState` until you need (a) gesture/velocity control or manual cancellation → `Animatable`, or (b) several animations coordinated by one state → `Transition`. Reaching for the low-level API without one of those needs is over-engineering."}]},{level:"senior",q:"How do you make an animation performant — what's the connection between animation and the three phases?",a:[{t:"p",text:"**The core insight**: an animation changes a value ~60 times a second, and *where you read that value* decides how much work each of those 60 frames costs. If the animated value is read during **composition**, every frame recomposes the composable (re-executing it, re-diffing children) — the most expensive path, and the usual cause of janky Compose animations. If it's read during **layout**, composition is skipped and only measurement/placement re-runs. If read during **draw**, only the paint step re-runs. So performant animation means reading the animated value as *late* in the pipeline as possible."},{t:"code",title:"Same animation, escalating efficiency",code:`// COMPOSITION read -> recomposes every frame (avoid for animations)
Box(Modifier.offset(x = animatedX.value.dp))

// LAYOUT read (lambda defers it) -> composition skipped
Box(Modifier.offset { IntOffset(animatedX.value.roundToPx(), 0) })

// DRAW read -> only redraw, for purely visual properties
Box(Modifier.graphicsLayer { alpha = animatedAlpha.value; scaleX = scale.value })`},{t:"list",items:["**The mechanism**: the *lambda* forms of `offset`, plus `graphicsLayer`/`drawBehind`/`drawWithContent`, invoke their lambda during the layout or draw phase. Reading the animated state *inside* that lambda records the read against that phase, so the state change only invalidates layout/draw — not composition.","**Property-to-phase mapping**: positional changes (offset, size) → layout-phase reads; purely visual changes (alpha, scale, rotation, translation, clip) → `graphicsLayer` draw-phase reads. Only animate in composition if the animation genuinely changes *what is composed* (e.g. `AnimatedContent` swapping content).","**`graphicsLayer` bonus**: it renders into a separate layer, so transformations are GPU-cheap and don't even trigger redraw of the content itself for things like alpha/scale.","This is the exact intersection the interviewer is testing: you understand *both* the animation APIs *and* the phase model, and you connect them — 'animate with deferred reads so the 60fps updates hit layout/draw, never composition.'"]}]},{level:"senior",q:"How would you build a custom design system on top of (or beside) MaterialTheme in Compose?",a:[{t:"p",text:"**The concept**: `MaterialTheme` provides exactly three CompositionLocals (color scheme, typography, shapes). A real design system usually needs *more* — custom spacing tokens, elevation levels, brand-specific color roles, custom shapes — and sometimes wants to fully replace Material's vocabulary. Compose supports both extending and replacing because a theme is 'just' a set of CompositionLocals plus an accessor object; you can make your own."},{t:"list",items:["**Approach 1 — extend MaterialTheme**: keep `MaterialTheme` for standard tokens and *add* your own CompositionLocals for what it lacks. Define `val LocalSpacing = staticCompositionLocalOf { Spacing() }`, provide it inside your `AppTheme`, and expose a convenient accessor object (often an `object AppTheme { val spacing @Composable get() = LocalSpacing.current; val colors @Composable get() = MaterialTheme.colorScheme }`) so call sites read `AppTheme.spacing.large`. Low-friction, interoperates with Material components.","**Approach 2 — fully custom theme**: define your *own* `ColorScheme`/`Typography` data classes and CompositionLocals, provide them, and build your own accessor — used when the brand's system doesn't map to Material's roles at all. More work (you re-implement what Material gave you and lose automatic component theming), so justify it only for strong brand systems.","**Provider choice matters**: static tokens (spacing, shapes) that never change at runtime → `staticCompositionLocalOf` (fast reads); anything that can change during use → `compositionLocalOf`.","**Reactive theming falls out for free**: because tokens are read via CompositionLocal (a state read), a theme change recomposes only the readers.","**Testability note**: keep the accessor thin and the tokens as plain data so previews can wrap content in `AppTheme { }` easily — a design system nobody can preview is a failed one."]},{t:"p",text:"The senior framing: a Compose design system is a small set of CompositionLocals + immutable token data classes + a thin accessor, layered on or beside MaterialTheme — and the key decisions are *what to add vs replace*, and *static vs tracked* locals per token's change frequency."}]},{level:"junior",q:"What are the three pillars of MaterialTheme (color, typography, shape)?",a:[{t:"p",text:"`MaterialTheme` provides three theming systems your whole UI reads from: a `ColorScheme` (semantic colors like `primary`, `surface`, `onSurface`), a `Typography` (named text styles like `bodyLarge`, `headlineSmall`), and `Shapes` (corner styles for `small`/`medium`/`large` components). Composables and Material components read these via `MaterialTheme.colorScheme`/`.typography`/`.shapes`, so changing the theme restyles everything consistently."},{t:"code",title:"Setting and reading the theme",code:`MaterialTheme(
    colorScheme = if (dark) darkColorScheme() else lightColorScheme(),
    typography = AppTypography,
    shapes = AppShapes,
) { AppContent() }

// Reading it anywhere inside:
Text("Hi", color = MaterialTheme.colorScheme.primary, style = MaterialTheme.typography.bodyLarge)`},{t:"list",items:["**`ColorScheme`** — semantic roles (`primary`, `secondary`, `surface`, `error`, and their `on*` pairs) — use roles, not raw hex, so dark mode and theming work.","**`Typography`** — named text styles; apply `style = MaterialTheme.typography.*` instead of hardcoding font sizes.","**`Shapes`** — `small`/`medium`/`large` corner shapes used by components (cards, buttons).","**Propagated via CompositionLocal** — `MaterialTheme` sets locals so any descendant reads the current values without prop drilling."]},{t:"note",text:"MaterialTheme provides ColorScheme (semantic roles: primary/surface/onSurface…), Typography (named styles: bodyLarge…), and Shapes (small/medium/large corners), propagated via CompositionLocal. Read roles/styles (MaterialTheme.colorScheme.primary), never raw hex/sizes — that's what makes dark mode and re-theming work."}]},{level:"junior",q:"Why use semantic color roles (onSurface, primary) instead of hardcoded colors?",a:[{t:"p",text:"Semantic roles describe a color's *purpose* (`primary`, `surface`, `onSurface`) rather than its value. When you use roles, switching to dark theme (or a different brand theme) just swaps the `ColorScheme`, and everything recolors correctly — including the `on*` pairs that guarantee readable contrast. Hardcoded `Color(0xFF...)` breaks in dark mode and duplicates values everywhere."},{t:"list",items:["**Roles adapt** — `surface`/`onSurface` resolve to light values in the light scheme and dark values in the dark scheme automatically.","**`on*` pairs = guaranteed contrast** — text on `primary` uses `onPrimary`, ensuring legibility in both themes.","**Single source of truth** — change the brand color once in the scheme, not in dozens of composables.","**Hardcoded hex** — looks fine in light mode, becomes invisible/ugly in dark mode, and can't be re-themed."]},{t:"code",title:"Roles vs hex",code:`// BAD: breaks in dark mode
Surface(color = Color(0xFFFFFFFF)) { Text("Hi", color = Color.Black) }
// GOOD: adapts to the scheme, correct contrast in both themes
Surface(color = MaterialTheme.colorScheme.surface) {
    Text("Hi", color = MaterialTheme.colorScheme.onSurface)
}`},{t:"note",text:"Semantic roles (primary/surface/onSurface) describe purpose, so swapping the ColorScheme (dark/brand) recolors everything correctly, and on* pairs guarantee contrast. Hardcoded hex breaks in dark mode and duplicates values. Always pair a background role with its on* role for text."}]},{level:"junior",q:"What is animateContentSize, and when do you use it?",a:[{t:"p",text:"`Modifier.animateContentSize()` smoothly animates a composable's size change when its content changes size — for example, an expanding/collapsing card whose text goes from one line to many. Instead of snapping to the new size, it interpolates, giving a polished expand/collapse without manually animating a height value."},{t:"code",title:"Expand/collapse with animateContentSize",code:`Column(
    Modifier
        .clickable { expanded = !expanded }
        .animateContentSize()          // animates the height change automatically
) {
    Text(title)
    if (expanded) Text(longDescription)   // adding/removing this animates the size
}`},{t:"list",items:["**Automatic size animation** — no need to compute/animate a height; it interpolates between measured sizes.","**Customizable** — pass an `animationSpec` (tween/spring) to control feel.","**Placement in the chain** — where you put it matters; typically after size-affecting modifiers you want animated and before padding you don't.","**Great for** — expandable cards, showing/hiding detail, content that grows as data loads."]},{t:"note",text:"Modifier.animateContentSize() smoothly interpolates a composable's size when its content changes size (expand/collapse cards, growing text) — no manual height animation. Pass an animationSpec for feel; mind its position in the modifier chain. The easiest way to animate reveal/hide of content."}]},{level:"junior",q:"How do you animate appearance and disappearance with AnimatedVisibility?",a:[{t:"p",text:"`AnimatedVisibility(visible)` animates a composable in and out with enter/exit transitions (fade, slide, expand, shrink). When `visible` flips, it plays the enter or exit animation and adds/removes the content — the standard way to animate showing/hiding UI like a FAB, banner, or expandable section."},{t:"code",title:"AnimatedVisibility with enter/exit",code:`AnimatedVisibility(
    visible = showBanner,
    enter = fadeIn() + slideInVertically(),
    exit = fadeOut() + slideOutVertically(),
) {
    Banner()   // animated in/out; removed from composition when fully hidden
}`},{t:"list",items:["**Enter/exit transitions** — combine `fadeIn/Out`, `slideIn/Out`, `expandIn`/`shrinkOut`, `scaleIn/Out` with `+`.","**Content is removed when hidden** — after the exit animation, the content leaves the composition (its state is forgotten unless hoisted).","**Children can animate individually** — inside its scope, `Modifier.animateEnterExit()` gives per-child transitions.","**`AnimatedContent`** is the sibling API for animating *between* different contents (e.g. count changes, tab switches)."]},{t:"note",text:"AnimatedVisibility(visible) animates content in/out via enter/exit specs (fadeIn/slideIn/expandIn + fadeOut/…). Content leaves the composition after the exit (state forgotten unless hoisted); children can use Modifier.animateEnterExit(). Use AnimatedContent to animate BETWEEN different contents."}]},{level:"junior",q:"What is AnimatedContent, and how does it differ from Crossfade?",a:[{t:"p",text:"`AnimatedContent` animates a transition *between different values/contents* — when a state changes, it animates the old content out and the new content in, with full control over the transition (and even size changes). `Crossfade` is the simpler special case that just fades between two contents. Use `AnimatedContent` for count changes, tab/step switches, or loading→loaded transitions."},{t:"code",title:"AnimatedContent for a changing value",code:`AnimatedContent(
    targetState = count,
    transitionSpec = {
        if (targetState > initialState) slideInVertically { it } togetherWith slideOutVertically { -it }
        else slideInVertically { -it } togetherWith slideOutVertically { it }
    },
) { c -> Text("$c", style = MaterialTheme.typography.headlineLarge) }`},{t:"list",items:["**`AnimatedContent`** — animate between arbitrary contents keyed on `targetState`; `transitionSpec` controls direction/feel; can animate size between states (`SizeTransform`).","**`Crossfade`** — just cross-dissolves between contents; less control, simpler API. Good for screen/tab swaps where a fade suffices.","**`togetherWith`** — pairs the enter and exit specs.","**Reads the target in the content lambda** — always render `targetState` (the lambda parameter), not the outer state, so each in/out frame shows the right content."]},{t:"note",text:"AnimatedContent animates BETWEEN contents keyed on targetState with a transitionSpec (direction, SizeTransform) — for counters, tab/step switches, loading→loaded. Crossfade is the simple fade-only special case. Render the lambda's targetState param (not outer state) so each frame shows the correct content."}]},{level:"junior",q:"How does updateTransition let you coordinate multiple animations together?",a:[{t:"p",text:"`updateTransition(targetState)` creates a `Transition` driven by one state, and you derive *multiple* animated values from it with `transition.animateColor`, `animateDp`, `animateFloat`, etc. All of them animate in sync when the state changes — perfect for a selected/unselected or expanded/collapsed component where color, size, and elevation should change together."},{t:"code",title:"Coordinated animations",code:`val transition = updateTransition(selected, label = "chip")
val color by transition.animateColor(label = "color") { if (it) Primary else Surface }
val elevation by transition.animateDp(label = "elev") { if (it) 8.dp else 2.dp }
val scale by transition.animateFloat(label = "scale") { if (it) 1.05f else 1f }
Card(Modifier.graphicsLayer { scaleX = scale; scaleY = scale }) { /* uses color, elevation */ }`},{t:"list",items:["**One source, many values** — all child animations share the transition's clock and target, staying coordinated.","**Per-value specs** — each `animate*` can have its own `transitionSpec` (different durations/easings) while still synced to the state.","**Labels** — help the Animation Preview/inspector tools.","**vs `animate*AsState`** — use that for a single independent value; use `updateTransition` when several values must change as a set."]},{t:"note",text:"updateTransition(state) drives MULTIPLE animated values (animateColor/animateDp/animateFloat) from one state, all synced — ideal for selected/expanded components where color+size+elevation change together. Each value can have its own spec. Use animate*AsState for a single value; updateTransition for a coordinated set."}]},{level:"junior",q:"How do you create an infinitely repeating animation (pulsing, loading)?",a:[{t:"p",text:"Use `rememberInfiniteTransition()` with `animateFloat`/`animateColor` and an `infiniteRepeatable` spec. It runs forever (while in composition), looping or reversing — ideal for pulsing dots, shimmer loading effects, rotating spinners, or breathing highlights."},{t:"code",title:"A pulsing alpha",code:`val transition = rememberInfiniteTransition(label = "pulse")
val alpha by transition.animateFloat(
    initialValue = 0.3f, targetValue = 1f,
    animationSpec = infiniteRepeatable(tween(800), RepeatMode.Reverse),
    label = "alpha",
)
Box(Modifier.alpha(alpha).background(Primary))`},{t:"list",items:["**`rememberInfiniteTransition`** — the source; animations run while it's in composition and stop when it leaves.","**`infiniteRepeatable(spec, RepeatMode.Restart/Reverse)`** — loop (restart) or ping-pong (reverse).","**Cheap-to-animate targets** — animate `alpha`/`scale`/`rotation` via `graphicsLayer` for draw-phase efficiency; avoid animating layout-affecting properties infinitely.","**Shimmer** — a common use: animate a gradient offset infinitely across placeholder shapes."]},{t:"note",text:"rememberInfiniteTransition() + animateFloat/Color with infiniteRepeatable(spec, RepeatMode.Reverse/Restart) makes forever-looping animations (pulse, shimmer, spinner) — stops when it leaves composition. Animate draw-phase props (alpha/scale/rotation via graphicsLayer), not layout-affecting ones, for efficiency."}]},{level:"junior",q:"How do you build a custom shape or draw custom graphics in Compose?",a:[{t:"p",text:"For custom outlines (a shape to clip/background), implement the `Shape` interface's `createOutline`. For freeform drawing, use the `Canvas` composable (or `Modifier.drawBehind`/`drawWithContent`), which gives a `DrawScope` with `drawRect`, `drawCircle`, `drawPath`, etc. This is how you make progress rings, charts, badges, and decorative graphics."},{t:"code",title:"Canvas drawing and a custom Shape",code:`Canvas(Modifier.size(120.dp)) {
    drawArc(color = Primary, startAngle = -90f, sweepAngle = 270f * progress,
        useCenter = false, style = Stroke(width = 12.dp.toPx(), cap = StrokeCap.Round))
}

// Custom Shape for clipping:
class TicketShape(val notch: Dp) : Shape {
    override fun createOutline(size: Size, dir: LayoutDirection, density: Density): Outline {
        val path = Path().apply { /* build with lines/arcs */ }
        return Outline.Generic(path)
    }
}`},{t:"list",items:["**`Canvas`/`drawBehind`/`drawWithContent`** — `DrawScope` primitives (`drawLine`, `drawPath`, `drawArc`, `drawImage`) for custom visuals.","**`Shape` + `createOutline`** — a reusable outline for `clip`/`background`/`border` (tickets, chat bubbles).","**`Path`** — build complex geometry; reuse/`remember` paths rather than allocating each draw.","**Draw phase** — drawing happens in the draw phase; animate draw inputs (a `progress` state) to animate the graphic cheaply."]},{t:"note",text:"Custom graphics: Canvas/drawBehind/drawWithContent give a DrawScope (drawArc/drawPath/drawCircle) for charts/rings/badges; implement Shape.createOutline for reusable clip/background outlines (tickets, bubbles). remember Paths to avoid per-draw allocation. Drawing is draw-phase — animating a progress state is cheap."}]},{level:"senior",q:"How do gesture-driven animations work (draggable, swipeable, Animatable)?",a:[{t:"p",text:"Gesture-driven animation combines a gesture modifier (`draggable`, `pointerInput` + `detectDragGestures`, or the anchored-draggable APIs) with an `Animatable` value you update from the gesture and then `animateTo` a resting position on release. `Animatable` is the low-level, coroutine-based animation primitive that lets you both snap (`snapTo`) during the drag and animate (`animateTo`) on fling/settle."},{t:"code",title:"Drag then settle with Animatable",code:`val offsetX = remember { Animatable(0f) }
val scope = rememberCoroutineScope()
Box(Modifier.pointerInput(Unit) {
    detectHorizontalDragGestures(
        onHorizontalDrag = { _, delta -> scope.launch { offsetX.snapTo(offsetX.value + delta) } },
        onDragEnd = { scope.launch { offsetX.animateTo(0f, spring()) } },  // settle back
    )
}.offset { IntOffset(offsetX.value.roundToInt(), 0) })`},{t:"list",items:["**`Animatable`** — coroutine-driven value with `snapTo` (instant, during drag) and `animateTo` (animated, on settle); supports `animateDecay` for flings.","**Gesture source** — `draggable`, `pointerInput` + `detectDragGestures`, or `anchoredDraggable` (the modern swipe-between-anchors API replacing `swipeable`).","**Read the offset in a lambda modifier** — `offset { }`/`graphicsLayer { }` to keep the drag in layout/draw (no recomposition per frame).","**Velocity & fling** — capture velocity to decide the target anchor / decay the fling naturally."]},{t:"note",text:"Gesture animation = a gesture modifier (draggable / detectDragGestures / anchoredDraggable) feeding an Animatable: snapTo during the drag, animateTo/animateDecay to settle or fling on release. Read the value in an offset{}/graphicsLayer{} lambda (no per-frame recomposition). anchoredDraggable replaces the old swipeable."}]},{level:"junior",q:"What are the common animationSpecs (tween, spring, keyframes) and when to use each?",a:[{t:"p",text:"An `animationSpec` defines *how* a value travels from start to end over time. The three you'll use most: `tween` (duration + easing curve), `spring` (physics-based, defined by stiffness/damping — no fixed duration), and `keyframes` (explicit values at specific times). Choosing the right one is what makes motion feel natural vs mechanical."},{t:"list",items:["**`spring`** — physics-based, interruptible, natural feel; great default for gesture/state-driven motion because it handles velocity when a target changes mid-animation. Tune `dampingRatio` and `stiffness`.","**`tween`** — fixed `durationMillis` + `easing` (e.g. `FastOutSlowInEasing`); use when you need a precise duration/curve (branded motion, timed reveals).","**`keyframes`** — set values at specific millis for multi-stage motion (overshoot then settle, custom paths).","**`repeatable`/`infiniteRepeatable`** — wrap a spec to loop; **`snap`** — jump instantly (no animation)."]},{t:"code",title:"Specifying the feel",code:`animateDpAsState(size, animationSpec = spring(dampingRatio = Spring.DampingRatioMediumBouncy))
animateFloatAsState(alpha, animationSpec = tween(300, easing = FastOutSlowInEasing))
animateDpAsState(x, animationSpec = keyframes { durationMillis = 400; 20.dp at 100; 0.dp at 400 })`},{t:"note",text:"animationSpec = how the value travels: spring (physics, interruptible, natural — best default for state/gesture motion), tween (fixed duration + easing — precise/branded timing), keyframes (values at specific times — multi-stage). Wrap in repeatable to loop, snap to jump instantly. Spring handles mid-animation target changes gracefully."}]},{level:"senior",q:"How do you avoid providing a CompositionLocal for data that should be a parameter?",a:[{t:"p",text:"`CompositionLocal` is for *ambient* values many composables read but that aren't part of a composable's core contract — theme, density, current locale. It's tempting to use it to avoid 'prop drilling', but overusing it makes data flow implicit and hard to trace, breaks reuse (a composable silently depends on an ambient it may not receive), and hurts testability. The rule: pass explicit parameters for a composable's actual inputs; reserve CompositionLocal for truly cross-cutting ambient values."},{t:"list",items:["**Good CompositionLocal uses** — theme, typography, `LocalContext`, `LocalDensity`, current logged-in user for a whole app, a design-system's tokens — things read widely and rarely passed explicitly.","**Bad uses** — passing a screen's ViewModel, a specific item's data, or a callback via a local to skip parameters; it hides dependencies.","**Costs of overuse** — implicit dependencies (composable breaks if the provider is missing), harder testing (must provide locals), and no compile-time guarantee the value exists.",'**Default value** — `compositionLocalOf { error("not provided") }` forces a provider; a sensible default can mask missing setup.']},{t:"note",text:"CompositionLocal is for ambient, widely-read, rarely-passed values (theme, density, context, design tokens) — NOT to dodge prop drilling for a composable's real inputs (ViewModel, item data, callbacks). Overuse makes data flow implicit, breaks reuse/testability, and risks missing-provider crashes. Prefer explicit parameters."}]},{level:"senior",q:"How do you implement shared-element transitions in Compose?",a:[{t:"p",text:"Compose provides shared-element transitions via `SharedTransitionLayout` + `Modifier.sharedElement(...)`/`sharedBounds(...)` tied to an `AnimatedContent`/`AnimatedVisibility` scope. You mark the 'same' element in two states (e.g. a thumbnail in a list and the hero image on the detail screen) with a matching key, and Compose animates its position/size/shape between them."},{t:"code",title:"Shared element sketch",code:`SharedTransitionLayout {
    AnimatedContent(targetState = screen) { s ->
        when (s) {
            List -> Thumb(Modifier.sharedElement(rememberSharedContentState(key = "img-$id"), this@AnimatedContent))
            Detail -> Hero(Modifier.sharedElement(rememberSharedContentState(key = "img-$id"), this@AnimatedContent))
        }
    }
}`},{t:"list",items:["**`SharedTransitionLayout`** — the container that coordinates shared elements.","**Matching keys** — the same `rememberSharedContentState(key)` in both states tells Compose these are the 'same' element to morph between.","**`sharedElement` vs `sharedBounds`** — `sharedElement` for identical content that moves/resizes; `sharedBounds` for containers whose content differs but the bounds morph.","**Needs an animation scope** — works within `AnimatedContent`/`AnimatedVisibility`/nav transitions; integrate with Navigation Compose's animated destinations."]},{t:"note",text:"SharedTransitionLayout + Modifier.sharedElement/sharedBounds with matching rememberSharedContentState(key) morph an element (thumbnail→hero image) between two states inside an AnimatedContent/AnimatedVisibility scope. sharedElement = same content moves/resizes; sharedBounds = differing content, morphing bounds. Integrates with Navigation Compose transitions."}]},{level:"junior",q:"How do you provide fonts and a custom Typography in Compose?",a:[{t:"p",text:"Define a `FontFamily` from font resources (or downloadable/Google Fonts), build a `Typography` mapping each named style (`bodyLarge`, `titleMedium`, etc.) to a `TextStyle` using that family, and pass it to `MaterialTheme`. Then components and your `Text`s read `MaterialTheme.typography.*` for consistent, themeable text."},{t:"code",title:"Custom font + typography",code:`val Inter = FontFamily(
    Font(R.font.inter_regular, FontWeight.Normal),
    Font(R.font.inter_bold, FontWeight.Bold),
)
val AppTypography = Typography(
    bodyLarge = TextStyle(fontFamily = Inter, fontSize = 16.sp, lineHeight = 24.sp),
    titleLarge = TextStyle(fontFamily = Inter, fontWeight = FontWeight.Bold, fontSize = 22.sp),
)
MaterialTheme(typography = AppTypography) { AppContent() }`},{t:"list",items:["**`FontFamily`** — group weights/styles from `res/font` (or `GoogleFont`/downloadable fonts to avoid bundling).","**`Typography`** — assign a `TextStyle` per Material role; use roles in UI (`style = MaterialTheme.typography.bodyLarge`).","**Downloadable fonts** — reduce APK size and load asynchronously via the Google Fonts provider.","**Consistency** — always reference typography roles; hardcoding `fontSize` scatters values and breaks scaling/theming."]},{t:"note",text:"Build a FontFamily from res/font (or Google/downloadable fonts), map named roles in a Typography (bodyLarge/titleLarge → TextStyle with the family), pass it to MaterialTheme, and use style = MaterialTheme.typography.* everywhere. Downloadable fonts cut APK size. Reference roles, don't hardcode sizes."}]},{level:"junior",q:"How do you respect the user's font scale and accessibility settings?",a:[{t:"p",text:"Use scalable pixel units (`sp`) for text so it honors the system font-scale accessibility setting, and avoid fixed pixel/dp sizing for text or fixed-height text containers that would clip enlarged text. Compose applies the user's font scale automatically to `sp`; your job is to build layouts that adapt when text grows."},{t:"list",items:["**`sp` for text, `dp` for everything else** — `sp` scales with the accessibility font-size setting; using `dp` for font size ignores it.","**Don't fix text container heights** — an enlarged font in a fixed-height box clips; let containers wrap content.","**Test at large scales** — enable the largest font size / display size in settings and verify no clipping or overlap.","**Content descriptions & semantics** — provide `contentDescription` for images/icons and use semantics so TalkBack works; touch targets ≥ 48dp.","**Non-linear font scaling** — modern Android applies non-linear scaling at large sizes; using `sp` gets this for free."]},{t:"note",text:"Use sp for text (scales with the accessibility font-size setting) and dp for other dimensions; never fix text-container heights (enlarged text clips). Test at max font/display size, add contentDescription/semantics for TalkBack, keep touch targets ≥48dp. sp also gets non-linear large-font scaling for free."}]},{level:"senior",q:"What is dynamic color (Material You), and how do you implement it with a fallback?",a:[{t:"p",text:"Dynamic color (Material You) derives your app's `ColorScheme` from the user's wallpaper on Android 12+, so the app matches their system palette. You implement it by using `dynamicLightColorScheme`/`dynamicDarkColorScheme` when available, and falling back to your brand `ColorScheme` on older versions or when you want a fixed brand identity."},{t:"code",title:"Dynamic color with fallback",code:`val context = LocalContext.current
val colorScheme = when {
    Build.VERSION.SDK_INT >= Build.VERSION_CODES.S ->
        if (dark) dynamicDarkColorScheme(context) else dynamicLightColorScheme(context)
    dark -> BrandDarkColors
    else -> BrandLightColors
}
MaterialTheme(colorScheme = colorScheme) { AppContent() }`},{t:"list",items:["**Android 12+ only** — `dynamic*ColorScheme` needs API 31+; gate it and fall back to brand colors below.","**Brand vs personalization trade-off** — dynamic color delights users but weakens brand consistency; some apps offer it as a toggle.","**Still use roles** — because you use semantic roles everywhere, swapping to a dynamic scheme just works.","**Test both** — verify contrast and brand-critical colors (logos) still look right under arbitrary wallpaper-derived palettes."]},{t:"note",text:"Dynamic color (Material You) builds the ColorScheme from the user's wallpaper on API 31+ via dynamicLight/DarkColorScheme(context); gate by SDK and fall back to brand colors below (or as a user toggle for brand consistency). It works because you use semantic roles. Test contrast under arbitrary palettes."}]},{level:"senior",q:"How do you animate between values as a low-level primitive with Animatable vs animate*AsState?",a:[{t:"p",text:"`animate*AsState` is the *declarative* high-level API: you give it a target and it animates whenever that target changes — no coroutine, ideal for simple state-driven animations. `Animatable` is the *imperative* low-level primitive: you control it in a coroutine with `animateTo`/`snapTo`/`stop`, giving precise control needed for gestures, sequences, and interruption logic."},{t:"table",headers:["","animate*AsState","Animatable"],rows:[["Style","declarative (target → auto-animate)","imperative (coroutine calls)"],["Control","just set target","animateTo / snapTo / stop / decay"],["Best for","simple state-driven values","gestures, sequences, precise control"],["Interruption","auto (retargets)","manual but fully controllable"]]},{t:"list",items:["**`animate*AsState`** — `val size by animateDpAsState(if (expanded) 200.dp else 100.dp)`; set-and-forget, retargets automatically.","**`Animatable`** — for drag-and-settle, animation *sequences* (`animateTo(a); animateTo(b)`), decays/flings, and reading/stopping mid-flight.","**Both are interruptible** — but `Animatable` lets you decide exactly how (e.g. capture current velocity into a fling)."]},{t:"note",text:"animate*AsState = declarative (set a target, it auto-animates on change) — for simple state-driven values. Animatable = imperative coroutine primitive (animateTo/snapTo/stop/decay) — for gestures, sequences, flings, and precise interruption control. Reach for Animatable when set-a-target isn't enough."}]},{level:"junior",q:"How do you handle system bars and edge-to-edge theming (status bar color/icons)?",a:[{t:"p",text:"With edge-to-edge (the modern default), you don't set opaque status/navigation bar colors; instead you make the bars transparent and control the *icon* appearance (light vs dark icons) to contrast with your content behind them. `enableEdgeToEdge()` in the Activity sets this up, and you adjust icon appearance based on your theme."},{t:"list",items:["**`enableEdgeToEdge()`** — call in `onCreate`; content draws behind transparent system bars.","**Icon appearance** — set light/dark status-bar icons to contrast with whatever your content shows there (via the systemBars style in `enableEdgeToEdge`, or `WindowCompat`/`WindowInsetsControllerCompat`).","**Apply insets** — pad content with `statusBarsPadding`/`navigationBarsPadding`/`safeDrawingPadding` so it isn't hidden under the bars.","**Deprecated approach** — directly setting `window.statusBarColor` is deprecated; prefer transparent bars + inset handling + icon appearance."]},{t:"note",text:"Edge-to-edge (default): call enableEdgeToEdge(), make bars transparent, and control status-bar ICON light/dark to contrast with your content — don't set opaque bar colors (window.statusBarColor is deprecated). Pad content with statusBarsPadding/safeDrawingPadding so it isn't hidden behind the bars."}]},{level:"junior",q:"How do you preview themes and dark mode in Android Studio?",a:[{t:"p",text:"Use `@Preview` annotations with parameters to render composables in the IDE without running the app, including dark mode via `uiMode = UI_MODE_NIGHT_YES`, different font scales, locales, and device sizes. Wrapping previews in your `AppTheme` shows real themed output; multipreview annotations render several variants at once."},{t:"code",title:"Theme and dark-mode previews",code:`@Preview(name = "Light")
@Preview(name = "Dark", uiMode = Configuration.UI_MODE_NIGHT_YES)
@Preview(name = "Large font", fontScale = 1.5f)
@Composable
fun CardPreview() {
    AppTheme { ProfileCard(sampleUser) }   // wrap in the real theme
}
// Custom multipreview: annotate once to get all variants
@Preview(uiMode = UI_MODE_NIGHT_YES) @Preview annotation class ThemePreviews`},{t:"list",items:["**`@Preview` params** — `uiMode` (dark), `fontScale`, `locale`, `device`, `showBackground`, `widthDp`.","**Wrap in `AppTheme`** — otherwise you see unthemed defaults; always theme previews.","**Multipreview annotations** — define one annotation (e.g. `@ThemePreviews`) combining several `@Preview`s to render light/dark/large-font together.","**`@PreviewParameter`** — feed sample data variants (empty, long text, error) into a preview."]},{t:"note",text:"Preview themes with @Preview(uiMode = UI_MODE_NIGHT_YES) for dark, plus fontScale/locale/device params — always wrap the preview in AppTheme. Combine variants with a custom multipreview annotation (@ThemePreviews) and feed sample data via @PreviewParameter (empty/long/error states). Renders in-IDE, no app run."}]},{level:"senior",q:"What are staticCompositionLocalOf and compositionLocalOf's recomposition implications, restated with a concrete example?",a:[{t:"p",text:"`compositionLocalOf` tracks reads and, when its provided value changes, recomposes only the composables that *read* it. `staticCompositionLocalOf` does *not* track reads — changing its value recomposes the *entire* content under the provider. So the choice hinges on how often the value changes: frequently-changing → `compositionLocalOf`; rarely/never-changing → `staticCompositionLocalOf` (faster reads, cheaper because no tracking)."},{t:"list",items:["**`compositionLocalOf`** — read-tracked; a value change recomposes only readers. Use for values that change during runtime (e.g. a theme the user can toggle live, current elevation).","**`staticCompositionLocalOf`** — not tracked; changing it recomposes everything below the provider. Use for values that are effectively constant for a subtree (e.g. `LocalContext`, a logging tag) — reads are cheaper.","**Concrete** — a live-toggleable accent color → `compositionLocalOf` (only accented widgets recompose). An app-wide immutable config → `staticCompositionLocalOf` (never changes, so no tracking overhead).","**Getting it wrong** — a frequently-changing `staticCompositionLocalOf` causes huge recomposition; a never-changing `compositionLocalOf` just wastes a little tracking overhead (less harmful)."]},{t:"note",text:"compositionLocalOf tracks reads → value change recomposes only readers (use for values that change at runtime). staticCompositionLocalOf skips tracking → value change recomposes the WHOLE subtree, but reads are cheaper (use for effectively-constant values like LocalContext). Wrong static choice = massive recomposition."}]},{level:"senior",q:"How do you structure a scalable design system on top of Material in a large app?",a:[{t:"p",text:"Build a thin layer that *wraps* MaterialTheme: define your own semantic tokens (colors, spacing, typography, elevation) exposed via a small `CompositionLocal`-backed `AppTheme`, plus a set of branded components (buttons, cards) that consume those tokens. This gives one source of truth, enforces consistency, and lets you extend beyond Material's slots (custom spacing scale, extra color roles) without fighting the framework."},{t:"list",items:["**Token layer** — colors/spacing/typography/shape as named tokens; extend Material's `ColorScheme` with extra roles via a custom `LocalAppColors` CompositionLocal for what Material lacks.","**`AppTheme` wrapper** — sets `MaterialTheme` *and* provides your extra locals; the single entry point.","**Component library** — `AppButton`, `AppCard`, etc. reading tokens, so screens never touch raw Material components or hex.","**Spacing scale** — a `LocalSpacing` (e.g. `xs/sm/md/lg`) since Material has no spacing system; enforces rhythm.","**Governance** — lint/detekt rules banning raw `Color(...)`/`.dp` literals in feature code to force token use; document usage."]},{t:"note",text:"Wrap MaterialTheme in an AppTheme that also provides custom tokens (extra color roles, a spacing scale) via CompositionLocals, plus a branded component library (AppButton/AppCard) reading those tokens. One source of truth, extends Material's gaps (spacing), and lint rules banning raw hex/dp keep feature code consistent."}]},{level:"junior",q:"How do you animate a color, and why not just use animateColorAsState blindly?",a:[{t:"p",text:"`animateColorAsState(targetColor)` animates a color whenever the target changes — it interpolates in a perceptually reasonable color space. The caveat is *where* you read the animated color: reading it in composition recomposes each frame, so for hot paths prefer reading it in a draw-phase modifier (`drawBehind`, `graphicsLayer`) or applying it to a property that only triggers redraw, not relayout."},{t:"code",title:"Animated color, read efficiently",code:`val bg by animateColorAsState(if (selected) Primary else Surface, label = "bg")
// Fine (background just redraws):
Box(Modifier.background(bg))
// Hot path: read in draw phase to avoid recomposition each frame
Box(Modifier.drawBehind { drawRect(bg) })`},{t:"list",items:["**`animateColorAsState`** — declarative; set the target and it animates, interpolating across the color space.","**Read location matters** — background/tint typically only redraw (cheap); avoid animated colors driving layout.","**Spec control** — pass `animationSpec = tween/spring` for feel.","**Batch related changes** — if color + size change together, use `updateTransition` so they stay synced."]},{t:"note",text:"animateColorAsState(target) animates on target change (interpolating in color space). It's cheap for background/tint (redraw only); for hot paths read it in a draw modifier (drawBehind) to avoid per-frame recomposition, and use updateTransition when color changes alongside size/elevation."}]},{level:"senior",q:"How do you test animations and make them deterministic in UI tests?",a:[{t:"p",text:"Compose UI tests run with an automatically-controlled clock: the test framework advances the animation clock manually so tests are deterministic. By default `waitForIdle`/auto-sync waits for animations to finish; when you need to assert *mid-animation* state, disable auto-advance and step the clock with `mainClock.advanceTimeBy`."},{t:"code",title:"Controlling the test clock",code:`composeTestRule.mainClock.autoAdvance = false   // stop auto-advancing
composeTestRule.onNodeWithText("Expand").performClick()
composeTestRule.mainClock.advanceTimeBy(150)    // step into the middle of the animation
// assert an intermediate state...
composeTestRule.mainClock.advanceTimeBy(1000)   // finish it
composeTestRule.onNodeWithText("Details").assertIsDisplayed()`},{t:"list",items:["**Auto-advance (default)** — the clock advances and `waitForIdle` blocks until animations settle; good for asserting end state.","**Manual clock** — set `mainClock.autoAdvance = false` and use `advanceTimeBy`/`advanceTimeByFrame` to assert intermediate frames deterministically.","**Infinite animations** — with an active infinite animation, the tree is never 'idle'; you must control the clock manually or the test hangs.","**Disable system animations** for instrumented tests, but Compose's own clock control is separate and reliable."]},{t:"note",text:"Compose UI tests use a controllable animation clock: by default it auto-advances and waitForIdle waits for animations to settle (assert end state). Set mainClock.autoAdvance = false + advanceTimeBy to assert mid-animation frames deterministically. Infinite animations never idle — you MUST control the clock or the test hangs."}]}],dS={id:"theming-animation",title:"Theming & Animation",tagline:"MaterialTheme & CompositionLocal, Material 3 color roles, dynamic color, animate*AsState, specs (tween/spring), Animatable/Transition, and animation performance.",tags:["Compose","Material 3","CompositionLocal","Animation","Theming"],content:lS,qa:cS},hS=[{heading:"Navigation Compose — the core model",blocks:[{t:"p",text:"**Navigation Compose** manages moving between screens (composables) as a **back stack** owned by a `NavController`. You declare a `NavHost` containing `composable(route) { }` destinations; the NavController swaps which destination is shown and maintains the stack for back handling. Each destination is a composable, not an Activity or Fragment — the whole app can be a single Activity with many composable destinations."},{t:"code",title:"The classic string-route setup",code:`val navController = rememberNavController()

NavHost(navController = navController, startDestination = "feed") {
    composable("feed") {
        FeedScreen(onOpenDetail = { id -> navController.navigate("detail/$id") })
    }
    composable(
        route = "detail/{itemId}",
        arguments = listOf(navArgument("itemId") { type = NavType.StringType }),
    ) { backStackEntry ->
        val itemId = backStackEntry.arguments?.getString("itemId")
        DetailScreen(itemId)
    }
}`},{t:"list",items:["`rememberNavController()` creates and remembers the controller; `navigate(route)` pushes; `popBackStack()` / `navigateUp()` pops.","**Arguments** travel in the route string (`detail/{itemId}`) and are read from `backStackEntry.arguments` — but importantly, they also land in the destination's `SavedStateHandle`, so a ViewModel scoped to that destination gets them automatically.",'**Navigation options**: `navigate(route) { popUpTo("feed") { inclusive = true }; launchSingleTop = true }` — control the stack (clear up to a destination, avoid duplicate top entries) — the Compose equivalent of Activity launch modes.','**Deep links**: `composable(route, deepLinks = listOf(navDeepLink { uriPattern = "app://detail/{itemId}" }))` maps external URIs to destinations.']}]},{heading:"Type-safe navigation (the modern approach)",blocks:[{t:"p",text:"String routes are stringly-typed — typos compile fine and crash at runtime, and argument types must be manually parsed. Recent Navigation Compose supports **type-safe routes** using `@Serializable` objects/classes as destinations, so arguments are real typed fields and the compiler catches mistakes:"},{t:"code",title:"Type-safe destinations",code:`@Serializable object Feed
@Serializable data class Detail(val itemId: String)

NavHost(navController, startDestination = Feed) {
    composable<Feed> {
        FeedScreen(onOpenDetail = { id -> navController.navigate(Detail(itemId = id)) })
    }
    composable<Detail> { backStackEntry ->
        val detail: Detail = backStackEntry.toRoute()   // typed, no manual parsing
        DetailScreen(detail.itemId)
    }
}`},{t:"list",items:["Arguments are constructor parameters of the route class — type-checked, no `navArgument` boilerplate, no string interpolation to get wrong.","`backStackEntry.toRoute<Detail>()` reconstructs the typed object; the ViewModel can also `savedStateHandle.toRoute<Detail>()`.","This is the recommended style for new code; string routes remain valid and common in existing apps — know both."]}]},{heading:"ViewModel scoping in navigation, and passing data between screens",blocks:[{t:"list",items:["**Each destination is a `ViewModelStoreOwner`**: `hiltViewModel()` inside a `composable(route) { }` scopes the ViewModel to *that back-stack entry* — created when you navigate in, cleared when the entry is popped. So a detail screen's ViewModel lives exactly as long as the detail screen.","**Nav-graph-scoped ViewModels** for multi-step flows: scope a ViewModel to a *nested graph* (a checkout flow's several screens) so they share one ViewModel that's cleared when the whole flow is popped — `hiltViewModel(navController.getBackStackEntry(\"checkout_graph\"))`.",'**Returning a result to the previous screen** (e.g. a picker): write into the *previous* entry\'s SavedStateHandle — `navController.previousBackStackEntry?.savedStateHandle?.set("picked", value)` then pop; the previous screen observes it. This replaces `startActivityForResult`/`setFragmentResult`.',"**Don't pass large objects as arguments**: routes/arguments go through saved state and URIs — pass *ids*, and load the data from a repository on the destination. Passing whole parcelable objects risks the same size limits as any saved state."]}]},{heading:"Interop: Compose in Views, and Views in Compose",blocks:[{t:"p",text:"Real apps migrate incrementally, so Compose interoperates with the View system **both directions** — a frequent interview topic because you'll rarely start greenfield."},{t:"code",title:"Both directions",code:`// VIEWS INSIDE COMPOSE: AndroidView hosts a classic View (MapView, WebView,
// AdView, a custom legacy View) inside a composition
AndroidView(
    factory = { context -> MapView(context).apply { onCreate(null) } },
    update = { mapView -> /* push new state INTO the view imperatively */ },
    modifier = Modifier.fillMaxSize(),
)

// COMPOSE INSIDE VIEWS: a ComposeView is a normal View you put in XML or
// add programmatically, then call setContent on
class LegacyFragment : Fragment() {
    override fun onCreateView(/* ... */): View =
        ComposeView(requireContext()).apply {
            setContent { MyComposableScreen() }
        }
}`},{t:"list",items:["**`AndroidView`** embeds a View in Compose: `factory` creates it once, `update` runs on recomposition to push new state into it imperatively (the View is mutated the old way). Use for things Compose lacks or that are expensive to reimplement — `MapView`, `WebView`, ad SDKs, camera preview, complex custom Views.","**`ComposeView`** embeds Compose in the View world: it's a regular View you place in XML (`<androidx.compose.ui.platform.ComposeView>`) or add in code, then call `setContent { }`. This is how you add Compose to an existing Fragment/Activity screen by screen.","**Lifecycle bridging gotchas**: a `MapView`/`WebView` inside `AndroidView` needs its own lifecycle callbacks (`onResume`/`onPause`/`onDestroy`) forwarded — a `DisposableEffect` + `LifecycleEventObserver` is the standard pattern, and forgetting it leaks or freezes the embedded view.","**`AndroidViewBinding`** embeds a whole XML layout (via ViewBinding) inside Compose — handy for reusing an existing complex layout during migration."]}]},{heading:"Semantics — how Compose UI is understood by accessibility and tests",blocks:[{t:"p",text:"Compose has no Views for accessibility services or test frameworks to inspect. Instead, every composable contributes to a parallel **semantics tree** — a description of *what the UI means* (this is a button, its text is 'Submit', it's disabled). Accessibility (TalkBack) and UI tests both read this tree, so getting semantics right serves both at once."},{t:"list",items:['Material components add semantics automatically (`Button` is announced as a button, `Text` exposes its text). You augment with `Modifier.semantics { }`, `contentDescription` (for images/icons), `Modifier.testTag("...")` (a test handle), and `mergeDescendants` (treat a composable + children as one node for a11y).',"`clickable` adds click semantics + focus + the a11y action; a raw `pointerInput` gesture does **not** — so custom gestures need explicit semantics or they're invisible to TalkBack and hard to test. This is a real accessibility bug pattern to name.","Because tests query the same tree, writing accessible UI *is* writing testable UI — a nice point to make."]}]},{heading:"Testing Compose UI",blocks:[{t:"p",text:"Compose UI tests use `createComposeRule()` (or `createAndroidComposeRule<Activity>()`), which sets content and drives it. You **find** nodes in the semantics tree, **act** on them, and **assert** — no Espresso view matchers, no ViewHolders:"},{t:"code",title:"A Compose UI test",code:`@get:Rule val composeRule = createComposeRule()

@Test
fun clickingIncrement_updatesCount() {
    composeRule.setContent { AppTheme { Counter() } }

    composeRule.onNodeWithText("Count: 0").assertIsDisplayed()   // FIND + ASSERT
    composeRule.onNodeWithText("Increment").performClick()        // ACT
    composeRule.onNodeWithText("Count: 1").assertExists()         // ASSERT
}`},{t:"list",items:["**Finders**: `onNodeWithText`, `onNodeWithContentDescription`, `onNodeWithTag` (paired with `Modifier.testTag`), `onNode(matcher)` with rich matchers (`hasText`, `isEnabled`, `hasClickAction`).","**Actions**: `performClick`, `performTextInput`, `performScrollTo`, `performTouchInput { swipeUp() }`.","**Assertions**: `assertIsDisplayed`, `assertExists`, `assertIsEnabled`, `assertTextEquals`, `assert(hasText(...))`.","**Synchronization**: the test **auto-waits for idle** — it synchronizes with recomposition and Compose-driven animations, so you rarely need manual waits. For indefinite animations or external async, control the clock (`composeRule.mainClock.autoAdvance = false`, `advanceTimeBy(...)`) or `waitUntil { }`.","**Prefer testing stateless content composables** (pass fake state + lambdas) for fast, focused UI tests; use the full route + ViewModel only for integration tests. `createComposeRule` runs as an instrumented test on device/emulator (or Robolectric for JVM-hosted Compose tests).","**Screenshot testing** (Paparazzi / Roborazzi / Compose Preview screenshot tests) catches visual regressions without a device — increasingly standard for design-system components."]}]}],uS=[{level:"junior",q:"How does navigation work in Jetpack Compose?",a:[{t:"p",text:"**The concept**: Navigation Compose treats screens as composables managed in a **back stack** by a `NavController`. You set up a `NavHost` that lists your destinations — each `composable(route) { ScreenContent() }` — and the NavController decides which one is currently shown and keeps the history for the back button. A whole app can be a single Activity hosting many composable destinations, rather than many Activities/Fragments."},{t:"code",title:"Minimal setup",code:`val navController = rememberNavController()
NavHost(navController, startDestination = "feed") {
    composable("feed") {
        FeedScreen(onOpen = { id -> navController.navigate("detail/$id") })
    }
    composable("detail/{id}") { entry ->
        DetailScreen(entry.arguments?.getString("id"))
    }
}`},{t:"p",text:"You navigate with `navController.navigate(route)` (push) and `popBackStack()`/`navigateUp()` (pop). Arguments ride in the route string (`detail/{id}`) and are read from the destination's `backStackEntry.arguments`. You can also control the stack with options like `popUpTo` and `launchSingleTop` — the Compose equivalent of Activity launch modes — and map external URIs with deep links. Modern Navigation also supports *type-safe* routes using `@Serializable` classes so arguments are real typed fields instead of strings."}]},{level:"junior",q:"How do you pass arguments to a destination, and why shouldn't you pass large objects?",a:[{t:"p",text:'**How**: with string routes, arguments are placeholders in the route (`"detail/{itemId}"`), supplied when navigating (`navigate("detail/42")`) and read from `backStackEntry.arguments`. With type-safe routes, they\'re constructor parameters of a `@Serializable` route class (`navigate(Detail(itemId = "42"))`), read back via `backStackEntry.toRoute<Detail>()`. Either way they also flow into the destination\'s `SavedStateHandle`, so a ViewModel scoped to that destination receives them automatically — which is the cleanest way to consume them.'},{t:"p",text:"**Why not large objects**: navigation arguments are serialized into the back stack's saved state (and, for deep links, into URIs). That mechanism is meant for small, identifier-like values — passing a whole parcelable object couples screens tightly, risks exceeding saved-state size limits (the same `TransactionTooLargeException` family as any Bundle abuse), and means the object can go stale. The idiomatic approach is to **pass an id** and have the destination load the actual data from a repository (which is the single source of truth anyway). If you truly need to hand a result *back* to the previous screen, use the previous back-stack entry's SavedStateHandle rather than forward arguments."}]},{level:"junior",q:"How do you embed a classic Android View (like a MapView or WebView) in Compose?",a:[{t:"p",text:"**The concept**: some UI isn't available in Compose or is expensive to reimplement — Google Maps' `MapView`, `WebView`, ad SDK views, camera preview, complex legacy custom Views. The `AndroidView` composable embeds any classic `View` inside a composition."},{t:"code",title:"AndroidView with factory + update",code:`AndroidView(
    factory = { context -> WebView(context) },   // created ONCE
    update = { webView -> webView.loadUrl(url) }, // runs on recomposition
    modifier = Modifier.fillMaxSize(),
)`},{t:"p",text:"`factory` builds the View a single time and Compose caches it; `update` runs whenever the composable recomposes, and it's where you *imperatively push new state into the View* (the old mutate-the-widget way) — bridging Compose's declarative world to the View's imperative one. The important gotcha to mention: Views like `MapView`/`WebView` have their own lifecycle (`onResume`/`onPause`/`onDestroy`) that Compose won't call — you forward those using a `DisposableEffect` with a `LifecycleEventObserver`, or the view leaks/freezes. The reverse direction (Compose inside Views) uses `ComposeView` — a normal View you place in XML or a Fragment and call `setContent { }` on, which is how apps migrate to Compose screen by screen."}]},{level:"junior",q:"How do you write a basic UI test for a composable?",a:[{t:"p",text:"**The concept**: Compose UI tests use a `ComposeTestRule` (from `createComposeRule()`), which lets you set content and then interact with it. The pattern is always **find → act → assert**: find a node in the semantics tree, perform an action, assert on the result. There are no Espresso view matchers — you query what the UI *means*, not View objects."},{t:"code",title:"Find, act, assert",code:`@get:Rule val rule = createComposeRule()

@Test fun increments() {
    rule.setContent { AppTheme { Counter() } }

    rule.onNodeWithText("Count: 0").assertIsDisplayed()  // find + assert
    rule.onNodeWithText("Increment").performClick()       // act
    rule.onNodeWithText("Count: 1").assertExists()        // assert
}`},{t:"list",items:['**Finders**: `onNodeWithText`, `onNodeWithContentDescription`, `onNodeWithTag` (with `Modifier.testTag("...")` in the UI).',"**Actions**: `performClick`, `performTextInput`, `performScrollTo`, `performTouchInput { swipeUp() }`.","**Assertions**: `assertIsDisplayed`, `assertExists`, `assertTextEquals`, `assertIsEnabled`.","**Auto-sync**: the test automatically waits for recomposition and Compose animations to settle before each assertion, so you rarely write manual waits. A big convenience over Espresso's idling resources."]}]},{level:"junior",q:"What is the semantics tree in Compose?",a:[{t:"p",text:"**The concept**: because Compose draws everything into essentially one custom View, there are no individual View objects for accessibility services or test frameworks to inspect. So Compose maintains a *parallel* tree — the **semantics tree** — that describes what the UI *means* rather than how it's drawn: 'this node is a button', 'its label is Submit', 'it's disabled', 'this text says Count: 3'. It's the machine-readable meaning of the screen."},{t:"p",text:"**Who reads it**: two audiences, and that's the key insight — **accessibility services** (TalkBack announces nodes from it) and **UI tests** (finders like `onNodeWithText` query it). Material components populate it automatically (a `Button` reports itself as a button), and you enrich it with `contentDescription` for images, `Modifier.semantics { }`, and `Modifier.testTag` for test handles. The practical consequence worth stating: because tests and accessibility read the *same* tree, writing accessible UI is simultaneously writing testable UI — and a custom gesture built on raw `pointerInput` (which adds no semantics) is invisible to *both* TalkBack and tests until you add semantics explicitly."}]},{level:"senior",q:"How are ViewModels scoped in Navigation Compose, and how do you share one across a multi-screen flow?",a:[{t:"p",text:"**The foundation**: each destination's `NavBackStackEntry` is a `ViewModelStoreOwner`. So calling `hiltViewModel()` inside a `composable(route) { }` scopes that ViewModel to *that back-stack entry* — it's created when you navigate to the destination and `onCleared()` when the entry is popped off the stack. This gives precise, automatic lifetimes: a detail screen's ViewModel lives exactly as long as the detail screen is on the stack, surviving that screen's own configuration changes but not outliving it."},{t:"list",items:["**Sharing across a flow** — the main technique: define a **nested navigation graph** for the flow (e.g. a checkout with address → payment → review screens) and scope the ViewModel to the *graph's* back-stack entry instead of an individual screen: `val parentEntry = remember(entry) { navController.getBackStackEntry(\"checkout_graph\") }; val vm = hiltViewModel(parentEntry)`. All three screens get the *same* ViewModel, and it's cleared only when the entire flow is popped. Perfect for wizard-style state that must persist across steps but not leak beyond the flow.","**Returning results to a previous screen**: write into `navController.previousBackStackEntry?.savedStateHandle` before popping; the previous screen observes that SavedStateHandle (as state/flow). This is the Compose replacement for `startActivityForResult` / `setFragmentResult` — used for pickers and selection screens.","**Avoid the anti-pattern**: scoping shared state to the *Activity* ViewModel to 'share it everywhere' — that outlives every flow, accumulates state, and obscures data flow. Graph scoping gives sharing *with* correct cleanup.",'**Arguments → SavedStateHandle**: because destination arguments land in the SavedStateHandle, a scoped ViewModel can read `savedStateHandle.toRoute<Detail>()` (or `savedStateHandle["id"]`) directly — no need to pass args through the composable into the ViewModel manually, and it survives process death.']}]},{level:"senior",q:"Compare string-based routes and type-safe navigation. What are the trade-offs?",a:[{t:"p",text:'**String routes** (`"detail/{itemId}"`) were the original model: destinations and arguments are encoded as strings, navigation is `navigate("detail/$id")`, and you declare argument types with `navArgument`. **Type-safe routes** (recent) use `@Serializable` objects/classes as destinations — `@Serializable data class Detail(val itemId: String)` — so `navigate(Detail(itemId = id))` and `backStackEntry.toRoute<Detail>()` are fully typed.'},{t:"list",items:["**Compile-time safety**: string routes fail at *runtime* on a typo or wrong argument type ('detial/42' compiles fine, crashes on navigate); type-safe routes are checked by the compiler — the biggest advantage.","**Argument handling**: strings require manual placeholders, `navArgument` type declarations, and parsing (`getString`, `getInt`); type-safe routes make arguments plain constructor properties with correct types, including nullable and default values, no parsing.","**Refactoring**: renaming a type-safe destination or changing an argument is a compiler-guided refactor; with strings you grep and pray.","**Readability & discoverability**: a route class documents exactly what arguments a screen needs; a route string hides it in a format convention.","**Costs of type-safe**: requires kotlinx.serialization setup; complex/custom argument types need custom `NavType`; and existing large codebases are full of string routes, so you must still read and maintain them."]},{t:"p",text:"**Recommendation**: type-safe routes for new code — they eliminate a whole class of runtime navigation crashes and make arguments first-class. String routes remain valid and ubiquitous, so know both; a migration can be incremental since they interoperate within the same NavHost."}]},{level:"senior",q:"You're embedding a MapView in Compose and it leaks / goes blank on backgrounding. What's wrong and how do you fix it?",a:[{t:"p",text:"**The root cause**: `MapView` (like `WebView`, camera preview, and other stateful legacy Views) is a lifecycle-aware component — it *requires* `onCreate`/`onStart`/`onResume`/`onPause`/`onStop`/`onDestroy` to be called at the right times to allocate/release its rendering resources. When you drop it into `AndroidView`, Compose creates the View but has **no idea** it needs these callbacks — nothing forwards them. So the map never gets `onResume` (goes blank) and never gets `onDestroy` (leaks native/graphics resources and often the Context)."},{t:"code",title:"The fix — forward the lifecycle via DisposableEffect + observer",code:`@Composable
fun MapViewContainer(modifier: Modifier = Modifier) {
    val context = LocalContext.current
    val lifecycle = LocalLifecycleOwner.current.lifecycle
    val mapView = remember { MapView(context) }

    // Forward host lifecycle events into the MapView
    DisposableEffect(lifecycle, mapView) {
        val observer = LifecycleEventObserver { _, event ->
            when (event) {
                Lifecycle.Event.ON_CREATE  -> mapView.onCreate(Bundle())
                Lifecycle.Event.ON_START   -> mapView.onStart()
                Lifecycle.Event.ON_RESUME  -> mapView.onResume()
                Lifecycle.Event.ON_PAUSE   -> mapView.onPause()
                Lifecycle.Event.ON_STOP    -> mapView.onStop()
                Lifecycle.Event.ON_DESTROY -> mapView.onDestroy()
                else -> {}
            }
        }
        lifecycle.addObserver(observer)
        onDispose {
            lifecycle.removeObserver(observer)
            mapView.onDestroy()   // release when the composable leaves
        }
    }

    AndroidView(factory = { mapView }, modifier = modifier)
}`},{t:"list",items:["**Why `remember` the MapView**: creating it in `AndroidView`'s factory is fine, but you need a stable reference to forward callbacks to — `remember { MapView(context) }` gives it, surviving recomposition.","**Why `DisposableEffect`**: it registers the lifecycle observer on enter and, crucially, its `onDispose` both removes the observer and calls `mapView.onDestroy()` when the composable leaves the composition — closing the leak.","**The general principle**: any stateful, lifecycle-owning View embedded via `AndroidView` needs its lifecycle bridged this way. In practice, use the official wrappers when they exist (Maps Compose's `GoogleMap`, Accompanist/AndroidX WebView wrappers) — they encapsulate exactly this. Hand-rolling it is the fallback and the thing to demonstrate you understand."]}]},{level:"senior",q:"How do you handle test synchronization and animations in Compose UI tests?",a:[{t:"p",text:"**What Compose does for you**: the test framework maintains an *idling* concept synchronized with the Compose runtime — before each assertion/action it waits until there are no pending recompositions, no pending layout/draw, and no running Compose-driven animations. This auto-synchronization is why Compose tests rarely need the manual `IdlingResource` dance Espresso required — you write `performClick()` then immediately `assertExists()` and the framework guarantees the recomposition settled in between."},{t:"list",items:["**Where auto-sync breaks — indefinite animations**: an infinite animation (loading shimmer, pulsing indicator) never goes idle, so the test would hang waiting. Fix: take control of the clock with `composeRule.mainClock.autoAdvance = false`, then manually `mainClock.advanceTimeBy(millis)` to step the animation to known points and assert frame by frame.","**External async (network, other threads)**: Compose can only sync with *its own* work, not your coroutines hitting a fake repository. Use fakes with controllable completion, or `composeRule.waitUntil(timeout) { condition }` to poll the semantics tree until the expected state appears.","**`waitForIdle()` / `awaitIdle()`**: explicit sync points when you've disabled auto-advance or need to force a flush.","**Controlling the clock is also a feature, not just a workaround**: pausing the clock lets you assert intermediate animation states deterministically (e.g. 'at 150ms the fade is half done'), which is impossible with a free-running clock.","**Testing strategy that avoids the pain**: prefer testing *stateless content composables* with injected fake state and lambdas — they have no async and settle instantly — and reserve full route+ViewModel+animation integration tests for the few flows that need them. Fast, stable unit-style UI tests plus a thin layer of integration tests."]}]},{level:"junior",q:"What are the core pieces of Navigation Compose (NavController, NavHost, composable destinations)?",a:[{t:"p",text:"Navigation Compose has three core pieces: a `NavController` (holds the back stack and performs navigation), a `NavHost` (maps routes to composable screens and shows the current one), and `composable(route) { }` entries (the destinations). You navigate by calling `navController.navigate(route)`; the `NavHost` swaps the displayed screen."},{t:"code",title:"Minimal navigation graph",code:`val navController = rememberNavController()
NavHost(navController, startDestination = "home") {
    composable("home") { HomeScreen(onOpen = { navController.navigate("detail/$it") }) }
    composable("detail/{id}") { entry ->
        DetailScreen(id = entry.arguments?.getString("id"))
    }
}`},{t:"list",items:["**`NavController`** — the single owner of the back stack; `navigate`, `popBackStack`, `navigateUp`.","**`NavHost`** — binds the controller to a graph and renders the current destination (each destination is a back-stack entry with its own lifecycle and saved state).","**`composable(route)`** — a destination; routes can carry path (`{id}`) and query (`?q=`) arguments.","**`rememberNavController()`** — creates/remembers the controller; hoist it if multiple composables need it."]},{t:"note",text:"Navigation Compose = NavController (owns the back stack: navigate/popBackStack/navigateUp) + NavHost (maps routes→screens, renders current, each destination a lifecycle-owning back-stack entry) + composable(route){} destinations. Navigate via navController.navigate(route); the NavHost swaps the screen."}]},{level:"junior",q:"How do you navigate back, and control the back stack (popUpTo, launchSingleTop)?",a:[{t:"p",text:"You go back with `navController.popBackStack()` or `navigateUp()`. To shape the stack when navigating forward, `navigate` takes options: `popUpTo` (pop destinations up to a given route before navigating) and `launchSingleTop` (don't create a duplicate if that destination is already on top). These prevent back-stack bloat and duplicate screens."},{t:"code",title:"Back-stack control",code:`// After login, go to home and clear the auth flow so back doesn't return to login:
navController.navigate("home") {
    popUpTo("login") { inclusive = true }   // remove login (and above) from the stack
    launchSingleTop = true                  // avoid a duplicate home on top
}
// Bottom-nav tab switch (avoid stacking tabs):
navController.navigate(route) {
    popUpTo(navController.graph.startDestinationId) { saveState = true }
    launchSingleTop = true
    restoreState = true
}`},{t:"list",items:["**`popBackStack()` / `navigateUp()`** — go back; `navigateUp` respects the nav host's up behavior.","**`popUpTo(route) { inclusive }`** — pop the stack up to (and optionally including) a route before navigating — clearing flows (login) or resetting.","**`launchSingleTop`** — reuse the top instance instead of duplicating (double-tap protection, tabs).","**`saveState`/`restoreState`** — preserve each tab's back stack in bottom navigation."]},{t:"note",text:"Back: popBackStack()/navigateUp(). Shape the forward stack in navigate {}: popUpTo(route){inclusive} to clear a flow (e.g. login), launchSingleTop to avoid duplicates, saveState/restoreState for per-tab back stacks in bottom nav. These stop back-stack bloat and duplicate screens."}]},{level:"junior",q:"How do you return a result from one screen to a previous one in Navigation Compose?",a:[{t:"p",text:"There's no `startActivityForResult` equivalent; instead you use the previous back-stack entry's `SavedStateHandle`. The destination that produced the result writes it into `previousBackStackEntry.savedStateHandle`, and the origin screen observes that key (as state or a flow) after popping back."},{t:"code",title:"Passing a result back",code:`// On the picker screen, set the result then pop:
navController.previousBackStackEntry?.savedStateHandle?.set("picked", selectedId)
navController.popBackStack()

// On the origin screen, observe it:
val entry = navController.currentBackStackEntry
val picked by entry?.savedStateHandle
    ?.getStateFlow<String?>("picked", null)
    ?.collectAsStateWithLifecycle() ?: return`},{t:"list",items:["**Use `SavedStateHandle` of the previous entry** — write on the producer, read on the consumer; it survives config change/process death.","**Observe as a flow/state** — `getStateFlow(key, default)` so you react when the result arrives after `popBackStack`.","**Clear after consuming** — remove the key so it doesn't re-deliver on the next recomposition/return.","**Alternative** — a shared ViewModel scoped to a nav graph for richer cross-screen data (see the shared-ViewModel question)."]},{t:"note",text:"Return results via the previousBackStackEntry.savedStateHandle: producer sets the key then popBackStack(); consumer observes getStateFlow(key) and clears it after reading. Survives config change/process death. For richer sharing, use a ViewModel scoped to a nav graph instead."}]},{level:"junior",q:"How do you set up bottom navigation with Navigation Compose?",a:[{t:"p",text:"Render a `NavigationBar` with items whose selection is driven by the current back-stack entry's route, and navigate on tap with `popUpTo(startDestination){ saveState }` + `restoreState` + `launchSingleTop` so each tab keeps its own back stack and you don't pile up destinations."},{t:"code",title:"Bottom nav wiring",code:`val backStack by navController.currentBackStackEntryAsState()
val current = backStack?.destination
NavigationBar {
    tabs.forEach { tab ->
        NavigationBarItem(
            selected = current?.hierarchy?.any { it.route == tab.route } == true,
            onClick = {
                navController.navigate(tab.route) {
                    popUpTo(navController.graph.findStartDestination().id) { saveState = true }
                    launchSingleTop = true; restoreState = true
                }
            },
            icon = { Icon(tab.icon, null) }, label = { Text(tab.label) },
        )
    }
}`},{t:"list",items:["**Selection from route** — `currentBackStackEntryAsState()` gives the current destination; check the hierarchy so nested destinations still highlight the right tab.","**`saveState`/`restoreState`** — preserve each tab's scroll/back stack across switches.","**`launchSingleTop`** — re-tapping a tab doesn't stack duplicates.","**`popUpTo(startDestination)`** — keeps the back stack shallow (back from any tab goes to start/exit, per your UX)."]},{t:"note",text:"Bottom nav: drive NavigationBarItem selection from currentBackStackEntryAsState() (check destination.hierarchy for nested routes), and navigate with popUpTo(startDestination){saveState=true} + launchSingleTop + restoreState so each tab keeps its own back stack without piling up duplicates."}]},{level:"senior",q:"How do nested navigation graphs work, and when do you use them?",a:[{t:"p",text:"A nested graph (`navigation(startDestination, route) { ... }`) groups related destinations under a parent route — e.g. an onboarding flow or a feature module's screens. It gives that flow its own start destination, a scope for a shared ViewModel, and a clean boundary you can navigate to/pop as a unit."},{t:"code",title:"A nested graph",code:`NavHost(navController, startDestination = "main") {
    composable("main") { MainScreen() }
    navigation(startDestination = "step1", route = "onboarding") {   // nested graph
        composable("step1") { Step1() }
        composable("step2") { Step2() }
    }
}
navController.navigate("onboarding")   // enters at step1`},{t:"list",items:["**Grouping + encapsulation** — a self-contained flow with its own start destination; feature modules expose a nested graph.","**Shared ViewModel scope** — a ViewModel scoped to the graph's back-stack entry is shared across the flow's screens and cleared when the flow is popped.",'**Navigate as a unit** — `navigate("onboarding")` enters the flow; `popUpTo("onboarding") { inclusive = true }` exits the whole flow.',"**Modularization** — each feature contributes its graph (`NavGraphBuilder` extension), keeping the app module thin."]},{t:"note",text:"Nested graphs (navigation(startDestination, route){}) group a flow (onboarding, a feature) with its own start destination, a shared-ViewModel scope (the graph back-stack entry), and unit navigation (enter with navigate(route), exit with popUpTo(route){inclusive}). They're how feature modules contribute self-contained nav graphs."}]},{level:"junior",q:"How do you implement deep links with Navigation Compose?",a:[{t:"p",text:"Add `deepLinks = listOf(navDeepLink { uriPattern = ... })` to a `composable` destination; Navigation matches an incoming `Uri` (from an intent, notification, or App Link) to that destination and extracts arguments from the pattern. You also declare the intent filter in the manifest for external links."},{t:"code",title:"A deep-linkable destination",code:`composable(
    route = "product/{id}",
    arguments = listOf(navArgument("id") { type = NavType.StringType }),
    deepLinks = listOf(navDeepLink { uriPattern = "https://shop.com/product/{id}" }),
) { entry -> ProductScreen(id = entry.arguments?.getString("id")) }
// + an <intent-filter> with the host/scheme in AndroidManifest for external App Links`},{t:"list",items:["**`navDeepLink { uriPattern }`** — maps a URI (with `{arg}` placeholders) to the destination; args are parsed automatically.","**Manifest intent filter** — required for links coming from outside the app (App Links with `autoVerify` for verified https links).","**NavController handling** — `NavController` reads the launching intent and navigates, building the proper back stack.",'**Testing** — `adb shell am start -d "https://shop.com/product/42"` to verify.']},{t:"note",text:'Deep links: add deepLinks = listOf(navDeepLink { uriPattern = "https://…/{id}" }) to the composable destination (args auto-parsed), plus a manifest <intent-filter> (autoVerify for App Links) for external URIs. NavController matches the launching intent and builds the back stack. Test with adb am start -d.'}]},{level:"senior",q:"How do you add screen transition animations in Navigation Compose?",a:[{t:"p",text:"`composable()` accepts `enterTransition`, `exitTransition`, `popEnterTransition`, and `popExitTransition` lambdas (with access to the initial/target states) so you can define slide/fade animations per destination or on the whole `NavHost`. This replaced the older Accompanist Navigation-Animation, which is now built in."},{t:"code",title:"Per-destination transitions",code:`composable(
    "detail/{id}",
    enterTransition = { slideInHorizontally { it } + fadeIn() },
    exitTransition = { slideOutHorizontally { -it } + fadeOut() },
    popEnterTransition = { slideInHorizontally { -it } + fadeIn() },
    popExitTransition = { slideOutHorizontally { it } + fadeOut() },
) { DetailScreen() }`},{t:"list",items:["**Four hooks** — enter/exit for forward navigation, popEnter/popExit for back navigation (so back animates in reverse).","**Set on NavHost or per-destination** — a default on the `NavHost`, overridden per `composable`.","**Access transition scope** — the lambdas expose `initialState`/`targetState` to vary the animation by route.","**Shared elements** — combine with `SharedTransitionLayout` for hero transitions between destinations."]},{t:"note",text:"Set enterTransition/exitTransition/popEnterTransition/popExitTransition on the NavHost or per composable() (slide/fade with access to initial/target states); pop* handle the reverse animation on back. Built-in now (replaced Accompanist Navigation-Animation). Combine with SharedTransitionLayout for hero transitions."}]},{level:"junior",q:"How do you find and interact with composables in a UI test (finders, matchers, actions)?",a:[{t:"p",text:"Compose testing works on the *semantics tree*: you find nodes with finders (`onNodeWithText`, `onNodeWithContentDescription`, `onNodeWithTag`), assert with matchers (`assertIsDisplayed`, `assertTextEquals`, `assertIsEnabled`), and act with actions (`performClick`, `performTextInput`, `performScrollTo`). A `testTag` gives a stable handle independent of visible text."},{t:"code",title:"Find, act, assert",code:`// In the composable: Modifier.testTag("submit")
composeTestRule.onNodeWithTag("emailField").performTextInput("a@b.com")
composeTestRule.onNodeWithTag("submit").performClick()
composeTestRule.onNodeWithText("Welcome").assertIsDisplayed()
composeTestRule.onNodeWithText("Error").assertDoesNotExist()`},{t:"list",items:["**Finders** — `onNodeWithText`, `onNodeWithContentDescription`, `onNodeWithTag`, `onAllNodes...`; add `useUnmergedTree = true` when a merged node hides children.","**Matchers/assertions** — `assertIsDisplayed`, `assertExists`/`assertDoesNotExist`, `assertTextEquals`, `assertIsEnabled`, `assertIsSelected`.","**Actions** — `performClick`, `performTextInput`, `performScrollTo`, `performTouchInput { swipeUp() }`.","**`testTag`** — a stable id for testing that survives copy changes and localization."]},{t:"note",text:"Compose tests operate on the semantics tree: find (onNodeWithText/ContentDescription/Tag, useUnmergedTree for hidden children), assert (assertIsDisplayed/assertTextEquals/assertDoesNotExist), act (performClick/performTextInput/performScrollTo). Use Modifier.testTag for stable handles independent of visible text/locale."}]},{level:"senior",q:"What is testTag and mergeDescendants, and how do semantics merging affect tests?",a:[{t:"p",text:"Compose *merges* semantics of a composable and its descendants into one accessible node when a parent sets `mergeDescendants = true` (buttons, list items do this) — so a whole button reads as one node to accessibility and to tests. This means a child's `testTag` or text can be hidden inside a merged parent; tests use the *merged* tree by default but can request the *unmerged* tree to reach children."},{t:"list",items:["**Merging** — a semantically-merging parent (`Modifier.semantics(mergeDescendants = true)`, or components like `Button`) combines descendant semantics into one node.","**Effect on tests** — `onNodeWithText` on child text inside a merged button finds the merged node; to assert on a specific child, use `useUnmergedTree = true`.","**`testTag`** — by default a tag participates in merging; a child tag may be subsumed. Place tags thoughtfully, or read the unmerged tree.","**Accessibility parallel** — merging is why TalkBack reads a button as one item; good semantics = testable *and* accessible."]},{t:"code",title:"Reaching a merged child",code:'composeTestRule.onNode(hasTestTag("badge"), useUnmergedTree = true).assertIsDisplayed()'},{t:"note",text:"Merging (mergeDescendants=true, as Button/ListItem do) combines a subtree's semantics into one node — good for TalkBack, but it hides child text/tags from the default (merged) test tree. Use useUnmergedTree = true to assert on specific children. Semantics that make tests work also make the app accessible."}]},{level:"senior",q:"How do you provide custom semantics for accessibility and testing?",a:[{t:"p",text:"Use `Modifier.semantics { }` to add accessibility/testing metadata — `contentDescription`, `role`, `stateDescription`, custom actions, or a `testTag` (via `testTagsAsResourceId` for UIAutomator). This makes custom-drawn or gesture-based composables (which have no built-in semantics) both accessible to TalkBack and findable in tests."},{t:"code",title:"Custom semantics on a custom control",code:`Box(
    Modifier
        .pointerInput(Unit) { detectTapGestures { toggle() } }
        .semantics {
            role = Role.Switch
            stateDescription = if (on) "On" else "Off"
            contentDescription = "Wi-Fi"
            // custom action for accessibility services:
            customActions = listOf(CustomAccessibilityAction("Toggle") { toggle(); true })
        }
)`},{t:"list",items:["**`semantics { }`** — attach `contentDescription`, `role`, `stateDescription`, `onClick`/custom actions.","**Why needed** — raw `pointerInput`/`Canvas` composables have no semantics, so TalkBack can't announce them and tests can't find them by role/state.","**`clearAndSetSemantics { }`** — replace a subtree's semantics wholesale (e.g. present a complex graphic as a single labeled element).","**`stateDescription`** — announces on/off/selected changes; keep it in sync with state so accessibility and tests reflect reality."]},{t:"note",text:"Modifier.semantics { } adds contentDescription/role/stateDescription/custom actions to composables that lack built-in semantics (custom pointerInput/Canvas controls) — making them TalkBack-accessible AND test-findable. Use clearAndSetSemantics to present a complex subtree as one labeled node. Keep stateDescription synced with state."}]},{level:"junior",q:"What is createComposeRule vs createAndroidComposeRule, and how do you set content in a test?",a:[{t:"p",text:"`createComposeRule()` hosts composables in a test without a specific Activity — fast, for pure UI/unit-level composable tests. `createAndroidComposeRule<MyActivity>()` launches a real Activity, needed when the composable depends on the Activity (permissions, `LocalContext` specifics, Hilt injection). You call `setContent { }` to render the composable under test."},{t:"code",title:"Setting content",code:`@get:Rule val rule = createComposeRule()

@Test fun showsGreeting() {
    rule.setContent { AppTheme { Greeting(name = "Sam") } }
    rule.onNodeWithText("Hello Sam").assertIsDisplayed()
}
// Activity-backed: @get:Rule val rule = createAndroidComposeRule<MainActivity>()`},{t:"list",items:["**`createComposeRule()`** — no Activity class; lighter and faster; use when the composable is self-contained.","**`createAndroidComposeRule<A>()`** — launches Activity `A`; use for Activity-dependent behavior or when the content is already set by the Activity (then don't call `setContent`).","**`setContent { }`** — render your composable, usually wrapped in `AppTheme` and any test doubles (fake ViewModel).","**Hilt** — `createAndroidComposeRule` + `HiltAndroidRule` for injected dependencies."]},{t:"note",text:"createComposeRule() hosts composables without an Activity (fast, self-contained tests); createAndroidComposeRule<A>() launches a real Activity (for Activity/Context/Hilt-dependent UI). Call rule.setContent { AppTheme { … } } to render — wrap in the theme and pass fakes. Use Hilt rule with the Android variant for DI."}]},{level:"senior",q:"How does test synchronization work in Compose, and how do you handle async content?",a:[{t:"p",text:"Compose tests auto-synchronize with the composition and animation clock: each assertion/action first waits for the app to be *idle* (no pending recomposition or animation). For async work (a coroutine loading data), you either let idling handle it or explicitly wait with `waitUntil { }` for a condition to become true, avoiding flaky `Thread.sleep`."},{t:"code",title:"Waiting for async content",code:`rule.setContent { ProductScreen(viewModel = fakeVmThatLoadsAsync) }
rule.waitUntil(timeoutMillis = 5000) {
    rule.onAllNodesWithTag("product").fetchSemanticsNodes().isNotEmpty()
}
rule.onNodeWithText("Loaded").assertIsDisplayed()`},{t:"list",items:["**Auto-sync** — actions/assertions block until idle (no recomposition/animation pending), so most UI updates are handled automatically.","**`waitUntil { }`** — poll a condition with a timeout for async data; better than sleeping.","**`mainClock` control** — for animations, advance the clock manually (auto-advance off) to test intermediate states.","**Idling resources** — a genuinely-async source not tracked by Compose (an external executor) may need coordination; prefer injecting a test dispatcher so coroutines run deterministically."]},{t:"note",text:"Compose tests auto-wait for idle (no pending recomposition/animation) before each action/assert. For async data use waitUntil { condition } with a timeout (never Thread.sleep); control mainClock for animations; inject a test dispatcher so ViewModel coroutines run deterministically. Idling makes most updates handled automatically."}]},{level:"senior",q:"How do you test a screen that uses a ViewModel and Compose state together?",a:[{t:"p",text:"Inject a *fake* or real-with-fakes ViewModel that exposes controllable state (a `MutableStateFlow` you push values into), set it as the screen's content, and drive the UI by emitting states — asserting the UI reflects each. Keep the ViewModel's coroutines on an injected test dispatcher so state changes are synchronous within the test."},{t:"code",title:"Driving the screen via fake state",code:`val state = MutableStateFlow<UiState>(UiState.Loading)
val fakeVm = FakeVm(state)
rule.setContent { ProductScreen(viewModel = fakeVm) }

rule.onNodeWithTag("spinner").assertIsDisplayed()      // Loading
state.value = UiState.Content(sampleProduct)            // emit new state
rule.onNodeWithText(sampleProduct.name).assertIsDisplayed()  // UI updated`},{t:"list",items:["**Controllable state** — a `MutableStateFlow` in the fake VM lets you script Loading → Content → Error and assert each.","**Assert events** — verify the VM's functions are called on interactions (spy/fake capturing calls) rather than testing the VM's internals here.","**Test dispatcher** — inject `StandardTestDispatcher`/`UnconfinedTestDispatcher` so VM coroutines resolve deterministically.","**Scope** — this is a UI test of the screen; test the ViewModel's logic separately as a unit test."]},{t:"note",text:"Test screens with a fake ViewModel exposing a MutableStateFlow you push states into (Loading→Content→Error), assert the UI per state, and verify VM functions are called on interactions. Inject a test dispatcher for deterministic coroutines. Keep it a UI test — unit-test the VM's logic separately."}]},{level:"junior",q:"How do you embed a Compose UI inside an existing View-based screen (ComposeView)?",a:[{t:"p",text:"Add a `ComposeView` to your XML layout (or create one in code) and call `setContent { }` on it to render Compose inside the View hierarchy. This is the primary interop direction for incremental migration — dropping Compose into a Fragment/Activity that's still View-based."},{t:"code",title:"Compose in a View layout",code:`<!-- in XML -->
<androidx.compose.ui.platform.ComposeView
    android:id="@+id/compose_view" ... />

// in the Fragment/Activity
binding.composeView.apply {
    setViewCompositionStrategy(ViewCompositionStrategy.DisposeOnViewTreeLifecycleDestroyed)
    setContent { AppTheme { MyComposable() } }
}`},{t:"list",items:["**`ComposeView.setContent { }`** — hosts Compose inside Views; use it in Fragments/Activities during migration.","**`ViewCompositionStrategy`** — set `DisposeOnViewTreeLifecycleDestroyed` (Fragments) so the composition is disposed with the view lifecycle (prevents leaks).","**Two-way interop** — Compose→View via `AndroidView`; View→Compose via `ComposeView`.","**Theming** — wrap in your `AppTheme`/`MdcTheme` bridge so the embedded Compose matches the app."]},{t:"note",text:"Embed Compose in Views via ComposeView.setContent { } (Fragments/Activities during migration). Set ViewCompositionStrategy.DisposeOnViewTreeLifecycleDestroyed in Fragments to dispose with the view lifecycle (avoid leaks). Compose→View uses AndroidView; wrap embedded Compose in your theme bridge."}]},{level:"senior",q:"How do you keep a View (like a MapView) in sync with Compose state via AndroidView's update block?",a:[{t:"p",text:"`AndroidView` has a `factory` (creates the View once) and an `update` block (runs on every recomposition where read state changed) to push Compose state into the View's imperative API. You create the View in `factory`, remember any handle, and mutate it in `update` when state changes — bridging the declarative and imperative worlds."},{t:"code",title:"AndroidView factory + update",code:`AndroidView(
    factory = { ctx -> MyChartView(ctx) },     // created once
    update = { view -> view.setData(chartData) }, // re-runs when chartData changes
    modifier = Modifier.fillMaxWidth(),
)`},{t:"list",items:["**`factory`** — called once to construct the View; do expensive setup here.","**`update`** — called after every recomposition that read changed state; push new values into the View (`setData`, `moveCamera`).","**Lifecycle** — for lifecycle-aware Views (MapView), forward lifecycle events (via a `DisposableEffect`/`LifecycleEventObserver`) or use the AndroidView `onRelease`/`onReset` callbacks; otherwise it leaks or goes blank on backgrounding.","**Don't recreate** — keep the View in `factory`; recreating it each recomposition is expensive and loses state."]},{t:"note",text:"AndroidView(factory, update): factory builds the View once; update runs on each recomposition with changed reads to push state into the View's imperative API (setData/moveCamera). For lifecycle Views (MapView), forward lifecycle events (DisposableEffect + observer) and use onRelease, or it leaks/blanks on backgrounding. Never recreate in factory."}]},{level:"senior",q:"How are ViewModels obtained and scoped correctly in Compose (viewModel() and scoping)?",a:[{t:"p",text:"`viewModel()` (or `hiltViewModel()`) returns a ViewModel scoped to the nearest `ViewModelStoreOwner` — by default the host Activity/Fragment, or in Navigation Compose the *nav back-stack entry*. This scoping determines the ViewModel's lifetime: a destination-scoped VM is cleared when that destination leaves the back stack, while an Activity-scoped one lives as long as the Activity."},{t:"list",items:["**`viewModel()`/`hiltViewModel()`** — scoped to the current back-stack entry in a NavHost, so each screen gets its own VM cleared on pop.",'**Graph-scoped sharing** — to share a VM across a flow, scope it to the *parent nav graph* entry: `hiltViewModel(navController.getBackStackEntry("onboarding"))`.',"**Activity-scoped** — pass the Activity as the owner to share across screens (use sparingly; couples lifetimes).","**Don't hoist a VM manually across screens** — rely on the correct `ViewModelStoreOwner` so lifecycle/clearing is handled."]},{t:"code",title:"Sharing a VM across a nav flow",code:`@Composable fun Step2(navController: NavController) {
    val parentEntry = remember(navController) { navController.getBackStackEntry("onboarding") }
    val shared: OnboardingVm = hiltViewModel(parentEntry)   // same instance as Step1
}`},{t:"note",text:`viewModel()/hiltViewModel() scope to the nearest ViewModelStoreOwner — in a NavHost that's the back-stack entry, so each screen's VM clears on pop. Share across a flow by scoping to the parent graph entry: hiltViewModel(navController.getBackStackEntry("graphRoute")). Let the store owner manage lifetime; don't hand-hoist VMs across screens.`}]},{level:"senior",q:"What are the trade-offs of type-safe navigation (Navigation Compose 2.8+) vs string routes?",a:[{t:"p",text:"Type-safe navigation (using `@Serializable` route classes/objects) replaces stringly-typed routes with Kotlin types: you `navigate(ProductRoute(id = 42))` and receive a typed object, with the compiler checking arguments. It removes a whole class of bugs (typos, missing/mis-typed args, manual encoding) at the cost of some boilerplate and being newer."},{t:"code",title:"Type-safe route",code:`@Serializable data class ProductRoute(val id: String)

NavHost(navController, startDestination = HomeRoute) {
    composable<ProductRoute> { entry ->
        val route: ProductRoute = entry.toRoute()   // typed args, no manual parsing
        ProductScreen(route.id)
    }
}
navController.navigate(ProductRoute(id = "42"))       // compile-checked args`},{t:"table",headers:["","Type-safe routes","String routes"],rows:[["Arg safety","compile-time checked","runtime (typos, missing args)"],["Encoding","automatic","manual (URL-encode strings)"],["Refactoring","rename-safe","fragile string edits"],["Complex args","serializable types","must flatten to strings"],["Maturity","newer (2.8+)","long-established, lots of samples"]]},{t:"list",items:["**Type-safe pros** — compile-time argument checking, automatic encoding, safe refactors, real objects instead of string parsing.","**String-route pros** — mature, ubiquitous examples, trivial for deep-link URI patterns.","**Guidance** — prefer type-safe for new apps on recent Nav versions; string routes remain fine and are still how raw deep-link URIs are matched."]},{t:"note",text:"Type-safe nav (@Serializable route classes, composable<Route>, entry.toRoute()) gives compile-checked args, automatic encoding, and safe refactors vs stringly-typed routes' runtime typos/manual encoding. Cost: newer, some boilerplate. Prefer it for new apps on Nav 2.8+; string routes still fine and used for raw deep-link URI patterns."}]},{level:"junior",q:"How do you pass complex or large data between destinations safely?",a:[{t:"p",text:"Pass only small identifiers (an id) in the route and re-fetch the full object on the destination from your repository/ViewModel — don't serialize large objects into navigation arguments. Navigation args go through a `Bundle` (size-limited) and represent app state that must survive process death; passing an id and reloading is robust and cheap."},{t:"list",items:["**Pass an id, load the rest** — the detail screen's ViewModel fetches by id (from cache/DB/network); survives process death because you can always re-fetch.","**Why not the whole object** — `Bundle` has a size limit (TransactionTooLarge risk), objects may be stale, and everything in the arg must be serializable.","**Small primitives are fine** — ids, flags, short strings as path/query args.","**Shared data** — for a genuinely shared object across a flow, a graph-scoped ViewModel holds it instead of routing it."]},{t:"code",title:"Id in, object loaded",code:'navController.navigate("detail/${product.id}")   // pass the id only\n// DetailViewModel(savedStateHandle) reads id, then repo.getProduct(id)'},{t:"note",text:"Pass small ids in the route and re-fetch the full object on the destination (survives process death, no stale/oversized data). Don't serialize large objects into nav args — Bundle size limits (TransactionTooLarge) and staleness. For truly shared objects across a flow, hold them in a graph-scoped ViewModel."}]},{level:"senior",q:"How do you test navigation itself (that tapping navigates to the right destination)?",a:[{t:"p",text:"Set up a `TestNavHostController` in the test, render your `NavHost` with it, perform the UI action, and assert the controller's `currentBackStackEntry.destination.route` matches the expected destination. This tests the wiring (clicks → navigation) without launching real screens' full behavior."},{t:"code",title:"Testing navigation wiring",code:`lateinit var navController: TestNavHostController
rule.setContent {
    navController = TestNavHostController(LocalContext.current).apply {
        navigatorProvider.addNavigator(ComposeNavigator())
    }
    AppNavHost(navController)
}
rule.onNodeWithText("Open product").performClick()
assertEquals("product/{id}", navController.currentBackStackEntry?.destination?.route)`},{t:"list",items:["**`TestNavHostController`** — a controllable NavController for tests; add the `ComposeNavigator`.","**Assert the route** — check `currentBackStackEntry?.destination?.route` (or arguments) after the action.","**Scope** — this verifies the graph/click wiring; screen content is tested separately.","**Alternative** — assert the destination screen's content appears (`onNodeWithText(...).assertIsDisplayed()`) for a more behavioral test."]},{t:"note",text:"Test navigation with a TestNavHostController (add ComposeNavigator): render the NavHost with it, perform the click, then assert currentBackStackEntry?.destination?.route (and arguments). Verifies click→navigation wiring; test screen content separately, or assert the destination content is displayed for a behavioral check."}]},{level:"junior",q:"How do you write a screenshot test for a composable?",a:[{t:"p",text:"Screenshot (snapshot) tests render a composable and compare its rendered image against a stored 'golden' image, failing if pixels differ — catching unintended visual regressions. Tools include Compose Preview Screenshot Testing (official, from previews), Paparazzi (JVM, no device), and Roborazzi (Robolectric-based)."},{t:"list",items:["**Compose Preview Screenshot Testing** — official; turns `@Preview`s into screenshot tests, generates/updates goldens via Gradle tasks.","**Paparazzi** — renders on the JVM (no emulator/device), fast in CI; records and verifies PNGs.","**Roborazzi** — uses Robolectric to capture Compose/View screenshots on the JVM.","**Workflow** — record goldens once, commit them, then CI compares; review diffs on intentional UI changes and re-record.","**Best for** — design-system components and key screens in multiple states (light/dark, large font, empty/error)."]},{t:"note",text:"Screenshot tests render a composable and diff it against a committed golden image to catch visual regressions. Options: official Compose Preview Screenshot Testing (from @Preview), Paparazzi (JVM, no device), Roborazzi (Robolectric). Record goldens, commit, CI compares; re-record on intentional changes. Great for design-system components across states."}]},{level:"junior",q:"How do you show the current destination's title or handle per-screen app bars with Navigation Compose?",a:[{t:"p",text:"Observe the current back-stack entry with `currentBackStackEntryAsState()` and derive the app-bar content (title, actions, whether to show a back arrow) from the current route. You typically keep a single `Scaffold` around the `NavHost` and vary its `topBar` based on the current destination."},{t:"code",title:"Route-driven app bar",code:`val backStack by navController.currentBackStackEntryAsState()
val route = backStack?.destination?.route
Scaffold(topBar = {
    TopAppBar(
        title = { Text(titleFor(route)) },
        navigationIcon = {
            if (navController.previousBackStackEntry != null)
                IconButton(onClick = { navController.navigateUp() }) { Icon(Icons.Default.ArrowBack, null) }
        },
    )
}) { padding -> NavHost(navController, startDestination = "home", Modifier.padding(padding)) { /* … */ } }`},{t:"list",items:["**`currentBackStackEntryAsState()`** — recomposes the app bar when the destination changes.","**Show back arrow conditionally** — only when `previousBackStackEntry != null` (i.e. not the start destination).","**Alternative: per-screen bars** — each screen renders its own `Scaffold`/`TopAppBar` for full control (often cleaner than a giant central `when`).","**Keep it declarative** — derive bar state from the route, don't imperatively mutate a shared bar."]},{t:"note",text:"Drive the app bar from currentBackStackEntryAsState(): derive title/actions from the route and show the back arrow only when previousBackStackEntry != null (navigateUp). Either vary a central Scaffold's topBar by route, or (often cleaner) give each screen its own Scaffold/TopAppBar. Keep bar state derived, not imperatively mutated."}]},{level:"senior",q:"What are common Compose/View interop pitfalls and how do you avoid them?",a:[{t:"p",text:"Interop between Compose and Views works well but has sharp edges around lifecycle, scrolling, and performance. Knowing the common pitfalls saves hours of debugging leaks and blank/janky content during migration."},{t:"list",items:["**Lifecycle leaks with `ComposeView`** — not setting a `ViewCompositionStrategy` in a Fragment leaks the composition; use `DisposeOnViewTreeLifecycleDestroyed`.","**Lifecycle Views in `AndroidView`** — a `MapView`/`WebView` needs its lifecycle forwarded (observe lifecycle in a `DisposableEffect`) or it goes blank/leaks on backgrounding.","**Nested scrolling conflicts** — a Compose scrollable inside a `RecyclerView` (or vice-versa) can fight for touch; use the nested-scroll interop APIs (`rememberNestedScrollInteropConnection`).","**Performance of many small ComposeViews** — hundreds of tiny `ComposeView`s in a `RecyclerView` are costly; prefer migrating the whole list to `LazyColumn`.","**Theming mismatch** — embedded Compose without your theme bridge looks wrong; wrap in `AppTheme`/use the Material theme adapter.","**Recreating Views in `factory`** — keep the View creation in `factory` (once), mutate in `update`; recreating each recomposition is slow and drops state."]},{t:"note",text:"Interop pitfalls: ComposeView leaks without a ViewCompositionStrategy (use DisposeOnViewTreeLifecycleDestroyed); lifecycle Views (MapView/WebView) in AndroidView need lifecycle forwarding or they blank/leak; nested-scroll conflicts need rememberNestedScrollInteropConnection; many tiny ComposeViews in a RecyclerView are slow (migrate the list); wrap embedded Compose in your theme; never recreate Views in factory."}]}],pS={id:"navigation-interop-testing",title:"Navigation, Interop & Testing",tagline:"Navigation Compose & type-safe routes, ViewModel scoping across flows, AndroidView/ComposeView interop, the semantics tree, and Compose UI testing.",tags:["Compose","Navigation","Interop","Semantics","UI Testing"],content:hS,qa:uS},mS={id:"compose",name:"Jetpack Compose (UI)",description:"Android's declarative UI toolkit — from what recomposition really is, through state, layout, lists, effects and performance, to theming, navigation and testing.",topics:[_x,Yx,Xx,tS,nS,rS,dS,pS]},fS=[{heading:"What a coroutine is — a suspendable computation",blocks:[{t:"p",text:"A **coroutine** is a computation that can be **suspended and resumed** without blocking a thread. That's the whole idea. A normal blocking call (like a synchronous network request) *occupies* its thread while waiting — the thread sits idle, unusable, until the result arrives. A coroutine that hits a suspension point instead **releases the thread** while it waits, letting that thread do other work, and resumes later (possibly on a different thread) when the result is ready. You get asynchronous, non-blocking code that *reads* like sequential, blocking code."},{t:"code",title:"Sequential-looking async code",code:`suspend fun loadProfile(id: String): Profile {
    val user = api.getUser(id)          // suspends (doesn't block) until response
    val posts = api.getPosts(id)         // suspends until response
    return Profile(user, posts)          // reads top-to-bottom like blocking code
}`},{t:"list",items:["**Not threads**: a coroutine is much lighter than a thread. Threads are OS resources (each costs ~1MB of stack and kernel scheduling); you can run *millions* of coroutines on a handful of threads because a suspended coroutine consumes almost no resources — it's just an object holding its state.","**The payoff on Android**: the main thread must never block (or the UI freezes / ANR). Coroutines let you write `val data = repository.load()` on the main thread — it *suspends* while loading (freeing the main thread to render) and resumes with the result, no callbacks, no blocking.","**Replaces callback hell and RxJava** for most async work: nested callbacks and complex Rx chains become flat, sequential suspend functions."]}]},{heading:"Suspend functions — the building block",blocks:[{t:"p",text:"A function marked **`suspend`** is one that *can suspend*: it may pause its coroutine at a suspension point and resume later. The keyword is a **compiler contract** — a suspend function can only be called from another suspend function or from within a coroutine. That's because suspension requires machinery the compiler injects (below), which only exists inside a coroutine."},{t:"list",items:["**`suspend` doesn't mean 'runs on a background thread'** — this is the #1 misconception. A suspend function runs on whatever thread its caller's coroutine is on; it just has the *ability* to suspend. Making it main-safe (moving work off the main thread) is a separate concern handled by dispatchers/`withContext`.","**Suspension points** are calls to other suspend functions — `delay()`, `withContext()`, a Retrofit suspend call, `Mutex.lock()`. Between them, code runs normally; *at* them, the coroutine may suspend.","**Calling rule**: `suspend fun` → callable only from a coroutine or another `suspend fun`. To start one from normal code, you launch a coroutine (`scope.launch { }`). This mirrors `@Composable` (callable only from composables) — same 'hidden compiler parameter' idea."]},{t:"note",text:'Interview trap: "Does marking a function `suspend` move it off the main thread?" **No.** It only gives it the *ability to suspend*; it runs on the caller\'s thread. Thread-switching is done with dispatchers (`withContext(Dispatchers.IO)`). Getting this right immediately signals real understanding.'}]},{heading:"How suspension actually works — Continuation Passing Style",blocks:[{t:"p",text:"Under the hood, the compiler transforms every suspend function using **Continuation Passing Style (CPS)** and a **state machine**. This is what interviewers mean by 'how do coroutines work internally':"},{t:"list",items:["**Hidden `Continuation` parameter**: the compiler adds an extra parameter to every suspend function — a `Continuation<T>`, essentially a callback representing 'the rest of the computation after this point'. `suspend fun foo(): T` becomes `fun foo(cont: Continuation<T>): Any?`. Calling `cont.resumeWith(result)` continues where the function left off.","**State machine**: the compiler splits the function body at each suspension point into states (like a `switch`), and generates a class that tracks which state to resume into plus the local variables needed. Each resume enters the `switch` at the right label. This is why a suspend function can 'pause' and 'continue' — it's really a state machine driven by continuation callbacks.","**Suspend or return**: at a suspension point, the callee either returns a value immediately (no suspension needed) or returns a special marker `COROUTINE_SUSPENDED`, signaling the coroutine to suspend. When the awaited result is ready, the continuation is resumed, re-entering the state machine at the saved label.","**So 'suspension' = saving state + returning + resuming later via a callback** — no thread is blocked in between; the thread is free to run other coroutines."]},{t:"code",title:"Roughly what the compiler generates (simplified)",code:`// Your code:
suspend fun load(): Data {
    val a = fetchA()   // suspension point 1
    val b = fetchB(a)  // suspension point 2
    return combine(a, b)
}

// Compiler produces (conceptually) a state machine:
// state 0 -> call fetchA(continuation); may suspend, resume into state 1
// state 1 -> have 'a'; call fetchB(a, continuation); resume into state 2
// state 2 -> have 'b'; return combine(a, b) via continuation.resumeWith(...)`}]},{heading:"suspend vs blocking — the crucial difference",blocks:[{t:"table",headers:["","Blocking (e.g. Thread.sleep)","Suspending (e.g. delay)"],rows:[["Holds the thread while waiting","yes — thread unusable","no — thread freed for other work"],["Cost of many concurrent waits","one thread each (expensive)","cheap — thread shared across coroutines"],["On the main thread","freezes UI / ANR","safe — main thread keeps rendering"],["How it waits","OS blocks the thread","saves state, returns, resumes via callback"]]},{t:"p",text:"`delay(1000)` and `Thread.sleep(1000)` both 'wait a second', but `delay` **suspends** (frees the thread — a thousand coroutines can `delay` on one thread) while `Thread.sleep` **blocks** (occupies the thread — a thousand sleeping tasks need consideration of a thousand threads). This is why coroutines scale so well: waiting is nearly free."}]},{heading:"Sequential by default; concurrency is explicit",blocks:[{t:"p",text:"Inside a coroutine, suspend function calls run **sequentially** — one completes before the next starts, just like normal code. Concurrency is opt-in, which keeps code predictable."},{t:"code",title:"Sequential vs concurrent",code:`// SEQUENTIAL — total time = A + B
suspend fun sequential(): Result {
    val a = fetchA()   // wait for A
    val b = fetchB()   // then wait for B
    return Result(a, b)
}

// CONCURRENT — total time = max(A, B) — start both, then await
suspend fun concurrent(): Result = coroutineScope {
    val a = async { fetchA() }   // starts immediately
    val b = async { fetchB() }   // starts immediately
    Result(a.await(), b.await()) // await both
}`},{t:"list",items:["**Default = sequential**: `val a = fetchA(); val b = fetchB()` waits for A, then B. Simple and correct when B depends on A.","**Explicit concurrency with `async`**: when A and B are independent, `async { }` starts each immediately and `await()` collects results — total time becomes the *slower* of the two, not their sum.","The design philosophy: sequential is the safe default (no accidental races), and you *opt into* concurrency where it helps — the opposite of thread-based code where concurrency is the error-prone default."]}]}],gS=[{level:"junior",q:"What is a coroutine, and how is it different from a thread?",a:[{t:"p",text:"**The concept**: a coroutine is a computation that can be *suspended and resumed* without blocking a thread. When it hits a point where it must wait (a network response, a timer), it releases the thread it was running on — freeing that thread to do other work — and resumes later when the result is ready. This lets you write asynchronous code that reads like straightforward sequential code."},{t:"list",items:["**A thread is a heavy OS resource** — each has its own stack (~1MB) and is scheduled by the operating system. You can only have a limited number before memory and context-switching costs hurt.","**A coroutine is lightweight** — it's essentially an object holding its suspended state, running *on* threads but not owning one. You can run millions of coroutines on a small thread pool because a *suspended* coroutine uses almost no resources.","**Blocking vs suspending is the key**: a blocking task holds its thread while waiting (idle but occupied); a suspending coroutine gives the thread back while waiting. So 1000 coroutines waiting on network can share a few threads, whereas 1000 blocking tasks would need consideration of 1000 threads."]},{t:"p",text:"On Android this matters because the main thread must never block or the UI freezes. Coroutines let you write `val data = repository.load()` right on the main thread — it *suspends* (freeing the main thread to keep rendering) rather than blocking, and resumes with the data."}]},{level:"junior",q:"What does the suspend keyword actually do?",a:[{t:"p",text:"**The concept**: `suspend` marks a function that *can suspend* — pause its coroutine at a suspension point and resume later. It's a compiler contract: a suspend function can only be called from another suspend function or from inside a coroutine, because suspension needs machinery (a hidden `Continuation` parameter) that only exists in a coroutine context."},{t:"p",text:"**The critical thing it does NOT do**: `suspend` does *not* make the function run on a background thread. This is the most common misconception. A suspend function runs on whatever thread its calling coroutine is on — it merely gains the *ability* to suspend at suspension points (calls to other suspend functions like `delay`, `withContext`, or a Retrofit call). If you want the function's work to actually run off the main thread, that's a separate decision made with dispatchers — e.g. wrapping the heavy part in `withContext(Dispatchers.IO)`. So `suspend` = 'this can pause and resume without blocking', not 'this runs elsewhere'."}]},{level:"junior",q:"What is the difference between delay() and Thread.sleep()?",a:[{t:"p",text:"**Both pause for a duration, but in fundamentally different ways.** `Thread.sleep(1000)` **blocks** the thread — the thread is occupied and unusable for that whole second, doing nothing. `delay(1000)` **suspends** the coroutine — it schedules a resume after a second and *releases the thread*, so during that second the thread is free to run other coroutines."},{t:"list",items:["**On the main thread**: `Thread.sleep` on the main thread freezes the UI for that duration (and risks an ANR). `delay` on the main thread is fine — the main thread keeps rendering while the coroutine waits.","**Scalability**: a thousand coroutines all calling `delay` can share a single thread (they're all just suspended, waiting for their resume). A thousand tasks calling `Thread.sleep` would each hold a thread.","**`delay` is a suspend function** (only callable from a coroutine); `Thread.sleep` is a normal blocking call."]},{t:"p",text:"This contrast is the clearest illustration of what 'suspending' means: waiting without blocking. It's why coroutines make waiting nearly free — a suspended coroutine costs almost nothing, while a blocked thread is an expensive resource sitting idle."}]},{level:"junior",q:"How do you run two independent network calls concurrently with coroutines?",a:[{t:"p",text:"**The concept**: by default, suspend calls run *sequentially* — `val a = fetchA(); val b = fetchB()` waits for A to finish before starting B, so the total time is A + B. To run independent calls *concurrently*, you start each with `async` (which begins immediately and returns a `Deferred`) and then `await` both. Total time becomes the *slower* of the two, not the sum."},{t:"code",title:"async for concurrency",code:`suspend fun loadDashboard(): Dashboard = coroutineScope {
    val user = async { api.getUser() }      // starts now
    val feed = async { api.getFeed() }       // starts now, in parallel
    Dashboard(user.await(), feed.await())    // wait for both
}`},{t:"p",text:"The key points: `async` starts the work immediately (not when you call `await`), so both requests are in flight at once; `await()` suspends until that particular result is ready; and you wrap them in `coroutineScope { }` so the function only returns when both complete, and if one fails the other is cancelled (structured concurrency). Use plain sequential calls when B *depends* on A's result; use `async` when they're independent and you want them overlapped."}]},{level:"senior",q:"Explain how coroutine suspension works under the hood (continuations and the state machine).",a:[{t:"p",text:"**The mechanism is Continuation Passing Style (CPS) plus a compiler-generated state machine.** When you write a suspend function, the Kotlin compiler rewrites it substantially:"},{t:"list",items:["**Hidden Continuation parameter**: the compiler adds an extra parameter of type `Continuation<T>` to every suspend function. A `Continuation` is essentially a callback with a `resumeWith(Result<T>)` method representing 'the rest of the computation'. So `suspend fun load(): Data` compiles to something like `fun load(completion: Continuation<Data>): Any?`.","**State machine transformation**: the compiler splits the function body at each suspension point into discrete states and generates a class (a `Continuation` implementation) whose `invokeSuspend` is a big `switch` over a label. Each suspension point gets a label; the class stores which label to resume at, plus the local variables that must survive the suspension.","**Suspend-or-return protocol**: at a suspension point, the called suspend function either returns its result immediately (fast path, no actual suspension) or returns the sentinel `COROUTINE_SUSPENDED`. Returning that sentinel unwinds the call stack — the thread is released. When the awaited work completes, someone calls the continuation's `resumeWith`, which re-invokes the state machine, jumping (via the stored label) to right after the suspension point, with locals restored."]},{t:"p",text:"**Why this matters conceptually**: 'suspension' is not magic threading — it's *saving the computation's state, returning to free the thread, and resuming later via a callback*. There's no blocked thread in between. This also explains real behaviors: locals are stored in the continuation object (so a coroutine has a small heap footprint, not a thread stack); resumption can happen on a different thread (the dispatcher decides); and deeply nested suspend calls form a chain of continuations. When an interviewer asks 'how do coroutines work', this CPS-plus-state-machine explanation is the complete answer."}]},{level:"senior",q:"Why is 'suspend doesn't mean background thread' an important distinction? Give a concrete example.",a:[{t:"p",text:"**Because it's the difference between code that helps and code that still freezes your UI.** `suspend` gives a function the *ability to suspend* at suspension points, but it runs on the caller's thread. If a suspend function does *blocking* or *CPU-heavy* work without a suspension point that switches threads, and it's called from a main-thread coroutine, it runs that heavy work **on the main thread** — freezing the UI exactly as a normal blocking call would."},{t:"code",title:"A suspend function that still blocks the main thread",code:`// BUG: 'suspend' but does blocking IO on whatever thread called it
suspend fun readFileBad(path: String): String {
    return File(path).readText()   // blocking! runs on caller's thread (Main!)
}

// FIX: move the blocking work off-main with a dispatcher
suspend fun readFileGood(path: String): String = withContext(Dispatchers.IO) {
    File(path).readText()          // now runs on an IO thread
}`},{t:"list",items:["The `suspend` keyword on `readFileBad` compiles fine and looks async, but calling it from `viewModelScope.launch { }` (which uses `Dispatchers.Main`) runs `readText()` on the main thread → jank/ANR. The keyword bought nothing here.","**Main-safety is a separate contract**: a well-behaved suspend function is *main-safe* — safe to call from the main thread because it internally switches to the right dispatcher for any blocking/CPU work (`withContext(IO)` for IO, `withContext(Default)` for CPU). Retrofit and Room suspend functions are main-safe because they do this internally.","**The layering rule**: the layer *doing* the blocking work owns the `withContext`, not the caller. So repositories/use-cases wrap their heavy work; ViewModels can then call them from the main thread safely without sprinkling `withContext` everywhere."]},{t:"p",text:"The distinction matters because it's a real, shippable bug: developers assume `suspend` = 'off the main thread', write blocking code in a suspend function, and ship UI jank. The correct mental model — `suspend` = suspendable, dispatchers = threading — prevents it."}]},{level:"senior",q:"Coroutines are described as 'sequential by default'. Why is that a good design, and how do you opt into concurrency safely?",a:[{t:"p",text:"**The design**: inside a coroutine, suspend calls execute one after another — `a()` fully completes before `b()` starts — exactly like ordinary synchronous code. Concurrency is not the default; you explicitly request it. This is a deliberate inversion of thread-based programming, where everything runs concurrently and you must *add* synchronization to make it safe."},{t:"list",items:["**Why sequential-by-default is good**: most bugs in concurrent code come from *accidental* concurrency — two things touching shared state at once, races, ordering assumptions. When the default is sequential, you can't have those bugs unless you deliberately introduce concurrency, and where you do, it's visible and localized. Code is predictable and reads top-to-bottom.","**Opting in with `async`**: for independent work, `async { }` starts a child coroutine immediately and returns a `Deferred<T>`; `await()` gets its result. `val a = async{...}; val b = async{...}; use(a.await(), b.await())` runs both concurrently, taking `max` time instead of the `sum`.","**Doing it *safely* means using structured concurrency**: wrap the `async` calls in `coroutineScope { }` (or launch them in a proper scope). Then the enclosing function won't return until all children complete, and — critically — if one child fails, the others are cancelled and the exception propagates, rather than leaking orphaned coroutines. Naked `GlobalScope.async` opts out of that safety and is an anti-pattern."]},{t:"code",title:"Safe concurrency = async inside a structured scope",code:`suspend fun load(): Screen = coroutineScope {   // structured boundary
    val a = async { repo.getA() }
    val b = async { repo.getB() }
    Screen(a.await(), b.await())
    // if getA() throws, getB() is cancelled and coroutineScope rethrows
}`},{t:"p",text:"So the senior framing: sequential default eliminates whole classes of concurrency bugs and makes async code readable; when you need parallelism you add `async` *inside a structured scope*, which gives you concurrency *with* automatic cancellation and error propagation — parallel speed without orphaned work or swallowed failures."}]},{level:"junior",q:"What is the core vocabulary of coroutines (suspend, builder, scope, context, job, dispatcher)?",a:[{t:"p",text:"Coroutines have a small set of interlocking concepts. Understanding how they relate demystifies the whole library: a *suspend function* is work that can pause; a *builder* starts a coroutine; a *scope* defines where coroutines live and when they're cancelled; a *context* carries configuration; a *job* is the handle to a running coroutine; a *dispatcher* decides which thread runs it."},{t:"list",items:["**`suspend` function** — a function that can pause and resume without blocking a thread.","**Builder** — `launch`, `async`, `runBlocking` — starts a coroutine from a scope.","**`CoroutineScope`** — defines a lifecycle boundary; cancelling the scope cancels its coroutines (structured concurrency).","**`CoroutineContext`** — an indexed set of elements (Job, Dispatcher, name, exception handler) that configures a coroutine.","**`Job`** — the handle: lets you `cancel()`, `join()`, and observe state; forms the parent-child tree.","**Dispatcher** — a context element deciding the thread pool (`Main`, `IO`, `Default`)."]},{t:"note",text:"Vocabulary: suspend fn (pausable work) → builder (launch/async) starts it in a scope (lifecycle boundary) using a context (Job + Dispatcher + name + handler); the Job is the handle (cancel/join) forming the parent-child tree, and the Dispatcher picks the thread. These six concepts underlie everything."}]},{level:"junior",q:"What is a suspending function, and how do you call one?",a:[{t:"p",text:"A suspending function (marked `suspend`) is one that may pause execution partway through and resume later, without blocking the thread it ran on. Because pausing requires coroutine machinery, a suspend function can only be called from another suspend function or from inside a coroutine builder — not from ordinary code."},{t:"code",title:"Declaring and calling suspend functions",code:`suspend fun fetchUser(id: String): User { /* may suspend on I/O */ }

// Called from another suspend function — fine:
suspend fun loadProfile(id: String): Profile {
    val user = fetchUser(id)   // suspends here, thread not blocked
    return Profile(user)
}

// From non-suspend code, launch a coroutine first:
viewModelScope.launch { val u = fetchUser(id) }   // builder provides the coroutine`},{t:"list",items:["**`suspend` marks pausable work** — the compiler transforms it so it can suspend/resume.","**Call site rule** — only from another `suspend` function or a coroutine builder; the compiler enforces this.","**No thread blocked while suspended** — that's the whole benefit over a blocking call.","**It looks synchronous** — you write sequential code; suspension is invisible in the source."]},{t:"note",text:"A suspend function can pause and resume without blocking its thread. Call it only from another suspend function or a coroutine builder (launch/async/runBlocking) — the compiler enforces this. It reads like sequential code; the suspension is handled by the compiler-generated state machine."}]},{level:"junior",q:"How do coroutines improve on callbacks and 'callback hell'?",a:[{t:"p",text:"Callback-based async code nests deeply (a callback inside a callback inside a callback) and scatters error handling and cancellation across levels — the infamous 'callback hell'. Coroutines let you write asynchronous code in a straight, sequential style: each suspending call reads like a normal function call, so the logic is linear and readable, with normal `try/catch` and structured cancellation."},{t:"code",title:"Callbacks vs coroutines",code:`// Callback hell
getUser(id) { user ->
    getPosts(user) { posts ->
        getComments(posts) { comments -> show(comments) }   // nested, hard to error-handle
    }
}
// Coroutines: linear
suspend fun load(id: String) {
    val user = getUser(id)
    val posts = getPosts(user)
    val comments = getComments(posts)   // sequential, try/catch works normally
    show(comments)
}`},{t:"list",items:["**Linear code** — no nesting; the happy path reads top-to-bottom.","**Normal error handling** — a single `try/catch` wraps the sequence instead of per-callback handling.","**Structured cancellation** — leaving the scope cancels the whole chain; callbacks have no built-in cancellation.","**Composability** — suspend functions call suspend functions, building complex flows from simple pieces."]},{t:"note",text:"Coroutines turn nested callbacks into linear, sequential code: each suspending call reads like a normal call, so logic flows top-to-bottom with ordinary try/catch and structured cancellation — eliminating callback hell's nesting, scattered error handling, and manual cancellation."}]},{level:"junior",q:"How do coroutines compare to RxJava?",a:[{t:"p",text:"Both handle asynchronous work, but coroutines are a *language feature* for sequential-looking async code, while RxJava is a *library* built around observable streams and operators. For one-shot async calls, coroutines are simpler; for complex event/stream processing, RxJava's operators are powerful — though Kotlin `Flow` now covers most streaming needs on top of coroutines."},{t:"table",headers:["","Coroutines (+Flow)","RxJava"],rows:[["Nature","language feature","library"],["One-shot async","`suspend` fun (simple)","Single/Completable (heavier)"],["Streams","`Flow`","Observable/Flowable"],["Cancellation","structured, automatic","manual (dispose)"],["Learning curve","sequential, familiar","many operators"],["Backpressure","suspension-based","explicit strategies"]]},{t:"list",items:["**Coroutines** — sequential style, structured concurrency (auto-cancellation), lighter for simple async; Kotlin-first.","**RxJava** — rich operator ecosystem for complex stream transformations; explicit backpressure; but manual disposal and a steeper curve.","**Flow** — brings Rx-like streaming to coroutines with cold streams and operators; most new Android code uses coroutines + Flow.","**Migration** — many apps moved from RxJava to coroutines/Flow for simpler cancellation and less boilerplate."]},{t:"note",text:"Coroutines = language feature for sequential async with structured (automatic) cancellation; RxJava = library of observable streams/operators with manual disposal and backpressure strategies. Coroutines are simpler for one-shot calls; Flow covers streaming. New Android code favors coroutines + Flow over RxJava."}]},{level:"junior",q:"What does it mean that coroutines are 'lightweight', and how many can you run?",a:[{t:"p",text:"Coroutines are lightweight because they're *not* backed one-to-one by OS threads — many coroutines multiplex onto a small pool of threads. A suspended coroutine holds no thread; it's just a small object (its state machine) parked in memory. So you can run hundreds of thousands of coroutines where the equivalent number of threads would exhaust memory and crash."},{t:"code",title:"100k coroutines vs 100k threads",code:`runBlocking {
    repeat(100_000) {
        launch { delay(1000); print(".") }   // fine — coroutines are cheap
    }
}
// repeat(100_000) { thread { Thread.sleep(1000) } }  // OutOfMemoryError`},{t:"list",items:["**No 1:1 thread mapping** — coroutines suspend and yield their thread; many share a few threads.","**Cheap to create** — a coroutine is a small object, not a ~1MB-stack thread.","**Suspended = free** — a waiting coroutine consumes no thread, unlike a blocked thread.","**Practical impact** — you can freely launch a coroutine per item/request without worrying about thread limits."]},{t:"note",text:"Coroutines are lightweight because they multiplex onto a few threads instead of mapping 1:1 — a suspended coroutine holds no thread, just a small state object. You can run hundreds of thousands (vs threads, which OOM in the thousands). Launch freely per item/request."}]},{level:"junior",q:"What is the difference between concurrency and parallelism in coroutines?",a:[{t:"p",text:"Concurrency is *dealing with* many tasks by interleaving them (making progress on multiple things, possibly on one thread); parallelism is *literally running* tasks at the same instant on multiple cores. Coroutines give you concurrency by default (suspension lets many tasks interleave on few threads); parallelism additionally requires a multi-threaded dispatcher."},{t:"list",items:["**Concurrency** — structure that lets tasks make progress independently; achievable on a single thread via suspension (e.g. many network waits interleaved).","**Parallelism** — simultaneous execution on multiple CPU cores; needs a multi-threaded dispatcher like `Dispatchers.Default`.","**Coroutines default to concurrency** — `async` two calls and they run concurrently; whether they run in *parallel* depends on the dispatcher's thread count.","**CPU vs I/O** — parallelism helps CPU-bound work (use `Default`); concurrency alone suffices for I/O-bound waiting (`IO` lets many suspend)."]},{t:"note",text:"Concurrency = interleaving many tasks (possible on one thread via suspension); parallelism = truly simultaneous on multiple cores (needs a multi-threaded dispatcher). Coroutines give concurrency by default; async two I/O calls interleave regardless, but CPU-bound parallelism needs Dispatchers.Default."}]},{level:"junior",q:"What is a Deferred, and how does await() work?",a:[{t:"p",text:"`Deferred<T>` is the result handle returned by `async` — a coroutine that will eventually produce a value of type `T`. You get that value by calling `await()`, a suspend function that pauses until the async coroutine completes and returns its result (or rethrows its exception). It's the coroutine equivalent of a `Future`/`Promise`."},{t:"code",title:"Deferred and await",code:`coroutineScope {
    val userDeferred: Deferred<User> = async { fetchUser() }
    val postsDeferred: Deferred<List<Post>> = async { fetchPosts() }
    // both run concurrently; await when you need the values
    val user = userDeferred.await()
    val posts = postsDeferred.await()
    show(user, posts)
}`},{t:"list",items:["**`Deferred<T>`** — a `Job` that also carries a result; returned by `async`.","**`await()`** — suspends until the value is ready; rethrows any exception the async coroutine threw.","**Concurrency** — start multiple `async`s, then `await` them, to run work in parallel and combine results.","**Don't `await` immediately** — `async { }.await()` in sequence defeats concurrency; start all, then await."]},{t:"note",text:"Deferred<T> is async's result handle (a Job with a value — like a Future). await() suspends until it's ready and rethrows its exception. Start several async blocks, then await them, to run concurrently and combine results — but don't await each immediately, or you serialize the work."}]},{level:"junior",q:"What happens if you call a suspend function without a coroutine, and how do you start one?",a:[{t:"p",text:"You can't — it won't compile. A suspend function requires a suspending context, so calling it from ordinary (non-suspend) code is a compile error: 'Suspend function can only be called within a coroutine body.' To start from regular code (a click handler, `onCreate`), use a coroutine builder from an appropriate scope."},{t:"code",title:"Starting a coroutine from normal code",code:`// ERROR: can't call suspend fun here
fun onButtonClick() { fetchUser(id) }   // won't compile

// FIX: launch from a scope
fun onButtonClick() {
    viewModelScope.launch { val u = fetchUser(id) }   // or lifecycleScope in UI
}`},{t:"list",items:["**Compile error, not runtime** — the compiler stops you, which is good.","**Use a builder** — `launch`/`async` from a scope (`viewModelScope`, `lifecycleScope`), or `runBlocking` in a `main`/test.","**Pick the right scope** — one tied to the correct lifecycle so the coroutine is cancelled appropriately.","**Not `runBlocking` in production UI** — it blocks the thread; reserve it for `main`/tests."]},{t:"note",text:"Calling a suspend function from non-suspend code is a compile error. Start one with a builder from the right scope: viewModelScope/lifecycleScope.launch { } in Android, runBlocking only in main/tests. Choosing a lifecycle-appropriate scope ensures the coroutine cancels when it should."}]},{level:"senior",q:"How do you convert a callback-based API into a suspend function?",a:[{t:"p",text:"Use `suspendCancellableCoroutine { continuation -> }` — it suspends the coroutine and gives you a `Continuation` you resume when the callback fires. Register the callback inside, call `continuation.resume(value)` on success or `resumeWithException(e)` on failure, and use `invokeOnCancellation { }` to unregister/clean up if the coroutine is cancelled."},{t:"code",title:"Wrapping a callback API",code:`suspend fun getLocation(): Location = suspendCancellableCoroutine { cont ->
    val callback = object : LocationCallback() {
        override fun onResult(loc: Location) { cont.resume(loc) }
        override fun onError(e: Exception) { cont.resumeWithException(e) }
    }
    locationClient.request(callback)
    cont.invokeOnCancellation { locationClient.remove(callback) }   // cleanup on cancel
}`},{t:"list",items:["**`suspendCancellableCoroutine`** — bridges a one-shot callback to a suspend function; prefer over `suspendCoroutine` because it supports cancellation.","**Resume once** — call `resume`/`resumeWithException` exactly once; resuming twice throws.","**`invokeOnCancellation`** — unregister the callback / cancel the underlying request when the coroutine is cancelled (prevents leaks).","**For streams of callbacks** — use `callbackFlow` instead (multiple emissions), not `suspendCancellableCoroutine` (single result)."]},{t:"note",text:"Wrap a one-shot callback with suspendCancellableCoroutine { cont -> }: register the callback, cont.resume(value)/resumeWithException(e) exactly once, and invokeOnCancellation { } to unregister on cancel. Prefer it over suspendCoroutine (cancellation support). For multi-emission callbacks, use callbackFlow instead."}]},{level:"junior",q:"What is 'main-safety', and whose responsibility is it?",a:[{t:"p",text:"Main-safety means a function is safe to call from the main (UI) thread — it won't block it, even if it does I/O or heavy computation, because it moves that work to a background dispatcher internally. The convention is that *suspend functions should be main-safe*: the caller shouldn't have to know or care which thread to use."},{t:"code",title:"A main-safe repository function",code:`class UserRepository(private val io: CoroutineDispatcher = Dispatchers.IO) {
    // Main-safe: caller can invoke from the main thread freely
    suspend fun getUser(id: String): User = withContext(io) {
        api.fetchUser(id)   // heavy I/O runs on IO, not the caller's thread
    }
}`},{t:"list",items:["**Suspend functions should be main-safe** — internally switch to `IO`/`Default` for blocking/CPU work so callers needn't manage threads.","**Push `withContext` down** — the *repository/data layer* owns the dispatcher choice, not the ViewModel or UI.","**Benefit** — the ViewModel just calls `repo.getUser(id)` from `viewModelScope` (Main) without wrapping it.","**Room/Retrofit are already main-safe** — their generated suspend functions handle the dispatcher, so don't double-wrap them."]},{t:"note",text:"Main-safety = a function is safe to call from the main thread because it internally moves blocking/CPU work to IO/Default. Convention: suspend functions are main-safe, with withContext pushed down into the repository/data layer — so the ViewModel just calls it. Room/Retrofit suspend functions are already main-safe."}]},{level:"senior",q:"Does a coroutine run on a single thread, or can it change threads?",a:[{t:"p",text:"A coroutine is not bound to one thread — it can resume on a *different* thread than it suspended on, and `withContext` deliberately moves it between dispatchers. This is a key difference from thread-based code: you can't rely on thread-local state or assume 'same thread throughout'. The dispatcher decides which thread runs each resumed continuation."},{t:"list",items:["**Thread-hopping on resume** — after a suspension point, the coroutine may continue on another thread from the dispatcher's pool.","**`withContext` switches deliberately** — the code before and after runs on different dispatchers/threads.","**Avoid thread-locals** — they don't reliably survive suspension; use `CoroutineContext` elements (`ThreadLocal.asContextElement()`) if you truly need per-coroutine data.","**`Dispatchers.Main` / single-thread confinement** — pin a coroutine to one thread when needed (e.g. UI updates, non-thread-safe libraries)."]},{t:"code",title:"Thread changes across withContext",code:`suspend fun work() {
    // runs on caller's dispatcher (e.g. Main)
    val data = withContext(Dispatchers.IO) { loadFromDisk() }   // now on an IO thread
    updateUi(data)   // back on Main
}`},{t:"note",text:"A coroutine isn't pinned to one thread — it can resume on a different thread after a suspension point, and withContext moves it between dispatchers on purpose. So don't rely on thread-locals or 'same thread throughout'. Use single-thread confinement (Dispatchers.Main / a single-thread dispatcher) when you truly need thread affinity."}]},{level:"junior",q:"How do you launch a coroutine from a non-suspend context like a click handler?",a:[{t:"p",text:"Call a coroutine builder (`launch`) on an appropriate `CoroutineScope`. In Android, that's typically `viewModelScope` (in a ViewModel) or `lifecycleScope` (in an Activity/Fragment) — scopes that cancel automatically when their owner is destroyed, so the coroutine doesn't outlive the screen."},{t:"code",title:"Launching from callbacks",code:`// In a ViewModel:
fun onRefresh() {
    viewModelScope.launch {
        _state.value = Loading
        _state.value = Content(repo.load())   // suspend call inside the coroutine
    }
}
// In an Activity/Fragment for UI-lifecycle work:
button.setOnClickListener { lifecycleScope.launch { doWork() } }`},{t:"list",items:["**`viewModelScope`** — for business/data work; cancelled when the ViewModel is cleared (survives config change).","**`lifecycleScope`** — for UI-lifecycle work; cancelled with the Activity/Fragment.","**Don't use `GlobalScope`** — it isn't lifecycle-bound and leaks.","**`launch` vs `async`** — use `launch` for fire-and-forget; `async` only when you need a returned value to `await`."]},{t:"note",text:"From a click handler, call launch on a lifecycle-bound scope: viewModelScope (business/data, survives config change) or lifecycleScope (UI-lifecycle work). Both auto-cancel with their owner. Avoid GlobalScope (leaks). Use launch for fire-and-forget, async only when you need a value."}]},{level:"junior",q:"What is a coroutine builder, and what are the main ones?",a:[{t:"p",text:"A coroutine builder is a function that *creates and starts* a coroutine. The three foundational ones are `launch` (fire-and-forget, returns a `Job`), `async` (returns a `Deferred<T>` result), and `runBlocking` (bridges blocking code to coroutines by blocking the current thread until done). There are also stream builders like `produce` (Channel) and `flow`."},{t:"table",headers:["Builder","Returns","Use for"],rows:[["`launch`","`Job`","fire-and-forget work"],["`async`","`Deferred<T>`","concurrent work with a result"],["`runBlocking`","`T` (blocks)","main()/tests bridging blocking↔suspend"],["`produce`","`ReceiveChannel<T>`","streaming values via a channel"],["`withContext`","`T`","switch context (not really a 'builder')"]]},{t:"list",items:["**`launch`** — starts a coroutine, returns a `Job` to cancel/join; doesn't return a result.","**`async`** — starts a coroutine that yields a value via `await()`; use for concurrency.","**`runBlocking`** — blocks the calling thread until the coroutine finishes; only for `main`/tests, never on the UI thread.","**`launch`/`async` are extensions on `CoroutineScope`** — they need a scope, enforcing structured concurrency."]},{t:"note",text:"Builders create+start coroutines: launch (Job, fire-and-forget), async (Deferred<T>, concurrent result), runBlocking (blocks the thread — main()/tests only), produce (ReceiveChannel stream). launch/async are CoroutineScope extensions, so they require a scope — enforcing structured concurrency."}]},{level:"junior",q:"What is the coroutine lifecycle (its states)?",a:[{t:"p",text:"A coroutine (via its `Job`) moves through well-defined states: *New* (created, not started), *Active* (running or suspended), *Completing* (waiting for children), *Completed* (finished), *Cancelling*, and *Cancelled*. Knowing these explains behaviors like why a parent waits for children, and how `isActive`/`isCancelled`/`isCompleted` reflect the state."},{t:"list",items:["**New** — created with `start = LAZY`, not yet running (starts on `start()`/`await()`).","**Active** — the normal running/suspended state after launch.","**Completing** — the coroutine's own code finished, but it waits for child coroutines to complete (structured concurrency).","**Completed** — fully done, including children.","**Cancelling → Cancelled** — after `cancel()` (or a failure), it winds down (running `finally`/cleanup) then reaches Cancelled."]},{t:"code",title:"Observing state",code:`val job = scope.launch { work() }
job.isActive      // true while running/suspended
job.cancel()      // -> Cancelling -> Cancelled
job.isCancelled   // true
job.isCompleted   // true once fully done (completed OR cancelled)`},{t:"note",text:"Job states: New (lazy, not started) → Active (running/suspended) → Completing (waiting for children) → Completed; or Cancelling → Cancelled after cancel()/failure. 'Completing' (waiting on children) is why a parent doesn't finish before its children — the essence of structured concurrency. Query via isActive/isCancelled/isCompleted."}]},{level:"junior",q:"What is a suspension point, and what is yield()?",a:[{t:"p",text:"A suspension point is a place in a coroutine where it *may* pause — every call to a suspend function is a potential suspension point. `yield()` is a suspend function that voluntarily gives up the thread at that point, letting other coroutines run and also acting as a cancellation check — useful in CPU-bound loops to stay cooperative."},{t:"code",title:"yield() in a loop",code:`suspend fun crunch(items: List<Int>) {
    for (item in items) {
        process(item)
        yield()   // suspension point: lets others run + checks for cancellation
    }
}`},{t:"list",items:["**Suspension point** — any `suspend` call; the coroutine can pause here and resume later, possibly on another thread.","**`yield()`** — voluntarily suspends briefly, allowing other coroutines on the dispatcher to progress (fairness).","**Cancellation check** — `yield()` (and other suspend calls) throw `CancellationException` if the coroutine was cancelled, making tight loops cancellable.","**CPU-bound loops** — a loop with no suspension points isn't cancellable and can starve others; add `yield()` or check `isActive`."]},{t:"note",text:"A suspension point is anywhere a coroutine may pause — every suspend call is one. yield() voluntarily suspends to let other coroutines run (fairness) and checks for cancellation (throws if cancelled). Add it to tight CPU-bound loops that otherwise have no suspension points, so they stay cancellable and don't starve the dispatcher."}]},{level:"junior",q:"Do suspend functions throw exceptions like normal functions?",a:[{t:"p",text:"Yes — a suspend function throws exceptions just like a regular function, and you catch them with an ordinary `try/catch` at the call site. This is a major ergonomic win over callbacks: error handling is synchronous-looking. (The subtlety is with `launch`/`async`, where exceptions propagate through the coroutine's job — but a direct suspend call in your code throws normally.)"},{t:"code",title:"try/catch around suspend calls",code:`suspend fun load(id: String): Result<User> = try {
    Result.success(api.fetchUser(id))   // suspend call may throw
} catch (e: IOException) {
    Result.failure(e)                    // caught like any exception
}`},{t:"list",items:["**Normal `try/catch`** — wraps a sequence of suspend calls; the failing call throws at that point.","**Sequential semantics** — the exception unwinds the suspend call stack like a synchronous one.","**`launch`/`async` differ** — an uncaught exception in `launch` propagates to the parent job (and handler); `async` holds it until `await()`. But your own suspend function throws normally.","**`CancellationException`** — is special: don't swallow it in a broad `catch` (it signals cancellation); rethrow it."]},{t:"note",text:"Yes — suspend functions throw like normal functions; catch with ordinary try/catch at the call site (synchronous-looking error handling). The nuance is builders: launch propagates to the parent job, async defers to await(). And never swallow CancellationException in a broad catch — rethrow it."}]},{level:"junior",q:"What is coroutineContext, accessible inside a coroutine?",a:[{t:"p",text:"Inside any suspend function or coroutine you can access the `coroutineContext` property — the set of context elements (Job, dispatcher, name, exception handler) the coroutine is running with. It lets you read the current Job (`coroutineContext[Job]`), check `isActive`, or inspect the dispatcher, without passing anything explicitly."},{t:"code",title:"Reading the current context",code:`suspend fun work() {
    val job = coroutineContext[Job]              // the current Job
    ensureActive()                                // throws if cancelled (uses context)
    coroutineContext.isActive                     // convenience for the current Job's state
    val name = coroutineContext[CoroutineName]    // if set
}`},{t:"list",items:["**`coroutineContext`** — the ambient context available in suspend scope; indexed by element keys.","**Common reads** — `[Job]`, `[CoroutineDispatcher]`, `[CoroutineName]`, `[CoroutineExceptionHandler]`.","**`ensureActive()` / `isActive`** — use it for cooperative cancellation in loops.","**Inherited** — child coroutines inherit the parent's context (with overrides), which is how dispatcher/Job propagate."]},{t:"note",text:"coroutineContext is the ambient context (Job, dispatcher, name, handler) readable inside any suspend function — coroutineContext[Job], isActive, ensureActive() for cancellation checks. Children inherit it (with overrides), which is how dispatcher and Job propagate down the tree."}]},{level:"junior",q:"What is the difference between Job.join() and Deferred.await()?",a:[{t:"p",text:"Both suspend until a coroutine finishes, but `join()` (on a `Job` from `launch`) just *waits* — it returns `Unit` and doesn't give a result or rethrow the exception into your code the same way; `await()` (on a `Deferred` from `async`) waits *and returns the value*, rethrowing any exception the coroutine threw."},{t:"list",items:["**`join()`** — waits for completion; returns `Unit`. A failure in a `launch` coroutine propagates to its parent (not rethrown by `join`).","**`await()`** — waits and returns `T`; rethrows the coroutine's exception at the `await` call site.","**Choosing** — use `launch` + `join` when you only need to wait; `async` + `await` when you need the result.","**Both are cancellable suspends** — cancelling the caller cancels the wait."]},{t:"code",title:"join vs await",code:`val job = launch { doWork() }
job.join()                       // just wait, no result

val deferred = async { compute() }
val result = deferred.await()    // wait AND get the value (rethrows on failure)`},{t:"note",text:"join() (Job/launch) waits for completion, returns Unit, and a launch failure propagates to the parent. await() (Deferred/async) waits AND returns the value, rethrowing the coroutine's exception at the call site. Use launch+join to just wait; async+await when you need the result."}]},{level:"junior",q:"How do coroutines improve on AsyncTask and Thread+Handler?",a:[{t:"p",text:"The old async patterns — `AsyncTask`, `Thread` + `Handler`, `ExecutorService` — were verbose, leak-prone, and awkward to cancel or compose. `AsyncTask` is deprecated for exactly these reasons. Coroutines replace them with concise sequential code, structured cancellation tied to lifecycle, easy thread switching, and composability."},{t:"list",items:["**Less boilerplate** — no `doInBackground`/`onPostExecute` ceremony; just suspend functions.","**Lifecycle-safe cancellation** — `viewModelScope`/`lifecycleScope` cancel automatically; `AsyncTask` famously leaked the Activity.","**Easy threading** — `withContext(IO)` vs juggling a `Handler` and background thread.","**Composability** — chain and combine suspend functions; `AsyncTask` didn't compose.","**Result + error handling** — normal return values and `try/catch` vs callback plumbing."]},{t:"note",text:"Coroutines replace AsyncTask (deprecated) and Thread+Handler with concise sequential code, lifecycle-bound automatic cancellation (no Activity leaks), easy thread switching via withContext, composability, and normal return/try-catch error handling — eliminating the boilerplate and leak-prone plumbing of the old patterns."}]},{level:"senior",q:"How does a suspended coroutine free its thread, conceptually?",a:[{t:"p",text:"When a coroutine hits a suspension point and actually suspends (e.g. waiting on I/O or a `delay`), the compiler-generated state machine *saves its progress* (local variables and the resume position) into a continuation object and *returns* the thread to the dispatcher's pool. The thread is now free to run other coroutines. When the awaited event completes, the continuation is dispatched to resume — possibly on a different thread."},{t:"list",items:["**Suspend = save & return** — state is captured in a continuation; the thread is released, not blocked.","**No blocked thread** — unlike `Thread.sleep`, a suspended coroutine holds no thread, so the pool serves many coroutines.","**Resume = re-dispatch** — when ready, the continuation is scheduled onto a dispatcher thread to continue from the saved point.","**This is why coroutines scale** — thousands can 'wait' concurrently on a handful of threads."]},{t:"note",text:"On suspension, the state machine saves the coroutine's locals + resume position into a continuation and returns the thread to the pool (freed, not blocked) — so other coroutines run on it. When the awaited event fires, the continuation is re-dispatched to resume (maybe on another thread). That's why thousands can wait on a few threads."}]},{level:"junior",q:"How do you structure a repository suspend function that does I/O?",a:[{t:"p",text:"A well-structured repository function is a *main-safe* suspend function: it declares `suspend`, moves blocking work to an injected `IO` dispatcher (unless the underlying library is already main-safe), returns a domain result, and lets exceptions propagate (or wraps them in a `Result`). The caller (ViewModel) just launches it from `viewModelScope`."},{t:"code",title:"A clean repository function",code:`class UserRepository(
    private val api: Api,
    private val dao: UserDao,
    private val io: CoroutineDispatcher = Dispatchers.IO,
) {
    suspend fun refreshUser(id: String): User = withContext(io) {
        val user = api.getUser(id)   // Retrofit suspend fn is already main-safe; withContext for any extra blocking work
        dao.insert(user)             // Room suspend fn
        user
    }
}`},{t:"list",items:["**`suspend` + main-safe** — internally handle threading so the ViewModel needn't.","**Inject the dispatcher** — a constructor default of `Dispatchers.IO`, overridable in tests with a test dispatcher.","**Don't double-wrap** — Retrofit/Room suspend functions already switch threads; `withContext(io)` is for your own blocking code.","**Return domain types / `Result`** — map data-layer models to domain, and decide errors-as-exceptions vs errors-as-values at this boundary."]},{t:"note",text:"Repository function = main-safe suspend fun with an injected IO dispatcher (default Dispatchers.IO, swappable in tests), returning domain types (or Result). Use withContext(io) for your own blocking code, but don't double-wrap already-main-safe Retrofit/Room suspend calls. The ViewModel just launches it from viewModelScope."}]},{level:"senior",q:"What are structured concurrency's guarantees at a glance, and why do they matter for basics?",a:[{t:"p",text:"Structured concurrency means every coroutine has a parent scope, and the scope doesn't complete until all its children do — and cancelling the scope cancels all children. Even as a 'basics' concept it's transformative: it makes coroutines *leak-free by construction* (no orphaned background work) and gives predictable cancellation and error propagation."},{t:"list",items:["**Parent waits for children** — a `coroutineScope { }` returns only after all launched children finish.","**Cancellation cascades** — cancelling the parent cancels every child.","**Errors propagate** — a child failure cancels siblings and surfaces to the parent (unless a `SupervisorJob` isolates them).","**No leaks** — you can't accidentally start work that outlives its scope, unlike `GlobalScope`/raw threads.","**Why it matters early** — it changes how you think: coroutines are owned by scopes, not free-floating, so lifecycle-bound scopes (`viewModelScope`) automatically clean up."]},{t:"note",text:"Structured concurrency: every coroutine has a parent scope that won't complete until its children do, cancelling the scope cancels all children, and child errors propagate up. Result: leak-free-by-construction, predictable cancellation and error handling. It's why lifecycle scopes (viewModelScope) auto-clean-up — coroutines are owned, not free-floating."}]},{level:"junior",q:"What is the difference between launch/async and just calling a suspend function directly?",a:[{t:"p",text:"Calling a suspend function directly runs it *sequentially* — your code waits for it before continuing. `launch`/`async` *start a new coroutine* that runs concurrently with the code after it. So the difference is concurrency: a direct call is 'do this now and wait'; a builder is 'start this alongside'."},{t:"code",title:"Sequential vs concurrent",code:`suspend fun example() = coroutineScope {
    // Sequential: b starts only after a finishes
    val a = fetchA(); val b = fetchB()          // ~ timeA + timeB

    // Concurrent: both start immediately
    val da = async { fetchA() }; val db = async { fetchB() }
    val total = da.await() + db.await()          // ~ max(timeA, timeB)
}`},{t:"list",items:["**Direct suspend call** — sequential; the caller suspends until it returns. Simplest, and correct when you genuinely need the result before proceeding.","**`launch`** — starts concurrent fire-and-forget work; the caller continues immediately.","**`async`** — starts concurrent work with a result; combine several for parallelism.","**Don't over-parallelize** — sequential is the safe default; reach for `async` only when calls are independent."]},{t:"note",text:"A direct suspend call is sequential (wait for it, then continue). launch/async start a new coroutine that runs concurrently. Use direct calls when you need the result next; use async for independent work you want in parallel (start both, then await). Sequential is the safe default."}]},{level:"senior",q:"What is CoroutineStart, and when would you use LAZY or ATOMIC?",a:[{t:"p",text:"The `start` parameter of `launch`/`async` controls *when and how* the coroutine begins. `DEFAULT` starts it immediately (scheduled to run). `LAZY` creates it but doesn't start until you call `start()`/`join()`/`await()`. `ATOMIC` and `UNDISPATCHED` are advanced modes affecting cancellation timing and initial dispatch."},{t:"code",title:"Lazy start",code:`val job = scope.launch(start = CoroutineStart.LAZY) { expensiveWork() }
// ... not running yet ...
if (needed) job.start()   // begins only now

val d = async(start = CoroutineStart.LAZY) { compute() }
val result = d.await()    // starts on first await`},{t:"list",items:["**`DEFAULT`** — start immediately (the usual choice).","**`LAZY`** — defer until `start()`/`await()`/`join()`; useful to prepare a coroutine you may or may not need, or to set up a dependency graph before running.","**`ATOMIC`** — starts non-cancellably up to the first suspension point (rarely needed; guarantees the coroutine begins even if cancelled immediately).","**`UNDISPATCHED`** — runs immediately on the current thread until the first suspension point (advanced; avoids an initial dispatch)."]},{t:"note",text:"CoroutineStart controls startup: DEFAULT (start now — usual), LAZY (start on start()/await()/join() — prepare-but-maybe-not-run), ATOMIC (begins non-cancellably to the first suspension point), UNDISPATCHED (runs on the current thread until first suspension). You'll mostly use DEFAULT and occasionally LAZY."}]}],yS={id:"basics",title:"Coroutine Basics & Suspend",tagline:"What a coroutine is, suspend functions, how suspension works (continuations + state machine), suspend vs blocking, and sequential vs concurrent.",tags:["Coroutines","suspend","Continuation","delay","async"],content:fS,qa:gS},wS=[{heading:"Coroutine builders — how you start a coroutine",blocks:[{t:"p",text:"You can't just call a suspend function from normal code — you need a **coroutine builder** to launch a coroutine that provides the required context. The three builders, each for a different situation:"},{t:"table",headers:["Builder","Returns","Blocks caller?","Use for"],rows:[["`launch`","`Job`","no","fire-and-forget work (no result needed)"],["`async`","`Deferred<T>`","no","concurrent work that produces a result"],["`runBlocking`","`T`","**yes** (blocks the thread)","bridging blocking code / tests / main() only"]]},{t:"code",title:"The three builders",code:`// launch — start work, don't wait for a result
scope.launch {
    repository.syncData()          // fire and forget
}

// async — start concurrent work, get a result via await()
val deferred = scope.async { api.fetch() }
val result = deferred.await()

// runBlocking — BLOCKS the current thread until the block completes
fun main() = runBlocking {          // bridges main() into the coroutine world
    launch { doWork() }
}`},{t:"list",items:["**`launch`** returns a `Job` — a handle to cancel or join the coroutine. Its block returns nothing useful; exceptions in it propagate to the parent (and can crash).","**`async`** returns a `Deferred<T>` (a `Job` with a result). You get the value with `await()`, and — importantly — an exception is *held* in the Deferred and thrown when you `await()` it.","**`runBlocking`** actually **blocks** the calling thread until its coroutine finishes — the opposite of the whole point of coroutines. It's a bridge for `main()` functions, unit tests (though `runTest` is preferred), and calling suspend code from a truly blocking context. **Never use it on the main thread in an app** — it freezes the UI."]}]},{heading:"CoroutineScope — the lifetime of coroutines",blocks:[{t:"p",text:"Every coroutine runs inside a **`CoroutineScope`**, which defines its **lifetime** and holds its **context** (dispatcher, Job, etc.). Builders like `launch`/`async` are extension functions *on* `CoroutineScope` — that's why you write `scope.launch { }`. The scope's job: when the scope is cancelled, *all* coroutines started in it are cancelled. This is the foundation of not leaking coroutines."},{t:"list",items:["A scope is created from a `CoroutineContext` — typically a `Job` (or `SupervisorJob`) plus a `Dispatcher`: `CoroutineScope(SupervisorJob() + Dispatchers.Main)`.","Coroutines launched in a scope are its **children** — cancelling the scope cancels the children; the scope isn't considered complete until its children finish (structured concurrency).","**Never use `GlobalScope`**: it's a scope with no lifetime bound to anything — coroutines launched there live until the process dies and aren't cancelled when your screen/component goes away. It's the classic coroutine-leak source."]}]},{heading:"The Android lifecycle scopes you get for free",blocks:[{t:"list",items:["**`viewModelScope`** — a scope tied to a `ViewModel`; cancelled automatically in `onCleared()`. The home for ViewModel coroutines (data loading, driving state). Uses `Dispatchers.Main.immediate` + a `SupervisorJob`.","**`lifecycleScope`** — tied to a `Lifecycle` (Activity/Fragment); cancelled when the lifecycle is destroyed. For UI-layer coroutines. Often paired with `repeatOnLifecycle` for lifecycle-aware flow collection.","**`rememberCoroutineScope()`** — a Compose scope tied to the composable's presence in the composition; cancelled when it leaves. For launching from event handlers (covered in the Compose Side Effects topic)."]},{t:"code",title:"Using the framework scopes",code:`class MyViewModel : ViewModel() {
    fun refresh() {
        viewModelScope.launch {            // cancelled in onCleared()
            _state.value = repository.load()
        }
    }
}

// In a Fragment:
viewLifecycleOwner.lifecycleScope.launch {
    repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.uiState.collect { render(it) }
    }
}`},{t:"p",text:"The point of these scopes is **automatic cancellation**: when the ViewModel is cleared or the screen is destroyed, every coroutine you launched is cancelled — no leaks, no callbacks firing into dead UI. You almost never create your own scope in app code; you use the lifecycle-provided ones."}]},{heading:"coroutineScope vs supervisorScope — scoping functions",blocks:[{t:"p",text:"`coroutineScope { }` and `supervisorScope { }` are **suspend functions** that create a *new scope* as a child of the current one, run their block, and **don't return until all child coroutines complete**. They're how you do structured concurrency *within* a suspend function (e.g. run several things concurrently and wait for all)."},{t:"code",title:"coroutineScope waits for all children",code:`suspend fun loadAll(): Data = coroutineScope {
    val a = async { fetchA() }
    val b = async { fetchB() }
    Data(a.await(), b.await())
    // returns only after both children finish;
    // if one throws, the other is cancelled and the exception propagates
}`},{t:"list",items:["**`coroutineScope`** — if *any* child fails, it cancels *all* the other children and rethrows the exception. All-or-nothing: use when the results are interdependent (all needed, or the whole operation fails).","**`supervisorScope`** — children fail *independently*: one child throwing does *not* cancel its siblings. Use when children are independent and one failing shouldn't abort the rest (e.g. loading several independent widgets — one can fail while others succeed).","Both differ from `runBlocking` in that they **suspend** (don't block a thread) — they're the suspend-world way to establish a scope, whereas `runBlocking` blocks."]}]},{heading:"launch vs async — and the exception subtlety",blocks:[{t:"list",items:["**`launch`** is for side-effecting, resultless work. An uncaught exception in a `launch` coroutine **propagates immediately** to its parent/scope (and, without a handler, can crash or be routed to a `CoroutineExceptionHandler`).","**`async`** is for producing a value. An exception in an `async` coroutine is **captured in the `Deferred`** and only *rethrown when you call `await()`** — so if you never await, or the async is a top-level coroutine, the exception can be silently deferred. (When `async` is a *child* of a scope, the exception still propagates to the parent on failure, per structured concurrency — the 'await to see it' rule is about the direct throwing site.)","**Practical rule**: use `launch` for 'do this'; use `async` for 'compute this, I'll await it'. Don't use `async` for fire-and-forget (you lose the exception unless you await), and don't use `async` if you're just going to immediately `await` a single call (that's just sequential work — a plain suspend call is clearer)."]},{t:"note",text:'Common interview question: "launch vs async?" → "launch returns a Job for fire-and-forget work and throws exceptions to its parent; async returns a Deferred<T> for concurrent results and holds the exception until await(). Use async only when you actually need the result concurrently."'}]}],vS=[{level:"junior",q:"What is the difference between launch and async?",a:[{t:"p",text:"**Both start a coroutine, but for different purposes.** `launch` is for *fire-and-forget* work that doesn't return a result — it returns a `Job` (a handle to cancel or join). `async` is for *concurrent work that produces a result* — it returns a `Deferred<T>`, and you get the value by calling `await()`."},{t:"code",title:"The distinction",code:`// launch — do something, no result
viewModelScope.launch { repository.sync() }

// async — compute something concurrently, get the result
val a = async { api.getA() }
val b = async { api.getB() }
val combined = a.await() + b.await()   // both ran in parallel`},{t:"list",items:["**Use `launch`** when you just want to perform an action — update state, trigger a save, start a collection. No value comes back.","**Use `async`** when you need a *result* and want *concurrency* — typically several `async` calls started together, then `await`ed, so they run in parallel.","**Don't misuse `async`** for a single call you immediately await — that's just sequential work dressed up; a plain suspend call is clearer. And don't use `async` for fire-and-forget, because its exception is held until you `await` (so it can go unnoticed)."]}]},{level:"junior",q:"What is runBlocking and when should (and shouldn't) you use it?",a:[{t:"p",text:"**The concept**: `runBlocking` is a bridge between the blocking world and the coroutine world. It starts a coroutine and **blocks the current thread** until that coroutine (and its children) complete, returning the result. It's the opposite of normal coroutine behavior — it deliberately blocks a thread — which is exactly why its use is narrow."},{t:"list",items:["**Use it for**: a `main()` function (to call suspend code from a plain entry point), unit tests that need to run suspend code (though `runTest` is preferred for its virtual time), and bridging suspend code into a genuinely blocking API you can't change.","**Never use it on the Android main thread** — it blocks the UI thread until the coroutine finishes, freezing the app and risking an ANR. This is a serious bug, not a style issue.","**Never use it to 'call a suspend function from normal code' in app logic** — that defeats the point of coroutines (you've reintroduced blocking). Instead, launch a coroutine in a proper scope (`viewModelScope.launch`)."]},{t:"p",text:"The mental model: `launch`/`async` are *non-blocking* (the caller keeps going), while `runBlocking` is *blocking* (the caller's thread waits). In app code you almost always want the non-blocking builders in a lifecycle scope; `runBlocking` is a tool for tests and top-level entry points."}]},{level:"junior",q:"What is a CoroutineScope and why does it matter?",a:[{t:"p",text:"**The concept**: a `CoroutineScope` defines the *lifetime* of the coroutines started within it. Every coroutine belongs to a scope, and the builders (`launch`, `async`) are extension functions on `CoroutineScope`. The scope's crucial property: **when it's cancelled, all coroutines started in it are cancelled too.** That's how you avoid leaking coroutines that keep running after the thing that started them is gone."},{t:"p",text:"**Why it matters on Android**: consider a network call launched from a screen. If the user leaves the screen, that call should stop — otherwise it wastes resources and might try to update a destroyed UI (crash). By launching in a scope tied to the screen's lifecycle (`viewModelScope`, `lifecycleScope`), cancellation is automatic: the scope is cancelled when the ViewModel is cleared or the screen destroyed, and every coroutine in it stops. The rule that follows: never use `GlobalScope`, which has *no* lifetime bound to anything — its coroutines live until the process dies and are the classic leak. Always launch in a scope that matches how long the work should live."}]},{level:"junior",q:"What is viewModelScope and when is it cancelled?",a:[{t:"p",text:"**The concept**: `viewModelScope` is a `CoroutineScope` provided by the `androidx.lifecycle` library on every `ViewModel`. Coroutines launched in it are automatically **cancelled when the ViewModel is cleared** — i.e. in `onCleared()`, which happens when the ViewModel's owner (screen) is permanently gone (not on rotation — the ViewModel survives that)."},{t:"code",title:"The standard usage",code:`class FeedViewModel : ViewModel() {
    fun load() {
        viewModelScope.launch {          // auto-cancelled in onCleared()
            _state.value = repository.getFeed()
        }
    }
}`},{t:"p",text:"It's built with `Dispatchers.Main.immediate` (so it starts on the main thread without an extra dispatch) and a `SupervisorJob` (so one failed child coroutine doesn't cancel its siblings). The benefit is you never manually manage cancellation for ViewModel work — leave the screen, and all in-flight loads stop. The only caveat: work that must *outlive* the screen (an upload that should finish after the user navigates away) should *not* go in `viewModelScope` — it belongs in an application-scoped scope or WorkManager, because `viewModelScope` is meant to die with the screen."}]},{level:"senior",q:"Explain coroutineScope vs supervisorScope. When would you use each?",a:[{t:"p",text:"**Both are suspend functions that create a new child scope, run a block, and suspend until all child coroutines complete — the difference is how they handle a child's failure.** `coroutineScope` uses a regular `Job`, so failure propagates: if any child throws, *all* other children are cancelled and the exception is rethrown from the scope. `supervisorScope` uses a `SupervisorJob`, so children fail *independently*: one child throwing does **not** cancel its siblings."},{t:"list",items:["**Use `coroutineScope` for all-or-nothing work** — when the results are interdependent and a partial result is useless. Loading a screen that needs *both* the user profile *and* the settings: if either fails, the whole load fails, and you don't want the other call wastefully continuing. This is the common case.","**Use `supervisorScope` for independent work** — when children don't depend on each other and one failing shouldn't abort the others. A dashboard loading several independent widgets (weather, news, stocks): if the news call fails, you still want weather and stocks to load and show, with just the news widget showing an error."]},{t:"code",title:"The behavioral difference",code:`// coroutineScope: b failing cancels a, whole thing throws
coroutineScope {
    val a = async { fetchA() }
    val b = async { fetchB() }   // if this throws, a is cancelled too
    combine(a.await(), b.await())
}

// supervisorScope: widgets fail independently
supervisorScope {
    launch { loadWeather() }     // failure here doesn't stop the others
    launch { loadNews() }
    launch { loadStocks() }
}`},{t:"p",text:"**A subtlety worth naming**: in `supervisorScope`, because failures don't propagate to the parent automatically, each child must handle its *own* exceptions (try/catch or a `CoroutineExceptionHandler` on the launch) — otherwise the exception is unhandled. In `coroutineScope`, the exception naturally surfaces at the scope's call site where you can catch it. So `supervisorScope` gives independence at the cost of you owning each child's error handling."}]},{level:"senior",q:"How does exception handling differ between launch and async?",a:[{t:"p",text:"**The core difference is *when* and *where* the exception surfaces.** In `launch`, an uncaught exception **propagates immediately** to the coroutine's parent/scope as soon as it's thrown — it travels up the Job hierarchy and, if unhandled, reaches a `CoroutineExceptionHandler` or crashes. In `async`, the exception is **captured inside the `Deferred`** and only *thrown when you call `await()`** — the throwing is deferred to the point of consumption."},{t:"list",items:["**`launch` — exception is 'pushed'**: it fires up to the parent right away. You handle it with a try/catch *inside* the coroutine, or a `CoroutineExceptionHandler` installed in the context (which only works for `launch`, not `async`).","**`async` — exception is 'pulled'**: `await()` re-throws it. So `try { deferred.await() } catch (e) { }` is the natural handling. If you *never* await a standalone async, the exception can sit unnoticed in the Deferred.","**The structured-concurrency twist**: when `async` is a *child* of a scope (the normal case, e.g. inside `coroutineScope`), a failure *also* propagates to the parent scope and cancels siblings — the same as launch — *in addition to* being rethrown at `await()`. The 'exception waits for await()' behavior in isolation is most visible for a top-level `async` on a scope. So the precise statement is: async holds the exception for its `await()`, but as a child it still participates in structured cancellation.","**`CoroutineExceptionHandler` caveat**: it catches uncaught exceptions from `launch` (root coroutines) only. It does nothing for `async` (whose exceptions are meant to be surfaced via `await`) and nothing for child coroutines (whose exceptions propagate to the parent). Knowing where the handler does and doesn't apply is a common senior discriminator."]},{t:"p",text:"**Practical guidance**: for fire-and-forget, use `launch` and handle errors with try/catch inside or a `CoroutineExceptionHandler` at the scope. For concurrent results, use `async` and wrap the `await()` in try/catch. Mixing them up — e.g. expecting a `CoroutineExceptionHandler` to catch an `async` failure — is a frequent bug."}]},{level:"senior",q:"Why is GlobalScope considered an anti-pattern, and what should you use instead?",a:[{t:"p",text:"**The problem**: `GlobalScope` is a scope whose lifetime is the *entire application process* — coroutines launched in it are bound to nothing and are cancelled only when the process dies. This breaks structured concurrency in every way that matters:"},{t:"list",items:["**Leaks**: a `GlobalScope.launch` started from a screen keeps running after the screen is gone — wasting CPU/network/battery, and potentially trying to update a destroyed UI (crash or leak of the destroyed component it captured).","**No automatic cancellation**: nothing cancels it when the relevant lifecycle ends, so you must remember to cancel manually (and usually don't).","**Untestable**: tests can't control or wait for GlobalScope coroutines the way they control a provided scope; work escapes the test's timing.","**Orphaned failures**: exceptions in GlobalScope coroutines don't propagate to any meaningful parent."]},{t:"list",items:["**Instead, use a lifecycle-bound scope**: `viewModelScope` for ViewModel work, `lifecycleScope`/`viewLifecycleOwner.lifecycleScope` for UI work, `rememberCoroutineScope()` in Compose. These cancel automatically when their owner is gone.","**For work that legitimately must outlive a screen** (an upload that should complete after the user navigates away, app-wide sync): inject an **application-scoped `CoroutineScope`** (created once, e.g. `CoroutineScope(SupervisorJob() + Dispatchers.Default)` provided by DI and tied to the Application) into the repository — it's still a *managed, injectable, testable* scope, unlike GlobalScope. For work that must survive process death, use **WorkManager**."]},{t:"p",text:"**The principle**: every coroutine should have an owner whose lifecycle bounds it. `GlobalScope` deliberately has no owner, so it's almost never correct. The rare legitimate 'outlive the caller' need is met by an *injected application scope* (managed, testable) — not by GlobalScope. Kotlin even requires an opt-in annotation to use GlobalScope now, signaling it's a code smell."}]},{level:"junior",q:"What does launch return, and what can you do with the Job?",a:[{t:"p",text:"`launch` returns a `Job` — a handle to the coroutine it started. Through it you can cancel the coroutine, wait for it (`join`), check its state, register completion callbacks, and access its place in the parent-child hierarchy. The `Job` is how you *control* a running coroutine after starting it."},{t:"code",title:"Using the returned Job",code:`val job = scope.launch { longWork() }
job.cancel()                       // stop it
job.join()                         // suspend until it finishes
job.isActive / job.isCancelled     // inspect state
job.invokeOnCompletion { cause -> cleanup(cause) }   // run on completion/cancel
val current = scope.launch { … }   // keep the handle to cancel later (e.g. a search)`},{t:"list",items:["**`cancel()` / `cancelAndJoin()`** — stop the coroutine (cooperatively).","**`join()`** — suspend until it completes.","**`isActive`/`isCancelled`/`isCompleted`** — inspect state.","**`invokeOnCompletion { }`** — a callback when it finishes or is cancelled (with the cause).","**Keep the handle** — store the `Job` to cancel a previous operation before starting a new one (cancel-previous pattern)."]},{t:"note",text:"launch returns a Job — the control handle: cancel()/cancelAndJoin() to stop, join() to wait, isActive/isCancelled to inspect, invokeOnCompletion{} for a finish callback. Keep the Job to implement cancel-previous (cancel the old search before launching a new one)."}]},{level:"junior",q:"How do you run several async operations concurrently and combine their results?",a:[{t:"p",text:"Start each independent operation with `async` (they begin immediately and run concurrently), then `await` them — or use `awaitAll` for a list. The total time is roughly the *longest* operation rather than the sum, because they overlap. Wrap them in a `coroutineScope` so a failure in one cancels the others."},{t:"code",title:"Concurrent fan-out with awaitAll",code:`suspend fun loadDashboard(): Dashboard = coroutineScope {
    val user = async { api.getUser() }
    val feed = async { api.getFeed() }
    val ads  = async { api.getAds() }
    Dashboard(user.await(), feed.await(), ads.await())   // ~ max of the three
}
// For a dynamic list:
val results = ids.map { async { fetch(it) } }.awaitAll()`},{t:"list",items:["**Start all `async` first, then await** — awaiting each right after starting serializes them.","**`awaitAll(list)`** — await a collection of `Deferred`s; throws if any fails.","**`coroutineScope`** — structured: if one child fails, the others are cancelled and the exception propagates.","**Time = max, not sum** — the win of concurrency for independent I/O."]},{t:"note",text:"Start each independent call with async (they run concurrently), then await/awaitAll — total time ≈ the longest, not the sum. Wrap in coroutineScope so one failure cancels the rest and propagates. Pitfall: awaiting each async immediately after starting it serializes the work."}]},{level:"senior",q:"How do you create a custom CoroutineScope, and what should its context contain?",a:[{t:"p",text:"Create one with the `CoroutineScope(context)` factory, giving it a `Job` (or `SupervisorJob`) plus a dispatcher. The `Job` makes it cancellable as a unit; the dispatcher sets the default thread. You must *cancel* the scope when its owner is destroyed, or its coroutines leak — this is exactly what `viewModelScope` does for you."},{t:"code",title:"A managed custom scope",code:`class SyncManager {
    private val scope = CoroutineScope(SupervisorJob() + Dispatchers.IO)
    fun start() { scope.launch { sync() } }
    fun close() { scope.cancel() }   // MUST cancel to avoid leaks
}`},{t:"list",items:["**Include a `Job`/`SupervisorJob`** — enables cancellation; `SupervisorJob` keeps children independent (one failure doesn't cancel siblings).","**Include a dispatcher** — the default thread for coroutines in the scope (`Main`, `IO`, `Default`).","**Optionally a `CoroutineExceptionHandler`** — to handle uncaught exceptions from `launch`.","**Cancel it** — in the owner's teardown (`close()`/`onCleared()`); forgetting this leaks running coroutines."]},{t:"note",text:"CoroutineScope(SupervisorJob() + Dispatchers.X [+ handler]) — the Job makes it cancellable (SupervisorJob keeps children independent), the dispatcher sets the default thread. You MUST scope.cancel() in the owner's teardown or coroutines leak. This is what viewModelScope/lifecycleScope automate."}]},{level:"junior",q:"What is lifecycleScope, and how does it differ from viewModelScope?",a:[{t:"p",text:"`lifecycleScope` is a scope tied to an `Activity`/`Fragment`'s `Lifecycle` — its coroutines are cancelled when that lifecycle is destroyed. `viewModelScope` is tied to a `ViewModel` and cancelled in `onCleared()`. The key difference is lifetime: a ViewModel survives configuration changes (rotation), while a Fragment's lifecycle does not."},{t:"table",headers:["","viewModelScope","lifecycleScope"],rows:[["Owner","ViewModel","Activity/Fragment"],["Cancelled on","onCleared()","lifecycle DESTROYED"],["Survives rotation","yes","no (recreated)"],["Use for","data/business work","UI-lifecycle work (collecting to update views)"],["Default dispatcher","Main.immediate","Main.immediate"]]},{t:"list",items:["**`viewModelScope`** — for work that should survive config change (loading data); cancelled when the ViewModel is truly gone.","**`lifecycleScope`** — for UI-tied work; cancelled and restarted with the Activity/Fragment.","**Both default to `Dispatchers.Main.immediate`** — so UI updates are safe without switching.","**Prefer collecting flows in the VM** and exposing state; use `lifecycleScope` + `repeatOnLifecycle` when collecting directly in the UI."]},{t:"note",text:"lifecycleScope (Activity/Fragment, cancelled on DESTROY, doesn't survive rotation) vs viewModelScope (ViewModel, cancelled in onCleared, survives config change). Both default to Main.immediate. Put data work in viewModelScope; use lifecycleScope + repeatOnLifecycle for UI-side flow collection."}]},{level:"senior",q:"What is repeatOnLifecycle, and why is it preferred over launchWhenStarted?",a:[{t:"p",text:"`repeatOnLifecycle(state)` runs a block whenever the lifecycle reaches `state` (e.g. STARTED) and *cancels* it when the lifecycle drops below that state — then restarts it on the next STARTED. This fully stops upstream work (like flow collection) when the UI is backgrounded. The older `launchWhenStarted`/`launchWhenX` only *paused* the coroutine, leaving upstream producers active (wasting resources)."},{t:"code",title:"Lifecycle-aware collection",code:`lifecycleScope.launch {
    repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.uiState.collect { render(it) }   // collection cancelled when STOPPED, restarted on START
    }
}`},{t:"list",items:["**`repeatOnLifecycle(STARTED)`** — cancels the block on STOP, restarts on START; upstream (flow) collection genuinely stops when backgrounded.","**`launchWhenStarted` (deprecated)** — only suspends the coroutine; the flow keeps collecting/buffering upstream, wasting work and risking stale updates.","**Pairs with `stateIn(WhileSubscribed)`** — the producer stops when there are no collectors.","**Or use `collectAsStateWithLifecycle`** in Compose, which encapsulates this."]},{t:"note",text:"repeatOnLifecycle(STARTED) cancels its block when the UI stops and restarts it on start — so flow collection truly halts when backgrounded. The deprecated launchWhenStarted only paused the coroutine, leaving upstream producers running (wasteful/stale). Pair with stateIn(WhileSubscribed); in Compose use collectAsStateWithLifecycle."}]},{level:"senior",q:"When and how do you cancel a scope, and what leaks if you don't?",a:[{t:"p",text:"You cancel a scope in its owner's teardown so all its coroutines stop. `viewModelScope`/`lifecycleScope` do this automatically; a *custom* scope you must cancel yourself (`scope.cancel()`). If you don't, coroutines keep running after the screen/object is gone — leaking memory (they hold references to the owner), wasting CPU/network, and possibly updating dead UI."},{t:"list",items:["**Managed scopes auto-cancel** — `viewModelScope` in `onCleared`, `lifecycleScope` on DESTROY.","**Custom scopes: cancel manually** — in `close()`/`onCleared()`/a lifecycle observer; call `scope.cancel()`.","**Leak symptoms** — the Activity/ViewModel isn't garbage-collected (retained), background work continues, log lines after the screen is gone.","**`cancel()` vs `coroutineContext.cancelChildren()`** — `cancel()` also makes the scope unusable (its Job is cancelled); use it for permanent teardown."]},{t:"code",title:"Cancel on teardown",code:"override fun onCleared() { super.onCleared(); customScope.cancel() }"},{t:"note",text:"Cancel a scope in its owner's teardown; viewModelScope/lifecycleScope do it automatically, custom scopes need scope.cancel() (in close/onCleared/observer). Forgetting it leaks: retained owner, running background work, updates to dead UI. cancel() also makes the scope unusable — right for permanent teardown."}]},{level:"junior",q:"What's the difference between the coroutineScope{} builder and a CoroutineScope instance?",a:[{t:"p",text:"The names are confusingly similar. `coroutineScope { }` (lowercase, a suspend *function*) is a builder that creates a scope for its block, waits for all children, and returns when they finish — used *inside* a suspend function to run concurrent children structurally. `CoroutineScope` (the interface/factory) is a long-lived *object* you launch coroutines on, tied to a lifecycle."},{t:"list",items:["**`coroutineScope { }`** — a suspend function; scopes concurrent work within a larger operation; suspends until all children complete; if a child fails, it cancels siblings and rethrows.","**`CoroutineScope(...)` / a scope object** — a durable entity (like `viewModelScope`) you call `launch`/`async` on; lives until cancelled.","**`supervisorScope { }`** — like `coroutineScope` but child failures are isolated.","**Rule of thumb** — use `coroutineScope { }` to structure a suspend function's internal concurrency; use a `CoroutineScope` object to own fire-and-forget work bound to a lifecycle."]},{t:"code",title:"Two different things",code:`// Builder: structure concurrency inside a suspend fun, wait for children
suspend fun load() = coroutineScope { val a = async{}; val b = async{}; a.await() + b.await() }

// Object: long-lived, launch onto it
val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main)
scope.launch { … }`},{t:"note",text:"coroutineScope { } (suspend function) scopes concurrent children inside a suspend fun and waits for them (child failure cancels siblings + rethrows). CoroutineScope (object/factory) is a long-lived, lifecycle-bound entity you launch onto. Builder = internal structured concurrency; object = owned fire-and-forget work."}]},{level:"senior",q:"How do you load several independent screen sections where one failing shouldn't kill the others?",a:[{t:"p",text:"Use `supervisorScope { }` (or a scope with a `SupervisorJob`) so each child's failure is *isolated* — one section failing doesn't cancel its siblings. Inside, launch each section and handle its failure individually (try/catch around each, or per-`async` await with catch), so the screen shows the sections that succeeded and an error state only for the one that failed."},{t:"code",title:"Independent sections with supervisorScope",code:`suspend fun loadScreen(): ScreenState = supervisorScope {
    val header = async { runCatching { api.header() } }
    val feed   = async { runCatching { api.feed() } }
    val promos = async { runCatching { api.promos() } }
    ScreenState(
        header = header.await().getOrNull(),   // one failing section = null, others still load
        feed   = feed.await().getOrDefault(emptyList()),
        promos = promos.await().getOrNull(),
    )
}`},{t:"list",items:["**`supervisorScope`** — child failures don't propagate to siblings (unlike `coroutineScope`, where one failure cancels all).","**Handle each failure** — `runCatching`/try-catch per section so a failure becomes a local error state, not a thrown exception.","**`coroutineScope` would be wrong here** — one section's exception would cancel the whole load.","**UX** — partial content is better than an all-or-nothing screen for independent widgets."]},{t:"note",text:"Use supervisorScope so each section's failure is isolated (siblings keep loading), and wrap each in runCatching so a failure becomes a local error/empty state rather than throwing. coroutineScope would cancel everything on one failure. Result: partial content instead of an all-or-nothing screen."}]},{level:"junior",q:"What do the CoroutineScope() and MainScope() factory functions give you?",a:[{t:"p",text:"`CoroutineScope(context)` builds a scope from whatever context you pass (Job + dispatcher + extras). `MainScope()` is a convenience that returns a scope with a `SupervisorJob` and `Dispatchers.Main` — handy for UI-owning classes that manage their own coroutines. Both require you to cancel them when done."},{t:"code",title:"Factory scopes",code:`val scope = CoroutineScope(Job() + Dispatchers.Default)   // fully custom
val ui = MainScope()   // == CoroutineScope(SupervisorJob() + Dispatchers.Main)
// remember to cancel:
ui.cancel()`},{t:"list",items:["**`CoroutineScope(context)`** — the general factory; you choose the Job type and dispatcher.","**`MainScope()`** — preset `SupervisorJob() + Dispatchers.Main`; good default for a UI controller not covered by `lifecycleScope`.","**Always cancel** — neither is lifecycle-aware on its own; tie cancellation to the owner's teardown.","**Prefer built-ins when available** — `viewModelScope`/`lifecycleScope` handle cancellation for you; use factories only for custom-lifecycle owners."]},{t:"note",text:"CoroutineScope(context) builds a scope from a context you choose; MainScope() presets SupervisorJob() + Dispatchers.Main for UI owners. Both need manual cancellation tied to the owner's teardown. Prefer viewModelScope/lifecycleScope when they fit; use factories only for custom-lifecycle classes."}]},{level:"senior",q:"How do you tie a coroutine scope to a custom-lifecycle class and clean it up?",a:[{t:"p",text:"Create a scope the class owns, launch work on it, and cancel it in the class's teardown hook. If the class has an Android lifecycle, observe it; otherwise expose an explicit `close()`/`release()`. The pattern mirrors `viewModelScope`: own the scope, cancel it exactly once when the owner dies."},{t:"code",title:"Scope owned by a manager",code:`class UploadManager : Closeable {
    private val scope = CoroutineScope(SupervisorJob() + Dispatchers.IO)
    fun enqueue(file: File) = scope.launch { upload(file) }
    override fun close() { scope.cancel() }   // called by the owner when done
}
// Lifecycle-bound variant:
class Widget(lifecycle: Lifecycle) {
    private val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main)
    init { lifecycle.addObserver(LifecycleEventObserver { _, e ->
        if (e == Lifecycle.Event.ON_DESTROY) scope.cancel() }) }
}`},{t:"list",items:["**Own the scope** — a private field with `SupervisorJob` + the right dispatcher.","**Cancel once, in teardown** — `close()`/`release()`, or an `ON_DESTROY` lifecycle observer.","**Expose intent, not the scope** — methods like `enqueue()`; don't leak the raw scope.","**Idempotent cleanup** — cancelling an already-cancelled scope is safe."]},{t:"note",text:"Give the class a private CoroutineScope(SupervisorJob() + dispatcher), launch work on it, and cancel it once in teardown (close()/release() or an ON_DESTROY observer). Expose methods, not the raw scope. It's the viewModelScope pattern for any custom-lifecycle owner; cancelling twice is harmless."}]},{level:"junior",q:"How do you launch a coroutine on a specific dispatcher?",a:[{t:"p",text:"Pass the dispatcher as the `context` argument to the builder: `launch(Dispatchers.IO) { }` or `async(Dispatchers.Default) { }`. The coroutine then starts on that dispatcher instead of inheriting the scope's default. You can also switch mid-coroutine with `withContext(dispatcher)`."},{t:"code",title:"Builder with a dispatcher",code:`viewModelScope.launch(Dispatchers.IO) { writeToDisk() }    // starts on IO
viewModelScope.launch {                                     // starts on Main (default)
    val data = withContext(Dispatchers.Default) { compute() }  // switch for CPU work
    render(data)                                            // back on Main
}`},{t:"list",items:["**Builder context arg** — `launch(dispatcher)`/`async(dispatcher)` sets the starting dispatcher.","**Prefer main-safe functions** — often better to keep `withContext` inside the repository than to launch on `IO` in the ViewModel.",'**Combine context elements** — `launch(Dispatchers.IO + CoroutineName("sync"))`.',"**`withContext`** — switch dispatchers within a coroutine for a sub-block, returning to the previous one after."]},{t:"note",text:"Pass the dispatcher to the builder: launch(Dispatchers.IO){} / async(Dispatchers.Default){}; combine elements with + (Dispatchers.IO + CoroutineName). Or switch mid-coroutine with withContext(dispatcher). Prefer pushing withContext into main-safe repository functions over launching on IO in the ViewModel."}]},{level:"senior",q:"What's the difference between runBlocking in tests and runTest?",a:[{t:"p",text:"`runBlocking` actually blocks the thread and runs coroutines in *real time* (a `delay(1000)` waits a real second). `runTest` (from kotlinx-coroutines-test) uses a *virtual clock* that auto-advances, so delays are skipped and tests run instantly, while still giving deterministic control over scheduling. For testing coroutine code, prefer `runTest`."},{t:"code",title:"runTest skips delays",code:`@Test fun example() = runTest {
    val vm = MyViewModel(repo, StandardTestDispatcher(testScheduler))
    vm.load()
    advanceUntilIdle()          // run all scheduled coroutines
    assertEquals(Content, vm.state.value)
}
// delay(10_000) inside completes instantly under the virtual clock`},{t:"list",items:["**`runBlocking`** — real time, blocks the thread; fine for `main()` or simple bridging, but slow for tests with delays.","**`runTest`** — virtual time; `delay` is skipped, `advanceTimeBy`/`advanceUntilIdle` control scheduling; fast and deterministic.","**Inject test dispatchers** — `StandardTestDispatcher`/`UnconfinedTestDispatcher` sharing the `testScheduler` so production coroutines use the virtual clock.","**`Dispatchers.setMain`** — replace `Dispatchers.Main` with a test dispatcher for ViewModel tests."]},{t:"note",text:"runBlocking runs in real time and blocks the thread (a delay waits for real); runTest uses a virtual clock that skips delays and gives deterministic scheduling (advanceUntilIdle/advanceTimeBy). Use runTest for coroutine tests, inject Test dispatchers on the shared testScheduler, and Dispatchers.setMain for ViewModel tests."}]},{level:"senior",q:"Why doesn't launch throw its exception at the call site, and what are the implications?",a:[{t:"p",text:"`launch` is fire-and-forget: it returns a `Job` immediately and the coroutine runs independently, so an exception thrown *inside* it can't surface at the `launch` call site (that code has already moved on). Instead the exception propagates through the coroutine's parent `Job` — cancelling the parent and siblings, and reaching a `CoroutineExceptionHandler` if present."},{t:"list",items:["**Fire-and-forget** — `launch` returns before the body runs, so a `try/catch` *around* `launch` catches nothing.","**Propagates via the job tree** — an uncaught exception cancels the parent scope (unless a `SupervisorJob` isolates it).","**Handle inside** — put `try/catch` *inside* the `launch` block, or install a `CoroutineExceptionHandler` on the scope.","**`async` differs** — it stores the exception and rethrows it at `await()`."]},{t:"code",title:"Catch inside, not around",code:`// WRONG: catches nothing
try { scope.launch { risky() } } catch (e: Exception) { }
// RIGHT: catch inside the coroutine
scope.launch { try { risky() } catch (e: Exception) { handle(e) } }`},{t:"note",text:"launch returns immediately (fire-and-forget), so an exception in its body can't reach a try/catch around launch — it propagates through the parent Job (cancelling the scope unless SupervisorJob) and to a CoroutineExceptionHandler. Handle it INSIDE the launch block or via a handler. async instead defers the exception to await()."}]},{level:"junior",q:"How do you decide between launch and async?",a:[{t:"p",text:"Use `launch` when you *don't need a result* — fire-and-forget work like saving to a database, sending analytics, or updating state as a side effect. Use `async` when you *need a value back* and especially when you want *concurrency* — several independent computations you'll combine. Choosing `async` for fire-and-forget just to ignore the `Deferred` is a smell (and can swallow exceptions)."},{t:"table",headers:["Need","Use"],rows:[["Side effect, no result","`launch`"],["A single result","just call the suspend fun (sequential)"],["Multiple results concurrently","`async` + `await`/`awaitAll`"],["Independent work you'll combine","`async`"]]},{t:"list",items:["**`launch`** — no return value; returns a `Job`. Most ViewModel operations are `launch`.","**`async`** — returns `Deferred<T>`; for concurrent results.","**Don't `async` a single thing you immediately `await`** — that's just a sequential suspend call with extra steps.","**Beware lost exceptions** — an `async` whose `Deferred` you never `await` can swallow its exception."]},{t:"note",text:"launch = fire-and-forget side effects (returns Job) — most ViewModel work. async = concurrent results you'll await/awaitAll. For a single result, just call the suspend function sequentially. Don't async-then-immediately-await (pointless), and don't leave an async's Deferred un-awaited (its exception can be swallowed)."}]},{level:"senior",q:"How do you name coroutines and improve debuggability?",a:[{t:"p",text:'Add a `CoroutineName("...")` to the context so the coroutine has a readable name in stack traces and logs, and enable coroutine debug mode (`-Dkotlinx.coroutines.debug` or `DEBUG_PROPERTY_NAME`) which appends coroutine ids/names to thread names and improves stack traces with the async call chain.'},{t:"code",title:"Named coroutine + debug",code:`scope.launch(CoroutineName("sync-worker") + Dispatchers.IO) { sync() }
// Enable debug (adds coroutine name/id to thread names, better stacktraces):
// JVM flag: -Dkotlinx.coroutines.debug
// or System.setProperty(DEBUG_PROPERTY_NAME, DEBUG_PROPERTY_VALUE_ON)`},{t:"list",items:["**`CoroutineName`** — a context element giving the coroutine a label in logs/traces.","**Debug mode** — adds coroutine id/name to thread names and reconstructs the async stack trace (the chain of suspend calls).","**`kotlinx-coroutines-debug` (DebugProbes)** — dump all active coroutines and their states/stacks; great for finding stuck/leaked coroutines.","**Structured names** — name long-lived scopes' coroutines so production logs are traceable."]},{t:"note",text:'Add CoroutineName("...") to the context for readable labels, and enable coroutine debug mode (-Dkotlinx.coroutines.debug) for coroutine ids in thread names and reconstructed async stack traces. Use DebugProbes (kotlinx-coroutines-debug) to dump active coroutines and find stuck/leaked ones.'}]},{level:"junior",q:"What dispatcher do viewModelScope and lifecycleScope use by default, and why does it matter?",a:[{t:"p",text:"Both default to `Dispatchers.Main.immediate`. That means coroutines launched on them start on the main thread — so you can update UI/state directly without switching — and `immediate` means if you're *already* on the main thread, execution isn't re-dispatched (no extra frame delay). Heavy work must still be moved off with `withContext(IO/Default)`."},{t:"list",items:["**`Main.immediate`** — runs on the main thread; safe for UI/state updates immediately after a suspend call returns.","**`immediate` optimization** — skips re-dispatching when already on Main, avoiding a needless post to the message queue.","**Still offload heavy work** — the default being Main means *you* must `withContext(IO/Default)` for I/O or CPU work.","**Why it matters** — you can write `viewModelScope.launch { state = repo.load() }` and the state assignment is main-safe, provided `repo.load()` is main-safe internally."]},{t:"note",text:"Both default to Dispatchers.Main.immediate — coroutines start on the main thread (safe for UI/state updates) and 'immediate' skips re-dispatch when already on Main. But heavy work still needs withContext(IO/Default). This is why viewModelScope.launch { state = repo.load() } is main-safe when the repo is main-safe."}]},{level:"senior",q:"How do you structure parallel work and combine results in a ViewModel operation?",a:[{t:"p",text:"Launch one coroutine from `viewModelScope` for the operation, and inside it use `coroutineScope`/`async` to fan out independent calls, combine the results, and update state once. Keep the threading main-safe (repositories switch to IO), so the ViewModel code stays on Main and can set state directly."},{t:"code",title:"Parallel fan-out in a ViewModel",code:`fun load() {
    viewModelScope.launch {
        _state.value = Loading
        try {
            val result = coroutineScope {
                val profile = async { repo.profile() }   // concurrent
                val orders  = async { repo.orders() }
                Screen(profile.await(), orders.await())
            }
            _state.value = Content(result)
        } catch (e: Exception) {
            _state.value = Error(e.message)
        }
    }
}`},{t:"list",items:["**Outer `launch`** — one coroutine per user action, tied to `viewModelScope`.","**Inner `coroutineScope` + `async`** — structured concurrency for the parallel calls; a failure cancels siblings and is caught by the outer try/catch.","**Update state once** — set Loading, then Content/Error, avoiding intermediate flicker.","**Main-safe repos** — no manual dispatcher juggling in the ViewModel."]},{t:"note",text:"One viewModelScope.launch per action; inside, coroutineScope { async … async … } for parallel independent calls, combine, then set state once (Loading→Content/Error). Structured concurrency means one failure cancels siblings and is caught by the outer try/catch. Keep repos main-safe so the VM stays on Main."}]},{level:"senior",q:"What does the '+' operator do when building a coroutine context (Job() + Dispatchers.IO)?",a:[{t:"p",text:'`CoroutineContext` is an indexed set of elements, and `+` *combines* two contexts into one — right-side elements override same-keyed left-side ones. So `Job() + Dispatchers.IO + CoroutineName("x")` builds a context containing that Job, that dispatcher, and that name. It\'s how you assemble the configuration for a scope or a builder.'},{t:"code",title:"Combining context elements",code:`val ctx = SupervisorJob() + Dispatchers.IO + CoroutineName("sync") + handler
val scope = CoroutineScope(ctx)
// Override just the dispatcher for one launch:
scope.launch(Dispatchers.Default) { }   // Default replaces IO for this child`},{t:"list",items:["**`+` merges contexts** — combine Job, dispatcher, name, exception handler into one context.","**Right overrides left** — for the same key (e.g. two dispatchers), the rightmost wins.","**Inheritance + override** — children inherit the parent context; passing a context to a builder overrides those elements for that child.","**Each element has a `Key`** — you retrieve elements with `context[Job]`, `context[CoroutineDispatcher]`, etc."]},{t:"note",text:"CoroutineContext is an indexed element set; + combines contexts with right-overrides-left for same-key elements (e.g. dispatcher). Assemble a scope's config as Job + Dispatcher + Name + Handler. Children inherit the parent context; a builder's context arg overrides specific elements for that child. Retrieve via context[Key]."}]},{level:"senior",q:"How do you implement a 'cancel the previous job when a new one starts' pattern?",a:[{t:"p",text:"Keep a reference to the current `Job`, and when starting a new operation, cancel the old one first. This is common for search-as-you-type, refresh buttons, or any action where a newer request should supersede an in-flight one. (For flows, `flatMapLatest`/`collectLatest` do this declaratively.)"},{t:"code",title:"Cancel-previous with a Job field",code:`private var searchJob: Job? = null
fun search(query: String) {
    searchJob?.cancel()                       // cancel the in-flight search
    searchJob = viewModelScope.launch {
        delay(300)                            // debounce
        _results.value = repo.search(query)
    }
}`},{t:"list",items:["**Hold the `Job`** — cancel it before launching the replacement.","**Debounce with `delay`** — a cancelled coroutine's `delay` throws immediately, so rapid input cancels before the network call fires.","**Flow alternative** — model the query as a `StateFlow` and use `debounce` + `flatMapLatest`, which cancels the previous search automatically.","**Avoid races** — cancelling ensures stale results can't overwrite newer ones."]},{t:"note",text:"Store the current Job; on a new action, oldJob.cancel() then relaunch — the cancel-previous pattern for search-as-you-type/refresh. delay-based debounce cancels cleanly before the network fires. The declarative Flow equivalent is a query StateFlow with debounce + flatMapLatest (auto-cancels the prior search)."}]},{level:"junior",q:"What is the difference between cancelling a whole scope and cancelling one child Job?",a:[{t:"p",text:"Cancelling a single `Job` (returned by `launch`) stops just that coroutine (and its children). Cancelling the *scope* (`scope.cancel()`) cancels *all* coroutines in it and puts the scope's Job into a cancelled state — after which the scope can't launch new coroutines. Use the former to stop one operation; the latter for teardown."},{t:"list",items:["**`job.cancel()`** — stops that coroutine and its child coroutines; the scope stays usable for new launches.","**`scope.cancel()`** — cancels every coroutine in the scope *and* the scope's own Job; the scope is now dead (further `launch` calls do nothing/throw).","**`scope.coroutineContext.cancelChildren()`** — cancels all children but keeps the scope alive (usable again).","**Choosing** — cancel a `Job` to abort one task; `cancelChildren()` to reset all tasks but reuse the scope; `cancel()` for permanent teardown."]},{t:"note",text:"job.cancel() stops one coroutine (and its children), scope stays usable. scope.cancel() cancels ALL its coroutines and kills the scope (no more launches). scope.coroutineContext.cancelChildren() cancels all children but keeps the scope alive. Use job.cancel for one task, cancelChildren to reset, cancel() for teardown."}]},{level:"senior",q:"Why is a manually-created scope in a ViewModel usually a mistake when viewModelScope exists?",a:[{t:"p",text:"`viewModelScope` is already lifecycle-aware — it's cancelled in `onCleared()` automatically and uses a `SupervisorJob` + `Dispatchers.Main.immediate`. Creating your own `CoroutineScope` in a ViewModel means you must remember to cancel it in `onCleared()` yourself; forgetting leaks coroutines past the ViewModel's death. There's rarely a reason to not just use `viewModelScope`."},{t:"list",items:["**`viewModelScope` auto-cancels** — no manual teardown, no leak risk.","**Sensible defaults** — `SupervisorJob` (children independent) + `Main.immediate`.","**A manual scope must be cancelled in `onCleared`** — easy to forget, causing leaks.","**Legit exceptions** — you might create a *child* scope for a specific sub-lifecycle, but derive it from `viewModelScope` (e.g. `viewModelScope + Job()`) so it's still tied to the ViewModel, and cancel that child when its sub-task ends."]},{t:"note",text:"viewModelScope is already lifecycle-aware (auto-cancelled in onCleared, SupervisorJob + Main.immediate), so a hand-rolled scope in a ViewModel just adds a manual-cancel obligation you can forget → leaks. Rare valid case: a sub-scope derived from viewModelScope for a shorter-lived sub-task, cancelled when that task ends."}]},{level:"junior",q:"How do you wait for multiple launched Jobs to all finish?",a:[{t:"p",text:"Collect the `Job`s and `join()` each (or `joinAll(jobs)`). More idiomatically, wrap the launches in a `coroutineScope { }` — it doesn't return until all child coroutines complete, so you don't have to track and join each one manually."},{t:"code",title:"Waiting for all children",code:`// Explicit joinAll
val jobs = items.map { scope.launch { process(it) } }
jobs.joinAll()

// Structured: coroutineScope waits for all children automatically
coroutineScope {
    items.forEach { launch { process(it) } }
}   // returns only after every child finishes`},{t:"list",items:["**`joinAll(jobs)`** — suspend until every listed Job completes.","**`coroutineScope { }`** — implicitly waits for all children before returning (preferred; structured).","**Failure behavior** — in `coroutineScope`, one child failing cancels the others and rethrows; use `supervisorScope` to isolate failures."]},{t:"note",text:"joinAll(jobs) waits for a list of Jobs; or just wrap the launches in coroutineScope { } which returns only after all children finish (preferred — structured). In coroutineScope one failure cancels siblings and rethrows; supervisorScope isolates failures."}]},{level:"senior",q:"What is a CoroutineExceptionHandler, and where do you install it?",a:[{t:"p",text:"A `CoroutineExceptionHandler` is a context element that catches *uncaught* exceptions from coroutines — a last-resort handler for `launch` coroutines whose exceptions weren't handled inside. It's installed on a *scope* (or the root coroutine), and only works there — installing it on an inner child coroutine has no effect, because the exception propagates to the parent first."},{t:"code",title:"Handler on the scope",code:`val handler = CoroutineExceptionHandler { _, e -> log("Uncaught: \${e.message}") }
val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main + handler)
scope.launch { risky() }   // if risky() throws uncaught, handler runs`},{t:"list",items:["**Catches uncaught `launch` exceptions** — a global safety net (log/report), not a replacement for local try/catch.","**Install on the scope/root** — installing on an inner `launch` doesn't work; the exception has already propagated to the parent.","**Doesn't work for `async`** — `async` exposes its exception via `await()`, so the handler isn't invoked for it (the `Deferred` holds it).","**Pairs with `SupervisorJob`** — so one child's handled failure doesn't cancel siblings."]},{t:"note",text:"CoroutineExceptionHandler is a context element catching UNCAUGHT launch exceptions — a scope-level safety net (log/report). Install on the scope/root, not inner children (exceptions propagate to the parent first). It doesn't apply to async (use await's try/catch). Pair with SupervisorJob to isolate failures."}]},{level:"junior",q:"Can you nest coroutine builders, and what is the parent-child relationship?",a:[{t:"p",text:"Yes — launching a coroutine inside another creates a *child* coroutine. The child inherits the parent's context (dispatcher, etc.), the parent won't complete until all children do, cancelling the parent cancels the children, and (with a regular Job) a child's failure cancels the parent. This parent-child tree is the backbone of structured concurrency."},{t:"code",title:"Nested builders form a tree",code:`scope.launch {                 // parent
    launch { taskA() }         // child 1
    launch { taskB() }         // child 2
    // parent 'completes' only after taskA and taskB finish
}`},{t:"list",items:["**Children inherit context** — dispatcher, name, etc., unless overridden in the builder.","**Parent waits** — it reaches Completed only after all children do (the 'Completing' state).","**Cancellation cascades down** — cancelling the parent cancels all descendants.","**Failure cascades up** — with a normal Job, a child exception cancels the parent and siblings; a `SupervisorJob` breaks that upward cascade."]},{t:"note",text:"Nesting builders creates child coroutines: children inherit the parent's context, the parent completes only after all children finish, cancelling the parent cancels children (down), and a child failure cancels the parent+siblings (up) unless a SupervisorJob isolates it. This tree is structured concurrency's backbone."}]}],bS={id:"builders-scopes",title:"Builders & Scopes",tagline:"launch/async/runBlocking, CoroutineScope & lifetime, viewModelScope/lifecycleScope, coroutineScope vs supervisorScope, and why GlobalScope is an anti-pattern.",tags:["Coroutines","launch","async","CoroutineScope","viewModelScope"],content:wS,qa:vS},kS=[{heading:"What structured concurrency means",blocks:[{t:"p",text:"**Structured concurrency** is the principle that coroutines form a **parent-child tree**, and a parent doesn't complete until all its children complete. Every coroutine launched inside another (or inside a scope) becomes a *child* of that scope's Job. This tree gives three guarantees that make coroutines safe: **no leaks** (children are tracked and can't be forgotten), **cancellation propagates** (cancel a parent → all children cancelled), and **errors propagate** (a child failing can cancel the parent and siblings). It's the opposite of raw threads/callbacks, where started work can be silently orphaned."},{t:"list",items:["**No orphans**: because a scope waits for its children, you can't accidentally leave a coroutine running after the code that started it has 'finished' — the scope literally won't complete until they do.","**Cancellation flows down**: cancelling a scope or parent Job cancels the entire subtree beneath it.","**Errors flow up (by default)**: an unhandled exception in a child cancels its parent, which cancels the other children — failures don't get lost.","**Where it comes from**: builders (`launch`/`async`) are extensions on `CoroutineScope`, and each creates a child Job of the scope's Job — so the tree is built automatically as you launch coroutines."]}]},{heading:"The Job — a handle and a tree node",blocks:[{t:"p",text:"A **`Job`** is the handle to a coroutine's lifecycle. `launch` returns one; every coroutine *has* one in its context. A Job is both a *control handle* (cancel it, wait for it) and a *node in the parent-child tree* (it has a parent Job and child Jobs)."},{t:"code",title:"Using a Job",code:`val job = scope.launch {
    doLongWork()
}

job.cancel()                 // request cancellation
job.join()                   // suspend until it finishes (or is cancelled)
job.cancelAndJoin()          // cancel then wait for it to actually stop

if (job.isActive) { }        // still running
if (job.isCancelled) { }     // cancelled
if (job.isCompleted) { }     // finished (normally or cancelled)

job.invokeOnCompletion { cause -> /* cleanup */ }`},{t:"list",items:["**States**: a Job moves through *New → Active → Completing → Completed*, or *Cancelling → Cancelled*. `isActive`, `isCancelled`, `isCompleted` expose this.","**`join()`** suspends until the Job completes — used to wait for a coroutine. **`cancel()`** requests cancellation (cooperative). **`cancelAndJoin()`** does both.","**A parent Job doesn't complete until all children complete** — this is the mechanism behind 'the scope waits for its children'. Even after the parent's own code finishes, it's in *Completing* until children are done.","**`Deferred<T>`** (from `async`) is a `Job` that also carries a result via `await()`."]}]},{heading:"The Job hierarchy and how failure propagates",blocks:[{t:"p",text:"Jobs form a tree. When something goes wrong, the *type* of Job at each level decides what happens — and this is the crux of both this topic and the exception-handling topic:"},{t:"list",items:["**With a regular `Job`**: a child's uncaught exception *cancels its parent*, which *cancels all the parent's other children*, and the failure propagates up. One failure tears down the whole subtree. This is the default and is right when the work is interdependent.","**Cancellation is different from failure**: cancelling a *child* (via `child.cancel()`) does **not** cancel the parent or siblings — cancellation is a normal, expected outcome. Only an *unhandled exception* (a failure) propagates upward and takes down siblings.","**Parent cancellation cascades down**: cancel the parent and every descendant is cancelled — regardless of Job type."]},{t:"code",title:"One child's failure cancels its siblings (regular Job)",code:`coroutineScope {                 // regular Job
    launch { delay(100); error("boom") }   // fails
    launch { delay(1000); doWork() }        // CANCELLED because sibling failed
}   // coroutineScope rethrows the exception`}]},{heading:"SupervisorJob — isolating child failures",blocks:[{t:"p",text:"A **`SupervisorJob`** changes the failure rule: children fail **independently** — one child's exception does **not** cancel the parent or the siblings. It only changes *downward* propagation of failure (child → parent); parent cancellation still cascades to all children."},{t:"code",title:"SupervisorJob keeps siblings alive",code:`val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main)

scope.launch { error("boom") }    // fails alone
scope.launch { doOtherWork() }    // KEEPS RUNNING — not cancelled by the sibling

// Same idea in a suspend function:
supervisorScope {
    launch { mightFail() }        // failure isolated
    launch { alsoIndependent() }  // survives
}`},{t:"list",items:["**Where you meet it**: `viewModelScope` uses a `SupervisorJob` — so one failed coroutine in a ViewModel doesn't tear down the whole ViewModel's scope. Same for `lifecycleScope`.","**The catch**: with a SupervisorJob, a child's exception is *not* propagated to a parent, so it becomes 'uncaught' unless *that child* handles it (try/catch inside, or a `CoroutineExceptionHandler`). Independence means each child owns its error handling.","**`SupervisorJob()` (the scope) vs `supervisorScope { }` (the suspend function)**: the former builds a long-lived scope; the latter creates a temporary supervised scope within a suspend function. Same failure semantics, different lifetime.","**Placement subtlety**: a `SupervisorJob` only supervises the coroutines that are its *direct* children. If you `launch` a coroutine that itself `launch`es more, those grandchildren are under a *regular* Job (the child's) unless you nest another supervisor — so a supervisor at the top doesn't magically isolate failures deep in the tree."]}]},{heading:"Putting it together — the mental model",blocks:[{t:"table",headers:["Event","Regular Job","SupervisorJob"],rows:[["Child throws uncaught exception","cancels parent + all siblings","isolated — siblings unaffected"],["Child is cancelled (not failed)","siblings unaffected","siblings unaffected"],["Parent is cancelled","all children cancelled","all children cancelled"],["Who handles a child's error","propagates up to scope","the child itself must"]]},{t:"note",text:`Interview-ready summary: "Coroutines form a parent-child Job tree. A regular Job means a child failure cancels the parent and siblings (all-or-nothing); a SupervisorJob isolates child failures (independent). Parent cancellation always cascades to all children. Cancelling a child (vs it failing) never affects siblings. viewModelScope uses a SupervisorJob so one failure doesn't kill the whole ViewModel scope."`}]}],xS=[{level:"junior",q:"What is structured concurrency?",a:[{t:"p",text:"**The concept**: structured concurrency means coroutines form a **parent-child tree**, and a parent coroutine (or scope) does not complete until all of its children have completed. Every coroutine you launch inside a scope becomes a child of that scope. This structure is what makes coroutines safe compared to raw threads or callbacks."},{t:"list",items:["**No leaked work**: because a scope waits for its children, you can't accidentally start a coroutine and forget about it — the scope tracks it and won't finish until it does.","**Cancellation propagates down**: cancel a scope or parent, and all its children are cancelled automatically. Leave a screen → its scope cancels → all its coroutines stop.","**Errors propagate up**: by default, an unhandled exception in a child cancels its parent and siblings, so failures aren't silently lost."]},{t:"p",text:"The practical payoff: you get automatic cleanup and error handling for free. Contrast the old callback world, where a started network request could keep running and call back into a destroyed screen because nothing tied its lifetime to anything. Structured concurrency ties every coroutine's lifetime to a scope, so 'this work belongs to this screen' is enforced by the framework."}]},{level:"junior",q:"What is a Job in coroutines?",a:[{t:"p",text:"**The concept**: a `Job` is the handle to a coroutine's lifecycle. `launch` returns one, and every coroutine has one in its context. It serves two roles: a *control handle* you can use to cancel or wait for the coroutine, and a *node in the parent-child tree* that connects a coroutine to its parent and children."},{t:"code",title:"What you do with a Job",code:`val job = scope.launch { work() }
job.cancel()          // request cancellation
job.join()            // suspend until it completes
job.cancelAndJoin()   // cancel and wait for it to stop
job.isActive          // still running?`},{t:"list",items:["A Job has states: New → Active → Completing → Completed, or Cancelling → Cancelled, exposed via `isActive`/`isCancelled`/`isCompleted`.","`cancel()` requests cooperative cancellation; `join()` waits for completion; `invokeOnCompletion { }` runs a callback when it finishes.","A parent Job stays in a 'completing' state until all its child Jobs finish — this is the mechanism behind 'a scope waits for its children'.","`Deferred<T>` (returned by `async`) is a Job that also carries a result via `await()`."]}]},{level:"junior",q:"What is a SupervisorJob and where do you encounter it?",a:[{t:"p",text:"**The concept**: normally, if one child coroutine fails with an unhandled exception, it cancels its parent and all sibling coroutines. A `SupervisorJob` changes that rule so children fail **independently** — one child throwing does *not* cancel its siblings or the parent. It isolates failures."},{t:"code",title:"Independent children",code:`val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main)
scope.launch { error("fails") }   // this one fails
scope.launch { keepsWorking() }   // NOT cancelled by the sibling's failure`},{t:"p",text:"**Where you encounter it**: `viewModelScope` and `lifecycleScope` both use a `SupervisorJob` internally — that's deliberate, so that one failed coroutine (say a failed analytics call) doesn't tear down the entire ViewModel's scope and cancel all its other work. You also use it explicitly via `supervisorScope { }` when launching several independent operations where one failing shouldn't stop the others (e.g. loading independent dashboard widgets). The one caveat: because a SupervisorJob doesn't propagate a child's error to the parent, each child must handle its own exceptions — otherwise the error goes uncaught."}]},{level:"junior",q:"If you cancel a scope, what happens to the coroutines running in it?",a:[{t:"p",text:"**They're all cancelled.** Cancellation propagates *down* the Job tree: cancelling a scope cancels its Job, which cancels every child coroutine, and their children, all the way down. This is the mechanism that makes lifecycle scopes safe — when a ViewModel is cleared, `viewModelScope` is cancelled, and every coroutine launched in it stops."},{t:"p",text:"Two important details: (1) cancellation is *cooperative* — a coroutine only actually stops at a suspension point (or where it checks `isActive`/`ensureActive`), so a tight non-suspending loop must check cancellation manually to respond. (2) Once a scope is cancelled, it's dead — you can't launch new coroutines in it (they'll be cancelled immediately). This is why you don't cancel `viewModelScope` yourself; the framework does it at the right time. The takeaway: scope cancellation is the single lever that cleans up all associated work, which is exactly why binding coroutines to the right scope matters so much."}]},{level:"senior",q:"Walk through exactly how a failure propagates in a coroutine tree, and how SupervisorJob changes it.",a:[{t:"p",text:"**With a regular `Job`, failure propagates bidirectionally to tear down the subtree.** When a child throws an *unhandled* exception: (1) the child fails; (2) it propagates the exception *up* to its parent; (3) the parent, on receiving a child failure, cancels *itself*, which (4) cancels *all its other children* (siblings of the failed one); (5) the exception continues propagating up until it reaches the root, where it's handled by a `CoroutineExceptionHandler` or crashes. Net effect: one unhandled failure cancels the entire scope's subtree."},{t:"list",items:["**`SupervisorJob` breaks step 3→4**: a child failure is *not* propagated to the parent, so the parent isn't cancelled and siblings survive. Failure is contained to the failing child. But this means the exception has nowhere to auto-propagate — so it must be handled *at that child* (try/catch or a `CoroutineExceptionHandler` on that launch), or it's unhandled.","**Cancellation ≠ failure**: this whole propagation is for *failures* (unhandled exceptions). If you *cancel* a child yourself (`child.cancel()`), that's a normal `CancellationException` — it does **not** propagate up and does **not** cancel siblings. Cancellation is an expected outcome; failure is exceptional. Conflating them is a common mistake.","**Parent cancellation always cascades down** regardless of Job type: cancel the parent and all children die. SupervisorJob only affects the *upward* (child→parent) direction of *failures*, never downward cancellation.","**Depth matters**: a `SupervisorJob` only supervises its *direct* children. If a supervised child launches its own children with a regular Job (the default), a failure among *those* grandchildren cancels their parent (the supervised child) — the supervisor at the top doesn't reach down to isolate them. You'd need another `supervisorScope` at that level."]},{t:"code",title:"The two behaviors side by side",code:`// Regular Job: sibling cancelled by failure
coroutineScope {
    launch { throw IOException() }     // fails
    launch { delay(1000); log("A") }   // CANCELLED, never logs "A"
}

// SupervisorJob: sibling survives (but the failing child needs its own handler)
supervisorScope {
    launch(handler) { throw IOException() } // isolated, handled by 'handler'
    launch { delay(1000); log("A") }        // runs, logs "A"
}`}]},{level:"senior",q:"Why does viewModelScope use a SupervisorJob, and what would go wrong with a regular Job?",a:[{t:"p",text:"**Because a ViewModel typically runs multiple independent coroutines, and one failing shouldn't kill all the others — or the scope itself.** A ViewModel might have a coroutine observing the database, another handling a button-triggered refresh, another logging analytics. These are independent. With a `SupervisorJob`, if the analytics coroutine throws, it fails alone; the database observation and everything else keep working."},{t:"p",text:"**What would go wrong with a regular Job**: a regular Job propagates any child failure up to the scope, cancelling the scope and *all* its other coroutines. So a single unhandled exception in *any* coroutine — even a trivial fire-and-forget analytics call — would cancel the entire `viewModelScope`. After that, the scope is dead: the screen's data observation stops, and any new `viewModelScope.launch` would be cancelled immediately. The user's screen would silently stop updating because one unrelated coroutine failed. That's a terrible failure mode, so the framework uses a `SupervisorJob` to contain failures to the individual coroutine that caused them."},{t:"list",items:["**The tradeoff you inherit**: because `viewModelScope` supervises, a failing coroutine's exception won't propagate anywhere useful — it becomes uncaught (and can still crash via the default handler for `launch`). So *you* are responsible for handling errors inside each `viewModelScope.launch` (try/catch, or mapping failures into UI error state). The scope protects the *other* coroutines, not the failing one.","**Consistency**: `lifecycleScope` uses a `SupervisorJob` for the same reason — UI-layer coroutines are independent, and one failing shouldn't tear down the screen's whole coroutine scope."]}]},{level:"senior",q:"What's the difference between cancelling a coroutine and a coroutine failing with an exception, in terms of the tree?",a:[{t:"p",text:"**They're treated fundamentally differently by structured concurrency, and this trips people up constantly.** *Cancellation* is a normal, expected outcome — a coroutine being told 'stop, you're no longer needed'. *Failure* is an exceptional outcome — a coroutine throwing an unexpected exception. The Job tree responds to each differently:"},{t:"list",items:["**Cancelling a child** (`child.cancel()`): raises a `CancellationException` inside that child, which unwinds it. It does **not** propagate to the parent, and does **not** cancel siblings. The parent and other children carry on. This is why you can cancel one specific coroutine (e.g. an outdated search) without disturbing anything else.","**A child failing** (throwing a non-cancellation exception): with a regular Job, this *does* propagate up and cancel the parent and siblings (as covered above). Failure is 'something went wrong, tear down the related work'.","**Why `CancellationException` is special**: the coroutine framework treats it as the signal for normal cancellation, not an error. That's why you must **never swallow a `CancellationException`** in a catch block — catching `Exception` broadly and not rethrowing it makes the coroutine think it recovered from cancellation, breaking the whole cancellation mechanism (the coroutine keeps running when it should stop). Always rethrow `CancellationException` (or catch specific exceptions only)."]},{t:"code",title:"The catch that breaks cancellation",code:`// BUG: swallows CancellationException -> coroutine won't cancel properly
try { doWork() } catch (e: Exception) { log(e) }

// CORRECT: let CancellationException propagate
try { doWork() }
catch (e: CancellationException) { throw e }   // rethrow!
catch (e: Exception) { log(e) }`},{t:"p",text:"**The mental model**: cancellation flows *down* (parent cancels children) and is a normal outcome that doesn't disturb siblings; failure (with a regular Job) flows *up* and takes down the subtree. `CancellationException` is the plumbing for cancellation and must never be swallowed. Understanding this distinction is what separates 'I use coroutines' from 'I understand coroutines'."}]},{level:"junior",q:"What are the three core guarantees of structured concurrency?",a:[{t:"p",text:"Structured concurrency is captured by three guarantees that together make concurrent code predictable and leak-free: a parent waits for its children, cancellation propagates, and errors propagate. Every coroutine has a parent scope and can't outlive it."},{t:"list",items:["**Completion** — a scope/parent doesn't complete until *all* its child coroutines complete. No orphaned work escapes.","**Cancellation** — cancelling a scope cancels all its coroutines (and their children), recursively.","**Error propagation** — an unhandled failure in a child cancels its siblings and surfaces to the parent (unless a `SupervisorJob` isolates it)."]},{t:"p",text:"The practical upshot: concurrency is *owned* by scopes, not free-floating. You can reason about a block of concurrent work as a single unit that starts, finishes, cancels, and fails as a whole — which is why coroutines don't leak the way raw threads/callbacks do."},{t:"note",text:"Three guarantees: (1) completion — a parent waits for all children; (2) cancellation — cancelling a scope cancels all its coroutines; (3) error propagation — a child failure cancels siblings and reaches the parent (unless SupervisorJob). Result: concurrency is owned by scopes, so it's predictable and leak-free."}]},{level:"junior",q:"How is the coroutine parent-child Job tree formed?",a:[{t:"p",text:"When you `launch`/`async` inside a scope (or another coroutine), the new coroutine's `Job` becomes a *child* of the scope's Job (or the enclosing coroutine's Job). This happens automatically through context inheritance: the builder takes the parent context, and the child's new Job is linked to the parent Job. The result is a tree rooted at the scope's Job."},{t:"code",title:"The tree",code:`val scope = CoroutineScope(Job())      // root Job
scope.launch {                          // child of root
    launch { }                          // grandchild
    async { }                           // grandchild
}`},{t:"list",items:["**Automatic linkage** — the builder reads the parent context and installs the new Job as a child of the parent Job.","**Context inheritance** — the child inherits dispatcher/name/etc., but gets its *own* new Job (child of the parent's).","**Rooted at the scope** — the scope's Job is the root; cancelling it cancels the whole tree.","**Breaking the link** — passing a new `Job()` into a child builder detaches it from the tree (usually a bug)."]},{t:"note",text:"launch/async inside a scope makes the new coroutine's Job a child of the scope's (or enclosing coroutine's) Job, via context inheritance — the child inherits dispatcher/name but gets its own new Job linked to the parent. The tree is rooted at the scope's Job. Passing a fresh Job() into a child detaches it (a bug)."}]},{level:"senior",q:"What is the 'Completing' state, and why does a parent wait for its children?",a:[{t:"p",text:"When a parent coroutine's own body finishes but it still has active children, its Job enters the *Completing* state — it's done with its own work but not yet Completed, because structured concurrency requires it to wait for all children first. Only when every child finishes does the parent transition to Completed."},{t:"code",title:"Parent waits in Completing",code:`coroutineScope {
    launch { delay(1000); println("child done") }
    println("parent body done")   // parent now 'Completing', not finished
}   // coroutineScope returns only after the child completes (~1s)`},{t:"list",items:["**Completing** — body finished, awaiting children; not yet Completed.","**Why** — guarantees no child outlives its parent scope (the completion guarantee).","**Effect on `coroutineScope`/`launch`** — the enclosing scope doesn't return until children finish.","**Failure during Completing** — if a child fails while the parent is Completing, the parent still fails/cancels accordingly."]},{t:"note",text:"'Completing' = the parent's own body finished but it has active children, so it waits (not yet Completed) — enforcing that no child outlives its parent. That's why a coroutineScope { } block returns only after all launched children finish, even if the block's last line already ran."}]},{level:"senior",q:"What is the behavioral difference between Job() and SupervisorJob() in a scope?",a:[{t:"p",text:"With a regular `Job()`, failure propagates *bidirectionally* among the family: a child's failure cancels the parent and all siblings. With a `SupervisorJob()`, failure propagates *downward only*: a child failing doesn't cancel its siblings or the parent — each child fails independently. The choice depends on whether children are interdependent or independent."},{t:"code",title:"Sibling isolation",code:`// Regular Job: child B failing cancels A and the scope
val scope = CoroutineScope(Job())
// SupervisorJob: child B failing leaves A running
val supervisor = CoroutineScope(SupervisorJob())
supervisor.launch { taskA() }   // keeps running
supervisor.launch { failB() }   // fails alone`},{t:"list",items:["**`Job()`** — one child's failure cancels siblings and the parent (all-or-nothing). Right when the tasks form one logical unit.","**`SupervisorJob()`** — children fail independently; siblings continue. Right for independent tasks (UI screen sections, a pool of workers).","**Still needs handling** — with `SupervisorJob`, each `launch`'s failure needs a local try/catch or a `CoroutineExceptionHandler`; it isn't swallowed automatically.","**`supervisorScope { }`** — the scoped-function equivalent for a block."]},{t:"note",text:"Job(): a child failure cancels siblings + parent (all-or-nothing) — for interdependent tasks. SupervisorJob(): failures propagate down only, siblings survive — for independent tasks (screen sections, worker pools). SupervisorJob still needs per-child try/catch or a handler. supervisorScope{} is the block form."}]},{level:"senior",q:"Why is passing a new Job() into a child launch a structured-concurrency bug?",a:[{t:"p",text:"Passing `launch(Job()) { }` overrides the child's Job with a brand-new root Job that has *no parent link*. This detaches the coroutine from the scope's tree — the scope no longer waits for it, cancelling the scope no longer cancels it, and its failures no longer propagate. You've silently created an unstructured, leak-prone coroutine."},{t:"code",title:"The detach bug",code:`// BUG: new Job() breaks the parent link — scope won't cancel this
scope.launch(Job()) { longWork() }
// Cancelling scope leaves longWork() running (leak)

// If you truly need independence within the scope, use SupervisorJob as the SCOPE's job,
// or a supervisorScope — not a detached Job on the child.`},{t:"list",items:["**New `Job()` = new root** — no parent, so structured guarantees don't apply to that child.","**Symptoms** — scope cancellation doesn't stop it; it outlives the owner (leak); its exceptions don't reach the scope.","**Intended alternative** — for isolating failures use `SupervisorJob` *as the scope's* Job or `supervisorScope { }`, which keep the parent link while changing failure propagation.","**Rule** — don't override a child's Job unless you deliberately want to detach it (very rare)."]},{t:"note",text:"launch(Job()) { } replaces the child's Job with a parentless root, detaching it from the tree: the scope won't wait for or cancel it and its failures don't propagate — an unstructured leak. To isolate failures, use SupervisorJob as the scope's Job or supervisorScope{} (which keep the parent link). Never override a child's Job unless you truly mean to detach."}]},{level:"senior",q:"How does cancellation propagate down the tree, and when does cleanup run?",a:[{t:"p",text:"Cancelling a coroutine cancels *all* its descendants: the cancellation flows down the Job tree, and each coroutine's next suspension point throws `CancellationException`, unwinding it. `finally` blocks (and `try/finally` around resources) run during this unwinding, so cleanup happens — but any *suspending* cleanup must use `withContext(NonCancellable)` since the coroutine is already cancelled."},{t:"code",title:"Cleanup during cancellation",code:`val job = scope.launch {
    try {
        useResource()      // cancelled here at a suspension point
    } finally {
        // runs during cancellation; suspend cleanup needs NonCancellable:
        withContext(NonCancellable) { saveProgress() }
        closeResource()
    }
}
job.cancel()   // cancels this coroutine AND its children`},{t:"list",items:["**Downward propagation** — cancelling a parent cancels every child/grandchild.","**Thrown at suspension points** — a cancelled coroutine throws `CancellationException` at its next `delay`/suspend call.","**`finally` runs** — resource cleanup executes during unwinding.","**Suspending cleanup** — must be wrapped in `withContext(NonCancellable)` or it's immediately cancelled too."]},{t:"note",text:"Cancellation flows down the tree (parent cancel → all descendants); each coroutine throws CancellationException at its next suspension point and unwinds, running finally blocks. Suspending cleanup in finally must use withContext(NonCancellable) — otherwise it's cancelled immediately since the coroutine is already cancelled."}]},{level:"senior",q:"How do you run cleanup that must complete even during cancellation (NonCancellable)?",a:[{t:"p",text:"Wrap the suspending cleanup in `withContext(NonCancellable) { }`. A cancelled coroutine throws `CancellationException` at every suspension point, so a suspend call in a `finally` block would be cancelled before it runs. `NonCancellable` is a special Job that ignores cancellation, letting that critical cleanup finish."},{t:"code",title:"Guaranteed cleanup",code:`try {
    stream()
} finally {
    withContext(NonCancellable) {
        flushBuffer()      // suspend cleanup that must complete
        db.saveState()
    }
}`},{t:"list",items:["**`withContext(NonCancellable)`** — makes the block immune to the ongoing cancellation so suspend cleanup runs.","**Use sparingly** — only for genuinely must-finish cleanup (save progress, release a lock, flush). It defeats cancellation, so keep it short.","**Non-suspending cleanup** — doesn't need it; plain `finally` code (closing a stream) runs fine.","**Don't do long work in it** — it blocks timely cancellation; keep it minimal."]},{t:"note",text:"Wrap suspending finally-block cleanup in withContext(NonCancellable) { } — a cancelled coroutine throws at suspension points, so without it the cleanup is cancelled before running. Use it only for short, must-finish work (save progress, release lock, flush). Non-suspending cleanup runs in a normal finally without it."}]},{level:"junior",q:"Why is GlobalScope considered 'unstructured', and what breaks?",a:[{t:"p",text:"`GlobalScope` is a scope with no parent and no lifecycle — coroutines launched in it live for the entire process and aren't tied to any component. That breaks structured concurrency: nothing waits for them, nothing cancels them when a screen dies, and their failures don't propagate anywhere useful. The result is leaks and orphaned work."},{t:"list",items:["**No lifecycle** — `GlobalScope` coroutines run until they finish or the process dies; leaving a screen doesn't cancel them.","**Leaks** — they hold references (the Activity/ViewModel), preventing garbage collection.","**No propagation** — errors and cancellation don't flow to any owner.","**Fix** — use a lifecycle-bound scope (`viewModelScope`/`lifecycleScope`) or a custom scope you cancel; reserve `GlobalScope` for truly app-lifetime work (very rare, and even then a purpose-built scope is clearer)."]},{t:"note",text:"GlobalScope has no parent and no lifecycle, so its coroutines run for the whole process — nothing waits for, cancels, or receives failures from them. That's 'unstructured': leaks and orphaned work. Use viewModelScope/lifecycleScope or a cancellable custom scope instead; GlobalScope is almost never right."}]},{level:"senior",q:"What happens to sibling coroutines when one child throws (regular Job)?",a:[{t:"p",text:"With a regular `Job` (as in `coroutineScope`), one child throwing an unhandled exception cancels the *entire* scope: the exception propagates to the parent, which cancels all other children and then rethrows. This all-or-nothing behavior is correct when the children form one logical operation (if one part fails, the whole thing is invalid)."},{t:"code",title:"Sibling cancellation",code:`coroutineScope {
    val a = async { slowSuccess() }   // gets cancelled when b fails
    val b = async { throw IOException() }
    a.await(); b.await()              // coroutineScope rethrows the IOException
}`},{t:"list",items:["**One fails → all cancelled** — the parent cancels surviving siblings.","**Exception rethrown** — `coroutineScope` propagates it to the caller, so a surrounding try/catch handles it.","**Right for unit operations** — combining data where any failure invalidates the result.","**Use `supervisorScope`** — if you want siblings to survive one child's failure."]},{t:"note",text:"With a regular Job (coroutineScope), one child's unhandled exception cancels all siblings and rethrows to the caller — all-or-nothing, correct when the children are one logical operation. If a failed part shouldn't invalidate the others, use supervisorScope to isolate failures."}]},{level:"senior",q:"When multiple children fail, which exception is reported?",a:[{t:"p",text:"The *first* exception to reach the parent is reported (thrown/handled); subsequent exceptions from other children are *suppressed* and attached to the first as suppressed exceptions (accessible via `Throwable.suppressed`). This avoids losing information while giving a single primary cause. `CancellationException`s from the cascade are ignored (they're the mechanism, not a failure)."},{t:"list",items:["**First failure wins** — it becomes the reported cause and triggers cancellation of the rest.","**Others suppressed** — additional real exceptions are added as `suppressed` on the first (not silently dropped).","**`CancellationException` ignored** — the cancellations triggered in siblings aren't reported as failures.","**Inspecting** — `exception.suppressed` lists the secondary failures if you need them."]},{t:"note",text:"The first exception to reach the parent is the reported cause; later failures from other children are attached to it as suppressed exceptions (Throwable.suppressed), not lost. The CancellationExceptions from the cascade are ignored. So you get one primary cause plus recoverable detail on the rest."}]},{level:"junior",q:"How does structured concurrency prevent memory leaks?",a:[{t:"p",text:"Because every coroutine is owned by a scope and cannot outlive it, cancelling the scope (which lifecycle-bound scopes do automatically) stops all its coroutines — releasing the references they hold. There's no way to accidentally start work that keeps running after its owner is gone, which is exactly how threads/callbacks/`AsyncTask` leaked."},{t:"list",items:["**Ownership** — coroutines belong to a scope; no free-floating work.","**Automatic cancellation** — `viewModelScope`/`lifecycleScope` cancel on destroy, stopping coroutines and freeing captured references.","**Contrast** — a raw `Thread` or callback can run forever holding an Activity reference; a scoped coroutine can't.","**Caveat** — you *can* still leak by using `GlobalScope` or detaching with `Job()`; structured usage is what prevents leaks."]},{t:"note",text:"Every coroutine is owned by a scope and can't outlive it, so cancelling the scope (automatic for viewModelScope/lifecycleScope) stops all coroutines and releases their captured references — no orphaned work holding the Activity. You only reintroduce leaks by escaping structure (GlobalScope, detached Job())."}]},{level:"senior",q:"A coroutine starts background work that outlives the screen. Is that structured, and how do you fix it?",a:[{t:"p",text:"It depends on *which scope* launched it. If the work must finish regardless of the screen (an upload that should complete even if the user navigates away), a screen-scoped coroutine is the *wrong* owner — it'll be cancelled on navigation. The fix is to move that work to a longer-lived owner: WorkManager for guaranteed/deferrable work, or an application/repository-scoped coroutine — not to detach it with `GlobalScope`."},{t:"list",items:["**Diagnose the ownership** — should this work die with the screen or outlive it?","**Should die with screen** — keep it in `viewModelScope`; cancellation on navigation is correct.","**Must outlive the screen (must-complete)** — use `WorkManager` (guaranteed, survives process death) for real background tasks.","**App-lifetime in-memory work** — an application-scoped `CoroutineScope` (owned by a singleton/DI, cancelled on app teardown) — deliberate and cancellable, unlike `GlobalScope`.","**Anti-pattern** — `GlobalScope.launch` to 'keep it alive'; it's unstructured and unmanaged."]},{t:"note",text:"Ask who should own the work. If it should die with the screen, viewModelScope (cancel-on-navigate) is right. If it must complete regardless, that's the wrong scope: use WorkManager (guaranteed, survives process death) or a deliberate application-scoped CoroutineScope — not GlobalScope, which is unstructured and unmanaged."}]},{level:"junior",q:"What is invokeOnCompletion, and how does it relate to the Job tree?",a:[{t:"p",text:"`Job.invokeOnCompletion { cause -> }` registers a callback that fires when the Job completes — normally (`cause == null`), by cancellation (`cause is CancellationException`), or by failure (`cause` is the exception). It's a low-level hook for cleanup or observing a coroutine's end without joining, and it fires only after the Job (and its children) are done."},{t:"code",title:"Completion callback",code:`val job = scope.launch { work() }
job.invokeOnCompletion { cause ->
    when (cause) {
        null -> log("completed")
        is CancellationException -> log("cancelled")
        else -> log("failed: \${cause.message}")
    }
}`},{t:"list",items:["**Fires on any completion** — success, cancellation, or failure; the `cause` distinguishes them.","**After children finish** — consistent with the Job completing only once its subtree does.","**Non-suspending** — the handler runs synchronously and can't suspend; keep it light.","**Uses** — resource cleanup, logging, decrementing counters, unregistering — without needing to `join`."]},{t:"note",text:"invokeOnCompletion { cause -> } fires when a Job completes: cause null (success), CancellationException (cancelled), or the exception (failed). It runs after the Job's subtree finishes, is non-suspending (keep it light), and is handy for cleanup/logging without joining."}]},{level:"senior",q:"How do you inspect a Job's children, and when is that useful?",a:[{t:"p",text:"A `Job` exposes its `children` (a `Sequence<Job>`), letting you iterate active child coroutines. It's rarely needed in app code, but useful for diagnostics (how many children are running), for cancelling all children while keeping the scope alive (`cancelChildren()`), or for waiting on them collectively."},{t:"code",title:"Working with children",code:`val parent = scope.coroutineContext[Job]!!
println("active children: \${parent.children.count()}")
parent.children.forEach { it.cancel() }         // cancel each child
parent.cancelChildren()                          // idiomatic: cancel all, keep scope usable`},{t:"list",items:["**`job.children`** — a sequence of the current child Jobs.","**`cancelChildren()`** — cancels all children but leaves the parent/scope active (reusable), unlike `cancel()`.","**Diagnostics** — count/inspect running coroutines when debugging leaks or hangs.","**Rarely needed** — structured concurrency usually handles this; reach for it in tooling/edge cases."]},{t:"note",text:"Job.children is a Sequence<Job> of active children — useful for diagnostics or bulk operations. cancelChildren() cancels them all while keeping the scope usable (vs cancel(), which kills the scope). Rarely needed in app code; handy for debugging leaks/hangs or resetting a scope."}]},{level:"senior",q:"From the parent's perspective, how does a child completing normally differ from being cancelled?",a:[{t:"p",text:"A child that *completes normally* (or is cancelled) doesn't affect the parent — the parent just no longer waits on it. A child that *fails* (throws a non-cancellation exception) cancels the parent and siblings (with a regular Job). Cancellation is treated as a normal, expected end; failure is exceptional. This is why `CancellationException` is special-cased and never propagates as a failure."},{t:"list",items:["**Normal completion** — the child finishes; parent proceeds; no cascade.","**Cancellation** — the child is cancelled (e.g. via `cancel()` or the cascade); treated like a controlled stop, not a failure; doesn't cancel the parent upward.","**Failure** — a real exception; cancels parent and siblings (regular Job) and propagates.","**Implication** — never wrap coroutine code in a broad `catch (e: Exception)` that swallows `CancellationException`, or you turn a controlled cancellation into a broken coroutine that thinks it's still active."]},{t:"note",text:"Normal completion and cancellation don't cascade to the parent (the parent just stops waiting); only a real failure cancels parent+siblings (regular Job). CancellationException is a controlled stop, not a failure — which is why you must rethrow it rather than swallow it in a broad catch."}]},{level:"junior",q:"Does withContext create a child coroutine in the tree?",a:[{t:"p",text:"`withContext` doesn't start a new *concurrent* coroutine — it runs its block in the *same* coroutine but with a modified context (typically a different dispatcher), suspending the caller until the block returns. It participates in the tree as the current coroutine (still cancellable, still a child of its parent); it just switches context for that section, then switches back."},{t:"list",items:["**Not concurrent** — `withContext` is sequential; the caller waits for it. Contrast `launch`/`async`, which run concurrently.","**Context switch** — usually a dispatcher change (`withContext(IO)`), returning to the previous dispatcher after.","**Same cancellation** — the block is part of the current coroutine, so cancelling the coroutine cancels it.","**Returns a value** — `val x = withContext(IO) { load() }` — unlike `launch`."]},{t:"note",text:"withContext runs its block in the SAME coroutine with a changed context (usually a dispatcher), sequentially — the caller suspends until it returns, then switches back. It's not a new concurrent child (unlike launch/async); it shares the current coroutine's cancellation and returns a value."}]},{level:"senior",q:"How is a coroutine launched from viewModelScope rooted in the tree?",a:[{t:"p",text:"`viewModelScope`'s context contains a `SupervisorJob` (created for the ViewModel) plus `Dispatchers.Main.immediate`. A coroutine you `launch` becomes a child of that `SupervisorJob`. When the ViewModel is cleared, `viewModelScope` cancels that root Job, cancelling all children. The `SupervisorJob` root means one launched coroutine failing doesn't cancel the others."},{t:"list",items:["**Root = `SupervisorJob`** — so independent `viewModelScope.launch` calls don't cancel each other on failure.","**Children of the root** — each `launch`/`async` you start is a child, cancelled when the ViewModel is cleared.","**Cancelled in `onCleared()`** — automatic teardown of the whole subtree.","**Failure handling still needed** — because it's a `SupervisorJob`, an unhandled `launch` exception isn't swallowed; handle it (try/catch or handler) or it crashes via the default handler."]},{t:"note",text:"viewModelScope = SupervisorJob + Main.immediate. Your launched coroutines are children of that SupervisorJob (so one failing doesn't cancel the others), and the whole subtree is cancelled in onCleared(). Because it's a SupervisorJob, unhandled launch exceptions still need local try/catch or a handler."}]},{level:"senior",q:"What is coroutineScope's rethrow behavior versus supervisorScope's?",a:[{t:"p",text:"`coroutineScope { }` rethrows the first child failure to its caller (after cancelling siblings), so a surrounding `try/catch` catches it. `supervisorScope { }` does *not* propagate child failures to the caller — each child's failure is that child's own concern (handle it locally or via a handler), and the scope completes normally even if a child failed."},{t:"code",title:"Rethrow vs isolate",code:`// coroutineScope: rethrows -> caught here
try { coroutineScope { launch { throw E() } } } catch (e: Exception) { /* caught */ }

// supervisorScope: NOT rethrown to the caller
supervisorScope {
    launch { try { risky() } catch (e: Exception) { handleLocally(e) } }   // handle per child
}`},{t:"list",items:["**`coroutineScope`** — child failure cancels siblings and rethrows to the caller (all-or-nothing, catchable outside).","**`supervisorScope`** — child failures are isolated; the scope doesn't rethrow them, so handle each child's error inside it.","**Choosing** — `coroutineScope` for a combined operation; `supervisorScope` for independent children you handle individually."]},{t:"note",text:"coroutineScope rethrows the first child failure (after cancelling siblings) so an outer try/catch catches it. supervisorScope does NOT rethrow child failures — handle each child's error locally (try/catch or handler); the scope completes even if a child failed. Combined op → coroutineScope; independent children → supervisorScope."}]},{level:"junior",q:"What's the difference between the 'Cancelling' and 'Cancelled' Job states?",a:[{t:"p",text:"After you call `cancel()` (or a failure triggers it), the Job enters *Cancelling* — it's winding down: running `finally`/cleanup, cancelling children, but not yet fully stopped. Once all that completes, it becomes *Cancelled* (a terminal state). During Cancelling, `isActive` is false but `isCompleted` is also false; only in Cancelled is `isCompleted` true."},{t:"list",items:["**Cancelling** — cancellation requested; cleanup and child cancellation in progress; `isActive == false`, `isCompleted == false`, `isCancelled == true`.","**Cancelled** — terminal; everything (including children and cleanup) finished; `isCompleted == true`.","**Why it matters** — a coroutine can still run cleanup code during Cancelling; suspend calls there throw unless `NonCancellable`.","**Observing** — `invokeOnCompletion` fires when it reaches the terminal Cancelled state."]},{t:"note",text:"Cancelling = cancellation requested, cleanup/child-cancellation in progress (isActive false, isCompleted false, isCancelled true). Cancelled = terminal, all done (isCompleted true). Cleanup runs during Cancelling; suspend cleanup there needs NonCancellable. invokeOnCompletion fires at the terminal state."}]},{level:"senior",q:"Trace what structured concurrency does when a user navigates away mid-load.",a:[{t:"p",text:"Say a ViewModel launched a data load with concurrent children, and the user leaves the screen. If the ViewModel is destroyed (e.g. the back stack entry is popped), `viewModelScope` is cancelled in `onCleared()`. That cancellation cascades down the tree: the load coroutine and all its `async` children get `CancellationException` at their next suspension point, in-flight network calls are cancelled (if the client supports it), `finally` cleanup runs, and everything terminates — no orphaned work, no updating a dead UI."},{t:"list",items:["**Trigger** — ViewModel cleared → `viewModelScope.cancel()` (its root SupervisorJob cancelled).","**Cascade** — every child coroutine is cancelled; suspension points throw `CancellationException`.","**Network** — Retrofit/OkHttp calls tied to the coroutine are cancelled (the request is abandoned).","**Cleanup** — `finally` blocks run (close resources); suspend cleanup needs `NonCancellable`.","**Result** — no leaked coroutines, no state update after teardown; the whole subtree is gone.","**Config change caveat** — on rotation the ViewModel is *retained*, so `viewModelScope` is *not* cancelled and the load continues — the desired behavior."]},{t:"note",text:"Navigate-away that destroys the VM → viewModelScope cancelled in onCleared → cancellation cascades to all child coroutines (CancellationException at suspension points), in-flight network calls abandoned, finally cleanup runs, subtree terminates — no orphaned work or dead-UI updates. On mere rotation the VM is retained, so the load continues (correct)."}]},{level:"senior",q:"How do you make two coroutines in one scope where cancelling one doesn't cancel the other?",a:[{t:"p",text:"Cancelling one child never cancels its *siblings* in the first place — `job.cancel()` only cancels that Job and its descendants, not others in the scope. The scenario people worry about is a *failure* cancelling siblings; to prevent that, use a `SupervisorJob`/`supervisorScope` so a sibling's failure is isolated. For explicit cancellation of one task, just cancel that task's Job."},{t:"code",title:"Independent siblings",code:`val scope = CoroutineScope(SupervisorJob())   // failures isolated
val a = scope.launch { taskA() }
val b = scope.launch { taskB() }
a.cancel()   // cancels ONLY a; b keeps running (true even with a regular Job)
// SupervisorJob additionally ensures a *failure* in a won't cancel b`},{t:"list",items:["**Explicit cancel is already isolated** — `a.cancel()` doesn't touch `b`.","**Failure isolation needs SupervisorJob** — otherwise `a` *failing* (not being cancelled) would cancel `b` and the scope.","**Per-task handles** — keep each `Job` to cancel tasks individually."]},{t:"note",text:"Cancelling one child (a.cancel()) never cancels siblings — that's already isolated. What cancels siblings is a *failure* under a regular Job; use SupervisorJob/supervisorScope to isolate failures too. Keep per-task Job handles to cancel tasks individually."}]},{level:"junior",q:"What is a Job, in one clear picture, and what can it do?",a:[{t:"p",text:"A `Job` is the handle and lifecycle representation of a coroutine (or a scope). It doesn't hold a result (that's `Deferred`), but it tracks state, forms the parent-child tree, and gives you control: cancel it, wait for it, observe completion. Every `launch` returns one; every scope has one in its context."},{t:"list",items:["**Lifecycle** — tracks New/Active/Completing/Completed/Cancelling/Cancelled via `isActive`/`isCancelled`/`isCompleted`.","**Control** — `cancel()`, `cancelAndJoin()`, `join()`, `start()` (for lazy), `invokeOnCompletion { }`.","**Hierarchy** — links to a parent and has `children`; the basis of structured concurrency.","**Not a result** — for a value use `Deferred` (from `async`), which *is* a Job plus a result."]},{t:"note",text:"A Job is a coroutine's (or scope's) lifecycle handle: it tracks state (isActive/isCancelled/isCompleted), gives control (cancel/join/start/invokeOnCompletion), and forms the parent-child tree that powers structured concurrency. It carries no result — Deferred (from async) is a Job that also has a value."}]},{level:"senior",q:"How does structured concurrency make coroutine code easier to test?",a:[{t:"p",text:"Because a scope waits for all its children, a test can run an operation and know that when it returns, *all* the concurrent work it spawned has completed — no dangling background coroutines to race against. Combined with `runTest`'s virtual clock and injected test dispatchers, this makes concurrent code deterministic: you advance the scheduler and everything the operation launched finishes predictably."},{t:"list",items:["**Deterministic completion** — `coroutineScope`/`viewModelScope` work is done when the operation completes; `advanceUntilIdle()` runs all scheduled coroutines.","**No orphaned work** — you don't have to hunt for background coroutines the test didn't await; structure guarantees ownership.","**Injected dispatchers** — replace `Main`/`IO` with test dispatchers sharing the `testScheduler` so children run on the virtual clock.","**Clear boundaries** — testing an operation = testing a well-scoped unit, not a web of independent threads."]},{t:"note",text:"A scope waits for all children, so when an operation returns in a test, its spawned work is done — no dangling coroutines to race. With runTest's virtual clock + injected test dispatchers (shared testScheduler) and advanceUntilIdle(), concurrent code becomes deterministic and unit-testable as a scoped whole."}]},{level:"junior",q:"If you cancel a scope, can you still launch new coroutines in it?",a:[{t:"p",text:"No — once a scope is cancelled (`scope.cancel()`), its Job is in a cancelled state, and any `launch`/`async` on it produces an already-cancelled coroutine that never runs its body. The scope is effectively dead. If you need to stop current work but reuse the scope, cancel the *children* instead (`coroutineContext.cancelChildren()`), which leaves the scope's Job active."},{t:"list",items:["**After `scope.cancel()`** — the scope is unusable; new coroutines are born cancelled and don't execute.","**Reuse instead** — `scope.coroutineContext.cancelChildren()` cancels running coroutines but keeps the scope alive for future launches.","**Lifecycle scopes** — `viewModelScope`/`lifecycleScope` are cancelled at end-of-life and shouldn't be reused (their owner is gone).","**Design implication** — create a fresh scope for a new lifecycle rather than reviving a cancelled one."]},{t:"note",text:"No — after scope.cancel() its Job is cancelled, so new launches are born cancelled and never run; the scope is dead. To stop work but keep the scope, use coroutineContext.cancelChildren() (children cancelled, scope stays active). Don't revive a cancelled scope — create a fresh one for a new lifecycle."}]}],SS={id:"structured-concurrency",title:"Structured Concurrency & Jobs",tagline:"The parent-child Job tree, Job lifecycle & control, how failure propagates, SupervisorJob for isolation, and cancellation vs failure.",tags:["Coroutines","Job","SupervisorJob","Structured Concurrency"],content:kS,qa:xS},AS=[{heading:"CoroutineContext — the environment a coroutine runs in",blocks:[{t:"p",text:"Every coroutine carries a **`CoroutineContext`** — a set of elements describing *how and where* it runs. Think of it as a map of typed elements combined with `+`. The main elements: a **`Job`** (its lifecycle/tree position), a **`CoroutineDispatcher`** (which thread(s) it runs on), an optional **`CoroutineExceptionHandler`**, and an optional **`CoroutineName`** (for debugging)."},{t:"code",title:"Building and inspecting context",code:`val scope = CoroutineScope(
    SupervisorJob() + Dispatchers.Main + CoroutineName("ui")
)

scope.launch(Dispatchers.IO + CoroutineName("sync")) {
    // this coroutine's context = parent's context, with IO and name overridden
}`},{t:"list",items:["**Context is inherited and overridable**: a child coroutine inherits its parent's context, and any element you pass to the builder *overrides* that element for the child (e.g. `launch(Dispatchers.IO)` keeps the parent's Job-parentage but switches the dispatcher).","**The Job in a child's context is always a *new child Job*** — you don't inherit the parent's Job as your own; you get a fresh child of it (that's how the tree forms). Other elements (dispatcher, name) are inherited by value.","Elements are combined with `+` and retrieved by key (`coroutineContext[Job]`, `coroutineContext[CoroutineDispatcher]`)."]}]},{heading:"Dispatchers — which thread(s) the coroutine uses",blocks:[{t:"p",text:"A **`CoroutineDispatcher`** determines what thread or thread pool a coroutine runs on. There are four standard dispatchers, each tuned for a kind of work:"},{t:"table",headers:["Dispatcher","Backed by","Use for"],rows:[["`Dispatchers.Main`","the Android main/UI thread","UI updates, calling into ViewModel, light work"],["`Dispatchers.IO`","a large elastic thread pool (64+)","blocking I/O — network, disk, database"],["`Dispatchers.Default`","a pool sized to CPU cores","CPU-intensive work — parsing, sorting, image processing"],["`Dispatchers.Unconfined`","no confinement (runs in caller thread until first suspension)","rarely — advanced/testing; not for app logic"]]},{t:"list",items:["**IO vs Default — the key distinction**: `IO` is for *blocking* operations that spend most of their time *waiting* (a network call, a file read) — so its pool is large (many threads can be blocked waiting cheaply). `Default` is for *CPU-bound* work that *keeps a core busy* — so its pool matches core count (more threads than cores would just thrash). Using IO for CPU work wastes threads; using Default for blocking IO starves CPU work.","**`Dispatchers.Main.immediate`** — a variant that skips re-dispatching if you're *already* on the main thread (avoids an unnecessary post to the message queue / one-frame delay). `viewModelScope` uses it.","**`IO` and `Default` share threads**: switching between them (`withContext(Default)` from an IO coroutine) may not actually change threads — the dispatcher framework optimizes this. But the semantics (pool sizing/limits) still apply."]}]},{heading:"withContext — switching dispatchers within a coroutine",blocks:[{t:"p",text:"`withContext(dispatcher) { }` runs its block on the given dispatcher and **suspends until it returns the result**, then continues on the original context. It's the primary tool for moving a *piece* of work to the right thread without launching a new coroutine — and it's how you make suspend functions **main-safe**."},{t:"code",title:"withContext for main-safety",code:`suspend fun loadUser(id: String): User = withContext(Dispatchers.IO) {
    val response = api.getUser(id)   // blocking-ish network on IO
    response.toUser()                 // parse
}
// Caller (on Main) can call this safely; it hops to IO internally and back.

// Heavy CPU work -> Default:
suspend fun sortLarge(list: List<Item>): List<Item> = withContext(Dispatchers.Default) {
    list.sortedByDescending { it.score }
}`},{t:"list",items:["**`withContext` returns a value and doesn't create a new coroutine tree branch for concurrency** — it's sequential. Use it to *switch threads for a block*, not to run things in parallel (that's `async`).","**Main-safety rule**: the layer *doing* the blocking/CPU work owns the `withContext`. A repository's suspend function wraps its own IO in `withContext(IO)`, so a ViewModel can call it from the main thread without any dispatcher juggling. Callers should *not* pre-emptively wrap calls in `withContext(IO)`.","**Don't double-wrap main-safe calls**: Retrofit and Room suspend functions are already main-safe (they manage their own threading), so wrapping them in `withContext(IO)` is redundant (harmless but reveals shallow understanding).","**Inject dispatchers, don't hardcode**: take a `CoroutineDispatcher` as a constructor parameter (`ioDispatcher: CoroutineDispatcher = Dispatchers.IO`) so tests can substitute a `TestDispatcher` and control virtual time — hardcoding `Dispatchers.IO` makes code untestable."]}]},{heading:"withContext vs launch/async for threading",blocks:[{t:"list",items:["**`withContext(d) { }`** — switch context for a block, get the result back, *sequentially*. 'Run this part on IO and give me the answer.' The default choice for 'do this work on the right thread'.","**`launch(d) { }`** — start a *new concurrent coroutine* on dispatcher `d`. Use when you want fire-and-forget work on a specific thread, not when you just need a result.","**`async(d) { }`** — start a *concurrent* coroutine on `d` that returns a result via `await()`. Use for parallel work on specific threads.","The mistake to avoid: using `launch`/`async` + immediate `await`/`join` just to switch threads for a single result — that's what `withContext` is for, and it's clearer and cheaper."]}]},{heading:"Context preservation and dispatcher confinement",blocks:[{t:"list",items:["A coroutine **stays on its dispatcher across suspension points** — after a `delay` or a suspend call resumes, it comes back on the same dispatcher (not necessarily the same *thread* within a pool, but the same dispatcher). So `Dispatchers.Main` code that suspends resumes back on Main — safe to touch UI.","**`withContext` restores the outer context** when its block ends — so `withContext(IO){}` inside a Main coroutine runs the block on IO and then continues on Main automatically.","**Custom dispatchers**: you can create your own with `newSingleThreadContext` / `newFixedThreadPoolContext` (for confinement to specific threads, e.g. a single-threaded dispatcher to serialize access to a non-thread-safe resource) or wrap a Java `Executor` with `.asCoroutineDispatcher()`. Close them when done (they hold real threads).","**`Dispatchers.Main` requires a main-thread implementation** — on Android it's provided by the coroutines-android artifact; in pure JVM unit tests it doesn't exist, which is why you `Dispatchers.setMain(testDispatcher)` in tests."]},{t:"note",text:'The threading trio to state cleanly: "Main for UI, IO for blocking waits (network/disk — large pool), Default for CPU work (parsing/sorting — core-sized pool). Switch with withContext, owned by the layer doing the work, and inject dispatchers for testability."'}]}],CS=[{level:"junior",q:"What are the main coroutine dispatchers and what is each for?",a:[{t:"list",items:["**`Dispatchers.Main`** — the Android main/UI thread. For updating UI, calling ViewModel functions, and light work. UI can only be touched here.","**`Dispatchers.IO`** — a large elastic thread pool (64+ threads) for *blocking I/O*: network requests, disk reads/writes, database queries. These operations spend their time *waiting*, so a big pool lets many block cheaply at once.","**`Dispatchers.Default`** — a pool sized to the number of CPU cores, for *CPU-intensive* work: parsing large JSON, sorting big lists, image processing, computations. These keep a core busy, so more threads than cores wouldn't help.","**`Dispatchers.Unconfined`** — starts in the calling thread and isn't confined to any specific thread after suspension. Rarely used in app code; mostly advanced scenarios and some testing."]},{t:"p",text:"The everyday split is Main / IO / Default. The key judgment is IO vs Default: IO for things that *wait* (network, disk), Default for things that *compute* (parsing, sorting). You switch to the right one with `withContext(dispatcher) { }` around the relevant work."}]},{level:"junior",q:"What is the difference between Dispatchers.IO and Dispatchers.Default?",a:[{t:"p",text:"**The distinction is blocking-and-waiting vs CPU-bound work, and it's reflected in the pool sizes.** `Dispatchers.IO` is for operations that spend most of their time *waiting* on something external — a network response, a disk read, a database query. Because a waiting thread isn't using the CPU, you can afford *many* of them, so IO's pool is large (64+ threads by default). `Dispatchers.Default` is for work that *keeps the CPU busy* — parsing, sorting, computation. Here more threads than CPU cores would just cause context-switching overhead without speedup, so Default's pool is sized to the core count."},{t:"list",items:["**Using the wrong one has real costs**: run CPU-heavy work on `IO` and you can spawn dozens of CPU-bound threads competing for a few cores (thrashing). Run blocking IO on `Default` and a handful of blocked threads exhaust the small pool, starving other CPU work.","**Rule of thumb**: 'Does this operation mostly *wait* or mostly *compute*?' Wait → IO. Compute → Default.","**Implementation detail**: IO and Default actually share an underlying thread pool, and switching between them may not physically change threads — but the semantics (how many concurrent tasks each permits) still matter, so choose by the nature of the work."]}]},{level:"junior",q:"What does withContext do?",a:[{t:"p",text:"**The concept**: `withContext(dispatcher) { }` runs its block on the specified dispatcher, *suspends* until the block finishes, returns the block's result, and then continues on the original context. It's the standard way to move a piece of work to the appropriate thread within a suspend function — without launching a separate coroutine."},{t:"code",title:"Moving blocking work off the main thread",code:`suspend fun loadData(): Data = withContext(Dispatchers.IO) {
    val json = file.readText()   // blocking IO on an IO thread
    parse(json)                   // returns the result
}
// A caller on Main can call this; it hops to IO and back automatically.`},{t:"p",text:"It's how you make a suspend function *main-safe* — safe to call from the main thread because it internally switches to the right dispatcher for its heavy work. Unlike `launch`/`async`, `withContext` is *sequential* — it doesn't start concurrent work, it just switches threads for a block and gives you the result. So use `withContext` for 'run this part elsewhere and give me the answer', and `async` for 'run these things in parallel'."}]},{level:"junior",q:"Where should you call withContext(Dispatchers.IO) — in the ViewModel or the repository?",a:[{t:"p",text:"**In the repository (or wherever the blocking work actually happens), not the ViewModel.** The principle is *main-safety owned by the layer doing the work*: the function that performs blocking IO wraps *itself* in `withContext(IO)`, so that every caller — including a ViewModel on the main thread — can call it safely without thinking about threads."},{t:"list",items:["**Why not the ViewModel**: if ViewModels wrap every repository call in `withContext(IO)` 'to be safe', threading policy gets scattered across the whole app, it's easy to forget one, and it hides which layer actually blocks. Centralizing it in the data layer means one correct place per operation.","**The ViewModel just launches**: `viewModelScope.launch { val data = repository.load() }` — no dispatcher juggling, because `repository.load()` is main-safe.","**Don't double-wrap main-safe calls**: Retrofit and Room suspend functions already switch threads internally, so wrapping *those* in `withContext(IO)` is redundant. You add `withContext` for *your own* blocking code (a manual file read, a heavy parse), not for libraries that are already main-safe."]},{t:"p",text:"So the clean architecture: repositories/use-cases are main-safe (own their `withContext`), the ViewModel calls them from `viewModelScope` (Main) freely, and the UI stays responsive. This keeps threading decisions in one predictable place."}]},{level:"senior",q:"What is a CoroutineContext, and how is it inherited by child coroutines?",a:[{t:"p",text:"**The concept**: a `CoroutineContext` is the set of elements defining how a coroutine runs — it's an indexed collection (like a typed map) of elements combined with the `+` operator. The main elements are the **`Job`** (lifecycle and tree position), the **`CoroutineDispatcher`** (threading), an optional **`CoroutineExceptionHandler`**, and a **`CoroutineName`** (debugging). You retrieve elements by key: `coroutineContext[Job]`, `coroutineContext[CoroutineDispatcher]`."},{t:"list",items:["**Inheritance with override**: a child coroutine's context is computed as *parent context + whatever you pass to the builder*. So `launch(Dispatchers.IO)` takes the parent's context and overrides just the dispatcher — the child keeps the parent's other elements (name, handler) unless you also override them. This is why children run on the parent's dispatcher by default.","**The Job is special**: a child does NOT inherit the parent's Job as its own — it always gets a *new Job that is a child of* the parent's Job. That's the mechanism that builds the structured-concurrency tree. Every other element is inherited by value; the Job is always freshly parented.",'**Combining**: `SupervisorJob() + Dispatchers.Main + CoroutineName("x")` builds a context; later elements of the same key win (`Dispatchers.IO + Dispatchers.Main` yields Main).',"**`withContext(element)`** temporarily overrides context elements for its block (commonly the dispatcher) and restores them after — a scoped, sequential context change."]},{t:"p",text:"**Why this matters**: understanding context inheritance explains real behavior — why a coroutine launched in a Main scope runs on Main (inherited dispatcher), why a launched child's failure reaches the parent (new child Job under the parent Job), and how to override just one aspect (pass one element to the builder). It's the unifying model beneath dispatchers, Jobs, and exception handlers — they're all just context elements."}]},{level:"senior",q:"Why should you inject dispatchers rather than hardcode Dispatchers.IO, and how does it help testing?",a:[{t:"p",text:"**Because hardcoded dispatchers make coroutine code non-deterministic and slow to test.** If a repository hardcodes `withContext(Dispatchers.IO)`, that work runs on real background threads during tests — outside the test's control. The test can't make it deterministic, can't fast-forward `delay`s, and may get flaky timing. Injecting the dispatcher lets the test substitute a `TestDispatcher` that runs on the test's scheduler with *virtual time*."},{t:"code",title:"Injected dispatcher enables test control",code:`class UserRepository(
    private val ioDispatcher: CoroutineDispatcher = Dispatchers.IO,  // injected
) {
    suspend fun load() = withContext(ioDispatcher) { /* work */ }
}

// In tests:
val testDispatcher = StandardTestDispatcher()
val repo = UserRepository(ioDispatcher = testDispatcher)   // controllable
// runTest can now advance virtual time, run deterministically`},{t:"list",items:["**Virtual time**: a `TestDispatcher` runs inside `runTest`, where `delay(1000)` completes instantly by advancing a fake clock. Hardcoded `Dispatchers.IO` escapes that — a `delay` there really waits, making tests slow. Injecting lets tests of debounce/retry/timeout run in milliseconds.","**Determinism**: `StandardTestDispatcher` queues coroutines so you control execution order with `advanceUntilIdle()`/`runCurrent()` and can assert intermediate states (like a `Loading` before `Content`). Real dispatchers give you races.","**Common pattern**: inject a `CoroutineDispatcher` (or a small `DispatcherProvider` interface bundling Main/IO/Default) as a constructor default `= Dispatchers.IO`, so production uses the real one and tests inject the test one. Zero production cost, full test control.","**Also improves flexibility**: you can swap dispatchers per environment or tune them without touching call sites."]},{t:"p",text:"**The principle**: a hardcoded dispatcher is a hidden dependency on real threads and real time. Injecting it makes threading an explicit, substitutable dependency — the same reason you inject a repository instead of `new`-ing one. It's a small habit that makes the difference between testable and untestable coroutine code."}]},{level:"senior",q:"When would you create a custom dispatcher, and what must you be careful about?",a:[{t:"p",text:"**The standard dispatchers cover almost everything, so custom dispatchers are for specific needs — most commonly *confining work to a specific thread or a bounded set of threads*.** The classic case: serializing access to a resource that isn't thread-safe. A single-threaded dispatcher guarantees all operations on that resource happen on one thread, in order — a lock-free way to prevent concurrent access."},{t:"list",items:['**`newSingleThreadContext("name")`** — a dispatcher backed by one dedicated thread. Use to serialize access to a non-thread-safe library, a legacy single-threaded native component, or to guarantee ordering. It replaces manual synchronization with confinement.','**`newFixedThreadPoolContext(n, "name")`** — a fixed pool of `n` threads, for when you need a bounded, isolated pool separate from IO/Default (e.g. to cap concurrency to a specific external system).',"**`Executor.asCoroutineDispatcher()`** — wrap an existing Java `Executor` (a thread pool you already manage, or one with special properties) as a coroutine dispatcher. Common when integrating with existing threading infrastructure.","**`Dispatchers.IO.limitedParallelism(n)`** — a modern, lighter alternative: get a *view* of the IO pool limited to `n` concurrent coroutines, without creating new threads. Good for capping parallelism to a rate-limited API while reusing the shared pool."]},{t:"list",items:["**The big caution — they hold real threads**: `newSingleThreadContext`/`newFixedThreadPoolContext` create actual OS threads that are NOT automatically released. You must `close()` them when done (or scope them to something), or you leak threads. This is why `limitedParallelism` is often preferred — it doesn't create threads.","**Don't create dispatchers per-call**: creating a `newSingleThreadContext` inside a frequently-called function leaks a thread every time. Create once, reuse, close on teardown.","**Prefer the standard dispatchers unless you have a concrete confinement/isolation need** — custom dispatchers add resource management burden for benefits most code doesn't need."]}]},{level:"junior",q:"What is Dispatchers.Main, and what does Main.immediate add?",a:[{t:"p",text:"`Dispatchers.Main` runs coroutines on the platform's main/UI thread — the only thread allowed to touch UI. `Dispatchers.Main.immediate` is an optimization: if you're *already* on the main thread, it executes the continuation *immediately* instead of scheduling it to run later on the message queue, avoiding an unnecessary re-dispatch (and a possible frame's delay)."},{t:"code",title:"Main vs Main.immediate",code:`// viewModelScope uses Main.immediate by default
viewModelScope.launch {            // starts on Main.immediate
    _state.value = Loading         // runs immediately (already on Main), no re-post
    val data = repo.load()         // main-safe suspend call
    _state.value = Content(data)   // back on Main
}`},{t:"list",items:["**`Main`** — the UI thread; required for UI/state updates.","**`Main.immediate`** — skips re-dispatch when already on Main; used by `viewModelScope`/`lifecycleScope`.","**Why it matters** — without `immediate`, a state update could be posted to run on a later loop iteration, causing a needless delay/flicker.","**Still offload heavy work** — Main means UI thread; move I/O/CPU to `IO`/`Default`."]},{t:"note",text:"Dispatchers.Main = the UI thread (only thread that can touch UI). Main.immediate runs the continuation right away if already on Main instead of re-posting to the message queue (avoids a needless delay/frame). viewModelScope/lifecycleScope default to Main.immediate; still offload heavy work to IO/Default."}]},{level:"senior",q:"How many threads does Dispatchers.Default have, and what is it for?",a:[{t:"p",text:"`Dispatchers.Default` is for *CPU-bound* work — sorting, parsing, image processing, JSON work, complex calculations. Its thread pool size equals the number of CPU cores (at least 2), because running more CPU-bound threads than cores just causes context-switching overhead without more throughput."},{t:"list",items:["**Sized to CPU cores** — `max(2, numberOfCores)` threads; matching cores maximizes CPU throughput for compute work.","**For CPU-bound work** — computation that keeps a thread busy (not waiting).","**Don't do blocking I/O on it** — a blocked thread ties up a scarce core-count thread, starving other CPU work; use `IO` for that.","**Default for `Default`** — it's the fallback dispatcher when none is specified in `GlobalScope`/`launch` without a context in some cases."]},{t:"code",title:"CPU work on Default",code:`val result = withContext(Dispatchers.Default) {
    hugeList.sortedBy { it.score }.map { transform(it) }   // CPU-bound
}`},{t:"note",text:"Dispatchers.Default is for CPU-bound work (sorting/parsing/image/JSON/calculations); its pool = max(2, CPU cores), since more CPU threads than cores just adds context-switch overhead. Don't do blocking I/O on it — a blocked thread wastes a scarce core-sized slot; use IO for waiting work."}]},{level:"senior",q:"How many threads does Dispatchers.IO have, and why so many?",a:[{t:"p",text:"`Dispatchers.IO` is for *I/O-bound* work — network calls, disk/database reads, file operations — where threads spend most time *waiting*. It has a much larger pool (default 64 threads, or more if there are more cores) because I/O threads are mostly idle-waiting, so you want many of them to run many concurrent I/O operations without one blocking the next."},{t:"list",items:["**~64 threads by default** — `max(64, cores)`; sized for concurrency, not CPU throughput.","**For blocking/waiting I/O** — network, disk, DB; the thread blocks waiting for the OS, so many threads let many I/O ops proceed.","**Shares a pool with `Default`** — `IO` and `Default` draw from the same underlying threads; switching between them may not change the actual thread, just the parallelism limit.","**Don't overload with CPU work** — 64 CPU-bound threads on a few cores thrash; keep CPU work on `Default`."]},{t:"note",text:"Dispatchers.IO is for I/O-bound work (network/disk/DB) where threads mostly wait, so it has ~64 threads (max(64, cores)) to run many concurrent blocking ops. It shares an underlying pool with Default (switching may not change the physical thread). Don't run CPU-bound work on it — that thrashes the cores."}]},{level:"senior",q:"What is limitedParallelism, and when do you use it?",a:[{t:"p",text:"`dispatcher.limitedParallelism(n)` creates a *view* of a dispatcher that allows at most `n` coroutines to run concurrently on it — without creating a new thread pool. It's the modern way to cap concurrency for a resource (e.g. a server that allows only 4 simultaneous requests, or serializing DB writes) while still sharing the shared IO/Default threads."},{t:"code",title:"Capping concurrency",code:`class Api {
    private val dispatcher = Dispatchers.IO.limitedParallelism(4)   // max 4 concurrent
    suspend fun call() = withContext(dispatcher) { network() }      // never more than 4 at once
}
// Serialize writes:
val writeDispatcher = Dispatchers.IO.limitedParallelism(1)          // single-writer`},{t:"list",items:["**Caps concurrency** — at most `n` coroutines run at once on the view; excess are queued.","**No new pool** — it reuses the parent dispatcher's threads, avoiding the cost of a dedicated thread pool.","**Uses** — rate-limiting an API, bounding parallel downloads, serializing writes (`n = 1` for single-thread-like confinement without a dedicated thread).","**Replaces `newFixedThreadPoolContext`** for most bounding needs (cheaper, no separate threads to close)."]},{t:"note",text:"dispatcher.limitedParallelism(n) makes a view of a dispatcher capping concurrent coroutines at n, reusing the parent's shared threads (no new pool). Use it to rate-limit an API, bound parallel downloads, or serialize writes (n=1). It's the modern, cheaper alternative to a dedicated newFixedThreadPoolContext."}]},{level:"junior",q:"What is Dispatchers.Unconfined, and why is it rarely used?",a:[{t:"p",text:"`Dispatchers.Unconfined` starts a coroutine on the *current* thread and, after a suspension point, resumes on whatever thread the resuming code runs on — it isn't confined to any particular thread or pool. It's an advanced/rare tool; in app code you almost never want it because you can't predict which thread you'll be on, which is dangerous for UI and thread-unsafe state."},{t:"list",items:["**Runs on the caller's thread initially** — then resumes wherever the continuation is invoked.","**Unpredictable threading** — after suspension you could be on any thread; unsafe for UI/shared state.","**Rare legit uses** — certain unit tests, or operators that must run synchronously up to the first suspension; not for production business logic.","**Prefer explicit dispatchers** — `Main`/`IO`/`Default` give predictable, correct threading."]},{t:"note",text:"Dispatchers.Unconfined runs on the current thread and, after suspension, resumes on whatever thread resumes it — not confined to a pool. Unpredictable threading makes it unsafe for UI/shared state, so it's rarely used (some tests/synchronous-until-first-suspension cases). Prefer explicit Main/IO/Default."}]},{level:"senior",q:"What happens if you do blocking I/O on Dispatchers.Default (or CPU work on IO)?",a:[{t:"p",text:"Using the wrong dispatcher degrades performance. Blocking I/O on `Default` ties up one of its few core-sized threads while it waits, starving other CPU work and potentially deadlocking under load. Heavy CPU work on `IO` spins up to 64 threads all fighting for a handful of cores, causing context-switch thrash and no real speedup. Match the dispatcher to the workload."},{t:"list",items:["**Blocking I/O on `Default`** — occupies a scarce (core-count) thread while waiting; few such threads exist, so concurrent I/O quickly exhausts them and stalls CPU work.","**CPU work on `IO`** — up to 64 compute-heavy threads thrash the CPU (more threads than cores = overhead), hurting throughput and battery.","**Rule** — `IO` for waiting (network/disk/DB), `Default` for computing.","**Symptoms** — jank, sluggish parallel work, or in the worst case starvation/deadlock."]},{t:"note",text:"Wrong dispatcher = degraded performance. Blocking I/O on Default ties up its scarce core-sized threads (waiting), starving CPU work and risking starvation. Heavy CPU on IO spawns up to 64 compute threads thrashing a few cores (no speedup, wasted battery). IO for waiting, Default for computing."}]},{level:"senior",q:"How do you confine shared mutable state to a single thread with a dispatcher?",a:[{t:"p",text:"Create a single-threaded dispatcher (or `limitedParallelism(1)`) and run all access to the shared state through it. Because only one coroutine runs at a time on that dispatcher, there are no concurrent modifications — an alternative to `Mutex` for protecting state (thread confinement instead of locking)."},{t:"code",title:"Single-thread confinement",code:`private val stateDispatcher = Dispatchers.Default.limitedParallelism(1)
private var counter = 0
suspend fun increment() = withContext(stateDispatcher) { counter++ }   // serialized, no race`},{t:"list",items:["**One coroutine at a time** — `limitedParallelism(1)` (or a single-thread dispatcher) serializes access, preventing data races.","**vs `Mutex`** — confinement moves work to one thread; `Mutex` locks around a critical section on any thread. Both prevent races; pick per situation.","**Coarse vs fine** — confining *all* access is simple but serializes everything; a `Mutex` can be more granular.","**Legacy** — `newSingleThreadContext` also works but creates a dedicated thread you must close; `limitedParallelism(1)` is cheaper."]},{t:"note",text:"Route all access to shared mutable state through a single-threaded dispatcher (Dispatchers.X.limitedParallelism(1)) so only one coroutine touches it at a time — thread confinement instead of a Mutex. Cheaper than newSingleThreadContext (no dedicated thread to close). Mutex is the finer-grained alternative."}]},{level:"senior",q:"How do you create a custom dispatcher, and what must you be careful about?",a:[{t:"p",text:"You can wrap an `ExecutorService` with `asCoroutineDispatcher()`, or use `newFixedThreadPoolContext`/`newSingleThreadContext`. The main cautions: these create *real threads* you must *close* (they don't get cleaned up automatically), too many custom pools waste memory, and you usually don't need one — `IO`/`Default` + `limitedParallelism` cover most cases."},{t:"code",title:"Executor-backed dispatcher",code:`val dispatcher = Executors.newFixedThreadPool(4).asCoroutineDispatcher()
try {
    withContext(dispatcher) { work() }
} finally {
    dispatcher.close()   // MUST close — releases the threads
}`},{t:"list",items:["**`asCoroutineDispatcher()`** — adapt any `Executor`/`ExecutorService` (useful to integrate an existing pool).","**Close it** — custom dispatchers hold real threads; forgetting `close()` leaks threads.","**Avoid proliferation** — many small pools waste memory and reduce the benefit of shared pools.","**Prefer `limitedParallelism`** — for bounding concurrency without a dedicated pool.","**When justified** — integrating a third-party executor, or needing strict thread affinity for a non-thread-safe native library."]},{t:"note",text:"Create custom dispatchers via Executor.asCoroutineDispatcher() or newFixed/SingleThreadContext — but they hold real threads you MUST close() (else leaks), and many pools waste memory. Prefer Dispatchers.IO/Default + limitedParallelism. Custom pools are for integrating an existing executor or strict thread affinity (non-thread-safe native libs)."}]},{level:"junior",q:"What's the difference between withContext(dispatcher) and launch(dispatcher)?",a:[{t:"p",text:"`withContext(dispatcher) { }` runs a block on that dispatcher *sequentially*, suspending the caller until it returns a value — it's for switching context for a section of the *current* coroutine. `launch(dispatcher) { }` starts a *new concurrent* coroutine on that dispatcher and returns a `Job` immediately. Use `withContext` to move work; `launch` to run something alongside."},{t:"code",title:"Switch vs start-concurrent",code:`// Switch this coroutine to IO for a value, then continue:
val data = withContext(Dispatchers.IO) { load() }

// Start a separate concurrent coroutine on IO (fire-and-forget):
scope.launch(Dispatchers.IO) { sync() }`},{t:"list",items:["**`withContext`** — sequential, returns a value, same coroutine; the go-to for 'do this part on IO/Default'.","**`launch`** — concurrent, returns a `Job`, new child coroutine.","**Don't `launch` just to switch threads** — if you need the result next, `withContext` is correct and clearer.","**Both cancellable** — tied to the enclosing structure."]},{t:"note",text:"withContext(dispatcher){} switches the CURRENT coroutine to that dispatcher for a block, sequentially, returning a value — use it to move work (load on IO). launch(dispatcher){} starts a NEW concurrent coroutine (returns a Job). Don't launch merely to switch threads when you need the result next — that's withContext."}]},{level:"senior",q:"Does withContext add overhead, and when should you avoid switching dispatchers?",a:[{t:"p",text:"`withContext` has a small cost: potentially a thread switch (re-dispatch) and coroutine bookkeeping. It's cheap relative to actual I/O, but pointless switches add up. Avoid switching when the work is already on the right dispatcher, when calling already-main-safe functions (Room/Retrofit), or wrapping tiny non-blocking operations."},{t:"list",items:["**Cost** — a possible thread hop and scheduling; negligible next to network/disk, but wasteful if done needlessly per call.","**Don't wrap main-safe functions** — Room/Retrofit suspend functions already switch internally; an extra `withContext(IO)` is redundant.","**Don't switch for trivial work** — wrapping a fast in-memory operation in `withContext(Default)` costs more than it saves.","**Do switch at the boundary** — one `withContext(IO)` around a chunk of blocking work, not per tiny call inside it."]},{t:"note",text:"withContext costs a possible thread hop + bookkeeping — cheap vs real I/O but wasteful if needless. Don't wrap already-main-safe Room/Retrofit calls, don't switch for trivial in-memory work, and switch once at the boundary of a blocking chunk rather than per tiny call inside it."}]},{level:"junior",q:"What dispatcher do Room and Retrofit suspend functions use — should you wrap them in withContext(IO)?",a:[{t:"p",text:"No — Room and Retrofit `suspend` functions are already *main-safe*: they internally move their blocking work off the main thread (Room uses its own executors/`Dispatchers.IO`; Retrofit's suspend adapter dispatches the call appropriately). Wrapping them in `withContext(Dispatchers.IO)` is redundant and just adds an unnecessary context switch."},{t:"code",title:"No extra wrapping needed",code:`// Room suspend DAO — already main-safe:
suspend fun getUser(id: String) = userDao.getUser(id)          // fine from Main
// Retrofit suspend API — already main-safe:
suspend fun fetch() = api.getData()                            // fine from Main
// Only wrap YOUR OWN blocking code:
suspend fun parseBigFile() = withContext(Dispatchers.IO) { blockingParse() }`},{t:"list",items:["**Room suspend DAOs** — main-safe; Room dispatches to its query executor.","**Retrofit suspend endpoints** — main-safe; the call runs off the main thread.","**Wrap only your own blocking code** — file parsing, a blocking SDK, synchronous crypto.","**Blocking (non-suspend) Room/OkHttp calls** — *those* need `withContext(IO)`; the suspend variants don't."]},{t:"note",text:"Room and Retrofit SUSPEND functions are already main-safe (they offload internally) — wrapping them in withContext(IO) is redundant. Only wrap your own blocking code (file parse, blocking SDK). The blocking/non-suspend Room/OkHttp variants DO need withContext(IO); the suspend ones don't."}]},{level:"senior",q:"How do you test coroutine code that uses dispatchers?",a:[{t:"p",text:"Inject dispatchers rather than hardcoding them, so tests can pass a `TestDispatcher` that runs on `runTest`'s virtual clock. For the main dispatcher (used by `viewModelScope`), swap it with `Dispatchers.setMain(testDispatcher)` in setup and `resetMain()` in teardown. This makes coroutine scheduling deterministic and fast."},{t:"code",title:"Dispatcher injection + setMain",code:`class UserRepo(private val io: CoroutineDispatcher = Dispatchers.IO) { … }

@get:Rule val mainRule = MainDispatcherRule()   // wraps setMain/resetMain

@Test fun loads() = runTest {
    val repo = UserRepo(StandardTestDispatcher(testScheduler))
    val vm = UserViewModel(repo)
    vm.load(); advanceUntilIdle()
    assertEquals(Content(user), vm.state.value)
}`},{t:"list",items:["**Inject dispatchers** — constructor params with production defaults (`Dispatchers.IO`), overridden in tests.","**`Dispatchers.setMain(testDispatcher)`** — replace `Main` so `viewModelScope` runs on the test dispatcher; `resetMain()` after (often via a JUnit rule).","**Share the `testScheduler`** — all test dispatchers use it so `advanceUntilIdle()` drives everything.","**Deterministic + instant** — virtual clock skips real delays; no flakiness from real threading."]},{t:"note",text:"Inject dispatchers (constructor default Dispatchers.IO, override with a TestDispatcher), and swap Main via Dispatchers.setMain(testDispatcher)/resetMain() (a MainDispatcherRule). Share the testScheduler so advanceUntilIdle() drives all coroutines. Result: deterministic, instant tests with no real threading."}]},{level:"junior",q:"What thread does a coroutine run on if you don't specify a dispatcher?",a:[{t:"p",text:"It *inherits* the dispatcher from its parent scope/coroutine. So a coroutine launched in `viewModelScope` (Main.immediate) runs on Main; one launched inside a `withContext(IO)` block runs on IO. If nothing in the chain sets a dispatcher (rare, e.g. `GlobalScope` without one), it falls back to `Dispatchers.Default`."},{t:"list",items:["**Inheritance** — a child uses the parent context's dispatcher unless overridden.","**`viewModelScope`/`lifecycleScope`** — Main.immediate, so children start on Main.","**Inside `withContext(X)`** — coroutines launched there inherit `X`.","**No dispatcher anywhere** — defaults to `Dispatchers.Default`."]},{t:"code",title:"Inherited dispatcher",code:`viewModelScope.launch {            // Main
    launch { }                     // inherits Main
    withContext(Dispatchers.IO) {
        launch { }                 // inherits IO
    }
}`},{t:"note",text:"Without an explicit dispatcher, a coroutine inherits its parent's: viewModelScope children run on Main, coroutines inside withContext(IO) run on IO. If nothing in the chain sets one, it falls back to Dispatchers.Default. Override per-builder by passing a dispatcher."}]},{level:"junior",q:"How do you switch back to the main thread to update the UI after background work?",a:[{t:"p",text:"If you launched from `viewModelScope`/`lifecycleScope` (Main) and used `withContext(IO)` for the background part, you're automatically back on Main after the `withContext` block returns — no manual switch needed. If you're on a background dispatcher and must touch UI, wrap the UI update in `withContext(Dispatchers.Main)`."},{t:"code",title:"Automatic return to Main",code:`viewModelScope.launch {                       // Main
    val data = withContext(Dispatchers.IO) { load() }   // background
    _state.value = Content(data)              // back on Main automatically
}
// If already on a background dispatcher:
withContext(Dispatchers.Main) { updateUi() }`},{t:"list",items:["**`withContext` returns to the caller's dispatcher** — code after it resumes on Main (if that's where you launched).","**Prefer this structure** — launch on Main, `withContext(IO)` the heavy part; state updates after are main-safe.","**Explicit switch** — `withContext(Dispatchers.Main) { }` only when you're genuinely on a background dispatcher.","**Compose/StateFlow** — you often just update a `StateFlow` (thread-safe) and let the UI collect on Main."]},{t:"note",text:"Launch on Main (viewModelScope) and withContext(IO) only the heavy part — after that block you're automatically back on Main, so state/UI updates are safe with no manual switch. Use withContext(Dispatchers.Main) explicitly only if you're actually on a background dispatcher. StateFlow updates are thread-safe regardless."}]},{level:"senior",q:"What is a CoroutineDispatcher conceptually, and how does it relate to CoroutineContext?",a:[{t:"p",text:"A `CoroutineDispatcher` is a `CoroutineContext` element that decides *which thread (or pool)* runs a coroutine's continuations. Conceptually it's the bridge between coroutines and threads: when a coroutine is ready to run or resume, the dispatcher schedules that work onto its thread(s). It's one element of the context, alongside the Job, name, and exception handler."},{t:"list",items:["**A context element** — retrievable as `context[CoroutineDispatcher]`; combined with `+`.","**Schedules continuations** — decides the thread for initial start and each resume after a suspension point.","**Interchangeable** — `Main`/`IO`/`Default`/custom all implement the same interface; you swap them freely (including test dispatchers).","**`dispatch`/`isDispatchNeeded`** — the low-level methods; `immediate` variants override `isDispatchNeeded` to skip re-posting."]},{t:"note",text:"A CoroutineDispatcher is the CoroutineContext element that maps coroutines to threads — it schedules a coroutine's start and each post-suspension resume onto its thread/pool. Retrieve via context[CoroutineDispatcher], combine with +. All dispatchers share one interface, so Main/IO/Default/test dispatchers are swappable."}]},{level:"senior",q:"How do you rate-limit access to a resource using dispatchers?",a:[{t:"p",text:"Use `limitedParallelism(n)` on a dispatcher to bound how many coroutines hit the resource concurrently, or a `Semaphore(n)` to gate access. Both cap concurrency to `n`; `limitedParallelism` does it at the dispatcher level (threads), while `Semaphore` is a suspending permit you acquire around the critical section regardless of dispatcher."},{t:"code",title:"Two ways to cap concurrency",code:`// Dispatcher view — at most 3 concurrent
private val limited = Dispatchers.IO.limitedParallelism(3)
suspend fun download(url: String) = withContext(limited) { fetch(url) }

// Semaphore — permits, dispatcher-agnostic
private val semaphore = Semaphore(3)
suspend fun download2(url: String) = semaphore.withPermit { fetch(url) }`},{t:"list",items:["**`limitedParallelism(n)`** — bounds concurrent coroutines on that dispatcher view; simple for I/O.","**`Semaphore(n)` + `withPermit`** — suspends until a permit is free; works across any dispatcher and around any code.","**Uses** — respecting an API's rate limit, limiting simultaneous uploads, protecting a connection-limited resource.","**Fairness/backpressure** — excess coroutines suspend (queue) rather than failing."]},{t:"note",text:"Cap concurrent access with Dispatchers.IO.limitedParallelism(n) (dispatcher-level bound) or a Semaphore(n) + withPermit (dispatcher-agnostic permits). Both suspend excess coroutines (queue, no failure). Use for API rate limits, bounded uploads, or connection-limited resources."}]},{level:"junior",q:"Why should you inject dispatchers instead of hardcoding Dispatchers.IO?",a:[{t:"p",text:"Hardcoding `Dispatchers.IO` inside a class makes it untestable — tests can't substitute a deterministic dispatcher, so they run on real threads with real timing (slow, flaky). Injecting the dispatcher (a constructor parameter with a production default) lets tests pass a `TestDispatcher` on the virtual clock, and documents the class's threading dependency."},{t:"code",title:"Injectable dispatcher",code:`class Repo(
    private val api: Api,
    private val ioDispatcher: CoroutineDispatcher = Dispatchers.IO,   // default in prod
) {
    suspend fun load() = withContext(ioDispatcher) { api.get() }
}
// Test: Repo(fakeApi, StandardTestDispatcher(testScheduler))`},{t:"list",items:["**Testability** — swap in a `TestDispatcher` for deterministic, instant tests.","**Default in production** — a constructor default (`= Dispatchers.IO`) keeps call sites clean.","**Often a `DispatcherProvider`** — an injected interface bundling Main/IO/Default for larger apps.","**Explicit dependency** — the class declares that it needs a dispatcher, aiding DI and clarity."]},{t:"note",text:"Hardcoded Dispatchers.IO can't be swapped in tests (real threads, slow/flaky). Inject it as a constructor param with a production default (= Dispatchers.IO), so tests pass a TestDispatcher on the virtual clock. Larger apps inject a DispatcherProvider bundling Main/IO/Default. It also makes the threading dependency explicit."}]},{level:"senior",q:"Why is the main thread special, and what is the 'main-thread rule'?",a:[{t:"p",text:"Android's UI toolkit is single-threaded: only the main thread may create/update views, and long work on it blocks rendering and input, causing jank or an ANR (Application Not Responding). The 'main-thread rule' is therefore two-sided: never do slow work on the main thread, and never touch UI off the main thread. Dispatchers make honoring both easy."},{t:"list",items:["**UI is single-threaded** — views must be accessed on Main; off-thread access throws or corrupts state.","**Don't block Main** — >~a few ms of work drops frames; >5s of input starvation triggers an ANR.","**Coroutine approach** — launch on Main, `withContext(IO/Default)` for heavy work, update UI back on Main.","**StateFlow/Compose** — updating a thread-safe `StateFlow` from any thread and collecting on Main sidesteps manual switching."]},{t:"note",text:"The UI toolkit is single-threaded: only Main can touch views, and slow work on Main drops frames or triggers an ANR (>5s). Main-thread rule: don't block Main, don't touch UI off Main. Coroutines honor it — launch on Main, withContext(IO/Default) for heavy work, update UI back on Main (or via a thread-safe StateFlow)."}]},{level:"senior",q:"What is the cost of creating many thread pools / custom dispatchers, and how do you avoid it?",a:[{t:"p",text:"Each custom thread pool consumes memory (each thread has a stack, ~0.5–1MB) and OS resources, and having many pools fragments your threads so none is used efficiently — plus you must remember to close each one. The shared `Dispatchers.IO`/`Default` pools exist precisely so the whole app reuses a common set of threads."},{t:"list",items:["**Threads are expensive** — memory per thread stack; many idle pools waste RAM.","**Fragmentation** — separate pools can't share load; the shared IO/Default pools balance work app-wide.","**Cleanup burden** — custom dispatchers must be `close()`d; forgetting leaks threads.","**Avoidance** — use `Dispatchers.IO`/`Default`; bound concurrency with `limitedParallelism` (no new threads) instead of new pools; reserve custom pools for genuine needs (thread affinity, integrating an existing executor)."]},{t:"note",text:"Each custom pool costs memory (per-thread stacks) and OS resources, fragments threads (no shared load balancing), and must be close()d (leak risk otherwise). Prefer the shared Dispatchers.IO/Default, and bound concurrency with limitedParallelism (reuses shared threads, no new pool) rather than creating pools."}]},{level:"junior",q:"How do you move a specific blocking call off the main thread without restructuring everything?",a:[{t:"p",text:"Wrap just that blocking call in `withContext(Dispatchers.IO)` inside a suspend function. This makes the function main-safe locally without changing callers — they still call it normally, and only the blocking portion runs off-main. It's the minimal, targeted fix for a stray blocking API."},{t:"code",title:"Targeted offload",code:`suspend fun readConfig(): Config = withContext(Dispatchers.IO) {
    file.readText().let { parse(it) }   // blocking file I/O + parse, off Main
}
// Callers stay unchanged:
viewModelScope.launch { val cfg = readConfig() }`},{t:"list",items:["**Wrap the blocking part** — one `withContext(IO)` around the offending call.","**Keep it main-safe** — callers don't need to know or manage threads.","**Push it into the data layer** — put the `withContext` in the repository, not the ViewModel, for consistency.","**Don't over-wrap** — one boundary switch, not per-line."]},{t:"note",text:"Wrap the blocking call in withContext(Dispatchers.IO) inside a suspend function — it becomes main-safe locally, callers unchanged, only the blocking part runs off-Main. Put the withContext in the repository/data layer, and switch once at the boundary rather than per line."}]},{level:"senior",q:"What is Dispatchers.setMain used for, and why is it necessary in tests?",a:[{t:"p",text:"`Dispatchers.setMain(dispatcher)` replaces the global `Dispatchers.Main` with a test dispatcher. It's necessary because `Dispatchers.Main` requires the Android UI looper, which doesn't exist in a plain JVM unit test — so any code using `viewModelScope` (Main) would throw. Swapping in a test dispatcher lets ViewModel tests run on the JVM with a controllable clock."},{t:"code",title:"MainDispatcherRule",code:`class MainDispatcherRule(
    private val dispatcher: TestDispatcher = UnconfinedTestDispatcher(),
) : TestWatcher() {
    override fun starting(d: Description) = Dispatchers.setMain(dispatcher)
    override fun finished(d: Description) = Dispatchers.resetMain()
}
// @get:Rule val mainRule = MainDispatcherRule()`},{t:"list",items:["**Why needed** — `Dispatchers.Main` has no real looper in JVM tests; without `setMain` it throws.","**`setMain` / `resetMain`** — install in setup, reset in teardown (wrap in a JUnit rule).","**Which test dispatcher** — `StandardTestDispatcher` (explicit `advanceUntilIdle`) or `UnconfinedTestDispatcher` (eager execution).","**Enables ViewModel testing** — `viewModelScope` now runs on the test dispatcher and virtual clock."]},{t:"note",text:"Dispatchers.setMain(testDispatcher) replaces the global Main (which needs an Android looper that JVM unit tests lack, otherwise viewModelScope throws) with a test dispatcher; resetMain() in teardown. Wrap in a MainDispatcherRule. Pick StandardTestDispatcher (manual advance) or UnconfinedTestDispatcher (eager). Enables JVM ViewModel tests."}]},{level:"junior",q:"What are the main dispatchers at a glance, and how do you choose?",a:[{t:"p",text:"There are four built-in dispatchers, each matched to a kind of work. Choosing correctly is mostly about whether the work *waits* (I/O) or *computes* (CPU) or *touches UI* (Main)."},{t:"table",headers:["Dispatcher","For","Pool"],rows:[["`Main`","UI updates, light work","the UI thread"],["`IO`","network, disk, DB (waiting)","~64 threads"],["`Default`","CPU work (sort/parse/compute)","= CPU cores"],["`Unconfined`","advanced/tests only","no confinement"]]},{t:"list",items:["**`Main`** — anything touching UI; keep it light.","**`IO`** — blocking/waiting operations; many threads for concurrency.","**`Default`** — CPU-bound computation; core-sized pool.","**`Unconfined`** — rarely; unpredictable threading.","**Rule of thumb** — waiting → IO, computing → Default, UI → Main."]},{t:"note",text:"Four dispatchers: Main (UI, keep light), IO (network/disk/DB — waiting, ~64 threads), Default (CPU work — core-sized pool), Unconfined (advanced/tests, unpredictable). Choose by workload: waiting → IO, computing → Default, UI → Main."}]},{level:"senior",q:"Do IO and Default share threads, and what does switching between them actually do?",a:[{t:"p",text:"Yes — `Dispatchers.IO` and `Dispatchers.Default` are backed by the *same* shared pool of threads. Switching from `Default` to `IO` (or vice versa) via `withContext` may *not* actually move to a different physical thread; it changes the *parallelism limit* the coroutine runs under. IO permits up to ~64 concurrent, Default up to core-count."},{t:"list",items:["**Shared pool** — both draw from one common set of worker threads.","**Switching = changing the concurrency view** — `IO` allows more concurrent tasks; `Default` fewer; the switch may reuse the same thread when possible (an optimization avoiding a real hop).","**Why it matters** — `withContext(IO)` from `Default` is cheap when it can keep the thread; you're mainly changing the parallelism budget.","**`limitedParallelism`** — carves a bounded slice out of this shared pool without new threads."]},{t:"note",text:"IO and Default share one thread pool; switching between them changes the parallelism limit (IO ~64 concurrent, Default = cores), and can reuse the same physical thread (an optimization avoiding a real hop). So withContext(IO) from Default is cheap. limitedParallelism carves a bounded slice from the same shared pool."}]},{level:"junior",q:"What happens if you forget to move blocking work off the main thread?",a:[{t:"p",text:"The blocking call runs *on* the main thread, freezing the UI: no frames render, no touch input is processed. Short freezes cause visible jank (dropped frames); a freeze longer than ~5 seconds triggers an ANR (Application Not Responding) dialog and can be reported as a crash-like event. The fix is to move the work to `IO`/`Default`."},{t:"list",items:["**UI freeze** — the main thread can't render or handle input while blocked.","**Jank** — dropped frames for shorter blocks (>16ms at 60fps).","**ANR** — >5s blocking input triggers 'App isn't responding'; a top cause of poor ratings.","**Fix** — `withContext(Dispatchers.IO)` (or `Default` for CPU) around the blocking work; keep Main light.","**Detection** — StrictMode flags main-thread disk/network; the ANR/jank shows in vitals."]},{t:"note",text:"Blocking on Main freezes the UI (no rendering/input): short blocks = jank (dropped frames >16ms), >5s = an ANR ('App isn't responding'). Fix by moving work to withContext(IO/Default). StrictMode catches main-thread disk/network, and Play vitals surface ANRs/jank."}]},{level:"senior",q:"How does CoroutineContext inheritance interact with dispatchers across withContext boundaries?",a:[{t:"p",text:"A child coroutine inherits the parent context, including its dispatcher, unless overridden. `withContext(X)` overrides the dispatcher (and any elements you pass) *for its block*, and coroutines launched inside that block inherit `X`. When `withContext` returns, execution resumes on the *original* dispatcher — the override is scoped to the block."},{t:"code",title:"Dispatcher inheritance and scoping",code:`viewModelScope.launch {                    // Main
    launch { }                             // inherits Main
    withContext(Dispatchers.IO) {          // IO for this block
        launch { }                         // inherits IO
        withContext(Dispatchers.Default) {
            // Default here
        }                                   // back to IO
    }                                       // back to Main
    // here: Main again
}`},{t:"list",items:["**Inheritance** — children take the parent's dispatcher (and other context elements) by default.","**`withContext` scopes the override** — only within its block; nested `withContext` nests the scoping.","**Resume on the original** — after the block, you're back on the caller's dispatcher.","**Other elements too** — the same inheritance/override applies to `CoroutineName`, exception handler, etc. (but the Job is always a new child, not inherited)."]},{t:"note",text:"Children inherit the parent context (including dispatcher) unless overridden; withContext(X) overrides it only within its block (coroutines launched inside inherit X), and execution resumes on the original dispatcher after. Nesting nests the scoping. Same for CoroutineName/handler — but the Job is always a fresh child, never inherited."}]}],TS={id:"dispatchers",title:"Dispatchers & Context",tagline:"CoroutineContext & inheritance, Main/IO/Default/Unconfined, withContext for main-safety, where threading belongs, injecting dispatchers, and custom dispatchers.",tags:["Coroutines","Dispatchers","withContext","Threading","Context"],content:AS,qa:CS},IS=[{heading:"Cancellation is cooperative",blocks:[{t:"p",text:"The single most important fact about coroutine cancellation: it is **cooperative**, not forceful. Calling `job.cancel()` doesn't kill the coroutine dead — it *requests* cancellation by marking the Job as cancelling. The coroutine only actually stops when it reaches a point that **checks for cancellation** and responds by throwing a `CancellationException`. A coroutine that never checks will keep running despite being 'cancelled'."},{t:"list",items:["**Where cancellation is checked automatically**: every suspension point in the kotlinx.coroutines library — `delay`, `withContext`, `yield`, a Retrofit/Room suspend call, `Mutex.lock`, channel operations. When a cancelled coroutine hits one of these, it throws `CancellationException` and unwinds.","**Where it is NOT checked**: your own tight loops and long computations that don't suspend. A `while (true) { computeStuff() }` with no suspension point ignores cancellation entirely.","**Why cooperative and not forceful**: forcefully killing a thread mid-operation (like the old `Thread.stop()`) leaves resources in inconsistent states — half-written files, held locks. Cooperative cancellation lets the coroutine unwind cleanly through normal exception handling (`finally` blocks run), which is safe."]}]},{heading:"Making CPU-bound work cancellable",blocks:[{t:"p",text:"If your coroutine does CPU work without natural suspension points, you must *cooperate* — periodically check whether you've been cancelled. Two idioms:"},{t:"code",title:"Cooperating with cancellation",code:`// Option A: ensureActive() — throws CancellationException if cancelled
suspend fun process(items: List<Item>) {
    for (item in items) {
        currentCoroutineContext().ensureActive()   // checkpoint
        heavyCompute(item)
    }
}

// Option B: isActive check — exit gracefully without throwing
suspend fun process2(items: List<Item>) {
    for (item in items) {
        if (!currentCoroutineContext().isActive) return   // bail out
        heavyCompute(item)
    }
}

// Option C: yield() — checks cancellation AND lets other coroutines run
suspend fun process3(items: List<Item>) {
    for (item in items) {
        yield()          // cooperative point
        heavyCompute(item)
    }
}`},{t:"list",items:["**`ensureActive()`** — throws `CancellationException` immediately if cancelled (the normal cancellation path). Preferred: it integrates with structured concurrency (the exception propagates correctly).","**`isActive`** — a boolean you check to exit gracefully (e.g. `return`). Use when you want to stop *without* throwing.","**`yield()`** — checks cancellation *and* gives other coroutines a chance to run (useful in long loops to avoid hogging a dispatcher thread).","**The `.cancellable()` flow operator** does the equivalent for flows that emit rapidly without other suspension points."]}]},{heading:"CancellationException — the special exception",blocks:[{t:"p",text:"Cancellation works by throwing a **`CancellationException`** inside the coroutine. This exception is *special*: the coroutine machinery treats it as a normal, expected signal (the coroutine was told to stop), **not** as a failure. That's why a cancelled coroutine doesn't crash the app or propagate a failure to its parent — it just quietly winds down."},{t:"list",items:["**Never swallow it**: the cardinal rule. A broad `try { } catch (e: Exception) { }` around a suspend call *catches* the `CancellationException` and, if you don't rethrow it, the coroutine thinks it recovered and keeps running — cancellation is broken. Always rethrow it, or catch specific exceptions only.","**`try/finally` still runs on cancellation**: because cancellation is an exception, `finally` blocks execute during unwinding — the correct place for cleanup (close resources, release locks). This is why cooperative cancellation is safe: cleanup happens.","**Suspending in a `finally`**: a coroutine is already cancelled when `finally` runs, so calling a *suspend* function there (e.g. to save state) will *immediately* throw `CancellationException` again. To run suspend cleanup during cancellation, wrap it in `withContext(NonCancellable) { }` (see below)."]},{t:"code",title:"Cleanup during cancellation",code:`val job = launch {
    try {
        work()
    } finally {
        // non-suspend cleanup runs fine here
        closeResources()
        // suspend cleanup needs NonCancellable:
        withContext(NonCancellable) {
            saveProgressToDb()   // still runs even though we're cancelled
        }
    }
}`}]},{heading:"withTimeout and withTimeoutOrNull",blocks:[{t:"p",text:"Cancellation powers **timeouts**. `withTimeout(ms) { }` runs a block and **cancels it** (throwing `TimeoutCancellationException`) if it doesn't finish in time. `withTimeoutOrNull(ms) { }` does the same but returns `null` instead of throwing — cleaner when a timeout is an expected outcome."},{t:"code",title:"Timeouts",code:`// Throws TimeoutCancellationException on timeout:
val result = withTimeout(5000) { api.fetch() }

// Returns null on timeout (no exception to catch):
val resultOrNull = withTimeoutOrNull(5000) { api.fetch() }
if (resultOrNull == null) showTimeoutMessage()`},{t:"list",items:["The block must be *cancellable* (cooperative) for the timeout to actually interrupt it — a tight non-suspending loop inside `withTimeout` won't be stopped at the deadline.","`TimeoutCancellationException` is a subclass of `CancellationException`, so it's treated as cancellation — it cancels the block but (as a normal cancellation) doesn't propagate as a failure to the parent. Catch it explicitly if you want to react (or use `withTimeoutOrNull`)."]}]},{heading:"How scope cancellation ties it together",blocks:[{t:"list",items:["**Cancelling a scope/parent cancels all descendants** — the tree cancellation from structured concurrency. `viewModelScope` cancelled in `onCleared()` → every ViewModel coroutine gets a `CancellationException` at its next suspension point.","**Cancellation is one-directional (down) for cancel, and a normal outcome** — cancelling a child doesn't affect siblings or parent (unlike a failure).","**A cancelled scope is dead** — you can't launch new coroutines in it (they cancel immediately). This is why you never cancel `viewModelScope` yourself.","**Cooperation is the developer's responsibility for custom loops** — the framework cancels at suspension points for free, but your CPU loops need `ensureActive`/`isActive`/`yield`."]},{t:"note",text:"The three cancellation facts to state: (1) it's cooperative — checked at suspension points, so tight loops need manual `ensureActive()`; (2) it works via `CancellationException`, which must never be swallowed (rethrow it) and lets `finally` run for cleanup; (3) suspend cleanup during cancellation needs `withContext(NonCancellable)`. Timeouts (`withTimeout`) are cancellation with a deadline."}]}],MS=[{level:"junior",q:"What does it mean that coroutine cancellation is 'cooperative'?",a:[{t:"p",text:"**The concept**: calling `cancel()` on a coroutine doesn't forcibly kill it — it *requests* cancellation by marking the coroutine's Job as cancelling. The coroutine actually stops only when it reaches a point that **checks for cancellation** and responds. So cancellation requires cooperation from the coroutine's code: it must pass through cancellation checkpoints for the request to take effect."},{t:"list",items:["**Suspension points check automatically**: `delay`, `withContext`, `yield`, network/database suspend calls all check for cancellation — so a coroutine that regularly suspends responds to cancellation promptly and for free.","**Tight non-suspending loops don't**: `while (true) { heavyCompute() }` with no suspension point will keep running even after cancellation, because it never hits a checkpoint. You'd need to add `ensureActive()` or `yield()` in the loop.","**Why cooperative rather than forceful**: forcibly killing a coroutine mid-operation could leave things half-done (partial file writes, held locks). Cooperative cancellation throws an exception that unwinds cleanly, running `finally` blocks — so resources are released properly."]}]},{level:"junior",q:"How do you make a long-running loop cancellable?",a:[{t:"p",text:"**The concept**: a CPU-bound loop with no suspension points won't respond to cancellation on its own, so you add a cancellation checkpoint inside it. There are three ways:"},{t:"code",title:"Three ways to cooperate",code:`for (item in items) {
    ensureActive()        // throws CancellationException if cancelled (preferred)
    // or: if (!isActive) return   // exit gracefully without throwing
    // or: yield()                 // check cancellation + let others run
    heavyCompute(item)
}`},{t:"list",items:["**`ensureActive()`** — throws `CancellationException` immediately if the coroutine was cancelled. This is the preferred option because it integrates correctly with structured concurrency (the exception propagates as a proper cancellation).","**`isActive`** — a boolean; check it and `return`/`break` to stop gracefully without throwing, if that's what you want.","**`yield()`** — checks for cancellation *and* yields the thread so other coroutines can run, which is polite in long loops that would otherwise monopolize a dispatcher thread."]},{t:"p",text:"The rule of thumb: if your coroutine does meaningful work without calling any suspend function for a while, sprinkle in one of these checkpoints — typically once per loop iteration. Without them, cancelling the coroutine (or its scope) has no effect until the whole computation finishes."}]},{level:"junior",q:"What is withTimeout and how is it different from withTimeoutOrNull?",a:[{t:"p",text:"**Both run a block with a time limit and cancel it if it exceeds the deadline — they differ in what happens on timeout.** `withTimeout(ms) { }` **throws** a `TimeoutCancellationException` if the block doesn't finish in time. `withTimeoutOrNull(ms) { }` instead **returns null** on timeout, with no exception to catch."},{t:"code",title:"Both forms",code:`// Throwing form — handle the exception
try {
    val data = withTimeout(5000) { api.fetch() }
} catch (e: TimeoutCancellationException) {
    showError("Timed out")
}

// Null form — cleaner when timeout is expected
val data = withTimeoutOrNull(5000) { api.fetch() }
if (data == null) showError("Timed out")`},{t:"p",text:"Use `withTimeoutOrNull` when a timeout is a normal, expected branch you handle with a null check (usually cleaner). Use `withTimeout` when a timeout is genuinely exceptional and you want it to throw. One caveat for both: the block must be *cancellable* (cooperative) for the timeout to interrupt it — if the block is a tight non-suspending computation, it won't be stopped at the deadline because there's no checkpoint to cancel at."}]},{level:"junior",q:"Why should you never catch and ignore a CancellationException?",a:[{t:"p",text:"**Because cancellation *is* a CancellationException — swallowing it breaks cancellation entirely.** When a coroutine is cancelled, the framework throws a `CancellationException` inside it to unwind it. If your code catches that exception broadly (`catch (e: Exception)`) and doesn't rethrow it, the coroutine 'recovers' from its own cancellation and keeps running — the exact opposite of what cancellation is supposed to do. The coroutine you thought you cancelled continues executing."},{t:"code",title:"The bug and the fix",code:`// BUG: swallows cancellation
try { doWork() } catch (e: Exception) { log(e) }

// FIX: rethrow CancellationException
try {
    doWork()
} catch (e: CancellationException) {
    throw e                    // let cancellation proceed
} catch (e: Exception) {
    log(e)                     // handle real errors only
}`},{t:"p",text:"This is a subtle, common bug because `CancellationException` is a subclass of `Exception`, so a broad catch grabs it unintentionally. The fix is to either catch `CancellationException` first and rethrow it, catch only the specific exceptions you actually expect, or use a helper like `runCatching`-with-rethrow. The principle: cancellation must be allowed to propagate; only *genuine errors* should be caught and handled."}]},{level:"senior",q:"You need to save progress to the database when a coroutine is cancelled, but the save is a suspend function. What's the problem and the fix?",a:[{t:"p",text:"**The problem**: cleanup goes in a `finally` block, but by the time `finally` runs during cancellation, the coroutine is *already in the cancelled state*. Any **suspend** function you call there — including your database save — will *immediately* throw `CancellationException` again, because a cancelled coroutine refuses to suspend. So your `saveProgress()` never actually runs; it's aborted the instant it suspends."},{t:"code",title:"The fix: NonCancellable",code:`val job = launch {
    try {
        doWork()
    } finally {
        // Non-suspend cleanup is fine:
        releaseInMemoryResources()

        // Suspend cleanup during cancellation MUST be shielded:
        withContext(NonCancellable) {
            database.saveProgress(currentState)   // now allowed to suspend & complete
        }
    }
}`},{t:"list",items:["**`NonCancellable`** is a special `Job` that can't be cancelled. Wrapping suspend cleanup in `withContext(NonCancellable) { }` creates a context where the coroutine is temporarily *not* cancellable, so suspend calls inside it are allowed to run to completion even though the outer coroutine is being cancelled.","**Use it sparingly and only for cleanup**: it deliberately ignores cancellation, so long or unbounded work inside `NonCancellable` defeats the point of cancellation (the coroutine can't be stopped until it finishes). Keep it to quick, essential cleanup (save state, flush a buffer, close a resource that needs a suspend call).","**Non-suspend cleanup doesn't need it**: closing a file handle or clearing a reference in `finally` works fine without `NonCancellable` — only *suspend* cleanup hits the 'already cancelled, can't suspend' problem."]},{t:"p",text:"**The senior insight**: this reveals a precise understanding of the cancellation state machine — that `finally` runs during unwinding, that a cancelled coroutine can't suspend, and that `NonCancellable` is the escape hatch for the narrow case of *suspending cleanup during cancellation*. It's a favorite question because the naive 'just save it in finally' fails silently."}]},{level:"senior",q:"A user leaves a screen but a network request keeps running. Walk through why, and how cancellation should prevent it.",a:[{t:"p",text:"**Why it happens**: the request was launched in a scope not tied to the screen's lifetime — the usual culprits are `GlobalScope.launch`, a manually-created `CoroutineScope` that's never cancelled, or a callback-based API (Retrofit `Call`, an old listener) that isn't coroutine-managed at all. Because nothing cancels that work when the screen is destroyed, it runs to completion, wasting network/battery and possibly trying to update a destroyed UI (leak or crash)."},{t:"list",items:["**The fix — launch in a lifecycle-bound scope**: run the request in `viewModelScope` (cancelled in `onCleared`) or `lifecycleScope` (cancelled on destroy). When the user leaves and the ViewModel is cleared / the lifecycle ends, the scope is cancelled, which — via structured concurrency — cancels the request coroutine. At its next suspension point (the network call is a suspension point), it throws `CancellationException` and stops.","**Retrofit/OkHttp cooperation**: modern Retrofit suspend functions are cancellation-aware — cancelling the coroutine actually cancels the underlying HTTP call. So the request isn't just abandoned in code; the socket work is cancelled too. (Old `enqueue`-callback style wasn't tied to any scope, which is why it leaked.)","**If the work must genuinely continue** (an upload that should finish regardless): that's a *different* requirement — it doesn't belong in the screen's scope at all. Put it in an application-scoped scope or WorkManager, so leaving the screen correctly does *not* cancel it."]},{t:"p",text:"**The principle to articulate**: 'a coroutine keeps running after the screen is gone' is almost always a *scope* bug — the work was bound to the wrong lifetime. Structured concurrency solves it by tying the coroutine to a lifecycle scope, so scope cancellation cascades down and cancels the request. The decision is simply: does this work belong to the screen (viewModelScope/lifecycleScope) or should it outlive the screen (app scope / WorkManager)? Bind it to the matching lifetime and cancellation takes care of itself."}]},{level:"junior",q:"How do you actually cancel a coroutine?",a:[{t:"p",text:"You cancel through the handle: `job.cancel()` for a single coroutine, `job.cancelAndJoin()` to cancel and wait until it's fully stopped, or `scope.cancel()` to cancel every coroutine in a scope. Cancellation is *requested* — the coroutine actually stops at its next suspension point (cooperative cancellation)."},{t:"code",title:"Ways to cancel",code:`val job = scope.launch { work() }
job.cancel()                       // request cancellation (returns immediately)
job.cancelAndJoin()                // cancel AND suspend until it's done winding down
scope.cancel()                     // cancel all coroutines in the scope
job.cancel(CancellationException("user aborted"))   // with a custom cause`},{t:"list",items:["**`cancel()`** — requests cancellation; the coroutine stops at the next suspension point.","**`cancelAndJoin()`** — cancel then wait for it to finish (including cleanup); use when you must ensure it's gone before proceeding.","**`scope.cancel()`** — cancels the whole scope's tree (used for teardown).","**Managed scopes** — `viewModelScope`/`lifecycleScope` cancel automatically; you rarely call these manually."]},{t:"note",text:"job.cancel() (request, returns immediately), job.cancelAndJoin() (cancel + wait until fully stopped), or scope.cancel() (whole tree). Cancellation is cooperative — it takes effect at the next suspension point. Lifecycle scopes cancel automatically, so manual cancel is mostly for custom scopes or cancel-previous patterns."}]},{level:"junior",q:"What is CancellationException, and why is it treated specially?",a:[{t:"p",text:"`CancellationException` is the exception thrown at suspension points to unwind a cancelled coroutine. It's treated as a *normal, expected* signal rather than an error: it doesn't propagate up to cancel the parent (unlike other exceptions), and the machinery relies on it flowing freely. That's why you must never swallow it in a broad `catch`."},{t:"code",title:"It flows through suspension points",code:`launch {
    try {
        delay(1000)   // throws CancellationException if cancelled here
    } catch (e: CancellationException) {
        // if you catch it, RETHROW it — don't swallow
        throw e
    }
}`},{t:"list",items:["**Signals cancellation** — thrown at suspension points in a cancelled coroutine to unwind it.","**Not a failure** — doesn't cancel the parent/siblings; it's the expected end of a cancelled coroutine.","**Must not be swallowed** — catching and ignoring it makes the coroutine 'un-cancellable' and breaks structured concurrency.","**`try/finally` still runs** — cleanup executes as it unwinds."]},{t:"note",text:"CancellationException is thrown at suspension points to unwind a cancelled coroutine — a normal, expected signal, not a failure (it doesn't cancel the parent). Never swallow it in a broad catch (that breaks cancellation); if you catch it, rethrow. finally blocks still run during the unwind."}]},{level:"senior",q:"Why can't coroutines be force-killed, and why is cooperative cancellation the design?",a:[{t:"p",text:"There's no safe way to forcibly stop a thread mid-execution (Java deprecated `Thread.stop()` for exactly this reason — it can leave locks held and state corrupted). So coroutines cancel *cooperatively*: cancellation sets a flag, and the coroutine notices at its next suspension point (or explicit check) and throws. This guarantees cleanup runs and state stays consistent."},{t:"list",items:["**Force-kill is unsafe** — abruptly stopping code can corrupt shared state and leak resources (held locks, open files).","**Cooperative = safe** — the coroutine unwinds via `CancellationException`, running `finally`/cleanup properly.","**Requires cooperation** — code that never suspends or checks `isActive` won't cancel; it's your job to make long loops cooperative.","**Built-in suspend functions cooperate** — `delay`, `withContext`, `yield`, and library suspend calls all check cancellation."]},{t:"note",text:"Force-killing a thread mid-execution is unsafe (Thread.stop is deprecated — leaves locks held, state corrupt), so coroutines cancel cooperatively: a flag is set and the coroutine throws CancellationException at its next suspension point, unwinding cleanly with finally/cleanup. The cost: non-suspending loops must check isActive/yield to be cancellable."}]},{level:"junior",q:"How do suspend functions detect cancellation?",a:[{t:"p",text:"All the built-in suspend functions (`delay`, `yield`, `withContext`, and library calls like Retrofit/Room) check the cancellation state and throw `CancellationException` if the coroutine was cancelled. So merely *having suspension points* usually makes a coroutine cancellable — the check happens automatically each time it suspends/resumes."},{t:"list",items:["**Automatic at suspension points** — every `suspend` call from the library is a cancellation check.","**Throws `CancellationException`** — unwinding the coroutine when cancelled.","**Most code is cancellable for free** — because it awaits I/O (`delay`, network, DB) frequently.","**The exception** — a tight CPU loop with *no* suspension points never checks; you must add `ensureActive()`/`yield()`."]},{t:"code",title:"Suspension points = cancellation checks",code:`suspend fun loadAll(ids: List<String>) {
    for (id in ids) {
        val item = api.get(id)   // suspends -> checks cancellation each iteration
        cache.put(id, item)
    }
}`},{t:"note",text:"Built-in suspend functions (delay/yield/withContext/Retrofit/Room) check cancellation and throw CancellationException when cancelled — so code with frequent suspension points (I/O) is cancellable for free. Only a tight CPU loop with no suspension points won't notice; add ensureActive()/yield() there."}]},{level:"senior",q:"What's the difference between ensureActive(), isActive, and yield() for cooperative cancellation?",a:[{t:"p",text:"All three help make non-suspending work cancellable, but differ. `isActive` is a boolean you check to break out of a loop; `ensureActive()` throws `CancellationException` immediately if cancelled (cleaner than checking `isActive` and returning); `yield()` both checks cancellation *and* gives other coroutines a turn (fairness)."},{t:"code",title:"Three cooperation tools",code:`// isActive: check and break
while (isActive) { computeStep() }

// ensureActive(): throw if cancelled (preferred for 'just check')
for (item in items) { ensureActive(); process(item) }

// yield(): check + let others run (for long fair loops)
for (item in items) { process(item); yield() }`},{t:"list",items:["**`isActive`** — a `Boolean`; use to exit a loop gracefully (returns partial work).","**`ensureActive()`** — throws `CancellationException` if cancelled; the idiomatic 'check for cancellation' that unwinds properly.","**`yield()`** — checks cancellation *and* suspends briefly so other coroutines progress; use in long loops that could starve the dispatcher.","**Pick** — `ensureActive()` for a plain check, `yield()` when fairness matters, `isActive` when you want to stop without throwing."]},{t:"note",text:"isActive = boolean to break a loop (partial result). ensureActive() = throws CancellationException if cancelled (idiomatic check, unwinds cleanly). yield() = checks cancellation AND yields to other coroutines (fairness in long loops). Use ensureActive for a plain check, yield when starvation is a risk, isActive to stop without throwing."}]},{level:"senior",q:"What happens if a CPU-bound loop never checks for cancellation?",a:[{t:"p",text:"It becomes *uncancellable* — cancelling the coroutine sets the flag, but the loop, having no suspension points or checks, runs to completion anyway. Worse, it can keep a thread busy after the screen/scope is gone (wasting CPU and battery), and on a limited dispatcher it can starve other coroutines waiting for that thread."},{t:"code",title:"Uncancellable vs cancellable",code:`// BAD: ignores cancellation, runs fully even if cancelled
launch { var x = 0L; while (x < 1_000_000_000) { x += compute() } }

// GOOD: cooperative
launch {
    var x = 0L
    while (x < 1_000_000_000) {
        ensureActive()     // or yield()
        x += compute()
    }
}`},{t:"list",items:["**Runs to completion** — cancellation has no effect without a check.","**Wastes resources** — CPU/battery spent on work whose result is discarded.","**Can starve others** — on `Default` (core-sized pool), a busy uncancellable loop hogs a scarce thread.","**Fix** — sprinkle `ensureActive()`/`yield()`/`isActive`, or break the work into suspending chunks."]},{t:"note",text:"A CPU loop with no suspension points or checks is uncancellable — cancellation sets a flag it never reads, so it runs fully, wasting CPU/battery on a discarded result and potentially starving a scarce Default thread. Fix with periodic ensureActive()/yield()/isActive checks or suspending chunks."}]},{level:"senior",q:"How does withTimeout work, and how do you handle its exception?",a:[{t:"p",text:"`withTimeout(ms) { }` runs a block and *cancels* it if it doesn't finish within the time limit, throwing a `TimeoutCancellationException` (a subclass of `CancellationException`). `withTimeoutOrNull(ms)` does the same but returns `null` instead of throwing on timeout. Use them to bound how long an operation may take."},{t:"code",title:"Timeouts",code:`try {
    val result = withTimeout(5000) { api.slowCall() }
} catch (e: TimeoutCancellationException) {
    showError("Request timed out")
}
// Or the non-throwing variant:
val result = withTimeoutOrNull(5000) { api.slowCall() } ?: fallback()`},{t:"list",items:["**`withTimeout`** — throws `TimeoutCancellationException` if the block exceeds the limit; the block is cancelled cooperatively.","**`withTimeoutOrNull`** — returns `null` on timeout (cleaner when a timeout is a normal outcome).","**Cooperative** — the block must be cancellable (suspension points) for the timeout to actually stop it.","**Catch caution** — `TimeoutCancellationException` *is* a `CancellationException`; catch it *specifically* (not broad `CancellationException`) so you don't interfere with real cancellation."]},{t:"note",text:"withTimeout(ms){} cancels the block and throws TimeoutCancellationException (a CancellationException subclass) if it overruns; withTimeoutOrNull returns null instead. The block must be cancellable (suspension points) to actually stop. Catch TimeoutCancellationException specifically — not broad CancellationException — to avoid swallowing real cancellation."}]},{level:"junior",q:"What's the difference between cancel() and cancelAndJoin()?",a:[{t:"p",text:"`cancel()` only *requests* cancellation and returns immediately — the coroutine may still be winding down (running cleanup) afterward. `cancelAndJoin()` requests cancellation *and suspends until the coroutine has fully finished*, including its `finally` cleanup. Use `cancelAndJoin()` when you must be sure the old work is completely gone before starting new work."},{t:"code",title:"Ensuring the old job is done",code:`// Restart a task, guaranteeing the old one is fully stopped first:
oldJob?.cancelAndJoin()      // wait until it's completely finished
oldJob = scope.launch { newWork() }`},{t:"list",items:["**`cancel()`** — fire-and-forget request; doesn't wait.","**`cancelAndJoin()`** — cancel then `join`; suspends until fully cancelled (cleanup done).","**Use `cancelAndJoin`** — when the new work must not overlap the old (shared resource, exclusive access).","**`cancel()` is fine** — when you don't care exactly when it stops."]},{t:"note",text:"cancel() just requests cancellation and returns immediately (coroutine may still be winding down). cancelAndJoin() cancels AND suspends until it's fully finished (cleanup done). Use cancelAndJoin when new work must not overlap the old (shared/exclusive resource); cancel() when timing doesn't matter."}]},{level:"senior",q:"How do you make a blocking (non-suspending) call cancellable?",a:[{t:"p",text:"A blocking Java call (a synchronous read, a library that blocks a thread) doesn't respond to coroutine cancellation on its own. Wrap it in `runInterruptible { }`, which runs the block so that coroutine cancellation *interrupts the thread* (setting the interrupt flag / throwing `InterruptedException`) — bridging thread interruption to coroutine cancellation. Alternatively, use `suspendCancellableCoroutine` with `invokeOnCancellation` to abort the underlying operation."},{t:"code",title:"runInterruptible bridges interruption",code:`suspend fun readBlocking(): ByteArray = withContext(Dispatchers.IO) {
    runInterruptible {
        blockingInputStream.readBytes()   // cancellation -> thread interrupt -> stops
    }
}`},{t:"list",items:["**`runInterruptible { }`** — maps coroutine cancellation to `Thread.interrupt()`, so interruptible blocking calls (many I/O APIs) abort.","**`suspendCancellableCoroutine` + `invokeOnCancellation`** — for callback/handle-based APIs, cancel the underlying request explicitly.","**Not all blocking is interruptible** — some native calls ignore interruption; then you can only abandon the coroutine (the thread stays busy until the call returns).","**Run on `IO`** — blocking work belongs off the main thread regardless."]},{t:"note",text:"Wrap a blocking call in runInterruptible { } (inside withContext(IO)) — it maps coroutine cancellation to Thread.interrupt(), aborting interruptible blocking I/O. For handle/callback APIs, use suspendCancellableCoroutine + invokeOnCancellation to cancel the underlying op. Non-interruptible native calls can't be stopped — the thread stays busy until they return."}]},{level:"junior",q:"Does delay() respond to cancellation immediately?",a:[{t:"p",text:"Yes — `delay` is a suspension point, so if the coroutine is cancelled during a `delay`, it throws `CancellationException` *immediately*, without waiting for the delay to elapse. This is why `delay`-based debouncing and backoff cancel cleanly the moment the coroutine is cancelled."},{t:"code",title:"delay cancels instantly",code:`val job = launch {
    delay(10_000)          // if cancelled at t=1s, throws now — doesn't wait to t=10s
    println("never prints if cancelled")
}
delay(1000)
job.cancel()               // delay above throws CancellationException immediately`},{t:"list",items:["**Immediate** — a cancelled coroutine's `delay` throws at once, not after the timeout.","**Great for debounce** — rapid input cancels the pending `delay` before the action fires.","**Contrast `Thread.sleep`** — blocks and ignores cancellation; never use it in coroutines.","**All suspension points behave this way** — `delay` is just the common example."]},{t:"note",text:"Yes — delay is a suspension point, so a cancelled coroutine's delay throws CancellationException immediately (doesn't wait out the timeout). That's why delay-based debounce/backoff cancels cleanly. Thread.sleep, by contrast, blocks and ignores cancellation — never use it in coroutines."}]},{level:"senior",q:"How do you clean up or unregister resources when a coroutine is cancelled?",a:[{t:"p",text:"Use `try/finally` for general cleanup (the `finally` runs during cancellation unwinding), and for callback-bridging use `invokeOnCancellation` inside `suspendCancellableCoroutine` (or `awaitClose` in `callbackFlow`) to unregister listeners. Remember: *suspending* cleanup in `finally` needs `withContext(NonCancellable)` since the coroutine is already cancelled."},{t:"code",title:"Cleanup patterns",code:`// try/finally for resources
try { useResource() } finally { resource.close() }

// invokeOnCancellation for callbacks
suspendCancellableCoroutine { cont ->
    val cb = register { cont.resume(it) }
    cont.invokeOnCancellation { unregister(cb) }   // cleanup if cancelled
}`},{t:"list",items:["**`try/finally`** — runs on normal completion *and* cancellation; close streams/resources here.","**`invokeOnCancellation`** — unregister a callback / abort the underlying request when cancelled.","**`awaitClose { }`** — the `callbackFlow` equivalent for stream cleanup.","**Suspending cleanup** — wrap in `withContext(NonCancellable)` so it isn't cancelled before running."]},{t:"note",text:"General cleanup: try/finally (runs during cancellation unwind). Callback bridges: invokeOnCancellation in suspendCancellableCoroutine, or awaitClose in callbackFlow, to unregister/abort. Suspending cleanup in finally must use withContext(NonCancellable) or it's cancelled before it runs."}]},{level:"senior",q:"Why does a broad catch(Exception) break cancellation, and what's the fix?",a:[{t:"p",text:"`CancellationException` is an `Exception`, so a broad `catch (e: Exception)` *swallows* it — the coroutine thinks it recovered and keeps running instead of unwinding, defeating cancellation and breaking structured concurrency. The fix is to rethrow `CancellationException` (or catch more specific types), so cancellation propagates while you still handle real errors."},{t:"code",title:"Preserve cancellation",code:`try {
    doWork()
} catch (e: CancellationException) {
    throw e                       // MUST rethrow — let cancellation propagate
} catch (e: Exception) {
    handleRealError(e)
}
// or catch specific types: catch (e: IOException) { ... }`},{t:"list",items:["**The trap** — `catch (Exception)` also catches `CancellationException`, keeping a cancelled coroutine alive.","**Fix 1** — rethrow `CancellationException` first, before the broad catch.","**Fix 2** — catch specific exceptions (`IOException`, etc.) instead of `Exception`.","**`runCatching` caveat** — it also swallows `CancellationException`; avoid it in coroutine code (or rethrow inside)."]},{t:"note",text:"CancellationException is an Exception, so catch(Exception) swallows it — the cancelled coroutine keeps running, breaking cancellation/structured concurrency. Fix: rethrow CancellationException first (or catch specific types like IOException). Note runCatching has the same problem — avoid it in coroutine code or rethrow inside."}]},{level:"junior",q:"Can a cancelled coroutine be restarted or resumed?",a:[{t:"p",text:"No — cancellation is terminal. Once a coroutine (its `Job`) is cancelled, it can't be resumed or restarted; the `Job` is in a final Cancelled state. To 'retry', you *launch a new coroutine*. Similarly, a cancelled scope can't launch new coroutines — you'd create a fresh scope."},{t:"list",items:["**Terminal state** — a cancelled `Job` stays cancelled; no resume/restart.","**Retry = new coroutine** — `launch` again for a fresh attempt.","**Cancelled scope is dead** — new launches on it are born cancelled; make a new scope.","**Design** — model retries as new work, not reviving old coroutines."]},{t:"note",text:"No — cancellation is terminal; a cancelled Job can't resume/restart, and a cancelled scope can't launch new coroutines. To retry, launch a fresh coroutine (or create a new scope). Model retries as new work, never as reviving a cancelled coroutine."}]},{level:"junior",q:"How does cancellation propagate to child coroutines?",a:[{t:"p",text:"Cancelling a coroutine cancels *all* of its children (and their children), recursively down the tree. This is a structured-concurrency guarantee: you can't leave orphaned children when the parent is cancelled. Each child receives `CancellationException` at its next suspension point and unwinds."},{t:"code",title:"Parent cancel → children cancel",code:`val parent = scope.launch {
    launch { childA() }   // cancelled when parent is
    launch { childB() }   // cancelled when parent is
}
parent.cancel()           // both children stop at their next suspension point`},{t:"list",items:["**Recursive** — the whole subtree is cancelled.","**At suspension points** — each child throws `CancellationException` and runs its cleanup.","**Guaranteed** — no child survives its parent's cancellation.","**Scope cancel** — `scope.cancel()` cancels every coroutine in the scope the same way."]},{t:"note",text:"Cancelling a coroutine recursively cancels all descendants — a structured-concurrency guarantee (no orphaned children). Each child throws CancellationException at its next suspension point and runs cleanup. scope.cancel() does this for the whole scope's tree."}]},{level:"senior",q:"How do you implement retry with a per-attempt timeout?",a:[{t:"p",text:"Combine a retry loop with `withTimeout` per attempt and a backoff `delay`. Each attempt is bounded by `withTimeout`; on failure or timeout you wait (increasing delay) and retry up to a limit, then give up. Because `delay` and `withTimeout` are cancellable, the whole retry respects coroutine cancellation."},{t:"code",title:"Retry with timeout and backoff",code:`suspend fun <T> retry(times: Int, timeoutMs: Long, block: suspend () -> T): T {
    var delayMs = 500L
    repeat(times - 1) { attempt ->
        try {
            return withTimeout(timeoutMs) { block() }
        } catch (e: CancellationException) {
            if (e is TimeoutCancellationException) { /* treat as retryable */ } else throw e
        } catch (e: Exception) { /* retryable */ }
        delay(delayMs); delayMs *= 2   // exponential backoff (cancellable)
    }
    return withTimeout(timeoutMs) { block() }   // last attempt, let it throw
}`},{t:"list",items:["**Per-attempt `withTimeout`** — bounds each try; a hung attempt times out and retries.","**Backoff `delay`** — space out retries (exponential + optional jitter); cancellable.","**Careful with `CancellationException`** — retry on `TimeoutCancellationException` but rethrow *real* cancellation, so navigation-away still stops the retry.","**Cap attempts** — give up and surface the error after N tries."]},{t:"note",text:"Retry loop + withTimeout(per attempt) + exponential backoff delay. Bound each try; on failure/timeout, delay (cancellable) and retry up to a cap. Subtlety: retry on TimeoutCancellationException but rethrow genuine CancellationException so real cancellation (navigation) still stops the retries."}]},{level:"senior",q:"What's the difference between cancelling a coroutine and it timing out?",a:[{t:"p",text:"Both end the coroutine via `CancellationException`, but the *cause* and *intent* differ. Cancellation is external — someone called `cancel()` (or a parent/scope was cancelled). A timeout is `withTimeout` cancelling the block because it ran too long, throwing the more specific `TimeoutCancellationException`. A timeout is a *deliberate deadline*; cancellation is a *lifecycle/decision* stop."},{t:"list",items:["**Cancellation** — triggered by `cancel()`/scope teardown; throws `CancellationException`; usually you don't handle it (let it propagate).","**Timeout** — triggered by `withTimeout` exceeding its limit; throws `TimeoutCancellationException` (a subclass); often you *do* handle it (show 'timed out', fall back).","**Distinguish by type** — catch `TimeoutCancellationException` specifically to react to timeouts while letting real cancellation propagate.","**`withTimeoutOrNull`** — turns a timeout into a `null` result instead of an exception."]},{t:"note",text:"Both throw CancellationException, but cancellation is external (cancel()/scope teardown — usually let it propagate) while a timeout is withTimeout hitting its deadline (throws the subclass TimeoutCancellationException — often handled: fallback/'timed out'). Catch TimeoutCancellationException specifically to react to timeouts without swallowing real cancellation."}]},{level:"senior",q:"How do you test that a coroutine cancels correctly?",a:[{t:"p",text:"In `runTest`, launch the coroutine, then cancel it and assert the observable effects: cleanup ran (`finally`/`invokeOnCancellation`), no further state changes happened, and downstream resources were released. The virtual clock and `advanceUntilIdle`/`advanceTimeBy` let you control exactly when cancellation happens relative to the work."},{t:"code",title:"Testing cancellation",code:`@Test fun cancelsCleanly() = runTest {
    var cleanedUp = false
    val job = launch {
        try { delay(10_000) } finally { cleanedUp = true }
    }
    advanceTimeBy(1000)     // let it start and suspend on delay
    job.cancelAndJoin()     // cancel and wait
    assertTrue(cleanedUp)   // finally ran
}`},{t:"list",items:["**`cancelAndJoin()`** — cancel and wait so assertions run after cleanup.","**Assert side effects** — `finally` cleanup flags, released resources, no post-cancel state updates.","**Virtual time** — `advanceTimeBy` to position cancellation mid-work.","**Verify unregistration** — for callback bridges, assert the listener was removed via a fake."]},{t:"note",text:"In runTest: launch, position with advanceTimeBy, then cancelAndJoin() and assert observable effects — finally/cleanup ran, resources released, no further state changes, callbacks unregistered (via a fake). The virtual clock lets you cancel at a precise point in the work deterministically."}]},{level:"junior",q:"What happens to a withContext block when the coroutine is cancelled?",a:[{t:"p",text:"The `withContext` block is part of the current coroutine, so cancelling the coroutine cancels the block too: at its next suspension point it throws `CancellationException` and unwinds (running any `finally`). Cancellation flows through `withContext` naturally — it doesn't shield the block from cancellation (that's what `NonCancellable` is for)."},{t:"list",items:["**Cancellable** — a `withContext(IO)` block stops at its next suspension point when the coroutine is cancelled.","**Cleanup runs** — `finally` inside executes during the unwind.","**Not a shield** — regular `withContext(dispatcher)` doesn't prevent cancellation; only `withContext(NonCancellable)` does.","**Return value discarded** — if cancelled mid-block, the coroutine unwinds; you don't get a partial result."]},{t:"note",text:"A withContext block is part of the current coroutine, so cancelling the coroutine cancels it — it throws CancellationException at the next suspension point and runs finally cleanup. withContext(dispatcher) doesn't shield the block; only withContext(NonCancellable) makes it ignore cancellation."}]},{level:"senior",q:"How do you race coroutines so the first result wins and the rest are cancelled?",a:[{t:"p",text:"Wrap the racers in a `coroutineScope` (or use `select`), take the first completed result, and cancel the scope — cancelling the losers. A common idiom: launch each source as an `async`, `select` over their `onAwait`, and let leaving the scope cancel the unfinished ones. This is how you implement 'fastest source wins' (e.g. cache vs network)."},{t:"code",title:"First-wins with select",code:`suspend fun fastest(): Data = coroutineScope {
    val cache = async { fromCache() }
    val net   = async { fromNetwork() }
    select {
        cache.onAwait { it }
        net.onAwait { it }
    }.also { coroutineContext.cancelChildren() }   // cancel the slower one
}`},{t:"list",items:["**`select { ... onAwait }`** — resumes with whichever `Deferred` completes first.","**Cancel the rest** — `cancelChildren()` (or leaving the scope) cancels the losers so they don't waste resources.","**Structured** — the `coroutineScope` ensures no racer outlives the call.","**Uses** — cache-vs-network, multiple mirrors, fallback endpoints."]},{t:"note",text:"Race with coroutineScope + async per source + select { onAwait } to take the first result, then cancelChildren() (or scope exit) to cancel the losers. Structured concurrency guarantees no racer outlives the call. Classic use: cache-vs-network 'fastest wins' or fallback endpoints."}]},{level:"senior",q:"Why is it important that network clients and repositories propagate cancellation?",a:[{t:"p",text:"If a repository or HTTP client ignores cancellation, cancelling the coroutine (e.g. when the user leaves the screen) won't actually abort the in-flight request — the work continues, wasting bandwidth/battery and possibly delivering a result to dead UI. Cancellation propagation means the whole chain, down to the socket, stops when the coroutine is cancelled."},{t:"list",items:["**End-to-end cancellation** — the coroutine cancel should reach the actual network/DB call and abort it.","**Retrofit/OkHttp suspend calls cooperate** — a cancelled coroutine cancels the underlying `Call` (aborts the request).","**Custom/blocking clients** — must bridge cancellation (`suspendCancellableCoroutine` + `invokeOnCancellation`, or `runInterruptible`) or they leak work.","**Payoff** — leaving a screen truly stops its network activity; no wasted requests or stale callbacks."]},{t:"note",text:"If the client/repo ignores cancellation, cancelling the coroutine won't abort the in-flight request — wasted bandwidth/battery and results delivered to dead UI. Retrofit/OkHttp suspend calls cancel the underlying Call automatically; custom/blocking clients must bridge cancellation (suspendCancellableCoroutine + invokeOnCancellation / runInterruptible). Then leaving a screen truly stops its work."}]},{level:"junior",q:"How do you cancel one specific coroutine while others keep running?",a:[{t:"p",text:"Keep the `Job` returned by that specific `launch`/`async` and call `cancel()` on it — this cancels only that coroutine and its children, leaving sibling coroutines in the same scope untouched. This is the basis of patterns like cancel-previous-search (cancel one operation, keep the rest of the screen working)."},{t:"code",title:"Targeted cancellation",code:`val searchJob = scope.launch { search() }
val syncJob = scope.launch { sync() }
searchJob.cancel()    // cancels ONLY the search; sync keeps running`},{t:"list",items:["**Hold the specific `Job`** — cancel it individually.","**Siblings unaffected** — cancelling one child doesn't touch others (cancellation flows down, not sideways).","**vs `scope.cancel()`** — that would cancel everything; use it only for teardown.","**Cancel-previous** — store the job, cancel it before relaunching for search-as-you-type."]},{t:"note",text:"Keep that coroutine's Job and call job.cancel() — it cancels only that coroutine (and its children); siblings keep running (cancellation flows down, not sideways). Use scope.cancel() only for teardown. This underlies cancel-previous patterns (cancel the old search, keep the rest working)."}]},{level:"senior",q:"How can you inspect why a coroutine was cancelled?",a:[{t:"p",text:'The cancellation *cause* is available via `invokeOnCompletion { cause -> }` or `job.getCancellationException()`. When you cancel with a custom message (`cancel(CancellationException("reason"))`), that cause is preserved, so completion handlers can distinguish why — user abort vs timeout vs parent failure.'},{t:"code",title:"Reading the cancellation cause",code:`job.cancel(CancellationException("user navigated away"))
job.invokeOnCompletion { cause ->
    when (cause) {
        is TimeoutCancellationException -> log("timed out")
        is CancellationException -> log("cancelled: \${cause.message}")
        null -> log("completed normally")
        else -> log("failed: $cause")
    }
}`},{t:"list",items:['**Custom cause** — `cancel(CancellationException("..."))` carries a reason.',"**`invokeOnCompletion { cause }`** — inspect the cause (null = success, `CancellationException` = cancelled, other = failure).","**Type distinguishes** — `TimeoutCancellationException` vs a plain cancellation.","**Debugging** — helpful in logs to understand why long-running work stopped."]},{t:"note",text:'Pass a cause: cancel(CancellationException("reason")), then read it in invokeOnCompletion { cause } (null=success, CancellationException=cancelled with your message, TimeoutCancellationException=timeout, else=failure) or job.getCancellationException(). Useful for logging why long-running work stopped.'}]},{level:"junior",q:"What is NonCancellable, and when do you use it?",a:[{t:"p",text:"`NonCancellable` is a special `Job` you pass to `withContext(NonCancellable) { }` to make a block *immune* to the ongoing cancellation. Because a cancelled coroutine throws at every suspension point, any *suspending* cleanup in a `finally` would be cancelled before it runs — `NonCancellable` lets that critical cleanup complete."},{t:"code",title:"Guaranteed suspending cleanup",code:`try {
    uploadChunks()
} finally {
    withContext(NonCancellable) {
        api.reportProgress(done)   // suspend cleanup that must finish even on cancel
    }
}`},{t:"list",items:["**Immune to cancellation** — the block runs to completion even though the coroutine is cancelled.","**Only for must-finish suspending cleanup** — saving progress, releasing a remote lock, flushing.","**Keep it short** — it defeats cancellation, so long work here delays teardown.","**Non-suspending cleanup doesn't need it** — plain `finally` code runs fine without `NonCancellable`."]},{t:"note",text:"withContext(NonCancellable){} makes a block immune to the ongoing cancellation — needed for SUSPENDING cleanup in finally (otherwise it's cancelled at the first suspension point). Use only for short must-finish work (save progress, release lock, flush). Non-suspending finally code doesn't need it."}]},{level:"senior",q:"Why does runCatching interfere with cancellation, and what should you use instead?",a:[{t:"p",text:"`runCatching { }` catches *all* `Throwable`s — including `CancellationException`. So inside a coroutine it swallows cancellation, turning a cancelled coroutine into one that appears to have 'failed gracefully' and keeps going, breaking structured concurrency. Prefer explicit `try/catch` that rethrows `CancellationException`, or a `runCatching` variant that rethrows it."},{t:"code",title:"Cancellation-safe error handling",code:`// RISKY: swallows CancellationException
val result = runCatching { doWork() }

// SAFE: rethrow cancellation
suspend fun <T> coRunCatching(block: suspend () -> T): Result<T> = try {
    Result.success(block())
} catch (e: CancellationException) {
    throw e
} catch (e: Exception) {
    Result.failure(e)
}`},{t:"list",items:["**`runCatching` catches `Throwable`** — including `CancellationException`, breaking cancellation.","**Symptom** — coroutines that don't stop when cancelled; leaked/continuing work.","**Fix** — a coroutine-aware wrapper that rethrows `CancellationException`, or explicit `try/catch` with specific types.","**Same care with broad `catch (Exception)`/`catch (Throwable)`.**"]},{t:"note",text:"runCatching catches all Throwables including CancellationException, so it swallows cancellation — the coroutine keeps running, breaking structured concurrency. Use a coroutine-aware wrapper that rethrows CancellationException (catch it first, throw e; then catch Exception), or explicit try/catch with specific types."}]},{level:"junior",q:"A user leaves a screen but work continues — how does cancellation fix it, and when does it NOT trigger?",a:[{t:"p",text:"If the work runs in `viewModelScope`/`lifecycleScope`, leaving the screen (destroying the owner) cancels the scope, which cancels the work — provided the work is cooperative (has suspension points). It does *not* trigger on a mere configuration change if you used `viewModelScope`, because the ViewModel is retained across rotation (which is the desired behavior)."},{t:"list",items:["**Fixes it** — scope cancellation on destroy stops cooperative coroutines, aborting network/DB work and preventing dead-UI updates.","**Requires cooperation** — a non-suspending CPU loop won't stop; add `ensureActive()`/`yield()`.","**Not on rotation (viewModelScope)** — the ViewModel survives config change, so the load continues (correct — you don't want to refetch on every rotation).","**Wrong scope = wrong behavior** — `GlobalScope`/detached work ignores the screen's lifecycle and leaks; use lifecycle-bound scopes."]},{t:"note",text:"Work in viewModelScope/lifecycleScope is cancelled when the owner is destroyed (leaving the screen), aborting cooperative coroutines — no dead-UI updates. It won't fire on rotation with viewModelScope (VM retained — intended). Requires cooperation (suspension points/ensureActive), and only works if you used a lifecycle scope, not GlobalScope."}]}],PS={id:"cancellation",title:"Cancellation",tagline:"Cooperative cancellation, making CPU loops cancellable, CancellationException (never swallow it), NonCancellable cleanup, timeouts, and scope cancellation.",tags:["Coroutines","Cancellation","ensureActive","withTimeout","NonCancellable"],content:IS,qa:MS},DS=[{heading:"Two exception models: launch propagates, async defers",blocks:[{t:"p",text:"Coroutine exception handling starts with one distinction: **`launch` and `async` handle exceptions differently.** In `launch`, an uncaught exception is *thrown immediately* and propagates up the Job hierarchy. In `async`, the exception is *stored in the `Deferred`* and only re-thrown when you call `await()`. Getting this right is the foundation for everything else."},{t:"code",title:"The two behaviors",code:`// launch: exception propagates right away (up to parent / handler / crash)
scope.launch {
    throw IOException()   // propagates immediately
}

// async: exception is held, re-thrown at await()
val deferred = scope.async {
    throw IOException()   // stored, not thrown here
}
deferred.await()          // NOW it throws`},{t:"list",items:["**`launch` — 'pushed'**: handle with a try/catch inside the coroutine, or a `CoroutineExceptionHandler` at the scope. It behaves like an uncaught exception on a thread.","**`async` — 'pulled'**: handle by wrapping `await()` in try/catch. If you never await, a standalone async's exception can sit unnoticed.","**The structured-concurrency overlay**: when either is a *child* of a scope, an unhandled failure *also* propagates to the parent (cancelling siblings, per the Jobs topic) — this is separate from the 'when is it thrown' question and always applies."]}]},{heading:"try/catch — the simplest tool, with a catch",blocks:[{t:"p",text:"You can wrap suspend calls in ordinary `try/catch`, and it works — but placement matters and there's a critical rule about `CancellationException`."},{t:"code",title:"try/catch around suspend calls",code:`viewModelScope.launch {
    try {
        val data = repository.load()          // suspend call
        _state.value = UiState.Content(data)
    } catch (e: CancellationException) {
        throw e                               // NEVER swallow cancellation — rethrow
    } catch (e: IOException) {
        _state.value = UiState.Error("Network error")
    }
}`},{t:"list",items:["**Rethrow `CancellationException`**: a broad `catch (e: Exception)` grabs the cancellation signal too. If you don't rethrow it, cancellation breaks. Catch it first and rethrow, or catch specific exception types only.","**Placement**: try/catch only catches exceptions from code *inside* it that runs *synchronously in this coroutine*. It does **not** catch exceptions from a *child* coroutine you `launch` inside the try — that child's failure propagates through the Job hierarchy, not through your try/catch. This surprises people.","**For a single operation**, try/catch is clean. For structuring error handling across a whole scope, a `CoroutineExceptionHandler` may fit better."]}]},{heading:"Why try/catch doesn't catch child coroutine failures",blocks:[{t:"code",title:"The gotcha",code:`// WRONG: the try does NOT catch the child launch's exception
scope.launch {
    try {
        launch {                 // a CHILD coroutine
            throw IOException()  // propagates via Job hierarchy, NOT to the try below
        }
    } catch (e: Exception) {
        // never reached!
    }
}

// RIGHT: put the try/catch INSIDE the coroutine that does the risky work
scope.launch {
    launch {
        try { risky() } catch (e: Exception) { handle(e) }   // caught here
    }
}`},{t:"p",text:"The rule: **a child coroutine's exception propagates up through the Job tree, not out through the parent's `try/catch`.** So to catch a child's failure, the try/catch must be *inside the child* (around its own work), or you use a `CoroutineExceptionHandler` in the context (which handles uncaught exceptions that reach the root). This is a direct consequence of structured concurrency — failures flow through Jobs, not call stacks."}]},{heading:"CoroutineExceptionHandler — the last-resort net",blocks:[{t:"p",text:"A **`CoroutineExceptionHandler`** is a context element that catches **uncaught** exceptions that reach the root of a coroutine tree — a global safety net for logging or graceful handling, not a control-flow mechanism."},{t:"code",title:"Installing a handler",code:`val handler = CoroutineExceptionHandler { _, throwable ->
    Log.e("App", "Uncaught coroutine exception", throwable)
    crashReporter.report(throwable)
}

val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main + handler)

scope.launch { riskyWork() }   // uncaught exception -> goes to handler`},{t:"list",items:["**Only works for `launch`, not `async`**: `async` exceptions are meant to surface via `await()`, so the handler never sees them.","**Only catches at the *root* coroutine**: it must be installed in the scope (or the root `launch`), not a nested child. An exception from a child propagates up to the root, and only there does the handler fire. Installing it on an inner `launch` does nothing.","**It's a net, not a strategy**: by the time it fires, the coroutine has already failed and (with a regular Job) taken down its subtree. Use it for *logging/reporting* and last-resort graceful degradation, not for handling expected errors — handle those with try/catch where they occur.","**With `SupervisorJob`**: since child failures don't propagate to the parent, each child that should be caught needs its own handling; a handler on the supervisor scope catches failures of its *direct* `launch` children."]}]},{heading:"Errors as values — the recommended architecture",blocks:[{t:"p",text:"The most robust approach in app code is to *not rely on exceptions crossing coroutine boundaries* at all — instead, **model expected failures as values**. The data layer catches technical exceptions at the boundary and returns a typed result; the ViewModel handles that result as an expected branch."},{t:"code",title:"Result types instead of thrown exceptions",code:`// Repository: catch at the boundary, return a typed result
suspend fun getUser(id: String): Result<User> = withContext(ioDispatcher) {
    runCatching { api.getUser(id).toUser() }
        // runCatching catches CancellationException too — rethrow it:
        .onFailure { if (it is CancellationException) throw it }
}

// ViewModel: no try/catch needed; handle the result
viewModelScope.launch {
    repository.getUser(id)
        .onSuccess { _state.value = UiState.Content(it) }
        .onFailure { _state.value = UiState.Error(it.message) }
}`},{t:"list",items:["**Why this is better**: expected failures (network down, 404) are *expected outcomes*, not exceptional control flow. Modeling them as `Result`/sealed types makes handling explicit and exhaustive, and keeps exceptions for genuine bugs.","**`runCatching` caveat**: it catches *everything*, including `CancellationException` — so in a coroutine you must rethrow cancellation (as shown), or use a custom `runSuspendCatching` helper that does it for you.","**Map to domain errors**: translate `HttpException`/`IOException` into a sealed `DataError` at the data-layer boundary, so upper layers never see library-specific exceptions (ties into the Clean Architecture error-handling topic)."]},{t:"note",text:"The layered answer to 'how do you handle coroutine exceptions': (1) `launch` propagates immediately, `async` defers to `await()`; (2) try/catch for local handling, but rethrow `CancellationException` and know it won't catch *child* failures; (3) `CoroutineExceptionHandler` as a root-level net for logging (launch only); (4) architecturally, prefer *errors as values* (Result/sealed types) so failures are explicit and exceptions are reserved for bugs."}]}],RS=[{level:"junior",q:"How do you handle an exception from a coroutine started with launch?",a:[{t:"p",text:"**The concept**: in a `launch` coroutine, an uncaught exception propagates immediately (up to the parent scope, and ultimately to a handler or a crash). The simplest way to handle it is a `try/catch` *inside* the coroutine around the risky suspend calls."},{t:"code",title:"try/catch inside launch",code:`viewModelScope.launch {
    try {
        val data = repository.load()
        _state.value = UiState.Content(data)
    } catch (e: CancellationException) {
        throw e                       // always rethrow cancellation
    } catch (e: Exception) {
        _state.value = UiState.Error(e.message)
    }
}`},{t:"p",text:"The one rule you must not forget: rethrow `CancellationException`. Since it's a subclass of `Exception`, a broad catch grabs it, and swallowing it breaks cancellation. So catch it first and rethrow, or catch only the specific exceptions you expect. For a single operation like this, try/catch is clean and clear. For app architecture, many teams prefer returning errors as values (a `Result` type) from the repository so the ViewModel handles failure as an explicit branch rather than catching exceptions."}]},{level:"junior",q:"How is exception handling different between launch and async?",a:[{t:"p",text:"**The difference is *when* the exception surfaces.** In `launch`, an exception is thrown *immediately* when it occurs and propagates up right away. In `async`, the exception is *captured inside the returned `Deferred`* and only re-thrown when you call `await()` on it."},{t:"code",title:"launch throws now, async throws at await",code:`launch { throw IOException() }          // propagates immediately

val d = async { throw IOException() }   // captured, not thrown yet
d.await()                                // throws here`},{t:"list",items:["**`launch`**: handle with try/catch inside, or a `CoroutineExceptionHandler` at the scope root.","**`async`**: handle by wrapping `await()` in try/catch. If you never call `await()` on a standalone async, its exception can go unnoticed.","**Both, as children of a scope**: an unhandled failure also propagates to the parent scope (cancelling siblings, with a regular Job) — that's structured concurrency and is separate from the 'when is it thrown' question."]},{t:"p",text:"So the practical guidance: for fire-and-forget work use `launch` and catch inside; for concurrent results use `async` and catch around `await()`. A common mistake is expecting a `CoroutineExceptionHandler` to catch an `async` exception — it won't, because async exceptions are meant to be surfaced through `await()`."}]},{level:"junior",q:"What is a CoroutineExceptionHandler and when does it work?",a:[{t:"p",text:"**The concept**: a `CoroutineExceptionHandler` is a coroutine context element that catches *uncaught* exceptions reaching the root of a coroutine tree. You install it in a scope's context, and it acts as a global net — typically for logging or crash reporting."},{t:"code",title:"Installing one",code:`val handler = CoroutineExceptionHandler { _, e ->
    Log.e("App", "Uncaught", e)
}
val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main + handler)
scope.launch { risky() }   // uncaught exception -> handler`},{t:"list",items:["**Works only for `launch`**, not `async` (async exceptions surface via `await()`).","**Works only at the *root* coroutine** — it must be in the scope or root launch; installing it on a nested child does nothing, because the exception propagates up to the root before the handler fires.","**It's a net, not a control-flow tool**: by the time it runs, the coroutine has already failed. Use it for logging/reporting and last-resort handling, not for handling expected errors (use try/catch or Result types for those)."]}]},{level:"junior",q:"Why doesn't a try/catch around a launch block catch exceptions from coroutines launched inside it?",a:[{t:"p",text:"**Because a child coroutine's exception propagates through the *Job hierarchy*, not up the call stack through your try/catch.** When you `launch` a child coroutine, it runs independently as a node in the coroutine tree. If it throws, the exception travels up the *parent-child Job relationship* — it does not 'return' to the point where you called `launch`, so the surrounding try/catch never sees it."},{t:"code",title:"The mistake and the fix",code:`// WRONG — try never catches the child's exception
launch {
    try {
        launch { throw IOException() }   // child; propagates via Job tree
    } catch (e: Exception) { /* never runs */ }
}

// RIGHT — catch inside the child
launch {
    launch {
        try { risky() } catch (e: Exception) { handle(e) }
    }
}`},{t:"p",text:"The fix is to put the try/catch *inside the coroutine that does the risky work* (so it's on the same call stack), or use a `CoroutineExceptionHandler` for uncaught exceptions at the root. This is a direct consequence of structured concurrency: exceptions flow along Job parent-child edges, not along synchronous call stacks. Understanding this prevents the common bug of 'my try/catch isn't catching anything'."}]},{level:"senior",q:"How does exception propagation interact with SupervisorJob and CoroutineExceptionHandler?",a:[{t:"p",text:"**The interaction is subtle and a favorite senior question.** With a *regular* Job, a child's uncaught exception propagates up to the parent, cancels the parent and siblings, and continues to the root where a `CoroutineExceptionHandler` (if present) handles it. With a `SupervisorJob`, the exception does **not** propagate to the parent — so the behavior changes:"},{t:"list",items:["**Under a `SupervisorJob`, each direct child is treated as a 'root' for exception purposes.** A child's uncaught exception doesn't cancel siblings or the parent — but it still needs handling, and a `CoroutineExceptionHandler` installed on the *supervisor scope* WILL catch the uncaught exceptions of its direct `launch` children (because for a supervised child, the scope is effectively its root).","**Placement of the handler matters**: with a regular Job, only a handler at the *root* matters (child handlers are ignored, since exceptions propagate to the root first). With a SupervisorJob, a handler on the supervisor catches direct children's failures. So `SupervisorJob() + handler` in the scope is a common pattern for 'isolate failures AND log them'.","**Still only `launch`, never `async`**: regardless of Job type, `CoroutineExceptionHandler` never handles `async` exceptions — those must be caught at `await()`.","**Depth caveat**: a SupervisorJob only supervises *direct* children. A grandchild (launched by a child with a regular Job) that fails will cancel *its* parent (the child), because that inner level uses a normal Job — the top supervisor doesn't reach down."]},{t:"code",title:"Isolate + log pattern",code:`val handler = CoroutineExceptionHandler { _, e -> log(e) }
val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main + handler)

scope.launch { widgetA() }   // if it fails: isolated (siblings survive) AND logged
scope.launch { widgetB() }   // unaffected by A's failure`},{t:"p",text:"**The complete mental model**: `SupervisorJob` controls *whether* a failure propagates to siblings/parent (it doesn't); `CoroutineExceptionHandler` controls *what happens* to an exception once it becomes uncaught at a root. Together: supervisor isolates, handler logs. Knowing that the handler works on direct supervised children (but not on children under a regular Job, where only the root handler fires) is the depth signal."}]},{level:"senior",q:"Why is 'errors as values' often preferred over exceptions for coroutine error handling in app code?",a:[{t:"p",text:"**Because most failures in an app are *expected outcomes*, not exceptional bugs — and modeling them as values makes handling explicit, exhaustive, and decoupled from coroutine mechanics.** A network being down or a 404 isn't a programming error; it's a normal case the UI must handle. Throwing exceptions for these means every caller must remember to try/catch (easy to forget), the exception's type is untyped in the signature, and you're relying on exceptions crossing coroutine boundaries correctly (with all the launch/async/SupervisorJob subtleties)."},{t:"list",items:["**Explicit and type-safe**: a repository returning `Result<User>` or a sealed `Either<DataError, User>` puts the failure *in the type signature*. Callers can't ignore it, and a `when` over a sealed error is exhaustive (the compiler ensures every case is handled).","**Failures become normal control flow**: `repository.getUser().onSuccess { }.onFailure { }` handles the error as a branch, no try/catch, no reliance on exception propagation rules.","**Separation of concerns**: exceptions are reserved for genuine *bugs* (a null where there shouldn't be one) which *should* crash in debug, while expected failures (offline, not-found) are data. This makes crash reports meaningful — a crash is a real bug, not 'the network was down'.","**Boundary translation**: the data layer catches technical exceptions (`IOException`, `HttpException`) at its edge and maps them to a domain error vocabulary (`DataError.NoConnection`, `DataError.NotFound`), so upper layers never depend on library exception types."]},{t:"code",title:"The pattern (with the runCatching cancellation caveat)",code:`suspend fun getUser(id: String): Result<User> = withContext(io) {
    runCatching { api.getUser(id).toUser() }
        .onFailure { if (it is CancellationException) throw it }  // don't swallow cancellation
}`},{t:"p",text:"**The important caveat**: `runCatching` catches *everything*, including `CancellationException` — so you must rethrow cancellation (or use a `runSuspendCatching` helper), or you'll break structured cancellation while trying to model errors as values. With that handled, errors-as-values gives you the cleanest, most testable error handling — and it's why modern Android architecture guidance leans on Result/sealed types rather than exceptions crossing coroutine boundaries."}]},{level:"junior",q:"How do you handle errors in a plain suspend function?",a:[{t:"p",text:"With an ordinary `try/catch` at the call site — a suspend function throws exceptions like any function, and you catch them synchronously. This is the simplest, most common error handling in coroutine code, and it's a big improvement over callbacks where errors arrive in a separate branch."},{t:"code",title:"Synchronous-style handling",code:`suspend fun loadUser(id: String): UserResult = try {
    val user = api.getUser(id)       // suspend call may throw
    UserResult.Success(user)
} catch (e: IOException) {
    UserResult.NetworkError
} catch (e: HttpException) {
    UserResult.ServerError(e.code())
}`},{t:"list",items:["**Normal `try/catch`** — wraps the suspend call sequence; the failing call throws at that point.","**Catch specific types** — `IOException`, `HttpException`, etc., not broad `Exception` (which swallows `CancellationException`).","**Rethrow `CancellationException`** if you must catch broadly.","**Direct calls throw** — unlike `launch` (where exceptions propagate via the job), a direct suspend call throws to you."]},{t:"note",text:"Use ordinary try/catch — a suspend function throws like any function and you catch it synchronously at the call site. Catch specific types (IOException/HttpException), not broad Exception (which swallows CancellationException). This is the common, simplest case; launch/async differ in how exceptions surface."}]},{level:"junior",q:"Does Kotlin have checked exceptions, and what does that mean for error handling?",a:[{t:"p",text:"No — Kotlin has *no checked exceptions*. Nothing forces you to declare or catch an exception; the compiler won't warn you that `api.getUser()` can throw `IOException`. This means error handling is entirely your discipline: you must *know* which calls can fail and choose to handle them. It's flexible but easy to forget, which is one reason many teams prefer modeling errors as return values."},{t:"list",items:["**No `throws` clause enforcement** — the compiler doesn't require catching or declaring exceptions.","**Your responsibility** — you must know and handle the failure modes; nothing prompts you.","**Interop** — Java checked exceptions become unchecked in Kotlin; `@Throws` documents them for Java callers.","**Motivates errors-as-values** — `Result`/sealed types make failures explicit and compiler-checked (`when` exhaustiveness)."]},{t:"note",text:"Kotlin has NO checked exceptions — the compiler never forces you to handle or declare them, so error handling is pure discipline (you must know what can throw). Java checked exceptions become unchecked in Kotlin (@Throws documents them for Java). This is a key reason teams model errors as values (Result/sealed) for compiler-checked handling."}]},{level:"junior",q:"What happens to an uncaught exception in a launch coroutine by default?",a:[{t:"p",text:"If a `launch` coroutine throws an exception you don't handle (no inner try/catch, no `CoroutineExceptionHandler`), it propagates up the job tree and is ultimately delivered to the *default uncaught exception handler* — which, on Android, crashes the app (like any uncaught exception on a thread). It also cancels the parent scope (with a regular Job)."},{t:"list",items:["**Propagates up** — through the parent job; with a regular Job it cancels the parent and siblings.","**Reaches the default handler** — the platform's uncaught exception handler; on Android that crashes the app and logs it.","**Not silent** — you'll see a crash + stack trace (and Crashlytics reports it if installed).","**Handle it** — inner try/catch, a `CoroutineExceptionHandler` on the scope, or model errors as values."]},{t:"note",text:"An unhandled launch exception propagates up the job tree (cancelling the parent/siblings with a regular Job) and reaches the platform's default uncaught handler — on Android, it crashes the app (with a stack trace, reported to Crashlytics). Handle it with inner try/catch, a CoroutineExceptionHandler, or errors-as-values."}]},{level:"senior",q:"How do you handle exceptions from multiple concurrent async operations?",a:[{t:"p",text:"Each `async`'s exception surfaces at its `await()`. Wrap the concurrent block in a `coroutineScope` (or `supervisorScope`) and a `try/catch` around the awaits. With `coroutineScope`, the first failure cancels the siblings and rethrows to your catch. If you want partial results, use `supervisorScope` and handle each `await` individually (e.g. `runCatching` per deferred, rethrowing cancellation)."},{t:"code",title:"Fail-fast vs partial",code:`// Fail-fast: any failure aborts all
val combined = try {
    coroutineScope { Data(a().await(), b().await()) }   // 'a','b' are async
} catch (e: Exception) { fallback() }

// Partial: independent results
supervisorScope {
    val a = async { fetchA() }; val b = async { fetchB() }
    val ra = a.runCatchingAwait(); val rb = b.runCatchingAwait()   // handle each
}`},{t:"list",items:["**Exception at `await()`** — `async` holds the exception until awaited, then rethrows it.","**`coroutineScope` + try/catch** — first failure cancels siblings, rethrows; good for all-or-nothing.","**`supervisorScope` + per-await handling** — isolate failures for partial results.","**Don't leave a failing `async` un-awaited** — its exception may be lost."]},{t:"note",text:"Each async's exception surfaces at await(). Wrap in coroutineScope + try/catch for fail-fast (first failure cancels siblings and rethrows), or supervisorScope with per-await handling (runCatching, rethrow cancellation) for partial results. Never leave a failing async un-awaited — its exception can be lost."}]},{level:"senior",q:"What happens if you never await an async that fails?",a:[{t:"p",text:"In a `coroutineScope`/structured context, an `async` child that throws will still propagate its exception to the parent (cancelling the scope) even if you don't `await` it — because it's a child of the scope's Job. But if you use `async` in a *detached* or supervisor context and never await, the exception can be effectively swallowed (held in the `Deferred` no one inspects). Relying on this is fragile."},{t:"list",items:["**Structured `async`** — the child's failure propagates to the parent job regardless of `await` (cancels the scope).","**Un-awaited in supervisor/detached** — the exception stays in the `Deferred`; if never awaited, it's lost/uncaught.","**Best practice** — always `await` (or `awaitAll`) every `async` you start, so exceptions surface predictably.","**Don't use `async` for fire-and-forget** — use `launch` (its exceptions propagate) if you won't await."]},{t:"note",text:"In structured scope, an async child's failure propagates to the parent even un-awaited (cancels the scope). But in supervisor/detached contexts, an un-awaited async's exception sits in the Deferred and can be lost. Always await every async you start; use launch (not async) for fire-and-forget so exceptions propagate."}]},{level:"senior",q:"How do you convert exceptions to a Result or sealed error type at the repository boundary?",a:[{t:"p",text:"Catch the low-level exceptions in the repository and map them to a domain result type — either Kotlin's `Result<T>` or a custom sealed hierarchy (`Success`/`NetworkError`/`ServerError`). This keeps exception handling in one place, hides framework details (Retrofit's `HttpException`) from the UI, and makes the ViewModel handle errors as data via an exhaustive `when`."},{t:"code",title:"Mapping at the boundary",code:`sealed interface UserResult {
    data class Success(val user: User) : UserResult
    data object Offline : UserResult
    data class Server(val code: Int) : UserResult
}
suspend fun getUser(id: String): UserResult = try {
    UserResult.Success(api.getUser(id))
} catch (e: IOException) { UserResult.Offline }
  catch (e: HttpException) { UserResult.Server(e.code()) }
  // (let CancellationException propagate — don't catch it here)`},{t:"list",items:["**Map exceptions → domain results** — one place (the repository) translates failures.","**Hide framework types** — the UI never sees `HttpException`; it sees `UserResult`.","**Exhaustive handling** — a sealed result + `when` forces the UI to handle every case.","**Preserve cancellation** — don't catch `CancellationException` in the mapping."]},{t:"note",text:"In the repository, catch low-level exceptions (IOException/HttpException) and map to a domain sealed result (Success/Offline/Server) or Result<T> — one place, hides framework types, and forces exhaustive when-handling in the UI. Don't catch CancellationException in the mapping (let it propagate)."}]},{level:"senior",q:"How do exceptions differ from cancellation in propagation?",a:[{t:"p",text:"A regular exception is a *failure*: with a normal Job it cancels the parent and all siblings, and propagates up to the default handler if unhandled. `CancellationException` is *not* a failure: it's a controlled stop that a coroutine uses to unwind itself; it doesn't cancel the parent upward, and it's expected. This is why the two are handled so differently."},{t:"table",headers:["","Regular exception","CancellationException"],rows:[["Meaning","failure","controlled stop"],["Cancels parent (regular Job)","yes","no (doesn't propagate up)"],["Cancels siblings","yes","no"],["Should you catch it","handle it","rethrow — never swallow"],["Reaches default handler","yes (if unhandled)","no"]]},{t:"list",items:["**Exception = failure** — cancels the family (regular Job) and crashes if unhandled.","**`CancellationException` = normal** — unwinds the one coroutine; doesn't fail the parent.","**Handling** — handle real exceptions; rethrow cancellation."]},{t:"note",text:"A regular exception is a failure — it cancels parent+siblings (regular Job) and crashes if unhandled. CancellationException is a controlled stop — it unwinds only that coroutine, doesn't propagate up as failure, and must be rethrown (never swallowed). That asymmetry is why broad catch(Exception) is dangerous."}]},{level:"junior",q:"How do you idiomatically handle network errors (IOException, HttpException)?",a:[{t:"p",text:"Catch the two main categories from Retrofit/OkHttp: `IOException` (no connectivity, timeouts, DNS — a *connectivity* problem) and `HttpException` (a non-2xx HTTP response — a *server/response* problem, with a status code). Map them to distinct user-facing states so you can show 'You're offline' vs 'Something went wrong (500)' appropriately."},{t:"code",title:"Two error categories",code:`try {
    api.getData()
} catch (e: IOException) {
    // network/connectivity: offline, timeout, DNS
    UiState.Offline
} catch (e: HttpException) {
    when (e.code()) {
        401 -> UiState.Unauthorized
        in 500..599 -> UiState.ServerError
        else -> UiState.Error(e.code())
    }
}`},{t:"list",items:["**`IOException`** — connectivity failures (offline, timeout); suggest retry/offline UI.","**`HttpException`** — the request reached the server but got an error status; branch on `code()` (401 → re-auth, 5xx → retry, 4xx → show message).","**Timeouts** — `SocketTimeoutException` is an `IOException`; treat as retryable.","**Don't catch `Exception` broadly** — you'd swallow cancellation and mask programming bugs."]},{t:"note",text:"Catch IOException (connectivity: offline/timeout/DNS → offline UI, retry) and HttpException (non-2xx response → branch on code(): 401 re-auth, 5xx retry, 4xx message). SocketTimeoutException is an IOException (retryable). Avoid broad catch(Exception) — it swallows cancellation and hides bugs."}]},{level:"senior",q:"Does CoroutineExceptionHandler catch exceptions from async? Why or why not?",a:[{t:"p",text:"No. `CoroutineExceptionHandler` only handles *uncaught* exceptions from `launch`-style coroutines (that have nowhere else to go). `async` *exposes* its exception through the `Deferred` — it's your job to catch it at `await()`. So an `async` exception is considered 'handled by await', and the handler is never invoked for it."},{t:"list",items:["**Handler is for `launch`** — catches uncaught exceptions that propagate to the root.","**`async` defers to `await`** — the exception lives in the `Deferred`; you catch it around `await()`.","**Consequence** — installing a handler doesn't protect against `async` failures; wrap `await` in try/catch.","**Root-level only** — the handler must be on the scope/root coroutine, not an inner child."]},{t:"code",title:"async needs try/catch at await",code:`val d = scope.async { risky() }
try { d.await() } catch (e: Exception) { handle(e) }   // handler won't fire for this`},{t:"note",text:"No — CoroutineExceptionHandler only catches uncaught launch exceptions at the root; async exposes its exception via the Deferred, so you must try/catch around await(). Installing a handler doesn't protect against async failures. And the handler only works on the scope/root, not inner children."}]},{level:"senior",q:"How do you install a global last-resort handler for uncaught coroutine exceptions?",a:[{t:"p",text:"Put a `CoroutineExceptionHandler` in the context of your *root* application/feature scope, so any uncaught `launch` exception in that scope's tree is delivered to it — where you can log/report it (e.g. to Crashlytics) rather than crash silently or unhelpfully. It's a safety net, not a substitute for local handling."},{t:"code",title:"App-scope handler",code:`val handler = CoroutineExceptionHandler { _, e ->
    Firebase.crashlytics.recordException(e)
    Log.e("App", "Uncaught coroutine exception", e)
}
val appScope = CoroutineScope(SupervisorJob() + Dispatchers.Default + handler)
appScope.launch { backgroundWork() }   // uncaught -> handler`},{t:"list",items:["**On the root/app scope** — catches uncaught `launch` exceptions in that tree.","**Log/report** — send to Crashlytics/analytics; optionally show a generic error.","**Pair with `SupervisorJob`** — so one child's handled failure doesn't cancel the whole scope.","**Not for `async`** — those need `await` try/catch; and it doesn't replace local handling of expected errors."]},{t:"note",text:"Install a CoroutineExceptionHandler in the ROOT/app scope's context to catch uncaught launch exceptions (log/report to Crashlytics). Pair with SupervisorJob so a handled failure doesn't kill the scope. It's a last-resort net — not for async (use await try/catch) and not a replacement for local handling of expected errors."}]},{level:"senior",q:"How does supervisorScope change exception handling compared to coroutineScope?",a:[{t:"p",text:"In `coroutineScope`, a child's failure cancels its siblings and rethrows to the caller (all-or-nothing). In `supervisorScope`, child failures are *isolated* — one child failing doesn't cancel the others, and the failure isn't rethrown to the scope; you must handle each child's exception locally (try/catch or a handler). Choose based on whether the children are interdependent."},{t:"list",items:["**`coroutineScope`** — first child failure cancels siblings, rethrows to caller. Catch it around the scope.","**`supervisorScope`** — failures isolated; siblings continue; not rethrown to the scope. Handle per child.","**Per-child handling in supervisor** — try/catch inside each `launch`, or a `CoroutineExceptionHandler`.","**Use** — `coroutineScope` for a combined operation; `supervisorScope` for independent tasks (screen sections, workers)."]},{t:"note",text:"coroutineScope: a child failure cancels siblings and rethrows to the caller (catch around it). supervisorScope: failures isolated — siblings survive, not rethrown to the scope, so handle each child locally (try/catch or handler). Combined op → coroutineScope; independent tasks → supervisorScope."}]},{level:"senior",q:"How do you retry only on specific (transient) exceptions?",a:[{t:"p",text:"In your retry loop, catch and retry only the exceptions that are worth retrying (transient: timeouts, 5xx, connectivity), and immediately rethrow the rest (4xx client errors, programming bugs, cancellation). Retrying a 400 or a `NullPointerException` is pointless and hides bugs."},{t:"code",title:"Selective retry",code:`suspend fun <T> retryTransient(times: Int, block: suspend () -> T): T {
    repeat(times - 1) {
        try { return block() }
        catch (e: CancellationException) { throw e }        // never retry cancellation
        catch (e: IOException) { /* transient: retry */ }
        catch (e: HttpException) { if (e.code() < 500) throw e /* client error: don't retry */ }
        delay(1000)
    }
    return block()
}`},{t:"list",items:["**Retry transient** — `IOException` (network), 5xx server errors, timeouts.","**Don't retry** — 4xx client errors (bad request/auth), programming exceptions, and `CancellationException` (rethrow it).","**Backoff + jitter** — space retries; add randomness to avoid thundering herds.","**Cap attempts** — surface the error after N tries."]},{t:"note",text:"Retry only transient failures (IOException, 5xx, timeouts) and rethrow the rest (4xx client errors, bugs, and always CancellationException). Retrying a 400 or NPE is pointless and hides bugs. Add backoff + jitter and cap attempts. Selectivity is what makes retry safe and useful."}]},{level:"junior",q:"How do exceptions propagate through withContext?",a:[{t:"p",text:"An exception thrown inside a `withContext(dispatcher) { }` block propagates out to the caller normally — `withContext` runs in the current coroutine, so the exception is thrown at the `withContext` call site and you catch it with an ordinary `try/catch`. It behaves like a regular function call for error purposes."},{t:"code",title:"Catch around withContext",code:`try {
    val data = withContext(Dispatchers.IO) { api.load() }   // throws here on failure
    render(data)
} catch (e: IOException) {
    showError()
}`},{t:"list",items:["**Propagates to the caller** — the exception is rethrown at the `withContext` call site.","**Ordinary `try/catch`** — wrap the `withContext` call.","**Unlike `launch`** — `withContext` doesn't need a handler; it's sequential.","**Cancellation flows too** — a `CancellationException` propagates through as well."]},{t:"note",text:"An exception in withContext(dispatcher){} propagates to the caller and is thrown at the withContext call site — catch it with ordinary try/catch, like a normal function call. Unlike launch, withContext is sequential and needs no handler. CancellationException flows through too."}]},{level:"senior",q:"How do you catch exceptions without breaking cancellation?",a:[{t:"p",text:"Catch *specific* exception types, or if you catch broadly, rethrow `CancellationException` first. Because `CancellationException` is an `Exception`, a naive `catch (e: Exception)` swallows it and breaks cancellation. The safe pattern makes the coroutine's cancellation survive while still handling real errors."},{t:"code",title:"Cancellation-safe catch",code:`try {
    doWork()
} catch (e: CancellationException) {
    throw e                         // let cancellation propagate
} catch (e: Exception) {
    handle(e)                       // real errors
}
// Better: catch specific types so cancellation isn't caught at all
try { doWork() } catch (e: IOException) { handle(e) }`},{t:"list",items:["**Prefer specific types** — `IOException`, `HttpException`; cancellation isn't caught.","**If broad, rethrow cancellation first** — `catch (CancellationException) { throw e }` before the generic catch.","**Avoid `runCatching`/`catch (Throwable)` in coroutines** — they swallow cancellation.","**Consistency** — apply this everywhere you handle coroutine errors."]},{t:"note",text:"Catch specific types (IOException/HttpException) so cancellation isn't caught, or if catching broadly, rethrow CancellationException first (catch it, throw e; then catch Exception). Avoid runCatching/catch(Throwable) in coroutines — they swallow cancellation and break structured concurrency."}]},{level:"senior",q:"How do you distinguish expected errors from programming bugs in coroutine code?",a:[{t:"p",text:"*Expected errors* are normal outcomes of the domain (network offline, 404, validation failure) — model them as data (`Result`/sealed) and show appropriate UI. *Programming bugs* (`NullPointerException`, `IllegalStateException`, index out of bounds) are defects — don't 'handle' them as recoverable; let them crash (in debug) and report them (in production) so you fix the root cause. Conflating the two hides bugs behind generic error screens."},{t:"list",items:["**Expected → data** — catch specific exceptions and map to result states (offline, not-found).","**Bugs → surface/report** — don't swallow `NPE`/`IllegalState`; let them reach Crashlytics; fix the cause.","**Broad catch hides bugs** — `catch (Exception) { showError() }` turns a crash you'd fix into a silent generic error.","**Fail fast in debug** — crash on bugs during development to catch them early."]},{t:"note",text:"Expected errors (offline, 404, validation) → model as data (Result/sealed) with proper UI. Programming bugs (NPE, IllegalState) → don't 'handle' as recoverable; let them crash/report to Crashlytics so you fix the root cause. Broad catch(Exception){showError()} conflates them, hiding bugs behind generic error screens."}]},{level:"senior",q:"How do you test that a coroutine surfaces errors correctly?",a:[{t:"p",text:"Use a fake that throws the exception you want to simulate, run the code under `runTest`, and assert the observable outcome — the error state emitted, the exception rethrown, or the fallback taken. For exceptions expected to propagate, assert with `assertFailsWith`; for errors mapped to state, assert the resulting UiState."},{t:"code",title:"Testing error paths",code:`@Test fun mapsNetworkErrorToOffline() = runTest {
    val repo = FakeRepo(throws = IOException())
    val vm = UserViewModel(repo)
    vm.load(); advanceUntilIdle()
    assertEquals(UiState.Offline, vm.state.value)
}
@Test fun rethrows() = runTest {
    assertFailsWith<IllegalStateException> { subject.doWork() }
}`},{t:"list",items:["**Fake that throws** — inject a test double raising the target exception.","**Assert the outcome** — the mapped error state, or `assertFailsWith` for propagation.","**`runTest` + `advanceUntilIdle`** — drive the coroutine to completion deterministically.","**Cover cancellation** — assert `CancellationException` isn't swallowed (the operation actually stops)."]},{t:"note",text:"Inject a fake that throws the target exception, run under runTest + advanceUntilIdle, and assert the observable result: the mapped error UiState, or assertFailsWith for propagation. Also test that CancellationException isn't swallowed. Fakes throwing on demand are the cleanest way to exercise error paths."}]},{level:"senior",q:"How do you make sure coroutine exceptions reach your crash reporter?",a:[{t:"p",text:"Uncaught `launch` exceptions already reach the default handler (which Crashlytics hooks into), so they're reported and crash. For exceptions you *catch* but want to log (non-fatal), explicitly report them (`recordException`). And install a `CoroutineExceptionHandler` on app-level scopes to catch and report anything that would otherwise be lost."},{t:"list",items:["**Uncaught `launch`** — reaches the default uncaught handler → Crashlytics reports it as a crash.","**Caught-but-notable** — call `Firebase.crashlytics.recordException(e)` to log non-fatals you handle.","**App-scope handler** — a `CoroutineExceptionHandler` that reports, so no uncaught coroutine exception goes unlogged.","**`async` exceptions** — reach the reporter only if you rethrow/record them at `await` (they won't hit the default handler on their own)."]},{t:"note",text:"Uncaught launch exceptions hit the default handler → Crashlytics reports them (crash). For handled-but-notable errors, call recordException() (non-fatal). Add an app-scope CoroutineExceptionHandler that reports, so nothing is lost. async exceptions only reach the reporter if you rethrow/record them at await."}]},{level:"junior",q:"How do you provide a fallback value when an operation fails?",a:[{t:"p",text:"Catch the failure and return a default, use `withTimeoutOrNull`/`getOrDefault`, or fall back to a cached value. The goal is graceful degradation — a failed remote call falls back to cache or a sensible default rather than breaking the screen."},{t:"code",title:"Fallback strategies",code:`// Fallback to cache on network failure
suspend fun getConfig(): Config = try {
    api.getConfig().also { cache.save(it) }
} catch (e: IOException) {
    cache.load() ?: Config.DEFAULT       // cached, then default
}
// Timeout fallback
val data = withTimeoutOrNull(3000) { fetch() } ?: cachedData()`},{t:"list",items:["**Catch → default** — return a sensible default on expected failures.","**Cache fallback** — serve last-known-good data when the network fails (offline-first).","**`withTimeoutOrNull` / `getOrDefault`** — concise fallbacks for timeouts/`Result`.","**Don't mask everything** — fall back for expected errors; let bugs surface."]},{t:"note",text:"Provide fallbacks for expected failures: catch → default, serve cached last-known-good data (offline-first), or withTimeoutOrNull/getOrDefault for concise timeout/Result fallbacks. Degrade gracefully — but fall back only for expected errors; let programming bugs surface, don't mask them."}]},{level:"senior",q:"What happens if an exception is thrown during cleanup in a finally block?",a:[{t:"p",text:"If a `finally` block throws, that exception can *mask* the original exception (the one being handled during unwinding) — the finally's exception propagates and the original may be lost or attached as suppressed. So cleanup code should be robust: wrap risky cleanup in its own try/catch, and never let cleanup throw over a more important original error."},{t:"code",title:"Safe cleanup",code:`try {
    useResource()
} finally {
    try { resource.close() } catch (e: Exception) { log(e) }   // don't let close() mask the real error
}`},{t:"list",items:["**Finally exceptions mask** — a throw in `finally` can replace/hide the original exception.","**Guard cleanup** — wrap risky cleanup in its own try/catch (log, don't rethrow).","**`use { }`** — Kotlin's `use` handles this correctly for `Closeable`, adding cleanup failures as suppressed.","**Cancellation cleanup** — remember suspending cleanup needs `NonCancellable`."]},{t:"note",text:"A throw in finally can mask the original exception (it propagates instead, original lost/suppressed). Guard risky cleanup in its own try/catch (log, don't rethrow), or use Kotlin's use { } (adds cleanup failures as suppressed). And suspending cleanup during cancellation needs NonCancellable."}]},{level:"junior",q:"How do you surface errors to the UI as state?",a:[{t:"p",text:"Represent errors as part of your `UiState` — an `Error` variant of a sealed state, or an `error` field/one-off event — so the UI renders them declaratively (error screen, snackbar, retry button). The ViewModel catches exceptions, maps them to these states, and the UI reacts; the UI itself never touches exceptions."},{t:"code",title:"Errors as UI state",code:`fun load() = viewModelScope.launch {
    _state.value = UiState.Loading
    _state.value = try {
        UiState.Content(repo.load())
    } catch (e: CancellationException) { throw e }
      catch (e: Exception) { UiState.Error(e.toUserMessage()) }
}`},{t:"list",items:["**Model errors in state** — an `Error` variant with a message/retry, or a transient event for snackbars.","**ViewModel maps** — catch exceptions, translate to a user-facing message/state.","**Declarative UI** — the UI renders whatever state it's given; no exception handling in composables/views.","**Distinguish persistent vs transient** — full error screen vs snackbar depending on severity/context."]},{t:"note",text:"Model errors as UiState (an Error variant with message/retry, or a one-off event for snackbars); the ViewModel catches exceptions (rethrowing CancellationException) and maps them to those states, and the UI renders declaratively. The UI never handles exceptions directly. Use full-screen error vs snackbar by severity/context."}]},{level:"senior",q:"Why shouldn't repositories leak low-level exceptions to the UI layer?",a:[{t:"p",text:"If a repository lets `HttpException`, `SQLiteException`, or `JsonParseException` bubble up to the ViewModel/UI, the upper layers become coupled to framework details, must know about every library's exception types, and tend to catch broadly (swallowing cancellation, hiding bugs). Mapping to domain errors at the boundary decouples layers and centralizes translation."},{t:"list",items:["**Decoupling** — the UI shouldn't know Retrofit/Room exist; it works with domain results.","**Single translation point** — one place maps low-level exceptions to domain errors.","**Avoids broad catches upstream** — the ViewModel handles a small set of domain results, not a zoo of framework exceptions.","**Swappability** — changing the network/DB library doesn't ripple error handling through the UI."]},{t:"note",text:"Leaking HttpException/SQLiteException to the UI couples layers to framework details, forces the UI to know every library's exceptions, and encourages broad catches (swallowing cancellation/hiding bugs). Map to domain errors at the repository boundary — one translation point, decoupled layers, swappable libraries."}]},{level:"junior",q:"How does error handling differ for launch vs async in a ViewModel?",a:[{t:"p",text:"With `launch` (the common ViewModel case), handle errors *inside* the coroutine with try/catch and map to state — a try/catch around `launch` catches nothing. With `async`, the exception surfaces at `await()`, so wrap the `await` in try/catch. Most ViewModel operations use `launch` and handle errors by updating state."},{t:"code",title:"launch vs async error handling",code:`// launch: catch inside
fun load() = viewModelScope.launch {
    try { _state.value = Content(repo.load()) }
    catch (e: CancellationException) { throw e }
    catch (e: Exception) { _state.value = Error(e) }
}
// async: catch at await
fun combine() = viewModelScope.launch {
    try {
        coroutineScope { Data(a().await(), b().await()) }
    } catch (e: Exception) { _state.value = Error(e) }
}`},{t:"list",items:["**`launch`** — try/catch *inside* the block; map to state. (Around `launch` catches nothing.)","**`async`** — try/catch around `await()`, where the exception surfaces.","**Rethrow cancellation** — in both, don't swallow `CancellationException`.","**Most VM code is `launch`** — set Loading → Content/Error in one place."]},{t:"note",text:"launch: handle errors INSIDE the block (try/catch around launch catches nothing) and map to state. async: handle at await() where the exception surfaces. Rethrow CancellationException in both. Most ViewModel operations are launch — set Loading then Content/Error inside."}]},{level:"senior",q:"When multiple parallel operations fail, how are the exceptions aggregated?",a:[{t:"p",text:"In a structured scope, the *first* exception to reach the parent becomes the reported cause and cancels the siblings; later exceptions from other children are attached as *suppressed* exceptions on the first (available via `Throwable.suppressed`). So you get one primary cause plus the secondary failures for diagnostics, and cancellation-induced `CancellationException`s are ignored."},{t:"list",items:["**First failure wins** — it's the thrown/reported cause and triggers sibling cancellation.","**Others suppressed** — added to the first via `addSuppressed`, not lost.","**Inspect** — `exception.suppressed` lists the additional failures.","**If you need all results** — use `supervisorScope` and collect each `runCatching`/`await` result individually instead of aggregating exceptions."]},{t:"note",text:"In structured scope, the first exception to reach the parent is the reported cause (and cancels siblings); later failures attach as suppressed (Throwable.suppressed), not lost; cascade CancellationExceptions are ignored. To collect every result instead, use supervisorScope with per-child runCatching/await."}]},{level:"junior",q:"Why doesn't a try/catch around launch catch the coroutine's exception?",a:[{t:"p",text:"`launch` returns immediately after *scheduling* the coroutine — the body runs later, concurrently. By the time the body throws, the `try/catch` around the `launch` statement has already completed. The exception has no way to travel back to that finished catch block; instead it propagates through the coroutine's job. So you must catch *inside* the `launch` block."},{t:"code",title:"Inside, not around",code:`// WRONG — catches nothing
try { scope.launch { throw IOException() } } catch (e: Exception) { }
// RIGHT — catch in the body
scope.launch { try { risky() } catch (e: Exception) { handle(e) } }`},{t:"list",items:["**`launch` schedules and returns** — the body runs after the surrounding try/catch is done.","**Exception goes to the job tree** — not back to the call site.","**Catch inside** — put try/catch within the coroutine body, or use a `CoroutineExceptionHandler`.","**`async` is different** — its exception surfaces at `await()`, so try/catch around `await` works."]},{t:"note",text:"launch schedules the coroutine and returns immediately, so the surrounding try/catch has already finished by the time the body throws — the exception propagates through the job tree, not back to the call site. Catch INSIDE the launch body (or use a CoroutineExceptionHandler). async differs: catch around await()."}]},{level:"senior",q:"Why is 'errors as values' often preferred over exceptions in coroutine app code?",a:[{t:"p",text:"Modeling expected failures as return values (`Result<T>` or a sealed type) makes them *explicit and compiler-checked*: the caller can't forget to handle them (an exhaustive `when`), the type signature documents what can go wrong, and you avoid the pitfalls of broad exception catching (swallowing `CancellationException`, masking bugs). Exceptions remain appropriate for truly exceptional/programming errors."},{t:"list",items:["**Explicit** — the return type shows failure is possible; callers must handle it.","**Compiler-checked** — sealed result + `when` exhaustiveness forces handling of each case.","**Avoids catch pitfalls** — no broad `catch (Exception)` that swallows cancellation or hides bugs.","**Still use exceptions** — for programming errors and truly exceptional conditions; convert them to results at boundaries for expected domain errors."]},{t:"note",text:"Errors-as-values (Result/sealed) make expected failures explicit and compiler-checked (exhaustive when), self-documenting in the signature, and avoid broad-catch pitfalls (swallowed cancellation, hidden bugs). Reserve exceptions for programming errors/exceptional conditions; map them to result types at boundaries for expected domain errors."}]}],ES={id:"exceptions",title:"Exception Handling",tagline:"launch vs async exception behavior, try/catch placement, why child failures escape try/catch, CoroutineExceptionHandler, and errors-as-values.",tags:["Coroutines","Exceptions","CoroutineExceptionHandler","Result"],content:DS,qa:RS},US=[{heading:"Channels — communication between coroutines",blocks:[{t:"p",text:"A **`Channel`** is a coroutine primitive for passing values *between* coroutines — conceptually a coroutine-friendly `BlockingQueue`, but with *suspending* operations instead of blocking ones. One coroutine `send`s values, another `receive`s them. Unlike a Flow (a cold, per-collector stream), a Channel is **hot** and each value goes to exactly **one** receiver — it's a hand-off pipe, not a broadcast."},{t:"code",title:"A basic channel",code:`val channel = Channel<Int>()

launch {                       // producer
    for (x in 1..5) channel.send(x)   // suspends if channel is full
    channel.close()            // signal no more values
}

launch {                       // consumer
    for (x in channel) {       // receives until closed; suspends if empty
        println(x)
    }
}`},{t:"list",items:["**`send` and `receive` suspend**: `send` suspends if the channel is full (backpressure); `receive` suspends if it's empty (waits for a value). No blocking.","**One value → one receiver**: if multiple coroutines receive from a channel, each value goes to only one of them (fan-out). This is fundamentally different from a `SharedFlow`, which broadcasts each value to *all* collectors.","**`close()`** signals completion — receivers' `for` loops end. A closed channel throws on further `send`.","**Flow vs Channel**: use a **Flow** for a stream you observe (cold, declarative, operators); use a **Channel** for actual *communication/hand-off* between coroutines (hot, each value consumed once) — like a work queue or a pipe between a producer and a worker."]}]},{heading:"Channel capacity and buffering",blocks:[{t:"table",headers:["Capacity","Behavior"],rows:[["`RENDEZVOUS` (default, 0)","send suspends until a receiver is ready — direct hand-off"],["`BUFFERED` (default size)","send doesn't suspend until the buffer is full"],["`CONFLATED`","keeps only the latest value; send never suspends, old value dropped"],["`UNLIMITED`","unbounded buffer; send never suspends (watch memory)"],["a specific number","buffer of that size, with an onBufferOverflow policy"]]},{t:"list",items:["**Rendezvous (0)** is the default — sender and receiver must 'meet': `send` waits for a `receive`. Tightest synchronization, no buffering.","**Conflated** — like `StateFlow`'s conflation: only the latest value is kept, useful when the consumer only cares about the newest.","**Unlimited** — never suspends `send`, but an unbounded buffer can grow without limit if the consumer is slow (memory risk).","You can also set `onBufferOverflow` (SUSPEND / DROP_OLDEST / DROP_LATEST) — the same knobs as SharedFlow, because they share machinery."]}]},{heading:"produce and actor — structured channel patterns",blocks:[{t:"code",title:"produce — a channel-backed producer coroutine",code:`fun CoroutineScope.produceNumbers(): ReceiveChannel<Int> = produce {
    var n = 0
    while (true) { send(n++); delay(100) }
}   // channel is closed automatically when the coroutine ends/cancels

val numbers = produceNumbers()
launch { for (n in numbers) println(n) }`},{t:"list",items:["**`produce { }`** — a builder that creates a coroutine bound to a `ReceiveChannel`; the channel closes automatically when the coroutine completes or is cancelled (structured — no leaked channel). The idiomatic way to make a producer.","**`actor { }`** — the inverse: a coroutine with an incoming channel that processes messages one at a time. Because it processes messages sequentially, it's a way to *safely manage state without locks* — send it messages, it handles them in order (the actor model). (Now somewhat legacy in favor of other patterns, but conceptually important.)","**Channels are hot and stateful** — a `produce` channel starts producing as soon as it's created and a receiver pulls; unlike a cold Flow, it doesn't restart per consumer."]}]},{heading:"Shared mutable state — the problem and the tools",blocks:[{t:"p",text:"Coroutines run concurrently, so multiple coroutines touching the same mutable variable causes **race conditions** — the same data corruption threads have. `var counter = 0` incremented from 100 coroutines won't reach 100 without synchronization. Kotlin offers several tools, in rough order of preference:"},{t:"list",items:["**Avoid shared mutable state (best)**: prefer immutable data and confine state to one place — e.g. a `StateFlow` updated only via atomic `update { }`, or confining mutations to a single coroutine/dispatcher. No shared mutation, no races.","**`Mutex`** — a *suspending* lock (mutual exclusion). `mutex.withLock { ... }` ensures only one coroutine executes the critical section at a time, and it *suspends* rather than blocks while waiting (unlike a `synchronized` block, which blocks a thread).","**Atomics** — `AtomicInteger`, `atomic()` (kotlinx-atomicfu) for simple counters/flags: lock-free, fast, but only for single-variable operations.","**Single-threaded confinement** — run all access to the state on one dedicated dispatcher (`newSingleThreadContext` or `limitedParallelism(1)`), so operations are serialized by construction — no explicit locking needed."]},{t:"code",title:"Mutex — suspending mutual exclusion",code:`val mutex = Mutex()
var counter = 0

suspend fun increment() {
    mutex.withLock {          // suspends (not blocks) if another coroutine holds it
        counter++             // critical section — one coroutine at a time
    }
}`}]},{heading:"Mutex vs synchronized, and Semaphore",blocks:[{t:"list",items:["**`Mutex` vs `synchronized`/`ReentrantLock`**: a `synchronized` block *blocks the thread* while waiting for the lock — inside a coroutine that blocks a valuable dispatcher thread. `Mutex.withLock` *suspends* the coroutine while waiting, freeing the thread. In coroutine code, prefer `Mutex`. Also, `Mutex` is **not reentrant** (a coroutine can't re-acquire a lock it already holds — that deadlocks), unlike `ReentrantLock`.","**`Semaphore`** — like a Mutex but allows *N* concurrent holders instead of 1. `Semaphore(permits = 4).withPermit { }` limits concurrency — e.g. cap parallel network requests to 4. Suspending, coroutine-friendly.","**Never hold a lock across arbitrary suspension points carelessly** — if you suspend for a long operation while holding a `Mutex`, other coroutines wait the whole time; keep critical sections short."]}]},{heading:"select — awaiting the first of several",blocks:[{t:"p",text:"**`select { }`** lets a coroutine await *multiple* suspending sources simultaneously and proceed with whichever is ready *first* — like a coroutine `switch` over async events. Useful for racing operations, timeouts, or multiplexing channels."},{t:"code",title:"Racing two sources with select",code:`val result = select<String> {
    deferredA.onAwait { "A won: $it" }      // whichever completes first
    deferredB.onAwait { "B won: $it" }
    channel.onReceive { "channel: $it" }
}`},{t:"list",items:["`select` picks the *first* clause to become ready and runs it, ignoring the rest. Common uses: implement a timeout by racing work against a `delay`, pick the fastest of redundant requests, or merge several channels.","It's an advanced primitive — most app code uses higher-level tools (`withTimeout`, `combine`, `merge`), but `select` is the building block underneath and appears in deeper interview questions."]},{t:"note",text:"Summary of the synchronization toolbox: prefer *avoiding* shared mutable state (immutability + StateFlow.update or single-thread confinement); for real critical sections use `Mutex.withLock` (suspending, non-reentrant) over `synchronized` (blocking); use `Semaphore` to limit concurrency to N; use `select` to await the first of several sources. Channels are for coroutine-to-coroutine hand-off (hot, one-value-one-receiver), distinct from Flows (cold, observed streams)."}]}],qS=[{level:"junior",q:"What is a Channel and how is it different from a Flow?",a:[{t:"p",text:"**The concept**: a `Channel` passes values *between* coroutines — one coroutine `send`s, another `receive`s, with both operations suspending (send suspends if full, receive suspends if empty). It's like a coroutine-friendly queue with non-blocking operations. It's used for actual communication/hand-off between coroutines, like a work queue or a pipe."},{t:"list",items:["**Channel is hot; Flow is cold**: a Channel exists and holds/transfers values independent of consumers; a Flow is a recipe that runs per collector.","**Channel: one value → one receiver**: each value sent is received by exactly one receiver (fan-out). A `SharedFlow`, by contrast, broadcasts each value to *all* collectors. This is the key behavioral difference.","**Flow is for observing a stream** (declarative, operators, cold); **Channel is for communication between coroutines** (hot, each value consumed once)."]},{t:"p",text:"Rule of thumb: if you're modeling 'a stream of data I want to observe and transform', use a Flow. If you're modeling 'coroutine A hands work items to coroutine B', use a Channel. In app code you often use Channels indirectly — e.g. `callbackFlow` and one-shot event delivery are built on channels — while using Flows for the observable-stream cases."}]},{level:"junior",q:"What is a Mutex and how is it different from a synchronized block?",a:[{t:"p",text:"**The concept**: a `Mutex` (mutual exclusion) is a lock that ensures only one coroutine executes a critical section at a time — you wrap the section in `mutex.withLock { }`. It protects shared mutable state from concurrent access, just like a lock in thread-based code."},{t:"p",text:"**The key difference from `synchronized`**: a `synchronized` block (or `ReentrantLock`) *blocks the thread* while waiting to acquire the lock. Inside a coroutine, that blocks a valuable dispatcher thread — defeating the point of coroutines. A `Mutex.withLock` *suspends* the coroutine while waiting, freeing the thread to run other coroutines. So in coroutine code you should prefer `Mutex` over `synchronized`."},{t:"code",title:"Mutex protecting a counter",code:`val mutex = Mutex()
var count = 0
suspend fun inc() = mutex.withLock { count++ }`},{t:"p",text:"One important gotcha: `Mutex` is **not reentrant** — a coroutine that already holds the lock cannot acquire it again (doing so deadlocks), unlike `ReentrantLock`. And you should keep critical sections short and avoid long suspensions while holding the lock, since other coroutines wait the whole time. Better still, where possible, avoid shared mutable state entirely (immutable data + `StateFlow.update`) so you don't need a lock at all."}]},{level:"junior",q:"How do you safely update shared state from multiple coroutines?",a:[{t:"p",text:"**The concept**: coroutines run concurrently, so multiple coroutines mutating the same variable causes race conditions — data corruption, lost updates. You need a synchronization strategy. In rough order of preference:"},{t:"list",items:["**Avoid shared mutable state (best)**: use immutable data and a single owner. A `StateFlow` updated via `_state.update { it.copy(...) }` is atomic (compare-and-set), so concurrent updates don't lose writes — no explicit lock needed. This is the idiomatic Android answer.","**`Mutex.withLock { }`**: a suspending lock for a genuine critical section — one coroutine at a time, suspends (doesn't block) while waiting.","**Atomics** (`AtomicInteger`, atomicfu): lock-free and fast for a *single* variable (a counter, a flag), but only for single-variable operations.","**Single-threaded confinement**: run all access on one dedicated dispatcher (`limitedParallelism(1)`), serializing operations by construction — no explicit locking."]},{t:"p",text:"The key insight interviewers want: the *best* solution is usually to *not share mutable state* — confine it or make it immutable. `StateFlow.update { }` covers most Android cases (it's atomic), and you drop to `Mutex`/atomics/confinement only when you have genuinely shared mutable state that can't be restructured. Reaching straight for a lock when `StateFlow.update` would do is a mild red flag."}]},{level:"junior",q:"What is a Semaphore used for in coroutines?",a:[{t:"p",text:"**The concept**: a `Semaphore` is like a `Mutex` but allows up to *N* coroutines into the critical section at once, instead of just one. You create it with a number of permits and wrap work in `semaphore.withPermit { }`; a coroutine suspends until a permit is available. It's used to **limit concurrency** to a fixed number."},{t:"code",title:"Limiting parallel network requests to 4",code:`val semaphore = Semaphore(permits = 4)

suspend fun fetchAll(urls: List<String>) = coroutineScope {
    urls.map { url ->
        async {
            semaphore.withPermit {   // at most 4 run concurrently
                api.fetch(url)
            }
        }
    }.awaitAll()
}`},{t:"p",text:"The classic use is rate-limiting parallel work: you want to fetch 100 URLs but not open 100 simultaneous connections, so a `Semaphore(4)` caps it at 4 in flight. It's suspending (doesn't block threads while waiting for a permit), which makes it coroutine-friendly. A related modern alternative for the same goal is `Dispatchers.IO.limitedParallelism(4)`, which gives a view of the IO pool capped at 4 concurrent coroutines — often simpler when the limit is about dispatcher usage rather than a specific critical section."}]},{level:"senior",q:"When would you use a Channel vs a SharedFlow for passing events between coroutines?",a:[{t:"p",text:"**The decision hinges on delivery semantics: one-receiver hand-off vs broadcast, and guaranteed delivery vs best-effort.** A `Channel` delivers each value to *exactly one* receiver and `send` *suspends until the value is received* (with default capacity) — so values aren't lost even if the receiver is momentarily absent. A `SharedFlow` *broadcasts* each value to *all* current collectors and drops values emitted while no one is collecting (with replay 0)."},{t:"list",items:["**Use a `Channel` when**: each event should be handled *once* by a single consumer, and you can't afford to lose events across brief gaps in collection. This is exactly why `Channel(BUFFERED).receiveAsFlow()` is the common choice for one-shot UI events (navigation, snackbars) — `send` waits through the lifecycle gap during a config change, so the event isn't dropped, and it's consumed once.","**Use a `SharedFlow` when**: multiple independent collectors each need to see *every* event (a true broadcast) — e.g. several components reacting to the same app-wide signal. Configure `replay`/buffer as needed. But accept that events emitted with no active collector (replay 0) are lost.","**The fan-out trap**: if you use a `Channel` but accidentally have *two* collectors, each event goes to only *one* of them (whichever receives first) — a source of 'half my events disappear' bugs. If you need every listener to get every event, that's a SharedFlow, not a Channel.","**Guaranteed-once vs broadcast is the crux**: Channel = 'exactly one consumer, delivery waits'; SharedFlow = 'all consumers, best-effort'. For navigation/snackbar events (one handler, must not drop), Channel wins; for broadcasting state changes to many observers, SharedFlow (or StateFlow) wins."]},{t:"p",text:"**Neither fully solves process death** — a killed process loses buffered channel items and un-replayed SharedFlow emissions alike; truly durable one-shot outcomes should be modeled as persisted state. But for in-process event delivery, the Channel-vs-SharedFlow choice is precisely about one-receiver-guaranteed vs all-receivers-best-effort."}]},{level:"senior",q:"What is the select expression and when is it useful?",a:[{t:"p",text:"**The concept**: `select { }` lets a coroutine wait on *multiple* suspending sources at once and proceed with whichever becomes ready *first*, running that clause and abandoning the rest. It's like a `switch`/`when` over asynchronous events — the coroutine equivalent of `select()` in Go or a multiplexing wait."},{t:"code",title:"Racing sources",code:`val winner = select<String> {
    fastReplica.onAwait { "fast: $it" }     // whichever Deferred finishes first
    slowReplica.onAwait { "slow: $it" }
    timeoutChannel.onReceive { "timed out" }
}`},{t:"list",items:["**Uses**: racing redundant requests and taking the fastest (hedged requests); implementing a custom timeout by racing work against a `delay`; multiplexing several channels into one consumer (proceed with whichever has data); building higher-level combinators.","**Clauses**: `onAwait` (for `Deferred`), `onReceive`/`onReceiveCatching` (for channels), `onSend` (for sending when a channel is ready), `onTimeout` — you list the alternatives and `select` picks the first ready one.","**It's a low-level primitive**: most app code uses higher-level tools built on similar ideas — `withTimeout` (race against a deadline), `merge` (combine flows), `combine`. You reach for raw `select` when you need custom 'first of several' logic those don't express.","**Fairness and atomicity caveats**: `select` chooses among ready clauses and the non-selected clauses are not consumed; care is needed with channels so you don't accidentally drop values (use `onReceiveCatching` and understand which clause 'wins')."]},{t:"p",text:"**When to bring it up**: `select` appears in senior questions about implementing timeouts, hedged/racing requests, or channel multiplexing from scratch. Knowing it exists and what it does (await the first-ready of several sources) — even while noting you'd usually prefer `withTimeout`/`merge` — demonstrates depth beyond the everyday coroutine API."}]},{level:"senior",q:"Compare using produce/actor, Mutex, and single-threaded confinement for managing shared state.",a:[{t:"p",text:"**These are three different philosophies for safe concurrent state, and the 'best' depends on the shape of the problem.** They range from lock-based to message-based to confinement-based."},{t:"list",items:["**`Mutex` (lock-based)**: protect a critical section so one coroutine mutates at a time. Simple and direct for guarding a small piece of shared state. Downsides: you must remember to lock *every* access, it's non-reentrant (deadlock risk), and holding it across suspensions serializes coroutines. Good for a few well-defined critical sections; error-prone if state is accessed from many places.","**`actor` (message-based / actor model)**: a single coroutine owns the state and processes incoming messages from a channel *one at a time*. Other coroutines don't touch the state — they *send messages*. Because the owner processes sequentially, there's no concurrent access and thus no locks. Excellent for state with complex operations and many mutators (a cache, a connection manager); the tradeoff is more ceremony (define a message type) and it's somewhat legacy in Kotlin now.","**Single-threaded confinement**: run all access to the state on one dedicated dispatcher (`limitedParallelism(1)` or `newSingleThreadContext`). Operations are serialized by the dispatcher, so no locks and no message types — you just `withContext(stateDispatcher) { mutate() }`. Clean when the state is naturally 'owned' by one context; the cost is every access must hop to that dispatcher."]},{t:"list",items:["**How to choose**: for *simple* shared state, prefer *avoiding* the problem — immutable data + `StateFlow.update` (atomic) or a single atomic — over any of these. For a *critical section* guarding modest state accessed in a few places, `Mutex`. For state with *rich operations and many concurrent mutators*, the actor pattern (or confinement) reads better than scattering locks. For state that *conceptually belongs to one owner*, single-threaded confinement is the cleanest.","**The unifying principle**: the safest concurrent state is *no shared mutable state* — confine ownership to one place (one coroutine, one dispatcher, or one atomic holder) so concurrency can't corrupt it. Locks are the fallback when you genuinely have shared mutation you can't restructure away."]}]},{level:"junior",q:"What is a Channel, conceptually, and what are its main operations?",a:[{t:"p",text:"A `Channel` is a coroutine-safe *queue* for passing values between coroutines — one or more coroutines `send` values, one or more `receive` them. It's a communication primitive (like a BlockingQueue, but suspending instead of blocking): `send` suspends if the channel is full, `receive` suspends if it's empty. It's *hot* — values flow whether or not anyone is receiving (subject to capacity)."},{t:"code",title:"Sender/receiver",code:`val channel = Channel<Int>()
launch { for (x in 1..5) channel.send(x); channel.close() }   // producer
launch { for (x in channel) println(x) }                       // consumer (until closed)`},{t:"list",items:["**`send(value)`** — suspends if full; delivers a value.","**`receive()`** — suspends if empty; takes the next value.","**`close()`** — signals no more values; receivers' loop ends.","**Hot & one-to-one delivery** — each value is received by exactly one receiver (unlike a broadcast)."]},{t:"note",text:"A Channel is a coroutine-safe suspending queue for passing values between coroutines: send (suspends if full), receive (suspends if empty), close (ends the stream). It's hot and each value goes to exactly ONE receiver (point-to-point), unlike a Flow (cold, per-collector) or SharedFlow (broadcast)."}]},{level:"senior",q:"What are the Channel buffer/capacity types (RENDEZVOUS, BUFFERED, CONFLATED, UNLIMITED)?",a:[{t:"p",text:"A Channel's capacity controls how `send` behaves when there's no ready receiver. The main options trade off backpressure vs buffering: `RENDEZVOUS` (0) makes `send` wait for a receiver; `BUFFERED`/a fixed size buffers up to N then suspends; `CONFLATED` keeps only the latest (drops older); `UNLIMITED` never suspends `send` (unbounded buffer)."},{t:"table",headers:["Capacity","send behavior","Use"],rows:[["RENDEZVOUS (0)","waits for a receiver","tight sync / backpressure"],["BUFFERED / N","buffers up to N, then suspends","smoothing bursts"],["CONFLATED","never suspends; keeps latest only","latest-value UI/state"],["UNLIMITED","never suspends; unbounded","when drops are unacceptable (watch memory)"]]},{t:"list",items:["**RENDEZVOUS** — default; `send` suspends until `receive` — natural backpressure.","**BUFFERED (fixed)** — absorbs bursts up to capacity, then applies backpressure.","**CONFLATED** — only the newest value survives; good for progress/state where stale values don't matter.","**UNLIMITED** — never blocks the producer, but risks OOM if the consumer lags.","**onBufferOverflow** — `DROP_OLDEST`/`DROP_LATEST`/`SUSPEND` for finer control."]},{t:"note",text:"Channel capacity controls send when no receiver: RENDEZVOUS(0) waits for a receiver (backpressure), BUFFERED/N buffers then suspends, CONFLATED keeps only the latest (drops older), UNLIMITED never suspends (OOM risk). onBufferOverflow (DROP_OLDEST/DROP_LATEST/SUSPEND) tunes overflow. Pick by backpressure vs latest-value needs."}]},{level:"senior",q:"What is the produce builder, and how does it create a channel?",a:[{t:"p",text:"`produce { }` is a coroutine builder that returns a `ReceiveChannel` and runs a producer coroutine that `send`s into it. It ties the channel's lifecycle to the coroutine (closing it when the block ends and cancelling the producer if the consumer cancels) — a structured, leak-safe way to build a stream of values from a coroutine."},{t:"code",title:"produce a stream",code:`fun CoroutineScope.numbers(): ReceiveChannel<Int> = produce {
    for (i in 1..100) { send(i); delay(100) }
}   // channel auto-closed when the block finishes; producer cancelled if consumer cancels

// consume:
val nums = numbers()
for (n in nums) println(n)`},{t:"list",items:["**Returns a `ReceiveChannel`** — consumers `receive`/iterate it.","**Structured** — the producer is a child coroutine; closing/cancelling is handled with the channel's lifecycle.","**Backpressure** — `send` respects the channel capacity, suspending the producer as needed.","**Modern alternative** — for most streaming, `Flow` (cold, more operators) is preferred; `produce`/Channel suit true hot, hand-off pipelines."]},{t:"note",text:"produce { } is a builder returning a ReceiveChannel with a structured producer coroutine that sends into it — auto-closes when done and cancels if the consumer cancels (leak-safe). send respects capacity (backpressure). For most streaming prefer Flow (cold, richer operators); use produce/Channel for genuine hot hand-off pipelines."}]},{level:"senior",q:"What is the actor pattern, and how does it manage shared state?",a:[{t:"p",text:"An *actor* is a coroutine that owns some private state and processes messages from an incoming `Channel` one at a time. Because only the actor coroutine touches the state, and it handles messages serially, there are *no data races* — synchronization is achieved by message-passing and single-coroutine confinement rather than locks."},{t:"code",title:"A counter actor",code:`sealed class Msg
object Inc : Msg()
class Get(val response: CompletableDeferred<Int>) : Msg()

fun CoroutineScope.counterActor() = actor<Msg> {
    var count = 0                       // private state, only this coroutine touches it
    for (msg in channel) when (msg) {   // one message at a time -> no races
        is Inc -> count++
        is Get -> msg.response.complete(count)
    }
}`},{t:"list",items:["**State confined to one coroutine** — no concurrent access, so no locks needed.","**Serial message processing** — messages handled one at a time from the channel.","**Request/response** — use `CompletableDeferred` in a message to get a value back.","**Note** — `actor` is experimental/less common now; `Mutex` or single-thread confinement are more typical, but the actor model is a clean conceptual answer for state management."]},{t:"note",text:"An actor is a coroutine owning private state, processing messages serially from a Channel — no data races because only it touches the state and it handles one message at a time (synchronization via message-passing, not locks). Use CompletableDeferred for request/response. actor is experimental; Mutex/single-thread confinement are more common today."}]},{level:"senior",q:"How does a Mutex differ from synchronized, and why use withLock?",a:[{t:"p",text:"A coroutine `Mutex` provides mutual exclusion that *suspends* instead of *blocking* — when the lock is held, a waiting coroutine suspends (freeing its thread) rather than blocking a thread like `synchronized`. Crucially, `mutex.withLock { }` can safely contain suspend calls, whereas holding a `synchronized` lock across a suspension point is dangerous (you'd block a thread while suspended, and the lock is thread-bound)."},{t:"code",title:"Mutex.withLock",code:`val mutex = Mutex()
var balance = 0
suspend fun deposit(amount: Int) = mutex.withLock {
    balance += amount            // safe: only one coroutine in here at a time
    // can even call suspend functions inside withLock
}`},{t:"list",items:["**Suspends, doesn't block** — waiting coroutines don't tie up threads.","**`withLock { }`** — acquires and releases the lock even on exception (like `synchronized` but suspend-friendly).","**Not reentrant** — a coroutine holding the lock re-acquiring it deadlocks (unlike `synchronized`).","**vs `synchronized`** — the latter is thread-based, blocks, and must not span suspension points."]},{t:"note",text:"Mutex suspends waiters instead of blocking threads (like synchronized), and withLock { } can safely span suspend calls (synchronized can't — it's thread-bound and would block a suspended thread). Caveat: Mutex is NOT reentrant (re-locking deadlocks). Use withLock for guaranteed release on exceptions."}]},{level:"junior",q:"Why can incrementing a shared variable from multiple coroutines lose updates?",a:[{t:"p",text:"`count++` is not atomic — it's read, add, write. If two coroutines (on different threads) do it concurrently, they can both read the same old value, both add one, and both write back the same result, so one increment is lost (a data race). Coroutines don't magically make shared mutable state safe; concurrent access still needs protection."},{t:"code",title:"The race and fixes",code:`var count = 0
// RACE: concurrent count++ can lose updates
repeat(1000) { launch(Dispatchers.Default) { count++ } }

// Fixes:
val counter = AtomicInteger(0); counter.incrementAndGet()   // atomic
// or Mutex.withLock { count++ }                              // mutual exclusion
// or single-thread confinement (limitedParallelism(1))`},{t:"list",items:["**`++` is read-modify-write** — not atomic; interleavings lose updates.","**Fixes** — `AtomicInteger`/atomics, `Mutex.withLock`, or single-thread confinement.","**Immutable state** — better still, avoid shared mutable state (immutable state + StateFlow updates).","**Visibility too** — without synchronization, threads may not see each other's writes."]},{t:"note",text:"count++ is read-modify-write (not atomic), so concurrent coroutines on different threads can lose updates (data race). Fix with atomics (AtomicInteger), Mutex.withLock, or single-thread confinement (limitedParallelism(1)) — or avoid shared mutable state entirely (immutable state + StateFlow). Coroutines don't make shared state thread-safe."}]},{level:"senior",q:"When should you use atomics vs a Mutex for shared state?",a:[{t:"p",text:"Use *atomics* (`AtomicInteger`, `AtomicReference`, `update { }`) for simple single-variable operations (counters, flags, compare-and-set) — they're lock-free and fast. Use a `Mutex` when you must protect a *compound* operation (multiple fields changed together, an invariant across several variables) that can't be expressed as one atomic step."},{t:"list",items:["**Atomics** — single-variable, lock-free; `incrementAndGet`, `compareAndSet`, `updateAndGet`. Fast, no suspension.","**`Mutex`** — protects a *critical section* spanning multiple operations/fields or suspend calls.","**Single-thread confinement** — an alternative for complex state: route all access through one dispatcher.","**Prefer immutability** — often the cleanest: replace whole immutable state atomically (e.g. `MutableStateFlow.update`)."]},{t:"code",title:"Atomic vs Mutex",code:`val counter = AtomicInteger()          // simple single value
counter.incrementAndGet()

val mutex = Mutex()                     // compound invariant across fields
mutex.withLock { from -= n; to += n }   // must be atomic together`},{t:"note",text:"Atomics (AtomicInteger/compareAndSet/update) for simple single-variable ops — lock-free, fast, no suspension. Mutex for compound critical sections (multiple fields/invariants or suspend calls together). Single-thread confinement is another option for complex state. Often best: immutable state replaced atomically (StateFlow.update)."}]},{level:"junior",q:"How do you safely share and update state in a ViewModel across coroutines?",a:[{t:"p",text:"Use `MutableStateFlow` and its atomic `update { }` function, which applies a change to immutable state as a compare-and-set loop — safe under concurrent coroutines without explicit locks. Because the state is immutable and replaced atomically, there's no data race, and collectors always see a consistent snapshot."},{t:"code",title:"Atomic state update",code:`private val _state = MutableStateFlow(UiState())
val state = _state.asStateFlow()

fun addItem(item: Item) {
    _state.update { it.copy(items = it.items + item) }   // atomic, race-free
}`},{t:"list",items:["**`MutableStateFlow.update { }`** — atomic compare-and-set update of immutable state; safe from concurrent coroutines.","**Immutable state** — `copy()` produces a new snapshot; no in-place mutation.","**Avoid `value = value.copy(...)`** in concurrent contexts — a read-then-write can race; `update { }` retries.","**No manual locks** — the atomic update handles concurrency."]},{t:"note",text:"Use MutableStateFlow with update { it.copy(...) } — an atomic compare-and-set on immutable state, race-free across coroutines without locks. Prefer update{} over value = value.copy(...) (which can race on read-then-write). Immutable state + atomic replacement is the idiomatic safe-state pattern."}]},{level:"senior",q:"What is a Semaphore, and how does it differ from a Mutex?",a:[{t:"p",text:"A `Semaphore` allows up to *N* coroutines into a section at once (N permits), whereas a `Mutex` allows exactly *one* (it's essentially a semaphore with one permit). Use a `Semaphore` to *limit concurrency* — e.g. at most 4 simultaneous uploads — rather than for mutual exclusion of a single critical section."},{t:"code",title:"Bounding concurrency",code:`val semaphore = Semaphore(permits = 4)   // at most 4 at once
suspend fun upload(file: File) = semaphore.withPermit {
    api.upload(file)                     // 5th coroutine suspends until a permit frees
}`},{t:"list",items:["**`Semaphore(n)`** — up to `n` concurrent holders; `withPermit { }` acquires/releases.","**`Mutex`** — exactly one holder (mutual exclusion); `Semaphore(1)` is similar.","**Use** — rate-limiting, bounding parallel network/DB operations, protecting a pool of N resources.","**Suspends, doesn't block** — waiters suspend until a permit is available."]},{t:"note",text:"A Semaphore permits up to N concurrent coroutines (withPermit acquires/releases); a Mutex permits exactly one (mutual exclusion, ~Semaphore(1)). Use Semaphore to LIMIT concurrency (max 4 uploads, resource pools, rate limits) — it suspends waiters until a permit frees. Mutex protects a single critical section."}]},{level:"senior",q:"When would you use a Channel vs a Flow?",a:[{t:"p",text:"Use a `Flow` for *streams of data* that are cold (re-run per collector) and declarative (operators) — the default for data pipelines. Use a `Channel` for *hot, one-time-delivery communication* between coroutines where each value goes to exactly one consumer — like a work queue or hand-off. The key distinctions: cold vs hot, multi-collector vs single-delivery."},{t:"table",headers:["","Flow","Channel"],rows:[["Temperature","cold (per collector)","hot"],["Delivery","each collector gets all values","each value to ONE receiver"],["Operators","rich (map/filter/…)","minimal"],["Use","data streams, transforms","work queues, coroutine hand-off"]]},{t:"list",items:["**`Flow`** — data streams, transformations, cold reproducible pipelines (the common choice).","**`Channel`** — distribute work among consumers, fan-out/fan-in, point-to-point hand-off.","**Events** — a `Channel` (or `SharedFlow`) for one-off events (each delivered once).","**Bridge** — `channel.receiveAsFlow()` / `channel.consumeAsFlow()` to expose a channel as a Flow."]},{t:"note",text:"Flow = cold, per-collector, operator-rich — the default for data streams/transforms. Channel = hot, each value to exactly one receiver — for work queues, fan-out/fan-in, coroutine hand-off, and one-off events. Bridge a channel to a Flow with receiveAsFlow()/consumeAsFlow()."}]},{level:"senior",q:"How do you implement fan-out (multiple consumers) and fan-in (multiple producers) with channels?",a:[{t:"p",text:"*Fan-out*: multiple consumer coroutines `receive` from the *same* channel — each value goes to exactly one of them, distributing work (a worker pool). *Fan-in*: multiple producer coroutines `send` to the same channel, merging their outputs into one stream. Channels support both because they're multi-producer/multi-consumer safe."},{t:"code",title:"Fan-out worker pool",code:`val tasks = Channel<Task>()
repeat(4) { worker ->                         // fan-out: 4 consumers
    launch { for (task in tasks) process(task) }   // each task handled by one worker
}
launch { jobs.forEach { tasks.send(it) }; tasks.close() }   // producer
// Fan-in: several producers all send into one channel`},{t:"list",items:["**Fan-out** — N consumers on one channel share the load; each item processed once.","**Fan-in** — N producers into one channel merge streams.","**Backpressure** — the channel capacity paces producers to consumer speed.","**Closing** — close when producers are done; consumers' loops end."]},{t:"note",text:"Fan-out: multiple consumers receive from one channel (worker pool — each value to one worker, distributing load). Fan-in: multiple producers send to one channel (merged stream). Channels are multi-producer/multi-consumer safe; capacity provides backpressure; close when producers finish so consumer loops end."}]},{level:"senior",q:"What is the select expression, and when is it useful?",a:[{t:"p",text:"`select { }` lets a coroutine *wait on multiple suspending operations simultaneously* and proceed with whichever is ready first — receiving from whichever of several channels has a value, awaiting whichever `Deferred` completes first, or timing out. It's the coroutine analog of `select`/`epoll`: multiplexing several possible events into one."},{t:"code",title:"select over channels",code:`select<Unit> {
    channelA.onReceive { value -> handleA(value) }
    channelB.onReceive { value -> handleB(value) }
    onTimeout(1000) { handleTimeout() }
}`},{t:"list",items:["**Multiplexes** — wait on several `onReceive`/`onAwait`/`onSend`/`onTimeout` clauses; the first ready wins.","**First-wins racing** — combine with `async` to take the fastest result (cache vs network).","**`onTimeout`** — add a timeout branch.","**Advanced** — powerful but relatively rare; often a well-structured Flow (`merge`, `combine`) is clearer."]},{t:"note",text:"select { } waits on multiple suspending operations at once (onReceive/onAwait/onSend/onTimeout) and proceeds with the first ready — coroutine multiplexing. Uses: racing sources (fastest wins), merging channels, adding a timeout branch. Powerful but rare; Flow's merge/combine is often clearer for streams."}]},{level:"senior",q:"How do you convert a Channel to a Flow, and why would you?",a:[{t:"p",text:"Use `channel.receiveAsFlow()` (or `consumeAsFlow()`) to expose a hot channel as a `Flow`, so consumers get Flow operators and lifecycle-aware collection while you keep channel semantics for delivery. This is common for one-off events: a ViewModel holds a `Channel`, exposes `receiveAsFlow()`, and the UI collects it."},{t:"code",title:"Channel as event Flow",code:`private val _events = Channel<UiEvent>()
val events = _events.receiveAsFlow()          // expose as Flow
fun onDone() { viewModelScope.launch { _events.send(UiEvent.NavigateBack) } }
// UI collects events like any Flow`},{t:"list",items:["**`receiveAsFlow()`** — each event delivered to a single collector (fan-out-safe across time, not broadcast).","**`consumeAsFlow()`** — consumes the channel; can only be collected once.","**Why** — get Flow operators and `collectAsStateWithLifecycle`/`repeatOnLifecycle` while keeping exactly-once delivery.","**Events use case** — the standard pattern for navigation/snackbar events (vs a state flag that re-fires)."]},{t:"note",text:"channel.receiveAsFlow() (or consumeAsFlow(), collect-once) exposes a hot channel as a Flow — keeping exactly-once delivery while gaining Flow operators and lifecycle-aware collection. Standard for one-off events (navigation/snackbar): ViewModel holds a Channel, exposes receiveAsFlow(), UI collects it."}]},{level:"junior",q:"What happens if you send to a closed channel or receive from an empty closed channel?",a:[{t:"p",text:"Sending to a *closed* channel throws a `ClosedSendChannelException`. Receiving from a channel that's closed *and empty* throws a `ClosedReceiveChannelException` (or `receiveCatching`/the `for` loop ends gracefully). Closing signals 'no more values'; the iteration idiom (`for (x in channel)`) handles the end cleanly."},{t:"list",items:["**`send` after close** — throws `ClosedSendChannelException`.","**`receive` when closed & empty** — throws `ClosedReceiveChannelException`; use `receiveCatching()` to get a result instead.","**`for (x in channel)`** — the loop ends normally when the channel is closed and drained.","**Close with a cause** — `close(exception)` propagates that exception to receivers."]},{t:"code",title:"Graceful consumption",code:`for (x in channel) process(x)     // ends when closed & drained
val result = channel.receiveCatching()   // returns a ChannelResult (no throw)`},{t:"note",text:"send to a closed channel throws ClosedSendChannelException; receive from a closed & empty channel throws ClosedReceiveChannelException (or use receiveCatching() for a non-throwing ChannelResult). The for (x in channel) loop ends gracefully when closed & drained. close(cause) propagates that exception to receivers."}]},{level:"senior",q:"What is single-thread confinement, and how does it compare to locking?",a:[{t:"p",text:"Single-thread confinement means routing all access to a piece of mutable state through *one* thread (a single-threaded dispatcher or `limitedParallelism(1)`), so only one coroutine touches it at a time — no data races, no locks. It's an alternative to `Mutex`: instead of locking around each access, you *move* the access to a dedicated thread."},{t:"code",title:"Confinement",code:`private val confine = Dispatchers.Default.limitedParallelism(1)
private val cache = mutableMapOf<String, Data>()
suspend fun put(k: String, v: Data) = withContext(confine) { cache[k] = v }
suspend fun get(k: String): Data? = withContext(confine) { cache[k] }`},{t:"list",items:["**One thread owns the state** — serial access, no races, no locks.","**vs `Mutex`** — confinement moves work to one place; a `Mutex` locks in place on any thread. Both correct; pick per situation.","**Coarse** — all access serializes; simple but can bottleneck if access is hot.","**`limitedParallelism(1)`** — cheap confinement without a dedicated thread."]},{t:"note",text:"Single-thread confinement routes all state access through one thread (Dispatchers.X.limitedParallelism(1)) so only one coroutine touches it — no races, no locks. vs Mutex: confinement moves access to one place, Mutex locks in place on any thread. Confinement is simple but serializes all access (can bottleneck if hot)."}]},{level:"senior",q:"How do you compare produce/actor, Mutex, and confinement for managing shared state?",a:[{t:"p",text:"These are three strategies for the same goal — safe concurrent state — with different trade-offs. `Mutex` locks around critical sections (familiar, fine-grained). Single-thread confinement serializes all access via one dispatcher (simple, no explicit locks). The actor/`produce` model owns state in a coroutine processing messages (message-passing, no shared state at all). Modern app code most often uses immutable state + `StateFlow.update` or a `Mutex`."},{t:"table",headers:["Approach","Mechanism","Best for"],rows:[["Mutex.withLock","lock critical section","compound invariants, occasional access"],["Confinement","one dispatcher owns access","complex state, simple model"],["Actor/produce","coroutine + message channel","encapsulated state machines"],["Immutable + StateFlow","atomic replace","UI state (most common)"]]},{t:"list",items:["**Mutex** — fine-grained locking; watch for non-reentrancy and holding across suspension.","**Confinement** — no locks, but serializes all access.","**Actor** — clean encapsulation via messages; more ceremony, `actor` is experimental.","**Immutable + `StateFlow.update`** — usually the simplest and safest for app state."]},{t:"note",text:"Four strategies: Mutex.withLock (lock critical sections — compound invariants), single-thread confinement (one dispatcher owns access — simple model), actor/produce (message-passing, no shared state — encapsulated machines), and immutable state + StateFlow.update (atomic replace — most common for UI state). Prefer immutable+StateFlow or Mutex in typical app code."}]},{level:"junior",q:"Why are coroutines not automatically thread-safe for shared state?",a:[{t:"p",text:"Coroutines can run on multiple threads (via multi-threaded dispatchers and thread-hopping across suspension points), so two coroutines accessing the same mutable object can genuinely run in parallel — the same race conditions as threads apply. The 'sequential' feel of a single coroutine doesn't extend to shared state touched by *multiple* coroutines."},{t:"list",items:["**Multi-threaded execution** — `Dispatchers.Default`/`IO` run coroutines on many threads, so concurrent access is real.","**Thread-hopping** — a coroutine can resume on a different thread, so even 'one' coroutine isn't thread-pinned.","**Same hazards** — lost updates, visibility issues, torn reads — like classic threading.","**Protect shared state** — atomics, `Mutex`, confinement, or (best) immutable state + atomic replacement."]},{t:"note",text:"Coroutines run on multiple threads (multi-threaded dispatchers, thread-hopping across suspension points), so multiple coroutines touching shared mutable state race exactly like threads (lost updates, visibility). A single coroutine's sequential feel doesn't cover shared state. Protect it: atomics/Mutex/confinement, or immutable state + atomic replace."}]},{level:"senior",q:"What are the risks of an UNLIMITED or large channel buffer?",a:[{t:"p",text:"An `UNLIMITED` (or very large) buffer means `send` never suspends — which removes *backpressure*. If the producer outpaces the consumer, the buffer grows without bound, consuming memory and eventually causing `OutOfMemoryError`. Backpressure (a bounded buffer or RENDEZVOUS) is usually desirable: it paces the producer to the consumer's speed."},{t:"list",items:["**No backpressure** — the producer runs unthrottled; a slow consumer can't slow it down.","**Unbounded memory** — the buffer grows with the backlog, risking OOM.","**Latency** — items may sit in the buffer long before being consumed (stale data).","**Prefer bounded** — a fixed buffer with `onBufferOverflow` (SUSPEND/DROP), or CONFLATED for latest-only, gives predictable behavior."]},{t:"note",text:"UNLIMITED/large buffers make send never suspend — removing backpressure. If the producer outpaces the consumer, the buffer grows unbounded (OOM) and items get stale. Prefer a bounded buffer with onBufferOverflow (SUSPEND applies backpressure, DROP_OLDEST/LATEST caps memory) or CONFLATED for latest-only."}]},{level:"senior",q:"How do you build a producer-consumer pipeline with channels?",a:[{t:"p",text:"Chain stages where each stage is a coroutine that consumes from an input channel and produces to an output channel — like Unix pipes. `produce { }` builds each stage returning a `ReceiveChannel`, and the next stage iterates it. This gives concurrent, backpressured processing where each stage runs in parallel with the others."},{t:"code",title:"A two-stage pipeline",code:`fun CoroutineScope.produceNumbers() = produce { for (i in 1..100) send(i) }
fun CoroutineScope.square(nums: ReceiveChannel<Int>) = produce {
    for (n in nums) send(n * n)
}
val squares = square(produceNumbers())
for (s in squares) println(s)   // stages run concurrently, paced by backpressure`},{t:"list",items:["**Stages as `produce` coroutines** — each consumes one channel, produces another.","**Concurrent** — stages run in parallel; backpressure paces them.","**Composable** — add stages (filter, transform) by chaining.","**Flow alternative** — for most pipelines, `Flow` operators (`map`/`filter`) are cleaner; channels suit true hot, multi-consumer stages."]},{t:"note",text:"Pipeline = chained produce { } stages, each consuming an input ReceiveChannel and producing an output one (like Unix pipes) — stages run concurrently with backpressure pacing them. Composable by adding stages. For most data pipelines Flow operators are cleaner; use channel pipelines for hot, multi-consumer processing."}]},{level:"junior",q:"What is CompletableDeferred, and when is it useful?",a:[{t:"p",text:"`CompletableDeferred<T>` is a `Deferred` you complete *manually* by calling `complete(value)` (or `completeExceptionally(e)`), rather than via an `async` block. It's useful for bridging callback/event APIs into a suspendable result, or for request/response patterns (as in the actor example) where one coroutine awaits a value another will provide."},{t:"code",title:"Manual completion",code:`val deferred = CompletableDeferred<User>()
someCallback.onResult { user -> deferred.complete(user) }   // completed externally
val user = deferred.await()                                  // suspends until completed`},{t:"list",items:["**Manually completed** — `complete(value)`/`completeExceptionally(e)` from anywhere.","**`await()`** — suspends until completed, like any `Deferred`.","**Uses** — request/response over a channel (actor), bridging one-shot callbacks, coordinating between coroutines.","**vs `suspendCancellableCoroutine`** — the latter is often better for pure callback bridging (supports cancellation cleanup)."]},{t:"note",text:"CompletableDeferred<T> is a Deferred you complete manually (complete(value)/completeExceptionally(e)) rather than via async; await() suspends until then. Uses: request/response over a channel (actor), bridging one-shot callbacks, coordinating coroutines. For pure callback bridging, suspendCancellableCoroutine is often better (cancellation support)."}]},{level:"senior",q:"How do you avoid deadlocks with Mutex in coroutines?",a:[{t:"p",text:"The main coroutine-`Mutex` deadlock risk is *non-reentrancy*: a coroutine that holds the lock and then calls code that tries to acquire the *same* lock will deadlock (unlike `synchronized`, which is reentrant). Avoid it by not re-entering `withLock` while already holding it, keeping locked sections small, and never holding a lock across a call that might re-acquire it."},{t:"list",items:["**Non-reentrant** — don't call a function that locks the same `Mutex` from within `withLock`.","**Keep sections small** — minimize what runs under the lock; avoid arbitrary suspend calls that could loop back.","**Consistent lock ordering** — if using multiple locks, always acquire them in the same order to avoid classic deadlocks.","**Prefer alternatives** — atomics/confinement/immutable state avoid locking pitfalls entirely for many cases."]},{t:"note",text:"Coroutine Mutex is NOT reentrant — re-acquiring the same lock you already hold deadlocks (unlike synchronized). Avoid by not re-entering withLock while holding it, keeping locked sections small, and using consistent lock ordering with multiple locks. Often better: atomics/confinement/immutable state to sidestep locking."}]},{level:"junior",q:"What's the difference between a rendezvous channel and a buffered one for backpressure?",a:[{t:"p",text:"A *rendezvous* channel (capacity 0, the default) makes `send` suspend until a receiver is ready — producer and consumer meet in lockstep, giving the strongest backpressure. A *buffered* channel lets the producer send up to N values ahead before suspending, smoothing bursts but weakening backpressure. The choice is about how much the producer may run ahead of the consumer."},{t:"list",items:["**Rendezvous (0)** — `send` waits for `receive`; no buffering; tightest coupling and backpressure.","**Buffered (N)** — producer runs up to N ahead, then suspends; absorbs bursts.","**Trade-off** — buffering improves throughput for bursty producers but uses memory and can hide a slow consumer.","**Choose** — rendezvous for strict pacing; a small buffer for smoothing; avoid huge/unlimited (loses backpressure)."]},{t:"note",text:"Rendezvous (capacity 0, default): send waits for a receiver — lockstep, strongest backpressure. Buffered (N): producer runs up to N ahead then suspends — smooths bursts but weaker backpressure and uses memory. Pick rendezvous for strict pacing, a small buffer for smoothing; avoid unlimited (no backpressure)."}]},{level:"senior",q:"How do you use volatile and @Volatile in Kotlin, and when is it insufficient?",a:[{t:"p",text:"`@Volatile` guarantees *visibility* — a write to the field is immediately visible to other threads (no stale cached value) — but it does *not* provide atomicity for compound operations. So `@Volatile var count` makes reads/writes visible, but `count++` is still a race (read-modify-write isn't atomic). Use it for a simple flag read/written by different threads; use atomics/locks for anything compound."},{t:"code",title:"Volatile visibility vs atomicity",code:`@Volatile private var running = true    // OK: simple flag, visibility guaranteed
fun stop() { running = false }           // other threads see it promptly

@Volatile private var count = 0
count++   // STILL a race — not atomic; use AtomicInteger or Mutex`},{t:"list",items:["**Guarantees visibility** — writes seen promptly across threads; no stale reads.","**Not atomic** — compound ops (`++`, check-then-act) still race.","**Good for** — a `stop`/`isReady` flag toggled by one thread and read by others.","**Insufficient for** — counters, invariants across fields — use atomics/`Mutex`/confinement."]},{t:"note",text:"@Volatile guarantees visibility (writes seen promptly, no stale cached reads) but NOT atomicity — count++ is still a race. Use it for a simple flag (running/isReady) toggled by one thread and read by others. For counters/compound ops use AtomicInteger/Mutex/confinement."}]},{level:"senior",q:"How do you broadcast one event to multiple collectors — Channel or SharedFlow?",a:[{t:"p",text:"A `Channel` delivers each value to *one* receiver, so it can't broadcast to multiple collectors. For broadcasting the same event/value to *many* collectors, use a `SharedFlow` (hot, multicast). `SharedFlow(replay, extraBufferCapacity, onBufferOverflow)` lets every active collector receive each emission; `StateFlow` is the special case that always holds/broadcasts the latest value."},{t:"code",title:"SharedFlow broadcast",code:`private val _events = MutableSharedFlow<Event>(extraBufferCapacity = 1)
val events = _events.asSharedFlow()          // many collectors each get every event
fun emit(e: Event) { _events.tryEmit(e) }
// StateFlow for latest-value broadcast (e.g. UI state)`},{t:"list",items:["**`Channel`** — point-to-point; one receiver per value. Not for broadcast.","**`SharedFlow`** — multicast; every active collector gets each emission; configurable replay/buffer.","**`StateFlow`** — always broadcasts the current value to all collectors (conflated, replay=1).","**Events vs state** — one-off events (each once) fit a `Channel` (single consumer) or `SharedFlow` (multiple); persistent latest state fits `StateFlow`."]},{t:"note",text:"A Channel delivers each value to ONE receiver — can't broadcast. For one-to-many, use SharedFlow (hot multicast; every active collector gets each emission, configurable replay/buffer) or StateFlow (always broadcasts the latest value). Channel for single-consumer hand-off/events; SharedFlow for multi-collector broadcast."}]}],OS={id:"channels-sync",title:"Channels & Synchronization",tagline:"Channels vs Flows, capacity/buffering, produce/actor, shared mutable state, Mutex vs synchronized, Semaphore, and the select expression.",tags:["Coroutines","Channel","Mutex","Semaphore","select"],content:US,qa:qS},FS={id:"coroutines",name:"Coroutines",description:"Kotlin's concurrency framework — suspension internals, builders & scopes, structured concurrency, dispatchers, cancellation, exceptions, and channels.",topics:[yS,bS,SS,TS,PS,ES,OS]},LS=[{heading:"What a Flow is — a suspend-friendly stream",blocks:[{t:"p",text:"A **`Flow<T>`** is Kotlin's type for an **asynchronous stream of values** — think of it as the async, coroutine-native cousin of a `List`. A `List<T>` gives you all its values at once, already computed. A `suspend fun` gives you a *single* value, later. A `Flow<T>` gives you *multiple* values, over time, each possibly arriving after a suspend — and it does so cooperatively with coroutines (it can suspend between emissions instead of blocking)."},{t:"table",headers:["You need","Use"],rows:[["One value, now","a normal function"],["One value, later (async)","`suspend fun`"],["Many values, now","`List` / `Sequence`"],["Many values, over time (async)","`Flow`"]]},{t:"list",items:["Examples that are naturally flows: database rows that re-emit when the table changes (Room `Flow` queries), location updates, UI state over time, a stream of search results as the user types, WebSocket messages.","A Flow is built from three roles: a **producer** that emits values (`emit(value)`), zero or more **intermediate operators** that transform the stream (`map`, `filter`), and a **collector** (consumer) that receives the values (`collect { }`).","Flow is part of `kotlinx.coroutines` — it's pure Kotlin (works in KMP, no Android dependency), which is a major reason it replaced LiveData and RxJava in modern Android."]}]},{heading:"Cold flows — the defining property",blocks:[{t:"p",text:"Flows are **cold** by default. Cold means: **the producer code does not run until someone collects, and it runs again, from scratch, for every collector.** Building a flow (`flow { ... }`) creates only a *recipe*; nothing executes until a terminal operator like `collect` pulls values through it. Each new `collect` re-executes the whole recipe independently."},{t:"code",title:"Cold in action — nothing runs until collection",code:`fun numbers(): Flow<Int> = flow {
    println("Flow started")     // producer body
    for (i in 1..3) {
        delay(100)
        emit(i)                 // hand a value to the collector
    }
}

// Building the flow prints NOTHING:
val f = numbers()

// First collection: prints "Flow started", then 1,2,3
f.collect { println(it) }

// Second collection: prints "Flow started" AGAIN, then 1,2,3 again
f.collect { println(it) }`},{t:"list",items:["**Lazy**: no collector, no work. A flow that hits the network does nothing until collected — no wasted requests.","**Independent per collector**: two collectors each get their own private run of the producer — their own network call, their own sequence from the start. There is no sharing.","**Why this matters for Android**: a `Flow` from a repository is safe to create eagerly and pass around; the expensive work only happens where it's actually observed, and it stops when collection stops (structured concurrency). Contrast **hot** streams (StateFlow/SharedFlow), which emit regardless of collectors and share one stream among all — covered in their own topic."]},{t:"note",text:'The cold/hot distinction is the single most-asked Flow interview question. One-liner: "Cold flows are lazy recipes that re-run per collector and only while collected; hot flows exist and emit independently of collectors, multicasting one stream to all."'}]},{heading:"Building flows — the producer side",blocks:[{t:"code",title:"The common builders",code:`// flow { } — the general builder; can suspend, loop, call suspend fns
flow {
    val page1 = api.load(1); emit(page1)
    val page2 = api.load(2); emit(page2)
}

// flowOf(...) — a fixed set of values
flowOf(1, 2, 3)

// asFlow() — from a collection/sequence/range
listOf("a", "b").asFlow()
(1..100).asFlow()

// channelFlow { } — when emissions come from multiple coroutines
channelFlow {
    launch { send(fetchA()) }
    launch { send(fetchB()) }
}

// callbackFlow { } — bridge a callback-based API into a Flow
callbackFlow {
    val listener = LocationListener { loc -> trySend(loc) }
    manager.register(listener)
    awaitClose { manager.unregister(listener) }
}`},{t:"list",items:["**`flow { }`** is the workhorse: its lambda is a suspend block, so you can `delay`, call other suspend functions, loop, and `emit` as many times as you want.","**Emission must stay in the builder's coroutine**: you can't `emit` from a different coroutine you launched inside `flow { }` — that throws. When you genuinely need concurrent producers, use **`channelFlow`**/`callbackFlow`, which provide a thread-safe `send`/`trySend`.","**`callbackFlow`** is how you turn listener/callback APIs (location, sensors, Firebase listeners) into flows — it must call `awaitClose { }` to unregister when collection stops (covered fully in the callbackFlow topic)."]}]},{heading:"Terminal operators — the collector side",blocks:[{t:"p",text:"A **terminal operator** is what actually starts the flow and consumes it. Everything else (map, filter) is just describing the pipeline; a terminal operator turns the crank. Terminal operators are `suspend` functions — they suspend the collecting coroutine until the flow completes (or forever, for infinite flows)."},{t:"code",title:"Terminal operators",code:`// collect — the fundamental one; runs the block per emission
flow.collect { value -> println(value) }

// collectLatest — cancels the block if a NEW value arrives before it finishes
searchQueries.collectLatest { query ->
    val results = repository.search(query)  // cancelled if query changes
    show(results)
}

// Aggregating terminals (they collect internally, then return one value):
val total = flow.reduce { acc, v -> acc + v }
val count = flow.count()
val list  = flow.toList()
val first = flow.first()          // takes one value then cancels the flow
val single = flow.single()

// launchIn — collect in a given scope without a suspend call site
flow.onEach { render(it) }.launchIn(viewModelScope)`},{t:"list",items:["**`collect`** processes every emission in order, waiting for your block to finish before the next value is delivered (backpressure by suspension — the producer waits if the collector is slow).","**`collectLatest`** cancels the *previous* block when a new value arrives — essential for 'only the latest matters' cases (search-as-you-type: abandon the in-flight search when the query changes).","**`launchIn(scope)`** = `scope.launch { flow.collect() }` — a convenient way to start collection in a scope (paired with `onEach` for the per-value logic). Common in ViewModels.","**`first()`/`take(n)`** cancel the flow early once they have what they need — cancellation propagates back to the producer (which stops emitting), a nice property of structured flows."]}]},{heading:"Flows are sequential, and respect cancellation",blocks:[{t:"list",items:["**Sequential by default**: within a single collection, values are produced and processed one at a time, in order — `emit` suspends until the collector's block (and downstream operators) finish with that value. There's no concurrency unless you explicitly add it (`buffer`, `flatMapMerge`, `channelFlow`).","**Context preservation**: a flow's producer runs in the collector's coroutine context by default — you don't switch threads implicitly. Changing the producer's context requires `flowOn` (its own topic); emitting from a different context without it throws (a safety check).","**Cancellation-cooperative**: because collection happens in a coroutine, cancelling that coroutine cancels the collection, which cancels the producer. Flow builders check for cancellation at each `emit`, so a cancelled flow stops promptly. (Tight CPU loops that never emit/suspend still need manual `ensureActive()`/`isActive` checks — same as any coroutine.)","**Exceptions propagate downward**: an exception in the producer or an operator surfaces at the collector, and can be handled with the `catch` operator or a try/catch around `collect` (error handling has its own topic)."]}]}],jS=[{level:"junior",q:"What is a Flow and when would you use one?",a:[{t:"p",text:"**The concept**: a `Flow<T>` is an asynchronous stream of values — it produces *multiple* values *over time*, cooperatively with coroutines. The easiest way to place it: a normal function returns one value now; a `suspend fun` returns one value later; a `List` gives many values now; a `Flow` gives many values later, as they become available, able to suspend between them instead of blocking."},{t:"p",text:"**When to use it**: anything that's a *stream* rather than a one-shot. Room database queries that should re-emit whenever the data changes; location or sensor updates; a UI state that changes over time; search results updating as the user types; WebSocket messages. If the answer to 'how many values, and when?' is 'several, over time', it's a Flow. If it's 'exactly one, later', it's a plain suspend function. Flow is also pure Kotlin (no Android dependency), which is why modern Android and KMP use it in place of LiveData and RxJava."}]},{level:"junior",q:"What does it mean that a Flow is 'cold'?",a:[{t:"p",text:"**The concept**: cold means the flow's producer code **doesn't run until someone collects it**, and it **runs again from the start for every collector**. Creating a flow with `flow { ... }` just builds a recipe — nothing executes. Only a terminal operator like `collect` pulls values through, and each separate `collect` triggers its own independent run of the producer."},{t:"code",title:"Two collections = two independent runs",code:`val f = flow { println("start"); emit(1); emit(2) }
// nothing printed yet
f.collect { }   // prints "start", emits 1,2
f.collect { }   // prints "start" AGAIN, emits 1,2 again`},{t:"p",text:"**Why it's useful**: laziness means no wasted work — a flow that calls the network does nothing until observed. Independence means each collector gets its own fresh sequence, with no interference. And because collection lives in a coroutine, when the collector's scope is cancelled, the producer stops too. The contrast is a *hot* stream (StateFlow/SharedFlow), which emits whether or not anyone is collecting and shares one stream across all collectors — but that's a separate concept with its own trade-offs."}]},{level:"junior",q:"What is the difference between an intermediate and a terminal operator?",a:[{t:"p",text:"**The concept**: flow operators come in two kinds. **Intermediate operators** (`map`, `filter`, `onEach`, `take`…) transform or configure the stream and return a *new flow* — but they're lazy: they only *describe* work and run nothing by themselves. **Terminal operators** (`collect`, `toList`, `first`, `reduce`, `count`…) actually start the flow, pull values through all the intermediate operators, and consume them. Terminal operators are `suspend` functions because they run over time."},{t:"p",text:"The practical consequence: a chain like `flow.map { }.filter { }` does *absolutely nothing* — no map, no filter — until you attach a terminal operator. This trips people up: 'my map isn't running' almost always means 'nothing is collecting'. Think of intermediate operators as assembling a pipeline and the terminal operator as opening the tap. `launchIn(scope)` is a convenience that packages `scope.launch { collect() }`, so `flow.onEach { }.launchIn(scope)` is the common 'start collecting in this scope' idiom."}]},{level:"junior",q:"How do you create a Flow? Name a few builders.",a:[{t:"list",items:["**`flow { }`** — the general builder. Its lambda is a suspend block, so you can call suspend functions, loop, `delay`, and `emit(value)` as many times as needed. The workhorse for custom producers.","**`flowOf(a, b, c)`** — a flow of a fixed set of values, like `listOf` for flows.","**`.asFlow()`** — turn a collection, sequence, or range into a flow (`(1..10).asFlow()`, `myList.asFlow()`).","**`callbackFlow { }`** — bridge a *callback/listener*-based API (location updates, sensor, Firebase listener) into a Flow; it gives a thread-safe `trySend` and requires `awaitClose { }` to clean up when collection stops.","**`channelFlow { }`** — like `flow { }` but allows emitting from *multiple* coroutines concurrently (via `send`), for when producers are parallel."]},{t:"p",text:"The one rule to remember about `flow { }`: you must `emit` from the builder's own coroutine — you can't launch a child coroutine inside it and emit from there (it throws, to protect flow's sequential context guarantees). When emissions genuinely come from multiple coroutines or a callback, that's exactly what `channelFlow`/`callbackFlow` are for."}]},{level:"junior",q:"What is the difference between collect and collectLatest?",a:[{t:"p",text:"**The concept**: both are terminal operators that consume every emission, but they differ in what happens when a new value arrives *while you're still processing the previous one*. `collect` processes each value to completion before accepting the next — your block runs fully for value 1, then fully for value 2, and so on (the producer waits for you). `collectLatest` **cancels** your still-running block when a newer value arrives and restarts it with the new value."},{t:"code",title:"collectLatest for search-as-you-type",code:`queryFlow.collectLatest { query ->
    // If the user types another letter before this search finishes,
    // this block is cancelled and restarted with the new query —
    // no wasted request, no stale results overwriting fresh ones.
    val results = repository.search(query)
    show(results)
}`},{t:"p",text:"**When to use which**: `collect` when every value must be fully handled (processing a queue of events, writing each item to a DB). `collectLatest` when *only the latest value matters* and older in-flight work should be abandoned — the classic case is search-as-you-type or reacting to the newest UI state, where finishing an outdated computation is wasteful and can cause the stale-result race (an old slow response overwriting a newer one). The operator equivalents on the transform side are `mapLatest`/`flatMapLatest`, which apply the same 'cancel previous' idea inside a pipeline."}]},{level:"senior",q:"Explain cold vs hot flows in depth. Give concrete Android examples of when to use each.",a:[{t:"p",text:"**Cold**: the producer runs *per collector*, starting on collection and stopping when collection stops; each collector gets an independent execution. **Hot**: the stream exists and emits *independently* of collectors, multicasting the same emissions to all current collectors; late collectors miss earlier values (unless replay is configured)."},{t:"list",items:["**Use cold** for *pull-based, per-consumer work that should be lazy and cancellable*: a repository function returning `flow { emit(api.fetch()) }` or a Room `Flow` query. Nothing runs until a screen observes it; two screens each get their own execution; navigating away cancels the work. This is the default and the right choice for most data-layer streams.","**Use hot** for *shared state or events that exist regardless of observers*: a `StateFlow` holding UI state (there's one current value, shared by all observers, and it exists whether or not the UI is on screen), or a `SharedFlow` broadcasting events to multiple listeners. Sensor/location streams that should run once and fan out to many collectors are also hot (often a cold `callbackFlow` converted to hot via `shareIn`).","**The bridge**: `stateIn`/`shareIn` convert a cold flow into a hot one — you take an expensive cold upstream (a DB query), collect it *once*, and multicast the result, so two screens don't trigger two DB observations. That's the standard ViewModel pattern (`stateIn(scope, WhileSubscribed(5000), initial)`)."]},{t:"p",text:"**The subtle bug this prevents**: if a ViewModel exposes a *cold* flow directly and two things collect it (the UI plus, say, a logger), you get two independent upstream executions — two network calls, two DB observations. Converting to hot with `stateIn` collects once and shares. Conversely, exposing raw mutable *hot* state lets collectors miss events or see stale replays. Knowing which to use where — cold in the data layer, hot for shared UI state, bridged with stateIn/shareIn — is the core competency the cold/hot question tests."}]},{level:"senior",q:"Why must you emit only from the flow builder's coroutine, and what do you use when values come from multiple coroutines or a callback?",a:[{t:"p",text:"**The reason for the restriction**: `flow { }` guarantees **context preservation** and **sequential emission** — the collector can rely on emissions arriving one at a time, in the collector's context (unless `flowOn` changes the producer's). If you could launch a child coroutine inside `flow { }` and call `emit` from it, emissions could happen concurrently and from a different context, breaking those guarantees. So the flow builder actively detects cross-coroutine emission and throws `IllegalStateException` ('Flow invariant is violated'). `emit` is not thread-safe by design."},{t:"list",items:["**`channelFlow { }`** — use when emissions genuinely originate from *multiple coroutines you launch* (concurrent producers). It backs the flow with a `Channel` and exposes a thread-safe `send`/`trySend` that *is* safe to call from any coroutine, precisely because a channel synchronizes them. You can `launch { send(a) }` and `launch { send(b) }` inside it.","**`callbackFlow { }`** — a `channelFlow` specialized for *callback/listener* APIs (which invoke you on their own threads). Register the listener, `trySend` from its callback, and call `awaitClose { unregister() }` so the listener is removed when collection is cancelled. This is the canonical way to wrap `LocationManager`, sensors, `ValueEventListener`, etc.","**Cost/trade-off**: both use a channel internally, so they're slightly heavier and their emission isn't strictly sequential in the same way `flow { }` is — you use them specifically *because* you need concurrent/external emission, and accept the channel's buffering semantics (configurable capacity, `onBufferOverflow`)."]},{t:"p",text:"The mental rule: plain `flow { }` for a single sequential producer (the common case); `channelFlow`/`callbackFlow` the moment emission must cross coroutine or thread boundaries."}]},{level:"senior",q:"How does cancellation work with flows, and what's the gotcha with tight loops?",a:[{t:"p",text:"**The model**: flow collection happens inside a coroutine, so it's governed by structured concurrency. Cancel the collecting coroutine (its scope is cancelled, the screen leaves, a `take(1)` completes) and the collection is cancelled — which propagates back up: the producer stops, and terminal operators like `first`/`take` cancel the upstream once they have enough. Flow builders are **cancellation-cooperative at each `emit`**: `emit` checks for cancellation, so a flow that emits or calls suspend functions periodically will stop promptly when cancelled, throwing `CancellationException` that unwinds the coroutine normally."},{t:"list",items:["**The gotcha**: cancellation is cooperative — it's only *checked* at suspension points (`emit`, `delay`, other suspend calls). A flow whose producer runs a tight **CPU loop without emitting or suspending** won't notice cancellation and will run to completion even after the collector is gone. Example: `flow { var x = 0L; while (true) { x++ } }` ignores cancellation.","**The fix**: insert cooperative checks — call `currentCoroutineContext().ensureActive()` (throws if cancelled) or check `isActive` in the loop, or use the `.cancellable()` operator which inserts a cancellation check on every emission (useful for flows like `(1..1_000_000).asFlow()` that emit rapidly without other suspension points).","**Related correctness rule**: never `catch` a `CancellationException` and swallow it inside flow operators or `collect` blocks — that breaks cancellation (the coroutine thinks it recovered). Rethrow it, or catch specific exceptions only. The `catch` operator is already cancellation-aware and won't swallow it, but a hand-written `try/catch (e: Exception)` around a suspend call will — a classic bug."]}]},{level:"junior",q:"How does a cold Flow work under the hood (the emit/collect handshake)?",a:[{t:"p",text:"A cold Flow is essentially a suspend lambda that isn't run until collected. When you call a terminal operator like `collect`, the flow's builder block executes; each `emit(value)` *suspends* the producer and calls the collector's block with the value, then resumes the producer when the collector finishes. It's a cooperative, sequential handshake between producer and consumer — all within coroutines."},{t:"code",title:"emit calls the collector",code:`val f = flow {
    emit(1)          // suspends, invokes collector with 1, resumes when done
    emit(2)
}
f.collect { value -> println(value) }   // runs the builder; prints 1 then 2`},{t:"list",items:["**Nothing runs until collected** — the builder is stored, not executed, until a terminal operator.","**`emit` → collector** — emitting suspends the producer and directly invokes the collector's lambda.","**Sequential** — one value flows through fully before the next is emitted (unless you add buffering/concurrency operators).","**Per-collector** — each `collect` runs the builder again (cold)."]},{t:"note",text:"A cold Flow is a suspend lambda that runs only when collected: the terminal operator executes the builder, and each emit() suspends the producer, invokes the collector's block with the value, then resumes — a sequential producer/consumer handshake in coroutines. Each collect re-runs the builder (cold)."}]},{level:"junior",q:"What are the common terminal operators, and what do they do?",a:[{t:"p",text:"A terminal operator is what *starts* the flow and consumes its values — it's a suspend function you call at the end of the chain. `collect` is the most general (runs a block per value); others reduce the stream to a single result or a collection."},{t:"list",items:["**`collect { }`** — the fundamental terminal; runs a block for each emission until the flow completes.","**`toList()` / `toSet()`** — gather all emissions into a collection (only for finite flows).","**`first()` / `firstOrNull()`** — take the first (matching) value, then cancel the flow.","**`single()`** — expect exactly one value (throws if zero or more than one).","**`reduce` / `fold`** — aggregate emissions into one result.","**`count()`** — number of emissions; **`launchIn(scope)`** — collect in a scope without a lambda."]},{t:"code",title:"Terminals",code:`flow.collect { println(it) }
val all = flow.toList()
val firstEven = flow.first { it % 2 == 0 }
val sum = flow.fold(0) { acc, x -> acc + x }`},{t:"note",text:"Terminal operators start and consume a flow (suspend functions): collect (per-value block), toList/toSet (gather — finite flows), first/firstOrNull (take one, then cancel), single (exactly one), reduce/fold (aggregate), count, and launchIn (collect in a scope). Without a terminal, a flow never runs."}]},{level:"junior",q:"What is launchIn, and how does it differ from collect?",a:[{t:"p",text:"`launchIn(scope)` collects a flow in the given `CoroutineScope` and returns a `Job` immediately — it's a shorthand for `scope.launch { flow.collect { } }`. Unlike `collect` (a suspend function that blocks the current coroutine until the flow finishes), `launchIn` is fire-and-forget: it starts collection concurrently. You typically pair it with `onEach` to do the per-value work."},{t:"code",title:"launchIn vs collect",code:`// collect: suspends here until the flow completes
scope.launch { flow.collect { handle(it) } }

// launchIn: starts collection, returns a Job immediately
flow.onEach { handle(it) }.launchIn(scope)`},{t:"list",items:["**`collect`** — suspend; runs inline until the flow completes.","**`launchIn(scope)`** — launches collection in `scope`, returns a `Job`; non-blocking.","**Pair with `onEach`** — put per-value logic in `onEach` since `launchIn` takes no lambda.","**Multiple flows** — `launchIn` is handy to start collecting several flows without nesting."]},{t:"note",text:"launchIn(scope) = scope.launch { flow.collect { } } — starts collection concurrently and returns a Job (fire-and-forget), unlike collect which suspends inline until the flow finishes. Pair launchIn with onEach for per-value logic. Great for launching several flow collections without nested launch/collect."}]},{level:"junior",q:"What's the difference between a Flow and a Sequence?",a:[{t:"p",text:"Both are lazy and process elements one at a time, but a `Sequence` is *synchronous/blocking* — its operations run on the calling thread and can't suspend — while a `Flow` is *asynchronous* — it can `suspend` (do I/O, `delay`, switch dispatchers) between emissions. Use a `Sequence` for lazy in-memory computation; use a `Flow` when values arrive over time or involve suspension."},{t:"list",items:["**`Sequence`** — synchronous, blocking; lazy transformations of in-memory data on the current thread.","**`Flow`** — asynchronous; can suspend, do I/O, emit over time, switch context (`flowOn`).","**Timing** — a `Sequence` computes each element on demand synchronously; a `Flow` can wait (network, timers).","**Choose** — `Sequence` for large in-memory pipelines; `Flow` for async streams (DB observations, network, events)."]},{t:"note",text:"Sequence = synchronous, blocking lazy pipeline on the current thread (in-memory data). Flow = asynchronous lazy stream that can suspend (I/O, delay, dispatcher switch) and emit over time. Use Sequence for lazy in-memory computation; Flow for values that arrive over time or need suspension."}]},{level:"junior",q:"How is a Flow different from a suspend function that returns a List?",a:[{t:"p",text:"A suspend function returning a `List` gives you *all the values at once*, after everything is ready — a single result. A `Flow` emits values *one at a time over time*, and can keep emitting (an ongoing stream). Use the list for a one-shot fetch; use a `Flow` when values arrive incrementally or the source keeps producing updates."},{t:"list",items:["**Suspend → `List`** — one-shot; you wait, then get the whole result. Good for a single request.","**`Flow<T>`** — multiple emissions over time; can be infinite (DB changes, location updates, UI events).","**Streaming updates** — a `Flow` from Room re-emits when the table changes; a suspend function would need re-calling.","**Incremental** — a `Flow` can emit results as they arrive (e.g. paging) rather than waiting for all."]},{t:"code",title:"One-shot vs stream",code:`suspend fun getUsers(): List<User>            // one result, then done
fun observeUsers(): Flow<List<User>>          // re-emits whenever data changes`},{t:"note",text:"A suspend function returning List gives all values at once (one-shot). A Flow emits values over time and can be ongoing/infinite (DB observations, events, location). Use List for a single fetch; use Flow for incremental results or a source that keeps producing updates (e.g. Room's Flow re-emits on change)."}]},{level:"junior",q:"How do you convert existing data into a Flow?",a:[{t:"p",text:"Use the appropriate builder: `flowOf(a, b, c)` for a fixed set of values, `listOf(...).asFlow()` (or `IntRange.asFlow()`) for a collection/range, and the general `flow { }` builder when you need to emit with logic or suspension. For callbacks, use `callbackFlow`; for a channel, `receiveAsFlow`."},{t:"code",title:"Builders",code:`flowOf(1, 2, 3)                      // fixed values
listOf("a", "b").asFlow()           // from a collection
(1..100).asFlow()                    // from a range
flow { repeat(5) { emit(it); delay(1000) } }   // custom, with suspension`},{t:"list",items:["**`flowOf(...)`** — emit a known set of values.","**`.asFlow()`** — turn a collection, range, or sequence into a flow.","**`flow { }`** — general builder for custom emission (loops, I/O, suspension).","**`callbackFlow`/`channelFlow`** — for callback sources or emitting from multiple coroutines."]},{t:"note",text:"flowOf(a,b,c) for fixed values, collection/range .asFlow(), the general flow { } builder for custom/suspending emission, and callbackFlow/channelFlow for callbacks or multi-coroutine emission. Pick the simplest that fits the source."}]},{level:"junior",q:"What does it mean that a Flow processes values sequentially?",a:[{t:"p",text:"By default a Flow handles one value at a time, start to finish, before the producer emits the next: `emit(1)` → collector processes 1 fully → `emit(2)` → collector processes 2. The producer and collector run in the *same* coroutine, so there's no built-in concurrency between emitting and collecting. Operators like `buffer`, `flatMapMerge`, or `collectLatest` change this."},{t:"list",items:["**One at a time** — each emission is fully collected before the next is produced.","**Same coroutine** — producer and collector share a coroutine by default (no parallelism).","**Implications** — a slow collector slows the producer (natural backpressure).","**Opt into concurrency** — `buffer()` (decouple), `flatMapMerge` (concurrent inner flows), `collectLatest` (cancel-and-restart)."]},{t:"note",text:"By default a Flow processes one value fully before emitting the next, with producer and collector in the same coroutine (no built-in concurrency) — so a slow collector paces the producer (natural backpressure). Opt into concurrency with buffer() (decouple stages), flatMapMerge (concurrent inners), or collectLatest."}]},{level:"junior",q:"What happens if you collect the same cold flow twice?",a:[{t:"p",text:"Each `collect` runs the flow's builder *from scratch* — a cold flow re-executes its entire producer for every collector. So collecting a network-backed flow twice makes two network calls; collecting a `flow { emit(...) }` twice runs the block twice. If you want to *share* one execution among collectors, convert it to a hot flow with `shareIn`/`stateIn`."},{t:"code",title:"Cold = re-run per collector",code:`val f = flow { println("fetching"); emit(api.get()) }
f.collect { }   // prints "fetching", one network call
f.collect { }   // prints "fetching" AGAIN, another network call

// Share one execution:
val shared = f.shareIn(scope, SharingStarted.WhileSubscribed(), replay = 1)`},{t:"list",items:["**Re-executes** — the builder runs anew for each collector (independent streams).","**Side effects repeat** — duplicate network/DB calls if multiple collectors.","**Fix for sharing** — `shareIn`/`stateIn` make it hot so collectors share one upstream execution.","**Cold is often desired** — independent, reproducible streams (e.g. each screen its own fetch)."]},{t:"note",text:"A cold flow re-runs its whole builder for each collector — collecting twice = two executions (two network calls). To share one upstream execution among collectors, make it hot with shareIn/stateIn. Cold is fine when you want independent streams; share when duplicate work/side effects are the problem."}]},{level:"junior",q:"What is the difference between first(), single(), and firstOrNull()?",a:[{t:"p",text:"These terminals take a value and stop. `first()` returns the first emission (throwing if the flow is empty) and *cancels* the flow after; `firstOrNull()` returns `null` instead of throwing on empty; `single()` expects *exactly one* emission and throws if there are zero or more than one. Each can take a predicate to match a specific value."},{t:"list",items:["**`first()`** — first emission; throws `NoSuchElementException` if empty; cancels upstream after.","**`firstOrNull()`** — first emission or `null` if empty (no throw).","**`single()`** — requires exactly one emission; throws if zero or more than one (use when you expect precisely one value).","**With predicate** — `first { it > 5 }` takes the first match, then cancels."]},{t:"note",text:"first() = first emission (throws if empty), cancels upstream after; firstOrNull() = first or null (no throw); single() = requires exactly one (throws on zero or >1). All accept a predicate (first { }). Use first/firstOrNull to grab one value from a stream, single when exactly one is expected."}]},{level:"junior",q:"What thread or context does a Flow run on by default?",a:[{t:"p",text:"By default, a Flow runs entirely in the *collector's* coroutine context — whatever dispatcher `collect` is called from. There's no built-in threading; the producer, operators, and collector all run on the collector's thread. To run part of the pipeline on a different dispatcher (e.g. do the upstream I/O on `IO`), use `flowOn(dispatcher)`."},{t:"code",title:"Collector context + flowOn",code:`flow { emit(loadFromDisk()) }   // runs on collector's context...
    .map { transform(it) }
    .flowOn(Dispatchers.IO)     // ...but this upstream runs on IO
    .collect { render(it) }     // collector on, say, Main`},{t:"list",items:["**Collector's context** — the whole flow runs where `collect` is called, unless changed.","**`flowOn(dispatcher)`** — changes the context of the *upstream* (operators/producer above it).","**No threading by default** — you must add `flowOn` for background work.","**Downstream unaffected** — `flowOn` only affects upstream; the collector stays on its own context."]},{t:"note",text:"A Flow runs in the collector's context by default (no built-in threading) — producer, operators, and collector all on the collect caller's dispatcher. Use flowOn(dispatcher) to run the UPSTREAM on another dispatcher (e.g. IO for the producer) while the collector stays on Main. flowOn affects only upstream."}]},{level:"senior",q:"What is context preservation, and why does emitting from a different context throw?",a:[{t:"p",text:"Flow enforces *context preservation*: a flow must emit from the same coroutine context in which the builder runs. If you `withContext(Dispatchers.IO) { emit(x) }` inside a `flow { }`, you're emitting from a different context than the collector expects, so Flow throws an `IllegalStateException` ('Flow invariant is violated'). The correct way to change context is `flowOn`, not `withContext` around `emit`."},{t:"code",title:"Wrong vs right",code:`// WRONG: emits from a different context -> throws
flow {
    withContext(Dispatchers.IO) { emit(load()) }   // IllegalStateException
}
// RIGHT: use flowOn to move the whole upstream
flow { emit(load()) }.flowOn(Dispatchers.IO)`},{t:"list",items:["**Invariant** — emissions must happen in the builder's context, not a switched one.","**Why** — it guarantees predictable threading and cancellation for collectors.","**`flowOn`** — the sanctioned way to change upstream context; it moves the producer's context correctly.","**`channelFlow`** — if you genuinely need to emit from multiple coroutines/contexts, use `channelFlow` + `send`."]},{t:"note",text:"Context preservation: a Flow must emit from the builder's context. Wrapping emit in withContext(IO) inside flow{} violates the invariant and throws IllegalStateException. Change context with flowOn (moves the whole upstream) instead. To emit from multiple coroutines/contexts, use channelFlow + send."}]},{level:"junior",q:"How do you limit a flow with take, drop, and takeWhile?",a:[{t:"p",text:"These operators bound how many values you process. `take(n)` collects only the first `n` emissions then cancels the flow; `drop(n)` skips the first `n`; `takeWhile { }`/`dropWhile { }` take/skip while a predicate holds. `take` is especially useful to turn an infinite flow into a finite one."},{t:"code",title:"Bounding a flow",code:`infiniteFlow.take(5)                 // first 5, then cancels upstream
flow.drop(2)                         // skip first 2
flow.takeWhile { it < 100 }          // until the predicate fails
flow.dropWhile { it.isBlank() }      // skip leading blanks`},{t:"list",items:["**`take(n)`** — first n emissions, then completes/cancels upstream.","**`drop(n)`** — ignore the first n, emit the rest.","**`takeWhile { }`** — emit until the predicate is false (then stop).","**`dropWhile { }`** — skip while the predicate holds, then emit the rest."]},{t:"note",text:"take(n) = first n then cancel upstream (turns infinite into finite); drop(n) = skip first n; takeWhile { } = emit until predicate fails; dropWhile { } = skip leading matches. take is the common way to bound an otherwise-infinite flow."}]},{level:"junior",q:"How do you aggregate a flow's values with reduce, fold, and scan?",a:[{t:"p",text:"`reduce` and `fold` are *terminal* operators that collapse all emissions into a single result (like their collection counterparts). `scan` is an *intermediate* operator that emits each *intermediate* accumulated value — great for running totals or accumulating state over a stream."},{t:"code",title:"reduce/fold/scan",code:`flowOf(1,2,3).reduce { acc, x -> acc + x }        // 6 (terminal)
flowOf(1,2,3).fold(10) { acc, x -> acc + x }      // 16 (terminal, with initial)
flowOf(1,2,3).scan(0) { acc, x -> acc + x }
    .collect { println(it) }                       // emits 0,1,3,6 (running totals)`},{t:"list",items:["**`reduce`** — terminal; combine emissions, no initial value (throws on empty).","**`fold(initial)`** — terminal; like reduce with a seed and a possibly different result type.","**`scan(initial)`** — intermediate; emits each running accumulation (a stream of states).","**Use `scan`** — running totals, accumulating UI state from a stream of events."]},{t:"note",text:"reduce (terminal, no seed) and fold(initial) (terminal, seeded) collapse a flow to one result. scan(initial) is INTERMEDIATE — it emits each running accumulation (0,1,3,6 for sums), ideal for running totals or accumulating state from an event stream."}]},{level:"senior",q:"What is the difference between flow{} and channelFlow{}?",a:[{t:"p",text:"`flow { }` is sequential and must emit from its own single coroutine (context preservation). `channelFlow { }` provides a `ProducerScope` where you `send` values and can do so from *multiple* coroutines / different contexts concurrently — it's backed by a channel. Use `channelFlow` when values come from concurrent sources or callbacks; use `flow` for simple sequential emission."},{t:"code",title:"channelFlow for concurrent emission",code:`channelFlow {
    launch { send(fromSourceA()) }   // multiple coroutines can send
    launch { send(fromSourceB()) }
    awaitClose { /* cleanup */ }
}`},{t:"list",items:["**`flow { }`** — sequential, single-coroutine emission; lightest weight.","**`channelFlow { }`** — `send` from multiple coroutines/contexts; backed by a channel (some overhead).","**`callbackFlow`** — a `channelFlow` specialized for callback bridging (with `awaitClose`).","**Choose `channelFlow`** — merging concurrent producers, emitting from launched children, or callbacks."]},{t:"note",text:"flow { } is sequential and must emit from its own coroutine (context preservation). channelFlow { } gives a ProducerScope where you send from MULTIPLE coroutines/contexts concurrently (channel-backed, some overhead). Use channelFlow (or callbackFlow) for concurrent producers/callbacks; flow{} for simple sequential emission."}]},{level:"junior",q:"Why do flows need coroutines, and how are they related?",a:[{t:"p",text:"Flow is built *on* coroutines: `emit` and `collect` are suspend functions, so a flow can only be collected inside a coroutine, and its emissions suspend/resume like any coroutine work. This is why flows integrate seamlessly with structured concurrency — collecting a flow in `viewModelScope` means it's cancelled when the scope is, and `flowOn` uses dispatchers."},{t:"list",items:["**`collect` is suspend** — you collect inside a coroutine (`launch`/`viewModelScope`).","**`emit` suspends** — flows leverage suspension for backpressure and async work.","**Structured concurrency** — collecting in a scope ties the flow's lifetime to that scope (auto-cancel).","**Dispatchers apply** — `flowOn` uses coroutine dispatchers for threading."]},{t:"note",text:"Flow is built on coroutines: emit/collect are suspend functions, so you collect only inside a coroutine, and flows inherit structured concurrency (collecting in viewModelScope auto-cancels) and dispatchers (flowOn). Flows are the streaming layer over coroutines' one-shot suspend model."}]},{level:"junior",q:"How do you collect a flow safely tied to a screen's lifecycle?",a:[{t:"p",text:"In Compose, use `collectAsStateWithLifecycle()`; in Views, collect inside `repeatOnLifecycle(STARTED)` within `lifecycleScope`. Both stop collecting when the UI is backgrounded (below STARTED) and resume when it returns, avoiding wasted work and updates to an off-screen UI."},{t:"code",title:"Lifecycle-aware collection",code:`// Compose
val state by viewModel.uiState.collectAsStateWithLifecycle()

// Views
lifecycleScope.launch {
    repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.uiState.collect { render(it) }
    }
}`},{t:"list",items:["**`collectAsStateWithLifecycle()`** — the Compose default; lifecycle-aware.","**`repeatOnLifecycle(STARTED)`** — the Views pattern; cancels collection on STOP, restarts on START.","**Pairs with `stateIn(WhileSubscribed)`** — so the upstream also stops when there are no collectors.","**Avoid plain `collect` in the UI without lifecycle** — it keeps collecting in the background."]},{t:"note",text:"Compose: collectAsStateWithLifecycle(). Views: repeatOnLifecycle(STARTED) inside lifecycleScope. Both stop collecting when backgrounded and resume on return (no wasted work/off-screen updates). Pair with stateIn(WhileSubscribed) so the upstream also stops with no collectors."}]},{level:"junior",q:"How do you perform a side effect for each value while collecting?",a:[{t:"p",text:"Use `onEach { }` — an intermediate operator that runs a side effect for each emission and passes the value through unchanged. It's ideal for logging, analytics, or triggering effects mid-pipeline, and pairs with `launchIn` to collect without a separate `collect` lambda."},{t:"code",title:"onEach for side effects",code:`flow
    .onEach { logAnalytics(it) }        // side effect, value unchanged
    .map { it.toUiModel() }
    .onEach { println("mapped: $it") }
    .launchIn(viewModelScope)`},{t:"list",items:["**`onEach { }`** — side effect per value; returns the value unchanged (unlike `map`, which transforms).","**Anywhere in the chain** — before/after transformations, for logging or triggering effects.","**With `launchIn`** — `flow.onEach { }.launchIn(scope)` collects without a `collect` block.","**Not for transformation** — use `map`/`transform` to change values."]},{t:"note",text:"onEach { } runs a side effect per emission and passes the value through unchanged (vs map, which transforms) — for logging/analytics/effects mid-pipeline. Pair with launchIn to collect without a separate collect lambda: flow.onEach { }.launchIn(scope)."}]},{level:"junior",q:"How do you handle an infinite flow that never completes?",a:[{t:"p",text:"Many flows are intentionally infinite (a Room `Flow`, a location stream, UI events) — they never call the collector's completion. You bound them by *scope* (collect in a lifecycle scope that cancels) or by *operators* (`take(n)`, `takeWhile`, `first()`) that stop after some condition. You don't 'wait' for them to finish; you cancel them when done."},{t:"list",items:["**Bound by scope** — collect in `viewModelScope`/`lifecycleScope`; cancellation stops the infinite flow.","**Bound by operator** — `take(n)`/`takeWhile`/`first()` complete after a condition.","**Don't use `toList()`** — it never returns on an infinite flow (waits for completion).","**Design** — treat infinite flows as ongoing subscriptions, cancelled via structured concurrency."]},{t:"note",text:"Infinite flows (Room, location, events) never complete — bound them by scope (collect in a lifecycle scope that cancels) or operators (take/takeWhile/first). Never toList() an infinite flow (it waits forever). Treat them as ongoing subscriptions cancelled via structured concurrency."}]},{level:"junior",q:"What is transform, and how is it more flexible than map?",a:[{t:"p",text:"`map` emits exactly one transformed value per input. `transform { }` is the general operator that can emit *zero, one, or many* values per input (via `emit`) — you use it when a single input should produce a variable number of outputs, or none. Most operators (`map`, `filter`) are actually built on `transform`."},{t:"code",title:"transform emits flexibly",code:`flow.transform { value ->
    emit("before $value")     // can emit multiple
    if (value > 0) emit("positive: $value")   // or conditionally
}`},{t:"list",items:["**`map`** — 1 input → 1 output.","**`transform`** — 1 input → 0, 1, or many outputs (call `emit` as needed).","**Use `transform`** — expand one value into several, or filter+map in one step.","**Foundation** — `map`/`filter` are implemented via `transform`."]},{t:"note",text:"map is 1→1; transform { } is 1→0/1/many (call emit as needed) — for expanding one input into several outputs or conditional emission. map/filter are built on transform. Reach for transform when a single input should produce a variable number of emissions."}]},{level:"senior",q:"How does exception handling flow through a Flow pipeline?",a:[{t:"p",text:"Exceptions thrown anywhere upstream (producer or operators) propagate down and can be caught by the `catch { }` operator, which only catches *upstream* exceptions (exception transparency). An exception in the *collector* is not caught by `catch` — wrap the `collect` in `try/catch` for that. This upstream/downstream distinction is why `catch` placement matters."},{t:"code",title:"catch catches upstream only",code:`flow
    .map { risky(it) }        // exception here...
    .catch { e -> emit(fallback) }   // ...caught here (upstream)
    .collect { render(it) }   // exception HERE is NOT caught by catch above
`},{t:"list",items:["**`catch { }`** — catches exceptions from *upstream* operators/producer; can `emit` a fallback or rethrow.","**Collector exceptions** — not caught by `catch`; use `try/catch` around `collect`.","**Placement** — put `catch` after the operators whose failures you want to handle; a `catch` at the end won't catch downstream collector errors.","**Exception transparency** — operators shouldn't swallow exceptions implicitly; `catch` makes handling explicit."]},{t:"note",text:"The catch { } operator catches only UPSTREAM exceptions (producer/operators above it) — exception transparency — and can emit a fallback or rethrow. Collector exceptions aren't caught by catch; use try/catch around collect. Placement matters: catch handles failures from operators above it, not below."}]},{level:"junior",q:"What is filter, and how do you chain intermediate operators?",a:[{t:"p",text:"`filter { }` emits only values matching a predicate — the flow equivalent of `Collection.filter`. Intermediate operators like `filter`, `map`, `onEach` are *lazy* and chainable: each returns a new flow, and nothing runs until a terminal operator collects. You build a declarative pipeline that executes per-value when collected."},{t:"code",title:"A chained pipeline",code:`flow
    .filter { it.isActive }        // keep matching
    .map { it.name }               // transform
    .distinctUntilChanged()        // skip consecutive duplicates
    .collect { println(it) }       // terminal starts it all`},{t:"list",items:["**`filter { }`** — emit only values where the predicate is true; `filterNot`, `filterIsInstance` variants.","**Lazy & chainable** — each intermediate operator returns a new flow; nothing runs until a terminal.","**Order matters** — operators apply top-to-bottom per value (filter before map changes what's transformed).","**Readable pipelines** — declarative, like collection operators but async."]},{t:"note",text:"filter { } emits only values matching a predicate (plus filterNot/filterIsInstance). Intermediate operators (filter/map/onEach) are lazy and chainable — each returns a new flow, and nothing runs until a terminal collects. Order matters (filter before map). It reads like collection operators but async."}]},{level:"junior",q:"How do you emit values from a loop or over time in a flow builder?",a:[{t:"p",text:"Inside `flow { }` you can call `emit` as many times as you like, including in loops and after `delay`, because the builder is a suspend lambda. This lets you produce a stream that ticks over time (a timer), polls periodically, or emits a computed sequence."},{t:"code",title:"Emitting over time",code:`fun ticker(): Flow<Int> = flow {
    var count = 0
    while (true) {
        emit(count++)     // emit each tick
        delay(1000)       // wait a second (cancellable)
    }
}
fun polls(): Flow<Data> = flow {
    while (true) { emit(api.poll()); delay(5000) }   // periodic polling
}`},{t:"list",items:["**`emit` in loops** — produce many values from a `while`/`for`.","**`delay` between emits** — time-based streams (tickers, polling).","**Cancellable** — `delay`/`emit` are suspension points, so the flow stops when the collector is cancelled.","**Infinite is fine** — bound it with scope or operators (`take`)."]},{t:"note",text:"Inside flow { } (a suspend lambda) call emit in loops and after delay to produce streams over time — tickers, periodic polling, computed sequences. delay/emit are suspension points so it's cancellable (stops when the collector is cancelled). Infinite loops are fine; bound with scope or take."}]},{level:"senior",q:"How do you share a cold flow among multiple collectors without re-running it?",a:[{t:"p",text:"Convert it to a *hot* flow with `shareIn` (for a `SharedFlow`) or `stateIn` (for a `StateFlow`). These run the upstream *once* in a given scope and multicast emissions to all collectors, so a network/DB source isn't re-executed per collector. `SharingStarted.WhileSubscribed()` keeps the upstream active only while there are collectors."},{t:"code",title:"Share one upstream execution",code:`val shared: SharedFlow<Data> = repository.dataFlow()
    .shareIn(scope, SharingStarted.WhileSubscribed(5000), replay = 1)

val state: StateFlow<UiState> = repository.dataFlow()
    .map { toUiState(it) }
    .stateIn(scope, SharingStarted.WhileSubscribed(5000), UiState.Loading)`},{t:"list",items:["**`shareIn`** — hot `SharedFlow`; multicasts to all collectors; configurable `replay`.","**`stateIn`** — hot `StateFlow` with a current value; ideal for UI state.","**`WhileSubscribed(5000)`** — upstream stops 5s after the last collector leaves (survives config change).","**Prevents duplicate work** — one network/DB subscription shared, not one per collector."]},{t:"note",text:"Make it hot: shareIn (SharedFlow, configurable replay) or stateIn (StateFlow with a current value) runs the upstream once in a scope and multicasts to all collectors — no per-collector re-execution. SharingStarted.WhileSubscribed(5000) keeps upstream alive only while subscribed (+5s for config change). Fixes duplicate network/DB calls."}]}],VS={id:"basics",title:"Flow Basics & Cold Flows",tagline:"What a Flow is, cold vs hot, builders (flow/callbackFlow/channelFlow), terminal operators, sequential emission, and cancellation.",tags:["Flow","Cold Flow","collect","Coroutines","Cancellation"],content:LS,qa:jS},NS=[{heading:"Operators are lazy transformations that return new flows",blocks:[{t:"p",text:"An **intermediate operator** takes a flow and returns a *new* flow with some transformation applied. They're **lazy** — calling `map { }` runs nothing; it just wraps the upstream flow in a new one that will apply the transformation *when collected*. You chain them into a pipeline, and only a terminal operator (`collect`) makes values flow through. Each operator runs in the collector's coroutine (unless `flowOn` intervenes), and processes values one at a time as they pass through."},{t:"code",title:"A pipeline reads top-to-bottom as values flow through",code:`repository.observeArticles()      // Flow<List<Article>>
    .map { it.filter(Article::isPublished) }   // transform each emission
    .filter { it.isNotEmpty() }                // drop empties
    .onEach { log("emitting \${it.size} articles") } // peek (side effect)
    .catch { emit(emptyList()) }               // handle upstream errors
    .collect { render(it) }                    // terminal: starts everything`}]},{heading:"Transforming operators: map, filter, transform, and friends",blocks:[{t:"list",items:["**`map { }`** — transform each value into exactly one new value (`Flow<A>` → `Flow<B>`).","**`filter { }`** / **`filterNot`** / **`filterNotNull`** / **`filterIsInstance<T>()`** — keep only values matching a predicate/type.","**`transform { }`** — the general operator that `map`/`filter` are built on: for each upstream value you can `emit` **zero, one, or many** downstream values. Use when the 1:1 shape of `map` doesn't fit.","**`onEach { }`** — perform a side effect per value without changing it (logging, triggering something); returns the same values. Great for debugging a pipeline.","**`withIndex()` / `runningReduce { }` / `scan(initial) { }`** — index values, or emit a running accumulation (scan emits the initial value then each intermediate fold — perfect for accumulating state)."]},{t:"code",title:"transform and scan",code:`// transform: expand each value into several
flowOf(1, 2, 3).transform { n ->
    emit("start $n"); emit("end $n")
}   // start 1, end 1, start 2, end 2, ...

// scan: emit a running total (initial, then each step)
flowOf(1, 2, 3).scan(0) { acc, n -> acc + n }   // 0, 1, 3, 6`}]},{heading:"Size/time-limiting operators",blocks:[{t:"list",items:["**`take(n)`** — take the first n values then cancel upstream. **`takeWhile { }`** — until the predicate fails. **`drop(n)` / `dropWhile { }`** — skip values.","**`debounce(ms)`** — emit a value only after `ms` of silence; each new value resets the timer. The search-box classic: wait until the user stops typing. Drops intermediate rapid values.","**`sample(ms)`** — emit the *latest* value every `ms` (a periodic snapshot). Debounce waits for a pause; sample takes regular snapshots regardless of pauses.","**`distinctUntilChanged()`** — suppress consecutive duplicates (only emit when the value differs from the previous). Prevents redundant downstream work when the same value repeats.","**`distinctUntilChangedBy { key }`** — same, comparing by a derived key."]},{t:"code",title:"The canonical search pipeline",code:`queryFlow
    .debounce(300)                 // wait for typing to pause
    .distinctUntilChanged()        // ignore no-op changes
    .filter { it.length >= 2 }     // don't search 1 char
    .flatMapLatest { q -> repository.search(q) }  // cancel old search
    .collect { render(it) }`}]},{heading:"Combining flows: zip, combine, merge",blocks:[{t:"p",text:"Three ways to bring multiple flows together — the differences are heavily tested because they behave subtly differently:"},{t:"table",headers:["Operator","Emits when","Pairing behavior"],rows:[["**`combine`**","ANY input emits (after all have emitted once)","latest value of each — recomputes on every change"],["**`zip`**","each input has a NEW value to pair","strict 1:1 pairing, waits for both; stops at the shorter"],["**`merge`**","any input emits","interleaves values from all into one stream (no pairing)"]]},{t:"code",title:"combine vs zip",code:`// combine: recompute UI state whenever ANY source changes
combine(userFlow, settingsFlow, cartFlow) { user, settings, cart ->
    HomeUiState(user, settings, cart.itemCount)
}.collect { render(it) }

// zip: pair values 1:1 (e.g. request with its matching response index)
flowOf(1, 2, 3).zip(flowOf("a", "b", "c")) { n, s -> "$n$s" }
// -> 1a, 2b, 3c`},{t:"list",items:["**`combine`** is the one you use most in Android — it's how you build a single `UiState` from several independent streams (user + settings + cart), recomputing whenever *any* changes. Note: it only starts emitting after *every* input has emitted at least once (give each an initial value if you need earlier output).","**`zip`** is for strict lockstep pairing — less common in UI, useful when two streams' values correspond positionally.","**`merge`** flattens several flows of the same type into one interleaved stream (e.g. merge events from several sources)."]}]},{heading:"Flattening operators: flatMapConcat, flatMapMerge, flatMapLatest",blocks:[{t:"p",text:"Sometimes each value in a flow *produces another flow* (a userId → a flow of that user's posts). You then have a `Flow<Flow<T>>` that must be flattened into `Flow<T>`. The three flatMap operators differ in **how they handle a new inner flow arriving while a previous one is still active** — the single most important distinction in this topic:"},{t:"table",headers:["Operator","When a new value arrives while inner flow is running","Use for"],rows:[["**`flatMapConcat`**","queues — waits for the current inner flow to finish first","order matters, process sequentially"],["**`flatMapMerge`**","runs concurrently — both inner flows active at once","parallel work, order doesn't matter"],["**`flatMapLatest`**","cancels the current inner flow, starts the new one","only the latest matters (search, latest-selection)"]]},{t:"code",title:"flatMapLatest — cancel stale work",code:`selectedUserId
    .flatMapLatest { id ->
        // if the user selects a different id, the in-flight
        // observation for the old id is CANCELLED
        repository.observeUserPosts(id)
    }
    .collect { render(it) }`},{t:"list",items:["**`flatMapLatest`** is the flow-pipeline equivalent of `collectLatest` — the go-to for search-as-you-type and 'react to the latest selection', because it cancels the previous inner flow (killing the stale-result race).","**`flatMapConcat`** preserves order and runs one inner flow at a time — use when sequencing matters (process each item's sub-stream fully before the next).","**`flatMapMerge`** runs inner flows concurrently (with a `concurrency` limit) — for parallel fan-out where order is irrelevant. Beware unbounded concurrency on large upstreams.","Mnemonic: **Concat = queue, Merge = parallel, Latest = cancel-and-replace.**"]}]},{heading:"Converting to and from other types",blocks:[{t:"list",items:["**`flow.asLiveData()`** — bridge a Flow to LiveData for legacy code (lifecycle-aware). Rarely needed in new code.","**`liveData.asFlow()`**, **`Observable.asFlow()`** (RxJava) — bring legacy reactive types into the Flow world during migration.","**`callbackFlow`/`channelFlow`** — from callbacks to flows (covered in the callbackFlow topic).","**`stateIn` / `shareIn`** — cold Flow → hot StateFlow/SharedFlow (covered in the hot flows topic).","**`flow.first()` / `flow.toList()`** — collapse a flow to a single value / a list (terminal)."]},{t:"note",text:"The three questions this topic must let you answer cold: (1) map vs transform (1:1 vs 0..n); (2) combine vs zip (latest-of-each on any change vs strict 1:1 pairing); (3) flatMapConcat vs Merge vs Latest (queue vs parallel vs cancel). Those three comparisons cover the majority of Flow-operator interview questions."}]}],WS=[{level:"junior",q:"What is the difference between map and transform?",a:[{t:"p",text:"**The concept**: both transform values flowing through a pipeline, but they differ in how many values they can produce per input. `map { }` is strictly **one-to-one** — each upstream value becomes exactly one downstream value. `transform { }` is the general form: for each upstream value you can call `emit` **zero, one, or many** times, producing any number of downstream values."},{t:"code",title:"map is 1:1, transform is 1:many (or 1:0)",code:`flowOf(1, 2).map { it * 10 }          // 10, 20  (one out per one in)

flowOf(1, 2).transform { n ->
    emit(n)                              // pass through
    emit(n * 10)                         // AND an extra value
}                                        // 1, 10, 2, 20`},{t:"p",text:"In fact `map` and `filter` are both implemented on top of `transform` — `map` just emits once, `filter` emits zero-or-one based on the predicate. You reach for `transform` directly only when the 1:1 shape doesn't fit: emitting a loading placeholder before a value, expanding one value into several, or conditionally emitting a variable number. For everyday 'change each value', `map` is clearer."}]},{level:"junior",q:"What does debounce do, and how is it different from sample?",a:[{t:"p",text:"**`debounce(ms)`** emits a value only after the flow has been *silent* for `ms` — every new value resets the timer. It's the 'wait until they stop' operator: in a search box, `debounce(300)` means you only search after the user pauses typing for 300ms, so you don't fire a request per keystroke."},{t:"p",text:"**`sample(ms)`** is different: it emits the *most recent* value on a fixed periodic tick, every `ms`, regardless of whether emissions are ongoing. Debounce reacts to *pauses*; sample takes *regular snapshots*. Example contrast: for a fast-moving sensor emitting continuously, `debounce` might emit *nothing* (there's never a pause) while `sample(1000)` gives you the latest reading once a second. Rule of thumb: use `debounce` to wait out bursts of activity (typing, rapid clicks); use `sample` to throttle a continuous stream to a manageable rate."}]},{level:"junior",q:"What does distinctUntilChanged do and why is it useful?",a:[{t:"p",text:"**The concept**: `distinctUntilChanged()` suppresses *consecutive* duplicate values — it only lets a value through if it's different from the one immediately before it (by `equals`). Note 'consecutive': `1,1,2,2,1` becomes `1,2,1` (the final 1 passes because the value right before it was 2). It's not global deduplication."},{t:"p",text:"**Why it's useful**: it prevents redundant downstream work when a source re-emits the same value. A common case: a flow derived with `map` might produce the same result for several different upstream values (e.g. mapping user objects to just their `isPremium` flag), and you don't want to re-render or re-query when nothing meaningful changed. `distinctUntilChangedBy { it.id }` compares by a key instead of the whole object. Worth knowing: `StateFlow` already has this behavior built in (it never emits a value equal to the current one), so you add `distinctUntilChanged` mainly on plain cold flows or after a `map` that collapses distinctions."}]},{level:"junior",q:"How do you combine two flows into one? When would you use combine?",a:[{t:"p",text:"**The concept**: `combine` merges multiple flows by taking the *latest value of each* and running your lambda whenever *any* of them emits. It's how you compose one derived value out of several independent streams. The most common Android use is building a single `UiState` from separate sources:"},{t:"code",title:"combine to build UI state from several streams",code:`combine(
    userRepository.observeUser(),
    settingsRepository.observeSettings(),
    cartRepository.observeCart(),
) { user, settings, cart ->
    HomeUiState(name = user.name, darkMode = settings.dark, cartCount = cart.size)
}.collect { render(it) }`},{t:"p",text:"So whenever the user, settings, *or* cart changes, `combine` recomputes `HomeUiState` with the latest of all three. Two things to remember: it only starts emitting once *every* input has emitted at least once (so give sources an initial value if you need output before all have fired), and it always uses the newest value of each — it doesn't pair them up. That last point is what distinguishes it from `zip`, which pairs values one-to-one and waits for a fresh value from *both* before emitting."}]},{level:"junior",q:"What does onEach do, and how is it different from map?",a:[{t:"p",text:"**`onEach { }`** runs a side effect for each value and passes the value through **unchanged** — the flow's type and values are identical before and after. **`map { }`** *transforms* each value into a (possibly different) new value, changing the stream. So `onEach` is for doing something *with* each value (logging, analytics, triggering an update) without altering the stream, while `map` is for *producing* new values."},{t:"p",text:"Two practical notes: `onEach` is excellent for debugging a pipeline — drop `.onEach { println(it) }` anywhere to peek at values at that stage without disturbing anything. And `onEach` combined with `launchIn(scope)` is a very common idiom: `flow.onEach { updateState(it) }.launchIn(viewModelScope)` starts collecting in a scope and handles each value, equivalent to `viewModelScope.launch { flow.collect { updateState(it) } }` but flatter."}]},{level:"senior",q:"Explain flatMapConcat, flatMapMerge, and flatMapLatest. When do you use each?",a:[{t:"p",text:"**The setup**: you use a flatMap operator when each value in a flow maps to *another flow* (a userId → a flow of that user's data), producing a `Flow<Flow<T>>` you need flattened to `Flow<T>`. The three variants differ entirely in **what happens when a new value arrives while the previous inner flow is still running** — that's the whole distinction."},{t:"list",items:["**`flatMapConcat`** — *sequential/queued*. It fully collects the current inner flow before starting the next. New upstream values wait their turn. Use when **order matters** and inner flows must not overlap — e.g. processing a queue of operations where each must complete before the next.","**`flatMapMerge`** — *concurrent*. It starts inner flows as values arrive and runs them **simultaneously**, interleaving their emissions (with an optional `concurrency` limit). Use for **parallel fan-out where order is irrelevant** — e.g. kicking off several independent fetches at once. Watch out: unbounded concurrency over a large upstream can launch too many parallel operations.","**`flatMapLatest`** — *cancel-and-replace*. When a new value arrives, it **cancels the current inner flow** and starts a fresh one for the new value. Use when **only the latest matters** — search-as-you-type, reacting to the latest selected item. It kills the stale-result race where an old, slow inner flow finishes after a newer one and overwrites correct data."]},{t:"code",title:"The definitive example — search",code:`queryFlow
    .debounce(300)
    .flatMapLatest { query ->            // old search cancelled on new query
        repository.search(query)         // returns Flow<Results>
    }
    .collect { showResults(it) }`},{t:"p",text:"**Mnemonic**: Concat = queue (one at a time, ordered), Merge = parallel (all at once, unordered), Latest = cancel-and-replace (only the newest). `flatMapLatest` is by far the most common in UI code; `flatMapConcat` when sequencing is required; `flatMapMerge` for deliberate parallelism."}]},{level:"senior",q:"Deep-dive combine vs zip vs merge — the exact semantics and a pitfall of each.",a:[{t:"list",items:["**`combine`** — emits whenever *any* input emits, using the *latest* value of *every* input. It waits until all inputs have emitted at least once before the first emission. **Pitfall**: because it uses latest-of-each, rapid emissions on one input can produce combinations you didn't 'intend' pairing-wise, and if one input never emits, `combine` never emits at all (a flow that's slow to produce its first value blocks all output — give it an initial/`onStart` value). It's the right tool for deriving UI state, wrong for strict pairing.","**`zip`** — emits only when it can form a *new pair*: it takes the next value from each input and pairs them 1:1, waiting for both. **Pitfall**: it advances in lockstep and **completes when the shortest flow completes**, so mismatched-length or mismatched-rate flows leave values unpaired and dropped. It's for positional correspondence (value #n of A with value #n of B), not for reacting to whichever changed.","**`merge`** — takes several flows of the *same type* and **interleaves** all their values into one stream, no pairing, no transformation, emitting each value as it arrives from any source. **Pitfall**: you lose track of which source a value came from (wrap values in a sealed type if you need to know), and a fast source can dominate the merged stream."]},{t:"p",text:"**The one-line differentiator**: `combine` = 'latest of each, recompute on any change' (for derived state); `zip` = 'strict 1:1 pairing, waits for both, stops at shorter' (for positional pairing); `merge` = 'flatten many into one interleaved stream' (for pooling same-typed events). The interview trap is using `combine` when you meant `zip` (getting extra recompositions) or `zip` when you meant `combine` (getting stuck waiting for a slow input)."}]},{level:"senior",q:"Build a debounced, cancel-stale search pipeline with flow operators and explain each operator's role.",a:[{t:"code",title:"The production-grade search pipeline",code:`val results: StateFlow<SearchUiState> = queryFlow
    .debounce(300)                          // 1
    .distinctUntilChanged()                 // 2
    .flatMapLatest { query ->               // 3
        if (query.length < 2) flowOf(SearchUiState.Empty)
        else repository.search(query)       // returns Flow<Results>
            .map { SearchUiState.Results(it) }
            .onStart { emit(SearchUiState.Loading) }   // 4
            .catch { emit(SearchUiState.Error(it.message)) } // 5
    }
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), SearchUiState.Empty) // 6`},{t:"list",items:["**1 — `debounce(300)`**: waits until the user pauses typing, so you issue at most one search per burst of keystrokes instead of one per letter. The single biggest reduction in wasted requests.","**2 — `distinctUntilChanged`**: if the debounced query equals the last one searched (e.g. they typed and deleted a character), skip re-searching — no redundant work.","**3 — `flatMapLatest`**: the crux. When a new query arrives, it **cancels the in-flight search** for the old query. This eliminates the stale-result race (a slow old response arriving after a newer one) *and* saves the abandoned request's remaining work.","**4 — `onStart { emit(Loading) }`**: emits a loading state as each new search begins, so the UI shows a spinner immediately.","**5 — `catch` inside the inner flow**: placing `catch` on the *inner* flow means an error in one search maps to an error state without tearing down the whole outer pipeline — the next keystroke can still search. A `catch` on the outer flow would end everything permanently after the first error.","**6 — `stateIn(WhileSubscribed(5000))`**: converts the cold pipeline into a hot `StateFlow` the UI observes, keeping it alive across configuration changes (the 5s window) but stopping the upstream when the screen is truly gone."]},{t:"p",text:"The senior signal is (a) reaching for `flatMapLatest` specifically for cancellation, (b) placing `catch` on the *inner* flow so errors are recoverable, and (c) knowing why `debounce` + `distinctUntilChanged` come *before* the flatMap (filter the query stream before spending a request). Each operator maps to a concrete failure it prevents — wasted requests, stale results, unrecoverable errors, lost state on rotation."}]},{level:"senior",q:"What is mapLatest, and how does it differ from map?",a:[{t:"p",text:"`map` transforms every value and waits for each transformation to finish before the next. `mapLatest` *cancels* the in-progress transformation when a new value arrives and restarts with the newest — so if a value's mapping is slow (a suspend call) and a newer value comes, the stale one is abandoned. It's the transform-side equivalent of `collectLatest`."},{t:"code",title:"map vs mapLatest",code:`// map: awaits each transform fully
query.map { slowLookup(it) }        // every query's lookup completes

// mapLatest: cancels the previous transform when a new value arrives
query.mapLatest { slowLookup(it) }  // only the latest query's lookup survives`},{t:"list",items:["**`map`** — 1→1, awaits each; no cancellation of prior work.","**`mapLatest`** — cancels the previous (suspending) transform when a newer value arrives.","**Use `mapLatest`** — search/lookup where only the latest input matters.","**Related** — `transformLatest`, `flatMapLatest`, `collectLatest` share the cancel-on-new behavior."]},{t:"note",text:"map transforms every value, awaiting each; mapLatest cancels the in-progress (suspending) transform when a newer value arrives and restarts with the latest — the transform-side collectLatest. Use mapLatest for search/lookup where only the newest input matters; the *Latest family all cancel-and-restart."}]},{level:"junior",q:"What do mapNotNull, filterNotNull, and filterIsInstance do?",a:[{t:"p",text:"These combine transformation/filtering with type or null handling. `mapNotNull { }` transforms and drops results that are `null`; `filterNotNull()` drops null emissions; `filterIsInstance<T>()` keeps only emissions of a given type (and smart-casts them). They're concise ways to clean or narrow a stream."},{t:"code",title:"Cleaning a stream",code:`flow.mapNotNull { parseOrNull(it) }        // transform, drop nulls
flow.filterNotNull()                        // drop null values
events.filterIsInstance<ClickEvent>()       // keep only ClickEvents (typed)`},{t:"list",items:["**`mapNotNull { }`** — map, then drop `null` results in one step.","**`filterNotNull()`** — remove null emissions from a `Flow<T?>` → `Flow<T>`.","**`filterIsInstance<T>()`** — keep only values of type `T`, typed as `T` downstream.","**Use** — narrowing a heterogeneous event flow, dropping unparseable items."]},{t:"note",text:"mapNotNull { } maps and drops null results; filterNotNull() removes nulls (Flow<T?> → Flow<T>); filterIsInstance<T>() keeps only values of a type (smart-cast downstream). Concise ways to clean/narrow a stream — e.g. drop unparseable items or filter a heterogeneous event flow to one type."}]},{level:"senior",q:"How does zip pair values, and what happens with different-length flows?",a:[{t:"p",text:"`zip` pairs the *nth* emission of one flow with the *nth* emission of another — it waits for both to emit before producing a combined value. When the flows have different lengths, `zip` completes as soon as *either* flow completes, so extra values from the longer flow are dropped. This lockstep pairing differs from `combine`, which emits on *any* change using the latest of each."},{t:"code",title:"zip pairs by index",code:`val letters = flowOf("a", "b", "c")
val numbers = flowOf(1, 2)          // shorter
letters.zip(numbers) { l, n -> "$l$n" }
    .collect { println(it) }         // a1, b2 — "c" dropped (numbers completed)`},{t:"list",items:["**Index-paired** — nth with nth; waits for both to emit each pair.","**Completes with the shorter** — stops when either flow ends; extras dropped.","**vs `combine`** — combine emits on any source change using the latest of each (not paired).","**Use `zip`** — when values genuinely correspond positionally (two parallel sequences)."]},{t:"note",text:"zip pairs nth-with-nth, waiting for both flows to emit each pair; it completes when EITHER flow ends, dropping the longer flow's extras. Contrast combine (emits on any change using the latest of each). Use zip for positionally-corresponding sequences; combine for 'latest of each' state."}]},{level:"senior",q:"How do you combine three or more flows, and what is combineTransform?",a:[{t:"p",text:"`combine` has overloads for multiple flows — `combine(a, b, c) { av, bv, cv -> ... }` — emitting whenever *any* source emits, using the latest of each. `combineTransform` is the flexible version that gives you an `emit` so you can produce zero, one, or many outputs per combination (e.g. emit a loading state then a result)."},{t:"code",title:"combine N flows and combineTransform",code:`combine(user, settings, notifications) { u, s, n -> ScreenState(u, s, n) }
    .collect { render(it) }

combineTransform(a, b) { av, bv ->
    emit(Loading)
    emit(compute(av, bv))       // multiple emissions per combination
}`},{t:"list",items:["**`combine(a,b,c,...)`** — up to several flows; emits latest-of-each on any change.","**List overload** — `combine(flows) { array -> }` for a dynamic number of flows.","**`combineTransform`** — emit 0/1/many per combination (interstitial states, filtering).","**Common use** — assembling a screen's `UiState` from several independent streams."]},{t:"note",text:"combine(a, b, c) { … } combines multiple flows, emitting latest-of-each on any change (there's also combine(listOfFlows) { array }). combineTransform gives an emit for 0/1/many outputs per combination (e.g. Loading then result). Classic use: build a screen's UiState from several independent streams."}]},{level:"senior",q:"What is transformLatest, and when would you use it?",a:[{t:"p",text:"`transformLatest { }` is like `transform` (you `emit` freely) but *cancels* the previous transform block when a new upstream value arrives — combining flexible emission with cancel-on-new semantics. Use it when each input triggers a multi-step or long-running emission process that should be abandoned if a newer input comes."},{t:"code",title:"transformLatest",code:`queryFlow.transformLatest { query ->
    emit(SearchState.Loading)          // show loading immediately
    delay(300)                          // debounce-ish (cancelled if new query)
    emit(SearchState.Results(search(query)))   // abandoned if a newer query arrives
}`},{t:"list",items:["**Flexible + cancel-on-new** — `emit` any number of values, but a new upstream value cancels the block.","**Use** — search flows that emit Loading then Results, where stale searches must be dropped.","**Family** — alongside `mapLatest`/`flatMapLatest`/`collectLatest`.","**Avoids stale UI** — only the latest input's emissions survive."]},{t:"note",text:"transformLatest { } = transform (emit freely) + cancel-on-new (a new upstream value cancels the current block). Use it when each input drives a multi-step emission (Loading → Results) that should be abandoned if a newer input arrives — e.g. a search that shows loading then results, dropping stale searches."}]},{level:"junior",q:"What are runningReduce and runningFold, and how do they relate to scan?",a:[{t:"p",text:"`runningFold(initial) { acc, value -> }` is the same as `scan` — it emits each intermediate accumulated value (starting with the initial). `runningReduce { acc, value -> }` is like it but without an initial value (the first emission is the first element). They produce a *stream of accumulations* rather than a single final result."},{t:"code",title:"Running accumulations",code:`flowOf(1, 2, 3, 4).runningReduce { acc, x -> acc + x }
    .collect { println(it) }     // 1, 3, 6, 10

flowOf(1, 2, 3).runningFold(100) { acc, x -> acc + x }
    .collect { println(it) }     // 100, 101, 103, 106 (same as scan)`},{t:"list",items:["**`runningFold(initial)` = `scan`** — emits each running accumulation from the seed.","**`runningReduce`** — no seed; first emission is the first value.","**Stream of states** — great for running totals, accumulating lists, building state from events.","**vs `reduce`/`fold`** — those are terminal (one result); these are intermediate (a stream)."]},{t:"note",text:"runningFold(initial) is identical to scan — emits each running accumulation from the seed. runningReduce has no seed (first emission = first value). Both are INTERMEDIATE (stream of accumulations) vs terminal reduce/fold (one result). Use for running totals, accumulating lists, or building state from an event stream."}]},{level:"junior",q:"How do you get the index of each emission (withIndex)?",a:[{t:"p",text:"`withIndex()` wraps each emission in an `IndexedValue(index, value)`, giving you a zero-based position alongside the value — useful for numbering, alternating logic, or skipping the first emission by index."},{t:"code",title:"withIndex",code:`flow.withIndex().collect { (index, value) ->
    if (index == 0) initialSetup(value) else handle(value)
}`},{t:"list",items:["**`withIndex()`** — emits `IndexedValue(index, value)` with a 0-based index.","**Uses** — numbering, first-vs-rest logic, alternating behavior.","**Destructure** — `collect { (i, v) -> }` for clean access.","**Alternative** — `runningReduce`/an external counter for more complex stateful indexing."]},{t:"note",text:"withIndex() wraps emissions in IndexedValue(index, value) (0-based), destructurable as collect { (i, v) -> }. Use for numbering, first-vs-rest logic, or index-based skipping. For richer stateful counting, use runningReduce or an external counter."}]},{level:"junior",q:"What does onEmpty do, and how does it differ from onStart?",a:[{t:"p",text:"`onStart { }` runs *before* the first emission (always, even if the flow is empty) — good for emitting an initial/loading value. `onEmpty { }` runs *only if the flow completes without emitting anything* — good for supplying a fallback/empty-state value when there was no data."},{t:"code",title:"onStart vs onEmpty",code:`flow
    .onStart { emit(UiState.Loading) }    // always, before upstream
    .onEmpty { emit(UiState.Empty) }      // only if upstream emitted nothing
    .collect { render(it) }`},{t:"list",items:["**`onStart { }`** — runs before the first upstream emission (loading state, logging start).","**`onEmpty { }`** — runs only when the flow completes with zero emissions (empty-state fallback).","**`onCompletion { }`** — runs when the flow finishes (success, error, or cancellation) with the cause.","**Together** — model Loading (onStart) → Content (upstream) / Empty (onEmpty)."]},{t:"note",text:"onStart { } runs before the first emission (always — loading state); onEmpty { } runs only if the flow completes with zero emissions (empty-state fallback); onCompletion { cause } runs on any finish. Combine: onStart emits Loading, upstream emits Content, onEmpty emits Empty."}]},{level:"senior",q:"How do you add a timeout to a Flow?",a:[{t:"p",text:"Use the `timeout(duration)` operator, which throws a `TimeoutCancellationException` if the flow doesn't emit within the given time between emissions (or before the first). Combine it with `catch` to convert the timeout into a fallback emission. It bounds how long you'll wait for the next value from a slow or stalled source."},{t:"code",title:"Flow timeout with fallback",code:`flow
    .timeout(5.seconds)                    // throws if no emission within 5s
    .catch { e -> if (e is TimeoutCancellationException) emit(cached) else throw e }
    .collect { render(it) }`},{t:"list",items:["**`timeout(duration)`** — errors if the gap between emissions (or to the first) exceeds the limit.","**Pairs with `catch`** — turn a timeout into a fallback value or an error state.","**Per-emission** — it's a between-emissions timeout, not a total-duration cap.","**Alternative** — `withTimeout` around the whole collection for a total bound."]},{t:"note",text:"Flow.timeout(duration) throws TimeoutCancellationException if no emission arrives within the limit (between emissions or before the first); pair with catch to emit a fallback/error state. It's a between-emissions timeout — for a total-duration cap, wrap the whole collect in withTimeout."}]},{level:"junior",q:"What is distinctUntilChangedBy, and when do you use it?",a:[{t:"p",text:"`distinctUntilChanged()` skips consecutive duplicate emissions (by `equals`). `distinctUntilChangedBy { selector }` skips consecutive emissions that have the same *selected key* — so you can dedupe based on one field (e.g. ignore updates unless the `id` changed), even if other fields differ."},{t:"code",title:"Dedupe by a key",code:`userFlow.distinctUntilChangedBy { it.id }        // only emit when id changes
locationFlow.distinctUntilChanged { a, b -> a.roughlyEquals(b) }  // custom equality`},{t:"list",items:["**`distinctUntilChanged()`** — skip consecutive `equals` duplicates.","**`distinctUntilChangedBy { key }`** — skip when the selected key is unchanged.","**Custom comparator** — `distinctUntilChanged { old, new -> ... }` for bespoke equality.","**Uses** — avoid redundant UI updates/network calls when only irrelevant fields changed."]},{t:"note",text:"distinctUntilChanged() skips consecutive equals-duplicates; distinctUntilChangedBy { key } skips when a selected field is unchanged (even if others differ); distinctUntilChanged { old, new -> } uses custom equality. Use to avoid redundant UI updates/network calls when only irrelevant fields changed."}]},{level:"senior",q:"How do you flatten a Flow<Flow<T>>?",a:[{t:"p",text:"Use the flattening operators: `flattenConcat()` (sequential — collect each inner flow fully before the next), `flattenMerge(concurrency)` (concurrent — collect multiple inner flows at once), or the `flatMap*` operators when you also transform. These handle a flow whose values are themselves flows (nested streams)."},{t:"code",title:"Flattening nested flows",code:`flowOfFlows.flattenConcat()          // sequential
flowOfFlows.flattenMerge(concurrency = 4)   // up to 4 concurrent inner flows
// With transform, use flatMapConcat/Merge/Latest instead:
ids.flatMapMerge { id -> fetchFlow(id) }`},{t:"list",items:["**`flattenConcat()`** — one inner flow at a time, in order.","**`flattenMerge(n)`** — up to `n` inner flows concurrently (order not guaranteed).","**`flatMapConcat`/`flatMapMerge`/`flatMapLatest`** — flatten *and* transform each value into a flow.","**Choose by semantics** — order (Concat), throughput (Merge), latest-only (Latest)."]},{t:"note",text:"Flatten a Flow<Flow<T>> with flattenConcat() (sequential, ordered), flattenMerge(concurrency) (concurrent, unordered), or the flatMap* operators when transforming a value into a flow. Choose by semantics: Concat (order), Merge (throughput), Latest (cancel stale, latest only)."}]},{level:"senior",q:"How do debounce, sample, and throttle differ for high-frequency streams?",a:[{t:"p",text:"All three reduce a rapid stream, but differently. `debounce(t)` emits a value only after the stream has been *quiet* for `t` (waits for a pause) — ideal for search-as-you-type. `sample(t)` emits the *latest* value every `t` regardless of pauses — ideal for periodic snapshots of a continuous stream. There's no built-in `throttleFirst`, but the concept (emit the first, then ignore for `t`) is implemented manually or via libraries."},{t:"table",headers:["Operator","Emits","Use"],rows:[["debounce(t)","after t of silence","search input (wait for typing pause)"],["sample(t)","latest value every t","periodic snapshot of a fast stream"],["throttleFirst (manual)","first, then ignore for t","prevent double-clicks"]]},{t:"list",items:["**`debounce`** — waits for a pause; drops rapid intermediate values; good for text input.","**`sample`** — time-sliced; emits the most recent value each interval; good for continuous sensors/scroll.","**throttle-first** — emit immediately, then ignore for a window; good for click de-bounce (not built-in; roll your own).","**Pick by intent** — 'wait until they stop' (debounce) vs 'check every N ms' (sample)."]},{t:"note",text:"debounce(t) emits after t of silence (wait for a pause — search input); sample(t) emits the latest value every t regardless of pauses (periodic snapshot — sensors/scroll); throttle-first (manual) emits the first then ignores for t (double-click guard). Pick by 'wait until they stop' vs 'check every N ms'."}]},{level:"senior",q:"How do you access the previous emission alongside the current one?",a:[{t:"p",text:"Use `runningReduce`/`scan` to carry a pair, or `zip` the flow with itself offset by one. The common idiom is `scan(null to null) { acc, value -> acc.second to value }` producing `(previous, current)` pairs, letting you compare consecutive emissions (deltas, transitions)."},{t:"code",title:"Previous + current pairs",code:`flow
    .scan<Int, Pair<Int?, Int>>(null to 0) { (_, prev), curr -> prev to curr }
    .drop(1)     // drop the seed
    .collect { (prev, curr) -> println("changed $prev -> $curr") }`},{t:"list",items:["**`scan`/`runningReduce`** — carry the previous value in the accumulator, emit `(prev, curr)`.","**`drop(1)`** — discard the seed pair if it's not meaningful.","**Uses** — computing deltas, detecting transitions (e.g. state A→B), diffing consecutive states.","**`zip` self-offset** — an alternative for pairing element n with n+1."]},{t:"note",text:"Carry the previous value with scan/runningReduce, emitting (previous, current) pairs (drop the seed) — for deltas, transitions, or diffing consecutive emissions. scan(prev to curr) { (_, p), c -> p to c } is the idiom; zipping the flow with itself offset by one is an alternative."}]},{level:"junior",q:"How do you pace or add a delay between emissions?",a:[{t:"p",text:"For a flow you build, put `delay` between `emit` calls in the builder. For an existing flow, use `onEach { delay(t) }` to space out emissions. Note that in a sequential flow this delays the whole pipeline (producer and collector share a coroutine), which is usually what you want for pacing."},{t:"code",title:"Spacing emissions",code:`// In a builder
flow { items.forEach { emit(it); delay(200) } }
// On an existing flow
sourceFlow.onEach { delay(200) }.collect { render(it) }`},{t:"list",items:["**`delay` in the builder** — between emits for a paced producer.","**`onEach { delay(t) }`** — space out an existing flow's emissions.","**Sequential effect** — delaying holds the whole pipeline (natural for pacing/animation).","**Cancellable** — `delay` respects cancellation, so pacing stops when the collector is cancelled."]},{t:"note",text:"Pace a flow with delay between emit calls in the builder, or onEach { delay(t) } on an existing flow. In a sequential flow this holds the whole pipeline (natural for pacing/reveal animations). delay is cancellable, so pacing stops when the collector is cancelled."}]},{level:"senior",q:"What is transformWhile, and how do you stop a flow conditionally?",a:[{t:"p",text:"`transformWhile { }` emits values and returns a `Boolean` deciding whether to *continue* — when you return `false`, the flow stops (after optionally emitting). It's like `takeWhile` but with the flexibility of `transform` (you control emission and the stop condition together). Use it to consume a stream until a sentinel/condition and then complete."},{t:"code",title:"Stop on a condition",code:`flow.transformWhile { value ->
    emit(value)
    value != SENTINEL          // continue while not the sentinel; stop after emitting it
}`},{t:"list",items:["**`transformWhile { }`** — emit as needed, return `false` to stop the flow.","**vs `takeWhile`** — `takeWhile` stops *before* emitting the failing value; `transformWhile` lets you emit it then stop.","**Uses** — read until a terminator, stop after a completion marker.","**Completes upstream** — returning `false` cancels the upstream flow."]},{t:"note",text:"transformWhile { emit(...); continueBoolean } emits freely and stops when you return false — like takeWhile but you control emission and the stop together (e.g. emit the sentinel THEN stop, which takeWhile can't). Returning false completes/cancels upstream. Use to consume until a terminator."}]},{level:"senior",q:"What does the concurrency parameter of flatMapMerge control?",a:[{t:"p",text:"`flatMapMerge(concurrency = N)` limits how many inner flows are collected *concurrently* — the default is `DEFAULT_CONCURRENCY` (16). It lets you run multiple transformations in parallel (e.g. fetch details for many ids at once) while bounding the parallelism so you don't overwhelm the network/server."},{t:"code",title:"Bounded parallel fetches",code:`ids.asFlow()
    .flatMapMerge(concurrency = 4) { id -> flow { emit(api.getDetails(id)) } }
    .collect { store(it) }   // up to 4 concurrent detail fetches`},{t:"list",items:["**`concurrency`** — max inner flows collected at once (default 16).","**Parallelism with a cap** — run many fetches in parallel without unbounded load.","**Order not preserved** — merged results arrive as they complete (use `flatMapConcat` for order).","**Tune it** — match the server's rate limits / device capacity."]},{t:"note",text:"flatMapMerge(concurrency = N) caps how many inner flows collect concurrently (default 16) — parallel transformations (e.g. fetch details for many ids) with a bound so you don't overwhelm the server. Results arrive out of order (use flatMapConcat to preserve order). Tune N to rate limits/capacity."}]},{level:"senior",q:"How do you switch to a different flow whenever a trigger changes (flatMapLatest)?",a:[{t:"p",text:"`flatMapLatest { }` maps each upstream value to a *new flow* and collects it, cancelling the previous inner flow when a new upstream value arrives. This is the pattern for 'when the input changes, switch to a fresh stream' — e.g. a selected filter driving a database query, where changing the filter cancels the old query and starts a new one."},{t:"code",title:"Trigger-driven switching",code:`selectedCategory
    .flatMapLatest { category ->
        repository.observeItems(category)   // new DB flow per category; old one cancelled
    }
    .collect { render(it) }`},{t:"list",items:["**`flatMapLatest`** — each upstream value → a new inner flow; the previous is cancelled on change.","**Uses** — filter/tab/query changes that should re-subscribe to a new source.","**No stale data** — the old stream is abandoned, so you never see results for a previous selection.","**Search** — combined with `debounce` for search-as-you-type over a query flow."]},{t:"note",text:"flatMapLatest { } maps each upstream value to a new inner flow and cancels the previous when a new value arrives — the 'input changed, switch streams' pattern (filter → new DB query, tab → new source). No stale data since the old stream is abandoned. With debounce it powers search-as-you-type."}]},{level:"senior",q:"When do you use merge versus combine for independent streams?",a:[{t:"p",text:"`merge` *interleaves* multiple flows of the *same type* into one, forwarding each emission as it arrives — you get a unified stream of events. `combine` produces a *tuple of the latest value from each* flow, emitting whenever any changes — you get combined state. Use `merge` to funnel several event sources together; use `combine` to derive state from multiple inputs."},{t:"code",title:"merge vs combine",code:`// merge: unify event sources (same type)
merge(clicks, swipes, keyEvents).collect { handleEvent(it) }

// combine: latest-of-each state
combine(query, filters) { q, f -> Search(q, f) }.collect { runSearch(it) }`},{t:"list",items:["**`merge`** — interleave same-typed flows into one event stream; each emission forwarded as-is.","**`combine`** — emit the latest of each source together, on any change (state derivation).","**Type** — `merge` needs a common type; `combine` mixes types into a tuple/object.","**Intent** — funnel events (merge) vs compute combined state (combine)."]},{t:"note",text:"merge interleaves same-typed flows into one event stream (each emission forwarded) — funnel event sources. combine emits the latest of each flow together on any change (mixes types) — derive combined state. Merge for events, combine for state."}]},{level:"senior",q:"How do you assemble a screen's UiState from several independent flows?",a:[{t:"p",text:"Use `combine` to merge the latest values of each source flow into a single `UiState`, then `stateIn` it in the ViewModel for the UI to collect. Whenever any source changes, `combine` recomputes the state, giving a single reactive source of truth composed from user data, settings, network status, etc."},{t:"code",title:"Composed UiState",code:`val uiState: StateFlow<UiState> = combine(
    userRepo.user,
    settingsRepo.settings,
    connectivity.isOnline,
) { user, settings, online ->
    UiState(user, settings, online)
}.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), UiState.Loading)`},{t:"list",items:["**`combine` the sources** — latest-of-each into one `UiState` on any change.","**`stateIn`** — expose as a `StateFlow` with an initial Loading value.","**Single source of truth** — the UI collects one flow; all inputs feed it reactively.","**`WhileSubscribed`** — upstream active only while the UI observes (efficient)."]},{t:"note",text:"combine the source flows (user, settings, connectivity) into one UiState (recomputed on any change), then stateIn(viewModelScope, WhileSubscribed(5000), Loading) for the UI. One reactive source of truth composed from independent inputs; WhileSubscribed keeps upstream active only while observed."}]},{level:"senior",q:"How do the flattening strategies map to real UI scenarios?",a:[{t:"p",text:"The three `flatMap*` operators each fit different real-world needs, decided by what should happen to in-flight work when a new value arrives. Picking the wrong one causes stale results, dropped work, or ordering bugs."},{t:"table",headers:["Operator","On new value","Scenario"],rows:[["flatMapLatest","cancel previous inner","search-as-you-type (drop stale)"],["flatMapConcat","queue, run in order","sequential uploads / ordered ops"],["flatMapMerge","run concurrently","parallel independent fetches"]]},{t:"list",items:["**`flatMapLatest`** — search, filters, any 'only the latest input matters' case.","**`flatMapConcat`** — ordered processing where each must finish before the next (upload queue, sequential steps).","**`flatMapMerge`** — independent parallel work (fetch many resources at once), optionally bounded by `concurrency`.","**Decide by** — cancel-stale (Latest), preserve-order (Concat), maximize-throughput (Merge)."]},{t:"note",text:"flatMapLatest → cancel stale (search-as-you-type); flatMapConcat → queue in order (sequential uploads/steps); flatMapMerge → run concurrently (parallel fetches, bound with concurrency). Pick by what happens to in-flight work: cancel-stale, preserve-order, or maximize-throughput. Wrong choice = stale/dropped/misordered results."}]},{level:"junior",q:"How do you prevent double-clicks or rapid duplicate actions with flow operators?",a:[{t:"p",text:"Model the clicks as a flow and apply a throttle-first behavior: emit the first click, then ignore subsequent ones for a short window. Since Flow has no built-in `throttleFirst`, you either implement it (track the last-accepted time) or use `debounce` if 'wait for the taps to stop' is acceptable. For a submit button, disabling it in state during the operation is often simpler and more reliable."},{t:"code",title:"Throttle-first for clicks",code:`fun <T> Flow<T>.throttleFirst(windowMs: Long): Flow<T> = flow {
    var last = 0L
    collect { value ->
        val now = System.currentTimeMillis()
        if (now - last >= windowMs) { last = now; emit(value) }
    }
}
clicks.throttleFirst(500).onEach { submit() }.launchIn(scope)`},{t:"list",items:["**Throttle-first** — accept the first, ignore for a window; ideal for buttons (act immediately, block repeats).","**`debounce`** — waits for a pause; wrong for buttons (delays the action).","**State-based guard** — often cleaner: set `isSubmitting = true` and disable the button until done.","**Combine** — throttle plus a disabled state for robustness."]},{t:"note",text:"Use throttle-first (accept first, ignore for a window) for buttons — implement it (track last-accepted time) since Flow lacks it built-in. debounce is wrong here (delays the action). Often simpler: guard with state (isSubmitting → disable the button until done). Combine both for robustness."}]},{level:"junior",q:"What is the difference between filter and takeWhile for stopping a stream?",a:[{t:"p",text:"`filter { }` *skips* non-matching values but keeps the flow going (it never stops the stream). `takeWhile { }` *stops* the flow as soon as the predicate is false. So `filter` is for selecting a subset across the whole stream; `takeWhile` is for consuming a prefix and then completing."},{t:"code",title:"filter vs takeWhile",code:`flowOf(1, 5, 2, 8, 3).filter { it < 5 }       // 1, 2, 3 (skips 5, 8; continues)
flowOf(1, 5, 2, 8, 3).takeWhile { it < 5 }    // 1 (stops at 5)`},{t:"list",items:["**`filter`** — drops non-matching values, keeps collecting the rest.","**`takeWhile`** — emits until the predicate fails, then completes/cancels upstream.","**Choose** — subset selection (filter) vs prefix-then-stop (takeWhile).","**`transformWhile`** — the flexible version of `takeWhile` (emit then decide to stop)."]},{t:"note",text:"filter skips non-matching values but never stops the flow (subset selection). takeWhile stops the flow the moment the predicate fails (prefix, then complete/cancel upstream). Use filter to select across the whole stream, takeWhile to consume a prefix. transformWhile is the flexible takeWhile."}]},{level:"senior",q:"How do you build a debounced, distinct, cancel-stale search pipeline?",a:[{t:"p",text:"Chain the operators that each solve one problem: `debounce` (wait for a typing pause), `distinctUntilChanged` (ignore no-op query changes), `filter` (skip too-short/blank queries), and `flatMapLatest` (cancel the previous search when a new query arrives). Together they produce a responsive, efficient search that never shows stale results."},{t:"code",title:"The full search pipeline",code:`queryFlow
    .debounce(300)                       // wait for typing to pause
    .distinctUntilChanged()              // ignore identical queries
    .filter { it.length >= 2 }           // skip trivial queries
    .flatMapLatest { q ->                // cancel prior search on new query
        repository.search(q)
            .catch { emit(SearchResult.Error) }   // per-search error handling
    }
    .collect { render(it) }`},{t:"list",items:["**`debounce(300)`** — one network call per typing pause, not per keystroke.","**`distinctUntilChanged`** — skip redundant identical queries.","**`filter`** — avoid searching blank/too-short input.","**`flatMapLatest`** — cancel the in-flight search when the query changes (no stale results).","**`catch` inside `flatMapLatest`** — so one failed search doesn't kill the whole pipeline."]},{t:"note",text:"Search pipeline: debounce(300) (per-pause, not per-keystroke) → distinctUntilChanged (skip identical) → filter (skip trivial) → flatMapLatest (cancel stale search on new query) → catch INSIDE flatMapLatest (a failed search doesn't kill the pipeline). Each operator solves one problem; together = responsive, efficient, no stale results."}]}],BS={id:"operators",title:"Operators & Transformations",tagline:"map/transform, debounce/sample/distinctUntilChanged, combine vs zip vs merge, flatMapConcat/Merge/Latest, and building real pipelines.",tags:["Flow","Operators","combine","flatMapLatest","debounce"],content:NS,qa:WS},zS=[{heading:"Hot flows — the opposite of cold",blocks:[{t:"p",text:"`StateFlow` and `SharedFlow` are **hot** flows. Recall cold flows run per-collector and only while collected. Hot flows are the opposite: they **exist independently of collectors** and **multicast** the same emissions to all current collectors. Emitting into a hot flow happens whether or not anyone is listening; collectors 'tune in' to a live broadcast rather than triggering their own private run."},{t:"list",items:["**One shared stream, many collectors**: ten collectors of a `StateFlow` all receive the same emissions from the one underlying stream — not ten independent executions.","**Emission is decoupled from collection**: you push values in (`_state.value = x`, `emit(event)`) from anywhere; collectors receive them if they're active.","**They don't complete on their own**: a `StateFlow`/`SharedFlow` never finishes (there's always potentially another value), so collecting one suspends forever until the collecting coroutine is cancelled.","**Use case**: representing *shared state* (UI state everyone observes) or *broadcasting events* to multiple listeners — things that conceptually exist regardless of who's watching."]}]},{heading:"StateFlow — a hot flow that always has a current value",blocks:[{t:"p",text:"**`StateFlow<T>`** is a hot flow specialized for **state**: it always holds exactly one *current value*, and new collectors immediately receive that current value, then every subsequent update. It's essentially an observable value holder — the modern replacement for `LiveData`."},{t:"code",title:"The canonical ViewModel pattern",code:`class ProfileViewModel : ViewModel() {
    private val _uiState = MutableStateFlow(ProfileUiState.Loading) // requires initial value
    val uiState: StateFlow<ProfileUiState> = _uiState.asStateFlow()  // read-only exposure

    fun load() {
        viewModelScope.launch {
            val user = repository.getUser()
            _uiState.update { it.copy(user = user, isLoading = false) } // atomic update
        }
    }
}

// Reading the current value synchronously (no collection):
val current = uiState.value`},{t:"list",items:["**Requires an initial value** — there's always a 'current' state, so `MutableStateFlow(initial)` must be given one. (`SharedFlow` has no initial value.)","**`.value`** reads/writes the current value synchronously — you can read state without collecting.","**Conflated + distinct**: `StateFlow` only emits when the new value is *different* (`!=`) from the current, and a slow collector only sees the *latest* value, not every intermediate one. It's about *state*, not every event.","**Always update immutably**: replace with `copy()` via `update { }` (atomic compare-and-set, safe against concurrent writers) rather than mutating the held object — mutating in place won't emit (same reference).","**Backing-property pattern**: private `MutableStateFlow`, public `StateFlow` via `asStateFlow()` — so only the ViewModel writes (single source of truth, UDF)."]}]},{heading:"SharedFlow — a configurable hot broadcast",blocks:[{t:"p",text:"**`SharedFlow<T>`** is the general hot flow: a broadcaster with no notion of a single 'current value', configurable via **replay**, **buffer**, and **overflow** policy. `StateFlow` is actually a specialized `SharedFlow` (replay=1, conflated, with an initial value)."},{t:"code",title:"Configuring a SharedFlow",code:`private val _events = MutableSharedFlow<UiEvent>(
    replay = 0,                                  // how many past values new collectors get
    extraBufferCapacity = 64,                    // buffer beyond replay
    onBufferOverflow = BufferOverflow.DROP_OLDEST, // what to do when buffer full
)
val events: SharedFlow<UiEvent> = _events.asSharedFlow()

suspend fun sendEvent(e: UiEvent) = _events.emit(e)   // suspends if buffer full (SUSPEND)
fun tryEvent(e: UiEvent) = _events.tryEmit(e)         // non-suspending; may drop`},{t:"list",items:["**`replay`** — how many of the most recent values a *new* collector receives on subscribing. `replay = 0` means new collectors only get values emitted *after* they subscribe (good for events); `replay = 1` mimics StateFlow-ish behavior (last value re-delivered).","**`extraBufferCapacity`** — buffer slots beyond replay, so `emit` doesn't suspend as long as there's room.","**`onBufferOverflow`** — `SUSPEND` (default; producer waits), `DROP_OLDEST`, or `DROP_LATEST` when the buffer is full.","**`emit` vs `tryEmit`**: `emit` suspends when the buffer is full (with SUSPEND policy); `tryEmit` never suspends and returns `false` if it couldn't buffer the value — useful in non-suspend contexts, but can silently drop."]}]},{heading:"StateFlow vs SharedFlow — the comparison",blocks:[{t:"table",headers:["","StateFlow","SharedFlow"],rows:[["Initial value","required","none"],["Current value","yes (`.value`)","no"],["Replay to new collectors","always 1 (the current value)","configurable (0..n)"],["Conflation / distinct","yes — skips equal values, keeps latest","no (configurable buffering)"],["Best for","state (UI state, a value over time)","events (one-shot signals, broadcasts)"],["Analogy","an observable variable","an event bus / broadcast"]]},{t:"list",items:["**Rule of thumb**: **state → StateFlow, events → SharedFlow** (with `replay = 0`). State has a 'current value' that a new observer should immediately see; events are transient and shouldn't replay to late subscribers.","**Why not use StateFlow for events**: its conflation and distinct-until-changed drop values — two identical events collapse to one, and a slow collector misses intermediates. Fine for state (you only care about *now*), wrong for events (each must be delivered).","**Why not use SharedFlow for state**: it has no current value, so a new collector sees nothing until the next emission (unless you set `replay = 1`, at which point you've basically rebuilt a worse StateFlow). StateFlow is purpose-built for the state case."]}]},{heading:"The event-delivery problem SharedFlow doesn't fully solve",blocks:[{t:"p",text:"A common trap: using `SharedFlow(replay = 0)` for one-shot events (navigation, snackbars) *seems* right, but **values emitted while no collector is active are dropped**. During a configuration change the UI's collector is briefly gone — an event emitted in that window vanishes. `tryEmit` with a full/zero buffer also silently fails."},{t:"list",items:["**Mitigations**: give `extraBufferCapacity` so `emit` from a non-suspend context buffers instead of dropping; or use a **`Channel` + `receiveAsFlow()`**, where `send` *suspends until delivered* so events survive collection gaps (the common pragmatic choice).","**Google's recommendation**: model even one-shot events as *state* with a consumption callback (a nullable field cleared after handling) — survives config change and process death, at the cost of some ceremony.","This is the same 'one-shot event problem' covered in the Architecture (MVVM/MVI) topics — SharedFlow is one tool with real delivery caveats, not a complete solution."]},{t:"note",text:'Interview-ready summary: "StateFlow for state (always has a value, conflated, distinct); SharedFlow for events (configurable replay/buffer, no current value). But raw SharedFlow can drop events across lifecycle gaps, so for critical one-shot events I use a Channel or model them as consumable state."'}]},{heading:"StateFlow vs LiveData — the migration question",blocks:[{t:"table",headers:["","LiveData","StateFlow"],rows:[["Platform","Android-only","pure Kotlin (KMP-friendly)"],["Lifecycle awareness","built-in","needs `repeatOnLifecycle` / `collectAsStateWithLifecycle`"],["Initial value","optional (nullable)","required (non-null by default)"],["Operators","few (`map`, `switchMap`)","full Flow operator set"],["Threading / async","manual","coroutine-native"]]},{t:"list",items:["StateFlow is the modern default; LiveData is in maintenance mode. The decisive factor for an Android/KMP engineer is **KMP**: LiveData can't move to `commonMain`, StateFlow can.","The tradeoff StateFlow introduces: it's *not* lifecycle-aware by itself, so you must collect it safely (`collectAsStateWithLifecycle` in Compose, `repeatOnLifecycle` in Views) — otherwise you keep collecting in the background. LiveData handled that automatically."]}]}],HS=[{level:"junior",q:"What is the difference between a cold flow and a hot flow like StateFlow?",a:[{t:"p",text:"**The concept**: a *cold* flow (plain `Flow` from `flow { }`) runs its producer separately for each collector, starting only when collected and stopping when collection ends — every collector gets a private, independent run. A *hot* flow (`StateFlow`, `SharedFlow`) exists on its own and **broadcasts** the same emissions to all current collectors; it emits whether or not anyone is listening, and collectors share one underlying stream."},{t:"p",text:"**Concrete difference**: collect a cold repository flow twice → two network calls. Collect a `StateFlow` twice → both collectors read from the same single value, no duplication. Also, a hot flow like `StateFlow` never completes (there's always potentially a next value), so collecting it suspends until the collecting coroutine is cancelled, whereas a cold flow of finite values completes on its own. Hot flows are for *shared state and events*; cold flows are for *per-consumer, lazy work*."}]},{level:"junior",q:"What is StateFlow and how do you use it in a ViewModel?",a:[{t:"p",text:"**The concept**: `StateFlow<T>` is a hot flow that always holds one *current value* and emits it (plus every update) to collectors. It's an observable state holder — the modern replacement for `LiveData`. The standard ViewModel pattern uses the backing-property idiom: a private `MutableStateFlow` you write to, exposed publicly as a read-only `StateFlow`."},{t:"code",title:"The pattern",code:`private val _uiState = MutableStateFlow(UiState())     // initial value required
val uiState: StateFlow<UiState> = _uiState.asStateFlow()

fun onNameChange(name: String) {
    _uiState.update { it.copy(name = name) }          // immutable, atomic update
}`},{t:"list",items:["**Initial value required** — there's always a current state; `MutableStateFlow(initial)`.","**Read `.value` anytime** — synchronous access to the current state without collecting.","**Only the ViewModel writes** — exposing the read-only `StateFlow` enforces single-source-of-truth / unidirectional data flow; the UI can only observe.","**Update immutably with `update { }`** — replace via `copy()`; `update` is an atomic compare-and-set safe against concurrent writers (unlike `value = value.copy()` which can lose writes under concurrency).","**Collect safely in UI** — `collectAsStateWithLifecycle()` in Compose or `repeatOnLifecycle` in Views (StateFlow isn't lifecycle-aware itself)."]}]},{level:"junior",q:"When should you use StateFlow vs SharedFlow?",a:[{t:"p",text:"**The rule**: **StateFlow for state, SharedFlow for events.** StateFlow always has a *current value* that a new observer should immediately see — perfect for UI state, a selected item, a toggle, anything that has a 'value right now'. SharedFlow has *no* current value and is a configurable broadcast — perfect for *events* that happen and are gone: navigation commands, snackbar triggers, one-shot signals."},{t:"list",items:["**Why not StateFlow for events**: StateFlow conflates and drops duplicates — two identical events (`ShowError` twice) collapse into one emission, and a slow collector only sees the latest. For events, every occurrence must be delivered, so that behavior is wrong.","**Why not SharedFlow for state**: SharedFlow has no 'current value', so a fresh collector sees nothing until the next emission — a screen that subscribes late would show no state. StateFlow is purpose-built to give new collectors the current value immediately.","**Practical setup**: `MutableStateFlow(initialState)` for UI state; `MutableSharedFlow<Event>(replay = 0)` (often with `extraBufferCapacity`) for events."]},{t:"p",text:"Mental model: StateFlow is an *observable variable*; SharedFlow is an *event bus*. Match the tool to whether the thing has a persistent 'current value' or is a transient occurrence."}]},{level:"junior",q:"Why does StateFlow require an initial value but SharedFlow doesn't?",a:[{t:"p",text:"**Because of what each represents.** `StateFlow` models *state* — a value that always exists 'right now'. There's no such thing as state with no value, so it must be initialized, and `.value` can always return something. That's also why a new collector instantly receives the current value: there's always one to give."},{t:"p",text:"`SharedFlow` models a *stream of events/broadcasts*, which has no inherent 'current value' — before the first event, there simply is nothing, and that's valid. A new collector, by default (`replay = 0`), receives only events emitted *after* it subscribes. If you *want* SharedFlow to replay the last value to new collectors you set `replay = 1` — and at that point you've essentially reconstructed StateFlow-like behavior (which is literally how StateFlow is implemented: a specialized SharedFlow with replay 1, conflation, and an initial value). The initial-value requirement is the API encoding the semantic difference: state always has a value, events don't."}]},{level:"junior",q:"How is StateFlow different from LiveData?",a:[{t:"list",items:["**Platform**: LiveData is Android-only; StateFlow is pure Kotlin, so it works in KMP `commonMain`. For an Android/KMP engineer this is the decisive difference — shared ViewModels need StateFlow.","**Lifecycle awareness**: LiveData is lifecycle-aware out of the box (observers auto-pause/resume). StateFlow is *not* — you must collect it safely with `collectAsStateWithLifecycle` (Compose) or `repeatOnLifecycle` (Views), or it keeps collecting in the background.","**Initial value**: LiveData can start empty (null); StateFlow requires an initial value (and is non-null-friendly).","**Operators & async**: StateFlow has the full Flow operator set and native coroutine integration; LiveData has only a few transformations and awkward async."]},{t:"p",text:"**Bottom line**: StateFlow is the modern default and LiveData is in maintenance mode. The one thing StateFlow *costs* you is automatic lifecycle handling — you opt back into it with the lifecycle-aware collectors. Migrating LiveData→StateFlow is usually mechanical, and worth it especially if any code might be shared with KMP."}]},{level:"senior",q:"Explain replay, extraBufferCapacity, and onBufferOverflow in MutableSharedFlow.",a:[{t:"p",text:"These three parameters define a SharedFlow's buffering behavior — how it handles values relative to collectors that may be absent or slow."},{t:"list",items:["**`replay`** — how many of the most recent values a *newly subscribing* collector immediately receives. `replay = 0`: new collectors get only future emissions (correct for events — you don't want a late subscriber replaying an old navigation command). `replay = 1`: the last value is re-delivered to new collectors (StateFlow-like caching). Replayed values are also held in the buffer.","**`extraBufferCapacity`** — buffer slots *in addition to* replay. Their purpose: let the producer `emit` (or `tryEmit`) *without suspending* as long as buffer space remains. With `extraBufferCapacity > 0` and the default SUSPEND policy, a fast producer can get ahead of a slow collector up to the buffer size before it has to wait.","**`onBufferOverflow`** — what happens when the buffer (replay + extra) is full and a new value arrives: `SUSPEND` (default — the emitting coroutine suspends until space frees up, applying backpressure), `DROP_OLDEST` (evict the oldest buffered value, keep flowing — good for 'latest wins' like sensor data), or `DROP_LATEST` (discard the incoming value, keep what's buffered)."]},{t:"p",text:"**How they interact with emit/tryEmit**: `emit` respects SUSPEND (waits when full); `tryEmit` never suspends and returns `false` if it couldn't place the value (which, with a full buffer and SUSPEND policy, means the value is *dropped*). A subtle senior point: `tryEmit` can *always* succeed if `onBufferOverflow` is a DROP policy (it evicts to make room), so pairing `tryEmit` with `DROP_OLDEST` gives a non-suspending, never-failing emitter that keeps the newest values — a common configuration for high-frequency non-critical streams."}]},{level:"senior",q:"You used SharedFlow(replay=0) for navigation events but some events are getting lost. Why, and how do you fix it?",a:[{t:"p",text:"**Why events are lost**: a `SharedFlow` broadcasts to *currently active* collectors only. With `replay = 0` and no buffer, a value emitted while **no collector is active is dropped entirely** — nobody was subscribed to receive it, and there's no replay to catch up. This bites hardest around lifecycle transitions: during a configuration change (rotation) the UI's collector is torn down and re-created, and any event emitted in that gap vanishes. If you emit with `tryEmit` and there's no buffer space, it also silently fails (returns false)."},{t:"list",items:["**Fix 1 — buffer so emissions survive the gap**: `MutableSharedFlow(replay = 0, extraBufferCapacity = 1, onBufferOverflow = SUSPEND)`. Now an event emitted with `tryEmit` while the collector is briefly gone is *buffered* and delivered when collection resumes. Cheap and often sufficient.","**Fix 2 — use a `Channel` + `receiveAsFlow()`**: a `Channel(BUFFERED)` whose `send` *suspends until the value is actually received*. Events wait through collection gaps instead of being dropped — the common pragmatic choice for one-shot events. Caveat: a Channel fans out to a *single* collector (each event goes to exactly one), which is usually what you want for navigation but not for broadcasting to many listeners.","**Fix 3 — model events as consumable state (Google's recommendation)**: put the event in your `StateFlow` UI state as a nullable field, have the UI perform it and call back `onEventConsumed()` to clear it. This survives configuration change *and* process death (if in SavedStateHandle), with no delivery-gap ambiguity. More ceremony, most robust."]},{t:"p",text:"**The senior framing**: SharedFlow is a *hot broadcast*, and hot broadcasts inherently can't deliver to absent listeners — so it's the wrong primitive for 'must happen exactly once' events unless you add buffering. Name the lifecycle-gap mechanism as the root cause, then pick the fix by delivery guarantee needed: buffer (good enough), Channel (suspends-until-delivered), or consumable state (survives process death)."}]},{level:"senior",q:"How does StateFlow's conflation and distinct-until-changed behavior affect what you can use it for?",a:[{t:"p",text:"**Two behaviors, both intentional for state:** (1) **distinct** — StateFlow never emits a value that `equals` the current one, so setting the same value twice emits once. (2) **conflation** — a slow collector only ever sees the *latest* value; intermediate values it was too slow to process are skipped, never queued."},{t:"list",items:["**Why this is correct for state**: a UI only cares about the *current* state. If state changes A→B→C faster than the UI can render, rendering only C is exactly right — rendering A and B first would be wasted frames showing stale data. And re-emitting an equal state would cause needless recomposition. So conflation + distinct make StateFlow efficient for its purpose.","**Where it makes StateFlow the WRONG tool**: anything where *every emission matters*. Analytics that must log each step (conflation drops steps under load). Animations queued per value (skipped intermediates break them). Events (two identical events collapse to one; a missed intermediate is a missed event). For these, use `SharedFlow` (buffered, no conflation) or a `Channel`.","**A concrete gotcha**: mutating a mutable object held in StateFlow and re-assigning it won't emit if `equals`/reference is unchanged — and even a `copy()` producing an equal value won't emit. If you *need* a re-emission of an 'equal' value (rare — usually a design smell), StateFlow can't express it; that's a sign the thing is an *event*, not state."]},{t:"p",text:"**The principle**: StateFlow is optimized for 'what is the value now', trading away 'every individual change'. Choose it when you only care about the latest state; choose SharedFlow/Channel when you must observe each discrete emission."}]},{level:"junior",q:"How do you create a MutableStateFlow and expose it read-only?",a:[{t:"p",text:"Create a private `MutableStateFlow(initial)` that the ViewModel writes to, and expose it publicly as a read-only `StateFlow` via `asStateFlow()`. This enforces a single writer (the ViewModel) — the UI can read/collect but can't mutate the state, preserving unidirectional data flow."},{t:"code",title:"Private mutable, public read-only",code:`private val _uiState = MutableStateFlow(UiState.Loading)
val uiState: StateFlow<UiState> = _uiState.asStateFlow()

fun load() { _uiState.value = UiState.Content(data) }   // only VM writes`},{t:"list",items:["**Private `MutableStateFlow`** — the writable backing state the VM updates.","**Public `StateFlow` via `asStateFlow()`** — read-only view; callers can't cast back to mutate.","**Single writer** — enforces UDF; UI sends events, VM updates state.","**Same pattern for events** — `MutableSharedFlow` + `asSharedFlow()`."]},{t:"note",text:"private val _state = MutableStateFlow(initial); val state = _state.asStateFlow() — the VM writes, the UI reads (asStateFlow prevents outside mutation). Enforces single-writer UDF. Same for events: MutableSharedFlow + asSharedFlow(). Never expose the Mutable type publicly."}]},{level:"junior",q:"How do you update a StateFlow safely: value = vs update { }?",a:[{t:"p",text:"For a simple set, `_state.value = newState` works. But when the new value *depends on the current one* (e.g. toggling a field), `_state.value = _state.value.copy(...)` can race under concurrent updates (read-then-write). `_state.update { it.copy(...) }` does an atomic compare-and-set loop, making it safe from multiple coroutines."},{t:"code",title:"Atomic update",code:`// Simple set:
_state.value = UiState.Loading

// Derive from current — use update (atomic):
_state.update { it.copy(count = it.count + 1) }
// Risky under concurrency: _state.value = _state.value.copy(count = _state.value.count + 1)`},{t:"list",items:["**`value = x`** — fine for a direct set not derived from the current value.","**`update { current -> new }`** — atomic compare-and-set; safe when the new value derives from the old.","**Avoid read-then-write** — `value = value.copy(...)` can lose concurrent updates.","**`getAndUpdate`/`updateAndGet`** — variants returning the old/new value."]},{t:"note",text:"Use value = x for a direct set; use update { it.copy(...) } (atomic compare-and-set) when the new value derives from the current — value = value.copy(...) can race under concurrency (read-then-write loses updates). getAndUpdate/updateAndGet return old/new."}]},{level:"senior",q:"Why is StateFlow always conflated, and when can it lose intermediate values?",a:[{t:"p",text:"StateFlow holds a *single current value* and conflates — it only keeps the latest, and a slow collector may skip intermediate values that were superseded before it collected them. It also skips *equal* consecutive values (`distinctUntilChanged` built in). This makes it perfect for *state* (you only care about the current value) but wrong for *events* (where every occurrence matters)."},{t:"code",title:"Conflation drops intermediates",code:`// If updates happen faster than collection:
_state.value = 1
_state.value = 2
_state.value = 3
// A slow collector might see only 3 (1 and 2 conflated away)`},{t:"list",items:["**Single latest value** — StateFlow always has a current value; new collectors get it immediately.","**Conflated** — intermediate values can be skipped if the collector is slower than updates.","**Distinct** — equal consecutive values are not re-emitted.","**Implication** — great for UI state (latest wins); use `SharedFlow`/`Channel` for events where you can't drop occurrences."]},{t:"note",text:"StateFlow keeps a single current value and conflates — a slow collector may skip intermediates, and equal consecutive values are dropped (built-in distinctUntilChanged). Perfect for state (only the latest matters), wrong for events (occurrences would be lost) — use SharedFlow/Channel for those."}]},{level:"senior",q:"What thread-safety guarantees does StateFlow provide?",a:[{t:"p",text:"StateFlow is thread-safe: reading `.value` and setting it (or `update { }`) can happen from any thread without external synchronization. `update { }` performs an atomic compare-and-set. So you can update UI state from background coroutines directly, and collectors on the main thread will observe a consistent latest value."},{t:"list",items:["**Thread-safe reads/writes** — `.value` get/set from any thread is safe.","**Atomic `update`** — compare-and-set avoids lost updates under concurrency.","**No manual locks** — you don't need a `Mutex` around state updates.","**Update from anywhere** — background coroutines can update state; UI collects the latest safely."]},{t:"note",text:"StateFlow is thread-safe — .value get/set and update { } (atomic compare-and-set) work from any thread without external locks. So background coroutines can update UI state directly and main-thread collectors see a consistent latest value. This is a key reason StateFlow is the go-to for VM state."}]},{level:"junior",q:"What is tryEmit, and how does it differ from emit for SharedFlow?",a:[{t:"p",text:"`emit(value)` is a *suspend* function — if the SharedFlow's buffer is full (and overflow is SUSPEND), it suspends until there's space. `tryEmit(value)` is *non-suspending* — it emits immediately if there's buffer space and returns `true`, or returns `false` without emitting if it can't. Use `tryEmit` from non-suspend contexts (like a synchronous callback), accepting that emission may fail."},{t:"code",title:"emit vs tryEmit",code:`private val _events = MutableSharedFlow<Event>(extraBufferCapacity = 1)
suspend fun send(e: Event) { _events.emit(e) }         // suspends if buffer full
fun sendNow(e: Event): Boolean = _events.tryEmit(e)    // non-blocking; false if it couldn't`},{t:"list",items:["**`emit`** — suspend; waits for buffer space (with SUSPEND overflow).","**`tryEmit`** — non-suspend; emits if possible, returns `false` otherwise.","**`tryEmit` needs buffer** — with `extraBufferCapacity` (or DROP overflow), else it can't emit and returns false.","**Use `tryEmit`** — from callbacks/non-suspend code where you can't suspend."]},{t:"note",text:"emit is suspend (waits for buffer space with SUSPEND overflow); tryEmit is non-suspending (emits if there's space, returns true/false). tryEmit needs buffer capacity (extraBufferCapacity or a DROP policy) to succeed. Use tryEmit from non-suspend callbacks, accepting it may return false."}]},{level:"senior",q:"What do replay and extraBufferCapacity control in MutableSharedFlow?",a:[{t:"p",text:"`replay` is how many past values a *new* collector immediately receives on subscription (a replay cache). `extraBufferCapacity` is additional buffer *beyond* replay for values not yet collected — it lets `tryEmit` succeed and emitters run ahead without suspending. Together with `onBufferOverflow`, they define the SharedFlow's buffering and delivery."},{t:"code",title:"Configuring a SharedFlow",code:`MutableSharedFlow<Event>(
    replay = 0,                     // new collectors get no history (events)
    extraBufferCapacity = 1,        // room so tryEmit works without a collector ready
    onBufferOverflow = BufferOverflow.DROP_OLDEST,
)`},{t:"list",items:["**`replay`** — values replayed to new subscribers (e.g. `1` for latest-value caching; `0` for events).","**`extraBufferCapacity`** — buffer beyond replay so emitters don't suspend and `tryEmit` can succeed.","**`onBufferOverflow`** — SUSPEND (default), DROP_OLDEST, or DROP_LATEST when full.","**StateFlow** — is like `SharedFlow(replay = 1)` + conflation + initial value + distinct."]},{t:"note",text:"replay = how many past values a NEW collector gets on subscription (replay cache); extraBufferCapacity = buffer beyond replay so emitters run ahead / tryEmit succeeds; onBufferOverflow = SUSPEND/DROP_OLDEST/DROP_LATEST when full. StateFlow ≈ SharedFlow(replay=1) + conflation + initial value + distinct."}]},{level:"senior",q:"How do you correctly emit one-off events (navigation, snackbar)?",a:[{t:"p",text:"Use a `Channel` exposed as a `Flow` (`receiveAsFlow()`) or a `SharedFlow(replay = 0)` — *not* a StateFlow (its conflation/replay would re-deliver on rotation). The `Channel` guarantees each event is delivered exactly once to a single collector, so navigation/snackbar events don't repeat when the screen recreates."},{t:"code",title:"Channel-based events",code:`private val _events = Channel<UiEvent>()
val events = _events.receiveAsFlow()               // exactly-once delivery
fun onSaved() { viewModelScope.launch { _events.send(UiEvent.NavigateBack) } }
// UI collects with repeatOnLifecycle/collectAsStateWithLifecycle`},{t:"list",items:["**`Channel` + `receiveAsFlow()`** — exactly-once, buffered; the safest for single-consumer events.","**`SharedFlow(replay = 0)`** — multicast, but events can drop if no collector; add buffer/`tryEmit` carefully.","**Not StateFlow** — its latest-value semantics re-fire on recreation.","**Collect lifecycle-aware** — so events aren't handled while stopped."]},{t:"note",text:"One-off events: a Channel exposed via receiveAsFlow() (exactly-once, buffered — safest for single-consumer) or SharedFlow(replay=0) (multicast, but can drop with no collector). NOT StateFlow (conflation/replay re-fires on rotation). Collect lifecycle-aware so events aren't handled while stopped."}]},{level:"junior",q:"What is a hot flow, and how does it change subscription semantics?",a:[{t:"p",text:"A *hot* flow (StateFlow, SharedFlow) exists and emits *independently of collectors* — it's always 'on', and collectors observe whatever is happening when they subscribe (they don't restart the producer). A *cold* flow starts fresh per collector. So hot flows are shared broadcast sources; late subscribers may miss earlier emissions (except replay)."},{t:"list",items:["**Hot** — produces regardless of collectors; multiple collectors share one stream; late subscribers miss past emissions (beyond replay).","**Cold** — starts per collector; each gets the full sequence from the beginning.","**StateFlow/SharedFlow** — hot; `flow { }` is cold.","**Implication** — hot flows suit shared state/events; you use `shareIn`/`stateIn` to make a cold flow hot."]},{t:"note",text:"Hot flows (StateFlow/SharedFlow) emit independently of collectors — always on, shared among collectors, late subscribers miss past emissions (except replay). Cold flows (flow{}) restart per collector with the full sequence. Use shareIn/stateIn to make a cold flow hot for sharing."}]},{level:"senior",q:"How do you convert a cold flow into a StateFlow?",a:[{t:"p",text:"Use `stateIn(scope, started, initialValue)` — it collects the cold flow once in the given scope and exposes the latest value as a `StateFlow`. This is the standard way to turn a repository's cold flow (Room, network) into observable UI state, sharing one upstream collection among all UI collectors."},{t:"code",title:"stateIn",code:`val uiState: StateFlow<UiState> = repository.observe()
    .map { UiState.Content(it) }
    .stateIn(
        scope = viewModelScope,
        started = SharingStarted.WhileSubscribed(5000),
        initialValue = UiState.Loading,
    )`},{t:"list",items:["**`stateIn(scope, started, initial)`** — hot `StateFlow` backed by the cold flow.","**`WhileSubscribed(5000)`** — upstream active only while collected (+5s across config change).","**Initial value required** — StateFlow always has a current value (e.g. `Loading`).","**Shares upstream** — one collection feeds all collectors (no duplicate work)."]},{t:"note",text:"stateIn(scope, started, initial) collects a cold flow once and exposes the latest as a StateFlow — the standard way to turn a repository flow (Room/network) into UI state. WhileSubscribed(5000) keeps upstream active only while observed (+5s for config change); an initial value is required."}]},{level:"senior",q:"How is StateFlow different from a SharedFlow with replay = 1?",a:[{t:"p",text:"They're close but not identical. `StateFlow` is essentially a `SharedFlow(replay = 1)` with three extras: it *requires an initial value* (always has a current value via `.value`), it *conflates* (only keeps the latest), and it *skips equal consecutive values* (`distinctUntilChanged` built in). A `SharedFlow(replay = 1)` has none of those guarantees by default."},{t:"table",headers:["","StateFlow","SharedFlow(replay=1)"],rows:[["Initial value","required","none"],["`.value` accessor","yes","no"],["Conflation","always","no (buffers per config)"],["Distinct (skip equal)","yes","no"]]},{t:"list",items:["**StateFlow** — value-holder semantics: current value, conflated, distinct; for state.","**SharedFlow(replay=1)** — replays the last value to new collectors but re-emits duplicates and buffers per config; more flexible for events with recent history.","**Choose** — StateFlow for state, SharedFlow for events (with tuned replay/buffer)."]},{t:"note",text:"StateFlow ≈ SharedFlow(replay=1) PLUS: required initial value + .value accessor, always conflated, and distinct (skips equal consecutive). SharedFlow(replay=1) replays the last value but re-emits duplicates and has no .value/initial. StateFlow for state; SharedFlow for events with recent history."}]},{level:"junior",q:"How do you model a whole screen's state with a single StateFlow?",a:[{t:"p",text:"Define one immutable `UiState` data class (or sealed type) holding everything the screen needs, back it with a `MutableStateFlow`, and update it via `update { it.copy(...) }`. The UI collects this single source of truth and renders declaratively — one state in, one screen out, matching `UI = f(state)`."},{t:"code",title:"One UiState StateFlow",code:`data class UiState(
    val isLoading: Boolean = false,
    val items: List<Item> = emptyList(),
    val error: String? = null,
)
private val _state = MutableStateFlow(UiState())
val state = _state.asStateFlow()

fun refresh() = viewModelScope.launch {
    _state.update { it.copy(isLoading = true, error = null) }
    _state.update { runCatching { repo.load() }
        .fold({ it2 -> UiState(items = it2) }, { UiState(error = it.message) }) }
}`},{t:"list",items:["**Single immutable `UiState`** — all screen data in one class.","**`MutableStateFlow` + `update`** — atomic, immutable updates.","**One source of truth** — the UI collects one flow.","**Declarative UI** — render whatever state is emitted."]},{t:"note",text:"One immutable UiState data class (all screen data) backed by MutableStateFlow, updated via update { it.copy(...) }, exposed read-only. The UI collects this single source of truth and renders declaratively (UI = f(state)). Atomic immutable updates keep it race-free and predictable."}]},{level:"senior",q:"How do you combine multiple StateFlows into one derived StateFlow?",a:[{t:"p",text:"Use `combine` on the source StateFlows, then `stateIn` the result to get a derived `StateFlow`. `combine` emits whenever any source changes, using the latest of each; `stateIn` gives it a current value and shares one computation. This derives combined UI state reactively from independent state sources."},{t:"code",title:"Derived StateFlow",code:`val screenState: StateFlow<ScreenState> = combine(
    userFlow, cartFlow, isOnlineFlow,
) { user, cart, online -> ScreenState(user, cart, online) }
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), ScreenState.Empty)`},{t:"list",items:["**`combine`** — recompute the derived value on any source change (latest-of-each).","**`stateIn`** — turn the combined cold flow back into a hot `StateFlow` with an initial value.","**Reactive** — the derived state updates automatically when any input does.","**Alternative** — a `MutableStateFlow` you update manually, but `combine` is cleaner for derivations."]},{t:"note",text:"combine the source StateFlows (latest-of-each on any change), then stateIn to get a derived StateFlow with an initial value and shared computation. It updates automatically when any input changes — cleaner than manually syncing a MutableStateFlow. Standard for building combined UI state."}]},{level:"junior",q:"How do you collect a StateFlow in the UI (Compose and Views)?",a:[{t:"p",text:"In Compose, use `collectAsStateWithLifecycle()` to get a `State` the UI reads. In Views, collect inside `repeatOnLifecycle(STARTED)` within `lifecycleScope`. Both are lifecycle-aware — they stop collecting when the UI is backgrounded and resume on return, which pairs with `stateIn(WhileSubscribed)` upstream."},{t:"code",title:"Collecting StateFlow",code:`// Compose
val state by viewModel.uiState.collectAsStateWithLifecycle()
// Views
lifecycleScope.launch {
    repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.uiState.collect { binding.render(it) }
    }
}`},{t:"list",items:["**Compose** — `collectAsStateWithLifecycle()` (preferred over `collectAsState`).","**Views** — `repeatOnLifecycle(STARTED)` collection.","**Lifecycle-aware** — stops when backgrounded; resumes on foreground.","**Immediate value** — a new collector gets the current StateFlow value right away."]},{t:"note",text:"Compose: collectAsStateWithLifecycle(). Views: repeatOnLifecycle(STARTED) in lifecycleScope. Both are lifecycle-aware (stop when backgrounded, resume on return) and pair with stateIn(WhileSubscribed). New collectors immediately get StateFlow's current value."}]},{level:"senior",q:"How does a slow collector interact with StateFlow versus SharedFlow?",a:[{t:"p",text:"With a `StateFlow`, a slow collector *conflates* — it may skip intermediate values and just see the latest, because StateFlow only holds the current value. With a `SharedFlow`, behavior depends on `onBufferOverflow`: SUSPEND makes fast emitters wait for the slow collector (backpressure), while DROP_OLDEST/DROP_LATEST drop values. So StateFlow is inherently lossy for intermediates; SharedFlow is configurable."},{t:"list",items:["**StateFlow** — conflated; slow collector sees the latest, intermediates dropped.","**SharedFlow + SUSPEND** — emitters suspend until the collector catches up (no loss, backpressure).","**SharedFlow + DROP_*** — values dropped to keep emitters unblocked.","**Implication** — for state, conflation is fine; for events you must choose overflow behavior deliberately."]},{t:"note",text:"StateFlow conflates — a slow collector sees only the latest (intermediates lost). SharedFlow depends on onBufferOverflow: SUSPEND applies backpressure (emitters wait, no loss), DROP_OLDEST/LATEST drop to keep emitters unblocked. State → conflation is fine; events → choose overflow deliberately."}]},{level:"junior",q:"Why shouldn't you expose MutableStateFlow or MutableSharedFlow publicly?",a:[{t:"p",text:"Exposing the mutable type lets any collector *write* to your state/events, breaking unidirectional data flow and encapsulation — the UI could mutate the ViewModel's state directly, causing hard-to-trace bugs. Expose the read-only `StateFlow`/`SharedFlow` (via `asStateFlow()`/`asSharedFlow()`) so only the owner mutates."},{t:"list",items:["**Encapsulation** — only the ViewModel should change its state; the UI reads and sends events.","**UDF** — a public mutable flow invites the UI to write state, breaking the one-way loop.","**`asStateFlow()`/`asSharedFlow()`** — return a read-only view that can't be cast back to mutable.","**Testability/clarity** — a single writer makes state changes traceable."]},{t:"note",text:"A public MutableStateFlow/MutableSharedFlow lets collectors write your state/events, breaking encapsulation and UDF (the UI could mutate VM state). Expose read-only via asStateFlow()/asSharedFlow() so only the owner writes — keeping the one-way data loop and making changes traceable."}]},{level:"senior",q:"Is StateFlow lossy, and what does that mean for what you can build with it?",a:[{t:"p",text:"Yes, for *intermediate* values — StateFlow conflates, so rapid successive updates may not all be observed; only the latest is guaranteed. This is exactly right for *state* (you render the current value) but means you can't use StateFlow for anything where every value must be processed (counters incremented by observing each change, event streams, analytics per-change)."},{t:"list",items:["**Lossy for intermediates** — fast updates can be conflated away.","**Fine for state** — the UI only needs the latest value.","**Not for 'process every value'** — use `SharedFlow`/`Channel` where each occurrence matters.","**Not for events** — a repeated identical event wouldn't even emit (distinct), and rotation could re-deliver."]},{t:"note",text:"StateFlow is lossy for intermediates (conflated — only the latest is guaranteed) and skips equal values (distinct). Perfect for state (render the latest), but don't use it where every value must be processed or for events — use SharedFlow/Channel there."}]},{level:"senior",q:"How do you test a StateFlow's emissions?",a:[{t:"p",text:"Two common approaches: read `.value` after driving the ViewModel (simple for the final state), or collect emissions into a list within `runTest` (to assert the *sequence* Loading → Content). The Turbine library makes sequence assertions clean. Because a StateFlow needs an active collector to run `WhileSubscribed` upstream, tests often collect in a background job or use `.value` with `advanceUntilIdle`."},{t:"code",title:"Testing emissions",code:`@Test fun emitsLoadingThenContent() = runTest {
    val vm = MyViewModel(fakeRepo)
    vm.uiState.test {                    // Turbine
        assertEquals(UiState.Loading, awaitItem())
        vm.load()
        assertEquals(UiState.Content(data), awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**`.value`** — assert the current/final state after `advanceUntilIdle()`.","**Turbine `test { }`** — assert the sequence of emissions cleanly.","**Collect in a background job** — for `WhileSubscribed` flows, an active collector starts the upstream.","**Test dispatcher + `runTest`** — deterministic scheduling."]},{t:"note",text:"Test StateFlow by reading .value after advanceUntilIdle (final state) or collecting the sequence with Turbine's test { awaitItem() } (Loading → Content). WhileSubscribed upstream needs an active collector to start, so collect in a background job. Use runTest + a test dispatcher for determinism."}]},{level:"junior",q:"Does a new collector of a StateFlow get the current value immediately?",a:[{t:"p",text:"Yes — because StateFlow always holds a current value (like `replay = 1`), any new collector immediately receives the latest value on subscription, then subsequent updates. This is why StateFlow is ideal for UI state: a screen that starts collecting instantly renders the current state without waiting for the next emission."},{t:"list",items:["**Immediate current value** — new collectors get `.value` right away.","**Then updates** — followed by future changes.","**Contrast `SharedFlow(replay=0)`** — a new collector gets *no* history, only future emissions.","**Good for UI** — the screen renders immediately on subscription."]},{t:"note",text:"Yes — StateFlow always has a current value (like replay=1), so a new collector immediately gets the latest, then subsequent updates. That's why it's ideal for UI state (instant render on subscription). SharedFlow(replay=0) gives new collectors no history — only future emissions."}]},{level:"senior",q:"When would you choose StateFlow over Compose's mutableStateOf for ViewModel state?",a:[{t:"p",text:"Prefer `StateFlow` when you want the ViewModel to be *Compose-independent* — testable without Compose, usable in KMP, and composable with Flow operators (`combine`, `debounce`). `mutableStateOf` in the ViewModel couples it to the Compose runtime and can't be transformed with Flow operators. For most apps, `StateFlow` is the default; `mutableStateOf` in the VM suits small Compose-only apps."},{t:"list",items:["**`StateFlow`** — Compose-agnostic (unit-testable, KMP), works with Flow operators, lifecycle-aware collection.","**`mutableStateOf` in VM** — simpler (no `collectAsState`), but ties the VM to Compose and lacks Flow operators.","**Consistency** — pick one per codebase.","**UI reads either** — `collectAsStateWithLifecycle()` for StateFlow; direct read for `mutableStateOf`."]},{t:"note",text:"Choose StateFlow to keep the ViewModel Compose-independent (testable, KMP) and composable with Flow operators (combine/debounce); collect with collectAsStateWithLifecycle. mutableStateOf in the VM is simpler (no collect) but couples to Compose and lacks operators. StateFlow is the default; pick one per codebase."}]},{level:"senior",q:"What are the onBufferOverflow strategies for SharedFlow, and when do you use each?",a:[{t:"p",text:"`onBufferOverflow` decides what happens when a `MutableSharedFlow`'s buffer is full and a new value is emitted: `SUSPEND` (default) makes the emitter wait (backpressure), `DROP_OLDEST` discards the oldest buffered value to make room, and `DROP_LATEST` discards the new value. The choice trades backpressure against data loss."},{t:"table",headers:["Strategy","On full buffer","Use"],rows:[["SUSPEND","emitter waits","no loss; emitter can suspend"],["DROP_OLDEST","drop oldest buffered","keep newest (latest-value feeds)"],["DROP_LATEST","drop the new value","keep the earliest until consumed"]]},{t:"list",items:["**SUSPEND** — backpressure; use when the emitter is a coroutine that can wait and no value should be lost.","**DROP_OLDEST** — keep the freshest data; good when only recent values matter (sensor/latest-state feeds) and with `tryEmit`.","**DROP_LATEST** — keep already-buffered values, drop new ones under pressure.","**With `tryEmit`** — you need a non-SUSPEND policy (or spare buffer) for `tryEmit` to succeed."]},{t:"note",text:"onBufferOverflow on a full buffer: SUSPEND (default — emitter waits, no loss), DROP_OLDEST (discard oldest — keep freshest, good with tryEmit/sensors), DROP_LATEST (discard the new value). tryEmit needs a non-SUSPEND policy or spare buffer to succeed. Trade backpressure vs loss."}]},{level:"junior",q:"How does StateFlow compare to LiveData, and why is it often preferred?",a:[{t:"p",text:"Both are observable state holders, but `StateFlow` is a Kotlin/coroutines-native primitive: it's not Android-tied (usable in pure Kotlin/KMP and unit-testable without Robolectric), composes with Flow operators, and always has a value. `LiveData` is lifecycle-aware out of the box but Android-only and lacks operators. StateFlow + `collectAsStateWithLifecycle`/`repeatOnLifecycle` matches LiveData's lifecycle-awareness while being more powerful."},{t:"table",headers:["","StateFlow","LiveData"],rows:[["Platform","pure Kotlin (KMP)","Android-only"],["Operators","full Flow operators","limited (map/switchMap)"],["Lifecycle","via collectAsStateWithLifecycle/repeatOnLifecycle","built-in"],["Initial value","required","optional (nullable)"],["Testing","plain JVM","needs InstantTaskExecutorRule"]]},{t:"list",items:["**StateFlow** — coroutines-native, KMP, operator-rich, always has a value; needs lifecycle-aware collection.","**LiveData** — Android-only, lifecycle-aware by default, limited operators, nullable.","**Migration** — new code favors StateFlow; LiveData remains fine in existing Android-only apps."]},{t:"note",text:"StateFlow is coroutines-native (pure Kotlin/KMP, JVM-testable, full Flow operators, always has a value) but needs lifecycle-aware collection (collectAsStateWithLifecycle/repeatOnLifecycle). LiveData is Android-only, lifecycle-aware by default, with limited operators. New code prefers StateFlow; LiveData is fine in existing Android apps."}]},{level:"senior",q:"How can a SharedFlow react to whether it has subscribers (subscriptionCount)?",a:[{t:"p",text:"`MutableSharedFlow.subscriptionCount` is a `StateFlow<Int>` of the current number of active collectors. You can observe it to start/stop upstream work only when someone is listening — this is exactly the mechanism `SharingStarted.WhileSubscribed` uses internally, and you can use it directly for custom hot sources (e.g. connect a WebSocket only while subscribed)."},{t:"code",title:"React to subscribers",code:`private val _events = MutableSharedFlow<Event>()
val events = _events.asSharedFlow()

init {
    scope.launch {
        _events.subscriptionCount
            .map { it > 0 }
            .distinctUntilChanged()
            .collect { hasSubscribers ->
                if (hasSubscribers) connectSocket() else disconnectSocket()
            }
    }
}`},{t:"list",items:["**`subscriptionCount`** — a `StateFlow<Int>` of active collectors.","**Start/stop upstream** — connect/disconnect a resource based on `> 0`.","**`WhileSubscribed` uses it** — the standard sharing strategy is built on this signal (plus a timeout).","**Custom hot sources** — manage expensive connections (sockets, sensors) tied to real demand."]},{t:"note",text:"MutableSharedFlow.subscriptionCount is a StateFlow<Int> of active collectors — observe it (map { it > 0 }.distinctUntilChanged()) to start/stop upstream work only when someone's listening (connect a socket only while subscribed). It's the mechanism behind SharingStarted.WhileSubscribed; use it directly for custom hot sources."}]}],KS={id:"stateflow-sharedflow",title:"StateFlow & SharedFlow",tagline:"Hot flows, StateFlow as observable state, SharedFlow as configurable broadcast, replay/buffer/overflow, the event-delivery problem, and StateFlow vs LiveData.",tags:["StateFlow","SharedFlow","Hot Flow","Events","LiveData"],content:zS,qa:HS},_S=[{heading:"Backpressure — what happens when the producer is faster than the collector",blocks:[{t:"p",text:"**Backpressure** is the problem of a producer emitting values faster than the collector can process them. In RxJava this needed special types (`Flowable`) and strategies. Kotlin Flow solves it *by default and for free* through **suspension**: because `emit` is a suspend function and flows are sequential, when the collector is busy, `emit` simply **suspends the producer** until the collector is ready for the next value. No buffer overflows, no dropped values, no special API — the producer naturally slows to the collector's pace."},{t:"code",title:"Default: producer waits for the collector",code:`flow {
    for (i in 1..3) {
        println("emitting $i")
        emit(i)                 // SUSPENDS here until collector finishes with i
    }
}.collect { value ->
    delay(1000)                 // slow collector
    println("collected $value")
}
// emitting 1 -> (1s) -> collected 1 -> emitting 2 -> ...
// Producer and collector run in lockstep; total ~3s`},{t:"list",items:["Because a flow is **sequential**, producing value N+1 can't start until value N has been fully processed downstream — the producer is automatically throttled to the collector's speed.","This is the safest default: nothing is lost, memory stays bounded (no unbounded queue), and the two sides self-synchronize. The cost is *latency* — the producer idles while the collector works, so total time is roughly producer time + collector time added serially.","When that serial cost hurts, you opt into concurrency between producer and collector with **`buffer`** — trading memory for throughput."]}]},{heading:"buffer() — decouple producer and collector for throughput",blocks:[{t:"p",text:"`buffer(capacity)` inserts a channel between producer and collector so they run **concurrently**: the producer emits into the buffer without waiting for the collector, and the collector drains the buffer at its own pace. This overlaps their work, improving throughput when the producer has idle time (e.g. it does I/O between emissions)."},{t:"code",title:"buffer overlaps production and consumption",code:`flow {
    for (i in 1..3) { delay(100); emit(i) }   // producer: 100ms each
}
.buffer()                                     // run producer & collector concurrently
.collect { delay(300) }                       // collector: 300ms each

// WITHOUT buffer: (100+300)*3 = 1200ms (serial)
// WITH buffer: producer runs ahead while collector works -> ~1000ms`},{t:"list",items:["`buffer()` takes an optional capacity and an `onBufferOverflow` policy (SUSPEND/DROP_OLDEST/DROP_LATEST) — the same knobs as SharedFlow.","The tradeoff is **memory for latency**: you hold buffered values in exchange for the two sides not blocking each other. With an unbounded producer and a slow collector, an unbounded buffer grows without limit — so bound it, or use a drop policy.","`buffer` is also what several other operators are built on — `flowOn`, `conflate`, and `produceIn` all involve a channel between stages."]}]},{heading:"conflate() — keep only the latest, drop intermediates",blocks:[{t:"p",text:"`conflate()` is a buffering strategy that says: **if the collector is slow, skip intermediate values and give it the latest**. When the collector finishes and asks for the next value, it gets the *most recent* one the producer emitted, and everything in between is dropped. It's `buffer(onBufferOverflow = DROP_OLDEST)` with capacity that keeps only the newest."},{t:"code",title:"conflate drops values the collector was too slow to see",code:`flow {
    for (i in 1..100) { delay(10); emit(i) }   // fast producer
}
.conflate()
.collect { value ->
    delay(100)                                  // slow collector
    println(value)                              // prints latest: e.g. 1, 15, 31, ... 100
}`},{t:"list",items:["**When to use**: the collector only cares about the *latest* state, not every value — progress indicators, live sensor/price displays, UI that renders current state. Showing every intermediate would be pointless and wasteful.","**When NOT to use**: every value matters (each must be processed/logged/persisted) — conflation silently loses data. Use `buffer` (keeps all) instead.","This is exactly why `StateFlow` is conflated — state cares only about 'now'. `conflate()` gives any flow that same 'latest-wins' semantics."]}]},{heading:"collectLatest / mapLatest — cancel in-progress work on new values",blocks:[{t:"p",text:"`conflate` drops intermediate values *before* the collector starts processing them. `collectLatest` (and `mapLatest`/`flatMapLatest`) take a different approach: they **start** processing every value, but **cancel** the in-progress processing if a newer value arrives. The difference matters when the per-value work is cancellable and you want the latest value's work to actually run to completion."},{t:"code",title:"conflate vs collectLatest",code:`// conflate: collector processes some values fully, skips others entirely
flow.conflate().collect { process(it) }         // process() always completes

// collectLatest: collector starts every value, cancels if superseded
flow.collectLatest { value ->
    process(value)                              // CANCELLED mid-way if new value arrives
}`},{t:"list",items:["**`conflate`** — never cancels; it just doesn't deliver skipped values. Good when the processing itself isn't cancellable or you want each delivered value fully handled.","**`collectLatest`** — cancels the block for the previous value when a new one arrives; only the latest value's processing survives to completion. Good when processing is expensive/cancellable and stale work should be abandoned (search-as-you-type).","Subtle but real distinction interviewers probe: with `conflate`, an already-started `process()` finishes; with `collectLatest`, it gets cancelled. If `process()` writes to a DB, `conflate` completes the write, `collectLatest` might cancel it half-done — choose accordingly."]}]},{heading:"The decision guide",blocks:[{t:"table",headers:["Situation","Tool","Effect"],rows:[["Default; every value matters, don't overwhelm memory","(nothing — suspension)","producer waits for collector; nothing lost"],["Producer & collector both slow; overlap them","`buffer()`","concurrent; all values kept; uses memory"],["Collector only needs the latest value","`conflate()`","skip intermediates; keep newest"],["Per-value work is cancellable; only latest should complete","`collectLatest` / `flatMapLatest`","cancel in-progress work on new value"],["Bounded buffer with explicit drop policy","`buffer(n, onBufferOverflow = ...)`","control exactly what happens when full"]]},{t:"note",text:'Interview one-liner: "Flow handles backpressure by default through suspension — the producer waits for the collector, so nothing is dropped and memory is bounded. When I need throughput I add `buffer` (keep all, use memory); when I only want the latest I use `conflate` (drop intermediates) or `collectLatest` (cancel in-progress work)."'}]}],GS=[{level:"junior",q:"What is backpressure, and how does Kotlin Flow handle it?",a:[{t:"p",text:"**The concept**: backpressure is what happens when a producer emits values *faster* than the collector can consume them. Without a strategy, values pile up (unbounded memory) or get lost. In RxJava this required special reactive types and explicit strategies."},{t:"p",text:"**How Flow handles it — for free, by default:** through **suspension**. `emit` is a suspend function, and flows are sequential, so producing the next value can't begin until the current value has been fully processed downstream. If the collector is slow, `emit` simply *suspends the producer* until the collector is ready. The producer automatically runs at the collector's pace — nothing is dropped, memory stays bounded, and there's no special API to learn. The only cost is latency: the producer idles while the collector works (their times add up serially). When that serial cost matters, you opt into `buffer` to let them run concurrently."}]},{level:"junior",q:"What does the buffer() operator do?",a:[{t:"p",text:"**The concept**: by default a flow runs the producer and collector in lockstep — the producer waits for the collector to finish each value. `buffer()` inserts a channel between them so they run **concurrently**: the producer emits into the buffer and keeps going without waiting, while the collector drains the buffer at its own speed. This overlaps their work and improves throughput when the producer has idle time between emissions (e.g. it does I/O)."},{t:"code",title:"The speedup",code:`flow { repeat(3) { delay(100); emit(it) } }   // 100ms per item
  .buffer()
  .collect { delay(300) }                     // 300ms per item
// Without buffer: serial -> (100+300)*3 = 1200ms
// With buffer: overlapped -> ~1000ms (producer works ahead)`},{t:"p",text:"The tradeoff is **memory for latency** — you hold buffered values so neither side blocks the other. You can bound the buffer (`buffer(capacity)`) and set an overflow policy (`SUSPEND`, `DROP_OLDEST`, `DROP_LATEST`) for when it fills. Without a bound, a fast producer feeding a slow collector grows the buffer indefinitely, so bound it or use a drop policy in that scenario."}]},{level:"junior",q:"What is the difference between buffer() and conflate()?",a:[{t:"p",text:"**Both let the producer run ahead of the collector, but they handle the backlog differently.** `buffer()` **keeps all** the values — the collector eventually processes every one, just later. `conflate()` **keeps only the latest** — when the collector is slow, intermediate values are dropped, and when it's ready for the next value it gets the most recent one the producer emitted."},{t:"list",items:["Use **`buffer`** when every value matters and you just want throughput — e.g. processing a stream of items each of which must be handled.","Use **`conflate`** when the collector only cares about the *current/latest* value and intermediates are pointless — e.g. a live price ticker or progress indicator, where rendering every intermediate frame the collector was too slow to see would be wasted work.","Memory-wise, `conflate` is bounded (it holds at most the latest value); `buffer` holds as many as its capacity allows."]},{t:"p",text:"A neat way to remember it: `conflate()` is essentially `buffer` with a 'drop oldest, keep newest' overflow policy and minimal capacity. And it's exactly why `StateFlow` behaves the way it does — state only cares about 'now', so it's conflated by design."}]},{level:"junior",q:"If Flow handles backpressure automatically, when do you actually need buffer or conflate?",a:[{t:"p",text:"**The default (suspension) is correct most of the time** — it's safe (nothing lost) and memory-bounded. You reach for `buffer`/`conflate` only to address the default's one downside: because producer and collector run serially, their times add up, which can be too slow."},{t:"list",items:["**Add `buffer`** when the producer and collector each take real time and running them *concurrently* would meaningfully speed things up — and you need every value. Example: a flow that reads pages from disk (producer) and parses them (collector); buffering lets the next page load while the current one parses.","**Add `conflate` (or `collectLatest`)** when the collector can't keep up *and* only the latest value is relevant — a fast sensor stream feeding a UI that just shows the current reading. Here you *want* to skip the backlog.","**Leave it alone** when values arrive slowly relative to processing (the common case) — adding buffering does nothing useful and only adds memory/complexity."]},{t:"p",text:"So the honest answer is: usually you don't need them. They're targeted optimizations for 'producer and collector are both slow, and I either want to overlap them (buffer) or only care about the latest (conflate)'."}]},{level:"senior",q:"Explain the difference between conflate() and collectLatest(). When would you choose each?",a:[{t:"p",text:"**Both give you 'latest wins' behavior for a slow collector, but at different points.** `conflate()` acts *before* processing: it drops intermediate values so the collector's block only ever *starts* with values it can keep up with — and once a block starts, it **runs to completion**. `collectLatest()` acts *during* processing: it starts the block for *every* value, but **cancels** the in-progress block if a newer value arrives, so only the latest value's processing survives."},{t:"code",title:"The behavioral difference",code:`// conflate: some values skipped, but each STARTED block finishes
flow.conflate().collect { writeToDb(it) }        // every write that starts, completes

// collectLatest: every value starts, superseded ones are CANCELLED mid-way
flow.collectLatest { writeToDb(it) }             // an in-progress write may be cancelled`},{t:"list",items:["**Choose `conflate`** when the per-value work must not be interrupted once begun — a DB write, a file operation, anything where a half-done cancellation is bad. You accept that some values are never processed, but the ones that are, complete cleanly.","**Choose `collectLatest`/`flatMapLatest`** when the work is *cancellable and worth abandoning* when stale — search-as-you-type, recomputing an expensive result for the newest input. The whole point is that finishing outdated work is wasteful and possibly harmful (stale-result race), so cancelling it is desirable.","**The subtle senior point**: `conflate` never cancels — it's about *delivery* (which values reach the collector). `collectLatest` is about *cancellation* (which in-progress work survives). If your processing has side effects that shouldn't be interrupted, that distinction decides correctness, not just performance."]}]},{level:"senior",q:"How does buffer() interact with flowOn, and how do multiple buffer stages behave in a pipeline?",a:[{t:"p",text:"**They're deeply related — both introduce a channel between stages.** `flowOn(dispatcher)` changes the context of the *upstream* and, to do so, it must run upstream in a different coroutine, which requires a channel to hand values across — so `flowOn` *implicitly buffers*. That means adding `flowOn` already decouples the upstream from the downstream (they run concurrently on different dispatchers), giving you a throughput benefit similar to `buffer` as a side effect."},{t:"list",items:["**Each `buffer`/`flowOn` boundary is a concurrency stage**: values cross a channel, and the stages on either side run in parallel. So a pipeline `A.map{}.flowOn(io).buffer().collect{}` has the `io`-dispatched upstream, a buffer, and the collector all potentially running concurrently — a small pipeline of overlapping stages.","**Multiple buffers compound**: each `buffer()` adds another channel and another capacity, so `flow.buffer().map{}.buffer().collect{}` creates two decoupled stages. This can help (more overlap) or hurt (more memory, more latency between first emission and first collection) — measure rather than sprinkle.","**Adjacent buffers may be fused**: the Flow implementation fuses consecutive `buffer`/`flowOn`/`conflate` operators where possible (e.g. `buffer(); buffer()` becomes one channel with combined capacity; `conflate(); buffer()` resolves per rules), so you don't always pay for every operator literally — but relying on fusion is fragile; write the intent you mean.","**Practical rule**: you rarely need explicit `buffer` if you already have a `flowOn` boundary where the expensive work happens — the `flowOn` gives you the concurrency. Add explicit `buffer` only when producer and collector are on the *same* context but you still want them overlapped, or to set a specific capacity/overflow policy."]},{t:"p",text:"**The mental model to convey**: think of `buffer`, `flowOn`, and `conflate` as all placing a channel between pipeline stages — that's why they share capacity/overflow parameters and why they interact. Understanding that they're the *same underlying mechanism* (a channel handoff) is what separates surface knowledge from real understanding here."}]},{level:"senior",q:"A flow producing values rapidly is causing memory growth or UI lag. Walk through diagnosing and choosing a backpressure strategy.",a:[{t:"list",items:["**1. Identify the mismatch**: confirm it's producer-faster-than-collector. Profile where time goes — is the *collector* slow (heavy per-value work: parsing, DB writes, recomposition) or is the *producer* flooding (a high-frequency sensor/socket)? The fix differs.","**2. Decide: do you need every value?** This is the pivotal question. If yes → keep-all strategies (`buffer`, fix the slow collector). If no, only latest matters → drop strategies (`conflate`, `collectLatest`, `sample`, `debounce`).","**3. If every value matters and memory is growing**: an unbounded buffer somewhere (explicit `buffer()` with no cap, or a `channelFlow`/`callbackFlow` with unlimited capacity) is queuing faster than draining. Bound it (`buffer(capacity)`), and if the collector simply can't keep up long-term, that's a design problem — move work off the hot path, batch, or apply `sample` to reduce rate deliberately.","**4. If only the latest matters (typical UI case)**: `conflate()` (skip intermediates, keep newest) for a stream you render, or `collectLatest`/`flatMapLatest` if the per-value work is cancellable and stale computations should be abandoned. For time-based thinning, `sample(interval)` (regular snapshots) or `debounce(timeout)` (wait for quiet) cut the rate at the source.","**5. If the collector is slow due to threading**: ensure the collector isn't doing heavy work on the main thread — move it with `flowOn` on the upstream heavy stage, or process off-main, so the UI isn't the bottleneck.","**6. Verify with numbers**: re-profile memory and frame timing after the change — backpressure fixes are easy to cargo-cult; confirm the buffer is bounded and the UI keeps up."]},{t:"p",text:"**The framing**: the entire decision reduces to 'must I process every value, or only the latest?' plus 'where is the actual bottleneck?'. Rapid producer + UI that shows current state → `conflate`/`sample`. Rapid producer + must-process-all → bounded `buffer` and a faster or off-loaded collector. Naming that decision tree, and bounding any unbounded buffer, is the senior-level answer."}]},{level:"junior",q:"What is backpressure, in plain terms?",a:[{t:"p",text:"Backpressure is the problem of a *producer emitting faster than a consumer can process*. Without a strategy, the excess values pile up (growing memory) or the consumer falls behind. 'Backpressure handling' means the system pushes back on the producer (slow it down) or manages the overflow (buffer, drop, conflate) so it stays stable."},{t:"list",items:["**The mismatch** — fast producer, slow consumer; values accumulate or the consumer lags.","**Strategies** — slow the producer (suspension), buffer, drop, or keep only the latest (conflate).","**Goal** — bounded memory and predictable behavior under load.","**Examples** — sensor data at 100Hz feeding a UI, rapid DB updates, a fast network stream to a slow parser."]},{t:"note",text:"Backpressure = a producer emitting faster than the consumer can handle, causing pile-up or lag. Handling it means pushing back on the producer (suspension) or managing overflow (buffer/drop/conflate) to keep memory bounded and behavior stable — e.g. 100Hz sensor data into a slow UI."}]},{level:"senior",q:"How does Flow's suspension-based backpressure differ from RxJava's strategies?",a:[{t:"p",text:"Kotlin Flow handles backpressure *automatically via suspension*: because `emit` is a suspend function, if the collector is slow, `emit` simply suspends until the collector is ready — the producer naturally waits, no explicit strategy needed. RxJava (Flowable) requires you to choose a `BackpressureStrategy` (BUFFER, DROP, LATEST, etc.) explicitly, and Observable has no backpressure at all."},{t:"list",items:["**Flow** — `emit` suspends when the collector is busy; backpressure is built into the suspension model (no config for the basic case).","**RxJava `Flowable`** — explicit `BackpressureStrategy`; `Observable` ignores backpressure (can overflow).","**Opt into more** — Flow adds `buffer`/`conflate`/`collectLatest` only when you *want* to decouple or drop.","**Simpler default** — Flow's sequential suspension 'just works' for the common case."]},{t:"note",text:"Flow handles backpressure automatically via suspension — emit suspends when the collector is slow, so the producer waits (no config needed). RxJava requires an explicit BackpressureStrategy on Flowable (Observable has none). Flow adds buffer/conflate/collectLatest only when you want to decouple or drop."}]},{level:"senior",q:"What does conflate() do precisely, and what does it drop?",a:[{t:"p",text:"`conflate()` keeps only the *latest* value when the collector is slower than the producer — intermediate values emitted while the collector was busy are dropped. It decouples producer and collector (like `buffer`) but with a capacity-1 conflating buffer: the collector always processes the most recent value, skipping stale ones. Great when only the newest value matters."},{t:"code",title:"conflate keeps the latest",code:`fastProducer
    .conflate()               // drop intermediates; collector gets the newest available
    .collect { slowRender(it) }   // if 1,2,3 emitted during a render, next sees 3`},{t:"list",items:["**Keeps latest** — drops values the collector missed while busy.","**Decouples** — producer isn't slowed to the collector's speed (unlike default).","**Capacity-1 conflating** — always the most recent value survives.","**Use** — progress bars, live position, UI state where stale intermediates are useless."]},{t:"note",text:"conflate() keeps only the latest value when the collector lags — intermediates emitted while it was busy are dropped (capacity-1 conflating buffer), and the producer isn't slowed. Use it when only the newest value matters (progress, live position, latest state). StateFlow is inherently conflated."}]},{level:"senior",q:"What is collectLatest, and how does it handle a slow collector?",a:[{t:"p",text:"`collectLatest { }` *cancels the collector block* when a new value arrives and restarts it with the newest value. Unlike `conflate` (which lets the current processing finish and just skips intermediates), `collectLatest` actively abandons in-progress work for a superseded value. Ideal when processing a value is cancellable and only the latest should complete (search, rendering the latest state)."},{t:"code",title:"collectLatest cancels stale work",code:`queryFlow.collectLatest { query ->
    val results = repository.search(query)   // cancelled if a new query arrives mid-search
    render(results)
}`},{t:"list",items:["**Cancel-and-restart** — a new value cancels the running block and starts fresh.","**vs `conflate`** — conflate lets current processing finish (skips intermediates); collectLatest cancels it.","**Best when work is cancellable** — the abandoned processing is genuinely stopped (suspending work).","**Uses** — search, rendering only the latest state, any 'only the newest matters and processing is cancellable' case."]},{t:"note",text:"collectLatest { } cancels the collector block when a new value arrives and restarts with the latest — actively abandoning stale processing (vs conflate, which lets current work finish and just skips intermediates). Use it when processing is cancellable and only the newest value should complete (search, latest-state rendering)."}]},{level:"senior",q:"What buffer overflow strategies does buffer() support?",a:[{t:"p",text:"`buffer(capacity, onBufferOverflow)` lets you choose what happens when the buffer fills: `SUSPEND` (default — the producer waits, preserving all values with backpressure), `DROP_OLDEST` (discard the oldest buffered value), or `DROP_LATEST` (discard the incoming value). You can also pass special capacities like `Channel.CONFLATED` or `Channel.UNLIMITED`."},{t:"code",title:"buffer with overflow policy",code:`flow.buffer(capacity = 64, onBufferOverflow = BufferOverflow.DROP_OLDEST)
flow.buffer(Channel.CONFLATED)      // == conflate()
flow.buffer(Channel.UNLIMITED)      // never suspends producer (OOM risk)`},{t:"list",items:["**SUSPEND (default)** — producer waits when full; no loss, backpressure applied.","**DROP_OLDEST** — keep the newest values (drop oldest buffered).","**DROP_LATEST** — keep buffered values, drop new ones under pressure.","**Special capacities** — `CONFLATED` (latest only), `UNLIMITED` (unbounded — memory risk), `RENDEZVOUS` (no buffer)."]},{t:"note",text:"buffer(capacity, onBufferOverflow): SUSPEND (default — producer waits, no loss), DROP_OLDEST (keep newest), DROP_LATEST (keep buffered). Special capacities: CONFLATED (==conflate), UNLIMITED (OOM risk), RENDEZVOUS (no buffer). Choose by whether values can be dropped and which to keep."}]},{level:"junior",q:"Why is a Flow sequential by default, with no buffering?",a:[{t:"p",text:"By default the producer and collector run in the *same coroutine*, so emitting a value directly invokes the collector and waits for it to finish before producing the next. There's no buffer — the producer is paced by the collector (natural backpressure). You add `buffer()` only when you want the producer and collector to run *concurrently* (overlap production with consumption)."},{t:"list",items:["**Same coroutine** — emit → collect → resume producer, one value at a time.","**Natural backpressure** — a slow collector slows the producer; no unbounded pile-up.","**No concurrency by default** — production and consumption don't overlap.","**`buffer()`** — decouples them so the producer can work ahead while the collector processes."]},{t:"note",text:"By default producer and collector share one coroutine — emit invokes the collector and waits, so it's sequential with natural backpressure (slow collector paces the producer, no pile-up). Add buffer() to run producer and collector concurrently (producer works ahead while the collector processes)."}]},{level:"senior",q:"How does buffer() improve throughput by decoupling stages?",a:[{t:"p",text:"`buffer()` inserts a channel between the producer and collector so they run *concurrently* in separate coroutines — the producer keeps emitting into the buffer while the collector processes at its own pace. This overlaps their work, so the total time approaches the *slower* of the two stages rather than their sum, improving throughput when both stages take real time."},{t:"code",title:"Concurrent stages",code:`flow {
    repeat(3) { emit(produce(it)); }   // e.g. 100ms each
}
.buffer()                              // producer and collector run concurrently
.collect { consume(it) }               // e.g. 100ms each
// Without buffer: ~600ms (serial). With buffer: ~400ms (overlapped)`},{t:"list",items:["**Concurrent execution** — producer and collector run in parallel coroutines via a channel.","**Overlap** — production of the next value overlaps consumption of the current.","**Throughput** — total time ≈ the slower stage, not the sum.","**Cost** — memory for the buffer and some coordination overhead."]},{t:"note",text:"buffer() puts a channel between producer and collector so they run concurrently — the producer emits ahead while the collector processes. Work overlaps, so total time ≈ the slower stage (not the sum), improving throughput when both stages take real time. Cost: buffer memory + coordination."}]},{level:"senior",q:"How does flowOn relate to buffering?",a:[{t:"p",text:"`flowOn(dispatcher)` changes the upstream's dispatcher, and because upstream and downstream now run on *different threads*, it introduces a *channel* (buffer) between them — so `flowOn` inherently adds concurrency/buffering at the boundary. Adjacent `flowOn` and `buffer` operators are *fused* by the runtime into a single efficient channel rather than stacking buffers."},{t:"list",items:["**`flowOn` implies a boundary** — upstream on one dispatcher, downstream on another, connected by a channel.","**Implicit buffer** — that channel provides some decoupling between the stages.","**Operator fusion** — consecutive `flowOn`/`buffer` merge into one channel with the combined config (no redundant buffers).","**Implication** — you rarely need `buffer()` right next to a `flowOn`; tune the fused capacity instead."]},{t:"note",text:"flowOn(dispatcher) runs upstream on another thread, introducing a channel (buffer) at the boundary — so it inherently adds concurrency. Adjacent flowOn/buffer operators are FUSED into one efficient channel (no stacked buffers). So you rarely need buffer() right beside a flowOn; adjust the fused capacity instead."}]},{level:"junior",q:"What is the cost of buffering, and how do you choose a capacity?",a:[{t:"p",text:"Buffering trades *memory* and *latency* for *throughput*. A larger buffer lets the producer run further ahead (better throughput, smoother bursts) but uses more memory and can increase latency (values sit in the buffer longer) and hide a slow consumer. Choose the smallest capacity that smooths your bursts; avoid `UNLIMITED` (risks OOM by removing backpressure)."},{t:"list",items:["**Memory** — the buffer holds up to N values; larger = more RAM.","**Latency** — buffered values wait before consumption; large buffers add delay.","**Throughput** — larger buffers absorb bursts and keep the producer busy.","**Choosing** — small buffer for smoothing; measure; never `UNLIMITED` for unbounded producers (OOM)."]},{t:"note",text:"Buffering trades memory + latency for throughput: bigger buffers smooth bursts and keep the producer busy but use more RAM, add latency (values wait), and can hide a slow consumer. Choose the smallest capacity that smooths your bursts; avoid UNLIMITED (removes backpressure → OOM risk)."}]},{level:"senior",q:"How do backpressure and cancellation relate in collectLatest?",a:[{t:"p",text:"`collectLatest`/`mapLatest`/`flatMapLatest` handle backpressure by *cancelling* rather than buffering or suspending: when a new value arrives, the current (cancellable) processing is cancelled and restarted. This only helps if the processing is genuinely cancellable (has suspension points) — a non-suspending CPU block won't be interrupted, so `collectLatest` won't drop it mid-way."},{t:"list",items:["**Cancel-based backpressure** — supersede stale work by cancelling it.","**Requires cancellable work** — suspension points let the cancellation take effect.","**Non-cancellable blocks** — a tight CPU loop won't be interrupted; add `ensureActive`/`yield`.","**vs suspend/buffer** — those preserve values; the *Latest operators discard in-progress work."]},{t:"note",text:"The *Latest operators (collectLatest/mapLatest/flatMapLatest) handle backpressure by cancelling in-progress processing when a new value arrives — but only if that processing is cancellable (has suspension points). A non-suspending CPU block won't be interrupted; add ensureActive/yield. Unlike suspend/buffer, they discard stale work rather than preserve it."}]},{level:"senior",q:"How do you decide between buffer, conflate, and collectLatest for a fast producer?",a:[{t:"p",text:"It comes down to whether you must process *every* value, only the *latest*, and whether processing is *cancellable*. `buffer` keeps all values (throughput, bounded memory); `conflate` keeps only the latest and lets current processing finish; `collectLatest` keeps only the latest and cancels in-progress processing."},{t:"table",headers:["Operator","Keeps","In-progress work"],rows:[["buffer(n)","all (up to n)","runs to completion"],["conflate","latest only","current finishes, intermediates skipped"],["collectLatest","latest only","cancelled and restarted"]]},{t:"list",items:["**`buffer`** — must process every value; overlap producer/consumer.","**`conflate`** — only latest matters; don't cancel current work (non-cancellable render).","**`collectLatest`** — only latest matters AND processing is cancellable (search).","**Decision** — every value? → buffer. Latest only? → conflate (keep current) or collectLatest (cancel current)."]},{t:"note",text:"buffer(n): keep all values, run each to completion (throughput). conflate: keep latest, let current work finish, skip intermediates. collectLatest: keep latest, CANCEL in-progress work. Decide: process every value → buffer; only latest → conflate (don't cancel) or collectLatest (cancel, if work is cancellable)."}]},{level:"senior",q:"How do you handle a burst of high-frequency sensor or scroll data?",a:[{t:"p",text:"For high-frequency streams (accelerometer, scroll offset), you usually don't need every sample — apply *rate reduction*: `sample(interval)` to take the latest value every N ms, `conflate()` to always process the newest, or `debounce` if you want to react only after motion stops. This protects the UI from a flood of updates and unnecessary recompositions."},{t:"code",title:"Rate-reducing a sensor stream",code:`sensorFlow
    .sample(100)                    // at most 10 updates/sec (latest each 100ms)
    .conflate()                     // or always process the newest available
    .collect { updateUi(it) }`},{t:"list",items:["**`sample(t)`** — periodic snapshots of the latest value (steady UI update rate).","**`conflate()`** — always process the most recent, skip the backlog.","**`debounce(t)`** — react only after the stream pauses (e.g. scroll settle).","**Move off main** — `flowOn` for any heavy processing, keep UI updates on Main."]},{t:"note",text:"For high-frequency data (sensors, scroll), reduce the rate: sample(t) (latest every N ms — steady update rate), conflate() (always newest, skip backlog), or debounce (react after it stops). Protects the UI from update floods/recomposition storms. Process heavy work with flowOn, update UI on Main."}]},{level:"senior",q:"Fast Room updates are causing UI jank. How do you diagnose and choose a strategy?",a:[{t:"p",text:"First confirm the cause: a Room `Flow` re-emits on every table change, so frequent writes cause rapid emissions and recompositions. Then reduce pressure at the right layer: `distinctUntilChanged` to skip no-op emissions, `conflate`/`sample` to cap the UI update rate, `debounce` if updates cluster, and ensure heavy mapping runs on `flowOn(Default)` — not on the main thread."},{t:"list",items:["**Diagnose** — is the Flow emitting more than the UI can render? Check emission frequency and mapping cost.","**`distinctUntilChanged`** — drop emissions that didn't actually change the rendered data.","**`conflate`/`sample`** — cap the UI update rate to what's perceivable (e.g. sample(16) ≈ per-frame).","**`flowOn(Default)`** — move expensive mapping off the main thread.","**Batch writes** — reduce the source frequency (transaction batching) if you control the writes."]},{t:"note",text:"Room Flows re-emit on every write, so rapid writes → emission/recomposition storms. Diagnose emission rate + mapping cost, then: distinctUntilChanged (drop no-ops), conflate/sample (cap UI update rate), debounce (clustered updates), flowOn(Default) for heavy mapping, and batch writes at the source if possible."}]},{level:"junior",q:"What happens when the consumer is faster than the producer?",a:[{t:"p",text:"Then there's *no backpressure problem* — the consumer simply waits (suspends) for the next value. In a default sequential flow, the collector's `collect` suspends until the producer emits again. No buffering, dropping, or conflation is needed; backpressure operators only matter when the *producer* outpaces the *consumer*."},{t:"list",items:["**Consumer waits** — `collect` suspends until the next emission.","**No overflow** — nothing accumulates; the producer sets the pace.","**No special operators** — `buffer`/`conflate` are unnecessary here.","**Symmetry** — backpressure is only about a fast producer / slow consumer."]},{t:"note",text:"If the consumer is faster, there's no backpressure issue — the collector just suspends waiting for the next emission (producer sets the pace, nothing accumulates). buffer/conflate/collectLatest matter only when the PRODUCER outpaces the consumer."}]},{level:"senior",q:"How do StateFlow and SharedFlow handle backpressure?",a:[{t:"p",text:"`StateFlow` handles it by *conflation* — it holds one value, so a slow collector just sees the latest (intermediates dropped), and emitters never suspend. `SharedFlow` handles it via its buffer + `onBufferOverflow`: SUSPEND applies backpressure (emitters wait), or DROP_OLDEST/DROP_LATEST drop values. So both hot flows avoid unbounded growth, but with different guarantees."},{t:"list",items:["**StateFlow** — conflated; latest-only; setting `.value` never suspends.","**SharedFlow** — buffer + overflow policy: SUSPEND (backpressure) or DROP (loss).","**No unbounded growth** — hot flows don't pile up indefinitely (unlike an UNLIMITED buffer).","**Implication** — StateFlow is lossy for intermediates by design; SharedFlow you configure."]},{t:"note",text:"StateFlow: conflation — one value, slow collectors see the latest, emitters never suspend (lossy for intermediates by design). SharedFlow: buffer + onBufferOverflow — SUSPEND (backpressure) or DROP_OLDEST/LATEST (loss). Both avoid unbounded growth, with different guarantees you choose for SharedFlow."}]},{level:"senior",q:"What is operator fusion, and how does it optimize buffer/flowOn chains?",a:[{t:"p",text:"The Flow runtime *fuses* adjacent context-changing and buffering operators (`flowOn`, `buffer`, `conflate`) into a single channel with the combined configuration, instead of creating a separate channel/coroutine per operator. This avoids redundant buffers and coroutine hops, making a chain like `.flowOn(IO).buffer()` as efficient as one boundary."},{t:"list",items:["**Fusion** — consecutive `flowOn`/`buffer`/`conflate` merge into one channel with merged config.","**Avoids overhead** — no stacked buffers or extra coroutine boundaries.","**Config wins** — the effective capacity/overflow is the combination (e.g. a later `conflate` overrides buffering).","**Practical** — don't over-add buffering operators; the runtime already optimizes adjacent ones."]},{t:"note",text:"Operator fusion merges adjacent flowOn/buffer/conflate into a single channel with combined config — no stacked buffers or extra coroutine hops. So .flowOn(IO).buffer() is one efficient boundary. Don't pile on buffering operators expecting more buffers; the runtime fuses them."}]},{level:"junior",q:"When is dropping values acceptable, and when must you keep them all?",a:[{t:"p",text:"Drop values when only the *current state* matters and intermediates are meaningless — UI state, progress, live position, sensor readings (conflate/collectLatest/sample). Keep every value when each represents a discrete *event or data item* you can't lose — transactions, messages, analytics events, items in a processing pipeline (buffer with SUSPEND, or a Channel)."},{t:"list",items:["**Drop OK** — latest-state scenarios: UI state, progress, cursor position, sensor value.","**Keep all** — discrete events/items: messages, transactions, analytics, work items.","**Operators** — conflate/collectLatest/sample (drop) vs buffer(SUSPEND)/Channel (keep).","**Ask** — 'if I skip an intermediate, does anything break?' If no, drop; if yes, keep."]},{t:"note",text:"Drop values when only the current state matters (UI state, progress, position, sensors — conflate/collectLatest/sample). Keep every value when each is a discrete event/item you can't lose (messages, transactions, analytics, work items — buffer(SUSPEND)/Channel). Ask: 'does skipping an intermediate break anything?'"}]},{level:"senior",q:"How does buffer capacity relate to Channel capacity?",a:[{t:"p",text:"`buffer()` is implemented with a `Channel` under the hood, so its capacity and overflow semantics *are* the channel's: `buffer(64)` uses a 64-capacity channel, `buffer(Channel.CONFLATED)` a conflated channel, `buffer(Channel.UNLIMITED)` an unlimited one, and the default `buffer()` uses a small default buffer. Understanding channels explains buffer behavior exactly."},{t:"list",items:["**Same primitive** — `buffer` inserts a `Channel` between producer and collector.","**Capacities map** — `RENDEZVOUS`/`CONFLATED`/`UNLIMITED`/`BUFFERED` all valid.","**`conflate()` = `buffer(CONFLATED)`** — a convenience for the conflated channel.","**Overflow** — the channel's `onBufferOverflow` (SUSPEND/DROP_*) drives buffer behavior."]},{t:"note",text:"buffer() is backed by a Channel — its capacity/overflow ARE the channel's: buffer(64)=64-capacity, buffer(CONFLATED)=conflate(), buffer(UNLIMITED)=unbounded, default=small buffer; onBufferOverflow is the channel's overflow policy. Knowing channels explains buffer semantics exactly."}]},{level:"junior",q:"What's the difference between conflate() and sample()?",a:[{t:"p",text:"Both reduce a fast stream to fewer values, but by different triggers. `conflate()` is *demand-driven* — it emits the latest value whenever the collector is ready (skipping whatever piled up while it was busy). `sample(interval)` is *time-driven* — it emits the latest value on a fixed timer, regardless of collector readiness. Use `sample` for a steady update rate; `conflate` to always give the collector the freshest value as fast as it can take them."},{t:"list",items:["**`conflate`** — emits latest when the collector is free (paced by the collector).","**`sample(t)`** — emits latest every `t` (paced by a timer).","**Rate** — `sample` gives a predictable cadence; `conflate` is as-fast-as-consumed.","**Choose** — steady periodic UI updates → sample; freshest value ASAP → conflate."]},{t:"note",text:"conflate() is demand-driven — emits the latest whenever the collector is ready (skips the backlog). sample(t) is time-driven — emits the latest every t regardless of the collector. sample for a steady cadence; conflate for the freshest value as fast as the collector can consume."}]},{level:"senior",q:"How do you rate-limit an event stream to protect the UI?",a:[{t:"p",text:"Pick the operator matching the desired behavior: `debounce(t)` (act after the burst stops — search input), `sample(t)` (steady snapshots — live counters), `throttleFirst` (act immediately then ignore — button clicks, not built-in), or `conflate` (always latest). The goal is to convert a flood of events into a manageable, UI-friendly rate without blocking the producer."},{t:"list",items:["**`debounce(t)`** — one emission after the stream is quiet for `t` (typing, resize).","**`sample(t)`** — periodic latest value (progress, telemetry).","**throttle-first** — immediate then cooldown (clicks); implement manually.","**`conflate`** — always process the newest as fast as the UI can.","**Keep the producer unblocked** — these drop/reduce rather than suspend the source."]},{t:"note",text:"Rate-limit events by intent: debounce(t) (after the burst stops — search/resize), sample(t) (steady snapshots — telemetry), throttle-first (immediate + cooldown — clicks, manual), conflate (always latest). Convert a flood into a UI-friendly rate without blocking the producer."}]},{level:"junior",q:"What are the signs you have a backpressure problem?",a:[{t:"p",text:"Watch for growing memory (a buffer or list accumulating unprocessed values), UI lag or ANRs (the collector can't keep up while the producer floods it), or stale/laggy UI (updates arriving long after the underlying data changed). These symptoms mean the producer is outpacing the consumer and you need a backpressure strategy."},{t:"list",items:["**Rising memory** — an unbounded buffer/queue growing over time.","**UI jank/ANR** — the main-thread collector overwhelmed by rapid emissions.","**Laggy/stale UI** — buffered values consumed late, so the UI trails reality.","**Diagnose** — measure emission rate vs processing time; check for `UNLIMITED` buffers and heavy per-value work."]},{t:"note",text:"Backpressure symptoms: growing memory (accumulating unprocessed values), UI jank/ANR (collector overwhelmed), and stale/laggy UI (values consumed late). Diagnose by comparing emission rate to processing time and checking for UNLIMITED buffers or heavy per-value work; then apply conflate/sample/buffer/collectLatest."}]},{level:"senior",q:"How does buffering interact with flow completion and exceptions?",a:[{t:"p",text:"With `buffer()`, the producer runs ahead in its own coroutine, so completion and exceptions are delivered *through the buffer* — the collector processes buffered values first, then sees completion or the exception in order. A producer exception cancels the buffer and propagates downstream; already-buffered values may or may not be consumed depending on timing, so don't rely on exact delivery counts under failure."},{t:"list",items:["**Ordered through the buffer** — the collector drains buffered values, then receives completion/exception.","**Producer exception** — propagates downstream (catchable with `catch`), cancelling the buffer.","**Timing-dependent** — under failure, how many buffered values were consumed isn't guaranteed.","**`catch` placement** — a `catch` after `buffer` handles upstream (including producer) exceptions."]},{t:"note",text:"With buffer(), the producer runs ahead, so the collector drains buffered values then receives completion/exception in order. A producer exception propagates downstream (catch after buffer handles it) and cancels the buffer; exact consumed counts under failure are timing-dependent — don't rely on them."}]},{level:"junior",q:"Do you always need a backpressure operator when using Flow?",a:[{t:"p",text:"No — Flow handles backpressure automatically via suspension, so for most cases you add *nothing*. You only reach for `buffer`/`conflate`/`sample`/`collectLatest` when you have a *specific* need: to overlap producer/consumer for throughput, to drop stale values, to cap an update rate, or to cancel superseded work. Adding them without a reason can cause unnecessary memory use or dropped data."},{t:"list",items:["**Default is fine** — sequential suspension handles the common case with natural backpressure.","**Add operators for a reason** — throughput (`buffer`), latest-only (`conflate`/`collectLatest`), rate-limit (`sample`/`debounce`).","**Don't over-apply** — needless buffers use memory; needless dropping loses data.","**Measure first** — confirm a real producer/consumer mismatch before optimizing."]},{t:"note",text:"No — Flow's suspension-based backpressure handles most cases with nothing added. Reach for buffer/conflate/sample/collectLatest only for a specific need (throughput, latest-only, rate-limit, cancel-stale). Adding them without cause wastes memory or drops data — measure for a real mismatch first."}]}],JS={id:"backpressure",title:"Buffering, Conflation & Backpressure",tagline:"How Flow handles backpressure via suspension, buffer() for throughput, conflate() for latest-wins, collectLatest vs conflate, and choosing a strategy.",tags:["Flow","Backpressure","buffer","conflate","collectLatest"],content:_S,qa:GS},YS=[{heading:"Context preservation — flows run where they're collected",blocks:[{t:"p",text:"By default, a flow's producer and operators run in **the coroutine context of the collector**. If you collect on `Dispatchers.Main`, the `flow { }` body and every `map`/`filter` also run on Main. This is called **context preservation**: the flow doesn't secretly hop threads. It's a deliberate safety property — you always know where your flow code executes, and emissions arrive on the collector's context (perfect for updating UI)."},{t:"p",text:"This is also why Flow **forbids changing context inside the builder** — you can't wrap `emit` in `withContext(IO)` inside `flow { }` (it throws 'Flow invariant is violated'). The framework enforces that context changes happen through the dedicated operator, `flowOn`, so the guarantees hold."}]},{heading:"flowOn — change the context of the UPSTREAM",blocks:[{t:"p",text:"`flowOn(dispatcher)` changes the context (usually the dispatcher/thread) of **everything upstream of it** — the operators and producer that come *before* it in the chain. Crucially it affects only upstream, not downstream, so you move heavy work off the main thread while the collector still receives values on its own context."},{t:"code",title:"flowOn moves upstream work off the main thread",code:`flow {
    val data = expensiveBlockingRead()   // runs on IO (below is flowOn(IO))
    emit(data)
}
.map { heavyTransform(it) }              // also runs on IO (upstream of flowOn)
.flowOn(Dispatchers.IO)                  // <-- everything ABOVE runs on IO
.map { it.toUiModel() }                  // runs on the COLLECTOR's context (Main)
.collect { render(it) }                  // Main — safe to touch UI`},{t:"list",items:["**Upstream vs downstream**: `flowOn` changes the context for operators *above* it; operators *below* it stay on the collector's context. So you place `flowOn(IO)` right after the heavy work and before the UI-facing operators.","**Multiple `flowOn`s** are allowed — each governs the segment above it up to the next `flowOn`, letting different stages run on different dispatchers.","**It implicitly buffers**: to run upstream on a different thread, `flowOn` hands values across a channel — so it also decouples producer and collector (a throughput side effect, as covered in the backpressure topic).","**Best practice**: in clean architecture the repository/data layer should already be main-safe (it does its own `withContext`), so you often don't need `flowOn` in the ViewModel — but it's the right tool when a flow's *producer* does blocking/CPU work and you want it off Main."]}]},{heading:"stateIn — convert a cold flow into a hot StateFlow",blocks:[{t:"p",text:"`stateIn` turns a **cold** flow into a **hot `StateFlow`**. Why you'd want this: a cold flow re-runs per collector, so if two things observe it you get duplicate work (two DB queries, two network calls). `stateIn` collects the upstream *once* in a given scope and multicasts the latest value to all collectors as shared state."},{t:"code",title:"The standard ViewModel derivation",code:`val uiState: StateFlow<UiState> = repository.observeData()   // cold Flow
    .map { data -> UiState.Content(data) }
    .stateIn(
        scope = viewModelScope,                                 // where collection runs
        started = SharingStarted.WhileSubscribed(5_000),        // when to start/stop upstream
        initialValue = UiState.Loading,                         // value before first emission
    )`},{t:"list",items:["**`scope`** — the coroutine scope that owns the single upstream collection (usually `viewModelScope`).","**`initialValue`** — StateFlow needs a current value immediately; this is it, shown until the upstream emits.","**`started`** — the sharing policy that controls when the upstream is actually collected (below).","**Result**: one upstream execution shared by all collectors, exposed as a `StateFlow` with `.value` — the idiomatic way to expose repository data as UI state."]}]},{heading:"SharingStarted — Eagerly, Lazily, WhileSubscribed",blocks:[{t:"table",headers:["Policy","Starts upstream","Stops upstream"],rows:[["`Eagerly`","immediately when stateIn/shareIn is created","only when the scope is cancelled"],["`Lazily`","when the first collector appears","only when the scope is cancelled"],["`WhileSubscribed(stopTimeout)`","when the first collector appears","stopTimeout ms after the LAST collector leaves"]]},{t:"p",text:"**`WhileSubscribed(5000)` is the Android default, and the 5000ms is deliberate.** It stops the upstream flow 5 seconds after the last collector disappears. The magic number exists because a **configuration change** briefly drops all collectors (the UI is recreated) — the 5s timeout keeps the expensive upstream (DB observation, location) alive *through* a rotation, but shuts it down for a real backgrounding (where the collector is gone for longer than 5s). It's the sweet spot between 'restart the upstream on every rotation' (0ms) and 'never stop, waste resources' (Eagerly)."},{t:"list",items:["**`Eagerly`** — upstream runs from creation, never stops until scope death. Fine for cheap, always-needed state; wasteful for expensive upstreams the UI isn't watching.","**`Lazily`** — starts on the first collector, then never stops. Rarely the right choice for UI.","**`WhileSubscribed(5000)`** — the recommended default for UI state: starts on demand, survives config changes, stops when genuinely backgrounded. Pairs with `collectAsStateWithLifecycle` / `repeatOnLifecycle(STARTED)` on the UI side so backgrounding stops collection → stops the upstream after 5s."]}]},{heading:"shareIn — convert to a hot SharedFlow",blocks:[{t:"p",text:"`shareIn` is `stateIn`'s sibling for **events/streams** rather than state: it turns a cold flow into a hot **`SharedFlow`** with configurable `replay`. Use it when you want to share an upstream among many collectors but there's no single 'current value' (or you want more than one replayed value)."},{t:"code",title:"shareIn for a shared event/data stream",code:`val sharedLocationUpdates: SharedFlow<Location> = locationFlow   // cold callbackFlow
    .shareIn(
        scope = externalScope,
        started = SharingStarted.WhileSubscribed(5_000),
        replay = 1,   // new collectors get the last known location
    )`},{t:"list",items:["**`stateIn` vs `shareIn`**: `stateIn` → `StateFlow` (always a current value, conflated, `replay = 1` effectively, needs `initialValue`); `shareIn` → `SharedFlow` (configurable replay, no required initial value, no conflation). Choose by whether the thing is *state* or a *stream/events*.","**Common use**: sharing one expensive upstream (a socket, a location listener wrapped in `callbackFlow`) among multiple consumers so it runs once — the 'single upstream, many collectors' optimization."]}]},{heading:"callbackFlow — bridging callback APIs into flows",blocks:[{t:"p",text:"Many Android APIs are **callback-based**: you register a listener and get invoked on events (`LocationManager`, sensors, `addSnapshotListener`, `TextWatcher`). `callbackFlow` wraps such an API into a Flow — turning push-callbacks into a collectable stream."},{t:"code",title:"The callbackFlow template — register, send, awaitClose",code:`fun locationUpdates(client: FusedLocationProviderClient): Flow<Location> = callbackFlow {
    val callback = object : LocationCallback() {
        override fun onLocationResult(result: LocationResult) {
            result.lastLocation?.let { trySend(it) }   // push into the flow (thread-safe)
        }
    }
    client.requestLocationUpdates(request, callback, Looper.getMainLooper())

    awaitClose {                                        // REQUIRED
        client.removeLocationUpdates(callback)          // unregister when collection stops
    }
}`},{t:"list",items:["**`trySend(value)`** — the thread-safe way to emit from inside a callback (which may fire on any thread). It's non-suspending and returns a result you can inspect (it can fail if the buffer is full).","**`awaitClose { }` is mandatory** — it suspends `callbackFlow` open until the collector cancels, and its block runs the cleanup (unregister the listener). Forgetting it leaks the listener *and* throws at runtime (callbackFlow requires it). This is the #1 callbackFlow interview point.","**Buffer/overflow**: `callbackFlow` uses a channel; you can set capacity and `onBufferOverflow` (e.g. `BufferOverflow.DROP_OLDEST` for high-frequency sensors where only the latest matters).","**Cold by default**: like any flow, it registers the listener *per collector*. To share one registration among many collectors, convert to hot with `shareIn`."]},{t:"note",text:"The three things to always say about callbackFlow: use `trySend` (thread-safe emit), always `awaitClose` (cleanup + keeps it open), and remember it's cold (one registration per collector — `shareIn` to share). Those three points answer nearly every callbackFlow question."}]}],QS=[{level:"junior",q:"What does flowOn do, and why can't you just use withContext inside a flow builder?",a:[{t:"p",text:"**The concept**: by default a flow runs where it's collected (context preservation) — if you collect on Main, the producer and operators run on Main too. `flowOn(dispatcher)` changes the context of everything **upstream** of it (the operators and producer *above* it in the chain), while everything downstream stays on the collector's context. So you place `flowOn(Dispatchers.IO)` after heavy work to run that work off the main thread, and the collector still receives values on Main to safely update UI."},{t:"code",title:"flowOn affects only what's above it",code:`flow { emit(blockingRead()) }     // runs on IO
    .map { heavyParse(it) }        // runs on IO (above flowOn)
    .flowOn(Dispatchers.IO)
    .map { it.toUiModel() }        // runs on collector's context (Main)
    .collect { render(it) }        // Main`},{t:"p",text:"**Why not `withContext(IO)` inside `flow { }`**: it's forbidden — the flow builder throws 'Flow invariant is violated' if you try to `emit` from a different context than the one the flow was collected in. Flow *guarantees* context preservation and sequential emission, and wrapping `emit` in a `withContext` would break that guarantee (emissions could arrive on a different thread than promised). `flowOn` is the sanctioned mechanism that changes upstream context *while keeping the invariant intact* (it does the thread handoff safely via an internal channel). So the rule is: never change context manually inside the builder; use `flowOn`."}]},{level:"junior",q:"What is stateIn and why would you use it in a ViewModel?",a:[{t:"p",text:"**The concept**: `stateIn` converts a *cold* flow into a hot `StateFlow`. Remember cold flows re-run per collector — so if a ViewModel exposed a cold flow and two things observed it (the UI plus a logger, or after a config change), you'd trigger the upstream work (a DB query, network call) twice. `stateIn` collects the upstream **once** in a scope and shares the latest value with all collectors as state."},{t:"code",title:"The idiomatic derivation",code:`val uiState: StateFlow<UiState> = repository.observeItems()
    .map { UiState.Content(it) }
    .stateIn(
        viewModelScope,
        SharingStarted.WhileSubscribed(5_000),
        UiState.Loading,
    )`},{t:"list",items:["It gives you a `StateFlow` — with a synchronous `.value`, a current value for new collectors, and conflated state semantics — which is exactly what a UI wants to observe.","It collects the upstream once (in `viewModelScope`), so multiple UI collectors don't duplicate the expensive work.","`initialValue` provides the state to show before the upstream emits (e.g. `Loading`).","The `started` policy (`WhileSubscribed(5000)`) controls when the upstream actually runs — starting on demand and stopping when the UI is truly gone."]}]},{level:"junior",q:"What is the difference between stateIn and shareIn?",a:[{t:"p",text:"**Both convert a cold flow to a hot one and share a single upstream among collectors — the difference is the output type and semantics.** `stateIn` produces a **`StateFlow`**: it always has a current value, is conflated (only latest), and needs an `initialValue`. `shareIn` produces a **`SharedFlow`**: no required current value, configurable `replay` (0, 1, or more), and no conflation."},{t:"p",text:"**Choose by whether the thing is *state* or a *stream/events***: use `stateIn` for state the UI observes (screen state, a value over time) — it needs a current value and 'latest wins' is correct. Use `shareIn` for sharing an event stream or a data source where there isn't a single 'current value', or where you want to control replay explicitly — e.g. broadcasting location updates from one shared listener to several consumers with `replay = 1` so new consumers get the last known location. In short: `stateIn` → StateFlow (state), `shareIn` → SharedFlow (streams/events)."}]},{level:"junior",q:"What is callbackFlow and when do you use it?",a:[{t:"p",text:"**The concept**: many Android APIs are *callback-based* — you register a listener and they call you back on events: `LocationManager`, sensors, Firebase's `addSnapshotListener`, `TextWatcher`, connectivity callbacks. `callbackFlow` wraps such an API into a `Flow`, converting the push-callback model into a stream you can collect and use with operators."},{t:"code",title:"The template",code:`fun connectivity(cm: ConnectivityManager): Flow<Boolean> = callbackFlow {
    val callback = object : ConnectivityManager.NetworkCallback() {
        override fun onAvailable(n: Network) { trySend(true) }
        override fun onLost(n: Network) { trySend(false) }
    }
    cm.registerDefaultNetworkCallback(callback)
    awaitClose { cm.unregisterNetworkCallback(callback) }   // cleanup — required
}`},{t:"list",items:["**`trySend(value)`** pushes a value into the flow from inside the callback — it's thread-safe (callbacks may fire on any thread) and non-suspending.","**`awaitClose { }` is mandatory**: it keeps the flow open (waiting for events) and runs cleanup — unregistering the listener — when the collector cancels. Forgetting it both leaks the listener and causes a runtime error.","Use it whenever you need to consume a callback API reactively — it lets you apply Flow operators (`debounce`, `map`, `distinctUntilChanged`) to callback events and integrates them with structured concurrency (collection stops → listener unregistered)."]}]},{level:"senior",q:"Explain SharingStarted.WhileSubscribed(5000). Why 5000, and how does it compare to Eagerly and Lazily?",a:[{t:"p",text:"**What it does**: `WhileSubscribed(stopTimeoutMillis)` is a sharing policy for `stateIn`/`shareIn` that starts the upstream when the first collector subscribes and **stops it `stopTimeoutMillis` after the last collector unsubscribes**, restarting it if a collector returns. It ties the expensive upstream's lifetime to whether anyone is actually watching."},{t:"p",text:"**Why 5000ms specifically**: a **configuration change** (rotation) tears down the UI and recreates it, which briefly drops all collectors — for a few hundred milliseconds there are zero subscribers. Without a timeout (i.e. `WhileSubscribed(0)`), the upstream would be *cancelled and cold-restarted* on every rotation: the DB query re-runs, the network re-observes, the user sees a flash of loading state. The 5-second window comfortably outlasts a config change, so the upstream survives rotation seamlessly — but for a *real* backgrounding (the app goes to the background and the lifecycle-aware collector stops for more than 5s), the upstream does stop, freeing resources. 5000ms is the empirical sweet spot between those two."},{t:"list",items:["**`Eagerly`** — upstream starts immediately when `stateIn` is created and runs until the scope (e.g. `viewModelScope`) is cancelled, regardless of collectors. Use for cheap state that's always needed; wasteful for expensive upstreams the UI might not be observing.","**`Lazily`** — starts on the first collector, then runs until scope cancellation (never stops on unsubscribe). Rarely ideal for UI — it doesn't free resources when backgrounded.","**`WhileSubscribed(5000)`** — the recommended default for UI state, because it's the only policy that both survives config changes *and* releases resources on genuine backgrounding. It relies on the UI collecting with lifecycle awareness (`collectAsStateWithLifecycle` / `repeatOnLifecycle(STARTED)`), so that backgrounding actually removes the collector and, after 5s, stops the upstream."]},{t:"p",text:"**The complete picture to convey**: `WhileSubscribed(5000)` + `stateIn(viewModelScope)` + `collectAsStateWithLifecycle` is a coordinated system — the lifecycle-aware collector drops the subscription on background, the 5s timeout ignores the momentary drop of rotation, and the upstream (DB/network/location) runs exactly when the user can see the result and stops shortly after they can't."}]},{level:"senior",q:"Why is awaitClose required in callbackFlow, and what happens if you forget it?",a:[{t:"p",text:"**Two reasons, both essential.** First, `callbackFlow` (built on a channel) would otherwise **complete immediately** after the builder block returns — but a callback API keeps calling back *after* registration, over time. `awaitClose { }` **suspends the flow open**, keeping it alive to receive future callbacks until the collector cancels. Without it, the flow ends right after registering the listener and you'd receive nothing. Second, `awaitClose`'s block is where you **unregister the listener** — it runs precisely when collection is cancelled (scope death, `take`, the collector leaving)."},{t:"list",items:["**What happens if you forget it**: `callbackFlow` actively checks for it and throws `IllegalStateException` ('Flow invariant is violated... awaitClose { } should be used') — so it's not silent, it fails fast. That's the framework forcing you to handle cleanup.","**The bug it prevents**: without unregistering, the listener outlives the flow's collection — a leaked `LocationListener`/`NetworkCallback`/Firebase listener that keeps firing (draining battery, holding references, possibly crashing when it touches a dead component). `awaitClose` guarantees the registration and unregistration are balanced by the collection lifecycle.","**Correct usage detail**: `awaitClose` must be the *last* thing in the builder, and everything before it is the setup; the cleanup lambda should undo exactly the setup (unregister the exact callback you registered)."]},{t:"p",text:"**The senior framing**: `callbackFlow` bridges a *manually-managed resource* (a registered listener) into structured concurrency, and `awaitClose` is the seam that makes the resource's lifetime follow the flow's collection lifetime — register on collect, unregister on cancel. It's the flow-world equivalent of `DisposableEffect`'s `onDispose` in Compose: the mandatory cleanup hook that prevents leaks."}]},{level:"senior",q:"A cold flow from your repository is being collected by two parts of the UI and causing duplicate network calls. How do you fix it?",a:[{t:"p",text:"**The root cause**: cold flows execute their producer *independently per collector*. Two collectors of the same cold repository flow = two separate executions = two network calls (or two DB observations). This is correct cold-flow behavior, not a bug in the flow — the fix is to **share a single upstream execution** among collectors by making it hot."},{t:"list",items:["**Primary fix — `stateIn`/`shareIn`**: in the ViewModel, collect the cold flow once and expose a hot flow. `repository.observeData().stateIn(viewModelScope, WhileSubscribed(5000), initial)` collects the upstream a single time and multicasts to all UI collectors — one network call, shared results. Use `stateIn` if it's state, `shareIn` if it's a stream/events.","**Make sure both collectors observe the SAME hot instance**: the `StateFlow` must be a single property on the ViewModel that both UI pieces read — not two calls that each create a new `stateIn`. A common mistake is calling `.stateIn(...)` in a getter or per-collection, which recreates the sharing each time and defeats it. Store it in a `val`.","**If sharing must span ViewModels/screens** (two screens want the same live data): move the single-source-of-truth *down to the data layer*. Have the repository hold the shared hot flow (e.g. a cache `MutableStateFlow`, or the DB as SSOT that both observe) so any number of ViewModels observing it triggers just one network refresh. This is the more robust architectural fix.","**For request de-duplication specifically**: the repository can also guard with a shared in-flight job/mutex so even concurrent *refresh* calls collapse into one network request."]},{t:"p",text:"**The principle**: cold = per-collector execution; when you want 'observe the same thing in many places, compute once', convert to hot at the appropriate layer — `stateIn`/`shareIn` in the ViewModel for one screen, or a shared hot source / SSOT in the repository for cross-screen sharing. Naming *both* levels (and why storing the `stateIn` result in a `val` matters) is the complete answer."}]},{level:"senior",q:"What does flowOn affect, what does it not affect, and can you chain multiple?",a:[{t:"p",text:"`flowOn(dispatcher)` changes the context of everything *upstream* of it (the producer and operators above), but *not* downstream (operators below and the collector stay on the collector's context). You can chain multiple `flowOn`s — each governs the segment above it up to the next `flowOn` — letting different stages run on different dispatchers."},{t:"code",title:"Multiple flowOn segments",code:`flow { emit(readFile()) }        // runs on IO (nearest flowOn above collector chain)
    .map { parse(it) }           // runs on Default
    .flowOn(Dispatchers.Default) // ^ governs map + producer? No — see below
    .map { toUiModel(it) }       // runs on the collector's context (downstream)
    .flowOn(Dispatchers.IO)      // governs the producer + first map
    .collect { render(it) }      // collector's own context (e.g. Main)`},{t:"list",items:["**Affects upstream** — the producer and operators above the `flowOn`.","**Not downstream** — operators below it and the collector keep the collector's context.","**Chainable** — each `flowOn` controls the segment above it up to the next one.","**Read bottom-up** — the nearest `flowOn` above an operator determines its context."]},{t:"note",text:"flowOn(dispatcher) changes the context of everything UPSTREAM of it (producer + operators above), never downstream (below it and the collector stay on the collector's context). Chain multiple flowOns — each governs the segment above it up to the next. The nearest flowOn above an operator sets its context."}]},{level:"senior",q:"What are the SharingStarted strategies (Eagerly, Lazily, WhileSubscribed)?",a:[{t:"p",text:"`SharingStarted` controls *when* the shared upstream of `shareIn`/`stateIn` starts and stops. `Eagerly` starts immediately and never stops; `Lazily` starts on the first subscriber and never stops; `WhileSubscribed(stopTimeout)` starts on the first subscriber and stops after the last leaves (plus a timeout). `WhileSubscribed(5000)` is the standard for UI state."},{t:"table",headers:["Strategy","Starts","Stops"],rows:[["Eagerly","immediately","never (until scope cancelled)"],["Lazily","first subscriber","never"],["WhileSubscribed(t)","first subscriber","t ms after last leaves"]]},{t:"list",items:["**`Eagerly`** — upstream runs even with no collectors; wastes work if unused; for always-needed data.","**`Lazily`** — starts once someone subscribes, then stays on; for data that should keep running after first use.","**`WhileSubscribed(5000)`** — starts/stops with subscribers (+5s to survive config change); the efficient default for UI state.","**`replayExpirationMillis`** — WhileSubscribed can also clear the replay cache after stopping."]},{t:"note",text:"SharingStarted: Eagerly (start now, never stop — always-needed data), Lazily (start on first subscriber, never stop), WhileSubscribed(t) (start on first subscriber, stop t ms after the last leaves — the efficient UI default, 5000ms survives config change). Governs when shareIn/stateIn's upstream runs."}]},{level:"senior",q:"What are shareIn's parameters, and how does replay behave?",a:[{t:"p",text:"`shareIn(scope, started, replay)` turns a cold flow into a hot `SharedFlow`. `scope` is where the upstream runs; `started` (a `SharingStarted`) controls start/stop; `replay` is how many recent values a *new* subscriber immediately receives. `replay = 0` gives late subscribers nothing (events); `replay = 1` caches the latest (like a StateFlow without the value accessor)."},{t:"code",title:"shareIn",code:`val shared: SharedFlow<Data> = repository.stream()
    .shareIn(
        scope = viewModelScope,
        started = SharingStarted.WhileSubscribed(5000),
        replay = 1,      // new subscribers get the last value
    )`},{t:"list",items:["**`scope`** — where the single shared upstream collection runs.","**`started`** — Eagerly/Lazily/WhileSubscribed(t).","**`replay`** — recent values delivered to new subscribers on subscription.","**Result** — a hot `SharedFlow`; one upstream execution multicast to all collectors."]},{t:"note",text:"shareIn(scope, started, replay) makes a cold flow hot (SharedFlow): scope runs the single upstream, started (Eagerly/Lazily/WhileSubscribed) controls start/stop, replay is how many recent values new subscribers get (0 for events, 1 to cache the latest). One upstream execution multicast to all collectors."}]},{level:"senior",q:"What is channelFlow, and how does it differ from callbackFlow?",a:[{t:"p",text:"`channelFlow { }` is a flow builder with a `ProducerScope` where you `send`/`trySend` values, and can do so from *multiple coroutines/contexts* concurrently (it's channel-backed). `callbackFlow { }` is a specialized `channelFlow` designed for *callback bridging* — same `send`/`awaitClose`, but semantically intended to wrap a callback API and clean it up in `awaitClose`."},{t:"code",title:"channelFlow and callbackFlow",code:`channelFlow {
    launch { send(sourceA()) }        // concurrent producers
    launch { send(sourceB()) }
    awaitClose { }
}
callbackFlow {                         // callback bridge
    val listener = Listener { trySend(it) }
    api.register(listener)
    awaitClose { api.unregister(listener) }   // cleanup
}`},{t:"list",items:["**`channelFlow`** — general; `send` from multiple coroutines/contexts concurrently.","**`callbackFlow`** — same mechanics, intended for callback/listener bridging with `awaitClose` cleanup.","**Both channel-backed** — slightly heavier than `flow { }`; use when you need concurrent emission.","**`awaitClose`** — required to keep the flow open until the source is done and to clean up."]},{t:"note",text:"channelFlow { } gives a ProducerScope to send/trySend from multiple coroutines/contexts (channel-backed). callbackFlow { } is a channelFlow specialized for bridging callback APIs — same send/awaitClose, but semantically for wrapping a listener and unregistering it in awaitClose. Both need awaitClose to stay open and clean up."}]},{level:"senior",q:"What is the difference between send and trySend, and when does trySend fail?",a:[{t:"p",text:"In a `channelFlow`/`callbackFlow`, `send` is a *suspend* function that waits for buffer space (backpressure), while `trySend` is *non-suspending* — it returns a `ChannelResult` indicating success or failure. `trySend` fails when the buffer is full (and overflow is SUSPEND) or the channel is closed. From a *non-suspending callback*, you must use `trySend` (you can't call `send` there)."},{t:"code",title:"trySend in a callback",code:`callbackFlow {
    val listener = Listener { value ->
        trySend(value)   // non-suspend; may fail if buffer full — often fine to drop or use a buffer
    }
    api.register(listener)
    awaitClose { api.unregister(listener) }
}.buffer(Channel.CONFLATED)   // avoid trySend failures for latest-value sources`},{t:"list",items:["**`send`** — suspend; waits for space; use from a coroutine.","**`trySend`** — non-suspend; returns success/failure; use from a synchronous callback.","**`trySend` fails** — buffer full (SUSPEND overflow) or channel closed.","**Mitigate** — add a buffer or use `Channel.CONFLATED`/DROP overflow so `trySend` succeeds for latest-value sources."]},{t:"note",text:"send is suspend (waits for buffer space); trySend is non-suspending (returns ChannelResult) and fails when the buffer is full (SUSPEND overflow) or the channel is closed. Use trySend from non-suspend callbacks. Add a buffer/CONFLATED/DROP overflow so trySend doesn't fail for latest-value sources."}]},{level:"senior",q:"How do you bridge a listener/observer API (like location or sensors) to a Flow?",a:[{t:"p",text:"Use `callbackFlow { }`: register the listener inside, emit each callback value with `trySend`, and unregister in `awaitClose { }` (which suspends until the flow is cancelled/closed). This turns an imperative callback API into a cold, cancellable Flow that cleans itself up — no leaks."},{t:"code",title:"Location updates as a Flow",code:`fun locationUpdates(): Flow<Location> = callbackFlow {
    val callback = object : LocationCallback() {
        override fun onLocation(loc: Location) { trySend(loc) }
    }
    client.requestUpdates(callback)
    awaitClose { client.removeUpdates(callback) }   // cleanup on cancel/close
}.flowOn(Dispatchers.Default)`},{t:"list",items:["**Register inside** — set up the listener in the `callbackFlow` block.","**`trySend`** — emit each callback value (non-suspend, from the callback).","**`awaitClose`** — unregister when the collector cancels; without it, a leak (and the flow never completes).","**Cold + cancellable** — each collector gets its own registration; cancellation cleans up."]},{t:"note",text:"Wrap a listener API in callbackFlow: register inside, emit via trySend from the callback, and unregister in awaitClose { } (suspends until cancelled). This makes an imperative callback into a cold, cancellable, leak-free Flow. Forgetting awaitClose leaks the listener and the flow never completes."}]},{level:"junior",q:"How do you interop between Flow and LiveData?",a:[{t:"p",text:"Convert a `Flow` to `LiveData` with `.asLiveData()` (from lifecycle-livedata-ktx), and a `LiveData` to a `Flow` with `.asFlow()`. This eases incremental migration — a repository can expose `Flow` while a legacy Fragment observes `LiveData`, or vice versa."},{t:"code",title:"Flow ↔ LiveData",code:`val liveData: LiveData<UiState> = viewModel.uiStateFlow.asLiveData()     // Flow -> LiveData
val flow: Flow<User> = someLiveData.asFlow()                            // LiveData -> Flow
// asLiveData(context, timeout) lets you set the coroutine context/timeout`},{t:"list",items:["**`Flow.asLiveData()`** — collects the flow and exposes it as lifecycle-aware `LiveData`.","**`LiveData.asFlow()`** — observe a `LiveData` as a `Flow`.","**Migration aid** — bridge old and new observable types during a transition.","**Prefer StateFlow** for new code, but interop keeps mixed codebases working."]},{t:"note",text:"Flow.asLiveData() (Flow → lifecycle-aware LiveData, optional context/timeout) and LiveData.asFlow() (LiveData → Flow) bridge the two — handy for incremental migration in a mixed codebase. Prefer StateFlow for new code, but interop keeps old observers working."}]},{level:"senior",q:"What are stateIn's three parameters, and how do you choose them?",a:[{t:"p",text:"`stateIn(scope, started, initialValue)`: `scope` runs the single upstream collection (usually `viewModelScope`); `started` (a `SharingStarted`) controls when the upstream is active; `initialValue` is the StateFlow's value before the upstream emits (typically a `Loading` state). Choosing `WhileSubscribed(5000)` for `started` is the standard for efficient, config-change-safe UI state."},{t:"list",items:["**`scope`** — `viewModelScope` so the upstream is cancelled with the ViewModel.","**`started`** — `WhileSubscribed(5000)` (efficient), `Eagerly` (always on), or `Lazily`.","**`initialValue`** — the value before the first upstream emission (e.g. `UiState.Loading`).","**Why an initial value** — StateFlow must always have a current value."]},{t:"code",title:"stateIn",code:`repo.observe()
    .map { UiState.Content(it) }
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), UiState.Loading)`},{t:"note",text:"stateIn(scope, started, initialValue): scope (viewModelScope — cancels with the VM), started (WhileSubscribed(5000) for efficient config-safe UI state, or Eagerly/Lazily), initialValue (value before the first emission, e.g. Loading — required since StateFlow always has a value)."}]},{level:"senior",q:"How do you build a repository flow that observes the database and refreshes from the network?",a:[{t:"p",text:"Make the *database* the single source of truth: expose the Room `Flow` as the observable stream, and trigger a network refresh separately that *writes into the DB* — the Room flow then re-emits automatically. This is the offline-first pattern: the UI observes the DB, the network updates the DB, and the flow propagates changes."},{t:"code",title:"DB as source of truth",code:`fun observeUser(id: String): Flow<User> = flow {
    emitAll(dao.observeUser(id))          // Room Flow — single source of truth
}
suspend fun refreshUser(id: String) {
    val fresh = api.getUser(id)
    dao.upsert(fresh)                     // write to DB -> the flow above re-emits
}
// UI collects observeUser; a refresh() call updates the DB and the UI updates reactively`},{t:"list",items:["**DB is the source of truth** — the UI observes the Room `Flow`.","**Network writes to DB** — refresh fetches and upserts; the flow re-emits.","**Decoupled** — reading (flow) and refreshing (suspend) are separate concerns.","**Offline-first** — cached data shows immediately; network updates flow through."]},{t:"note",text:"Make the DB the single source of truth: expose the Room Flow for reads, and have network refresh write into the DB (upsert) — the Room flow re-emits automatically. UI observes the DB flow; refresh() updates the DB and the UI updates reactively. The offline-first pattern."}]},{level:"senior",q:"How do you turn a one-shot suspend call into a refreshable StateFlow?",a:[{t:"p",text:"Drive it with a *trigger* flow: a `MutableStateFlow` (or `Channel`) whose changes cause a re-fetch via `flatMapLatest`/`mapLatest`, then `stateIn` the result. Emitting a new trigger value (on refresh) restarts the fetch, giving a StateFlow that re-loads on demand."},{t:"code",title:"Trigger-driven refresh",code:`private val refreshTrigger = MutableStateFlow(0)
val state: StateFlow<UiState> = refreshTrigger
    .flatMapLatest {
        flow {
            emit(UiState.Loading)
            emit(runCatching { repo.load() }.fold({ UiState.Content(it) }, { UiState.Error }))
        }
    }
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), UiState.Loading)

fun refresh() { refreshTrigger.value++ }   // re-runs the fetch`},{t:"list",items:["**Trigger flow** — a `MutableStateFlow`/`Channel` whose changes drive fetches.","**`flatMapLatest`** — each trigger cancels the prior fetch and starts a new one.","**`stateIn`** — expose the result as UI state with an initial value.","**`refresh()`** — bump the trigger to re-load."]},{t:"note",text:"Drive a one-shot suspend call with a trigger flow (MutableStateFlow/Channel): flatMapLatest maps each trigger to a fetch flow (Loading → Content/Error), then stateIn exposes it. refresh() bumps the trigger to re-run (flatMapLatest cancels the prior fetch). A refreshable StateFlow from a suspend function."}]},{level:"senior",q:"How do you share an expensive upstream (like a WebSocket) across the whole app?",a:[{t:"p",text:"Create the flow once (e.g. a `callbackFlow` wrapping the socket) and `shareIn` it in an *application-scoped* `CoroutineScope`, using `SharingStarted.WhileSubscribed` so the socket connects only while something is collecting and disconnects when nothing is. Provide this single shared flow via DI so all consumers share one connection."},{t:"code",title:"App-wide shared socket flow",code:`class SocketRepository(appScope: CoroutineScope) {
    val messages: SharedFlow<Message> = socketFlow()   // callbackFlow wrapping the socket
        .shareIn(appScope, SharingStarted.WhileSubscribed(5000), replay = 0)
}`},{t:"list",items:["**Create once, share** — one `shareIn` in an app/singleton scope; inject the shared flow.","**`WhileSubscribed`** — connect only while collected; disconnect when idle (saves battery/data).","**`replay`** — 0 for live messages, or a small cache if late subscribers need recent history.","**Single connection** — all consumers multicast off one socket, not one per screen."]},{t:"note",text:"Wrap the socket in a callbackFlow and shareIn it in an application-scoped CoroutineScope with WhileSubscribed — the socket connects only while collected and disconnects when idle (saves battery/data). Inject the shared flow via DI so all consumers multicast off ONE connection. replay 0 for live messages."}]},{level:"senior",q:"How does the replay value in shareIn affect late subscribers?",a:[{t:"p",text:"`replay` is the number of most-recent emissions a *new* subscriber receives immediately upon subscribing. `replay = 0` means late subscribers get only *future* emissions (nothing from the past — right for events). `replay = 1` gives them the latest value (right for 'current state' sharing). Larger replay caches more history for late joiners, at a memory cost."},{t:"list",items:["**`replay = 0`** — late subscribers get only future values (events).","**`replay = 1`** — late subscribers get the latest value (state-like).","**`replay = n`** — the last n values are cached and replayed.","**Memory** — larger replay holds more values; keep it minimal."]},{t:"note",text:"replay = how many recent emissions a NEW subscriber gets on subscribing: 0 (only future values — events), 1 (the latest — state-like sharing), n (last n cached, more memory). Choose by whether late joiners need past values, keeping it minimal for memory."}]},{level:"senior",q:"When do you use callbackFlow versus suspendCancellableCoroutine?",a:[{t:"p",text:"Use `suspendCancellableCoroutine` for a *one-shot* callback (a single result → a suspend function). Use `callbackFlow` for a callback that fires *multiple times* (a stream of values → a Flow). The distinction is single result vs ongoing stream — matching the callback's cardinality."},{t:"list",items:["**`suspendCancellableCoroutine`** — one callback invocation → one suspend result (`resume` once).","**`callbackFlow`** — repeated callbacks → a stream of emissions (`trySend` many times).","**Both support cleanup** — `invokeOnCancellation` vs `awaitClose`.","**Choose by cardinality** — one value (suspend fn) vs many values (Flow)."]},{t:"note",text:"suspendCancellableCoroutine for a ONE-shot callback → suspend function (resume once, invokeOnCancellation cleanup). callbackFlow for a callback that fires MANY times → Flow (trySend repeatedly, awaitClose cleanup). Match the callback's cardinality: one result vs ongoing stream."}]},{level:"senior",q:"How do you inject and switch dispatchers for a flow in a testable way?",a:[{t:"p",text:"Inject the dispatcher (a `CoroutineDispatcher` parameter with a `Dispatchers.IO` default) and use it in `flowOn(injectedDispatcher)`, rather than hardcoding `Dispatchers.IO`. Tests can then pass a `TestDispatcher` so the flow runs on the virtual clock deterministically."},{t:"code",title:"Injectable flow dispatcher",code:`class Repo(
    private val io: CoroutineDispatcher = Dispatchers.IO,
) {
    fun stream(): Flow<Data> = flow { emitAll(dao.observe()) }.flowOn(io)
}
// Test: Repo(StandardTestDispatcher(testScheduler))`},{t:"list",items:["**Inject the dispatcher** — constructor param with a production default.","**`flowOn(injected)`** — use it instead of a hardcoded `Dispatchers.IO`.","**Test dispatcher** — pass a `TestDispatcher` sharing the `testScheduler`.","**Deterministic** — the flow runs on the virtual clock in tests."]},{t:"note",text:"Inject the dispatcher (constructor param, default Dispatchers.IO) and use flowOn(injected) instead of hardcoding — tests pass a TestDispatcher on the shared testScheduler so the flow runs deterministically on the virtual clock. Same dispatcher-injection principle as suspend functions."}]},{level:"junior",q:"Should you put flowOn(IO) in the repository or collect the flow on IO?",a:[{t:"p",text:"Put `flowOn(Dispatchers.IO)` in the *repository* (near the producer) so the flow is *main-safe*: the upstream I/O runs on IO regardless of where it's collected, and the UI can collect on Main without wrapping. Collecting on IO from the UI is wrong — the collector (and any UI update) would be off the main thread."},{t:"list",items:["**`flowOn(IO)` in the repository** — the producer's I/O runs on IO; the flow is main-safe.","**Collect on Main** — the UI collects and updates on the main thread.","**Don't collect on IO** — UI updates must be on Main; `flowOn` handles upstream threading.","**Separation** — the data layer owns threading; the UI stays declarative."]},{t:"note",text:"Put flowOn(Dispatchers.IO) in the repository (near the producer) so the flow is main-safe — upstream I/O runs on IO regardless of collection site, and the UI collects on Main without wrapping. Don't collect on IO (UI updates must be on Main). The data layer owns threading."}]},{level:"senior",q:"How do you implement a cache-then-network (NetworkBoundResource) flow?",a:[{t:"p",text:"Emit the cached data first, then fetch from the network, update the cache, and let the cache flow re-emit the fresh data — optionally wrapping emissions in a `Resource`/state type (Loading/Success/Error). The pattern gives instant cached content followed by a refresh, all as one flow."},{t:"code",title:"Cache-then-network as a flow",code:`fun user(id: String): Flow<Resource<User>> = flow {
    emit(Resource.Loading)
    val cached = dao.get(id)
    if (cached != null) emit(Resource.Success(cached))   // show cache immediately
    try {
        val fresh = api.getUser(id)
        dao.upsert(fresh)
        emit(Resource.Success(fresh))                     // then fresh
    } catch (e: IOException) {
        emit(Resource.Error(e, cached))                   // error, keep cache
    }
}`},{t:"list",items:["**Emit cache first** — instant content from the DB.","**Fetch + update cache** — network writes to DB; emit fresh data.","**Wrap in `Resource`** — Loading/Success/Error states the UI renders.","**Robust** — on network error, keep showing cached data with an error indicator."]},{t:"note",text:"Cache-then-network: emit Loading, emit cached data immediately (instant content), fetch from network, update the DB, emit fresh data; on error emit Error while keeping the cache. Wrap in a Resource (Loading/Success/Error) type. Instant cached UI + background refresh as one flow (the NetworkBoundResource pattern)."}]},{level:"senior",q:"How do you create an interval/ticker flow shared across collectors?",a:[{t:"p",text:"Build a `flow` that emits on a `delay` loop, then `shareIn` it so all collectors share one timer instead of each starting their own. This is useful for a clock, a countdown, or periodic polling that multiple UI parts observe."},{t:"code",title:"Shared ticker",code:`val tick: SharedFlow<Long> = flow {
    while (true) { emit(System.currentTimeMillis()); delay(1000) }
}.shareIn(appScope, SharingStarted.WhileSubscribed(), replay = 1)`},{t:"list",items:["**Interval flow** — `while (true) { emit(...); delay(t) }`.","**`shareIn`** — one shared timer for all collectors (not one per collector).","**`WhileSubscribed`** — the timer runs only while observed.","**`replay = 1`** — new collectors get the latest tick immediately."]},{t:"note",text:"Build an interval flow (while(true){ emit; delay(t) }) and shareIn it so all collectors share ONE timer (not one each). WhileSubscribed runs it only while observed; replay=1 gives new collectors the latest tick. Good for a clock/countdown/polling observed by multiple UI parts."}]},{level:"junior",q:"What are common callbackFlow mistakes?",a:[{t:"p",text:"The frequent bugs are: forgetting `awaitClose` (the flow completes immediately or leaks the listener because it's never unregistered), using `send` from a non-suspend callback (won't compile — use `trySend`), and ignoring `trySend` failures when the buffer is full. Each breaks the bridge in a subtle way."},{t:"list",items:["**Missing `awaitClose`** — the builder ends right away (flow completes) or the listener leaks; always `awaitClose { unregister }`.","**`send` in a callback** — callbacks are non-suspend; use `trySend`.","**Ignoring `trySend` result** — under a full buffer, emissions silently fail; add a buffer/CONFLATED/DROP policy.","**Heavy work in the callback** — keep it light; do processing downstream with operators."]},{t:"note",text:"callbackFlow pitfalls: forgetting awaitClose (flow ends immediately / listener leaks — always unregister there), using send instead of trySend in a non-suspend callback, and ignoring trySend failures on a full buffer (add buffer/CONFLATED/DROP). Keep callback work light; process downstream."}]},{level:"senior",q:"How do you share one flow across multiple ViewModels or app-wide?",a:[{t:"p",text:"Expose the shared flow from a *singleton* (a repository injected via DI) and `shareIn`/`stateIn` it in an application-scoped `CoroutineScope`, so every ViewModel collecting it observes the same hot upstream. This avoids each ViewModel starting its own cold collection (duplicate work) and keeps a single source of truth."},{t:"code",title:"App-scoped shared flow via DI",code:`@Singleton
class SessionRepository @Inject constructor(
    @ApplicationScope private val appScope: CoroutineScope,
) {
    val currentUser: StateFlow<User?> = userSource()
        .stateIn(appScope, SharingStarted.WhileSubscribed(5000), null)
}
// Any ViewModel injects SessionRepository and collects currentUser`},{t:"list",items:["**Singleton repository** — owns and shares the flow via DI.","**App-scoped `shareIn`/`stateIn`** — one hot upstream for all consumers.","**No duplicate work** — ViewModels multicast off the shared flow.","**Single source of truth** — e.g. current user/session across the app."]},{t:"note",text:"Expose the flow from a @Singleton repository and shareIn/stateIn it in an application-scoped CoroutineScope, injected via DI — every ViewModel collects the same hot upstream (no per-VM duplicate cold collection). One source of truth app-wide (e.g. current user/session)."}]},{level:"senior",q:"How does WhileSubscribed compare to Eagerly for a hot upstream, in practice?",a:[{t:"p",text:"`WhileSubscribed(5000)` keeps the upstream active only while there are collectors (plus 5s to survive config changes), so it stops expensive work (network, DB observation) when the UI is gone — saving battery/data. `Eagerly` runs the upstream from creation forever (until the scope dies), so it wastes resources when nobody's watching, but guarantees the latest value is always ready."},{t:"list",items:["**`WhileSubscribed(5000)`** — start/stop with subscribers; efficient; the recommended default for UI state.","**`Eagerly`** — always running; wasteful if unobserved, but data is always fresh/ready.","**Config change** — the 5s timeout bridges rotation without restarting the upstream.","**Choose Eagerly** — only when you truly need the upstream always active (rare for UI)."]},{t:"note",text:"WhileSubscribed(5000) runs the upstream only while collected (+5s to survive rotation) — stops expensive work when the UI is gone (battery/data savings), the recommended UI-state default. Eagerly runs forever from creation (wasteful if unobserved, but always ready). Prefer WhileSubscribed for UI."}]},{level:"junior",q:"Why can't you just use withContext inside a flow builder to change threads?",a:[{t:"p",text:"Flow enforces context preservation: emissions must come from the builder's own context. Wrapping `emit` in `withContext(IO)` emits from a different context and throws `IllegalStateException` ('Flow invariant is violated'). The correct tool is `flowOn(IO)`, which changes the *entire upstream's* context safely without you switching around `emit`."},{t:"code",title:"Use flowOn, not withContext",code:`// WRONG
flow { withContext(Dispatchers.IO) { emit(load()) } }   // throws
// RIGHT
flow { emit(load()) }.flowOn(Dispatchers.IO)`},{t:"list",items:["**Context preservation** — `emit` must run in the builder's context.","**`withContext` around `emit`** — violates the invariant, throws.","**`flowOn`** — the sanctioned way to move the upstream's context.","**Multiple contexts** — use `channelFlow` if you must emit from different contexts."]},{t:"note",text:"Flow's context-preservation invariant requires emit to run in the builder's context — withContext(IO) { emit } emits from a different context and throws IllegalStateException. Use flowOn(IO) (moves the whole upstream) instead; use channelFlow if you genuinely must emit from multiple contexts."}]},{level:"senior",q:"How do you keep a shared flow alive briefly across a configuration change?",a:[{t:"p",text:"Use `SharingStarted.WhileSubscribed(stopTimeoutMillis = 5000)`. When the last collector unsubscribes (e.g. the Activity is recreating during rotation), the upstream stays active for the timeout before stopping — so the new Activity re-subscribes within the window and the upstream *isn't* restarted, avoiding a redundant network/DB call on every rotation."},{t:"list",items:["**5s timeout** — spans the brief gap while the UI recreates.","**No restart on rotation** — the upstream keeps running through the gap; the new collector reuses it.","**Avoids refetch** — without it, rotation would restart the upstream (duplicate work).","**Also `replayExpirationMillis`** — controls when the replay cache is cleared after stopping."]},{t:"note",text:"SharingStarted.WhileSubscribed(5000): when the last collector leaves, the upstream stays alive 5s before stopping — spanning the rotation gap so the recreated UI re-subscribes without restarting the upstream (no refetch per rotation). replayExpirationMillis controls replay-cache clearing after stop."}]},{level:"junior",q:"How do you convert a Flow<T> into a StateFlow for the UI, and what must you provide?",a:[{t:"p",text:"Call `.stateIn(scope, started, initialValue)`. You must provide an *initial value* because a `StateFlow` always has a current value (the UI needs something to render before the first emission — typically `Loading`). The scope and `SharingStarted` control the upstream's lifetime and sharing."},{t:"code",title:"Flow to StateFlow",code:`val uiState: StateFlow<UiState> = repo.observe()
    .map(::toUiState)
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), UiState.Loading)`},{t:"list",items:["**`stateIn`** — collects the cold flow once and exposes a hot `StateFlow`.","**Initial value required** — StateFlow always has a value; use `Loading`/`Empty`.","**Scope + started** — `viewModelScope` + `WhileSubscribed(5000)` for UI state.","**Shares upstream** — all collectors observe one execution."]},{t:"note",text:"Use .stateIn(scope, started, initialValue) — the initial value is required because a StateFlow always has a current value (render Loading before the first emission). viewModelScope + WhileSubscribed(5000) is the UI-state standard; it shares one upstream execution among collectors."}]}],$S={id:"context-conversion",title:"Context, flowOn & Conversions",tagline:"Context preservation, flowOn for threading, stateIn/shareIn to go hot, SharingStarted policies (WhileSubscribed 5000), and callbackFlow with awaitClose.",tags:["flowOn","stateIn","shareIn","callbackFlow","SharingStarted"],content:YS,qa:QS},XS=[{heading:"How exceptions travel through a flow",blocks:[{t:"p",text:"An exception thrown anywhere in a flow — in the producer, in an operator like `map`, or in the collector's block — **propagates downstream** and, if unhandled, surfaces at the terminal operator (`collect`), where it can be caught with an ordinary try/catch or the `catch` operator. The important principle Flow enforces is **exception transparency**: operators should not hide exceptions from downstream, and you handle them declaratively rather than wrapping random pieces in try/catch."},{t:"code",title:"The naive way vs the Flow way",code:`// Works but discouraged — imperative, easy to get wrong:
try {
    flow.collect { value -> render(value) }
} catch (e: Exception) {
    showError(e)
}

// Idiomatic — declarative, composable:
flow
    .catch { e -> emit(fallbackValue) }   // handle UPSTREAM errors
    .collect { render(it) }`}]},{heading:"The catch operator — handle upstream errors only",blocks:[{t:"p",text:"`catch { }` catches exceptions that occur **upstream** of it (in the producer and any operators above it). It does *not* catch exceptions thrown *downstream* (in operators below it or in the `collect` block) — this is deliberate and follows the 'catch only what's above you' rule, so placement matters."},{t:"code",title:"Placement determines what's caught",code:`flow
    .map { riskyTransform(it) }   // errors here ARE caught below
    .catch { e ->
        // only sees exceptions from the producer and the map above
        emit(defaultValue)        // you can emit a fallback,
        // or rethrow, or log — but you're 'above' the collect
    }
    .map { it.toUiModel() }       // errors here are NOT caught by the catch above
    .collect { render(it) }       // errors here are NOT caught either`},{t:"list",items:["**Inside `catch` you can**: emit a fallback value (`emit(...)`), rethrow (to propagate), or just log/handle. It gives you the exception and an `emit` capability.","**Why not catch downstream too**: catching the collector's own errors would conflate 'the data source failed' with 'my rendering code has a bug' — Flow keeps them separate. To handle collector errors, use a try/catch around `collect` or move logic into an operator above the `catch`.","**Placement pattern**: put `catch` right after the risky upstream and before the UI-facing operators, so it handles data errors but not UI-code bugs."]}]},{heading:"The inner-flow catch pattern (critical for pipelines)",blocks:[{t:"p",text:"A subtle but vital pattern: in a `flatMapLatest`/`combine` pipeline (like search), where you place `catch` decides whether one error kills the *entire* pipeline or just one operation. A `catch` on the **outer** flow ends everything permanently after the first error — the user can never search again. A `catch` on the **inner** flow contains the error to that one operation, leaving the pipeline alive."},{t:"code",title:"Inner catch keeps the pipeline recoverable",code:`queryFlow
    .flatMapLatest { query ->
        repository.search(query)
            .map { SearchState.Results(it) }
            .onStart { emit(SearchState.Loading) }
            .catch { emit(SearchState.Error(it.message)) }  // INNER: this search fails,
    }                                                        // but the next keystroke works
    .collect { render(it) }
// A catch placed AFTER flatMapLatest (outer) would terminate the whole
// pipeline on the first failed search — no recovery.`}]},{heading:"retry and retryWhen — recovering from transient failures",blocks:[{t:"code",title:"Retry with backoff",code:`flow
    .retryWhen { cause, attempt ->
        // return true to retry, false to give up
        if (cause is IOException && attempt < 3) {
            delay(1000 * (attempt + 1))   // exponential-ish backoff
            true
        } else {
            false                          // propagate the error downstream
        }
    }
    .catch { emit(cachedFallback) }
    .collect { render(it) }`},{t:"list",items:["**`retry(count)`** — re-collect the flow up to `count` times on any exception (optionally filtered by a predicate). Simple case.","**`retryWhen { cause, attempt -> }`** — full control: inspect the exception and attempt number, `delay` for backoff, return `true` to retry or `false` to propagate. The standard way to retry network calls with exponential backoff, only on transient errors (`IOException`) and a bounded attempt count.","**Retry re-runs the upstream** (it's cold) — so a `retry` on a network flow re-issues the request. Combine with `catch` as the final fallback after retries are exhausted."]}]},{heading:"onStart, onEmpty, onCompletion — lifecycle hooks",blocks:[{t:"list",items:["**`onStart { }`** — runs *before* the first value; use to emit a loading state or trigger a side effect when collection begins (`.onStart { emit(Loading) }`).","**`onCompletion { cause -> }`** — runs when the flow finishes, whether normally (`cause == null`) or due to an exception/cancellation (`cause != null`). Use for cleanup or 'hide loading' logic. Note it *observes* the cause but doesn't handle it — the exception still propagates (put `catch` before it to handle).","**`onEmpty { }`** — runs if the flow completes without emitting anything; emit a default or empty state."]},{t:"code",title:"Composing lifecycle hooks with error handling",code:`repository.observeData()
    .onStart { emit(UiState.Loading) }
    .map { UiState.Content(it) }
    .catch { emit(UiState.Error(it.message)) }       // handle before onCompletion
    .onCompletion { cause -> if (cause != null) log("flow ended: $cause") }
    .collect { render(it) }`},{t:"note",text:"Order matters: `onStart`/`onCompletion` and `catch` are order-sensitive because each only sees what's upstream of it. The idiomatic order is `onStart` (loading) → transform → `catch` (errors → error state) → `onCompletion` (observe end). `CancellationException` must be rethrown, never swallowed — the `catch` operator already ignores it correctly, but a manual try/catch around a suspend call must rethrow it."}]},{heading:"Testing flows — Turbine and virtual time",blocks:[{t:"p",text:"Flows are tested inside `runTest` (which provides a `TestScope` with **virtual time** — `delay`s complete instantly by advancing a virtual clock). Collecting a flow and asserting its emissions is far cleaner with **Turbine**, a small library that turns collection into an await-based API."},{t:"code",title:"Testing a flow with Turbine",code:`@Test
fun searchEmitsLoadingThenResults() = runTest {
    val viewModel = SearchViewModel(FakeRepository(results = listOf(item)))

    viewModel.uiState.test {                     // Turbine: collect the flow
        assertEquals(SearchState.Loading, awaitItem())
        viewModel.onQueryChange("kotlin")
        advanceTimeBy(300)                        // skip debounce via virtual time
        assertEquals(SearchState.Results(listOf(item)), awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**Turbine basics**: `flow.test { }` collects the flow; inside, `awaitItem()` gets the next emission, `awaitComplete()` asserts completion, `awaitError()` asserts an exception, `expectNoEvents()` asserts nothing emitted. It fails the test if you leave unconsumed events (catching over-emission).","**Virtual time**: `runTest` runs `delay`s instantly by advancing a virtual clock; `advanceTimeBy(ms)` / `advanceUntilIdle()` let you test `debounce`, `sample`, and retry backoff *deterministically and fast* — no real waiting.","**The `stateIn(WhileSubscribed)` gotcha**: such a flow is cold until collected, so asserting `.value` right after construction sees only the `initialValue`. You must *collect* it (Turbine's `.test { }`, or `backgroundScope.launch { flow.collect() }`) to start the upstream and see real emissions.","**Inject a TestDispatcher**: with `viewModelScope` (which uses `Dispatchers.Main`), install `Dispatchers.setMain(StandardTestDispatcher())` via a JUnit rule so virtual time controls the ViewModel's coroutines; use `StandardTestDispatcher` when you need to assert intermediate states, `UnconfinedTestDispatcher` for eager execution."]}]}],ZS=[{level:"junior",q:"How do you handle errors in a Flow?",a:[{t:"p",text:"**The concept**: an exception anywhere in a flow — producer, an operator, or the collect block — propagates downstream to the terminal operator. You *can* wrap `collect` in a try/catch, but the idiomatic, composable way is the **`catch` operator**, which handles exceptions declaratively as part of the pipeline."},{t:"code",title:"The catch operator",code:`repository.observeData()
    .map { UiState.Content(it) }
    .catch { e -> emit(UiState.Error(e.message)) }   // handle upstream errors
    .collect { render(it) }`},{t:"p",text:"Inside `catch` you can emit a fallback value, rethrow to propagate, or just log. The key thing to know is that `catch` only catches exceptions from **upstream** (operators and the producer *above* it), not from operators below it or from the `collect` block — so you place it after the risky part and before the UI-facing code. This 'catch only what's above you' rule keeps data-source errors separate from bugs in your rendering code. For network flows you often combine it with `retry`/`retryWhen` (retry transient failures first) and then `catch` as the final fallback."}]},{level:"junior",q:"What does onStart and onCompletion do?",a:[{t:"list",items:["**`onStart { }`** runs *before* the first value is emitted — and it can itself `emit`. The classic use is emitting a loading state as collection begins: `.onStart { emit(UiState.Loading) }` so the UI shows a spinner immediately, before the real data arrives.","**`onCompletion { cause -> }`** runs when the flow ends — for any reason. If it completed normally, `cause` is null; if it ended from an exception or cancellation, `cause` is that throwable. Use it for cleanup or 'hide the loading indicator' logic that should run regardless of success or failure."]},{t:"p",text:"An important subtlety about `onCompletion`: it *observes* the completion cause but doesn't *handle* it — if the flow failed, the exception still propagates downstream after `onCompletion` runs. So `onCompletion` is not a substitute for `catch`; you put `catch` before it if you want to actually handle the error. The idiomatic order is `onStart` (loading) → transforms → `catch` (turn errors into an error state) → `onCompletion` (observe the end / cleanup)."}]},{level:"junior",q:"How do you retry a flow that fails, e.g. a flaky network call?",a:[{t:"p",text:"**The concept**: since a flow is cold, re-collecting it re-runs the upstream — so 'retry' means re-executing the producer. Flow gives you two operators for this. `retry(count)` simply re-collects up to `count` times on any exception (optionally filtered by a predicate). `retryWhen { cause, attempt -> }` gives full control: you inspect the exception and the attempt number, can `delay` between attempts (for backoff), and return `true` to retry or `false` to give up."},{t:"code",title:"Retry only transient errors, with backoff",code:`flow
    .retryWhen { cause, attempt ->
        if (cause is IOException && attempt < 3) {
            delay(1000 * (attempt + 1))   // wait longer each time
            true                          // retry
        } else false                      // give up -> error propagates
    }
    .catch { emit(cachedFallback) }       // final fallback after retries fail
    .collect { render(it) }`},{t:"p",text:"Best practice is to retry *only* transient failures (like `IOException` for network blips) and *not* things like a 404 or a parsing error that will fail identically every time — retrying those just wastes time. Bound the attempts, add increasing backoff so you don't hammer a struggling server, and pair it with a final `catch` that provides a fallback (cached data, an error state) once retries are exhausted."}]},{level:"junior",q:"How do you test a Flow?",a:[{t:"p",text:"**The concept**: flow tests run inside `runTest`, which provides virtual time (so `delay`s complete instantly by advancing a fake clock, making tests fast and deterministic). To assert emissions, the cleanest approach is the **Turbine** library, which collects the flow and gives you an await-based API."},{t:"code",title:"Turbine test",code:`@Test fun emitsValues() = runTest {
    flowOf(1, 2, 3).test {
        assertEquals(1, awaitItem())
        assertEquals(2, awaitItem())
        assertEquals(3, awaitItem())
        awaitComplete()
    }
}`},{t:"list",items:["Inside `flow.test { }`: `awaitItem()` gets the next emission, `awaitComplete()` asserts the flow finished, `awaitError()` asserts an exception, `expectNoEvents()` asserts nothing was emitted. Turbine also fails the test if emissions are left unconsumed, catching accidental over-emission.","For time-based operators (`debounce`, `sample`, retry backoff), use `advanceTimeBy(ms)` or `advanceUntilIdle()` to move virtual time forward without real waiting — so a test of a 300ms debounce runs instantly.","For ViewModels using `viewModelScope` (which uses `Dispatchers.Main`), install a `TestDispatcher` as Main via a JUnit rule so the test controls the coroutines."]}]},{level:"senior",q:"Explain exception transparency in Flow and why placement of the catch operator matters.",a:[{t:"p",text:"**Exception transparency** is a Flow design principle: a flow must not *catch* exceptions from downstream and *emit* in response — emissions and exception handling stay clearly separated so that downstream code can trust the contract. Concretely, this is why you can't put a try/catch *inside* a `flow { }` builder around an `emit` and then emit again from the catch (it violates transparency and throws). Handling belongs in the dedicated `catch` operator, which has well-defined scope."},{t:"p",text:"**Why placement matters**: `catch` only sees exceptions from **upstream** of it — the producer and operators *above* it. Anything *below* the `catch` (later operators, the `collect` block) is invisible to it. This is intentional: it separates 'the data source or a transformation failed' (which `catch` should handle) from 'my rendering code threw a bug' (which should *not* be silently swallowed by a data-error handler)."},{t:"code",title:"Placement changes behavior",code:`flow.map { parse(it) }      // A: caught
    .catch { emit(default) } //    <- catches producer + map A
    .map { render(it) }      // B: NOT caught (a bug here surfaces at collect)
    .collect { ui(it) }      // C: NOT caught`},{t:"list",items:["Put `catch` *right after the risky upstream* (the network/DB producer and its parsing) and *before* UI-facing operators, so data errors become error states but UI bugs still surface loudly.","**The pipeline corollary** (the high-value senior point): in a `flatMapLatest` search pipeline, a `catch` on the **inner** flow contains a failure to one search (the pipeline keeps working for the next query), while a `catch` on the **outer** flow terminates the entire pipeline permanently after the first error. Where you place `catch` literally decides whether the feature recovers.","`catch` correctly ignores `CancellationException` (it must, to not break cancellation) — but a hand-written `try/catch (e: Exception)` around a suspend call inside a flow will swallow it and break cancellation, a classic bug."]}]},{level:"senior",q:"In a flatMapLatest search pipeline, where do you put catch and why does it matter so much?",a:[{t:"p",text:"**Put `catch` on the INNER flow** — inside the `flatMapLatest` lambda, on the per-query flow — not on the outer pipeline. This is one of the most consequential placement decisions in real Flow code, because it determines whether a single failed search breaks the feature forever."},{t:"code",title:"Inner catch = recoverable; outer catch = broken forever",code:`// CORRECT — inner catch: one search fails, next keystroke still works
queryFlow.flatMapLatest { q ->
    repository.search(q)
        .map { SearchState.Results(it) }
        .onStart { emit(SearchState.Loading) }
        .catch { emit(SearchState.Error(it.message)) }   // contained here
}.collect { render(it) }

// WRONG — outer catch: first failed search terminates the WHOLE pipeline;
// queryFlow is now dead, no future query is ever processed
queryFlow.flatMapLatest { q -> repository.search(q).map { ... } }
    .catch { emit(SearchState.Error(it.message)) }
    .collect { render(it) }`},{t:"list",items:["**Why the outer catch is fatal**: when an exception reaches the outer `catch`, the outer flow *terminates* — and the outer flow is `queryFlow` itself. Once it's terminated, no further queries flow through; the user types and nothing happens. The `catch` 'handled' the error but killed the stream that feeds it.","**Why the inner catch works**: each search is its own short-lived inner flow. Catching *there* turns a failed search into an `Error` state emission that `flatMapLatest` passes along, and then the inner flow completes normally. The outer `queryFlow` never sees an exception, so it stays alive — the next keystroke starts a fresh inner search.","**General rule**: catch errors at the *narrowest* scope that should fail — the individual operation, not the long-lived stream that orchestrates operations. This applies to `combine`, `flatMapMerge`, and any long-lived flow driving short-lived sub-operations."]}]},{level:"senior",q:"What are the gotchas of testing a ViewModel that exposes stateIn(WhileSubscribed) StateFlow?",a:[{t:"p",text:"**The core gotcha**: a flow produced by `stateIn(scope, WhileSubscribed(...), initial)` is **cold until it has a collector** — `WhileSubscribed` specifically starts the upstream only when someone subscribes. So in a test, if you construct the ViewModel and immediately assert `viewModel.uiState.value`, you'll only ever see the **`initialValue`**, because nothing has subscribed to trigger the upstream. The real emissions never happen."},{t:"list",items:["**Fix — actually collect it**: use Turbine's `viewModel.uiState.test { }` (which subscribes, starting the upstream) and `awaitItem()` through the emissions, or launch a collector in the test's `backgroundScope` (`backgroundScope.launch { viewModel.uiState.collect() }`) so the sharing starts. Only then will `WhileSubscribed` run the upstream and produce real states.","**Install a TestDispatcher as Main**: `viewModelScope` uses `Dispatchers.Main`, which doesn't exist on the JVM. A JUnit rule calling `Dispatchers.setMain(testDispatcher)` / `resetMain()` is required, and it also routes the ViewModel's coroutines through virtual time.","**Dispatcher choice affects what you can observe**: `StandardTestDispatcher` queues coroutines until you `advanceUntilIdle()`/`runCurrent()`, letting you assert *intermediate* states like `Loading` before `Content`. `UnconfinedTestDispatcher` runs eagerly, which is simpler but can skip past transient states before you can assert them — pick based on whether you need to see the in-between states.","**Virtual time for operators**: if the pipeline has `debounce`/`stateIn` timeouts, use `advanceTimeBy` to move the clock; note `WhileSubscribed`'s own 5s stop-timeout can matter if you test unsubscribe behavior.","**Inject dispatchers into the ViewModel/repository**: hardcoded `Dispatchers.IO` inside escapes the test scheduler, making tests slow or flaky; constructor-inject dispatchers so the test substitutes the TestDispatcher and controls all timing."]},{t:"p",text:"**The one-sentence trap**: 'stateIn(WhileSubscribed) does nothing until collected, so a test must subscribe (Turbine or backgroundScope) before asserting — otherwise you only see initialValue.' Naming that, plus the Main-dispatcher rule and StandardvsUnconfined choice, is the complete senior answer."}]},{level:"junior",q:"What does the catch operator catch, and how do you emit a fallback?",a:[{t:"p",text:"`catch { e -> }` handles exceptions from *upstream* (the producer and operators above it) — this is exception transparency. Inside the `catch` block you can `emit` a fallback value, rethrow, or log. It does *not* catch exceptions thrown in the *collector* (downstream); use `try/catch` around `collect` for those."},{t:"code",title:"catch with a fallback",code:`repository.dataFlow()
    .map { transform(it) }
    .catch { e ->
        log(e)
        emit(fallbackData)        // provide a default and continue
    }
    .collect { render(it) }       // an exception HERE isn't caught above`},{t:"list",items:["**Catches upstream** — producer/operators above the `catch`.","**Can `emit`** — supply a fallback value to keep the flow going.","**Can rethrow** — `throw e` to propagate (or rethrow specific types).","**Not downstream** — collector exceptions need `try/catch` around `collect`."]},{t:"note",text:"catch { e -> } handles UPSTREAM exceptions (producer + operators above — exception transparency); inside you can emit a fallback, rethrow, or log. It does NOT catch collector (downstream) exceptions — wrap collect in try/catch for those. Placement matters: catch handles failures above it."}]},{level:"senior",q:"What is the difference between retry and retryWhen?",a:[{t:"p",text:"`retry(n) { cause -> Boolean }` re-collects the flow up to `n` times, retrying only if the predicate returns true for the exception. `retryWhen { cause, attempt -> Boolean }` is the flexible version — it gives you both the exception *and* the attempt number, so you can implement backoff (delay based on attempt) and conditional retry logic."},{t:"code",title:"retry vs retryWhen",code:`// retry: fixed count, predicate on the exception
flow.retry(3) { it is IOException }

// retryWhen: exception + attempt index, for backoff
flow.retryWhen { cause, attempt ->
    if (cause is IOException && attempt < 3) { delay(1000 * (attempt + 1)); true } else false
}`},{t:"list",items:["**`retry(n) { }`** — up to n retries; predicate decides per exception.","**`retryWhen { cause, attempt -> }`** — access the attempt number for backoff and caps.","**Backoff lives in `retryWhen`** — `delay` before returning `true`.","**Retry re-collects** — the whole upstream re-runs (a fresh network call, etc.)."]},{t:"note",text:"retry(n) { cause -> } re-collects up to n times if the predicate matches. retryWhen { cause, attempt -> } also gives the attempt index, enabling backoff (delay before returning true) and caps. Both re-run the whole upstream. Use retryWhen for exponential backoff + conditional retry."}]},{level:"senior",q:"How do you implement exponential backoff with jitter for a flaky flow?",a:[{t:"p",text:"Use `retryWhen`, delaying by an exponentially increasing amount plus randomness (jitter) before each retry, and only retry transient errors up to a cap. Jitter prevents many clients retrying in sync (thundering herd). After the cap, let the exception propagate to a `catch` that surfaces an error state."},{t:"code",title:"Backoff with jitter",code:`flow
    .retryWhen { cause, attempt ->
        if (cause is IOException && attempt < 3) {
            val backoff = (1000L * (1 shl attempt.toInt())) + Random.nextLong(0, 500)
            delay(backoff)   // 1s, 2s, 4s (+ jitter)
            true
        } else false
    }
    .catch { emit(UiState.Error) }   // give up after retries`},{t:"list",items:["**Exponential** — `base * 2^attempt`; spreads retries out.","**Jitter** — add randomness to avoid synchronized retries.","**Transient only** — retry `IOException`/5xx, not client errors.","**Cap + `catch`** — stop after N attempts and surface an error state."]},{t:"note",text:"retryWhen with delay = base * 2^attempt + random jitter, retrying only transient errors (IOException/5xx) up to a cap, then let it propagate to catch (error state). Exponential spreads retries; jitter avoids a thundering herd of synchronized clients. Don't retry client errors."}]},{level:"senior",q:"What does onCompletion's cause tell you, and how does it differ from catch and finally?",a:[{t:"p",text:"`onCompletion { cause -> }` runs when the flow finishes for *any* reason and gives the `cause`: `null` for success, or the exception for failure/cancellation. Unlike `catch`, it does *not* handle the exception (it can't emit a fallback to recover) — it's for cleanup/logging on completion. It's the flow analog of `finally`, but with knowledge of *why* it ended."},{t:"code",title:"onCompletion for cleanup",code:`flow
    .onCompletion { cause ->
        if (cause == null) log("done") else log("ended with $cause")
        hideLoading()      // cleanup regardless of outcome
    }
    .catch { emit(fallback) }   // catch (below) actually handles the error`},{t:"list",items:["**Runs on any completion** — success, error, or cancellation; `cause` distinguishes.","**Doesn't handle** — it observes; `catch` is what recovers/emits fallbacks.","**Placement vs `catch`** — put `onCompletion` where you want completion observed; `catch` catches upstream of it.","**Use** — hide loading spinner, logging, releasing resources on completion."]},{t:"note",text:"onCompletion { cause -> } runs on any finish (success=null, else the exception/cancellation) — for cleanup/logging, like finally-with-a-reason. It does NOT handle the error (can't emit a recovery) — that's catch's job. Use onCompletion to hide loading/log; catch to recover."}]},{level:"senior",q:"How do you handle per-item errors without stopping the whole flow?",a:[{t:"p",text:"Wrap each item's risky processing in a `map` that produces a `Result` (or catches locally), so a single item's failure becomes a value rather than terminating the flow. A `catch` operator, by contrast, ends the flow on the first upstream error — so for 'skip the bad ones, keep going', handle errors *inside* the transform."},{t:"code",title:"Per-item error as a value",code:`flow
    .map { item -> runCatching { process(item) } }   // failure -> Result.failure, flow continues
    .collect { result ->
        result.onSuccess { render(it) }.onFailure { logSkipped(it) }
    }`},{t:"list",items:["**Errors as values per item** — `map { runCatching { ... } }` keeps the flow alive.","**`catch` ends the flow** — it's for terminal failure handling, not skipping items.","**Rethrow cancellation** — `runCatching` swallows `CancellationException`; guard if needed.","**Use** — processing a stream where individual failures are tolerable (skip and continue)."]},{t:"note",text:"Handle per-item errors INSIDE a transform: map { runCatching { process(it) } } turns a failure into a Result value so the flow continues (skip bad items). catch ends the flow on the first error — wrong for 'skip and continue'. Watch runCatching swallowing CancellationException."}]},{level:"junior",q:"What's the difference between the catch operator and try/catch around collect?",a:[{t:"p",text:"The `catch` *operator* handles exceptions from *upstream* (producer/operators) and can emit fallbacks to keep the flow going. A `try/catch` around `collect` handles exceptions from the *collector block* (and upstream if there's no `catch`), but can't emit into the flow — it just handles the terminal failure. Use `catch` for upstream recovery; `try/catch` for the collection site."},{t:"code",title:"Two error boundaries",code:`// Operator: upstream, can emit fallback
flow.catch { emit(fallback) }.collect { render(it) }

// try/catch: handles collector errors (and uncaught upstream)
try {
    flow.collect { render(it) }   // if render throws, caught here
} catch (e: Exception) { showError() }`},{t:"list",items:["**`catch` operator** — upstream exceptions; can `emit` a fallback; keeps flow alive.","**`try/catch` around `collect`** — collector-block exceptions (and uncaught upstream); terminal handling.","**Combine** — `catch` for producer failures, `try/catch` for rendering failures.","**Placement** — `catch` only sees what's above it in the chain."]},{t:"note",text:"The catch operator handles UPSTREAM exceptions and can emit a fallback (flow continues). try/catch around collect handles the COLLECTOR block's exceptions (and uncaught upstream) but can't emit — terminal handling. Use catch for producer recovery, try/catch for rendering errors; combine both."}]},{level:"senior",q:"How do you catch errors in a flow without breaking cancellation?",a:[{t:"p",text:"Be careful that `catch` and error handling don't swallow `CancellationException`. The `catch` operator itself already *rethrows* `CancellationException` (it only catches real errors), which is correct. But if you use `runCatching` inside a `map` or a broad `try/catch` in the collector, you can swallow cancellation — so rethrow it or catch specific types."},{t:"list",items:["**`catch` operator is safe** — it does not catch `CancellationException` (rethrows it).","**`runCatching` in operators** — swallows cancellation; rethrow `CancellationException` inside.","**Broad collector `try/catch`** — rethrow `CancellationException` first, or catch specific types.","**Goal** — real errors handled, cancellation still propagates so the flow stops."]},{t:"note",text:"The catch operator is cancellation-safe (it rethrows CancellationException, catching only real errors). Danger is elsewhere: runCatching in a map or a broad collector try/catch swallows cancellation — rethrow CancellationException or catch specific types. Keep errors handled while cancellation still propagates."}]},{level:"junior",q:"How do you retry with a limit and fall back gracefully when it's exhausted?",a:[{t:"p",text:"Combine `retry`/`retryWhen` (bounded attempts) with a `catch` that emits a fallback or error state once retries are exhausted. The retry handles transient failures; the `catch` handles the final give-up, so the UI shows an error/cached state rather than crashing."},{t:"code",title:"Retry then fall back",code:`repository.stream()
    .retry(3) { it is IOException }        // transient retries
    .catch { emit(UiState.Error(it.message)) }   // give up gracefully
    .collect { render(it) }`},{t:"list",items:["**`retry(n)`** — bounded transient retries.","**`catch`** — after retries fail, emit an error/fallback state.","**Order** — `retry` above `catch` so `catch` handles the exhausted case.","**Cached fallback** — `catch` can emit last-known-good data instead of an error."]},{t:"note",text:"retry(n) { transient } for bounded retries, then catch { emit(Error/cached) } to give up gracefully once exhausted — retry above catch so catch handles the final failure. The UI shows an error or cached state instead of crashing."}]},{level:"senior",q:"What is Turbine, and why use it over toList() for testing flows?",a:[{t:"p",text:"Turbine is a testing library for flows that lets you assert emissions *one at a time* with `awaitItem()`, `awaitError()`, `awaitComplete()`. It's better than `toList()` for hot flows and infinite flows (which never complete, so `toList()` hangs) and for asserting the *sequence and timing* of emissions interactively, including errors."},{t:"code",title:"Turbine test",code:`viewModel.uiState.test {
    assertEquals(UiState.Loading, awaitItem())
    viewModel.load()
    assertEquals(UiState.Content(data), awaitItem())
    cancelAndIgnoreRemainingEvents()
}`},{t:"list",items:["**`awaitItem()`/`awaitError()`/`awaitComplete()`** — assert emissions/errors/completion in order.","**Works for hot/infinite flows** — unlike `toList()`, which needs completion.","**Interactive** — trigger actions between assertions to test reactive sequences.","**`cancelAndIgnoreRemainingEvents()`** — end the test cleanly."]},{t:"note",text:"Turbine tests flows by asserting emissions one at a time (awaitItem/awaitError/awaitComplete), interactively (trigger actions between assertions). Better than toList() for hot/infinite flows (which never complete, so toList hangs) and for asserting emission sequences including errors. End with cancelAndIgnoreRemainingEvents()."}]},{level:"junior",q:"How do you test a finite flow's emissions with toList()?",a:[{t:"p",text:"For a *finite* cold flow, collect all emissions into a list with `toList()` inside `runTest`, then assert the list. It's the simplest approach when the flow completes; it does *not* work for infinite/hot flows (it would wait forever), where you'd use Turbine or `take(n).toList()`."},{t:"code",title:"toList for finite flows",code:`@Test fun emitsTransformed() = runTest {
    val result = repository.numbers().map { it * 2 }.toList()
    assertEquals(listOf(2, 4, 6), result)
}
// Infinite flow: bound it first
val firstThree = infiniteFlow.take(3).toList()`},{t:"list",items:["**`toList()`** — gather all emissions from a finite flow; assert the list.","**Finite only** — infinite/hot flows never complete; use `take(n)` or Turbine.","**`runTest`** — virtual clock skips delays for fast tests.","**Simple assertions** — good for pure transformation pipelines."]},{t:"note",text:"toList() collects a FINITE cold flow's emissions into a list to assert (inside runTest). It hangs on infinite/hot flows — bound with take(n).toList() or use Turbine. Great for asserting transformation pipelines; runTest's virtual clock keeps it fast."}]},{level:"senior",q:"How do you test a flow that involves delays (debounce, timers)?",a:[{t:"p",text:"Run it in `runTest`, which uses a *virtual clock* — `delay`/`debounce` don't wait real time. Advance the clock with `advanceTimeBy(ms)` and `advanceUntilIdle()` to move past debounce windows and timers deterministically, then assert emissions. This makes time-based flow tests instant and reliable."},{t:"code",title:"Testing debounce with virtual time",code:`@Test fun debounces() = runTest {
    val flow = queryFlow.debounce(300)
    flow.test {
        queryFlow.value = "a"
        queryFlow.value = "ab"
        advanceTimeBy(301)          // move past the debounce window
        assertEquals("ab", awaitItem())   // only the last survives
    }
}`},{t:"list",items:["**Virtual clock** — `runTest` skips real delays.","**`advanceTimeBy(ms)`** — move past debounce/sample/timer windows precisely.","**`advanceUntilIdle()`** — run all pending scheduled work.","**Deterministic** — no real waiting, no flakiness from timing."]},{t:"note",text:"Test time-based flows in runTest (virtual clock — delays don't wait real time): advanceTimeBy(ms) to move past debounce/sample/timer windows and advanceUntilIdle() for all pending work, then assert. Instant and deterministic — no real waiting or timing flakiness."}]},{level:"senior",q:"How do you test a flow that emits an error?",a:[{t:"p",text:"Make a fake that throws the error (in the producer), then assert the flow's error handling: with Turbine use `awaitError()` (or `awaitItem()` for a mapped error state), or with `toList()` on a `catch`-guarded flow assert the fallback emission. Verify both that the error is surfaced correctly and that cancellation isn't swallowed."},{t:"code",title:"Testing error emission",code:`@Test fun surfacesError() = runTest {
    val flow = flow<Int> { throw IOException() }.catch { emit(-1) }
    assertEquals(listOf(-1), flow.toList())    // fallback emitted
}
@Test fun propagatesError() = runTest {
    flow<Int> { throw IOException() }.test { assertTrue(awaitError() is IOException) }
}`},{t:"list",items:["**Fake throws** — inject a producer that raises the target exception.","**Turbine `awaitError()`** — assert the error type propagates (uncaught flows).","**Assert fallback** — for `catch`-guarded flows, assert the emitted fallback/error state.","**Check cancellation** — ensure error handling doesn't swallow `CancellationException`."]},{t:"note",text:"Inject a fake producer that throws, then assert: Turbine awaitError() for propagated errors, or the emitted fallback/error state for catch-guarded flows (toList/awaitItem). Verify the error surfaces correctly and cancellation isn't swallowed."}]},{level:"senior",q:"How do you test a ViewModel that exposes a StateFlow?",a:[{t:"p",text:"Set the Main dispatcher to a test dispatcher (`Dispatchers.setMain`), drive the ViewModel, and assert `state.value` after `advanceUntilIdle()` — or collect the sequence with Turbine. For `stateIn(WhileSubscribed)` flows, the upstream only starts with an active collector, so collect in a background job (or read `.value` which activates it) during the test."},{t:"code",title:"StateFlow ViewModel test",code:`@get:Rule val mainRule = MainDispatcherRule()

@Test fun loadsContent() = runTest {
    val vm = MyViewModel(FakeRepo(data))
    vm.state.test {
        assertEquals(UiState.Loading, awaitItem())
        assertEquals(UiState.Content(data), awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**`Dispatchers.setMain`** — so `viewModelScope` runs on the test dispatcher (via a rule).","**Turbine or `.value`** — assert the sequence or the final state.","**`WhileSubscribed` needs a collector** — Turbine's `test { }` provides one; or read `.value` with `advanceUntilIdle`.","**Fakes** — inject fake repositories that return controlled data/errors."]},{t:"note",text:"Test StateFlow ViewModels with a MainDispatcherRule (Dispatchers.setMain), inject fakes, and assert with Turbine's test { awaitItem() } (sequence) or .value after advanceUntilIdle (final). WhileSubscribed upstream needs an active collector — Turbine provides one; otherwise read .value to activate it."}]},{level:"senior",q:"How do you test a Channel-based or SharedFlow event stream?",a:[{t:"p",text:"Collect the events with Turbine (`test { }`) started *before* triggering the action, then assert each event with `awaitItem()`. Because events aren't replayed (Channel/SharedFlow with replay 0), the collector must be active when the event is emitted — so start collecting first, then perform the action."},{t:"code",title:"Testing events",code:`@Test fun emitsNavigateEvent() = runTest {
    viewModel.events.test {
        viewModel.onSaved()                       // trigger AFTER collection starts
        assertEquals(UiEvent.NavigateBack, awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**Collect first** — start Turbine before triggering, since events aren't replayed.","**`awaitItem()`** — assert each event in order.","**No replay** — a late collector would miss the event; ordering matters in the test.","**Deterministic** — `runTest` + test dispatcher for scheduling."]},{t:"note",text:"Test event streams (Channel/SharedFlow replay 0) with Turbine's test { }, starting collection BEFORE triggering the action (events aren't replayed, so a late collector misses them), then awaitItem() each event. runTest + test dispatcher for deterministic scheduling."}]},{level:"junior",q:"How do you avoid flaky flow tests?",a:[{t:"p",text:"Use `runTest`'s virtual clock (never `Thread.sleep` or real delays), inject test dispatchers so all coroutines share the same scheduler, set the Main dispatcher via `setMain`, and use Turbine or `advanceUntilIdle()` to synchronize rather than racing. Flakiness usually comes from real timing, uncontrolled dispatchers, or asserting before work completes."},{t:"list",items:["**Virtual time** — `runTest` + `advanceTimeBy`/`advanceUntilIdle`; no real delays.","**Injected test dispatchers** — share the `testScheduler`; `setMain` for `viewModelScope`.","**Synchronize, don't race** — Turbine `awaitItem`/`advanceUntilIdle` instead of sleeping.","**Deterministic fakes** — controlled data/errors, no real network/DB."]},{t:"note",text:"Avoid flaky flow tests with runTest's virtual clock (no Thread.sleep/real delays), injected test dispatchers sharing the testScheduler, Dispatchers.setMain for viewModelScope, and Turbine/advanceUntilIdle to synchronize instead of racing. Flakiness comes from real timing, uncontrolled dispatchers, or asserting too early."}]},{level:"senior",q:"How do you test a combine or flatMapLatest pipeline?",a:[{t:"p",text:"Drive the *input* flows (e.g. `MutableStateFlow`s or `MutableSharedFlow`s) with controlled values inside `runTest`, and assert the combined output with Turbine. Emit values to the inputs step by step and check that the pipeline recomputes correctly, using `advanceUntilIdle()`/`advanceTimeBy` to handle any `debounce`."},{t:"code",title:"Testing a combine",code:`@Test fun combinesInputs() = runTest {
    val a = MutableStateFlow(1); val b = MutableStateFlow(10)
    val combined = combine(a, b) { x, y -> x + y }
    combined.test {
        assertEquals(11, awaitItem())
        a.value = 2
        assertEquals(12, awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**Controllable inputs** — `MutableStateFlow`/`MutableSharedFlow` you push values to.","**Turbine** — assert the combined emissions after each input change.","**Virtual time** — advance past any `debounce` in the pipeline.","**`flatMapLatest`** — verify the previous inner flow is cancelled when the trigger changes."]},{t:"note",text:"Test combine/flatMapLatest pipelines by driving controllable input flows (MutableStateFlow/SharedFlow) with values in runTest and asserting the output via Turbine, advancing virtual time past any debounce. For flatMapLatest, verify the prior inner flow is cancelled when the trigger changes."}]},{level:"junior",q:"How do you observe or log errors in a flow without consuming them?",a:[{t:"p",text:"Use `catch { e -> log(e); throw e }` — the `catch` operator can log (or report) the exception and then *rethrow* it so downstream handling still occurs. Or use `onCompletion { cause -> if (cause != null) log(cause) }` to observe failures on completion without altering propagation."},{t:"code",title:"Log then rethrow",code:`flow
    .catch { e -> analytics.logError(e); throw e }   // observe, then propagate
    .onCompletion { cause -> if (cause != null) log("ended: $cause") }
    .collect { render(it) }`},{t:"list",items:["**`catch { log; throw e }`** — observe and rethrow (doesn't consume).","**`onCompletion { cause }`** — log failures on completion without changing flow.","**Rethrow to preserve handling** — downstream `catch`/`try` still runs.","**Don't swallow** — logging-only shouldn't accidentally recover the error."]},{t:"note",text:"Log without consuming: catch { e -> log(e); throw e } (observe then rethrow so downstream handling still runs) or onCompletion { cause -> log if non-null } (observe on completion without altering propagation). Rethrow to preserve error handling; don't accidentally recover."}]},{level:"senior",q:"How do you verify a shared flow makes only one network call for multiple collectors?",a:[{t:"p",text:"Use a fake repository/data source that *counts* invocations, collect the shared flow from multiple collectors in the test, and assert the count is 1. This verifies that `shareIn`/`stateIn` truly shares one upstream execution rather than re-running it per collector."},{t:"code",title:"Assert single upstream execution",code:`@Test fun sharesUpstream() = runTest {
    var calls = 0
    val shared = flow { calls++; emit(fetch()) }
        .shareIn(this, SharingStarted.Lazily, replay = 1)
    shared.first(); shared.first()         // two collectors
    advanceUntilIdle()
    assertEquals(1, calls)                 // upstream ran once
}`},{t:"list",items:["**Counting fake** — increment on each upstream execution.","**Multiple collectors** — collect the shared flow more than once.","**Assert count == 1** — proves sharing (vs cold re-execution).","**Contrast** — the same test on a cold flow would count 2."]},{t:"note",text:"Use a fake that counts upstream invocations, collect the shared flow from multiple collectors in runTest, and assert the count is 1 — proving shareIn/stateIn shares one execution (a cold flow would count per collector). Verifies you fixed duplicate network calls."}]},{level:"senior",q:"How do you test that a flow stops when its scope is cancelled?",a:[{t:"p",text:"Collect the flow in a `launch` within `runTest`, then cancel that job and assert no further emissions are processed (e.g. via a counter or a fake that records how far the upstream got). This verifies structured cancellation — the flow's collection and upstream stop when the scope/job is cancelled."},{t:"code",title:"Cancellation stops collection",code:`@Test fun stopsOnCancel() = runTest {
    var collected = 0
    val job = launch { infiniteFlow.collect { collected++ } }
    advanceTimeBy(100)
    job.cancelAndJoin()
    val countAtCancel = collected
    advanceTimeBy(1000)
    assertEquals(countAtCancel, collected)   // no more emissions after cancel
}`},{t:"list",items:["**Collect in a job** — `launch { flow.collect { } }`.","**Cancel the job** — `cancelAndJoin()`.","**Assert no further work** — the count stops advancing.","**Verifies structured cancellation** — upstream stops with the collector."]},{t:"note",text:"Collect the flow in a launch within runTest, cancelAndJoin() the job, then advance time and assert no further emissions were processed (via a counter/fake) — verifying the flow's collection and upstream stop when the scope is cancelled (structured cancellation)."}]},{level:"junior",q:"How do you provide a fallback value when a flow fails?",a:[{t:"p",text:"Use `catch { emit(fallback) }` to emit a default/cached value when the upstream errors, keeping the flow alive so the UI shows something useful. For a 'last known good' fallback, emit cached data; for an error state, emit an `Error` variant of your UiState."},{t:"code",title:"catch fallback",code:`repository.config()
    .catch { emit(cachedConfig ?: Config.DEFAULT) }   // fallback on error
    .collect { apply(it) }`},{t:"list",items:["**`catch { emit(...) }`** — supply a fallback value on upstream error.","**Cached fallback** — serve last-known-good data (offline-friendly).","**Error state** — or emit a `UiState.Error` for the UI to render.","**Rethrow bugs** — only fall back for expected errors; rethrow unexpected ones if appropriate."]},{t:"note",text:"catch { emit(fallback) } supplies a default/cached value when the upstream errors, keeping the flow alive (offline-friendly last-known-good, or a UiState.Error). Fall back for expected errors; consider rethrowing unexpected bugs rather than masking them."}]},{level:"senior",q:"Why does the placement of catch matter, and where should it go in a pipeline?",a:[{t:"p",text:"Because `catch` only handles exceptions from operators *above* it (exception transparency), its position determines which failures it catches. A `catch` at the very end catches everything upstream; a `catch` in the middle catches only the operators above it, letting you handle a specific stage's errors while allowing later stages to fail differently."},{t:"code",title:"Placement changes coverage",code:`flow
    .map { risky1(it) }
    .catch { emit(fallback1) }    // catches only risky1 (and producer)
    .map { risky2(it) }           // a failure HERE is NOT caught above
    .catch { emit(fallback2) }    // catches risky2`},{t:"list",items:["**Catches upstream only** — operators above the `catch`.","**End placement** — one `catch` at the end handles all upstream failures.","**Mid-pipeline** — scope error handling to a specific stage.","**In `flatMapLatest`** — put `catch` *inside* the inner flow so one failed item doesn't kill the whole search pipeline."]},{t:"note",text:"catch handles only exceptions from operators ABOVE it (exception transparency), so placement decides coverage: end placement catches all upstream; mid-pipeline scopes handling to a stage. Crucially, in a flatMapLatest search, put catch INSIDE the inner flow so one failed search doesn't terminate the whole pipeline."}]},{level:"junior",q:"What do onStart and onCompletion do together for loading UI?",a:[{t:"p",text:"`onStart { }` runs before the first emission (emit a Loading state, show a spinner), and `onCompletion { }` runs when the flow finishes (hide the spinner). Together they bracket the flow's active period — a clean way to manage loading indicators declaratively around a data stream."},{t:"code",title:"Loading bracket",code:`repository.data()
    .onStart { emit(UiState.Loading) }        // before first value
    .onCompletion { /* hide spinner / log */ }// on finish (any reason)
    .catch { emit(UiState.Error) }
    .collect { render(it) }`},{t:"list",items:["**`onStart`** — runs (and can emit) before the upstream; ideal for Loading.","**`onCompletion { cause }`** — runs on completion/error/cancellation; hide loading, log.","**Declarative loading** — brackets the flow without manual flags.","**Order** — `onStart`/`onCompletion` see upstream below them; place thoughtfully with `catch`."]},{t:"note",text:"onStart { emit(Loading) } runs before the first emission (show spinner); onCompletion { cause } runs on any finish (hide spinner, log). Together they bracket the flow's active period for declarative loading UI, paired with catch for the error state."}]},{level:"senior",q:"How do you test retry logic in a flow?",a:[{t:"p",text:"Use a fake that fails a set number of times then succeeds (a call counter), collect the flow with the retry operator in `runTest`, advance virtual time past the backoff `delay`s, and assert both the final success emission and the number of attempts. This verifies the retry count, backoff timing, and that it eventually succeeds or gives up."},{t:"code",title:"Testing retry",code:`@Test fun retriesThenSucceeds() = runTest {
    var attempts = 0
    val flow = flow {
        attempts++
        if (attempts < 3) throw IOException() else emit("ok")
    }.retryWhen { cause, a -> if (cause is IOException && a < 3) { delay(1000); true } else false }

    flow.test {
        assertEquals("ok", awaitItem())
        awaitComplete()
    }
    assertEquals(3, attempts)   // failed twice, succeeded on the third
}`},{t:"list",items:["**Failing-then-succeeding fake** — count attempts; throw N times, then emit.","**Advance virtual time** — `runTest` skips the backoff `delay`s.","**Assert attempts + result** — verify retry count and eventual success/give-up.","**Test the give-up path** — a fake that always fails should surface the error after the cap."]},{t:"note",text:"Test retry with a fake that fails N times then succeeds (attempt counter): collect with the retry operator in runTest (virtual time skips backoff delays), assert the final emission and attempt count. Also test the give-up path (always-failing fake surfaces the error after the cap)."}]}],eA={id:"error-testing",title:"Error Handling & Testing",tagline:"Exception transparency, the catch operator & placement, inner vs outer catch, retry/retryWhen, onStart/onCompletion, and testing flows with Turbine + virtual time.",tags:["Flow","catch","retry","Turbine","Testing"],content:XS,qa:ZS},tA={id:"flows",name:"Kotlin Flows",description:"Asynchronous streams in Kotlin — cold vs hot, operators, StateFlow/SharedFlow, backpressure, threading & conversions, and error handling & testing.",topics:[VS,BS,KS,JS,$S,eA]},aA=[{heading:"What the Activity lifecycle is and why it exists",blocks:[{t:"p",text:"An **Activity** is a single screen in an Android app. Because the OS — not your app — controls when screens are shown, hidden, and destroyed (the user switches apps, gets a call, rotates the phone, the system needs memory), Android notifies your Activity of these transitions through **lifecycle callbacks**. Understanding them is fundamental because doing the *right work at the right callback* is what makes an app correct: acquire resources when visible, release them when not, and save state before you might be destroyed."},{t:"list",items:["The lifecycle exists because Android is a **resource-constrained, multitasking OS** that can destroy your Activity at any time to reclaim memory. Your app must cooperate: set up when needed, tear down when not, and be able to recreate itself.","Each callback is a signal about *visibility* and *interactivity*: created, visible, in-foreground-interactive, and the reverse on the way down."]}]},{heading:"The seven callbacks",blocks:[{t:"table",headers:["Callback","Meaning","Typical work"],rows:[["`onCreate()`","Activity created (once per instance)","inflate UI (setContent), init ViewModels, restore state"],["`onStart()`","becoming visible (not yet interactive)","start visible-only work (register some observers)"],["`onResume()`","now in foreground, interactive","start camera/sensors, resume animations, acquire exclusive resources"],["`onPause()`","losing focus (partially obscured)","pause animations, release camera, save critical data quickly (must be fast)"],["`onStop()`","no longer visible","release heavier resources, unregister observers, persist state"],["`onDestroy()`","being destroyed (finish or config change)","final cleanup"],["`onRestart()`","coming back to visible after onStop","re-prepare before onStart"]]},{t:"code",title:"The lifecycle flow",code:`onCreate -> onStart -> onResume   // launching, now interactive
                                  // ... user uses the screen ...
onPause -> onStop                 // user leaves (another app/screen)
onRestart -> onStart -> onResume  // user returns
onPause -> onStop -> onDestroy    // finishing / system destroys`},{t:"list",items:["**Visible lifetime** = `onStart` → `onStop` (the Activity is at least partially visible). **Foreground lifetime** = `onResume` → `onPause` (has focus, interactive). **Entire lifetime** = `onCreate` → `onDestroy`.","**Pairs**: acquire in one, release in its mirror — `onStart`/`onStop`, `onResume`/`onPause`, `onCreate`/`onDestroy`. Camera in `onResume`, release in `onPause`.","**`onPause` must be fast**: the next Activity can't resume until it returns, so no heavy work there — it briefly blocks the transition. Heavy persistence goes in `onStop`."]}]},{heading:"Configuration changes — the recreation trap",blocks:[{t:"p",text:"A **configuration change** (rotation, dark mode toggle, language change, window resize, font size change) by default **destroys and recreates the Activity** — full `onPause → onStop → onDestroy → onCreate → onStart → onResume`. This surprises newcomers: rotating the phone rebuilds your entire screen. Any state held in Activity fields is *lost* unless you preserve it."},{t:"list",items:["**Why recreate?** A different configuration may need different resources (a landscape layout, a different-language string, a larger font) — recreating lets Android load the right resources for the new configuration cleanly.","**What survives**: a `ViewModel` (retained across configuration changes), and small state saved via `onSaveInstanceState`/`rememberSaveable`. What's lost: everything in Activity fields, local variables, non-saved state.","**Common bug**: storing screen data in Activity fields → it vanishes on rotation → the screen resets. The fix is architectural: put state in a ViewModel (survives) and small UI state in saved instance state."]}]},{heading:"onSaveInstanceState / onRestoreInstanceState",blocks:[{t:"p",text:"`onSaveInstanceState(Bundle)` is called before an Activity *may* be destroyed (configuration change, or the system killing it in the background to reclaim memory). You write small key-value data into the `Bundle`; it's handed back in `onCreate` (and `onRestoreInstanceState`) when the Activity is recreated. This is how you preserve transient UI state across both configuration changes *and* process death."},{t:"code",title:"Saving and restoring state",code:`override fun onSaveInstanceState(outState: Bundle) {
    super.onSaveInstanceState(outState)
    outState.putString("query", currentQuery)   // small, Bundle-able data only
}

override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)
    val restored = savedInstanceState?.getString("query")
}`},{t:"list",items:["**For small transient UI state only**: scroll position, text input, selected tab. It uses the same `Bundle` mechanism as everything else, so it must be *small* and *Parcelable/Serializable* — the Binder transaction limit (~1MB, shared) means dumping large data causes `TransactionTooLargeException`.","**Not for large data**: screen data (a list of items, a user object) belongs in a ViewModel (config change) and a repository/database (process death). Save *ids* and reload, not the data itself.","**When it's called**: before a *possible* destruction — so it runs on config change and on background process kills, but NOT when the user explicitly finishes the Activity (back press / finish), because then there's nothing to restore.","**Modern equivalents**: `rememberSaveable` in Compose and `SavedStateHandle` in ViewModels both build on this same mechanism — they're the idiomatic ways to use saved instance state today."]}]},{heading:"Configuration change vs process death",blocks:[{t:"table",headers:["","Configuration change","Process death (background kill)"],rows:[["What happens","Activity destroyed & recreated; process lives","entire process killed by the system"],["ViewModel","survives (retained)","destroyed — recreated fresh"],["Activity fields / in-memory state","lost","lost"],["onSaveInstanceState Bundle","restored","restored (this is its main purpose)"],["How to test","rotate the device","background app, kill via Android Studio / adb, relaunch"]]},{t:"note",text:"Interview essentials to state: the seven callbacks and their pairs; `onPause` must be fast; configuration changes *recreate* the Activity by default (losing field state); `onSaveInstanceState` preserves *small* transient UI state across recreation AND process death; ViewModel survives config change but NOT process death — so the robust pattern is ViewModel (config change) + SavedStateHandle/persistent layer (process death)."}]}],iA=[{level:"junior",q:"Walk through the Activity lifecycle callbacks.",a:[{t:"list",items:["**`onCreate()`** — called once when the Activity is first created. Set up the UI (`setContentView`/`setContent`), initialize ViewModels, restore saved state. This is where one-time initialization goes.","**`onStart()`** — the Activity is becoming *visible* but not yet interactive. Start work that should run while visible.","**`onResume()`** — the Activity is now in the *foreground and interactive*. Acquire resources that need the screen active — camera, sensors, resume animations.","**`onPause()`** — the Activity is losing focus (a dialog appeared, or another Activity is starting). Release exclusive resources (camera) and pause animations. Must be *fast* — the next screen waits for it.","**`onStop()`** — the Activity is no longer visible. Release heavier resources, unregister observers, persist data.","**`onDestroy()`** — the Activity is being destroyed (finished or recreated for a config change). Final cleanup.","**`onRestart()`** — called when a stopped Activity is coming back, before `onStart()`."]},{t:"p",text:"The key mental model is *pairs*: you acquire in one callback and release in its mirror — `onStart`/`onStop`, `onResume`/`onPause`, `onCreate`/`onDestroy`. Launching goes create→start→resume; leaving goes pause→stop; returning goes restart→start→resume; finishing goes pause→stop→destroy. Putting the right work in the right callback (and cleaning up in the mirror) is what makes an Activity behave correctly."}]},{level:"junior",q:"What happens to an Activity when you rotate the screen?",a:[{t:"p",text:"**By default, the Activity is destroyed and completely recreated.** Rotation is a *configuration change*, and Android's default response is to run the full teardown-and-rebuild cycle: `onPause → onStop → onDestroy`, then a brand-new Activity instance goes `onCreate → onStart → onResume`. Your original Activity object is gone; a new one takes its place."},{t:"p",text:"**Why**: a new configuration (landscape orientation) may require different resources — a landscape layout, different dimensions — and recreating lets Android cleanly load the correct resources for the new configuration. **The consequence**: anything stored in Activity fields or local variables is *lost*. If you kept the screen's data in an Activity property, the screen resets on rotation — a classic bug. What survives is a `ViewModel` (which is retained across configuration changes) and small state written to `onSaveInstanceState`. So the correct approach is architectural: hold screen state in a ViewModel and small transient UI state (scroll position, text input) in saved instance state (`rememberSaveable`/`SavedStateHandle`), never in Activity fields."}]},{level:"junior",q:"What is onSaveInstanceState used for?",a:[{t:"p",text:"**The concept**: `onSaveInstanceState(Bundle)` is called before the Activity *might* be destroyed — on a configuration change, or when the system kills your app in the background to reclaim memory. You write small pieces of transient UI state into the `Bundle`, and it's returned to you in `onCreate` (and `onRestoreInstanceState`) when the Activity is recreated, so you can restore the screen to how the user left it."},{t:"list",items:["**Use it for small transient UI state**: scroll position, text field contents, which tab was selected, a search query. Things that would annoy the user to lose but aren't the actual screen data.","**Not for large data**: it uses the `Bundle`/Binder mechanism with a ~1MB shared transaction limit, so putting big lists or objects in it risks `TransactionTooLargeException`. Save *ids* and reload the data, not the data itself.","**Screen data belongs elsewhere**: a ViewModel holds it across config changes; a database/repository provides it after process death."]},{t:"p",text:"It's important because it's the *only* mechanism that survives **process death** — when the system kills your backgrounded app entirely, the ViewModel is gone, but the saved-instance-state Bundle is preserved by the system and handed back on relaunch. Modern APIs like `rememberSaveable` (Compose) and `SavedStateHandle` (ViewModel) are built on this exact mechanism and are the idiomatic ways to use it today."}]},{level:"junior",q:"Why should onPause be fast, and what should you not do in it?",a:[{t:"p",text:"**Because `onPause` runs during a screen transition, and the incoming Activity cannot become interactive (`onResume`) until the outgoing one's `onPause` returns.** So any slow work in `onPause` directly delays the next screen appearing — the user sees a lag or freeze during navigation. It's on the critical path of every transition away from the screen."},{t:"list",items:["**Don't do heavy work in `onPause`**: no database writes of large data, no network calls, no expensive computation. These block the transition.","**Do release exclusive/lightweight resources**: pause animations, release the camera (so another app can use it), unregister a sensor — quick operations that must happen the moment the screen loses focus.","**Put heavier persistence in `onStop`** instead: `onStop` isn't on the transition's critical path in the same way, so saving data or releasing heavier resources belongs there. Historically people over-used `onPause` for saving; modern guidance is to save in `onStop` (or continuously via a ViewModel/repository) and keep `onPause` minimal."]},{t:"p",text:"The rule of thumb: `onPause` is for *quick* 'I'm losing focus' cleanup; `onStop` is for 'I'm no longer visible' work that can afford to take a bit longer. Keeping `onPause` fast keeps navigation snappy."}]},{level:"senior",q:"Explain the difference between configuration change and process death, and how you build for both.",a:[{t:"p",text:"**They're two different ways your Activity can be torn down, and they preserve different things.** A *configuration change* (rotation, dark mode, locale, resize) destroys and recreates the Activity, but the *process stays alive* — so the ViewModel (which is retained) survives, and only Activity-instance state is lost. *Process death* is when the system kills your entire backgrounded process to reclaim memory — the ViewModel and all in-memory state are gone; only the saved-instance-state Bundle (held by the system) survives."},{t:"table",headers:["Survives?","Config change","Process death"],rows:[["ViewModel / in-memory state","yes / yes","no / no"],["onSaveInstanceState Bundle","restored","restored"],["Activity fields","lost","lost"]]},{t:"list",items:["**Build strategy — layered**: hold screen state in a **ViewModel** (covers configuration changes for free, in memory, no serialization). Hold the *minimal identity/input* needed to rebuild — selected id, query text, scroll anchor — in **`SavedStateHandle`** (which is backed by saved instance state, so it survives process death too). And treat the **persistent layer** (database/repository) as the source of truth for actual data, reloading it on recreation using the saved id.","**The recreation path is the same for both**: on recreate, the ViewModel reads its id from `SavedStateHandle` and re-subscribes to the repository — identical to first launch, so process-death recovery isn't a special code path. That's the elegance of the pattern.","**Don't over-save**: putting large data in `SavedStateHandle`/`onSaveInstanceState` risks `TransactionTooLargeException` (Binder limit). Save ids and reload; never parcel entire lists.","**Test both distinctly**: rotate to test config changes; to test process death, background the app and kill it via Android Studio's 'Terminate' or `adb shell am kill <pkg>`, then relaunch from recents. Rotation testing alone gives false confidence — it never exercises the ViewModel-is-gone path."]},{t:"p",text:"**The common interview trap**: 'does the ViewModel survive process death?' — *No*. It survives configuration changes only. Candidates frequently get this wrong. The complete answer is the layered strategy: ViewModel for config changes, SavedStateHandle (+ persistent layer) for process death, and reloading data from the source of truth rather than trying to serialize it all."}]},{level:"senior",q:"How can you handle configuration changes without the Activity being recreated, and why is that usually discouraged?",a:[{t:"p",text:'**You can opt out of automatic recreation by declaring `android:configChanges` in the manifest** — listing the configuration types you\'ll handle yourself (e.g. `android:configChanges="orientation|screenSize|keyboardHidden"`). When one of those changes, Android does *not* recreate the Activity; instead it calls `onConfigurationChanged(newConfig)`, and you manually update whatever needs updating.'},{t:"list",items:["**Why it's usually discouraged**: by handling config changes yourself, you take responsibility for reloading *all* configuration-dependent resources — layouts, dimensions, strings, drawables — for the new configuration. Miss one and you get subtle bugs (a landscape layout that never loads, a string that stays in the old language). The system's recreation does all of this correctly and automatically; opting out means reimplementing it by hand, error-prone.","**It doesn't solve the real problem**: people reach for `configChanges` to 'avoid losing state on rotation', but the *correct* solution to state loss is a ViewModel + saved instance state, which also handles process death (which `configChanges` does nothing for). So `configChanges` treats the symptom while leaving the actual robustness gap.","**Legitimate uses exist**: it's appropriate for specific cases like a full-screen video player or a game rendering with a custom `SurfaceView`/OpenGL context, where recreating would cause a jarring reload or lose a GPU context, and where there are few configuration-dependent resources to manage. Compose also handles many config changes gracefully within a single Activity, reducing the need."]},{t:"p",text:"**The senior framing**: `configChanges` trades the system's correct-but-recreating behavior for manual control, and it's the *wrong tool* for state preservation (use ViewModel + SavedStateHandle for that). Reserve it for the narrow cases — media/rendering surfaces — where recreation is genuinely undesirable and you can correctly handle the resource updates yourself. Reaching for it to 'fix rotation bugs' is an anti-pattern that hides the real architectural fix."}]},{level:"junior",q:"What are onCreate, onStart, and onResume each responsible for?",a:[{t:"p",text:"These are the three 'coming to the foreground' callbacks. `onCreate` runs *once* when the Activity is created — inflate the UI, initialize, restore saved state. `onStart` runs when the Activity becomes *visible* (can happen multiple times). `onResume` runs when it becomes the *foreground/interactive* Activity — start things that need focus (camera preview, animations, sensor updates)."},{t:"table",headers:["Callback","When","Do"],rows:[["onCreate","created (once)","inflate UI, init, restore state"],["onStart","becoming visible","register visible-only observers"],["onResume","foreground/interactive","acquire focus-needing resources"]]},{t:"list",items:["**`onCreate(savedInstanceState)`** — one-time setup; `setContentView`/`setContent`; restore state.","**`onStart`** — visible but maybe not focused; can run multiple times.","**`onResume`** — foreground; start camera/sensors/animations; the user can interact.","**Pairing** — `onResume`↔`onPause`, `onStart`↔`onStop`, `onCreate`↔`onDestroy`."]},{t:"note",text:"onCreate: one-time setup (inflate UI, init, restore state). onStart: becoming visible (visible-only observers). onResume: foreground/interactive (acquire focus-needing resources — camera/sensors/animations). They pair with onPause/onStop/onDestroy respectively. onStart/onResume can run multiple times; onCreate once."}]},{level:"junior",q:"What is the difference between onPause and onStop?",a:[{t:"p",text:"`onPause` fires when the Activity *loses focus* but may still be *partially visible* (a dialog or translucent Activity appears on top) — it should be fast. `onStop` fires when the Activity is *no longer visible* at all (fully covered or backgrounded) — a better place for heavier teardown like saving to a database or releasing resources."},{t:"list",items:["**`onPause`** — losing focus, maybe still visible; keep it quick (no heavy I/O).","**`onStop`** — no longer visible; do heavier cleanup (persist data, release resources).","**Partial visibility** — a translucent/dialog Activity triggers `onPause` but *not* `onStop`.","**Ordering** — `onPause` → `onStop` when fully backgrounded."]},{t:"code",title:"Pause vs stop",code:`override fun onPause() { super.onPause(); pausePlayback() }   // quick, focus lost
override fun onStop() { super.onStop(); saveDraftToDb() }     // not visible, heavier work`},{t:"note",text:"onPause: losing focus but maybe still visible (dialog/translucent on top) — keep it FAST, no heavy I/O. onStop: no longer visible — do heavier teardown (persist data, release resources). A translucent Activity triggers onPause but not onStop. Fully backgrounding goes onPause → onStop."}]},{level:"junior",q:"When is onDestroy called, and when is it NOT called?",a:[{t:"p",text:"`onDestroy` runs when the Activity is finishing (user pressed back, `finish()` called) or being destroyed for a configuration change. But it is *not* guaranteed — if the system kills the process (low memory) or force-stops the app, `onDestroy` may *not* run. So never rely on `onDestroy` for critical persistence; save important state earlier (`onStop`/`onPause`) or reactively."},{t:"list",items:["**Called** — on `finish()`/back press, and on configuration change (then recreated).","**Not guaranteed** — process kill (low memory) or force-stop may skip it.","**`isFinishing`** — distinguish real finish from a config-change destroy.","**Don't rely on it** — persist critical state in `onStop`/`onPause`, not `onDestroy`."]},{t:"note",text:"onDestroy runs on finish()/back press and config-change destroy (then recreate), but is NOT guaranteed — a process kill or force-stop can skip it. Use isFinishing to tell a real finish from a config-change destroy. Never rely on onDestroy for critical persistence; save in onStop/onPause."}]},{level:"junior",q:"What is onRestart, and what is the full path back to the foreground?",a:[{t:"p",text:"When a *stopped* (backgrounded) Activity comes back to the foreground, it doesn't re-run `onCreate` — it goes `onRestart` → `onStart` → `onResume`. `onRestart` runs only in this stopped→visible transition, so it's the place to refresh anything that might have changed while the Activity was hidden."},{t:"code",title:"Return-to-foreground path",code:`// Backgrounded then returned:
onPause -> onStop         // going away
onRestart -> onStart -> onResume   // coming back (no onCreate!)`},{t:"list",items:["**`onRestart`** — only when a stopped Activity restarts (not on first create).","**Path back** — `onRestart` → `onStart` → `onResume`.","**No `onCreate`** — the Activity wasn't destroyed, just stopped.","**Use** — refresh data that may have changed while backgrounded."]},{t:"note",text:"A stopped Activity returning to foreground goes onRestart → onStart → onResume (no onCreate — it wasn't destroyed). onRestart runs only on the stopped→visible transition — a place to refresh data that may have changed while hidden."}]},{level:"senior",q:"What are lifecycle-aware components and DefaultLifecycleObserver?",a:[{t:"p",text:"Lifecycle-aware components observe an owner's `Lifecycle` and react to its state, so they can *self-manage* setup/teardown instead of the Activity/Fragment manually calling them in each callback. Implement `DefaultLifecycleObserver` (override `onStart`/`onStop`/etc.) and register it with `lifecycle.addObserver(...)` — it then follows the owner's lifecycle automatically."},{t:"code",title:"A self-managing component",code:`class LocationTracker(private val lifecycle: Lifecycle) : DefaultLifecycleObserver {
    init { lifecycle.addObserver(this) }
    override fun onStart(owner: LifecycleOwner) { startUpdates() }
    override fun onStop(owner: LifecycleOwner) { stopUpdates() }
}`},{t:"list",items:["**`DefaultLifecycleObserver`** — override lifecycle methods; register with `addObserver`.","**Self-managing** — the component starts/stops itself with the owner's lifecycle.","**Avoids boilerplate** — no manual calls in every Activity/Fragment callback.","**Examples** — `lifecycleScope`, `LiveData`, `collectAsStateWithLifecycle` are lifecycle-aware."]},{t:"note",text:"Lifecycle-aware components observe an owner's Lifecycle and self-manage setup/teardown. Implement DefaultLifecycleObserver (onStart/onStop/…) and lifecycle.addObserver(it) — it follows the owner automatically, avoiding manual calls in each Activity/Fragment callback. lifecycleScope/LiveData are built this way."}]},{level:"senior",q:"What are Lifecycle.State and Lifecycle.Event?",a:[{t:"p",text:"The `Lifecycle` has *states* (`INITIALIZED`, `CREATED`, `STARTED`, `RESUMED`, `DESTROYED`) and *events* (`ON_CREATE`, `ON_START`, `ON_RESUME`, `ON_PAUSE`, `ON_STOP`, `ON_DESTROY`) that move between them. `repeatOnLifecycle(STARTED)` and `collectAsStateWithLifecycle` use the *state* to run work only at/above a given level, restarting on the state transitions."},{t:"list",items:["**States** — INITIALIZED → CREATED → STARTED → RESUMED (and down to DESTROYED).","**Events** — the transitions (`ON_START`, `ON_STOP`, etc.).","**`currentState.isAtLeast(STARTED)`** — check if visible.","**`repeatOnLifecycle(STARTED)`** — run a block while at least STARTED, cancel below it."]},{t:"note",text:"Lifecycle has states (INITIALIZED/CREATED/STARTED/RESUMED/DESTROYED) and events (ON_CREATE/ON_START/ON_RESUME/ON_PAUSE/ON_STOP/ON_DESTROY) between them. repeatOnLifecycle(STARTED)/collectAsStateWithLifecycle use the STATE to run work only while at/above a level, restarting across transitions."}]},{level:"senior",q:"What is onNewIntent, and when is it called?",a:[{t:"p",text:"`onNewIntent` is called when an *existing* Activity instance receives a new Intent instead of a new instance being created — which happens with launch modes `singleTop` (when it's already on top) or `singleTask`/`singleInstance`. Because the Activity isn't recreated, you must handle the new Intent here (and call `setIntent(intent)` to update `getIntent()`)."},{t:"code",title:"onNewIntent",code:`override fun onNewIntent(intent: Intent) {
    super.onNewIntent(intent)
    setIntent(intent)          // update getIntent()
    handleDeepLink(intent)     // the Activity wasn't recreated
}`},{t:"list",items:["**Existing instance reused** — no new `onCreate`; `onNewIntent` delivers the new Intent.","**Triggered by** — `singleTop` (already on top), `singleTask`/`singleInstance`.","**`setIntent()`** — update the stored intent so `getIntent()` reflects the new one.","**Uses** — deep links, notifications re-opening an existing screen."]},{t:"note",text:"onNewIntent delivers a new Intent to an EXISTING Activity instance (no new onCreate) — with launch modes singleTop (already on top), singleTask/singleInstance. Call setIntent(intent) so getIntent() reflects it, then handle it. Common for deep links / notifications reopening a live screen."}]},{level:"senior",q:"What survives a configuration change, process death, and neither?",a:[{t:"p",text:"Three tiers: a *ViewModel* survives configuration changes (kept in memory) but not process death. *`onSaveInstanceState`/`SavedStateHandle`* survive *both* config change and process death (serialized to a Bundle). Plain instance fields survive *neither* (lost on recreation). The robust approach combines them: ViewModel for data + SavedStateHandle for the small state needed to rebuild after process death."},{t:"table",headers:["Mechanism","Config change","Process death"],rows:[["ViewModel","survives","lost"],["SavedStateHandle / saved instance state","survives","survives"],["plain fields","lost","lost"]]},{t:"list",items:["**ViewModel** — in-memory across config change; cleared on real finish; gone on process death.","**SavedStateHandle** — small Bundle-able state; survives both.","**Combine** — ViewModel holds data; SavedStateHandle holds ids/inputs to re-fetch after process death.","**Plain fields** — reset on any recreation."]},{t:"note",text:"ViewModel survives config change (in memory), not process death. SavedStateHandle/saved instance state survives BOTH (Bundle). Plain fields survive neither. Combine: ViewModel for data + SavedStateHandle for the ids/inputs to rebuild after process death."}]},{level:"senior",q:"What is SavedStateHandle, and how does it survive process death?",a:[{t:"p",text:"`SavedStateHandle` is a key-value map (injected into a ViewModel) whose contents are saved into the Activity's saved-instance-state `Bundle`, so they survive *process death* (not just config change like the rest of the ViewModel). Use it to persist the small inputs (an id, a query) needed to rebuild the screen's data after the OS kills and restores the process."},{t:"code",title:"SavedStateHandle in a ViewModel",code:`class DetailViewModel(private val handle: SavedStateHandle) : ViewModel() {
    private val id: String = handle["itemId"] ?: error("missing id")
    val query = handle.getStateFlow("query", "")   // observable, saved
    fun setQuery(q: String) { handle["query"] = q }
}`},{t:"list",items:["**Injected into the ViewModel** — Hilt/AndroidX provides it.","**Backed by the saved-state Bundle** — survives process death.","**`getStateFlow(key, default)`** — an observable, saved value.","**Store small inputs** — ids/queries to re-fetch, not large objects."]},{t:"note",text:"SavedStateHandle is a ViewModel-injected key-value map backed by the saved-instance-state Bundle, so it survives PROCESS DEATH (the rest of the ViewModel doesn't). Store the small inputs (id/query) needed to rebuild data after restore; getStateFlow(key, default) gives an observable saved value. Bundle-size limits apply."}]},{level:"senior",q:"What is the Bundle size limit for saved state, and why does it matter?",a:[{t:"p",text:"Saved instance state (and `SavedStateHandle`) is serialized into a `Bundle` delivered through a Binder transaction, which has a limit (~1MB shared across the transaction, but practically you should stay well under ~50KB). Exceeding it throws `TransactionTooLargeException`. So you must save only *small* state (ids, scroll position, form inputs) — never large lists, bitmaps, or full objects."},{t:"list",items:["**Bundle → Binder transaction** — limited size (~1MB total, keep well under).","**`TransactionTooLargeException`** — from oversized saved state.","**Save small** — ids, selected tab, scroll position, text input.","**Not for large data** — re-fetch big data from cache/DB using the saved ids."]},{t:"note",text:"Saved instance state/SavedStateHandle serializes into a Bundle over a Binder transaction with a size limit (~1MB total; keep well under ~50KB) — exceeding it throws TransactionTooLargeException. Save only small state (ids, scroll position, inputs); re-fetch large data from cache/DB using the saved ids."}]},{level:"senior",q:"When is onSaveInstanceState called, and how does it relate to onRestoreInstanceState?",a:[{t:"p",text:"`onSaveInstanceState(bundle)` is called when the system *might* destroy the Activity to reclaim resources — before `onStop` (on modern Android). The saved Bundle is passed back to `onCreate(savedInstanceState)` and to `onRestoreInstanceState(bundle)` (called after `onStart`, only if there was saved state). You can restore in either `onCreate` (check for null) or `onRestoreInstanceState`."},{t:"list",items:["**`onSaveInstanceState`** — called before the Activity may be destroyed (before `onStop`); save small UI state.","**Not on finish** — not called when the user explicitly finishes (state won't be needed).","**Restore in `onCreate`** — `savedInstanceState?.let { }` (null on first launch).","**Or `onRestoreInstanceState`** — after `onStart`, only when there's saved state to restore."]},{t:"note",text:"onSaveInstanceState is called before the Activity might be destroyed (before onStop on modern Android) — not on explicit finish. The Bundle returns to onCreate(savedInstanceState) and onRestoreInstanceState (after onStart, only if state exists). Restore in onCreate (null-check) or onRestoreInstanceState."}]},{level:"junior",q:"What is the Activity Result API, and why did it replace onActivityResult?",a:[{t:"p",text:"The Activity Result API (`registerForActivityResult` / `rememberLauncherForActivityResult` in Compose) is the modern way to start another Activity (or request a permission) and get a result via a typed callback. It replaced the old `startActivityForResult`/`onActivityResult` (with manual request codes and a giant `when`), which was error-prone and hard to modularize."},{t:"code",title:"Activity Result API",code:`val launcher = registerForActivityResult(ActivityResultContracts.GetContent()) { uri ->
    uri?.let { handleImage(it) }        // typed result callback
}
launcher.launch("image/*")
// Compose: val launcher = rememberLauncherForActivityResult(contract) { result -> }`},{t:"list",items:["**`registerForActivityResult(contract) { result -> }`** — typed contracts + callback.","**No request codes** — the callback is tied to the specific launcher.","**Contracts** — `StartActivityForResult`, `GetContent`, `RequestPermission`, custom.","**Survives recreation** — registered in `onCreate`; robust across process death."]},{t:"note",text:"The Activity Result API (registerForActivityResult(contract){result} / rememberLauncherForActivityResult) starts activities/permission requests and returns a typed result via callback — replacing error-prone startActivityForResult/onActivityResult with request codes. Register in onCreate; contracts include GetContent, RequestPermission, StartActivityForResult, custom."}]},{level:"senior",q:"What lifecycle happens when a translucent or dialog Activity appears on top?",a:[{t:"p",text:"If a *translucent* Activity or a dialog-themed Activity opens over your Activity, your Activity is still *partially visible*, so it receives `onPause` but *not* `onStop` — it stays in the STARTED state. This is a key reason `onPause` shouldn't be used for teardown you'd want on 'not visible': the Activity may still be showing behind the overlay."},{t:"list",items:["**Partial visibility** — translucent/dialog Activity on top keeps yours visible.","**`onPause` only** — no `onStop` (still STARTED).","**Implication** — don't release visible-only resources in `onPause`.","**Full cover** — a normal opaque Activity triggers `onStop` too."]},{t:"note",text:"A translucent or dialog-themed Activity on top leaves yours partially visible, so you get onPause but NOT onStop (stays STARTED). So don't tear down visible-only resources in onPause — the Activity may still be showing. A full opaque Activity triggers onStop as well."}]},{level:"senior",q:"What is the lifecycle callback order when Activity A launches Activity B?",a:[{t:"p",text:"The transitions interleave: A's `onPause` runs first, then B goes `onCreate` → `onStart` → `onResume` (B is now foreground), and only *then* does A's `onStop` run (A is no longer visible). This ordering is why `onPause` must be fast — B can't fully resume until A's `onPause` returns."},{t:"code",title:"A launches B",code:`A.onPause
    B.onCreate -> B.onStart -> B.onResume
A.onStop
// Pressing back from B:
B.onPause -> A.onRestart -> A.onStart -> A.onResume -> B.onStop -> B.onDestroy`},{t:"list",items:["**A.onPause first** — before B starts.","**B fully resumes** — onCreate → onStart → onResume.","**A.onStop last** — after B is visible.","**Why `onPause` fast** — B's resume waits on A's `onPause`."]},{t:"note",text:"A launches B: A.onPause → B.onCreate → B.onStart → B.onResume → A.onStop (A stops only after B is visible). Back from B: B.onPause → A.onRestart/onStart/onResume → B.onStop → B.onDestroy. This interleaving is why onPause must be fast — B's resume waits on it."}]},{level:"junior",q:"What are common Activity lifecycle mistakes that cause leaks?",a:[{t:"p",text:"The classic mistakes: registering listeners/receivers/observers in one callback but not unregistering in the paired one; holding the Activity `Context` in a static/singleton (leaks the whole Activity); starting background work not tied to the lifecycle; and not releasing resources (camera, sensors) in the matching teardown callback. Always pair setup with teardown."},{t:"list",items:["**Unpaired register/unregister** — register in `onStart`, unregister in `onStop` (or `onResume`/`onPause`).","**Static Activity reference** — a singleton holding the Activity/Context leaks it; use `applicationContext` for long-lived needs.","**Unscoped background work** — use `lifecycleScope`/`viewModelScope` so it cancels.","**Resource release** — release camera/sensors/`MediaPlayer` in the paired callback."]},{t:"note",text:"Lifecycle leak mistakes: register without unregister (pair onStart/onStop etc.), static/singleton holding the Activity Context (use applicationContext), background work not tied to lifecycleScope/viewModelScope, and not releasing resources (camera/sensors/MediaPlayer) in the paired teardown. Always pair setup with teardown."}]},{level:"senior",q:"Where should you acquire and release resources like camera or location across the lifecycle?",a:[{t:"p",text:"Acquire in `onStart`/`onResume` and release in the *paired* callback (`onStop`/`onPause`), matching the resource's visibility needs. Focus-sensitive resources (camera preview, exclusive audio) belong in `onResume`/`onPause`; visible-but-not-focused ones (location updates, sensor listeners) in `onStart`/`onStop`. Lifecycle-aware components or `repeatOnLifecycle` can automate this."},{t:"list",items:["**Focus-sensitive** — camera, exclusive audio → acquire in `onResume`, release in `onPause`.","**Visible-scoped** — location, sensors → `onStart`/`onStop`.","**Pair them** — every acquire has a matching release.","**Automate** — `DefaultLifecycleObserver` or `repeatOnLifecycle(STARTED)` for flows."]},{t:"note",text:"Acquire/release resources in paired callbacks matched to visibility: focus-sensitive (camera, exclusive audio) in onResume/onPause; visible-scoped (location, sensors) in onStart/onStop. Automate with DefaultLifecycleObserver or repeatOnLifecycle(STARTED) so it follows the lifecycle without manual pairing."}]},{level:"senior",q:"How does the Activity lifecycle relate to Compose (setContent)?",a:[{t:"p",text:"`setContent { }` in `onCreate` installs a `ComposeView` and starts the composition; the composition is tied to the Activity's lifecycle and disposed when the Activity is destroyed. Compose reads the Activity's `Lifecycle` (via `LocalLifecycleOwner`) so `collectAsStateWithLifecycle` and lifecycle effects work. Configuration changes still recreate the Activity and re-run `setContent` — Compose state uses `rememberSaveable`/ViewModel to survive."},{t:"list",items:["**`setContent` in `onCreate`** — starts the composition; disposed on destroy.","**`LocalLifecycleOwner`** — Compose sees the Activity's lifecycle.","**Config change** — recreates the Activity and re-composes; use `rememberSaveable`/ViewModel.","**Lifecycle-aware Compose** — `collectAsStateWithLifecycle`, `LifecycleResumeEffect`."]},{t:"note",text:"setContent { } in onCreate starts the composition, tied to and disposed with the Activity lifecycle; Compose reads it via LocalLifecycleOwner (so collectAsStateWithLifecycle/lifecycle effects work). Config change recreates the Activity and re-runs setContent — use rememberSaveable/ViewModel to survive."}]},{level:"junior",q:"How do you simulate and test process death during development?",a:[{t:"p",text:"Enable **Developer Options → 'Don't keep activities'** to destroy every Activity as soon as you leave it (simulating aggressive destruction), or use the 'Background process limit'. To simulate true *process death*, background the app and terminate the process from Android Studio's *App Quality Insights*/terminate button (or `adb shell am kill <package>`), then return — the OS restores from saved state."},{t:"list",items:["**'Don't keep activities'** — destroys Activities on leave; catches state-restoration bugs.","**Terminate the process** — Studio's stop/terminate or `adb shell am kill` while backgrounded, then reopen.","**Verify** — the screen restores correctly from `SavedStateHandle`/saved state.","**Not the same as swipe-away** — swiping from Recents finishes the task; process death is OS-initiated."]},{t:"note",text:"Simulate aggressive destruction with Developer Options → 'Don't keep activities'. Simulate true process death by backgrounding the app and terminating the process (Studio terminate or adb shell am kill <pkg>), then reopening — verify restoration from SavedStateHandle. Note swipe-away from Recents finishes the task, which differs from OS-initiated process death."}]},{level:"junior",q:"What is the difference between the visible and foreground lifecycle states?",a:[{t:"p",text:"An Activity is *visible* when it's in the STARTED state (between `onStart` and `onStop`) — the user can see it, possibly behind a dialog. It's in the *foreground* (interactive) when RESUMED (after `onResume`, before `onPause`) — it has focus and the user can interact. The gap (visible but not focused) happens when a dialog/translucent Activity is on top."},{t:"list",items:["**Visible (STARTED)** — seen but maybe not focused; between `onStart`/`onStop`.","**Foreground (RESUMED)** — focused and interactive; between `onResume`/`onPause`.","**In-between** — visible but not focused (dialog on top): STARTED, not RESUMED.","**Choose work by state** — visible-only vs focus-required."]},{t:"note",text:"Visible = STARTED (seen, maybe behind a dialog; onStart↔onStop). Foreground = RESUMED (focused, interactive; onResume↔onPause). The visible-but-not-focused gap happens with a dialog/translucent Activity on top. Scope work by state: visible-only (STARTED) vs focus-required (RESUMED)."}]},{level:"junior",q:"Why is the ViewModel the right place for data that must survive the lifecycle?",a:[{t:"p",text:"A `ViewModel` is scoped to the screen's lifecycle *owner* and survives configuration changes — so data it holds isn't re-fetched on every rotation, and it's cleared only when the screen is truly finished (`onCleared`). This makes it the correct home for UI-related data and in-flight operations, keeping the Activity/Fragment thin and avoiding reload-on-rotation."},{t:"list",items:["**Survives config change** — data persists across rotation (no refetch).","**Cleared on real finish** — `onCleared()` when the owner is gone for good.","**Holds data + operations** — `viewModelScope` work survives rotation too.","**Thin UI** — the Activity/Fragment observes state; logic lives in the ViewModel."]},{t:"note",text:"A ViewModel is scoped to the screen and survives configuration changes (data isn't refetched on rotation), cleared only on true finish (onCleared). It's the right home for UI data and in-flight operations (viewModelScope survives rotation too), keeping the Activity/Fragment thin. Pair with SavedStateHandle for process death."}]},{level:"senior",q:"How does lifecycleScope relate to the lifecycle, and when should you use it?",a:[{t:"p",text:"`lifecycleScope` is a `CoroutineScope` tied to a `LifecycleOwner` (Activity/Fragment) — its coroutines are cancelled when the lifecycle reaches DESTROYED. Use it for UI-lifecycle work like collecting flows to update views (with `repeatOnLifecycle(STARTED)`), not for data that should survive configuration changes (use `viewModelScope` for that)."},{t:"code",title:"lifecycleScope usage",code:`lifecycleScope.launch {
    repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.uiState.collect { render(it) }   // cancelled/restarted with visibility
    }
}`},{t:"list",items:["**Tied to the owner** — cancelled at DESTROYED.","**UI-lifecycle work** — collecting flows, view updates.","**Doesn't survive config change** — the Fragment/Activity is recreated.","**vs `viewModelScope`** — use that for data work that must survive rotation."]},{t:"note",text:"lifecycleScope is a CoroutineScope tied to a LifecycleOwner — cancelled at DESTROYED. Use it for UI-lifecycle work (collecting flows to update views, with repeatOnLifecycle(STARTED)); it doesn't survive config change. Use viewModelScope for data work that must survive rotation."}]},{level:"junior",q:"What happens to the lifecycle when the user presses Home versus Back?",a:[{t:"p",text:"Pressing **Home** *backgrounds* the Activity without finishing it — `onPause` → `onStop`; it stays in memory and returns via `onRestart` → `onStart` → `onResume`. Pressing **Back** (on the root Activity) *finishes* it — `onPause` → `onStop` → `onDestroy` — so returning re-creates it from scratch."},{t:"list",items:["**Home** — background (not finished): onPause → onStop; return via onRestart/onStart/onResume.","**Back** — finish: onPause → onStop → onDestroy; `isFinishing` is true.","**State** — Home keeps the instance; Back destroys it (state via saved-instance/ViewModel).","**Recents swipe** — finishes the task (like back for the whole task)."]},{t:"note",text:"Home backgrounds without finishing (onPause → onStop; returns via onRestart/onStart/onResume, instance kept). Back on the root finishes (onPause → onStop → onDestroy, isFinishing true; recreated on return). Recents swipe finishes the task. Use ViewModel/saved state for data across these."}]},{level:"senior",q:"How does multi-window / split-screen affect the Activity lifecycle?",a:[{t:"p",text:"In multi-window/split-screen, multiple Activities can be *visible* at once, but only *one* is in the RESUMED (focused) state at a time — the others are STARTED (visible but not focused). So an Activity you can still see may be in `onPause`. Don't tie critical 'keep running' work (video playback) strictly to `onResume`/`onPause`; consider `onStart`/`onStop` and declare `resizeableActivity`."},{t:"list",items:["**Multiple visible** — split-screen shows several Activities.","**One focused** — only one RESUMED; others STARTED (paused but visible).","**Implication** — a paused Activity may still be visible; use STARTED-scoped work for visible behavior.","**`resizeableActivity`** — declare support; handle resize as a configuration change."]},{t:"note",text:"In multi-window, multiple Activities are visible but only one is RESUMED (focused) — others are STARTED (visible but paused). So a visible Activity may be in onPause; tie visible behavior (playback) to onStart/onStop, not onResume/onPause. Declare resizeableActivity and handle resize as a config change."}]},{level:"junior",q:"Why does the whole Activity recreate on rotation, and how do you keep it smooth?",a:[{t:"p",text:"A rotation is a *configuration change* — the system destroys and recreates the Activity so it can load configuration-specific resources (layouts, dimens) for the new orientation. To keep it smooth: hold data in a `ViewModel` (survives recreation, no refetch), save small UI state via `rememberSaveable`/`onSaveInstanceState`, and let the framework re-inflate resources. Avoid `configChanges` hacks unless you have a specific reason."},{t:"list",items:["**Config change → recreate** — to load orientation-specific resources.","**ViewModel** — data survives; no reload on rotate.","**`rememberSaveable`/saved state** — small UI state survives.","**Avoid `android:configChanges`** — handling it yourself skips resource reloading and is error-prone."]},{t:"note",text:"Rotation is a configuration change — the Activity recreates to load orientation-specific resources. Keep it smooth with a ViewModel (data survives, no refetch) + rememberSaveable/onSaveInstanceState (small UI state). Avoid android:configChanges hacks — they skip resource reloading and are error-prone."}]}],nA={id:"activity-lifecycle",title:"Activity Lifecycle & State",tagline:"The seven callbacks and their pairs, configuration changes & recreation, onSaveInstanceState, and config change vs process death.",tags:["Android","Activity","Lifecycle","onSaveInstanceState","Process Death"],content:aA,qa:iA},oA=[{heading:"What a Fragment is",blocks:[{t:"p",text:"A **Fragment** is a reusable, modular portion of UI that lives *inside* an Activity (or another Fragment). Think of it as a 'sub-screen' with its own lifecycle, layout, and behavior, hosted by an Activity. Fragments were introduced to support **multiple screen sizes** (a tablet showing a list and detail side-by-side, a phone showing one at a time) and to enable **reusable UI components** and **navigation** within a single Activity."},{t:"list",items:["A Fragment can't exist on its own — it must be hosted by an Activity (or parent Fragment) and added to a container view via the `FragmentManager`.","**Single-Activity architecture**: the modern Android pattern is one Activity hosting many Fragments (or many Compose destinations), with the Navigation component managing transitions — rather than one Activity per screen.","Fragments have their own lifecycle that's *tied to but distinct from* the host Activity's — which is the source of most Fragment complexity and bugs."]}]},{heading:"The Fragment lifecycle — and the crucial view lifecycle",blocks:[{t:"p",text:"A Fragment has *two* lifecycles you must distinguish: the **Fragment instance lifecycle** and the **Fragment's view lifecycle**. This distinction is the #1 source of Fragment bugs and interview questions."},{t:"code",title:"The main callbacks",code:`onAttach()          // attached to its host (Activity)
onCreate()          // fragment instance created (no view yet)
onCreateView()      // create/inflate the fragment's VIEW hierarchy
onViewCreated()     // view is ready — set up UI, observe data HERE
onStart / onResume  // visible / interactive (mirrors Activity)
onPause / onStop    // losing focus / not visible
onDestroyView()     // the fragment's VIEW is destroyed (but the fragment lives!)
onDestroy()         // the fragment instance is destroyed
onDetach()          // detached from host`},{t:"list",items:["**The instance lifecycle** (`onCreate` → `onDestroy`) tracks the Fragment *object*. **The view lifecycle** (`onCreateView` → `onDestroyView`) tracks the Fragment's *UI*. The key insight: **a Fragment's view can be destroyed and recreated while the Fragment instance stays alive.**","This happens when a Fragment goes onto the **back stack**: navigate away, and `onDestroyView` runs (the view is torn down to save memory), but `onDestroy` does *not* — the Fragment instance is retained. Navigate back, and `onCreateView` runs again, building a *new* view for the *same* Fragment instance.","**Consequence**: the Fragment instance can outlive multiple views. Observing data or holding view references using the Fragment's own lifecycle (instead of the view's) leaks the old destroyed view or crashes when the callback fires on a dead view."]}]},{heading:"viewLifecycleOwner — the fix for the two-lifecycle problem",blocks:[{t:"p",text:"Because the view can be destroyed while the Fragment lives, you must tie view-related work (observing LiveData/Flow, view bindings) to the **view's** lifecycle, not the Fragment's. `viewLifecycleOwner` is a `LifecycleOwner` scoped to the current view — it's created in `onCreateView` and destroyed in `onDestroyView`."},{t:"code",title:"Always observe with viewLifecycleOwner",code:`override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
    // CORRECT — tied to the view's lifecycle
    viewLifecycleOwner.lifecycleScope.launch {
        viewLifecycleOwner.repeatOnLifecycle(Lifecycle.State.STARTED) {
            viewModel.uiState.collect { render(it) }
        }
    }

    // WRONG — tied to the FRAGMENT's lifecycle:
    // lifecycleScope.launch { viewModel.uiState.collect { } }
    // On back-stack return, the OLD collector is still active AND a new one
    // starts -> duplicate collectors, updating a destroyed view -> crash/leak.
}`},{t:"list",items:["**Use `viewLifecycleOwner`** (not `this`) for: observing LiveData, collecting flows, anything touching views. Its lifecycle matches the view, so observers are removed in `onDestroyView` — no leaks, no updates to dead views.","**The ViewBinding leak**: the same reason you null out a ViewBinding in `onDestroyView` (`_binding = null`) — the binding references views that are destroyed, so holding it past `onDestroyView` leaks the view hierarchy. The standard pattern uses a nullable backing property cleared in `onDestroyView`.","**Rule**: view-related → `viewLifecycleOwner`; fragment-instance-related (rare) → `this`. When in doubt for UI work, it's the view lifecycle."]}]},{heading:"Communication: between Fragments and with the Activity",blocks:[{t:"list",items:["**Shared ViewModel** (the modern way): scope a ViewModel to the host Activity (`by activityViewModels()`) or a navigation graph, so multiple Fragments share the same instance and communicate through its state. Clean and lifecycle-safe.","**Fragment Result API**: `setFragmentResult(key, bundle)` / `setFragmentResultListener(key) { }` — for one-off results between a Fragment and its parent (e.g. a picker Fragment returning a selection). Replaces the old interface-callback pattern.","**Navigation component**: for moving between Fragments with type-safe arguments and a managed back stack — the standard for Fragment navigation, analogous to Navigation Compose.","**Avoid**: direct references between Fragments (tight coupling, lifecycle hazards) and the old 'Fragment implements a callback interface the Activity calls' pattern (boilerplate, leak-prone)."]}]},{heading:"Fragments vs Activities, and the modern picture",blocks:[{t:"list",items:["**Activity** = an entry point / window managed by the OS, heavier, declared in the manifest. **Fragment** = a lighter UI module hosted within an Activity, managed by your `FragmentManager`, not in the manifest. Fragments enable multiple UI panels in one Activity and reusable UI.","**Single-Activity architecture** became the recommendation: one Activity, many Fragments (or Compose destinations) + Navigation component. Fewer Activities means simpler manifest, easier shared state (activity-scoped ViewModel), and smoother transitions.","**With Jetpack Compose**, Fragments become largely optional — you can have a single Activity hosting Compose destinations with Navigation-Compose and no Fragments at all. But Fragments remain ubiquitous in existing codebases, and interop (`ComposeView` in a Fragment, or a Fragment in a Compose-based app) is common — so understanding Fragment lifecycle is still essential."]},{t:"note",text:"The Fragment essentials to nail: the two lifecycles (instance vs view) and why they differ (back stack destroys the view but keeps the instance); always use `viewLifecycleOwner` for view-related observation and null ViewBinding in `onDestroyView`; communicate via shared ViewModels or the Fragment Result API, never direct references; and know that single-Activity + Navigation (or Compose) is the modern architecture."}]}],sA=[{level:"junior",q:"What is a Fragment and why do Fragments exist?",a:[{t:"p",text:"**The concept**: a Fragment is a reusable, modular piece of UI with its own lifecycle and layout that lives *inside* an Activity. It's like a sub-screen or UI component hosted by an Activity — it can't exist on its own. You add it to a container in the Activity via the `FragmentManager`."},{t:"p",text:"**Why they exist**: two main reasons. First, **adaptive layouts for different screen sizes** — on a tablet you might show a list Fragment and a detail Fragment side by side, while on a phone you show them one at a time; Fragments let you compose the same UI pieces differently per device. Second, **reusable UI and single-Activity navigation** — instead of one Activity per screen (heavy, manifest-declared, awkward to share state), you have one Activity hosting many Fragments that you navigate between, sharing an activity-scoped ViewModel. Modern Android favors this single-Activity architecture with the Navigation component. With Jetpack Compose, Fragments become optional (Compose destinations replace them), but they remain everywhere in existing codebases."}]},{level:"junior",q:"What is the difference between a Fragment and an Activity?",a:[{t:"list",items:["**Ownership/management**: an Activity is an app entry point managed by the *OS* and declared in the manifest; a Fragment is a UI module managed by *your* `FragmentManager` and not in the manifest. Fragments live inside Activities.","**Weight**: Activities are heavier (full window, OS-tracked); Fragments are lighter UI components you can add, remove, and swap within one Activity.","**Multiplicity**: one Activity can host many Fragments simultaneously (multi-pane layouts) or swap them (navigation), enabling single-Activity architecture.","**Lifecycle**: both have lifecycles, but a Fragment has *two* (instance and view), and its lifecycle is tied to — but distinct from — its host Activity's."]},{t:"p",text:"The practical takeaway: use Activities as coarse app entry points (often just one, or a few), and Fragments (or Compose destinations) as the actual screens within them. This gives simpler shared state (activity-scoped ViewModels), smoother transitions, and a cleaner manifest than one-Activity-per-screen. The main cost is Fragment lifecycle complexity — especially the two-lifecycle issue — which is why they have a reputation for being tricky."}]},{level:"junior",q:"Where should you set up UI and observe data in a Fragment — onCreateView, onViewCreated, or onCreate?",a:[{t:"p",text:"**Set up UI and observe data in `onViewCreated`.** The reasoning: `onCreate` runs before the view exists (so you can't touch views there); `onCreateView` is specifically for *creating/inflating* the view hierarchy and returning it (with ViewBinding, this is where you inflate); and `onViewCreated` runs right after, when the view is fully created and ready — the correct place to configure views, set click listeners, and start observing data to render into the view."},{t:"code",title:"The standard structure",code:`override fun onCreateView(inflater, container, saved): View {
    _binding = FragmentBinding.inflate(inflater, container, false)
    return binding.root                       // just create the view
}

override fun onViewCreated(view: View, saved: Bundle?) {
    super.onViewCreated(view, saved)
    binding.button.setOnClickListener { }     // configure views here
    viewLifecycleOwner.lifecycleScope.launch { // observe data here
        viewModel.uiState.collect { render(it) }
    }
}

override fun onDestroyView() {
    super.onDestroyView()
    _binding = null                            // avoid leaking the view
}`},{t:"p",text:"And crucially, when observing, use `viewLifecycleOwner` (not `this`), because the observation should be tied to the *view's* lifetime, not the Fragment instance's. Do fragment-instance-level setup (that doesn't touch views) in `onCreate`, and view-level setup in `onViewCreated`."}]},{level:"junior",q:"How do two Fragments communicate with each other?",a:[{t:"p",text:"**The modern, recommended way is a shared ViewModel** — scope a ViewModel to the host Activity (or a navigation graph) with `by activityViewModels()`, so both Fragments get the *same* ViewModel instance and communicate through its shared state. Fragment A updates the ViewModel's state; Fragment B observes it. This is clean, lifecycle-safe, and decoupled — neither Fragment references the other."},{t:"list",items:["**Shared ViewModel** (`activityViewModels()` or nav-graph-scoped) — for ongoing shared state between Fragments. The go-to approach.","**Fragment Result API** — `setFragmentResult(key, bundle)` and `setFragmentResultListener(key) { }` — for one-off results, like a picker Fragment returning a selection to the Fragment that launched it. Replaces the old callback-interface pattern.","**Navigation component with arguments** — for passing data when navigating from one Fragment to another (type-safe args)."]},{t:"p",text:"What to *avoid*: direct references from one Fragment to another (tight coupling, and lifecycle hazards since Fragments come and go), and the old pattern where a Fragment defines a callback interface that the Activity implements to relay messages (boilerplate and leak-prone). The principle is the same as elsewhere in Android — communicate through shared state or explicit result channels, not direct object references."}]},{level:"senior",q:"Explain the difference between a Fragment's lifecycle and its view lifecycle, and why it matters.",a:[{t:"p",text:"**A Fragment has two distinct lifecycles: the *instance* lifecycle (`onCreate` → `onDestroy`, tracking the Fragment object) and the *view* lifecycle (`onCreateView` → `onDestroyView`, tracking its UI). The critical fact is that the view can be destroyed and recreated while the Fragment instance stays alive.** This happens on the back stack: when you navigate away from a Fragment that's added to the back stack, Android calls `onDestroyView` to tear down its view (freeing memory) but does *not* call `onDestroy` — the Fragment instance is retained. Navigate back, and `onCreateView` runs again, creating a *brand-new view* for the *same* Fragment instance."},{t:"list",items:["**Why it matters — leaks and crashes**: if you observe data or hold references using the Fragment's *own* lifecycle (`this`), those observers survive across `onDestroyView`. So when you return to the Fragment, you have the old observer (still trying to update the *destroyed* view — crash, or a leak of that view) *plus* a new observer registered in the new `onViewCreated` — duplicate observers, one of them pointed at a dead view.","**The fix — `viewLifecycleOwner`**: for anything view-related (observing LiveData/Flow, updating views), use `viewLifecycleOwner`, whose lifecycle matches the *view* (created in `onCreateView`, destroyed in `onDestroyView`). Then observers are automatically removed in `onDestroyView`, so there's exactly one live observer tied to the current view, and none pointing at destroyed views.","**The ViewBinding corollary**: a ViewBinding holds references to the view hierarchy, so keeping it past `onDestroyView` leaks the whole destroyed view. The standard pattern is a nullable backing field set to null in `onDestroyView` (`_binding = null`), forcing you to only access the binding while the view exists."]},{t:"code",title:"The bug the two-lifecycle distinction causes",code:`// BUG: 'this' (fragment lifecycle) outlives the view
lifecycleScope.launch { viewModel.data.collect { binding.text.text = it } }
// After back-stack return: old collector alive (binding is stale/null) + new one

// CORRECT: viewLifecycleOwner matches the view
viewLifecycleOwner.lifecycleScope.launch {
    viewLifecycleOwner.repeatOnLifecycle(STARTED) {
        viewModel.data.collect { binding.text.text = it }
    }
}`},{t:"p",text:"**The senior framing**: the two lifecycles exist because Fragments optimize memory by destroying views of back-stacked Fragments while keeping the (cheaper) instance and its state. That optimization means 'the Fragment' and 'the Fragment's view' have different lifetimes, and *every* view-touching operation must be scoped to the view lifecycle (`viewLifecycleOwner`) or you get the classic leak/crash. This single distinction explains the majority of Fragment bugs, which is why it's such a common senior question."}]},{level:"senior",q:"Why is a ViewModel scoped to a Fragment vs its Activity vs a navigation graph, and how do you choose?",a:[{t:"p",text:"**A ViewModel's scope determines how long it lives and who shares it — you choose the scope to match the lifetime and sharing you need.** Each scope corresponds to a `ViewModelStoreOwner` whose lifecycle bounds the ViewModel."},{t:"list",items:["**Fragment-scoped** (`by viewModels()`) — the ViewModel lives as long as *this* Fragment, cleared when the Fragment is permanently removed. Use for state private to one screen. It survives that Fragment's configuration changes but is not shared with anyone.","**Activity-scoped** (`by activityViewModels()`) — one ViewModel shared by *all* Fragments in the host Activity, living as long as the Activity. Use to **share state between sibling Fragments** (the classic master-detail communication) or for state that should persist across several Fragments in the same Activity. The caution: it lives for the whole Activity, so it can accumulate state and be overkill if only a couple of Fragments need it briefly.","**Navigation-graph-scoped** (`by navGraphViewModels(R.id.checkout_graph)` / `hiltViewModel(backStackEntry)`) — one ViewModel shared by all Fragments *within a specific navigation (sub)graph*, cleared when you leave that graph. Use for **multi-step flows** — a checkout or onboarding wizard — where several screens share state that should be scoped to *just that flow* and cleaned up when it ends. This is often better than activity-scoping because the lifetime matches the flow, not the whole app session."]},{t:"p",text:"**How to choose**: match the ViewModel's lifetime to the *breadth and duration* of what needs the state. State for one screen → Fragment scope. State shared across sibling screens for the Activity's lifetime → Activity scope. State shared across a bounded multi-screen flow that should be discarded afterward → nav-graph scope (the sweet spot for wizards). The anti-pattern to avoid is activity-scoping *everything* as a lazy 'share it globally' habit — that outlives its usefulness, accumulates stale state, and blurs data ownership. The right scope gives you sharing *and* correct, automatic cleanup."}]},{level:"junior",q:"What are the Fragment lifecycle callbacks, in order?",a:[{t:"p",text:"A Fragment has more callbacks than an Activity because it has *two* lifecycles — the fragment and its view. The order on creation: `onAttach` → `onCreate` → `onCreateView` → `onViewCreated` → `onViewStateRestored` → `onStart` → `onResume`; on teardown: `onPause` → `onStop` → `onDestroyView` → `onDestroy` → `onDetach`."},{t:"list",items:["**`onAttach`** — attached to the host (Activity/parent).","**`onCreate`** — fragment created (no view yet); read arguments.","**`onCreateView`** — inflate/return the view.","**`onViewCreated`** — view ready; set up UI, observe data (with `viewLifecycleOwner`).","**`onDestroyView`** — the view is destroyed (fragment may live on); null out bindings.","**`onDestroy`/`onDetach`** — fragment destroyed, then detached."]},{t:"note",text:"Fragment order: onAttach → onCreate → onCreateView → onViewCreated → onViewStateRestored → onStart → onResume; teardown: onPause → onStop → onDestroyView → onDestroy → onDetach. Two lifecycles (fragment + view) — onCreateView/onViewCreated/onDestroyView are the view-lifecycle ones."}]},{level:"senior",q:"What is viewLifecycleOwner, and why use it for observing data?",a:[{t:"p",text:"A Fragment's *view* has a shorter lifecycle than the Fragment itself — the view is created in `onCreateView` and destroyed in `onDestroyView`, but the Fragment instance can outlive it (e.g. on the back stack). `viewLifecycleOwner` represents the *view's* lifecycle. Observe LiveData/collect flows with it (not the Fragment's `this`) so observers are removed when the view is destroyed, preventing crashes and leaks."},{t:"code",title:"Observe with viewLifecycleOwner",code:`override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
    viewModel.data.observe(viewLifecycleOwner) { render(it) }   // NOT 'this'
    viewLifecycleOwner.lifecycleScope.launch {
        repeatOnLifecycle(Lifecycle.State.STARTED) { viewModel.state.collect { } }
    }
}`},{t:"list",items:["**View lifecycle < fragment lifecycle** — view destroyed in `onDestroyView`, fragment may persist.","**`viewLifecycleOwner`** — the view's lifecycle owner.","**Observe with it** — observers auto-removed on `onDestroyView` (no updates to a dead view).","**Bug with `this`** — using the Fragment as owner can re-add duplicate observers or update a destroyed view."]},{t:"note",text:"A Fragment's view lifecycle is shorter than the Fragment's (view: onCreateView→onDestroyView; fragment can outlive it on the back stack). Observe LiveData/flows with viewLifecycleOwner (not `this`) so observers are removed on onDestroyView — preventing updates to a destroyed view and duplicate observers."}]},{level:"senior",q:"How do you avoid the ViewBinding leak in a Fragment?",a:[{t:"p",text:"A Fragment can outlive its view, so a `binding` field holding the (destroyed) view's binding leaks it. The standard fix: assign the binding in `onCreateView`/`onViewCreated` and *null it out in `onDestroyView`*. A common pattern uses a nullable backing field with a non-null accessor valid only between those callbacks."},{t:"code",title:"Null binding in onDestroyView",code:`private var _binding: FragmentHomeBinding? = null
private val binding get() = _binding!!   // valid between onCreateView and onDestroyView

override fun onCreateView(...): View {
    _binding = FragmentHomeBinding.inflate(inflater, container, false)
    return binding.root
}
override fun onDestroyView() { super.onDestroyView(); _binding = null }   // avoid leak`},{t:"list",items:["**Fragment outlives view** — a retained binding leaks the old view hierarchy.","**Null in `onDestroyView`** — release the binding when the view is gone.","**Nullable + accessor** — `_binding` nullable, `binding` non-null accessor between view callbacks.","**Delegate libraries** — `viewBinding()` delegates automate the null-out."]},{t:"note",text:"A Fragment outlives its view, so a retained ViewBinding leaks the destroyed view. Fix: nullable _binding + non-null accessor, assign in onCreateView, and set _binding = null in onDestroyView. Or use a viewBinding() property delegate that auto-nulls. Only touch the binding between onCreateView and onDestroyView."}]},{level:"senior",q:"What is the FragmentManager, and what are transactions?",a:[{t:"p",text:"The `FragmentManager` manages a host's fragments and their back stack. You change fragments via a *transaction* (`beginTransaction()...commit()`) — a batched set of operations (add/replace/remove/show/hide) applied atomically. Optionally `addToBackStack()` so back navigation reverses the transaction."},{t:"code",title:"A transaction",code:`supportFragmentManager.beginTransaction()
    .replace(R.id.container, DetailFragment())
    .addToBackStack("detail")     // back reverses this
    .commit()`},{t:"list",items:["**`FragmentManager`** — `supportFragmentManager` (Activity), `childFragmentManager`, `parentFragmentManager`.","**Transaction** — batched add/replace/remove/show/hide, committed atomically.","**`addToBackStack`** — makes back navigation undo the transaction.","**Modern** — Navigation Component wraps this; you rarely write raw transactions."]},{t:"note",text:"FragmentManager manages a host's fragments + back stack (supportFragmentManager/childFragmentManager/parentFragmentManager). A transaction (beginTransaction()…commit()) applies batched add/replace/remove/show/hide atomically; addToBackStack makes back reverse it. Navigation Component wraps this; you rarely write raw transactions now."}]},{level:"junior",q:"What's the difference between add and replace in a fragment transaction?",a:[{t:"p",text:"`add` puts a new fragment *on top* of the container, keeping the previous fragment (and its view) underneath. `replace` *removes* the existing fragment(s) in the container and adds the new one — the old fragment's view is destroyed. Use `replace` for typical screen navigation; `add` (with `hide`/`show`) when you want to keep the previous fragment's state/view alive."},{t:"list",items:["**`add`** — stack a fragment over the container; previous stays (view alive).","**`replace`** — remove existing, add new; old view destroyed.","**`hide`/`show`** — with `add`, toggle visibility without destroying views.","**Back stack** — `addToBackStack` + `replace` re-adds the removed fragment on back."]},{t:"note",text:"add stacks a fragment over the container (previous stays, view alive underneath); replace removes existing fragments and adds the new one (old view destroyed). Use replace for screen navigation, add + hide/show to keep the previous fragment's view/state alive (e.g. bottom-nav tabs)."}]},{level:"senior",q:"What is the difference between commit, commitNow, and commitAllowingStateLoss?",a:[{t:"p",text:"`commit()` schedules the transaction *asynchronously* (runs on the next main-loop pass). `commitNow()` executes it *synchronously* (can't be added to the back stack). `commitAllowingStateLoss()` is like `commit` but *won't throw* if called after `onSaveInstanceState` — at the cost of potentially losing that transaction on state restore. The classic `IllegalStateException: Can not perform this action after onSaveInstanceState` comes from committing too late."},{t:"list",items:["**`commit()`** — async; throws if after `onSaveInstanceState`.","**`commitNow()`** — synchronous; no back stack.","**`commitAllowingStateLoss()`** — won't throw post-save-state, but may lose the transaction.","**Better** — restructure so you don't commit after state is saved (e.g. `lifecycleScope` at STARTED)."]},{t:"note",text:"commit() = async (throws IllegalStateException if after onSaveInstanceState); commitNow() = synchronous (no back stack); commitAllowingStateLoss() = won't throw post-save-state but may lose the transaction on restore. The 'after onSaveInstanceState' crash means committing too late — restructure to commit while at least STARTED instead of relying on allowingStateLoss."}]},{level:"senior",q:"What is the Fragment Result API (setFragmentResult)?",a:[{t:"p",text:"The Fragment Result API lets one fragment pass a result to another via the `FragmentManager` without direct references. The receiver registers a listener with `setFragmentResultListener(requestKey)`, and the sender calls `setFragmentResult(requestKey, bundle)`. The result is delivered when the receiver is at least STARTED, surviving configuration changes — the modern replacement for target fragments / interface callbacks."},{t:"code",title:"Fragment results",code:`// Receiver
setFragmentResultListener("pickColor") { _, bundle ->
    val color = bundle.getInt("color")
}
// Sender (e.g. a DialogFragment)
setFragmentResult("pickColor", bundleOf("color" to selected))`},{t:"list",items:["**`setFragmentResultListener(key) { }`** — receiver listens for a result.","**`setFragmentResult(key, bundle)`** — sender delivers it.","**Via FragmentManager** — no direct references between fragments.","**Lifecycle-safe** — delivered at STARTED; survives config change. Replaces setTargetFragment."]},{t:"note",text:"Fragment Result API: receiver setFragmentResultListener(requestKey) { }, sender setFragmentResult(requestKey, bundle) — passes results via the FragmentManager with no direct references, delivered when the receiver is ≥ STARTED and surviving config change. The modern replacement for setTargetFragment/interface callbacks."}]},{level:"junior",q:"How do you pass arguments to a Fragment, and why not use the constructor?",a:[{t:"p",text:"Pass data via the `arguments` `Bundle` (set it right after construction, usually in a `newInstance` factory). *Don't* use a custom constructor with parameters, because the system *recreates fragments* using the *no-arg constructor* on configuration change/process death — your constructor args would be lost, causing crashes. The `arguments` Bundle is restored automatically."},{t:"code",title:"newInstance + arguments",code:`class DetailFragment : Fragment() {
    companion object {
        fun newInstance(id: String) = DetailFragment().apply {
            arguments = bundleOf("id" to id)
        }
    }
    private val id get() = requireArguments().getString("id")!!
}`},{t:"list",items:["**`arguments` Bundle** — set after construction; restored by the system.","**No-arg constructor required** — the system recreates fragments with it.","**`newInstance` factory** — the idiomatic pattern to set arguments.","**Navigation Component** — Safe Args generates typed argument passing."]},{t:"note",text:"Pass data via the arguments Bundle (set in a newInstance factory) — NOT a custom constructor, because the system recreates fragments with the no-arg constructor on config change/process death, losing constructor args (crash). The arguments Bundle is restored automatically; Navigation Safe Args gives typed access."}]},{level:"senior",q:"What is the difference between childFragmentManager and parentFragmentManager?",a:[{t:"p",text:"`childFragmentManager` manages fragments *nested inside* this fragment (e.g. a ViewPager's pages, or fragments in this fragment's own container). `parentFragmentManager` is the FragmentManager of whatever *hosts* this fragment (the Activity or a parent fragment). Use `childFragmentManager` for nested fragments; `parentFragmentManager` to interact with siblings/host-level navigation."},{t:"list",items:["**`childFragmentManager`** — this fragment's own nested fragments.","**`parentFragmentManager`** — the host's manager (Activity or parent fragment).","**Nesting** — ViewPager2/nested navigation use the child manager.","**Result API scope** — set the listener on the manager the sender uses."]},{t:"note",text:"childFragmentManager manages fragments nested INSIDE this fragment (ViewPager pages, nested containers); parentFragmentManager is the host's manager (Activity or parent fragment). Use child for nested fragments, parent for sibling/host interaction. Fragment results must use the same manager on both ends."}]},{level:"junior",q:"What is a DialogFragment, and why prefer it over a raw Dialog?",a:[{t:"p",text:"A `DialogFragment` is a Fragment that hosts a dialog — it manages the dialog's lifecycle and, crucially, *survives configuration changes* (the system recreates it), unlike a raw `Dialog` you'd have to manually dismiss/recreate on rotation. Show it with `show(fragmentManager, tag)`."},{t:"code",title:"DialogFragment",code:`class ConfirmDialog : DialogFragment() {
    override fun onCreateDialog(savedInstanceState: Bundle?): Dialog =
        AlertDialog.Builder(requireContext())
            .setTitle("Delete?")
            .setPositiveButton("Yes") { _, _ -> setFragmentResult("confirm", bundleOf("ok" to true)) }
            .create()
}
ConfirmDialog().show(parentFragmentManager, "confirm")`},{t:"list",items:["**Lifecycle-managed** — survives config change (recreated by the system).","**`onCreateDialog`** — build the dialog; or `onCreateView` for a custom layout.","**`show(fm, tag)`** — display it via the FragmentManager.","**Results** — pair with the Fragment Result API to return the choice."]},{t:"note",text:"A DialogFragment hosts a dialog with lifecycle management and survives configuration changes (system recreates it) — unlike a raw Dialog you'd manually handle on rotation. Build it in onCreateDialog (or onCreateView for custom UI), show via show(fm, tag), and return choices with the Fragment Result API."}]},{level:"junior",q:"What is the single-Activity architecture with fragments?",a:[{t:"p",text:"Single-Activity architecture uses *one* Activity hosting a `NavHostFragment`, with each screen as a *fragment* (or Compose destination) navigated via the Navigation Component. It centralizes navigation, simplifies the back stack, shares Activity-scoped state easily, and avoids the complexity of many Activities with intents. It's the recommended modern approach."},{t:"list",items:["**One Activity** — hosts a `NavHostFragment`; screens are fragments/Compose.","**Navigation Component** — centralized nav graph and back stack.","**Shared state** — Activity/graph-scoped ViewModels across screens.","**Benefits** — simpler navigation, transitions, deep links vs multi-Activity + intents."]},{t:"note",text:"Single-Activity architecture: one Activity hosting a NavHostFragment, each screen a fragment (or Compose destination) navigated via the Navigation Component. Centralizes navigation/back stack, eases shared (graph/activity-scoped) state, and simplifies transitions/deep links vs multi-Activity + intents. The recommended modern approach."}]},{level:"senior",q:"What is the difference between hide/show and add/remove/replace?",a:[{t:"p",text:"`hide`/`show` toggle a fragment's *visibility* while keeping its instance *and view* alive (state preserved, instant switch). `remove`/`replace` *destroy* the fragment's view (`onDestroyView`) and detach the fragment. Use `hide`/`show` for tabs where re-inflating is wasteful and you want to preserve scroll/state; use `replace` for normal navigation where you don't need the old screen retained."},{t:"list",items:["**`hide`/`show`** — keep instance + view; toggle visibility; state preserved.","**`remove`/`replace`** — destroy the view; fragment detached/destroyed.","**Tabs** — `hide`/`show` (or `add`) avoids re-creating each tab.","**Navigation** — `replace` for forward navigation."]},{t:"note",text:"hide/show toggle visibility while keeping the fragment instance AND view alive (state preserved, instant switch); remove/replace destroy the view (onDestroyView) and detach the fragment. Use hide/show (or add) for bottom-nav tabs (avoid re-inflation, keep scroll/state); replace for forward navigation."}]},{level:"senior",q:"What is FragmentFactory, and when do you need it?",a:[{t:"p",text:"A `FragmentFactory` customizes how the system *instantiates* fragments — needed when you want *constructor injection* of dependencies (since you can't use a custom constructor with the default factory, which requires no-arg). You register a factory that constructs fragments with their dependencies; Hilt provides this automatically for `@AndroidEntryPoint` fragments."},{t:"list",items:["**Customizes instantiation** — enables constructor injection into fragments.","**Register on the FragmentManager** — `fragmentManager.fragmentFactory = ...`.","**Recreation-safe** — the factory is used when the system recreates fragments too.","**Hilt** — `@AndroidEntryPoint` fragments get field injection without a manual factory."]},{t:"note",text:"FragmentFactory customizes fragment instantiation, enabling constructor injection (impossible with the default no-arg factory). Register it on the FragmentManager (used even on recreation). Hilt's @AndroidEntryPoint provides field injection into fragments without writing a manual factory."}]},{level:"junior",q:"How do fragments survive configuration changes?",a:[{t:"p",text:"By default, fragments are *destroyed and recreated* on configuration change along with their host Activity — their `arguments` Bundle and saved instance state are restored, and any `ViewModel` (scoped to the fragment) survives in memory. The old `retainInstance = true` (which kept the fragment instance across recreation) is *deprecated* — use a `ViewModel` instead."},{t:"list",items:["**Recreated** — fragment destroyed and rebuilt with the Activity.","**`arguments` + saved state** — restored automatically.","**ViewModel** — survives config change; holds data.","**`retainInstance` deprecated** — use a ViewModel for retained state."]},{t:"note",text:"Fragments are destroyed and recreated on config change (with the host Activity) — arguments Bundle + saved instance state restored, and a fragment-scoped ViewModel survives in memory. The old retainInstance = true is deprecated; use a ViewModel to retain data across recreation."}]},{level:"senior",q:"What are common Fragment pitfalls and how do you avoid them?",a:[{t:"p",text:"The frequent bugs: leaking the view/binding (not nulling in `onDestroyView`), observing with the Fragment instead of `viewLifecycleOwner` (updating a dead view / duplicate observers), committing transactions after `onSaveInstanceState` (IllegalStateException), using constructor args instead of `arguments`, and holding the host Activity/Context beyond the fragment's life."},{t:"list",items:["**View/binding leak** — null the binding in `onDestroyView`.","**Wrong observation owner** — use `viewLifecycleOwner`, not `this`.","**Late transactions** — don't commit after state is saved (or use STARTED-scoped triggers).","**Constructor args** — use the `arguments` Bundle + `newInstance`.","**Context retention** — don't hold the Activity/Context after `onDetach`."]},{t:"note",text:"Fragment pitfalls: view/binding leaks (null in onDestroyView), observing with `this` instead of viewLifecycleOwner (dead-view updates/duplicate observers), committing after onSaveInstanceState (IllegalStateException), constructor args instead of the arguments Bundle, and retaining the host Context past onDetach."}]},{level:"junior",q:"What is a BottomSheetDialogFragment?",a:[{t:"p",text:"`BottomSheetDialogFragment` is a `DialogFragment` that presents its content as a Material *bottom sheet* — sliding up from the bottom, draggable, with expand/collapse states. It gives you a modal bottom sheet with proper lifecycle management and config-change survival, without manually wiring a `BottomSheetBehavior`."},{t:"list",items:["**Modal bottom sheet** — slides up, draggable, expand/collapse.","**Lifecycle-managed** — like `DialogFragment`; survives config change.","**`onCreateView`** — provide the sheet's content layout.","**Behavior** — control peek height, draggability, state via `BottomSheetBehavior`."]},{t:"note",text:"BottomSheetDialogFragment is a DialogFragment presenting content as a Material modal bottom sheet (slide-up, draggable, expand/collapse) with lifecycle management and config-change survival. Provide content in onCreateView; tune peek height/draggability via BottomSheetBehavior. Compose has ModalBottomSheet as the equivalent."}]},{level:"senior",q:"How do fragment transactions interact with the back stack?",a:[{t:"p",text:"Calling `addToBackStack(name)` on a transaction records it so that pressing *back* (or `popBackStack()`) *reverses* it — re-adding removed fragments and removing added ones. Without `addToBackStack`, the transaction isn't undoable by back. You can pop to a named entry (`popBackStack(name, flags)`) to unwind multiple transactions at once."},{t:"list",items:["**`addToBackStack(name)`** — makes the transaction reversible by back.","**`popBackStack()`** — undo the last back-stack transaction.","**`popBackStack(name, INCLUSIVE)`** — unwind to (and optionally including) a named entry.","**Without it** — the transaction is permanent (back won't undo it)."]},{t:"note",text:"addToBackStack(name) records a transaction so back/popBackStack() reverses it (re-adding removed fragments). Without it, the transaction isn't undoable by back. popBackStack(name, INCLUSIVE) unwinds multiple transactions to a named entry — how you clear a flow (like login) from the back stack."}]},{level:"senior",q:"What is FragmentContainerView, and why use it over the <fragment> tag?",a:[{t:"p",text:"`FragmentContainerView` is the recommended container for hosting fragments (including `NavHostFragment`). It fixes issues the old `<fragment>` tag and `FrameLayout` had with fragment transactions, animations, and window insets — particularly around exit animations drawing on top. Use it as your nav host container and for dynamically added fragments."},{t:"code",title:"FragmentContainerView",code:`<androidx.fragment.app.FragmentContainerView
    android:id="@+id/nav_host"
    android:name="androidx.navigation.fragment.NavHostFragment"
    app:navGraph="@navigation/nav_graph"
    app:defaultNavHost="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />`},{t:"list",items:["**Recommended container** — for nav hosts and dynamic fragments.","**Fixes** — transaction/animation/insets issues of `<fragment>`/`FrameLayout`.","**Z-ordering** — correct exit-animation draw order.","**Inflate a fragment** — via `android:name`, or add transactions at runtime."]},{t:"note",text:"FragmentContainerView is the recommended fragment container (nav hosts, dynamic fragments) — it fixes the old <fragment> tag/FrameLayout problems with transactions, exit-animation z-ordering, and window insets. Use it for your NavHostFragment and any runtime-added fragments."}]},{level:"senior",q:"How does the Navigation Component work with fragments?",a:[{t:"p",text:"The Navigation Component uses a `NavHostFragment` (in a `FragmentContainerView`) driven by a nav graph (XML or Kotlin DSL) that declares destinations (fragments) and actions (edges). You navigate with a `NavController` (`findNavController().navigate(...)`), pass typed arguments via Safe Args, and get automatic back-stack handling, transitions, and deep links."},{t:"list",items:["**`NavHostFragment` + nav graph** — declares fragment destinations and actions.","**`NavController`** — `findNavController().navigate(R.id.action)` / with Safe Args.","**Safe Args** — typed argument passing between destinations.","**Automatic** — back stack, transitions, deep links, up navigation."]},{t:"note",text:"Navigation Component: a NavHostFragment (in a FragmentContainerView) + a nav graph declaring fragment destinations and actions; navigate via NavController (findNavController().navigate) with Safe Args for typed arguments. It handles the back stack, transitions, deep links, and up navigation automatically — the basis of single-Activity apps."}]},{level:"junior",q:"Where should you inflate the view versus set up UI logic in a Fragment?",a:[{t:"p",text:"Inflate and return the view in `onCreateView` (keep it to inflation). Do UI setup — click listeners, adapters, observing data — in `onViewCreated`, where the view is guaranteed created and `viewLifecycleOwner` is available. Don't touch views in `onCreate` (no view yet)."},{t:"code",title:"onCreateView vs onViewCreated",code:`override fun onCreateView(inflater: LayoutInflater, ...): View {
    _binding = FragmentXBinding.inflate(inflater, container, false)
    return binding.root                       // just inflate
}
override fun onViewCreated(view: View, s: Bundle?) {
    binding.list.adapter = adapter            // set up UI here
    viewModel.state.observe(viewLifecycleOwner) { render(it) }
}`},{t:"list",items:["**`onCreateView`** — inflate and return the view only.","**`onViewCreated`** — set up listeners/adapters, observe data.","**`viewLifecycleOwner`** — available in `onViewCreated`.","**Not `onCreate`** — no view exists there."]},{t:"note",text:"Inflate/return the view in onCreateView (inflation only); do UI setup (listeners, adapters, data observation with viewLifecycleOwner) in onViewCreated, where the view is guaranteed. Don't touch views in onCreate (no view yet)."}]},{level:"junior",q:"How do you share a ViewModel between fragments?",a:[{t:"p",text:"Scope the ViewModel to a *common owner* both fragments share — the Activity (`by activityViewModels()`) or a navigation graph (`by navGraphViewModels(R.id.graph)`). Both fragments then get the *same* ViewModel instance, so one can update state the other observes — a clean way to share data without direct references."},{t:"code",title:"Shared ViewModel",code:`class FragmentA : Fragment() {
    private val shared: SharedViewModel by activityViewModels()   // Activity-scoped
}
class FragmentB : Fragment() {
    private val shared: SharedViewModel by activityViewModels()   // same instance
}`},{t:"list",items:["**`by activityViewModels()`** — Activity-scoped; shared across all its fragments.","**`by navGraphViewModels(id)`** — scoped to a nav graph (a flow).","**Same instance** — both fragments share state.","**Avoid over-sharing** — Activity-scope only what genuinely needs to be shared."]},{t:"note",text:"Share a ViewModel by scoping it to a common owner: by activityViewModels() (Activity-scoped, all fragments) or by navGraphViewModels(R.id.graph) (a flow). Both fragments get the same instance, so one updates state the other observes — no direct references. Scope narrowly (graph over activity) to avoid over-sharing."}]},{level:"senior",q:"What is a nested fragment, and when do you use childFragmentManager?",a:[{t:"p",text:"A nested fragment is a fragment hosted *inside another fragment* — added via the parent fragment's `childFragmentManager`. Common cases: a `ViewPager2` whose pages are fragments, or a screen with a sub-section that's itself a fragment. Nesting keeps the child's lifecycle tied to the parent fragment's view."},{t:"list",items:["**Nested fragment** — a fragment inside a fragment's own container.","**`childFragmentManager`** — manage the nested fragments.","**Uses** — `ViewPager2` with fragment pages, sub-sections, nested navigation.","**Lifecycle** — the child follows the parent fragment's view lifecycle."]},{t:"note",text:"A nested fragment lives inside another fragment, managed via the parent's childFragmentManager — used for ViewPager2 fragment pages, sub-sections, or nested navigation. The child's lifecycle is tied to the parent fragment's view. Use childFragmentManager (not parentFragmentManager) for these."}]},{level:"junior",q:"How do you save and restore a Fragment's own state?",a:[{t:"p",text:"A Fragment has `onSaveInstanceState(outState)` for small UI state (like an Activity), restored via the `savedInstanceState` passed to `onCreate`/`onCreateView`/`onViewCreated`. For data that should survive config change, use a fragment-scoped `ViewModel`; for process-death-critical inputs, use `SavedStateHandle`. The `arguments` Bundle is also restored automatically."},{t:"list",items:["**`onSaveInstanceState`** — small UI state; restored via `savedInstanceState`.","**ViewModel** — data across config change.","**`SavedStateHandle`** — process-death-critical inputs.","**`arguments`** — restored automatically."]},{t:"note",text:"Fragment state: onSaveInstanceState(outState) for small UI state (restored via savedInstanceState in onCreate/onCreateView/onViewCreated); a fragment-scoped ViewModel for data across config change; SavedStateHandle for process-death inputs; the arguments Bundle is restored automatically."}]},{level:"senior",q:"Why was onActivityCreated deprecated, and what replaces it?",a:[{t:"p",text:"`onActivityCreated` was deprecated because it coupled the fragment's setup to the *host Activity's* creation, which is the wrong dependency — a fragment should depend on its *own view* lifecycle, not the Activity's. Move view setup to `onViewCreated` (with `viewLifecycleOwner`) and non-view initialization to `onCreate`. This decoupling makes fragments more reusable and testable."},{t:"list",items:["**Deprecated** — coupled fragment setup to the Activity's creation.","**View setup → `onViewCreated`** — with `viewLifecycleOwner`.","**Non-view init → `onCreate`** — reading arguments, etc.","**Benefit** — fragment depends on its own lifecycle, not the host's."]},{t:"note",text:"onActivityCreated was deprecated because it tied fragment setup to the host Activity's creation — the wrong dependency. Do view setup in onViewCreated (viewLifecycleOwner) and non-view init in onCreate, so the fragment depends on its own lifecycle — more reusable and testable."}]}],rA={id:"fragments",title:"Fragments & Lifecycle",tagline:"What Fragments are, the instance vs view lifecycle, viewLifecycleOwner & ViewBinding leaks, communication patterns, and ViewModel scoping.",tags:["Android","Fragments","viewLifecycleOwner","Lifecycle"],content:oA,qa:sA},lA=[{heading:"Intents — the messaging system",blocks:[{t:"p",text:"An **Intent** is a messaging object used to request an action from a component — most commonly to *start an Activity*, but also to start services or deliver broadcasts. It's how components (even across different apps) communicate. There are two kinds: **explicit** (you name the exact component) and **implicit** (you describe *what you want done* and let the system find a component that can do it)."},{t:"code",title:"Explicit vs implicit intents",code:`// EXPLICIT — you name the target component (within your app)
val intent = Intent(this, DetailActivity::class.java).apply {
    putExtra("itemId", 42)
}
startActivity(intent)

// IMPLICIT — describe an action; the system finds a handler
val share = Intent(Intent.ACTION_SEND).apply {
    type = "text/plain"
    putExtra(Intent.EXTRA_TEXT, "Check this out")
}
startActivity(Intent.createChooser(share, "Share via"))

// Implicit: open a URL — any browser can handle it
startActivity(Intent(Intent.ACTION_VIEW, Uri.parse("https://example.com")))`},{t:"list",items:["**Explicit intent** — names the target class directly. Used for navigation *within your app* (you know exactly what to launch). Required for starting your own components securely.","**Implicit intent** — specifies an *action* (`ACTION_VIEW`, `ACTION_SEND`, `ACTION_DIAL`), optional *data* (a URI), and *category*; the system matches it against apps' declared `<intent-filter>`s and either launches the handler or shows a chooser. Used for cross-app actions: share, open URL, dial, pick a photo.","**Extras** — key/value data attached via `putExtra`; the receiver reads them from `intent`. Also `data` (a URI) and `flags` (task/launch behavior).","**`PendingIntent`** — a wrapper that lets *another* app or the system fire an intent *on your behalf later* (used by notifications, alarms, widgets). It grants the holder permission to execute your intent."]}]},{heading:"Intent filters — how implicit intents get resolved",blocks:[{t:"p",text:"An app declares what implicit intents it can handle via **`<intent-filter>`** elements in the manifest. When an implicit intent is sent, the system matches it against all installed apps' filters by **action**, **data** (scheme/host/mime type), and **category**. If exactly one matches it launches directly; if several match, the user sees a chooser."},{t:"code",title:"Declaring the app can handle a deep link",code:`<activity android:name=".ProductActivity">
    <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="shop.example.com"
              android:pathPrefix="/product" />
    </intent-filter>
</activity>`},{t:"list",items:["This is how **deep links** work: a URL or notification intent resolves to your Activity because its filter matches. `autoVerify` + a Digital Asset Links file makes it an **App Link** (opens your app directly without a chooser).","The `LAUNCHER` filter (`ACTION_MAIN` + `CATEGORY_LAUNCHER`) is what puts an Activity's icon in the app drawer as the entry point.",'Security note: an Activity with an intent filter is **exported** (callable by other apps) unless you set `android:exported="false"` — a common security consideration.']}]},{heading:"Tasks and the back stack",blocks:[{t:"p",text:"A **task** is a collection of Activities the user interacts with to accomplish something, arranged in a **back stack** (a stack of Activities). When you launch an Activity, it's normally *pushed* onto the current task's back stack; pressing Back *pops* it. The task is what you see in the Recents/Overview screen. Understanding tasks and the back stack is essential for controlling navigation behavior."},{t:"list",items:["**Default behavior**: `startActivity` pushes the new Activity onto the current task's stack. Back pops to the previous one. Simple LIFO.","**The back stack can contain multiple instances** of the same Activity by default — navigate A → B → A and you have two A instances stacked. Sometimes that's wrong (you want to return to the *existing* A, not create a new one), which is what launch modes and flags control.","**Recents**: each task appears as a card; the OS may destroy Activities within a backgrounded task to reclaim memory (process death), recreating them from saved state when the user returns."]}]},{heading:"Launch modes — controlling how Activities are added to tasks",blocks:[{t:"p",text:"**Launch modes** control whether launching an Activity creates a new instance or reuses an existing one, and in which task. Set via `android:launchMode` in the manifest (or Intent flags at runtime). The four modes:"},{t:"table",headers:["Mode","Behavior"],rows:[["`standard` (default)","always creates a new instance, pushed on the current task — duplicates allowed"],["`singleTop`","if an instance is already at the TOP of the stack, reuse it (onNewIntent) instead of creating a new one"],["`singleTask`","only one instance exists in the whole system; if it exists, bring its task forward and clear Activities above it (onNewIntent)"],["`singleInstance`","like singleTask, but the Activity is the ONLY one in its task — no other Activities can join it"]]},{t:"list",items:["**`standard`** — the default; every launch is a new instance. Fine for most screens.","**`singleTop`** — prevents *consecutive duplicates*. If A is on top and you launch A again, the existing one receives the new intent via `onNewIntent()` rather than stacking a second A. Common for a search or notification target you don't want duplicated when already showing.","**`singleTask`** — a *single* system-wide instance; launching it brings its task to the front and clears everything above it in that task. Used for a main/home Activity or a document root — an entry point that should be unique.","**`singleInstance`** — the strictest: sole instance in its own dedicated task, nothing else allowed in that task. Rare — for special standalone screens (e.g. a launcher, some telephony screens)."]}]},{heading:"Intent flags — runtime control of task behavior",blocks:[{t:"list",items:["**`FLAG_ACTIVITY_NEW_TASK`** — start the Activity in a new task (required when launching from a non-Activity context like a Service or notification).","**`FLAG_ACTIVITY_CLEAR_TOP`** — if the target already exists in the stack, clear all Activities above it and deliver the intent to it (rather than making a new one). Common for 'go back to home and clear the stack'.","**`FLAG_ACTIVITY_SINGLE_TOP`** — the flag equivalent of `singleTop` behavior for this launch.","**`CLEAR_TASK` + `NEW_TASK`** — clear the entire task and start fresh (e.g. logout → return to login with no back-stack history).","Flags let you apply launch behavior *per launch* at runtime, whereas `launchMode` sets it *per Activity* in the manifest. Flags override/augment the manifest mode for that specific launch."]},{t:"note",text:"The essentials: Intents (explicit = name the target, implicit = describe an action for the system to resolve via intent filters); PendingIntent lets others fire your intent later; tasks are back stacks of Activities; launch modes (standard/singleTop/singleTask/singleInstance) and Intent flags (NEW_TASK, CLEAR_TOP, SINGLE_TOP, CLEAR_TASK) control instance reuse and task placement. onNewIntent() delivers intents to a reused instance."}]}],cA=[{level:"junior",q:"What is the difference between an explicit and an implicit Intent?",a:[{t:"p",text:"**An Intent is a messaging object that requests an action — usually to start an Activity. The difference is whether you name the target or describe what you want done.** An *explicit* intent names the exact component (`Intent(this, DetailActivity::class.java)`) — you know precisely what to launch, used for navigation within your own app. An *implicit* intent describes an *action* (`ACTION_VIEW`, `ACTION_SEND`) and optional data, and lets the system find an app that can handle it."},{t:"code",title:"Both kinds",code:`// Explicit — launch a known component
startActivity(Intent(this, DetailActivity::class.java))

// Implicit — 'open this URL', system finds a browser
startActivity(Intent(Intent.ACTION_VIEW, Uri.parse("https://x.com")))`},{t:"p",text:"Use **explicit** for internal navigation (and it's required for security when starting your own components). Use **implicit** for cross-app actions where you don't care *which* app does it — sharing text, opening a URL, dialing a number, picking a photo. The system matches implicit intents against apps' declared `<intent-filter>`s; if multiple apps can handle it, the user gets a chooser. You attach data with `putExtra` (extras) and the receiver reads it from its `intent`."}]},{level:"junior",q:"What is an intent filter?",a:[{t:"p",text:"**The concept**: an `<intent-filter>` is a manifest declaration that tells the system *what implicit intents a component can handle*. It specifies an **action** (what to do, like `VIEW` or `SEND`), optional **data** (a URI scheme/host/path or MIME type), and a **category**. When an implicit intent is fired, Android matches it against every app's intent filters to find components that can respond."},{t:"list",items:["**It's how implicit intents get resolved**: your intent says 'ACTION_VIEW this https URL', and any Activity whose filter declares it handles `VIEW` + `https` data becomes a candidate.","**Deep links** use intent filters — declaring a filter for your app's URLs (`scheme=https, host=shop.example.com`) lets those links open your app. Adding `autoVerify` + a Digital Asset Links file makes it an **App Link** that opens directly without a chooser.","**The launcher icon** comes from a special filter: `ACTION_MAIN` + `CATEGORY_LAUNCHER` marks the Activity as the app's entry point in the drawer.",'**Security implication**: declaring an intent filter makes the component *exported* (other apps can invoke it) unless you explicitly set `android:exported="false"`.']}]},{level:"junior",q:"What is a task and the back stack in Android?",a:[{t:"p",text:"**The concept**: a *task* is a collection of Activities the user works through to accomplish something, and the *back stack* is how those Activities are arranged — a stack (last-in, first-out). When you launch an Activity, it's pushed onto the current task's back stack; pressing the Back button pops the top one, returning to the previous. A task is what appears as a card in the Recents/Overview screen."},{t:"p",text:"By default it's simple LIFO: navigate A → B → C and the stack is [A, B, C]; Back takes you C → B → A. One thing to know is that, by default, launching an Activity *always creates a new instance* — so A → B → A leaves two separate A instances on the stack. Sometimes that's not what you want (you'd rather return to the existing A), which is exactly what launch modes and Intent flags let you control. Also, when a task is in the background, the system may destroy its Activities to reclaim memory and recreate them from saved instance state when the user returns — which is why saving state matters."}]},{level:"junior",q:"What is a PendingIntent?",a:[{t:"p",text:"**The concept**: a `PendingIntent` is a wrapper around an Intent that you hand to *another* app or the system so *they* can execute it *later, on your behalf* — with your app's identity and permissions. A normal Intent you fire yourself, immediately; a PendingIntent is a 'token' that lets someone else fire your intent at a future time."},{t:"p",text:"**Where it's used**: notifications (when the user taps the notification, the system fires your PendingIntent to open your app), alarms (`AlarmManager` fires it at the scheduled time), app widgets, and any callback where an external component needs to trigger your Activity/Service later. You create it with `PendingIntent.getActivity(...)` / `getBroadcast(...)` / `getService(...)`. Two things to know: on Android 12+ you *must* specify mutability (`FLAG_IMMUTABLE` or `FLAG_MUTABLE`) — usually `FLAG_IMMUTABLE` for security unless something needs to fill in the intent; and flags like `FLAG_UPDATE_CURRENT` control what happens if a matching PendingIntent already exists. The key idea is that it delegates the *permission to execute* your intent to another process safely."}]},{level:"senior",q:"Explain the four launch modes and when you'd use each.",a:[{t:"p",text:"**Launch modes control whether launching an Activity creates a new instance or reuses an existing one, and in which task.** They're set via `android:launchMode` in the manifest."},{t:"list",items:["**`standard`** (default) — every launch creates a *new* instance pushed onto the current task, duplicates allowed. Correct for most screens (a detail screen you can stack multiple of). Use unless you have a reason not to.","**`singleTop`** — if an instance is *already at the top* of the stack, reuse it (its `onNewIntent()` is called) instead of creating a duplicate. Use to avoid consecutive duplicates — e.g. a search results screen or a notification target that shouldn't stack a second copy when it's already showing. Note: it only reuses if the instance is *on top*; if it's lower in the stack, a new one is still created.","**`singleTask`** — only *one* instance can exist system-wide. Launching it brings its task to the front and *clears any Activities above it* in that task (delivering the intent via `onNewIntent()`). Use for a unique entry point — a home/main screen or a document root that should have exactly one instance you return to, not multiple.","**`singleInstance`** — like `singleTask`, but the Activity is the *only* Activity allowed in its task; any Activity it launches goes into a *different* task. The strictest and rarest — for special standalone screens (a launcher, an incoming-call screen) that must be isolated."]},{t:"p",text:"**How to reason about it**: the question is always 'should launching this create a new instance or reuse an existing one, and should it be unique?' Most screens → `standard`. Don't-duplicate-when-on-top → `singleTop`. Unique-entry-point-that-clears-above-it → `singleTask`. Fully-isolated-single-instance → `singleInstance`. A subtle senior point: launch modes interact with Intent *flags* (which apply per-launch at runtime and can override the manifest mode), and `singleTask`/`singleInstance` affect *task affinity* and can produce surprising back-stack behavior, so they should be used deliberately, not casually."}]},{level:"senior",q:"How do you clear the back stack when a user logs out, returning to the login screen with no history?",a:[{t:"p",text:"**You launch the login (or home) Activity with Intent flags that clear the existing task, so the user can't press Back into the now-invalid authenticated screens.** The standard combination is `FLAG_ACTIVITY_NEW_TASK` + `FLAG_ACTIVITY_CLEAR_TASK`, which finishes all Activities in the current task and starts the target as the new root — an empty back stack behind it."},{t:"code",title:"Clearing the stack on logout",code:`fun logout() {
    authRepository.clearSession()
    val intent = Intent(this, LoginActivity::class.java).apply {
        flags = Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TASK
    }
    startActivity(intent)
    finish()   // finish the current Activity too
}`},{t:"list",items:["**`FLAG_ACTIVITY_CLEAR_TASK`** (requires `NEW_TASK`) — clears the *entire* existing task before starting the target, so nothing remains behind it. Pressing Back from login exits the app, not into the authenticated screens. This is the cleanest logout approach.","**Alternative — `FLAG_ACTIVITY_CLEAR_TOP` + `SINGLE_TOP`**: if login is already lower in the stack, this clears everything above it and reuses it. But `CLEAR_TASK` is more robust for logout because it doesn't depend on login being in the stack.","**In single-Activity / Navigation-component apps**: you'd instead use the Navigation API — `navController.navigate(loginRoute) { popUpTo(0) { inclusive = true } }` (or `popUpTo(nav_graph) { inclusive = true }`) to pop the entire back stack. Same concept (clear history), expressed through Navigation rather than task flags. In Navigation-Compose it's `popUpTo` with `inclusive`.","**Why it matters beyond UX**: leaving authenticated Activities on the back stack after logout is also a *security/privacy* issue — the user (or someone else) could Back-navigate into screens showing personal data from the previous session. Clearing the stack prevents that."]},{t:"p",text:"**The senior framing**: logout isn't just navigation — it's invalidating a security boundary, so you must ensure no path (Back button, Recents) leads back into authenticated state. `CLEAR_TASK` + `NEW_TASK` (or `popUpTo(inclusive)` in Navigation) enforces that by wiping the history, and you pair it with clearing the session/tokens so even a restored Activity can't show stale authenticated data."}]},{level:"junior",q:"How do you pass data with an Intent, and what are the size limits?",a:[{t:"p",text:"Attach data as *extras* — `intent.putExtra(key, value)` for primitives/String, and `Parcelable`/`Serializable` for objects. Read them with `getStringExtra`/`getParcelableExtra`. But Intent extras travel through Binder (the ~1MB transaction buffer shared per process), so keep them *small* — pass ids, not large objects/bitmaps, or risk `TransactionTooLargeException`."},{t:"code",title:"Intent extras",code:`val intent = Intent(this, DetailActivity::class.java).apply {
    putExtra("id", itemId)                       // small primitive
    putExtra("user", user)                       // Parcelable (keep small)
}
startActivity(intent)
// In DetailActivity:
val id = intent.getStringExtra("id")`},{t:"list",items:["**`putExtra`** — primitives, String, `Parcelable`, `Serializable`.","**Prefer `Parcelable`** — faster than `Serializable` on Android (`@Parcelize`).","**Keep small** — Binder ~1MB limit; oversized extras throw `TransactionTooLargeException`.","**Pass ids** — re-fetch large data from cache/DB in the target."]},{t:"note",text:"Attach data as extras (putExtra: primitives/String/Parcelable/Serializable; prefer @Parcelize over Serializable). Extras cross Binder's ~1MB buffer, so keep them small — pass ids, not large objects/bitmaps (else TransactionTooLargeException), and re-fetch big data in the target."}]},{level:"senior",q:"What is the difference between Parcelable and Serializable for Intent data?",a:[{t:"p",text:"Both serialize objects to pass through Intents/Bundles, but `Parcelable` is Android's optimized mechanism (no reflection, much faster, less garbage) while `Serializable` is Java's reflection-based approach (slower, more allocations). Use `Parcelable` — the `@Parcelize` annotation (kotlin-parcelize) generates the boilerplate for you."},{t:"code",title:"@Parcelize",code:`@Parcelize
data class User(val id: String, val name: String) : Parcelable   // auto-generated`},{t:"list",items:["**`Parcelable`** — Android-optimized, fast, no reflection; preferred.","**`Serializable`** — Java reflection-based; slower, more GC.","**`@Parcelize`** — generates the `Parcelable` implementation automatically.","**Still keep small** — Parcelable is faster but Binder size limits still apply."]},{t:"note",text:"Parcelable is Android's optimized serialization (no reflection, fast, low GC) — preferred; @Parcelize (kotlin-parcelize) generates it. Serializable is Java's reflection-based approach (slower, more allocations). Use Parcelable, but still keep payloads small (Binder size limits apply either way)."}]},{level:"junior",q:"What is the difference between startActivity and startActivityForResult (now the Result API)?",a:[{t:"p",text:"`startActivity` launches another Activity *without* expecting a result back. To *get a result* (a picked photo, a confirmation), you use the modern *Activity Result API* (`registerForActivityResult`) — the old `startActivityForResult`/`onActivityResult` with request codes is deprecated. The Result API gives typed contracts and a callback, and survives recreation."},{t:"list",items:["**`startActivity`** — fire-and-forget launch; no result.","**Activity Result API** — `registerForActivityResult(contract) { result -> }` for results.","**Deprecated** — `startActivityForResult`/`onActivityResult` (request codes).","**Contracts** — `StartActivityForResult`, `GetContent`, `RequestPermission`, custom."]},{t:"note",text:"startActivity launches without expecting a result; for a result use the Activity Result API (registerForActivityResult(contract){result}) — typed contracts + callback, survives recreation. The old startActivityForResult/onActivityResult (request codes) is deprecated."}]},{level:"senior",q:"What are the common Intent flags, and what do they do?",a:[{t:"p",text:"Intent flags modify launch behavior and task/back-stack handling. Key ones: `FLAG_ACTIVITY_NEW_TASK` (launch into a new/existing task — required from non-Activity contexts), `FLAG_ACTIVITY_CLEAR_TOP` (clear activities above the target), `FLAG_ACTIVITY_SINGLE_TOP` (don't recreate if already on top), and `FLAG_ACTIVITY_CLEAR_TASK` (clear the whole task — with NEW_TASK, for logout)."},{t:"code",title:"Clearing the stack on logout",code:`val intent = Intent(this, LoginActivity::class.java).apply {
    addFlags(Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TASK)
}
startActivity(intent)   // new task, old stack cleared`},{t:"list",items:["**`NEW_TASK`** — launch into a new/existing task (required from app/service context).","**`CLEAR_TOP`** — pop activities above an existing target instance.","**`SINGLE_TOP`** — reuse the top instance (delivers via `onNewIntent`).","**`CLEAR_TASK`** (+ NEW_TASK) — wipe the task (logout → login)."]},{t:"note",text:"Intent flags modify launch/task behavior: NEW_TASK (into a new/existing task — required from non-Activity contexts), CLEAR_TOP (pop above the target), SINGLE_TOP (reuse top, onNewIntent), CLEAR_TASK+NEW_TASK (wipe the task — logout→login). They mirror launch modes but per-launch."}]},{level:"junior",q:"How do you use an implicit Intent to open a URL, dial, or share?",a:[{t:"p",text:"An implicit Intent declares an *action* and *data*, letting the system pick an app that can handle it. Use `ACTION_VIEW` with a URL/geo/tel URI, `ACTION_DIAL` for the dialer, or `ACTION_SEND` (often wrapped in a chooser) to share text/files. Always check there's a handler or wrap in `try/catch`/`resolveActivity` to avoid `ActivityNotFoundException`."},{t:"code",title:"Implicit intents",code:`startActivity(Intent(Intent.ACTION_VIEW, Uri.parse("https://example.com")))
startActivity(Intent(Intent.ACTION_DIAL, Uri.parse("tel:+123456789")))
val share = Intent(Intent.ACTION_SEND).apply {
    type = "text/plain"; putExtra(Intent.EXTRA_TEXT, "Check this out")
}
startActivity(Intent.createChooser(share, "Share via"))`},{t:"list",items:["**`ACTION_VIEW`** — open URLs, maps (`geo:`), etc.","**`ACTION_DIAL`/`ACTION_SEND`** — dialer / share sheet.","**Chooser** — `Intent.createChooser` to always show the picker.","**Handle no app** — check `resolveActivity` or catch `ActivityNotFoundException`."]},{t:"note",text:"Implicit intents declare an action + data so the system picks a handler: ACTION_VIEW (URLs/geo), ACTION_DIAL (dialer), ACTION_SEND (+ createChooser for the share sheet). Guard against no handler (resolveActivity / catch ActivityNotFoundException). On Android 11+, package visibility may require <queries> in the manifest."}]},{level:"senior",q:"What is package visibility, and why do implicit intents sometimes fail on Android 11+?",a:[{t:"p",text:"Android 11 (API 30) introduced *package visibility*: your app can no longer see all installed apps by default. So `resolveActivity`/`queryIntentActivities` may return null even when a handler exists, and some implicit intents fail. To query or target specific apps, declare them in a `<queries>` element in the manifest (common actions like `ACTION_VIEW` for https are auto-visible)."},{t:"code",title:"<queries> declaration",code:`<queries>
    <intent>
        <action android:name="android.intent.action.SEND" />
        <data android:mimeType="image/*" />
    </intent>
    <package android:name="com.some.specific.app" />
</queries>`},{t:"list",items:["**Package visibility (API 30+)** — apps can't enumerate all others by default.","**Symptom** — `resolveActivity` returns null; implicit intent seems to have no handler.","**Fix** — declare `<queries>` (by intent or package) in the manifest.","**Auto-visible** — common web/dial/etc. handlers don't need declaring."]},{t:"note",text:"Android 11+ package visibility hides other installed apps by default, so resolveActivity/queryIntentActivities can return null and implicit intents may fail. Declare the intents/packages you query in a <queries> manifest element (common web/dial handlers are auto-visible)."}]},{level:"senior",q:"What is the difference between singleTop and singleTask launch modes?",a:[{t:"p",text:"`singleTop`: if an instance is already at the *top* of the stack, reuse it (deliver via `onNewIntent`) instead of creating a new one; otherwise a new instance is created (can have multiple instances lower in the stack). `singleTask`: only *one* instance exists in the system, at the *root* of its task; launching it brings its task forward and clears activities above it (`onNewIntent`)."},{t:"list",items:["**`singleTop`** — reuse only if already on top; multiple instances allowed elsewhere.","**`singleTask`** — a single system-wide instance at its task root; clears above on relaunch.","**`onNewIntent`** — both deliver new intents to the reused instance.","**Uses** — `singleTop` for a screen re-launched from notifications; `singleTask` for a main/entry screen."]},{t:"note",text:"singleTop: reuse the instance only if it's already on TOP (else create new; multiple instances allowed) — onNewIntent. singleTask: exactly one instance system-wide at its task ROOT; relaunch brings the task forward and clears activities above it — onNewIntent. singleTop for notification re-entry; singleTask for a main/entry screen."}]},{level:"senior",q:"What are the different types of PendingIntent and the mutability flags?",a:[{t:"p",text:"A `PendingIntent` wraps an Intent so *another* app/system component can execute it *as your app* later — used for notifications, alarms, widgets. Create one with `getActivity`/`getService`/`getBroadcast`. Since API 31 you *must* specify mutability: `FLAG_IMMUTABLE` (the safe default — the receiver can't modify it) or `FLAG_MUTABLE` (only when the system needs to fill in fields, e.g. inline reply)."},{t:"code",title:"PendingIntent with flags",code:`val pi = PendingIntent.getActivity(
    context, 0, intent,
    PendingIntent.FLAG_IMMUTABLE or PendingIntent.FLAG_UPDATE_CURRENT,
)`},{t:"list",items:["**Types** — `getActivity`, `getService`/`getForegroundService`, `getBroadcast`.","**`FLAG_IMMUTABLE`** — required-ish default; receiver can't alter the intent (security).","**`FLAG_MUTABLE`** — only when the system must fill fields (inline reply, bubbles).","**`FLAG_UPDATE_CURRENT`/`FLAG_CANCEL_CURRENT`** — reuse vs replace an existing PendingIntent."]},{t:"note",text:"A PendingIntent lets another component run an Intent as your app (notifications/alarms/widgets) via getActivity/getService/getBroadcast. Since API 31 you must set mutability: FLAG_IMMUTABLE (safe default) or FLAG_MUTABLE (only when the system fills fields, e.g. inline reply). Use FLAG_UPDATE_CURRENT to reuse/update."}]},{level:"senior",q:"How do you handle a deep link that opens a specific screen?",a:[{t:"p",text:"Declare an `<intent-filter>` with `ACTION_VIEW`, the `BROWSABLE`/`DEFAULT` categories, and a `<data>` scheme/host (or use Navigation's `<deepLink>`). When a matching URI opens the app, the target Activity receives it via `getIntent()`/`onNewIntent`; you parse the URI to navigate to the right screen and build a sensible back stack (e.g. with `TaskStackBuilder` or Navigation)."},{t:"code",title:"Deep link intent filter",code:`<intent-filter android:autoVerify="true">
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="https" android:host="shop.com" android:pathPrefix="/product" />
</intent-filter>`},{t:"list",items:["**`<intent-filter>` + `<data>`** — declare the URI scheme/host/path.",'**App Links** — `autoVerify="true"` + a `.well-known/assetlinks.json` for verified https links (no chooser).',"**Parse in the target** — `getIntent().data` / `onNewIntent`.","**Back stack** — build a proper up-stack (Navigation deep links, `TaskStackBuilder`)."]},{t:"note",text:"Declare an <intent-filter> (ACTION_VIEW, BROWSABLE/DEFAULT, <data> scheme/host/path), or Navigation's <deepLink>. The target parses getIntent().data/onNewIntent to navigate. Use autoVerify + assetlinks.json for verified App Links (no chooser), and build a proper back stack (Navigation deep links / TaskStackBuilder)."}]},{level:"junior",q:"What is a task, and how does affinity affect it?",a:[{t:"p",text:"A *task* is a stack of Activities the user works through for a job (e.g. composing an email), presented as one entry in Recents. *Task affinity* (`android:taskAffinity`) determines which task an Activity prefers to belong to — by default all your Activities share the app's affinity (one task). Changing affinity (with `NEW_TASK`) can place an Activity in a separate task."},{t:"list",items:["**Task** — a back-stack of Activities for one user job; a Recents entry.","**Default affinity** — all app Activities share one task.","**`taskAffinity`** — override which task an Activity joins.","**With `NEW_TASK`** — affinity decides the target task (or a new one)."]},{t:"note",text:"A task is a back-stack of Activities for one user job (one Recents entry). Task affinity (android:taskAffinity, default = app package) decides which task an Activity prefers; combined with FLAG_ACTIVITY_NEW_TASK it determines the target/new task. Most apps use one task (default affinity)."}]},{level:"senior",q:"How do you build a proper back stack for a notification or deep link?",a:[{t:"p",text:"When a notification/deep link opens a *deep* screen, the user should still be able to press back to a logical parent — so you build a *synthetic back stack*. Use `TaskStackBuilder` (adding the parent chain) or Navigation Component's deep-link support (which constructs the back stack from the graph), rather than dropping the user on an isolated screen with nowhere to go back to."},{t:"code",title:"TaskStackBuilder",code:`val pending = TaskStackBuilder.create(context)
    .addNextIntentWithParentStack(Intent(context, DetailActivity::class.java).putExtra("id", id))
    .getPendingIntent(0, PendingIntent.FLAG_IMMUTABLE)`},{t:"list",items:["**Synthetic back stack** — so back goes to a logical parent, not out of the app.","**`TaskStackBuilder`** — `addNextIntentWithParentStack` uses manifest `parentActivityName`.","**Navigation deep links** — build the stack from the nav graph automatically.","**Avoid** — dropping the user on a dead-end screen."]},{t:"note",text:"For a notification/deep link into a deep screen, build a synthetic back stack so back reaches a logical parent — TaskStackBuilder.addNextIntentWithParentStack (uses manifest parentActivityName) or Navigation Component deep links (builds the stack from the graph). Don't strand the user on a dead-end screen."}]},{level:"junior",q:"What is the difference between sending a broadcast and starting an Activity/Service via Intent?",a:[{t:"p",text:"The Intent *action verb* differs: `startActivity` launches a UI screen, `startService`/`startForegroundService` starts a background component, and `sendBroadcast` delivers an event to *any* registered receivers (one-to-many). Broadcasts are for *announcements* (something happened); activities/services are for *doing* something specific."},{t:"list",items:["**`startActivity`** — launch a screen (one target).","**`startService`/`startForegroundService`** — start a background component.","**`sendBroadcast`** — announce an event to many receivers (one-to-many).","**Modern** — prefer in-app events (Flow/LiveData) over broadcasts within your app."]},{t:"note",text:"startActivity launches a screen; startService/startForegroundService starts a background component; sendBroadcast announces an event to any registered receivers (one-to-many). Broadcasts = announcements ('X happened'); activities/services = do a specific thing. Within your app, prefer Flow/LiveData over broadcasts."}]},{level:"senior",q:"What is standard versus singleInstance launch mode?",a:[{t:"p",text:"`standard` (default) creates a *new instance* every time the Activity is launched, allowing multiple instances across tasks. `singleInstance` is the most restrictive: the Activity is the *sole* member of its *own task* — no other Activities can be placed in that task. Use `singleInstance` rarely, for a self-contained screen that must never share a task (e.g. a launcher, a call screen)."},{t:"list",items:["**`standard`** — new instance per launch; multiple instances allowed.","**`singleInstance`** — alone in its own task; nothing else joins that task.","**Rare** — for isolated, self-contained Activities.","**Others** — `singleTop`, `singleTask` sit between these."]},{t:"note",text:"standard (default): new instance every launch, multiple allowed. singleInstance: the Activity is alone in its own task — nothing else joins it. singleInstance is rare (isolated self-contained screens like a launcher/call UI). singleTop/singleTask are the in-between modes."}]},{level:"junior",q:"What is a sticky intent, and why is it deprecated?",a:[{t:"p",text:"A *sticky broadcast* (`sendStickyBroadcast`) stayed around after being sent, so newly-registered receivers could get the last value (e.g. battery status). It's *deprecated* because it has no security (any app can read/modify it) and no delivery guarantees. Instead, query the current state directly (e.g. `registerReceiver(null, filter)` for battery) or use proper state holders."},{t:"list",items:["**Sticky broadcast** — persisted last value for late receivers.","**Deprecated** — no security, no delivery guarantees.","**Battery example** — query via `registerReceiver(null, IntentFilter(ACTION_BATTERY_CHANGED))`.","**Modern** — hold state in a ViewModel/repository, not sticky broadcasts."]},{t:"note",text:"Sticky broadcasts (sendStickyBroadcast) persisted the last value for late receivers (e.g. battery) — deprecated for lacking security and delivery guarantees. Query current state directly (registerReceiver(null, filter)) or hold it in a ViewModel/repository instead."}]},{level:"senior",q:"How do you launch another app's Activity safely?",a:[{t:"p",text:"For a *known* app, use an explicit Intent with its package/component (if you have permission and it's exported). For a *capability* (open a map, share), use an implicit Intent by action — but first check a handler exists (`resolveActivity`, plus `<queries>` on API 30+) and wrap in `try/catch` for `ActivityNotFoundException`. Never assume the target app is installed."},{t:"list",items:["**Explicit** — set package/component for a specific known app (must be exported).","**Implicit** — by action for a capability; check `resolveActivity`.","**`<queries>`** — declare packages/intents to see them on API 30+.","**Guard** — `try/catch` `ActivityNotFoundException`; offer a fallback."]},{t:"note",text:"Known app: explicit Intent with its package/component (must be exported). Capability: implicit Intent by action, but check a handler exists (resolveActivity + <queries> on API 30+) and try/catch ActivityNotFoundException with a fallback. Never assume the target is installed."}]},{level:"senior",q:"What is the difference between an ordered and a regular broadcast?",a:[{t:"p",text:"A *regular* broadcast (`sendBroadcast`) is delivered to all receivers roughly in parallel, with no defined order and no ability to pass results between them. An *ordered* broadcast (`sendOrderedBroadcast`) delivers to receivers *one at a time* by priority, letting each receiver abort the broadcast or pass a result to the next. Ordered broadcasts are heavier and rarely needed in app code."},{t:"list",items:["**Regular** — parallel-ish delivery, no order, no result passing.","**Ordered** — sequential by priority; can abort or pass results.","**Cost** — ordered is slower (serial).","**Modern** — most in-app eventing uses Flow/LiveData, not broadcasts."]},{t:"note",text:"Regular broadcast (sendBroadcast): delivered to all receivers in no defined order, no result passing. Ordered broadcast (sendOrderedBroadcast): sequential by priority, each receiver can abort or pass a result onward (slower). Rarely needed — prefer Flow/LiveData for in-app events."}]},{level:"junior",q:"What is the difference between getIntent and onNewIntent?",a:[{t:"p",text:"`getIntent()` returns the Intent that *originally started* the Activity (available from `onCreate` onward). `onNewIntent(intent)` is called when a *reused* Activity instance (singleTop/singleTask) receives a *new* Intent without being recreated — you must handle it there and call `setIntent(intent)` so subsequent `getIntent()` returns the new one."},{t:"list",items:["**`getIntent()`** — the launching Intent; static for a created instance until updated.","**`onNewIntent`** — new Intent to a reused instance (no recreation).","**`setIntent()`** — update the stored Intent in `onNewIntent`.","**Deep links/notifications** — handle both paths (fresh create + reuse)."]},{t:"note",text:"getIntent() returns the Intent that started the Activity (from onCreate on). onNewIntent(intent) fires when a reused instance (singleTop/singleTask) gets a new Intent without recreation — handle it and call setIntent(intent) so getIntent() reflects it. Handle both paths for deep links/notifications."}]},{level:"junior",q:"What is the difference between an Intent action, data, category, and extras?",a:[{t:"p",text:"An Intent is described by several fields: the *action* (what to do, e.g. `ACTION_VIEW`), the *data* (a URI to act on), *categories* (additional hints like `CATEGORY_BROWSABLE`), the *type* (MIME type), the *component* (explicit target), and *extras* (a Bundle of parameters). Implicit-intent resolution matches action + data + category against intent filters."},{t:"list",items:["**Action** — the operation (`ACTION_VIEW`, `ACTION_SEND`).","**Data (URI)** — what to operate on (`https://…`, `tel:…`).","**Category** — extra matching hints (`DEFAULT`, `BROWSABLE`).","**Extras** — parameters (`putExtra`); component — explicit target class."]},{t:"note",text:"Intent fields: action (what to do), data (URI to act on), category (matching hints), type (MIME), component (explicit target), extras (Bundle of params). Implicit resolution matches action + data + category against intent filters; extras carry the payload."}]},{level:"senior",q:"How do you securely receive an Intent (validate extras, exported components)?",a:[{t:"p",text:"Any exported component (Activity/Service/Receiver with an intent filter or `exported=true`) can be launched by *other apps*, so treat incoming Intents as *untrusted input*: validate/sanitize extras, don't blindly trust URIs (guard against path traversal / SSRF), check permissions, and set `android:exported` explicitly (required on API 31+). Keep internal components `exported=false`."},{t:"list",items:["**Exported = attack surface** — other apps can send Intents to it.","**Validate extras/URIs** — never trust incoming data; sanitize.","**`android:exported`** — set explicitly (mandatory API 31+); false for internal-only.","**Permissions** — protect sensitive components with a permission."]},{t:"note",text:"Exported components can be launched by other apps, so treat incoming Intents as untrusted: validate/sanitize extras and URIs (guard path traversal/SSRF), check permissions, and set android:exported explicitly (mandatory on API 31+) — false for internal-only components. Only export what genuinely needs external access."}]},{level:"junior",q:"What is the difference between finish and finishAffinity?",a:[{t:"p",text:"`finish()` closes the *current* Activity, returning to the previous one in the task. `finishAffinity()` closes the current Activity *and all Activities below it with the same task affinity* — effectively exiting the whole task/app flow. Use `finishAffinity` (or `finishAndRemoveTask`) for things like logout or a full exit."},{t:"list",items:["**`finish()`** — close this Activity; go back to the previous.","**`finishAffinity()`** — close this and all same-affinity Activities below.","**`finishAndRemoveTask()`** — finish and remove the task from Recents.","**Uses** — `finishAffinity` for logout/exit flows."]},{t:"note",text:"finish() closes the current Activity (back to the previous). finishAffinity() closes it AND all same-affinity Activities below (exits the flow). finishAndRemoveTask() also removes the task from Recents. Use finishAffinity/CLEAR_TASK for logout/full-exit."}]},{level:"senior",q:"How do you grant another app temporary access to a file via an Intent?",a:[{t:"p",text:"Don't pass a raw `file://` URI (it throws `FileUriExposedException` and isn't accessible to other apps). Instead expose the file through a `FileProvider` to get a `content://` URI, and add `FLAG_GRANT_READ_URI_PERMISSION` to the Intent so the receiving app has temporary, scoped access — used for sharing images, opening PDFs, camera capture."},{t:"code",title:"FileProvider + grant flag",code:`val uri = FileProvider.getUriForFile(context, "\${packageName}.fileprovider", file)
val intent = Intent(Intent.ACTION_VIEW).apply {
    setDataAndType(uri, "application/pdf")
    addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)
}
startActivity(intent)`},{t:"list",items:["**`FileProvider`** — turns a file into a shareable `content://` URI.","**`FLAG_GRANT_READ_URI_PERMISSION`** — temporary scoped access for the receiver.","**No `file://`** — raw file URIs throw `FileUriExposedException`.","**Uses** — sharing images/files, camera capture output, opening documents."]},{t:"note",text:"Expose files via a FileProvider (content:// URI) — never a raw file:// (throws FileUriExposedException) — and add FLAG_GRANT_READ_URI_PERMISSION so the receiving app gets temporary scoped access. Standard for sharing images/files, camera capture, opening documents."}]},{level:"junior",q:"What is an intent chooser, and when should you use one?",a:[{t:"p",text:"`Intent.createChooser(intent, title)` wraps an implicit Intent so the system *always* shows the app picker (share sheet), even if the user previously set a default. Use it for *sharing* (so the user picks the target each time) and when you want to force the disambiguation dialog rather than silently launching a default handler."},{t:"code",title:"Chooser",code:`val send = Intent(Intent.ACTION_SEND).apply { type = "text/plain"; putExtra(Intent.EXTRA_TEXT, text) }
startActivity(Intent.createChooser(send, "Share via"))`},{t:"list",items:["**`createChooser`** — always shows the picker (ignores defaults).","**Sharing** — let the user choose the target each time.","**Force disambiguation** — when multiple handlers should be offered.","**Android Sharesheet** — the modern share UI with direct-share targets."]},{t:"note",text:"Intent.createChooser(intent, title) forces the app picker (share sheet) even if a default is set — use it for sharing (user picks each time) or to force disambiguation. It surfaces the Android Sharesheet with direct-share targets."}]},{level:"senior",q:"How do launch modes compare to Intent flags?",a:[{t:"p",text:"*Launch modes* (`android:launchMode` in the manifest) set an Activity's default instancing behavior for *all* launches. *Intent flags* (`FLAG_ACTIVITY_*`) set behavior for a *specific* launch at runtime. They overlap (e.g. `singleTop` mode vs `FLAG_ACTIVITY_SINGLE_TOP`); flags give per-launch control, launch modes give a fixed policy. Prefer flags when the behavior is situational."},{t:"table",headers:["Launch mode","Similar flag"],rows:[["singleTop","FLAG_ACTIVITY_SINGLE_TOP"],["singleTask","FLAG_ACTIVITY_NEW_TASK + CLEAR_TOP"],["(clear stack)","FLAG_ACTIVITY_CLEAR_TASK"]]},{t:"list",items:["**Launch mode** — manifest; fixed policy for every launch of that Activity.","**Intent flag** — runtime; per-launch behavior.","**Overlap** — many modes have a flag equivalent.","**Prefer flags** — for situational behavior; modes for an inherent property."]},{t:"note",text:"Launch modes (android:launchMode) set an Activity's default instancing for ALL launches; Intent flags (FLAG_ACTIVITY_*) set behavior per launch at runtime. They overlap (singleTop ↔ FLAG_ACTIVITY_SINGLE_TOP). Use flags for situational behavior, launch modes for an inherent property of the Activity."}]},{level:"senior",q:"What does FLAG_ACTIVITY_REORDER_TO_FRONT do?",a:[{t:"p",text:"`FLAG_ACTIVITY_REORDER_TO_FRONT` brings an *existing* instance of the target Activity to the *front* of its task (if it's already in the stack) instead of creating a new one — preserving the rest of the stack order below it. It's useful for tab-like or hub navigation where re-selecting a screen should surface the existing instance rather than stacking duplicates."},{t:"list",items:["**Reorders, not recreates** — moves an existing instance to the top.","**Preserves the stack** — other activities keep their relative order.","**vs `CLEAR_TOP`** — that pops (destroys) activities above; REORDER just moves the target up.","**Uses** — hub/tab navigation avoiding duplicate instances."]},{t:"note",text:"FLAG_ACTIVITY_REORDER_TO_FRONT moves an existing instance of the target Activity to the front of its task (no new instance), preserving the rest of the stack — unlike CLEAR_TOP which destroys activities above the target. Good for hub/tab navigation avoiding duplicates."}]},{level:"senior",q:"How do you handle receiving shared content from other apps (ACTION_SEND)?",a:[{t:"p",text:"Declare an `<intent-filter>` for `ACTION_SEND` (and `ACTION_SEND_MULTIPLE`) with the MIME types you accept, so your app appears in others' share sheets. In the receiving Activity, read the shared content from the Intent — `EXTRA_TEXT` for text, `EXTRA_STREAM` (a content URI) for files/images — and take a persistable URI permission if you need longer access."},{t:"code",title:"Receiving a share",code:`<intent-filter>
    <action android:name="android.intent.action.SEND" />
    <category android:name="android.intent.category.DEFAULT" />
    <data android:mimeType="image/*" />
</intent-filter>
// In onCreate:
val uri = intent.getParcelableExtra<Uri>(Intent.EXTRA_STREAM)
val text = intent.getStringExtra(Intent.EXTRA_TEXT)`},{t:"list",items:["**`<intent-filter>` for `ACTION_SEND`** — with accepted MIME types; you appear in share sheets.","**`EXTRA_TEXT`/`EXTRA_STREAM`** — read the shared text / content URI.","**`ACTION_SEND_MULTIPLE`** — multiple items (`EXTRA_STREAM` as a list).","**URI permission** — you already have temporary read; persist if needed."]},{t:"note",text:"Declare an ACTION_SEND (+ ACTION_SEND_MULTIPLE) intent filter with accepted MIME types to appear in others' share sheets; in the receiver read EXTRA_TEXT (text) or EXTRA_STREAM (content URI for files/images). You get temporary read access — persist the URI permission if you need it longer."}]}],dA={id:"intents-tasks",title:"Intents, Tasks & Launch Modes",tagline:"Explicit vs implicit intents, intent filters & deep links, PendingIntent, tasks & the back stack, the four launch modes, and intent flags.",tags:["Android","Intents","Launch Modes","Back Stack","Deep Links"],content:lA,qa:cA},hA=[{heading:"The four app components — a quick map",blocks:[{t:"p",text:"Android apps are built from four types of **components**, each an entry point the system can start: **Activity** (a screen — covered separately), **Service** (long-running background work with no UI), **BroadcastReceiver** (responds to system/app-wide events), and **ContentProvider** (shares data between apps). This topic covers the latter three. All are declared in the manifest (except some runtime-registered receivers), and the system can instantiate any of them."}]},{heading:"Services — background work without UI",blocks:[{t:"p",text:"A **Service** is a component for work that should continue *without a UI*, potentially when the user isn't in the app. Important modern caveat: a Service does **not** automatically run on a background thread — it runs on the main thread by default, so you still move work to a background thread/coroutine yourself. There are three flavors:"},{t:"table",headers:["Type","What it is","Use for"],rows:[["Foreground service","runs with a persistent notification; user-visible ongoing work","music playback, navigation, active file upload, fitness tracking"],["Background service","runs without a notification (heavily restricted since Android 8)","mostly deprecated — use WorkManager instead"],["Bound service","other components bind to it and call its methods (client-server)","an in-app API surface, IPC via AIDL"]]},{t:"list",items:["**Foreground service** — for ongoing, *user-aware* work. It must show a persistent notification (so the user knows it's running) and, since Android 10+, declare a `foregroundServiceType`. This is the correct tool for music, navigation, an in-progress upload the user is watching.","**Background services are heavily restricted**: since Android 8 (Oreo), the system kills background services shortly after the app leaves the foreground. So 'just start a Service to do background work' no longer works — for deferrable/guaranteed background work you use **WorkManager**, and for immediate user-visible work you use a foreground service.","**Bound service** — components `bindService()` to it and get an interface to call; the service lives while bound. Used for a client-server relationship within an app, or cross-process IPC (via AIDL). `startService`/`stopService` (started services) vs `bindService`/`unbindService` (bound) are the two interaction models.","**Lifecycle**: started services have `onStartCommand`; bound services have `onBind`. The return value of `onStartCommand` (`START_STICKY` etc.) tells the system whether to recreate the service if killed."]}]},{heading:"Choosing the right background tool",blocks:[{t:"table",headers:["Need","Tool"],rows:[["Ongoing user-visible work (music, navigation)","Foreground Service"],["Deferrable, guaranteed work (sync, upload) surviving process death/reboot","WorkManager"],["Short async tied to a screen","coroutine in viewModelScope"],["Exact-time work (alarm, reminder)","AlarmManager"],["In-app client-server / IPC","Bound Service (AIDL for cross-process)"]]},{t:"note",text:"The single most common Services interview question is 'when do you use a Service vs WorkManager?' Answer: a foreground service for *ongoing, immediate, user-visible* work (must show a notification); WorkManager for *deferrable, guaranteed* work that must survive process death and reboots (it internally uses a foreground service when needed). Plain background services are effectively deprecated by the Android 8+ restrictions."}]},{heading:"BroadcastReceivers — responding to events",blocks:[{t:"p",text:"A **BroadcastReceiver** listens for **broadcast** messages — system-wide or app-wide events delivered as Intents. The system broadcasts events like connectivity changes, battery low, boot completed, airplane mode toggled; apps can send custom broadcasts too. A receiver's `onReceive` runs briefly to react."},{t:"code",title:"Two ways to register a receiver",code:`// 1) Runtime (context-registered) — tied to a component's lifecycle
val receiver = object : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        // react — keep it FAST (onReceive runs on main thread, ~10s limit)
    }
}
registerReceiver(receiver, IntentFilter(Intent.ACTION_BATTERY_LOW))
// must unregisterReceiver(receiver) later to avoid leaks

// 2) Manifest-declared (static) — for a few allowed system broadcasts
// <receiver android:name=".BootReceiver" android:exported="true">
//   <intent-filter><action android:name="android.intent.action.BOOT_COMPLETED"/></intent-filter>
// </receiver>`},{t:"list",items:["**Runtime-registered** — you register in code (usually in an Activity/Service) and *must* unregister (in the mirror lifecycle callback) or leak. Only active while registered. Preferred for most cases; in Compose, register/unregister in a `DisposableEffect`.","**Manifest-declared (static)** — declared in the manifest so it can be triggered even when the app isn't running. **Heavily restricted since Android 8**: most implicit broadcasts can no longer be received via manifest declaration (to stop apps waking up constantly). A few exceptions remain (like `BOOT_COMPLETED`).","**`onReceive` must be fast**: it runs on the main thread with a short time limit (~10 seconds); do no heavy work there. For real work, hand off to WorkManager or a foreground service. It's a *trigger*, not a place to do the work.","**Security**: broadcasts can be a vector for attacks — use `exported=false` for internal receivers, permission-protect sensitive broadcasts, and prefer `LocalBroadcastManager` alternatives (now deprecated — use a shared Flow/StateFlow in a repository for in-app events instead)."]}]},{heading:"ContentProviders — sharing data across apps",blocks:[{t:"p",text:"A **ContentProvider** exposes an app's data to *other apps* through a standard interface (query/insert/update/delete) identified by a `content://` URI. It's Android's mechanism for **structured data sharing across app boundaries** with permission control. You interact with providers via a `ContentResolver`."},{t:"code",title:"Reading from a system ContentProvider (contacts)",code:`val cursor = contentResolver.query(
    ContactsContract.Contacts.CONTENT_URI,   // content:// URI
    projection, selection, selectionArgs, sortOrder
)
cursor?.use { while (it.moveToNext()) { /* read columns */ } }`},{t:"list",items:["**When you consume one**: to read *system* data — contacts, calendar, media store, call log — you query the relevant system ContentProvider via `contentResolver` (with the right runtime permission).","**When you build one**: to *share your app's data with other apps* (e.g. a dictionary app exposing definitions), or historically to feed certain framework features. If your data is *only* used within your own app, you generally *don't* need a ContentProvider — use a database/repository directly. Building one just for internal use is over-engineering.","**`FileProvider`** — a special ContentProvider for securely sharing *files* with other apps via `content://` URIs (instead of exposing raw `file://` paths, which is forbidden since Android 7). Used for sharing images, PDFs, camera output.","**A hidden role**: ContentProviders are initialized very early in app startup (before `Application.onCreate` in some cases), which libraries exploit to auto-initialize themselves (the AndroidX `App Startup` library and Firebase use a ContentProvider to bootstrap without you writing init code) — a piece of trivia that explains 'why is there a provider I didn't add?'."]}]},{heading:"Manifest, exported, and component security",blocks:[{t:"list",items:["**All components are declared in the manifest** (except runtime-registered receivers) — `<activity>`, `<service>`, `<receiver>`, `<provider>`. The manifest is how the OS knows what your app contains and can launch.","**`android:exported`** — whether *other apps* can start/access the component. Since Android 12, you *must* explicitly set it for any component with an intent filter. Default to `exported=false` unless the component genuinely needs to be reachable by other apps (a shared provider, a public deep-link Activity) — an exported component is an attack surface.","**Permissions** — components can be protected by permissions (`android:permission`) so only callers holding that permission can access them. Providers can enforce read/write permissions separately."]}]}],uA=[{level:"junior",q:"What are the four main Android app components?",a:[{t:"list",items:["**Activity** — a single screen with a UI; the entry point for user interaction.","**Service** — a component for long-running work without a UI (like music playback), which can continue when the user isn't looking at the app.","**BroadcastReceiver** — responds to system-wide or app-wide broadcast events (connectivity change, boot completed, battery low).","**ContentProvider** — exposes structured data to other apps through a standard query interface, identified by a `content://` URI."]},{t:"p",text:"All four are *entry points* the Android system can instantiate and start — the OS, not just your app, decides when to create them. Three of the four (Activity, Service, BroadcastReceiver, and Provider) are declared in the manifest so the system knows they exist (runtime-registered receivers are the exception). Understanding these components — especially their lifecycles and the modern restrictions on Services and background receivers — is core Android knowledge."}]},{level:"junior",q:"What is a Service and does it run on a background thread?",a:[{t:"p",text:"**A Service is a component for work that runs without a UI, potentially continuing when the user leaves the app.** The critical thing to know — and a common interview trap — is that **a Service does NOT run on a background thread by default. It runs on the main thread.** So if you do heavy or blocking work directly in a Service, you'll freeze the UI (or ANR) just like anywhere else. You must still move the actual work to a background thread or coroutine yourself."},{t:"list",items:["**Foreground service** — for ongoing, *user-visible* work; shows a persistent notification so the user knows it's running (music, navigation, active upload).","**Background service** — heavily restricted since Android 8: the system kills it soon after the app leaves the foreground, so it's rarely usable now.","**Bound service** — other components bind to it to call its methods (a client-server relationship, or IPC)."]},{t:"p",text:"The practical modern guidance: for immediate, ongoing, user-aware work use a *foreground service* (with its required notification); for deferrable/guaranteed background work use *WorkManager*, not a plain Service. And always remember to do the actual work off the main thread — the Service is just the component that keeps your process running, not a background executor."}]},{level:"junior",q:"What is a BroadcastReceiver and when would you use one?",a:[{t:"p",text:"**A BroadcastReceiver listens for broadcast events — messages delivered as Intents, either from the system or from apps.** The system broadcasts things like connectivity changes, boot completed, battery low, airplane mode toggled; apps can send custom broadcasts too. When a matching broadcast arrives, the receiver's `onReceive` runs to react to it."},{t:"list",items:["**Runtime-registered**: you register in code (`registerReceiver`) tied to a component's lifecycle, and must unregister to avoid leaks. Only active while registered — preferred for most cases.","**Manifest-declared (static)**: declared in the manifest so it can trigger even when the app isn't running — but this is heavily restricted since Android 8 (most implicit broadcasts can't be received statically anymore, to stop apps constantly waking up). A few exceptions like `BOOT_COMPLETED` remain."]},{t:"p",text:"The most important rule: **`onReceive` runs on the main thread and must be fast** (there's about a 10-second limit). It's a *trigger*, not a place to do work — if you need to do real work in response, hand off to WorkManager or a foreground service. Use a BroadcastReceiver to *react* to system events; for *in-app* event communication, don't use broadcasts at all — expose a shared Flow/StateFlow from a repository instead (the old `LocalBroadcastManager` is deprecated)."}]},{level:"junior",q:"What is a ContentProvider and do you always need one?",a:[{t:"p",text:"**A ContentProvider exposes an app's structured data to *other apps* through a standard interface (query/insert/update/delete), addressed by a `content://` URI.** It's Android's mechanism for sharing data across app boundaries with permission control. You access providers through a `ContentResolver`."},{t:"p",text:"**No, you don't always need one — and that's the key insight.** You need to *consume* a ContentProvider to read *system* data (contacts, calendar, media store) via `contentResolver`. But you only need to *build* one if you're **sharing your app's data with other apps**. If your data is used only within your own app, a ContentProvider is unnecessary over-engineering — just use a database/repository directly. A related special case is `FileProvider`, a built-in ContentProvider for securely sharing *files* with other apps via `content://` URIs (required since Android 7 forbade sharing raw `file://` paths). One piece of trivia worth knowing: ContentProviders initialize very early in app startup, which libraries like Firebase and AndroidX App Startup exploit to auto-initialize themselves — so if you see a provider in the merged manifest you didn't add, that's usually why."}]},{level:"senior",q:"When would you use a foreground Service versus WorkManager?",a:[{t:"p",text:"**The distinction is immediate-and-user-visible versus deferrable-and-guaranteed.** A *foreground service* is for work that must run *right now* and that the *user is aware of* — it shows a persistent notification. WorkManager is for work that can be *deferred*, must be *guaranteed* to eventually run (surviving process death and reboots), and may have *constraints*."},{t:"list",items:["**Use a foreground service when**: the work is ongoing and user-facing and must run immediately and continuously — music playback, turn-by-turn navigation, an active workout tracker, a live location share, a file upload the user is actively watching. The notification is mandatory precisely because the work is user-visible and consumes resources. It runs until you stop it (or the user does).","**Use WorkManager when**: the work is deferrable and must be *reliable* — syncing data, uploading logs, periodic backups, processing that should happen 'sometime soon' under conditions (on Wi-Fi, while charging). WorkManager persists the work request in a database, so it survives the app being killed and even device reboots, and it retries on failure with backoff. It respects Doze/battery restrictions and enforces constraints.","**They're not mutually exclusive**: WorkManager can *itself* run as a long-running foreground worker (`setForeground`) when a task needs to run immediately and show progress — so 'guaranteed AND immediate AND user-visible' work (a large download) is often a WorkManager foreground worker, getting both reliability and immediacy."]},{t:"p",text:"**The senior framing**: since Android 8's background restrictions, you can't just 'start a background service' for deferred work — the system kills it. So the decision collapses to: is it *immediate + user-visible*? → foreground service. Is it *deferrable + must-not-be-lost*? → WorkManager. And for anything tied to a screen's lifetime, neither — a coroutine in `viewModelScope`. Naming that WorkManager persists across process death/reboot (which a service does not) and that it can escalate to a foreground worker is what demonstrates real understanding."}]},{level:"senior",q:"Why are background services and manifest-declared broadcast receivers restricted on modern Android, and what do you use instead?",a:[{t:"p",text:"**The restrictions (starting with Android 8 Oreo) exist to fix a systemic battery-and-performance problem: apps were abusing background services and static broadcast receivers to run constantly and wake up on every system event, draining battery and consuming resources even when the user wasn't using them.** A device with dozens of apps each listening for `CONNECTIVITY_CHANGE` and spinning up services on every network blip had terrible battery life. Android clamped down to protect the user."},{t:"list",items:["**Background service restriction**: an app in the background can no longer freely run background services — the system stops them shortly after the app leaves the foreground. This prevents apps from doing indefinite invisible work.","**Implicit broadcast restriction**: apps can no longer register for most *implicit* broadcasts (like `CONNECTIVITY_CHANGE`) in the *manifest* — that was the mechanism for waking up when not running. This stopped the 'every app wakes on every event' storm. Explicitly-targeted broadcasts and a small allowlist (e.g. `BOOT_COMPLETED`) still work statically; runtime-registered receivers still work while the app is active.","**Doze and App Standby** (from Android 6) added further deferral — batching background work into maintenance windows when the device is idle."]},{t:"list",items:["**What to use instead — deferrable guaranteed work → WorkManager**: it batches work respecting Doze/Standby, persists across process death and reboots, and enforces constraints — the sanctioned replacement for 'do work in the background reliably'.","**Immediate user-visible ongoing work → foreground service** (with its notification), which is exempt from the background limits because the user knows about it.","**Reacting to events while the app is active → runtime-registered receiver** (register in `onStart`/`DisposableEffect`, unregister when done). For *in-app* events, don't use broadcasts at all — expose a shared Flow/StateFlow from a repository.","**Waking on connectivity/state to do work → WorkManager constraints** (e.g. a work request that runs when the network is available) rather than a receiver that triggers a service."]},{t:"p",text:"**The senior framing**: modern Android deliberately made 'run freely in the background' hard because unbounded background execution was the primary cause of poor battery life. The design shift is from 'services + receivers that keep the app awake' to 'declare *what* work you need and *under what conditions*, and let WorkManager schedule it efficiently within the OS's battery-protection rules'. Understanding *why* (battery/UX at the OS level) and the replacement toolset (WorkManager + foreground services + runtime receivers) is the complete answer."}]},{level:"senior",q:"What is the difference between a started and a bound Service?",a:[{t:"p",text:"A *started* Service (`startService`/`startForegroundService`) runs independently until it stops itself (`stopSelf`) or is stopped — for fire-and-forget work. A *bound* Service (`bindService`) provides a client-server interface (via `onBind` returning an `IBinder`) and lives only while clients are bound — for ongoing interaction. A Service can be both."},{t:"list",items:["**Started** — runs until `stopSelf`/`stopService`; independent of callers.","**Bound** — clients `bindService`, communicate via `IBinder`; dies when the last client unbinds.","**Both** — a started+bound service persists until stopped *and* unbound.","**Modern** — most background work should be WorkManager/coroutines, not raw Services."]},{t:"note",text:"Started Service (startService) runs independently until stopSelf/stopService — fire-and-forget. Bound Service (bindService) offers an IBinder client interface and lives only while clients are bound. A service can be both (persists until stopped AND unbound). Prefer WorkManager/coroutines for most background work."}]},{level:"junior",q:"What is a foreground Service, and what does it require?",a:[{t:"p",text:"A *foreground* Service performs user-visible ongoing work (music playback, navigation, active tracking) and must show a *persistent notification* so the user knows it's running. Since Android 8+ you start it with `startForegroundService` and must call `startForeground(id, notification)` within ~5 seconds, and (API 34+) declare a `foregroundServiceType` in the manifest."},{t:"code",title:"Foreground service",code:`override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
    startForeground(1, buildNotification())   // required promptly
    // do work...
    return START_STICKY
}
// Manifest: <service android:foregroundServiceType="mediaPlayback" />`},{t:"list",items:["**Persistent notification** — mandatory; users can see/stop it.","**`startForegroundService` + `startForeground`** — within ~5s or it crashes (ANR).","**`foregroundServiceType`** — declare the type (API 34+ requires it + permissions).","**Uses** — music, navigation, active location, calls, uploads in progress."]},{t:"note",text:"A foreground Service does user-visible ongoing work (playback, navigation, tracking) with a mandatory persistent notification. Start via startForegroundService + startForeground(id, notification) within ~5s (or crash), and declare foregroundServiceType (required + permission-gated on API 34+). For deferrable work, use WorkManager instead."}]},{level:"senior",q:"How do you register a BroadcastReceiver, and what are the restrictions?",a:[{t:"p",text:"Two ways: *statically* in the manifest (`<receiver>`) — but since Android 8, most implicit broadcasts *can't* be received this way (background execution limits); or *dynamically* via `registerReceiver` in code (tied to a lifecycle, must `unregisterReceiver`). For app-internal events, prefer a `LocalBroadcastManager` replacement like a shared Flow. Explicit broadcasts and a few exempt system broadcasts still work statically."},{t:"list",items:["**Static (manifest)** — most implicit broadcasts blocked since API 26; a few exemptions remain.","**Dynamic (`registerReceiver`)** — works while registered; unregister to avoid leaks.","**`RECEIVER_EXPORTED`/`NOT_EXPORTED`** — required flag on API 34+.","**In-app events** — use Flow/LiveData instead of broadcasts."]},{t:"note",text:"Register a receiver statically (<receiver> — but most implicit broadcasts are blocked since API 26) or dynamically (registerReceiver, tied to lifecycle, must unregister). API 34+ requires RECEIVER_EXPORTED/NOT_EXPORTED. For app-internal events prefer a shared Flow over broadcasts (LocalBroadcastManager is deprecated)."}]},{level:"senior",q:"What is the difference between stopSelf and stopService?",a:[{t:"p",text:"`stopSelf()` is called by the Service *itself* to stop when its work is done; `stopService(intent)` is called by a *client* to stop it externally. For started services handling multiple commands, `stopSelf(startId)` stops only if that's the most recent start command (avoiding stopping while newer work is pending)."},{t:"list",items:["**`stopSelf()`** — the Service stops itself after finishing work.","**`stopService(intent)`** — a client stops the Service.","**`stopSelf(startId)`** — stop only if `startId` is the latest command (safe with concurrent starts).","**Bound services** — stop when the last client unbinds (not via stopSelf/stopService alone)."]},{t:"note",text:"stopSelf() — the Service stops itself when done; stopService(intent) — a client stops it externally. stopSelf(startId) stops only if that's the most recent start command (safe under concurrent starts). Bound services stop when the last client unbinds."}]},{level:"senior",q:"How does a bound Service communicate with clients (IBinder, Messenger, AIDL)?",a:[{t:"p",text:"A bound Service returns an `IBinder` from `onBind`. For *same-process* binding, a `LocalBinder` exposing the service instance is simplest. For *cross-process* IPC, use a `Messenger` (message-based, serialized, single-threaded) or `AIDL` (for a full method interface with concurrent calls). Most modern apps avoid this in favor of coroutines/Flow within the app."},{t:"list",items:["**`LocalBinder`** — same-process; expose the service directly (simplest).","**`Messenger`** — cross-process, message-based, serialized (one at a time).","**AIDL** — cross-process method interface with concurrency; more complex.","**Modern** — prefer in-app coroutines/Flow; bound-service IPC is niche."]},{t:"note",text:"A bound Service returns an IBinder from onBind: LocalBinder for same-process (expose the instance, simplest), Messenger for cross-process message passing (serialized), AIDL for a full cross-process method interface with concurrency. Most apps avoid this now — use coroutines/Flow in-app; reserve IPC for genuine cross-process needs."}]},{level:"junior",q:"What does onStartCommand's return value (START_STICKY etc.) mean?",a:[{t:"p",text:"`onStartCommand` returns how the system should behave if it *kills* the Service and later has resources to restart it: `START_STICKY` (recreate the service, but with a null Intent — for ongoing work like playback), `START_NOT_STICKY` (don't recreate unless there's a pending Intent — for work that's fine to skip), and `START_REDELIVER_INTENT` (recreate and redeliver the last Intent — for work that must complete)."},{t:"list",items:["**`START_STICKY`** — recreate with null Intent; ongoing services (media).","**`START_NOT_STICKY`** — don't recreate unless a pending Intent; skippable work.","**`START_REDELIVER_INTENT`** — recreate and redeliver the Intent; must-complete work.","**Choose by** — whether restarting and/or redelivering the command makes sense."]},{t:"note",text:"onStartCommand's return controls restart-after-kill: START_STICKY (recreate with null Intent — ongoing like media), START_NOT_STICKY (don't recreate unless pending Intent — skippable), START_REDELIVER_INTENT (recreate + redeliver the last Intent — must-complete). Choose by whether restart/redelivery makes sense."}]},{level:"senior",q:"What are the modern alternatives to Services and BroadcastReceivers?",a:[{t:"p",text:"Because background Services and manifest receivers are restricted, modern apps replace them: *WorkManager* for deferrable guaranteed background work; *coroutines/Flow* for in-app async and eventing (instead of Services and `LocalBroadcastManager`); *foreground Services* only for user-visible ongoing work; and specific APIs (`ConnectivityManager.NetworkCallback`, `AlarmManager` for exact timing) instead of broad system broadcasts."},{t:"list",items:["**WorkManager** — deferrable, guaranteed, constraint-aware background work.","**Coroutines/Flow** — in-app async and events (replace LocalBroadcastManager).","**Foreground Service** — user-visible ongoing work only.","**Targeted APIs** — `NetworkCallback`, `AlarmManager`, lifecycle callbacks over broad broadcasts."]},{t:"note",text:"Modern replacements: WorkManager (deferrable guaranteed work), coroutines/Flow (in-app async + events, replacing Services and LocalBroadcastManager), foreground Services (user-visible ongoing only), and targeted APIs (NetworkCallback, AlarmManager) over broad system broadcasts. Raw background Services/manifest receivers are last resorts."}]},{level:"senior",q:"What is JobScheduler, and how does it relate to WorkManager?",a:[{t:"p",text:"`JobScheduler` is the framework API for scheduling deferrable background jobs with constraints (network, charging), batching them for battery efficiency. `WorkManager` is the recommended *higher-level* library that wraps JobScheduler (and older mechanisms) — adding guaranteed execution, chaining, observability, and backward compatibility. You almost always use WorkManager, not JobScheduler directly."},{t:"list",items:["**`JobScheduler`** — framework job scheduling with constraints/batching (API 21+).","**`WorkManager`** — wraps it; guarantees, chaining, observability, retro-compat.","**Use WorkManager** — the recommended abstraction for deferrable work.","**Under the hood** — WorkManager picks JobScheduler on modern APIs."]},{t:"note",text:"JobScheduler is the framework API for deferrable constraint-aware jobs (battery-batched). WorkManager is the recommended library wrapping it (and older mechanisms) with guaranteed execution, chaining, observability, and backward compatibility. Use WorkManager; it uses JobScheduler underneath on modern APIs."}]},{level:"senior",q:"What is the BOOT_COMPLETED receiver used for, and what are its constraints?",a:[{t:"p",text:"A `BOOT_COMPLETED` receiver (with the `RECEIVE_BOOT_COMPLETED` permission) runs after the device restarts — the classic use is *rescheduling* work that was lost on reboot (alarms, WorkManager jobs). It's one of the few implicit broadcasts still deliverable to a manifest receiver. Keep its `onReceive` fast (it runs on the main thread with a short window) — just enqueue work, don't do it inline."},{t:"list",items:["**Reschedule after reboot** — alarms, WorkManager (WorkManager auto-reschedules persisted work).","**`RECEIVE_BOOT_COMPLETED` permission** — required.","**Still manifest-deliverable** — an exempt implicit broadcast.","**Keep `onReceive` fast** — enqueue work, don't run it inline (short window, main thread)."]},{t:"note",text:"A BOOT_COMPLETED receiver (needs RECEIVE_BOOT_COMPLETED) reschedules work lost on reboot (alarms; WorkManager already auto-reschedules persisted work) — one of the few implicit broadcasts still deliverable to a manifest receiver. Keep onReceive fast (enqueue work, don't run inline — short window, main thread)."}]},{level:"senior",q:"How do you run a long-running task in a Service correctly today?",a:[{t:"p",text:"If it's user-visible and immediate, use a *foreground Service*: extend `LifecycleService`, call `startForeground` with a notification, and run the work in `lifecycleScope` (coroutines), calling `stopSelf` when done. If it's deferrable/guaranteed, use *WorkManager* instead. Never do blocking work on the Service's main thread, and always provide the required notification/type for foreground services."},{t:"code",title:"Coroutines in a service",code:`class UploadService : LifecycleService() {
    override fun onStartCommand(i: Intent?, f: Int, id: Int): Int {
        super.onStartCommand(i, f, id)
        startForeground(1, notification())
        lifecycleScope.launch { upload(); stopSelf() }   // off main thread
        return START_NOT_STICKY
    }
}`},{t:"list",items:["**Foreground + `LifecycleService`** — visible ongoing work; notification required.","**`lifecycleScope` coroutines** — off the main thread; `stopSelf` when done.","**WorkManager** — for deferrable/guaranteed work instead.","**Type + permission** — declare `foregroundServiceType` (API 34+)."]},{t:"note",text:"Long-running work: foreground Service (LifecycleService + startForeground(notification) + lifecycleScope coroutines + stopSelf when done) for user-visible immediate work; WorkManager for deferrable/guaranteed. Never block the Service's main thread; declare foregroundServiceType (API 34+)."}]},{level:"senior",q:"What are the security considerations for exported components?",a:[{t:"p",text:"An *exported* component can be invoked by other apps, so it's an attack surface. Set `android:exported` explicitly (mandatory on API 31+), keep internal components `exported=false`, protect sensitive ones with custom `permission`s, validate all incoming Intent data as untrusted, and be careful with `PendingIntent` mutability and implicit intents that could be intercepted."},{t:"list",items:["**Explicit `android:exported`** — mandatory API 31+; false unless external access is needed.","**Permissions** — guard sensitive exported components.","**Validate input** — treat incoming Intents/URIs as untrusted.","**`PendingIntent` immutable** — and avoid leaking implicit intents."]},{t:"note",text:"Exported components are an attack surface: set android:exported explicitly (mandatory API 31+, false unless needed), protect sensitive ones with permissions, validate incoming Intent data as untrusted, and use FLAG_IMMUTABLE PendingIntents / avoid interceptable implicit intents. Only export what genuinely needs external access."}]},{level:"junior",q:"What is the manifest, and what does it declare about components?",a:[{t:"p",text:"`AndroidManifest.xml` declares the app's components (Activities, Services, Receivers, Providers), their intent filters, the `application` element, permissions requested/defined, hardware/software features, the min/target SDK, and metadata. The system reads it to know what your app can do and how to launch its components — components not declared there can't be started."},{t:"list",items:["**Component declarations** — `<activity>`, `<service>`, `<receiver>`, `<provider>` (+ intent filters).","**Permissions** — `<uses-permission>` and custom `<permission>`.","**App config** — `<application>` (name, theme, backup), features, SDK levels.","**Must declare** — undeclared components can't be instantiated."]},{t:"note",text:"AndroidManifest.xml declares components (Activity/Service/Receiver/Provider + intent filters), permissions, the <application> config, hardware/software features, and SDK levels. The system reads it to know and launch your components — undeclared components can't be started. Manifest merging combines library manifests."}]},{level:"senior",q:"What was IntentService, and why is it deprecated?",a:[{t:"p",text:"`IntentService` was a Service subclass that ran each incoming Intent sequentially on a *background worker thread* and stopped itself when the queue emptied — a convenient way to do off-main-thread work. It's *deprecated* because of background execution limits (a background app can't reliably start it) and because coroutines + `WorkManager` do the job better with lifecycle awareness and guarantees."},{t:"list",items:["**Background worker thread** — processed Intents serially, auto-stopped.","**Deprecated** — hit background start limits; not lifecycle-aware.","**Replacements** — `WorkManager` (deferrable/guaranteed), or coroutines in a `LifecycleService`.","**`JobIntentService`** — a stopgap; also superseded by WorkManager."]},{t:"note",text:"IntentService ran incoming Intents serially on a background worker thread and self-stopped — convenient but deprecated (background start limits, not lifecycle-aware). Use WorkManager (deferrable/guaranteed) or coroutines in a LifecycleService instead. JobIntentService was a stopgap, also superseded."}]},{level:"junior",q:"What is a LifecycleService?",a:[{t:"p",text:"`LifecycleService` is a Service that implements `LifecycleOwner`, giving it a `Lifecycle` and thus a `lifecycleScope` — so you can launch coroutines that are automatically cancelled when the service is destroyed, and use lifecycle-aware components. It's the modern base class for services that run coroutine-based work."},{t:"code",title:"LifecycleService",code:`class SyncService : LifecycleService() {
    override fun onStartCommand(i: Intent?, f: Int, id: Int): Int {
        super.onStartCommand(i, f, id)
        lifecycleScope.launch { sync(); stopSelf() }   // cancelled on destroy
        return START_NOT_STICKY
    }
}`},{t:"list",items:["**`LifecycleOwner` service** — has a `Lifecycle` and `lifecycleScope`.","**Coroutines** — auto-cancelled when the service is destroyed.","**Lifecycle-aware** — can use observers/components.","**Modern base** — for coroutine-driven services."]},{t:"note",text:"LifecycleService is a Service that is a LifecycleOwner — it has a Lifecycle and lifecycleScope, so coroutines launched in it auto-cancel on destroy and it can use lifecycle-aware components. The modern base class for coroutine-based services."}]},{level:"junior",q:"How do you show and update a foreground Service's notification?",a:[{t:"p",text:"Build a notification (on a channel), call `startForeground(id, notification)` to make the service foreground, and to *update* it, build a new notification with the same id and call `NotificationManager.notify(id, updated)` (or `startForeground` again). This is how progress bars for downloads/uploads update while the service runs."},{t:"code",title:"Update the notification",code:`startForeground(1, buildNotification(progress = 0))
// later, as work progresses:
notificationManager.notify(1, buildNotification(progress = 50))`},{t:"list",items:["**Notification channel** — required (API 26+).","**`startForeground(id, notification)`** — enter foreground.","**Update** — `notify(id, updated)` with the same id (e.g. progress).","**Remove** — `stopForeground(true/STOP_FOREGROUND_REMOVE)` or stopping the service."]},{t:"note",text:"Build a notification on a channel, startForeground(id, notification) to go foreground, then update with notificationManager.notify(id, updated) using the same id (e.g. progress). Remove via stopForeground(STOP_FOREGROUND_REMOVE) or stopping the service. This drives download/upload progress notifications."}]},{level:"senior",q:"What do the bindService flags like BIND_AUTO_CREATE do?",a:[{t:"p",text:"`bindService(intent, connection, flags)` takes flags controlling the binding. `BIND_AUTO_CREATE` creates the service if it isn't running (the common choice). Others include `BIND_IMPORTANT` (raise the service's process priority) and `BIND_ABOVE_CLIENT`. You supply a `ServiceConnection` whose `onServiceConnected`/`onServiceDisconnected` give you the `IBinder`."},{t:"list",items:["**`BIND_AUTO_CREATE`** — start the service if needed while bound (usual).","**`BIND_IMPORTANT`** — bump the service process's importance.","**`ServiceConnection`** — receives the `IBinder` on connect.","**Unbind** — `unbindService(connection)` when done (or you leak the binding)."]},{t:"note",text:"bindService(intent, connection, flags): BIND_AUTO_CREATE creates the service if not running (usual), BIND_IMPORTANT raises its process priority. A ServiceConnection's onServiceConnected gives the IBinder. Always unbindService(connection) when done or you leak the binding (and keep the service alive)."}]},{level:"senior",q:"How do you communicate from a Service to the UI?",a:[{t:"p",text:"Modern approach: expose state from the service via a *shared observable* the UI can collect — a `StateFlow`/`SharedFlow` in a repository or a bound service's exposed flow, or update a data source (DB/DataStore) the UI already observes. Avoid broadcasts for this. For a bound service, the UI can call methods and collect flows; for a started service, route through a shared repository."},{t:"list",items:["**Shared observable** — a repository `StateFlow`/`SharedFlow` the UI collects.","**Bound service** — expose flows/methods via the `IBinder`.","**Data source** — update Room/DataStore the UI already observes.","**Avoid broadcasts** — for in-app service→UI communication."]},{t:"note",text:"Service → UI: expose state via a shared observable (repository StateFlow/SharedFlow the UI collects) or update a data source (DB/DataStore) the UI observes. A bound service can expose flows/methods via its IBinder. Avoid broadcasts for in-app communication."}]},{level:"senior",q:"What is a wakelock, and when do you need one?",a:[{t:"p",text:"A `WakeLock` (via `PowerManager`) keeps the CPU (or screen) awake so work continues while the device would otherwise sleep. You rarely need one directly — WorkManager and foreground services manage wake state for you. If you must, use a `PARTIAL_WAKE_LOCK` for the *shortest* time and always release it (a held wakelock drains the battery — a common bug)."},{t:"list",items:["**`WakeLock`** — keep CPU/screen awake during work.","**Rarely needed** — WorkManager/foreground services handle it.","**`PARTIAL_WAKE_LOCK`** — CPU only; acquire briefly, release promptly.","**Leak = battery drain** — a forgotten wakelock keeps the device awake."]},{t:"note",text:"A WakeLock (PowerManager) keeps the CPU/screen awake during work. You rarely need it directly — WorkManager and foreground services manage wake state. If required, use PARTIAL_WAKE_LOCK for the shortest time and always release it; a held wakelock drains the battery (a classic bug)."}]},{level:"senior",q:"What restrictions apply to starting a foreground service from the background (API 31+)?",a:[{t:"p",text:"On Android 12 (API 31), apps generally *can't start a foreground service while in the background* — attempting it throws `ForegroundServiceStartNotAllowedException`. There are exemptions (a high-priority FCM message, an exact alarm, the user interacting). For deferrable background-initiated work, use `WorkManager` (which can run expedited work as a foreground job under the hood) instead."},{t:"list",items:["**API 31+ restriction** — can't start a foreground service from the background (with exemptions).","**Exception** — `ForegroundServiceStartNotAllowedException` if you violate it.","**Exemptions** — high-priority FCM, exact alarm, recent user interaction, etc.","**Use WorkManager** — expedited work handles background-triggered tasks compliantly."]},{t:"note",text:"API 31+ generally forbids starting a foreground service while backgrounded (throws ForegroundServiceStartNotAllowedException), with exemptions (high-priority FCM, exact alarm, recent user interaction). For background-triggered deferrable work, use WorkManager (expedited work runs as a foreground job compliantly)."}]},{level:"senior",q:"How do you observe connectivity changes without a broadcast receiver?",a:[{t:"p",text:"Use `ConnectivityManager.registerNetworkCallback` (or `registerDefaultNetworkCallback`) with a `NetworkCallback` — it delivers `onAvailable`/`onLost`/`onCapabilitiesChanged` for the network(s) you care about. This replaced the deprecated `CONNECTIVITY_ACTION` broadcast, and you typically wrap it in a `callbackFlow` to expose connectivity as a Flow."},{t:"code",title:"Connectivity as a Flow",code:`fun connectivity(cm: ConnectivityManager): Flow<Boolean> = callbackFlow {
    val cb = object : ConnectivityManager.NetworkCallback() {
        override fun onAvailable(n: Network) { trySend(true) }
        override fun onLost(n: Network) { trySend(false) }
    }
    cm.registerDefaultNetworkCallback(cb)
    awaitClose { cm.unregisterNetworkCallback(cb) }
}`},{t:"list",items:["**`registerNetworkCallback`** — `onAvailable`/`onLost`/`onCapabilitiesChanged`.","**Replaces `CONNECTIVITY_ACTION`** — the deprecated connectivity broadcast.","**`callbackFlow`** — expose connectivity as a Flow; `unregister` in `awaitClose`.","**WorkManager** — has network constraints built in for background work."]},{t:"note",text:"Use ConnectivityManager.registerNetworkCallback (or registerDefaultNetworkCallback) with a NetworkCallback (onAvailable/onLost/onCapabilitiesChanged) — replacing the deprecated CONNECTIVITY_ACTION broadcast. Wrap it in a callbackFlow to expose connectivity as a Flow (unregister in awaitClose). WorkManager has network constraints for background work."}]},{level:"junior",q:"What was LocalBroadcastManager, and what replaces it?",a:[{t:"p",text:"`LocalBroadcastManager` let you send broadcasts *within your own app* (not exposed to other apps) — a common pattern for in-app eventing. It's *deprecated* because it mixes the observer pattern awkwardly with the broadcast system. Replace it with app-architecture observables: a `SharedFlow`/`LiveData` in a shared ViewModel/repository that components observe."},{t:"list",items:["**In-app broadcasts** — no cross-app exposure.","**Deprecated** — awkward pattern; better tools exist.","**Replacement** — `SharedFlow`/`StateFlow` (or LiveData) in a shared component.","**Benefits** — type-safe, lifecycle-aware, testable, no Intent plumbing."]},{t:"note",text:"LocalBroadcastManager sent in-app-only broadcasts for eventing — deprecated (awkward observer-over-broadcast pattern). Replace with a SharedFlow/StateFlow (or LiveData) in a shared ViewModel/repository that components observe — type-safe, lifecycle-aware, testable, no Intent plumbing."}]},{level:"senior",q:"What is the Storage Access Framework / DocumentsProvider for?",a:[{t:"p",text:"The Storage Access Framework (SAF) lets your app access files the user picks from *any* provider (local storage, Google Drive, etc.) via a system picker — using `ACTION_OPEN_DOCUMENT`/`ACTION_CREATE_DOCUMENT`. A `DocumentsProvider` is how a storage backend *exposes* its files to SAF. SAF is the scoped-storage-friendly way to read/write user-selected files without broad storage permissions."},{t:"list",items:["**SAF** — user picks files from any provider via a system picker.","**`ACTION_OPEN_DOCUMENT`/`ACTION_CREATE_DOCUMENT`** — pick/create, returns a `content://` URI.","**`DocumentsProvider`** — expose a storage backend to SAF.","**Scoped storage** — access user-chosen files without broad `READ/WRITE_EXTERNAL_STORAGE`."]},{t:"note",text:"The Storage Access Framework lets the user pick files from any provider via a system picker (ACTION_OPEN_DOCUMENT/CREATE_DOCUMENT → content:// URI); a DocumentsProvider exposes a storage backend to SAF. It's the scoped-storage-friendly way to read/write user-selected files without broad storage permissions."}]},{level:"junior",q:"What are app shortcuts, and how do you create them?",a:[{t:"p",text:"App shortcuts are quick actions exposed on long-press of the launcher icon (or via Assistant). *Static* shortcuts are defined in an XML resource; *dynamic* shortcuts are added at runtime via `ShortcutManager`/`ShortcutManagerCompat` (push based on user behavior); *pinned* shortcuts the user places on the home screen. Each shortcut is an Intent into a specific screen."},{t:"list",items:["**Static** — declared in a shortcuts XML resource.","**Dynamic** — `ShortcutManagerCompat.pushDynamicShortcut(...)` at runtime.","**Pinned** — user-placed on the home screen.","**Each = an Intent** — deep-links into a specific screen."]},{t:"note",text:"App shortcuts are quick actions on the launcher icon long-press: static (XML resource), dynamic (ShortcutManagerCompat.pushDynamicShortcut at runtime, based on behavior), and pinned (user-placed). Each shortcut is an Intent deep-linking into a specific screen. Also feeds Assistant/Google shortcuts."}]},{level:"senior",q:"What determines an Android component's lifetime versus the process lifetime?",a:[{t:"p",text:"A component's lifetime (an Activity's create→destroy, a Service's start→stop) is managed by the system based on user interaction and your calls. The *process* lifetime is separate and coarser — the OS keeps the process alive based on its importance (running components) and kills it under memory pressure. A component can be destroyed while the process lives on (cached), and the process can be killed independently, skipping `onDestroy`."},{t:"list",items:["**Component lifetime** — driven by user actions and your start/stop/finish calls.","**Process lifetime** — OS-managed by importance; killed under memory pressure.","**Decoupled** — a destroyed component can leave the process cached; process death can skip `onDestroy`.","**Implication** — save state (don't rely on `onDestroy`); design for process death."]},{t:"note",text:"Component lifetime (Activity create→destroy, Service start→stop) is driven by user actions/your calls; process lifetime is coarser, OS-managed by importance and killed under memory pressure. They're decoupled — a component can be destroyed while the process is cached, and process death can skip onDestroy. Save state; design for process death."}]}],pA={id:"services-components",title:"Services, Receivers & Providers",tagline:"Service types (foreground/bound), Service vs WorkManager, BroadcastReceivers & registration, ContentProviders & FileProvider, and modern background restrictions.",tags:["Android","Services","BroadcastReceiver","ContentProvider","WorkManager"],content:hA,qa:uA},mA=[{heading:"What Context is",blocks:[{t:"p",text:"**`Context`** is your handle to the Android application environment — it provides access to resources, system services, app-level operations, and information about the app. Almost everything in Android needs a Context: inflating layouts, starting Activities, accessing `getSystemService`, reading resources/strings, opening databases and files, showing toasts. It's 'the bridge between your code and the Android system'."},{t:"list",items:["**Things Context gives you**: resources (`getString`, `getDrawable`), system services (`getSystemService` for connectivity, notifications, etc.), starting components (`startActivity`, `startService`), file/database access, and app info (`packageName`).","It's an *abstract class*; the concrete instances you actually use are `Application`, `Activity`, and `Service` (they all *are* Contexts), plus a few wrappers."]}]},{heading:"Application Context vs Activity Context — the crucial distinction",blocks:[{t:"p",text:"The two Contexts you use most are the **Application Context** and the **Activity Context**, and choosing the wrong one causes memory leaks. The key difference is *lifetime*: the Application Context lives for the *entire app process*; an Activity Context lives only as long as *that Activity*."},{t:"table",headers:["","Application Context","Activity Context"],rows:[["Lifetime","the whole app process (singleton)","only while that Activity exists"],["Get it via","`applicationContext` / injected","`this` in an Activity"],["UI operations (dialogs, theming)","wrong — no theme/window","correct — themed, tied to a window"],["Long-lived references (singletons, WorkManager)","correct — won't leak","wrong — leaks the Activity"]]},{t:"list",items:["**Use the Activity Context for UI-related things**: showing a dialog, inflating a view into the Activity, anything that needs the Activity's theme or window. The Application Context lacks a theme and window, so UI operations with it misbehave or crash.","**Use the Application Context for long-lived things**: singletons, repositories, WorkManager, a database instance — anything that outlives an Activity. Storing an *Activity* Context in a long-lived object leaks the entire Activity (and its view hierarchy) because the object keeps the Activity alive after it should be destroyed.","**The classic leak**: a singleton holding an Activity Context (`object Manager { lateinit var ctx: Context }` set to an Activity) — the Activity can never be garbage-collected. Always pass `applicationContext` to anything that lives longer than the Activity.","**In practice with DI**: inject `@ApplicationContext` (Hilt) into repositories/managers, and only use the Activity Context locally within the Activity for UI."]}]},{heading:"The Application class",blocks:[{t:"p",text:"The **`Application`** class is a singleton created *before any other component* when your app's process starts, and it lives for the entire process lifetime. It *is* the Application Context. You can subclass it (declared in the manifest via `android:name`) to do app-wide initialization and hold app-global state."},{t:"code",title:"A custom Application class",code:`class MyApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // App-wide init: DI graph, crash reporting, logging
        // BUT keep this FAST — it delays every app start
    }
}`},{t:"list",items:["**`onCreate()` runs once per process**, before the first Activity. It's where DI frameworks initialize (Hilt generates an `Application` subclass), and where you'd set up crash reporting, logging, etc.","**Keep `Application.onCreate` lean**: it runs on the *main thread* on *every* cold start, so heavy work here directly increases startup time. Defer non-critical initialization (lazy init, App Startup library, background init) — a bloated `onCreate` is a common cause of slow app launches.","**Don't use it as a global variable dump**: it's tempting to store 'global state' here, but that's an anti-pattern (untestable, leak-prone). Use proper scoping (DI singletons) instead."]}]},{heading:"Processes — apps run in their own sandbox",blocks:[{t:"list",items:["**Each app runs in its own Linux process** with its own instance of the Android Runtime (ART) and its own user ID — a security *sandbox* isolating apps from each other. By default all of an app's components run in this single process on the **main thread** (also called the UI thread).","**The main thread** handles UI rendering and event dispatch. Blocking it (heavy work, network, disk) freezes the UI and triggers an **ANR** (Application Not Responding) if blocked ~5 seconds — hence all the emphasis on moving work to background threads/coroutines.","**The system manages process lifetime by priority**: foreground processes (visible Activity) are protected; background/cached processes are killed first when memory is needed. This is *process death* — your app's process is terminated, and it must restore from saved state when relaunched.","**Multi-process apps** (rare): you *can* run components in separate processes (`android:process` in the manifest) — e.g. isolating a crash-prone or memory-heavy component. But it complicates everything (separate memory, IPC needed to communicate, the Application class runs per-process), so it's used only for specific needs."]}]},{heading:"App startup and cold/warm/hot start",blocks:[{t:"table",headers:["Start type","What it involves","Speed"],rows:[["Cold start","process created from scratch: fork, Application.onCreate, first Activity","slowest — full setup"],["Warm start","process alive but Activity recreated (e.g. returned after being destroyed)","faster"],["Hot start","Activity already in memory, just brought to front","fastest"]]},{t:"list",items:["**Cold start** is the one to optimize — it does the most work (create the process, run `Application.onCreate`, inflate and draw the first frame). Users judge the app by it.","**What slows cold start**: heavy `Application.onCreate`, eager initialization of libraries, doing work before the first frame, large layouts, synchronous disk/network on the startup path.","**How to speed it up**: lean `Application.onCreate` (defer/lazy-init), the **App Startup** library (to order and consolidate initializers, avoiding multiple ContentProvider-based inits), **Baseline Profiles** (AOT-compile the startup path so it isn't JIT-compiled during launch — often 20-40% faster cold start), and avoiding heavy work before the first frame.","**The AndroidX App Startup library** replaces the old pattern where each library added its own ContentProvider to auto-initialize (each provider adding startup cost) — it consolidates them into a single, ordered initializer."]},{t:"note",text:"Context essentials: it's the bridge to the Android environment (resources, services, components). Use Activity Context for UI (needs theme/window), Application Context for long-lived objects (won't leak the Activity). The Application class is a per-process singleton for app-wide init — keep its onCreate fast. Each app runs in an isolated process on a main thread that must never block (ANR at ~5s). Optimize cold start with a lean Application.onCreate, App Startup, and Baseline Profiles."}]}],fA=[{level:"junior",q:"What is Context in Android?",a:[{t:"p",text:"**The concept**: `Context` is your handle to the Android app environment — the bridge between your code and the Android system. Almost everything needs it because it provides access to resources (strings, drawables), system services (`getSystemService`), the ability to start components (`startActivity`), file and database access, and app information."},{t:"list",items:["You need a Context to: inflate a layout, show a Toast or dialog, read a string resource, get a system service (connectivity, notifications), start an Activity or Service, open a database or file.","It's an abstract class; the concrete Contexts you actually use are `Application`, `Activity`, and `Service` — they *are* Contexts (they extend it).","The two you pick between most are the **Application Context** (lives for the whole app) and the **Activity Context** (lives only as long as that Activity) — and choosing correctly matters for avoiding leaks."]},{t:"p",text:"Think of Context as 'the environment handle' — whenever an Android API needs to know *which app* or *which screen* it's acting on behalf of, or needs access to system-provided things, it asks for a Context."}]},{level:"junior",q:"What is the difference between Application Context and Activity Context?",a:[{t:"p",text:"**The core difference is lifetime.** The Application Context lives for the *entire app process* (it's a singleton). An Activity Context lives only as long as *that specific Activity* — it dies when the Activity is destroyed. This lifetime difference dictates which you should use where."},{t:"list",items:["**Use the Activity Context for UI**: showing a dialog, inflating a view into the Activity, anything needing the Activity's theme or window. The Application Context has no theme or window, so UI operations with it look wrong or crash.","**Use the Application Context for long-lived objects**: singletons, repositories, a database, WorkManager — anything that outlives an Activity. Giving these an Activity Context would keep the Activity alive after it should be destroyed — a memory leak."]},{t:"code",title:"The classic leak",code:`// LEAK: singleton holding an Activity Context keeps the Activity alive forever
object Manager { lateinit var ctx: Context }
Manager.ctx = this   // 'this' is an Activity -> leaked

// CORRECT: give long-lived objects the application context
Manager.ctx = applicationContext`},{t:"p",text:"The rule of thumb: if the object using the Context lives *longer* than an Activity, use the Application Context; if the operation needs UI/theme/window, use the Activity Context. With dependency injection you inject `@ApplicationContext` into repositories and managers, and reserve the Activity Context for local UI work inside the Activity."}]},{level:"junior",q:"What is the Application class and what do you use it for?",a:[{t:"p",text:"**The concept**: the `Application` class is a singleton created before any other component when your app's process starts, living for the whole process lifetime. It *is* the Application Context. You can subclass it (declared in the manifest with `android:name`) to run app-wide initialization and hold process-global setup."},{t:"list",items:["**`onCreate()` runs once per process**, before the first Activity — the place for app-wide setup: initializing the DI graph (Hilt generates an Application subclass for this), crash reporting, logging frameworks.","**Keep it lean**: `Application.onCreate` runs on the *main thread* on *every* cold start, so heavy work here directly slows down every app launch. Defer non-critical initialization (lazy init, background threads, the App Startup library).","**Don't use it as a global-variable dump**: storing mutable 'global state' in the Application class is an anti-pattern — untestable and easy to leak. Use proper DI-scoped singletons instead."]},{t:"p",text:"So it's for genuine app-wide, process-lifetime concerns (initialization, providing the Application Context), not a convenient bag for globals. The main performance caution is that a bloated `onCreate` is a very common cause of slow cold starts, since it's squarely on the critical launch path."}]},{level:"junior",q:"What is an ANR and what causes it?",a:[{t:"p",text:"**ANR stands for 'Application Not Responding'** — a dialog the system shows (and a event it logs) when your app's *main thread* is blocked for too long and can't process user input or draw. The main thresholds: the main thread unresponsive to input for ~5 seconds, a BroadcastReceiver's `onReceive` taking more than ~10 seconds, or a Service not completing startup in time."},{t:"p",text:"**The cause is always the same fundamental mistake: doing slow work on the main thread.** The main thread (UI thread) is responsible for rendering the UI and handling input events; if you block it — with a network request, a large database query, heavy computation, or file I/O — it can't do its job, the UI freezes, and after ~5 seconds the system declares an ANR. The fix is to move all slow work *off* the main thread: use coroutines with `Dispatchers.IO`/`Default`, `withContext`, or background threads, keeping the main thread free to render and respond. This is *the* reason Android emphasizes coroutines and background threading so heavily — every 'don't block the main thread' rule exists to prevent ANRs and jank."}]},{level:"senior",q:"How do Android processes work, and what is process death?",a:[{t:"p",text:"**Each Android app runs in its own Linux process** with its own instance of the Android Runtime (ART) and a unique Linux user ID. This process is a *security sandbox* — apps can't directly access each other's memory or data; cross-app interaction goes through controlled channels (Intents, ContentProviders, permissions). By default, all of an app's components run in this single process, and specifically on its **main thread**."},{t:"list",items:["**The system manages processes by priority to reclaim memory**: it ranks processes — foreground (a visible Activity or foreground service) is highest priority and protected; visible, service, background (cached) processes descend in priority. When the system needs memory, it kills the *lowest-priority* processes first, starting with cached background ones.","**Process death** is when the system terminates your app's process (usually while it's in the background) to free resources. Everything in memory is gone — the ViewModel, singletons, static state, in-flight work. When the user returns, Android *recreates* the process and the Activity from scratch, restoring only the saved-instance-state Bundle.","**Why it matters for architecture**: because process death loses all in-memory state, robust apps must be able to rebuild from persisted state — the source of truth is the database/repository (for data) plus `SavedStateHandle`/`onSaveInstanceState` (for transient input/ids). Relying on in-memory state or singletons to 'always be there' is the bug process death exposes.","**Multi-process apps** exist (`android:process` in the manifest puts a component in a separate process — used to isolate a memory-heavy or crash-prone component, or for security isolation), but they add significant complexity: separate memory spaces, the Application class runs *per process*, and communication requires IPC (bound services/AIDL, ContentProviders). It's a deliberate choice for specific needs, not a default."]},{t:"p",text:"**The senior framing**: the process model is fundamentally about the OS treating memory as a shared, reclaimable resource and apps as disposable/recreatable. Understanding that your process can be killed at any time in the background — and that recovery means rebuilding from persisted state, not assuming memory survives — is what separates apps that lose the user's place on return from ones that restore seamlessly. It's the deeper 'why' behind the whole state-saving discipline."}]},{level:"senior",q:"What is the difference between cold, warm, and hot start, and how do you optimize cold start?",a:[{t:"p",text:"**These describe how much work Android must do to bring your app to the foreground, from most to least.** A *cold start* means the process doesn't exist yet — the system must create it (fork the process, initialize ART), run `Application.onCreate`, then create and draw the first Activity. A *warm start* means the process is alive but the Activity needs recreating (e.g. it was destroyed in the background) — less work. A *hot start* means the Activity is still in memory and just needs bringing to the front — the fastest."},{t:"list",items:["**Cold start is the one to optimize** — it does the most work and users judge the app's speed by it (especially first launch). It's measured as 'time to initial display' (first frame) and 'time to fully drawn'.","**What slows cold start**: a heavy `Application.onCreate` (runs synchronously on the main thread before anything), eager initialization of many libraries, synchronous disk/network on the startup path, large/complex initial layouts, and — a big one — JIT compilation of the startup code path happening *during* launch."]},{t:"list",items:["**Optimization 1 — lean Application.onCreate**: initialize only what's needed to draw the first screen; lazy-initialize or defer everything else (analytics, non-critical SDKs). Move eager work to background threads or trigger it after first frame.","**Optimization 2 — App Startup library**: consolidates library initializers into a single, ordered ContentProvider instead of each library adding its *own* ContentProvider (each provider adds measurable startup cost). It also lets you control initialization order and laziness.","**Optimization 3 — Baseline Profiles**: this is often the biggest single win. Without them, the startup code path is JIT-compiled *during* the user's launch. A Baseline Profile ships a list of the hot startup methods that get AOT-compiled at install time, so the launch path is already native — commonly 20-40% faster cold start and smoother first frames.","**Optimization 4 — avoid work before the first frame**: don't block the first draw on data loading (show a skeleton/placeholder and load asynchronously); keep the initial layout simple; use a proper splash screen (the SplashScreen API) rather than a heavy custom one.","**Measure it**: use `adb shell am start -W` for start time, Macrobenchmark with `StartupTimingMetric` for reliable numbers on release builds, and Perfetto/system traces to find what's on the critical path — optimize based on measurement, not guesses."]}]},{level:"junior",q:"What is Context actually used for?",a:[{t:"p",text:"`Context` is the handle to the Android application environment — it provides access to resources, assets, system services, app-level operations, and the ability to launch other components. Almost every Android API that needs to interact with the system takes a `Context`."},{t:"list",items:["**Resources/assets** — `getString`, `getDrawable`, `resources`.","**System services** — `getSystemService()` (ConnectivityManager, NotificationManager…).","**Launch components** — `startActivity`, `startService`, `sendBroadcast`.","**App info/files** — `packageName`, `filesDir`, `getSharedPreferences`, `getDatabasePath`.","**Theming/inflation** — inflating views needs a themed Context."]},{t:"note",text:"Context is the handle to the app environment: access resources/assets, system services (getSystemService), launch components (startActivity/Service/broadcast), app files/prefs, and theming for view inflation. Almost every system-interacting API needs one."}]},{level:"senior",q:"Which Context should you use for what, and what are the leak risks?",a:[{t:"p",text:"Use the *Activity Context* for UI things tied to the Activity (inflating views, showing dialogs, themed operations) because it carries the theme and window. Use the *Application Context* for anything that lives *longer* than the Activity (singletons, DI, databases, long-lived listeners) — holding an Activity Context in a long-lived object *leaks the whole Activity*."},{t:"code",title:"Right context, no leak",code:`// BAD: singleton holds an Activity -> leaks it
object Manager { lateinit var context: Context }   // if set to an Activity -> leak

// GOOD: use application context for long-lived needs
class Repo(context: Context) {
    private val appContext = context.applicationContext   // safe to retain
}`},{t:"list",items:["**Activity Context** — UI: inflate views, dialogs, themed resources; don't retain it long-term.","**Application Context** — long-lived: singletons, DB, DI, listeners; safe to hold.","**Leak** — a static/singleton/long-lived object holding an Activity Context prevents GC of the Activity.","**Rule** — need it after the Activity dies? Use `applicationContext`."]},{t:"note",text:"Activity Context for UI (inflation, dialogs, themed resources) — don't retain it. Application Context for anything longer-lived than the Activity (singletons, DB, DI, listeners) — safe to hold. Holding an Activity Context in a long-lived object leaks the whole Activity. Need it after the Activity dies? Use applicationContext."}]},{level:"senior",q:"How do you get and use system services?",a:[{t:"p",text:"Call `context.getSystemService(...)` to obtain framework managers — connectivity, notifications, alarms, clipboard, input methods, etc. The Kotlin-friendly `getSystemService<T>()` (core-ktx) or `ContextCompat.getSystemService` gives type-safe access. These managers are your gateway to system features."},{t:"code",title:"System services",code:`val nm = context.getSystemService<NotificationManager>()!!
val cm = ContextCompat.getSystemService(context, ConnectivityManager::class.java)
val am = context.getSystemService(Context.ALARM_SERVICE) as AlarmManager`},{t:"list",items:["**`getSystemService<T>()`** — type-safe (core-ktx) system manager access.","**Common services** — NotificationManager, ConnectivityManager, AlarmManager, InputMethodManager, ClipboardManager.","**Use app or activity context** — most services work with either; some (WindowManager for a window) need an Activity/UI context.","**Cache carefully** — obtain via the right context; don't leak an Activity context."]},{t:"note",text:"getSystemService gives framework managers (Notification/Connectivity/Alarm/InputMethod/Clipboard…). Use the type-safe getSystemService<T>() (core-ktx) or ContextCompat.getSystemService. Most work with app or activity context; UI/window services need an Activity context. Obtain via the correct context to avoid leaks."}]},{level:"senior",q:"What is a ContextWrapper / ContextThemeWrapper?",a:[{t:"p",text:"`ContextWrapper` is a Context that *delegates* to another Context — it's the base for Activity, Service, and Application (they wrap a base context). `ContextThemeWrapper` additionally applies a *theme*, which is why an Activity (a themed context) can inflate styled views. You can wrap a context to override the theme for a specific inflation."},{t:"code",title:"Theming a context",code:`val themed = ContextThemeWrapper(baseContext, R.style.MyDialogTheme)
val view = LayoutInflater.from(themed).inflate(R.layout.dialog, null)   // uses the theme`},{t:"list",items:["**`ContextWrapper`** — delegates to a base context; the base class of Activity/Service/Application.","**`ContextThemeWrapper`** — adds a theme; why Activity can inflate themed views.","**Override a theme** — wrap a context to inflate with a specific style.","**Application context isn't themed** — inflating with it uses the default theme (a common bug)."]},{t:"note",text:"ContextWrapper delegates to a base Context (the base class for Activity/Service/Application). ContextThemeWrapper adds a theme — why an Activity inflates styled views. Wrap a context to inflate with a specific theme. The Application context isn't themed, so inflating with it uses the default theme (a common styling bug)."}]},{level:"junior",q:"What can't you do with the Application context?",a:[{t:"p",text:"The Application context lacks a *theme* and a *window/UI scope*, so you shouldn't use it to: inflate themed views or show dialogs (they'll be unstyled or crash — dialogs need an Activity), or `startActivity` without adding `FLAG_ACTIVITY_NEW_TASK` (it has no task to launch into). Use the Activity context for UI-related operations."},{t:"list",items:["**No theme** — inflating themed views/dialogs looks wrong; dialogs require an Activity context.","**`startActivity` needs `NEW_TASK`** — from app context there's no task; add the flag.","**No window** — UI/window operations need an Activity.","**Fine for** — resources, system services, files, DB, long-lived non-UI work."]},{t:"note",text:"The Application context has no theme or window, so don't use it to inflate themed views / show dialogs (dialogs need an Activity) or startActivity without FLAG_ACTIVITY_NEW_TASK (no task to launch into). It's fine for resources, system services, files, DB, and long-lived non-UI work. UI → Activity context."}]},{level:"junior",q:"What is the difference between a process and a thread?",a:[{t:"p",text:"A *process* is an isolated instance of your app with its own memory space (by default, your app runs in one process). A *thread* is a unit of execution *within* a process, sharing that memory. Android gives each app a main (UI) thread plus a pool of binder threads; you create more threads (via coroutines/executors) for background work — but they share the process's memory."},{t:"list",items:["**Process** — isolated app instance, own memory; killed as a unit by the OS.","**Thread** — execution within a process, shared memory.","**Main thread** — the UI thread; binder threads handle IPC.","**Background threads** — coroutines/executors for work off the main thread."]},{t:"note",text:"A process is an isolated app instance with its own memory (your app = one process by default), killed as a unit by the OS. A thread is execution within a process sharing memory (main/UI thread + binder threads + your background threads). Process death loses all threads/memory; the OS restores from saved state."}]},{level:"senior",q:"How does the OS decide which process to kill under memory pressure?",a:[{t:"p",text:"Android ranks processes by *importance* and, under memory pressure, kills the *least important* first (via the Low Memory Killer). The hierarchy: foreground (visible/active) processes are killed last; then visible, then service, then *cached/background* processes (killed first). So a backgrounded app is a prime candidate for process death — which is why you save state."},{t:"list",items:["**Foreground** — has a visible Activity/foreground service; killed last.","**Visible** — visible but not focused; next.","**Service** — running a background service; mid-priority.","**Cached/background** — no visible components; killed first.","**Implication** — backgrounded apps get killed; persist state (SavedStateHandle) to restore."]},{t:"note",text:"The OS ranks processes by importance and kills the least important first (Low Memory Killer): foreground (visible/active) last, then visible, then service, then cached/background first. A backgrounded app is a prime process-death candidate — save state (SavedStateHandle/saved instance) so it restores cleanly."}]},{level:"senior",q:"What constraints apply to Application.onCreate?",a:[{t:"p",text:"`Application.onCreate` runs *once* when the process starts, *before* any Activity/Service — so it's a place for global initialization (DI, logging, crash reporting). But it runs on the *main thread* and blocks the app's cold start, so keep it *fast*: avoid heavy/blocking work, defer non-critical init (lazy, App Startup, background), or you'll slow every launch."},{t:"list",items:["**Runs once, first** — before any component; global setup (DI, Timber, Crashlytics).","**Main thread** — blocks cold start; keep it minimal.","**Defer non-critical init** — lazy initialization, App Startup, or background threads.","**Don't do I/O/network here** — it delays the first frame."]},{t:"note",text:"Application.onCreate runs once at process start, before any component (global init: DI, logging, crash reporting) — but on the main thread, blocking cold start. Keep it fast: defer non-critical init (lazy/App Startup/background), no heavy I/O/network. Slow onCreate = slow every launch."}]},{level:"senior",q:"What is the App Startup library, and why use it?",a:[{t:"p",text:"`androidx.startup` (App Startup) provides a single, shared `ContentProvider` to initialize components at app startup — replacing the pattern where *each library* declares its own auto-init `ContentProvider` (which adds startup overhead, one per library). You define `Initializer`s with dependencies, and App Startup runs them in order through one provider."},{t:"code",title:"An Initializer",code:`class TimberInitializer : Initializer<Unit> {
    override fun create(context: Context) { Timber.plant(Timber.DebugTree()) }
    override fun dependencies(): List<Class<out Initializer<*>>> = emptyList()
}`},{t:"list",items:["**One shared `ContentProvider`** — instead of one per auto-initializing library.","**`Initializer<T>`** — define init logic and dependencies (ordering).","**Faster startup** — fewer providers to instantiate.","**Manual init** — you can disable auto-init and call `AppInitializer` lazily."]},{t:"note",text:"App Startup (androidx.startup) uses ONE shared ContentProvider to run Initializers (with declared dependencies for ordering) — replacing the per-library auto-init ContentProviders that each add startup cost. Fewer providers = faster cold start; you can also disable auto-init and initialize lazily via AppInitializer."}]},{level:"senior",q:"How do libraries like Firebase and WorkManager auto-initialize?",a:[{t:"p",text:"Many libraries auto-initialize by declaring a `ContentProvider` in their manifest — `ContentProvider.onCreate` runs *before* `Application.onCreate`, giving them a hook to set up with the app `Context` without you writing init code. The downside is each such provider adds cold-start cost, which is why App Startup consolidates them."},{t:"list",items:["**Auto-init via `ContentProvider`** — runs before `Application.onCreate`.","**No user code needed** — the library initializes itself with the context.","**Cost** — each provider instantiation adds startup time.","**Consolidate/disable** — App Startup, or remove the provider via manifest merge and init manually."]},{t:"note",text:"Libraries auto-initialize by declaring a ContentProvider whose onCreate runs before Application.onCreate — self-setup with the app Context, no user code. Cost: each provider adds cold-start time. Consolidate with App Startup, or disable a library's provider (manifest merge) and initialize it manually to speed startup."}]},{level:"junior",q:"How do you detect the app moving to the background or foreground?",a:[{t:"p",text:"Use `ProcessLifecycleOwner`, which provides a `Lifecycle` for the *whole app process* (not a single Activity) — its `ON_START`/`ON_STOP` fire when the app as a whole enters the foreground/background. Observe it to run app-level logic (pause syncing, lock the app, log sessions) without tracking every Activity manually."},{t:"code",title:"App-level foreground/background",code:`ProcessLifecycleOwner.get().lifecycle.addObserver(object : DefaultLifecycleObserver {
    override fun onStart(owner: LifecycleOwner) { /* app foregrounded */ }
    override fun onStop(owner: LifecycleOwner) { /* app backgrounded */ }
})`},{t:"list",items:["**`ProcessLifecycleOwner`** — a lifecycle for the whole process.","**ON_START/ON_STOP** — app foreground/background (debounced across Activity transitions).","**Uses** — app lock, session tracking, pausing sync when backgrounded.","**Not per-Activity** — it aggregates all Activities."]},{t:"note",text:"ProcessLifecycleOwner exposes a Lifecycle for the whole app process — ON_START/ON_STOP fire on app-level foreground/background (debounced across Activity transitions), so you don't track each Activity. Use for app lock, session tracking, or pausing sync when backgrounded."}]},{level:"senior",q:"What is StrictMode, and what does it catch?",a:[{t:"p",text:"`StrictMode` is a developer tool that detects accidental *main-thread violations* (disk/network I/O on the UI thread) and *VM policy violations* (leaked Activities, unclosed resources like cursors/streams). Enable it in debug builds to catch jank/leak sources early — it logs or crashes on violations you'd otherwise miss."},{t:"code",title:"Enabling StrictMode",code:`if (BuildConfig.DEBUG) {
    StrictMode.setThreadPolicy(StrictMode.ThreadPolicy.Builder()
        .detectDiskReads().detectNetwork().penaltyLog().build())
    StrictMode.setVmPolicy(StrictMode.VmPolicy.Builder()
        .detectActivityLeaks().detectLeakedClosableObjects().penaltyLog().build())
}`},{t:"list",items:["**Thread policy** — main-thread disk/network I/O detection.","**VM policy** — leaked Activities, unclosed closables, SQLite issues.","**Debug only** — enable in debug; `penaltyLog`/`penaltyDeath`.","**Catches early** — jank and leaks you'd otherwise ship."]},{t:"note",text:"StrictMode detects main-thread violations (disk/network I/O on the UI thread) and VM violations (leaked Activities, unclosed cursors/streams). Enable in debug builds (penaltyLog/penaltyDeath) to catch jank and leak sources early — it surfaces mistakes you'd otherwise miss until production."}]},{level:"senior",q:"What threads exist in an Android app process?",a:[{t:"p",text:"Every app process has a *main (UI) thread* (runs the Looper/message queue, handles UI and lifecycle), a pool of *binder threads* (handle incoming IPC calls, e.g. from the system), and any *background threads* you create (coroutine dispatchers, executors). Only the main thread may touch UI; binder threads shouldn't be blocked (they serve system calls)."},{t:"list",items:["**Main/UI thread** — Looper-driven; UI, lifecycle, input; never block it.","**Binder thread pool** — handles incoming IPC (system callbacks, bound services).","**Background threads** — your coroutine dispatchers/executors for work.","**Rule** — UI only on main; offload heavy work; don't block binder threads."]},{t:"note",text:"An app process has: the main/UI thread (Looper — UI, lifecycle, input; never block), a binder thread pool (incoming IPC/system callbacks), and background threads you create (dispatchers/executors). Only main touches UI; offload heavy work; don't block binder threads (they serve system calls)."}]},{level:"junior",q:"Why should singletons and DI use the application context?",a:[{t:"p",text:"Singletons and DI-provided objects live as long as the *process*, so if they hold an *Activity* context, they keep that (now-destroyed) Activity alive — a leak. Using `applicationContext` (which lives for the whole process anyway) avoids the leak. In Hilt, `@ApplicationContext` provides exactly this."},{t:"code",title:"Application context in DI",code:`class AnalyticsManager @Inject constructor(
    @ApplicationContext private val context: Context,   // safe, process-lived
) { }`},{t:"list",items:["**Long-lived objects** — singletons/DI live for the process.","**Activity context in them** — leaks the Activity.","**`applicationContext`** — process-lived, safe to retain.","**Hilt `@ApplicationContext`** — injects the app context."]},{t:"note",text:"Singletons/DI objects live for the whole process, so holding an Activity context leaks that Activity. Use applicationContext (process-lived, safe) — in Hilt, inject @ApplicationContext. Never retain an Activity context in a long-lived object."}]},{level:"senior",q:"What is a multi-process app, and what are the trade-offs?",a:[{t:"p",text:"By default an app runs in one process, but you can declare `android:process` on a component to run it in a *separate* process (e.g. an isolated media/crash-handling process). Trade-offs: isolation and separate memory (a crash in one process doesn't kill the other), but *no shared memory* (statics/singletons are duplicated), IPC overhead to communicate, and a separate `Application.onCreate` per process."},{t:"list",items:["**`android:process`** — run a component in another process.","**Isolation** — separate memory; one process crashing doesn't kill the other.","**No shared state** — statics/singletons/DI graphs are duplicated per process.","**Cost** — IPC to communicate; `Application.onCreate` runs in each process (guard init).","**Uses** — isolated risky work (media codecs, WebView), reducing main-process memory."]},{t:"note",text:"android:process runs a component in a separate process — isolation/separate memory (a crash there doesn't kill the main app) but NO shared memory (statics/singletons/DI duplicated), IPC overhead, and Application.onCreate runs per process (guard init by process name). Used for isolated risky work (media/WebView) or memory separation."}]},{level:"senior",q:"What is Binder / IPC, and how does it relate to TransactionTooLargeException?",a:[{t:"p",text:"Binder is Android's IPC mechanism — the kernel driver that lets processes communicate (your app ↔ system services, bound services, `startActivity` with extras). Data passed through Binder goes into a fixed-size *transaction buffer* (~1MB shared per process). Passing too much (huge `Bundle` extras, saved state) throws `TransactionTooLargeException`, which is why you keep IPC payloads (Intents, saved state) small."},{t:"list",items:["**Binder** — the IPC driver for cross-process calls (system services, bound services, Intents).","**Transaction buffer** — ~1MB shared per process for in-flight IPC.","**`TransactionTooLargeException`** — from oversized payloads (big Intent extras, saved state).","**Keep small** — pass ids, not large objects, across process boundaries."]},{t:"note",text:"Binder is Android's IPC mechanism (app ↔ system services/bound services, Intent delivery). Data crosses through a fixed ~1MB transaction buffer per process — oversized payloads (huge Intent extras, saved state) throw TransactionTooLargeException. Keep IPC payloads small (pass ids, not large objects)."}]},{level:"junior",q:"What is the role of the Application class in dependency injection?",a:[{t:"p",text:"The `Application` class is the process-lifetime root, so it's the natural place to host the app-wide DI container. In Hilt, you annotate it `@HiltAndroidApp`, which generates the application-level component (the DI graph root) that lives as long as the process and provides `@ApplicationContext` and singleton-scoped dependencies."},{t:"code",title:"Hilt application",code:`@HiltAndroidApp
class MyApp : Application() {
    override fun onCreate() { super.onCreate(); /* global init */ }
}`},{t:"list",items:["**Process-lifetime root** — hosts the app-wide DI graph.","**`@HiltAndroidApp`** — generates the application component (DI root).","**Provides** — `@ApplicationContext`, `@Singleton` dependencies.","**Other frameworks** — Koin/Dagger also initialize their graph in `Application`."]},{t:"note",text:"The Application class is the process-lifetime root, so it hosts the app-wide DI container: @HiltAndroidApp generates the application component (DI graph root) living for the process, providing @ApplicationContext and @Singleton dependencies. Koin/Dagger similarly initialize their graph in Application."}]},{level:"senior",q:"How does an app restore after process death?",a:[{t:"p",text:"When the user returns to a process-killed app, Android *recreates the task and the top Activity*, passing back the saved instance state `Bundle` (which includes `SavedStateHandle` contents). A fresh process starts (`Application.onCreate` runs again), the Activity is recreated with `savedInstanceState`, and your ViewModel rebuilds data from the restored ids. The user ideally can't tell it was killed."},{t:"list",items:["**Task restored** — the back stack of the top task is recreated.","**Fresh process** — `Application.onCreate` runs again (new DI graph).","**Saved state returned** — `onCreate(savedInstanceState)` + `SavedStateHandle`.","**Rebuild data** — the ViewModel re-fetches from restored ids (data itself wasn't saved)."]},{t:"note",text:"After process death, returning recreates the task and top Activity with the saved instance-state Bundle (incl. SavedStateHandle); a fresh process starts (Application.onCreate reruns), the Activity restores small UI state, and the ViewModel rebuilds data from restored ids. Save ids (not large data) so restoration is seamless."}]},{level:"junior",q:"What is the difference between Application.onCreate and Activity.onCreate?",a:[{t:"p",text:"`Application.onCreate` runs *once per process* when the app starts, before any component — for global, app-wide initialization. `Activity.onCreate` runs *each time an Activity is created* (including on every configuration change) — for screen-specific setup. They differ in scope and frequency: app-global-once vs screen-specific-many."},{t:"list",items:["**`Application.onCreate`** — once per process; global init (DI, logging).","**`Activity.onCreate`** — per Activity creation (and every config change); screen setup.","**Frequency** — app: once; activity: potentially many times.","**Keep both lean** — they block startup/screen-display respectively."]},{t:"note",text:"Application.onCreate: once per process at app start, before any component — global init. Activity.onCreate: each Activity creation (incl. every config change) — screen setup. Scope/frequency differ: app-global-once vs screen-specific-many. Keep both lean (they block startup / screen display)."}]},{level:"senior",q:"Why must you guard initialization in a multi-process app's Application.onCreate?",a:[{t:"p",text:"In a multi-process app, `Application.onCreate` runs *once in each process* — so global initialization (analytics, DI, heavy setup) would run redundantly (or incorrectly) in a secondary process (e.g. a `:media` process). Guard by checking the current process name and only running the main-process init where appropriate."},{t:"code",title:"Process-guarded init",code:`override fun onCreate() {
    super.onCreate()
    if (getProcessName() == packageName) {   // main process only
        initAnalytics(); initHeavyStuff()
    }
}`},{t:"list",items:["**Runs per process** — each process instantiates `Application`.","**Guard by process name** — `getProcessName()`/`Application.getProcessName()`.","**Avoid redundant init** — don't run main-process setup in secondary processes.","**Lightweight secondary** — secondary processes should init only what they need."]},{t:"note",text:"In a multi-process app, Application.onCreate runs once per process — so guard global init (analytics/DI) by checking getProcessName() == packageName to run main-process setup only in the main process, avoiding redundant/incorrect init in secondary processes (e.g. :media). Init secondary processes minimally."}]},{level:"junior",q:"How do you avoid leaking a Context, in practice?",a:[{t:"p",text:"Leaks happen when a long-lived object retains a short-lived Context (usually an Activity). Avoid them by: using `applicationContext` for anything outliving the Activity, not keeping static/singleton references to Activities/Views, unregistering listeners in the paired lifecycle callback, and using `WeakReference` only as a last resort. Tools like LeakCanary catch these automatically."},{t:"list",items:["**`applicationContext` for long-lived** — singletons, DB, DI, managers.","**No static Activity/View** — a static field holding a View/Activity leaks it.","**Unregister** — listeners/receivers/observers in the paired teardown.","**LeakCanary** — detects retained Activities/Fragments in debug builds."]},{t:"note",text:"Avoid Context leaks: use applicationContext for anything outliving the Activity, never keep static/singleton references to Activities/Views, unregister listeners in the paired callback, and rely on LeakCanary (debug) to catch retained Activities/Fragments. WeakReference is a last resort, not a first fix."}]},{level:"senior",q:"What is the difference between the process lifecycle and the Activity lifecycle?",a:[{t:"p",text:"The *Activity lifecycle* tracks a single screen (create→resume→pause→destroy). The *process lifecycle* is coarser — the process exists from `Application.onCreate` until the OS kills it, spanning many Activities. A process can outlive individual Activities (backgrounded app kept in memory) and can be killed independently of any Activity's lifecycle (process death without `onDestroy`)."},{t:"list",items:["**Activity lifecycle** — per-screen callbacks; recreated on config change.","**Process lifecycle** — `Application.onCreate` → OS kill; spans all Activities.","**Independence** — process death can skip `onDestroy`; a process outlives backgrounded Activities.","**`ProcessLifecycleOwner`** — observes app-level foreground/background."]},{t:"note",text:"Activity lifecycle = per-screen (create→destroy, recreated on config change). Process lifecycle = coarser, Application.onCreate until OS kill, spanning many Activities. Process death can skip onDestroy and happens independently of any Activity. ProcessLifecycleOwner observes app-level foreground/background."}]},{level:"senior",q:"Why does starting an Activity from a non-Activity context require FLAG_ACTIVITY_NEW_TASK?",a:[{t:"p",text:"Activities normally launch into the *task* of the Activity that started them. A non-Activity context (Application, Service, BroadcastReceiver) has *no task* to launch into, so `startActivity` from it requires `FLAG_ACTIVITY_NEW_TASK` to create/find a task — otherwise it throws `AndroidRuntimeException: Calling startActivity() from outside of an Activity context`."},{t:"code",title:"NEW_TASK from app context",code:`val intent = Intent(appContext, DetailActivity::class.java)
    .addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)   // required from non-Activity context
appContext.startActivity(intent)`},{t:"list",items:["**Activities launch into a task** — the starter Activity's task by default.","**Non-Activity context has no task** — Application/Service/Receiver.","**`FLAG_ACTIVITY_NEW_TASK`** — required to create/find a task.","**Without it** — throws (calling startActivity outside an Activity context)."]},{t:"note",text:"Activities launch into the starter's task, but a non-Activity context (Application/Service/Receiver) has no task — so startActivity from it needs FLAG_ACTIVITY_NEW_TASK to create/find one, else it throws 'Calling startActivity() from outside of an Activity context'."}]},{level:"senior",q:"What causes an ANR, and how do you diagnose one?",a:[{t:"p",text:"An ANR (Application Not Responding) fires when the *main thread* is blocked too long: ~5s for input events, ~10s for a `BroadcastReceiver`'s `onReceive`, or foreground service start timeouts. Causes: heavy work/I/O on the main thread, deadlocks, or slow binder calls. Diagnose via the ANR trace (`/data/anr/traces.txt` / Play Console → Android vitals), which shows the main thread's stack at the freeze."},{t:"list",items:["**Thresholds** — ~5s input, ~10s broadcast receiver, service start limits.","**Causes** — main-thread I/O/computation, deadlocks, slow synchronous IPC.","**Diagnose** — ANR traces (main-thread stack), Play Console Android vitals, Perfetto/systrace.","**Fix** — move work off the main thread (coroutines), avoid blocking calls, use StrictMode to catch early."]},{t:"note",text:"ANR = main thread blocked too long (~5s input, ~10s BroadcastReceiver, service-start timeouts) — from main-thread I/O/computation, deadlocks, or slow IPC. Diagnose via ANR traces (main-thread stack), Play Console Android vitals, Perfetto. Fix by moving work off main (coroutines) and catching early with StrictMode."}]}],gA={id:"context-process",title:"Context, Processes & App Startup",tagline:"What Context is, Application vs Activity Context & leaks, the Application class, processes & process death, ANRs, and cold/warm/hot start optimization.",tags:["Android","Context","Application","Process Death","ANR","Startup"],content:mA,qa:fA},yA=[{heading:"The main thread and the single-threaded UI rule",blocks:[{t:"p",text:"Android UI is **single-threaded**: all UI updates must happen on the **main thread** (a.k.a. the UI thread). This is a hard rule — touching a View from a background thread throws `CalledFromWrongThreadException`. Conversely, the main thread must *never* be blocked (doing network, disk, or heavy work on it freezes the UI and causes an ANR after ~5 seconds). These two rules together — 'update UI only on main, do heavy work only off main' — are the source of all Android threading patterns."},{t:"list",items:["**Why single-threaded UI**: making the UI toolkit thread-safe (locks on every view operation) would be slow and deadlock-prone; a single UI thread avoids that entirely, at the cost of you managing the boundary.","**The boundary you constantly cross**: do work on a background thread → deliver the result to the main thread to update the UI. Coroutines make this clean (`withContext(IO)` for work, resume on Main), but it's built on lower-level machinery: the Handler/Looper/MessageQueue."]}]},{heading:"Handler, Looper, and MessageQueue — how the main thread works",blocks:[{t:"p",text:"The main thread isn't magic — it's a normal thread running an infinite **message loop**. Three pieces make it work: a **`Looper`** (runs the loop), a **`MessageQueue`** (a queue of work to do), and **`Handler`**s (used to post work onto the queue and process it). This is the foundation beneath everything — the coroutine `Dispatchers.Main`, `runOnUiThread`, `View.post`, all sit on top of it."},{t:"list",items:["**`Looper`** — an object that runs an infinite loop (`Looper.loop()`) pulling messages off a queue and dispatching them, one at a time. The main thread has a Looper set up for you (`Looper.getMainLooper()`); background threads don't by default.","**`MessageQueue`** — the queue of `Message`s/`Runnable`s waiting to be processed, ordered by time. The Looper takes the next one when its time arrives.","**`Handler`** — bound to a Looper; you use it to *post* work onto that thread's queue (`handler.post { }`, `handler.postDelayed({ }, 1000)`). A Handler on the main Looper is how a background thread delivers a result to the main thread: it posts a Runnable that the main Looper picks up and runs *on the main thread*.","**The whole model**: the main thread loops forever, processing queued messages (input events, UI updates, posted Runnables) one by one. That's why UI work is serialized and why blocking any single message (a long operation) stalls everything behind it — the loop can't advance."]},{t:"code",title:"Posting to the main thread (the classic pre-coroutine pattern)",code:`// Background thread doing work, delivering result to main:
val mainHandler = Handler(Looper.getMainLooper())
thread {
    val result = doHeavyWork()
    mainHandler.post {           // runs on the main thread
        textView.text = result   // safe to touch UI here
    }
}
// Modern coroutine equivalent:
lifecycleScope.launch {
    val result = withContext(Dispatchers.IO) { doHeavyWork() }
    textView.text = result       // back on Main automatically
}`},{t:"note",text:"Interview-ready: the main thread runs a Looper that pulls from a MessageQueue; Handlers post work onto a thread's queue. `Dispatchers.Main` (coroutines), `runOnUiThread`, `View.post`, LiveData delivery — all use this machinery to run work on the main thread. Blocking the loop (a long task) stalls all queued UI work → jank/ANR. Coroutines are the modern abstraction over this, but the Handler/Looper model is what's underneath."}]},{heading:"The evolution of Android threading",blocks:[{t:"table",headers:["Era","Tool","Status"],rows:[["Early","Thread + Handler, AsyncTask","AsyncTask deprecated (leak-prone, lifecycle-blind)"],["Middle","Loaders, IntentService, RxJava","Loaders/IntentService deprecated; RxJava still used"],["Modern","Kotlin Coroutines + Flow, WorkManager","current standard"]]},{t:"list",items:["**`AsyncTask`** (deprecated) — the old way to do background work with UI callbacks; deprecated because it leaked Activities, ignored lifecycle, and had error-prone threading. Know it exists (legacy code) but never use it in new code.","**Coroutines are the modern answer** — structured, lifecycle-aware (via scopes), cancellable, and readable. They replaced AsyncTask/Loaders/most manual Handler use.","You still occasionally touch `Handler`/`Looper` directly for things like a periodic UI update loop or `postDelayed`, but coroutines (`delay`, `withContext`) cover almost all cases more cleanly."]}]},{heading:"Permissions — the model",blocks:[{t:"p",text:"Android **permissions** gate access to sensitive data and capabilities (location, camera, contacts, microphone). There are two broad types by *protection level*:"},{t:"list",items:["**Normal permissions** — low-risk (internet, vibrate, set alarm). Declared in the manifest and granted *automatically* at install; no user prompt.","**Dangerous (runtime) permissions** — access sensitive data/capabilities (location, camera, contacts, storage, microphone). Declared in the manifest *and* must be requested *at runtime*, with the user explicitly granting or denying via a system dialog. Introduced in Android 6 (Marshmallow) — before that, all permissions were granted at install.","**Special permissions** — a few high-risk ones (display over other apps, modify system settings, exact alarms, all-files access) require the user to toggle them in a dedicated Settings screen, not a simple dialog."]}]},{heading:"Requesting runtime permissions",blocks:[{t:"code",title:"The modern Activity Result API",code:`// Register a launcher (in an Activity/Fragment)
val requestPermission = registerForActivityResult(
    ActivityResultContracts.RequestPermission()
) { granted ->
    if (granted) startLocationUpdates()
    else showRationaleOrDeniedUi()
}

// Request when needed:
when {
    checkSelfPermission(ACCESS_FINE_LOCATION) == PERMISSION_GRANTED ->
        startLocationUpdates()                       // already have it
    shouldShowRequestPermissionRationale(ACCESS_FINE_LOCATION) ->
        showRationaleDialog()                         // explain why, then request
    else ->
        requestPermission.launch(ACCESS_FINE_LOCATION) // ask
}`},{t:"list",items:["**The flow**: check if already granted → if not, show a rationale if appropriate (`shouldShowRequestPermissionRationale` returns true after a previous denial) → request via the launcher → handle the result.","**Use the Activity Result API** (`registerForActivityResult`) — the modern replacement for the old `onRequestPermissionsResult` callback (which was verbose and error-prone). It handles the plumbing and works with the lifecycle.","**Handle denial gracefully**: the user can deny, deny-with-'don't ask again' (after which the system won't show the dialog and you should guide them to Settings), or grant. Design for all outcomes — never assume you'll get the permission.","**Request in context, with rationale**: ask for a permission *when the feature needs it* (not all upfront at launch), and explain *why* — this dramatically improves grant rates and is required for sensitive permissions on modern Android.","**Newer nuances**: location has *coarse vs fine* and *background* location (background requires a separate, harder-to-get grant); Android 13+ added granular media permissions (photos vs video vs audio instead of broad storage) and a `POST_NOTIFICATIONS` runtime permission."]}]}],wA=[{level:"junior",q:"Why can't you update the UI from a background thread?",a:[{t:"p",text:"**Because Android's UI toolkit is single-threaded — all UI updates must happen on the main (UI) thread.** Touching a View from any other thread throws `CalledFromWrongThreadException`. The reason is a deliberate design choice: making the entire UI toolkit thread-safe would require locking on every view operation, which would be slow and prone to deadlocks. Instead, Android confines all UI work to one thread, which is simpler and faster — at the cost of you having to manage the boundary between background work and UI updates."},{t:"p",text:"So the pattern is always: do heavy work (network, disk, computation) on a *background* thread, then *deliver the result to the main thread* to update the UI. The flip side is equally important — you must *not block* the main thread either, because it's responsible for rendering and input; blocking it freezes the UI and causes an ANR after ~5 seconds. Coroutines make crossing this boundary clean: `withContext(Dispatchers.IO) { work() }` runs off-main and automatically resumes on Main to update the UI. Underneath, this uses the Handler/Looper machinery to post the result back to the main thread."}]},{level:"junior",q:"What are Handler, Looper, and MessageQueue?",a:[{t:"p",text:"**Together they're the machinery that makes a thread able to process a stream of work — and it's what the main thread is built on.** The main thread isn't special magic; it's a normal thread running an infinite message loop made of these three pieces:"},{t:"list",items:["**`Looper`** — runs an infinite loop that pulls messages off a queue one at a time and dispatches them. The main thread has one set up automatically (`Looper.getMainLooper()`); regular background threads don't.","**`MessageQueue`** — the queue of pending work (`Message`s / `Runnable`s), ordered by time, that the Looper pulls from.","**`Handler`** — bound to a specific Looper; you use it to *post* work onto that thread's queue (`handler.post { }`, `postDelayed`). A Handler tied to the main Looper is how a background thread hands a result to the main thread."]},{t:"code",title:"Delivering a result to the main thread",code:`val main = Handler(Looper.getMainLooper())
thread {
    val result = heavyWork()
    main.post { textView.text = result }   // runs on the main thread
}`},{t:"p",text:"The model: the main thread loops forever, processing queued messages (input events, UI updates, posted Runnables) sequentially. This is why UI work is serialized, and why a single long operation on the main thread stalls everything behind it (jank/ANR) — the loop can't advance until each message finishes. Higher-level tools all sit on this: `Dispatchers.Main`, `runOnUiThread`, `View.post`, and LiveData delivery all ultimately post to the main Looper via a Handler. Coroutines are the modern abstraction, but Handler/Looper is the foundation."}]},{level:"junior",q:"What is the difference between normal and dangerous (runtime) permissions?",a:[{t:"p",text:"**They differ by risk level and how they're granted.** *Normal* permissions are low-risk (internet access, vibrate, set an alarm) — you declare them in the manifest and the system grants them *automatically at install* with no user prompt. *Dangerous* (runtime) permissions guard sensitive user data or capabilities (location, camera, contacts, microphone, storage) — you declare them in the manifest *and* must request them *at runtime*, where the user sees a system dialog and explicitly grants or denies."},{t:"list",items:["**Normal** — internet, network state, vibrate, bluetooth (basic). Granted at install, no runtime code needed.","**Dangerous** — anything touching private data or hardware that could harm privacy: fine/coarse location, camera, microphone, contacts, calendar, SMS, media/storage. Must be requested at runtime (since Android 6 Marshmallow; before that all permissions were install-time).","**Special** — a few high-risk ones (draw over other apps, modify system settings, exact alarms, all-files access) require the user to enable them in a dedicated Settings screen rather than a simple dialog."]},{t:"p",text:"The runtime model exists so users control access to sensitive data *contextually* — they can grant camera access to a camera app but deny it to a game, and revoke it later. As an app developer this means you must *check* whether you have a dangerous permission before using the feature, *request* it if not (ideally with a rationale explaining why), and *handle denial gracefully* — never assume you have it."}]},{level:"junior",q:"How do you request a runtime permission in a modern Android app?",a:[{t:"p",text:"**You use the Activity Result API (`registerForActivityResult`) with a permission contract, following a check → rationale → request → handle-result flow.** This is the modern replacement for the old, verbose `onRequestPermissionsResult` callback."},{t:"code",title:"The pattern",code:`val launcher = registerForActivityResult(
    ActivityResultContracts.RequestPermission()
) { granted -> if (granted) useFeature() else showDenied() }

when {
    checkSelfPermission(CAMERA) == PERMISSION_GRANTED -> useFeature()
    shouldShowRequestPermissionRationale(CAMERA) -> showRationale()  // then launch
    else -> launcher.launch(CAMERA)
}`},{t:"list",items:["**Check first**: `checkSelfPermission` — if already granted, just use the feature; don't re-ask.","**Show rationale when appropriate**: `shouldShowRequestPermissionRationale` returns true if the user previously denied (but not permanently) — explain *why* you need it before re-requesting, which improves grant rates.","**Request via the launcher**: `launcher.launch(permission)` shows the system dialog; the result callback tells you granted or denied.","**Handle all outcomes**: granted, denied, or denied-permanently ('don't ask again'). After a permanent denial the dialog won't show again, so you guide the user to app Settings. Design the feature to degrade gracefully if denied — never assume you'll get it."]},{t:"p",text:"Best practice: request permissions *in context* — right when the feature needs it, not all at launch — and only ask for what you truly need. Modern Android also has finer-grained permissions (coarse vs fine location, background location as a separate grant, granular media permissions on Android 13+, and a runtime `POST_NOTIFICATIONS` permission), so request the *least* powerful permission that satisfies the feature."}]},{level:"senior",q:"Why was AsyncTask deprecated, and how do coroutines solve its problems?",a:[{t:"p",text:"**`AsyncTask` was the old API for running background work with UI callbacks (`doInBackground` on a background thread, `onPostExecute` on the main thread). It was deprecated because it had several serious, structural problems that made it a frequent source of bugs — problems coroutines solve by design.**"},{t:"list",items:["**Memory leaks**: AsyncTasks were often inner classes holding an implicit reference to the Activity. If the Activity was destroyed (rotation!) while the task was still running, the task kept the Activity alive — a leak — and then tried to update a destroyed UI. Coroutines tied to a lifecycle scope (`viewModelScope`, `lifecycleScope`) are *cancelled* when the scope dies, so they don't leak or update dead UI.","**Lifecycle-blindness**: AsyncTask had no awareness of the Activity/Fragment lifecycle — it just ran to completion regardless, causing crashes when `onPostExecute` touched a gone Activity. Coroutine scopes are lifecycle-aware, so work is cancelled at the right time automatically.","**No structured concurrency / poor cancellation**: cancelling an AsyncTask was cooperative and awkward, and there was no clean way to compose multiple tasks or propagate errors. Coroutines have structured concurrency — a parent-child tree with proper cancellation and error propagation.","**Threading pitfalls & serial executor confusion**: AsyncTasks shared a single serial executor by default (so parallel tasks unexpectedly queued), and configuring parallelism was error-prone. Coroutines make threading explicit and correct via dispatchers.","**Verbose, callback-based, hard to read**: nested AsyncTasks (task after task) became callback hell. Coroutines let you write sequential-looking async code (`val a = fetchA(); val b = fetchB(a)`), far more readable."]},{t:"p",text:"**The senior framing**: AsyncTask's failures all trace to the same root — it treated background work as *unstructured* and *lifecycle-unaware*, so the developer had to manually manage leaks, cancellation, and threading, and usually got at least one wrong. Coroutines invert this: work is *structured* (bound to a scope with automatic cancellation), *lifecycle-aware* (via `viewModelScope`/`lifecycleScope`), *cancellation-cooperative* by default, and *readable* (sequential style). That's why the entire ecosystem moved to coroutines, and why AsyncTask, Loaders, and IntentService were all deprecated in favor of coroutines + WorkManager."}]},{level:"senior",q:"What happens if you block the main thread's Looper, and how does this connect to jank and ANRs?",a:[{t:"p",text:"**The main thread runs a single `Looper` that processes messages from its `MessageQueue` one at a time, sequentially. If you block the processing of any one message with a long operation, the loop cannot advance to the next message — so every queued UI update, input event, and animation frame behind it is stalled.** This is the mechanical root of both jank and ANRs."},{t:"list",items:["**How rendering works via the Looper**: the system schedules a 'draw' message roughly every 16ms (at 60Hz) via the Choreographer, which posts frame callbacks to the main thread's queue. For smooth UI, each frame's work (measure, layout, draw, plus any of your main-thread code) must complete within that ~16ms budget so the next frame's draw message can be processed on time.","**Jank** happens when a message takes *longer than the frame budget* — say your `onBindViewHolder` or a recomposition does 30ms of work. The draw message for that frame is delayed, the frame is dropped, and the user sees a stutter. It's not a crash, just a missed frame, but repeated jank feels broken.","**ANR** happens when a message blocks the loop for *seconds* — a synchronous network call or a huge database query on the main thread holds the loop, so input events (taps) sit unprocessed in the queue. After ~5 seconds of unresponsiveness to input, the system shows the 'Application Not Responding' dialog. Same mechanism as jank, just a much longer block.","**Why it's all one problem**: jank and ANR are the *same* phenomenon — the Looper being unable to process pending messages promptly — at different severities. A 20ms block drops a frame; a 5s block triggers an ANR."]},{t:"p",text:"**The fix and the framing**: keep every unit of main-thread work *short*. Move anything slow (I/O, computation, large queries) off the main thread with coroutines/dispatchers, so the main thread's Looper only ever handles quick UI operations and can keep processing frames and input on time. This is *why* 'never block the main thread' is drilled so hard — it's not an abstract rule, it's about keeping the message loop flowing so frames render and taps respond. Understanding that jank and ANR are the same Looper-starvation problem at different timescales is the senior-level insight; it also tells you how to *diagnose* them (find the long-running main-thread message via Perfetto/systrace)."}]},{level:"junior",q:"What is the main thread, and what runs on it?",a:[{t:"p",text:"The main thread (a.k.a. the UI thread) is where Android runs your app's UI rendering, lifecycle callbacks, input event handling, and the message loop (`Looper`). Because it's single-threaded and drives the UI, any slow work on it freezes the app. All UI updates *must* happen here; all heavy work must happen *off* it."},{t:"list",items:["**UI + lifecycle + input** — all run on the main thread.","**`Looper`/message queue** — the main thread processes messages continuously.","**Single-threaded** — slow work blocks rendering (jank/ANR).","**Rule** — UI only on main; heavy work off main (coroutines)."]},{t:"note",text:"The main (UI) thread runs UI rendering, lifecycle callbacks, input handling, and the Looper message loop. It's single-threaded, so slow work freezes the UI (jank/ANR). All UI updates must happen here; all heavy/blocking work must go off it (coroutines/executors)."}]},{level:"senior",q:"What is the difference between synchronized, @Volatile, and atomic types?",a:[{t:"p",text:"These solve different aspects of thread safety. `synchronized` provides *mutual exclusion* (a lock) around a block/method so only one thread runs it at a time. `@Volatile` guarantees *visibility* (writes are seen by other threads immediately) but not atomicity. Atomic types (`AtomicInteger`, `AtomicReference`) provide *lock-free atomic operations* (compare-and-set) for single variables."},{t:"code",title:"Three tools",code:`@Synchronized fun update() { shared++ }         // mutual exclusion (lock)
@Volatile var running = true                     // visibility only (flag)
val counter = AtomicInteger(0); counter.incrementAndGet()   // atomic single-var op`},{t:"list",items:["**`synchronized`** — mutual exclusion; for compound critical sections.","**`@Volatile`** — visibility only; for a simple flag (not `count++`).","**Atomics** — lock-free atomic ops on one variable (counters, CAS).","**Coroutines** — prefer `Mutex`/immutable state + `StateFlow.update` in coroutine code."]},{t:"note",text:"synchronized = mutual exclusion (lock, compound sections); @Volatile = visibility only (a flag — count++ is still a race); atomics (AtomicInteger/Reference) = lock-free atomic ops on one variable (CAS). In coroutine code prefer Mutex or immutable state + StateFlow.update over these Java primitives."}]},{level:"junior",q:"What is the POST_NOTIFICATIONS runtime permission (API 33+)?",a:[{t:"p",text:"On Android 13 (API 33), posting notifications requires the `POST_NOTIFICATIONS` *runtime* permission — declare it and request it from the user (previously notifications needed no permission). Without it, your notifications are silently *not shown*. Request in context (when the user enables a notifying feature) and handle denial gracefully."},{t:"code",title:"Notification permission",code:`<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
if (Build.VERSION.SDK_INT >= 33) launcher.launch(Manifest.permission.POST_NOTIFICATIONS)`},{t:"list",items:["**API 33+ runtime permission** — notifications now require user grant.","**Silently dropped** — without it, notifications don't appear.","**Request in context** — when enabling a notifying feature.","**Below 33** — implicitly granted (declare it; no prompt)."]},{t:"note",text:"Android 13 (API 33) made POST_NOTIFICATIONS a runtime permission — declare and request it, or notifications are silently dropped (before 33, none needed). Request in context; handle denial gracefully. On <33 it's implicitly granted."}]},{level:"junior",q:"What is permission auto-reset for unused apps?",a:[{t:"p",text:"Android automatically *revokes* runtime permissions from apps the user hasn't opened in a few months (auto-reset, Android 11+, Play-backported). So a permission you were granted may be *gone* when the user returns — always re-check permissions (never cache 'granted' permanently) and re-request if needed."},{t:"list",items:["**Auto-reset** — unused apps lose runtime permissions after months.","**Re-check every time** — don't assume a past grant still holds.","**Re-request** — as normal if revoked.","**`isAutoRevokeWhitelisted`** — check/prompt to exempt only if justified."]},{t:"note",text:"Android auto-resets (revokes) runtime permissions for apps unused for months (API 11+, Play-backported) — so always re-check permissions each time (never cache 'granted' permanently) and re-request if revoked. Prompt to disable auto-reset only if genuinely justified."}]},{level:"senior",q:"How do you handle 'don't ask again' / permanently denied permissions?",a:[{t:"p",text:"When a user denies a permission and checks 'don't ask again' (or denies twice on modern Android), the system won't show the dialog again — `shouldShowRequestPermissionRationale` returns `false` *and* it's not granted. Detect this state and direct the user to *App Settings* (via an intent to `ACTION_APPLICATION_DETAILS_SETTINGS`) to grant it manually, with an explanation of why it's needed."},{t:"code",title:"Send to settings",code:`if (!granted && !shouldShowRequestPermissionRationale(permission)) {
    // permanently denied -> guide to settings
    startActivity(Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS,
        Uri.fromParts("package", packageName, null)))
}`},{t:"list",items:["**Permanently denied** — not granted AND `shouldShowRationale` false.","**Can't re-prompt** — the system won't show the dialog.","**Guide to settings** — `ACTION_APPLICATION_DETAILS_SETTINGS` with rationale.","**Degrade gracefully** — offer reduced functionality if they decline."]},{t:"note",text:"Permanently denied = not granted AND shouldShowRequestPermissionRationale returns false — the system won't re-prompt. Detect it and send the user to App Settings (ACTION_APPLICATION_DETAILS_SETTINGS) with an explanation. Degrade gracefully if they still decline."}]},{level:"senior",q:"How do location permissions work (foreground, background, coarse/fine)?",a:[{t:"p",text:"Location is tiered: `ACCESS_COARSE_LOCATION` (approximate) and `ACCESS_FINE_LOCATION` (precise) are foreground permissions requested together (users can grant only coarse on Android 12+). `ACCESS_BACKGROUND_LOCATION` is a *separate* request that must come *after* foreground is granted, and routes through settings ('Allow all the time'). Request the minimum you need and only when needed."},{t:"list",items:["**Coarse vs fine** — approximate vs precise; users may grant only coarse (API 31+).","**Foreground first** — request coarse/fine together while in use.","**Background separately** — `ACCESS_BACKGROUND_LOCATION` after foreground, via settings.","**Minimize** — request the least precision/scope needed, in context."]},{t:"note",text:"Location tiers: COARSE (approximate) + FINE (precise) requested together as foreground (users may grant only coarse on API 31+); BACKGROUND_LOCATION is a separate request AFTER foreground, routed through settings ('Allow all the time'). Request the minimum precision/scope, in context, and handle coarse-only grants."}]},{level:"junior",q:"What is the Choreographer, and how does it relate to jank?",a:[{t:"p",text:"The `Choreographer` coordinates the app's work with the display's *vsync* signal — it schedules input, animation, and draw callbacks to run once per frame (~16.6ms at 60Hz). If your main-thread work for a frame exceeds the budget, the frame is *dropped* (jank). Tools like `FrameMetrics` and the Choreographer's frame callbacks help measure per-frame timing."},{t:"list",items:["**Vsync-driven** — schedules per-frame input/animation/draw callbacks.","**Frame budget** — ~16.6ms (60Hz), ~8.3ms (120Hz); exceed it = dropped frame (jank).","**Measure** — `FrameMetrics`, JankStats, Perfetto per-frame timing.","**Cause of jank** — heavy main-thread work stealing the frame budget."]},{t:"note",text:"The Choreographer syncs the app's input/animation/draw work to the display's vsync, once per frame (~16.6ms at 60Hz). Main-thread work exceeding the frame budget drops the frame (jank). Measure with FrameMetrics/JankStats/Perfetto. Jank = heavy main-thread work stealing the frame budget."}]},{level:"junior",q:"What is the difference between blocking and suspending on Android?",a:[{t:"p",text:"A *blocking* call (`Thread.sleep`, synchronous network/disk) *occupies its thread* until it returns — on the main thread that freezes the UI; on a pool thread it ties up a worker. A *suspending* call (`delay`, a suspend network call) *releases the thread* while waiting, so the thread runs other work. Coroutines make waiting non-blocking, which is why they scale and keep the UI responsive."},{t:"list",items:["**Blocking** — holds the thread until done (freezes UI on main; ties up a worker).","**Suspending** — frees the thread while waiting; resumes later.","**`delay` vs `Thread.sleep`** — suspend vs block; use `delay` in coroutines.","**Main-safety** — suspend functions offload blocking internally (withContext)."]},{t:"note",text:"Blocking (Thread.sleep, sync I/O) holds its thread until done (freezes the UI on main, ties up a worker). Suspending (delay, suspend I/O) releases the thread while waiting and resumes later. Use delay over Thread.sleep in coroutines; suspend functions offload blocking work internally (main-safety)."}]},{level:"junior",q:"What thread does UI work happen on, and how do you get back to it?",a:[{t:"p",text:"All UI updates must happen on the main thread — touching a View from a background thread throws `CalledFromWrongThreadException`. To get back to the main thread: with coroutines, `withContext(Dispatchers.Main)` (or just launch on `Dispatchers.Main`); with Views, `runOnUiThread { }`, `view.post { }`, or a main-thread `Handler`. With `StateFlow`/Compose, update state from any thread and let the UI collect on main."},{t:"list",items:["**UI on main only** — off-thread View access throws.","**Coroutines** — `withContext(Dispatchers.Main)` / launch on Main.","**Views** — `runOnUiThread`, `view.post`, main `Handler`.","**StateFlow/Compose** — update state anywhere, collect on main."]},{t:"note",text:"UI updates must be on the main thread (off-thread View access throws CalledFromWrongThreadException). Get back via coroutines (withContext(Dispatchers.Main)), or Views (runOnUiThread/view.post/main Handler). With StateFlow/Compose, update state from any thread and let the UI collect on main."}]},{level:"senior",q:"What is the difference between concurrency approaches: Thread, Executor, Coroutines?",a:[{t:"p",text:"Raw `Thread` is low-level (manual creation, no pooling, easy to leak). `ExecutorService`/`ThreadPoolExecutor` pool threads and queue tasks (better resource use, callbacks/Futures). *Coroutines* are the modern default: lightweight (multiplexed on a pool), structured (lifecycle-scoped cancellation), sequential-looking, with easy thread switching. Use coroutines for app code; executors when integrating Java libraries; raw threads almost never."},{t:"table",headers:["","Thread","Executor","Coroutines"],rows:[["Pooling","no","yes","yes (dispatchers)"],["Cancellation","manual","Future.cancel","structured/automatic"],["Style","callbacks","Futures/callbacks","sequential"],["Weight","heavy","medium","light"]]},{t:"list",items:["**`Thread`** — low-level, no pooling; avoid directly.","**`ExecutorService`** — pooled, queued; Java-friendly.","**Coroutines** — lightweight, structured, sequential; the modern default.","**Interop** — `Executor.asCoroutineDispatcher()` bridges them."]},{t:"note",text:"Thread: low-level, no pooling (avoid). Executor/ThreadPoolExecutor: pooled, queued, Java-friendly (Futures). Coroutines: lightweight (multiplexed), structured (lifecycle cancellation), sequential — the modern default for app code. Use executors for Java-library interop (Executor.asCoroutineDispatcher bridges), raw threads almost never."}]},{level:"junior",q:"How do you declare and check a permission in the manifest and code?",a:[{t:"p",text:'Declare every permission with `<uses-permission android:name="..."/>` in the manifest (both install-time and runtime). For runtime permissions, also check at runtime with `ContextCompat.checkSelfPermission(context, permission) == PERMISSION_GRANTED` before using the protected API, and request it if not granted.'},{t:"code",title:"Declare + check",code:`<uses-permission android:name="android.permission.CAMERA" />
// In code:
if (ContextCompat.checkSelfPermission(context, Manifest.permission.CAMERA)
    == PackageManager.PERMISSION_GRANTED) { useCamera() }`},{t:"list",items:["**Manifest** — `<uses-permission>` for every permission.","**`checkSelfPermission`** — runtime check before using the API.","**Request if missing** — via the Activity Result API.","**Handle revocation** — permissions can be revoked while the app runs."]},{t:"note",text:"Declare all permissions with <uses-permission> in the manifest. For runtime permissions, check ContextCompat.checkSelfPermission(...) == PERMISSION_GRANTED before using the protected API, and request via the Activity Result API if missing. Permissions can be revoked at runtime — always check, don't assume."}]},{level:"senior",q:"What is scoped storage, and how did it change file access permissions?",a:[{t:"p",text:"Scoped storage (Android 10+) restricts broad file access: apps get their own sandboxed directory (no permission needed) and access to *media* via `MediaStore` (with granular media permissions on API 33+: `READ_MEDIA_IMAGES/VIDEO/AUDIO`), while `READ/WRITE_EXTERNAL_STORAGE` is largely deprecated. For arbitrary files, use the Storage Access Framework (user picks). The broad `MANAGE_EXTERNAL_STORAGE` is heavily restricted on Play."},{t:"list",items:["**App-specific storage** — no permission for your own sandboxed dir.","**Media via `MediaStore`** — granular `READ_MEDIA_*` permissions (API 33+).","**`READ/WRITE_EXTERNAL_STORAGE` deprecated** — broad access gone.","**SAF for arbitrary files** — user picks; `MANAGE_EXTERNAL_STORAGE` Play-restricted."]},{t:"note",text:"Scoped storage (Android 10+): apps get a permission-free sandboxed dir and media via MediaStore (granular READ_MEDIA_IMAGES/VIDEO/AUDIO on API 33+); READ/WRITE_EXTERNAL_STORAGE is largely deprecated. Use SAF for arbitrary files (user picks). MANAGE_EXTERNAL_STORAGE (all files) is heavily Play-restricted."}]},{level:"senior",q:"What are HandlerThread and the main-thread message barrier?",a:[{t:"p",text:"A `HandlerThread` is a thread with its own `Looper`, so you can post work to it via a `Handler` and it runs serially off the main thread — useful for a dedicated background worker with ordered execution (e.g. sensor/camera callbacks). It's a lower-level tool; coroutines with a single-threaded dispatcher usually replace it in modern code."},{t:"list",items:["**`HandlerThread`** — a background thread with a `Looper`; post via a `Handler`.","**Serial execution** — ordered work off the main thread.","**Uses** — dedicated worker for camera/sensor callbacks, ordered background tasks.","**Modern** — a single-threaded coroutine dispatcher usually replaces it."]},{t:"note",text:"A HandlerThread is a background thread with its own Looper — post work via a Handler for serial off-main execution (dedicated worker for camera/sensor callbacks, ordered tasks). Lower-level; a single-threaded coroutine dispatcher (limitedParallelism(1)) usually replaces it in modern code."}]},{level:"junior",q:"What is the difference between a permission group and an individual permission?",a:[{t:"p",text:"Dangerous permissions belong to *groups* (Location, Contacts, Camera, etc.). Historically, granting one permission in a group auto-granted others in it, but modern Android requires requesting each permission and shows per-permission dialogs. You still declare and request individual permissions (e.g. `ACCESS_FINE_LOCATION`), and the group mainly affects how the system presents them."},{t:"list",items:["**Permission groups** — Location, Contacts, Camera, Microphone, etc.","**Request individual permissions** — declare/request each specific one.","**Per-permission dialogs** — modern Android prompts individually.","**Group context** — affects settings grouping and presentation."]},{t:"note",text:"Dangerous permissions belong to groups (Location/Contacts/Camera…). Modern Android requires requesting each individual permission (e.g. ACCESS_FINE_LOCATION) with per-permission dialogs; the group mainly affects how the system groups/presents them in settings. Don't assume one grant covers a group."}]},{level:"senior",q:"How do you switch threads with coroutines instead of Handlers?",a:[{t:"p",text:"Instead of posting between threads with `Handler`s, use `withContext(dispatcher)` to move a block of work: `withContext(Dispatchers.IO) { }` for blocking I/O, `Dispatchers.Default` for CPU work, and the coroutine automatically returns to its original (Main) dispatcher after. This replaces the error-prone Handler-posting pattern with clean sequential code."},{t:"code",title:"withContext replaces Handler posting",code:`viewModelScope.launch {                     // Main
    val data = withContext(Dispatchers.IO) { loadFromDisk() }   // background
    render(data)                            // back on Main automatically
}`},{t:"list",items:["**`withContext(IO/Default)`** — move a block to a background dispatcher.","**Auto-return** — resumes on the original dispatcher (Main) after.","**No `Handler` juggling** — sequential, readable.","**Main-safe repos** — push `withContext` into the data layer."]},{t:"note",text:"Use withContext(Dispatchers.IO/Default) to move a block off main; the coroutine auto-returns to its original (Main) dispatcher after — replacing Handler.post thread-switching with clean sequential code. Push withContext into main-safe repository functions so the ViewModel stays on Main."}]},{level:"junior",q:"What is a race condition, and how do you avoid it on Android?",a:[{t:"p",text:"A race condition happens when two threads access shared mutable state concurrently and the result depends on timing — causing lost updates or corruption (e.g. two threads incrementing a counter). Avoid it by: confining state to one thread (main thread / single dispatcher), using atomics (`AtomicInteger`) or a `Mutex` for critical sections, or — best — using immutable state updated atomically (`StateFlow.update`)."},{t:"list",items:["**Race** — concurrent access to shared mutable state; timing-dependent bugs.","**Confinement** — keep state on one thread (main / single dispatcher).","**Atomics/Mutex** — for shared counters/critical sections.","**Immutable + atomic update** — `StateFlow.update { it.copy() }` (preferred)."]},{t:"note",text:"A race condition = concurrent access to shared mutable state with timing-dependent results (lost updates/corruption). Avoid via thread confinement (single thread/dispatcher), atomics (AtomicInteger)/Mutex for critical sections, or best: immutable state updated atomically (StateFlow.update { it.copy() })."}]},{level:"senior",q:"What special permissions require a settings-screen grant rather than a dialog?",a:[{t:"p",text:"Some high-impact permissions can't be granted via the normal dialog — the user must toggle them in a dedicated Settings screen. Examples: `SYSTEM_ALERT_WINDOW` (draw over other apps), `MANAGE_EXTERNAL_STORAGE` (all files access), `SCHEDULE_EXACT_ALARM` (exact alarms on API 31+), `POST_NOTIFICATIONS` is a normal runtime one but notification-listener access is special, and 'ignore battery optimizations'. You send the user there with a specific settings Intent."},{t:"list",items:["**`SYSTEM_ALERT_WINDOW`** — overlay; `ACTION_MANAGE_OVERLAY_PERMISSION`.","**`MANAGE_EXTERNAL_STORAGE`** — all files; Play-restricted.","**`SCHEDULE_EXACT_ALARM`** — exact alarms (API 31+).","**Route via Intent** — a specific settings screen, not a runtime dialog."]},{t:"note",text:"Special/high-impact permissions use a Settings-screen toggle, not a dialog: SYSTEM_ALERT_WINDOW (overlay), MANAGE_EXTERNAL_STORAGE (all files, Play-restricted), SCHEDULE_EXACT_ALARM (API 31+), ignore-battery-optimizations. Route the user via a specific settings Intent (e.g. ACTION_MANAGE_OVERLAY_PERMISSION)."}]},{level:"senior",q:"What is thread starvation, and how can coroutine dispatchers cause it?",a:[{t:"p",text:"Thread starvation is when work can't proceed because all threads in a pool are occupied (often blocked). It happens if you run *blocking* code on a dispatcher not meant for it — e.g. blocking I/O on `Dispatchers.Default` (few, core-count threads), so a burst of blocking calls exhausts them and other CPU work stalls. Fix by using `Dispatchers.IO` for blocking work (or `limitedParallelism`), and never blocking a scarce pool."},{t:"list",items:["**Starvation** — no free thread to run ready work.","**Cause** — blocking calls on a small pool (`Default`) tie up its threads.","**Fix** — blocking I/O on `Dispatchers.IO` (large pool); CPU on `Default`.","**Bound** — `limitedParallelism` to cap concurrency for a resource."]},{t:"note",text:"Thread starvation = no free thread to run ready work, often because blocking calls occupy a small pool. Blocking I/O on Dispatchers.Default (core-count threads) can starve CPU work. Fix: run blocking work on Dispatchers.IO (large pool), CPU on Default; use limitedParallelism to bound a resource without starving."}]},{level:"junior",q:"How do you explain to the user why you need a permission (rationale)?",a:[{t:"p",text:"Show a *rationale* — a brief in-app explanation of *why* the feature needs the permission — *before* (or after a first denial of) the system dialog, gated by `shouldShowRequestPermissionRationale`. Request permissions *in context* (when the user triggers the feature), not upfront at launch, so the reason is obvious and grant rates are higher."},{t:"list",items:["**Rationale UI** — explain the benefit before the system prompt.","**`shouldShowRequestPermissionRationale`** — true after a denial (or to preempt).","**In-context requests** — ask when the feature is used, not at launch.","**Graceful denial** — offer reduced functionality if declined."]},{t:"note",text:"Show a rationale (why the feature needs the permission) before/after-first-denial, gated by shouldShowRequestPermissionRationale, and request in context (when the user triggers the feature) — not upfront at launch. Contextual requests with clear rationale get higher grant rates; degrade gracefully on denial."}]},{level:"senior",q:"How does structured concurrency prevent the leaks that plagued AsyncTask?",a:[{t:"p",text:"`AsyncTask` leaked because it wasn't tied to a lifecycle — an inner-class task held the Activity and kept running after it was destroyed. Structured concurrency fixes this: coroutines launched in `viewModelScope`/`lifecycleScope` are *owned* by that scope and *cancelled* when it ends, so the work stops and references are released. You can't accidentally leave orphaned work holding a dead screen."},{t:"list",items:["**AsyncTask leak** — inner class holding the Activity, no lifecycle cancellation.","**Scoped coroutines** — owned by `viewModelScope`/`lifecycleScope`.","**Auto-cancellation** — work stops when the scope ends; references freed.","**No orphans** — structure prevents work outliving its owner."]},{t:"note",text:"AsyncTask leaked because it wasn't lifecycle-tied (inner class holding the Activity, running past destruction). Structured concurrency: coroutines in viewModelScope/lifecycleScope are owned by the scope and cancelled when it ends, so work stops and references are freed — no orphaned work holding a dead screen."}]},{level:"junior",q:"Which dispatcher should you use for network, database, and image processing?",a:[{t:"p",text:"Match the dispatcher to the workload: `Dispatchers.IO` for *waiting* work (network, disk, database reads) — it has many threads for concurrent blocking calls; `Dispatchers.Default` for *CPU-bound* work (image processing, JSON parsing, sorting) — sized to CPU cores; `Dispatchers.Main` only for UI updates. Room/Retrofit suspend functions are already main-safe, so you don't wrap those."},{t:"table",headers:["Work","Dispatcher"],rows:[["Network / disk / DB (waiting)","IO"],["Image/JSON/CPU computation","Default"],["UI updates","Main"]]},{t:"list",items:["**`IO`** — waiting work (network/disk/DB); large pool.","**`Default`** — CPU work (image/parse/sort); core-count pool.","**`Main`** — UI only.","**Don't wrap main-safe libs** — Room/Retrofit suspend functions already switch."]},{t:"note",text:"IO for waiting work (network/disk/DB — large pool), Default for CPU-bound work (image/JSON/sort — core-count pool), Main for UI. Room/Retrofit suspend functions are already main-safe (don't wrap them). Wrong dispatcher causes starvation/thrash."}]},{level:"senior",q:"How do you test permission-gated flows?",a:[{t:"p",text:"Abstract permission checking behind an interface (e.g. `PermissionChecker`) that you can *fake* in tests — so unit/ViewModel tests don't touch the real permission system. For instrumented tests, use `GrantPermissionRule` to grant permissions automatically, or UIAutomator to interact with the system dialog. Testing through an abstraction keeps logic tests fast and deterministic."},{t:"code",title:"Testable permission checking",code:`interface PermissionChecker { fun has(permission: String): Boolean }
// Test: FakePermissionChecker(granted = false) -> assert the 'request' path

// Instrumented:
@get:Rule val grant = GrantPermissionRule.grant(Manifest.permission.CAMERA)`},{t:"list",items:["**Abstract behind an interface** — fake it in unit/ViewModel tests.","**`GrantPermissionRule`** — auto-grant for instrumented tests.","**UIAutomator** — interact with the real system dialog if needed.","**Deterministic** — the abstraction avoids depending on device state."]},{t:"note",text:"Abstract permission checks behind an interface (PermissionChecker) to fake in unit/ViewModel tests (assert request vs use paths). For instrumented tests, GrantPermissionRule auto-grants, or UIAutomator drives the system dialog. The abstraction keeps logic tests fast and deterministic."}]},{level:"junior",q:"What are the camera and microphone privacy indicators (Android 12+)?",a:[{t:"p",text:"Android 12 added *privacy indicators* — a small dot in the status bar (green) that appears whenever an app is *actively using* the camera or microphone, plus toggles in Quick Settings to disable camera/mic access globally. This means users can see and cut off access in real time, so your app should only access these sensors while genuinely needed and handle the case where access is toggled off."},{t:"list",items:["**Indicator dot** — shows active camera/mic use.","**Quick Settings toggles** — user can disable camera/mic globally.","**Handle revocation** — access can be cut off mid-use.","**Use minimally** — access sensors only when needed; release promptly."]},{t:"note",text:"Android 12+ privacy indicators show a status-bar dot when an app actively uses the camera/mic, plus Quick Settings toggles to disable them globally. Users see and can cut off access in real time — access these sensors only when needed, release promptly, and handle mid-use revocation gracefully."}]},{level:"senior",q:"How do you handle runtime permissions in a Compose screen?",a:[{t:"p",text:"Use `rememberLauncherForActivityResult(RequestPermission())` for the raw API, or Accompanist Permissions' `rememberPermissionState(permission)` / `rememberMultiplePermissionsState(...)` which expose the permission *status* (granted, denied, needs-rationale) as observable state you drive UI from. Request in response to a user action, show rationale, and route to settings if permanently denied."},{t:"code",title:"Compose permission state",code:`val cameraState = rememberPermissionState(Manifest.permission.CAMERA)
when {
    cameraState.status.isGranted -> CameraUi()
    cameraState.status.shouldShowRationale -> RationaleUi { cameraState.launchPermissionRequest() }
    else -> Button(onClick = { cameraState.launchPermissionRequest() }) { Text("Enable camera") }
}`},{t:"list",items:["**`rememberLauncherForActivityResult`** — the raw Result API in Compose.","**Accompanist `rememberPermissionState`** — status as observable state.","**Branch on status** — granted / needs-rationale / denied.","**Request on action** — plus rationale and settings-redirect for permanent denial."]},{t:"note",text:"Compose: rememberLauncherForActivityResult(RequestPermission()) for the raw API, or Accompanist rememberPermissionState/rememberMultiplePermissionsState exposing status (granted/denied/shouldShowRationale) as observable state. Branch UI on status, request on a user action, show rationale, and route to settings on permanent denial."}]}],vA={id:"threading-permissions",title:"Threading, Handler/Looper & Permissions",tagline:"The single-threaded UI rule, Handler/Looper/MessageQueue, threading evolution (AsyncTask → coroutines), and the runtime permission model.",tags:["Android","Main Thread","Handler","Looper","Permissions","ANR"],content:yA,qa:wA},bA={id:"android-core",name:"Android Core",description:"The Android framework itself — Activity & Fragment lifecycles, Intents/tasks/launch modes, Services/Receivers/Providers, Context & processes, threading, and permissions.",topics:[nA,rA,dA,pA,gA,vA]},kA=[{heading:"SQLite and why Room exists",blocks:[{t:"p",text:"**SQLite** is a lightweight, embedded relational database built into Android — every app can use it for structured local storage, with no server. You *could* use it directly through the raw `SQLiteOpenHelper`/`SQLiteDatabase` APIs, but that means writing SQL as strings, manually mapping `Cursor` rows to objects, and getting no compile-time checking — error-prone and verbose. **Room** is a Jetpack library that sits on top of SQLite as an **ORM (Object-Relational Mapping)** layer: you define your schema with annotated Kotlin classes, write queries as annotated methods, and Room generates the boilerplate with *compile-time verification* of your SQL."},{t:"list",items:["**Room's headline benefit — compile-time SQL verification**: your `@Query` SQL is checked against your schema *when you build*, so a typo in a column name or a wrong type is a compile error, not a runtime crash. Raw SQLite would only fail at runtime.","**Less boilerplate**: no manual `Cursor` iteration, no manual object mapping — Room maps query results to your objects automatically.","**Reactive & coroutine support built in**: queries can return `Flow` (auto-updating), `suspend` functions (main-safe), LiveData — first-class integration with modern Android.","Room is the recommended way to use a database on Android; raw SQLite is legacy/niche."]}]},{heading:"The three Room components: Entity, DAO, Database",blocks:[{t:"code",title:"The complete minimal setup",code:`// 1) ENTITY — a table, defined by a data class
@Entity(tableName = "users")
data class UserEntity(
    @PrimaryKey val id: String,
    @ColumnInfo(name = "full_name") val name: String,
    val email: String,
    val createdAt: Long,
)

// 2) DAO — Data Access Object: your queries as annotated methods
@Dao
interface UserDao {
    @Query("SELECT * FROM users ORDER BY createdAt DESC")
    fun observeUsers(): Flow<List<UserEntity>>          // reactive, auto-updates

    @Query("SELECT * FROM users WHERE id = :id")
    suspend fun getUser(id: String): UserEntity?        // suspend = main-safe

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun upsert(user: UserEntity)

    @Delete
    suspend fun delete(user: UserEntity)
}

// 3) DATABASE — ties entities and DAOs together
@Database(entities = [UserEntity::class], version = 1)
abstract class AppDatabase : RoomDatabase() {
    abstract fun userDao(): UserDao
}`},{t:"list",items:["**`@Entity`** — a class mapped to a *table*; each property is a *column*. `@PrimaryKey` marks the unique key (can be `autoGenerate = true`); `@ColumnInfo` customizes column names; `@Ignore` excludes a property.","**`@Dao`** — an interface of database operations. `@Query` for SQL (verified at compile time), `@Insert`/`@Update`/`@Delete`/`@Upsert` for convenience operations. Methods can be `suspend` (one-shot, main-safe) or return `Flow`/`LiveData` (observable).","**`@Database`** — an abstract class listing the entities and exposing the DAOs, with a `version` number (critical for migrations). You build one instance (a singleton) with `Room.databaseBuilder(...)`."]}]},{heading:"Reactive queries with Flow — Room as the source of truth",blocks:[{t:"p",text:"The most powerful Room feature: a `@Query` returning **`Flow<T>`** *automatically re-emits whenever the underlying data changes*. Room tracks which tables a query touches and, when any write modifies those tables, it re-runs the query and emits the new result to all collectors. This is what makes Room the ideal **single source of truth** — the UI observes the database, writes go into the database, and the UI updates automatically."},{t:"code",title:"The observe-local, write-anywhere pattern",code:`// Repository: UI observes the DB; network writes INTO the DB
class UserRepository(private val dao: UserDao, private val api: Api) {
    // Reads always come from the DB (single source of truth)
    fun observeUsers(): Flow<List<User>> =
        dao.observeUsers().map { it.map(UserEntity::toDomain) }

    // Refresh writes into the DB; the Flow above re-emits automatically
    suspend fun refresh() {
        val remote = api.getUsers()
        dao.upsertAll(remote.map { it.toEntity() })   // triggers Flow re-emission
    }
}`},{t:"list",items:["**Why it's powerful**: multiple screens observing the same query all update in the same frame when a write happens — no manual notification, no event bus. Offline works because reads always come from the DB.","**suspend vs Flow**: use `suspend` for one-shot operations (insert, a single lookup); use `Flow` for data the UI should *observe* over time. A `suspend` query runs once and returns; a `Flow` query keeps emitting.","**Room handles threading**: `suspend` DAO functions and `Flow` queries are main-safe — Room runs the actual query on a background executor. You don't need to wrap them in `withContext(IO)` (doing so is redundant)."]}]},{heading:"Transactions and atomicity",blocks:[{t:"p",text:"A **transaction** groups multiple database operations so they happen **atomically** — all succeed together or all roll back together, with no partial state visible to other queries. Use `@Transaction` when a logical operation spans several writes/reads that must be consistent."},{t:"code",title:"@Transaction for multi-step consistency",code:`@Dao
interface OrderDao {
    @Transaction
    suspend fun replaceCart(items: List<CartItem>) {
        clearCart()              // delete old
        insertAll(items)         // insert new
        // If insertAll fails, clearCart is rolled back too — atomic
    }
}`},{t:"list",items:["**Atomicity**: without a transaction, a crash between `clearCart()` and `insertAll()` would leave an empty cart. With `@Transaction`, either both happen or neither does.","**Consistency for reads**: `@Transaction` on a query method ensures the reads within it see a consistent database snapshot — important for relation queries (`@Relation`) that do multiple reads, so they don't see the DB change mid-way.","**Performance**: batching many inserts in one transaction is *much* faster than many individual inserts (each insert is otherwise its own implicit transaction with fsync overhead). Inserting 1000 rows in a transaction can be orders of magnitude faster."]}]},{heading:"Building and configuring the database",blocks:[{t:"code",title:"Creating the singleton (usually via DI)",code:`val db = Room.databaseBuilder(
    context.applicationContext,       // application context — it's app-lived
    AppDatabase::class.java,
    "app.db",
)
    .fallbackToDestructiveMigration() // dev-only: wipe on schema change (DON'T ship)
    .build()`},{t:"list",items:["**Make it a singleton**: opening a database is expensive and multiple instances cause conflicts — build one instance for the whole app (provide it via Hilt/DI as `@Singleton`).","**Use the application context**: the database outlives any Activity, so it must not hold an Activity context.","**Never do DB work on the main thread**: Room *enforces* this — a non-suspend, non-Flow query on the main thread throws (unless you explicitly `allowMainThreadQueries()`, which you shouldn't). Use `suspend`/`Flow`.","**Type converters**: SQLite only stores primitives/String/blob. For custom types (a `Date`, an enum, a list), you write a `@TypeConverter` telling Room how to convert to/from a storable type (`Date` ↔ `Long`)."]},{t:"note",text:"Room essentials: it's a compile-time-verified ORM over SQLite. Three parts — @Entity (table), @Dao (queries), @Database (config + version). Flow queries auto-update, making Room the single source of truth (observe local, write into it). suspend/Flow queries are main-safe. @Transaction gives atomicity and speeds up batch writes. Build one singleton with the application context; never query on the main thread."}]}],xA=[{level:"junior",q:"What is Room and why use it instead of raw SQLite?",a:[{t:"p",text:"**Room is a Jetpack library that acts as an ORM (object-relational mapping) layer over SQLite.** SQLite is the embedded database built into Android, but using it directly means writing SQL as raw strings, manually iterating `Cursor` results and mapping them to objects, and getting no compile-time safety. Room lets you define your schema with annotated Kotlin classes and your queries as annotated methods, and it generates all that boilerplate for you."},{t:"list",items:["**Compile-time SQL verification** — the headline benefit. Room checks your `@Query` SQL against your schema *at build time*, so a misspelled column or wrong type is a compile error, not a runtime crash. Raw SQLite fails only at runtime.","**Less boilerplate** — no manual `Cursor` handling or object mapping; Room maps rows to your objects automatically.","**Reactive & coroutine support** — queries can return `Flow` (auto-updating when data changes), be `suspend` (main-safe), or return LiveData, integrating cleanly with modern Android."]},{t:"p",text:"Room is the recommended approach for local databases on Android; you'd only touch raw SQLite for unusual cases. The three building blocks are `@Entity` (a table), `@Dao` (your queries), and `@Database` (which ties them together and holds the version number)."}]},{level:"junior",q:"What are the three main components of Room?",a:[{t:"list",items:["**`@Entity`** — a Kotlin class mapped to a database *table*, where each property is a column. You mark the primary key with `@PrimaryKey`, and can customize column names with `@ColumnInfo`. This defines your schema.","**`@Dao`** (Data Access Object) — an interface declaring your database operations as annotated methods: `@Query` for SQL queries (verified at compile time), and `@Insert`/`@Update`/`@Delete`/`@Upsert` for common operations. Methods can be `suspend` (one-shot) or return `Flow`/`LiveData` (observable).","**`@Database`** — an abstract class that lists all your entities, exposes the DAOs, and carries a `version` number (used for migrations). You build a single instance of it with `Room.databaseBuilder`."]},{t:"code",title:"The three together",code:`@Entity data class User(@PrimaryKey val id: String, val name: String)

@Dao interface UserDao {
    @Query("SELECT * FROM User") fun observeAll(): Flow<List<User>>
    @Insert suspend fun insert(user: User)
}

@Database(entities = [User::class], version = 1)
abstract class AppDb : RoomDatabase() { abstract fun userDao(): UserDao }`},{t:"p",text:"Mental model: Entity = *what* you store (the table), DAO = *how* you access it (the queries), Database = the *container* that wires them together and versions the schema. You typically build one Database singleton and provide the DAOs through it via dependency injection."}]},{level:"junior",q:"What happens when a Room query returns a Flow?",a:[{t:"p",text:"**The Flow automatically re-emits a fresh result whenever the underlying data changes.** Room tracks which tables a query reads from; when *any* write modifies one of those tables (an insert, update, or delete), Room re-runs the query and emits the new result to every active collector. So a `Flow<List<User>>` from a query keeps your UI in sync with the database without any manual refresh."},{t:"p",text:"This is what makes Room the ideal **single source of truth**. The pattern is: the UI *observes* a Room Flow, and all writes (including network responses) go *into* the database. When you refresh from the network and write the results to Room, the Flow re-emits automatically and the UI updates — you never manually push data to the screen. Multiple screens observing the same query all update together. It also means offline works naturally, since reads always come from the local database. Contrast this with a `suspend` query, which runs *once* and returns a single result — you use `suspend` for one-shot operations (a single lookup, an insert) and `Flow` for data the UI should track over time."}]},{level:"junior",q:"Can you run Room queries on the main thread?",a:[{t:"p",text:"**No — and Room actively enforces this.** By default, if you try to run a synchronous (non-suspend, non-Flow) query on the main thread, Room throws an `IllegalStateException` telling you not to. This is because database operations involve disk I/O, which can be slow and would block the UI thread, causing jank or an ANR."},{t:"p",text:"The correct approach is to make your DAO methods either `suspend` functions (for one-shot operations) or return `Flow`/`LiveData` (for observable queries). Both are **main-safe** — Room automatically runs the actual database work on a background executor and delivers results appropriately, so you can call them from a main-thread coroutine (like `viewModelScope.launch`) without any manual threading. You do *not* need to wrap them in `withContext(Dispatchers.IO)` — Room already handles the threading, so that wrapping is redundant. There is an escape hatch, `allowMainThreadQueries()`, but you should essentially never use it; it exists for niche cases and defeats the safety Room provides."}]},{level:"senior",q:"What is a Room transaction and when do you need @Transaction?",a:[{t:"p",text:"**A transaction groups multiple database operations so they execute atomically — all of them succeed together, or all roll back together, with no partially-applied state ever visible.** You mark a DAO method `@Transaction`, and Room wraps its operations in a single SQLite transaction."},{t:"list",items:["**Atomicity — the primary reason**: when a logical operation spans several writes that must be consistent. Example: replacing a cart means deleting old items *and* inserting new ones. Without a transaction, a crash or error between the delete and insert leaves an empty cart. With `@Transaction`, if the insert fails, the delete is rolled back too — the cart is never left in a broken intermediate state.","**Read consistency**: `@Transaction` on a query method ensures all reads within it see a *consistent snapshot* of the database. This matters especially for `@Relation` queries, which internally do multiple reads (one for the parent, one for the children) — without a transaction, a concurrent write could change the data between those reads, giving an inconsistent combined result. Room actually requires/recommends `@Transaction` on relation queries for this reason.","**Performance — a big one**: batching many writes in a single transaction is dramatically faster than doing them individually. Each standalone insert is its own implicit transaction with its own disk-sync (fsync) overhead. Wrapping 1000 inserts in one transaction turns 1000 syncs into one, which can be orders of magnitude faster. So `@Transaction` is both a correctness *and* a performance tool for bulk operations."]},{t:"p",text:"**The senior framing**: reach for `@Transaction` whenever (a) multiple operations must be all-or-nothing, (b) a read must see a consistent snapshot across multiple internal queries (relations), or (c) you're doing bulk writes and want the performance of a single sync. It's the database-level guarantee that your multi-step operations don't leave corrupt or inconsistent state — the same 'atomic' guarantee databases are built to provide."}]},{level:"senior",q:"How does Room make the database a 'single source of truth', and why is that valuable?",a:[{t:"p",text:"**Room enables the single-source-of-truth (SSOT) pattern through its auto-updating Flow queries: you designate the database as the one place the app reads from, and everything — including network data — flows *into* it, so the UI is always a reflection of the local database.** The mechanism is Room's table-change tracking: a `Flow` query re-emits whenever its tables are written, so writing fresh network data to the DB automatically propagates to every observer."},{t:"code",title:"The SSOT repository pattern",code:`class ArticleRepository(private val dao: ArticleDao, private val api: Api) {
    // UI observes the DB — the single source of truth
    fun observeArticles(): Flow<List<Article>> =
        dao.observeArticles().map { it.map(ArticleEntity::toDomain) }

    // Network writes INTO the DB; observers update automatically
    suspend fun refresh() {
        val fresh = api.getArticles()
        dao.upsertAll(fresh.map { it.toEntity() })   // Flow above re-emits
    }
}`},{t:"list",items:["**Consistency across the app**: every screen observing the same query shows the *same* data and updates *together* when it changes — no screen can drift out of sync, because there's exactly one source. This eliminates a whole class of 'screen A shows the new value but screen B still shows the old' bugs.","**Offline-first for free**: since reads *always* come from the local DB, the app works offline by default — the network is just a mechanism to *update* the DB, not a dependency for *displaying* data. When offline, the user sees the last-cached data; when back online, a refresh updates the DB and the UI follows.","**Decoupled refresh**: writing data and displaying data are separate concerns. A pull-to-refresh, a push notification, or a background sync can all write to the DB from anywhere, and the UI updates automatically — no screen-to-screen communication, no event bus, no manual 'notify the UI' code.","**Simpler mental model**: the UI is a pure function of the database (`UI = f(DB)`), which is easy to reason about and test — you can verify behavior by writing to the DB and asserting emissions."]},{t:"p",text:"**Why it's valuable overall**: SSOT with Room turns 'keep the UI, the cache, and the network in sync' — a notoriously bug-prone coordination problem — into a one-directional flow where the database is authoritative and everything observes it. It's the foundation of Google's recommended offline-first architecture, and it's why Room's reactive Flow queries are considered its most important feature, not just a convenience."}]},{level:"junior",q:"What is an @Entity, and how do you define a table?",a:[{t:"p",text:"An `@Entity` is a Kotlin data class annotated to map to a *database table* — each property becomes a column, and you mark a `@PrimaryKey`. Room generates the `CREATE TABLE` and column mapping from it. Annotations like `@ColumnInfo`, `@Ignore`, and `tableName` customize the mapping."},{t:"code",title:"An entity",code:`@Entity(tableName = "users")
data class User(
    @PrimaryKey val id: String,
    @ColumnInfo(name = "full_name") val name: String,
    val age: Int,
    @Ignore val transientField: String = "",   // not persisted
)`},{t:"list",items:["**`@Entity`** — maps a class to a table (properties → columns).","**`@PrimaryKey`** — the unique key (`autoGenerate = true` for auto ids).","**`@ColumnInfo`** — custom column name/type.","**`@Ignore`** — a field not stored; `tableName`/`indices`/`foreignKeys` in the annotation."]},{t:"note",text:"An @Entity data class maps to a table — properties become columns, with a @PrimaryKey (autoGenerate for auto ids). Customize via @ColumnInfo (column name), @Ignore (not persisted), and @Entity's tableName/indices/foreignKeys. Room generates the schema from it."}]},{level:"junior",q:"What are the DAO annotations (@Query, @Insert, @Update, @Delete)?",a:[{t:"p",text:'A `@Dao` interface declares database operations. `@Query("...")` runs arbitrary SQL (with compile-time verification); `@Insert`, `@Update`, `@Delete` generate the SQL for you from entity parameters. DAO functions can be `suspend` (one-shot) or return a `Flow` (observable).'},{t:"code",title:"A DAO",code:`@Dao
interface UserDao {
    @Query("SELECT * FROM users WHERE id = :id")
    suspend fun getById(id: String): User?

    @Query("SELECT * FROM users ORDER BY name")
    fun observeAll(): Flow<List<User>>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(user: User)

    @Update suspend fun update(user: User)
    @Delete suspend fun delete(user: User)
}`},{t:"list",items:["**`@Query`** — arbitrary SQL, verified at compile time.","**`@Insert`/`@Update`/`@Delete`** — generated SQL from entity params.","**`suspend`** — one-shot; **`Flow`** — observable stream.","**Return values** — `@Insert` returns rowId(s); `@Update`/`@Delete` return affected count."]},{t:"note",text:"A @Dao declares operations: @Query (arbitrary compile-verified SQL), @Insert/@Update/@Delete (generated from entity params). Functions are suspend (one-shot) or Flow-returning (observable). @Insert returns rowId(s); @Update/@Delete return the affected row count."}]},{level:"junior",q:"What are the @Insert onConflict strategies?",a:[{t:"p",text:"`@Insert(onConflict = ...)` decides what happens when inserting a row that violates a unique/primary-key constraint. `REPLACE` deletes the old row and inserts the new (upsert-like, but it *deletes* — triggering foreign-key cascades and losing the old rowId); `IGNORE` keeps the existing row and skips the new one; `ABORT` (default) rolls back with an exception."},{t:"list",items:["**`REPLACE`** — replace the conflicting row (delete + insert; watch cascades/rowId change).","**`IGNORE`** — keep the existing, skip the new insert.","**`ABORT` (default)** — throw and roll back the transaction.","**Prefer `@Upsert`** — for a true insert-or-update without the delete side effects."]},{t:"note",text:"@Insert onConflict: REPLACE (delete old + insert new — beware FK cascades and changed rowId), IGNORE (keep existing, skip new), ABORT (default — throw and roll back). For a true insert-or-update without REPLACE's delete side effects, use @Upsert."}]},{level:"junior",q:"What is @Upsert, and how does it differ from @Insert(REPLACE)?",a:[{t:"p",text:"`@Upsert` (Room 2.5+) *inserts if new, updates if it exists* — a genuine upsert. Unlike `@Insert(onConflict = REPLACE)`, which *deletes* the conflicting row and inserts a fresh one (triggering foreign-key cascade deletes and changing the auto-generated id), `@Upsert` *updates in place*, preserving relationships and the id."},{t:"code",title:"@Upsert",code:`@Upsert
suspend fun upsert(user: User)   // insert if new, update if exists (in place)`},{t:"list",items:["**`@Upsert`** — insert-or-update in place.","**vs `@Insert(REPLACE)`** — REPLACE deletes+inserts (cascades fire, id changes).","**Preserves relationships** — no cascade deletes on update.","**Room 2.5+** — the clean way to do idempotent writes."]},{t:"note",text:"@Upsert (Room 2.5+) inserts if new, updates in place if existing — a true upsert. Unlike @Insert(REPLACE) which deletes the conflicting row and inserts fresh (firing FK cascades, changing auto ids), @Upsert updates in place, preserving relationships and ids. Prefer it for idempotent writes."}]},{level:"junior",q:"How does Room verify queries at compile time?",a:[{t:"p",text:"Room's annotation processor *parses your `@Query` SQL at compile time* against the known schema — catching typos, wrong column/table names, and type mismatches between the query result and the return type *before you run the app*. This is a major advantage over raw SQLite (where such errors surface as runtime crashes)."},{t:"list",items:["**Compile-time SQL validation** — column/table names, syntax checked.","**Return-type checking** — result columns must match the return type.","**Fails the build** — errors caught early, not at runtime.","**vs raw SQLite** — those are runtime crashes there."]},{t:"note",text:"Room parses @Query SQL at compile time against the schema — catching typos, wrong column/table names, and result/return-type mismatches before running. A big win over raw SQLite (runtime crashes). This is why Room is preferred: SQL errors fail the build."}]},{level:"junior",q:"How do you build and provide a Room database instance?",a:[{t:"p",text:'Build it with `Room.databaseBuilder(context, AppDatabase::class.java, "name").build()`. The database is *expensive* to create, so make it a *singleton* (one instance per app) — typically provided via DI (Hilt `@Singleton`) or a manual double-checked singleton. Use the `applicationContext` to avoid leaks.'},{t:"code",title:"Singleton database",code:`@Provides @Singleton
fun provideDb(@ApplicationContext ctx: Context): AppDatabase =
    Room.databaseBuilder(ctx, AppDatabase::class.java, "app.db")
        .addMigrations(MIGRATION_1_2)
        .build()`},{t:"list",items:["**`Room.databaseBuilder`** — creates the database; add migrations/callbacks.","**Singleton** — expensive to build; one instance app-wide (DI `@Singleton`).","**`applicationContext`** — avoid leaking an Activity.","**In-memory variant** — `Room.inMemoryDatabaseBuilder` for tests."]},{t:"note",text:'Room.databaseBuilder(context, Db::class.java, "name").build() — but the database is expensive, so make it a singleton (Hilt @Singleton or double-checked), using applicationContext to avoid leaks. Add migrations/callbacks on the builder. Room.inMemoryDatabaseBuilder for tests.'}]},{level:"senior",q:"What is the difference between a suspend DAO function and a Flow-returning one?",a:[{t:"p",text:"A `suspend` DAO function is a *one-shot* query — it runs once and returns the current result. A `Flow`-returning DAO function is *observable* — Room re-runs the query and emits a new result whenever any table it reads *changes* (via the invalidation tracker). Use suspend for a single read/write; use Flow to keep the UI in sync with the database."},{t:"code",title:"One-shot vs observable",code:`@Query("SELECT * FROM users WHERE id = :id")
suspend fun getUser(id: String): User?          // one-shot

@Query("SELECT * FROM users")
fun observeUsers(): Flow<List<User>>            // re-emits on any change to 'users'`},{t:"list",items:["**`suspend`** — runs once; current result.","**`Flow`** — re-emits on table changes (invalidation tracking).","**Reactive UI** — collect the Flow so the screen updates automatically.","**Writes** — always `suspend` (or `Flow` doesn't apply)."]},{t:"note",text:"suspend DAO = one-shot query (current result). Flow DAO = observable — Room re-runs and re-emits whenever a read table changes (invalidation tracker). Use suspend for single reads/writes; Flow to keep the UI reactively in sync with the DB (single source of truth)."}]},{level:"senior",q:"How does Room's invalidation tracker make Flows re-emit?",a:[{t:"p",text:"Room maintains an *InvalidationTracker* that watches which tables each observable query reads. When a write modifies a table (via triggers Room installs), the tracker notifies the affected queries, which re-run and emit fresh results to their `Flow` collectors. This is how a `Flow<List<User>>` automatically updates after an insert/update/delete anywhere in the app."},{t:"list",items:["**InvalidationTracker** — tracks table→query dependencies.","**Triggers on write** — modifications mark tables dirty.","**Re-run + emit** — affected observable queries re-execute and emit.","**Table-level** — granular to tables, not rows (any change to a read table re-emits)."]},{t:"note",text:"Room's InvalidationTracker watches which tables each observable query reads; on any write (via installed triggers) it marks tables dirty and the affected Flow queries re-run and emit fresh results. It's table-level (any change to a read table re-emits) — how Flow DAOs auto-update the UI after writes."}]},{level:"junior",q:"What are @ColumnInfo, @Ignore, and @Embedded?",a:[{t:"p",text:"These customize how entity fields map to columns. `@ColumnInfo(name = ...)` renames a column (or sets its type/index). `@Ignore` excludes a field from persistence (computed/transient values). `@Embedded` flattens a nested object's fields into the parent table's columns (e.g. an `Address` object stored as `street`, `city` columns)."},{t:"code",title:"Field mapping",code:`@Entity data class User(
    @PrimaryKey val id: String,
    @ColumnInfo(name = "full_name") val name: String,
    @Embedded val address: Address,        // street/city become columns
    @Ignore val cachedAvatar: Bitmap? = null,  // not stored
)
data class Address(val street: String, val city: String)`},{t:"list",items:["**`@ColumnInfo`** — column name/type/index override.","**`@Ignore`** — exclude from the table.","**`@Embedded`** — flatten a nested object into columns (prefix optional).","**Composition** — model structured data without extra tables."]},{t:"note",text:"@ColumnInfo renames/configures a column; @Ignore excludes a field from persistence (computed/transient); @Embedded flattens a nested object's fields into the parent table's columns (Address → street/city). @Embedded composes structured data without a separate table."}]},{level:"senior",q:"How do you write a dynamic query with @RawQuery?",a:[{t:"p",text:"`@RawQuery` lets you pass a `SupportSQLiteQuery` built at *runtime* — for cases where the SQL structure varies (dynamic sorting, optional filters) and can't be a fixed `@Query` string. You lose compile-time verification, so build the query carefully (and use bound arguments to avoid SQL injection)."},{t:"code",title:"@RawQuery",code:`@RawQuery(observedEntities = [User::class])
fun search(query: SupportSQLiteQuery): Flow<List<User>>

// Build at runtime:
val q = SimpleSQLiteQuery("SELECT * FROM users ORDER BY $sortColumn", arrayOf())
dao.search(q)`},{t:"list",items:["**`@RawQuery`** — runtime-built `SupportSQLiteQuery`.","**Dynamic SQL** — variable sorting/filtering not expressible statically.","**No compile-time check** — verify manually; use bound args (avoid injection).","**`observedEntities`** — needed so a `Flow` `@RawQuery` knows what to observe."]},{t:"note",text:"@RawQuery takes a runtime-built SupportSQLiteQuery for dynamic SQL (variable sorting/filters) that a static @Query can't express — but you lose compile-time verification, so build carefully with bound args (avoid injection). For Flow @RawQuery, set observedEntities so it knows what to observe."}]},{level:"junior",q:"How do you return a subset of columns (a projection) from a query?",a:[{t:"p",text:"Define a plain data class (POJO) with just the fields you want, and write a `@Query` that `SELECT`s exactly those columns — Room maps the result to the POJO. This avoids loading full entities when you only need a few fields (e.g. a list of id+name for a dropdown), improving efficiency."},{t:"code",title:"Projection",code:`data class UserSummary(val id: String, @ColumnInfo(name = "full_name") val name: String)

@Query("SELECT id, full_name FROM users")
fun summaries(): Flow<List<UserSummary>>`},{t:"list",items:["**Projection POJO** — a class with only the needed fields.","**`SELECT` those columns** — Room maps to the POJO.","**Efficiency** — don't load full entities for a few fields.","**Column names must match** — or use `@ColumnInfo`/aliases."]},{t:"note",text:"Define a POJO with only the fields you need and SELECT exactly those columns — Room maps the result to the POJO (a projection). Avoids loading full entities for a few fields (id+name for a dropdown). Column names must match the POJO (use @ColumnInfo or SQL aliases)."}]},{level:"junior",q:"How do you pass parameters, including a list, to a @Query?",a:[{t:"p",text:"Bind parameters with `:name` in the SQL, matching function parameters — Room binds them safely (no injection). For an `IN` clause with a *collection*, use `IN (:ids)` with a `List` parameter — Room expands it to the right number of placeholders at runtime."},{t:"code",title:"Parameters and IN",code:`@Query("SELECT * FROM users WHERE age > :minAge")
suspend fun olderThan(minAge: Int): List<User>

@Query("SELECT * FROM users WHERE id IN (:ids)")
suspend fun byIds(ids: List<String>): List<User>   // IN clause with a list`},{t:"list",items:["**`:name` binding** — safe parameter binding (no injection).","**`IN (:list)`** — pass a `List`; Room expands placeholders.","**Type-checked** — parameter types verified against the query.","**Multiple params** — as many `:name` bindings as needed."]},{t:"note",text:"Bind parameters with :name matching function params (safe, no injection). For an IN clause with a collection, use IN (:ids) with a List param — Room expands it to the right number of placeholders at runtime. Types are verified against the query."}]},{level:"senior",q:"How does Room handle threading for queries?",a:[{t:"p",text:"Room runs queries on background executors, not the main thread. `suspend` DAO functions dispatch to Room's *query/transaction executors* (so they're main-safe — you can call them from the main dispatcher). `Flow`-returning queries also run off the main thread. Room *forbids* synchronous (blocking) queries on the main thread by default (throws), forcing you to use suspend/Flow."},{t:"list",items:["**Background executors** — Room's query/transaction executors run the SQL.","**suspend/Flow are main-safe** — dispatch off the main thread internally.","**Main-thread blocking queries forbidden** — throws unless `allowMainThreadQueries` (don't).","**Custom executors** — configurable on the builder if needed."]},{t:"note",text:"Room runs queries on background executors; suspend and Flow DAO functions are main-safe (dispatch off main internally). Synchronous blocking queries on the main thread are forbidden by default (throws) — use suspend/Flow. You can set custom query/transaction executors on the builder."}]},{level:"junior",q:"How do you prepopulate a Room database?",a:[{t:"p",text:'Use `createFromAsset("path")` (ship a prebuilt `.db` file in assets) or `createFromFile(file)` on the database builder to seed initial data. Alternatively, use a `RoomDatabase.Callback`\'s `onCreate` to insert seed data programmatically the first time the database is created. Prepopulation is useful for reference/static data (e.g. a list of countries).'},{t:"code",title:"Prepopulate",code:`Room.databaseBuilder(ctx, AppDatabase::class.java, "app.db")
    .createFromAsset("database/seed.db")      // ship a prebuilt DB
    .build()
// or:
.addCallback(object : RoomDatabase.Callback() {
    override fun onCreate(db: SupportSQLiteDatabase) { /* insert seed rows */ }
})`},{t:"list",items:["**`createFromAsset`/`createFromFile`** — seed from a prebuilt DB.","**`Callback.onCreate`** — insert seed data programmatically on first create.","**Reference data** — static lists (countries, categories).","**Version carefully** — prepackaged DBs must match your schema version."]},{t:"note",text:'Prepopulate with createFromAsset("path") / createFromFile(file) (ship a prebuilt .db) or a RoomDatabase.Callback.onCreate to insert seed rows on first create. Good for reference/static data. The prepackaged DB must match your schema version, or migrations apply.'}]},{level:"senior",q:"How do you test Room with an in-memory database?",a:[{t:"p",text:"Use `Room.inMemoryDatabaseBuilder(context, AppDatabase::class.java)` — it creates a database that lives only in memory and is wiped when the process ends, perfect for fast, isolated instrumented tests of DAOs. Allow main-thread queries in tests if needed, insert test data, and assert query results. Run these as instrumented tests (Room needs the SQLite runtime)."},{t:"code",title:"In-memory DB test",code:`@Before fun setup() {
    db = Room.inMemoryDatabaseBuilder(context, AppDatabase::class.java)
        .allowMainThreadQueries().build()
    dao = db.userDao()
}
@Test fun insertAndRead() = runTest {
    dao.insert(User("1", "Sam", 30))
    assertEquals("Sam", dao.getById("1")?.name)
}`},{t:"list",items:["**`inMemoryDatabaseBuilder`** — ephemeral DB for tests.","**Instrumented** — needs the Android SQLite runtime (or Robolectric).","**Test DAOs** — insert, query, assert.","**Isolated** — each test gets a fresh DB."]},{t:"note",text:"Room.inMemoryDatabaseBuilder(context, Db::class.java) creates an ephemeral DB (wiped on process end) for fast, isolated DAO tests — insert data, run queries, assert. Run as instrumented tests (needs SQLite runtime; or Robolectric). Each test gets a fresh DB."}]},{level:"senior",q:"What is exportSchema, and why enable it?",a:[{t:"p",text:"`exportSchema = true` on `@Database` makes Room write the schema as a JSON file at build time (into a schemas directory). This is valuable for *migration testing* (Room's `MigrationTestHelper` uses the exported schemas to verify migrations) and for tracking schema changes in version control. Keep it on and commit the schema files."},{t:"code",title:"exportSchema",code:`@Database(entities = [User::class], version = 2, exportSchema = true)
abstract class AppDatabase : RoomDatabase() { ... }
// Configure the schema output dir in build.gradle (room.schemaLocation)`},{t:"list",items:["**`exportSchema = true`** — writes schema JSON per version at build time.","**Migration testing** — `MigrationTestHelper` uses these to validate migrations.","**Version control** — commit schema files to track changes.","**Configure output** — `room.schemaLocation` in the build config."]},{t:"note",text:"exportSchema = true writes Room's schema as JSON per version at build time (set room.schemaLocation). It powers migration testing (MigrationTestHelper validates migrations against exported schemas) and lets you track schema changes in version control. Keep it on and commit the schema files."}]},{level:"junior",q:"How do you write count, exists, and aggregate queries in Room?",a:[{t:"p",text:"Use SQL aggregate functions in a `@Query` and return the appropriate type — `SELECT COUNT(*)` returning `Int`, `SELECT EXISTS(...)` returning `Boolean`, `SELECT SUM/AVG/MAX(...)`. These can be `suspend` (one-shot) or `Flow` (observable count that updates as data changes — great for badges)."},{t:"code",title:"Aggregates",code:`@Query("SELECT COUNT(*) FROM users")
fun count(): Flow<Int>

@Query("SELECT EXISTS(SELECT 1 FROM users WHERE id = :id)")
suspend fun exists(id: String): Boolean

@Query("SELECT AVG(age) FROM users")
suspend fun averageAge(): Double`},{t:"list",items:["**`COUNT(*)`** → `Int`/`Flow<Int>` (observable badge count).","**`EXISTS(...)`** → `Boolean`.","**`SUM`/`AVG`/`MAX`/`MIN`** → numeric types.","**Grouping** — `GROUP BY` with a POJO for grouped results."]},{t:"note",text:"Use SQL aggregates in @Query with matching return types: COUNT(*) → Int/Flow<Int> (observable badge counts), EXISTS(...) → Boolean, SUM/AVG/MAX/MIN → numeric. Flow variants update automatically as data changes. Use GROUP BY + a POJO for grouped aggregates."}]},{level:"senior",q:"How does Room compare to raw SQLite and to SQLDelight?",a:[{t:"p",text:"*Raw SQLite* (SQLiteOpenHelper) is verbose and error-prone (cursors, manual mapping, runtime SQL errors). *Room* is an abstraction over SQLite with compile-time query verification, annotations, coroutines/Flow support, and migration tools — the Android-recommended default. *SQLDelight* is an alternative that generates typesafe Kotlin from `.sq` SQL files, is multiplatform (KMP), and gives SQL-first ergonomics."},{t:"table",headers:["","Room","SQLDelight","Raw SQLite"],rows:[["Verification","compile-time","compile-time","runtime"],["KMP","no (Android)","yes","no"],["Style","annotations","SQL files → Kotlin","manual"],["Recommended","Android default","KMP / SQL-first","avoid directly"]]},{t:"list",items:["**Room** — Android default; annotations, compile-time checks, Flow, migrations.","**SQLDelight** — SQL-first, typesafe generation, multiplatform (KMP).","**Raw SQLite** — low-level; avoid directly.","**Choose** — Room for Android-only; SQLDelight for KMP/SQL-first teams."]},{t:"note",text:"Raw SQLite = verbose, runtime errors (avoid). Room = Android-recommended abstraction with compile-time verification, annotations, Flow, migrations. SQLDelight = SQL-first typesafe generation, multiplatform (KMP). Choose Room for Android-only, SQLDelight for KMP/SQL-first."}]},{level:"junior",q:"How do you observe a single row reactively, and handle it being absent?",a:[{t:"p",text:"Return `Flow<User?>` from a single-row `@Query` — Room emits the row when present and `null` when it's absent (or deleted), re-emitting on changes. Make the return type nullable so a missing row is `null` rather than an error, and handle the null in the UI (empty/not-found state)."},{t:"code",title:"Observing one row",code:`@Query("SELECT * FROM users WHERE id = :id")
fun observeUser(id: String): Flow<User?>   // null when absent, re-emits on change`},{t:"list",items:["**`Flow<User?>`** — emits the row or null, reactively.","**Nullable** — a missing row is `null`, not a crash.","**Re-emits** — updates when the row changes/appears/disappears.","**UI** — handle null as a not-found/empty state."]},{t:"note",text:"Return Flow<User?> for a single row — Room emits the row or null (when absent/deleted) and re-emits on changes. Make it nullable so a missing row is null, not an error; handle null as a not-found/empty state in the UI."}]},{level:"junior",q:"What is a @DatabaseView, and when is it useful?",a:[{t:"p",text:"A `@DatabaseView` is a read-only, predefined SQL query treated like a virtual table — you can query it and map results to it like an entity, but it isn't stored. It's useful for encapsulating a complex join/aggregation you reuse across queries, keeping DAO SQL simpler."},{t:"code",title:"@DatabaseView",code:`@DatabaseView("""
    SELECT u.id, u.name, COUNT(o.id) AS orderCount
    FROM users u LEFT JOIN orders o ON o.userId = u.id GROUP BY u.id
""")
data class UserWithOrderCount(val id: String, val name: String, val orderCount: Int)

@Query("SELECT * FROM UserWithOrderCount")
fun observe(): Flow<List<UserWithOrderCount>>`},{t:"list",items:["**`@DatabaseView`** — a named, read-only query (virtual table).","**Reuse** — encapsulate a complex join/aggregation.","**Query it like an entity** — but it's not stored.","**Register** — add it to `@Database(views = [...])`."]},{t:"note",text:"A @DatabaseView is a predefined read-only SQL query treated as a virtual table — query it like an entity (but not stored). Use it to encapsulate a reusable complex join/aggregation, simplifying DAO queries. Register it in @Database(views = [...])."}]},{level:"junior",q:"How do you clear all data from the database?",a:[{t:"p",text:'Call `db.clearAllTables()` — it deletes all rows from every table (resetting auto-increment counters) in a single transaction, without dropping the schema. Use it for logout/reset scenarios. Run it off the main thread (it\'s a blocking call). To delete specific data, use `@Delete` or `@Query("DELETE FROM ...")`.'},{t:"list",items:["**`clearAllTables()`** — empties every table; keeps the schema.","**Logout/reset** — a common use.","**Off main thread** — it's blocking; run in a coroutine (`withContext(IO)`).",'**Targeted** — `@Delete` / `@Query("DELETE FROM table")` for specific rows.']},{t:"note",text:`db.clearAllTables() empties every table (resets auto-increment) in one transaction, keeping the schema — for logout/reset. It's blocking, so run off the main thread (withContext(IO)). For specific deletions use @Delete or @Query("DELETE FROM table").`}]},{level:"senior",q:"How do you enable and use full-text search (FTS) in Room?",a:[{t:"p",text:"Annotate an entity with `@Fts4` (or `@Fts3`) to create a full-text-search virtual table, then use the `MATCH` operator in queries for fast text search (ranking, prefix matching). FTS is far faster than `LIKE '%...%'` for searching large text bodies, at the cost of extra storage and a separate table synced with the source."},{t:"code",title:"FTS entity",code:`@Fts4
@Entity(tableName = "notes_fts")
data class NoteFts(val title: String, val body: String)

@Query("SELECT * FROM notes_fts WHERE notes_fts MATCH :query")
fun search(query: String): Flow<List<NoteFts>>`},{t:"list",items:["**`@Fts4`/`@Fts3`** — creates an FTS virtual table.","**`MATCH`** — fast text search (prefix, ranking) vs slow `LIKE '%...%'`.","**Cost** — extra storage; keep the FTS table synced with the source (or use `contentEntity`).","**Uses** — searching notes, messages, articles."]},{t:"note",text:"Annotate an entity @Fts4/@Fts3 to make an FTS virtual table, then query with the MATCH operator for fast text search (prefix/ranking) — far faster than LIKE '%...%' for large text. Cost: extra storage and keeping the FTS table synced (use contentEntity to link to the source). Great for notes/messages search."}]},{level:"senior",q:"How do you run multiple DAO operations atomically in Room?",a:[{t:"p",text:"Wrap them in a transaction. For a DAO method combining several operations, annotate it `@Transaction` (and make it a default/suspend method calling other DAO functions). For arbitrary work, use `db.withTransaction { }` (the coroutine-friendly API) — everything inside commits together or rolls back on failure, ensuring consistency."},{t:"code",title:"withTransaction",code:`suspend fun transfer(from: String, to: String, amount: Int) = db.withTransaction {
    dao.debit(from, amount)
    dao.credit(to, amount)      // both commit together, or both roll back
}`},{t:"list",items:["**`@Transaction`** — on a DAO method combining operations (also for multi-step reads consistency).","**`db.withTransaction { }`** — coroutine-friendly transaction block.","**Atomic** — all-or-nothing; rollback on exception.","**Use** — transfers, multi-table writes, read-then-write consistency."]},{t:"note",text:"Run atomic multi-op writes in a transaction: @Transaction on a DAO method, or db.withTransaction { } (coroutine-friendly) — everything commits together or rolls back on failure. Use for transfers, multi-table writes, and consistent read-then-write sequences."}]},{level:"junior",q:"What return types can Room DAO functions have?",a:[{t:"p",text:"Room supports many return types: for reads, a single object or `List`, nullable for optional, `Flow<T>` for observable, and `PagingSource` for Paging 3; for writes, `Unit`/`suspend`, `Long`/`List<Long>` (inserted rowIds), or `Int` (rows affected by update/delete). It also integrates with `LiveData`, RxJava types, and `Cursor` for interop."},{t:"list",items:["**Reads** — `T`, `T?`, `List<T>`, `Flow<T>`, `PagingSource<Int, T>`, `LiveData<T>`.","**`@Insert`** — `Long`/`List<Long>` (rowIds) or `Unit`.","**`@Update`/`@Delete`** — `Int` (rows affected) or `Unit`.","**Interop** — RxJava (`Single`/`Flowable`), `Cursor`."]},{t:"note",text:"Room DAO return types: reads — T/T?/List<T>/Flow<T>/PagingSource/LiveData; @Insert — Long/List<Long> (rowIds) or Unit; @Update/@Delete — Int (rows affected) or Unit; plus RxJava types and Cursor for interop. Choose Flow for observable, suspend for one-shot."}]}],SA={id:"room-fundamentals",title:"Room & SQLite Fundamentals",tagline:"SQLite vs Room, @Entity/@Dao/@Database, Flow queries as single source of truth, suspend main-safety, transactions, and building the database.",tags:["Room","SQLite","DAO","Flow","Single Source of Truth"],content:kA,qa:xA},AA=[{heading:"Relationships between entities",blocks:[{t:"p",text:"Real data has relationships — a user *has many* posts, a playlist *contains many* songs. In a relational database these are modeled with **foreign keys** (a column in one table pointing to another table's key). Room lets you query related data together using `@Relation`, which fetches a parent and its children and assembles them into a combined object."},{t:"code",title:"One-to-many with @Relation",code:`@Entity data class User(@PrimaryKey val id: String, val name: String)

@Entity data class Post(
    @PrimaryKey val id: String,
    val userId: String,           // foreign key -> User.id
    val text: String,
)

// A combined object: a user with all their posts
data class UserWithPosts(
    @Embedded val user: User,
    @Relation(parentColumn = "id", entityColumn = "userId")
    val posts: List<Post>,
)

@Dao interface UserDao {
    @Transaction                  // relations do multiple reads — keep consistent
    @Query("SELECT * FROM User")
    fun getUsersWithPosts(): Flow<List<UserWithPosts>>
}`},{t:"list",items:["**`@Embedded`** — inlines another object's columns into the query result (here the `User`'s columns). **`@Relation`** — tells Room to fetch the related rows (the posts whose `userId` matches the user's `id`) and populate the `posts` list.","**Always annotate relation queries `@Transaction`** — Room does the parent query and the children query separately, so a transaction ensures they see a consistent snapshot (no write sneaking in between).","**Types of relations**: one-to-one (a single related object), one-to-many (a list, as above), and many-to-many (via an intermediate 'junction'/'associative' table with `@Junction`, e.g. students↔courses).","**`@ForeignKey`** — optionally declares an actual database foreign-key constraint (with `onDelete = CASCADE` etc.) for referential integrity — the DB enforces that a Post's `userId` refers to a real User. `@Relation` works without it, but foreign keys add DB-level integrity."]}]},{heading:"Why migrations exist",blocks:[{t:"p",text:"Your database schema evolves — you add a column, a table, an index. But users have the *old* schema on their devices with *their data* in it. A **migration** is code that transforms the database from one schema version to the next *while preserving the user's data*. Without a correct migration, changing the schema crashes the app (Room detects the schema mismatch and throws) or, worse, loses user data."},{t:"list",items:["**The version number** in `@Database(version = N)` tracks the schema version. When you change the schema, you bump the version and provide a migration from the old version to the new one.","**Room compares the schema at startup**: if the code's schema (version N) doesn't match the on-device database (version N-1) and there's no migration path, Room throws `IllegalStateException`. Migrations bridge the gap.","**The stakes**: this is *user data* — losing it (an account's saved content, offline data) is a serious bug. Migrations must be correct and tested."]}]},{heading:"Writing a migration",blocks:[{t:"code",title:"A migration adding a column",code:`val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(db: SupportSQLiteDatabase) {
        // raw SQL to transform the schema, preserving data
        db.execSQL("ALTER TABLE users ADD COLUMN phone TEXT")
    }
}

val MIGRATION_2_3 = object : Migration(2, 3) {
    override fun migrate(db: SupportSQLiteDatabase) {
        db.execSQL("CREATE TABLE IF NOT EXISTS orders (id TEXT PRIMARY KEY NOT NULL, userId TEXT)")
    }
}

Room.databaseBuilder(context, AppDatabase::class.java, "app.db")
    .addMigrations(MIGRATION_1_2, MIGRATION_2_3)   // register all migrations
    .build()`},{t:"list",items:["A `Migration(from, to)` runs raw SQL (`ALTER TABLE`, `CREATE TABLE`, etc.) to change the schema *without dropping existing data*. Room runs the chain of migrations needed to get from the device's version to the current one (e.g. 1→2→3).","**Complex migrations** (splitting a table, changing a column type, adding a NOT NULL column with a default) require careful SQL — often create a new table, copy transformed data, drop the old, rename. SQLite's limited `ALTER TABLE` sometimes forces this create-copy-drop-rename dance.","**Export the schema** (`room.schemaLocation`) — Room can export a JSON schema per version, which enables *automated migration testing* (`MigrationTestHelper`) that verifies a migration produces the expected schema and preserves data. Testing migrations is essential because they touch real user data."]}]},{heading:"Automated migrations and the destructive shortcut",blocks:[{t:"list",items:["**`@AutoMigration`** (Room 2.4+) — for *simple* schema changes (adding a column/table), Room can *generate* the migration for you: `@Database(autoMigrations = [AutoMigration(from = 1, to = 2)])`. You only hand-write migrations for changes Room can't infer (renames, deletes, type changes — where you use `@RenameColumn`/`@DeleteColumn` spec hints). Auto-migrations reduce boilerplate and error for the common cases.","**`fallbackToDestructiveMigration()`** — the shortcut that *drops and recreates* the database (losing all data) when a migration is missing. **Never ship this to production** — it wipes user data on every schema change. It's acceptable *only* during development (when your local data is disposable) to avoid writing migrations while iterating. Shipping it means users lose their data on app updates.","**The decision**: production apps must have real migrations (auto or manual) for every version bump. Destructive fallback is a dev convenience only."]}]},{heading:"Type converters and indices",blocks:[{t:"code",title:"TypeConverter for a non-primitive type",code:`class Converters {
    @TypeConverter fun fromDate(date: Date?): Long? = date?.time
    @TypeConverter fun toDate(millis: Long?): Date? = millis?.let { Date(it) }
}

@Database(entities = [Event::class], version = 1)
@TypeConverters(Converters::class)
abstract class AppDatabase : RoomDatabase() { /* ... */ }`},{t:"list",items:["**Type converters** — SQLite stores only primitives, String, and blobs. For a custom type (a `Date`, an enum, a `List` serialized to JSON), you write a `@TypeConverter` pair telling Room how to convert to/from a storable type. Register them with `@TypeConverters`.","**Indices** (`@Entity(indices = [Index(\"userId\")])`) — like a book's index, they speed up queries that filter/sort on a column, at the cost of extra storage and slightly slower writes. Add an index to columns you frequently query by (especially foreign keys). Room warns if a foreign key column isn't indexed.","**`@Fts4`** — full-text search support for fast text search over a table."]},{t:"note",text:"Relations & migrations essentials: model relationships with @Relation + @Embedded (annotate the query @Transaction); foreign keys add DB-level integrity. Migrations transform the schema across versions while preserving user data — bump the version, provide a Migration (or @AutoMigration for simple changes), and TEST them. Never ship fallbackToDestructiveMigration (it wipes data). Type converters handle non-primitive types; indices speed up filtered queries."}]}],CA=[{level:"junior",q:"How do you model a one-to-many relationship in Room?",a:[{t:"p",text:"**You use `@Relation` together with `@Embedded` in a combined data class that Room populates.** The child entity holds a foreign-key column pointing to the parent, and you define a result class that embeds the parent and declares the related children."},{t:"code",title:"A user with their posts",code:`data class UserWithPosts(
    @Embedded val user: User,                          // parent columns inlined
    @Relation(parentColumn = "id", entityColumn = "userId")
    val posts: List<Post>,                             // matching children
)

@Transaction @Query("SELECT * FROM User")
fun getUsersWithPosts(): Flow<List<UserWithPosts>>`},{t:"list",items:["**`@Embedded`** inlines the parent object's columns into the result. **`@Relation`** tells Room to fetch the child rows whose `entityColumn` (`userId`) matches the parent's `parentColumn` (`id`) and put them in the list.","**Annotate the query `@Transaction`** — Room runs the parent query and the children query separately, so a transaction ensures they see a consistent snapshot with no write in between.","The same pattern covers one-to-one (a single related object instead of a list) and many-to-many (adding a `@Junction` through an associative table)."]},{t:"p",text:"Room assembles the combined objects for you — you get a list of users each with their posts populated, in one observable query. Optionally add `@ForeignKey` on the child entity for database-level referential integrity (and cascade-delete behavior), though `@Relation` works without it."}]},{level:"junior",q:"What is a Room migration and why is it needed?",a:[{t:"p",text:"**A migration is code that transforms the database from one schema version to the next while preserving the user's existing data.** As your app evolves, you change the schema — add a column, add a table, add an index. But users already have the *old* schema on their devices *with their data in it*. A migration bridges the old schema to the new one without losing that data."},{t:"p",text:"**Why it's needed**: Room tracks a schema `version` number. At startup, it compares the schema your code expects against the database actually on the device. If they differ and there's no migration path, Room throws an exception — the app crashes. So whenever you bump the version (because you changed the schema), you must provide a migration from the previous version. The stakes are high because this is *user data* — an incorrect migration or a missing one can crash the app or lose data the user cares about (offline content, saved items). You write a `Migration(from, to)` containing SQL (`ALTER TABLE ADD COLUMN`, `CREATE TABLE`, etc.) that changes the schema while keeping existing rows, register it with `addMigrations(...)`, and — importantly — test it."}]},{level:"junior",q:"What does fallbackToDestructiveMigration do and when is it acceptable?",a:[{t:"p",text:"**`fallbackToDestructiveMigration()` tells Room that, if it can't find a migration path for a schema change, it should *drop the entire database and recreate it from scratch* — destroying all existing data.** It's a shortcut to avoid writing migrations: instead of transforming the data, Room just wipes it and starts fresh with the new schema."},{t:"p",text:"**When it's acceptable: only during development, never in production.** While you're actively iterating on the schema locally, your test data is disposable, and constantly writing migrations for every experimental change is tedious — so destructive fallback lets you change the schema freely without migrations. But shipping it to production means **every user loses all their local data whenever you change the schema** in an app update — a serious bug that would wipe accounts' offline content, saved items, drafts, etc. Production apps must have real migrations (hand-written or `@AutoMigration`) for every version bump. The rule of thumb: if you see `fallbackToDestructiveMigration()` in code destined for release, that's a red flag to fix before shipping."}]},{level:"junior",q:"What is a TypeConverter in Room?",a:[{t:"p",text:"**A TypeConverter tells Room how to store a type that SQLite can't store natively.** SQLite only understands a few storage types — integers, real numbers, text, and blobs. So a custom type like `Date`, an enum, or a `List` has no direct representation. A `@TypeConverter` is a pair of functions that convert your custom type *to* a storable type (for writing) and *back* (for reading)."},{t:"code",title:"Storing a Date as a Long",code:`class Converters {
    @TypeConverter fun fromDate(date: Date?): Long? = date?.time
    @TypeConverter fun toDate(millis: Long?): Date? = millis?.let { Date(it) }
}
// Register with @TypeConverters(Converters::class) on the @Database`},{t:"p",text:"Here, Room stores a `Date` as its millisecond `Long` value and reconstructs the `Date` when reading. You register the converter class on your `@Database` (or a specific entity/DAO) with `@TypeConverters`, and then you can use `Date` fields in entities transparently. Common uses: dates (↔ Long), enums (↔ String name or Int ordinal), and small lists/objects (↔ a JSON String via a serialization library). One caution: converting a big object to JSON and storing it as a String works but you lose the ability to query on its contents — for data you need to query, model it as proper columns/tables instead of a serialized blob."}]},{level:"senior",q:"How do you handle a complex migration, like changing a column type or splitting a table?",a:[{t:"p",text:"**Complex migrations usually require the 'create new table, copy transformed data, drop old, rename' pattern, because SQLite's `ALTER TABLE` is limited — it can add columns and rename, but can't change a column's type, drop a column (in older versions), or add constraints to an existing table.** So for anything beyond a simple add, you rebuild the table."},{t:"code",title:"The create-copy-drop-rename dance",code:`val MIGRATION_3_4 = object : Migration(3, 4) {
    override fun migrate(db: SupportSQLiteDatabase) {
        // 1. Create the new table with the desired schema
        db.execSQL("""
            CREATE TABLE users_new (
                id TEXT PRIMARY KEY NOT NULL,
                name TEXT NOT NULL,
                age INTEGER NOT NULL DEFAULT 0   -- new column / new type
            )
        """)
        // 2. Copy and transform existing data
        db.execSQL("""
            INSERT INTO users_new (id, name, age)
            SELECT id, name, CAST(age_text AS INTEGER) FROM users
        """)
        // 3. Drop the old table
        db.execSQL("DROP TABLE users")
        // 4. Rename the new table to the original name
        db.execSQL("ALTER TABLE users_new RENAME TO users")
    }
}`},{t:"list",items:["**Why the four steps**: since you can't alter the column in place, you create a fresh table with the correct schema, `INSERT ... SELECT` to copy data (applying any transformation like `CAST`, splitting a column, or providing defaults for NOT NULL additions), drop the old table, and rename the new one to take its place. Data is preserved because you explicitly copy it.","**Splitting a table** follows the same idea: create the two new tables, `INSERT ... SELECT` the appropriate columns into each from the old table, drop the old one. For moving data to a related table, you populate the child table with a foreign key derived from the old rows.","**Recreate indices and foreign keys** on the new table — dropping the old table drops its indices, so re-add them in the migration.","**Test it rigorously** with `MigrationTestHelper`: export the schema JSON per version (`room.schemaLocation`), create a DB at the old version with representative data, run the migration, and assert both the resulting schema *and* that the data was correctly transformed and preserved. Migration bugs corrupt or lose real user data, so this testing is not optional."]},{t:"p",text:"**The senior framing**: complex migrations are where the 'preserve user data' responsibility gets real — you're hand-transforming production data through SQL, and a mistake is unrecoverable for users who've already updated. So the discipline is: use the create-copy-drop-rename pattern for anything SQLite can't do in place, be meticulous about transforming and copying every column, re-establish indices/constraints, and *test with the schema-export + MigrationTestHelper* on realistic data before shipping. For simple additive changes, prefer `@AutoMigration` to eliminate the chance of hand-written SQL errors entirely."}]},{level:"senior",q:"When should you add an index to a Room entity, and what are the trade-offs?",a:[{t:"p",text:"**Add an index to a column when you frequently query, filter (`WHERE`), sort (`ORDER BY`), or join on it — an index is a separate sorted data structure that lets SQLite find matching rows quickly instead of scanning the whole table.** Without an index, a query filtering on a column does a full table scan (O(n)); with one, it's a fast lookup (roughly O(log n)). For large tables, this is the difference between an instant query and a slow one."},{t:"list",items:["**Index foreign-key columns**: columns you join on (a `userId` in a posts table) should almost always be indexed — Room even warns you if a `@ForeignKey` column isn't indexed, because joins on an unindexed column are slow.","**Index columns in frequent WHERE/ORDER BY clauses**: if you constantly query `WHERE status = ?` or `ORDER BY createdAt`, indexing those columns speeds those queries dramatically on large tables.","**Unique indices** (`Index(value = [...], unique = true)`) also enforce uniqueness at the DB level, useful for natural keys."]},{t:"list",items:["**Trade-off 1 — storage**: each index is an additional data structure stored on disk, so indices increase the database size. Many indices on a large table add up.","**Trade-off 2 — slower writes**: every insert/update/delete must *also* update every index on the affected columns. So a table with many indices has slower writes. If a table is write-heavy and rarely queried by a given column, indexing that column costs more than it saves.","**Trade-off 3 — over-indexing is a real anti-pattern**: indexing every column 'just in case' bloats storage and slows all writes while most indices go unused. Index based on *actual query patterns*, not speculatively."]},{t:"p",text:"**The senior framing**: indexing is a read-vs-write trade-off — you speed up reads on the indexed column at the cost of storage and write speed. The heuristic is 'index the columns you query by, especially foreign keys and frequent filter/sort columns, on tables large enough for scans to matter; don't index write-heavy columns you rarely filter on.' On a small table (a few dozen rows), indices barely matter since a full scan is already fast. The right approach is to profile actual slow queries (SQLite's `EXPLAIN QUERY PLAN` shows whether an index is used) and add indices where the query patterns and table size justify them."}]},{level:"senior",q:"What is @Relation, and why does it need @Transaction?",a:[{t:"p",text:"`@Relation` lets Room populate a nested collection/object by running a *second* query and matching foreign keys — e.g. a `UserWithPosts` class with a `@Relation` list of posts. Because Room runs *two* queries (parent, then children), you annotate the DAO method `@Transaction` so both run in one atomic transaction — otherwise a concurrent write between the two queries could give inconsistent data."},{t:"code",title:"@Relation",code:`data class UserWithPosts(
    @Embedded val user: User,
    @Relation(parentColumn = "id", entityColumn = "userId")
    val posts: List<Post>,
)
@Transaction
@Query("SELECT * FROM users")
fun observeUsersWithPosts(): Flow<List<UserWithPosts>>   // two queries, one transaction`},{t:"list",items:["**`@Relation`** — Room runs a second query and stitches children by key.","**`@Embedded`** — the parent entity's columns.","**`@Transaction`** — makes the multi-query read atomic/consistent.","**Convenience** — no manual join + grouping."]},{t:"note",text:"@Relation populates a nested collection/object via a second query matching foreign keys (parentColumn/entityColumn), with @Embedded for the parent. Room runs multiple queries, so annotate the method @Transaction for a consistent atomic read. It replaces manual join + grouping."}]},{level:"senior",q:"How do you model a many-to-many relationship in Room?",a:[{t:"p",text:"Create a *cross-reference* (junction) entity holding the two foreign keys (e.g. `PlaylistSongCrossRef`), then use `@Relation` with `@Junction` to load the associated entities across it. This models many-to-many (a playlist has many songs; a song is in many playlists) without duplicating data."},{t:"code",title:"Cross-ref + @Junction",code:`@Entity(primaryKeys = ["playlistId", "songId"])
data class PlaylistSongCrossRef(val playlistId: String, val songId: String)

data class PlaylistWithSongs(
    @Embedded val playlist: Playlist,
    @Relation(
        parentColumn = "id", entityColumn = "id",
        associateBy = Junction(PlaylistSongCrossRef::class,
            parentColumn = "playlistId", entityColumn = "songId"),
    ) val songs: List<Song>,
)`},{t:"list",items:["**Junction entity** — a table with both foreign keys (composite PK).","**`@Junction`** — tells `@Relation` how to bridge via the cross-ref.","**Bidirectional** — model both `PlaylistWithSongs` and `SongWithPlaylists`.","**No duplication** — the join table holds the associations."]},{t:"note",text:"Many-to-many: a cross-reference (junction) entity with both foreign keys (composite PK), then @Relation + @Junction to load associated entities across it. Model both directions (PlaylistWithSongs / SongWithPlaylists). The junction table holds associations without data duplication."}]},{level:"senior",q:"How do you set up a foreign key with cascade actions?",a:[{t:"p",text:"Declare `foreignKeys` in `@Entity` with `@ForeignKey`, specifying the parent entity, parent/child columns, and `onDelete`/`onUpdate` actions (`CASCADE`, `SET_NULL`, `RESTRICT`, `NO_ACTION`). `CASCADE` on delete automatically removes child rows when the parent is deleted. Add an index on the foreign-key column (Room warns if you don't) for performance."},{t:"code",title:"@ForeignKey",code:`@Entity(
    foreignKeys = [ForeignKey(
        entity = User::class, parentColumns = ["id"], childColumns = ["userId"],
        onDelete = ForeignKey.CASCADE,
    )],
    indices = [Index("userId")],
)
data class Post(@PrimaryKey val id: String, val userId: String, val text: String)`},{t:"list",items:["**`@ForeignKey`** — parent/child columns + actions.","**`onDelete = CASCADE`** — delete children with the parent.","**`SET_NULL`/`RESTRICT`/`NO_ACTION`** — other behaviors.","**Index the FK column** — Room warns; improves join/lookup speed."]},{t:"note",text:"Declare foreignKeys with @ForeignKey (parentColumns/childColumns + onDelete/onUpdate actions: CASCADE, SET_NULL, RESTRICT, NO_ACTION). CASCADE deletes children with the parent. Add an index on the FK column (Room warns otherwise) for join/lookup performance."}]},{level:"senior",q:"What is the difference between @Embedded and @Relation?",a:[{t:"p",text:"`@Embedded` flattens a nested object into the *same* table's columns (one-to-one, stored inline) — no extra query. `@Relation` links to a *separate* table/entity via foreign keys and loads it with an additional query (one-to-many/many-to-many). Use `@Embedded` for value objects that belong to the row; `@Relation` for genuinely separate entities."},{t:"list",items:["**`@Embedded`** — inline the object's fields into the same table; no extra query.","**`@Relation`** — separate entity/table, loaded via a second query by key.","**Cardinality** — `@Embedded` one-to-one inline; `@Relation` one/many-to-many.","**Choose** — value object of the row (Embedded) vs independent entity (Relation)."]},{t:"note",text:"@Embedded flattens a nested object into the same table's columns (inline, one-to-one, no extra query). @Relation links a separate entity/table via foreign keys, loaded with an additional query (one/many-to-many). Use @Embedded for value objects belonging to the row; @Relation for independent entities."}]},{level:"senior",q:"What does a Migration object look like, and how do you register it?",a:[{t:"p",text:"A `Migration(startVersion, endVersion)` overrides `migrate(db)` where you run the SQL to transform the schema from one version to the next. You register it on the database builder with `.addMigrations(...)`. When the stored DB version differs from the `@Database(version = ...)`, Room runs the matching migration path."},{t:"code",title:"A migration",code:`val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(db: SupportSQLiteDatabase) {
        db.execSQL("ALTER TABLE users ADD COLUMN age INTEGER NOT NULL DEFAULT 0")
    }
}
Room.databaseBuilder(ctx, AppDatabase::class.java, "app.db")
    .addMigrations(MIGRATION_1_2).build()`},{t:"list",items:["**`Migration(from, to)`** — override `migrate(db)` with SQL.","**`.addMigrations(...)`** — register on the builder.","**Version bump** — increment `@Database(version)` for each schema change.","**Path** — Room chains migrations (1→2→3) or uses a direct 1→3 if provided."]},{t:"note",text:"A Migration(startVersion, endVersion) overrides migrate(db) with SQL to transform the schema; register via .addMigrations(...). Bump @Database(version) per change. Room chains migrations along the path (1→2→3) or uses a direct one if provided. Missing migrations crash unless destructive fallback."}]},{level:"junior",q:"How do you add a new column or table in a migration?",a:[{t:"p",text:"Adding a *column* is simple: `ALTER TABLE table ADD COLUMN name TYPE NOT NULL DEFAULT value`. Adding a *table* is `CREATE TABLE ...` matching Room's expected schema exactly (column names, types, constraints). The generated schema JSON (from `exportSchema`) shows the exact `CREATE TABLE` Room expects, which you can copy."},{t:"code",title:"Add column / table",code:`override fun migrate(db: SupportSQLiteDatabase) {
    db.execSQL("ALTER TABLE users ADD COLUMN verified INTEGER NOT NULL DEFAULT 0")
    db.execSQL("""CREATE TABLE IF NOT EXISTS orders (
        id TEXT NOT NULL PRIMARY KEY, userId TEXT NOT NULL, total INTEGER NOT NULL)""")
}`},{t:"list",items:["**Add column** — `ALTER TABLE ... ADD COLUMN` (with `NOT NULL DEFAULT` for non-null).","**Add table** — `CREATE TABLE` matching Room's exact expected schema.","**Match exactly** — types/constraints must match or Room's validation fails.","**Copy from schema JSON** — the exported schema shows the exact DDL."]},{t:"note",text:"Add a column with ALTER TABLE ... ADD COLUMN (NOT NULL DEFAULT for non-null); add a table with CREATE TABLE matching Room's exact expected schema (types/constraints must match or validation fails). Copy the exact DDL from the exported schema JSON."}]},{level:"senior",q:"What are Room auto-migrations, and when can you use them?",a:[{t:"p",text:"`@AutoMigration(from, to)` (Room 2.4+) lets Room *generate* the migration for simple schema changes (adding tables/columns) by diffing the exported schemas — no hand-written SQL. For ambiguous changes (renames, deletions, splits), you provide an `AutoMigrationSpec` with `@RenameColumn`/`@DeleteTable` hints. Complex transformations still need a manual `Migration`."},{t:"code",title:"Auto-migration",code:`@Database(
    entities = [User::class], version = 2, exportSchema = true,
    autoMigrations = [AutoMigration(from = 1, to = 2)],
)
abstract class AppDatabase : RoomDatabase()

// For a rename, provide a spec:
@RenameColumn(tableName = "users", fromColumnName = "name", toColumnName = "full_name")
class MySpec : AutoMigrationSpec`},{t:"list",items:["**`@AutoMigration`** — generated from schema diffs (needs `exportSchema`).","**Simple changes** — add table/column handled automatically.","**`AutoMigrationSpec`** — hints for renames/deletions.","**Complex** — data transformations still need a manual `Migration`."]},{t:"note",text:"@AutoMigration(from, to) (Room 2.4+) generates migrations for simple changes (add table/column) by diffing exported schemas (needs exportSchema). Provide an AutoMigrationSpec (@RenameColumn/@DeleteTable) for ambiguous changes. Complex data transformations still require a manual Migration."}]},{level:"junior",q:"What happens if you bump the database version without providing a migration?",a:[{t:"p",text:"Room throws `IllegalStateException: A migration from X to Y was required but not found` at runtime when it opens the database — the app crashes. You must either provide a `Migration` (or auto-migration) covering the version change, or opt into `fallbackToDestructiveMigration()` (which *wipes and recreates* the database, losing all data). Never ship a version bump without one of these."},{t:"list",items:["**Crash** — 'migration required but not found' at DB open.","**Fix 1** — provide a `Migration`/auto-migration for the path.","**Fix 2** — `fallbackToDestructiveMigration()` (wipes data — only for caches).","**Never ship** — a version bump with neither."]},{t:"note",text:"Bumping @Database(version) without a migration crashes at DB open ('migration required but not found'). Provide a Migration/auto-migration, or opt into fallbackToDestructiveMigration() (wipes and recreates — only acceptable for caches, not user data). Never ship a version bump with neither."}]},{level:"senior",q:"How do you test Room migrations?",a:[{t:"p",text:"Use `MigrationTestHelper` (with the exported schema JSON). Create the database at the *old* version, insert data, run `runMigrationsAndValidate(name, newVersion, validateDroppedTables, migration)` — it applies your migration and *validates* the resulting schema matches Room's expected schema, and you assert the data survived correctly. This catches broken migrations before release."},{t:"code",title:"Migration test",code:`@get:Rule val helper = MigrationTestHelper(
    InstrumentationRegistry.getInstrumentation(), AppDatabase::class.java)

@Test fun migrate1To2() {
    helper.createDatabase(TEST_DB, 1).apply { execSQL("INSERT INTO users ..."); close() }
    val db = helper.runMigrationsAndValidate(TEST_DB, 2, true, MIGRATION_1_2)
    // assert data survived / new column exists
}`},{t:"list",items:["**`MigrationTestHelper`** — uses exported schemas.","**Create at old version** — insert representative data.","**`runMigrationsAndValidate`** — applies + validates the schema.","**Assert data** — verify it migrated correctly."]},{t:"note",text:"Test migrations with MigrationTestHelper (needs exportSchema): create the DB at the old version, insert data, then runMigrationsAndValidate(name, newVersion, ..., migration) — it applies the migration and validates the schema matches Room's expected one; assert the data survived. Catches broken migrations pre-release."}]},{level:"junior",q:"How do you persist an enum, a List, or a Date in Room?",a:[{t:"p",text:"SQLite has no type for these, so you write a `TypeConverter` — a pair of functions converting the type to/from a storable primitive (String/Long). For an enum, store its `name`; for a `List`, serialize to JSON; for a `Date`, store the epoch millis. Register converters with `@TypeConverters` on the database (or entity/DAO)."},{t:"code",title:"TypeConverters",code:`class Converters {
    @TypeConverter fun fromStatus(s: Status) = s.name
    @TypeConverter fun toStatus(s: String) = Status.valueOf(s)
    @TypeConverter fun fromList(l: List<String>) = Json.encodeToString(l)
    @TypeConverter fun toList(s: String) = Json.decodeFromString<List<String>>(s)
    @TypeConverter fun fromDate(d: Date) = d.time
    @TypeConverter fun toDate(t: Long) = Date(t)
}`},{t:"list",items:["**Enum** — store `name` (or ordinal, but name is safer against reordering).","**List/object** — serialize to JSON String.","**Date** — store epoch millis (`Long`).","**Register** — `@TypeConverters(Converters::class)` on the database/entity/DAO."]},{t:"note",text:"Write a TypeConverter (to/from a storable primitive) for non-SQLite types: enum → name (safer than ordinal), List/object → JSON String, Date → epoch millis (Long). Register with @TypeConverters(Converters::class) on the database (or entity/DAO to scope it). Consider a real relation for complex nested data."}]},{level:"senior",q:"What is the N+1 query problem with relations, and how does Room avoid it?",a:[{t:"p",text:"The N+1 problem is running 1 query for parents then N queries (one per parent) for children — slow for large lists. Room's `@Relation` *avoids* this: it runs *one* query for all parents and *one* query for all children (using `WHERE key IN (...)`), then stitches them in memory — 2 queries total regardless of parent count. So `@Relation` is efficient, unlike naive per-item loading."},{t:"list",items:["**N+1** — 1 parent query + N child queries (one per parent); slow.","**Room `@Relation`** — 1 parent + 1 child query (`IN` batch), stitched in memory.","**2 queries total** — regardless of parent count.","**Avoid manual per-item queries** — use `@Relation` or a join."]},{t:"note",text:"N+1 = 1 parent query + N per-parent child queries (slow for big lists). Room's @Relation avoids it: one query for parents, one for all children (WHERE key IN (...)), stitched in memory — 2 queries total regardless of count. So @Relation is efficient; avoid manual per-item child queries."}]},{level:"senior",q:"When should you denormalize instead of using relations?",a:[{t:"p",text:"Relations (normalized data) avoid duplication and keep data consistent, but require joins/multiple queries. *Denormalization* (duplicating some data, e.g. storing an author's name on each post) trades storage and update complexity for *read speed* and simpler queries. Denormalize when reads dominate, the duplicated data rarely changes, and joins are a measured bottleneck — but keep the duplicates in sync."},{t:"list",items:["**Normalized (relations)** — no duplication, consistent, but joins.","**Denormalized** — duplicate some data for faster reads/simpler queries.","**When** — read-heavy, rarely-changing duplicated data, joins are a bottleneck.","**Cost** — must keep duplicates in sync on writes."]},{t:"note",text:"Relations (normalized) avoid duplication and keep data consistent but need joins. Denormalize (duplicate some data — e.g. author name on each post) for faster reads/simpler queries when reads dominate and the duplicated data rarely changes and joins are a measured bottleneck. Cost: keep duplicates synced on writes."}]},{level:"senior",q:"How do foreign key constraints get enforced, and how do you handle nullable foreign keys?",a:[{t:"p",text:"Room enables SQLite foreign-key enforcement by default when you declare `@ForeignKey`, so inserting a child with a non-existent parent fails. For an *optional* relationship, make the foreign-key column *nullable* and use `onDelete = SET_NULL` so deleting the parent nulls the reference instead of deleting the child. Ensure the FK column is indexed."},{t:"list",items:["**Enforcement** — declaring `@ForeignKey` enables constraint checking (bad refs fail).","**Nullable FK** — optional relationship; the column can be null.","**`onDelete = SET_NULL`** — parent delete nulls the child's reference.","**Index the FK** — for performance and to satisfy Room's warning."]},{t:"note",text:"Declaring @ForeignKey enables SQLite FK enforcement (inserting a child with a missing parent fails). For optional relationships, make the FK column nullable and use onDelete = SET_NULL (parent delete nulls the reference instead of cascading). Index the FK column."}]},{level:"senior",q:"How do you migrate when you need to change a column type or rename it?",a:[{t:"p",text:"SQLite can't directly change a column type or (historically) rename easily, so use the *create-copy-drop-rename* pattern: create a new table with the desired schema, `INSERT INTO new SELECT ... FROM old` (transforming as needed), `DROP TABLE old`, and `ALTER TABLE new RENAME TO old`. This preserves data through an incompatible schema change."},{t:"code",title:"Recreate-table migration",code:`override fun migrate(db: SupportSQLiteDatabase) {
    db.execSQL("CREATE TABLE users_new (id TEXT PRIMARY KEY NOT NULL, age INTEGER NOT NULL)")
    db.execSQL("INSERT INTO users_new (id, age) SELECT id, CAST(age AS INTEGER) FROM users")
    db.execSQL("DROP TABLE users")
    db.execSQL("ALTER TABLE users_new RENAME TO users")
}`},{t:"list",items:["**Create new table** — with the target schema.","**Copy with transform** — `INSERT ... SELECT`, casting/converting.","**Drop old, rename new** — swap in the new table.","**In a transaction** — the migration runs atomically."]},{t:"note",text:"For type changes/renames SQLite can't do directly, use create-copy-drop-rename: CREATE new table with the target schema, INSERT ... SELECT (transforming), DROP old, ALTER ... RENAME TO old. The migration runs in a transaction (atomic). Test it with MigrationTestHelper."}]},{level:"senior",q:"What is a unique index, and how does it differ from a regular index?",a:[{t:"p",text:'A regular index speeds up lookups on a column. A *unique* index additionally *enforces uniqueness* — inserting a duplicate value fails (a constraint). Declare it via `@Index(value = ["email"], unique = true)`. Use it to guarantee no duplicate emails/usernames while also speeding lookups on that column.'},{t:"code",title:"Unique index",code:`@Entity(indices = [Index(value = ["email"], unique = true)])
data class User(@PrimaryKey val id: String, val email: String)`},{t:"list",items:["**Regular index** — faster lookups; allows duplicates.","**Unique index** — faster lookups AND enforces uniqueness (duplicate insert fails).","**`@Index(unique = true)`** — declare on the entity.","**Composite** — a unique index over multiple columns enforces combined uniqueness."]},{t:"note",text:'A regular index speeds lookups (allows duplicates); a unique index also enforces uniqueness — duplicate inserts fail. Declare @Index(value = ["email"], unique = true). Use for no-duplicate constraints (email/username) while speeding lookups. Composite unique indices enforce combined uniqueness.'}]},{level:"junior",q:"How do you add an index in a migration?",a:[{t:"p",text:"Run `CREATE INDEX` (or `CREATE UNIQUE INDEX`) in the migration's `migrate` method, using Room's index naming convention (`index_TableName_columnName`) so its schema validation passes. You add the index to the entity (`indices = [...]`) *and* create it in the migration — the entity declaration is for new installs, the migration for upgrades."},{t:"code",title:"Index migration",code:`override fun migrate(db: SupportSQLiteDatabase) {
    db.execSQL("CREATE INDEX IF NOT EXISTS index_posts_userId ON posts(userId)")
}`},{t:"list",items:["**`CREATE INDEX`** in `migrate` — with Room's naming (`index_Table_column`).",'**Update the entity too** — `indices = [Index("userId")]` for new installs.',"**Match the schema** — name must match or validation fails.","**Unique** — `CREATE UNIQUE INDEX` for uniqueness."]},{t:"note",text:"Add an index in a migration with CREATE INDEX (IF NOT EXISTS) using Room's naming (index_Table_column) so schema validation passes, AND add indices = [...] to the entity (for new installs). The migration handles upgrades, the entity declaration handles fresh installs; names must match."}]},{level:"junior",q:"How do you model a one-to-one relationship in Room?",a:[{t:"p",text:"Two options: *embed* the related data with `@Embedded` (stored inline in the same row — simplest for a value object), or use `@Relation` between two entities where the child has a unique foreign key to the parent (separate tables, loaded via a second query). Use `@Embedded` for tightly-coupled value data; `@Relation` for a genuinely separate entity with its own lifecycle."},{t:"code",title:"One-to-one via @Relation",code:`data class UserWithProfile(
    @Embedded val user: User,
    @Relation(parentColumn = "id", entityColumn = "userId")
    val profile: Profile,   // single object, not a list
)`},{t:"list",items:["**`@Embedded`** — inline value data in the same table.","**`@Relation` (single)** — separate entity via a unique FK; a single object, not a list.","**Choose** — value object (Embedded) vs independent entity (Relation).","**Unique FK** — ensures the one-to-one cardinality."]},{t:"note",text:"One-to-one: @Embedded (inline value data in the same row — simplest) or @Relation between two entities with a unique FK (separate tables, second query, returns a single object not a list). Use @Embedded for tightly-coupled value data, @Relation for an independent entity."}]},{level:"senior",q:"What is a composite primary key, and how do you declare one?",a:[{t:"p",text:'A composite primary key uses *multiple columns* together as the unique identifier — declared via `@Entity(primaryKeys = ["a", "b"])`. It\'s common for *junction tables* (many-to-many cross-refs) where the pair of foreign keys uniquely identifies a row, and for natural keys spanning multiple fields.'},{t:"code",title:"Composite key",code:`@Entity(primaryKeys = ["playlistId", "songId"])
data class PlaylistSongCrossRef(val playlistId: String, val songId: String)`},{t:"list",items:["**`primaryKeys = [...]`** — multiple columns form the key.","**Junction tables** — the FK pair is the natural composite key.","**Uniqueness** — the *combination* must be unique.","**vs single `@PrimaryKey`** — use that for a single-column key."]},{t:"note",text:'A composite primary key uses multiple columns together as the identifier: @Entity(primaryKeys = ["a", "b"]) — the combination must be unique. Common for junction tables (the FK pair) and multi-field natural keys. Use single @PrimaryKey for one-column keys.'}]},{level:"senior",q:"How does Room decide the migration path across multiple versions?",a:[{t:"p",text:"Room finds a *path* of registered migrations from the stored version to the current `@Database(version)`. If you provide 1→2 and 2→3, it chains them for a 1→3 upgrade; if you also provide a direct 1→3, Room uses that (more efficient). If *no* path exists for some jump, it crashes (unless destructive fallback). So you must cover every version gap your users could upgrade from."},{t:"list",items:["**Path-finding** — chains registered migrations from stored → current version.","**Chaining** — 1→2 + 2→3 covers 1→3; a direct 1→3 is used if provided.","**Gaps crash** — a missing path throws (unless destructive fallback).","**Cover all versions** — users may upgrade from any older version."]},{t:"note",text:"Room finds a path of registered migrations from the stored version to @Database(version) — chaining (1→2 + 2→3 covers 1→3) or using a direct migration if provided. A missing path crashes (unless destructive fallback). Cover every version gap your users could upgrade from."}]},{level:"junior",q:"What does @Transaction do on a DAO method?",a:[{t:"p",text:"`@Transaction` makes a DAO method run its database work inside a *single transaction* — either all operations commit together or all roll back on failure. It's required for `@Relation` methods (multiple queries → consistency), for multi-step writes that must be atomic, and to get a consistent snapshot for a multi-query read."},{t:"list",items:["**Atomic** — all-or-nothing; rollback on exception.","**`@Relation`** — needed so the parent + children queries are consistent.","**Multi-step writes** — group inserts/updates atomically.","**Consistent reads** — a multi-query read sees one snapshot."]},{t:"note",text:"@Transaction runs a DAO method's work in one transaction (all-or-nothing, rollback on failure). Required for @Relation methods (consistent multi-query reads), atomic multi-step writes, and consistent multi-query reads. For coroutine blocks spanning DAOs, use db.withTransaction { }."}]},{level:"senior",q:"How do you handle a migration that splits one table into two?",a:[{t:"p",text:"Create the two new tables, copy the relevant columns from the old table into each (`INSERT INTO new_a SELECT ...`, `INSERT INTO new_b SELECT ...`), then drop the old table. If the split introduces a relationship, populate the foreign keys during the copy. Do it all in the migration's transaction so it's atomic, and validate with `MigrationTestHelper`."},{t:"code",title:"Splitting a table",code:`override fun migrate(db: SupportSQLiteDatabase) {
    db.execSQL("CREATE TABLE profiles (userId TEXT PRIMARY KEY NOT NULL, bio TEXT)")
    db.execSQL("INSERT INTO profiles (userId, bio) SELECT id, bio FROM users")
    db.execSQL("CREATE TABLE users_new (id TEXT PRIMARY KEY NOT NULL, name TEXT NOT NULL)")
    db.execSQL("INSERT INTO users_new (id, name) SELECT id, name FROM users")
    db.execSQL("DROP TABLE users")
    db.execSQL("ALTER TABLE users_new RENAME TO users")
}`},{t:"list",items:["**Create new tables** — the split targets.","**Copy columns** — `INSERT ... SELECT` into each.","**Populate FKs** — if the split creates a relationship.","**Drop old + validate** — atomic in the transaction; test with MigrationTestHelper."]},{t:"note",text:"Split a table by creating the new tables, copying relevant columns into each (INSERT ... SELECT), populating any new foreign keys, then dropping the old table — all atomic in the migration's transaction. Validate with MigrationTestHelper against the exported schema."}]},{level:"senior",q:"What is the trade-off of adding an index to a Room entity?",a:[{t:"p",text:"An index *speeds up reads* (WHERE/JOIN/ORDER BY on the indexed column) by maintaining a sorted structure, but *slows down writes* (each insert/update/delete must also update the index) and uses extra storage. Add indices to columns you frequently filter/join/sort on (especially foreign keys), but don't index everything — measure and index where reads dominate."},{t:"list",items:["**Faster reads** — WHERE/JOIN/ORDER BY on the column.","**Slower writes** — index maintenance on every write.","**Extra storage** — the index structure.","**Index selectively** — FKs and frequently-queried columns; not every column."]},{t:"note",text:"An index speeds reads (WHERE/JOIN/ORDER BY on the column) but slows writes (index maintenance per write) and uses storage. Index foreign keys and frequently filtered/joined/sorted columns; don't index everything — measure and index where reads dominate."}]},{level:"junior",q:"When is fallbackToDestructiveMigration acceptable?",a:[{t:"p",text:"`fallbackToDestructiveMigration()` tells Room to *drop and recreate* the database (losing all data) when a migration is missing. It's acceptable only for data that can be *regenerated* — a pure *cache* of remote data you can re-fetch. It is *never* acceptable for user-generated data (notes, drafts, offline changes) that can't be recovered. Prefer real migrations for anything the user would miss."},{t:"list",items:["**Destructive** — wipes and recreates the DB on a missing migration.","**OK for caches** — data you can re-fetch/regenerate.","**Never for user data** — unrecoverable loss.","**Variants** — `fallbackToDestructiveMigrationOnDowngrade()`, `fallbackToDestructiveMigrationFrom(versions)`."]},{t:"note",text:"fallbackToDestructiveMigration() drops and recreates the DB (losing data) when a migration is missing — acceptable ONLY for regenerable caches (re-fetchable remote data), NEVER for user-generated data. Use real migrations for anything the user would miss. Variants scope it to downgrades or specific versions."}]},{level:"senior",q:"How do you return a nested relation efficiently for a list screen?",a:[{t:"p",text:"For a list of parents each with children, use a `@Relation` + `@Transaction` query returning `Flow<List<ParentWithChildren>>` — Room batches it into 2 queries. But if children lists are *large* or you only need a *count*, prefer a projection (parent + child count via a subquery/JOIN) instead of loading full child lists, to avoid pulling excessive data into memory for a scrolling list."},{t:"list",items:["**`@Relation` + `@Transaction`** — 2 queries, stitched; fine for small child lists.","**Large children** — prefer a count/summary projection, not full lists.","**Projection** — parent + `COUNT(child)` via subquery for list screens.","**Paging** — for very large data, use Paging 3 with the relation."]},{t:"note",text:"For a list of parents+children, @Relation + @Transaction (Flow<List<ParentWithChildren>>) batches into 2 queries — fine for small child lists. For large children or when you only need a count, use a projection (parent + COUNT(child) subquery) to avoid loading big lists into memory; use Paging 3 for very large data."}]}],TA={id:"room-relations-migrations",title:"Room Relations & Migrations",tagline:"@Relation/@Embedded relationships, foreign keys, schema migrations & @AutoMigration, the destructive fallback, type converters, and indices.",tags:["Room","Relations","Migrations","TypeConverter","Indices"],content:AA,qa:CA},IA=[{heading:"The problem: storing small key-value data",blocks:[{t:"p",text:"Apps need to store small pieces of data — user settings, a theme choice, an auth token, a 'has the user seen onboarding' flag, the last-selected tab. This is too small and simple for a database. Android's tools for this are **SharedPreferences** (the old way) and **DataStore** (the modern replacement). Both store key-value data, but DataStore fixes serious flaws in SharedPreferences."}]},{heading:"SharedPreferences and its problems",blocks:[{t:"p",text:"**SharedPreferences** is the classic API: a simple key-value store backed by an XML file, accessed synchronously. It works, but it has well-known flaws that DataStore was created to solve:"},{t:"code",title:"SharedPreferences — simple but flawed",code:`val prefs = context.getSharedPreferences("settings", Context.MODE_PRIVATE)
prefs.edit().putString("theme", "dark").apply()   // async write
val theme = prefs.getString("theme", "light")     // SYNCHRONOUS read — on main thread!`},{t:"list",items:["**Synchronous API that can block the main thread**: reads (`getString`) are synchronous, and the first access to a SharedPreferences file loads and parses the entire XML *on the calling thread* — often the main thread — which can cause jank or ANRs (a documented source of ANRs).","**`apply()` vs `commit()` gotcha**: `commit()` writes synchronously (blocks); `apply()` writes asynchronously but can still cause issues (it blocks the main thread on `fsync` during certain lifecycle events like `onPause`).","**No error signaling**: if a write fails, you don't find out — errors are silently swallowed.","**No type safety**: keys are strings, values are untyped — easy to mismatch a key or type at runtime.","**Not reactive**: to observe changes you register an `OnSharedPreferenceChangeListener` (clunky, error-prone) — there's no Flow of updates.","**Runtime exceptions on parse errors / not transactional**: partial updates are possible; no strong consistency guarantees."]}]},{heading:"DataStore — the modern replacement",blocks:[{t:"p",text:"**DataStore** is Jetpack's modern key-value (and typed) storage, built on **Kotlin coroutines and Flow**. It stores data asynchronously, exposes it as a `Flow` (reactive), handles errors, and never blocks the main thread. It comes in two flavors: **Preferences DataStore** (key-value, like SharedPreferences but better) and **Proto DataStore** (typed, schema-defined with Protocol Buffers)."},{t:"code",title:"Preferences DataStore",code:`// Define the DataStore (once, as a top-level property)
val Context.dataStore by preferencesDataStore(name = "settings")
private val THEME_KEY = stringPreferencesKey("theme")

// READ — a Flow that emits on every change (async, main-safe)
val themeFlow: Flow<String> = context.dataStore.data
    .map { prefs -> prefs[THEME_KEY] ?: "light" }

// WRITE — a suspend function (async, transactional, error-aware)
suspend fun setTheme(theme: String) {
    context.dataStore.edit { prefs -> prefs[THEME_KEY] = theme }
}`},{t:"list",items:["**Fully asynchronous & main-safe**: reads are a `Flow` and writes are `suspend` functions — no blocking, no ANR risk. All I/O happens off the main thread via coroutines.","**Reactive by default**: `dataStore.data` is a `Flow` that emits the current value and re-emits on every change — perfect for observing settings in the UI (collect it like any Flow).","**Transactional & consistent**: writes are atomic (via `edit { }`), and DataStore guarantees consistency — no partial/corrupt states.","**Error handling**: reads can surface errors (e.g. `IOException` on a read failure) that you can `catch` in the Flow, rather than silently failing.","**Type-safe keys**: `stringPreferencesKey`, `intPreferencesKey`, etc. give typed keys, reducing runtime mistakes."]}]},{heading:"Preferences DataStore vs Proto DataStore",blocks:[{t:"table",headers:["","Preferences DataStore","Proto DataStore"],rows:[["Data shape","key-value pairs (untyped keys)","a defined schema (typed object) via Protocol Buffers"],["Type safety","typed keys, but no schema for the whole object","full type safety — the whole structure is a typed message"],["Setup","minimal (just define keys)","more (define a .proto schema + serializer)"],["Best for","simple, independent settings (theme, flags)","structured, related settings (a UserPreferences object)"]]},{t:"list",items:["**Preferences DataStore** — the direct SharedPreferences replacement: key-value, quick to set up, for loose independent settings. Its limitation: no schema, so you can still typo a key or store the wrong shape.","**Proto DataStore** — stores a *typed object* defined by a Protocol Buffers schema, giving full type safety across the whole structure and validation. More setup (define the `.proto`, a serializer), but ideal when your preferences form a cohesive structured object (all of a user's settings together).","**Choose Preferences DataStore** for simple cases (most apps); **Proto DataStore** when you want a strongly-typed, schema-validated settings object."]}]},{heading:"When to use what — the storage decision",blocks:[{t:"table",headers:["Data","Storage"],rows:[["Small key-value settings, flags, tokens","DataStore (Preferences or Proto)"],["Structured, queryable, relational data (lists, entities)","Room"],["Large files, images, media","the file system (internal/external storage)"],["Legacy code already using it","SharedPreferences (migrate to DataStore over time)"]]},{t:"list",items:["**DataStore for settings, Room for data**: DataStore is for a *handful* of key-value settings, not for structured or large data — you can't query it, and it loads everything into memory. Lists of entities, anything you filter/sort/relate → Room.","**Migration path**: DataStore provides a `SharedPreferencesMigration` to import existing SharedPreferences data into DataStore automatically, so you can migrate legacy prefs cleanly.","**Not for secrets in plaintext**: DataStore/SharedPreferences store data unencrypted by default. For sensitive data (tokens, keys), use encryption — `EncryptedSharedPreferences`, or encrypt values before storing, or the Android Keystore for keys."]},{t:"note",text:"The comparison to nail: SharedPreferences is synchronous (can block the main thread → ANRs), swallows errors, and isn't reactive. DataStore is coroutine/Flow-based — fully async and main-safe, reactive (data is a Flow), transactional, and error-aware. Preferences DataStore = key-value replacement; Proto DataStore = typed schema. Use DataStore for small settings, Room for structured data, files for large blobs. DataStore is the recommended modern choice."}]}],MA=[{level:"junior",q:"What is SharedPreferences used for and what are its main problems?",a:[{t:"p",text:"**SharedPreferences is a simple key-value store backed by an XML file, used for small pieces of data** — settings, a theme choice, feature flags, an auth token, a 'seen onboarding' flag. It's the classic Android API for lightweight persistent data that's too small for a database."},{t:"list",items:["**Synchronous, can block the main thread**: reads are synchronous, and the first access loads and parses the whole XML file on the calling thread — often the main thread — which is a documented cause of jank and ANRs.","**No error handling**: if a write fails, it's silently swallowed; you have no way to know.","**Not reactive**: observing changes requires a clunky `OnSharedPreferenceChangeListener`; there's no Flow of updates.","**No type safety**: string keys and untyped values make mismatches easy at runtime.","**`apply()` vs `commit()` confusion**: `commit()` blocks; `apply()` is async but can still block on `fsync` during lifecycle events."]},{t:"p",text:"These flaws — especially the main-thread blocking and lack of reactivity — are why Google introduced **DataStore** as the modern replacement. SharedPreferences still works and is fine in legacy code, but new code should use DataStore, which is fully async (coroutines/Flow), reactive, transactional, and error-aware."}]},{level:"junior",q:"What is DataStore and how is it better than SharedPreferences?",a:[{t:"p",text:"**DataStore is Jetpack's modern key-value (and typed) storage, built on Kotlin coroutines and Flow.** It solves SharedPreferences' problems by being fully asynchronous and reactive: reads are exposed as a `Flow`, and writes are `suspend` functions, so nothing blocks the main thread."},{t:"list",items:["**Fully async & main-safe** — reads (`Flow`) and writes (`suspend`) run off the main thread via coroutines, eliminating the ANR risk SharedPreferences had.","**Reactive** — `dataStore.data` is a `Flow` that emits the current value and re-emits on every change, so you observe settings in the UI like any other Flow (no manual change listeners).","**Transactional & consistent** — writes go through `edit { }` atomically; no partial or corrupt states.","**Error-aware** — read failures surface as catchable errors in the Flow, instead of being silently swallowed.","**Typed keys** — `stringPreferencesKey`, `intPreferencesKey`, etc. reduce runtime key/type mistakes."]},{t:"code",title:"The reactive read + suspend write",code:`val theme: Flow<String> = context.dataStore.data.map { it[THEME_KEY] ?: "light" }
suspend fun setTheme(v: String) = context.dataStore.edit { it[THEME_KEY] = v }`},{t:"p",text:"It comes in two forms: **Preferences DataStore** (key-value, the direct SharedPreferences replacement) and **Proto DataStore** (a typed object defined by a Protocol Buffers schema, for full type safety). DataStore is the recommended modern choice for small persistent data."}]},{level:"junior",q:"What is the difference between Preferences DataStore and Proto DataStore?",a:[{t:"p",text:"**Both are DataStore, but they differ in how strongly typed the stored data is.** *Preferences DataStore* stores loose key-value pairs (like SharedPreferences, but async and reactive) — you use typed keys, but there's no schema for the overall data. *Proto DataStore* stores a single *typed object* defined by a Protocol Buffers schema, giving full type safety and validation across the whole structure."},{t:"list",items:["**Preferences DataStore** — minimal setup (just define keys), good for a handful of simple, independent settings (theme, a flag, a token). Its limitation: no schema, so you can still typo a key or store an unexpected shape; the 'object' is just a bag of keys.","**Proto DataStore** — more setup (define a `.proto` schema and a serializer), but the entire stored value is a strongly-typed message, so the compiler enforces the structure and you get validation. Ideal when your preferences form a cohesive, structured object — e.g. a `UserSettings` with several related fields you want to treat as one typed unit."]},{t:"p",text:"**How to choose**: Preferences DataStore for most apps and simple independent settings; Proto DataStore when you want a strongly-typed, schema-validated settings object and are willing to define the schema. The trade-off is setup effort vs type safety — Proto gives more guarantees at the cost of more boilerplate."}]},{level:"junior",q:"When would you use DataStore vs Room vs the file system?",a:[{t:"list",items:["**DataStore** — for *small key-value settings*: theme, flags, an auth token, last-selected tab, onboarding-seen. A handful of simple values you read and write, not query.","**Room** — for *structured, queryable, relational data*: lists of entities, anything you filter, sort, join, or that has relationships. A cached feed, saved items, a local database of records.","**File system** — for *large files and binary blobs*: images, videos, downloaded documents, cached media. Internal storage (private to your app) or external/scoped storage."]},{t:"p",text:"The decision comes down to the data's shape and size. DataStore holds everything in memory and can't be queried, so it's wrong for lists or large data — a few settings, yes; a hundred cached articles, no (that's Room). Room is for structured data you need to query. Files are for large binary content that doesn't belong in a database or key-value store. A common mistake is stuffing a serialized list into SharedPreferences/DataStore as a JSON string — it works for tiny data but doesn't scale and can't be queried; use Room instead. One more consideration: none of these encrypt data by default, so for sensitive values (tokens) you'd add encryption (EncryptedSharedPreferences, or encrypt before storing, or the Keystore for keys)."}]},{level:"senior",q:"Why exactly does SharedPreferences cause ANRs, and how does DataStore's design prevent that?",a:[{t:"p",text:"**SharedPreferences causes ANRs because of *when and where* its I/O happens: the first read of a preferences file synchronously loads and parses the entire XML file on the calling thread, and certain writes force a synchronous disk sync on the main thread.** The two specific mechanisms:"},{t:"list",items:["**Synchronous first load**: `getSharedPreferences` returns immediately, but the XML file is loaded lazily on first *access* (`getString`, etc.), and that load/parse is *blocking*. If it happens on the main thread (common — reading a setting to configure UI), and the file is large or the disk is busy, the main thread blocks. Enough blocking → ANR.","**`apply()` and lifecycle fsync**: `apply()` writes to memory immediately and schedules the disk write asynchronously — but Android *waits* for all pending `apply()` disk writes to complete during certain lifecycle transitions (notably `Activity.onPause`/`onStop` and service commands), *on the main thread*. So a burst of `apply()` calls can turn into a blocking `fsync` at `onPause`, stalling the transition — a well-documented ANR source. `commit()` is even worse (synchronous by design)."]},{t:"list",items:["**DataStore's design eliminates both**: it's built on coroutines, so *all* I/O — reads and writes — happens on a background dispatcher (`Dispatchers.IO` internally), never on the main thread. Reads are a `Flow` you collect (suspending, off-main); writes are `suspend` functions (off-main). There is no synchronous main-thread I/O path at all, so the blocking-load and fsync-at-onPause problems simply can't occur.","**No lifecycle-coupled fsync**: because writes are already async coroutine operations that you `await` where appropriate, there's no hidden 'block the main thread at onPause to flush pending writes' behavior.","**Transactional consistency**: DataStore serializes writes through a single actor and guarantees atomic updates, so it also avoids the partial-write/corruption issues SharedPreferences could have — without any main-thread cost."]},{t:"p",text:"**The senior framing**: the SharedPreferences ANR problem isn't 'it's slow', it's that its *synchronous API forces disk I/O onto the main thread* at read time and at lifecycle-coupled flush time. DataStore fixes it at the API level — by making I/O exclusively asynchronous through coroutines, there's no way to accidentally do main-thread disk I/O. That architectural shift (from a synchronous API to a coroutine/Flow-based one) is the whole reason DataStore exists and why it's the recommended replacement, even though for tiny apps the practical ANR risk of SharedPreferences is small."}]},{level:"senior",q:"How would you migrate from SharedPreferences to DataStore in a shipping app without losing data?",a:[{t:"p",text:"**DataStore provides a built-in `SharedPreferencesMigration` that automatically imports existing SharedPreferences data into DataStore the first time the DataStore is accessed, so you migrate without manual copying or data loss.** You configure the DataStore with the migration, pointing at the old SharedPreferences file(s), and DataStore reads the old values in and (optionally) deletes the old file once migrated."},{t:"code",title:"Configuring the automatic migration",code:`val Context.dataStore by preferencesDataStore(
    name = "settings",
    produceMigrations = { context ->
        listOf(SharedPreferencesMigration(context, "old_prefs_name"))
    }
)
// On first access, values from 'old_prefs_name' are copied into DataStore.`},{t:"list",items:["**The migration runs once, lazily**: the first time DataStore's `data` Flow is collected or edited, the migration copies the SharedPreferences keys/values into DataStore, then marks the file as migrated (and can clean it up). Subsequent accesses skip it. So it's transparent and one-time.","**Do it incrementally, not all at once**: in a large app, you don't rewrite every SharedPreferences usage in one PR. Migrate one settings area at a time — set up the DataStore with the migration, switch the *reads and writes* for that area to DataStore, ship, verify, then move to the next. The migration handles the data; you handle the code call sites gradually.","**Watch for concurrent access during transition**: while migrating, make sure code isn't *still writing* to the old SharedPreferences after DataStore has taken over (or those writes would be lost/ignored) — the migration is a one-way import. Route all access for a migrated key through DataStore.","**Test the migration path**: write a test (or manual QA) that starts with a populated old SharedPreferences file, accesses DataStore, and verifies the values came through correctly — since this touches real user settings on update, you want to confirm nothing is dropped or type-mismatched.","**Proto DataStore migration**: if migrating to Proto DataStore, you map the old key-value pairs into the typed proto message in the migration's transform — a bit more work but the same one-time-import mechanism."]},{t:"p",text:"**The senior framing**: the migration itself is a solved problem (`SharedPreferencesMigration` does the data copy safely and once), so the real work is *rolling it out incrementally* — migrating settings area by area, ensuring each migrated key is accessed *only* through DataStore afterward, and testing that existing users' data survives the update. This lets you modernize a shipping app's storage without a risky big-bang rewrite or any user-facing data loss."}]},{level:"junior",q:"How do you read and write with Preferences DataStore?",a:[{t:"p",text:"Preferences DataStore stores key-value pairs. You *read* by observing `dataStore.data` (a `Flow<Preferences>`) and mapping the key; you *write* with `dataStore.edit { }`, a suspend function that updates atomically. Reads are reactive (emit on change) and writes are transactional."},{t:"code",title:"Read/write",code:`val THEME = stringPreferencesKey("theme")
val theme: Flow<String> = context.dataStore.data.map { it[THEME] ?: "system" }
suspend fun setTheme(value: String) {
    context.dataStore.edit { prefs -> prefs[THEME] = value }
}`},{t:"list",items:["**Read** — observe `dataStore.data` (`Flow<Preferences>`), map by key.","**Write** — `edit { prefs[key] = value }` (suspend, atomic).","**Typed keys** — `stringPreferencesKey`, `intPreferencesKey`, etc.","**Reactive** — reads re-emit when values change."]},{t:"note",text:"Preferences DataStore: read by observing dataStore.data (Flow<Preferences>) mapped by a typed key (stringPreferencesKey/intPreferencesKey/…); write with dataStore.edit { prefs[key] = value } (suspend, atomic). Reads are reactive (re-emit on change); no synchronous get."}]},{level:"junior",q:"Why is DataStore asynchronous, and what does that mean for reading a value?",a:[{t:"p",text:"DataStore is *fully asynchronous* (Flow-based, coroutine-backed) — there's no synchronous `getString` like SharedPreferences. Reading a value means collecting the `Flow` or getting `.first()`; this guarantees you never block the main thread on disk I/O. The trade-off is you can't read a value synchronously in-line; you observe it or suspend."},{t:"code",title:"Getting a one-shot value",code:'val theme: String = context.dataStore.data.map { it[THEME] ?: "system" }.first()   // suspend'},{t:"list",items:["**No synchronous get** — always Flow/suspend (no main-thread blocking).","**Observe** — collect the Flow for reactive updates.","**One-shot** — `.first()` (suspend) for a single read.","**Trade-off** — can't read in-line synchronously; must be in a coroutine."]},{t:"note",text:"DataStore is fully async (Flow/coroutine-backed) — no synchronous getString (so it never blocks the main thread on disk). Read reactively by collecting data, or one-shot with .first() (suspend). Trade-off: no in-line synchronous read; you must observe or suspend."}]},{level:"senior",q:"How do you use Proto DataStore with a typed schema?",a:[{t:"p",text:"Proto DataStore stores a *typed object* defined by a Protocol Buffers schema (a `.proto` file), giving compile-time type safety (no stringly-typed keys). You provide a `Serializer` to read/write the proto, then read `dataStore.data` (a `Flow<YourProto>`) and update with `dataStore.updateData { it.toBuilder()... }`. It's ideal for structured settings."},{t:"code",title:"Proto DataStore",code:`val settings: Flow<Settings> = context.settingsDataStore.data
suspend fun setDarkMode(enabled: Boolean) {
    context.settingsDataStore.updateData { it.toBuilder().setDarkMode(enabled).build() }
}`},{t:"list",items:["**Typed schema** — a `.proto` defines the structure; no string keys.","**`Serializer`** — you supply read/write of the proto.","**`updateData { }`** — atomic typed update.","**Type safety** — compile-time checked fields vs Preferences' strings."]},{t:"note",text:"Proto DataStore stores a typed object from a Protocol Buffers .proto schema (compile-time type safety, no string keys). Provide a Serializer, read dataStore.data (Flow<Proto>), update with updateData { it.toBuilder()...build() }. Ideal for structured settings; Preferences DataStore for simple key-values."}]},{level:"senior",q:"How does DataStore handle concurrent writes and consistency?",a:[{t:"p",text:"DataStore serializes writes — `edit`/`updateData` run transactionally and are *ordered*, so concurrent writers don't corrupt data or lose updates (each sees the latest state). Reads always reflect a consistent snapshot. This is a key improvement over SharedPreferences' `apply()`, where concurrent edits and the commit model could race."},{t:"list",items:["**Serialized writes** — transactional and ordered; no lost updates.","**Consistent reads** — always a coherent snapshot.","**No corruption** — safe under concurrency.","**vs SharedPreferences** — its apply/commit model had races and main-thread risks."]},{t:"note",text:"DataStore serializes writes (edit/updateData are transactional and ordered), so concurrent writers don't corrupt data or lose updates, and reads reflect a consistent snapshot. A key improvement over SharedPreferences' apply/commit model, which could race under concurrent edits."}]},{level:"junior",q:"How do you create a DataStore instance correctly?",a:[{t:"p",text:"Create it *once per file* — use the `preferencesDataStore` (or `dataStore`) property delegate at the *top level* (or provide a singleton via DI). Creating multiple DataStore instances for the same file throws (`IllegalStateException`), because DataStore must have a single owner to guarantee consistency."},{t:"code",title:"Single instance",code:`// Top-level delegate — one instance per file
val Context.dataStore by preferencesDataStore(name = "settings")
// Or DI:
@Provides @Singleton fun provideDataStore(@ApplicationContext ctx: Context) = ctx.dataStore`},{t:"list",items:["**One instance per file** — via the `preferencesDataStore` delegate (top-level) or DI singleton.","**Multiple instances throw** — DataStore enforces single ownership.","**`applicationContext`** — for the singleton.","**Why** — single owner guarantees consistency/ordering."]},{t:"note",text:"Create one DataStore per file — the top-level preferencesDataStore/dataStore property delegate, or a DI @Singleton. Multiple instances for the same file throw (IllegalStateException) because DataStore requires single ownership to guarantee consistency/ordering."}]},{level:"senior",q:"How do you handle read errors (like corrupted data) in DataStore?",a:[{t:"p",text:"DataStore surfaces read errors through the `data` Flow. For `IOException`s (corruption, disk issues), catch them with the `catch` operator and emit a default (`emptyPreferences()` or a default proto). For Proto DataStore, provide a `corruptionHandler` in the serializer to recover. Don't let a read error crash the app — degrade to defaults."},{t:"code",title:"Handling read errors",code:`context.dataStore.data
    .catch { e -> if (e is IOException) emit(emptyPreferences()) else throw e }
    .map { it[THEME] ?: "system" }`},{t:"list",items:["**Errors via the Flow** — `IOException` on corruption/disk issues.","**`catch` + emit default** — `emptyPreferences()`/default proto.","**`corruptionHandler`** — recover corrupted Proto DataStore.","**Don't crash** — degrade to defaults gracefully."]},{t:"note",text:"DataStore read errors come through the data Flow. Catch IOExceptions with .catch and emit a default (emptyPreferences()/default proto); rethrow other exceptions. For Proto DataStore, provide a corruptionHandler in the serializer. Degrade to defaults — don't let a read error crash the app."}]},{level:"junior",q:"How do you store a file, and where should app files live?",a:[{t:"p",text:"For large binary data (images, downloads, exports), write to the file system. Use *internal storage* (`context.filesDir`, `context.cacheDir`) for app-private files — no permission needed, removed on uninstall. Use `cacheDir` for regenerable data the system can clear under pressure. For user-facing files or sharing, use `MediaStore`/SAF. Never store large blobs in DataStore/Room."},{t:"code",title:"App file storage",code:`File(context.filesDir, "report.pdf").writeBytes(bytes)   // private, persists
File(context.cacheDir, "thumb.jpg").writeBytes(bytes)   // private, clearable`},{t:"list",items:["**`filesDir`** — app-private persistent files; no permission; removed on uninstall.","**`cacheDir`** — regenerable data; system can clear it.","**MediaStore/SAF** — user-facing/shared files.","**Not DataStore/Room** — for large blobs use the file system."]},{t:"note",text:"Store large binary data in the file system: filesDir (app-private, persistent, no permission, removed on uninstall), cacheDir (regenerable, system-clearable). Use MediaStore/SAF for user-facing/shared files. Never put large blobs in DataStore/Room — store the file and keep a path/id in the DB."}]},{level:"senior",q:"How do you store an encrypted value (like an auth token)?",a:[{t:"p",text:"Don't store secrets in plain DataStore/SharedPreferences. Use the Android *Keystore* to hold cryptographic keys and encrypt the value before storing it, or use `EncryptedSharedPreferences` (Jetpack Security) / an encrypted DataStore wrapper. The Keystore keeps the key hardware-backed and non-exportable, so even a rooted device can't easily extract it."},{t:"list",items:["**Android Keystore** — hardware-backed key storage; encrypt/decrypt values.","**`EncryptedSharedPreferences`** — Jetpack Security (though maintenance-mode; consider Keystore + DataStore).","**Never plaintext** — tokens/passwords shouldn't be in plain prefs/DataStore.","**Short-lived tokens** — prefer refresh tokens + secure storage over long-lived secrets."]},{t:"note",text:"Never store secrets (tokens) in plain DataStore/SharedPreferences. Use the Android Keystore (hardware-backed, non-exportable keys) to encrypt values before storing, or EncryptedSharedPreferences (Jetpack Security). Prefer short-lived tokens + secure refresh over long-lived plaintext secrets."}]},{level:"senior",q:"How do you observe and combine multiple DataStore values?",a:[{t:"p",text:"Since `dataStore.data` is a `Flow`, map each value out and use `combine` to produce a derived settings object that emits whenever any value changes. Expose it as a `Flow`/`StateFlow` the UI collects — giving reactive, always-current settings without manual synchronization."},{t:"code",title:"Combined settings flow",code:`val settings: Flow<Settings> = context.dataStore.data.map { prefs ->
    Settings(
        theme = prefs[THEME] ?: "system",
        notifications = prefs[NOTIFS] ?: true,
    )
}   // one map over all keys — emits when any changes`},{t:"list",items:["**Map the Preferences** — read multiple keys in one `map`.","**Emits on any change** — the whole object re-emits.","**`combine`** — for values across *different* DataStores.","**Expose reactively** — as a Flow/StateFlow the UI collects."]},{t:"note",text:"dataStore.data is a Flow, so map multiple keys into a derived settings object in one map (emits when any key changes), or combine across different DataStores. Expose as a Flow/StateFlow the UI collects — reactive, always-current settings without manual sync."}]},{level:"junior",q:"How do you clear or remove values from DataStore?",a:[{t:"p",text:"In an `edit { }` block, remove a single key with `prefs.remove(key)` or wipe everything with `prefs.clear()`. Both are transactional. For Proto DataStore, `updateData { defaultInstance }` resets to defaults. Use this for logout (clear tokens/user prefs) or a settings reset."},{t:"code",title:"Clearing",code:`context.dataStore.edit { prefs ->
    prefs.remove(TOKEN)     // remove one key
    // or prefs.clear()     // remove all
}`},{t:"list",items:["**`prefs.remove(key)`** — delete one value.","**`prefs.clear()`** — wipe all values.","**Proto** — `updateData { defaultInstance }` resets.","**Uses** — logout, settings reset; transactional."]},{t:"note",text:"In edit { }: prefs.remove(key) for one value, prefs.clear() for all (transactional). Proto DataStore: updateData { defaultInstance } resets to defaults. Use for logout (clear token/user prefs) or settings reset."}]},{level:"senior",q:"How do you test code that uses DataStore?",a:[{t:"p",text:"Abstract DataStore access behind a repository interface you can fake in unit tests, *or* create a real DataStore backed by a temporary file in a test (using a test `CoroutineScope` and a temp dir) and exercise it with `runTest`. The repository-abstraction approach keeps ViewModel/logic tests fast; the real-DataStore approach tests the actual read/write behavior."},{t:"list",items:["**Repository abstraction** — fake it in unit/ViewModel tests (fast).","**Real DataStore + temp file** — test actual read/write with a temp dir + test scope.","**`runTest`** — drive the coroutines deterministically.","**Clean up** — delete the temp file after."]},{t:"note",text:"Test DataStore either by abstracting it behind a repository interface you fake (fast logic tests), or by creating a real DataStore backed by a temp file with a test CoroutineScope and exercising it under runTest (tests actual read/write). Clean up the temp file after."}]},{level:"junior",q:"What data types can Preferences DataStore store?",a:[{t:"p",text:"Preferences DataStore supports the same primitives as SharedPreferences: `Int`, `Long`, `Float`, `Double`, `Boolean`, `String`, and `Set<String>` — via typed key factories (`intPreferencesKey`, `stringSetPreferencesKey`, etc.). For structured/complex data (objects, lists), either serialize to a JSON String or use *Proto* DataStore (typed)."},{t:"list",items:["**Primitives** — Int/Long/Float/Double/Boolean/String/Set<String>.","**Typed keys** — `intPreferencesKey`, `stringSetPreferencesKey`, etc.","**Complex data** — JSON String, or use Proto DataStore.","**No arbitrary objects** — Preferences is key-value primitives only."]},{t:"note",text:"Preferences DataStore stores primitives (Int/Long/Float/Double/Boolean/String/Set<String>) via typed key factories. For objects/lists, serialize to a JSON String or use Proto DataStore (typed). It's key-value primitives only — no arbitrary objects."}]},{level:"senior",q:"What are the downsides or limitations of DataStore?",a:[{t:"p",text:"DataStore isn't a database: it has *no querying, indexing, or partial updates* — reading always deserializes the whole file, so it's unsuitable for large datasets (performance degrades as the file grows). It's also fully async (no synchronous read), which can be awkward when a value is needed immediately (e.g. very early startup). Use Room for large/queryable data."},{t:"list",items:["**No querying/indexing** — not a database; whole-file read.","**Scales poorly** — large data slows reads (deserialize everything).","**Async only** — no synchronous read (awkward for immediate startup needs).","**Use Room** — for large, queryable, or relational data."]},{t:"note",text:"DataStore isn't a database: no querying/indexing/partial updates (whole-file read/deserialize), so it scales poorly for large data. It's async-only (no synchronous read), awkward when a value is needed immediately (early startup). Use Room for large/queryable/relational data; DataStore for small settings."}]},{level:"junior",q:"How do you provide a default value when a key is absent?",a:[{t:"p",text:"When mapping the `Preferences`, use the Elvis operator to supply a default — `prefs[KEY] ?: default` — since `prefs[KEY]` returns `null` for an absent key. This gives you a sensible value on first launch (before anything is written) without special-casing."},{t:"code",title:"Defaults",code:`val fontScale: Flow<Float> = dataStore.data.map { it[FONT_SCALE] ?: 1.0f }
val onboarded: Flow<Boolean> = dataStore.data.map { it[ONBOARDED] ?: false }`},{t:"list",items:["**`prefs[KEY] ?: default`** — absent key returns null; provide a default.","**First launch** — sensible values before any write.","**Consistency** — define defaults in one place (a settings mapper).","**Proto** — defaults come from the schema's default field values."]},{t:"note",text:"Map with prefs[KEY] ?: default — an absent key returns null, so Elvis supplies the default (sensible first-launch values without special-casing). Proto DataStore gets defaults from the schema's default field values. Centralize defaults in a settings mapper."}]},{level:"senior",q:"What is the difference between DataStore's edit and updateData?",a:[{t:"p",text:"`edit { }` is the Preferences DataStore API — it gives you a `MutablePreferences` to set/remove keys. `updateData { }` is the Proto DataStore API — it gives you the current typed object and you return a new one (via the builder). Both are suspend, atomic, and transactional; they differ only by which DataStore flavor you're using."},{t:"code",title:"edit vs updateData",code:`// Preferences DataStore
prefsDataStore.edit { it[KEY] = value }
// Proto DataStore
protoDataStore.updateData { current -> current.toBuilder().setField(value).build() }`},{t:"list",items:["**`edit { }`** — Preferences DataStore; mutate a `MutablePreferences`.","**`updateData { }`** — Proto DataStore; return a new typed object.","**Both** — suspend, atomic, transactional.","**Flavor-specific** — same guarantees, different API shape."]},{t:"note",text:"edit { } (Preferences DataStore — mutate MutablePreferences) vs updateData { } (Proto DataStore — return a new typed object via the builder). Both are suspend, atomic, and transactional; they differ only by the DataStore flavor. Same guarantees, different API shape."}]},{level:"senior",q:"How does DataStore persist data on disk, and how are writes made durable?",a:[{t:"p",text:"DataStore serializes its data to a single file in the app's private directory. Writes are made *durable and atomic* by writing to a *temporary file* and then *renaming* it over the target (an atomic filesystem operation) — so a crash mid-write can't corrupt the existing data (you either have the old or the new file, never a partial one). This is safer than SharedPreferences' XML writes."},{t:"list",items:["**Single file** — in app-private storage (Preferences or serialized proto).","**Write-then-rename** — atomic; a crash can't leave a partial file.","**Durable** — old or new content, never corrupt/partial.","**Safer than SharedPreferences** — which could corrupt on crash."]},{t:"note",text:"DataStore serializes to a single app-private file, making writes atomic/durable via write-to-temp-then-rename (an atomic FS op) — a crash mid-write can't corrupt data (old or new file, never partial). Safer than SharedPreferences' XML writes."}]},{level:"junior",q:"How do you expose DataStore-backed settings to the UI cleanly?",a:[{t:"p",text:"Wrap DataStore in a *repository* that exposes settings as `Flow`s (for reads) and suspend functions (for writes), hiding the DataStore API. The ViewModel collects the flows into UI state and calls the repository's write functions. This keeps DataStore details out of the UI and makes it testable (fake the repository)."},{t:"code",title:"Settings repository",code:`class SettingsRepository(private val dataStore: DataStore<Preferences>) {
    val theme: Flow<Theme> = dataStore.data.map { Theme.from(it[THEME]) }
    suspend fun setTheme(theme: Theme) { dataStore.edit { it[THEME] = theme.name } }
}`},{t:"list",items:["**Repository** — expose `Flow`s (reads) + suspend functions (writes).","**Hide DataStore** — the UI/ViewModel doesn't touch keys.","**ViewModel** — collect flows into state; call write functions.","**Testable** — fake the repository in tests."]},{t:"note",text:"Wrap DataStore in a repository exposing settings as Flows (reads) + suspend functions (writes), hiding the DataStore API and keys. The ViewModel collects the flows into UI state and calls the write functions. Keeps DataStore out of the UI and is testable (fake the repository)."}]},{level:"senior",q:"How would you implement feature flags with DataStore or remote config?",a:[{t:"p",text:"For *local* flags (user toggles, A/B assignment cache), store them in DataStore behind a repository exposing `Flow<Boolean>` per flag. For *remotely-controlled* flags, use Firebase Remote Config (or your backend), cache the fetched values in DataStore for offline/instant access, and expose a unified flag repository. The UI observes flags reactively so toggling updates immediately."},{t:"list",items:["**Local flags** — DataStore behind a repository (`Flow<Boolean>`).","**Remote flags** — Remote Config/backend, cached in DataStore.","**Unified repository** — one source the app queries.","**Reactive** — observe flags so changes apply without restart."]},{t:"note",text:"Feature flags: local ones in DataStore behind a repository (Flow<Boolean> per flag); remote ones via Firebase Remote Config/backend cached in DataStore for offline/instant access. Expose a unified flag repository the UI observes reactively so toggles apply immediately."}]},{level:"junior",q:"How do you implement a first-launch or onboarding-completed flag?",a:[{t:"p",text:"Store a boolean in DataStore (e.g. `onboarding_completed`) defaulting to `false`. On app start, observe it: if false, show onboarding; when the user finishes, write `true`. Because it's persisted, onboarding shows only once (until data is cleared/reinstalled). Using DataStore (not a runtime variable) makes it survive process death and restarts."},{t:"code",title:"Onboarding flag",code:`val onboarded: Flow<Boolean> = dataStore.data.map { it[ONBOARDED] ?: false }
suspend fun completeOnboarding() { dataStore.edit { it[ONBOARDED] = true } }`},{t:"list",items:["**Persisted boolean** — default false; set true on completion.","**Observe at start** — show onboarding while false.","**Survives restart/process death** — because it's persisted.","**Reset** — cleared on app data wipe/reinstall."]},{t:"note",text:"Store onboarding_completed as a persisted boolean (default false); observe it at start to show onboarding while false, and write true on completion. Persistence (DataStore, not a runtime var) means it survives process death/restart and shows only once until data is cleared."}]},{level:"senior",q:"What happens to DataStore data on uninstall, clear-data, and auto-backup?",a:[{t:"p",text:"DataStore files live in app-private storage, so they're *deleted on uninstall* and on 'Clear data'. They *can* be included in Android *Auto Backup* (cloud backup, restored on reinstall on a new device) unless you exclude them — which matters for sensitive data (you should exclude tokens/secrets from backup via `backup_rules`). So don't rely on DataStore surviving uninstall, and exclude secrets from backup."},{t:"list",items:["**Uninstall/clear-data** — DataStore files deleted (private storage).","**Auto Backup** — included by default; restored on reinstall unless excluded.","**Exclude secrets** — configure backup rules to omit tokens/sensitive prefs.","**Don't rely on persistence** — across uninstall."]},{t:"note",text:"DataStore files (app-private) are deleted on uninstall and 'Clear data'. They're included in Android Auto Backup by default (restored on reinstall) unless excluded — exclude tokens/secrets via backup rules. Don't rely on DataStore surviving uninstall; exclude sensitive data from backup."}]},{level:"senior",q:"How do you evolve a Proto DataStore schema safely?",a:[{t:"p",text:"Follow Protocol Buffers evolution rules: *add* new fields with new field numbers (old data reads them as defaults); *never reuse or change* existing field numbers or types; *don't remove* required semantics. Because proto is backward/forward compatible when you follow these rules, old serialized data deserializes fine after you add fields — no explicit migration needed for additive changes."},{t:"list",items:["**Add fields with new numbers** — old data defaults them.","**Never reuse/change field numbers or types** — breaks deserialization.","**Additive changes are safe** — proto is backward/forward compatible.","**Migrations** — needed only for semantic transformations, via the serializer."]},{t:"note",text:"Evolve Proto DataStore per protobuf rules: add fields with NEW field numbers (old data reads them as defaults), never reuse/change existing numbers or types. Additive changes are backward/forward compatible — no explicit migration needed. Semantic transformations require handling in the serializer."}]},{level:"senior",q:"Can DataStore be accessed from multiple processes?",a:[{t:"p",text:"Standard DataStore is *not* multi-process safe — a single DataStore instance must own its file, and accessing the same file from multiple processes can corrupt data or miss updates. For genuine multi-process needs there's a *multi-process DataStore* variant (with `MultiProcessDataStoreFactory`), but most apps are single-process and should keep DataStore access there. Avoid `android:process` splits touching the same DataStore otherwise."},{t:"list",items:["**Not multi-process by default** — one owner per file per process.","**Corruption/missed updates** — if two processes touch the same file.","**Multi-process variant** — `MultiProcessDataStoreFactory` for real needs.","**Most apps** — single-process; keep DataStore there."]},{t:"note",text:"Standard DataStore isn't multi-process safe (one owner per file; two processes touching it can corrupt/miss updates). For genuine multi-process needs use MultiProcessDataStoreFactory. Most apps are single-process — keep DataStore access there and avoid android:process splits sharing a DataStore."}]},{level:"junior",q:"What's the difference between DataStore and an in-memory StateFlow cache?",a:[{t:"p",text:"A `StateFlow` (or in-memory cache) holds state only while the *process is alive* — it's lost on process death/restart. DataStore *persists* to disk, so values survive restarts. Often you use both: DataStore as the persistent source of truth, and a `StateFlow` (via `stateIn`) as the in-memory reactive view the UI collects. Use DataStore for anything that must persist; StateFlow alone for ephemeral UI state."},{t:"list",items:["**StateFlow/in-memory** — lives only while the process runs; lost on death.","**DataStore** — persisted to disk; survives restarts.","**Combine** — DataStore (source of truth) → `stateIn` StateFlow (reactive view).","**Choose** — persist (DataStore) vs ephemeral (StateFlow)."]},{t:"note",text:"A StateFlow/in-memory cache lives only while the process runs (lost on process death); DataStore persists to disk (survives restarts). Often combine them: DataStore as the persistent source of truth, a stateIn StateFlow as the reactive in-memory view. Persist → DataStore; ephemeral UI state → StateFlow."}]},{level:"senior",q:"How do you build a settings screen end-to-end with DataStore?",a:[{t:"p",text:"Layer it: a *SettingsRepository* exposes each setting as a `Flow` (read) and a suspend setter (write) over DataStore; the *ViewModel* combines the flows into a `SettingsUiState` (`stateIn`) and exposes handlers calling the repository; the *UI* collects the state and renders toggles/pickers that invoke the handlers. Changes write to DataStore, which re-emits, updating the UI reactively — a clean unidirectional loop."},{t:"code",title:"Settings flow",code:`// Repository: Flow reads + suspend writes over DataStore
// ViewModel:
val uiState = repo.settingsFlow.stateIn(viewModelScope, WhileSubscribed(5000), Default)
fun onToggleDarkMode(on: Boolean) = viewModelScope.launch { repo.setDarkMode(on) }
// UI: Switch(checked = uiState.darkMode, onCheckedChange = viewModel::onToggleDarkMode)`},{t:"list",items:["**Repository** — `Flow` reads + suspend writes over DataStore.","**ViewModel** — `stateIn` combined state + handlers.","**UI** — toggles/pickers bound to state and handlers.","**Reactive loop** — write → DataStore re-emits → UI updates."]},{t:"note",text:"Settings end-to-end: SettingsRepository (Flow reads + suspend writes over DataStore) → ViewModel (stateIn combined SettingsUiState + handlers calling the repo) → UI (toggles bound to state/handlers). A change writes to DataStore, which re-emits, updating the UI reactively — a clean UDF loop."}]}],PA={id:"datastore-prefs",title:"DataStore & SharedPreferences",tagline:"SharedPreferences and its ANR/reactivity flaws, DataStore's coroutine/Flow design, Preferences vs Proto DataStore, the storage decision, and migration.",tags:["DataStore","SharedPreferences","Preferences","Storage","ANR"],content:IA,qa:MA},DA=[{heading:"Why caching and offline-first matter",blocks:[{t:"p",text:"Mobile networks are unreliable — slow, intermittent, or absent. An app that shows a spinner and nothing else when offline is a poor experience. **Caching** means storing data locally so it's available instantly and without the network; **offline-first** is an architecture where the app is *designed* to work from local data by default, treating the network as a way to *update* that local data rather than a prerequisite for showing anything. Done well, the app feels fast (instant local reads), works offline, and uses less data/battery."}]},{heading:"Single source of truth — the foundation",blocks:[{t:"p",text:"Offline-first is built on the **single source of truth (SSOT)** pattern: one local store (usually a Room database) is the *authoritative* source the app reads from, and everything — including network responses — writes *into* it. The UI never reads directly from the network; it observes the local store, which the network updates. This is the same pattern covered in Room and Architecture, and it's what makes offline-first natural."},{t:"code",title:"The SSOT / observe-local, refresh-remote pattern",code:`class ArticleRepository(
    private val dao: ArticleDao,     // the single source of truth
    private val api: Api,
) {
    // READS always come from the local DB — works offline, instant
    fun observeArticles(): Flow<List<Article>> =
        dao.observeArticles().map { it.map(Entity::toDomain) }

    // NETWORK writes into the DB; the Flow above re-emits automatically
    suspend fun refresh(): Result<Unit> = runCatching {
        val fresh = api.getArticles()
        dao.upsertAll(fresh.map { it.toEntity() })
    }
}`},{t:"list",items:["**Reads never depend on the network**: the UI observes the DB Flow, so it always has *something* to show (the last-cached data), even offline. When a refresh succeeds, the DB updates and the UI follows automatically.","**Writes go into the DB**: a network fetch is just a mechanism to *update the local source of truth*. This decouples 'display data' from 'fetch data' — the two happen independently.","**Errors don't blank the screen**: if a refresh fails (offline), you still show the cached data and surface the error non-destructively (a snackbar), rather than an empty error screen."]}]},{heading:"Caching strategies",blocks:[{t:"table",headers:["Strategy","How it works","Trade-off"],rows:[["Cache-first (offline-first)","read cache immediately; refresh from network in background","instant + offline, but may briefly show stale data"],["Network-first","try network; fall back to cache on failure","freshest data, but slow/blocked when network is slow"],["Cache-then-network","show cache instantly, then update with network result","best UX — fast AND fresh (the common choice)"],["Network-only","always fetch, no cache","always fresh, but no offline and slow"]]},{t:"list",items:["**Cache-then-network** (a.k.a. stale-while-revalidate) is the most common for offline-first: show the cached data immediately (instant, works offline), and *simultaneously* kick off a network refresh that updates the cache (and thus the UI) when it returns. The user sees content instantly and it silently updates. The SSOT Flow pattern gives this naturally — observe the DB (instant cache), trigger a refresh (updates DB → UI).","**Network-first** suits data that must be current (a bank balance) where showing stale data is worse than waiting — but degrade to cache on failure.","**Choose per data type**: a feed → cache-then-network; a rarely-changing config → cache-first with occasional refresh; a payment confirmation → network-only or network-first."]}]},{heading:"Cache invalidation and freshness",blocks:[{t:"p",text:'"There are only two hard things in computer science: cache invalidation and naming things." The core challenge of caching is knowing *when* cached data is too stale to trust. Strategies:'},{t:"list",items:["**Time-based (TTL)**: store a timestamp with cached data; consider it stale after N minutes and refresh. Simple and common — 'refresh if the cache is older than 5 minutes'.","**Event-based**: invalidate on specific triggers — a pull-to-refresh, a push notification signaling new data, a user action that changes the data.","**Version/ETag-based**: the server provides a version tag (ETag); on refresh you send it, and the server responds 'not modified' (cheap) or with new data — efficient conditional fetching (HTTP caching).","**Manual/explicit**: the app decides when to refresh (on screen open, on app foreground, on a timer). Common combined with TTL.","**The trade-off**: refresh too often → wasted data/battery and unnecessary load; refresh too rarely → stale data. TTL + explicit refresh (pull-to-refresh) covers most cases."]}]},{heading:"Offline writes and syncing",blocks:[{t:"p",text:"Reading offline is straightforward (serve from cache). *Writing* offline is harder — the user creates or edits data with no network. Offline-first apps handle this with **local-first writes and background sync**:"},{t:"list",items:["**Write locally first**: apply the change to the local DB immediately (so the UI updates and the user isn't blocked), and mark the record as 'pending sync' (a `syncStatus` flag or a separate outbox table).","**Sync in the background**: a background worker (WorkManager, so it survives process death and runs when network returns) reads pending changes and pushes them to the server, then marks them synced. WorkManager's network constraint means it runs the sync automatically when connectivity is available.","**Conflict resolution**: when local and server versions diverge (the record changed on the server while you were offline), you need a policy — last-write-wins (simplest), server-wins, client-wins, or a merge. This is the genuinely hard part of offline sync and is a repository/domain concern.","**The outbox pattern**: queue outgoing operations durably (in the DB) so they survive app kills and are retried until they succeed — a reliable way to guarantee eventual delivery of offline changes."]}]},{heading:"HTTP caching and memory caching",blocks:[{t:"list",items:["**HTTP-level caching (OkHttp)**: OkHttp can cache HTTP *responses* automatically based on `Cache-Control`/`ETag` headers (`Cache` set on the client). This caches at the network layer — useful for images and responses that rarely change — and is complementary to app-level DB caching (which gives you queryable, structured, observable data).","**Memory cache**: an in-memory cache (a `Map`, an `LruCache`) for the fastest access to recently-used data, backed by the DB (disk) which is backed by the network — a three-tier hierarchy (memory → disk → network). Image loaders (Coil, Glide) do exactly this: memory cache → disk cache → network.","**Image caching specifically**: use a library (Coil/Glide) — they handle memory + disk caching, downsampling, and lifecycle automatically. Never hand-roll image caching.","**Layering**: a mature app has memory cache (fastest, volatile) → Room/disk (persistent, queryable, SSOT) → network (source of new data). Reads try the fastest available; writes populate the layers."]},{t:"note",text:"Offline-first essentials: build on SSOT (Room as authoritative; UI observes DB, network writes into it). Cache-then-network (show cache instantly, refresh in background) is the common strategy. Invalidate with TTL/events/ETags — balancing freshness vs waste. Offline writes go local-first + a WorkManager background sync (with a conflict-resolution policy and an outbox for durability). Layer caches memory → disk → network; use Coil/Glide for images."}]}],RA=[{level:"junior",q:"What does 'offline-first' mean and why is it good?",a:[{t:"p",text:"**Offline-first is an architecture where the app is designed to work from *local data* by default, treating the network as a way to *update* that local data rather than a prerequisite for showing anything.** Instead of 'fetch from network → show result (or spinner/error if offline)', the flow is 'show local data instantly → update it from the network in the background when possible'."},{t:"list",items:["**It feels fast**: reads come from a local database instantly, with no network round-trip — the UI appears immediately with cached content.","**It works offline**: because the app displays local data, it's usable with no connection — you see the last-synced content instead of an error screen.","**It's resilient**: a failed network refresh doesn't blank the screen; you keep showing cached data and surface the error gently.","**It saves data and battery**: you fetch only to update, not on every view, and can batch/schedule syncs efficiently."]},{t:"p",text:"The foundation is the *single source of truth* pattern: a local store (usually Room) is authoritative, the UI observes it, and the network writes into it. This is Google's recommended architecture because mobile networks are unreliable, and an app that gracefully handles being offline is far better than one that's helpless without a connection."}]},{level:"junior",q:"What is the single source of truth pattern and how does it enable offline support?",a:[{t:"p",text:"**Single source of truth (SSOT) means one local store — typically a Room database — is the *authoritative* source the app reads from, and everything (including network responses) writes *into* it.** The UI never reads directly from the network; it observes the local store, and the network's only job is to keep that store updated."},{t:"code",title:"Observe local, refresh remote",code:`fun observeData(): Flow<List<Item>> = dao.observeAll()  // UI reads the DB
suspend fun refresh() { dao.upsertAll(api.fetch()) }    // network writes to DB`},{t:"p",text:"**How it enables offline support**: since the UI always reads from the local DB, it *always has data to show* — the last-cached data — regardless of connectivity. The network being available or not only affects whether a *refresh* succeeds; it never affects whether the app can display something. When you're online and a refresh succeeds, the DB updates and (because Room's Flow queries auto-emit) the UI updates automatically. When you're offline, you simply see the cached data and can surface the connection error without wiping the screen. This cleanly separates 'displaying data' (always from the DB) from 'fetching data' (an independent update mechanism), which is exactly what makes offline behavior natural rather than a special case you bolt on."}]},{level:"junior",q:"What is 'cache-then-network' and why is it a popular strategy?",a:[{t:"p",text:"**Cache-then-network means: show the cached data immediately, and *simultaneously* start a network request that updates the cache (and thus the UI) when it returns.** The user gets instant content from the cache, and it silently refreshes to the latest data a moment later. It's also called 'stale-while-revalidate'."},{t:"p",text:"**Why it's popular**: it gives the best of both worlds — *fast* (no waiting for the network to see something) and *fresh* (the data updates to current). Compare the alternatives: network-first makes the user wait for the network before seeing anything (slow, and blocked when offline); cache-only never updates (stale); network-only is always slow and has no offline support. Cache-then-network avoids all those downsides. And it falls out *naturally* from the SSOT pattern: the UI observes the database (so it shows the cache instantly), and you trigger a refresh that writes to the database (so the UI updates when the network returns). The main trade-off is a brief moment of potentially-stale data before the refresh completes — acceptable for most content like feeds, though not for data that must always be current (a bank balance), where you'd use network-first instead."}]},{level:"junior",q:"How should you cache images in an Android app?",a:[{t:"p",text:"**Use an established image-loading library — Coil (Kotlin-first, coroutine-based) or Glide — and let it handle caching; never hand-roll image caching.** These libraries automatically manage a two-tier cache: a *memory cache* (an in-memory LRU cache of decoded bitmaps for the fastest repeat access) and a *disk cache* (persisted encoded images so they survive app restarts and don't need re-downloading), falling back to the network only when neither has the image."},{t:"list",items:["They also handle the hard parts you'd otherwise get wrong: downsampling images to the target view size (avoiding huge bitmaps and out-of-memory crashes), lifecycle awareness (cancelling loads when the view/Activity is gone), placeholder/error images, and request deduplication.","In Compose, Coil's `AsyncImage` / `rememberAsyncImagePainter` integrates directly; in Views, `imageView.load(url)` (Coil) or `Glide.with(...).load(url)`.","The reason not to hand-roll: correct image caching involves memory management, bitmap decoding/downsampling, disk eviction policies, and lifecycle handling — all easy to get subtly wrong (OOM crashes, leaks, jank). The libraries have solved this thoroughly."]},{t:"p",text:"This fits the general caching hierarchy — memory (fastest, volatile) → disk (persistent) → network (source) — applied specifically to images. It's complementary to your app-data caching (Room), which handles structured data; images are binary blobs best left to a dedicated loader."}]},{level:"senior",q:"How do you handle cache invalidation — deciding when cached data is too stale?",a:[{t:"p",text:"**Cache invalidation is deciding when cached data can no longer be trusted and must be refreshed — famously one of the hard problems in computing, because the goal is a balance: refresh too often and you waste data/battery and load; refresh too rarely and users see stale data.** There are several strategies, usually combined:"},{t:"list",items:["**Time-based (TTL)**: store a timestamp alongside cached data and treat it as stale after a defined interval ('refresh if older than 5 minutes'). Simple, predictable, and covers most cases. You check the age on access or on screen open and refresh if expired.","**Event-based**: invalidate on specific triggers — an explicit pull-to-refresh, a push notification signaling that server data changed, or a user action that you know altered the data. This keeps caches fresh precisely when it matters without polling.","**Version/ETag-based (conditional fetch)**: the server tags data with a version (ETag or Last-Modified). On refresh you send the tag; the server replies '304 Not Modified' (cheap, no body) if nothing changed, or sends new data. This makes refreshing *efficient* — you 'refresh' frequently but only pay for actual changes. HTTP caching in OkHttp supports this at the network layer.","**App-lifecycle triggers**: refresh on app foreground, on screen entry, or on a periodic background sync (WorkManager) — often combined with TTL so you don't refresh redundantly."]},{t:"list",items:["**Choose per data type**: a rarely-changing config can have a long TTL (hours); a live feed a short one (minutes) plus pull-to-refresh; a chat needs push-based invalidation (real-time). Match the strategy to how fast the data actually changes and how bad staleness is.","**Combine strategies**: a robust approach is TTL (baseline freshness) + explicit refresh (user control) + ETags (efficient checks) + push invalidation for critical real-time data.","**Don't over-engineer**: for many apps, 'refresh on screen open if the cache is older than N minutes, plus pull-to-refresh' is entirely sufficient. Reserve ETags/push invalidation for cases where efficiency or real-time freshness genuinely matter."]},{t:"p",text:"**The senior framing**: there's no universal answer — invalidation is a per-data-type trade-off between freshness and cost. The skill is picking the *cheapest* strategy that meets the data's freshness requirement: TTL for most, ETags when you refresh often and want efficiency, push/event-based for real-time. And it's why the SSOT pattern helps — invalidation just means 'trigger a refresh that updates the DB', and the observing UI follows automatically; you're never manually reconciling cache and UI."}]},{level:"senior",q:"How do you handle writes that happen while the user is offline?",a:[{t:"p",text:"**The pattern is local-first writes plus background sync: apply the change to the local database *immediately* (so the UI updates and the user isn't blocked), mark it as pending synchronization, and have a durable background worker push it to the server when connectivity returns.** Reading offline is easy (serve from cache); *writing* offline is the genuinely hard part, and this pattern makes it reliable."},{t:"list",items:["**1. Write locally, optimistically**: when the user creates or edits data offline, immediately write it to the local DB (the single source of truth) and flag it — e.g. a `syncStatus = PENDING` column, or an 'outbox' table of queued operations. The UI reflects the change instantly (optimistic update), so the app feels responsive even offline.","**2. Sync in the background with WorkManager**: a worker with a *network constraint* reads the pending changes and sends them to the server, then marks them `SYNCED`. WorkManager is the right tool because it *persists the work across process death and reboots* and *automatically runs when the network is available* — so the sync is guaranteed to eventually happen without the user doing anything. A plain coroutine wouldn't survive the app being killed.","**3. Handle conflicts**: the hard part. While offline, the same record may have changed on the server. When syncing, you need a conflict-resolution policy: *last-write-wins* (compare timestamps, newest wins — simplest), *server-wins*, *client-wins*, or a *field-level merge* (combine non-conflicting changes). This is a domain/repository decision, and there's no universal right answer — it depends on the data's semantics (a collaborative doc needs merging; a single-user note can use last-write-wins).","**4. Use the outbox pattern for durability**: queue outgoing operations as durable records in the DB (not just in-memory), so they survive app kills and are retried with backoff until they succeed. This guarantees *eventual delivery* — no offline change is silently lost."]},{t:"list",items:["**Optimistic UI + rollback**: since you apply the change locally before the server confirms, you need to handle the case where the server *rejects* it (validation failure, conflict lost) — roll back the local change or surface an error, keeping the user informed.","**Idempotency**: give each operation a client-generated id so that retries (WorkManager may retry) don't create duplicates on the server — the server dedupes by the id."]},{t:"p",text:"**The senior framing**: offline writes turn a simple 'send to server' into a distributed-systems problem — you're reconciling two copies of data that can diverge. The robust solution is: write local-first (responsive + durable), queue the operation in an outbox, sync via WorkManager (survives kills, runs on reconnect), resolve conflicts with a policy appropriate to the data, and use idempotency keys so retries are safe. Naming the conflict-resolution challenge and WorkManager's role in guaranteed delivery is what demonstrates you understand offline writes are fundamentally harder than offline reads."}]},{level:"senior",q:"What is the NetworkBoundResource pattern?",a:[{t:"p",text:"NetworkBoundResource is a pattern that unifies cache and network into one reactive flow: it *emits cached data immediately* (from the DB), decides whether to *fetch fresh data* (based on staleness), fetches and *saves it to the DB* (which re-emits via the observable query), and emits loading/error states along the way. The DB is the single source of truth; the network updates it."},{t:"code",title:"The shape",code:`fun user(id: String): Flow<Resource<User>> = flow {
    emit(Resource.Loading)
    val cached = dao.observe(id).first()
    if (shouldFetch(cached)) {
        try { dao.upsert(api.getUser(id)) }
        catch (e: IOException) { emit(Resource.Error(e, cached)) }
    }
    emitAll(dao.observe(id).map { Resource.Success(it) })   // DB is source of truth
}`},{t:"list",items:["**Emit cache first** — instant content from the DB.","**Decide to fetch** — based on staleness (`shouldFetch`).","**Save to DB** — network writes; the observable query re-emits.","**Resource states** — Loading/Success/Error the UI renders."]},{t:"note",text:"NetworkBoundResource unifies cache+network: emit cached data immediately, decide whether to fetch (staleness), fetch and save to the DB (which re-emits via the observable query), emitting Loading/Success/Error. The DB is the single source of truth; the network updates it. Instant cached UI + background refresh."}]},{level:"senior",q:"What are the main caching strategies (cache-first, network-first, stale-while-revalidate)?",a:[{t:"p",text:"Common strategies trade freshness vs speed/offline: *cache-first* (serve cache, only hit network on miss — fast, offline-friendly, may be stale); *network-first* (try network, fall back to cache — fresh, but slow/fails offline); *stale-while-revalidate* (serve cache immediately, refresh in the background — fast *and* eventually fresh, the common mobile choice); *cache-only*/*network-only* for special cases."},{t:"table",headers:["Strategy","Behavior","Use"],rows:[["Cache-first","cache, network on miss","static/rarely-changing data"],["Network-first","network, cache fallback","must-be-fresh data"],["Stale-while-revalidate","cache now + refresh in bg","feeds (fast + fresh)"]]},{t:"list",items:["**Cache-first** — fast/offline; may be stale.","**Network-first** — fresh; slow/fails offline.","**Stale-while-revalidate** — instant cache + background refresh (common).","**Choose per data** — freshness requirements vs speed/offline."]},{t:"note",text:"Cache-first (cache, network on miss — fast/offline, may be stale); network-first (network, cache fallback — fresh, slow/offline-fragile); stale-while-revalidate (serve cache now + refresh in bg — fast AND eventually fresh, common for feeds). Choose per data's freshness needs."}]},{level:"senior",q:"How does HTTP caching (ETag, Cache-Control) work with OkHttp?",a:[{t:"p",text:"OkHttp has a built-in HTTP response cache honoring standard headers: `Cache-Control` (max-age, no-cache), `ETag`/`If-None-Match`, and `Last-Modified`/`If-Modified-Since`. You install a `Cache` on the OkHttpClient; then cacheable responses are stored, and conditional requests (`If-None-Match`) let the server return `304 Not Modified` (no body) when data is unchanged — saving bandwidth. This is transport-level caching, distinct from your DB cache."},{t:"code",title:"OkHttp cache",code:`val client = OkHttpClient.Builder()
    .cache(Cache(File(context.cacheDir, "http"), 10L * 1024 * 1024))   // 10MB
    .build()
// Server sends ETag; OkHttp revalidates with If-None-Match -> 304 reuses cache`},{t:"list",items:["**`Cache` on OkHttpClient** — stores HTTP responses on disk.","**`Cache-Control`** — max-age/no-cache directives control caching.","**`ETag`/`If-None-Match`** — conditional requests → `304 Not Modified`.","**Transport-level** — complements (not replaces) your DB source of truth."]},{t:"note",text:"OkHttp's built-in Cache honors Cache-Control (max-age/no-cache), ETag/If-None-Match, Last-Modified — install a Cache on the client; conditional requests get 304 Not Modified (no body) when unchanged, saving bandwidth. It's transport-level caching, complementary to a DB source of truth."}]},{level:"senior",q:"How do you sync offline writes back to the server (write queue)?",a:[{t:"p",text:"Write to the *local DB first* (optimistic update — the UI reflects it immediately) and enqueue a *sync operation* (mark the row `pending`, or add to an outbox table). A `WorkManager` job (with a network constraint) processes the queue when connectivity returns, sends changes to the server, and marks them synced (or handles conflicts). This gives offline-capable writes that reconcile later."},{t:"list",items:["**Optimistic local write** — update the DB and UI immediately.","**Outbox/pending flag** — track unsynced changes.","**WorkManager (network constraint)** — flush the queue when online.","**Reconcile** — mark synced, resolve conflicts, retry with backoff."]},{t:"note",text:"Offline writes: write to the local DB first (optimistic — UI updates instantly) and enqueue a sync op (pending flag / outbox table). A WorkManager job with a network constraint flushes the queue when online, sends to the server, and marks synced (handling conflicts/retries). Offline-capable writes that reconcile later."}]},{level:"senior",q:"How do you resolve conflicts when offline changes clash with server changes?",a:[{t:"p",text:"When a local change and a server change conflict, you need a resolution policy: *last-write-wins* (by timestamp — simple, can lose data), *server-wins*/*client-wins* (authoritative side), *merge* (field-level or CRDT-based — complex but lossless), or *prompt the user*. Track versions/timestamps to detect conflicts, and choose the policy per data's importance (e.g. server-wins for reference data, merge/prompt for user documents)."},{t:"list",items:["**Detect** — compare versions/timestamps (or use `updatedAt`/vector clocks).","**Last-write-wins** — simplest; may drop a change.","**Server/client-wins** — an authoritative side.","**Merge / prompt** — field-level merge or ask the user (for important data)."]},{t:"note",text:"Conflict resolution policies: last-write-wins (timestamp — simple, lossy), server-wins/client-wins (authoritative side), field-level merge/CRDT (lossless, complex), or prompt the user. Detect via versions/timestamps; choose per data importance (server-wins for reference data, merge/prompt for user documents)."}]},{level:"junior",q:"What are the levels of caching in an Android app (memory, disk, network)?",a:[{t:"p",text:"Caching happens at layers, fastest to slowest: *in-memory* (a `StateFlow`/LruCache — instant, lost on process death), *disk* (Room DB, DataStore, OkHttp cache, image disk cache — survives restarts), and the *network* (the source of truth, slowest). A good architecture serves from the fastest available layer and refreshes from deeper ones (memory → disk → network)."},{t:"list",items:["**In-memory** — LruCache/StateFlow; instant, volatile.","**Disk** — Room/DataStore/OkHttp/image cache; survives restarts.","**Network** — source of truth; slowest.","**Layered** — serve from fastest, refresh from deeper."]},{t:"note",text:"Caching layers fastest→slowest: in-memory (LruCache/StateFlow — instant, lost on process death), disk (Room/DataStore/OkHttp/image cache — survives restarts), network (source of truth, slowest). Serve from the fastest available and refresh from deeper layers (memory → disk → network)."}]},{level:"senior",q:"How does Coil/Glide cache images, and how do you tune it?",a:[{t:"p",text:"Image loaders use a two-tier cache: an in-memory `LruCache` of decoded bitmaps (instant re-display) and a disk cache of the original bytes (survives restarts, avoids re-download). They key by URL (+ transformations). You tune the memory/disk cache sizes, set cache policies per request (`memoryCachePolicy`), and control keys — and always request the *displayed size* so cached bitmaps aren't oversized."},{t:"list",items:["**Memory cache** — decoded bitmaps (LruCache); instant re-display.","**Disk cache** — original bytes; avoids re-download across sessions.","**Keyed by URL + transforms** — with per-request cache policies.","**Tune** — cache sizes, request the displayed size (avoid oversized bitmaps)."]},{t:"note",text:"Coil/Glide use two tiers: in-memory LruCache of decoded bitmaps (instant re-display) + disk cache of original bytes (avoid re-download), keyed by URL + transformations. Tune cache sizes and per-request memoryCachePolicy, and request the displayed size so cached bitmaps aren't oversized (memory/jank)."}]},{level:"senior",q:"How do you implement cache expiration / TTL?",a:[{t:"p",text:"Store a *timestamp* with cached data (a `fetchedAt`/`updatedAt` column, or a separate metadata table), and treat data as stale when `now - fetchedAt > ttl`. Your fetch logic (e.g. `shouldFetch`) checks this to decide whether to refresh. For HTTP-level caching, rely on `Cache-Control: max-age`. Choose the TTL per data volatility (seconds for prices, hours for profiles)."},{t:"code",title:"TTL check",code:`fun shouldFetch(cached: CachedData?): Boolean =
    cached == null || System.currentTimeMillis() - cached.fetchedAt > TTL_MILLIS`},{t:"list",items:["**Store `fetchedAt`** — with the cached row/metadata.","**Stale check** — `now - fetchedAt > ttl`.","**Drive refresh** — `shouldFetch` uses it.","**TTL by volatility** — short for prices, long for profiles; HTTP `max-age` for transport."]},{t:"note",text:"Store a fetchedAt/updatedAt timestamp with cached data and treat it stale when now - fetchedAt > ttl — your shouldFetch logic uses this to trigger refresh. For HTTP-level, use Cache-Control: max-age. Pick TTL by data volatility (seconds for prices, hours for profiles)."}]},{level:"senior",q:"How do you show optimistic updates while a write is in flight?",a:[{t:"p",text:"Apply the change to local state/DB *immediately* (optimistically) so the UI responds instantly, then send the request in the background. If it *succeeds*, keep the change (or reconcile with the server response). If it *fails*, *roll back* to the previous state and show an error. Track a pending/failed status so the UI can indicate in-flight or retryable items."},{t:"list",items:["**Apply immediately** — update DB/state before the network call.","**On success** — keep/reconcile with the server result.","**On failure** — roll back and show an error.","**Status tracking** — pending/failed flags for UI indicators/retry."]},{t:"note",text:"Optimistic update: apply the change to local DB/state immediately (instant UI), send the request in the background; on success keep/reconcile, on failure roll back to the previous state and show an error. Track pending/failed status so the UI can indicate in-flight items and offer retry."}]},{level:"junior",q:"Why is the database (not the network) the right single source of truth?",a:[{t:"p",text:"Making the *local database* the single source of truth means the UI always observes one consistent, always-available store — it works offline, updates reactively (Room `Flow`), and doesn't flicker between network and cache. The network's job is just to *update the database*; the UI never talks to the network directly. This decouples UI from connectivity and gives a smooth, offline-first experience."},{t:"list",items:["**UI observes the DB** — one consistent, always-available source.","**Offline-capable** — the DB works without network.","**Reactive** — Room `Flow` re-emits on any update.","**Network updates the DB** — the UI never reads the network directly."]},{t:"note",text:"The local DB as single source of truth: the UI observes one consistent, always-available store (works offline, reactive via Room Flow, no cache/network flicker). The network's only job is to update the DB; the UI never reads the network directly — decoupling UI from connectivity (offline-first)."}]},{level:"senior",q:"How do you handle pagination with offline caching (RemoteMediator)?",a:[{t:"p",text:"Use Paging 3's `RemoteMediator` with Room: the `PagingSource` reads pages *from the DB* (source of truth), and the `RemoteMediator` fetches network pages and *writes them into the DB* as the user scrolls near the end. This gives paginated, offline-capable lists — cached pages show instantly, new pages load and persist, and the DB drives the UI."},{t:"list",items:["**`PagingSource` from Room** — the DB is the source of truth for pages.","**`RemoteMediator`** — fetches network pages, writes to the DB.","**Offline** — cached pages display without network.","**Triggered by scroll** — loads more near the list end, persists it."]},{t:"note",text:"Paging 3 + Room via RemoteMediator: the PagingSource reads pages from the DB (source of truth), and RemoteMediator fetches network pages and writes them to the DB as the user nears the end. Cached pages show instantly offline; new pages load, persist, and the DB drives the UI."}]},{level:"junior",q:"How do you avoid stale data problems in a cache?",a:[{t:"p",text:"Combine strategies: use TTL/timestamps to detect staleness and refresh; invalidate cache on relevant writes (Room `Flow` auto-refreshes); use HTTP `ETag`/`Cache-Control` for conditional refresh; and pull-to-refresh for user-initiated updates. The goal is to serve fast (cache) while ensuring the user isn't stuck with outdated data — refresh in the background and on demand."},{t:"list",items:["**TTL/timestamps** — refresh when stale.","**Write invalidation** — Room `Flow` re-emits on writes.","**Conditional refresh** — `ETag`/`Cache-Control`.","**Pull-to-refresh** — user-initiated freshness."]},{t:"note",text:"Avoid stale data by combining TTL/timestamps (refresh when stale), write invalidation (Room Flow auto-refreshes), conditional HTTP refresh (ETag/Cache-Control), and pull-to-refresh (user-initiated). Serve fast from cache while refreshing in the background and on demand so the user isn't stuck with old data."}]},{level:"senior",q:"What is the difference between an in-memory LruCache and a disk cache?",a:[{t:"p",text:"An `LruCache` holds recently-used items *in memory* — extremely fast but volatile (lost on process death) and bounded by RAM (evicts least-recently-used when full). A *disk cache* (Room, files, OkHttp/image disk cache) is slower but *persistent* across restarts and larger. Layer them: check memory first, then disk, then network."},{t:"list",items:["**`LruCache`** — in-memory, fast, volatile, RAM-bounded (LRU eviction).","**Disk cache** — persistent, larger, slower.","**Layered** — memory → disk → network.","**Size limits** — memory sized to avoid OOM; disk to a byte budget."]},{t:"note",text:"LruCache = in-memory, very fast, volatile (lost on process death), RAM-bounded with LRU eviction. Disk cache (Room/files/OkHttp) = persistent across restarts, larger, slower. Layer them: memory → disk → network. Size memory to avoid OOM, disk to a byte budget."}]},{level:"senior",q:"How do you test offline-first behavior?",a:[{t:"p",text:"Abstract the network and DB behind repositories with fakes: test that data is served from the cache when the network fails, that a fetch updates the DB (which re-emits), and that writes queue when offline and flush when online. Use a fake network that can be toggled offline/error, an in-memory Room DB, and `runTest` to drive the flows deterministically."},{t:"list",items:["**Fake network** — toggle offline/error to exercise fallbacks.","**In-memory Room** — real DB behavior in tests.","**Assert cache-serving** — data returned when network fails.","**Assert sync** — queued writes flush when 'online'."]},{t:"note",text:"Test offline-first with a fake network (toggle offline/error) + an in-memory Room DB: assert data is served from cache when the network fails, a fetch updates the DB (re-emits), and offline writes queue then flush when online. Drive flows with runTest for determinism."}]},{level:"junior",q:"What is optimistic vs pessimistic UI updating?",a:[{t:"p",text:"*Optimistic* updating applies the change to the UI *immediately*, assuming the server call will succeed (rolling back on failure) — feels instant and responsive. *Pessimistic* updating waits for the server to confirm before updating the UI (showing a spinner) — safer but slower. Optimistic is preferred for common, low-risk actions (like/favorite); pessimistic for critical operations (payments) where you must confirm first."},{t:"list",items:["**Optimistic** — update now, roll back on failure; instant feel.","**Pessimistic** — wait for confirmation; safer, slower.","**Optimistic for** — likes, toggles, low-risk edits.","**Pessimistic for** — payments, irreversible/critical actions."]},{t:"note",text:"Optimistic: update the UI immediately (roll back on failure) — instant, for common low-risk actions (like/favorite). Pessimistic: wait for server confirmation (spinner) — safer/slower, for critical operations (payments). Choose by risk and reversibility."}]},{level:"senior",q:"How do you decide what to cache and how much storage to use?",a:[{t:"p",text:"Cache data that's *expensive to fetch and frequently re-read* (feeds, images, profiles) and *safe to store* (not sensitive without encryption). Bound storage: set size limits on caches (LRU eviction for memory, byte budgets for disk/image caches), use `cacheDir` for clearable data, and periodically prune stale entries. Don't cache everything — balance UX (offline/speed) against storage and privacy."},{t:"list",items:["**Cache** — expensive-to-fetch, frequently-read, non-sensitive data.","**Bound it** — size limits (LRU/byte budgets), `cacheDir` for clearable data.","**Prune** — evict stale/oldest entries.","**Don't over-cache** — balance offline/speed vs storage and privacy."]},{t:"note",text:"Cache data that's expensive to fetch and frequently re-read (feeds/images/profiles) and non-sensitive. Bound storage with size limits (LRU for memory, byte budgets for disk/image caches), use cacheDir for clearable data, and prune stale entries. Balance UX (offline/speed) against storage and privacy — don't cache everything."}]},{level:"senior",q:"What is the difference between HTTP caching and app-level database caching?",a:[{t:"p",text:"*HTTP caching* (OkHttp cache with ETag/Cache-Control) is *transport-level* — it caches raw responses and revalidates with the server, saving bandwidth transparently. *App-level DB caching* (Room) is *domain-level* — it stores parsed, queryable, relational data as the single source of truth the UI observes. HTTP caching helps efficiency; DB caching provides offline-first structure. They complement each other."},{t:"list",items:["**HTTP cache** — raw responses, revalidation (ETag), bandwidth savings; transparent.","**DB cache** — parsed/queryable domain data; single source of truth; offline-first.","**Complementary** — HTTP for efficiency, DB for structure/offline.","**UI observes the DB** — not the HTTP cache directly."]},{t:"note",text:"HTTP caching (OkHttp ETag/Cache-Control) is transport-level — caches raw responses, revalidates, saves bandwidth transparently. App DB caching (Room) is domain-level — parsed, queryable data as the single source of truth the UI observes (offline-first). They complement: HTTP for efficiency, DB for structure/offline."}]},{level:"senior",q:"How do you clear the cache on logout or user switch?",a:[{t:"p",text:"On logout, wipe user-specific cached data so the next user doesn't see it: `db.clearAllTables()` (or delete user-scoped rows), clear DataStore user prefs, clear the HTTP/image caches if they hold user data, and cancel in-flight sync work. Do it in a transaction/coordinated step, off the main thread, before navigating to login. Keep app-level (non-user) caches if appropriate."},{t:"list",items:["**Wipe user data** — `clearAllTables()`/delete user rows.","**Clear prefs/tokens** — DataStore user keys, encrypted token.","**Clear caches** — HTTP/image caches holding user data.","**Cancel sync work** — WorkManager jobs; then navigate to login."]},{t:"note",text:"On logout/user switch, wipe user-specific data: db.clearAllTables() (or delete user rows), clear DataStore user prefs + token, clear HTTP/image caches with user data, and cancel in-flight sync work — off the main thread, before navigating to login. Prevents the next user seeing stale data."}]},{level:"senior",q:"What is a cache stampede, and how do you avoid it?",a:[{t:"p",text:"A cache stampede (or 'thundering herd') is when a cached item expires and *many* concurrent requests all miss and hit the network/DB simultaneously to refill it — overloading the source. Avoid it by *deduplicating* in-flight refreshes (one fetch shared among callers, e.g. via `flatMapLatest`/a shared Flow), staggering TTLs with jitter, and serving stale data while a single background refresh runs (stale-while-revalidate)."},{t:"list",items:["**Stampede** — many concurrent misses refill the same item at once.","**Dedup in-flight fetches** — one shared refresh (shared Flow / single-flight).","**TTL jitter** — stagger expirations to avoid synchronized misses.","**Stale-while-revalidate** — serve stale during a single refresh."]},{t:"note",text:"A cache stampede is many concurrent requests all missing an expired item and refilling it at once (overloading the source). Avoid via single-flight/dedup of in-flight refreshes (one shared fetch), TTL jitter (stagger expirations), and stale-while-revalidate (serve stale during one background refresh)."}]},{level:"senior",q:"How do you design periodic background sync?",a:[{t:"p",text:"Use `WorkManager` with a `PeriodicWorkRequest` and constraints (network, maybe charging) to sync data at intervals — it's battery-friendly (batched by the system) and survives reboots. The worker fetches updates, writes to the DB (source of truth), and handles failures with retry/backoff. For push-driven freshness, complement it with FCM data messages triggering an immediate one-off sync."},{t:"list",items:["**`PeriodicWorkRequest`** — interval sync (min 15 min) with constraints.","**Constraints** — network/charging; battery-friendly, survives reboot.","**Write to DB** — the source of truth; retry/backoff on failure.","**FCM-triggered** — push a one-off sync for immediate freshness."]},{t:"note",text:"Periodic sync: WorkManager PeriodicWorkRequest (≥15 min) with constraints (network/charging) — battery-friendly, survives reboot; the worker fetches, writes to the DB (source of truth), retries with backoff. Complement with FCM data messages triggering a one-off expedited sync for immediate freshness."}]},{level:"senior",q:"What is the difference between full sync and delta (incremental) sync?",a:[{t:"p",text:"*Full sync* re-downloads the entire dataset each time — simple but wasteful (bandwidth, battery) and slow for large data. *Delta sync* only fetches *changes since the last sync* (using a timestamp/cursor/version the server understands), applying them to the local DB — far more efficient. Delta sync needs server support (a 'changes since X' endpoint) and careful handling of deletions and conflicts."},{t:"list",items:["**Full sync** — re-fetch everything; simple, wasteful, slow at scale.","**Delta sync** — fetch only changes since a cursor/timestamp; efficient.","**Needs server support** — a 'changes since X' endpoint; handle deletions.","**Use delta** — for large/frequently-synced datasets."]},{t:"note",text:"Full sync re-downloads everything (simple, wasteful, slow at scale); delta sync fetches only changes since a cursor/timestamp/version and applies them locally (efficient). Delta needs server support (a 'changes since X' endpoint) and careful deletion/conflict handling. Prefer delta for large/frequent syncs."}]},{level:"senior",q:"Why is idempotency important for retried offline writes?",a:[{t:"p",text:"Offline writes are retried (on reconnect, after failures), so the *same* request may reach the server *multiple times*. Without idempotency, that could create duplicate records or double-apply an action (e.g. two identical orders). Attach an *idempotency key* (a client-generated unique id per operation) so the server can detect and dedupe retries — applying the operation exactly once."},{t:"list",items:["**Retries cause duplicates** — the same write may arrive multiple times.","**Idempotency key** — a client-generated unique id per operation.","**Server dedupes** — applies the operation once, ignores retries with the same key.","**Exactly-once effect** — safe retries for offline sync."]},{t:"note",text:"Retried offline writes may reach the server multiple times, risking duplicates/double-applied actions. Attach a client-generated idempotency key per operation so the server dedupes retries and applies it exactly once. Essential for safe offline-write sync."}]},{level:"junior",q:"How do you show connectivity/offline status in the UI?",a:[{t:"p",text:"Observe connectivity via `ConnectivityManager.NetworkCallback` (wrapped in a `callbackFlow`) and expose it as a `Flow<Boolean>`; the UI collects it to show an offline banner/indicator. Combine it with sync state (pending writes) to communicate 'offline — changes will sync later'. Keep the app usable offline (serving from the DB) rather than blocking the UI."},{t:"list",items:["**Observe connectivity** — `NetworkCallback` → `callbackFlow` → `Flow<Boolean>`.","**Offline banner** — the UI collects and shows status.","**Combine with sync state** — 'changes will sync when online'.","**Stay usable** — serve from the DB; don't block offline."]},{t:"note",text:"Observe connectivity (ConnectivityManager.NetworkCallback wrapped in callbackFlow → Flow<Boolean>) and show an offline banner; combine with pending-sync state to convey 'offline — changes will sync later'. Keep the app usable offline (serve from the DB), don't block the UI."}]},{level:"senior",q:"How do you handle flaky or intermittent connectivity?",a:[{t:"p",text:"Design for it: serve from the DB cache so the UI works regardless of connectivity; retry failed requests with *exponential backoff + jitter* (only transient errors); use timeouts so a flaky connection doesn't hang; queue writes to sync when a stable connection returns (WorkManager network constraint); and debounce connectivity flapping so you don't thrash retries on every brief reconnect."},{t:"list",items:["**DB-backed UI** — works despite connectivity gaps.","**Retry with backoff + jitter** — transient errors only.","**Timeouts** — don't hang on a stalled connection.","**Queue + WorkManager** — flush writes when stably online; debounce flapping."]},{t:"note",text:"For flaky connectivity: serve from the DB cache (UI works offline), retry transient failures with exponential backoff + jitter, use timeouts (don't hang), queue writes to sync via WorkManager (network constraint) when stably online, and debounce connectivity flapping to avoid thrashing retries."}]}],EA={id:"caching-offline",title:"Caching & Offline-First",tagline:"Offline-first architecture on SSOT, caching strategies (cache-then-network), invalidation (TTL/ETag/events), offline writes & sync, and cache layering.",tags:["Caching","Offline-First","SSOT","Sync","WorkManager"],content:DA,qa:RA},UA=[{heading:"The problem Paging solves",blocks:[{t:"p",text:"Some lists are effectively unbounded — a social feed, search results, a message history with thousands of entries. Loading *all* of it at once is impossible: it would exhaust memory, take forever, and waste bandwidth on data the user never scrolls to. The solution is **pagination** — loading the data in small chunks ('pages') as the user scrolls. **Paging 3** is Jetpack's library that handles this: it loads pages on demand, manages the in-memory list, triggers the next load as the user approaches the end, and integrates loading/error states."},{t:"list",items:["Doing pagination by hand is surprisingly hard: tracking the current page, detecting when to load more (scroll math), deduplicating requests, handling errors and retries, showing loading indicators at the right place, and keeping memory bounded. Paging 3 encapsulates all of it.","It's the recommended solution for large/unbounded lists, integrating with Room (as the source of truth), Compose/RecyclerView (UI), and coroutines/Flow."]}]},{heading:"The core pieces: PagingSource, Pager, PagingData",blocks:[{t:"code",title:"The three building blocks",code:`// 1) PagingSource — defines HOW to load a page (given a key, return items + next key)
class ArticlePagingSource(private val api: Api) : PagingSource<Int, Article>() {
    override suspend fun load(params: LoadParams<Int>): LoadResult<Int, Article> {
        return try {
            val page = params.key ?: 1
            val response = api.getArticles(page, params.loadSize)
            LoadResult.Page(
                data = response.items,
                prevKey = if (page == 1) null else page - 1,
                nextKey = if (response.items.isEmpty()) null else page + 1,
            )
        } catch (e: Exception) {
            LoadResult.Error(e)
        }
    }
    override fun getRefreshKey(state: PagingState<Int, Article>): Int? = null
}

// 2) Pager — configures paging and produces a stream of PagingData
val pager = Pager(PagingConfig(pageSize = 20)) { ArticlePagingSource(api) }

// 3) PagingData — a stream of paged data the ViewModel exposes
val articles: Flow<PagingData<Article>> = pager.flow.cachedIn(viewModelScope)`},{t:"list",items:["**`PagingSource`** — you implement `load()`: given a *key* (usually a page number or a cursor), return a `LoadResult.Page` with the items plus the *previous* and *next* keys (or `LoadResult.Error` on failure). This is where you define *how* to fetch a page. The keys chain the pages together.","**`Pager`** — configured with `PagingConfig` (page size, prefetch distance, etc.), it turns your `PagingSource` into a `Flow<PagingData<T>>`.","**`PagingData`** — a container for one snapshot of paged data. It's a *stream of self-updating list snapshots*, not a normal list — which is why it needs its own consumption API and doesn't work with regular Flow operators like `stateIn`.","**`cachedIn(scope)`** — *mandatory* in the ViewModel: it caches the loaded pages in the given scope so they survive configuration changes. Without it, rotating the device re-fetches everything from page 1."]}]},{heading:"Consuming PagingData in the UI",blocks:[{t:"code",title:"In Compose",code:`val items = viewModel.articles.collectAsLazyPagingItems()

LazyColumn {
    items(
        count = items.itemCount,
        key = items.itemKey { it.id },
        contentType = items.itemContentType { "article" },
    ) { index ->
        val article = items[index]          // accessing near the edge triggers next load
        if (article != null) ArticleCard(article) else PlaceholderCard()
    }

    // Load states as list items:
    when (items.loadState.append) {
        is LoadState.Loading -> item { LoadingRow() }
        is LoadState.Error -> item { RetryRow { items.retry() } }
        else -> Unit
    }
}`},{t:"list",items:["**`collectAsLazyPagingItems()`** (Compose) / `PagingDataAdapter` (RecyclerView) — the UI-side consumer. Accessing an item near the loaded edge is what *triggers* the next page load — no manual scroll listeners.","**`loadState`** — exposes the loading/error/success state for `refresh` (initial load), `append` (loading more at the end), and `prepend` (loading at the start). You render these as list items (a spinner row, a retry row) or full-screen states — giving proper first-load spinners, end-of-list loaders, and retry.","**Placeholders** — if enabled, Paging can show placeholder items (null entries) before their data loads, so the scrollbar reflects the full count and there's no visual jump."]}]},{heading:"RemoteMediator — network + database paging (offline-first)",blocks:[{t:"p",text:"The most powerful Paging 3 setup combines network *and* database: the **`RemoteMediator`** loads pages from the network *into Room*, and the `PagingSource` reads pages *from Room*. Room is the single source of truth; the network fills it as the user scrolls. This gives offline-first pagination — cached pages are available offline, and scrolling fetches new pages when online."},{t:"list",items:["**How it fits together**: the `Pager` uses Room's `PagingSource` (Room generates one for a paged query — `@Query ... : PagingSource<Int, Entity>`) as the source, and a `RemoteMediator` as the thing that *fills* Room when the source runs out. As you scroll past the cached data, the mediator fetches the next network page and writes it to Room, which the `PagingSource` then serves.","**Offline benefit**: because the UI reads from Room (the SSOT), previously-loaded pages are shown instantly and work offline; the network only fetches *new* pages. This is the pagination version of the offline-first / SSOT pattern.","**`RemoteMediator.load()`** handles `REFRESH`/`APPEND`/`PREPEND` load types, writing fetched pages to Room in a transaction (often clearing on refresh), and tracks remote keys (the next-page tokens) in a separate table so it knows what to fetch next."]}]},{heading:"Transformations and gotchas",blocks:[{t:"list",items:["**Transforming PagingData**: use the dedicated operators (`map`, `filter`, `insertSeparators` on `PagingData`) *before* `cachedIn`, not regular Flow operators — `PagingData` isn't a normal Flow of values. `insertSeparators` adds headers/separators (e.g. date dividers) between items.","**`cachedIn` is mandatory and interviewers check it**: forget it and every configuration change re-fetches from scratch (rotation reloads page 1, losing scroll position and wasting requests). Put it at the end of the pipeline in the ViewModel.","**Keys must be stable**: pass `itemKey`/`itemContentType` in the UI (as with any lazy list) so inserts/updates don't cause cascade recomposition and item state migrates correctly.","**When NOT to use Paging**: bounded lists that fit comfortably in memory (a user's 20 settings, a fixed menu) — Paging's ceremony (PagingSource, load states, cachedIn) is overkill. Use a plain `items(list)`. Paging earns its complexity only for genuinely large or unbounded, usually network-backed, data."]},{t:"note",text:"Paging 3 essentials: PagingSource (how to load a page — key → items + next/prev keys), Pager (config → Flow<PagingData>), PagingData (self-updating paged snapshots, needs cachedIn in the ViewModel — mandatory, or rotation re-fetches). Consume with collectAsLazyPagingItems / PagingDataAdapter; accessing near the edge triggers loads; loadState gives loading/error/retry as list items. RemoteMediator + Room = offline-first paging (network fills Room, UI reads Room). Only use Paging for genuinely large/unbounded lists."}]}],qA=[{level:"junior",q:"What problem does Paging 3 solve?",a:[{t:"p",text:"**Paging 3 handles loading large or unbounded lists in small chunks ('pages') as the user scrolls, instead of loading everything at once.** For a feed, search results, or a long message history, loading all the data would exhaust memory, be slow, and waste bandwidth on data the user never reaches. Pagination loads a page at a time, on demand."},{t:"p",text:"The reason to use the library rather than rolling your own is that manual pagination is deceptively hard: you'd have to track the current page, detect when the user is near the end (scroll math), avoid firing duplicate load requests, handle errors and retries, show loading indicators in the right place, and keep memory bounded by discarding far-away pages. Paging 3 encapsulates all of that. It integrates with Room (as the local source of truth), with Compose and RecyclerView on the UI side, and with coroutines/Flow, exposing paged data as a `Flow<PagingData<T>>`. It's the recommended solution whenever a list is genuinely large or unbounded."}]},{level:"junior",q:"What are the main components of Paging 3?",a:[{t:"list",items:["**`PagingSource`** — defines *how* to load a page. You implement `load()`: given a key (usually a page number or cursor), fetch that page and return the items plus the previous and next keys (which chain the pages together), or an error. This is where your actual data-fetching logic lives.","**`Pager`** — configured with a `PagingConfig` (page size, prefetch distance), it turns your `PagingSource` into a `Flow<PagingData<T>>`.","**`PagingData`** — a container for a snapshot of paged data; it's a self-updating stream of paged list snapshots that the ViewModel exposes and the UI consumes.","**UI consumer** — `collectAsLazyPagingItems()` in Compose or a `PagingDataAdapter` in RecyclerView, which displays the items and triggers loads as you scroll."]},{t:"p",text:"The flow is: `PagingSource` (how to load) → `Pager` (configuration) → `Flow<PagingData>` (exposed by the ViewModel, cached with `cachedIn`) → UI consumer (displays and triggers loading). Optionally a `RemoteMediator` sits alongside to fill a local Room database from the network for offline-first paging. Accessing an item near the end of the loaded data is what automatically triggers loading the next page — you never write scroll-position math yourself."}]},{level:"junior",q:"How does the UI load more data as the user scrolls in Paging 3?",a:[{t:"p",text:"**Automatically — accessing an item near the edge of the currently-loaded data triggers the next page load; you don't write any scroll-detection logic.** When you display paged items (via `collectAsLazyPagingItems` in Compose or `PagingDataAdapter` in RecyclerView) and the user scrolls so that they're accessing items close to the end of what's loaded, Paging detects this (based on the `prefetchDistance` in `PagingConfig`) and calls your `PagingSource.load()` (or `RemoteMediator`) to fetch the next page."},{t:"code",title:"Accessing items drives loading",code:`val items = viewModel.feed.collectAsLazyPagingItems()
LazyColumn {
    items(items.itemCount, key = items.itemKey { it.id }) { index ->
        val item = items[index]   // near the edge -> triggers next-page load
        if (item != null) Row(item) else Placeholder()
    }
}`},{t:"p",text:"This is the big convenience over hand-rolled pagination, where you'd add a scroll listener, compute whether the last visible item is within N of the end, guard against firing while a load is in progress, and manually append results. Paging does all of that internally. You also get `loadState` to render the loading spinner (for the append) and an error/retry row as list items, so the 'loading more…' UI at the bottom is handled cleanly too."}]},{level:"junior",q:"What does cachedIn do and why is it important?",a:[{t:"p",text:"**`cachedIn(scope)` caches the loaded pages in the given coroutine scope (usually `viewModelScope`) so they survive configuration changes.** You apply it at the end of the paging pipeline in the ViewModel: `pager.flow.cachedIn(viewModelScope)`."},{t:"p",text:"**Why it's important — and why interviewers check for it**: without `cachedIn`, the `PagingData` flow is cold and tied to the collector, so when a configuration change happens (rotation), the whole thing restarts and **re-fetches from page 1**, losing all the pages the user had scrolled through and their scroll position. With `cachedIn(viewModelScope)`, the loaded pages are kept alive in the ViewModel's scope (which survives configuration changes), so after rotation the UI re-attaches to the *same* cached data — no re-fetch, scroll position preserved. It also makes the paging flow shareable/multicast-safe. Forgetting `cachedIn` is one of the most common Paging 3 bugs: everything *works*, but every rotation silently reloads from the start, wasting requests and jarring the user. So the rule is: always end your ViewModel's paging pipeline with `cachedIn(viewModelScope)` (after any `PagingData` transformations)."}]},{level:"senior",q:"How does RemoteMediator enable offline-first pagination with Room?",a:[{t:"p",text:"**`RemoteMediator` implements the offline-first pattern for paging: the `PagingSource` reads pages from *Room* (the single source of truth), and the `RemoteMediator` *fills Room from the network* when the local data runs out. So the UI always reads from the local database — giving instant, offline-capable display of cached pages — while the network is only used to fetch *new* pages as the user scrolls past what's cached.**"},{t:"list",items:["**The architecture**: the `Pager` is built with Room's `PagingSource` (Room auto-generates one for a paged query — a DAO method returning `PagingSource<Int, Entity>`) as the *source of items*, and a `RemoteMediator` as the *boundary callback* that runs when the source is about to run out of loaded items. The mediator fetches the next network page, writes it into Room (in a transaction), and Room's `PagingSource` then serves those newly-inserted rows to the UI.","**`RemoteMediator.load()` handles three load types**: `REFRESH` (initial or pull-to-refresh — often clears Room and loads the first network page), `APPEND` (user scrolled to the end — fetch and insert the next page), and `PREPEND` (load earlier data, if applicable). It returns whether it reached the end of pagination.","**Remote keys tracking**: since the network uses page tokens/cursors that don't map cleanly to Room rows, you typically keep a separate `remote_keys` table storing the next/prev key per item or per page, so on `APPEND` the mediator knows what to fetch next. Managing these keys correctly is the fiddly part.","**The offline benefit**: because the UI reads from Room, previously-loaded pages display *instantly and offline* — no network needed to re-show them. The network only fetches genuinely new pages. When offline, the user sees all cached pages; scrolling past them shows an error/end state until connectivity returns. This is the pagination-specific realization of the SSOT/offline-first architecture."]},{t:"p",text:"**The senior framing**: `RemoteMediator` + Room is Paging's answer to 'how do I paginate a network list *and* have it work offline and survive process death?' The key insight is that Room remains the single source of truth — the UI never reads the network directly; the mediator's only job is to *populate* Room from the network at the right times. This gives you all the SSOT benefits (instant cached reads, offline support, consistency) applied to an unbounded, incrementally-loaded list. It's more setup than a pure-network `PagingSource` (you write the mediator, the remote-keys handling, and the Room queries), so you use it specifically when offline paging and caching matter — for a pure in-memory network list, a plain `PagingSource` without Room is simpler."}]},{level:"senior",q:"When should you NOT use Paging 3, and how does PagingData differ from a normal list/Flow?",a:[{t:"p",text:"**Don't use Paging 3 for bounded lists that fit comfortably in memory** — a user's settings, a fixed menu, a small fixed result set (a few dozen items). Paging's machinery — implementing a `PagingSource` (or `RemoteMediator`), handling load states, `cachedIn`, the special `PagingData` consumption API — is real complexity that only pays off for *genuinely large or unbounded* data, usually network-backed. For a small list, a plain `items(list)` over a `StateFlow<List<T>>` is far simpler and correct. Reaching for Paging on a 20-item list is over-engineering."},{t:"list",items:["**`PagingData` is NOT a normal list or a normal Flow of values** — it's a *stream of self-updating paged snapshots* with its own internal diffing and state. This has concrete consequences that trip people up:","**You can't use regular Flow operators on its contents**: `stateIn`, `combine`, a plain `map` over the *list* don't apply. Instead, `PagingData` has its *own* operators — `PagingData.map`, `PagingData.filter`, `PagingData.insertSeparators` — which transform individual items *within* the paged stream, and you apply them *before* `cachedIn`. Trying to treat `PagingData` like a `Flow<List<T>>` and operate on it with normal collection/Flow functions doesn't work.","**It doesn't hold a materialized list you can index freely**: you access items through the UI consumer (`collectAsLazyPagingItems`, which returns a `LazyPagingItems` you index by position, possibly getting null for not-yet-loaded placeholders). You don't get a `List<T>` you can call `.size` or `.filter` on directly.","**It manages its own loading/error state** (`loadState` for refresh/append/prepend) rather than you tracking a separate `isLoading` boolean — because loading is per-page and ongoing, not a single state."]},{t:"p",text:"**The senior framing**: the decision is about data size and boundedness — Paging earns its complexity only for large/unbounded lists where incremental loading and bounded memory genuinely matter; for small lists it's needless ceremony. And understanding that `PagingData` is a *specialized reactive paging primitive*, not a list or ordinary Flow (hence its own `map`/`filter`/`insertSeparators`, `cachedIn`, and `loadState`), is what prevents the common mistakes of trying to `stateIn` it or operate on it with standard Flow/collection operators."}]},{level:"junior",q:"What is a PagingSource, and how do you implement one?",a:[{t:"p",text:"A `PagingSource<Key, Value>` defines how to load a page of data — you implement `load(params)` to fetch the page for a given key (e.g. a page number or item offset) and return a `LoadResult.Page` with the data, the previous key, and the next key (or `LoadResult.Error`). Paging calls it as the user scrolls. `getRefreshKey` tells Paging where to reload from on refresh."},{t:"code",title:"A network PagingSource",code:`class UserPagingSource(private val api: Api) : PagingSource<Int, User>() {
    override suspend fun load(params: LoadParams<Int>): LoadResult<Int, User> = try {
        val page = params.key ?: 1
        val users = api.getUsers(page, params.loadSize)
        LoadResult.Page(users, prevKey = if (page == 1) null else page - 1,
            nextKey = if (users.isEmpty()) null else page + 1)
    } catch (e: IOException) { LoadResult.Error(e) }
    override fun getRefreshKey(state: PagingState<Int, User>) = state.anchorPosition?.let { … }
}`},{t:"list",items:["**`load(params)`** — fetch a page; return `LoadResult.Page(data, prevKey, nextKey)`.","**Keys** — page number/offset; `nextKey = null` signals the end.","**`getRefreshKey`** — where to reload from on refresh.","**Errors** — `LoadResult.Error` (retryable)."]},{t:"note",text:"A PagingSource<Key, Value> implements load(params) — fetch a page for the key, return LoadResult.Page(data, prevKey, nextKey) (nextKey=null = end) or LoadResult.Error. getRefreshKey says where to reload on refresh. Paging calls it as the user scrolls. Room can generate a PagingSource from a @Query."}]},{level:"junior",q:"What is a Pager, and how do you configure it?",a:[{t:"p",text:"A `Pager` ties together a `PagingConfig` and a `PagingSource` factory to produce a `Flow<PagingData<T>>`. `PagingConfig` sets the `pageSize`, `prefetchDistance` (how far ahead to load), `initialLoadSize`, and `enablePlaceholders`. You build the `Pager` in the ViewModel/repository and expose its flow to the UI."},{t:"code",title:"Building a Pager",code:`val pager = Pager(
    config = PagingConfig(pageSize = 20, prefetchDistance = 5, enablePlaceholders = false),
    pagingSourceFactory = { UserPagingSource(api) },
).flow.cachedIn(viewModelScope)`},{t:"list",items:["**`Pager(config, sourceFactory)`** — produces `Flow<PagingData<T>>`.","**`PagingConfig`** — `pageSize`, `prefetchDistance`, `initialLoadSize`, `enablePlaceholders`.","**`.cachedIn(scope)`** — cache the paged stream across config changes.","**Expose the flow** — the UI collects and submits it."]},{t:"note",text:"A Pager(config, pagingSourceFactory) produces Flow<PagingData<T>>; PagingConfig sets pageSize/prefetchDistance/initialLoadSize/enablePlaceholders. Build it in the ViewModel/repo, .cachedIn(scope) to survive config changes, and expose the flow to the UI."}]},{level:"junior",q:"How do you display PagingData in Compose and in Views?",a:[{t:"p",text:"In Compose, collect the flow with `collectAsLazyPagingItems()` and render with `items(lazyPagingItems.itemCount) { }` (or the key/contentType overloads) inside a `LazyColumn`, handling `loadState`. In Views, use a `PagingDataAdapter` (a `RecyclerView.Adapter`) and call `submitData(pagingData)` from a coroutine. Both handle incremental loading and diffing automatically."},{t:"code",title:"Compose paging list",code:`val items = viewModel.pagingFlow.collectAsLazyPagingItems()
LazyColumn {
    items(items.itemCount, key = items.itemKey { it.id }) { index ->
        items[index]?.let { UserRow(it) }
    }
    when (items.loadState.append) { is LoadState.Loading -> item { Spinner() }; else -> {} }
}`},{t:"list",items:["**Compose** — `collectAsLazyPagingItems()` + `LazyColumn`; handle `loadState`.","**Views** — `PagingDataAdapter` + `submitData(pagingData)`.","**Automatic** — incremental loading, diffing, placeholders.","**Load states** — show append/refresh spinners and errors."]},{t:"note",text:"Compose: collectAsLazyPagingItems() + LazyColumn (items by itemCount, itemKey), handle loadState. Views: PagingDataAdapter + submitData(pagingData) from a coroutine. Both do incremental loading + diffing automatically; render append/refresh load states (spinners/errors)."}]},{level:"senior",q:"How do you handle load states (loading, error, retry) in Paging 3?",a:[{t:"p",text:"Paging exposes `CombinedLoadStates` via `loadState` — with `refresh`, `append`, and `prepend` states, each `Loading`/`NotLoading`/`Error`. You render a full-screen spinner/error for `refresh`, inline spinners/retry rows for `append`/`prepend`, and call `retry()` on error. In Compose you read `lazyPagingItems.loadState`; in Views you add a `LoadStateAdapter` or observe `loadStateFlow`."},{t:"list",items:["**`refresh`** — initial/refresh load; full-screen spinner/error.","**`append`/`prepend`** — loading more; inline spinner/retry row.","**`retry()`** — re-attempt failed loads.","**Compose `loadState` / Views `LoadStateAdapter`** — render the states."]},{t:"note",text:"Paging's CombinedLoadStates (loadState) has refresh/append/prepend, each Loading/NotLoading/Error. Render a full-screen spinner/error for refresh, inline spinner/retry for append/prepend, and call retry() on error. Compose reads lazyPagingItems.loadState; Views use a LoadStateAdapter/loadStateFlow."}]},{level:"senior",q:"What do placeholders do in Paging 3, and when should you enable them?",a:[{t:"p",text:"With `enablePlaceholders = true`, Paging reports the *total item count* up front and returns `null` items for not-yet-loaded positions — so the list shows placeholder UI (skeletons) and has an accurate scrollbar. It requires the data source to know the total count (`COUNT(*)` for Room). Disable placeholders when the count is unknown/expensive (most network sources) or you don't want null items."},{t:"list",items:["**Placeholders on** — total count known; null items for unloaded positions; accurate scrollbar/skeletons.","**Needs a count** — Room provides it; many network sources don't.","**Handle nulls** — your UI must render a placeholder for null items.","**Off** — when count is unknown/expensive or nulls are undesirable."]},{t:"note",text:"enablePlaceholders = true reports the total count up front and returns null for unloaded positions (skeleton UI, accurate scrollbar) — needs the source to know the count (Room COUNT(*)). Your UI must render placeholders for null items. Disable when the count is unknown/expensive (network) or nulls are undesirable."}]},{level:"senior",q:"How do you transform PagingData (map, filter, insert separators)?",a:[{t:"p",text:"Apply transformations on the `PagingData` flow so they run per-page as data loads: `.map { }`/`.filter { }` transform items, and `.insertSeparators { before, after -> }` inserts header/separator items (e.g. date dividers). Do these *after* `cachedIn` if the transform is presentation-only (so it re-runs cheaply), or before if it should be cached. Filtering paged data can leave short pages, so prefer filtering at the source."},{t:"code",title:"Transforming PagingData",code:`pager.flow
    .map { pagingData -> pagingData.map { it.toUiModel() } }
    .map { it.insertSeparators { before, after ->
        if (before?.date != after?.date) DateHeader(after?.date) else null } }
    .cachedIn(viewModelScope)`},{t:"list",items:["**`PagingData.map`/`filter`** — per-item transforms (run per page).","**`insertSeparators`** — add headers/dividers between items.","**Placement vs `cachedIn`** — cache expensive transforms; presentation ones after.","**Filtering caveat** — can produce short pages; prefer filtering at the source."]},{t:"note",text:"Transform the PagingData flow (runs per page): .map/.filter for items, .insertSeparators for headers/dividers (date sections). Cache expensive transforms before cachedIn; presentation-only after. Filtering paged data can leave short pages — prefer filtering at the source query/API."}]},{level:"senior",q:"How does RemoteMediator coordinate network and database for paging?",a:[{t:"p",text:"`RemoteMediator` implements `load(loadType, state)` for the *network* side of a DB-backed pager: on `REFRESH`/`APPEND` it fetches the next network page, writes it into Room (and updates remote keys tracking pagination), and returns `MediatorResult`. The `Pager`'s `PagingSource` comes from Room (`@Query ... PagingSource`), so the UI always reads from the DB while the mediator keeps it topped up — offline-first pagination."},{t:"code",title:"Pager with RemoteMediator",code:`Pager(
    config = PagingConfig(pageSize = 20),
    remoteMediator = UserRemoteMediator(api, db),   // network -> DB
    pagingSourceFactory = { db.userDao().pagingSource() },  // DB -> UI
).flow`},{t:"list",items:["**`RemoteMediator.load`** — fetch network pages, write to Room.","**Remote keys** — a table tracking next/prev page keys per item.","**PagingSource from Room** — the UI reads the DB (source of truth).","**Offline-first** — cached pages show offline; mediator refills online."]},{t:"note",text:"RemoteMediator.load(loadType, state) handles the network side of a DB-backed pager: fetch the next page, write to Room, update a remote-keys table, return MediatorResult. The PagingSource comes from Room (the UI reads the DB), so cached pages show offline and the mediator refills online — offline-first pagination."}]},{level:"junior",q:"How do you refresh or invalidate a paged list?",a:[{t:"p",text:"Call `refresh()` on the paging adapter/`LazyPagingItems` to reload from the start (re-invoking `getRefreshKey`). Underneath, a `PagingSource` is *invalidated* when its data changes — for a Room-backed source, any write to the observed tables auto-invalidates it and Paging creates a new `PagingSource`, reloading. For pull-to-refresh, call `refresh()`."},{t:"list",items:["**`refresh()`** — reload from the start (pull-to-refresh).","**Invalidation** — a `PagingSource` invalidates when its data changes.","**Room-backed** — writes to observed tables auto-invalidate and reload.","**New PagingSource** — Paging recreates it on invalidation."]},{t:"note",text:"Call refresh() on the adapter/LazyPagingItems to reload from the start (pull-to-refresh; re-runs getRefreshKey). A PagingSource invalidates when its data changes — a Room-backed source auto-invalidates on writes to observed tables, and Paging creates a new PagingSource to reload."}]},{level:"senior",q:"Why is cachedIn important, and where do you place it?",a:[{t:"p",text:"`cachedIn(scope)` caches the `PagingData` stream in a `CoroutineScope` (usually `viewModelScope`) so it *survives configuration changes* and is *shared* among multiple collectors without re-fetching. It also makes the flow safe to collect multiple times (a raw `Pager.flow` can only be collected once). Place it as the *last* operator in the ViewModel, after any caching-worthy transforms."},{t:"list",items:["**Survives config change** — no re-fetch on rotation.","**Multi-collect safe** — a raw Pager flow is single-collection.","**Shared** — multiple collectors reuse the cached data.","**Placement** — last in the ViewModel, after cache-worthy transforms."]},{t:"note",text:"cachedIn(scope) caches the PagingData stream in a scope (viewModelScope) so it survives config changes and is shared/multi-collectable (a raw Pager.flow can only be collected once). Place it last in the ViewModel, after transforms you want cached. Without it, rotation re-fetches and multi-collect crashes."}]},{level:"senior",q:"How do you test a PagingSource?",a:[{t:"p",text:"Test the `PagingSource` directly by calling `load()` with `LoadParams.Refresh`/`Append` and asserting the returned `LoadResult.Page` (data, prevKey, nextKey) or `LoadResult.Error`. Use a fake API/DAO. For the whole flow (adapter behavior, diffing), the `AsyncPagingDataDiffer`/snapshot APIs let you assert the presented items. Test edge cases: first page, empty result (nextKey null), and errors."},{t:"code",title:"Testing load()",code:`@Test fun firstPage() = runTest {
    val source = UserPagingSource(fakeApi)
    val result = source.load(LoadParams.Refresh(key = null, loadSize = 20, false))
    assertEquals(expectedUsers, (result as LoadResult.Page).data)
    assertEquals(2, result.nextKey)
}`},{t:"list",items:["**Call `load()`** — assert `LoadResult.Page`/`Error` and keys.","**Fake API/DAO** — controlled data/errors.","**Edge cases** — first page, end (nextKey null), errors.","**Full flow** — `AsyncPagingDataDiffer`/snapshot for presented items."]},{t:"note",text:"Test a PagingSource by calling load(LoadParams.Refresh/Append) and asserting LoadResult.Page (data, prevKey, nextKey) or Error, with a fake API/DAO — cover first page, end (nextKey null), and errors. For the full presented flow, use AsyncPagingDataDiffer/snapshot to assert items after diffing."}]},{level:"junior",q:"How does prefetchDistance control loading, and how do you tune page size?",a:[{t:"p",text:"`prefetchDistance` is how many items from the edge of the loaded data the user must scroll before Paging loads the *next* page — a larger value loads earlier (smoother, more eager) but uses more data upfront. `pageSize` is items per page (and roughly the load granularity). Tune them so pages load *before* the user reaches the end (avoiding visible spinners) without over-fetching; `initialLoadSize` (default 3×pageSize) sizes the first load."},{t:"list",items:["**`prefetchDistance`** — how far from the edge to trigger the next load.","**`pageSize`** — items per page (load granularity).","**`initialLoadSize`** — first load (default 3× pageSize).","**Tune** — load before the user hits the end, without over-fetching."]},{t:"note",text:"prefetchDistance = how many items from the loaded edge trigger the next page load (larger = earlier/smoother, more data upfront); pageSize = items per page; initialLoadSize = first load (default 3× pageSize). Tune so pages load before the user reaches the end (no visible spinners) without over-fetching."}]},{level:"senior",q:"How does Paging 3 handle duplicate or changing items across pages?",a:[{t:"p",text:"Paging uses `DiffUtil` (via a `DiffUtil.ItemCallback`) to diff presented items and animate insertions/updates/removals — so provide accurate `areItemsTheSame` (by id) and `areContentsTheSame`. Server-side pagination that reorders/inserts items can cause *duplicates or skips* across pages; mitigate with stable keyset (cursor) pagination rather than offset pagination, which is prone to shifting."},{t:"list",items:["**`DiffUtil.ItemCallback`** — `areItemsTheSame` (id) + `areContentsTheSame`.","**Diffing** — animates changes, avoids full rebinds.","**Offset pagination** — prone to duplicates/skips when data shifts.","**Keyset/cursor pagination** — stable against inserts/reorders."]},{t:"note",text:"Paging diffs presented items via DiffUtil.ItemCallback (areItemsTheSame by id, areContentsTheSame) to animate changes. Offset pagination can duplicate/skip items when the dataset shifts between page loads — prefer keyset (cursor) pagination, which is stable against inserts/reorders."}]},{level:"junior",q:"When should you NOT use Paging 3?",a:[{t:"p",text:"Paging adds complexity (PagingSource, load states, PagingData) that isn't worth it for *small, bounded* lists you can load at once — a settings list, a fixed set of categories, a short search result. Use a plain `List`/`Flow<List>` there. Reserve Paging for *large or unbounded* datasets (feeds, search over big corpora, chat history) where loading everything is wasteful or impossible."},{t:"list",items:["**Small/bounded lists** — load at once with `Flow<List>`; skip Paging.","**Fixed data** — settings, categories, short results.","**Use Paging for** — large/unbounded feeds, big search, infinite scroll.","**Complexity cost** — PagingSource/load states aren't free."]},{t:"note",text:"Skip Paging 3 for small, bounded lists you can load at once (settings, categories, short results) — use a plain Flow<List>. Its machinery (PagingSource, load states, PagingData) is worth it only for large/unbounded datasets (feeds, big search, chat history) where loading everything is wasteful/impossible."}]},{level:"senior",q:"How do you combine paged data with other reactive state (e.g. a filter)?",a:[{t:"p",text:"Drive the `Pager` from a trigger flow (the filter/query) using `flatMapLatest` — when the filter changes, create a *new* `Pager` (new `PagingSource`), and `cachedIn` the result. For presentation-only combos (a favorites set overlaid on items), transform the `PagingData` with `.map` combining the external state. Don't try to `combine` a `PagingData` flow with a normal flow directly — recreate the pager on filter changes."},{t:"code",title:"Filter-driven paging",code:`val paged = queryFlow.flatMapLatest { query ->
    Pager(config) { UserPagingSource(api, query) }.flow
}.cachedIn(viewModelScope)`},{t:"list",items:["**`flatMapLatest`** — recreate the `Pager` when the filter/query changes.","**`cachedIn`** — after the flatMapLatest.","**Presentation combos** — transform `PagingData` with `.map`.","**Don't `combine` PagingData** — recreate the pager instead."]},{t:"note",text:"Drive the Pager from a trigger flow (filter/query) with flatMapLatest — a filter change creates a new Pager/PagingSource — then cachedIn. For presentation-only combos (favorites overlay), transform PagingData with .map. Don't combine a PagingData flow with a normal flow; recreate the pager on changes."}]},{level:"junior",q:"What problem does pagination solve compared to loading everything?",a:[{t:"p",text:"Loading an entire large dataset at once is slow (long initial wait), memory-heavy (thousands of items in RAM), and wasteful (the user may scroll only a bit). *Pagination* loads data in *chunks as needed* — fast initial display, bounded memory, and less bandwidth. Paging 3 automates the mechanics (loading, diffing, states, retries) so you don't hand-roll scroll listeners and offsets."},{t:"list",items:["**Fast initial load** — show the first page immediately.","**Bounded memory** — only loaded pages held.","**Less bandwidth** — load only what's viewed.","**Paging 3 automates** — loading triggers, diffing, states, retries."]},{t:"note",text:"Loading everything is slow (long wait), memory-heavy (thousands in RAM), and wasteful (user scrolls a little). Pagination loads chunks as needed — fast initial display, bounded memory, less bandwidth. Paging 3 automates the mechanics (load triggers, diffing, states, retries) vs hand-rolled scroll listeners/offsets."}]},{level:"senior",q:"What is the difference between offset and keyset (cursor) pagination?",a:[{t:"p",text:"*Offset* pagination uses `LIMIT n OFFSET m` (or page numbers) — simple, but slow for deep pages (the DB scans/skips m rows) and *unstable* when items are inserted/deleted (rows shift, causing duplicates/skips). *Keyset* (cursor) pagination uses a `WHERE id > lastSeenId ORDER BY id LIMIT n` — fast at any depth (uses an index) and *stable* against inserts. Prefer keyset for large or actively-changing datasets."},{t:"table",headers:["","Offset","Keyset (cursor)"],rows:[["Deep pages","slow (scans/skips)","fast (indexed)"],["Stability","shifts on insert/delete","stable"],["Simplicity","simple","needs a stable sort key"]]},{t:"list",items:["**Offset** — `LIMIT/OFFSET`/page numbers; simple; slow deep; unstable.","**Keyset** — `WHERE key > cursor`; fast (indexed); stable.","**Keyset needs** — a stable, unique sort key (id/timestamp).","**Prefer keyset** — for large/changing data."]},{t:"note",text:"Offset pagination (LIMIT/OFFSET/page numbers): simple but slow for deep pages (scans/skips) and unstable when items shift (duplicates/skips). Keyset/cursor (WHERE key > cursor ORDER BY key): fast at any depth (indexed) and stable against inserts. Prefer keyset for large/actively-changing datasets."}]},{level:"senior",q:"What are the three LoadTypes in RemoteMediator?",a:[{t:"p",text:"`RemoteMediator.load` receives a `LoadType`: `REFRESH` (initial load or `refresh()` — load the first page, often clearing the cache), `APPEND` (user scrolled to the end — load the next page after the last item), and `PREPEND` (scrolled to the start — load before the first item, often returning success with `endOfPaginationReached = true` if you don't support backward loading)."},{t:"list",items:["**`REFRESH`** — initial/refresh; load the first page (may clear cache in a transaction).","**`APPEND`** — end reached; load the next page after the last item.","**`PREPEND`** — start reached; load before the first (or signal end).","**`endOfPaginationReached`** — return in `MediatorResult.Success` when no more pages."]},{t:"note",text:"RemoteMediator LoadTypes: REFRESH (initial/refresh — load first page, often clearing cache), APPEND (end reached — next page after the last item), PREPEND (start reached — before the first, or signal end). Return MediatorResult.Success(endOfPaginationReached = true) when there are no more pages."}]},{level:"senior",q:"What is the remote keys table, and why does RemoteMediator need it?",a:[{t:"p",text:"Because the `PagingSource` reads from the DB (which doesn't know network page numbers), `RemoteMediator` needs to *track pagination keys* — so you keep a `RemoteKeys` table mapping each item (or page) to its `prevKey`/`nextKey`. On `APPEND`, you look up the last item's `nextKey` to fetch the next network page; on `REFRESH`, you clear and rebuild. Without it, the mediator can't know which page to fetch."},{t:"code",title:"Remote keys",code:`@Entity data class RemoteKeys(
    @PrimaryKey val itemId: String, val prevKey: Int?, val nextKey: Int?)
// APPEND: fetch remoteKeysForLastItem(state)?.nextKey`},{t:"list",items:["**Tracks pagination** — maps items/pages to prev/next network keys.","**`APPEND`** — look up the last item's `nextKey`.","**`REFRESH`** — clear keys + data in a transaction, refetch.","**Needed** — the DB-backed PagingSource doesn't know network page numbers."]},{t:"note",text:"A RemoteKeys table maps items/pages to prevKey/nextKey so RemoteMediator knows which network page to fetch (the DB-backed PagingSource doesn't track network page numbers). APPEND uses the last item's nextKey; REFRESH clears keys+data in a transaction and refetches. Essential for DB-backed paging."}]},{level:"junior",q:"How do you show an empty state in a paged list?",a:[{t:"p",text:"Detect empty by checking that the `refresh` load state is `NotLoading` (finished) *and* `itemCount == 0` — then show an empty-state UI instead of the list. You must combine the load state with the item count, because a zero count during loading isn't 'empty', it's 'not loaded yet'."},{t:"code",title:"Empty detection",code:`val items = flow.collectAsLazyPagingItems()
val isEmpty = items.loadState.refresh is LoadState.NotLoading && items.itemCount == 0
if (isEmpty) EmptyView() else LazyColumn { /* items */ }`},{t:"list",items:["**Empty = refresh NotLoading + itemCount 0** — finished loading with no items.","**Not during load** — zero count while loading isn't empty.","**Distinct states** — loading, empty, error, content.","**Show empty UI** — illustration/CTA instead of the list."]},{t:"note",text:"Empty state = loadState.refresh is NotLoading AND itemCount == 0 (finished loading with no items) — combine load state with count, since zero count while loading is 'not loaded yet', not 'empty'. Show a distinct empty UI (illustration/CTA) vs loading/error/content."}]},{level:"junior",q:"How do you implement pull-to-refresh with Paging?",a:[{t:"p",text:"Wrap the paged list in a pull-to-refresh container and call `lazyPagingItems.refresh()` (Compose) or `adapter.refresh()` (Views) on pull. Drive the refresh indicator from the `refresh` load state (`loadState.refresh is LoadState.Loading`). Paging reloads from the start; for DB-backed paging, `RemoteMediator`'s `REFRESH` refetches and updates the DB."},{t:"code",title:"Pull-to-refresh",code:`val items = flow.collectAsLazyPagingItems()
val refreshing = items.loadState.refresh is LoadState.Loading
PullToRefreshBox(isRefreshing = refreshing, onRefresh = { items.refresh() }) {
    LazyColumn { /* items */ }
}`},{t:"list",items:["**`refresh()`** — reload from the start on pull.","**Indicator from load state** — `loadState.refresh is Loading`.","**RemoteMediator REFRESH** — refetches and updates the DB.","**Don't reset scroll manually** — Paging handles the reload."]},{t:"note",text:"Wrap the paged list in a pull-to-refresh container and call lazyPagingItems.refresh() (Compose) / adapter.refresh() (Views) on pull, driving the indicator from loadState.refresh is Loading. Paging reloads from the start; RemoteMediator's REFRESH refetches and updates the DB."}]},{level:"senior",q:"What is the maxSize config, and when are pages dropped?",a:[{t:"p",text:"By default Paging keeps *all* loaded pages in memory as you scroll. Setting `PagingConfig.maxSize` caps the loaded window — when it's exceeded, Paging *drops* pages furthest from the viewport (and reloads them if you scroll back). This bounds memory for very long scrolls, at the cost of re-loading dropped pages. It requires placeholders (or careful handling) so positions stay stable."},{t:"list",items:["**Default** — keeps all loaded pages in memory.","**`maxSize`** — caps the loaded window; drops far-from-viewport pages.","**Trade-off** — bounded memory vs reloading dropped pages on scroll-back.","**Placeholders** — needed for stable positions when dropping."]},{t:"note",text:"By default Paging keeps all loaded pages in memory. PagingConfig.maxSize caps the loaded window — exceeding it drops pages furthest from the viewport (reloaded on scroll-back). Bounds memory for very long lists at the cost of reloads; needs placeholders for stable positions. Only set it for extreme cases."}]},{level:"senior",q:"How do you paginate a debounced search?",a:[{t:"p",text:"Hold the query in a `StateFlow`, `debounce` + `distinctUntilChanged` it, then `flatMapLatest` to a new `Pager` per query (`Pager(config) { SearchPagingSource(api, query) }.flow`), and `cachedIn`. Each new query cancels the previous pager and starts a fresh paged search — combining debounced input with paginated results."},{t:"code",title:"Paged search",code:`val results = queryFlow
    .debounce(300).distinctUntilChanged()
    .flatMapLatest { q -> Pager(config) { SearchPagingSource(api, q) }.flow }
    .cachedIn(viewModelScope)`},{t:"list",items:["**Query `StateFlow`** — updated from the text field.","**`debounce` + `distinctUntilChanged`** — one search per typing pause.","**`flatMapLatest` → new `Pager`** — cancels the old search, paginates the new.","**`cachedIn`** — after the flatMapLatest."]},{t:"note",text:"Paged search: query StateFlow → debounce(300) + distinctUntilChanged → flatMapLatest to a new Pager per query (Pager(config){ SearchPagingSource(api, q) }.flow) → cachedIn. Each query cancels the previous pager and starts a fresh paginated search. Combines debounced input with pagination."}]},{level:"senior",q:"How do you add header/footer load-state UI to a paged list?",a:[{t:"p",text:"In Compose, add `item { }` blocks at the top/bottom of the `LazyColumn` conditioned on `loadState.prepend`/`loadState.append` (spinner or retry row). In Views, use `adapter.withLoadStateHeaderAndFooter(header, footer)` with `LoadStateAdapter`s that render the loading/error/retry views. This shows progress/retry inline at the list ends as more data loads."},{t:"code",title:"Compose append footer",code:`LazyColumn {
    items(pagingItems.itemCount) { /* row */ }
    when (val append = pagingItems.loadState.append) {
        is LoadState.Loading -> item { Spinner() }
        is LoadState.Error -> item { RetryRow { pagingItems.retry() } }
        else -> {}
    }
}`},{t:"list",items:["**Compose** — conditional `item { }` on `loadState.append`/`prepend`.","**Views** — `withLoadStateHeaderAndFooter(LoadStateAdapter)`.","**States** — loading spinner, error + retry.","**Inline** — at the list ends as more loads."]},{t:"note",text:"Header/footer load-state UI: Compose adds conditional item { } blocks on loadState.prepend/append (spinner/retry); Views use adapter.withLoadStateHeaderAndFooter(LoadStateAdapter). Render loading spinners and error+retry rows inline at the list ends as more data loads."}]},{level:"senior",q:"How do you keep paged list item rendering performant?",a:[{t:"p",text:"Same principles as any lazy list, plus paging specifics: provide stable `key`s (`itemKey { it.id }`) and `contentType` for mixed items; keep item composition cheap (precompute in a mapper, avoid heavy work per item); size images to the display; use accurate `DiffUtil` callbacks so only changed items rebind; and don't over-transform `PagingData` on the hot path. Profile on a release build."},{t:"list",items:["**Stable keys + contentType** — correct diffing/reuse.","**Cheap item content** — precompute; avoid heavy per-item work.","**Right-sized images** — Coil to the display size.","**Accurate DiffUtil** — minimal rebinds; profile on release."]},{t:"note",text:"Performant paged lists: stable keys (itemKey { it.id }) + contentType, cheap item content (precompute in a mapper), right-sized image loading, accurate DiffUtil callbacks (minimal rebinds), and avoid heavy PagingData transforms on the hot path. Profile on a release build on a real device."}]}],OA={id:"paging",title:"Paging 3",tagline:"Paginating large lists — PagingSource/Pager/PagingData, cachedIn, consuming in the UI, RemoteMediator for offline paging, and when not to use Paging.",tags:["Paging 3","PagingSource","RemoteMediator","cachedIn","Room"],content:UA,qa:qA},FA={id:"data-storage",name:"Data & Storage",description:"Local persistence — Room & SQLite, relations & migrations, DataStore vs SharedPreferences, caching & offline-first architecture, and Paging 3.",topics:[SA,TA,PA,EA,OA]},LA=[{heading:"HTTP — the request/response protocol",blocks:[{t:"p",text:"**HTTP (HyperText Transfer Protocol)** is the protocol apps use to talk to servers. It's a **request/response** model: the client (your app) sends a *request* to a URL, the server processes it and sends back a *response*. It's *stateless* — each request is independent, carrying everything the server needs (the server doesn't remember previous requests unless you send state like a token or cookie each time). Understanding the anatomy of requests and responses is the foundation of all networking."},{t:"code",title:"Anatomy of a request and response",code:`// REQUEST
GET /api/users/42 HTTP/1.1          // method + path + version
Host: api.example.com               // headers (metadata)
Authorization: Bearer eyJ...
Accept: application/json
// (body — for POST/PUT: the data being sent)

// RESPONSE
HTTP/1.1 200 OK                     // status line: version + status code
Content-Type: application/json      // headers
{ "id": "42", "name": "Revanth" }   // body — the returned data`},{t:"list",items:["**A request has**: a *method* (GET, POST…), a *URL/path*, *headers* (metadata: auth token, content type, accept), and optionally a *body* (data sent to the server, for POST/PUT).","**A response has**: a *status code* (200, 404…), *headers* (content type, caching info), and a *body* (the returned data, usually JSON).","**Stateless**: the server treats each request independently. To maintain a 'session', the client includes identifying state (an auth token in the `Authorization` header) on every request."]}]},{heading:"HTTP methods (verbs)",blocks:[{t:"table",headers:["Method","Purpose","Has body?","Idempotent?"],rows:[["GET","read/retrieve data","no","yes (safe — no side effects)"],["POST","create a new resource / submit data","yes","no (repeating creates duplicates)"],["PUT","replace/update a resource fully","yes","yes (same result if repeated)"],["PATCH","partially update a resource","yes","not necessarily"],["DELETE","remove a resource","usually no","yes (deleting twice = still deleted)"]]},{t:"list",items:["**GET** — retrieve data; should have *no side effects* (safe) and be cacheable. Never use GET to change server state.","**POST** — create a resource or submit data (a form, a new item). *Not idempotent*: sending it twice creates two resources — which is why retrying a POST needs care (idempotency keys).","**PUT vs PATCH** — PUT replaces the *entire* resource (send all fields); PATCH updates *part* of it (send only changed fields). PUT is idempotent (same result each time); PATCH may not be.","**Idempotent** means repeating the request produces the same result — important for *retries*: safe to retry GET/PUT/DELETE, risky to blindly retry POST."]}]},{heading:"Status codes",blocks:[{t:"table",headers:["Range","Meaning","Examples"],rows:[["2xx","success","200 OK, 201 Created, 204 No Content"],["3xx","redirection","301 Moved Permanently, 304 Not Modified"],["4xx","client error (your request is wrong)","400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 429 Too Many Requests"],["5xx","server error (the server failed)","500 Internal Server Error, 502 Bad Gateway, 503 Service Unavailable"]]},{t:"list",items:["**The first digit is the category** — 2xx success, 3xx redirect, 4xx client's fault, 5xx server's fault. Knowing the category tells you how to react.","**Key ones to handle**: 401 (unauthenticated — refresh the token or log in), 403 (forbidden — no permission), 404 (not found), 429 (rate-limited — back off and retry), 500/503 (server error — retry with backoff).","**4xx vs 5xx for retries**: 5xx (server errors) are often *transient* — retrying may succeed. 4xx (client errors) mean *your request is wrong* — retrying the same request will fail identically, so don't retry (except 401 after refreshing auth, or 429 after waiting).","**304 Not Modified** — the caching response: 'the data hasn't changed since your cached version', so no body is sent (efficient conditional requests via ETag)."]}]},{heading:"REST — the API design style",blocks:[{t:"p",text:"**REST (Representational State Transfer)** is an architectural style for designing web APIs around *resources* (nouns) manipulated with the standard HTTP methods (verbs). A resource is identified by a URL, and you act on it with GET/POST/PUT/DELETE. Most Android apps consume REST APIs, so recognizing RESTful design is important."},{t:"code",title:"RESTful resource design",code:`GET    /api/users          // list all users
POST   /api/users          // create a new user (body = user data)
GET    /api/users/42       // get user 42
PUT    /api/users/42       // replace user 42
PATCH  /api/users/42       // partially update user 42
DELETE /api/users/42       // delete user 42
GET    /api/users/42/posts // user 42's posts (nested resource)`},{t:"list",items:["**Resources as nouns, methods as verbs**: the URL names *what* (a user, a collection of users), the HTTP method says *what to do* (read, create, update, delete). Well-designed REST URLs don't have verbs (`/getUser` is not RESTful; `GET /users/42` is).","**Statelessness**: each request is self-contained (carries its own auth); the server keeps no client session state between requests.","**JSON is the common data format** — requests and responses carry JSON bodies (covered in the serialization topic).","**Alternatives to know**: **GraphQL** (client specifies exactly what data it wants in one flexible query — avoids over/under-fetching), **gRPC** (binary protocol over HTTP/2, efficient for service-to-service). REST is still the most common for mobile, but knowing the alternatives shows breadth."]}]},{heading:"Headers, auth, and content negotiation",blocks:[{t:"list",items:["**`Authorization` header** — carries credentials, most commonly `Bearer <token>` for token-based auth. Sent on every request (statelessness) to identify the user.","**`Content-Type`** — declares the format of the *request body* you're sending (`application/json`). **`Accept`** — tells the server what format you want *back*. Together they're 'content negotiation'.","**Caching headers** — `Cache-Control`, `ETag`, `Last-Modified` control HTTP-level caching. An `ETag` is a version fingerprint; sending `If-None-Match: <etag>` lets the server reply `304 Not Modified` (no body) if unchanged — efficient conditional fetching.","**HTTPS is mandatory** — all traffic should be encrypted (TLS). Android blocks cleartext (plain HTTP) by default since Android 9. For extra security, **certificate pinning** restricts which certificates your app trusts (preventing man-in-the-middle attacks) — important for sensitive apps."]},{t:"note",text:"HTTP/REST essentials: request (method + URL + headers + optional body) / response (status code + headers + body); stateless (send auth each time). Methods: GET (read, safe), POST (create, not idempotent), PUT (replace, idempotent), PATCH (partial), DELETE. Status codes by category: 2xx success, 4xx client error (don't blindly retry), 5xx server error (retry with backoff). REST = resources (URLs) + verbs (methods). Use HTTPS; know GraphQL/gRPC as alternatives."}]}],jA=[{level:"junior",q:"What are the main HTTP methods and what is each for?",a:[{t:"list",items:["**GET** — retrieve data. It should be *safe* (no side effects) and is cacheable. Use it for reads only; never change server state with a GET.","**POST** — create a new resource or submit data (a form, a new item). It carries a body and is *not idempotent* — sending it twice creates two resources.","**PUT** — replace/update a resource *fully* (you send all its fields). It's idempotent — the same request produces the same result each time.","**PATCH** — partially update a resource (send only the changed fields).","**DELETE** — remove a resource. Idempotent (deleting something twice leaves it deleted)."]},{t:"p",text:"The concept that ties these together is *idempotency* — whether repeating a request has the same effect as doing it once. GET, PUT, and DELETE are idempotent, which makes them safe to retry (important when a request times out and you're unsure if it went through). POST is *not* idempotent, so blindly retrying it can create duplicates — which is why retrying a POST needs care (like an idempotency key the server uses to dedupe). Knowing which methods are safe to retry directly informs how you build resilient networking."}]},{level:"junior",q:"What do HTTP status code categories mean?",a:[{t:"p",text:"**The first digit of a status code tells you the category of outcome**, which tells you how to react:"},{t:"list",items:["**2xx — Success**: the request worked. `200 OK` (here's the data), `201 Created` (resource created), `204 No Content` (success, nothing to return).","**3xx — Redirection**: further action needed. `301 Moved Permanently`, `304 Not Modified` (your cached version is still current — no body sent, used for efficient caching).","**4xx — Client error**: *your request* is wrong. `400 Bad Request`, `401 Unauthorized` (not authenticated), `403 Forbidden` (no permission), `404 Not Found`, `429 Too Many Requests` (rate-limited).","**5xx — Server error**: the *server* failed. `500 Internal Server Error`, `502 Bad Gateway`, `503 Service Unavailable`."]},{t:"p",text:"The practical distinction for handling errors: **5xx errors are often transient** (the server hiccupped), so retrying with backoff may succeed. **4xx errors mean your request is fundamentally wrong**, so retrying the *same* request will fail identically — don't retry (with two exceptions: retry after a `401` if you can refresh the auth token, and retry after a `429` once you've waited the required time). Mapping status codes to the right reaction — retry, re-authenticate, show an error, back off — is core to robust networking."}]},{level:"junior",q:"What is REST?",a:[{t:"p",text:"**REST (Representational State Transfer) is an architectural style for web APIs, organized around *resources* (nouns) that you act on with the standard HTTP *methods* (verbs).** A resource — a user, a post, a collection of orders — is identified by a URL, and you manipulate it with GET (read), POST (create), PUT/PATCH (update), and DELETE (remove)."},{t:"code",title:"RESTful design",code:`GET    /users        // list users
POST   /users        // create a user
GET    /users/42     // read user 42
PUT    /users/42     // replace user 42
DELETE /users/42     // delete user 42`},{t:"p",text:"The key ideas: **URLs name resources (nouns), not actions** — `/getUser` is not RESTful, but `GET /users/42` is (the verb is the HTTP method, not part of the URL). It's **stateless** — each request carries everything the server needs (like an auth token), and the server keeps no session between requests. Data is typically exchanged as **JSON**. Most Android apps consume REST APIs. Worth knowing as alternatives: **GraphQL** (the client specifies exactly what fields it wants in one query, avoiding over- and under-fetching) and **gRPC** (an efficient binary protocol over HTTP/2, common for backend-to-backend). REST remains the most common for mobile, but recognizing when an API is or isn't RESTful, and knowing the alternatives, shows solid fundamentals."}]},{level:"junior",q:"How does authentication typically work in a REST API?",a:[{t:"p",text:"**Because HTTP is stateless, the client must prove its identity on *every* request — most commonly by sending a token in the `Authorization` header.** The typical flow: the user logs in (POST credentials), the server returns a token (often a JWT), and the app then includes `Authorization: Bearer <token>` on every subsequent request. The server validates the token to identify the user, without keeping any session state itself."},{t:"list",items:["**Token-based (Bearer)** — the standard for mobile. The token is a credential the server issues and the client presents on each request. Tokens usually expire for security.","**Access + refresh tokens** — a common pattern: a short-lived *access token* (used on requests) plus a long-lived *refresh token* (used to get a new access token when it expires, without re-login). This limits the damage if an access token leaks.","**Where to store tokens** — securely: `EncryptedSharedPreferences`, or encrypted storage, or the Android Keystore — never in plaintext.","**How it's applied** — in Android you typically add the token to every request automatically with an OkHttp *interceptor* (rather than manually on each call), and handle token refresh in an `Authenticator` when a request returns 401."]},{t:"p",text:"The stateless nature of HTTP is *why* auth works this way — the server can't 'remember' you're logged in between requests, so you re-assert your identity every time via the token. This is different from older cookie/session server-side approaches; mobile APIs almost always use stateless token auth."}]},{level:"senior",q:"Which HTTP requests are safe to retry, and why does idempotency matter for a mobile app?",a:[{t:"p",text:"**Idempotent requests — those where repeating them has the same effect as doing them once — are safe to retry; non-idempotent ones are not, because a retry could duplicate an action.** GET, PUT, and DELETE are idempotent (reading twice, replacing twice, or deleting twice all leave the same end state). POST is *not* idempotent — each POST typically creates a new resource, so retrying it can create duplicates."},{t:"list",items:["**Why this matters acutely on mobile**: mobile networks are flaky. A request can *time out or lose the connection after the server processed it but before the response reached the app* — so the app doesn't know whether it succeeded. For an idempotent request (GET/PUT/DELETE), you can just retry safely. For a POST (say, 'place order' or 'send payment'), a blind retry might place *two* orders or charge *twice* — a serious bug.","**The solution for non-idempotent requests — idempotency keys**: the client generates a unique key (a UUID) per logical operation and sends it (e.g. in an `Idempotency-Key` header). The server records processed keys; if it sees the same key again (from a retry), it returns the *original* result instead of performing the action again. This makes even POST safe to retry. Payment APIs (Stripe, etc.) universally use this.","**Which errors to retry**: retry on network failures and 5xx (transient server errors), typically with *exponential backoff* (increasing delays) to avoid hammering a struggling server. Do *not* retry 4xx client errors (the request is wrong — it'll fail identically), except 401 after refreshing the token and 429 after honoring the rate-limit delay.","**Bound retries**: cap the number of attempts and add jitter (randomized backoff) so many clients don't retry in lockstep (the 'thundering herd' problem)."]},{t:"p",text:"**The senior framing**: retry logic isn't just 'try again on failure' — it's a correctness concern rooted in idempotency. On unreliable mobile networks you *will* face ambiguous failures (did it go through?), so you design for them: retry idempotent operations freely, make critical non-idempotent operations idempotent via keys, retry only transient (5xx/network) errors with bounded exponential backoff and jitter, and never blindly retry a POST that could double a side effect. This is what separates a robust networking layer from one that occasionally double-charges users."}]},{level:"senior",q:"What is an ETag and how does conditional caching with 304 work?",a:[{t:"p",text:"**An ETag (Entity Tag) is a version fingerprint the server assigns to a resource — a short identifier (like a hash) that changes whenever the resource's content changes. It enables *conditional requests*: the client can ask 'give me this resource, but only if it changed since the version I have', letting the server skip sending the body when nothing changed.**"},{t:"list",items:['**How it flows**: the first response includes an `ETag: "abc123"` header. The client caches the response *and* the ETag. On the next request for that resource, the client sends `If-None-Match: "abc123"`. The server compares: if the resource still has ETag `abc123` (unchanged), it responds `304 Not Modified` with *no body* — telling the client \'your cached copy is current\'. If it changed, the server responds `200 OK` with the new body and a new ETag.',"**Why it's efficient**: a `304` response is tiny (just headers, no body), so you 'refresh' the data — confirming it's current — without re-downloading it. For large responses or frequent polling, this saves enormous bandwidth and time. The client keeps showing its cached data on a 304, and only replaces it on a 200.","**`Last-Modified` / `If-Modified-Since`** is a timestamp-based alternative to ETags (same idea, using a modification date instead of a fingerprint). ETags are more precise (they detect any content change, even if the timestamp is the same).","**In Android**: OkHttp handles this automatically at the HTTP layer if you configure a `Cache` — it stores responses with their ETags and transparently sends conditional requests, serving the cached body on a 304. You get efficient caching for free without app-level code."]},{t:"p",text:"**The senior framing**: ETags implement 'stale-while-revalidate' efficiently at the protocol level — you can check freshness *cheaply* (a conditional request that usually returns a bodyless 304) rather than re-fetching. This is complementary to app-level caching (Room as SSOT): ETags optimize the *network* layer's freshness checks, while Room gives you queryable, offline, observable structured data. For frequently-polled or large resources that rarely change, ETag-based conditional caching is the right tool to minimize data usage — and it's why understanding HTTP caching headers matters even when you also cache in a database."}]},{level:"junior",q:"Which HTTP methods are idempotent and safe, and what do those terms mean?",a:[{t:"p",text:"*Safe* means the request has no side effects (read-only): `GET`, `HEAD`, `OPTIONS`. *Idempotent* means making the request multiple times has the same effect as once: `GET`, `PUT`, `DELETE` (deleting twice ends in the same state). `POST` is *neither* — it creates a new resource each time, so retrying it can duplicate. This matters for safe retries."},{t:"table",headers:["Method","Safe","Idempotent"],rows:[["GET/HEAD","yes","yes"],["PUT/DELETE","no","yes"],["POST","no","no"],["PATCH","no","usually not"]]},{t:"list",items:["**Safe** — read-only, no side effects (GET/HEAD/OPTIONS).","**Idempotent** — same result if repeated (GET/PUT/DELETE).","**POST** — neither; retrying can create duplicates.","**Retry policy** — safely retry idempotent methods; use idempotency keys for POST."]},{t:"note",text:"Safe = no side effects (GET/HEAD/OPTIONS). Idempotent = repeating gives the same result (GET/PUT/DELETE). POST is neither (retrying can duplicate); PATCH usually isn't. Retry idempotent methods freely; use an idempotency key to safely retry POST."}]},{level:"junior",q:"What is the difference between PUT and PATCH?",a:[{t:"p",text:"`PUT` *replaces* the entire resource with the provided representation — you send the full object, and missing fields are cleared. `PATCH` *partially updates* — you send only the fields to change. Use `PUT` for a full replace (idempotent), `PATCH` for targeted updates (often smaller payloads)."},{t:"list",items:["**`PUT`** — full replacement; send the whole resource; idempotent.","**`PATCH`** — partial update; send only changed fields.","**Payload** — PUT is complete; PATCH is minimal.","**Semantics** — PUT clears omitted fields; PATCH leaves them."]},{t:"note",text:"PUT replaces the entire resource (send the full object; omitted fields cleared; idempotent). PATCH partially updates (send only changed fields). Use PUT for full replace, PATCH for targeted updates with smaller payloads."}]},{level:"junior",q:"What do the key HTTP status codes mean, and how should you handle each category?",a:[{t:"p",text:"Categories: *2xx* success (200 OK, 201 Created, 204 No Content), *3xx* redirect (301/302/304), *4xx* client error (400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 409 Conflict, 429 Too Many Requests), *5xx* server error (500, 502, 503). Handling differs: 4xx means *you* sent something wrong (usually don't retry — fix the request or surface the error); 5xx is a *server* problem (retry with backoff)."},{t:"list",items:["**2xx** — success; process the body (204 has none).","**401/403** — auth: refresh token (401) or show forbidden (403).","**404/409** — not found / conflict: surface to the user.","**429** — rate limited: back off (respect `Retry-After`).","**5xx** — server error: retry with backoff."]},{t:"note",text:"2xx success (200/201/204), 3xx redirect (301/302/304), 4xx client error (400/401/403/404/409/429), 5xx server error (500/502/503). 4xx = your request is wrong (usually don't retry — fix/surface; 401→refresh, 429→back off with Retry-After); 5xx = server problem (retry with backoff)."}]},{level:"junior",q:"What are the principles of good REST resource/URI design?",a:[{t:"p",text:"REST models *resources* (nouns) addressed by URIs, manipulated with HTTP methods. Good design: use plural nouns (`/users`, `/users/42`), nest for relationships (`/users/42/orders`), avoid verbs in URIs (the method is the verb), use query params for filtering/sorting/pagination, and keep URIs consistent and hierarchical. Actions map to methods, not URL segments (`DELETE /users/42`, not `/deleteUser`)."},{t:"list",items:["**Resources as nouns** — `/users`, `/users/42` (plural, hierarchical).","**Nesting** — `/users/42/orders` for relationships.","**Methods are verbs** — no `/deleteUser`; use `DELETE`.","**Query params** — filtering/sorting/pagination (`?sort=&page=`)."]},{t:"note",text:"REST design: resources as plural nouns (/users, /users/42), nest for relationships (/users/42/orders), no verbs in URIs (the HTTP method is the verb), query params for filter/sort/paginate. Consistent, hierarchical URIs; actions map to methods (DELETE /users/42), not URL segments."}]},{level:"senior",q:"What is content negotiation (Accept / Content-Type)?",a:[{t:"p",text:"Content negotiation lets client and server agree on the data format. The `Content-Type` header describes the format of the *body being sent* (e.g. `application/json`), and the `Accept` header tells the server what formats the client *can handle* in the response. The server responds in a supported format (with its own `Content-Type`). Most mobile APIs use `application/json` for both."},{t:"list",items:["**`Content-Type`** — format of the request body (`application/json`).","**`Accept`** — formats the client accepts in the response.","**Server picks** — a supported format; sets response `Content-Type`.","**Versioning** — some APIs negotiate versions via `Accept` media types."]},{t:"note",text:"Content negotiation: Content-Type describes the body being sent (application/json); Accept tells the server what the client can handle in the response; the server replies in a supported format (with its Content-Type). Mostly application/json in mobile; some APIs version via Accept media types."}]},{level:"senior",q:"How is pagination typically done in REST APIs?",a:[{t:"p",text:'Common approaches: *offset/limit* (`?offset=40&limit=20` or `?page=3&size=20`) — simple but shifts on inserts; *cursor/keyset* (`?cursor=abc`) — stable and efficient for large data; and *Link headers* (RFC 5988, with `rel="next"`/`"prev"` URLs) so the client follows links rather than constructing them. The server also returns total counts or a `next` token.'},{t:"list",items:["**Offset/page** — `?page=&size=`; simple, but shifts on inserts.","**Cursor/keyset** — `?cursor=`; stable, efficient for big data.",'**Link headers** — `Link: <url>; rel="next"` to follow.',"**Client** — follow `next` (with `@Url`) or build the next page."]},{t:"note",text:'REST pagination: offset/page (?page=&size= — simple, shifts on inserts), cursor/keyset (?cursor= — stable/efficient for large data), or Link headers (rel="next"/"prev" URLs to follow). Servers return total counts or a next token. Prefer cursor pagination for large/changing datasets.'}]},{level:"senior",q:"How do you handle rate limiting (429) on the client?",a:[{t:"p",text:"A `429 Too Many Requests` means you've hit the API's rate limit. The response usually includes a `Retry-After` header (seconds or a date) telling you when to retry. Honor it — wait that long before retrying — and proactively throttle your requests (debounce, batch, cache) to avoid hitting the limit. Don't retry immediately in a loop (that makes it worse)."},{t:"list",items:["**429** — rate limit exceeded.","**`Retry-After`** — wait the specified time before retrying.","**Throttle proactively** — debounce, batch, cache to stay under limits.","**No tight retry loop** — worsens the situation."]},{t:"note",text:"429 Too Many Requests = rate limited. Honor the Retry-After header (wait before retrying), and proactively throttle (debounce/batch/cache) to avoid hitting limits. Never retry immediately in a loop — that worsens it. Some APIs also send X-RateLimit-Remaining/Reset headers to pace requests."}]},{level:"junior",q:"What is the difference between authentication and authorization?",a:[{t:"p",text:"*Authentication* verifies *who you are* (login — proving identity with credentials/tokens). *Authorization* determines *what you're allowed to do* (permissions/roles). In HTTP, a `401 Unauthorized` means *not authenticated* (log in / refresh token), while `403 Forbidden` means *authenticated but not authorized* (you don't have permission)."},{t:"list",items:["**Authentication** — who you are (login/token); `401` if missing/invalid.","**Authorization** — what you can do (roles/permissions); `403` if disallowed.","**Order** — authenticate first, then authorize.","**Handling** — 401 → re-auth/refresh; 403 → show 'no permission'."]},{t:"note",text:"Authentication = who you are (login/token) → 401 if missing/invalid. Authorization = what you're allowed to do (roles/permissions) → 403 if authenticated but not permitted. Handle 401 by re-authenticating/refreshing, 403 by showing 'no permission'."}]},{level:"senior",q:"How does OAuth2 / bearer token authentication work at a high level?",a:[{t:"p",text:"The client obtains an *access token* (short-lived) — often via an OAuth2 flow (e.g. authorization code, or exchanging credentials) — and sends it in the `Authorization: Bearer <token>` header on each request. When the access token expires, a longer-lived *refresh token* is exchanged for a new access token (without re-login). The server validates the token per request."},{t:"list",items:["**Access token** — short-lived; sent as `Authorization: Bearer`.","**Refresh token** — longer-lived; exchanged for new access tokens.","**OAuth2 flows** — authorization code (with PKCE for mobile), etc.","**Storage** — keep tokens securely (Keystore/encrypted), not plaintext."]},{t:"note",text:"OAuth2/bearer: obtain a short-lived access token (via an OAuth2 flow, e.g. auth code + PKCE on mobile), send it as Authorization: Bearer on each request; when it expires, exchange the longer-lived refresh token for a new one (no re-login). Store tokens securely (Keystore/encrypted). Server validates per request."}]},{level:"senior",q:"What do Cache-Control headers do?",a:[{t:"p",text:"`Cache-Control` directives tell caches (including OkHttp's) how to handle a response: `max-age=N` (fresh for N seconds), `no-cache` (must revalidate before using), `no-store` (don't cache at all), `private` (only client caches, not shared), `public`. Combined with `ETag`/`Last-Modified`, they control HTTP-level caching and revalidation transparently."},{t:"list",items:["**`max-age=N`** — cache is fresh for N seconds.","**`no-cache`** — cache but revalidate before use.","**`no-store`** — never cache (sensitive data).","**`private`/`public`** — client-only vs shared caches."]},{t:"note",text:"Cache-Control directs caches: max-age=N (fresh for N sec), no-cache (revalidate before use), no-store (never cache — sensitive), private (client-only), public. With ETag/Last-Modified it controls HTTP-level caching/revalidation. OkHttp honors these automatically when a Cache is installed."}]},{level:"senior",q:"What is the difference between REST and GraphQL?",a:[{t:"p",text:"*REST* exposes multiple endpoints returning fixed resource shapes — you may over-fetch (get fields you don't need) or under-fetch (need several calls). *GraphQL* has a single endpoint where the *client specifies exactly which fields* it wants in one query, avoiding over/under-fetching and reducing round trips. GraphQL is more flexible but adds server/schema complexity and complicates HTTP caching."},{t:"table",headers:["","REST","GraphQL"],rows:[["Endpoints","many (per resource)","one"],["Fetching","fixed shapes (over/under-fetch)","client picks fields"],["Round trips","possibly several","often one"],["Caching","HTTP caching easy","harder (POST queries)"]]},{t:"list",items:["**REST** — multiple endpoints, fixed shapes; simple, HTTP-cacheable.","**GraphQL** — one endpoint, client-selected fields; avoids over/under-fetch.","**Trade-off** — GraphQL flexibility vs server complexity/caching difficulty.","**Android** — Apollo is the common GraphQL client."]},{t:"note",text:"REST: many endpoints, fixed resource shapes (over/under-fetch, HTTP-cacheable, simple). GraphQL: one endpoint, client picks exactly the fields it needs in one query (avoids over/under-fetch, fewer round trips) but adds server/schema complexity and complicates caching. Apollo is the common Android GraphQL client."}]},{level:"senior",q:"What is the difference between HTTP/1.1, HTTP/2, and HTTP/3?",a:[{t:"p",text:"*HTTP/1.1* uses one request per connection at a time (head-of-line blocking; browsers open multiple connections). *HTTP/2* multiplexes many concurrent requests over one connection with header compression — faster, fewer connections. *HTTP/3* runs over QUIC (UDP-based), eliminating TCP head-of-line blocking and connecting faster (better on flaky mobile networks). OkHttp supports HTTP/2 (and HTTP/3 is emerging)."},{t:"list",items:["**HTTP/1.1** — one request at a time per connection; head-of-line blocking.","**HTTP/2** — multiplexed streams, header compression; one connection.","**HTTP/3 (QUIC/UDP)** — no TCP head-of-line blocking; faster on flaky networks.","**OkHttp** — HTTP/2 supported; HTTP/3 emerging."]},{t:"note",text:"HTTP/1.1: one request at a time per connection (head-of-line blocking). HTTP/2: multiplexed concurrent streams + header compression over one connection. HTTP/3: over QUIC (UDP) — no TCP head-of-line blocking, faster on flaky mobile networks. OkHttp supports HTTP/2; HTTP/3 is emerging."}]},{level:"junior",q:"What do the redirect status codes (301, 302, 307, 308) mean?",a:[{t:"p",text:"3xx redirects tell the client the resource is elsewhere. `301 Moved Permanently` and `308 Permanent Redirect` mean update your URL permanently; `302 Found` and `307 Temporary Redirect` are temporary. The difference between 301/302 and 307/308 is method preservation: 307/308 *preserve* the original method/body (a POST stays a POST), while 301/302 historically may change POST to GET. OkHttp follows redirects automatically."},{t:"list",items:["**301/308** — permanent (update the URL); 308 preserves method.","**302/307** — temporary; 307 preserves method/body.","**Method preservation** — 307/308 keep POST as POST.","**OkHttp** — follows redirects automatically (configurable)."]},{t:"note",text:"3xx redirects: 301/308 permanent (update the URL), 302/307 temporary. 307/308 preserve the original method/body (POST stays POST); 301/302 historically may change POST→GET. OkHttp follows redirects automatically (followRedirects/followSslRedirects configurable)."}]},{level:"junior",q:"Why is HTTPS/TLS essential, and how does it work at a high level?",a:[{t:"p",text:"HTTPS wraps HTTP in *TLS*, which *encrypts* the traffic (so it can't be read on the wire), *authenticates* the server (via its certificate signed by a trusted CA), and ensures *integrity* (tamper detection). Without it, credentials and data are exposed to anyone on the network. Modern Android blocks *cleartext* (HTTP) traffic by default; you must use HTTPS (or explicitly allow cleartext, which you shouldn't for real APIs)."},{t:"list",items:["**Encryption** — traffic unreadable on the wire.","**Authentication** — the server's certificate proves identity (CA-signed).","**Integrity** — tampering is detected.","**Cleartext blocked** — modern Android forbids HTTP by default; use HTTPS."]},{t:"note",text:"HTTPS = HTTP over TLS: encrypts traffic (unreadable on the wire), authenticates the server (CA-signed certificate), and ensures integrity (tamper detection). Without it, data/credentials are exposed. Modern Android blocks cleartext HTTP by default — use HTTPS (Network Security Config controls exceptions)."}]},{level:"junior",q:"What are common HTTP request and response headers?",a:[{t:"p",text:"*Request* headers include `Authorization` (credentials), `Content-Type` (body format), `Accept` (desired response format), `User-Agent`, `Accept-Encoding` (gzip). *Response* headers include `Content-Type`, `Content-Length`, `Cache-Control`/`ETag` (caching), `Set-Cookie`, `Location` (redirects/created resources), and `Retry-After` (rate limits). Headers carry metadata about the request/response."},{t:"list",items:["**Request** — `Authorization`, `Content-Type`, `Accept`, `Accept-Encoding`, `User-Agent`.","**Response** — `Content-Type`, `Cache-Control`/`ETag`, `Location`, `Retry-After`.","**Metadata** — headers describe the body and control behavior.","**Custom** — `X-` headers for API-specific data (rate limits, request ids)."]},{t:"note",text:"Request headers: Authorization, Content-Type, Accept, Accept-Encoding (gzip), User-Agent. Response headers: Content-Type, Cache-Control/ETag, Location, Retry-After, Set-Cookie. Plus custom X- headers (rate limits, request ids). Headers carry metadata and control caching/auth/negotiation."}]},{level:"junior",q:"When do you use path parameters, query parameters, and the request body?",a:[{t:"p",text:"*Path* parameters identify a specific resource (`/users/42` — the id is part of the resource's address). *Query* parameters filter/sort/paginate a collection (`/users?role=admin&sort=name`). The *body* carries the data payload for create/update (`POST`/`PUT`/`PATCH`). Use path for identity, query for options, body for content."},{t:"list",items:["**Path** — resource identity (`/users/42`).","**Query** — filtering/sorting/pagination (`?role=&sort=&page=`).","**Body** — the payload for create/update.","**Don't put secrets in the URL** — path/query are logged/cached."]},{t:"note",text:"Path params identify a resource (/users/42); query params filter/sort/paginate a collection (?role=&sort=&page=); the body carries create/update payloads (POST/PUT/PATCH). Path = identity, query = options, body = content. Never put secrets in path/query (they're logged/cached)."}]},{level:"junior",q:"What are the HEAD and OPTIONS methods for?",a:[{t:"p",text:"`HEAD` is like `GET` but returns *only headers, no body* — useful to check if a resource exists, get its size/`ETag`, or test freshness without downloading it. `OPTIONS` asks what methods/capabilities a resource supports (used in CORS preflight on the web). Both are safe and idempotent."},{t:"list",items:["**`HEAD`** — headers only (existence, size, `ETag`) without the body.","**`OPTIONS`** — supported methods/capabilities (CORS preflight on web).","**Safe + idempotent** — no side effects.","**Mobile** — HEAD occasionally for cheap existence/freshness checks."]},{t:"note",text:"HEAD = GET without the body (headers only — check existence, size, ETag, freshness cheaply). OPTIONS = query supported methods/capabilities (CORS preflight on web). Both safe and idempotent. On mobile, HEAD is occasionally used for cheap existence/freshness checks."}]},{level:"senior",q:"What is a 202 Accepted response, and how do you handle async processing?",a:[{t:"p",text:"`202 Accepted` means the server *accepted the request for processing* but hasn't completed it — common for long-running operations (video processing, report generation). The response often includes a status URL or job id; the client then *polls* that endpoint (or receives a *webhook*/push) until the operation completes. Design the client to handle the pending state, not assume immediate completion."},{t:"list",items:["**202 Accepted** — request accepted, processing not done.","**Status URL/job id** — poll for completion.","**Poll or push** — polling with backoff, or a webhook/FCM notification.","**Client UX** — show a pending/processing state."]},{t:"note",text:"202 Accepted = the server accepted the request but processing isn't complete (long-running ops). It usually returns a status URL/job id; the client polls it (with backoff) or receives a webhook/push until done. Design the client for the pending state, not immediate completion."}]},{level:"senior",q:"How do you safely retry a POST with an idempotency key?",a:[{t:"p",text:"Since `POST` isn't idempotent, retrying it (after a timeout where you're unsure it succeeded) risks creating duplicates. Send a client-generated *idempotency key* (a unique id per logical operation) in a header; the server records it and, on a retry with the same key, returns the *original result* instead of creating a duplicate. This makes POST safely retryable — essential for payments/orders."},{t:"code",title:"Idempotency key",code:`@POST("orders")
suspend fun createOrder(
    @Header("Idempotency-Key") key: String,   // client-generated UUID per order
    @Body order: OrderRequest,
): Order`},{t:"list",items:["**Client-generated key** — unique per logical operation (UUID).","**Server dedupes** — same key returns the original result.","**Safe retry** — no duplicate orders/payments.","**Requires server support** — the API must honor the key."]},{t:"note",text:"POST isn't idempotent, so retrying risks duplicates. Send a client-generated idempotency key (UUID per operation) in a header; the server records it and returns the original result on retries with the same key — making POST safely retryable. Essential for payments/orders; requires server support."}]},{level:"senior",q:"What is a webhook, and how does it differ from polling?",a:[{t:"p",text:"A *webhook* is a server-to-server push: when an event happens, the server sends an HTTP request to a *callback URL* you registered — so you're notified immediately without asking. *Polling* is the client repeatedly asking 'anything new?' — simpler but wasteful and delayed. On mobile, the equivalent of a webhook is *FCM push* (the server notifies the app via Firebase), avoiding constant polling."},{t:"list",items:["**Webhook** — server pushes to a callback URL on an event (immediate).","**Polling** — client repeatedly asks; wasteful, delayed.","**Mobile equivalent** — FCM push notifies the app (no constant polling).","**Trade-off** — push (efficient, needs infra) vs polling (simple, laggy)."]},{t:"note",text:"A webhook is a server-to-server push to a registered callback URL on an event (immediate, efficient) vs polling (client repeatedly asks — wasteful/delayed). On mobile, FCM push is the webhook equivalent: the server notifies the app via Firebase, avoiding constant polling."}]},{level:"senior",q:"How should you handle 4xx versus 5xx errors differently?",a:[{t:"p",text:"*4xx* means the *client* did something wrong — retrying the same request won't help; you should fix the request (400), re-authenticate (401), show 'forbidden' (403), 'not found' (404), or handle a conflict (409). *5xx* means the *server* failed — the same request might succeed later, so *retry with exponential backoff*. Treating them the same (e.g. retrying a 400 forever) wastes resources and hides bugs."},{t:"list",items:["**4xx** — client error; don't blindly retry; fix/surface (401→refresh, 429→back off).","**5xx** — server error; retry with exponential backoff.","**Distinguish** — check the status code before retrying.","**Don't retry 400/404** — the same request will fail again."]},{t:"note",text:"4xx = client error — don't blindly retry; fix/surface (400 fix request, 401 refresh, 403 forbidden, 404 not found, 429 back off). 5xx = server error — retry with exponential backoff (may succeed later). Check the status before retrying; retrying a 400/404 wastes resources and hides bugs."}]},{level:"senior",q:"What does a well-designed API error response look like?",a:[{t:"p",text:"A good error response uses the *right status code* and a *structured JSON body* with a machine-readable error `code`, a human-readable `message`, and optionally `details`/`field` errors for validation. Consistency lets the client map errors to typed domain errors and show appropriate UI. Avoid returning `200` with an error in the body (breaks status-based handling)."},{t:"code",title:"Structured error",code:`// 422 Unprocessable Entity
{ "code": "VALIDATION_ERROR", "message": "Invalid input",
  "errors": [{ "field": "email", "message": "must be a valid email" }] }`},{t:"list",items:["**Correct status code** — not `200` for errors.","**Machine-readable `code`** — map to typed errors.","**Human-readable `message`** — for display/logging.","**Field/validation details** — for form errors."]},{t:"note",text:"A good error response: correct status code + structured JSON (machine-readable code, human-readable message, optional field/validation details). Consistency lets the client map to typed domain errors and show the right UI. Never return 200 with an error body — it breaks status-based handling."}]},{level:"senior",q:"What is the difference between REST and gRPC?",a:[{t:"p",text:"*REST* uses HTTP/JSON — human-readable, ubiquitous, easy to debug. *gRPC* uses HTTP/2 + Protocol Buffers (binary) with a defined service schema — much smaller/faster payloads, strongly-typed generated clients, and support for streaming (bidirectional). gRPC suits high-performance internal/microservice communication; REST is simpler and more universal for public/mobile APIs (though gRPC is used on mobile too)."},{t:"table",headers:["","REST","gRPC"],rows:[["Format","JSON (text)","Protobuf (binary)"],["Transport","HTTP/1.1 or 2","HTTP/2"],["Schema","optional (OpenAPI)","required (.proto)"],["Streaming","limited","first-class (bidi)"]]},{t:"list",items:["**REST/JSON** — human-readable, universal, easy to debug.","**gRPC/protobuf** — binary, fast, typed clients, streaming.","**gRPC** — performance/microservices; needs a `.proto` schema.","**Choose** — REST for simplicity/reach; gRPC for performance/streaming."]},{t:"note",text:"REST = HTTP/JSON (readable, universal, easy to debug). gRPC = HTTP/2 + Protobuf (binary — smaller/faster, strongly-typed generated clients, first-class streaming, needs a .proto schema). gRPC for high-performance/streaming/microservices; REST for simplicity and reach."}]},{level:"junior",q:"What is the difference between PUT and POST for creating resources?",a:[{t:"p",text:"`POST /users` creates a new resource where the *server* assigns the id (not idempotent — repeating creates multiple). `PUT /users/42` creates-or-replaces the resource *at a client-specified id* (idempotent — repeating yields the same resource). Use `POST` when the server generates the id (the common case); `PUT` when the client controls the identity."},{t:"list",items:["**`POST`** — server assigns id; not idempotent (repeats create duplicates).","**`PUT`** — client-specified id; idempotent (repeats replace).","**Common** — `POST` for creation (server ids).","**`PUT` for creation** — when the client controls the id."]},{t:"note",text:"POST /users creates a resource with a server-assigned id (not idempotent — repeats duplicate). PUT /users/42 creates-or-replaces at a client-specified id (idempotent — repeats yield the same resource). Use POST when the server generates ids (common); PUT when the client controls identity."}]},{level:"senior",q:"How do you version a REST API, and why does it matter for mobile?",a:[{t:"p",text:"APIs are versioned so changes don't break existing clients — critical for mobile because *old app versions stay installed* for a long time. Common approaches: *URI versioning* (`/v1/users`), *header versioning* (`Accept: application/vnd.api.v2+json`), or *query param*. The client sends its expected version; the server maintains backward compatibility (additive changes) and deprecates old versions gradually. Never break a shipped app's API contract abruptly."},{t:"list",items:["**URI versioning** — `/v1/`, `/v2/` (most common, explicit).","**Header versioning** — via `Accept` media type.","**Mobile constraint** — old app versions linger; can't force-update everyone.","**Backward compatibility** — additive changes; deprecate gradually."]},{t:"note",text:"Version APIs (URI /v1/, Accept-header media type, or query param) so changes don't break existing clients — critical on mobile where old app versions stay installed for months/years. Keep backward compatibility (additive changes), deprecate old versions gradually, and never abruptly break a shipped app's contract."}]}],VA={id:"http-rest",title:"HTTP & REST Fundamentals",tagline:"Request/response anatomy, HTTP methods & idempotency, status code categories, REST design, auth, and caching headers (ETag/304).",tags:["HTTP","REST","Status Codes","Idempotency","ETag"],content:LA,qa:jA},NA=[{heading:"The two-layer stack: Retrofit on top of OkHttp",blocks:[{t:"p",text:"Android's standard networking stack has two layers. **OkHttp** is the *HTTP client* — it does the actual network work: opening connections, sending requests, receiving responses, connection pooling, caching, retries, timeouts. **Retrofit** is a *type-safe wrapper on top of OkHttp* — you define your API as a Kotlin interface with annotations, and Retrofit generates the code that turns method calls into HTTP requests (using OkHttp underneath) and parses responses into your objects. You describe *what* the API looks like; Retrofit handles *how* to call it."},{t:"list",items:["**OkHttp** = the engine (raw HTTP: connections, interceptors, caching, pooling). You can use it directly, but it's low-level (manual request building, response parsing).","**Retrofit** = the ergonomic layer (declare an interface, get type-safe methods, automatic JSON parsing). It uses OkHttp as its HTTP client.","Together they're the de-facto standard for REST networking on Android. (Ktor is the KMP alternative — covered separately.)"]}]},{heading:"Defining an API with Retrofit",blocks:[{t:"code",title:"A Retrofit API interface",code:`interface UserApi {
    @GET("users")
    suspend fun getUsers(@Query("page") page: Int): List<UserDto>

    @GET("users/{id}")
    suspend fun getUser(@Path("id") id: String): UserDto

    @POST("users")
    suspend fun createUser(@Body user: CreateUserRequest): UserDto

    @PUT("users/{id}")
    suspend fun updateUser(@Path("id") id: String, @Body user: UserDto): UserDto

    @DELETE("users/{id}")
    suspend fun deleteUser(@Path("id") id: String)

    @GET("search")
    suspend fun search(@Query("q") query: String, @Header("X-Trace") trace: String): SearchDto
}`},{t:"list",items:["**HTTP method annotations**: `@GET`, `@POST`, `@PUT`, `@DELETE`, `@PATCH` — the path (relative to the base URL) goes in the annotation.","**`@Path`** — substitute a value into the URL path (`users/{id}`). **`@Query`** — add a query parameter (`?page=2`). **`@Body`** — send an object as the request body (serialized to JSON). **`@Header`/`@Headers`** — add headers. **`@Field`/`@FormUrlEncoded`** — form submissions.","**`suspend` functions** — Retrofit natively supports coroutines: a `suspend` API function is *main-safe* (Retrofit runs the network call off the main thread) and returns the parsed result directly. This is the modern way (older Retrofit used `Call<T>` with callbacks, or RxJava).","**Return types**: `suspend fun ...(): T` returns the parsed body directly (throwing on error); `suspend fun ...(): Response<T>` gives you the full response (status code, headers) for manual handling."]}]},{heading:"Building Retrofit and the converter",blocks:[{t:"code",title:"Wiring it up",code:`val okHttpClient = OkHttpClient.Builder()
    .connectTimeout(15, TimeUnit.SECONDS)
    .readTimeout(15, TimeUnit.SECONDS)
    .addInterceptor(loggingInterceptor)      // interceptors here
    .build()

val retrofit = Retrofit.Builder()
    .baseUrl("https://api.example.com/")     // must end with /
    .client(okHttpClient)
    .addConverterFactory(                      // how to parse bodies
        Json.asConverterFactory("application/json".toMediaType())  // kotlinx.serialization
    )
    .build()

val userApi: UserApi = retrofit.create(UserApi::class.java)  // Retrofit generates the impl`},{t:"list",items:["**`baseUrl`** — the API root (endpoint paths are relative to it). **`client`** — the configured OkHttpClient. **A converter factory** — tells Retrofit how to serialize request bodies and parse responses (kotlinx.serialization, Moshi, or Gson).","**`retrofit.create(...)`** generates the interface implementation via a dynamic proxy — you call methods, Retrofit builds and executes the OkHttp request and parses the response.","**Provide these as singletons via DI** (Hilt) — building Retrofit/OkHttp is expensive, and OkHttp's connection pool and cache should be shared across the app. Never create a new client per request."]}]},{heading:"OkHttp interceptors — the powerful middleware",blocks:[{t:"p",text:"**Interceptors** are OkHttp's middleware — they sit in the request/response pipeline and can observe, modify, or short-circuit requests and responses. Every request passes through the chain of interceptors on the way out, and every response on the way back. This is where you add cross-cutting networking concerns: auth, logging, headers, retries."},{t:"code",title:"An auth interceptor adds the token to every request",code:`class AuthInterceptor(private val tokenProvider: TokenProvider) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response {
        val request = chain.request().newBuilder()
            .addHeader("Authorization", "Bearer \${tokenProvider.token()}")
            .build()
        return chain.proceed(request)          // continue the chain
    }
}`},{t:"list",items:["**Common uses**: adding auth tokens/headers to every request (so you don't repeat it per call), logging (`HttpLoggingInterceptor`), adding analytics/trace headers, custom retry logic, response caching tweaks, and mocking responses in tests.","**Two types**: *application interceptors* (added with `addInterceptor` — called once per request, see the request as your app made it) and *network interceptors* (`addNetworkInterceptor` — closer to the wire, see redirects/retries, can observe the actual network request). Application interceptors are the common choice.","**`HttpLoggingInterceptor`** — logs requests/responses for debugging (set level to BODY in debug, NONE in release — never log bodies with sensitive data in production).","**Order matters**: interceptors run in the order added; the auth interceptor should run before logging so the logged request includes the header (or after, if you want to hide it)."]}]},{heading:"Timeouts, connection pooling, and caching",blocks:[{t:"list",items:["**Timeouts** — configure `connectTimeout` (time to establish a connection), `readTimeout` (time waiting for data), `writeTimeout` (time to send the body). Sensible defaults (10-30s) prevent requests from hanging forever on a bad network. Without timeouts, a stalled request blocks resources indefinitely.","**Connection pooling** — OkHttp *reuses* TCP connections across requests (keep-alive) instead of opening a new one each time. This is a major performance win (connection setup, especially TLS handshake, is expensive) and is why you share one OkHttpClient — each client has its own pool.","**HTTP caching** — configure a `Cache` on the client, and OkHttp automatically caches responses per HTTP headers (`Cache-Control`, ETag), serving cached responses and doing conditional requests (304). Complementary to app-level DB caching.","**HTTP/2 support** — OkHttp supports HTTP/2 (multiplexing multiple requests over one connection), automatically negotiated — more efficient than HTTP/1.1's one-request-per-connection.","**Retries** — OkHttp automatically retries some connection failures; for application-level retry (on specific status codes with backoff), you add a custom interceptor or handle it in the repository."]},{t:"note",text:"Retrofit + OkHttp essentials: OkHttp = the HTTP engine (connections, pooling, caching, interceptors, timeouts); Retrofit = type-safe interface layer on top (annotations → requests, auto JSON parsing, suspend support = main-safe). Define APIs as annotated interfaces; build Retrofit with a baseUrl, the OkHttpClient, and a converter factory — as DI singletons. Interceptors are middleware for auth/logging/headers/retry. Configure timeouts; rely on connection pooling and HTTP caching. Share one client for pooling."}]}],WA=[{level:"junior",q:"What is the relationship between Retrofit and OkHttp?",a:[{t:"p",text:"**They're two layers of the same networking stack. OkHttp is the actual HTTP client — the engine that does the network work; Retrofit is a type-safe wrapper on top of OkHttp that makes calling REST APIs ergonomic.**"},{t:"list",items:["**OkHttp** handles the low-level HTTP: opening and pooling connections, sending requests, receiving responses, TLS, caching, timeouts, retries, and interceptors. You *can* use it directly, but it's manual — you build requests and parse responses yourself.",'**Retrofit** sits on top: you declare your API as a Kotlin interface with annotations (`@GET("users/{id}")`), and Retrofit generates the code that turns a method call into an OkHttp request and parses the JSON response into your objects. It delegates the actual HTTP work to OkHttp.']},{t:"p",text:"So the division is: Retrofit gives you the *type-safe, declarative interface* (describe the API, get parsed results), and OkHttp is the *underlying HTTP machinery* it runs on. When you configure things like timeouts, interceptors (for auth/logging), and caching, you do that on the OkHttpClient and hand it to Retrofit. Together they're the standard Android REST networking stack."}]},{level:"junior",q:"How do you define an API endpoint in Retrofit?",a:[{t:"p",text:"**You declare a Kotlin interface where each method represents an endpoint, annotated with the HTTP method and path, and Retrofit generates the implementation.** The method's parameters are annotated to describe how they map into the request."},{t:"code",title:"An interface with common annotations",code:`interface UserApi {
    @GET("users/{id}")
    suspend fun getUser(@Path("id") id: String): UserDto      // path param

    @GET("users")
    suspend fun getUsers(@Query("page") page: Int): List<UserDto>  // query param

    @POST("users")
    suspend fun create(@Body body: CreateUserRequest): UserDto     // JSON body
}`},{t:"list",items:["**Method annotations** — `@GET`, `@POST`, `@PUT`, `@DELETE`, `@PATCH`, with the relative path in the annotation.","**Parameter annotations** — `@Path` (substitute into the URL), `@Query` (add a `?key=value`), `@Body` (serialize an object as the JSON request body), `@Header` (add a header).","**`suspend`** — make the function `suspend` and it's automatically main-safe (Retrofit runs the network off the main thread) and returns the parsed result directly."]},{t:"p",text:"You then get the working implementation with `retrofit.create(UserApi::class.java)` — Retrofit builds it via a dynamic proxy. Returning `T` directly gives you the parsed body (throwing on HTTP errors), or you can return `Response<T>` to access the status code and headers for manual handling. This declarative approach means you describe *what* the API looks like and Retrofit handles all the request-building and response-parsing boilerplate."}]},{level:"junior",q:"What does making a Retrofit function 'suspend' do?",a:[{t:"p",text:"**Making a Retrofit API function `suspend` gives you native coroutine support: the function is main-safe (Retrofit automatically runs the network call off the main thread) and returns the parsed result directly, so you can `await` it in sequential-looking code.** You call it from a coroutine and it suspends while the network request happens, then resumes with the result."},{t:"code",title:"Calling a suspend Retrofit function",code:`// In a repository (main-safe — no withContext needed)
suspend fun loadUser(id: String): User = api.getUser(id).toDomain()

// In a ViewModel
viewModelScope.launch {
    val user = repository.loadUser("42")   // suspends during network, resumes with result
    _state.value = UiState.Content(user)
}`},{t:"p",text:"The key points: it's **main-safe**, so you can call it from a main-thread coroutine (`viewModelScope`) without wrapping it in `withContext(Dispatchers.IO)` — Retrofit handles the threading internally (wrapping it is redundant). It returns the parsed body directly (throwing `HttpException` on non-2xx or `IOException` on network failure), which you handle with try/catch or by returning `Response<T>`. And it integrates with structured concurrency — if the calling coroutine is cancelled (the user leaves the screen), Retrofit cancels the underlying HTTP call. This is the modern approach; older Retrofit used `Call<T>` with `enqueue` callbacks (verbose, leak-prone) or RxJava. Suspend functions make networking read like simple sequential code."}]},{level:"junior",q:"What is an OkHttp interceptor and what would you use one for?",a:[{t:"p",text:"**An interceptor is OkHttp's middleware — it sits in the request/response pipeline and can observe, modify, or short-circuit requests and responses.** Every outgoing request passes through the chain of interceptors, and every incoming response comes back through them, so it's the place to add *cross-cutting* networking concerns that should apply to many or all requests."},{t:"code",title:"Adding an auth token to every request",code:`class AuthInterceptor(private val token: () -> String) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response {
        val request = chain.request().newBuilder()
            .addHeader("Authorization", "Bearer \${token()}")
            .build()
        return chain.proceed(request)
    }
}`},{t:"list",items:["**Adding auth headers** — attach the `Authorization` token to every request in one place, instead of repeating it on each API method.","**Logging** — `HttpLoggingInterceptor` logs requests/responses for debugging (BODY level in debug, NONE in release).","**Adding common headers** — API keys, trace/analytics headers, `Accept-Language`, app version.","**Retry / caching logic** — custom retry on specific failures, or tweaking cache behavior."]},{t:"p",text:"The value is *centralization* — a concern like auth applies to every request, and an interceptor lets you implement it once rather than in every call. You add them with `addInterceptor` on the OkHttpClient builder. They run in the order added, and calling `chain.proceed(request)` continues the chain (you can also short-circuit by returning a response without proceeding, which is how response mocking in tests works)."}]},{level:"senior",q:"Why should you share a single OkHttpClient/Retrofit instance across the app?",a:[{t:"p",text:"**Because OkHttp maintains shared, expensive resources — a connection pool, a thread pool, and optionally a response cache — that are meant to be reused across all requests. Creating a new client per request (or per screen) discards those benefits and wastes resources.**"},{t:"list",items:["**Connection pooling** — the big one. OkHttp *reuses* TCP/TLS connections across requests (keep-alive) instead of opening a new one each time. Establishing a connection, especially the TLS handshake, is *expensive* (multiple round trips). A shared client reuses warm connections, dramatically reducing latency for subsequent requests to the same host. A new client per request means a cold connection (full handshake) every time — much slower and more battery/data.","**Thread pool** — OkHttp has a dispatcher with a thread pool for async requests; each client creates its own. Many clients = many thread pools = wasted memory and threads.","**Response cache** — if you configure an HTTP cache, it's tied to the client; multiple clients would have separate (or conflicting) caches, defeating caching.","**Consistent configuration** — interceptors (auth, logging), timeouts, and TLS settings live on the client. One shared, correctly-configured client ensures every request behaves consistently; ad-hoc clients drift."]},{t:"list",items:["**How to share it**: provide the OkHttpClient and Retrofit as *singletons* via dependency injection (Hilt `@Singleton`). All API interfaces are created from the one Retrofit instance, which uses the one OkHttpClient.","**When you DO create variants**: sometimes you need a *specialized* client (different timeouts for a long-polling endpoint, a different interceptor set for an unauthenticated auth endpoint) — but you create it by *sharing the base client's pool* (`baseClient.newBuilder()...build()`), which reuses the underlying connection pool and thread pool while overriding specific settings. You never build a fully independent client just to change one setting."]},{t:"p",text:"**The senior framing**: OkHttp is *designed* around a shared client — the connection pool is the performance heart of the library, and it only helps if reused. Creating clients per request is a classic performance mistake that shows up as slow requests (repeated TLS handshakes), excess memory (duplicate thread pools), and broken caching. The correct pattern — one DI-provided singleton, with `newBuilder()` for the rare specialized variant that still shares the pool — is both the performance-correct and the maintainability-correct choice."}]},{level:"senior",q:"How do timeouts work in OkHttp, and why are they important?",a:[{t:"p",text:"**Timeouts bound how long different phases of a request can take before OkHttp gives up and fails the request. Without them, a request can hang indefinitely on a bad network, holding resources and leaving the user staring at a spinner forever.** OkHttp has several distinct timeouts for different phases:"},{t:"list",items:["**`connectTimeout`** — the maximum time to *establish* the connection to the server (TCP + TLS handshake). If the server is unreachable or very slow to connect, this fires.","**`readTimeout`** — the maximum time to wait for *data* once connected — specifically, the max gap between bytes arriving. If the server accepts the connection but then stalls (sends nothing), this fires. This is the most commonly hit one on flaky networks.","**`writeTimeout`** — the maximum time to *send* the request body to the server (relevant for large uploads).","**`callTimeout`** — an overall cap on the *entire* call (connect + write + read + redirects). A safety net that bounds total time regardless of the per-phase timeouts."]},{t:"list",items:["**Why they're important**: mobile networks fail in ways that *hang* rather than cleanly error — a connection that never completes, a server that stops responding mid-response. Without timeouts, the coroutine/thread waits forever, the UI shows an eternal spinner, resources (connections, threads) are tied up, and the user can't tell if it's working. Timeouts convert 'hung forever' into a *prompt, handleable failure* so you can show an error and offer retry.","**Choosing values**: too short and you fail requests that would have succeeded on a slow-but-working network (frustrating on poor connections); too long and the user waits ages before seeing an error. Typical values are 10-30 seconds, tuned per endpoint — a quick API call might use 15s, while a large file upload needs a longer write/call timeout. You can override per-request with `newBuilder()` for endpoints with different needs.","**Timeouts + retries together**: a timeout produces an `IOException`/`SocketTimeoutException`, which your retry logic can catch and retry (with backoff) — since a timeout is often transient. So timeouts and retry policy work together to make networking resilient."]},{t:"p",text:"**The senior framing**: timeouts are a *reliability* requirement, not an optional tuning knob — on unreliable mobile networks, requests hang, and unbounded waits are a real UX and resource-leak problem. The discipline is: always set sensible timeouts (never rely on 'no timeout'), pick per-phase values matched to the endpoint (short for quick APIs, longer for uploads), use `callTimeout` as an overall safety net, and pair timeouts with retry-on-transient-failure so a timed-out request gets a bounded retry rather than either hanging or failing permanently."}]},{level:"junior",q:"What are the Retrofit HTTP method annotations?",a:[{t:"p",text:"Retrofit maps interface methods to HTTP requests via annotations: `@GET`, `@POST`, `@PUT`, `@PATCH`, `@DELETE`, `@HEAD` — each takes the relative URL path. `@GET` fetches, `@POST` creates, `@PUT` replaces, `@PATCH` partially updates, `@DELETE` removes. You add a request body with `@Body` for the ones that carry data."},{t:"code",title:"Method annotations",code:`interface Api {
    @GET("users/{id}") suspend fun getUser(@Path("id") id: String): User
    @POST("users") suspend fun create(@Body user: User): User
    @PUT("users/{id}") suspend fun replace(@Path("id") id: String, @Body u: User): User
    @PATCH("users/{id}") suspend fun update(@Path("id") id: String, @Body u: PatchDto): User
    @DELETE("users/{id}") suspend fun delete(@Path("id") id: String)
}`},{t:"list",items:["**`@GET`/`@DELETE`** — fetch/remove (path + query params).","**`@POST`/`@PUT`/`@PATCH`** — create/replace/update (with `@Body`).","**Relative path** — resolved against the base URL.","**`@HEAD`** — headers only."]},{t:"note",text:"Retrofit method annotations map interface functions to HTTP: @GET/@POST/@PUT/@PATCH/@DELETE/@HEAD with a relative path. @GET fetches, @POST creates, @PUT replaces, @PATCH partially updates, @DELETE removes; add @Body for data-carrying requests. Paths resolve against the base URL."}]},{level:"junior",q:"What are the Retrofit parameter annotations (@Path, @Query, @Body, @Header)?",a:[{t:"p",text:"Parameter annotations bind function arguments to parts of the request: `@Path` fills a URL path placeholder, `@Query` adds a query parameter, `@QueryMap` a map of them, `@Body` sets the request body (serialized), `@Header`/`@HeaderMap` add headers, and `@Field`/`@Part` for form/multipart bodies."},{t:"code",title:"Parameter binding",code:`@GET("search")
suspend fun search(
    @Query("q") query: String,
    @Query("page") page: Int = 1,
    @Header("Authorization") token: String,
): SearchResult

@GET("repos/{owner}/{repo}")
suspend fun repo(@Path("owner") owner: String, @Path("repo") repo: String): Repo`},{t:"list",items:["**`@Path`** — fill a `{placeholder}` in the URL.","**`@Query`/`@QueryMap`** — query string parameters.","**`@Body`** — the serialized request body.","**`@Header`/`@HeaderMap`** — request headers."]},{t:"note",text:"Parameter annotations bind args to the request: @Path (URL placeholder), @Query/@QueryMap (query params), @Body (serialized body), @Header/@HeaderMap (headers), @Field/@Part (form/multipart). Nullable @Query params are omitted when null — handy for optional filters."}]},{level:"junior",q:"How do you upload files or send form data with Retrofit?",a:[{t:"p",text:"For URL-encoded forms, use `@FormUrlEncoded` + `@Field`. For file uploads (and mixed data), use `@Multipart` + `@Part` with `MultipartBody.Part` (wrapping a `RequestBody` from the file). This sends `multipart/form-data`, the standard for uploads."},{t:"code",title:"Form and multipart",code:`@FormUrlEncoded @POST("login")
suspend fun login(@Field("user") u: String, @Field("pass") p: String): Token

@Multipart @POST("upload")
suspend fun upload(
    @Part file: MultipartBody.Part,          // the file
    @Part("desc") desc: RequestBody,         // a field
): UploadResult
// file = MultipartBody.Part.createFormData("file", name, requestBody)`},{t:"list",items:["**`@FormUrlEncoded` + `@Field`** — URL-encoded form data.","**`@Multipart` + `@Part`** — files + fields (`multipart/form-data`).","**`MultipartBody.Part`** — wrap the file's `RequestBody`.","**Large files** — stream from a source; consider progress + `@Streaming` for downloads."]},{t:"note",text:"Forms: @FormUrlEncoded + @Field. File uploads: @Multipart + @Part with MultipartBody.Part (wrapping a RequestBody from the file) — sends multipart/form-data. Stream large files from a source; track progress via a custom RequestBody."}]},{level:"junior",q:"What is a Converter, and how do you configure JSON parsing in Retrofit?",a:[{t:"p",text:"A `Converter.Factory` tells Retrofit how to serialize the request body and deserialize the response — i.e. the JSON library. You add one when building Retrofit: `kotlinx-serialization` (modern, no reflection), Moshi, or Gson (legacy). The converter turns your Kotlin data classes into JSON and back."},{t:"code",title:"Adding a converter",code:`val retrofit = Retrofit.Builder()
    .baseUrl("https://api.example.com/")
    .client(okHttpClient)
    .addConverterFactory(json.asConverterFactory("application/json".toMediaType()))  // kotlinx
    .build()`},{t:"list",items:["**`addConverterFactory`** — plugs in the JSON library.","**kotlinx-serialization** — modern, compile-time, no reflection (recommended).","**Moshi** — good, codegen or reflection.","**Gson** — legacy; reflection-based, some pitfalls."]},{t:"note",text:"A Converter.Factory (addConverterFactory) tells Retrofit how to serialize/deserialize bodies — the JSON library. Prefer kotlinx-serialization (compile-time, no reflection); Moshi is solid; Gson is legacy. The converter maps Kotlin data classes ↔ JSON."}]},{level:"senior",q:"What is a CallAdapter, and how does Retrofit support suspend/Result?",a:[{t:"p",text:"A `CallAdapter.Factory` adapts Retrofit's `Call<T>` into another return type — `suspend` support is built in (returns `T` directly, or `Response<T>`). You can add custom adapters to return, say, `Result<T>` or a sealed `ApiResponse<T>`, moving error handling into the return type instead of try/catch at every call site."},{t:"list",items:["**`CallAdapter`** — adapts `Call<T>` to your return type.","**suspend built-in** — return `T` or `Response<T>` directly.","**Custom `Result`/`ApiResponse` adapter** — errors-as-values return types.","**RxJava adapters** — `Single`/`Observable` (legacy)."]},{t:"code",title:"suspend + custom Result",code:`@GET("user") suspend fun user(): User                 // built-in suspend
@GET("user") suspend fun user2(): Response<User>       // headers/status
@GET("user") suspend fun user3(): ApiResult<User>      // custom CallAdapter (errors-as-values)`},{t:"note",text:"A CallAdapter.Factory adapts Call<T> to another return type. suspend is built in (return T or Response<T>). A custom CallAdapter can return Result<T>/sealed ApiResponse<T>, moving error handling into the return type instead of try/catch everywhere. RxJava adapters exist for legacy code."}]},{level:"senior",q:"What is the difference between an application and a network interceptor?",a:[{t:"p",text:"*Application* interceptors sit at the top of the OkHttp chain — they see the original request, run once per call (not per redirect/retry), and can short-circuit without a network call. *Network* interceptors sit closer to the network — they run for each network request (including redirects/retries), see the actual request/response on the wire (including OkHttp-added headers), and can observe the redirect chain."},{t:"list",items:["**Application interceptor** — `addInterceptor`; once per call; sees the original request; can short-circuit.","**Network interceptor** — `addNetworkInterceptor`; per network request (redirects/retries); sees wire-level details.","**Auth headers** — usually an application interceptor (once).","**Logging the actual bytes** — a network interceptor (sees compressed/redirected)."]},{t:"note",text:"Application interceptors (addInterceptor): top of the chain, once per call, see the original request, can short-circuit. Network interceptors (addNetworkInterceptor): per network request (redirects/retries), see wire-level details (OkHttp-added headers). Use application for auth headers; network for wire-level logging."}]},{level:"junior",q:"How do you add authentication headers with an interceptor?",a:[{t:"p",text:"Add an *application interceptor* that reads the request, appends the `Authorization` header (from your token store), and proceeds. This centralizes auth so every request is authenticated without repeating the header. Read the token from a repository/DataStore; for token refresh on 401, use an `Authenticator` instead."},{t:"code",title:"Auth interceptor",code:`class AuthInterceptor(private val tokens: TokenStore) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response {
        val request = chain.request().newBuilder()
            .addHeader("Authorization", "Bearer \${tokens.access()}")
            .build()
        return chain.proceed(request)
    }
}`},{t:"list",items:["**Application interceptor** — append `Authorization` to every request.","**Central** — no per-call header repetition.","**Token source** — repository/DataStore (read the current token).","**Refresh** — use an `Authenticator` for 401 → refresh (not the interceptor)."]},{t:"note",text:"Add an application interceptor that appends the Authorization: Bearer <token> header (from your token store) to every request — centralizing auth. Read the token from a repository/DataStore. For refreshing on 401, use an OkHttp Authenticator (not the interceptor)."}]},{level:"senior",q:"What is an OkHttp Authenticator, and how does it handle 401 token refresh?",a:[{t:"p",text:"An `Authenticator` is called automatically by OkHttp when a response is `401 Unauthorized` — it lets you *refresh the token* and return a new request with the updated `Authorization` header, which OkHttp retries. The concurrency challenge: multiple simultaneous 401s shouldn't each refresh; synchronize so only one refresh happens and others reuse the new token."},{t:"code",title:"Authenticator",code:`class TokenAuthenticator(private val tokens: TokenStore) : Authenticator {
    override fun authenticate(route: Route?, response: Response): Request? {
        synchronized(this) {
            val newToken = tokens.refreshIfNeeded(response) ?: return null   // give up -> logout
            return response.request.newBuilder()
                .header("Authorization", "Bearer $newToken").build()
        }
    }
}`},{t:"list",items:["**Called on 401** — refresh the token, return a retried request.","**Return `null`** — to give up (e.g. refresh failed → log out).","**Concurrency** — synchronize/dedupe so one refresh serves many 401s.","**Avoid loops** — check the attempt count to prevent infinite retries."]},{t:"note",text:"An OkHttp Authenticator is called on 401 — refresh the token and return a retried request with the new Authorization (or null to give up → logout). Concurrency challenge: synchronize/dedupe so simultaneous 401s trigger ONE refresh (others reuse it); guard against infinite retry loops."}]},{level:"senior",q:"When should a Retrofit function return Response<T> versus bare T?",a:[{t:"p",text:"Return bare `T` (or your domain type) when you only care about the successful body — Retrofit throws `HttpException` for non-2xx. Return `Response<T>` when you need the *status code*, *headers* (ETag, pagination links), or the *error body* on failure — `Response` gives you `isSuccessful`, `code()`, `headers()`, and `errorBody()`."},{t:"code",title:"Response<T> for details",code:`@GET("users") suspend fun users(): Response<List<User>>
val r = api.users()
if (r.isSuccessful) handle(r.body(), r.headers()["ETag"])
else handleError(r.code(), r.errorBody()?.string())`},{t:"list",items:["**Bare `T`** — only need the success body; non-2xx throws `HttpException`.","**`Response<T>`** — need status/headers/error body.","**Headers** — ETag, pagination, rate limits.","**Error body** — parse structured error responses."]},{t:"note",text:"Bare T when you only need the success body (non-2xx throws HttpException). Response<T> when you need the status code, headers (ETag, pagination, rate limits), or the error body — via isSuccessful/code()/headers()/errorBody(). Use Response<T> for conditional requests and structured error handling."}]},{level:"senior",q:"How do you parse a structured error body from a failed response?",a:[{t:"p",text:"For non-2xx responses, the error JSON is in `response.errorBody()` (or `HttpException.response()?.errorBody()`). Read it as a string or convert it with a Retrofit `Converter` for your error DTO. Do this in your repository's error mapping so the UI gets a typed domain error (message, code) rather than a raw body."},{t:"code",title:"Parsing errorBody",code:`catch (e: HttpException) {
    val error = e.response()?.errorBody()?.string()?.let {
        json.decodeFromString<ApiError>(it)
    }
    return Result.failure(DomainError(error?.message ?: "Unknown", e.code()))
}`},{t:"list",items:["**`errorBody()`** — the non-2xx response body.","**Convert** — deserialize to an error DTO (converter or manual).","**Map to domain** — a typed error (message/code) for the UI.","**`errorBody()` is one-shot** — read it once (consumes the stream)."]},{t:"note",text:"Parse the error JSON from response.errorBody() (or HttpException.response()?.errorBody()) — deserialize to an error DTO and map to a typed domain error (message/code) in the repository. Note errorBody() is a one-shot stream (read once). Gives the UI structured errors, not raw bodies."}]},{level:"senior",q:"How does OkHttp's connection pool and HTTP/2 improve performance?",a:[{t:"p",text:"OkHttp maintains a *connection pool* that *reuses* TCP/TLS connections (keep-alive) across requests to the same host — avoiding the expensive handshake each time. With *HTTP/2*, it multiplexes many requests over a *single* connection (concurrent streams), reducing latency and overhead. Sharing one `OkHttpClient` is what lets the whole app benefit from the shared pool."},{t:"list",items:["**Connection pool** — reuse keep-alive connections; skip handshakes.","**HTTP/2 multiplexing** — many requests over one connection.","**Shared client** — one pool for the whole app.","**Less latency** — no repeated TCP/TLS setup."]},{t:"note",text:"OkHttp's connection pool reuses TCP/TLS keep-alive connections across requests (skipping handshakes); HTTP/2 multiplexes many concurrent requests over one connection. Sharing a single OkHttpClient means the whole app benefits from the shared pool — a key reason to reuse one client."}]},{level:"senior",q:"What is certificate pinning, and when should you use it?",a:[{t:"p",text:"Certificate pinning restricts which certificates your app trusts for a host — you 'pin' the server's certificate (or public key hash) so a man-in-the-middle with a rogue-but-valid CA cert is rejected. Configure it via OkHttp's `CertificatePinner` (or Network Security Config). Use it for high-security apps (banking), but *plan for rotation* — a pinned cert expiring without an app update breaks connectivity."},{t:"code",title:"CertificatePinner",code:`val client = OkHttpClient.Builder()
    .certificatePinner(CertificatePinner.Builder()
        .add("api.example.com", "sha256/AAAAAAA...")
        .build())
    .build()`},{t:"list",items:["**Pin the cert/public key** — trust only specific certs for a host.","**Blocks MITM** — even with a rogue valid CA cert.","**Use for** — high-security apps (finance, health).","**Rotation risk** — pin backups; expiring pins break the app without an update."]},{t:"note",text:"Certificate pinning trusts only specific certs/public-key hashes for a host (OkHttp CertificatePinner or Network Security Config), blocking MITM even with a rogue valid CA cert. Use for high-security apps — but pin backup keys and plan rotation, since an expiring pin without an app update breaks connectivity."}]},{level:"junior",q:"How do you cancel an in-flight network request?",a:[{t:"p",text:"With suspend Retrofit functions, *coroutine cancellation* cancels the request automatically — when the enclosing coroutine (e.g. `viewModelScope`) is cancelled, Retrofit cancels the underlying OkHttp `Call`, aborting the request. So leaving a screen (which cancels the scope) stops its network calls. For manual `Call<T>`, call `call.cancel()`."},{t:"list",items:["**Coroutine cancellation** — cancels the underlying OkHttp Call automatically.","**Scope teardown** — leaving a screen cancels `viewModelScope` → aborts requests.","**Manual `Call<T>`** — `call.cancel()`.","**Server work** — cancellation aborts the client request; the server may still finish."]},{t:"note",text:"For suspend Retrofit functions, coroutine cancellation cancels the underlying OkHttp Call automatically — so leaving a screen (cancelling viewModelScope) aborts its requests. For manual Call<T>, use call.cancel(). Cancellation aborts the client request; the server may still complete its work."}]},{level:"senior",q:"How do you configure a logging interceptor safely?",a:[{t:"p",text:"Add `HttpLoggingInterceptor` to log requests/responses at a chosen level (`BASIC`, `HEADERS`, `BODY`). Crucially, only enable verbose (`BODY`) logging in *debug* builds — logging bodies/headers in production leaks sensitive data (tokens, PII) and hurts performance. Redact auth headers and gate the interceptor behind `BuildConfig.DEBUG`."},{t:"code",title:"Debug-only logging",code:`if (BuildConfig.DEBUG) {
    builder.addInterceptor(HttpLoggingInterceptor().apply {
        level = HttpLoggingInterceptor.Level.BODY
        redactHeader("Authorization")
    })
}`},{t:"list",items:["**`HttpLoggingInterceptor`** — levels BASIC/HEADERS/BODY.","**Debug only** — never log bodies/headers in production (leaks tokens/PII).","**`redactHeader`** — hide sensitive headers.","**Gate on `BuildConfig.DEBUG`** — strip from release."]},{t:"note",text:'Add HttpLoggingInterceptor (BASIC/HEADERS/BODY), but enable BODY only in debug builds — logging bodies/headers in production leaks tokens/PII and hurts performance. redactHeader("Authorization") and gate the interceptor behind BuildConfig.DEBUG.'}]},{level:"senior",q:"How do you test Retrofit code with MockWebServer?",a:[{t:"p",text:'`MockWebServer` (OkHttp) runs a local HTTP server you point Retrofit at (`server.url("/")`), enqueue canned responses (`MockResponse` with status/body/delay), and assert the requests your code makes (`server.takeRequest()`). It tests the *real* Retrofit/OkHttp stack (parsing, headers, error handling) against controlled responses — more realistic than mocking the API interface.'},{t:"code",title:"MockWebServer test",code:`val server = MockWebServer()
server.enqueue(MockResponse().setResponseCode(200).setBody("""{"id":"1","name":"Sam"}"""))
val api = Retrofit.Builder().baseUrl(server.url("/")).addConverterFactory(...).build().create(Api::class.java)
val user = api.getUser("1")
assertEquals("Sam", user.name)
assertEquals("/users/1", server.takeRequest().path)`},{t:"list",items:["**Point Retrofit at `server.url`** — a local test server.","**`enqueue(MockResponse)`** — canned status/body/delay/errors.","**`takeRequest()`** — assert the outgoing request (path, headers, body).","**Tests the real stack** — parsing, converters, error handling."]},{t:"note",text:"MockWebServer runs a local server you point Retrofit at (server.url); enqueue MockResponses (status/body/delay/errors) and assert outgoing requests via takeRequest() (path/headers/body). It tests the real Retrofit/OkHttp stack (parsing/converters/error handling) against controlled responses — more realistic than mocking the API interface."}]},{level:"senior",q:"What is @Streaming, and when do you need it?",a:[{t:"p",text:"By default Retrofit *buffers the entire response body into memory* before returning — fine for JSON, but bad for *large downloads* (a big file could OOM). `@Streaming` makes Retrofit return the `ResponseBody` without buffering, so you stream it to disk with a `source`/`InputStream` while tracking progress. Use it for file downloads."},{t:"code",title:"@Streaming download",code:`@Streaming @GET
suspend fun download(@Url url: String): ResponseBody

val body = api.download(url)
body.byteStream().use { input -> file.outputStream().use { input.copyTo(it) } }`},{t:"list",items:["**Default buffers** — whole body in memory (OOM risk for big files).","**`@Streaming`** — returns `ResponseBody` unbuffered; stream to disk.","**Progress** — read in chunks and report bytes.","**Downloads** — the standard for large file downloads."]},{t:"note",text:"Retrofit buffers the whole response body by default (OOM risk for large files). @Streaming returns the ResponseBody unbuffered so you stream it to disk (byteStream/source) in chunks with progress. Use it for file downloads; keep default buffering for JSON."}]},{level:"senior",q:"How do you use a dynamic base URL or full URL per request?",a:[{t:"p",text:"Use `@Url` to pass a complete URL as a parameter (overriding the base URL) — useful for following pagination links, CDNs, or per-environment hosts returned by the server. For switching the base URL globally, you'd rebuild Retrofit or use an interceptor that rewrites the host."},{t:"code",title:"@Url",code:`@GET suspend fun page(@Url url: String): PageResponse   // full URL (e.g. next-page link)
@Streaming @GET suspend fun file(@Url fileUrl: String): ResponseBody`},{t:"list",items:["**`@Url`** — pass a full URL, overriding the base.","**Uses** — pagination `next` links, CDN/asset URLs, per-response hosts.","**Global base switch** — rebuild Retrofit or rewrite host in an interceptor.","**Relative resolution** — `@Url` can also be relative to the base."]},{t:"note",text:"@Url passes a complete URL per request, overriding the base URL — for following pagination next-links, CDN/asset URLs, or server-returned hosts. To switch the base URL globally, rebuild Retrofit or rewrite the host in an interceptor."}]},{level:"senior",q:"What does retryOnConnectionFailure do, and how does it differ from app-level retry?",a:[{t:"p",text:"OkHttp's `retryOnConnectionFailure` (default on) transparently retries when a *connection* fails (e.g. a stale pooled connection, or one of multiple IPs is down) — it does *not* retry on HTTP error responses (4xx/5xx) or timeouts. App-level retry (with backoff) is what you add for *transient server errors* (5xx) or timeouts, with your own policy. They operate at different layers."},{t:"list",items:["**`retryOnConnectionFailure`** — retries connection-level failures (stale conn, multi-IP); not HTTP errors.","**App-level retry** — your policy for 5xx/timeouts with backoff.","**Different layers** — OkHttp handles connectivity, you handle response errors.","**Idempotency** — only retry safe/idempotent requests at the app level."]},{t:"note",text:"OkHttp's retryOnConnectionFailure (default) transparently retries connection-level failures (stale pooled connection, alternate IPs) — NOT HTTP errors (4xx/5xx) or timeouts. App-level retry (backoff) handles transient server errors/timeouts with your policy. Different layers; only retry idempotent requests at the app level."}]},{level:"senior",q:"How do you provide Retrofit and OkHttp via dependency injection?",a:[{t:"p",text:"Provide a single `OkHttpClient` and a single `Retrofit` as `@Singleton`s (via Hilt/Koin), then provide each API interface with `retrofit.create(Api::class.java)`. This ensures one shared client/connection pool app-wide and makes APIs injectable into repositories. Interceptors (auth, logging) and converters are configured in the provider."},{t:"code",title:"Hilt network module",code:`@Provides @Singleton fun okHttp(auth: AuthInterceptor): OkHttpClient =
    OkHttpClient.Builder().addInterceptor(auth).build()
@Provides @Singleton fun retrofit(client: OkHttpClient): Retrofit =
    Retrofit.Builder().baseUrl(BASE_URL).client(client).addConverterFactory(...).build()
@Provides @Singleton fun api(retrofit: Retrofit): Api = retrofit.create(Api::class.java)`},{t:"list",items:["**Singleton client + Retrofit** — one shared instance app-wide.","**Provide each API** — `retrofit.create(Api::class.java)`.","**Configure once** — interceptors/converters in the provider.","**Inject APIs** — into repositories."]},{t:"note",text:"Provide a single @Singleton OkHttpClient and Retrofit (Hilt/Koin), then provide each API via retrofit.create(Api::class.java). One shared client/connection pool app-wide, APIs injectable into repositories, interceptors/converters configured once in the module."}]},{level:"junior",q:"What is the difference between Call<T> and a suspend function in Retrofit?",a:[{t:"p",text:"`Call<T>` is Retrofit's classic callback/blocking handle — you call `enqueue(callback)` (async) or `execute()` (blocking). A `suspend` function is the modern coroutine style — Retrofit runs the request off the main thread and returns the result (or throws) directly, integrating with structured concurrency and cancellation. Prefer `suspend` for new code."},{t:"list",items:["**`Call<T>`** — `enqueue(callback)` (async) or `execute()` (blocking); manual threading.","**`suspend`** — coroutine-friendly; main-safe; structured cancellation.","**Modern** — prefer `suspend` (or Flow via a stream adapter).","**Interop** — `Call<T>` still used in some libraries/legacy code."]},{t:"note",text:"Call<T> is the classic handle: enqueue(callback) (async) or execute() (blocking), manual threading. A suspend function is coroutine-style: main-safe, returns the result directly, integrates with structured concurrency/cancellation. Prefer suspend for new code; Call<T> remains for legacy/interop."}]},{level:"senior",q:"How does interceptor ordering affect behavior?",a:[{t:"p",text:"Interceptors run in the *order added*, forming a chain: the request flows *down* through them to the network, and the response flows *back up* in reverse. So an auth interceptor added before a logging interceptor adds the header *before* logging sees it. Order matters for things like adding headers (before logging), caching, and retry — place them deliberately."},{t:"list",items:["**Order = add order** — request goes down, response comes back up.","**Auth before logging** — so logging sees the added header.","**Network vs application** — network interceptors sit closer to the wire.","**Deliberate placement** — headers, caching, retry depend on order."]},{t:"note",text:"Interceptors run in add-order as a chain: request flows down to the network, response flows back up in reverse. So add auth before logging (logging sees the header). Application interceptors sit above network interceptors. Order matters for headers, caching, and retry — place them deliberately."}]},{level:"senior",q:"How does OkHttp's Dispatcher control concurrency?",a:[{t:"p",text:"OkHttp's `Dispatcher` manages the async request queue and *concurrency limits*: `maxRequests` (total concurrent requests, default 64) and `maxRequestsPerHost` (per host, default 5). When limits are hit, requests queue. For suspend calls (async), this bounds how many run at once; you can tune it for high-throughput scenarios or to avoid overwhelming a server."},{t:"list",items:["**`maxRequests`** — total concurrent async requests (default 64).","**`maxRequestsPerHost`** — per-host limit (default 5); often the real bottleneck.","**Queueing** — excess requests wait.","**Tune** — raise per-host for many parallel calls to one API; lower to be gentle."]},{t:"note",text:"OkHttp's Dispatcher bounds async concurrency: maxRequests (total, default 64) and maxRequestsPerHost (default 5 — often the real limit). Excess requests queue. Tune maxRequestsPerHost up for many parallel calls to one API, or keep it low to avoid overwhelming a server."}]},{level:"junior",q:"How do you send optional query parameters that should be omitted when null?",a:[{t:"p",text:"Make the `@Query` parameter *nullable* — Retrofit *omits* it from the URL when it's `null`. This is the clean way to handle optional filters (sort, category, cursor) without building URLs manually or sending empty params."},{t:"code",title:"Optional queries",code:`@GET("products")
suspend fun products(
    @Query("category") category: String? = null,   // omitted if null
    @Query("sort") sort: String? = null,
    @Query("cursor") cursor: String? = null,
): ProductPage`},{t:"list",items:["**Nullable `@Query`** — omitted from the URL when null.","**Optional filters** — sort/category/cursor without manual URL building.","**`@QueryMap`** — for a dynamic set of optional params.","**Defaults** — combine with Kotlin default arguments."]},{t:"note",text:"Make the @Query parameter nullable — Retrofit omits it from the URL when null. Clean handling of optional filters (sort/category/cursor) without manual URL building. Use @QueryMap for a dynamic set of optional params; combine with Kotlin default arguments."}]},{level:"senior",q:"How do you handle timeouts and slow networks gracefully?",a:[{t:"p",text:"Configure OkHttp's `connectTimeout`, `readTimeout`, and `writeTimeout` (and `callTimeout` for the whole call) to sensible values so requests fail fast instead of hanging. On timeout (`SocketTimeoutException`), treat it as a *retryable transient error* — retry with backoff for idempotent requests, or show an error with retry. Also serve cached data so a slow network doesn't block the UI."},{t:"code",title:"Timeouts",code:`OkHttpClient.Builder()
    .connectTimeout(10, TimeUnit.SECONDS)
    .readTimeout(30, TimeUnit.SECONDS)
    .writeTimeout(30, TimeUnit.SECONDS)
    .callTimeout(60, TimeUnit.SECONDS)   // whole-call cap
    .build()`},{t:"list",items:["**Timeouts** — connect/read/write + `callTimeout`; fail fast, don't hang.","**`SocketTimeoutException`** — treat as transient/retryable.","**Retry idempotent** — with backoff; show retry for others.","**Cache** — serve cached data so slow networks don't block the UI."]},{t:"note",text:"Set OkHttp connect/read/write timeouts (+ callTimeout for the whole call) so requests fail fast, not hang. Treat SocketTimeoutException as a transient/retryable error (backoff for idempotent requests, retry UI otherwise), and serve cached data so a slow network doesn't block the UI."}]}],BA={id:"retrofit-okhttp",title:"Retrofit & OkHttp",tagline:"The two-layer stack, defining APIs with annotations, suspend support, building Retrofit, interceptors as middleware, timeouts, and connection pooling.",tags:["Retrofit","OkHttp","Interceptors","Timeouts","Networking"],content:NA,qa:WA},zA=[{heading:"What serialization is and why it's needed",blocks:[{t:"p",text:"**Serialization** is converting an object into a format that can be stored or transmitted (like a JSON string), and **deserialization** (or parsing) is the reverse — turning that format back into objects. APIs communicate in JSON (text), but your app works with Kotlin objects, so you need to convert between them: your `User` object → JSON to send in a request body, and JSON response → your `User` object to use in code. A serialization library automates this mapping so you don't hand-write parsing."},{t:"code",title:"The conversion in both directions",code:`// Deserialization: JSON string -> Kotlin object
val json = """{"id":"42","name":"Revanth","is_premium":true}"""
val user: User = decode(json)     // -> User(id="42", name="Revanth", premium=true)

// Serialization: Kotlin object -> JSON string
val body: String = encode(user)   // -> {"id":"42",...}`}]},{heading:"The three main libraries",blocks:[{t:"table",headers:["Library","How it works","Notes"],rows:[["kotlinx.serialization","compile-time codegen via a compiler plugin","Kotlin-first, KMP-compatible, no reflection — the modern choice"],["Moshi","reflection or codegen (codegen recommended)","Square library, good Kotlin support, widely used"],["Gson","runtime reflection","older, Google's; works but has Kotlin pitfalls (ignores defaults/nullability) — legacy"]]},{t:"list",items:["**kotlinx.serialization** — the modern, Kotlin-first choice: it's a compiler plugin that generates serialization code *at compile time* (no runtime reflection), respects Kotlin's null safety and default values, and works in **KMP** (crucial for multiplatform). Use `@Serializable` on your classes.","**Moshi** — Square's library, a solid choice especially with its codegen (`moshi-kotlin-codegen`), which avoids reflection and handles Kotlin properly. Common in Retrofit apps.","**Gson** — Google's older library using runtime reflection. It works but has real Kotlin problems: it *ignores* default values and can put `null` into non-null Kotlin properties (breaking null safety) because it doesn't understand Kotlin's constructor semantics. Prefer kotlinx.serialization or Moshi for new code.","**For an Android/KMP engineer, kotlinx.serialization is the default recommendation** — it's Kotlin-native, KMP-ready, and avoids reflection."]}]},{heading:"kotlinx.serialization in practice",blocks:[{t:"code",title:"Annotating a data class",code:`@Serializable
data class UserDto(
    val id: String,
    val name: String,
    @SerialName("is_premium") val premium: Boolean = false,  // map JSON key -> property
    val email: String? = null,                                // nullable + default
    @Transient val localOnly: String = "",                    // excluded from JSON
)

// Configure the Json instance
val json = Json {
    ignoreUnknownKeys = true      // don't crash on extra JSON fields
    coerceInputValues = true      // use defaults for null/invalid where possible
    explicitNulls = false         // omit null fields when encoding
}`},{t:"list",items:["**`@Serializable`** — marks a class for serialization; the compiler plugin generates the serializer. **`@SerialName`** — maps a JSON key to a differently-named property (JSON `is_premium` → Kotlin `premium`), so your Kotlin code uses idiomatic names while matching the API's JSON.","**`@Transient`** — excludes a property from serialization (for computed or local-only fields). **`@Required`** — makes a property with a default mandatory in JSON.","**`ignoreUnknownKeys = true`** — *critical for robustness*: without it, an unexpected extra field in the JSON (which servers add over time) *crashes* parsing. With it, unknown fields are ignored. Almost always set this.","**Default values** — kotlinx.serialization respects Kotlin defaults: if a field is missing from the JSON, the default is used (not a crash). This makes your models resilient to optional/absent fields."]}]},{heading:"The DTO vs domain model distinction",blocks:[{t:"p",text:"A best practice: your **DTOs** (Data Transfer Objects — the classes that mirror the API's JSON exactly) should be *separate* from your **domain models** (the clean objects your app logic uses). The DTO is shaped by the *API contract* (nullable fields, snake_case names, exactly the server's structure); the domain model is shaped by *your app's needs* (non-null invariants, idiomatic names, only the fields you use). You map DTO → domain at the data-layer boundary."},{t:"code",title:"DTO mirrors JSON; domain model is clean",code:`// DTO — matches the API's JSON exactly (nullable, server names)
@Serializable
data class UserDto(
    @SerialName("user_id") val id: String?,
    @SerialName("full_name") val name: String?,
    @SerialName("is_premium") val premium: Boolean?,
)

// Domain model — clean, non-null, what your app uses
data class User(val id: String, val name: String, val premium: Boolean)

// Mapper at the boundary — resolves nullability, renames, validates
fun UserDto.toDomain() = User(
    id = id ?: error("missing id"),
    name = name ?: "Unknown",
    premium = premium ?: false,
)`},{t:"list",items:["**Why separate them**: the API's JSON changes independently of your app. Keeping DTOs separate means a server field rename or added field touches only the DTO and its mapper — not your whole codebase. The domain model stays stable and clean.","**Nullability resolved once**: servers often make everything nullable. The mapper resolves nullability at the boundary (providing defaults, validating), so the rest of your app works with clean non-null domain types instead of `?` everywhere.","**Pragmatic caveat**: for a tiny app, one model reused as both DTO and domain is a defensible shortcut — but split them the moment the API shape and your app's needs diverge (which they will). This ties into Clean Architecture's model-mapping principle."]}]},{heading:"Robustness and common pitfalls",blocks:[{t:"list",items:["**Always handle unknown/missing fields gracefully**: `ignoreUnknownKeys = true` (extra fields don't crash) and default values (missing fields use defaults). APIs evolve, and brittle parsing that crashes on any change is a common production bug.","**Polymorphic/sealed JSON**: for JSON that can be one of several shapes (a `type` discriminator field), kotlinx.serialization supports `@Serializable` sealed classes with a class discriminator — cleanly modeling 'this response is one of N types'.","**Dates and custom types**: JSON has no native date type — dates come as strings or numbers. Use a custom serializer (`@Serializable(with = ...)`) to convert to/from a proper type (`Instant`, `LocalDate`).","**Gson's Kotlin trap** (why to avoid it): Gson uses reflection and Java constructor semantics, so it *bypasses* Kotlin's default-value and null-check logic — it can leave a non-null property as `null` (a latent crash) or ignore your defaults. kotlinx.serialization and Moshi-codegen understand Kotlin properly.","**Retrofit integration**: add the converter factory (`Json.asConverterFactory(...)` for kotlinx.serialization, `MoshiConverterFactory`, or `GsonConverterFactory`) to your Retrofit builder, and Retrofit uses it to parse response bodies and serialize request bodies automatically."]},{t:"note",text:"Serialization essentials: converting objects ↔ JSON. kotlinx.serialization (compile-time, Kotlin-first, KMP, no reflection) is the modern choice; Moshi is solid; Gson is legacy with Kotlin pitfalls (ignores defaults/nullability). Use @Serializable/@SerialName; set ignoreUnknownKeys=true and use default values for robustness against API changes. Separate DTOs (mirror the API) from domain models (clean, non-null), mapping at the boundary. Integrate with Retrofit via a converter factory."}]}],HA=[{level:"junior",q:"What is JSON serialization and why do you need it?",a:[{t:"p",text:"**Serialization is converting a Kotlin object into a transmittable format (a JSON string), and deserialization is the reverse — parsing JSON back into objects.** You need it because APIs communicate in JSON (text) but your app works with Kotlin objects. So when you send data you serialize your object to JSON for the request body, and when you receive a response you deserialize the JSON into your objects to use in code."},{t:"code",title:"Both directions",code:`// JSON -> object (deserialize)
val user: UserDto = json.decodeFromString("""{"id":"1","name":"Rev"}""")
// object -> JSON (serialize)
val body: String = json.encodeToString(user)`},{t:"p",text:"A serialization library automates this mapping so you don't hand-write parsing code (which would be tedious and error-prone). On Android the main libraries are kotlinx.serialization (the modern Kotlin-first choice), Moshi, and Gson (legacy). You typically don't call serialize/deserialize manually — you configure a converter on Retrofit, and it automatically parses response bodies into your objects and serializes request bodies to JSON. The library reads your annotated data classes to know how to map between fields and JSON keys."}]},{level:"junior",q:"Which serialization library should you use and why?",a:[{t:"p",text:"**For new Android/KMP code, use kotlinx.serialization.** It's Kotlin-first: a compiler plugin generates serialization code *at compile time* (no runtime reflection), it fully respects Kotlin's null safety and default values, and — importantly for an Android/KMP engineer — it works in Kotlin Multiplatform, so the same models serialize on Android and iOS."},{t:"list",items:["**kotlinx.serialization** — the modern default. Compile-time, no reflection, KMP-compatible, understands Kotlin semantics. Mark classes `@Serializable`.","**Moshi** — Square's library, a solid choice too, especially with its codegen (avoiding reflection). Well-integrated with Retrofit. A reasonable alternative if you're not targeting KMP.","**Gson** — Google's older library, uses runtime reflection. It works but has real Kotlin pitfalls: it ignores Kotlin default values and can put `null` into non-null properties (because it bypasses Kotlin's constructor logic), silently breaking null safety. It's legacy — fine to maintain in existing code, but not for new code."]},{t:"p",text:"The short version: kotlinx.serialization for new code (Kotlin-native, KMP-ready, safe), Moshi as a solid alternative, and avoid Gson for new Kotlin code due to its null-safety and default-value problems. The KMP compatibility is often the deciding factor — Gson and Moshi are JVM-only, so a multiplatform project essentially must use kotlinx.serialization."}]},{level:"junior",q:"How do you handle a JSON field whose name doesn't match your Kotlin property?",a:[{t:"p",text:"**You use the `@SerialName` annotation (in kotlinx.serialization) to map the JSON key to your property, so your Kotlin code can use idiomatic names while still matching the API's JSON.** APIs often use snake_case (`is_premium`, `full_name`) while Kotlin convention is camelCase — `@SerialName` bridges that."},{t:"code",title:"Mapping JSON keys to Kotlin properties",code:`@Serializable
data class UserDto(
    @SerialName("user_id") val id: String,        // JSON "user_id" -> id
    @SerialName("full_name") val name: String,    // JSON "full_name" -> name
    @SerialName("is_premium") val premium: Boolean = false,
)`},{t:"p",text:'So the JSON key stays whatever the server sends (`user_id`), but in your code you use the clean name (`id`). Without this, your property name would have to exactly match the JSON key, forcing non-idiomatic Kotlin names. (Moshi uses `@Json(name = "...")` and Gson uses `@SerializedName("...")` for the same purpose.) This is also part of why keeping DTOs separate from domain models is useful — the DTO carries the API\'s naming via `@SerialName`, and the mapper translates to your clean domain model, so the server\'s naming conventions never leak into your app logic.'}]},{level:"junior",q:"How do you prevent parsing from crashing when the API adds a new field?",a:[{t:"p",text:"**You configure the parser to ignore unknown keys — in kotlinx.serialization, set `ignoreUnknownKeys = true` on the `Json` instance.** By default, strict parsing *crashes* when the JSON contains a field your data class doesn't declare. Since servers routinely add new fields over time (which they consider backward-compatible), strict parsing means a server-side addition can crash your released app. `ignoreUnknownKeys = true` makes the parser skip fields it doesn't recognize."},{t:"code",title:"Robust Json configuration",code:`val json = Json {
    ignoreUnknownKeys = true    // extra JSON fields don't crash parsing
    coerceInputValues = true    // use defaults for null/invalid values
    explicitNulls = false       // omit null fields when encoding
}`},{t:"p",text:"Combined with **default values** on your properties (so a *missing* field uses the default rather than failing), this makes your models resilient to API evolution in both directions — extra fields are ignored, and absent optional fields fall back to defaults. This is a genuinely important production concern: apps that crash whenever the backend adds a field are fragile, and the crash happens to *already-released* app versions you can't easily fix. Setting `ignoreUnknownKeys = true` and using sensible defaults is a standard robustness practice for any networked app."}]},{level:"senior",q:"Why separate DTOs from domain models, and where does mapping happen?",a:[{t:"p",text:"**A DTO (Data Transfer Object) mirrors the API's JSON *exactly* — its structure, nullability, and field names are dictated by the API contract. A domain model is shaped by *your app's* needs — clean names, non-null invariants, only the fields you use. Keeping them separate decouples your app from the API, and you map DTO → domain at the data-layer boundary (in the repository or a mapper it calls).**"},{t:"list",items:["**Decoupling from API changes** — the main reason. The server's JSON evolves independently of your app: fields get renamed, added, made nullable, restructured. If your whole app uses the DTO directly, every such change ripples through your entire codebase. With separate models, an API change touches only the DTO and its mapper — your domain model and all the code using it stay stable. It's a firewall between the external contract and your internal code.","**Nullability resolved once** — servers often mark everything nullable (defensive API design). If you use the DTO directly, `?` infects your whole app (`user?.name?.length`). The mapper resolves nullability *at the boundary* — providing defaults, validating, or failing fast for truly-required fields — so the rest of your app works with clean non-null domain types.","**Right shape for each layer** — the DTO can have API concerns (snake_case via `@SerialName`, serialization annotations, extra fields you ignore); the domain model has none of that noise and can add behavior/invariants. Serialization annotations don't pollute your business objects.","**Where mapping happens** — in the *data layer*, at the boundary where external data enters: the repository receives the DTO from Retrofit and maps it to the domain model (`dto.toDomain()`) before anything else in the app sees it. Mappers are plain functions, trivially unit-tested, and the place where dirty/optional server data gets validated and normalized."]},{t:"list",items:["**Often a three-way split**: DTO (network) → domain model (business) → UI model (presentation, formatted for display), mapping at each boundary. Plus a DB entity if you cache. Each is shaped by its layer's concerns.","**Pragmatic caveat**: for a tiny app or a throwaway prototype, reusing one model as both DTO and domain is a defensible YAGNI shortcut — but the moment the API shape and your app's needs diverge (which they inevitably do), split them. The cost of splitting later is higher than doing it up front for anything non-trivial."]},{t:"p",text:"**The senior framing**: this is the model-mapping principle from Clean Architecture applied to networking — each layer owns models shaped by *its* concerns, and you translate at boundaries. The payoff is a stable core that's insulated from an unstable, externally-controlled API, plus nullability and formatting resolved once at the edge instead of leaking everywhere. It's a small amount of mapping boilerplate that buys significant resilience and cleanliness."}]},{level:"senior",q:"How do you handle polymorphic JSON — a response that can be one of several types?",a:[{t:"p",text:"**You model it with a sealed class hierarchy plus a *class discriminator* — a field in the JSON (often `type`) that tells the parser which concrete type each object is. kotlinx.serialization supports this natively for `@Serializable` sealed classes.** The parser reads the discriminator field and deserializes into the matching subtype."},{t:"code",title:"Polymorphic deserialization with a discriminator",code:`@Serializable
sealed class Notification {
    @Serializable @SerialName("message")
    data class Message(val text: String, val from: String) : Notification()

    @Serializable @SerialName("friend_request")
    data class FriendRequest(val userId: String) : Notification()

    @Serializable @SerialName("system")
    data class System(val code: Int) : Notification()
}

val json = Json { classDiscriminator = "type" }  // JSON's "type" field selects the subtype
// {"type":"message","text":"hi","from":"a"} -> Notification.Message(...)`},{t:"list",items:['**How it works**: each subtype is annotated with a `@SerialName` that matches its discriminator *value* (`"message"`, `"friend_request"`). The `classDiscriminator` config names the JSON *field* that holds that value (`"type"`). When parsing, kotlinx.serialization reads the `type` field, finds the subtype whose `@SerialName` matches, and deserializes the rest into that type.',"**Why sealed + discriminator is the right model**: it turns 'this response could be one of N shapes' into a type-safe sealed hierarchy, so consuming code uses an *exhaustive `when`* over the subtypes — the compiler ensures you handle every notification type, and adding a new one flags every `when` that needs updating. This is far safer than parsing into a generic map and branching on a string.","**Custom discriminator handling**: if the API's discriminator doesn't fit the built-in mechanism (the type indicator is nested, or the shapes are distinguished by *presence* of fields rather than an explicit type field), you write a *custom serializer* that inspects the JSON and picks the type manually. kotlinx.serialization's `JsonContentPolymorphicSerializer` is designed for exactly the 'no explicit discriminator, distinguish by content' case.","**Open polymorphism / third-party types**: for types not in a sealed hierarchy (open polymorphism across modules), you register subtypes in a `SerializersModule`. Sealed hierarchies are preferred when possible because they're closed and exhaustive."]},{t:"p",text:"**The senior framing**: polymorphic JSON is common (notification feeds, event streams, content blocks, payment methods), and the robust approach is to model the closed set of shapes as a `@Serializable` sealed class with a discriminator, giving you type-safe, exhaustive handling. When the API's discrimination is irregular (no clean `type` field), you drop to a custom content-based serializer. Either way, the goal is to convert loosely-typed 'one of several JSON shapes' into a strongly-typed sealed hierarchy at the parsing boundary, so the rest of your code enjoys compile-time exhaustiveness rather than runtime string-matching."}]},{level:"junior",q:"How do you set up kotlinx.serialization for a data class?",a:[{t:"p",text:"Apply the `kotlin-serialization` Gradle plugin, annotate your data class with `@Serializable`, and use a `Json` instance to encode/decode. The compiler *generates* the serializer at compile time (no reflection), so it's fast and works on KMP. Property names map to JSON keys by default."},{t:"code",title:"@Serializable",code:`@Serializable
data class User(val id: String, val name: String, val age: Int)

val json = Json { ignoreUnknownKeys = true }
val user = json.decodeFromString<User>(jsonString)
val text = json.encodeToString(user)`},{t:"list",items:["**Plugin + `@Serializable`** — compile-time generated serializer (no reflection).","**`Json` instance** — configure once; `decodeFromString`/`encodeToString`.","**KMP-friendly** — works across platforms.","**Retrofit** — `Json.asConverterFactory(...)` integrates it."]},{t:"note",text:"Apply the kotlin-serialization plugin, annotate data classes @Serializable, and use a configured Json instance (decodeFromString/encodeToString). The serializer is generated at compile time (no reflection — fast, KMP-friendly). Integrate with Retrofit via Json.asConverterFactory."}]},{level:"junior",q:"How do you provide default values and handle missing fields?",a:[{t:"p",text:"Give the property a *default value* in the data class — if the JSON omits that field, the default is used (so the response doesn't fail). By default kotlinx.serialization *encodes* defaults; set `encodeDefaults = false` to omit them when serializing. Defaults make your models resilient to optional/absent fields."},{t:"code",title:"Defaults",code:`@Serializable
data class Settings(
    val theme: String = "system",       // used if JSON omits it
    val notifications: Boolean = true,
)
val json = Json { encodeDefaults = false }   // omit defaults when serializing`},{t:"list",items:["**Default value** — used when the field is absent.","**Resilient** — missing optional fields don't crash.","**`encodeDefaults`** — control whether defaults are serialized.","**Distinguish absent vs null** — nullable + default for both cases."]},{t:"note",text:"Give properties default values — absent JSON fields use the default (no crash). encodeDefaults (default true) controls whether defaults are serialized; set false to omit them. Use nullable + default to distinguish absent from explicit null. Defaults make models resilient to optional fields."}]},{level:"senior",q:"What is the difference between missing, null, and default in JSON parsing?",a:[{t:"p",text:"Three distinct cases: a field can be *absent* (not in the JSON at all), present with a value of `null`, or present with a value. A *non-nullable* property with a *default* handles 'absent' (uses the default) but *fails on explicit null*. A *nullable* property handles null. To fully distinguish absent vs null (e.g. for PATCH semantics), use a nullable type with a default of `null` — but note that then you can't tell 'absent' from 'null' without extra tooling."},{t:"list",items:["**Absent** — field not in JSON; a default fills it.","**Null** — field present, value null; needs a nullable type.","**Value** — present with data.","**Both absent+null** — nullable + default `null` (can't distinguish the two without custom handling)."]},{t:"note",text:"Absent (not in JSON — default fills it), null (present but null — needs nullable type), or a value. A non-nullable property with a default handles absent but fails on explicit null. Nullable + default null handles both, but you can't distinguish absent from null without extra tooling (matters for PATCH)."}]},{level:"junior",q:"How do you handle nested objects and lists in JSON?",a:[{t:"p",text:"Nest `@Serializable` data classes and use `List<T>`/`Map<K, V>` of serializable types — the serializer handles them recursively. A response with an object containing an array of objects maps directly to a data class with a `List<Child>` property. No special handling needed beyond marking every type `@Serializable`."},{t:"code",title:"Nested structures",code:`@Serializable data class Response(val user: User, val posts: List<Post>)
@Serializable data class User(val id: String, val name: String)
@Serializable data class Post(val id: String, val tags: List<String>)`},{t:"list",items:["**Nest `@Serializable` classes** — recursive serialization.","**`List`/`Map`/`Set`** — of serializable types.","**Direct mapping** — JSON structure ↔ class structure.","**Mark all types** — every nested type needs `@Serializable`."]},{t:"note",text:"Nest @Serializable data classes and use List<T>/Map<K,V> of serializable types — the serializer handles them recursively, mapping the JSON structure directly to the class structure. Every nested type must be @Serializable. No special handling beyond that."}]},{level:"senior",q:"How do you write a custom serializer for a non-standard type?",a:[{t:"p",text:"Implement `KSerializer<T>` (define `serialize`/`deserialize` and a `descriptor`) for types the library doesn't handle out of the box — like a `Date`/`Instant` in a specific format, or a value wrapped differently. Apply it with `@Serializable(with = MySerializer::class)` on the property, or register it in the `Json` module for global use."},{t:"code",title:"Custom serializer",code:`object InstantSerializer : KSerializer<Instant> {
    override val descriptor = PrimitiveSerialDescriptor("Instant", PrimitiveKind.STRING)
    override fun serialize(e: Encoder, v: Instant) = e.encodeString(v.toString())
    override fun deserialize(d: Decoder): Instant = Instant.parse(d.decodeString())
}
@Serializable data class Event(@Serializable(with = InstantSerializer::class) val at: Instant)`},{t:"list",items:["**`KSerializer<T>`** — implement `serialize`/`deserialize` + `descriptor`.","**Apply** — `@Serializable(with = ...)` per property, or a contextual/module registration.","**Uses** — dates/times, custom formats, value classes.","**Contextual** — `contextualSerializer` for types you can't annotate."]},{t:"note",text:"Implement KSerializer<T> (serialize/deserialize + descriptor) for unsupported types (dates in a specific format, custom wrappers), applied via @Serializable(with = ...) per property or registered in the Json module (contextual). Handles types the library doesn't serialize out of the box."}]},{level:"senior",q:"How do you configure the Json instance (lenient, ignoreUnknownKeys, etc.)?",a:[{t:"p",text:"Build a `Json { }` with the options your API needs: `ignoreUnknownKeys = true` (don't fail on new/extra fields — important for forward compatibility), `coerceInputValues = true` (use defaults for null/invalid values), `isLenient = true` (accept malformed JSON like unquoted keys), `explicitNulls = false` (omit nulls), and `encodeDefaults`. Configure one shared instance."},{t:"code",title:"Json config",code:`val json = Json {
    ignoreUnknownKeys = true      // forward-compatible with new fields
    coerceInputValues = true       // null/invalid -> default
    explicitNulls = false          // omit null fields when encoding
    isLenient = false              // strict JSON (usually)
}`},{t:"list",items:["**`ignoreUnknownKeys`** — tolerate extra/new fields (forward-compat).","**`coerceInputValues`** — fall back to defaults for null/invalid.","**`explicitNulls`** — omit nulls on encode.","**`isLenient`** — accept malformed JSON (usually keep strict)."]},{t:"note",text:"Configure one shared Json { }: ignoreUnknownKeys = true (forward-compatible with new fields), coerceInputValues = true (null/invalid → default), explicitNulls = false (omit nulls), encodeDefaults, isLenient (usually strict). These control resilience and output shape."}]},{level:"junior",q:"How do you rename a JSON field with @SerialName?",a:[{t:"p",text:'When the JSON key doesn\'t match your Kotlin property name (e.g. `snake_case` JSON vs `camelCase` Kotlin), annotate the property with `@SerialName("json_key")`. For a whole API using snake_case, you can instead configure a `JsonNamingStrategy` (SnakeCase) to convert automatically rather than annotating every field.'},{t:"code",title:"@SerialName / naming strategy",code:`@Serializable data class User(
    @SerialName("full_name") val fullName: String,
    @SerialName("created_at") val createdAt: String,
)
// Or globally:
val json = Json { namingStrategy = JsonNamingStrategy.SnakeCase }`},{t:"list",items:['**`@SerialName("key")`** — map a property to a specific JSON key.',"**`JsonNamingStrategy.SnakeCase`** — convert all names automatically.","**Use per-field** — for one-off mismatches.","**Use strategy** — for a consistently snake_case API."]},{t:"note",text:'Use @SerialName("json_key") to map a property to a differently-named JSON key (snake_case JSON vs camelCase Kotlin). For a wholly snake_case API, configure Json { namingStrategy = JsonNamingStrategy.SnakeCase } to convert automatically instead of annotating every field.'}]},{level:"senior",q:"How do you handle a field that can be different types (e.g. sometimes a string, sometimes an object)?",a:[{t:"p",text:"This 'union type' JSON is awkward. Options: parse it as `JsonElement` and inspect/branch manually; write a custom serializer that checks the shape and produces a sealed type; or (better) push back on the API for consistency. For a field that's sometimes a value and sometimes null/absent, use a nullable type. Avoid designing this into your own APIs."},{t:"list",items:["**`JsonElement`** — parse raw, inspect the shape, branch.","**Custom serializer** — detect the type, map to a sealed result.","**Nullable** — for value-or-null fields.","**Prefer consistent APIs** — push back on shape-shifting fields."]},{t:"note",text:"For a field that changes type, parse it as JsonElement and branch on the shape, or write a custom serializer producing a sealed type. For value-or-null, use a nullable type. Best: push the API toward consistent shapes — don't design shape-shifting fields into your own APIs."}]},{level:"senior",q:"How do you serialize sealed classes / polymorphic types with a discriminator?",a:[{t:"p",text:"Mark the sealed hierarchy `@Serializable` — kotlinx.serialization adds a *class discriminator* field (default `\"type\"`) whose value selects the subtype on decode. You can customize the discriminator name and each subtype's name with `@SerialName`. This handles responses that are 'one of several types' (e.g. a feed of different item kinds)."},{t:"code",title:"Polymorphic sealed types",code:`@Serializable
sealed interface FeedItem {
    @Serializable @SerialName("post") data class Post(val text: String) : FeedItem
    @Serializable @SerialName("ad") data class Ad(val url: String) : FeedItem
}
val json = Json { classDiscriminator = "kind" }   // customize the discriminator field`},{t:"list",items:['**`@Serializable` sealed** — auto discriminator (default `"type"`).',"**`@SerialName`** — subtype names in the discriminator.","**`classDiscriminator`** — customize the field name.","**Uses** — mixed feeds, event types, one-of responses."]},{t:"note",text:'Mark the sealed hierarchy @Serializable — kotlinx.serialization adds a class discriminator field (default "type") whose value picks the subtype; customize with @SerialName per subtype and Json { classDiscriminator = "kind" }. Handles one-of responses (mixed feeds, event types).'}]},{level:"senior",q:"How do you parse an enum, and handle unknown enum values gracefully?",a:[{t:"p",text:"Mark the enum `@Serializable` (map values with `@SerialName`). The problem: if the API adds a new enum value your app doesn't know, parsing *fails*. Handle it by including an `UNKNOWN` fallback and setting `coerceInputValues = true` (with the fallback as the default), or use a custom serializer that maps unrecognized strings to `UNKNOWN` — so a new server value doesn't crash old apps."},{t:"code",title:"Enum with fallback",code:`@Serializable enum class Status {
    @SerialName("active") ACTIVE,
    @SerialName("inactive") INACTIVE,
    UNKNOWN;   // fallback for new/unrecognized values
}
val json = Json { coerceInputValues = true }   // unknown -> UNKNOWN (if it's the default)`},{t:"list",items:["**`@Serializable` enum + `@SerialName`** — map JSON values.","**Unknown values crash** — new server enum breaks old apps.","**`UNKNOWN` fallback + `coerceInputValues`** — degrade gracefully.","**Or custom serializer** — map unrecognized → `UNKNOWN`."]},{t:"note",text:"Mark enums @Serializable (@SerialName per value). Unknown server values crash parsing — add an UNKNOWN fallback + coerceInputValues (fallback as default), or a custom serializer mapping unrecognized strings to UNKNOWN. Prevents a new server enum from crashing old app versions."}]},{level:"senior",q:"What is the difference between reflection-based (Gson) and codegen (Moshi/kotlinx) serialization?",a:[{t:"p",text:"*Reflection-based* (Gson, Moshi-reflect) inspects classes at *runtime* to map fields — flexible but slower, larger (keeps reflection metadata), and doesn't respect Kotlin nullability/defaults well (Gson can create nulls in non-null fields). *Codegen* (kotlinx.serialization, Moshi-codegen) generates serializers at *compile time* — faster, smaller, Kotlin-aware (nullability, defaults), and KMP-capable. Prefer codegen."},{t:"table",headers:["","Reflection (Gson)","Codegen (kotlinx/Moshi)"],rows:[["When","runtime","compile time"],["Speed","slower","faster"],["Kotlin-aware","poor (nulls)","yes"],["KMP","no","yes (kotlinx)"]]},{t:"list",items:["**Reflection (Gson)** — runtime mapping; flexible but slow, Kotlin-unaware.","**Codegen (kotlinx/Moshi)** — compile-time serializers; fast, Kotlin-aware.","**Gson pitfall** — can put null in non-null Kotlin fields.","**Prefer codegen** — kotlinx.serialization for new/KMP code."]},{t:"note",text:"Reflection-based (Gson) maps at runtime — flexible but slower, larger, and Kotlin-unaware (can put null in non-null fields). Codegen (kotlinx.serialization, Moshi-codegen) generates serializers at compile time — faster, smaller, respects nullability/defaults, KMP-capable. Prefer codegen (kotlinx) for new code."}]},{level:"junior",q:"How do you serialize and parse a raw JSON tree without a data class?",a:[{t:"p",text:'Use `JsonElement` (and `JsonObject`/`JsonArray`/`JsonPrimitive`) to work with JSON dynamically — parse with `Json.parseToJsonElement(str)`, navigate (`jsonObject["key"]`), and read primitives. Useful for unstructured/dynamic data, inspecting before deciding a type, or extracting one field from a large response. Prefer typed data classes when the structure is known.'},{t:"code",title:"JsonElement",code:`val element = Json.parseToJsonElement(jsonString)
val name = element.jsonObject["user"]?.jsonObject?.get("name")?.jsonPrimitive?.content`},{t:"list",items:["**`JsonElement`/`JsonObject`/`JsonArray`** — dynamic JSON tree.","**`parseToJsonElement`** — parse without a target class.",'**Navigate** — `jsonObject["key"]`, `jsonPrimitive.content`.',"**Prefer typed classes** — when the structure is known."]},{t:"note",text:'Use JsonElement/JsonObject/JsonArray/JsonPrimitive for dynamic JSON: Json.parseToJsonElement(str), navigate with jsonObject["key"], read jsonPrimitive.content. For unstructured/dynamic data or extracting one field. Prefer typed data classes when the structure is known.'}]},{level:"senior",q:"How do you handle dates and times in JSON?",a:[{t:"p",text:"JSON has no date type, so dates come as strings (ISO-8601, e.g. `2024-01-15T10:30:00Z`) or epoch numbers. Parse them into a proper type (`Instant`/`LocalDateTime` via kotlinx-datetime or java.time) with a custom serializer or by storing the string and converting in your mapper. Always be explicit about *time zones* (prefer UTC/ISO-8601 with offset) to avoid bugs."},{t:"list",items:["**Dates as strings/numbers** — ISO-8601 or epoch millis.","**Parse to a type** — `Instant`/`LocalDateTime` (kotlinx-datetime/java.time).","**Custom serializer** — map the string format to the type.","**Time zones** — use UTC/ISO-8601 with offset; convert for display."]},{t:"note",text:"JSON has no date type — dates arrive as ISO-8601 strings or epoch numbers. Parse to Instant/LocalDateTime (kotlinx-datetime/java.time) via a custom serializer (or store the string, convert in the mapper). Be explicit about time zones (prefer UTC/ISO-8601 with offset) to avoid bugs."}]},{level:"junior",q:"Why should you make DTO fields nullable or defaulted defensively?",a:[{t:"p",text:"Servers can send incomplete, unexpected, or evolving responses (a field missing, null where you expected a value, new fields). Making DTO fields *nullable or defaulted* prevents a single unexpected field from crashing the *entire* parse. You then map to a clean, non-null *domain model* — handling the messiness once at the boundary rather than letting it propagate."},{t:"list",items:["**Servers are imperfect** — missing/null/new fields happen.","**Nullable/defaulted DTOs** — one bad field doesn't crash the whole parse.","**Map to domain** — clean, non-null models after validation.","**Boundary handling** — deal with messiness once, at parsing."]},{t:"note",text:"Servers send incomplete/unexpected/evolving responses, so make DTO fields nullable or defaulted defensively — one bad field won't crash the whole parse. Then map DTOs to clean non-null domain models, handling the messiness once at the boundary rather than letting it propagate."}]},{level:"senior",q:"How do you test serialization code?",a:[{t:"p",text:"Test round-trips (encode then decode equals the original) and, importantly, *decode real/sample JSON* (including edge cases: missing fields, nulls, unknown fields, wrong types) to assert your DTOs and config handle them without crashing. Keep sample JSON fixtures from the actual API. These tests catch mapping bugs and guard against API changes breaking parsing."},{t:"code",title:"Serialization test",code:`@Test fun parsesMissingFields() {
    val user = json.decodeFromString<User>("""{"id":"1"}""")   // name absent
    assertEquals("default", user.name)   // default used, no crash
}`},{t:"list",items:["**Round-trip** — encode→decode equals the original.","**Decode sample JSON** — real responses + edge cases (missing/null/unknown/wrong types).","**Fixtures** — keep sample JSON from the actual API.","**Catch mapping bugs** — and API-change regressions."]},{t:"note",text:"Test serialization with round-trips (encode→decode equals original) and by decoding real/sample JSON including edge cases (missing/null/unknown fields, wrong types) to assert no crashes and correct defaults. Keep API JSON fixtures. Catches mapping bugs and guards against API changes breaking parsing."}]},{level:"senior",q:"What is the performance cost of JSON parsing, and how do you optimize it?",a:[{t:"p",text:"Parsing large JSON is CPU-bound and allocates — it can jank the UI if done on the main thread. Optimize by: parsing off the main thread (Retrofit suspend functions do this), using codegen serializers (no reflection), streaming very large responses instead of buffering, requesting only needed fields (or a leaner endpoint), and avoiding re-parsing (cache the parsed model). For huge datasets, consider a binary format (protobuf)."},{t:"list",items:["**Off main thread** — Retrofit suspend functions parse in the background.","**Codegen serializers** — faster than reflection.","**Stream large responses** — avoid buffering everything.","**Request less** — leaner endpoints; consider binary (protobuf) for huge data."]},{t:"note",text:"JSON parsing is CPU-bound and allocating — off the main thread (Retrofit suspend does this) to avoid jank. Optimize with codegen serializers (no reflection), streaming large responses, requesting only needed fields, caching parsed models, and considering a binary format (protobuf) for very large datasets."}]},{level:"junior",q:"How does the DTO-to-domain mapping layer work?",a:[{t:"p",text:"DTOs (data transfer objects) mirror the API's JSON shape (nullable, snake_case, server quirks). *Mappers* convert them to clean *domain models* (non-null, validated, in your app's terms) at the repository boundary. This decouples the app from the API — if the API changes, you fix the DTO/mapper, not the whole app — and lets the domain be exactly what the app needs."},{t:"code",title:"DTO → domain",code:`@Serializable data class UserDto(val id: String?, val full_name: String?)
data class User(val id: String, val name: String)   // clean domain model
fun UserDto.toDomain() = User(id = id.orEmpty(), name = full_name ?: "Unknown")`},{t:"list",items:["**DTO** — mirrors the API (nullable, snake_case, quirks).","**Domain model** — clean, non-null, app-centric.","**Mapper** — converts DTO→domain at the boundary.","**Decoupling** — API changes stay isolated to DTO/mapper."]},{t:"note",text:"DTOs mirror the API's JSON (nullable, snake_case, quirks); mappers convert them to clean non-null domain models at the repository boundary. This decouples the app from the API — API changes only touch the DTO/mapper — and lets the domain be exactly what the app needs."}]},{level:"junior",q:"How do you exclude a field from serialization with @Transient?",a:[{t:"p",text:"Annotate a property `@Transient` to exclude it from serialization/deserialization — it won't be written or read, and must have a default value (since it's not populated from JSON). Use it for computed, cached, or runtime-only fields that shouldn't cross the wire."},{t:"code",title:"@Transient",code:`@Serializable data class User(
    val id: String,
    val name: String,
    @Transient val isSelected: Boolean = false,   // not serialized; needs a default
)`},{t:"list",items:["**`@Transient`** — exclude from serialization/deserialization.","**Needs a default** — it's not read from JSON.","**Uses** — computed/cached/runtime-only fields.","**vs `@Ignore`** — that's Room; `@Transient` is serialization."]},{t:"note",text:"@Transient excludes a property from serialization/deserialization (needs a default, since it's not read from JSON) — for computed/cached/runtime-only fields that shouldn't cross the wire. (Room's equivalent is @Ignore.)"}]},{level:"senior",q:"How do you require a field to be present with @Required?",a:[{t:"p",text:"A property *with a default* is optional (parsing succeeds if it's absent). To make a defaulted property *mandatory* — fail if the JSON omits it — annotate it `@Required`. This is useful when a field is critical and a missing value indicates a malformed response you'd rather catch than silently default."},{t:"code",title:"@Required",code:`@Serializable data class Config(
    @Required val apiVersion: String = "",   // must be present despite the default
    val optional: String = "x",
)`},{t:"list",items:["**Default = optional** — absent field uses the default.","**`@Required`** — forces presence even if defaulted.","**Fails on absence** — throws if the field is missing.","**Use** — critical fields where absence is an error."]},{t:"note",text:"A property with a default is optional (absent → default). @Required forces it to be present in the JSON despite the default (throws if missing) — for critical fields where absence indicates a malformed response you'd rather catch than silently default."}]},{level:"senior",q:"How do you handle large numbers and precision in JSON?",a:[{t:"p",text:"JSON numbers can exceed `Int` range or lose precision as `Double` (money!). Use `Long` for large integers (ids/timestamps) and *never* store currency as `Double` (floating-point errors) — represent money as an integer of minor units (cents) or as a `String`/`BigDecimal` in JSON. Large ids from some servers should be `String` or `Long`, not `Int`, to avoid overflow."},{t:"list",items:["**`Long` for large ints** — ids/timestamps beyond `Int` range.","**Money ≠ `Double`** — floating-point errors; use minor units (cents) or String/BigDecimal.","**Large ids as `String`** — avoid overflow/precision loss.","**Precision** — JSON parsers may coerce big numbers to `Double`; be deliberate."]},{t:"note",text:"Use Long for large integers (ids/timestamps beyond Int); never store money as Double (floating-point errors) — use integer minor units (cents) or String/BigDecimal. Represent very large ids as String/Long to avoid overflow. Be deliberate about number types to prevent precision loss."}]},{level:"senior",q:"How does kotlinx.serialization support formats other than JSON?",a:[{t:"p",text:"kotlinx.serialization is format-agnostic: the same `@Serializable` classes can be encoded/decoded with different formats — `Json`, `Cbor` (compact binary), `Protobuf` (schema-compatible binary), and `Properties`. You pick the format instance; the serializers are the same. This lets you use JSON for APIs and a binary format (CBOR/Protobuf) where size/speed matters (e.g. caching, IPC)."},{t:"code",title:"Multiple formats",code:`@Serializable data class User(val id: String, val name: String)
val jsonBytes = Json.encodeToString(user)
val cborBytes = Cbor.encodeToByteArray(user)      // compact binary
val protoBytes = ProtoBuf.encodeToByteArray(user)  // protobuf-compatible`},{t:"list",items:["**Format-agnostic** — one `@Serializable`, many formats.","**`Json`/`Cbor`/`Protobuf`/`Properties`** — pick the encoder.","**Binary** — smaller/faster for caching, IPC, storage.","**Same serializers** — no per-format annotations."]},{t:"note",text:"kotlinx.serialization is format-agnostic: the same @Serializable classes encode/decode with Json, Cbor (compact binary), Protobuf (schema-compatible), or Properties — pick the format instance. Use JSON for APIs, binary (CBOR/Protobuf) where size/speed matters (caching/IPC/storage). Same serializers throughout."}]},{level:"senior",q:"How do you evolve a JSON schema without breaking old app versions?",a:[{t:"p",text:"Make changes *additive and backward-compatible*: add new fields (old apps ignore them via `ignoreUnknownKeys`; new fields need defaults so absence is fine for old servers), never remove/rename fields old apps rely on (use `@SerialName` to keep old keys), and don't change types. For breaking changes, version the API. Because old app versions persist, the server must tolerate old clients and vice versa."},{t:"list",items:["**Additive changes** — add fields with defaults; `ignoreUnknownKeys` on the client.","**Don't remove/rename** — old apps break; keep old keys via `@SerialName`.","**Don't change types** — breaks parsing.","**Version for breaking changes** — old app versions linger."]},{t:"note",text:"Evolve JSON additively: add fields with defaults (old apps ignore extras via ignoreUnknownKeys), never remove/rename fields old apps use (keep old keys with @SerialName), don't change types. Version the API for breaking changes. Old app versions persist, so both sides must tolerate each other."}]},{level:"senior",q:"What is contextual serialization, and when do you need it?",a:[{t:"p",text:"Contextual serialization lets you use a serializer for a type you *can't annotate* (a third-party class, or where you want to swap serializers per context). You register a serializer in the `Json` module (`serializersModule = SerializersModule { contextual(...) }`) and mark the property `@Contextual`. It's the way to serialize types you don't own or want configured centrally."},{t:"code",title:"Contextual",code:`val json = Json {
    serializersModule = SerializersModule { contextual(InstantSerializer) }
}
@Serializable data class Event(@Contextual val at: Instant)`},{t:"list",items:["**`@Contextual`** — use a serializer registered in the module.","**Module registration** — `contextual(Serializer)`.","**For un-annotatable types** — third-party classes.","**Central config** — swap serializers per `Json` instance."]},{t:"note",text:"Contextual serialization uses a serializer registered in the Json module (serializersModule { contextual(...) }) with @Contextual on the property — for types you can't annotate (third-party classes) or want configured centrally. Lets you serialize types you don't own or vary the serializer per context."}]},{level:"senior",q:"How do Moshi adapters compare to kotlinx.serialization?",a:[{t:"p",text:"Both are modern JSON libraries. *Moshi* uses `JsonAdapter`s (with codegen via KSP or reflection) — mature, great error messages, adapter-based customization. *kotlinx.serialization* uses compiler-plugin-generated serializers — no reflection, *multiplatform* (KMP), format-agnostic (JSON/CBOR/Protobuf). For KMP or a fully-Kotlin stack, kotlinx.serialization is the natural choice; Moshi is excellent for Android-only projects."},{t:"table",headers:["","Moshi","kotlinx.serialization"],rows:[["Mechanism","JsonAdapter (codegen/reflect)","compiler plugin"],["KMP","no","yes"],["Formats","JSON","JSON/CBOR/Protobuf"],["Maturity","very mature","mature, Kotlin-first"]]},{t:"list",items:["**Moshi** — JsonAdapters, codegen (KSP), mature; Android-focused.","**kotlinx** — compiler plugin, no reflection, KMP, multi-format.","**KMP / all-Kotlin** — kotlinx.serialization.","**Android-only** — either; both are solid."]},{t:"note",text:"Moshi (JsonAdapters via KSP codegen/reflection — mature, great errors, JSON-only, Android) vs kotlinx.serialization (compiler-plugin serializers — no reflection, KMP, JSON/CBOR/Protobuf). Choose kotlinx for KMP/all-Kotlin stacks; Moshi is excellent for Android-only. Both are modern and solid."}]}],KA={id:"serialization",title:"JSON Serialization",tagline:"Serialize/deserialize, kotlinx.serialization vs Moshi vs Gson, @Serializable/@SerialName, robustness (ignoreUnknownKeys), DTO vs domain, and polymorphic JSON.",tags:["JSON","kotlinx.serialization","Moshi","DTO","Serialization"],content:zA,qa:HA},_A=[{heading:"The kinds of network failures",blocks:[{t:"p",text:"Networking fails in several distinct ways, and robust apps distinguish them because each needs a different response. Lumping all failures into 'something went wrong' gives poor UX and wrong retry behavior."},{t:"list",items:["**No connectivity / IO failure** — `IOException` (no network, connection dropped, DNS failure, timeout). Transient — the request never reached the server or got cut off. *Retry-able*; show 'check your connection'.","**HTTP error responses** — the server responded, but with a non-2xx status. Retrofit surfaces these as `HttpException` (with the status code). Sub-cases matter: 401 (re-authenticate), 403 (no permission), 404 (not found), 429 (rate-limited — back off), 5xx (server error — retry).","**Serialization/parsing errors** — the response arrived but couldn't be parsed (malformed JSON, unexpected shape). Usually a bug or API mismatch, not transient — don't retry; log it.","**Cancellation** — the coroutine was cancelled (user left the screen). This is *normal*, not an error — `CancellationException` must be rethrown, not treated as a failure."]}]},{heading:"Modeling errors as values (Result types)",blocks:[{t:"p",text:"The recommended approach: don't let raw exceptions propagate up through your layers. Instead, **catch technical exceptions at the data-layer boundary and map them into a typed result** — a sealed error type in domain terms. The ViewModel then handles that result as an expected branch (map to UI state), never dealing with `HttpException`/`IOException` directly."},{t:"code",title:"A typed error vocabulary + safe API call wrapper",code:`sealed interface DataError {
    data object NoConnection : DataError
    data object Unauthorized : DataError
    data object NotFound : DataError
    data class Server(val code: Int) : DataError
    data class Unknown(val cause: Throwable) : DataError
}

suspend fun <T> safeApiCall(call: suspend () -> T): Result<T, DataError> = try {
    Result.Success(call())
} catch (e: CancellationException) {
    throw e                                    // NEVER swallow cancellation
} catch (e: IOException) {
    Result.Failure(DataError.NoConnection)     // no network / timeout
} catch (e: HttpException) {
    Result.Failure(when (e.code()) {
        401 -> DataError.Unauthorized
        404 -> DataError.NotFound
        in 500..599 -> DataError.Server(e.code())
        else -> DataError.Unknown(e)
    })
} catch (e: Exception) {
    Result.Failure(DataError.Unknown(e))       // parse errors, etc.
}`},{t:"list",items:["**Map technical → domain errors at the boundary**: the data layer translates `IOException`/`HttpException` into a domain vocabulary (`NoConnection`, `Unauthorized`), so upper layers never depend on library-specific exception types.","**Always rethrow `CancellationException`** — a broad catch would swallow it and break coroutine cancellation. Catch it first and rethrow (or catch specific types only).","**The ViewModel handles the result exhaustively** — `when (error) { NoConnection -> showOffline(); Unauthorized -> goToLogin(); ... }` — mapping each error to the right UX, with the compiler ensuring every case is covered."]}]},{heading:"Mapping errors to good UX",blocks:[{t:"table",headers:["Error","UX response"],rows:[["No connection","'You're offline' + retry; keep showing cached data if available"],["Unauthorized (401)","attempt token refresh; if that fails, send to login"],["Forbidden (403)","'You don't have access' — not a retry situation"],["Not found (404)","'Not found' / empty state"],["Rate limited (429)","back off and retry after the delay; maybe a gentle 'try again shortly'"],["Server error (5xx)","'Something went wrong' + retry (with backoff)"]]},{t:"list",items:["**Don't blank the screen on error if you have cached data** — in an offline-first app, a failed refresh should keep showing the cached content and surface the error non-destructively (a snackbar), not replace everything with a full-screen error.","**Distinguish retryable from not** — offer retry for transient errors (network, 5xx); for a 403 or a 404, retry is pointless, so guide the user differently.","**Be specific but not technical** — 'Check your connection' is helpful; 'HttpException 503' is not. Map error types to human messages."]}]},{heading:"Token authentication and automatic refresh",blocks:[{t:"p",text:"Most APIs use token auth: a short-lived **access token** on each request, and a long-lived **refresh token** to get a new access token when it expires. The two mechanisms in OkHttp: an **Interceptor** to *attach* the token to every request, and an **Authenticator** to *refresh* it automatically on a 401."},{t:"code",title:"Interceptor (attach) + Authenticator (refresh on 401)",code:`// Attach the current access token to every request
class AuthInterceptor(private val tokens: TokenStore) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response =
        chain.proceed(
            chain.request().newBuilder()
                .header("Authorization", "Bearer \${tokens.access()}")
                .build()
        )
}

// When a request comes back 401, refresh the token and retry automatically
class TokenAuthenticator(private val tokens: TokenStore, private val authApi: AuthApi) : Authenticator {
    override fun authenticate(route: Route?, response: Response): Request? {
        synchronized(this) {                     // avoid many parallel refreshes
            val newToken = runBlocking { authApi.refresh(tokens.refresh()) }
            tokens.saveAccess(newToken.access)
            return response.request.newBuilder()
                .header("Authorization", "Bearer \${newToken.access}")
                .build()                         // OkHttp retries with the new token
        }
    }
}`},{t:"list",items:["**Interceptor attaches the token** to every outgoing request — so you never manually add it per call.","**Authenticator handles refresh**: when a response is 401 (token expired), OkHttp calls the `Authenticator`; you refresh the access token using the refresh token, and return the retried request with the new token. OkHttp automatically re-sends it. Returning `null` means 'give up' (refresh failed → log out).","**Concurrency care**: if many requests 401 at once, you don't want N simultaneous refresh calls — synchronize/deduplicate the refresh (a mutex, or check if the token was already refreshed) so only one refresh happens and the others use the new token.","**On refresh failure** (refresh token also expired/invalid) → clear the session and route to login. This is the 'you've been logged out' flow.","**Store tokens securely** — `EncryptedSharedPreferences` or encrypted storage, never plaintext."]}]},{heading:"Retry strategy",blocks:[{t:"list",items:["**Retry only transient errors** — network failures and 5xx (server errors). Don't retry 4xx client errors (they'll fail identically), except 401 (after refresh) and 429 (after the rate-limit delay).","**Exponential backoff with jitter** — increase the delay between attempts (1s, 2s, 4s…) and add randomness (jitter) so many clients don't retry in perfect sync (the 'thundering herd' that overwhelms a recovering server).","**Bound the attempts** — cap retries (e.g. 3) so a persistent failure doesn't loop forever draining battery.","**Idempotency for non-idempotent requests** — only blindly retry idempotent operations (GET/PUT/DELETE); for POST, use an idempotency key so a retry doesn't duplicate the action.","**Where to implement** — a Flow `retryWhen` operator, a custom OkHttp interceptor, or explicit logic in the repository. `retryWhen` in a Flow pipeline is clean for observable data."]},{t:"note",text:"Error/auth essentials: distinguish failure types (IOException = transient/retry; HttpException by code — 401 refresh, 403/404 don't retry, 429 back off, 5xx retry; parse errors = bug; cancellation = normal, rethrow). Model errors as typed values at the data-layer boundary; handle exhaustively in the ViewModel mapping to UX (keep cached data on refresh failure). Token auth: Interceptor attaches the token, Authenticator refreshes on 401 (deduplicated); refresh failure → logout; store tokens encrypted. Retry only transient errors with bounded exponential backoff + jitter."}]}],GA=[{level:"junior",q:"What are the different kinds of network errors you need to handle?",a:[{t:"list",items:["**Connectivity / IO failures** (`IOException`) — no network, connection dropped, DNS failure, timeout. The request never completed. These are *transient*, so retrying may work; show a 'check your connection' message.","**HTTP error responses** (`HttpException` in Retrofit) — the server responded with a non-2xx status. The specific code matters: 401 (re-authenticate), 403 (no permission), 404 (not found), 429 (rate-limited), 5xx (server error).","**Parsing/serialization errors** — the response arrived but couldn't be deserialized (malformed or unexpected JSON). Usually a bug or API mismatch, not transient — don't retry, log it.","**Cancellation** (`CancellationException`) — the coroutine was cancelled because the user left the screen. This is *normal*, not an error, and must be rethrown, not caught as a failure."]},{t:"p",text:"The reason to distinguish them is that each needs a *different* response. A network failure should offer retry and keep showing cached data; a 401 should trigger token refresh; a 404 should show an empty state; a parse error should be logged as a bug; and a cancellation should be ignored. Treating all failures as a generic 'error' gives poor UX (a retry button on a 403 that will never succeed) and wrong behavior (retrying a request that can't succeed, or breaking cancellation by swallowing `CancellationException`)."}]},{level:"junior",q:"Why should you map network exceptions to a typed result instead of letting them propagate?",a:[{t:"p",text:"**Because network failures are *expected outcomes* on mobile (the network is unreliable), not exceptional bugs — so modeling them as typed values makes handling explicit, exhaustive, and decoupled from library details.** Instead of letting `IOException`/`HttpException` propagate up through your layers, you catch them at the data-layer boundary and return a typed result — a sealed error type in your app's own vocabulary."},{t:"code",title:"Typed errors handled exhaustively",code:`sealed interface DataError { object NoConnection; object Unauthorized; data class Server(val code: Int) }

// ViewModel handles each case — compiler ensures all are covered
when (error) {
    DataError.NoConnection -> showOfflineMessage()
    DataError.Unauthorized -> navigateToLogin()
    is DataError.Server -> showRetry()
}`},{t:"list",items:["**Explicit and exhaustive** — a sealed error type forces the ViewModel to handle every case in a `when`, so you can't forget to handle 'offline' or 'unauthorized'. Exceptions are easy to forget to catch.","**Decoupled from libraries** — mapping `HttpException`/`IOException` to your own `DataError` at the boundary means the rest of your app never depends on Retrofit/OkHttp exception types. If you swap networking libraries, only the mapper changes.","**Better UX mapping** — each typed error maps cleanly to a UX response (offline message, login redirect, retry), rather than parsing exception messages.","**Reserves exceptions for real bugs** — expected failures become data; a thrown exception then genuinely means something is wrong (a programming error), making crash reports meaningful."]},{t:"p",text:"The one caution: when catching broadly to build the result, always rethrow `CancellationException` first (or catch specific types), or you'll break coroutine cancellation. This 'errors as values' approach is the modern recommended pattern and ties into Clean Architecture's layered error handling."}]},{level:"junior",q:"How does token-based authentication work in an Android app with Retrofit?",a:[{t:"p",text:"**The app obtains a token at login and then attaches it to every request via an OkHttp interceptor, so the server can identify the user on each stateless request.** The typical setup uses two OkHttp mechanisms: an *Interceptor* to attach the token, and an *Authenticator* to refresh it when it expires."},{t:"list",items:["**Login** — the user posts credentials; the server returns an access token (and usually a refresh token).","**Attach with an Interceptor** — an `AuthInterceptor` adds `Authorization: Bearer <accessToken>` to every outgoing request automatically, so you don't add it manually on each API call.","**Refresh with an Authenticator** — when a request returns 401 (token expired), OkHttp calls the `Authenticator`, which uses the refresh token to get a new access token and retries the request with it — transparently, without the user noticing.","**Store securely** — tokens go in `EncryptedSharedPreferences` or encrypted storage, never plaintext."]},{t:"p",text:"The access token is usually short-lived (for security — if it leaks, it expires soon), and the refresh token is longer-lived and used only to mint new access tokens. If refreshing *also* fails (the refresh token expired or was revoked), the app clears the session and sends the user to login — the 'you've been logged out' flow. This design works precisely because HTTP is stateless: the server doesn't remember you between requests, so you re-present the token every time, and the interceptor makes that automatic."}]},{level:"junior",q:"When should you retry a failed network request and when should you not?",a:[{t:"p",text:"**Retry *transient* failures — ones that might succeed on a second attempt — and don't retry failures that will fail identically no matter how many times you try.** The distinction comes from the error type and HTTP status code."},{t:"list",items:["**Retry**: network/IO failures (`IOException`, timeouts) — the request may have just hit a temporary connectivity blip. And 5xx server errors — the server had a transient problem that may resolve.","**Don't retry**: 4xx client errors — they mean *your request is wrong* (400 bad request, 403 forbidden, 404 not found), so retrying the same request fails the same way. Also don't retry parse errors (a bug/API mismatch).","**Special cases**: retry a 401 *after* refreshing the token (the retry uses the new token); retry a 429 (rate-limited) only *after* waiting the required delay."]},{t:"p",text:"And when you do retry, do it properly: use *exponential backoff* (increasing delays between attempts) so you don't hammer a struggling server, add *jitter* (randomness) so many clients don't retry in sync, and *bound* the attempts (e.g. 3) so a persistent failure doesn't loop forever draining battery. One more critical point: only blindly retry *idempotent* requests (GET/PUT/DELETE); for a POST that has side effects (placing an order), a blind retry could duplicate the action, so those need an idempotency key. The short rule: retry transient errors (network, 5xx) with bounded exponential backoff + jitter; never blindly retry client errors or non-idempotent operations."}]},{level:"senior",q:"How do you implement automatic token refresh, and what's the concurrency challenge?",a:[{t:"p",text:"**You use an OkHttp `Authenticator`, which OkHttp invokes automatically when a response comes back 401. In it, you refresh the access token using the refresh token and return the original request rebuilt with the new token — OkHttp then automatically retries it. Returning `null` signals 'give up' (refresh failed → log out).** The `Authenticator` is specifically designed for this reactive 'refresh on 401' flow, distinct from the `Interceptor` that proactively attaches the token."},{t:"code",title:"The Authenticator with refresh dedup",code:`class TokenAuthenticator(private val store: TokenStore, private val authApi: AuthApi) : Authenticator {
    override fun authenticate(route: Route?, response: Response): Request? {
        synchronized(this) {
            // If another thread already refreshed, use the new token instead of refreshing again
            val current = store.access()
            if (response.request.header("Authorization") != "Bearer $current") {
                return response.request.newBuilder()
                    .header("Authorization", "Bearer $current").build()
            }
            val refreshed = runBlocking { authApi.refresh(store.refresh()) } ?: return null
            store.saveAccess(refreshed.access)
            return response.request.newBuilder()
                .header("Authorization", "Bearer \${refreshed.access}").build()
        }
    }
}`},{t:"list",items:["**The concurrency challenge — the thundering herd of refreshes**: if the token expires while *several* requests are in flight, they *all* come back 401 around the same time, and each would independently trigger a refresh. That means N simultaneous refresh calls — wasteful, and worse, dangerous: many auth servers *rotate* the refresh token on use (return a new refresh token and invalidate the old), so parallel refreshes race, some using an already-invalidated refresh token, and you can end up logging the user out incorrectly.","**The fix — serialize and deduplicate the refresh**: use a lock (`synchronized`/`Mutex`) so only one refresh happens at a time. Critically, *before* refreshing inside the lock, check whether the token was *already refreshed* by a concurrent request (compare the failing request's token to the current stored token) — if so, just retry with the already-new token instead of refreshing again. This 'check-then-refresh under a lock' ensures exactly one refresh serves all the 401s.","**On refresh failure** — return `null` from the `Authenticator` so OkHttp stops retrying, and trigger the logout flow (clear session, navigate to login). Distinguish 'refresh token expired' (log out) from a transient network error during refresh (which you might retry).","**Avoid infinite loops** — if the retried request *also* 401s (e.g. the new token is immediately rejected), the Authenticator could loop; guard against retrying more than once (OkHttp limits this, but check `responseCount`).","**`runBlocking` caveat** — the `Authenticator` API is synchronous, so calling a suspend refresh requires `runBlocking`, which is acceptable here because it runs on OkHttp's background thread (not the main thread), but it's a known friction point."]},{t:"p",text:"**The senior framing**: automatic token refresh is straightforward in the happy path but the *concurrency* is where it goes wrong in production — concurrent 401s causing racing refreshes and (with rotating refresh tokens) spurious logouts. The robust implementation serializes refresh with a lock, deduplicates by checking if the token was already refreshed, handles refresh failure by logging out, and guards against retry loops. Getting this right is what makes 'the session silently keeps working' feel seamless instead of randomly logging users out."}]},{level:"senior",q:"In an offline-first app, how should a failed network refresh affect the UI?",a:[{t:"p",text:"**It should *not* blank the screen or replace content with a full-screen error — because the app has cached data to show. Instead, keep displaying the cached content and surface the refresh failure *non-destructively* (e.g. a snackbar or a subtle indicator), so the user still sees useful data and just knows the latest fetch didn't succeed.** This follows directly from the single-source-of-truth architecture: the UI reads from the local database, and a failed network refresh simply means the database wasn't updated — it doesn't affect the UI's ability to show what's already cached."},{t:"list",items:["**The mechanism**: because the UI observes the local DB (SSOT), it *always* has data to display. A refresh is a separate operation that *tries* to update the DB. If the refresh fails (offline, server error), the DB keeps its previous data, the observing UI keeps showing it, and you report the *refresh's* failure separately from the *data display*.","**Separate 'data' state from 'refresh' state**: the UI state should distinguish 'here's the data' (from the DB, always present if cached) from 'the last refresh failed' (a transient status). A common shape: `UiState(items = cached, isRefreshing = false, refreshError = \"Couldn't update\")`. The list shows `items`; the error shows as a snackbar; neither wipes the other.","**Contrast with the empty case**: if there's *no* cached data at all (first launch, offline), *then* a failure does warrant a full-screen error/empty state with retry — because there's genuinely nothing to show. So the rule is conditional: show cached data + gentle error if cache exists; show full-screen error + retry only if there's nothing cached.","**Offer retry appropriately**: for transient errors (network, 5xx), a retry affordance (pull-to-refresh, a retry button on the snackbar) makes sense. For a 403, retry is pointless — guide the user differently."]},{t:"p",text:"**The senior framing**: this is a defining benefit of offline-first / SSOT architecture — errors become *non-blocking* because the UI's data source (the DB) is decoupled from the network. The design principle is 'never punish the user for a failed background refresh when you already have data to show' — degrade gracefully by keeping cached content visible and reporting the refresh failure gently, reserving full-screen error states for the genuine 'no data at all' case. An app that shows a full-screen error and loses the user's content every time a refresh fails on a flaky network is exactly the fragile experience offline-first exists to prevent."}]},{level:"senior",q:"How do you model network results as a sealed type (Result/Resource)?",a:[{t:"p",text:"Wrap network outcomes in a sealed type — `Result<T>` (Kotlin's) or a custom `Resource<T>`/`ApiResult<T>` with `Success(data)`, `Error(type/message)`, and optionally `Loading`. The repository catches exceptions and maps them to this type, so callers handle outcomes with an exhaustive `when` instead of try/catch scattered everywhere — errors become data."},{t:"code",title:"Sealed result",code:`sealed interface ApiResult<out T> {
    data class Success<T>(val data: T) : ApiResult<T>
    data class Error(val type: ErrorType, val message: String?) : ApiResult<Nothing>
}
suspend fun getUser(id: String): ApiResult<User> = try {
    ApiResult.Success(api.getUser(id))
} catch (e: IOException) { ApiResult.Error(ErrorType.Network, null) }
  catch (e: HttpException) { ApiResult.Error(ErrorType.from(e.code()), e.message()) }`},{t:"list",items:["**Sealed `Success`/`Error`** — outcomes as data.","**Repository maps** — catch exceptions, produce the result.","**Exhaustive `when`** — callers handle all cases.","**Preserve cancellation** — rethrow `CancellationException`."]},{t:"note",text:"Model outcomes as a sealed type (Result<T>/Resource<T> with Success/Error, optional Loading). The repository catches exceptions and maps to it, so callers use an exhaustive when instead of scattered try/catch — errors as data. Rethrow CancellationException in the mapping."}]},{level:"senior",q:"How do you implement exponential backoff with jitter for retries?",a:[{t:"p",text:"Retry transient failures with a delay that grows exponentially (`base * 2^attempt`) plus random *jitter* (so many clients don't retry in sync — the thundering herd). Cap the number of attempts and the max delay, and only retry idempotent/transient errors (5xx, timeouts, connectivity) — not 4xx. In coroutines, `delay` makes the wait cancellable."},{t:"code",title:"Backoff retry",code:`suspend fun <T> retry(times: Int = 3, block: suspend () -> T): T {
    var delayMs = 500L
    repeat(times - 1) { attempt ->
        try { return block() }
        catch (e: CancellationException) { throw e }
        catch (e: Exception) { if (!e.isTransient()) throw e }
        delay(delayMs + Random.nextLong(0, 250)); delayMs = (delayMs * 2).coerceAtMost(8000)
    }
    return block()
}`},{t:"list",items:["**Exponential** — `base * 2^attempt`; spreads retries.","**Jitter** — randomness avoids synchronized retries.","**Cap** — max attempts + max delay.","**Transient only** — 5xx/timeouts/connectivity; not 4xx or cancellation."]},{t:"note",text:"Retry transient failures with delay = base * 2^attempt + random jitter (avoid thundering herd), capping attempts and max delay, only for transient errors (5xx/timeouts/connectivity — not 4xx). Rethrow CancellationException. delay makes the wait cancellable in coroutines."}]},{level:"senior",q:"How do you securely store authentication tokens?",a:[{t:"p",text:"Don't store tokens in plain SharedPreferences/DataStore. Use the Android *Keystore* to hold an encryption key (hardware-backed, non-exportable) and encrypt the token before persisting, or use `EncryptedSharedPreferences`. Prefer *short-lived access tokens* + a refresh token so a leaked access token expires quickly. On logout, clear all tokens."},{t:"list",items:["**Not plaintext** — never plain prefs/DataStore for tokens.","**Keystore-encrypted** — hardware-backed key; encrypt the token.","**Short-lived access + refresh** — limit exposure of a leaked token.","**Clear on logout** — wipe tokens."]},{t:"note",text:"Store tokens encrypted: use the Android Keystore (hardware-backed, non-exportable key) to encrypt before persisting, or EncryptedSharedPreferences — never plaintext prefs/DataStore. Prefer short-lived access tokens + refresh tokens to limit leaked-token exposure; clear all tokens on logout."}]},{level:"senior",q:"What is the difference between an interceptor and an Authenticator for auth?",a:[{t:"p",text:"An *interceptor* runs on *every* request — use it to *proactively add* the `Authorization` header. An *Authenticator* is called *reactively* by OkHttp only when a response is `401 Unauthorized` — use it to *refresh* the token and retry. So the interceptor attaches the current token; the Authenticator recovers from an expired one. They work together."},{t:"list",items:["**Interceptor** — proactive; adds the auth header to every request.","**Authenticator** — reactive; called on 401 to refresh + retry.","**Together** — interceptor attaches token, Authenticator refreshes on expiry.","**Authenticator can return null** — to give up (logout)."]},{t:"note",text:"An interceptor runs on every request (proactively add Authorization); an Authenticator is called by OkHttp only on 401 (reactively refresh the token and retry, or return null to give up). Interceptor attaches the current token; Authenticator recovers from expiry. They complement each other."}]},{level:"junior",q:"How do you distinguish between different network error types?",a:[{t:"p",text:"Map exceptions/status codes to distinct types: `IOException`/`UnknownHostException` → *no connectivity*; `SocketTimeoutException` → *timeout* (retryable); `HttpException` with 401 → *unauthorized* (refresh); 403 → *forbidden*; 404 → *not found*; 4xx → *client error*; 5xx → *server error* (retryable); `SerializationException` → *parse error* (a bug/API change). Each maps to different UI and retry behavior."},{t:"list",items:["**Connectivity** — `IOException`/`UnknownHostException` → offline UI.","**Timeout** — `SocketTimeoutException` → retryable.","**HTTP** — `HttpException` code: 401 refresh, 403/404 surface, 5xx retry.","**Parse** — `SerializationException` → a bug/API change (report)."]},{t:"note",text:"Map to types: IOException/UnknownHostException = offline; SocketTimeoutException = timeout (retryable); HttpException by code (401 refresh, 403/404 surface, 4xx client, 5xx retryable); SerializationException = parse error (bug/API change — report). Each drives different UI and retry behavior."}]},{level:"senior",q:"How do you handle a 401 that means the session is truly expired?",a:[{t:"p",text:"A 401 first triggers a *token refresh* attempt. If the refresh *also fails* (refresh token expired/revoked), the session is genuinely over — you should *log the user out*: clear tokens, clear user data, and navigate to login. Emit a global 'session expired' event (a Channel/SharedFlow the app observes) so this happens once, cleanly, regardless of which request hit the 401."},{t:"list",items:["**401 → refresh** — try to get a new token first.","**Refresh fails → logout** — clear tokens/data, go to login.","**Global event** — emit 'session expired' once (Channel/SharedFlow).","**Clean, single logout** — regardless of which request triggered it."]},{t:"note",text:"401 first triggers a refresh; if refresh also fails (refresh token expired/revoked), the session is over — log out: clear tokens/user data, navigate to login. Emit a global 'session expired' event (Channel/SharedFlow) so logout happens once cleanly, no matter which request hit the 401."}]},{level:"junior",q:"How do you show errors to the user meaningfully?",a:[{t:"p",text:"Map typed errors to *helpful, actionable* messages: 'You're offline — check your connection' (with retry) for connectivity, 'Something went wrong, try again' for 5xx (retry), specific validation messages for 4xx, and 'Session expired, please log in' for auth. Avoid raw exception text/codes. Distinguish transient (retry) from permanent (fix/inform) errors, and keep the app usable (cached data) where possible."},{t:"list",items:["**Actionable messages** — 'offline, retry', not raw stack traces.","**Match error type** — connectivity/server/validation/auth.","**Retry affordance** — for transient errors.","**Keep usable** — show cached data when offline."]},{t:"note",text:"Map typed errors to actionable messages: 'offline — check connection' (retry) for connectivity, 'try again' for 5xx, specific validation for 4xx, 'session expired' for auth — never raw exception text/codes. Distinguish transient (retry) from permanent (fix/inform); keep the app usable with cached data."}]},{level:"senior",q:"What is a circuit breaker, and when is it useful?",a:[{t:"p",text:"A circuit breaker stops hammering a failing service: after a threshold of failures, it 'opens' (fails fast without calling the network) for a cooldown, then 'half-opens' to test recovery. This prevents wasting resources and worsening an outage, and gives a fast, predictable failure to the UI. It's more common server-side but useful on the client for a repeatedly-failing endpoint."},{t:"list",items:["**Open on repeated failures** — fail fast without calling.","**Cooldown then half-open** — test if the service recovered.","**Avoids hammering** — a struggling service and wasting battery/data.","**Client use** — a repeatedly-failing endpoint; combine with backoff."]},{t:"note",text:"A circuit breaker opens after repeated failures (fail fast without calling), waits a cooldown, then half-opens to test recovery — preventing hammering a failing service and giving fast predictable failures. More common server-side but useful client-side for a repeatedly-failing endpoint; combine with backoff."}]},{level:"senior",q:"How do you handle authentication for WebSockets or long-lived connections?",a:[{t:"p",text:"WebSockets can't easily add per-message auth headers, so authenticate at *connection time*: pass the token in the connection request headers (or a query param, less ideal — it's logged), or send an auth message as the first frame after connecting. When the token expires, you typically *reconnect* with a fresh token. Handle 401/auth-failure on connect by refreshing and retrying the connection."},{t:"list",items:["**Auth at connect** — token in the upgrade request headers (preferred).","**Or first-frame auth** — send an auth message after connecting.","**Token expiry** — reconnect with a refreshed token.","**Avoid token in query** — it gets logged."]},{t:"note",text:"WebSockets authenticate at connection time: pass the token in the upgrade request headers (preferred) or send an auth message as the first frame; avoid tokens in the query (logged). On expiry, reconnect with a refreshed token; handle connect-time auth failure by refreshing and retrying."}]},{level:"senior",q:"How do you test error and auth handling?",a:[{t:"p",text:"Use `MockWebServer` to enqueue error responses (500, 401, timeouts via `setBodyDelay`, malformed bodies) and assert your code maps them to the right typed errors and UI states. Test the *refresh flow*: enqueue a 401 then a successful retry after refresh, and assert one refresh happened and the request succeeded. Test the *give-up* path (refresh fails → logout)."},{t:"list",items:["**MockWebServer error responses** — 500/401/timeouts/malformed.","**Assert error mapping** — correct typed errors/UI states.","**Refresh flow** — 401 then success; assert single refresh.","**Give-up path** — refresh fails → logout event."]},{t:"note",text:"Test with MockWebServer: enqueue error responses (500/401/timeouts via setBodyDelay/malformed) and assert correct typed-error mapping and UI states. Test the refresh flow (401 → success after one refresh) and the give-up path (refresh fails → logout event). Verify concurrency (simultaneous 401s → one refresh)."}]},{level:"junior",q:"Why should errors be handled in the repository, not the UI?",a:[{t:"p",text:"The repository is the boundary between the network/data layer and the app. Handling errors there — catching exceptions, mapping to typed domain errors — means the UI receives clean, typed results (`Success`/`Error` states) and doesn't need to know about `HttpException`/`IOException`/parsing. This decouples the UI from network details, centralizes error logic, and makes both layers simpler and more testable."},{t:"list",items:["**Boundary** — the repository translates network errors to domain errors.","**Clean UI** — receives typed results, not framework exceptions.","**Centralized** — one place for error mapping.","**Testable** — the UI tests use typed states; the repo tests cover mapping."]},{t:"note",text:"Handle errors in the repository (the data-layer boundary): catch exceptions and map to typed domain errors, so the UI gets clean Success/Error states without knowing HttpException/IOException/parsing. Decouples the UI from network details, centralizes error logic, and makes both layers simpler and testable."}]},{level:"senior",q:"How do you handle partial failures when loading multiple sources?",a:[{t:"p",text:"When a screen loads several independent things (profile, feed, ads) and one fails, don't fail the whole screen. Use `supervisorScope` + `async` so one failure doesn't cancel the others, wrap each in `runCatching`, and render the successful sections with an error/placeholder only for the failed one. This gives partial content instead of an all-or-nothing screen."},{t:"list",items:["**`supervisorScope` + `async`** — isolate each source's failure.","**`runCatching` per source** — a failure becomes a local error state.","**Partial rendering** — show successes, error only the failed section.","**vs `coroutineScope`** — that would fail the whole load."]},{t:"note",text:"For a screen loading independent sources, use supervisorScope + async so one failure doesn't cancel the rest, with runCatching per source turning a failure into a local error/placeholder — render successful sections and error only the failed one. Partial content beats an all-or-nothing screen (coroutineScope would fail everything)."}]},{level:"senior",q:"How do you propagate a global logout event across the app?",a:[{t:"p",text:"When auth fails irrecoverably (refresh failed), you need *one* logout regardless of which request triggered it. Emit a global event from an app-scoped source — a `SharedFlow`/`Channel` in a session/auth manager (singleton). The app (or a root observer) collects it, clears session data, and navigates to login. Using a shared event source avoids each failing request trying to log out separately."},{t:"code",title:"Global logout event",code:`object SessionManager {
    private val _events = MutableSharedFlow<SessionEvent>(extraBufferCapacity = 1)
    val events = _events.asSharedFlow()
    fun forceLogout() { _events.tryEmit(SessionEvent.LoggedOut) }
}
// App root collects events -> clear data, navigate to login`},{t:"list",items:["**App-scoped event source** — `SharedFlow`/`Channel` in a session manager.","**Single logout** — one event handled once, not per request.","**Root observer** — clears data, navigates to login.","**Triggered by** — failed refresh / irrecoverable 401."]},{t:"note",text:"For an app-wide logout (e.g. failed refresh), emit a global event from an app-scoped SharedFlow/Channel in a session manager; a root observer collects it once, clears session data, and navigates to login — avoiding each failing request logging out separately. One clean logout regardless of trigger."}]},{level:"junior",q:"How do you add auth to a multi-module app cleanly?",a:[{t:"p",text:"Keep auth concerns in a *network/auth module* that provides the configured `OkHttpClient` (with the auth interceptor + Authenticator) and a `TokenStore` interface. Feature modules depend on the API interfaces, not the auth wiring. The token store and session manager are singletons in the auth module, injected via DI — so every feature's requests are authenticated without each module handling auth."},{t:"list",items:["**Auth in one module** — client + interceptor + Authenticator + TokenStore.","**Feature modules** — depend on API interfaces, not auth wiring.","**DI** — singleton token store/session manager injected.","**Centralized** — auth logic in one place, reused everywhere."]},{t:"note",text:"Put auth in a network/auth module: it provides the configured OkHttpClient (auth interceptor + Authenticator) and a TokenStore interface, all singletons via DI. Feature modules depend on API interfaces, not the auth wiring — so every request is authenticated with auth logic centralized in one place."}]},{level:"senior",q:"How do you avoid leaking sensitive data through logs and errors?",a:[{t:"p",text:"Don't log request/response bodies or headers containing tokens/PII in production (gate `HttpLoggingInterceptor` behind `BuildConfig.DEBUG`, `redactHeader` for auth). Don't include raw server errors or tokens in crash reports/analytics. Sanitize error messages shown to users (no internal details). Store tokens encrypted. Treat any logged/reported string as potentially visible."},{t:"list",items:["**No body/header logging in prod** — gate on debug; redact auth headers.","**Sanitize crash reports** — no tokens/PII/raw server data.","**User-facing messages** — no internal details.","**Encrypt tokens** — and never log them."]},{t:"note",text:"Avoid leaking data: don't log bodies/headers with tokens/PII in production (gate logging on BuildConfig.DEBUG, redactHeader auth); keep tokens/PII/raw server data out of crash reports/analytics; sanitize user-facing messages; encrypt tokens and never log them. Treat any logged/reported string as potentially exposed."}]},{level:"senior",q:"How do you implement request deduplication for identical concurrent calls?",a:[{t:"p",text:"If multiple parts of the app request the *same* data simultaneously, dedupe so only one network call happens and all share the result. Use a shared in-flight cache keyed by request (a map of `Deferred`), or expose the data as a shared hot flow (`shareIn`) so collectors reuse one upstream. This avoids redundant calls (and is how a well-designed repository serves multiple observers)."},{t:"list",items:["**In-flight map** — key → shared `Deferred`; callers await the same one.","**Shared flow** — `shareIn`/`stateIn` so collectors reuse one upstream.","**Avoids redundant calls** — one fetch serves many callers.","**Repository role** — a good repository dedupes for its observers."]},{t:"note",text:"Dedupe identical concurrent requests: keep an in-flight map (key → shared Deferred) so callers await the same fetch, or expose data as a shared hot flow (shareIn/stateIn) so collectors reuse one upstream. One network call serves many callers — a well-designed repository does this for its observers."}]},{level:"senior",q:"What is refresh token rotation, and how do you handle it?",a:[{t:"p",text:"With *rotation*, each time you use a refresh token to get a new access token, the server *also issues a new refresh token* and *invalidates the old one*. This limits the damage of a stolen refresh token (it's single-use). The client must *store the new refresh token* atomically after each refresh — and if two refreshes race, one gets an invalid-token error, so you must serialize refresh (single-flight) to avoid invalidating your own token."},{t:"list",items:["**Rotation** — refresh returns a new refresh token; old one invalidated.","**Store the new one** — atomically after each refresh.","**Single-flight** — serialize refresh so races don't invalidate the token.","**Security** — a stolen refresh token is single-use."]},{t:"note",text:"Refresh token rotation issues a NEW refresh token on each refresh and invalidates the old (single-use — limits theft damage). The client must store the new token atomically; because a race could invalidate your own token, serialize refresh (single-flight with a Mutex). Handle 'invalid refresh token' by logging out."}]},{level:"senior",q:"How do you refresh tokens proactively and handle clock skew?",a:[{t:"p",text:"Rather than only reacting to 401s, you can *proactively* refresh when the access token is *about to expire* (check `expiresAt` before a request). To handle *clock skew* (the device clock differing from the server's), refresh a bit *early* (e.g. 60s before expiry) so a slightly-fast device clock doesn't send an already-expired token. Combine proactive refresh with the reactive 401 Authenticator as a fallback."},{t:"list",items:["**Proactive refresh** — refresh before expiry (check `expiresAt`).","**Skew buffer** — refresh early (e.g. 60s) to tolerate clock differences.","**Fallback** — the 401 Authenticator still catches unexpected expiry.","**Fewer 401s** — smoother UX than reacting after failure."]},{t:"note",text:"Proactively refresh when the access token is near expiry (check expiresAt), with an early buffer (~60s) to tolerate clock skew (a fast device clock could send an expired token). Keep the reactive 401 Authenticator as a fallback. Proactive refresh reduces 401s and smooths UX."}]},{level:"junior",q:"How do you bound the total time of a network operation?",a:[{t:"p",text:"Beyond OkHttp's per-phase timeouts, you can bound a whole *operation* (which might involve several calls or retries) with `withTimeout(duration) { }` in the coroutine — it cancels the block and throws `TimeoutCancellationException` if it overruns. Use `withTimeoutOrNull` to fall back to cached data instead of throwing. This guarantees the user isn't waiting indefinitely."},{t:"code",title:"Operation timeout",code:"val data = withTimeoutOrNull(5000) { repo.loadWithRetries() } ?: cachedData()"},{t:"list",items:["**OkHttp timeouts** — per connect/read/write/call.","**`withTimeout`** — bound a whole coroutine operation (multiple calls/retries).","**`withTimeoutOrNull`** — fall back to cache instead of throwing.","**Guarantee** — the user never waits indefinitely."]},{t:"note",text:"OkHttp bounds per-request phases; withTimeout(duration) { } bounds a whole coroutine operation (multiple calls/retries) — cancels + throws TimeoutCancellationException on overrun. withTimeoutOrNull falls back to cache instead of throwing. Guarantees the user isn't waiting indefinitely."}]},{level:"senior",q:"How do you handle SSL/TLS errors gracefully?",a:[{t:"p",text:"TLS errors (`SSLHandshakeException`, `SSLPeerUnverifiedException`) mean the secure connection couldn't be established or verified — often a proxy/MITM, an expired/invalid server cert, a clock issue, or a pinning mismatch. *Never* disable certificate validation to 'fix' it (that removes security). Surface a clear error ('Secure connection failed'), log for diagnosis, and check certificate/pinning configuration. For dev, use a debug-only network config, never in release."},{t:"list",items:["**TLS errors** — handshake/verification failures (MITM, bad cert, clock, pinning).","**Never disable validation** — it removes security (a major vulnerability).","**Surface clearly** — 'secure connection failed'; log for diagnosis.","**Debug-only config** — for dev servers; never trust-all in release."]},{t:"note",text:"TLS errors (SSLHandshakeException/SSLPeerUnverifiedException) = failed/unverified secure connection (MITM, bad/expired cert, clock skew, pinning mismatch). NEVER disable cert validation to 'fix' it (removes security). Surface a clear error, log, check cert/pinning config. Use debug-only network config for dev, never trust-all in release."}]},{level:"senior",q:"What is request signing (HMAC), and when is it used?",a:[{t:"p",text:"Request signing computes a cryptographic signature (e.g. HMAC-SHA256 over the method, path, body, timestamp, and a secret) and sends it in a header; the server recomputes it to verify the request wasn't tampered with and came from a legitimate client. It's used by some APIs (payments, AWS-style) for integrity/authenticity beyond bearer tokens. On mobile, protect the signing secret carefully (it's a client secret — inherently at some risk)."},{t:"list",items:["**HMAC signature** — over method/path/body/timestamp + secret.","**Server verifies** — integrity + authenticity (tamper detection).","**Timestamp** — prevents replay (with a nonce/short window).","**Secret protection** — a client-embedded secret is a risk; minimize exposure."]},{t:"note",text:"Request signing computes an HMAC (over method/path/body/timestamp + secret) sent in a header; the server recomputes to verify integrity/authenticity (tamper detection), with a timestamp/nonce against replay. Used by payment/AWS-style APIs beyond bearer tokens. Protect the signing secret — a client-embedded secret is inherently at some risk."}]},{level:"senior",q:"How do you handle a required app update triggered by the API?",a:[{t:"p",text:"When a backend introduces a breaking change, it may return a specific status/error (or a version-check endpoint) indicating the client is too old. The app detects this (via a response header/code or a config check) and shows a *force-update* screen linking to the Play Store, blocking further use. Combine with API versioning and Play *in-app updates* (immediate flow) for a smooth forced upgrade."},{t:"list",items:["**Version signal** — a status/header/config indicating min supported version.","**Force-update screen** — block use, link to Play Store.","**In-app updates** — Play's immediate update flow.","**API versioning** — keep old clients working until forced upgrade."]},{t:"note",text:"For a breaking API change, the backend signals the client is too old (a status/header or version-check/Remote Config). The app shows a force-update screen (link to Play, block use), ideally via Play In-App Updates (immediate flow). Combine with API versioning so old clients work until they must upgrade."}]},{level:"junior",q:"How do you cancel and clean up in-flight requests on logout?",a:[{t:"p",text:"On logout, cancel any in-flight work so authenticated requests don't complete (or update UI) after the session ends. Because requests run in scopes (`viewModelScope`), navigating away cancels them; for app-scoped work, cancel that scope or the specific jobs. Also clear the token so no new authenticated requests go out, and clear cached user data. Coroutine cancellation aborts the underlying OkHttp calls."},{t:"list",items:["**Cancel scopes/jobs** — abort in-flight authenticated requests.","**Clear the token** — stop new authenticated requests.","**Clear user data** — DB/cache/prefs.","**Coroutine cancellation** — aborts the underlying OkHttp calls."]},{t:"note",text:"On logout, cancel in-flight work (scope/job cancellation aborts the underlying OkHttp calls) so authenticated requests don't complete after the session ends, clear the token (no new authenticated requests), and wipe cached user data. viewModelScope work cancels on navigation; cancel app-scoped work explicitly."}]},{level:"senior",q:"What is graceful degradation for network features?",a:[{t:"p",text:"Graceful degradation means the app stays *useful* when the network is unavailable or degraded, rather than showing a dead screen. Serve cached data (offline-first), disable/queue actions that need connectivity (with clear messaging), show reduced functionality instead of errors, and recover automatically when connectivity returns. The principle: degrade the experience proportionally, don't fail completely."},{t:"list",items:["**Serve cache** — offline-first content stays available.","**Queue/disable network actions** — with clear messaging.","**Reduced functionality** — over dead screens/errors.","**Auto-recover** — resume when connectivity returns."]},{t:"note",text:"Graceful degradation keeps the app useful under poor/no network: serve cached data (offline-first), queue or disable connectivity-dependent actions with clear messaging, show reduced functionality instead of errors, and auto-recover when online. Degrade proportionally — don't fail completely."}]}],JA={id:"error-auth",title:"Error Handling & Auth",tagline:"Failure types, modeling errors as typed values, mapping errors to UX, token auth with Interceptor + Authenticator (refresh dedup), and retry strategy.",tags:["Networking","Error Handling","Auth","Token Refresh","Retry"],content:_A,qa:GA},YA=[{heading:"Ktor Client — the KMP networking library",blocks:[{t:"p",text:"**Ktor** is a Kotlin networking library from JetBrains. The **Ktor Client** is the relevant part for apps — it's an HTTP client written in pure Kotlin with coroutines, and crucially it's **multiplatform**: the same networking code runs on Android, iOS, desktop, and web. This is why Ktor is the standard networking choice for **Kotlin Multiplatform (KMP)** projects, where Retrofit (JVM/Android-only) can't be used in shared code."},{t:"code",title:"A Ktor client and a request",code:`val client = HttpClient(CIO) {                 // CIO = the engine (platform-specific)
    install(ContentNegotiation) {
        json(Json { ignoreUnknownKeys = true })  // kotlinx.serialization
    }
    install(HttpTimeout) { requestTimeoutMillis = 15_000 }
    install(Logging) { level = LogLevel.HEADERS }
    defaultRequest { url("https://api.example.com/") }
}

// Requests are suspend functions returning typed results
suspend fun getUser(id: String): UserDto =
    client.get("users/$id").body()            // auto-parsed via ContentNegotiation

suspend fun createUser(user: CreateUserRequest): UserDto =
    client.post("users") {
        contentType(ContentType.Application.Json)
        setBody(user)                          // serialized to JSON
    }.body()`},{t:"list",items:["**Engine + config**: a Ktor client has an *engine* (the actual HTTP implementation — `CIO`, `OkHttp`, `Darwin` for iOS, `Android`) and a set of installed *plugins* (features). The engine is platform-specific (chosen per target via expect/actual); the client code is shared.","**Plugins (features)**: `ContentNegotiation` (serialization, integrates kotlinx.serialization), `HttpTimeout`, `Logging`, `Auth` (token auth), `HttpRequestRetry` (retries), `HttpCache`. You install what you need — Ktor is modular.","**Coroutine-native**: all requests are `suspend` functions; responses are parsed with `.body<T>()`. No interface-generation like Retrofit — you write request functions directly."]}]},{heading:"Ktor vs Retrofit",blocks:[{t:"table",headers:["","Retrofit (+OkHttp)","Ktor Client"],rows:[["Platform","JVM/Android only","Multiplatform (Android, iOS, desktop, web)"],["API style","declarative interface + annotations","imperative request functions (DSL)"],["Underlying engine","OkHttp","pluggable (CIO, OkHttp, Darwin, ...)"],["Maturity on Android","very mature, huge ecosystem","growing; standard for KMP"],["Best for","Android-only apps","KMP / shared networking code"]]},{t:"list",items:["**Choose Retrofit** for Android-only apps — it's extremely mature, has a huge ecosystem, and the declarative interface style is concise for REST.","**Choose Ktor** for KMP projects, where networking must live in `commonMain` and run on iOS too — Retrofit can't. On an all-Android project Ktor is also fine but has less momentum.","**They share concepts**: both are coroutine-based, both integrate kotlinx.serialization, both support interceptors/plugins for auth/logging/retry — so the mental model transfers. Ktor can even use OkHttp as its engine on Android (getting OkHttp's connection pooling under Ktor's API).","**For an Android/KMP engineer**: know both — Retrofit for Android-specific code, Ktor for the shared KMP layer."]}]},{heading:"WebSockets — real-time bidirectional communication",blocks:[{t:"p",text:"HTTP is request/response — the client asks, the server answers, then the connection is done. For **real-time** features (chat, live updates, presence, live scores), that's inefficient: you'd have to poll (repeatedly ask 'anything new?'). A **WebSocket** is a *persistent, bidirectional* connection: after an initial handshake (which upgrades an HTTP connection), both client and server can send messages to each other *at any time* over the open connection, until one side closes it."},{t:"list",items:["**Persistent** — the connection stays open, unlike HTTP's one-shot request/response. No repeated connection setup.","**Bidirectional (full-duplex)** — the *server* can push messages to the client without the client asking (impossible with plain HTTP request/response). And the client can send anytime too.","**Efficient for real-time** — no polling overhead, low latency, instant delivery. Ideal for chat, live feeds, collaborative editing, notifications, live dashboards.","**Starts as HTTP** — a WebSocket connection begins with an HTTP request with an `Upgrade: websocket` header; the server agrees, and the connection 'upgrades' to the WebSocket protocol (`ws://` or secure `wss://`)."]}]},{heading:"WebSockets in practice — polling vs WebSockets vs SSE",blocks:[{t:"code",title:"A WebSocket with Ktor, exposed as a Flow",code:`fun observeMessages(): Flow<ChatMessage> = callbackFlow {
    client.webSocket(urlString = "wss://chat.example.com/ws") {
        try {
            for (frame in incoming) {                 // receive messages as they arrive
                if (frame is Frame.Text) {
                    trySend(Json.decodeFromString(frame.readText()))
                }
            }
        } finally { /* connection closed */ }
    }
    awaitClose { /* cleanup */ }
}`},{t:"table",headers:["Approach","How","Use when"],rows:[["Polling","repeatedly GET on a timer","simple, infrequent updates; wasteful and laggy for real-time"],["Long polling","hold a request open until data is ready","near-real-time without WebSockets; a middle ground"],["WebSockets","persistent bidirectional connection","true real-time, bidirectional (chat, live collab)"],["Server-Sent Events (SSE)","server pushes over a one-way HTTP stream","server→client only updates (live feed, notifications) — simpler than WS"]]},{t:"list",items:["**WebSockets vs polling**: polling wastes battery/data (constant requests, most returning 'nothing new') and adds latency (updates only arrive on the next poll). WebSockets push instantly with one connection. But WebSockets need connection management (reconnection, heartbeats).","**SSE (Server-Sent Events)** — a simpler alternative when you only need *server → client* pushing (a live feed, notifications). It's a one-way HTTP stream, easier to implement and more firewall-friendly than WebSockets, but not bidirectional.","**Connection management is the hard part**: WebSockets drop (network changes, backgrounding, server restarts), so you need *reconnection with backoff*, *heartbeats/pings* to detect dead connections, and handling of message ordering/buffering across reconnects. Expose the stream as a Flow (via `callbackFlow`) so the UI observes it cleanly.","**Both OkHttp and Ktor support WebSockets** — OkHttp has a `WebSocket` API with a listener; Ktor has `client.webSocket { }` with coroutine-based `incoming`/`send`."]},{t:"note",text:"Ktor = pure-Kotlin, multiplatform HTTP client (engine + plugins, coroutine-native) — the KMP networking standard where Retrofit (Android-only) can't go; use Retrofit for Android-only, Ktor for shared KMP code. WebSockets = persistent, bidirectional connection for real-time (chat, live updates) — the server can push, no polling; starts as an HTTP upgrade. Alternatives: polling (simple, wasteful), long polling (middle ground), SSE (server→client only, simpler). Key challenge: connection management (reconnect + backoff, heartbeats); expose as a Flow via callbackFlow."}]}],QA=[{level:"junior",q:"What is Ktor and when would you use it over Retrofit?",a:[{t:"p",text:"**Ktor is a Kotlin networking library from JetBrains; its client is a pure-Kotlin, coroutine-based HTTP client that is *multiplatform* — the same networking code runs on Android, iOS, desktop, and web.** That multiplatform capability is the main reason to choose it: in a **Kotlin Multiplatform (KMP)** project, networking code lives in shared `commonMain`, and Retrofit — which is JVM/Android-only — can't be used there, but Ktor can."},{t:"list",items:["**Use Retrofit** for Android-only apps — it's very mature, has a large ecosystem, and its declarative interface-with-annotations style is concise for REST.","**Use Ktor** for KMP projects where the networking layer must be shared and run on iOS too. It's the standard KMP networking choice.","They share concepts — both are coroutine-based, both integrate kotlinx.serialization, both support plugins/interceptors for auth/logging/retry — so knowledge transfers. Ktor can even use OkHttp as its engine on Android."]},{t:"p",text:"The difference in style: Retrofit generates an implementation from an annotated interface, while Ktor uses imperative request functions (`client.get(\"users/$id\").body()`). For an Android/KMP engineer, the practical answer is 'Retrofit for Android-specific code, Ktor for the shared multiplatform layer' — and knowing both is expected."}]},{level:"junior",q:"What is a WebSocket and how is it different from HTTP?",a:[{t:"p",text:"**A WebSocket is a persistent, bidirectional connection between client and server, used for real-time communication. It's different from HTTP's request/response model: with HTTP, the client asks and the server answers, then it's done; with a WebSocket, the connection stays open and *both sides can send messages at any time*.**"},{t:"list",items:["**Persistent** — the connection stays open (unlike HTTP's one-shot exchange), so there's no repeated connection setup for each message.","**Bidirectional (full-duplex)** — crucially, the *server can push messages to the client* without the client asking. Plain HTTP can't do this; the client always has to initiate.","**Real-time & efficient** — messages arrive instantly with no polling, low latency and overhead. Ideal for chat, live feeds, presence, collaborative editing, live scores.","**Starts as HTTP** — a WebSocket begins with an HTTP request carrying an `Upgrade: websocket` header; the server agrees and the connection 'upgrades' to the WebSocket protocol (`ws://` or secure `wss://`)."]},{t:"p",text:"The core distinction to state: HTTP is *pull* (client requests, server responds, connection closes), while a WebSocket is a *persistent two-way channel* where the server can *push*. That's what makes real-time features practical — without WebSockets you'd have to constantly poll ('anything new?'), which is wasteful and laggy. The trade-off is that WebSockets require connection management (reconnection, heartbeats), which HTTP's stateless model doesn't."}]},{level:"junior",q:"When would you use WebSockets versus polling?",a:[{t:"p",text:"**Use WebSockets for genuine real-time, frequent, or bidirectional updates; use polling for simple, infrequent updates where near-real-time is fine.** The trade-off is real-time efficiency vs implementation simplicity."},{t:"list",items:["**Polling** (repeatedly GET on a timer) is simple — just a normal HTTP request on a schedule — and fine for data that changes infrequently and where a delay is acceptable (checking for a status update every 30 seconds). But it's *wasteful* (most requests return 'nothing new', burning battery and data) and *laggy* (updates only arrive on the next poll interval). For anything needing instant updates, it's a poor fit.","**WebSockets** give *instant* delivery with a single persistent connection and support the server *pushing* to the client — essential for chat, live collaboration, live feeds, or anything where the server needs to notify the client immediately. The cost is complexity: you must manage the connection (reconnect on drops, heartbeats to detect dead connections)."]},{t:"p",text:"There are middle grounds worth knowing: **long polling** (the server holds a request open until it has data, then responds — near-real-time without full WebSockets) and **Server-Sent Events (SSE)** (the server pushes over a one-way HTTP stream — simpler than WebSockets and firewall-friendly, but *server→client only*, so good for live feeds/notifications but not bidirectional chat). The decision: infrequent updates + simplicity → polling; server→client real-time only → SSE; full bidirectional real-time → WebSockets. Don't reach for WebSockets' complexity if simple polling or SSE meets the need."}]},{level:"junior",q:"How do you consume a WebSocket stream in a Compose/ViewModel architecture?",a:[{t:"p",text:"**You wrap the WebSocket in a `callbackFlow` (or `channelFlow`) to expose incoming messages as a Kotlin `Flow`, which the ViewModel collects and turns into UI state — just like any other reactive data source.** This bridges the WebSocket's callback/streaming nature into the coroutine world so it fits cleanly into the standard architecture."},{t:"code",title:"WebSocket as a Flow",code:`fun observeMessages(): Flow<ChatMessage> = callbackFlow {
    client.webSocket("wss://chat.example.com/ws") {
        for (frame in incoming) {                       // suspends, receives frames
            if (frame is Frame.Text) trySend(parse(frame.readText()))
        }
    }
    awaitClose { /* close connection / cleanup */ }
}
// ViewModel collects it into StateFlow like any other Flow`},{t:"p",text:"Exposing it as a Flow means the rest of your app treats real-time messages the same as any observable data — the repository provides `Flow<ChatMessage>`, the ViewModel collects it (often accumulating into a `StateFlow<List<ChatMessage>>`), and the UI observes that state. The `callbackFlow` + `awaitClose` pattern ensures the WebSocket is properly closed when collection stops (the user leaves the screen), tying the connection's lifetime to the observer via structured concurrency. For sending messages, the repository exposes a suspend function that writes to the socket's `send`. The key idea is that WebSockets don't need a *special* architecture — bridge them to a Flow and they slot into the same unidirectional-data-flow pattern as everything else."}]},{level:"senior",q:"What are the challenges of managing a WebSocket connection on mobile, and how do you handle them?",a:[{t:"p",text:"**The core challenge is that mobile connections are *unstable and lifecycle-constrained* — networks change (Wi-Fi ↔ cellular), the app gets backgrounded, the device sleeps, servers restart — so a WebSocket *will* drop repeatedly, and naive code that assumes a stable persistent connection breaks. Robust WebSocket handling is mostly about connection lifecycle management, not the messaging itself.**"},{t:"list",items:["**Reconnection with backoff**: the connection drops, and you must automatically reconnect — but with *exponential backoff* (increasing delays) and *jitter*, so you don't hammer the server (especially if it's down and thousands of clients reconnect at once — the thundering herd). Cap the backoff at a maximum and keep retrying. On reconnect, re-establish any subscriptions/state the server needs.","**Heartbeats / ping-pong**: a dropped connection often *doesn't* raise an error immediately — the socket looks open but is dead (a 'half-open' connection). You send periodic pings and expect pongs; if a pong doesn't arrive in time, you consider the connection dead and reconnect. Without heartbeats, you'd silently stop receiving messages while thinking you're connected.","**Lifecycle awareness**: keeping a WebSocket open while the app is backgrounded wastes battery and the OS may kill it anyway. Typically you *close* the connection when the app backgrounds (or the screen is not visible) and *reopen* on foreground — tying the connection to `lifecycleScope`/`repeatOnLifecycle(STARTED)` or the Compose lifecycle. For truly background real-time (a chat needing to notify when closed), you'd use push notifications (FCM) instead of holding a socket open.","**Message ordering & gap handling across reconnects**: while disconnected, you may have missed messages. On reconnect, you need a strategy — the server sends a 'since timestamp/id' catch-up, or you re-fetch missed data via REST — so the user doesn't have holes in their message history. Give messages sequence numbers/ids to detect gaps and dedupe.","**Send buffering**: messages the user sends while momentarily disconnected should be queued and sent on reconnect (or shown as 'sending…' and retried), not silently dropped.","**Authentication on (re)connect**: the connection needs auth (a token in the handshake or a first message); on reconnect with a possibly-expired token, you may need to refresh the token before reconnecting."]},{t:"p",text:"**The senior framing**: a WebSocket on mobile is not 'open it once and read forever' — it's a *managed, self-healing connection* that must survive constant network changes and lifecycle events. The robust implementation wraps the socket in a layer that handles reconnection (backoff + jitter), liveness (heartbeats), lifecycle (connect/disconnect on foreground/background), and continuity (catch-up on reconnect, send queue, dedup). You expose the *result* as a clean Flow of messages to the app, hiding all that management. Underestimating the connection-management complexity — treating a WebSocket like a reliable pipe — is the classic mistake; on mobile, the management *is* the hard part, and for background delivery you often complement WebSockets with push notifications rather than trying to keep a socket alive indefinitely."}]},{level:"senior",q:"How does Ktor's engine/plugin architecture work, and why is it well-suited to KMP?",a:[{t:"p",text:"**Ktor Client separates the *engine* (the actual platform HTTP implementation) from the *client API and plugins* (the shared, platform-agnostic features). This separation is exactly what makes it multiplatform: the API and your networking code are written once in shared code, while the engine is swapped per platform via the `expect/actual` mechanism.**"},{t:"list",items:["**The engine** is the low-level HTTP implementation — how bytes actually go over the network. Ktor provides several: `CIO` (pure Kotlin, cross-platform), `OkHttp` and `Android` (JVM/Android), `Darwin` (iOS, using Apple's URLSession), `Js` (browser). You pick the appropriate engine per target (often via `expect/actual`), and the *rest of your code doesn't change* — it talks to the same `HttpClient` API regardless of engine.","**Plugins (features)** are modular, installable capabilities that live in the shared layer: `ContentNegotiation` (serialization with kotlinx.serialization — itself multiplatform), `Auth` (token auth with refresh), `Logging`, `HttpTimeout`, `HttpRequestRetry`, `HttpCache`, `WebSockets`. You `install()` the ones you need. They're implemented against the engine-agnostic API, so they work on every platform.","**Why this suits KMP**: the value proposition of KMP is 'write business/data logic once, run on Android and iOS'. Networking is a big part of the data layer, and Ktor lets the *entire* networking layer — request definitions, serialization, auth, retry, error handling — live in `commonMain` and be shared. Only the engine (a single line choosing the platform implementation) differs per target. Retrofit can't do this because it's fundamentally tied to the JVM (dynamic proxies, OkHttp), so it can never run on iOS.","**Pairs with kotlinx.serialization**: since Ktor's ContentNegotiation uses kotlinx.serialization (also multiplatform, compile-time, reflection-free), the whole serialize→request→parse pipeline is shared and works on Kotlin/Native (iOS) where reflection-based libraries like Gson/Moshi don't."]},{t:"p",text:"**The senior framing**: Ktor's engine/plugin split is a deliberate design for portability — abstract the *what* (the HTTP semantics, features, and your API code) away from the *how* (the platform-specific byte transport), so the former is shared and only the latter is platform-specific. This mirrors KMP's whole philosophy (`expect/actual` for platform specifics, shared code for everything else) and is why Ktor + kotlinx.serialization is *the* KMP networking stack. On an Android-only project you'd still likely pick Retrofit for its maturity, but the moment iOS shares the networking code, Ktor's architecture is what makes that possible."}]},{level:"junior",q:"How do you make a basic HTTP request with Ktor Client?",a:[{t:"p",text:"Create an `HttpClient` (with an engine and plugins), then call suspend request functions — `client.get(url)`, `client.post(url) { setBody(...) }` — and read the typed body with `.body<T>()` (using content negotiation). Ktor is coroutine-first, so requests are suspend functions."},{t:"code",title:"Ktor request",code:`val client = HttpClient(CIO) {
    install(ContentNegotiation) { json() }
}
suspend fun getUser(id: String): User = client.get("https://api.example.com/users/$id").body()
suspend fun create(u: User): User = client.post("https://api.example.com/users") {
    contentType(ContentType.Application.Json); setBody(u)
}.body()`},{t:"list",items:["**`HttpClient(engine) { plugins }`** — configure once.","**`get`/`post`/…** — suspend request functions.","**`.body<T>()`** — typed deserialization (ContentNegotiation).","**Coroutine-first** — all requests are suspend."]},{t:"note",text:"Ktor: create an HttpClient(engine) { install plugins }, call suspend functions (client.get/post { setBody }) and read .body<T>() via ContentNegotiation. It's coroutine-first (all requests suspend). Configure the client once and share it, like OkHttp."}]},{level:"senior",q:"How does Ktor's plugin (feature) architecture work?",a:[{t:"p",text:"Ktor Client is built from *plugins* installed on the `HttpClient` — each intercepts the request/response pipeline to add behavior: `ContentNegotiation` (serialization), `Logging`, `Auth` (bearer/refresh), `HttpTimeout`, `HttpRequestRetry`, `WebSockets`, `HttpCache`. You compose exactly the features you need. This modular design is lightweight and KMP-friendly (no Android-specific dependencies in the core)."},{t:"code",title:"Installing plugins",code:`HttpClient(CIO) {
    install(ContentNegotiation) { json() }
    install(Logging) { level = LogLevel.HEADERS }
    install(HttpTimeout) { requestTimeoutMillis = 30_000 }
    install(Auth) { bearer { loadTokens { BearerTokens(access, refresh) } } }
}`},{t:"list",items:["**Plugins** — intercept the pipeline to add features.","**Compose** — ContentNegotiation, Logging, Auth, Timeout, Retry, WebSockets.","**Modular** — install only what you need.","**KMP-friendly** — core has no platform dependencies."]},{t:"note",text:"Ktor Client composes plugins on the HttpClient, each intercepting the request/response pipeline: ContentNegotiation, Logging, Auth (bearer/refresh), HttpTimeout, HttpRequestRetry, WebSockets, HttpCache. Install exactly what you need — modular, lightweight, KMP-friendly (core has no platform deps)."}]},{level:"senior",q:"What are Ktor engines, and how do they relate to KMP?",a:[{t:"p",text:"A Ktor `HttpClient` needs an *engine* — the platform-specific HTTP implementation: `OkHttp`/`CIO`/`Android` on Android/JVM, `Darwin` on iOS, `Js` on web. The API is the same across engines, so your networking code is *shared* in KMP `commonMain` and each platform plugs in its engine. This is why Ktor is the go-to networking library for Kotlin Multiplatform."},{t:"list",items:["**Engine** — platform HTTP impl (OkHttp/CIO/Android, Darwin/iOS, Js).","**Same API** — engine-agnostic client code.","**KMP** — shared networking in commonMain; per-platform engine.","**Choice** — pick the engine per platform's strengths."]},{t:"note",text:"A Ktor HttpClient uses a platform engine (OkHttp/CIO/Android on JVM, Darwin on iOS, Js on web) behind a common API — so networking code is shared in KMP commonMain and each platform plugs in its engine. This engine abstraction is why Ktor is the standard KMP networking library."}]},{level:"senior",q:"How do you handle authentication and token refresh in Ktor?",a:[{t:"p",text:"Install the `Auth` plugin with `bearer { }`: `loadTokens` supplies the current access/refresh tokens, and `refreshTokens` is called automatically on a 401 to fetch new tokens (returning new `BearerTokens`). Ktor handles attaching the header and retrying — analogous to OkHttp's interceptor + Authenticator, but built-in and configured in one place."},{t:"code",title:"Ktor Auth",code:`install(Auth) {
    bearer {
        loadTokens { BearerTokens(store.access(), store.refresh()) }
        refreshTokens {   // called on 401
            val new = api.refresh(oldTokens?.refreshToken)
            store.save(new); BearerTokens(new.access, new.refresh)
        }
    }
}`},{t:"list",items:["**`Auth { bearer { } }`** — built-in bearer token support.","**`loadTokens`** — supply current tokens.","**`refreshTokens`** — auto-called on 401 to refresh.","**One place** — attaching + refreshing configured together."]},{t:"note",text:"Ktor's Auth plugin with bearer { }: loadTokens supplies current tokens, refreshTokens is auto-called on 401 to fetch new BearerTokens. Ktor attaches the header and retries — like OkHttp's interceptor + Authenticator but built-in and configured in one place."}]},{level:"senior",q:"How do you send and receive messages over a WebSocket in Ktor?",a:[{t:"p",text:"Install the `WebSockets` plugin, then open a session with `client.webSocket(url) { }`. Inside the session you `send(...)` frames and receive them by iterating `incoming` (a `ReceiveChannel<Frame>`) — reading `Frame.Text`/`Frame.Binary`. The session block is a coroutine, so it's structured and cancellable; leaving it closes the connection."},{t:"code",title:"WebSocket session",code:`client.webSocket("wss://example.com/chat") {
    send(Frame.Text("hello"))
    for (frame in incoming) {
        if (frame is Frame.Text) handle(frame.readText())
    }
}   // leaving the block closes the connection`},{t:"list",items:["**`install(WebSockets)`** + **`client.webSocket(url) { }`** — open a session.","**`send(Frame.Text/Binary)`** — send messages.","**`for (frame in incoming)`** — receive frames.","**Structured** — the session is a coroutine; leaving closes it."]},{t:"note",text:"Install WebSockets, open client.webSocket(url) { }: send(Frame.Text/Binary) to send, iterate incoming (ReceiveChannel<Frame>) to receive (readText/readBytes). The session is a coroutine (structured, cancellable) — leaving the block closes the connection."}]},{level:"senior",q:"How do you reconnect a WebSocket after a drop?",a:[{t:"p",text:"WebSockets drop on network changes, timeouts, or server restarts. Implement reconnection: wrap the session in a loop that, on disconnect/exception, waits with *exponential backoff + jitter* and reconnects — until cancelled. Re-authenticate and *re-subscribe* to any channels/topics on reconnect, and consider resuming from a last-received message id if the protocol supports it (to avoid missed messages)."},{t:"code",title:"Reconnect loop",code:`while (isActive) {
    try { client.webSocket(url) { collectMessages() } }
    catch (e: Exception) { /* connection dropped */ }
    delay(backoff()); backoff.increase()   // exponential + jitter
}`},{t:"list",items:["**Reconnect loop** — retry on drop with backoff + jitter.","**Re-auth + re-subscribe** — restore state on reconnect.","**Resume** — from a last message id if supported (avoid gaps).","**Cancellable** — stop reconnecting when the scope is cancelled."]},{t:"note",text:"WebSockets drop on network changes/timeouts/restarts. Reconnect: loop that on disconnect waits (exponential backoff + jitter) and reconnects until cancelled; re-authenticate and re-subscribe to channels on reconnect, and resume from a last-received message id if the protocol supports it (avoid missed messages)."}]},{level:"senior",q:"How do you keep a WebSocket alive (heartbeat/ping-pong)?",a:[{t:"p",text:"Idle connections can be dropped by intermediaries (NATs, proxies) or die silently. WebSockets use *ping/pong* control frames as a heartbeat — Ktor's `WebSockets` plugin can send periodic pings (`pingInterval`), and a missing pong signals a dead connection to trigger reconnection. Some apps also send app-level heartbeat messages. Heartbeats detect half-open connections the OS hasn't noticed."},{t:"code",title:"Ping interval",code:"install(WebSockets) { pingInterval = 20_000 }   // ping every 20s"},{t:"list",items:["**Ping/pong frames** — keep the connection alive and detect death.","**`pingInterval`** — Ktor sends periodic pings.","**Missing pong** — signals a dead connection → reconnect.","**App-level heartbeat** — some protocols add their own."]},{t:"note",text:"Idle WebSockets get dropped by intermediaries or die silently. Use ping/pong heartbeats — Ktor's WebSockets plugin sends periodic pings (pingInterval); a missing pong signals a dead connection to trigger reconnection. Detects half-open connections the OS hasn't noticed. Some protocols add app-level heartbeats."}]},{level:"junior",q:"What is the WebSocket handshake, and how does it upgrade from HTTP?",a:[{t:"p",text:"A WebSocket connection starts as an *HTTP request* with an `Upgrade: websocket` header (and `Connection: Upgrade`). If the server agrees (responding `101 Switching Protocols`), the same TCP connection is *upgraded* to the WebSocket protocol — a persistent, full-duplex channel. After the handshake, HTTP is gone; both sides send frames anytime. This is why WebSocket URLs use `ws://`/`wss://`."},{t:"list",items:["**Starts as HTTP** — `Upgrade: websocket` request.","**`101 Switching Protocols`** — server accepts the upgrade.","**Persistent full-duplex** — the connection becomes a WebSocket.","**`ws://`/`wss://`** — WebSocket (and secure) schemes."]},{t:"note",text:"A WebSocket begins as an HTTP request with Upgrade: websocket; the server responds 101 Switching Protocols, upgrading the same TCP connection to a persistent full-duplex WebSocket channel (HTTP is then gone — both sides send frames anytime). URLs use ws://wss://."}]},{level:"senior",q:"How do you expose a WebSocket stream as a Flow?",a:[{t:"p",text:"Wrap the WebSocket session in a `callbackFlow` (or `flow`) that opens the connection, emits each incoming message, and closes it in `awaitClose`. Combine with a reconnect loop so the Flow re-establishes the connection transparently. Expose it (often via `shareIn`) so the UI collects messages reactively, with lifecycle-aware collection stopping the socket when off-screen."},{t:"code",title:"WebSocket → Flow",code:`fun messages(): Flow<Message> = flow {
    client.webSocket(url) {
        for (frame in incoming) if (frame is Frame.Text) emit(parse(frame.readText()))
    }
}.retryWhen { _, _ -> delay(backoff()); true }   // reconnect
    .flowOn(Dispatchers.IO)`},{t:"list",items:["**`callbackFlow`/`flow`** — emit incoming messages; `awaitClose` cleanup.","**Reconnect** — `retryWhen`/loop re-establishes the connection.","**`shareIn`** — share one socket among collectors.","**Lifecycle-aware** — stop the socket when the UI is off-screen."]},{t:"note",text:"Wrap the WebSocket session in a callbackFlow/flow that emits incoming messages (awaitClose cleanup), with a reconnect loop (retryWhen + backoff). Expose via shareIn so the UI collects reactively; collect lifecycle-aware so the socket stops when off-screen (WhileSubscribed)."}]},{level:"junior",q:"What is Server-Sent Events (SSE), and how does it compare to WebSockets?",a:[{t:"p",text:"*Server-Sent Events* is a one-way (server→client) streaming protocol over plain HTTP — the server keeps the response open and pushes text events. It's simpler than WebSockets (no upgrade handshake, works over standard HTTP, auto-reconnect built in) but only *one direction* and text-only. Use SSE for server-push-only streams (notifications, live scores, progress); use WebSockets when you also need to send *to* the server."},{t:"list",items:["**SSE** — server→client only, over HTTP, text events, auto-reconnect.","**Simpler** — no upgrade handshake; standard HTTP infrastructure.","**One direction** — can't send to the server (use a separate POST).","**WebSocket** — when bidirectional communication is needed."]},{t:"note",text:"SSE is one-way server→client streaming over plain HTTP (text events, built-in auto-reconnect) — simpler than WebSockets (no handshake, standard HTTP) but single-direction and text-only. Use SSE for server-push-only (notifications/scores/progress); WebSockets when you also send to the server."}]},{level:"senior",q:"What are the mobile-specific challenges of maintaining a WebSocket?",a:[{t:"p",text:"Mobile connections are unstable: networks switch (Wi-Fi↔cellular), the app is backgrounded (the OS may kill the connection or the process), radios sleep (Doze), and battery/data are constrained. So a persistent WebSocket needs robust reconnection, must handle backgrounding (often closing the socket and relying on FCM push when backgrounded), and shouldn't hold the connection open needlessly (battery drain). Tie it to a lifecycle/foreground scope."},{t:"list",items:["**Network switches** — Wi-Fi↔cellular drops the connection; reconnect.","**Backgrounding/Doze** — OS may kill it; close on background, use FCM push.","**Battery/data** — a held socket drains; don't keep it open needlessly.","**Lifecycle-scoped** — connect while foreground/visible."]},{t:"note",text:"Mobile WebSocket challenges: network switches (Wi-Fi↔cellular) drop connections; backgrounding/Doze can kill the socket/process; battery/data constraints. So: robust reconnection, close on background and rely on FCM push, don't hold the socket needlessly (drain), and scope it to foreground/visible lifecycle."}]},{level:"junior",q:"How do you handle timeouts and retries in Ktor?",a:[{t:"p",text:"Install the `HttpTimeout` plugin (`requestTimeoutMillis`, `connectTimeoutMillis`, `socketTimeoutMillis`) to bound requests, and the `HttpRequestRetry` plugin to retry failed requests with a configurable policy (which responses/exceptions to retry, max retries, exponential delay). Both are composed on the client, so timeout/retry behavior is centralized rather than per-call."},{t:"code",title:"Timeout + retry plugins",code:`HttpClient(CIO) {
    install(HttpTimeout) { requestTimeoutMillis = 30_000; connectTimeoutMillis = 10_000 }
    install(HttpRequestRetry) {
        retryOnServerErrors(maxRetries = 3)
        exponentialDelay()
    }
}`},{t:"list",items:["**`HttpTimeout`** — request/connect/socket timeouts.","**`HttpRequestRetry`** — retry policy (which, how many, delay).","**`retryOnServerErrors` / `exponentialDelay`** — sensible defaults.","**Centralized** — configured on the client, not per call."]},{t:"note",text:"Ktor: install HttpTimeout (requestTimeoutMillis/connect/socket) to bound requests, and HttpRequestRetry (retryOnServerErrors, maxRetries, exponentialDelay) for retries — both composed on the client, centralizing timeout/retry behavior rather than per-call."}]},{level:"senior",q:"How do you handle backpressure with a high-frequency WebSocket stream?",a:[{t:"p",text:"If the server pushes messages faster than the UI can process, apply Flow backpressure: `conflate()` or `sample()` to show only the latest (live prices), `buffer()` to decouple and process all, or `collectLatest` to cancel stale processing. Process/parse messages off the main thread (`flowOn(Default)`), and batch UI updates. Don't update the UI per message at high frequency — it janks."},{t:"list",items:["**Latest-only** — `conflate`/`sample` for live values (prices).","**Process all** — `buffer` to decouple; parse off main.","**`collectLatest`** — cancel stale processing.","**Batch UI** — don't render per message at high frequency."]},{t:"note",text:"For a fast WebSocket stream, apply Flow backpressure: conflate/sample (latest-only — live prices), buffer (process all, decoupled), or collectLatest (cancel stale). Parse off the main thread (flowOn(Default)) and batch UI updates — don't render per message at high frequency (jank)."}]},{level:"senior",q:"How do you test WebSocket or Ktor networking code?",a:[{t:"p",text:"Ktor provides `MockEngine` — you supply a lambda that returns canned responses for requests, so you test the client's request building and response handling without a real server. For WebSockets, abstract the connection behind a repository interface with a fake that emits scripted messages, and test your reconnection/backpressure logic against it (with `runTest`)."},{t:"list",items:["**Ktor `MockEngine`** — canned responses; no real server.","**Abstract the socket** — a repository interface + fake stream.","**Test reconnection/backpressure** — against scripted messages.","**`runTest`** — deterministic coroutine testing."]},{t:"note",text:"Ktor: use MockEngine (return canned responses per request) to test request building/response handling without a server. For WebSockets, abstract the connection behind a repository interface with a fake emitting scripted messages, and test reconnection/backpressure logic against it under runTest."}]},{level:"junior",q:"What is a WebSocket frame, and what types are there?",a:[{t:"p",text:"WebSocket data is sent in *frames*. The main data frames are `Text` (UTF-8 strings, usually JSON) and `Binary` (raw bytes). There are also *control frames*: `Ping`/`Pong` (heartbeat) and `Close` (graceful shutdown with a status code/reason). In Ktor you match on `Frame.Text`/`Frame.Binary`/`Frame.Close` when reading `incoming`."},{t:"list",items:["**`Text`** — UTF-8 strings (JSON messages).","**`Binary`** — raw bytes (protobuf, files).","**`Ping`/`Pong`** — heartbeat control frames.","**`Close`** — graceful shutdown (status + reason)."]},{t:"note",text:"WebSocket data is framed: Text (UTF-8 strings, usually JSON) and Binary (raw bytes) data frames, plus control frames Ping/Pong (heartbeat) and Close (graceful shutdown with status/reason). In Ktor, match Frame.Text/Binary/Close on incoming."}]},{level:"senior",q:"How do you manage a single shared WebSocket connection across the app?",a:[{t:"p",text:"Wrap the connection in a *singleton repository* (app-scoped) that owns one `HttpClient` session, exposes incoming messages as a *shared hot flow* (`shareIn` with `WhileSubscribed`), and provides send/subscribe methods. All features use this one connection instead of each opening their own — saving battery and coordinating subscriptions. The connection lives while subscribed and reconnects transparently."},{t:"list",items:["**Singleton repository** — owns one connection.","**Shared hot flow** — `shareIn(WhileSubscribed)` for messages.","**One connection** — all features multiplex over it.","**Transparent reconnect** — inside the repository."]},{t:"note",text:"Own the WebSocket in an app-scoped singleton repository: one session, incoming messages exposed as a shared hot flow (shareIn WhileSubscribed), with send/subscribe methods. All features share one connection (saves battery, coordinates subscriptions); it lives while subscribed and reconnects transparently."}]},{level:"senior",q:"How do WebSockets interact with app backgrounding and Doze?",a:[{t:"p",text:"When the app is backgrounded or the device enters *Doze*, the OS restricts network access and may kill the connection or the process — so you *can't reliably keep a WebSocket open in the background*. The pattern: keep the socket only while foregrounded, close it on background, and use *FCM push* to wake the app or deliver messages when backgrounded. For must-stay-connected apps (calls), use a foreground service."},{t:"list",items:["**Background/Doze** — network restricted; connection likely killed.","**Foreground only** — keep the socket while visible; close on background.","**FCM push** — deliver/wake when backgrounded.","**Foreground service** — for must-stay-connected apps (calls)."]},{t:"note",text:"Backgrounding/Doze restrict network access and can kill the WebSocket/process — you can't reliably keep it open in the background. Keep the socket foreground-only (close on background) and use FCM push to deliver/wake when backgrounded; use a foreground service for must-stay-connected apps (calls)."}]},{level:"junior",q:"How do you handle content negotiation and serialization in Ktor?",a:[{t:"p",text:"Install the `ContentNegotiation` plugin with a serializer (`json()` using kotlinx.serialization) — then request/response bodies are automatically serialized/deserialized based on `Content-Type`. You call `.body<T>()` to get a typed result and `setBody(obj)` to send one; Ktor handles the JSON conversion. You can register multiple content types (JSON, XML) and Ktor picks by negotiation."},{t:"code",title:"ContentNegotiation",code:`HttpClient(CIO) {
    install(ContentNegotiation) {
        json(Json { ignoreUnknownKeys = true })
    }
}
val user: User = client.get(url).body()     // auto-deserialized`},{t:"list",items:["**`ContentNegotiation` + `json()`** — kotlinx.serialization integration.","**`.body<T>()` / `setBody(obj)`** — typed (de)serialization.","**By `Content-Type`** — negotiated automatically.","**Multiple formats** — register JSON/XML; Ktor picks."]},{t:"note",text:"Install ContentNegotiation with json() (kotlinx.serialization) — bodies auto-(de)serialize by Content-Type; call .body<T>() for typed results and setBody(obj) to send. Register multiple content types (JSON/XML) and Ktor negotiates. Configure the Json (ignoreUnknownKeys) as usual."}]},{level:"senior",q:"How do you handle message ordering and delivery guarantees over WebSockets?",a:[{t:"p",text:"WebSockets deliver messages *in order* over a single connection (TCP guarantees ordering), but a *reconnect* can create gaps (messages sent while disconnected are missed) unless the protocol supports resuming from a last-received id/sequence. For at-least-once delivery, the app acknowledges messages and the server resends unacknowledged ones; for exactly-once, dedupe by message id. Design the protocol for your delivery needs — WebSockets alone don't guarantee delivery across reconnects."},{t:"list",items:["**In-order on one connection** — TCP guarantees ordering.","**Reconnect gaps** — resume from a last id/sequence to avoid missing messages.","**At-least-once** — ack + server resend of unacked.","**Exactly-once** — dedupe by message id."]},{t:"note",text:"WebSockets deliver in order over one connection (TCP), but reconnects create gaps unless you resume from a last-received id/sequence. For at-least-once: ack messages + server resends unacked; for exactly-once: dedupe by message id. WebSockets alone don't guarantee delivery across reconnects — design the protocol."}]},{level:"junior",q:"What is the difference between Ktor Client and Ktor Server?",a:[{t:"p",text:"Ktor is *both* a client and a server framework. *Ktor Client* is the HTTP client you use in an app to *call* APIs (what mobile devs use, often in KMP). *Ktor Server* is a framework to *build* backend HTTP servers/APIs in Kotlin. They share concepts (plugins, coroutines) but are separate libraries — on Android you use Ktor Client."},{t:"list",items:["**Ktor Client** — call APIs from an app (mobile/KMP use).","**Ktor Server** — build backends/APIs in Kotlin.","**Shared concepts** — plugins, coroutine-based, pipelines.","**Android** — you use Ktor Client."]},{t:"note",text:"Ktor is both: Ktor Client (an HTTP client to call APIs — what mobile/KMP apps use) and Ktor Server (a framework to build backends in Kotlin). They share plugin/coroutine concepts but are separate libraries. On Android you use Ktor Client."}]},{level:"senior",q:"How do you manage the HttpClient lifecycle (sharing and closing)?",a:[{t:"p",text:"Creating an `HttpClient` is expensive (it holds an engine, connection pool, coroutine scope), so create *one shared instance* (a DI singleton) and reuse it — like sharing an OkHttpClient. It should live for the app's lifetime; call `client.close()` only when truly done (rarely in an app). Don't create a new client per request — that leaks resources and defeats connection pooling."},{t:"list",items:["**Expensive** — engine, connection pool, coroutine scope.","**Share one** — DI singleton, app-lifetime.","**`close()`** — only when truly done (rare in an app).","**Never per-request** — leaks resources, no pooling."]},{t:"note",text:"An HttpClient is expensive (engine, connection pool, coroutine scope) — create one shared DI singleton for the app's lifetime and reuse it (like OkHttpClient). Call close() only when truly done (rare in an app). Never create a client per request (leaks resources, defeats connection pooling)."}]},{level:"senior",q:"How do you gracefully close a WebSocket, and what are close codes?",a:[{t:"p",text:'To close cleanly, send a *Close frame* with a *status code* and reason (`close(CloseReason(CloseReason.Codes.NORMAL, "done"))`) — both sides then tear down the TCP connection. Standard codes include `1000` (normal), `1001` (going away), `1011` (server error). Reading a `Frame.Close` from `incoming` tells you the peer initiated closure. Always close in a `finally`/`awaitClose` so the connection isn\'t leaked.'},{t:"list",items:["**Close frame + code** — `close(CloseReason(NORMAL, ...))`.","**Codes** — 1000 normal, 1001 going away, 1011 server error.","**`Frame.Close`** — peer-initiated closure.","**Clean up** — close in finally/awaitClose to avoid leaks."]},{t:"note",text:'Close gracefully with a Close frame + status code (close(CloseReason(NORMAL, "done"))) — both sides tear down TCP. Codes: 1000 normal, 1001 going away, 1011 server error. A Frame.Close on incoming = peer-initiated close. Always close in finally/awaitClose to avoid leaking the connection.'}]},{level:"senior",q:"How do you multiplex multiple subscriptions over one WebSocket?",a:[{t:"p",text:"Instead of one connection per topic, use *one* WebSocket and a *message protocol* with a `type`/`channel`/`subscriptionId` field. The client sends `subscribe`/`unsubscribe` messages, and each incoming message carries its channel so you route it to the right subscriber (a `Flow` per topic, filtered from the shared stream). This saves connections/battery and is how real apps handle many live data streams over one socket."},{t:"list",items:["**One connection** — multiplex topics with a channel/id in each message.","**Subscribe/unsubscribe** — control messages manage topics.","**Route by channel** — filter the shared stream into per-topic flows.","**Efficient** — one socket vs many; less battery."]},{t:"note",text:"Multiplex over one WebSocket with a message protocol (type/channel/subscriptionId): the client sends subscribe/unsubscribe control messages, and each incoming message's channel routes it to the right per-topic Flow (filtered from the shared stream). One connection for many streams — saves connections/battery."}]},{level:"junior",q:"What is long polling, and how does it compare to WebSockets?",a:[{t:"p",text:"*Long polling* is an HTTP technique where the client makes a request and the server *holds it open* until it has data (or a timeout), then responds; the client immediately re-requests. It simulates push over plain HTTP without a persistent protocol — simpler and firewall-friendly, but higher overhead (repeated requests/headers) and latency than WebSockets. Use it as a fallback when WebSockets aren't available; prefer WebSockets/SSE for true real-time."},{t:"list",items:["**Long polling** — server holds the request open until data, then client re-requests.","**Simulates push** — over plain HTTP; firewall-friendly.","**Overhead** — repeated request/header cost, more latency.","**Fallback** — when WebSockets aren't available."]},{t:"note",text:"Long polling: the client requests, the server holds it open until data (or timeout), responds, and the client immediately re-requests — simulating push over plain HTTP (simple, firewall-friendly) but with higher overhead/latency than WebSockets. Use as a fallback; prefer WebSockets/SSE for true real-time."}]}],$A={id:"ktor-websockets",title:"Ktor & WebSockets",tagline:"Ktor Client & KMP networking, Ktor vs Retrofit, engine/plugin architecture, WebSockets for real-time, polling vs WS vs SSE, and connection management.",tags:["Ktor","KMP","WebSockets","Real-time","SSE"],content:YA,qa:QA},XA={id:"networking",name:"Networking",description:"Talking to servers — HTTP & REST fundamentals, Retrofit & OkHttp, JSON serialization, error handling & auth, and Ktor & WebSockets.",topics:[VA,BA,KA,JA,$A]},ZA=[{heading:"What dependency injection is",blocks:[{t:"p",text:"**Dependency injection (DI)** is a simple idea with a fancy name: instead of a class *creating* the objects it needs (its 'dependencies'), those objects are *given to it* from the outside — usually through its constructor. A class that needs a `Repository` doesn't do `val repo = Repository(Api(), Database())` inside itself; instead it declares `class ViewModel(private val repo: Repository)` and someone *provides* the repository when creating the ViewModel. The 'inversion' is that the class no longer controls *how* its dependencies are built — that control is inverted to the caller."},{t:"code",title:"Without DI vs with DI",code:`// WITHOUT DI — the class creates its own dependencies (tightly coupled)
class UserViewModel {
    private val repository = UserRepository(     // hardcoded construction
        RetrofitApi(OkHttpClient()),
        AppDatabase.getInstance()
    )
}

// WITH DI — dependencies are provided from outside (loosely coupled)
class UserViewModel(
    private val repository: UserRepository,      // just declares what it needs
)
// Someone else decides how to build the repository and passes it in.`},{t:"list",items:["**The core move**: 'don't create your dependencies, ask for them'. The class declares *what* it needs; something external decides *how* to build and supply it.","**Constructor injection** is the primary and preferred form — dependencies come through the constructor. (Field/setter injection exists but is inferior — see later.)","DI is a *pattern*, not a library. You can do it *manually* (pass dependencies by hand). Libraries like Hilt, Dagger, and Koin *automate* it, but the concept is independent of any tool."]}]},{heading:"Why DI matters — the benefits",blocks:[{t:"list",items:["**Testability** — the biggest reason. If a class creates its own dependencies, tests are stuck with the real ones (real network, real database). With DI, a test *injects fakes/mocks* — a fake repository instead of a real one — so you can test the class in isolation, fast and deterministically. DI is what makes unit testing practical.","**Loose coupling / flexibility** — a class depends on an *abstraction* (an interface) that's injected, not a concrete implementation it hardcodes. You can swap implementations (a real repository, a fake, a different backend) without changing the class. This enables the dependency-inversion principle.","**Single responsibility** — a class focused on its actual job shouldn't also be responsible for constructing a whole graph of dependencies. DI removes construction concerns, keeping classes focused.","**Reusability** — a class that receives its dependencies works in any context that can provide them; one that hardcodes them is stuck.","**Centralized, controlled object creation** — the wiring of what-depends-on-what lives in one place (the DI setup / composition root), making the app's structure explicit and changeable."]},{t:"note",text:"If asked 'why use DI?', lead with *testability* — it's the most concrete, undeniable benefit. A class that `new`s its dependencies can't have those dependencies replaced in tests, so you're forced into slow, flaky integration tests. DI lets you inject fakes and unit-test cleanly. The other benefits (loose coupling, SRP, flexibility) follow from the same 'don't create, receive' principle."}]},{heading:"DI and the dependency inversion principle",blocks:[{t:"p",text:"DI is closely tied to the **Dependency Inversion Principle** (the 'D' in SOLID): high-level modules should depend on *abstractions*, not concrete low-level details. DI is the *mechanism* that delivers this — by injecting an interface, the high-level class depends on the abstraction, and the concrete implementation is supplied from outside."},{t:"code",title:"Injecting an abstraction",code:`// The ViewModel depends on the INTERFACE, not a concrete class
class UserViewModel(private val repository: UserRepository)  // UserRepository is an interface

interface UserRepository { suspend fun getUser(id: String): User }

class UserRepositoryImpl(...) : UserRepository { ... }        // real implementation
class FakeUserRepository(...) : UserRepository { ... }        // test implementation

// DI binds the interface to an implementation — production uses the real one,
// tests inject the fake. The ViewModel never changes.`},{t:"list",items:["**Depend on interfaces, inject implementations**: the class references the abstraction (`UserRepository`); DI supplies the concrete type (`UserRepositoryImpl` in production, `FakeUserRepository` in tests).","This is what makes the whole app *substitutable and testable* — the essence of clean architecture's dependency rule, realized through DI.","Don't over-abstract: not every class needs an interface. Add an interface when you genuinely need substitutability (across modules, for testing a boundary, for multiple implementations). A concrete class can be injected directly and faked with an open class or a test double if needed."]}]},{heading:"Manual DI vs a DI framework",blocks:[{t:"p",text:"You can do DI entirely by hand — construct objects and pass them down. For a small app this is fine and has zero dependencies. But as the app grows, manual wiring becomes tedious and error-prone: a deep object graph means threading dependencies through many constructors, managing singletons and scopes manually, and updating lots of construction code when dependencies change. This is why DI *frameworks* exist."},{t:"code",title:"Manual DI — a composition root",code:`// A container that builds the object graph by hand (manual DI)
class AppContainer(context: Context) {
    private val api = RetrofitApi(OkHttpClient())
    private val db = AppDatabase.build(context)
    val userRepository: UserRepository = UserRepositoryImpl(api, db.userDao())
    // ViewModels get their dependencies from here
}`},{t:"list",items:["**Manual DI** — you write the wiring yourself in a 'container' / 'composition root'. Pros: no library, full control, easy to understand. Cons: boilerplate grows with the app, manual scope/singleton management, error-prone as the graph deepens.","**DI frameworks** automate the wiring: you *declare* how to provide each dependency, and the framework generates or resolves the graph. **Dagger/Hilt** do it at *compile time* (code generation, verified at build); **Koin** does it at *runtime* (a service-locator-style registry). More on these in the following topics.","**When to use which**: manual DI is genuinely fine for small apps and is a good way to understand the pattern. For medium-to-large apps (the typical professional context), a framework (Hilt on Android) pays off by eliminating the boilerplate and catching wiring errors — Hilt at compile time, in particular, turns 'missing dependency' into a build error rather than a runtime crash."]}]},{heading:"Types of injection",blocks:[{t:"table",headers:["Type","How","Verdict"],rows:[["Constructor injection","dependencies passed via the constructor","preferred — immutable, explicit, guaranteed set"],["Field/property injection","dependencies set on fields after construction","needed for framework-created classes (Activities); otherwise avoid"],["Method/setter injection","dependencies passed to a method/setter","rare; for optional or reconfigurable dependencies"]]},{t:"list",items:["**Constructor injection is preferred**: dependencies are `val` (immutable), the object is fully-formed and valid the moment it's constructed (no half-initialized state), and its needs are explicit in the signature. Prefer it always when you control construction.","**Field injection** is a compromise for classes *you don't construct* — Android creates Activities/Fragments, so you can't inject via their constructor; instead the framework injects into annotated fields *after* creation (`@Inject lateinit var`). It's necessary there but inferior generally (mutable, can be accessed before injection, hidden dependencies).","**The rule**: constructor injection everywhere you can; field injection only for framework-instantiated entry points (Activities, Fragments, Services) where the constructor isn't yours to control."]}]}],eC=[{level:"junior",q:"What is dependency injection?",a:[{t:"p",text:"**Dependency injection is a pattern where a class receives the objects it needs (its dependencies) from the outside, instead of creating them itself.** Rather than a ViewModel doing `val repo = Repository(...)` internally, it declares `class ViewModel(private val repo: Repository)` and something external provides the repository when constructing it. The 'inversion' is that the class no longer controls *how* its dependencies are built — that responsibility moves to whoever creates the class."},{t:"code",title:"The essence",code:`// Not DI — creates its own dependency
class A { private val b = B() }

// DI — receives its dependency
class A(private val b: B)   // someone passes B in`},{t:"p",text:"The simplest form is *constructor injection* — dependencies come through the constructor. DI is a *pattern*, not a specific library: you can do it manually (pass objects by hand) or use a framework (Hilt, Dagger, Koin) to automate the wiring. The whole idea reduces to 'don't create your dependencies, ask for them' — which sounds trivial but has big benefits, chiefly making classes testable (you can inject fakes) and loosely coupled (you can swap implementations)."}]},{level:"junior",q:"Why use dependency injection? What are the benefits?",a:[{t:"p",text:"**The headline benefit is testability.** If a class creates its own dependencies (`val repo = RealRepository(realApi, realDb)`), you're stuck with those real objects in tests — real network calls, real database — which makes tests slow, flaky, and hard to control. With DI, a test injects *fakes* (a fake repository returning canned data), so you can test the class in isolation, fast and deterministically. DI is essentially what makes unit testing practical."},{t:"list",items:["**Testability** — inject fakes/mocks instead of real dependencies, enabling fast isolated unit tests.","**Loose coupling** — the class depends on an abstraction (interface) that's injected, so you can swap implementations without changing the class.","**Single responsibility** — the class focuses on its job, not on constructing a graph of dependencies.","**Flexibility & reuse** — a class that receives its dependencies works in any context that can provide them; different builds (debug/release, different backends) inject different implementations.","**Centralized wiring** — the 'what depends on what' lives in one place, making the app's structure explicit and easy to change."]},{t:"p",text:"When answering this, lead with testability because it's the most concrete and undeniable — a class that `new`s its dependencies literally cannot have them replaced in a test. The other benefits (coupling, SRP, flexibility) all flow from the same principle of receiving rather than creating dependencies."}]},{level:"junior",q:"What is the difference between constructor injection and field injection, and which is preferred?",a:[{t:"p",text:"**Constructor injection passes dependencies through the constructor; field injection sets them on fields *after* the object is created. Constructor injection is strongly preferred.**"},{t:"list",items:["**Constructor injection** (`class A(private val b: B)`): dependencies are provided at construction. Benefits — they can be `val` (immutable), the object is *fully valid the moment it's created* (no half-initialized state), and the dependencies are *explicit* in the signature (you can see exactly what the class needs). This is the ideal.","**Field injection** (`@Inject lateinit var b: B`): the dependency is injected into a field after the object exists. Downsides — the field must be mutable (`var`), there's a window where it's accessed before being set (crash risk), and dependencies are *hidden* (not visible in the constructor signature)."]},{t:"p",text:"**Why field injection exists at all**: some classes you *don't construct yourself* — Android instantiates Activities, Fragments, and Services via the framework, so you can't inject through their constructors. For those, field injection is the only option: the DI framework injects into annotated fields after the framework creates the object. So the rule is: **use constructor injection everywhere you control construction (ViewModels, repositories, use cases — the vast majority of your classes), and field injection only for framework-created entry points** where the constructor isn't yours. In Hilt, this is exactly the split — `@Inject constructor(...)` for your classes, and `@AndroidEntryPoint` + `@Inject lateinit var` for Activities/Fragments."}]},{level:"junior",q:"Is dependency injection the same as a DI framework like Hilt?",a:[{t:"p",text:"**No — dependency injection is a *pattern*, and Hilt/Dagger/Koin are *tools that automate* that pattern. You can do DI with no framework at all.** The pattern is just 'pass dependencies in from outside instead of creating them internally'. You can implement it *manually* by constructing objects and passing them down through constructors yourself."},{t:"code",title:"Manual DI — no framework needed",code:`// A composition root that wires things by hand
val api = RetrofitApi()
val repository = UserRepositoryImpl(api)
val viewModel = UserViewModel(repository)   // manual DI`},{t:"p",text:"For a small app, manual DI is perfectly fine and has zero library overhead. The reason frameworks exist is that as an app grows, wiring by hand becomes tedious and error-prone — deep dependency graphs mean threading objects through many constructors, and you must manually manage singletons and scopes. Frameworks *automate the wiring*: you declare how to provide each dependency and the framework builds the graph. Dagger/Hilt do this at *compile time* (generating code, so missing dependencies are build errors); Koin does it at *runtime* (a registry). So the distinction to make in an interview: DI is the concept (receive, don't create), and Hilt is one implementation that generates the boilerplate for you — understanding the concept independent of the tool is what matters."}]},{level:"senior",q:"How does dependency injection relate to the Dependency Inversion Principle and clean architecture?",a:[{t:"p",text:"**Dependency injection is the *mechanism* that implements the Dependency Inversion Principle (the 'D' in SOLID), which states that high-level modules should depend on *abstractions*, not on concrete low-level details. DI delivers this by injecting an interface into the high-level class while supplying the concrete implementation from outside.**"},{t:"code",title:"Inversion via injected abstraction",code:`// Domain (high-level) defines and depends on the ABSTRACTION
interface UserRepository { suspend fun getUser(id: String): User }
class GetUserUseCase(private val repo: UserRepository)   // depends on interface

// Data (low-level) provides the IMPLEMENTATION
class UserRepositoryImpl(...) : UserRepository { ... }

// DI binds interface -> impl. The high-level code never names the low-level class.`},{t:"list",items:["**The 'inversion'**: normally high-level code would depend on low-level code (a use case directly creating a Retrofit-backed repository). DIP inverts this — the high-level domain defines the *interface* it needs, and the low-level data layer *implements* that interface. Now the dependency arrow points *toward* the abstraction, not toward the concrete detail. DI is what wires the implementation to the interface at runtime without the high-level code ever referencing the concrete class.","**Why it matters for clean architecture**: this is *exactly* the dependency rule — dependencies point inward toward the domain, which knows nothing about the outer layers. The domain defines repository *ports* (interfaces); the data layer provides *adapters* (implementations); DI connects them. Without DI you couldn't achieve this cleanly — something has to supply the concrete implementation to the abstraction-dependent code, and that something is the DI container/composition root, which lives at the outermost layer.","**Testability falls out of it**: because the high-level code depends on an interface, tests inject a fake implementation. So DIP + DI simultaneously give you clean layering *and* testability — the same structural choice serves both.","**The composition root**: the one place that knows all the concrete types and wires them (the DI module setup, or a manual container) sits at the app's outer edge — it's allowed to depend on everything, precisely so the inner layers don't have to."]},{t:"p",text:"**The senior framing**: DIP is the *principle* ('depend on abstractions'), and DI is the *technique* that realizes it ('inject the abstraction, supply the implementation externally'). Together they're the backbone of clean architecture — the domain defines interfaces, outer layers implement them, and DI wires it all at the composition root, giving you an app where high-level policy is independent of low-level detail, layers are substitutable, and everything is testable. That's why DI isn't just a convenience for reducing boilerplate — it's the enabling mechanism for the whole layered, testable architecture."}]},{level:"senior",q:"When is manual DI appropriate versus adopting a framework like Hilt, and what are the trade-offs?",a:[{t:"p",text:"**Manual DI (wiring dependencies by hand in a composition root) is appropriate for small apps, learning, and codebases where the object graph is shallow; a framework becomes worth it as the graph grows deep and scope/singleton management gets complex. The trade-off is boilerplate/complexity now (manual) versus a learning curve and build-time cost (framework) that pays off at scale.**"},{t:"list",items:["**Manual DI — pros**: zero dependencies, full transparency (you can read exactly how everything is wired), no framework to learn, no build-time codegen cost, complete control. It's genuinely fine for small apps and is the best way to *understand* DI (frameworks hide the mechanics).","**Manual DI — cons**: boilerplate grows with the app. A deep graph means threading dependencies through many layers of constructors; you manually manage singletons (ensuring one instance) and scopes (per-screen vs app-lifetime); and every dependency change ripples through construction code. Wiring mistakes are runtime problems, and it becomes a maintenance burden at scale.","**Framework (Hilt) — pros**: automates the wiring (declare how to provide each type, framework builds the graph), manages scopes/singletons declaratively, and — for Dagger/Hilt specifically — verifies the graph *at compile time*, turning 'missing or cyclic dependency' into a *build error* rather than a runtime crash. It integrates with Android components (ViewModels, Activities, WorkManager) out of the box. At scale this eliminates enormous boilerplate.","**Framework — cons**: a learning curve (Dagger/Hilt concepts — components, scopes, modules, qualifiers — are non-trivial), added build time from annotation processing (KSP mitigates this), some 'magic' that obscures the wiring, and cryptic error messages when the graph is wrong."]},{t:"list",items:["**The decision heuristic**: for a small app or prototype, manual DI is often the right, simplest choice. For a professional medium-to-large app — especially multi-module, with ViewModels, background workers, and varied scopes — a framework (Hilt is the Android standard) pays for itself by removing boilerplate and catching wiring errors at build time. For KMP shared code, Koin is common (it's multiplatform; Hilt is Android-only).","**A nuance**: 'framework' isn't binary with 'manual' — you can start manual and adopt Hilt later, or use lightweight Koin (runtime, less boilerplate, easier learning curve) as a middle ground between hand-wiring and Dagger's compile-time rigor."]},{t:"p",text:"**The senior framing**: the choice is about *scale and team*, not dogma. Manual DI's cost is linear boilerplate and manual scope management, which is acceptable when small and painful when large. A compile-time framework like Hilt trades a learning curve and build cost for automated wiring and build-time correctness guarantees — a great deal for a real app, overkill for a tiny one. Being able to articulate *why* (graph depth, scope complexity, compile-time verification, team size) rather than reflexively reaching for Hilt everywhere is the mature position."}]},{level:"junior",q:"What are the three types of injection (constructor, field, method)?",a:[{t:"p",text:"Dependencies can be provided three ways: *constructor injection* (passed as constructor parameters — the preferred way), *field injection* (set directly on a field, used for framework-created classes like Activities where you can't control the constructor), and *method/setter injection* (passed to a method — least common). Constructor injection is preferred because dependencies are explicit, the object is fully-formed and immutable, and it's easy to test."},{t:"code",title:"Three ways",code:`class A(private val repo: Repo)           // constructor (preferred)
class MyActivity { @Inject lateinit var repo: Repo }   // field (framework classes)
class C { fun setRepo(repo: Repo) { } }   // method/setter (rare)`},{t:"list",items:["**Constructor** — deps as params; explicit, immutable, testable (preferred).","**Field** — set on a field; for Activities/Fragments you don't construct.","**Method/setter** — passed to a method; least common.","**Prefer constructor** — fully-formed objects, easy to test."]},{t:"note",text:"Constructor injection (deps as params — explicit, immutable, testable; preferred), field injection (set on a field — for framework-created classes like Activities), method/setter injection (rare). Prefer constructor injection; use field injection only where you can't control construction."}]},{level:"junior",q:"What is Inversion of Control (IoC)?",a:[{t:"p",text:"Inversion of Control is the principle that an object shouldn't *create* its own dependencies — instead, control of creation/wiring is 'inverted' to an external party (a DI container or the caller). DI is one way to implement IoC. Instead of `class A { val b = B() }` (A controls B's creation), A receives B from outside — decoupling A from *how* B is made."},{t:"list",items:["**IoC** — objects don't create their dependencies; an external party does.","**DI implements IoC** — dependencies are provided, not constructed internally.","**Decoupling** — A doesn't know how B is built.","**'Don't call us, we'll call you'** — the framework wires things."]},{t:"note",text:"Inversion of Control: an object doesn't create its own dependencies — control of creation/wiring is inverted to an external party (container/caller). DI is one implementation of IoC. It decouples a class from HOW its dependencies are built, improving flexibility and testability."}]},{level:"junior",q:"What is a dependency graph?",a:[{t:"p",text:"A dependency graph is the network of objects and their dependencies — a `ViewModel` depends on a `Repository`, which depends on an `ApiService` and a `Dao`, etc. A DI framework *builds this graph* by resolving each type's dependencies transitively, so requesting a `ViewModel` automatically constructs everything it needs in the right order. The graph must be acyclic (no circular dependencies)."},{t:"list",items:["**Objects + dependencies** — who needs what, transitively.","**Framework resolves it** — builds the whole graph from the root request.","**Transitive** — requesting one type constructs its entire dependency tree.","**Acyclic** — circular dependencies aren't allowed."]},{t:"note",text:"A dependency graph is the network of objects and their (transitive) dependencies (ViewModel → Repository → Api + Dao). A DI framework builds it by resolving each type's dependencies, so requesting the root constructs everything it needs in order. It must be acyclic (no circular dependencies)."}]},{level:"senior",q:"What is the difference between a service locator and dependency injection?",a:[{t:"p",text:"With *dependency injection*, dependencies are *pushed* into an object (via its constructor) — the object declares what it needs and receives it, staying unaware of the container. With a *service locator*, the object *pulls* dependencies by asking a global locator (`ServiceLocator.get<Repo>()`) — which hides dependencies (they're not in the signature), couples code to the locator, and is harder to test. DI is generally preferred."},{t:"list",items:["**DI** — dependencies pushed in (constructor); explicit; testable.","**Service locator** — object pulls from a global registry; hidden deps.","**Hidden dependencies** — locator deps aren't in the signature.","**Coupling** — service locator couples to the locator; DI doesn't."]},{t:"note",text:"DI pushes dependencies into an object (constructor) — explicit, testable, container-unaware. A service locator has the object pull deps from a global registry (ServiceLocator.get()) — hidden dependencies (not in the signature), coupling to the locator, harder to test. DI is generally preferred."}]},{level:"senior",q:"What is a circular dependency, and how do you break it?",a:[{t:"p",text:"A circular dependency is when A depends on B and B depends on A (directly or transitively) — the graph has a cycle, so neither can be constructed first (DI frameworks error at compile/build time). Break it by: introducing an interface/abstraction, using *lazy* injection (`Lazy<T>`/`Provider<T>`) to defer one side, extracting the shared logic into a third component, or rethinking the design (a cycle often signals a responsibility problem)."},{t:"list",items:["**Cycle** — A↔B; can't construct either first (build error).","**Lazy/Provider** — defer one dependency's creation.","**Extract** — pull shared logic into a third class.","**Rethink** — a cycle often signals mixed responsibilities."]},{t:"note",text:"A circular dependency (A↔B) makes the graph cyclic — neither can be built first (DI errors at build time). Break it with lazy/Provider injection (defer one side), an interface, extracting shared logic to a third class, or redesigning (a cycle usually signals a responsibility problem)."}]},{level:"senior",q:"How do you inject two different implementations of the same type?",a:[{t:"p",text:'Use *qualifiers* (named bindings) to disambiguate — annotate each binding and injection point with a qualifier so the framework knows which one you want. In Dagger/Hilt, `@Named("...")` or custom `@Qualifier` annotations; in Koin, named definitions. Without a qualifier, two bindings of the same type are ambiguous and error.'},{t:"code",title:"Qualifiers",code:`@Qualifier annotation class Authenticated
@Qualifier annotation class Public

@Provides @Authenticated fun authClient(): OkHttpClient = ...
@Provides @Public fun publicClient(): OkHttpClient = ...

class Repo(@Authenticated private val client: OkHttpClient)`},{t:"list",items:["**Qualifiers** — `@Named`/custom `@Qualifier` distinguish same-typed bindings.","**On both ends** — the binding and the injection point.","**Custom qualifiers** — type-safe (better than string `@Named`).","**Koin** — named definitions serve the same purpose."]},{t:"note",text:'Use qualifiers to inject two implementations of the same type: @Named("x") or custom @Qualifier annotations (Dagger/Hilt) on both the binding and injection point (Koin: named definitions). Custom qualifiers are more type-safe than string @Named. Without one, same-typed bindings are ambiguous.'}]},{level:"senior",q:"What is the difference between compile-time and runtime DI?",a:[{t:"p",text:"*Compile-time* DI (Dagger/Hilt) generates the wiring code at build time — errors (missing bindings, cycles) are caught *at compilation*, and there's *no runtime reflection* (fast, small). *Runtime* DI (Koin, older frameworks) resolves the graph *at runtime* — simpler setup, no code generation/build cost, but errors surface *when the app runs* and there's some runtime overhead. Trade compile-time safety/performance vs setup simplicity."},{t:"table",headers:["","Compile-time (Dagger/Hilt)","Runtime (Koin)"],rows:[["Errors caught","at compile","at runtime"],["Reflection","none (codegen)","minimal/DSL"],["Build cost","annotation processing","low"],["Setup","more ceremony","simpler"]]},{t:"list",items:["**Compile-time** — codegen; errors at build; no reflection; fast.","**Runtime** — resolve at runtime; simpler; errors at run; some overhead.","**Dagger/Hilt** — compile-time; **Koin** — runtime.","**Trade-off** — safety/performance vs simplicity/build speed."]},{t:"note",text:"Compile-time DI (Dagger/Hilt): generates wiring at build time — errors (missing bindings/cycles) caught at compilation, no runtime reflection (fast). Runtime DI (Koin): resolves at runtime — simpler, no codegen build cost, but errors surface at runtime with some overhead. Safety/performance vs setup simplicity."}]},{level:"junior",q:"What is scope/lifetime in dependency injection?",a:[{t:"p",text:"Scope controls *how long* a dependency lives and *how many* instances exist. A *singleton* scope creates one instance shared for the container's lifetime; an *unscoped/factory* binding creates a *new* instance each time it's requested; *lifecycle scopes* (Activity, ViewModel) tie an instance to a component's lifetime. Choosing the right scope prevents leaks (over-scoping) and inconsistency (under-scoping shared state)."},{t:"list",items:["**Singleton** — one instance for the container's lifetime.","**Factory/unscoped** — new instance per request.","**Lifecycle scopes** — Activity/ViewModel-scoped instances.","**Right scope** — avoid leaks (over-scope) and inconsistency (under-scope)."]},{t:"note",text:"Scope controls a dependency's lifetime and instance count: singleton (one shared for the container lifetime), factory/unscoped (new per request), lifecycle scopes (Activity/ViewModel-tied). Choose the right scope — over-scoping leaks, under-scoping loses shared state."}]},{level:"senior",q:"What is lazy or provider injection, and when do you need it?",a:[{t:"p",text:"Sometimes you don't want a dependency created *eagerly* at construction. `Lazy<T>` defers creation until first accessed (`lazy.get()`); `Provider<T>` (Dagger) returns a *new* instance each `get()` call (or a fresh scoped one). Use them for expensive dependencies not always needed, to break circular dependencies, or when you need multiple instances over time."},{t:"code",title:"Lazy / Provider",code:`class A(private val heavy: Lazy<HeavyThing>) {   // created on first use
    fun use() = heavy.get().doWork()
}
class B(private val factory: Provider<Item>) {   // new instance per get()
    fun make() = factory.get()
}`},{t:"list",items:["**`Lazy<T>`** — deferred creation on first access.","**`Provider<T>`** — new instance per `get()`.","**Uses** — expensive/rarely-used deps, breaking cycles, multiple instances.","**Avoids eager construction** — of the whole graph upfront."]},{t:"note",text:"Lazy<T> defers a dependency's creation until first access; Provider<T> (Dagger) returns a new instance per get(). Use them for expensive/rarely-needed deps, breaking circular dependencies, or getting multiple instances over time — avoiding eager construction of the whole graph."}]},{level:"senior",q:"What is assisted injection, and what problem does it solve?",a:[{t:"p",text:"Assisted injection handles the case where *some* constructor parameters come from the DI graph but *others* are only known at *runtime* (e.g. an id passed from the UI). You annotate the runtime params `@Assisted` and generate a factory (`@AssistedFactory`) that takes those params and supplies the injected ones. This is common for ViewModels needing a runtime argument (though `SavedStateHandle` often covers it)."},{t:"code",title:"Assisted injection",code:`class DetailViewModel @AssistedInject constructor(
    private val repo: Repo,                 // from DI
    @Assisted private val itemId: String,   // from runtime
)
@AssistedFactory interface Factory { fun create(itemId: String): DetailViewModel }`},{t:"list",items:["**`@Assisted`** — runtime-provided params.","**`@AssistedFactory`** — a generated factory taking those params.","**Mixes DI + runtime** — graph deps + runtime args.","**Uses** — ViewModels/objects needing a runtime id."]},{t:"note",text:"Assisted injection mixes DI-provided and runtime params: mark runtime ones @Assisted and generate an @AssistedFactory that takes them and supplies the injected deps. For objects/ViewModels needing a runtime argument (an id). (Hilt ViewModels often use SavedStateHandle for the id instead.)"}]},{level:"junior",q:"How does dependency injection improve testability?",a:[{t:"p",text:"Because dependencies are *injected* (not created internally), tests can pass *fakes/mocks* instead of the real ones — a `ViewModel` constructed with a fake repository, so you test its logic without a real network/DB. Without DI, the class creates its own dependencies and you can't substitute them, forcing slow/flaky integration tests. DI makes units *isolatable*."},{t:"code",title:"Injecting a fake",code:`// Production: MyViewModel(realRepo)
// Test:
val vm = MyViewModel(FakeRepo(cannedData))   // no real network/DB
vm.load()
assertEquals(expected, vm.state.value)`},{t:"list",items:["**Substitute fakes** — inject test doubles instead of real deps.","**Isolate the unit** — test logic without network/DB.","**Fast/deterministic** — no real I/O.","**Without DI** — internal creation blocks substitution."]},{t:"note",text:"DI lets tests inject fakes/mocks instead of real dependencies — construct a ViewModel with a FakeRepo to test its logic without real network/DB (fast, deterministic, isolated). Without DI, a class creates its own dependencies and you can't substitute them, forcing slow/flaky integration tests."}]},{level:"senior",q:"What is the 'too many constructor parameters' smell, and what does it indicate?",a:[{t:"p",text:"When a class needs *many* injected dependencies (say 6+), it's often a sign the class has *too many responsibilities* (violating the Single Responsibility Principle). The fix is usually to *split* the class or *group* related dependencies into a cohesive collaborator, not to work around it (e.g. via a service locator). DI surfaces this smell clearly — a long constructor is a design signal."},{t:"list",items:["**Many deps** — often too many responsibilities (SRP violation).","**Fix by splitting** — smaller classes / grouping related deps into a collaborator.","**Don't hide it** — service locators mask the smell, don't fix it.","**DI surfaces it** — the constructor makes coupling visible."]},{t:"note",text:"A constructor with many (6+) injected dependencies signals too many responsibilities (SRP violation). Fix by splitting the class or grouping related dependencies into a cohesive collaborator — don't hide it with a service locator. DI makes this smell visible via the constructor."}]},{level:"junior",q:"How does DI enable programming to interfaces?",a:[{t:"p",text:"DI lets you depend on *abstractions* (interfaces) and inject the *concrete* implementation — a `ViewModel` depends on a `Repository` interface, and DI binds it to `RepositoryImpl`. This decouples the consumer from the implementation, so you can swap implementations (real vs fake, or different strategies) without changing the consumer. It's the practical enabler of the Dependency Inversion Principle."},{t:"code",title:"Interface binding",code:`interface Repository { suspend fun load(): Data }
class RepositoryImpl(...) : Repository
// DI binds Repository -> RepositoryImpl
class MyViewModel(private val repo: Repository)   // depends on the interface`},{t:"list",items:["**Depend on interfaces** — the consumer knows only the abstraction.","**Inject the impl** — DI binds interface → implementation.","**Swappable** — real/fake/alternative implementations.","**Enables DIP** — high-level code doesn't depend on low-level details."]},{t:"note",text:"DI lets you depend on interfaces and inject the concrete implementation (Repository → RepositoryImpl) — decoupling the consumer from the impl so you can swap (real/fake/alternative) without changing it. It's the practical enabler of the Dependency Inversion Principle (@Binds does this efficiently in Dagger)."}]},{level:"junior",q:"What is a DI container or component, conceptually?",a:[{t:"p",text:"A DI container (Dagger 'component', Koin 'module'/container) is the object that *holds the bindings* (how to create each type) and *resolves the graph* when you request something. It knows the recipes for all dependencies and constructs them (respecting scopes) on demand. Your code asks the container for a type; the container builds and returns it with all its dependencies wired."},{t:"list",items:["**Holds bindings** — recipes for creating each type.","**Resolves the graph** — constructs a type + its dependencies on request.","**Respects scopes** — reuses singletons, creates factories.","**Dagger Component / Koin container** — the concrete forms."]},{t:"note",text:"A DI container/component holds the bindings (how to create each type) and resolves the graph on request — constructing a type with all its dependencies wired, respecting scopes (singletons reused, factories new). Dagger calls it a Component; Koin has modules/a container."}]},{level:"senior",q:"What is eager versus lazy initialization of dependencies?",a:[{t:"p",text:"*Eager* initialization creates a dependency immediately (e.g. at app startup / when the component is built) — ready to use but adds to startup cost. *Lazy* initialization defers creation until first use — faster startup, but a small cost on first access. Balance them: eagerly init things needed immediately (a crash reporter), lazily init expensive things used later (a heavy parser)."},{t:"list",items:["**Eager** — created upfront; ready but adds startup cost.","**Lazy** — created on first use; faster startup.","**Balance** — eager for immediately-needed, lazy for expensive/later.","**Startup impact** — too much eager init slows cold start."]},{t:"note",text:"Eager init creates a dependency immediately (ready, but adds startup cost); lazy init defers to first use (faster startup, small first-access cost). Balance: eager for immediately-needed deps (crash reporter), lazy for expensive/later ones (heavy parser). Too much eager init slows cold start."}]},{level:"senior",q:"How does dependency injection support modularization?",a:[{t:"p",text:"In a multi-module app, DI lets modules depend on *interfaces* defined in shared/api modules while implementations live in separate modules — so feature modules don't depend on each other's internals. The DI framework wires the concrete implementations at the app level. This keeps modules decoupled, enables replacing implementations, and supports independent development/testing."},{t:"list",items:["**Interface in api module** — implementation in a separate module.","**App wires concretes** — the DI graph assembled at the top.","**Decoupled modules** — features depend on abstractions, not each other.","**Swappable/testable** — replace implementations per module."]},{t:"note",text:"DI supports modularization: modules depend on interfaces (in shared/api modules) while implementations live in separate modules, and the DI framework wires concretes at the app level. Keeps feature modules decoupled (depend on abstractions, not each other's internals), swappable, and independently testable."}]},{level:"junior",q:"What is the difference between DI and the factory pattern?",a:[{t:"p",text:"A *factory* is a class/method whose job is to *create* objects (encapsulating construction logic). *DI* is broader — it's about *providing* dependencies to objects (which may use factories internally). DI frameworks often *generate* factories for you. You still use explicit factories for runtime-parameterized creation (assisted injection) or complex construction; DI handles the wiring of the graph."},{t:"list",items:["**Factory** — creates objects; encapsulates construction.","**DI** — provides dependencies (may use factories internally).","**DI generates factories** — for you (Dagger).","**Explicit factories** — for runtime args / complex creation."]},{t:"note",text:"A factory creates objects (encapsulating construction logic); DI is broader — providing dependencies to objects (and DI frameworks often generate factories). Use explicit factories for runtime-parameterized/complex creation (assisted injection); DI wires the overall graph. They complement each other."}]},{level:"junior",q:"How do you inject dependencies into framework-created classes like Activities?",a:[{t:"p",text:"You don't construct Activities/Fragments/Services yourself (the system does), so you can't use constructor injection. Instead use *field injection*: annotate fields `@Inject` and have the framework populate them (Hilt does this automatically for `@AndroidEntryPoint`-annotated classes). The DI framework injects into the instance the system created."},{t:"code",title:"Field injection into an Activity",code:`@AndroidEntryPoint
class MyActivity : AppCompatActivity() {
    @Inject lateinit var analytics: Analytics   // populated by Hilt
}`},{t:"list",items:["**Framework constructs them** — can't use constructor injection.","**Field injection** — `@Inject lateinit var`.","**Hilt `@AndroidEntryPoint`** — auto-injects Android classes.","**ViewModels** — constructor-injected (Hilt handles the factory)."]},{t:"note",text:"Activities/Fragments/Services are system-constructed, so use field injection (@Inject lateinit var) — Hilt populates them for @AndroidEntryPoint classes. ViewModels are the exception: constructor-injected (Hilt provides the factory via @HiltViewModel)."}]},{level:"senior",q:"What is manual DI, and what does it look like?",a:[{t:"p",text:"Manual DI means wiring the graph *by hand* — a container class (or the Application) that constructs dependencies and passes them down, with no framework. It's fine for small apps: explicit, no build cost, no learning curve. But it becomes tedious/error-prone as the graph grows (scopes, lifecycles, many dependencies) — which is when a framework (Hilt/Koin) pays off."},{t:"code",title:"Manual container",code:`class AppContainer(context: Context) {
    private val db = Room.databaseBuilder(...).build()
    private val api = Retrofit.Builder()...create(Api::class.java)
    val userRepository = UserRepository(api, db.userDao())
}
// Application holds AppContainer; screens pull from it`},{t:"list",items:["**Hand-wired** — a container/Application constructs and passes deps.","**No framework** — explicit, no build cost/learning curve.","**Good for small apps** — clear and simple.","**Scales poorly** — scopes/lifecycles/many deps get tedious → use a framework."]},{t:"note",text:"Manual DI wires the graph by hand — a container class (or Application) constructs dependencies and passes them down, no framework. Explicit, no build cost, fine for small apps. It gets tedious/error-prone with scopes, lifecycles, and many dependencies — where Hilt/Koin pays off."}]},{level:"senior",q:"What is the singleton anti-pattern, and how does a DI singleton scope differ?",a:[{t:"p",text:"The *singleton anti-pattern* is a globally-accessible `object`/static instance that classes reach out to directly — it hides dependencies, couples code globally, and is hard to test/replace. A *DI singleton scope* achieves one shared instance but *injects* it (explicit in constructors) and lets you *swap* it in tests — keeping the single-instance benefit without the global-access downsides."},{t:"list",items:["**Singleton anti-pattern** — global object; hidden deps, global coupling, untestable.","**DI singleton scope** — one shared instance, but injected and swappable.","**Explicit** — appears in constructors, not hidden global access.","**Testable** — provide a fake for the scoped instance."]},{t:"note",text:"The singleton anti-pattern is a global object classes reach out to directly — hidden dependencies, global coupling, hard to test/replace. A DI singleton scope gives one shared instance but injects it (explicit in constructors) and lets tests swap it — single-instance benefit without global-access downsides."}]},{level:"junior",q:"What is the overhead or cost of using a DI framework?",a:[{t:"p",text:"Costs vary: *compile-time* frameworks (Dagger/Hilt) add *build time* (annotation processing/KSP) and a learning curve, but *no runtime reflection* (fast at runtime, small). *Runtime* frameworks (Koin) add slight *runtime* overhead and defer error detection to runtime, but faster builds. Both add some conceptual complexity. For most non-trivial apps the maintainability/testability benefits outweigh the costs."},{t:"list",items:["**Dagger/Hilt** — build-time cost (annotation processing), learning curve; fast runtime.","**Koin** — slight runtime overhead, runtime error detection; fast builds.","**Complexity** — a concept/setup cost either way.","**Worth it** — for non-trivial apps (maintainability/testability)."]},{t:"note",text:"DI framework costs: Dagger/Hilt add build time (annotation processing/KSP) + a learning curve but no runtime reflection (fast); Koin adds slight runtime overhead and defers errors to runtime but builds faster. Both add conceptual complexity. For non-trivial apps the maintainability/testability benefits outweigh the cost."}]},{level:"junior",q:"Why is field injection generally discouraged compared to constructor injection?",a:[{t:"p",text:"Field injection hides dependencies (they're not in the constructor signature — you can construct the object incompletely), requires mutable `lateinit var` fields (risking `UninitializedPropertyAccessException`), and makes testing harder (you must reflectively set fields or use the framework). Constructor injection makes dependencies explicit, produces fully-formed immutable objects, and is trivially testable — so it's preferred everywhere you control construction."},{t:"list",items:["**Hidden deps** — not in the constructor signature.","**Mutable `lateinit`** — risk of uninitialized access.","**Harder to test** — can't just pass fakes to a constructor.","**Constructor injection** — explicit, immutable, testable (prefer it)."]},{t:"note",text:"Field injection hides dependencies (not in the signature — incomplete construction possible), needs mutable lateinit vars (UninitializedPropertyAccessException risk), and complicates testing. Constructor injection is explicit, immutable, and trivially testable — prefer it wherever you control construction (field injection only for framework classes)."}]},{level:"senior",q:"How does DI relate to the Dependency Inversion Principle?",a:[{t:"p",text:"The Dependency Inversion Principle (the 'D' in SOLID) says high-level modules should depend on *abstractions*, not concrete low-level details, and abstractions shouldn't depend on details. DI is the *mechanism* that realizes this: high-level code declares a dependency on an interface, and DI injects the concrete implementation — so the high-level module never references the concrete class. DIP is the principle; DI is a way to apply it."},{t:"list",items:["**DIP** — depend on abstractions, not concretions.","**DI realizes it** — inject the concrete for a declared interface.","**High-level decoupled** — never references low-level classes.","**Principle vs mechanism** — DIP is the goal; DI is the tool."]},{t:"note",text:"The Dependency Inversion Principle: high-level modules depend on abstractions, not concrete details. DI is the mechanism that realizes it — high-level code declares a dependency on an interface, DI injects the concrete implementation, so it never references the concrete class. DIP is the principle; DI applies it."}]},{level:"senior",q:"What is a multibinding (injecting a set or map of implementations)?",a:[{t:"p",text:"Multibindings let you contribute multiple implementations to a *collection* injected as a `Set<T>` or `Map<K, T>` — each module adds bindings with `@IntoSet`/`@IntoMap`, and a consumer injects the whole set/map. It's ideal for plugin-style architectures: many modules register handlers/initializers, and a coordinator injects and iterates them without knowing each one."},{t:"code",title:"Multibinding",code:`@Binds @IntoSet fun bindA(a: HandlerA): Handler
@Binds @IntoSet fun bindB(b: HandlerB): Handler
class Dispatcher(private val handlers: Set<@JvmSuppressWildcards Handler>)   // all of them`},{t:"list",items:["**`@IntoSet`/`@IntoMap`** — contribute to a collection.","**Inject `Set<T>`/`Map<K,T>`** — the aggregated implementations.","**Plugin architecture** — modules register; a coordinator iterates.","**Decoupled** — the consumer doesn't know each contributor."]},{t:"note",text:"Multibindings contribute multiple implementations to a Set<T>/Map<K,T> via @IntoSet/@IntoMap; a consumer injects the whole collection. Great for plugin architectures — many modules register handlers/initializers and a coordinator injects and iterates them without knowing each one."}]}],tC={id:"fundamentals",title:"DI Fundamentals & Concepts",tagline:"What DI is, why it matters (testability first), dependency inversion, constructor vs field injection, and manual DI vs a framework.",tags:["DI","Constructor Injection","Dependency Inversion","Testability"],content:ZA,qa:eC},aC=[{heading:"What Dagger is and its defining trait",blocks:[{t:"p",text:"**Dagger** is a dependency injection framework that generates the wiring code for you — but its defining trait is that it does so **at compile time**, using annotation processing. You annotate your classes and 'recipes' for providing dependencies, and Dagger *generates* the code that constructs your object graph *during the build*. This is fundamentally different from runtime DI (like Koin): if a dependency is missing or the graph has a cycle, you get a **compile error**, not a runtime crash — the correctness of your dependency graph is verified at build time."},{t:"list",items:["**Compile-time = safety + performance**: the graph is validated when you build (missing dependencies fail the build), and there's no runtime reflection (the generated code is plain, fast constructor calls).","**Hilt is built on Dagger**: Hilt (the recommended Android DI) is a *layer on top of Dagger* that removes most of Dagger's boilerplate and provides Android-specific integration. Understanding Dagger's concepts explains what Hilt generates for you — which is exactly what interviewers probe.","**The trade-off**: Dagger has a notoriously steep learning curve and can produce cryptic error messages, which is precisely why Hilt was created to simplify it."]}]},{heading:"The core annotations",blocks:[{t:"table",headers:["Annotation","Meaning"],rows:[["`@Inject`","on a constructor: 'Dagger, you can create this class'; on a field: 'inject here'"],["`@Module`","a class that tells Dagger *how* to provide types it can't construct itself"],["`@Provides`","a method inside a module that returns an instance of a type"],["`@Binds`","a more efficient way to bind an interface to its implementation"],["`@Component`","the bridge that connects modules to where dependencies are injected"],["`@Singleton` / scopes","mark a dependency to be reused (one instance) within a scope"]]},{t:"code",title:"@Inject — Dagger can construct this",code:`// @Inject constructor tells Dagger how to CREATE this class,
// and what its dependencies are (which Dagger will also provide)
class UserRepository @Inject constructor(
    private val api: UserApi,        // Dagger provides these
    private val dao: UserDao,
)`},{t:"p",text:"When a class has an `@Inject constructor`, Dagger knows how to build it *and* what it depends on — so it recursively provides those dependencies too. This is the ideal case: no extra configuration needed, Dagger figures out the whole chain. You only need modules for types Dagger *can't* construct via `@Inject constructor`."}]},{heading:"Modules — providing types Dagger can't construct",blocks:[{t:"p",text:"Some types you *can't* add an `@Inject constructor` to — interfaces (you can't construct an interface), classes from libraries (Retrofit, OkHttp, Room — you don't own their source), or types needing custom construction logic. For these, you write a **`@Module`** with **`@Provides`** methods that tell Dagger how to build them."},{t:"code",title:"@Provides for library types",code:`@Module
class NetworkModule {
    @Provides
    @Singleton
    fun provideOkHttp(): OkHttpClient = OkHttpClient.Builder().build()

    @Provides
    @Singleton
    fun provideRetrofit(client: OkHttpClient): Retrofit =   // Dagger passes the OkHttpClient
        Retrofit.Builder().baseUrl("...").client(client).build()

    @Provides
    fun provideUserApi(retrofit: Retrofit): UserApi = retrofit.create(UserApi::class.java)
}`},{t:"list",items:["**`@Provides`** — a method that *returns* an instance. Its parameters are *its* dependencies, which Dagger supplies (note `provideRetrofit` takes an `OkHttpClient`, which Dagger gets from `provideOkHttp`). This is how you build a chain of library objects.","**`@Binds`** — a more efficient alternative for the common 'bind interface to implementation' case. Instead of a `@Provides` method that just returns the impl, `@Binds` is an *abstract* method that tells Dagger 'when someone asks for the interface, give them this implementation' — Dagger generates less code for it.","**Why `@Binds` over `@Provides` for interfaces**: `@Provides` requires a full method body and generates a factory; `@Binds` is a declaration with no body (abstract), so Dagger produces more efficient generated code. Use `@Binds` for interface→impl bindings, `@Provides` when you need construction logic."]},{t:"code",title:"@Binds — bind interface to implementation efficiently",code:`@Module
abstract class RepositoryModule {
    @Binds
    abstract fun bindUserRepository(impl: UserRepositoryImpl): UserRepository
    // 'when someone needs UserRepository, provide UserRepositoryImpl'
}`}]},{heading:"Components — connecting the graph",blocks:[{t:"p",text:"A **`@Component`** is the central piece that ties everything together: it lists the modules (sources of dependencies) and declares where dependencies can be injected. Dagger generates the actual implementation of the component — the code that constructs and wires the whole graph. In plain Dagger, you create and manage components yourself; **this is the tedious part that Hilt automates** (Hilt provides pre-made components tied to Android lifecycles)."},{t:"code",title:"A Dagger component",code:`@Singleton
@Component(modules = [NetworkModule::class, RepositoryModule::class])
interface AppComponent {
    fun inject(activity: MainActivity)      // where to inject
    fun userRepository(): UserRepository    // or expose a dependency
}

// You build and hold it (usually in Application):
val appComponent = DaggerAppComponent.create()
appComponent.inject(mainActivity)`},{t:"list",items:["The component is the *composition root* — it knows all the modules and generates the graph. `DaggerAppComponent` is the generated implementation.","**Subcomponents / scoped components** create scoped sub-graphs (a per-activity or per-fragment graph inheriting from the app graph) — how Dagger models different lifetimes. Managing this hierarchy manually is complex, which is the main thing Hilt removes.","**What Hilt does with all this**: Hilt provides the standard components (`SingletonComponent`, `ActivityComponent`, `ViewModelComponent`, etc.) already defined and wired to Android lifecycles, so you never write `@Component` or `DaggerAppComponent` — you just add modules with `@InstallIn(...)`. That's the boilerplate Hilt eliminates."]}]},{heading:"Scopes — controlling instance lifetime",blocks:[{t:"p",text:"A **scope** annotation tells Dagger to *reuse* a single instance of a dependency within a component's lifetime, rather than creating a new one each time it's requested. `@Singleton` is the app-wide scope; you can define custom scopes (`@ActivityScope`) for narrower lifetimes."},{t:"list",items:["**Unscoped (default)**: Dagger creates a *new instance every time* the dependency is requested. Fine for cheap, stateless objects.","**Scoped (`@Singleton`, custom scopes)**: Dagger creates *one instance* and reuses it for all requests within that component's lifetime. Use for expensive-to-create or stateful objects that should be shared (a database, a Retrofit instance, a repository).","**Scope must match the component**: a `@Singleton` dependency lives in the singleton (app) component; an activity-scoped one lives in the activity component. A scoped binding in the wrong component is an error.","**Don't over-scope**: making everything `@Singleton` keeps objects alive for the whole app even when not needed (memory), and can cause issues with stateful objects being shared unexpectedly. Scope deliberately — singletons for genuinely app-lived shared resources, narrower scopes (or unscoped) otherwise."]},{t:"note",text:"Dagger essentials: a compile-time DI framework (graph verified at build → missing deps are build errors, no reflection). @Inject constructor = 'Dagger can build this'; @Module + @Provides = how to build types Dagger can't (libraries, interfaces); @Binds = efficient interface→impl binding; @Component = the graph connector (composition root); scopes (@Singleton) control instance reuse within a component. Hilt is built on Dagger and generates the components/wiring for you — knowing Dagger's pieces explains what Hilt does under the hood."}]}],iC=[{level:"junior",q:"What is Dagger and how is it different from a runtime DI framework?",a:[{t:"p",text:"**Dagger is a dependency injection framework that generates the wiring code *at compile time* using annotation processing.** You annotate your classes and provider recipes, and during the build Dagger generates the code that constructs your object graph. This compile-time nature is its defining trait and the main difference from a runtime DI framework like Koin."},{t:"list",items:["**Compile-time verification**: because Dagger builds the graph at compile time, a missing dependency or a cycle is a *build error*, not a runtime crash. You find out something's wrong when you build, not when a user hits that screen.","**No reflection**: Dagger generates plain code (factory classes, direct constructor calls), so there's no runtime reflection overhead. Koin, by contrast, resolves dependencies at runtime via a registry.","**The trade-off**: this safety and performance come at the cost of a steep learning curve and sometimes cryptic error messages, plus added build time from annotation processing."]},{t:"p",text:"It's worth knowing that **Hilt is built on top of Dagger** — Hilt is Google's recommended Android DI, and it's essentially Dagger with the boilerplate removed and Android integration added. So understanding Dagger's concepts (`@Inject`, `@Module`, `@Provides`, `@Component`, scopes) explains what Hilt is doing under the hood, which is exactly what deeper DI interview questions probe."}]},{level:"junior",q:"What does @Inject on a constructor do in Dagger?",a:[{t:"p",text:"**Putting `@Inject` on a constructor tells Dagger two things: 'you can create this class', and 'here are its dependencies'.** Dagger reads the constructor parameters, understands what the class needs, and knows how to build it — recursively providing those dependencies too (as long as they're also injectable)."},{t:"code",title:"@Inject constructor",code:`class UserRepository @Inject constructor(
    private val api: UserApi,    // Dagger will provide these
    private val dao: UserDao,
)`},{t:"p",text:"This is the *ideal* case in Dagger — when a class has an `@Inject constructor`, you need no extra configuration; Dagger figures out the whole construction chain automatically. If `UserRepository` needs a `UserApi`, and `UserApi` is also provided somehow, Dagger wires it all up. You only have to write *modules* (`@Provides`/`@Binds`) for types that *can't* have an `@Inject constructor` — interfaces (can't construct an interface), library classes (you don't own their source, like Retrofit/OkHttp), or types needing custom construction logic. So the strategy is: annotate your own classes' constructors with `@Inject` (most of your codebase), and write modules only for the types Dagger can't construct itself."}]},{level:"junior",q:"What is a Dagger Module and when do you need one?",a:[{t:"p",text:"**A `@Module` is a class that tells Dagger *how to provide* types it can't construct on its own.** Dagger can automatically build any class with an `@Inject constructor`, but for types where that's not possible, you write a module with methods that supply the instances."},{t:"list",items:["**You need a module for**: interfaces (Dagger can't instantiate an interface — you must tell it which implementation to use); library/framework classes you don't own (Retrofit, OkHttp, Room database — you can't add `@Inject` to their constructors); and types requiring custom construction (a builder, configuration, conditional logic).","**`@Provides` methods** — a method that returns an instance of a type. Its parameters are its own dependencies, which Dagger supplies. This lets you build chains of library objects (a Retrofit method taking an OkHttpClient that another method provides)."]},{t:"code",title:"A module providing library types",code:`@Module
class NetworkModule {
    @Provides @Singleton
    fun provideRetrofit(client: OkHttpClient): Retrofit =
        Retrofit.Builder().baseUrl("...").client(client).build()
}`},{t:"p",text:"So the mental split is: your own classes get `@Inject constructor` (no module needed), and everything you *can't* annotate (interfaces + third-party classes) goes in a module with `@Provides`. In Hilt, modules work the same way but you add `@InstallIn(SomeComponent::class)` to say which scope they belong to, and Hilt handles the component wiring."}]},{level:"junior",q:"What are scopes in Dagger, like @Singleton?",a:[{t:"p",text:"**A scope tells Dagger to reuse a *single instance* of a dependency within a component's lifetime, instead of creating a new one every time it's requested.** By default (unscoped), Dagger creates a fresh instance on each request; a scope annotation makes it cache and reuse one."},{t:"list",items:["**`@Singleton`** is the app-wide scope — one instance for the whole application lifetime. Use it for expensive or shared objects: the Retrofit instance, the database, repositories.","**Unscoped (default)** — a new instance every time. Fine for cheap, stateless objects where sharing doesn't matter.","**Custom scopes** (`@ActivityScoped`, `@ViewModelScoped` in Hilt) — narrower lifetimes tied to a component, so an instance is shared within, say, one Activity and recreated for the next."]},{t:"p",text:"The key idea is matching the *lifetime* of an object to how it should be shared: a database should be a `@Singleton` (one for the app — creating multiple is wasteful and can conflict), while a screen-specific helper might be activity-scoped or unscoped. A caution: don't make everything `@Singleton` — that keeps objects alive for the whole app even when unneeded (memory cost), and sharing stateful objects app-wide can cause surprising bugs. Scope deliberately based on the object's intended lifetime and sharing. In Hilt, scopes are tied to its predefined components (`SingletonComponent`, `ActivityComponent`, `ViewModelComponent`), so you pick the scope matching the Android lifecycle you want."}]},{level:"senior",q:"What is the difference between @Provides and @Binds, and why prefer @Binds for interfaces?",a:[{t:"p",text:"**Both tell Dagger how to satisfy a dependency, but `@Binds` is a more efficient way to express the specific 'bind this interface to that implementation' case. `@Provides` is a concrete method with a body that *returns* an instance; `@Binds` is an *abstract* method with no body that just *declares* a mapping from an interface to an implementation.**"},{t:"code",title:"The same binding, two ways",code:`// @Provides — a full method with a body
@Provides
fun provideRepo(impl: UserRepositoryImpl): UserRepository = impl

// @Binds — abstract, no body, just declares the mapping
@Binds
abstract fun bindRepo(impl: UserRepositoryImpl): UserRepository`},{t:"list",items:["**Why `@Binds` is more efficient**: for `@Provides`, Dagger generates a full *factory class* that calls your method to produce the instance. For `@Binds`, since it's just declaring 'the interface *is* this implementation' (which Dagger already knows how to construct via its `@Inject constructor`), Dagger can generate much *less* code — often no factory at all, just a direct cast/reference. Less generated code means smaller binary and slightly faster build.","**The constraint**: `@Binds` only works when the implementation is already constructable by Dagger (has an `@Inject constructor` or is provided elsewhere) and you're purely *aliasing* one type to another. It can't have a body, so it can't do construction logic. It must be in an *abstract* module (an abstract class or interface).","**When to use each**: use `@Binds` for the common 'bind interface to its implementation' pattern (which is most of your repository/use-case bindings) — it's the idiomatic, efficient choice. Use `@Provides` when you need to *construct* something with logic — a library object built via a builder (Retrofit, OkHttp, Room), an object requiring configuration, or conditional provision. You can't `@Binds` a Retrofit instance because there's no `@Inject constructor` for it; you must `@Provides` it."]},{t:"p",text:"**The senior framing**: the rule of thumb is 'binding an interface to an implementation you own → `@Binds`; constructing a concrete object (especially library types) with logic → `@Provides`'. `@Binds` exists specifically to optimize the extremely common interface-aliasing case, generating minimal code, while `@Provides` handles everything requiring actual construction. Knowing to reach for `@Binds` on interface bindings (and that it must be abstract, in an abstract module, with the impl being Dagger-constructable) demonstrates you understand not just *how* to wire Dagger but how to do it efficiently — a genuine depth signal."}]},{level:"senior",q:"What is a Dagger Component, and what does Hilt do with components that plain Dagger makes you do manually?",a:[{t:"p",text:"**A `@Component` is the central connector of a Dagger graph — it lists the modules (where dependencies come from) and declares the injection points (where dependencies are needed), and Dagger generates its implementation (the code that actually constructs and wires the whole object graph). It's the composition root. In plain Dagger, *you* define, create, hold, and manage the lifecycle of every component and subcomponent yourself — and that manual management is the primary tedium Hilt eliminates.**"},{t:"list",items:["**What plain Dagger requires you to do manually**: define an `@Component` interface listing modules; build it (`DaggerAppComponent.create()`); hold the instance somewhere (usually the Application); define *subcomponents* or scoped components for narrower lifetimes (an activity component, a fragment component); manage the *hierarchy* between them (which component is a child of which); and wire each Android entry point (Activity/Fragment) to the right component, injecting at the right lifecycle moment. For a real app with app/activity/fragment/view-model scopes, this is a lot of intricate, boilerplate-heavy, easy-to-get-wrong code.","**What Hilt provides instead**: a *standard, predefined set of components* already wired to Android's lifecycles — `SingletonComponent` (app), `ActivityRetainedComponent`, `ViewModelComponent`, `ActivityComponent`, `FragmentComponent`, `ViewComponent`, `ServiceComponent` — with the parent-child hierarchy already established (fragment component is a child of activity, which is a child of singleton, etc.). You *never* write `@Component`, never call `DaggerAppComponent`, never manage component lifecycles.","**How you use Hilt's components**: annotate the Application with `@HiltAndroidApp` (Hilt generates and manages the singleton component), annotate Android entry points with `@AndroidEntryPoint` (Hilt injects them from the right component at the right time), and put modules with `@InstallIn(SomeComponent::class)` to say which scope a module belongs to. Hilt generates all the component code and wiring that you'd hand-write in Dagger.","**Corollary — scopes map to components**: Hilt's scope annotations (`@Singleton`, `@ActivityScoped`, `@ViewModelScoped`) correspond directly to its predefined components, so 'scope' and 'which component' become the same decision, and Hilt enforces the lifecycle. In plain Dagger you'd define these scopes and their components yourself."]},{t:"p",text:"**The senior framing**: the component (and especially the *component hierarchy* with scopes and subcomponents) is the most complex, boilerplate-heavy part of Dagger, and it's boilerplate that's essentially identical across all Android apps (everyone needs app/activity/fragment/viewmodel scopes tied to those lifecycles). Hilt's core insight was to *standardize* that — provide the components and hierarchy every Android app needs, pre-wired to the lifecycles, so developers only declare modules and injection points. So 'what does Hilt do?' at the component level: it replaces the manual, error-prone `@Component`/subcomponent/lifecycle management with a fixed set of lifecycle-bound components you opt into via `@InstallIn` and `@AndroidEntryPoint`. Understanding that Hilt = 'Dagger + standard Android components + reduced boilerplate' is the complete answer."}]},{level:"junior",q:"What is a Dagger @Component, and what does it do?",a:[{t:"p",text:"A `@Component` is the *bridge* between modules (which define bindings) and the classes that need dependencies — Dagger generates its implementation, which knows how to construct the whole graph. You declare it as an interface listing its modules and *provision methods* (or injection methods), and Dagger generates a `DaggerXComponent` you build and use to get dependencies."},{t:"code",title:"@Component",code:`@Component(modules = [NetworkModule::class])
interface AppComponent {
    fun userRepository(): UserRepository   // provision method
    fun inject(activity: MainActivity)     // members injection
}
val component = DaggerAppComponent.create()`},{t:"list",items:["**Bridge** — connects modules to injection targets.","**Generated impl** — `DaggerXComponent` builds the graph.","**Provision methods** — expose dependencies; `inject(target)` for field injection.","**Hilt** — predefines and generates components so you don't."]},{t:"note",text:"A @Component is the interface bridging modules (bindings) to injection targets; Dagger generates DaggerXComponent that constructs the graph. It lists modules + provision methods (or inject(target) for members injection). You build it (DaggerAppComponent.create()) and use it. Hilt predefines/generates components for you."}]},{level:"senior",q:"What are Dagger subcomponents, and how do they enable scoping?",a:[{t:"p",text:"A `@Subcomponent` is a child component that *inherits* its parent's bindings and adds its own scope/bindings — creating a hierarchy (e.g. an app component → activity subcomponent → fragment subcomponent). Subcomponents enable *lifecycle scopes*: a subcomponent lives as long as its owner (an Activity), so `@ActivityScoped` bindings are single instances within it. Hilt automates this hierarchy."},{t:"list",items:["**`@Subcomponent`** — child inheriting parent bindings + own scope.","**Hierarchy** — app → activity → fragment components.","**Lifecycle scopes** — a subcomponent's lifetime scopes its bindings.","**Hilt** — predefines this component hierarchy."]},{t:"note",text:"A @Subcomponent is a child component inheriting the parent's bindings and adding its own scope/bindings — forming a hierarchy (app → activity → fragment). Its lifetime scopes its bindings (@ActivityScoped = single instance within it). Hilt automates this whole hierarchy so you don't write subcomponents manually."}]},{level:"junior",q:"What is @Inject on a field versus a constructor in Dagger?",a:[{t:"p",text:"`@Inject` on a *constructor* tells Dagger how to *create* that class (and its dependencies) — the preferred way. `@Inject` on a *field* (with `lateinit var`) marks it as an *injection target* to be populated by a component's `inject(target)` method — used for classes Dagger doesn't construct (Activities). Constructor injection is cleaner; field injection is for framework-created objects."},{t:"list",items:["**Constructor `@Inject`** — Dagger constructs the class; preferred.","**Field `@Inject`** — populated via `component.inject(target)`.","**Field for** — Activities/Fragments you don't construct.","**Prefer constructor** — explicit, immutable, testable."]},{t:"note",text:"@Inject on a constructor tells Dagger how to create the class (preferred). @Inject on a field marks an injection target populated via component.inject(target) — for framework-created classes (Activities). Constructor injection is explicit/immutable/testable; field injection is for objects Dagger doesn't construct."}]},{level:"senior",q:"How do you pass runtime values into a Dagger component?",a:[{t:"p",text:"Use `@Component.Builder`/`@Component.Factory` with `@BindsInstance` to supply runtime values (like the `Application`/`Context` or a user id) into the graph when building the component. `@BindsInstance` binds the passed instance so it's injectable throughout. This is how you get the `Context` into the graph without a module."},{t:"code",title:"@BindsInstance",code:`@Component
interface AppComponent {
    @Component.Factory interface Factory {
        fun create(@BindsInstance app: Application): AppComponent
    }
}
val component = DaggerAppComponent.factory().create(application)`},{t:"list",items:["**`@Component.Factory`/`Builder`** — customize component creation.","**`@BindsInstance`** — bind a runtime value into the graph.","**Uses** — Application/Context, config, user id.","**Injectable** — the bound instance is available throughout."]},{t:"note",text:"Use @Component.Factory/@Component.Builder with @BindsInstance to pass runtime values (Application/Context, user id, config) into the graph when building the component — the bound instance becomes injectable throughout. It's how the Context enters the graph without a module. Hilt does this automatically for the Application."}]},{level:"senior",q:"What is a custom scope annotation in Dagger, and how does it work?",a:[{t:"p",text:"You define a `@Scope` annotation (`@UserScope`, `@ActivityScope`) and apply it to a component and its bindings — Dagger then keeps *one instance* of a scoped binding *per component instance*. The scope is enforced by the component lifetime: a new component = new scoped instances. Scopes are just annotations; the component instance defines the actual lifetime."},{t:"code",title:"Custom scope",code:`@Scope @Retention(AnnotationRetention.RUNTIME) annotation class UserScope

@UserScope @Subcomponent interface UserComponent { ... }
@UserScope class UserSession @Inject constructor(...)   // one per UserComponent`},{t:"list",items:["**`@Scope` annotation** — define a custom scope.","**Apply to component + bindings** — one instance per component.","**Lifetime = component lifetime** — new component, new instances.","**Enforcement** — Dagger checks scoped bindings match the component's scope."]},{t:"note",text:"Define a @Scope annotation (@UserScope) and apply it to a component and its bindings — Dagger keeps one instance per component instance. The scope's actual lifetime is the component's lifetime (new component = new scoped instances). Scopes are annotations; the component defines the lifetime. Hilt predefines the standard Android scopes."}]},{level:"senior",q:"What is component dependency versus a subcomponent?",a:[{t:"p",text:"Both let components share bindings, but differently. A *subcomponent* is *nested* inside its parent and has full access to the parent's graph. A *component dependency* (`@Component(dependencies = [Other::class])`) is a looser link — the dependent component can only use the bindings the parent *explicitly exposes* via provision methods. Component dependencies decouple components (useful across modules that shouldn't fully share graphs); subcomponents are tighter."},{t:"list",items:["**Subcomponent** — nested; full access to the parent graph.","**Component dependency** — looser; only explicitly-exposed bindings.","**Decoupling** — component dependencies across modules.","**Tighter** — subcomponents for a clear parent-child lifecycle."]},{t:"note",text:"Subcomponent: nested in the parent with full access to its graph (tight). Component dependency (@Component(dependencies=[...])): looser — only the parent's explicitly-exposed provision methods are available. Use component dependencies to decouple components across modules; subcomponents for a clear parent-child lifecycle."}]},{level:"junior",q:"What is the difference between @Provides and @Binds again, and when is each required?",a:[{t:"p",text:"`@Provides` is a *concrete method* that returns an instance — use it when construction needs logic (a Retrofit builder) or for third-party classes. `@Binds` is an *abstract method* that just maps an interface to an already-injectable implementation — more efficient (no method body). Use `@Binds` for simple interface→impl bindings; `@Provides` when you must write construction code."},{t:"list",items:["**`@Provides`** — concrete; write construction logic; third-party classes.","**`@Binds`** — abstract; interface → injectable impl; efficient.","**`@Binds` needs** — the impl to be `@Inject`-constructable.","**`@Provides` needs** — a method body returning the instance."]},{t:"note",text:"@Provides = concrete method returning an instance (for construction logic / third-party classes). @Binds = abstract method mapping an interface to an already-injectable impl (more efficient, no body). Use @Binds for interface→impl; @Provides when you must write construction code."}]},{level:"senior",q:"How does Dagger generate code, and what does that mean at runtime?",a:[{t:"p",text:"Dagger is an *annotation processor* (kapt/KSP) that generates *factory* and *component* classes at *compile time* — e.g. `User_Factory`, `DaggerAppComponent`. At runtime, these are plain Kotlin/Java calling constructors directly — *no reflection*. This makes Dagger fast and verifiable (errors caught at compile time), at the cost of build time (annotation processing)."},{t:"list",items:["**Annotation processing** — generates factories/components at compile time.","**No runtime reflection** — generated code calls constructors directly.","**Fast + verifiable** — errors at compile, fast at runtime.","**Build cost** — kapt/KSP processing time."]},{t:"note",text:"Dagger is an annotation processor (kapt/KSP) generating factory/component classes at compile time (User_Factory, DaggerAppComponent) — at runtime it's plain code calling constructors, no reflection. Fast and compile-time-verified; the cost is build-time annotation processing."}]},{level:"senior",q:"What is multibinding in Dagger (@IntoSet, @IntoMap)?",a:[{t:"p",text:"Multibindings let multiple modules contribute to a `Set<T>` or `Map<K, T>` that Dagger aggregates and injects. `@IntoSet` adds an element to a set; `@IntoMap` with a `@MapKey` adds a keyed entry. This is the foundation of plugin architectures — many modules register handlers/initializers/workers, and a consumer injects the whole collection without knowing each contributor."},{t:"code",title:"@IntoMap",code:`@Binds @IntoMap @ClassKey(HomeViewModel::class)
abstract fun bind(vm: HomeViewModel): ViewModel   // classic ViewModel multibinding
`},{t:"list",items:["**`@IntoSet`** — contribute to a `Set<T>`.","**`@IntoMap` + `@MapKey`** — contribute a keyed entry.","**Plugin architecture** — modules register; consumer injects the collection.","**Classic use** — the pre-Hilt ViewModel factory multibinding map."]},{t:"note",text:"Dagger multibindings aggregate contributions into a Set<T> (@IntoSet) or Map<K,T> (@IntoMap + @MapKey) that a consumer injects. Foundation of plugin architectures (register handlers/initializers) and the classic pre-Hilt ViewModel factory (a Map<Class, ViewModel>). Hilt has its own multibinding support too."}]},{level:"senior",q:"How do you provide qualified bindings in Dagger?",a:[{t:"p",text:"Define a `@Qualifier` annotation (or use `@Named`) and apply it to both the binding and the injection point — Dagger uses the qualifier to disambiguate multiple bindings of the same type. Custom qualifiers are preferred over `@Named` because they're type-safe (a typo in `@Named(\"...\")` fails silently, a misspelled annotation won't compile)."},{t:"code",title:"Qualifier",code:`@Qualifier annotation class BaseUrl
@Provides @BaseUrl fun baseUrl(): String = "https://api.example.com/"
class Api(@BaseUrl private val url: String)`},{t:"list",items:["**`@Qualifier`/`@Named`** — disambiguate same-typed bindings.","**On both ends** — binding + injection point.","**Custom > `@Named`** — type-safe (no string typos).","**Uses** — two clients, multiple config strings."]},{t:"note",text:"Define a @Qualifier annotation (or @Named) on both the binding and injection point to disambiguate same-typed bindings. Prefer custom qualifiers over @Named — they're type-safe (a misspelled annotation won't compile, unlike a string typo). For two OkHttpClients, multiple config strings, etc."}]},{level:"senior",q:"What is lazy and provider injection in Dagger?",a:[{t:"p",text:"`Lazy<T>` defers a dependency's creation until first `.get()` (then caches it); `Provider<T>` returns a *new* instance each `.get()` (or a fresh scoped one). Use `Lazy` for expensive dependencies not always needed; `Provider` when you need multiple instances or to break a scope/cycle. Both let you control *when* and *how many* instances are created."},{t:"code",title:"Lazy / Provider",code:`class A @Inject constructor(
    private val heavy: Lazy<Heavy>,       // created on first get()
    private val itemFactory: Provider<Item>,  // new per get()
)`},{t:"list",items:["**`Lazy<T>`** — deferred, cached after first `get()`.","**`Provider<T>`** — new instance per `get()`.","**`Lazy`** — expensive/rarely-used deps.","**`Provider`** — multiple instances / breaking cycles."]},{t:"note",text:"Dagger Lazy<T> defers creation until first get() (then caches); Provider<T> returns a new instance per get(). Use Lazy for expensive/rarely-needed deps, Provider for multiple instances or breaking cycles. Both control when and how many instances are created."}]},{level:"junior",q:"What does 'unscoped' mean, and what happens with an unscoped binding?",a:[{t:"p",text:"An *unscoped* binding (no scope annotation) creates a *new instance every time* it's injected — Dagger doesn't cache it. This is the default and is fine for cheap, stateless objects (mappers, use cases). Add a scope (`@Singleton`, etc.) only when you need a *shared* single instance (state, expensive resources like a database). Over-scoping wastes memory; under-scoping recreates state."},{t:"list",items:["**Unscoped** — new instance per injection (default).","**Fine for** — cheap, stateless objects.","**Add scope for** — shared single instances (state, expensive resources).","**Balance** — over-scope leaks/wastes; under-scope recreates."]},{t:"note",text:"An unscoped binding (no scope) creates a new instance per injection (the default) — fine for cheap stateless objects (mappers/use cases). Add a scope (@Singleton) only for shared single instances (state, expensive resources like a DB). Over-scoping wastes memory; under-scoping recreates state."}]},{level:"senior",q:"How do you inject a ViewModel with plain Dagger (pre-Hilt)?",a:[{t:"p",text:"The classic approach: use a *multibinding map* — bind each ViewModel `@IntoMap @ClassKey(...)` to `ViewModel`, and provide a custom `ViewModelProvider.Factory` that looks up the ViewModel by class from the injected `Map<Class, Provider<ViewModel>>`. This let Dagger create ViewModels with dependencies. Hilt replaced all this boilerplate with `@HiltViewModel`."},{t:"list",items:["**Multibinding map** — `@IntoMap @ClassKey` → `ViewModel`.","**Custom factory** — looks up by class from the injected map.","**Enabled DI ViewModels** — before Hilt.","**Hilt** — `@HiltViewModel` removes this entirely."]},{t:"note",text:"Pre-Hilt ViewModel injection used a multibinding map: bind each ViewModel @IntoMap @ClassKey to ViewModel, and a custom ViewModelProvider.Factory looks it up from the injected Map<Class, Provider<ViewModel>>. Boilerplate that @HiltViewModel replaced entirely."}]},{level:"senior",q:"What is @Reusable scope, and how does it differ from @Singleton?",a:[{t:"p",text:"`@Reusable` is a *relaxed* scope: Dagger *may* cache and reuse an instance, but doesn't *guarantee* a single instance (it can create more than one, and isn't tied to a component). Use it for stateless objects where you'd *prefer* to avoid recreating them but don't *need* strict singleton semantics. `@Singleton` guarantees exactly one per component; `@Reusable` is a best-effort optimization."},{t:"list",items:["**`@Reusable`** — may cache; no single-instance guarantee.","**`@Singleton`** — exactly one per component.","**Use `@Reusable`** — stateless objects, avoid-recreation preference.","**Best-effort** — an optimization, not strict scoping."]},{t:"note",text:"@Reusable is a relaxed scope — Dagger may cache and reuse an instance but doesn't guarantee a single one (not component-bound). Use it for stateless objects you'd prefer not to recreate but don't need strict singleton semantics. @Singleton guarantees exactly one per component; @Reusable is a best-effort optimization."}]},{level:"junior",q:"Why is Dagger preferred over runtime DI for large apps?",a:[{t:"p",text:"Dagger's *compile-time* code generation means: missing/incorrect bindings and dependency cycles are caught *at build time* (not runtime crashes), there's *no reflection* (fast, small, no runtime graph resolution), and the generated code is efficient. For large apps with complex graphs, this compile-time safety and performance is valuable — worth the build-time and learning cost."},{t:"list",items:["**Compile-time verification** — missing bindings/cycles caught at build.","**No reflection** — fast runtime, small footprint.","**Efficient generated code** — direct constructor calls.","**Trade-off** — build time + learning curve for large-graph safety."]},{t:"note",text:"Dagger's compile-time codegen catches missing bindings/cycles at build time (not runtime crashes), uses no reflection (fast, small), and generates efficient code — valuable for large apps with complex graphs. The cost is build time and a learning curve. Hilt makes this ergonomic for Android."}]},{level:"senior",q:"How do you test code that uses Dagger?",a:[{t:"p",text:"You generally *don't* need Dagger in unit tests — since dependencies are constructor-injected, tests just pass fakes directly. For *integration* tests needing the graph, create a *test component* with test modules that provide fakes (or override production modules). But most tests bypass DI entirely by constructing the class under test with test doubles."},{t:"list",items:["**Unit tests** — construct with fakes directly; no Dagger needed.","**Integration** — a test component with test modules providing fakes.","**Override modules** — swap production bindings for test ones.","**Constructor injection** — is what makes tests DI-free."]},{t:"note",text:"Most Dagger-based code needs no Dagger in unit tests — constructor injection lets you pass fakes directly. For integration tests needing the graph, build a test component with test modules providing fakes (overriding production modules). Constructor injection is what makes tests DI-free."}]},{level:"senior",q:"What is the object graph, and how does Dagger resolve it?",a:[{t:"p",text:"The object graph is the set of all objects and their dependency relationships. Dagger *resolves* it at *compile time*: it reads `@Inject` constructors, `@Provides`/`@Binds` bindings, and scopes, then generates factory code that constructs each object with its dependencies in the correct order. If a required binding is missing or a cycle exists, compilation fails with a clear error naming the type."},{t:"list",items:["**Object graph** — objects + dependency relationships.","**Compile-time resolution** — reads `@Inject`/`@Provides`/`@Binds`/scopes.","**Generated factories** — construct in the right order.","**Errors** — missing binding/cycle fails the build with a named type."]},{t:"note",text:"The object graph is all objects + their dependency relationships. Dagger resolves it at compile time from @Inject constructors, @Provides/@Binds bindings, and scopes, generating factory code that constructs each in order. A missing binding or cycle fails compilation with a clear error naming the type."}]},{level:"junior",q:"What is the relationship between Dagger, Hilt, and Anvil?",a:[{t:"p",text:"*Dagger* is the core compile-time DI framework. *Hilt* is Google's opinionated layer *on top of Dagger* for Android — predefined components/scopes and Android integration. *Anvil* (Square) is a Kotlin compiler plugin that reduces Dagger boilerplate differently (contributing modules/bindings automatically, faster than kapt). All three ultimately use Dagger's model; Hilt is the mainstream Android choice."},{t:"list",items:["**Dagger** — the core compile-time DI framework.","**Hilt** — Android layer on Dagger (predefined components).","**Anvil** — Kotlin plugin reducing Dagger boilerplate (auto-contribution).","**Mainstream** — Hilt for Android apps."]},{t:"note",text:"Dagger = the core compile-time DI framework. Hilt = Google's Android layer on Dagger (predefined components/scopes, Android integration). Anvil (Square) = a Kotlin compiler plugin cutting Dagger boilerplate via automatic contribution. All use Dagger's model; Hilt is the mainstream Android choice."}]},{level:"senior",q:"How do you break a dependency cycle in Dagger?",a:[{t:"p",text:"Dagger fails to compile on a cycle (A needs B, B needs A). Break it by injecting a `Lazy<T>` or `Provider<T>` on one side (deferring that dependency's creation so the graph can be built), introducing an interface to invert the dependency, or refactoring to remove the cycle (extract shared logic). A cycle usually indicates a design issue worth fixing rather than just deferring."},{t:"list",items:["**`Lazy`/`Provider`** — defer one side's creation.","**Interface** — invert one dependency direction.","**Refactor** — extract shared logic to a third class.","**Design smell** — a cycle often signals mixed responsibilities."]},{t:"note",text:"Dagger fails to compile on a cycle (A↔B). Break it with Lazy<T>/Provider<T> on one side (defer creation), an interface to invert the dependency, or refactoring (extract shared logic). A cycle usually signals a design issue — prefer fixing it over just deferring."}]},{level:"senior",q:"What is the performance impact of Dagger on build and app?",a:[{t:"p",text:"At *runtime*, Dagger is essentially free — generated code calls constructors directly, no reflection, minimal overhead. The cost is at *build time*: annotation processing (kapt) can noticeably slow builds, especially for large graphs. Migrating to *KSP* (which Dagger/Hilt now support) speeds this up significantly. So Dagger trades some build time for zero-reflection runtime performance."},{t:"list",items:["**Runtime** — negligible; direct constructor calls, no reflection.","**Build time** — annotation processing (kapt) can be slow.","**KSP** — faster than kapt; use it where supported.","**Trade-off** — build cost for fast, verified runtime."]},{t:"note",text:"Dagger's runtime cost is negligible (direct constructor calls, no reflection); the cost is build-time annotation processing (kapt can be slow for large graphs). Migrating to KSP speeds builds significantly. It trades some build time for zero-reflection, compile-verified runtime performance."}]},{level:"junior",q:"What is a @MapKey, and when do you use it?",a:[{t:"p",text:"`@MapKey` defines the *key type* for a multibinding `Map` — you annotate the key annotation with `@MapKey`, then use it on `@IntoMap` bindings. Built-in ones include `@ClassKey` (Class keys), `@StringKey`, `@IntKey`. It's how each `@IntoMap` entry declares its key (e.g. mapping a `Class` to a `ViewModel` in the classic ViewModel factory)."},{t:"code",title:"@MapKey",code:`@MapKey annotation class ViewModelKey(val value: KClass<out ViewModel>)
@Binds @IntoMap @ViewModelKey(HomeViewModel::class)
abstract fun bind(vm: HomeViewModel): ViewModel`},{t:"list",items:["**`@MapKey`** — declares the key type for a multibinding map.","**Built-in** — `@ClassKey`, `@StringKey`, `@IntKey`.","**On `@IntoMap`** — each entry supplies its key.","**Classic use** — ViewModel factory `Map<Class, ViewModel>`."]},{t:"note",text:"@MapKey defines the key type for a multibinding Map; annotate a key annotation with it (or use built-in @ClassKey/@StringKey/@IntKey), then apply it on @IntoMap bindings so each entry declares its key. Classic use: the ViewModel factory Map<Class, ViewModel>."}]},{level:"senior",q:"How do you scope a dependency to a user session or feature flow?",a:[{t:"p",text:"Create a custom-scoped *subcomponent* whose lifetime matches the session/flow — build the subcomponent when the session starts, hold the reference, and *release* it (null the reference) when the session ends. Bindings annotated with that scope are single instances for the session's lifetime and are garbage-collected when you drop the subcomponent. This is how you model per-login or per-flow state cleanly."},{t:"list",items:["**Custom-scoped subcomponent** — lifetime = session/flow.","**Build on start** — hold the reference; deps scoped to it.","**Release on end** — drop the reference; instances GC'd.","**Uses** — per-login session, per-checkout flow state."]},{t:"note",text:"Scope to a session/flow with a custom-scoped subcomponent: build it when the session starts (hold the reference), and release it (drop the reference) when it ends — scoped bindings are single instances for its lifetime and GC'd on release. Models per-login/per-flow state cleanly. Hilt has @ActivityRetainedScoped for similar needs."}]},{level:"junior",q:"Do you still need to know Dagger if you use Hilt?",a:[{t:"p",text:"Yes, conceptually — Hilt *is* Dagger with predefined components, so understanding Dagger's model (bindings, scopes, `@Provides`/`@Binds`, the graph, qualifiers) is essential to use Hilt well and debug its errors. Hilt saves you from writing components and wiring, but the underlying concepts (and error messages) are Dagger's. For advanced cases (custom components) you drop to Dagger constructs."},{t:"list",items:["**Hilt is Dagger** — with predefined components.","**Concepts transfer** — bindings/scopes/@Provides/@Binds/qualifiers.","**Debugging** — Hilt errors are Dagger errors.","**Advanced** — custom components use Dagger constructs."]},{t:"note",text:"Yes — Hilt IS Dagger with predefined components, so Dagger's concepts (bindings, scopes, @Provides/@Binds, the graph, qualifiers) are essential to use Hilt well and read its (Dagger) error messages. Hilt removes the component/wiring boilerplate, but the model underneath is Dagger's; advanced cases drop to Dagger."}]},{level:"senior",q:"How do you migrate a Dagger app to KSP from kapt?",a:[{t:"p",text:"Dagger and Hilt now support *KSP* (Kotlin Symbol Processing), which is significantly faster than *kapt* (which generates Java stubs). Migrate by applying the KSP Gradle plugin and switching the Dagger/Hilt dependencies from `kapt(...)` to `ksp(...)` in the build file. The generated code and behavior are the same; you just get faster builds. This is a low-risk, high-value build-speed improvement."},{t:"list",items:["**KSP > kapt** — faster (no Java stub generation).","**Apply KSP plugin** — and switch `kapt(...)` → `ksp(...)`.","**Same behavior** — generated code unchanged.","**Low-risk win** — meaningful build-speed improvement."]},{t:"note",text:"Dagger/Hilt support KSP (Kotlin Symbol Processing), much faster than kapt (which generates Java stubs). Migrate by applying the KSP plugin and switching Dagger/Hilt deps from kapt(...) to ksp(...). Same generated code/behavior, faster builds — a low-risk, high-value improvement."}]}],nC={id:"dagger",title:"Dagger Fundamentals",tagline:"Compile-time DI, @Inject constructors, modules with @Provides & @Binds, components as the composition root, scopes, and what Hilt generates for you.",tags:["Dagger","@Inject","@Provides","@Binds","Components"],content:aC,qa:iC},oC=[{heading:"What Hilt is",blocks:[{t:"p",text:"**Hilt** is Google's recommended dependency injection library for Android — it's a layer built *on top of Dagger* that removes most of Dagger's boilerplate and adds Android-specific integration. Where Dagger makes you define and manage components, subcomponents, scopes, and their lifecycles by hand, Hilt provides a *standard set of components already wired to Android lifecycles* (Application, Activity, Fragment, ViewModel, Service). You just declare how to provide dependencies and where to inject them; Hilt generates and manages all the component code. It keeps Dagger's compile-time safety (missing dependencies = build errors, no reflection) while being far easier to use."}]},{heading:"The basic setup",blocks:[{t:"code",title:"The four things you need",code:`// 1) Annotate the Application — Hilt generates the app-level component
@HiltAndroidApp
class MyApp : Application()

// 2) Annotate Android entry points — Hilt can inject into them
@AndroidEntryPoint
class MainActivity : AppCompatActivity() {
    @Inject lateinit var analytics: Analytics    // field injection (framework-created)
}

// 3) Your own classes use @Inject constructor
class UserRepository @Inject constructor(private val api: UserApi)

// 4) Modules for types you can't construct (interfaces, libraries)
@Module
@InstallIn(SingletonComponent::class)             // which scope/component
object NetworkModule {
    @Provides @Singleton
    fun provideRetrofit(): Retrofit = Retrofit.Builder().baseUrl("...").build()
}`},{t:"list",items:["**`@HiltAndroidApp`** on your Application class — bootstraps Hilt and generates the app-level (`SingletonComponent`) dependency container. Required, and it's the root.","**`@AndroidEntryPoint`** on Activities, Fragments, Services, BroadcastReceivers, Views — tells Hilt it can inject into them (via field injection, since the framework creates them). Without it, injection into that component fails.","**`@Inject constructor`** on your own classes — same as Dagger; Hilt constructs them and provides their dependencies.","**`@Module` + `@InstallIn`** — modules work like Dagger, but `@InstallIn(component)` tells Hilt which of its predefined components (scope) the bindings belong to. No manual `@Component` — that's the boilerplate Hilt removes."]}]},{heading:"Hilt components and scopes",blocks:[{t:"p",text:"Hilt provides a fixed hierarchy of **components**, each tied to an Android lifecycle, and each with a matching **scope** annotation. You pick which component a module installs into (and which scope to use) based on the lifetime you want."},{t:"table",headers:["Component","Scope annotation","Lifetime"],rows:[["SingletonComponent","@Singleton","the whole application"],["ActivityRetainedComponent","@ActivityRetainedScoped","across config changes (like ViewModel)"],["ViewModelComponent","@ViewModelScoped","one ViewModel"],["ActivityComponent","@ActivityScoped","one Activity"],["FragmentComponent","@FragmentScoped","one Fragment"],["ServiceComponent","@ServiceScoped","one Service"]]},{t:"list",items:["**The hierarchy is nested**: Fragment ⊂ Activity ⊂ ActivityRetained ⊂ Singleton. A component can access dependencies from its parents (a Fragment-scoped thing can use Activity- and Singleton-scoped dependencies), but not children.","**Choosing a scope**: `@Singleton` for app-wide shared resources (database, network, repositories); `@ViewModelScoped` for something shared within one ViewModel's dependencies; `@ActivityScoped` for something tied to one Activity. Unscoped (no annotation) = new instance each time.","**`@InstallIn` matches the scope**: a `@Singleton` binding goes in a module `@InstallIn(SingletonComponent::class)`; a `@ViewModelScoped` binding goes in `@InstallIn(ViewModelComponent::class)`. The scope and component must correspond."]}]},{heading:"ViewModel injection with Hilt",blocks:[{t:"p",text:"One of Hilt's best features is seamless **ViewModel injection** — a very common need. You annotate the ViewModel with `@HiltViewModel` and use `@Inject constructor`, and Hilt handles creating it with its dependencies (including `SavedStateHandle`), scoped correctly to the ViewModel's lifecycle."},{t:"code",title:"Injecting a ViewModel",code:`@HiltViewModel
class UserViewModel @Inject constructor(
    private val repository: UserRepository,       // Hilt provides it
    private val savedStateHandle: SavedStateHandle, // Hilt provides it too
) : ViewModel()

// In a @AndroidEntryPoint Activity/Fragment:
private val viewModel: UserViewModel by viewModels()   // Hilt wires it up

// In Compose:
@Composable fun Screen(viewModel: UserViewModel = hiltViewModel()) { }`},{t:"list",items:["**`@HiltViewModel` + `@Inject constructor`** — Hilt generates the ViewModel factory, so `by viewModels()` (Views) or `hiltViewModel()` (Compose) automatically construct it with its injected dependencies.","**`SavedStateHandle` is available for injection** — just add it as a constructor parameter and Hilt provides it, giving you access to navigation arguments and process-death survival with no extra wiring.","**No manual factory** — before Hilt, injecting a ViewModel with constructor arguments required writing a `ViewModelProvider.Factory` by hand. Hilt generates it, which is a major convenience."]}]},{heading:"Qualifiers, and injecting the same type twice",blocks:[{t:"p",text:"What if you need *two different instances of the same type* — two `OkHttpClient`s (one authenticated, one not), or two `String`s (a base URL and an API key)? Dagger/Hilt can't tell them apart by type alone. **Qualifiers** are annotations that distinguish bindings of the same type."},{t:"code",title:"Qualifiers disambiguate same-type bindings",code:`@Qualifier annotation class AuthClient
@Qualifier annotation class PublicClient

@Module @InstallIn(SingletonComponent::class)
object NetworkModule {
    @Provides @AuthClient
    fun provideAuthClient(): OkHttpClient = OkHttpClient.Builder().addInterceptor(auth).build()

    @Provides @PublicClient
    fun providePublicClient(): OkHttpClient = OkHttpClient.Builder().build()
}

// Request the specific one with the qualifier:
class Repo @Inject constructor(@AuthClient private val client: OkHttpClient)`},{t:"list",items:["**`@Qualifier`** — you define a custom annotation, mark each provider with a different qualifier, and request the specific one at the injection site. This resolves the 'multiple bindings for the same type' ambiguity.","**Built-in `@ApplicationContext` / `@ActivityContext`** — Hilt provides these qualifiers so you can inject the correct `Context` (app-scoped vs activity-scoped) — important for avoiding the context-leak issues (inject `@ApplicationContext` into long-lived objects).","**Without a qualifier, duplicate bindings of the same type are a compile error** — Hilt (via Dagger) can't guess which you want, so it fails the build, prompting you to add qualifiers."]},{t:"note",text:"Hilt essentials: Dagger + standard Android components + less boilerplate. Setup: @HiltAndroidApp (Application), @AndroidEntryPoint (Activities/Fragments/Services), @Inject constructor (your classes), @Module + @InstallIn(component) (types you can't construct). Predefined components/scopes map to Android lifecycles (@Singleton, @ViewModelScoped, @ActivityScoped...). @HiltViewModel + hiltViewModel() gives seamless ViewModel injection with SavedStateHandle. Qualifiers (@Qualifier, @ApplicationContext) distinguish same-type bindings. Keeps compile-time safety."}]}],sC=[{level:"junior",q:"What is Hilt and how does it relate to Dagger?",a:[{t:"p",text:"**Hilt is Google's recommended dependency injection library for Android, built as a layer on top of Dagger.** It keeps Dagger's benefits — compile-time verification (missing dependencies are build errors) and no runtime reflection — while removing most of Dagger's boilerplate and adding Android-specific integration."},{t:"p",text:"The core thing Hilt does is provide a *standard set of components already wired to Android lifecycles* — Application, Activity, Fragment, ViewModel, Service. In plain Dagger you have to define these components, their scopes, their parent-child hierarchy, and manage their lifecycles yourself, which is a lot of intricate boilerplate that's essentially the same in every Android app. Hilt standardizes it: you annotate your Application with `@HiltAndroidApp`, your entry points with `@AndroidEntryPoint`, use `@Inject constructor` on your classes, and install modules into predefined components with `@InstallIn`. You never write a `@Component`. So the relationship is: **Dagger is the engine, Hilt is Dagger pre-configured for Android with the boilerplate removed.** Understanding Dagger explains what Hilt generates under the hood, but for day-to-day Android work, Hilt is what you use."}]},{level:"junior",q:"What are the basic annotations you need to set up Hilt?",a:[{t:"list",items:["**`@HiltAndroidApp`** — on your `Application` class. It bootstraps Hilt and generates the app-level component. This is the required entry point.","**`@AndroidEntryPoint`** — on Activities, Fragments, Services, BroadcastReceivers, and Views where you want to inject dependencies. It tells Hilt these framework-created classes can receive injections (via field injection).","**`@Inject constructor`** — on your own classes (repositories, use cases). Hilt uses this to construct them and provide their dependencies, exactly like Dagger.","**`@Module` + `@InstallIn(Component::class)`** — for types you can't construct yourself (interfaces, library classes). The `@InstallIn` says which Hilt component (scope) the bindings belong to."]},{t:"code",title:"The minimal setup",code:`@HiltAndroidApp class MyApp : Application()

@AndroidEntryPoint class MainActivity : AppCompatActivity() {
    @Inject lateinit var analytics: Analytics
}

class Repo @Inject constructor(private val api: Api)`},{t:"p",text:"That's the whole setup for basic usage — no `@Component`, no manual factory, no `DaggerXxx` calls. The `@HiltAndroidApp` is the root, `@AndroidEntryPoint` marks where injection happens, `@Inject constructor` handles your classes, and modules handle the rest. Forgetting `@AndroidEntryPoint` on an Activity/Fragment is the most common beginner mistake — injection into it will fail because Hilt doesn't know it's an injection target."}]},{level:"junior",q:"How do you inject a ViewModel with Hilt?",a:[{t:"p",text:"**You annotate the ViewModel with `@HiltViewModel` and use `@Inject constructor`, then retrieve it with `by viewModels()` (Views) or `hiltViewModel()` (Compose). Hilt handles constructing it with its dependencies and scoping it correctly.**"},{t:"code",title:"ViewModel injection",code:`@HiltViewModel
class UserViewModel @Inject constructor(
    private val repository: UserRepository,
    private val savedStateHandle: SavedStateHandle,   // Hilt provides this too
) : ViewModel()

// Compose
@Composable fun Screen(vm: UserViewModel = hiltViewModel()) { }
// Views (in an @AndroidEntryPoint Activity/Fragment)
private val vm: UserViewModel by viewModels()`},{t:"p",text:"This is one of Hilt's most valued features because ViewModel injection used to be painful. Before Hilt, a ViewModel with constructor arguments required hand-writing a `ViewModelProvider.Factory` for every ViewModel — significant boilerplate. Hilt generates that factory automatically, so `hiltViewModel()`/`by viewModels()` just work. It also makes `SavedStateHandle` injectable as a simple constructor parameter, giving you navigation arguments and process-death survival with no extra code. The only requirements: the ViewModel needs `@HiltViewModel`, and the Activity/Fragment hosting it (or the composition) must be under an `@AndroidEntryPoint` component. This seamless ViewModel support is a big part of why Hilt is the standard Android DI choice."}]},{level:"junior",q:"What is @InstallIn and why is it needed?",a:[{t:"p",text:"**`@InstallIn(Component::class)` on a Hilt module declares *which Hilt component (and therefore which scope/lifecycle) the module's bindings belong to*.** Since Hilt has a predefined set of components tied to Android lifecycles (SingletonComponent for the app, ViewModelComponent for a ViewModel, ActivityComponent for an Activity, etc.), every module must say where it installs, so Hilt knows the lifetime and availability of the dependencies it provides."},{t:"code",title:"Installing a module into the app-level component",code:`@Module
@InstallIn(SingletonComponent::class)   // these bindings live app-wide
object NetworkModule {
    @Provides @Singleton
    fun provideRetrofit(): Retrofit = ...
}`},{t:"p",text:"It's needed because in plain Dagger you'd manually attach a module to a specific `@Component` you defined; Hilt replaces that with `@InstallIn` pointing at one of its standard components. The choice determines *where the dependency is available and how long it lives*: `@InstallIn(SingletonComponent::class)` makes bindings available app-wide (for app-scoped things like Retrofit, the database, repositories — the common case); `@InstallIn(ViewModelComponent::class)` makes them available only within ViewModels; and so on. The `@InstallIn` component must be consistent with any scope annotation you use — a `@Singleton` binding must be in a module installed in `SingletonComponent`. So `@InstallIn` is how you tell Hilt 'these dependencies belong to this lifecycle', which is the Hilt-simplified version of Dagger's manual component assignment."}]},{level:"senior",q:"How do you inject two different instances of the same type (e.g. two OkHttpClients)?",a:[{t:"p",text:"**You use *qualifiers* — custom annotations that distinguish bindings of the same type, so Hilt/Dagger knows which one you want at each injection site.** By type alone, Dagger can't tell two `OkHttpClient`s apart (one authenticated, one public) — it sees two bindings for the same type, which is ambiguous and a compile error. A qualifier annotation resolves the ambiguity."},{t:"code",title:"Qualifiers for same-type bindings",code:`@Qualifier annotation class AuthClient
@Qualifier annotation class PublicClient

@Module @InstallIn(SingletonComponent::class)
object NetworkModule {
    @Provides @AuthClient
    fun authClient(): OkHttpClient = OkHttpClient.Builder().addInterceptor(authInterceptor).build()

    @Provides @PublicClient
    fun publicClient(): OkHttpClient = OkHttpClient.Builder().build()
}

// Request the specific one by its qualifier
class ApiRepo @Inject constructor(@AuthClient private val client: OkHttpClient)`},{t:"list",items:["**Define a `@Qualifier` annotation** for each variant, mark each `@Provides` method with a different qualifier, and annotate the injection site (constructor parameter) with the qualifier for the one you want. Now Hilt knows exactly which binding to use.","**Common real cases**: two HTTP clients (authenticated vs public/refresh), two dispatchers (`@IoDispatcher` vs `@DefaultDispatcher` — a very common pattern for injecting `CoroutineDispatcher`s testably), a base URL and an API key (both `String`), or different database instances.","**Built-in qualifiers**: Hilt provides `@ApplicationContext` and `@ActivityContext` so you can inject the correct `Context` — this is important for avoiding leaks (inject `@ApplicationContext` into long-lived objects like repositories, never an Activity context).",'**Alternative for grouping — `@Named`**: Hilt also has a built-in `@Named("...")` string qualifier, but custom `@Qualifier` annotations are preferred (type-safe, refactorable, no string typos).']},{t:"p",text:"**The senior framing**: qualifiers solve the 'multiple bindings, same type' problem that arises constantly in real apps — dispatchers, HTTP clients, contexts, config strings. Without them, Hilt fails the build (which is actually good — it forces you to be explicit rather than silently picking one). The idiomatic approach is custom type-safe `@Qualifier` annotations, and knowing the built-in `@ApplicationContext`/`@ActivityContext` (and using the right one to avoid context leaks) shows practical Hilt fluency. This is also a favorite question because it reveals whether someone has used Hilt beyond the trivial single-instance case."}]},{level:"senior",q:"How does Hilt work with dynamic feature modules or WorkManager, and what are the gotchas?",a:[{t:"p",text:"**Hilt integrates smoothly with most Android components, but two areas have specific gotchas: WorkManager (needs a special integration for injecting Workers) and dynamic feature modules (which invert Hilt's normal dependency direction and can't use standard `@AndroidEntryPoint`).**"},{t:"list",items:["**WorkManager — `@HiltWorker` + custom factory**: a `Worker` is instantiated by the WorkManager framework, not by you, so you can't just `@Inject constructor` it normally. Hilt provides `@HiltWorker` and `@AssistedInject` — you annotate the worker `@HiltWorker`, use `@AssistedInject constructor` with `@Assisted` for the framework-provided `Context` and `WorkerParameters`, and your real dependencies as normal parameters. You also configure a `HiltWorkerFactory` in the WorkManager initialization (disabling the default initializer and providing Hilt's factory). Once set up, workers get their dependencies injected. The gotcha is the extra setup — forgetting the factory configuration means injection silently fails.","**Dynamic feature modules — the graph is inverted**: normally Hilt's components flow app → feature (the app depends on features' code at compile time, so Hilt can aggregate their bindings). But a *dynamic feature module* depends *on* the app module (the reverse), so at compile time the app can't see the dynamic module's Hilt code — meaning **you can't use `@AndroidEntryPoint`/standard Hilt injection in a dynamic feature module**, because Hilt's aggregation can't reach it.","**The workaround for dynamic features — `@EntryPoint`**: you define an `@EntryPoint` interface (installed in the appropriate component) listing the dependencies the dynamic module needs, and retrieve them at runtime via `EntryPointAccessors.fromApplication(context, MyEntryPoint::class.java)`. This is a manual, reflection-like accessor rather than clean field injection — clunky by design, because you're pulling from a graph the module can't participate in normally.","**Other `@EntryPoint` uses**: the same mechanism is needed anywhere Hilt can't reach with `@AndroidEntryPoint` — content providers (which initialize very early), or accessing Hilt dependencies from a class the framework instantiates in a way Hilt doesn't support."]},{t:"p",text:"**The senior framing**: Hilt covers the common Android components (Activities, Fragments, Services, ViewModels, and — with `@HiltWorker` — WorkManager) cleanly, but its *compile-time aggregation* model means it struggles when the dependency direction is inverted (dynamic features depending on the app) or when a class is created by a framework in an unsupported way. The escape hatch is `@EntryPoint` + `EntryPointAccessors`, which lets you *reach into* a Hilt component from code Hilt can't inject into directly. Knowing that dynamic feature modules can't use `@AndroidEntryPoint` (and why — the inverted graph) and that `@HiltWorker` + a factory is needed for WorkManager are the two gotchas that separate someone who's used Hilt on a real, multi-module app from someone who's only done the basic single-module setup. It's also a reason some heavily-dynamic apps consider Koin, whose runtime resolution doesn't care about compile-time graph direction."}]},{level:"junior",q:"What does @HiltAndroidApp do?",a:[{t:"p",text:"`@HiltAndroidApp` on your `Application` class triggers Hilt's code generation — it creates the application-level DI component (the root of the dependency graph) and a base `Application` that attaches it. It's the mandatory entry point: every Hilt app needs it, and it enables field injection in other Android classes."},{t:"code",title:"@HiltAndroidApp",code:`@HiltAndroidApp
class MyApp : Application()`},{t:"list",items:["**On the `Application`** — generates the app-level component (graph root).","**Mandatory** — every Hilt app needs it.","**Enables injection** — into `@AndroidEntryPoint` classes.",'**Register in manifest** — `android:name=".MyApp"`.']},{t:"note",text:"@HiltAndroidApp on the Application class generates Hilt's application-level DI component (the graph root) and wiring — mandatory for every Hilt app, and it enables field injection into @AndroidEntryPoint classes. Register the Application in the manifest."}]},{level:"junior",q:"What does @AndroidEntryPoint do?",a:[{t:"p",text:"`@AndroidEntryPoint` on an Android class (Activity, Fragment, Service, BroadcastReceiver, View) tells Hilt to *inject dependencies* into it — Hilt generates a component tied to that class's lifecycle and populates its `@Inject` fields. Without it, field injection into that class won't happen. Its container class must also be annotated (a `@AndroidEntryPoint` Fragment needs its Activity annotated too)."},{t:"code",title:"@AndroidEntryPoint",code:`@AndroidEntryPoint
class HomeFragment : Fragment() {
    @Inject lateinit var analytics: Analytics   // injected by Hilt
    private val viewModel: HomeViewModel by viewModels()   // Hilt-provided
}`},{t:"list",items:["**On Android classes** — Activity/Fragment/Service/Receiver/View.","**Enables field injection** — populates `@Inject` fields.","**Lifecycle-scoped component** — generated per class.","**Container must be annotated** — Fragment needs its Activity annotated."]},{t:"note",text:"@AndroidEntryPoint marks an Android class (Activity/Fragment/Service/Receiver/View) for Hilt injection — it generates a lifecycle-scoped component and populates @Inject fields. Required for field injection into that class; the container must also be annotated (a Fragment's host Activity needs it too)."}]},{level:"junior",q:"What is @HiltViewModel, and how are ViewModels provided?",a:[{t:"p",text:"`@HiltViewModel` lets you *constructor-inject* a ViewModel — Hilt generates the `ViewModelProvider.Factory` so you can request it with `by viewModels()`/`hiltViewModel()` and get its dependencies injected (including `SavedStateHandle`). This replaces writing a manual ViewModel factory for each ViewModel with dependencies."},{t:"code",title:"@HiltViewModel",code:`@HiltViewModel
class HomeViewModel @Inject constructor(
    private val repo: Repository,
    private val handle: SavedStateHandle,   // provided automatically
) : ViewModel()`},{t:"list",items:["**Constructor-inject the ViewModel** — deps as params.","**Generated factory** — `by viewModels()`/`hiltViewModel()` just work.","**`SavedStateHandle`** — injected automatically.","**No manual factory** — Hilt writes it."]},{t:"note",text:"@HiltViewModel enables constructor injection of a ViewModel — Hilt generates the ViewModelProvider.Factory, so by viewModels()/hiltViewModel() supply its dependencies (including SavedStateHandle automatically). Replaces writing a manual factory per ViewModel."}]},{level:"senior",q:"What is the Hilt component hierarchy?",a:[{t:"p",text:"Hilt predefines a hierarchy of components tied to Android lifecycles: `SingletonComponent` (Application), `ActivityRetainedComponent` (survives config change), `ViewModelComponent` (a ViewModel), `ActivityComponent`, `FragmentComponent`, `ViewComponent`, `ServiceComponent`. A binding installed in a component is available to it and its children. This lets you scope dependencies to the right lifetime."},{t:"list",items:["**`SingletonComponent`** — app lifetime.","**`ActivityRetainedComponent`** — survives config change (like a ViewModel).","**`ViewModelComponent`** — a ViewModel's lifetime.","**`Activity`/`Fragment`/`View`/`ServiceComponent`** — those lifecycles."]},{t:"note",text:"Hilt's component hierarchy maps to Android lifecycles: SingletonComponent (Application) → ActivityRetainedComponent (survives config change) → ViewModelComponent, ActivityComponent → FragmentComponent → ViewComponent, plus ServiceComponent. Bindings installed in a component are available to it and its children — scope deps to the right lifetime."}]},{level:"senior",q:"What are Hilt's predefined scopes?",a:[{t:"p",text:"Each component has a scope annotation that makes a binding a *single instance* within that component: `@Singleton` (app-wide), `@ActivityRetainedScoped` (survives config change), `@ViewModelScoped` (per ViewModel), `@ActivityScoped`, `@FragmentScoped`, `@ViewScoped`, `@ServiceScoped`. An unscoped binding creates a new instance per injection. Scope to match the dependency's intended lifetime."},{t:"list",items:["**`@Singleton`** — one app-wide instance.","**`@ActivityRetainedScoped`** — survives config change.","**`@ViewModelScoped`** — one per ViewModel.","**`@ActivityScoped`/etc.** — per that lifecycle; unscoped = new each time."]},{t:"note",text:"Hilt scopes make a binding a single instance within a component: @Singleton (app), @ActivityRetainedScoped (survives config change), @ViewModelScoped, @ActivityScoped/@FragmentScoped/@ViewScoped/@ServiceScoped. Unscoped = new instance per injection. Scope to the dependency's intended lifetime."}]},{level:"junior",q:"How do you write a Hilt module with @Provides?",a:[{t:"p",text:"A `@Module @InstallIn(component)` object with `@Provides` functions tells Hilt how to create dependencies it can't construct directly — like third-party classes (Retrofit, Room) or interfaces. Each `@Provides` function's parameters are themselves injected. Add a scope annotation to make the provided instance scoped."},{t:"code",title:"@Provides module",code:`@Module @InstallIn(SingletonComponent::class)
object NetworkModule {
    @Provides @Singleton
    fun provideRetrofit(client: OkHttpClient): Retrofit =
        Retrofit.Builder().baseUrl(BASE_URL).client(client).build()
}`},{t:"list",items:["**`@Module @InstallIn`** — where the bindings live.","**`@Provides`** — construct a dependency; params are injected.","**Scope** — `@Singleton` etc. for a single instance.","**Use for** — third-party classes and interfaces you can't `@Inject`."]},{t:"note",text:"A @Module @InstallIn(component) with @Provides functions tells Hilt how to create dependencies it can't construct (third-party classes like Retrofit/Room, or interfaces); each function's params are injected. Add a scope annotation (@Singleton) for a single instance."}]},{level:"senior",q:"How do you use @Binds in a Hilt module, and why prefer it for interfaces?",a:[{t:"p",text:"`@Binds` maps an interface to its implementation efficiently — in an *abstract* module (or interface), an abstract function takes the implementation and returns the interface. Prefer `@Binds` over `@Provides` for interface bindings because it generates *less code* (no factory method body) and is more efficient — Hilt just knows to substitute the implementation."},{t:"code",title:"@Binds",code:`@Module @InstallIn(SingletonComponent::class)
abstract class RepoModule {
    @Binds abstract fun bindRepo(impl: RepositoryImpl): Repository
}`},{t:"list",items:["**`@Binds`** — interface → implementation; abstract function.","**Efficient** — less generated code than `@Provides`.","**Impl must be `@Inject`-constructable** — Hilt builds it.","**Abstract module** — `@Binds` requires an abstract class/interface."]},{t:"note",text:"@Binds maps an interface to its (constructor-injectable) implementation via an abstract function in an abstract module — prefer it over @Provides for interfaces because it generates less code (no factory body). The implementation must be @Inject-constructable."}]},{level:"junior",q:"What are @ApplicationContext and @ActivityContext?",a:[{t:"p",text:"Hilt provides the `Context` via two built-in qualifiers: `@ApplicationContext` (the application context — for long-lived dependencies, safe to retain) and `@ActivityContext` (the Activity context — for UI/theme-dependent needs, not retained). Inject the right one to avoid leaks (long-lived deps must use the application context)."},{t:"code",title:"Context qualifiers",code:`class Analytics @Inject constructor(
    @ApplicationContext private val context: Context,   // long-lived, safe
)`},{t:"list",items:["**`@ApplicationContext`** — app context; long-lived, retainable.","**`@ActivityContext`** — Activity context; UI/theme, not retained.","**Avoid leaks** — long-lived deps use the application context.","**Predefined** — Hilt provides these qualifiers."]},{t:"note",text:"Hilt's built-in qualifiers: @ApplicationContext (app context — long-lived deps, safe to retain) and @ActivityContext (Activity context — UI/theme needs, don't retain). Inject the right one; long-lived dependencies must use @ApplicationContext to avoid leaking an Activity."}]},{level:"senior",q:"How do you inject dependencies into a WorkManager Worker?",a:[{t:"p",text:"Use `@HiltWorker` on the Worker with an `@AssistedInject` constructor — the `Context` and `WorkerParameters` are `@Assisted` (provided by WorkManager at runtime), and your dependencies are injected by Hilt. You also configure a custom `WorkerFactory` (via `HiltWorkerFactory`) in the Application's `Configuration.Provider`."},{t:"code",title:"@HiltWorker",code:`@HiltWorker
class SyncWorker @AssistedInject constructor(
    @Assisted context: Context,
    @Assisted params: WorkerParameters,
    private val repo: Repository,          // injected by Hilt
) : CoroutineWorker(context, params)
// Application implements Configuration.Provider using HiltWorkerFactory`},{t:"list",items:["**`@HiltWorker` + `@AssistedInject`** — Hilt injects a Worker.","**`@Assisted`** — Context/WorkerParameters from WorkManager.","**`HiltWorkerFactory`** — configured in `Configuration.Provider`.","**Injected deps** — repositories/etc. as normal."]},{t:"note",text:"Inject into a Worker with @HiltWorker + @AssistedInject: Context and WorkerParameters are @Assisted (from WorkManager), your dependencies injected by Hilt. Configure HiltWorkerFactory in the Application's Configuration.Provider (and disable WorkManager's default initializer)."}]},{level:"senior",q:"What is @EntryPoint, and when do you need it?",a:[{t:"p",text:"`@EntryPoint` lets *non-Hilt* code access the Hilt graph — for classes Hilt doesn't support directly (a `ContentProvider`, or code in a library/module without `@AndroidEntryPoint`). You define an interface annotated `@EntryPoint @InstallIn(component)` with accessor methods, then retrieve it via `EntryPointAccessors`. It's an escape hatch to bridge Hilt with un-annotatable code."},{t:"code",title:"@EntryPoint",code:`@EntryPoint @InstallIn(SingletonComponent::class)
interface AnalyticsEntryPoint { fun analytics(): Analytics }

val entryPoint = EntryPointAccessors.fromApplication(context, AnalyticsEntryPoint::class.java)
entryPoint.analytics().track("event")`},{t:"list",items:["**Access the graph from non-Hilt code** — ContentProviders, libraries.","**`@EntryPoint` interface** — accessor methods, `@InstallIn`.","**`EntryPointAccessors`** — retrieve it (from application/activity).","**Escape hatch** — bridge Hilt with un-annotatable classes."]},{t:"note",text:"@EntryPoint bridges Hilt to non-Hilt code (ContentProviders, library classes without @AndroidEntryPoint): define an @EntryPoint @InstallIn interface with accessor methods, retrieve it via EntryPointAccessors.fromApplication/Activity. An escape hatch to access the graph from un-annotatable classes."}]},{level:"senior",q:"How do you write Hilt tests with @HiltAndroidTest, @BindValue, and @UninstallModules?",a:[{t:"p",text:"For instrumented tests, `@HiltAndroidTest` sets up the test graph, a `HiltAndroidRule` performs injection, and `@BindValue` binds a test double into the graph (replacing a real binding). `@UninstallModules` removes a production module so you can provide a test module instead. This lets tests substitute fakes throughout the DI graph."},{t:"code",title:"Hilt test",code:`@HiltAndroidTest
@UninstallModules(NetworkModule::class)
class MyTest {
    @get:Rule val hiltRule = HiltAndroidRule(this)
    @BindValue val fakeRepo: Repository = FakeRepository()
    @Before fun setup() { hiltRule.inject() }
}`},{t:"list",items:["**`@HiltAndroidTest` + `HiltAndroidRule`** — test graph + injection.","**`@BindValue`** — bind a test double into the graph.","**`@UninstallModules`** — remove a production module.","**Substitute fakes** — throughout the DI graph."]},{t:"note",text:"Hilt tests: @HiltAndroidTest + HiltAndroidRule (test graph + inject()), @BindValue to bind a test double into the graph, @UninstallModules to remove a production module (replace with a test module). Lets you substitute fakes throughout the DI graph for instrumented tests."}]},{level:"junior",q:"How do you get a Hilt ViewModel in Compose?",a:[{t:"p",text:"Use `hiltViewModel()` (from `androidx.hilt:hilt-navigation-compose`) inside a composable — it retrieves the `@HiltViewModel` scoped to the current nav back-stack entry (or Activity), with dependencies injected. For a graph-scoped shared ViewModel, pass the parent back-stack entry."},{t:"code",title:"hiltViewModel()",code:`@Composable fun HomeScreen() {
    val viewModel: HomeViewModel = hiltViewModel()   // Hilt-provided, scoped to the entry
}`},{t:"list",items:["**`hiltViewModel()`** — Hilt-provided ViewModel in Compose.","**Scoped to the nav entry** — or Activity.","**Requires** — `@HiltViewModel` + `@AndroidEntryPoint` Activity.","**Shared** — pass a parent back-stack entry for a graph-scoped VM."]},{t:"note",text:"Use hiltViewModel() (hilt-navigation-compose) in a composable — it provides the @HiltViewModel scoped to the current nav back-stack entry (or Activity) with deps injected. Requires @HiltViewModel + an @AndroidEntryPoint Activity. Pass a parent entry for a graph-scoped shared ViewModel."}]},{level:"senior",q:"How does Hilt reduce Dagger boilerplate?",a:[{t:"p",text:"Plain Dagger makes you *define components*, *component interfaces*, *manage their lifecycles*, and *wire injection* into Android classes manually. Hilt *predefines* the standard Android components and scopes, *generates* the component classes, and *handles* injecting Android classes (`@AndroidEntryPoint`) and ViewModels (`@HiltViewModel`) — so you just write modules and annotations. It's opinionated Dagger for Android, removing the repetitive component plumbing."},{t:"list",items:["**Predefined components/scopes** — no manual component definitions.","**Generated wiring** — Hilt creates the components and injectors.","**Android integration** — `@AndroidEntryPoint`/`@HiltViewModel` built in.","**You write** — modules + annotations, not component plumbing."]},{t:"note",text:"Plain Dagger requires defining components, their interfaces, lifecycles, and manual Android injection. Hilt predefines standard Android components/scopes, generates the component classes, and handles injecting Android classes (@AndroidEntryPoint) and ViewModels (@HiltViewModel) — so you just write modules + annotations. Opinionated Dagger for Android."}]},{level:"senior",q:"How do you choose which component to @InstallIn?",a:[{t:"p",text:"Install a module in the component matching where the dependency should be *available* and *scoped*. `SingletonComponent` for app-wide singletons (repositories, Retrofit, database); `ViewModelComponent` for ViewModel-scoped things; `ActivityComponent` for Activity-lifecycle dependencies. Installing too high (Singleton for everything) can over-retain; too low limits availability. Match availability + lifetime."},{t:"list",items:["**`SingletonComponent`** — app-wide (repos, network, DB).","**`ViewModelComponent`** — ViewModel-scoped deps.","**`ActivityComponent`** — Activity-lifecycle deps.","**Match** — availability + intended lifetime; don't over-scope."]},{t:"note",text:"Install a module in the component matching where the dependency should be available/scoped: SingletonComponent (app-wide singletons — repos/network/DB), ViewModelComponent (ViewModel-scoped), ActivityComponent (Activity lifecycle). Don't install everything in Singleton (over-retention); match availability + lifetime."}]},{level:"senior",q:"How do you provide different bindings for debug versus release?",a:[{t:"p",text:"Put build-variant-specific modules in the corresponding source sets (`src/debug`, `src/release`), each providing the same binding differently (e.g. a real vs a fake/mock implementation, or different config). Gradle compiles the right one per variant. Alternatively, use a `BuildConfig` flag inside a shared module's `@Provides`. This lets debug builds use test servers/loggers without affecting release."},{t:"list",items:["**Variant source sets** — `src/debug`/`src/release` modules.","**Same binding, different impl** — real vs fake/config per variant.","**Or `BuildConfig` flag** — branch inside a shared `@Provides`.","**Uses** — debug loggers/test servers/mock data."]},{t:"note",text:"Provide variant-specific bindings by placing modules in src/debug and src/release source sets (each providing the same type differently — real vs fake, different config); Gradle picks per variant. Or branch on a BuildConfig flag in a shared @Provides. Lets debug use test servers/loggers without affecting release."}]},{level:"senior",q:"How does Hilt handle a ViewModel's SavedStateHandle and process death?",a:[{t:"p",text:"Hilt automatically provides `SavedStateHandle` to a `@HiltViewModel`'s constructor — you just declare it as a parameter. This gives the ViewModel access to saved state that survives process death (the ids/inputs you store there), combining Hilt's DI with process-death resilience. No extra factory or wiring is needed; Hilt integrates with the ViewModel's `SavedStateHandle`."},{t:"code",title:"SavedStateHandle injection",code:`@HiltViewModel
class DetailViewModel @Inject constructor(
    private val repo: Repository,
    savedStateHandle: SavedStateHandle,   // survives process death; Hilt provides it
) : ViewModel() {
    private val id: String = savedStateHandle["id"]!!
}`},{t:"list",items:["**Auto-provided** — declare `SavedStateHandle` as a constructor param.","**Process-death survival** — saved state restores after a kill.","**DI + resilience** — Hilt deps + SavedStateHandle together.","**No extra wiring** — Hilt integrates it."]},{t:"note",text:"Hilt auto-provides SavedStateHandle to a @HiltViewModel constructor — just declare it as a parameter. It gives the ViewModel process-death-surviving saved state (ids/inputs), combining Hilt DI with resilience, no extra factory/wiring needed."}]},{level:"junior",q:"What are common Hilt errors, and how do you fix them?",a:[{t:"p",text:"Frequent build errors: *missing binding* ('cannot be provided without an @Inject constructor or @Provides') — add a `@Provides`/`@Binds` or `@Inject` constructor; *wrong component* — a binding installed in a lower component than where it's injected; *missing `@AndroidEntryPoint`* — field injection into an un-annotated class; *missing `@HiltAndroidApp`*; and *scope mismatch* (a scoped binding in the wrong component). Read the error — Hilt's messages usually name the missing type and location."},{t:"list",items:["**Missing binding** — add `@Provides`/`@Binds`/`@Inject`.","**Wrong `@InstallIn`** — binding not available where injected.","**Missing `@AndroidEntryPoint`/`@HiltAndroidApp`** — no injection setup.","**Scope mismatch** — scoped binding in the wrong component."]},{t:"note",text:"Common Hilt errors: missing binding ('cannot be provided...' — add @Provides/@Binds/@Inject), wrong @InstallIn component (not available where injected), missing @AndroidEntryPoint/@HiltAndroidApp, scope mismatch. Hilt's compile errors usually name the missing type and location — read them."}]},{level:"senior",q:"What is @ActivityRetainedScoped, and how does it differ from @Singleton?",a:[{t:"p",text:"`@ActivityRetainedScoped` (in `ActivityRetainedComponent`) makes a binding a single instance that *survives configuration changes* but is *scoped to the Activity* (cleared when the Activity truly finishes) — the same lifetime as a ViewModel. `@Singleton` lives for the *whole app*. Use `@ActivityRetainedScoped` for state that should be shared across a screen's config changes but not app-wide."},{t:"list",items:["**`@ActivityRetainedScoped`** — survives config change; cleared on Activity finish (ViewModel-like).","**`@Singleton`** — app lifetime.","**Use retained** — per-screen state across rotation, not app-wide.","**Component** — `ActivityRetainedComponent` vs `SingletonComponent`."]},{t:"note",text:"@ActivityRetainedScoped (ActivityRetainedComponent) is a single instance surviving config changes but scoped to the Activity (cleared on true finish — ViewModel lifetime); @Singleton lives for the whole app. Use retained scope for per-screen state that survives rotation but isn't app-wide."}]},{level:"junior",q:"What is the minimal setup to add Hilt to an app?",a:[{t:"p",text:"Add the Hilt Gradle plugin and dependencies (with KSP/kapt), annotate the `Application` with `@HiltAndroidApp`, annotate Android classes that need injection with `@AndroidEntryPoint`, mark ViewModels `@HiltViewModel`, and create `@Module @InstallIn` classes with `@Provides`/`@Binds` for dependencies Hilt can't construct. Then constructor/field injection works throughout."},{t:"list",items:["**Plugin + deps** — Hilt Gradle plugin, KSP/kapt.","**`@HiltAndroidApp`** — on the Application.","**`@AndroidEntryPoint`/`@HiltViewModel`** — on Android classes/ViewModels.","**Modules** — `@Provides`/`@Binds` for un-constructable deps."]},{t:"note",text:"Hilt setup: add the plugin + deps (KSP/kapt), @HiltAndroidApp on the Application, @AndroidEntryPoint on injected Android classes, @HiltViewModel on ViewModels, and @Module @InstallIn classes with @Provides/@Binds for un-constructable dependencies. Then injection works throughout."}]},{level:"senior",q:"How do you migrate from manual Dagger to Hilt?",a:[{t:"p",text:"Migrate incrementally: add Hilt, annotate the `Application` `@HiltAndroidApp`, then convert your existing Dagger modules to `@InstallIn` the appropriate Hilt components (reusing the `@Provides`/`@Binds`), replace manual component injection with `@AndroidEntryPoint`, and convert ViewModel factories to `@HiltViewModel`. Hilt can coexist with custom Dagger components during the transition via `@EntryPoint`/`@DefineComponent`."},{t:"list",items:["**Add `@HiltAndroidApp`** — the entry point.","**Reuse modules** — add `@InstallIn` to existing `@Provides`/`@Binds`.","**Replace injection** — `@AndroidEntryPoint`, `@HiltViewModel`.","**Coexist** — `@EntryPoint`/custom components during transition."]},{t:"note",text:"Migrate to Hilt incrementally: @HiltAndroidApp on the Application, add @InstallIn to existing Dagger modules (reuse @Provides/@Binds), replace manual injection with @AndroidEntryPoint, convert ViewModel factories to @HiltViewModel. Hilt coexists with custom Dagger components via @EntryPoint/@DefineComponent during the transition."}]},{level:"senior",q:"How do you do assisted injection with Hilt?",a:[{t:"p",text:"For a class needing both DI-provided and *runtime* parameters, use `@AssistedInject` with `@Assisted` on the runtime params and an `@AssistedFactory` interface — Hilt generates the factory, which you can then inject. Common for objects that need a runtime id along with injected dependencies (though ViewModels usually use `SavedStateHandle` instead)."},{t:"code",title:"Assisted injection in Hilt",code:`class ImageLoader @AssistedInject constructor(
    private val cache: Cache,               // injected
    @Assisted private val config: Config,   // runtime
)
@AssistedFactory interface ImageLoaderFactory { fun create(config: Config): ImageLoader }`},{t:"list",items:["**`@AssistedInject` + `@Assisted`** — mix injected + runtime params.","**`@AssistedFactory`** — generated factory you inject.","**Uses** — objects needing a runtime arg + dependencies.","**ViewModels** — often use SavedStateHandle for the runtime id."]},{t:"note",text:"Hilt assisted injection: @AssistedInject constructor with @Assisted on runtime params + an @AssistedFactory interface Hilt generates and you inject. For objects needing a runtime arg alongside injected deps. ViewModels usually use SavedStateHandle for the runtime id instead."}]},{level:"senior",q:"How do you inject dependencies into a Service or BroadcastReceiver?",a:[{t:"p",text:"Annotate the `Service` or `BroadcastReceiver` with `@AndroidEntryPoint` and use field injection (`@Inject lateinit var`). Hilt generates a `ServiceComponent`/handles the receiver and populates the fields. For a receiver, Hilt injects in `onReceive`; ensure the receiver isn't doing long work there. This is the same pattern as Activities/Fragments."},{t:"code",title:"@AndroidEntryPoint Service",code:`@AndroidEntryPoint
class SyncService : Service() {
    @Inject lateinit var repo: Repository   // injected by Hilt
}`},{t:"list",items:["**`@AndroidEntryPoint`** — on the Service/Receiver.","**Field injection** — `@Inject lateinit var`.","**`ServiceComponent`** — for service-scoped deps.","**Receiver** — injected in `onReceive`; keep it fast."]},{t:"note",text:"Inject into a Service/BroadcastReceiver by annotating it @AndroidEntryPoint and using field injection (@Inject lateinit var) — Hilt generates the ServiceComponent/handles the receiver. Receivers are injected in onReceive (keep it fast). Same pattern as Activities/Fragments."}]},{level:"junior",q:"How do you provide a coroutine dispatcher or scope via Hilt?",a:[{t:"p",text:"Provide dispatchers through a module with qualifiers so they're injectable and swappable in tests. Define `@Qualifier` annotations (`@IoDispatcher`, `@DefaultDispatcher`) and `@Provides` each; also provide an application-scoped `CoroutineScope` for app-lifetime work. Then repositories inject the dispatcher instead of hardcoding `Dispatchers.IO`, enabling test dispatchers."},{t:"code",title:"Dispatcher module",code:`@Qualifier annotation class IoDispatcher
@Module @InstallIn(SingletonComponent::class)
object DispatchersModule {
    @Provides @IoDispatcher fun io(): CoroutineDispatcher = Dispatchers.IO
    @Provides @Singleton fun appScope(@IoDispatcher d: CoroutineDispatcher) =
        CoroutineScope(SupervisorJob() + d)
}`},{t:"list",items:["**Qualified dispatchers** — `@IoDispatcher`/`@DefaultDispatcher` `@Provides`.","**Inject, don't hardcode** — repos take the dispatcher.","**App scope** — provide a `@Singleton CoroutineScope` for app-lifetime work.","**Testable** — swap a test dispatcher via `@BindValue`/test module."]},{t:"note",text:"Provide dispatchers via a module with @Qualifier annotations (@IoDispatcher/@DefaultDispatcher @Provides), plus a @Singleton application CoroutineScope. Repositories inject the dispatcher instead of hardcoding Dispatchers.IO — enabling a test dispatcher in tests (via @BindValue/test module)."}]},{level:"senior",q:"How do you provide a dependency that itself depends on multiple others?",a:[{t:"p",text:"Just declare the dependencies as *parameters* of the `@Provides` function (or the `@Inject` constructor) — Hilt resolves and injects each of them transitively. You never construct the dependency graph manually; Hilt builds it. As long as each parameter is itself provided (via `@Provides`/`@Binds`/`@Inject`), Hilt wires the whole chain."},{t:"code",title:"Transitive provides",code:`@Provides @Singleton
fun provideRepo(api: Api, dao: UserDao, dispatcher: CoroutineDispatcher): Repository =
    RepositoryImpl(api, dao, dispatcher)   // Hilt injects api, dao, dispatcher
`},{t:"list",items:["**Params are injected** — `@Provides`/constructor parameters resolved transitively.","**No manual wiring** — Hilt builds the graph.","**Each must be provided** — via `@Provides`/`@Binds`/`@Inject`.","**Chain resolved** — the whole dependency tree assembled."]},{t:"note",text:"Declare the sub-dependencies as parameters of the @Provides function or @Inject constructor — Hilt resolves and injects each transitively, building the whole graph. You never wire it manually; as long as every parameter is itself provided (@Provides/@Binds/@Inject), Hilt assembles the chain."}]}],rC={id:"hilt",title:"Hilt (Android DI)",tagline:"Hilt as Dagger-for-Android, setup annotations, predefined components & scopes, @InstallIn, ViewModel injection, qualifiers, and WorkManager/dynamic-feature gotchas.",tags:["Hilt","@HiltViewModel","@InstallIn","Qualifiers","Components"],content:oC,qa:sC},lC=[{heading:"Koin — runtime DI",blocks:[{t:"p",text:"**Koin** is a dependency injection library that takes the opposite approach to Dagger/Hilt: it resolves dependencies **at runtime** using a service-locator-style registry, written in pure Kotlin with a DSL — no annotation processing, no code generation. You declare *modules* describing how to build each dependency, start Koin with those modules, and request dependencies with `get()`/`inject()`. It's simpler and faster to learn than Dagger, and — importantly — it's **multiplatform**, so it works in KMP shared code where Hilt (Android-only) can't."},{t:"code",title:"A Koin module and usage",code:`// Declare how to build each dependency (a DSL, not annotations)
val appModule = module {
    single { OkHttpClient() }                          // single = singleton
    single { Retrofit.Builder().client(get()).build() } // get() resolves dependencies
    single<UserRepository> { UserRepositoryImpl(get()) } // bind interface to impl
    factory { SomeUseCase(get()) }                     // factory = new instance each time
    viewModel { UserViewModel(get()) }                 // ViewModel support
}

// Start Koin (in Application)
startKoin { modules(appModule) }

// Retrieve dependencies
class MyActivity : ComponentActivity() {
    private val repo: UserRepository by inject()       // lazy injection
    private val vm: UserViewModel by viewModel()
}`},{t:"list",items:["**`module { }`** — a DSL block declaring providers. `single { }` = one shared instance (like `@Singleton`); `factory { }` = a new instance each request; `viewModel { }` = ViewModel support. `get()` inside resolves the needed dependencies from the registry.","**No codegen** — Koin is a runtime registry; you `startKoin { modules(...) }` and then `get()`/`by inject()` pull from it. Fast build (no annotation processing), pure Kotlin.","**`by inject()` (lazy) / `get()` (immediate)** — retrieve dependencies, typically in Android entry points."]}]},{heading:"Koin vs Hilt/Dagger — the trade-off",blocks:[{t:"table",headers:["","Hilt/Dagger","Koin"],rows:[["Resolution","compile-time (codegen)","runtime (registry)"],["Missing dependency","**build error**","**runtime crash** (when requested)"],["Reflection/codegen","codegen, no reflection","no codegen, minimal reflection"],["Build time","slower (annotation processing)","faster (no processing)"],["Learning curve","steep","gentle"],["Multiplatform (KMP)","Android-only","**multiplatform**"],["Runtime performance","faster (generated code)","slightly slower (runtime lookup)"]]},{t:"list",items:["**The core trade-off — compile-time safety vs simplicity**: Hilt catches a missing/cyclic dependency at *build time* (a build error you fix before shipping); Koin only fails at *runtime* when that dependency is requested (a potential crash in production if a path wasn't exercised). That compile-time guarantee is Hilt's biggest advantage; Koin trades it for a much gentler learning curve, faster builds, and less boilerplate.","**Choose Hilt** for Android-only apps where compile-time safety and Google's recommendation matter — it's the standard.","**Choose Koin** for KMP (it's multiplatform — Hilt can't run in shared code), for smaller apps/teams valuing simplicity, or when build time / codegen is a concern. Many KMP projects use Koin precisely because it works across all targets.","**For an Android/KMP engineer**: Hilt for Android modules, Koin for shared KMP code — a common combination."]}]},{heading:"DI in Kotlin Multiplatform",blocks:[{t:"p",text:"In a KMP project, the shared code (`commonMain`) needs DI too — but Hilt/Dagger are JVM/Android-only and can't run there. The options:"},{t:"list",items:["**Koin** — the most popular KMP DI choice. It's fully multiplatform, so you define your shared modules in `commonMain` and start Koin from each platform (Android's Application, iOS's app init). The same DI setup works everywhere.","**Manual DI / a hand-written factory** — for smaller shared layers, a plain factory or 'component' class constructed in common code works fine, with platform-specific pieces injected via `expect/actual`. No library needed.","**Kotlin-Inject / Metro / other codegen options** — newer compile-time DI libraries that support KMP are emerging, offering Dagger-like compile-time safety in multiplatform. Worth knowing they exist.","**The common pattern**: shared modules define most dependencies; platform-specific ones (a database driver, a settings implementation) are provided via `expect/actual` or passed in when starting the DI container per platform. Android might *also* use Hilt at the app layer, bridging to the shared Koin/factory graph."]}]},{heading:"DI and testing — the payoff",blocks:[{t:"p",text:"DI's biggest benefit is testability, and it shows up concretely in *how easy tests become*. Because classes receive their dependencies, tests simply *inject fakes* — no framework magic needed for unit tests."},{t:"code",title:"Unit test — inject a fake, no DI framework needed",code:`@Test
fun loadUser_success() = runTest {
    val fakeRepo = FakeUserRepository(user = testUser)   // a fake implementation
    val viewModel = UserViewModel(fakeRepo, SavedStateHandle())  // just construct it!

    viewModel.loadUser("42")

    assertEquals(testUser, viewModel.uiState.value.user)
}`},{t:"list",items:["**Unit tests don't use the DI framework at all** — since the ViewModel takes its dependencies via constructor, you just *construct it directly* with fakes. The whole point of constructor injection is that the class is trivially instantiable in a test. This is why constructor injection is preferred and why DI is described as 'making code testable'.","**Prefer fakes over mocks** — a `FakeUserRepository` (a real in-memory implementation of the interface) usually reads better and is less brittle than a mock; both work because the ViewModel depends on the interface.","**For instrumented/integration tests, Hilt has test support** — `@HiltAndroidTest`, `@UninstallModules` (remove a production module), and `@BindValue` / test modules let you *replace* production dependencies with fakes in the Hilt graph for on-device tests. So Hilt swaps real modules for test ones at the framework level when you need the full graph.","**Koin has `koin-test`** — utilities to declare test modules and verify the graph (`checkModules()` catches missing bindings in a test, partially recovering the compile-time safety Koin lacks)."]},{t:"note",text:"Wrap-up: Koin = runtime DI (registry + DSL, no codegen, multiplatform, gentle learning curve) vs Hilt/Dagger = compile-time (codegen, build-time safety, Android-only). Trade-off: compile-time safety (Hilt catches missing deps at build) vs simplicity + KMP support (Koin). For KMP shared code use Koin (or manual DI / newer KMP codegen libs); Hilt can't go there. DI's testing payoff: constructor injection means unit tests just construct classes with fakes — no framework needed; Hilt/Koin provide test-module swapping for integration tests."}]}],cC=[{level:"junior",q:"What is Koin and how does it differ from Hilt?",a:[{t:"p",text:"**Koin is a dependency injection library that resolves dependencies at *runtime* using a registry, written in pure Kotlin with a DSL — no annotation processing or code generation. This is the opposite of Hilt/Dagger, which resolve dependencies at *compile time* via code generation.** You declare modules describing how to build each dependency, start Koin with them, and retrieve dependencies with `get()`/`by inject()`."},{t:"code",title:"Koin's DSL style",code:`val appModule = module {
    single { Retrofit.Builder().build() }
    single<UserRepository> { UserRepositoryImpl(get()) }
    viewModel { UserViewModel(get()) }
}
startKoin { modules(appModule) }`},{t:"list",items:["**Runtime vs compile-time** — the fundamental difference. Koin builds the graph when the app runs; Hilt builds it when you compile.","**Missing dependency**: with Hilt it's a *build error*; with Koin it's a *runtime crash* when that dependency is first requested. This is Koin's biggest downside.","**Simplicity** — Koin has a much gentler learning curve, no codegen (faster builds), less boilerplate, and a readable DSL. That's its main appeal.","**Multiplatform** — Koin works in KMP shared code; Hilt is Android-only."]},{t:"p",text:"So the short version: Koin trades Hilt's compile-time safety for simplicity and multiplatform support. Hilt is the Android standard (Google-recommended, compile-time safe); Koin is popular for smaller apps, teams preferring simplicity, and especially KMP projects where Hilt can't run."}]},{level:"junior",q:"Why is dependency injection good for testing?",a:[{t:"p",text:"**Because DI lets you replace real dependencies with fakes in tests — and with constructor injection, this is trivial: you just construct the class with fake dependencies, no framework needed.** A class that takes its dependencies via constructor is directly instantiable in a test with whatever you want to pass in."},{t:"code",title:"Testing with a fake — just construct it",code:`@Test fun test() = runTest {
    val fakeRepo = FakeUserRepository(user = testUser)   // fake implementation
    val vm = UserViewModel(fakeRepo, SavedStateHandle()) // construct directly

    vm.loadUser("42")
    assertEquals(testUser, vm.uiState.value.user)
}`},{t:"p",text:"Contrast this with a class that creates its own dependencies (`val repo = RealRepository(realApi, realDb)`) — in a test you can't replace those, so you're stuck with real network calls and a real database, making tests slow, flaky, and hard to control. With DI, you inject a `FakeUserRepository` that returns canned data, and the test is fast, isolated, and deterministic. Notice that **unit tests don't use the DI framework at all** — the whole benefit of constructor injection is that the class is plainly constructable, so you bypass Hilt/Koin entirely and just `new` the object with fakes. This is the concrete reason DI is described as 'what makes code testable', and it's why constructor injection is preferred over field injection (field-injected classes are harder to set up in tests). For full integration tests that need the real graph, Hilt and Koin both provide ways to swap in test modules."}]},{level:"junior",q:"What is the difference between 'single' and 'factory' in Koin?",a:[{t:"p",text:"**They control instance lifetime — `single` creates one shared instance (a singleton), while `factory` creates a new instance every time the dependency is requested.** This is Koin's equivalent of scoped vs unscoped in Dagger/Hilt."},{t:"list",items:["**`single { }`** — Koin creates the instance once and reuses it for all requests. Use for expensive or shared objects that should have one instance: the database, the Retrofit client, repositories. Equivalent to `@Singleton` in Hilt.","**`factory { }`** — Koin creates a fresh instance on each request. Use for lightweight, stateless objects where a shared instance isn't needed or where you specifically want a new one each time. Equivalent to unscoped in Dagger.","**`viewModel { }`** — a special one for ViewModels, scoped to the ViewModel lifecycle (like `@HiltViewModel`), so it survives configuration changes appropriately.","**`scoped { }`** — instances tied to a custom scope's lifetime (e.g. an Activity scope), between singleton and factory."]},{t:"p",text:"The decision is the same as choosing scopes anywhere: match the instance's lifetime to how it should be shared. A database is a `single` (one for the app — multiple would be wasteful and could conflict); a short-lived stateless helper can be a `factory`. Getting this right matters for both correctness (sharing stateful objects unintentionally causes bugs) and resources (unnecessary singletons stay in memory). It's the runtime-DSL expression of the same scoping concept that `@Singleton`/unscoped represents in Hilt."}]},{level:"senior",q:"How do you handle dependency injection in a Kotlin Multiplatform project?",a:[{t:"p",text:"**The main constraint is that Hilt/Dagger are JVM/Android-only and cannot run in shared `commonMain` code, so you need a multiplatform-capable DI approach for the shared layer. The most common choice is Koin (fully multiplatform), with manual DI as a lightweight alternative, and newer KMP-capable codegen libraries emerging.**"},{t:"list",items:["**Koin — the popular choice**: it's multiplatform, so you define your shared DI modules in `commonMain` (repositories, use cases, the network client, etc.) and start Koin from *each* platform's entry point (Android's `Application`, iOS's app initialization). The same module definitions serve all targets. This is why many KMP projects standardize on Koin — one DI setup across Android and iOS.","**Manual DI** — for a smaller shared layer, a hand-written factory or 'component' class constructed in common code works without any library. Platform-specific dependencies (a SQLDelight database driver, a settings implementation, an HTTP engine) are provided via `expect/actual` or passed in when constructing the shared graph. No library overhead, full control.","**Platform-specific pieces via expect/actual**: regardless of approach, some dependencies are inherently platform-specific (database driver, file paths, secure storage). These are declared `expect` in common and `actual` per platform, then injected into the shared graph — so the shared modules stay platform-agnostic while the concrete platform bits are supplied where needed.","**Newer options**: KMP-capable compile-time DI libraries (kotlin-inject, Metro, etc.) are maturing, offering Dagger-like compile-time safety in multiplatform — worth knowing as the space evolves, though Koin/manual remain the pragmatic mainstream."]},{t:"list",items:["**A common hybrid**: the shared KMP layer uses Koin (or manual DI), and the Android app layer *also* uses Hilt for Android-specific wiring (ViewModels, WorkManager), bridging to the shared graph. So an Android/KMP engineer often works with *both* — Hilt in the Android module, Koin in shared code.","**iOS consumption**: the shared DI graph is initialized from Swift (calling the shared `startKoin`/factory), and iOS retrieves shared dependencies through the Kotlin framework interface. Koin has Swift-friendly helpers for this."]},{t:"p",text:"**The senior framing**: KMP DI is fundamentally about the shared layer needing a *platform-neutral* solution, since the Android-standard Hilt can't cross the boundary. Koin is the mainstream answer (multiplatform, same modules everywhere), manual DI works for smaller graphs, and platform specifics are handled via `expect/actual`. The realistic professional setup often combines Hilt (Android app layer) with Koin/manual DI (shared KMP layer) — and being able to explain *why* Hilt stops at the Android boundary and what fills the gap in common code is exactly the KMP-aware answer an interviewer for an Android/KMP role is looking for."}]},{level:"senior",q:"How do you swap dependencies for testing in Hilt vs Koin?",a:[{t:"p",text:"**For pure unit tests, neither framework is involved — you construct classes directly with fakes (thanks to constructor injection). The framework-level swapping matters for *integration/instrumented tests* that exercise the real object graph, and both Hilt and Koin provide mechanisms to replace production dependencies with test doubles.**"},{t:"list",items:["**The unit-test case (no framework)**: because your classes take dependencies via constructor, a unit test just does `UserViewModel(fakeRepo, SavedStateHandle())` — you bypass Hilt/Koin entirely and pass fakes directly. This is the common case and the primary testing payoff of DI; you almost never need the framework for unit tests.","**Hilt integration testing**: use `@HiltAndroidTest` on the test class (with a `HiltAndroidRule`). To replace a production dependency, you either **`@UninstallModules(ProductionModule::class)`** and provide a test module in its place, or use **`@BindValue`** to bind a specific test instance into the graph for that test. Hilt then builds the graph with your test doubles instead of the real ones, so an on-device test exercises the real wiring with fakes at the boundaries (e.g. a fake network layer). This is how you test full flows through the Hilt-managed graph.","**Koin integration testing (`koin-test`)**: you start Koin in the test with a *test module* that overrides the production bindings (Koin lets a later module's definitions override earlier ones), so `get()`/`by inject()` resolve to your fakes. Koin also offers **`checkModules()`** — a test that walks the graph and verifies every dependency can be resolved, which partially recovers the compile-time safety Koin otherwise lacks by catching missing bindings *in a test* rather than in production."]},{t:"list",items:["**The philosophical difference in testability**: Hilt's compile-time graph means wiring errors are caught at build time regardless of tests; Koin's runtime graph means you *should* add a `checkModules()` test to catch missing bindings, since they'd otherwise only surface at runtime. So with Koin, part of your test suite compensates for the lack of compile-time verification.","**Best practice regardless**: keep the *unit* test layer framework-free (construct with fakes) — it's fast and simple — and reserve framework-level module swapping (`@UninstallModules`/`@BindValue` in Hilt, override modules in Koin) for the smaller set of *integration* tests that genuinely need the assembled graph."]},{t:"p",text:"**The senior framing**: DI's testing story has two layers — unit tests (bypass the framework, inject fakes via constructor; the bulk of your tests) and integration tests (use the framework's swap mechanism to substitute fakes into the real graph). Hilt provides `@UninstallModules`/`@BindValue`/test modules; Koin provides override modules plus `checkModules()` to catch its runtime-resolution risk in a test. The mature approach is to lean heavily on framework-free unit tests (which is *why* you did constructor injection) and use graph-swapping sparingly for the few tests that need it — and to add `checkModules()` when using Koin to recover some of the safety Hilt gets for free."}]},{level:"junior",q:"How do you define and start a Koin module?",a:[{t:"p",text:"A Koin `module { }` is a DSL declaring how to create dependencies — `single { }` for singletons, `factory { }` for new instances, `viewModel { }` for ViewModels. You then `startKoin { modules(...) }` (usually in the Application) to build the container. It's runtime DI: definitions are resolved when requested."},{t:"code",title:"Koin module",code:`val appModule = module {
    single { Retrofit.Builder()...create(Api::class.java) }
    single<Repository> { RepositoryImpl(get(), get()) }   // get() resolves deps
    viewModel { HomeViewModel(get()) }
}
// Application: startKoin { androidContext(this@App); modules(appModule) }`},{t:"list",items:["**`module { }`** — DSL of definitions.","**`single`/`factory`/`viewModel`** — instance strategies.","**`get()`** — resolve a dependency inside a definition.","**`startKoin { modules(...) }`** — build the container."]},{t:"note",text:"A Koin module { } declares definitions: single { } (singleton), factory { } (new each time), viewModel { } (ViewModel); get() resolves dependencies inside a definition. startKoin { androidContext(...); modules(...) } builds the container (in the Application). It's runtime DI — resolved on request."}]},{level:"junior",q:"How do you inject dependencies with Koin (by inject, get, by viewModel)?",a:[{t:"p",text:"In Android classes, use `by inject<T>()` (lazy) or `get<T>()` (eager) to retrieve dependencies, and `by viewModel<T>()` for ViewModels. Constructor injection works by having definitions call `get()`. Koin resolves the type from its modules at the point of retrieval — no annotations or code generation."},{t:"code",title:"Retrieving dependencies",code:`class MyActivity : ComponentActivity() {
    private val repo: Repository by inject()      // lazy
    private val viewModel: HomeViewModel by viewModel()
}`},{t:"list",items:["**`by inject()`** — lazy dependency retrieval.","**`get()`** — eager retrieval.","**`by viewModel()`** — Koin-provided ViewModel.","**No annotations** — runtime resolution from modules."]},{t:"note",text:"Koin retrieval: by inject<T>() (lazy) / get<T>() (eager) for dependencies, by viewModel<T>() for ViewModels; definitions use get() for constructor deps. No annotations or codegen — Koin resolves types from modules at retrieval time (runtime DI)."}]},{level:"senior",q:"How do you scope dependencies in Koin?",a:[{t:"p",text:"Beyond `single` (app-wide) and `factory` (new each time), Koin has *scopes* — `scope<MyActivity> { scoped { ... } }` creates instances tied to a scope's lifetime (e.g. an Activity), released when the scope closes. Koin also provides lifecycle-aware scopes (`activityScope`, `fragmentScope`). Scoped instances are single within their scope, like Dagger's lifecycle scopes but resolved at runtime."},{t:"list",items:["**`single`** — app-wide singleton.","**`factory`** — new instance each request.","**`scoped` in a `scope`** — tied to a scope's lifetime.","**Lifecycle scopes** — `activityScope`/`fragmentScope`."]},{t:"note",text:"Koin scoping: single (app-wide), factory (new each time), and scope<T> { scoped { } } for lifetime-tied instances (released when the scope closes), plus lifecycle-aware activityScope/fragmentScope. Scoped instances are single within their scope — like Dagger scopes but resolved at runtime."}]},{level:"senior",q:"What are the trade-offs of Koin versus Hilt?",a:[{t:"p",text:"*Koin* is runtime DI (a Kotlin DSL) — simple setup, no annotation processing/codegen (fast builds), KMP-friendly, easy to learn. But errors surface *at runtime* (missing definition = crash when requested), and there's a small runtime cost. *Hilt* is compile-time — errors caught at build, no reflection (fast runtime), but more build time and ceremony. Choose Koin for simplicity/KMP/fast builds; Hilt for compile-time safety in large Android apps."},{t:"table",headers:["","Koin","Hilt"],rows:[["Resolution","runtime (DSL)","compile-time (codegen)"],["Errors","at runtime","at build"],["Build cost","low","annotation processing"],["KMP","yes","no"]]},{t:"list",items:["**Koin** — runtime DSL, simple, fast builds, KMP; runtime errors.","**Hilt** — compile-time safe, no reflection; more build cost/ceremony.","**Koin for** — simplicity, KMP, small/medium apps.","**Hilt for** — compile-time safety in large Android apps."]},{t:"note",text:"Koin: runtime DSL — simple, no codegen (fast builds), KMP-friendly, but errors at runtime (missing definition crashes) + slight runtime cost. Hilt: compile-time — errors caught at build, no reflection, but more build cost/ceremony. Koin for simplicity/KMP; Hilt for compile-time safety in large Android apps."}]},{level:"senior",q:"How do you expose a shared Koin graph to iOS in a KMP project?",a:[{t:"p",text:"Define the shared Koin modules in `commonMain` and provide a Kotlin *initializer function* (e.g. `initKoin()`) that iOS calls from Swift to start Koin. Since iOS can't use Koin's DSL directly from Swift ergonomically, expose *helper accessor functions* (or a `KoinComponent` facade) in shared code that Swift calls to obtain dependencies. This lets the iOS app bootstrap and use the shared graph."},{t:"list",items:["**Shared modules** — in `commonMain`.","**`initKoin()`** — a Kotlin function iOS calls to start Koin.","**Accessor facade** — helper functions Swift calls for deps.","**Bootstrap** — iOS app starts and uses the shared graph."]},{t:"note",text:"Expose the shared Koin graph to iOS: define modules in commonMain, provide a Kotlin initializer (initKoin()) iOS calls from Swift, and expose accessor helper functions (or a KoinComponent facade) in shared code that Swift calls to obtain dependencies — since Swift can't ergonomically use Koin's DSL directly."}]},{level:"senior",q:"How do you swap dependencies for testing in Koin?",a:[{t:"p",text:"Because Koin resolves at runtime, you can override definitions easily: start Koin with a *test module* that provides fakes (or use `koinApplication`/`startKoin` with `modules(testModule)`), or use Koin's test support (`KoinTest`, `declareMock`, `checkModules`). Overriding a definition with the same type replaces the real one — simple to inject fakes without recompiling a component."},{t:"list",items:["**Test module** — provide fakes; start Koin with it.","**Override** — same-type definition replaces the real one.","**`KoinTest`/`declareMock`** — test support.","**`checkModules`** — verify the graph resolves."]},{t:"note",text:"Koin's runtime resolution makes test-swapping easy: start Koin with a test module providing fakes (or override definitions — same type replaces the real one), using KoinTest/declareMock. checkModules verifies the graph resolves. No component recompile needed to inject fakes."}]},{level:"junior",q:"How do you handle constructor injection with Koin's DSL?",a:[{t:"p",text:"In a Koin definition, call `get()` for each constructor parameter — Koin resolves them from the graph. So a class uses normal constructor injection (dependencies as parameters), and the module's definition wires them with `get()`. This keeps classes framework-agnostic (they don't know about Koin) while the module does the wiring."},{t:"code",title:"get() wiring",code:`class UserRepository(private val api: Api, private val dao: UserDao)   // plain constructor
val module = module {
    single { UserRepository(get(), get()) }   // Koin resolves api and dao
}`},{t:"list",items:["**`get()` per parameter** — Koin resolves each from the graph.","**Plain constructors** — classes are framework-agnostic.","**Module wires** — the definition supplies dependencies.",'**Named/params** — `get(named("x"))` / `get { parametersOf(...) }`.']},{t:"note",text:`In a Koin definition, call get() for each constructor parameter — Koin resolves them from the graph. Classes use plain constructor injection (framework-agnostic, don't know Koin); the module does the wiring. Use get(named("x")) for qualified deps and parametersOf for runtime params.`}]},{level:"junior",q:"How do you bind an interface to an implementation in Koin?",a:[{t:"p",text:"Specify the *interface type* on the definition — `single<Repository> { RepositoryImpl(get()) }` binds the `Repository` interface to `RepositoryImpl`, so injecting `Repository` gives you the impl. Without the explicit type, Koin registers it as `RepositoryImpl`. This is how you program to interfaces with Koin (the equivalent of Dagger's `@Binds`)."},{t:"code",title:"Interface binding",code:`single<Repository> { RepositoryImpl(get()) }   // inject Repository -> RepositoryImpl
// consumer:
class ViewModel(private val repo: Repository)   // depends on the interface`},{t:"list",items:["**`single<Interface> { Impl(...) }`** — bind interface → impl.","**Without the type** — registered as the concrete class.","**Program to interfaces** — consumers depend on the abstraction.","**= Dagger `@Binds`** — the runtime equivalent."]},{t:"note",text:"Specify the interface type: single<Repository> { RepositoryImpl(get()) } binds Repository → RepositoryImpl (injecting Repository gives the impl). Without the type it registers as the concrete class. This is programming-to-interfaces in Koin — the runtime equivalent of Dagger's @Binds."}]},{level:"senior",q:"How do you verify a Koin graph is complete?",a:[{t:"p",text:"Since Koin resolves at runtime, a missing definition only crashes when that dependency is requested — potentially in production. Mitigate with `checkModules()` (a Koin test that attempts to resolve every definition, catching missing dependencies at test time), and `verify()` for module verification. Running these in CI recovers some of the compile-time safety Koin otherwise lacks."},{t:"list",items:["**Runtime resolution risk** — missing definition crashes on request.","**`checkModules()`** — a test resolving all definitions.","**`verify()`** — module verification.","**CI** — run these to catch missing deps before production."]},{t:"note",text:"Koin resolves at runtime, so a missing definition crashes only when requested. Use checkModules() (a test that resolves every definition, catching missing deps at test time) and verify() in CI — recovering some of the compile-time safety Koin lacks. Run them to catch graph gaps before production."}]},{level:"senior",q:"How do you inject a ViewModel with Koin?",a:[{t:"p",text:"Declare it in a module with `viewModel { HomeViewModel(get()) }`, then retrieve it with `by viewModel()` (Android) or `koinViewModel()` (Compose). Koin's `koin-androidx-viewmodel` integration handles the `ViewModelProvider.Factory` and scoping. For a runtime parameter, use `viewModel { (id: String) -> DetailViewModel(id, get()) }` and pass it via `parametersOf`."},{t:"code",title:"Koin ViewModel",code:`val module = module {
    viewModel { HomeViewModel(get()) }
    viewModel { (id: String) -> DetailViewModel(id, get()) }
}
// Compose: val vm = koinViewModel<HomeViewModel>()
// with param: koinViewModel { parametersOf(id) }`},{t:"list",items:["**`viewModel { }`** — declare the ViewModel definition.","**`by viewModel()`/`koinViewModel()`** — retrieve (Views/Compose).","**Parameters** — `viewModel { (arg) -> }` + `parametersOf`.","**Integration** — koin-androidx-viewmodel handles the factory."]},{t:"note",text:"Koin ViewModel: declare viewModel { HomeViewModel(get()) }, retrieve with by viewModel() (Views) or koinViewModel() (Compose); koin-androidx-viewmodel handles the factory/scoping. For runtime params: viewModel { (id) -> DetailViewModel(id, get()) } passed via parametersOf(id)."}]},{level:"senior",q:"How do you pass runtime parameters to a Koin definition?",a:[{t:"p",text:"Koin supports *parameter injection*: a definition can take runtime parameters via a lambda `{ (param) -> ... }`, and you pass them at retrieval with `parametersOf(value)`. This handles the same case as Dagger's assisted injection (mixing injected deps with a runtime id) but at runtime, without a generated factory."},{t:"code",title:"Parameters",code:`single { (userId: String) -> UserSession(userId, get()) }
val session: UserSession = get { parametersOf(currentUserId) }`},{t:"list",items:["**`{ (param) -> }`** — definition takes runtime params.","**`parametersOf(value)`** — pass at retrieval.","**Runtime + injected** — like assisted injection.","**No generated factory** — resolved at runtime."]},{t:"note",text:"Koin parameter injection: a definition takes runtime params via { (param) -> ... } and you pass them with parametersOf(value) at retrieval — handling the assisted-injection case (injected deps + a runtime id) at runtime, without a generated factory."}]},{level:"senior",q:"What DI options exist for Kotlin Multiplatform, and how do they compare?",a:[{t:"p",text:"The main KMP DI options are *Koin* (runtime DSL, most popular, simple, multiplatform) and *Kodein-DI* (another runtime multiplatform container). *Hilt/Dagger are Android-only* (JVM annotation processing) so they can't be used in shared KMP code. Some teams use *manual DI* in `commonMain` (a shared container class) to avoid a framework entirely. Koin is the common choice."},{t:"list",items:["**Koin** — runtime DSL, popular, simple, multiplatform.","**Kodein-DI** — another multiplatform container.","**Manual DI** — a shared container in `commonMain`.","**Not Hilt/Dagger** — Android-only."]},{t:"note",text:"KMP DI options: Koin (runtime DSL, popular, simple, multiplatform), Kodein-DI (another multiplatform container), or manual DI (a shared container in commonMain). Hilt/Dagger are Android-only (JVM annotation processing) — unusable in shared code. Koin is the common choice."}]},{level:"junior",q:"How do you handle Android context and platform dependencies in Koin?",a:[{t:"p",text:"Pass the Android `Context` into Koin at startup with `androidContext(this)` in `startKoin { }`, then retrieve it in definitions with `androidContext()` (or `get()`). For platform-specific dependencies in KMP, use `expect`/`actual` factory functions or separate platform Koin modules — the Android module provides Android impls, the iOS module provides iOS impls."},{t:"code",title:"androidContext",code:`startKoin { androidContext(this@App); modules(appModule) }
val module = module {
    single { AppDatabase.build(androidContext()) }   // Context available
}`},{t:"list",items:["**`androidContext(this)`** — provide the Context at startup.","**`androidContext()`** — retrieve it in definitions.","**Platform deps** — `expect`/`actual` or platform modules.","**KMP** — per-platform module provides platform impls."]},{t:"note",text:"Provide the Android Context with androidContext(this) in startKoin, retrieve with androidContext() in definitions. For KMP platform-specific dependencies, use expect/actual factories or separate platform Koin modules (Android module = Android impls, iOS module = iOS impls)."}]},{level:"senior",q:"What is a fake versus a mock, and which should you inject in tests?",a:[{t:"p",text:"A *fake* is a working lightweight implementation (an in-memory repository) you write; a *mock* is a generated object whose behavior you stub and whose calls you verify (Mockito/MockK). *Prefer fakes* for most tests — they're more realistic, less brittle, and reusable; use mocks for verifying interactions (was `analytics.log()` called?) or when a fake is impractical. Both are enabled by DI."},{t:"list",items:["**Fake** — a real lightweight implementation (in-memory).","**Mock** — stubbed behavior + verifiable calls (MockK/Mockito).","**Prefer fakes** — realistic, less brittle, reusable.","**Mocks for** — interaction verification / impractical fakes."]},{t:"note",text:"A fake is a working lightweight implementation you write (in-memory repo); a mock is a generated stub with verifiable calls (MockK/Mockito). Prefer fakes (realistic, less brittle, reusable); use mocks to verify interactions (was log() called?) or when a fake is impractical. DI enables injecting either."}]},{level:"senior",q:"How do you organize DI modules in a large app?",a:[{t:"p",text:"Split modules by *concern* or *feature*: a network module, database module, and per-feature modules — rather than one giant module. In multi-module apps, each Gradle module owns its DI module(s). This keeps DI maintainable, makes dependencies discoverable, allows feature modules to declare their own bindings, and supports testing modules in isolation. Aggregate them at the app level."},{t:"list",items:["**Split by concern/feature** — network, DB, per-feature modules.","**Per Gradle module** — each owns its DI module(s).","**Discoverable + maintainable** — not one giant module.","**Aggregate at app** — combine into the graph."]},{t:"note",text:"Organize DI modules by concern/feature (network, DB, per-feature) rather than one giant module; in multi-module apps each Gradle module owns its DI module(s), aggregated at the app level. Keeps DI maintainable/discoverable, lets features declare their own bindings, and supports isolated testing."}]},{level:"junior",q:"What Koin extensions exist for Android and Compose?",a:[{t:"p",text:"Koin has Android-specific artifacts: `koin-android` (for `androidContext`, `by inject`, scopes), `koin-androidx-viewmodel` (`by viewModel`), `koin-androidx-workmanager` (worker injection), and `koin-androidx-compose` (`koinViewModel()`, `koinInject()` in composables). These integrate Koin with Android/Compose lifecycles and components idiomatically."},{t:"list",items:["**`koin-android`** — `androidContext`, `by inject`, scopes.","**`koin-androidx-viewmodel`** — `by viewModel`.","**`koin-androidx-compose`** — `koinViewModel()`/`koinInject()`.","**`koin-androidx-workmanager`** — Worker injection."]},{t:"note",text:"Koin Android/Compose extensions: koin-android (androidContext, by inject, scopes), koin-androidx-viewmodel (by viewModel), koin-androidx-compose (koinViewModel()/koinInject()), koin-androidx-workmanager (worker injection) — integrating Koin with Android/Compose lifecycles idiomatically."}]},{level:"senior",q:"How do you handle DI for feature modules that can be dynamically loaded?",a:[{t:"p",text:"Dynamic feature modules complicate DI because they load at runtime. In *Koin*, load the feature's module when the feature starts (`loadKoinModules(featureModule)`) and unload it when done. In *Hilt*, dynamic feature modules can't add to the app component directly — you use `@EntryPoint`/a separate component or expose dependencies from the base module. Koin's runtime nature makes dynamic loading simpler."},{t:"list",items:["**Koin** — `loadKoinModules`/`unloadKoinModules` at feature load/unload.","**Hilt** — dynamic features can't extend the app component; use `@EntryPoint`.","**Base exposes deps** — features consume from the base module.","**Koin simpler** — runtime loading fits dynamic features."]},{t:"note",text:"Dynamic feature modules load at runtime. Koin: loadKoinModules(featureModule) on load, unloadKoinModules on unload. Hilt: dynamic features can't extend the app component — use @EntryPoint or expose deps from the base module. Koin's runtime nature makes dynamic-feature DI simpler than Hilt."}]},{level:"junior",q:"How do you test a repository with an injected fake data source?",a:[{t:"p",text:"Construct the repository with a *fake* API/DAO (an in-memory or scripted implementation of the interface) and test its logic — caching, mapping, error handling — without real network/DB. Since the repository takes its data sources via constructor, you just pass fakes. Assert the repository returns the expected domain data and handles errors correctly."},{t:"code",title:"Repository test",code:`val repo = UserRepository(FakeApi(cannedUsers), FakeDao())
val result = repo.getUsers()
assertEquals(expectedUsers, result)`},{t:"list",items:["**Fake data sources** — in-memory/scripted API/DAO.","**Constructor injection** — pass fakes directly.","**Test logic** — caching, mapping, errors.","**No real I/O** — fast, deterministic."]},{t:"note",text:"Construct the repository with fake data sources (in-memory/scripted API/DAO implementing the interfaces) and test its logic — caching, mapping, error handling — without real network/DB. Constructor injection lets you pass fakes directly; assert the repo returns expected domain data and handles errors."}]},{level:"senior",q:"When would you choose manual DI over Koin or Hilt?",a:[{t:"p",text:"Manual DI (a hand-written container) suits *small apps*, *simple graphs*, *learning*, or *KMP without a framework* — it's explicit, has no dependencies/build cost, and no learning curve. Choose a framework (Hilt/Koin) when the graph grows complex (scopes, lifecycles, many dependencies, ViewModel injection) where manual wiring becomes tedious and error-prone. The threshold is roughly when scope management and boilerplate outweigh the framework's cost."},{t:"list",items:["**Manual DI** — small apps, simple graphs, learning, framework-free KMP.","**Explicit, no cost** — no build/learning overhead.","**Framework** — complex graphs, scopes, lifecycles, ViewModels.","**Threshold** — when boilerplate/scoping outweighs framework cost."]},{t:"note",text:"Manual DI suits small apps, simple graphs, learning, or framework-free KMP — explicit, no build/learning cost. Choose Hilt/Koin when the graph grows complex (scopes, lifecycles, many deps, ViewModel injection) where manual wiring gets tedious/error-prone. The threshold: when boilerplate/scope management outweighs the framework's cost."}]},{level:"senior",q:"How do you inject platform-specific implementations in KMP?",a:[{t:"p",text:"Use `expect`/`actual` for the *declaration* (define an `expect` function/class in `commonMain`, `actual` implementations per platform) or provide platform-specific *Koin modules* that bind platform impls. For example, a `DatabaseDriverFactory` is `expect` in common, `actual` on Android (Room/SQLDelight Android driver) and iOS (native driver), and the shared code depends on the common interface."},{t:"list",items:["**`expect`/`actual`** — common declaration, per-platform implementation.","**Platform Koin modules** — bind platform impls.","**Shared code depends on the common interface** — impls injected per platform.","**Example** — DatabaseDriverFactory, platform APIs."]},{t:"note",text:"KMP platform impls: use expect/actual (common declaration, per-platform actual) or platform-specific Koin modules binding platform impls. Shared code depends on the common interface; each platform provides its implementation (e.g. DatabaseDriverFactory: Room/SQLDelight driver on Android, native on iOS)."}]},{level:"junior",q:"What is the role of the Application class in DI setup?",a:[{t:"p",text:"The `Application` is the process-lifetime root, so it's where you *initialize* the DI container: `@HiltAndroidApp` for Hilt (generates the root component), or `startKoin { }` for Koin. It hosts the app-scoped graph and provides the application `Context`. Every DI framework bootstraps from the `Application`."},{t:"list",items:["**Process-lifetime root** — hosts the app-scoped graph.","**Hilt** — `@HiltAndroidApp` generates the root component.","**Koin** — `startKoin { modules(...) }`.","**Provides app Context** — for the container."]},{t:"note",text:"The Application (process-lifetime root) initializes the DI container: @HiltAndroidApp (Hilt — generates the root component) or startKoin { modules(...) } (Koin). It hosts the app-scoped graph and provides the application Context. Every DI framework bootstraps from the Application."}]},{level:"senior",q:"How do you avoid the service-locator anti-pattern while using Koin?",a:[{t:"p",text:"Koin *can* be used like a service locator (calling `get()`/`inject()` everywhere), which reintroduces hidden dependencies. Avoid it by using *constructor injection* — have definitions call `get()` to build objects, but make classes themselves receive dependencies via their *constructors* (not by calling Koin inside). Reserve `by inject()`/`get()` for the entry points (Activities/Fragments) where constructor injection isn't possible."},{t:"list",items:["**Constructor injection** — classes receive deps via constructors.","**`get()` in definitions** — to build, not inside class logic.","**Entry-point retrieval only** — `by inject()` in Activities/Fragments.","**Avoid** — calling Koin throughout class code (hidden deps)."]},{t:"note",text:"Koin can degrade into a service locator if you call get()/inject() everywhere (hidden deps). Avoid it: use constructor injection (definitions call get() to build, but classes receive deps via constructors), reserving by inject()/get() for entry points (Activities/Fragments) where constructor injection isn't possible."}]},{level:"junior",q:"How do you use named qualifiers in Koin?",a:[{t:"p",text:'To register multiple definitions of the same type, give them *names* (qualifiers): `single(named("auth")) { ... }` and `single(named("public")) { ... }`, then retrieve with `get(named("auth"))` or `by inject(named("auth"))`. This is Koin\'s equivalent of Dagger qualifiers for disambiguating same-typed bindings.'},{t:"code",title:"Named definitions",code:`single(named("auth")) { authClient() }
single(named("public")) { publicClient() }
val client: OkHttpClient = get(named("auth"))`},{t:"list",items:['**`named("x")`** — qualify a definition.','**Retrieve** — `get(named("x"))`/`by inject(named("x"))`.',"**Disambiguate** — multiple same-typed definitions.","**= Dagger qualifiers** — the runtime equivalent."]},{t:"note",text:`Koin named qualifiers disambiguate same-typed definitions: single(named("auth")) { } / single(named("public")) { }, retrieve with get(named("auth")). The runtime equivalent of Dagger's @Qualifier/@Named for two OkHttpClients, multiple config strings, etc.`}]},{level:"senior",q:"How do you test a ViewModel with a fake in a DI-agnostic way?",a:[{t:"p",text:"The cleanest ViewModel tests bypass DI entirely: construct the ViewModel directly with fakes (`HomeViewModel(FakeRepo())`) — no Hilt/Koin needed. Because the ViewModel takes dependencies via its constructor, tests provide test doubles and assert its state/behavior with `runTest` + a test dispatcher. Reserve DI-framework test setup (Hilt test rules, Koin test modules) for integration tests."},{t:"code",title:"DI-agnostic VM test",code:`@Test fun loads() = runTest {
    val vm = HomeViewModel(FakeRepo(cannedData))   // no DI framework
    vm.load(); advanceUntilIdle()
    assertEquals(UiState.Content(cannedData), vm.state.value)
}`},{t:"list",items:["**Construct directly** — pass fakes, no DI framework.","**Constructor injection** — makes this possible.","**`runTest` + test dispatcher** — deterministic.","**DI test setup** — reserve for integration tests."]},{t:"note",text:"Test ViewModels DI-agnostically: construct directly with fakes (HomeViewModel(FakeRepo())) — no Hilt/Koin needed, since constructor injection lets you pass test doubles. Use runTest + a test dispatcher for determinism. Reserve DI-framework test setup (Hilt rules, Koin test modules) for integration tests."}]},{level:"senior",q:"What are the pros and cons of runtime DI for a large team?",a:[{t:"p",text:"Runtime DI (Koin) *pros*: fast builds (no annotation processing), easy onboarding (simple DSL), KMP support, flexible dynamic loading. *Cons* for large teams/codebases: missing bindings crash *at runtime* (not caught in CI unless you run `checkModules`), the service-locator temptation, and less tooling for large graphs. Large Android teams often prefer *compile-time* DI (Hilt) for the safety net; smaller teams/KMP favor Koin's simplicity."},{t:"list",items:["**Pros** — fast builds, easy onboarding, KMP, flexible.","**Cons** — runtime errors (mitigate with checkModules in CI), service-locator risk.","**Large Android teams** — often prefer Hilt's compile-time safety.","**Small teams/KMP** — favor Koin's simplicity."]},{t:"note",text:"Runtime DI (Koin) pros: fast builds, easy onboarding, KMP, flexible dynamic loading. Cons for large teams: runtime errors on missing bindings (mitigate with checkModules in CI), service-locator temptation, less large-graph tooling. Large Android teams often prefer Hilt's compile-time safety; small teams/KMP favor Koin."}]}],dC={id:"koin-kmp-testing",title:"Koin, KMP DI & Testing",tagline:"Koin's runtime DI, Koin vs Hilt trade-offs, DI in Kotlin Multiplatform, and the testing payoff (constructor injection + framework module swapping).",tags:["Koin","KMP","Runtime DI","Testing","single/factory"],content:lC,qa:cC},hC={id:"dependency-injection",name:"Dependency Injection",description:"Providing dependencies cleanly — DI fundamentals & dependency inversion, Dagger's compile-time model, Hilt for Android, and Koin/KMP DI & testing.",topics:[tC,nC,rC,dC]},uC=[{heading:"What WorkManager is and the problem it solves",blocks:[{t:"p",text:"**WorkManager** is Jetpack's library for **deferrable, guaranteed background work** — tasks that should run reliably even if the app is killed or the device reboots, and that can wait for the right conditions (network available, charging). Before WorkManager, background work was a mess of fragmented, deprecated APIs (`JobScheduler`, `AlarmManager`, `GcmNetworkManager`, background services) each with different behavior across Android versions and battery restrictions. WorkManager unifies them: you describe *what* work to do and *under what constraints*, and it picks the right underlying mechanism and guarantees the work eventually runs."},{t:"list",items:["**Guaranteed execution** — the killer feature. WorkManager *persists* work requests in an internal database, so the work survives the app being killed, the process dying, and even a device reboot. A plain coroutine or service can't promise that.","**Deferrable & constraint-aware** — work runs when conditions are met (Wi-Fi, charging, battery not low), respecting Doze and battery-optimization rules. It's for work that should happen 'soon, under the right conditions', not 'this exact instant'.","**Backward-compatible** — it internally uses `JobScheduler` on newer Android and `AlarmManager` + broadcast receivers on older, so you write one API and it works across versions.","**The use cases**: syncing data with a server, uploading logs/photos, periodic backups, prefetching content, sending queued analytics — anything that must reliably complete but doesn't need to run *right now* or tie up the UI."]}]},{heading:"What WorkManager is NOT for",blocks:[{t:"list",items:["**Not for immediate work tied to the UI** — loading data for a screen belongs in a coroutine in `viewModelScope`, not WorkManager. WorkManager has scheduling overhead and may defer the work.","**Not for exact-timing work** — a precise alarm/reminder at 9:00 AM is `AlarmManager`'s job (exact alarms). WorkManager is deferrable by nature.","**Not for ongoing user-visible tasks that must run continuously right now** — music playback or active navigation is a *foreground service*. (Though WorkManager *can* run as a long-running foreground worker for guaranteed-and-immediate work like a large download.)","**The decision heuristic**: is the work *deferrable* and must it be *guaranteed* to eventually run (surviving app death/reboot)? → WorkManager. Immediate + UI-tied → coroutine. Exact time → AlarmManager. Ongoing + user-visible → foreground service."]}]},{heading:"Defining work: the Worker",blocks:[{t:"code",title:"A CoroutineWorker",code:`class UploadWorker(
    context: Context,
    params: WorkerParameters,
) : CoroutineWorker(context, params) {

    override suspend fun doWork(): Result {
        val fileUri = inputData.getString("uri") ?: return Result.failure()
        return try {
            uploadFile(fileUri)                    // the actual background work
            Result.success()                        // done — don't retry
        } catch (e: IOException) {
            Result.retry()                          // transient failure — retry later
        } catch (e: Exception) {
            Result.failure()                        // permanent failure — give up
        }
    }
}`},{t:"list",items:["**`CoroutineWorker`** — the modern base class; `doWork()` is a `suspend` function, so you use coroutines directly (and it runs off the main thread). (`Worker` is the older, non-coroutine base with a blocking `doWork()`.)","**Three results**: `Result.success()` (finished, don't retry), `Result.retry()` (transient failure — WorkManager will retry per the backoff policy), `Result.failure()` (permanent failure — don't retry). Returning the right one is important: retry only genuinely transient failures.","**`inputData`** — small key-value data passed *into* the worker (`Data` objects, like a `Bundle` — Bundle-sized limits apply). `Result.success(outputData)` can return small output data too. For large data, pass a reference (a URI, an id) and load it, not the data itself.","**The worker runs in the background** with its own lifecycle — it may run when the app isn't even open, so it can't touch UI or assume the app's state; it should be self-contained."]}]},{heading:"Scheduling work: OneTime vs Periodic",blocks:[{t:"code",title:"Enqueuing work requests",code:`// One-time work
val uploadRequest = OneTimeWorkRequestBuilder<UploadWorker>()
    .setInputData(workDataOf("uri" to fileUri))
    .setConstraints(
        Constraints.Builder()
            .setRequiredNetworkType(NetworkType.CONNECTED)  // needs network
            .build()
    )
    .build()
WorkManager.getInstance(context).enqueue(uploadRequest)

// Periodic work (minimum interval is 15 minutes)
val syncRequest = PeriodicWorkRequestBuilder<SyncWorker>(
    repeatInterval = 6, TimeUnit.HOURS
).build()
WorkManager.getInstance(context).enqueue(syncRequest)`},{t:"list",items:["**`OneTimeWorkRequest`** — runs once (with retries if it returns `retry`). For a single upload, a one-off sync, a queued action.","**`PeriodicWorkRequest`** — repeats on an interval, with a **minimum of 15 minutes** (the system enforces this to protect battery — you can't schedule periodic work more frequently). For regular background sync/backup. Note: periodic work is *not* precisely timed — the system batches it into maintenance windows.","**`setInputData`** — attach input; **`setConstraints`** — conditions that must be met to run; **`setInitialDelay`** — wait before first run; **`setBackoffCriteria`** — how retries are spaced.","**`enqueue()`** persists the request; WorkManager takes over from there, running it when constraints are met, surviving app kills/reboots."]}]},{heading:"Observing work state",blocks:[{t:"code",title:"Tracking progress and result",code:`WorkManager.getInstance(context)
    .getWorkInfoByIdFlow(uploadRequest.id)         // observe as a Flow
    .collect { workInfo ->
        when (workInfo?.state) {
            WorkInfo.State.ENQUEUED -> showQueued()
            WorkInfo.State.RUNNING -> showProgress()
            WorkInfo.State.SUCCEEDED -> showDone(workInfo.outputData)
            WorkInfo.State.FAILED -> showError()
            else -> Unit
        }
    }`},{t:"list",items:["**`WorkInfo`** exposes the work's state — `ENQUEUED`, `RUNNING`, `SUCCEEDED`, `FAILED`, `BLOCKED` (waiting on a prerequisite), `CANCELLED` — observable as a `Flow` (or LiveData), so the UI can reflect progress.","**`setProgress()`** inside the worker publishes progress updates the UI can observe (e.g. upload percentage).","**Output data** — a succeeded/failed worker can return small output data, readable from its `WorkInfo`.","This lets you build a responsive UI around guaranteed background work — show 'uploading…', progress, and completion — while the work itself is decoupled from the UI's lifetime."]},{t:"note",text:"WorkManager fundamentals: for deferrable, GUARANTEED background work that survives app kill/reboot (sync, uploads, backups). Define a CoroutineWorker with doWork() returning success/retry/failure; schedule OneTime or Periodic (min 15-min) work requests with input data and constraints; enqueue persists it. Observe state via WorkInfo Flow. NOT for immediate UI work (use coroutines), exact timing (AlarmManager), or ongoing user-visible tasks (foreground service)."}]}],pC=[{level:"junior",q:"What is WorkManager and what is it used for?",a:[{t:"p",text:"**WorkManager is Jetpack's library for deferrable, guaranteed background work — tasks that must run reliably (even if the app is killed or the device reboots) but can wait for the right conditions.** Its defining feature is *guaranteed execution*: it persists work requests in an internal database, so the work survives the app being killed, the process dying, and even a reboot — something a plain coroutine or background service can't promise."},{t:"list",items:["**Use cases**: syncing data with a server, uploading photos or logs, periodic backups, prefetching content, sending queued analytics — work that must reliably complete but doesn't need to run *this instant* or block the UI.","**Constraint-aware**: work runs when conditions are met (network available, charging, battery not low), respecting the OS's battery-saving rules (Doze).","**Backward-compatible**: it internally uses `JobScheduler` on newer Android and older mechanisms on older versions, so you write one API that works everywhere."]},{t:"p",text:"The mental model: you *describe* what work to do and under what conditions, `enqueue` it, and WorkManager takes over — guaranteeing it eventually runs, retrying on failure, and surviving whatever happens to the app. It replaced a fragmented set of older, deprecated background APIs with one reliable, modern solution."}]},{level:"junior",q:"When should you NOT use WorkManager?",a:[{t:"list",items:["**Not for immediate work tied to the UI** — loading data for the current screen belongs in a coroutine in `viewModelScope`. WorkManager has scheduling overhead and may defer the work, so it's wrong for 'fetch this now to show it'.","**Not for exact-timing work** — a precise alarm or reminder at a specific time (9:00 AM) is `AlarmManager`'s job (exact alarms). WorkManager is deferrable by design and won't fire at an exact moment.","**Not for ongoing, user-visible tasks that must run continuously right now** — music playback or active turn-by-turn navigation is a *foreground service*."]},{t:"p",text:"The decision heuristic: ask 'is this work *deferrable*, and must it be *guaranteed* to eventually run (surviving app death/reboot)?' If yes → WorkManager. If it's immediate and UI-tied → coroutine in a lifecycle scope. If it needs an exact time → AlarmManager. If it's ongoing and user-visible → foreground service. A common mistake is reaching for WorkManager for a quick screen-scoped network call — that just adds overhead and latency for no benefit. WorkManager earns its complexity specifically when reliability across process death matters."}]},{level:"junior",q:"What are the possible results a Worker can return?",a:[{t:"p",text:"**A Worker's `doWork()` returns one of three results, and returning the right one is important for correct retry behavior:**"},{t:"list",items:["**`Result.success()`** — the work finished successfully; WorkManager marks it done and won't retry. Can include small output data (`Result.success(outputData)`).","**`Result.retry()`** — a *transient* failure occurred (a network blip, a temporary server error), and WorkManager should retry the work later according to its backoff policy. Use this for failures that might succeed on a later attempt.","**`Result.failure()`** — a *permanent* failure (invalid input, a non-recoverable error); WorkManager should give up and not retry. Use this when retrying would fail identically."]},{t:"code",title:"Choosing the right result",code:`override suspend fun doWork(): Result = try {
    upload(); Result.success()
} catch (e: IOException) {
    Result.retry()        // transient — try again later
} catch (e: Exception) {
    Result.failure()      // permanent — give up
}`},{t:"p",text:"The distinction mirrors network error handling: retry transient failures (network, 5xx), give up on permanent ones (bad input, 4xx). Returning `retry()` for a genuinely permanent failure wastes battery looping forever; returning `failure()` for a transient one loses work that would have succeeded. So map the failure type to the right result, just as you would for retry logic elsewhere."}]},{level:"junior",q:"What is the difference between OneTimeWorkRequest and PeriodicWorkRequest?",a:[{t:"p",text:"**`OneTimeWorkRequest` runs the work once (with retries if it fails transiently); `PeriodicWorkRequest` repeats it on an interval.** You choose based on whether the work is a single action or a recurring one."},{t:"list",items:["**`OneTimeWorkRequest`** — for a single task: uploading a specific photo, a one-off sync, processing a queued action. It runs once when its constraints are met (retrying on `Result.retry()`).","**`PeriodicWorkRequest`** — for recurring tasks: syncing every few hours, a nightly backup. The important constraint is a **minimum interval of 15 minutes** — the system enforces this to protect battery, so you *cannot* schedule periodic work more frequently than every 15 minutes."]},{t:"p",text:"A key nuance about periodic work: it is *not precisely timed*. The system batches periodic work into maintenance windows (respecting Doze and battery optimization), so 'every 6 hours' means 'approximately every 6 hours, when convenient for the system', not exactly on the clock. If you need exact timing you'd use AlarmManager instead. Also, for frequent syncs (more often than 15 minutes) you'd need a different approach — periodic work can't do it, and that frequency usually indicates you want push (FCM) or a foreground service instead. So: OneTime for single actions, Periodic for recurring work at 15-minutes-or-longer intervals, understanding that periodic timing is approximate."}]},{level:"senior",q:"How does WorkManager guarantee that work survives app kill and device reboot?",a:[{t:"p",text:"**WorkManager persists every enqueued work request in an internal database (Room, under the hood), and it registers with the OS's job-scheduling infrastructure so the system will run the work on WorkManager's behalf even when the app process is dead — including re-scheduling everything after a reboot.** The guarantee comes from *durability* (the request is stored on disk, not held in memory) plus *delegation to the OS scheduler* (the system, not your app, is responsible for actually triggering the work)."},{t:"list",items:["**Persistence**: when you `enqueue` a request, WorkManager writes it to its database with its constraints, state, and input data. So the request exists independently of your app's process memory — killing the app doesn't lose it.","**Delegation to the OS scheduler**: WorkManager schedules the work with the platform's job scheduler (`JobScheduler` on API 23+, or `AlarmManager` + a broadcast receiver on older versions). The *OS* holds the schedule and wakes up / restarts your app's process to run the worker when constraints are met — so the work runs even if your app was killed in the meantime.","**Reboot survival**: WorkManager registers a `BOOT_COMPLETED` broadcast receiver. After a reboot (which clears all scheduled jobs), this receiver fires, WorkManager reads its database of pending work, and *re-schedules* everything with the OS scheduler. This is how work survives a reboot — it's re-registered from the persisted database on boot.","**State machine & retries**: the database also tracks each work's state (enqueued/running/succeeded/failed) and retry/backoff info, so an interrupted worker (process killed mid-run) is retried, and chains resume correctly."]},{t:"p",text:"**The senior framing**: the guarantee isn't magic — it's *durability + OS delegation*. By persisting requests to disk and handing scheduling to the OS-level job scheduler (which survives your process), plus re-registering everything on `BOOT_COMPLETED`, WorkManager ensures the work outlives any app or device lifecycle event. This is precisely what a plain coroutine (dies with the process) or a background service (killed by Android 8+ restrictions, gone on reboot) *cannot* do, and it's the concrete reason WorkManager is the answer for guaranteed background work. Contrast with AlarmManager, which also survives reboot (via a boot receiver you write) but is about *exact timing*, not constraint-based deferrable work."}]},{level:"senior",q:"Why is periodic work limited to a 15-minute minimum interval, and what do you do if you need more frequent execution?",a:[{t:"p",text:"**The 15-minute minimum is a deliberate battery-protection constraint imposed by the platform's job scheduler, which WorkManager builds on. Waking the device and running work frequently is a major battery drain, so Android caps how often periodic background work can run — and batches it into maintenance windows rather than running it precisely on schedule.** You cannot bypass this with WorkManager; a `PeriodicWorkRequest` with a shorter interval is silently clamped to 15 minutes."},{t:"list",items:["**Why the limit exists**: each background execution potentially wakes the device from Doze, uses CPU and network, and drains battery. If apps could run periodic work every minute, a device with many apps would have terrible battery life. The 15-minute floor (plus batching into maintenance windows and Doze deferral) is the OS enforcing collective battery discipline — the same philosophy behind the Android 8+ background restrictions.","**Also, periodic timing is approximate**: even at 15+ minutes, the exact fire time isn't guaranteed — the system batches periodic jobs to run together in maintenance windows to minimize wakeups. So you can't rely on periodic work for precise timing at all."]},{t:"list",items:["**If you need more frequent, or exactly-timed, or immediate execution, WorkManager is the wrong tool — choose based on the actual need**:","**Server-driven updates → Push (FCM)**: instead of polling frequently, have the server *push* a notification when there's new data, and the app reacts (often kicking off a one-time WorkManager job to sync). This is far more battery-efficient than frequent polling and gives near-real-time updates — the correct pattern for 'the app needs fresh data promptly'.","**Exact-time task → AlarmManager** (exact alarms) for something that must fire at a precise moment (a calendar reminder).","**Ongoing/continuous work → foreground service** (with its notification) for something that must run continuously right now (a live tracking session).","**Chained one-time work** — if you need something 'roughly every few minutes' during active use, you can self-schedule one-time work that re-enqueues itself, but this is discouraged (it fights the battery model); the frequent-updates need almost always points to push instead."]},{t:"p",text:"**The senior framing**: the 15-minute floor is the OS telling you 'frequent background polling is a battery anti-pattern' — so the *right* answer to 'I need updates more often than 15 minutes' is usually to *invert the model* from polling to push (FCM), letting the server notify the app when there's genuinely something to do. Fighting the limit (self-rescheduling loops, exact alarms abused for polling) works against the battery model and against the user. Recognizing that the constraint is pushing you toward a better architecture (push over poll) rather than being an obstacle to route around is the mature response — it's the same 'the OS restriction is guiding you to the efficient design' theme as the broader background-execution limits."}]},{level:"junior",q:"What is a Worker, and what does doWork() do?",a:[{t:"p",text:"A `Worker` is the class that defines the *task* WorkManager runs — you subclass it and implement `doWork()`, which contains the work and returns a `Result` (`success`/`retry`/`failure`). WorkManager instantiates and runs it on a background thread when the work's constraints are met. It's the unit of background work."},{t:"code",title:"A Worker",code:`class UploadWorker(ctx: Context, params: WorkerParameters) : CoroutineWorker(ctx, params) {
    override suspend fun doWork(): Result = try {
        uploadPhotos()
        Result.success()
    } catch (e: IOException) { Result.retry() }
}`},{t:"list",items:["**`Worker` subclass** — defines the task.","**`doWork()`** — the work; returns a `Result`.","**Background thread** — WorkManager runs it off the main thread.","**`Result`** — `success()`/`retry()`/`failure()`."]},{t:"note",text:"A Worker defines the background task — subclass it and implement doWork() (the work, returning Result.success/retry/failure). WorkManager instantiates and runs it on a background thread when constraints are met. It's the unit of background work; CoroutineWorker's doWork() is suspend."}]},{level:"junior",q:"What is the difference between Worker, CoroutineWorker, and RxWorker?",a:[{t:"p",text:"They differ by threading/async model. `Worker`'s `doWork()` runs *synchronously* on a background thread (blocking). `CoroutineWorker`'s `doWork()` is a *suspend* function running in a coroutine — the modern default for Kotlin, integrating with coroutines/Flow. `RxWorker` returns a `Single<Result>` for RxJava codebases. Use `CoroutineWorker` in Kotlin apps."},{t:"list",items:["**`Worker`** — synchronous `doWork()` on a background thread.","**`CoroutineWorker`** — suspend `doWork()`; coroutine-based (preferred).","**`RxWorker`** — `Single<Result>` for RxJava.","**Kotlin** — use `CoroutineWorker`."]},{t:"note",text:"Worker: synchronous doWork() on a background thread. CoroutineWorker: suspend doWork() in a coroutine — modern Kotlin default (integrates coroutines/Flow). RxWorker: Single<Result> for RxJava. Use CoroutineWorker in Kotlin apps."}]},{level:"junior",q:"How do you build and enqueue a WorkRequest?",a:[{t:"p",text:"Build a `OneTimeWorkRequestBuilder<YourWorker>()` (or `PeriodicWorkRequestBuilder`), configure it (constraints, input data, backoff, tags), then enqueue it with `WorkManager.getInstance(context).enqueue(request)`. WorkManager persists and schedules it. For deduplication, use `enqueueUniqueWork`."},{t:"code",title:"Enqueue work",code:`val request = OneTimeWorkRequestBuilder<UploadWorker>()
    .setConstraints(Constraints.Builder().setRequiredNetworkType(NetworkType.CONNECTED).build())
    .setInputData(workDataOf("photoId" to id))
    .addTag("upload")
    .build()
WorkManager.getInstance(context).enqueue(request)`},{t:"list",items:["**`OneTimeWorkRequestBuilder`/`PeriodicWorkRequestBuilder`** — build the request.","**Configure** — constraints, input data, backoff, tags.","**`enqueue(request)`** — schedule and persist it.","**`enqueueUniqueWork`** — for deduplication."]},{t:"note",text:"Build a OneTimeWorkRequestBuilder<Worker>() (or Periodic), configure (constraints/input/backoff/tags), and enqueue with WorkManager.getInstance(context).enqueue(request) — it persists and schedules the work. Use enqueueUniqueWork for deduplication."}]},{level:"junior",q:"How do you pass input to a Worker and return output?",a:[{t:"p",text:'Pass input via a `Data` object (`workDataOf("key" to value)`) set on the request with `setInputData`; read it in the Worker with `inputData.getString(...)`. Return output by including a `Data` in the `Result` (`Result.success(workDataOf(...))`), which is available to the next chained worker or via `WorkInfo`. `Data` holds only small primitives/arrays (like a Bundle) — pass ids, not large blobs.'},{t:"code",title:"Input/output Data",code:`// Enqueue: .setInputData(workDataOf("url" to url))
// In Worker:
val url = inputData.getString("url")!!
return Result.success(workDataOf("resultId" to id))`},{t:"list",items:["**Input** — `setInputData(workDataOf(...))`; read via `inputData`.","**Output** — `Result.success(workDataOf(...))`.","**Small only** — primitives/arrays (like a Bundle); pass ids.","**Chaining** — output feeds the next worker's input."]},{t:"note",text:'Input: setInputData(workDataOf("key" to value)), read via inputData.getString(...). Output: Result.success(workDataOf(...)) — available to the next chained worker or via WorkInfo. Data holds only small primitives/arrays (like a Bundle) — pass ids, not large blobs.'}]},{level:"senior",q:"How do you observe the status of enqueued work?",a:[{t:"p",text:"Observe `WorkInfo` — via `getWorkInfoByIdLiveData(id)`, `getWorkInfosByTagLiveData(tag)`, or the Flow variants. `WorkInfo` gives the `State` (ENQUEUED, RUNNING, SUCCEEDED, FAILED, BLOCKED, CANCELLED), progress, and output `Data`. The UI can show progress/completion by collecting this. WorkManager also supports `setProgress` inside a Worker to report intermediate progress."},{t:"code",title:"Observing WorkInfo",code:`WorkManager.getInstance(context)
    .getWorkInfoByIdLiveData(request.id)
    .observe(owner) { info ->
        when (info?.state) { WorkInfo.State.SUCCEEDED -> show(info.outputData); else -> {} }
    }`},{t:"list",items:["**`WorkInfo`** — state, progress, output.","**By id/tag/unique name** — LiveData or Flow.","**States** — ENQUEUED/RUNNING/SUCCEEDED/FAILED/BLOCKED/CANCELLED.","**`setProgress`** — report intermediate progress from the Worker."]},{t:"note",text:"Observe WorkInfo (getWorkInfoByIdLiveData/getWorkInfosByTagLiveData or Flow variants) — it exposes State (ENQUEUED/RUNNING/SUCCEEDED/FAILED/BLOCKED/CANCELLED), progress, and output Data. Use setProgress in the Worker for intermediate progress; the UI collects it for progress/completion."}]},{level:"junior",q:"How do you cancel enqueued or running work?",a:[{t:"p",text:"Cancel by *id* (`cancelWorkById(id)`), by *tag* (`cancelAllWorkByTag(tag)`), by *unique name* (`cancelUniqueWork(name)`), or all (`cancelAllWork()`). Cancellation stops pending work and signals running work (the Worker's `isStopped`/coroutine cancellation) to stop cooperatively. Handle cancellation in the Worker (release resources, don't leave partial state)."},{t:"list",items:["**`cancelWorkById(id)`** — one request.","**`cancelAllWorkByTag(tag)`** — by tag.","**`cancelUniqueWork(name)`** — a unique chain.","**Cooperative** — running Workers get `isStopped`/coroutine cancellation."]},{t:"note",text:"Cancel work by id (cancelWorkById), tag (cancelAllWorkByTag), unique name (cancelUniqueWork), or all (cancelAllWork). Pending work is cancelled; running Workers are signaled (isStopped / coroutine cancellation) to stop cooperatively — handle it (release resources, avoid partial state)."}]},{level:"senior",q:"What does WorkManager use under the hood?",a:[{t:"p",text:"WorkManager is an *abstraction* over the platform's scheduling mechanisms — it picks the best one for the API level: `JobScheduler` on API 23+, and (historically) a combination of `AlarmManager` + `BroadcastReceiver` on older versions. It stores work in a `Room` database for persistence. You don't deal with these directly; WorkManager handles compatibility and guarantees."},{t:"list",items:["**Abstraction** — picks the best scheduler per API level.","**`JobScheduler`** — API 23+.","**AlarmManager + Receiver** — legacy fallback.","**Room DB** — persists work for reboot survival."]},{t:"note",text:"WorkManager abstracts the platform schedulers — JobScheduler on API 23+ (AlarmManager + BroadcastReceiver on older), storing work in a Room database for persistence. It handles compatibility/guarantees so you don't touch JobScheduler directly."}]},{level:"senior",q:"How does WorkManager survive process death and reboot?",a:[{t:"p",text:"WorkManager persists enqueued work (and its constraints/state) in a *Room database* on disk. So if the process is killed or the device reboots, the work isn't lost — WorkManager (via a `BOOT_COMPLETED` receiver and its scheduler) reschedules pending work when the app/device restarts. This *guaranteed execution* is WorkManager's key differentiator from a plain coroutine/service, which die with the process."},{t:"list",items:["**Persisted in Room** — work survives on disk.","**Reschedules on reboot** — via BOOT_COMPLETED.","**Guaranteed execution** — runs even after kill/restart.","**vs coroutine/service** — those die with the process."]},{t:"note",text:"WorkManager persists work + constraints/state in a Room DB, so process kill or reboot doesn't lose it — it reschedules pending work on restart (BOOT_COMPLETED). This guaranteed execution is the key difference from a plain coroutine/service, which die with the process."}]},{level:"junior",q:"What is an initial delay, and how do you schedule work for later?",a:[{t:"p",text:"`setInitialDelay(duration)` on a `WorkRequest` delays the *earliest* time the work can run (WorkManager may run it later depending on constraints/Doze). WorkManager isn't for *exact* timing — it's for *deferrable* work. For an exact time (an alarm/reminder), use `AlarmManager`; for 'run after ~X, when convenient', use `setInitialDelay`."},{t:"code",title:"Initial delay",code:`OneTimeWorkRequestBuilder<Worker>()
    .setInitialDelay(30, TimeUnit.MINUTES)   // earliest run time
    .build()`},{t:"list",items:["**`setInitialDelay`** — earliest run time (not exact).","**Deferrable** — WorkManager may run later (constraints/Doze).","**Not exact timing** — use `AlarmManager` for that.","**Use** — 'run after ~X, when convenient'."]},{t:"note",text:"setInitialDelay(duration) sets the earliest time work can run (WorkManager may run later per constraints/Doze — it's deferrable, not exact). For exact timing (alarms/reminders) use AlarmManager; use setInitialDelay for 'run after ~X, when convenient'."}]},{level:"senior",q:"How do you handle a Result.retry, and what triggers it?",a:[{t:"p",text:"Return `Result.retry()` from `doWork()` when the work failed *transiently* (network error, temporary server issue) and should be re-attempted. WorkManager then re-runs it according to the request's *backoff policy* (exponential or linear, with a delay). Return `Result.failure()` for permanent failures (won't retry) and `Result.success()` when done. Retry is how WorkManager handles transient failures reliably."},{t:"list",items:["**`Result.retry()`** — transient failure; re-run per backoff.","**`Result.failure()`** — permanent; won't retry.","**`Result.success()`** — done.","**Backoff policy** — controls the retry delay/growth."]},{t:"note",text:"Return Result.retry() for transient failures (network/temporary) — WorkManager re-runs per the backoff policy (exponential/linear). Result.failure() = permanent (no retry); Result.success() = done. Retry + backoff is how WorkManager reliably handles transient failures."}]},{level:"senior",q:"How do you test a Worker?",a:[{t:"p",text:"Use `TestListenableWorkerBuilder`/`TestWorkerBuilder` to construct a Worker with test input data and run its `doWork()` directly, asserting the `Result`. For integration testing scheduling/constraints, use `WorkManagerTestInitHelper` with a `SynchronousExecutor` to drive work synchronously and manipulate constraints/delays via the test driver. This tests both the work logic and the scheduling behavior."},{t:"code",title:"Testing a Worker",code:`val worker = TestListenableWorkerBuilder<UploadWorker>(context)
    .setInputData(workDataOf("id" to "1")).build()
val result = worker.doWork()   // (or startWork() for ListenableWorker)
assertEquals(Result.success(), result)`},{t:"list",items:["**`TestListenableWorkerBuilder`** — build + run `doWork()` directly.","**Assert `Result`** — success/retry/failure.","**`WorkManagerTestInitHelper`** — drive scheduling synchronously.","**Test driver** — manipulate constraints/delays."]},{t:"note",text:"Test Workers with TestListenableWorkerBuilder/TestWorkerBuilder (build with input data, run doWork() directly, assert Result). For scheduling/constraints, use WorkManagerTestInitHelper with a SynchronousExecutor and its test driver to satisfy constraints/delays. Tests both work logic and scheduling."}]},{level:"senior",q:"What is the difference between WorkManager and a plain coroutine or service?",a:[{t:"p",text:"A *coroutine* (in `viewModelScope`/a scope) runs while the process/scope lives — it's for *in-app, immediate* work tied to the UI. A *foreground service* runs user-visible ongoing work now. *WorkManager* is for *deferrable, guaranteed* background work that must survive app kill/reboot (sync, upload). Use a coroutine for immediate UI-scoped work; WorkManager when the work must complete eventually regardless of app state."},{t:"table",headers:["","Coroutine","Foreground service","WorkManager"],rows:[["When","immediate, in-app","immediate, visible","deferrable"],["Survives kill/reboot","no","no","yes"],["Use","UI-scoped work","playback/nav","sync/upload"]]},{t:"list",items:["**Coroutine** — immediate UI-scoped work; dies with the scope.","**Foreground service** — immediate user-visible work.","**WorkManager** — deferrable, guaranteed; survives kill/reboot.","**Choose** — by immediacy, visibility, and durability."]},{t:"note",text:"Coroutine: immediate in-app UI-scoped work (dies with the scope). Foreground service: immediate user-visible work. WorkManager: deferrable, guaranteed work surviving kill/reboot (sync/upload). Use a coroutine for immediate UI work, WorkManager when it must complete eventually regardless of app state."}]},{level:"senior",q:"How do you inject dependencies into a Worker?",a:[{t:"p",text:"Use `@HiltWorker` with an `@AssistedInject` constructor: `Context` and `WorkerParameters` are `@Assisted` (WorkManager provides them), and your dependencies (repository, etc.) are injected by Hilt. Configure `HiltWorkerFactory` in the Application's `Configuration.Provider` (and disable WorkManager's default initializer). Without Hilt, you'd use a custom `WorkerFactory`."},{t:"code",title:"@HiltWorker",code:`@HiltWorker
class SyncWorker @AssistedInject constructor(
    @Assisted ctx: Context, @Assisted params: WorkerParameters,
    private val repo: Repository,
) : CoroutineWorker(ctx, params)`},{t:"list",items:["**`@HiltWorker` + `@AssistedInject`** — Hilt injects the Worker.","**`@Assisted`** — Context/WorkerParameters from WorkManager.","**`HiltWorkerFactory`** — in `Configuration.Provider`.","**Without Hilt** — a custom `WorkerFactory`."]},{t:"note",text:"Inject into a Worker with @HiltWorker + @AssistedInject (Context/WorkerParameters @Assisted from WorkManager, your deps injected by Hilt); configure HiltWorkerFactory in the Application's Configuration.Provider (disable the default initializer). Without Hilt, use a custom WorkerFactory."}]},{level:"junior",q:"What are work tags, and how do you use them?",a:[{t:"p",text:'A *tag* is a string label you attach to a `WorkRequest` (`addTag("upload")`) to group and reference related work. You can then observe all work with a tag (`getWorkInfosByTag`), cancel by tag (`cancelAllWorkByTag`), or query status. Multiple requests can share a tag, and a request can have multiple tags — useful for managing categories of background work.'},{t:"list",items:['**`addTag("...")`** — label a request.',"**Group work** — many requests can share a tag.","**Query/cancel by tag** — `getWorkInfosByTag`/`cancelAllWorkByTag`.","**Multiple tags** — a request can have several."]},{t:"note",text:'A tag (addTag("upload")) labels a WorkRequest to group/reference related work — observe by tag (getWorkInfosByTag), cancel by tag (cancelAllWorkByTag), or query status. Requests can share tags and have multiple — useful for managing categories of background work.'}]},{level:"senior",q:"What is the flex interval in periodic work?",a:[{t:"p",text:"A `PeriodicWorkRequest` runs once per interval (min 15 min), but the *flex interval* defines a window at the *end* of each period during which the work can run (rather than at an exact point) — giving the system flexibility to batch it for battery efficiency. So the work runs sometime in the flex window near the end of each interval, not at a precise time."},{t:"code",title:"Flex interval",code:`PeriodicWorkRequestBuilder<SyncWorker>(
    repeatInterval = 1, repeatIntervalTimeUnit = TimeUnit.HOURS,
    flexTimeInterval = 15, flexTimeIntervalUnit = TimeUnit.MINUTES,   // last 15 min window
).build()`},{t:"list",items:["**Interval** — how often (min 15 min).","**Flex interval** — a window at the period's end to run in.","**Battery** — lets the system batch the work.","**Not exact** — runs sometime in the flex window."]},{t:"note",text:"A PeriodicWorkRequest runs once per interval (≥15 min); the flex interval is a window at the END of each period during which it may run — giving the system flexibility to batch for battery. So work runs sometime in the flex window, not at a precise time."}]},{level:"senior",q:"What is onStopped, and how do you handle Worker cancellation?",a:[{t:"p",text:"When work is cancelled or preempted (constraints no longer met, cancelled, or the system needs to stop it), WorkManager signals the Worker — `onStopped()` is called and `isStopped` becomes true; in a `CoroutineWorker`, the coroutine is *cancelled*. You should stop work cooperatively (check `isStopped`/handle `CancellationException`), release resources, and not leave partial state. Stopped work is typically re-run later."},{t:"list",items:["**`onStopped()`/`isStopped`** — signaled on cancellation/preemption.","**`CoroutineWorker`** — the coroutine is cancelled.","**Cooperative stop** — check `isStopped`/handle cancellation.","**Clean up** — release resources; avoid partial state."]},{t:"note",text:"When cancelled/preempted (constraints lost, cancelled, system stop), WorkManager signals the Worker — onStopped()/isStopped, and a CoroutineWorker's coroutine is cancelled. Stop cooperatively (check isStopped/handle CancellationException), release resources, avoid partial state. Stopped work is usually re-run later."}]},{level:"junior",q:"What is the guarantee model of WorkManager (deferrable but guaranteed)?",a:[{t:"p",text:"WorkManager guarantees your work *will run* (eventually), even across app kills and reboots — but it does *not* guarantee *when* (it's *deferrable*, subject to constraints, Doze, and batching). So it's perfect for work that *must complete* but *doesn't need to run immediately or at an exact time* (syncing, uploading, cleanup). For 'must run now', use a foreground service; for exact time, AlarmManager."},{t:"list",items:["**Guaranteed** — runs eventually, survives kill/reboot.","**Deferrable** — timing not guaranteed (constraints/Doze/batching).","**Perfect for** — must-complete but not-urgent work.","**Not for** — immediate (foreground service) or exact-time (AlarmManager)."]},{t:"note",text:"WorkManager guarantees work WILL run eventually (surviving kill/reboot) but NOT WHEN (deferrable — constraints/Doze/batching). Ideal for must-complete-but-not-urgent work (sync/upload/cleanup). Use a foreground service for 'run now', AlarmManager for exact time."}]},{level:"junior",q:"How do you set constraints so work runs only under certain conditions?",a:[{t:"p",text:"Attach a `Constraints` object to the request — WorkManager waits until *all* constraints are met before running. Common constraints: `setRequiredNetworkType` (connected/unmetered), `setRequiresCharging`, `setRequiresBatteryNotLow`, `setRequiresDeviceIdle`, `setRequiresStorageNotLow`. This ensures work runs only when appropriate (e.g. upload photos only on unmetered Wi-Fi while charging)."},{t:"code",title:"Constraints",code:`val constraints = Constraints.Builder()
    .setRequiredNetworkType(NetworkType.UNMETERED)
    .setRequiresCharging(true)
    .build()
OneTimeWorkRequestBuilder<Worker>().setConstraints(constraints).build()`},{t:"list",items:["**`Constraints`** — conditions that must all be met.","**Network** — connected/unmetered.","**Charging/battery/storage/idle** — device conditions.","**Waits** — WorkManager defers until satisfied."]},{t:"note",text:"Attach Constraints — WorkManager waits until ALL are met: network (connected/unmetered), charging, battery-not-low, device-idle, storage-not-low. Ensures work runs only when appropriate (upload only on unmetered Wi-Fi while charging). WorkManager defers the work until satisfied."}]},{level:"senior",q:"What happens to WorkManager work when the user force-stops the app?",a:[{t:"p",text:"If the *user* force-stops the app (from Settings), Android puts the app in a *stopped state* — its scheduled work (and alarms, jobs) are *cancelled* and won't run until the user launches the app again. This is a deliberate OS behavior (force-stop means 'stop everything'). So force-stopped is different from a normal process kill (where WorkManager reschedules) — the user explicitly halted the app."},{t:"list",items:["**Force-stop** — user-initiated; app enters a stopped state.","**Work cancelled** — won't run until the app is relaunched.","**Different from process kill** — normal kills reschedule.","**Deliberate** — force-stop means 'stop everything'."]},{t:"note",text:"User force-stop (from Settings) puts the app in a stopped state — scheduled work/alarms/jobs are cancelled and won't run until the user relaunches the app. This differs from a normal process kill (where WorkManager reschedules) — force-stop is a deliberate user 'stop everything'."}]},{level:"senior",q:"How do you make a Worker's work main-safe and cancellable?",a:[{t:"p",text:"In a `CoroutineWorker`, `doWork()` runs on `Dispatchers.Default` by default — do blocking I/O with `withContext(Dispatchers.IO)` (main-safe), and respect cancellation (coroutine cancellation on stop propagates, so cooperative suspend calls stop cleanly). Override `getForegroundInfo` for long-running work. Avoid blocking without a dispatcher switch, and check for cancellation in long loops."},{t:"list",items:["**`CoroutineWorker`** — `doWork()` on `Dispatchers.Default`.","**`withContext(IO)`** — for blocking I/O (main-safe).","**Cancellation** — coroutine cancellation on stop; use suspend calls.","**Long loops** — check `isStopped`/`ensureActive`."]},{t:"note",text:"In CoroutineWorker, doWork() runs on Dispatchers.Default — use withContext(Dispatchers.IO) for blocking I/O (main-safe). Coroutine cancellation on stop propagates, so cooperative suspend calls stop cleanly; check isStopped/ensureActive in long loops. Override getForegroundInfo for long-running work."}]},{level:"senior",q:"How do you throttle or debounce background work enqueues?",a:[{t:"p",text:"To avoid enqueueing the same work repeatedly (e.g. a sync triggered by many events), use `enqueueUniqueWork` with `ExistingWorkPolicy.KEEP` (ignore new requests while one is pending) — deduplicating. For periodic syncing, a `PeriodicWorkRequest` with `KEEP` avoids duplicates. Combine with constraints and backoff so the system batches efficiently rather than running on every trigger."},{t:"list",items:["**`enqueueUniqueWork` + `KEEP`** — dedupe; ignore new while pending.","**`REPLACE`** — cancel and re-enqueue (latest wins) if needed.","**Periodic + `KEEP`** — avoid duplicate periodic work.","**Constraints/backoff** — let the system batch efficiently."]},{t:"note",text:"Dedupe/throttle with enqueueUniqueWork + ExistingWorkPolicy.KEEP (ignore new requests while one is pending) — or REPLACE for latest-wins. Periodic work with KEEP avoids duplicates. Combine with constraints/backoff so the system batches efficiently rather than running on every trigger."}]},{level:"junior",q:"How do you report progress from a long-running Worker?",a:[{t:"p",text:'Call `setProgress(workDataOf("percent" to n))` inside `doWork()` to publish intermediate progress; observers reading `WorkInfo.progress` see it and can update a progress bar. For a `CoroutineWorker`, `setProgress` is a suspend function. Progress is cleared when the work completes. This lets the UI reflect an upload/download\'s advancement.'},{t:"code",title:"setProgress",code:`override suspend fun doWork(): Result {
    for (i in 0..100 step 10) { setProgress(workDataOf("percent" to i)); upload(i) }
    return Result.success()
}`},{t:"list",items:["**`setProgress(data)`** — publish intermediate progress.","**`WorkInfo.progress`** — observers read it.","**Cleared on completion** — progress resets.","**Uses** — upload/download progress bars."]},{t:"note",text:'Call setProgress(workDataOf("percent" to n)) in doWork() (suspend in CoroutineWorker) to publish intermediate progress; observers read WorkInfo.progress to update a progress bar. Progress clears on completion. For upload/download advancement in the UI.'}]},{level:"senior",q:"How does WorkManager interact with app updates?",a:[{t:"p",text:"When the app is *updated* (a new APK installed over the old), Android sends `MY_PACKAGE_REPLACED` and WorkManager *reschedules* its persisted work — so enqueued work survives an update (it's stored in the Room DB). A normal app update doesn't put the app in the stopped state (unlike force-stop), so scheduled work continues. This is part of WorkManager's durability guarantee."},{t:"list",items:["**Update survives** — work persisted in Room is rescheduled.","**`MY_PACKAGE_REPLACED`** — triggers rescheduling.","**Not stopped state** — unlike force-stop.","**Durability** — enqueued work continues across updates."]},{t:"note",text:"On app update, Android sends MY_PACKAGE_REPLACED and WorkManager reschedules its persisted (Room-stored) work — so enqueued work survives an update. A normal update doesn't enter the stopped state (unlike force-stop), so scheduled work continues. Part of WorkManager's durability guarantee."}]},{level:"junior",q:"How do you initialize WorkManager, and what is on-demand initialization?",a:[{t:"p",text:"By default WorkManager auto-initializes via a `ContentProvider` at app startup. For custom configuration (a custom `WorkerFactory` for Hilt, a custom executor, logging level), you disable the default initializer in the manifest and have your `Application` implement `Configuration.Provider` — WorkManager then initializes *on demand* with your config the first time it's used. This is required for `@HiltWorker` injection."},{t:"code",title:"Custom config",code:`@HiltAndroidApp
class MyApp : Application(), Configuration.Provider {
    @Inject lateinit var workerFactory: HiltWorkerFactory
    override val workManagerConfiguration get() =
        Configuration.Builder().setWorkerFactory(workerFactory).build()
}`},{t:"list",items:["**Default** — auto-init via a ContentProvider.","**Custom** — disable default init + `Configuration.Provider`.","**On-demand** — WorkManager inits with your config on first use.","**Required for** — `@HiltWorker` (custom WorkerFactory)."]},{t:"note",text:"WorkManager auto-initializes via a ContentProvider by default. For custom config (HiltWorkerFactory, custom executor, logging), disable the default initializer in the manifest and implement Configuration.Provider in the Application — WorkManager initializes on-demand with your config. Required for @HiltWorker injection."}]}],mC={id:"workmanager-fundamentals",title:"WorkManager Fundamentals",tagline:"Deferrable guaranteed work, when (not) to use it, CoroutineWorker & results, OneTime vs Periodic, observing WorkInfo, and how it survives kill/reboot.",tags:["WorkManager","CoroutineWorker","Background","Periodic","WorkInfo"],content:uC,qa:pC},fC=[{heading:"Constraints — running work only under the right conditions",blocks:[{t:"p",text:"**Constraints** tell WorkManager the conditions that must be true before your work runs. WorkManager waits until they're all satisfied, then executes — and if conditions change (network lost) mid-run, it can stop and retry later. This is how you make background work battery- and data-friendly: only sync on Wi-Fi, only do heavy work while charging."},{t:"code",title:"Common constraints",code:`val constraints = Constraints.Builder()
    .setRequiredNetworkType(NetworkType.UNMETERED)   // Wi-Fi only (not cellular)
    .setRequiresCharging(true)                         // only while charging
    .setRequiresBatteryNotLow(true)                    // not on low battery
    .setRequiresDeviceIdle(true)                        // device not in active use
    .setRequiresStorageNotLow(true)                    // enough storage
    .build()

val request = OneTimeWorkRequestBuilder<SyncWorker>()
    .setConstraints(constraints)
    .build()`},{t:"list",items:["**Network** — `CONNECTED` (any network), `UNMETERED` (Wi-Fi, to avoid cellular data charges), `METERED`, `NOT_REQUIRED`. Use `UNMETERED` for large uploads/downloads to respect the user's data plan.","**Charging / battery** — run heavy or non-urgent work only while charging or when the battery isn't low, so you don't drain the user's battery.","**Device idle / storage** — run when the device isn't in active use, or only if there's enough storage.","**Behavior**: WorkManager holds the work in a `BLOCKED`/`ENQUEUED` state until constraints are met. If a constraint is violated *during* execution (charger unplugged), WorkManager stops the worker and retries when conditions return — so your worker should handle being interrupted."]}]},{heading:"Chaining work",blocks:[{t:"p",text:"You can build **sequences and graphs** of work where the output of one worker feeds the next, and steps run in order (or in parallel). WorkManager guarantees the ordering and passes data between steps. This models multi-step background pipelines: compress → upload → notify, or download several files in parallel then process the combined result."},{t:"code",title:"Sequential and parallel chains",code:`WorkManager.getInstance(context)
    .beginWith(listOf(downloadA, downloadB))   // these two run in PARALLEL
    .then(mergeWorker)                          // runs after BOTH finish
    .then(uploadWorker)                         // then this
    .enqueue()

// Output of one becomes input of the next automatically:
// mergeWorker's inputData includes the outputData of downloadA and downloadB`},{t:"list",items:["**`beginWith(...).then(...).then(...)`** — a sequential chain; each step runs after the previous succeeds. `beginWith(listOf(a, b))` runs a and b in *parallel*, and `then(c)` runs c after *both* complete.","**Data flows through the chain** — a worker's `Result.success(outputData)` becomes the next worker's `inputData` (merged if multiple predecessors, via an `InputMerger`).","**Failure propagation** — if any worker in a chain returns `failure()`, the *dependent* work is cancelled (the chain stops). A `retry()` retries just that worker before continuing.","**Use for multi-step pipelines** where steps depend on each other and the whole sequence must be guaranteed — e.g. process a photo (resize → filter → upload) reliably even across app kills."]}]},{heading:"Unique work — preventing duplicates",blocks:[{t:"p",text:"Often you want *only one* instance of a particular kind of work — one sync running at a time, not ten queued because the user pulled to refresh repeatedly. **Unique work** enforces this: you give the work a unique name and a policy for what to do if work with that name already exists."},{t:"code",title:"Unique work with a conflict policy",code:`WorkManager.getInstance(context).enqueueUniqueWork(
    "sync",                                    // unique name
    ExistingWorkPolicy.KEEP,                   // what to do if "sync" already exists
    syncRequest,
)

// For periodic:
WorkManager.getInstance(context).enqueueUniquePeriodicWork(
    "periodic_sync",
    ExistingPeriodicWorkPolicy.UPDATE,
    periodicSyncRequest,
)`},{t:"list",items:["**`ExistingWorkPolicy` for one-time work**: `KEEP` (if work with this name exists, ignore the new request — the common choice for 'ensure one sync is scheduled'), `REPLACE` (cancel the existing and enqueue the new), `APPEND`/`APPEND_OR_REPLACE` (chain the new after the existing).","**`ExistingPeriodicWorkPolicy` for periodic**: `KEEP` or `UPDATE` (update the existing periodic work's parameters without losing its schedule).","**Why it matters**: without unique work, repeated triggers (pull-to-refresh, multiple app launches) queue *duplicate* work — wasteful and potentially buggy (multiple concurrent syncs racing). Unique work with `KEEP` ensures exactly one is scheduled. This is a very common real-world requirement and a frequent interview point."]}]},{heading:"Expedited work and long-running (foreground) workers",blocks:[{t:"list",items:["**Expedited work** (`setExpedited(...)`) — for work that's *important and should run as soon as possible* (not deferred), while still being guaranteed. WorkManager runs it quickly (using a foreground service quota on newer Android, or high-priority job). Use for user-initiated work that shouldn't wait — e.g. sending a message the user just tapped 'send' on. It's the middle ground between deferrable and immediate.","**Long-running / foreground workers** (`setForeground(...)`) — for guaranteed work that takes a *long time* and should show the user a notification (a large file download/upload). The worker calls `setForeground(ForegroundInfo(...))` to run as a foreground service, so the OS won't kill it and the user sees progress. This combines WorkManager's *guarantee* with a foreground service's *immediacy and visibility*.","**The distinction**: expedited = 'run soon, important, brief'; foreground worker = 'run now, long-running, user-visible with a notification'. Both escalate beyond plain deferrable work when needed."]}]},{heading:"Retry, backoff, and cancellation",blocks:[{t:"code",title:"Backoff policy",code:`val request = OneTimeWorkRequestBuilder<SyncWorker>()
    .setBackoffCriteria(
        BackoffPolicy.EXPONENTIAL,          // or LINEAR
        WorkRequest.MIN_BACKOFF_MILLIS,     // starting delay (10s minimum)
        TimeUnit.MILLISECONDS,
    )
    .build()`},{t:"list",items:["**Backoff policy** — when a worker returns `Result.retry()`, WorkManager waits before retrying, increasing the delay per the policy: `EXPONENTIAL` (doubling — good for avoiding hammering a failing server) or `LINEAR`. Minimum backoff is 10 seconds. This prevents a failing task from retrying in a tight loop.","**Cancellation** — cancel by id (`cancelWorkById`), by unique name (`cancelUniqueWork`), by tag (`cancelAllWorkByTag`), or all (`cancelAllWork`). A cancelled worker's coroutine is cancelled cooperatively (respect cancellation in `doWork`).",'**Tags** — `addTag("sync")` groups related work so you can query or cancel by tag.',"**Hilt integration** — inject dependencies into workers with `@HiltWorker` + `@AssistedInject` and a `HiltWorkerFactory` (covered in the DI topic)."]},{t:"note",text:"WorkManager advanced: constraints (network/charging/battery/idle) gate when work runs — use UNMETERED + charging for heavy work. Chaining (beginWith/then) builds sequential/parallel pipelines with data flowing between steps; failure cancels dependents. Unique work (enqueueUniqueWork + KEEP/REPLACE) prevents duplicate work — a common requirement. Expedited work runs important tasks soon; foreground workers (setForeground) run long, user-visible guaranteed work. Backoff (exponential) spaces retries; cancel by id/name/tag."}]}],gC=[{level:"junior",q:"What are constraints in WorkManager and why are they useful?",a:[{t:"p",text:"**Constraints are conditions that must be true before WorkManager runs your work — like requiring network connectivity, charging, or the battery not being low.** WorkManager holds the work until all constraints are satisfied, then runs it, making background work battery- and data-friendly."},{t:"list",items:["**Network** — `CONNECTED` (any) or `UNMETERED` (Wi-Fi only). Use `UNMETERED` for large uploads/downloads so you don't consume the user's cellular data.","**Charging / battery not low** — run heavy or non-urgent work only while charging or when the battery is healthy, so you don't drain the user's battery.","**Device idle / storage not low** — run only when the device isn't actively used, or only if there's enough storage."]},{t:"code",title:"Adding constraints",code:`val constraints = Constraints.Builder()
    .setRequiredNetworkType(NetworkType.UNMETERED)
    .setRequiresCharging(true)
    .build()`},{t:"p",text:"Why they matter: they let you be a good citizen with the user's resources. A photo-backup app that syncs gigabytes should require Wi-Fi and charging, so it never eats mobile data or battery. WorkManager also *re-evaluates* constraints during execution — if a required constraint is violated mid-run (charger unplugged), it stops the worker and retries when conditions return, so your worker should handle being interrupted gracefully."}]},{level:"junior",q:"How do you ensure only one instance of a background task runs at a time?",a:[{t:"p",text:"**You use *unique work* — `enqueueUniqueWork` with a unique name and an `ExistingWorkPolicy` that decides what happens if work with that name already exists.** This prevents duplicate work from being queued when the same task is triggered multiple times."},{t:"code",title:"Unique work with KEEP",code:`WorkManager.getInstance(context).enqueueUniqueWork(
    "sync",                        // unique name
    ExistingWorkPolicy.KEEP,       // if "sync" already exists, ignore the new one
    syncRequest,
)`},{t:"list",items:["**`ExistingWorkPolicy.KEEP`** — if work with this name is already scheduled/running, ignore the new request. This is the common choice for 'ensure exactly one sync is scheduled' — repeated triggers don't pile up.","**`REPLACE`** — cancel the existing work and enqueue the new one (use when the new request supersedes the old).","**`APPEND` / `APPEND_OR_REPLACE`** — chain the new work after the existing one.","**For periodic work**, use `enqueueUniquePeriodicWork` with `KEEP` or `UPDATE`."]},{t:"p",text:"This solves a real, common problem: without unique work, actions like pull-to-refresh or launching the app repeatedly would each enqueue a *new* sync, resulting in many duplicate (and possibly concurrent, racing) syncs — wasteful and buggy. With `enqueueUniqueWork(..., KEEP, ...)`, only one is ever scheduled regardless of how many times it's triggered. It's a frequent interview point because duplicate background work is such a common mistake."}]},{level:"junior",q:"How do you run multiple background tasks in sequence with WorkManager?",a:[{t:"p",text:"**You chain them with `beginWith(...).then(...).then(...)`, and WorkManager guarantees the ordering and passes each step's output to the next.** This models multi-step background pipelines where later steps depend on earlier ones."},{t:"code",title:"A chain (with a parallel first step)",code:`WorkManager.getInstance(context)
    .beginWith(listOf(downloadA, downloadB))  // run in PARALLEL
    .then(mergeWorker)                         // after BOTH finish
    .then(uploadWorker)                        // then upload
    .enqueue()`},{t:"list",items:["**`beginWith(worker)`** starts the chain; **`.then(worker)`** adds a step that runs after the previous succeeds. Passing a *list* to `beginWith` or `then` runs those workers in *parallel*, and the next `.then` waits for all of them.","**Data flows through automatically** — a worker's `Result.success(outputData)` becomes the next worker's `inputData` (merged if there are multiple predecessors).","**Failure stops the chain** — if a worker returns `failure()`, the dependent (later) work is cancelled; a `retry()` retries just that step before continuing."]},{t:"p",text:"The whole chain is *guaranteed* like any WorkManager work — it survives app kills and reboots, resuming where it left off. So a 'compress → upload → notify server' pipeline runs reliably to completion even if the app is closed partway through. Use chaining when you have genuinely dependent multi-step background work; for independent tasks, just enqueue them separately."}]},{level:"junior",q:"What is a backoff policy in WorkManager?",a:[{t:"p",text:"**A backoff policy controls how long WorkManager waits before retrying work that returned `Result.retry()`, and how that delay grows with repeated failures.** It prevents a failing task from retrying in a tight loop that would waste battery and hammer a struggling server."},{t:"list",items:["**`BackoffPolicy.EXPONENTIAL`** — the delay doubles with each retry (e.g. 10s, 20s, 40s…). This is usually preferred because it backs off aggressively from a persistently failing operation, giving a struggling server room to recover.","**`BackoffPolicy.LINEAR`** — the delay increases by a fixed amount each time.","**Minimum backoff is 10 seconds** — you can't retry faster than that."]},{t:"code",title:"Setting exponential backoff",code:`OneTimeWorkRequestBuilder<SyncWorker>()
    .setBackoffCriteria(BackoffPolicy.EXPONENTIAL, 10, TimeUnit.SECONDS)
    .build()`},{t:"p",text:"It's the same principle as retry logic in networking — retry transient failures, but with *increasing* delays so you don't overwhelm a failing system or drain the battery with rapid retries. WorkManager applies the backoff automatically whenever `doWork()` returns `Result.retry()`, so you just return `retry()` for transient failures and configure the policy. The default is exponential with a 30-second initial delay, which is sensible for most cases."}]},{level:"senior",q:"What is expedited work and a long-running (foreground) worker, and when do you use each?",a:[{t:"p",text:"**Both escalate work beyond plain deferrable execution, but for different needs. *Expedited work* is for important work that should run *as soon as possible* (not deferred) while remaining guaranteed and brief. A *long-running / foreground worker* is for guaranteed work that takes a *long time* and should be visible to the user with a notification.**"},{t:"list",items:["**Expedited work** (`setExpedited(...)`): tells WorkManager 'run this quickly, don't defer it', while still giving you WorkManager's guarantee and constraint handling. On newer Android it runs using a foreground-service quota; if the quota is exhausted it falls back to a regular (deferred) job. Use it for *user-initiated, important, short* work that shouldn't wait — e.g. the user taps 'send' on a message, or 'process' on an action; you want it to happen now, reliably, but it's brief. It's the middle ground between fully deferrable work and an immediate foreground service.","**Long-running / foreground worker** (`setForeground(ForegroundInfo(...))` inside `doWork`): promotes the worker to run as a *foreground service*, showing a mandatory notification and preventing the OS from killing it during long execution. Use it for guaranteed work that runs for a while and that the user should see — a large file download or upload, exporting a big dataset. This combines WorkManager's *reliability* (persisted, survives kill/reboot, retries) with a foreground service's *immediacy and user-visibility* (runs now, shows progress)."]},{t:"list",items:["**The decision**: is it *important and brief* and shouldn't wait? → expedited. Is it *long-running and user-visible*? → foreground worker. Is it fine to *defer* (run under constraints whenever convenient)? → plain WorkManager. All three are guaranteed; they differ in *urgency* and *visibility*.","**Why not just a foreground service directly for the long case?** Because a raw foreground service doesn't survive process death/reboot or get automatic retries — a WorkManager foreground worker does, so you get reliability *plus* the foreground behavior. That's the advantage of `setForeground` over a standalone service."]},{t:"p",text:"**The senior framing**: WorkManager isn't only for 'run later when convenient' — it spans a spectrum. Expedited work handles 'run now-ish, important, short'; foreground workers handle 'run now, long, visible'; and both keep the guarantee (persistence, retries, reboot survival) that raw services and coroutines lack. Knowing that WorkManager can escalate to immediate and to foreground — and *why* you'd use `setForeground` over a plain foreground service (to keep the guarantee) — shows you understand WorkManager as the unified reliable-work solution, not just a deferred-task scheduler."}]},{level:"senior",q:"Design a reliable photo-upload feature with WorkManager: what constraints, uniqueness, chaining, and failure handling would you use?",a:[{t:"p",text:"**I'd design it as a guaranteed, constraint-aware, deduplicated pipeline that survives app death and handles failures with retries and backoff — using WorkManager's chaining, unique work, constraints, and (for large files) a foreground worker.** Here's the concrete design and the reasoning behind each choice:"},{t:"list",items:["**Local-first, then enqueue**: when the user picks a photo, immediately save a local record marking it 'pending upload' (in Room), so the UI shows it instantly and the intent is durable. Then enqueue the upload work. This decouples the user action from the network and makes the queue survive process death (the offline-write pattern).","**Constraints**: `setRequiredNetworkType(UNMETERED)` if you want to respect data plans (or `CONNECTED` with a user setting), and optionally `setRequiresBatteryNotLow(true)` for large batches. This ensures uploads don't burn cellular data or drain a low battery — being a good resource citizen.","**Unique work per photo, or a unique queue**: use `enqueueUniqueWork` keyed by the photo id (or a single named upload queue with `APPEND`) so the same photo isn't uploaded twice if the user retries, and duplicate triggers don't create duplicate uploads. `KEEP` or a per-id name prevents duplication.","**Chaining for multi-step processing**: if the photo needs processing, chain `beginWith(compressWorker).then(uploadWorker).then(notifyServerWorker)` — each step's output (the compressed file URI, the upload result) flows to the next, and the whole pipeline is guaranteed. If compression fails permanently, the upload won't run (dependent work is cancelled).","**Foreground worker for large files**: for a big upload, call `setForeground(ForegroundInfo(notification))` so it runs as a foreground service showing progress (`setProgress` → observed via `WorkInfo` in the UI), won't be killed mid-upload, and still keeps WorkManager's guarantee.","**Failure handling**: in `doWork`, return `Result.retry()` for transient failures (network errors, 5xx) with `EXPONENTIAL` backoff so retries don't hammer the server; return `Result.failure()` for permanent failures (invalid file, 400) so it doesn't loop forever. On permanent failure, update the local record to 'failed' so the UI can show an error and offer manual retry.","**Idempotency**: give each upload a client-generated id sent to the server, so a retried upload (WorkManager may retry after a network drop that actually succeeded server-side) doesn't create a duplicate on the backend — the server dedupes by the id.","**Observe for UI**: the ViewModel observes each work's `WorkInfo` Flow to show queued/uploading/progress/done/failed states, keeping the UI responsive while the actual upload is decoupled from any screen's lifetime."]},{t:"p",text:"**The senior framing**: the design combines several WorkManager features to hit the real requirements — *guarantee* (survives kill/reboot via persistence), *deduplication* (unique work), *resource-friendliness* (constraints), *user visibility* (foreground worker + progress), *resilience* (retry with backoff, idempotency keys), and *responsive UX* (local-first record + WorkInfo observation). The key insight is that reliable upload is a *distributed-systems* problem (ambiguous failures, duplicates, offline queueing), and WorkManager provides exactly the primitives to handle it correctly — which is why it's the right tool over a plain coroutine or service. Being able to assemble these pieces into a coherent, failure-aware design is what the question is really testing."}]},{level:"senior",q:"What is the OutOfQuotaPolicy for expedited work?",a:[{t:"p",text:"Expedited work runs against a system *quota* (limited to prevent abuse). `OutOfQuotaPolicy` decides what happens when the quota is exhausted: `RUN_AS_NON_EXPEDITED_WORK_REQUEST` (fall back to normal deferred work — the safe default) or `DROP_WORK_REQUEST` (cancel it). Set it in `setExpedited(policy)` so your important work degrades gracefully rather than failing when quota runs out."},{t:"code",title:"OutOfQuotaPolicy",code:".setExpedited(OutOfQuotaPolicy.RUN_AS_NON_EXPEDITED_WORK_REQUEST)  // fall back if no quota"},{t:"list",items:["**Quota** — expedited work is rate-limited.","**`RUN_AS_NON_EXPEDITED_WORK_REQUEST`** — fall back to deferred (safe).","**`DROP_WORK_REQUEST`** — cancel when out of quota.","**Graceful degradation** — important work still runs, just deferred."]},{t:"note",text:"Expedited work has a system quota; OutOfQuotaPolicy sets the fallback when exhausted: RUN_AS_NON_EXPEDITED_WORK_REQUEST (fall back to normal deferred — safe default) or DROP_WORK_REQUEST (cancel). Set it in setExpedited so important work degrades gracefully rather than failing."}]},{level:"senior",q:"How do you run a long-running Worker as a foreground service?",a:[{t:"p",text:"For work that takes a while (a large download), make it a *long-running worker* by calling `setForeground(getForegroundInfo())` — WorkManager runs it as a foreground service with a notification, so the system doesn't kill it and the user sees progress. Override `getForegroundInfo()` to supply the notification. This combines WorkManager's guarantees with foreground-service longevity."},{t:"code",title:"Foreground worker",code:`override suspend fun doWork(): Result {
    setForeground(getForegroundInfo())   // becomes a foreground service
    download()
    return Result.success()
}
override suspend fun getForegroundInfo() = ForegroundInfo(1, buildNotification())`},{t:"list",items:["**`setForeground`** — run as a foreground service.","**`getForegroundInfo`** — supply the notification.","**Not killed** — foreground priority; user sees progress.","**Long work** — large downloads/uploads."]},{t:"note",text:"For long work (large downloads), call setForeground(getForegroundInfo()) to run the Worker as a foreground service (notification, not killed, user sees progress); override getForegroundInfo() for the notification. Combines WorkManager's guarantees with foreground-service longevity."}]},{level:"senior",q:"How do you chain work with parallel and sequential steps?",a:[{t:"p",text:"Use `beginWith(...)`/`then(...)` to build a `WorkContinuation` — sequential steps run one after another (each starts when the prior succeeds), and you can run steps in *parallel* by passing a *list* of requests. Combine parallel results into a next step (WorkManager merges their output `Data` via an `InputMerger`). This models complex pipelines (compress 3 images in parallel, then upload)."},{t:"code",title:"Chaining",code:`WorkManager.getInstance(ctx)
    .beginWith(listOf(compress1, compress2, compress3))   // parallel
    .then(upload)                                          // after all succeed
    .then(cleanup)
    .enqueue()`},{t:"list",items:["**`beginWith`/`then`** — build a chain.","**Parallel** — pass a list of requests.","**Sequential** — `then` runs after the prior succeeds.","**`InputMerger`** — merges parallel outputs into the next input."]},{t:"note",text:"Chain with beginWith(...).then(...) → a WorkContinuation: sequential steps run after the prior succeeds; pass a list for parallel steps. WorkManager merges parallel outputs (InputMerger) into the next step's input. Models pipelines (compress N images in parallel, then upload, then cleanup). A failure fails the chain."}]},{level:"senior",q:"What is an InputMerger, and when do you need one?",a:[{t:"p",text:"When multiple parallel workers feed into a single next worker, WorkManager combines their output `Data` into the next worker's input using an `InputMerger`. The default `OverwritingInputMerger` overwrites same keys; `ArrayCreatingInputMerger` collects same-key values into arrays. Set it with `setInputMerger` on the merging request — needed when parallel steps produce data the next step must combine."},{t:"list",items:["**Combines parallel outputs** — into the next worker's input.","**`OverwritingInputMerger`** — default; same keys overwrite.","**`ArrayCreatingInputMerger`** — collect same-key values into arrays.","**`setInputMerger`** — on the merging request."]},{t:"note",text:"An InputMerger combines multiple parallel workers' output Data into the next worker's input: OverwritingInputMerger (default — same keys overwrite) or ArrayCreatingInputMerger (collect same-key values into arrays). Set via setInputMerger on the merging request when parallel steps produce data the next must combine."}]},{level:"senior",q:"How do you limit retries and give up after too many attempts?",a:[{t:"p",text:"WorkManager retries indefinitely by default (per backoff) — to *cap* retries, check `runAttemptCount` inside `doWork()` and return `Result.failure()` after a threshold. This prevents infinite retry loops for work that keeps failing (a permanently-bad request). Combine with distinguishing transient (`retry()`) from permanent (`failure()`) errors."},{t:"code",title:"Capping retries",code:`override suspend fun doWork(): Result {
    return try { sync(); Result.success() }
    catch (e: IOException) {
        if (runAttemptCount >= 3) Result.failure() else Result.retry()
    }
}`},{t:"list",items:["**`runAttemptCount`** — how many times this ran.","**Cap** — `failure()` after a threshold.","**Avoid infinite loops** — for permanently-failing work.","**Transient vs permanent** — retry vs fail deliberately."]},{t:"note",text:"WorkManager retries indefinitely by default; cap it by checking runAttemptCount in doWork() and returning Result.failure() after a threshold — preventing infinite loops for permanently-failing work. Combine with distinguishing transient errors (retry()) from permanent (failure())."}]},{level:"senior",q:"How do you ensure unique periodic work isn't duplicated?",a:[{t:"p",text:"Use `enqueueUniquePeriodicWork(name, ExistingPeriodicWorkPolicy.KEEP, request)` — with `KEEP`, if periodic work with that name already exists, the new request is ignored (no duplicate schedule). Use `UPDATE`/`REPLACE` to change the existing one. This is essential because enqueueing periodic work on every app start would otherwise create multiple schedules."},{t:"code",title:"Unique periodic",code:`WorkManager.getInstance(ctx).enqueueUniquePeriodicWork(
    "sync", ExistingPeriodicWorkPolicy.KEEP, syncRequest)`},{t:"list",items:["**`enqueueUniquePeriodicWork(name, policy, request)`** — named periodic work.","**`KEEP`** — ignore new if one exists (no duplicate).","**`UPDATE`/`REPLACE`** — change the existing schedule.","**Essential** — avoid multiple schedules from repeated enqueues."]},{t:"note",text:"Use enqueueUniquePeriodicWork(name, ExistingPeriodicWorkPolicy.KEEP, request) — KEEP ignores the new request if periodic work with that name exists (no duplicate). UPDATE/REPLACE changes it. Essential because enqueueing periodic work on every app start would otherwise create multiple schedules."}]},{level:"senior",q:"What is the difference between KEEP, REPLACE, and APPEND work policies?",a:[{t:"p",text:"For `enqueueUniqueWork`: `KEEP` ignores the new request if one is pending (dedupe); `REPLACE` cancels the existing and enqueues the new (latest wins); `APPEND` adds the new work to run *after* the existing chain (queue). `APPEND_OR_REPLACE` appends unless the existing failed/cancelled. Choose by whether duplicates should be ignored, replaced, or queued."},{t:"list",items:["**`KEEP`** — ignore new if pending (dedupe).","**`REPLACE`** — cancel existing, enqueue new (latest wins).","**`APPEND`** — queue after the existing chain.","**`APPEND_OR_REPLACE`** — append unless the existing failed."]},{t:"note",text:"enqueueUniqueWork policies: KEEP (ignore new if pending — dedupe), REPLACE (cancel existing + enqueue new — latest wins), APPEND (queue after the existing chain), APPEND_OR_REPLACE (append unless existing failed/cancelled). Choose by whether duplicates should be ignored, replaced, or queued."}]},{level:"senior",q:"How do you handle failures in a work chain?",a:[{t:"p",text:"If any worker in a chain returns `Result.failure()`, the *entire chain stops* — dependent workers are marked `FAILED` and don't run. So return `retry()` for transient failures (WorkManager retries that worker with backoff) and `failure()` only for permanent errors. Design chains so a recoverable step retries rather than failing the whole pipeline, and observe the terminal `WorkInfo` for the outcome."},{t:"list",items:["**Failure stops the chain** — dependents marked FAILED.","**`retry()`** — for transient (retries that step with backoff).","**`failure()`** — permanent only.","**Observe** — the chain's terminal WorkInfo for the outcome."]},{t:"note",text:"A Result.failure() in a chain stops the whole chain (dependents marked FAILED, don't run). Return retry() for transient failures (WorkManager retries that worker with backoff), failure() only for permanent errors. Design so recoverable steps retry, not fail the pipeline; observe the terminal WorkInfo for the outcome."}]},{level:"senior",q:"How do you design a reliable data-sync-on-reconnect feature?",a:[{t:"p",text:"Queue changes locally (an outbox/pending table) as the user makes them offline, and enqueue a *unique* sync `Worker` with a `NetworkType.CONNECTED` constraint — WorkManager runs it when connectivity returns. The worker sends pending changes to the server (with idempotency keys for safe retries), marks them synced, and retries transient failures with backoff. Observe `WorkInfo` to show sync status."},{t:"list",items:["**Local outbox** — queue offline changes in the DB.","**Unique worker + network constraint** — runs on reconnect.","**Idempotency keys** — safe retries; mark synced on success.","**Backoff + WorkInfo** — retry transient failures; show status."]},{t:"note",text:"Sync-on-reconnect: queue offline changes in a local outbox/pending table, enqueue a unique sync Worker with a NetworkType.CONNECTED constraint (runs when online), send pending changes with idempotency keys (safe retries), mark synced on success, retry transient failures with backoff, and observe WorkInfo for status."}]},{level:"senior",q:"How do you observe the final result of a chained work sequence?",a:[{t:"p",text:"Enqueue the chain with a *unique name*, then observe `getWorkInfosForUniqueWorkLiveData(name)` (or by the last worker's id) — it gives the `WorkInfo` list for the chain, from which you read the terminal state and the final worker's output `Data`. Since a chain succeeds only when all steps succeed, watch for the last worker's `SUCCEEDED` (or any `FAILED`) to know the overall outcome."},{t:"list",items:["**Unique name** — reference the whole chain.","**`getWorkInfosForUniqueWork`** — the chain's WorkInfos.","**Terminal state** — last worker SUCCEEDED / any FAILED.","**Final output** — the last worker's output Data."]},{t:"note",text:"Observe a chain via a unique name: getWorkInfosForUniqueWorkLiveData(name) (or the last worker's id) gives the chain's WorkInfos — read the terminal state and the final worker's output. A chain succeeds only when all steps do, so watch the last worker's SUCCEEDED (or any FAILED) for the outcome."}]},{level:"junior",q:"How do constraints interact with Doze mode and battery optimization?",a:[{t:"p",text:"WorkManager constraints (network, charging) already defer work to appropriate conditions, but *Doze mode* (deep sleep) further batches and delays deferrable work into maintenance windows — so constrained work may wait longer when the device is idle. WorkManager respects these OS restrictions (it's designed to be battery-friendly). Expedited/foreground work bypasses some deferral for urgent tasks."},{t:"list",items:["**Constraints defer** — to appropriate conditions.","**Doze batches** — deferrable work into maintenance windows.","**Longer waits** — when idle; WorkManager respects this.","**Expedited/foreground** — bypass some deferral for urgent work."]},{t:"note",text:"WorkManager constraints defer work to good conditions, and Doze further batches/delays deferrable work into maintenance windows (longer waits when idle) — WorkManager respects these battery restrictions. Expedited/foreground work bypasses some deferral for urgent tasks."}]},{level:"senior",q:"How do you migrate periodic work to run more frequently than 15 minutes?",a:[{t:"p",text:"WorkManager's periodic minimum is 15 minutes (a battery constraint). For more frequent execution, you can: chain *one-time* work that re-enqueues itself with a shorter `setInitialDelay` (though the system may still throttle), use a *foreground service* with your own loop for genuinely continuous work, or reconsider the need (frequent polling drains battery — prefer *push*/FCM). There's no way to make *periodic* work faster than 15 minutes."},{t:"list",items:["**15-min minimum** — a hard limit for periodic work.","**Self-rescheduling one-time work** — shorter delays (still throttled).","**Foreground service** — for continuous work with your own loop.","**Prefer push** — FCM over frequent polling (battery)."]},{t:"note",text:"Periodic work's 15-min minimum is a hard battery limit. For faster: chain self-rescheduling one-time work with a short setInitialDelay (system may throttle), or a foreground service with your own loop for continuous work. Better: use FCM push over frequent polling (battery). Periodic can't go below 15 min."}]},{level:"senior",q:"What is the difference between constraints and initial delay?",a:[{t:"p",text:"A *constraint* is a *condition* that must be true for work to run (network connected, charging) — WorkManager waits indefinitely until it's met. An *initial delay* is a *time* offset — the earliest the work can run after enqueue. They combine: work runs when *both* the delay has passed *and* all constraints are satisfied. Constraints are about device state; delay is about timing."},{t:"list",items:["**Constraint** — a condition (network/charging); waits until met.","**Initial delay** — earliest run time after enqueue.","**Combined** — both delay elapsed AND constraints met.","**State vs timing** — constraints = device state, delay = time."]},{t:"note",text:"A constraint is a condition that must hold for work to run (network/charging — WorkManager waits until met); an initial delay is the earliest run time after enqueue. Work runs when both the delay has elapsed AND all constraints are satisfied. Constraints = device state; delay = timing."}]},{level:"senior",q:"How do you cancel a running Worker gracefully and clean up?",a:[{t:"p",text:"When a Worker is cancelled (by id/tag or constraint loss), a `CoroutineWorker`'s coroutine is *cancelled* — so cooperative suspend calls throw `CancellationException` and stop. Handle cleanup in a `try/finally` (release resources, close streams, revert partial state), using `withContext(NonCancellable)` for suspend cleanup that must finish. For a synchronous `Worker`, check `isStopped` in loops to exit early."},{t:"list",items:["**Cancellation** — CoroutineWorker's coroutine is cancelled.","**`try/finally`** — clean up (resources, streams, partial state).","**`NonCancellable`** — for suspend cleanup that must complete.","**Sync Worker** — check `isStopped` in loops."]},{t:"note",text:"On cancellation, a CoroutineWorker's coroutine is cancelled (cooperative suspend calls throw CancellationException). Clean up in try/finally (resources, streams, revert partial state), using withContext(NonCancellable) for must-finish suspend cleanup. A synchronous Worker checks isStopped in loops to exit early."}]},{level:"senior",q:"How do you schedule work that syncs data efficiently?",a:[{t:"p",text:"For periodic sync: a `PeriodicWorkRequest` (unique, KEEP) with network constraints, syncing to the DB (source of truth) with retry/backoff. For *event-driven* freshness, trigger a *one-time* expedited sync from an FCM data message. Combine: periodic baseline sync + push-triggered immediate sync. Use delta sync (changes since last) to minimize data, and constraints to run on good networks."},{t:"list",items:["**Periodic baseline** — unique periodic work + network constraint.","**Push-triggered** — FCM → one-time expedited sync for freshness.","**Write to the DB** — source of truth; retry/backoff.","**Delta sync** — changes since last, to minimize data."]},{t:"note",text:"Efficient sync: a unique PeriodicWorkRequest (KEEP) with network constraints as a baseline, plus FCM-triggered one-time expedited sync for event-driven freshness. Write to the DB (source of truth) with retry/backoff, use delta sync (changes since last) to minimize data, and constraints to run on good networks."}]},{level:"junior",q:"What states can work be in, and what do they mean?",a:[{t:"p",text:"`WorkInfo.State`: `ENQUEUED` (scheduled, waiting for constraints/delay), `RUNNING` (executing), `SUCCEEDED` (finished with `success()`), `FAILED` (finished with `failure()`), `BLOCKED` (waiting for prerequisite work in a chain), and `CANCELLED`. SUCCEEDED/FAILED/CANCELLED are terminal for one-time work; periodic work returns to ENQUEUED after each run."},{t:"list",items:["**ENQUEUED** — scheduled, waiting.","**RUNNING** — executing now.","**SUCCEEDED/FAILED** — terminal outcomes.","**BLOCKED/CANCELLED** — waiting on prerequisites / cancelled."]},{t:"note",text:"WorkInfo.State: ENQUEUED (waiting for constraints/delay), RUNNING, SUCCEEDED (success()), FAILED (failure()), BLOCKED (waiting on chain prerequisites), CANCELLED. Terminal for one-time work; periodic work returns to ENQUEUED after each run. Observe these for UI progress/completion."}]},{level:"senior",q:"How do you handle work that depends on user authentication?",a:[{t:"p",text:"If work needs a valid token (upload requires auth), handle expiry *inside* the Worker: refresh the token via your auth flow, and if refresh fails (session truly expired), return `Result.failure()` (don't retry indefinitely) and signal the app to prompt re-login. Don't retry auth-failed work forever. Consider whether the work should be *cancelled on logout* (cancel by tag/name when the user logs out)."},{t:"list",items:["**Refresh in the Worker** — get a valid token before the request.","**Refresh fails → `failure()`** — don't retry forever; prompt re-login.","**Cancel on logout** — cancel pending auth-dependent work.","**Don't loop** — distinguish transient from permanent auth failure."]},{t:"note",text:"For auth-dependent work: refresh the token inside the Worker before the request; if refresh fails (session expired), return Result.failure() (don't retry forever) and signal the app to prompt re-login. Cancel pending auth-dependent work on logout (by tag/name). Distinguish transient (retry) from permanent auth failure."}]},{level:"senior",q:"What is the execution time limit for a Worker, and how do you handle long work?",a:[{t:"p",text:"A Worker has roughly a *10-minute* execution window (from the underlying JobScheduler) before the system may stop it. For work that can exceed this, run it as a *long-running/foreground worker* (`setForeground`) which isn't subject to the same limit and shows a notification, or *break the work into smaller chunks* across multiple workers (chaining or self-rescheduling). Don't assume unlimited runtime in a normal Worker."},{t:"list",items:["**~10-minute limit** — the system may stop a Worker after it.","**Foreground worker** — `setForeground` for longer work.","**Chunk it** — split across workers (chain/reschedule).","**Don't assume unlimited** — in a normal Worker."]},{t:"note",text:"A Worker has ~10 minutes of execution (from JobScheduler) before the system may stop it. For longer work, run a foreground worker (setForeground — not subject to the limit, shows a notification) or chunk the work across multiple workers (chaining/self-reschedule). Don't assume unlimited runtime."}]},{level:"junior",q:"How do you debug and inspect WorkManager?",a:[{t:"p",text:"Use Android Studio's *Background Task Inspector* to see enqueued/running work, their states, constraints, and chains visually. From the command line, `adb shell dumpsys jobscheduler` shows scheduled jobs. Enable WorkManager's verbose logging (via `Configuration.setMinimumLoggingLevel`) for detailed logs. Observing `WorkInfo` in code also helps trace states."},{t:"list",items:["**Background Task Inspector** — visualize work, states, chains.","**`adb shell dumpsys jobscheduler`** — scheduled jobs.","**Verbose logging** — `setMinimumLoggingLevel`.","**`WorkInfo`** — observe states in code."]},{t:"note",text:"Debug WorkManager with Android Studio's Background Task Inspector (visualize work/states/constraints/chains), adb shell dumpsys jobscheduler (scheduled jobs), WorkManager verbose logging (Configuration.setMinimumLoggingLevel), and observing WorkInfo in code."}]},{level:"senior",q:"How does a Worker notify the user or update the UI when it finishes?",a:[{t:"p",text:"A Worker can post a *notification* directly (for user-facing completion, like 'upload done'). To update the *UI*, don't touch views from the Worker — instead write results to the *DB* (which the UI observes) or observe the Worker's `WorkInfo` in the ViewModel/UI (`getWorkInfoByIdLiveData`) and react to `SUCCEEDED`/output. The Worker runs in the background, so communicate via persisted state or WorkInfo, not direct UI calls."},{t:"list",items:["**Notification** — for user-facing completion.","**Write to DB** — the UI observes it (single source of truth).","**Observe `WorkInfo`** — react to SUCCEEDED/output in the UI.","**No direct UI** — the Worker is background; use persisted state/WorkInfo."]},{t:"note",text:"A Worker can post a notification for user-facing completion. To update the UI, write results to the DB (UI observes it) or observe the Worker's WorkInfo (getWorkInfoByIdLiveData) in the ViewModel and react to SUCCEEDED/output — don't touch views from the background Worker."}]},{level:"senior",q:"How do you schedule work for a specific time of day?",a:[{t:"p",text:"WorkManager isn't for exact times, but you can *approximate* a daily task: compute the delay until the target time and use `setInitialDelay`, then re-enqueue for the next day on completion (or use periodic work with a ~24h interval, accepting the flex window). For a *precise* time (an alarm at 8:00 AM), use `AlarmManager` with `setExactAndAllowWhileIdle` instead — WorkManager only guarantees eventual, deferrable execution."},{t:"list",items:["**Approximate** — `setInitialDelay` to the target time; re-enqueue daily.","**Periodic ~24h** — accept the flex window (not exact).","**Exact time** — use `AlarmManager` (`setExactAndAllowWhileIdle`).","**WorkManager** — eventual/deferrable, not precise timing."]},{t:"note",text:"WorkManager isn't for exact times: approximate a daily task with setInitialDelay to the target and re-enqueue daily (or periodic ~24h, accepting the flex window). For a precise time (8:00 AM alarm), use AlarmManager (setExactAndAllowWhileIdle). WorkManager guarantees eventual, deferrable execution only."}]},{level:"senior",q:"How do you handle very large or resumable file uploads?",a:[{t:"p",text:"For large uploads, run a *foreground worker* (avoiding the ~10-min limit) and use a *resumable/chunked* upload protocol (e.g. tus, or the server's multipart resumable API) — upload in chunks, track the offset, and on interruption/retry resume from the last confirmed offset rather than restarting. Report progress via `setProgress`, use network constraints, and persist upload state so a retry continues where it left off."},{t:"list",items:["**Foreground worker** — avoid the time limit; show progress.","**Resumable/chunked** — upload in chunks, track offset.","**Resume on retry** — from the last confirmed offset.","**Persist state + constraints** — continue after interruption."]},{t:"note",text:"Large/resumable uploads: run a foreground worker (avoid the ~10-min limit, show progress) with a resumable/chunked protocol (tus/server resumable API) — upload chunks, track the offset, resume from the last confirmed offset on retry (don't restart). Persist upload state, use network constraints, report progress via setProgress."}]},{level:"senior",q:"What is the difference between setForeground work and a standalone foreground service?",a:[{t:"p",text:"A `setForeground` *worker* is WorkManager running your work as a foreground service temporarily — it gets WorkManager's guarantees (persistence, constraints, retry) *plus* foreground longevity for that task. A *standalone* foreground service is a component *you* manage directly (start/stop, notification, lifecycle) for ongoing user-visible work (music, navigation). Use the foreground worker for guaranteed background *tasks* that happen to be long; a standalone service for continuous user-facing features."},{t:"list",items:["**Foreground worker** — WorkManager guarantees + foreground longevity for a task.","**Standalone service** — you manage it; ongoing user-visible work.","**Worker** — for guaranteed background tasks that are long.","**Service** — for continuous features (music/nav)."]},{t:"note",text:"A setForeground worker is WorkManager running your task as a foreground service temporarily — WorkManager's guarantees (persistence/constraints/retry) plus foreground longevity. A standalone foreground service is a component you manage directly for ongoing user-visible work (music/nav). Worker for guaranteed long tasks; service for continuous features."}]},{level:"senior",q:"How do you observe work across app restarts?",a:[{t:"p",text:"Because WorkManager persists work with stable *unique names*/*ids*, you can re-observe it after an app restart by name: `getWorkInfosForUniqueWorkLiveData(name)` (or by id if you persisted the id). On app launch, query the state of ongoing work (an in-progress upload) and restore the UI accordingly. Use unique names so you can reliably reference work whose id you didn't persist."},{t:"list",items:["**Unique names** — reference persisted work reliably after restart.","**`getWorkInfosForUniqueWork`** — re-observe by name.","**On launch** — query ongoing work, restore UI.","**Persist the id** — or use a unique name to find it."]},{t:"note",text:"WorkManager persists work with stable unique names/ids, so re-observe after restart via getWorkInfosForUniqueWorkLiveData(name) (or by persisted id). On launch, query ongoing work (an in-progress upload) and restore the UI. Use unique names to reference work whose id you didn't persist."}]}],yC={id:"workmanager-advanced",title:"WorkManager Advanced",tagline:"Constraints, chaining (sequential/parallel with data flow), unique work to prevent duplicates, expedited & foreground workers, and backoff/retry/cancellation.",tags:["WorkManager","Constraints","Chaining","Unique Work","Expedited"],content:fC,qa:gC},wC=[{heading:"AlarmManager — exact-time execution",blocks:[{t:"p",text:"**AlarmManager** schedules work to run at a *specific time* (or after a specific delay), even waking the device if needed. It's the tool for **exact timing** — a calendar reminder at 9:00 AM, an alarm clock, a medication reminder — where the work must fire at a precise moment. This is the key difference from WorkManager: WorkManager is *deferrable* (runs when convenient under constraints), while AlarmManager is about *when*, precisely."},{t:"code",title:"Scheduling an alarm",code:`val alarmManager = context.getSystemService(Context.ALARM_SERVICE) as AlarmManager
val pendingIntent = PendingIntent.getBroadcast(
    context, requestCode, Intent(context, AlarmReceiver::class.java),
    PendingIntent.FLAG_IMMUTABLE,
)

// Exact alarm at a specific time (fires even in Doze with ...AllowWhileIdle)
alarmManager.setExactAndAllowWhileIdle(
    AlarmManager.RTC_WAKEUP,        // wall-clock time, wake the device
    triggerTimeMillis,
    pendingIntent,
)`},{t:"list",items:["**It fires a `PendingIntent`** — usually to a `BroadcastReceiver` — when the time arrives. The receiver's `onReceive` runs briefly; for real work, it hands off to WorkManager or a foreground service (onReceive must be fast).","**Alarm types**: `RTC_WAKEUP` (wall-clock time, wakes the device) / `RTC` (doesn't wake); `ELAPSED_REALTIME_WAKEUP` / `ELAPSED_REALTIME` (time since boot). Choose based on whether you need wall-clock time and whether to wake a sleeping device.","**Exact vs inexact**: `setExact`/`setExactAndAllowWhileIdle` fire at a precise time (the latter even during Doze); `set`/`setInexactRepeating` let the system batch them to save battery (fire *around* the time). Use exact only when precision genuinely matters.","**Survives reboot with a boot receiver**: alarms are cleared on reboot, so you register a `BOOT_COMPLETED` receiver to re-schedule them — the same pattern WorkManager does internally."]}]},{heading:"Exact alarm restrictions",blocks:[{t:"list",items:["**Exact alarms are increasingly restricted** because they wake the device and drain battery. Since Android 12, using exact alarms requires the `SCHEDULE_EXACT_ALARM` permission (and on Android 13+/14, for many apps this is a *special* permission the user must grant in Settings, or you use `USE_EXACT_ALARM` only if your app is a genuine alarm-clock/calendar app).","**The guidance**: only use exact alarms when the feature *fundamentally requires* firing at an exact time that the user is aware of (an alarm clock, a precise reminder). For anything else — periodic sync, deferrable notifications — use WorkManager, which is battery-friendly and doesn't need the permission.","**Doze interaction**: even exact alarms are subject to Doze unless you use `setExactAndAllowWhileIdle` (which is rate-limited — you can't fire these too frequently). This reflects the OS's ongoing push to limit battery-draining wakeups."]}]},{heading:"Foreground services — ongoing, user-visible work",blocks:[{t:"p",text:"A **foreground service** runs ongoing work that the user is *aware of*, showing a persistent notification. It's for tasks that must run *continuously right now* and that the user knows about — music playback, turn-by-turn navigation, fitness/location tracking, an active call, a large ongoing download. The mandatory notification is the deal: the user sees the service is running (and can't dismiss it), which is why the OS lets it run when background services can't."},{t:"code",title:"Starting a foreground service",code:`class LocationService : Service() {
    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        val notification = buildNotification()      // required
        startForeground(NOTIFICATION_ID, notification, FOREGROUND_SERVICE_TYPE_LOCATION)
        // do the ongoing work (on a background thread/coroutine)
        return START_STICKY
    }
    override fun onBind(intent: Intent?) = null
}
// Start it:
ContextCompat.startForegroundService(context, Intent(context, LocationService::class.java))`},{t:"list",items:["**Must call `startForeground(...)` promptly** (within ~5-10 seconds of starting) with a notification, or the system kills it (and throws). The notification is not optional — it's what makes it a 'foreground' service.","**Foreground service types** (Android 10+): you declare a type (`location`, `mediaPlayback`, `camera`, `microphone`, `dataSync`, etc.) in the manifest and pass it to `startForeground`. Android 14 tightened this — each type has requirements and the type must match the actual work.","**Still runs on the main thread by default** — like any service, a foreground service doesn't automatically use a background thread; you move the actual work to a coroutine/thread.","**vs a WorkManager foreground worker**: for guaranteed *and* immediate *and* user-visible work (a large upload that must complete), a WorkManager foreground worker (`setForeground`) is often better — you get the notification *plus* WorkManager's persistence and retries. Use a raw foreground service for truly ongoing continuous tasks (media, navigation) that aren't a discrete unit of work."]}]},{heading:"Foreground service restrictions (Android 12+)",blocks:[{t:"list",items:["**You can't start a foreground service from the background** (Android 12+) — there are restrictions on launching foreground services while your app is in the background, to prevent abuse. There are exceptions (from a high-priority FCM message, a user action, an exact alarm, etc.), but generally you start foreground services in response to user interaction or an allowed trigger.","**Android 14 requires declared types with justification** — each foreground service type has specific use-case requirements, and using the wrong type or an unjustified one can cause rejection. This is part of the ongoing tightening of what apps can do in the background.","**The trend**: like background services and receivers, foreground services are increasingly governed to protect battery and privacy. The sanctioned uses are genuinely user-visible ongoing tasks; for deferrable guaranteed work, WorkManager (which can escalate to a foreground worker when needed) is preferred."]}]},{heading:"Bound services and IPC (briefly)",blocks:[{t:"list",items:["**Bound services** — a component `bindService()`s to a service to get an interface and call its methods (a client-server relationship), and the service lives while bound. Used for an in-app API surface or, across processes, **IPC via AIDL** (Android Interface Definition Language, which generates the marshalling code for cross-process method calls).","**IPC (Inter-Process Communication)** — how separate processes/apps communicate: `Messenger` (simpler, message-based, serialized through a Handler), `AIDL` (full interface-based, supports concurrent calls, more complex), or higher-level mechanisms (ContentProviders, broadcasts, `BroadcastReceiver`). The underlying transport is Binder.","**When it comes up**: multi-process apps, a service exposed to other apps, or communicating with a system service. Most apps don't need bound services / AIDL, but knowing they exist (and that Binder underlies IPC) is useful for depth questions."]},{t:"note",text:"AlarmManager & foreground services: AlarmManager = exact-time execution (fires a PendingIntent at a precise moment, waking the device) — for alarms/reminders; increasingly restricted (SCHEDULE_EXACT_ALARM permission), so use only when exact timing is essential, WorkManager otherwise. Foreground service = ongoing user-visible work with a mandatory notification (music, navigation, tracking); must call startForeground promptly with a declared type; restricted from background starts (Android 12+). For guaranteed+immediate+visible work, prefer a WorkManager foreground worker. Bound services/AIDL handle IPC."}]}],vC=[{level:"junior",q:"What is AlarmManager used for, and how does it differ from WorkManager?",a:[{t:"p",text:"**AlarmManager schedules work to run at a *specific, exact time* (or after a precise delay), even waking the device — it's the tool for exact timing.** Use it for things that must fire at a precise moment: an alarm clock, a calendar reminder at 9:00 AM, a medication reminder."},{t:"p",text:"**The difference from WorkManager is *exact timing vs deferrable*.** WorkManager runs work when *convenient* under constraints (network, charging) — it's deferrable and battery-friendly, and doesn't guarantee a precise fire time. AlarmManager fires at an *exact* time you specify. So the decision is: does the work need to happen at a precise moment the user expects (reminder, alarm)? → AlarmManager. Can it run 'sometime soon under the right conditions' (sync, upload)? → WorkManager. AlarmManager fires a `PendingIntent` (usually to a BroadcastReceiver) when the time arrives; since `onReceive` must be fast, the receiver typically hands off real work to WorkManager or a foreground service. One more note: exact alarms are increasingly restricted (they wake the device and drain battery), requiring special permission on modern Android, so you should only use them when exact timing is genuinely essential."}]},{level:"junior",q:"What is a foreground service and when do you use one?",a:[{t:"p",text:"**A foreground service runs ongoing work that the user is *aware of*, showing a persistent (non-dismissible) notification. It's for tasks that must run continuously *right now* and that the user knows about** — music playback, turn-by-turn navigation, fitness/location tracking, an active call, a large ongoing download."},{t:"list",items:["**The mandatory notification is the point** — it tells the user the service is running and consuming resources, which is *why* the OS permits a foreground service to run when ordinary background services (since Android 8) can't.","**You must call `startForeground(...)` promptly** (within seconds of starting) with a notification, or the system kills the service and throws.","**Foreground service types** (Android 10+) — you declare a type like `location`, `mediaPlayback`, `camera`, `dataSync` in the manifest, and Android 14 requires the type to match the actual work.","**It still runs on the main thread by default** — like any service, you must move the actual work to a background thread/coroutine."]},{t:"p",text:"You use it specifically for *ongoing, immediate, user-visible* work — not deferrable work (that's WorkManager) and not exact-time triggers (AlarmManager). A useful nuance: for guaranteed work that's *also* immediate and user-visible (a large upload that must complete), a *WorkManager foreground worker* (`setForeground`) is often better than a raw foreground service, because you get the notification plus WorkManager's persistence and retries. Use a raw foreground service for genuinely continuous tasks like media or navigation that aren't a discrete unit of work."}]},{level:"junior",q:"Why are exact alarms restricted on modern Android?",a:[{t:"p",text:"**Because exact alarms wake the device at a precise time — potentially from a low-power Doze state — which drains battery, and apps were overusing them.** Firing exact alarms frequently, or using them for things that don't truly need exact timing (like periodic polling), was a significant battery cost across the ecosystem. So Android progressively restricted them to protect battery life."},{t:"list",items:["**Android 12+**: using exact alarms requires the `SCHEDULE_EXACT_ALARM` permission. On Android 13+/14, for most apps this became a *special* permission the user must grant in Settings — you can't just declare it and use it freely.","**Genuine alarm/calendar apps** can use `USE_EXACT_ALARM` (granted at install) since exact timing is their core purpose, but general apps must justify and request `SCHEDULE_EXACT_ALARM` or avoid exact alarms.","**Even exact alarms respect Doze** unless you use `setExactAndAllowWhileIdle`, which is itself rate-limited (you can't fire these too often)."]},{t:"p",text:"The guidance that follows: only use exact alarms when the feature *fundamentally requires* firing at an exact, user-expected time — an alarm clock, a specific reminder. For anything deferrable — background sync, periodic tasks, notifications that don't need a precise second — use WorkManager, which is battery-friendly and needs no special permission. This is the same theme as the broader background restrictions: the OS steers you away from battery-draining precise wakeups toward deferrable, batched, constraint-based work, and reserves exact timing for the narrow cases that genuinely need it."}]},{level:"junior",q:"What is the relationship between AlarmManager, BroadcastReceiver, and doing actual work?",a:[{t:"p",text:"**AlarmManager fires a `PendingIntent` (typically targeting a `BroadcastReceiver`) at the scheduled time; the receiver's `onReceive` runs briefly to *react*, but because `onReceive` must be fast, it hands off any real work to WorkManager or a foreground service.** These three pieces form the common exact-time work pattern."},{t:"list",items:["**AlarmManager** — schedules the trigger (the *when*), firing a PendingIntent at the exact time.","**BroadcastReceiver** — receives the fired intent in `onReceive`. But `onReceive` runs on the main thread with a ~10-second limit, so it's only for a quick reaction (show a notification, enqueue work) — *not* for actual heavy work.","**WorkManager / foreground service** — where the real work goes. If the alarm needs to trigger a sync or a substantial task, the receiver enqueues a WorkManager job or starts a foreground service, which does the work properly off the main thread."]},{t:"p",text:"So a medication reminder at an exact time might be: AlarmManager fires at the time → BroadcastReceiver's `onReceive` runs → it posts a notification (quick, fine to do in onReceive) or, if it needs to fetch data first, enqueues a WorkManager job to do that and then notify. The key rule is *don't do slow work in onReceive* — it's a trigger, and heavier work belongs in WorkManager (deferrable) or a foreground service (immediate/ongoing). This mirrors the general principle that BroadcastReceivers are lightweight reaction points, not places to do the actual work."}]},{level:"senior",q:"What are the restrictions on starting foreground services from the background, and why do they exist?",a:[{t:"p",text:"**Since Android 12, apps generally cannot start a foreground service while the app is in the background — attempting to throws a `ForegroundServiceStartNotAllowedException`. This exists to stop apps from silently launching foreground services (and their persistent, resource-consuming work) when the user isn't actively engaging with the app.** It's part of the same battery-and-privacy tightening as the background service and exact-alarm restrictions."},{t:"list",items:["**The problem it addresses**: before this, an app could wake up in the background (via a broadcast, a job) and start a foreground service to do ongoing work, effectively bypassing background limits. Multiplied across many apps, this drained battery and let apps run persistent work the user didn't initiate. The restriction closes that loophole.","**The exceptions (allowed background starts)**: you *can* start a foreground service from the background in specific sanctioned cases — in response to a *user action* (even recent), from a *high-priority FCM message* (so a genuinely time-sensitive push, like an incoming call, can start a service), from an *exact alarm* the user set, from a `BOOT_COMPLETED` receiver for certain types, when the app is granted certain permissions (like a device admin), etc. The common thread: there's a legitimate, user-relevant trigger.","**Android 13+/14 further tightening**: added per-type requirements and, in 14, requires that the declared foreground service type genuinely matches the work, with runtime enforcement — using `location` type without actually needing location, for example, is rejected."]},{t:"list",items:["**How to work within it**: for time-sensitive server-driven work, use a *high-priority FCM message* (which is an allowed trigger and can start a foreground service or expedited work). For deferrable work, use WorkManager (which can escalate to a foreground *worker* under its own rules). For truly user-initiated ongoing tasks, start the foreground service *from the user's action* while the app is foregrounded. The general strategy is to tie foreground-service starts to a legitimate trigger rather than trying to run one from arbitrary background code."]},{t:"p",text:"**The senior framing**: the background-start restriction reflects Android's consistent philosophy — persistent, resource-consuming work should be *tied to user awareness or a legitimate, user-relevant trigger*, not started silently whenever the app happens to wake up. The exceptions are precisely the 'user-relevant' cases (user action, high-priority push, user-set alarm). So the right mental model isn't 'how do I bypass this' but 'what legitimate trigger justifies this foreground work' — usually a user action or an FCM push. Understanding the *why* (preventing silent background resource use) and the sanctioned triggers (rather than fighting the restriction) is what demonstrates you're building within Android's modern background-execution model, which is exactly what these questions probe."}]},{level:"senior",q:"How do bound services and IPC (AIDL) work, and when would you need them?",a:[{t:"p",text:"**A bound service exposes an interface that other components `bindService()` to and call methods on — a client-server relationship where the service lives while bound. When the client and service are in *different processes*, this cross-process method calling is IPC (Inter-Process Communication), implemented with AIDL (Android Interface Definition Language) on top of Android's Binder mechanism.** You need them for genuinely multi-process scenarios, which most apps don't have — but they come up in depth questions and specific architectures."},{t:"list",items:["**Bound service basics**: the client calls `bindService()`, gets back an `IBinder` (via `onServiceConnected`), and casts it to an interface to call the service's methods. The service runs as long as at least one client is bound (`unbindService` releases it). Within a single process, the binder is just a direct object reference — simple method calls.","**Cross-process → AIDL**: when the service is in a *different process* (another app, or your own app's separate process via `android:process`), you can't share object references — the call must be *marshalled* (serialized) across the process boundary. AIDL is a small interface definition language; you write a `.aidl` file describing the methods, and the build generates the stub/proxy code that marshals arguments and return values across processes. AIDL supports concurrent calls (multi-threaded) — the reason to use it over the simpler `Messenger`.","**Binder is the underlying transport**: all Android IPC (AIDL, Messenger, ContentProviders, even Intents to a degree) ultimately goes through the kernel Binder driver, which has the ~1MB per-transaction limit (the source of `TransactionTooLargeException`). AIDL/Messenger/ContentProvider are higher-level APIs over Binder.","**Simpler alternative — `Messenger`**: if you don't need concurrent cross-process calls, `Messenger` provides message-based IPC (serialized through a single Handler, so calls are queued) — much simpler than AIDL. Use AIDL only when you need a full concurrent interface across processes."]},{t:"list",items:["**When you'd actually need this**: exposing a service to *other apps* (a shared platform service), an app deliberately split into *multiple processes* (isolating a memory-heavy or crash-prone component that must still communicate with the main process), or communicating with certain *system services*. For the vast majority of apps — single process, no cross-app service — you never write AIDL; you use normal method calls, coroutines, and shared repositories."]},{t:"p",text:"**The senior framing**: bound services and AIDL solve *cross-boundary communication* — one process calling methods in another — which Android handles by marshalling calls over Binder. AIDL generates the marshalling code for a concurrent interface; Messenger is the simpler serialized alternative; both sit on Binder (with its transaction-size limit). The honest note is that these are *specialized* tools for multi-process or cross-app scenarios that most apps don't have, so the mature answer is knowing what they're *for* and that Binder underlies all IPC, while recognizing that reaching for AIDL in a normal single-process app would be a serious over-complication — you'd use a shared repository and coroutines instead."}]},{level:"junior",q:"How do you schedule an alarm with AlarmManager?",a:[{t:"p",text:"Get the `AlarmManager` system service and call `setExactAndAllowWhileIdle`/`setAndAllowWhileIdle`/`setWindow` with a trigger time and a `PendingIntent` (usually to a `BroadcastReceiver`). The alarm fires the PendingIntent at the scheduled time, even from the background — for exact/time-critical scheduling. Choose the method by how precise/battery-friendly you need it."},{t:"code",title:"Scheduling an alarm",code:`val pi = PendingIntent.getBroadcast(ctx, 0, Intent(ctx, AlarmReceiver::class.java),
    PendingIntent.FLAG_IMMUTABLE)
alarmManager.setExactAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, triggerAtMillis, pi)`},{t:"list",items:["**`AlarmManager` service** — `getSystemService`.","**`set*`** — `setExactAndAllowWhileIdle`/`setWindow`/`setAndAllowWhileIdle`.","**`PendingIntent`** — usually to a `BroadcastReceiver`.","**RTC vs ELAPSED_REALTIME** — wall-clock vs boot-relative time."]},{t:"note",text:"Schedule with AlarmManager.set*/setExactAndAllowWhileIdle(type, triggerMillis, pendingIntent) — the PendingIntent (usually to a BroadcastReceiver) fires at the time, even from the background. RTC_WAKEUP = wall-clock + wake device; ELAPSED_REALTIME = boot-relative. Choose exact vs inexact by precision/battery needs."}]},{level:"senior",q:"What is the difference between exact and inexact alarms?",a:[{t:"p",text:"*Exact* alarms (`setExact`/`setExactAndAllowWhileIdle`) fire at a precise time — for alarms/clocks/calendar reminders. *Inexact* alarms (`set`/`setWindow`/`setInexactRepeating`) fire *around* a time, letting the system batch them for battery efficiency. Exact alarms are *restricted* (need permission on modern Android) because they wake the device precisely, hurting battery — use inexact unless the exact time genuinely matters to the user."},{t:"list",items:["**Exact** — precise time; for user-critical timing (alarm clock).","**Inexact** — approximate; batched for battery.","**Exact restricted** — needs permission (`SCHEDULE_EXACT_ALARM`).","**Prefer inexact** — unless exact time matters to the user."]},{t:"note",text:"Exact alarms (setExact*) fire at a precise time (alarm clock, reminders) but are restricted (need SCHEDULE_EXACT_ALARM on modern Android — battery). Inexact alarms (set/setWindow/setInexactRepeating) fire around a time, batched for battery. Prefer inexact unless the exact time genuinely matters to the user."}]},{level:"senior",q:"What permission do exact alarms require on modern Android?",a:[{t:"p",text:"On Android 12+ (API 31), exact alarms require the `SCHEDULE_EXACT_ALARM` permission (or `USE_EXACT_ALARM` on API 33+ for calendar/alarm-clock apps). `SCHEDULE_EXACT_ALARM` is a *special* permission the user grants in Settings (and it can be revoked); `USE_EXACT_ALARM` is granted at install but only for apps whose *core function* is alarms/reminders. Check `canScheduleExactAlarms()` before scheduling and fall back gracefully."},{t:"list",items:["**`SCHEDULE_EXACT_ALARM` (API 31+)** — special permission, user-granted in Settings.","**`USE_EXACT_ALARM` (API 33+)** — install-granted, only for alarm/calendar apps.","**`canScheduleExactAlarms()`** — check before scheduling.","**Fall back** — to inexact if not permitted."]},{t:"note",text:"Exact alarms need SCHEDULE_EXACT_ALARM (API 31+, a special Settings-granted permission that can be revoked) or USE_EXACT_ALARM (API 33+, install-granted only for apps whose core function is alarms/reminders). Check canScheduleExactAlarms() before scheduling and fall back to inexact gracefully."}]},{level:"junior",q:"How do foreground services show a notification, and what types are there?",a:[{t:"p",text:"A foreground service *must* show a persistent notification (via `startForeground(id, notification)`) so the user knows it's running. Since Android 10+ you declare a `foregroundServiceType` in the manifest (`location`, `mediaPlayback`, `dataSync`, `camera`, `microphone`, `phoneCall`, etc.), and API 34+ enforces the type and its required permissions. The type communicates *why* the service runs in the foreground."},{t:"list",items:["**Mandatory notification** — `startForeground(id, notification)`.","**`foregroundServiceType`** — location/mediaPlayback/dataSync/camera/etc.","**API 34+** — type + permissions enforced.","**Communicates purpose** — why it's in the foreground."]},{t:"note",text:"A foreground service must show a persistent notification (startForeground(id, notification)). Declare a foregroundServiceType (location/mediaPlayback/dataSync/camera/mic/phoneCall) in the manifest (Android 10+); API 34+ enforces the type and its required permissions. The type communicates why the service runs in the foreground."}]},{level:"senior",q:"How do you create and manage a notification channel?",a:[{t:"p",text:"Since Android 8 (API 26), every notification must belong to a `NotificationChannel` (created once, e.g. at app start) with an importance level; users control each channel's behavior (sound, vibration) in Settings. You build a channel with `NotificationManager.createNotificationChannel`, set the channel id on the notification, and the importance determines how intrusive it is (high = heads-up)."},{t:"code",title:"Notification channel",code:`val channel = NotificationChannel("uploads", "Uploads", NotificationManager.IMPORTANCE_LOW)
notificationManager.createNotificationChannel(channel)
val notif = NotificationCompat.Builder(ctx, "uploads")...build()`},{t:"list",items:["**`NotificationChannel` (API 26+)** — required; created once.","**Importance** — controls intrusiveness (high = heads-up).","**User control** — per-channel settings (sound/vibration).","**Channel id** — set on the notification."]},{t:"note",text:"Since API 26, notifications need a NotificationChannel (created once, e.g. at startup) with an importance level (high = heads-up); users control each channel's behavior in Settings. Create via createNotificationChannel, set the channel id on the notification. Foreground services and reminders both need a channel."}]},{level:"senior",q:"What is goAsync() in a BroadcastReceiver, and when do you use it?",a:[{t:"p",text:"`onReceive` normally must finish quickly (the receiver is considered done and can be killed when it returns). `goAsync()` returns a `PendingResult` that keeps the receiver alive for a *short* additional window (~10s) so you can do brief async work on a background thread, calling `finish()` when done. Use it for a quick task that doesn't warrant a full WorkManager job — but for anything substantial, enqueue work instead."},{t:"code",title:"goAsync",code:`override fun onReceive(ctx: Context, intent: Intent) {
    val result = goAsync()
    scope.launch { doQuickWork(); result.finish() }   // brief background work
}`},{t:"list",items:["**`goAsync()`** — keeps the receiver alive ~10s for async work.","**`PendingResult.finish()`** — call when done.","**Brief tasks only** — not a substitute for WorkManager.","**Substantial work** — enqueue WorkManager instead."]},{t:"note",text:"onReceive must finish fast; goAsync() returns a PendingResult keeping the receiver alive ~10s for brief background work (call finish() when done). Use it for quick tasks not warranting a WorkManager job — for anything substantial, enqueue work instead."}]},{level:"junior",q:"What is the difference between RTC and ELAPSED_REALTIME alarm types?",a:[{t:"p",text:"`RTC`/`RTC_WAKEUP` schedule based on *wall-clock time* (`System.currentTimeMillis()`) — for alarms at a specific real-world time (8:00 AM), but affected by clock/timezone changes. `ELAPSED_REALTIME`/`ELAPSED_REALTIME_WAKEUP` schedule based on time *since boot* (`SystemClock.elapsedRealtime()`) — for relative delays ('30 min from now'), unaffected by clock changes. The `_WAKEUP` variants wake the device; without it, the alarm waits until the device is awake."},{t:"list",items:["**`RTC`** — wall-clock time; specific real-world times.","**`ELAPSED_REALTIME`** — since boot; relative delays.","**`_WAKEUP`** — wakes the device; else waits for wake.","**Clock changes** — affect RTC, not ELAPSED_REALTIME."]},{t:"note",text:"RTC/RTC_WAKEUP: wall-clock time (System.currentTimeMillis) for specific real-world times (affected by clock/timezone). ELAPSED_REALTIME/_WAKEUP: since boot (SystemClock.elapsedRealtime) for relative delays (unaffected by clock). _WAKEUP wakes the device; without it, the alarm waits until awake."}]},{level:"senior",q:"How do you build a reminder/notification-at-a-time feature reliably?",a:[{t:"p",text:"Use `AlarmManager.setExactAndAllowWhileIdle` (with `SCHEDULE_EXACT_ALARM`/`USE_EXACT_ALARM` and permission checks) targeting a `BroadcastReceiver` that posts the notification. Persist the reminders (DB) and *re-register* all alarms on `BOOT_COMPLETED` (alarms are lost on reboot). For repeating reminders, re-schedule the next one after each fires. Handle permission revocation gracefully (fall back / prompt)."},{t:"list",items:["**`setExactAndAllowWhileIdle`** — precise, fires in Doze; needs the permission.","**Receiver posts notification** — the reminder.","**Persist + re-register on BOOT_COMPLETED** — alarms lost on reboot.","**Repeating** — reschedule the next after each fires."]},{t:"note",text:"Reminders: AlarmManager.setExactAndAllowWhileIdle (with SCHEDULE_EXACT_ALARM/USE_EXACT_ALARM + permission checks) → a BroadcastReceiver that posts the notification. Persist reminders and re-register alarms on BOOT_COMPLETED (lost on reboot); reschedule the next after each repeat fires. Handle permission revocation gracefully."}]},{level:"senior",q:"What is setAlarmClock, and how does it differ from other alarm methods?",a:[{t:"p",text:"`setAlarmClock` schedules a *user-visible alarm* (like a physical alarm clock) — it's treated as high-priority, fires even in Doze, and shows in the system's next-alarm UI. It's meant for *actual alarm-clock functionality*, so it bypasses most restrictions. Regular `setExact`/`setWindow` are for app-internal scheduling and are more constrained. Use `setAlarmClock` only for genuine user alarms."},{t:"list",items:["**`setAlarmClock`** — user-visible, high-priority alarm.","**Fires in Doze** — treated like a real alarm clock.","**Next-alarm UI** — shown in the system.","**Genuine alarms only** — not for general scheduling."]},{t:"note",text:"setAlarmClock schedules a user-visible, high-priority alarm (like a physical alarm clock) — fires even in Doze, shows in the system next-alarm UI, and bypasses most restrictions. Use it only for genuine user alarm-clock functionality, not general app scheduling (use setExact/WorkManager for that)."}]},{level:"junior",q:"How do you stop a foreground service?",a:[{t:"p",text:"Call `stopSelf()` (from within) or `stopService()` (from outside) to stop the service, and `stopForeground(STOP_FOREGROUND_REMOVE)` to remove the notification. For a foreground service doing finite work (an upload), stop it when the work completes. Leaving a foreground service running needlessly drains battery and annoys users with a persistent notification."},{t:"list",items:["**`stopSelf()`/`stopService()`** — stop the service.","**`stopForeground(STOP_FOREGROUND_REMOVE)`** — remove the notification.","**On work complete** — stop finite-work services.","**Don't linger** — needless foreground drains battery."]},{t:"note",text:"Stop a foreground service with stopSelf() (inside) or stopService() (outside), and stopForeground(STOP_FOREGROUND_REMOVE) to remove the notification. Stop it when finite work completes — a lingering foreground service drains battery and shows a persistent notification. WorkManager foreground workers stop automatically."}]},{level:"senior",q:"What are the foreground service type restrictions on Android 14?",a:[{t:"p",text:"Android 14 (API 34) requires every foreground service to *declare its type* (`foregroundServiceType`) in the manifest AND hold the corresponding *permission* (e.g. `dataSync` type, `location` type + location permission). The system rejects starting a foreground service without a valid type/permission. Types like `dataSync` and `mediaProcessing` also have *time limits*. This tightens foreground-service use to legitimate, declared purposes."},{t:"list",items:["**Type + permission required** — API 34 enforces both.","**Rejected without them** — can't start the service.","**Time limits** — some types (dataSync) are time-bound.","**Legitimate use** — types must match the actual purpose."]},{t:"note",text:"Android 14 (API 34) requires each foreground service to declare its foregroundServiceType AND hold the matching permission (e.g. location type + location permission); starting without a valid type/permission is rejected. Some types (dataSync/mediaProcessing) have time limits. Tightens foreground-service use to declared, legitimate purposes."}]},{level:"senior",q:"How do you handle repeating alarms efficiently?",a:[{t:"p",text:"For *inexact* repeating, `setInexactRepeating` lets the system batch alarms (battery-friendly). For *exact* repeating (rare, user-facing), *don't* use exact repeating APIs (they're removed/discouraged) — instead re-schedule the next single exact alarm after each fires. For most *periodic background work*, use `WorkManager` periodic work instead of alarms (it's designed for battery-efficient recurring tasks)."},{t:"list",items:["**`setInexactRepeating`** — batched, battery-friendly.","**Exact repeating** — reschedule the next after each fires.","**Periodic work** — use WorkManager for recurring background tasks.","**Avoid tight exact repeats** — battery drain."]},{t:"note",text:"Repeating alarms: setInexactRepeating for batched, battery-friendly recurrence; for exact repeating, reschedule the next single exact alarm after each fires (exact repeating APIs are discouraged). For periodic background work, prefer WorkManager periodic work — designed for battery-efficient recurring tasks."}]},{level:"junior",q:"What is a PendingIntent, and why do alarms and notifications use it?",a:[{t:"p",text:"A `PendingIntent` wraps an `Intent` so *another process* (the system's AlarmManager or NotificationManager) can execute it *as your app* later. Alarms and notifications fire outside your app's immediate execution, so they need a PendingIntent to launch your Activity/Service/Receiver when triggered. Since API 31 you must specify mutability (`FLAG_IMMUTABLE` by default for security)."},{t:"list",items:["**Wraps an Intent** — executed by another process as your app.","**Deferred execution** — alarms/notifications fire it later.","**Types** — `getActivity`/`getService`/`getBroadcast`.","**`FLAG_IMMUTABLE`** — required mutability (API 31+)."]},{t:"note",text:"A PendingIntent wraps an Intent so another process (AlarmManager/NotificationManager) can execute it as your app later — needed because alarms/notifications fire outside your app's execution. Types: getActivity/getService/getBroadcast. Since API 31, specify mutability (FLAG_IMMUTABLE default for security)."}]},{level:"senior",q:"How do you keep a foreground service alive for a phone-call-like feature?",a:[{t:"p",text:"For a must-stay-running feature (VoIP call, active navigation, live tracking), use a foreground service with the appropriate `foregroundServiceType` (`phoneCall`, `location`) and its permissions — it runs with high priority and a persistent notification, so the system won't kill it while active. Handle reconnection and lifecycle carefully, and stop it promptly when the feature ends. This is the legitimate use of long-lived foreground services."},{t:"list",items:["**Foreground service + type** — `phoneCall`/`location`; high priority.","**Persistent notification** — user-visible; not killed while active.","**Reconnection/lifecycle** — handle carefully.","**Stop promptly** — when the feature ends."]},{t:"note",text:"For a must-stay-running feature (VoIP call, active navigation), use a foreground service with the right foregroundServiceType (phoneCall/location) + permissions — high priority, persistent notification, not killed while active. Handle reconnection/lifecycle and stop promptly when done. The legitimate use of long-lived foreground services."}]},{level:"senior",q:"How do you test alarms and foreground services?",a:[{t:"p",text:"For alarms, abstract `AlarmManager` behind an interface you can fake in unit tests (assert the scheduling call), or use `adb shell` to trigger/inspect. For the receiver logic, test it directly. For foreground services, test the *work logic* (extracted into testable classes) separately, and verify notification/lifecycle behavior with instrumented tests. Abstracting the framework APIs is key to testing this."},{t:"list",items:["**Abstract `AlarmManager`** — fake it; assert scheduling.","**Test receiver logic** — directly.","**Extract work logic** — test it separately from the service.","**Instrumented** — notification/lifecycle behavior."]},{t:"note",text:"Test alarms by abstracting AlarmManager behind an interface (fake it, assert the scheduling call) and testing receiver logic directly. For foreground services, extract the work logic into testable classes and test it separately; verify notification/lifecycle with instrumented tests. Abstracting framework APIs is key."}]},{level:"junior",q:"How do you promote a started service to a foreground service?",a:[{t:"p",text:"Start the service (`startForegroundService` from the background, or `startService` from the foreground), then within ~5 seconds call `startForeground(id, notification)` to promote it — otherwise the system throws (`ANR`/crash). On modern Android you must also declare the `foregroundServiceType` and hold its permission. Promote when the work becomes user-visible/long-running; you can later `stopForeground` to demote."},{t:"list",items:["**`startForegroundService`** — then `startForeground` within ~5s.","**Miss the window** — the system crashes it.","**Type + permission** — declared for modern Android.","**`stopForeground`** — to demote back."]},{t:"note",text:"Start the service (startForegroundService from background / startService from foreground), then call startForeground(id, notification) within ~5s to promote it (or the system crashes it). Declare foregroundServiceType + permission on modern Android. Promote when work becomes user-visible/long; stopForeground to demote."}]},{level:"senior",q:"Why do exact alarms and foreground services face increasing restrictions?",a:[{t:"p",text:"Both can *drain battery* and be *abused* — exact alarms wake the device precisely (defeating Doze batching), and foreground services run with high priority indefinitely. Android restricts them (permissions, types, background-start limits) to protect battery life and prevent apps from running unnecessarily. The guidance: use the *most deferrable* mechanism that meets your need (WorkManager over foreground service over exact alarm) and only escalate when genuinely required."},{t:"list",items:["**Battery + abuse** — exact alarms defeat Doze; foreground services run indefinitely.","**Restrictions** — permissions, types, background-start limits.","**Use most-deferrable** — WorkManager > foreground service > exact alarm.","**Escalate only when needed** — for genuine urgency/precision."]},{t:"note",text:"Exact alarms (wake the device precisely, defeat Doze) and foreground services (high-priority, indefinite) drain battery and are abusable — so Android restricts them (permissions, types, background-start limits). Use the most-deferrable mechanism that meets your need (WorkManager > foreground service > exact alarm); escalate only for genuine urgency/precision."}]},{level:"junior",q:"How do you cancel a scheduled alarm?",a:[{t:"p",text:"Call `alarmManager.cancel(pendingIntent)` with a `PendingIntent` *equivalent* to the one used to schedule it (same requestCode, action, and component — Android matches PendingIntents by these). So keep a consistent PendingIntent (or recreate an equal one) to cancel. For repeating reminders, cancel when the user removes them."},{t:"code",title:"Cancel an alarm",code:`val pi = PendingIntent.getBroadcast(ctx, requestCode, intent, PendingIntent.FLAG_IMMUTABLE)
alarmManager.cancel(pi)   // matches by requestCode/action/component`},{t:"list",items:["**`alarmManager.cancel(pendingIntent)`** — cancel by equivalent PendingIntent.","**Matching** — same requestCode/action/component.","**Consistent PendingIntent** — keep or recreate an equal one.","**Uses** — user removes a reminder; reschedule changes."]},{t:"note",text:"Cancel with alarmManager.cancel(pendingIntent) using a PendingIntent equivalent to the scheduling one (Android matches by requestCode/action/component) — so keep or recreate an equal PendingIntent. Cancel repeating reminders when the user removes them or reschedules."}]},{level:"senior",q:"How does the POST_NOTIFICATIONS permission affect foreground services and reminders?",a:[{t:"p",text:"On Android 13+ (API 33), posting notifications requires the `POST_NOTIFICATIONS` runtime permission. If it's *denied*, your foreground service still runs but its notification *isn't shown* (a foreground service without a visible notification), and reminder notifications are silently dropped. Request the permission in context, and design for the case where the user denies it (the service works but the user won't see the notification)."},{t:"list",items:["**API 33+ runtime permission** — for showing notifications.","**Denied** — foreground service runs but notification hidden; reminders dropped.","**Request in context** — when enabling a notifying feature.","**Design for denial** — the service works without a visible notification."]},{t:"note",text:"Android 13+ requires POST_NOTIFICATIONS (runtime) to show notifications. If denied, a foreground service still runs but its notification isn't shown, and reminders are silently dropped. Request in context; design for denial (the service functions without a visible notification)."}]},{level:"junior",q:"How do you add actions like reply or dismiss to a notification?",a:[{t:"p",text:"Add `NotificationCompat.Action`s with a label and a `PendingIntent` (to a `BroadcastReceiver`/Activity that handles the action). For inline reply, use `RemoteInput` attached to an action so the user types directly in the notification. The receiver reads the input and updates state (e.g. sends the reply). Actions make notifications interactive without opening the app."},{t:"code",title:"Notification action",code:`val replyAction = NotificationCompat.Action.Builder(icon, "Reply", replyPendingIntent)
    .addRemoteInput(RemoteInput.Builder("key_reply").build()).build()
builder.addAction(replyAction)`},{t:"list",items:["**`NotificationCompat.Action`** — label + `PendingIntent`.","**`RemoteInput`** — inline text reply.","**Receiver handles it** — reads input, updates state.","**Interactive** — act without opening the app."]},{t:"note",text:"Add NotificationCompat.Actions (label + PendingIntent to a receiver/Activity); for inline reply attach a RemoteInput so the user types in the notification. The receiver reads the input and acts (send reply). Actions make notifications interactive without opening the app."}]},{level:"senior",q:"What is the difference between AlarmManager and JobScheduler?",a:[{t:"p",text:"`AlarmManager` triggers at a *specific time* (wall-clock or elapsed) — for time-based scheduling (alarms, reminders). `JobScheduler` runs deferrable jobs when *conditions* are met (network, charging, idle) — for background work, batched for battery. They serve different needs: exact timing (AlarmManager) vs conditional deferrable work (JobScheduler, which WorkManager wraps). For most background work, use WorkManager (over JobScheduler); for exact time, AlarmManager."},{t:"list",items:["**AlarmManager** — time-based triggers (exact/inexact).","**JobScheduler** — condition-based deferrable jobs (network/charging/idle).","**WorkManager wraps JobScheduler** — the recommended abstraction.","**Choose** — exact time (Alarm) vs conditional work (Job/WorkManager)."]},{t:"note",text:"AlarmManager triggers at a specific time (alarms/reminders); JobScheduler runs deferrable jobs when conditions are met (network/charging/idle — battery-batched). WorkManager wraps JobScheduler as the recommended background-work abstraction. Use AlarmManager for exact time, WorkManager for conditional deferrable work."}]},{level:"senior",q:"How do you show ongoing progress in a foreground service notification?",a:[{t:"p",text:"Use `NotificationCompat.Builder.setProgress(max, current, indeterminate)` and re-post the notification with the same id as work advances (`notificationManager.notify(id, updated)`). For a download, update the progress bar periodically (throttle updates to avoid spamming). Set `setOngoing(true)` so it can't be swiped away while active, and remove it (`stopForeground`) when done."},{t:"code",title:"Progress notification",code:`builder.setProgress(100, progress, false).setOngoing(true)
notificationManager.notify(1, builder.build())   // update as progress changes`},{t:"list",items:["**`setProgress(max, current, indeterminate)`** — progress bar.","**Re-post same id** — to update.","**Throttle updates** — avoid spamming.","**`setOngoing(true)`** — non-dismissable while active."]},{t:"note",text:"Show progress with NotificationCompat setProgress(max, current, indeterminate) and re-post the notification with the same id as work advances (throttle updates). setOngoing(true) makes it non-dismissable while active; remove it (stopForeground) when done. Standard for download/upload foreground services."}]},{level:"junior",q:"What are notification importance levels, and what is a heads-up notification?",a:[{t:"p",text:"A channel's *importance* (HIGH, DEFAULT, LOW, MIN) controls how intrusive its notifications are: HIGH makes sound *and* shows a *heads-up* (a peek that appears over the current screen) — for time-sensitive alerts (messages, calls); DEFAULT makes sound but no peek; LOW/MIN are quiet (ongoing/foreground service notifications). Since API 26, importance is set on the *channel* (not the notification), and users can override it."},{t:"list",items:["**HIGH** — sound + heads-up peek; time-sensitive.","**DEFAULT** — sound, no peek.","**LOW/MIN** — quiet; ongoing/foreground.","**On the channel** — API 26+; user-overridable."]},{t:"note",text:"Channel importance (HIGH/DEFAULT/LOW/MIN) sets intrusiveness: HIGH = sound + heads-up peek (time-sensitive alerts), DEFAULT = sound no peek, LOW/MIN = quiet (foreground/ongoing). Set on the channel (API 26+), user-overridable. Use LOW for a foreground service notification so it's not intrusive."}]},{level:"senior",q:"How do exact alarms behave in Doze mode?",a:[{t:"p",text:"In *Doze* (deep idle), most alarms are *deferred* to maintenance windows to save battery. To fire during Doze, you need `setExactAndAllowWhileIdle` or `setAndAllowWhileIdle` (the `AllowWhileIdle` variants) — but even these are *rate-limited* in Doze (roughly once every ~9 minutes per app) to prevent abuse. `setAlarmClock` (genuine user alarms) is exempt. So don't rely on frequent exact alarms during Doze."},{t:"list",items:["**Doze defers** — alarms to maintenance windows.","**`AllowWhileIdle` variants** — fire during Doze.","**Rate-limited** — ~once per 9 min per app in Doze.","**`setAlarmClock`** — exempt (genuine user alarms)."]},{t:"note",text:"In Doze, most alarms are deferred to maintenance windows (battery). To fire during Doze use setExactAndAllowWhileIdle/setAndAllowWhileIdle — but even these are rate-limited (~once per 9 min per app). setAlarmClock (genuine user alarms) is exempt. Don't rely on frequent exact alarms during Doze."}]}],bC={id:"alarms-foreground",title:"AlarmManager & Foreground Services",tagline:"Exact-time execution with AlarmManager, exact-alarm restrictions, foreground services & types, background-start limits, and bound services/IPC.",tags:["AlarmManager","Foreground Service","Exact Alarms","IPC","AIDL"],content:wC,qa:vC},kC=[{heading:"Doze mode — the OS batching background work",blocks:[{t:"p",text:"**Doze mode** (introduced in Android 6, strengthened since) is a power-saving state the system enters when the device is *unused* (screen off, stationary, unplugged for a while). In Doze, the OS *defers* apps' background activity — network access, jobs, alarms, wakelocks — and batches them into periodic **maintenance windows**, so the device can stay in a deep low-power state most of the time instead of constantly waking for background work. This dramatically improves standby battery life, and it's a major reason background work is 'deferrable, not immediate'."},{t:"list",items:["**What Doze restricts**: network access is suspended, `AlarmManager` alarms are deferred (except `setExactAndAllowWhileIdle`, which is rate-limited), `JobScheduler`/WorkManager jobs are deferred, wakelocks are ignored, and syncs are paused — all batched to maintenance windows that grow further apart the longer the device is idle.","**Maintenance windows**: periodically, Doze briefly lifts to let deferred work run, then the device returns to Doze. The intervals lengthen over time (a device idle overnight has very infrequent windows).","**Deep Doze vs light Doze**: light Doze (screen off but device recently used) applies milder restrictions sooner; deep Doze (long idle, stationary) applies the full set.","**High-priority FCM is the escape**: a *high-priority* Firebase Cloud Messaging message can wake the app from Doze immediately (for genuinely time-sensitive things like a message or call) — which is why push is the right pattern for real-time delivery instead of polling."]}]},{heading:"App Standby and standby buckets",blocks:[{t:"list",items:["**App Standby** — the system limits background activity of apps the user *hasn't used recently*, independent of Doze. An app you haven't opened in weeks gets less background freedom than one you use daily.","**App Standby Buckets** (Android 9+) — the OS classifies each app into a bucket based on usage: **Active** (in use now), **Working set** (used regularly), **Frequent**, **Rare**, and **Restricted** (rarely used or misbehaving). The bucket determines how much background work, how many jobs/alarms, and how much network the app gets — more usage = more freedom.","**The implication**: you can't assume your background work runs promptly if the user rarely opens your app — it'll be throttled. Design for eventual, not immediate, execution, and rely on push (FCM) for anything the user must be notified about promptly regardless of bucket."]}]},{heading:"Battery optimizations and the trend",blocks:[{t:"list",items:["**Battery optimization exemptions** — users (or the app, with a request) can exempt an app from some restrictions via 'ignore battery optimizations'. But requesting this is discouraged and Play has policies against unjustified use — Google's stance is that a well-designed app shouldn't need it. Only genuine always-on use cases (a companion app for a medical device) justify it.","**The overarching trend**: every Android version since 6 has *tightened* background execution — Doze, App Standby, background service limits (8), background location limits (10), foreground service and exact alarm restrictions (12+), foreground service types (14). The direction is unmistakable: unrestricted background work is a battery/privacy problem, so the OS keeps constraining it.","**What this means for developers**: stop thinking 'run in the background whenever I want' and start thinking 'declare *what* work I need and *under what conditions*, and let the OS schedule it efficiently' (WorkManager), or 'let the server tell me when there's something to do' (push/FCM). Fighting the battery model (wakelocks, exact alarms for polling, exemptions) is fragile and user-hostile; working *with* it is the correct approach."]}]},{heading:"Choosing the right tool — the decision table",blocks:[{t:"p",text:"This is the single most important background-work interview question: given a task, which mechanism? The decision comes down to four axes — *deferrable vs immediate*, *guaranteed vs best-effort*, *exact-timing vs flexible*, and *user-visible vs invisible*."},{t:"table",headers:["The task","Use"],rows:[["Load data for the current screen (immediate, UI-tied)","Coroutine in viewModelScope"],["Deferrable, guaranteed work (sync, upload, backup) surviving kill/reboot","WorkManager"],["Important, guaranteed, should run soon (user tapped 'send')","WorkManager expedited work"],["Guaranteed, long-running, user-visible (large download)","WorkManager foreground worker (setForeground)"],["Ongoing, continuous, user-visible (music, navigation, live tracking)","Foreground service"],["Exact-time trigger (alarm, precise reminder)","AlarmManager (exact alarm)"],["React to server data promptly / real-time (chat, notifications)","Push (FCM high-priority) — not polling"],["In-app client-server / cross-process communication","Bound service (AIDL for IPC)"]]},{t:"list",items:["**The mental flowchart**: Is it tied to a screen and immediate? → coroutine. Must it be *guaranteed* and can it be *deferred*? → WorkManager (expedited if urgent, foreground worker if long/visible). Must it run *continuously now* and is user-visible? → foreground service. Does it need an *exact time*? → AlarmManager. Does the *server* know when there's work? → push (FCM). This flowchart answers virtually every 'which background tool' question.","**The most common mistake**: reaching for WorkManager for immediate UI work (adds overhead/latency), or trying to use a background service / polling for something that should be push or WorkManager. Match the tool to the four axes."]},{t:"note",text:"Doze/battery essentials: Doze batches background work into maintenance windows when the device is idle (deferring network/jobs/alarms) to save battery; App Standby buckets throttle rarely-used apps; the whole trend since Android 6 is tightening background execution. Design for deferrable, not immediate, and use push (FCM high-priority) for real-time — it escapes Doze. Choosing the tool: coroutine (immediate/UI), WorkManager (deferrable/guaranteed, expedited/foreground variants), foreground service (ongoing/visible), AlarmManager (exact time), FCM (server-driven/real-time)."}]}],xC=[{level:"junior",q:"What is Doze mode?",a:[{t:"p",text:"**Doze mode is a power-saving state Android enters when the device is unused — screen off, stationary, and unplugged for a while. In Doze, the OS defers apps' background activity (network access, jobs, alarms, wakelocks) and batches it into periodic *maintenance windows*, so the device can stay in a deep low-power state most of the time instead of constantly waking for background work.** This greatly improves standby battery life."},{t:"list",items:["**What's deferred in Doze**: network access is suspended, WorkManager/JobScheduler jobs and standard alarms are delayed, wakelocks are ignored, and syncs pause — all held until a maintenance window.","**Maintenance windows**: periodically Doze briefly lifts to let deferred work run, then the device goes back to sleep. These windows get *further apart* the longer the device stays idle, so overnight your background work runs very infrequently.","**The escape hatch**: a *high-priority FCM (push) message* can wake the app from Doze immediately for genuinely time-sensitive things (an incoming message or call)."]},{t:"p",text:"Doze is a big reason background work is described as 'deferrable, not immediate' — you can't assume your background task runs promptly when the device is idle. The practical takeaway: design background work to run *eventually* under the OS's batching (WorkManager handles this correctly), and use push notifications for anything that must reach the user in real time, since push is what can wake the device from Doze."}]},{level:"junior",q:"What are App Standby Buckets?",a:[{t:"p",text:"**App Standby Buckets are a system classification (Android 9+) that limits an app's background activity based on how recently and frequently the user uses it. The less you use an app, the less background freedom it gets.** It's a complement to Doze — where Doze restricts *all* apps when the device is idle, App Standby restricts *individual* apps based on their usage, regardless of device state."},{t:"list",items:["**The buckets** (most to least freedom): **Active** (app in use right now), **Working set** (used regularly), **Frequent** (used somewhat often), **Rare** (rarely used), and **Restricted** (barely used or misbehaving — the tightest limits).","**What the bucket affects**: how much background work the app can do, how many jobs and alarms it can run, and how much network access it gets. A 'Rare' app has its background jobs deferred much longer than an 'Active' one.","The system moves apps between buckets automatically based on usage patterns (and OEMs/ML may influence it)."]},{t:"p",text:"The implication for developers: you *cannot* assume your background work runs promptly if the user rarely opens your app — it'll be throttled into a lower bucket. So design for eventual (not immediate) execution, and for anything the user must be notified about regardless of how often they use the app, rely on *push (FCM)* rather than background polling — push delivery isn't subject to the same bucket throttling. This reinforces the general principle: don't depend on frequent, prompt background execution; use the OS-friendly mechanisms (WorkManager for deferrable work, push for real-time)."}]},{level:"junior",q:"Which background tool would you use to sync data with a server?",a:[{t:"p",text:"**WorkManager — because syncing is deferrable, guaranteed work that should run reliably (surviving app kill and reboot) but doesn't need to happen at an exact instant.** It's the textbook WorkManager use case."},{t:"list",items:["You'd typically use a `PeriodicWorkRequest` (for regular background sync, minimum 15-minute interval) or a `OneTimeWorkRequest` (for a sync triggered by an event like pull-to-refresh or a push notification).","Add **constraints** — `NetworkType.CONNECTED` (or `UNMETERED` for large syncs to save cellular data) — so it only runs when there's a network.","Use **unique work** (`enqueueUniqueWork`/`enqueueUniquePeriodicWork` with `KEEP`) so repeated triggers don't create duplicate concurrent syncs.","Return `Result.retry()` (with exponential backoff) on transient failures so a failed sync retries reliably."]},{t:"p",text:"The reasoning: sync must be *guaranteed* (it should eventually complete even if the app is closed), it's *deferrable* (running a few minutes later under the right conditions is fine), and it should be *battery-friendly* (constraints, respecting Doze) — exactly WorkManager's strengths. You would *not* use a plain coroine (it dies with the process, no guarantee), a background service (killed by Android 8+ limits), or AlarmManager (that's for exact timing, not deferrable work). For *real-time* server-driven updates, you'd pair WorkManager with push: an FCM message tells the app there's new data, and the app enqueues a one-time WorkManager sync to fetch it — far more efficient than frequent polling."}]},{level:"junior",q:"You need to notify the user of new server data in real time. Polling with WorkManager or something else?",a:[{t:"p",text:"**Use push notifications (Firebase Cloud Messaging), not polling.** Polling — repeatedly asking the server 'anything new?' on a timer — is the wrong approach for real-time updates: WorkManager's minimum periodic interval is 15 minutes (too slow for real-time), and even that runs on the system's schedule (deferred by Doze/App Standby), so polling is both *laggy* and *battery-wasteful* (most polls return 'nothing new', burning battery and data for no result)."},{t:"p",text:"**The right pattern inverts the model from *poll* to *push*:** instead of the app constantly asking, the *server* sends a push (FCM) message the moment there's new data. This is efficient (the app does nothing until there's genuinely something to deliver), real-time (the message arrives promptly — a *high-priority* FCM message can even wake the app from Doze), and battery-friendly (no wasted polling). When the push arrives, the app typically shows a notification and/or enqueues a WorkManager job to fetch the new data. So the architecture is: server → high-priority FCM push → app reacts (notify + sync). This is *the* answer to 'how do I get real-time updates', and knowing to reject polling in favor of push — and *why* (Doze/App Standby make polling both slow and wasteful, while push escapes Doze) — is exactly what the question tests."}]},{level:"senior",q:"Walk through how you'd choose the right background execution mechanism for a given task.",a:[{t:"p",text:"**I evaluate the task along four axes — immediate vs deferrable, guaranteed vs best-effort, exact-timing vs flexible, and user-visible vs invisible — and that determines the mechanism. Here's the decision flow:**"},{t:"list",items:["**Is it immediate and tied to a screen?** (loading data to display) → a *coroutine in `viewModelScope`/`lifecycleScope`*. No need for background scheduling — it runs now, cancels when the screen is gone. Using WorkManager here would add overhead and latency for nothing.","**Is it deferrable and must be guaranteed** (survive kill/reboot)? → *WorkManager*. This covers sync, uploads, backups, prefetch. Then refine: if it's *important and should run soon* (user tapped send) → WorkManager *expedited* work; if it's *long-running and user-visible* (large download) → WorkManager *foreground worker* (`setForeground`), which keeps the guarantee plus shows a notification.","**Is it ongoing, continuous, and user-visible** (music, navigation, live location tracking)? → a *foreground service* with its mandatory notification. This is for a continuous task the user is aware of, not a discrete unit of work.","**Does it need to fire at an exact time** (alarm, precise reminder)? → *AlarmManager* (exact alarm) — but only if exact timing is genuinely essential, given the restrictions; otherwise WorkManager.","**Is it server-driven / needs to reach the user in real time** (chat message, breaking news)? → *push (FCM high-priority)*, not polling. The server notifies the app; the app reacts (notify + optionally a WorkManager sync).","**Is it cross-process / in-app client-server communication**? → a *bound service* (AIDL for cross-process IPC) — rare, for multi-process apps."]},{t:"list",items:["**The four-axis summary**: *immediate + UI* → coroutine; *deferrable + guaranteed* → WorkManager (with expedited/foreground variants for urgency/visibility); *ongoing + visible* → foreground service; *exact time* → AlarmManager; *server-driven/real-time* → FCM. Nearly every task maps cleanly onto one of these.","**Common mistakes to call out**: using WorkManager for immediate UI work (unnecessary overhead); using a background service or polling for something that should be WorkManager or push (fights the OS restrictions, drains battery); using exact alarms for polling (battery abuse, needs special permission). The modern battery model rewards matching the tool precisely to the need."]},{t:"p",text:"**The senior framing**: this is *the* core background-work competency, because Android's whole background story is about picking the mechanism that fits the task's *actual* requirements rather than defaulting to 'run something in the background'. The OS has progressively removed the ability to run arbitrary background work precisely to force this discipline. So the mature answer isn't a single tool — it's the *decision framework* (the four axes) plus knowing that the OS-friendly path for real-time is push and for reliable deferrable work is WorkManager. Demonstrating the flowchart, and *why* each restriction pushes you toward it, is what separates someone who understands modern Android background execution from someone who reaches for a service or a polling loop."}]},{level:"senior",q:"Why has Android progressively restricted background execution, and how should this shape your app's architecture?",a:[{t:"p",text:"**Android has tightened background execution in nearly every release since Android 6 because unrestricted background work was the primary cause of poor battery life and a significant privacy concern — apps running constantly, waking the device, accessing location and network in the background, draining battery the user attributed to 'Android being bad'. The OS took control of background execution to protect the user's battery and privacy, and the trend is only continuing.**"},{t:"list",items:["**The progression**: Doze + App Standby (6, batch/throttle background work when idle or app unused); background service limits (8, kill background services shortly after the app leaves foreground); background location limits (10, restrict location access when not in use); foreground service + exact alarm restrictions (12, can't start FG services from background freely, exact alarms need permission); foreground service types with enforcement (14). Each step removes a way apps could run unrestricted background work.","**The consistent principle**: background work must be either *deferrable and scheduled by the OS* (so it can be batched efficiently), *tied to genuine user awareness* (a foreground service with a notification), or *triggered by a legitimate, time-sensitive event* (a high-priority push). Silent, arbitrary, continuous background execution is being systematically eliminated."]},{t:"list",items:["**How it shapes architecture**: (1) *Declare what and when, don't imperatively run* — use WorkManager to declare deferrable work with constraints and let the OS schedule it, rather than spinning up services. (2) *Invert polling to push* — for real-time/server-driven needs, use FCM so the server notifies the app, instead of the app polling (which the OS throttles anyway). (3) *Tie continuous work to user awareness* — use foreground services (with notifications) only for genuinely user-visible ongoing tasks. (4) *Persist and recover* — assume your process can be killed anytime (offline-first, save state), and use WorkManager's guarantees for work that must complete. (5) *Don't fight the model* — avoid wakelocks, battery-optimization exemptions, and exact-alarm-for-polling hacks; they're fragile, user-hostile, and increasingly blocked by Play policies.","**The payoff**: an app designed *with* the battery model — deferrable work via WorkManager, real-time via push, continuous work via foreground services, resilient to process death — is more reliable *and* a better citizen, and it won't break with the next Android version's restrictions (which will only tighten further). An app that fights the model breaks repeatedly and drains battery."]},{t:"p",text:"**The senior framing**: the restrictions aren't obstacles to route around — they're the OS enforcing a fundamentally better model where background work is *scheduled, justified, and battery-conscious* rather than *arbitrary and continuous*. The architectural response is to embrace that: declare deferrable work (WorkManager), react to pushes instead of polling (FCM), tie continuous work to user-visible foreground services, and build for process death. Being able to articulate the *why* (battery/privacy at the OS level), the *trajectory* (each version tightens further), and the *architectural implications* (push over poll, declare over imperatively-run, persist over assume-alive) is exactly the systems-level understanding these questions probe — it's the difference between knowing the APIs and understanding the platform's direction."}]},{level:"senior",q:"What happens to network and jobs during Doze?",a:[{t:"p",text:"In Doze, the system *suspends* network access, *defers* jobs/syncs/alarms to periodic *maintenance windows*, ignores wakelocks, and doesn't fire standard alarms until the window. Maintenance windows occur periodically (less often the longer the device is idle). So background work batches up and runs in bursts during these windows, dramatically reducing battery drain — but delaying your app's background work."},{t:"list",items:["**Network suspended** — no access outside maintenance windows.","**Jobs/syncs/alarms deferred** — to maintenance windows.","**Wakelocks ignored** — can't keep the CPU awake.","**Maintenance windows** — periodic, less frequent over time."]},{t:"note",text:"In Doze, the system suspends network access, defers jobs/syncs/alarms to periodic maintenance windows, and ignores wakelocks — background work batches and runs in bursts during windows (which get less frequent the longer idle). Dramatically saves battery but delays background work. FCM high-priority and AllowWhileIdle alarms are exceptions."}]},{level:"senior",q:"How do FCM high-priority messages interact with Doze?",a:[{t:"p",text:"*High-priority* FCM messages can *wake the device from Doze* and give your app a brief window to run (e.g. show a notification, fetch data) — they bypass Doze restrictions for time-sensitive delivery (chat messages, calls). *Normal-priority* messages are deferred until the next maintenance window. Use high priority *only* for genuinely time-sensitive, user-visible messages (abuse leads to throttling); this is the recommended way to deliver real-time updates without polling."},{t:"list",items:["**High-priority FCM** — wakes from Doze; brief run window.","**Normal-priority** — deferred to maintenance windows.","**Use sparingly** — only time-sensitive/user-visible (abuse → throttling).","**Real-time** — the way to push updates without polling."]},{t:"note",text:"High-priority FCM messages wake the device from Doze and give a brief run window (notification/fetch) — for time-sensitive delivery (chat/calls), bypassing Doze. Normal-priority is deferred to maintenance windows. Use high priority only for genuinely time-sensitive user-visible messages (abuse → throttling). The recommended real-time delivery without polling."}]},{level:"junior",q:"What is battery optimization, and what does 'ignore battery optimizations' do?",a:[{t:"p",text:"*Battery optimization* (Doze + App Standby) is applied to apps by default to limit their background activity. The `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` special permission lets an app ask the user to *exempt* it — removing most background restrictions. But Google Play *restricts* this to apps that genuinely need it (alarms, comms, device management); requesting it without justification can get an app rejected. Prefer designing within the restrictions."},{t:"list",items:["**Battery optimization** — Doze + App Standby on by default.","**Ignore/exempt** — special permission removes most restrictions.","**Play-restricted** — only justified use cases (alarms/comms).","**Prefer** — design within the restrictions, not exemption."]},{t:"note",text:"Battery optimization (Doze + App Standby) limits background activity by default. REQUEST_IGNORE_BATTERY_OPTIMIZATIONS lets an app ask to be exempted (removing most restrictions), but Google Play restricts it to genuinely-needing apps (alarms/comms/device management) — misuse risks rejection. Prefer designing within the restrictions."}]},{level:"senior",q:"What is adaptive battery, and how does it affect background work?",a:[{t:"p",text:"*Adaptive Battery* (Android 9+) uses on-device ML to learn which apps the user uses and *prioritizes* battery/resources accordingly — apps the user rarely opens get more aggressively restricted (placed in lower standby buckets, throttled jobs/FCM). It's dynamic and per-user. The takeaway: background reliability isn't guaranteed and varies by user engagement, so design essential features to work with minimal background dependency."},{t:"list",items:["**Adaptive Battery (API 28+)** — ML-driven resource prioritization.","**Rarely-used apps restricted** — lower buckets, throttled.","**Per-user, dynamic** — varies by engagement.","**Design** — essential features with minimal background dependency."]},{t:"note",text:"Adaptive Battery (Android 9+) uses on-device ML to prioritize resources for apps the user actually uses — rarely-opened apps get more aggressively restricted (lower standby buckets, throttled jobs/FCM). It's dynamic/per-user, so background reliability varies by engagement — design essential features to need minimal background work."}]},{level:"senior",q:"How do you choose between WorkManager, foreground service, and AlarmManager?",a:[{t:"p",text:"Decide by *urgency*, *visibility*, and *timing*: deferrable, guaranteed work → *WorkManager* (sync, upload, cleanup); user-visible, immediate, ongoing work → *foreground service* (music, navigation, active tracking); exact-time trigger → *AlarmManager* (alarm clock, reminder). For real-time server-pushed updates → *FCM* (not polling). Prefer the most deferrable/least intrusive mechanism that meets the need."},{t:"table",headers:["Need","Tool"],rows:[["Deferrable guaranteed work","WorkManager"],["Immediate user-visible ongoing","Foreground service"],["Exact time","AlarmManager"],["Real-time server push","FCM"]]},{t:"list",items:["**WorkManager** — deferrable/guaranteed (sync/upload).","**Foreground service** — immediate/visible/ongoing.","**AlarmManager** — exact time.","**FCM** — real-time push (not polling)."]},{t:"note",text:"Choose by urgency/visibility/timing: WorkManager (deferrable guaranteed — sync/upload), foreground service (immediate user-visible ongoing — music/nav), AlarmManager (exact time — alarms/reminders), FCM (real-time server push, not polling). Prefer the most deferrable/least-intrusive mechanism that meets the need."}]},{level:"junior",q:"Why should you use FCM push instead of polling for updates?",a:[{t:"p",text:"*Polling* (repeatedly asking the server 'anything new?') wastes battery and data (frequent wakeups/requests), is delayed (updates arrive only on the next poll), and is throttled by Doze/buckets. *FCM push* lets the *server notify the app* when something changes — instant, battery-efficient (the system's single persistent connection serves all apps), and works through Doze (high-priority). Prefer push for real-time updates; use WorkManager for periodic baseline sync."},{t:"list",items:["**Polling** — wasteful, delayed, throttled.","**FCM push** — server notifies; instant, battery-efficient.","**Single connection** — the system shares one for all apps.","**Through Doze** — high-priority messages wake the device."]},{t:"note",text:"Polling wastes battery/data (frequent wakeups), is delayed, and is throttled by Doze/buckets. FCM push lets the server notify the app instantly and efficiently (the system's single persistent connection serves all apps, works through Doze via high-priority). Prefer push for real-time; WorkManager for periodic baseline sync."}]},{level:"senior",q:"How do you handle background location updates given the restrictions?",a:[{t:"p",text:"Background location is heavily restricted: it needs the separate `ACCESS_BACKGROUND_LOCATION` permission (granted via Settings, after foreground), and continuous updates require a *foreground service* (type `location`) with a visible notification. For occasional background location, use the batched/passive APIs or geofencing (system-managed, battery-efficient). Request the minimum precision/frequency, and be transparent — Play scrutinizes background location use."},{t:"list",items:["**`ACCESS_BACKGROUND_LOCATION`** — separate, Settings-granted permission.","**Continuous** — foreground service (type location) + notification.","**Occasional** — geofencing/passive/batched APIs (efficient).","**Minimize + transparent** — Play scrutinizes background location."]},{t:"note",text:"Background location is restricted: needs ACCESS_BACKGROUND_LOCATION (separate, Settings-granted after foreground); continuous updates require a foreground service (type location) + notification. For occasional needs use geofencing/passive/batched APIs (battery-efficient). Request minimum precision/frequency; Play scrutinizes background location use."}]},{level:"senior",q:"What is the difference between Doze and App Standby?",a:[{t:"p",text:"*Doze* is *device-wide* — triggered when the *whole device* is unused (screen off, stationary, unplugged), deferring all apps' background work into maintenance windows. *App Standby* is *per-app* — an individual app the user hasn't interacted with recently gets its background access restricted (regardless of device state). Doze responds to device idleness; App Standby to per-app inactivity. Both reduce battery drain from background work."},{t:"list",items:["**Doze** — device-wide; whole device idle.","**App Standby** — per-app; individual app unused.","**Trigger** — device idleness vs app inactivity.","**Both** — restrict background work for battery."]},{t:"note",text:"Doze is device-wide (whole device idle — screen off/stationary/unplugged — deferring all apps' background work to maintenance windows); App Standby is per-app (an individual unused app gets restricted regardless of device state). Doze responds to device idleness, App Standby to per-app inactivity. Both save battery."}]},{level:"senior",q:"How do you test how your app behaves in Doze and App Standby?",a:[{t:"p",text:"Use `adb` to force these states: `adb shell dumpsys deviceidle force-idle` (enter Doze), `unforce`/`step` to advance, and `adb shell am set-inactive <pkg> true` (App Standby). Then verify your background work behaves correctly (deferred, resumes in maintenance windows, high-priority FCM still delivers). Testing these ensures your app's background features are reliable under real-world battery restrictions."},{t:"list",items:["**`dumpsys deviceidle force-idle`** — force Doze.","**`am set-inactive <pkg> true`** — App Standby.","**Verify** — deferral, maintenance-window resume, FCM delivery.","**Reliability** — under real battery restrictions."]},{t:"note",text:"Force Doze/App Standby with adb: adb shell dumpsys deviceidle force-idle (+unforce/step) and adb shell am set-inactive <pkg> true. Verify background work defers correctly, resumes in maintenance windows, and high-priority FCM still delivers — ensuring reliability under real battery restrictions."}]},{level:"junior",q:"When is a persistent WebSocket appropriate versus FCM on mobile?",a:[{t:"p",text:"A *persistent WebSocket* is appropriate for *active, foreground* real-time features (a live chat screen open, a trading dashboard, an ongoing call) where low-latency bidirectional communication is needed *while the user is engaged*. But you *can't* keep it alive in the background (Doze/backgrounding kill it) — so for *background* real-time delivery, use *FCM*. The pattern: WebSocket while foregrounded, FCM to wake/notify when backgrounded."},{t:"list",items:["**WebSocket** — active foreground real-time (chat screen, dashboard, call).","**Can't background** — Doze/backgrounding kill it.","**FCM** — background real-time delivery.","**Combine** — WebSocket foreground, FCM background."]},{t:"note",text:"A persistent WebSocket suits active foreground real-time features (open chat screen, dashboard, ongoing call) needing low-latency bidirectional communication while engaged — but can't survive backgrounding/Doze. Use FCM for background real-time delivery. Pattern: WebSocket while foregrounded, FCM to wake/notify when backgrounded."}]},{level:"senior",q:"How do you architect an offline-first app given background restrictions?",a:[{t:"p",text:"Make the *local DB the single source of truth* — the UI always reads from it, so the app works fully offline regardless of background limits. *WorkManager* syncs the DB (deferrable, guaranteed); *FCM* triggers immediate syncs for freshness. Writes queue locally (outbox) and sync when possible. This decouples the app's usability from background execution — the restrictions delay *sync freshness*, not *availability*."},{t:"list",items:["**DB source of truth** — UI reads it; works offline.","**WorkManager sync** — deferrable, guaranteed.","**FCM** — immediate sync triggers.","**Outbox** — queue writes; sync when possible."]},{t:"note",text:"Offline-first given background limits: local DB as single source of truth (UI reads it — works offline regardless of restrictions), WorkManager to sync (deferrable/guaranteed), FCM for immediate sync triggers, an outbox for queued writes. Decouples usability from background execution — restrictions delay freshness, not availability."}]},{level:"senior",q:"What is the Restricted App Standby bucket, and how do you avoid it?",a:[{t:"p",text:"The *Restricted* bucket is the most limited — apps land there for *bad behavior* (excessive background work, wakelocks, battery drain) or user restriction. Restricted apps get *very limited* background jobs, alarms, and FCM. Avoid it by being a *good background citizen*: minimize wakelocks and background work, use WorkManager/FCM appropriately, and don't drain battery. Users can also manually restrict an app."},{t:"list",items:["**Restricted bucket** — most limited; for bad behavior/user restriction.","**Very limited** — jobs/alarms/FCM.","**Avoid** — minimize wakelocks/background work; be efficient.","**Manual** — users can restrict an app too."]},{t:"note",text:"The Restricted App Standby bucket is the most limited (very few jobs/alarms/FCM) — apps land there for bad behavior (excessive background work/wakelocks/battery drain) or user restriction. Avoid it by being a good background citizen (minimize wakelocks/background work, use WorkManager/FCM appropriately). Users can also manually restrict apps."}]},{level:"junior",q:"What is a wakelock, and why should you avoid holding one?",a:[{t:"p",text:"A `WakeLock` keeps the CPU (or screen) awake, preventing the device from sleeping. Holding one drains battery fast and is a common cause of poor battery life — and Doze *ignores* wakelocks anyway. Modern APIs (WorkManager, foreground services) manage wake state for you, so you rarely need a wakelock. If you must, use `PARTIAL_WAKE_LOCK` for the shortest time and always release it."},{t:"list",items:["**`WakeLock`** — keeps CPU/screen awake.","**Battery drain** — a common poor-battery cause; a leak keeps the device awake.","**Ignored in Doze** — anyway.","**Rarely needed** — WorkManager/foreground services manage it."]},{t:"note",text:"A WakeLock keeps the CPU/screen awake (prevents sleep) — holding one drains battery fast (a leak is a common poor-battery cause), and Doze ignores wakelocks anyway. WorkManager/foreground services manage wake state, so you rarely need one; if you must, use PARTIAL_WAKE_LOCK briefly and always release it."}]},{level:"senior",q:"How do you minimize your app's battery impact from background work?",a:[{t:"p",text:"Batch work (constraints, WorkManager batching), defer non-urgent tasks, use FCM push instead of polling, avoid wakelocks and frequent alarms, coalesce network requests, respect Doze/buckets, and use appropriate constraints (charging/unmetered) for heavy work. Profile battery with the *Battery Historian*/Android vitals. The principle: do as little background work as possible, as batched and deferred as possible."},{t:"list",items:["**Batch + defer** — constraints, WorkManager batching.","**Push over polling** — FCM.","**Avoid wakelocks/frequent alarms** — big drainers.","**Constraints for heavy work** — charging/unmetered; profile with vitals."]},{t:"note",text:"Minimize battery impact: batch/defer work (constraints, WorkManager), use FCM push over polling, avoid wakelocks and frequent alarms, coalesce requests, respect Doze/buckets, constrain heavy work to charging/unmetered. Profile with Battery Historian/Android vitals. Principle: as little background work as possible, batched and deferred."}]},{level:"junior",q:"What is the impact of App Standby buckets on FCM message delivery?",a:[{t:"p",text:"App Standby buckets impose a *quota* on how many *normal-priority* FCM messages an app can receive — lower buckets get fewer. *High-priority* messages are generally exempt (delivered promptly) but should be reserved for time-sensitive, user-visible content. So a rarely-used app's normal-priority messages may be delayed/limited. Use high priority judiciously for messages that must arrive immediately."},{t:"list",items:["**Normal-priority quota** — limited by bucket (lower = fewer).","**High-priority** — generally exempt; delivered promptly.","**Rarely-used apps** — normal messages delayed/limited.","**Use high priority** — only for time-sensitive/visible."]},{t:"note",text:"App Standby buckets impose a quota on normal-priority FCM messages (lower buckets = fewer); high-priority messages are generally exempt (prompt delivery) but should be reserved for time-sensitive user-visible content. Rarely-used apps' normal messages may be delayed/limited. Use high priority judiciously."}]},{level:"senior",q:"How do you handle a task that must run soon but not exactly now?",a:[{t:"p",text:"For 'run reasonably soon but not exact-time' (send a queued message when back online), use *expedited WorkManager work* (`setExpedited`) — it requests immediate execution with a quota and network constraints. It's more urgent than normal deferred work but still battery-aware (unlike an exact alarm or a foreground service). If it's genuinely user-initiated and time-sensitive, expedited work is the sweet spot between deferred and immediate."},{t:"list",items:["**Expedited work** — run ASAP with a quota.","**Between deferred and immediate** — not exact, not ongoing.","**Battery-aware** — unlike alarms/foreground services.","**User-initiated** — send message, immediate sync."]},{t:"note",text:"For 'run soon but not exact-time' (send a queued message on reconnect), use expedited WorkManager work (setExpedited) — immediate-ish execution with a quota and constraints, battery-aware (unlike exact alarms/foreground services). The sweet spot between deferred and immediate for user-initiated time-sensitive tasks."}]},{level:"senior",q:"How do aggressive OEM battery managers affect background work?",a:[{t:"p",text:"Beyond stock Android, some OEMs (Xiaomi, Huawei, OnePlus, Samsung) add *aggressive* battery managers that kill background processes, restrict auto-start, and ignore standard scheduling even more than stock Doze — so WorkManager/alarms/FCM may not run reliably on those devices. Mitigate by prompting the user to whitelist the app / disable battery restrictions (with guidance), relying on FCM (which OEMs generally respect), and testing on affected devices. The `dontkillmyapp.com` site documents per-OEM behavior."},{t:"list",items:["**Aggressive OEMs** — kill background, restrict auto-start beyond stock.","**Unreliable scheduling** — WorkManager/alarms may not run.","**Mitigate** — prompt to whitelist/disable restrictions; rely on FCM.","**Test on affected devices** — Xiaomi/Huawei/OnePlus/Samsung."]},{t:"note",text:"Some OEMs (Xiaomi/Huawei/OnePlus/Samsung) add aggressive battery managers that kill background processes and restrict auto-start beyond stock Doze — so WorkManager/alarms/FCM may run unreliably. Mitigate: prompt to whitelist/disable restrictions, rely on FCM (generally respected), test on affected devices (see dontkillmyapp.com)."}]},{level:"senior",q:"How do you diagnose and reduce your app's battery drain?",a:[{t:"p",text:"Measure with *Battery Historian* (parse a bug report to see wakeups, wakelocks, jobs, network per app), Android Studio's *Energy Profiler*, and Play Console's *Android vitals* (excessive wakeups, wakelocks, background usage). Identify the culprits (frequent alarms/jobs, held wakelocks, chatty network) and reduce them: batch work, defer, use FCM over polling, release wakelocks, and add constraints. Vitals also flags apps for excessive background battery use."},{t:"list",items:["**Battery Historian / Energy Profiler** — wakeups, wakelocks, jobs, network.","**Android vitals** — excessive wakeups/wakelocks/background usage.","**Reduce** — batch/defer, FCM over polling, release wakelocks, constraints.","**Iterate** — measure, fix the worst offenders, re-measure."]},{t:"note",text:"Diagnose battery drain with Battery Historian (bug report → wakeups/wakelocks/jobs/network), Energy Profiler, and Play Android vitals (excessive wakeups/wakelocks/background). Reduce: batch/defer work, FCM over polling, release wakelocks, add constraints. Vitals flags excessive background battery use — measure, fix, re-measure."}]},{level:"junior",q:"What is the difference between deferrable and time-critical background work?",a:[{t:"p",text:"*Deferrable* work can run *whenever the system finds it convenient* (within battery/Doze constraints) — sync, upload, cleanup — perfectly suited to WorkManager. *Time-critical* work must run at a *specific moment* (an alarm) or *immediately* (a user-visible action). The distinction drives tool choice: deferrable → WorkManager (batched, battery-friendly); time-critical → AlarmManager (exact) or foreground service/FCM (immediate). Most background work is (and should be treated as) deferrable."},{t:"list",items:["**Deferrable** — run when convenient; WorkManager.","**Time-critical** — exact moment (AlarmManager) or immediate (FG service/FCM).","**Drives tool choice** — batched vs precise/urgent.","**Most work** — is/should be deferrable."]},{t:"note",text:"Deferrable work runs when the system finds convenient (within constraints/Doze) — sync/upload/cleanup, ideal for WorkManager. Time-critical work needs an exact moment (AlarmManager) or immediate execution (foreground service/FCM). Most background work is/should be treated as deferrable — it's more battery-friendly and reliable."}]},{level:"senior",q:"How do FCM data messages trigger background work?",a:[{t:"p",text:"An FCM *data message* delivers a payload to your `FirebaseMessagingService.onMessageReceived`, where you can *enqueue WorkManager* to do the actual work (sync, download) — since `onMessageReceived` runs briefly and, if the app is backgrounded, you have limited time. High-priority data messages wake the device from Doze for this. The pattern: FCM as the *trigger*, WorkManager as the *executor* of guaranteed background work."},{t:"list",items:["**Data message** → `onMessageReceived`.","**Enqueue WorkManager** — for the actual (guaranteed) work.","**High-priority** — wakes from Doze; limited run time.","**FCM = trigger, WorkManager = executor**."]},{t:"note",text:"An FCM data message hits FirebaseMessagingService.onMessageReceived (brief, limited time when backgrounded) — enqueue WorkManager there for the actual work (sync/download). High-priority data messages wake the device from Doze. Pattern: FCM as the trigger, WorkManager as the executor of guaranteed background work."}]},{level:"junior",q:"If you must poll, how do you choose a sensible interval?",a:[{t:"p",text:"First, prefer push (FCM) over polling. If you must poll, use the *longest* interval that meets the requirement (WorkManager periodic min is 15 min anyway), add constraints (network, maybe charging), use *adaptive* intervals (poll more when the user is active, less when idle), and stop polling when not needed (screen off / backgrounded). Frequent polling drains battery and gets throttled by Doze/buckets — minimize it."},{t:"list",items:["**Prefer push** — FCM over polling.","**Longest acceptable interval** — WorkManager min 15 min.","**Adaptive** — more when active, less when idle.","**Stop when not needed** — screen off/backgrounded."]},{t:"note",text:"Prefer FCM push. If you must poll: use the longest acceptable interval (WorkManager periodic min is 15 min), add constraints (network/charging), poll adaptively (more when active, less when idle), and stop when not needed (screen off/backgrounded). Frequent polling drains battery and is throttled — minimize it."}]},{level:"senior",q:"How do constraints, Doze, and standby buckets together determine when work runs?",a:[{t:"p",text:"Work runs only when *all* factors align: your *constraints* are satisfied (network/charging), the device isn't restricting it via *Doze* (deferred to maintenance windows if idle), and the app's *standby bucket* quota allows it (lower buckets = less frequent). So the actual run time is the *intersection* of these — 'when my constraints are met AND the system's battery policies permit'. This is why background timing is *never* guaranteed, only eventual."},{t:"list",items:["**Constraints** — your conditions (network/charging).","**Doze** — defers when the device is idle.","**Standby bucket** — quota by app usage.","**Intersection** — runs when all align; timing not guaranteed."]},{t:"note",text:"Work runs at the intersection of: your constraints being met (network/charging), Doze not deferring it (maintenance windows when idle), and the standby bucket quota allowing it (lower buckets = less frequent). Actual timing is 'constraints met AND battery policies permit' — which is why background execution is eventual, never guaranteed-when."}]},{level:"senior",q:"How do you keep essential features reliable despite background restrictions?",a:[{t:"p",text:"Design so essential functionality *doesn't depend on background execution*: offline-first with a local DB source of truth (the app works fully without background sync), FCM for server-pushed updates (respected across restrictions), and refresh-on-open (sync when the user opens the app). Treat background sync as an *optimization* for freshness, not a requirement for the app to function. This makes the app reliable regardless of Doze/buckets/OEMs."},{t:"list",items:["**Offline-first** — local DB source of truth; works without background.","**FCM** — server push, respected across restrictions.","**Refresh-on-open** — sync when the user returns.","**Background = optimization** — not a functional requirement."]},{t:"note",text:"Keep essential features reliable by not depending on background execution: offline-first (local DB source of truth — works without background sync), FCM for pushed updates (respected across restrictions), refresh-on-open (sync when the user returns). Treat background sync as a freshness optimization, not a requirement — reliable regardless of Doze/buckets/OEMs."}]},{level:"junior",q:"What is the maintenance window in Doze, and how often does it occur?",a:[{t:"p",text:"A *maintenance window* is a brief period during Doze when the system *temporarily lifts* restrictions — deferred jobs, syncs, and alarms all run, and network access is restored. Windows occur *periodically* but *progressively less often* the longer the device stays idle (from every hour or so down to once every few hours). So deferred background work batches up and executes in these bursts, then the device returns to deep idle."},{t:"list",items:["**Maintenance window** — restrictions briefly lifted; deferred work runs.","**Periodic** — occurs regularly during Doze.","**Less frequent over time** — longer idle = rarer windows.","**Batched bursts** — work executes, then back to deep idle."]},{t:"note",text:"A Doze maintenance window is a brief period when restrictions lift — deferred jobs/syncs/alarms run and network is restored. Windows occur periodically but progressively less often the longer the device stays idle (hourly down to every few hours). Deferred background work batches and runs in these bursts."}]}],SC={id:"doze-choosing",title:"Doze, Battery & Choosing the Right Tool",tagline:"Doze & maintenance windows, App Standby buckets, the battery-restriction trend, push over polling, and the decision framework for background mechanisms.",tags:["Doze","App Standby","Battery","FCM","Choosing the Tool"],content:kC,qa:xC},AC={id:"background-work",name:"Background Work",description:"Running work off the UI — WorkManager fundamentals & advanced (constraints, chaining, unique work), AlarmManager & foreground services, and Doze/battery & choosing the right tool.",topics:[mC,yC,bC,SC]},CC=[{heading:"Why test, and what tests give you",blocks:[{t:"p",text:"**Testing** means writing code that verifies your app's code behaves correctly, automatically. The value isn't just 'catching bugs' — it's *confidence to change code*. Without tests, every change risks silently breaking something, so you become afraid to refactor. With a good test suite, you change code freely and the tests tell you immediately if you broke something. Tests also *document* expected behavior (a test is an executable spec of what a function should do) and catch regressions (a bug you fix, plus a test, never comes back)."},{t:"list",items:["**Confidence to refactor** — the biggest benefit. Tests are a safety net that lets you improve code without fear.","**Regression prevention** — a bug fixed with a test can't silently return.","**Living documentation** — tests show how code is *meant* to be used and what it should do.","**Faster feedback** — a failing unit test tells you exactly what broke in seconds, versus discovering it in production."]}]},{heading:"The testing pyramid",blocks:[{t:"p",text:"The **testing pyramid** is a strategy for *how many of each kind of test* to write. It's a triangle: lots of fast, cheap **unit tests** at the bottom; fewer **integration tests** in the middle; and a small number of slow, expensive **end-to-end / UI tests** at the top. The shape reflects a cost/speed/confidence trade-off — you want most of your coverage from the fast, cheap tests, and only a few of the slow, brittle ones."},{t:"table",headers:["Layer","What it tests","Speed / cost","How many"],rows:[["Unit tests (bottom)","a single class/function in isolation (a ViewModel, a use case, a mapper)","very fast (JVM, milliseconds), cheap","many (the bulk)"],["Integration tests (middle)","several components together (repository + DAO, ViewModel + repository)","slower (may need a device/Robolectric)","some"],["UI / end-to-end (top)","whole flows through the real UI (login → home → detail)","slow (on-device), brittle","few (critical paths only)"]]},{t:"list",items:["**Unit tests (base)** — test one unit in isolation, on the JVM (no device), with dependencies faked. Fast (milliseconds), reliable, and pinpoint exactly what broke. Most of your tests should be here — ViewModels, use cases, repositories (with fakes), mappers, utilities.","**Integration tests (middle)** — test that components work *together* correctly (a repository actually reading from a real Room DB, a ViewModel driving a repository). Slower, but catch wiring bugs unit tests miss.","**UI / end-to-end tests (top)** — drive the real UI through a whole user flow. They give the most *realistic* confidence but are slow (run on an emulator/device), brittle (break on UI changes), and hard to debug. Reserve them for a few critical journeys (login, checkout)."]}]},{heading:"Why the pyramid shape (not an inverted one)",blocks:[{t:"list",items:["**Speed** — unit tests run in milliseconds on the JVM; UI tests take seconds each on a device. A suite of thousands of unit tests runs in seconds; thousands of UI tests would take hours. Fast tests get run often (every save, every commit); slow ones get skipped.","**Reliability** — unit tests are deterministic; UI tests are *flaky* (timing issues, animations, device state, emulator hiccups). A flaky suite that fails randomly gets ignored, defeating the point.","**Precision** — a failing unit test points at the exact function; a failing end-to-end test tells you 'something in this whole flow broke' but not *what*, requiring debugging.","**The anti-pattern — 'ice cream cone'**: mostly UI tests, few unit tests. It's slow, flaky, and expensive to maintain — the inverse of what you want. Aim for a pyramid: build correctness bottom-up with cheap unit tests, and use a *thin* layer of UI tests for confidence that the pieces integrate."]}]},{heading:"Android's test categories: local vs instrumented",blocks:[{t:"table",headers:["","Local (unit) tests","Instrumented tests"],rows:[["Where","`src/test/` — run on the JVM (your machine)","`src/androidTest/` — run on a device/emulator"],["Speed","very fast (no device)","slow (device deployment + execution)"],["Android framework","not available (or use Robolectric to fake it)","real Android framework available"],["Use for","pure logic — ViewModels, use cases, repositories with fakes","UI tests, Room DAO tests, things needing real Android"]]},{t:"list",items:["**Local/unit tests (`src/test/`)** run on the JVM — fast, no device. Pure Kotlin/Java logic tests go here. Since the Android framework isn't available, you either avoid it (test framework-free logic) or use **Robolectric** (which provides a fake Android framework on the JVM).","**Instrumented tests (`src/androidTest/`)** run on a real device/emulator with the actual Android framework — needed for UI tests (Compose/Espresso) and things that need real Android behavior (some Room, real Context). Slower but realistic.","**The design implication**: architecture that *separates logic from the framework* (Clean Architecture, framework-free domain/ViewModel logic) is what makes most of your tests fast JVM unit tests. Logic tangled with Android forces slow instrumented tests. This is a major practical reason for the layered architecture."]},{t:"note",text:"Testing strategy essentials: tests give confidence to refactor, prevent regressions, and document behavior. The pyramid — many fast unit tests (JVM, isolated, faked dependencies), some integration tests (components together), few UI/e2e tests (whole flows, slow/brittle) — because fast reliable tests get run and pinpoint failures, while slow flaky UI tests should be a thin top layer. Android split: local tests (src/test, JVM, fast) vs instrumented (src/androidTest, device, real framework). Framework-free architecture keeps most tests fast."}]}],TC=[{level:"junior",q:"What is the testing pyramid?",a:[{t:"p",text:"**The testing pyramid is a strategy for how many of each kind of test to write, shaped like a triangle: many fast, cheap *unit tests* at the bottom, fewer *integration tests* in the middle, and a small number of slow, expensive *UI/end-to-end tests* at the top.** The shape reflects a trade-off — you get most of your coverage from the fast, reliable tests, and use only a few of the slow, brittle ones."},{t:"list",items:["**Unit tests (base, many)** — test a single class or function in isolation on the JVM with its dependencies faked. Milliseconds fast, deterministic, and they pinpoint exactly what broke. Most of your tests live here — ViewModels, use cases, mappers, repositories with fakes.","**Integration tests (middle, some)** — test that several components work *together* (a repository with a real Room database, a ViewModel driving a repository). Slower, but catch wiring bugs unit tests miss.","**UI / end-to-end tests (top, few)** — drive the real UI through whole flows (login → home). Most realistic, but slow (run on a device), brittle, and hard to debug — so reserve them for a few critical journeys."]},{t:"p",text:"The reason for the shape is speed and reliability: fast unit tests get run constantly and tell you precisely what broke, while slow, flaky UI tests should be a *thin* top layer. The anti-pattern is the inverted 'ice cream cone' — mostly UI tests — which is slow, flaky, and expensive to maintain. Build correctness bottom-up with cheap unit tests, and use a small set of UI tests to confirm the pieces integrate."}]},{level:"junior",q:"Why write tests at all?",a:[{t:"p",text:"**The biggest reason is *confidence to change code*.** Without tests, every change risks silently breaking something elsewhere, so developers become afraid to refactor or add features. With a good test suite, you change code freely and the tests immediately tell you if you broke something — tests are a safety net that makes the codebase malleable rather than fragile."},{t:"list",items:["**Confidence to refactor** — you can improve or restructure code without fear, because tests catch regressions. This keeps a codebase healthy over time.","**Regression prevention** — a bug you fix *with* a test can't silently come back; the test guards against it forever.","**Living documentation** — a test is an executable spec showing how a function is meant to be used and what it should do; unlike comments, it can't go stale (it'd fail).","**Fast, precise feedback** — a failing unit test tells you exactly what broke in seconds, versus discovering the bug in production or during slow manual QA."]},{t:"p",text:"The framing that lands: tests aren't primarily about *finding* bugs the first time (though they do) — they're about *preventing* the reintroduction of bugs and enabling *fearless change*. A codebase with good tests can evolve; one without them ossifies because nobody dares touch it. That's why testing is considered a core engineering discipline, not an optional extra."}]},{level:"junior",q:"What is the difference between local (unit) tests and instrumented tests in Android?",a:[{t:"p",text:"**Local tests run on the JVM (your development machine) without a device; instrumented tests run on a real device or emulator with the actual Android framework.** This location difference drives everything about them."},{t:"list",items:["**Local/unit tests** live in `src/test/` and run on the JVM — very *fast* (no device deployment) and used for pure logic: ViewModels, use cases, repositories (with fakes), mappers, utilities. The catch is the Android framework isn't available, so you either test framework-free code or use *Robolectric* (which provides a simulated Android framework on the JVM).","**Instrumented tests** live in `src/androidTest/` and run on a device/emulator with the *real* Android framework — needed for UI tests (Compose/Espresso), Room DAO tests, and anything that needs genuine Android behavior (real Context, real database). They're *slower* (deploy to device + execute) but realistic."]},{t:"p",text:"The practical implication is architectural: if your logic is *separated from the Android framework* (Clean Architecture — framework-free domain and ViewModel logic), most of your tests can be fast JVM unit tests. If your logic is tangled with Android APIs, you're forced into slow instrumented tests. So keeping business logic framework-independent isn't just clean design — it directly makes your test suite fast, which is a major practical reason for the layered architecture. You want the bulk of the pyramid (unit tests) to be local/JVM tests, with instrumented tests reserved for UI and genuinely Android-dependent pieces."}]},{level:"senior",q:"Why is the inverted testing pyramid (mostly UI tests) an anti-pattern?",a:[{t:"p",text:"**The inverted pyramid — often called the 'ice cream cone' (many UI/end-to-end tests, few unit tests) — is an anti-pattern because it optimizes for the *most realistic* tests at the expense of the properties that make a test suite actually useful: speed, reliability, and precision. It produces a suite that's slow, flaky, and hard to maintain, which paradoxically leads to *less* effective testing.**"},{t:"list",items:["**Speed — it becomes too slow to run**: UI tests take seconds each on a device; a suite dominated by them takes many minutes or hours. Slow suites don't get run frequently — developers skip them locally, CI becomes a bottleneck, and feedback that arrives an hour after a change is nearly useless. Fast unit suites run on every save.","**Reliability — flakiness erodes trust**: UI tests are inherently flaky (timing, animations, device/emulator state, network). A suite that fails randomly ~5% of the time trains the team to *ignore* failures ('just re-run it'), which defeats the entire purpose — a test you ignore protects nothing. Unit tests are deterministic and stay trustworthy.","**Precision — poor failure localization**: when an end-to-end test fails, it tells you 'something in this whole flow broke' but not *what* or *where* — you have to debug through the entire stack. A failing unit test points at the exact function. Debugging inverted-pyramid failures is expensive.","**Maintenance cost — brittle to change**: UI tests break on *any* UI change (a moved button, a renamed label), even when behavior is correct, so a UI-heavy suite generates constant maintenance churn that isn't catching real bugs. Unit tests are coupled to *behavior*, not presentation, so they survive UI refactors."]},{t:"list",items:["**How it happens**: teams sometimes reach for UI tests because they *feel* more real ('it tests what the user does') and because the code wasn't architected for unit testing (logic tangled with Android/UI, so unit tests are hard to write). The fix is both cultural (value fast unit tests) and architectural (separate logic from framework so it's unit-testable).","**The right shape**: build correctness *bottom-up* — thorough, fast unit tests for logic (the bulk), a moderate layer of integration tests for component wiring, and a *thin* top layer of UI tests covering only the few critical user journeys (login, checkout) for integration confidence. You get fast feedback, precise failures, and a small, high-value UI layer."]},{t:"p",text:"**The senior framing**: the goal of a test suite isn't 'maximum realism per test' — it's *maximum confidence per unit of time and maintenance cost*. Unit tests win overwhelmingly on that metric (fast, reliable, precise, cheap to maintain), so they should dominate; UI tests buy integration confidence but at high cost, so they should be few and targeted. The inverted pyramid mistakes 'realistic' for 'valuable' and ends up with a suite too slow and flaky to trust — which is worse than fewer, faster tests that people actually run and believe. Recognizing that testing is about *sustainable confidence*, and that architecture (framework-free logic) is what enables the healthy shape, is the mature view."}]},{level:"senior",q:"How does app architecture affect testability, and what makes code easy or hard to test?",a:[{t:"p",text:"**Architecture is the single biggest factor in testability — well-architected code is easy to test almost incidentally, while poorly-architected code is hard to test no matter how much effort you put in. The core principle is *separation of concerns plus dependency injection*: isolate logic from the framework and from its collaborators, so each piece can be tested independently with fakes.**"},{t:"list",items:["**Dependency injection makes code testable** — the most important factor. A class that *receives* its dependencies (constructor injection) can be tested by injecting fakes; a class that *creates* its dependencies (`val repo = RealRepository(realApi, realDb)`) forces real collaborators (real network, real DB) into every test, making them slow, flaky, or impossible. DI is what lets you construct a unit in isolation with controlled inputs.","**Separating logic from the Android framework** makes tests *fast JVM unit tests* instead of slow instrumented ones. A ViewModel or use case that's pure Kotlin (no Android imports, framework accessed via injected abstractions) runs on the JVM in milliseconds. One tangled with `Context`, `View`, or static Android calls needs a device or Robolectric. Clean Architecture's framework-free domain/ViewModel layers exist partly for exactly this.","**Single responsibility makes tests focused** — a class doing one thing has a small, clear set of behaviors to test; a God class doing ten things needs a sprawling, brittle test that's hard to write and understand. Small units → small, clear tests.","**Depending on interfaces/abstractions enables substitution** — when a ViewModel depends on a `Repository` *interface*, tests inject a `FakeRepository`. When it depends on a concrete `RepositoryImpl`, you're stuck with the real one (or must mock a concrete class, which is more fragile). This is the dependency-inversion principle serving testability.","**Pure functions / immutable state** — logic expressed as pure functions (same input → same output, no side effects) is trivially testable (just assert outputs). Code full of hidden mutable state and side effects is hard to test because behavior depends on invisible context. Unidirectional data flow and immutable UiState make ViewModels assertable ('given this input, the emitted state is X')."]},{t:"list",items:["**What makes code *hard* to test (the anti-patterns)**: static/global state and singletons accessed directly (can't substitute); hardcoded dependencies (`new`/direct construction); logic buried in Android components (Activities, custom Views) that need a device; hidden side effects and mutable shared state; hardcoded dispatchers/threads (can't control timing); and tight coupling to concrete classes. Each forces slow, brittle, or impossible tests.","**The virtuous cycle**: designing for testability *also* produces better design — DI, separation of concerns, interfaces, pure functions, and immutability are good architecture *independent* of testing. So 'is this testable?' is a useful design lens: if a class is hard to test, it's usually because it's doing too much or is too coupled — the difficulty is a *design smell*, not just a testing inconvenience."]},{t:"p",text:"**The senior framing**: testability isn't something you bolt on — it's a *property of good architecture*. The same choices that make code clean (DI, layered separation, interfaces, single responsibility, immutable state, injected dispatchers) are exactly what make it testable, and code that resists testing is usually revealing a design problem. So the mature perspective is bidirectional: architect for separation and injection, and you get testability for free; and conversely, use 'how hard is this to test?' as a signal about design quality. This is why the earlier categories (Clean Architecture, DI, framework-free ViewModels, injected dispatchers) keep connecting back to testing — they're the *enablers* of the healthy testing pyramid."}]},{level:"junior",q:"What is the cost/speed/reliability trade-off across test types?",a:[{t:"p",text:"As you go up the pyramid, tests get *slower, more expensive to write/maintain, and flakier*, but *more realistic*. Unit tests are fast/cheap/reliable but narrow; UI/E2E tests catch integration bugs but are slow, brittle, and flaky. This trade-off is *why* the pyramid recommends *many* fast unit tests, *some* integration tests, and *few* UI/E2E tests — maximizing confidence per unit of cost."},{t:"table",headers:["","Unit","Integration","UI/E2E"],rows:[["Speed","fast","medium","slow"],["Cost/maintenance","low","medium","high"],["Flakiness","low","medium","high"],["Realism","narrow","moderate","full"]]},{t:"list",items:["**Unit** — fast, cheap, reliable, narrow.","**UI/E2E** — realistic but slow, expensive, flaky.","**Pyramid** — many unit, some integration, few UI.","**Maximize confidence per cost.**"]},{t:"note",text:"Up the pyramid: slower, costlier, flakier, but more realistic. Unit tests are fast/cheap/reliable but narrow; UI/E2E catch integration bugs but are slow/brittle/flaky. This trade-off drives the pyramid — many fast unit tests, some integration, few UI/E2E — for maximum confidence per cost."}]},{level:"junior",q:"What is a smoke test?",a:[{t:"p",text:"A *smoke test* is a quick, shallow check that the *most critical paths work* — 'does the app launch, log in, and show the main screen without crashing?' It's run early (after a build/deploy) to catch catastrophic failures before deeper testing. Smoke tests are broad but shallow (breadth over depth), giving fast confidence that the build is fundamentally sound."},{t:"list",items:["**Quick, shallow** — critical paths work at all.","**Run early** — after build/deploy.","**Breadth over depth** — cover key flows superficially.","**Catch catastrophic failures** — before deeper testing."]},{t:"note",text:"A smoke test is a quick, shallow check of the most critical paths (launch, log in, main screen — no crash), run early after a build/deploy to catch catastrophic failures before deeper testing. Broad but shallow — fast confidence the build is fundamentally sound."}]},{level:"junior",q:"What is regression testing?",a:[{t:"p",text:"Regression testing verifies that *new changes haven't broken existing functionality* — re-running the test suite (or the relevant subset) after every change. Automated tests are the primary defense against regressions: a good suite catches when a fix or feature accidentally breaks something else. This is a core value of tests — they let you change code confidently."},{t:"list",items:["**Catch broken existing functionality** — after changes.","**Re-run the suite** — every change/PR (CI).","**Automated** — the main defense against regressions.","**Confidence** — change code without fear."]},{t:"note",text:"Regression testing verifies new changes haven't broken existing functionality — re-running the suite (or relevant subset) after every change, automated in CI. It's a core value of tests: catching when a fix/feature accidentally breaks something else, so you can change code confidently."}]},{level:"senior",q:"What is end-to-end (E2E) testing, and where does it fit?",a:[{t:"p",text:"E2E tests exercise a *complete user journey* through the *real app* (and often real/staging backend) — e.g. sign up → browse → purchase. They give the highest confidence that the whole system works together, but are the *slowest, flakiest, and most expensive* to maintain. Keep them *few* (top of the pyramid) — cover only the *most critical* happy paths, and rely on unit/integration tests for detailed logic."},{t:"list",items:["**Full user journey** — real app (+ backend).","**Highest confidence** — the whole system works.","**Slow/flaky/expensive** — keep them few.","**Critical happy paths only** — top of the pyramid."]},{t:"note",text:"E2E tests exercise a complete user journey through the real app (+ real/staging backend) — highest confidence the whole system works, but slowest/flakiest/most expensive. Keep them few (pyramid top), covering only the most critical happy paths; rely on unit/integration for detailed logic."}]},{level:"junior",q:"What is the difference between black-box and white-box testing?",a:[{t:"p",text:"*Black-box* testing tests behavior through the *public interface* without knowing the internal implementation — you verify inputs→outputs (most unit/integration/UI tests). *White-box* testing uses *knowledge of the internals* to target specific code paths/branches (coverage-driven). Black-box tests are more robust (survive refactoring); white-box helps ensure thorough path coverage. Prefer black-box for behavior, using white-box knowledge to find edge cases."},{t:"list",items:["**Black-box** — test via the interface; input→output.","**White-box** — use internal knowledge; target paths/branches.","**Black-box** — robust to refactoring.","**White-box** — thorough path coverage."]},{t:"note",text:"Black-box testing verifies behavior through the public interface without knowing internals (input→output — most tests); white-box uses internal knowledge to target specific paths/branches (coverage-driven). Black-box is refactor-robust; white-box ensures path coverage. Prefer black-box, using white-box knowledge to find edge cases."}]},{level:"senior",q:"What should you NOT test?",a:[{t:"p",text:"Don't test: *third-party/framework code* (trust that Room, Retrofit, the SDK work — test *your* usage, not their internals), *trivial code* (getters/setters, simple data classes with no logic), *generated code*, and *implementation details* (private methods, exact internal calls) that would make tests brittle. Focus tests on *your business logic, edge cases, and integration points* — where bugs actually live."},{t:"list",items:["**Third-party/framework** — trust it; test your usage.","**Trivial code** — getters/setters, plain data classes.","**Generated code** — not worth testing.","**Implementation details** — brittle; test behavior."]},{t:"note",text:"Don't test third-party/framework internals (test your usage, not Room/Retrofit's internals), trivial code (getters/setters, plain data classes), generated code, or implementation details (private methods, exact internal calls — brittle). Focus on your business logic, edge cases, and integration points — where bugs live."}]},{level:"senior",q:"What is the difference between verification and validation?",a:[{t:"p",text:"*Verification* asks 'are we building the product *right*?' — does the code meet its specification (correct behavior, no bugs)? Tests are verification. *Validation* asks 'are we building the *right* product?' — does it actually solve the user's problem/meet their needs? Validation involves user feedback, usability testing, analytics. Both matter: verification ensures correctness; validation ensures usefulness."},{t:"list",items:["**Verification** — building it right (meets spec); tests.","**Validation** — building the right thing (meets user needs).","**Validation** — user feedback, usability, analytics.","**Both matter** — correct AND useful."]},{t:"note",text:"Verification: 'are we building it right?' — does the code meet its spec (tests verify this). Validation: 'are we building the right thing?' — does it solve the user's problem (user feedback, usability, analytics). Verification ensures correctness; validation ensures usefulness. Both matter."}]},{level:"senior",q:"How do you decide what to test first (risk-based testing)?",a:[{t:"p",text:"Prioritize by *risk* — test the areas where a bug would be *most costly* and *most likely*: core business logic (payments, auth), complex logic with many edge cases, frequently-changed code, and critical user paths. Deprioritize trivial, stable, or low-impact code. Risk-based testing focuses limited effort where it delivers the most confidence, rather than testing everything equally."},{t:"list",items:["**High impact** — payments, auth, data integrity.","**High likelihood** — complex/frequently-changed code.","**Critical paths** — core user journeys.","**Deprioritize** — trivial/stable/low-impact."]},{t:"note",text:"Risk-based testing prioritizes where a bug is most costly AND most likely: core business logic (payments/auth/data integrity), complex/edge-case-heavy code, frequently-changed code, critical user paths. Deprioritize trivial/stable/low-impact code. Focus limited effort for maximum confidence."}]},{level:"senior",q:"What is contract testing?",a:[{t:"p",text:"Contract testing verifies that a *consumer* (the app) and a *provider* (the API/backend) agree on the *contract* (request/response shapes) — without full integration. A *consumer-driven contract* (e.g. Pact) records the app's expectations and verifies the provider satisfies them. This catches API changes that would break the app *before* deployment, more reliably than hoping E2E tests catch them."},{t:"list",items:["**Consumer + provider agree** — on request/response contracts.","**Consumer-driven (Pact)** — record app expectations, verify provider.","**Catches API changes** — before they break the app.","**More reliable** — than hoping E2E catches it."]},{t:"note",text:"Contract testing verifies a consumer (app) and provider (API) agree on the contract (request/response shapes) without full integration — consumer-driven contracts (Pact) record app expectations and verify the provider meets them. Catches breaking API changes before deployment, more reliably than E2E."}]},{level:"junior",q:"What is the role of manual and exploratory testing?",a:[{t:"p",text:"Automated tests can't catch everything — *manual/exploratory* testing (a human freely exploring the app) finds *usability issues*, *visual glitches*, *unexpected interactions*, and things you didn't think to automate. It complements automation: automate the *repetitive, deterministic* checks (regression), and use human testing for *judgment-based* exploration and UX. Both are part of a complete strategy."},{t:"list",items:["**Exploratory** — human freely exploring; finds the unexpected.","**Usability/visual/UX** — hard to automate.","**Complements automation** — not a replacement.","**Automate** — repetitive regression; explore for judgment."]},{t:"note",text:"Manual/exploratory testing (a human freely exploring) finds usability issues, visual glitches, unexpected interactions, and things you didn't automate — complementing automation (which handles repetitive deterministic regression). Automate the repetitive; use human testing for judgment-based UX exploration. Both matter."}]},{level:"senior",q:"How do you add tests to legacy code that has none?",a:[{t:"p",text:"Start with *characterization tests* — tests that capture the *current* behavior (even if buggy) so you have a safety net before refactoring. Then refactor to *break dependencies* (introduce interfaces/DI at *seams*) so the code becomes testable, adding proper unit tests as you go. Focus on the areas you're changing (don't try to test everything at once). Michael Feathers' 'Working Effectively with Legacy Code' is the reference."},{t:"list",items:["**Characterization tests** — capture current behavior as a net.","**Break dependencies** — introduce seams (interfaces/DI).","**Refactor to testable** — then add unit tests.","**Focus on changed areas** — incremental, not all at once."]},{t:"note",text:"For untested legacy code: write characterization tests (capture current behavior as a safety net), then break dependencies at seams (interfaces/DI) to make it testable, adding unit tests as you refactor. Focus on the areas you're changing, incrementally. See Feathers' 'Working Effectively with Legacy Code'."}]},{level:"junior",q:"What is 'shift-left' testing, and why does it matter?",a:[{t:"p",text:"'Shift-left' means testing *earlier* in development (moving testing 'left' on the timeline) — writing unit tests as you code, running tests in CI on every commit, catching bugs *before* they reach QA or production. It matters because the *cost of a bug grows dramatically* the later it's found (a bug caught in code review is cheap; one in production is expensive and damaging). Early testing = cheaper, faster fixes."},{t:"list",items:["**Test earlier** — unit tests while coding, CI on commits.","**Catch bugs early** — before QA/production.","**Cost grows** — bugs are far costlier later.","**Cheaper/faster fixes** — the shift-left payoff."]},{t:"note",text:"Shift-left = testing earlier in development (unit tests while coding, CI on every commit) to catch bugs before QA/production. It matters because a bug's cost grows dramatically the later it's found (code review = cheap; production = expensive/damaging). Early testing means cheaper, faster fixes."}]},{level:"senior",q:"How do you handle flaky tests in CI?",a:[{t:"p",text:"Flaky tests (pass/fail non-deterministically) *erode trust* in the suite — people start ignoring failures. Handle them: *quarantine* flaky tests (mark them so they don't block CI while you fix them), *investigate root causes* (real time/`Thread.sleep`, shared state, race conditions, network dependence), *fix* them (inject clocks/dispatchers, proper synchronization, fakes), and *don't just retry* (retries hide real bugs). A reliable suite is more valuable than a large flaky one."},{t:"list",items:["**Quarantine** — isolate flaky tests from blocking CI.","**Root-cause** — timing, shared state, races, network.","**Fix** — inject clocks/dispatchers, synchronize, use fakes.","**Don't just retry** — retries hide real bugs."]},{t:"note",text:"Flaky tests erode trust (people ignore failures). Handle them: quarantine (don't block CI while fixing), root-cause (real time/Thread.sleep, shared state, races, network), fix (inject clocks/dispatchers, proper synchronization, fakes) — don't just retry (that hides bugs). A reliable suite beats a large flaky one."}]},{level:"senior",q:"What is a characterization test?",a:[{t:"p",text:"A characterization (or 'golden master') test *documents the existing behavior* of code — you write a test that asserts what the code *currently does* (not what it *should* do), creating a safety net for refactoring legacy code. If a refactor changes the captured behavior, the test fails, alerting you. It's about *pinning down* current behavior before you change the implementation."},{t:"list",items:["**Captures current behavior** — what the code does now.","**Safety net** — for refactoring legacy code.","**Fails on behavior change** — alerts you.","**Not correctness** — pins current behavior (even if buggy)."]},{t:"note",text:"A characterization (golden master) test documents what code CURRENTLY does (not what it should) — a safety net for refactoring legacy code. If a refactor changes the captured behavior, it fails, alerting you. It pins current behavior before you change the implementation."}]},{level:"junior",q:"What is acceptance testing / BDD?",a:[{t:"p",text:"*Acceptance tests* verify the software meets *business requirements/user acceptance criteria* — 'given a logged-in user, when they add an item, then the cart count increases'. *BDD* (Behavior-Driven Development) expresses these in natural language (Gherkin: Given/When/Then, tools like Cucumber), bridging business and code. They're higher-level, focused on user-facing behavior and shared understanding rather than internal units."},{t:"list",items:["**Acceptance tests** — verify business/user criteria.","**BDD** — Given/When/Then natural-language scenarios.","**Bridges business + code** — shared understanding.","**User-facing behavior** — higher-level than units."]},{t:"note",text:"Acceptance tests verify the software meets business/user acceptance criteria (Given a logged-in user, When they add an item, Then the cart count increases). BDD expresses these in natural language (Gherkin Given/When/Then, Cucumber), bridging business and code — higher-level, user-facing behavior and shared understanding."}]},{level:"junior",q:"What is the difference between functional and non-functional testing?",a:[{t:"p",text:"*Functional* testing verifies *what* the app does — features work correctly (login, purchase). *Non-functional* testing verifies *how well* it does it — performance (speed/jank), security, accessibility, battery, memory, usability, compatibility across devices. Both matter: a feature can be functionally correct but fail non-functionally (correct but slow/inaccessible). Mobile especially needs non-functional testing (fragmentation, resources)."},{t:"list",items:["**Functional** — features work (what it does).","**Non-functional** — performance/security/accessibility/battery (how well).","**Both matter** — correct AND performant/accessible.","**Mobile** — non-functional is critical (fragmentation/resources)."]},{t:"note",text:"Functional testing verifies what the app does (features work — login/purchase); non-functional verifies how well (performance/jank, security, accessibility, battery, memory, compatibility). Both matter — a feature can be functionally correct but slow/inaccessible. Mobile especially needs non-functional testing."}]},{level:"senior",q:"What mobile-specific concerns should your test strategy cover?",a:[{t:"p",text:"Mobile adds concerns beyond logic: *device fragmentation* (screen sizes, API levels, OEMs — test on a matrix or use Firebase Test Lab), *configuration changes* (rotation, process death — verify state restoration), *offline/flaky networks*, *permissions* (grant/deny paths), *lifecycle* (background/foreground), *performance* on low-end devices, and *accessibility*. A mobile test strategy must cover these, not just business logic."},{t:"list",items:["**Fragmentation** — screens/API levels/OEMs (Test Lab matrix).","**Config change/process death** — state restoration.","**Offline/permissions/lifecycle** — real-world conditions.","**Performance/accessibility** — on low-end devices."]},{t:"note",text:"Mobile test strategy must cover: device fragmentation (screens/API levels/OEMs — Firebase Test Lab matrix), config changes/process death (state restoration), offline/flaky networks, permission grant/deny paths, lifecycle (background/foreground), low-end performance, and accessibility — not just business logic."}]},{level:"senior",q:"How much testing is enough — balancing coverage and development speed?",a:[{t:"p",text:"There's no fixed number — balance *risk* against *effort*. Test *thoroughly* where bugs are costly/likely (core logic, edge cases) and *lightly* where they're not (trivial/stable code). Fast, focused tests that give real confidence are worth writing; exhaustive tests of trivial code slow development for little value. The goal is *confidence to ship and refactor*, not a coverage number — enough tests that you trust the suite."},{t:"list",items:["**Balance risk vs effort** — no fixed target.","**Thorough** — costly/likely-bug areas (core logic/edge cases).","**Light** — trivial/stable code.","**Goal** — confidence to ship/refactor, not a coverage %."]},{t:"note",text:"Enough testing = balance risk vs effort (no fixed number): thorough where bugs are costly/likely (core logic/edge cases), light where not (trivial/stable). Fast focused tests that give real confidence are worth it; exhaustive trivial-code tests slow development. The goal is confidence to ship/refactor, not a coverage number."}]},{level:"senior",q:"How do tests serve as living documentation?",a:[{t:"p",text:"Well-written tests *document how code is meant to be used and behave* — a test named `` `withdraw fails when balance is insufficient` `` with clear Arrange-Act-Assert shows the contract better than comments (which drift out of date). Because tests are *executed*, they can't lie — if the behavior changes, the test fails. New developers can read tests to understand a component's behavior and edge cases."},{t:"list",items:["**Document usage/behavior** — how code is meant to work.","**Can't drift** — executed, so always current.","**Better than comments** — which go stale.","**Onboarding** — read tests to understand a component."]},{t:"note",text:"Tests are living documentation — a descriptive test with clear AAA shows how code is meant to be used and behave, better than comments (which drift). Because tests execute, they can't lie: behavior change fails the test. New developers read tests to understand components and edge cases."}]},{level:"junior",q:"What is accessibility testing, and why include it?",a:[{t:"p",text:"Accessibility testing verifies the app works for users with disabilities — screen readers (TalkBack) announce elements correctly (content descriptions), touch targets are large enough (≥48dp), color contrast is sufficient, and the app is navigable without sight/precise touch. Include it because accessibility is a *legal/ethical requirement* and broadens your audience. Tools: Accessibility Scanner, Espresso accessibility checks, Compose semantics assertions."},{t:"list",items:["**Screen reader** — content descriptions, TalkBack navigation.","**Touch targets/contrast** — ≥48dp, sufficient contrast.","**Legal/ethical** — and broadens audience.","**Tools** — Accessibility Scanner, Espresso/Compose checks."]},{t:"note",text:"Accessibility testing verifies the app works for users with disabilities: screen readers (TalkBack, content descriptions), touch targets ≥48dp, sufficient color contrast, navigability without sight/precise touch. Include it — it's a legal/ethical requirement and broadens your audience. Tools: Accessibility Scanner, Espresso/Compose semantics checks."}]},{level:"senior",q:"What is the difference between a test suite, test case, and test scenario?",a:[{t:"p",text:"A *test case* is a single test verifying one behavior (`login_withValidCredentials_succeeds`). A *test suite* is a *collection* of test cases (a class's tests, or a curated set run together, e.g. a 'smoke suite'). A *test scenario* is a higher-level *situation* being tested (a user journey), which may span multiple cases. Suites organize cases; scenarios describe what's being exercised."},{t:"list",items:["**Test case** — one test, one behavior.","**Test suite** — a collection of cases run together.","**Test scenario** — a higher-level situation/journey.","**Suites organize; scenarios describe.**"]},{t:"note",text:"Test case = one test verifying one behavior; test suite = a collection of cases run together (a class's tests, a smoke suite); test scenario = a higher-level situation/journey being tested (may span cases). Suites organize cases; scenarios describe what's exercised."}]},{level:"senior",q:"How do you integrate testing into a CI/CD pipeline?",a:[{t:"p",text:"Run tests automatically on every push/PR: *unit tests* on every commit (fast — block merge on failure), *instrumented/UI tests* on emulators/Firebase Test Lab (slower — maybe on merge to main or nightly), *lint/static analysis*, and *coverage* reporting. Gate merges on passing tests (required checks). Keep the fast feedback loop tight (unit tests in minutes) and run expensive tests less frequently. This enforces quality continuously."},{t:"list",items:["**Unit tests** — every commit/PR; block on failure.","**Instrumented/UI** — Firebase Test Lab; on merge/nightly.","**Lint/static analysis + coverage** — in the pipeline.","**Gate merges** — required passing checks."]},{t:"note",text:"CI/CD testing: run unit tests on every commit/PR (fast — block merge on failure), instrumented/UI tests on emulators/Firebase Test Lab (slower — on merge to main/nightly), plus lint/static analysis and coverage. Gate merges on required passing checks. Keep the fast feedback loop tight; run expensive tests less often."}]},{level:"senior",q:"How does the Testing Trophy differ from the pyramid?",a:[{t:"p",text:"The *Testing Trophy* (Kent C. Dodds) reweights the pyramid to emphasize *integration tests* as the sweet spot — from bottom to top: static analysis (types/lint), unit tests, a *large* integration layer, and few E2E tests. The idea: integration tests give the best *confidence-to-cost* ratio (they test real interactions without full E2E fragility). On Android, this translates to testing ViewModel+repository or feature slices heavily, with focused unit and few UI tests."},{t:"list",items:["**Trophy** — static → unit → *large* integration → few E2E.","**Integration emphasis** — best confidence-to-cost.","**vs pyramid** — pyramid weights unit heaviest.","**Android** — test feature slices (VM+repo) heavily."]},{t:"note",text:"The Testing Trophy (Kent C. Dodds) emphasizes integration tests as the sweet spot: static analysis → unit → large integration → few E2E — integration gives the best confidence-to-cost (real interactions without E2E fragility). vs the pyramid (unit-heaviest). On Android: test feature slices (ViewModel+repo) heavily."}]},{level:"junior",q:"What is the difference between a test failing and a test erroring?",a:[{t:"p",text:"A test *failure* means an *assertion* didn't hold — the code ran but produced the wrong result (`assertEquals` mismatch). A test *error* means an *unexpected exception* was thrown (a `NullPointerException`, a setup crash) — the test couldn't even complete its checks. Failures point to a behavior bug; errors often point to a test-setup problem or a crash. Both fail the build, but they indicate different things."},{t:"list",items:["**Failure** — an assertion didn't hold (wrong result).","**Error** — an unexpected exception/crash.","**Failure → behavior bug** — the code is wrong.","**Error → setup/crash** — often a test problem."]},{t:"note",text:"A failure = an assertion didn't hold (code ran, wrong result — a behavior bug). An error = an unexpected exception (NPE, setup crash — the test couldn't complete, often a setup problem or a real crash). Both fail the build but indicate different things."}]},{level:"senior",q:"How do you test error handling and edge cases systematically?",a:[{t:"p",text:"Don't just test the happy path — enumerate the *failure modes* and *boundaries* for each unit: null/empty inputs, boundary values (0, max, off-by-one), error responses (network failure, 4xx/5xx), concurrent access, and unexpected states. Techniques: *equivalence partitioning* (group inputs that behave the same, test one per group) and *boundary value analysis* (test at and around edges). Systematically covering these catches the bugs that happy-path tests miss."},{t:"list",items:["**Enumerate failure modes** — null/empty, errors, concurrency.","**Boundary values** — 0, max, off-by-one.","**Equivalence partitioning** — one test per behavior group.","**Not just happy path** — where real bugs hide."]},{t:"note",text:"Test edge cases systematically: enumerate failure modes (null/empty inputs, boundary values, error responses, concurrency, unexpected states) per unit. Use equivalence partitioning (one test per behavior group) and boundary value analysis (at/around edges). Happy-path-only tests miss the bugs that live at the boundaries."}]}],IC={id:"strategy-pyramid",title:"Testing Strategy & the Pyramid",tagline:"Why test (confidence to change), the testing pyramid, why the shape matters, local vs instrumented tests, and how architecture drives testability.",tags:["Testing","Test Pyramid","Unit Tests","Strategy","Testability"],content:CC,qa:TC},MC=[{heading:"Anatomy of a unit test",blocks:[{t:"p",text:"A **unit test** verifies one small piece of code (a function, a class) in isolation. On Android/Kotlin you write them with **JUnit** (the test framework — usually JUnit4 or JUnit5), and a good test follows the **Arrange–Act–Assert** (AAA), or Given–When–Then, structure: set up the inputs and dependencies, perform the action, and assert the expected outcome."},{t:"code",title:"A well-structured unit test",code:`class DiscountCalculatorTest {
    @Test
    fun premiumUser_gets20PercentOff() {
        // Arrange (Given) — set up inputs/dependencies
        val calculator = DiscountCalculator()
        val user = User(isPremium = true)

        // Act (When) — perform the action under test
        val price = calculator.finalPrice(basePrice = 100.0, user = user)

        // Assert (Then) — verify the expected outcome
        assertEquals(80.0, price, 0.001)
    }
}`},{t:"list",items:["**`@Test`** marks a test method; JUnit runs each independently. **`@Before`/`@After`** run setup/teardown before/after each test (JUnit5: `@BeforeEach`/`@AfterEach`).","**AAA structure** keeps tests readable: clear input setup, one action, focused assertions. Each test should verify *one* behavior — a descriptive name (`premiumUser_gets20PercentOff`) states what it checks.","**Assertions** — `assertEquals`, `assertTrue`, `assertNull`, `assertThrows`, etc. Libraries like **Truth** (Google) or **AssertK** give fluent, readable assertions (`assertThat(price).isEqualTo(80.0)`) with better failure messages.","**Tests should be FIRST**: Fast, Isolated (independent of each other and external state), Repeatable (same result every run), Self-validating (pass/fail, no manual checking), Timely (written close to the code)."]}]},{heading:"Test doubles — replacing real dependencies",blocks:[{t:"p",text:"To test a class in isolation, you replace its real dependencies with **test doubles** — stand-in objects you control. There are several kinds (the general term is 'test double'), but the two that matter most are **fakes** and **mocks**:"},{t:"table",headers:["Double","What it is"],rows:[["Dummy","a placeholder passed but never used"],["Stub","returns hardcoded/canned responses to calls"],["Fake","a real, working (but simplified) implementation — e.g. an in-memory repository"],["Mock","a double you set expectations on and verify interactions with (was this method called?)"],["Spy","wraps a real object, records interactions (partial mock)"]]},{t:"list",items:["**Fake** — a genuine, lightweight implementation of the same interface, e.g. a `FakeUserRepository` backed by an in-memory `MutableList` instead of a real database. It *behaves* like the real thing (you can add, query, etc.), just simpler and faster. Tests use it like the real dependency.","**Mock** — a generated object where you *program* what its methods return and *verify* how it was called. Created with a mocking library (MockK, Mockito). You say 'when `getUser(42)` is called, return this' and later assert 'verify `getUser` was called once'.","The distinction — fakes vs mocks — is one of the most-asked testing questions and shapes how you write tests."]}]},{heading:"Fakes vs mocks — the key distinction",blocks:[{t:"code",title:"The same test, faked vs mocked",code:`// FAKE — a real in-memory implementation
class FakeUserRepository : UserRepository {
    private val users = mutableMapOf<String, User>()
    fun seed(user: User) { users[user.id] = user }
    override suspend fun getUser(id: String) = users[id] ?: error("not found")
}

@Test fun withFake() = runTest {
    val repo = FakeUserRepository().apply { seed(testUser) }
    val vm = UserViewModel(repo)
    vm.load("42")
    assertEquals(testUser, vm.state.value.user)   // assert STATE (outcome)
}

// MOCK — program returns, verify interactions (MockK)
@Test fun withMock() = runTest {
    val repo = mockk<UserRepository>()
    coEvery { repo.getUser("42") } returns testUser   // program the return
    val vm = UserViewModel(repo)
    vm.load("42")
    coVerify { repo.getUser("42") }                    // verify the INTERACTION
}`},{t:"list",items:["**Fakes test *state/behavior*** — you assert the *outcome* ('the ViewModel's state contains the user'). They read like real usage, don't break when you refactor internals, and one fake serves many tests. **Prefer fakes** for your own repositories/data sources.","**Mocks test *interactions*** — you verify *how* collaborators were called ('getUser was called once with 42'). Useful when the interaction *is* the behavior (verifying an analytics event fired, a callback was invoked) or for dependencies you can't easily fake.","**Why fakes are usually preferred**: mock-heavy tests are *brittle* — they're coupled to the exact method calls, so refactoring the implementation (even without changing behavior) breaks them. They can also 'pass' while testing nothing real (you programmed the return, so you're partly testing your own mock setup). Fakes verify actual behavior and survive refactors.","**When mocks win**: verifying a side effect happened (an event was logged, a method was called) where there's no observable state to assert; or replacing an awkward-to-fake external dependency. Modern guidance (Google) leans toward *fakes for your own types, mocks sparingly for interactions/side effects*."]}]},{heading:"MockK — the Kotlin mocking library",blocks:[{t:"p",text:"**MockK** is the Kotlin-first mocking library (Mockito is the older Java one, usable via mockito-kotlin). MockK understands Kotlin — coroutines (`coEvery`/`coVerify` for suspend functions), final classes (Kotlin classes are final by default, which trips up Mockito), objects, and extension functions."},{t:"code",title:"MockK essentials",code:`val service = mockk<PaymentService>()

every { service.isAvailable() } returns true          // stub a return
coEvery { service.charge(any()) } returns Result.success()  // suspend fn
every { service.log(any()) } just Runs                // stub a Unit function

// ... run the code under test ...

verify { service.isAvailable() }                       // verify called
coVerify(exactly = 1) { service.charge(100) }          // verify with count/args
verify(exactly = 0) { service.refund(any()) }          // verify NOT called

val slot = slot<Payment>()                             // capture arguments
verify { service.charge(capture(slot)) }
assertEquals(100, slot.captured.amount)`},{t:"list",items:["**`mockk<T>()`** creates a mock; **`every { } returns`** stubs a return; **`coEvery`** for suspend functions. **`just Runs`** for `Unit`-returning functions.","**`verify { }`** / **`coVerify { }`** assert a call happened (with `exactly = n`, `atLeast`, argument matchers like `any()`).","**`slot` / `capture`** capture the actual arguments passed, to assert on them.","**`relaxed = true`** creates a mock that returns sensible defaults without stubbing every call (handy but can hide missing stubs).","**`spyk`** wraps a real object (a spy) to override some methods while keeping others real."]},{t:"note",text:"Unit testing essentials: JUnit + Arrange-Act-Assert (one behavior per test, descriptive names, FIRST properties). Test doubles replace dependencies — fakes (real simplified implementations, e.g. in-memory repo — assert STATE/outcome, prefer these) vs mocks (programmed returns + interaction verification — assert CALLS, use for side effects). Mocks are brittle (coupled to calls, break on refactor); fakes survive refactors and read like real usage. MockK is the Kotlin mocking library (coEvery/coVerify for suspend, handles final classes). Prefer fakes for your own types, mocks sparingly."}]}],PC=[{level:"junior",q:"What is the structure of a good unit test?",a:[{t:"p",text:"**A good unit test follows the Arrange–Act–Assert (AAA), also called Given–When–Then, structure: set up the inputs and dependencies, perform the single action under test, then assert the expected outcome.** This keeps tests readable and focused."},{t:"code",title:"AAA in practice",code:`@Test fun premiumUser_gets20PercentOff() {
    // Arrange
    val calculator = DiscountCalculator()
    val user = User(isPremium = true)
    // Act
    val price = calculator.finalPrice(100.0, user)
    // Assert
    assertEquals(80.0, price, 0.001)
}`},{t:"list",items:["**One behavior per test** — each test verifies a single thing, with a descriptive name that states what it checks (`premiumUser_gets20PercentOff`). If a test needs multiple unrelated assertions, it's probably testing too much.","**FIRST properties** — Fast (milliseconds), Isolated (independent of other tests and external state), Repeatable (same result every run), Self-validating (clearly pass/fail), Timely (written close to the code).","**Clear assertions** — use `assertEquals`, `assertTrue`, etc., or fluent libraries like Truth/AssertK for readable failure messages."]},{t:"p",text:"The AAA structure matters because it makes tests *documentation* — a reader immediately sees the setup, the action, and the expected result. Tests that mix these (asserting mid-setup, multiple actions) are hard to follow and often brittle. Keeping each test small, focused, and clearly structured is what makes a test suite maintainable and trustworthy."}]},{level:"junior",q:"What is a test double? Name the main kinds.",a:[{t:"p",text:"**A test double is a stand-in object that replaces a real dependency in a test, so you can test a class in isolation with controlled behavior.** 'Test double' is the general term (like a stunt double); there are several specific kinds:"},{t:"list",items:["**Dummy** — a placeholder object passed to satisfy a parameter but never actually used.","**Stub** — returns hardcoded, canned responses to method calls (no logic, just 'return this').","**Fake** — a real, working but simplified implementation, like an in-memory repository backed by a `MutableList` instead of a real database. It genuinely *behaves* like the real thing.","**Mock** — a double you program with expected returns and then *verify* how it was called (was this method invoked, with what arguments, how many times).","**Spy** — wraps a real object, recording interactions while delegating to the real implementation (a partial mock)."]},{t:"p",text:"In practice, the two you use most and that matter most in interviews are **fakes** and **mocks**. The reason for doubles at all is *isolation* — to unit-test a ViewModel, you don't want a real network call or database, so you replace the repository with a fake (in-memory) or a mock (programmed). Which you choose (fake vs mock) is a meaningful decision that affects how robust your tests are — fakes test outcomes and survive refactors, mocks test interactions and can be brittle."}]},{level:"junior",q:"What is MockK and why use it over Mockito on Android?",a:[{t:"p",text:"**MockK is a Kotlin-first mocking library — it's designed for Kotlin and handles Kotlin's features natively, whereas Mockito is the older Java library that struggles with some Kotlin constructs.** You use MockK to create mocks, program their return values, and verify how they were called."},{t:"list",items:["**Handles Kotlin's final-by-default classes** — Kotlin classes are `final` unless marked `open`, and Mockito historically couldn't mock final classes without an extra plugin. MockK mocks them out of the box.","**Coroutine support** — `coEvery { }` and `coVerify { }` for stubbing and verifying `suspend` functions, which is essential since modern Android code is full of them.","**Kotlin idioms** — it understands objects, extension functions, and provides a clean Kotlin DSL (`every { } returns`, `just Runs` for Unit functions, `slot`/`capture` for arguments)."]},{t:"code",title:"MockK basics",code:`val repo = mockk<UserRepository>()
coEvery { repo.getUser("42") } returns testUser   // stub a suspend fn
// ... run code ...
coVerify(exactly = 1) { repo.getUser("42") }       // verify it was called`},{t:"p",text:"So on a Kotlin Android project, MockK is the natural choice — it speaks Kotlin fluently (suspend functions, final classes, objects), which Mockito needs workarounds for. (Mockito is still usable via the mockito-kotlin wrapper, and some teams use it for legacy reasons.) That said, a common piece of guidance is to *prefer fakes over mocks* for your own types and use MockK's mocking mainly for verifying interactions/side effects or for dependencies that are awkward to fake — so you'd reach for MockK selectively rather than mocking everything."}]},{level:"senior",q:"What is the difference between a fake and a mock, and which should you prefer?",a:[{t:"p",text:"**A fake is a real, working (but simplified) implementation of a dependency — you use it and assert on the *outcome/state* it produces. A mock is a programmed object where you specify return values and *verify the interactions* (which methods were called, how). The fundamental difference is *state-based testing* (fakes) vs *interaction-based testing* (mocks) — and for most cases, fakes are preferred.**"},{t:"code",title:"The distinction in code",code:`// FAKE — real in-memory implementation; assert the OUTCOME
class FakeRepo : Repo { private val m = mutableMapOf<String,User>()
    fun seed(u: User){ m[u.id]=u }; override suspend fun get(id:String)=m[id]!! }
val vm = ViewModel(FakeRepo().apply{ seed(user) })
vm.load("1"); assertEquals(user, vm.state.value.user)   // assert state

// MOCK — programmed; verify the CALL
val repo = mockk<Repo>(); coEvery { repo.get("1") } returns user
val vm2 = ViewModel(repo)
vm2.load("1"); coVerify { repo.get("1") }               // assert interaction`},{t:"list",items:["**Fakes test behavior/state and are robust**: you assert what the code *produced* (the ViewModel's state contains the user), not *how* it got there. This means the test survives refactoring — if you change the ViewModel's internal calls but the outcome is the same, the fake test still passes. Fakes also read like real usage and one fake serves many tests.","**Mocks test interactions and are brittle**: you assert *specific method calls*, so the test is coupled to the implementation's exact call pattern. Refactor the implementation (even without changing behavior) and mock verifications break — a *false failure* that wastes time. Worse, a mock-heavy test can 'pass' while verifying almost nothing real, because you programmed both the input (stub) and check the output against your own setup — you can end up testing your mock configuration rather than the code.","**When mocks are the right tool**: when the *interaction itself is the behavior you care about* and there's no observable state to assert — verifying that an analytics event was logged, that a callback was invoked, that a fire-and-forget side effect happened. Also for replacing an awkward-to-fake external dependency where writing a full fake isn't worth it. In these cases, verifying the call *is* the meaningful assertion.","**The modern guidance (Google's testing docs)**: prefer *fakes for your own types* (repositories, data sources — write a simple in-memory fake, ideally provided by the owner of the interface), and use *mocks sparingly* for interaction verification and side effects. Avoid mocking types you don't own (their behavior can change, and your mock encodes assumptions about them)."]},{t:"p",text:"**The senior framing**: the fake-vs-mock choice is really about *what you're asserting* — outcomes or interactions. Outcome-based tests (fakes) are more valuable because they verify *what the code does* in a way that's decoupled from *how*, so they catch real behavior changes and survive refactors — exactly the properties that make a test suite trustworthy and maintainable. Interaction-based tests (mocks) are appropriate specifically when the interaction is the observable behavior (side effects with no state). Over-reliance on mocks is a common testing anti-pattern that produces brittle suites coupled to implementation details, so the mature default is 'fakes for state, mocks for verifying side effects', with a bias toward fakes."}]},{level:"senior",q:"What makes tests brittle or flaky, and how do you avoid it?",a:[{t:"p",text:"**A *brittle* test breaks when you change implementation details even though behavior is correct (a false failure that generates maintenance churn); a *flaky* test passes or fails non-deterministically across runs without code changes (eroding trust in the suite). Both undermine the value of testing, and both have identifiable causes and fixes.**"},{t:"list",items:["**Brittleness cause — testing implementation, not behavior**: over-mocking (verifying exact method calls) couples tests to *how* code works, so any refactor breaks them. Fix: prefer state/outcome assertions (fakes) over interaction verification; test through the public API, not internals; don't assert on private details.","**Brittleness cause — UI tests coupled to presentation**: asserting on exact text/positions breaks on cosmetic changes. Fix: use stable test tags/semantics, assert on behavior not layout, keep UI tests few and focused on critical flows.","**Flakiness cause — real time and async**: tests that use real delays, real threads, or wall-clock time race non-deterministically. Fix: use *virtual time* (`runTest` with `TestDispatcher`) so coroutine delays are controlled, and *inject dispatchers* so tests control threading. Never use real `Thread.sleep`/`delay` in tests to 'wait' for async work.","**Flakiness cause — shared/external state**: tests depending on shared mutable state, execution order, real network, real files, or a real database that persists between tests. Fix: make each test *isolated* — fresh state per test (`@Before` setup), fakes instead of real external resources, no dependence on other tests' side effects. Reset singletons/global state.","**Flakiness cause — UI timing**: animations, unsynchronized async in UI tests. Fix: rely on the test framework's idle synchronization (Compose auto-syncs, Espresso idling resources), control the clock for animations, disable animations on test devices, use `waitUntil` for genuine async.","**Flakiness cause — hidden nondeterminism**: relying on `Set`/`Map` iteration order, random values, current date/time. Fix: inject a `Clock`/random source you control; assert on sorted/normalized data; avoid order-dependent assertions on unordered collections."]},{t:"list",items:["**The cost of ignoring it**: a brittle suite generates constant false-failure maintenance that isn't catching real bugs; a flaky suite trains the team to *ignore failures* ('just re-run it'), at which point the tests protect nothing — a flaky test is arguably worse than no test, because it costs time and provides false confidence. So flakiness must be treated as a real bug to fix (or the test quarantined), not tolerated.","**The unifying principle**: test *behavior through public interfaces* with *controlled, isolated, deterministic* inputs — inject time and dispatchers (control async), inject dependencies (use fakes, avoid real external resources), keep each test independent with fresh state, and assert on outcomes rather than implementation details or presentation. Do that, and tests are neither brittle nor flaky."]},{t:"p",text:"**The senior framing**: brittleness and flakiness are the two ways a test suite loses its value — one by breaking on non-changes (maintenance burden, no real signal), the other by failing randomly (destroys trust). The root causes are *testing implementation instead of behavior* (brittleness) and *uncontrolled nondeterminism — real time, threads, shared state* (flakiness). The fixes are the same disciplines that define good testing: assert outcomes not internals, inject and control time/dispatchers/dependencies, and isolate every test. Being able to diagnose *why* a test is brittle or flaky and name the specific fix (virtual time, injected dispatchers, fakes, isolation, behavior-based assertions) — and recognizing that a flaky test is a bug to fix, not tolerate — is exactly the testing maturity these questions probe."}]},{level:"junior",q:"What is JUnit, and what's the difference between JUnit 4 and 5?",a:[{t:"p",text:"JUnit is the standard Java/Kotlin unit-testing framework — you annotate methods `@Test` and run assertions. *JUnit 4* uses `@Test`, `@Before`, `@Rule`, and `@RunWith`. *JUnit 5* (Jupiter) modernizes it: `@BeforeEach`/`@AfterEach`, `@Nested` classes, `@ParameterizedTest`, extensions (`@ExtendWith`) instead of rules, and better Kotlin support. Android historically used JUnit 4 (much tooling assumes it), though JUnit 5 is usable."},{t:"list",items:["**JUnit 4** — `@Test`/`@Before`/`@Rule`/`@RunWith`.","**JUnit 5** — `@BeforeEach`, `@Nested`, `@ParameterizedTest`, extensions.","**Android** — JUnit 4 is the default; JUnit 5 needs extra setup.","**Assertions** — `assertEquals` etc., or a library (Truth/AssertJ)."]},{t:"note",text:"JUnit is the standard unit-test framework (@Test + assertions). JUnit 4: @Test/@Before/@Rule/@RunWith. JUnit 5 (Jupiter): @BeforeEach, @Nested, @ParameterizedTest, extensions (@ExtendWith) over rules, better Kotlin support. Android defaults to JUnit 4 (tooling assumes it); JUnit 5 is usable with setup."}]},{level:"junior",q:"What do @Before, @After, and setup/teardown do?",a:[{t:"p",text:"`@Before` runs *before each test* — set up the subject and fresh test doubles so tests are independent. `@After` runs *after each test* — clean up (close resources, reset globals like `Dispatchers.resetMain`). `@BeforeClass`/`@AfterClass` run *once* for the class (expensive shared setup). Fresh per-test setup prevents tests from affecting each other (a key to reliability)."},{t:"code",title:"Setup/teardown",code:`@Before fun setup() { repo = FakeRepo(); vm = MyViewModel(repo) }
@After fun tearDown() { Dispatchers.resetMain() }`},{t:"list",items:["**`@Before`** — per-test setup (fresh subject/doubles).","**`@After`** — per-test cleanup (resources/globals).","**`@BeforeClass`/`@AfterClass`** — once per class (expensive shared).","**Independence** — fresh setup prevents cross-test contamination."]},{t:"note",text:"@Before runs before each test (fresh subject/doubles → independent tests); @After after each (cleanup — close resources, Dispatchers.resetMain). @BeforeClass/@AfterClass run once per class (expensive shared setup). Fresh per-test setup prevents tests affecting each other."}]},{level:"junior",q:"What is a JUnit @Rule / TestRule?",a:[{t:"p",text:"A `@Rule` is a reusable piece of test setup/teardown logic that wraps test execution — like `InstantTaskExecutorRule` (runs LiveData synchronously), `MainDispatcherRule` (swaps `Dispatchers.Main`), or `TemporaryFolder`. It factors out common cross-test boilerplate into a declarative `@get:Rule` field. JUnit 5 replaces rules with *extensions* (`@ExtendWith`)."},{t:"code",title:"@Rule",code:`@get:Rule val instantTaskExecutorRule = InstantTaskExecutorRule()   // LiveData sync
@get:Rule val mainDispatcherRule = MainDispatcherRule()             // swap Main`},{t:"list",items:["**`@Rule`** — reusable setup/teardown wrapping tests.","**Examples** — InstantTaskExecutorRule, MainDispatcherRule, TemporaryFolder.","**`@get:Rule`** — in Kotlin (property with a getter).","**JUnit 5** — extensions (`@ExtendWith`) replace rules."]},{t:"note",text:"A @Rule wraps test execution with reusable setup/teardown (InstantTaskExecutorRule for sync LiveData, MainDispatcherRule to swap Dispatchers.Main, TemporaryFolder). In Kotlin, @get:Rule. It factors out cross-test boilerplate. JUnit 5 replaces rules with extensions (@ExtendWith)."}]},{level:"junior",q:"What assertion library should you use, and why?",a:[{t:"p",text:"Beyond JUnit's `assertEquals`, use a *fluent* assertion library like *Truth* (Google) or *AssertJ* — they read naturally (`assertThat(result).isEqualTo(expected)`, `assertThat(list).containsExactly(...)`) and give *better failure messages* than bare JUnit asserts. Kotlin also has `kotlin.test` assertions. Fluent assertions make tests more readable and debuggable."},{t:"code",title:"Fluent assertions",code:`assertThat(user.name).isEqualTo("Sam")            // Truth
assertThat(items).containsExactly(a, b, c).inOrder()
assertThat(result).isInstanceOf(Error::class.java)`},{t:"list",items:["**Truth/AssertJ** — fluent, readable assertions.","**Better messages** — clearer failures than bare JUnit.","**Rich matchers** — collections, types, null, ordering.","**`kotlin.test`** — Kotlin assertions (assertEquals/assertFailsWith)."]},{t:"note",text:"Use a fluent assertion library — Truth (Google) or AssertJ — for readable assertions (assertThat(x).isEqualTo(...), containsExactly) with better failure messages than bare JUnit asserts. kotlin.test provides Kotlin assertions. Fluent assertions improve readability and debuggability."}]},{level:"senior",q:"What is parameterized testing?",a:[{t:"p",text:"Parameterized tests run the *same test logic* with *multiple input sets* — instead of copy-pasting a test for each case, you provide a list of inputs/expected outputs and the framework runs the test once per case. JUnit 4 uses `@RunWith(Parameterized::class)`; JUnit 5 uses `@ParameterizedTest` with `@ValueSource`/`@CsvSource`/`@MethodSource`. It's great for testing many edge cases of a pure function concisely."},{t:"code",title:"Parameterized (JUnit 5)",code:`@ParameterizedTest
@CsvSource("0, false", "17, false", "18, true", "100, true")
fun isAdult(age: Int, expected: Boolean) {
    assertThat(isAdult(age)).isEqualTo(expected)
}`},{t:"list",items:["**Same logic, many inputs** — one test per case.","**JUnit 4** — `@RunWith(Parameterized)`.","**JUnit 5** — `@ParameterizedTest` + sources.","**Edge cases** — concise coverage of pure functions."]},{t:"note",text:"Parameterized tests run the same logic with multiple input sets (one run per case) instead of duplicating tests: JUnit 4 @RunWith(Parameterized), JUnit 5 @ParameterizedTest + @ValueSource/@CsvSource/@MethodSource. Great for covering many edge cases of a pure function concisely."}]},{level:"junior",q:"How do you test that a function throws an exception?",a:[{t:"p",text:"Use `assertFailsWith<T> { }` (kotlin.test) or `assertThrows<T> { }` (JUnit 5) — it runs the block and asserts it throws the expected exception type, returning the exception so you can also assert its message. This verifies error paths (invalid input → `IllegalArgumentException`). Avoid try/catch-with-fail boilerplate."},{t:"code",title:"assertFailsWith",code:`val ex = assertFailsWith<IllegalArgumentException> { validate("") }
assertThat(ex.message).contains("must not be blank")`},{t:"list",items:["**`assertFailsWith<T> { }`** — kotlin.test; asserts the throw.","**`assertThrows<T> { }`** — JUnit 5.","**Returns the exception** — assert its message/details.","**Error paths** — verify invalid input throws."]},{t:"note",text:"Use assertFailsWith<T> { } (kotlin.test) or assertThrows<T> { } (JUnit 5) — runs the block, asserts it throws the expected type, and returns the exception (assert its message). Cleaner than try/catch-with-fail. For verifying error paths (invalid input → IllegalArgumentException)."}]},{level:"senior",q:"What is a stub, and how does it differ from a mock?",a:[{t:"p",text:"A *stub* provides *canned answers* to calls (returns preset data) — it's about *state* (feeding inputs). A *mock* additionally *records and verifies interactions* — it's about *behavior* (was this method called with these args?). Both are test doubles: a stub just returns values you configured; a mock lets you assert *how* the subject used its dependency. MockK can act as either."},{t:"list",items:["**Stub** — canned return values; state-based.","**Mock** — records + verifies calls; behavior-based.","**Stub** — 'when asked, return X'.","**Mock** — 'assert it was called with Y'."]},{t:"note",text:"A stub provides canned answers (returns preset data — state-based, 'when asked, return X'). A mock additionally records and verifies interactions (behavior-based, 'assert it was called with Y'). Both are test doubles; MockK can act as either (every for stubbing, verify for interactions)."}]},{level:"senior",q:"What is a spy, and when do you use one?",a:[{t:"p",text:"A *spy* wraps a *real object* — calls go to the real implementation *unless* you stub specific methods, and you can *verify* interactions. Use it to test a real object while overriding one troublesome method, or to verify calls on a mostly-real collaborator. Spies are a code smell if overused (they mix real and fake behavior); prefer fakes/mocks for clarity."},{t:"code",title:"Spy (MockK)",code:`val service = spyk(RealService())
every { service.expensiveCall() } returns cached   // override one method
service.doWork()
verify { service.log(any()) }                       // verify a real call happened`},{t:"list",items:["**Spy** — wraps a real object; real calls unless stubbed.","**Override selectively** — stub specific methods.","**Verify** — check interactions on a real object.","**Use sparingly** — mixing real/fake is a smell."]},{t:"note",text:"A spy wraps a real object — calls hit the real implementation unless you stub specific methods, and you can verify interactions (spyk in MockK). Use to test a real object while overriding one method. Overusing spies (mixing real and fake) is a smell — prefer fakes/mocks for clarity."}]},{level:"senior",q:"How do you verify interactions with verify() in MockK?",a:[{t:"p",text:"`verify { mock.method(args) }` asserts the method was called; `verify(exactly = n)`, `verify(atLeast = ...)`, `verifyOrder`, and `confirmVerified` add precision. For suspend functions use `coVerify`. But *behavior verification* (checking calls) can make tests brittle — prefer verifying *outcomes* (state) over *interactions* unless the interaction itself is the contract (e.g. analytics was logged)."},{t:"code",title:"verify",code:`verify(exactly = 1) { analytics.log("purchase") }
coVerify { repo.save(user) }                       // suspend
verify(exactly = 0) { api.call() }                  // asserts NOT called`},{t:"list",items:["**`verify { }`** — assert a call happened.","**`exactly`/`atLeast`/`verifyOrder`** — precision.","**`coVerify`** — for suspend functions.","**Prefer state** — behavior verification is brittle; verify outcomes."]},{t:"note",text:"verify { mock.method(args) } asserts a call (with exactly/atLeast/verifyOrder/confirmVerified; coVerify for suspend). But behavior verification is brittle — prefer verifying outcomes (state) over interactions, unless the interaction IS the contract (analytics logged, a specific API called)."}]},{level:"senior",q:"What is a relaxed mock, and when is it useful?",a:[{t:"p",text:"By default MockK requires you to *stub every method* the subject calls (or it throws for unstubbed calls). A *relaxed* mock (`mockk(relaxed = true)`/`relaxedUnitFun`) returns *sensible defaults* (0, empty, null, or a nested mock) for unstubbed methods — so you only stub what matters. Useful when a dependency has many methods but you only care about a few; the trade-off is it can hide unexpected calls."},{t:"code",title:"Relaxed mock",code:`val logger = mockk<Logger>(relaxed = true)   // unstubbed methods return defaults
val repo = mockk<Repo>(relaxUnitFun = true)  // Unit-returning methods relaxed`},{t:"list",items:["**Default MockK** — throws on unstubbed calls.","**Relaxed** — returns sensible defaults for unstubbed methods.","**Only stub what matters** — for many-method dependencies.","**Trade-off** — can hide unexpected calls."]},{t:"note",text:"By default MockK throws on unstubbed calls; a relaxed mock (mockk(relaxed = true)/relaxUnitFun) returns sensible defaults (0/empty/null/nested mock) for unstubbed methods, so you stub only what matters — useful for many-method dependencies. Trade-off: it can hide unexpected calls."}]},{level:"senior",q:"How do you mock suspend functions and coroutines with MockK?",a:[{t:"p",text:"Use the `co`-prefixed functions: `coEvery { mock.suspendFn() } returns value` to stub, and `coVerify { mock.suspendFn() }` to verify. These handle the suspend context. Run the test in `runTest` so the coroutines execute. MockK's coroutine support makes testing suspend-heavy code (repositories, use cases) straightforward."},{t:"code",title:"coEvery / coVerify",code:`coEvery { repo.getUser("1") } returns fakeUser
val vm = MyViewModel(repo)
vm.load(); advanceUntilIdle()
coVerify(exactly = 1) { repo.getUser("1") }`},{t:"list",items:["**`coEvery { }`** — stub a suspend function.","**`coVerify { }`** — verify a suspend call.","**`runTest`** — execute the coroutines.","**Suspend-heavy code** — repositories, use cases."]},{t:"note",text:"MockK coroutine support: coEvery { mock.suspendFn() } returns value (stub), coVerify { } (verify) — handling the suspend context. Run tests in runTest so coroutines execute. Makes testing suspend-heavy code (repositories/use cases) straightforward. Prefer fakes for repositories where practical."}]},{level:"senior",q:"Should you test private methods, and how?",a:[{t:"p",text:"Generally *don't test private methods directly* — test them *through the public API* that uses them (they're implementation details; testing them couples tests to internals and makes refactoring hard). If a private method has complex logic worth testing in isolation, that's a signal to *extract it* into its own class/function with a public interface. Reflection to test privates is a smell."},{t:"list",items:["**Test via the public API** — privates are implementation details.","**Complex private logic** — extract it to a testable class/function.","**Don't use reflection** — to test privates (a smell).","**Refactor-friendly** — testing behavior, not internals."]},{t:"note",text:"Don't test private methods directly — test them through the public API that uses them (they're implementation details; testing internals couples tests and blocks refactoring). If private logic is complex enough to test in isolation, extract it into its own testable class/function. Reflection to test privates is a smell."}]},{level:"senior",q:"What is code coverage, and is 100% the goal?",a:[{t:"p",text:"Code coverage measures what percentage of code your tests *execute* (line/branch). It's a useful *indicator* of untested areas, but *not a goal in itself* — 100% coverage doesn't mean the tests are *good* (you can execute code without asserting meaningful behavior), and chasing it wastes effort on trivial code. Aim for *high coverage of important logic* (business rules, edge cases) and use coverage to find *gaps*, not as a target."},{t:"list",items:["**Coverage** — % of code executed by tests (line/branch).","**Not a goal** — 100% ≠ good tests (execution isn't assertion).","**Find gaps** — use it to spot untested important logic.","**Focus** — business rules/edge cases over trivial code."]},{t:"note",text:"Code coverage = % of code executed by tests — a useful indicator of gaps, not a goal. 100% doesn't mean good tests (executing ≠ asserting behavior), and chasing it wastes effort on trivial code. Aim for high coverage of important logic (business rules/edge cases); use coverage to find gaps, not as a target."}]},{level:"junior",q:"What is Test-Driven Development (TDD)?",a:[{t:"p",text:"TDD is writing the *test first*, then the code to make it pass, in a *red-green-refactor* cycle: (1) *Red* — write a failing test for the next small behavior; (2) *Green* — write the minimal code to pass it; (3) *Refactor* — clean up with the test as a safety net. It drives simple designs, ensures testability, and gives fast feedback. You don't have to do strict TDD, but writing tests early improves design."},{t:"list",items:["**Red** — write a failing test first.","**Green** — minimal code to pass.","**Refactor** — clean up safely.","**Benefits** — testable design, fast feedback, simple code."]},{t:"note",text:"TDD: write the test first, then code to pass it — red (failing test for the next behavior), green (minimal passing code), refactor (clean up with the test as a net). It drives testable, simple designs and fast feedback. Strict TDD is optional, but writing tests early improves design."}]},{level:"senior",q:"How do you make code testable?",a:[{t:"p",text:"Testable code: use *dependency injection* (inject collaborators so you can pass fakes), favor *pure functions* (deterministic, no side effects — trivial to test), depend on *interfaces/abstractions* (swap implementations), inject sources of non-determinism (a `Clock`, dispatchers, random) so you control them, and keep functions *small and single-responsibility*. Hard-to-test code (static singletons, hidden dependencies, side effects) signals a design problem."},{t:"list",items:["**Dependency injection** — pass fakes for collaborators.","**Pure functions** — deterministic, no side effects.","**Interfaces** — swap implementations.","**Inject non-determinism** — Clock/dispatchers/random."]},{t:"note",text:"Testable code: dependency injection (pass fakes), pure functions (deterministic, no side effects), depend on interfaces (swap impls), inject non-determinism (Clock/dispatchers/random), small single-responsibility functions. Hard-to-test code (static singletons, hidden deps, side effects) signals a design problem — testability drives good design."}]},{level:"senior",q:"What is the difference between state and behavior verification?",a:[{t:"p",text:"*State verification* checks the *result*/*state* after the action (assert the returned value or the object's new state) — robust, refactor-friendly. *Behavior verification* checks *how* the subject interacted with dependencies (verify a method was called) — necessary when the interaction *is* the contract (analytics logged, an email sent), but brittle when overused (couples tests to implementation). Prefer state verification; use behavior verification only for genuine interaction contracts."},{t:"list",items:["**State** — assert the result/state; robust.","**Behavior** — verify interactions; for interaction contracts.","**Prefer state** — refactor-friendly.","**Behavior when** — the call itself is the observable outcome."]},{t:"note",text:"State verification asserts the result/state after the action (robust, refactor-friendly); behavior verification checks how the subject interacted with dependencies (verify calls — brittle when overused). Prefer state; use behavior verification only when the interaction IS the contract (analytics logged, email sent)."}]},{level:"junior",q:"What makes a good test name?",a:[{t:"p",text:"A good test name describes *what's being tested and the expected outcome* under a condition — readable as a sentence. Common patterns: `methodName_condition_expectedResult` (`login_withInvalidPassword_returnsError`) or backtick Kotlin names (`` `login with invalid password returns error` ``). The name should tell you what broke *without reading the test body*, so failures are self-documenting."},{t:"code",title:"Descriptive names",code:"@Test fun `load returns Error when the network fails`() { }\n@Test fun isAdult_ageUnder18_returnsFalse() { }"},{t:"list",items:["**Describe outcome** — what + expected result + condition.","**Patterns** — `method_condition_result` or backtick sentences.","**Self-documenting** — the name explains the failure.","**Readable** — like a spec."]},{t:"note",text:"A good test name describes what's tested + the expected outcome under a condition, readable as a sentence: method_condition_result (login_withInvalidPassword_returnsError) or Kotlin backtick names. The name should explain what broke without reading the body — self-documenting failures."}]},{level:"senior",q:"How do you test time-dependent code?",a:[{t:"p",text:"Don't call `System.currentTimeMillis()`/`Instant.now()` directly — *inject a `Clock`* (or a time provider) so tests can supply a *fixed* time. Then you can test 'is this token expired?' or 'is it after 5 PM?' deterministically. For coroutine delays, `runTest`'s virtual clock handles them. Injecting the source of time is the key to testing time-dependent logic."},{t:"code",title:"Inject a Clock",code:`class Token(val expiresAt: Instant, private val clock: Clock) {
    fun isExpired() = clock.instant().isAfter(expiresAt)
}
// Test: Token(expiresAt, Clock.fixed(now, UTC))`},{t:"list",items:["**Inject a `Clock`** — supply a fixed time in tests.","**Deterministic** — no real-time flakiness.","**`runTest` virtual clock** — for coroutine delays.","**Don't call `now()` directly** — inject the time source."]},{t:"note",text:"Inject a Clock (or time provider) instead of calling System.currentTimeMillis()/Instant.now() directly — tests supply a fixed time (Clock.fixed) for deterministic time-dependent logic (expiry, after-5-PM). runTest's virtual clock handles coroutine delays. Injecting the time source is the key."}]},{level:"senior",q:"How do you make a good fake for a repository?",a:[{t:"p",text:"A fake repository is a *working in-memory implementation* of the repository interface — it stores data in a map/list, returns it, and lets you preload data or simulate errors (a flag to throw). It behaves like the real thing (correct semantics) but without network/DB, making it *reusable* across tests and more *realistic* than a mock. Expose helpers to seed data and control error behavior."},{t:"code",title:"Fake repository",code:`class FakeUserRepository : UserRepository {
    private val users = mutableMapOf<String, User>()
    var shouldThrow = false
    fun seed(user: User) { users[user.id] = user }
    override suspend fun getUser(id: String): User =
        if (shouldThrow) throw IOException() else users[id] ?: error("not found")
}`},{t:"list",items:["**In-memory impl** — of the repository interface.","**Seed helpers** — preload data.","**Simulate errors** — a flag to throw.","**Reusable + realistic** — behaves like the real thing."]},{t:"note",text:"A fake repository is a working in-memory implementation of the interface (data in a map/list, seed helpers, an error flag to simulate failures) — realistic (correct semantics) and reusable across tests, without network/DB. More robust than a mock; the preferred double for repositories."}]},{level:"junior",q:"What is the difference between a unit test and an integration test?",a:[{t:"p",text:"A *unit test* verifies a *single unit* (a class/function) in *isolation*, with dependencies faked/mocked — fast, deterministic, run on the JVM. An *integration test* verifies that *multiple components work together* (a repository with a real DB, or a ViewModel + repository) — slower, more realistic, catches wiring/interaction bugs units miss. You need both: many unit tests + fewer integration tests (the pyramid)."},{t:"list",items:["**Unit** — one unit in isolation; fast, faked deps.","**Integration** — components together; slower, realistic.","**Integration catches** — wiring/interaction bugs.","**Both** — many unit + fewer integration (pyramid)."]},{t:"note",text:"A unit test verifies one class/function in isolation (faked deps — fast, deterministic, JVM). An integration test verifies multiple components together (repository + real DB, ViewModel + repository — slower, realistic, catches wiring bugs). Need both: many unit + fewer integration tests (the pyramid)."}]},{level:"senior",q:"How do you keep unit tests fast and deterministic?",a:[{t:"p",text:"Keep them *pure JVM* (no Android framework — use Robolectric only when needed), avoid *real I/O* (fake network/DB), avoid *real time* (inject Clock, use `runTest`'s virtual clock — never `Thread.sleep`), inject *test dispatchers*, avoid *shared mutable state* between tests (fresh setup), and don't depend on *ordering* or external systems. Fast, deterministic tests run in CI reliably and give quick feedback."},{t:"list",items:["**Pure JVM** — no framework/real I/O.","**No real time** — inject Clock; `runTest` virtual clock; no `Thread.sleep`.","**Test dispatchers** — deterministic coroutines.","**Isolated** — fresh setup, no shared state/ordering."]},{t:"note",text:"Fast/deterministic unit tests: pure JVM (no framework — Robolectric only when needed), no real I/O (fake network/DB), no real time (inject Clock, runTest virtual clock, never Thread.sleep), test dispatchers, isolated (fresh setup, no shared state/ordering). Reliable in CI with quick feedback."}]},{level:"senior",q:"What is mutation testing?",a:[{t:"p",text:"Mutation testing measures test *quality* (not just coverage): a tool (e.g. PIT) introduces small *mutations* (change `>` to `>=`, negate a condition, replace a return) and checks whether your tests *catch* them (a test fails). Surviving mutations reveal weak tests that execute code but don't *assert* meaningfully. It's a stronger quality signal than coverage but slower to run."},{t:"list",items:["**Introduces mutations** — small code changes.","**Tests should catch them** — surviving mutations = weak tests.","**Stronger than coverage** — measures assertion quality.","**Slower** — run periodically, not every build."]},{t:"note",text:"Mutation testing (PIT) introduces small code mutations (> → >=, negate conditions) and checks if tests catch them (fail). Surviving mutations reveal weak tests that execute code without meaningfully asserting. A stronger quality signal than coverage, but slower — run periodically."}]},{level:"junior",q:"How do you organize test files and structure a test class?",a:[{t:"p",text:"Mirror the source structure — `src/test/.../MyViewModelTest.kt` next to the package of `MyViewModel`. Within a test class: a `@Before` setup creating the subject with fakes, then grouped tests (by method or scenario), each following Arrange-Act-Assert. Use `@Nested` classes (JUnit 5) or naming prefixes to group related cases. Keep one behavior per test and descriptive names."},{t:"list",items:["**Mirror source** — `XTest` in the same package under `src/test`.","**`@Before` setup** — subject + fakes.","**Group tests** — by method/scenario (`@Nested` or naming).","**One behavior per test** — AAA + descriptive names."]},{t:"note",text:"Organize tests mirroring the source (XTest in the same package under src/test/androidTest). In a class: @Before setup (subject + fakes), grouped tests (by method/scenario via @Nested or naming), each Arrange-Act-Assert, one behavior per test with descriptive names."}]},{level:"junior",q:"What is the Arrange-Act-Assert (Given-When-Then) structure?",a:[{t:"p",text:"A clear test has three phases: *Arrange* (set up the subject and inputs/test doubles), *Act* (invoke the behavior under test — ideally one action), and *Assert* (verify the outcome). *Given-When-Then* is the same idea in BDD terms. Separating these visually (blank lines/comments) makes tests readable and keeps each test focused on one behavior."},{t:"code",title:"AAA",code:`@Test fun \`load sets Content on success\`() = runTest {
    val vm = MyViewModel(FakeRepo(data))   // Arrange
    vm.load(); advanceUntilIdle()          // Act
    assertThat(vm.state.value).isEqualTo(UiState.Content(data))  // Assert
}`},{t:"list",items:["**Arrange** — set up subject, inputs, doubles.","**Act** — invoke the behavior (one action).","**Assert** — verify the outcome.","**Given-When-Then** — the BDD phrasing of the same."]},{t:"note",text:"Arrange-Act-Assert (Given-When-Then): Arrange (set up subject/inputs/doubles), Act (invoke one behavior), Assert (verify the outcome). Separate the phases visually and keep one behavior per test — makes tests readable and focused."}]},{level:"senior",q:"How do you avoid over-mocking, and why does it matter?",a:[{t:"p",text:"*Over-mocking* — mocking everything and verifying every interaction — produces *brittle* tests that break on any refactor and test the *implementation* rather than *behavior*. Avoid it by: preferring *fakes* over mocks (realistic, reusable), mocking only at true *boundaries* (network/DB), verifying *outcomes* (state) over interactions, and not mocking value objects/data classes (just use them). Tests should survive refactoring that preserves behavior."},{t:"list",items:["**Over-mocking** — brittle tests coupled to implementation.","**Prefer fakes** — realistic, reusable.","**Mock boundaries only** — network/DB, not everything.","**Verify outcomes** — not every interaction; don't mock value objects."]},{t:"note",text:"Over-mocking (mocking everything, verifying every call) makes brittle tests coupled to implementation, breaking on refactors. Avoid it: prefer fakes (realistic/reusable), mock only true boundaries (network/DB), verify outcomes (state) over interactions, don't mock value objects. Tests should survive behavior-preserving refactors."}]}],DC={id:"unit-testing",title:"Unit Testing: JUnit, Fakes & Mocks",tagline:"JUnit & Arrange-Act-Assert, test doubles, fakes vs mocks (the key distinction), MockK for Kotlin, and avoiding brittle/flaky tests.",tags:["Unit Testing","JUnit","Fakes","Mocks","MockK"],content:MC,qa:PC},RC=[{heading:"The challenge: testing async code",blocks:[{t:"p",text:"Testing coroutines and flows is harder than testing plain functions because the code is *asynchronous* — things happen over time, on different threads, with delays. Naively, a test might finish before the coroutine's work completes, or use real `delay`s that make it slow. The coroutines test library solves this with **`runTest`** and **virtual time**: it runs your suspend code in a controlled scope where `delay`s complete *instantly* (a virtual clock is advanced instead of real waiting), giving fast, deterministic async tests."}]},{heading:"runTest and virtual time",blocks:[{t:"code",title:"runTest — the coroutine test builder",code:`@Test
fun loadUser_succeeds() = runTest {          // provides a TestScope
    val repo = FakeUserRepository(user = testUser)
    val viewModel = UserViewModel(repo)

    viewModel.load("42")                      // launches a coroutine
    advanceUntilIdle()                        // run all pending coroutines to completion

    assertEquals(testUser, viewModel.state.value.user)
}

@Test
fun debounce_test() = runTest {
    // delay(300) inside completes INSTANTLY via virtual time:
    viewModel.onQueryChange("kotlin")
    advanceTimeBy(300)                         // advance the virtual clock past debounce
    runCurrent()                               // run tasks scheduled up to now
    assertEquals(expectedResults, viewModel.results.value)
}`},{t:"list",items:["**`runTest { }`** — the entry point for coroutine tests. It provides a `TestScope` with a `TestDispatcher` and a virtual clock, and it *auto-advances* time so `delay`s don't actually wait. A test of a 300ms debounce runs in microseconds.","**Virtual time controls**: `advanceUntilIdle()` (run all pending coroutines until none are left), `advanceTimeBy(ms)` (move the clock forward by a duration, running tasks scheduled up to then), `runCurrent()` (run tasks scheduled at the current time). These let you *deterministically* step through async execution.","**Why it's fast and reliable**: no real waiting (delays are virtual), no real threads racing — the scheduler runs tasks in a controlled order, so async tests are as deterministic as synchronous ones."]}]},{heading:"TestDispatcher: Standard vs Unconfined",blocks:[{t:"p",text:"`runTest` uses a **`TestDispatcher`**, and there are two kinds with different execution semantics — choosing the right one matters for what you can test."},{t:"table",headers:["","StandardTestDispatcher","UnconfinedTestDispatcher"],rows:[["Execution","queues coroutines — they don't run until you advance","runs coroutines eagerly (immediately, until they suspend)"],["Control","precise — you step execution with advance*","less control — things just run"],["Intermediate states","can assert them (e.g. Loading before Content)","may skip past them"],["Use for","testing step-by-step behavior, transient states","simpler tests where you don't need intermediate states"]]},{t:"list",items:["**`StandardTestDispatcher`** (the `runTest` default) *queues* launched coroutines — they don't execute until you call `advanceUntilIdle()`/`runCurrent()`. This gives precise control, letting you assert *intermediate* states — e.g. that the ViewModel shows `Loading` before it shows `Content`.","**`UnconfinedTestDispatcher`** runs coroutines *eagerly* (immediately, until first suspension). Simpler for tests where you only care about the final state, but you may miss transient states because they've already passed by the time you assert.","**Choosing**: use `StandardTestDispatcher` when you need to verify intermediate states or exact ordering; `UnconfinedTestDispatcher` for simpler 'just give me the final result' tests. Many ViewModel tests use Unconfined for simplicity, Standard when asserting Loading→Content transitions."]}]},{heading:"The Main dispatcher rule",blocks:[{t:"p",text:"`viewModelScope` uses `Dispatchers.Main`, which *doesn't exist* on the JVM (it's provided by the Android UI thread). So a plain JVM unit test of a ViewModel that launches in `viewModelScope` fails with 'Module with the Main dispatcher had failed to initialize'. The fix: replace `Dispatchers.Main` with a `TestDispatcher` for the test's duration, via `Dispatchers.setMain()`. A reusable JUnit rule does this cleanly."},{t:"code",title:"MainDispatcherRule",code:`class MainDispatcherRule(
    val dispatcher: TestDispatcher = UnconfinedTestDispatcher(),
) : TestWatcher() {
    override fun starting(description: Description) = Dispatchers.setMain(dispatcher)
    override fun finished(description: Description) = Dispatchers.resetMain()
}

// In the test class:
@get:Rule val mainDispatcherRule = MainDispatcherRule()

@Test fun test() = runTest {
    val vm = UserViewModel(fakeRepo)   // viewModelScope now uses the test dispatcher
    // ...
}`},{t:"list",items:["**`Dispatchers.setMain(testDispatcher)`** substitutes a test dispatcher for `Dispatchers.Main` so `viewModelScope` (and any `Main`-dispatched code) runs on the controllable test scheduler. **`Dispatchers.resetMain()`** restores it after.","The `MainDispatcherRule` (a `TestWatcher`) applies this automatically before/after each test — a standard piece of ViewModel-test infrastructure you'll write once and reuse.","This is *the* reason a ViewModel test needs setup beyond just constructing it — without the Main dispatcher rule, launching in `viewModelScope` throws."]}]},{heading:"Testing Flows with Turbine",blocks:[{t:"p",text:"Flows emit multiple values over time, which is awkward to assert with plain collection. **Turbine** (a small library) makes it clean: `flow.test { }` collects the flow and gives you `awaitItem()`/`awaitComplete()`/`awaitError()` to assert emissions one by one."},{t:"code",title:"Turbine flow test",code:`@Test fun state_emitsLoadingThenContent() = runTest {
    val viewModel = SearchViewModel(FakeRepo(results = listOf(item)))

    viewModel.uiState.test {                       // collect the flow
        assertEquals(UiState.Loading, awaitItem())  // first emission
        viewModel.search("query")
        assertEquals(UiState.Content(listOf(item)), awaitItem())  // next emission
        cancelAndIgnoreRemainingEvents()            // stop collecting
    }
}`},{t:"list",items:["**`flow.test { }`** — starts collecting; inside, `awaitItem()` returns the next emission, `awaitComplete()` asserts the flow finished, `awaitError()` asserts an exception, `expectNoEvents()` asserts nothing emitted. It fails the test if you leave unconsumed emissions — catching accidental over-emission.","**The `stateIn(WhileSubscribed)` gotcha**: a flow produced with `WhileSubscribed` is cold until collected, so asserting `.value` right after constructing the ViewModel sees only the `initialValue`. You must *collect* it (Turbine's `.test { }` subscribes and triggers the upstream), or launch a collector in `backgroundScope`.","**Combine with virtual time** for time-based operators: `advanceTimeBy(300)` inside a Turbine test to skip a `debounce` deterministically."]},{t:"note",text:"Coroutine/Flow testing essentials: runTest gives a TestScope with virtual time (delays complete instantly), controlled with advanceUntilIdle/advanceTimeBy/runCurrent. TestDispatcher: Standard (queues — precise control, assert intermediate states) vs Unconfined (eager — simpler). ViewModels need Dispatchers.setMain(testDispatcher) via a MainDispatcherRule (viewModelScope uses Main, absent on JVM). Test Flows with Turbine (.test { awaitItem() }); remember stateIn(WhileSubscribed) is cold until collected. Inject dispatchers so tests control them."}]}],EC=[{level:"junior",q:"How do you test coroutines? What does runTest do?",a:[{t:"p",text:"**You test coroutines with `runTest`, which runs your suspend code in a controlled test scope with *virtual time* — so `delay`s complete instantly instead of actually waiting, making async tests fast and deterministic.** It provides a `TestScope` with a test dispatcher and a virtual clock."},{t:"code",title:"A coroutine test",code:`@Test fun test() = runTest {
    val vm = MyViewModel(fakeRepo)
    vm.load()
    advanceUntilIdle()               // run all pending coroutines
    assertEquals(expected, vm.state.value)
}`},{t:"list",items:["**Virtual time** — a `delay(1000)` inside the code under test completes *instantly* because `runTest` advances a fake clock rather than waiting a real second. So testing a debounce or a timeout takes microseconds, not real time.","**Control functions** — `advanceUntilIdle()` runs all pending coroutines to completion; `advanceTimeBy(ms)` moves the virtual clock forward (running tasks scheduled up to then, useful for debounce); `runCurrent()` runs tasks scheduled at the current moment.","**Deterministic** — because the scheduler runs tasks in a controlled order with no real threads racing, async tests are as reliable as synchronous ones."]},{t:"p",text:"The old way (`runBlockingTest`, `TestCoroutineDispatcher`) is deprecated; `runTest` is the modern API. The key mental shift is that you're not *waiting* for async work — you're *controlling* time explicitly, advancing the clock and running scheduled tasks when you choose, which is what makes coroutine tests both fast and precise."}]},{level:"junior",q:"Why do you need Dispatchers.setMain when testing a ViewModel?",a:[{t:"p",text:"**Because `viewModelScope` uses `Dispatchers.Main`, and `Dispatchers.Main` doesn't exist on the JVM where unit tests run — it's provided by the Android UI thread, which isn't present in a local test.** So a ViewModel that launches a coroutine in `viewModelScope` throws 'Module with the Main dispatcher had failed to initialize' in a plain JVM test. `Dispatchers.setMain(testDispatcher)` substitutes a controllable test dispatcher for `Main` so the ViewModel's coroutines can run under the test's control."},{t:"code",title:"MainDispatcherRule",code:`class MainDispatcherRule(
    private val d: TestDispatcher = UnconfinedTestDispatcher(),
) : TestWatcher() {
    override fun starting(desc: Description) = Dispatchers.setMain(d)
    override fun finished(desc: Description) = Dispatchers.resetMain()
}

@get:Rule val mainDispatcherRule = MainDispatcherRule()`},{t:"p",text:"You typically wrap this in a reusable `MainDispatcherRule` (a JUnit `TestWatcher`) that calls `setMain` before each test and `resetMain` after — standard ViewModel-test infrastructure you write once. This is *the* reason a ViewModel test needs setup beyond just constructing the ViewModel: without it, anything launched in `viewModelScope` fails immediately. It also has a bonus — by setting Main to a `TestDispatcher`, the ViewModel's `viewModelScope` coroutines now run on the controllable test scheduler with virtual time, so you can advance the clock and assert states deterministically."}]},{level:"junior",q:"How do you test a Flow?",a:[{t:"p",text:"**The cleanest way is the Turbine library: `flow.test { }` collects the flow and gives you `awaitItem()` to assert each emission in order, `awaitComplete()` for completion, and `awaitError()` for exceptions — all inside `runTest` so you have virtual time.**"},{t:"code",title:"Turbine test",code:`@Test fun test() = runTest {
    viewModel.uiState.test {
        assertEquals(UiState.Loading, awaitItem())
        viewModel.load()
        assertEquals(UiState.Content(data), awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**`flow.test { }`** starts collecting; `awaitItem()` returns the next emitted value, `awaitComplete()`/`awaitError()`/`expectNoEvents()` assert completion, error, or silence. Turbine fails the test if emissions are left unconsumed, catching accidental over-emission.","**Combine with virtual time** for operators like `debounce` — `advanceTimeBy(300)` inside the `test { }` block to skip the debounce deterministically.","You *can* test flows without Turbine (collect into a list with `toList()` for finite flows, or launch a collector), but Turbine's await-based API is far cleaner for the typical 'assert these emissions in order' case."]},{t:"p",text:"One important gotcha: a `StateFlow` produced with `stateIn(WhileSubscribed(...))` is *cold until collected* — it does nothing until someone subscribes. So if you construct a ViewModel and immediately assert `uiState.value`, you'll only see the `initialValue`. Using Turbine's `.test { }` subscribes to the flow, which triggers the upstream to actually run and emit — so you must collect it (via Turbine or a `backgroundScope.launch { collect() }`) before you can observe real emissions."}]},{level:"senior",q:"What's the difference between StandardTestDispatcher and UnconfinedTestDispatcher, and when do you use each?",a:[{t:"p",text:"**They differ in *when* launched coroutines execute. `StandardTestDispatcher` *queues* coroutines — they don't run until you explicitly advance time (`advanceUntilIdle`/`runCurrent`), giving you precise step-by-step control. `UnconfinedTestDispatcher` runs coroutines *eagerly* — immediately, until they suspend — which is simpler but gives up fine control.** The choice affects what you can observe in a test."},{t:"list",items:["**`StandardTestDispatcher`** (the `runTest` default): launched coroutines are *scheduled but not run* until you advance the virtual clock. This lets you assert *intermediate states* and *exact ordering* — for example, verifying that a ViewModel emits `Loading` *before* it emits `Content`. You control execution: launch the operation, assert the loading state, then `advanceUntilIdle()` to let it complete, then assert the final state. Use it when the *transient states or ordering* matter.","**`UnconfinedTestDispatcher`**: launched coroutines run *eagerly and immediately* until their first suspension point, without you advancing anything. This is simpler — the code 'just runs' — but you may *miss intermediate states* because they've already transitioned by the time your assertion runs. For instance, by the time you check, the state may already be `Content` and you never saw `Loading`. Use it for tests where you only care about the *final outcome*."]},{t:"list",items:["**How to choose**: if your test needs to verify a *sequence* of states (Loading → Content, or that something happens before something else), use `StandardTestDispatcher` and step through with `advance*`. If you just want to run everything and assert the end result, `UnconfinedTestDispatcher` is simpler and less error-prone (no forgetting to advance). Many ViewModel tests use Unconfined for the `MainDispatcherRule` for simplicity, switching to Standard for the specific tests that assert intermediate states.","**A subtle trap with Standard**: because coroutines don't run until you advance, forgetting to call `advanceUntilIdle()`/`runCurrent()` means your assertion runs *before the code executed* — the test sees the initial state and may pass or fail misleadingly. With Standard you must remember to advance; with Unconfined you don't (but lose control). Understanding this trade-off is what prevents confusing test failures."]},{t:"p",text:"**The senior framing**: the two dispatchers represent a control-vs-simplicity trade-off in async testing. `StandardTestDispatcher` gives you a 'single-stepping debugger' for coroutines — nothing runs until you say so, so you can observe every intermediate state and ordering, at the cost of having to drive execution explicitly. `UnconfinedTestDispatcher` is 'run it all and check the result' — simpler, but blind to transient states. The mature approach is to default to whichever fits the test's needs: Unconfined when asserting final state (most tests), Standard when the *timeline* of states or ordering is what you're verifying. Knowing that Standard requires explicit advancing (and that forgetting to advance is a common source of confusing failures) is the practical depth that distinguishes someone who's actually written coroutine tests."}]},{level:"senior",q:"Walk through testing a ViewModel that has a debounced search with stateIn(WhileSubscribed). What are the pitfalls?",a:[{t:"p",text:"**This combines all the coroutine-testing challenges: the Main dispatcher, virtual time for the debounce, and the cold-until-collected nature of `stateIn(WhileSubscribed)`. The test must set up the Main dispatcher, collect the flow to trigger it, and advance virtual time past the debounce — miss any of these and the test fails misleadingly.**"},{t:"code",title:"The complete test",code:`@get:Rule val mainDispatcherRule = MainDispatcherRule(StandardTestDispatcher())

@Test fun search_debouncesAndEmitsResults() = runTest {
    val repo = FakeSearchRepository(results = mapOf("kotlin" to listOf(item)))
    val viewModel = SearchViewModel(repo)   // uses viewModelScope + stateIn(WhileSubscribed)

    viewModel.uiState.test {                 // MUST collect — triggers the cold stateIn
        assertEquals(SearchState.Empty, awaitItem())   // initialValue

        viewModel.onQueryChange("kotlin")
        advanceTimeBy(300)                    // skip the debounce via virtual time
        runCurrent()

        assertEquals(SearchState.Loading, awaitItem())
        assertEquals(SearchState.Results(listOf(item)), awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**Pitfall 1 — the Main dispatcher**: `viewModelScope` uses `Dispatchers.Main`, absent on the JVM, so you need `Dispatchers.setMain(testDispatcher)` via the `MainDispatcherRule`. Without it, the test throws immediately on any `viewModelScope.launch`.","**Pitfall 2 — cold `stateIn(WhileSubscribed)`**: this is the big one. `WhileSubscribed` makes the flow *cold until collected* — it does nothing until a subscriber appears. So if you assert `viewModel.uiState.value` right after construction, you *only ever see the `initialValue`* (`Empty`), never the real emissions — the upstream never started because nothing subscribed. You *must* collect the flow (Turbine's `.test { }` subscribes, triggering the upstream) or launch a collector in `backgroundScope`. This trips up almost everyone the first time — the test 'passes' or 'fails' based on initialValue and you can't figure out why real values never appear.","**Pitfall 3 — virtual time for debounce**: the `debounce(300)` won't emit until 300ms of silence. In a test you *advance virtual time* (`advanceTimeBy(300)` then `runCurrent()`) to skip past it deterministically — you never actually wait. Forgetting to advance means the debounced value never emits and `awaitItem()` times out.","**Pitfall 4 — dispatcher choice for intermediate states**: to assert Loading *then* Results, use `StandardTestDispatcher` (so you can control the stepping); with Unconfined you might skip past Loading. And ensure the repository/flow uses *injected* dispatchers so they run on the test scheduler, not a hardcoded `Dispatchers.IO` that escapes virtual time.","**Pitfall 5 — injected dispatchers**: if the ViewModel or repository hardcodes `Dispatchers.IO`, that work runs on real threads outside the test's control, breaking virtual time and determinism. Dispatchers must be injected so the test substitutes a `TestDispatcher`."]},{t:"p",text:"**The senior framing**: this scenario is a favorite because it requires understanding *all* the coroutine-testing machinery working together — the Main dispatcher substitution (for `viewModelScope`), virtual time control (for `debounce`), the cold-until-collected behavior of `stateIn(WhileSubscribed)` (must subscribe to trigger it), the Standard-vs-Unconfined choice (to observe intermediate states), and injected dispatchers (so everything runs on the test scheduler). The single most common and confusing failure is the `stateIn(WhileSubscribed)` one — 'why does my ViewModel only ever emit the initial value?' — and knowing the answer (it's cold, you must collect it to start the upstream) is the tell that someone has genuinely tested modern ViewModels rather than just read about it. Being able to enumerate these pitfalls and their fixes demonstrates real, hands-on coroutine-testing competence."}]},{level:"junior",q:"What is the TestScope and testScheduler in runTest?",a:[{t:"p",text:"`runTest { }` runs the test body in a `TestScope` backed by a `TestCoroutineScheduler` (the *virtual clock*). The scheduler controls time — `delay` doesn't wait real time; it advances virtually. You share this `testScheduler` with any test dispatchers so all coroutines run on the same virtual clock, making the test deterministic. `this.testScheduler` (or `testScheduler` in the scope) exposes it."},{t:"list",items:["**`TestScope`** — the coroutine scope for the test body.","**`TestCoroutineScheduler`** — the virtual clock.","**Share the scheduler** — with test dispatchers.","**Deterministic** — all coroutines on one virtual clock."]},{t:"note",text:"runTest runs in a TestScope backed by a TestCoroutineScheduler (virtual clock — delay advances virtually, not real time). Share this testScheduler with your test dispatchers so all coroutines run on the same clock, making tests deterministic. Access via testScheduler in the scope."}]},{level:"junior",q:"What do advanceUntilIdle, advanceTimeBy, and runCurrent do?",a:[{t:"p",text:"These drive the virtual clock in `runTest`: `advanceUntilIdle()` runs *all* scheduled coroutines until nothing is left to do (the common choice to 'let everything finish'); `advanceTimeBy(ms)` advances the clock by a duration (running work scheduled within it — for testing `delay`/`debounce` at specific points); `runCurrent()` runs work scheduled at the *current* time without advancing. They give precise control over coroutine execution."},{t:"list",items:["**`advanceUntilIdle()`** — run everything to completion.","**`advanceTimeBy(ms)`** — advance the clock (test delays/debounce).","**`runCurrent()`** — run current-time work without advancing.","**Precise control** — over coroutine execution."]},{t:"note",text:"In runTest: advanceUntilIdle() runs all scheduled coroutines to completion ('let everything finish'); advanceTimeBy(ms) advances the virtual clock (test delay/debounce at specific points); runCurrent() runs current-time work without advancing. Precise control over coroutine execution and timing."}]},{level:"senior",q:"How does the MainDispatcherRule work, and why do you need it?",a:[{t:"p",text:"`Dispatchers.Main` requires the Android UI looper, which doesn't exist in JVM unit tests — so any code using `viewModelScope` (which uses `Main.immediate`) throws. A `MainDispatcherRule` (a JUnit rule) calls `Dispatchers.setMain(testDispatcher)` before each test and `resetMain()` after — replacing Main with a test dispatcher so ViewModel coroutines run on the virtual clock. It's boilerplate you write once and reuse."},{t:"code",title:"MainDispatcherRule",code:`class MainDispatcherRule(val dispatcher: TestDispatcher = UnconfinedTestDispatcher()) : TestWatcher() {
    override fun starting(d: Description) = Dispatchers.setMain(dispatcher)
    override fun finished(d: Description) = Dispatchers.resetMain()
}`},{t:"list",items:["**`Dispatchers.Main` needs a looper** — absent in JVM tests.","**`setMain(testDispatcher)`/`resetMain()`** — swap it.","**Rule** — applies before/after each test.","**Enables** — viewModelScope on the virtual clock."]},{t:"note",text:"Dispatchers.Main needs the Android looper (absent in JVM tests), so viewModelScope code throws. A MainDispatcherRule (JUnit TestWatcher) calls Dispatchers.setMain(testDispatcher) before each test and resetMain() after — swapping Main for a test dispatcher so ViewModel coroutines run on the virtual clock. Write once, reuse."}]},{level:"senior",q:"How do you test a Flow with Turbine?",a:[{t:"p",text:"Turbine's `flow.test { }` collects the flow and lets you assert emissions one at a time: `awaitItem()` (next value), `awaitError()` (an error), `awaitComplete()` (completion), and `expectNoEvents()`. It's ideal for hot/infinite flows (which `toList()` can't handle) and for asserting *sequences* of emissions interactively. End with `cancelAndIgnoreRemainingEvents()`."},{t:"code",title:"Turbine",code:`viewModel.uiState.test {
    assertThat(awaitItem()).isEqualTo(UiState.Loading)
    viewModel.load()
    assertThat(awaitItem()).isEqualTo(UiState.Content(data))
    cancelAndIgnoreRemainingEvents()
}`},{t:"list",items:["**`flow.test { }`** — collect and assert emissions.","**`awaitItem`/`awaitError`/`awaitComplete`** — sequence assertions.","**Hot/infinite flows** — where `toList()` hangs.","**`cancelAndIgnoreRemainingEvents()`** — end cleanly."]},{t:"note",text:"Turbine's flow.test { } collects a flow and asserts emissions one at a time: awaitItem (next), awaitError, awaitComplete, expectNoEvents. Ideal for hot/infinite flows (toList hangs) and asserting emission sequences interactively. End with cancelAndIgnoreRemainingEvents()."}]},{level:"senior",q:"How do you test a StateFlow-based ViewModel's state sequence?",a:[{t:"p",text:"With a `MainDispatcherRule`, inject a fake repository, and assert the `StateFlow` — either read `.value` after `advanceUntilIdle()` (final state) or use Turbine's `test { }` to assert the sequence (Loading → Content). For `stateIn(WhileSubscribed)` flows, the upstream only starts with an active collector, so Turbine's collection (or reading `.value`) activates it. Drive actions between assertions to test transitions."},{t:"code",title:"State sequence test",code:`@Test fun loadsContent() = runTest {
    val vm = MyViewModel(FakeRepo(data))
    vm.state.test {
        assertThat(awaitItem()).isEqualTo(UiState.Loading)
        assertThat(awaitItem()).isEqualTo(UiState.Content(data))
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**`MainDispatcherRule` + fake repo** — setup.","**Turbine sequence** — Loading → Content.","**Or `.value`** — after `advanceUntilIdle` (final).","**`WhileSubscribed`** — needs an active collector to start."]},{t:"note",text:"Test StateFlow ViewModels with a MainDispatcherRule + fake repo: assert .value after advanceUntilIdle (final state) or Turbine's test { } for the sequence (Loading → Content). WhileSubscribed upstream needs an active collector — Turbine/reading .value activates it. Drive actions between assertions for transitions."}]},{level:"senior",q:"How do you inject test dispatchers into the code under test?",a:[{t:"p",text:"Inject dispatchers rather than hardcoding `Dispatchers.IO` — a constructor parameter (`ioDispatcher: CoroutineDispatcher = Dispatchers.IO`) with a production default. In tests, pass a `TestDispatcher` sharing the `runTest` `testScheduler`, so the code's coroutines run on the virtual clock. A `DispatcherProvider` interface bundling Main/IO/Default is common for larger apps. This makes all coroutine code deterministic in tests."},{t:"code",title:"Injected dispatcher",code:`class Repo(private val io: CoroutineDispatcher = Dispatchers.IO) { ... }
// Test:
val repo = Repo(StandardTestDispatcher(testScheduler))`},{t:"list",items:["**Inject dispatchers** — constructor param, production default.","**Test dispatcher** — sharing `runTest`'s `testScheduler`.","**`DispatcherProvider`** — bundle Main/IO/Default for larger apps.","**Deterministic** — coroutines on the virtual clock."]},{t:"note",text:"Inject dispatchers (constructor param, default Dispatchers.IO) instead of hardcoding, so tests pass a TestDispatcher sharing runTest's testScheduler — coroutines run on the virtual clock (deterministic). Larger apps inject a DispatcherProvider bundling Main/IO/Default."}]},{level:"senior",q:"How do you test a debounced search flow?",a:[{t:"p",text:"In `runTest`, drive the query (via a `MutableStateFlow` or the ViewModel's input), then `advanceTimeBy` past the debounce window and assert the resulting emission/search call. The virtual clock skips the real 300ms wait. Test that rapid changes produce *one* search (debounce collapses them) and that `distinctUntilChanged`/`flatMapLatest` behave (only the latest query searches)."},{t:"code",title:"Debounce test",code:`val query = MutableStateFlow("")
val results = query.debounce(300).distinctUntilChanged()...
results.test {
    query.value = "a"; query.value = "ab"
    advanceTimeBy(301)                      // past debounce
    assertThat(awaitItem()).isEqualTo(searchFor("ab"))   // only the last
}`},{t:"list",items:["**Drive the query** — `MutableStateFlow`/input.","**`advanceTimeBy`** — past the debounce window.","**Assert one search** — debounce collapses rapid changes.","**Latest only** — `flatMapLatest`/`distinctUntilChanged`."]},{t:"note",text:"Test debounced search in runTest: drive the query (MutableStateFlow/input), advanceTimeBy past the debounce window (virtual clock skips the real wait), assert one search for the latest query. Verify rapid changes collapse to one search (debounce) and only the latest searches (flatMapLatest/distinctUntilChanged)."}]},{level:"senior",q:"How do you test that a coroutine is cancelled properly?",a:[{t:"p",text:"In `runTest`, launch the coroutine, then cancel it and assert the observable effects: cleanup ran (`finally`/flags), no further state changes, resources released. Use `advanceTimeBy` to position cancellation mid-work. Assert that `CancellationException` wasn't swallowed (the coroutine actually stopped). This verifies structured cancellation behaves correctly."},{t:"code",title:"Cancellation test",code:`@Test fun cleansUpOnCancel() = runTest {
    var cleaned = false
    val job = launch { try { delay(10_000) } finally { cleaned = true } }
    advanceTimeBy(100); job.cancelAndJoin()
    assertThat(cleaned).isTrue()
}`},{t:"list",items:["**Launch + cancel** — `cancelAndJoin()` to wait.","**Assert effects** — cleanup ran, no further changes.","**`advanceTimeBy`** — position cancellation mid-work.","**Not swallowed** — the coroutine actually stopped."]},{t:"note",text:"Test cancellation in runTest: launch, position with advanceTimeBy, cancelAndJoin(), then assert observable effects — cleanup ran (finally/flags), no further state changes, resources released, CancellationException not swallowed. Verifies structured cancellation works."}]},{level:"senior",q:"How do you test a flow that emits errors?",a:[{t:"p",text:"Inject a fake that throws, then assert with Turbine's `awaitError()` (for a propagated error) or `awaitItem()` (for a mapped error state if the flow has a `catch`). Test both paths: an uncaught error propagates (`awaitError`), and a `catch`-guarded flow emits a fallback/error state. Also verify `CancellationException` isn't caught by error handling."},{t:"code",title:"Flow error test",code:`flow<Int> { throw IOException() }.catch { emit(-1) }.test {
    assertThat(awaitItem()).isEqualTo(-1)   // fallback
    awaitComplete()
}`},{t:"list",items:["**Fake throws** — inject the error.","**`awaitError()`** — propagated error.","**`awaitItem()`** — mapped error/fallback state.","**Cancellation** — ensure it isn't swallowed."]},{t:"note",text:"Test flow errors with a fake that throws: Turbine awaitError() for propagated errors, or awaitItem() for a catch-guarded fallback/error state. Test both paths (uncaught propagates, catch emits fallback) and verify CancellationException isn't swallowed by error handling."}]},{level:"senior",q:"What are the pitfalls of testing stateIn(WhileSubscribed) StateFlows?",a:[{t:"p",text:"`stateIn(WhileSubscribed(5000))` upstream *only starts when there's a collector* — so in a test, if you just read `.value` without collecting, you get the initial value and the upstream never runs. Fixes: collect it (Turbine's `test { }` counts as a collector, or `launch { flow.collect { } }` in a background job), and `advanceUntilIdle()`. Also, the 5s stop-timeout is virtual — the test clock handles it. Forgetting the collector is the classic pitfall (state stuck at initial)."},{t:"list",items:["**No collector = no upstream** — `.value` stays initial.","**Collect it** — Turbine `test { }` or a background `launch { collect }`.","**`advanceUntilIdle`** — let the upstream run.","**Classic pitfall** — state stuck at initial (no collector)."]},{t:"note",text:"stateIn(WhileSubscribed) upstream only starts with a collector — so reading .value without collecting gives the initial value and the upstream never runs (the classic pitfall: state stuck at initial). Collect it (Turbine test { } or a background launch { collect }) + advanceUntilIdle. The 5s timeout is virtual (test clock handles it)."}]},{level:"junior",q:"Why can't you use Thread.sleep or real delays in coroutine tests?",a:[{t:"p",text:"`Thread.sleep` and real time make tests *slow* (a `delay(10_000)` would wait 10 real seconds) and *flaky* (timing-dependent). `runTest`'s *virtual clock* solves this — `delay` is skipped/advanced instantly and deterministically. Never `Thread.sleep` to 'wait for a coroutine'; use `advanceUntilIdle()`/`advanceTimeBy()` to control the virtual clock. This makes coroutine tests fast and reliable."},{t:"list",items:["**Real time = slow + flaky** — timing-dependent.","**Virtual clock** — `runTest` skips/advances `delay` instantly.","**`advanceUntilIdle`/`advanceTimeBy`** — control time.","**Never `Thread.sleep`** — to wait for coroutines."]},{t:"note",text:"Thread.sleep/real delays make tests slow (delay(10_000) waits 10 real seconds) and flaky (timing-dependent). runTest's virtual clock skips/advances delay instantly and deterministically — use advanceUntilIdle()/advanceTimeBy() to control time, never Thread.sleep to wait for coroutines."}]},{level:"senior",q:"How do you test coroutines launched in a custom scope?",a:[{t:"p",text:"Inject the scope (or its dispatcher) so the test controls it. For a class using an app-scoped `CoroutineScope`, inject a `TestScope`/scope built with a `TestDispatcher` sharing the `testScheduler`, so launched coroutines run on the virtual clock and you can `advanceUntilIdle()`. Avoid hardcoded `GlobalScope`/scopes you can't control — inject them for testability, like dispatchers."},{t:"list",items:["**Inject the scope/dispatcher** — control it in tests.","**`TestScope`/test dispatcher** — share the `testScheduler`.","**`advanceUntilIdle`** — drive launched coroutines.","**Don't hardcode** — GlobalScope/uncontrollable scopes."]},{t:"note",text:"Inject the CoroutineScope (or its dispatcher) so tests control it — pass a TestScope/scope with a TestDispatcher sharing the testScheduler, so launched coroutines run on the virtual clock (advanceUntilIdle). Don't hardcode GlobalScope/uncontrollable scopes — inject them for testability, like dispatchers."}]},{level:"junior",q:"How do you test a suspend function?",a:[{t:"p",text:"Call it inside `runTest { }` (which provides a coroutine context) and assert the result — since a suspend function returns a value directly, it's like testing a normal function but in a coroutine scope. Inject fakes for its dependencies (a fake repository/API), and use `advanceUntilIdle()` if it launches child coroutines. Most suspend-function tests are simple: call, await, assert."},{t:"code",title:"Suspend test",code:`@Test fun loadsUser() = runTest {
    val repo = FakeRepo(user)
    val result = repo.getUser("1")   // suspend call
    assertThat(result).isEqualTo(user)
}`},{t:"list",items:["**`runTest { }`** — provides the coroutine context.","**Call + assert** — like a normal function.","**Fakes** — for dependencies.","**`advanceUntilIdle`** — if it launches children."]},{t:"note",text:"Test a suspend function inside runTest { } (provides the coroutine context) — call it, assert the result (it returns a value directly). Inject fakes for dependencies; advanceUntilIdle() if it launches child coroutines. Most suspend tests are simple: call, await, assert."}]},{level:"senior",q:"How do you test that emissions happen in the right order and timing?",a:[{t:"p",text:"Use Turbine to assert the *order* (`awaitItem()` returns emissions in sequence) and `advanceTimeBy` to test *timing* (a value emitted after a delay appears only after advancing the clock). For time-based operators (`debounce`, `sample`, `delay`), advance the clock to specific points and assert what's emitted at each. This deterministically verifies both order and timing without real waits."},{t:"list",items:["**Turbine `awaitItem()`** — order of emissions.","**`advanceTimeBy`** — timing (emit after delay).","**Time operators** — advance to points, assert emissions.","**Deterministic** — no real waits."]},{t:"note",text:"Test order with Turbine (awaitItem() returns emissions in sequence) and timing with advanceTimeBy (a value emitted after a delay appears only after advancing). For debounce/sample/delay, advance the clock to specific points and assert emissions at each — deterministic order/timing verification without real waits."}]},{level:"junior",q:"What is the difference between launch and async when testing?",a:[{t:"p",text:"When testing code that uses `launch` (fire-and-forget), you need `advanceUntilIdle()` (or `runCurrent()`) to let the launched coroutine run before asserting. Code using `async` requires `await()` to get the result. In tests, the key is *driving the virtual clock* so launched/async coroutines execute — reading state too early (before advancing) gives the pre-execution value, a common test bug."},{t:"list",items:["**`launch`** — `advanceUntilIdle()` to run it before asserting.","**`async`** — `await()` for the result.","**Drive the clock** — so coroutines execute.","**Common bug** — asserting before advancing (pre-execution value)."]},{t:"note",text:"Testing launch (fire-and-forget): advanceUntilIdle()/runCurrent() to let it run before asserting. async: await() for the result. The key is driving the virtual clock so coroutines execute — reading state before advancing gives the pre-execution value (a common test bug)."}]},{level:"senior",q:"How do you test retry logic with backoff in a flow?",a:[{t:"p",text:"Use a fake that fails N times then succeeds (an attempt counter), collect the flow with `retry`/`retryWhen` in `runTest`, `advanceTimeBy` past the backoff delays (virtual clock skips them), and assert the final success and attempt count. Also test the give-up path (a fake that always fails surfaces the error after the cap). The virtual clock makes backoff testing instant."},{t:"code",title:"Retry test",code:`var attempts = 0
val flow = flow { attempts++; if (attempts < 3) throw IOException() else emit("ok") }
    .retryWhen { _, a -> if (a < 3) { delay(1000); true } else false }
flow.test { assertThat(awaitItem()).isEqualTo("ok"); awaitComplete() }
assertThat(attempts).isEqualTo(3)`},{t:"list",items:["**Fail-then-succeed fake** — attempt counter.","**`advanceTimeBy`** — past backoff (virtual clock).","**Assert result + attempts** — success after N.","**Give-up path** — always-fail surfaces error after cap."]},{t:"note",text:"Test retry+backoff with a fake failing N times then succeeding (attempt counter): collect with retry/retryWhen in runTest, advanceTimeBy past the backoff delays (virtual clock skips them), assert final success + attempt count. Also test the give-up path (always-fail surfaces the error after the cap)."}]},{level:"junior",q:"How do you assert a Flow emits nothing (no unexpected emissions)?",a:[{t:"p",text:"With Turbine, use `expectNoEvents()` to assert nothing has been emitted at that point (e.g. before a trigger, or during a debounce window before it elapses). This verifies a flow *doesn't* emit prematurely — important for debounce (no emission before the pause) or conditional logic (no emission when the condition isn't met)."},{t:"code",title:"expectNoEvents",code:`query.debounce(300).test {
    query.value = "a"
    advanceTimeBy(100); expectNoEvents()   // debounce not elapsed
    advanceTimeBy(250); awaitItem()         // now it emits
}`},{t:"list",items:["**`expectNoEvents()`** — assert nothing emitted.","**Debounce** — no emission before the pause.","**Conditional** — no emission when the condition is false.","**Verifies absence** — not just presence."]},{t:"note",text:"Use Turbine's expectNoEvents() to assert a flow hasn't emitted at that point — verifying it doesn't emit prematurely (debounce before the window elapses, conditional logic when the condition isn't met). Testing absence of emissions, not just presence."}]},{level:"senior",q:"How do you test a callbackFlow or channelFlow?",a:[{t:"p",text:"For a `callbackFlow` wrapping a callback API, inject a *fake* of the underlying API you can trigger — collect the flow with Turbine, invoke the fake's callback to produce emissions, and assert them. Also verify *cleanup*: check the listener is unregistered (via the fake) when the flow is cancelled (`awaitClose`). This tests both emission and resource cleanup."},{t:"list",items:["**Fake the callback source** — trigger it in the test.","**Turbine** — collect and assert emissions.","**Invoke the callback** — produce emissions.","**Verify `awaitClose`** — listener unregistered on cancel."]},{t:"note",text:"Test callbackFlow/channelFlow by injecting a fake of the underlying callback API you can trigger: collect with Turbine, invoke the fake's callback to produce emissions, assert them. Also verify cleanup — the listener is unregistered (via the fake) on cancel (awaitClose). Tests emission AND resource cleanup."}]},{level:"senior",q:"How do you avoid the 'test passes but coroutine didn't run' problem?",a:[{t:"p",text:"The bug: you assert state *before* the launched coroutine executed, so the test checks the pre-execution value and passes incorrectly. Avoid it by *advancing the clock* before asserting — `advanceUntilIdle()` runs all pending coroutines, `runCurrent()` runs current-time work. With `UnconfinedTestDispatcher`, coroutines run eagerly (may not need advancing); with `StandardTestDispatcher`, you *must* advance. Understand your dispatcher's execution model."},{t:"list",items:["**Assert too early** — checks pre-execution state; false pass.","**Advance first** — `advanceUntilIdle()`/`runCurrent()`.","**`StandardTestDispatcher`** — must advance.","**`UnconfinedTestDispatcher`** — eager; may not need advancing."]},{t:"note",text:"The 'test passes but coroutine didn't run' bug: asserting state before the launched coroutine executed (checks the pre-execution value, false pass). Fix: advance the clock before asserting (advanceUntilIdle/runCurrent). StandardTestDispatcher requires advancing; UnconfinedTestDispatcher runs eagerly (may not). Know your dispatcher's model."}]},{level:"junior",q:"How do you collect a Flow into a list for assertion?",a:[{t:"p",text:"For a *finite* flow, `flow.toList()` inside `runTest` collects all emissions into a list you assert. For a *hot/infinite* flow, `toList()` hangs (never completes) — bound it with `take(n).toList()` or use Turbine. For a `StateFlow`, collect emissions in a background `launch` into a list (and cancel it), since it never completes. Choose based on whether the flow completes."},{t:"code",title:"toList",code:`@Test fun emits() = runTest {
    val result = repo.numbers().map { it * 2 }.toList()   // finite flow
    assertThat(result).containsExactly(2, 4, 6)
}`},{t:"list",items:["**Finite** — `toList()` collects and asserts.","**Hot/infinite** — `take(n).toList()` or Turbine.","**StateFlow** — background `launch` collecting into a list.","**Choose** — by whether the flow completes."]},{t:"note",text:"Finite flow: toList() in runTest collects emissions to assert. Hot/infinite: toList() hangs — use take(n).toList() or Turbine. StateFlow: collect in a background launch into a list (cancel it) since it never completes. Choose by whether the flow completes."}]},{level:"junior",q:"What replaced the deprecated runBlockingTest, and why?",a:[{t:"p",text:"`runBlockingTest` (and `TestCoroutineDispatcher`/`TestCoroutineScope`) were *deprecated* in favor of `runTest` (with `TestScope`, `StandardTestDispatcher`, `UnconfinedTestDispatcher`). The new API has a *cleaner virtual-clock model*, better handles uncaught exceptions and unfinished coroutines, and integrates the scheduler properly. Use `runTest` for all new coroutine tests; migrate old `runBlockingTest` code."},{t:"list",items:["**`runBlockingTest` deprecated** — replaced by `runTest`.","**`runTest`** — TestScope + Standard/UnconfinedTestDispatcher.","**Cleaner model** — better exceptions/unfinished-coroutine handling.","**Migrate** — use runTest for all new tests."]},{t:"note",text:"runBlockingTest (and TestCoroutineDispatcher/Scope) were deprecated in favor of runTest (TestScope, StandardTestDispatcher, UnconfinedTestDispatcher) — a cleaner virtual-clock model with better exception/unfinished-coroutine handling. Use runTest for all new coroutine tests."}]},{level:"senior",q:"How do you test one-off events (Channel/SharedFlow) from a ViewModel?",a:[{t:"p",text:"Collect the events *before* triggering the action (events aren't replayed, so a late collector misses them). With Turbine, start `viewModel.events.test { }`, then trigger the action, and `awaitItem()` the event. Because a `Channel`/`SharedFlow(replay=0)` delivers each event once, the collector must be active at emit time — so order matters in the test."},{t:"code",title:"Event test",code:`viewModel.events.test {
    viewModel.onSaved()                       // trigger AFTER collection starts
    assertThat(awaitItem()).isEqualTo(UiEvent.NavigateBack)
    cancelAndIgnoreRemainingEvents()
}`},{t:"list",items:["**Collect first** — before triggering (no replay).","**Turbine `test { }`** — start collection.","**Trigger then `awaitItem()`** — assert the event.","**Order matters** — collector active at emit time."]},{t:"note",text:"Test one-off events (Channel/SharedFlow replay=0) by collecting BEFORE triggering — start Turbine's test { }, trigger the action, awaitItem() the event. Events aren't replayed, so the collector must be active at emit time; order matters in the test."}]},{level:"senior",q:"How do you test a combine of multiple flows?",a:[{t:"p",text:"Drive controllable input flows (`MutableStateFlow`s) with values in `runTest`, collect the combined output with Turbine, and assert emissions after each input change. `combine` emits the latest-of-each on any change, so push values one at a time and assert the recomputed result. Advance virtual time past any `debounce` in the pipeline."},{t:"code",title:"combine test",code:`val a = MutableStateFlow(1); val b = MutableStateFlow(10)
combine(a, b) { x, y -> x + y }.test {
    assertThat(awaitItem()).isEqualTo(11)
    a.value = 2
    assertThat(awaitItem()).isEqualTo(12)
    cancelAndIgnoreRemainingEvents()
}`},{t:"list",items:["**Controllable inputs** — `MutableStateFlow`s.","**Turbine** — collect combined output.","**Push one at a time** — assert recomputed result.","**Advance time** — past any debounce."]},{t:"note",text:"Test combine by driving controllable input flows (MutableStateFlows) with values in runTest, collecting the combined output with Turbine, and asserting after each input change (combine emits latest-of-each). Push values one at a time; advance virtual time past any debounce."}]},{level:"senior",q:"How do you test collectLatest or conflate behavior?",a:[{t:"p",text:"To test that `collectLatest` cancels stale processing: emit values with delays, and assert that only the *latest* value's processing completes (earlier ones are cancelled — e.g. track completions and assert only the last ran). Use the virtual clock (`advanceTimeBy`) to emit values before the prior processing finishes. This verifies cancel-and-restart semantics deterministically."},{t:"list",items:["**Emit with delays** — trigger cancel-and-restart.","**Assert only latest completes** — earlier cancelled.","**Track completions** — verify which ran.","**Virtual clock** — control timing precisely."]},{t:"note",text:"Test collectLatest/conflate by emitting values with delays (via the virtual clock) so a new value arrives before the prior processing finishes, then assert only the LATEST value's processing completes (earlier cancelled) — e.g. track completions. Verifies cancel-and-restart semantics deterministically."}]},{level:"senior",q:"How do you assert uncaught exceptions and unfinished coroutines in runTest?",a:[{t:"p",text:"`runTest` *fails the test* if a child coroutine throws an uncaught exception or if coroutines are still *unfinished* when the test body completes (it waits for them) — catching leaked/hung coroutines. So a background `launch` that throws will surface as a test failure. If you *expect* an uncaught exception, use a `CoroutineExceptionHandler` or structure the test to catch it. This safety net catches bugs a naive test would miss."},{t:"list",items:["**Uncaught exception** — fails the test.","**Unfinished coroutines** — runTest waits; hung ones fail it.","**Catches leaks** — background launches that throw/hang.","**Expected exceptions** — handle deliberately."]},{t:"note",text:"runTest fails the test on an uncaught child-coroutine exception or unfinished coroutines when the body completes (it waits for them) — catching leaked/hung coroutines a naive test would miss. A background launch that throws surfaces as a failure. For expected exceptions, handle them deliberately (handler/structure)."}]}],UC={id:"coroutine-flow-testing",title:"Coroutine & Flow Testing",tagline:"runTest & virtual time, StandardTestDispatcher vs UnconfinedTestDispatcher, the MainDispatcherRule, Turbine for Flows, and the stateIn(WhileSubscribed) gotcha.",tags:["Testing","runTest","TestDispatcher","Turbine","Virtual Time"],content:RC,qa:EC},qC=[{heading:"Testing ViewModels — the sweet spot",blocks:[{t:"p",text:"ViewModels are the ideal unit-test target: they hold the presentation logic, and (if well-designed) they're framework-free plain classes taking dependencies via constructor. You test a ViewModel by injecting fakes, invoking its functions, and asserting its exposed state — no device needed. This is where most of your *behavior* coverage should come from."},{t:"code",title:"A ViewModel test",code:`@get:Rule val mainDispatcherRule = MainDispatcherRule()

@Test fun refresh_showsLoadingThenContent() = runTest {
    val repo = FakeArticleRepository(articles = listOf(article))
    val viewModel = FeedViewModel(repo)

    viewModel.uiState.test {
        assertEquals(FeedUiState.Loading, awaitItem())
        viewModel.refresh()
        assertEquals(FeedUiState.Content(listOf(article)), awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**Inject a fake repository**, invoke ViewModel functions, assert the emitted `UiState` — this verifies the ViewModel's *behavior* (given this data/input, it produces this state).","**Needs the `MainDispatcherRule`** (for `viewModelScope`) and, for flows, Turbine + virtual time (covered in the coroutine-testing topic).","**Assert state, not implementation** — check what state the ViewModel *produces*, not which repository methods it called (prefer fakes over mocks here). This makes tests robust to refactoring.","Because ViewModels concentrate logic and are cheaply testable, thorough ViewModel tests give high confidence for little cost — the heart of the pyramid's base."]}]},{heading:"Testing Room (DAOs)",blocks:[{t:"p",text:"Room DAO tests verify your queries and database logic against a *real* SQLite database — so they're **instrumented tests** (run on a device/emulator) using an *in-memory* database (fast, discarded after the test). You can't meaningfully unit-test SQL against a fake; you test it against real SQLite."},{t:"code",title:"A DAO test with an in-memory database",code:`@RunWith(AndroidJUnit4::class)
class UserDaoTest {
    private lateinit var db: AppDatabase
    private lateinit var dao: UserDao

    @Before fun setup() {
        db = Room.inMemoryDatabaseBuilder(   // in-memory: fast, no persistence
            ApplicationProvider.getApplicationContext(), AppDatabase::class.java
        ).build()
        dao = db.userDao()
    }
    @After fun teardown() = db.close()

    @Test fun insertAndRetrieve() = runTest {
        dao.insert(UserEntity("1", "Revanth"))
        assertEquals("Revanth", dao.getUser("1")?.name)
    }
}`},{t:"list",items:["**In-memory database** (`Room.inMemoryDatabaseBuilder`) — a real Room/SQLite instance that lives only in memory, so tests are fast and isolated (fresh DB per test, discarded after). This tests your actual queries against real SQLite behavior.","**Instrumented** — DAO tests run on a device/emulator because they need the real SQLite engine. (You *can* run them on the JVM via Robolectric, which some teams do for speed.)","**Also test migrations** — use `MigrationTestHelper` with exported schemas to verify migrations preserve data (covered in the Room topic). Migration tests are critical since they touch real user data."]}]},{heading:"Compose UI testing",blocks:[{t:"p",text:"Compose UI tests use `createComposeRule()` (or `createAndroidComposeRule<Activity>()`), which sets your composable content and lets you interact with it. The pattern is **find → act → assert** against the *semantics tree* (Compose's parallel description of the UI that both accessibility and tests read)."},{t:"code",title:"A Compose UI test",code:`@get:Rule val composeRule = createComposeRule()

@Test fun clickingIncrement_updatesCount() {
    composeRule.setContent { AppTheme { Counter() } }

    composeRule.onNodeWithText("Count: 0").assertIsDisplayed()   // find + assert
    composeRule.onNodeWithText("Increment").performClick()        // act
    composeRule.onNodeWithText("Count: 1").assertExists()         // assert
}`},{t:"list",items:['**Finders** — `onNodeWithText`, `onNodeWithContentDescription`, `onNodeWithTag` (paired with `Modifier.testTag("...")`), `onNode(matcher)` with rich matchers (`hasText`, `isEnabled`, `hasClickAction`).',"**Actions** — `performClick`, `performTextInput`, `performScrollTo`, `performTouchInput { swipeUp() }`.","**Assertions** — `assertIsDisplayed`, `assertExists`, `assertIsEnabled`, `assertTextEquals`.","**Auto-synchronization** — the test automatically waits for recomposition and Compose-driven animations to settle before each assertion, so you rarely need manual waits (a big convenience over Espresso's idling resources). For indefinite animations, control the clock (`mainClock.autoAdvance = false`).","**Test stateless content composables** with fake state + lambdas for fast, focused UI tests; reserve full route+ViewModel tests for integration."]}]},{heading:"Espresso and the View system",blocks:[{t:"p",text:"**Espresso** is the classic UI testing framework for the *View* system (XML-based UIs), still relevant for legacy screens and hybrid apps. It follows the same find-act-assert idea with a different API: `onView(matcher)` to find a View, `perform(action)` to interact, `check(assertion)` to verify."},{t:"code",title:"An Espresso test",code:`@Test fun login_showsError_onEmptyPassword() {
    onView(withId(R.id.username)).perform(typeText("user"))
    onView(withId(R.id.loginButton)).perform(click())
    onView(withText("Password required")).check(matches(isDisplayed()))
}`},{t:"list",items:["**`onView(withId(...))` / `withText(...)`** find a View; **`perform(click()/typeText(...))`** interact; **`check(matches(...))`** assert. `onData` handles AdapterView/list items.","**Idling resources** — Espresso synchronizes with the main thread automatically, but for background async work you register `IdlingResource`s so it waits (the clunky part Compose testing improved on).","For a Compose app you use Compose testing; for a View app, Espresso; for a hybrid, both (Espresso can drive into `ComposeView` content via interop)."]}]},{heading:"Robolectric, screenshot tests, and CI",blocks:[{t:"list",items:["**Robolectric** — runs Android framework code on the *JVM* by providing a simulated Android environment. This lets you test Android-dependent code (some `Context` usage, resources, even Compose/DAO tests) *without a device*, so they run fast in CI as 'local' tests. The trade-off: it's a *simulation*, so it may not perfectly match real device behavior — for genuine device fidelity you still need instrumented tests. Robolectric is great for the middle ground (framework-touching logic that doesn't need a real device).","**Screenshot testing** (Paparazzi by Cash App, Roborazzi, or Compose Preview screenshot tests) — renders composables/screens to images and compares against a stored 'golden' reference, catching *visual* regressions (a broken layout, wrong color) without a device. Increasingly standard for design-system components. Paparazzi runs on the JVM (no device), which is a big advantage.","**CI (Continuous Integration)** — run tests automatically on every push/PR (GitHub Actions, Bitrise, etc.). Fast JVM unit tests run on every PR; slower instrumented/UI tests run on emulators (or a device farm like Firebase Test Lab) — often on a schedule or pre-merge, given their cost. The pyramid shape matters here: a fast unit suite gives quick PR feedback; the slow UI layer runs less often.","**Test coverage** — tools (JaCoCo, Kover) measure what percentage of code tests exercise. Useful as a *signal* (uncovered critical logic is a gap) but not a goal in itself — 100% coverage of trivial code with no meaningful assertions is worthless; coverage of critical logic with good assertions is what matters."]},{t:"note",text:"Android testing: ViewModels — inject fakes, assert state (the pyramid's base, needs MainDispatcherRule). Room DAOs — instrumented tests with an in-memory database (real SQLite); test migrations too. Compose UI — createComposeRule, find (onNodeWithText/Tag) → act (performClick) → assert (assertIsDisplayed) against the semantics tree, auto-synced. Espresso — the View-system equivalent (onView/perform/check, idling resources). Robolectric runs Android on the JVM (fast, simulated); screenshot tests (Paparazzi) catch visual regressions; CI runs unit tests per-PR, UI tests less often."}]}],OC=[{level:"junior",q:"How do you test a ViewModel?",a:[{t:"p",text:"**You inject a fake repository, call the ViewModel's functions, and assert its exposed state — no device needed, because a well-designed ViewModel is a plain framework-free class taking dependencies via constructor.** ViewModels are the ideal unit-test target since they hold the presentation logic and are cheaply testable."},{t:"code",title:"The pattern",code:`@get:Rule val mainDispatcherRule = MainDispatcherRule()

@Test fun test() = runTest {
    val vm = FeedViewModel(FakeRepository(articles = listOf(article)))
    vm.uiState.test {
        assertEquals(FeedUiState.Loading, awaitItem())
        vm.refresh()
        assertEquals(FeedUiState.Content(listOf(article)), awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**Inject a fake**, invoke functions, assert the emitted `UiState` — this verifies behavior: given this data/input, the ViewModel produces this state.","**Needs the `MainDispatcherRule`** (because `viewModelScope` uses `Dispatchers.Main`, absent on the JVM) and, for flows, Turbine + virtual time.","**Assert state, not method calls** — check what the ViewModel *produces*, not which repository methods it called; prefer fakes over mocks so tests survive refactoring."]},{t:"p",text:"This is where most of your behavior coverage should come from — ViewModels concentrate the logic and test cheaply on the JVM, so thorough ViewModel tests give high confidence for low cost. It's the base of the testing pyramid. The main setup beyond constructing the ViewModel is the Main dispatcher rule and, for observing state flows, collecting with Turbine."}]},{level:"junior",q:"How do you test a Room DAO?",a:[{t:"p",text:"**You test DAOs against a *real* in-memory database using instrumented tests, because you can't meaningfully fake SQL — you need real SQLite to verify your queries.** You create an in-memory Room database (fast, discarded after the test), run your DAO operations, and assert the results."},{t:"code",title:"In-memory database test",code:`@Before fun setup() {
    db = Room.inMemoryDatabaseBuilder(context, AppDatabase::class.java).build()
    dao = db.userDao()
}
@Test fun insertAndRetrieve() = runTest {
    dao.insert(UserEntity("1", "Rev"))
    assertEquals("Rev", dao.getUser("1")?.name)
}`},{t:"list",items:["**`Room.inMemoryDatabaseBuilder`** creates a real Room/SQLite database that lives only in memory — fast, isolated (fresh per test), and discarded afterward. This exercises your actual queries against real SQLite behavior.","**Instrumented** — these run on a device/emulator because they need the real SQLite engine (though you can run them on the JVM via Robolectric for speed).","**Also test migrations** — use `MigrationTestHelper` with exported schemas to verify migrations preserve data; these are critical because migrations touch real user data."]},{t:"p",text:"The key insight is that database queries are exactly the kind of thing you can't unit-test with a fake — the whole point is verifying that your SQL and Room annotations produce correct results against real SQLite. So DAO tests are a legitimate use of an integration/instrumented test with a real (in-memory) database, testing the query logic itself. Above the DAO, the repository can be tested with a fake DAO (unit test), and the DAO's own correctness is verified with these database tests."}]},{level:"junior",q:"How do you write a Compose UI test?",a:[{t:"p",text:"**You use `createComposeRule()`, set your composable content, then follow find → act → assert against the semantics tree.** The test finds UI nodes, performs actions on them, and asserts on their state."},{t:"code",title:"Find, act, assert",code:`@get:Rule val rule = createComposeRule()

@Test fun test() {
    rule.setContent { AppTheme { Counter() } }
    rule.onNodeWithText("Count: 0").assertIsDisplayed()   // find + assert
    rule.onNodeWithText("Increment").performClick()        // act
    rule.onNodeWithText("Count: 1").assertExists()         // assert
}`},{t:"list",items:["**Finders** — `onNodeWithText`, `onNodeWithContentDescription`, `onNodeWithTag` (with `Modifier.testTag`), or `onNode(matcher)` with matchers like `hasText`, `isEnabled`.","**Actions** — `performClick`, `performTextInput`, `performScrollTo`, `performTouchInput { swipeUp() }`.","**Assertions** — `assertIsDisplayed`, `assertExists`, `assertIsEnabled`, `assertTextEquals`.","**Auto-synchronization** — the test automatically waits for recomposition and Compose animations to settle before each assertion, so you rarely need manual waits (a big improvement over Espresso's idling resources)."]},{t:"p",text:"Compose tests query the *semantics tree* — the same parallel description of the UI that accessibility services read — which is why writing accessible UI (content descriptions, semantics) also makes it testable. For fast, focused UI tests, test *stateless content composables* by passing fake state and lambdas directly (they settle instantly, no async); reserve full route + ViewModel + navigation tests for the few integration cases that need them."}]},{level:"junior",q:"What is Robolectric and when do you use it?",a:[{t:"p",text:"**Robolectric runs Android framework code on the JVM by providing a *simulated* Android environment — so you can test Android-dependent code without deploying to a real device or emulator, making those tests fast enough to run as 'local' JVM tests.** It fills in fake implementations of Android classes (Context, resources, etc.) so framework-touching code can run in a plain JVM test."},{t:"list",items:["**Use it for** framework-dependent logic that you want to test *fast* without a device — code that uses `Context`, resources, or some Android APIs, and increasingly even Room DAO tests or Compose tests (Robolectric can run them on the JVM for speed).","**The trade-off** — it's a *simulation* of Android, not the real thing, so its behavior may not perfectly match a real device. For genuine device fidelity (real rendering, real system behavior, real hardware), you still need instrumented tests on an actual device/emulator.","**It occupies the middle ground** — faster than instrumented tests (no device), but more realistic than pure JVM tests that have no Android at all."]},{t:"p",text:"So the decision is: pure logic with no Android → plain JVM unit test (fastest); Android-touching logic you want fast → Robolectric (JVM, simulated Android); genuine device behavior needed (real UI rendering, hardware, exact system behavior) → instrumented test on a device. Robolectric lets you pull some framework-dependent tests down from the slow instrumented layer into the fast local layer, which speeds up CI — but you accept that it's an approximation, so critical device-specific behavior should still be verified with real instrumented tests."}]},{level:"senior",q:"What is a screenshot test and where does it fit in your testing strategy?",a:[{t:"p",text:"**A screenshot (or 'snapshot') test renders a composable or screen to an image and compares it against a stored reference image (the 'golden'), failing if they differ. It catches *visual* regressions — a broken layout, wrong spacing, incorrect color, a component that renders differently after a change — which functional tests (that check behavior and semantics) don't catch.**"},{t:"list",items:["**What it verifies that other tests don't**: functional UI tests assert *behavior and structure* ('the button exists and is clickable', 'the text says X') but not *appearance* — a layout could be visually broken (overlapping, wrong padding, clipped text) while all semantic assertions pass. Screenshot tests catch exactly these visual regressions by comparing pixels.","**Tools**: **Paparazzi** (Cash App) renders composables to images *on the JVM* — no device needed, so it's fast and CI-friendly, a major advantage. **Roborazzi** does similar via Robolectric. **Compose Preview screenshot testing** (Google's official tooling) generates screenshots from `@Preview` functions. Older approaches used instrumented tests capturing device screenshots (slower).","**How it works**: you write a test that renders the component with specific state, generate the golden image once (reviewed and committed), and thereafter every run compares against it. A visual change fails the test, showing a diff; if the change is intentional, you *re-record* the golden."]},{t:"list",items:["**Where it fits — especially design systems and components**: screenshot tests shine for *design-system components* and reusable UI, where you want to guarantee that a `Button`, `Card`, or `Chip` renders identically across changes and in different states (enabled/disabled, light/dark, different sizes). They're increasingly standard for component libraries. They also catch theme regressions (a color-scheme change breaking contrast) and layout regressions across screen sizes/font scales.","**Trade-offs and cautions**: screenshot tests can be *brittle* — they fail on *any* pixel difference, including intentional cosmetic changes (which then require re-recording goldens) and, if not careful, on font-rendering or anti-aliasing differences across environments (which is why JVM-based rendering like Paparazzi, with a consistent environment, is preferred over device screenshots that vary). They also don't test *behavior* — a screenshot can be pixel-perfect while the button does nothing. So they *complement* functional tests, not replace them.","**In the pyramid**: they're a specialized layer — cheaper than full UI tests (JVM-based ones need no device) but focused purely on visual correctness. Use them for design-system components and key screens' visual states, alongside unit tests for logic and a thin layer of functional UI tests for critical flows."]},{t:"p",text:"**The senior framing**: screenshot testing fills a specific gap — *visual* correctness — that behavior-based tests structurally cannot cover, and it's become practical and fast thanks to JVM rendering (Paparazzi/Roborazzi) that needs no device. It's most valuable for design systems and reusable components, where visual consistency across changes and states is a real requirement, and for catching theme/layout regressions. The mature view is that it's a *complement*: it verifies 'does it look right', while unit tests verify 'does the logic work' and functional UI tests verify 'does the flow work' — three different questions, and a robust strategy uses screenshot tests specifically for the visual one, being mindful of their brittleness (re-recording goldens on intentional changes, consistent rendering environments). Knowing when to reach for them (component libraries, visual regression) and their limits (don't test behavior, can be pixel-brittle) shows a well-rounded testing perspective."}]},{level:"senior",q:"How would you structure the test suite for a feature, and integrate it into CI?",a:[{t:"p",text:"**I'd structure it as a pyramid mapped onto the architecture's layers — thorough fast unit tests for logic, targeted integration tests for component wiring, and a thin layer of UI tests for the critical flow — then wire it into CI so fast tests gate every PR and slow tests run appropriately.** Concretely, for a typical feature (say a search screen):**"},{t:"list",items:["**Unit tests (the bulk, JVM, fast)**: the *ViewModel* (inject a fake repository, assert `UiState` for each input — loading, results, empty, error; needs MainDispatcherRule + Turbine); *use cases / domain logic* (pure functions, straightforward assertions); *mappers* (DTO→domain→UI, plain input/output tests); *repository* (with a fake DAO and fake API, testing the caching/merge logic). These cover the vast majority of behavior, run in milliseconds, and pinpoint failures.","**Integration tests (some)**: the *DAO* against an in-memory Room database (real SQLite, verifying queries and migrations); the *repository against a real DAO* (verifying the actual DB integration, not just a fake); optionally a *serialization* round-trip test (real JSON → DTO). These catch wiring/integration bugs unit tests with fakes miss.","**UI tests (few, targeted)**: a *Compose UI test* of the screen's stateless content with fake states (fast — verifies the UI renders each state correctly: shows results, shows empty state, shows error), plus *one end-to-end test of the critical flow* (search → see results → tap → detail) through the real ViewModel and navigation for integration confidence. Keep these minimal — they're the slow, brittle layer.","**Screenshot tests (specialized)**: for any reusable components the feature introduces, a Paparazzi test locking their visual appearance across states."]},{t:"list",items:["**CI integration — gate by speed**: run the *fast JVM unit tests on every push/PR* (they finish in seconds/a couple minutes, giving quick feedback — this is the gate that must be green to merge). Run *instrumented/UI tests* less frequently and on emulators or a device farm (Firebase Test Lab, Gradle Managed Devices) — pre-merge for critical suites, or on a schedule/nightly given their cost and flakiness. This mirrors the pyramid: the fast base gates everything, the slow top runs selectively.","**Make CI reliable**: quarantine or fix flaky tests immediately (a flaky suite that fails randomly trains people to ignore CI); run instrumented tests on consistent managed emulators (not developer machines) to reduce environment flakiness; parallelize test execution to keep wall-clock time down.","**Coverage as a signal, not a target**: measure coverage (Kover/JaCoCo) to *find gaps* in critical logic, but don't chase a coverage number — 100% coverage with weak assertions is worthless; meaningful assertions on critical paths are what matter. Perhaps enforce a floor for new code to prevent untested additions, without dogmatically requiring a global percentage.","**Fast local loop**: developers should be able to run the unit tests locally in seconds before pushing (the pyramid's base enables this), catching most issues before CI even runs."]},{t:"p",text:"**The senior framing**: structuring a feature's tests is about *mapping the pyramid onto the architecture* — each layer (ViewModel, use case, repository, DAO, UI) gets the *cheapest* test that meaningfully verifies it (unit with fakes where possible, integration with real infrastructure where the integration itself is the point like DAOs, UI tests only for critical flows), producing mostly fast tests and few slow ones. CI then *gates on the fast tests* (quick PR feedback, must be green) and *runs the slow ones selectively* (emulators/device farm, pre-merge or scheduled), with discipline around flakiness and coverage-as-signal. The insight that ties it together is that good architecture (DI, framework-free logic, separated layers) is what *makes* this structure possible — testable code lets most tests be fast unit tests, which is what makes the whole strategy sustainable. Being able to lay out the layer-by-layer test plan *and* the CI gating strategy — and connect both back to architecture and the pyramid — is the comprehensive answer."}]},{level:"junior",q:"What is the difference between local (unit) and instrumented tests?",a:[{t:"p",text:"*Local* (unit) tests live in `src/test/`, run on the *JVM* (your machine), are *fast*, and can't use the Android framework (unless with Robolectric). *Instrumented* tests live in `src/androidTest/`, run on a *real device/emulator*, are *slower*, and *can* use the framework (real Room, Espresso, Compose UI tests). Put logic tests (ViewModels, use cases) in local; UI/DB/integration tests in instrumented."},{t:"list",items:["**Local (`src/test/`)** — JVM, fast, no framework (or Robolectric).","**Instrumented (`src/androidTest/`)** — device/emulator, slower, full framework.","**Local** — ViewModels, use cases, logic.","**Instrumented** — UI, Room, Espresso, integration."]},{t:"note",text:"Local (unit) tests: src/test/, run on the JVM, fast, no Android framework (or Robolectric). Instrumented tests: src/androidTest/, run on a device/emulator, slower, full framework (Room/Espresso/Compose UI). Put logic (ViewModels/use cases) in local; UI/DB/integration in instrumented."}]},{level:"junior",q:"How do you find and interact with composables in a Compose UI test?",a:[{t:"p",text:"Compose tests work on the *semantics tree*: *find* nodes with `onNodeWithText`/`onNodeWithContentDescription`/`onNodeWithTag`, *assert* with `assertIsDisplayed`/`assertTextEquals`/`assertIsEnabled`, and *act* with `performClick`/`performTextInput`/`performScrollTo`. `testTag` (via `Modifier.testTag`) gives a stable handle independent of visible text. Tests auto-synchronize (wait for idle) before each action."},{t:"code",title:"Find/act/assert",code:`composeTestRule.onNodeWithTag("email").performTextInput("a@b.com")
composeTestRule.onNodeWithText("Submit").performClick()
composeTestRule.onNodeWithText("Welcome").assertIsDisplayed()`},{t:"list",items:["**Finders** — `onNodeWithText`/`ContentDescription`/`Tag`.","**Assertions** — `assertIsDisplayed`/`assertTextEquals`.","**Actions** — `performClick`/`performTextInput`/`performScrollTo`.","**`testTag`** — stable handle; auto-sync before actions."]},{t:"note",text:"Compose tests operate on the semantics tree: find (onNodeWithText/ContentDescription/Tag), assert (assertIsDisplayed/assertTextEquals/assertIsEnabled), act (performClick/performTextInput/performScrollTo). Modifier.testTag gives a stable handle independent of visible text. Tests auto-wait for idle before actions."}]},{level:"senior",q:"What is createComposeRule vs createAndroidComposeRule?",a:[{t:"p",text:"`createComposeRule()` hosts composables in a test *without* a specific Activity — lighter/faster, for self-contained composable tests. `createAndroidComposeRule<MyActivity>()` launches a real *Activity* — needed when the composable depends on the Activity (Hilt injection, `LocalContext` specifics, or the Activity already sets the content). Call `setContent { }` to render the composable under test."},{t:"code",title:"Compose rules",code:`@get:Rule val rule = createComposeRule()
@Test fun greets() {
    rule.setContent { AppTheme { Greeting("Sam") } }
    rule.onNodeWithText("Hello Sam").assertIsDisplayed()
}`},{t:"list",items:["**`createComposeRule()`** — no Activity; light, self-contained.","**`createAndroidComposeRule<A>()`** — launches Activity A.","**`setContent { }`** — render the composable.","**Android variant** — for Hilt/Context/Activity-set content."]},{t:"note",text:"createComposeRule() hosts composables without an Activity (light, self-contained tests); createAndroidComposeRule<A>() launches a real Activity (for Hilt/Context-dependent UI or Activity-set content). Call rule.setContent { AppTheme { … } } to render — wrap in the theme, pass fakes."}]},{level:"senior",q:"How does test synchronization work in Compose tests?",a:[{t:"p",text:"Compose tests *auto-synchronize* — each assertion/action first waits for the app to be *idle* (no pending recomposition or animation). For async data, use `waitUntil { condition }` (poll with a timeout, no `Thread.sleep`). For animations, control the clock: `mainClock.autoAdvance = false` + `advanceTimeBy` to assert mid-animation. Infinite animations keep the tree never-idle, so you must control the clock or the test hangs."},{t:"list",items:["**Auto-sync** — waits for idle before each action.","**`waitUntil { }`** — poll for async conditions.","**`mainClock`** — control animation timing.","**Infinite animations** — never idle; must control the clock."]},{t:"note",text:"Compose tests auto-synchronize (wait for idle — no pending recomposition/animation — before each action). For async: waitUntil { condition } (poll, no Thread.sleep). For animations: mainClock.autoAdvance = false + advanceTimeBy. Infinite animations never idle — control the clock or the test hangs."}]},{level:"senior",q:"How do you test a Compose screen that uses a ViewModel?",a:[{t:"p",text:"Inject a *fake ViewModel* (or a real one with fakes) exposing a controllable `StateFlow`, set it as the screen's content, and drive the UI by emitting states — asserting the UI reflects each. Push Loading → Content → Error and assert the rendered UI per state. Verify the ViewModel's functions are called on interactions (via a spy/fake). Keep it a UI test of the screen; unit-test the ViewModel separately."},{t:"code",title:"Screen + fake VM",code:`val state = MutableStateFlow<UiState>(UiState.Loading)
rule.setContent { ProductScreen(viewModel = FakeVm(state)) }
rule.onNodeWithTag("spinner").assertIsDisplayed()
state.value = UiState.Content(product)
rule.onNodeWithText(product.name).assertIsDisplayed()`},{t:"list",items:["**Fake VM** — controllable `StateFlow`.","**Emit states** — Loading → Content → Error; assert UI.","**Verify interactions** — VM functions called on clicks.","**Separate** — unit-test the VM logic elsewhere."]},{t:"note",text:"Test a Compose screen with a fake ViewModel exposing a controllable StateFlow: set it as content, emit states (Loading → Content → Error), assert the UI per state, verify VM functions are called on interactions. Keep it a UI test of the screen; unit-test the ViewModel logic separately."}]},{level:"senior",q:"How do you handle semantics merging in Compose tests?",a:[{t:"p",text:"Compose *merges* a composable's semantics with its descendants when a parent sets `mergeDescendants` (buttons, list items do this) — so a button reads as one node to tests and TalkBack. This means a child's text/testTag can be hidden inside the merged node. Tests use the *merged* tree by default; to assert on a specific child, request the *unmerged* tree with `useUnmergedTree = true`."},{t:"code",title:"Unmerged tree",code:'rule.onNode(hasTestTag("badge"), useUnmergedTree = true).assertIsDisplayed()'},{t:"list",items:["**Merging** — parent combines descendant semantics (Button/ListItem).","**Hidden children** — child text/tag subsumed in the merged node.","**`useUnmergedTree = true`** — reach specific children.","**a11y parallel** — merging aids TalkBack too."]},{t:"note",text:"Compose merges a composable's semantics with descendants when a parent sets mergeDescendants (Button/ListItem) — one node for tests/TalkBack, hiding child text/tags. Tests use the merged tree by default; use useUnmergedTree = true to assert on specific children. Semantics that aid testing also aid accessibility."}]},{level:"senior",q:"How do you test navigation in a Compose app?",a:[{t:"p",text:"Set up a `TestNavHostController`, render your `NavHost` with it, perform the UI action, and assert `navController.currentBackStackEntry?.destination?.route` matches the expected destination (and arguments). Alternatively, assert the destination screen's content is displayed (more behavioral). This verifies the click→navigation wiring; test screen content separately."},{t:"code",title:"Navigation test",code:`lateinit var navController: TestNavHostController
rule.setContent {
    navController = TestNavHostController(LocalContext.current).apply {
        navigatorProvider.addNavigator(ComposeNavigator()) }
    AppNavHost(navController)
}
rule.onNodeWithText("Open").performClick()
assertThat(navController.currentBackStackEntry?.destination?.route).isEqualTo("detail/{id}")`},{t:"list",items:["**`TestNavHostController`** — controllable nav controller.","**Perform action** — then assert the route.","**Or assert content** — destination screen displayed.","**Wiring** — click→navigation; content tested separately."]},{t:"note",text:"Test navigation with a TestNavHostController (add ComposeNavigator): render the NavHost with it, perform the click, assert currentBackStackEntry?.destination?.route (and args) — or assert the destination content is displayed. Verifies click→navigation wiring; test screen content separately."}]},{level:"senior",q:"How do you provide custom semantics for testing a custom composable?",a:[{t:"p",text:"Custom-drawn or gesture-based composables (raw `Canvas`/`pointerInput`) have *no built-in semantics*, so tests (and TalkBack) can't find them. Add `Modifier.semantics { }` with `contentDescription`, `role`, `stateDescription`, `testTag`, or custom actions — making the composable both *testable* and *accessible*. Well-designed semantics serve both purposes."},{t:"code",title:"Custom semantics",code:`Box(Modifier.pointerInput(Unit) { detectTapGestures { toggle() } }
    .semantics { role = Role.Switch; stateDescription = if (on) "On" else "Off"; testTag = "wifiToggle" })`},{t:"list",items:["**Custom composables** — no built-in semantics.","**`Modifier.semantics { }`** — contentDescription/role/state/testTag.","**Testable + accessible** — same metadata serves both.","**`clearAndSetSemantics`** — present a subtree as one node."]},{t:"note",text:"Custom-drawn/gesture composables (Canvas/pointerInput) lack built-in semantics, so add Modifier.semantics { } (contentDescription/role/stateDescription/testTag/custom actions) — making them testable AND accessible. clearAndSetSemantics presents a subtree as one node. Good semantics serve both tests and TalkBack."}]},{level:"junior",q:"How do you test a Compose list (LazyColumn)?",a:[{t:"p",text:"Because a `LazyColumn` only composes *visible* items, tests must *scroll* to off-screen items before asserting them: `onNodeWithText(...).performScrollTo()` or `onNode(...).performScrollToIndex(n)` on the list. Assert visible items directly. Give items stable `testTag`s or use their text. This handles the lazy nature — you can't assert an item that isn't composed yet."},{t:"code",title:"Scrolling to an item",code:`rule.onNodeWithTag("list").performScrollToIndex(20)
rule.onNodeWithText("Item 20").assertIsDisplayed()`},{t:"list",items:["**Lazy = only visible composed** — scroll to off-screen items.","**`performScrollTo`/`performScrollToIndex`** — bring into view.","**Assert visible items** — directly.","**Stable tags/text** — to find items."]},{t:"note",text:"A LazyColumn only composes visible items, so scroll to off-screen ones before asserting: onNodeWithText(...).performScrollTo() or performScrollToIndex(n) on the list. Assert visible items directly. Use stable testTags/text. You can't assert an item that isn't composed yet."}]},{level:"senior",q:"How do you use @Preview to create screenshot tests?",a:[{t:"p",text:"Compose Preview Screenshot Testing turns your existing `@Preview` composables into screenshot tests — you add the plugin, mark previews for testing, and Gradle tasks *generate* golden images (`updateDebugScreenshotTest`) and *verify* against them (`validateDebugScreenshotTest`). This reuses the previews you already write for design, giving low-effort visual regression coverage across states (light/dark, different data)."},{t:"code",title:"Preview screenshot test",code:`@Preview(showBackground = true)
@Composable fun ProfileCardPreview() { AppTheme { ProfileCard(sampleUser) } }
// ./gradlew updateDebugScreenshotTest  (record)
// ./gradlew validateDebugScreenshotTest (verify)`},{t:"list",items:["**Reuse `@Preview`s** — as screenshot tests.","**Record** — `updateScreenshotTest` generates goldens.","**Verify** — `validateScreenshotTest` diffs.","**Low effort** — cover states you already preview."]},{t:"note",text:"Compose Preview Screenshot Testing turns @Preview composables into screenshot tests — add the plugin, record goldens (updateScreenshotTest) and verify (validateScreenshotTest) via Gradle. Reuses previews you write for design → low-effort visual regression coverage across states (light/dark, different data)."}]},{level:"senior",q:"How do you test animations in Compose?",a:[{t:"p",text:"Compose tests use a *controllable animation clock*. By default it auto-advances and `waitForIdle` waits for animations to finish (assert the end state). To assert *mid-animation*, set `mainClock.autoAdvance = false` and step with `mainClock.advanceTimeBy(ms)` to specific points. Infinite animations never let the tree idle, so you *must* control the clock manually or the test hangs."},{t:"code",title:"Animation clock",code:`rule.mainClock.autoAdvance = false
rule.onNodeWithText("Expand").performClick()
rule.mainClock.advanceTimeBy(150)   // mid-animation
// assert intermediate state
rule.mainClock.advanceTimeBy(1000)  // finish`},{t:"list",items:["**Controllable clock** — auto-advance or manual.","**End state** — auto-advance + waitForIdle.","**Mid-animation** — `autoAdvance = false` + `advanceTimeBy`.","**Infinite animations** — must control the clock or hang."]},{t:"note",text:"Compose tests use a controllable animation clock: default auto-advances (waitForIdle waits for animations — assert end state); mainClock.autoAdvance = false + advanceTimeBy asserts mid-animation. Infinite animations never idle — you MUST control the clock or the test hangs."}]},{level:"senior",q:"How do you write Espresso tests for View-based UI?",a:[{t:"p",text:"Espresso tests View-based UI: `onView(withId(R.id.button))` finds a view, `.perform(click())` acts, `.check(matches(isDisplayed()))` asserts. It auto-synchronizes with the UI thread (waits for idle). Use `IdlingResource` for async work Espresso can't see. For interacting with the system UI (dialogs, other apps, notifications), use `UIAutomator`. Espresso is for in-app View testing; Compose has its own test API."},{t:"code",title:"Espresso",code:`onView(withId(R.id.email)).perform(typeText("a@b.com"))
onView(withText("Submit")).perform(click())
onView(withText("Welcome")).check(matches(isDisplayed()))`},{t:"list",items:["**`onView(matcher)`** — find a view.","**`.perform(action)`** — click/type/scroll.","**`.check(matches(...))`** — assert.","**`IdlingResource`/UIAutomator** — async / system UI."]},{t:"note",text:"Espresso tests Views: onView(matcher).perform(action).check(matches(...)) — auto-syncs with the UI thread; IdlingResource for invisible async work. UIAutomator for system UI (dialogs, other apps, notifications). Espresso for in-app Views; Compose has its own test API (both can coexist)."}]},{level:"junior",q:"How do you test a Room database with migrations?",a:[{t:"p",text:"For DAO logic, use an *in-memory* database (`Room.inMemoryDatabaseBuilder`) in instrumented tests — insert data, run queries, assert. For *migrations*, use `MigrationTestHelper` (with exported schemas): create the DB at the old version, insert data, run `runMigrationsAndValidate`, and assert the schema and data survived. Both need instrumented tests (or Robolectric) since Room needs the SQLite runtime."},{t:"list",items:["**In-memory DB** — fast DAO tests (insert/query/assert).","**`MigrationTestHelper`** — test migrations against exported schemas.","**Create old → migrate → validate** — assert data/schema.","**Instrumented/Robolectric** — Room needs SQLite runtime."]},{t:"note",text:"DAO tests: Room.inMemoryDatabaseBuilder (fast, insert/query/assert). Migration tests: MigrationTestHelper (exported schemas) — create at old version, insert data, runMigrationsAndValidate, assert schema+data survived. Both need instrumented tests (or Robolectric) — Room needs the SQLite runtime."}]},{level:"senior",q:"How do you use Hilt in instrumented tests?",a:[{t:"p",text:"Annotate the test `@HiltAndroidTest`, use a `HiltAndroidRule` (to inject), a custom test runner (`HiltTestRunner` extending `AndroidJUnitRunner` with `HiltTestApplication`), and swap dependencies with `@BindValue` (bind a fake) or `@UninstallModules` (remove a production module, provide a test one). This lets instrumented tests run against the real DI graph with test doubles substituted."},{t:"code",title:"Hilt test",code:`@HiltAndroidTest
class ScreenTest {
    @get:Rule(order = 0) val hiltRule = HiltAndroidRule(this)
    @get:Rule(order = 1) val composeRule = createAndroidComposeRule<MainActivity>()
    @BindValue val repo: Repository = FakeRepository()
    @Before fun setup() { hiltRule.inject() }
}`},{t:"list",items:["**`@HiltAndroidTest` + `HiltAndroidRule`** — test graph + inject.","**Custom runner** — `HiltTestApplication`.","**`@BindValue`/`@UninstallModules`** — swap dependencies.","**Rule order** — Hilt rule before the Compose/Activity rule."]},{t:"note",text:"Hilt instrumented tests: @HiltAndroidTest, HiltAndroidRule (inject), a custom runner with HiltTestApplication, and @BindValue (bind a fake) / @UninstallModules (replace a production module). Order the Hilt rule before the Compose/Activity rule. Runs against the real DI graph with test doubles."}]},{level:"senior",q:"How do you run instrumented tests efficiently (Firebase Test Lab, Gradle Managed Devices)?",a:[{t:"p",text:"Instrumented tests are slow and need devices — run them efficiently with *Firebase Test Lab* (run on a *matrix* of real/virtual devices in the cloud, catching device-specific issues) or *Gradle Managed Devices* (define emulators in Gradle; the build spins them up, runs tests, tears down — reproducible in CI). Both let you test across API levels/devices without maintaining physical hardware, integrated into CI."},{t:"list",items:["**Firebase Test Lab** — cloud device matrix; real device coverage.","**Gradle Managed Devices** — emulators defined in Gradle; reproducible.","**Across API levels/devices** — without physical hardware.","**CI integration** — automated instrumented runs."]},{t:"note",text:"Run instrumented tests efficiently with Firebase Test Lab (cloud device matrix — real device coverage across API levels) or Gradle Managed Devices (emulators defined in Gradle, auto spun-up/torn-down — reproducible CI). Both test across devices without maintaining physical hardware, integrated into CI."}]},{level:"junior",q:"How do you test text input and form validation in Compose?",a:[{t:"p",text:'Use `performTextInput("...")` on the `TextField` node, then assert the resulting state — an error message appears (`onNodeWithText("invalid email").assertIsDisplayed()`), the submit button enables/disables (`assertIsEnabled`/`assertIsNotEnabled`), or the value updates. Test both valid and invalid inputs and the boundary between them. Use `testTag`s on fields for reliable selection.'},{t:"code",title:"Form test",code:`rule.onNodeWithTag("email").performTextInput("invalid")
rule.onNodeWithText("Enter a valid email").assertIsDisplayed()
rule.onNodeWithText("Submit").assertIsNotEnabled()`},{t:"list",items:["**`performTextInput`** — type into a field.","**Assert validation** — error messages, enabled state.","**Valid + invalid** — and the boundary.","**`testTag`s** — reliable field selection."]},{t:"note",text:"Test forms with performTextInput on the TextField node, then assert the resulting state: error messages (onNodeWithText.assertIsDisplayed), submit enabled/disabled (assertIsEnabled/assertIsNotEnabled), value updates. Test valid + invalid inputs and the boundary. Use testTags for reliable field selection."}]},{level:"senior",q:"What is the difference between testing recomposition and testing behavior?",a:[{t:"p",text:"*Behavior* testing (the norm) verifies *what the user sees/can do* — the UI shows the right content, responds to clicks — via the semantics tree, robust to implementation. *Recomposition* testing (rare, performance-focused) checks *how often* composables recompose — done via the Layout Inspector's recomposition counts or Compose benchmark tests, not the standard test API. UI tests assert behavior; performance tools measure recomposition."},{t:"list",items:["**Behavior** — what the user sees/does (semantics; robust).","**Recomposition** — how often composables recompose (performance).","**UI tests** — assert behavior.","**Perf tools** — Layout Inspector/benchmarks for recomposition."]},{t:"note",text:"Behavior testing (the norm) verifies what the user sees/does via the semantics tree (robust). Recomposition testing (rare, performance) checks how often composables recompose — via Layout Inspector counts or Compose benchmarks, not the standard test API. UI tests assert behavior; perf tools measure recomposition."}]},{level:"senior",q:"How do you test a feature end-to-end within the app?",a:[{t:"p",text:"Write an integration/UI test that exercises the *feature's full slice* — launch the screen (with a fake backend via MockWebServer or fake repositories, not the real network), perform the user flow (type, click, navigate), and assert the outcomes at each step. Use Hilt to swap in fakes, Compose/Espresso to drive the UI, and assert both UI state and side effects. This catches wiring bugs unit tests miss, without full E2E fragility."},{t:"list",items:["**Feature slice** — full flow with fakes (MockWebServer/fake repos).","**Drive the UI** — Compose/Espresso through the flow.","**Assert at each step** — UI state + side effects.","**Hilt swaps fakes** — catches wiring bugs, less fragile than E2E."]},{t:"note",text:"Test a feature end-to-end (within the app) with an integration/UI test: launch the screen with fakes (MockWebServer/fake repos, not real network), perform the user flow (type/click/navigate), assert outcomes at each step. Hilt swaps in fakes; Compose/Espresso drives the UI. Catches wiring bugs without full E2E fragility."}]},{level:"junior",q:"How do you assert a composable does NOT exist or is hidden?",a:[{t:"p",text:"Use `assertDoesNotExist()` (the node isn't in the tree at all) or `assertIsNotDisplayed()` (present but not visible). For example, after dismissing a dialog, assert it `assertDoesNotExist()`; a scrolled-off item might exist but `assertIsNotDisplayed()`. Testing *absence* verifies conditional UI (an error message *shouldn't* show when input is valid)."},{t:"code",title:"Asserting absence",code:`rule.onNodeWithText("Error").assertDoesNotExist()      // not in the tree
rule.onNodeWithTag("offscreen").assertIsNotDisplayed()  // present but hidden`},{t:"list",items:["**`assertDoesNotExist()`** — not in the tree.","**`assertIsNotDisplayed()`** — present but not visible.","**Conditional UI** — error shouldn't show when valid.","**Test absence** — not just presence."]},{t:"note",text:"assertDoesNotExist() (node not in the tree — e.g. dismissed dialog) vs assertIsNotDisplayed() (present but not visible — e.g. scrolled off). Testing absence verifies conditional UI (an error message shouldn't show when input is valid). Test absence, not just presence."}]},{level:"senior",q:"How do you test a dialog or bottom sheet in Compose?",a:[{t:"p",text:"Trigger the dialog/sheet (perform the action that shows it), then assert its content is displayed (`onNodeWithText(...).assertIsDisplayed()`), interact (click a button), and assert it dismisses (`assertDoesNotExist()`). Note dialogs render in a *separate window/layer*, but the Compose test API sees them through the semantics tree. For custom dismiss behavior (back press, outside tap), test those paths too."},{t:"code",title:"Dialog test",code:`rule.onNodeWithText("Delete").performClick()          // shows dialog
rule.onNodeWithText("Are you sure?").assertIsDisplayed()
rule.onNodeWithText("Confirm").performClick()
rule.onNodeWithText("Are you sure?").assertDoesNotExist()  // dismissed`},{t:"list",items:["**Trigger** — the action that shows it.","**Assert displayed** — content visible.","**Interact + dismiss** — click, then `assertDoesNotExist`.","**Separate window** — still visible via semantics."]},{t:"note",text:"Test dialogs/sheets: trigger the show action, assert content displayed (onNodeWithText.assertIsDisplayed), interact (click), assert dismissed (assertDoesNotExist). Dialogs render in a separate window/layer but the Compose test API sees them via the semantics tree. Test dismiss paths (back press, outside tap) too."}]},{level:"senior",q:"What is an IdlingResource, and when do you need one?",a:[{t:"p",text:"Espresso auto-synchronizes with the *UI thread* but can't see *background* async work (a network call, a custom thread pool) — so a test might assert before the async work finishes. An `IdlingResource` tells Espresso 'I'm busy' until the async work completes, so Espresso waits. Modern coroutine-based apps often don't need it (Compose tests handle idle, and test dispatchers control timing), but it's essential for legacy async."},{t:"list",items:["**Espresso sees the UI thread** — not background async.","**`IdlingResource`** — signals busy until async completes.","**Espresso waits** — for registered resources.","**Modern** — often unneeded (Compose idle, test dispatchers)."]},{t:"note",text:"Espresso auto-syncs with the UI thread but not background async work — an IdlingResource signals 'busy' until that async completes, so Espresso waits (avoiding asserting too early). Modern coroutine apps often don't need it (Compose handles idle, test dispatchers control timing); essential for legacy async."}]},{level:"junior",q:"How do you test that clicking a button triggers the right action?",a:[{t:"p",text:'Set the composable\'s content with a *callback you can observe* (a lambda that records it was called, or a fake ViewModel), perform the click (`onNodeWithText("Submit").performClick()`), and assert the callback fired / the ViewModel function was called / the state changed. This verifies the click wiring — the UI invokes the correct action.'},{t:"code",title:"Click test",code:`var clicked = false
rule.setContent { MyButton(onClick = { clicked = true }) }
rule.onNodeWithText("Submit").performClick()
assertThat(clicked).isTrue()`},{t:"list",items:["**Observable callback** — record the invocation.","**`performClick()`** — trigger the button.","**Assert** — callback fired / state changed.","**Wiring** — UI invokes the correct action."]},{t:"note",text:"Set content with an observable callback (a lambda recording it fired, or a fake ViewModel), performClick() the button, and assert the callback/ViewModel function was invoked (or state changed). Verifies the click wiring — the UI invokes the correct action."}]},{level:"senior",q:"How do you test different UI states (loading, error, empty, content)?",a:[{t:"p",text:"For each state, render the screen with a `UiState` (via a controllable `StateFlow` or by passing the state directly to a stateless composable) and assert the correct UI: Loading → spinner shown; Error → error message + retry; Empty → empty view; Content → the data. Stateless composables (state hoisted) make this trivial — just pass the state and assert. Cover all states, not just the happy path."},{t:"code",title:"State tests",code:`@Test fun showsError() {
    rule.setContent { ProductScreen(uiState = UiState.Error("Failed")) }
    rule.onNodeWithText("Failed").assertIsDisplayed()
    rule.onNodeWithText("Retry").assertIsDisplayed()
}`},{t:"list",items:["**Render per state** — pass each `UiState`.","**Assert the right UI** — spinner/error/empty/content.","**Stateless composables** — make this trivial.","**Cover all states** — not just happy path."]},{t:"note",text:"Test each UI state by rendering the screen with that UiState (controllable StateFlow or passing state to a stateless composable) and asserting the correct UI (Loading→spinner, Error→message+retry, Empty→empty view, Content→data). Stateless composables make this trivial. Cover all states, not just the happy path."}]},{level:"senior",q:"How do you test accessibility (semantics) in Compose?",a:[{t:"p",text:"Assert the *semantics* your UI exposes for accessibility: content descriptions (`onNodeWithContentDescription(...)`), roles (`assert(hasRole(Role.Button))`), state descriptions, and that interactive elements have labels. The `printToLog()` helper dumps the semantics tree to inspect what a screen reader would see. Because Compose tests *are* semantics-based, testing findability by content description also verifies TalkBack accessibility."},{t:"list",items:["**Content descriptions** — `onNodeWithContentDescription`.","**Roles/state** — `hasRole`, state descriptions.","**`printToLog()`** — dump the semantics tree.","**Tests = semantics** — findability verifies TalkBack too."]},{t:"note",text:"Test accessibility by asserting semantics: content descriptions (onNodeWithContentDescription), roles (hasRole(Role.Button)), state descriptions, and that interactive elements have labels. printToLog() dumps the semantics tree (what a screen reader sees). Since Compose tests are semantics-based, testing findability by content description also verifies TalkBack accessibility."}]}],FC={id:"android-compose-testing",title:"Android & Compose Testing",tagline:"Testing ViewModels, Room DAOs (in-memory DB), Compose UI (createComposeRule), Espresso, Robolectric, screenshot tests, and CI integration.",tags:["Testing","Compose Test","Room Test","Espresso","Robolectric","CI"],content:qC,qa:OC},LC={id:"testing",name:"Testing",description:"Verifying your app — the testing pyramid & strategy, unit testing with JUnit/fakes/mocks, coroutine & Flow testing, and Android/Compose testing.",topics:[IC,DC,UC,FC]},jC=[{heading:"What a memory leak is on Android",blocks:[{t:"p",text:"A **memory leak** happens when an object that's no longer needed *can't be garbage-collected* because something still holds a reference to it. On Android, the classic and most damaging leak is a **leaked Activity (or Fragment/View)**: the Activity is destroyed, but some longer-lived object still references it, so the entire Activity — and its whole view hierarchy, bitmaps, and everything it holds — stays in memory. Repeat this (rotate the screen a few times) and you leak multiple Activities, growing memory until the app slows down or crashes with `OutOfMemoryError`."},{t:"list",items:["**How GC works**: the garbage collector reclaims objects that are no longer *reachable* from a 'GC root' (a long-lived anchor like a static field, a running thread, the Application). If a dead Activity is still reachable through some chain of references, GC can't collect it — that's the leak.","**Why Activities are the big leak**: an Activity holds its entire view tree, and views hold bitmaps (often large). Leaking one Activity can leak megabytes. And they're leaked easily because they're destroyed/recreated frequently (rotation).","**Consequences**: growing memory usage → more frequent GC (which pauses the app, causing jank) → eventually `OutOfMemoryError` and a crash. Leaks degrade performance gradually before they crash."]}]},{heading:"Common leak patterns",blocks:[{t:"table",headers:["Pattern","Why it leaks"],rows:[["Static reference to a Context/Activity/View","static fields live for the whole process — they pin the Activity forever"],["Inner/anonymous class holding an implicit outer reference","a non-static inner class (a Handler, listener, AsyncTask) implicitly holds the Activity; if it outlives the Activity, it leaks it"],["Long-lived listener/callback not unregistered","registering a listener on a singleton/manager without unregistering keeps the registrant (often an Activity) alive"],["Handler with delayed messages","a delayed message holds the Handler, which (if inner) holds the Activity, until the delay elapses"],["Singleton holding an Activity Context","the singleton lives forever; giving it an Activity context pins the Activity"],["Coroutine/thread capturing an Activity, not cancelled","a running coroutine (GlobalScope) or thread capturing the Activity keeps it alive until it finishes"]]},{t:"list",items:["**Static references** — a `companion object` or static field holding a `Context`, `View`, or `Activity`. Statics live for the whole process, so whatever they reference can never be collected. Never store an Activity/View in a static field.","**Inner classes** — a non-static inner class (including anonymous ones — listeners, `Handler`s, old `AsyncTask`s) *implicitly* holds a reference to its outer class. If that inner object outlives the Activity (a pending Handler message, a running background task), it leaks the whole Activity.","**Unregistered listeners** — registering a `BroadcastReceiver`, sensor listener, or callback on a long-lived object (a singleton, a system service) and forgetting to unregister keeps the registrant alive. Always unregister in the mirror lifecycle callback.","**Wrong Context** — giving a long-lived object (a singleton, a cache) an *Activity* context instead of the *application* context pins the Activity. Use `applicationContext` for anything that outlives an Activity."]}]},{heading:"How modern architecture prevents most leaks",blocks:[{t:"list",items:["**ViewModels + not referencing Views** — the whole reason a ViewModel must never hold a View/Activity/Context: the ViewModel outlives the Activity (survives rotation), so referencing one leaks it. Keeping ViewModels framework-free eliminates a huge class of leaks.","**Lifecycle-scoped coroutines** — `viewModelScope`/`lifecycleScope` cancel their coroutines when the scope dies, so a coroutine capturing UI can't outlive it. Contrast `GlobalScope`, which keeps captured objects alive until the work finishes — a leak source.","**Lifecycle-aware observation** — `collectAsStateWithLifecycle`/`repeatOnLifecycle` stop observing when the UI is gone, so observers don't leak. And `viewLifecycleOwner` in Fragments ties view observation to the view's life (not the Fragment's), preventing the leaked-view bug.","**Compose** — `DisposableEffect`'s `onDispose` unregisters listeners when a composable leaves; `rememberCoroutineScope` cancels on exit. The declarative model with lifecycle-scoped effects avoids many manual-cleanup leaks.","**The pattern**: modern Android (ViewModel + lifecycle scopes + lifecycle-aware observation) is designed so that following the idioms prevents most leaks automatically. Leaks today usually come from *not* following them (GlobalScope, static contexts, unregistered listeners, referencing views from ViewModels)."]}]},{heading:"Detecting leaks: LeakCanary",blocks:[{t:"p",text:"**LeakCanary** (by Square) is the standard tool for detecting memory leaks in debug builds. You add it as a debug dependency and it works automatically — no code. It watches destroyed Activities, Fragments, ViewModels, and views; if one *should* have been garbage-collected but wasn't (still reachable after destruction), LeakCanary detects it, analyzes the *reference chain* keeping it alive, and shows you a notification with the exact leak path."},{t:"code",title:"Adding LeakCanary (debug only)",code:`dependencies {
    debugImplementation("com.squareup.leakcanary:leakcanary-android:2.x")
    // That's it — no code needed. It auto-installs and monitors.
}`},{t:"list",items:["**How it works**: it holds *weak references* to destroyed objects, triggers GC after they should be collectible, and if they're still around, it dumps the heap and computes the *shortest strong reference path* from a GC root to the leaked object — telling you exactly *what* is holding *what*.","**Reading a leak trace**: it shows the chain, e.g. `MainActivity ← this$0 of MyHandler ← ... ← static field`, pointing at the culprit (here a static field or an inner Handler). You fix the leak by breaking that reference (nulling it, unregistering, using the app context, making the inner class static).","**Debug-only** — never ship LeakCanary in release (it's heavy: heap dumps, monitoring). `debugImplementation` scopes it correctly.","**For deeper analysis** — the Android Studio **Memory Profiler** lets you capture a heap dump manually, see all objects and their retained sizes, and trace references — useful for leaks LeakCanary doesn't auto-catch or for general memory investigation."]}]},{heading:"References: strong, weak, soft",blocks:[{t:"list",items:["**Strong reference** (the default) — as long as a strong reference exists, the object is *not* collected. Leaks happen because of unwanted strong references from long-lived objects.","**`WeakReference`** — doesn't prevent collection; the object can be GC'd even while weakly referenced (you get null after). Used to reference something *without* keeping it alive — e.g. a Handler holding a `WeakReference` to its Activity, so it doesn't leak it. A pattern for breaking potential leaks.","**`SoftReference`** — collected only when memory is low (more lenient than weak). Historically used for memory-sensitive caches, though modern caching (LruCache) is usually preferred.","**When to reach for WeakReference**: rarely, and as a targeted fix — when an object legitimately needs to reference a shorter-lived one without pinning it. Usually the *better* fix is to remove the reference at the right lifecycle moment (unregister, cancel, null out) rather than weakly holding it. WeakReference is a tool, not the first resort."]},{t:"note",text:"Memory leaks: an object that can't be GC'd because something still references it — classically a leaked Activity (holds its whole view tree → OOM over time). Common causes: static Context/View refs, inner classes implicitly holding the Activity, unregistered listeners, Handlers with delayed messages, Activity context in singletons, uncancelled coroutines. Modern architecture (ViewModel without View refs, lifecycle scopes, lifecycle-aware observation) prevents most. Detect with LeakCanary (debug — auto-finds leaks + reference chain) and the Memory Profiler. WeakReference references without pinning."}]}],VC=[{level:"junior",q:"What is a memory leak and why are they especially bad on Android?",a:[{t:"p",text:"**A memory leak is when an object that's no longer needed can't be garbage-collected because something still holds a reference to it — so it stays in memory, wasting it.** The garbage collector reclaims objects that are no longer *reachable* from a long-lived anchor (a GC root); if a dead object is still reachable through some reference chain, GC can't collect it, and it leaks."},{t:"p",text:"**On Android, the classic and worst case is a leaked Activity.** When an Activity is destroyed (which happens often — every rotation), it *should* be collected. But if some longer-lived object still references it, the entire Activity stays in memory — and because an Activity holds its whole view hierarchy (and views hold large bitmaps), leaking one Activity can leak megabytes. Rotate the screen a few times with a leak and you accumulate multiple leaked Activities. The consequences compound: growing memory usage triggers more frequent garbage collection (which pauses the app, causing jank), and eventually the app runs out of memory and crashes with `OutOfMemoryError`. So leaks degrade performance gradually (via GC pressure) before they crash — which is why they're especially damaging on memory-constrained mobile devices, and why detecting them (with LeakCanary) is standard practice."}]},{level:"junior",q:"What are common causes of memory leaks in Android?",a:[{t:"list",items:["**Static references to a Context, View, or Activity** — static fields live for the whole process, so anything they reference can *never* be collected. Storing an Activity or View in a `companion object`/static field pins it forever.","**Non-static inner classes** — an inner class (including anonymous ones: listeners, Handlers, old AsyncTasks) *implicitly* holds a reference to its outer class (often an Activity). If the inner object outlives the Activity (a pending Handler message, a running background task), it leaks the Activity.","**Unregistered listeners/callbacks** — registering a BroadcastReceiver, sensor listener, or callback on a long-lived object (a singleton, a system service) and forgetting to unregister keeps the registrant (usually an Activity) alive.","**Handlers with delayed messages** — a `postDelayed` message holds the Handler (which if inner holds the Activity) until the delay elapses.","**Activity context in a singleton** — giving a long-lived singleton an Activity context (instead of the application context) pins the Activity.","**Uncancelled coroutines/threads** — a `GlobalScope` coroutine or raw thread capturing an Activity keeps it alive until the work finishes."]},{t:"p",text:"The common thread in all of them is a **long-lived object holding a reference to a short-lived one** (usually an Activity/View/Context). The fixes follow: use the application context (not Activity) for long-lived objects, unregister listeners in the mirror lifecycle callback, make inner classes static (or use WeakReference), remove delayed Handler messages, and use lifecycle-scoped coroutines (`viewModelScope`) that auto-cancel rather than `GlobalScope`. Modern architecture (ViewModels that never reference Views, lifecycle scopes, lifecycle-aware observation) prevents most of these automatically."}]},{level:"junior",q:"What is LeakCanary and how do you use it?",a:[{t:"p",text:"**LeakCanary is the standard library for automatically detecting memory leaks in debug builds. You add it as a debug-only dependency and it works with no code — it monitors destroyed Activities, Fragments, ViewModels, and views, and when one that should have been garbage-collected wasn't, it detects the leak and shows you the exact reference chain keeping it alive.**"},{t:"code",title:"Adding it — that's the whole setup",code:'debugImplementation("com.squareup.leakcanary:leakcanary-android:2.x")'},{t:"list",items:["**How it works**: it holds *weak references* to destroyed objects, forces a garbage collection after they should be collectible, and if they're still in memory, it dumps the heap and computes the *shortest strong-reference path* from a GC root to the leaked object — telling you precisely what is holding it.","**Reading the result**: it notifies you with a leak trace showing the chain, e.g. the Activity being held by a static field or an inner Handler — pointing directly at the culprit so you know what reference to break.","**Debug-only** — you use `debugImplementation` so it never ships in release (it's heavy: heap dumps and monitoring would hurt production performance)."]},{t:"p",text:"The value is that leaks are otherwise *invisible* until they crash — LeakCanary surfaces them during development, automatically, with the exact cause. For leaks it doesn't auto-catch, or for general memory investigation, the Android Studio Memory Profiler lets you capture heap dumps manually and inspect object retention and reference chains. But for the common Activity/Fragment/ViewModel leaks, LeakCanary's zero-setup automatic detection is the go-to tool."}]},{level:"junior",q:"What is a WeakReference and when would you use one?",a:[{t:"p",text:"**A `WeakReference` lets you reference an object *without* keeping it alive — unlike a normal (strong) reference, a weak reference doesn't prevent garbage collection, so the object can be collected even while weakly referenced (after which the weak reference returns null).** It's a tool for referencing something without pinning it in memory."},{t:"list",items:["**The contrast**: a *strong* reference (the default) keeps an object alive as long as it exists — this is what causes leaks (a long-lived object strongly referencing a short-lived one). A *weak* reference doesn't, so GC can reclaim the target when nothing *strongly* references it.","**Classic use**: a `Handler` that needs to reference its Activity can hold a `WeakReference<Activity>` instead of a strong one, so a pending delayed message doesn't leak the Activity — when the Activity is destroyed, GC can collect it and the weak reference just returns null (which the Handler checks before using).","**`SoftReference`** is related — collected only when memory is *low* (more lenient than weak), historically used for memory-sensitive caches."]},{t:"p",text:"The important nuance for an interview: `WeakReference` is a *targeted tool*, not the first resort. Usually the *better* fix for a leak is to remove the strong reference at the right lifecycle moment — unregister the listener, cancel the coroutine, null out the reference in `onDestroy`, use the application context — rather than weakly holding it. Reaching for `WeakReference` everywhere is a code smell that hides the real problem (a reference that shouldn't exist at that lifetime). Use it specifically when an object *legitimately* needs to reference a shorter-lived one without pinning it and there's no clean lifecycle point to clear the reference. Modern architecture (lifecycle scopes, ViewModels without View refs) makes explicit WeakReference use rarely necessary."}]},{level:"senior",q:"Why must a ViewModel never hold a reference to a View or Activity, in terms of memory?",a:[{t:"p",text:"**Because a ViewModel *outlives* the Activity/View — it survives configuration changes — so if it holds a reference to one, that Activity/View can't be garbage-collected when it's destroyed, leaking it (and its entire view hierarchy). This is a structural leak guaranteed by the ViewModel's lifecycle, not an accidental one.**"},{t:"list",items:["**The lifetime mismatch**: a ViewModel is retained across configuration changes — when the Activity is destroyed and recreated on rotation, the *same* ViewModel instance persists (that's its whole purpose). So the ViewModel lives *longer* than any single Activity instance. If it holds a reference to the destroyed Activity (or a View, or an Activity `Context`), that Activity can't be collected — it's pinned by the surviving ViewModel until the ViewModel itself is cleared. On rotation, you'd leak the old Activity every time.","**The magnitude**: leaking an Activity means leaking its entire view tree, all its bitmaps, and everything those hold — potentially megabytes per leaked instance. And it happens on *every* rotation, so it accumulates fast.","**Why it's structural**: this isn't a subtle bug you might avoid — the ViewModel's longer lifetime makes *any* View/Activity reference in it a leak by construction. That's why the rule is absolute: a ViewModel must be framework-free, referencing no View, Activity, Fragment, or Activity Context."]},{t:"list",items:["**How the architecture avoids it**: the ViewModel exposes *state* (via `StateFlow`/`LiveData`) that the View *observes* — the View references the ViewModel, never the reverse. So data flows to the UI without the ViewModel holding the UI. If the ViewModel needs application-level context (rare — usually a sign work belongs elsewhere), it uses the *application* context (a process-lifetime singleton, safe) via `AndroidViewModel` or DI's `@ApplicationContext`, never an Activity context.","**The general principle it illustrates**: memory leaks come from *long-lived objects referencing short-lived ones*. The ViewModel-View rule is the cleanest example — the framework deliberately gives the ViewModel a longer lifetime (to survive rotation), which is exactly why it must not reference the shorter-lived UI. Understanding *why* (the lifetime relationship) rather than just memorizing the rule is what distinguishes real understanding."]},{t:"p",text:"**The senior framing**: this rule is the intersection of the ViewModel's config-change-survival benefit and memory safety — the very property that makes ViewModels useful (outliving the Activity) is what makes referencing the Activity a guaranteed leak. So the architecture is designed around one-directional references (View → ViewModel → data, never back) and framework-free ViewModels precisely to make this class of leak *impossible by construction*. It's a great example of how good architecture prevents whole categories of bugs — follow the pattern (observe state, never reference the View) and the leak can't happen; violate it and you leak an Activity on every rotation. Being able to explain the lifetime mismatch and connect it to the general 'long-lived references short-lived' leak principle is the complete answer."}]},{level:"senior",q:"How would you investigate and fix a memory leak in a production app?",a:[{t:"p",text:"**I'd combine automated detection (LeakCanary in debug), heap analysis (Memory Profiler / heap dumps), and production signals (crash reports, memory metrics), then trace the reference chain to the root cause and break the offending reference — verifying the fix with the same tools.** Here's the systematic approach:**"},{t:"list",items:["**1. Detect and reproduce**: in *debug*, LeakCanary automatically catches leaked Activities/Fragments/ViewModels and shows the reference chain — often the fastest path to the cause. Reproduce the leak by exercising the suspect flow (rotate the screen repeatedly, navigate in and out of a screen) and watch memory/LeakCanary. For leaks that only show in production, use *crash reports* (OutOfMemoryError frequency) and *memory metrics* (Play Console's Android Vitals reports excessive memory usage; custom memory logging) to identify which screens/flows correlate with growth.","**2. Capture a heap dump**: use the Android Studio *Memory Profiler* to capture a heap dump at the moment of suspected leak (e.g. after navigating away from a screen and forcing GC). Look for *retained* instances that shouldn't still exist — e.g. multiple `MainActivity` instances when there should be one (a smoking gun for an Activity leak).","**3. Trace the reference chain**: for a leaked object, find the *shortest strong-reference path from a GC root* to it (LeakCanary computes this automatically; the profiler lets you navigate references manually). This chain tells you *exactly what is holding what* — e.g. `Activity ← inner Handler ← pending message`, or `Activity ← static field`, or `Activity ← listener registered on a singleton`.","**4. Identify the root cause pattern**: map the chain to a known leak pattern — static context, inner class, unregistered listener, Activity context in a singleton, uncancelled coroutine, wrong lifecycle owner in a Fragment (`this` instead of `viewLifecycleOwner`). The chain points at the specific bad reference.","**5. Fix by breaking the reference at the right lifecycle point**: unregister the listener in `onDestroy`/`onStop` (or `onDispose` in Compose); use the application context instead of Activity context for the long-lived holder; cancel the coroutine (use a lifecycle scope, not GlobalScope); remove delayed Handler messages; make an inner class static + WeakReference; null out a ViewBinding in `onDestroyView`; use `viewLifecycleOwner` for Fragment observation. Choose the fix that removes the reference at the correct lifetime rather than papering over it.","**6. Verify**: re-run the reproduction with LeakCanary/profiler and confirm the object is now collected (no retained instances, no leak notification). Add the fix and, where possible, a test or lint rule to prevent regression."]},{t:"list",items:["**Prevention going forward**: adopt the architecture that prevents leaks structurally (ViewModels without View refs, lifecycle-scoped coroutines, lifecycle-aware observation, `viewLifecycleOwner` in Fragments, application context in singletons), and keep LeakCanary in debug so new leaks are caught during development, not in production."]},{t:"p",text:"**The senior framing**: leak investigation is fundamentally about *finding what's holding a reference that shouldn't exist* — the whole game is tracing the reference chain from a GC root to the leaked object, which tells you the culprit. LeakCanary automates this in debug (and is the first tool to reach for); the Memory Profiler and heap dumps handle deeper or production-informed investigation; and production signals (OOM crashes, memory vitals) tell you *where* to look. The fix is always to break the offending reference *at the right lifecycle moment*, and the durable solution is architecture that makes the reference impossible in the first place. Being able to lay out this detect → dump → trace-chain → identify-pattern → fix → verify workflow, and connect the fixes to the leak patterns and preventive architecture, is the comprehensive, senior-level answer."}]},{level:"junior",q:"What is the difference between a memory leak and high memory usage?",a:[{t:"p",text:"*High memory usage* is legitimately holding a lot of memory you're using (big caches, bitmaps) — it can be fine or intentional. A *memory leak* is holding memory you *no longer need* because a stale reference prevents GC. High usage plateaus; a leak *grows unboundedly* over time (each leaked instance accumulates), eventually causing `OutOfMemoryError`. The tell of a leak is monotonic growth without a corresponding increase in actual work."},{t:"list",items:["**High usage** — legitimately holding memory you use; may plateau.","**Leak** — holding memory you no longer need; grows unboundedly.","**Tell of a leak** — monotonic growth without more actual work.","**Endgame of a leak** — `OutOfMemoryError`."]},{t:"note",text:"High memory usage = legitimately holding memory you use (may plateau, can be fine). A leak = holding memory you no longer need (stale reference blocks GC) — grows unboundedly, eventually OOM. The tell of a leak is monotonic growth without a matching increase in work."}]},{level:"senior",q:"How does a static reference cause a memory leak?",a:[{t:"p",text:"A `static` field lives for the *entire process lifetime* (it's a GC root via the class). If it references an Activity, View, or Context — directly or through a chain — that object can never be collected while the process lives, even after the Activity is destroyed. Classic example: a `static` variable holding a Context, or a static list/cache that accumulates objects tied to Activities. Fix: don't store Contexts/Views statically; use the Application Context if you truly need a long-lived Context."},{t:"code",title:"A static leak",code:`object Cache {
    // BAD: static holds Activity Context for the whole process
    var context: Context? = null
}
// setting Cache.context = this (an Activity) leaks the Activity forever
// FIX: store applicationContext, or don't store Context at all`},{t:"note",text:"A static field is a GC root living for the whole process — if it references an Activity/View/Context (directly or via a chain), that object never gets collected (leak). Classic: static Context or a static cache accumulating Activity-tied objects. Fix: never store Contexts/Views statically; use applicationContext if a long-lived Context is truly needed."}]},{level:"senior",q:"How do non-static inner classes and anonymous classes cause leaks?",a:[{t:"p",text:"A non-static inner class (and any anonymous class / lambda that captures) holds an *implicit reference to its outer instance*. If such an object (a `Handler`, `Runnable`, listener, callback) outlives the Activity — e.g. it's posted with a delay, or held by a long-lived object — it keeps the *entire outer Activity* alive → leak. Fix: use `static`/top-level classes with a `WeakReference` to the Activity, or cancel/remove the callback in `onDestroy`, or use lifecycle-aware scopes."},{t:"code",title:"Leaky anonymous Runnable",code:`// BAD: anonymous Runnable captures the Activity; posted delayed → leaks it
handler.postDelayed({ updateUi() }, 60_000)
// FIX: remove callbacks in onDestroy, or use lifecycleScope which auto-cancels
override fun onDestroy() { handler.removeCallbacksAndMessages(null); super.onDestroy() }`},{t:"note",text:"Non-static inner/anonymous classes (and capturing lambdas) hold an implicit reference to the outer instance — if such an object (Handler/Runnable/listener) outlives the Activity, it keeps the whole Activity alive (leak). Fix: static class + WeakReference, remove callbacks in onDestroy, or lifecycle-aware scopes (lifecycleScope) that auto-cancel."}]},{level:"senior",q:"How do unregistered listeners and receivers cause leaks?",a:[{t:"p",text:"When an Activity/Fragment registers a callback with a *longer-lived* object (a `BroadcastReceiver`, `LocationListener`, `SensorEventListener`, an event bus, a singleton observer), that object holds a reference to the listener (often the Activity). If you don't *unregister* it in the matching lifecycle callback (`onStop`/`onDestroy`), the Activity leaks. Rule: every `register` needs a matching `unregister`; every `add(listener)` needs a `remove`. Lifecycle-aware components (`lifecycleScope`, `repeatOnLifecycle`, `LiveData`) handle this automatically."},{t:"list",items:["**Long-lived object holds the listener** — often the Activity.","**No unregister** → Activity leaks.","**Rule** — every register/add needs an unregister/remove.","**Lifecycle-aware** — LiveData/repeatOnLifecycle handle it automatically."]},{t:"note",text:"Registering a callback with a longer-lived object (receiver, sensor/location listener, event bus, singleton) makes it hold the listener (often the Activity) — no unregister in onStop/onDestroy → leak. Rule: every register/add needs a matching unregister/remove. Lifecycle-aware components (LiveData, repeatOnLifecycle) handle it automatically."}]},{level:"junior",q:"What is the difference between Activity Context and Application Context for leaks?",a:[{t:"p",text:"The *Activity Context* is tied to the Activity's lifecycle — holding it beyond the Activity's life leaks the whole Activity (and its views). The *Application Context* lives for the entire process, so it's safe to hold long-term (in singletons). Rule of thumb: for anything that *outlives* an Activity (a singleton, a cache, a long-lived manager), use `applicationContext`; use the Activity Context only for UI things (inflating views, showing dialogs) that are inherently Activity-scoped."},{t:"list",items:["**Activity Context** — Activity-lifecycle-scoped; holding it long leaks the Activity.","**Application Context** — process-lifetime; safe to hold long-term.","**Long-lived** — use `applicationContext` (singletons/caches/managers).","**Activity Context** — only for UI (inflation, dialogs, themed views)."]},{t:"note",text:"Activity Context is Activity-lifecycle-scoped — holding it beyond the Activity leaks it (+ views). Application Context lives for the whole process — safe for singletons/caches. Rule: anything outliving an Activity uses applicationContext; use the Activity Context only for UI (inflation, dialogs, themed views)."}]},{level:"senior",q:"How can coroutines cause memory leaks?",a:[{t:"p",text:"A coroutine launched in a scope that *isn't cancelled* keeps its captured references (Activity, View) alive while it runs — e.g. `GlobalScope.launch { ... }` capturing an Activity, or a scope you never cancel. If the coroutine is long-running/suspended and the Activity is destroyed, the Activity leaks until the coroutine completes. Fix: use *lifecycle-aware scopes* (`viewModelScope`, `lifecycleScope`) that auto-cancel, and never use `GlobalScope` for UI-tied work."},{t:"list",items:["**Uncancelled scope** — keeps captured Activity/View alive while running.","**`GlobalScope`** — never cancelled; classic leak source.","**Fix** — `viewModelScope`/`lifecycleScope` auto-cancel.","**Never** — `GlobalScope` for UI-tied work."]},{t:"note",text:"A coroutine in an uncancelled scope keeps its captured references (Activity/View) alive while running — GlobalScope.launch capturing an Activity, or a never-cancelled scope, leaks a destroyed Activity until it completes. Fix: lifecycle-aware scopes (viewModelScope/lifecycleScope) that auto-cancel; never GlobalScope for UI work."}]},{level:"junior",q:"What is a GC root, and why does it matter for leaks?",a:[{t:"p",text:"A *GC root* is an always-reachable anchor the garbage collector starts from — static fields, active threads, the stack of running methods, JNI references. An object is *reachable* (not collectable) if there's a reference chain from *some GC root* to it. A leak is exactly this: a dead object still reachable from a GC root via some chain. To *fix* a leak you find and break that chain — which is why leak tools show you the *reference chain from a GC root* to the leaked object."},{t:"list",items:["**GC root** — always-reachable anchor (statics, threads, stacks, JNI).","**Reachable** — a chain from a root exists → not collectable.","**Leak** — dead object still reachable from a root.","**Fix** — break the reference chain (tools show it)."]},{t:"note",text:"A GC root is an always-reachable anchor (static fields, threads, method stacks, JNI). An object is reachable (not collectable) if a chain from some GC root reaches it. A leak = a dead object still reachable from a root. Fixing means breaking that chain — why leak tools show the reference chain from a GC root."}]},{level:"senior",q:"What is the difference between SoftReference, WeakReference, and PhantomReference?",a:[{t:"p",text:"All are references that don't prevent collection as strongly as a normal (*strong*) reference. A *WeakReference* is collected at the next GC once no strong refs remain (good for caches/back-references you want gone promptly). A *SoftReference* is kept until the JVM needs memory (collected under memory pressure — a memory-sensitive cache, though `LruCache` is usually preferred). A *PhantomReference* is for cleanup actions after collection (advanced, rarely used in app code)."},{t:"table",headers:["Reference","Collected when","Use"],rows:[["Strong","Never (while referenced)","Normal references"],["Soft","Under memory pressure","Memory-sensitive cache"],["Weak","Next GC (no strong refs)","Back-references, canonical maps"],["Phantom","After finalization","Post-collection cleanup (rare)"]]},{t:"note",text:"Weak: collected at next GC once no strong refs (back-refs, prompt-clear caches). Soft: kept until memory pressure (memory-sensitive cache, though LruCache is usually better). Phantom: post-collection cleanup (rare). All allow collection that a strong reference would prevent."}]},{level:"senior",q:"How does LeakCanary detect leaks under the hood?",a:[{t:"p",text:"LeakCanary hooks into lifecycle to know when objects *should* be gone (destroyed Activities/Fragments, cleared ViewModels). It holds a `WeakReference` to each and, after they should be collected, triggers a GC and checks whether the weak reference was cleared. If the object is *still retained*, LeakCanary dumps the heap, analyzes it (using Shark) to find the *shortest strong reference chain from a GC root* to the leaked object, and reports that chain — telling you exactly what's holding it."},{t:"list",items:["**Watches** — destroyed Activities/Fragments, cleared ViewModels.","**WeakReference + GC** — checks if the object was collected.","**Still retained** → heap dump + Shark analysis.","**Reports** — shortest strong reference chain from a GC root."]},{t:"note",text:"LeakCanary watches objects that should die (destroyed Activities/Fragments, cleared ViewModels) via WeakReference; after they should be collected it GCs and checks if the ref cleared. Still retained → heap dump + Shark analysis → reports the shortest strong reference chain from a GC root to the leak."}]},{level:"senior",q:"How do you read a heap dump to find a leak?",a:[{t:"p",text:"Capture a heap dump (Memory Profiler → 'Dump Java heap', or LeakCanary/MAT). Look for instances that *shouldn't exist* (multiple leaked Activities of the same type after rotations) and inspect the *reference chain (path to GC root)* for one — it shows the exact field/object holding it (a static, a Handler, a listener). That chain names the culprit; break it (unregister, use WeakReference, clear the static). Grouping by class and sorting by *retained size* finds the biggest offenders."},{t:"list",items:["**Capture** — Memory Profiler / LeakCanary / MAT.","**Find** — instances that shouldn't exist (duplicate Activities).","**Reference chain to GC root** — names the culprit field/object.","**Retained size** — sort to find the biggest offenders."]},{t:"note",text:"Capture a heap dump (Memory Profiler 'Dump Java heap' / LeakCanary / MAT), find instances that shouldn't exist (duplicate leaked Activities after rotations), inspect the reference chain (path to GC root) — it names the culprit (static/Handler/listener). Break that chain. Sort by retained size for the biggest offenders."}]},{level:"junior",q:"How do bitmaps cause memory problems, and how do you avoid them?",a:[{t:"p",text:"Bitmaps are *large* (a full-screen photo can be tens of MB decoded) and dominate an app's memory. Loading full-resolution images into small views wastes memory and risks `OutOfMemoryError`; holding decoded bitmaps in static/long-lived references leaks a lot at once. Avoid by using an image library (Coil/Glide) that downsamples to the target size, caches with size limits (`LruCache`), and recycles/releases. Never keep large bitmaps in static fields."},{t:"list",items:["**Large** — a full-screen photo is tens of MB decoded.","**Downsample** — to the target view size (Coil/Glide).","**Cache with limits** — `LruCache`; don't hold statically.","**Risk** — OOM from full-res decode / static bitmaps."]},{t:"note",text:"Bitmaps are large (a full-screen photo = tens of MB decoded) and dominate app memory — full-res decode into a small view risks OOM; static bitmaps leak a lot. Use Coil/Glide to downsample to target size and cache with size limits (LruCache). Never hold large bitmaps in static fields."}]},{level:"senior",q:"What is the difference between Java heap and native memory on Android?",a:[{t:"p",text:"The *Java (managed) heap* holds Kotlin/Java objects, GC-managed, capped by the per-app heap limit (visible in the Memory Profiler). *Native memory* is allocated by C/C++ (via NDK, some framework internals, older bitmap storage) — not GC-tracked, and native leaks don't show in the Java heap dump. Total app memory (RSS/PSS) includes both plus graphics and code. A growing native footprint (NDK code, native leaks) needs different tools (Native Memory Profiler, `malloc` debug) than Java-heap leak tools."},{t:"list",items:["**Java heap** — managed objects, GC'd, per-app cap.","**Native memory** — C/C++ allocations, not GC-tracked.","**Native leaks** — invisible in Java heap dumps.","**Tools** — Native Memory Profiler for native growth."]},{t:"note",text:"Java (managed) heap: Kotlin/Java objects, GC'd, per-app capped (Memory Profiler). Native memory: C/C++ allocations (NDK, framework internals), not GC-tracked — native leaks don't appear in Java heap dumps. Total RSS/PSS includes both + graphics + code. Native growth needs the Native Memory Profiler, not Java-heap tools."}]},{level:"junior",q:"What are onTrimMemory and onLowMemory for?",a:[{t:"p",text:"The system calls `onTrimMemory(level)` (and legacy `onLowMemory`) to tell your app the device is under memory pressure or your app went to the background — a hint to *release memory you can rebuild* (caches, bitmaps, non-critical buffers). Responding well reduces the chance the system kills your process to reclaim memory (improving the odds your process survives in the background). The `level` indicates severity (e.g. `TRIM_MEMORY_UI_HIDDEN` when your UI is hidden, up to `TRIM_MEMORY_COMPLETE`)."},{t:"list",items:["**Memory-pressure signal** — release rebuildable memory (caches/bitmaps).","**`level`** — severity (UI hidden → complete).","**Benefit** — less likely the process is killed.","**Respond** — trim caches proportionally to the level."]},{t:"note",text:"onTrimMemory(level) (and legacy onLowMemory) signal memory pressure / backgrounding — release rebuildable memory (caches/bitmaps) proportional to the level (UI_HIDDEN → COMPLETE). Responding well lowers the chance the system kills your process to reclaim memory."}]},{level:"senior",q:"How do you build a cache that doesn't leak?",a:[{t:"p",text:"Use a *bounded* cache with an eviction policy — `LruCache` (size-limited by count or bytes, evicts least-recently-used) — rather than an unbounded `HashMap` that grows forever. Cache *values* that don't reference Activities/Views (or use the Application Context). For memory-sensitive caches, `LruCache` sized to a fraction of available heap is standard. Avoid `SoftReference`-based caches (unpredictable GC behavior). Clear/trim caches in `onTrimMemory`."},{t:"code",title:"A bounded LruCache",code:`// size = 1/8 of available memory, in KB
val maxKb = (Runtime.getRuntime().maxMemory() / 1024 / 8).toInt()
val cache = object : LruCache<String, Bitmap>(maxKb) {
    override fun sizeOf(key: String, value: Bitmap) = value.byteCount / 1024
}`},{t:"note",text:"Non-leaking cache: bounded with eviction — LruCache (size-limited by count/bytes, evicts LRU) not an unbounded HashMap. Cache values that don't reference Activities/Views (or use appContext). Size LruCache to a fraction of heap; trim in onTrimMemory. Avoid SoftReference caches (unpredictable)."}]},{level:"junior",q:"What is memory churn, and how does it relate to GC?",a:[{t:"p",text:"*Memory churn* is rapidly allocating and discarding many short-lived objects (e.g. in a loop, `onDraw`, or per-frame code). It's not a leak — the objects *are* collected — but the constant allocation triggers *frequent garbage collection*, and GC pauses cause jank. Reduce churn by allocating outside hot paths, reusing objects, avoiding autoboxing, and using primitive arrays. The Memory Profiler's allocation tracking shows churn as a sawtooth memory graph."},{t:"list",items:["**Churn** — rapid allocate/discard of short-lived objects.","**Not a leak** — but triggers frequent GC → jank.","**Reduce** — allocate outside hot paths, reuse, avoid boxing.","**Sign** — sawtooth memory graph in the profiler."]},{t:"note",text:"Memory churn = rapidly allocating/discarding short-lived objects (loops, onDraw, per-frame). Not a leak (they're collected) but triggers frequent GC → jank. Reduce: allocate outside hot paths, reuse objects, avoid autoboxing, use primitive arrays. Shows as a sawtooth graph in the Memory Profiler."}]},{level:"senior",q:"How can a Fragment leak, and what is the Fragment view-lifecycle pitfall?",a:[{t:"p",text:"A Fragment can outlive its *view* — the Fragment instance survives (e.g. on the back stack) while its view is destroyed and recreated. Holding view references (or binding) in Fragment fields past `onDestroyView` leaks the old view hierarchy. Fix: null out the binding in `onDestroyView`, and observe with `viewLifecycleOwner` (not the Fragment) so observers are removed when the view dies. Also, a Fragment held by a longer-lived object (or a leaked Activity) leaks like any object."},{t:"code",title:"Safe Fragment view binding",code:`private var _binding: FooBinding? = null
private val binding get() = _binding!!
override fun onCreateView(...) = FooBinding.inflate(inflater).also { _binding = it }.root
override fun onDestroyView() { _binding = null; super.onDestroyView() } // avoid leaking the view`},{t:"note",text:"A Fragment outlives its view (survives on back stack while the view is recreated) — holding view refs/binding past onDestroyView leaks the old hierarchy. Fix: null the binding in onDestroyView; observe with viewLifecycleOwner (not the Fragment) so observers clear when the view dies."}]},{level:"junior",q:"What tools besides LeakCanary help find memory issues?",a:[{t:"p",text:"The *Android Studio Memory Profiler* shows live memory, allocation tracking, and captures Java heap dumps to inspect instances and reference chains. *Perfetto* traces memory over time. *MAT (Eclipse Memory Analyzer)* does deep heap-dump analysis (dominator tree, retained sizes). For native memory, the *Native Memory Profiler*. Play vitals reports *excessive memory* / OOM crashes in production. LeakCanary is best for *automatic* detection in debug; these others for *investigation* and native/production insight."},{t:"list",items:["**Memory Profiler** — live memory, allocations, heap dumps.","**MAT** — deep heap analysis (dominator tree, retained size).","**Native Memory Profiler** — native allocations.","**Play vitals** — production OOM/excessive-memory reports."]},{t:"note",text:"Beyond LeakCanary (automatic debug detection): Memory Profiler (live memory, allocation tracking, heap dumps + reference chains), MAT (deep heap analysis — dominator tree, retained size), Native Memory Profiler (native), Perfetto (memory over time), Play vitals (production OOM). LeakCanary for detection; others for investigation."}]},{level:"senior",q:"Why is a ViewModel a safe place for state across config changes but a dangerous place for a Context?",a:[{t:"p",text:"A `ViewModel` *survives* configuration changes (it's retained across the Activity recreation and only cleared in `onCleared` when the scope is truly gone) — perfect for UI state. But that very survival makes holding an *Activity Context or View* dangerous: after a rotation the old Activity is destroyed but the ViewModel (holding it) lives on → leaked Activity. Rule: ViewModels hold *state and app-scoped dependencies* (use `AndroidViewModel`'s Application if you need a Context), never Activity/View references."},{t:"list",items:["**Survives config changes** — great for UI state.","**That survival** — makes holding an Activity/View a leak.","**After rotation** — old Activity destroyed, ViewModel lives → leak.","**Rule** — state + app-scoped deps only; Application Context via `AndroidViewModel`."]},{t:"note",text:"A ViewModel survives config changes (cleared only in onCleared) — ideal for UI state, but that survival makes holding an Activity Context/View a leak (post-rotation the old Activity is destroyed while the ViewModel lives). Rule: ViewModels hold state + app-scoped deps only; use AndroidViewModel's Application if a Context is needed."}]},{level:"senior",q:"How do RxJava/Flow subscriptions leak, and how do you prevent it?",a:[{t:"p",text:"A subscription (RxJava `Disposable`, a `Flow` collection) that isn't cancelled keeps its downstream (often capturing a View/Activity) alive as long as the upstream lives. In RxJava, not disposing subscriptions in `onDestroy` (or not using a `CompositeDisposable`) leaks. In Flow, collecting in a non-lifecycle scope, or not using `repeatOnLifecycle`/`flowWithLifecycle`, keeps collecting (and holding the collector) after the view is gone. Fix: tie subscriptions to lifecycle (`CompositeDisposable.clear()` in `onDestroy`; `repeatOnLifecycle` for Flow)."},{t:"list",items:["**Uncancelled subscription** — keeps the capturing collector alive.","**RxJava** — dispose via `CompositeDisposable` in `onDestroy`.","**Flow** — collect with `repeatOnLifecycle`/`flowWithLifecycle`.","**Root cause** — subscription outliving the view."]},{t:"note",text:"An uncancelled subscription (Rx Disposable, Flow collection) keeps its downstream (capturing a View/Activity) alive while upstream lives. RxJava: clear a CompositeDisposable in onDestroy. Flow: collect with repeatOnLifecycle/flowWithLifecycle. Always tie subscription lifetime to the view's lifecycle."}]},{level:"junior",q:"What is the difference between a memory leak and OutOfMemoryError?",a:[{t:"p",text:"A *memory leak* is a cause; `OutOfMemoryError` (OOM) is a possible *consequence*. A leak slowly consumes memory that can't be reclaimed; when the app's heap fills (from leaks, or from a single huge allocation like a giant bitmap), the next allocation throws `OutOfMemoryError` and typically crashes. But OOM isn't always a leak — it can be one legitimately-too-large allocation. So on an OOM, check both: leaks (gradual growth) and oversized allocations (bitmaps, huge lists)."},{t:"list",items:["**Leak** — a cause (unreclaimable memory grows).","**OOM** — a consequence (heap full → allocation fails).","**Not always a leak** — can be one huge allocation.","**On OOM** — check gradual growth AND oversized allocations."]},{t:"note",text:"A leak is a cause; OutOfMemoryError is a consequence — the heap fills (from leaks or a single huge allocation like a giant bitmap) and the next allocation throws OOM (crash). OOM isn't always a leak. On OOM, investigate both gradual growth (leaks) and oversized allocations (bitmaps/huge lists)."}]},{level:"senior",q:"How does a Handler/Looper leak, and how do you prevent it?",a:[{t:"p",text:"A `Handler` created on the main thread posts `Message`s/`Runnable`s to the main `Looper`'s queue. A delayed message holds a reference to its `Runnable` (and, if that's an inner class, to the outer Activity) *until it runs* — so a message posted with a long delay keeps the Activity alive after it's destroyed. Prevent it: use a `static` Handler subclass with a `WeakReference` to the Activity, and always `removeCallbacksAndMessages(null)` in `onDestroy`."},{t:"code",title:"Clearing Handler callbacks",code:`override fun onDestroy() {
    handler.removeCallbacksAndMessages(null) // drop pending messages holding this Activity
    super.onDestroy()
}`},{t:"note",text:"A Handler posts messages/Runnables to the Looper's queue; a delayed message holds its Runnable (and, if inner, the outer Activity) until it runs — a long delay keeps a destroyed Activity alive. Prevent: static Handler subclass + WeakReference, and removeCallbacksAndMessages(null) in onDestroy."}]},{level:"junior",q:"Does a memory leak always crash the app?",a:[{t:"p",text:"Not necessarily — a small or slow leak may never fill the heap during a typical session, so the app doesn't crash but still suffers: higher memory use, more frequent GC (jank), and a greater chance the *system kills the process* in the background (losing state). Large or fast-accumulating leaks (leaking Activities with big bitmaps on every rotation) do reach `OutOfMemoryError`. So leaks degrade experience even without a crash — worth fixing regardless."},{t:"list",items:["**Small/slow leak** — may not crash but degrades (GC jank, memory).","**Background kill** — more likely; loses state.","**Large/fast leak** — reaches OutOfMemoryError.","**Fix regardless** — leaks hurt experience even without a crash."]},{t:"note",text:"Not always — a small/slow leak may never fill the heap in a session (no crash) but still causes higher memory, more GC (jank), and a greater chance the system kills the backgrounded process (lost state). Large/fast leaks do reach OOM. Leaks degrade UX even without crashing, so fix them regardless."}]},{level:"senior",q:"How do you confirm you've actually fixed a leak?",a:[{t:"p",text:"Reproduce the leak scenario (e.g. open/close the screen or rotate several times), then verify the object is *gone*: with LeakCanary, no leak is reported; in the Memory Profiler, capture a heap dump and confirm there are *no lingering instances* of the class (e.g. zero old Activities), and that memory returns to baseline after GC. Automate a regression guard where feasible. Don't rely on 'memory looks lower' — confirm the specific instances no longer exist via a heap dump."},{t:"list",items:["**Reproduce** — repeat the leak scenario (open/close, rotate).","**LeakCanary** — no leak reported.","**Heap dump** — zero lingering instances of the class.","**Confirm** — memory returns to baseline after GC; guard against regression."]},{t:"note",text:"Confirm a leak fix by reproducing the scenario (open/close, rotate several times), then verifying the object is gone: LeakCanary reports nothing, and a heap dump shows zero lingering instances of the class with memory returning to baseline after GC. Don't trust 'memory looks lower' — confirm the specific instances via heap dump; add a regression guard."}]},{level:"senior",q:"How do you prevent leaks in a long-lived singleton or manager?",a:[{t:"p",text:"Singletons live for the whole process, so *anything they reference lives forever*. Rules: (1) never store Activity/View/Fragment references or Activity Contexts — use `applicationContext`; (2) if a singleton must hold transient callbacks/listeners, use `WeakReference` or require explicit register/unregister tied to lifecycle; (3) bound any internal caches. A singleton holding a Context is the single most common singleton leak — inject the Application Context, not an Activity."},{t:"list",items:["**Process-lifetime** — its references live forever.","**No Activity/View/Fragment refs** — use `applicationContext`.","**Callbacks** — `WeakReference` or lifecycle-tied register/unregister.","**Bound caches** — inside the singleton."]},{t:"note",text:"A singleton lives for the whole process, so anything it references lives forever. Never store Activity/View/Fragment refs or an Activity Context (use applicationContext); hold callbacks via WeakReference or lifecycle-tied register/unregister; bound internal caches. The classic singleton leak is holding an Activity Context — inject the Application."}]}],NC={id:"memory-leaks",title:"Memory Leaks & Management",tagline:"What leaks are & why Activity leaks are worst, common leak patterns, how architecture prevents them, LeakCanary & the Memory Profiler, and reference types.",tags:["Performance","Memory Leaks","LeakCanary","GC","WeakReference"],content:jC,qa:VC},WC=[{heading:"The frame budget — the root of smooth UI",blocks:[{t:"p",text:"A smooth UI depends on rendering each frame *fast enough*. Displays refresh at a fixed rate — 60Hz means a new frame every **~16.6ms** (120Hz = ~8.3ms). For the UI to be smooth, *all* the work to produce a frame — running your code (recomposition/binding), measuring, laying out, drawing — must finish within that budget. When a frame takes *longer* than the budget, the display can't show a new frame in time, so it repeats the old one: a dropped frame, which the user perceives as a **stutter or jank**. Smooth UI = consistently hitting the frame budget; jank = missing it."},{t:"list",items:["**60fps ≈ 16ms per frame** is the classic target; modern high-refresh displays (90/120Hz) demand even less (~11ms/~8ms). The exact number matters less than the principle: every frame's work must fit the budget.","**Jank** = one or more frames took too long, so frames were dropped → visible stutter, hitchy scrolling, laggy animations. It's a *performance* problem, not a crash — but it's what users *feel* as 'the app is slow/janky'.","**Everything runs on the main thread** for UI, so anything slow on the main thread (in the frame's work *or* blocking it) eats into the budget and causes jank."]}]},{heading:"What causes jank",blocks:[{t:"list",items:["**Heavy work on the main thread during a frame** — expensive computation, JSON parsing, sorting a big list, synchronous I/O, or bitmap decoding done in `onBindViewHolder`/a composable/a click handler. Move it off the main thread (coroutines + IO/Default dispatcher).","**Over-drawing / complex layouts** — deeply nested view hierarchies (expensive measure/layout), overdraw (painting the same pixel many times with stacked opaque backgrounds), and unnecessary invalidations. Flatten layouts, remove redundant backgrounds.","**Excessive recomposition (Compose)** — a composable recomposing far more than needed (unstable parameters, reading fast-changing state high up) burns frame time. Fix with stability, keys, and deferring reads (covered in the Compose performance topic).","**Long RecyclerView bind / expensive list items** — each item binds *on the frame* as you scroll; heavy work there (image decode, formatting) drops frames at the scroll edge. Keep binds cheap, use a caching image loader, precompute.","**Main-thread I/O** — reading a file, a database, or SharedPreferences synchronously on the main thread (SharedPreferences' first load is a classic one). Any blocking I/O on the main thread stalls frames.","**GC pauses** — excessive object allocation (churning objects in a tight loop or per-frame) triggers frequent garbage collection, which pauses the app and drops frames. Reduce allocations in hot paths."]}]},{heading:"ANRs — when the main thread blocks too long",blocks:[{t:"p",text:"An **ANR (Application Not Responding)** is the extreme case of main-thread blocking: instead of dropping a frame or two (jank), the main thread is blocked so long that the app can't respond to input at all, and the system shows the 'App isn't responding' dialog. **Jank and ANR are the same problem — a blocked/overloaded main thread — at different severities.** Jank is milliseconds over budget; an ANR is *seconds*."},{t:"list",items:["**ANR triggers**: the main thread unresponsive to input for **~5 seconds**; a `BroadcastReceiver`'s `onReceive` taking more than ~10s; a Service not completing in time; or (Android 11+) a slow `ContentProvider` or a deadlock detected in a foreground app.","**The cause is always main-thread blocking** — a long synchronous operation (network on the main thread, a huge database query, heavy computation, a deadlock, waiting on a lock held by another thread). The fix is the same as jank: move slow work off the main thread.","**Deadlocks** are a subtle ANR cause — the main thread waiting on a lock that another thread holds indefinitely. Careful with synchronization involving the main thread.","**Where you see ANRs in production**: Play Console's **Android Vitals** reports your ANR rate (a Play-monitored metric — a high rate hurts your Play ranking and visibility). ANRs are a key quality signal Google tracks."]}]},{heading:"Diagnosing jank and ANRs",blocks:[{t:"list",items:["**Systrace / Perfetto** — system-level tracing that shows exactly what the main thread is doing frame by frame, so you can *see* which frames overran and what work caused it. The primary tool for diagnosing jank — it pinpoints the expensive operation on the timeline.","**Android Studio Profiler** — CPU profiler (method traces to find expensive methods), plus the frame-rendering view. Find *what* is taking main-thread time.","**Jank detection APIs** — `FrameMetrics`/`JankStats` (Jetpack) report per-frame durations at runtime, so you can measure and log jank in the field.","**Macrobenchmark + `FrameTimingMetric`** — measures real jank (frame durations) on release builds for specific journeys (scroll the feed), giving objective, reproducible numbers to optimize against and track over time.","**Android Vitals (Play Console)** — production ANR and 'excessive frozen/slow frames' metrics across real users' devices — tells you *whether* you have a problem and on which devices, complementing the local diagnosis of *what*.","**The debug-build caveat** (again): judge rendering performance on *release* builds — debug builds have no R8 and JIT-compile during use, so they're expected to be janky. Measure on release, ideally with baseline profiles applied."]}]},{heading:"Fixing jank — the toolkit",blocks:[{t:"list",items:["**Move work off the main thread** — the #1 fix. Network, disk, database, heavy computation → coroutines with `Dispatchers.IO`/`Default`. The main thread should only do UI work, kept short.","**Reduce main-thread work per frame** — cheap RecyclerView binds / composables, precompute/format data off the main thread (in the ViewModel/repository, so the UI receives render-ready data), avoid synchronous I/O.","**Optimize layouts** — flatten view hierarchies (ConstraintLayout in Views, or the single-pass measurement of Compose), remove overdraw (eliminate redundant opaque backgrounds), avoid unnecessary invalidations.","**Optimize rendering** — use a caching image loader (Coil/Glide) with proper downsampling (never decode a huge bitmap into a small view); use `RecyclerView` (or LazyColumn) for large lists instead of loading everything.","**Reduce allocations in hot paths** — avoid creating objects per frame / per bind / in tight loops to cut GC pressure and its pauses.","**Compose-specific** — stability (immutable/stable params), keys in lazy lists, defer high-frequency state reads to layout/draw (lambda modifiers, `graphicsLayer`), and baseline profiles (covered in the Compose performance topic)."]},{t:"note",text:"Jank & ANRs: every frame's work (code + measure + layout + draw) must fit the frame budget (~16ms at 60Hz); exceeding it drops frames → jank (stutter). ANR is the extreme — main thread blocked ~5s → 'not responding' dialog (a Play-tracked Vitals metric). Both = an overloaded/blocked main thread at different severities. Causes: heavy work/I/O on the main thread, overdraw/complex layouts, excessive recomposition, expensive list binds, GC pauses. Diagnose with Perfetto/systrace, the profiler, JankStats, Macrobenchmark, and Android Vitals. Fix by moving work off the main thread and reducing per-frame work. Measure on release builds."}]}],BC=[{level:"junior",q:"What is jank and what causes it?",a:[{t:"p",text:"**Jank is visible stuttering or hitching in the UI, caused by dropped frames — when a frame takes longer to produce than the display's refresh budget, the display can't show a new frame in time and repeats the old one, which the user perceives as a stutter.** For a smooth 60Hz display, every frame's work (running your code, measuring, laying out, drawing) must finish within ~16ms; miss that budget and you drop frames."},{t:"list",items:["**Heavy work on the main thread during a frame** — expensive computation, JSON parsing, sorting a large list, synchronous I/O, or bitmap decoding happening in a bind/composable/click handler eats the frame budget.","**Complex layouts / overdraw** — deeply nested view hierarchies (expensive to measure/layout) and painting the same pixels multiple times (stacked opaque backgrounds).","**Expensive list item binds** — each RecyclerView item binds *on the frame* as you scroll; heavy work there drops frames at the scroll edge.","**Excessive recomposition (Compose)** or **main-thread I/O** (reading a file/DB/SharedPreferences synchronously) or **GC pauses** from churning allocations."]},{t:"p",text:"The unifying cause is *too much work on the main thread within a frame's time budget*. The main fix is to move slow work off the main thread (coroutines with IO/Default dispatchers) and keep per-frame main-thread work cheap (cheap binds, precomputed data, flat layouts). Jank is a performance problem, not a crash — but it's exactly what users mean when they say an app feels 'slow' or 'laggy', so it directly affects perceived quality."}]},{level:"junior",q:"What is an ANR and how do you prevent it?",a:[{t:"p",text:"**An ANR (Application Not Responding) is when the main thread is blocked for so long that the app can't respond to input, and the system shows the 'App isn't responding' dialog. It's the extreme version of jank — where jank is a frame or two over budget, an ANR is the main thread blocked for *seconds*.** The main triggers: the main thread unresponsive to input for ~5 seconds, a BroadcastReceiver's `onReceive` taking more than ~10 seconds, or a Service not completing in time."},{t:"p",text:"**The cause is always the same: a long synchronous operation blocking the main thread** — a network call on the main thread, a huge database query, heavy computation, or a deadlock (the main thread waiting on a lock another thread holds). **The prevention is also always the same: never do slow work on the main thread** — move all networking, disk/database I/O, and heavy computation to background threads via coroutines (`Dispatchers.IO` for I/O, `Dispatchers.Default` for CPU work), keeping the main thread free to handle input and rendering. This is *the* reason Android emphasizes coroutines and background threading so heavily — every 'don't block the main thread' rule exists to prevent ANRs (and jank). ANRs are also a Play-tracked quality metric (in Android Vitals), so a high ANR rate hurts your app's Play Store ranking, making them a business concern, not just a UX one."}]},{level:"junior",q:"How are jank and ANRs related?",a:[{t:"p",text:"**They're the same fundamental problem — a blocked or overloaded main thread — at different severities. Jank is the main thread taking a bit too long (missing a frame's ~16ms budget by milliseconds, dropping a frame or two → a visible stutter). An ANR is the main thread blocked for *seconds*, so long that the app can't respond to input at all → the 'not responding' dialog.**"},{t:"p",text:"The mechanism connects them: the main thread runs a loop processing work (rendering, input) one item at a time. If one item takes 30ms, the next frame's rendering is delayed → a dropped frame (jank). If one item takes 5 seconds (a synchronous network call), input events sit unprocessed for that whole time → an ANR. So it's a continuum: a little too much main-thread work causes jank; a lot causes an ANR. This is why the *fix* is identical for both — move slow work off the main thread and keep main-thread work short — and why understanding them as one problem (main-thread starvation) at different timescales is more useful than treating them as separate issues. It also tells you how to *diagnose* both: find the long-running main-thread operation (via Perfetto/systrace), whether it's a 30ms bind causing jank or a 5-second call causing an ANR."}]},{level:"junior",q:"How do you diagnose a janky screen?",a:[{t:"list",items:["**First, test on a *release* build** — debug builds have no R8 optimization and JIT-compile code during use, so they're *expected* to be janky. A huge fraction of 'my screen is janky' reports disappear on release. Always measure performance on release (minified) builds, ideally with baseline profiles.","**Use Perfetto / systrace** — system-level tracing that shows exactly what the main thread does frame by frame, so you can *see* which frames overran the budget and what operation caused it. This is the primary tool — it pinpoints the expensive work on the timeline.","**Use the Android Studio CPU Profiler** — capture a method trace to find which methods consume main-thread time during the janky interaction.","**Use JankStats / FrameMetrics** — Jetpack APIs that report per-frame durations at runtime, so you can measure and log jank in the field.","**Use Macrobenchmark with FrameTimingMetric** — measures real jank on release builds for a specific journey (scrolling the feed), giving objective, reproducible numbers."]},{t:"p",text:"The workflow: confirm it's real (release build), then find *which* frames drop and *what* work caused them (Perfetto shows the timeline; the profiler shows expensive methods). Once you know the culprit — a heavy bind, a main-thread I/O call, excessive recomposition, an expensive layout — the fix is usually to move that work off the main thread or reduce it. For production, Android Vitals in the Play Console tells you *whether* you have a jank/ANR problem across real users and on which devices, complementing the local diagnosis of *what*. The key discipline is measurement-driven: find the actual expensive operation with tools rather than guessing."}]},{level:"senior",q:"Explain the frame budget and why it matters for high-refresh-rate displays.",a:[{t:"p",text:"**The frame budget is the maximum time all the work to produce one frame can take while still keeping the UI smooth, and it's determined by the display's refresh rate. At 60Hz, the display refreshes every ~16.6ms, so *all* the per-frame work — running your code (recomposition/binding), measuring, laying out, drawing — must finish within ~16ms. Exceed it and the display has no new frame to show, so it repeats the old one: a dropped frame, perceived as jank.**"},{t:"list",items:["**The math**: budget = 1000ms ÷ refresh rate. 60Hz → ~16.6ms; 90Hz → ~11.1ms; 120Hz → ~8.3ms. Higher refresh rate = *smaller* budget per frame. (In practice a portion of the budget is reserved for the system's own compositing, so your usable budget is somewhat less than the full interval.)","**Why high-refresh displays make it harder**: modern phones increasingly ship 90Hz and 120Hz displays, which halve the budget (from ~16ms to ~8ms). Work that comfortably fit a 60Hz frame might *overrun* a 120Hz frame — so an app that was smooth on 60Hz devices can jank on 120Hz ones. The higher the refresh rate, the less main-thread time you can afford per frame, and the more disciplined you must be about keeping per-frame work minimal.","**The consistency requirement**: it's not just *average* frame time that matters — a single frame that overruns causes a visible hitch. So you need *every* frame to fit the budget, not just most. Occasional expensive frames (a heavy bind when a complex item scrolls in, a GC pause, a large recomposition) cause perceptible stutters even if the average is fine. This is why jank is often intermittent — tied to specific expensive operations rather than constant slowness."]},{t:"list",items:["**The implications for how you build**: keep per-frame main-thread work minimal and *predictable* — move all heavy/variable work off the main thread (so no frame is at the mercy of a slow operation), precompute render-ready data (so binds/composables are cheap), reduce allocations (so GC doesn't pause a frame), and profile against the *tightest* budget your users' devices demand (test on 120Hz devices, not just 60Hz). Adaptive refresh rate adds nuance — the system may lower the rate for static content and raise it for scrolling/animation, so your smooth-scroll path specifically must hit the high-refresh budget."]},{t:"p",text:"**The senior framing**: the frame budget is the fundamental constraint behind all UI performance — 'smooth' literally means 'every frame's work fits the budget', and 'janky' means 'some frames don't'. High-refresh displays matter because they *shrink* the budget (16ms → 8ms), so they expose performance problems that 60Hz hid, and they raise the bar for how little main-thread work you can afford. The practical consequence is that you optimize for the *tightest* budget and for *consistency* (every frame, not the average), which drives the core disciplines — off-main-thread work, cheap per-frame operations, minimal allocations. Understanding that the budget is refresh-rate-dependent and shrinking (as devices move to 120Hz) — and that consistency across frames, not average performance, is what the user perceives — is the systems-level understanding these questions probe."}]},{level:"senior",q:"A list scrolls smoothly in your testing but users report janky scrolling. Walk through diagnosing and fixing it.",a:[{t:"list",items:["**1. Rule out the debug-build artifact first**: 'smooth in my testing' — was that a *debug* build? Debug builds are janky by nature (no R8, JIT during use). But the *reverse* here (smooth for you, janky for users) suggests a real difference — likely device (users on lower-end/higher-refresh devices), data (users with more/larger items), or build. Confirm you're testing a *release* build on a *representative* (mid-range, and high-refresh) device with realistic data, not a flagship with a tiny test dataset.","**2. Reproduce with realistic conditions**: use a lower-end device (or an emulator throttled to match), a large/real dataset, and real images (not placeholders). Jank often only appears with production-scale data on slower hardware — the gap between your testing and users' experience.","**3. Measure with the right tools**: run a Macrobenchmark with `FrameTimingMetric` scrolling the list on a release build to get objective jank numbers; use Perfetto/systrace to see *which* frames drop during scroll and *what* work causes them on the timeline. This turns 'users say it's janky' into 'these specific frames overran doing this specific work'.","**4. Check the usual list-jank causes in order of likelihood**: (a) *expensive item binds* — heavy work in `onBindViewHolder`/the item composable (formatting, computation, synchronous image decode) that runs on the frame as items scroll in; (b) *image loading* — decoding full-size bitmaps into small views (should use Coil/Glide with downsampling and caching); (c) *Compose instability* — item composables recomposing unnecessarily due to unstable parameters or missing keys (check the compiler reports / Layout Inspector recomposition counts); (d) *no baseline profile* — first-scroll jank from JIT-compiling the scroll path (baseline profiles precompile it); (e) *main-thread work triggered by scroll* — loading more data synchronously, or over-fetching.","**5. Fix based on the finding**: move formatting/computation out of binds into the ViewModel (so the UI gets render-ready data); route images through a caching loader with proper sizing; fix Compose stability (immutable item models, stable keys) so items skip recomposition; add a *baseline profile* for the scroll journey (often the single biggest win for first-scroll jank); ensure pagination/loading is off the main thread. Keep item binds/composables *cheap and cheap-to-skip*.","**6. Verify with the same measurement**: re-run the Macrobenchmark on the representative device and confirm frame timings improved; check Android Vitals over the following release to confirm real-user 'slow/frozen frames' metrics drop."]},{t:"list",items:["**The key insight about the 'smooth for me, janky for users' gap**: it almost always comes down to *testing conditions* — better hardware, smaller data, or a debug-vs-release/baseline-profile difference. Your flagship dev device with 10 test items and a debug build hides jank that a mid-range 120Hz device with 500 real items and a release build exposes. So the *first* move is closing that gap (representative device, data, release build), which often reveals the problem your testing masked."]},{t:"p",text:"**The senior framing**: the scenario tests two things — knowing the *specific* causes of list jank (expensive binds, unoptimized images, Compose instability, missing baseline profiles) and the diagnostic discipline (measure on release + representative device/data with Perfetto/Macrobenchmark, don't guess). But the sharpest insight is recognizing that 'smooth for me, janky for users' is a *testing-representativeness* problem — the fix starts by reproducing under realistic conditions (device, data, build), because you can't fix what you can't reproduce. Naming the ordered list-jank causes, the measurement tools, *and* the testing-gap insight — plus verifying with the same metrics and Android Vitals — is the comprehensive, senior answer that connects local diagnosis to real-user impact."}]},{level:"senior",q:"What is the Choreographer, and how does it relate to jank?",a:[{t:"p",text:"The `Choreographer` coordinates the app's frame work with the display's *vsync* signal — it schedules input, animation, and draw callbacks to run once per frame. If your main-thread work for a frame exceeds the budget (~16.6ms at 60Hz), the frame is *dropped* (jank). You can hook `Choreographer.postFrameCallback` to measure per-frame timing, and `FrameMetrics` builds on this."},{t:"list",items:["**Vsync-driven** — schedules per-frame input/animation/draw.","**Frame budget** — exceed it → dropped frame (jank).","**`postFrameCallback`** — measure per-frame timing.","**FrameMetrics** — built on it for detailed timing."]},{t:"note",text:"The Choreographer syncs the app's input/animation/draw work to the display's vsync, once per frame — main-thread work exceeding the budget (~16.6ms @60Hz) drops the frame (jank). postFrameCallback measures per-frame timing; FrameMetrics builds on it. Jank = missing the Choreographer's frame deadline."}]},{level:"junior",q:"What are the ANR thresholds for different components?",a:[{t:"p",text:"ANRs fire when the *main thread* is blocked too long, with different thresholds: ~5 seconds for *input event* handling (no response to a tap), ~10 seconds (foreground) / ~60s (background) for a `BroadcastReceiver`'s `onReceive`, and foreground-service-start timeouts (~10–20s). Exceeding these triggers the 'App isn't responding' dialog. So keep the main thread free — no blocking I/O, heavy computation, or synchronous IPC."},{t:"list",items:["**Input** — ~5s no response to input.","**`BroadcastReceiver`** — ~10s (foreground) / longer (background).","**Service start** — ~10–20s foreground-service-start timeout.","**Cause** — main-thread blocking (I/O/computation/IPC)."]},{t:"note",text:"ANR thresholds: ~5s for input handling, ~10s (foreground)/~60s (background) for a BroadcastReceiver's onReceive, ~10–20s for foreground-service start. Exceeding them triggers 'App isn't responding'. Keep the main thread free — no blocking I/O, heavy computation, or synchronous IPC."}]},{level:"senior",q:"What is overdraw, and how do you reduce it?",a:[{t:"p",text:"Overdraw is painting the *same pixel multiple times* in one frame (stacked opaque backgrounds, layers over layers) — each extra layer costs GPU time and contributes to jank in the draw phase. Reduce it by removing redundant/nested backgrounds, clipping to the visible area, and flattening layers. Diagnose with 'Debug GPU Overdraw' (developer options), which colors pixels by overdraw count (blue=1×, red=4×+) — aim to reduce red."},{t:"list",items:["**Overdraw** — same pixel painted multiple times per frame.","**Cost** — GPU draw time; contributes to jank.","**Reduce** — remove redundant backgrounds, clip, flatten.","**Diagnose** — 'Debug GPU Overdraw' (blue=1×…red=4×+)."]},{t:"note",text:"Overdraw = painting the same pixel multiple times per frame (stacked opaque backgrounds/layers) — a draw-phase GPU cost contributing to jank. Reduce by removing redundant/nested backgrounds, clipping to visible area, flattening. Diagnose with Debug GPU Overdraw (blue=1×…red=4×+); shrink the red."}]},{level:"senior",q:"What is the difference between the UI thread and the RenderThread?",a:[{t:"p",text:"The *UI (main) thread* runs your app code — layout, measure, and *recording* draw commands (a display list). The *RenderThread* (a separate system thread) takes that display list and *executes* the GPU rendering (rasterization) off the main thread, so animations can continue smoothly even if the main thread is briefly busy. Jank can occur on either: main-thread work overrunning the frame, or heavy GPU work on the RenderThread."},{t:"list",items:["**UI thread** — app code, layout/measure, records draw commands.","**RenderThread** — executes GPU rendering off the main thread.","**Smoother animations** — GPU work offloaded.","**Jank on either** — main-thread overrun or heavy GPU."]},{t:"note",text:"The UI (main) thread runs app code + layout/measure + records draw commands (display list); the RenderThread executes GPU rendering (rasterization) off the main thread, keeping animations smooth when main is briefly busy. Jank can come from either — main-thread overrun or heavy GPU work on the RenderThread."}]},{level:"junior",q:"What are the three rendering phases?",a:[{t:"p",text:"Rendering a frame goes through *measure* (each view/composable computes its size given constraints), *layout* (positions are assigned), and *draw* (pixels are painted). A deep view hierarchy makes measure/layout expensive (multiple passes), and complex drawing (overdraw, custom canvas) makes draw expensive. Understanding which phase is slow guides the fix (flatten layouts vs reduce overdraw)."},{t:"list",items:["**Measure** — compute sizes given constraints.","**Layout** — assign positions.","**Draw** — paint pixels.","**Diagnose the slow phase** — layout (flatten) vs draw (overdraw)."]},{t:"note",text:"Rendering phases: measure (compute sizes), layout (assign positions), draw (paint pixels). Deep hierarchies make measure/layout expensive (multiple passes); overdraw/complex canvas makes draw expensive. Identify the slow phase to fix it (flatten layouts vs reduce overdraw). Compose has the same three phases."}]},{level:"senior",q:"How do you measure jank programmatically (FrameMetrics, JankStats)?",a:[{t:"p",text:"`FrameMetrics` (API 24+) reports per-frame timing (input, animation, layout, draw, GPU) so you can detect slow frames. The *JankStats* Jetpack library builds on it, providing a simple API to track jank (and attach *state* — which screen was active) and report it (to analytics), so you know *where* jank happens in production. In the lab, use the *Macrobenchmark* library's `FrameTimingMetric` for repeatable jank numbers."},{t:"list",items:["**`FrameMetrics`** — per-frame timing (API 24+).","**JankStats** — tracks jank + attaches state; report to analytics.","**Macrobenchmark `FrameTimingMetric`** — repeatable lab numbers.","**Production insight** — know where jank happens."]},{t:"note",text:"FrameMetrics (API 24+) reports per-frame timing (input/animation/layout/draw/GPU). The JankStats Jetpack library builds on it — tracks jank with attached state (which screen) and reports to analytics (production insight). In the lab, Macrobenchmark's FrameTimingMetric gives repeatable jank numbers."}]},{level:"junior",q:"What are slow frames and frozen frames in Play vitals?",a:[{t:"p",text:"Play Console's Android vitals tracks two rendering metrics: *slow frames* (frames taking >16ms — visible jank/stutter) and *frozen frames* (frames taking >700ms — the app appears hung, a much worse experience). Google flags apps exceeding thresholds (they can affect discoverability). Frozen frames are especially bad (users perceive a freeze); slow frames are stutters. Both indicate main-thread work overrunning frame budgets."},{t:"list",items:["**Slow frames** — >16ms; visible stutter.","**Frozen frames** — >700ms; app appears hung (worse).","**Play vitals** — flags exceeding thresholds (discoverability).","**Cause** — main-thread work overrunning frame budgets."]},{t:"note",text:"Play vitals rendering metrics: slow frames (>16ms — visible stutter) and frozen frames (>700ms — app appears hung, much worse). Google flags apps exceeding thresholds (affecting discoverability). Both indicate main-thread work overrunning frame budgets. Frozen frames especially hurt UX."}]},{level:"senior",q:"How do you read an ANR trace to diagnose the cause?",a:[{t:"p",text:"When an ANR occurs, the system captures a *trace* (in `/data/anr/`, or Play Console → Android vitals → ANRs) showing the *stack of the main thread at the moment of the freeze* (plus other threads). Read the main thread's stack to see *what it was blocked on* — a synchronous network/DB call, a lock (deadlock), a slow binder call, or heavy computation. That points directly to the code to fix (move off the main thread)."},{t:"list",items:["**ANR trace** — `/data/anr/` or Play vitals.","**Main thread stack** — what it was blocked on at the freeze.","**Culprits** — sync I/O, locks/deadlock, slow IPC, computation.","**Fix** — move the blocking work off the main thread."]},{t:"note",text:"An ANR trace (in /data/anr/ or Play vitals → ANRs) shows the main thread's stack at the freeze (plus other threads). Read it to see what main was blocked on — sync network/DB, a lock (deadlock), slow binder, or computation — pointing to the code to move off the main thread."}]},{level:"senior",q:"How can a deadlock cause an ANR?",a:[{t:"p",text:"If the *main thread* acquires a lock and waits for another lock held by a background thread that's waiting for the main thread's lock (or the main thread waits on a lock a stuck background thread holds), the main thread is *blocked indefinitely* → ANR. Deadlocks show in the ANR trace as the main thread `BLOCKED`/`WAITING` on a monitor. Avoid by minimizing locks on the main thread, consistent lock ordering, and not blocking the main thread on background work (use callbacks/coroutines)."},{t:"list",items:["**Deadlock** — main thread blocked on a lock held by a stuck thread.","**Indefinite block** → ANR.","**Trace** — main thread BLOCKED/WAITING on a monitor.","**Avoid** — minimize main-thread locks, consistent ordering, don't block main."]},{t:"note",text:"A deadlock (main thread blocked on a lock held by a stuck/waiting background thread) blocks main indefinitely → ANR. The trace shows main BLOCKED/WAITING on a monitor. Avoid: minimize main-thread locks, use consistent lock ordering, and don't block the main thread on background work (coroutines/callbacks)."}]},{level:"senior",q:"How can a lazy list (RecyclerView/LazyColumn) cause jank, and how do you fix it?",a:[{t:"p",text:"Jank comes from *expensive per-item work* on the main thread during scroll: heavy binding/composition, oversized image decoding, complex layouts, or reading rapidly-changing state. Fix by: keeping item content cheap (precompute in the ViewModel), right-sizing images (Coil/Glide to display size), providing stable keys + contentType (Compose) / DiffUtil (Views), avoiding heavy work in `onBind`/composition, and generating a Baseline Profile. Profile on a release build."},{t:"list",items:["**Expensive per-item work** — binding/composition, image decode, layout.","**Fix** — cheap item content, right-sized images, stable keys/DiffUtil.","**No heavy work** — in onBind/composition; precompute.","**Baseline Profile** — + profile on release."]},{t:"note",text:"Lazy-list jank = expensive per-item main-thread work during scroll (heavy binding/composition, oversized image decode, complex layouts, rapid state reads). Fix: cheap item content (precompute), right-sized images, stable keys + contentType/DiffUtil, no heavy work in onBind/composition, a Baseline Profile. Profile on release."}]},{level:"senior",q:"How does image loading cause jank, and how do you prevent it?",a:[{t:"p",text:"Decoding a full-resolution image into a small view is expensive (CPU + big allocations → GC pauses) and, if done on the main thread, blocks frames. Prevent it by using an image library (Coil/Glide) that decodes *off the main thread*, *downsamples* to the displayed size (crucial — pass a bounded size), caches (memory + disk), and uses placeholders/crossfade. Never decode bitmaps on the main thread or load full-res into a thumbnail."},{t:"list",items:["**Full-res decode** — CPU + allocations → GC jank; blocks if on main.","**Coil/Glide** — decode off main, downsample to display size.","**Cache + placeholders** — memory/disk; smooth loading.","**Never** — decode on main / load full-res into a thumbnail."]},{t:"note",text:"Image jank: decoding full-res into a small view (CPU + big allocations → GC pauses; blocks frames if on main). Prevent with Coil/Glide — decode off-main, downsample to the displayed size (pass a bounded size), cache (memory+disk), placeholders/crossfade. Never decode bitmaps on main or load full-res into a thumbnail."}]},{level:"senior",q:"How does garbage collection cause jank?",a:[{t:"p",text:"Excessive object *allocation* (especially in hot paths like scroll or per-frame) fills the heap, triggering *garbage collection* — GC pauses (even short ones) can cause the app to miss a frame (jank). Reduce it by avoiding allocations in hot paths: reuse objects, avoid autoboxing (use `IntArray` over `List<Int>`), don't allocate in `onDraw`/per-frame code, and use object pools where appropriate. Profile allocations with the Memory Profiler."},{t:"list",items:["**Excessive allocation** — fills the heap, triggers GC.","**GC pauses** — can miss a frame (jank).","**Reduce** — reuse objects, avoid boxing, no allocation in hot paths.","**Profile** — Memory Profiler for allocation hotspots."]},{t:"note",text:"Excessive object allocation (in hot paths — scroll/per-frame) fills the heap and triggers GC; even short GC pauses can miss a frame (jank). Reduce: reuse objects, avoid autoboxing (IntArray over List<Int>), no allocation in onDraw/per-frame, object pools. Profile allocations with the Memory Profiler."}]},{level:"senior",q:"How do deep view hierarchies hurt rendering, and how do you flatten them?",a:[{t:"p",text:"Deeply nested layouts (many `LinearLayout`s inside each other) make *measure/layout* expensive — nested weights can cause *multiple measure passes* (exponential in depth). Flatten with `ConstraintLayout` (one flat hierarchy replacing nesting), remove redundant container views, and use `merge`/`ViewStub`. In *Compose*, the layout system is more efficient (single-pass by default), but avoid unnecessary nesting and intrinsic measurements. Diagnose with Layout Inspector."},{t:"list",items:["**Deep nesting** — expensive measure/layout; nested weights = multiple passes.","**Flatten** — `ConstraintLayout`, remove redundant containers, `merge`.","**Compose** — more efficient; avoid needless nesting/intrinsics.","**Diagnose** — Layout Inspector."]},{t:"note",text:"Deep view hierarchies make measure/layout expensive — nested LinearLayout weights cause multiple measure passes (exponential in depth). Flatten with ConstraintLayout, remove redundant containers, use merge/ViewStub. Compose is more efficient (single-pass) but avoid needless nesting/intrinsics. Diagnose with Layout Inspector."}]},{level:"senior",q:"How does Compose recomposition affect jank, and how do you reduce it?",a:[{t:"p",text:"Excessive or expensive *recomposition* (re-running composables) on the main thread can jank — especially reading rapidly-changing state high in the tree, unstable parameters preventing skipping, or heavy work in composable bodies. Reduce with: stable/immutable parameters (enable skipping), `derivedStateOf`/lambda deferral for fast-changing state, reading state in the smallest scope, and moving computation out of composition (`remember`/ViewModel). Measure with Layout Inspector recomposition counts."},{t:"list",items:["**Excessive recomposition** — re-running composables janks.","**Causes** — unstable params, high state reads, heavy body work.","**Reduce** — stability/skipping, derivedStateOf, defer reads, move computation out.","**Measure** — Layout Inspector recomposition counts."]},{t:"note",text:"Excessive/expensive Compose recomposition on main can jank (rapid state read high in the tree, unstable params blocking skipping, heavy body work). Reduce: stable/immutable params (skipping), derivedStateOf/lambda deferral, read state in the smallest scope, move computation out (remember/ViewModel). Measure with Layout Inspector recomposition counts."}]},{level:"junior",q:"What is the difference between input latency and rendering jank?",a:[{t:"p",text:"*Input latency* is the delay between the user's touch and the app *responding* (e.g. a button feels slow to react) — often because the main thread is busy when the input arrives. *Rendering jank* is *dropped/stuttering frames* during animation/scroll (the app responds but janks). Both stem from a busy main thread, but they're different symptoms: latency = slow to start reacting; jank = choppy visuals. Both are fixed by keeping the main thread free."},{t:"list",items:["**Input latency** — delay before the app reacts to touch.","**Rendering jank** — dropped/stuttering frames.","**Both** — from a busy main thread.","**Different symptoms** — slow-to-react vs choppy visuals."]},{t:"note",text:"Input latency = delay between touch and the app responding (main thread busy when input arrives). Rendering jank = dropped/stuttering frames during animation/scroll. Both stem from a busy main thread but are different symptoms (slow-to-react vs choppy). Both fixed by keeping main free."}]},{level:"senior",q:"How do you profile a slow frame in Perfetto/systrace?",a:[{t:"p",text:"Capture a *system trace* (Perfetto, via Android Studio's profiler or `perfetto`/`systrace`), find the janky frames (marked in the frame timeline), and *expand* them to see what ran on the main thread and RenderThread during that frame — which functions took long. Custom `Trace.beginSection`/`endSection` markers let you label your own code in the trace. This pinpoints the exact slow operation causing the dropped frame."},{t:"list",items:["**System trace** — Perfetto/systrace via profiler.","**Find janky frames** — marked in the frame timeline.","**Expand** — see main/RenderThread work during the frame.","**`Trace.beginSection`** — label your own code."]},{t:"note",text:"Capture a system trace (Perfetto via Android Studio profiler or perfetto/systrace), find janky frames in the frame timeline, expand them to see main/RenderThread work during that frame (which functions took long). Trace.beginSection/endSection labels your own code. Pinpoints the exact slow operation."}]},{level:"junior",q:"How do you keep animations smooth at 60/120fps?",a:[{t:"p",text:"Animate *cheap, draw-phase* properties (alpha, translation, scale, rotation) rather than layout-affecting ones (which re-run measure/layout each frame). In Compose, use `graphicsLayer`/`offset {}` lambdas so animation reads happen in the draw/layout phase, not recomposition. Keep per-frame work minimal (no allocation/heavy computation), and remember higher refresh rates (120Hz) *halve* the frame budget (~8.3ms), demanding even leaner frames."},{t:"list",items:["**Animate draw-phase props** — alpha/translation/scale/rotation.","**Avoid layout-affecting** — re-runs measure/layout per frame.","**`graphicsLayer`/`offset {}`** — defer reads in Compose.","**120Hz** — halves the budget (~8.3ms); leaner frames."]},{t:"note",text:"Smooth animations: animate cheap draw-phase properties (alpha/translation/scale/rotation), not layout-affecting ones (re-run measure/layout). In Compose use graphicsLayer/offset {} lambdas to defer reads to draw/layout, not recomposition. Keep per-frame work minimal (no allocation). 120Hz halves the budget (~8.3ms) — leaner frames needed."}]},{level:"junior",q:"How do you avoid ANRs when doing heavy work?",a:[{t:"p",text:"Never do blocking/heavy work on the main thread — offload it to a *coroutine* with `withContext(Dispatchers.IO)` (I/O) or `Default` (CPU), and update the UI back on Main. For very heavy computation, chunk it (yield periodically) or use WorkManager for deferrable background work. Also avoid synchronous IPC/binder calls and slow content-provider queries on main. StrictMode (debug) flags main-thread I/O early."},{t:"list",items:["**Offload** — `withContext(IO/Default)`; UI back on Main.","**Chunk heavy computation** — yield periodically.","**WorkManager** — for deferrable background work.","**Avoid** — sync IPC/slow provider queries on main; StrictMode catches I/O."]},{t:"note",text:"Avoid ANRs by never blocking main: offload with withContext(Dispatchers.IO/Default), update UI back on Main; chunk very heavy computation (yield); use WorkManager for deferrable work. Avoid synchronous IPC/slow provider queries on main. StrictMode (debug) flags main-thread I/O early."}]},{level:"senior",q:"How do you handle a BroadcastReceiver or Service that causes an ANR?",a:[{t:"p",text:"A `BroadcastReceiver`'s `onReceive` and a `Service`'s callbacks run on the *main thread* with time limits (~10s / ~20s) — doing heavy work there causes an ANR. Fix: in `onReceive`, *enqueue WorkManager* or use `goAsync()` for brief work (don't do it inline); in a `Service`, offload to a coroutine (`lifecycleScope`) and don't block the main thread. The receiver/service should be a lightweight *trigger*, not the executor of heavy work."},{t:"list",items:["**Main-thread limits** — ~10s receiver, ~20s service start.","**Receiver** — enqueue WorkManager / `goAsync()` for brief work.","**Service** — offload to a coroutine; don't block main.","**Trigger, not executor** — of heavy work."]},{t:"note",text:"onReceive (~10s) and Service callbacks (~20s) run on main with time limits — heavy work there ANRs. Fix: receiver enqueues WorkManager or goAsync() for brief work; Service offloads to a coroutine (lifecycleScope), never blocking main. The receiver/service is a lightweight trigger, not the executor of heavy work."}]},{level:"senior",q:"What is the difference between a CPU-bound and GPU-bound janky frame?",a:[{t:"p",text:"A *CPU-bound* frame is slow because *main-thread work* (measure/layout, recomposition, computation, allocations) overran the budget — fix by reducing that work. A *GPU-bound* frame is slow because *rendering* (rasterization on the RenderThread) took too long — usually *overdraw*, complex shaders, large/expensive drawing — fix by reducing overdraw and drawing complexity. A system trace shows which: long main-thread work vs long GPU/RenderThread work."},{t:"list",items:["**CPU-bound** — main-thread work overran (layout/recompose/compute).","**GPU-bound** — rendering (rasterization) too long (overdraw/complex draw).","**Trace shows which** — main-thread vs GPU/RenderThread duration.","**Fix accordingly** — reduce main work vs draw complexity."]},{t:"note",text:"CPU-bound frame: main-thread work (measure/layout/recompose/compute/allocations) overran — reduce that. GPU-bound frame: rendering (rasterization) took too long — usually overdraw/complex draw — reduce those. A system trace shows which (long main-thread vs long GPU/RenderThread). Fix the actual bottleneck."}]},{level:"senior",q:"What is a frozen frame, and why is it worse than a slow frame?",a:[{t:"p",text:"A *slow frame* misses the frame budget (>16ms) — a brief stutter the user may barely notice. A *frozen frame* takes >700ms — the app visibly *hangs* for most of a second, which users perceive as the app being broken (much worse than a stutter). Frozen frames usually indicate a big main-thread block (sync I/O, huge computation, a giant layout) — closer to a mini-ANR. Play vitals tracks both separately; eliminating frozen frames is higher priority."},{t:"list",items:["**Slow frame** — >16ms; brief stutter.","**Frozen frame** — >700ms; app visibly hangs (much worse).","**Cause** — big main-thread block (sync I/O, huge layout).","**Priority** — eliminate frozen frames first."]},{t:"note",text:"Slow frame (>16ms) = brief stutter; frozen frame (>700ms) = the app visibly hangs for most of a second (users read it as broken — much worse, like a mini-ANR). Frozen frames usually mean a big main-thread block. Play vitals tracks both; eliminating frozen frames is higher priority."}]},{level:"senior",q:"How do you handle heavy computation without janking the UI?",a:[{t:"p",text:"Move it off the main thread — `withContext(Dispatchers.Default)` for CPU-bound work — and post only the result back to Main. If the computation must partly touch the main thread (or produces incremental UI), *chunk* it and `yield()` between chunks so frames can render in between. For truly heavy or repeated work, cache/precompute results. Never run an O(n) loop over a large dataset on the main thread during a frame — it blows the budget."},{t:"code",title:"Offloading + chunking",code:`// CPU-bound work off the main thread
val result = withContext(Dispatchers.Default) { expensiveCompute(data) }
render(result) // back on Main

// or chunk long work so frames can render between pieces
for (chunk in items.chunked(500)) { process(chunk); yield() }`},{t:"note",text:"Heavy computation: move off main with withContext(Dispatchers.Default), post only the result back to Main. If it must touch main incrementally, chunk it and yield() between chunks so frames render. Cache/precompute repeated work. Never run an O(n) loop over a large dataset on main during a frame — it blows the budget."}]},{level:"junior",q:"What is the difference between a soft and hard ANR?",a:[{t:"p",text:"A *soft* (input-dispatch) ANR fires when the app fails to process an *input event* within ~5s — the user's tap/scroll goes unanswered, triggering the 'App isn't responding' dialog. A *hard* ANR-type situation relates to component timeouts — a `BroadcastReceiver` not finishing `onReceive` in time, or a service/foreground-service-start timeout. The common root cause is the same: the *main thread is blocked*. Both are diagnosed via the ANR trace showing the stuck main thread."},{t:"list",items:["**Soft (input) ANR** — no response to input within ~5s.","**Component timeout** — receiver/service didn't finish in time.","**Common cause** — blocked main thread.","**Diagnose** — ANR trace of the stuck main thread."]},{t:"note",text:"Soft/input ANR: failed to process an input event within ~5s ('App isn't responding'). Component-timeout ANRs: a BroadcastReceiver/service didn't finish in its window. Both share a root cause — a blocked main thread — and are diagnosed via the ANR trace showing the stuck main thread."}]},{level:"junior",q:"What is StrictMode, and how does it help catch jank sources?",a:[{t:"p",text:"`StrictMode` (debug tool) detects *main-thread policy violations* — disk/network I/O on the UI thread — which are prime jank/ANR causes, and *VM violations* (leaked Activities, unclosed resources). Enable it in debug builds with `penaltyLog`/`penaltyDeath` to catch these mistakes *during development*, before they ship as production jank. It surfaces accidental main-thread I/O you might not notice otherwise."},{t:"list",items:["**Thread policy** — main-thread disk/network I/O detection.","**VM policy** — leaks, unclosed resources.","**Debug only** — `penaltyLog`/`penaltyDeath`.","**Catch early** — accidental main-thread I/O before production."]},{t:"note",text:"StrictMode (debug) detects main-thread I/O (disk/network on the UI thread — prime jank/ANR causes) and VM violations (leaks, unclosed resources). Enable in debug (penaltyLog/penaltyDeath) to catch accidental main-thread I/O during development, before it ships as production jank."}]}],zC={id:"anr-jank",title:"ANRs, Jank & Rendering",tagline:"The frame budget, what causes jank, ANRs as extreme main-thread blocking, how jank & ANRs relate, diagnosis tools, and the fixing toolkit.",tags:["Performance","Jank","ANR","Frame Budget","Rendering"],content:WC,qa:BC},HC=[{heading:"Profile, don't guess — the golden rule",blocks:[{t:"p",text:"The single most important principle of performance work: **measure before optimizing**. Developers' intuitions about *where* an app is slow are frequently wrong — the actual bottleneck is often somewhere you didn't expect. **Profiling** means measuring your app's real behavior (CPU time, memory, frame timing, network, energy) with tools, to find the *actual* bottleneck, then optimizing that specific thing and *measuring again* to confirm the fix. Optimizing without profiling wastes effort on things that don't matter and can even make things worse."},{t:"list",items:["**The cost of guessing**: you spend time 'optimizing' code that wasn't the bottleneck (no user-visible improvement), while the real problem stays. Or you add complexity (caching, micro-optimizations) that isn't needed, making code harder to maintain for no benefit.","**The workflow**: reproduce the problem → profile to find the bottleneck → fix that specific thing → profile again to confirm improvement → repeat. Measurement bookends every optimization.","**Always profile release builds** — debug builds have no R8 and JIT-compile during use, so their performance is unrepresentative. Optimizing based on debug numbers is optimizing the wrong thing."]}]},{heading:"The Android Studio Profiler",blocks:[{t:"list",items:["**CPU Profiler** — records what methods run and how long they take (method traces, or sampled/system traces). Use it to find *where* CPU time goes — the expensive method causing jank or slow startup. It shows call stacks and time attribution.","**Memory Profiler** — tracks allocations and heap usage over time; capture *heap dumps* to inspect all live objects, their retained sizes, and reference chains (for leak hunting and reducing memory footprint).","**Energy Profiler** — shows what's consuming battery (CPU, network, GPS, wakelocks) — for diagnosing battery drain.","**Network Profiler** — inspects network requests/responses, timing, and payload sizes — for finding slow or oversized requests.","**Choosing**: match the tool to the symptom — slow/janky → CPU profiler + Perfetto; growing memory/OOM → Memory profiler; battery drain → Energy profiler; slow loading → Network profiler."]}]},{heading:"Perfetto / systrace — system-level tracing",blocks:[{t:"p",text:"**Perfetto** (the modern successor to systrace) captures a detailed *system-level* trace — a timeline showing what every thread (including the main thread and the system) is doing, frame by frame, with your own custom trace markers. It's the primary tool for diagnosing **jank and startup** because it shows *exactly* which frame overran and what work (which method, which thread contention, which lock, which GC pause) caused it, in context."},{t:"list",items:["**Why it's better than the CPU profiler for jank/startup**: it shows the *whole system timeline* — the main thread's frame work, background threads, the system compositor, GC events — so you see not just 'this method is slow' but *why a specific frame missed its budget* (a lock wait, a GC pause, main-thread I/O) in the full context.",'**Custom trace markers** — you add `trace("myOperation") { }` markers in your code so they appear on the timeline, letting you measure and locate your own operations precisely.',"**Use it for**: jank (which frames drop and why), startup (what happens during launch, on the critical path), thread contention, and general 'where does the time go' at a system level."]}]},{heading:"Benchmarking: Micro and Macro",blocks:[{t:"list",items:["**Microbenchmark** (Jetpack) — measures the performance of a *small piece of code* (a function, an algorithm) in isolation, running it many times and reporting precise timing. Use it to compare implementations (is this sort faster?) or catch performance regressions in a specific hot function. It handles warmup and runs on-device for realistic numbers.","**Macrobenchmark** (Jetpack) — measures *whole user journeys* on a *release build* — app startup, scrolling a list — reporting metrics like `StartupTimingMetric` (cold/warm start times) and `FrameTimingMetric` (jank). This is the tool for *realistic, reproducible* performance numbers on the things users actually experience, and for tracking them over time / catching regressions in CI.","**Why Macrobenchmark matters**: it measures on release builds with real journeys, giving objective numbers you can optimize against and regression-test. It's also how you *generate baseline profiles*."]}]},{heading:"App startup and Baseline Profiles",blocks:[{t:"p",text:"**Startup time** is a critical metric — users judge an app by how fast it launches, especially cold start. Cold start does the most work (create the process, run `Application.onCreate`, inflate and draw the first frame). The two biggest levers are a *lean startup path* and *baseline profiles*."},{t:"list",items:["**Cold vs warm vs hot start** — cold (process created from scratch, slowest), warm (process alive, Activity recreated), hot (Activity in memory, fastest). Optimize *cold* start — it's the worst and what users judge.","**Lean `Application.onCreate`** — it runs on the main thread on every cold start, so heavy work there directly slows launch. Defer/lazy-init non-critical libraries; use the **App Startup** library to consolidate and order initializers (instead of each library adding its own ContentProvider, each adding cost).","**Baseline Profiles — the biggest single win**: Android ships apps as bytecode that must be compiled to native code; without help, the startup (and first-scroll) code path is *JIT-compiled during* the user's first launch, causing lag. A **Baseline Profile** is a shipped list of hot methods/classes that get *ahead-of-time (AOT) compiled at install time*, so the critical paths are already native on first run. Real impact: commonly **20-40% faster cold startup** and much smoother first scroll.","**Generating one** — a Macrobenchmark test with `BaselineProfileRule` exercises the critical journeys (startup, scroll), recording the hot paths into a `baseline-prof.txt` bundled with the app; the Baseline Profile Gradle plugin automates it. Compose *libraries* ship their own, but your *app's* screens need your own profile.","**Other startup levers** — avoid work before the first frame (show a skeleton, load async), keep the initial layout simple, use the SplashScreen API, and measure startup with `Macrobenchmark`/`adb shell am start -W`."]},{t:"note",text:"Profiling: measure before optimizing (intuitions are often wrong) — profile → fix the real bottleneck → re-measure, on release builds. Tools: Android Studio Profiler (CPU/Memory/Energy/Network), Perfetto/systrace (system timeline — the primary jank/startup tool, shows why a frame overran), Microbenchmark (isolated code) and Macrobenchmark (whole journeys on release — StartupTimingMetric/FrameTimingMetric). Startup: optimize cold start (lean Application.onCreate, App Startup lib) and use Baseline Profiles (AOT-compile the hot path at install → 20-40% faster cold start), generated via Macrobenchmark."}]}],KC=[{level:"junior",q:"Why should you profile before optimizing?",a:[{t:"p",text:"**Because developers' intuitions about where an app is slow are frequently wrong — the actual bottleneck is often somewhere you didn't expect — so optimizing without measuring wastes effort on things that don't matter while the real problem stays.** Profiling means measuring your app's real behavior (CPU time, memory, frame timing) with tools to find the *actual* bottleneck, then fixing that specific thing and measuring again to confirm."},{t:"list",items:["**Guessing wastes effort**: you spend time 'optimizing' code that wasn't the bottleneck (no user-visible improvement), while the true problem — which you didn't measure — remains. Or you add unnecessary complexity (caching, micro-optimizations) that makes code harder to maintain for no real gain.","**Optimization can make things worse**: a change that seems faster might not be, or might trade one problem for another. Only measurement tells you whether an optimization actually helped.","**The workflow**: reproduce → profile to find the bottleneck → fix that specific thing → profile again to confirm improvement. Measurement bookends every optimization."]},{t:"p",text:"There's also a critical detail: **profile on release builds**, not debug — debug builds have no R8 optimization and JIT-compile during use, so their performance is unrepresentative, and optimizing based on debug numbers optimizes the wrong thing. The principle is 'measure, don't guess': performance work should be *data-driven*, targeting the proven bottleneck, because the cost of guessing is wasted effort and added complexity with no benefit. This is why every performance answer emphasizes tools (Perfetto, the profiler, Macrobenchmark) — they're how you find the real problem instead of guessing."}]},{level:"junior",q:"What tools do you use to profile an Android app?",a:[{t:"list",items:["**Android Studio Profiler** — has several views: *CPU* (what methods run and how long — find where CPU time goes), *Memory* (allocations and heap dumps — find leaks and memory usage), *Energy* (battery consumers), and *Network* (request timing and sizes).","**Perfetto / systrace** — captures a system-level trace showing what every thread does frame by frame, with your custom markers. The primary tool for jank and startup — it shows *why* a specific frame overran (a lock wait, GC pause, main-thread I/O) in full system context.","**Macrobenchmark** — measures whole user journeys (startup, scroll) on release builds with objective metrics (`StartupTimingMetric`, `FrameTimingMetric`) — for realistic, reproducible numbers you can track over time.","**Microbenchmark** — measures a small piece of code in isolation, for comparing implementations or catching regressions in a hot function.","**Android Vitals (Play Console)** — production metrics (ANR rate, slow/frozen frames, crashes) across real users' devices — tells you *whether* you have a problem in the field."]},{t:"p",text:"The key is matching the tool to the symptom: slow/janky UI → CPU profiler + Perfetto; growing memory/OOM → Memory profiler + heap dumps + LeakCanary; battery drain → Energy profiler; slow requests → Network profiler; realistic startup/scroll numbers → Macrobenchmark; production reality → Android Vitals. Local tools (profiler, Perfetto) tell you *what* is slow and *why*; Android Vitals tells you *whether and where* (which devices) it matters to real users. A thorough performance investigation uses both — Vitals to find the problem's scope, local profiling to find the cause."}]},{level:"junior",q:"What is a Baseline Profile and why does it help?",a:[{t:"p",text:"**A Baseline Profile is a list of your app's hot code paths (classes and methods) shipped with the app, which the Android runtime *ahead-of-time compiles to native code at install time* — so those critical paths are already fast on the very first run, instead of being JIT-compiled during the user's first launch.** It primarily speeds up cold startup and first-scroll performance."},{t:"list",items:["**The problem it solves**: Android ships apps as bytecode that must be compiled to native code to run fast. Without a baseline profile, the startup and first-scroll code paths are compiled *just-in-time, during* the user's first interaction — causing launch lag and initial jank.","**How it helps**: with a baseline profile listing those hot paths, they're AOT-compiled at *install* time, so they're already native when the user first launches or scrolls. The measured impact is significant — commonly **20-40% faster cold startup** and much smoother first scroll.","**How you generate one**: a Macrobenchmark test with `BaselineProfileRule` exercises the critical journeys (launch the app, scroll the main list), recording the hot methods into a `baseline-prof.txt` that ships with the app."]},{t:"p",text:"It's often the single biggest performance win available, and it's the correct answer to 'the first scroll always stutters but later scrolls are smooth' (that pattern is classic JIT-on-first-use, which baseline profiles fix). One nuance: Compose *libraries* ship their own baseline profiles, so the framework internals are covered — but your *app's* screens aren't, so you must generate a profile for your own critical journeys to get the app-level benefit. Baseline profiles are low-effort (a benchmark test + Gradle plugin) and high-impact, which is why they're strongly recommended for any app where startup and scroll performance matter."}]},{level:"junior",q:"What is the difference between cold, warm, and hot start, and which do you optimize?",a:[{t:"p",text:"**They describe how much work Android must do to bring your app to the foreground, from most to least: cold (the process doesn't exist — create it, run Application.onCreate, create and draw the first Activity — slowest), warm (process alive but the Activity needs recreating — less work), and hot (Activity still in memory, just bring it to front — fastest).** You optimize *cold start* because it does the most work and users judge the app by it, especially on first launch."},{t:"list",items:["**Cold start** — the full launch: fork the process, initialize the runtime, run `Application.onCreate`, then create/inflate/draw the first screen. The slowest and the one to optimize.","**Warm start** — the process is still around but the Activity was destroyed (e.g. in the background); it's recreated. Faster — less to do.","**Hot start** — the Activity is still in memory; the system just brings it forward. Fastest, little to optimize."]},{t:"p",text:"The main levers for cold start: a **lean `Application.onCreate`** (it runs on the main thread on every cold start, so defer/lazy-init non-critical work; use the App Startup library to consolidate initializers rather than each library adding its own ContentProvider); a **Baseline Profile** (AOT-compile the startup path — often the biggest win); and **avoiding work before the first frame** (show a skeleton/placeholder and load data asynchronously, keep the initial layout simple, use the SplashScreen API). You measure cold start with `Macrobenchmark`'s `StartupTimingMetric` or `adb shell am start -W`. Since cold start is the worst case and the first impression, focusing optimization there gives the most user-visible benefit."}]},{level:"senior",q:"When would you use Perfetto/systrace versus the CPU profiler?",a:[{t:"p",text:"**Use the CPU profiler when you want to know *which methods consume CPU time* (method-level attribution) in your app's code; use Perfetto/systrace when you need to understand *why a specific frame or startup missed its target* in full system context — including thread contention, lock waits, GC pauses, and the system compositor, not just your method timings.** They're complementary, but Perfetto is the primary tool for jank and startup because those problems are often about *what the whole system was doing*, not just one slow method."},{t:"list",items:["**CPU profiler — method-centric**: it records call stacks and time spent per method (via sampling or instrumentation), answering 'where is CPU time going in my code?' It's great for finding a computationally expensive method or an inefficient algorithm — the classic 'this function is hot' investigation. But it's centered on *your app's method execution*.","**Perfetto/systrace — system-timeline-centric**: it captures a *timeline of every thread* (your main thread, background threads, and system threads like the render thread and the surface compositor), with frame boundaries, GC events, lock/monitor contention, binder calls, and your custom trace markers. This answers 'why did *this frame* take 30ms?' — and the answer is often *not* a slow method but a *lock wait* (main thread blocked waiting for a background thread), a *GC pause*, *main-thread I/O*, or *the render thread being overloaded*. The CPU profiler wouldn't clearly show these because they're not about method CPU time — they're about *waiting* and *system-level* events."]},{t:"list",items:["**Concrete decision**: 'this computation is slow' (an algorithm, a parse) → CPU profiler to find and optimize the hot method. 'scrolling drops frames' or 'startup is slow' → Perfetto first, because you need to see *which frames drop, on which thread, and why* (contention, GC, I/O, compositor) in context — then possibly drill into a specific slow method with the CPU profiler.","**Custom markers make Perfetto precise**: adding `trace(\"loadFeed\") { }` markers in your code puts your operations on the system timeline, so you can locate exactly where *your* work sits relative to frame boundaries and system events — bridging 'my code' and 'the whole system'.","**Startup specifically**: Perfetto shows the entire launch sequence across threads — process init, `Application.onCreate`, first inflate/compose, first draw — revealing what's on the *critical path* to the first frame, which is exactly what you need to optimize cold start. The CPU profiler alone wouldn't show the cross-thread launch sequence."]},{t:"p",text:"**The senior framing**: the distinction is *method CPU time* (CPU profiler) vs *system-level timeline including waiting and system events* (Perfetto). Jank and startup are frequently caused by things the CPU profiler doesn't surface well — lock contention, GC pauses, main-thread I/O, render-thread overload, cross-thread dependencies — because those are about *what the system was doing and waiting on*, not just which method ran. So Perfetto is the primary tool for those problems (it shows *why a frame missed its budget* in full context), while the CPU profiler is for drilling into a specific hot method once you know computation is the issue. Knowing that jank often isn't 'a slow method' but 'the main thread was blocked/waiting/paused' — and that Perfetto is what reveals that — is the depth that distinguishes someone who's actually diagnosed hard performance problems."}]},{level:"senior",q:"How would you approach reducing your app's cold startup time?",a:[{t:"p",text:"**I'd measure the current startup with Macrobenchmark to establish a baseline and find what's on the critical path (with Perfetto), then attack the biggest contributors in order — a lean Application.onCreate, deferred initialization, a baseline profile, and avoiding work before the first frame — remeasuring after each change.** The measurement-first, critical-path-focused approach is essential because startup has many potential contributors and you want to fix the ones that actually matter."},{t:"list",items:["**1. Measure and find the critical path**: use `Macrobenchmark` with `StartupTimingMetric` on a release build to get a reliable baseline (time to initial display and time to fully drawn), and Perfetto to see the launch timeline across threads — *what's actually on the path to the first frame*. This tells you where the time goes (process init, `Application.onCreate`, first inflate/compose, data loading) rather than guessing.","**2. Slim down `Application.onCreate`**: it runs on the main thread on *every* cold start, so anything heavy there directly adds to launch time. Audit it — move non-critical initialization off the critical path: lazy-initialize libraries (only when first used), defer analytics/non-essential SDKs to after the first frame, and move eager work to background threads. Use the **App Startup** library to consolidate library initializers into one ordered, lazy-capable initializer instead of each library adding its own ContentProvider (each of which adds startup cost).","**3. Add a Baseline Profile**: this is often the single biggest win. Without it, the startup code path is JIT-compiled *during* launch; a baseline profile AOT-compiles it at install, commonly cutting cold start 20-40%. Generate it with a `BaselineProfileRule` Macrobenchmark exercising the launch journey.","**4. Avoid work before the first frame**: don't block the first draw on data loading — show a skeleton/placeholder and load asynchronously (so time-to-first-frame is fast even if content arrives slightly later); keep the initial layout/composition simple (defer complex UI); use the official SplashScreen API (rather than a heavy custom splash Activity that adds a step).","**5. Optimize dependency initialization**: DI graph creation (Hilt), database opening, and network client setup on the startup path should be lazy where possible — don't eagerly build everything the app *might* need before showing the first screen.","**6. Remeasure after each change**: confirm each optimization actually improved the metric (Macrobenchmark), and watch for regressions over time (run the startup benchmark in CI). Also verify against **Android Vitals** in production to confirm real-user startup improved."]},{t:"list",items:["**Prioritize by impact**: the biggest contributors are usually a bloated `Application.onCreate`/eager initialization and the missing baseline profile — attack those first. Micro-optimizing a fast path while a heavy synchronous init sits on the critical path is wasted effort, which is why measuring the critical path first matters."]},{t:"p",text:"**The senior framing**: cold-start optimization is a *critical-path* problem — the goal is to minimize the work between the user tapping the icon and seeing a usable first frame. So you measure to find what's *on that path* (Macrobenchmark + Perfetto), then remove or defer everything non-essential from it: lean `Application.onCreate` and lazy init (don't do work you don't need before the first frame), a baseline profile (so the path is precompiled, not JIT'd during launch), and asynchronous data loading (so the first frame doesn't wait on content). The measurement discipline — baseline, fix the biggest critical-path contributor, remeasure, and regression-test in CI — is what makes it effective rather than a scattershot of micro-optimizations. Naming the specific high-impact levers (Application.onCreate, App Startup, baseline profiles, first-frame-async) *in priority order*, grounded in critical-path measurement, is the comprehensive answer."}]},{level:"senior",q:"What is a Baseline Profile and how does it work?",a:[{t:"p",text:"A *Baseline Profile* is a list of hot code paths (classes/methods) shipped with your app that tells ART to *ahead-of-time compile* them at install time, instead of interpreting/JIT-ing on first run. This speeds up *startup* and *jank-prone paths* (like first scroll) by 20–30%+. You generate it with the Macrobenchmark library by exercising critical journeys, and it ships in the app bundle; Play delivers it so the code is precompiled before the user runs it."},{t:"list",items:["**Hot code list** — classes/methods AOT-compiled at install.","**Speeds** — startup and jank-prone paths (~20–30%+).","**Generate** — Macrobenchmark exercising critical journeys.","**Delivered** — via the app bundle / Play."]},{t:"note",text:"A Baseline Profile lists hot code paths shipped with the app so ART AOT-compiles them at install (instead of interpret/JIT on first run) — speeding startup and jank-prone paths (first scroll) ~20–30%+. Generate with Macrobenchmark exercising critical journeys; it ships in the bundle and Play delivers it precompiled."}]},{level:"senior",q:"What is the Macrobenchmark library and what does it measure?",a:[{t:"p",text:"*Macrobenchmark* measures *whole-app, user-visible* performance on a real device/build — startup time (`StartupTimingMetric`) and rendering/jank (`FrameTimingMetric`) — by launching your app (in a release-like build) and running an interaction, repeatedly, for stable numbers. It's the tool for measuring startup and scroll performance, and for *generating Baseline Profiles*. Contrast with *Microbenchmark* (measures a small code snippet in isolation). Run it on a physical device, on a release/non-debuggable build."},{t:"list",items:["**Whole-app metrics** — `StartupTimingMetric`, `FrameTimingMetric`.","**Release-like build** — launches + interacts repeatedly.","**Generates** — Baseline Profiles.","**vs Microbenchmark** — small isolated code snippets."]},{t:"note",text:"Macrobenchmark measures whole-app user-visible performance (StartupTimingMetric, FrameTimingMetric) by launching a release-like build and running interactions repeatedly for stable numbers — the tool for startup/scroll perf and generating Baseline Profiles. Microbenchmark measures isolated snippets. Run on a physical, non-debuggable build."}]},{level:"junior",q:"What is the difference between Microbenchmark and Macrobenchmark?",a:[{t:"p",text:"*Microbenchmark* measures the speed of a *small piece of code* (a function, an algorithm) in isolation, in a tight loop, warming up the JIT and reporting nanosecond-level timing. *Macrobenchmark* measures *user-visible, whole-app* behavior (startup, scrolling) by driving the real app. Use Micro to compare two implementations of a hot function; use Macro to measure/regression-test startup and jank. Both run on a device, but at very different scopes."},{t:"table",headers:["","Microbenchmark","Macrobenchmark"],rows:[["Scope","One function/algorithm","Whole app / journey"],["Measures","ns-level code speed","Startup, jank"],["Use","Compare implementations","Startup/scroll regression"],["Also","—","Generates Baseline Profiles"]]},{t:"note",text:"Microbenchmark: speed of a small code piece in isolation (tight loop, ns timing) — compare two implementations of a hot function. Macrobenchmark: user-visible whole-app behavior (startup, scroll) by driving the real app — regression-test startup/jank and generate Baseline Profiles. Same device, very different scopes."}]},{level:"senior",q:"What is the difference between cold, warm, and hot startup?",a:[{t:"p",text:"*Cold start*: the process doesn't exist — the system creates it, runs `Application.onCreate`, then the first Activity (slowest; the one to optimize). *Warm start*: the process exists but the Activity must be recreated (some work reused). *Hot start*: the Activity is already in memory, just brought to the foreground (fastest). Optimize *cold* start because it's the worst case and users' first impression; measure it with `StartupTimingMetric`/`reportFullyDrawn`."},{t:"table",headers:["Type","State","Cost"],rows:[["Cold","Process created from scratch","Slowest (optimize this)"],["Warm","Process alive, Activity recreated","Medium"],["Hot","Activity in memory, foregrounded","Fastest"]]},{t:"note",text:"Cold: process created from scratch (Application.onCreate + first Activity) — slowest, optimize this. Warm: process alive but Activity recreated — medium. Hot: Activity in memory, just foregrounded — fastest. Optimize cold start (worst case + first impression); measure with StartupTimingMetric/reportFullyDrawn."}]},{level:"senior",q:"What is Time To Initial Display (TTID) vs Time To Full Display (TTFD)?",a:[{t:"p",text:"*TTID* is the time until the *first frame* is drawn (the UI appears, possibly with placeholders). *TTFD* is the time until the app is *fully usable* with real content loaded — you signal it by calling `reportFullyDrawn()` once the meaningful content is ready. TTID measures perceived launch speed; TTFD measures when the user can actually use the screen. Optimizing both matters: show something fast (TTID) and load real data promptly (TTFD)."},{t:"list",items:["**TTID** — first frame drawn (UI appears).","**TTFD** — app fully usable; signal via `reportFullyDrawn()`.","**TTID** — perceived launch speed.","**TTFD** — when the user can truly use the screen."]},{t:"note",text:"TTID = time to the first frame drawn (UI appears, maybe placeholders). TTFD = time until fully usable with real content — you signal it with reportFullyDrawn(). TTID = perceived speed; TTFD = actual usability. Optimize both: show something fast, load real data promptly."}]},{level:"senior",q:"How does the App Startup library help, and what problem does it solve?",a:[{t:"p",text:"Many libraries auto-initialize using a *ContentProvider* at startup (each provider adds overhead, and they run *before* your `Application.onCreate` in an uncontrolled order). The Jetpack *App Startup* library consolidates these into a *single* ContentProvider and lets you define `Initializer`s with explicit *dependencies* and *lazy* initialization — reducing startup cost and giving you control over ordering. It's a common cold-start optimization: fewer providers, deferred non-critical init."},{t:"list",items:["**Problem** — many library ContentProviders each add startup cost.","**App Startup** — one provider + `Initializer`s with dependencies.","**Lazy init** — defer non-critical initialization.","**Benefit** — less cold-start overhead, controlled ordering."]},{t:"note",text:"Libraries auto-initializing via separate ContentProviders each add startup cost and run before Application.onCreate in uncontrolled order. Jetpack App Startup consolidates them into one provider with Initializers (explicit dependencies, lazy init) — a common cold-start win: fewer providers, deferred non-critical init."}]},{level:"senior",q:"What work commonly bloats Application.onCreate, and how do you fix it?",a:[{t:"p",text:"`Application.onCreate` runs on the main thread *before the first frame* — heavy work here directly delays startup. Common culprits: eagerly initializing analytics/crash/DI/image libraries, reading disk/preferences, network calls. Fix: *defer* non-critical init (lazy, App Startup, or after first frame), move I/O off the main thread, and initialize only what the first screen needs. Measure with a trace to see what's spending time in `onCreate`."},{t:"list",items:["**Runs before first frame** — on main; delays startup.","**Culprits** — eager analytics/DI/image init, disk/network.","**Fix** — defer non-critical (lazy/App Startup/post-frame).","**Only** — init what the first screen needs."]},{t:"note",text:"Application.onCreate runs on main before the first frame — heavy work delays startup. Culprits: eager analytics/crash/DI/image init, disk reads, network. Fix: defer non-critical init (lazy, App Startup, or post-first-frame), move I/O off main, init only what the first screen needs. Trace onCreate to find the cost."}]},{level:"junior",q:"What is a splash screen's role in perceived startup, and how do you do it right?",a:[{t:"p",text:"A splash screen fills the *unavoidable* cold-start gap (process/UI init) with branding so the app doesn't look frozen — it improves *perceived* speed but doesn't make startup faster. Use the official *SplashScreen API* (androidx.core.splashscreen) rather than a fake splash Activity (which *adds* an Activity transition and slows startup). You can keep the splash visible while critical data loads (`setKeepOnScreenCondition`), but keep that brief."},{t:"list",items:["**Fills the cold-start gap** — perceived, not actual, speed.","**Official SplashScreen API** — not a fake splash Activity.","**Fake splash Activity** — adds a transition, slows startup.","**`setKeepOnScreenCondition`** — keep briefly while critical data loads."]},{t:"note",text:"A splash screen fills the unavoidable cold-start gap with branding (perceived, not actual, speed). Use the official SplashScreen API (androidx.core.splashscreen), not a fake splash Activity (which adds a transition and slows startup). Keep it on briefly for critical data via setKeepOnScreenCondition."}]},{level:"junior",q:"How do you measure startup time reliably?",a:[{t:"p",text:"Use `Macrobenchmark`'s `StartupTimingMetric` on a *physical device* with a *release/non-debuggable* build, running many iterations for a stable median (debug builds and emulators mislead). For quick local checks, `adb shell am start -W` reports `TotalTime`. In production, Play Console's Android vitals reports startup times across real devices. Always distinguish cold/warm/hot and report `reportFullyDrawn` (TTFD), not just the first frame."},{t:"list",items:["**Macrobenchmark `StartupTimingMetric`** — physical, release build, many iterations.","**`am start -W`** — quick `TotalTime` locally.","**Play vitals** — production startup across devices.","**Distinguish** — cold/warm/hot; report TTFD."]},{t:"note",text:"Measure startup with Macrobenchmark's StartupTimingMetric on a physical, non-debuggable release build over many iterations (debug/emulator mislead). Quick local check: adb shell am start -W (TotalTime). Production: Play vitals. Distinguish cold/warm/hot and report reportFullyDrawn (TTFD), not just first frame."}]},{level:"senior",q:"What is the difference between the CPU Profiler and a system trace (Perfetto)?",a:[{t:"p",text:"The *CPU Profiler* (method tracing / sampling) shows *your app's* method call stacks and timing — great for finding a slow function inside your code. A *system trace (Perfetto)* shows the *whole system*: all threads, the RenderThread, scheduling, binder calls, frame timeline, and how your app interacts with the OS — great for jank, startup, and cross-thread/system-level issues. Use CPU Profiler for 'which of my methods is slow', Perfetto for 'why is this frame/startup slow overall'."},{t:"table",headers:["","CPU Profiler","System trace (Perfetto)"],rows:[["Scope","Your app's methods","Whole system, all threads"],["Best for","Slow function in your code","Jank, startup, scheduling"],["Shows","Call stacks + timing","Frame timeline, binder, RenderThread"]]},{t:"note",text:"CPU Profiler (method trace/sampling): your app's call stacks + timing — find a slow function in your code. System trace (Perfetto): whole system — all threads, RenderThread, scheduling, binder, frame timeline — for jank/startup/cross-thread issues. 'Which of my methods is slow' → CPU Profiler; 'why is this frame/startup slow' → Perfetto."}]},{level:"senior",q:"What is the difference between sampled and instrumented (traced) profiling?",a:[{t:"p",text:"*Sampled* profiling periodically captures the call stack (low overhead, may miss very short methods) — good for finding where time is generally spent without distorting timings. *Instrumented/traced* profiling records *every* method entry/exit (exact call counts and timing, but high overhead that can distort the very timings you measure). Start with sampling for realistic hotspots; use instrumentation when you need precise call counts for a narrow section."},{t:"list",items:["**Sampled** — periodic stacks; low overhead; may miss short methods.","**Instrumented** — every entry/exit; exact; high overhead (distorts).","**Sampling** — realistic hotspots first.","**Instrumentation** — precise call counts for a narrow section."]},{t:"note",text:"Sampled profiling: periodically captures stacks — low overhead, realistic hotspots, may miss short methods. Instrumented/traced: records every method entry/exit — exact counts/timing but high overhead that distorts timings. Start with sampling; use instrumentation for precise call counts on a narrow section."}]},{level:"junior",q:"How do custom trace markers (Trace.beginSection) help profiling?",a:[{t:"p",text:"`Trace.beginSection(\"name\")`/`endSection()` (or `androidx.tracing`'s `trace(\"name\") { }`) add *named markers* to a system trace, so your own operations (e.g. 'loadUser', 'decodeImage') appear as labeled spans in Perfetto — making it far easier to see *your* code's timing amid system activity. Add them around suspected-slow sections. They're cheap and can stay in release (system tracing must be actively capturing to record them)."},{t:"code",title:"Custom trace section",code:`androidx.tracing.trace("loadUserProfile") {
    val user = repository.loadUser(id)   // shows as a labeled span in Perfetto
    render(user)
}`},{t:"note",text:`Trace.beginSection/endSection (or androidx.tracing trace("name"){}) add named markers to a system trace, so your operations appear as labeled spans in Perfetto — far easier to spot your code's timing amid system activity. Add around suspected-slow sections; cheap enough to leave in (only recorded while tracing is active).`}]},{level:"senior",q:"How does R8/dex optimization relate to startup performance?",a:[{t:"p",text:"R8 shrinks and optimizes code (removing unused classes/methods, inlining) — a smaller, simpler dex means *less code to load and verify* at startup and fewer methods, which can improve cold start and reduce app size. It also enables more effective *class verification* and works with Baseline Profiles. Ensure R8 is on for release; overly broad `keep` rules bloat the dex and can hurt startup. Optimization + Baseline Profiles together give the best startup."},{t:"list",items:["**Smaller/simpler dex** — less to load and verify at startup.","**Fewer methods** — can improve cold start + size.","**Works with** — Baseline Profiles.","**Watch** — broad keep rules bloat dex, hurt startup."]},{t:"note",text:"R8 shrinks/optimizes code (removes unused, inlines) — a smaller/simpler dex means less to load and verify at startup, improving cold start and size. Works with Baseline Profiles. Keep R8 on for release; avoid overly broad keep rules (they bloat dex and hurt startup). R8 + Baseline Profiles = best startup."}]},{level:"junior",q:"Why should you profile on a release build and a real device?",a:[{t:"p",text:"*Debug* builds are unoptimized (no R8, debuggable, extra checks) and run code differently (more interpretation), so their timings don't reflect what users experience — they can be several times slower and mislead you. *Emulators* have different CPU/GPU/memory characteristics than real phones (especially low-end). Always measure performance on a *release/non-debuggable* build on a *representative physical device* (ideally a low-end one) for numbers that match production."},{t:"list",items:["**Debug builds** — unoptimized, debuggable; misleadingly slow/different.","**Emulators** — different CPU/GPU/memory than real phones.","**Measure** — release/non-debuggable build.","**Device** — representative physical (ideally low-end)."]},{t:"note",text:"Debug builds are unoptimized (no R8, debuggable, extra checks, more interpretation) — timings don't reflect users (can be several× slower). Emulators differ from real phones. Always measure on a release/non-debuggable build on a representative physical device (ideally low-end) for production-matching numbers."}]},{level:"senior",q:"What is lazy initialization and how does it help startup?",a:[{t:"p",text:"Lazy initialization defers creating an object until it's *first used* (`by lazy { }`, or lazy DI providers) rather than at startup. Since startup work directly delays the first frame, deferring anything not needed for the first screen (analytics that can init after launch, heavy singletons, secondary feature managers) speeds cold start. Balance: don't lazily init something on a hot path where the deferred cost causes jank later — defer what's genuinely not needed *early*."},{t:"code",title:"Deferring non-critical work",code:`// heavy manager not needed for the first screen — created on first use, not at startup
val analytics by lazy { AnalyticsManager(appContext) }
// or initialize after the first frame:
window.decorView.post { initNonCriticalLibraries() }`},{t:"note",text:"Lazy init defers creating an object until first use (by lazy, lazy DI) instead of at startup — deferring anything not needed for the first screen (post-launch analytics, heavy singletons) speeds cold start. Balance: don't defer hot-path work where the later cost causes jank. Defer what's genuinely not needed early."}]},{level:"senior",q:"How do you profile and optimize a slow list scroll?",a:[{t:"p",text:"Capture a *system trace* while scrolling, find janky frames, and expand them to see the expensive main-thread work (binding/composition, image decode, layout, allocations). Then fix the specific cause: cheaper item layouts, right-sized images off the main thread, stable keys/DiffUtil, precomputed data, fewer allocations, and a Baseline Profile. Verify with Macrobenchmark's `FrameTimingMetric` on a release build. Iterate: measure → fix the top cost → re-measure."},{t:"list",items:["**System trace** — find janky frames, expand to see the cost.","**Fix causes** — cheap items, right-sized images, stable keys.","**Baseline Profile** — for the scroll path.","**Verify** — Macrobenchmark `FrameTimingMetric` (release)."]},{t:"note",text:"Profile a slow scroll: system trace while scrolling → find janky frames → expand to see expensive main-thread work (binding/composition, image decode, layout, allocations). Fix the specific cause (cheap items, right-sized off-main images, stable keys, precompute, fewer allocations, Baseline Profile). Verify with Macrobenchmark FrameTimingMetric on release."}]},{level:"junior",q:"What is the flame chart / call chart in the profiler, and how do you read it?",a:[{t:"p",text:"A *call chart* (flame chart) visualizes call stacks over time: the x-axis is time, and each bar is a method call with its callees stacked *below* it — a *wide* bar means that method (or its children) took a long time. Read it top-down to find *wide* bars (time sinks). The related *flame graph* aggregates by total time per method (ignoring order) to show cumulative hotspots. Use these to spot which function dominates."},{t:"list",items:["**Call chart** — time on x-axis, callees stacked below.","**Wide bar** — that method/subtree took long.","**Flame graph** — aggregates cumulative time per method.","**Read** — find the widest bars (time sinks)."]},{t:"note",text:"A call/flame chart plots call stacks over time (x=time, callees stacked below) — a wide bar means that method/subtree took long; read top-down for the widest bars. A flame graph aggregates cumulative time per method (order-independent) for hotspots. Both find which function dominates."}]},{level:"senior",q:"How do you regression-test performance in CI?",a:[{t:"p",text:"Add *Macrobenchmark* tests (startup, key scrolls) that run on a *physical device* (a Firebase Test Lab device or a self-hosted device farm) and record metrics. Track the numbers over time and *fail/alert on regressions* beyond a threshold. Pair with Baseline Profile generation. Because device numbers are noisy, run multiple iterations and compare medians, and use consistent device models. This catches performance regressions before they ship, like tests catch functional ones."},{t:"list",items:["**Macrobenchmark in CI** — startup + key scrolls.","**Physical device** — Firebase Test Lab / device farm.","**Alert on regression** — beyond a threshold; track over time.","**Noise** — multiple iterations, medians, consistent devices."]},{t:"note",text:"Regression-test perf with Macrobenchmark tests (startup, key scrolls) on physical devices (Firebase Test Lab/device farm), tracking metrics and alerting on regressions beyond a threshold. Handle device noise with multiple iterations/medians and consistent models. Catches perf regressions pre-ship like tests catch functional ones."}]},{level:"junior",q:"What is the difference between latency and throughput in performance?",a:[{t:"p",text:"*Latency* is how long *one* operation takes (a single request's response time, one frame's render time) — it drives perceived responsiveness. *Throughput* is how *many* operations complete per unit time (frames per second, requests per second) — it drives capacity. They can trade off: batching improves throughput but may raise per-item latency. On mobile UI, low per-frame *latency* (staying under the frame budget) is usually the priority for smoothness."},{t:"list",items:["**Latency** — time for one operation (responsiveness).","**Throughput** — operations per unit time (capacity).","**Trade-off** — batching boosts throughput, may raise latency.","**Mobile UI** — low per-frame latency for smoothness."]},{t:"note",text:"Latency = time for one operation (drives perceived responsiveness); throughput = operations per unit time (drives capacity). They trade off (batching boosts throughput but raises per-item latency). On mobile UI, low per-frame latency (under the frame budget) is usually the smoothness priority."}]},{level:"senior",q:"How do you decide what to optimize (where to focus)?",a:[{t:"p",text:"Optimize by *impact* × *frequency*, guided by data: measure first to find the *actual* bottleneck (Amdahl's law — speeding up something that's 5% of the time barely helps), prioritize what users hit most (startup, main scroll, key journeys), and confirm with production telemetry (Play vitals, custom traces) which screens are slow *in the field*. Avoid micro-optimizing cold paths. Set a target (e.g. 'cold start under X ms on low-end'), measure against it, and stop when you meet it."},{t:"list",items:["**Impact × frequency** — measure the actual bottleneck first.","**Amdahl's law** — optimizing a small fraction barely helps.","**Prioritize** — startup, main scroll, hot journeys.","**Target-driven** — set a goal, measure, stop when met."]},{t:"note",text:"Optimize by impact × frequency, data-driven: measure to find the real bottleneck (Amdahl — speeding up 5% of time barely helps), prioritize what users hit most (startup, main scroll), confirm with production telemetry (Play vitals) which screens are slow in the field. Set a target, measure against it, stop when met."}]},{level:"senior",q:"What is dex verification / class loading and how does it affect startup?",a:[{t:"p",text:"At runtime ART must *load* and *verify* classes before use — on first run (or without AOT compilation) this happens lazily and costs time during startup and first interactions. A *Baseline Profile* pre-compiles the hot classes/methods so this work is done at install; *R8* reduces the number of classes/methods to verify. Excessive classes, reflection, and huge dependency graphs increase this cost. This is a key reason Baseline Profiles help startup so much."},{t:"list",items:["**Load + verify** — ART processes classes before use.","**First run** — happens lazily; costs startup time.","**Baseline Profile** — pre-compiles hot classes at install.","**R8** — fewer classes/methods to verify."]},{t:"note",text:"ART loads and verifies classes before use — on first run (no AOT) this is lazy and costs startup/first-interaction time. Baseline Profiles pre-compile hot classes at install; R8 reduces the count to verify. Excessive classes/reflection/huge graphs increase the cost — a key reason Baseline Profiles help startup."}]},{level:"senior",q:"How do you profile power/CPU usage over time?",a:[{t:"p",text:"Use *Perfetto/systrace* to see CPU scheduling and wakeups over time, `Battery Historian` (from a bug report) to analyze wakelocks, jobs, and battery drain, and the *Energy Profiler* (Android Studio, on supported devices) for a rough power view. Look for *background CPU activity* (busy loops, frequent wakeups, chatty network), and correlate CPU spikes with your operations via trace markers. Excessive CPU when idle is the main power/perf red flag."},{t:"list",items:["**Perfetto** — CPU scheduling, wakeups over time.","**Battery Historian** — wakelocks, jobs, drain (from bug report).","**Energy Profiler** — rough power view.","**Red flag** — background CPU/wakeups when idle."]},{t:"note",text:"Profile power/CPU with Perfetto/systrace (CPU scheduling, wakeups over time), Battery Historian (wakelocks/jobs/drain from a bug report), Energy Profiler (rough power). Look for background CPU (busy loops, frequent wakeups, chatty network); correlate spikes with your ops via trace markers. Idle CPU is the main red flag."}]},{level:"senior",q:"How do you profile Compose recomposition and performance?",a:[{t:"p",text:"Use the *Layout Inspector*'s recomposition counts to see which composables recompose (and how often) — high or unexpected counts point to instability or bad state reads. The *Compose compiler metrics/reports* tell you which composables are *skippable/restartable* and which parameters are *unstable* (why skipping fails). For frame-level timing, use a *system trace* (composition/layout/draw phases are traced). Combine: reports to find instability, Layout Inspector to see counts, trace to measure the cost."},{t:"list",items:["**Layout Inspector** — recomposition counts per composable.","**Compiler metrics/reports** — skippable/restartable, unstable params.","**System trace** — composition/layout/draw phase timing.","**Combine** — reports (why) + counts (where) + trace (cost)."]},{t:"note",text:"Profile Compose with Layout Inspector recomposition counts (which composables recompose, how often), the compiler metrics/reports (skippable/restartable + unstable params explaining failed skipping), and a system trace for phase timing. Combine: reports (why unstable) + Inspector (where) + trace (cost)."}]},{level:"junior",q:"What does 'measure before optimizing' mean in practice?",a:[{t:"p",text:"It means never guess where the slowness is — *profile first* to find the real bottleneck, because intuition is often wrong (the slow part is rarely where you'd expect). Get a baseline number, make one change, and *re-measure* to confirm it actually helped (and didn't regress elsewhere). Premature optimization wastes effort on non-bottlenecks and adds complexity. The loop is: measure → identify the top cost → fix it → measure again."},{t:"list",items:["**Profile first** — find the real bottleneck (intuition misleads).","**Baseline** — measure before changing.","**Re-measure** — confirm the change helped, no regression.","**Avoid** — premature optimization of non-bottlenecks."]},{t:"note",text:"Measure before optimizing = profile first to find the real bottleneck (intuition is usually wrong), get a baseline, make one change, re-measure to confirm it helped without regressing. Premature optimization wastes effort and adds complexity. Loop: measure → top cost → fix → measure again."}]}],_C={id:"profiling-startup",title:"Profiling, Startup & Baseline Profiles",tagline:"Profile-don't-guess, the Android Studio Profiler, Perfetto/systrace, Micro/Macrobenchmark, cold-start optimization, and Baseline Profiles.",tags:["Performance","Profiling","Perfetto","Startup","Baseline Profiles"],content:HC,qa:KC},GC=[{heading:"Why app size matters",blocks:[{t:"p",text:"**App download size affects install rates** — larger apps are less likely to be installed (users on limited data or storage abandon big downloads), and Play shows the download size prominently. Beyond installs, size affects update frequency (big updates get deferred), storage pressure on the device, and even the size limit for instant apps. So reducing app size is a real business concern, not just tidiness. The main levers are code shrinking (R8), the App Bundle format, and resource/asset optimization."}]},{heading:"R8 — code shrinking, obfuscation, optimization",blocks:[{t:"p",text:"**R8** is the tool (replacing the older ProGuard) that shrinks and optimizes your code at build time for release builds. It does three things: **shrinking** (removing unused code and resources — 'tree shaking'), **obfuscation** (renaming classes/methods to short meaningless names, which shrinks and lightly obscures the code), and **optimization** (inlining, removing dead branches). It significantly reduces APK size and can improve performance."},{t:"code",title:"Enabling R8 in release builds",code:`android {
    buildTypes {
        release {
            isMinifyEnabled = true          // R8 shrinking + obfuscation
            isShrinkResources = true         // remove unused resources
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro",        // your keep rules
            )
        }
    }
}`},{t:"list",items:["**Shrinking (tree shaking)** — R8 analyzes which code is actually reachable and removes the rest (unused methods, classes, libraries you pull in but barely use). This is where most of the size reduction comes from.","**Obfuscation** — renames identifiers to short names (`a`, `b`), reducing size and making reverse-engineering slightly harder. You upload a *mapping file* to Play so crash reports can be de-obfuscated back to real names.","**Optimization** — inlining, dead-code elimination, and other transformations that can also improve runtime performance.","**Keep rules** — R8 uses static analysis, so code accessed *reflectively* (by name at runtime — some serialization, DI, or JNI) can be wrongly removed or renamed. You write `-keep` rules in `proguard-rules.pro` to tell R8 to preserve those. Libraries ship their own keep rules; you add rules for your reflective code. Missing keep rules cause release-only crashes (works in debug where R8 is off) — a classic gotcha."]}]},{heading:"The Android App Bundle (AAB) — split delivery",blocks:[{t:"p",text:"The **Android App Bundle (`.aab`)** is the modern publishing format (required for new apps on Play since 2021). Instead of you building one universal APK containing *everything* for *every* device, you upload an AAB — a bundle of all your app's code and resources — and Google Play generates *optimized APKs per device* from it (**split APKs**), delivering only what each device actually needs. This is a major size win at the *download* level."},{t:"list",items:["**The problem with a universal APK**: it includes resources for *all* screen densities, native libraries for *all* CPU architectures (arm64, armv7, x86), and *all* language strings — but any given device uses only one density, one architecture, and a few languages. A universal APK forces every device to download resources it can't use.","**How the AAB fixes it**: Play splits your bundle by **density** (deliver only the device's density resources), **architecture** (only its ABI's native libs), and **language** (only its languages), so each device downloads a much smaller, tailored APK. Same app, much smaller download — often 15-35% smaller.","**APK vs AAB**: an APK is the installable artifact a device runs; an AAB is a *publishing format* you upload (not directly installable) from which Play generates APKs. You develop and test with APKs; you *publish* an AAB.","**Play App Signing** — AAB delivery requires Play to sign the generated APKs, so you enroll in Play App Signing (Play holds the signing key and signs the per-device APKs)."]}]},{heading:"Dynamic Feature Modules and resource optimization",blocks:[{t:"list",items:["**Dynamic Feature Modules (Play Feature Delivery)** — deliver parts of your app *on demand* rather than at install. A rarely-used feature (a complex editor, an onboarding flow) can be a dynamic module downloaded only when the user needs it, keeping the initial install small. Also supports conditional delivery (by device capability) and instant delivery.","**Resource optimization** — remove unused resources (`shrinkResources`), use vector drawables instead of multiple PNG densities (one scalable asset vs many bitmaps), compress images (WebP instead of PNG/JPEG — smaller at similar quality), and remove unused languages/densities (the AAB handles per-device, but you can also configure `resConfigs`).","**Native library optimization** — native libs (`.so`) are large and per-architecture; the AAB splits them, and you can further reduce with `android:extractNativeLibs=false` and by only including needed ABIs.","**Analyze size** — the **APK Analyzer** in Android Studio breaks down what's taking space (code, resources, native libs, assets), so you can target the biggest contributors — the same 'measure first' principle as performance."]}]},{heading:"Battery optimization",blocks:[{t:"p",text:"**Battery drain** is a top user complaint and a reason apps get uninstalled. Battery is consumed by CPU work (computation, wakeups), radio use (network, GPS), and keeping the device/screen awake. The overarching strategy is to *do less, less often, and batch it* — which aligns with the OS's own battery-protection mechanisms (Doze, background limits)."},{t:"list",items:["**Minimize wakeups and background work** — waking the device from sleep is expensive. Use WorkManager (which batches work into maintenance windows respecting Doze) rather than frequent alarms/polling. Avoid wakelocks; if you must keep the CPU awake, hold it as briefly as possible.","**Batch network requests** — the cellular radio stays powered for a while after each request (the 'tail energy'), so many small spread-out requests keep the radio on far more than a few batched ones. Batch and defer non-urgent network work (WorkManager with constraints); prefetch efficiently.","**Prefer push over polling** — polling for updates wastes battery (constant wakeups + radio); FCM push lets the server notify the app only when there's something, far more efficient.","**Use location sparingly** — GPS is a major drain. Request the *lowest accuracy* that works, the *lowest frequency*, and stop updates when not needed; use `FusedLocationProvider` with appropriate priority.","**Respect the OS mechanisms** — Doze, App Standby, and background limits *exist* to save battery; working with them (deferrable work, push, constraints) rather than fighting them (wakelocks, exact-alarm polling, battery-optimization exemptions) is both the correct approach and what Play policies require.","**Measure** — the Energy Profiler (and Battery Historian / `dumpsys batterystats`) shows what's draining battery, so you target the real culprit rather than guessing."]},{t:"note",text:"App size: matters for install/update rates. R8 shrinks (tree-shaking), obfuscates (rename + mapping file for crashes), and optimizes code in release builds — keep rules for reflective code (missing ones = release-only crashes). AAB is the publish format; Play generates per-device split APKs (by density/ABI/language) — much smaller downloads than a universal APK. Dynamic Feature Modules deliver features on demand. Optimize resources (WebP, vectors, shrinkResources); analyze with APK Analyzer. Battery: minimize wakeups/background work, batch network (radio tail energy), push over polling, sparing location, respect Doze; measure with the Energy Profiler."}]}],JC=[{level:"junior",q:"What is R8 and what does it do?",a:[{t:"p",text:"**R8 is the build-time tool (replacing the older ProGuard) that shrinks and optimizes your code for release builds.** It does three things: *shrinking* (removing unused code and resources — 'tree shaking'), *obfuscation* (renaming classes and methods to short meaningless names), and *optimization* (inlining, dead-code elimination). The result is a significantly smaller APK and sometimes better runtime performance."},{t:"list",items:["**Shrinking** — R8 analyzes which code is actually reachable and removes the rest (unused methods, classes, and parts of libraries you barely use). This is where most of the size reduction comes from.","**Obfuscation** — renames identifiers to short names (`a`, `b`), reducing size and slightly hindering reverse-engineering. You upload a *mapping file* to Play so crash reports can be translated back to real names.","**Optimization** — inlining and dead-code removal that can also improve performance."]},{t:"code",title:"Enabling it",code:`release {
    isMinifyEnabled = true      // R8
    isShrinkResources = true    // drop unused resources
}`},{t:"p",text:"The important gotcha: R8 uses *static analysis*, so code accessed *reflectively* (by name at runtime — some serialization, DI, JNI) can be wrongly removed or renamed, causing crashes. You add `-keep` rules to preserve such code. Because R8 runs only on release builds, missing keep rules cause *release-only crashes* that don't appear in debug — a classic source of 'works in debug, crashes in release' bugs. Libraries ship their own keep rules; you add rules for your own reflective code."}]},{level:"junior",q:"What is the difference between an APK and an AAB?",a:[{t:"p",text:"**An APK is the installable app artifact a device runs; an AAB (Android App Bundle) is a *publishing format* you upload to Play — it's not directly installable. From the AAB, Google Play generates optimized, per-device APKs (split APKs) delivering only what each device needs.** Since 2021, new apps on Play must publish as AAB."},{t:"list",items:["**APK** — the traditional installable package. You still build and test with APKs during development, and it's what's ultimately installed on the device.","**AAB** — a bundle of *all* your app's code and resources (for all densities, architectures, languages) that you upload to Play. Play uses it to generate tailored APKs per device.","**The benefit — smaller downloads**: a universal APK includes resources for *every* screen density, native libraries for *every* CPU architecture, and *every* language — but a device uses only one of each. Play splits the AAB by density, architecture, and language, so each device downloads only what it needs — often 15-35% smaller than a universal APK."]},{t:"p",text:"So the mental model: AAB is what you *publish* (a superset Play optimizes from), APK is what gets *installed* (a device-specific subset Play generates). You develop/test with APKs and publish an AAB. One consequence is that AAB delivery requires *Play App Signing* — since Play generates and must sign the per-device APKs, it holds (or manages) your signing key. The AAB format is a significant download-size win at no code cost, which is why Google made it mandatory."}]},{level:"junior",q:"How do you reduce an Android app's size?",a:[{t:"list",items:["**Enable R8** (`minifyEnabled`) — shrinks unused code via tree-shaking and obfuscates; the biggest code-size lever. Add `shrinkResources` to remove unused resources.","**Publish as an AAB** — Play generates per-device split APKs (by density, architecture, language), so each device downloads only what it needs — much smaller than a universal APK, with no code changes.","**Optimize images/resources** — use WebP instead of PNG/JPEG (smaller at similar quality), vector drawables instead of multiple PNG densities (one scalable asset vs many bitmaps), and remove unused resources.","**Use Dynamic Feature Modules** — deliver rarely-used features on demand instead of at install, keeping the initial download small.","**Trim dependencies** — a heavy library used for one small thing bloats the app; prefer lighter alternatives or R8 shrinking, and avoid pulling in large SDKs you barely use.","**Reduce native libraries** — `.so` files are large and per-architecture; the AAB splits them, and you include only needed ABIs."]},{t:"p",text:"The approach mirrors performance work — *measure first*: use the **APK Analyzer** in Android Studio to break down what's actually taking space (code, resources, native libs, assets), then target the biggest contributors. Guessing wastes effort; a heavy dependency or uncompressed assets are often the real culprits, and the analyzer shows you. The two highest-leverage, low-effort wins are usually enabling R8 (`minifyEnabled = true` + `shrinkResources`) and publishing as an AAB — both give substantial size reductions with minimal work. From there, resource optimization (WebP/vectors) and on-demand delivery (dynamic features) address specific large contributors."}]},{level:"junior",q:"What are the main causes of battery drain and how do you reduce it?",a:[{t:"p",text:"**Battery is consumed by CPU work (computation, device wakeups), radio use (network, GPS), and keeping the device or screen awake. The strategy to reduce drain is to *do less, less often, and batch it* — which happens to align with the OS's own battery-protection mechanisms.**"},{t:"list",items:["**Minimize wakeups and background work** — waking the device from sleep is expensive. Use WorkManager (which batches work into maintenance windows respecting Doze) instead of frequent alarms/polling; avoid wakelocks.","**Batch network requests** — the cellular radio stays powered for a while after each request ('tail energy'), so many small spread-out requests keep it on far more than a few batched ones. Batch/defer non-urgent network work.","**Prefer push over polling** — polling wastes battery (constant wakeups + radio); FCM push notifies the app only when there's genuinely something, far more efficient.","**Use location sparingly** — GPS is a major drain. Request the lowest accuracy and frequency that works, and stop updates when not needed."]},{t:"p",text:"The overarching principle is to *respect the OS's battery mechanisms* (Doze, App Standby, background limits) rather than fight them — they exist to save battery, and working with them (deferrable work via WorkManager, push over polling, location constraints) is both the correct approach and what Play policies require. Fighting them (wakelocks, exact alarms for polling, requesting battery-optimization exemptions) is fragile, user-hostile, and increasingly restricted. And as always, *measure* — the Energy Profiler (or Battery Historian / `dumpsys batterystats`) shows what's actually draining battery, so you target the real culprit (often location or excessive network/wakeups) rather than guessing. Battery drain is a top uninstall reason, so it's a real quality concern."}]},{level:"senior",q:"Why does the App Bundle produce smaller downloads than a universal APK, and what does Play do behind the scenes?",a:[{t:"p",text:"**A universal APK must contain resources and code for *every* device it might run on — all screen densities, all CPU architectures, and all languages — because it's a single artifact that any device could install. But any *given* device uses only one density, one architecture, and a few languages, so a universal APK forces every device to download large amounts of resources it can never use. The App Bundle lets Play generate *device-specific* APKs that include only what each device needs.**"},{t:"list",items:["**The three main split dimensions**: (1) *Density* — Android ships drawables at multiple densities (mdpi, hdpi, xhdpi, xxhdpi…) so images look sharp on any screen; a device needs only *its* density's assets, but a universal APK includes *all* of them. (2) *ABI (architecture)* — native libraries (`.so` files, from NDK code or dependencies with native components) are compiled per-CPU-architecture (arm64-v8a, armeabi-v7a, x86_64); a device runs one architecture, but a universal APK bundles all of them, and native libs are often large. (3) *Language* — string resources for every supported language; a device typically uses one or two.","**What Play does behind the scenes**: you upload the AAB (containing everything), and Play's server-side build system generates a set of **split APKs** from it — a *base* APK plus *configuration* APKs for each density, ABI, and language. When a specific device installs your app, Play delivers only the base plus the config splits matching *that device* (its density, its architecture, its languages). The device assembles these into the running app. The user's download is dramatically smaller because it excludes all the other densities, architectures, and languages.","**The magnitude**: the savings are largest for apps with many densities of images and native libraries (games, apps with heavy native SDKs) — often 15-35% smaller downloads, sometimes more. For an app with big native libs across four architectures, dropping to one architecture alone is a huge reduction.","**Play App Signing is required because of this**: since Play *generates and must sign* the per-device APKs (they didn't exist when you built the bundle), Play needs your app-signing key. You enroll in Play App Signing — Play holds the app-signing key and signs each generated APK; you keep an *upload key* to authenticate your uploads. This is why AAB and Play App Signing go together."]},{t:"list",items:["**Additional AAB-enabled features**: because Play now generates delivery from a bundle, it also enables *Dynamic Feature Modules* (on-demand delivery of features), *asset packs* (large assets delivered separately, useful for games), and conditional/instant delivery — all of which further reduce initial install size by not shipping everything up front.","**Testing implication**: since the device-specific APKs are generated by Play (not by you), testing the *actual* delivered artifacts requires tools like `bundletool` (to generate device-specific APKs locally from your AAB) or Play's internal testing track — you can't just install the AAB directly."]},{t:"p",text:"**The senior framing**: the AAB works by *deferring the final packaging decision to Play*, which has the information you lack at build time — the *specific device* installing the app. You ship a superset (the bundle); Play carves out the exact subset each device needs (base + matching density/ABI/language splits). This eliminates the fundamental waste of the universal APK (shipping resources for devices other than the one installing), yielding much smaller downloads for free, plus enabling on-demand delivery. The trade-offs are that Play must sign the generated APKs (hence Play App Signing) and that testing the real artifacts needs `bundletool`/internal tracks. Understanding *why* the split works (per-device resources the universal APK duplicated), *what Play does* (server-side split-APK generation and signing), and the *ecosystem implications* (Play App Signing, dynamic delivery, testing) is the complete answer."}]},{level:"senior",q:"A user reports your app drains their battery. How would you investigate and address it?",a:[{t:"p",text:"**I'd measure to identify the actual drain source (Energy Profiler, Battery Historian, Android Vitals) — since battery drain has several possible causes (excessive wakeups, network, location, CPU) and intuition is unreliable — then address the specific culprit, aligning with the OS's battery mechanisms.** The measurement-first approach is essential because 'drains battery' is a symptom with many possible causes."},{t:"list",items:["**1. Confirm and scope with production data**: check **Android Vitals** in the Play Console — it reports excessive wakeups, wakelocks, and battery-related metrics across real users, telling you *whether* it's a widespread problem and on which devices/versions. This distinguishes one user's device issue from a real app problem.","**2. Measure locally to find the source**: use the **Energy Profiler** (shows CPU, network, and location/GPS energy over time) and, for deeper analysis, **Battery Historian** (visualizes `dumpsys batterystats` — wakelocks, wakeups, jobs, network, GPS usage attributed to your app). These reveal *what specifically* is draining battery — the app is doing too many wakeups, or holding a wakelock, or using GPS continuously, or making constant network requests.","**3. Diagnose against the common culprits**: (a) *Excessive wakeups/background work* — an alarm or job firing too often, waking the device repeatedly (each wakeup is expensive). (b) *Wakelocks held too long* — code keeping the CPU awake unnecessarily. (c) *Location/GPS* — requesting high-accuracy location too frequently or not stopping updates (a major drain). (d) *Network* — frequent, unbatched requests keeping the radio powered (tail energy), or polling instead of push. (e) *Runaway CPU* — a background loop or inefficient work spinning the CPU.","**4. Fix the specific cause**: for excessive background work → move to WorkManager (batches into maintenance windows, respects Doze) and reduce frequency; for polling → switch to FCM push (notify only when there's something); for location → lower accuracy/frequency, use `FusedLocationProvider` with appropriate priority, stop updates when not needed; for network → batch and defer non-urgent requests with WorkManager constraints; for wakelocks → hold them minimally or eliminate them; for CPU → optimize or move the work.","**5. Respect (don't fight) the OS mechanisms**: the fixes align with Doze/App Standby/background limits — deferrable work, push over polling, constrained/batched network. If the instinct is to request a battery-optimization *exemption* to keep doing heavy background work, that's usually the wrong answer (fragile, user-hostile, Play-policy-restricted) — the right answer is to *reduce* the work, not exempt it.","**6. Verify**: remeasure with the Energy Profiler after the fix, and monitor Android Vitals over the next release to confirm real-user battery metrics improved."]},{t:"p",text:"**The senior framing**: battery investigation is a *measurement-driven, source-identification* problem — 'drains battery' is a symptom, and the job is to find *which* resource (CPU/wakeups, radio/network, GPS/location, wakelocks) is being overused, using Vitals (scope) and the Energy Profiler / Battery Historian (cause). Once identified, the fixes are consistent — do less, less often, batched, and via the OS-friendly mechanisms (WorkManager, push, location constraints) rather than fighting them. The key insight is that battery drain almost always traces to *excessive wakeups, unbatched network, or aggressive location*, and that the solution is to *reduce and batch* work in alignment with Doze/background limits — not to seek exemptions. Naming the measurement tools, the specific culprits, the aligned fixes, and the 'work with the OS, don't fight it' principle is the comprehensive answer that connects diagnosis to Android's battery model."}]},{level:"junior",q:"What is the difference between R8, ProGuard, and D8?",a:[{t:"p",text:"*D8* is the *dexer* — it turns Java/Kotlin bytecode into Android's dex format. *R8* is the *shrinker/optimizer* that (in release builds) also does the dexing — it removes unused code (tree-shaking), optimizes, inlines, and obfuscates (renames) in one step. *ProGuard* was the older, separate shrinker/obfuscator that R8 replaced (R8 is faster and reuses ProGuard's rule format). Today you use R8 (with ProGuard-syntax `keep` rules); D8 handles debug dexing."},{t:"list",items:["**D8** — the dexer (bytecode → dex).","**R8** — shrink + optimize + obfuscate + dex (release).","**ProGuard** — older separate shrinker R8 replaced.","**Rules** — R8 reuses ProGuard `keep` syntax."]},{t:"note",text:"D8 = dexer (bytecode → dex). R8 = release shrinker/optimizer that also dexes — tree-shakes unused code, optimizes, inlines, obfuscates in one step. ProGuard = the older separate tool R8 replaced (R8 reuses its keep-rule syntax). Today: R8 for release, D8 for debug dexing."}]},{level:"senior",q:"How does resource shrinking work, and how is it different from code shrinking?",a:[{t:"p",text:"*Code shrinking* (R8) removes unused *classes/methods*. *Resource shrinking* (`shrinkResources true`, requires code shrinking on) removes unused *resources* (drawables, layouts, strings) not referenced by the kept code. It's important because resources can dominate APK size. Caveat: resources referenced *dynamically* (by name via `getIdentifier`) look unused and may be stripped — protect them with a `keep.xml` or `tools:keep`. Enable both together for the smallest build."},{t:"list",items:["**Code shrinking** — removes unused classes/methods (R8).","**Resource shrinking** — removes unused resources (needs code shrinking).","**Dynamic refs** — `getIdentifier` resources look unused; may strip.","**Protect** — `keep.xml` / `tools:keep`."]},{t:"note",text:"Code shrinking (R8) removes unused classes/methods; resource shrinking (shrinkResources, needs code shrinking on) removes unused resources (drawables/layouts/strings). Caveat: dynamically referenced resources (getIdentifier) look unused and may be stripped — protect with keep.xml/tools:keep. Enable both for the smallest build."}]},{level:"senior",q:"What are dynamic feature modules and how do they reduce size?",a:[{t:"p",text:"*Dynamic feature modules* let you ship parts of the app that are *downloaded on demand* (via Play Feature Delivery) rather than in the base install — so the initial download is smaller. Use them for rarely-used or large features (an onboarding flow, a heavy editor). They can be *install-time*, *on-demand* (requested via `SplitInstallManager`), or *conditional*. The trade-off is complexity (handling the download, availability) — worth it for genuinely large optional features."},{t:"list",items:["**Downloaded on demand** — not in the base install.","**Smaller initial download** — for large/rare features.","**Delivery** — install-time, on-demand (`SplitInstallManager`), conditional.","**Trade-off** — complexity handling download/availability."]},{t:"note",text:"Dynamic feature modules ship features downloaded on demand (Play Feature Delivery) instead of in the base install — smaller initial download. Modes: install-time, on-demand (SplitInstallManager), conditional. Use for large/rare features (onboarding, heavy editor). Trade-off: complexity of handling download/availability."}]},{level:"senior",q:"How do you analyze what's taking up space in your APK/AAB?",a:[{t:"p",text:"Use Android Studio's *APK Analyzer* (Build → Analyze APK, or on an AAB) to see a size breakdown by component: dex (code), resources, assets, native libs, and per-file sizes — comparing raw vs download size. This shows the biggest contributors (often images/assets or a large native lib) so you optimize the right thing. For AABs, also inspect generated *split APKs* (via bundletool) to see per-device download size. Measure before cutting."},{t:"list",items:["**APK Analyzer** — breakdown: dex, resources, assets, native libs.","**Raw vs download size** — per component/file.","**bundletool** — inspect per-device split APK sizes.","**Measure first** — target the biggest contributors."]},{t:"note",text:"Analyze size with the APK Analyzer (Build → Analyze APK/AAB) — breakdown by dex, resources, assets, native libs, per-file, raw vs download. Shows the biggest contributors (often images/assets or a native lib) to optimize the right thing. Use bundletool to inspect per-device split APK sizes. Measure before cutting."}]},{level:"junior",q:"How do image formats (WebP, vectors) affect app size?",a:[{t:"p",text:"Images often dominate app size. Prefer *vector drawables* for icons/simple graphics (one small, resolution-independent file instead of multiple PNG densities). For photos/complex images, *WebP* is significantly smaller than PNG/JPEG at similar quality (Android Studio can convert). Avoid shipping multiple density-specific bitmaps when a vector works, and remove unused images. These format choices typically yield the biggest easy size wins."},{t:"list",items:["**Vector drawables** — icons/simple graphics; one small file, any density.","**WebP** — smaller than PNG/JPEG for photos (convertible).","**Avoid** — multiple density bitmaps where a vector works.","**Biggest easy wins** — images usually dominate size."]},{t:"note",text:"Images usually dominate app size. Use vector drawables for icons/simple graphics (one small resolution-independent file vs multiple PNG densities) and WebP for photos (smaller than PNG/JPEG at similar quality, Android Studio converts). Remove unused images. Format choices give the biggest easy size wins."}]},{level:"senior",q:"How do native libraries (.so) affect size, and how do you manage them?",a:[{t:"p",text:"Native libraries (`.so` files, per ABI: arm64-v8a, armeabi-v7a, x86_64) can be large and multiply across ABIs. With the *App Bundle*, Play delivers only the *device's ABI*, cutting download size automatically. Additionally, `useLegacyPackaging false` (uncompressed native libs) enables the OS to load them without extraction (smaller on-device footprint). Remove unused ABIs/libs, and check the APK Analyzer for oversized `.so` files from dependencies you may not need."},{t:"list",items:["**Per-ABI `.so`** — large, multiplied across architectures.","**App Bundle** — Play delivers only the device's ABI.","**`useLegacyPackaging false`** — uncompressed, no extraction.","**Audit** — remove unused ABIs/libs (APK Analyzer)."]},{t:"note",text:"Native libs (.so per ABI: arm64-v8a, armeabi-v7a, x86_64) are large and multiply across ABIs. The App Bundle delivers only the device's ABI (smaller download). useLegacyPackaging false (uncompressed) lets the OS load without extraction. Remove unused ABIs/libs; check the APK Analyzer for oversized dependency .so files."}]},{level:"junior",q:"What is a wakelock, and how does it drain battery?",a:[{t:"p",text:"A *wakelock* keeps the device (CPU or screen) *awake* even when it would otherwise sleep. Holding one too long — or forgetting to release it — keeps the CPU running and drains the battery fast. Modern Android discourages manual `PowerManager` wakelocks; prefer higher-level APIs (`WorkManager`, foreground services) that manage wake state for you. If you must hold one, keep it as brief as possible and always release it (use timeouts). Battery Historian shows problematic wakelocks."},{t:"list",items:["**Wakelock** — keeps CPU/screen awake past sleep.","**Drain** — held too long / not released → CPU stays on.","**Prefer** — WorkManager/foreground services over manual locks.","**If needed** — brief, with timeout, always release."]},{t:"note",text:"A wakelock keeps the CPU/screen awake past when the device would sleep — held too long or unreleased, it drains battery fast. Prefer WorkManager/foreground services (they manage wake state) over manual PowerManager locks. If you must hold one: keep it brief, use a timeout, always release. Battery Historian flags bad wakelocks."}]},{level:"senior",q:"How does Doze mode and App Standby save battery?",a:[{t:"p",text:"*Doze* activates when the device is idle (screen off, still, unplugged): the system batches deferred work into periodic *maintenance windows* and restricts network access, wakelocks, and alarms in between — dramatically cutting background drain. *App Standby* restricts apps the user hasn't touched recently (limiting their background/network). To play well: use `WorkManager` (Doze-aware), avoid exact alarms except when truly needed, and don't fight Doze. High-priority FCM can wake the app for urgent messages."},{t:"list",items:["**Doze** — idle device: batches work into maintenance windows.","**Restricts** — network, wakelocks, alarms between windows.","**App Standby** — limits rarely-used apps' background.","**Play well** — WorkManager, avoid exact alarms, high-priority FCM for urgent."]},{t:"note",text:"Doze (idle: screen off, still, unplugged) batches deferred work into periodic maintenance windows and restricts network/wakelocks/alarms between them — cutting background drain. App Standby limits rarely-used apps. Play well: WorkManager (Doze-aware), avoid exact alarms, high-priority FCM for urgent wake-ups. Don't fight Doze."}]},{level:"senior",q:"How does network activity affect battery, and how do you minimize it?",a:[{t:"p",text:"The cellular radio is a major battery consumer: each transmission wakes the radio into a high-power state that stays on for seconds after (the *radio tail*), so *frequent small requests* are far worse than *batched* ones. Minimize drain by *batching* network calls, deferring non-urgent work to when on Wi-Fi/charging (WorkManager constraints), using efficient formats and caching (avoid redundant fetches), and preferring server push (FCM) over polling. Chatty background networking is a top battery offender."},{t:"list",items:["**Radio tail** — each transmission keeps the radio high-power for seconds.","**Batch** — frequent small requests are worse than batched.","**Defer** — WorkManager constraints (Wi-Fi/charging).","**Push over polling** — FCM; cache to avoid redundant fetches."]},{t:"note",text:"The cellular radio is a big battery drain — each transmission keeps it high-power for seconds (the radio tail), so frequent small requests are worse than batched ones. Minimize: batch calls, defer non-urgent work (WorkManager Wi-Fi/charging constraints), cache to avoid redundant fetches, prefer FCM push over polling. Chatty background networking is a top offender."}]},{level:"senior",q:"How does location tracking affect battery, and how do you optimize it?",a:[{t:"p",text:"Continuous high-accuracy GPS is one of the heaviest battery drains. Optimize by requesting the *lowest accuracy* and *longest interval* that meets your need (`Priority.BALANCED_POWER_ACCURACY` over `HIGH_ACCURACY`), using *batched*/passive updates, *geofencing* or the *Fused Location Provider* (which fuses sensors efficiently), and *stopping* updates when not needed (in the background/when the screen is off). Never poll GPS continuously in the background; use the coarsest source that works."},{t:"list",items:["**GPS high-accuracy** — heavy drain.","**Lowest accuracy + longest interval** — that still meets the need.","**Fused Location / geofencing** — efficient, sensor-fused.","**Stop when idle** — don't poll GPS in the background."]},{t:"note",text:"Continuous high-accuracy GPS is one of the heaviest drains. Optimize: lowest accuracy + longest interval that works (BALANCED_POWER over HIGH_ACCURACY), Fused Location Provider (efficient sensor fusion), geofencing, batched/passive updates, and stop updates when not needed (background/screen off). Never poll GPS continuously in the background."}]},{level:"junior",q:"What is the difference between installed size and download size?",a:[{t:"p",text:"*Download size* is what the user downloads from Play (compressed, and with App Bundle, only their device's config splits). *Installed size* is what the app occupies on the device after extraction (larger — uncompressed code/resources plus generated ART files, caches, and data). Play shows download size to users. When optimizing, know which you're targeting: download size affects install conversion; installed size affects device storage pressure. The APK Analyzer shows both."},{t:"list",items:["**Download size** — compressed, device-specific splits (Play shows this).","**Installed size** — extracted on device (larger; + ART/caches/data).","**Download** — affects install conversion.","**Installed** — affects device storage."]},{t:"note",text:"Download size = what the user downloads (compressed, device-specific splits with App Bundle; Play shows this). Installed size = space on device after extraction (larger — uncompressed + ART files/caches/data). Download size affects install conversion; installed size affects storage pressure. APK Analyzer shows both."}]},{level:"senior",q:"How do you reduce the number of dependencies' impact on size?",a:[{t:"p",text:"Each library adds code (and sometimes resources/native libs) — audit them: remove unused ones, avoid pulling a huge library for one function, prefer smaller/modular alternatives, and ensure R8 can tree-shake them (some libraries with heavy reflection resist shrinking). Check the APK Analyzer / dependency size, and watch transitive dependencies. A single heavy dependency (or duplicate versions) can bloat the app significantly."},{t:"list",items:["**Audit** — remove unused; don't pull a huge lib for one function.","**Prefer** — smaller/modular alternatives.","**R8 tree-shaking** — reflection-heavy libs resist it.","**Watch** — transitive deps, duplicate versions."]},{t:"note",text:"Each dependency adds code (sometimes resources/native libs). Audit: remove unused, don't pull a huge library for one function, prefer smaller/modular alternatives, ensure R8 can tree-shake (reflection-heavy libs resist it). Check APK Analyzer and transitive deps; a single heavy or duplicated dependency bloats the app."}]},{level:"senior",q:"What is the difference between deferring work and doing it eagerly, for battery?",a:[{t:"p",text:"Doing background work *eagerly* (immediately, individually) wakes the device/radio repeatedly and fights Doze. *Deferring* it — batching with `WorkManager` and constraints (network, charging, idle) — lets the system run it during favorable windows (batched with other apps' work, when charging or on Wi-Fi), dramatically reducing battery cost. The principle: for anything not user-facing-urgent, *defer and batch* rather than run now. Only truly time-critical work (a live call, a user-initiated sync) should be immediate."},{t:"list",items:["**Eager** — immediate/individual; repeated wakeups, fights Doze.","**Deferred** — WorkManager + constraints; runs in favorable windows.","**Batching** — with other work / charging / Wi-Fi saves battery.","**Immediate only** — truly time-critical work."]},{t:"note",text:"Eager background work (immediate, individual) wakes the device/radio repeatedly and fights Doze. Deferring — batching via WorkManager with constraints (network/charging/idle) — runs it in favorable windows, cutting battery cost. Principle: defer and batch anything not user-urgent; reserve immediate execution for truly time-critical work."}]},{level:"junior",q:"How do you measure and diagnose battery drain?",a:[{t:"p",text:"Use *Battery Historian* — analyze a `bugreport` to see wakelocks, jobs, alarms, network, and CPU over time and pinpoint what your app did while the screen was off. The *Energy Profiler* (Android Studio, supported devices) gives a live rough view. On-device, Settings → Battery usage shows per-app drain. In production, Play Console's *excessive background wakelock/wakeups* vitals flag battery issues. Look for background activity when the device should be idle."},{t:"list",items:["**Battery Historian** — bugreport analysis: wakelocks, jobs, alarms, network.","**Energy Profiler** — live rough view.","**Settings → Battery** — per-app drain on device.","**Play vitals** — excessive wakelock/wakeup flags."]},{t:"note",text:"Diagnose battery drain with Battery Historian (analyze a bugreport — wakelocks, jobs, alarms, network, CPU over time, especially screen-off), the Energy Profiler (live rough view), Settings → Battery (per-app), and Play vitals (excessive wakelock/wakeup flags). Look for background activity when the device should be idle."}]},{level:"senior",q:"What is language/density/ABI splitting in the App Bundle?",a:[{t:"p",text:"The App Bundle lets Play generate *configuration splits* so each device downloads only the resources it needs: its *language* (not all translations), its *screen density* (not all drawable buckets), and its *ABI* (not all native libs). This can cut download size substantially versus a universal APK that includes everything. It's automatic with the AAB — you upload one bundle, Play builds and serves optimized splits per device (Play App Signing enables this)."},{t:"list",items:["**Config splits** — language, density, ABI per device.","**Only needed resources** — not all languages/densities/ABIs.","**Automatic** — with the AAB; Play generates splits.","**vs universal APK** — substantial download savings."]},{t:"note",text:"The App Bundle lets Play generate configuration splits so each device downloads only its language, screen density, and ABI — not all of them. This cuts download size vs a universal APK that bundles everything. Automatic with the AAB: upload one bundle, Play serves optimized per-device splits (via Play App Signing)."}]},{level:"senior",q:"How does JobScheduler/WorkManager batching help battery at the system level?",a:[{t:"p",text:"`WorkManager` (backed by `JobScheduler`) lets the *system* decide *when* to run deferrable jobs — it can *batch* jobs from many apps to run together during a maintenance window or when the device is charging/on Wi-Fi, so the device wakes fewer times overall. Compared to each app scheduling exact alarms and waking independently, this coalescing dramatically reduces total wakeups and radio use across the device — a system-wide battery win that also benefits your app's reputation in vitals."},{t:"list",items:["**System decides when** — deferrable jobs via JobScheduler.","**Batches across apps** — run together in a window/while charging.","**Fewer wakeups** — vs each app's independent exact alarms.","**System-wide battery win** — and better vitals."]},{t:"note",text:"WorkManager (via JobScheduler) lets the system choose when to run deferrable jobs — batching jobs across apps into maintenance windows or charging/Wi-Fi periods, so the device wakes fewer times. Versus each app's independent exact alarms, this coalescing cuts total wakeups/radio use device-wide — a battery win that also helps your vitals."}]},{level:"junior",q:"What is the multidex problem and how is it handled today?",a:[{t:"p",text:"A single dex file can hold ~65,536 methods (the '64K method limit'). Apps exceeding it need *multidex* (multiple dex files). On modern Android (API 21+, ART), multidex is *natively supported* — no special handling needed. Only very old (pre-21) devices required the multidex support library and a slower startup. Today the practical concern isn't multidex itself but keeping method count (and thus size/startup) down via R8 shrinking."},{t:"list",items:["**64K method limit** — per single dex file.","**Multidex** — multiple dex files when exceeded.","**API 21+ (ART)** — natively supported, no special handling.","**Today** — keep method count down via R8 shrinking."]},{t:"note",text:"A single dex holds ~65,536 methods (the 64K limit); exceeding it needs multidex (multiple dex files). API 21+/ART supports multidex natively (no special handling); only pre-21 needed the support library + slower startup. Today the real concern is keeping method count/size down via R8, not multidex itself."}]},{level:"senior",q:"How do you reduce battery use from sensors and background services?",a:[{t:"p",text:"Sensors (accelerometer, gyroscope) and long-running services keep the CPU awake and drain power. Register sensor listeners at the *lowest sampling rate* that works and *unregister* them when not needed (e.g. in `onPause`). Avoid persistent background services; use `WorkManager` for deferrable work and *foreground services only for genuinely ongoing user-visible tasks* (with the smallest footprint). The principle: do the minimum, at the lowest frequency, and stop as soon as you can."},{t:"list",items:["**Sensors** — lowest sampling rate; unregister in `onPause`.","**Avoid** — persistent background services.","**WorkManager** — deferrable work; foreground only when user-visible.","**Principle** — minimum work, lowest frequency, stop early."]},{t:"note",text:"Sensors and long-running services keep the CPU awake and drain power. Register sensor listeners at the lowest rate that works and unregister when idle (onPause). Avoid persistent background services — use WorkManager for deferrable work; foreground services only for genuinely ongoing user-visible tasks. Do the minimum, at the lowest frequency, stop early."}]},{level:"senior",q:"What are startup and size trade-offs of using reflection-heavy libraries?",a:[{t:"p",text:"Reflection-heavy libraries (some DI, serialization, ORM tools) resist R8's static analysis — R8 can't prove which classes/methods are used, so you must add broad `keep` rules, which *bloat* the dex (larger size) and reduce optimization. Reflection is also slower at runtime and can hurt startup. Prefer *compile-time* alternatives (KSP/annotation-processor-based DI like Hilt, code-gen serialization like kotlinx.serialization) that are shrinkable and faster. When you must keep reflection, scope the keep rules tightly."},{t:"list",items:["**Resist R8** — can't prove usage → broad keep rules.","**Bloat + less optimization** — from broad keeps.","**Runtime cost** — reflection is slower; hurts startup.","**Prefer** — compile-time (KSP/codegen) alternatives."]},{t:"note",text:"Reflection-heavy libraries resist R8 (it can't prove usage) — forcing broad keep rules that bloat the dex and reduce optimization; reflection is also slower at runtime (hurts startup). Prefer compile-time alternatives (KSP/annotation-processor DI like Hilt, codegen serialization) that are shrinkable and faster. If you must use reflection, scope keep rules tightly."}]},{level:"junior",q:"How does dark mode / screen brightness relate to battery?",a:[{t:"p",text:"On *OLED/AMOLED* screens (common on phones), black pixels are effectively *off*, so a true-dark (dark mode) UI draws less power than a bright/white one — the display is often the single biggest battery consumer. Supporting dark theme (and using true blacks on OLED) can meaningfully save battery, especially with the screen on for long periods. On LCD screens the effect is smaller (backlight is always on). Also avoid keeping the screen forcibly bright or awake unnecessarily."},{t:"list",items:["**OLED** — black pixels off; dark UI draws less power.","**Display** — often the biggest battery consumer.","**Dark theme + true blacks** — meaningful savings on OLED.","**LCD** — smaller effect (backlight always on)."]},{t:"note",text:"On OLED/AMOLED screens black pixels are effectively off, so a true-dark UI draws less power than a bright/white one — and the display is often the biggest battery consumer. Dark theme with true blacks meaningfully saves battery on OLED (smaller effect on LCD). Also avoid forcing the screen bright or awake unnecessarily."}]},{level:"senior",q:"How do you set and enforce an app-size budget?",a:[{t:"p",text:"Treat size like a metric: establish a *baseline download size*, set a *budget/threshold*, and check it in CI (fail or alert when a build exceeds it, e.g. via APK Analyzer output or bundletool size reports). Investigate regressions the way you would perf regressions — usually a new dependency or unoptimized asset. Combine with periodic audits (APK Analyzer). A budget prevents slow creep that eventually hurts install conversion (larger apps convert worse, especially on limited data plans)."},{t:"list",items:["**Baseline + budget** — set a download-size threshold.","**CI check** — fail/alert on exceeding it.","**Investigate regressions** — usually a new dep or asset.","**Why** — larger apps convert worse (install rate)."]},{t:"note",text:"Enforce an app-size budget like a metric: baseline the download size, set a threshold, check it in CI (fail/alert on exceeding via APK Analyzer/bundletool reports), investigate regressions (usually a new dependency or unoptimized asset). A budget prevents slow creep that hurts install conversion — larger apps convert worse, especially on limited data."}]},{level:"senior",q:"What is the difference between compressed and uncompressed native libraries in the APK?",a:[{t:"p",text:"Historically native libs were *compressed* in the APK and *extracted* to disk at install (using extra device storage — effectively stored twice). Setting `useLegacyPackaging false` (the modern default for API 23+) ships them *uncompressed and page-aligned*, so the OS loads them *directly from the APK* without extraction — reducing on-device install size (no duplicate copy), at a slightly larger *download* (uncompressed). The App Bundle handles this optimally per device."},{t:"list",items:["**Legacy (compressed)** — extracted at install; stored twice.","**`useLegacyPackaging false`** — uncompressed, loaded from APK.","**Smaller install** — no duplicate extracted copy.","**Trade-off** — slightly larger download; AAB optimizes per device."]},{t:"note",text:"Legacy: native libs compressed in the APK and extracted at install (stored twice — extra device storage). useLegacyPackaging false (modern default, API 23+) ships them uncompressed/page-aligned so the OS loads directly from the APK (no extraction, smaller install), at a slightly larger download. The AAB optimizes this per device."}]},{level:"junior",q:"What is Play Feature Delivery, and how does it differ from a monolithic APK?",a:[{t:"p",text:"*Play Feature Delivery* (built on the App Bundle) lets you split your app into a *base module* plus *feature modules* delivered at install-time, on-demand, or conditionally — so users get a smaller initial download and only fetch features they use. A *monolithic APK* bundles everything (all features, all configs) into one download regardless of what the device/user needs. Feature delivery reduces size and lets you deliver large/optional features without bloating the base install."},{t:"list",items:["**Feature Delivery** — base + feature modules (install-time/on-demand/conditional).","**Smaller initial download** — fetch features as used.","**Monolithic APK** — everything in one download.","**Enables** — large/optional features without base bloat."]},{t:"note",text:"Play Feature Delivery (on the App Bundle) splits the app into a base module + feature modules delivered install-time/on-demand/conditionally — smaller initial download, fetch features as used. A monolithic APK bundles everything regardless of need. Feature delivery ships large/optional features without bloating the base install."}]},{level:"senior",q:"How do you balance size/performance trade-offs (e.g. Baseline Profiles add size)?",a:[{t:"p",text:"Some optimizations trade one metric for another: *Baseline Profiles* add a little size but improve startup/jank; *uncompressed native libs* add download size but shrink install size and speed loading; *aggressive image compression* saves size but may reduce quality. Decide by what matters for your users and app: a startup-sensitive app accepts a few KB for a Baseline Profile; a size-sensitive market (low-end, limited data) may prioritize download size. Measure both metrics and choose per the product's priorities."},{t:"list",items:["**Baseline Profile** — small size cost, big startup/jank gain.","**Uncompressed libs** — bigger download, smaller install + faster load.","**Image compression** — smaller size vs quality.","**Decide** — by user/market priorities; measure both metrics."]},{t:"note",text:"Optimizations trade metrics: Baseline Profiles add a little size but improve startup/jank; uncompressed native libs add download but shrink install + speed loading; aggressive image compression saves size vs quality. Choose by what matters (startup-sensitive vs size-sensitive/low-end markets), measuring both metrics against product priorities."}]}],YC={id:"size-battery",title:"App Size & Battery",tagline:"Why size matters, R8 shrinking/obfuscation & keep rules, APK vs AAB & split delivery, dynamic features & resource optimization, and battery optimization.",tags:["Performance","R8","App Bundle","APK Size","Battery"],content:GC,qa:JC},QC={id:"performance",name:"Performance & Memory",description:"Making apps fast & efficient — memory leaks & LeakCanary, ANRs/jank/rendering, profiling & startup & baseline profiles, and app size & battery.",topics:[NC,zC,_C,YC]},$C=[{heading:"What Gradle is and does",blocks:[{t:"p",text:"**Gradle** is the build system Android uses — it takes your source code, resources, and dependencies and produces the runnable app (APK/AAB). It handles *dependency management* (downloading libraries and their transitive dependencies), *compilation* (Kotlin/Java → bytecode → DEX), *resource processing*, running annotation processors/KSP, packaging, and signing. You configure it with build scripts (`build.gradle.kts` in Kotlin DSL, or Groovy `.gradle`), and the **Android Gradle Plugin (AGP)** adds all the Android-specific build logic on top of Gradle."},{t:"list",items:["**Project vs module build files**: the *root* `build.gradle.kts` configures plugins/settings for the whole project; each *module* has its own `build.gradle.kts` declaring that module's plugins, dependencies, and Android config.","**`settings.gradle.kts`** lists the modules that make up the project and configures dependency repositories.","**Dependencies** are declared with configurations — `implementation` (used internally, not exposed to consumers), `api` (exposed to consumers), `testImplementation`/`androidTestImplementation` (test-only), `debugImplementation` (debug builds only, e.g. LeakCanary), `ksp`/`kapt` (annotation processors)."]}]},{heading:"Build types",blocks:[{t:"p",text:"A **build type** defines how the app is built for a stage of development — most commonly **debug** and **release**. Build types differ in things like whether code is minified, whether it's debuggable, and which signing config is used."},{t:"code",title:"Debug vs release build types",code:`android {
    buildTypes {
        debug {
            isMinifyEnabled = false
            applicationIdSuffix = ".debug"     // separate app id, installs alongside release
            isDebuggable = true
        }
        release {
            isMinifyEnabled = true              // R8 shrinking/obfuscation
            isShrinkResources = true
            signingConfig = signingConfigs.getByName("release")
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }
}`},{t:"list",items:["**debug** — for development: not minified (fast builds, but janky — as covered), debuggable, signed with an auto-generated debug key, often with an `applicationIdSuffix` so it installs alongside the release build.","**release** — for production: minified/shrunk (R8), not debuggable, signed with your real release key, optimized. This is what you publish.","You can define *custom* build types (e.g. `staging`) for other environments (a staging server, a QA build)."]}]},{heading:"Product flavors",blocks:[{t:"p",text:"**Product flavors** create different *versions* of your app from the same codebase — for example a `free` vs `paid` version, or `dev`/`staging`/`prod` pointing at different backends, or white-labeled apps for different brands. Each flavor can override configuration (application id, resources, code, backend URL) while sharing most of the code."},{t:"code",title:"Flavors for free/paid",code:`android {
    flavorDimensions += "tier"
    productFlavors {
        create("free") {
            dimension = "tier"
            applicationIdSuffix = ".free"
            buildConfigField("String", "TIER", "\\"free\\"")
        }
        create("paid") {
            dimension = "tier"
            applicationIdSuffix = ".paid"
            buildConfigField("String", "TIER", "\\"paid\\"")
        }
    }
}`},{t:"list",items:["**A flavor overrides config** — application id, version, resources (a flavor-specific `src/free/res/` overrides `src/main/res/`), source code (`src/free/java/`), and `BuildConfig` fields (compile-time constants you read in code, like the backend URL or feature flags).","**Flavor dimensions** — you can combine flavors from multiple dimensions (e.g. a `tier` dimension × an `environment` dimension), producing every combination.","**Use flavors for genuinely different app variants** (free/paid, brands, environments); use *build types* for *how* the app is built (debug/release). They're orthogonal."]}]},{heading:"Build variants — the combination",blocks:[{t:"p",text:"A **build variant** is the *combination* of a build type and product flavors. If you have `debug`/`release` build types and `free`/`paid` flavors, you get four variants: `freeDebug`, `freeRelease`, `paidDebug`, `paidRelease`. Each is a distinct buildable/installable output. You select which variant to build/run in Android Studio's Build Variants panel, and each can have its own source sets and configuration."},{t:"list",items:["**Variant = build type × flavors** — the matrix of all combinations. Each is a separate artifact you can build, test, and (for release variants) publish.","**Source sets per variant** — `src/main/` is shared; `src/free/`, `src/debug/`, and even `src/freeDebug/` provide variant-specific code/resources that override or add to main. This lets you swap implementations per variant (e.g. a mock backend in a `dev` flavor).","**BuildConfig / manifest placeholders** — variant-specific compile-time values (`BuildConfig.TIER`, a backend URL) and manifest values (an API key per environment) let code adapt to the variant without runtime checks."]}]},{heading:"Build performance and modern practices",blocks:[{t:"list",items:["**Build speed matters** — slow builds kill productivity. Key levers: **Gradle configuration cache** (skips the configuration phase on repeat builds — a big win), **build cache** (reuses task outputs, local and remote for CI), **parallel execution** (`org.gradle.parallel` — builds independent modules concurrently, benefits from modularization), and **KSP over KAPT** (KSP is ~2× faster for annotation processing since it avoids Java stub generation).","**Version catalogs** (`gradle/libs.versions.toml`) — a single source of truth for dependency versions and coordinates, with type-safe accessors (`libs.androidx.core`), so 30 modules don't each hardcode versions.","**Convention plugins** (in an included `build-logic` build) — capture shared build configuration (compile SDK, common dependencies) so module build files stay minimal and consistent (the Now-in-Android pattern). Better than `subprojects {}` blocks, which break configuration caching.","**Incremental compilation** — Gradle recompiles only what changed; keeping this working (avoiding things that invalidate it) keeps incremental builds fast — a major reason to modularize (unchanged modules aren't recompiled)."]},{t:"note",text:"Gradle: the build system (dependency management, compile, DEX, resources, package, sign); AGP adds Android logic. Build types = how it's built (debug: not minified, debuggable; release: R8-minified, real signing). Product flavors = different app versions (free/paid, dev/prod) with overridden config (app id, resources, code, BuildConfig). Build variant = build type × flavors (freeDebug, paidRelease…). Speed: config cache, build cache, parallel, KSP over KAPT, version catalogs, convention plugins."}]}],XC=[{level:"junior",q:"What is Gradle and what does it do for an Android project?",a:[{t:"p",text:"**Gradle is the build system Android uses — it turns your source code, resources, and dependencies into the runnable app (APK/AAB).** It automates the whole build process: downloading dependency libraries (and their transitive dependencies), compiling Kotlin/Java to bytecode and then to DEX, processing resources, running annotation processors (KSP/KAPT), packaging everything, and signing the output."},{t:"list",items:["You configure it with **build scripts** — `build.gradle.kts` (Kotlin DSL) in the root project and in each module — declaring plugins, dependencies, and Android configuration.","The **Android Gradle Plugin (AGP)** adds all the Android-specific build logic on top of Gradle (the `android { }` block, build types, flavors, manifest merging, etc.).","**Dependencies** are declared with configurations like `implementation` (internal use), `api` (exposed to consumers), `testImplementation` (test-only), and `debugImplementation` (debug-only, e.g. LeakCanary)."]},{t:"p",text:"In short, Gradle is the engine that assembles your app — you *declare* what your app needs (dependencies, config, build variants) and Gradle *executes* the build. The key concepts to understand on top of it are build types (how the app is built — debug/release), product flavors (different versions of the app), and how they combine into build variants. Understanding Gradle matters because build configuration, dependency management, and build speed are daily concerns for any Android developer."}]},{level:"junior",q:"What is the difference between a build type and a product flavor?",a:[{t:"p",text:"**A build type defines *how* the app is built for a stage of development (debug vs release); a product flavor defines a different *version* of the app (free vs paid, dev vs prod). They're orthogonal — build types are about the build configuration, flavors are about app variants.**"},{t:"list",items:["**Build type** — controls build-level settings: whether code is minified (R8), whether it's debuggable, which signing key is used, whether it's optimized. The two standard ones are **debug** (not minified, debuggable, debug key — for development) and **release** (minified, not debuggable, real key — for publishing). You might add a `staging` type.","**Product flavor** — creates genuinely different versions of the app from the same codebase: a `free` vs `paid` version, `dev`/`staging`/`prod` pointing at different backends, or branded/white-label variants. Each flavor overrides configuration — application id, resources, code, backend URL, feature flags (`BuildConfig` fields)."]},{t:"p",text:"The clearest way to remember it: build type answers 'how do I build this?' (debug or release settings), and flavor answers 'which version of the app am I building?' (free or paid, which backend). They combine into **build variants** — a build type × flavors — so `free`/`paid` flavors and `debug`/`release` types give you `freeDebug`, `freeRelease`, `paidDebug`, `paidRelease`. Use build types for the dev-vs-production distinction and flavors for actual product variants; mixing up the two concepts (e.g. making 'free' a build type) leads to awkward configuration."}]},{level:"junior",q:"What is a build variant?",a:[{t:"p",text:"**A build variant is the combination of a build type and product flavors — it's a specific, distinct buildable/installable output of your app.** If you have `debug`/`release` build types and `free`/`paid` flavors, you get four variants: `freeDebug`, `freeRelease`, `paidDebug`, `paidRelease`. Each is a separate artifact you can build, run, test, and (for release variants) publish."},{t:"list",items:["**Variant = build type × flavors** — the matrix of all combinations. With no flavors, your variants are just your build types (debug, release).","**Each variant can have its own source set** — `src/main/` is shared across all, while `src/free/`, `src/debug/`, or even `src/freeDebug/` provide variant-specific code and resources that override or add to main. This lets you, say, use a mock backend in a `dev` flavor or different branding per flavor.","**You select the active variant** in Android Studio's Build Variants panel to build/run a specific one."]},{t:"p",text:"Variants are how you produce all the different builds of your app from one codebase — a debug build of the free version for development, a release build of the paid version for the store, etc. Combined with `BuildConfig` fields and manifest placeholders (variant-specific compile-time values), variants let the same code adapt to different environments and product tiers without runtime branching. For example, `BuildConfig.TIER` or a per-flavor backend URL lets code behave correctly for each variant, resolved at build time."}]},{level:"junior",q:"How do you speed up Gradle builds?",a:[{t:"list",items:["**Gradle configuration cache** — caches the result of the configuration phase, so repeat builds skip re-configuring the project. Often a big win; enable it in `gradle.properties`.","**Build cache** — reuses the outputs of tasks whose inputs haven't changed (local, and *remote* for CI so builds share results across machines).","**Parallel execution** (`org.gradle.parallel=true`) — builds independent modules concurrently; the benefit scales with how modularized (and wide) your project is.","**KSP instead of KAPT** — KSP is roughly 2× faster for annotation processing (Hilt, Room, Moshi) because it works directly with Kotlin instead of generating Java stubs.","**Modularization** — with modules, Gradle recompiles only the modules that changed (compile avoidance) and builds independent ones in parallel; a monolith recompiles everything.","**Version catalogs and convention plugins** — reduce configuration duplication and keep it cache-friendly (avoid `subprojects {}` / `allprojects {}` blocks, which break configuration caching)."]},{t:"p",text:"Build speed matters because slow builds directly hurt developer productivity — every build is waiting time, multiplied across the whole team all day. The biggest levers are usually enabling the caches (configuration cache + build cache), migrating KAPT to KSP, and modularizing so unchanged code isn't recompiled. As with performance work, it helps to *measure* — Gradle build scans (`--scan`) show where build time goes, so you optimize the actual bottleneck (often annotation processing, a slow module on the critical path, or unnecessary reconfiguration) rather than guessing."}]},{level:"senior",q:"How would you structure build configuration for an app with multiple environments (dev/staging/prod) and free/paid tiers?",a:[{t:"p",text:"**I'd use two *flavor dimensions* — one for environment (dev/staging/prod) and one for tier (free/paid) — combined with the standard debug/release build types, giving a clean matrix where each concern is expressed independently and combined automatically. This keeps configuration DRY and makes every meaningful combination buildable.**"},{t:"code",title:"Two flavor dimensions + build types",code:`android {
    flavorDimensions += listOf("environment", "tier")
    productFlavors {
        create("dev")     { dimension = "environment"; buildConfigField("String","BASE_URL","\\"https://dev.api\\"") }
        create("staging") { dimension = "environment"; buildConfigField("String","BASE_URL","\\"https://staging.api\\"") }
        create("prod")    { dimension = "environment"; buildConfigField("String","BASE_URL","\\"https://api\\"") }
        create("free")    { dimension = "tier"; applicationIdSuffix = ".free" }
        create("paid")    { dimension = "tier"; applicationIdSuffix = ".paid" }
    }
    // build types: debug, release
}`},{t:"list",items:["**Two dimensions, not one flat list**: environment (dev/staging/prod) and tier (free/paid) are *independent* concerns, so they belong in separate flavor dimensions. Gradle then produces every combination — `devFreeDebug`, `prodPaidRelease`, etc. — automatically. Cramming both into one dimension would force you to enumerate all six combinations manually (devFree, devPaid, stagingFree…), which is repetitive and error-prone. Dimensions express the *orthogonality* correctly.","**Environment drives config, not code duplication**: each environment flavor sets `BuildConfig` fields (backend URL, API keys via manifest placeholders, feature flags) so the *same code* reads `BuildConfig.BASE_URL` and points at the right backend per variant — no runtime environment checks, no duplicated code. Secrets per environment come from Gradle properties / CI secrets, not hardcoded.","**Tier drives features and app id**: the tier flavor sets an `applicationIdSuffix` (so free and paid install as separate apps / are separate Play listings if desired) and a `BuildConfig.TIER` flag, plus tier-specific source sets (`src/paid/`) for premium-only code/resources. Feature gating reads the tier flag or uses the source-set override.","**Build types stay about build config**: debug (unminified, debuggable, debug key) vs release (R8, real signing) — orthogonal to both flavor dimensions, so a `prodPaidRelease` is your production paid build and `devFreeDebug` is a developer's free-tier dev build.","**Manage the combinatorial explosion**: two dimensions × build types = 3×2×2 = 12 variants, which is a lot. Use `variantFilter` (or the newer variant API) to *disable* nonsensical combinations (you probably never ship a `dev` release), keeping only the variants you actually build. This prevents build/test time blowing up on variants nobody uses."]},{t:"list",items:["**Keep it DRY with version catalogs + convention plugins**: shared dependencies and config live in a version catalog and convention plugins, so adding a module or changing the compile SDK is one edit, not one-per-module. Environment-specific values come from `BuildConfig`/manifest placeholders fed by Gradle properties (and CI secrets for keys), never hardcoded per flavor.","**CI builds the right variants**: CI produces `prodFreeRelease`/`prodPaidRelease` AABs for the store, runs tests on a representative variant (e.g. `prodFreeDebug`), and can build `staging` variants for QA — all from the same configuration."]},{t:"p",text:"**The senior framing**: the key insight is expressing *independent concerns as independent dimensions* — environment and tier are orthogonal, so two flavor dimensions (not one flat flavor list) model them correctly and let Gradle generate the matrix, keeping configuration DRY. Environment differences are handled via `BuildConfig`/manifest values that the *same code* reads (config, not code duplication), and tier differences via flags + source-set overrides for premium features. Then you *prune* the combinatorial explosion with `variantFilter` so you only build meaningful variants, and keep the whole thing maintainable with version catalogs and convention plugins. This gives a clean, scalable build setup where adding an environment or tier is a small, localized change — versus the anti-pattern of one flat flavor dimension with manually enumerated combinations, or environment logic scattered as runtime `if` checks. Demonstrating the dimensions-for-orthogonal-concerns modeling, config-over-code-duplication, and variant pruning is the comprehensive answer."}]},{level:"senior",q:"What is the difference between implementation and api dependency configurations, and why does it matter for build speed?",a:[{t:"p",text:"**`implementation` keeps a dependency *internal* to the module — consumers of the module can't see it and, crucially, don't recompile when it changes. `api` *exposes* the dependency to consumers — they can use its types and *do* recompile when it changes. The build-speed impact comes from this recompilation difference.**"},{t:"list",items:["**`implementation`** — the dependency is a private implementation detail. Its types aren't visible on the module's public API, and — the key part — when that dependency's *ABI (public signature)* changes, modules that *depend on this module* do **not** need to recompile, because they never saw that dependency. This limits the 'blast radius' of a change.","**`api`** — the dependency is re-exported as part of the module's public API. Consumers can reference its types directly, and when it changes, everything that depends on this module *must* recompile (because the change might affect the exposed API). This propagates recompilation transitively."]},{t:"list",items:["**Why it matters for build speed — compile avoidance**: in a multi-module project, Gradle tries to *avoid recompiling* modules whose inputs haven't changed. `implementation` boundaries *enable* this — a change deep in a dependency only recompiles the module using it, not the whole graph above. `api` *defeats* it — the change ripples up to every transitive consumer. So a project that overuses `api` (or uses it by accident) has changes that cascade recompilation across many modules, making incremental builds slow; one that uses `implementation` correctly keeps recompilation localized and builds fast.","**The rule**: use `implementation` by default (always), and `api` *only* when your module's public API genuinely exposes the dependency's types (e.g. a `core-model` module whose public classes are used by consumers). Accidental `api` usage — or the old `compile` configuration which behaved like `api` — is a common reason 'we modularized but builds didn't get faster': the recompilation cascades because dependencies leak through `api`.","**How to audit it**: build scans and dependency reports reveal `api` leakage; the fix is demoting unnecessary `api` to `implementation`, which shrinks the recompilation blast radius."]},{t:"p",text:"**The senior framing**: `implementation` vs `api` is fundamentally about *encapsulation* (does this module leak its dependency?) and that encapsulation directly determines *build performance* via compile avoidance. `implementation` is a wall that stops both visibility *and* recompilation from propagating; `api` is a window that lets both through. The build-speed payoff of modularization *depends* on using `implementation` boundaries so changes stay localized — overusing `api` collapses the modules back into effectively one recompilation unit. So the guidance is 'implementation always, api only when the public API truly requires it', and diagnosing slow modular builds often means finding and removing `api` leakage. Connecting the encapsulation semantics to the compile-avoidance/build-speed consequence is exactly the depth these questions probe — and it ties directly to the modularization topic's theme that modular boundaries must be `implementation` to deliver their build-speed benefit."}]},{level:"junior",q:"What is the difference between Gradle and the Android Gradle Plugin (AGP)?",a:[{t:"p",text:"*Gradle* is the general-purpose build tool (task graph, dependency resolution) — not Android-specific. The *Android Gradle Plugin (AGP)* is the plugin that teaches Gradle *how to build Android* — it adds build types, flavors, the manifest merger, resource processing, dexing (D8/R8), and packaging into APKs/AABs. You apply AGP (`com.android.application`) in your module. AGP versions are tied to Android Studio and specific Gradle versions."},{t:"list",items:["**Gradle** — general build tool (tasks, dependencies).","**AGP** — plugin adding Android build logic.","**AGP adds** — build types/flavors, manifest merge, R8, packaging.","**Coupled** — AGP version ↔ Gradle version ↔ Studio."]},{t:"note",text:"Gradle is the general build tool (task graph, dependency resolution); the Android Gradle Plugin (AGP) teaches it to build Android — build types/flavors, manifest merger, resource processing, D8/R8, APK/AAB packaging. You apply AGP (com.android.application). AGP versions are tied to specific Gradle and Studio versions."}]},{level:"senior",q:"What are Gradle's three build phases?",a:[{t:"p",text:"*Initialization* — Gradle determines which projects/modules take part (reads `settings.gradle`). *Configuration* — it evaluates every module's build script to build the *task graph* (this runs even for tasks you won't execute — why heavy configuration slows every build). *Execution* — it runs the selected tasks (and their dependencies) in order. Understanding this explains why 'configuration avoidance' (lazy task creation) and the *configuration cache* matter for speed."},{t:"list",items:["**Initialization** — which modules participate (`settings.gradle`).","**Configuration** — evaluate scripts, build the task graph.","**Execution** — run selected tasks + dependencies.","**Insight** — configuration runs every build (avoid heavy work there)."]},{t:"note",text:"Gradle phases: initialization (which modules participate — settings.gradle), configuration (evaluate every script to build the task graph — runs even for tasks you won't run), execution (run selected tasks). Heavy configuration slows every build — why configuration avoidance and the configuration cache matter."}]},{level:"junior",q:"What is the difference between settings.gradle and build.gradle?",a:[{t:"p",text:'`settings.gradle(.kts)` is *project-level* — it defines which modules are included (`include(":app", ":core")`) and repository/plugin management for the whole build. `build.gradle(.kts)` is *per-module* (and one at the root) — it configures *how that module builds*: plugins applied, dependencies, `android {}` block (compileSdk, build types, flavors). One settings file per project; one build file per module.'},{t:"list",items:["**`settings.gradle`** — project-level; which modules + repos.","**`build.gradle`** — per-module; plugins, deps, `android {}`.","**One settings** — per project.","**One build** — per module (+ root)."]},{t:"note",text:'settings.gradle(.kts) is project-level — includes modules (include(":app")) and manages repositories/plugins. build.gradle(.kts) is per-module — applies plugins, declares dependencies, configures android {} (compileSdk, build types, flavors). One settings file per project; one build file per module plus root.'}]},{level:"junior",q:"What is the Gradle wrapper, and why use it?",a:[{t:"p",text:"The *Gradle wrapper* (`gradlew`/`gradlew.bat` + `gradle-wrapper.properties`) pins the *exact Gradle version* for the project and downloads it automatically. Everyone (and CI) builds with the *same* Gradle version — no 'works on my machine' from version drift, and no need to install Gradle manually. Always run `./gradlew` (not a system `gradle`) and commit the wrapper files. Upgrade the version via `gradle-wrapper.properties`."},{t:"list",items:["**Pins the Gradle version** — per project.","**Auto-downloads** — no manual install.","**Consistency** — everyone + CI use the same version.","**Use `./gradlew`** — commit the wrapper files."]},{t:"note",text:"The Gradle wrapper (gradlew + gradle-wrapper.properties) pins the exact Gradle version and auto-downloads it, so everyone and CI build with the same version (no version-drift 'works on my machine', no manual install). Always run ./gradlew and commit the wrapper files; upgrade via gradle-wrapper.properties."}]},{level:"senior",q:"What is a version catalog (libs.versions.toml)?",a:[{t:"p",text:"A *version catalog* (`gradle/libs.versions.toml`) is a central, type-safe place to declare dependency *versions* and *coordinates*, referenced across modules as `libs.retrofit` etc. It replaces scattered hardcoded versions and `ext` variables — one source of truth, consistent versions everywhere, IDE autocomplete, and easy upgrades. It also supports *bundles* (groups of deps) and *plugin* versions. The modern standard for multi-module dependency management."},{t:"code",title:"libs.versions.toml + usage",code:`# gradle/libs.versions.toml
[versions]
retrofit = "2.11.0"
[libraries]
retrofit = { module = "com.squareup.retrofit2:retrofit", version.ref = "retrofit" }

// build.gradle.kts
dependencies { implementation(libs.retrofit) }`},{t:"note",text:"A version catalog (gradle/libs.versions.toml) centralizes dependency versions and coordinates, referenced type-safely as libs.retrofit across modules — one source of truth, consistent versions, IDE autocomplete, easy upgrades. Supports bundles and plugin versions. The modern standard for multi-module dependency management."}]},{level:"senior",q:"What is the Gradle configuration cache, and how does it speed builds?",a:[{t:"p",text:"The *configuration cache* stores the result of the *configuration phase* (the task graph) so subsequent builds *skip re-configuring* when nothing relevant changed — jumping straight to execution. Since configuration runs every build and can be slow in large projects, this is a big speedup. It requires build logic to be *compatible* (no reading mutable state at execution time, no `Project` access in task actions) — enabling it often surfaces build-script issues to fix."},{t:"list",items:["**Caches** — the configuration-phase result (task graph).","**Skips re-configuration** — when nothing relevant changed.","**Big win** — configuration runs every build; slow in large projects.","**Requires** — compatible build logic (no Project access at execution)."]},{t:"note",text:"The configuration cache stores the configuration-phase result (task graph), so unchanged builds skip re-configuring and jump to execution — a big speedup since configuration runs every build. It requires compatible build logic (no Project access / mutable-state reads in task actions); enabling it surfaces script issues to fix."}]},{level:"senior",q:"What is the Gradle build cache (local and remote)?",a:[{t:"p",text:"The *build cache* stores *task outputs* keyed by their inputs, so if a task's inputs are unchanged, Gradle *reuses the cached output* instead of re-running it. The *local* cache reuses outputs across builds on your machine; the *remote* cache (shared, e.g. on CI) lets the whole team/CI reuse each other's outputs — so a clean build can pull compiled results rather than recompiling. Combined with incremental builds, it dramatically cuts build times."},{t:"list",items:["**Caches task outputs** — keyed by inputs.","**Unchanged inputs** — reuse output, skip the task.","**Local** — across builds on your machine.","**Remote** — shared team/CI cache (reuse others' outputs)."]},{t:"note",text:"The build cache stores task outputs keyed by inputs — unchanged inputs → reuse the cached output instead of re-running. Local cache reuses across builds on your machine; remote (shared, CI) lets the team/CI reuse each other's outputs (a clean build pulls compiled results). With incremental builds, it cuts build times sharply."}]},{level:"junior",q:"What is the difference between the Groovy and Kotlin DSL for Gradle?",a:[{t:"p",text:"Gradle build scripts can be written in *Groovy* (`build.gradle`) or the *Kotlin DSL* (`build.gradle.kts`). Kotlin DSL gives *type safety*, better IDE support (autocomplete, refactoring, click-through), and compile-time error checking — at the cost of slightly slower first-time script compilation. Groovy is more lenient/dynamic. New Android projects default to the Kotlin DSL; it's the recommended choice for its tooling benefits, especially in larger builds."},{t:"list",items:["**Groovy** — `build.gradle`; dynamic, lenient.","**Kotlin DSL** — `build.gradle.kts`; type-safe, better IDE.","**Kotlin DSL cost** — slower first script compilation.","**Default** — Kotlin DSL for new projects."]},{t:"note",text:"Gradle scripts: Groovy (build.gradle — dynamic, lenient) or Kotlin DSL (build.gradle.kts — type-safe, better IDE autocomplete/refactoring, compile-time checks, slightly slower first compilation). New Android projects default to and recommend the Kotlin DSL for its tooling benefits, especially in larger builds."}]},{level:"senior",q:"What is buildSrc versus convention plugins for sharing build logic?",a:[{t:"p",text:"Both share build logic across modules. `buildSrc` is a special directory compiled before the build — its code is available to all build scripts, but *any change invalidates the whole build* (recompiles everything). *Convention plugins* (custom plugins in an included build, e.g. `build-logic`) let you define reusable configuration (`my-android-library` convention) applied per module, with *better incrementality* (a change doesn't invalidate everything) and cleaner composition. Convention plugins are the modern recommendation for large multi-module builds."},{t:"list",items:["**Both** — share build logic across modules.","**`buildSrc`** — global, but any change invalidates the whole build.","**Convention plugins** — reusable per-module config, better incrementality.","**Modern** — convention plugins (`build-logic`) for large builds."]},{t:"note",text:"Sharing build logic: buildSrc (compiled before the build, available everywhere, but any change invalidates the whole build) vs convention plugins (custom plugins in an included build-logic, applied per module — reusable config with better incrementality and composition). Convention plugins are the modern recommendation for large multi-module builds."}]},{level:"junior",q:"What is the difference between debug and release build types?",a:[{t:"p",text:"*Debug* is for development: `debuggable true`, signed with the auto-generated debug key, no shrinking/obfuscation (fast builds), often extra logging/tools. *Release* is for production: signed with your *release key*, `minifyEnabled`/`shrinkResources` on (R8), `debuggable false`, optimized and smaller. You customize each in the `buildTypes {}` block. Always test performance/size on *release* — debug behaves very differently."},{t:"table",headers:["","Debug","Release"],rows:[["Signing","Debug key (auto)","Release keystore"],["Shrinking","Off (fast builds)","R8 on (smaller)"],["debuggable","true","false"],["Use","Development","Production"]]},{t:"note",text:"Debug: development — debuggable, auto debug key, no shrinking (fast), extra logging. Release: production — release keystore, minifyEnabled/shrinkResources (R8), not debuggable, optimized/smaller. Customize each in buildTypes {}. Always measure performance/size on release — debug behaves very differently."}]},{level:"senior",q:"What is BuildConfig, and how do you use build config fields?",a:[{t:"p",text:"`BuildConfig` is a generated class exposing build-time constants: `BuildConfig.DEBUG`, `BuildConfig.VERSION_NAME`, and *custom fields* you define per build type/flavor with `buildConfigField`. Use it to bake environment-specific values (API base URLs, feature flags, keys that aren't secret) into the build so code can branch on them. Enable it with `buildFeatures { buildConfig = true }` (now opt-in). Don't put real secrets here — they're extractable from the APK."},{t:"code",title:"Custom BuildConfig field",code:`android {
  buildFeatures { buildConfig = true }
  buildTypes {
    release { buildConfigField("String", "BASE_URL", "\\"https://api.example.com\\"") }
    debug   { buildConfigField("String", "BASE_URL", "\\"https://staging.example.com\\"") }
  }
}
// usage: Retrofit.Builder().baseUrl(BuildConfig.BASE_URL)`},{t:"note",text:"BuildConfig is a generated class of build-time constants — DEBUG, VERSION_NAME, and custom buildConfigField values per build type/flavor (API URLs, feature flags). Enable with buildFeatures { buildConfig = true }. Branch code on them for environment config. Never store real secrets here — they're extractable from the APK."}]},{level:"junior",q:"What are manifest placeholders?",a:[{t:"p",text:"*Manifest placeholders* let you inject build-variant-specific values into `AndroidManifest.xml` at build time. You define them in Gradle (`manifestPlaceholders = [key: value]`) and reference them in the manifest as `${key}`. Common uses: per-flavor deep-link hosts, API keys for services declared in the manifest (like Maps), or app labels. This avoids maintaining separate manifests per variant — one manifest, values filled in per build."},{t:"code",title:"Manifest placeholder",code:`// build.gradle.kts
defaultConfig { manifestPlaceholders["mapsKey"] = "AIza..." }

<!-- AndroidManifest.xml -->
<meta-data android:name="com.google.android.geo.API_KEY"
           android:value="\${mapsKey}" />`},{t:"note",text:"Manifest placeholders inject build-variant-specific values into AndroidManifest.xml at build time — defined in Gradle (manifestPlaceholders), referenced as ${key}. Uses: per-flavor deep-link hosts, Maps/service API keys, app labels. One manifest with values filled per build, instead of separate manifests per variant."}]},{level:"senior",q:"How does Gradle resolve dependency version conflicts?",a:[{t:"p",text:"When multiple dependencies bring *different versions* of the same library (transitively), Gradle by default picks the *highest* version (newest wins) to satisfy everyone. You can override with *constraints*, `resolutionStrategy.force`, or a version catalog to pin a specific version. `./gradlew :app:dependencies` (or the dependency insight report) shows the resolved graph and *why* a version was chosen. Conflicts causing runtime crashes usually mean an incompatible forced/transitive version."},{t:"list",items:["**Default** — highest version wins (newest).","**Override** — constraints, `force`, version catalog pinning.","**Inspect** — `:app:dependencies` / dependencyInsight.","**Crashes** — often an incompatible forced/transitive version."]},{t:"note",text:"On conflicting transitive versions of a library, Gradle picks the highest (newest wins). Override with constraints, resolutionStrategy.force, or a version catalog. Inspect with ./gradlew :app:dependencies or the dependencyInsight report to see the resolved graph and why. Runtime crashes often mean an incompatible forced/transitive version."}]},{level:"junior",q:"What is compileOnly and runtimeOnly, versus implementation?",a:[{t:"p",text:"`implementation` puts a dependency on both the compile and runtime classpath (and hides it from consumers). `compileOnly` is available only at *compile time*, not packaged at runtime (for annotations, provided APIs, or libraries supplied by the environment). `runtimeOnly` is packaged and available at *runtime* but not compile time (e.g. a driver/implementation you don't reference directly). Most app dependencies are `implementation`; the others are for specific provided/runtime scenarios."},{t:"list",items:["**`implementation`** — compile + runtime; most deps.","**`compileOnly`** — compile only, not packaged (annotations/provided).","**`runtimeOnly`** — runtime only, not on compile classpath.","**Pick by** — when the dependency is actually needed."]},{t:"note",text:"implementation: compile + runtime classpath (most deps; hidden from consumers). compileOnly: compile-time only, not packaged (annotations, provided APIs). runtimeOnly: packaged for runtime but not on the compile classpath (drivers/implementations you don't reference). Choose by when the dependency is actually needed."}]},{level:"senior",q:"What is the Gradle daemon, and how does it help?",a:[{t:"p",text:"The *Gradle daemon* is a long-lived background JVM process that stays warm between builds — avoiding JVM startup cost each time and keeping caches/classes hot (JIT-optimized). This makes repeated builds significantly faster. It's on by default. Occasionally a stale/misbehaving daemon causes odd build issues (fix with `--stop` to kill daemons). Give it enough heap (`org.gradle.jvmargs`) for large projects."},{t:"list",items:["**Long-lived JVM** — warm between builds.","**Avoids** — JVM startup cost each build.","**Keeps caches hot** — faster repeated builds.","**Stale daemon** — `--stop`; size heap via `org.gradle.jvmargs`."]},{t:"note",text:"The Gradle daemon is a long-lived background JVM kept warm between builds — avoiding per-build JVM startup and keeping caches/JIT hot, so repeated builds are much faster (on by default). A stale daemon can cause odd issues (--stop to kill). Give it enough heap via org.gradle.jvmargs for large projects."}]},{level:"senior",q:"What is KSP versus KAPT, and how does it affect build speed?",a:[{t:"p",text:"Both process annotations to generate code (for Room, Hilt, Moshi, etc.). *KAPT* (Kotlin Annotation Processing) works by generating *Java stubs* for your Kotlin so Java-based processors can run — this stub generation is *slow*. *KSP* (Kotlin Symbol Processing) reads Kotlin directly via a lightweight API — *much faster* (often ~2×) and Kotlin-idiomatic. Migrate processors to KSP where supported for significant build-speed wins; KAPT remains only for processors without a KSP version."},{t:"list",items:["**Both** — annotation processing → generated code.","**KAPT** — generates Java stubs; slow.","**KSP** — reads Kotlin directly; ~2× faster.","**Migrate** — to KSP where supported for build speed."]},{t:"note",text:"KAPT (Kotlin Annotation Processing) generates Java stubs so Java processors run — slow. KSP (Kotlin Symbol Processing) reads Kotlin directly via a light API — much faster (~2×) and idiomatic. Migrate Room/Hilt/Moshi processors to KSP where supported for big build-speed wins; keep KAPT only for processors without a KSP version."}]},{level:"junior",q:"What is a Gradle task, and how do tasks relate to each other?",a:[{t:"p",text:"A *task* is a unit of build work (`compileDebugKotlin`, `assembleRelease`, `test`). Tasks declare *dependencies* on other tasks, forming a *directed acyclic graph (DAG)*; Gradle runs the needed tasks in dependency order, skipping *up-to-date* ones (unchanged inputs/outputs). You run tasks by name (`./gradlew assembleDebug`). Custom tasks let you script build steps. Understanding tasks explains incremental builds (only affected tasks re-run)."},{t:"list",items:["**Task** — a unit of build work.","**DAG** — tasks depend on tasks; run in order.","**Up-to-date** — skipped when inputs/outputs unchanged.","**Run** — `./gradlew <task>`; custom tasks script steps."]},{t:"note",text:"A Gradle task is a unit of build work (compileDebugKotlin, assembleRelease). Tasks depend on tasks forming a DAG; Gradle runs them in order and skips up-to-date ones (unchanged inputs/outputs) — the basis of incremental builds. Run by name (./gradlew assembleDebug); custom tasks script build steps."}]},{level:"senior",q:"How does modularization improve build times?",a:[{t:"p",text:"Splitting the app into modules lets Gradle *build them in parallel* and *only rebuild changed modules* (and their dependents) — a change in a leaf feature module doesn't recompile the whole app. Combined with the build cache, unchanged modules are pulled from cache. It also improves *incrementality* and enables faster CI. The key is a good dependency graph (few wide dependencies) so changes have a small blast radius; over-coupling erodes the benefit."},{t:"list",items:["**Parallel builds** — modules built concurrently.","**Rebuild only changed** — modules + dependents.","**Build cache** — unchanged modules from cache.","**Needs** — a good graph (small change blast radius)."]},{t:"note",text:"Modularization lets Gradle build modules in parallel and rebuild only changed modules + their dependents (a leaf change doesn't recompile everything); with the build cache, unchanged modules come from cache. Needs a good dependency graph (small change blast radius) — over-coupling erodes the benefit."}]},{level:"junior",q:"What is minifyEnabled and shrinkResources in the build config?",a:[{t:"p",text:"`minifyEnabled true` turns on *R8* (code shrinking, optimization, obfuscation) for that build type; `shrinkResources true` removes unused *resources* (requires `minifyEnabled`). You enable both on `release` for a smaller, optimized, harder-to-reverse APK. They need correct `keep` rules (`proguard-rules.pro`) so R8 doesn't strip code used only via reflection. Keep them *off* for debug (faster builds, easier debugging)."},{t:"code",title:"Enabling shrinking on release",code:`buildTypes {
  release {
    isMinifyEnabled = true      // R8: shrink + optimize + obfuscate
    isShrinkResources = true    // remove unused resources
    proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
  }
}`},{t:"note",text:"minifyEnabled true enables R8 (shrink/optimize/obfuscate) for a build type; shrinkResources true removes unused resources (needs minifyEnabled). Enable both on release for a smaller, harder-to-reverse APK, with correct keep rules so reflection-used code survives. Keep them off for debug (faster builds, easier debugging)."}]},{level:"senior",q:"What are the main levers for speeding up Gradle builds?",a:[{t:"p",text:"Beyond a fast machine: enable the *configuration cache* and *build cache*, keep the *daemon* and *parallel* execution on (`org.gradle.parallel=true`), give Gradle enough *heap*, use *KSP over KAPT*, *modularize* for parallelism and small rebuild scope, avoid heavy work in the configuration phase (lazy task/config avoidance), keep dependencies/AGP up to date, and don't run unnecessary tasks (e.g. skip lint/tests in inner-loop builds). Profile with `--scan` / build scans to find the actual bottleneck."},{t:"list",items:["**Caches** — configuration + build cache.","**Parallel + daemon + heap** — `org.gradle.parallel`, jvmargs.","**KSP over KAPT; modularize** — for speed/parallelism.","**Profile** — build scans (`--scan`) to find the bottleneck."]},{t:"note",text:"Speed levers: configuration cache + build cache, daemon + parallel execution (org.gradle.parallel), enough heap, KSP over KAPT, modularization (parallelism + small rebuild scope), avoid heavy configuration-phase work, keep AGP/deps current, skip unneeded tasks in inner loop. Profile with build scans (--scan) to find the real bottleneck."}]},{level:"senior",q:"What is the difference between a project and a module (subproject) in Gradle?",a:[{t:"p",text:"In Gradle terms, the *root project* is the whole build; each *module* (`:app`, `:core`) is a *subproject* included in `settings.gradle`. Confusingly, Android Studio calls subprojects 'modules'. Each subproject has its own `build.gradle` and produces its own artifact (an app, a library). Subprojects depend on each other via `implementation(project(\":core\"))`. The root project usually holds shared config, not code."},{t:"list",items:["**Root project** — the whole build.","**Subproject/module** — `:app`, `:core`; own build.gradle + artifact.",'**Depend** — `implementation(project(":core"))`.',"**Root** — shared config, usually no code."]},{t:"note",text:`In Gradle the root project is the whole build; each module (:app, :core) is a subproject included in settings.gradle (Studio calls these 'modules'). Each has its own build.gradle and produces its own artifact; they depend via implementation(project(":core")). The root project holds shared config, usually no code.`}]},{level:"senior",q:"How do product flavors and flavor dimensions work for build variants?",a:[{t:"p",text:"*Flavors* define different app versions (free/paid, brandA/brandB) with distinct config, resources, and code (source sets like `src/paid/`). *Flavor dimensions* let you combine independent axes — e.g. a `tier` dimension (free/paid) × an `env` dimension (dev/prod) yields flavors like `freeDev`, `paidProd`. Each flavor × build type is a *variant* (`freeDevDebug`). Use flavors for genuinely different product configurations; overusing them multiplies variants (and build time)."},{t:"code",title:"Flavor dimensions",code:`android {
  flavorDimensions += listOf("tier", "env")
  productFlavors {
    create("free") { dimension = "tier" }
    create("paid") { dimension = "tier" }
    create("dev")  { dimension = "env" }
    create("prod") { dimension = "env" }
  }
}
// variants: freeDevDebug, paidProdRelease, ...`},{t:"note",text:"Flavors define app versions (free/paid) with distinct config/resources/source sets; flavor dimensions combine independent axes (tier × env → freeDev, paidProd). Each flavor × build type is a variant (freeDevDebug). Use for genuinely different products; overuse multiplies variants and build time."}]},{level:"senior",q:"How do you keep secrets (API keys) out of the build and version control?",a:[{t:"p",text:"Never hardcode secrets in source or commit them. Keep them in a *gitignored* `local.properties` or `secrets.properties` (or environment variables in CI), read them in Gradle, and expose via `BuildConfig`/manifest placeholders only what the app truly needs. Remember: anything compiled into the APK is *extractable* — so genuinely sensitive secrets (signing keys, server secrets) should live server-side, not in the app at all. The `secrets-gradle-plugin` helps manage this for keys like Maps."},{t:"list",items:["**Gitignored** — `local.properties`/`secrets.properties` or CI env vars.","**Read in Gradle** — expose minimal via BuildConfig/placeholders.","**APK is extractable** — no true secrets in the app.","**Server-side** — signing keys, server secrets."]},{t:"note",text:"Keep secrets out of source/VCS: store in gitignored local.properties/secrets.properties or CI env vars, read in Gradle, expose only what's needed via BuildConfig/placeholders. Anything in the APK is extractable — genuinely sensitive secrets belong server-side, not in the app. The secrets-gradle-plugin helps for keys like Maps."}]},{level:"junior",q:"What is the difference between assemble, bundle, and install tasks?",a:[{t:"p",text:"`assembleRelease` builds the *APK(s)* for that variant; `bundleRelease` builds the *AAB* (App Bundle) for Play upload; `installDebug` builds and *installs* the APK on a connected device/emulator. `build` runs assemble plus checks (lint/tests). For Play you upload the *AAB* (`bundle`); for local device testing you `install` or `assemble`. Knowing which task produces which artifact avoids uploading the wrong thing."},{t:"list",items:["**`assemble<Variant>`** — build APK(s).","**`bundle<Variant>`** — build AAB for Play.","**`install<Variant>`** — build + install on device.","**`build`** — assemble + checks (lint/tests)."]},{t:"note",text:"assembleRelease builds APK(s); bundleRelease builds the AAB (for Play upload); installDebug builds + installs on a device; build runs assemble plus lint/tests. Upload the AAB (bundle) to Play; use install/assemble for local testing. Know which task makes which artifact to avoid uploading the wrong one."}]}],ZC={id:"gradle-build",title:"Gradle & the Build System",tagline:"What Gradle & AGP do, build types vs product flavors, build variants & source sets, build speed levers, and implementation vs api.",tags:["Gradle","Build Types","Flavors","Variants","Build Speed"],content:$C,qa:XC},eT=[{heading:"App signing — why every app is signed",blocks:[{t:"p",text:"**Every Android app must be cryptographically signed** to be installed. Signing serves two purposes: it *identifies* the app's author (proving updates come from the same developer), and it lets Android *verify integrity* (the app wasn't tampered with since signing). The critical rule: **an app update must be signed with the *same key* as the installed version** — Android rejects an update signed with a different key. So your signing key is precious: lose it, and you can't update your app (you'd have to publish a new app with a new package name)."},{t:"list",items:["**Debug signing** — during development, Android Studio auto-signs with a debug keystore (a shared, insecure key). Fine for testing, never for release.","**Release signing** — for publishing, you sign with *your own* keystore containing a private key you generate and guard. This is the identity of your app forever.","**The keystore** — a file (`.jks`/`.keystore`) holding your key(s), protected by passwords. It (and its passwords) must be kept secure and backed up — losing it is catastrophic (you can never update the app)."]}]},{heading:"Configuring release signing",blocks:[{t:"code",title:"Signing config (keep credentials OUT of source)",code:`android {
    signingConfigs {
        create("release") {
            // Load from environment/properties — NEVER hardcode in the build file
            storeFile = file(System.getenv("KEYSTORE_PATH") ?: "release.keystore")
            storePassword = System.getenv("KEYSTORE_PASSWORD")
            keyAlias = System.getenv("KEY_ALIAS")
            keyPassword = System.getenv("KEY_PASSWORD")
        }
    }
    buildTypes {
        release { signingConfig = signingConfigs.getByName("release") }
    }
}`},{t:"list",items:["**Never commit keystore credentials** to version control — load them from environment variables, a `keystore.properties` file that's git-ignored, or CI secrets. Hardcoding passwords in `build.gradle` (which is committed) leaks them.","**Signing schemes** — Android has evolved v1 (JAR signing), v2, v3 (key rotation support), and v4 signature schemes; modern builds use v2+ for faster verification and better security. AGP handles this.","**With Play App Signing** (covered separately), Play holds your *app signing key* and you use an *upload key* — so even if the upload key is compromised, you can reset it, and the real signing key stays safe with Google."]}]},{heading:"R8 / ProGuard — shrinking, obfuscation, keep rules",blocks:[{t:"p",text:"**R8** (the modern replacement for ProGuard) runs on release builds to shrink, obfuscate, and optimize code (covered in the Performance topic). Here the focus is the *practical build concern*: **keep rules** and the **mapping file**, which are frequent sources of release-only bugs and crash-debugging pain."},{t:"list",items:["**Keep rules (`proguard-rules.pro`)** — R8 uses static analysis to find reachable code, so anything accessed *reflectively* (by name at runtime) can be wrongly removed or renamed: some serialization (data classes referenced by name), DI, JNI/native calls, or classes referenced only from the manifest/XML. You write `-keep` rules to preserve them.","**The classic bug** — the app works in debug (R8 off) but *crashes only in release* (R8 removed/renamed something reflective). Symptoms: `ClassNotFoundException`, `NoSuchMethodError`, serialization failing, or a library misbehaving. The fix is adding the right keep rules. Libraries ship their own; you add rules for *your* reflective code (and consumer rules for libraries you publish).","**Mapping file** — obfuscation renames everything to `a`, `b`, `c`, so *crash stack traces from release builds are unreadable* (they show obfuscated names). R8 produces a `mapping.txt`; you **upload it to Play** (or your crash reporter — Crashlytics) so it can *de-obfuscate* stack traces back to real names. Without the mapping file, production crash reports are useless. Keep the mapping file for every release (it's per-build).","**Testing** — always test the *release* build (or a minified staging build) before shipping, precisely to catch missing keep rules that only manifest with R8 on. Shipping a release you only tested in debug is how R8 crashes reach users."]}]},{heading:"Versioning",blocks:[{t:"p",text:"An app has two version identifiers: **`versionCode`** (an integer, for the *system/Play* to order versions) and **`versionName`** (a string, for *users* to read). They serve different audiences and must be managed correctly for updates."},{t:"code",title:"Version fields",code:`android {
    defaultConfig {
        versionCode = 42            // integer — MUST increase every Play release
        versionName = "2.3.1"       // string — shown to users (semantic versioning)
    }
}`},{t:"list",items:["**`versionCode`** — a positive integer that the system and Play use to determine which version is *newer*. It **must strictly increase** with every release you upload to Play — Play rejects an upload with a versionCode ≤ an existing one, and updates only apply if the new versionCode is higher. Users never see it. Common schemes: simple increment, or derived from the version name / a build number.","**`versionName`** — a human-readable string shown in the Play listing and app settings (e.g. `2.3.1`). Typically follows *semantic versioning* (major.minor.patch). It has *no* technical role in update ordering — that's `versionCode`'s job — so you could ship the same versionName with different versionCodes (though you normally bump both).","**The rule that matters**: always increase `versionCode` for every Play release. Forgetting to (or reusing one) is a common upload rejection. With split APKs from an AAB, Play manages per-APK version codes derived from your base versionCode."]},{t:"note",text:"Signing: every app is cryptographically signed — identifies the author + verifies integrity; updates MUST use the same key (lose it = can't update). Keep the keystore + passwords secure and out of source control (env vars/CI secrets). R8/ProGuard: keep rules preserve reflectively-accessed code (missing ones = release-only crashes); upload the mapping.txt so crash reports de-obfuscate. Test the release build. Versioning: versionCode (integer, must strictly increase per Play release, for ordering) vs versionName (string, shown to users, semantic versioning)."}]}],tT=[{level:"junior",q:"Why must Android apps be signed?",a:[{t:"p",text:"**Every Android app must be cryptographically signed to be installed, for two reasons: signing *identifies the author* (proving updates come from the same developer) and lets Android *verify integrity* (the app wasn't tampered with since it was signed).** The system uses the signature to establish trust in who published the app and that it hasn't been modified."},{t:"list",items:["**Author identity** — the signature ties the app to a developer's key. This is how Android knows an update genuinely comes from the same source as the installed app.","**Integrity** — the signature verifies the app's contents haven't been altered (by malware, a repackaging attack) since signing.","**The critical rule** — an *update must be signed with the same key* as the installed version. Android rejects an update signed with a different key."]},{t:"p",text:"The practical consequence of that rule is that your signing key is *precious and permanent*: if you lose it, you can never update your app again (a different key would be rejected as an update) — you'd have to publish a *new* app with a new package name and lose your existing users' upgrade path. So the keystore holding your key, and its passwords, must be securely stored and backed up. During development, Android Studio auto-signs with a throwaway debug key (fine for testing), but for release you sign with your own carefully-guarded key. This is also why Play App Signing exists — to let Google securely hold your app signing key so you can't lose it."}]},{level:"junior",q:"What is the difference between versionCode and versionName?",a:[{t:"p",text:"**`versionCode` is an integer used by the *system and Play* to determine which version is newer; `versionName` is a string shown to *users*.** They serve different audiences and roles."},{t:"list",items:["**`versionCode`** — a positive integer (e.g. `42`) that Android and Play use to order versions. It **must strictly increase** with every release you upload — Play rejects an upload whose versionCode is ≤ an existing one, and an update only applies if the new versionCode is higher. Users never see it. It's the *technical* version identifier.","**`versionName`** — a human-readable string (e.g. `2.3.1`) shown in the Play listing and app settings, typically following semantic versioning (major.minor.patch). It has *no* technical role in update ordering — that's entirely `versionCode`'s job. It's the *user-facing* version."]},{t:"p",text:"The rule that matters in practice: **always increase `versionCode` for every Play release** — forgetting to (or reusing one) causes an upload rejection, and it's what determines whether users get the update. `versionName` you bump for meaningful releases per your versioning scheme, but technically you could ship the same versionName with different versionCodes. A common mistake is bumping only versionName and forgetting versionCode, which Play rejects. So: versionCode = the integer the system uses to know 'this is newer' (must go up); versionName = the string users read."}]},{level:"junior",q:"What is a keystore and how do you keep signing credentials secure?",a:[{t:"p",text:"**A keystore is a file (`.jks`/`.keystore`) that holds your app's signing key(s), protected by passwords. It's the cryptographic identity of your app — you use it to sign release builds.** Because an app update must be signed with the same key, the keystore is effectively permanent and irreplaceable: lose it and you can't update your app."},{t:"list",items:["**Never commit credentials to version control** — the keystore file, and especially its passwords, must *not* be hardcoded in `build.gradle` (which is committed) or checked into git. Doing so leaks your signing identity to anyone with repo access.","**Load credentials from outside source control** — from environment variables, a git-ignored `keystore.properties` file, or (for CI) encrypted secrets. The build script reads them at build time without them being in the committed code.","**Back up the keystore securely** — since losing it means never being able to update the app, keep secure backups (encrypted, in a safe place). This is a genuine business risk."]},{t:"p",text:"The modern best practice that mitigates the risk is **Play App Signing**: instead of holding the real app signing key yourself, you let Google Play hold it, and you sign your *uploads* with a separate *upload key*. If the upload key is ever compromised, you can reset it with Google's help — but the actual app signing key stays safely with Google, so you can't lose it and updates always work. This is why Play App Signing is required for AAB and strongly recommended: it removes the single-point-of-failure risk of you losing your one irreplaceable key."}]},{level:"senior",q:"Why do R8 keep rules matter, and what's the classic bug they cause?",a:[{t:"p",text:"**R8 keep rules matter because R8 uses *static analysis* to determine which code is reachable and removes/renames the rest — but it *cannot see* code accessed *reflectively* (by name at runtime), so without keep rules, R8 wrongly removes or renames that code, causing crashes. The classic bug is 'works in debug, crashes only in release' — because R8 runs only on release builds.**"},{t:"list",items:["**Why it happens**: R8 traces reachability through *static* references (direct method calls, type usage). Code that's only referenced *by name at runtime* — via reflection — is invisible to this analysis, so R8 thinks it's unused and removes it, or renames it (obfuscation) so the reflective lookup by the original name fails. Common culprits: serialization libraries that look up data classes/fields by name, dependency injection using reflection, JNI/native code calling Java by name, classes referenced only from the manifest or XML layouts, and enums used reflectively.","**The classic symptom — release-only crashes**: the app works perfectly in *debug* (where R8 is off — code isn't shrunk or renamed) but *crashes in release* with `ClassNotFoundException`, `NoSuchMethodError`, serialization failures, or a library silently misbehaving. Because the difference is R8, and R8 only runs on release, the bug is invisible during normal debug development — it appears only when you build/test a release, or worse, only in production if you didn't test the release build.","**The fix — `-keep` rules**: you add rules in `proguard-rules.pro` telling R8 to *preserve* (not remove, not rename) the reflectively-accessed classes/members. Well-behaved libraries *ship their own* keep rules (consumer rules bundled in the library), so their reflective needs are handled automatically — but you must add rules for *your own* reflective code (e.g. keeping your serializable model classes if using a reflection-based serializer, though kotlinx.serialization avoids this by being compile-time)."]},{t:"list",items:["**Prevention — test the release build**: the reliable way to catch missing keep rules is to *build and test a minified (release or staging) build* before shipping, not just debug. Many teams have a minified `staging` build type and run QA/automated tests against it. Shipping a release you only tested in debug is exactly how R8 crashes reach users.","**Reduce reliance on reflection**: using compile-time tools (kotlinx.serialization instead of reflection-based Gson, KSP-based DI) minimizes the reflective code R8 can't see, reducing the keep-rule surface and the risk entirely."]},{t:"p",text:"**The senior framing**: the root cause is a *mismatch between R8's static analysis and runtime reflection* — R8 optimizes based on what it can *see* statically, and reflection is invisible to it, so reflectively-used code gets removed/renamed unless explicitly kept. This produces the insidious 'debug works, release crashes' bug precisely because R8 only runs on release, so the failure mode is hidden during ordinary development. The defenses are: keep rules for necessary reflection, *testing the release/minified build* (the discipline that catches it), and preferring compile-time libraries that avoid reflection. Knowing *why* it happens (static analysis vs runtime reflection), the *signature symptom* (release-only crashes), and the *prevention* (keep rules + test the minified build + avoid reflection) is exactly the practical R8 competence these questions probe — it's a bug that has burned nearly every Android developer."}]},{level:"senior",q:"Why is the R8 mapping file important, and what happens without it?",a:[{t:"p",text:"**The mapping file (`mapping.txt`) records how R8 renamed every class, method, and field during obfuscation (original name → obfuscated name). It's important because obfuscation makes *release crash stack traces unreadable* — they show meaningless names like `a.b.c()` instead of real names — and the mapping file is what *de-obfuscates* them back to the original names. Without it, production crash reports are essentially useless.**"},{t:"list",items:["**What obfuscation does to crashes**: R8 renames `com.myapp.PaymentProcessor.processPayment()` to something like `a.b.a()`. When that code crashes in production, the stack trace reported from users' devices shows the *obfuscated* names — `at a.b.a(Unknown Source)` — which tells you nothing about *where* or *what* actually crashed. You can't debug from unreadable stack traces.","**What the mapping file does**: it's the dictionary mapping obfuscated names back to originals. A de-obfuscation tool (or Play / your crash reporter) uses it to translate the obfuscated stack trace back into `at com.myapp.PaymentProcessor.processPayment()`, making the crash report readable and debuggable.","**Where it goes**: you **upload the mapping file to Play** (Play Console associates it with the release and de-obfuscates crash reports in Android Vitals) *and/or* to your crash-reporting SDK (Crashlytics uploads it automatically via a Gradle plugin, so Crashlytics de-obfuscates traces). Either way, the mapping must reach wherever you read crash reports."]},{t:"list",items:["**It's per-build — you must keep every release's mapping**: R8 renames things *differently* on each build (a change in code can shift the obfuscated names), so *each release has its own mapping file*, and it only de-obfuscates crashes from *that specific build*. So you must retain (or upload) the mapping file for *every* release you ship — if a crash comes from version 2.3.1 and you've lost 2.3.1's mapping, you can't de-obfuscate it. CI pipelines archive/upload the mapping per release automatically.","**The consequence of not having it**: production crashes from real users become undebuggable — you see that *something* crashed and how often, but not *what*, so you can't fix it. For an obfuscated release app, the mapping file is the difference between actionable crash reports and useless ones. This makes 'upload the mapping file for every release' a non-negotiable part of the release process."]},{t:"p",text:"**The senior framing**: obfuscation is a *tradeoff* — it shrinks the app and lightly protects the code, but it *destroys* the readability of stack traces, and the mapping file is what recovers that readability for *your* debugging while keeping the shipped code obfuscated. So the mapping file is the essential companion to R8 obfuscation: without it, you get the size/security benefits but lose the ability to diagnose production crashes, which is unacceptable. The operational requirements — upload it to Play and/or your crash reporter, and *retain it per release* (since each build's obfuscation is unique) — must be baked into the release/CI process, because a crash report you can't de-obfuscate is a bug you can't fix. Understanding that obfuscation and the mapping file are two halves of one mechanism (obscure the release, but keep the key to read its crashes), and that the mapping is per-build and must be preserved for every version, is the complete answer."}]},{level:"junior",q:"What is R8, and what does it do?",a:[{t:"p",text:"R8 is Android's default *code shrinker, optimizer, and obfuscator* (it replaced ProGuard). In release builds it: *shrinks* (tree-shakes unused classes/methods/fields), *optimizes* (inlining, dead-code removal, simplification), *obfuscates* (renames to short names, making reverse-engineering harder), and *dexes* (produces the final dex). The result is a smaller, faster-to-load, harder-to-reverse app. It's driven by `keep` rules that protect code R8 can't see is used (reflection, JNI, entry points)."},{t:"list",items:["**Shrink** — remove unused code (tree-shaking).","**Optimize** — inline, dead-code elimination.","**Obfuscate** — rename to short names.","**Driven by** — keep rules protecting reflectively-used code."]},{t:"note",text:"R8 (replaced ProGuard) is Android's release shrinker/optimizer/obfuscator: shrinks unused code (tree-shaking), optimizes (inlining, dead-code removal), obfuscates (renames), and dexes — smaller, faster-loading, harder-to-reverse app. Keep rules protect code R8 can't see is used (reflection, JNI, entry points)."}]},{level:"junior",q:"What is the difference between shrinking, optimization, and obfuscation?",a:[{t:"p",text:"*Shrinking* removes code/resources that are never used (smaller app). *Optimization* rewrites the remaining code to be faster/smaller (inlining, removing dead branches, merging). *Obfuscation* renames classes/methods to meaningless short names — it doesn't remove anything but makes the app harder to reverse-engineer (and slightly smaller). R8 does all three together in release. They're distinct goals: less code, faster code, and harder-to-read code."},{t:"table",headers:["Step","Does","Goal"],rows:[["Shrink","Remove unused code/resources","Smaller app"],["Optimize","Rewrite code (inline, dead-code)","Faster/smaller"],["Obfuscate","Rename to short names","Harder to reverse"]]},{t:"note",text:"Shrinking removes unused code/resources (smaller); optimization rewrites remaining code faster/smaller (inline, dead-code); obfuscation renames to meaningless names (harder to reverse, slightly smaller). R8 does all three in release. Distinct goals: less code, faster code, harder-to-read code."}]},{level:"senior",q:"How do you write and scope keep rules correctly?",a:[{t:"p",text:"Add `-keep` rules only for code R8 *can't see* is used: classes accessed by *reflection* (some DI, serialization), *JNI/native* entry points, classes referenced by name from the manifest/XML, and public library APIs. Scope them *narrowly* — keep specific classes/members, not whole packages (`-keep class **` bloats the app and defeats shrinking/obfuscation). Prefer annotations (`@Keep`) for individual members. Test the *release* build thoroughly — missing keeps cause `ClassNotFoundException`/`NoSuchMethodError` only in release."},{t:"list",items:["**Keep only** — reflection/JNI/manifest-referenced/library-API code.","**Scope narrowly** — specific classes/members, not whole packages.","**`@Keep`** — for individual members.","**Test release** — missing keeps crash only there."]},{t:"note",text:"Write keep rules only for code R8 can't see is used (reflection, JNI, manifest/XML-referenced, public library APIs). Scope narrowly (specific classes/members, not -keep class ** which bloats and defeats shrinking); prefer @Keep for members. Test the release build — missing keeps cause ClassNotFoundException/NoSuchMethodError only in release."}]},{level:"senior",q:"What is the difference between APK Signature Scheme v1, v2, v3, and v4?",a:[{t:"p",text:"*v1* (JAR signing) signs individual files — slow to verify, and doesn't protect the whole archive. *v2* (API 24+) signs the *entire APK* as a blob (faster, tamper-evident, protects everything). *v3* (API 28+) adds *key rotation* support (change signing key while trusting the old one). *v4* (API 30+) enables *incremental delivery* (fast ADB install / Play streaming) via a separate signature file. Modern signing uses v2+v3 (and v4 where applicable); enable all your min-SDK supports."},{t:"table",headers:["Scheme","Since","Adds"],rows:[["v1 (JAR)","Always","Per-file signing (slow)"],["v2","API 24","Whole-APK signing (fast, tamper-evident)"],["v3","API 28","Key rotation"],["v4","API 30","Incremental delivery"]]},{t:"note",text:"v1 (JAR): per-file signing, slow, partial protection. v2 (API 24): whole-APK blob signing — fast, tamper-evident. v3 (API 28): key rotation. v4 (API 30): incremental delivery (fast ADB/Play streaming). Use v2+v3 (and v4 where applicable); enable all schemes your minSdk supports."}]},{level:"junior",q:"What is a signing config, and how do you set one up in Gradle?",a:[{t:"p",text:"A `signingConfig` tells Gradle *which keystore/key* to sign a build type with. You define it in `android { signingConfigs {} }` (store file, store password, key alias, key password) and reference it from a build type (`release { signingConfig = ... }`). Keep the *credentials out of source* (read from `local.properties`/env). With *Play App Signing*, you sign the *upload* artifact with your upload key and Play re-signs with the app key — so your local signing config uses the *upload* key."},{t:"code",title:"Signing config (credentials externalized)",code:`signingConfigs {
  create("release") {
    storeFile = file(providers.gradleProperty("STORE_FILE").get())
    storePassword = providers.gradleProperty("STORE_PASSWORD").get()
    keyAlias = providers.gradleProperty("KEY_ALIAS").get()
    keyPassword = providers.gradleProperty("KEY_PASSWORD").get()
  }
}
buildTypes { release { signingConfig = signingConfigs.getByName("release") } }`},{t:"note",text:"A signingConfig specifies the keystore/key to sign a build type (store file/password, key alias/password) in android { signingConfigs {} }, referenced from a build type. Keep credentials out of source (local.properties/env). With Play App Signing, your local config uses the upload key; Play re-signs with the app key."}]},{level:"senior",q:"What happens if you lose your keystore (without Play App Signing)?",a:[{t:"p",text:"Without Play App Signing, the app-signing key *is* your keystore — losing it (or its password) means you *cannot update the app*: Play rejects any upload not signed with the original key, and users can't update (a differently-signed APK is treated as a different app requiring uninstall/reinstall). There's no recovery. This is exactly why *Play App Signing* is strongly recommended: Google securely holds the app key, and if you lose your *upload* key you can *reset* it — the app key is safe."},{t:"list",items:["**Key = keystore** — losing it blocks all future updates.","**Play rejects** — uploads not signed with the original key.","**No recovery** — users must uninstall/reinstall a re-signed app.","**Play App Signing** — Google holds the app key; upload key is resettable."]},{t:"note",text:"Without Play App Signing, losing the app-signing keystore/password means you can never update the app — Play rejects differently-signed uploads and users can't update (a re-signed APK is a different app). No recovery. This is why Play App Signing is recommended: Google holds the app key; a lost upload key is resettable."}]},{level:"senior",q:"What is key rotation, and how does Play App Signing support it?",a:[{t:"p",text:"*Key rotation* (APK Signature Scheme v3) lets you *change your signing key* while the platform still trusts apps signed by the old key (via a signed *proof of rotation*), so updates keep working. With Play App Signing you can *upgrade* the app-signing key (e.g. to a stronger key) and Play manages the rotation for compatible devices. Rotation is useful if a key is weak or potentially compromised; older devices may still rely on the original key."},{t:"list",items:["**Rotation (v3)** — change key while trusting the old one.","**Proof of rotation** — links old → new key.","**Play App Signing** — can upgrade/manage the app key.","**Use** — weak/compromised key; older devices keep the original."]},{t:"note",text:"Key rotation (Signature Scheme v3) changes the signing key while the platform still trusts the old key via a signed proof of rotation, so updates keep working. Play App Signing can upgrade/manage the app key. Useful for a weak/compromised key; older devices may still rely on the original."}]},{level:"junior",q:"What is the difference between the upload key and the app signing key?",a:[{t:"p",text:"With *Play App Signing*, there are two keys: the *upload key* signs the AAB you *upload* to Play (Play verifies it's really you), and the *app signing key* is what Play uses to *re-sign* the APKs delivered to users (the key devices actually trust). Google securely stores the app signing key. If you lose the *upload* key, you can request a reset (the app key is unaffected). Separating them means your most critical key never leaves Google."},{t:"list",items:["**Upload key** — signs your AAB upload; Play verifies it.","**App signing key** — Play re-signs delivered APKs; devices trust it.","**Google stores** — the app signing key securely.","**Lost upload key** — resettable; app key safe."]},{t:"note",text:"Play App Signing uses two keys: the upload key signs the AAB you upload (Play verifies it's you), and the app signing key is what Play uses to re-sign delivered APKs (what devices trust — Google stores it). Lose the upload key → request a reset; the app key is unaffected. Your critical key never leaves Google."}]},{level:"senior",q:"How does versionCode interact with Play's update model?",a:[{t:"p",text:"`versionCode` is an integer Play uses to order releases — a new upload must have a *higher* versionCode than any already on that track, and users are offered an update only to a *higher* code. You can't reuse or lower a versionCode. With multiple APKs/splits, each must have a distinct code. Common practice: derive versionCode from CI build number or a monotonic scheme so it always increases. `versionName` is the human string and doesn't affect update ordering."},{t:"list",items:["**Higher wins** — new upload must exceed existing codes.","**Users update** — only to a higher versionCode.","**No reuse/lower** — strictly increasing.","**Practice** — derive from CI/monotonic scheme."]},{t:"note",text:"versionCode is the integer Play uses to order releases — a new upload must exceed existing codes on that track, and users update only to a higher code (no reuse/lowering). Derive it from a monotonic/CI scheme so it always increases. versionName is the human string and doesn't affect update ordering."}]},{level:"senior",q:"How do you debug a release-only crash caused by R8?",a:[{t:"p",text:"R8-related crashes (missing keep rules) appear only in release: `ClassNotFoundException`, `NoSuchMethodError`, or a library failing to find a class by reflection. Steps: *deobfuscate the stack trace* with the `mapping.txt` (via the Play Console or `retrace`), identify the removed/renamed class, and add a *narrow* keep rule for it. To confirm R8 is the cause, temporarily set `minifyEnabled false` — if the crash disappears, it's a shrinking/obfuscation issue. Then fix with a scoped keep, not by disabling R8."},{t:"list",items:["**Symptom** — ClassNotFound/NoSuchMethod only in release.","**Deobfuscate** — with `mapping.txt` (retrace/Play).","**Confirm** — `minifyEnabled false` makes it vanish.","**Fix** — a narrow keep rule, not disabling R8."]},{t:"note",text:"R8 release-only crashes (missing keeps): ClassNotFoundException/NoSuchMethodError/reflection failures. Deobfuscate the trace with mapping.txt (retrace/Play), find the removed/renamed class, add a narrow keep. Confirm by temporarily setting minifyEnabled false (crash vanishes → shrinking issue). Fix with a scoped keep, not by disabling R8."}]},{level:"junior",q:"What is the mapping.txt file, and why must you retain it per release?",a:[{t:"p",text:"When R8 obfuscates, it produces `mapping.txt` — the map from *obfuscated names back to original*. Without it, production stack traces are unreadable (obfuscated names). You *upload it to Play (or Crashlytics) per release* so crash reports are *deobfuscated* automatically. Each release has a *unique* mapping (names differ per build), so you must retain the exact mapping for every version you ship. Losing it means you can't decode that version's crashes."},{t:"list",items:["**`mapping.txt`** — obfuscated → original names.","**Upload per release** — Play/Crashlytics deobfuscate traces.","**Unique per build** — retain each version's mapping.","**Lost** — that version's crashes are undecodable."]},{t:"note",text:"R8 produces mapping.txt (obfuscated → original names). Upload it to Play/Crashlytics per release so production stack traces are deobfuscated automatically. Each build has a unique mapping — retain the exact one for every shipped version; losing it makes that version's crashes undecodable."}]},{level:"senior",q:"How do libraries provide their own R8/ProGuard rules (consumer rules)?",a:[{t:"p",text:"Libraries ship *consumer ProGuard rules* (`consumerProguardFiles`) bundled in their AAR — these rules are *automatically applied* to any app that depends on them, so the app doesn't need to know the library's internal keep requirements. That's why most well-behaved libraries 'just work' with R8. If a library misbehaves under R8, check whether it ships consumer rules (or you must add keeps yourself). You add your app's own rules in `proguard-rules.pro`."},{t:"list",items:["**Consumer rules** — shipped in the library's AAR.","**Auto-applied** — to dependent apps.","**Why libraries 'just work'** — under R8.","**App rules** — your own `proguard-rules.pro`."]},{t:"note",text:"Libraries ship consumer ProGuard rules (consumerProguardFiles) in their AAR, automatically applied to dependent apps — so apps don't need to know a library's internal keep requirements (why most libs 'just work' with R8). If a library breaks under R8, check for missing consumer rules or add keeps yourself. Your app's rules go in proguard-rules.pro."}]},{level:"junior",q:"Why should you not disable R8 to fix a crash?",a:[{t:"p",text:"Disabling R8 (`minifyEnabled false`) makes a keep-rule crash disappear, but it's the *wrong fix*: you lose shrinking (bigger app), optimization (slower/larger), and obfuscation (easier to reverse-engineer). The right fix is a *narrow keep rule* for the specific class R8 wrongly removed. Disabling R8 for release also diverges your production build from best practice. Use `minifyEnabled false` only *temporarily* to *diagnose* that R8 is the cause, then re-enable and add the keep."},{t:"list",items:["**Disabling** — loses shrinking/optimization/obfuscation.","**Right fix** — a narrow keep rule.","**Diagnostic only** — toggle off to confirm R8 is the cause.","**Re-enable** — then add the scoped keep."]},{t:"note",text:"Disabling R8 to kill a crash loses shrinking (bigger app), optimization (slower/larger), and obfuscation (easier to reverse). The right fix is a narrow keep rule for the wrongly-removed class. Use minifyEnabled false only temporarily to diagnose that R8 is the cause, then re-enable and add the scoped keep."}]},{level:"senior",q:"What is full mode versus compatibility mode in R8?",a:[{t:"p",text:"R8 has a more aggressive *full mode* (default in recent AGP) that optimizes harder than the ProGuard-*compatibility* mode (which mimics ProGuard's more conservative behavior for easier migration). Full mode can shrink/optimize more but may need *additional or more precise keep rules* (it makes fewer conservative assumptions). If migrating from ProGuard-compat and hitting issues, you may need to tighten rules. Full mode is recommended for the best results once your rules are correct."},{t:"list",items:["**Full mode** — aggressive; default in recent AGP.","**Compatibility mode** — mimics ProGuard (conservative).","**Full mode** — better results but needs precise keeps.","**Migrating** — may require tightening rules."]},{t:"note",text:"R8 full mode (recent-AGP default) optimizes more aggressively than ProGuard-compatibility mode (which mimics ProGuard's conservative behavior for easy migration). Full mode shrinks/optimizes harder but makes fewer conservative assumptions — may need additional/precise keep rules. Prefer full mode for best results once rules are correct."}]},{level:"junior",q:"How do you verify an APK's signature and which schemes it uses?",a:[{t:"p",text:"Use `apksigner verify --verbose app.apk` (from the build-tools) — it reports whether the APK is verified and *which schemes* (v1/v2/v3/v4) it's signed with, plus the signer certificate. `apksigner` can also *sign* an APK. For an AAB you inspect via bundletool. Verifying schemes matters: an APK signed only with v1 is slow/weaker; you want v2+ for modern devices. Check the certificate to confirm it's your key."},{t:"code",title:"Verify signing",code:`apksigner verify --verbose --print-certs app.apk
# prints: Verified using v1/v2/v3 scheme: true/false, signer certificate details`},{t:"note",text:"apksigner verify --verbose app.apk reports whether the APK is verified, which schemes (v1/v2/v3/v4) it's signed with, and the signer certificate; apksigner can also sign. Inspect AABs via bundletool. Verify you have v2+ (not v1-only) and that the certificate is your key."}]},{level:"senior",q:"What are the security benefits of obfuscation, and its limits?",a:[{t:"p",text:"Obfuscation (renaming) makes decompiled code *harder to read* — raising the effort to reverse-engineer logic, find keys, or tamper. But it's *not encryption*: a determined attacker can still decompile and analyze, and *strings/resources aren't obfuscated*. So don't rely on it to hide secrets (they're extractable) or enforce security. Treat it as *defense-in-depth* — combine with server-side validation, no embedded secrets, integrity checks (Play Integrity), and encryption for real protection."},{t:"list",items:["**Benefit** — harder to read/reverse; raises attacker effort.","**Not encryption** — code is still decompilable.","**Strings/resources** — not obfuscated.","**Defense-in-depth** — + server validation, Play Integrity, no embedded secrets."]},{t:"note",text:"Obfuscation makes decompiled code harder to read (raises reverse-engineering effort) but isn't encryption — code is still decompilable and strings/resources aren't obfuscated. Don't rely on it to hide secrets (extractable). Treat as defense-in-depth: combine with server-side validation, Play Integrity, no embedded secrets, and encryption."}]},{level:"senior",q:"How does R8 handle resource shrinking, and what is the strict vs safe mode?",a:[{t:"p",text:"Resource shrinking (`shrinkResources true`) removes resources unreferenced by the *kept code*. It has a *safe* (default) mode that keeps resources referenced *dynamically* (e.g. `Resources.getIdentifier`) to avoid breaking them, and a *strict* mode that removes even those unless explicitly kept — smaller but riskier. Protect dynamically-referenced resources with a `keep.xml` (`tools:keep`/`tools:discard`). Verify the release build actually shows the intended resources (missing images/strings signal over-shrinking)."},{t:"list",items:["**Removes** — resources unreferenced by kept code.","**Safe mode** — keeps dynamically-referenced resources.","**Strict mode** — removes those too unless kept (smaller, riskier).","**Protect** — `keep.xml` (`tools:keep`/`tools:discard`)."]},{t:"note",text:"Resource shrinking removes resources unreferenced by kept code. Safe mode (default) keeps dynamically-referenced ones (getIdentifier); strict mode removes those too unless kept (smaller, riskier). Protect dynamic resources with keep.xml (tools:keep/tools:discard). Verify the release build shows intended resources — missing images/strings mean over-shrinking."}]},{level:"junior",q:"What is zipalign, and does it still matter?",a:[{t:"p",text:"`zipalign` aligns uncompressed data in the APK on 4-byte (or, for native libs, page) boundaries so the OS can *mmap* them directly, reducing runtime memory. It's a required release step — but with modern tooling and Play App Signing / the App Bundle, *Play/Gradle handle alignment automatically* (the AGP and `apksigner` pipeline aligns). You rarely run `zipalign` manually now; just know it's part of producing a correctly-packaged release APK."},{t:"list",items:["**Aligns** — uncompressed data on boundaries for mmap.","**Benefit** — lower runtime memory.","**Automatic** — AGP/apksigner/Play handle it.","**Rarely manual** — but part of a correct release."]},{t:"note",text:"zipalign aligns uncompressed APK data on 4-byte/page boundaries so the OS can mmap directly (lower runtime memory) — a required release step, but modern AGP/apksigner/Play handle it automatically. You rarely run it manually now; just know it's part of a correctly-packaged release."}]},{level:"senior",q:"How do you keep model classes used by reflection (Gson/serialization) from being stripped?",a:[{t:"p",text:"Reflection-based serializers (Gson, older Moshi reflective mode) access model fields *by name at runtime* — R8, seeing no direct references, may *rename or remove* fields, breaking (de)serialization silently (nulls) or with errors. Fix: keep the model classes/fields (`-keep class com.app.model.** { *; }` scoped to models, or `@Keep`). Better: use *codegen* serializers (kotlinx.serialization, Moshi codegen) that generate direct code R8 can see — no keep rules needed and faster."},{t:"code",title:"Keeping reflective models (or avoid via codegen)",code:`# scoped keep for reflectively-serialized models
-keep class com.app.data.model.** { *; }
# BETTER: kotlinx.serialization / Moshi codegen needs no keep rules`},{t:"note",text:"Reflection serializers (Gson, reflective Moshi) access fields by name — R8 may rename/remove them, breaking (de)serialization (silent nulls/errors). Fix: scoped keep of the model classes/fields or @Keep. Better: codegen serializers (kotlinx.serialization, Moshi codegen) generate direct code R8 sees — no keep rules, faster."}]},{level:"junior",q:"What is the difference between debug signing and release signing?",a:[{t:"p",text:"*Debug* builds are auto-signed with a *debug keystore* (`~/.android/debug.keystore`) generated by the SDK — shared, insecure, only for development/testing (not accepted by Play). *Release* builds must be signed with *your own release key* (a keystore you create and guard), which establishes app identity for Play and updates. Never publish a debug-signed build. The debug key is convenient for local runs; the release key is a guarded, long-lived secret."},{t:"list",items:["**Debug** — auto debug keystore; dev only, not for Play.","**Release** — your own guarded release key.","**Release key** — app identity + update chain.","**Never** — publish a debug-signed build."]},{t:"note",text:"Debug builds are auto-signed with the shared debug keystore (~/.android/debug.keystore) — dev/testing only, not accepted by Play. Release builds use your own guarded release key (app identity + update chain). Never publish a debug-signed build. Debug key = convenience; release key = a guarded long-lived secret."}]},{level:"senior",q:"How do you validate that shrinking didn't break anything before release?",a:[{t:"p",text:"Always test the *actual release (minified) build*, not just debug. Run your full test suite against a release build (or a `minifyEnabled true` benchmark variant), do a manual QA pass over reflection-heavy paths (serialization, DI, navigation, deep links), and use the *Play pre-launch report* to catch crashes on real devices. Add *instrumentation tests* that exercise serialization/reflection. Missing keeps only manifest in minified builds, so debug passing tells you nothing about R8 correctness."},{t:"list",items:["**Test the minified build** — not just debug.","**QA reflection paths** — serialization, DI, deep links.","**Pre-launch report** — real-device crash detection.","**Debug passing** — says nothing about R8 correctness."]},{t:"note",text:"Validate shrinking by testing the actual release (minified) build: run the test suite against it, QA reflection-heavy paths (serialization, DI, navigation, deep links), and use the Play pre-launch report for real-device crashes. Missing keeps only show in minified builds — debug passing tells you nothing about R8 correctness."}]},{level:"senior",q:"What is Play Integrity API, and how does it relate to app protection?",a:[{t:"p",text:"The *Play Integrity API* lets your app/server verify that a request comes from a *genuine, unmodified app binary* installed from Play, running on a *genuine Android device* — helping detect tampered APKs, emulators, or abusive clients. It returns a signed verdict your *server* validates before trusting sensitive actions. It complements obfuscation/signing (which don't stop a determined attacker) by moving trust decisions server-side. Use it for anti-abuse, not as the only defense."},{t:"list",items:["**Verifies** — genuine unmodified app + genuine device + Play install.","**Signed verdict** — validated server-side.","**Detects** — tampered APKs, emulators, abuse.","**Complements** — signing/obfuscation; server-side trust."]},{t:"note",text:"The Play Integrity API verifies a request comes from a genuine, unmodified app (Play-installed) on a genuine device, returning a signed verdict your server validates before trusting sensitive actions — detecting tampered APKs, emulators, abuse. It complements signing/obfuscation by moving trust server-side. Use for anti-abuse, not as the sole defense."}]},{level:"junior",q:"How do you create a release keystore and keep it safe?",a:[{t:"p",text:"Create one with `keytool -genkeypair` (or Android Studio's *Generate Signed Bundle* wizard), choosing a *long validity* (25+ years) and strong passwords. Then guard it: *back it up securely* (it's irreplaceable without Play App Signing), *never commit it* to VCS, store passwords in a secret manager/CI secrets (not in `build.gradle`), and restrict access. Enroll in *Play App Signing* so a lost *upload* key is recoverable. Treat the keystore as a top-tier secret."},{t:"code",title:"Generate a keystore",code:`keytool -genkeypair -v -keystore release.jks \\
  -keyalg RSA -keysize 2048 -validity 10000 -alias upload
# then: back up securely, never commit, store passwords in secrets`},{t:"note",text:"Create a release keystore with keytool -genkeypair (or Studio's wizard) — long validity, strong passwords. Guard it: back up securely (irreplaceable without Play App Signing), never commit, store passwords in CI/secret manager not build.gradle, restrict access. Enroll in Play App Signing so a lost upload key is recoverable. Top-tier secret."}]},{level:"junior",q:"What is the difference between R8 and ProGuard?",a:[{t:"p",text:"*ProGuard* was the older, separate shrinker/optimizer/obfuscator. *R8* replaced it as Android's default — it does the same job (shrink/optimize/obfuscate) *plus dexing* in one integrated, faster pass, and generally optimizes better. Crucially, R8 *reuses ProGuard's rules syntax* (`proguard-rules.pro`, `-keep`), so existing rules carry over. Today you use R8; 'ProGuard rules' just refers to the rule file format R8 consumes. There's no reason to use standalone ProGuard on modern AGP."},{t:"list",items:["**ProGuard** — older, separate tool.","**R8** — default replacement; shrink+optimize+obfuscate+dex, faster.","**Same rule syntax** — `-keep` carries over.","**Today** — use R8; 'ProGuard rules' = the rule format."]},{t:"note",text:"ProGuard was the older separate shrinker/optimizer/obfuscator; R8 replaced it as the default — same job plus dexing in one faster, better-optimizing pass, reusing ProGuard's rule syntax (-keep, proguard-rules.pro). Use R8 today; 'ProGuard rules' just means the rule format R8 consumes. No reason for standalone ProGuard on modern AGP."}]},{level:"senior",q:"How do you reduce app size with shrinking beyond the defaults?",a:[{t:"p",text:"Beyond `minifyEnabled`/`shrinkResources`: remove unused *dependencies* and unused *resources/translations* (`resConfigs` to limit locales you actually ship), use *R8 full mode*, prefer *vectors/WebP*, split via the *App Bundle* (per-device delivery), avoid reflection-heavy libraries (they force broad keeps), and audit with the *APK Analyzer*. Ensure keep rules are *tight* (broad keeps prevent shrinking). Measure the result and iterate on the biggest contributors."},{t:"list",items:["**Remove** — unused deps, resources, `resConfigs` locales.","**R8 full mode + tight keeps** — maximize shrinking.","**App Bundle** — per-device delivery.","**Audit** — APK Analyzer for biggest contributors."]},{t:"note",text:"Beyond minify/shrinkResources: drop unused dependencies/resources, limit locales (resConfigs), use R8 full mode with tight keeps (broad keeps block shrinking), vectors/WebP, App Bundle per-device delivery, avoid reflection-heavy libs (force broad keeps). Audit with the APK Analyzer and iterate on the biggest contributors."}]}],aT={id:"signing-shrinking",title:"Signing, R8 & Versioning",tagline:"App signing & why the key is permanent, secure keystore handling, R8 keep rules & the release-only-crash bug, the mapping file, and versionCode vs versionName.",tags:["Signing","Keystore","R8","Mapping File","Versioning"],content:eT,qa:tT},iT=[{heading:"APK — the installable artifact",blocks:[{t:"p",text:"An **APK (Android Package)** is the file format that packages a complete app for installation on a device — it contains the compiled code (DEX), resources, assets, the manifest, and native libraries, all signed. It's what actually gets installed and run. Historically you built and published a single APK; today you still build APKs for development/testing, but you *publish* an App Bundle (from which Play generates APKs)."},{t:"list",items:["**A universal APK** contains *everything* for *every* device — all screen densities, all CPU architectures (arm64, armv7, x86), all languages — so any device can install it. The downside: every device downloads resources it can't use, making it larger than necessary.","**Split APKs** — the modern approach: instead of one universal APK, an app is delivered as a *base* APK plus *configuration* APKs (per density, per architecture, per language), so a device installs only the base plus the splits matching it. This is what Play generates from an App Bundle.","**You install/test APKs**; you publish an AAB. APKs remain the on-device runtime format."]}]},{heading:"AAB — the publishing format",blocks:[{t:"p",text:"The **Android App Bundle (`.aab`)** is the *publishing format* required for new apps on Play since 2021. You upload one AAB — a bundle of all your app's compiled code and resources for all configurations — and **Google Play generates optimized split APKs per device** from it, delivering only what each device needs. The AAB itself is *not installable*; it's an intermediate format Play processes."},{t:"list",items:["**Why it exists — smaller downloads**: a universal APK forces every device to download resources for densities/architectures/languages it doesn't use. The AAB lets Play split by those dimensions and deliver a tailored, smaller APK per device — often 15-35% smaller downloads, with no code changes.","**AAB vs APK, precisely**: an APK is the *installable* artifact a device runs; an AAB is a *publishing* format you upload (not directly installable). You develop and test with APKs; you publish an AAB.","**It enables more than size** — dynamic feature delivery (on-demand modules), asset packs (large assets delivered separately, for games), and conditional/instant delivery all build on the bundle format.","**Testing the real output** — since Play generates the device-specific APKs (not you), to test the *actual* delivered artifacts locally you use **`bundletool`** (generates device-specific APKs from your AAB) or Play's internal test track."]}]},{heading:"Play App Signing — why it's needed and how it works",blocks:[{t:"p",text:"Because Play *generates and delivers* the per-device APKs from your AAB, *Play* must sign those APKs — they didn't exist when you built the bundle. **Play App Signing** is the system where **Google holds your app signing key** and signs the generated APKs, while *you* sign your *uploads* with a separate **upload key**. This is required for AAB publishing and solves the 'lose your key, can't update' problem."},{t:"list",items:["**Two keys**: the **app signing key** (the real key that end-user APKs are signed with — Google holds it securely) and the **upload key** (which you use to sign the AAB you upload — Play verifies it, then re-signs the generated APKs with the app signing key). They're different keys serving different roles.","**The key benefit — recoverability**: if your *upload key* is ever lost or compromised, you can *reset* it with Google's help — you're not locked out. The real *app signing key* stays safely with Google, so it can't be lost, and updates always remain possible. This removes the catastrophic single-point-of-failure of holding your one irreplaceable key yourself.","**Enrollment options**: Play can *generate* the app signing key for you, or you can *upload* your existing key (opting into Play App Signing for an existing app). Once enrolled, Google manages the app signing key.","**Security posture**: Google's key management is more secure than most developers' (HSM-backed, access-controlled), and the upload-key indirection means a compromised upload key doesn't compromise the app's actual signature."]}]},{heading:"The full picture — how it fits together",blocks:[{t:"code",title:"The publishing pipeline",code:`// 1. You build an AAB, signed with your UPLOAD key
./gradlew bundleRelease            // produces app-release.aab

// 2. Upload the AAB to Play Console
//    Play verifies the upload key signature

// 3. Play generates device-specific split APKs from the AAB
//    and signs them with the APP SIGNING KEY (held by Google)

// 4. A device installs the base APK + config splits matching it
//    (its density, architecture, languages) — a small, tailored download`},{t:"list",items:["**Your responsibility**: build and upload an AAB signed with your upload key; keep the upload key reasonably safe (but it's resettable).","**Play's responsibility**: verify the upload, generate optimized split APKs, sign them with the app signing key it holds, and deliver the right splits to each device.","**The device's experience**: a smaller, tailored download (only its density/architecture/languages), assembled into the running app."]},{t:"note",text:"APK = installable artifact a device runs (universal = everything; splits = base + per-density/ABI/language). AAB = publishing format you upload (not installable); Play generates optimized split APKs from it → 15-35% smaller downloads. Required for new Play apps since 2021. Test the real output with bundletool. Play App Signing: Google holds the app signing key (signs the generated APKs), you use a resettable upload key — required for AAB, and it solves 'lose your key = can't update' since the real key can't be lost and the upload key is recoverable."}]}],nT=[{level:"junior",q:"What is an APK?",a:[{t:"p",text:"**An APK (Android Package) is the file that packages a complete app for installation on a device — it contains the compiled code (DEX), resources, assets, the manifest, and native libraries, all cryptographically signed. It's what actually gets installed and run on the device.** It's the on-device runtime format."},{t:"list",items:["A **universal APK** contains everything for every device — all screen densities, all CPU architectures, all languages — so any device can install it. The downside is size: every device downloads resources it can't use.","**Split APKs** are the modern approach — an app is delivered as a *base* APK plus *configuration* APKs (per density, per architecture, per language), so each device installs only the base plus the splits matching it. This is what Play generates from an App Bundle.","You still **build and test with APKs** during development, but for publishing you upload an App Bundle (AAB), from which Play generates the APKs."]},{t:"p",text:"So the APK is the fundamental installable/runnable unit — a device runs an APK. The evolution has been from shipping one universal APK (simple but wasteful) to Play generating device-tailored split APKs from an App Bundle (smaller downloads). Understanding that the APK is what's *installed* — while the AAB is what you *publish* — is the key distinction."}]},{level:"junior",q:"What is an Android App Bundle (AAB) and why did Google make it mandatory?",a:[{t:"p",text:"**An AAB (Android App Bundle) is a *publishing format* — you upload one AAB containing all your app's code and resources for all configurations, and Google Play generates optimized, device-specific split APKs from it, delivering only what each device needs. The AAB itself is not installable; it's an intermediate format Play processes.** Google made it mandatory for new apps (since 2021) primarily to reduce download sizes."},{t:"list",items:["**The problem it solves**: a universal APK forces every device to download resources for densities, CPU architectures, and languages it doesn't use. That's wasteful.","**How the AAB fixes it**: Play splits the bundle by density, architecture, and language, delivering a tailored, smaller APK per device — often 15-35% smaller downloads, with no code changes on your part.","**AAB vs APK**: the APK is the *installable* artifact a device runs; the AAB is a *publishing* format you upload (not directly installable). Develop/test with APKs, publish an AAB."]},{t:"p",text:"Beyond size, the AAB format also enables dynamic feature delivery (on-demand modules), asset packs (large assets delivered separately, useful for games), and conditional/instant delivery. Google made it mandatory because the download-size reduction benefits users (faster installs, less data/storage) at essentially no cost to developers, and it lets Play optimize delivery centrally. One practical note: since Play generates the actual delivered APKs (not you), testing the *real* output requires `bundletool` (to generate device-specific APKs from the AAB locally) or Play's internal test track — you can't just install the AAB directly."}]},{level:"junior",q:"What is Play App Signing?",a:[{t:"p",text:"**Play App Signing is the system where Google Play holds your app's real signing key and signs the APKs it delivers to users, while you sign your *uploads* with a separate *upload key*.** It's required for publishing App Bundles, because Play *generates* the device-specific APKs from your AAB and therefore must be the one to sign them."},{t:"list",items:["**Two keys**: the **app signing key** (the real key end-user APKs are signed with — Google holds it securely) and the **upload key** (which you use to sign the AAB you upload — Play verifies it, then re-signs the generated APKs with the app signing key).","**Why it's needed for AAB**: Play generates the split APKs *after* you upload the bundle (they didn't exist at your build time), so Play must sign them — which means Play needs to hold a signing key. That's the app signing key.","**The big benefit — recoverability**: if your *upload key* is lost or compromised, you can reset it with Google's help; you're not locked out. The real app signing key stays safely with Google, so it can't be lost and updates always remain possible."]},{t:"p",text:"This directly solves the catastrophic 'lose your key, can't update your app' risk of holding your single irreplaceable signing key yourself. With Play App Signing, the irreplaceable key is safe with Google (better secured than most developers can manage), and the key *you* handle (the upload key) is *replaceable*. When enrolling, Play can either generate the app signing key for you or let you upload your existing one. So Play App Signing both enables AAB delivery (Play can sign the generated APKs) and removes the single-point-of-failure of key loss."}]},{level:"senior",q:"Walk through what happens from building an AAB to a user installing the app, including signing.",a:[{t:"p",text:"**The pipeline separates *your* responsibility (build and upload a bundle signed with your upload key) from *Play's* (generate device-specific split APKs and sign them with the app signing key it holds), ending with the device downloading only the splits it needs. The signing has two distinct keys at two distinct stages.**"},{t:"list",items:["**1. You build the AAB, signed with the *upload key***: `./gradlew bundleRelease` produces an `.aab` containing all your app's code and resources for every configuration (all densities, architectures, languages). It's signed with your *upload key* — the key you hold and that authenticates the upload to Play. (R8 has already shrunk/obfuscated the code, and you've kept the mapping file.)","**2. You upload the AAB to Play Console**: Play *verifies the upload key signature* to confirm the bundle genuinely came from you. If the upload key doesn't match, Play rejects it. At this point the AAB is Play's to process.","**3. Play generates device-specific split APKs from the AAB**: Play's server-side build system carves the bundle into a *base* APK plus *configuration* APKs — split by density, ABI (architecture), and language. These are the actual installable artifacts, and they *did not exist* when you built the bundle — Play created them.","**4. Play signs the generated APKs with the *app signing key***: because Play generated these APKs, Play must sign them — using the *app signing key* it holds (a different key from your upload key). This is why Play App Signing is required for AAB: the entity that generates the APKs (Play) must sign them, so Play holds the real signing key.","**5. A device installs the matching splits**: when a specific device installs your app, Play delivers the base APK plus only the config splits matching *that device* (its screen density, its CPU architecture, its languages). The device assembles them into the running app — a small, tailored download excluding all the other densities/architectures/languages."]},{t:"list",items:["**The two-key separation is the crux**: the *upload key* (yours, resettable) authenticates *you* to Play at upload; the *app signing key* (Google's, safe) signs the *delivered APKs* to the user. They're decoupled so that (a) Play *can* sign the APKs it generates, and (b) if your upload key is compromised you reset it without affecting the app's actual signature or locking you out. From the user's device's perspective, the app is signed by the app signing key — consistent across all versions (satisfying the same-key-for-updates rule), managed by Google.","**Update consistency**: because Play always signs delivered APKs with the same app signing key, updates always verify correctly (same-key rule), even though you only ever handle the upload key. This is what makes key loss non-catastrophic — the update-critical key never leaves Google."]},{t:"p",text:"**The senior framing**: the pipeline works by *deferring both packaging and final signing to Play*, which has what you lack — the specific installing device (for splitting) and secure key custody (for signing). The elegance is the two-key indirection: you authenticate uploads with a *replaceable* upload key, while the *irreplaceable* app signing key that satisfies the same-key-update rule is held by Google, generated fresh per-device-APK at delivery. This simultaneously enables the App Bundle's per-device optimization (Play must generate and sign the splits, so it must hold a key) *and* removes the historical single-point-of-failure of developers guarding one irreplaceable key. Understanding *why* the two keys exist (Play generates the APKs so Play must sign them; the upload key is decoupled so it's resettable), *what each stage does* (build+upload-key → verify → generate splits → app-signing-key → deliver matching splits), and *how it preserves update consistency* (same app signing key always) is the comprehensive answer that ties together the AAB, split delivery, and Play App Signing into one coherent system."}]},{level:"senior",q:"How do you test the actual APKs that Play will deliver to users from your AAB?",a:[{t:"p",text:"**Because Play generates the device-specific split APKs from your AAB (you never build them directly), testing the *actual delivered artifacts* requires either `bundletool` (to generate device-specific APKs from your AAB locally) or Play's internal/closed testing tracks (which serve the real Play-generated APKs). You can't fully validate delivery by just installing a universal/debug APK, because that's not what users receive.**"},{t:"list",items:["**Why this is a real concern**: the split-APK delivery introduces things that a monolithic debug APK doesn't exercise — per-density resource loading, per-ABI native libraries, per-language resource splits, and (if used) dynamic feature module installation. A bug could exist in the *split* delivery (a resource in the wrong split, a missing language, a native lib not delivered for an architecture, a dynamic feature that fails to install) that never appears when you run a universal debug build. So testing the real output matters.","**`bundletool` — local testing of the real splits**: `bundletool` is the same tool Play uses to generate APKs from a bundle. You can run it locally to produce a set of APKs (`.apks`) from your AAB, then either extract the APKs for a *specific device configuration* (using `bundletool build-apks --connected-device` to build exactly what that device would get) and install them, or generate a *universal* APK from the bundle for broad testing. `bundletool install-apks` deploys the correct device-specific splits to a connected device — so you're testing the *actual* split composition that device would receive from Play.","**Play internal/closed testing tracks — the real thing**: uploading the AAB to Play's *internal testing* track (fast, for a small trusted group) or *closed testing* track and installing through Play delivers the *genuinely Play-generated and Play-signed* APKs to testers' devices — the exact artifacts production users would get, including the correct splits and app-signing-key signature. This is the highest-fidelity test because it exercises the entire real delivery pipeline (generation, signing, per-device splitting, even staged rollout mechanics)."]},{t:"list",items:["**A practical testing progression**: develop/debug with normal (universal) debug APKs for speed; use `bundletool` to validate the *release* AAB's device-specific splits locally on representative devices before uploading; then use Play *internal testing* to verify the real Play-delivered, Play-signed artifacts (and the release process itself) before promoting to closed/open/production. Each step increases fidelity to what users actually receive.","**Dynamic features especially need this**: if you use dynamic feature modules, on-demand installation *only* works through the Play delivery mechanism (or `bundletool`'s local simulation), so you *must* test via internal testing / bundletool — you can't validate on-demand module install with a plain debug build at all."]},{t:"p",text:"**The senior framing**: the App Bundle model means *you no longer build the final installable artifact* — Play does — so the thing users install is not the thing you compiled, and validating delivery requires reproducing or using Play's generation. `bundletool` lets you *locally* generate and install the exact device-specific splits (the same tool Play uses), and Play's *internal testing track* delivers the *genuinely* Play-generated-and-signed artifacts end-to-end — the highest fidelity. The insight is that split delivery introduces device-configuration-specific behavior (density/ABI/language splits, dynamic features, Play signing) that a universal debug build can't exercise, so 'it works when I install the debug APK' is *not* sufficient validation for an AAB-published app. Knowing to reach for `bundletool` (local, per-device splits) and Play internal testing (real end-to-end delivery) — and *why* (you don't build the final APKs, Play does) — is the practical release-engineering competence these questions probe."}]},{level:"junior",q:"What is the difference between an APK and an AAB?",a:[{t:"p",text:"An *APK* is the final installable package delivered to a device. An *AAB (Android App Bundle)* is a *publishing format* you upload to Play — it contains all your compiled code and resources for *every* configuration, but isn't installed directly. Play uses it to *generate optimized APKs per device* (right density, language, ABI) via *split APKs*, so users download only what they need (smaller). Since Aug 2021, new apps must publish as AAB. You test the generated APKs with bundletool."},{t:"table",headers:["","APK","AAB"],rows:[["Role","Installable package","Publishing format"],["Contains","One config","All configs"],["Delivered to device","Yes","No (Play generates APKs)"],["Download size","Universal (larger)","Per-device (smaller)"]]},{t:"note",text:"An APK is the installable package; an AAB (App Bundle) is a publishing format you upload to Play containing all configs. Play generates optimized per-device split APKs from it (right density/language/ABI) — smaller downloads. New apps must publish as AAB (since Aug 2021). Test generated APKs with bundletool."}]},{level:"senior",q:"What is bundletool, and what do you use it for?",a:[{t:"p",text:"`bundletool` is the tool Play uses internally to build APKs from an AAB — and you can run it locally to *test what Play will deliver*. It generates an `.apks` set from your AAB (optionally for a specific device spec) and can *install* the right split APKs to a connected device. Use it to verify per-device splits, check sizes, and reproduce the exact artifact users get. It's the way to test an AAB locally since you can't install an AAB directly."},{t:"code",title:"Build & install APKs from an AAB",code:`bundletool build-apks --bundle=app.aab --output=app.apks \\
  --ks=release.jks --ks-key-alias=upload
bundletool install-apks --apks=app.apks   # installs the right splits for the device`},{t:"note",text:"bundletool builds APKs from an AAB (what Play does internally) and lets you test locally: generate an .apks set (optionally per device spec) and install the correct splits to a device. Use it to verify per-device splits, sizes, and reproduce the exact user artifact — the way to test an AAB since you can't install one directly."}]},{level:"junior",q:"What are split APKs, and what types are there?",a:[{t:"p",text:"From your AAB, Play generates *split APKs* so a device downloads only what it needs: a *base APK* (core code/resources) plus *configuration splits* for the device's *density*, *language*, and *ABI*. They install together and behave as one app. There are also *feature splits* (dynamic feature modules). This is why AAB downloads are smaller than a universal APK — the device skips other densities, unused languages, and other ABIs."},{t:"list",items:["**Base APK** — core code/resources.","**Config splits** — density, language, ABI for the device.","**Feature splits** — dynamic feature modules.","**Install together** — behave as one app; smaller download."]},{t:"note",text:"Play generates split APKs from an AAB: a base APK (core) plus configuration splits for the device's density, language, and ABI (and feature splits for dynamic modules). They install together as one app. This makes AAB downloads smaller than a universal APK — the device skips other densities/languages/ABIs."}]},{level:"senior",q:"What is a universal APK, and when do you need one?",a:[{t:"p",text:"A *universal APK* is a single APK (generated from the AAB) containing *all* configurations — every density, language, and ABI — so it installs on any device without splits. It's *larger* than per-device splits. You need it for *non-Play distribution* (sideloading, alternate stores, CI device testing, some enterprise/MDM setups) where the Play split mechanism isn't available. Generate it via `bundletool build-apks --mode=universal`. For Play delivery you don't use it — splits are smaller."},{t:"list",items:["**Universal APK** — one APK with all configs.","**Installs anywhere** — no splits needed; larger.","**Use for** — sideloading, other stores, CI, enterprise.","**Generate** — `bundletool --mode=universal`."]},{t:"note",text:"A universal APK is one APK (from the AAB) with all configs (every density/language/ABI) — installs anywhere without splits, but larger. Need it for non-Play distribution: sideloading, alternate stores, CI device testing, enterprise/MDM. Generate with bundletool build-apks --mode=universal. Not for Play (splits are smaller)."}]},{level:"senior",q:"How does Play App Signing change your release/signing workflow?",a:[{t:"p",text:"With *Play App Signing*, you sign the uploaded AAB with your *upload key*; Google holds the *app signing key* and re-signs the delivered APKs with it. So your CI/local build uses the *upload* keystore (which is resettable if lost), and you never handle the app signing key after enrollment. Benefits: the critical key is safe with Google, it enables per-device split signing, and key rotation. You enroll once (Play generates or you provide the app key)."},{t:"list",items:["**You sign** — the AAB with the upload key.","**Google re-signs** — delivered APKs with the app key.","**Upload key** — resettable if lost.","**Enables** — split signing, key rotation, safe key storage."]},{t:"note",text:"With Play App Signing you sign the uploaded AAB with the upload key; Google holds the app signing key and re-signs delivered APKs. Your CI/build uses the upload keystore (resettable if lost) and never handles the app key post-enrollment. Benefits: critical key safe with Google, per-device split signing, key rotation. Enroll once."}]},{level:"junior",q:"Why did Google make the AAB format mandatory?",a:[{t:"p",text:"To deliver *smaller, optimized downloads* automatically: by uploading all configurations once (the AAB), Play can generate *per-device APKs* so users download only their density/language/ABI — typically a meaningful size reduction versus a universal APK. It also enables *features* only possible when Play controls packaging: dynamic feature delivery, asset packs, and per-device optimization. Mandatory since Aug 2021 for *new* apps on Play. The trade-off is Play App Signing (Google holds the app key)."},{t:"list",items:["**Smaller downloads** — per-device APKs (skip unused configs).","**Enables** — dynamic features, asset packs, per-device optimization.","**Mandatory** — new apps since Aug 2021.","**Trade-off** — requires Play App Signing."]},{t:"note",text:"Google mandated the AAB to deliver smaller, optimized downloads automatically — uploading all configs once lets Play generate per-device APKs (users download only their density/language/ABI). It also enables dynamic feature delivery and asset packs. Mandatory for new apps since Aug 2021; trade-off is Play App Signing (Google holds the app key)."}]},{level:"senior",q:"What are asset packs and how do they differ from feature modules?",a:[{t:"p",text:"*Asset packs* (part of Play Asset Delivery) let large *non-code assets* (textures, media — common in games) be delivered separately from the base APK, with delivery modes: *install-time*, *fast-follow* (downloaded right after install), or *on-demand*. *Feature modules* (dynamic delivery) deliver *code + resources* for optional features on demand. Both keep the base install small, but asset packs are for *assets* (up to large sizes) and feature modules for *functionality/code*. Games use asset packs heavily."},{t:"list",items:["**Asset packs** — large non-code assets; install-time/fast-follow/on-demand.","**Feature modules** — code + resources for optional features.","**Both** — keep the base install small.","**Asset packs** — for assets (games); features for functionality."]},{t:"note",text:"Asset packs (Play Asset Delivery) deliver large non-code assets (textures/media, common in games) separately — install-time, fast-follow, or on-demand. Feature modules (dynamic delivery) deliver code + resources for optional features on demand. Both shrink the base install; asset packs are for assets, feature modules for functionality/code."}]},{level:"junior",q:"How do you test the exact APKs Play will deliver from your AAB?",a:[{t:"p",text:"Use `bundletool` to generate an `.apks` set from your AAB and install the right splits to a device (`build-apks` then `install-apks`), reproducing what a user gets. Or use Play's *internal app sharing* (upload the AAB, get a link, install on a device — Play generates the real delivered APKs). Also use *internal testing track* for a full end-to-end Play delivery test. Testing the *release AAB's output* (not just a debug APK) catches split/signing/shrinking issues before production."},{t:"list",items:["**bundletool** — build-apks + install-apks (local).","**Internal app sharing** — Play generates the real APKs from a link.","**Internal testing track** — end-to-end Play delivery.","**Test the release AAB's output** — catches split/signing issues."]},{t:"note",text:"Test the delivered APKs via bundletool (build-apks + install-apks reproduces the user's splits), Play internal app sharing (upload AAB → link → Play generates real APKs), or the internal testing track (full Play delivery). Testing the release AAB's output — not a debug APK — catches split/signing/shrinking issues before production."}]},{level:"senior",q:"What is Play internal app sharing, and how is it different from internal testing?",a:[{t:"p",text:"*Internal app sharing* is for *quick, ad-hoc* sharing: upload any AAB/APK (even debuggable, no version bump needed), get a *shareable link*, and testers install the *Play-generated* build instantly — great for QA and reproducing delivered artifacts. *Internal testing (track)* is a *managed release track* with a tester list, going through the release flow (versioned, reviewed-lite), a step toward production. Use internal app sharing for fast one-off builds; internal testing for structured pre-release testing."},{t:"list",items:["**Internal app sharing** — ad-hoc link, any build, instant.","**Internal testing track** — managed, versioned, tester list.","**Sharing** — quick QA / reproduce delivery.","**Track** — structured pre-release step."]},{t:"note",text:"Internal app sharing: quick ad-hoc — upload any AAB/APK (no version bump), get a link, testers install the Play-generated build instantly (great for QA). Internal testing track: a managed, versioned release track with a tester list, part of the release flow. Sharing = fast one-offs; track = structured pre-release testing."}]},{level:"senior",q:"How does the AAB reduce app size compared to a universal APK, concretely?",a:[{t:"p",text:"A universal APK ships *all* drawable densities, *all* language translations, and *all* ABI native libs — most of which a given device never uses. The AAB lets Play send only the device's *one* density bucket, *its* languages, and *its* ABI — so it drops the other ~4 density variants, dozens of unused translations, and other-architecture `.so` files. For apps with many locales or large native libs, this can cut download size substantially (often 20–50%+)."},{t:"list",items:["**Universal** — all densities/languages/ABIs bundled.","**AAB** — device's density + languages + ABI only.","**Drops** — other densities, unused locales, other-arch `.so`.","**Savings** — often 20–50%+ for multi-locale/native apps."]},{t:"note",text:"A universal APK ships all densities, all translations, and all ABI native libs — most unused per device. The AAB sends only the device's density bucket, its languages, and its ABI — dropping ~4 other density variants, dozens of unused locales, and other-architecture .so files. Often cuts download size 20–50%+ for multi-locale/native-heavy apps."}]},{level:"junior",q:"Can you still distribute an APK outside Google Play with an AAB workflow?",a:[{t:"p",text:"Yes — the AAB requirement is *only for publishing on Google Play*. For other channels (direct download, alternate stores, enterprise/MDM, sideloading), you generate an *APK* from your AAB with `bundletool` (typically a *universal* APK, or device-specific ones). You sign it with your own key. So one build (the AAB) can serve Play (splits) and everything else (an APK via bundletool). Many CI setups produce both an AAB (Play) and a universal APK (other distribution)."},{t:"list",items:["**AAB required** — only for Play publishing.","**Other channels** — generate an APK via bundletool.","**Usually** — a universal APK, self-signed.","**One build** — AAB for Play + APK for the rest."]},{t:"note",text:"Yes — the AAB requirement is only for publishing on Google Play. For other channels (direct download, alternate stores, enterprise, sideloading) generate an APK from the AAB with bundletool (usually universal), signed with your key. One AAB build serves Play (splits) and everything else (an APK). CI often produces both."}]},{level:"senior",q:"What happens to signing when Play generates split APKs?",a:[{t:"p",text:"Each generated *split APK* must be signed so the device accepts them as one app — with Play App Signing, *Play signs every split with the app signing key* it holds. You never sign the splits yourself; you only sign the *uploaded AAB* with your upload key (proving authorship). This is one reason Play App Signing is required for AAB delivery: Play needs the app key to sign the many per-device APKs it generates on the fly."},{t:"list",items:["**Splits signed** — by Play with the app signing key.","**You sign** — only the uploaded AAB (upload key).","**Devices** — accept co-signed splits as one app.","**Why Play App Signing** — Play needs the app key to sign splits."]},{t:"note",text:"Play signs every generated split APK with the app signing key it holds (so the device accepts them as one app); you only sign the uploaded AAB with your upload key. This is why Play App Signing is required for AAB delivery — Play needs the app key to sign the many per-device APKs it generates on the fly."}]},{level:"junior",q:"What is the difference between versionCode and versionName in a release?",a:[{t:"p",text:"`versionCode` is an *integer* Play (and the system) use to order versions — must strictly increase per upload; users update only to a higher code. `versionName` is a *human-readable string* (e.g. `2.4.1`) shown to users, with no ordering meaning. You bump `versionName` for marketing/semantic clarity and `versionCode` for every uploaded build. They're independent: many `versionCode` bumps can share a `versionName`, or vice versa."},{t:"list",items:["**`versionCode`** — integer; strictly increasing; ordering.","**`versionName`** — human string (e.g. 2.4.1); display only.","**Update** — only to a higher versionCode.","**Independent** — bump code every build, name semantically."]},{t:"note",text:"versionCode: an integer Play uses to order versions — strictly increasing per upload; users update only to a higher code. versionName: a human string (2.4.1) shown to users, no ordering meaning. Bump versionCode every uploaded build, versionName for semantic/marketing clarity. They're independent."}]},{level:"senior",q:"How do you migrate an existing app to Play App Signing safely?",a:[{t:"p",text:"For an existing app, you can let Google *use your existing app key* as the app signing key (upload it securely via the Play Console's provided tooling) and then create a *new upload key* for future uploads — so existing installs (signed with the original key) keep updating seamlessly. Do it carefully: back up keys first, follow the Console's export/upload steps, and verify with a test release. After migration you upload with the *upload* key and Play re-signs with the original app key, preserving update continuity."},{t:"list",items:["**Existing key** — becomes the app signing key (uploaded securely).","**New upload key** — for future uploads.","**Continuity** — existing installs keep updating.","**Do carefully** — back up, follow Console steps, test-release."]},{t:"note",text:"Migrate an existing app by letting Google use your existing app key as the app signing key (upload securely via the Console) and creating a new upload key for future uploads — existing installs (original-key-signed) keep updating. Back up keys, follow the export/upload steps, verify with a test release. Then you upload with the upload key; Play re-signs with the original app key."}]},{level:"junior",q:"What is targetSdkVersion, and why does Play enforce a minimum?",a:[{t:"p",text:"`targetSdkVersion` declares the API level your app is *tested and designed against* — the system applies *behavior changes* for that level (and runs compatibility behaviors for newer OS features you haven't adopted). Play *requires* new apps and updates to target a *recent* API level (updated yearly) to ensure apps use modern privacy/security/behavior standards. So you must periodically raise `targetSdk` (and handle its behavior changes) to keep publishing updates. It's separate from `minSdk` (lowest supported)."},{t:"list",items:["**`targetSdk`** — API level you're designed/tested against.","**Applies** — that level's behavior changes.","**Play requires** — a recent target (yearly bump) to publish.","**Separate** — from `minSdk` (lowest supported)."]},{t:"note",text:"targetSdkVersion declares the API level your app is designed/tested against — the system applies that level's behavior changes. Play requires new apps/updates to target a recent API level (raised yearly) for modern privacy/security standards, so you must periodically bump targetSdk (and handle its behavior changes) to keep publishing. Separate from minSdk."}]},{level:"senior",q:"What is the difference between minSdk, targetSdk, and compileSdk?",a:[{t:"p",text:"*`minSdk`* — the *lowest* API level the app runs on (Play won't offer it to older devices; you must guard newer APIs). *`targetSdk`* — the level you *designed/tested against* (determines applied behavior changes). *`compileSdk`* — the level you *compile* against (which APIs are *available* to call at build time; usually the latest). You can call an API up to `compileSdk` but must *runtime-guard* anything above `minSdk`. Typical: compile latest, target recent (Play requirement), min as low as you support."},{t:"table",headers:["","Means","Typical"],rows:[["minSdk","Lowest device it runs on","As low as you support"],["targetSdk","Designed/tested against","Recent (Play requires)"],["compileSdk","APIs available at build","Latest"]]},{t:"note",text:"minSdk: lowest API the app runs on (guard newer APIs above it). targetSdk: what you designed/tested against (applied behavior changes; Play requires recent). compileSdk: what you compile against (available APIs; usually latest). You can call APIs up to compileSdk but must runtime-guard anything above minSdk."}]},{level:"junior",q:"How do you generate a signed AAB in Android Studio and via CI?",a:[{t:"p",text:"In Android Studio: *Build → Generate Signed Bundle / APK → Android App Bundle*, choose your (upload) keystore, and it produces a signed AAB. In CI: configure a `signingConfig` (credentials from secrets/env) and run `./gradlew bundleRelease` — the output AAB is at `app/build/outputs/bundle/release/`. Then upload to Play (manually or via the *Play Developer API*/fastlane/Gradle Play Publisher). CI signing keeps the keystore/passwords in secure secrets, never in the repo."},{t:"code",title:"CI: build a signed AAB",code:`./gradlew bundleRelease
# output: app/build/outputs/bundle/release/app-release.aab
# then upload via Play Developer API / fastlane / gradle-play-publisher`},{t:"note",text:"Studio: Build → Generate Signed Bundle → App Bundle with your upload keystore. CI: set a signingConfig (secrets/env) and run ./gradlew bundleRelease (output in app/build/outputs/bundle/release/), then upload via the Play Developer API/fastlane/gradle-play-publisher. Keep keystore/passwords in secure secrets, never in the repo."}]},{level:"senior",q:"What are the trade-offs of dynamic feature modules for delivery?",a:[{t:"p",text:"*Pros*: smaller base install, features fetched on demand (or install-time/conditional), and code separation. *Cons*: added complexity — you must handle the *download flow* (`SplitInstallManager`), *availability* (the feature's code/resources aren't present until installed, so guard access), possible download failures/latency, and testing more delivery paths. They shine for genuinely large or rarely-used features; for small features the complexity outweighs the size savings — prefer them only when the size win is real."},{t:"list",items:["**Pros** — smaller base, on-demand fetch, separation.","**Cons** — download flow, availability guarding, failure handling.","**More testing** — multiple delivery paths.","**Use** — only when the size win is genuinely large."]},{t:"note",text:"Dynamic feature modules: pros — smaller base install, on-demand (or install-time/conditional) delivery, code separation. Cons — complexity: handle the SplitInstallManager download flow, guard availability (code absent until installed), handle failures/latency, test more paths. Worth it for large/rare features; overkill for small ones — use only when the size win is real."}]},{level:"junior",q:"What is the app signing key certificate (SHA-1/SHA-256) used for?",a:[{t:"p",text:"The app signing certificate's *fingerprints* (SHA-1/SHA-256) identify your app to external services: you register them for *Google Sign-In*, *Firebase*, *Maps*, *App Links (Digital Asset Links)*, and other APIs that verify your app's identity. With *Play App Signing*, the relevant fingerprint is the *app signing key's* (from Google), plus your *upload key's* for some flows — both are shown in the Play Console. If sign-in/links break in production, a mismatched/missing fingerprint is a common cause."},{t:"list",items:["**Fingerprints** — SHA-1/SHA-256 of the signing cert.","**Register for** — Google Sign-In, Firebase, Maps, App Links.","**Play App Signing** — use the app key's fingerprint (+ upload key).","**Common bug** — mismatched fingerprint breaks prod sign-in/links."]},{t:"note",text:"The app signing certificate's SHA-1/SHA-256 fingerprints identify your app to services (Google Sign-In, Firebase, Maps, App Links/Digital Asset Links). With Play App Signing use the app signing key's fingerprint (from the Console), plus the upload key's for some flows. Mismatched/missing fingerprints are a common cause of broken production sign-in/links."}]},{level:"senior",q:"How do App Links relate to signing and the AAB?",a:[{t:"p",text:"*Verified App Links* (opening `https://` URLs directly in your app, no chooser) require a *Digital Asset Links* file (`assetlinks.json`) on your domain listing your app's package and *signing certificate fingerprint (SHA-256)*. With Play App Signing, that fingerprint is the *app signing key's* (Google's) — a frequent mistake is registering the upload key's fingerprint, so verification fails in production. Get the correct SHA-256 from the Play Console's App Signing page and publish it in `assetlinks.json`."},{t:"list",items:["**Verified App Links** — need `assetlinks.json` with the SHA-256.","**Play App Signing** — use the *app signing key's* fingerprint.","**Common mistake** — using the upload key's fingerprint.","**Source** — Play Console App Signing page."]},{t:"note",text:"Verified App Links (open https:// URLs directly, no chooser) need a Digital Asset Links assetlinks.json on your domain with your package + signing cert SHA-256. With Play App Signing use the app signing key's fingerprint (Google's) — using the upload key's is a common mistake that fails verification. Get the correct SHA-256 from the Console's App Signing page."}]},{level:"senior",q:"What is the difference between a release track's rollout and the AAB it serves?",a:[{t:"p",text:"The *AAB* is the *artifact* (your compiled code/resources for all configs). A *release* on a *track* (internal/closed/open/production) is a *distribution decision*: which AAB (versionCode) goes to which audience, at what *rollout percentage*. The same AAB can be *promoted* across tracks (e.g. from internal → production) without rebuilding. So 'build once (AAB), release many' — the artifact is fixed; the track/rollout controls exposure. This separation enables safe staged releases."},{t:"list",items:["**AAB** — the fixed artifact (versionCode).","**Release/track** — who gets it, at what rollout %.","**Promote** — same AAB across tracks without rebuild.","**Build once, release many** — artifact fixed, exposure controlled."]},{t:"note",text:"The AAB is the artifact (compiled code for all configs); a release on a track (internal/closed/open/production) is a distribution decision — which AAB goes to which audience at what rollout %. The same AAB promotes across tracks without rebuilding. Build once, release many: artifact fixed, track/rollout controls exposure — enabling safe staged releases."}]},{level:"junior",q:"How do you handle native libraries (ABIs) with the App Bundle?",a:[{t:"p",text:"The AAB *automatically* creates ABI splits (arm64-v8a, armeabi-v7a, x86_64) and delivers only the device's ABI — you don't need manual `abiFilters`/APK splits for Play. Just include all the ABIs you support in the build; Play handles per-device delivery. For *non-Play* distribution you may still use `abiFilters` or build a universal APK. This is a key AAB win for native-heavy apps (large `.so` files no longer multiply into every download)."},{t:"list",items:["**Automatic ABI splits** — Play delivers the device's ABI.","**Include all supported ABIs** — Play splits them.","**No manual** — `abiFilters`/APK splits needed for Play.","**Non-Play** — may still use `abiFilters`/universal APK."]},{t:"note",text:"The AAB automatically creates ABI splits (arm64-v8a, armeabi-v7a, x86_64) and delivers only the device's ABI — no manual abiFilters/APK splits needed for Play. Include all supported ABIs; Play handles per-device delivery. A key win for native-heavy apps (large .so files no longer bloat every download). Non-Play may still use abiFilters/universal."}]},{level:"senior",q:"Walk through what happens from building an AAB to a user installing the app.",a:[{t:"p",text:"You build a signed AAB (`bundleRelease`, signed with the *upload key*) and upload it to a Play track. Play verifies the upload signature, then *stores* the AAB. When a user on a specific device requests install/update, Play *generates the split APKs* for that device (base + its density/language/ABI), *signs each split with the app signing key* (which Google holds), and delivers just those. The device installs the co-signed splits as one app. You test this path locally with bundletool or internal app sharing."},{t:"list",items:["**Build + sign AAB** — upload key; upload to a track.","**Play verifies + stores** — the AAB.","**On install** — generates + app-key-signs device splits.","**Device** — installs co-signed splits as one app."]},{t:"note",text:"Build a signed AAB (bundleRelease, upload key) → upload to a Play track → Play verifies the upload signature and stores it → on a device's install/update, Play generates split APKs (base + device density/language/ABI), signs each with the app signing key it holds, and delivers just those → device installs the co-signed splits as one app. Test via bundletool/internal app sharing."}]},{level:"junior",q:"What is the difference between an AAB and an APK for CI/artifact storage?",a:[{t:"p",text:"For Play, your CI produces and archives the *AAB* (`bundleRelease`) as the release artifact — it's the single source Play delivers from. If you also need installable APKs (device testing, QA, non-Play channels), CI can *additionally* generate them from the AAB with bundletool (or build `assembleRelease`). Best practice: archive the AAB *and* its `mapping.txt` (for deobfuscation) per release, tagged with the versionCode, so you can always reproduce/deobfuscate a shipped build."},{t:"list",items:["**Archive** — the AAB as the release artifact (for Play).","**Also generate** — APKs via bundletool for testing/non-Play.","**Keep** — `mapping.txt` per release for deobfuscation.","**Tag** — by versionCode to reproduce a shipped build."]},{t:"note",text:"CI archives the AAB (bundleRelease) as the Play release artifact; additionally generate installable APKs from it (bundletool/assembleRelease) for device testing/non-Play. Archive the AAB and its mapping.txt per release, tagged by versionCode, so you can always reproduce and deobfuscate a shipped build."}]},{level:"senior",q:"How do you inspect the size breakdown of what each device will download?",a:[{t:"p",text:"Use the Play Console's *App Bundle Explorer* (per-device download/install sizes for a release) or `bundletool get-size total --apks=app.apks` (with a device spec) to see the *actual delivered size* per configuration. The *APK Analyzer* on the AAB shows contents but not the split-adjusted per-device size — bundletool/Console give the real number. This tells you the *download* size users see (which affects install conversion), separate from the raw AAB size."},{t:"list",items:["**App Bundle Explorer** — per-device sizes in the Console.","**`bundletool get-size`** — delivered size per config locally.","**APK Analyzer** — contents, not split-adjusted per-device size.","**Matters** — real download size affects install conversion."]},{t:"note",text:"Inspect per-device download size with the Play Console's App Bundle Explorer or bundletool get-size total (with a device spec) — the actual delivered size per config. The APK Analyzer shows AAB contents but not the split-adjusted per-device size. The real download size (not raw AAB size) is what affects install conversion."}]}],oT={id:"aab-play-signing",title:"AAB, APK & Play App Signing",tagline:"APK as the installable artifact, split APKs, the AAB publishing format & why it's smaller, Play App Signing's two-key model, and testing with bundletool.",tags:["AAB","APK","Play App Signing","Split APKs","bundletool"],content:iT,qa:nT},sT=[{heading:"The Play Console and testing tracks",blocks:[{t:"p",text:"The **Play Console** is Google's web dashboard for publishing and managing your app on the Play Store — uploading releases, managing testers, monitoring quality (Android Vitals), reading reviews, and configuring the store listing. A core concept is **testing tracks**: a staged pipeline of release channels that let you test with progressively larger audiences before reaching all users, catching problems early with limited blast radius."},{t:"table",headers:["Track","Audience","Purpose"],rows:[["Internal testing","up to ~100 trusted testers (your team)","fast (minutes), for quick internal verification of a build"],["Closed testing","a defined list / group of testers (alpha)","broader QA with a controlled group before going wider"],["Open testing","anyone who opts in (beta)","public beta — real-world feedback from volunteers before production"],["Production","all users on the Play Store","the live release everyone gets"]]},{t:"list",items:["**Internal testing** — the fastest track (a build is available in minutes), for a small trusted group. Use it for quick verification and to test the real Play-delivered/signed artifacts.","**Closed testing (alpha)** — a controlled group of testers you invite (by email list or Google Group), for wider QA before public exposure.","**Open testing (beta)** — anyone can opt in via a link, giving you real-world feedback and crash data at scale before production.","**Production** — the full release. The progression internal → closed → open → production lets you catch bugs with a small audience first, reducing the risk of a bad release reaching everyone."]}]},{heading:"Staged rollouts",blocks:[{t:"p",text:"A **staged (or phased) rollout** releases a new production version to a *percentage* of users gradually, rather than everyone at once. You might roll out to 5% of users, monitor for crashes/ANRs/bad reviews, then increase to 20%, 50%, 100% over days — or **halt/rollback** if you detect a problem. This limits the damage of a bad release: a crash-causing bug affects 5% of users, not 100%, and you can stop it before it spreads."},{t:"list",items:["**Why stage**: even with testing, some bugs only surface at scale or on specific devices in the field. A staged rollout is a safety net — you watch real production metrics (Android Vitals: crash rate, ANR rate) on a small slice before committing everyone.","**Halt and resume**: if metrics degrade at 5%, you *halt* the rollout (no more users get it) and can fix the issue. Existing users on the new version stay, but you stop the spread.","**Rollback caveat**: you can halt a staged rollout, but you generally *can't* force users already on the new version back to the old one (they'd need a new higher-versionCode release with the fix). So staged rollout limits *exposure*, and the fix is a new patched release — which is why catching it early (at 5%) matters.","**Best practice**: stage significant releases (start small, monitor Vitals, ramp up), and pair with good crash monitoring (Crashlytics/Vitals) so you *see* problems during the ramp. This is standard release hygiene for any app with a meaningful user base."]}]},{heading:"In-app updates",blocks:[{t:"p",text:"The **In-App Updates API** lets your app prompt users to update *without leaving the app* — useful for pushing important updates (a critical fix, a required minimum version) to users who might not update via the Play Store on their own. There are two flavors:"},{t:"list",items:["**Flexible update** — a *non-blocking* update: the user can keep using the app while the update downloads in the background, then you prompt to restart to apply it. Use for optional/recommended updates where you don't want to interrupt the user. It's the gentle option.","**Immediate update** — a *blocking, full-screen* update flow the user must complete (or exit the app) before continuing. Use for *critical* updates — a security fix, a breaking backend change that makes old versions unusable, enforcing a minimum supported version. It's forceful, so reserve it for genuinely mandatory updates.","**How it works**: you check for an available update (`AppUpdateManager`), and if one exists (and meets your criteria, e.g. priority or staleness), you trigger the flexible or immediate flow. Play provides the update priority (you set it when publishing) and how many days it's been available, so you can decide when to prompt or force.","**Why use it**: many users don't update apps promptly; in-app updates let you *drive* adoption of an important version, reducing the long tail of users stuck on old, buggy versions — which matters for support burden and for retiring old API contracts."]}]},{heading:"Store listing, review, and other Console features",blocks:[{t:"list",items:["**Store listing** — the app's Play Store page: title, description, screenshots, feature graphic, icon. Optimizing this (App Store Optimization) affects discoverability and conversion. Play also supports *custom store listings* and *store listing experiments* (A/B testing listing assets).","**Review process** — new apps and updates go through Google's review before going live. Review times vary (hours to days); policy violations (permissions misuse, content, deceptive behavior) cause rejections. Sensitive permissions and data practices require declarations (the Data Safety section).","**Pre-launch report** — when you upload to a testing track, Play automatically runs your app on real devices (crawling the UI) and reports crashes, performance issues, accessibility problems, and security vulnerabilities *before* release — a free automated QA pass.","**Android Vitals** — production quality metrics (crash rate, ANR rate, excessive wakeups, slow/frozen frames, battery) across real users. Google uses these for Play ranking/visibility, and they're your window into real-world quality — the 'is there a problem?' signal that complements local profiling.","**Other** — pricing/monetization (in-app purchases, subscriptions via Play Billing), release management, user feedback/reviews (with the ability to reply), and analytics."]},{t:"note",text:"Play Console: the publishing/management dashboard. Testing tracks (internal → closed → open → production) test with progressively larger audiences before full release. Staged rollout releases to a % of users gradually — monitor Vitals, halt if bad (limits blast radius; can't force-downgrade, so catch early). In-app updates: flexible (non-blocking background download) for optional updates, immediate (blocking full-screen) for critical/mandatory ones. Also: store listing (+ experiments), review process, pre-launch report (auto device testing), Android Vitals (production quality metrics for ranking)."}]}],rT=[{level:"junior",q:"What are the testing tracks in the Play Console?",a:[{t:"p",text:"**Testing tracks are a staged pipeline of release channels that let you test your app with progressively larger audiences before it reaches all users, catching problems early with a limited blast radius.** The progression is internal → closed → open → production."},{t:"list",items:["**Internal testing** — the fastest track (a build is available in minutes), for up to ~100 trusted testers (your team). Use it for quick internal verification and to test the real Play-delivered, Play-signed artifacts.","**Closed testing (alpha)** — a controlled group of testers you invite by email list or Google Group, for broader QA before public exposure.","**Open testing (beta)** — anyone can opt in via a link, giving you real-world feedback and crash data at scale before production.","**Production** — the full release that all Play Store users get."]},{t:"p",text:"The value of the progression is *risk reduction*: you catch bugs with a small, controlled audience first, so a problem is found by testers or beta users rather than your entire user base. Each track widens the audience, so you gain confidence at each stage before promoting to the next. A build can be promoted up the tracks (or you upload directly to a track). This staged approach — combined with staged rollouts *within* production — is standard release hygiene, letting you ship with confidence and limited exposure to any given release's risks."}]},{level:"junior",q:"What is a staged rollout and why use one?",a:[{t:"p",text:"**A staged (or phased) rollout releases a new production version to a *percentage* of users gradually — say 5%, then 20%, 50%, 100% over days — rather than to everyone at once, so you can monitor for problems on a small slice and halt if something's wrong before it reaches all users.** It limits the damage of a bad release."},{t:"list",items:["**Why**: even with thorough testing, some bugs only surface at scale or on specific devices in the real world. A staged rollout is a safety net — you watch production metrics (Android Vitals: crash rate, ANR rate) and reviews on a small percentage first. If a crash-causing bug slipped through, it affects 5% of users, not 100%.","**Halt if bad**: if metrics degrade during the ramp, you *halt* the rollout — no more users get the new version — and fix the issue. This stops the spread.","**Ramp up if healthy**: if metrics look good at each stage, you increase the percentage until reaching 100%."]},{t:"p",text:"An important caveat: you can *halt* a staged rollout to stop *new* users from getting the bad version, but you generally *can't* force users already on the new version back to the old one (Android's same-or-higher versionCode rule for updates). So the fix for those users is a *new patched release* with a higher versionCode. This is exactly why catching problems *early* (at 5%) matters — the fewer users on the bad version, the less impact before your fix ships. Staged rollouts pair with good crash monitoring (Crashlytics/Vitals) so you actually *see* problems during the ramp. It's standard practice for any app with a meaningful user base — ship gradually, watch, and stop if needed."}]},{level:"junior",q:"What is the difference between a flexible and an immediate in-app update?",a:[{t:"p",text:"**The In-App Updates API lets you prompt users to update *without leaving your app*. A *flexible* update is non-blocking (the user keeps using the app while the update downloads in the background); an *immediate* update is blocking (a full-screen flow the user must complete before continuing).** You choose based on how critical the update is."},{t:"list",items:["**Flexible update** — non-blocking. The update downloads in the background while the user continues using the app, and you prompt them to restart to apply it when it's ready. Use for *optional/recommended* updates where you don't want to interrupt the user. It's the gentle option.","**Immediate update** — blocking. A full-screen flow the user must complete (or exit the app) before they can continue. Use for *critical* updates — a security fix, or a breaking backend change that makes old app versions unusable, or enforcing a minimum supported version. It's forceful, so reserve it for genuinely mandatory updates."]},{t:"p",text:"The reason in-app updates exist is that many users don't update apps promptly on their own, leaving a long tail stuck on old, buggy versions — which increases support burden and prevents you from retiring old API contracts. In-app updates let you *drive* adoption of an important version from within the app. The decision between flexible and immediate is about *how much you can interrupt the user*: recommended update → flexible (non-intrusive); must-update (security/breaking change) → immediate (blocking). You check for an available update with `AppUpdateManager`, and Play tells you the update's priority (which you set when publishing) and how long it's been available, so you can decide when to prompt (flexible) or force (immediate)."}]},{level:"junior",q:"What is the Play pre-launch report?",a:[{t:"p",text:"**The pre-launch report is an automated QA pass Play runs when you upload your app to a testing track — Play installs and runs your app on a range of *real* devices, automatically crawls through the UI, and reports crashes, performance issues, accessibility problems, and security vulnerabilities it finds, *before* your release goes live.** It's a free automated test on real hardware you might not have."},{t:"list",items:["**What it catches**: crashes on specific device/OS combinations, performance issues, accessibility problems (missing content descriptions, contrast), and security vulnerabilities — surfaced automatically without you writing tests or owning the devices.","**How it works**: Play's crawler navigates your app's screens on various real devices (different manufacturers, OS versions, screen sizes) and records what it finds — screenshots, crash traces, and issues per device.","**When it runs**: automatically after you upload to a testing track (before promoting to production), so you see problems early."]},{t:"p",text:"It's valuable because it tests on *real devices across the fragmented Android ecosystem* — manufacturers and OS versions you likely don't have on hand — catching device-specific crashes and issues that your own testing (on a few devices) would miss. It complements your own test suite: your tests verify *known* behavior deterministically, while the pre-launch report *explores* your app broadly on hardware you don't own, finding surprises. It's essentially free extra QA, so reviewing it before promoting a release to production is good release hygiene — especially for catching the device-fragmentation issues that are hard to test otherwise."}]},{level:"senior",q:"How would you design a safe release process for an app with millions of users?",a:[{t:"p",text:"**I'd build a *staged, monitored* pipeline that progressively widens exposure while watching quality metrics at each step — combining testing tracks, staged production rollout, robust crash/quality monitoring, and the ability to halt and hotfix — so any bad release is caught with the smallest possible blast radius and can be responded to quickly.** With millions of users, even a small crash rate means many affected people, so limiting exposure and reacting fast is paramount."},{t:"list",items:["**1. Pre-release validation**: thorough automated tests (the pyramid) run in CI on every change; test the *release* (minified) build to catch R8 issues; validate the real AAB artifacts (bundletool / internal testing). Review the *pre-launch report* for device-fragmentation issues. This catches problems before any user sees the release.","**2. Testing tracks — progressive audiences**: promote through *internal* (team, minutes) → *closed* (broader QA group) → *open beta* (volunteers at scale, real-world crash data) before production. Each stage exposes the build to more real usage/devices with feedback, catching issues a controlled test wouldn't.","**3. Staged production rollout — the key safety mechanism**: never release to 100% at once. Start at a small percentage (1-5%), *monitor Android Vitals* (crash rate, ANR rate) and Crashlytics in real time, and only ramp up (10% → 25% → 50% → 100%) if metrics stay healthy over enough time to gather signal. At millions of users, even 1% is a large, statistically meaningful sample — so problems surface fast at low exposure.","**4. Robust monitoring with alerting**: Crashlytics (real-time crash reporting with de-obfuscated traces via the mapping file) and Android Vitals (crash/ANR rates, and Play's *release-over-release* comparison) — with *alerts* on regressions (a spike in crash rate for the new version). You must *see* problems during the ramp, ideally automatically, not by manually checking dashboards. Compare the new version's metrics against the previous version's baseline.","**5. Halt-and-hotfix capability**: if metrics regress during the ramp, *halt* the staged rollout immediately (stops new users getting the bad version). Since you can't force-downgrade users already on it, prepare a *hotfix* — a new higher-versionCode release with the fix — and roll *that* out (staged again). For critical issues, consider *feature flags / remote config* to disable the broken feature server-side *without* a new release (the fastest mitigation), and *in-app immediate updates* to push the hotfix to users on the bad version.","**6. Feature flags decouple release from rollout**: ship risky features *behind a remote flag* (Firebase Remote Config), disabled by default, then enable them gradually server-side *after* the binary is out and stable. This separates *deploying code* from *enabling a feature*, so you can turn a problematic feature off instantly without any app release — a crucial safety layer at scale."]},{t:"list",items:["**The overarching principles**: *progressive exposure* (tracks + staged rollout — never everyone at once), *fast detection* (real-time monitoring + alerts + release comparison), *fast response* (halt, feature-flag kill switch, hotfix + immediate in-app update), and *decoupling deploy from release* (feature flags). Together these ensure that at millions of users, a bad release affects the fewest people for the shortest time.","**Also**: maintain the ability to reproduce and debug production issues (keep every release's mapping file for de-obfuscation), and have a documented incident-response runbook so the team reacts consistently under pressure."]},{t:"p",text:"**The senior framing**: at scale, the release process is fundamentally about *risk management* — you assume some bad releases will slip through testing (they always do), so you design to *limit their blast radius* and *respond fast*. The mechanisms compound: testing tracks and staged rollout *limit exposure* (progressive audiences, then a small production percentage), real-time monitoring with release-comparison and alerts *detects fast* (a statistically meaningful sample at 1% of millions surfaces problems quickly), and halting + feature-flag kill switches + hotfixes with immediate in-app updates enable *fast response*. The most powerful pattern is *decoupling deploy from release* via feature flags — shipping code dark and enabling it server-side gradually — because it makes the riskiest changes reversible *without* an app release. A process that ships to 100% at once with only manual monitoring is the anti-pattern; the mature approach treats every production release as a *monitored experiment* that can be halted and rolled forward (with a fix), never a fire-and-forget event. Demonstrating this layered, risk-managed pipeline — and specifically the feature-flag decoupling and the halt/hotfix/kill-switch response toolkit — is the comprehensive, senior-level answer."}]},{level:"senior",q:"You discover a critical crash in a version that's already at 100% rollout. What are your options?",a:[{t:"p",text:"**Since the bad version is already fully rolled out and you can't force-downgrade users, the options are, in order of speed: (1) disable the broken feature server-side via a feature flag / remote config if it's flag-gated (instant, no release), (2) fix a server-side cause if the crash is backend-triggered, and (3) ship a hotfix release (staged, then pushed via immediate in-app update). The right response depends on what's causing the crash and whether you have kill-switch infrastructure.**"},{t:"list",items:["**First — assess and mitigate immediately, before the full fix**: how severe (crash rate, which users/devices), and what's the *fastest* way to *stop the bleeding*? The fastest mitigations don't require a new app release:","**Option 1 — Feature flag / Remote Config kill switch (instant, if available)**: if the crashing feature is gated behind a remote flag (Firebase Remote Config), *turn it off server-side*. Users' apps fetch the config and disable the feature *without any app update* — the crash stops within minutes/hours for everyone, including those on the bad version. This is why shipping risky features behind flags is so valuable: it makes them killable without a release. If you have this, it's almost always the first move.","**Option 2 — Server-side fix (if the crash is backend-triggered)**: if the crash is caused by something the *backend* sends (a malformed response, a new field the old code chokes on, a bad config), fixing it on the *server* resolves it for all clients without an app release. Many 'app crashes' are actually triggered by server changes and are fixable server-side — check this early.","**Option 3 — Hotfix release (the real fix, but slower)**: prepare a new version with the fix and a *higher versionCode*, test it (release build!), and roll it out — ideally *staged* again (start small to confirm the fix works and introduces no new issues), then ramp up. To *accelerate adoption* among users on the bad version, use the **In-App Updates API with an *immediate* (blocking) update** and high update priority, prompting/forcing users to update to the fixed version. This drives the fix out faster than waiting for organic updates. (Play review time can delay this, which is another reason the server-side mitigations matter for immediate relief.)"]},{t:"list",items:["**What you *can't* do**: force users already on the bad version back to the previous version — Android only allows same-or-higher versionCode updates, so 'rolling back' means rolling *forward* to a fixed higher version. This is the key constraint that makes the instant mitigations (flags, server fix) so important — they're the only way to help affected users *quickly* without waiting for a hotfix release cycle.","**Halt is irrelevant here** (it's already at 100%) — halting a staged rollout only helps *before* full exposure. This scenario is precisely why you *stage* rollouts in the first place: to avoid ever being in this 'already at 100%' position. The lesson feeds back into process: stage rollouts and gate risky features behind flags so this situation is both rarer and more recoverable.","**Communication & monitoring**: notify stakeholders, monitor the crash rate as mitigations take effect (confirm the flag kill / hotfix actually reduces it), and do a post-incident review — was this catchable in a staged rollout? Should the feature have been flag-gated? — to improve the process."]},{t:"p",text:"**The senior framing**: the scenario tests whether you understand the *constraint* (no force-downgrade — you can only roll forward) and therefore prioritize *release-free mitigations* first. The fastest response is a *feature-flag kill switch* (disable the broken feature server-side, no app update needed) or a *server-side fix* (if the crash is backend-triggered) — both help affected users in minutes without a release. Only then does the *hotfix* (staged forward-roll + immediate in-app update to drive adoption) provide the permanent fix, accepting that it's slower (build, test, Play review, propagation). The meta-lesson is that this situation is *far better prevented* — staged rollouts (so you never hit 100% with a bad build) and feature flags (so risky code is killable without a release) are the infrastructure that turns a 'critical crash at 100%' catastrophe into a 'flip a flag' non-event. Demonstrating the constraint-awareness (roll forward, not back), the *ordered* mitigation toolkit (flag kill → server fix → hotfix + immediate update), and the preventive-infrastructure insight (stage + feature-flag) is the complete, senior answer."}]},{level:"junior",q:"What is the difference between internal, closed, and open testing tracks?",a:[{t:"p",text:"Play's testing tracks escalate audience/visibility: *internal* — up to ~100 named testers, near-instant availability (no review delay), for quick QA; *closed* — a larger controlled group (email lists or Google Groups), still invite-only, for beta with a broader set; *open* — anyone with the link can join (public beta), largest reach. You *promote* the same build up the ladder (internal → closed → open → production) as confidence grows. Choose the track by how wide and controlled you want testers."},{t:"table",headers:["Track","Audience","Use"],rows:[["Internal","~100 named testers, instant","Quick QA"],["Closed","Controlled group (lists/groups)","Broader beta"],["Open","Anyone with the link","Public beta"],["Production","All users","Live release"]]},{t:"note",text:"Testing tracks escalate: internal (~100 named testers, instant, quick QA), closed (controlled invite-only group — email lists/Google Groups, broader beta), open (anyone with the link, public beta), production (all users). Promote the same build up the ladder as confidence grows. Choose by how wide/controlled you want testers."}]},{level:"junior",q:"What is a staged (percentage) rollout, and why use one?",a:[{t:"p",text:"A *staged rollout* releases a new version to a *percentage* of production users (e.g. 1% → 5% → 20% → 50% → 100%) instead of everyone at once. This limits *blast radius*: if the release has a crash/regression, only a small fraction is affected, and you can *halt or roll back* before it spreads. You watch vitals (crashes, ANRs) at each stage and increase the percentage as it proves stable. Standard practice for any app with meaningful user numbers."},{t:"list",items:["**Percentage release** — 1% → … → 100% of production.","**Limits blast radius** — few users hit a bad build.","**Halt/rollback** — before a problem spreads.","**Watch vitals** — increase % as stability proves out."]},{t:"note",text:"A staged rollout releases to a percentage of production users (1% → 5% → 20% → … → 100%) instead of all at once — limiting blast radius so a crash/regression hits few users and you can halt/roll back before it spreads. Watch vitals at each stage and increase % as it proves stable. Standard for apps with meaningful scale."}]},{level:"senior",q:"What can and can't you do when halting or rolling back a staged rollout?",a:[{t:"p",text:"You *can* **halt** a staged rollout (stop increasing %, freezing exposure) at any point, and *resume* later. You *cannot truly 'un-install'* a bad version from users who already got it — 'rollback' means *halting the bad rollout and releasing a new higher-versionCode build* (a fix, or a re-release of the previous code with a new code) to supersede it. Since `versionCode` can't go backward, the fix always ships as a *new, higher* version. So plan releases so you can quickly build/ship a forward fix."},{t:"list",items:["**Halt** — freeze the rollout %; resume later.","**Can't un-install** — from users who already updated.","**Rollback** — halt + ship a new higher-versionCode fix.","**No going backward** — versionCode only increases."]},{t:"note",text:"You can halt a staged rollout (freeze %) and resume later, but you can't un-install a bad version from users who got it. 'Rollback' = halt the bad rollout + release a new higher-versionCode build (a fix or re-released prior code with a new code). versionCode only increases, so the fix always ships as a new version — plan for a fast forward fix."}]},{level:"junior",q:"What are release notes, and how are they localized?",a:[{t:"p",text:"*Release notes* ('What's new') describe changes in a version, shown to users on the store and in the update prompt. You provide them per *locale* (localized strings), and they're attached to a *release* (versionCode range). Keep them concise and user-facing (what improved), not internal changelog. Play requires them for each release; you can reuse/copy across releases. Good release notes can nudge users to update and communicate value."},{t:"list",items:["**'What's new'** — per-version change description.","**Localized** — provided per locale.","**User-facing** — value/changes, not internal changelog.","**Per release** — attached to a versionCode."]},{t:"note",text:"Release notes ('What's new') describe a version's changes, shown on the store and update prompt, provided per locale (localized). Keep them concise and user-facing (what improved), not an internal changelog. Play requires them per release; they can nudge users to update and communicate value."}]},{level:"senior",q:"How does update priority work with in-app updates?",a:[{t:"p",text:"When you roll out a release you can set an *update priority* (0–5) via the Play Developer API. Your app reads it from `AppUpdateInfo.updatePriority()` and *decides the flow*: high priority → trigger the *immediate* (blocking) update; medium → *flexible*; low → maybe just a subtle prompt or ignore for now. Priority lets you *classify releases* (critical vs routine) and drive the in-app update UX accordingly, without hardcoding per-version logic. Combine with `clientVersionStalenessDays` to escalate over time."},{t:"list",items:["**Priority 0–5** — set per release via the Developer API.","**App reads** — `updatePriority()` to choose the flow.","**High → immediate** — low → flexible/subtle.","**+ staleness** — escalate as the update ages."]},{t:"note",text:"Update priority (0–5, set per release via the Play Developer API) is read by the app from AppUpdateInfo.updatePriority() to choose the in-app update flow: high → immediate (blocking), medium → flexible, low → subtle/ignore. It classifies releases (critical vs routine) driving UX without per-version hardcoding. Combine with clientVersionStalenessDays to escalate over time."}]},{level:"senior",q:"What are Android vitals, and which metrics matter most?",a:[{t:"p",text:"*Android vitals* is the Play Console's production quality dashboard, aggregating real-device metrics: *crash rate* and *ANR rate* (the core stability metrics — Google sets *bad-behavior thresholds* that can hurt discoverability if exceeded), plus *excessive wakeups*, *stuck partial wakelocks*, *slow/frozen frames*, and startup times. Watch crash-free and ANR-free rates closely during rollouts. Vitals is your *production* signal — it catches issues real users hit across devices you can't test locally."},{t:"list",items:["**Core** — crash rate + ANR rate (thresholds affect discoverability).","**Also** — wakeups, wakelocks, slow/frozen frames, startup.","**Watch** — during rollouts.","**Production signal** — real-user issues across devices."]},{t:"note",text:"Android vitals is Play's production quality dashboard of real-device metrics: crash rate and ANR rate (core — Google's bad-behavior thresholds can hurt discoverability), plus excessive wakeups/wakelocks, slow/frozen frames, startup times. Watch crash-free/ANR-free rates during rollouts. It catches issues real users hit on devices you can't test."}]},{level:"junior",q:"What is the app review process, and how long does it take?",a:[{t:"p",text:"Play *reviews* new apps and updates for *policy compliance* (content, permissions, data safety, restricted features) before they go live. Review times vary — often hours to a few days (new apps and sensitive permissions take longer; updates are usually faster). A release stays 'In review'/'Pending publication' until approved. Plan releases with buffer, don't schedule a hard launch for the instant you upload, and avoid last-minute policy-sensitive changes that trigger longer review."},{t:"list",items:["**Reviews** — policy compliance (content, permissions, data safety).","**Time** — hours to days; new/sensitive apps longer.","**Status** — 'In review' until approved.","**Plan** — buffer; avoid launch-instant-on-upload assumptions."]},{t:"note",text:"Play reviews new apps and updates for policy compliance (content, permissions, data safety, restricted features) before going live — often hours to a few days (new/sensitive apps longer, updates faster). Releases stay 'In review' until approved. Plan with buffer; don't assume instant publish, and avoid last-minute policy-sensitive changes that lengthen review."}]},{level:"senior",q:"What is the Data Safety section, and why does it matter?",a:[{t:"p",text:"The *Data Safety* form (in the Play Console) is a mandatory declaration of *what data your app collects/shares*, why, whether it's encrypted in transit, and whether users can request deletion — shown to users on your store listing. It must *accurately match your app's actual behavior* (including third-party SDKs' data practices). Inaccurate declarations are a *policy violation* that can get the app removed. Audit your SDKs' data collection to fill it correctly; update it when data practices change."},{t:"list",items:["**Declares** — data collected/shared, purpose, encryption, deletion.","**Shown to users** — on the store listing.","**Must match** — actual behavior incl. third-party SDKs.","**Inaccurate** — policy violation → possible removal."]},{t:"note",text:"The Data Safety form declares what data your app collects/shares, why, encryption-in-transit, and deletion options — shown on your store listing. It must accurately match actual behavior including third-party SDKs. Inaccurate declarations are a policy violation risking removal. Audit SDK data collection to fill it correctly and update on changes."}]},{level:"senior",q:"How do you design a safe release process for an app with millions of users?",a:[{t:"p",text:"Ladder builds through *internal → closed → open* testing, review the *pre-launch report*, then a *staged production rollout* (1% → grow) while monitoring *vitals* (crash/ANR-free) and *Crashlytics* at each stage. Gate features behind *Remote Config/feature flags* so you can disable a bad feature *without a new release*. Keep releases *small and frequent* (easier to diagnose), always retain *mapping.txt*, and have a *forward-fix* plan (since you can't roll back). Automate with CI + the Play Developer API."},{t:"list",items:["**Ladder** — internal → closed → open → staged production.","**Monitor** — vitals + Crashlytics at each stage.","**Feature flags** — disable bad features without a release.","**Forward-fix ready** — small frequent releases, keep mapping.txt."]},{t:"note",text:"Safe release at scale: ladder internal→closed→open, review the pre-launch report, staged production rollout (1%→) monitoring vitals + Crashlytics at each stage; gate features behind Remote Config/flags (disable without a release); keep releases small/frequent, retain mapping.txt, and plan forward-fixes (no rollback). Automate via CI + Play Developer API."}]},{level:"senior",q:"How do you handle a critical crash discovered at 100% rollout?",a:[{t:"p",text:"You can't recall the version, so: (1) if the bad feature is *flag-gated*, disable it via *Remote Config* immediately (fastest, no release); (2) *halt* any ongoing rollout; (3) build and ship a *forward-fix* as a *new higher-versionCode* release, expedited (consider a staged rollout that ramps quickly while watching vitals); (4) for the most severe cases, ship a *fix release* and monitor Crashlytics for the crash-free rate recovering. Post-incident: add a test/guard and a kill-switch to prevent recurrence."},{t:"list",items:["**Flag-gated?** — disable via Remote Config instantly.","**Halt** — any ongoing rollout.","**Forward-fix** — new higher-versionCode release, expedited.","**Monitor + prevent** — Crashlytics recovery; add kill-switch/test."]},{t:"note",text:"Critical crash at 100%: you can't recall it — if flag-gated, disable via Remote Config instantly (no release); halt ongoing rollouts; ship a forward-fix as a new higher-versionCode release (expedited, watch vitals); monitor Crashlytics for recovery. Post-incident: add a kill-switch and a test/guard to prevent recurrence."}]},{level:"junior",q:"What is the difference between managed publishing and timed publishing?",a:[{t:"p",text:"By default an approved release *publishes automatically*. *Managed publishing* lets you *hold* approved changes and release them *when you choose* (click 'Publish'), so you can align a store update with a marketing moment or coordinate multiple changes. It separates *review approval* from *going live*. Use it when timing matters (a coordinated launch) or to batch approved changes and publish together. Turn it on in the Console's Publishing overview."},{t:"list",items:["**Default** — approved releases publish automatically.","**Managed publishing** — hold approved changes, publish on click.","**Separates** — approval from going live.","**Use** — coordinated launches, batching changes."]},{t:"note",text:"By default approved releases publish automatically. Managed publishing lets you hold approved changes and publish them when you choose (separating review approval from going live) — for coordinated launches or batching multiple approved changes to publish together. Enable it in the Console's Publishing overview."}]},{level:"senior",q:"What is the Play Developer API, and how do you automate releases?",a:[{t:"p",text:"The *Play Developer (Publishing) API* lets you automate release tasks programmatically — upload an AAB, assign it to a track, set rollout percentage, update listing/metadata, and manage testers — using a *service account* with granted permissions. Tools like *fastlane (supply)* and the *Gradle Play Publisher* plugin wrap it. This enables CI/CD: a merge triggers a build that uploads to internal testing (or a staged production rollout) with no manual Console steps — faster, repeatable, less error-prone releases."},{t:"list",items:["**Automates** — upload AAB, set track/rollout, metadata, testers.","**Auth** — a service account with permissions.","**Tools** — fastlane supply, Gradle Play Publisher.","**Enables** — CI/CD releases without manual Console steps."]},{t:"note",text:"The Play Developer (Publishing) API automates release tasks — upload an AAB, assign a track, set rollout %, update metadata/testers — via a service account. Tools like fastlane (supply) and Gradle Play Publisher wrap it. Enables CI/CD: a merge builds and uploads to a track (or staged rollout) with no manual Console steps — faster, repeatable, less error-prone."}]},{level:"junior",q:"What is a store listing, and what affects install conversion?",a:[{t:"p",text:"The *store listing* is your app's Play page: title, short/full description, icon, screenshots, feature graphic, and (optionally) a promo video. These drive *install conversion* (visitors → installers): a clear icon, compelling screenshots showing real value, a concise benefit-led description, and good ratings. Play lets you run *store listing experiments (A/B tests)* on these assets to measure conversion. Also, *download size* and *ratings/reviews* strongly affect conversion. Treat the listing as an optimizable funnel."},{t:"list",items:["**Listing** — title, description, icon, screenshots, graphic, video.","**Conversion** — icon/screenshots/description/ratings drive it.","**A/B test** — store listing experiments on assets.","**Also** — download size and reviews affect conversion."]},{t:"note",text:"The store listing (title, descriptions, icon, screenshots, feature graphic, video) drives install conversion (visitors → installers) — clear icon, value-showing screenshots, benefit-led description, good ratings. Play offers store listing experiments (A/B tests) to measure asset changes. Download size and reviews also affect conversion. Treat the listing as an optimizable funnel."}]},{level:"senior",q:"What are store listing experiments (A/B testing on the Play page)?",a:[{t:"p",text:"*Store listing experiments* let you A/B test listing assets (icon, screenshots, description, feature graphic) on *real Play traffic* — Play shows variants to different user segments and measures which yields higher *install conversion* (with statistical significance), then you apply the winner. It's the data-driven way to optimize the top of your acquisition funnel, separate from optimizing the app itself. Run one variable at a time for clear signal, and let it run long enough for significance."},{t:"list",items:["**A/B test** — icon/screenshots/description on real traffic.","**Measures** — install conversion with significance.","**Apply the winner** — data-driven listing optimization.","**Best practice** — one variable at a time, sufficient duration."]},{t:"note",text:"Store listing experiments A/B test listing assets (icon, screenshots, description, feature graphic) on real Play traffic — Play serves variants to segments, measures install conversion with significance, and you apply the winner. The data-driven way to optimize the acquisition funnel. Test one variable at a time, run long enough for significance."}]},{level:"senior",q:"How do you handle a policy violation or app suspension?",a:[{t:"p",text:"Read the *violation notice* carefully to understand the exact policy and offending element (permission misuse, deceptive behavior, data-safety mismatch, restricted content). *Fix the issue* (remove the offending code/permission, correct the declaration), then *appeal* via the Console with a clear explanation of the remediation. To *avoid* violations: review policies before using sensitive permissions/features, keep Data Safety accurate, and test against Play's requirements. Repeated violations risk account termination, so treat notices seriously and promptly."},{t:"list",items:["**Understand** — the exact policy and offending element.","**Fix** — remove/correct the violating behavior.","**Appeal** — via the Console explaining remediation.","**Prevent** — review policies, accurate Data Safety, test."]},{t:"note",text:"On a policy violation/suspension: read the notice to pinpoint the exact policy and offending element (permission misuse, deception, data-safety mismatch), fix it (remove/correct), then appeal via the Console explaining the remediation. Prevent by reviewing policies before sensitive features, keeping Data Safety accurate, and testing. Repeated violations risk account termination — act promptly."}]},{level:"junior",q:"What is the difference between a production release and a testing release?",a:[{t:"p",text:"A *testing release* (internal/closed/open track) goes only to *testers* and doesn't affect your public rating or general users — safe to iterate. A *production release* goes to *all users* on the Play Store, counts toward your public rating and vitals, and is what most people install. You *promote* a validated build from testing to production (often via a staged rollout). Never push straight to production without testing-track validation and a pre-launch report."},{t:"list",items:["**Testing release** — testers only; safe to iterate.","**Production release** — all users; public rating + vitals.","**Promote** — validated build testing → production.","**Never** — skip testing-track validation."]},{t:"note",text:"A testing release (internal/closed/open) goes only to testers — no public-rating impact, safe to iterate. A production release goes to all users (counts toward rating/vitals). Promote a validated build from testing to production (often via staged rollout). Never push straight to production without testing-track validation and a pre-launch report."}]},{level:"senior",q:"How do you use Remote Config / feature flags with releases?",a:[{t:"p",text:"Ship features *disabled behind a flag* (Firebase Remote Config or your own), then *enable them remotely* — decoupling *code deployment* from *feature launch*. Benefits: turn a feature on gradually (or for a segment), *kill-switch* a broken feature *without a new release* (critical since you can't roll back an app version), run staged feature exposure independent of app rollout, and A/B test behaviors. Best practice for any risky feature: dark-launch it behind a flag so you retain control post-release."},{t:"list",items:["**Decouples** — code deployment from feature launch.","**Kill-switch** — disable a broken feature without a release.","**Gradual/segmented** — enable per rollout or audience.","**Dark-launch** — risky features behind a flag."]},{t:"note",text:"Ship features disabled behind a flag (Remote Config/own) and enable remotely — decoupling code deployment from feature launch. You can kill-switch a broken feature without a new release (vital since apps can't roll back), enable gradually/per-segment, and A/B test. Dark-launch any risky feature behind a flag to keep post-release control."}]},{level:"junior",q:"What are ratings and reviews' impact, and how do you request reviews well?",a:[{t:"p",text:"Ratings/reviews affect *store ranking* and *install conversion* (users trust higher-rated apps). To gather them well, use the *In-App Review API* — it shows a *native review card in-context* (no leaving the app) and Play controls frequency to avoid spamming. Trigger it at a *positive moment* (after a success/completion), *never* gate features on leaving a review or nag repeatedly (a policy violation). Respond to reviews to improve sentiment. Good in-app moments raise both volume and average rating."},{t:"list",items:["**Impact** — ranking + install conversion.","**In-App Review API** — native in-context card, frequency-capped.","**Trigger** — at a positive moment; never nag or gate.","**Respond** — to reviews to improve sentiment."]},{t:"note",text:"Ratings/reviews affect ranking and install conversion. Use the In-App Review API (native in-context card, Play-frequency-capped) triggered at a positive moment (after success) — never gate features on reviewing or nag repeatedly (policy violation). Respond to reviews to improve sentiment. Good in-app timing raises both volume and average rating."}]},{level:"senior",q:"How do you manage multiple app versions across tracks simultaneously?",a:[{t:"p",text:"Play lets each track hold its own active release, so you might have (say) v10 at 20% in production, v11 in open testing, and v12 in internal — progressing independently. Manage this by keeping *versionCodes strictly increasing across all tracks* (a testing build must out-number production), *promoting* builds up the ladder rather than rebuilding, and being careful that a *higher-versionCode testing build isn't accidentally offered to production users* (Play handles track eligibility, but coordinate codes). Use the Console's release dashboard to see all tracks at a glance."},{t:"list",items:["**Per-track releases** — different versions progress independently.","**Increasing versionCodes** — across all tracks.","**Promote** — up the ladder, don't rebuild.","**Dashboard** — view all tracks; coordinate codes."]},{t:"note",text:"Each track holds its own release (e.g. v10 at 20% production, v11 open, v12 internal) progressing independently. Keep versionCodes strictly increasing across all tracks (testing out-numbers production), promote builds up the ladder rather than rebuild, and coordinate codes so track eligibility stays correct. Use the Console release dashboard to view all tracks."}]},{level:"junior",q:"What is the difference between an update and a fresh install for users?",a:[{t:"p",text:"An *update* replaces the installed app *in place* — user data, preferences, and databases persist (same signing key required), and it can happen automatically (auto-update) or via the In-App Updates API. A *fresh install* is a new user getting the app for the first time (no prior data), or a user who *uninstalled and reinstalled* (data gone unless backed up). Updates must preserve data (test migrations!); fresh installs go through onboarding. Play distinguishes these in acquisition metrics."},{t:"list",items:["**Update** — in-place; data persists; same signing key.","**Fresh install** — first-time or reinstall; no prior data.","**Updates** — must preserve data (test migrations).","**Fresh** — onboarding path."]},{t:"note",text:"An update replaces the app in place — data/preferences/databases persist (same signing key), auto or via In-App Updates. A fresh install is a first-time user (or reinstall, data gone unless backed up). Updates must preserve data (test Room migrations!); fresh installs hit onboarding. Play separates these in acquisition metrics."}]},{level:"senior",q:"How do you monitor a release's health in the first hours?",a:[{t:"p",text:"Immediately after (and during) a rollout, watch *Crashlytics* for new/spiking crashes on the new versionCode (velocity alerts), *Android vitals* for crash/ANR-free rates dipping, and *reviews* for sudden negative feedback. Compare the new version's metrics against the prior version at the *same rollout stage*. Keep the rollout *small initially* so problems surface at 1% not 100%. If metrics degrade, *halt* and *forward-fix* (or flag-disable). Have alerting so you're notified rather than polling."},{t:"list",items:["**Crashlytics** — new/spiking crashes on the new versionCode.","**Vitals** — crash/ANR-free rate dips.","**Compare** — vs prior version at the same stage.","**Degrade → halt + forward-fix**; alerting, not polling."]},{t:"note",text:"Post-release, watch Crashlytics for new/spiking crashes on the new versionCode (velocity alerts), Android vitals for crash/ANR-free dips, and reviews for sudden negatives — comparing against the prior version at the same rollout stage. Keep the initial % small so issues surface early. Degrade → halt + forward-fix/flag-disable. Set alerts, don't poll."}]},{level:"senior",q:"What is the difference between forced updates and recommended updates?",a:[{t:"p",text:"A *recommended* (flexible) update *suggests* the user update but lets them continue on the old version — good for routine improvements. A *forced* (immediate) update *blocks* app use until updated — reserve it for genuinely necessary cases: a critical security/data bug, a broken API contract, or a legal requirement, since forcing interrupts users. Implement forcing via the *In-App Updates immediate flow* (using update *priority*/staleness) or a *server-driven minimum version* check that gates the app. Overusing forced updates frustrates users."},{t:"list",items:["**Recommended** — suggest; user can keep using old version.","**Forced** — block until updated; for critical cases only.","**Implement** — immediate In-App Update / min-version gate.","**Overuse** — frustrates users; reserve for necessity."]},{t:"note",text:"Recommended (flexible) updates suggest updating but let users continue; forced (immediate) updates block use until updated — reserve for critical security/data bugs, broken API contracts, or legal needs. Implement via the immediate In-App Update flow (priority/staleness) or a server-driven minimum-version gate. Overusing forced updates frustrates users."}]},{level:"senior",q:"How do you handle target API level requirement deadlines from Play?",a:[{t:"p",text:"Play *raises the required `targetSdkVersion`* each year — after the deadline, you *can't submit updates* (and eventually new installs may be restricted on newer devices) unless you target a recent API level. Plan ahead: bump `targetSdk` well before the deadline, *handle that level's behavior changes* (permissions, background limits, scoped storage, etc.), and test thoroughly (behavior changes can break things silently). Existing apps get an extension window but shouldn't rely on it. Treat the annual target bump as scheduled maintenance."},{t:"list",items:["**Annual bump** — Play raises required targetSdk yearly.","**After deadline** — can't submit updates without it.","**Handle** — that level's behavior changes; test thoroughly.","**Plan ahead** — treat as scheduled maintenance."]},{t:"note",text:"Play raises the required targetSdkVersion yearly — after the deadline you can't submit updates (and new installs may be restricted on newer devices) without targeting a recent level. Bump targetSdk ahead of the deadline, handle that level's behavior changes (permissions, background limits, scoped storage), and test thoroughly. Treat the annual bump as scheduled maintenance."}]},{level:"junior",q:"What is Play Console's release dashboard and 'release with confidence' flow?",a:[{t:"p",text:"The Console guides a structured release: create a release on a track, upload the AAB, add *release notes*, review the *pre-launch report* and any *errors/warnings* Play flags (missing mapping file, oversized, policy issues), set the *rollout percentage*, and confirm. It surfaces *pre-release checks* (deobfuscation file, target API level, permissions) before you publish. Following this flow (not skipping the warnings) prevents common release mistakes. The dashboard shows each track's current release and rollout state."},{t:"list",items:["**Guided flow** — track → AAB → notes → checks → rollout %.","**Pre-release checks** — mapping file, target API, permissions.","**Heed warnings** — don't skip flagged errors.","**Dashboard** — per-track release + rollout state."]},{t:"note",text:"The Console guides releases: create a release on a track, upload the AAB, add release notes, review the pre-launch report and Play's warnings (missing mapping file, oversized, target API, permissions), set rollout %, confirm. Heeding these pre-release checks prevents common mistakes. The dashboard shows each track's current release and rollout state."}]}],lT={id:"play-console",title:"Play Console: Tracks, Rollouts & Updates",tagline:"Testing tracks (internal → production), staged rollouts, in-app updates (flexible vs immediate), the pre-launch report, and a safe release process at scale.",tags:["Play Console","Testing Tracks","Staged Rollout","In-App Updates"],content:sT,qa:rT},cT={id:"distribution",name:"Build, Distribution & Play Store",description:"Shipping the app — Gradle & the build system, signing/R8/versioning, AAB/APK & Play App Signing, and the Play Console (tracks, rollouts, in-app updates).",topics:[ZC,aT,oT,lT]},dT=[{heading:"What FCM is and why it matters",blocks:[{t:"p",text:"**Firebase Cloud Messaging (FCM)** is Google's service for sending **push notifications** and data messages from a server to devices. It's the standard way to deliver real-time, server-initiated updates to an app — a chat message, a breaking-news alert, a 'your order shipped' notification. Crucially, FCM is *far more battery-efficient* than the app polling a server, because it uses a *single, shared, persistent connection* the OS maintains for all apps, and it can wake an app even from Doze. This is why 'use push, not polling' is the recommended pattern for real-time server-driven updates."},{t:"list",items:["**Server → device**: your backend tells FCM 'send this to this device/topic', and FCM delivers it — the app doesn't have to be running or polling.","**Battery-efficient**: the OS maintains *one* connection to Google's servers shared across all apps (not one per app), and delivers messages over it — vastly cheaper than every app holding its own connection or polling. A *high-priority* message can even wake the app from Doze.","**The alternative it replaces**: polling (the app repeatedly asking 'anything new?') wastes battery and data and is laggy. FCM push notifies only when there's genuinely something, in near-real-time."]}]},{heading:"Registration tokens",blocks:[{t:"p",text:"To send a message to a specific device, the server needs to address it — that's the **registration token** (FCM token). Each app instance gets a unique token from FCM; the app sends this token to *your* backend, which stores it and uses it to target messages to that device. Managing the token lifecycle correctly is essential and a common interview point."},{t:"code",title:"Getting and refreshing the token",code:`// Get the current token
FirebaseMessaging.getInstance().token.addOnCompleteListener { task ->
    val token = task.result
    sendTokenToYourServer(token)     // your backend stores it to target this device
}

// The token can CHANGE — you must handle refresh
class MyMessagingService : FirebaseMessagingService() {
    override fun onNewToken(token: String) {
        sendTokenToYourServer(token)  // send the NEW token to your backend
    }
}`},{t:"list",items:["**The token identifies the app instance on this device** — your server uses it as the 'address' to send a message to that device.","**Tokens can change / rotate** — on app reinstall, data clear, restore to a new device, or periodically for security. FCM calls `onNewToken` when it does; you *must* send the new token to your backend, or messages will stop reaching that device (they'd go to a dead token). Not handling `onNewToken` is a classic bug (notifications silently stop for some users).",'**Topics** — instead of individual tokens, devices can *subscribe to topics* (`subscribeToTopic("news")`), and the server sends to a topic to reach all subscribers at once — good for broadcast-style messaging (news, announcements) without managing individual tokens.']}]},{heading:"Notification vs data messages — the critical distinction",blocks:[{t:"p",text:"FCM has two message types, and their *handling behavior differs significantly* depending on whether the app is in the foreground or background — this is *the* most important FCM concept and a very common interview question."},{t:"table",headers:["","Notification message","Data message"],rows:[["Contains","a `notification` payload (title, body)","custom key-value `data` only"],["App in foreground","delivered to onMessageReceived (you handle it)","delivered to onMessageReceived"],["App in background","**system auto-displays it** in the tray; onMessageReceived NOT called","delivered to onMessageReceived (you build the notification)"],["Control over display","limited (system builds it when backgrounded)","full (you always build it in code)"]]},{t:"list",items:["**Notification message** — has a `notification` block (title/body). When the app is *backgrounded*, the *system automatically displays* it in the notification tray, and your `onMessageReceived` is *not* called. When *foregrounded*, it comes to `onMessageReceived` for you to handle. So you get automatic display when backgrounded but *limited control*.","**Data message** — has only a custom `data` payload (your key-value pairs). It's *always* delivered to `onMessageReceived` (foreground *and* background), and *you build the notification in code*. This gives *full control* (custom logic, conditional display, updating app state) but *you're responsible* for showing anything.","**The common gotcha**: developers send a *notification* message and wonder why `onMessageReceived` isn't called when the app is backgrounded (it's the system displaying it, not your code). If you need to *always* run your code (to build a custom notification, update the DB, decide whether to notify), use a *data* message. Many apps use data messages precisely for this control.","**Combined messages** (both `notification` and `data`) exist but have the notification-message backgrounding behavior — so for guaranteed code execution, prefer pure data messages."]}]},{heading:"Handling messages and building notifications",blocks:[{t:"code",title:"onMessageReceived and posting a notification",code:`class MyMessagingService : FirebaseMessagingService() {
    override fun onMessageReceived(message: RemoteMessage) {
        // For a DATA message (or foreground notification message):
        val title = message.data["title"]
        val body = message.data["body"]

        // Build and show the notification yourself
        val notification = NotificationCompat.Builder(this, CHANNEL_ID)
            .setContentTitle(title)
            .setContentText(body)
            .setSmallIcon(R.drawable.ic_notification)
            .setContentIntent(pendingIntentToOpenScreen())  // where tapping goes
            .build()
        NotificationManagerCompat.from(this).notify(id, notification)
    }
}`},{t:"list",items:["**Notification channels** (required since Android 8) — every notification must belong to a *channel* (a category the user can configure — sound, importance, on/off per channel). You create channels once; the user controls them in system settings.","**`POST_NOTIFICATIONS` runtime permission** (Android 13+) — you must request this permission to show notifications; without it, notifications are silently dropped. A newer requirement to handle.","**Tapping a notification** — set a `PendingIntent` (usually with a deep link) so tapping opens the right screen with the right data.","**`onMessageReceived` is for foreground + data messages** — do quick work there (build the notification, update local state); for heavy work, hand off to WorkManager. There's a time limit (~10-20s) and, for background data messages, high-priority is needed to run reliably."]},{t:"note",text:"FCM: server→device push, battery-efficient (one shared OS connection, wakes from Doze) — use it instead of polling for real-time updates. Registration token addresses a device; handle onNewToken (tokens rotate — send new ones to your backend or notifications stop) and topics for broadcast. Notification message: system auto-displays when backgrounded (onMessageReceived NOT called), limited control. Data message: always delivered to onMessageReceived (you build the notification), full control — use for guaranteed code execution. Notification channels (Android 8+) and POST_NOTIFICATIONS permission (Android 13+) required."}]}],hT=[{level:"junior",q:"What is FCM and why use it instead of polling?",a:[{t:"p",text:"**FCM (Firebase Cloud Messaging) is Google's service for sending push notifications and data messages from a server to devices — the standard way to deliver real-time, server-initiated updates (a chat message, a news alert, an order update).** You use it instead of polling because it's far more battery-efficient and delivers updates in near-real-time."},{t:"list",items:["**Push, not poll**: with polling, the app repeatedly asks the server 'anything new?' on a timer — wasteful (most requests return nothing, burning battery and data) and laggy (updates only arrive on the next poll). With FCM, the *server* pushes a message the moment there's something, so the app does nothing until there's genuinely an update.","**Battery-efficient by design**: the OS maintains *one* persistent connection to Google's servers *shared across all apps* (not one per app), and delivers messages over it — vastly cheaper than every app holding its own connection or polling.","**Wakes from Doze**: a high-priority FCM message can wake the app even when the device is in Doze (deep sleep), enabling genuinely real-time delivery — which polling can't do (it's deferred by Doze)."]},{t:"p",text:"So FCM is the recommended pattern for anything requiring real-time server-driven updates. The typical flow: the app gets a registration token and sends it to your backend; when there's something to deliver, your backend tells FCM to send to that token; FCM delivers it, waking the app if needed. This 'server notifies the app' model is far more efficient than the app constantly checking — which is also why, when you need frequent updates (more often than WorkManager's 15-minute periodic minimum allows), the answer is push, not more polling."}]},{level:"junior",q:"What is an FCM registration token and why must you handle onNewToken?",a:[{t:"p",text:"**A registration token (FCM token) is a unique identifier for an app instance on a specific device — it's the 'address' your server uses to target a push message to that device. The app gets its token from FCM and sends it to *your* backend, which stores it and uses it to send messages to that device.**"},{t:"list",items:["**It addresses a device**: to send a message to a specific device, the server needs its token. So the app must retrieve its token and register it with your backend.","**Tokens can change (rotate)** — on app reinstall, clearing app data, restoring to a new device, or periodically for security. FCM notifies you via `onNewToken(token)` when this happens.","**You *must* handle `onNewToken`** — when the token changes, you have to send the *new* token to your backend. If you don't, your backend keeps using the *old, now-invalid* token, and messages to that device silently stop being delivered (they go to a dead token)."]},{t:"p",text:"This is a classic FCM bug: notifications work initially, then silently stop for some users — because their token rotated (e.g. after a reinstall or restore) and the app didn't update the backend with the new one, so the backend is sending to a dead address. Correct handling means overriding `onNewToken` in your `FirebaseMessagingService` and sending the new token to your backend every time it fires, *plus* fetching and registering the current token on app start. Getting the token lifecycle right — retrieve on start, update on `onNewToken` — is essential for reliable delivery. (For broadcast-style messaging where you don't need to target individual devices, *topics* are an alternative — devices subscribe to a topic and the server sends to the topic, avoiding per-device token management.)"}]},{level:"junior",q:"How do you handle a notification when a user taps it?",a:[{t:"p",text:"**You attach a `PendingIntent` to the notification (usually one that triggers a deep link), so that tapping the notification opens the right screen with the right data.** The `PendingIntent` is set as the notification's content intent, and the system fires it when the user taps."},{t:"code",title:"Notification with a tap destination",code:`val intent = Intent(this, MainActivity::class.java).apply {
    putExtra("screen", "orderDetail")
    putExtra("orderId", orderId)
}
val pendingIntent = PendingIntent.getActivity(
    this, 0, intent, PendingIntent.FLAG_IMMUTABLE or PendingIntent.FLAG_UPDATE_CURRENT
)
val notification = NotificationCompat.Builder(this, CHANNEL_ID)
    .setContentTitle(title).setContentText(body)
    .setSmallIcon(R.drawable.ic_notif)
    .setContentIntent(pendingIntent)   // fired on tap
    .setAutoCancel(true)               // dismiss the notification when tapped
    .build()`},{t:"p",text:"The `PendingIntent` is a token that lets the *system* fire your intent on your behalf when the user taps — you build the intent describing where to go (the target Activity plus extras like the order id, or a deep-link URI), wrap it in a `PendingIntent`, and set it as the notification's content intent. Best practice is to use a *deep link* so the notification routes to the correct destination and the app handles it consistently (whether cold-starting or already running), and to pass any needed data (the order id) as extras so the target screen knows what to show. On Android 12+ you must specify the PendingIntent's mutability (`FLAG_IMMUTABLE` for security unless you need it mutable). Setting `setAutoCancel(true)` dismisses the notification when tapped. So the answer connects notifications back to the PendingIntent/deep-link mechanism — tapping fires a PendingIntent that opens the intended screen."}]},{level:"senior",q:"What is the difference between a notification message and a data message in FCM, and why does it matter?",a:[{t:"p",text:"**The difference is in *who displays the notification* and *whether your code runs*, and it changes based on whether the app is foreground or background — which is the single most important (and most commonly misunderstood) FCM concept. A *notification message* is auto-displayed by the system when the app is backgrounded (your code doesn't run); a *data message* is always delivered to your `onMessageReceived`, so *you* build the notification and your code always runs.**"},{t:"list",items:["**Notification message** — contains a `notification` payload (title, body). When the app is in the *foreground*, it's delivered to `onMessageReceived` for you to handle. But when the app is in the *background*, **the system automatically displays it in the notification tray and your `onMessageReceived` is NOT called** — the OS handles display using the payload. So you get automatic display but *limited control* and *no code execution* when backgrounded.","**Data message** — contains only a custom `data` payload (your key-value pairs, no `notification` block). It's **always delivered to `onMessageReceived`**, in *both* foreground and background, so *you* build and show the notification in code. This gives *full control* — you can run custom logic, decide whether/what to display, update local state, or do work — but *you're responsible* for showing anything (nothing appears automatically)."]},{t:"list",items:["**Why it matters — the classic bug**: a developer sends a *notification* message and adds logic in `onMessageReceived` (custom display, updating the database, analytics), then wonders why it doesn't run when the app is backgrounded. The reason: for a backgrounded notification message, the *system* displays it and never calls their code. The fix is to send a *data* message, which always invokes `onMessageReceived`.","**When to use each**: use a *notification* message for simple 'just show this text' notifications where you're fine with the system displaying it and don't need custom handling. Use a *data* message when you need *guaranteed code execution* — building a custom/rich notification, running conditional logic (only notify if the user isn't already on that screen), updating app state (mark data as needing refresh), or doing background work. Many production apps use data messages precisely for this control, building all notifications themselves.","**The combined-message caveat**: a message with *both* `notification` and `data` blocks behaves like a *notification* message when backgrounded (system displays the notification part, `onMessageReceived` gets only the data if tapped) — so it does *not* give you guaranteed background code execution. For reliable code execution on every message, use a *pure data* message.","**Background data-message reliability**: to run reliably in the background (especially in Doze), data messages should be sent with *high priority*; normal-priority background messages may be deferred. And `onMessageReceived` has a time limit (~10-20s), so heavy work should be handed to WorkManager."]},{t:"p",text:"**The senior framing**: the notification-vs-data distinction is fundamentally about the *tradeoff between convenience and control* — notification messages are convenient (the system displays them automatically when backgrounded) but take control away (your code doesn't run in that case); data messages give full control (always deliver to your code) but require you to do the work (build everything yourself). The critical, non-obvious behavior — that backgrounded notification messages *bypass your `onMessageReceived`* — is where nearly everyone gets caught, so the mature answer leads with that. The practical rule: if you need your code to run on every message (custom display, state updates, logic), use *data* messages with high priority; if you just want to show simple text and don't care about custom handling, notification messages are simpler. Understanding the foreground/background × notification/data matrix, the 'system displays backgrounded notification messages without calling your code' gotcha, and the guidance to use data messages for guaranteed control is exactly the FCM depth these questions probe."}]},{level:"senior",q:"Design a reliable push notification system for a chat app. What would you consider?",a:[{t:"p",text:"**I'd use FCM data messages (for guaranteed code execution and control) with high priority (for real-time delivery through Doze), a robust token lifecycle, server-side targeting and fan-out, and careful handling of delivery edge cases — plus fallbacks, since push is best-effort. A chat app needs *timely, reliable, correctly-targeted* notifications, so each of these matters.**"},{t:"list",items:["**Data messages, high priority**: use *data* messages (not notification messages) so `onMessageReceived` always runs — letting me build a rich notification (sender, message preview, avatar), decide *whether* to notify (suppress if the user is already in that chat), update the local message store, and update the unread badge. Send them *high priority* so they wake the app from Doze for real-time delivery — chat needs immediacy.","**Token lifecycle done right**: retrieve the token on app start and on `onNewToken`, and register it with the backend *associated with the user account* (so messages target the right user's current devices). Handle *multiple devices per user* (a user logged in on phone + tablet — send to all their tokens), and *remove stale tokens* on logout and when the backend gets a 'not registered' error from FCM (so it stops sending to dead tokens). Token hygiene is critical — most 'notifications stopped working' bugs are token-management failures.","**Server-side targeting and fan-out**: the backend maps a chat message → the recipient user(s) → their device tokens, and sends via FCM. For group chats, fan out to all members' tokens. Consider FCM's batch/multicast send for efficiency. Include the data needed to build the notification (sender, preview, chat id, message id) and to deep-link on tap.","**Handle delivery is best-effort — don't rely solely on push**: FCM does *not* guarantee delivery (a device offline for a long time may have messages dropped/collapsed; push can be delayed). So the source of truth for messages is the *backend* (and local cache), not the notification — when the app opens or reconnects, it *syncs* missed messages from the server (or via a WebSocket/long-lived connection while active). Push is for *timely notification*, not the message transport itself. This is essential: never treat a push as the only way a message arrives.","**Collapse and priority**: use a *collapse key* so many rapid messages in one chat collapse to the latest notification rather than flooding (or update a single notification with a count). Balance high priority (real-time, wakes device) against battery — chat justifies high priority, but not every message needs to wake a sleeping device.","**Notification UX**: notification channels (a 'Messages' channel the user can configure), `POST_NOTIFICATIONS` permission handling (Android 13+), grouping notifications per chat, a *reply action* (inline reply via `RemoteInput`) so users respond from the notification, deep-link on tap to the specific chat, and marking read to clear the notification across devices.","**Suppress when appropriate**: don't notify if the user is *actively viewing that chat* (check app state in `onMessageReceived`), and sync read state across devices so a message read on one device clears its notification on others."]},{t:"list",items:["**Edge cases and reliability**: handle the app being killed (data message with high priority can still trigger `onMessageReceived`, but very heavy work should defer to WorkManager); handle notification permission denied (degrade gracefully — the in-app experience still works via sync); handle multiple devices and read-state sync; and monitor delivery (FCM provides some delivery data / BigQuery export) to catch systemic failures."]},{t:"p",text:"**The senior framing**: a reliable chat push system rests on a few key insights — (1) use *data messages* for the control/code-execution chat needs (custom notifications, suppression, state updates), high-priority for real-time; (2) *token lifecycle hygiene* (multi-device, refresh, stale removal) is where reliability is usually won or lost; (3) *push is best-effort, not a message transport* — the backend is the source of truth and the app *syncs* missed messages on open/reconnect, so a dropped or delayed push never means a lost message; and (4) thoughtful UX (collapse, grouping, inline reply, suppress-when-viewing, cross-device read sync) makes it feel polished. The most important architectural point is decoupling *notification* (FCM, timely but unreliable) from *message delivery* (backend sync, reliable) — treating push as a nudge to sync rather than the data channel itself. Demonstrating the data-message choice, token-lifecycle rigor, the push-is-not-transport principle, and the UX considerations is the comprehensive answer that shows you've thought about real-world push reliability, not just the happy path."}]},{level:"senior",q:"How does FCM deliver a message end-to-end?",a:[{t:"p",text:"Your *app server* sends a message (with a device token or topic) to the *FCM backend* via the HTTP v1 API. FCM authenticates it, queues it, and delivers it to the device over a *persistent connection* maintained by Google Play services. On the device, Play services routes it to your `FirebaseMessagingService`. FCM handles connectivity, retries, and battery-efficient delivery — you never hold your own socket. If the device is offline, FCM stores the message (subject to TTL) and delivers when it reconnects."},{t:"list",items:["**App server** — sends via HTTP v1 API (token/topic).","**FCM backend** — auth, queue, deliver.","**Persistent connection** — via Play services.","**Offline** — stored (TTL) and delivered on reconnect."]},{t:"note",text:"End-to-end: your app server sends to FCM (HTTP v1, token/topic) → FCM authenticates, queues, and delivers over Play services' persistent connection → the device routes to your FirebaseMessagingService. FCM handles connectivity, retries, and battery-efficient delivery; offline devices get stored messages (subject to TTL) on reconnect."}]},{level:"senior",q:"How is message handling different when the app is in the foreground versus background?",a:[{t:"p",text:"It depends on the *message type*. A *notification message* while the app is *backgrounded* is displayed *automatically* by the system (in the tray) — `onMessageReceived` is NOT called; the data (if any) arrives in the launch intent extras on tap. A *notification message* while *foregrounded* delivers to `onMessageReceived` (you display it). A *data message* *always* delivers to `onMessageReceived` (foreground or background) — you must build the notification yourself. This distinction trips up many developers."},{t:"table",headers:["Message type","Foreground","Background"],rows:[["Notification","onMessageReceived","Auto-shown in tray (no callback)"],["Data-only","onMessageReceived","onMessageReceived"],["Notification + data","onMessageReceived","Auto-shown; data in tap intent"]]},{t:"note",text:"Notification message backgrounded → system auto-shows it, onMessageReceived NOT called (data arrives in the tap intent). Notification foregrounded → onMessageReceived (you display). Data-only → always onMessageReceived (you build the notification). This foreground/background × type matrix is a classic FCM gotcha."}]},{level:"junior",q:"What is onMessageReceived, and when is it called?",a:[{t:"p",text:"`onMessageReceived(RemoteMessage)` is the callback in your `FirebaseMessagingService` where you *receive and handle* a message. It's called for *data messages always* (foreground and background), and for *notification messages only when the app is in the foreground*. Inside it you read `remoteMessage.data`/`.notification` and typically build a notification with `NotificationCompat` or trigger a sync. It runs on a background thread with ~10–20s to finish — for longer work, enqueue WorkManager."},{t:"code",title:"onMessageReceived",code:`class MyFirebaseService : FirebaseMessagingService() {
  override fun onMessageReceived(msg: RemoteMessage) {
    val title = msg.data["title"] ?: msg.notification?.title
    showNotification(title, msg.data["body"])   // build with NotificationCompat
    // for heavy work: WorkManager.getInstance(this).enqueue(...)
  }
}`},{t:"note",text:"onMessageReceived (in FirebaseMessagingService) handles a message — called for data messages always, and notification messages only in the foreground. Read remoteMessage.data/.notification and build a notification or trigger a sync. It has ~10–20s on a background thread; enqueue WorkManager for longer work."}]},{level:"junior",q:"What are notification channels, and why are they required?",a:[{t:"p",text:"Since Android 8 (Oreo), every notification must belong to a *channel* — a user-visible category (e.g. 'Messages', 'Promotions') with its own importance, sound, vibration, and light settings that *the user controls*. You create channels once at startup (`NotificationManager.createNotificationChannel`). If you post a notification without a valid channel on 8+, it won't show. Channels give users fine-grained control (mute promotions, keep messages) instead of all-or-nothing, improving trust and reducing opt-outs."},{t:"code",title:"Creating a channel",code:`val channel = NotificationChannel("messages", "Messages", NotificationManager.IMPORTANCE_HIGH)
getSystemService(NotificationManager::class.java).createNotificationChannel(channel)
// then post with NotificationCompat.Builder(ctx, "messages")`},{t:"note",text:"Since Android 8, every notification needs a channel — a user-controlled category (importance, sound, vibration). Create channels at startup (createNotificationChannel); without a valid channel, notifications won't show on 8+. Channels let users mute categories individually (promotions vs messages) instead of all-or-nothing — better trust, fewer opt-outs."}]},{level:"senior",q:"What is message priority (high vs normal) in FCM, and how does it affect delivery?",a:[{t:"p",text:'FCM messages have a *priority*: *high* priority wakes the device (even in Doze) to deliver immediately — for time-sensitive content (chat, calls). *Normal* priority may be *delayed/batched* to save battery (delivered in a maintenance window). Notification messages default to high; data messages default to normal (set `"priority": "high"` explicitly if urgent). Overusing high priority for non-urgent messages wastes battery and, if abused, Google may throttle you. Match priority to genuine urgency.'},{t:"list",items:["**High** — wakes device (even Doze), immediate; for urgent content.","**Normal** — may be delayed/batched to save battery.","**Data messages** — default normal; set high if urgent.","**Don't abuse high** — battery cost, possible throttling."]},{t:"note",text:"FCM priority: high wakes the device (even in Doze) for immediate delivery (chat/calls); normal may be batched/delayed to save battery. Notification messages default high, data messages default normal (set priority:high if urgent). Don't abuse high for non-urgent messages — battery cost and possible throttling. Match priority to real urgency."}]},{level:"junior",q:"What is FCM topic messaging?",a:[{t:"p",text:"*Topics* let you send one message to *all devices subscribed to a named topic* (e.g. `news`, `weather_london`) without managing individual tokens. Devices subscribe with `subscribeToTopic(\"news\")`; your server sends to `/topics/news` and FCM fans it out. Great for broadcast content (announcements, categories the user opts into). Subscriptions are managed by FCM (persist across app restarts). Topics aren't for targeting *specific users* (use tokens for that) — they're for *group broadcast*."},{t:"code",title:"Subscribe to a topic",code:`Firebase.messaging.subscribeToTopic("news")
  .addOnCompleteListener { /* subscribed */ }
// server sends to condition/topic "news" → all subscribers receive it`},{t:"note",text:"Topics send one message to all devices subscribed to a named topic (news, weather_london) — no token management. Devices subscribeToTopic; server sends to the topic and FCM fans out. Great for opt-in broadcast content; FCM persists subscriptions. Not for targeting specific users (use tokens) — topics are for group broadcast."}]},{level:"senior",q:"How do you send to multiple devices (multicast, topics, conditions)?",a:[{t:"p",text:"Options: send to *individual tokens* (loop, or *batch/multicast* up to 500 tokens per request with `sendEachForMulticast`); use *topics* for opt-in broadcast groups; or use *conditions* (boolean topic expressions like `'news' in topics && 'sports' in topics`) to target intersections/unions. For large user bases, topics/conditions scale better than managing millions of tokens. FCM returns per-token results so you can *prune invalid tokens* (unregistered) from your database."},{t:"list",items:["**Tokens** — batch/multicast up to 500 per request.","**Topics** — opt-in broadcast groups.","**Conditions** — boolean topic expressions (intersections).","**Prune** — remove invalid/unregistered tokens from per-result."]},{t:"note",text:"Send to many via: individual tokens (batch/multicast up to 500 per request), topics (opt-in broadcast), or conditions (boolean topic expressions for intersections/unions). Topics/conditions scale better than millions of tokens. Use FCM's per-token results to prune invalid/unregistered tokens from your DB."}]},{level:"senior",q:"What is TTL and collapse key in FCM?",a:[{t:"p",text:"*TTL (time-to-live)* sets how long FCM stores a message for an offline device before dropping it (default ~4 weeks; set to 0 for 'deliver now or discard' — good for real-time-only data). A *collapse key* groups messages so that when the device comes online, *only the latest* message in a group is delivered (older ones collapsed) — useful for 'sync now' or state-update pings where only the newest matters, avoiding a backlog of redundant messages. Both control delivery of stored/queued messages."},{t:"list",items:["**TTL** — how long FCM stores for an offline device (0 = now-or-never).","**Collapse key** — deliver only the latest of a group.","**Collapse use** — sync pings/state updates (newest matters).","**Both** — control stored/queued delivery."]},{t:"note",text:"TTL: how long FCM stores a message for an offline device before dropping (default ~4 weeks; 0 = deliver-now-or-discard for real-time-only). Collapse key: groups messages so only the latest in a group is delivered on reconnect (older collapsed) — for sync/state pings where only the newest matters. Both govern stored/queued delivery."}]},{level:"junior",q:"How do you handle the POST_NOTIFICATIONS permission on Android 13+?",a:[{t:"p",text:"Since Android 13 (API 33), posting notifications requires the *runtime permission* `POST_NOTIFICATIONS` — the user must grant it (like other runtime permissions). Declare it in the manifest and *request it at an appropriate moment* (with rationale — after the user sees why notifications help, not on first launch). If denied, notifications silently don't show. On pre-13 devices, notifications are allowed by default (no request). Handle the denied case gracefully and offer a path to settings."},{t:"list",items:["**Android 13+** — `POST_NOTIFICATIONS` runtime permission.","**Declare + request** — at a good moment with rationale.","**Denied** — notifications silently don't show.","**Pre-13** — allowed by default."]},{t:"note",text:"Android 13+ requires the POST_NOTIFICATIONS runtime permission to show notifications — declare it and request at a good moment (with rationale, not on first launch). Denied → notifications silently don't show; offer a path to settings. Pre-13 devices allow notifications by default. Handle the denied case gracefully."}]},{level:"senior",q:"What is the difference between the FCM HTTP v1 API and the legacy API?",a:[{t:"p",text:"The *legacy* HTTP/XMPP API used a static *server key* for auth and a flatter JSON. The *HTTP v1 API* (current, required — legacy is deprecated/shut down) uses *OAuth2 access tokens* from a *service account* (more secure, short-lived), a *structured message format* with platform-specific overrides (`android`, `apns`, `webpush`), and better error handling. Migrate to v1: generate tokens from a service account (via the Admin SDK or Google auth libraries) and use the new payload shape. Never embed a server key in the app."},{t:"list",items:["**Legacy** — static server key; deprecated/shut down.","**HTTP v1** — OAuth2 from a service account (secure, short-lived).","**v1 format** — platform overrides (android/apns/webpush).","**Migrate** — Admin SDK/auth libs; never embed keys in the app."]},{t:"note",text:"Legacy FCM API: static server key auth, flat JSON — deprecated/shut down. HTTP v1 (current): OAuth2 access tokens from a service account (secure, short-lived), structured payload with platform overrides (android/apns/webpush), better errors. Migrate via the Admin SDK/Google auth libs. Never embed a server key in the app."}]},{level:"senior",q:"How does Doze mode affect FCM message delivery?",a:[{t:"p",text:"In *Doze* (device idle), *normal-priority* messages are *deferred* to maintenance windows (may be delayed significantly), while *high-priority* messages can *wake the device* and deliver immediately (with temporary allowances to run briefly). So for time-critical delivery (chat, calls), use high priority; for non-urgent, normal priority respects Doze and saves battery. Don't set everything high — Google can *throttle* apps that abuse high priority. Design your message priorities around actual urgency and Doze behavior."},{t:"list",items:["**Normal priority** — deferred to maintenance windows in Doze.","**High priority** — wakes device, immediate (brief allowance).","**Urgent** — high; non-urgent — normal (battery-friendly).","**Abuse** — Google throttles over-use of high."]},{t:"note",text:"In Doze, normal-priority messages are deferred to maintenance windows; high-priority can wake the device for immediate delivery (with a brief run allowance). Use high for time-critical (chat/calls), normal for non-urgent (respects Doze, saves battery). Don't set everything high — Google throttles abuse. Design priorities around real urgency."}]},{level:"senior",q:"What are data-only (silent) messages used for?",a:[{t:"p",text:"*Data-only* messages carry a custom key-value payload and *no notification* — they always hit `onMessageReceived`, letting the app *react silently*: trigger a background sync, update cached data, invalidate a token, or decide *whether and how* to show a notification (full client control over presentation/localization). Caveats: they're subject to *background restrictions* (normal priority may be delayed in Doze; the app must not be force-stopped), and heavy work needs WorkManager. Use them when the client should decide the UX, or for pure sync."},{t:"list",items:["**No notification** — always `onMessageReceived`.","**Uses** — silent sync, cache update, client-decided UI.","**Full control** — presentation/localization on-device.","**Caveats** — Doze delays (normal priority); WorkManager for heavy work."]},{t:"note",text:"Data-only (silent) messages carry key-value data and no notification — always onMessageReceived, so the app reacts silently: background sync, cache update, token invalidation, or client-decided notification (full presentation/localization control). Caveats: subject to Doze delays (normal priority), app must not be force-stopped, WorkManager for heavy work. Use for client-controlled UX or pure sync."}]},{level:"senior",q:"How do you manage registration tokens on the server?",a:[{t:"p",text:"Store each device's token *associated with the user/account* on your server, update it when `onNewToken` fires (send the new token to your backend), and *remove stale tokens* when FCM reports them *unregistered/invalid* in send responses (app uninstalled, token rotated). Tokens can change (app restore, clear data, reinstall), so treat them as *ephemeral*. Deduplicate per device, and consider token *expiry* (refresh periodically). Clean token management avoids sending to dead endpoints and keeps delivery metrics healthy."},{t:"list",items:["**Store** — token ↔ user/account on the server.","**Update** — on `onNewToken` (push to backend).","**Remove** — tokens FCM reports unregistered/invalid.","**Ephemeral** — tokens change; dedupe and refresh."]},{t:"note",text:"Store tokens associated with the user on your server, update on onNewToken, and remove stale ones when FCM reports unregistered/invalid (uninstall/rotation). Tokens are ephemeral (change on restore/clear-data/reinstall) — dedupe per device and refresh periodically. Clean management avoids dead endpoints and keeps delivery metrics healthy."}]},{level:"junior",q:"What is the notification payload size limit in FCM?",a:[{t:"p",text:"FCM message payloads are limited to *4KB* (4096 bytes) for data messages (and topic messages have a smaller ~2KB limit). This means you *can't* send large content in the push — instead send a small payload (an ID or minimal data) and have the app *fetch the full content* from your API when it arrives. This 'notification as a signal, fetch the rest' pattern is standard and also keeps sensitive data out of the push. Keep payloads lean."},{t:"list",items:["**Limit** — ~4KB (topics ~2KB).","**Pattern** — send an ID, fetch full content from your API.","**Benefit** — no large data in push; keeps secrets out.","**Keep lean** — minimal payload."]},{t:"note",text:"FCM payloads are limited to ~4KB (topics ~2KB). Don't send large content — send a small payload (an ID/minimal data) and have the app fetch the full content from your API on arrival. This 'push as a signal, fetch the rest' pattern is standard and keeps sensitive data out of the push. Keep payloads lean."}]},{level:"senior",q:"What is notification trampolining, and why did Android 12 restrict it?",a:[{t:"p",text:"*Notification trampolining* was launching an Activity *indirectly* from a notification tap — the tap started a `BroadcastReceiver`/`Service` that then started the Activity. This *delayed* the app appearing (janky) and let apps do work before the UI. Android 12 *blocks* starting an Activity from a service/receiver launched by a notification tap; you must use a `PendingIntent` that *directly starts the Activity*. Fix by setting the notification's content intent to the Activity's `PendingIntent`, doing any routing inside the Activity."},{t:"list",items:["**Trampolining** — tap → service/receiver → Activity (indirect).","**Problem** — delayed UI, jank.","**Android 12** — blocks Activity start from that path.","**Fix** — PendingIntent directly to the Activity; route inside it."]},{t:"note",text:"Notification trampolining = launching an Activity indirectly (tap → service/receiver → Activity), which delayed the UI. Android 12 blocks starting an Activity from a service/receiver launched by a notification tap. Fix: set the content intent to a PendingIntent that directly starts the Activity, and do routing inside the Activity."}]},{level:"junior",q:"How do you display a rich notification with image and actions?",a:[{t:"p",text:"Use `NotificationCompat.Builder` with styles and actions: `BigPictureStyle` for a large image, `BigTextStyle` for expandable text, `addAction()` for buttons (each with its own `PendingIntent`), and set the content intent for the tap. For FCM, send a *data message* so the app builds this rich notification (auto-displayed notification messages are limited to title/body). Load images off the main thread (Coil/Glide) before building. Set the channel, small icon, and priority appropriately."},{t:"code",title:"Rich notification",code:`val n = NotificationCompat.Builder(ctx, "messages")
  .setSmallIcon(R.drawable.ic_msg)
  .setContentTitle(title).setContentText(body)
  .setStyle(NotificationCompat.BigPictureStyle().bigPicture(bitmap))
  .addAction(R.drawable.ic_reply, "Reply", replyPendingIntent)
  .setContentIntent(openPendingIntent).build()`},{t:"note",text:"Build rich notifications with NotificationCompat: BigPictureStyle (image), BigTextStyle (expandable text), addAction (buttons with PendingIntents), content intent for tap. For FCM, use a data message so the app builds it (auto notification messages are title/body only). Load images off-main first; set channel, small icon, priority."}]},{level:"junior",q:"How do you test FCM messages during development?",a:[{t:"p",text:"Quick options: the *Firebase console* → Cloud Messaging → send a test message to a specific *registration token* (good for notification messages, less so for data). Or `curl` the *HTTP v1 API* with a service-account OAuth token and a JSON payload (tests data messages and platform options). Log the device token (`FirebaseMessaging.getInstance().token`) to target it. Test *all four states*: foreground/background × notification/data, since behavior differs. Also test tap deep-linking and Doze/high-priority delivery."},{t:"list",items:["**Firebase console** — send test to a token (notifications).","**curl HTTP v1** — data messages + platform options.","**Log the token** — to target the device.","**Test all states** — fg/bg × notification/data, tap, Doze."]},{t:"note",text:"Test FCM via the Firebase console (send to a specific token — good for notifications) or curl the HTTP v1 API with a service-account token (data messages + platform options). Log the device token to target it. Test all four states (foreground/background × notification/data — behavior differs), plus tap deep-linking and high-priority/Doze delivery."}]},{level:"senior",q:"How do you deep-link from a notification to the right screen?",a:[{t:"p",text:"Put a *destination hint* in the message data (a screen id, entity id, or a deep-link URI), attach it to the notification's *content `PendingIntent`* (extras or an `ACTION_VIEW` deep-link intent), and in the target Activity read the extras/intent to *navigate* to the right screen. Use `TaskStackBuilder` (or a proper back stack) so 'back' returns sensibly. Ensure `PendingIntent` uses `FLAG_IMMUTABLE` and a unique request code. For auto-displayed FCM notifications, the data arrives in the launcher intent on tap."},{t:"list",items:["**Destination in data** — screen/entity id or deep-link URI.","**Attach** — to the content PendingIntent (extras/ACTION_VIEW).","**Navigate** — read in the Activity; TaskStackBuilder for back stack.","**PendingIntent** — FLAG_IMMUTABLE + unique request code."]},{t:"note",text:"Deep-link by putting a destination hint (screen/entity id or deep-link URI) in message data, attaching it to the content PendingIntent (extras or ACTION_VIEW), and navigating from the target Activity. Use TaskStackBuilder for a sensible back stack; PendingIntent with FLAG_IMMUTABLE + unique request code. Auto-shown FCM notifications deliver data in the tap launcher intent."}]},{level:"senior",q:"Why should you never embed FCM/server credentials in the app?",a:[{t:"p",text:"The *server key / service-account credentials* let anyone send messages *as your app* — embedding them in the APK means they can be *extracted* (the APK is decompilable) and abused to spam your users or impersonate you. Sending must happen *server-side* (your backend holds the credentials securely). The app only holds the *client* config (`google-services.json`, which is not a secret — it identifies the project) and its own registration token. Keep all send-side secrets on the server."},{t:"list",items:["**Server credentials** — can send as your app.","**In APK** — extractable → spam/impersonation.","**Send server-side** — backend holds secrets.","**App holds** — only client config + its token (not secret)."]},{t:"note",text:"FCM server key/service-account credentials let anyone send as your app — embedded in the APK they're extractable and abusable (spam/impersonation). Sending must be server-side (backend holds credentials). The app only holds client config (google-services.json — not a secret, just project identity) and its token. Keep all send-side secrets server-side."}]},{level:"senior",q:"How do you ensure notifications aren't shown twice or out of order?",a:[{t:"p",text:"Give each logical notification a *stable id* (e.g. based on the message/conversation id) so re-delivery *updates* rather than duplicates (`notify(sameId, ...)`), and use a *server-side message id* to *dedupe* if the same push could arrive twice. For ordering, don't rely on push arrival order (FCM doesn't guarantee it) — include a *timestamp/sequence* in the payload and let the client render by that. For chat, treat push as a *trigger to fetch ordered data* from your API rather than the source of truth."},{t:"list",items:["**Stable notification id** — update, don't duplicate.","**Server message id** — dedupe re-delivery.","**Ordering** — timestamp/sequence in payload; don't trust arrival order.","**Chat** — push triggers an ordered fetch from the API."]},{t:"note",text:"Avoid duplicates with a stable notification id (notify(sameId) updates), and dedupe by a server message id if a push can arrive twice. Don't rely on FCM arrival order — include a timestamp/sequence and render by it. For chat, treat push as a trigger to fetch ordered data from your API (source of truth), not the data itself."}]},{level:"junior",q:"What is a foreground service notification, and how does it relate to FCM?",a:[{t:"p",text:"A *foreground service* must show an ongoing *notification* (so the user knows it's running) — used for user-visible ongoing tasks (music, navigation, an active call). FCM relates when a *high-priority data message* triggers work that needs to run reliably: on recent Android you may start a foreground service *from* an FCM message only within allowed constraints (there are background-start restrictions). For most push work, prefer *WorkManager* (Doze-aware) over a foreground service unless the task is genuinely ongoing and user-visible (e.g. an incoming call UI)."},{t:"list",items:["**Foreground service** — ongoing notification; user-visible tasks.","**FCM trigger** — high-priority message → reliable work.","**Restrictions** — background foreground-service-start limits.","**Prefer** — WorkManager unless genuinely ongoing (e.g. calls)."]},{t:"note",text:"A foreground service shows an ongoing notification (user knows it runs) — for user-visible ongoing tasks (music, calls). FCM can trigger one from a high-priority message within background-start restrictions. For most push-triggered work prefer WorkManager (Doze-aware); use a foreground service only for genuinely ongoing user-visible tasks like an incoming-call UI."}]},{level:"senior",q:"How do you measure and improve notification delivery and engagement?",a:[{t:"p",text:"Track *delivery* (FCM's delivery data / BigQuery export shows sent vs delivered vs dropped) and *engagement* (opens/taps via Analytics, attributing which notifications drive action). Improve delivery by using correct priority, valid tokens (prune invalid), and respecting Doze; improve engagement with relevant, well-timed, *personalized* content, good channels (so users don't mute you), rich formatting, and A/B testing copy/timing. Watch *opt-out/mute rates* — over-notifying erodes delivery permission. Treat notifications as a measured, respectful channel."},{t:"list",items:["**Delivery data** — FCM/BigQuery: sent vs delivered vs dropped.","**Engagement** — opens/taps via Analytics.","**Improve** — correct priority, valid tokens, relevance, timing.","**Watch** — opt-out/mute rates; don't over-notify."]},{t:"note",text:"Measure delivery (FCM delivery data/BigQuery export: sent vs delivered vs dropped) and engagement (opens/taps via Analytics). Improve delivery with correct priority, valid tokens (prune), Doze respect; improve engagement with relevant, well-timed, personalized content, good channels, rich formatting, A/B tests. Watch opt-out/mute rates — over-notifying erodes delivery permission."}]},{level:"junior",q:"What is google-services.json, and is it a secret?",a:[{t:"p",text:"`google-services.json` is the Firebase *client config* file (project id, app id, API keys for client SDKs, sender id) that the *google-services Gradle plugin* reads at build time to wire up Firebase. It's *not a secret* in the sensitive sense — it identifies your Firebase project and is embedded in the app anyway; security comes from *Security Rules* and *server-side credentials*, not from hiding this file. That said, teams often keep it out of public repos by convention. Losing it isn't a breach; leaking your *service-account key* would be."},{t:"list",items:["**Client config** — project/app id, client API keys, sender id.","**Read by** — the google-services Gradle plugin at build.","**Not a secret** — embedded in the app; security via Rules/server.","**Contrast** — service-account key IS sensitive."]},{t:"note",text:"google-services.json is Firebase client config (project/app id, client API keys, sender id) read by the google-services Gradle plugin at build. It's not a sensitive secret — it identifies the project and ships in the app; security comes from Security Rules and server credentials. Teams may keep it out of public repos by convention. The service-account key, by contrast, IS sensitive."}]},{level:"senior",q:"How would you architect push for reliability when messages must not be lost?",a:[{t:"p",text:"FCM is *best-effort* — not a guaranteed delivery bus. For must-not-lose messages, treat push as a *wake-up signal* and make the *server the source of truth*: on receipt, the client *fetches* any missed items from an ordered, idempotent API (using a cursor/last-synced marker), so even a dropped push is recovered on the next sync or app open. Persist unread state server-side, use high priority for urgency, dedupe by message id, and periodically *reconcile* (on app foreground). Never rely solely on the push payload for critical data."},{t:"list",items:["**FCM best-effort** — not guaranteed delivery.","**Push = wake-up** — server is source of truth.","**Fetch missed** — ordered idempotent API + cursor.","**Reconcile** — on foreground; dedupe by message id."]},{t:"note",text:"FCM is best-effort, not guaranteed. For must-not-lose messages, treat push as a wake-up signal with the server as source of truth: on receipt, fetch missed items from an ordered, idempotent API (cursor/last-synced), so a dropped push is recovered on next sync/open. Persist unread server-side, use high priority, dedupe by id, reconcile on foreground. Never rely solely on the payload for critical data."}]},{level:"senior",q:"What happens to a token when the app is uninstalled or data is cleared?",a:[{t:"p",text:"On *uninstall* the token becomes *invalid* — FCM eventually reports it as `NotRegistered`/`UNREGISTERED` in send responses, and you should *delete it* from your server. On *clear data* or some *restores*, the token is *regenerated* and `onNewToken` fires with the new one (update the server). So a token is not permanent: always handle rotation via `onNewToken`, and prune tokens that FCM marks unregistered. Sending to a dead token wastes quota and skews delivery metrics."},{t:"list",items:["**Uninstall** — token invalid; FCM reports NotRegistered → delete it.","**Clear data/restore** — token regenerated; `onNewToken` fires.","**Not permanent** — handle rotation, prune dead tokens.","**Dead token** — wastes quota, skews metrics."]},{t:"note",text:"On uninstall the token becomes invalid — FCM reports NotRegistered/UNREGISTERED in send responses; delete it server-side. On clear-data/some restores the token regenerates and onNewToken fires (update the server). Tokens aren't permanent — handle rotation and prune dead ones; sending to them wastes quota and skews delivery metrics."}]}],uT={id:"fcm",title:"Cloud Messaging (FCM)",tagline:"Push over polling & why it's efficient, registration tokens & onNewToken, notification vs data messages, handling & building notifications.",tags:["Firebase","FCM","Push Notifications","Tokens","Data Messages"],content:dT,qa:hT},pT=[{heading:"Crashlytics — production crash reporting",blocks:[{t:"p",text:"**Firebase Crashlytics** is a crash-reporting tool: it automatically captures crashes (and non-fatal errors) that happen on real users' devices in production, and reports them to a dashboard with the *stack trace*, device/OS info, and context. You can't reproduce every crash yourself — Crashlytics is how you *find out* what's crashing for real users, *how often*, and *on which devices*, so you can prioritize and fix. It's essentially a requirement for any production app; shipping without crash reporting means flying blind."},{t:"list",items:["**Automatic crash capture** — it hooks into the app and records unhandled exceptions/crashes with full stack traces, sending them when the app next has connectivity. You add the SDK, and crashes are reported with no per-crash code.","**Grouping** — it intelligently *groups* identical crashes into a single issue (with a count of affected users/occurrences), so you see 'this crash affects 500 users' rather than 500 separate reports — letting you prioritize by impact.","**Context** — each report includes device model, OS version, app version, and a *breadcrumb log* of events leading up to the crash (you can log custom keys/events), which helps diagnose *why* it happened.","**Non-fatal reporting** — you can also record *handled* exceptions (`recordException`) — errors you caught but want to track (a failed API call, a recoverable error) — to monitor issues that don't crash the app."]}]},{heading:"Crashlytics and R8 — the mapping file",blocks:[{t:"p",text:"A critical integration point: release builds are *obfuscated* by R8 (class/method names renamed to `a`, `b`, `c`), so raw crash stack traces from production are *unreadable*. Crashlytics de-obfuscates them using the **R8 mapping file** — but only if the mapping file is uploaded for that build."},{t:"list",items:["**The Crashlytics Gradle plugin automatically uploads the mapping file** for each release build, so Crashlytics can translate obfuscated stack traces back to real class/method names — making crash reports readable.","**If the mapping isn't uploaded** (misconfiguration), crash reports show obfuscated names (`a.b.c()`) and are useless — you can't tell what crashed. So verifying the mapping upload is part of a correct Crashlytics setup.","**Per-build mapping** — R8 obfuscates differently per build, so each version's mapping de-obfuscates only that version's crashes; the plugin handles associating the right mapping with the right build automatically.","This is the same mapping-file concept from the R8/distribution topic, applied to crash reporting — the mapping is what makes obfuscated production crashes debuggable."]}]},{heading:"Firebase Analytics — understanding user behavior",blocks:[{t:"p",text:"**Firebase Analytics** (Google Analytics for Firebase) tracks *what users do* in your app — which screens they visit, which features they use, where they drop off — by logging *events*. It answers product questions: which features are used, where users abandon a flow, what drives engagement/retention. It's about *behavior* (product/business insight), distinct from Crashlytics (*stability*) and performance monitoring (*speed*)."},{t:"code",title:"Logging events",code:`val analytics = Firebase.analytics

// Log a custom event with parameters
analytics.logEvent("add_to_cart") {
    param("item_id", itemId)
    param("item_name", itemName)
    param("value", price)
}

// Automatic events (screen views, first open, session) are logged for you
// Set user properties for segmentation:
analytics.setUserProperty("subscription_tier", "premium")`},{t:"list",items:["**Events** — the core unit: something the user did (`add_to_cart`, `level_complete`, `search`), optionally with *parameters* (item id, value). Some events are *automatic* (screen views, first open, sessions); you add *custom* events for your app's specific actions.","**User properties** — attributes for *segmenting* users (subscription tier, user type, region), so you can analyze behavior by segment ('do premium users use this feature more?').","**Funnels & retention** — analyze multi-step flows (how many complete checkout vs abandon at each step) and how users return over time — key product metrics.","**Integration** — Analytics feeds *audiences* used by other Firebase tools (Remote Config targeting, A/B testing, FCM campaigns), and can export raw data to BigQuery for deep analysis."]}]},{heading:"Performance Monitoring and the observability picture",blocks:[{t:"list",items:["**Firebase Performance Monitoring** — tracks *how fast* your app is in the field: automatic traces for app startup time, screen rendering (slow/frozen frames), and network request timing (success rate, latency, payload size), plus custom traces you define. It's the *performance* pillar — telling you real-world speed across devices, complementing local profiling (which tells you *why*).","**The three observability pillars** — a well-instrumented app tracks: **stability** (Crashlytics — what's crashing), **behavior** (Analytics — what users do), and **performance** (Performance Monitoring — how fast). Together they tell you the real-world health of your app across millions of devices you'll never touch.","**Why it matters**: production is the ultimate test environment — real devices, real networks, real usage at scale reveal issues your testing can't. Observability tools are your eyes into that, turning 'users complain it's buggy/slow' into specific, prioritizable, actionable data (this crash affects X users, this screen is slow on these devices, users abandon at this step).","**Privacy considerations** — analytics and crash data must respect user privacy: comply with consent requirements (GDPR), don't log PII (personally identifiable info) in events/crash logs, and honor the Play Data Safety declarations. Handle data collection responsibly."]},{t:"note",text:"Crashlytics: automatic production crash reporting (stack traces, device context, grouped by issue with impact counts; also non-fatal recordException). Needs the R8 mapping file uploaded (the Gradle plugin does it) to de-obfuscate release crashes — else reports are unreadable. Analytics: track user behavior via events (+ params) and user properties (segmentation); funnels/retention; feeds audiences for Remote Config/A-B/FCM; exports to BigQuery. Performance Monitoring: real-world speed (startup, rendering, network). Together = the observability pillars (stability, behavior, performance). Respect privacy (consent, no PII)."}]}],mT=[{level:"junior",q:"What is Firebase Crashlytics and why is it important?",a:[{t:"p",text:"**Crashlytics is a crash-reporting tool that automatically captures crashes happening on real users' devices in production and reports them to a dashboard with the stack trace, device/OS info, and context.** It's important because you can't reproduce every crash yourself — Crashlytics is how you *find out* what's actually crashing for real users, how often, and on which devices, so you can prioritize and fix."},{t:"list",items:["**Automatic capture** — it records unhandled exceptions/crashes with full stack traces and sends them when the app next has connectivity, with no per-crash code (you just add the SDK).","**Grouping by impact** — it groups identical crashes into a single issue with a count of affected users/occurrences, so you see 'this crash affects 500 users' and can prioritize by real impact rather than sifting through duplicates.","**Context** — each report includes device model, OS version, app version, and a breadcrumb log of events before the crash (with custom keys you can log), helping you diagnose *why*.","**Non-fatals** — you can also record *handled* exceptions (`recordException`) to track recoverable errors that don't crash the app."]},{t:"p",text:"It's essentially a requirement for any production app — shipping without crash reporting means flying blind, discovering problems only from user complaints (or not at all). With Crashlytics, production crashes become concrete, prioritized, actionable data. One critical setup detail: since release builds are obfuscated by R8, you must ensure the *mapping file* is uploaded (the Crashlytics Gradle plugin does this) so crash reports are de-obfuscated and readable — otherwise the stack traces show meaningless renamed symbols."}]},{level:"junior",q:"What is Firebase Analytics used for?",a:[{t:"p",text:"**Firebase Analytics tracks *what users do* in your app — which screens they visit, which features they use, where they drop off — by logging *events*. It answers product and business questions: which features are popular, where users abandon a flow, what drives engagement and retention.** It's about user *behavior*, distinct from Crashlytics (stability) and Performance Monitoring (speed)."},{t:"code",title:"Logging an event",code:`Firebase.analytics.logEvent("add_to_cart") {
    param("item_id", itemId)
    param("value", price)
}`},{t:"list",items:["**Events** are the core unit — something the user did (`add_to_cart`, `search`, `level_complete`), optionally with parameters. Some events are logged automatically (screen views, first open, sessions); you add custom events for your app's specific actions.","**User properties** segment users (subscription tier, region), so you can analyze behavior by group ('do premium users use this feature more?').","**Funnels and retention** let you see how many users complete a multi-step flow vs abandon at each step, and how users return over time — key product metrics."]},{t:"p",text:"The value is turning intuition into data — instead of guessing which features matter or where users struggle, you *measure* it. Analytics also feeds other Firebase tools: it builds *audiences* used for Remote Config targeting, A/B testing, and FCM campaigns, and raw data can export to BigQuery for deep analysis. Used well, it drives product decisions (which features to invest in, where to fix friction). One responsibility to note: analytics must respect user privacy — comply with consent (GDPR), don't log personally identifiable information in events, and honor Play's Data Safety declarations."}]},{level:"junior",q:"What are the different Firebase tools for understanding app health in production?",a:[{t:"p",text:"**Firebase provides three tools covering the three pillars of production observability — stability, behavior, and performance:**"},{t:"list",items:["**Crashlytics (stability)** — *what's crashing*. Automatic crash and non-fatal error reporting with stack traces, device context, and impact-grouped issues. Tells you your app's reliability in the field.","**Analytics (behavior)** — *what users do*. Event tracking (screen views, feature usage, funnels, retention) and user-property segmentation. Tells you how the app is actually used and where users drop off.","**Performance Monitoring (performance)** — *how fast* the app is. Automatic traces for startup time, screen rendering (slow/frozen frames), and network request timing, plus custom traces. Tells you real-world speed across devices."]},{t:"p",text:"Together they give a complete picture of real-world app health across the millions of devices you'll never physically test on. Production is the ultimate test environment — real hardware, real networks, real usage at scale reveal issues local testing can't — and these tools are your window into it. They turn vague signals ('users say it's buggy/slow') into specific, prioritizable data: *this* crash affects *this many* users, *this* screen is slow on *these* devices, users abandon at *this* step. A well-instrumented app uses all three so problems are detected and prioritized by real impact. (Firebase also has related tools — Remote Config, A/B Testing, App Distribution — but Crashlytics/Analytics/Performance are the core observability trio.)"}]},{level:"senior",q:"Why do crash reports need the R8 mapping file, and what happens without it?",a:[{t:"p",text:"**Because release builds are obfuscated by R8 — class, method, and field names are renamed to short meaningless symbols (`a`, `b`, `c`) — so raw crash stack traces from production show those obfuscated names and are unreadable. The mapping file records the original-name → obfuscated-name translation, and Crashlytics uses it to *de-obfuscate* stack traces back to real names, making crash reports debuggable. Without it, reports are useless.**"},{t:"list",items:["**What obfuscation does to a crash**: R8 renames `com.myapp.PaymentProcessor.processPayment()` to something like `a.b.a()`. When that crashes in production, the stack trace reported from users' devices shows `at a.b.a(...)` — which tells you nothing about where or what actually crashed.","**What the mapping file does**: it's the dictionary mapping obfuscated names back to originals. Crashlytics applies it to translate the obfuscated stack trace back into readable `at com.myapp.PaymentProcessor.processPayment()`, so you can actually diagnose the crash.","**How it's uploaded**: the Crashlytics Gradle plugin automatically uploads the mapping file for each release build and associates it with that build, so de-obfuscation just works — provided the plugin is configured correctly."]},{t:"list",items:["**What happens without it**: crash reports show obfuscated symbols (`a.b.c()`), and you *cannot tell what crashed* — the reports are effectively worthless. You'd see that *something* is crashing and how often, but not *what* or *where*, so you can't fix it. For an obfuscated release app, the mapping file is the difference between actionable and useless crash reports.","**It's per-build**: R8 obfuscates *differently* on each build (a code change shifts the renamed symbols), so each version has its own mapping file that only de-obfuscates *that version's* crashes. The plugin handles matching the right mapping to the right build, but the implication is that every release's mapping must be uploaded — if a crash comes from a version whose mapping was never uploaded, it can't be de-obfuscated.","**The verification discipline**: because a misconfigured mapping upload silently produces unreadable reports (the app works, crashes are captured, but they're gibberish), verifying that de-obfuscation is working — checking that crash reports show real names — is part of a correct Crashlytics setup. Discovering during an incident that your crash reports are obfuscated is a painful, common mistake."]},{t:"p",text:"**The senior framing**: this is the intersection of two mechanisms — R8 obfuscation (shrinks and lightly protects the shipped code by renaming symbols) and crash reporting (which needs *readable* symbols to be useful). The mapping file is what reconciles them: the release stays obfuscated for users, but Crashlytics holds the key to translate its crashes back for *your* debugging. So obfuscation and the mapping file are two halves of one system, and the operational requirement — upload the mapping for *every* release (the Gradle plugin automates it) and *verify* de-obfuscation works — must be baked into the release process, because a crash report you can't read is a bug you can't fix. Understanding that obfuscation makes production crashes unreadable, that the per-build mapping file is what restores readability, and that verifying its upload is essential (a silent failure otherwise) is the practical crash-reporting competence these questions probe — and it connects directly to the same mapping-file concept in the build/distribution topic."}]},{level:"senior",q:"How would you instrument an app to make production issues diagnosable and prioritizable?",a:[{t:"p",text:"**I'd instrument all three observability pillars — crash/error reporting (Crashlytics), behavioral analytics (Analytics), and performance monitoring (Performance Monitoring) — with rich context (custom keys, breadcrumbs, user/session identifiers) so issues are not just *detected* but *diagnosable* (enough context to understand *why*) and *prioritizable* (impact data to know *what matters most*). Detection alone isn't enough — the goal is actionable signal.**"},{t:"list",items:["**Crash & error reporting with context**: Crashlytics for automatic crashes, *plus* `recordException` for handled/non-fatal errors I want to track (failed critical API calls, recoverable errors, caught-but-unexpected states). Add *custom keys* (current screen, feature flags active, user tier, relevant IDs) and *breadcrumb logs* (`Crashlytics.log`) tracing the user's path, so a crash report shows not just the stack trace but the *state and sequence* that led to it — turning 'it crashed here' into 'it crashed here, for premium users, on the checkout screen, after this action'. Ensure the mapping file is uploaded (de-obfuscated traces) and set a user identifier (non-PII) so I can correlate a specific user's crashes if they report an issue.","**Behavioral analytics for impact and funnels**: log key events (feature usage, funnel steps, conversions) with parameters and user properties. This provides the *impact* dimension — how many users hit a feature, where they drop off — which prioritizes both bugs (a crash in a high-traffic flow matters more than one in a rarely-used corner) and product work (invest where usage/friction is). Funnels reveal *where* users abandon, complementing crash data.","**Performance monitoring for speed regressions**: automatic startup/rendering/network traces plus custom traces around critical operations, so I catch performance regressions in the field (a screen that got slow on certain devices, a network endpoint degrading) — which users experience as 'slow/laggy' and which local testing on good devices misses.","**Tie it to releases and alerting**: associate metrics with app *version* so I can compare release-over-release (did this version's crash rate spike? did startup regress?) — essential for staged rollouts (halt if the new version's metrics degrade). Set up *alerts* on regressions (crash-rate spike, new crash affecting many users, ANR increase) so I'm notified proactively, not by manually checking dashboards. Android Vitals (Play Console) complements Firebase with Play-tracked crash/ANR/vitals used for ranking.","**Correlate the three**: the pillars are more powerful together — a *performance* trace showing a slow screen + *analytics* showing high traffic there + *crash* reports on that screen = a clear, high-priority problem. Cross-referencing behavior (impact), stability (crashes), and performance (speed) is how you prioritize accurately."]},{t:"list",items:["**Privacy and hygiene**: no PII in events, crash logs, or custom keys; comply with consent (GDPR) and Play Data Safety; use stable non-PII identifiers for correlation. Don't over-log (noise and cost) — instrument the events and traces that answer real questions, and add custom keys/breadcrumbs strategically around risky or important flows.","**Close the loop**: use the data to *act* — triage crashes by impact (users affected × severity), fix top issues, verify the fix reduced the metric in the next release, and feed learnings back (add a test for a fixed crash, add a breadcrumb where diagnosis was hard). Observability is only valuable if it drives action."]},{t:"p",text:"**The senior framing**: good instrumentation isn't just 'add Crashlytics' — it's designing for *diagnosability* and *prioritization*. Diagnosability means capturing enough *context* (custom keys, breadcrumbs, non-fatal errors, de-obfuscated traces) that a report tells you *why*, not just *that* something failed. Prioritization means capturing *impact* (analytics for traffic/usage, crash grouping by affected users, version comparison) so you fix what matters most — a crash in a high-traffic flow over one in a dead corner. The three pillars (stability, behavior, performance) are strongest *correlated* — behavior gives impact, stability gives failures, performance gives speed, and together they pinpoint high-priority problems. Wrapping it with release-versioned metrics, proactive alerting, privacy discipline, and a fix-verify feedback loop turns raw telemetry into an engineering advantage: production becomes a source of precise, actionable, prioritized signal rather than vague complaints. Demonstrating the detect → diagnose → prioritize → act framing, with the specific instrumentation choices (context for diagnosis, impact for priority, correlation across pillars, versioning/alerting), is the comprehensive senior answer."}]},{level:"senior",q:"How does Crashlytics capture and report a crash under the hood?",a:[{t:"p",text:"Crashlytics installs handlers: an *uncaught exception handler* for JVM/Kotlin crashes and *native (NDK) signal handlers* for C/C++ crashes. When a crash occurs, it *writes a minimal report to disk immediately* (the process is dying, so it can't upload then), capturing the stack trace, thread states, device/OS info, and your custom keys/logs. On the *next app launch*, it uploads the stored report to the Firebase backend, which *symbolicates/deobfuscates* it (using your mapping/symbol files) and *groups* similar crashes into issues."},{t:"list",items:["**Handlers** — uncaught exception + native signal handlers.","**On crash** — write a minimal report to disk (process dying).","**Next launch** — upload the stored report.","**Backend** — deobfuscate + group into issues."]},{t:"note",text:"Crashlytics installs an uncaught-exception handler (+ native signal handlers). On a crash it writes a minimal report to disk (the process is dying, can't upload) with stack/threads/device/custom keys, then uploads on the next launch. The backend deobfuscates (mapping/symbol files) and groups similar crashes into issues."}]},{level:"junior",q:"What is the difference between a crash and a non-fatal (logged) exception?",a:[{t:"p",text:"A *crash* (fatal) is an *uncaught* exception/signal that *terminates* the app — Crashlytics captures it automatically. A *non-fatal* is an exception you *caught and handled* but still want visibility into — you *record it manually* (`recordException(e)`) so it shows in Crashlytics (grouped, with trends) without crashing the app. Use non-fatals for handled errors that matter (a failed but recovered network call, an unexpected-but-survivable state) to spot problems that don't crash but degrade experience."},{t:"code",title:"Recording a non-fatal",code:`try {
  riskyOperation()
} catch (e: IOException) {
  Firebase.crashlytics.recordException(e) // logged, app keeps running
  showRetry()
}`},{t:"note",text:"A crash (fatal) is an uncaught exception terminating the app — captured automatically. A non-fatal is a caught/handled exception you record manually (recordException) for visibility (grouped, trended) without crashing. Use non-fatals for handled-but-important errors (recovered network fail, survivable bad state) to catch degradations that don't crash."}]},{level:"junior",q:"What are custom keys and custom logs in Crashlytics?",a:[{t:"p",text:'*Custom keys* (`setCustomKey("screen", "checkout")`) attach *key-value context* to crash reports — the app\'s state at crash time (current screen, user tier, feature-flag values) to help diagnose *why* it happened. *Custom logs* (`log("tapped pay")`) add a *breadcrumb trail* of recent actions leading up to the crash. Together they turn a bare stack trace into a *story*: what the user was doing and in what state. Add them at key points so production crashes are actionable, not mysterious.'},{t:"list",items:["**Custom keys** — key-value state (screen, tier, flags).","**Custom logs** — breadcrumb trail of recent actions.","**Together** — turn a stack trace into a story.","**Add** — at key points for actionable crashes."]},{t:"note",text:"Custom keys (setCustomKey) attach key-value state (screen, user tier, flags) to a crash — the 'why'. Custom logs (log) add a breadcrumb trail of recent actions — the 'what led here'. Together they turn a bare stack trace into a story of what the user was doing and in what state. Add them at key points to make production crashes actionable."}]},{level:"senior",q:"How does Crashlytics group crashes into issues, and why does grouping matter?",a:[{t:"p",text:"Crashlytics *groups* crashes with the same root cause (similar stack trace/exception) into a single *issue*, so a crash hitting 10,000 users is *one* issue with a count — not 10,000 separate reports. This lets you *prioritize by impact* (users affected, crash-free rate delta) instead of drowning in individual reports. Grouping isn't perfect (you can *merge* wrongly-split issues or the algorithm may over-group) — but it's what makes triage tractable. Prioritize the issues affecting the most users first."},{t:"list",items:["**Groups** — same-root-cause crashes into one issue.","**Count** — 10k crashes → 1 issue with impact.","**Prioritize** — by users affected, not raw reports.","**Imperfect** — merge/split issues as needed."]},{t:"note",text:"Crashlytics groups same-root-cause crashes (similar stack/exception) into one issue with a count — 10k crashes become one prioritizable issue, not 10k reports. This lets you triage by impact (users affected, crash-free delta) instead of drowning. Grouping is imperfect (merge/split as needed) but makes triage tractable. Fix highest-impact issues first."}]},{level:"junior",q:"What is the crash-free users metric, and why is it a key KPI?",a:[{t:"p",text:"*Crash-free users* is the percentage of users who *didn't experience a crash* in a period (similarly, crash-free sessions). It's a top stability KPI because it directly reflects *how many real users* are affected — a single high-frequency crash can drop it noticeably. Teams set a *target* (e.g. 99.5%+), watch it *during rollouts* (a dip signals a bad release), and gate releases on it. It's more user-centric than raw crash counts (which don't show reach). Track it per version."},{t:"list",items:["**Crash-free users** — % who hit no crash in a period.","**User-centric** — reflects real reach of crashes.","**Target + watch** — e.g. 99.5%+; monitor during rollouts.","**Per version** — a dip flags a bad release."]},{t:"note",text:"Crash-free users = % of users who experienced no crash in a period (also crash-free sessions). A top stability KPI because it reflects real user reach — one frequent crash drops it visibly. Set a target (99.5%+), watch it during rollouts (a dip = bad release), gate releases on it, track per version. More user-centric than raw crash counts."}]},{level:"senior",q:"How do you ensure Crashlytics reports are deobfuscated with R8 enabled?",a:[{t:"p",text:"R8 obfuscates names, so raw crash traces are unreadable — Crashlytics needs the *mapping file* to deobfuscate. The Crashlytics Gradle plugin *automatically uploads `mapping.txt`* per build (ensure `mappingFileUploadEnabled` is on for release). For *native* crashes, upload the *native symbols* (`uploadCrashlyticsSymbolFile`/the NDK plugin). Each build's mapping is unique, so this must happen for *every* release. Verify by checking that production traces show real class/method names, not `a.b.c`."},{t:"list",items:["**Needs mapping** — to deobfuscate R8 traces.","**Plugin uploads** — `mapping.txt` per build automatically.","**Native** — upload symbol files (NDK).","**Verify** — production traces show real names."]},{t:"note",text:"R8 obfuscation makes raw traces unreadable — Crashlytics needs the mapping file. Its Gradle plugin auto-uploads mapping.txt per build (keep mappingFileUploadEnabled on for release); upload native symbols for NDK crashes. Each build's mapping is unique, so it must happen every release. Verify production traces show real class/method names, not a.b.c."}]},{level:"junior",q:"What is Firebase Analytics' event and parameter model?",a:[{t:"p",text:'Analytics is *event-based*: you log *events* (`logEvent("add_to_cart", params)`) — named actions — each with up to ~25 *parameters* (key-value details like item id, value, screen). Firebase provides *recommended events* (e.g. `screen_view`, `purchase`, `login`) with standard params (which power built-in reports and integrations), plus *custom events* for your app\'s specific actions. *User properties* describe the user (tier, cohort) for segmentation. Design a consistent event taxonomy so data is analyzable.'},{t:"list",items:["**Events** — named actions (`add_to_cart`), ~25 params each.","**Recommended events** — standard names power reports.","**Custom events** — app-specific actions.","**User properties** — user attributes for segmentation."]},{t:"note",text:"Firebase Analytics is event-based: log events (named actions like add_to_cart) with up to ~25 parameters each. Use recommended events (screen_view, purchase) with standard params for built-in reports/integrations, plus custom events for specifics. User properties (tier, cohort) enable segmentation. Design a consistent event taxonomy so data stays analyzable."}]},{level:"senior",q:"What is the difference between events, user properties, and audiences?",a:[{t:"p",text:"*Events* are *actions* (what the user did — `level_up`, `purchase`). *User properties* are *attributes* of the user (persistent traits — subscription tier, favorite genre, app version). *Audiences* are *segments* defined by combinations of events/properties (e.g. 'users who purchased in the last 7 days' or 'free-tier users who opened checkout') — used for targeting in Remote Config, A/B testing, notifications, and analysis. Events feed audiences; properties describe users; audiences group them for action."},{t:"table",headers:["","Is","Example"],rows:[["Event","An action","purchase, level_up"],["User property","A user attribute","tier = premium"],["Audience","A segment (event+property rules)","purchased last 7d"]]},{t:"note",text:"Events = actions (what the user did — purchase, level_up). User properties = persistent user attributes (tier, genre, version). Audiences = segments defined by event/property rules (purchased last 7d) used to target Remote Config, A/B tests, notifications, and analysis. Events feed audiences, properties describe users, audiences group them for action."}]},{level:"junior",q:"How does Firebase Analytics differ from crash reporting?",a:[{t:"p",text:"*Analytics* answers *'what are users doing?'* — behavior, funnels, retention, feature usage (product/business insight). *Crashlytics* answers *'what's broken?'* — stability, crashes, non-fatals (engineering/quality insight). They're complementary halves of production observability: Analytics tells you a feature is used a lot (so its bugs matter more), Crashlytics tells you it's crashing. Use Analytics to prioritize by usage and Crashlytics to prioritize by breakage; together they guide where to invest."},{t:"list",items:["**Analytics** — what users do (behavior, funnels, retention).","**Crashlytics** — what's broken (crashes, non-fatals).","**Complementary** — usage + stability.","**Together** — prioritize by usage × breakage."]},{t:"note",text:"Analytics answers 'what are users doing?' (behavior, funnels, retention — product insight); Crashlytics answers 'what's broken?' (crashes, non-fatals — quality insight). Complementary halves of observability: Analytics shows a feature is heavily used (bugs matter more), Crashlytics shows it's crashing. Prioritize by usage × breakage."}]},{level:"senior",q:"What is Crashlytics velocity alerting, and how do you respond to it?",a:[{t:"p",text:"*Velocity alerts* notify you when a crash issue is *spiking* — affecting a significant, rapidly-growing percentage of sessions (often a new regression in a fresh release). Respond fast: confirm the *version* and *impact*, check if it's *flag-gated* (disable via Remote Config), *halt the rollout* if staged, and ship a *forward-fix*. Velocity alerts are your early-warning during rollouts — wire them to Slack/email/PagerDuty so the on-call sees a regression at 1% rollout, not after it reaches everyone."},{t:"list",items:["**Velocity alert** — a crash spiking across sessions.","**Often** — a new-release regression.","**Respond** — confirm version/impact, flag-disable, halt, forward-fix.","**Wire to** — Slack/PagerDuty for on-call."]},{t:"note",text:"Velocity alerts fire when a crash issue spikes across a growing % of sessions (often a fresh-release regression). Respond fast: confirm version/impact, disable if flag-gated (Remote Config), halt the rollout, ship a forward-fix. Wire alerts to Slack/PagerDuty so on-call catches a regression at 1% rollout, not after it reaches everyone."}]},{level:"senior",q:"How do you attribute a crash to a specific release and user segment?",a:[{t:"p",text:"Crashlytics tags each report with the *app version (versionCode/Name)*, OS, and device — so you can filter an issue by *version* to see if it's new in a release. Add *custom keys* (user tier, experiment variant, feature-flag state) and integrate *Analytics* (BigQuery) to cross-reference the crash with *behavior/segment*. This lets you answer 'does this crash only hit v2.3 on Android 14, premium users, with flag X on?' — pinpointing the trigger and scoping a fix or a targeted flag-disable."},{t:"list",items:["**Auto tags** — version, OS, device per report.","**Filter by version** — is it new in this release?","**Custom keys** — tier, variant, flag state.","**Cross-ref Analytics** — segment/behavior via BigQuery."]},{t:"note",text:"Crashlytics tags each report with app version, OS, device (filter an issue by version to see if it's new). Add custom keys (tier, experiment variant, flag state) and cross-reference Analytics/BigQuery for segment/behavior. Answer 'does this hit only v2.3 on Android 14, premium, flag X on?' — pinpointing the trigger to scope a fix or targeted flag-disable."}]},{level:"junior",q:"What is DebugView, and how do you validate analytics during development?",a:[{t:"p",text:"*DebugView* (in the Firebase console) shows your Analytics events *in near real-time* for a device in *debug mode* (`adb shell setprop debug.firebase.analytics.app <pkg>`) — so you can verify events fire with the right names/parameters *as you use the app*, instead of waiting ~24h for normal processing. Use it to validate your event taxonomy before shipping (correct names, params present, no typos/duplicates). Catching instrumentation bugs here prevents garbage data in production reports."},{t:"list",items:["**DebugView** — near-real-time events for a debug device.","**Enable** — `setprop debug.firebase.analytics.app`.","**Validate** — names/params fire correctly as you use the app.","**Prevents** — garbage data from bad instrumentation."]},{t:"note",text:"DebugView (Firebase console) shows Analytics events in near-real-time for a debug-mode device (adb setprop debug.firebase.analytics.app <pkg>) — verify events fire with correct names/params as you use the app, vs waiting ~24h. Validate your taxonomy before shipping (no typos/dupes, params present). Catches instrumentation bugs before they pollute production data."}]},{level:"senior",q:"How do you design an event taxonomy that scales?",a:[{t:"p",text:"Define a *consistent naming convention* (snake_case, verb_noun like `checkout_started`), a *documented schema* (each event's meaning + required params), and reuse *recommended events*/params where they exist (for built-in reports). Keep events *purposeful* (tied to a question you'll ask), avoid *over-instrumenting* (noise) and *PII* (don't log personal data), and version/govern changes so the meaning stays stable over time. A shared spec (and a thin logging wrapper enforcing it) prevents the drift that makes analytics unanalyzable."},{t:"list",items:["**Convention** — snake_case verb_noun; documented schema.","**Reuse** — recommended events/params for built-in reports.","**Purposeful** — tied to a question; avoid noise + PII.","**Govern** — a shared spec + logging wrapper enforces it."]},{t:"note",text:"Scalable event taxonomy: consistent naming (snake_case verb_noun like checkout_started), a documented schema (meaning + required params per event), reuse recommended events/params, keep events purposeful (avoid noise and PII), and govern changes so meaning stays stable. A shared spec plus a thin logging wrapper enforcing it prevents the drift that makes analytics unusable."}]},{level:"junior",q:"What is a funnel, and how does Analytics help you find drop-off?",a:[{t:"p",text:"A *funnel* is a sequence of steps toward a goal (e.g. `view_product → add_to_cart → begin_checkout → purchase`). Analytics (via funnel/exploration reports) shows *how many users complete each step* and *where they drop off* — the biggest drop is where you're losing conversions. This turns vague 'sales are low' into a specific 'we lose 60% at checkout', directing where to investigate (a bug? confusing UI?). Instrument each funnel step as an event to make this analysis possible."},{t:"list",items:["**Funnel** — ordered steps toward a goal.","**Shows** — completion + drop-off per step.","**Biggest drop** — where you lose conversions.","**Instrument** — each step as an event."]},{t:"note",text:"A funnel is an ordered sequence toward a goal (view_product → add_to_cart → begin_checkout → purchase). Analytics' funnel/exploration reports show completion and drop-off per step — the biggest drop is where you lose conversions, turning 'sales are low' into 'we lose 60% at checkout' to direct investigation. Instrument each step as an event."}]},{level:"senior",q:"How do you integrate Firebase data with BigQuery, and why?",a:[{t:"p",text:"Firebase can *export raw Analytics (and Crashlytics) data to BigQuery* — giving you *every event, unaggregated*, queryable with SQL. Why: the console's reports are useful but *limited/sampled*; BigQuery lets you run *custom, precise analyses*, join with your own data (backend, CRM), build custom funnels/cohorts, and power dashboards (Looker/Data Studio). It's the path from 'canned reports' to 'answer any question'. Set up the export (daily or streaming) and query with cost-awareness (partition/limit scans)."},{t:"list",items:["**Export** — raw Analytics/Crashlytics events to BigQuery.","**Why** — unaggregated, SQL, join with your data.","**Enables** — custom analyses, cohorts, dashboards.","**Beyond** — the console's limited/sampled reports."]},{t:"note",text:"Firebase exports raw Analytics/Crashlytics data to BigQuery — every event, unaggregated, SQL-queryable. Why: console reports are limited/sampled; BigQuery enables custom precise analyses, joins with your backend/CRM data, custom funnels/cohorts, and dashboards (Looker). The path from canned reports to answering any question. Set up daily/streaming export; query cost-aware (partition/limit)."}]},{level:"junior",q:"How do you respect user privacy and consent with Analytics?",a:[{t:"p",text:"Don't log *PII* (names, emails, precise location) as event params/user properties — it violates policy and privacy. Honor *consent*: gate analytics collection on the user's choice (`setAnalyticsCollectionEnabled(false)` until consent), integrate a *Consent Mode* where required (GDPR/CCPA), and disclose collection in your *Data Safety* form and privacy policy. Provide opt-out. Respecting consent isn't just legal — mis-declaring data practices risks Play removal. Collect the minimum needed for your questions."},{t:"list",items:["**No PII** — in events/user properties.","**Consent** — gate collection; Consent Mode for GDPR/CCPA.","**Disclose** — Data Safety + privacy policy; offer opt-out.","**Minimize** — collect only what you need."]},{t:"note",text:"Respect privacy: never log PII (names/emails/precise location) in params/properties; honor consent (gate collection until granted — setAnalyticsCollectionEnabled(false), Consent Mode for GDPR/CCPA); disclose in Data Safety + privacy policy; offer opt-out. Mis-declaring risks Play removal. Collect the minimum needed for your questions."}]},{level:"senior",q:"What is the difference between Crashlytics and Play Console ANR/crash reporting?",a:[{t:"p",text:"*Crashlytics* is *SDK-based* — it captures crashes/non-fatals with your *custom keys/logs* and rich context, reports *quickly* (near real-time), and works for all installs (not just Play). *Play Console vitals* is *platform-level* — it reports crashes AND *ANRs* (which Crashlytics historically captured less well), across all Play users *without an SDK*, and is what Google uses for *discoverability thresholds*. Use both: Crashlytics for fast, detailed engineering triage; Play vitals for ANRs, platform-truth metrics, and the threshold view."},{t:"table",headers:["","Crashlytics","Play vitals"],rows:[["Source","SDK in your app","Platform (no SDK)"],["Detail","Custom keys/logs, fast","Aggregate, incl. ANRs"],["Used for","Engineering triage","Discoverability thresholds"]]},{t:"note",text:"Crashlytics: SDK-based, rich context (custom keys/logs), fast near-real-time, all installs. Play vitals: platform-level (no SDK), reports crashes AND ANRs across all Play users, drives Google's discoverability thresholds. Use both — Crashlytics for fast detailed triage, Play vitals for ANRs and platform-truth threshold metrics."}]},{level:"senior",q:"How do you make a production crash reproducible from a report?",a:[{t:"p",text:"Enrich reports so a trace becomes reproducible: *custom keys* for state (screen, inputs, flags, experiment), *breadcrumb logs* for the action sequence, *user/session context* (without PII), and *deobfuscated* traces. From those, reconstruct the *exact path and state*, write a *failing test* that reproduces it, then fix. If it's device/OS-specific (tag shows it), reproduce on that config (or Firebase Test Lab). The goal: never guess — the report should carry enough context to recreate the crash deterministically."},{t:"list",items:["**Enrich** — custom keys (state), breadcrumb logs (actions).","**Deobfuscate** — readable traces.","**Reconstruct** — path + state → failing test → fix.","**Device-specific** — reproduce on that config / Test Lab."]},{t:"note",text:"Make crashes reproducible by enriching reports: custom keys (state — screen/inputs/flags), breadcrumb logs (action sequence), session context (no PII), deobfuscated traces. Reconstruct the exact path/state, write a failing test, then fix. Device/OS-specific? Reproduce on that config or Firebase Test Lab. Goal: the report carries enough to recreate the crash deterministically."}]},{level:"junior",q:"What is the difference between logging to Logcat and to Crashlytics?",a:[{t:"p",text:"*Logcat* (`Log.d`) is *local, development-time* logging — visible only on a connected device, gone in production. *Crashlytics logs* (`crashlytics.log(...)`) are *attached to crash reports* and uploaded, so you can see the *breadcrumbs from a real user's crash* you'll never have a device for. Use Logcat for local debugging; use Crashlytics logs for the *context you need after a production crash*. Don't log sensitive data to either in a way that leaks; keep Crashlytics logs concise breadcrumbs."},{t:"list",items:["**Logcat** — local, dev-time; gone in production.","**Crashlytics logs** — attached to reports, uploaded.","**Crashlytics** — breadcrumbs from real users' crashes.","**Neither** — should leak sensitive data."]},{t:"note",text:"Logcat (Log.d) is local dev-time logging — gone in production. Crashlytics logs (crashlytics.log) attach to crash reports and upload, giving breadcrumbs from real users' crashes you'll never have a device for. Logcat for local debugging; Crashlytics logs for post-production-crash context. Don't leak sensitive data to either; keep Crashlytics logs concise."}]},{level:"senior",q:"How do you monitor and act on a release using Crashlytics + Analytics together?",a:[{t:"p",text:"During a staged rollout: watch *Crashlytics* (crash-free rate, new/velocity issues on the new version) for *stability*, and *Analytics* for *behavioral regressions* (did a key funnel's conversion drop? did feature usage crater — a sign something silently broke without crashing?). Stability green but conversion down can mean a *non-crashing* bug (a broken button, a failed silent request). Combine: crash-free confirms it's not crashing; Analytics confirms users can still *do* the thing. Halt/forward-fix on either signal degrading."},{t:"list",items:["**Crashlytics** — crash-free, new/velocity issues (stability).","**Analytics** — funnel/usage regressions (silent breakage).","**Both green** — not crashing AND still usable.","**Either degrades** — halt + forward-fix."]},{t:"note",text:"Monitor a rollout with both: Crashlytics (crash-free rate, new/velocity issues) for stability, Analytics (funnel conversion, feature usage) for behavioral regressions — a silent non-crashing bug (broken button/failed request) shows as dropped conversion, not a crash. Both green = not crashing AND still usable. Halt/forward-fix on either degrading."}]},{level:"junior",q:"What is opt-in/opt-out for crash reporting, and when is it needed?",a:[{t:"p",text:"You can *disable Crashlytics collection* by default and enable it *only after user consent* (`setCrashlyticsCollectionEnabled(true)`) — needed where privacy regulations or your policy require consent before sending any data (including crash data, which can contain context). Some apps let users toggle 'send diagnostics'. Balance: you want crash data to fix bugs, but must honor consent/regulation. Declare crash-data collection in your *Data Safety* form. Default-on is common but confirm it's compliant for your audience."},{t:"list",items:["**Toggle collection** — `setCrashlyticsCollectionEnabled`.","**Enable after consent** — where regulation/policy requires.","**Declare** — crash-data collection in Data Safety.","**Balance** — bug-fixing data vs consent."]},{t:"note",text:"You can disable Crashlytics collection by default and enable only after consent (setCrashlyticsCollectionEnabled(true)) — needed where regulation/policy requires consent before sending any data (crash reports carry context). Offer a 'send diagnostics' toggle; declare crash-data collection in Data Safety. Default-on is common but confirm compliance for your audience."}]},{level:"senior",q:"How do you avoid noisy or low-value crash reports drowning real issues?",a:[{t:"p",text:"Reduce noise: *don't record expected exceptions as non-fatals* (e.g. routine cancellations), *filter/ignore* known-benign issues in the console, *merge* wrongly-split issues, and *fix or suppress* high-volume-but-cosmetic ones so they don't mask serious crashes. Prioritize by *crash-free impact* and *velocity*, not raw count. Set up *alerts* only for meaningful spikes (not every issue). Keep custom logs *concise* (breadcrumbs, not spam). A curated Crashlytics is actionable; an unfiltered one is ignored."},{t:"list",items:["**Don't record** — expected exceptions as non-fatals.","**Filter/merge** — benign or split issues.","**Prioritize** — by impact/velocity, not raw count.","**Alerts** — only meaningful spikes; concise logs."]},{t:"note",text:"Avoid crash-report noise: don't record expected exceptions (routine cancellations) as non-fatals, filter/ignore known-benign issues, merge split ones, fix/suppress high-volume cosmetic ones that mask real crashes. Prioritize by crash-free impact and velocity (not raw count); alert only on meaningful spikes; keep logs concise breadcrumbs. A curated Crashlytics is actionable; an unfiltered one gets ignored."}]},{level:"junior",q:"What is a non-fatal versus an ANR versus a crash, for triage?",a:[{t:"p",text:"Three distinct production quality signals: a *crash* is the app terminating from an uncaught exception (highest severity — user loses their session). An *ANR* is the app *freezing* (unresponsive main thread) — the user sees 'isn't responding' (bad, sometimes worse-perceived than a crash). A *non-fatal* is a *handled* error you logged — lower severity but a signal of degraded experience. Triage order roughly: crashes and ANRs by user impact first (they break the app), then high-signal non-fatals."},{t:"list",items:["**Crash** — app terminates (uncaught); highest severity.","**ANR** — app freezes (main-thread block); often perceived worse.","**Non-fatal** — handled error; degraded but not broken.","**Triage** — crashes/ANRs by impact, then key non-fatals."]},{t:"note",text:"Crash = app terminates (uncaught exception). ANR = app freezes (unresponsive main thread — 'isn't responding', often perceived worse than a crash). Non-fatal = a handled error you logged (degraded, not broken). Triage crashes and ANRs by user impact first (they break the app), then high-signal non-fatals."}]},{level:"senior",q:"How do you set up alerting and on-call for production quality?",a:[{t:"p",text:"Configure *Crashlytics velocity alerts* and *new-issue* / *regression* alerts routed to *Slack/PagerDuty/email* (via integrations), plus *Play vitals* threshold alerts for crash/ANR rates. Define *thresholds* (crash-free drops below X, a new issue affects >Y sessions) and an *on-call* who triages within a set time. Tie in *rollout monitoring* so a regression at low % pages before wide exposure. Document a *runbook* (assess impact → flag-disable/halt → forward-fix). The goal: humans learn of regressions from alerts, not user reviews."},{t:"list",items:["**Alerts** — velocity/new-issue/regression → Slack/PagerDuty.","**Play vitals** — crash/ANR threshold alerts.","**Thresholds + on-call** — triage within a set time.","**Runbook** — assess → flag-disable/halt → forward-fix."]},{t:"note",text:"Set up Crashlytics velocity/new-issue/regression alerts to Slack/PagerDuty/email, plus Play vitals threshold alerts. Define thresholds (crash-free below X, issue affects >Y sessions) and an on-call who triages promptly; wire rollout monitoring so low-% regressions page early. Keep a runbook (assess → flag-disable/halt → forward-fix). Goal: learn of regressions from alerts, not reviews."}]},{level:"senior",q:"What are the limits of Analytics data (sampling, latency, cardinality)?",a:[{t:"p",text:"Console reports can be *sampled* (approximated at scale) and *delayed* (standard events process ~a day; DebugView is real-time). *Parameter cardinality* is limited — high-cardinality custom dimensions (e.g. user id) aren't good analytics params (and PII is banned). There are *quotas* on custom events/params/user properties. For precise, unsampled, low-latency analysis, use the *BigQuery export*. Knowing these limits prevents mis-reading reports (e.g. treating sampled numbers as exact or expecting instant data)."},{t:"list",items:["**Sampling** — reports approximate at scale.","**Latency** — ~1 day standard (DebugView real-time).","**Cardinality/quotas** — limited params/dimensions; no PII.","**Precise** — use the BigQuery export."]},{t:"note",text:"Analytics limits: console reports may be sampled (approximate at scale) and delayed (~1 day; DebugView real-time); parameter cardinality and custom event/property counts are quota-limited (and no PII/high-cardinality ids). For precise, unsampled, low-latency analysis use the BigQuery export. Knowing these prevents mis-reading sampled numbers as exact or expecting instant data."}]}],fT={id:"crashlytics-analytics",title:"Crashlytics & Analytics",tagline:"Crashlytics crash reporting & the mapping file, Analytics events & user properties, Performance Monitoring, and the observability pillars.",tags:["Firebase","Crashlytics","Analytics","Observability","Mapping File"],content:pT,qa:mT},gT=[{heading:"Remote Config — change behavior without a release",blocks:[{t:"p",text:"**Firebase Remote Config** lets you define values (feature flags, config parameters, content) *on the server* that your app fetches at runtime — so you can change your app's behavior *without shipping an app update*. This is powerful: you can toggle features on/off, adjust settings, change UI text or thresholds, or roll out features gradually, all from the Firebase console, and the change reaches users on their next config fetch — no new release, no review, no waiting for users to update."},{t:"code",title:"Using Remote Config",code:`val remoteConfig = Firebase.remoteConfig
remoteConfig.setDefaultsAsync(R.xml.remote_config_defaults)  // local fallback defaults

// Fetch and activate the server values
remoteConfig.fetchAndActivate().addOnCompleteListener {
    val newCheckoutEnabled = remoteConfig.getBoolean("new_checkout_enabled")
    val maxItems = remoteConfig.getLong("max_items")
    val bannerText = remoteConfig.getString("promo_banner")
    // Use the values to drive behavior
}`},{t:"list",items:["**Feature flags** — the killer use: ship a feature *behind a flag* (disabled by default), then enable it remotely — gradually (to a % of users), for specific segments, or instantly. This *decouples deploying code from releasing a feature*: the code is in the app, but the feature is off until you flip the flag server-side.","**Kill switch** — if a feature causes problems in production, *disable it via the flag* instantly, *without an app release* — the fastest possible mitigation for a bad feature (covered in the release-safety topic). This is a huge operational advantage.","**Targeting** — you can serve different values to different *conditions*: user segments (audiences from Analytics), app version, country, percentage (for gradual rollout / A/B). So a feature can be on for beta users, off for everyone else, or on for 10% of users.","**Defaults & fetch** — you set *local defaults* (used before/without a fetch, so the app works offline and on first run), then `fetchAndActivate` pulls server values (throttled/cached — you don't fetch every launch). The app reads the active values."]}]},{heading:"A/B Testing",blocks:[{t:"p",text:"**Firebase A/B Testing** builds on Remote Config and Analytics to run *experiments*: serve different variants of a feature/config to different user groups and measure which performs better against a goal (conversion, retention, engagement). Instead of guessing whether a new checkout flow or button color is better, you *test it* on a slice of users and let the data decide."},{t:"list",items:["**How it works**: you define variants via Remote Config values (variant A = old flow, variant B = new flow), assign each to a portion of users, and pick a *goal metric* (Analytics event — e.g. purchases). Firebase splits users, serves the variants, and reports which variant improved the metric with statistical confidence.","**Why**: data-driven product decisions. Rather than shipping a change to everyone and hoping it helps (or hurts), you validate it on a controlled experiment first — reducing the risk of a change that *seems* good but actually decreases conversion.","**Combines the tools**: Remote Config (delivers variants), Analytics (measures the goal + segments users), and the experiment framework (splits users, computes significance). It's the 'measure before committing' principle applied to product features."]}]},{heading:"App Distribution — getting builds to testers",blocks:[{t:"p",text:"**Firebase App Distribution** is a service for distributing *pre-release* builds (debug/staging/beta APKs or AABs) to your *testers* — QA, stakeholders, beta users — *outside* the Play Store. It's for getting a build into testers' hands quickly during development, without going through Play's tracks/review. You upload a build (manually or from CI), and testers get a notification/link to install it."},{t:"list",items:["**The problem it solves**: during development you need to share builds with testers *fast* and *frequently* — before they're ready for even Play's internal track, or for people/builds you don't want on Play at all. App Distribution makes this easy: upload a build, add tester emails/groups, they install via a link.","**vs Play testing tracks**: Play's internal/closed/open tracks distribute through Play (real Play delivery/signing, tied to your Play listing). App Distribution is *outside* Play — simpler and faster for early dev builds and ad-hoc sharing, but not the real Play delivery pipeline. Use App Distribution for *dev/QA iteration*, Play tracks for *pre-production validation of the real artifacts*.","**CI integration** — you typically wire App Distribution into CI so every merge/build is automatically distributed to testers, giving continuous access to the latest build. Combined with a tester feedback channel, it tightens the dev→test loop.","**Tester management** — organize testers into groups (QA, design, stakeholders) and control who gets which builds."]}]},{heading:"The Firebase ecosystem — how it fits together",blocks:[{t:"list",items:["**Firebase is a suite** of backend/infrastructure services that save you building common things: FCM (push), Crashlytics (crashes), Analytics (behavior), Performance (speed), Remote Config (dynamic config/flags), A/B Testing (experiments), App Distribution (test builds), Auth (sign-in), Firestore/Realtime DB (databases), Cloud Functions (serverless backend), Storage (files), and more.","**They interconnect**: Analytics builds *audiences* that Remote Config, A/B Testing, and FCM campaigns target; Remote Config powers A/B tests; Crashlytics + Performance + Analytics form observability. The value is an integrated toolkit where the pieces reinforce each other.","**When to use Firebase**: it's excellent for shipping fast without building infrastructure — great for startups, MVPs, and standard app needs (push, crashes, analytics, flags are near-universal). The tradeoffs: vendor lock-in (Google), cost at scale, and less control than a custom backend. For an Android engineer, the near-universal ones — Crashlytics, Analytics, FCM, Remote Config — are worth knowing deeply; the rest are situational.","**Remote Config's operational power is the standout**: the ability to flip features and configuration server-side without a release is transformative for release safety (kill switches, gradual rollout, decoupling deploy from release) — which is why it recurs in the release-safety discussion."]},{t:"note",text:"Remote Config: server-defined values (flags, config, content) the app fetches → change behavior WITHOUT a release. Feature flags decouple deploy from release; kill switch disables a bad feature instantly; targeting by segment/version/%/country; local defaults + fetchAndActivate. A/B Testing: run experiments (variants via Remote Config, goal via Analytics) — data-driven decisions. App Distribution: share pre-release builds with testers outside Play (fast dev/QA iteration, CI-integrated) — vs Play tracks for real-artifact validation. Firebase is an integrated suite; the near-universal ones (Crashlytics, Analytics, FCM, Remote Config) are worth knowing well."}]}],yT=[{level:"junior",q:"What is Firebase Remote Config and what can you use it for?",a:[{t:"p",text:"**Remote Config lets you define values (feature flags, config parameters, content) on the server that your app fetches at runtime, so you can change your app's behavior *without shipping an update*.** You set values in the Firebase console, the app fetches them, and the change reaches users on their next fetch — no new release, no Play review, no waiting for users to update."},{t:"list",items:["**Feature flags** — ship a feature *behind a flag* (off by default), then enable it remotely, gradually or for specific segments. This decouples *deploying code* from *releasing a feature*.","**Kill switch** — if a feature causes problems in production, disable it via the flag *instantly, without an app release* — the fastest way to mitigate a bad feature.","**Gradual rollout / targeting** — serve different values to different users by segment, app version, country, or percentage, so you can roll a feature out to 10% of users, or turn it on only for beta users.","**Config without a release** — adjust thresholds, URLs, promotional text, or settings server-side."]},{t:"p",text:"The power is the ability to *change behavior server-side without going through the whole build-release-review-adopt cycle*. You set *local defaults* (so the app works on first run and offline), then `fetchAndActivate` pulls the server values (cached/throttled — you don't fetch every launch). This is transformative for release safety: shipping risky features behind flags means you can enable them gradually and *turn them off instantly* if they misbehave — a kill switch that needs no release. It's why Remote Config is one of the near-universal Firebase tools worth knowing well."}]},{level:"junior",q:"What is Firebase A/B Testing?",a:[{t:"p",text:"**Firebase A/B Testing lets you run experiments — serve different variants of a feature or config to different groups of users and measure which performs better against a goal (conversion, retention, engagement) — so you make data-driven product decisions instead of guessing.** It builds on Remote Config (to deliver the variants) and Analytics (to measure the outcome)."},{t:"list",items:["**How it works**: you define variants as Remote Config values (variant A = current design, variant B = new design), assign each to a portion of users, and pick a *goal metric* (an Analytics event, e.g. purchases). Firebase splits users, serves the variants, measures the goal, and reports which variant improved it with statistical confidence.","**Why**: rather than shipping a change to everyone and hoping it helps, you *test* it on a controlled slice first and let the data decide. This catches changes that *seem* better but actually hurt (a redesign that lowers conversion).","**Combines the tools**: Remote Config (delivers variants), Analytics (measures + segments), and the experiment framework (splits users, computes significance)."]},{t:"p",text:"It's the 'measure before committing' principle applied to product features — the same data-driven discipline as profiling before optimizing, but for product decisions. Instead of debates about which design or flow is better, you run an experiment and the metrics answer. Once a winning variant is proven, you roll it out to everyone (via Remote Config). This reduces the risk of large product changes and grounds decisions in evidence rather than opinion — valuable for any feature where you're uncertain of the impact."}]},{level:"junior",q:"What is Firebase App Distribution and how does it differ from Play testing tracks?",a:[{t:"p",text:"**Firebase App Distribution is a service for distributing pre-release builds (debug/staging/beta APKs or AABs) to your testers — QA, stakeholders, beta users — *outside* the Play Store. You upload a build (often from CI), add testers, and they get a link to install it.** It's for getting builds into testers' hands quickly during development, without going through Play's tracks or review."},{t:"list",items:["**App Distribution** — distributes *outside* Play, directly to testers you specify. Fast and simple: upload a build, add tester emails/groups, they install via a link/notification. No Play review, no tie to your Play listing. Great for frequent dev/QA iteration and ad-hoc sharing.","**Play testing tracks** (internal/closed/open) — distribute *through* Play, using the real Play delivery and signing pipeline, tied to your Play listing. Slower to set up but they validate the *actual* Play-delivered, Play-signed artifacts and the release process itself."]},{t:"p",text:"So the distinction is *dev/QA iteration* vs *pre-production validation of the real thing*. Use App Distribution during active development to share the latest builds with testers rapidly (typically wired into CI so every build is distributed automatically, tightening the dev→test loop). Use Play testing tracks when you're closer to release and want to verify the genuine Play-generated APKs and delivery mechanics before production. They're complementary — App Distribution for fast internal iteration, Play tracks for validating the real release pipeline. A common workflow: CI distributes every build via App Distribution for QA, and release candidates go to Play internal testing before promoting toward production."}]},{level:"senior",q:"How does Remote Config enable safer releases, and what are the pitfalls to watch for?",a:[{t:"p",text:"**Remote Config enables safer releases primarily by *decoupling deploying code from releasing a feature* — you ship features behind flags (off by default) and enable them server-side gradually, with the ability to instantly disable (kill-switch) a problematic feature *without an app release*. This transforms release risk: the riskiest changes become reversible in minutes, not a multi-day release cycle. But there are real pitfalls in defaults, fetch behavior, and flag hygiene.**"},{t:"list",items:["**Decoupling deploy from release**: ship the feature code in the app binary but gated behind a flag defaulting to *off*. The binary reaches users (via normal release), but the feature stays dark until you enable the flag server-side. Then you can enable it *gradually* (10% → 50% → 100% via percentage targeting), for *specific segments* (beta users first), and *monitor* (Crashlytics/Analytics) as you ramp — catching problems at low exposure, just like a staged rollout but at the *feature* level and *without* needing a new binary.","**Kill switch — the standout safety benefit**: if an enabled feature causes crashes or issues, flip the flag *off* and users' apps disable it on their next config fetch — the *fastest* production mitigation available (minutes, no release, no review). This is why 'ship risky features behind a flag' is a core release-safety practice: it turns 'critical bug at 100%' from a hotfix scramble into a config toggle.","**Experimentation and targeted rollout**: combined with A/B testing, you validate a feature on a slice before full rollout, and targeting lets you enable features per version/segment/region — reducing the blast radius of any change."]},{t:"list",items:["**Pitfall 1 — defaults and offline/first-run**: the app must have sensible *local defaults* (in-app fallback values) because before the first successful fetch (first launch, offline), Remote Config returns the defaults. If your defaults are wrong (a feature defaulting *on* that should be off, or unsafe values), users get bad behavior until a fetch succeeds. Always set safe local defaults — typically new features default *off* — so the pre-fetch state is safe.","**Pitfall 2 — fetch timing and caching**: Remote Config *caches* values and *throttles* fetches (you can't fetch every launch — there are minimum fetch intervals to avoid hammering the server). So a flag change *doesn't* reach all users instantly — it propagates as their apps fetch (which may be minutes to hours depending on your fetch interval and app usage). For a kill switch you want a *short* fetch interval so disabling reaches users fast, but too short wastes resources — a real tradeoff. And a user who never opens the app won't get the change. So Remote Config is *not* instant for everyone; understand the propagation delay.","**Pitfall 3 — flag rot / complexity**: flags accumulate. A codebase littered with stale flags (features fully rolled out but never cleaned up, dead code behind permanently-off flags) becomes hard to reason about — every flag is a branch, and N flags = 2^N possible states, most untested. Discipline: *remove flags* once a feature is fully rolled out and stable (clean up the flag *and* the code path), and don't gate everything behind flags — reserve them for genuinely risky or gradually-rolled-out features.","**Pitfall 4 — testing the combinations**: because flags create branching behavior, you must test *both* states of important flags (on and off), and the app must behave correctly for *any* combination of flag values the server might send (including unexpected ones). A feature that only works with a specific flag combination is fragile.","**Pitfall 5 — security/trust**: don't put secrets in Remote Config (it's fetched to the client, not secret), and validate/sanitize values (a malformed server value shouldn't crash the app — hence robust defaults and type-safe reads)."]},{t:"p",text:"**The senior framing**: Remote Config's release-safety value comes from *decoupling code deployment from feature activation* — ship dark, enable gradually server-side, and kill instantly if needed — which makes the riskiest changes reversible without a release, the single most powerful safety mechanism for a large app. But it's not magic: the safety depends on *safe defaults* (for the pre-fetch/offline state), understanding *propagation delay* (it's not instant — flags reach users as they fetch, so a kill switch has a short-but-nonzero lag and misses inactive users), and *flag hygiene* (removing stale flags to avoid combinatorial complexity and dead code). The mature perspective is that flags are a powerful *operational* tool with a real *maintenance cost* — use them deliberately for risky/gradual features, default new features off, tune fetch intervals for your kill-switch responsiveness needs, and *retire* flags after rollout. Demonstrating both the decouple-and-kill-switch benefit *and* the defaults/propagation/flag-rot pitfalls — rather than treating Remote Config as a free instant control — is the balanced senior answer."}]},{level:"senior",q:"When would you choose Firebase for a project, and what are the tradeoffs?",a:[{t:"p",text:"**Firebase is an integrated suite of backend/infrastructure services that lets you ship fast without building common infrastructure yourself — I'd choose it when speed-to-market and standard needs (push, crashes, analytics, flags, auth, a database) outweigh the desire for full control, which is true for most startups, MVPs, and standard apps. The tradeoffs are vendor lock-in, cost at scale, and less control than a custom backend.**"},{t:"list",items:["**What Firebase gives you**: FCM (push), Crashlytics (crashes), Analytics (behavior), Performance (speed), Remote Config (flags/config), A/B Testing (experiments), App Distribution (test builds), Auth (sign-in with many providers), Firestore/Realtime DB (databases with real-time sync and offline support), Cloud Functions (serverless), Storage (files). These cover a huge fraction of what a typical app backend needs, pre-built and interconnected.","**Why choose it — speed and integration**: you avoid building and operating infrastructure (a push service, crash pipeline, analytics, auth, a real-time database with offline sync). For a small team or an MVP, this is enormous leverage — you focus on the app, not the backend. And the pieces *interconnect* (Analytics audiences → Remote Config/A-B/FCM targeting; Crashlytics + Performance + Analytics = observability), so the whole is more than the parts. Near-universal needs — crash reporting, analytics, push, feature flags — are genuinely well-served and worth adopting almost regardless."]},{t:"list",items:["**Tradeoff 1 — vendor lock-in**: your app becomes coupled to Google's ecosystem — Firestore's data model, Auth's flows, FCM's mechanics. Migrating off later is costly. Mitigate by *abstracting* Firebase behind your own interfaces (a repository over Firestore, an auth abstraction) so the coupling is contained, and by being deliberate about which services you deeply depend on (crash/analytics/push are easy to swap; a Firestore-centric data model is not).","**Tradeoff 2 — cost at scale**: Firebase's pricing (especially Firestore reads/writes, Cloud Functions, and bandwidth) can become expensive as you grow — usage-based pricing that's cheap at small scale can surprise you at large scale. A cost model that's fine for an MVP might favor a custom backend at millions of users. Evaluate the cost trajectory, not just the starting price.","**Tradeoff 3 — less control and flexibility**: managed services do things *their* way. Firestore's query capabilities are limited compared to a full SQL database (no complex joins, restricted queries); Auth's flows are prescribed; you can't tune the infrastructure. For apps with unusual requirements (complex data relationships, specific compliance/data-residency needs, heavy custom backend logic), the constraints chafe, and a custom backend gives control the managed service can't.","**Tradeoff 4 — data and compliance**: your data lives on Google's infrastructure, which raises data-residency, privacy, and compliance considerations (GDPR, region requirements) that some organizations can't accept."]},{t:"list",items:["**A pragmatic middle path**: use Firebase's *observability and operational* tools (Crashlytics, Analytics, Performance, Remote Config, FCM) almost universally — they're low-lock-in, high-value, and hard to justify building yourself — while being more *deliberate* about the *data/backend* services (Firestore, Auth, Functions) where lock-in and cost are higher and a custom or hybrid backend may be warranted as you scale. Abstract the deeper dependencies behind interfaces to preserve optionality."]},{t:"p",text:"**The senior framing**: the Firebase decision is a classic *buy vs build* / *managed vs custom* tradeoff — you're trading *control, long-term cost, and flexibility* for *speed, reduced operational burden, and integration*. For most apps early on, that trade strongly favors Firebase: shipping fast with proven infrastructure beats building your own, and the observability tools (crashes, analytics, push, flags) are near-universal wins. The nuance is recognizing *which* services carry more lock-in/cost risk (data-layer: Firestore, Auth, Functions) versus which are low-risk high-value (Crashlytics, Analytics, FCM, Remote Config), and *architecting to contain the deeper dependencies* (abstractions/repositories) so you keep optionality as scale and requirements evolve. The immature answer is 'Firebase is great, use it for everything' or 'never use it, build your own'; the mature answer is a *differentiated* view — adopt the low-lock-in operational tools freely, be deliberate and abstraction-guarded about the data-layer services, and re-evaluate the cost/control tradeoff as you scale. Demonstrating that buy-vs-build framing with the specific lock-in/cost/control tradeoffs and the service-differentiated, abstraction-guarded strategy is the comprehensive senior answer."}]},{level:"senior",q:"How does Remote Config fetch, cache, and activate values?",a:[{t:"p",text:"Remote Config has a two-step model: *fetch* downloads the latest values from the server into a *pending cache*, and *activate* makes them the *active* values your app reads. This separation lets you fetch in the background but *apply changes at a safe moment* (e.g. next launch, not mid-session). A *minimum fetch interval* throttles server calls (default ~12h in production; lower for dev). Values are *cached locally*, so the app has usable config offline. Common pattern: `fetchAndActivate()` on startup, then read config."},{t:"list",items:["**Fetch** — download to a pending cache.","**Activate** — promote pending → active (read by app).","**Separation** — apply changes at a safe moment.","**Fetch interval** — throttles calls (~12h prod); cached offline."]},{t:"note",text:"Remote Config: fetch downloads latest values to a pending cache; activate promotes them to active (what the app reads) — so you apply changes at a safe moment (next launch), not mid-session. A minimum fetch interval throttles calls (~12h prod). Values cache locally (works offline). Common: fetchAndActivate() on startup, then read."}]},{level:"junior",q:"What are default values in Remote Config, and why set them?",a:[{t:"p",text:"*In-app defaults* (`setDefaultsAsync`) are the values the app uses *before any successful fetch* — on first launch, offline, or if the fetch fails. Setting sensible defaults ensures the app *always has valid config* and behaves correctly without the network. Best practice: defaults should represent the *safe/baseline* behavior, and server values override them when available. Never rely on a fetch having succeeded to have usable config — always ship defaults matching your intended baseline."},{t:"list",items:["**Defaults** — used before/without a successful fetch.","**Cover** — first launch, offline, fetch failure.","**Safe baseline** — server overrides when available.","**Always ship them** — never assume a fetch succeeded."]},{t:"note",text:"In-app defaults (setDefaultsAsync) are used before any successful fetch — first launch, offline, or fetch failure — so the app always has valid config. Make them the safe/baseline behavior; server values override when available. Never rely on a fetch having succeeded; always ship defaults matching your intended baseline."}]},{level:"senior",q:"What are Remote Config conditions, and how do you target segments?",a:[{t:"p",text:"*Conditions* let a parameter serve *different values to different users* based on rules: app version, platform, language/country, user *Analytics audience*, a *percentage* of users (random), or a user property. So you can enable a feature for 10% of users, or only on app version ≥ X, or only for 'premium' audience. This powers *gradual rollouts*, *targeting*, and *A/B tests* without a new app release. Conditions are evaluated server-side per fetch, so the same parameter resolves per-user."},{t:"list",items:["**Conditions** — rules serving different values per user.","**Criteria** — version, platform, country, audience, %.","**Enables** — gradual rollout, targeting, A/B tests.","**Server-evaluated** — per user, per fetch."]},{t:"note",text:"Remote Config conditions serve different parameter values by rules — app version, platform, country/language, Analytics audience, a random percentage, or user property. Enable a feature for 10%, or only version ≥ X, or 'premium' audience — powering gradual rollouts, targeting, and A/B tests without a release. Evaluated server-side per user per fetch."}]},{level:"senior",q:"How do you implement a feature flag / kill switch with Remote Config?",a:[{t:"p",text:'Add a boolean parameter (e.g. `new_checkout_enabled`), gate the feature on it (`if (config.getBoolean("new_checkout_enabled")) ...`), and ship the feature *off by default*. You can then *turn it on* (globally or for a % / audience) *without a release*, and — crucially — *turn it off instantly* if it misbehaves (a *kill switch*), which is vital since you can\'t roll back an app version. Fetch/activate at startup so the flag is current; consider `realtime Remote Config` for near-instant propagation of a kill.'},{t:"code",title:"Flag-gated feature",code:`// default false; enable server-side gradually, kill instantly if broken
if (Firebase.remoteConfig.getBoolean("new_checkout_enabled")) {
  showNewCheckout()
} else {
  showLegacyCheckout()
}`},{t:"note",text:"Feature flag/kill switch: a boolean parameter gating the feature, shipped off by default — turn it on gradually (%/audience) without a release and turn it off instantly if broken (kill switch, vital since apps can't roll back). Fetch/activate at startup so it's current; use realtime Remote Config for near-instant kill propagation."}]},{level:"senior",q:"What is realtime Remote Config, and when is it useful?",a:[{t:"p",text:"*Realtime Remote Config* lets the app *subscribe* (`addOnConfigUpdateListener`) and be *notified within seconds* when config changes on the server, then fetch/activate — instead of waiting for the next periodic fetch. Useful for *fast kill switches* (disable a broken feature and have live users pick it up quickly), live-event toggles, or urgent config changes. Handle updates carefully (apply at a safe point, not disruptively mid-flow). It complements the throttled periodic model with a push channel for urgency."},{t:"list",items:["**Realtime** — subscribe; notified in seconds of changes.","**Then** — fetch/activate immediately.","**Useful** — fast kill switch, live toggles, urgent changes.","**Apply carefully** — at a safe point, not mid-flow."]},{t:"note",text:"Realtime Remote Config lets the app subscribe (addOnConfigUpdateListener) and get notified within seconds of server changes, then fetch/activate — instead of waiting for the periodic fetch. Useful for fast kill switches, live-event toggles, urgent changes. Apply updates at a safe point (not disruptively mid-flow). A push channel complementing the throttled periodic model."}]},{level:"junior",q:"What is Firebase A/B Testing, and how does it relate to Remote Config?",a:[{t:"p",text:"*A/B Testing* builds on Remote Config (and/or Notifications): you define *variants* (different Remote Config values), Firebase *randomly assigns* users to variants, and it measures a *goal metric* (via Analytics — conversion, retention, revenue) with *statistical significance*, telling you which variant wins. So Remote Config *delivers* the variant values; A/B Testing *orchestrates the experiment and analysis*. Use it to make *data-driven* decisions (does the new onboarding actually improve retention?) rather than guessing."},{t:"list",items:["**Built on** — Remote Config variants.","**Firebase** — randomizes users, measures a goal via Analytics.","**Significance** — tells you the winning variant.","**Data-driven** — decisions vs guessing."]},{t:"note",text:"Firebase A/B Testing builds on Remote Config: define variants (different config values), Firebase randomly assigns users and measures a goal metric via Analytics with statistical significance to find the winner. Remote Config delivers variant values; A/B Testing orchestrates the experiment/analysis — data-driven decisions (does new onboarding improve retention?) instead of guessing."}]},{level:"senior",q:"How do you run a statistically valid A/B test?",a:[{t:"p",text:"Define *one clear hypothesis* and a *primary goal metric* upfront, *randomize* users into control + variant(s), size the experiment for enough *sample/duration* to reach *significance* (don't peek and stop early — that inflates false positives), and change *one variable* so you know what caused the result. Guard against *novelty effects* (run long enough) and *seasonality*. Only ship the winner if the lift is significant *and* meaningful. Firebase computes significance; your job is a sound design and honest interpretation."},{t:"list",items:["**One hypothesis + primary metric** — defined upfront.","**Randomize** — control + variants; change one variable.","**Sufficient sample/duration** — for significance; don't peek.","**Guard** — novelty/seasonality; ship only significant + meaningful."]},{t:"note",text:"Valid A/B test: one hypothesis + a primary goal metric upfront, randomize into control + variant(s), run long enough for significance (don't peek/stop early — inflates false positives), change one variable. Guard against novelty effects/seasonality; ship the winner only if the lift is significant AND meaningful. Firebase computes significance; you own sound design + honest interpretation."}]},{level:"junior",q:"What is Firebase App Distribution, and how does it differ from Play testing tracks?",a:[{t:"p",text:"*App Distribution* distributes *pre-release builds* (APK or AAB) to a chosen list of *testers* directly (via email invite / a tester app), *without going through Play review* — so it's *fast and flexible* for internal/QA/stakeholder builds. Play *testing tracks* go through Play's pipeline (review-lite, Play delivery) and are a step toward production. Use App Distribution for quick dogfooding and ad-hoc test builds (even debuggable ones); use Play tracks for structured pre-release testing on the path to release."},{t:"table",headers:["","App Distribution","Play tracks"],rows:[["Review","None","Play pipeline"],["Speed","Fast, ad-hoc","Slower, structured"],["Use","Dogfooding/QA builds","Pre-release toward prod"]]},{t:"note",text:"Firebase App Distribution sends pre-release builds (APK/AAB) directly to a tester list (email/tester app) without Play review — fast, flexible for internal/QA/stakeholder builds. Play testing tracks go through Play's pipeline toward production. Use App Distribution for quick dogfooding/ad-hoc builds; Play tracks for structured pre-release testing on the release path."}]},{level:"senior",q:"How does Remote Config enable safer releases, and what are the pitfalls?",a:[{t:"p",text:"It *decouples feature launch from app release*: ship code dark (flag off), enable gradually (%/audience), and kill instantly if broken — so a bad feature doesn't require an app rollback (impossible anyway). Pitfalls: *fetch throttling* means changes aren't instant (mitigate with realtime/lower interval and applying on next launch), *stale/inconsistent state* if you activate mid-session, *default drift* (defaults must match a safe baseline), *too many flags* (config sprawl/tech debt — remove stale flags), and forgetting that *offline* users run on defaults. Also test *both* flag states."},{t:"list",items:["**Decouples** — launch from release; gradual + kill switch.","**Pitfalls** — throttling delay, mid-session activation, default drift.","**Flag sprawl** — remove stale flags (tech debt).","**Test** — both states; offline runs on defaults."]},{t:"note",text:"Remote Config decouples feature launch from release (ship dark, enable gradually, kill instantly — no app rollback needed). Pitfalls: fetch throttling (changes not instant — use realtime/apply on launch), mid-session activation causing inconsistency, default drift (must be a safe baseline), flag sprawl (remove stale flags), offline users on defaults. Test both flag states."}]},{level:"senior",q:"How do you clean up feature flags and avoid config debt?",a:[{t:"p",text:"Flags are *temporary* by intent but rot into permanent complexity if left. Practice: *name and document* each flag with an owner and an *expiry/removal plan*, remove the flag *and the dead code path* once a feature is fully rolled out (or killed), audit flags periodically, and distinguish *release flags* (short-lived, delete after rollout) from *operational* toggles (long-lived kill switches). Too many stale flags create untested combinations and confusing branching. Treat flag removal as part of finishing a feature."},{t:"list",items:["**Document** — owner + expiry/removal plan per flag.","**Remove** — flag AND dead path after full rollout.","**Distinguish** — short-lived release flags vs long-lived toggles.","**Audit** — periodically; stale flags = untested combos."]},{t:"note",text:"Flags are meant to be temporary but rot into complexity. Document each with an owner + removal plan, delete the flag AND its dead code path after full rollout/kill, distinguish short-lived release flags from long-lived operational kill switches, and audit periodically. Stale flags create untested combinations. Treat flag removal as part of finishing a feature."}]},{level:"junior",q:"What parameter types and value sources does Remote Config support?",a:[{t:"p",text:"Parameters can be read as *boolean*, *long*, *double*, *string*, or *JSON/byte array* (`getString` then parse for complex config). The *value source* for a read is: *remote* (fetched+activated server value), *default* (your in-app default), or *static* (the type's zero value if neither exists) — you can check the source to know whether a real value was applied. Use JSON strings for structured config (a list of items, a settings object) parsed on-device. Keep types consistent with server definitions."},{t:"list",items:["**Types** — boolean, long, double, string, JSON/bytes.","**Sources** — remote, default, static (zero).","**JSON string** — for structured/complex config.","**Check source** — know if a real value applied."]},{t:"note",text:"Remote Config reads as boolean, long, double, string, or JSON/byte array (parse for complex config). Value source is remote (fetched+activated), default (in-app), or static (type zero if neither) — checkable to know if a real value applied. Use JSON strings for structured config parsed on-device. Keep types consistent with server definitions."}]},{level:"senior",q:"How do you avoid a Remote Config change breaking older app versions?",a:[{t:"p",text:"Old app versions still fetch config, so a value or a *new required parameter* can break them if they don't handle it. Guard with *app-version conditions* (serve certain values only to versions ≥ X), keep *backward-compatible defaults and parsing* (tolerate missing/unknown fields in JSON config), never make a new flag *required* for old clients, and test config changes against *supported old versions*. Treat Remote Config like a *server API contract*: changes must be compatible with every app version still in the wild."},{t:"list",items:["**Old versions** — still fetch; can break on new values.","**Version conditions** — scope values to versions ≥ X.","**Tolerant parsing** — handle missing/unknown fields.","**Contract** — compatible with all live versions; test them."]},{t:"note",text:"Old app versions still fetch config, so a value/new parameter can break them. Guard with app-version conditions (values only for versions ≥ X), backward-compatible defaults and tolerant JSON parsing (handle missing/unknown fields), never require a new flag for old clients, and test against supported old versions. Treat Remote Config like a server API contract — compatible with every live version."}]},{level:"junior",q:"What is the minimum fetch interval, and why does it matter?",a:[{t:"p",text:"The *minimum fetch interval* is the shortest time between successful server fetches — default ~*12 hours* in production (to limit server load/battery). Fetching more often than this returns *cached* values (no server call) unless you lower the interval. In *development* you set it low (or 0) to see changes immediately; in *production* keep it reasonable (or use *realtime* Remote Config for urgent changes). Misunderstanding this causes 'my config change isn't showing up' — it's throttled, not broken."},{t:"list",items:["**Interval** — shortest gap between server fetches (~12h prod).","**Within interval** — returns cache, no server call.","**Dev** — set low/0 for instant changes.","**Urgent prod** — use realtime Remote Config."]},{t:"note",text:"The minimum fetch interval is the shortest gap between successful fetches — ~12h in production (limits server load/battery). Fetching sooner returns cached values unless you lower it. Set it low/0 in dev for instant changes; keep reasonable in prod (or use realtime for urgent). Explains 'my config change isn't showing' — it's throttled, not broken."}]},{level:"senior",q:"How does Remote Config personalization differ from A/B testing?",a:[{t:"p",text:"*A/B testing* finds the *single best variant for everyone* (then you roll it out). *Remote Config Personalization* uses *machine learning* to *automatically choose the best value per user* to optimize a goal — different users may get different values that maximize the objective (e.g. engagement). So A/B is *experiment → pick one winner*; Personalization is *continuous per-user optimization*. Use A/B to learn, Personalization to auto-optimize when the best value genuinely varies by user. Both build on Remote Config parameters."},{t:"list",items:["**A/B testing** — find one best variant for all.","**Personalization** — ML picks the best value per user.","**A/B** — experiment then pick a winner.","**Personalization** — continuous per-user optimization."]},{t:"note",text:"A/B testing finds the single best variant for everyone (then roll out). Remote Config Personalization uses ML to automatically choose the best value per user to optimize a goal (different users get different values). A/B = experiment → one winner; Personalization = continuous per-user optimization. Use A/B to learn, Personalization when the best value varies by user."}]},{level:"junior",q:"How do testers get and install builds from App Distribution?",a:[{t:"p",text:"You upload a build (via the console, Gradle plugin, `firebase` CLI, or CI) and assign it to *tester groups*. Testers receive an *email invite*, accept, and install via the *App Tester* app (or a link) — which also *notifies them of new builds*. No Play account or review needed. You can attach *release notes* and see *who installed*. It's ideal for CI-driven dogfooding: every merge pushes a build testers can grab minutes later. Testers must enroll their device once."},{t:"list",items:["**Upload** — console/Gradle/CLI/CI; assign tester groups.","**Testers** — email invite → App Tester app/link.","**Notifies** — of new builds; shows who installed.","**CI-driven** — every merge → a grabbable build."]},{t:"note",text:"Upload a build (console/Gradle plugin/firebase CLI/CI) and assign tester groups; testers get an email invite and install via the App Tester app or a link (notified of new builds), no Play account/review. Attach release notes; see who installed. Ideal for CI-driven dogfooding — every merge pushes a grabbable build. Testers enroll their device once."}]},{level:"senior",q:"When would you choose Remote Config over a custom config endpoint?",a:[{t:"p",text:"*Remote Config* gives you targeting/conditions, A/B testing, gradual rollout, caching/offline, and a console — *for free*, no backend to build. Choose it for feature flags, UI/values tuning, and experiments. A *custom endpoint* is better when you need *highly dynamic/large/frequently-changing* data, *complex server logic*, *tight coupling to your data model*, *sub-second freshness beyond realtime's guarantees*, or you already have config infra. Many apps use *both*: Remote Config for flags/experiments, your API for domain config. Match the tool to freshness/complexity needs."},{t:"list",items:["**Remote Config** — flags, targeting, A/B, offline, no backend.","**Custom endpoint** — dynamic/large data, complex logic, tight coupling.","**Both** — Config for flags, API for domain config.","**Match** — freshness/complexity to the tool."]},{t:"note",text:"Remote Config gives targeting, A/B, gradual rollout, caching/offline, and a console free (no backend) — choose it for flags, value tuning, experiments. A custom endpoint suits highly dynamic/large/frequently-changing data, complex server logic, or tight data-model coupling. Many apps use both: Config for flags/experiments, your API for domain config. Match freshness/complexity to the tool."}]},{level:"junior",q:"What is a rollout in Remote Config, and how does it differ from a Play staged rollout?",a:[{t:"p",text:"A Remote Config *rollout* gradually enables a *parameter change* (a feature flag) to an increasing % of users — *without shipping a new app version* (the code is already installed, just flag-gated). A Play *staged rollout* gradually delivers a *new app binary* to a % of users. They operate at different layers: Play rolls out *code*; Remote Config rolls out *behavior* within already-shipped code. Combine them — ship the binary via a Play staged rollout, then enable its new feature via a Remote Config rollout for finer control."},{t:"list",items:["**Config rollout** — enable a flag to a growing %; no new binary.","**Play rollout** — deliver a new binary to a %.","**Layers** — Config = behavior; Play = code.","**Combine** — Play ships code, Config enables the feature."]},{t:"note",text:"A Remote Config rollout gradually enables a parameter/flag change to more users without a new app version (code already installed, flag-gated). A Play staged rollout delivers a new binary to a % of users. Config rolls out behavior; Play rolls out code. Combine: Play ships the binary, Config enables its feature — finer control."}]},{level:"senior",q:"How do you keep Remote Config and A/B experiments from skewing analytics?",a:[{t:"p",text:"Log an *experiment/variant identifier* (as a user property or event param) so analytics can be *segmented by variant* (otherwise a variant's behavior is mixed into aggregate metrics, hiding effects). Ensure *consistent assignment* (a user stays in one variant) so their data isn't split. Be aware experiments *change behavior*, so compare *within the experiment's control vs variant*, not against pre-experiment baselines. Clean up: stop logging an experiment's props after it ends. This keeps both the experiment analysis and general analytics interpretable."},{t:"list",items:["**Log variant id** — segment analytics by variant.","**Consistent assignment** — user stays in one variant.","**Compare** — control vs variant within the experiment.","**Clean up** — stop logging after it ends."]},{t:"note",text:"Keep experiments from skewing analytics: log a variant identifier (user property/param) to segment by variant (else effects hide in aggregates), ensure consistent assignment (user stays in one variant), compare control vs variant within the experiment (not vs pre-experiment baselines), and stop logging after it ends. Keeps both experiment and general analytics interpretable."}]},{level:"junior",q:"What are Remote Config templates, and how do you manage changes safely?",a:[{t:"p",text:"The full set of parameters/conditions is a *template*. Firebase *versions* templates — you can see *change history*, *who changed what*, and *roll back* to a previous template if a change breaks things. Manage safely: use the *staging vs production* separation where possible, *review* changes (treat config like code), *publish deliberately*, and *roll back* fast on problems. The *Remote Config REST API* lets you automate/validate template changes in CI. Versioning + rollback is your safety net for config mistakes."},{t:"list",items:["**Template** — the full parameter/condition set.","**Versioned** — history, author, rollback.","**Manage** — review changes, publish deliberately.","**REST API** — automate/validate in CI."]},{t:"note",text:"The full parameter/condition set is a template; Firebase versions it — change history, author, and rollback to a previous template if something breaks. Manage safely: review changes (config-as-code), publish deliberately, roll back fast on problems, and use the Remote Config REST API to automate/validate in CI. Versioning + rollback is your safety net for config mistakes."}]},{level:"senior",q:"How do you test a feature behind a Remote Config flag?",a:[{t:"p",text:"Test *both flag states* (on and off) — an unshipped-but-present code path is still code that must work. In tests, *inject the flag value* (wrap Remote Config behind an interface / provide a fake config) so unit/UI tests can force each state deterministically, rather than depending on a real fetch. Manually QA both paths on a build (use a low fetch interval / debug override to flip the flag). For experiments, verify each *variant* renders/behaves correctly. Never ship a flag you've only tested in one state."},{t:"list",items:["**Both states** — on and off; both are live code.","**Inject the flag** — interface/fake config for deterministic tests.","**Manual QA** — flip via low interval/debug override.","**Experiments** — verify each variant."]},{t:"note",text:"Test a flagged feature in both states (on/off — both are live code). Wrap Remote Config behind an interface / inject a fake config so unit/UI tests force each state deterministically (no real fetch). Manually QA both paths (low fetch interval/debug override to flip). For experiments, verify each variant. Never ship a flag tested in only one state."}]},{level:"junior",q:"What is the difference between App Distribution and internal app sharing / TestFlight?",a:[{t:"p",text:"*Firebase App Distribution* is *cross-platform* (Android + iOS) pre-release distribution to a tester list, integrated with Firebase/CI. *Play internal app sharing* is Android-only, quick ad-hoc sharing via Play (generates the real delivered build). *TestFlight* is Apple's equivalent for iOS. If you build for both platforms, App Distribution gives *one* tool/flow for both; if you're Android-only and want Play-accurate delivery, internal app sharing is handy. They overlap but differ in platform reach and integration."},{t:"list",items:["**App Distribution** — cross-platform (Android+iOS), Firebase/CI.","**Internal app sharing** — Android-only, Play-accurate, ad-hoc.","**TestFlight** — Apple's iOS equivalent.","**Choose** — by platform reach + integration."]},{t:"note",text:"Firebase App Distribution: cross-platform (Android+iOS) pre-release distribution to testers, Firebase/CI-integrated. Play internal app sharing: Android-only, ad-hoc, Play-accurate delivery. TestFlight: Apple's iOS equivalent. For both platforms App Distribution gives one flow; for Android-only Play-accurate builds, internal app sharing. Overlap but differ in reach/integration."}]},{level:"senior",q:"How do you handle Remote Config activation timing to avoid UI inconsistency?",a:[{t:"p",text:"If you *activate* new values *mid-session*, the UI can show *inconsistent state* (part of the screen on old config, part on new) or change under the user's feet. Best practice: *fetch* in the background anytime, but *activate at a safe boundary* — typically *next app launch* (fetch this session, activate on the next), or a clean navigation point. For urgent kills, activate immediately but ensure the affected UI *re-reads* config cleanly. Decide per parameter whether it can change live or must wait for a restart."},{t:"list",items:["**Mid-session activate** — risks inconsistent/changing UI.","**Fetch anytime** — activate at a safe boundary.","**Typical** — activate on next launch.","**Urgent kill** — activate now but re-read cleanly."]},{t:"note",text:"Activating new config mid-session risks inconsistent UI (part old, part new) or changing under the user. Best practice: fetch anytime, activate at a safe boundary — usually next launch (fetch now, activate next), or a clean navigation point. For urgent kills, activate immediately but ensure affected UI re-reads cleanly. Decide per parameter: live-changeable vs restart-required."}]},{level:"senior",q:"How do you architect a testable feature-flag layer around Remote Config?",a:[{t:"p",text:"Don't call `Firebase.remoteConfig` directly throughout the app — wrap it behind a *`FeatureFlags` interface* (e.g. `fun isNewCheckoutEnabled(): Boolean`) with a Remote Config-backed implementation and a *fake* for tests. Centralize flag *keys and defaults* in one place, expose flags as *typed* accessors (not raw string lookups scattered around), and optionally as *Flows* so UI reacts to changes. This gives testability (inject the fake), a single source of truth, refactor safety, and decoupling from the Firebase SDK (swappable). It's the clean-architecture way to consume config."},{t:"list",items:["**Wrap** — `FeatureFlags` interface + Remote Config impl + fake.","**Centralize** — keys/defaults; typed accessors, not raw lookups.","**Optionally Flows** — UI reacts to changes.","**Benefits** — testable, single source, SDK-decoupled."]},{t:"note",text:"Wrap Remote Config behind a FeatureFlags interface (typed accessors like isNewCheckoutEnabled()) with a real impl + a fake for tests; centralize keys/defaults in one place; optionally expose flags as Flows so UI reacts. Benefits: testability (inject fake), single source of truth, refactor safety, decoupling from the Firebase SDK. The clean way to consume config."}]},{level:"junior",q:"What happens to Remote Config when the app is offline?",a:[{t:"p",text:"Remote Config *caches the last activated values locally*, so an offline app *keeps using the most recently fetched/activated config* — and if it never fetched successfully, it uses your *in-app defaults*. Fetches simply fail silently offline (you keep the cached/default values). This is why shipping sensible defaults matters and why the app must never *block* on a fetch. When connectivity returns, the next fetch updates the cache (activated per your timing). Design so the app is fully functional on cached/default config."},{t:"list",items:["**Cached values** — used offline (last activated).","**No prior fetch** — falls back to in-app defaults.","**Fetch fails silently** — keep cached/defaults.","**Design** — never block on a fetch; work on defaults."]},{t:"note",text:"Offline, Remote Config uses the last activated cached values (or in-app defaults if it never fetched successfully); fetches fail silently and you keep cached/default values. So ship sensible defaults and never block on a fetch. Reconnection triggers the next fetch (activated per your timing). Design the app to be fully functional on cached/default config."}]},{level:"junior",q:"What metrics do you track to evaluate a feature rollout via Remote Config?",a:[{t:"p",text:"Track *stability* (crash-free rate for flag-on vs flag-off cohorts — did enabling it introduce crashes?), the *target behavior metric* (did the feature improve the intended KPI — conversion, engagement, retention?), *performance* (startup/jank if relevant), and *guardrail metrics* (did anything else regress — e.g. did a new feature hurt an unrelated funnel?). Compare *on vs off* cohorts (Analytics segmented by the flag). Roll forward only if stability holds and the target metric improves without guardrail regressions."},{t:"list",items:["**Stability** — crash-free: flag-on vs flag-off cohorts.","**Target metric** — the intended KPI improvement.","**Guardrails** — nothing else regressed.","**Compare** — on vs off cohorts; roll forward if healthy."]},{t:"note",text:"Evaluate a Remote Config rollout with: stability (crash-free for flag-on vs flag-off cohorts), the target behavior metric (intended KPI — conversion/engagement/retention), performance if relevant, and guardrail metrics (nothing else regressed). Compare on-vs-off cohorts (Analytics segmented by flag). Roll forward only if stability holds and the target improves without guardrail regressions."}]}],wT={id:"remote-config-distribution",title:"Remote Config, A/B Testing & Distribution",tagline:"Remote Config feature flags & kill switches, A/B testing experiments, App Distribution vs Play tracks, and the Firebase ecosystem tradeoffs.",tags:["Firebase","Remote Config","Feature Flags","A/B Testing","App Distribution"],content:gT,qa:yT},vT=[{heading:"Firebase Authentication",blocks:[{t:"p",text:"**Firebase Authentication** provides ready-made user sign-in — it handles the complex, security-sensitive work of authenticating users so you don't build it yourself. It supports many *providers*: email/password, phone (SMS OTP), and federated sign-in (Google, Apple, Facebook, etc.), plus anonymous auth. You get a signed-in user with a stable UID and a token you use to authorize requests, without implementing password hashing, OAuth flows, or SMS verification yourself."},{t:"list",items:["**Providers** — email/password, phone (SMS), Google/Apple/Facebook/GitHub (OAuth), anonymous (a temporary account you can later 'upgrade' to a real one when the user signs up). You enable the providers you want and Firebase handles the flows.","**What you get** — a `FirebaseUser` with a unique UID (stable identity for that user), and an **ID token** (a JWT) you send to your backend or use to secure Firebase services; the backend/services verify it to authenticate the request.","**Session management** — Firebase persists the auth state (the user stays signed in across launches) and handles token refresh automatically. You observe auth state to react to sign-in/out.","**Security handled for you** — password storage, OAuth token exchange, SMS verification, and token signing/verification are all managed by Firebase, which is more secure than most teams rolling their own. This is a strong reason to use it — auth is easy to get *wrong* in ways that are catastrophic."]}]},{heading:"Firestore and Realtime Database",blocks:[{t:"p",text:"Firebase offers two cloud NoSQL databases with *real-time sync* and *offline support*: the older **Realtime Database** and the newer, more capable **Cloud Firestore**. Both let clients read/write data that *syncs in real time* across devices and *works offline* (caching locally, syncing when back online) — powerful for collaborative/real-time apps without you building a backend + sync layer."},{t:"table",headers:["","Realtime Database","Cloud Firestore"],rows:[["Data model","one big JSON tree","collections of documents (structured, nested collections)"],["Queries","limited (basic filtering/sorting)","richer (compound queries, better indexing)"],["Scaling","single-region, less scalable","multi-region, scales better"],["Pricing","by bandwidth + storage","by document reads/writes + storage"],["Recommendation","legacy / simple real-time cases","the modern default for most new apps"]]},{t:"list",items:["**Real-time sync** — you *listen* to data and get *live updates* when it changes anywhere (another user, another device) — ideal for chat, collaborative editing, live dashboards. No polling; changes push to listeners.","**Offline support** — the SDK caches data locally and lets the app read/write offline, syncing when connectivity returns (with conflict handling). Offline-capable by default.","**Firestore is the modern default** — a document/collection model (more structured than RTDB's single JSON tree), richer queries, better scaling and multi-region. Use Firestore for new apps unless you have a specific reason for RTDB (e.g. very simple real-time state, presence).","**Security Rules** — access control is enforced by server-side *Security Rules* (declarative rules on who can read/write what, based on auth and data). Since clients talk to the database directly, these rules are your security boundary — getting them right is critical (a common mistake is overly permissive rules exposing data)."]}]},{heading:"The tradeoff: BaaS vs a custom backend",blocks:[{t:"p",text:"Firestore/RTDB are **Backend-as-a-Service (BaaS)** — the client talks *directly* to a managed database (secured by Security Rules), with no server code in between (though Cloud Functions can add server logic). This is a different architecture from a traditional *custom backend* (your server + API in front of a database), with real tradeoffs."},{t:"list",items:["**BaaS pros** — extremely fast to build (no backend to write/operate), real-time sync and offline built in, scales without ops work. Great for MVPs, real-time apps, and small teams — you ship features instead of building infrastructure.","**BaaS cons** — *limited queries* (NoSQL, no complex joins/aggregations like SQL), *cost at scale* (Firestore bills per read/write — a query pattern cheap at small scale can get expensive), *business logic on the client or in Cloud Functions* (no traditional server layer — sensitive logic in the client is a concern, so it moves to Security Rules + Functions), and *vendor lock-in* (the data model couples you to Firestore).","**When to use BaaS** — real-time/collaborative features, MVPs, standard CRUD apps where speed matters and query needs are simple. **When to prefer a custom backend** — complex relational data and queries, heavy server-side business logic, specific compliance/data-residency needs, or cost/control requirements at scale.","**Hybrid is common** — many apps use Firebase for *some* things (auth, push, crashes, analytics, maybe a real-time feature) and a custom backend for their *core* data/logic. You don't have to go all-in."]}]},{heading:"Other Firebase services (brief)",blocks:[{t:"list",items:["**Cloud Functions** — serverless backend code (run on events like a Firestore write, an auth event, or an HTTP call) — for server-side logic without managing servers (validation, triggers, integrations, sending FCM messages).","**Cloud Storage** — file storage (images, videos, user uploads) with security rules, integrated with Auth.","**App Check** — protects your backend/Firebase resources by verifying requests come from *your genuine app* (not a bot/emulator/repackaged app) — an abuse/security layer.","**Dynamic Links** (being deprecated) / **Hosting**, **In-App Messaging**, **ML Kit** (on-device ML), and more — the suite is broad; you adopt what you need."]},{t:"note",text:"Firebase Auth: ready-made sign-in (email/password, phone, Google/Apple/etc., anonymous) — handles security-sensitive work (password storage, OAuth, SMS, tokens); gives a UID + ID token; manages sessions/refresh. Databases: Firestore (modern default — document/collection, richer queries, scales) and Realtime DB (older, JSON tree) — both real-time sync + offline, secured by server-side Security Rules (your security boundary). BaaS (client talks directly to DB) vs custom backend: BaaS = fast/real-time/offline but limited queries, cost at scale, lock-in — use for MVPs/real-time; hybrid is common. Also Cloud Functions (serverless), Storage, App Check."}]}],bT=[{level:"junior",q:"What is Firebase Authentication and what does it provide?",a:[{t:"p",text:"**Firebase Authentication provides ready-made user sign-in — it handles the complex, security-sensitive work of authenticating users so you don't build it yourself.** It supports many providers (email/password, phone via SMS, Google/Apple/Facebook OAuth, anonymous) and gives you a signed-in user with a stable identity and a token, without you implementing password hashing, OAuth flows, or SMS verification."},{t:"list",items:["**Providers** — you enable the sign-in methods you want (email/password, phone OTP, federated Google/Apple/etc., or anonymous accounts you can later upgrade), and Firebase handles each flow.","**What you get** — a `FirebaseUser` with a unique UID (stable identity) and an *ID token* (a JWT) that your backend or Firebase services verify to authenticate requests.","**Session management** — Firebase persists auth state (users stay signed in across launches) and refreshes tokens automatically; you observe auth state to react to sign-in/out.","**Security handled for you** — password storage, OAuth token exchange, SMS verification, and token signing/verification are all managed by Firebase."]},{t:"p",text:"The main reason to use it is that *authentication is easy to get catastrophically wrong* — insecure password storage, flawed OAuth, token vulnerabilities. Firebase Auth is built and maintained by Google's security teams, so it's far more secure than most teams rolling their own, and it saves enormous effort (OAuth flows and SMS verification alone are significant work). You get robust, multi-provider auth by configuring it rather than building it — which is why it's one of the commonly-adopted Firebase services even for apps that use a custom backend for everything else (they use Firebase Auth's token to authenticate to their own backend)."}]},{level:"junior",q:"What is the difference between Firestore and Realtime Database?",a:[{t:"p",text:"**Both are Firebase's cloud NoSQL databases with real-time sync and offline support, but Firestore is the newer, more capable one and the recommended default for new apps; Realtime Database is the older, simpler option.** The main differences are the data model, query capabilities, and scaling."},{t:"list",items:["**Data model** — Realtime Database stores everything as *one big JSON tree*; Firestore uses *collections of documents* (a more structured model with nested sub-collections), which is easier to organize and scale.","**Queries** — Firestore has *richer* querying (compound queries, better indexing); Realtime Database's queries are more *limited* (basic filtering/sorting).","**Scaling** — Firestore scales better and supports multi-region; Realtime Database is single-region and less scalable.","**Pricing** — Realtime DB bills by bandwidth + storage; Firestore bills by *document reads/writes* + storage (so query patterns affect cost differently)."]},{t:"p",text:"Both share the core Firebase database strengths: *real-time sync* (you listen to data and get live updates when it changes anywhere — no polling, great for chat/collaboration) and *offline support* (the SDK caches locally and syncs when back online). For new apps, **use Firestore** unless you have a specific reason for RTDB (like very simple real-time state or presence, where RTDB's simplicity or lower latency for tiny frequent updates can fit). Both are secured by server-side *Security Rules* — since clients talk to the database directly, those rules are your security boundary, and getting them right (not overly permissive) is critical."}]},{level:"junior",q:"What are Firebase Security Rules and why do they matter?",a:[{t:"p",text:"**Firebase Security Rules are server-side, declarative access-control rules that define who can read or write what data in Firestore/Realtime Database/Storage, based on the authenticated user and the data itself. They matter because clients talk to the database *directly* — so these rules are the *only* thing standing between a malicious client and your data. They're your security boundary.**"},{t:"list",items:["**Why they're essential**: in the Firebase/BaaS model, there's no server-side API layer enforcing access — the client SDK reads/writes the database directly. So access control *must* live in Security Rules, enforced by Firebase's servers. Without proper rules, any client could read or write any data.","**What they express**: rules like 'a user can only read/write documents where the document's `ownerId` matches their auth UID', or 'only authenticated users can read this collection', or validation ('a message must have a non-empty body and a valid timestamp'). They combine *auth* state and *data* conditions.","**The common mistake**: overly permissive rules (e.g. `allow read, write: if true` left from a prototype, or `if request.auth != null` when finer control is needed) that expose or allow tampering with data. Firebase even warns about test-mode rules that expire."]},{t:"p",text:"The key insight is that in a direct-client-to-database architecture, security *cannot* be in the client (which an attacker controls) — it must be in the server-enforced Security Rules. So writing correct, restrictive rules is not optional polish; it's the core of securing a Firebase app. This is a genuine tradeoff of the BaaS model versus a custom backend (where the server API enforces access): the convenience of no backend comes with the responsibility of getting Security Rules right, which requires care and testing (Firebase provides a rules simulator and emulator for this). A Firebase data breach is almost always a Security Rules failure."}]},{level:"senior",q:"What is Backend-as-a-Service (BaaS), and what are the tradeoffs of Firestore versus a custom backend?",a:[{t:"p",text:"**Backend-as-a-Service (BaaS) means the client talks *directly* to a managed database (Firestore/RTDB), secured by Security Rules, with *no server API layer* you write in between (though Cloud Functions can add server logic). This is architecturally different from a traditional custom backend (your server + API in front of a database), and the tradeoff is *speed and simplicity* versus *control, query power, and cost predictability*.**"},{t:"list",items:["**BaaS pros — speed and built-in capabilities**: you build extremely fast because there's *no backend to write or operate* — no API, no server infrastructure, no ops. Real-time sync and offline support come *built in* (huge for collaborative/real-time apps — building a reliable sync+offline layer yourself is a major undertaking). It scales without you managing servers. For MVPs, small teams, and real-time apps, this is enormous leverage — you ship features instead of building and running infrastructure.","**BaaS con — limited queries**: Firestore is NoSQL, so you *lose* SQL's power — no complex joins, no aggregations, restricted compound queries, and you often must *denormalize* data (duplicate it across documents) to support your read patterns. Data modeling is driven by *how you'll query* (design for reads), which is a different discipline than relational modeling. Apps with complex relational data or ad-hoc querying needs chafe against this.","**BaaS con — cost at scale**: Firestore bills *per document read/write*, so a design that's cheap at small scale can get expensive at large scale — e.g. a feed that reads many documents per user per session, or listeners that re-read data, can rack up costs. You must design *cost-consciously* (denormalization, pagination, careful listeners), and at very large scale a custom backend with a database you control may be cheaper.","**BaaS con — business logic placement**: with no server layer, business logic goes in the *client* (visible to and modifiable by attackers — bad for sensitive logic) or in *Cloud Functions* (serverless, but a different model than a traditional backend). Security-sensitive logic *must* be in Security Rules + Functions, not the client.","**BaaS con — vendor lock-in**: your data model and access patterns become coupled to Firestore; migrating off is costly."]},{t:"list",items:["**When to choose BaaS (Firestore)**: real-time/collaborative features (chat, shared docs, live updates), MVPs and prototypes, standard CRUD apps where speed matters and query needs are simple, and small teams without backend expertise. The built-in real-time+offline is often the deciding factor.","**When to prefer a custom backend**: complex relational data and rich/ad-hoc queries (where SQL shines), heavy server-side business logic, specific compliance/data-residency requirements, or predictable cost/control at large scale.","**Hybrid is very common and often the right answer**: use Firebase for what it's great at (Auth, push, crashes, analytics, and maybe a real-time feature or two) and a *custom backend* for the *core* data and business logic. You're not forced to go all-in — many production apps use Firebase Auth + a custom backend, or Firestore for one real-time feature alongside a traditional API for the rest."]},{t:"p",text:"**The senior framing**: BaaS inverts the traditional architecture — instead of client → your API → database, it's client → (Security Rules) → managed database. This trades away the *control point* of a server API (rich queries, business logic, cost control, flexibility) for *speed* (no backend to build/run) and *built-in real-time + offline sync* (genuinely hard to build yourself). The decision hinges on your *query complexity*, *scale/cost trajectory*, *business-logic needs*, and *how much real-time/offline matters* — Firestore wins decisively for real-time/collaborative MVPs with simple queries, while relational-heavy, logic-heavy, or cost-sensitive-at-scale apps favor a custom backend. The mature view rejects the all-or-nothing framing: it's a *per-concern* choice, and hybrids (Firebase for auth/push/observability/real-time, custom backend for core data) are common and sensible. Understanding the architectural inversion (no server layer → security in Rules, logic in client/Functions), the specific tradeoffs (query power, cost model, lock-in, real-time/offline benefit), and the differentiated/hybrid decision approach — rather than 'Firestore is great' or 'always build your own' — is the comprehensive senior answer, and it mirrors the general Firebase buy-vs-build tradeoff applied specifically to the data layer."}]},{level:"senior",q:"How would you design data in Firestore for a social feed, and what would you watch out for?",a:[{t:"p",text:"**I'd design the data model *around the read patterns* (denormalizing as needed), because Firestore is NoSQL with limited queries and per-read billing — so the goal is to make the feed cheap and fast to *read*, accepting duplication and write-time work as the cost. Key concerns: denormalization for read efficiency, the fan-out-on-write vs fan-out-on-read tradeoff, avoiding expensive queries, pagination, and Security Rules.**"},{t:"list",items:["**Design for reads, not normalization**: unlike SQL where you normalize and join at read time, Firestore has no joins and bills per document read — so you *denormalize*. For a post displayed in a feed, embed the data needed to render it (author name, avatar URL, post text, counts) *in the feed document itself*, so rendering the feed reads *one document per item* rather than reading the post *and* joining to the author *and* to counts. Duplication is the accepted cost of cheap reads.","**The fan-out decision — the core social-feed tradeoff**: how does a user's feed get populated? *Fan-out on write* — when someone posts, *write a copy of that post into each follower's feed collection* (so reading a feed is a simple, cheap query of one collection, but a post by a user with millions of followers triggers millions of writes). *Fan-out on read* — store posts once, and *build the feed by querying the posts of everyone the user follows at read time* (cheap writes, but expensive/complex reads, and Firestore's query limits make 'posts from these 1000 people, sorted by time' hard). Most large social feeds use *fan-out on write* (often via Cloud Functions triggered on a new post) because reads vastly outnumber writes and reads must be fast/cheap — accepting the write amplification and using strategies for celebrity accounts (hybrid: fan-out for normal users, fan-out-on-read for high-follower accounts).","**Avoid expensive query patterns**: Firestore can't do many things efficiently — no 'count all', limited compound queries, no full-text search (use a dedicated service like Algolia for search). Design so the feed is a *simple ordered query with pagination* (`orderBy(timestamp).limit(20)` with cursor-based paging via `startAfter`), not a complex query. Store aggregates (like counts) as *maintained fields* (updated on write, or via Functions/distributed counters) rather than computing them at read time.","**Watch listener costs**: real-time listeners re-read documents on changes and bill accordingly — a feed with live listeners on many documents can be costly. Use listeners judiciously (maybe on the visible window), and prefer paginated one-time reads with pull-to-refresh where real-time isn't essential.","**Security Rules**: since clients read/write directly, rules must enforce that users can only write their *own* posts, can only read feeds they're allowed to, can't tamper with counts/denormalized fields (those should be written by Cloud Functions, not clients — rules can restrict who writes them), etc. The denormalized/aggregate fields especially need protection so a client can't forge a like count.","**Offline and consistency**: Firestore's offline support means the feed works offline (cached), and writes queue — good UX. But denormalized data raises *consistency* concerns: if an author changes their name, all the copies of it embedded in feed items are stale until updated (a background job/Function must propagate the change, or you accept eventual/approximate consistency for such fields). This is the classic denormalization tradeoff — fast reads, but you own keeping copies in sync."]},{t:"p",text:"**The senior framing**: designing Firestore for a feed is fundamentally about *inverting the relational instinct* — instead of normalizing and joining at read time (SQL), you *denormalize and design around read patterns* because Firestore has no joins, limited queries, and per-read billing, so cheap fast reads are the priority and duplication + write-time work are the accepted costs. The defining architectural decision is *fan-out on write vs read* (write copies into follower feeds for cheap reads, vs query-at-read for cheap writes) — usually fan-out-on-write via Cloud Functions for read-heavy feeds, with special handling for high-follower accounts. Around that, you paginate with cursors, maintain aggregates as fields (protected by rules/Functions, not client-computed), delegate search to a dedicated service, use listeners cost-consciously, and accept eventual consistency for denormalized fields (with propagation jobs). The pitfalls to watch — write amplification, listener/read costs, denormalization staleness, query limitations, and Security-Rules protection of derived fields — are exactly where naive Firestore feed designs fail at scale. Demonstrating the design-for-reads/denormalize mindset, the fan-out tradeoff, and the cost/consistency/security pitfalls is the comprehensive answer that shows you understand NoSQL data modeling and Firestore's specific constraints, not just the API."}]},{level:"senior",q:"How does Firebase Auth work under the hood (tokens)?",a:[{t:"p",text:"On sign-in, Firebase Auth issues the client a short-lived *ID token* (a signed JWT, ~1h expiry) containing the user's uid and claims, plus a long-lived *refresh token*. The SDK *auto-refreshes* the ID token before expiry. The ID token is what proves identity: *Security Rules* verify it automatically, and your *own backend* verifies it with the *Admin SDK* (`verifyIdToken`) to authenticate API calls. So the client holds tokens, the SDK refreshes them, and servers *validate the signature/claims* — never trusting a raw uid from the client."},{t:"list",items:["**ID token** — signed JWT (~1h), uid + claims.","**Refresh token** — long-lived; SDK auto-refreshes.","**Rules** — verify the token automatically.","**Your backend** — Admin SDK `verifyIdToken`."]},{t:"note",text:"Firebase Auth issues a short-lived ID token (signed JWT, ~1h, uid + claims) + a long-lived refresh token; the SDK auto-refreshes. Security Rules verify the token automatically; your backend verifies it with the Admin SDK (verifyIdToken). Servers validate the signature/claims — never trust a raw uid from the client."}]},{level:"junior",q:"What sign-in methods does Firebase Auth support?",a:[{t:"p",text:"Email/password, phone (SMS OTP), and federated *identity providers*: Google, Apple, Facebook, Twitter/X, GitHub, plus generic OAuth/OIDC and SAML (enterprise). It also supports *anonymous* auth (a temporary account you can later *link* to a permanent method) and *custom auth* (mint a token from your own server via the Admin SDK for an existing auth system). *FirebaseUI* provides a drop-in flow. Choose methods by your audience; you can enable several and let users link them to one account."},{t:"list",items:["**Email/password, phone (OTP)** — basic methods.","**Federated** — Google, Apple, Facebook, GitHub, OAuth/OIDC/SAML.","**Anonymous** — temporary, linkable later.","**Custom auth** — your own server via Admin SDK."]},{t:"note",text:"Firebase Auth supports email/password, phone (SMS OTP), federated providers (Google, Apple, Facebook, GitHub, generic OAuth/OIDC, SAML), anonymous auth (temporary, linkable to a permanent method), and custom auth (mint a token from your server via the Admin SDK). FirebaseUI offers a drop-in flow. Enable several and let users link them to one account."}]},{level:"junior",q:"What is anonymous authentication, and when is it useful?",a:[{t:"p",text:"*Anonymous auth* creates a temporary account (a real uid) *without* the user signing up — so they can use the app and have data/state saved *before* committing to registration. Later you *link* the anonymous account to a permanent method (Google, email), *preserving their data* under the same uid. Useful for lowering the sign-up barrier (try before you register), cart/progress persistence, and per-device state. Caveat: anonymous accounts are lost on uninstall/clear-data if never linked — encourage linking to keep data."},{t:"list",items:["**Temporary account** — real uid, no sign-up.","**Save state early** — before registration.","**Link later** — to a permanent method, keep uid/data.","**Caveat** — lost on uninstall if never linked."]},{t:"note",text:"Anonymous auth creates a temporary account (real uid) without sign-up, so users have saved state before registering; later link it to a permanent method (Google/email) preserving data under the same uid. Useful to lower the sign-up barrier and persist cart/progress. Caveat: unlinked anonymous accounts are lost on uninstall/clear-data — encourage linking."}]},{level:"senior",q:"How do Firestore Security Rules actually work?",a:[{t:"p",text:"Security Rules are a *declarative* language evaluated *server-side* on every read/write, deciding *allow/deny* per operation. They access the *authenticated user* (`request.auth.uid`), the incoming data (`request.resource.data`), and existing data (`resource.data`), and can enforce ownership, validation, and role checks. They are *not filters* — a query only succeeds if the rules allow *every* document it could return (you must design queries to match rules). Rules are your *primary security boundary* for direct client access; test them with the emulator."},{t:"code",title:"Ownership rule",code:`match /users/{userId}/notes/{noteId} {
  allow read, write: if request.auth != null && request.auth.uid == userId;
}`},{t:"note",text:"Security Rules are declarative, evaluated server-side per read/write (allow/deny), using request.auth.uid, request.resource.data (incoming), and resource.data (existing) to enforce ownership/validation/roles. They're not filters — a query succeeds only if rules allow every doc it could return (design queries to match). They're the primary security boundary for direct client access; test with the emulator."}]},{level:"senior",q:"Why can't you rely on client-side checks for Firestore security?",a:[{t:"p",text:"Clients are *untrusted* — anyone can modify the app, call the Firestore SDK/REST API directly, and bypass your UI's checks. So *client-side validation is only UX*; the *real* enforcement must be in *Security Rules* (server-side), which no client can bypass. If your rules are permissive ('anyone authenticated can write anything'), a malicious user can read/alter others' data regardless of your app code. Design rules as the source of truth for *who can do what*, and treat client checks as convenience only."},{t:"list",items:["**Clients untrusted** — can call the API directly, bypass UI.","**Client checks** — UX only, not security.","**Rules** — the real, unbypassable enforcement.","**Permissive rules** — expose all data regardless of app code."]},{t:"note",text:"Clients are untrusted — anyone can modify the app or hit the Firestore SDK/REST directly, bypassing UI checks. Client-side validation is only UX; real enforcement is in server-side Security Rules that no client can bypass. Permissive rules expose everyone's data regardless of your app code. Rules are the source of truth for who-can-do-what."}]},{level:"senior",q:"How does Firestore's offline persistence work?",a:[{t:"p",text:"Firestore *caches data locally* and, with offline persistence (enabled by default on mobile), serves *reads from the cache* when offline and *queues writes* to sync when back online. Listeners fire immediately with cached data (and a `fromCache`/pending-writes flag), giving an *offline-first* experience. Writes are *optimistic* — reflected locally at once, then confirmed/merged with the server. Conflicts resolve *last-write-wins* per field on the server. This makes Firestore usable offline with automatic sync, but you must handle *eventual* consistency and pending-write UI states."},{t:"list",items:["**Local cache** — reads served offline.","**Queued writes** — sync when back online (optimistic).","**Listeners** — fire with cached data + pending/fromCache flags.","**Conflicts** — last-write-wins per field server-side."]},{t:"note",text:"Firestore caches locally (offline persistence on by default): reads served from cache offline, writes queued and synced on reconnect (optimistic — reflected locally at once). Listeners fire immediately with cached data + fromCache/pending flags. Server conflict resolution is last-write-wins per field. Offline-first with auto-sync, but handle eventual consistency and pending-write UI."}]},{level:"senior",q:"How is Firestore data modeling different from a relational database?",a:[{t:"p",text:"Firestore is a *NoSQL document store* — you model for *how you read* (denormalize, duplicate data to avoid joins), not for normalized relations. There are *no server-side joins*; you either *nest*/duplicate data or do multiple reads. Structure into *collections → documents → subcollections*. Because you *pay per read* and queries are shallow, you often *denormalize* (store a user's name on each of their posts) and accept update fan-out. Design around your *queries and access patterns* first, unlike relational's normalize-first approach."},{t:"list",items:["**Document store** — model for reads, not normalization.","**No joins** — nest/duplicate or multiple reads.","**Structure** — collections → documents → subcollections.","**Denormalize** — accept update fan-out; design for queries."]},{t:"note",text:"Firestore is a NoSQL document store — model for how you read (denormalize/duplicate to avoid joins) not normalized relations. No server-side joins (nest/duplicate or multiple reads); structure as collections → documents → subcollections. You pay per read, so denormalize (user name on each post) accepting update fan-out. Design around queries/access patterns first."}]},{level:"senior",q:"What are the query limitations of Firestore?",a:[{t:"p",text:"Firestore queries are *shallow* and constrained: no joins, no full-text search (use Algolia/Typesense), limited *`OR`* (via `in`/`array-contains-any`, capped sizes), only *one range/inequality field* per query (composite constraints), and compound queries need *composite indexes* (auto-suggested). Queries return whole documents (no partial-field selection). These limits push you to *denormalize* and shape data to your queries. Know them upfront — a data model that fights these limits leads to expensive workarounds."},{t:"list",items:["**No joins / full-text** — external search for text.","**One inequality field** — per query.","**Composite indexes** — needed for compound queries.","**Whole documents** — no partial-field reads."]},{t:"note",text:"Firestore query limits: no joins, no full-text search (use Algolia/Typesense), limited OR (in/array-contains-any, capped), only one range/inequality field per query, compound queries need composite indexes, and reads return whole documents. These push you to denormalize and shape data to queries. Know them upfront — fighting them leads to expensive workarounds."}]},{level:"junior",q:"How does Firestore pricing work, and how do you optimize it?",a:[{t:"p",text:"Firestore bills primarily by *document reads, writes, and deletes* (plus storage and network) — *not* by query complexity, and a query reading 100 docs costs *100 reads*. Optimize by: *paginating* (don't read whole collections), *caching*/using offline persistence (cache reads don't bill), *denormalizing* to avoid extra reads, *listening* efficiently (listeners bill for changed docs, not re-reads), and *aggregating* (count() aggregation vs reading all docs). Watch expensive listeners on large collections. Cost scales with reads, so design read-efficient access patterns."},{t:"list",items:["**Bills** — reads/writes/deletes (not query complexity).","**100-doc query** — 100 reads.","**Optimize** — paginate, cache, denormalize, aggregate.","**Watch** — listeners on large collections."]},{t:"note",text:"Firestore bills by document reads/writes/deletes (+ storage/network), not query complexity — a 100-doc query = 100 reads. Optimize: paginate (don't read whole collections), use cache/offline persistence (cache reads don't bill), denormalize to cut reads, use count() aggregation, and efficient listeners. Watch listeners on big collections. Cost scales with reads — design read-efficient."}]},{level:"senior",q:"What are Firestore transactions and batched writes?",a:[{t:"p",text:"A *batched write* groups multiple writes into one *atomic* commit (all succeed or none) — for updating several documents together without reading first. A *transaction* *reads then writes atomically* with *optimistic concurrency*: it reads, computes, and writes, and if the read data changed before commit, it *retries* — for operations that depend on current values (incrementing a counter, transferring balance). Use batches for independent multi-writes, transactions when a write depends on a consistent read. Both are atomic and server-enforced."},{t:"list",items:["**Batched write** — atomic multi-write, no read.","**Transaction** — atomic read-then-write, retries on conflict.","**Transaction** — when a write depends on current values.","**Both** — atomic, server-enforced."]},{t:"note",text:"Batched write: groups multiple writes into one atomic commit (all-or-none), no read needed. Transaction: atomic read-then-write with optimistic concurrency — retries if read data changed (for counters/balance transfers depending on current values). Use batches for independent multi-writes, transactions when a write depends on a consistent read. Both atomic and server-enforced."}]},{level:"junior",q:"What is the difference between get() and a realtime listener (snapshot)?",a:[{t:"p",text:"`get()` is a *one-time* read — fetch the current data once. A *snapshot listener* (`addSnapshotListener`/`snapshots()`) *subscribes* and *re-fires whenever the data changes*, giving realtime updates (and initial data). Listeners power live UIs (a chat that updates instantly) but keep a *connection* and bill for changed docs — *detach* them when not needed (lifecycle) to avoid leaks/cost. Use `get()` for data you read once (a settings load); use listeners for data that should stay live on screen."},{t:"list",items:["**`get()`** — one-time read.","**Snapshot listener** — subscribes, re-fires on changes.","**Listeners** — live UI; detach on lifecycle.","**Choose** — get() for one-shot, listener for live data."]},{t:"note",text:"get() is a one-time read; a snapshot listener subscribes and re-fires on every change (realtime updates + initial data). Listeners power live UIs but hold a connection and bill for changed docs — detach on lifecycle to avoid leaks/cost. Use get() for one-shot reads (settings), listeners for data that should stay live on screen (chat/feed)."}]},{level:"senior",q:"How do you structure Firestore data for a chat app?",a:[{t:"p",text:"A common model: a `chats` (or `conversations`) collection where each document holds metadata (participants, last message preview, timestamps), and a *subcollection* `messages` per chat (ordered by timestamp) for the message documents. Store a *denormalized* `lastMessage` on the chat doc for the list view (one read per chat, no fan-out to messages). Use *listeners* on the active chat's messages for realtime, and *pagination* for history. Security Rules restrict each chat to its participants. Index by timestamp for ordered queries."},{t:"list",items:["**`chats` doc** — participants, lastMessage preview, timestamps.","**`messages` subcollection** — per chat, ordered by time.","**Denormalize** — lastMessage for the list view.","**Listener + pagination** — active chat live, history paged."]},{t:"note",text:"Chat model: a chats/conversations collection (participants, denormalized lastMessage preview, timestamps) + a messages subcollection per chat ordered by timestamp. Denormalized lastMessage powers the list (one read/chat). Listen on the active chat's messages for realtime; paginate history. Rules restrict a chat to its participants; index by timestamp."}]},{level:"senior",q:"What is the difference between Firestore and Realtime Database in practice?",a:[{t:"p",text:"*Realtime Database (RTDB)* is one big *JSON tree* — very low-latency, simple, great for presence and high-frequency small updates, but limited querying (shallow, no compound queries) and harder to scale/structure. *Firestore* is a *document/collection* model with richer *queries*, *composite indexes*, *better scaling*, *stronger security rules*, and multi-region — the default for most new apps. Choose RTDB for simple, ultra-low-latency sync (presence, live cursors); Firestore for structured data, complex queries, and scale. You can use both for different jobs."},{t:"table",headers:["","Realtime DB","Firestore"],rows:[["Model","One JSON tree","Documents/collections"],["Queries","Shallow/limited","Rich, indexed"],["Best for","Presence, low-latency sync","Structured data, scale"]]},{t:"note",text:"RTDB: one JSON tree — ultra-low-latency, simple, great for presence/high-frequency small updates, but limited queries and harder to scale. Firestore: documents/collections with richer queries, composite indexes, better scaling, stronger rules, multi-region — default for new apps. RTDB for simple low-latency sync (presence/cursors); Firestore for structured data + complex queries + scale. Use both if needed."}]},{level:"senior",q:"How do you handle role-based access with custom claims?",a:[{t:"p",text:"*Custom claims* are key-value attributes (e.g. `admin: true`, `role: 'editor'`) you set on a user's token *via the Admin SDK* (server-side only — never from the client). They're embedded in the *ID token*, so *Security Rules* (`request.auth.token.admin == true`) and your backend can *authorize by role* without a database lookup. Use them for coarse roles/permissions. Caveat: claims update on *token refresh* (~1h or force-refresh), so role changes aren't instant. Keep claims small; store fine-grained permissions in the database."},{t:"list",items:["**Custom claims** — role attributes set via Admin SDK (server).","**In the ID token** — Rules/backend authorize by role.","**No DB lookup** — for coarse roles.","**Caveat** — update on token refresh (~1h), not instant."]},{t:"note",text:"Custom claims are role attributes (admin:true) set server-side via the Admin SDK (never client), embedded in the ID token so Security Rules (request.auth.token.admin) and your backend authorize by role without a DB lookup. Use for coarse roles. Caveat: claims refresh with the token (~1h/force-refresh) — role changes aren't instant. Keep claims small; fine-grained perms in the DB."}]},{level:"junior",q:"How do you observe authentication state in the app?",a:[{t:"p",text:"Use an *auth state listener* (`FirebaseAuth.addAuthStateListener` / `auth.authStateChanged` as a Flow) that fires whenever the user *signs in or out* (and on startup with the restored session). Drive your navigation/UI from it — show the app when a user is present, the login screen when null — rather than checking `currentUser` once. This keeps the UI in sync with token refresh/sign-out and handles the *restored session* on launch (Firebase persists the session). It's the reactive source of truth for 'is someone logged in?'."},{t:"code",title:"Auth state as a Flow",code:`val authState = callbackFlow {
  val l = FirebaseAuth.AuthStateListener { trySend(it.currentUser) }
  Firebase.auth.addAuthStateListener(l)
  awaitClose { Firebase.auth.removeAuthStateListener(l) }
}`},{t:"note",text:"Observe auth via an auth state listener (addAuthStateListener / a callbackFlow) that fires on sign-in/out and startup (restored session). Drive navigation/UI from it (app vs login) instead of checking currentUser once — keeping UI in sync with refresh/sign-out and handling the persisted session on launch. The reactive source of truth for 'is someone logged in?'."}]},{level:"senior",q:"What are common Firestore Security Rules mistakes?",a:[{t:"p",text:"Common pitfalls: leaving rules in *test mode* (`allow read, write: if true`) in production (wide-open data), assuming rules *filter* queries (they don't — a query fails unless all matched docs are allowed), not *validating incoming data* (`request.resource.data`) so clients can write bad/oversized fields, forgetting *field-level* checks, not restricting *who can set sensitive fields* (e.g. a `role` field), and not *testing* rules. Also, overly broad reads leak data. Always deny by default, validate writes, scope by `uid`, and test with the emulator."},{t:"list",items:["**Test mode** — `if true` left in production.","**Rules aren't filters** — query fails if any doc disallowed.","**No validation** — clients write bad/sensitive fields.","**Fix** — deny by default, validate, scope by uid, test."]},{t:"note",text:"Common Rules mistakes: leaving test mode (allow if true) in prod, assuming rules filter queries (they don't — the query fails unless all matched docs are allowed), not validating request.resource.data (clients write bad/sensitive fields like role), missing field-level checks, and not testing. Deny by default, validate writes, scope by uid, and test with the emulator."}]},{level:"senior",q:"How do you paginate Firestore queries efficiently?",a:[{t:"p",text:"Use *cursor-based* pagination with `orderBy` + `startAfter(lastDocumentSnapshot)` + `limit(n)` — fetch a page, then start the next page *after the last doc* of the previous one. This is efficient (reads only the page) and stable. Avoid *offset*-style pagination (Firestore has no true offset; skipping still reads/bills skipped docs). Pass the *DocumentSnapshot* (not just a field value) as the cursor for correctness with ties. For infinite scroll, keep the last snapshot and load more on scroll. Combine with an index on the ordered field."},{t:"code",title:"Cursor pagination",code:`val first = db.collection("posts").orderBy("createdAt").limit(20)
val snap = first.get().await()
val last = snap.documents.last()
val next = db.collection("posts").orderBy("createdAt").startAfter(last).limit(20)`},{t:"note",text:"Paginate with cursors: orderBy + startAfter(lastSnapshot) + limit(n) — fetch a page, start the next after the previous page's last doc (reads only the page, stable). Avoid offset-style (no true offset; skipped docs still bill). Pass the DocumentSnapshot as the cursor for tie correctness. Keep the last snapshot for infinite scroll; index the ordered field."}]},{level:"junior",q:"What is Firebase Cloud Functions, and how does it complement client apps?",a:[{t:"p",text:"*Cloud Functions* run *your backend code* (server-side) in response to events — Firestore/RTDB *triggers* (on document write), Auth events (on user create), HTTPS *callable* functions (invoked from the app), or scheduled jobs. They let you do things clients *shouldn't*: privileged writes, sending FCM, calling third-party APIs with secret keys, data validation/aggregation, and fan-out (denormalization). They complement a BaaS app by providing *trusted server logic* without managing servers. Use them for anything needing secrets or that must be authoritative."},{t:"list",items:["**Server code** — on events (Firestore/Auth triggers, HTTPS, scheduled).","**Privileged work** — secret keys, FCM sends, aggregation.","**Trusted logic** — what clients shouldn't do.","**Serverless** — no server management."]},{t:"note",text:"Cloud Functions run your backend code on events — Firestore/RTDB triggers, Auth events, HTTPS callable functions, scheduled jobs. They do what clients shouldn't: privileged writes, sending FCM, third-party APIs with secret keys, validation/aggregation, denormalization fan-out. They give a BaaS app trusted, authoritative server logic without managing servers. Use for anything needing secrets or authority."}]},{level:"senior",q:"How do you keep denormalized data consistent in Firestore?",a:[{t:"p",text:"Denormalization duplicates data (a user's name on each post), so an update must *fan out* to all copies. Options: do the fan-out in a *Cloud Function* triggered on the source change (server-side, reliable, off the client), use *batched writes/transactions* for atomic multi-doc updates, or accept *eventual consistency* where staleness is tolerable. Decide *what to denormalize* by read frequency vs update cost. For large fan-outs, background the work. The trade-off is read speed/cost vs write complexity — choose per access pattern."},{t:"list",items:["**Fan-out** — update all duplicated copies.","**Cloud Function** — reliable server-side fan-out on change.","**Batches/transactions** — atomic multi-doc updates.","**Trade-off** — read speed vs write complexity."]},{t:"note",text:"Denormalization duplicates data, so updates must fan out to all copies. Options: a Cloud Function triggered on the source change (reliable, server-side), batched writes/transactions for atomic multi-doc updates, or accept eventual consistency where tolerable. Denormalize by read frequency vs update cost; background large fan-outs. Trade-off: read speed/cost vs write complexity — choose per access pattern."}]},{level:"junior",q:"How do you securely store and never trust the user's uid on the client?",a:[{t:"p",text:"The client's `auth.currentUser.uid` is fine to *read* for the app's own logic, but your *backend/Rules must derive identity from the verified token* (`request.auth.uid` / Admin SDK `verifyIdToken`), *never* from a uid the client *sends in a request body* (which can be forged). So: never write 'ownerId' based on a client-supplied value without checking it against the authenticated token; enforce in Rules that `request.resource.data.ownerId == request.auth.uid`. Trust the *token*, not client-provided identifiers."},{t:"list",items:["**Client uid** — fine for app logic, not for authorization.","**Server/Rules** — derive identity from the verified token.","**Never trust** — a uid sent in the request body.","**Enforce** — ownerId == request.auth.uid in Rules."]},{t:"note",text:"The client's uid is fine for app logic, but authorization must derive identity from the verified token (request.auth.uid / Admin SDK verifyIdToken) — never from a uid the client sends in a body (forgeable). Enforce in Rules that request.resource.data.ownerId == request.auth.uid. Trust the token, not client-provided identifiers."}]},{level:"senior",q:"What is the Firebase Local Emulator Suite, and why use it?",a:[{t:"p",text:"The *Emulator Suite* runs Firebase services (Auth, Firestore, Functions, RTDB, Storage) *locally* — so you develop and *test against local instances* without touching production, incurring cost, or needing network. Crucially, it lets you *unit-test Security Rules* (with the rules-unit-testing library), test Cloud Functions and triggers, and seed/reset data deterministically in CI. Use it to make Firebase-dependent code *testable and safe*: fast, offline, no prod data risk, and reproducible. It's the standard way to test rules and backend logic."},{t:"list",items:["**Local Firebase** — Auth, Firestore, Functions, Storage.","**No prod/cost/network** — safe local dev.","**Test Rules + Functions** — deterministically, in CI.","**Reproducible** — seed/reset data."]},{t:"note",text:"The Local Emulator Suite runs Firebase services (Auth, Firestore, Functions, RTDB, Storage) locally — develop/test without touching prod, cost, or network. It enables unit-testing Security Rules (rules-unit-testing), testing Functions/triggers, and deterministic seed/reset in CI. The standard way to make Firebase-dependent code testable and safe: fast, offline, no prod-data risk, reproducible."}]},{level:"senior",q:"How do you architect a Firebase app to keep it testable and decoupled?",a:[{t:"p",text:"Don't scatter `Firebase.firestore`/`Firebase.auth` calls through the UI/domain. Wrap Firebase behind *repository interfaces* (e.g. `UserRepository`, `AuthRepository`) with Firebase-backed implementations, mapping Firestore documents to *domain models* at the boundary. The rest of the app depends on the interfaces (testable with fakes, swappable). Expose data as *Flows*. This isolates the SDK, keeps domain/UI Firebase-agnostic, eases testing (fake repos + emulator for integration), and would let you swap backends. Standard clean-architecture treatment of a BaaS SDK."},{t:"list",items:["**Wrap** — Firebase behind repository interfaces.","**Map** — documents → domain models at the boundary.","**App depends on interfaces** — fakes for tests.","**Benefits** — isolation, testability, swappable backend."]},{t:"note",text:"Keep Firebase testable/decoupled: wrap it behind repository interfaces (UserRepository, AuthRepository) with Firebase-backed impls, map documents → domain models at the boundary, and have domain/UI depend on the interfaces (fakes for tests, emulator for integration), exposing Flows. Isolates the SDK, keeps the app Firebase-agnostic, swappable. Standard clean-architecture treatment of a BaaS SDK."}]},{level:"senior",q:"How do you handle account linking and multiple providers for one user?",a:[{t:"p",text:"Firebase Auth can associate *multiple sign-in providers with one account* (same uid) via *linking* (`linkWithCredential`) — e.g. a user who signed up with email later links Google, and either method logs into the same account/data. Handle the `collision` case: if the provider's email already belongs to another account, Firebase throws `account-exists-with-different-credential` — you *fetch sign-in methods for the email* and prompt the user to sign in with the existing method, then link. Designing for linking avoids duplicate accounts and preserves user data across methods."},{t:"list",items:["**Link** — multiple providers → one uid (`linkWithCredential`).","**Same account/data** — via any linked method.","**Collision** — `account-exists-with-different-credential`: sign in existing, then link.","**Avoids** — duplicate accounts; preserves data."]},{t:"note",text:"Firebase Auth links multiple providers to one account/uid (linkWithCredential) — email user later links Google, both log into the same data. Handle account-exists-with-different-credential: fetch the email's sign-in methods, have the user sign in with the existing one, then link. Designing for linking avoids duplicate accounts and preserves data across methods."}]},{level:"senior",q:"How do you migrate from Firebase to (or integrate with) your own backend?",a:[{t:"p",text:"Because you should already have Firebase *wrapped behind repositories*, swapping the data source is localized. For *auth*, Firebase can *mint custom tokens* from your backend (or your backend can *verify Firebase ID tokens* with the Admin SDK), letting the two coexist during migration. For *data*, run a *dual-write / backfill* period (write to both, migrate reads gradually), keep the domain models stable, and cut over per feature. Plan for *export* (Firestore export/BigQuery) and *user migration* (custom auth). Incremental, repository-isolated migration avoids a risky big-bang."},{t:"list",items:["**Repository-wrapped** — swap is localized.","**Auth coexist** — custom tokens / verify ID tokens (Admin SDK).","**Data** — dual-write/backfill, migrate reads gradually.","**Incremental** — per feature; export via Firestore/BigQuery."]},{t:"note",text:"With Firebase wrapped behind repositories, swapping is localized. Auth: coexist via custom tokens or verifying Firebase ID tokens (Admin SDK). Data: dual-write/backfill, migrate reads gradually, keep domain models stable, cut over per feature. Plan export (Firestore export/BigQuery) and user migration (custom auth). Incremental, repository-isolated migration beats a risky big-bang."}]},{level:"junior",q:"What is Firebase Storage, and how does it relate to auth and rules?",a:[{t:"p",text:"*Cloud Storage for Firebase* stores *files* (images, videos, uploads) with resumable up/downloads from the client. Like Firestore, access is governed by *Storage Security Rules* using the *authenticated user* — you scope paths per user (`/users/{uid}/...`) and validate *content type/size*. Common pattern: upload a file to Storage, then store its *download URL/path* in Firestore alongside metadata. Never make buckets world-writable; enforce ownership and size/type limits in rules. It complements the databases for binary data (which you shouldn't put in documents)."},{t:"list",items:["**Storage** — files (images/video) with resumable transfer.","**Storage Rules** — auth-based, per-path, validate type/size.","**Pattern** — upload file, store URL/path in Firestore.","**Never** — world-writable buckets."]},{t:"note",text:"Cloud Storage for Firebase stores files (images/video) with resumable transfers, governed by Storage Security Rules using the authenticated user — scope paths per uid, validate content type/size. Common pattern: upload to Storage, store the download URL/path + metadata in Firestore. Never make buckets world-writable; enforce ownership and limits in rules. Complements the databases for binary data."}]}],kT={id:"auth-databases",title:"Auth & Databases",tagline:"Firebase Auth (providers, tokens, sessions), Firestore vs Realtime Database, Security Rules, and the BaaS vs custom-backend tradeoff.",tags:["Firebase","Auth","Firestore","Security Rules","BaaS"],content:vT,qa:bT},xT={id:"firebase",name:"Firebase",description:"Google's app platform — Cloud Messaging (FCM), Crashlytics & Analytics, Remote Config/A-B Testing/App Distribution, and Auth & Databases.",topics:[uT,fT,wT,kT]},ST=[{heading:"What Kotlin Multiplatform is",blocks:[{t:"p",text:"**Kotlin Multiplatform (KMP)** lets you write code *once* in Kotlin and run it on *multiple platforms* — Android, iOS, desktop, web, and server. The key idea is **sharing *logic*, not necessarily UI**: you write your business logic, data layer, networking, and domain models once in shared Kotlin code, and each platform keeps its own native UI (or uses Compose Multiplatform for shared UI too). This is fundamentally different from cross-platform frameworks like Flutter or React Native, which typically abstract the UI too — KMP shares the *logic* and lets each platform be natively itself."},{t:"list",items:["**Share logic, keep native**: the appeal is writing the *hard, business-critical logic* once (so it can't diverge between platforms) while each app stays natively performant and platform-idiomatic. An iOS app using KMP is still a real iOS app (Swift UI, native feel), just with a shared Kotlin brain.","**How it compiles**: Kotlin compiles to different targets — JVM bytecode (Android), native binaries via **Kotlin/Native** (iOS, using LLVM), JavaScript/Wasm (web). The *same* Kotlin source produces platform-appropriate output.","**Not 'write once, run anywhere' for UI** — it's 'write the logic once, build native UIs' (unless you opt into Compose Multiplatform). This distinction is the most important thing to understand about KMP's philosophy.","**Maturity** — KMP reached *Stable* (the core, in 2023); it's used in production by major companies (Netflix, McDonald's, Cash App, etc.). It's a mature, Google- and JetBrains-backed approach for an Android/KMP engineer."]}]},{heading:"Source sets: commonMain and platform-specific",blocks:[{t:"p",text:"A KMP module organizes code into **source sets**. **`commonMain`** holds the *shared* code that compiles for *all* targets — it can only use Kotlin's multiplatform standard library and multiplatform libraries (no platform-specific APIs). Each platform has its own source set (`androidMain`, `iosMain`, `desktopMain`, `jsMain`) for platform-specific code that can use that platform's APIs."},{t:"code",title:"The source set structure",code:`src/
  commonMain/     // shared — pure Kotlin, multiplatform libs only (no android.*, no Foundation)
    kotlin/
      Repository.kt
      DomainModels.kt
  androidMain/    // Android-specific — can use android.* APIs
    kotlin/
      AndroidDatabaseDriver.kt
  iosMain/        // iOS-specific — can use Foundation/UIKit via cinterop
    kotlin/
      IosDatabaseDriver.kt
  commonTest/     // shared tests
  androidUnitTest/, iosTest/  // platform tests`},{t:"list",items:["**`commonMain`** — the heart of KMP: your shared logic (repositories, use cases, models, networking, ViewModels). It can't reference `android.*`, iOS `Foundation`, or any single platform's API — only *multiplatform* code, which is enforced by the compiler.","**Platform source sets** (`androidMain`, `iosMain`, …) — for code that *needs* platform APIs. When shared code needs something platform-specific (a database driver, secure storage, the current time formatted per locale), the platform source set provides it.","**Hierarchy / intermediate source sets** — you can have intermediate sets shared by *some* targets (e.g. an `appleMain` shared by iOS/macOS), so code common to a subset of platforms isn't duplicated.","**The rule** — put as much as possible in `commonMain` (that's the sharing benefit); drop to platform source sets only for genuinely platform-specific needs, connected via `expect/actual`."]}]},{heading:"expect/actual — the platform bridge",blocks:[{t:"p",text:"When shared `commonMain` code needs something that's *implemented differently per platform* — the current platform name, a database driver, secure storage, a UUID generator — you use the **`expect`/`actual`** mechanism. In `commonMain` you declare an **`expect`** (a promise: 'this exists, each platform provides it'), and each platform source set provides the **`actual`** implementation. The compiler enforces that every target has an `actual` for every `expect`."},{t:"code",title:"expect/actual for platform-specific behavior",code:`// commonMain — declare the expectation
expect class Platform() {
    val name: String
}
expect fun getSecureStorage(): SecureStorage

// androidMain — provide the Android implementation
actual class Platform actual constructor() {
    actual val name: String = "Android \${Build.VERSION.SDK_INT}"
}
actual fun getSecureStorage(): SecureStorage = AndroidKeystoreStorage()

// iosMain — provide the iOS implementation
actual class Platform actual constructor() {
    actual val name: String = UIDevice.currentDevice.systemName
}
actual fun getSecureStorage(): SecureStorage = IosKeychainStorage()`},{t:"list",items:["**`expect`** (in commonMain) — declares *what* is needed without *how*. Shared code uses it as a normal type/function. Can be a class, function, property, or object.","**`actual`** (in each platform source set) — provides the *how* for that platform, using its native APIs. Every platform *must* supply an actual (compile-enforced), so there's no missing implementation.","**When to use it** — for the *thin* platform-specific layer beneath shared logic: database drivers (Room/SQLDelight need a platform driver), secure storage (Keystore vs Keychain), platform info, date/locale formatting, random/UUID. The shared logic calls the `expect` and each platform fills in the `actual`.","**The alternative — interfaces + DI**: instead of `expect/actual`, you can define an *interface* in commonMain and *inject* a platform implementation (constructed in platform code). Often preferred for testability (you can inject a fake) and flexibility — `expect/actual` is more rigid (exactly one implementation per platform, harder to fake). Many teams use interfaces+DI for most cases and `expect/actual` for simple platform primitives."]}]},{heading:"Targets and project structure",blocks:[{t:"list",items:["**Targets** — you declare which platforms your module builds for in the Gradle config: `androidTarget()`, `iosX64()`/`iosArm64()`/`iosSimulatorArm64()` (the iOS variants), `jvm()` (desktop/server), `js()`/`wasmJs()` (web). Each target gets its source set and produces its platform artifact.","**Typical project shape** — a `shared` module (KMP, with commonMain + platform source sets) consumed by an `androidApp` (a normal Android app depending on `shared` as a Gradle dependency) and an `iosApp` (an Xcode project consuming the shared code as a *framework*).","**The iOS framework** — Kotlin/Native compiles the shared module into an Obj-C-compatible *framework* (or XCFramework) that the iOS app imports and calls like any Swift/Obj-C library. This is how Swift code uses your shared Kotlin (covered in the iOS interop topic).","**Gradle-based** — KMP is configured via the Kotlin Multiplatform Gradle plugin; you declare targets, source set dependencies, and shared libraries in `build.gradle.kts`. Dependencies can be common (available to all — Ktor, kotlinx.serialization, coroutines) or platform-specific."]},{t:"note",text:"KMP: write logic once in Kotlin, run on Android/iOS/desktop/web — shares LOGIC (not necessarily UI; each platform keeps native UI unless using Compose Multiplatform). Compiles via JVM bytecode (Android), Kotlin/Native (iOS, LLVM), JS/Wasm (web). Source sets: commonMain (shared, multiplatform-only, no platform APIs) + androidMain/iosMain/etc (platform-specific). expect/actual bridges platform differences (expect in common declares what, actual per platform provides how — for db drivers, secure storage, platform info); interfaces+DI is a testable alternative. Targets declared in Gradle; shared module → framework for iOS."}]}],AT=[{level:"junior",q:"What is Kotlin Multiplatform and how is it different from Flutter or React Native?",a:[{t:"p",text:"**Kotlin Multiplatform (KMP) lets you write code once in Kotlin and run it on multiple platforms — Android, iOS, desktop, web. The defining characteristic is that it shares *logic*, not necessarily UI: you write business logic, data, networking, and domain models once in shared Kotlin, while each platform keeps its own *native* UI.** This is fundamentally different from Flutter and React Native, which typically abstract the UI too."},{t:"list",items:["**KMP — shares logic, keeps native UI**: your shared Kotlin code is the 'brain' (repositories, use cases, models), and the iOS app is a *real* iOS app with SwiftUI (or the Android app with Compose). Each app is natively performant and platform-idiomatic; only the logic is shared.","**Flutter — shares everything including UI**: Flutter draws its *own* UI with its own rendering engine (not native components), using one Dart codebase for both logic and UI. The apps look/behave identically but aren't using native UI toolkits.","**React Native — shares logic + a UI abstraction**: JavaScript logic plus a bridge to native components, with a shared UI layer."]},{t:"p",text:"So the philosophy differs: Flutter/RN aim for 'write once, run anywhere' *including UI* (maximizing code sharing, accepting a non-native UI layer), while KMP aims for 'write the *logic* once, build *native* UIs' (sharing the risky/complex business logic while keeping each UI natively excellent). KMP's bet is that the *logic* is where duplication hurts most (and where bugs diverge between platforms), while the UI benefits from being native. It compiles via JVM bytecode for Android, Kotlin/Native (LLVM) for iOS, and JS/Wasm for web. For an Android engineer, KMP is especially appealing because it's *Kotlin* — you already know the language, and you can incrementally share logic without rewriting UIs. It reached Stable in 2023 and is used in production by major companies."}]},{level:"junior",q:"What is commonMain and what can you put in it?",a:[{t:"p",text:"**`commonMain` is the shared source set in a KMP module — the code here compiles for *all* target platforms, so it's where your shared logic lives (repositories, use cases, domain models, networking, ViewModels). The constraint is that it can only use *multiplatform* code: Kotlin's common standard library and multiplatform libraries — no platform-specific APIs.**"},{t:"list",items:["**What goes in it**: business logic, domain models, repository implementations, networking (via Ktor), serialization (kotlinx.serialization), coroutines/Flow-based logic, ViewModels (with the multiplatform lifecycle library) — the platform-agnostic core of your app.","**What you *cannot* use**: `android.*` APIs, iOS `Foundation`/`UIKit`, or anything specific to one platform. The compiler enforces this — `commonMain` only sees multiplatform declarations. So you can't call `Context`, `Log`, `UIDevice`, etc. directly here.","**When shared code needs a platform API**: you bridge to it via `expect/actual` (declare the need in commonMain, implement per platform) or an interface injected from platform code."]},{t:"p",text:"`commonMain` is the heart of KMP — the more you can put here, the more you share. The discipline is to keep it *platform-agnostic*: write logic against multiplatform libraries and abstractions, and push anything platform-specific down to the platform source sets (`androidMain`, `iosMain`) via `expect/actual` or DI. This is also why KMP rewards clean architecture — a framework-free domain/data layer (no Android imports) drops straight into `commonMain` unchanged, while logic tangled with `android.*` can't be shared. The compiler enforcing 'commonMain sees only multiplatform code' is what guarantees the shared code genuinely runs everywhere."}]},{level:"junior",q:"What is expect/actual in KMP?",a:[{t:"p",text:"**`expect`/`actual` is KMP's mechanism for handling things that must be *implemented differently on each platform*. In `commonMain` you declare an `expect` (a promise that something exists — 'each platform provides this'), and each platform's source set provides the `actual` implementation using that platform's native APIs. The compiler requires every platform to supply an `actual` for every `expect`.**"},{t:"code",title:"Platform-specific implementation via expect/actual",code:`// commonMain
expect fun platformName(): String

// androidMain
actual fun platformName(): String = "Android \${Build.VERSION.SDK_INT}"

// iosMain
actual fun platformName(): String = UIDevice.currentDevice.systemName`},{t:"list",items:["**`expect`** (commonMain) — declares *what* the shared code needs, without the *how*. Shared code uses it like a normal function/class/property. The shared logic stays platform-agnostic while relying on it.","**`actual`** (each platform source set) — provides the *how* for that platform, free to use native APIs (`Build.VERSION` on Android, `UIDevice` on iOS). Every target must provide one, so there's never a missing implementation.","**Common uses**: database drivers (SQLDelight/Room need a platform driver), secure storage (Android Keystore vs iOS Keychain), platform/device info, date/locale formatting, UUID/random generation — the thin platform-specific layer beneath shared logic."]},{t:"p",text:"It's the bridge that lets shared code depend on platform-specific behavior without knowing the platform. An alternative many teams prefer for most cases is defining an *interface* in commonMain and *injecting* a platform implementation via DI — that's more testable (you can inject a fake) and flexible than `expect/actual` (which allows exactly one implementation per platform and is harder to mock). So a common pattern is: interfaces + DI for most platform dependencies (testable), and `expect/actual` for simple platform primitives. Either way, the goal is the same — keep the shared logic in commonMain and fill in platform-specific pieces per target."}]},{level:"senior",q:"How do you decide what code to share in commonMain versus keep platform-specific?",a:[{t:"p",text:"**The principle is to share everything that's *platform-agnostic logic* (the more the better — that's the value of KMP) and keep platform-specific only what genuinely *requires* a platform's APIs or is inherently native (UI, platform integrations). The decision is driven by *whether the code needs platform APIs* and *whether sharing it reduces meaningful duplication/divergence risk*.**"},{t:"list",items:["**Share (put in commonMain)**: business logic and rules, domain models, use cases, repository implementations, networking (Ktor), serialization (kotlinx.serialization), data mapping, validation, caching logic, ViewModels/presentation logic (with multiplatform lifecycle), and anything expressible in pure Kotlin against multiplatform libraries. This is the *bulk* of an app's non-UI code, and it's exactly where you *want* one source of truth — the business logic shouldn't diverge between iOS and Android, and sharing it eliminates the double-implementation bugs.","**Keep platform-specific**: the UI (native SwiftUI/UIKit on iOS, Compose on Android — unless using Compose Multiplatform), platform integrations (permissions, notifications, camera, biometrics, deep links — each platform's APIs differ), and the *thin* platform primitives beneath shared logic (database driver, secure storage, file paths, platform info) — accessed from shared code via `expect/actual` or injected interfaces.","**The gray area — presentation logic**: ViewModels can be shared (KMP's ViewModel is multiplatform since lifecycle 2.8), which is valuable (share state management), but the *observation* mechanism differs per platform (Compose collects StateFlow directly; SwiftUI needs a bridge). So you often share the ViewModel logic but have a thin per-platform adapter for UI binding."]},{t:"list",items:["**The architectural leverage**: clean architecture makes this decision natural — a *framework-free domain and data layer* (no Android/iOS imports, dependencies inverted through interfaces) drops straight into commonMain. So the same discipline that makes code testable (separate logic from framework, inject dependencies) is what makes it *shareable*. Code that's tangled with `android.*` can't be shared; well-layered code can. This is a strong practical argument for clean architecture in KMP projects.","**Incremental adoption**: you don't have to share everything at once. A common path is to start by sharing a *slice* — the networking + data layer, or one feature's logic — and expand. This lets teams adopt KMP with low risk, sharing the high-value logic first while keeping UIs native.","**Don't force-share the un-shareable**: fighting to share something inherently platform-specific (UI, a platform-only capability) via awkward abstractions is counterproductive — the goal is to share what *benefits* from being shared (logic that would otherwise be duplicated and diverge), not to maximize the shared-line count for its own sake."]},{t:"p",text:"**The senior framing**: the share-vs-platform decision is fundamentally 'share the logic, keep the platform-native' — put the *platform-agnostic business logic* (the risky, complex, duplication-prone core) in commonMain where it becomes a single source of truth across platforms, and keep *genuinely platform-specific* code (UI, platform integrations, and the thin native primitives beneath shared logic via `expect/actual`/DI) per-platform. The enabling insight is that *clean architecture and testable design are the same thing as shareable design* — framework-free, dependency-injected layers move to commonMain for free, so the effort you spend on good architecture pays off doubly in KMP. And the pragmatic wisdom is to share *incrementally* (start with a high-value slice, expand) and to *not force-share* the inherently native — the metric is 'does sharing this reduce meaningful duplication/divergence?', not raw code-sharing percentage. Demonstrating the logic-vs-native principle, the architecture-enables-sharing insight, the presentation-layer nuance, and incremental/pragmatic adoption is the comprehensive senior answer — and it's exactly what an interviewer for an Android/KMP role wants to hear."}]},{level:"senior",q:"What are the tradeoffs of adopting KMP, and when would you (not) use it?",a:[{t:"p",text:"**KMP's core tradeoff is *shared logic and reduced duplication* versus *added build/tooling complexity and an evolving ecosystem, particularly on iOS*. I'd adopt it when the app has substantial non-UI logic worth sharing across Android and iOS and the team has (or can build) the Kotlin/KMP expertise; I'd be cautious for UI-heavy apps with little shared logic, single-platform apps, or teams without iOS/Kotlin cross-competence.**"},{t:"list",items:["**Benefit — single source of truth for logic**: the business logic, data layer, and models are written once, so they *can't diverge* between platforms — a bug fixed once is fixed everywhere, and behavior is guaranteed consistent. This is the biggest win: eliminating the double-implementation-and-drift problem for the app's complex core.","**Benefit — native UIs preserved**: unlike Flutter/RN, each app stays natively excellent (SwiftUI/Compose), so you don't sacrifice platform feel, performance, or access to the latest native UI features. You share what benefits from sharing (logic) and keep native what benefits from being native (UI).","**Benefit — leverage existing Kotlin skills**: for an Android team, KMP is *Kotlin* — the same language, coroutines, Flow — so the learning curve is far gentler than adopting Dart (Flutter) or a JS stack (RN), and you can share code with the *existing* Android codebase incrementally."]},{t:"list",items:["**Cost — build/tooling complexity**: KMP adds Gradle multiplatform configuration, source-set management, and a more complex build. The iOS integration (Kotlin/Native compiling to a framework/XCFramework, consumed by Xcode) adds friction — slower Kotlin/Native compile times, and the iOS build setup is more involved than a pure-Swift project.","**Cost — iOS interop friction**: the Kotlin→Swift boundary has rough edges — coroutines/Flow don't map natively to Swift (you need SKIE or wrappers), Kotlin generics and sealed classes translate imperfectly to Obj-C, and Swift developers consuming the shared code face a somewhat un-idiomatic API. Tooling (SKIE) has improved this a lot, but it's still a real consideration.","**Cost — evolving ecosystem**: while the core is stable, the ecosystem (especially some libraries and Compose Multiplatform for iOS) is younger than pure-native. Fewer battle-tested libraries exist for every need in common code, though the essential ones (Ktor, kotlinx.serialization, SQLDelight, coroutines, Koin) are solid.","**Cost — team dynamics**: it works best when the team has *both* Android/Kotlin and iOS competence (someone must own the iOS integration and Swift consumption). A pure-Android team adopting KMP still needs iOS knowledge to ship the iOS app well."]},{t:"list",items:["**When to use it**: apps with *substantial shared logic* across Android and iOS (complex business rules, significant data/networking/domain layers) where duplication and divergence are real pain; teams wanting to leverage Kotlin skills; and organizations that value *native UIs* (so Flutter/RN's non-native UI is a dealbreaker) but want logic sharing. It's ideal for logic-heavy, multi-platform apps where consistency of the core matters.","**When to be cautious / not use it**: *single-platform* apps (no sharing benefit — just added complexity); *UI-heavy apps with thin logic* (little to share — the shared layer would be small relative to the complexity added); teams *without* iOS or KMP expertise and no capacity to build it; and cases where a fully-shared-UI approach (Flutter/RN) genuinely fits better (rapid prototyping where native feel matters less, or teams standardized on Dart/JS).","**Incremental adoption reduces risk**: because KMP shares *logic* and integrates with existing native apps, you can adopt it *gradually* — share one feature or the data layer first, prove the value, and expand. This de-risks the decision versus an all-or-nothing rewrite, and is a strong point in KMP's favor for existing apps."]},{t:"p",text:"**The senior framing**: the KMP decision is a *shared-logic-value vs added-complexity* tradeoff, and the answer is contextual, not dogmatic. KMP shines when there's *substantial, complex, duplication-prone logic* to share across Android and iOS, the team values *native UIs* (ruling out Flutter/RN's shared-UI model), and Kotlin expertise exists — then the single-source-of-truth-for-logic benefit outweighs the build/interop/ecosystem costs. It's a poor fit for single-platform apps, UI-heavy/logic-thin apps, or teams lacking cross-platform competence. The key *risk mitigator* is incremental adoption — KMP's logic-sharing, native-UI-preserving design lets you start small (share the data layer or one feature) and expand, so you can validate the value with low commitment. And the honest acknowledgment of costs — iOS interop friction (coroutines/Flow bridging, Obj-C translation, though SKIE helps), build complexity, and a younger ecosystem — is what distinguishes a mature assessment from KMP boosterism. Demonstrating the contextual, tradeoff-driven decision (with specific benefits *and* costs), the incremental-adoption de-risking, and the honest iOS-friction acknowledgment is the comprehensive senior answer — especially valuable for an Android/KMP engineer expected to advocate for KMP *credibly*, not blindly."}]},{level:"junior",q:"What are Kotlin/JVM, Kotlin/Native, and Kotlin/JS?",a:[{t:"p",text:"These are Kotlin's *compiler backends/targets*. *Kotlin/JVM* compiles to JVM bytecode (Android, server). *Kotlin/Native* compiles to *native binaries* via LLVM (iOS, macOS, Linux, Windows) — no VM, produces a framework/executable. *Kotlin/JS* compiles to JavaScript (web). KMP works by compiling your *shared Kotlin* to each target with the appropriate backend — so `commonMain` code runs as JVM bytecode on Android and as a native framework on iOS. Understanding the backends explains why some platform APIs differ."},{t:"list",items:["**Kotlin/JVM** — bytecode (Android, server).","**Kotlin/Native** — native binaries via LLVM (iOS, desktop).","**Kotlin/JS** — JavaScript (web).","**KMP** — compiles shared code to each target."]},{t:"note",text:"Kotlin's compiler backends: Kotlin/JVM (bytecode — Android/server), Kotlin/Native (native binaries via LLVM — iOS/desktop, no VM), Kotlin/JS (JavaScript — web). KMP compiles shared commonMain code to each target with the right backend (JVM bytecode on Android, a native framework on iOS). The backends explain platform API differences."}]},{level:"senior",q:"What is the source set hierarchy in a KMP module?",a:[{t:"p",text:"A KMP module organizes code into *source sets*: `commonMain` (shared, no platform APIs) and platform sets (`androidMain`, `iosMain`, `desktopMain`) that add platform-specific code and can use that platform's APIs. *Intermediate (hierarchical) source sets* (e.g. `appleMain` shared by `iosX64Main`/`iosArm64Main`, or a `nativeMain`) let you share code among *a subset* of targets. Each `*Main` has a matching `*Test`. Dependencies and `expect`/`actual` declarations flow down this hierarchy — common declares, platform sets implement."},{t:"list",items:["**`commonMain`** — shared, no platform APIs.","**Platform sets** — `androidMain`/`iosMain`, platform APIs.","**Intermediate sets** — share among a subset (e.g. `appleMain`).","**`expect`/`actual`** — common declares, platform implements."]},{t:"note",text:"KMP source sets: commonMain (shared, no platform APIs) + platform sets (androidMain, iosMain) with platform code/APIs, plus intermediate/hierarchical sets (appleMain, nativeMain) to share among a subset of targets. Each *Main has a *Test. Dependencies and expect/actual flow down: common declares, platform sets implement."}]},{level:"senior",q:"How does expect/actual work for functions, classes, and typealiases?",a:[{t:"p",text:"`expect` in `commonMain` declares an API *without implementation*; each platform provides the `actual`. For a *function*, common declares the signature, platforms implement it. For a *class*, common declares members (`expect class`), platforms provide `actual class` with real bodies. A powerful shortcut is *`actual typealias`*: map the expected class to an *existing platform type* (e.g. `actual typealias UUID = java.util.UUID` on JVM) — no re-implementation. Use expect/actual to abstract platform differences behind a common API."},{t:"code",title:"expect/actual with typealias",code:`// commonMain
expect class Platform() { val name: String }
// androidMain
actual class Platform actual constructor() { actual val name = "Android \${Build.VERSION.SDK_INT}" }
// or map to an existing type:
// actual typealias UUID = java.util.UUID`},{t:"note",text:"expect (in commonMain) declares an API without a body; each platform gives the actual. Functions: common declares, platforms implement. Classes: expect class + actual class with bodies. actual typealias maps the expected type to an existing platform type (actual typealias UUID = java.util.UUID) — no re-implementation. Abstract platform differences behind a common API."}]},{level:"junior",q:"What is the difference between KMP and KMM?",a:[{t:"p",text:"*KMP (Kotlin Multiplatform)* is the general technology for sharing Kotlin across *any* targets (Android, iOS, desktop, web, server). *KMM (Kotlin Multiplatform Mobile)* was the earlier *branding* for the *mobile-focused* subset (Android + iOS). Google/JetBrains have largely consolidated on 'KMP' (and Compose Multiplatform) as the umbrella term; 'KMM' is now mostly historical. Functionally they're the same core tech — KMM was just KMP scoped to mobile. In interviews, use 'KMP' and mention KMM as the older name."},{t:"list",items:["**KMP** — general Kotlin sharing across any targets.","**KMM** — older branding for the mobile (Android+iOS) subset.","**Consolidated** — on 'KMP' now.","**Same core tech** — KMM was KMP scoped to mobile."]},{t:"note",text:"KMP (Kotlin Multiplatform) is the general tech for sharing Kotlin across any targets (Android, iOS, desktop, web, server). KMM (Kotlin Multiplatform Mobile) was the older branding for the mobile-focused subset. The ecosystem consolidated on 'KMP'; KMM is now mostly historical. Same core tech — use 'KMP', mention KMM as the old name."}]},{level:"senior",q:"How does the Kotlin/Native memory model work?",a:[{t:"p",text:"The *new* Kotlin/Native memory manager (default since Kotlin 1.7.20+) allows *sharing mutable state across threads* like the JVM (with a tracing GC), removing the old model's severe restrictions (the old one *froze* objects and forbade cross-thread mutation, which made coroutines painful). Now common concurrency (coroutines, shared state) 'just works' across platforms much more like Android. You rarely deal with `freeze()`/`@ThreadLocal` anymore. This change hugely improved the KMP developer experience, especially for coroutines/Flows shared with iOS."},{t:"list",items:["**New memory manager** — JVM-like, tracing GC (default 1.7.20+).","**Shares mutable state** — across threads (no freezing).","**Old model** — froze objects, forbade cross-thread mutation.","**Result** — coroutines/shared state work smoothly."]},{t:"note",text:"The new Kotlin/Native memory manager (default since 1.7.20+) allows JVM-like sharing of mutable state across threads with a tracing GC — removing the old model's object freezing and cross-thread-mutation ban that made coroutines painful. Now coroutines/shared state 'just work' across platforms; you rarely touch freeze()/@ThreadLocal. A huge KMP DX improvement."}]},{level:"junior",q:"What targets can a KMP project produce?",a:[{t:"p",text:"KMP can target *Android* (JVM), *iOS* (arm64 device + x64/arm64 simulator, via Kotlin/Native frameworks), *desktop* (JVM — Windows/macOS/Linux), *web* (Kotlin/JS or the newer Wasm), *server* (JVM), *macOS/watchOS/tvOS*, and more. You declare targets in the Gradle KMP block; shared code compiles to each. Most mobile teams target Android + iOS (sharing logic), sometimes adding desktop/web with *Compose Multiplatform* for shared UI. The breadth means one Kotlin codebase can reach many platforms."},{t:"list",items:["**Mobile** — Android (JVM) + iOS (Native).","**Desktop/server** — JVM.","**Web** — Kotlin/JS or Wasm.","**More** — macOS/watchOS/tvOS; declared in Gradle."]},{t:"note",text:"KMP targets Android (JVM), iOS (Native frameworks), desktop (JVM), web (Kotlin/JS or Wasm), server (JVM), and macOS/watchOS/tvOS. Declare targets in the Gradle KMP block; shared code compiles to each. Most teams target Android+iOS (shared logic), sometimes adding desktop/web with Compose Multiplatform for shared UI."}]},{level:"senior",q:"What is the difference between sharing logic and sharing UI in KMP?",a:[{t:"p",text:"*Sharing logic* (the common, mature approach) puts business logic, networking, data, and ViewModels in `commonMain`, while *each platform keeps its native UI* (Jetpack Compose on Android, SwiftUI on iOS) — you get shared correctness with fully native UX. *Sharing UI* uses *Compose Multiplatform* to also share the *UI layer* across Android/iOS/desktop — more code shared, but the iOS UI is Compose-rendered (not native SwiftUI), a newer/less-battle-tested path. Many teams share logic first (lower risk) and consider shared UI selectively."},{t:"table",headers:["","Share logic","Share UI (CMP)"],rows:[["UI","Native per platform","Compose everywhere"],["Maturity","Mature, low risk","Newer"],["Sharing","Logic/data/VM","+ UI layer"]]},{t:"note",text:"Share logic (mature): business logic/networking/data/ViewModels in commonMain, native UI per platform (Compose + SwiftUI) — shared correctness, native UX. Share UI (Compose Multiplatform): also share the UI layer across platforms — more sharing but iOS renders Compose, not native SwiftUI (newer). Many teams share logic first (low risk), add shared UI selectively."}]},{level:"junior",q:"How does iOS consume the shared KMP code?",a:[{t:"p",text:"The shared module compiles (via Kotlin/Native) into an *Objective-C-compatible framework* that the iOS app *imports and calls like any framework* — Swift code calls the Kotlin APIs (through the Obj-C bridge). You integrate it via *CocoaPods*, the *Swift Package Manager* export, or a direct framework, and (increasingly) with a *tool like SKIE/KMP-NativeCoroutines* to make coroutines/Flows and generics Swift-friendly. On Android it's just a regular Kotlin/JVM dependency. So Android consumes bytecode; iOS consumes a native framework."},{t:"list",items:["**iOS** — a native Obj-C-compatible framework (Kotlin/Native).","**Swift calls** — Kotlin APIs via the Obj-C bridge.","**Integrate** — CocoaPods/SPM/direct framework.","**Android** — a regular Kotlin/JVM dependency."]},{t:"note",text:"The shared module compiles (Kotlin/Native) into an Obj-C-compatible framework the iOS app imports and calls like any framework (Swift → Kotlin via the Obj-C bridge), integrated via CocoaPods/SPM/direct framework (often with SKIE for Swift-friendly coroutines/Flows). Android consumes it as a regular Kotlin/JVM dependency. Android = bytecode; iOS = native framework."}]},{level:"senior",q:"How do you access platform-specific APIs from common code?",a:[{t:"p",text:"Three main patterns: (1) *`expect`/`actual`* — declare the API in common, implement per platform (good for small, well-defined platform gaps). (2) *Interface + platform implementation injected via DI* — define an interface in common, provide platform implementations and inject them (cleaner for larger abstractions, more testable). (3) *Multiplatform libraries* that already abstract the platform (Ktor for networking, SQLDelight for DB) — prefer these so you don't reinvent. Choose expect/actual for thin gaps, DI'd interfaces for substantial platform services."},{t:"list",items:["**expect/actual** — thin, well-defined platform gaps.","**Interface + DI** — larger abstractions; more testable.","**Multiplatform libs** — Ktor/SQLDelight abstract the platform.","**Choose** — by the size of the platform surface."]},{t:"note",text:"Access platform APIs via: expect/actual (thin well-defined gaps), an interface in common with platform implementations injected via DI (larger abstractions, more testable), or multiplatform libraries that already abstract it (Ktor, SQLDelight — prefer these). Use expect/actual for small gaps, DI'd interfaces for substantial platform services."}]},{level:"junior",q:"How mature and stable is KMP today?",a:[{t:"p",text:"Kotlin Multiplatform reached *Stable* (production-ready) — JetBrains declared it stable, the new memory manager removed old pain, and major companies ship it. *Compose Multiplatform* is stable on Android/desktop and iOS support has matured (stable more recently). The *ecosystem* (Ktor, SQLDelight, Koin, kotlinx.serialization/coroutines/datetime) is solid for shared logic. Tooling (Android Studio + the KMP plugin, Xcode integration) keeps improving. It's a *safe choice* for sharing business logic; shared UI is viable but newer. Frame it as production-ready with an active, maturing ecosystem."},{t:"list",items:["**KMP** — Stable, production-ready; used by major companies.","**Compose Multiplatform** — stable, iOS matured.","**Ecosystem** — Ktor/SQLDelight/Koin/kotlinx solid.","**Safe** — for shared logic; shared UI newer."]},{t:"note",text:"KMP is Stable (production-ready — JetBrains declared it stable, new memory manager removed old pain, major companies ship it). Compose Multiplatform is stable with matured iOS. The ecosystem (Ktor, SQLDelight, Koin, kotlinx) is solid for shared logic; tooling keeps improving. A safe choice for sharing business logic; shared UI is viable but newer."}]},{level:"senior",q:"How do you incrementally adopt KMP in an existing app?",a:[{t:"p",text:"Start *small and low-risk*: extract a *single, well-bounded piece of logic* (e.g. validation, a networking/data layer, or a feature's use-cases) into a shared KMP module, keep both apps' UI native, and verify the iOS integration works end-to-end. Then expand *feature by feature*. Wrap shared code behind clean interfaces so platform code depends on abstractions. Avoid a big-bang rewrite. This de-risks adoption: prove the toolchain/interop on one slice, build team confidence, then grow the shared surface where sharing pays off."},{t:"list",items:["**Start small** — one bounded piece (validation/data layer).","**Keep UI native** — verify iOS integration end-to-end.","**Expand** — feature by feature.","**Avoid** — big-bang rewrite; grow where sharing pays."]},{t:"note",text:"Adopt KMP incrementally: extract one well-bounded piece of logic (validation, networking/data layer, a feature's use-cases) into a shared module, keep UI native, verify iOS integration end-to-end, then expand feature by feature behind clean interfaces. Avoid a big-bang rewrite — prove the toolchain/interop on one slice, build confidence, grow where sharing pays off."}]},{level:"junior",q:"What is commonTest, and how do you test shared code?",a:[{t:"p",text:"`commonTest` holds tests for `commonMain` that run on *every target* — you write them once (using `kotlin.test`, `kotlinx-coroutines-test`, and multiplatform assertion/mocking libs) and they execute on JVM, Native, etc. This is a *big KMP benefit*: your shared business logic is tested once and validated across platforms. Platform-specific tests go in `androidTest`/`iosTest`. Prefer putting logic in common (with injected platform abstractions) so most of it is covered by fast, shared `commonTest` tests."},{t:"list",items:["**`commonTest`** — tests for common code, run on all targets.","**Write once** — validated across platforms.","**Tools** — `kotlin.test`, coroutines-test, MP mocking.","**Benefit** — shared logic tested once, everywhere."]},{t:"note",text:"commonTest holds tests for commonMain that run on every target (kotlin.test, kotlinx-coroutines-test, MP mocking) — write once, validated across JVM/Native. A big KMP benefit: shared logic tested once, everywhere. Platform-specific tests go in androidTest/iosTest. Put logic in common (with injected platform abstractions) so most is covered by fast shared tests."}]},{level:"senior",q:"What is cinterop, and when do you need it?",a:[{t:"p",text:"*cinterop* is Kotlin/Native's tool for *interoperating with C (and Objective-C) libraries* — it generates Kotlin bindings from C headers so your Native code can call platform C APIs or third-party C/Obj-C libraries. You need it when a shared module must use an *iOS/native C library* not already wrapped by a Kotlin API. For most app code you *don't* touch cinterop (multiplatform libraries cover networking/DB/etc.); it's for lower-level integration with native SDKs. It's powerful but adds build complexity — use it only when necessary."},{t:"list",items:["**cinterop** — generates Kotlin bindings from C/Obj-C headers.","**Use** — call native C/Obj-C libraries from Kotlin/Native.","**Most apps** — don't need it (MP libraries suffice).","**Trade-off** — powerful but adds build complexity."]},{t:"note",text:"cinterop is Kotlin/Native's tool to interoperate with C/Obj-C libraries — generating Kotlin bindings from C headers so Native code calls platform/third-party C APIs. Needed when a shared module must use a native C/Obj-C library without a Kotlin wrapper. Most app code doesn't touch it (MP libraries cover the common cases); it adds build complexity — use only when necessary."}]},{level:"junior",q:"How do coroutines work in shared KMP code?",a:[{t:"p",text:"`kotlinx.coroutines` is *multiplatform* — you use `suspend` functions, `Flow`, `CoroutineScope`, and dispatchers in `commonMain`, and they work on every target (thanks to the new Native memory model). On Android they run on the JVM; on iOS they run via Kotlin/Native. The main friction is *consuming* them from *Swift* (Swift can't await Kotlin coroutines natively) — solved with *SKIE* or *KMP-NativeCoroutines*, which expose suspend/Flow as Swift async/Combine. So coroutines are fully usable in shared code; only the Swift boundary needs a helper."},{t:"list",items:["**kotlinx.coroutines** — multiplatform; works on all targets.","**commonMain** — `suspend`, `Flow`, scopes, dispatchers.","**New memory model** — makes them smooth on Native.","**Swift boundary** — SKIE/NativeCoroutines for async/Combine."]},{t:"note",text:"kotlinx.coroutines is multiplatform — suspend, Flow, scopes, dispatchers work in commonMain on every target (smooth thanks to the new Native memory model). Only friction: consuming them from Swift (can't await Kotlin coroutines natively) — solved by SKIE/KMP-NativeCoroutines exposing them as Swift async/Combine. Coroutines are fully usable in shared code; just the Swift boundary needs a helper."}]},{level:"senior",q:"What is the difference between 'multiplatform' and 'cross-platform'?",a:[{t:"p",text:"*Cross-platform* frameworks (Flutter, React Native) typically ship *their own runtime/UI engine* and aim to write the *whole app once*, rendering a non-native UI (Flutter draws its own widgets; RN bridges to native views). *Multiplatform* (KMP) shares *Kotlin code* but *compiles to each platform natively* and (in the logic-sharing model) *keeps native UI* per platform — you share what makes sense and stay native where it matters. The philosophy differs: KMP is 'share code, stay native'; Flutter/RN is 'one UI everywhere'. This is why KMP integrates smoothly into existing native apps."},{t:"list",items:["**Cross-platform (Flutter/RN)** — own runtime/UI, whole app once.","**Multiplatform (KMP)** — share Kotlin, compile natively.","**KMP philosophy** — share code, stay native.","**Integration** — KMP slots into existing native apps."]},{t:"note",text:"Cross-platform (Flutter/RN): own runtime/UI engine, write the whole app once, non-native rendering. Multiplatform (KMP): share Kotlin but compile natively per platform, keep native UI (logic-sharing model) — share what makes sense, stay native where it matters. KMP = 'share code, stay native'; Flutter/RN = 'one UI everywhere'. Why KMP integrates smoothly into existing native apps."}]},{level:"junior",q:"What is a shared module's output on each platform?",a:[{t:"p",text:"On *Android*, the shared module builds like a normal Kotlin/JVM library — an *AAR/JVM artifact* the app depends on directly. On *iOS*, it builds (via Kotlin/Native) into a *framework* (`.framework`, Obj-C-compatible) that the Xcode project links and Swift imports. So the *same shared source* produces a JVM dependency for Android and a native framework for iOS. The Gradle KMP plugin generates both from your one `commonMain` (plus platform sets). This dual output is the essence of 'write once, compile natively per platform'."},{t:"list",items:["**Android** — AAR/JVM artifact (direct dependency).","**iOS** — a native `.framework` (Kotlin/Native).","**Same source** — two native outputs.","**Gradle KMP plugin** — generates both."]},{t:"note",text:"The shared module outputs an AAR/JVM artifact on Android (a direct dependency) and a native .framework (Obj-C-compatible) on iOS via Kotlin/Native (linked in Xcode, imported by Swift). Same commonMain source → two native outputs, both generated by the Gradle KMP plugin. This dual output embodies 'write once, compile natively per platform'."}]},{level:"senior",q:"What kinds of code should NOT go in commonMain?",a:[{t:"p",text:"Keep out of common: *platform UI* (Views/Compose Android-only, SwiftUI iOS-only — unless using Compose Multiplatform deliberately), *platform-only APIs* (Android `Context`, iOS `UIKit`) used directly, *JVM-only libraries* (Retrofit, Hilt, Room's Android artifact, `java.*`-dependent code), and anything inherently tied to one OS. Instead, abstract those behind `expect`/`actual` or interfaces and put *only portable logic* in common. If code can't compile on Kotlin/Native, it doesn't belong in `commonMain` — move it to the platform set."},{t:"list",items:["**Platform UI** — Views/Compose/SwiftUI (unless CMP).","**Platform APIs** — Context/UIKit directly.","**JVM-only libs** — Retrofit/Hilt/Room/`java.*`.","**Rule** — if it can't compile on Native, not in common."]},{t:"note",text:"Don't put in commonMain: platform UI (Views/Compose/SwiftUI unless CMP), platform-only APIs used directly (Context, UIKit), JVM-only libraries (Retrofit, Hilt, Room-Android, java.*-dependent code), and OS-tied code. Abstract those behind expect/actual or interfaces; keep only portable logic in common. If it can't compile on Kotlin/Native, it belongs in a platform set."}]},{level:"junior",q:"What is Compose Multiplatform's relationship to KMP?",a:[{t:"p",text:"*Compose Multiplatform (CMP)* is a UI framework *built on KMP* that lets you write *shared UI* in Jetpack Compose that runs on Android, desktop, iOS, and web — extending code sharing from logic to the *UI layer*. KMP is the foundation (shared Kotlin, targets); CMP is one thing you can build *with* it. You can use KMP *without* CMP (share logic, native UI) or *with* it (share UI too). On iOS, CMP renders Compose (via Skia), not native SwiftUI. So: KMP = share code; CMP = share Compose UI on top of KMP."},{t:"list",items:["**CMP** — shared Compose UI, built on KMP.","**Extends sharing** — logic → UI layer.","**Optional** — use KMP with or without CMP.","**iOS** — renders Compose (Skia), not SwiftUI."]},{t:"note",text:"Compose Multiplatform (CMP) is a UI framework built on KMP for shared Compose UI across Android/desktop/iOS/web — extending sharing from logic to UI. KMP is the foundation; CMP is optional on top (use KMP with native UI, or with CMP for shared UI). On iOS, CMP renders Compose via Skia, not native SwiftUI. KMP = share code; CMP = share Compose UI."}]},{level:"senior",q:"How do you handle platform differences like dates, files, and threading?",a:[{t:"p",text:"Use *multiplatform libraries* where they exist: `kotlinx-datetime` for dates/times, `okio`/multiplatform APIs for files, `kotlinx.coroutines` (with `Dispatchers`) for threading. For gaps, wrap the platform behind `expect`/`actual` or an injected interface (e.g. an `expect fun currentTimeMillis()` or a `PlatformFileStorage` interface). Avoid `java.time`/`java.io` in common (JVM-only). The rule: reach for a multiplatform library first, abstract the rest behind a common API, and keep the platform specifics thin and in platform source sets."},{t:"list",items:["**Dates** — `kotlinx-datetime`.","**Files** — okio / multiplatform APIs.","**Threading** — coroutines/dispatchers.","**Gaps** — expect/actual or injected interfaces; avoid `java.*`."]},{t:"note",text:"Handle platform differences with multiplatform libraries: kotlinx-datetime (dates), okio/MP APIs (files), kotlinx.coroutines + Dispatchers (threading). For gaps, wrap the platform behind expect/actual or an injected interface. Avoid java.time/java.io in common (JVM-only). Reach for an MP library first, abstract the rest behind a common API, keep specifics thin in platform sets."}]},{level:"junior",q:"What tooling and IDEs do you use for KMP development?",a:[{t:"p",text:"*Android Studio* (or IntelliJ) with the *Kotlin Multiplatform plugin* is the primary IDE for shared/Kotlin code and Android; you use *Xcode* for the iOS app (and to build/run on iOS, since it needs the Apple toolchain). Gradle drives the build; the KMP Gradle plugin manages targets and framework export. JetBrains' *Kotlin Multiplatform* wizard/plugin scaffolds projects. The DX is 'Kotlin in Android Studio, iOS app in Xcode, shared module bridging both' — improving steadily but still a two-IDE workflow for full-stack mobile."},{t:"list",items:["**Android Studio + KMP plugin** — shared Kotlin + Android.","**Xcode** — the iOS app + Apple toolchain.","**Gradle** — build + framework export.","**Workflow** — two IDEs bridged by the shared module."]},{t:"note",text:"KMP tooling: Android Studio/IntelliJ + the Kotlin Multiplatform plugin for shared Kotlin and Android; Xcode for the iOS app (needs the Apple toolchain). Gradle + the KMP plugin manage targets and framework export; a JetBrains wizard scaffolds projects. DX is 'Kotlin in Android Studio, iOS in Xcode, shared module bridging both' — improving but still a two-IDE workflow."}]},{level:"senior",q:"What are the performance characteristics of shared KMP code?",a:[{t:"p",text:"Shared code compiles to *native* on each platform (JVM bytecode on Android, LLVM-compiled Native on iOS) — so it runs at *near-native speed*, not through an interpreter/bridge like some cross-platform frameworks. On Android it's identical to normal Kotlin. On iOS, Kotlin/Native performance is generally good; the *Obj-C interop bridge* adds some overhead at the *boundary* (frequent fine-grained Swift↔Kotlin calls or large object conversions can cost), so design *coarse-grained* APIs across the boundary. Binary size adds the Kotlin/Native runtime. Overall: native execution, mind the interop boundary."},{t:"list",items:["**Compiles native** — JVM + LLVM Native; near-native speed.","**Android** — identical to normal Kotlin.","**iOS boundary** — Obj-C bridge overhead; design coarse APIs.","**Size** — adds the Native runtime."]},{t:"note",text:"Shared code compiles native (JVM bytecode on Android, LLVM Native on iOS) — near-native speed, no interpreter/bridge for execution. Android = identical to normal Kotlin. iOS: good Native perf, but the Obj-C interop bridge adds boundary overhead (frequent fine-grained Swift↔Kotlin calls/large conversions cost) — design coarse-grained APIs. Binary adds the Native runtime."}]},{level:"junior",q:"What is the role of Gradle in a KMP project?",a:[{t:"p",text:"Gradle (with the *Kotlin Multiplatform plugin*) is the *build system* that ties everything together: it declares *targets* (android, iosArm64, etc.), defines *source sets* and their *dependencies* (common vs platform), compiles shared code to each target, and *exports the iOS framework* (via CocoaPods plugin or direct framework tasks). You configure it in the module's `build.gradle.kts` `kotlin { }` block. Gradle also handles versioning of multiplatform dependencies. It's the single place that orchestrates producing both the Android artifact and the iOS framework."},{t:"list",items:["**KMP plugin** — declares targets + source sets.","**Dependencies** — per source set (common/platform).","**Compiles** — shared code to each target.","**Exports** — the iOS framework (CocoaPods/direct)."]},{t:"note",text:"Gradle + the Kotlin Multiplatform plugin is the build system: declares targets (android, iosArm64…), defines source sets and their dependencies (common vs platform), compiles shared code to each target, and exports the iOS framework (CocoaPods plugin/direct tasks) — configured in the kotlin { } block. It orchestrates producing both the Android artifact and the iOS framework."}]},{level:"junior",q:"What is a typical KMP project module structure?",a:[{t:"p",text:"A common layout: a *`shared` (or `:core`) KMP module* with `commonMain` (business logic, data, ViewModels, networking) plus `androidMain`/`iosMain` for platform bits; an *`androidApp`* module (the Android app, native Compose UI depending on `shared`); and an *`iosApp`* (an Xcode project consuming the shared framework). Larger apps split `shared` into *feature/layer modules* (e.g. `:network`, `:database`, `:feature-auth`) each multiplatform. The principle: shared Kotlin modules for logic, thin platform app modules for UI/entry points."},{t:"list",items:["**`shared` module** — commonMain + platform sets.","**`androidApp`** — native UI depending on shared.","**`iosApp`** — Xcode project consuming the framework.","**Scale** — split shared into feature/layer modules."]},{t:"note",text:"Typical KMP structure: a shared/core KMP module (commonMain logic/data/VM + androidMain/iosMain), an androidApp module (native Compose UI depending on shared), and an iosApp (Xcode consuming the shared framework). Larger apps split shared into multiplatform feature/layer modules (:network, :database, :feature-auth). Shared Kotlin for logic, thin platform app modules for UI."}]},{level:"senior",q:"What is the difference between an expect class and an interface with platform implementations?",a:[{t:"p",text:"Both abstract platform differences, but differ in *binding*. `expect class` is *compile-time*: the common declaration is resolved to *the* platform `actual` at build time (one implementation per platform, chosen by the compiler) — good for a single, well-defined platform primitive. An *interface* is *runtime*: you can have *multiple implementations*, *inject* them (via DI), and *swap a fake* in tests — better for testability and larger services. Rule of thumb: `expect/actual` for thin, singular platform gaps; interfaces + DI for anything you'll want to mock or vary."},{t:"table",headers:["","expect class","Interface + DI"],rows:[["Binding","Compile-time","Runtime"],["Implementations","One per platform","Multiple, swappable"],["Testability","Harder to fake","Easy to fake"],["Best for","Thin platform primitive","Larger service, mockable"]]},{t:"note",text:"expect class binds at compile-time to one actual per platform (good for a singular platform primitive). An interface binds at runtime — multiple implementations, injectable via DI, fakeable in tests (better for testability/larger services). Rule: expect/actual for thin singular gaps; interfaces + DI for anything you'll mock or vary."}]},{level:"senior",q:"How do you decide between native, KMP, and Flutter/RN for a new app?",a:[{t:"p",text:"Weigh *team skills, UX bar, code-reuse goals, and risk*. *Fully native* (separate Android/iOS): best UX/platform fit, most duplicated effort — good when platforms diverge or teams are siloed. *KMP*: share *logic* (and optionally UI), keep native UX, integrates into existing native apps, low risk for the logic layer — great when you value native UI but want one source of truth for business logic. *Flutter/RN*: fastest to one UI everywhere, non-native rendering, own ecosystem — good for UI-uniform apps or non-Kotlin teams. Choose by how much you value native UX vs maximal sharing."},{t:"list",items:["**Native** — best UX, most duplication.","**KMP** — share logic, native UX, low-risk, native-integration.","**Flutter/RN** — one UI fast, non-native rendering.","**Decide** — native UX value vs maximal sharing + team skills."]},{t:"note",text:"Choose by team skills, UX bar, reuse goals, risk. Native: best UX, most duplication (diverging platforms/siloed teams). KMP: share logic (± UI), keep native UX, integrates into native apps, low-risk logic layer (value native UI + one logic source). Flutter/RN: fastest one-UI-everywhere, non-native rendering (UI-uniform apps/non-Kotlin teams). Trade native UX vs maximal sharing."}]}],CT={id:"fundamentals",title:"KMP Fundamentals",tagline:"What KMP is (share logic, keep native) vs Flutter/RN, source sets (commonMain & platform), expect/actual, targets & project structure.",tags:["KMP","commonMain","expect/actual","Source Sets","Targets"],content:ST,qa:AT},TT=[{heading:"How shared Kotlin reaches Swift",blocks:[{t:"p",text:"For iOS to use your shared Kotlin code, **Kotlin/Native compiles the shared module into an Obj-C-compatible framework** (or XCFramework) that the iOS app imports and calls from Swift. The bridge goes Kotlin → Objective-C → Swift: Kotlin/Native generates Obj-C headers for your shared code, and Swift can call Obj-C, so Swift calls your Kotlin. This works, but the *Kotlin→Obj-C* translation is where the friction lives — because Obj-C's type system is less rich than Kotlin's (and Swift's), some Kotlin features translate awkwardly."},{t:"list",items:["**The framework** — the shared module builds into a `.framework`/`.xcframework` (a binary + Obj-C headers). The iOS app adds it as a dependency and `import shared`, then calls the shared classes/functions like any library.","**The Obj-C intermediary** — because Kotlin/Native exposes to Obj-C (not directly to Swift), everything passes through Obj-C's model, which loses some of Kotlin's type information. This is the root of most interop rough edges.","**Multi-module → umbrella framework** — if the shared code spans multiple KMP modules, you export them into *one* framework via an 'umbrella' module (direct multi-framework consumption breaks type identity across frameworks). One framework, one import."]}]},{heading:"The interop pain points",blocks:[{t:"table",headers:["Kotlin feature","How it translates to Swift","Issue"],rows:[["suspend functions","become completion-handler / async functions","usable but not idiomatic; no structured concurrency across the boundary"],["Flow / StateFlow","no native equivalent — needs wrapping","Swift can't collect a Flow directly"],["sealed classes","become classes with no exhaustiveness","Swift loses the exhaustive `switch` — must use `is` checks"],["Generics","largely erased to Any/Obj-C types","type safety lost across the boundary"],["Kotlin exceptions","must be @Throws-annotated or they crash Swift","uncaught Kotlin exceptions terminate the app"],["Default arguments","not supported in Obj-C","Swift must provide all arguments"]]},{t:"list",items:["**Coroutines/Flow** — the biggest one. Obj-C/Swift have no coroutines, so `suspend` functions become completion handlers (or Swift `async` in newer Kotlin), and `Flow` has *no* native mapping — Swift can't just `for await` a Kotlin Flow. You need a bridge (SKIE or hand-written wrappers).","**Sealed classes → no exhaustiveness** — Kotlin's exhaustive `when` becomes, in Swift, a chain of `is` type checks with no compiler guarantee you handled every case — you lose a key safety feature.","**Generics erased** — Kotlin generics mostly don't survive to Obj-C, so type safety across the boundary is weakened.","**Exceptions** — an uncaught Kotlin exception crossing to Swift *crashes* the app unless the function is `@Throws`-annotated (which maps it to a Swift error). So error handling needs care — prefer modeling errors as return values (Result/sealed) over throwing across the boundary."]}]},{heading:"SKIE — smoothing the Swift experience",blocks:[{t:"p",text:"**SKIE** (Swift Kotlin Interface Enhancer, by Touchlab) is a tool that dramatically improves the Swift-facing API of your shared Kotlin code. It's a compiler plugin that generates a nicer Swift layer over the Obj-C interface, translating Kotlin features into *idiomatic Swift* — the most important being **turning Flows into Swift `AsyncSequence`** and **sealed classes into proper Swift enums** (restoring exhaustiveness)."},{t:"list",items:["**Flow → Swift `AsyncSequence`** — SKIE makes a Kotlin `Flow` collectable in Swift with `for await value in flow`, natively — the single most valuable thing it does, since reactive streams are central and otherwise painful.","**Sealed classes → Swift enums** — SKIE turns Kotlin sealed hierarchies into real Swift enums, so Swift gets *exhaustive `switch`* (the compiler ensures every case is handled) — restoring the safety lost in raw Obj-C interop.","**Better suspend functions, default arguments, and more** — SKIE improves several translations, making the shared API feel like a native Swift library rather than an Obj-C one.","**Why it matters** — without SKIE, Swift developers consuming your shared Kotlin get an awkward, un-idiomatic API (completion handlers, no enums, manual Flow bridging), which hurts adoption and productivity. SKIE makes the shared code pleasant to use from Swift, which is important for the iOS team accepting KMP."]}]},{heading:"Bridging state to SwiftUI",blocks:[{t:"p",text:"A shared ViewModel exposes state as `StateFlow<UiState>`. SwiftUI is state-driven (via `@Published`/`@Observable`), so you bridge the Kotlin StateFlow to a SwiftUI-observable object — typically a thin Swift `ObservableObject` adapter that collects the flow (via SKIE's AsyncSequence) and republishes into a `@Published` property SwiftUI observes."},{t:"code",title:"Swift adapter over a shared ViewModel (with SKIE)",code:`@MainActor
class ProfileObservable: ObservableObject {
    @Published var state: ProfileUiState = ProfileUiState.Loading()
    private let viewModel: ProfileViewModel   // shared Kotlin ViewModel

    init(viewModel: ProfileViewModel) {
        self.viewModel = viewModel
        Task {
            for await state in viewModel.uiState {   // SKIE: Flow -> AsyncSequence
                self.state = state
            }
        }
    }
    deinit { viewModel.clear() }   // clean up the shared ViewModel
}
// SwiftUI view observes ProfileObservable.state`},{t:"list",items:["**The pattern** — a Swift `ObservableObject` wraps the shared ViewModel, subscribes to its `StateFlow` (as an AsyncSequence via SKIE), and republishes into `@Published` state; the SwiftUI view observes *that*. Intents are just method calls into the shared ViewModel (Swift calls Kotlin functions natively — that direction is easy).","**Lifecycle/threading** — the adapter must call `clear()`/cancel on `deinit` (the shared VM has no automatic iOS lifecycle), and state updates must land on the *main thread* for SwiftUI (`@MainActor`).","**The result** — the *same* Kotlin ViewModel drives both a Compose UI on Android (which collects the StateFlow directly) and a SwiftUI UI on iOS (via the adapter). The presentation *logic* is shared; only the thin UI-binding adapter differs — the essence of KMP done well."]},{t:"note",text:"iOS interop: Kotlin/Native compiles shared code to an Obj-C framework (multi-module → umbrella/XCFramework) that Swift imports. Friction from Kotlin→Obj-C translation: coroutines/Flow have no native Swift mapping (suspend → completion handlers/async; Flow needs wrapping), sealed classes lose exhaustiveness, generics erased, uncaught exceptions crash Swift (use @Throws / errors-as-values). SKIE fixes the worst: Flow → Swift AsyncSequence, sealed classes → Swift enums (exhaustive switch). Bridge shared StateFlow to SwiftUI via an ObservableObject adapter (collect flow → @Published), calling clear() on deinit; intents are direct Kotlin calls."}]}],IT=[{level:"junior",q:"How does iOS use shared Kotlin code in a KMP project?",a:[{t:"p",text:"**Kotlin/Native compiles the shared module into an Objective-C-compatible framework (or XCFramework), which the iOS app imports and calls from Swift — the bridge goes Kotlin → Objective-C → Swift.** Kotlin/Native generates Obj-C headers for your shared code, and since Swift can call Obj-C, Swift can call your Kotlin classes and functions like any imported library."},{t:"list",items:["**The framework** — the shared KMP module builds into a `.framework`/`.xcframework` (a compiled binary plus Obj-C headers). The iOS (Xcode) app adds it as a dependency, does `import shared`, and calls the shared code directly.","**The Obj-C intermediary** — Kotlin/Native exposes to Obj-C, not directly to Swift. Swift then consumes the Obj-C interface. This intermediary is where interop friction comes from, because Obj-C's type system is less rich than Kotlin's/Swift's.","**Multiple modules → one framework** — if shared code spans several KMP modules, you export them through an 'umbrella' module into a single framework (consuming multiple frameworks directly breaks type identity across them)."]},{t:"p",text:"So calling *into* shared Kotlin from Swift is straightforward — Swift calls Kotlin functions/constructors natively (that direction works well). The complications arise from *how certain Kotlin features translate* through Obj-C: coroutines/Flow, sealed classes, generics, and exceptions don't map cleanly. Tools like SKIE smooth these over to make the Swift experience idiomatic. The typical project shape is a `shared` module consumed by both an `androidApp` (Gradle dependency) and an `iosApp` (the framework in Xcode)."}]},{level:"junior",q:"What is SKIE and what problem does it solve?",a:[{t:"p",text:"**SKIE (Swift Kotlin Interface Enhancer) is a tool that generates a nicer, more idiomatic Swift layer over the Obj-C interface of your shared Kotlin code. It solves the problem that raw Kotlin→Obj-C interop produces an awkward, un-idiomatic Swift API — most importantly by turning Kotlin `Flow`s into Swift `AsyncSequence` and sealed classes into proper Swift enums.**"},{t:"list",items:["**Flow → Swift `AsyncSequence`** — the most valuable feature. Without SKIE, a Kotlin `Flow` has no native Swift equivalent, so Swift can't just collect it — you'd write manual bridging. SKIE makes it collectable natively with `for await value in flow`. Since reactive streams (StateFlow for UI state) are central to KMP apps, this is huge.","**Sealed classes → Swift enums** — SKIE converts Kotlin sealed hierarchies into real Swift enums, restoring *exhaustive `switch`* (the compiler ensures every case is handled). Raw interop loses this — sealed classes become plain classes needing `is` checks with no exhaustiveness guarantee.","**Better suspend functions, default arguments, and more** — SKIE improves several translations so the shared API feels like a native Swift library rather than an Obj-C one."]},{t:"p",text:"The reason it matters is *adoption and productivity on the iOS side*. Without SKIE, Swift developers consuming your shared Kotlin face an unpleasant API (completion handlers instead of async, manual Flow bridging, no exhaustive enums), which creates friction and resistance to KMP. SKIE makes the shared code *pleasant* to use from Swift — which is important for getting the iOS team on board with a KMP approach. It's essentially become a standard part of the KMP-for-iOS toolchain precisely because the two things it fixes (Flow bridging and sealed-class exhaustiveness) are the two most painful raw-interop issues."}]},{level:"junior",q:"Why can't Swift directly use Kotlin coroutines and Flows?",a:[{t:"p",text:"**Because Swift and Objective-C have no concept of Kotlin coroutines, and the Kotlin→Obj-C bridge can't represent them natively. Coroutines are a Kotlin-specific mechanism (with continuations, structured concurrency, and `Flow` as a reactive stream type) that has no direct equivalent in the Obj-C type system the interop goes through.**"},{t:"list",items:["**Suspend functions** — Kotlin/Native translates a `suspend` function into a *completion-handler-based* function (or, in newer Kotlin, a Swift `async` function). So Swift *can* call it, but it's not the same as Kotlin's structured concurrency — the coroutine machinery doesn't cross the boundary; it's adapted to Swift's callback/async model.","**Flow** — this is the harder one. `Flow`/`StateFlow` are Kotlin reactive stream types with *no* native Swift/Obj-C equivalent. Raw interop exposes a Flow as a class you'd have to manually subscribe to via callbacks — Swift can't just `for await` it or collect it idiomatically. There's no built-in mapping to Swift's `AsyncSequence` or Combine publishers.","**The consequence** — consuming the reactive state that a shared ViewModel exposes (a `StateFlow<UiState>`) from SwiftUI requires *bridging*: either hand-written wrapper classes that subscribe to the Flow and republish, or a tool like SKIE that converts Flows to Swift `AsyncSequence` automatically."]},{t:"p",text:"The practical upshot: because reactive streams (StateFlow for UI state) are how KMP shared ViewModels expose state, and Swift can't natively consume them, the Flow→Swift bridge is *the* central interop concern. This is why SKIE (Flow → AsyncSequence) is so valuable, and why the standard pattern for iOS is a thin Swift `ObservableObject` adapter that collects the shared StateFlow (via SKIE) and republishes into a `@Published` property SwiftUI observes. The mismatch stems from coroutines being a Kotlin language feature that the Obj-C-based interop layer simply has no way to express directly — so it's adapted (suspend → callbacks/async) or requires tooling (Flow → SKIE)."}]},{level:"senior",q:"How do you expose a shared ViewModel's state to SwiftUI, and what lifecycle/threading concerns arise?",a:[{t:"p",text:"**You wrap the shared Kotlin ViewModel in a thin Swift `ObservableObject` adapter that subscribes to the ViewModel's `StateFlow` (as a Swift `AsyncSequence` via SKIE), republishes each emission into a `@Published` property, and lets the SwiftUI view observe *that* — while calling method(s) on the shared ViewModel for user intents (which is a direct, easy Kotlin call). The concerns are lifecycle (the shared VM has no automatic iOS lifecycle) and threading (state must reach SwiftUI on the main thread).**"},{t:"code",title:"The adapter pattern",code:`@MainActor
class ProfileObservable: ObservableObject {
    @Published var state: ProfileUiState = ProfileUiState.Loading()
    private let viewModel: ProfileViewModel

    init(viewModel: ProfileViewModel) {
        self.viewModel = viewModel
        Task {
            for await s in viewModel.uiState {   // SKIE: StateFlow -> AsyncSequence
                self.state = s                    // republish to SwiftUI
            }
        }
    }
    func onRefresh() { viewModel.refresh() }      // intent -> direct Kotlin call
    deinit { viewModel.clear() }                  // lifecycle cleanup
}`},{t:"list",items:["**The bridging pattern**: the shared ViewModel exposes state as `StateFlow<UiState>` (works identically on both platforms). On Android, Compose collects it directly (`collectAsStateWithLifecycle`). On iOS, SwiftUI can't collect a Flow, so the `ObservableObject` adapter collects it (via SKIE's AsyncSequence) and mirrors it into `@Published var state`, which the SwiftUI view observes with `@StateObject`/`@ObservedObject`. *Intents* go the other direction — Swift calls the ViewModel's methods directly (Kotlin→Swift function calls are easy), so `onRefresh()` just invokes `viewModel.refresh()`. The *presentation logic* (the ViewModel) is shared; only this thin adapter is platform-specific.","**Lifecycle concern — no automatic iOS lifecycle**: the shared Kotlin ViewModel has no iOS lifecycle owner managing it (on Android, `viewModelScope` is cleared automatically; iOS has no equivalent). So the adapter must *own* the ViewModel's lifetime — cancel the collection and call `viewModel.clear()` (cancelling `viewModelScope`) on `deinit`, or the ViewModel's coroutines leak. Managing this correctly (creating the VM per screen, clearing on deinit) is a real responsibility that Android handles for you but iOS doesn't.","**Threading concern — main thread for SwiftUI**: SwiftUI requires `@Published` updates on the main thread. The state emissions from the Kotlin Flow must land on the main thread — either the shared VM emits on `Dispatchers.Main`, or the adapter hops to main (`@MainActor` on the class handles this in the example). Getting this wrong causes SwiftUI update warnings/crashes or missed updates.","**Error/exception concern**: uncaught Kotlin exceptions crossing to Swift *crash* the app, so the shared VM should model errors as *state* (an error variant in UiState) or use `@Throws`/Result — never let exceptions propagate across the boundary into Swift."]},{t:"list",items:["**Alternatives/evolution**: libraries like **KMP-ObservableViewModel** or frameworks like **Decompose** (with its lifecycle-aware components) or **MVIKotlin** provide more structured cross-platform state/lifecycle handling, reducing the boilerplate of hand-written adapters. Newer Swift `@Observable` (Observation framework) can replace `ObservableObject`/`@Published`. But the core pattern — collect the shared Flow, republish to SwiftUI, manage lifetime, handle threading — is the same."]},{t:"p",text:"**The senior framing**: the essence of KMP done well is that the *presentation logic* (the ViewModel exposing `StateFlow<UiState>`) is *shared*, and only a *thin, platform-specific UI-binding adapter* differs — Compose collects the flow directly on Android; a Swift `ObservableObject` adapter collects it (via SKIE) and republishes to SwiftUI on iOS. The concerns that arise are precisely the things Android's framework handles *for* you but iOS doesn't: *lifecycle* (you must own the shared VM's lifetime and `clear()` on deinit — no automatic `viewModelScope` clearing) and *threading* (state must reach SwiftUI on the main thread — `@MainActor`/main-dispatched emissions), plus *error handling* (never let Kotlin exceptions cross to Swift — model errors as state). Understanding that the shared-VM-with-per-platform-adapter is the target architecture, that SKIE's Flow→AsyncSequence is what makes the iOS side feasible, and that iOS requires *manual* lifecycle/threading management the shared VM doesn't get for free — is exactly the KMP iOS-integration depth an Android/KMP interviewer probes, and it demonstrates you've actually shipped shared code to iOS, not just written commonMain logic."}]},{level:"senior",q:"What are the main friction points in Kotlin-to-Swift interop, and how do you mitigate them?",a:[{t:"p",text:"**The friction comes from Kotlin features translating imperfectly through the Obj-C bridge (which is less expressive than both Kotlin and Swift): coroutines/Flow have no native Swift mapping, sealed classes lose exhaustiveness, generics are erased, exceptions crash Swift if uncaught, and default arguments/some Kotlin idioms don't survive. The mitigations are SKIE (for the biggest ones), careful API design at the boundary, and modeling errors as values.**"},{t:"list",items:["**Friction — Coroutines/Flow (the biggest)**: `suspend` functions become completion-handler/async functions (usable but not structured-concurrency), and `Flow` has *no* native Swift equivalent, so Swift can't collect it directly. **Mitigation**: use **SKIE**, which turns Flows into Swift `AsyncSequence` (collectable with `for await`) and improves suspend translation — the single most impactful tool. Without SKIE, hand-written wrapper classes subscribing to the Flow.","**Friction — sealed classes lose exhaustiveness**: Kotlin's exhaustive `when` becomes, in raw Swift, `is`-check chains with no compiler guarantee all cases are handled — losing a key safety feature (and risking unhandled cases when you add a variant). **Mitigation**: **SKIE** converts sealed classes to real Swift enums, restoring exhaustive `switch`. This is especially important for UI state (a sealed `UiState`), where you *want* Swift forced to handle every state.","**Friction — generics erased**: Kotlin generics mostly don't survive to Obj-C, so type safety weakens across the boundary (things become `Any`/opaque). **Mitigation**: design the *public* shared API to minimize exposed generics at the boundary — return concrete types or well-defined sealed hierarchies rather than heavily-generic signatures; keep generics *internal* to commonMain where they work fully.","**Friction — exceptions crash Swift**: an uncaught Kotlin exception crossing to Swift *terminates the app* unless the function is `@Throws`-annotated (mapping it to a Swift error). **Mitigation**: *model errors as values* (a `Result` type or a sealed error, or an error variant in UiState) rather than throwing across the boundary — the errors-as-values approach (from clean-architecture error handling) is doubly valuable in KMP because it avoids the exception-crashes-Swift problem entirely. Where you must throw, annotate `@Throws`.","**Friction — default arguments, some idioms**: Obj-C doesn't support default arguments, so Swift must provide all arguments; certain Kotlin constructs (extension functions, some collection types) translate awkwardly. **Mitigation**: design the boundary API to be *Swift-friendly* — explicit parameters, simple types, avoid relying on Kotlin-only conveniences at the public surface; keep the clever Kotlin internal."]},{t:"list",items:["**Overarching mitigation — treat the shared API as a *public product* for Swift consumers**: the shared module's public surface is an API that Swift developers use, so design it deliberately for that audience — SKIE-enhanced (Flow/enums), errors-as-values (no crossing exceptions), concrete boundary types (limit exposed generics), explicit parameters (no default-arg reliance), and a thin, clear interface (not exposing every internal Kotlin detail). Keep the rich Kotlin idioms *internal* to commonMain and expose a clean, Swift-idiomatic boundary.","**Frameworks that structure this**: Decompose, MVIKotlin, and KMP-ObservableViewModel provide patterns/tooling that reduce boilerplate and handle the state/lifecycle bridging more robustly than hand-rolled adapters — worth adopting for larger apps.","**The trend — improving**: the interop story keeps improving (SKIE, better Swift-export in newer Kotlin/Native, the upcoming direct Swift export). So while friction is real *today*, it's diminishing, and the mitigations (SKIE especially) make it manageable in production now."]},{t:"p",text:"**The senior framing**: Kotlin-to-Swift friction is fundamentally the *impedance mismatch* between Kotlin's rich features and the Obj-C bridge (less expressive than either language), concentrated in coroutines/Flow, sealed-class exhaustiveness, generics, and exceptions. The mitigation strategy has two pillars: *tooling* (SKIE, which directly fixes the two worst — Flow→AsyncSequence and sealed→enum) and *deliberate boundary API design* (treat the shared module's public surface as a product for Swift consumers — errors-as-values not exceptions, concrete types not exposed generics, explicit parameters, SKIE-enhanced idioms, and a clean thin interface with clever Kotlin kept internal). The key mindset is that the shared code isn't just 'Kotlin that also runs on iOS' — its *public API* must be *designed for Swift ergonomics*, because a KMP project's success on iOS depends on Swift developers finding the shared code pleasant to use. And the honest note is that this friction is *real but shrinking* (SKIE and evolving Swift-export make it manageable today, and improving). Demonstrating the specific frictions *with* their mitigations, the 'design the boundary as a Swift-facing product' mindset, the errors-as-values connection, and the balanced 'real but improving' assessment is the comprehensive senior answer that shows genuine KMP-on-iOS production experience rather than surface familiarity."}]},{level:"senior",q:"How does the Kotlin-to-Objective-C bridge work?",a:[{t:"p",text:"Kotlin/Native compiles your shared module into an *Objective-C-compatible framework* — it generates an Obj-C *header* exposing your Kotlin classes/functions as Obj-C interfaces, which Swift consumes via its Obj-C interop. So Swift calls Kotlin *through Obj-C* (not directly). This bridge shapes what's exposed: Kotlin types map to Obj-C/Swift equivalents, but *Obj-C's limitations* (no generics richness, no default args, no sealed exhaustiveness, name mangling) leak into the Swift API — which is exactly what tools like SKIE smooth over."},{t:"list",items:["**Kotlin/Native** — outputs an Obj-C-compatible framework.","**Generates** — an Obj-C header of your Kotlin API.","**Swift** — calls Kotlin via Obj-C interop.","**Obj-C limits** — leak into the Swift API (SKIE smooths)."]},{t:"note",text:"Kotlin/Native outputs an Obj-C-compatible framework — generating an Obj-C header exposing Kotlin classes/functions, which Swift consumes via Obj-C interop (Swift → Obj-C → Kotlin). The bridge shapes the API: Obj-C limitations (weak generics, no default args, no sealed exhaustiveness, mangling) leak into Swift — exactly what SKIE smooths over."}]},{level:"senior",q:"How do Kotlin types map to Swift/Objective-C?",a:[{t:"p",text:"Common mappings: Kotlin `String`↔`String`, `Int`/`Long`↔`Int`/`Int64` (boxed as `KotlinInt` in generics), `List`↔`Array`, `Map`↔`Dictionary`, nullable↔optional. But: Kotlin *generics* become loosely-typed (often `Any`/`id`) across the bridge, `sealed class`es lose exhaustiveness (Swift can't `switch` exhaustively), *default parameters* disappear (all args required), `data class` niceties (copy/componentN) aren't exposed, and companion objects/enums map awkwardly. Design shared APIs aware of this — prefer simple, explicit signatures, and use SKIE to restore sealed/Flow ergonomics."},{t:"list",items:["**Basics** — String/collections/nullable map cleanly.","**Generics** — lose type info across the bridge.","**Sealed classes** — no Swift exhaustiveness.","**Default args/data-class niceties** — not exposed."]},{t:"note",text:"Mappings: String/Int/List/Map/nullable map to Swift equivalents (Int boxed as KotlinInt in generics). But Kotlin generics lose type info, sealed classes lose exhaustiveness, default parameters disappear (all args required), and data-class copy/componentN aren't exposed. Design shared APIs with simple explicit signatures; use SKIE to restore sealed/Flow ergonomics."}]},{level:"junior",q:"How do you integrate the shared framework into an Xcode project?",a:[{t:"p",text:"Three common ways: *CocoaPods* (the KMP CocoaPods Gradle plugin publishes the shared module as a pod the iOS app depends on — simplest for many teams), *Swift Package Manager* (export the framework as an SPM package/XCFramework), or a *direct framework* (a Gradle task builds the `.framework`/XCFramework, added to Xcode's linked frameworks + a build phase). CocoaPods is popular for tight Gradle integration; XCFramework/SPM is cleaner for distributing a prebuilt binary. In all cases, Swift then `import shared` and calls the Kotlin API."},{t:"list",items:["**CocoaPods** — KMP plugin publishes a pod (common).","**SPM** — export as an XCFramework/package.","**Direct framework** — Gradle builds `.framework`, link in Xcode.","**Then** — Swift `import shared` and call Kotlin."]},{t:"note",text:"Integrate via CocoaPods (KMP CocoaPods Gradle plugin publishes the shared module as a pod — simplest), Swift Package Manager (export as an XCFramework/package), or a direct framework (Gradle builds .framework/XCFramework, linked in Xcode with a build phase). CocoaPods for tight Gradle integration; XCFramework/SPM for a prebuilt binary. Then Swift import shared and calls Kotlin."}]},{level:"senior",q:"What is KMP-NativeCoroutines, and how does it differ from SKIE?",a:[{t:"p",text:"Both solve *consuming Kotlin coroutines/Flows from Swift*. *KMP-NativeCoroutines* is a library where you *annotate* suspend/Flow APIs and it generates Swift-friendly wrappers (async/await, Combine publishers, or an async-sequence), with proper cancellation. *SKIE* is a *compiler plugin* that *transparently* enhances the generated Swift API — Flows become async sequences, suspend functions become Swift `async`, and *sealed classes become exhaustive Swift enums* — with little to no annotation. SKIE is more automatic/broad; NativeCoroutines is explicit/targeted. Both address the coroutine/Flow interop gap."},{t:"list",items:["**Both** — Swift-friendly coroutines/Flows.","**NativeCoroutines** — annotate APIs → generated wrappers.","**SKIE** — compiler plugin, transparent, + sealed→enum.","**SKIE** — broader/automatic; NativeCoroutines targeted."]},{t:"note",text:"Both make Kotlin coroutines/Flows Swift-friendly. KMP-NativeCoroutines: annotate suspend/Flow APIs → generated async/Combine wrappers with cancellation. SKIE: a compiler plugin that transparently turns Flows into async sequences, suspend into Swift async, and sealed classes into exhaustive Swift enums — minimal annotation. SKIE is broader/automatic; NativeCoroutines explicit/targeted."}]},{level:"senior",q:"How do you handle threading when Swift calls shared Kotlin code?",a:[{t:"p",text:"With the *new memory manager*, Kotlin/Native no longer requires freezing, so you can share state and run coroutines across threads much like Android. Still, be deliberate: *suspend functions* called from Swift should complete on a sensible dispatcher, and *UI updates must happen on the main thread* — ensure Flows/results are delivered on Main before Swift updates SwiftUI (tools like SKIE/NativeCoroutines and `MainScope`/`Dispatchers.Main` help). Avoid blocking the main thread with heavy shared work. The mental model is close to Android's main-safety, applied at the Swift boundary."},{t:"list",items:["**New memory model** — no freezing; threads like Android.","**Deliver to Main** — before Swift updates SwiftUI.","**Tools** — SKIE/NativeCoroutines, Dispatchers.Main.","**Avoid** — blocking main with heavy shared work."]},{t:"note",text:"The new memory manager removes freezing — share state/coroutines across threads like Android. Still, deliver Flow/suspend results on the main thread before Swift updates SwiftUI (SKIE/NativeCoroutines, Dispatchers.Main help), and don't block main with heavy shared work. The model is close to Android main-safety, applied at the Swift boundary."}]},{level:"senior",q:"How do you share a ViewModel between Android and iOS?",a:[{t:"p",text:"Put a *shared ViewModel/presenter* in `commonMain` exposing state as a `StateFlow` and actions as functions. Android collects the `StateFlow` in Compose normally. iOS consumes it via *SKIE/NativeCoroutines* (as an async sequence/Combine publisher, or an observable wrapper) and drives SwiftUI. Manage the *lifecycle*: the shared VM's `CoroutineScope` must be cancelled when the screen goes away — on iOS you call a `clear()`/`onCleared` from the view's disappear, since there's no Android `ViewModelStore`. Libraries like *Moko-MVVM* or *Decompose* help standardize this cross-platform."},{t:"list",items:["**Shared VM** — StateFlow state + action functions in common.","**Android** — collect StateFlow in Compose.","**iOS** — SKIE/NativeCoroutines → SwiftUI.","**Lifecycle** — cancel the scope on iOS disappear (no ViewModelStore)."]},{t:"note",text:"Share a ViewModel/presenter in commonMain exposing StateFlow state + action functions. Android collects it in Compose; iOS consumes via SKIE/NativeCoroutines into SwiftUI. Manage lifecycle: cancel the VM's CoroutineScope on iOS view-disappear (no Android ViewModelStore). Moko-MVVM/Decompose help standardize this cross-platform."}]},{level:"junior",q:"Why must UI stay native (SwiftUI) even when logic is shared?",a:[{t:"p",text:"In the *logic-sharing* KMP model, the shared module has *no UI* — each platform builds its own native UI (Compose on Android, SwiftUI on iOS) consuming the shared state/logic. This gives *fully native look, feel, and platform conventions* (navigation, gestures, accessibility) with no rendering compromise, while still guaranteeing consistent behavior via shared logic. (Compose Multiplatform can share UI too, but then iOS renders Compose, not SwiftUI.) So 'shared logic + native UI' deliberately keeps SwiftUI to preserve the best iOS UX."},{t:"list",items:["**Shared module** — no UI in the logic-sharing model.","**Native UI** — Compose + SwiftUI per platform.","**Benefit** — fully native UX + consistent shared behavior.","**Alternative** — CMP shares UI (Compose, not SwiftUI)."]},{t:"note",text:"In the logic-sharing model the shared module has no UI — each platform builds native UI (Compose/SwiftUI) over shared state/logic, giving fully native look/feel/conventions with consistent behavior. (Compose Multiplatform can share UI, but then iOS renders Compose, not SwiftUI.) 'Shared logic + native UI' keeps SwiftUI to preserve the best iOS UX."}]},{level:"senior",q:"How do you expose sealed classes to Swift usefully?",a:[{t:"p",text:"By default, a Kotlin *sealed class* crosses the bridge as a set of Obj-C classes *without exhaustiveness* — Swift can't `switch` over it with compiler-checked completeness (you'd use `if let ... as?` chains and a fallback). *SKIE* fixes this: it generates an *exhaustive Swift enum* for sealed classes/interfaces, so Swift gets a real `switch` with `case` per subtype and compile-time completeness. Without SKIE, expose a simpler API (e.g. an enum-like discriminator + accessors) to make consumption safe. Sealed-class ergonomics are a top reason teams adopt SKIE."},{t:"list",items:["**Default** — sealed loses Swift exhaustiveness (as? chains).","**SKIE** — generates exhaustive Swift enums with switch.","**Without SKIE** — expose a discriminator + accessors.","**Key reason** — teams adopt SKIE for this."]},{t:"note",text:"By default a Kotlin sealed class crosses as Obj-C classes without exhaustiveness — Swift uses if-let-as? chains + fallback, no checked switch. SKIE generates an exhaustive Swift enum (real switch, compile-time completeness). Without SKIE, expose a simpler discriminator + accessors. Sealed-class ergonomics are a top reason to adopt SKIE."}]},{level:"senior",q:"How do you handle errors/exceptions across the Kotlin-Swift boundary?",a:[{t:"p",text:"Kotlin *unchecked exceptions* don't map to Swift's `throws` automatically — an uncaught Kotlin exception crossing the boundary *crashes* the app. To surface errors to Swift as `throws`, mark functions with *`@Throws`* (Kotlin) so they bridge to Swift error handling for the listed exception types. Better for domain errors: model them *in the return type* (a sealed `Result`/`Either`, or a state with an error field) so Swift handles them as *values*, not exceptions. Reserve `@Throws` for genuinely exceptional cases; prefer typed results across the boundary."},{t:"list",items:["**Unchecked exceptions** — crossing → crash.","**`@Throws`** — bridge listed exceptions to Swift `throws`.","**Prefer** — model errors as return values (Result/sealed).","**Reserve** — `@Throws` for truly exceptional cases."]},{t:"note",text:"Kotlin unchecked exceptions don't map to Swift throws — an uncaught one crossing the boundary crashes. Mark functions @Throws to bridge listed exceptions to Swift error handling. Better: model domain errors in the return type (sealed Result/Either or an error state) so Swift handles them as values. Reserve @Throws for truly exceptional cases; prefer typed results."}]},{level:"junior",q:"What is an XCFramework, and why use it?",a:[{t:"p",text:"An *XCFramework* is Apple's format bundling a framework's binaries for *multiple architectures/platforms* (device arm64, simulator arm64/x64) in one artifact — so a single XCFramework works for both real devices and the simulator. KMP can produce an XCFramework for the shared module, which you distribute (via SPM/CocoaPods or drop-in) to the iOS app. It's the modern way to ship a *prebuilt* shared binary (e.g. so iOS devs don't need the full Kotlin build), versus building the framework from source each time."},{t:"list",items:["**XCFramework** — multi-arch/platform framework bundle.","**One artifact** — device + simulator.","**KMP** — can produce it for the shared module.","**Use** — ship a prebuilt binary (SPM/CocoaPods)."]},{t:"note",text:"An XCFramework bundles a framework's binaries for multiple architectures/platforms (device + simulator) in one artifact. KMP can produce one for the shared module to distribute (SPM/CocoaPods/drop-in) to the iOS app — the modern way to ship a prebuilt shared binary (so iOS devs skip the full Kotlin build) instead of building from source each time."}]},{level:"senior",q:"How do you consume a Kotlin Flow from SwiftUI?",a:[{t:"p",text:"Raw, a `Flow` is hard to consume in Swift (no native collection of a suspend stream). With *SKIE*, a `Flow` becomes a Swift *async sequence* you can `for await` over (or an observable), and a `StateFlow` maps to something SwiftUI can bind. With *KMP-NativeCoroutines*, you get a Combine `Publisher` or async sequence. You then *collect* it in the SwiftUI view (e.g. in a `.task {}` or via an `@Observable`/`ObservableObject` wrapper) and update `@State`/`@Published` — always on the main thread. Cancel collection when the view disappears."},{t:"list",items:["**SKIE** — Flow → Swift async sequence (`for await`).","**NativeCoroutines** — Flow → Combine Publisher/async.","**Collect** — in `.task {}` / an observable wrapper.","**Main thread + cancel** — on view disappear."]},{t:"note",text:"Consume a Flow in SwiftUI via SKIE (Flow → Swift async sequence, for await; StateFlow bindable) or KMP-NativeCoroutines (Combine Publisher/async sequence). Collect it in a .task {} or an @Observable/ObservableObject wrapper, updating @State/@Published on the main thread, and cancel collection when the view disappears."}]},{level:"senior",q:"What performance costs exist at the Kotlin-Swift interop boundary?",a:[{t:"p",text:"Crossing the Obj-C bridge isn't free: each *Swift↔Kotlin call* has overhead, and *object conversions* (Kotlin collections ↔ Swift Array/Dictionary, boxing primitives as `KotlinInt`) cost — so *chatty, fine-grained* interop (thousands of tiny calls, or converting huge collections repeatedly) can become a bottleneck. Mitigate with *coarse-grained* APIs (return a whole result object, not many getters), avoid unnecessary conversions, and don't put per-frame/hot-loop calls across the boundary. Execution *within* shared code is native-fast; the *boundary* is where you design carefully."},{t:"list",items:["**Per-call overhead** — each Swift↔Kotlin call.","**Conversions** — collections/primitives cost.","**Chatty interop** — many tiny calls = bottleneck.","**Mitigate** — coarse APIs, fewer conversions, no hot-loop crossing."]},{t:"note",text:"The Obj-C boundary has costs: per-call overhead and object conversions (collections ↔ Array/Dictionary, boxing as KotlinInt). Chatty fine-grained interop (thousands of tiny calls, repeated huge-collection conversions) can bottleneck. Mitigate with coarse-grained APIs (return whole result objects), fewer conversions, no per-frame/hot-loop crossing. Shared execution is native-fast; design the boundary carefully."}]},{level:"junior",q:"How do iOS developers debug shared Kotlin code?",a:[{t:"p",text:"Debugging spans two worlds: iOS devs debug Swift in *Xcode*, and can set breakpoints in the *shared Kotlin* (Kotlin/Native supports LLDB debugging, so you can step into Kotlin from Xcode with the right setup) — though the experience is less seamless than Android's. Kotlin devs debug shared logic in *Android Studio* (and via `commonTest`). Logging that surfaces on both platforms (a multiplatform logger like Napier/Kermit) helps. In practice, cover shared logic with *tests* (debuggable in AS) so you rarely need to step through Kotlin inside Xcode."},{t:"list",items:["**Xcode** — debug Swift; can breakpoint shared Kotlin (LLDB).","**Android Studio** — debug shared logic + `commonTest`.","**Logging** — multiplatform logger (Kermit/Napier).","**Best** — cover shared logic with tests to avoid stepping."]},{t:"note",text:"iOS devs debug Swift in Xcode and can breakpoint shared Kotlin (Kotlin/Native LLDB support, less seamless than Android). Kotlin devs debug shared logic in Android Studio + commonTest. A multiplatform logger (Kermit/Napier) surfaces logs on both. Best practice: cover shared logic with tests (debuggable in AS) so you rarely step through Kotlin in Xcode."}]},{level:"senior",q:"How do you expose an enum or sealed hierarchy that Swift can switch on safely?",a:[{t:"p",text:"Kotlin `enum class`es map to Obj-C/Swift enums reasonably (Swift can switch, though not always exhaustively without help). For *sealed* hierarchies, use *SKIE* to get exhaustive Swift enums, or design the API so Swift can branch safely: expose a *discriminator property* (a Kotlin enum tag) plus typed accessors, so Swift switches on the tag. Avoid relying on Swift `as?` downcasts without a fallback (a new Kotlin subtype would silently fall through). The goal: Swift should handle *all* cases with compile-time confidence."},{t:"list",items:["**Enums** — map reasonably; Swift can switch.","**Sealed** — SKIE for exhaustive Swift enums.","**Or** — discriminator tag + typed accessors.","**Avoid** — bare `as?` without a fallback."]},{t:"note",text:"Kotlin enum classes map to Swift enums (switchable). For sealed hierarchies, use SKIE for exhaustive Swift enums, or expose a discriminator (enum tag) + typed accessors so Swift switches safely. Avoid bare as? downcasts without a fallback (a new subtype silently falls through). Goal: Swift handles all cases with compile-time confidence."}]},{level:"junior",q:"What is the developer workflow for a KMP team with Android and iOS devs?",a:[{t:"p",text:"Typically: *Kotlin/Android devs* own the shared module (write logic in `commonMain`, test in `commonTest`, work in Android Studio). *iOS devs* consume the shared framework in Xcode and build SwiftUI, occasionally contributing to shared code. The shared module is a *contract* both sides depend on — changes to its API affect both apps, so coordinate (versioning, clear APIs, tests). CI builds the shared module, runs `commonTest`, and produces the iOS framework. Good communication around the shared API surface is key to smooth collaboration."},{t:"list",items:["**Kotlin devs** — own shared module (AS, commonTest).","**iOS devs** — consume the framework, build SwiftUI.","**Shared module** — a contract both depend on.","**CI** — builds shared, runs tests, produces the framework."]},{t:"note",text:"Workflow: Kotlin/Android devs own the shared module (commonMain logic, commonTest, Android Studio); iOS devs consume the framework in Xcode and build SwiftUI, sometimes contributing to shared code. The shared module is a contract both apps depend on — coordinate API changes (versioning, tests). CI builds shared, runs commonTest, produces the iOS framework. Communication around the API surface is key."}]},{level:"senior",q:"How do you keep the shared API Swift-friendly by design?",a:[{t:"p",text:"Design the *public* shared API for the Obj-C bridge: prefer *simple, explicit signatures* (avoid heavy generics that lose type info), *don't rely on default arguments* (provide explicit overloads), model errors as *return values*, expose *coarse-grained* functions (fewer boundary calls), keep names clear (Obj-C mangling can produce ugly Swift), and use *SKIE* to restore Flow/sealed/async ergonomics. Treat the shared API like a *published SDK for Swift consumers* — internal Kotlin can be idiomatic, but the exposed surface should be pleasant and safe in Swift."},{t:"list",items:["**Simple signatures** — avoid type-losing generics.","**Explicit overloads** — no reliance on default args.","**Errors as values; coarse APIs** — fewer boundary calls.","**SKIE** — restore Flow/sealed/async; treat as an SDK."]},{t:"note",text:"Design the public shared API for the Obj-C bridge: simple explicit signatures (avoid type-losing generics), explicit overloads (no default-arg reliance), errors as return values, coarse-grained functions (fewer boundary calls), clear names (avoid ugly mangling), and SKIE for Flow/sealed/async. Treat the exposed surface as a published SDK for Swift — internal Kotlin can be idiomatic, the API should be Swift-pleasant."}]},{level:"senior",q:"How do you manage dependency injection across the Kotlin-Swift boundary?",a:[{t:"p",text:"Use a *multiplatform DI* (Koin, or manual factory) in `commonMain` to wire shared dependencies. Expose a *single entry point* (e.g. a `KoinComponent`/factory object or a small facade) that iOS calls to obtain shared objects, rather than iOS constructing internal graph pieces. Platform-specific dependencies (an iOS keychain, a URL session) are provided via `expect`/`actual` or injected from the platform side into the shared graph at startup. Keep the graph *initialized once* (call an `initKoin()` from both apps' startup). Swift shouldn't manage Kotlin's internal DI — give it a clean facade."},{t:"list",items:["**Multiplatform DI** — Koin/manual in commonMain.","**Single entry point** — facade for iOS to get shared objects.","**Platform deps** — via expect/actual or injected at startup.","**Init once** — `initKoin()` from both apps."]},{t:"note",text:"Use multiplatform DI (Koin/manual) in commonMain, exposing a single entry point/facade iOS calls to obtain shared objects (don't have iOS build internal graph pieces). Provide platform deps (keychain, URL session) via expect/actual or inject them into the shared graph at startup. Initialize once (initKoin() from both apps). Give Swift a clean facade, not Kotlin's internal DI."}]},{level:"junior",q:"Can iOS use Jetpack Compose through KMP?",a:[{t:"p",text:"Yes — via *Compose Multiplatform*, which now supports iOS (stable): you write *shared Compose UI* that renders on iOS (through Skia, in a `UIViewController` you embed in SwiftUI/UIKit). This shares the UI layer, not just logic. But it means iOS shows *Compose-rendered* UI, not native SwiftUI — a trade-off in native feel and access to the latest SwiftUI components. Teams use it when maximal UI sharing outweighs native-UI purity; others keep native SwiftUI and share only logic. So iOS *can* use Compose, but it's an explicit choice."},{t:"list",items:["**Compose Multiplatform** — shared Compose UI on iOS (stable).","**Renders** — via Skia in a UIViewController.","**Trade-off** — Compose UI, not native SwiftUI.","**Choice** — max UI sharing vs native purity."]},{t:"note",text:"Yes — Compose Multiplatform supports iOS (stable): shared Compose UI renders on iOS via Skia (in a UIViewController embedded in SwiftUI/UIKit), sharing the UI layer. Trade-off: iOS shows Compose-rendered UI, not native SwiftUI (feel/latest-components). Use when maximal UI sharing outweighs native purity; otherwise keep SwiftUI and share only logic. Compose on iOS is an explicit choice."}]},{level:"senior",q:"How do you handle iOS-specific platform code within a KMP module?",a:[{t:"p",text:"Put it in `iosMain` (or a shared `appleMain`), where you *can call Apple platform APIs directly* — Kotlin/Native exposes *UIKit/Foundation* etc. via platform bindings (e.g. `platform.Foundation.NSDate`, `platform.UIKit`). Use `expect`/`actual` to give common code an implementation backed by these iOS APIs (e.g. `actual` reading the iOS keychain or `NSUserDefaults`). For C/Obj-C libraries without bindings, use *cinterop*. So iOS platform code lives in the iOS source set, calling Apple APIs, and satisfies `expect` declarations from common."},{t:"list",items:["**`iosMain`/`appleMain`** — call Apple APIs directly.","**Bindings** — `platform.Foundation`/`platform.UIKit`.","**`actual`** — iOS-backed impls (keychain, NSUserDefaults).","**cinterop** — for unbound C/Obj-C libraries."]},{t:"note",text:"iOS-specific code goes in iosMain/appleMain, calling Apple APIs directly via Kotlin/Native bindings (platform.Foundation.NSDate, platform.UIKit). Use expect/actual so common declares and iosMain implements (keychain, NSUserDefaults). For unbound C/Obj-C libraries, use cinterop. iOS platform code lives in the iOS source set, calls Apple APIs, and satisfies common's expect declarations."}]},{level:"junior",q:"What are the main friction points of KMP iOS interop today?",a:[{t:"p",text:"Key frictions: *coroutines/Flows* aren't natively Swift-consumable (need SKIE/NativeCoroutines); *sealed classes* lose exhaustiveness; *generics* degrade across the bridge; *default arguments* and some Kotlin niceties don't survive; *debugging* shared code in Xcode is less smooth; the *two-IDE* workflow (Android Studio + Xcode); and *build/tooling* setup (framework export, CocoaPods) has a learning curve. Most are *mitigated* (SKIE, better tooling, the new memory model), and the trajectory is steadily improving — but they're the realistic rough edges to plan for."},{t:"list",items:["**Coroutines/Flows** — need SKIE/NativeCoroutines.","**Sealed/generics/default args** — degrade across the bridge.","**Debugging + two-IDE** — less smooth.","**Mitigated** — SKIE, tooling, new memory model; improving."]},{t:"note",text:"KMP iOS interop frictions: coroutines/Flows not natively Swift-consumable (SKIE/NativeCoroutines), sealed classes lose exhaustiveness, generics degrade, default args/niceties don't survive, Xcode debugging of shared code is rough, the two-IDE workflow, and build/tooling setup. Most are mitigated (SKIE, tooling, new memory model) and steadily improving — realistic rough edges to plan for."}]},{level:"senior",q:"How do you version and distribute the shared framework to the iOS team?",a:[{t:"p",text:"Two models: *build from source* (the iOS build runs the Gradle framework task each time — always current, but iOS devs need the Kotlin toolchain and pay build time) or *distribute a prebuilt binary* (publish a versioned *XCFramework* via SPM/CocoaPods/a binary repo — iOS devs pull a release, faster, no Kotlin build, but you manage versioning/publishing). Larger teams often prefer prebuilt binaries with *semantic versions* and a *changelog*, treating the shared module like an internal SDK. Match the model to team size and how often the shared API changes."},{t:"list",items:["**Build from source** — always current; needs Kotlin toolchain.","**Prebuilt binary** — versioned XCFramework via SPM/CocoaPods.","**Prebuilt** — faster, no Kotlin build, manage versioning.","**Larger teams** — semantic versions + changelog (like an SDK)."]},{t:"note",text:"Distribute the shared framework by building from source (iOS build runs the Gradle task each time — current, but needs the Kotlin toolchain + build time) or a prebuilt binary (versioned XCFramework via SPM/CocoaPods/binary repo — faster, no Kotlin build, you manage versioning). Larger teams prefer prebuilt with semantic versions + changelog, treating shared as an internal SDK. Match to team size + API churn."}]},{level:"junior",q:"What is Skie's effect on the generated Swift API, concretely?",a:[{t:"p",text:"Concretely, SKIE transforms the auto-generated Swift API to feel *idiomatic*: Kotlin `suspend` functions become Swift `async` functions (awaitable, cancellable); `Flow`/`StateFlow` become Swift *async sequences* (and observable-friendly); *sealed classes/interfaces* become *exhaustive Swift enums* (real `switch`); and it improves *default-argument* handling and some type mappings. The net effect: iOS devs consume the shared module almost like a native Swift library, instead of wrestling with raw Obj-C-bridged signatures. It's a compiler plugin, so it works largely transparently."},{t:"list",items:["**suspend** → Swift `async` (awaitable, cancellable).","**Flow** → Swift async sequence.","**sealed** → exhaustive Swift enum.","**Net** — consume shared like a native Swift library."]},{t:"note",text:"SKIE makes the generated Swift API idiomatic: suspend → Swift async (awaitable/cancellable), Flow/StateFlow → Swift async sequences (observable-friendly), sealed classes → exhaustive Swift enums (real switch), plus better default-arg/type handling. Net: iOS devs consume the shared module almost like a native Swift library, not raw Obj-C-bridged signatures. It's a transparent compiler plugin."}]},{level:"junior",q:"What is CocoaPods integration in a KMP project?",a:[{t:"p",text:"The *Kotlin CocoaPods Gradle plugin* lets your shared module be consumed as a *CocoaPod* by the iOS app: you configure the `cocoapods { }` block (name, version, framework name), and Gradle generates a *podspec* so the iOS `Podfile` can depend on `pod 'shared'`. Running `pod install` wires the framework into Xcode, and the plugin rebuilds it as part of the iOS build. It's a popular integration path because it fits existing iOS dependency workflows and keeps the shared framework current. It also lets the shared module *depend on* other pods if needed."},{t:"list",items:["**CocoaPods plugin** — shared module as a pod.","**`cocoapods { }`** — generates a podspec.","**iOS Podfile** — `pod 'shared'`; `pod install` wires Xcode.","**Popular** — fits existing iOS dependency workflows."]},{t:"note",text:"The Kotlin CocoaPods Gradle plugin makes the shared module a CocoaPod: configure cocoapods { } (name/version/framework), Gradle generates a podspec, the iOS Podfile does pod 'shared', pod install wires Xcode, and the plugin rebuilds the framework in the iOS build. Popular because it fits existing iOS dependency workflows and can let shared depend on other pods."}]},{level:"senior",q:"How do you handle cancellation of shared coroutines from Swift?",a:[{t:"p",text:"A Kotlin `suspend`/`Flow` launched for Swift must be *cancellable* so leaving a screen doesn't leak work. *SKIE* maps suspend to Swift `async` and Flow to async sequences that respect *Swift task cancellation* — cancelling the Swift `Task` cancels the underlying Kotlin coroutine. *KMP-NativeCoroutines* returns a cancellable handle/Combine cancellable you dispose. Without these, you'd manually expose a `Job`/`cancel()` for Swift to call on view disappear. The principle: tie the shared coroutine's lifecycle to the *SwiftUI view's lifecycle* (cancel on disappear) to avoid leaks — just like structured concurrency on Android."},{t:"list",items:["**Cancellable** — so leaving a screen doesn't leak.","**SKIE** — Swift Task cancellation → cancels the coroutine.","**NativeCoroutines** — cancellable handle/Combine cancellable.","**Tie** — coroutine lifecycle to the SwiftUI view."]},{t:"note",text:"Make shared suspend/Flow cancellable so leaving a screen doesn't leak. SKIE ties Swift Task cancellation to the underlying Kotlin coroutine; KMP-NativeCoroutines returns a cancellable handle/Combine cancellable to dispose. Otherwise expose a Job/cancel() for Swift to call on disappear. Tie the coroutine lifecycle to the SwiftUI view's lifecycle — like structured concurrency on Android."}]},{level:"senior",q:"How do you test the iOS integration of shared code?",a:[{t:"p",text:"Layered: cover shared *logic* with `commonTest` (runs on all targets, including the iOS/Native target — so it validates the code compiles and behaves on Native, not just JVM). Add *iosTest* for iOS-specific `actual`s. Then test the *integration* from the *Swift side* (XCTest calling the shared framework) to verify the *bridged API* works as expected (coroutines/Flows via SKIE, error mapping). Run the Native `commonTest` in CI to catch Native-only issues early (some code compiles on JVM but not Native). This catches interop regressions before iOS devs hit them."},{t:"list",items:["**`commonTest` on Native** — validates behavior + Native compilation.","**`iosTest`** — iOS-specific `actual`s.","**Swift XCTest** — the bridged API from the iOS side.","**CI** — run Native tests to catch Native-only issues."]},{t:"note",text:"Test iOS integration in layers: commonTest (runs on the Native target too — validates behavior + that code compiles on Native), iosTest for iOS-specific actuals, and Swift XCTest calling the shared framework to verify the bridged API (SKIE Flows, error mapping). Run Native commonTest in CI (some code compiles on JVM but not Native) to catch interop regressions before iOS devs hit them."}]}],MT={id:"ios-interop",title:"iOS Interop",tagline:"Kotlin/Native → Obj-C framework, the interop pain points (coroutines/Flow, sealed classes, generics, exceptions), SKIE, and bridging state to SwiftUI.",tags:["KMP","iOS","SKIE","Swift Interop","Kotlin/Native"],content:TT,qa:IT},PT=[{heading:"The multiplatform library ecosystem",blocks:[{t:"p",text:"Shared `commonMain` code can only use *multiplatform* libraries (ones that provide implementations for all your targets). Fortunately, the essential ones for a data/logic layer are all mature and multiplatform — the ecosystem for the *logic* layer (which is what KMP shares) is solid. Knowing the standard KMP library stack is important for an Android/KMP engineer."},{t:"table",headers:["Need","Multiplatform library"],rows:[["Concurrency","kotlinx.coroutines (Flow, coroutines — multiplatform)"],["Networking","Ktor Client (multiplatform HTTP client)"],["Serialization","kotlinx.serialization (compile-time, multiplatform)"],["Local database","SQLDelight (or Room KMP)"],["Key-value storage","multiplatform-settings, or DataStore (multiplatform)"],["Dependency injection","Koin (multiplatform)"],["Date/time","kotlinx-datetime"],["ViewModel / lifecycle","androidx.lifecycle (multiplatform since 2.8)"]]},{t:"list",items:["**These cover a full data/domain/presentation layer in commonMain**: coroutines/Flow for async, Ktor for networking, kotlinx.serialization for JSON, SQLDelight for the database, Koin for DI, and the multiplatform ViewModel for presentation. You can build the entire non-UI app in shared code with these.","**Why these specifically** — they're *reflection-free* and *pure Kotlin* where possible (kotlinx.serialization is compile-time; Ktor is coroutine-native), so they work on Kotlin/Native (iOS), unlike JVM-only libraries (Retrofit, Gson, Moshi, Hilt) that *can't* go in commonMain.","**The Android-only equivalents don't work in commonMain** — Retrofit (JVM/Android), Gson/Moshi (reflection, JVM), Hilt (Android). So the KMP stack *substitutes*: Ktor for Retrofit, kotlinx.serialization for Gson/Moshi, Koin for Hilt. Knowing these substitutions is a common KMP interview point."]}]},{heading:"Key libraries in depth",blocks:[{t:"list",items:["**Ktor Client** — the multiplatform HTTP client (covered in Networking). Pluggable engines per platform (OkHttp on Android, Darwin on iOS), coroutine-native, ContentNegotiation with kotlinx.serialization. Your entire networking layer lives in commonMain.","**SQLDelight** — a multiplatform database library that generates *type-safe Kotlin APIs from SQL* (you write `.sq` SQL files, it generates Kotlin). Works via platform database drivers (Android SQLite driver, native driver on iOS) provided per-platform. The KMP alternative to Room (though Room now has experimental KMP support too).","**kotlinx.serialization** — compile-time, reflection-free JSON (and other formats) — works everywhere including Kotlin/Native, unlike Gson/Moshi. The KMP serialization standard.","**Koin** — runtime DI that's multiplatform, so your DI graph is defined in commonMain and started per platform. The KMP DI choice (Hilt is Android-only). Some teams use manual DI in shared code instead.","**androidx.lifecycle (ViewModel)** — since 2.8, `ViewModel`, `viewModelScope`, and (later) `SavedStateHandle` are multiplatform, so you can put ViewModels in commonMain and share presentation logic. Before this, teams used moko-mvvm or hand-rolled expect/actual ViewModels."]}]},{heading:"Compose Multiplatform — sharing the UI too",blocks:[{t:"p",text:"**Compose Multiplatform (CMP)** extends Jetpack Compose to *other platforms* — you can write your UI *once* in Compose and run it on Android, iOS, desktop, and web. This goes beyond KMP's default 'share logic, keep native UI': with CMP you share the *UI too*, moving KMP closer to the Flutter/RN model (one UI codebase) — but using Compose, which Android developers already know."},{t:"list",items:["**How it relates to KMP** — CMP is a UI framework *built on* KMP. Plain KMP shares logic and keeps native UIs (SwiftUI on iOS); CMP lets you *also* share the Compose UI across platforms, so even iOS runs your Compose code (rendered via Skia, not native UIKit components).","**Maturity by platform** — CMP is *stable* on Android (it's just Compose) and *desktop*; **iOS** reached stable in 2025 (production-ready but younger); web (Wasm) is still maturing. So the 'share the UI on iOS too' story is now viable but newer than the logic-sharing story.","**The tradeoff — same as Flutter/RN's UI question**: sharing the UI via CMP maximizes code sharing (one Compose UI everywhere) but the iOS UI is *Compose-rendered* (via Skia), not native SwiftUI/UIKit — so it may not feel perfectly native or use the latest iOS-native UI features, similar to the Flutter tradeoff. Teams choosing CMP-on-iOS accept a Compose UI on iOS for the code-sharing benefit.","**The spectrum** — KMP gives you a *choice* on the sharing spectrum: share *only logic* (native UIs — the conservative, native-feel-preserving default), or *also share the UI* (Compose Multiplatform — maximum sharing, Compose-rendered iOS UI). You can even mix (CMP for some screens, native for others). This flexibility — pick your sharing level per need — is a distinctive KMP strength."]}]},{heading:"Project structure and the full picture",blocks:[{t:"code",title:"A typical KMP project layout",code:`myproject/
  shared/                    // KMP module — the shared logic (and maybe UI)
    src/commonMain/          // repositories, use cases, models, networking, ViewModels
    src/androidMain/         // Android actuals (db driver, etc.)
    src/iosMain/             // iOS actuals
  androidApp/                // Android app — Compose UI, depends on :shared
  iosApp/                    // Xcode project — SwiftUI (or CMP), imports shared framework
  // (multi-module: shared:domain, shared:data, shared:feature-x, umbrella for iOS)`},{t:"list",items:["**The shape** — a `shared` module (commonMain + platform source sets) consumed by an `androidApp` (Gradle dependency, Compose UI) and an `iosApp` (Xcode, the shared framework, SwiftUI or CMP UI).","**Multi-module KMP** — larger apps modularize the shared code (`shared:domain`, `shared:data`, `shared:feature-x`), with an *umbrella* module exporting them into one iOS framework — combining KMP with the modularization patterns from that topic.","**Build with Gradle** — the Kotlin Multiplatform plugin configures targets, source sets, and dependencies; the iOS app builds the framework as part of its Xcode build (via a Gradle task)."]},{t:"note",text:"KMP libraries: commonMain uses MULTIPLATFORM libs — coroutines/Flow, Ktor (networking, replaces Retrofit), kotlinx.serialization (replaces Gson/Moshi), SQLDelight or Room-KMP (database), Koin (DI, replaces Hilt), kotlinx-datetime, androidx ViewModel (multiplatform since 2.8). JVM-only libs (Retrofit/Gson/Hilt) can't go in commonMain — hence the substitutions. Compose Multiplatform: share the UI too (Compose on Android/iOS/desktop/web) — stable on Android/desktop, iOS stable 2025, web maturing; iOS UI is Compose-rendered (Skia), not native — the Flutter-like tradeoff. KMP lets you choose the sharing level: logic-only (native UI) or also-UI (CMP)."}]}],DT=[{level:"junior",q:"What libraries do you use for networking, serialization, and DI in shared KMP code?",a:[{t:"p",text:"**In shared `commonMain` code you must use *multiplatform* libraries, so the Android-only defaults are replaced with multiplatform equivalents: Ktor (networking) instead of Retrofit, kotlinx.serialization (JSON) instead of Gson/Moshi, and Koin (DI) instead of Hilt.** These substitutions are a common KMP interview point."},{t:"list",items:["**Networking — Ktor Client** (replaces Retrofit): a multiplatform, coroutine-native HTTP client with pluggable engines per platform (OkHttp on Android, Darwin on iOS). Retrofit is JVM/Android-only, so it can't go in commonMain.","**Serialization — kotlinx.serialization** (replaces Gson/Moshi): compile-time and reflection-free, so it works on Kotlin/Native (iOS). Gson and Moshi use reflection and are JVM-only, so they don't work in shared code.","**DI — Koin** (replaces Hilt): a multiplatform runtime DI library, so the DI graph lives in commonMain and starts per platform. Hilt is Android-only (compile-time codegen, JVM). Some teams use manual DI in shared code instead."]},{t:"p",text:"The reason for these substitutions is that shared code compiles for *all* targets including Kotlin/Native (iOS), so it can only use libraries that provide implementations for every target. The JVM-only libraries (Retrofit, Gson, Moshi, Hilt) rely on JVM features (reflection, JVM bytecode) that don't exist on Kotlin/Native. The good news is the multiplatform equivalents are mature — Ktor, kotlinx.serialization, and Koin are production-ready — plus coroutines/Flow, SQLDelight (database), and the multiplatform androidx ViewModel. Together they let you build a complete data/domain/presentation layer entirely in commonMain."}]},{level:"junior",q:"What is Compose Multiplatform and how does it relate to KMP?",a:[{t:"p",text:"**Compose Multiplatform (CMP) extends Jetpack Compose to run on other platforms — Android, iOS, desktop, and web — so you can write your UI *once* in Compose and share it across platforms. It's a UI framework *built on* KMP.** This goes beyond KMP's default 'share logic, keep native UI' by letting you share the *UI too*."},{t:"list",items:["**The relationship**: KMP shares *logic* (and by default each platform keeps its native UI — SwiftUI on iOS, Compose on Android). CMP is a layer that lets you *also* share the Compose UI, so even iOS runs your Compose code — moving KMP closer to the Flutter/RN 'one UI codebase' model, but using Compose (which Android developers already know) instead of Dart/JS.","**How the iOS UI renders**: CMP renders the Compose UI via Skia (a graphics engine), *not* native UIKit/SwiftUI components. So the UI is Compose-drawn, similar to how Flutter draws its own UI.","**Maturity**: CMP is stable on Android (it's just Compose) and desktop; iOS reached stable in 2025 (production-ready but younger); web (Wasm) is still maturing."]},{t:"p",text:"The key thing CMP gives you is a *choice on the sharing spectrum*: you can share *only logic* (native UIs — the conservative default that preserves perfect native feel) or *also share the UI* via CMP (maximum code sharing, but a Compose-rendered iOS UI). This is a distinctive KMP strength — you pick your sharing level, and can even mix (CMP for some screens, native SwiftUI for others). The tradeoff for CMP-on-iOS is the same as Flutter's UI question: the iOS UI is Compose-rendered rather than native, so it may not feel perfectly native or use the latest native UI features — teams accept that for the code-sharing benefit. For an Android developer, CMP is attractive because it's *Compose* — the same UI toolkit you already use."}]},{level:"junior",q:"Why can't you use Retrofit or Hilt in commonMain?",a:[{t:"p",text:"**Because Retrofit and Hilt are JVM/Android-only — they rely on features (JVM reflection, dynamic proxies, JVM bytecode) that don't exist on Kotlin/Native (iOS). Shared `commonMain` code compiles for *all* targets including iOS, so it can only use libraries that work on *every* target.**"},{t:"list",items:["**Retrofit** — built on JVM dynamic proxies (it generates the API implementation from your interface at runtime using reflection) and OkHttp (JVM). None of that works on Kotlin/Native. So networking in shared code uses **Ktor** instead (pure Kotlin, coroutine-native, pluggable per-platform engines).","**Hilt** — a compile-time code-generation DI framework tied to the JVM/Android (Android components, JVM). It has no Kotlin/Native support. So shared-code DI uses **Koin** (multiplatform runtime DI) or manual DI.","**Gson/Moshi** — reflection-based (JVM) serialization, so they also don't work in commonMain; **kotlinx.serialization** (compile-time, no reflection) is the multiplatform replacement."]},{t:"p",text:"The general principle is that commonMain code runs on *every* target, so any library it uses must provide implementations for all of them. JVM-specific libraries (relying on reflection, dynamic proxies, or JVM-only APIs) can't satisfy Kotlin/Native, so they're excluded from shared code. This is why the KMP stack has a distinct set of *substitutions* for the familiar Android libraries — Ktor for Retrofit, kotlinx.serialization for Gson/Moshi, Koin for Hilt, SQLDelight for Room. Note that these Android-only libraries can *still* be used in the `androidMain` source set or the Android app module (which target only the JVM) — the restriction is specifically that they can't go in *common* code. So a common pattern is: shared logic in commonMain with the multiplatform stack, and any Android-specific needs in androidMain."}]},{level:"senior",q:"How would you architect a KMP project to maximize meaningful code sharing?",a:[{t:"p",text:"**I'd apply clean architecture with a framework-free, dependency-injected design so the domain, data, and presentation layers live in `commonMain` (shared), with only the UI and thin platform primitives platform-specific — modularized for larger apps, and using the multiplatform library stack throughout. The key is that *good architecture is what enables sharing*: the same separation that makes code testable makes it shareable.**"},{t:"list",items:["**Domain layer in commonMain**: pure-Kotlin domain models, use cases, and repository *interfaces* — no platform dependencies, so it drops into commonMain unchanged. This is the most shareable layer and the most valuable to share (business logic must not diverge between platforms).","**Data layer in commonMain**: repository *implementations* using the multiplatform stack — Ktor (networking), kotlinx.serialization (JSON), SQLDelight (database), DataStore/multiplatform-settings (prefs). Platform-specific pieces (the database *driver*, secure storage) are provided per-platform via `expect/actual` or injected interfaces. So the entire data layer — the caching, SSOT, offline logic — is shared.","**Presentation layer in commonMain**: ViewModels (using the multiplatform androidx lifecycle ViewModel) exposing `StateFlow<UiState>`. Share the state-management logic; only the thin UI-binding adapter differs per platform (Compose collects the flow on Android; a SwiftUI `ObservableObject` adapter on iOS). This shares even the presentation logic.","**UI platform-specific (or CMP)**: native Compose on Android and SwiftUI on iOS (logic-only sharing, native feel), *or* Compose Multiplatform to share the UI too — a per-project choice based on whether native iOS feel or maximum sharing matters more.","**Platform primitives via expect/actual or DI**: the thin native layer — database driver, secure storage, platform info, date formatting — using `expect/actual` for simple primitives and injected interfaces for testable/complex ones."]},{t:"list",items:["**Modularize for larger apps**: split shared code into `shared:domain`, `shared:data`, `shared:feature-x` modules (applying the modularization patterns), with an *umbrella* module exporting them into a single iOS framework (multi-framework consumption breaks type identity). This combines KMP with clean module boundaries.","**DI with Koin (or manual)**: define the DI graph in commonMain (Koin modules) and start it per platform (Android Application, iOS init) — so the wiring is shared too. On Android you might *also* use Hilt at the app layer, bridging to the shared Koin graph.","**The architecture-enables-sharing insight**: the discipline that makes code *testable* (framework-free logic, dependency inversion through interfaces, injected dispatchers, no platform coupling) is *exactly* what makes it *shareable* — a well-architected Android app is already ~80% of the way to KMP. So investing in clean architecture pays off doubly. Conversely, code tangled with `android.*` or static platform calls can't be shared, so refactoring toward clean architecture is often the first step in KMP adoption.","**Share incrementally**: don't rewrite everything at once — start by sharing a high-value slice (the data layer, or one feature's full stack), prove it, and expand. This de-risks adoption and lets you migrate an existing app gradually."]},{t:"p",text:"**The senior framing**: architecting for maximum *meaningful* sharing in KMP is really about applying *clean architecture* rigorously — framework-free domain/data/presentation layers, dependencies inverted through interfaces, the multiplatform library stack (Ktor/serialization/SQLDelight/Koin/lifecycle) — so those layers naturally live in commonMain, with only genuinely platform-specific code (UI, native primitives) per-platform via `expect/actual` or DI. The unifying insight is that *testable design and shareable design are the same thing*: the separation-of-concerns and dependency-injection that make code testable are precisely what let it compile and run on every target, so good architecture is the enabler of sharing (and a well-architected Android app is most of the way to KMP already). For scale, you combine this with modularization (shared sub-modules + iOS umbrella framework) and share the DI graph too. And the pragmatic wisdom is *incremental adoption* — share a high-value slice first and expand — plus a deliberate choice about *how far up the stack* to share (logic-only with native UIs, or also-UI with CMP). The metric throughout is *meaningful* sharing (eliminating real duplication/divergence in the complex core), not maximizing shared-line count. Demonstrating the clean-architecture-enables-sharing principle, the layer-by-layer sharing plan with the multiplatform stack, the modularization-for-scale point, and the incremental/deliberate adoption approach is the comprehensive senior answer that an Android/KMP interviewer specifically wants — it shows you can *lead* a KMP adoption, not just write commonMain code."}]},{level:"senior",q:"How do you handle a KMP project's build, iOS integration, and multi-module structure?",a:[{t:"p",text:"**The build is Gradle-based (Kotlin Multiplatform plugin declaring targets and source sets), the iOS app consumes the shared code as a framework built via a Gradle task in the Xcode build, and multi-module projects use an *umbrella* module to export several shared modules into one iOS framework. The complexity concentrates at the iOS boundary and the multi-module framework packaging.**"},{t:"list",items:["**Gradle build with the KMP plugin**: the shared module's `build.gradle.kts` declares *targets* (`androidTarget()`, `iosX64()`/`iosArm64()`/`iosSimulatorArm64()`, maybe `jvm()`/`js()`), configures *source set dependencies* (common dependencies available to all, platform dependencies per source set), and the *framework* output for iOS. Android consumes the shared module as a normal Gradle dependency; the multiplatform plugin handles compiling each target appropriately (JVM bytecode, Kotlin/Native binaries).","**iOS integration**: the shared module compiles into an Obj-C framework/XCFramework. The Xcode project consumes it — historically via a Gradle build-phase script (a 'run script' phase that invokes a Gradle task to build the framework), or more modern setups via CocoaPods integration or direct XCFramework/SPM. So building the iOS app triggers the Kotlin framework build as part of the Xcode build. This coupling (Gradle ↔ Xcode) is the main integration complexity, and Kotlin/Native compile times (LLVM) can make iOS builds slower — a real consideration.","**Multi-module structure and the umbrella framework**: larger apps split shared code into multiple KMP modules (`shared:domain`, `shared:data`, `shared:feature-x`). But iOS wants *one* framework, and consuming *multiple* Kotlin frameworks directly *breaks type identity* (the same Kotlin class exported by two frameworks becomes two incompatible Obj-C classes). So you add an **umbrella module** that depends on and `export`s the others, producing a *single* XCFramework the iOS app imports — one framework, consistent types. You *curate* what the umbrella exports (each exported module grows the binary and iOS compile time — export only what iOS needs, and keep the iOS-facing API in exported modules)."]},{t:"list",items:["**Source set hierarchy**: use intermediate source sets (e.g. `appleMain` shared by iOS variants, or a `commonMain` shared by all) to avoid duplicating code across the multiple iOS target variants (x64 simulator, arm64 device, arm64 simulator). The Kotlin plugin's default hierarchy template handles common cases.","**Build performance and tooling**: Kotlin/Native compilation is the slow part of iOS builds — mitigate with the usual Gradle caching/config-cache and by keeping the exported iOS surface lean. Tooling like KMMBridge (Touchlab) helps distribute pre-built frameworks so iOS developers don't rebuild the Kotlin every time. SKIE runs on the umbrella framework to enhance the Swift API.","**CI considerations**: CI must build both the Android artifact (Gradle) and the iOS framework (Gradle + Xcode), which needs a macOS runner for the iOS side. Distributing pre-built frameworks (KMMBridge) can decouple iOS developers from the Kotlin build, improving their workflow."]},{t:"p",text:"**The senior framing**: a KMP project's build is fundamentally *Gradle orchestrating multiple compilation targets* (JVM for Android, Kotlin/Native for iOS, etc.), with the notable complexity at the *iOS boundary* — the shared code compiles to a framework consumed by Xcode (via a Gradle build phase / CocoaPods / SPM), coupling the two build systems and introducing Kotlin/Native's slower compile times. The multi-module wrinkle is that iOS wants *one* framework, so you use an *umbrella module* to `export` several shared modules into a single XCFramework (avoiding the type-identity-breaking multi-framework problem), *curating* the export surface to control binary size and iOS build time. Around this, you use source-set hierarchies to avoid duplication across iOS target variants, mitigate build performance (caching, lean exports, KMMBridge for pre-built framework distribution), run SKIE on the umbrella for Swift ergonomics, and set up CI with macOS runners for the iOS build. Understanding that the build complexity concentrates at the Gradle↔Xcode framework boundary and the multi-module umbrella packaging — and knowing the mitigations (umbrella export curation, source-set hierarchy, KMMBridge, build caching) — is the practical KMP-project-setup competence that distinguishes someone who's actually *shipped* a KMP app to both stores from someone who's only written shared logic. It ties KMP together with the modularization patterns and demonstrates the operational, not just conceptual, mastery an Android/KMP role requires."}]},{level:"junior",q:"What is Ktor, and why use it for KMP networking?",a:[{t:"p",text:"*Ktor Client* is JetBrains' *multiplatform* HTTP client — it works in `commonMain` (unlike Retrofit, which is JVM-only). You write shared networking code once, and Ktor uses a platform-specific *engine* under the hood (OkHttp on Android, Darwin/`NSURLSession` on iOS) selected per source set. It integrates with *kotlinx.serialization* for JSON, coroutines for async, and supports interceptors/auth/logging. It's the de-facto networking choice for shared KMP code because it's coroutine-first and truly cross-platform."},{t:"list",items:["**Ktor Client** — multiplatform HTTP (works in commonMain).","**Engines** — OkHttp (Android), Darwin (iOS).","**Integrates** — kotlinx.serialization, coroutines.","**De-facto** — shared KMP networking."]},{t:"note",text:"Ktor Client is JetBrains' multiplatform HTTP client — works in commonMain (Retrofit is JVM-only). Write networking once; Ktor uses a platform engine (OkHttp on Android, Darwin/NSURLSession on iOS) per source set. Integrates with kotlinx.serialization, coroutines, interceptors/auth. The de-facto choice for shared KMP networking — coroutine-first and truly cross-platform."}]},{level:"junior",q:"What is SQLDelight, and how does it enable shared persistence?",a:[{t:"p",text:"*SQLDelight* is a *multiplatform database* library: you write *SQL* (in `.sq` files), and it generates *type-safe Kotlin APIs* from your queries, backed by a platform *SQLite driver* (Android driver, Native driver for iOS). This gives shared, type-checked persistence in `commonMain` — unlike *Room*, which is Android-only. It supports coroutines/Flow for reactive queries and transactions. It's the standard KMP DB choice: SQL-first, compile-time-verified queries, cross-platform SQLite."},{t:"list",items:["**SQLDelight** — multiplatform, SQL-first DB.","**Generates** — type-safe Kotlin from `.sq` queries.","**Drivers** — Android + Native (iOS) SQLite.","**Reactive** — coroutines/Flow queries; vs Android-only Room."]},{t:"note",text:"SQLDelight is a multiplatform DB: write SQL in .sq files, it generates type-safe Kotlin APIs backed by a platform SQLite driver (Android + Native/iOS). Shared, compile-time-verified persistence in commonMain (Room is Android-only). Supports coroutines/Flow reactive queries and transactions. The standard KMP DB choice — SQL-first, cross-platform SQLite."}]},{level:"senior",q:"How does dependency injection work in KMP (Koin vs Hilt)?",a:[{t:"p",text:"*Hilt/Dagger* are *JVM/Android-only* (annotation processing tied to Android) — you *can't* use them in `commonMain`. For shared DI you use *Koin* (multiplatform, runtime DI via a DSL) or *kotlin-inject* (compile-time multiplatform DI), or *manual DI* (factory functions). Koin is popular: define modules in common, `initKoin()` from both apps, and resolve dependencies anywhere. On Android you may still use Hilt in the *Android app layer* and bridge to the shared Koin/manual graph. Choose Koin/manual for the shared module; keep Hilt (if used) at the Android edge."},{t:"list",items:["**Hilt/Dagger** — Android-only; not in commonMain.","**Shared DI** — Koin (runtime) or kotlin-inject (compile-time) or manual.","**Koin** — modules in common, `initKoin()` both apps.","**Bridge** — Hilt at the Android edge if used."]},{t:"note",text:"Hilt/Dagger are Android/JVM-only (can't be in commonMain). For shared DI use Koin (multiplatform runtime DSL), kotlin-inject (compile-time MP), or manual DI. Koin is popular: modules in common, initKoin() from both apps. You may still use Hilt in the Android app layer bridging to the shared graph. Koin/manual for shared; Hilt at the Android edge."}]},{level:"junior",q:"What is kotlinx.serialization, and why is it used in KMP?",a:[{t:"p",text:"*kotlinx.serialization* is the *multiplatform* JSON (and other formats) library — it works in `commonMain` (unlike Gson/Moshi, which are JVM-only). You annotate a class `@Serializable`, and it *generates* the serialization code at *compile time* (no reflection — good for Native and R8). It integrates directly with *Ktor* for request/response bodies. It's the default serializer for shared KMP code: cross-platform, compile-time-safe, reflection-free. Define your DTOs `@Serializable` in common and both platforms parse identically."},{t:"code",title:"Shared serializable DTO",code:`@Serializable
data class User(val id: String, val name: String)
// works in commonMain; Ktor uses it for JSON bodies; no reflection`},{t:"note",text:"kotlinx.serialization is the multiplatform serializer — works in commonMain (Gson/Moshi are JVM-only). Annotate @Serializable and it generates code at compile time (no reflection — Native/R8-friendly), integrating with Ktor for bodies. The default for shared KMP: cross-platform, compile-time-safe, reflection-free. Define @Serializable DTOs in common; both platforms parse identically."}]},{level:"senior",q:"How does Compose Multiplatform render UI on different platforms?",a:[{t:"p",text:"Compose Multiplatform runs the *same Compose runtime and compiler* everywhere, but the *rendering backend* differs: on Android it uses the native Android Compose (as usual); on *desktop and iOS* it renders via *Skia* (Skiko) — Compose draws its own pixels onto a surface (a `UIViewController` on iOS). Web uses Wasm/Canvas or DOM. So the *composables and state* are shared; the *drawing* is handled per platform. This is why iOS shows Compose-drawn UI (not UIKit views) — powerful for sharing, but a different rendering model than native."},{t:"list",items:["**Same** — Compose runtime/compiler everywhere.","**Android** — native Android Compose.","**Desktop/iOS** — Skia (Skiko) rendering.","**Shared composables** — per-platform drawing."]},{t:"note",text:"Compose Multiplatform shares the Compose runtime/compiler; the rendering backend differs: Android uses native Android Compose, desktop/iOS render via Skia (Skiko — Compose draws its own pixels, iOS into a UIViewController), web via Wasm/Canvas/DOM. Composables and state are shared; drawing is per-platform. Why iOS shows Compose-drawn (not UIKit) UI."}]},{level:"junior",q:"What is a multiplatform library, and how do you know if one exists?",a:[{t:"p",text:"A *multiplatform library* publishes artifacts for multiple targets (common + platform), so you can add it to `commonMain` and use it on all platforms. To find them: check *klibs.io* / awesome-kmp lists, the library's docs (does it list `iosArm64` etc.?), or its Gradle metadata. Core ecosystem libs are multiplatform: *Ktor, SQLDelight, kotlinx.serialization/coroutines/datetime, Koin, Kermit, Multiplatform Settings, Store, Apollo (GraphQL)*. If a library is JVM-only (Retrofit, Room, Hilt, Gson), it *can't* go in common — you need the multiplatform alternative."},{t:"list",items:["**Multiplatform lib** — artifacts for common + platforms.","**Find** — klibs.io, docs (lists iOS targets?), metadata.","**Core** — Ktor, SQLDelight, kotlinx, Koin, Kermit, Settings.","**JVM-only** — can't go in common; use the MP alternative."]},{t:"note",text:"A multiplatform library publishes artifacts for multiple targets, so it works in commonMain everywhere. Find them via klibs.io/awesome-kmp, docs (does it list iosArm64?), or Gradle metadata. Core MP libs: Ktor, SQLDelight, kotlinx.serialization/coroutines/datetime, Koin, Kermit, Multiplatform Settings, Store, Apollo. JVM-only libs (Retrofit/Room/Hilt/Gson) can't go in common — use the MP alternative."}]},{level:"senior",q:"How do you share a repository/data layer in KMP?",a:[{t:"p",text:"Put the *repository, use-cases, models, DTOs, and mappers* in `commonMain`, using *Ktor* (remote), *SQLDelight* (local), and *kotlinx.serialization* (parsing) — all multiplatform. The repository exposes *suspend functions/Flows* of *domain models*, implementing offline-first/caching logic once. Platform-specific needs (secure storage, connectivity) are injected via interfaces/expect-actual. Both apps consume the same repository through DI. This is the *sweet spot* of KMP: the entire data layer shared and tested once, with native UI on top."},{t:"list",items:["**In common** — repository, use-cases, models, mappers.","**Libs** — Ktor + SQLDelight + serialization.","**Exposes** — suspend/Flow of domain models.","**Sweet spot** — data layer shared + tested once."]},{t:"note",text:"Share the data layer by putting repository/use-cases/models/DTOs/mappers in commonMain using Ktor (remote), SQLDelight (local), kotlinx.serialization (parsing) — all multiplatform. The repository exposes suspend/Flow of domain models with offline-first logic written once; platform needs (secure storage) injected via interfaces. Both apps consume it via DI. KMP's sweet spot: data layer shared + tested once, native UI on top."}]},{level:"junior",q:"What is Kermit / Napier, and why use a multiplatform logger?",a:[{t:"p",text:"*Kermit* and *Napier* are *multiplatform logging* libraries — they let shared code log once and route output to each platform's native logging (Logcat on Android, `NSLog`/os_log on iOS). Since `android.util.Log` is Android-only, you can't use it in `commonMain`; a multiplatform logger fills that gap. They support log levels, tags, and can forward to Crashlytics. Use one so your shared logic can log diagnostics visible on both platforms — important for debugging shared code you can't easily step through in Xcode."},{t:"list",items:["**Kermit/Napier** — multiplatform logging.","**Routes** — Logcat (Android), NSLog/os_log (iOS).","**Fills gap** — `android.util.Log` is Android-only.","**Can forward** — to Crashlytics; aids shared-code debugging."]},{t:"note",text:"Kermit/Napier are multiplatform loggers — shared code logs once, routed to native logging (Logcat/NSLog/os_log). Needed because android.util.Log is Android-only (unusable in commonMain). They support levels/tags and can forward to Crashlytics. Use one so shared logic's diagnostics are visible on both platforms — key for debugging shared code you can't easily step through in Xcode."}]},{level:"senior",q:"What are the KMP alternatives to common Android libraries?",a:[{t:"p",text:"Map each Android/JVM-only lib to its multiplatform counterpart: *Retrofit/OkHttp → Ktor Client*; *Room → SQLDelight* (or the newer multiplatform Room); *Hilt/Dagger → Koin / kotlin-inject / manual*; *Gson/Moshi → kotlinx.serialization*; *SharedPreferences/DataStore → Multiplatform Settings* (or multiplatform DataStore); *android.util.Log → Kermit/Napier*; *WorkManager → platform-specific (expect/actual)*; *java.time → kotlinx-datetime*; *Glide/Coil → Coil 3 (now multiplatform) / Kamel*. Knowing these mappings is essential for planning a shared module."},{t:"table",headers:["Android/JVM","Multiplatform"],rows:[["Retrofit/OkHttp","Ktor Client"],["Room","SQLDelight / MP Room"],["Hilt/Dagger","Koin / kotlin-inject"],["Gson/Moshi","kotlinx.serialization"],["DataStore/Prefs","Multiplatform Settings"]]},{t:"note",text:"KMP alternatives: Retrofit→Ktor, Room→SQLDelight (or MP Room), Hilt→Koin/kotlin-inject/manual, Gson/Moshi→kotlinx.serialization, Prefs/DataStore→Multiplatform Settings, Log→Kermit/Napier, java.time→kotlinx-datetime, Coil/Glide→Coil 3 (MP)/Kamel, WorkManager→expect/actual. Knowing these mappings is essential to plan a shared module."}]},{level:"junior",q:"What is Multiplatform Settings, and what does it replace?",a:[{t:"p",text:"*Multiplatform Settings* is a small library for *key-value preferences* in shared code — it wraps each platform's native storage (`SharedPreferences`/`DataStore` on Android, `NSUserDefaults` on iOS) behind a common API. It replaces direct use of `SharedPreferences` (Android-only) in `commonMain`. Good for simple settings/flags; it can expose values as Flows and integrate with serialization. For larger structured local data, use SQLDelight instead. It's the go-to for lightweight shared preferences."},{t:"list",items:["**Multiplatform Settings** — shared key-value prefs.","**Wraps** — SharedPreferences/DataStore + NSUserDefaults.","**Replaces** — Android-only SharedPreferences in common.","**For larger data** — use SQLDelight."]},{t:"note",text:"Multiplatform Settings wraps each platform's key-value storage (SharedPreferences/DataStore on Android, NSUserDefaults on iOS) behind a common API — replacing Android-only SharedPreferences in commonMain. Good for simple settings/flags (can expose Flows, integrate serialization). For larger structured local data, use SQLDelight. The go-to for lightweight shared preferences."}]},{level:"senior",q:"How do you structure navigation in a Compose Multiplatform app?",a:[{t:"p",text:"Options: *Jetpack Navigation* now has multiplatform support, or dedicated MP navigation libraries like *Decompose* (component-based, lifecycle-aware, great for shared navigation logic) or *Voyager* (Compose-focused, simple). These let you define navigation/back-stack in *shared* code driving the shared Compose UI. If sharing only logic (native UI), each platform handles its own navigation (Navigation Compose / SwiftUI navigation) driven by shared state. Choose based on whether UI is shared: shared UI → Decompose/Voyager/Nav-MP; native UI → native navigation per platform."},{t:"list",items:["**Shared-UI nav** — Decompose, Voyager, Navigation-MP.","**Decompose** — component/lifecycle-based shared nav.","**Native-UI** — each platform's own navigation, shared state.","**Choose** — by whether UI is shared."]},{t:"note",text:"CMP navigation: Jetpack Navigation (now MP), Decompose (component-based, lifecycle-aware — great for shared nav logic), or Voyager (Compose-focused, simple) — define back-stack in shared code driving shared Compose UI. If sharing only logic, each platform navigates natively (Navigation Compose/SwiftUI) on shared state. Shared UI → Decompose/Voyager/Nav-MP; native UI → per-platform navigation."}]},{level:"senior",q:"How do you handle images and resources in Compose Multiplatform?",a:[{t:"p",text:"CMP provides a *multiplatform resources* system (`compose.components.resources`) — you put images, strings, fonts in `commonMain/composeResources` and access them with generated accessors (`Res.drawable.logo`, `stringResource(Res.string.title)`), working across platforms. For *network images*, use a multiplatform image loader — *Coil 3* (now multiplatform) or *Kamel* — that loads/caches on all targets. This replaces Android-only resource access (`R.drawable`) and Coil/Glide's Android-only versions. So shared UI can reference shared resources and load remote images uniformly."},{t:"list",items:["**compose.components.resources** — shared images/strings/fonts.","**Access** — `Res.drawable.*`, `stringResource(Res.string.*)`.","**Network images** — Coil 3 (MP) / Kamel.","**Replaces** — Android-only `R.drawable`/Glide."]},{t:"note",text:"CMP has a multiplatform resources system (compose.components.resources) — put images/strings/fonts in commonMain/composeResources, access via generated Res.drawable.*/stringResource(Res.string.*) across platforms. For network images use a multiplatform loader (Coil 3, now MP, or Kamel). Replaces Android-only R.drawable/Glide. Shared UI references shared resources and loads remote images uniformly."}]},{level:"junior",q:"Why can't you use Room or Retrofit in commonMain?",a:[{t:"p",text:"Both are *JVM/Android-only*: Retrofit depends on `java.*`/OkHttp and Android; Room's compiler and runtime target Android's SQLite/framework. `commonMain` must compile to *all* targets including *Kotlin/Native (iOS)*, where these JVM/Android APIs don't exist — so referencing them there won't compile. You use their *multiplatform equivalents* (Ktor for Retrofit, SQLDelight for Room) in common, or keep the JVM library in `androidMain` behind an `expect`/`actual` interface. The rule: commonMain code must be portable to Native."},{t:"list",items:["**JVM/Android-only** — Retrofit (java/OkHttp), Room (Android SQLite).","**commonMain** — compiles to Native too; those APIs absent.","**Use** — Ktor / SQLDelight in common.","**Or** — keep in androidMain behind expect/actual."]},{t:"note",text:"Retrofit (java.*/OkHttp/Android) and Room (Android SQLite/compiler) are JVM/Android-only. commonMain compiles to all targets including Kotlin/Native (iOS), where those APIs don't exist — so they won't compile there. Use multiplatform equivalents (Ktor, SQLDelight) in common, or keep the JVM lib in androidMain behind an expect/actual interface. commonMain must be Native-portable."}]},{level:"senior",q:"What is the newer multiplatform Room, and how does it compare to SQLDelight?",a:[{t:"p",text:"Google has released *Room with KMP support* (works on iOS/Native, not just Android) — so if you know Room, you can now use its *DAO/entity/annotation* model in shared code. *SQLDelight* is *SQL-first* (write SQL, generate Kotlin) and has been multiplatform for years (mature, driver-based, reactive). Choice: Room-KMP for teams preferring Room's ORM-style API and existing Room knowledge; SQLDelight for SQL-first control, maturity, and its ecosystem. Both give shared SQLite persistence — pick by API preference and maturity needs."},{t:"list",items:["**Room-KMP** — Room's DAO/annotation model, now on Native.","**SQLDelight** — SQL-first, generate Kotlin, long-mature.","**Room** — for existing Room knowledge/ORM style.","**SQLDelight** — SQL control + maturity."]},{t:"note",text:"Room now has KMP support (works on iOS/Native) — use its DAO/entity/annotation model in shared code. SQLDelight is SQL-first (write SQL → generate Kotlin), multiplatform for years, mature and reactive. Choose Room-KMP for ORM-style API/existing Room knowledge, SQLDelight for SQL-first control and maturity. Both give shared SQLite — pick by API preference and maturity."}]},{level:"senior",q:"How do you architect a KMP project's modules for maximum sharing?",a:[{t:"p",text:"Layer it: a *shared domain/data* module (models, use-cases, repositories, networking, DB — all multiplatform), optionally a *shared presentation* module (ViewModels/state with StateFlow), and *thin platform app modules* (Android Compose, iOS SwiftUI) that only render state and dispatch actions. Push logic *down* into common; keep platform modules as thin as possible. Use *interfaces + DI* for platform services. For very large apps, split shared into *feature modules* (each multiplatform). The goal: everything portable lives in shared, only UI/entry points are platform-specific."},{t:"list",items:["**Shared domain/data** — models, use-cases, repo, network, DB.","**Shared presentation** — ViewModels/state (optional).","**Thin platform apps** — render + dispatch only.","**Push logic down** — into common; UI stays platform."]},{t:"note",text:"Architect for sharing: a shared domain/data module (models/use-cases/repos/networking/DB — all MP), optionally a shared presentation module (ViewModels/StateFlow), and thin platform app modules (Compose/SwiftUI) that only render state and dispatch actions. Push logic into common; platform services via interfaces + DI; split shared into feature modules at scale. Everything portable in shared, only UI/entry points platform-specific."}]},{level:"junior",q:"What is expect/actual's role when using platform-specific libraries?",a:[{t:"p",text:"When a capability needs a *different library per platform* (e.g. secure storage: Android Keystore vs iOS Keychain), you *declare* the API in `commonMain` (`expect`) and *implement* it in each platform set (`actual`) using that platform's library. Common code depends only on the expected API; the platform provides the real implementation. This lets you use the *best native library* on each side while presenting a *single shared interface* — the primary mechanism (alongside DI'd interfaces) for bridging platform-specific dependencies into shared code."},{t:"list",items:["**expect** — declare the capability in common.","**actual** — implement per platform with its library.","**Best native lib** — each side; single shared API.","**Bridges** — platform-specific deps into shared code."]},{t:"note",text:"When a capability needs a different library per platform (secure storage: Keystore vs Keychain), declare it in commonMain (expect) and implement in each platform set (actual) with that platform's library. Common depends only on the expected API; platforms provide the real impl — using the best native library each side behind one shared interface. The primary mechanism (with DI'd interfaces) for platform-specific deps."}]},{level:"senior",q:"What is the Compose compiler's role in Compose Multiplatform?",a:[{t:"p",text:"The *Compose compiler plugin* transforms `@Composable` functions (adding the composer, stability inference, recomposition scopes) — and it's the *same compiler* whether you target Android or Compose Multiplatform. So all the *stability/recomposition* concepts (skippable functions, stable parameters, `remember`) apply identically in CMP. The difference is only the *runtime/rendering* backend (Skia off Android). This means your Compose performance knowledge transfers directly to CMP, and the same optimization rules (immutability, keys, deferring reads) hold across platforms."},{t:"list",items:["**Same Compose compiler** — Android and CMP.","**Adds** — composer, stability, recomposition scopes.","**Concepts transfer** — skippable, stable params, remember.","**Differs** — only runtime/rendering backend."]},{t:"note",text:"The Compose compiler plugin (composer injection, stability inference, recomposition scopes) is the same whether targeting Android or Compose Multiplatform — so stability/recomposition concepts (skippable functions, stable params, remember) apply identically in CMP. Only the runtime/rendering backend differs (Skia off Android). Your Compose performance knowledge transfers directly; the same optimization rules hold."}]},{level:"junior",q:"How do you handle date and time in shared KMP code?",a:[{t:"p",text:"Use *kotlinx-datetime* — the multiplatform date/time library — instead of `java.time` (JVM-only) or platform date APIs. It provides `Instant`, `LocalDateTime`, `TimeZone`, and arithmetic that work in `commonMain` across Android and iOS. For formatting/parsing and time-zone handling you use its APIs (with platform time-zone data). This keeps date logic shared and consistent. Don't reach for `java.time` in common (won't compile on Native) or duplicate date logic per platform — kotlinx-datetime is the standard."},{t:"list",items:["**kotlinx-datetime** — multiplatform dates/times.","**Provides** — Instant, LocalDateTime, TimeZone, arithmetic.","**Avoid** — `java.time` (JVM-only) in common.","**Keeps** — date logic shared and consistent."]},{t:"note",text:"Use kotlinx-datetime (multiplatform) for dates/times in shared code — Instant, LocalDateTime, TimeZone, arithmetic working in commonMain across Android/iOS — instead of java.time (JVM-only) or platform date APIs. Keeps date logic shared and consistent. Don't use java.time in common (won't compile on Native) or duplicate per platform. The standard KMP date library."}]},{level:"senior",q:"How do you manage the Gradle build for a KMP + Compose Multiplatform project?",a:[{t:"p",text:"Apply the *Kotlin Multiplatform* plugin and the *Compose Multiplatform* plugin (`org.jetbrains.compose`), declare *targets* (android, iosX64/Arm64/SimulatorArm64, desktop), configure *source sets* and their dependencies (Compose UI in `commonMain`), and set up *iOS framework export* (CocoaPods/XCFramework). Use a *version catalog* for consistent dependency versions and *convention plugins* to share build logic across modules. Keep the Compose compiler and Kotlin versions aligned. The build orchestrates producing the Android app, iOS framework, and desktop artifact from shared code."},{t:"list",items:["**Plugins** — KMP + Compose Multiplatform.","**Targets + source sets** — Compose UI in commonMain.","**iOS export** — CocoaPods/XCFramework.","**Version catalog + convention plugins** — consistency."]},{t:"note",text:"Apply the KMP plugin + Compose Multiplatform plugin (org.jetbrains.compose), declare targets (android, iOS, desktop), configure source sets (Compose UI in commonMain), set up iOS framework export (CocoaPods/XCFramework). Use a version catalog + convention plugins for consistency, keeping Compose/Kotlin versions aligned. The build produces Android app, iOS framework, and desktop artifact from shared code."}]},{level:"senior",q:"What are the maturity trade-offs of Compose Multiplatform on iOS?",a:[{t:"p",text:"CMP on iOS is *stable* but *newer* than Android Compose or native SwiftUI. Trade-offs: you get *maximal UI sharing*, but iOS renders *Compose (Skia)* — so *native-feel details* (system components, exact iOS look, some accessibility/text behaviors, deep OS integrations) may lag native SwiftUI, and you're on a faster-moving/less-battle-tested stack. Interop with UIKit/SwiftUI is possible but adds seams. Weigh: if pixel-perfect native iOS UX and full SwiftUI access matter, share only logic; if UI-sharing velocity matters more, CMP is increasingly viable. Assess per product."},{t:"list",items:["**Stable but newer** — than Android Compose/SwiftUI.","**Pro** — maximal UI sharing.","**Con** — native-feel/components/accessibility may lag.","**Weigh** — native UX vs UI-sharing velocity."]},{t:"note",text:"CMP on iOS is stable but newer: pro — maximal UI sharing; con — iOS renders Compose (Skia), so native-feel details (system components, exact iOS look, some accessibility/text, OS integrations) may lag SwiftUI, on a faster-moving stack (UIKit/SwiftUI interop adds seams). If pixel-perfect native UX matters, share only logic; if UI-sharing velocity matters, CMP is increasingly viable. Assess per product."}]},{level:"junior",q:"What is Apollo Kotlin / Store, and where do they fit in KMP?",a:[{t:"p",text:"*Apollo Kotlin* is a *multiplatform GraphQL* client — if your backend is GraphQL, it generates type-safe models and works in `commonMain` (like Ktor for REST). *Store* is a multiplatform library for *building a caching/data layer* (combining network + local source with a single-source-of-truth, offline-first policy) — it standardizes fetch/cache/refresh logic. Both fit the *shared data layer*: Apollo for GraphQL networking, Store for orchestrating caching. They're examples of the maturing KMP ecosystem covering more of the stack."},{t:"list",items:["**Apollo Kotlin** — multiplatform GraphQL client.","**Store** — caching/data layer (network + local SSOT).","**Fit** — the shared data layer.","**Ecosystem** — maturing, covers more of the stack."]},{t:"note",text:"Apollo Kotlin is a multiplatform GraphQL client (type-safe, works in commonMain — like Ktor for REST) if your backend is GraphQL. Store is a multiplatform caching/data-layer library (network + local single-source-of-truth, offline-first fetch/cache/refresh). Both fit the shared data layer — Apollo for GraphQL, Store for caching orchestration. Examples of the maturing KMP ecosystem."}]},{level:"junior",q:"What is Coil 3, and how does it enable shared image loading?",a:[{t:"p",text:"*Coil 3* is now *multiplatform* — the popular Android image loader works in `commonMain`/Compose Multiplatform, loading and caching images across Android, iOS, and desktop. You use `AsyncImage`/`rememberAsyncImagePainter` in shared Compose UI to display remote images with the same API everywhere. This replaces relying on Android-only Coil 2 or Glide in shared code. For non-Compose shared code, *Kamel* is an alternative. Coil 3 is the go-to for image loading in Compose Multiplatform apps."},{t:"list",items:["**Coil 3** — multiplatform image loading (was Android-only).","**Works in** — commonMain / Compose Multiplatform.","**API** — `AsyncImage` in shared Compose UI.","**Alternative** — Kamel; replaces Coil 2/Glide in shared code."]},{t:"note",text:"Coil 3 is now multiplatform — the Android image loader works in commonMain/Compose Multiplatform, loading/caching across Android/iOS/desktop with the same AsyncImage API. Replaces Android-only Coil 2/Glide in shared code. Kamel is an alternative for non-Compose. The go-to for image loading in Compose Multiplatform apps."}]},{level:"senior",q:"How do you test shared code that uses multiplatform libraries?",a:[{t:"p",text:"Test in `commonTest` (runs on all targets). For *Ktor*, use its *MockEngine* to stub HTTP responses without a network — verify your repository parses/handles them. For *SQLDelight*, use the *in-memory/JVM driver* in tests to exercise queries. For *coroutines/Flow*, use `kotlinx-coroutines-test` (`runTest`, test dispatchers, Turbine for Flows). Inject *fakes* for platform-abstraction interfaces. Because these libraries are multiplatform, the *same tests validate behavior on Native too* — run them on the iOS target in CI to catch Native-only issues."},{t:"list",items:["**Ktor** — MockEngine to stub HTTP.","**SQLDelight** — in-memory/JVM driver.","**Coroutines/Flow** — `runTest` + Turbine.","**Run on Native** — same tests catch Native-only issues."]},{t:"note",text:"Test shared code in commonTest: Ktor MockEngine to stub HTTP (verify repo parsing), SQLDelight in-memory/JVM driver for queries, kotlinx-coroutines-test (runTest, test dispatchers, Turbine) for Flows, and fakes for platform interfaces. Since the libs are multiplatform, the same tests validate Native too — run them on the iOS target in CI to catch Native-only issues."}]},{level:"junior",q:"What is the difference between a common dependency and a platform dependency in Gradle?",a:[{t:"p",text:"In a KMP module, you add dependencies *per source set*. A *common dependency* (in `commonMain`'s dependencies) must be *multiplatform* — it's available to all targets (e.g. Ktor core, kotlinx.serialization). A *platform dependency* (in `androidMain`/`iosMain`) is available *only to that platform* and can be platform-specific (e.g. the OkHttp Ktor engine in `androidMain`, the Darwin engine in `iosMain`, or an Android-only library). So you put shared libs in common and platform-specific engines/libs in the matching platform set — a common pattern is 'common API + per-platform engine'."},{t:"code",title:"Per-source-set dependencies",code:`sourceSets {
  commonMain.dependencies { implementation(libs.ktor.core) }        // multiplatform
  androidMain.dependencies { implementation(libs.ktor.engine.okhttp) } // Android engine
  iosMain.dependencies { implementation(libs.ktor.engine.darwin) }     // iOS engine
}`},{t:"note",text:"In KMP you add dependencies per source set: a common dependency (commonMain) must be multiplatform (Ktor core, serialization — all targets); a platform dependency (androidMain/iosMain) is platform-only (OkHttp engine on Android, Darwin engine on iOS, Android-only libs). Pattern: common API + per-platform engine — shared libs in common, platform engines in the matching set."}]},{level:"senior",q:"How do you decide whether to share UI (CMP) or keep it native?",a:[{t:"p",text:"Decide by *UX bar, team, and reuse goals*. Share UI (CMP) when: UI is *fairly uniform* across platforms, you want to *minimize duplicated UI work*, the team is Kotlin-centric, and near-native (not pixel-perfect-native) iOS feel is acceptable. Keep UI native when: you need *best-in-class platform UX* (exact iOS conventions, latest SwiftUI, deep platform integration), have iOS expertise, or the UI differs significantly per platform. A common pragmatic path: *share logic first* (proven, low-risk), then adopt CMP for *some* screens where sharing UI clearly pays off."},{t:"list",items:["**Share UI (CMP)** — uniform UI, minimize dup, Kotlin team.","**Native UI** — best platform UX, iOS expertise, divergent UI.","**Pragmatic** — share logic first, CMP for select screens.","**Decide** — UX bar vs reuse; per product/team."]},{t:"note",text:"Share UI (CMP) when UI is fairly uniform, you want minimal duplicated UI work, the team is Kotlin-centric, and near-native iOS feel is acceptable. Keep native when you need best-in-class platform UX (exact iOS conventions, latest SwiftUI, deep integration), have iOS expertise, or UI diverges. Pragmatic path: share logic first (low-risk), adopt CMP for select screens where it clearly pays."}]}],RT={id:"libraries-cmp",title:"Libraries & Compose Multiplatform",tagline:"The multiplatform library stack (Ktor/serialization/SQLDelight/Koin) & why JVM libs can't be shared, Compose Multiplatform, and project/build structure.",tags:["KMP","Ktor","SQLDelight","Compose Multiplatform","Koin"],content:PT,qa:DT},ET={id:"kmp",name:"Kotlin Multiplatform",description:"Sharing Kotlin across platforms — KMP fundamentals (source sets, expect/actual), iOS interop & SKIE, and the multiplatform library stack & Compose Multiplatform.",topics:[CT,MT,RT]},UT=[{heading:"What a mobile system design interview tests",blocks:[{t:"p",text:"A **mobile system design interview** asks you to design a feature or app component (an image loader, a chat feature, an offline-sync system, a news feed) — testing whether you can make sound *architectural* decisions, handle *mobile-specific constraints* (unreliable network, limited memory/battery, lifecycle, offline), reason about *trade-offs*, and communicate a coherent design. Unlike backend system design (which centers on servers, databases, and scale), *mobile* system design centers on the *client*: data flow, caching, offline behavior, the UI/data layers, and the constraints of running on a device."},{t:"list",items:["**It's about judgment, not memorized answers** — there's rarely one 'right' design. The interviewer wants to see how you *think*: how you clarify requirements, decompose the problem, weigh options, and justify choices.","**Mobile-specific focus** — the distinguishing concerns are *client-side*: caching strategies, offline-first behavior, network efficiency, memory/battery constraints, lifecycle handling, and the layered app architecture — not primarily server scaling (though the client↔server contract matters).","**Communication is half the evaluation** — a great design poorly explained scores worse than a good design clearly communicated. Think aloud, structure your answer, and check in with the interviewer."]}]},{heading:"A framework for answering",blocks:[{t:"p",text:"Have a *structured approach* so you don't ramble or miss key areas. A common, effective framework:"},{t:"list",items:["**1. Clarify requirements & scope** — *don't start designing immediately*. Ask questions to define what you're building: functional requirements (what features?), non-functional (offline support? real-time? scale? which platforms?), and constraints. Nail down scope — 'design Instagram' is too broad; clarify to 'the photo feed with offline caching'. This step is critical and often where candidates fail (jumping in without understanding the problem).","**2. Define the API / data contract** — what data does the client get from the server, and how? Sketch the key endpoints/models (or note it's given). This grounds the design in the actual data.","**3. High-level architecture** — sketch the layers and data flow: UI (Compose/Views) → ViewModel/presentation → domain (use cases) → data (repository → local DB + network). Establish the *single source of truth* and how data flows. This is your architectural backbone.","**4. Deep-dive the core components** — pick the *interesting* parts (the ones the question is really about — caching, offline sync, pagination, real-time) and design them in detail: data models, caching strategy, sync logic, error handling. This is where you show depth.","**5. Address mobile concerns** — offline behavior, network efficiency, caching, memory/battery, lifecycle/process death, error/edge cases. These are the mobile-specific differentiators.","**6. Discuss trade-offs & alternatives** — for key decisions, explain *why* you chose one approach over another (and when you'd choose differently). This demonstrates senior judgment."]}]},{heading:"The mobile-specific concerns to always cover",blocks:[{t:"list",items:["**Data flow & single source of truth** — where does data live authoritatively (usually a local DB), and how does it flow to the UI? The SSOT/offline-first pattern (UI observes the DB, network updates the DB) is the backbone of most mobile designs.","**Caching strategy** — what's cached, where (memory/disk), how it's invalidated (TTL/ETag/events), and the read strategy (cache-then-network, etc.). Caching is central to mobile UX and a near-universal topic.","**Offline support** — how the app behaves offline (serve cache), and how *offline writes* sync when back online (local-first + background sync + conflict resolution). Offline is *the* mobile differentiator.","**Network efficiency** — minimizing requests and data (batching, pagination, delta sync, compression, image optimization), respecting the user's data plan and battery.","**Memory & performance** — bounded memory (don't load everything — paginate, recycle, downsample images), avoiding jank/leaks, efficient rendering.","**Lifecycle & reliability** — handling process death (persist state), configuration changes, background execution limits, and graceful error/retry handling.","**Real-time (if relevant)** — WebSockets/push for live features, with connection management."]}]},{heading:"How to communicate well",blocks:[{t:"list",items:["**Think aloud and structure** — narrate your reasoning, and signal structure ('First I'll clarify requirements, then sketch the architecture, then deep-dive caching'). This makes you easy to follow and shows organized thinking.","**Drive the conversation, but check in** — lead the design, but periodically confirm scope/priorities with the interviewer ('Should I focus on the offline sync or the real-time part?'). It's collaborative, not a monologue.","**Draw diagrams** — a layered architecture diagram (UI → ViewModel → repository → DB/network) and data-flow arrows make your design concrete and easy to discuss. Use the whiteboard/tool.","**Justify with trade-offs** — every significant choice should come with a 'because' and an acknowledgment of alternatives. 'I'd use a local DB as the source of truth *because* it gives offline support and consistency; the alternative — reading directly from the network — is simpler but breaks offline.' This is the senior signal.","**Manage time & prioritize** — you can't design everything; spend time on the *core, interesting* parts (what the question is testing), and mention the rest briefly. Don't rabbit-hole on a minor detail.","**Connect to real patterns** — ground your design in the established Android patterns (clean architecture, SSOT/offline-first, Room, WorkManager, Paging, Coil, coroutines/Flow) — you're not inventing from scratch; you're *applying* the toolkit appropriately."]},{t:"note",text:"Mobile system design tests architectural judgment, mobile-constraint handling (offline, network, memory, battery, lifecycle), trade-off reasoning, and communication. Framework: (1) clarify requirements/scope FIRST, (2) define the data contract, (3) high-level layered architecture + SSOT, (4) deep-dive core components, (5) address mobile concerns (caching, offline, network efficiency, memory, lifecycle), (6) discuss trade-offs. Always cover: SSOT/data flow, caching, offline (incl. writes/sync), network efficiency, memory, lifecycle/process death. Communicate: think aloud, structure, diagram, justify with trade-offs, prioritize the core, ground in real patterns."}]}],qT=[{level:"junior",q:"How is mobile system design different from backend system design?",a:[{t:"p",text:"**Mobile system design centers on the *client* — data flow within the app, caching, offline behavior, the UI/data layers, and the constraints of running on a device — whereas backend system design centers on *servers* (scaling, distributed databases, load balancing, handling millions of requests).** They test different concerns."},{t:"list",items:["**Mobile focuses on**: the app's *layered architecture* (UI → ViewModel → repository → local DB + network), *caching strategies*, *offline-first* behavior (working without a network, syncing writes when back online), *network efficiency* (unreliable/metered connections), *memory and battery* constraints, and *lifecycle* handling (process death, configuration changes, background limits).","**Backend focuses on**: server *scale* (handling load), *distributed systems* (sharding, replication, consistency), *databases* at scale, *caching layers* (Redis), *load balancing*, and *availability*."]},{t:"p",text:"The key distinction is that mobile design is about the *constraints of the device and the unreliable link to the server* — a phone has limited memory and battery, an intermittent network, and a lifecycle where the OS can kill your process anytime. So mobile designs emphasize *offline support*, *caching* (to be fast and work offline), *efficient networking* (respect data/battery), and *resilience to lifecycle events* — concerns that barely exist in backend design. The client↔server *contract* matters (what data the API provides), but you're not designing the server's scaling; you're designing how the *client* handles data, caching, offline, and the device's realities. This is why mobile designs almost always revolve around the single-source-of-truth/offline-first pattern (local DB as the authority, network as an updater), which is the mobile-specific architectural backbone."}]},{level:"junior",q:"What's the first thing you should do in a system design interview?",a:[{t:"p",text:"**Clarify the requirements and scope — do *not* start designing immediately.** Ask questions to understand exactly what you're building before proposing any architecture. Jumping straight into a design without understanding the problem is the most common way candidates fail, because they end up solving the wrong problem or missing key constraints."},{t:"list",items:["**Functional requirements** — what features are in scope? 'Design Instagram' is far too broad; clarify to something concrete like 'the photo feed with infinite scroll and offline caching'. Pin down what you're actually building.","**Non-functional requirements** — does it need offline support? Real-time updates? What's the expected scale/data volume? Which platforms? These shape the whole design.","**Constraints** — any limits or priorities? What should you focus on (the interviewer often has a specific area they want to explore)?"]},{t:"p",text:"This step matters for two reasons. First, it ensures you *solve the right problem* — a design that's brilliant but addresses the wrong scope is useless. Second, it *demonstrates good engineering judgment* — real senior engineers clarify before building; jumping in signals inexperience. It also lets you *scope the problem to something answerable* in the time available (design the core, not everything). Spend a few minutes here, confirm your understanding with the interviewer ('So I'm designing X with offline support, focusing on the caching — is that right?'), and only then move to the architecture. The interviewer is often *deliberately* vague to see if you'll ask — treating the clarification as part of the test, not a preamble to it, is what distinguishes a strong candidate."}]},{level:"junior",q:"What mobile-specific concerns should you always address?",a:[{t:"list",items:["**Data flow & single source of truth** — where data lives authoritatively (usually a local database) and how it flows to the UI. The SSOT/offline-first pattern (UI observes the DB; network updates the DB) is the backbone of most mobile designs.","**Caching** — what's cached, where (memory/disk), how it's invalidated (TTL/ETag/events), and the read strategy (cache-then-network for instant + fresh). Central to mobile UX.","**Offline support** — how the app works offline (serve cached data), and critically how *offline writes* are handled (write locally, sync in the background when online, resolve conflicts). Offline is *the* mobile differentiator.","**Network efficiency** — minimizing requests and data: pagination, batching, delta/incremental sync, compression, image optimization — respecting metered connections and battery.","**Memory & performance** — bounded memory (paginate, recycle, downsample images — don't load everything), avoiding jank and leaks.","**Lifecycle & reliability** — surviving process death (persist state), configuration changes, background execution limits, and graceful error/retry handling."]},{t:"p",text:"These are the concerns that *distinguish mobile design* from generic software design — they stem from the realities of running on a resource-constrained device with an unreliable network and an OS that controls your lifecycle. Addressing them (especially SSOT/offline-first, caching, and offline writes) is what shows you understand *mobile* engineering, not just architecture in the abstract. A design that ignores offline behavior, caching, or memory constraints would be flagged as naive for a mobile context. So in any mobile design, proactively cover: how does this work offline? what's cached and how is it invalidated? how do writes sync? is the network use efficient? does it survive process death? Weaving these in demonstrates mobile-specific competence."}]},{level:"senior",q:"Walk through your framework for a mobile system design question, using a concrete example.",a:[{t:"p",text:"**I use a structured 6-step framework — clarify, define the data contract, high-level architecture, deep-dive core components, address mobile concerns, discuss trade-offs — which keeps the answer organized and ensures I cover the key areas. Let me walk through it with 'design a news feed with offline support' as the example.**"},{t:"list",items:["**1. Clarify requirements & scope**: I'd ask — infinite scroll or paged? Offline reading required (yes, given the prompt)? Real-time updates or pull-to-refresh? Rich media (images)? Scale of feed? Let's say: infinite-scroll feed of articles with images, offline reading of cached articles, pull-to-refresh, no real-time. This scopes it to something concrete.","**2. Define the data contract**: the API provides paginated articles — `GET /feed?cursor=X&limit=20` returning articles (id, title, body, imageUrl, timestamp) plus a next-cursor. Note I'm consuming this, not designing the server. This grounds the design.","**3. High-level architecture**: layered — Compose UI → FeedViewModel (exposes `StateFlow<PagingData>`) → FeedRepository → Room (local DB, the *single source of truth*) + Ktor/Retrofit (network). The UI *observes Room*; the network *fills Room*. This SSOT backbone gives offline support and consistency for free. Diagram it: UI ↕ ViewModel ↕ Repository ↕ (Room ← Network).","**4. Deep-dive core components** (the interesting parts): (a) *Pagination* — use Paging 3 with a `RemoteMediator` so the network fills Room page-by-page and the UI reads from Room (offline-capable paging). (b) *Caching/SSOT* — articles cached in Room; the feed reads from Room (instant, offline), refresh writes new pages into Room. (c) *Images* — Coil with memory+disk cache and downsampling. (d) *Offline behavior* — reads always from Room, so cached articles show offline; refresh fails gracefully (keep showing cache + a snackbar).","**5. Address mobile concerns**: *offline* (Room SSOT + cached images = offline reading works); *caching invalidation* (TTL or refresh-on-open; RemoteMediator clears/replaces on refresh); *network efficiency* (paginate 20 at a time, don't over-fetch; images downsampled to view size; conditional requests/ETags if the API supports); *memory* (Paging bounds in-memory items, LazyColumn recycles, Coil downsamples — no OOM); *lifecycle* (Paging's `cachedIn` survives config changes; Room persists across process death; scroll position restorable).","**6. Trade-offs**: SSOT/Room-as-truth adds complexity (mapping, RemoteMediator, remote-keys) vs a simpler network-only feed — *but* it's the only way to get offline + consistency, which the requirement demands. Paging 3 vs hand-rolled pagination — Paging handles dedup/retry/load-states but has a learning curve; justified for a real infinite feed. Coil vs custom image caching — always use a library. I'd also note where I'd change: a *real-time* feed would add WebSockets/push; a *tiny* fixed feed wouldn't need Paging."]},{t:"list",items:["**Throughout, I'd communicate well**: think aloud, signal structure ('now the deep-dive on pagination'), draw the layered diagram, justify each choice with a trade-off, check in with the interviewer on where to focus, and prioritize the *interesting* parts (SSOT + paging + offline) over boilerplate."]},{t:"p",text:"**The senior framing**: the framework's value is that it imposes *structure* (so the answer is organized and complete, not a ramble) while ensuring I hit the areas interviewers evaluate — *clarifying first* (solving the right problem, showing judgment), *grounding in the data contract*, establishing the *architectural backbone* (SSOT/offline-first layered design), *deep-diving the interesting core* (where depth is shown), *explicitly addressing mobile concerns* (the differentiators — offline, caching, network, memory, lifecycle), and *justifying with trade-offs* (the senior signal). The concrete example shows how each step produces real design decisions grounded in the established Android toolkit (Room SSOT, Paging 3 + RemoteMediator, Coil, coroutines/Flow) — I'm *applying* proven patterns appropriately to the constraints, not inventing from scratch. And the meta-point is that *communication* (structure, thinking aloud, diagramming, trade-off justification, prioritization, checking in) is half the evaluation — a well-structured, clearly-narrated, trade-off-justified design scores far better than a technically-similar one delivered as a disorganized monologue. Demonstrating the framework, a concrete application of it, the grounding in real patterns, and the communication discipline is the comprehensive answer that shows you can *navigate* a system design interview, which is exactly what it tests."}]},{level:"senior",q:"How do you demonstrate senior-level judgment in a system design interview?",a:[{t:"p",text:"**Senior judgment shows in *how* you reason, not just *what* you design — specifically: clarifying and scoping before designing, justifying every significant decision with explicit trade-offs and alternatives, proactively addressing the hard mobile concerns (offline, edge cases, failure modes), prioritizing the interesting core over boilerplate, and adapting the design to the stated constraints rather than reciting a one-size-fits-all answer.**"},{t:"list",items:["**Trade-off reasoning is the #1 signal**: for every meaningful choice, explain *why* this over that, and *when you'd choose differently*. 'I'd use Room as the source of truth *because* it gives offline support and consistency; the alternative — reading directly from the network — is simpler but breaks offline, so I'd only do that for a real-time-only feature where staleness is unacceptable.' This shows you understand there's no universal right answer — design depends on requirements. Reciting a design as if it's *the* answer (without trade-offs) is the junior tell.","**Clarify and scope deliberately**: treating requirement-clarification as essential (not a formality), and *scoping* the problem to what matters, shows you solve the right problem — a senior instinct. Juniors jump straight to a solution.","**Proactively surface the hard parts**: don't wait to be asked about offline writes, conflict resolution, error handling, process death, or edge cases — *raise* them yourself. 'Now, offline writes are the tricky part — I need local-first writes, background sync via WorkManager, and a conflict-resolution policy; let me think about last-write-wins vs merge here.' Anticipating the hard problems (rather than only the happy path) is distinctly senior.","**Handle failure modes and edge cases**: designs that only cover the happy path are naive. Address what happens when the network fails mid-sync, when data conflicts, when the process is killed, when the cache is stale, when a request times out. Robustness under failure is a mark of experience.","**Prioritize and manage time**: spend depth on the *core, interesting* components (what the question tests) and mention the rest briefly — don't rabbit-hole on a minor detail or spread too thin. Knowing *where* to invest depth is judgment.","**Adapt to constraints**: tailor the design to the *stated* requirements (scale, offline, real-time, platform) rather than presenting a generic maximal design. 'Since this is a small fixed list, I *wouldn't* use Paging — that'd be over-engineering; a simple StateFlow<List> suffices.' Recognizing when *not* to use a heavy pattern is as senior as knowing when to."]},{t:"list",items:["**Connect decisions to real-world consequences**: 'this caching choice matters because on a metered connection, re-fetching images would burn the user's data'; 'this needs to survive process death because Android kills backgrounded apps, and losing the user's place would be a bad experience'. Grounding decisions in *user and device realities* shows mobile-specific maturity.","**Acknowledge what you're *not* doing and why**: explicitly noting scope boundaries ('I'm not designing the server scaling — assuming the API is given') and deferred concerns ('I'd add analytics/monitoring but let me focus on the core data flow') shows disciplined prioritization rather than trying to boil the ocean."]},{t:"p",text:"**The senior framing**: system design interviews evaluate *engineering judgment under ambiguity*, and seniority shows in the *reasoning process*, not the raw design. The defining behaviors are: *trade-off reasoning* (every choice justified with why-this-over-that and when-you'd-differ — the single strongest signal, because it proves you understand design is contextual, not memorized), *deliberate clarification and scoping* (solving the right problem), *proactively surfacing the hard parts* (offline writes, conflicts, failures, edge cases — anticipating difficulty rather than only the happy path), *prioritization* (depth on the interesting core, breadth briefly elsewhere), and *adapting to constraints* (including recognizing when *not* to use a heavy pattern — avoiding over-engineering is as senior as knowing advanced patterns). Underlying all of it is *grounding decisions in real user/device consequences* (data plans, battery, process death, offline) — mobile-specific maturity. The junior anti-patterns are the mirror image: jumping in without clarifying, presenting a design as *the* answer without trade-offs, covering only the happy path, over-engineering with every pattern regardless of need, and reciting generic solutions. Demonstrating the *judgment behaviors* — trade-off justification, hard-part anticipation, constraint-adaptation, prioritization, and real-world grounding — is how you signal seniority, and it's ultimately what the interview is designed to reveal."}]}],OT={id:"approach",title:"Approaching Mobile System Design",tagline:"What these interviews test, a 6-step framework (clarify → contract → architecture → deep-dive → mobile concerns → trade-offs), and how to communicate & show judgment.",tags:["System Design","Framework","Requirements","Trade-offs","Communication"],content:UT,qa:qT},FT=[{heading:"The problem and requirements",blocks:[{t:"p",text:"'Design an image-loading library' (like Coil or Glide) is a classic mobile system design question because it touches *caching, memory management, threading, and lifecycle* — all core mobile concerns — in a bounded, concrete problem. The task: given a URL, efficiently load the image, display it in a view/composable, and do so *without* jank, memory bloat, or leaks, even when scrolling a list of hundreds of images."},{t:"list",items:["**Functional**: load an image from a URL (or resource/file) into a target (ImageView/Composable); support placeholders and error images; handle transformations (resize, crop, rounded corners).","**Non-functional**: *fast* (instant for cached images), *memory-safe* (no OOM even with many large images), *no jank* (loading off the main thread), *no leaks* (respect the target's lifecycle), *efficient* (don't re-download or re-decode unnecessarily, respect the network).","**The hard parts**: the *cache hierarchy* (memory → disk → network), *bitmap memory management* (images are large; decode to the right size), *threading* (decode off-main, deliver on-main), *request lifecycle* (cancel when the target is gone or recycled), and *concurrency* (dedup identical in-flight requests)."]}]},{heading:"The cache hierarchy — the core of the design",blocks:[{t:"p",text:"The heart of an image loader is a **multi-tier cache**: check the fastest cache first, fall back to slower ones, and populate them as you go. This is what makes cached images instant and avoids redundant work."},{t:"code",title:"The load flow through the cache hierarchy",code:`fun load(url: String, target: Target) {
    // 1. Memory cache — decoded Bitmap, fastest (in-RAM)
    memoryCache[url]?.let { bitmap ->
        target.setImage(bitmap)     // instant, on main thread
        return
    }
    // 2. Disk cache — encoded bytes, persistent (survives restart)
    // 3. Network — download, then decode, then populate both caches
    launchOnBackgroundThread {
        val bytes = diskCache[url] ?: download(url).also { diskCache[url] = it }
        val bitmap = decode(bytes, target.width, target.height)  // downsample to target size
        memoryCache[url] = bitmap
        deliverOnMain { target.setImage(bitmap) }
    }
}`},{t:"list",items:["**Memory cache (tier 1)** — an in-memory `LruCache` of *decoded Bitmaps*, keyed by URL (+ target size/transformations). Fastest (no decode, no I/O) — a cached image displays *instantly*, which is what makes scrolling smooth. Bounded by size (a fraction of the app's memory budget); LRU evicts the least-recently-used when full.","**Disk cache (tier 2)** — *encoded* image bytes (the downloaded file) on disk, persistent across app restarts. Slower than memory (I/O + decode needed) but avoids re-downloading. Also LRU-bounded by size.","**Network (tier 3)** — the source. Download only if not in either cache; then populate *both* caches so the next request is fast.","**Why two tiers**: memory holds *decoded* bitmaps (fast to display but volatile and large in RAM); disk holds *encoded* bytes (compact, persistent, but needs decoding). The hierarchy trades memory (fast, small, volatile) → disk (persistent, needs decode) → network (source, slow) — each tier a fallback for the one above."]}]},{heading:"Bitmap memory management — avoiding OOM",blocks:[{t:"p",text:"Images are the #1 cause of `OutOfMemoryError` on Android, because a decoded bitmap is *huge* — a 4000×3000 photo is ~48MB in memory (4 bytes/pixel), regardless of the JPEG's small file size. Loading full-size images into small views wastes enormous memory. The key technique is **downsampling**: decode the image only to the *size actually needed* for the target view."},{t:"list",items:["**Downsample to target size** — decode the bitmap at the resolution the view will display, not the source resolution. Decoding a 4000px image into a 200px thumbnail at full size wastes ~99% of the memory. Use `BitmapFactory.Options.inSampleSize` (or the modern `ImageDecoder`) to decode at a reduced size. This alone prevents most image OOMs.","**Bounded memory cache** — the memory `LruCache` is sized to a *fraction* of the app's available memory (e.g. 1/8th via `ActivityManager.memoryClass`), and evicts LRU when full — so the cache never grows unbounded and OOMs.","**Bitmap pooling / reuse** — advanced: reuse bitmap memory (`inBitmap`) instead of allocating a new one each time, reducing GC churn during scrolling (Glide does this heavily).","**Appropriate config** — use `RGB_565` (2 bytes/pixel) instead of `ARGB_8888` (4 bytes) when alpha isn't needed, halving memory — a trade-off (slightly lower quality).","**The principle**: never hold more bitmap memory than needed — decode to display size, bound the cache, and reuse memory. Images are large and volatile, so disciplined memory management is essential."]}]},{heading:"Threading, request lifecycle, and concurrency",blocks:[{t:"list",items:["**Threading** — downloading and *decoding* must happen *off the main thread* (both are slow — I/O and CPU), then the bitmap is delivered *on the main thread* to update the view. Use coroutines (`Dispatchers.IO` for download/decode, `Dispatchers.Main` to deliver) or a thread pool. Doing decode on the main thread causes jank; this is why a naive `imageView.setImageBitmap(decode(download(url)))` on the main thread is wrong.","**Request lifecycle & cancellation** — a request must be *cancelled* when its target is no longer valid: the view is *recycled* in a RecyclerView (scrolled away — a new image is now wanted for that view), or the Activity/Fragment is *destroyed*. Otherwise you waste work and risk setting the *wrong* image on a recycled view (the classic 'wrong image flashes in a list' bug) or leaking the destroyed context. So requests are tied to the target's/lifecycle's scope and cancelled when it ends.","**The recycled-view bug** — in a list, view A's image request is in-flight when the user scrolls and view A is recycled to show item B. If A's request completes and sets its image, you see item A's image on item B's row. The fix: *cancel* the previous request when a view is rebound, and *tag* the view/request so a completing request checks it's still the intended target before setting the image.","**Concurrency / request dedup** — if two views request the *same* URL simultaneously (or the same image is requested while already loading), *don't* download/decode it twice — dedup identical in-flight requests so the work happens once and both targets get the result. This saves bandwidth and CPU.","**Lifecycle awareness** — the request should observe the target's lifecycle (Activity/Fragment/Compose) and cancel automatically when it's destroyed — preventing leaks and wasted work. Coil integrates with the lifecycle; in Compose, `AsyncImage` ties to the composition."]}]},{heading:"Putting it together — the API and full flow",blocks:[{t:"code",title:"A clean loading API (Coil-style)",code:`// Simple call site — the library handles all the complexity
imageView.load("https://...") {
    placeholder(R.drawable.loading)
    error(R.drawable.error)
    transformations(RoundedCornersTransformation(8f))
}
// In Compose:
AsyncImage(model = url, contentDescription = null)`},{t:"list",items:["**The full flow**: `load(url, target)` → check memory cache (instant if hit) → else launch a *cancellable* request scoped to the target's lifecycle → check disk cache, else download (deduped) → decode *downsampled to target size* off-main → populate memory + disk caches → deliver on main *if the target is still valid* → handle placeholder (during load) and error (on failure).","**Components**: a *request* model (URL, target, size, transformations, placeholders), a *cache* subsystem (memory LRU + disk LRU), a *fetcher* (network/file/resource), a *decoder* (bytes → downsampled bitmap), a *lifecycle/cancellation* manager, and a *dispatcher* (threading). This decomposition mirrors real libraries.","**Extensibility** — pluggable fetchers (URL, file, content URI), decoders (JPEG, GIF, SVG, video frames), and transformations (crop, blur, rounded corners) — an open architecture so the library handles varied sources and effects.","**Why use Coil/Glide, not hand-roll** — the real point: correct image loading requires all of the above (cache hierarchy, downsampling, threading, cancellation, dedup, lifecycle, bitmap pooling) done right, which is a lot of subtle work. Libraries have solved it; you use one. The *design exercise* is understanding *what they do and why*."]},{t:"note",text:"Image loader design: core = multi-tier cache (memory LruCache of decoded bitmaps = instant → disk cache of encoded bytes = persistent → network = source; populate as you go). Memory management (images cause OOM): DOWNSAMPLE to target size (a 4000px photo is ~48MB!), bound the cache, reuse bitmaps, use RGB_565 when possible. Threading: download+decode off-main, deliver on-main (else jank). Lifecycle/cancellation: cancel on view-recycle (the 'wrong image in a list' bug — tag/check target) and destroy (leaks); dedup identical in-flight requests. Components: request, cache, fetcher, decoder, lifecycle manager, dispatcher. Use Coil/Glide — they've solved this."}]}],LT=[{level:"junior",q:"What is the cache hierarchy in an image-loading library?",a:[{t:"p",text:"**It's a multi-tier cache — memory → disk → network — where you check the fastest cache first and fall back to slower ones, populating them as you go. This makes cached images display instantly and avoids redundant downloads/decodes.**"},{t:"list",items:["**Memory cache (fastest)** — an in-memory `LruCache` of *decoded Bitmaps*, keyed by URL. A hit displays the image *instantly* (no I/O, no decoding), which is what makes list scrolling smooth. It's bounded (a fraction of the app's memory) and evicts least-recently-used entries when full.","**Disk cache (persistent)** — the *encoded* image bytes (the downloaded file) stored on disk, surviving app restarts. Slower than memory (needs disk I/O + decoding) but avoids re-downloading. Also LRU-bounded.","**Network (source)** — download only if the image isn't in either cache; then populate *both* caches so the next request is fast."]},{t:"p",text:"The design rationale is that each tier trades off differently: memory holds *decoded* bitmaps (instant to display but volatile and large in RAM), disk holds *encoded* bytes (compact and persistent but needs decoding), and the network is the slow source of truth. So you check memory (instant), then disk (fast, avoids re-download), then network (last resort), and when you fetch from a slower tier you populate the faster ones. This hierarchy is the core of why image loaders feel fast — the same image scrolled past twice loads instantly the second time from the memory cache, and even after an app restart it loads from disk without re-downloading. Coil and Glide both implement exactly this memory+disk+network hierarchy."}]},{level:"junior",q:"Why do images cause OutOfMemoryError, and how do you prevent it?",a:[{t:"p",text:"**Because a *decoded* bitmap is enormous in memory — far larger than its file size — and loading full-size images into small views wastes huge amounts of memory. The prevention is *downsampling*: decode the image only to the size the view actually needs.**"},{t:"list",items:["**Why they're huge**: a decoded bitmap uses ~4 bytes per pixel (ARGB_8888), so a 4000×3000 photo is ~48MB in memory — regardless of the JPEG file being only a few MB (JPEG is compressed; the decoded bitmap is not). Load several full-size images and you exhaust the app's memory budget → OOM crash.","**The wasteful mistake**: decoding a 4000px image to display in a 200px thumbnail loads the full 48MB when you only need ~0.5MB — wasting ~99% of the memory for pixels the view can't even show."]},{t:"list",items:["**Downsample to target size** — the key fix. Decode the bitmap at the *view's* resolution, not the source's, using `BitmapFactory.Options.inSampleSize` (or the modern `ImageDecoder`). This alone prevents most image OOMs.","**Bound the memory cache** — size the `LruCache` to a fraction of available memory and evict LRU when full, so it never grows unbounded.","**Use RGB_565 when alpha isn't needed** — 2 bytes/pixel instead of 4, halving memory (slight quality trade-off).","**Reuse bitmap memory** (`inBitmap`) — advanced, reduces GC churn during scrolling."]},{t:"p",text:"The core principle is *never hold more bitmap memory than the display actually needs* — decode to display size, bound the cache, and reuse memory. Images are the #1 cause of OOM on Android precisely because their decoded size is so large and easy to over-allocate. This is also a big reason to use a library (Coil/Glide) rather than hand-decoding: they downsample automatically based on the target view size, so you get memory-safe image loading without manually computing sample sizes — a naive `BitmapFactory.decodeStream` into a small ImageView loads the full-size bitmap and OOMs on large images."}]},{level:"junior",q:"Why does image loading need to be off the main thread?",a:[{t:"p",text:"**Because both *downloading* (network I/O) and *decoding* (turning compressed bytes into a bitmap, which is CPU-intensive) are slow operations, and doing them on the main thread would block it — causing jank (dropped frames) or an ANR. So the download and decode happen on a background thread, and only the final bitmap is delivered to the main thread to update the view.**"},{t:"list",items:["**Downloading** is network I/O — potentially slow and variable (especially on a poor connection). Blocking the main thread on it freezes the UI.","**Decoding** is CPU-heavy — decompressing a JPEG and building a bitmap takes real time, especially for large images. Doing it on the main thread during scrolling drops frames.","**The pattern**: download + decode on a background thread (coroutines with `Dispatchers.IO`, or a thread pool), then deliver the finished bitmap on `Dispatchers.Main` to set it on the view."]},{t:"p",text:"This is why a naive `imageView.setImageBitmap(BitmapFactory.decodeStream(URL(url).openStream()))` on the main thread is wrong — it does network I/O *and* decoding on the UI thread, freezing the app (and it'd throw `NetworkOnMainThreadException` for the network part). The correct flow moves the slow work off-main and only touches the view (which must be on-main) at the end. It connects directly to the general 'never block the main thread' principle — the main thread must stay free to render frames and handle input, so anything slow (I/O, decoding, computation) goes to a background thread. Image loaders handle this threading internally, which is another reason to use one."}]},{level:"senior",q:"How do you handle request cancellation and the 'wrong image in a list' bug?",a:[{t:"p",text:"**You cancel a request when its target is no longer valid — when a view is recycled (scrolled away) or its lifecycle owner is destroyed — and you *tag* the target so a completing request verifies it's still the intended one before setting the image. The 'wrong image in a list' bug happens when a stale in-flight request sets its image on a view that's since been recycled to show a different item.**"},{t:"list",items:["**The bug explained**: in a RecyclerView/LazyColumn, view A starts loading image X (in-flight). The user scrolls, and view A is *recycled* to now display item B, which loads image Y. If image X's request completes *after* the recycle and naively sets its bitmap, you see image X on item B's row — the wrong image flashes in. It's a race between the async load and view recycling.","**Fix part 1 — cancel on rebind**: when a view is rebound to a new item (recycled), *cancel* its previous in-flight request. So when view A is reused for item B, A's request for image X is cancelled — it won't complete and set the wrong image. This is the primary fix.","**Fix part 2 — tag/verify the target**: tag the view (or request) with the URL/request-id it's currently loading. When a request completes, it checks the target *still* wants *this* image (the tag matches) before setting it; if the view has been rebound to something else, the stale result is discarded. This guards against races the cancellation might miss."]},{t:"list",items:["**Lifecycle cancellation (leaks + waste)**: beyond recycling, a request must be cancelled when its lifecycle owner (Activity/Fragment/composition) is *destroyed* — otherwise you waste work loading an image nobody will see, and risk *leaking* the destroyed context/view the request holds. So requests are scoped to the target's lifecycle and cancelled when it ends. Coil integrates with the Android lifecycle for this; in Compose, `AsyncImage` ties to the composition and cancels when it leaves.","**Structured cancellation with coroutines**: modeling each load as a *cancellable coroutine* scoped to the target makes this natural — rebinding cancels the old coroutine, destruction cancels the scope. The download and decode check for cancellation and stop promptly, freeing resources.","**Request dedup (related concurrency concern)**: if the *same* URL is requested by multiple views or while already loading, dedup the in-flight request so the download/decode happens *once* and all targets receive the result — saving bandwidth and CPU. This interacts with cancellation (a shared request isn't cancelled while another target still wants it)."]},{t:"p",text:"**The senior framing**: the 'wrong image in a list' bug is the canonical example of an *async operation racing with view recycling* — a fundamental challenge whenever you have long-running work tied to reusable UI. The robust solution has two layers: *cancel* the stale request when the view is rebound (so it never completes), and *verify the target* on completion (so even a race that slips through doesn't set the wrong image). Both are needed because cancellation isn't instantaneous and races can occur. More broadly, every image request must be *lifecycle-scoped* — cancelled on view-recycle *and* on lifecycle destruction — to avoid both the wrong-image bug and memory leaks/wasted work, which coroutines' structured cancellation models cleanly (scope the load to the target; rebinding or destruction cancels it). Adding request *dedup* handles the concurrency dimension (same image requested multiple times → load once). Understanding that this is fundamentally a *race between async loads and recyclable views*, and that the fix is *cancel-on-rebind + verify-on-complete + lifecycle-scoping*, is exactly the depth this question tests — it's the same class of problem as the callback-stale-capture and coroutine-cancellation concerns elsewhere, applied to image loading. And it reinforces *why* you use Coil/Glide: they solve this correctly, and hand-rolling it is where the subtle wrong-image and leak bugs creep in."}]},{level:"senior",q:"Design the overall architecture of an image-loading library. What are the components?",a:[{t:"p",text:"**I'd decompose it into: a *request* model, a *cache* subsystem (memory + disk), a *fetcher* (gets the raw bytes), a *decoder* (bytes → downsampled bitmap), a *lifecycle/cancellation* manager, a *dispatcher* (threading), and pluggable *transformations* — orchestrated by a loader that runs the request through the cache hierarchy with proper threading and lifecycle handling. This mirrors how Coil and Glide are actually structured.**"},{t:"list",items:["**Request** — models a single load: the source (URL/file/resource), the *target* (ImageView/Composable), the *target size*, transformations, placeholder/error images, and the lifecycle owner. This captures everything needed to fulfill and cancel one load.","**Cache subsystem** — the multi-tier cache: a *memory cache* (`LruCache` of decoded bitmaps, bounded, for instant hits) and a *disk cache* (LRU of encoded bytes, persistent, avoids re-download). The loader checks memory → disk → network and populates back up.","**Fetcher** — retrieves the raw *encoded bytes* from the source, pluggable per source type: a network fetcher (OkHttp/Ktor with the disk cache), a file fetcher, a content-URI fetcher, a resource fetcher. Extensibility comes from registering fetchers for different source types.","**Decoder** — turns encoded bytes into a *bitmap*, *downsampled to the target size* (the memory-safety step), pluggable per format (JPEG/PNG, GIF, SVG, video-frame, etc.). Applies the target size and config (RGB_565 etc.).","**Transformations** — pluggable post-decode effects (resize, center-crop, rounded corners, blur, grayscale) applied to the bitmap, and factored into the cache key (a transformed image is a distinct cache entry).","**Dispatcher / threading** — runs fetch + decode off the main thread (coroutine dispatchers or a thread pool) and delivers the result on the main thread. Manages concurrency limits (don't decode 100 images at once).","**Lifecycle/cancellation manager** — ties each request to its target's/owner's lifecycle, cancels on view-recycle and destruction, and handles request dedup (identical in-flight requests share one execution)."]},{t:"list",items:["**The orchestration (the load flow)**: `load(request)` → compute the cache key (URL + size + transformations) → check *memory cache* (instant deliver if hit) → else launch a *cancellable* coroutine scoped to the request's lifecycle → check *disk cache*, else *fetch* from network (deduped, populating disk) → *decode* downsampled to target size → apply *transformations* → populate *memory cache* → deliver on *main thread* *if the target is still valid* (tag check). Placeholder shows during load; error image on failure.","**Design qualities**: the *pluggable* fetchers/decoders/transformations make it an *open, extensible* architecture (handle new sources/formats/effects without changing the core). The *cache key* (source + size + transformations) ensures correctness (differently-sized or transformed versions are distinct entries). The *separation of concerns* (fetch vs decode vs cache vs threading vs lifecycle) makes each part testable and replaceable.","**Trade-offs to mention**: memory cache size (bigger = more hits but more RAM pressure — tune to the memory budget); disk cache size (persistence vs storage); RGB_565 vs ARGB_8888 (memory vs quality); how aggressively to preload/prefetch (smoother scrolling vs wasted bandwidth). And the meta-trade-off: you'd *use Coil/Glide*, not build this — the exercise is understanding the design, because getting all these pieces right (especially memory management, cancellation, and the cache hierarchy) is substantial, subtle work the libraries have already solved."]},{t:"p",text:"**The senior framing**: designing an image loader is really about *decomposing a deceptively-simple task ('show this URL as an image') into the concerns it actually involves* — caching (the multi-tier hierarchy for speed), memory management (downsampling + bounded caches to avoid OOM), threading (off-main fetch/decode, on-main delivery, to avoid jank), lifecycle/cancellation (scope to the target, cancel on recycle/destroy, to avoid the wrong-image bug and leaks), and extensibility (pluggable fetchers/decoders/transformations). The clean architecture separates these into composable components (request, cache, fetcher, decoder, dispatcher, lifecycle manager, transformations) orchestrated by a loader that runs the cache hierarchy with correct threading and lifecycle. The *cache key* (source + size + transformations) is a subtle but crucial correctness detail. This decomposition mirrors the real libraries because those concerns are intrinsic to the problem — which is exactly why the question is a good test: it reveals whether you understand that 'load an image' encompasses caching, memory, threading, and lifecycle, and can architect a clean separation of those concerns. And the honest conclusion — that you'd *use* Coil/Glide because this is solved, hard work — demonstrates the judgment to not reinvent solved problems while understanding *why* they're designed as they are. Demonstrating the component decomposition, the orchestrated load flow, the cache-key correctness detail, the trade-offs, and the use-a-library conclusion is the comprehensive senior answer."}]}],jT={id:"image-loader",title:"Design an Image-Loading Library",tagline:"The cache hierarchy (memory/disk/network), bitmap memory management & downsampling, threading, request cancellation & the wrong-image bug, and the architecture.",tags:["System Design","Image Loading","Caching","Memory","Coil/Glide"],content:FT,qa:LT},VT=[{heading:"The problem — a fully offline-capable app",blocks:[{t:"p",text:"'Design an offline-first sync system' (like a notes app, a to-do app, or an email client that works fully offline) is a rich mobile system design question because it forces you to handle the *hard* half of offline: not just *reading* offline (serve from cache), but *writing* offline and *synchronizing* those changes with the server when connectivity returns — including *conflict resolution* when the same data changed in two places. It's a distributed-systems problem on a mobile client."},{t:"list",items:["**Requirements**: the app works *fully* offline — read *and* write. Changes made offline persist and sync automatically when back online. Data stays consistent across the user's devices and the server. The UI is responsive (never blocked on the network).","**The easy half — offline reads**: serve data from a local database (SSOT). This is the standard offline-first pattern (covered in Data/Caching).","**The hard half — offline writes + sync**: a user edits a note offline; that change must be stored locally, reflected in the UI immediately, and *pushed to the server* when online — reliably (surviving app kills), and *reconciled* if the server's version also changed. This is where the design gets interesting."]}]},{heading:"The foundation — SSOT and local-first writes",blocks:[{t:"p",text:"The architecture is built on the **single source of truth** (a local database, usually Room) that the UI *always* reads from, combined with **local-first (optimistic) writes**: a write is applied to the local DB *immediately* (so the UI updates and the user isn't blocked), and *then* synced to the server in the background."},{t:"code",title:"Local-first write with a sync queue",code:`// Entity carries sync metadata
data class NoteEntity(
    @PrimaryKey val id: String,
    val content: String,
    val updatedAt: Long,
    val syncStatus: SyncStatus,   // PENDING, SYNCED, or CONFLICT
    val version: Int,             // for conflict detection
)

suspend fun updateNote(id: String, content: String) {
    // 1. Write locally FIRST — UI updates instantly (optimistic)
    dao.update(id, content, updatedAt = now(), syncStatus = PENDING)
    // 2. Trigger background sync (WorkManager — survives app kill)
    scheduleSyncWork()
}`},{t:"list",items:["**SSOT (local DB)** — the UI observes the DB (via Flow), so it always shows local data (offline-capable), and writes go *into* the DB. The server is a *sync partner*, not the read source.","**Optimistic local writes** — apply the change locally immediately and mark it `PENDING` sync. The UI reflects it instantly (responsive, works offline), and the actual server sync happens later/in the background. The user never waits on the network to make a change.","**Sync metadata on each record** — a `syncStatus` (pending/synced/conflict) and a `version`/timestamp, so the sync engine knows what needs pushing and can detect conflicts."]}]},{heading:"The sync engine and the outbox pattern",blocks:[{t:"p",text:"The **sync engine** pushes local changes to the server and pulls remote changes down. For *reliability* — so offline changes are never lost even if the app is killed — you use an **outbox pattern**: queue outgoing operations durably (in the DB), and a background worker processes the queue, retrying until each succeeds."},{t:"list",items:["**Outbox pattern** — every offline mutation is recorded as a durable *operation* (in an outbox table or via the record's `PENDING` status), so it survives app kills and reboots. A background sync reads pending operations and pushes them to the server, marking them synced on success. This *guarantees eventual delivery* — no offline change is silently lost.","**WorkManager for the sync worker** — the sync runs in a WorkManager job with a *network constraint*, so it *automatically runs when connectivity returns*, survives process death/reboot, and retries with backoff on failure. This is exactly WorkManager's purpose (guaranteed, deferrable, network-aware work) — a plain coroutine couldn't guarantee the sync happens.","**Push then pull** — sync typically *pushes* local pending changes up, then *pulls* remote changes down (updating the local DB, which the UI observes). The pull can be *full* (fetch everything) or *incremental/delta* (fetch only changes since the last sync — far more efficient).","**Idempotency** — each operation carries a client-generated id so retries (WorkManager may retry after an ambiguous failure) don't duplicate the action server-side — the server dedupes by the id. Essential because mobile failures are often ambiguous (did the request go through?)."]}]},{heading:"Conflict resolution — the hardest part",blocks:[{t:"p",text:"The genuinely hard problem: while a user edited a note offline, the *same note* may have changed on the server (edited on another device). When syncing, you have two divergent versions — a **conflict** — and need a *policy* to reconcile them. There's no universal right answer; it depends on the data's semantics."},{t:"table",headers:["Strategy","How it resolves a conflict","Trade-off"],rows:[["Last-write-wins (LWW)","compare timestamps; the newest edit wins, discarding the other","simple, but *loses* the other edit — data loss"],["Server-wins / client-wins","one side always wins","simple, predictable, but arbitrary loss"],["Field-level merge","merge non-conflicting field changes; conflict only on the same field","preserves more, more complex"],["CRDTs / operational transform","data structures that merge automatically (collaborative editing)","powerful (no loss), complex — for real-time collab (Google Docs)"],["Manual / user-resolved","present both versions, let the user choose/merge","no data loss, but interrupts the user"]]},{t:"list",items:["**Detecting a conflict** — compare *versions*: each record has a version/timestamp; the server rejects a push whose *base version* doesn't match the current server version (optimistic concurrency), signaling a conflict. Or compare on pull (local `PENDING` + remote changed = conflict).","**Last-write-wins** — the simplest: newest timestamp wins. Acceptable for single-user, low-conflict data (a personal note edited on one device at a time), but it *silently loses* the losing edit — bad for important data.","**Field-level merge** — if two devices edited *different fields* of the same record, merge both changes (no real conflict); only a genuine same-field conflict needs resolution. Preserves more data, more complex.","**CRDTs / OT** — conflict-free replicated data types (or operational transformation) *automatically merge* concurrent edits with no data loss — the basis of real-time collaborative editing (Google Docs, Figma). Powerful but complex; for genuine multi-user real-time collaboration.","**Manual resolution** — present the conflicting versions and let the *user* decide/merge. No data loss, but interrupts them — appropriate for high-value data where automatic resolution risks losing something important.","**The judgment**: pick the strategy matching the data — LWW for simple single-user data, field-merge for structured multi-field records, CRDTs for real-time collaboration, manual for critical data. Stating that conflict resolution *depends on data semantics* (not a one-size-fits-all) is the senior signal."]}]},{heading:"Putting it together and edge cases",blocks:[{t:"list",items:["**Full flow**: read from local DB (offline-capable) → write locally + mark PENDING (optimistic, instant UI) → WorkManager sync worker (network-constrained) pushes PENDING ops (idempotent) → server accepts or reports conflict → resolve per policy → pull remote changes (delta) into the DB → UI updates via the observed Flow. Rinse and repeat on each connectivity change.","**Optimistic UI + rollback** — since you apply changes before server confirmation, handle *rejection* (validation failure, lost conflict): roll back the local change or surface an error, keeping the user informed.","**Delta sync** — sync only *changes* since the last sync (using a sync token/timestamp), not the whole dataset — critical for efficiency (bandwidth, battery) with large data.","**Deletion handling** — deletes are tricky: a hard-delete locally that isn't synced could resurrect on the next pull. Use *soft deletes* (a `deleted` flag) synced like other changes, or tombstones, so deletions propagate correctly.","**Ordering & dependencies** — if offline operations have dependencies (create a note, then edit it), sync them in order; the outbox preserves ordering.","**Real-time layer (optional)** — for faster convergence, add a push (FCM) or WebSocket to *notify* the app of remote changes so it syncs promptly, rather than only on app open — combining offline-first durability with near-real-time freshness."]},{t:"note",text:"Offline-first sync: foundation = SSOT (local DB, UI reads it — offline reads) + optimistic local-first writes (apply locally + mark PENDING → instant UI). Sync engine + OUTBOX pattern (durable queue of ops, survives kill) via WorkManager (network-constrained, guaranteed, retries) — push PENDING (idempotent ids) then pull (delta). CONFLICT RESOLUTION is the hard part (same data changed both places): last-write-wins (simple, data loss), field-merge, CRDTs (auto-merge, for real-time collab), or manual — choose by DATA SEMANTICS. Edge cases: optimistic rollback on rejection, delta sync, soft deletes (avoid resurrection), operation ordering, optional push/WebSocket for freshness."}]}],NT=[{level:"junior",q:"What's the difference between offline reads and offline writes, and why are writes harder?",a:[{t:"p",text:"**Offline *reads* mean showing data when there's no network — easy: serve it from a local cache/database. Offline *writes* mean letting the user *change* data offline and then *synchronizing* those changes with the server when connectivity returns — much harder, because it's a distributed-systems problem involving reliable delivery and conflict resolution.**"},{t:"list",items:["**Offline reads (easy)**: use a local database as the single source of truth. The UI always reads from it, so data displays with or without a network. The network just *updates* the local DB. This is the standard offline-first read pattern.","**Offline writes (hard)**: the user edits something offline. That change must (1) persist locally and show in the UI immediately, (2) *reliably reach the server* when back online — surviving app kills, retrying on failure, and (3) be *reconciled* if the same data changed on the server meanwhile (a conflict)."]},{t:"p",text:"Writes are harder because they turn the app into a *distributed system* with two (or more) copies of data that can *diverge*. Reads just display a cached copy — nothing diverges. But an offline write creates a local version that must eventually merge with the server's version, and if both changed (e.g. edited on two devices), you have a genuine conflict with no obvious resolution. Plus, the sync must be *reliable* — a change made offline can't be lost just because the app was killed before it synced. So offline writes require: local-first optimistic writes (instant UI), a durable sync queue (the outbox pattern, so changes survive), background sync via WorkManager (guaranteed, network-aware), idempotency (safe retries), and a conflict-resolution policy. This is why 'design offline-first sync' is a rich question — the write/sync half is where all the interesting distributed-systems challenges live."}]},{level:"junior",q:"What is optimistic (local-first) writing?",a:[{t:"p",text:"**Optimistic (local-first) writing means applying a change to the local database *immediately* — so the UI updates instantly and the user isn't blocked — and syncing it to the server *later* in the background, rather than waiting for the server to confirm before showing the change.** You 'optimistically' assume the write will succeed and reflect it right away."},{t:"list",items:["**The flow**: the user makes a change → write it to the local DB immediately and mark it as `PENDING` sync → the UI (observing the DB) updates instantly → a background sync later pushes it to the server.","**Why**: it makes the app *responsive* and *offline-capable*. The user's action takes effect immediately regardless of network — no spinner waiting for the server, and it works with no connection at all. The network round-trip happens invisibly in the background.","**The trade-off**: since you show the change before the server confirms it, you must handle the case where the server later *rejects* it (validation failure, a lost conflict) — by rolling back the local change or showing an error. This is 'optimistic UI with rollback'."]},{t:"p",text:"The alternative — *pessimistic* writing (wait for the server to confirm before updating the UI) — is simpler to reason about but gives a worse experience: the user waits on every action, and it doesn't work offline at all. Optimistic writing is essential for offline-first because the whole point is that the app works without waiting on the network. The pattern pairs with the single-source-of-truth design: writes go into the local DB (which the UI observes), marked pending, and a background sync engine (WorkManager) reliably pushes them to the server, handling retries and conflicts. So optimistic local writes give the *responsiveness and offline capability*, while the background sync gives the *eventual consistency* with the server."}]},{level:"junior",q:"Why use WorkManager for syncing offline changes?",a:[{t:"p",text:"**Because syncing offline changes must be *guaranteed* (it can't be lost if the app is killed), *network-aware* (it should run when connectivity returns), and *retried* on failure — which is exactly what WorkManager provides. A plain coroutine can't guarantee the sync happens, because it dies with the process.**"},{t:"list",items:["**Guaranteed execution** — WorkManager persists the work request in its database, so the sync survives the app being killed and even a device reboot. An offline change queued for sync will *eventually* sync, no matter what happens to the app. A coroutine in `viewModelScope` would be cancelled when the screen/app goes away, losing the sync.","**Network-aware** — you add a network *constraint*, so WorkManager automatically runs the sync *when connectivity returns* — you don't have to poll for connectivity or manually retry when the network comes back.","**Automatic retry with backoff** — if a sync attempt fails (transient server error, connection drop), WorkManager retries with exponential backoff, so a temporary failure doesn't lose the change."]},{t:"p",text:"The core requirement offline sync has is *reliability* — a change the user made offline must not be silently lost, and it must sync as soon as it can. WorkManager is purpose-built for exactly this: *deferrable, guaranteed, constraint-aware* background work that survives process death and reboots. This is why it's the right tool over a coroutine (which is tied to a lifecycle and dies with it) or a background service (killed by Android 8+ restrictions, gone on reboot). The design pairs WorkManager with the outbox pattern — durable operations queued in the DB — so the worker reads pending operations and pushes them, WorkManager guaranteeing the worker eventually runs. Together they ensure *eventual delivery*: no offline change is lost, and sync happens automatically when the network is available."}]},{level:"senior",q:"How do you handle conflict resolution in a sync system?",a:[{t:"p",text:"**A conflict occurs when the same data changed in two places (e.g. edited offline on one device while also edited on the server/another device), producing two divergent versions that must be reconciled. There's no universal solution — you choose a resolution *strategy* based on the *data's semantics*: last-write-wins, field-level merge, CRDTs, or manual resolution. Stating that the strategy depends on the data (not one-size-fits-all) is the key insight.**"},{t:"list",items:["**First — detecting the conflict**: each record carries a *version* (or timestamp). The client sends its *base version* with a push; the server accepts only if that matches the current server version (optimistic concurrency) — a mismatch means the server changed since the client last synced, i.e. a conflict. Alternatively, detect on pull (local `PENDING` change + a changed remote version).","**Last-write-wins (LWW)** — the simplest: whichever edit has the newer timestamp wins, discarding the other. Fine for *single-user, low-conflict* data (a personal note usually edited on one device at a time), but it *silently loses* the losing edit — unacceptable for important data where losing a change matters.","**Server-wins / client-wins** — one side always wins. Simple and predictable, but arbitrarily discards the other side's changes.","**Field-level merge** — if the two versions changed *different fields*, merge both changes (there's no real conflict — one changed the title, the other the body). Only a genuine *same-field* conflict needs a resolution decision. Preserves far more data than LWW; more complex to implement (track per-field changes).","**CRDTs / Operational Transformation** — conflict-free replicated data types (or OT) are data structures/algorithms that *automatically merge* concurrent edits with *no data loss* — the foundation of real-time collaborative editing (Google Docs, Figma). Very powerful (concurrent edits just merge correctly) but complex; appropriate for genuine multi-user real-time collaboration.","**Manual / user-resolved** — present both conflicting versions and let the *user* choose or merge. No data loss, but it interrupts the user — appropriate for high-value data where automatic resolution risks losing something important (like a document the user cares deeply about)."]},{t:"list",items:["**Choosing by data semantics**: LWW for simple single-user data where conflicts are rare and occasional loss is tolerable; field-merge for structured records with independent fields; CRDTs for real-time collaborative editing; manual for critical data where no automatic policy is safe. The *nature and value of the data* dictates the choice — a to-do checkbox can use LWW, a collaborative document needs CRDTs, a legal contract might need manual resolution.","**Practical considerations**: mark conflicted records with a `CONFLICT` status so they're surfaced (for manual resolution) or handled by the policy; log conflicts for debugging; and design to *minimize* conflicts where possible (fine-grained records reduce the chance two edits collide; frequent syncing reduces the divergence window)."]},{t:"p",text:"**The senior framing**: conflict resolution is the genuinely *hard* part of offline sync because it's a *distributed-systems* problem — two replicas of data diverged, and merging them is fundamentally ambiguous when both changed. The crucial insight (and the senior signal) is that *there is no universal right answer — the strategy must match the data's semantics and value*: last-write-wins for simple single-user data (accepting possible loss for simplicity), field-level merge for independent-field records (preserving more), CRDTs for real-time collaboration (automatic, lossless, complex), and manual resolution for critical data (no loss, but user-interrupting). Detection relies on *versioning* (optimistic concurrency — reject pushes with a stale base version). Around the core, you minimize conflicts (fine-grained records, frequent sync), surface unresolvable ones (CONFLICT status), and handle the practical edges (deletion/tombstones, ordering). The immature answer is 'just use last-write-wins' as if it's universal; the mature answer *analyzes the data* — 'for a personal note, LWW is fine and simple; but if this were collaborative editing, LWW would lose edits and I'd need CRDTs; and for critical data I'd surface it to the user' — demonstrating that you understand conflict resolution as a *contextual trade-off between simplicity and data preservation* rooted in what the data *is*. Demonstrating the strategies, the detection-via-versioning, the choose-by-data-semantics principle, and the conflict-minimization practices is the comprehensive senior answer to the hardest part of offline sync."}]},{level:"senior",q:"Design the full architecture of an offline-first note-taking app that syncs across devices.",a:[{t:"p",text:"**I'd build it on a single-source-of-truth local database with optimistic local-first writes, a durable outbox-based sync engine driven by WorkManager, versioning for conflict detection with a data-appropriate resolution policy, and delta sync for efficiency — plus a push/WebSocket layer for near-real-time convergence across devices. Let me lay out the layers and the sync flow.**"},{t:"list",items:["**Local database as SSOT (Room)**: notes stored locally with sync metadata per record — `id`, `content`, `updatedAt`, `version`, `syncStatus` (SYNCED/PENDING/CONFLICT), and a `deleted` soft-delete flag. The UI *always* observes the DB (via Flow), so notes display instantly and fully offline. This is the read foundation.","**Optimistic local-first writes**: creating/editing/deleting a note writes to the DB *immediately* (marked PENDING), so the UI updates instantly and the user never waits on the network. Deletes are *soft* (set `deleted = true`, PENDING) so the deletion syncs like any change (a hard local delete could resurrect on the next pull).","**Outbox + WorkManager sync engine**: pending changes form a durable queue (the PENDING records, or a separate outbox table preserving operation order). A WorkManager worker with a *network constraint* runs the sync — guaranteed (survives app kill/reboot), automatic on reconnect, retried with backoff. Each operation carries a *client-generated id* for idempotency (safe retries — the server dedupes).","**Sync flow — push then pull**: (1) *Push* PENDING changes to the server, sending each note's *base version*; the server accepts (bumping the version, returning it → mark SYNCED) or *rejects with a conflict* if its version changed. (2) *Pull* remote changes via *delta sync* — request changes since the last sync token/timestamp, apply them to the local DB (which the UI observes). Delta sync (not full fetch) keeps it efficient for large note sets.","**Conflict resolution**: on a rejected push, detect the conflict via version mismatch. For a *personal* note-taking app (mostly single-user, edited on one device at a time), *last-write-wins by `updatedAt`* is a reasonable default (simple, rare conflicts) — but I'd note that if notes could be *collaboratively* edited, LWW would lose edits and I'd move to field/text-level merge or CRDTs. For safety on important notes, I could mark genuine conflicts `CONFLICT` and surface both versions for the user to resolve, avoiding silent loss.","**Optimistic UI + rollback**: since changes show before server confirmation, handle rejections (validation, lost conflict) by rolling back or flagging the note, keeping the user informed.","**Near-real-time cross-device convergence**: add an FCM push (or WebSocket) so that when a note changes on one device, the server *notifies* the user's other devices to sync promptly — rather than only syncing on app open. This combines offline-first *durability* with near-real-time *freshness* across devices."]},{t:"list",items:["**Edge cases I'd address**: *deletion* (soft deletes/tombstones to prevent resurrection); *operation ordering* (create-then-edit must sync in order — the outbox preserves it); *idempotency* (client ids so retries don't duplicate); *conflict minimization* (fine-grained notes, frequent sync to shrink the divergence window); *first sync / large data* (paginate the initial pull); and *auth* (the sync worker needs a valid token — refresh if expired).","**Trade-offs to state**: LWW (simple, possible loss) vs merge/manual (safer, complex) — chosen for the *personal-notes* semantics but flagged as data-dependent; delta vs full sync (efficiency vs simplicity — delta for scale); soft vs hard deletes (correctness vs storage); and push/WebSocket for freshness (complexity + battery vs faster convergence — worth it for multi-device)."]},{t:"p",text:"**The senior framing**: the design assembles the offline-first toolkit into a coherent whole — *SSOT local DB* (offline reads + the UI's data source), *optimistic local-first writes* (responsive, offline-capable, with rollback), a *durable outbox + WorkManager sync engine* (guaranteed eventual delivery surviving app kills, running on reconnect, retrying, idempotent), *versioning-based conflict detection* with a *data-appropriate resolution policy* (LWW for personal notes, but explicitly flagged as depending on whether editing is collaborative), *delta sync* (efficiency at scale), *soft deletes and operation ordering* (correctness edges), and an optional *push/WebSocket* layer (near-real-time cross-device freshness). The unifying architecture is that the *local DB is authoritative and the UI reads it*, while a *reliable background sync engine* reconciles it with the server — decoupling the user's experience (instant, offline) from the network reality (eventual, reconciled). The senior signals are: recognizing offline *writes* as a distributed-systems problem (not just caching), choosing conflict resolution *by data semantics* (and saying so — LWW here but CRDTs if collaborative), handling the *failure/edge cases* (kill-survival, deletes, ordering, idempotency, rollback) rather than just the happy path, and stating the *trade-offs* explicitly. This demonstrates the ability to design a genuinely robust offline-first system — combining the SSOT/caching, WorkManager, and conflict-resolution concepts into a real, failure-aware architecture — which is exactly what this rich system-design question tests."}]}],WT={id:"offline-sync",title:"Design Offline-First Sync",tagline:"SSOT & optimistic local-first writes, the outbox pattern & WorkManager sync engine, conflict resolution strategies, delta sync, and edge cases.",tags:["System Design","Offline-First","Sync","Conflict Resolution","Outbox"],content:VT,qa:NT},BT=[{heading:"Designing a paginated feed",blocks:[{t:"p",text:"'Design a news/social feed' (Twitter, Instagram, a news app) is a very common question. The core challenges: an *effectively infinite* list (can't load it all — paginate), *smooth scrolling* (efficient rendering, image loading), *caching/offline* (show cached content instantly and offline), and *freshness* (new content, pull-to-refresh). It brings together pagination, caching/SSOT, image loading, and rendering performance."},{t:"list",items:["**Pagination** — the feed is unbounded, so load it in *pages* as the user scrolls (cursor-based pagination: `GET /feed?cursor=X&limit=20` returning items + a next-cursor). Cursor-based is preferred over offset-based for feeds because it's stable when items are inserted/removed (offset pagination duplicates/skips items when the list shifts).","**Architecture** — Paging 3 with a `RemoteMediator` + Room: the network fills Room *page by page*, and the UI reads *from Room*. This gives *offline-capable pagination* — cached pages show instantly and offline; scrolling fetches new pages when online (the SSOT/offline-first pattern applied to an infinite list).","**Rendering** — a `LazyColumn` (or RecyclerView) that recycles views (only visible items composed/bound), with stable *keys* (so inserts don't cascade recomposition), and a caching *image loader* (Coil) with downsampling for feed images. This keeps memory bounded and scrolling smooth.","**Freshness** — pull-to-refresh (fetch the newest page, prepend to Room); optionally a 'new posts' indicator; and for real-time feeds, a push/WebSocket to signal new content. Cache invalidation via TTL or refresh-on-open."]}]},{heading:"Feed deep-dives: memory, prefetch, and mixed content",blocks:[{t:"list",items:["**Bounded memory** — never hold the whole feed in memory. Paging keeps only a window of items in memory (dropping far-away pages), the list recycles views, and images are downsampled + LRU-cached. So even a feed scrolled for hours stays memory-safe. This is the key mobile constraint for a feed.","**Prefetch** — load the *next* page slightly *before* the user reaches the end (Paging's `prefetchDistance`), so the next content is ready when they get there — smooth infinite scroll with no visible loading pause. Balance against wasted data (don't prefetch too far).","**Mixed content types** — feeds have heterogeneous items (text posts, image posts, videos, ads). Model as a sealed type and render each variant (`when (item) { is Post -> ...; is Ad -> ... }`); use `contentType` in the lazy list for efficient view recycling per type.","**Image/video optimization** — images downsampled to display size (Coil); videos loaded lazily and only played when visible (autoplay-on-scroll with lifecycle-aware players); placeholders during load to avoid layout jank.","**Read state / analytics** — track which items were seen (impressions) for analytics, and read state if relevant — efficiently (batch, don't fire per item).","**Trade-offs** — Paging 3 + RemoteMediator (offline, robust, but complex) vs a simpler in-memory paginated list (simpler, no offline); cursor vs offset pagination (stability vs simplicity); how much to prefetch/cache (smoothness vs data/battery)."]}]},{heading:"Designing a chat feature",blocks:[{t:"p",text:"'Design a chat/messaging feature' is a rich question because it needs *real-time* delivery, *offline* support (send/read messages offline), *reliable* delivery (messages must not be lost), *ordering*, and *state* (sent/delivered/read). It combines real-time (WebSockets/push), offline-first, and sync."},{t:"list",items:["**Real-time transport** — a *WebSocket* (persistent bidirectional connection) for live message send/receive while the app is active, so messages arrive instantly with no polling. Plus *FCM push* for delivering messages/notifications when the app is *backgrounded* (a WebSocket isn't kept alive in the background). So: WebSocket when active, push when backgrounded.","**Local DB as SSOT** — messages stored in Room; the chat UI *observes* Room (so it shows history offline and updates live). Incoming messages (from WebSocket/push/sync) write to Room → UI updates. This is the offline-first foundation.","**Sending messages — optimistic + reliable**: on send, write the message to Room immediately with status `SENDING` (shows instantly in the chat, optimistic), then send via WebSocket (or queue for sync if offline). On server ack, update to `SENT`; on delivery/read receipts, `DELIVERED`/`READ`. If sending fails or offline, it stays queued (outbox) and retries — so a message sent offline reliably delivers when back online.","**Message status** — the sent/delivered/read lifecycle (the checkmarks) — each message carries a status updated by acks and receipts from the server."]}]},{heading:"Chat deep-dives: ordering, reliability, and sync",blocks:[{t:"list",items:["**Ordering** — messages must display in the correct order despite async delivery and clock differences. Use *server-assigned* sequence numbers or timestamps (not just client time, which varies across devices) to order messages consistently. A locally-sent message shows immediately (optimistic) and settles into the correct position when the server assigns its sequence.","**Reliable delivery — push is best-effort, so sync is the backbone**: WebSocket/push can *drop* or *delay* messages (device offline, connection lost), so they are *not* the reliable transport. The *source of truth* is the server + local DB — when the app opens/reconnects, it *syncs missed messages* (fetch messages since the last-known message id). So push/WebSocket give *timely* delivery, but *sync* guarantees *completeness* — never treat a push as the only way a message arrives (a dropped push must not mean a lost message).","**Pagination of history** — a chat can have thousands of messages; load them paginated (load recent, fetch older as the user scrolls up — reverse pagination). Room + Paging, reading from the local cache.","**Offline sending** — messages sent offline queue in the outbox (Room, `SENDING`/`PENDING`), and a sync/WorkManager job (or the WebSocket on reconnect) sends them when online, with idempotency ids so retries don't duplicate. Same offline-write pattern as the sync topic.","**Deduplication** — because a message might arrive via *both* WebSocket and sync (or a retry), dedup by message id so it's not shown twice.","**Typing indicators / presence** — ephemeral real-time signals (typing, online status) sent over the WebSocket, *not* persisted (they're transient) — a lighter-weight real-time channel alongside message delivery.","**Group chat / scale** — fan-out (deliver to all members' devices), read receipts per member, and efficient delivery (the server handles fan-out; the client receives its messages)."]},{t:"note",text:"Feed: unbounded → paginate (cursor-based, stable); Paging 3 + RemoteMediator + Room = offline-capable pagination (network fills Room, UI reads Room); LazyColumn recycling + keys + Coil (downsampled) for smooth memory-safe scrolling; prefetch for seamless infinite scroll; sealed types + contentType for mixed items; pull-to-refresh + optional push for freshness. Chat: WebSocket (active) + FCM push (backgrounded) for real-time; Room SSOT (offline history + live updates); optimistic send (SENDING → SENT → DELIVERED → READ) + outbox for offline; ORDER by server sequence/timestamp; push is BEST-EFFORT so SYNC on reconnect guarantees completeness (dropped push ≠ lost message); paginate history; dedup by id; ephemeral typing/presence over WebSocket."}]}],zT=[{level:"junior",q:"How would you design an infinitely-scrolling news feed?",a:[{t:"p",text:"**The feed is effectively infinite, so you *paginate* — load it in pages as the user scrolls — and use a local database as the source of truth so the feed is fast and works offline. The standard architecture is Paging 3 + a RemoteMediator + Room: the network fills Room page by page, and the UI reads from Room.**"},{t:"list",items:["**Pagination** — load pages of ~20 items as the user approaches the end (cursor-based: `GET /feed?cursor=X&limit=20`), rather than loading everything (impossible) or all upfront (slow, memory-heavy).","**Offline-capable via Room SSOT** — the network writes pages *into Room*, and the UI reads *from Room* (via Paging). So cached pages show instantly and offline, and scrolling fetches new pages when online — the offline-first pattern applied to an infinite list. Paging 3's `RemoteMediator` orchestrates this.","**Smooth rendering** — a LazyColumn (or RecyclerView) recycles views (only visible items are composed), stable *keys* prevent cascade recomposition on updates, and a caching image loader (Coil) with downsampling keeps feed images memory-safe.","**Freshness** — pull-to-refresh fetches the newest page; cache invalidation via TTL or refresh-on-open; optionally push for real-time new content."]},{t:"p",text:"The key mobile constraints are *bounded memory* (never hold the whole feed — Paging keeps a window in memory, the list recycles, images are downsampled) and *smooth scrolling* (cheap item binds, prefetch the next page before the user reaches it). So the design combines pagination (for the infinite list), SSOT/Room (for offline + consistency), efficient rendering (LazyColumn + keys + Coil), and prefetch (for seamless scroll). This grounds the feed in the established Android toolkit — Paging 3, Room, Coil, coroutines/Flow — applied to the feed's specific challenges of infinite length, memory limits, and offline support."}]},{level:"junior",q:"What transport would you use for a real-time chat app?",a:[{t:"p",text:"**A combination: a WebSocket for real-time messaging while the app is *active* (a persistent bidirectional connection delivering messages instantly), and FCM push for delivering messages/notifications when the app is *backgrounded* (a WebSocket can't be kept alive in the background). So WebSocket when foregrounded, push when backgrounded.**"},{t:"list",items:["**WebSocket (active)** — a persistent, bidirectional connection so messages send and arrive *instantly* with no polling, and the server can push messages to the client in real time. Ideal while the user is in the chat.","**FCM push (backgrounded)** — when the app is in the background, you can't keep a WebSocket alive (battery, OS limits), so the server sends messages via push notification. High-priority pushes can even wake the app to sync.","**Local DB (Room) as the source of truth** — incoming messages (from WebSocket, push, or sync) write to Room, and the chat UI observes Room — so it shows offline history and updates live."]},{t:"p",text:"The critical insight beyond the transport choice is that *push and WebSocket are best-effort, not reliable* — they can drop or delay messages (device offline, connection lost). So they must *not* be the sole delivery mechanism. The reliable backbone is *sync*: when the app opens or reconnects, it fetches any missed messages from the server (messages since the last-known id), guaranteeing completeness. So WebSocket/push give *timely* delivery, and *sync* guarantees *nothing is lost* — a dropped push never means a lost message, because the app will sync it. This decoupling of *notification* (real-time but unreliable) from *message delivery* (reliable via sync from the server-and-DB source of truth) is the key architectural principle for reliable chat."}]},{level:"junior",q:"How do you handle sending a message so it feels instant but is reliable?",a:[{t:"p",text:"**You use optimistic sending with a reliable outbox: write the message to the local DB *immediately* with a `SENDING` status (so it appears in the chat instantly), then send it via WebSocket (or queue it if offline); update its status as acknowledgments come back (`SENT` → `DELIVERED` → `READ`), and retry from the queue if it fails.**"},{t:"list",items:["**Optimistic display** — on send, immediately write the message to Room (marked `SENDING`) so the UI (observing Room) shows it *instantly*, with no waiting on the network. This is what makes sending feel immediate, even offline.","**Reliable delivery** — the message is durably queued (in Room / an outbox), so if the send fails or the user is offline, it's *not lost* — a sync/WorkManager job (or the WebSocket on reconnect) retries sending it when online. It carries an *idempotency id* so retries don't create duplicates server-side.","**Status lifecycle** — the server acks the send (→ `SENT`), then delivery/read receipts update it (→ `DELIVERED` → `READ`) — the checkmarks users see. If it permanently fails, mark it `FAILED` with a retry option."]},{t:"p",text:"This is the same optimistic-local-first-write + outbox pattern as offline sync, applied to messages: the *local write* gives instant, offline-capable UX, and the *durable queue + background retry* gives reliability (the message eventually delivers). The message *appears* the moment you tap send (optimistic), reflects its true state via status updates, and is *guaranteed* to eventually send even if you were offline or the app was killed. The key is separating the *UX* (instant, via the local write) from the *delivery guarantee* (eventual, via the outbox + retry) — you don't block the user on the network, but you also don't lose their message. Handling the failure case (mark FAILED, allow retry) and idempotency (no duplicates on retry) completes the picture."}]},{level:"senior",q:"How do you ensure messages appear in the correct order and none are lost in a chat app?",a:[{t:"p",text:"**Ordering: use *server-assigned* sequence numbers (or server timestamps), not client clocks, so all devices order messages consistently. Reliability: treat WebSocket/push as best-effort *notification* and use *sync from the server as the source of truth* to guarantee completeness — on reconnect/open, fetch all messages since the last-known id, so a dropped push never means a lost message. Dedup by message id since a message may arrive via multiple paths.**"},{t:"list",items:["**Ordering via server sequence, not client time**: client clocks differ across devices (and can be wrong), so ordering by client timestamp produces inconsistent order. Instead, the *server* assigns each message a monotonic *sequence number* (or authoritative timestamp) when it receives it, and clients order by that. A locally-sent message shows *immediately* (optimistic, at the bottom) and *settles* into its correct position once the server assigns its sequence — so the ordering converges to the server's canonical order across all devices.","**Reliability — sync is the backbone, push is best-effort**: the crucial principle is that WebSocket and push can *drop or delay* messages (offline, connection loss, push throttling), so they are *not* a reliable transport. The reliable source of truth is the *server + local DB*. When the app opens or reconnects, it *syncs* — requests all messages *since the last message id it has* — filling any gaps. So push/WebSocket provide *timely* delivery when they work, and *sync* provides *completeness* regardless. A dropped push is fine because the next sync retrieves the message. Never treat a push/WebSocket message as the *only* way a message arrives.","**Gap detection**: because messages have sequence numbers, the client can *detect a gap* (received sequence 5 then 8 — missing 6, 7) and trigger a sync to fetch the missing ones, ensuring no silent holes in the conversation.","**Deduplication by id**: a message might arrive via WebSocket *and* via sync (or a retry sends it twice) — so dedup by the message's unique id (upsert into Room by id) to avoid showing it twice. Client-generated ids for sent messages also enable idempotent sending (the server dedupes retries)."]},{t:"list",items:["**Offline sending fits in**: messages sent offline queue in the outbox and send on reconnect with idempotency ids; the server assigns their sequence on receipt, so they slot into the correct order once delivered. So both *receiving* (sync fills gaps, sequence orders) and *sending* (outbox guarantees delivery, server assigns order) are covered.","**Read/delivery state consistency**: sync also reconciles message *status* (delivered/read) across devices, so a message read on one device shows read on others — another reason sync (not just real-time) is the backbone."]},{t:"p",text:"**The senior framing**: correct ordering and no-loss in chat come from two principles. *Ordering*: rely on *server-assigned sequence numbers*, not client clocks (which diverge) — the server is the ordering authority, and optimistically-shown local messages settle into the server's canonical order, so all devices converge. *No loss*: recognize that *real-time transport (WebSocket/push) is best-effort and unreliable*, so it cannot be the delivery *guarantee* — the guarantee comes from *sync against the server-and-local-DB source of truth*, which fills gaps on reconnect (detectable via sequence gaps) and reconciles state. This *decouples timeliness from completeness*: push/WebSocket make messages *fast*, sync makes them *complete*, and neither depends on the other. Deduplication by id handles the multiple-delivery-paths reality, and idempotency handles retries. The insight that distinguishes a senior answer is understanding that a chat app is fundamentally a *sync problem with a real-time optimization* — the server + local DB is the reliable source of truth reconciled via sync, and WebSocket/push is a *latency optimization* layered on top, *not* the reliability mechanism. Treating push as reliable (and thus losing messages when it drops) is the naive mistake; treating the app as 'sync for completeness + real-time for speed, ordered by server sequence, deduped by id' is the robust design. Demonstrating the server-sequence ordering, the push-is-best-effort/sync-is-the-backbone principle, gap detection, and deduplication is the comprehensive senior answer to chat reliability."}]},{level:"senior",q:"For a feed, what are the key performance and efficiency considerations, and how do you handle them?",a:[{t:"p",text:"**The key considerations are *bounded memory* (never hold the whole feed), *smooth scrolling* (cheap per-frame work, prefetch), *efficient networking* (paginate, cache, don't over-fetch), and *efficient media* (downsampled, lazily-loaded images/videos) — all stemming from the feed being effectively infinite on a resource-constrained device. Each has a specific technique.**"},{t:"list",items:["**Bounded memory** — the feed is unbounded, so you *cannot* hold it all in memory. Paging keeps only a *window* of loaded items in memory (dropping far-away pages), the LazyColumn/RecyclerView *recycles* views (only visible items are composed/bound), and images are *downsampled* to display size and LRU-cached. So even after scrolling for hours, memory stays bounded — no OOM. This is the primary mobile constraint for a feed.","**Smooth scrolling (frame budget)** — each item binds/composes *on the frame* as it scrolls in, so per-item work must be *cheap*: precompute/format data in the ViewModel (the UI gets render-ready data), keep item composables skippable (stable/immutable models, keys), and load images via a caching loader (not synchronously decoding on the bind). Heavy per-item work drops frames at the scroll edge. Also, judge scroll performance on *release* builds with baseline profiles (debug is janky by nature).","**Prefetch for seamless infinite scroll** — load the *next* page *before* the user reaches the end (Paging's `prefetchDistance`), so content is ready with no visible loading pause. Balance against wasted bandwidth (don't prefetch too aggressively on metered connections).","**Efficient networking** — paginate (fetch ~20 at a time, not the whole feed), cache pages in Room (avoid re-fetching what's cached — the SSOT gives instant cached reads and offline), use *cursor-based* pagination (stable under inserts), and support conditional/delta fetches if the API allows (don't re-download unchanged content). Respect the user's data plan.","**Efficient media** — feed images are the biggest data/memory cost: downsample to display size (Coil handles it), lazy-load (only fetch images for visible/near-visible items), use appropriate formats/quality, and for videos, load lazily and play only when visible (lifecycle-aware players, autoplay-on-scroll). Placeholders prevent layout jank during load."]},{t:"list",items:["**Mixed content efficiency** — heterogeneous items (posts, images, videos, ads) modeled as a sealed type with `contentType` in the lazy list, so view recycling is efficient per type (a post reuses a post's structure).","**Measurement** — profile scroll performance (Perfetto/Macrobenchmark on release builds), watch Layout Inspector recomposition counts, and add baseline profiles for the scroll path (often the biggest first-scroll-jank win). Optimize based on measurement, not guessing.","**Trade-offs** — how much to cache/prefetch (smoothness/offline vs storage/data/battery); Paging 3's robustness vs its complexity for simpler feeds; aggressive image quality vs data usage. State these as data-driven choices."]},{t:"p",text:"**The senior framing**: a feed's performance challenges all stem from one fact — it's *effectively infinite* running on a *memory-, battery-, and bandwidth-constrained device with a frame budget*. So the design is a set of *bounding and efficiency techniques*: *bound memory* (paging window + view recycling + image downsampling — never hold it all), *hit the frame budget* (cheap per-item binds via render-ready data and stable/skippable composables, plus baseline profiles), *bound network* (pagination + Room caching + cursor stability + delta where possible — don't over-fetch), and *bound media cost* (downsampled, lazily-loaded images/videos — the biggest consumer). Prefetch makes the infinite scroll *seamless*, mixed-content typing keeps recycling *efficient*, and *measurement* (Perfetto/Macrobenchmark on release) ensures you optimize the real bottleneck. The unifying insight is that every technique is about *not doing more than necessary given the constraints* — only load what's visible (+ a prefetch margin), only keep in memory what's near, only decode images to display size, only fetch pages as needed — which is exactly the discipline mobile's constraints demand. Connecting the feed's specific concerns (memory, frame budget, network, media) to concrete techniques (paging, recycling, downsampling, prefetch, caching, baseline profiles) grounded in the real toolkit (Paging 3, Room, Coil, LazyColumn), plus the measurement discipline and trade-offs, is the comprehensive senior answer that shows you understand feed performance as a bounded-resource problem, not just 'make it fast'."}]}],HT={id:"feed-chat",title:"Design a Feed & Chat",tagline:"Paginated feeds (Paging 3 + RemoteMediator, memory, prefetch, mixed content) and chat (WebSocket + push, optimistic send, ordering, sync-for-reliability).",tags:["System Design","Feed","Chat","Pagination","Real-time"],content:BT,qa:zT},KT={id:"system-design",name:"Mobile System Design",description:"Designing mobile systems — how to approach the interview, and worked designs: an image-loading library, offline-first sync, and a feed & chat.",topics:[OT,jT,WT,HT]},rg=[zx,{id:"architecture",name:"Architecture",description:"App architecture patterns and how they play out on Android & KMP — the layer every interview starts with.",topics:[cx,ux,fx,wx,kx,Ax]},bA,mS,FS,tA,FA,XA,hC,AC,LC,QC,cT,xT,ET,KT];function _T(t,i){const n=rg.find(r=>r.id===t),s=n==null?void 0:n.topics.find(r=>r.id===i);return n&&s?{category:n,topic:s}:null}function GT(){return g.jsx("div",{className:"noise relative min-h-screen bg-ink text-body",children:g.jsxs("div",{className:"mx-auto max-w-4xl px-6 py-16 sm:py-24",children:[g.jsx(Xn,{to:"/",className:"text-sm text-body/70 transition-colors hover:text-bright",children:"← Back to portfolio"}),g.jsxs("header",{className:"mt-10",children:[g.jsx("p",{className:"font-mono text-xs uppercase tracking-[0.3em] text-violet",children:"Interview Preparation"}),g.jsxs("h1",{className:"font-display mt-4 text-3xl font-bold text-bright sm:text-5xl",children:["Android & KMP ",g.jsx("span",{className:"text-gradient",children:"Interview Prep"})]}),g.jsxs("p",{className:"mt-5 max-w-2xl leading-relaxed",children:["This page is dedicated to all information regarding preparing for Android and KMP developer interviews. Pick a category below, then a topic — each topic page has a ",g.jsx("strong",{className:"text-bright",children:"Content"})," ","tab to learn from and an"," ",g.jsx("strong",{className:"text-bright",children:"Interview Prep"})," tab with questions whose answers stay hidden until you reveal them."]})]}),g.jsx("div",{className:"mt-14 space-y-10",children:rg.map(t=>g.jsxs("section",{className:"glass rounded-2xl p-6 sm:p-8",children:[g.jsx("h2",{className:"font-display text-xl font-semibold text-bright",children:t.name}),g.jsx("p",{className:"mt-2 text-sm leading-relaxed",children:t.description}),g.jsx("div",{className:"mt-5 flex flex-wrap gap-3",children:t.topics.map(i=>g.jsxs(Xn,{to:`/interview_preparation/${t.id}/${i.id}`,className:"glow-card rounded-xl border border-line bg-white/[0.04] px-5 py-3 font-display text-sm font-semibold text-bright transition-colors hover:text-violet",children:[i.title," →"]},i.id))})]},t.id))}),g.jsx("p",{className:"mt-14 text-xs text-body/50",children:"More categories and topics are added over time."})]})})}function Ri({text:t}){return String(t).split(/(\*\*[^*]+\*\*|`[^`]+`)/g).map((n,s)=>n.startsWith("**")&&n.endsWith("**")?g.jsx("strong",{className:"font-semibold text-bright",children:n.slice(2,-2)},s):n.startsWith("`")&&n.endsWith("`")?g.jsx("code",{className:"rounded bg-white/[0.07] px-1.5 py-0.5 font-mono text-[0.85em] text-cyan",children:n.slice(1,-1)},s):g.jsx(Dt.Fragment,{children:n},s))}function JT({title:t,code:i}){return g.jsxs("figure",{className:"overflow-hidden rounded-xl border border-line bg-ink-2",children:[t&&g.jsx("figcaption",{className:"border-b border-line px-4 py-2 font-mono text-xs text-violet",children:t}),g.jsx("div",{className:"overflow-x-auto",children:g.jsx("pre",{className:"p-4 font-mono text-[13px] leading-relaxed text-bright/90",children:g.jsx("code",{children:i})})})]})}function YT({headers:t,rows:i}){return g.jsx("div",{className:"overflow-x-auto rounded-xl border border-line",children:g.jsxs("table",{className:"w-full min-w-[560px] text-left text-sm",children:[g.jsx("thead",{children:g.jsx("tr",{className:"border-b border-line bg-white/[0.03]",children:t.map((n,s)=>g.jsx("th",{className:"px-4 py-3 font-semibold text-bright",children:g.jsx(Ri,{text:n})},s))})}),g.jsx("tbody",{children:i.map((n,s)=>g.jsx("tr",{className:"border-b border-line/60 last:border-0 align-top",children:n.map((r,c)=>g.jsx("td",{className:"px-4 py-3 text-body",children:g.jsx(Ri,{text:r})},c))},s))})]})})}function lg({blocks:t}){return g.jsx("div",{className:"space-y-4",children:t.map((i,n)=>{switch(i.t){case"p":return g.jsx("p",{className:"leading-relaxed text-body",children:g.jsx(Ri,{text:i.text})},n);case"h3":return g.jsx("h4",{className:"font-display pt-2 text-base font-semibold text-bright",children:g.jsx(Ri,{text:i.text})},n);case"list":return g.jsx("ul",{className:"space-y-2 pl-5",children:i.items.map((s,r)=>g.jsx("li",{className:"list-disc leading-relaxed text-body marker:text-violet",children:g.jsx(Ri,{text:s})},r))},n);case"code":return g.jsx(JT,{title:i.title,code:i.code},n);case"table":return g.jsx(YT,{headers:i.headers,rows:i.rows},n);case"note":return g.jsxs("div",{className:"rounded-xl border border-violet/30 bg-violet/[0.07] px-4 py-3 text-sm leading-relaxed text-body",children:[g.jsx("span",{className:"mr-2 font-semibold text-violet",children:"Interview tip"}),g.jsx(Ri,{text:i.text})]},n);default:return null}})})}const QT=[{id:"content",label:"Content"},{id:"qa",label:"Interview Prep"}],$T=[{id:"all",label:"All"},{id:"junior",label:"Junior"},{id:"senior",label:"Senior"}];function XT({level:t}){const i=t==="senior";return g.jsx("span",{className:`rounded-full border px-2.5 py-0.5 font-mono text-[10px] uppercase tracking-wider ${i?"border-fuchsia/40 bg-fuchsia/10 text-fuchsia":"border-emerald/40 bg-emerald/10 text-emerald"}`,children:t})}function ZT({item:t,isOpen:i,onToggle:n}){return g.jsxs("div",{className:"glass overflow-hidden rounded-xl",children:[g.jsxs("button",{type:"button",onClick:n,"aria-expanded":i,className:"flex w-full items-center gap-3 px-5 py-4 text-left transition-colors hover:bg-white/[0.03]",children:[g.jsx(XT,{level:t.level}),g.jsx("span",{className:"flex-1 font-display text-sm font-semibold text-bright sm:text-[15px]",children:t.q}),g.jsx("span",{className:`text-violet transition-transform duration-200 ${i?"rotate-180":""}`,children:"▾"})]}),i&&g.jsx("div",{className:"border-t border-line px-5 py-5",children:g.jsx(lg,{blocks:t.a})})]})}function eI({qa:t}){const[i,n]=x.useState("all"),[s,r]=x.useState(()=>new Set),c=x.useMemo(()=>t.map((p,m)=>({item:p,index:m})).filter(({item:p})=>i==="all"||p.level===i),[t,i]);function h(p){r(m=>{const f=new Set(m);return f.has(p)?f.delete(p):f.add(p),f})}return g.jsxs("div",{children:[g.jsxs("div",{className:"mb-6 flex flex-wrap items-center gap-2",children:[$T.map(p=>g.jsx("button",{type:"button",onClick:()=>n(p.id),className:`rounded-full border px-4 py-1.5 text-xs font-semibold transition-colors ${i===p.id?"border-violet/60 bg-violet/15 text-bright":"border-line text-body hover:text-bright"}`,children:p.label},p.id)),g.jsxs("span",{className:"ml-auto text-xs text-body/60",children:[c.length," questions — click one to reveal the answer"]})]}),g.jsx("div",{className:"space-y-3",children:c.map(({item:p,index:m})=>g.jsx(ZT,{item:p,isOpen:s.has(m),onToggle:()=>h(m)},m))})]})}function tI({content:t}){return g.jsx("div",{className:"space-y-12",children:t.map((i,n)=>g.jsxs("section",{children:[g.jsxs("h2",{className:"font-display mb-5 flex items-baseline gap-3 text-lg font-bold text-bright sm:text-xl",children:[g.jsx("span",{className:"font-mono text-sm text-violet",children:String(n+1).padStart(2,"0")}),i.heading]}),g.jsx(lg,{blocks:i.blocks})]},n))})}function aI(){const{categoryId:t,topicId:i}=ik(),[n,s]=x.useState("content"),r=_T(t,i);if(!r)return g.jsx(ag,{to:"/interview_preparation",replace:!0});const{category:c,topic:h}=r;return g.jsx("div",{className:"noise relative min-h-screen bg-ink text-body",children:g.jsxs("div",{className:"mx-auto max-w-4xl px-6 py-16 sm:py-20",children:[g.jsx(Xn,{to:"/interview_preparation",className:"text-sm text-body/70 transition-colors hover:text-bright",children:"← All topics"}),g.jsxs("header",{className:"mt-8",children:[g.jsx("p",{className:"font-mono text-xs uppercase tracking-[0.3em] text-violet",children:c.name}),g.jsx("h1",{className:"font-display mt-3 text-3xl font-bold text-bright sm:text-4xl",children:h.title}),g.jsx("p",{className:"mt-3 max-w-2xl text-sm leading-relaxed",children:h.tagline}),g.jsx("div",{className:"mt-4 flex flex-wrap gap-2",children:h.tags.map(p=>g.jsx("span",{className:"rounded-full border border-line px-3 py-1 font-mono text-[11px] text-body/80",children:p},p))})]}),g.jsx("nav",{className:"mt-10 flex gap-1 border-b border-line",children:QT.map(p=>g.jsx("button",{type:"button",onClick:()=>s(p.id),className:`-mb-px border-b-2 px-5 py-3 font-display text-sm font-semibold transition-colors ${n===p.id?"border-violet text-bright":"border-transparent text-body hover:text-bright"}`,children:p.label},p.id))}),g.jsx("div",{className:"mt-10",children:n==="content"?g.jsx(tI,{content:h.content}):g.jsx(eI,{qa:h.qa})})]})})}const rd=x.createContext({});function ro(t){const i=x.useRef(null);return i.current===null&&(i.current=t()),i.current}const tr=x.createContext(null),lo=x.createContext({transformPagePoint:t=>t,isStatic:!1,reducedMotion:"never"});class iI extends x.Component{getSnapshotBeforeUpdate(i){const n=this.props.childRef.current;if(n&&i.isPresent&&!this.props.isPresent){const s=this.props.sizeRef.current;s.height=n.offsetHeight||0,s.width=n.offsetWidth||0,s.top=n.offsetTop,s.left=n.offsetLeft}return null}componentDidUpdate(){}render(){return this.props.children}}function nI({children:t,isPresent:i}){const n=x.useId(),s=x.useRef(null),r=x.useRef({width:0,height:0,top:0,left:0}),{nonce:c}=x.useContext(lo);return x.useInsertionEffect(()=>{const{width:h,height:p,top:m,left:f}=r.current;if(i||!s.current||!h||!p)return;s.current.dataset.motionPopId=n;const y=document.createElement("style");return c&&(y.nonce=c),document.head.appendChild(y),y.sheet&&y.sheet.insertRule(`
          [data-motion-pop-id="${n}"] {
            position: absolute !important;
            width: ${h}px !important;
            height: ${p}px !important;
            top: ${m}px !important;
            left: ${f}px !important;
          }
        `),()=>{document.head.removeChild(y)}},[i]),g.jsx(iI,{isPresent:i,childRef:s,sizeRef:r,children:x.cloneElement(t,{ref:s})})}const oI=({children:t,initial:i,isPresent:n,onExitComplete:s,custom:r,presenceAffectsLayout:c,mode:h})=>{const p=ro(sI),m=x.useId(),f=x.useCallback(v=>{p.set(v,!0);for(const b of p.values())if(!b)return;s&&s()},[p,s]),y=x.useMemo(()=>({id:m,initial:i,isPresent:n,custom:r,onExitComplete:f,register:v=>(p.set(v,!1),()=>p.delete(v))}),c?[Math.random(),f]:[n,f]);return x.useMemo(()=>{p.forEach((v,b)=>p.set(b,!1))},[n]),x.useEffect(()=>{!n&&!p.size&&s&&s()},[n]),h==="popLayout"&&(t=g.jsx(nI,{isPresent:n,children:t})),g.jsx(tr.Provider,{value:y,children:t})};function sI(){return new Map}function cg(t=!0){const i=x.useContext(tr);if(i===null)return[!0,null];const{isPresent:n,onExitComplete:s,register:r}=i,c=x.useId();x.useEffect(()=>{t&&r(c)},[t]);const h=x.useCallback(()=>t&&s&&s(c),[c,s,t]);return!n&&s?[!1,h]:[!0]}const Cs=t=>t.key||"";function Zp(t){const i=[];return x.Children.forEach(t,n=>{x.isValidElement(n)&&i.push(n)}),i}const ld=typeof window<"u",ar=ld?x.useLayoutEffect:x.useEffect,cd=({children:t,custom:i,initial:n=!0,onExitComplete:s,presenceAffectsLayout:r=!0,mode:c="sync",propagate:h=!1})=>{const[p,m]=cg(h),f=x.useMemo(()=>Zp(t),[t]),y=h&&!p?[]:f.map(Cs),v=x.useRef(!0),b=x.useRef(f),A=ro(()=>new Map),[T,I]=x.useState(f),[M,R]=x.useState(f);ar(()=>{v.current=!1,b.current=f;for(let z=0;z<M.length;z++){const V=Cs(M[z]);y.includes(V)?A.delete(V):A.get(V)!==!0&&A.set(V,!1)}},[M,y.length,y.join("-")]);const O=[];if(f!==T){let z=[...f];for(let V=0;V<M.length;V++){const K=M[V],ie=Cs(K);y.includes(ie)||(z.splice(V,0,K),O.push(K))}c==="wait"&&O.length&&(z=O),R(Zp(z)),I(f);return}const{forceRender:q}=x.useContext(rd);return g.jsx(g.Fragment,{children:M.map(z=>{const V=Cs(z),K=h&&!p?!1:f===M||y.includes(V),ie=()=>{if(A.has(V))A.set(V,!0);else return;let J=!0;A.forEach(ce=>{ce||(J=!1)}),J&&(q==null||q(),R(b.current),h&&(m==null||m()),s&&s())};return g.jsx(oI,{isPresent:K,initial:!v.current||n?void 0:!1,custom:K?void 0:i,presenceAffectsLayout:r,mode:c,onExitComplete:K?void 0:ie,children:z},V)})})},rt=t=>t;let rI=rt,dg=rt;function dd(t){let i;return()=>(i===void 0&&(i=t()),i)}const ii=(t,i,n)=>{const s=i-t;return s===0?1:(n-t)/s},ca=t=>t*1e3,da=t=>t/1e3,lI={useManualTiming:!1};function cI(t){let i=new Set,n=new Set,s=!1,r=!1;const c=new WeakSet;let h={delta:0,timestamp:0,isProcessing:!1};function p(f){c.has(f)&&(m.schedule(f),t()),f(h)}const m={schedule:(f,y=!1,v=!1)=>{const A=v&&s?i:n;return y&&c.add(f),A.has(f)||A.add(f),f},cancel:f=>{n.delete(f),c.delete(f)},process:f=>{if(h=f,s){r=!0;return}s=!0,[i,n]=[n,i],i.forEach(p),i.clear(),s=!1,r&&(r=!1,m.process(f))}};return m}const Ts=["read","resolveKeyframes","update","preRender","render","postRender"],dI=40;function hg(t,i){let n=!1,s=!0;const r={delta:0,timestamp:0,isProcessing:!1},c=()=>n=!0,h=Ts.reduce((R,O)=>(R[O]=cI(c),R),{}),{read:p,resolveKeyframes:m,update:f,preRender:y,render:v,postRender:b}=h,A=()=>{const R=performance.now();n=!1,r.delta=s?1e3/60:Math.max(Math.min(R-r.timestamp,dI),1),r.timestamp=R,r.isProcessing=!0,p.process(r),m.process(r),f.process(r),y.process(r),v.process(r),b.process(r),r.isProcessing=!1,n&&i&&(s=!1,t(A))},T=()=>{n=!0,s=!0,r.isProcessing||t(A)};return{schedule:Ts.reduce((R,O)=>{const q=h[O];return R[O]=(z,V=!1,K=!1)=>(n||T(),q.schedule(z,V,K)),R},{}),cancel:R=>{for(let O=0;O<Ts.length;O++)h[Ts[O]].cancel(R)},state:r,steps:h}}const{schedule:fe,cancel:$t,state:ze,steps:uc}=hg(typeof requestAnimationFrame<"u"?requestAnimationFrame:rt,!0),ug=x.createContext({strict:!1}),em={animation:["animate","variants","whileHover","whileTap","exit","whileInView","whileFocus","whileDrag"],exit:["exit"],drag:["drag","dragControls"],focus:["whileFocus"],hover:["whileHover","onHoverStart","onHoverEnd"],tap:["whileTap","onTap","onTapStart","onTapCancel"],pan:["onPan","onPanStart","onPanSessionStart","onPanEnd"],inView:["whileInView","onViewportEnter","onViewportLeave"],layout:["layout","layoutId"]},ji={};for(const t in em)ji[t]={isEnabled:i=>em[t].some(n=>!!i[n])};function hI(t){for(const i in t)ji[i]={...ji[i],...t[i]}}const uI=new Set(["animate","exit","variants","initial","style","values","variants","transition","transformTemplate","custom","inherit","onBeforeLayoutMeasure","onAnimationStart","onAnimationComplete","onUpdate","onDragStart","onDrag","onDragEnd","onMeasureDragConstraints","onDirectionLock","onDragTransitionEnd","_dragX","_dragY","onHoverStart","onHoverEnd","onViewportEnter","onViewportLeave","globalTapTarget","ignoreStrict","viewport"]);function Ws(t){return t.startsWith("while")||t.startsWith("drag")&&t!=="draggable"||t.startsWith("layout")||t.startsWith("onTap")||t.startsWith("onPan")||t.startsWith("onLayout")||uI.has(t)}let pg=t=>!Ws(t);function pI(t){t&&(pg=i=>i.startsWith("on")?!Ws(i):t(i))}try{pI(require("@emotion/is-prop-valid").default)}catch{}function mI(t,i,n){const s={};for(const r in t)r==="values"&&typeof t.values=="object"||(pg(r)||n===!0&&Ws(r)||!i&&!Ws(r)||t.draggable&&r.startsWith("onDrag"))&&(s[r]=t[r]);return s}function fI(t){if(typeof Proxy>"u")return t;const i=new Map,n=(...s)=>t(...s);return new Proxy(n,{get:(s,r)=>r==="create"?t:(i.has(r)||i.set(r,t(r)),i.get(r))})}const ir=x.createContext({});function Zn(t){return typeof t=="string"||Array.isArray(t)}function nr(t){return t!==null&&typeof t=="object"&&typeof t.start=="function"}const hd=["animate","whileInView","whileFocus","whileHover","whileTap","whileDrag","exit"],ud=["initial",...hd];function or(t){return nr(t.animate)||ud.some(i=>Zn(t[i]))}function mg(t){return!!(or(t)||t.variants)}function gI(t,i){if(or(t)){const{initial:n,animate:s}=t;return{initial:n===!1||Zn(n)?n:void 0,animate:Zn(s)?s:void 0}}return t.inherit!==!1?i:{}}function yI(t){const{initial:i,animate:n}=gI(t,x.useContext(ir));return x.useMemo(()=>({initial:i,animate:n}),[tm(i),tm(n)])}function tm(t){return Array.isArray(t)?t.join(" "):t}const wI=Symbol.for("motionComponentSymbol");function Ei(t){return t&&typeof t=="object"&&Object.prototype.hasOwnProperty.call(t,"current")}function vI(t,i,n){return x.useCallback(s=>{s&&t.onMount&&t.onMount(s),i&&(s?i.mount(s):i.unmount()),n&&(typeof n=="function"?n(s):Ei(n)&&(n.current=s))},[i])}const pd=t=>t.replace(/([a-z])([A-Z])/gu,"$1-$2").toLowerCase(),bI="framerAppearId",fg="data-"+pd(bI),{schedule:md}=hg(queueMicrotask,!1),gg=x.createContext({});function kI(t,i,n,s,r){var c,h;const{visualElement:p}=x.useContext(ir),m=x.useContext(ug),f=x.useContext(tr),y=x.useContext(lo).reducedMotion,v=x.useRef(null);s=s||m.renderer,!v.current&&s&&(v.current=s(t,{visualState:i,parent:p,props:n,presenceContext:f,blockInitialAnimation:f?f.initial===!1:!1,reducedMotionConfig:y}));const b=v.current,A=x.useContext(gg);b&&!b.projection&&r&&(b.type==="html"||b.type==="svg")&&xI(v.current,n,r,A);const T=x.useRef(!1);x.useInsertionEffect(()=>{b&&T.current&&b.update(n,f)});const I=n[fg],M=x.useRef(!!I&&!(!((c=window.MotionHandoffIsComplete)===null||c===void 0)&&c.call(window,I))&&((h=window.MotionHasOptimisedAnimation)===null||h===void 0?void 0:h.call(window,I)));return ar(()=>{b&&(T.current=!0,window.MotionIsMounted=!0,b.updateFeatures(),md.render(b.render),M.current&&b.animationState&&b.animationState.animateChanges())}),x.useEffect(()=>{b&&(!M.current&&b.animationState&&b.animationState.animateChanges(),M.current&&(queueMicrotask(()=>{var R;(R=window.MotionHandoffMarkAsComplete)===null||R===void 0||R.call(window,I)}),M.current=!1))}),b}function xI(t,i,n,s){const{layoutId:r,layout:c,drag:h,dragConstraints:p,layoutScroll:m,layoutRoot:f}=i;t.projection=new n(t.latestValues,i["data-framer-portal-id"]?void 0:yg(t.parent)),t.projection.setOptions({layoutId:r,layout:c,alwaysMeasureLayout:!!h||p&&Ei(p),visualElement:t,animationType:typeof c=="string"?c:"both",initialPromotionConfig:s,layoutScroll:m,layoutRoot:f})}function yg(t){if(t)return t.options.allowProjection!==!1?t.projection:yg(t.parent)}function SI({preloadedFeatures:t,createVisualElement:i,useRender:n,useVisualState:s,Component:r}){var c,h;t&&hI(t);function p(f,y){let v;const b={...x.useContext(lo),...f,layoutId:AI(f)},{isStatic:A}=b,T=yI(f),I=s(f,A);if(!A&&ld){CI();const M=TI(b);v=M.MeasureLayout,T.visualElement=kI(r,I,b,i,M.ProjectionNode)}return g.jsxs(ir.Provider,{value:T,children:[v&&T.visualElement?g.jsx(v,{visualElement:T.visualElement,...b}):null,n(r,f,vI(I,T.visualElement,y),I,A,T.visualElement)]})}p.displayName=`motion.${typeof r=="string"?r:`create(${(h=(c=r.displayName)!==null&&c!==void 0?c:r.name)!==null&&h!==void 0?h:""})`}`;const m=x.forwardRef(p);return m[wI]=r,m}function AI({layoutId:t}){const i=x.useContext(rd).id;return i&&t!==void 0?i+"-"+t:t}function CI(t,i){x.useContext(ug).strict}function TI(t){const{drag:i,layout:n}=ji;if(!i&&!n)return{};const s={...i,...n};return{MeasureLayout:i!=null&&i.isEnabled(t)||n!=null&&n.isEnabled(t)?s.MeasureLayout:void 0,ProjectionNode:s.ProjectionNode}}const II=["animate","circle","defs","desc","ellipse","g","image","line","filter","marker","mask","metadata","path","pattern","polygon","polyline","rect","stop","switch","symbol","svg","text","tspan","use","view"];function fd(t){return typeof t!="string"||t.includes("-")?!1:!!(II.indexOf(t)>-1||/[A-Z]/u.test(t))}function am(t){const i=[{},{}];return t==null||t.values.forEach((n,s)=>{i[0][s]=n.get(),i[1][s]=n.getVelocity()}),i}function gd(t,i,n,s){if(typeof i=="function"){const[r,c]=am(s);i=i(n!==void 0?n:t.custom,r,c)}if(typeof i=="string"&&(i=t.variants&&t.variants[i]),typeof i=="function"){const[r,c]=am(s);i=i(n!==void 0?n:t.custom,r,c)}return i}const Uc=t=>Array.isArray(t),MI=t=>!!(t&&typeof t=="object"&&t.mix&&t.toValue),PI=t=>Uc(t)?t[t.length-1]||0:t,Qe=t=>!!(t&&t.getVelocity);function qs(t){const i=Qe(t)?t.get():t;return MI(i)?i.toValue():i}function DI({scrapeMotionValuesFromProps:t,createRenderState:i,onUpdate:n},s,r,c){const h={latestValues:RI(s,r,c,t),renderState:i()};return n&&(h.onMount=p=>n({props:s,current:p,...h}),h.onUpdate=p=>n(p)),h}const wg=t=>(i,n)=>{const s=x.useContext(ir),r=x.useContext(tr),c=()=>DI(t,i,s,r);return n?c():ro(c)};function RI(t,i,n,s){const r={},c=s(t,{});for(const b in c)r[b]=qs(c[b]);let{initial:h,animate:p}=t;const m=or(t),f=mg(t);i&&f&&!m&&t.inherit!==!1&&(h===void 0&&(h=i.initial),p===void 0&&(p=i.animate));let y=n?n.initial===!1:!1;y=y||h===!1;const v=y?p:h;if(v&&typeof v!="boolean"&&!nr(v)){const b=Array.isArray(v)?v:[v];for(let A=0;A<b.length;A++){const T=gd(t,b[A]);if(T){const{transitionEnd:I,transition:M,...R}=T;for(const O in R){let q=R[O];if(Array.isArray(q)){const z=y?q.length-1:0;q=q[z]}q!==null&&(r[O]=q)}for(const O in I)r[O]=I[O]}}}return r}const Ki=["transformPerspective","x","y","z","translateX","translateY","translateZ","scale","scaleX","scaleY","rotate","rotateX","rotateY","rotateZ","skew","skewX","skewY"],ni=new Set(Ki),vg=t=>i=>typeof i=="string"&&i.startsWith(t),bg=vg("--"),EI=vg("var(--"),yd=t=>EI(t)?UI.test(t.split("/*")[0].trim()):!1,UI=/var\(--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)$/iu,kg=(t,i)=>i&&typeof t=="number"?i.transform(t):t,Xt=(t,i,n)=>n>i?i:n<t?t:n,_i={test:t=>typeof t=="number",parse:parseFloat,transform:t=>t},eo={..._i,transform:t=>Xt(0,1,t)},Is={..._i,default:1},co=t=>({test:i=>typeof i=="string"&&i.endsWith(t)&&i.split(" ").length===1,parse:parseFloat,transform:i=>`${i}${t}`}),Ua=co("deg"),Yt=co("%"),ae=co("px"),qI=co("vh"),OI=co("vw"),im={...Yt,parse:t=>Yt.parse(t)/100,transform:t=>Yt.transform(t*100)},FI={borderWidth:ae,borderTopWidth:ae,borderRightWidth:ae,borderBottomWidth:ae,borderLeftWidth:ae,borderRadius:ae,radius:ae,borderTopLeftRadius:ae,borderTopRightRadius:ae,borderBottomRightRadius:ae,borderBottomLeftRadius:ae,width:ae,maxWidth:ae,height:ae,maxHeight:ae,top:ae,right:ae,bottom:ae,left:ae,padding:ae,paddingTop:ae,paddingRight:ae,paddingBottom:ae,paddingLeft:ae,margin:ae,marginTop:ae,marginRight:ae,marginBottom:ae,marginLeft:ae,backgroundPositionX:ae,backgroundPositionY:ae},LI={rotate:Ua,rotateX:Ua,rotateY:Ua,rotateZ:Ua,scale:Is,scaleX:Is,scaleY:Is,scaleZ:Is,skew:Ua,skewX:Ua,skewY:Ua,distance:ae,translateX:ae,translateY:ae,translateZ:ae,x:ae,y:ae,z:ae,perspective:ae,transformPerspective:ae,opacity:eo,originX:im,originY:im,originZ:ae},nm={..._i,transform:Math.round},wd={...FI,...LI,zIndex:nm,size:ae,fillOpacity:eo,strokeOpacity:eo,numOctaves:nm},jI={x:"translateX",y:"translateY",z:"translateZ",transformPerspective:"perspective"},VI=Ki.length;function NI(t,i,n){let s="",r=!0;for(let c=0;c<VI;c++){const h=Ki[c],p=t[h];if(p===void 0)continue;let m=!0;if(typeof p=="number"?m=p===(h.startsWith("scale")?1:0):m=parseFloat(p)===0,!m||n){const f=kg(p,wd[h]);if(!m){r=!1;const y=jI[h]||h;s+=`${y}(${f}) `}n&&(i[h]=f)}}return s=s.trim(),n?s=n(i,r?"":s):r&&(s="none"),s}function vd(t,i,n){const{style:s,vars:r,transformOrigin:c}=t;let h=!1,p=!1;for(const m in i){const f=i[m];if(ni.has(m)){h=!0;continue}else if(bg(m)){r[m]=f;continue}else{const y=kg(f,wd[m]);m.startsWith("origin")?(p=!0,c[m]=y):s[m]=y}}if(i.transform||(h||n?s.transform=NI(i,t.transform,n):s.transform&&(s.transform="none")),p){const{originX:m="50%",originY:f="50%",originZ:y=0}=c;s.transformOrigin=`${m} ${f} ${y}`}}const WI={offset:"stroke-dashoffset",array:"stroke-dasharray"},BI={offset:"strokeDashoffset",array:"strokeDasharray"};function zI(t,i,n=1,s=0,r=!0){t.pathLength=1;const c=r?WI:BI;t[c.offset]=ae.transform(-s);const h=ae.transform(i),p=ae.transform(n);t[c.array]=`${h} ${p}`}function om(t,i,n){return typeof t=="string"?t:ae.transform(i+n*t)}function HI(t,i,n){const s=om(i,t.x,t.width),r=om(n,t.y,t.height);return`${s} ${r}`}function bd(t,{attrX:i,attrY:n,attrScale:s,originX:r,originY:c,pathLength:h,pathSpacing:p=1,pathOffset:m=0,...f},y,v){if(vd(t,f,v),y){t.style.viewBox&&(t.attrs.viewBox=t.style.viewBox);return}t.attrs=t.style,t.style={};const{attrs:b,style:A,dimensions:T}=t;b.transform&&(T&&(A.transform=b.transform),delete b.transform),T&&(r!==void 0||c!==void 0||A.transform)&&(A.transformOrigin=HI(T,r!==void 0?r:.5,c!==void 0?c:.5)),i!==void 0&&(b.x=i),n!==void 0&&(b.y=n),s!==void 0&&(b.scale=s),h!==void 0&&zI(b,h,p,m,!1)}const kd=()=>({style:{},transform:{},transformOrigin:{},vars:{}}),xg=()=>({...kd(),attrs:{}}),xd=t=>typeof t=="string"&&t.toLowerCase()==="svg";function Sg(t,{style:i,vars:n},s,r){Object.assign(t.style,i,r&&r.getProjectionStyles(s));for(const c in n)t.style.setProperty(c,n[c])}const Ag=new Set(["baseFrequency","diffuseConstant","kernelMatrix","kernelUnitLength","keySplines","keyTimes","limitingConeAngle","markerHeight","markerWidth","numOctaves","targetX","targetY","surfaceScale","specularConstant","specularExponent","stdDeviation","tableValues","viewBox","gradientTransform","pathLength","startOffset","textLength","lengthAdjust"]);function Cg(t,i,n,s){Sg(t,i,void 0,s);for(const r in i.attrs)t.setAttribute(Ag.has(r)?r:pd(r),i.attrs[r])}const Bs={};function KI(t){Object.assign(Bs,t)}function Tg(t,{layout:i,layoutId:n}){return ni.has(t)||t.startsWith("origin")||(i||n!==void 0)&&(!!Bs[t]||t==="opacity")}function Sd(t,i,n){var s;const{style:r}=t,c={};for(const h in r)(Qe(r[h])||i.style&&Qe(i.style[h])||Tg(h,t)||((s=n==null?void 0:n.getValue(h))===null||s===void 0?void 0:s.liveStyle)!==void 0)&&(c[h]=r[h]);return c}function Ig(t,i,n){const s=Sd(t,i,n);for(const r in t)if(Qe(t[r])||Qe(i[r])){const c=Ki.indexOf(r)!==-1?"attr"+r.charAt(0).toUpperCase()+r.substring(1):r;s[c]=t[r]}return s}function _I(t,i){try{i.dimensions=typeof t.getBBox=="function"?t.getBBox():t.getBoundingClientRect()}catch{i.dimensions={x:0,y:0,width:0,height:0}}}const sm=["x","y","width","height","cx","cy","r"],GI={useVisualState:wg({scrapeMotionValuesFromProps:Ig,createRenderState:xg,onUpdate:({props:t,prevProps:i,current:n,renderState:s,latestValues:r})=>{if(!n)return;let c=!!t.drag;if(!c){for(const p in r)if(ni.has(p)){c=!0;break}}if(!c)return;let h=!i;if(i)for(let p=0;p<sm.length;p++){const m=sm[p];t[m]!==i[m]&&(h=!0)}h&&fe.read(()=>{_I(n,s),fe.render(()=>{bd(s,r,xd(n.tagName),t.transformTemplate),Cg(n,s)})})}})},JI={useVisualState:wg({scrapeMotionValuesFromProps:Sd,createRenderState:kd})};function Mg(t,i,n){for(const s in i)!Qe(i[s])&&!Tg(s,n)&&(t[s]=i[s])}function YI({transformTemplate:t},i){return x.useMemo(()=>{const n=kd();return vd(n,i,t),Object.assign({},n.vars,n.style)},[i])}function QI(t,i){const n=t.style||{},s={};return Mg(s,n,t),Object.assign(s,YI(t,i)),s}function $I(t,i){const n={},s=QI(t,i);return t.drag&&t.dragListener!==!1&&(n.draggable=!1,s.userSelect=s.WebkitUserSelect=s.WebkitTouchCallout="none",s.touchAction=t.drag===!0?"none":`pan-${t.drag==="x"?"y":"x"}`),t.tabIndex===void 0&&(t.onTap||t.onTapStart||t.whileTap)&&(n.tabIndex=0),n.style=s,n}function XI(t,i,n,s){const r=x.useMemo(()=>{const c=xg();return bd(c,i,xd(s),t.transformTemplate),{...c.attrs,style:{...c.style}}},[i]);if(t.style){const c={};Mg(c,t.style,t),r.style={...c,...r.style}}return r}function ZI(t=!1){return(n,s,r,{latestValues:c},h)=>{const m=(fd(n)?XI:$I)(s,c,h,n),f=mI(s,typeof n=="string",t),y=n!==x.Fragment?{...f,...m,ref:r}:{},{children:v}=s,b=x.useMemo(()=>Qe(v)?v.get():v,[v]);return x.createElement(n,{...y,children:b})}}function eM(t,i){return function(s,{forwardMotionProps:r}={forwardMotionProps:!1}){const h={...fd(s)?GI:JI,preloadedFeatures:t,useRender:ZI(r),createVisualElement:i,Component:s};return SI(h)}}function Pg(t,i){if(!Array.isArray(i))return!1;const n=i.length;if(n!==t.length)return!1;for(let s=0;s<n;s++)if(i[s]!==t[s])return!1;return!0}function sr(t,i,n){const s=t.getProps();return gd(s,i,n!==void 0?n:s.custom,t)}const Dg=dd(()=>window.ScrollTimeline!==void 0);class tM{constructor(i){this.stop=()=>this.runAll("stop"),this.animations=i.filter(Boolean)}get finished(){return Promise.all(this.animations.map(i=>"finished"in i?i.finished:i))}getAll(i){return this.animations[0][i]}setAll(i,n){for(let s=0;s<this.animations.length;s++)this.animations[s][i]=n}attachTimeline(i,n){const s=this.animations.map(r=>{if(Dg()&&r.attachTimeline)return r.attachTimeline(i);if(typeof n=="function")return n(r)});return()=>{s.forEach((r,c)=>{r&&r(),this.animations[c].stop()})}}get time(){return this.getAll("time")}set time(i){this.setAll("time",i)}get speed(){return this.getAll("speed")}set speed(i){this.setAll("speed",i)}get startTime(){return this.getAll("startTime")}get duration(){let i=0;for(let n=0;n<this.animations.length;n++)i=Math.max(i,this.animations[n].duration);return i}runAll(i){this.animations.forEach(n=>n[i]())}flatten(){this.runAll("flatten")}play(){this.runAll("play")}pause(){this.runAll("pause")}cancel(){this.runAll("cancel")}complete(){this.runAll("complete")}}class aM extends tM{then(i,n){return Promise.all(this.animations).then(i).catch(n)}}function Ad(t,i){return t?t[i]||t.default||t:void 0}const qc=2e4;function Rg(t){let i=0;const n=50;let s=t.next(i);for(;!s.done&&i<qc;)i+=n,s=t.next(i);return i>=qc?1/0:i}function Cd(t){return typeof t=="function"}function rm(t,i){t.timeline=i,t.onfinish=null}const Td=t=>Array.isArray(t)&&typeof t[0]=="number",iM={linearEasing:void 0};function nM(t,i){const n=dd(t);return()=>{var s;return(s=iM[i])!==null&&s!==void 0?s:n()}}const zs=nM(()=>{try{document.createElement("div").animate({opacity:0},{easing:"linear(0, 1)"})}catch{return!1}return!0},"linearEasing"),Eg=(t,i,n=10)=>{let s="";const r=Math.max(Math.round(i/n),2);for(let c=0;c<r;c++)s+=t(ii(0,r-1,c))+", ";return`linear(${s.substring(0,s.length-2)})`};function Ug(t){return!!(typeof t=="function"&&zs()||!t||typeof t=="string"&&(t in Oc||zs())||Td(t)||Array.isArray(t)&&t.every(Ug))}const zn=([t,i,n,s])=>`cubic-bezier(${t}, ${i}, ${n}, ${s})`,Oc={linear:"linear",ease:"ease",easeIn:"ease-in",easeOut:"ease-out",easeInOut:"ease-in-out",circIn:zn([0,.65,.55,1]),circOut:zn([.55,0,1,.45]),backIn:zn([.31,.01,.66,-.59]),backOut:zn([.33,1.53,.69,.99])};function qg(t,i){if(t)return typeof t=="function"&&zs()?Eg(t,i):Td(t)?zn(t):Array.isArray(t)?t.map(n=>qg(n,i)||Oc.easeOut):Oc[t]}const Vt={x:!1,y:!1};function Og(){return Vt.x||Vt.y}function Id(t,i,n){var s;if(t instanceof Element)return[t];if(typeof t=="string"){let r=document;const c=(s=void 0)!==null&&s!==void 0?s:r.querySelectorAll(t);return c?Array.from(c):[]}return Array.from(t)}function Fg(t,i){const n=Id(t),s=new AbortController,r={passive:!0,...i,signal:s.signal};return[n,r,()=>s.abort()]}function lm(t){return i=>{i.pointerType==="touch"||Og()||t(i)}}function oM(t,i,n={}){const[s,r,c]=Fg(t,n),h=lm(p=>{const{target:m}=p,f=i(p);if(typeof f!="function"||!m)return;const y=lm(v=>{f(v),m.removeEventListener("pointerleave",y)});m.addEventListener("pointerleave",y,r)});return s.forEach(p=>{p.addEventListener("pointerenter",h,r)}),c}const Lg=(t,i)=>i?t===i?!0:Lg(t,i.parentElement):!1,Md=t=>t.pointerType==="mouse"?typeof t.button!="number"||t.button<=0:t.isPrimary!==!1,sM=new Set(["BUTTON","INPUT","SELECT","TEXTAREA","A"]);function rM(t){return sM.has(t.tagName)||t.tabIndex!==-1}const Hn=new WeakSet;function cm(t){return i=>{i.key==="Enter"&&t(i)}}function pc(t,i){t.dispatchEvent(new PointerEvent("pointer"+i,{isPrimary:!0,bubbles:!0}))}const lM=(t,i)=>{const n=t.currentTarget;if(!n)return;const s=cm(()=>{if(Hn.has(n))return;pc(n,"down");const r=cm(()=>{pc(n,"up")}),c=()=>pc(n,"cancel");n.addEventListener("keyup",r,i),n.addEventListener("blur",c,i)});n.addEventListener("keydown",s,i),n.addEventListener("blur",()=>n.removeEventListener("keydown",s),i)};function dm(t){return Md(t)&&!Og()}function cM(t,i,n={}){const[s,r,c]=Fg(t,n),h=p=>{const m=p.currentTarget;if(!dm(p)||Hn.has(m))return;Hn.add(m);const f=i(p),y=(A,T)=>{window.removeEventListener("pointerup",v),window.removeEventListener("pointercancel",b),!(!dm(A)||!Hn.has(m))&&(Hn.delete(m),typeof f=="function"&&f(A,{success:T}))},v=A=>{y(A,n.useGlobalTarget||Lg(m,A.target))},b=A=>{y(A,!1)};window.addEventListener("pointerup",v,r),window.addEventListener("pointercancel",b,r)};return s.forEach(p=>{!rM(p)&&p.getAttribute("tabindex")===null&&(p.tabIndex=0),(n.useGlobalTarget?window:p).addEventListener("pointerdown",h,r),p.addEventListener("focus",f=>lM(f,r),r)}),c}function dM(t){return t==="x"||t==="y"?Vt[t]?null:(Vt[t]=!0,()=>{Vt[t]=!1}):Vt.x||Vt.y?null:(Vt.x=Vt.y=!0,()=>{Vt.x=Vt.y=!1})}const jg=new Set(["width","height","top","left","right","bottom",...Ki]);let Os;function hM(){Os=void 0}const Qt={now:()=>(Os===void 0&&Qt.set(ze.isProcessing||lI.useManualTiming?ze.timestamp:performance.now()),Os),set:t=>{Os=t,queueMicrotask(hM)}};function Pd(t,i){t.indexOf(i)===-1&&t.push(i)}function Dd(t,i){const n=t.indexOf(i);n>-1&&t.splice(n,1)}class Rd{constructor(){this.subscriptions=[]}add(i){return Pd(this.subscriptions,i),()=>Dd(this.subscriptions,i)}notify(i,n,s){const r=this.subscriptions.length;if(r)if(r===1)this.subscriptions[0](i,n,s);else for(let c=0;c<r;c++){const h=this.subscriptions[c];h&&h(i,n,s)}}getSize(){return this.subscriptions.length}clear(){this.subscriptions.length=0}}function Ed(t,i){return i?t*(1e3/i):0}const hm=30,uM=t=>!isNaN(parseFloat(t));class pM{constructor(i,n={}){this.version="11.18.2",this.canTrackVelocity=null,this.events={},this.updateAndNotify=(s,r=!0)=>{const c=Qt.now();this.updatedAt!==c&&this.setPrevFrameValue(),this.prev=this.current,this.setCurrent(s),this.current!==this.prev&&this.events.change&&this.events.change.notify(this.current),r&&this.events.renderRequest&&this.events.renderRequest.notify(this.current)},this.hasAnimated=!1,this.setCurrent(i),this.owner=n.owner}setCurrent(i){this.current=i,this.updatedAt=Qt.now(),this.canTrackVelocity===null&&i!==void 0&&(this.canTrackVelocity=uM(this.current))}setPrevFrameValue(i=this.current){this.prevFrameValue=i,this.prevUpdatedAt=this.updatedAt}onChange(i){return this.on("change",i)}on(i,n){this.events[i]||(this.events[i]=new Rd);const s=this.events[i].add(n);return i==="change"?()=>{s(),fe.read(()=>{this.events.change.getSize()||this.stop()})}:s}clearListeners(){for(const i in this.events)this.events[i].clear()}attach(i,n){this.passiveEffect=i,this.stopPassiveEffect=n}set(i,n=!0){!n||!this.passiveEffect?this.updateAndNotify(i,n):this.passiveEffect(i,this.updateAndNotify)}setWithVelocity(i,n,s){this.set(n),this.prev=void 0,this.prevFrameValue=i,this.prevUpdatedAt=this.updatedAt-s}jump(i,n=!0){this.updateAndNotify(i),this.prev=i,this.prevUpdatedAt=this.prevFrameValue=void 0,n&&this.stop(),this.stopPassiveEffect&&this.stopPassiveEffect()}get(){return this.current}getPrevious(){return this.prev}getVelocity(){const i=Qt.now();if(!this.canTrackVelocity||this.prevFrameValue===void 0||i-this.updatedAt>hm)return 0;const n=Math.min(this.updatedAt-this.prevUpdatedAt,hm);return Ed(parseFloat(this.current)-parseFloat(this.prevFrameValue),n)}start(i){return this.stop(),new Promise(n=>{this.hasAnimated=!0,this.animation=i(n),this.events.animationStart&&this.events.animationStart.notify()}).then(()=>{this.events.animationComplete&&this.events.animationComplete.notify(),this.clearAnimation()})}stop(){this.animation&&(this.animation.stop(),this.events.animationCancel&&this.events.animationCancel.notify()),this.clearAnimation()}isAnimating(){return!!this.animation}clearAnimation(){delete this.animation}destroy(){this.clearListeners(),this.stop(),this.stopPassiveEffect&&this.stopPassiveEffect()}}function Jt(t,i){return new pM(t,i)}function mM(t,i,n){t.hasValue(i)?t.getValue(i).set(n):t.addValue(i,Jt(n))}function fM(t,i){const n=sr(t,i);let{transitionEnd:s={},transition:r={},...c}=n||{};c={...c,...s};for(const h in c){const p=PI(c[h]);mM(t,h,p)}}function gM(t){return!!(Qe(t)&&t.add)}function Fc(t,i){const n=t.getValue("willChange");if(gM(n))return n.add(i)}function Vg(t){return t.props[fg]}const Ng=(t,i,n)=>(((1-3*n+3*i)*t+(3*n-6*i))*t+3*i)*t,yM=1e-7,wM=12;function vM(t,i,n,s,r){let c,h,p=0;do h=i+(n-i)/2,c=Ng(h,s,r)-t,c>0?n=h:i=h;while(Math.abs(c)>yM&&++p<wM);return h}function ho(t,i,n,s){if(t===i&&n===s)return rt;const r=c=>vM(c,0,1,t,n);return c=>c===0||c===1?c:Ng(r(c),i,s)}const Wg=t=>i=>i<=.5?t(2*i)/2:(2-t(2*(1-i)))/2,Bg=t=>i=>1-t(1-i),zg=ho(.33,1.53,.69,.99),Ud=Bg(zg),Hg=Wg(Ud),Kg=t=>(t*=2)<1?.5*Ud(t):.5*(2-Math.pow(2,-10*(t-1))),qd=t=>1-Math.sin(Math.acos(t)),_g=Bg(qd),Gg=Wg(qd),Jg=t=>/^0[^.\s]+$/u.test(t);function bM(t){return typeof t=="number"?t===0:t!==null?t==="none"||t==="0"||Jg(t):!0}const _n=t=>Math.round(t*1e5)/1e5,Od=/-?(?:\d+(?:\.\d+)?|\.\d+)/gu;function kM(t){return t==null}const xM=/^(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))$/iu,Fd=(t,i)=>n=>!!(typeof n=="string"&&xM.test(n)&&n.startsWith(t)||i&&!kM(n)&&Object.prototype.hasOwnProperty.call(n,i)),Yg=(t,i,n)=>s=>{if(typeof s!="string")return s;const[r,c,h,p]=s.match(Od);return{[t]:parseFloat(r),[i]:parseFloat(c),[n]:parseFloat(h),alpha:p!==void 0?parseFloat(p):1}},SM=t=>Xt(0,255,t),mc={..._i,transform:t=>Math.round(SM(t))},ti={test:Fd("rgb","red"),parse:Yg("red","green","blue"),transform:({red:t,green:i,blue:n,alpha:s=1})=>"rgba("+mc.transform(t)+", "+mc.transform(i)+", "+mc.transform(n)+", "+_n(eo.transform(s))+")"};function AM(t){let i="",n="",s="",r="";return t.length>5?(i=t.substring(1,3),n=t.substring(3,5),s=t.substring(5,7),r=t.substring(7,9)):(i=t.substring(1,2),n=t.substring(2,3),s=t.substring(3,4),r=t.substring(4,5),i+=i,n+=n,s+=s,r+=r),{red:parseInt(i,16),green:parseInt(n,16),blue:parseInt(s,16),alpha:r?parseInt(r,16)/255:1}}const Lc={test:Fd("#"),parse:AM,transform:ti.transform},Ui={test:Fd("hsl","hue"),parse:Yg("hue","saturation","lightness"),transform:({hue:t,saturation:i,lightness:n,alpha:s=1})=>"hsla("+Math.round(t)+", "+Yt.transform(_n(i))+", "+Yt.transform(_n(n))+", "+_n(eo.transform(s))+")"},it={test:t=>ti.test(t)||Lc.test(t)||Ui.test(t),parse:t=>ti.test(t)?ti.parse(t):Ui.test(t)?Ui.parse(t):Lc.parse(t),transform:t=>typeof t=="string"?t:t.hasOwnProperty("red")?ti.transform(t):Ui.transform(t)},CM=/(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))/giu;function TM(t){var i,n;return isNaN(t)&&typeof t=="string"&&(((i=t.match(Od))===null||i===void 0?void 0:i.length)||0)+(((n=t.match(CM))===null||n===void 0?void 0:n.length)||0)>0}const Qg="number",$g="color",IM="var",MM="var(",um="${}",PM=/var\s*\(\s*--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)|#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\)|-?(?:\d+(?:\.\d+)?|\.\d+)/giu;function to(t){const i=t.toString(),n=[],s={color:[],number:[],var:[]},r=[];let c=0;const p=i.replace(PM,m=>(it.test(m)?(s.color.push(c),r.push($g),n.push(it.parse(m))):m.startsWith(MM)?(s.var.push(c),r.push(IM),n.push(m)):(s.number.push(c),r.push(Qg),n.push(parseFloat(m))),++c,um)).split(um);return{values:n,split:p,indexes:s,types:r}}function Xg(t){return to(t).values}function Zg(t){const{split:i,types:n}=to(t),s=i.length;return r=>{let c="";for(let h=0;h<s;h++)if(c+=i[h],r[h]!==void 0){const p=n[h];p===Qg?c+=_n(r[h]):p===$g?c+=it.transform(r[h]):c+=r[h]}return c}}const DM=t=>typeof t=="number"?0:t;function RM(t){const i=Xg(t);return Zg(t)(i.map(DM))}const Oa={test:TM,parse:Xg,createTransformer:Zg,getAnimatableNone:RM},EM=new Set(["brightness","contrast","saturate","opacity"]);function UM(t){const[i,n]=t.slice(0,-1).split("(");if(i==="drop-shadow")return t;const[s]=n.match(Od)||[];if(!s)return t;const r=n.replace(s,"");let c=EM.has(i)?1:0;return s!==n&&(c*=100),i+"("+c+r+")"}const qM=/\b([a-z-]*)\(.*?\)/gu,jc={...Oa,getAnimatableNone:t=>{const i=t.match(qM);return i?i.map(UM).join(" "):t}},OM={...wd,color:it,backgroundColor:it,outlineColor:it,fill:it,stroke:it,borderColor:it,borderTopColor:it,borderRightColor:it,borderBottomColor:it,borderLeftColor:it,filter:jc,WebkitFilter:jc},Ld=t=>OM[t];function ey(t,i){let n=Ld(t);return n!==jc&&(n=Oa),n.getAnimatableNone?n.getAnimatableNone(i):void 0}const FM=new Set(["auto","none","0"]);function LM(t,i,n){let s=0,r;for(;s<t.length&&!r;){const c=t[s];typeof c=="string"&&!FM.has(c)&&to(c).values.length&&(r=t[s]),s++}if(r&&n)for(const c of i)t[c]=ey(n,r)}const pm=t=>t===_i||t===ae,mm=(t,i)=>parseFloat(t.split(", ")[i]),fm=(t,i)=>(n,{transform:s})=>{if(s==="none"||!s)return 0;const r=s.match(/^matrix3d\((.+)\)$/u);if(r)return mm(r[1],i);{const c=s.match(/^matrix\((.+)\)$/u);return c?mm(c[1],t):0}},jM=new Set(["x","y","z"]),VM=Ki.filter(t=>!jM.has(t));function NM(t){const i=[];return VM.forEach(n=>{const s=t.getValue(n);s!==void 0&&(i.push([n,s.get()]),s.set(n.startsWith("scale")?1:0))}),i}const Vi={width:({x:t},{paddingLeft:i="0",paddingRight:n="0"})=>t.max-t.min-parseFloat(i)-parseFloat(n),height:({y:t},{paddingTop:i="0",paddingBottom:n="0"})=>t.max-t.min-parseFloat(i)-parseFloat(n),top:(t,{top:i})=>parseFloat(i),left:(t,{left:i})=>parseFloat(i),bottom:({y:t},{top:i})=>parseFloat(i)+(t.max-t.min),right:({x:t},{left:i})=>parseFloat(i)+(t.max-t.min),x:fm(4,13),y:fm(5,14)};Vi.translateX=Vi.x;Vi.translateY=Vi.y;const ai=new Set;let Vc=!1,Nc=!1;function ty(){if(Nc){const t=Array.from(ai).filter(s=>s.needsMeasurement),i=new Set(t.map(s=>s.element)),n=new Map;i.forEach(s=>{const r=NM(s);r.length&&(n.set(s,r),s.render())}),t.forEach(s=>s.measureInitialState()),i.forEach(s=>{s.render();const r=n.get(s);r&&r.forEach(([c,h])=>{var p;(p=s.getValue(c))===null||p===void 0||p.set(h)})}),t.forEach(s=>s.measureEndState()),t.forEach(s=>{s.suspendedScrollY!==void 0&&window.scrollTo(0,s.suspendedScrollY)})}Nc=!1,Vc=!1,ai.forEach(t=>t.complete()),ai.clear()}function ay(){ai.forEach(t=>{t.readKeyframes(),t.needsMeasurement&&(Nc=!0)})}function WM(){ay(),ty()}class jd{constructor(i,n,s,r,c,h=!1){this.isComplete=!1,this.isAsync=!1,this.needsMeasurement=!1,this.isScheduled=!1,this.unresolvedKeyframes=[...i],this.onComplete=n,this.name=s,this.motionValue=r,this.element=c,this.isAsync=h}scheduleResolve(){this.isScheduled=!0,this.isAsync?(ai.add(this),Vc||(Vc=!0,fe.read(ay),fe.resolveKeyframes(ty))):(this.readKeyframes(),this.complete())}readKeyframes(){const{unresolvedKeyframes:i,name:n,element:s,motionValue:r}=this;for(let c=0;c<i.length;c++)if(i[c]===null)if(c===0){const h=r==null?void 0:r.get(),p=i[i.length-1];if(h!==void 0)i[0]=h;else if(s&&n){const m=s.readValue(n,p);m!=null&&(i[0]=m)}i[0]===void 0&&(i[0]=p),r&&h===void 0&&r.set(i[0])}else i[c]=i[c-1]}setFinalKeyframe(){}measureInitialState(){}renderEndStyles(){}measureEndState(){}complete(){this.isComplete=!0,this.onComplete(this.unresolvedKeyframes,this.finalKeyframe),ai.delete(this)}cancel(){this.isComplete||(this.isScheduled=!1,ai.delete(this))}resume(){this.isComplete||this.scheduleResolve()}}const iy=t=>/^-?(?:\d+(?:\.\d+)?|\.\d+)$/u.test(t),BM=/^var\(--(?:([\w-]+)|([\w-]+), ?([a-zA-Z\d ()%#.,-]+))\)/u;function zM(t){const i=BM.exec(t);if(!i)return[,];const[,n,s,r]=i;return[`--${n??s}`,r]}function ny(t,i,n=1){const[s,r]=zM(t);if(!s)return;const c=window.getComputedStyle(i).getPropertyValue(s);if(c){const h=c.trim();return iy(h)?parseFloat(h):h}return yd(r)?ny(r,i,n+1):r}const oy=t=>i=>i.test(t),HM={test:t=>t==="auto",parse:t=>t},sy=[_i,ae,Yt,Ua,OI,qI,HM],gm=t=>sy.find(oy(t));class ry extends jd{constructor(i,n,s,r,c){super(i,n,s,r,c,!0)}readKeyframes(){const{unresolvedKeyframes:i,element:n,name:s}=this;if(!n||!n.current)return;super.readKeyframes();for(let m=0;m<i.length;m++){let f=i[m];if(typeof f=="string"&&(f=f.trim(),yd(f))){const y=ny(f,n.current);y!==void 0&&(i[m]=y),m===i.length-1&&(this.finalKeyframe=f)}}if(this.resolveNoneKeyframes(),!jg.has(s)||i.length!==2)return;const[r,c]=i,h=gm(r),p=gm(c);if(h!==p)if(pm(h)&&pm(p))for(let m=0;m<i.length;m++){const f=i[m];typeof f=="string"&&(i[m]=parseFloat(f))}else this.needsMeasurement=!0}resolveNoneKeyframes(){const{unresolvedKeyframes:i,name:n}=this,s=[];for(let r=0;r<i.length;r++)bM(i[r])&&s.push(r);s.length&&LM(i,s,n)}measureInitialState(){const{element:i,unresolvedKeyframes:n,name:s}=this;if(!i||!i.current)return;s==="height"&&(this.suspendedScrollY=window.pageYOffset),this.measuredOrigin=Vi[s](i.measureViewportBox(),window.getComputedStyle(i.current)),n[0]=this.measuredOrigin;const r=n[n.length-1];r!==void 0&&i.getValue(s,r).jump(r,!1)}measureEndState(){var i;const{element:n,name:s,unresolvedKeyframes:r}=this;if(!n||!n.current)return;const c=n.getValue(s);c&&c.jump(this.measuredOrigin,!1);const h=r.length-1,p=r[h];r[h]=Vi[s](n.measureViewportBox(),window.getComputedStyle(n.current)),p!==null&&this.finalKeyframe===void 0&&(this.finalKeyframe=p),!((i=this.removedTransforms)===null||i===void 0)&&i.length&&this.removedTransforms.forEach(([m,f])=>{n.getValue(m).set(f)}),this.resolveNoneKeyframes()}}const ym=(t,i)=>i==="zIndex"?!1:!!(typeof t=="number"||Array.isArray(t)||typeof t=="string"&&(Oa.test(t)||t==="0")&&!t.startsWith("url("));function KM(t){const i=t[0];if(t.length===1)return!0;for(let n=0;n<t.length;n++)if(t[n]!==i)return!0}function _M(t,i,n,s){const r=t[0];if(r===null)return!1;if(i==="display"||i==="visibility")return!0;const c=t[t.length-1],h=ym(r,i),p=ym(c,i);return!h||!p?!1:KM(t)||(n==="spring"||Cd(n))&&s}const GM=t=>t!==null;function rr(t,{repeat:i,repeatType:n="loop"},s){const r=t.filter(GM),c=i&&n!=="loop"&&i%2===1?0:r.length-1;return!c||s===void 0?r[c]:s}const JM=40;class ly{constructor({autoplay:i=!0,delay:n=0,type:s="keyframes",repeat:r=0,repeatDelay:c=0,repeatType:h="loop",...p}){this.isStopped=!1,this.hasAttemptedResolve=!1,this.createdAt=Qt.now(),this.options={autoplay:i,delay:n,type:s,repeat:r,repeatDelay:c,repeatType:h,...p},this.updateFinishedPromise()}calcStartTime(){return this.resolvedAt?this.resolvedAt-this.createdAt>JM?this.resolvedAt:this.createdAt:this.createdAt}get resolved(){return!this._resolved&&!this.hasAttemptedResolve&&WM(),this._resolved}onKeyframesResolved(i,n){this.resolvedAt=Qt.now(),this.hasAttemptedResolve=!0;const{name:s,type:r,velocity:c,delay:h,onComplete:p,onUpdate:m,isGenerator:f}=this.options;if(!f&&!_M(i,s,r,c))if(h)this.options.duration=0;else{m&&m(rr(i,this.options,n)),p&&p(),this.resolveFinishedPromise();return}const y=this.initPlayback(i,n);y!==!1&&(this._resolved={keyframes:i,finalKeyframe:n,...y},this.onPostResolved())}onPostResolved(){}then(i,n){return this.currentFinishedPromise.then(i,n)}flatten(){this.options.type="keyframes",this.options.ease="linear"}updateFinishedPromise(){this.currentFinishedPromise=new Promise(i=>{this.resolveFinishedPromise=i})}}const Re=(t,i,n)=>t+(i-t)*n;function fc(t,i,n){return n<0&&(n+=1),n>1&&(n-=1),n<1/6?t+(i-t)*6*n:n<1/2?i:n<2/3?t+(i-t)*(2/3-n)*6:t}function YM({hue:t,saturation:i,lightness:n,alpha:s}){t/=360,i/=100,n/=100;let r=0,c=0,h=0;if(!i)r=c=h=n;else{const p=n<.5?n*(1+i):n+i-n*i,m=2*n-p;r=fc(m,p,t+1/3),c=fc(m,p,t),h=fc(m,p,t-1/3)}return{red:Math.round(r*255),green:Math.round(c*255),blue:Math.round(h*255),alpha:s}}function Hs(t,i){return n=>n>0?i:t}const gc=(t,i,n)=>{const s=t*t,r=n*(i*i-s)+s;return r<0?0:Math.sqrt(r)},QM=[Lc,ti,Ui],$M=t=>QM.find(i=>i.test(t));function wm(t){const i=$M(t);if(!i)return!1;let n=i.parse(t);return i===Ui&&(n=YM(n)),n}const vm=(t,i)=>{const n=wm(t),s=wm(i);if(!n||!s)return Hs(t,i);const r={...n};return c=>(r.red=gc(n.red,s.red,c),r.green=gc(n.green,s.green,c),r.blue=gc(n.blue,s.blue,c),r.alpha=Re(n.alpha,s.alpha,c),ti.transform(r))},XM=(t,i)=>n=>i(t(n)),uo=(...t)=>t.reduce(XM),Wc=new Set(["none","hidden"]);function ZM(t,i){return Wc.has(t)?n=>n<=0?t:i:n=>n>=1?i:t}function eP(t,i){return n=>Re(t,i,n)}function Vd(t){return typeof t=="number"?eP:typeof t=="string"?yd(t)?Hs:it.test(t)?vm:iP:Array.isArray(t)?cy:typeof t=="object"?it.test(t)?vm:tP:Hs}function cy(t,i){const n=[...t],s=n.length,r=t.map((c,h)=>Vd(c)(c,i[h]));return c=>{for(let h=0;h<s;h++)n[h]=r[h](c);return n}}function tP(t,i){const n={...t,...i},s={};for(const r in n)t[r]!==void 0&&i[r]!==void 0&&(s[r]=Vd(t[r])(t[r],i[r]));return r=>{for(const c in s)n[c]=s[c](r);return n}}function aP(t,i){var n;const s=[],r={color:0,var:0,number:0};for(let c=0;c<i.values.length;c++){const h=i.types[c],p=t.indexes[h][r[h]],m=(n=t.values[p])!==null&&n!==void 0?n:0;s[c]=m,r[h]++}return s}const iP=(t,i)=>{const n=Oa.createTransformer(i),s=to(t),r=to(i);return s.indexes.var.length===r.indexes.var.length&&s.indexes.color.length===r.indexes.color.length&&s.indexes.number.length>=r.indexes.number.length?Wc.has(t)&&!r.values.length||Wc.has(i)&&!s.values.length?ZM(t,i):uo(cy(aP(s,r),r.values),n):Hs(t,i)};function dy(t,i,n){return typeof t=="number"&&typeof i=="number"&&typeof n=="number"?Re(t,i,n):Vd(t)(t,i)}const nP=5;function hy(t,i,n){const s=Math.max(i-nP,0);return Ed(n-t(s),i-s)}const qe={stiffness:100,damping:10,mass:1,velocity:0,duration:800,bounce:.3,visualDuration:.3,restSpeed:{granular:.01,default:2},restDelta:{granular:.005,default:.5},minDuration:.01,maxDuration:10,minDamping:.05,maxDamping:1},yc=.001;function oP({duration:t=qe.duration,bounce:i=qe.bounce,velocity:n=qe.velocity,mass:s=qe.mass}){let r,c,h=1-i;h=Xt(qe.minDamping,qe.maxDamping,h),t=Xt(qe.minDuration,qe.maxDuration,da(t)),h<1?(r=f=>{const y=f*h,v=y*t,b=y-n,A=Bc(f,h),T=Math.exp(-v);return yc-b/A*T},c=f=>{const v=f*h*t,b=v*n+n,A=Math.pow(h,2)*Math.pow(f,2)*t,T=Math.exp(-v),I=Bc(Math.pow(f,2),h);return(-r(f)+yc>0?-1:1)*((b-A)*T)/I}):(r=f=>{const y=Math.exp(-f*t),v=(f-n)*t+1;return-yc+y*v},c=f=>{const y=Math.exp(-f*t),v=(n-f)*(t*t);return y*v});const p=5/t,m=rP(r,c,p);if(t=ca(t),isNaN(m))return{stiffness:qe.stiffness,damping:qe.damping,duration:t};{const f=Math.pow(m,2)*s;return{stiffness:f,damping:h*2*Math.sqrt(s*f),duration:t}}}const sP=12;function rP(t,i,n){let s=n;for(let r=1;r<sP;r++)s=s-t(s)/i(s);return s}function Bc(t,i){return t*Math.sqrt(1-i*i)}const lP=["duration","bounce"],cP=["stiffness","damping","mass"];function bm(t,i){return i.some(n=>t[n]!==void 0)}function dP(t){let i={velocity:qe.velocity,stiffness:qe.stiffness,damping:qe.damping,mass:qe.mass,isResolvedFromDuration:!1,...t};if(!bm(t,cP)&&bm(t,lP))if(t.visualDuration){const n=t.visualDuration,s=2*Math.PI/(n*1.2),r=s*s,c=2*Xt(.05,1,1-(t.bounce||0))*Math.sqrt(r);i={...i,mass:qe.mass,stiffness:r,damping:c}}else{const n=oP(t);i={...i,...n,mass:qe.mass},i.isResolvedFromDuration=!0}return i}function uy(t=qe.visualDuration,i=qe.bounce){const n=typeof t!="object"?{visualDuration:t,keyframes:[0,1],bounce:i}:t;let{restSpeed:s,restDelta:r}=n;const c=n.keyframes[0],h=n.keyframes[n.keyframes.length-1],p={done:!1,value:c},{stiffness:m,damping:f,mass:y,duration:v,velocity:b,isResolvedFromDuration:A}=dP({...n,velocity:-da(n.velocity||0)}),T=b||0,I=f/(2*Math.sqrt(m*y)),M=h-c,R=da(Math.sqrt(m/y)),O=Math.abs(M)<5;s||(s=O?qe.restSpeed.granular:qe.restSpeed.default),r||(r=O?qe.restDelta.granular:qe.restDelta.default);let q;if(I<1){const V=Bc(R,I);q=K=>{const ie=Math.exp(-I*R*K);return h-ie*((T+I*R*M)/V*Math.sin(V*K)+M*Math.cos(V*K))}}else if(I===1)q=V=>h-Math.exp(-R*V)*(M+(T+R*M)*V);else{const V=R*Math.sqrt(I*I-1);q=K=>{const ie=Math.exp(-I*R*K),J=Math.min(V*K,300);return h-ie*((T+I*R*M)*Math.sinh(J)+V*M*Math.cosh(J))/V}}const z={calculatedDuration:A&&v||null,next:V=>{const K=q(V);if(A)p.done=V>=v;else{let ie=0;I<1&&(ie=V===0?ca(T):hy(q,V,K));const J=Math.abs(ie)<=s,ce=Math.abs(h-K)<=r;p.done=J&&ce}return p.value=p.done?h:K,p},toString:()=>{const V=Math.min(Rg(z),qc),K=Eg(ie=>z.next(V*ie).value,V,30);return V+"ms "+K}};return z}function km({keyframes:t,velocity:i=0,power:n=.8,timeConstant:s=325,bounceDamping:r=10,bounceStiffness:c=500,modifyTarget:h,min:p,max:m,restDelta:f=.5,restSpeed:y}){const v=t[0],b={done:!1,value:v},A=J=>p!==void 0&&J<p||m!==void 0&&J>m,T=J=>p===void 0?m:m===void 0||Math.abs(p-J)<Math.abs(m-J)?p:m;let I=n*i;const M=v+I,R=h===void 0?M:h(M);R!==M&&(I=R-v);const O=J=>-I*Math.exp(-J/s),q=J=>R+O(J),z=J=>{const ce=O(J),we=q(J);b.done=Math.abs(ce)<=f,b.value=b.done?R:we};let V,K;const ie=J=>{A(b.value)&&(V=J,K=uy({keyframes:[b.value,T(b.value)],velocity:hy(q,J,b.value),damping:r,stiffness:c,restDelta:f,restSpeed:y}))};return ie(0),{calculatedDuration:null,next:J=>{let ce=!1;return!K&&V===void 0&&(ce=!0,z(J),ie(J)),V!==void 0&&J>=V?K.next(J-V):(!ce&&z(J),b)}}}const hP=ho(.42,0,1,1),uP=ho(0,0,.58,1),py=ho(.42,0,.58,1),pP=t=>Array.isArray(t)&&typeof t[0]!="number",mP={linear:rt,easeIn:hP,easeInOut:py,easeOut:uP,circIn:qd,circInOut:Gg,circOut:_g,backIn:Ud,backInOut:Hg,backOut:zg,anticipate:Kg},xm=t=>{if(Td(t)){dg(t.length===4);const[i,n,s,r]=t;return ho(i,n,s,r)}else if(typeof t=="string")return mP[t];return t};function fP(t,i,n){const s=[],r=n||dy,c=t.length-1;for(let h=0;h<c;h++){let p=r(t[h],t[h+1]);if(i){const m=Array.isArray(i)?i[h]||rt:i;p=uo(m,p)}s.push(p)}return s}function my(t,i,{clamp:n=!0,ease:s,mixer:r}={}){const c=t.length;if(dg(c===i.length),c===1)return()=>i[0];if(c===2&&i[0]===i[1])return()=>i[1];const h=t[0]===t[1];t[0]>t[c-1]&&(t=[...t].reverse(),i=[...i].reverse());const p=fP(i,s,r),m=p.length,f=y=>{if(h&&y<t[0])return i[0];let v=0;if(m>1)for(;v<t.length-2&&!(y<t[v+1]);v++);const b=ii(t[v],t[v+1],y);return p[v](b)};return n?y=>f(Xt(t[0],t[c-1],y)):f}function gP(t,i){const n=t[t.length-1];for(let s=1;s<=i;s++){const r=ii(0,i,s);t.push(Re(n,1,r))}}function fy(t){const i=[0];return gP(i,t.length-1),i}function yP(t,i){return t.map(n=>n*i)}function wP(t,i){return t.map(()=>i||py).splice(0,t.length-1)}function Ks({duration:t=300,keyframes:i,times:n,ease:s="easeInOut"}){const r=pP(s)?s.map(xm):xm(s),c={done:!1,value:i[0]},h=yP(n&&n.length===i.length?n:fy(i),t),p=my(h,i,{ease:Array.isArray(r)?r:wP(i,r)});return{calculatedDuration:t,next:m=>(c.value=p(m),c.done=m>=t,c)}}const vP=t=>{const i=({timestamp:n})=>t(n);return{start:()=>fe.update(i,!0),stop:()=>$t(i),now:()=>ze.isProcessing?ze.timestamp:Qt.now()}},bP={decay:km,inertia:km,tween:Ks,keyframes:Ks,spring:uy},kP=t=>t/100;class lr extends ly{constructor(i){super(i),this.holdTime=null,this.cancelTime=null,this.currentTime=0,this.playbackSpeed=1,this.pendingPlayState="running",this.startTime=null,this.state="idle",this.stop=()=>{if(this.resolver.cancel(),this.isStopped=!0,this.state==="idle")return;this.teardown();const{onStop:m}=this.options;m&&m()};const{name:n,motionValue:s,element:r,keyframes:c}=this.options,h=(r==null?void 0:r.KeyframeResolver)||jd,p=(m,f)=>this.onKeyframesResolved(m,f);this.resolver=new h(c,p,n,s,r),this.resolver.scheduleResolve()}flatten(){super.flatten(),this._resolved&&Object.assign(this._resolved,this.initPlayback(this._resolved.keyframes))}initPlayback(i){const{type:n="keyframes",repeat:s=0,repeatDelay:r=0,repeatType:c,velocity:h=0}=this.options,p=Cd(n)?n:bP[n]||Ks;let m,f;p!==Ks&&typeof i[0]!="number"&&(m=uo(kP,dy(i[0],i[1])),i=[0,100]);const y=p({...this.options,keyframes:i});c==="mirror"&&(f=p({...this.options,keyframes:[...i].reverse(),velocity:-h})),y.calculatedDuration===null&&(y.calculatedDuration=Rg(y));const{calculatedDuration:v}=y,b=v+r,A=b*(s+1)-r;return{generator:y,mirroredGenerator:f,mapPercentToKeyframes:m,calculatedDuration:v,resolvedDuration:b,totalDuration:A}}onPostResolved(){const{autoplay:i=!0}=this.options;this.play(),this.pendingPlayState==="paused"||!i?this.pause():this.state=this.pendingPlayState}tick(i,n=!1){const{resolved:s}=this;if(!s){const{keyframes:J}=this.options;return{done:!0,value:J[J.length-1]}}const{finalKeyframe:r,generator:c,mirroredGenerator:h,mapPercentToKeyframes:p,keyframes:m,calculatedDuration:f,totalDuration:y,resolvedDuration:v}=s;if(this.startTime===null)return c.next(0);const{delay:b,repeat:A,repeatType:T,repeatDelay:I,onUpdate:M}=this.options;this.speed>0?this.startTime=Math.min(this.startTime,i):this.speed<0&&(this.startTime=Math.min(i-y/this.speed,this.startTime)),n?this.currentTime=i:this.holdTime!==null?this.currentTime=this.holdTime:this.currentTime=Math.round(i-this.startTime)*this.speed;const R=this.currentTime-b*(this.speed>=0?1:-1),O=this.speed>=0?R<0:R>y;this.currentTime=Math.max(R,0),this.state==="finished"&&this.holdTime===null&&(this.currentTime=y);let q=this.currentTime,z=c;if(A){const J=Math.min(this.currentTime,y)/v;let ce=Math.floor(J),we=J%1;!we&&J>=1&&(we=1),we===1&&ce--,ce=Math.min(ce,A+1),!!(ce%2)&&(T==="reverse"?(we=1-we,I&&(we-=I/v)):T==="mirror"&&(z=h)),q=Xt(0,1,we)*v}const V=O?{done:!1,value:m[0]}:z.next(q);p&&(V.value=p(V.value));let{done:K}=V;!O&&f!==null&&(K=this.speed>=0?this.currentTime>=y:this.currentTime<=0);const ie=this.holdTime===null&&(this.state==="finished"||this.state==="running"&&K);return ie&&r!==void 0&&(V.value=rr(m,this.options,r)),M&&M(V.value),ie&&this.finish(),V}get duration(){const{resolved:i}=this;return i?da(i.calculatedDuration):0}get time(){return da(this.currentTime)}set time(i){i=ca(i),this.currentTime=i,this.holdTime!==null||this.speed===0?this.holdTime=i:this.driver&&(this.startTime=this.driver.now()-i/this.speed)}get speed(){return this.playbackSpeed}set speed(i){const n=this.playbackSpeed!==i;this.playbackSpeed=i,n&&(this.time=da(this.currentTime))}play(){if(this.resolver.isScheduled||this.resolver.resume(),!this._resolved){this.pendingPlayState="running";return}if(this.isStopped)return;const{driver:i=vP,onPlay:n,startTime:s}=this.options;this.driver||(this.driver=i(c=>this.tick(c))),n&&n();const r=this.driver.now();this.holdTime!==null?this.startTime=r-this.holdTime:this.startTime?this.state==="finished"&&(this.startTime=r):this.startTime=s??this.calcStartTime(),this.state==="finished"&&this.updateFinishedPromise(),this.cancelTime=this.startTime,this.holdTime=null,this.state="running",this.driver.start()}pause(){var i;if(!this._resolved){this.pendingPlayState="paused";return}this.state="paused",this.holdTime=(i=this.currentTime)!==null&&i!==void 0?i:0}complete(){this.state!=="running"&&this.play(),this.pendingPlayState=this.state="finished",this.holdTime=null}finish(){this.teardown(),this.state="finished";const{onComplete:i}=this.options;i&&i()}cancel(){this.cancelTime!==null&&this.tick(this.cancelTime),this.teardown(),this.updateFinishedPromise()}teardown(){this.state="idle",this.stopDriver(),this.resolveFinishedPromise(),this.updateFinishedPromise(),this.startTime=this.cancelTime=null,this.resolver.cancel()}stopDriver(){this.driver&&(this.driver.stop(),this.driver=void 0)}sample(i){return this.startTime=0,this.tick(i,!0)}}function xP(t){return new lr(t)}const SP=new Set(["opacity","clipPath","filter","transform"]);function AP(t,i,n,{delay:s=0,duration:r=300,repeat:c=0,repeatType:h="loop",ease:p="easeInOut",times:m}={}){const f={[i]:n};m&&(f.offset=m);const y=qg(p,r);return Array.isArray(y)&&(f.easing=y),t.animate(f,{delay:s,duration:r,easing:Array.isArray(y)?"linear":y,fill:"both",iterations:c+1,direction:h==="reverse"?"alternate":"normal"})}const CP=dd(()=>Object.hasOwnProperty.call(Element.prototype,"animate")),_s=10,TP=2e4;function IP(t){return Cd(t.type)||t.type==="spring"||!Ug(t.ease)}function MP(t,i){const n=new lr({...i,keyframes:t,repeat:0,delay:0,isGenerator:!0});let s={done:!1,value:t[0]};const r=[];let c=0;for(;!s.done&&c<TP;)s=n.sample(c),r.push(s.value),c+=_s;return{times:void 0,keyframes:r,duration:c-_s,ease:"linear"}}const gy={anticipate:Kg,backInOut:Hg,circInOut:Gg};function PP(t){return t in gy}class Sm extends ly{constructor(i){super(i);const{name:n,motionValue:s,element:r,keyframes:c}=this.options;this.resolver=new ry(c,(h,p)=>this.onKeyframesResolved(h,p),n,s,r),this.resolver.scheduleResolve()}initPlayback(i,n){let{duration:s=300,times:r,ease:c,type:h,motionValue:p,name:m,startTime:f}=this.options;if(!p.owner||!p.owner.current)return!1;if(typeof c=="string"&&zs()&&PP(c)&&(c=gy[c]),IP(this.options)){const{onComplete:v,onUpdate:b,motionValue:A,element:T,...I}=this.options,M=MP(i,I);i=M.keyframes,i.length===1&&(i[1]=i[0]),s=M.duration,r=M.times,c=M.ease,h="keyframes"}const y=AP(p.owner.current,m,i,{...this.options,duration:s,times:r,ease:c});return y.startTime=f??this.calcStartTime(),this.pendingTimeline?(rm(y,this.pendingTimeline),this.pendingTimeline=void 0):y.onfinish=()=>{const{onComplete:v}=this.options;p.set(rr(i,this.options,n)),v&&v(),this.cancel(),this.resolveFinishedPromise()},{animation:y,duration:s,times:r,type:h,ease:c,keyframes:i}}get duration(){const{resolved:i}=this;if(!i)return 0;const{duration:n}=i;return da(n)}get time(){const{resolved:i}=this;if(!i)return 0;const{animation:n}=i;return da(n.currentTime||0)}set time(i){const{resolved:n}=this;if(!n)return;const{animation:s}=n;s.currentTime=ca(i)}get speed(){const{resolved:i}=this;if(!i)return 1;const{animation:n}=i;return n.playbackRate}set speed(i){const{resolved:n}=this;if(!n)return;const{animation:s}=n;s.playbackRate=i}get state(){const{resolved:i}=this;if(!i)return"idle";const{animation:n}=i;return n.playState}get startTime(){const{resolved:i}=this;if(!i)return null;const{animation:n}=i;return n.startTime}attachTimeline(i){if(!this._resolved)this.pendingTimeline=i;else{const{resolved:n}=this;if(!n)return rt;const{animation:s}=n;rm(s,i)}return rt}play(){if(this.isStopped)return;const{resolved:i}=this;if(!i)return;const{animation:n}=i;n.playState==="finished"&&this.updateFinishedPromise(),n.play()}pause(){const{resolved:i}=this;if(!i)return;const{animation:n}=i;n.pause()}stop(){if(this.resolver.cancel(),this.isStopped=!0,this.state==="idle")return;this.resolveFinishedPromise(),this.updateFinishedPromise();const{resolved:i}=this;if(!i)return;const{animation:n,keyframes:s,duration:r,type:c,ease:h,times:p}=i;if(n.playState==="idle"||n.playState==="finished")return;if(this.time){const{motionValue:f,onUpdate:y,onComplete:v,element:b,...A}=this.options,T=new lr({...A,keyframes:s,duration:r,type:c,ease:h,times:p,isGenerator:!0}),I=ca(this.time);f.setWithVelocity(T.sample(I-_s).value,T.sample(I).value,_s)}const{onStop:m}=this.options;m&&m(),this.cancel()}complete(){const{resolved:i}=this;i&&i.animation.finish()}cancel(){const{resolved:i}=this;i&&i.animation.cancel()}static supports(i){const{motionValue:n,name:s,repeatDelay:r,repeatType:c,damping:h,type:p}=i;if(!n||!n.owner||!(n.owner.current instanceof HTMLElement))return!1;const{onUpdate:m,transformTemplate:f}=n.owner.getProps();return CP()&&s&&SP.has(s)&&!m&&!f&&!r&&c!=="mirror"&&h!==0&&p!=="inertia"}}const DP={type:"spring",stiffness:500,damping:25,restSpeed:10},RP=t=>({type:"spring",stiffness:550,damping:t===0?2*Math.sqrt(550):30,restSpeed:10}),EP={type:"keyframes",duration:.8},UP={type:"keyframes",ease:[.25,.1,.35,1],duration:.3},qP=(t,{keyframes:i})=>i.length>2?EP:ni.has(t)?t.startsWith("scale")?RP(i[1]):DP:UP;function OP({when:t,delay:i,delayChildren:n,staggerChildren:s,staggerDirection:r,repeat:c,repeatType:h,repeatDelay:p,from:m,elapsed:f,...y}){return!!Object.keys(y).length}const Nd=(t,i,n,s={},r,c)=>h=>{const p=Ad(s,t)||{},m=p.delay||s.delay||0;let{elapsed:f=0}=s;f=f-ca(m);let y={keyframes:Array.isArray(n)?n:[null,n],ease:"easeOut",velocity:i.getVelocity(),...p,delay:-f,onUpdate:b=>{i.set(b),p.onUpdate&&p.onUpdate(b)},onComplete:()=>{h(),p.onComplete&&p.onComplete()},name:t,motionValue:i,element:c?void 0:r};OP(p)||(y={...y,...qP(t,y)}),y.duration&&(y.duration=ca(y.duration)),y.repeatDelay&&(y.repeatDelay=ca(y.repeatDelay)),y.from!==void 0&&(y.keyframes[0]=y.from);let v=!1;if((y.type===!1||y.duration===0&&!y.repeatDelay)&&(y.duration=0,y.delay===0&&(v=!0)),v&&!c&&i.get()!==void 0){const b=rr(y.keyframes,p);if(b!==void 0)return fe.update(()=>{y.onUpdate(b),y.onComplete()}),new aM([])}return!c&&Sm.supports(y)?new Sm(y):new lr(y)};function FP({protectedKeys:t,needsAnimating:i},n){const s=t.hasOwnProperty(n)&&i[n]!==!0;return i[n]=!1,s}function yy(t,i,{delay:n=0,transitionOverride:s,type:r}={}){var c;let{transition:h=t.getDefaultTransition(),transitionEnd:p,...m}=i;s&&(h=s);const f=[],y=r&&t.animationState&&t.animationState.getState()[r];for(const v in m){const b=t.getValue(v,(c=t.latestValues[v])!==null&&c!==void 0?c:null),A=m[v];if(A===void 0||y&&FP(y,v))continue;const T={delay:n,...Ad(h||{},v)};let I=!1;if(window.MotionHandoffAnimation){const R=Vg(t);if(R){const O=window.MotionHandoffAnimation(R,v,fe);O!==null&&(T.startTime=O,I=!0)}}Fc(t,v),b.start(Nd(v,b,A,t.shouldReduceMotion&&jg.has(v)?{type:!1}:T,t,I));const M=b.animation;M&&f.push(M)}return p&&Promise.all(f).then(()=>{fe.update(()=>{p&&fM(t,p)})}),f}function zc(t,i,n={}){var s;const r=sr(t,i,n.type==="exit"?(s=t.presenceContext)===null||s===void 0?void 0:s.custom:void 0);let{transition:c=t.getDefaultTransition()||{}}=r||{};n.transitionOverride&&(c=n.transitionOverride);const h=r?()=>Promise.all(yy(t,r,n)):()=>Promise.resolve(),p=t.variantChildren&&t.variantChildren.size?(f=0)=>{const{delayChildren:y=0,staggerChildren:v,staggerDirection:b}=c;return LP(t,i,y+f,v,b,n)}:()=>Promise.resolve(),{when:m}=c;if(m){const[f,y]=m==="beforeChildren"?[h,p]:[p,h];return f().then(()=>y())}else return Promise.all([h(),p(n.delay)])}function LP(t,i,n=0,s=0,r=1,c){const h=[],p=(t.variantChildren.size-1)*s,m=r===1?(f=0)=>f*s:(f=0)=>p-f*s;return Array.from(t.variantChildren).sort(jP).forEach((f,y)=>{f.notify("AnimationStart",i),h.push(zc(f,i,{...c,delay:n+m(y)}).then(()=>f.notify("AnimationComplete",i)))}),Promise.all(h)}function jP(t,i){return t.sortNodePosition(i)}function VP(t,i,n={}){t.notify("AnimationStart",i);let s;if(Array.isArray(i)){const r=i.map(c=>zc(t,c,n));s=Promise.all(r)}else if(typeof i=="string")s=zc(t,i,n);else{const r=typeof i=="function"?sr(t,i,n.custom):i;s=Promise.all(yy(t,r,n))}return s.then(()=>{t.notify("AnimationComplete",i)})}const NP=ud.length;function wy(t){if(!t)return;if(!t.isControllingVariants){const n=t.parent?wy(t.parent)||{}:{};return t.props.initial!==void 0&&(n.initial=t.props.initial),n}const i={};for(let n=0;n<NP;n++){const s=ud[n],r=t.props[s];(Zn(r)||r===!1)&&(i[s]=r)}return i}const WP=[...hd].reverse(),BP=hd.length;function zP(t){return i=>Promise.all(i.map(({animation:n,options:s})=>VP(t,n,s)))}function HP(t){let i=zP(t),n=Am(),s=!0;const r=m=>(f,y)=>{var v;const b=sr(t,y,m==="exit"?(v=t.presenceContext)===null||v===void 0?void 0:v.custom:void 0);if(b){const{transition:A,transitionEnd:T,...I}=b;f={...f,...I,...T}}return f};function c(m){i=m(t)}function h(m){const{props:f}=t,y=wy(t.parent)||{},v=[],b=new Set;let A={},T=1/0;for(let M=0;M<BP;M++){const R=WP[M],O=n[R],q=f[R]!==void 0?f[R]:y[R],z=Zn(q),V=R===m?O.isActive:null;V===!1&&(T=M);let K=q===y[R]&&q!==f[R]&&z;if(K&&s&&t.manuallyAnimateOnMount&&(K=!1),O.protectedKeys={...A},!O.isActive&&V===null||!q&&!O.prevProp||nr(q)||typeof q=="boolean")continue;const ie=KP(O.prevProp,q);let J=ie||R===m&&O.isActive&&!K&&z||M>T&&z,ce=!1;const we=Array.isArray(q)?q:[q];let Te=we.reduce(r(R),{});V===!1&&(Te={});const{prevResolvedValues:Ke={}}=O,je={...Ke,...Te},Ve=oe=>{J=!0,b.has(oe)&&(ce=!0,b.delete(oe)),O.needsAnimating[oe]=!0;const W=t.getValue(oe);W&&(W.liveStyle=!1)};for(const oe in je){const W=Te[oe],Z=Ke[oe];if(A.hasOwnProperty(oe))continue;let H=!1;Uc(W)&&Uc(Z)?H=!Pg(W,Z):H=W!==Z,H?W!=null?Ve(oe):b.add(oe):W!==void 0&&b.has(oe)?Ve(oe):O.protectedKeys[oe]=!0}O.prevProp=q,O.prevResolvedValues=Te,O.isActive&&(A={...A,...Te}),s&&t.blockInitialAnimation&&(J=!1),J&&(!(K&&ie)||ce)&&v.push(...we.map(oe=>({animation:oe,options:{type:R}})))}if(b.size){const M={};b.forEach(R=>{const O=t.getBaseTarget(R),q=t.getValue(R);q&&(q.liveStyle=!0),M[R]=O??null}),v.push({animation:M})}let I=!!v.length;return s&&(f.initial===!1||f.initial===f.animate)&&!t.manuallyAnimateOnMount&&(I=!1),s=!1,I?i(v):Promise.resolve()}function p(m,f){var y;if(n[m].isActive===f)return Promise.resolve();(y=t.variantChildren)===null||y===void 0||y.forEach(b=>{var A;return(A=b.animationState)===null||A===void 0?void 0:A.setActive(m,f)}),n[m].isActive=f;const v=h(m);for(const b in n)n[b].protectedKeys={};return v}return{animateChanges:h,setActive:p,setAnimateFunction:c,getState:()=>n,reset:()=>{n=Am(),s=!0}}}function KP(t,i){return typeof i=="string"?i!==t:Array.isArray(i)?!Pg(i,t):!1}function Qa(t=!1){return{isActive:t,protectedKeys:{},needsAnimating:{},prevResolvedValues:{}}}function Am(){return{animate:Qa(!0),whileInView:Qa(),whileHover:Qa(),whileTap:Qa(),whileDrag:Qa(),whileFocus:Qa(),exit:Qa()}}class Fa{constructor(i){this.isMounted=!1,this.node=i}update(){}}class _P extends Fa{constructor(i){super(i),i.animationState||(i.animationState=HP(i))}updateAnimationControlsSubscription(){const{animate:i}=this.node.getProps();nr(i)&&(this.unmountControls=i.subscribe(this.node))}mount(){this.updateAnimationControlsSubscription()}update(){const{animate:i}=this.node.getProps(),{animate:n}=this.node.prevProps||{};i!==n&&this.updateAnimationControlsSubscription()}unmount(){var i;this.node.animationState.reset(),(i=this.unmountControls)===null||i===void 0||i.call(this)}}let GP=0;class JP extends Fa{constructor(){super(...arguments),this.id=GP++}update(){if(!this.node.presenceContext)return;const{isPresent:i,onExitComplete:n}=this.node.presenceContext,{isPresent:s}=this.node.prevPresenceContext||{};if(!this.node.animationState||i===s)return;const r=this.node.animationState.setActive("exit",!i);n&&!i&&r.then(()=>n(this.id))}mount(){const{register:i}=this.node.presenceContext||{};i&&(this.unmount=i(this.id))}unmount(){}}const YP={animation:{Feature:_P},exit:{Feature:JP}};function ao(t,i,n,s={passive:!0}){return t.addEventListener(i,n,s),()=>t.removeEventListener(i,n)}function po(t){return{point:{x:t.pageX,y:t.pageY}}}const QP=t=>i=>Md(i)&&t(i,po(i));function Gn(t,i,n,s){return ao(t,i,QP(n),s)}const Cm=(t,i)=>Math.abs(t-i);function $P(t,i){const n=Cm(t.x,i.x),s=Cm(t.y,i.y);return Math.sqrt(n**2+s**2)}class vy{constructor(i,n,{transformPagePoint:s,contextWindow:r,dragSnapToOrigin:c=!1}={}){if(this.startEvent=null,this.lastMoveEvent=null,this.lastMoveEventInfo=null,this.handlers={},this.contextWindow=window,this.updatePoint=()=>{if(!(this.lastMoveEvent&&this.lastMoveEventInfo))return;const v=vc(this.lastMoveEventInfo,this.history),b=this.startEvent!==null,A=$P(v.offset,{x:0,y:0})>=3;if(!b&&!A)return;const{point:T}=v,{timestamp:I}=ze;this.history.push({...T,timestamp:I});const{onStart:M,onMove:R}=this.handlers;b||(M&&M(this.lastMoveEvent,v),this.startEvent=this.lastMoveEvent),R&&R(this.lastMoveEvent,v)},this.handlePointerMove=(v,b)=>{this.lastMoveEvent=v,this.lastMoveEventInfo=wc(b,this.transformPagePoint),fe.update(this.updatePoint,!0)},this.handlePointerUp=(v,b)=>{this.end();const{onEnd:A,onSessionEnd:T,resumeAnimation:I}=this.handlers;if(this.dragSnapToOrigin&&I&&I(),!(this.lastMoveEvent&&this.lastMoveEventInfo))return;const M=vc(v.type==="pointercancel"?this.lastMoveEventInfo:wc(b,this.transformPagePoint),this.history);this.startEvent&&A&&A(v,M),T&&T(v,M)},!Md(i))return;this.dragSnapToOrigin=c,this.handlers=n,this.transformPagePoint=s,this.contextWindow=r||window;const h=po(i),p=wc(h,this.transformPagePoint),{point:m}=p,{timestamp:f}=ze;this.history=[{...m,timestamp:f}];const{onSessionStart:y}=n;y&&y(i,vc(p,this.history)),this.removeListeners=uo(Gn(this.contextWindow,"pointermove",this.handlePointerMove),Gn(this.contextWindow,"pointerup",this.handlePointerUp),Gn(this.contextWindow,"pointercancel",this.handlePointerUp))}updateHandlers(i){this.handlers=i}end(){this.removeListeners&&this.removeListeners(),$t(this.updatePoint)}}function wc(t,i){return i?{point:i(t.point)}:t}function Tm(t,i){return{x:t.x-i.x,y:t.y-i.y}}function vc({point:t},i){return{point:t,delta:Tm(t,by(i)),offset:Tm(t,XP(i)),velocity:ZP(i,.1)}}function XP(t){return t[0]}function by(t){return t[t.length-1]}function ZP(t,i){if(t.length<2)return{x:0,y:0};let n=t.length-1,s=null;const r=by(t);for(;n>=0&&(s=t[n],!(r.timestamp-s.timestamp>ca(i)));)n--;if(!s)return{x:0,y:0};const c=da(r.timestamp-s.timestamp);if(c===0)return{x:0,y:0};const h={x:(r.x-s.x)/c,y:(r.y-s.y)/c};return h.x===1/0&&(h.x=0),h.y===1/0&&(h.y=0),h}const ky=1e-4,eD=1-ky,tD=1+ky,xy=.01,aD=0-xy,iD=0+xy;function bt(t){return t.max-t.min}function nD(t,i,n){return Math.abs(t-i)<=n}function Im(t,i,n,s=.5){t.origin=s,t.originPoint=Re(i.min,i.max,t.origin),t.scale=bt(n)/bt(i),t.translate=Re(n.min,n.max,t.origin)-t.originPoint,(t.scale>=eD&&t.scale<=tD||isNaN(t.scale))&&(t.scale=1),(t.translate>=aD&&t.translate<=iD||isNaN(t.translate))&&(t.translate=0)}function Jn(t,i,n,s){Im(t.x,i.x,n.x,s?s.originX:void 0),Im(t.y,i.y,n.y,s?s.originY:void 0)}function Mm(t,i,n){t.min=n.min+i.min,t.max=t.min+bt(i)}function oD(t,i,n){Mm(t.x,i.x,n.x),Mm(t.y,i.y,n.y)}function Pm(t,i,n){t.min=i.min-n.min,t.max=t.min+bt(i)}function Yn(t,i,n){Pm(t.x,i.x,n.x),Pm(t.y,i.y,n.y)}function sD(t,{min:i,max:n},s){return i!==void 0&&t<i?t=s?Re(i,t,s.min):Math.max(t,i):n!==void 0&&t>n&&(t=s?Re(n,t,s.max):Math.min(t,n)),t}function Dm(t,i,n){return{min:i!==void 0?t.min+i:void 0,max:n!==void 0?t.max+n-(t.max-t.min):void 0}}function rD(t,{top:i,left:n,bottom:s,right:r}){return{x:Dm(t.x,n,r),y:Dm(t.y,i,s)}}function Rm(t,i){let n=i.min-t.min,s=i.max-t.max;return i.max-i.min<t.max-t.min&&([n,s]=[s,n]),{min:n,max:s}}function lD(t,i){return{x:Rm(t.x,i.x),y:Rm(t.y,i.y)}}function cD(t,i){let n=.5;const s=bt(t),r=bt(i);return r>s?n=ii(i.min,i.max-s,t.min):s>r&&(n=ii(t.min,t.max-r,i.min)),Xt(0,1,n)}function dD(t,i){const n={};return i.min!==void 0&&(n.min=i.min-t.min),i.max!==void 0&&(n.max=i.max-t.min),n}const Hc=.35;function hD(t=Hc){return t===!1?t=0:t===!0&&(t=Hc),{x:Em(t,"left","right"),y:Em(t,"top","bottom")}}function Em(t,i,n){return{min:Um(t,i),max:Um(t,n)}}function Um(t,i){return typeof t=="number"?t:t[i]||0}const qm=()=>({translate:0,scale:1,origin:0,originPoint:0}),qi=()=>({x:qm(),y:qm()}),Om=()=>({min:0,max:0}),Le=()=>({x:Om(),y:Om()});function Pt(t){return[t("x"),t("y")]}function Sy({top:t,left:i,right:n,bottom:s}){return{x:{min:i,max:n},y:{min:t,max:s}}}function uD({x:t,y:i}){return{top:i.min,right:t.max,bottom:i.max,left:t.min}}function pD(t,i){if(!i)return t;const n=i({x:t.left,y:t.top}),s=i({x:t.right,y:t.bottom});return{top:n.y,left:n.x,bottom:s.y,right:s.x}}function bc(t){return t===void 0||t===1}function Kc({scale:t,scaleX:i,scaleY:n}){return!bc(t)||!bc(i)||!bc(n)}function Za(t){return Kc(t)||Ay(t)||t.z||t.rotate||t.rotateX||t.rotateY||t.skewX||t.skewY}function Ay(t){return Fm(t.x)||Fm(t.y)}function Fm(t){return t&&t!=="0%"}function Gs(t,i,n){const s=t-n,r=i*s;return n+r}function Lm(t,i,n,s,r){return r!==void 0&&(t=Gs(t,r,s)),Gs(t,n,s)+i}function _c(t,i=0,n=1,s,r){t.min=Lm(t.min,i,n,s,r),t.max=Lm(t.max,i,n,s,r)}function Cy(t,{x:i,y:n}){_c(t.x,i.translate,i.scale,i.originPoint),_c(t.y,n.translate,n.scale,n.originPoint)}const jm=.999999999999,Vm=1.0000000000001;function mD(t,i,n,s=!1){const r=n.length;if(!r)return;i.x=i.y=1;let c,h;for(let p=0;p<r;p++){c=n[p],h=c.projectionDelta;const{visualElement:m}=c.options;m&&m.props.style&&m.props.style.display==="contents"||(s&&c.options.layoutScroll&&c.scroll&&c!==c.root&&Fi(t,{x:-c.scroll.offset.x,y:-c.scroll.offset.y}),h&&(i.x*=h.x.scale,i.y*=h.y.scale,Cy(t,h)),s&&Za(c.latestValues)&&Fi(t,c.latestValues))}i.x<Vm&&i.x>jm&&(i.x=1),i.y<Vm&&i.y>jm&&(i.y=1)}function Oi(t,i){t.min=t.min+i,t.max=t.max+i}function Nm(t,i,n,s,r=.5){const c=Re(t.min,t.max,r);_c(t,i,n,c,s)}function Fi(t,i){Nm(t.x,i.x,i.scaleX,i.scale,i.originX),Nm(t.y,i.y,i.scaleY,i.scale,i.originY)}function Ty(t,i){return Sy(pD(t.getBoundingClientRect(),i))}function fD(t,i,n){const s=Ty(t,n),{scroll:r}=i;return r&&(Oi(s.x,r.offset.x),Oi(s.y,r.offset.y)),s}const Iy=({current:t})=>t?t.ownerDocument.defaultView:null,gD=new WeakMap;class yD{constructor(i){this.openDragLock=null,this.isDragging=!1,this.currentDirection=null,this.originPoint={x:0,y:0},this.constraints=!1,this.hasMutatedConstraints=!1,this.elastic=Le(),this.visualElement=i}start(i,{snapToCursor:n=!1}={}){const{presenceContext:s}=this.visualElement;if(s&&s.isPresent===!1)return;const r=y=>{const{dragSnapToOrigin:v}=this.getProps();v?this.pauseAnimation():this.stopAnimation(),n&&this.snapToCursor(po(y).point)},c=(y,v)=>{const{drag:b,dragPropagation:A,onDragStart:T}=this.getProps();if(b&&!A&&(this.openDragLock&&this.openDragLock(),this.openDragLock=dM(b),!this.openDragLock))return;this.isDragging=!0,this.currentDirection=null,this.resolveConstraints(),this.visualElement.projection&&(this.visualElement.projection.isAnimationBlocked=!0,this.visualElement.projection.target=void 0),Pt(M=>{let R=this.getAxisMotionValue(M).get()||0;if(Yt.test(R)){const{projection:O}=this.visualElement;if(O&&O.layout){const q=O.layout.layoutBox[M];q&&(R=bt(q)*(parseFloat(R)/100))}}this.originPoint[M]=R}),T&&fe.postRender(()=>T(y,v)),Fc(this.visualElement,"transform");const{animationState:I}=this.visualElement;I&&I.setActive("whileDrag",!0)},h=(y,v)=>{const{dragPropagation:b,dragDirectionLock:A,onDirectionLock:T,onDrag:I}=this.getProps();if(!b&&!this.openDragLock)return;const{offset:M}=v;if(A&&this.currentDirection===null){this.currentDirection=wD(M),this.currentDirection!==null&&T&&T(this.currentDirection);return}this.updateAxis("x",v.point,M),this.updateAxis("y",v.point,M),this.visualElement.render(),I&&I(y,v)},p=(y,v)=>this.stop(y,v),m=()=>Pt(y=>{var v;return this.getAnimationState(y)==="paused"&&((v=this.getAxisMotionValue(y).animation)===null||v===void 0?void 0:v.play())}),{dragSnapToOrigin:f}=this.getProps();this.panSession=new vy(i,{onSessionStart:r,onStart:c,onMove:h,onSessionEnd:p,resumeAnimation:m},{transformPagePoint:this.visualElement.getTransformPagePoint(),dragSnapToOrigin:f,contextWindow:Iy(this.visualElement)})}stop(i,n){const s=this.isDragging;if(this.cancel(),!s)return;const{velocity:r}=n;this.startAnimation(r);const{onDragEnd:c}=this.getProps();c&&fe.postRender(()=>c(i,n))}cancel(){this.isDragging=!1;const{projection:i,animationState:n}=this.visualElement;i&&(i.isAnimationBlocked=!1),this.panSession&&this.panSession.end(),this.panSession=void 0;const{dragPropagation:s}=this.getProps();!s&&this.openDragLock&&(this.openDragLock(),this.openDragLock=null),n&&n.setActive("whileDrag",!1)}updateAxis(i,n,s){const{drag:r}=this.getProps();if(!s||!Ms(i,r,this.currentDirection))return;const c=this.getAxisMotionValue(i);let h=this.originPoint[i]+s[i];this.constraints&&this.constraints[i]&&(h=sD(h,this.constraints[i],this.elastic[i])),c.set(h)}resolveConstraints(){var i;const{dragConstraints:n,dragElastic:s}=this.getProps(),r=this.visualElement.projection&&!this.visualElement.projection.layout?this.visualElement.projection.measure(!1):(i=this.visualElement.projection)===null||i===void 0?void 0:i.layout,c=this.constraints;n&&Ei(n)?this.constraints||(this.constraints=this.resolveRefConstraints()):n&&r?this.constraints=rD(r.layoutBox,n):this.constraints=!1,this.elastic=hD(s),c!==this.constraints&&r&&this.constraints&&!this.hasMutatedConstraints&&Pt(h=>{this.constraints!==!1&&this.getAxisMotionValue(h)&&(this.constraints[h]=dD(r.layoutBox[h],this.constraints[h]))})}resolveRefConstraints(){const{dragConstraints:i,onMeasureDragConstraints:n}=this.getProps();if(!i||!Ei(i))return!1;const s=i.current,{projection:r}=this.visualElement;if(!r||!r.layout)return!1;const c=fD(s,r.root,this.visualElement.getTransformPagePoint());let h=lD(r.layout.layoutBox,c);if(n){const p=n(uD(h));this.hasMutatedConstraints=!!p,p&&(h=Sy(p))}return h}startAnimation(i){const{drag:n,dragMomentum:s,dragElastic:r,dragTransition:c,dragSnapToOrigin:h,onDragTransitionEnd:p}=this.getProps(),m=this.constraints||{},f=Pt(y=>{if(!Ms(y,n,this.currentDirection))return;let v=m&&m[y]||{};h&&(v={min:0,max:0});const b=r?200:1e6,A=r?40:1e7,T={type:"inertia",velocity:s?i[y]:0,bounceStiffness:b,bounceDamping:A,timeConstant:750,restDelta:1,restSpeed:10,...c,...v};return this.startAxisValueAnimation(y,T)});return Promise.all(f).then(p)}startAxisValueAnimation(i,n){const s=this.getAxisMotionValue(i);return Fc(this.visualElement,i),s.start(Nd(i,s,0,n,this.visualElement,!1))}stopAnimation(){Pt(i=>this.getAxisMotionValue(i).stop())}pauseAnimation(){Pt(i=>{var n;return(n=this.getAxisMotionValue(i).animation)===null||n===void 0?void 0:n.pause()})}getAnimationState(i){var n;return(n=this.getAxisMotionValue(i).animation)===null||n===void 0?void 0:n.state}getAxisMotionValue(i){const n=`_drag${i.toUpperCase()}`,s=this.visualElement.getProps(),r=s[n];return r||this.visualElement.getValue(i,(s.initial?s.initial[i]:void 0)||0)}snapToCursor(i){Pt(n=>{const{drag:s}=this.getProps();if(!Ms(n,s,this.currentDirection))return;const{projection:r}=this.visualElement,c=this.getAxisMotionValue(n);if(r&&r.layout){const{min:h,max:p}=r.layout.layoutBox[n];c.set(i[n]-Re(h,p,.5))}})}scalePositionWithinConstraints(){if(!this.visualElement.current)return;const{drag:i,dragConstraints:n}=this.getProps(),{projection:s}=this.visualElement;if(!Ei(n)||!s||!this.constraints)return;this.stopAnimation();const r={x:0,y:0};Pt(h=>{const p=this.getAxisMotionValue(h);if(p&&this.constraints!==!1){const m=p.get();r[h]=cD({min:m,max:m},this.constraints[h])}});const{transformTemplate:c}=this.visualElement.getProps();this.visualElement.current.style.transform=c?c({},""):"none",s.root&&s.root.updateScroll(),s.updateLayout(),this.resolveConstraints(),Pt(h=>{if(!Ms(h,i,null))return;const p=this.getAxisMotionValue(h),{min:m,max:f}=this.constraints[h];p.set(Re(m,f,r[h]))})}addListeners(){if(!this.visualElement.current)return;gD.set(this.visualElement,this);const i=this.visualElement.current,n=Gn(i,"pointerdown",m=>{const{drag:f,dragListener:y=!0}=this.getProps();f&&y&&this.start(m)}),s=()=>{const{dragConstraints:m}=this.getProps();Ei(m)&&m.current&&(this.constraints=this.resolveRefConstraints())},{projection:r}=this.visualElement,c=r.addEventListener("measure",s);r&&!r.layout&&(r.root&&r.root.updateScroll(),r.updateLayout()),fe.read(s);const h=ao(window,"resize",()=>this.scalePositionWithinConstraints()),p=r.addEventListener("didUpdate",(({delta:m,hasLayoutChanged:f})=>{this.isDragging&&f&&(Pt(y=>{const v=this.getAxisMotionValue(y);v&&(this.originPoint[y]+=m[y].translate,v.set(v.get()+m[y].translate))}),this.visualElement.render())}));return()=>{h(),n(),c(),p&&p()}}getProps(){const i=this.visualElement.getProps(),{drag:n=!1,dragDirectionLock:s=!1,dragPropagation:r=!1,dragConstraints:c=!1,dragElastic:h=Hc,dragMomentum:p=!0}=i;return{...i,drag:n,dragDirectionLock:s,dragPropagation:r,dragConstraints:c,dragElastic:h,dragMomentum:p}}}function Ms(t,i,n){return(i===!0||i===t)&&(n===null||n===t)}function wD(t,i=10){let n=null;return Math.abs(t.y)>i?n="y":Math.abs(t.x)>i&&(n="x"),n}class vD extends Fa{constructor(i){super(i),this.removeGroupControls=rt,this.removeListeners=rt,this.controls=new yD(i)}mount(){const{dragControls:i}=this.node.getProps();i&&(this.removeGroupControls=i.subscribe(this.controls)),this.removeListeners=this.controls.addListeners()||rt}unmount(){this.removeGroupControls(),this.removeListeners()}}const Wm=t=>(i,n)=>{t&&fe.postRender(()=>t(i,n))};class bD extends Fa{constructor(){super(...arguments),this.removePointerDownListener=rt}onPointerDown(i){this.session=new vy(i,this.createPanHandlers(),{transformPagePoint:this.node.getTransformPagePoint(),contextWindow:Iy(this.node)})}createPanHandlers(){const{onPanSessionStart:i,onPanStart:n,onPan:s,onPanEnd:r}=this.node.getProps();return{onSessionStart:Wm(i),onStart:Wm(n),onMove:s,onEnd:(c,h)=>{delete this.session,r&&fe.postRender(()=>r(c,h))}}}mount(){this.removePointerDownListener=Gn(this.node.current,"pointerdown",i=>this.onPointerDown(i))}update(){this.session&&this.session.updateHandlers(this.createPanHandlers())}unmount(){this.removePointerDownListener(),this.session&&this.session.end()}}const Fs={hasAnimatedSinceResize:!0,hasEverUpdated:!1};function Bm(t,i){return i.max===i.min?0:t/(i.max-i.min)*100}const jn={correct:(t,i)=>{if(!i.target)return t;if(typeof t=="string")if(ae.test(t))t=parseFloat(t);else return t;const n=Bm(t,i.target.x),s=Bm(t,i.target.y);return`${n}% ${s}%`}},kD={correct:(t,{treeScale:i,projectionDelta:n})=>{const s=t,r=Oa.parse(t);if(r.length>5)return s;const c=Oa.createTransformer(t),h=typeof r[0]!="number"?1:0,p=n.x.scale*i.x,m=n.y.scale*i.y;r[0+h]/=p,r[1+h]/=m;const f=Re(p,m,.5);return typeof r[2+h]=="number"&&(r[2+h]/=f),typeof r[3+h]=="number"&&(r[3+h]/=f),c(r)}};class xD extends x.Component{componentDidMount(){const{visualElement:i,layoutGroup:n,switchLayoutGroup:s,layoutId:r}=this.props,{projection:c}=i;KI(SD),c&&(n.group&&n.group.add(c),s&&s.register&&r&&s.register(c),c.root.didUpdate(),c.addEventListener("animationComplete",()=>{this.safeToRemove()}),c.setOptions({...c.options,onExitComplete:()=>this.safeToRemove()})),Fs.hasEverUpdated=!0}getSnapshotBeforeUpdate(i){const{layoutDependency:n,visualElement:s,drag:r,isPresent:c}=this.props,h=s.projection;return h&&(h.isPresent=c,r||i.layoutDependency!==n||n===void 0?h.willUpdate():this.safeToRemove(),i.isPresent!==c&&(c?h.promote():h.relegate()||fe.postRender(()=>{const p=h.getStack();(!p||!p.members.length)&&this.safeToRemove()}))),null}componentDidUpdate(){const{projection:i}=this.props.visualElement;i&&(i.root.didUpdate(),md.postRender(()=>{!i.currentAnimation&&i.isLead()&&this.safeToRemove()}))}componentWillUnmount(){const{visualElement:i,layoutGroup:n,switchLayoutGroup:s}=this.props,{projection:r}=i;r&&(r.scheduleCheckAfterUnmount(),n&&n.group&&n.group.remove(r),s&&s.deregister&&s.deregister(r))}safeToRemove(){const{safeToRemove:i}=this.props;i&&i()}render(){return null}}function My(t){const[i,n]=cg(),s=x.useContext(rd);return g.jsx(xD,{...t,layoutGroup:s,switchLayoutGroup:x.useContext(gg),isPresent:i,safeToRemove:n})}const SD={borderRadius:{...jn,applyTo:["borderTopLeftRadius","borderTopRightRadius","borderBottomLeftRadius","borderBottomRightRadius"]},borderTopLeftRadius:jn,borderTopRightRadius:jn,borderBottomLeftRadius:jn,borderBottomRightRadius:jn,boxShadow:kD};function AD(t,i,n){const s=Qe(t)?t:Jt(t);return s.start(Nd("",s,i,n)),s.animation}function CD(t){return t instanceof SVGElement&&t.tagName!=="svg"}const TD=(t,i)=>t.depth-i.depth;class ID{constructor(){this.children=[],this.isDirty=!1}add(i){Pd(this.children,i),this.isDirty=!0}remove(i){Dd(this.children,i),this.isDirty=!0}forEach(i){this.isDirty&&this.children.sort(TD),this.isDirty=!1,this.children.forEach(i)}}function MD(t,i){const n=Qt.now(),s=({timestamp:r})=>{const c=r-n;c>=i&&($t(s),t(c-i))};return fe.read(s,!0),()=>$t(s)}const Py=["TopLeft","TopRight","BottomLeft","BottomRight"],PD=Py.length,zm=t=>typeof t=="string"?parseFloat(t):t,Hm=t=>typeof t=="number"||ae.test(t);function DD(t,i,n,s,r,c){r?(t.opacity=Re(0,n.opacity!==void 0?n.opacity:1,RD(s)),t.opacityExit=Re(i.opacity!==void 0?i.opacity:1,0,ED(s))):c&&(t.opacity=Re(i.opacity!==void 0?i.opacity:1,n.opacity!==void 0?n.opacity:1,s));for(let h=0;h<PD;h++){const p=`border${Py[h]}Radius`;let m=Km(i,p),f=Km(n,p);if(m===void 0&&f===void 0)continue;m||(m=0),f||(f=0),m===0||f===0||Hm(m)===Hm(f)?(t[p]=Math.max(Re(zm(m),zm(f),s),0),(Yt.test(f)||Yt.test(m))&&(t[p]+="%")):t[p]=f}(i.rotate||n.rotate)&&(t.rotate=Re(i.rotate||0,n.rotate||0,s))}function Km(t,i){return t[i]!==void 0?t[i]:t.borderRadius}const RD=Dy(0,.5,_g),ED=Dy(.5,.95,rt);function Dy(t,i,n){return s=>s<t?0:s>i?1:n(ii(t,i,s))}function _m(t,i){t.min=i.min,t.max=i.max}function Mt(t,i){_m(t.x,i.x),_m(t.y,i.y)}function Gm(t,i){t.translate=i.translate,t.scale=i.scale,t.originPoint=i.originPoint,t.origin=i.origin}function Jm(t,i,n,s,r){return t-=i,t=Gs(t,1/n,s),r!==void 0&&(t=Gs(t,1/r,s)),t}function UD(t,i=0,n=1,s=.5,r,c=t,h=t){if(Yt.test(i)&&(i=parseFloat(i),i=Re(h.min,h.max,i/100)-h.min),typeof i!="number")return;let p=Re(c.min,c.max,s);t===c&&(p-=i),t.min=Jm(t.min,i,n,p,r),t.max=Jm(t.max,i,n,p,r)}function Ym(t,i,[n,s,r],c,h){UD(t,i[n],i[s],i[r],i.scale,c,h)}const qD=["x","scaleX","originX"],OD=["y","scaleY","originY"];function Qm(t,i,n,s){Ym(t.x,i,qD,n?n.x:void 0,s?s.x:void 0),Ym(t.y,i,OD,n?n.y:void 0,s?s.y:void 0)}function $m(t){return t.translate===0&&t.scale===1}function Ry(t){return $m(t.x)&&$m(t.y)}function Xm(t,i){return t.min===i.min&&t.max===i.max}function FD(t,i){return Xm(t.x,i.x)&&Xm(t.y,i.y)}function Zm(t,i){return Math.round(t.min)===Math.round(i.min)&&Math.round(t.max)===Math.round(i.max)}function Ey(t,i){return Zm(t.x,i.x)&&Zm(t.y,i.y)}function ef(t){return bt(t.x)/bt(t.y)}function tf(t,i){return t.translate===i.translate&&t.scale===i.scale&&t.originPoint===i.originPoint}class LD{constructor(){this.members=[]}add(i){Pd(this.members,i),i.scheduleRender()}remove(i){if(Dd(this.members,i),i===this.prevLead&&(this.prevLead=void 0),i===this.lead){const n=this.members[this.members.length-1];n&&this.promote(n)}}relegate(i){const n=this.members.findIndex(r=>i===r);if(n===0)return!1;let s;for(let r=n;r>=0;r--){const c=this.members[r];if(c.isPresent!==!1){s=c;break}}return s?(this.promote(s),!0):!1}promote(i,n){const s=this.lead;if(i!==s&&(this.prevLead=s,this.lead=i,i.show(),s)){s.instance&&s.scheduleRender(),i.scheduleRender(),i.resumeFrom=s,n&&(i.resumeFrom.preserveOpacity=!0),s.snapshot&&(i.snapshot=s.snapshot,i.snapshot.latestValues=s.animationValues||s.latestValues),i.root&&i.root.isUpdating&&(i.isLayoutDirty=!0);const{crossfade:r}=i.options;r===!1&&s.hide()}}exitAnimationComplete(){this.members.forEach(i=>{const{options:n,resumingFrom:s}=i;n.onExitComplete&&n.onExitComplete(),s&&s.options.onExitComplete&&s.options.onExitComplete()})}scheduleRender(){this.members.forEach(i=>{i.instance&&i.scheduleRender(!1)})}removeLeadSnapshot(){this.lead&&this.lead.snapshot&&(this.lead.snapshot=void 0)}}function jD(t,i,n){let s="";const r=t.x.translate/i.x,c=t.y.translate/i.y,h=(n==null?void 0:n.z)||0;if((r||c||h)&&(s=`translate3d(${r}px, ${c}px, ${h}px) `),(i.x!==1||i.y!==1)&&(s+=`scale(${1/i.x}, ${1/i.y}) `),n){const{transformPerspective:f,rotate:y,rotateX:v,rotateY:b,skewX:A,skewY:T}=n;f&&(s=`perspective(${f}px) ${s}`),y&&(s+=`rotate(${y}deg) `),v&&(s+=`rotateX(${v}deg) `),b&&(s+=`rotateY(${b}deg) `),A&&(s+=`skewX(${A}deg) `),T&&(s+=`skewY(${T}deg) `)}const p=t.x.scale*i.x,m=t.y.scale*i.y;return(p!==1||m!==1)&&(s+=`scale(${p}, ${m})`),s||"none"}const ei={type:"projectionFrame",totalNodes:0,resolvedTargetDeltas:0,recalculatedProjection:0},Kn=typeof window<"u"&&window.MotionDebug!==void 0,kc=["","X","Y","Z"],VD={visibility:"hidden"},af=1e3;let ND=0;function xc(t,i,n,s){const{latestValues:r}=i;r[t]&&(n[t]=r[t],i.setStaticValue(t,0),s&&(s[t]=0))}function Uy(t){if(t.hasCheckedOptimisedAppear=!0,t.root===t)return;const{visualElement:i}=t.options;if(!i)return;const n=Vg(i);if(window.MotionHasOptimisedAnimation(n,"transform")){const{layout:r,layoutId:c}=t.options;window.MotionCancelOptimisedAnimation(n,"transform",fe,!(r||c))}const{parent:s}=t;s&&!s.hasCheckedOptimisedAppear&&Uy(s)}function qy({attachResizeListener:t,defaultParent:i,measureScroll:n,checkIsScrollRoot:s,resetTransform:r}){return class{constructor(h={},p=i==null?void 0:i()){this.id=ND++,this.animationId=0,this.children=new Set,this.options={},this.isTreeAnimating=!1,this.isAnimationBlocked=!1,this.isLayoutDirty=!1,this.isProjectionDirty=!1,this.isSharedProjectionDirty=!1,this.isTransformDirty=!1,this.updateManuallyBlocked=!1,this.updateBlockedByResize=!1,this.isUpdating=!1,this.isSVG=!1,this.needsReset=!1,this.shouldResetTransform=!1,this.hasCheckedOptimisedAppear=!1,this.treeScale={x:1,y:1},this.eventHandlers=new Map,this.hasTreeAnimated=!1,this.updateScheduled=!1,this.scheduleUpdate=()=>this.update(),this.projectionUpdateScheduled=!1,this.checkUpdateFailed=()=>{this.isUpdating&&(this.isUpdating=!1,this.clearAllSnapshots())},this.updateProjection=()=>{this.projectionUpdateScheduled=!1,Kn&&(ei.totalNodes=ei.resolvedTargetDeltas=ei.recalculatedProjection=0),this.nodes.forEach(zD),this.nodes.forEach(JD),this.nodes.forEach(YD),this.nodes.forEach(HD),Kn&&window.MotionDebug.record(ei)},this.resolvedRelativeTargetAt=0,this.hasProjected=!1,this.isVisible=!0,this.animationProgress=0,this.sharedNodes=new Map,this.latestValues=h,this.root=p?p.root||p:this,this.path=p?[...p.path,p]:[],this.parent=p,this.depth=p?p.depth+1:0;for(let m=0;m<this.path.length;m++)this.path[m].shouldResetTransform=!0;this.root===this&&(this.nodes=new ID)}addEventListener(h,p){return this.eventHandlers.has(h)||this.eventHandlers.set(h,new Rd),this.eventHandlers.get(h).add(p)}notifyListeners(h,...p){const m=this.eventHandlers.get(h);m&&m.notify(...p)}hasListeners(h){return this.eventHandlers.has(h)}mount(h,p=this.root.hasTreeAnimated){if(this.instance)return;this.isSVG=CD(h),this.instance=h;const{layoutId:m,layout:f,visualElement:y}=this.options;if(y&&!y.current&&y.mount(h),this.root.nodes.add(this),this.parent&&this.parent.children.add(this),p&&(f||m)&&(this.isLayoutDirty=!0),t){let v;const b=()=>this.root.updateBlockedByResize=!1;t(h,()=>{this.root.updateBlockedByResize=!0,v&&v(),v=MD(b,250),Fs.hasAnimatedSinceResize&&(Fs.hasAnimatedSinceResize=!1,this.nodes.forEach(of))})}m&&this.root.registerSharedNode(m,this),this.options.animate!==!1&&y&&(m||f)&&this.addEventListener("didUpdate",({delta:v,hasLayoutChanged:b,hasRelativeTargetChanged:A,layout:T})=>{if(this.isTreeAnimationBlocked()){this.target=void 0,this.relativeTarget=void 0;return}const I=this.options.transition||y.getDefaultTransition()||eR,{onLayoutAnimationStart:M,onLayoutAnimationComplete:R}=y.getProps(),O=!this.targetLayout||!Ey(this.targetLayout,T)||A,q=!b&&A;if(this.options.layoutRoot||this.resumeFrom&&this.resumeFrom.instance||q||b&&(O||!this.currentAnimation)){this.resumeFrom&&(this.resumingFrom=this.resumeFrom,this.resumingFrom.resumingFrom=void 0),this.setAnimationOrigin(v,q);const z={...Ad(I,"layout"),onPlay:M,onComplete:R};(y.shouldReduceMotion||this.options.layoutRoot)&&(z.delay=0,z.type=!1),this.startAnimation(z)}else b||of(this),this.isLead()&&this.options.onExitComplete&&this.options.onExitComplete();this.targetLayout=T})}unmount(){this.options.layoutId&&this.willUpdate(),this.root.nodes.remove(this);const h=this.getStack();h&&h.remove(this),this.parent&&this.parent.children.delete(this),this.instance=void 0,$t(this.updateProjection)}blockUpdate(){this.updateManuallyBlocked=!0}unblockUpdate(){this.updateManuallyBlocked=!1}isUpdateBlocked(){return this.updateManuallyBlocked||this.updateBlockedByResize}isTreeAnimationBlocked(){return this.isAnimationBlocked||this.parent&&this.parent.isTreeAnimationBlocked()||!1}startUpdate(){this.isUpdateBlocked()||(this.isUpdating=!0,this.nodes&&this.nodes.forEach(QD),this.animationId++)}getTransformTemplate(){const{visualElement:h}=this.options;return h&&h.getProps().transformTemplate}willUpdate(h=!0){if(this.root.hasTreeAnimated=!0,this.root.isUpdateBlocked()){this.options.onExitComplete&&this.options.onExitComplete();return}if(window.MotionCancelOptimisedAnimation&&!this.hasCheckedOptimisedAppear&&Uy(this),!this.root.isUpdating&&this.root.startUpdate(),this.isLayoutDirty)return;this.isLayoutDirty=!0;for(let y=0;y<this.path.length;y++){const v=this.path[y];v.shouldResetTransform=!0,v.updateScroll("snapshot"),v.options.layoutRoot&&v.willUpdate(!1)}const{layoutId:p,layout:m}=this.options;if(p===void 0&&!m)return;const f=this.getTransformTemplate();this.prevTransformTemplateValue=f?f(this.latestValues,""):void 0,this.updateSnapshot(),h&&this.notifyListeners("willUpdate")}update(){if(this.updateScheduled=!1,this.isUpdateBlocked()){this.unblockUpdate(),this.clearAllSnapshots(),this.nodes.forEach(nf);return}this.isUpdating||this.nodes.forEach(_D),this.isUpdating=!1,this.nodes.forEach(GD),this.nodes.forEach(WD),this.nodes.forEach(BD),this.clearAllSnapshots();const p=Qt.now();ze.delta=Xt(0,1e3/60,p-ze.timestamp),ze.timestamp=p,ze.isProcessing=!0,uc.update.process(ze),uc.preRender.process(ze),uc.render.process(ze),ze.isProcessing=!1}didUpdate(){this.updateScheduled||(this.updateScheduled=!0,md.read(this.scheduleUpdate))}clearAllSnapshots(){this.nodes.forEach(KD),this.sharedNodes.forEach($D)}scheduleUpdateProjection(){this.projectionUpdateScheduled||(this.projectionUpdateScheduled=!0,fe.preRender(this.updateProjection,!1,!0))}scheduleCheckAfterUnmount(){fe.postRender(()=>{this.isLayoutDirty?this.root.didUpdate():this.root.checkUpdateFailed()})}updateSnapshot(){this.snapshot||!this.instance||(this.snapshot=this.measure())}updateLayout(){if(!this.instance||(this.updateScroll(),!(this.options.alwaysMeasureLayout&&this.isLead())&&!this.isLayoutDirty))return;if(this.resumeFrom&&!this.resumeFrom.instance)for(let m=0;m<this.path.length;m++)this.path[m].updateScroll();const h=this.layout;this.layout=this.measure(!1),this.layoutCorrected=Le(),this.isLayoutDirty=!1,this.projectionDelta=void 0,this.notifyListeners("measure",this.layout.layoutBox);const{visualElement:p}=this.options;p&&p.notify("LayoutMeasure",this.layout.layoutBox,h?h.layoutBox:void 0)}updateScroll(h="measure"){let p=!!(this.options.layoutScroll&&this.instance);if(this.scroll&&this.scroll.animationId===this.root.animationId&&this.scroll.phase===h&&(p=!1),p){const m=s(this.instance);this.scroll={animationId:this.root.animationId,phase:h,isRoot:m,offset:n(this.instance),wasRoot:this.scroll?this.scroll.isRoot:m}}}resetTransform(){if(!r)return;const h=this.isLayoutDirty||this.shouldResetTransform||this.options.alwaysMeasureLayout,p=this.projectionDelta&&!Ry(this.projectionDelta),m=this.getTransformTemplate(),f=m?m(this.latestValues,""):void 0,y=f!==this.prevTransformTemplateValue;h&&(p||Za(this.latestValues)||y)&&(r(this.instance,f),this.shouldResetTransform=!1,this.scheduleRender())}measure(h=!0){const p=this.measurePageBox();let m=this.removeElementScroll(p);return h&&(m=this.removeTransform(m)),tR(m),{animationId:this.root.animationId,measuredBox:p,layoutBox:m,latestValues:{},source:this.id}}measurePageBox(){var h;const{visualElement:p}=this.options;if(!p)return Le();const m=p.measureViewportBox();if(!(((h=this.scroll)===null||h===void 0?void 0:h.wasRoot)||this.path.some(aR))){const{scroll:y}=this.root;y&&(Oi(m.x,y.offset.x),Oi(m.y,y.offset.y))}return m}removeElementScroll(h){var p;const m=Le();if(Mt(m,h),!((p=this.scroll)===null||p===void 0)&&p.wasRoot)return m;for(let f=0;f<this.path.length;f++){const y=this.path[f],{scroll:v,options:b}=y;y!==this.root&&v&&b.layoutScroll&&(v.wasRoot&&Mt(m,h),Oi(m.x,v.offset.x),Oi(m.y,v.offset.y))}return m}applyTransform(h,p=!1){const m=Le();Mt(m,h);for(let f=0;f<this.path.length;f++){const y=this.path[f];!p&&y.options.layoutScroll&&y.scroll&&y!==y.root&&Fi(m,{x:-y.scroll.offset.x,y:-y.scroll.offset.y}),Za(y.latestValues)&&Fi(m,y.latestValues)}return Za(this.latestValues)&&Fi(m,this.latestValues),m}removeTransform(h){const p=Le();Mt(p,h);for(let m=0;m<this.path.length;m++){const f=this.path[m];if(!f.instance||!Za(f.latestValues))continue;Kc(f.latestValues)&&f.updateSnapshot();const y=Le(),v=f.measurePageBox();Mt(y,v),Qm(p,f.latestValues,f.snapshot?f.snapshot.layoutBox:void 0,y)}return Za(this.latestValues)&&Qm(p,this.latestValues),p}setTargetDelta(h){this.targetDelta=h,this.root.scheduleUpdateProjection(),this.isProjectionDirty=!0}setOptions(h){this.options={...this.options,...h,crossfade:h.crossfade!==void 0?h.crossfade:!0}}clearMeasurements(){this.scroll=void 0,this.layout=void 0,this.snapshot=void 0,this.prevTransformTemplateValue=void 0,this.targetDelta=void 0,this.target=void 0,this.isLayoutDirty=!1}forceRelativeParentToResolveTarget(){this.relativeParent&&this.relativeParent.resolvedRelativeTargetAt!==ze.timestamp&&this.relativeParent.resolveTargetDelta(!0)}resolveTargetDelta(h=!1){var p;const m=this.getLead();this.isProjectionDirty||(this.isProjectionDirty=m.isProjectionDirty),this.isTransformDirty||(this.isTransformDirty=m.isTransformDirty),this.isSharedProjectionDirty||(this.isSharedProjectionDirty=m.isSharedProjectionDirty);const f=!!this.resumingFrom||this!==m;if(!(h||f&&this.isSharedProjectionDirty||this.isProjectionDirty||!((p=this.parent)===null||p===void 0)&&p.isProjectionDirty||this.attemptToResolveRelativeTarget||this.root.updateBlockedByResize))return;const{layout:v,layoutId:b}=this.options;if(!(!this.layout||!(v||b))){if(this.resolvedRelativeTargetAt=ze.timestamp,!this.targetDelta&&!this.relativeTarget){const A=this.getClosestProjectingParent();A&&A.layout&&this.animationProgress!==1?(this.relativeParent=A,this.forceRelativeParentToResolveTarget(),this.relativeTarget=Le(),this.relativeTargetOrigin=Le(),Yn(this.relativeTargetOrigin,this.layout.layoutBox,A.layout.layoutBox),Mt(this.relativeTarget,this.relativeTargetOrigin)):this.relativeParent=this.relativeTarget=void 0}if(!(!this.relativeTarget&&!this.targetDelta)){if(this.target||(this.target=Le(),this.targetWithTransforms=Le()),this.relativeTarget&&this.relativeTargetOrigin&&this.relativeParent&&this.relativeParent.target?(this.forceRelativeParentToResolveTarget(),oD(this.target,this.relativeTarget,this.relativeParent.target)):this.targetDelta?(this.resumingFrom?this.target=this.applyTransform(this.layout.layoutBox):Mt(this.target,this.layout.layoutBox),Cy(this.target,this.targetDelta)):Mt(this.target,this.layout.layoutBox),this.attemptToResolveRelativeTarget){this.attemptToResolveRelativeTarget=!1;const A=this.getClosestProjectingParent();A&&!!A.resumingFrom==!!this.resumingFrom&&!A.options.layoutScroll&&A.target&&this.animationProgress!==1?(this.relativeParent=A,this.forceRelativeParentToResolveTarget(),this.relativeTarget=Le(),this.relativeTargetOrigin=Le(),Yn(this.relativeTargetOrigin,this.target,A.target),Mt(this.relativeTarget,this.relativeTargetOrigin)):this.relativeParent=this.relativeTarget=void 0}Kn&&ei.resolvedTargetDeltas++}}}getClosestProjectingParent(){if(!(!this.parent||Kc(this.parent.latestValues)||Ay(this.parent.latestValues)))return this.parent.isProjecting()?this.parent:this.parent.getClosestProjectingParent()}isProjecting(){return!!((this.relativeTarget||this.targetDelta||this.options.layoutRoot)&&this.layout)}calcProjection(){var h;const p=this.getLead(),m=!!this.resumingFrom||this!==p;let f=!0;if((this.isProjectionDirty||!((h=this.parent)===null||h===void 0)&&h.isProjectionDirty)&&(f=!1),m&&(this.isSharedProjectionDirty||this.isTransformDirty)&&(f=!1),this.resolvedRelativeTargetAt===ze.timestamp&&(f=!1),f)return;const{layout:y,layoutId:v}=this.options;if(this.isTreeAnimating=!!(this.parent&&this.parent.isTreeAnimating||this.currentAnimation||this.pendingAnimation),this.isTreeAnimating||(this.targetDelta=this.relativeTarget=void 0),!this.layout||!(y||v))return;Mt(this.layoutCorrected,this.layout.layoutBox);const b=this.treeScale.x,A=this.treeScale.y;mD(this.layoutCorrected,this.treeScale,this.path,m),p.layout&&!p.target&&(this.treeScale.x!==1||this.treeScale.y!==1)&&(p.target=p.layout.layoutBox,p.targetWithTransforms=Le());const{target:T}=p;if(!T){this.prevProjectionDelta&&(this.createProjectionDeltas(),this.scheduleRender());return}!this.projectionDelta||!this.prevProjectionDelta?this.createProjectionDeltas():(Gm(this.prevProjectionDelta.x,this.projectionDelta.x),Gm(this.prevProjectionDelta.y,this.projectionDelta.y)),Jn(this.projectionDelta,this.layoutCorrected,T,this.latestValues),(this.treeScale.x!==b||this.treeScale.y!==A||!tf(this.projectionDelta.x,this.prevProjectionDelta.x)||!tf(this.projectionDelta.y,this.prevProjectionDelta.y))&&(this.hasProjected=!0,this.scheduleRender(),this.notifyListeners("projectionUpdate",T)),Kn&&ei.recalculatedProjection++}hide(){this.isVisible=!1}show(){this.isVisible=!0}scheduleRender(h=!0){var p;if((p=this.options.visualElement)===null||p===void 0||p.scheduleRender(),h){const m=this.getStack();m&&m.scheduleRender()}this.resumingFrom&&!this.resumingFrom.instance&&(this.resumingFrom=void 0)}createProjectionDeltas(){this.prevProjectionDelta=qi(),this.projectionDelta=qi(),this.projectionDeltaWithTransform=qi()}setAnimationOrigin(h,p=!1){const m=this.snapshot,f=m?m.latestValues:{},y={...this.latestValues},v=qi();(!this.relativeParent||!this.relativeParent.options.layoutRoot)&&(this.relativeTarget=this.relativeTargetOrigin=void 0),this.attemptToResolveRelativeTarget=!p;const b=Le(),A=m?m.source:void 0,T=this.layout?this.layout.source:void 0,I=A!==T,M=this.getStack(),R=!M||M.members.length<=1,O=!!(I&&!R&&this.options.crossfade===!0&&!this.path.some(ZD));this.animationProgress=0;let q;this.mixTargetDelta=z=>{const V=z/1e3;sf(v.x,h.x,V),sf(v.y,h.y,V),this.setTargetDelta(v),this.relativeTarget&&this.relativeTargetOrigin&&this.layout&&this.relativeParent&&this.relativeParent.layout&&(Yn(b,this.layout.layoutBox,this.relativeParent.layout.layoutBox),XD(this.relativeTarget,this.relativeTargetOrigin,b,V),q&&FD(this.relativeTarget,q)&&(this.isProjectionDirty=!1),q||(q=Le()),Mt(q,this.relativeTarget)),I&&(this.animationValues=y,DD(y,f,this.latestValues,V,O,R)),this.root.scheduleUpdateProjection(),this.scheduleRender(),this.animationProgress=V},this.mixTargetDelta(this.options.layoutRoot?1e3:0)}startAnimation(h){this.notifyListeners("animationStart"),this.currentAnimation&&this.currentAnimation.stop(),this.resumingFrom&&this.resumingFrom.currentAnimation&&this.resumingFrom.currentAnimation.stop(),this.pendingAnimation&&($t(this.pendingAnimation),this.pendingAnimation=void 0),this.pendingAnimation=fe.update(()=>{Fs.hasAnimatedSinceResize=!0,this.currentAnimation=AD(0,af,{...h,onUpdate:p=>{this.mixTargetDelta(p),h.onUpdate&&h.onUpdate(p)},onComplete:()=>{h.onComplete&&h.onComplete(),this.completeAnimation()}}),this.resumingFrom&&(this.resumingFrom.currentAnimation=this.currentAnimation),this.pendingAnimation=void 0})}completeAnimation(){this.resumingFrom&&(this.resumingFrom.currentAnimation=void 0,this.resumingFrom.preserveOpacity=void 0);const h=this.getStack();h&&h.exitAnimationComplete(),this.resumingFrom=this.currentAnimation=this.animationValues=void 0,this.notifyListeners("animationComplete")}finishAnimation(){this.currentAnimation&&(this.mixTargetDelta&&this.mixTargetDelta(af),this.currentAnimation.stop()),this.completeAnimation()}applyTransformsToTarget(){const h=this.getLead();let{targetWithTransforms:p,target:m,layout:f,latestValues:y}=h;if(!(!p||!m||!f)){if(this!==h&&this.layout&&f&&Oy(this.options.animationType,this.layout.layoutBox,f.layoutBox)){m=this.target||Le();const v=bt(this.layout.layoutBox.x);m.x.min=h.target.x.min,m.x.max=m.x.min+v;const b=bt(this.layout.layoutBox.y);m.y.min=h.target.y.min,m.y.max=m.y.min+b}Mt(p,m),Fi(p,y),Jn(this.projectionDeltaWithTransform,this.layoutCorrected,p,y)}}registerSharedNode(h,p){this.sharedNodes.has(h)||this.sharedNodes.set(h,new LD),this.sharedNodes.get(h).add(p);const f=p.options.initialPromotionConfig;p.promote({transition:f?f.transition:void 0,preserveFollowOpacity:f&&f.shouldPreserveFollowOpacity?f.shouldPreserveFollowOpacity(p):void 0})}isLead(){const h=this.getStack();return h?h.lead===this:!0}getLead(){var h;const{layoutId:p}=this.options;return p?((h=this.getStack())===null||h===void 0?void 0:h.lead)||this:this}getPrevLead(){var h;const{layoutId:p}=this.options;return p?(h=this.getStack())===null||h===void 0?void 0:h.prevLead:void 0}getStack(){const{layoutId:h}=this.options;if(h)return this.root.sharedNodes.get(h)}promote({needsReset:h,transition:p,preserveFollowOpacity:m}={}){const f=this.getStack();f&&f.promote(this,m),h&&(this.projectionDelta=void 0,this.needsReset=!0),p&&this.setOptions({transition:p})}relegate(){const h=this.getStack();return h?h.relegate(this):!1}resetSkewAndRotation(){const{visualElement:h}=this.options;if(!h)return;let p=!1;const{latestValues:m}=h;if((m.z||m.rotate||m.rotateX||m.rotateY||m.rotateZ||m.skewX||m.skewY)&&(p=!0),!p)return;const f={};m.z&&xc("z",h,f,this.animationValues);for(let y=0;y<kc.length;y++)xc(`rotate${kc[y]}`,h,f,this.animationValues),xc(`skew${kc[y]}`,h,f,this.animationValues);h.render();for(const y in f)h.setStaticValue(y,f[y]),this.animationValues&&(this.animationValues[y]=f[y]);h.scheduleRender()}getProjectionStyles(h){var p,m;if(!this.instance||this.isSVG)return;if(!this.isVisible)return VD;const f={visibility:""},y=this.getTransformTemplate();if(this.needsReset)return this.needsReset=!1,f.opacity="",f.pointerEvents=qs(h==null?void 0:h.pointerEvents)||"",f.transform=y?y(this.latestValues,""):"none",f;const v=this.getLead();if(!this.projectionDelta||!this.layout||!v.target){const I={};return this.options.layoutId&&(I.opacity=this.latestValues.opacity!==void 0?this.latestValues.opacity:1,I.pointerEvents=qs(h==null?void 0:h.pointerEvents)||""),this.hasProjected&&!Za(this.latestValues)&&(I.transform=y?y({},""):"none",this.hasProjected=!1),I}const b=v.animationValues||v.latestValues;this.applyTransformsToTarget(),f.transform=jD(this.projectionDeltaWithTransform,this.treeScale,b),y&&(f.transform=y(b,f.transform));const{x:A,y:T}=this.projectionDelta;f.transformOrigin=`${A.origin*100}% ${T.origin*100}% 0`,v.animationValues?f.opacity=v===this?(m=(p=b.opacity)!==null&&p!==void 0?p:this.latestValues.opacity)!==null&&m!==void 0?m:1:this.preserveOpacity?this.latestValues.opacity:b.opacityExit:f.opacity=v===this?b.opacity!==void 0?b.opacity:"":b.opacityExit!==void 0?b.opacityExit:0;for(const I in Bs){if(b[I]===void 0)continue;const{correct:M,applyTo:R}=Bs[I],O=f.transform==="none"?b[I]:M(b[I],v);if(R){const q=R.length;for(let z=0;z<q;z++)f[R[z]]=O}else f[I]=O}return this.options.layoutId&&(f.pointerEvents=v===this?qs(h==null?void 0:h.pointerEvents)||"":"none"),f}clearSnapshot(){this.resumeFrom=this.snapshot=void 0}resetTree(){this.root.nodes.forEach(h=>{var p;return(p=h.currentAnimation)===null||p===void 0?void 0:p.stop()}),this.root.nodes.forEach(nf),this.root.sharedNodes.clear()}}}function WD(t){t.updateLayout()}function BD(t){var i;const n=((i=t.resumeFrom)===null||i===void 0?void 0:i.snapshot)||t.snapshot;if(t.isLead()&&t.layout&&n&&t.hasListeners("didUpdate")){const{layoutBox:s,measuredBox:r}=t.layout,{animationType:c}=t.options,h=n.source!==t.layout.source;c==="size"?Pt(v=>{const b=h?n.measuredBox[v]:n.layoutBox[v],A=bt(b);b.min=s[v].min,b.max=b.min+A}):Oy(c,n.layoutBox,s)&&Pt(v=>{const b=h?n.measuredBox[v]:n.layoutBox[v],A=bt(s[v]);b.max=b.min+A,t.relativeTarget&&!t.currentAnimation&&(t.isProjectionDirty=!0,t.relativeTarget[v].max=t.relativeTarget[v].min+A)});const p=qi();Jn(p,s,n.layoutBox);const m=qi();h?Jn(m,t.applyTransform(r,!0),n.measuredBox):Jn(m,s,n.layoutBox);const f=!Ry(p);let y=!1;if(!t.resumeFrom){const v=t.getClosestProjectingParent();if(v&&!v.resumeFrom){const{snapshot:b,layout:A}=v;if(b&&A){const T=Le();Yn(T,n.layoutBox,b.layoutBox);const I=Le();Yn(I,s,A.layoutBox),Ey(T,I)||(y=!0),v.options.layoutRoot&&(t.relativeTarget=I,t.relativeTargetOrigin=T,t.relativeParent=v)}}}t.notifyListeners("didUpdate",{layout:s,snapshot:n,delta:m,layoutDelta:p,hasLayoutChanged:f,hasRelativeTargetChanged:y})}else if(t.isLead()){const{onExitComplete:s}=t.options;s&&s()}t.options.transition=void 0}function zD(t){Kn&&ei.totalNodes++,t.parent&&(t.isProjecting()||(t.isProjectionDirty=t.parent.isProjectionDirty),t.isSharedProjectionDirty||(t.isSharedProjectionDirty=!!(t.isProjectionDirty||t.parent.isProjectionDirty||t.parent.isSharedProjectionDirty)),t.isTransformDirty||(t.isTransformDirty=t.parent.isTransformDirty))}function HD(t){t.isProjectionDirty=t.isSharedProjectionDirty=t.isTransformDirty=!1}function KD(t){t.clearSnapshot()}function nf(t){t.clearMeasurements()}function _D(t){t.isLayoutDirty=!1}function GD(t){const{visualElement:i}=t.options;i&&i.getProps().onBeforeLayoutMeasure&&i.notify("BeforeLayoutMeasure"),t.resetTransform()}function of(t){t.finishAnimation(),t.targetDelta=t.relativeTarget=t.target=void 0,t.isProjectionDirty=!0}function JD(t){t.resolveTargetDelta()}function YD(t){t.calcProjection()}function QD(t){t.resetSkewAndRotation()}function $D(t){t.removeLeadSnapshot()}function sf(t,i,n){t.translate=Re(i.translate,0,n),t.scale=Re(i.scale,1,n),t.origin=i.origin,t.originPoint=i.originPoint}function rf(t,i,n,s){t.min=Re(i.min,n.min,s),t.max=Re(i.max,n.max,s)}function XD(t,i,n,s){rf(t.x,i.x,n.x,s),rf(t.y,i.y,n.y,s)}function ZD(t){return t.animationValues&&t.animationValues.opacityExit!==void 0}const eR={duration:.45,ease:[.4,0,.1,1]},lf=t=>typeof navigator<"u"&&navigator.userAgent&&navigator.userAgent.toLowerCase().includes(t),cf=lf("applewebkit/")&&!lf("chrome/")?Math.round:rt;function df(t){t.min=cf(t.min),t.max=cf(t.max)}function tR(t){df(t.x),df(t.y)}function Oy(t,i,n){return t==="position"||t==="preserve-aspect"&&!nD(ef(i),ef(n),.2)}function aR(t){var i;return t!==t.root&&((i=t.scroll)===null||i===void 0?void 0:i.wasRoot)}const iR=qy({attachResizeListener:(t,i)=>ao(t,"resize",i),measureScroll:()=>({x:document.documentElement.scrollLeft||document.body.scrollLeft,y:document.documentElement.scrollTop||document.body.scrollTop}),checkIsScrollRoot:()=>!0}),Sc={current:void 0},Fy=qy({measureScroll:t=>({x:t.scrollLeft,y:t.scrollTop}),defaultParent:()=>{if(!Sc.current){const t=new iR({});t.mount(window),t.setOptions({layoutScroll:!0}),Sc.current=t}return Sc.current},resetTransform:(t,i)=>{t.style.transform=i!==void 0?i:"none"},checkIsScrollRoot:t=>window.getComputedStyle(t).position==="fixed"}),nR={pan:{Feature:bD},drag:{Feature:vD,ProjectionNode:Fy,MeasureLayout:My}};function hf(t,i,n){const{props:s}=t;t.animationState&&s.whileHover&&t.animationState.setActive("whileHover",n==="Start");const r="onHover"+n,c=s[r];c&&fe.postRender(()=>c(i,po(i)))}class oR extends Fa{mount(){const{current:i}=this.node;i&&(this.unmount=oM(i,n=>(hf(this.node,n,"Start"),s=>hf(this.node,s,"End"))))}unmount(){}}class sR extends Fa{constructor(){super(...arguments),this.isActive=!1}onFocus(){let i=!1;try{i=this.node.current.matches(":focus-visible")}catch{i=!0}!i||!this.node.animationState||(this.node.animationState.setActive("whileFocus",!0),this.isActive=!0)}onBlur(){!this.isActive||!this.node.animationState||(this.node.animationState.setActive("whileFocus",!1),this.isActive=!1)}mount(){this.unmount=uo(ao(this.node.current,"focus",()=>this.onFocus()),ao(this.node.current,"blur",()=>this.onBlur()))}unmount(){}}function uf(t,i,n){const{props:s}=t;t.animationState&&s.whileTap&&t.animationState.setActive("whileTap",n==="Start");const r="onTap"+(n==="End"?"":n),c=s[r];c&&fe.postRender(()=>c(i,po(i)))}class rR extends Fa{mount(){const{current:i}=this.node;i&&(this.unmount=cM(i,n=>(uf(this.node,n,"Start"),(s,{success:r})=>uf(this.node,s,r?"End":"Cancel")),{useGlobalTarget:this.node.props.globalTapTarget}))}unmount(){}}const Gc=new WeakMap,Ac=new WeakMap,lR=t=>{const i=Gc.get(t.target);i&&i(t)},cR=t=>{t.forEach(lR)};function dR({root:t,...i}){const n=t||document;Ac.has(n)||Ac.set(n,{});const s=Ac.get(n),r=JSON.stringify(i);return s[r]||(s[r]=new IntersectionObserver(cR,{root:t,...i})),s[r]}function hR(t,i,n){const s=dR(i);return Gc.set(t,n),s.observe(t),()=>{Gc.delete(t),s.unobserve(t)}}const uR={some:0,all:1};class pR extends Fa{constructor(){super(...arguments),this.hasEnteredView=!1,this.isInView=!1}startObserver(){this.unmount();const{viewport:i={}}=this.node.getProps(),{root:n,margin:s,amount:r="some",once:c}=i,h={root:n?n.current:void 0,rootMargin:s,threshold:typeof r=="number"?r:uR[r]},p=m=>{const{isIntersecting:f}=m;if(this.isInView===f||(this.isInView=f,c&&!f&&this.hasEnteredView))return;f&&(this.hasEnteredView=!0),this.node.animationState&&this.node.animationState.setActive("whileInView",f);const{onViewportEnter:y,onViewportLeave:v}=this.node.getProps(),b=f?y:v;b&&b(m)};return hR(this.node.current,h,p)}mount(){this.startObserver()}update(){if(typeof IntersectionObserver>"u")return;const{props:i,prevProps:n}=this.node;["amount","margin","root"].some(mR(i,n))&&this.startObserver()}unmount(){}}function mR({viewport:t={}},{viewport:i={}}={}){return n=>t[n]!==i[n]}const fR={inView:{Feature:pR},tap:{Feature:rR},focus:{Feature:sR},hover:{Feature:oR}},gR={layout:{ProjectionNode:Fy,MeasureLayout:My}},Jc={current:null},Ly={current:!1};function yR(){if(Ly.current=!0,!!ld)if(window.matchMedia){const t=window.matchMedia("(prefers-reduced-motion)"),i=()=>Jc.current=t.matches;t.addListener(i),i()}else Jc.current=!1}const wR=[...sy,it,Oa],vR=t=>wR.find(oy(t)),pf=new WeakMap;function bR(t,i,n){for(const s in i){const r=i[s],c=n[s];if(Qe(r))t.addValue(s,r);else if(Qe(c))t.addValue(s,Jt(r,{owner:t}));else if(c!==r)if(t.hasValue(s)){const h=t.getValue(s);h.liveStyle===!0?h.jump(r):h.hasAnimated||h.set(r)}else{const h=t.getStaticValue(s);t.addValue(s,Jt(h!==void 0?h:r,{owner:t}))}}for(const s in n)i[s]===void 0&&t.removeValue(s);return i}const mf=["AnimationStart","AnimationComplete","Update","BeforeLayoutMeasure","LayoutMeasure","LayoutAnimationStart","LayoutAnimationComplete"];class kR{scrapeMotionValuesFromProps(i,n,s){return{}}constructor({parent:i,props:n,presenceContext:s,reducedMotionConfig:r,blockInitialAnimation:c,visualState:h},p={}){this.current=null,this.children=new Set,this.isVariantNode=!1,this.isControllingVariants=!1,this.shouldReduceMotion=null,this.values=new Map,this.KeyframeResolver=jd,this.features={},this.valueSubscriptions=new Map,this.prevMotionValues={},this.events={},this.propEventSubscriptions={},this.notifyUpdate=()=>this.notify("Update",this.latestValues),this.render=()=>{this.current&&(this.triggerBuild(),this.renderInstance(this.current,this.renderState,this.props.style,this.projection))},this.renderScheduledAt=0,this.scheduleRender=()=>{const A=Qt.now();this.renderScheduledAt<A&&(this.renderScheduledAt=A,fe.render(this.render,!1,!0))};const{latestValues:m,renderState:f,onUpdate:y}=h;this.onUpdate=y,this.latestValues=m,this.baseTarget={...m},this.initialValues=n.initial?{...m}:{},this.renderState=f,this.parent=i,this.props=n,this.presenceContext=s,this.depth=i?i.depth+1:0,this.reducedMotionConfig=r,this.options=p,this.blockInitialAnimation=!!c,this.isControllingVariants=or(n),this.isVariantNode=mg(n),this.isVariantNode&&(this.variantChildren=new Set),this.manuallyAnimateOnMount=!!(i&&i.current);const{willChange:v,...b}=this.scrapeMotionValuesFromProps(n,{},this);for(const A in b){const T=b[A];m[A]!==void 0&&Qe(T)&&T.set(m[A],!1)}}mount(i){this.current=i,pf.set(i,this),this.projection&&!this.projection.instance&&this.projection.mount(i),this.parent&&this.isVariantNode&&!this.isControllingVariants&&(this.removeFromVariantTree=this.parent.addVariantChild(this)),this.values.forEach((n,s)=>this.bindToMotionValue(s,n)),Ly.current||yR(),this.shouldReduceMotion=this.reducedMotionConfig==="never"?!1:this.reducedMotionConfig==="always"?!0:Jc.current,this.parent&&this.parent.children.add(this),this.update(this.props,this.presenceContext)}unmount(){pf.delete(this.current),this.projection&&this.projection.unmount(),$t(this.notifyUpdate),$t(this.render),this.valueSubscriptions.forEach(i=>i()),this.valueSubscriptions.clear(),this.removeFromVariantTree&&this.removeFromVariantTree(),this.parent&&this.parent.children.delete(this);for(const i in this.events)this.events[i].clear();for(const i in this.features){const n=this.features[i];n&&(n.unmount(),n.isMounted=!1)}this.current=null}bindToMotionValue(i,n){this.valueSubscriptions.has(i)&&this.valueSubscriptions.get(i)();const s=ni.has(i),r=n.on("change",p=>{this.latestValues[i]=p,this.props.onUpdate&&fe.preRender(this.notifyUpdate),s&&this.projection&&(this.projection.isTransformDirty=!0)}),c=n.on("renderRequest",this.scheduleRender);let h;window.MotionCheckAppearSync&&(h=window.MotionCheckAppearSync(this,i,n)),this.valueSubscriptions.set(i,()=>{r(),c(),h&&h(),n.owner&&n.stop()})}sortNodePosition(i){return!this.current||!this.sortInstanceNodePosition||this.type!==i.type?0:this.sortInstanceNodePosition(this.current,i.current)}updateFeatures(){let i="animation";for(i in ji){const n=ji[i];if(!n)continue;const{isEnabled:s,Feature:r}=n;if(!this.features[i]&&r&&s(this.props)&&(this.features[i]=new r(this)),this.features[i]){const c=this.features[i];c.isMounted?c.update():(c.mount(),c.isMounted=!0)}}}triggerBuild(){this.build(this.renderState,this.latestValues,this.props)}measureViewportBox(){return this.current?this.measureInstanceViewportBox(this.current,this.props):Le()}getStaticValue(i){return this.latestValues[i]}setStaticValue(i,n){this.latestValues[i]=n}update(i,n){(i.transformTemplate||this.props.transformTemplate)&&this.scheduleRender(),this.prevProps=this.props,this.props=i,this.prevPresenceContext=this.presenceContext,this.presenceContext=n;for(let s=0;s<mf.length;s++){const r=mf[s];this.propEventSubscriptions[r]&&(this.propEventSubscriptions[r](),delete this.propEventSubscriptions[r]);const c="on"+r,h=i[c];h&&(this.propEventSubscriptions[r]=this.on(r,h))}this.prevMotionValues=bR(this,this.scrapeMotionValuesFromProps(i,this.prevProps,this),this.prevMotionValues),this.handleChildMotionValue&&this.handleChildMotionValue(),this.onUpdate&&this.onUpdate(this)}getProps(){return this.props}getVariant(i){return this.props.variants?this.props.variants[i]:void 0}getDefaultTransition(){return this.props.transition}getTransformPagePoint(){return this.props.transformPagePoint}getClosestVariantNode(){return this.isVariantNode?this:this.parent?this.parent.getClosestVariantNode():void 0}addVariantChild(i){const n=this.getClosestVariantNode();if(n)return n.variantChildren&&n.variantChildren.add(i),()=>n.variantChildren.delete(i)}addValue(i,n){const s=this.values.get(i);n!==s&&(s&&this.removeValue(i),this.bindToMotionValue(i,n),this.values.set(i,n),this.latestValues[i]=n.get())}removeValue(i){this.values.delete(i);const n=this.valueSubscriptions.get(i);n&&(n(),this.valueSubscriptions.delete(i)),delete this.latestValues[i],this.removeValueFromRenderState(i,this.renderState)}hasValue(i){return this.values.has(i)}getValue(i,n){if(this.props.values&&this.props.values[i])return this.props.values[i];let s=this.values.get(i);return s===void 0&&n!==void 0&&(s=Jt(n===null?void 0:n,{owner:this}),this.addValue(i,s)),s}readValue(i,n){var s;let r=this.latestValues[i]!==void 0||!this.current?this.latestValues[i]:(s=this.getBaseTargetFromProps(this.props,i))!==null&&s!==void 0?s:this.readValueFromInstance(this.current,i,this.options);return r!=null&&(typeof r=="string"&&(iy(r)||Jg(r))?r=parseFloat(r):!vR(r)&&Oa.test(n)&&(r=ey(i,n)),this.setBaseTarget(i,Qe(r)?r.get():r)),Qe(r)?r.get():r}setBaseTarget(i,n){this.baseTarget[i]=n}getBaseTarget(i){var n;const{initial:s}=this.props;let r;if(typeof s=="string"||typeof s=="object"){const h=gd(this.props,s,(n=this.presenceContext)===null||n===void 0?void 0:n.custom);h&&(r=h[i])}if(s&&r!==void 0)return r;const c=this.getBaseTargetFromProps(this.props,i);return c!==void 0&&!Qe(c)?c:this.initialValues[i]!==void 0&&r===void 0?void 0:this.baseTarget[i]}on(i,n){return this.events[i]||(this.events[i]=new Rd),this.events[i].add(n)}notify(i,...n){this.events[i]&&this.events[i].notify(...n)}}class jy extends kR{constructor(){super(...arguments),this.KeyframeResolver=ry}sortInstanceNodePosition(i,n){return i.compareDocumentPosition(n)&2?1:-1}getBaseTargetFromProps(i,n){return i.style?i.style[n]:void 0}removeValueFromRenderState(i,{vars:n,style:s}){delete n[i],delete s[i]}handleChildMotionValue(){this.childSubscription&&(this.childSubscription(),delete this.childSubscription);const{children:i}=this.props;Qe(i)&&(this.childSubscription=i.on("change",n=>{this.current&&(this.current.textContent=`${n}`)}))}}function xR(t){return window.getComputedStyle(t)}class SR extends jy{constructor(){super(...arguments),this.type="html",this.renderInstance=Sg}readValueFromInstance(i,n){if(ni.has(n)){const s=Ld(n);return s&&s.default||0}else{const s=xR(i),r=(bg(n)?s.getPropertyValue(n):s[n])||0;return typeof r=="string"?r.trim():r}}measureInstanceViewportBox(i,{transformPagePoint:n}){return Ty(i,n)}build(i,n,s){vd(i,n,s.transformTemplate)}scrapeMotionValuesFromProps(i,n,s){return Sd(i,n,s)}}class AR extends jy{constructor(){super(...arguments),this.type="svg",this.isSVGTag=!1,this.measureInstanceViewportBox=Le}getBaseTargetFromProps(i,n){return i[n]}readValueFromInstance(i,n){if(ni.has(n)){const s=Ld(n);return s&&s.default||0}return n=Ag.has(n)?n:pd(n),i.getAttribute(n)}scrapeMotionValuesFromProps(i,n,s){return Ig(i,n,s)}build(i,n,s){bd(i,n,this.isSVGTag,s.transformTemplate)}renderInstance(i,n,s,r){Cg(i,n,s,r)}mount(i){this.isSVGTag=xd(i.tagName),super.mount(i)}}const CR=(t,i)=>fd(t)?new AR(i):new SR(i,{allowProjection:t!==x.Fragment}),TR=eM({...YP,...fR,...nR,...gR},CR),He=fI(TR);function Vy(t,i){let n;const s=()=>{const{currentTime:r}=i,h=(r===null?0:r.value)/100;n!==h&&t(h),n=h};return fe.update(s,!0),()=>$t(s)}const Ls=new WeakMap;let qa;function IR(t,i){if(i){const{inlineSize:n,blockSize:s}=i[0];return{width:n,height:s}}else return t instanceof SVGElement&&"getBBox"in t?t.getBBox():{width:t.offsetWidth,height:t.offsetHeight}}function MR({target:t,contentRect:i,borderBoxSize:n}){var s;(s=Ls.get(t))===null||s===void 0||s.forEach(r=>{r({target:t,contentSize:i,get size(){return IR(t,n)}})})}function PR(t){t.forEach(MR)}function DR(){typeof ResizeObserver>"u"||(qa=new ResizeObserver(PR))}function RR(t,i){qa||DR();const n=Id(t);return n.forEach(s=>{let r=Ls.get(s);r||(r=new Set,Ls.set(s,r)),r.add(i),qa==null||qa.observe(s)}),()=>{n.forEach(s=>{const r=Ls.get(s);r==null||r.delete(i),r!=null&&r.size||qa==null||qa.unobserve(s)})}}const js=new Set;let Qn;function ER(){Qn=()=>{const t={width:window.innerWidth,height:window.innerHeight},i={target:window,size:t,contentSize:t};js.forEach(n=>n(i))},window.addEventListener("resize",Qn)}function UR(t){return js.add(t),Qn||ER(),()=>{js.delete(t),!js.size&&Qn&&(Qn=void 0)}}function qR(t,i){return typeof t=="function"?UR(t):RR(t,i)}const OR=50,ff=()=>({current:0,offset:[],progress:0,scrollLength:0,targetOffset:0,targetLength:0,containerLength:0,velocity:0}),FR=()=>({time:0,x:ff(),y:ff()}),LR={x:{length:"Width",position:"Left"},y:{length:"Height",position:"Top"}};function gf(t,i,n,s){const r=n[i],{length:c,position:h}=LR[i],p=r.current,m=n.time;r.current=t[`scroll${h}`],r.scrollLength=t[`scroll${c}`]-t[`client${c}`],r.offset.length=0,r.offset[0]=0,r.offset[1]=r.scrollLength,r.progress=ii(0,r.scrollLength,r.current);const f=s-m;r.velocity=f>OR?0:Ed(r.current-p,f)}function jR(t,i,n){gf(t,"x",i,n),gf(t,"y",i,n),i.time=n}function VR(t,i){const n={x:0,y:0};let s=t;for(;s&&s!==i;)if(s instanceof HTMLElement)n.x+=s.offsetLeft,n.y+=s.offsetTop,s=s.offsetParent;else if(s.tagName==="svg"){const r=s.getBoundingClientRect();s=s.parentElement;const c=s.getBoundingClientRect();n.x+=r.left-c.left,n.y+=r.top-c.top}else if(s instanceof SVGGraphicsElement){const{x:r,y:c}=s.getBBox();n.x+=r,n.y+=c;let h=null,p=s.parentNode;for(;!h;)p.tagName==="svg"&&(h=p),p=s.parentNode;s=h}else break;return n}const Yc={start:0,center:.5,end:1};function yf(t,i,n=0){let s=0;if(t in Yc&&(t=Yc[t]),typeof t=="string"){const r=parseFloat(t);t.endsWith("px")?s=r:t.endsWith("%")?t=r/100:t.endsWith("vw")?s=r/100*document.documentElement.clientWidth:t.endsWith("vh")?s=r/100*document.documentElement.clientHeight:t=r}return typeof t=="number"&&(s=i*t),n+s}const NR=[0,0];function WR(t,i,n,s){let r=Array.isArray(t)?t:NR,c=0,h=0;return typeof t=="number"?r=[t,t]:typeof t=="string"&&(t=t.trim(),t.includes(" ")?r=t.split(" "):r=[t,Yc[t]?t:"0"]),c=yf(r[0],n,s),h=yf(r[1],i),c-h}const BR={All:[[0,0],[1,1]]},zR={x:0,y:0};function HR(t){return"getBBox"in t&&t.tagName!=="svg"?t.getBBox():{width:t.clientWidth,height:t.clientHeight}}function KR(t,i,n){const{offset:s=BR.All}=n,{target:r=t,axis:c="y"}=n,h=c==="y"?"height":"width",p=r!==t?VR(r,t):zR,m=r===t?{width:t.scrollWidth,height:t.scrollHeight}:HR(r),f={width:t.clientWidth,height:t.clientHeight};i[c].offset.length=0;let y=!i[c].interpolate;const v=s.length;for(let b=0;b<v;b++){const A=WR(s[b],f[h],m[h],p[c]);!y&&A!==i[c].interpolatorOffsets[b]&&(y=!0),i[c].offset[b]=A}y&&(i[c].interpolate=my(i[c].offset,fy(s),{clamp:!1}),i[c].interpolatorOffsets=[...i[c].offset]),i[c].progress=Xt(0,1,i[c].interpolate(i[c].current))}function _R(t,i=t,n){if(n.x.targetOffset=0,n.y.targetOffset=0,i!==t){let s=i;for(;s&&s!==t;)n.x.targetOffset+=s.offsetLeft,n.y.targetOffset+=s.offsetTop,s=s.offsetParent}n.x.targetLength=i===t?i.scrollWidth:i.clientWidth,n.y.targetLength=i===t?i.scrollHeight:i.clientHeight,n.x.containerLength=t.clientWidth,n.y.containerLength=t.clientHeight}function GR(t,i,n,s={}){return{measure:()=>_R(t,s.target,n),update:r=>{jR(t,n,r),(s.offset||s.target)&&KR(t,n,s)},notify:()=>i(n)}}const Vn=new WeakMap,wf=new WeakMap,Cc=new WeakMap,vf=t=>t===document.documentElement?window:t;function Wd(t,{container:i=document.documentElement,...n}={}){let s=Cc.get(i);s||(s=new Set,Cc.set(i,s));const r=FR(),c=GR(i,t,r,n);if(s.add(c),!Vn.has(i)){const p=()=>{for(const b of s)b.measure()},m=()=>{for(const b of s)b.update(ze.timestamp)},f=()=>{for(const b of s)b.notify()},y=()=>{fe.read(p,!1,!0),fe.read(m,!1,!0),fe.update(f,!1,!0)};Vn.set(i,y);const v=vf(i);window.addEventListener("resize",y,{passive:!0}),i!==document.documentElement&&wf.set(i,qR(i,y)),v.addEventListener("scroll",y,{passive:!0})}const h=Vn.get(i);return fe.read(h,!1,!0),()=>{var p;$t(h);const m=Cc.get(i);if(!m||(m.delete(c),m.size))return;const f=Vn.get(i);Vn.delete(i),f&&(vf(i).removeEventListener("scroll",f),(p=wf.get(i))===null||p===void 0||p(),window.removeEventListener("resize",f))}}function JR({source:t,container:i,axis:n="y"}){t&&(i=t);const s={value:0},r=Wd(c=>{s.value=c[n].progress*100},{container:i,axis:n});return{currentTime:s,cancel:r}}const Tc=new Map;function Ny({source:t,container:i=document.documentElement,axis:n="y"}={}){t&&(i=t),Tc.has(i)||Tc.set(i,{});const s=Tc.get(i);return s[n]||(s[n]=Dg()?new ScrollTimeline({source:i,axis:n}):JR({source:i,axis:n})),s[n]}function YR(t){return t.length===2}function Wy(t){return t&&(t.target||t.offset)}function QR(t,i){return YR(t)||Wy(i)?Wd(n=>{t(n[i.axis].progress,n)},i):Vy(t,Ny(i))}function $R(t,i){if(t.flatten(),Wy(i))return t.pause(),Wd(n=>{t.time=t.duration*n[i.axis].progress},i);{const n=Ny(i);return t.attachTimeline?t.attachTimeline(n,s=>(s.pause(),Vy(r=>{s.time=s.duration*r},n))):rt}}function XR(t,{axis:i="y",...n}={}){const s={axis:i,...n};return typeof t=="function"?QR(t,s):$R(t,s)}function bf(t,i){rI(!!(!i||i.current))}const ZR=()=>({scrollX:Jt(0),scrollY:Jt(0),scrollXProgress:Jt(0),scrollYProgress:Jt(0)});function eE({container:t,target:i,layoutEffect:n=!0,...s}={}){const r=ro(ZR);return(n?ar:x.useEffect)(()=>(bf("target",i),bf("container",t),XR((h,{x:p,y:m})=>{r.scrollX.set(p.current),r.scrollXProgress.set(p.progress),r.scrollY.set(m.current),r.scrollYProgress.set(m.progress)},{...s,container:(t==null?void 0:t.current)||void 0,target:(i==null?void 0:i.current)||void 0})),[t,i,JSON.stringify(s.offset)]),r}function io(t){const i=ro(()=>Jt(t)),{isStatic:n}=x.useContext(lo);if(n){const[,s]=x.useState(t);x.useEffect(()=>i.on("change",s),[])}return i}function kf(t){return typeof t=="number"?t:parseFloat(t)}function no(t,i={}){const{isStatic:n}=x.useContext(lo),s=x.useRef(null),r=io(Qe(t)?kf(t.get()):t),c=x.useRef(r.get()),h=x.useRef(()=>{}),p=()=>{const f=s.current;f&&f.time===0&&f.sample(ze.delta),m(),s.current=xP({keyframes:[r.get(),c.current],velocity:r.getVelocity(),type:"spring",restDelta:.001,restSpeed:.01,...i,onUpdate:h.current})},m=()=>{s.current&&s.current.stop()};return x.useInsertionEffect(()=>r.attach((f,y)=>n?y(f):(c.current=f,h.current=y,fe.update(p),r.get()),m),[JSON.stringify(i)]),ar(()=>{if(Qe(t))return t.on("change",f=>r.set(kf(f)))},[r]),r}const tE={some:0,all:1};function aE(t,i,{root:n,margin:s,amount:r="some"}={}){const c=Id(t),h=new WeakMap,p=f=>{f.forEach(y=>{const v=h.get(y.target);if(y.isIntersecting!==!!v)if(y.isIntersecting){const b=i(y);typeof b=="function"?h.set(y.target,b):m.unobserve(y.target)}else typeof v=="function"&&(v(y),h.delete(y.target))})},m=new IntersectionObserver(p,{root:n,rootMargin:s,threshold:typeof r=="number"?r:tE[r]});return c.forEach(f=>m.observe(f)),()=>m.disconnect()}function iE(t,{root:i,margin:n,amount:s,once:r=!1}={}){const[c,h]=x.useState(!1);return x.useEffect(()=>{if(!t.current||r&&c)return;const p=()=>(h(!0),r?void 0:()=>h(!1)),m={root:i&&i.current||void 0,margin:n,amount:s};return aE(t.current,p,m)},[i,t,n,r,s]),c}function De({children:t,delay:i=0,y:n=28,className:s="",once:r=!0}){return g.jsx(He.div,{className:s,initial:{opacity:0,y:n},whileInView:{opacity:1,y:0},viewport:{once:r,margin:"-60px"},transition:{duration:.7,delay:i,ease:[.21,.65,.32,.99]},children:t})}function nE({value:t,suffix:i="",className:n=""}){const s=x.useRef(null),r=iE(s,{once:!0,margin:"-40px"}),[c,h]=x.useState(0);return x.useEffect(()=>{if(!r)return;const p=1400,m=performance.now();let f;const y=v=>{const b=Math.min((v-m)/p,1),A=1-Math.pow(1-b,3);h(Math.round(A*t)),b<1&&(f=requestAnimationFrame(y))};return f=requestAnimationFrame(y),()=>cancelAnimationFrame(f)},[r,t]),g.jsxs("span",{ref:s,className:n,children:[c.toLocaleString(),i]})}function oE({children:t,className:i="",max:n=7}){const s=x.useRef(null),r=io(0),c=io(0),h=no(r,{stiffness:180,damping:20}),p=no(c,{stiffness:180,damping:20});function m(y){const v=s.current.getBoundingClientRect(),b=(y.clientX-v.left)/v.width-.5,A=(y.clientY-v.top)/v.height-.5;c.set(b*n),r.set(-A*n)}function f(){r.set(0),c.set(0)}return g.jsx(He.div,{ref:s,onMouseMove:m,onMouseLeave:f,style:{rotateX:h,rotateY:p,transformStyle:"preserve-3d",perspective:900},className:i,children:t})}function Qc({children:t,strength:i=.3,className:n=""}){const s=x.useRef(null),r=io(0),c=io(0),h=no(r,{stiffness:200,damping:15}),p=no(c,{stiffness:200,damping:15});function m(y){const v=s.current.getBoundingClientRect();r.set((y.clientX-v.left-v.width/2)*i),c.set((y.clientY-v.top-v.height/2)*i)}function f(){r.set(0),c.set(0)}return g.jsx(He.div,{ref:s,onMouseMove:m,onMouseLeave:f,style:{x:h,y:p},className:n,children:t})}function Gi({index:t,title:i,accent:n}){return g.jsxs(De,{className:"mb-12 md:mb-16",children:[g.jsxs("div",{className:"flex items-baseline gap-4",children:[g.jsx("span",{className:"font-display text-sm tracking-[0.3em] text-violet uppercase",children:t}),g.jsxs("h2",{className:"font-display text-4xl md:text-5xl font-bold text-bright",children:[i," ",n&&g.jsx("span",{className:"text-gradient",children:n})]})]}),g.jsx("div",{className:"mt-5 h-px w-full bg-gradient-to-r from-violet/60 via-line to-transparent"})]})}function By({size:t=40}){return g.jsxs("svg",{width:t,height:t,viewBox:"0 0 48 48",fill:"none","aria-label":"RJ monogram",children:[g.jsx("defs",{children:g.jsxs("linearGradient",{id:"rj-grad",x1:"0",y1:"0",x2:"48",y2:"48",children:[g.jsx("stop",{offset:"0%",stopColor:"#a78bfa"}),g.jsx("stop",{offset:"55%",stopColor:"#e879f9"}),g.jsx("stop",{offset:"100%",stopColor:"#67e8f9"})]})}),g.jsx("rect",{x:"1.5",y:"1.5",width:"45",height:"45",rx:"12",stroke:"url(#rj-grad)",strokeWidth:"2.5"}),g.jsx("text",{x:"24",y:"31.5",textAnchor:"middle",fontFamily:"Space Grotesk, sans-serif",fontWeight:"700",fontSize:"19",fill:"url(#rj-grad)",children:"RJ"})]})}function cr({src:t,alt:i,label:n,className:s="",accent:r="violet"}){const c={violet:"from-violet-500/25 via-fuchsia-500/15",cyan:"from-cyan-400/25 via-sky-500/15",emerald:"from-emerald-400/25 via-teal-500/15",amber:"from-amber-400/25 via-orange-500/15"};return t?g.jsx("img",{src:t,alt:i,loading:"lazy",className:`h-full w-full object-cover ${s}`}):g.jsxs("div",{className:`flex h-full w-full flex-col items-center justify-center gap-3 bg-gradient-to-br ${c[r]||c.violet} to-transparent ${s}`,children:[g.jsxs("svg",{width:"44",height:"44",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"1.3",className:"text-bright/40",children:[g.jsx("rect",{x:"3",y:"3",width:"18",height:"18",rx:"3"}),g.jsx("circle",{cx:"9",cy:"9",r:"2"}),g.jsx("path",{d:"m21 15-4.5-4.5L7 20"})]}),g.jsxs("span",{className:"px-4 text-center text-xs tracking-wide text-bright/40",children:[n||i," — screenshot coming soon"]})]})}function Ni({children:t,className:i=""}){return g.jsx("span",{className:`inline-block rounded-full border border-line bg-white/[0.03] px-3 py-1 text-xs font-medium tracking-wide text-body ${i}`,children:t})}const sE="modulepreload",rE=function(t){return"/"+t},xf={},zy=function(i,n,s){let r=Promise.resolve();if(n&&n.length>0){let h=function(f){return Promise.all(f.map(y=>Promise.resolve(y).then(v=>({status:"fulfilled",value:v}),v=>({status:"rejected",reason:v}))))};document.getElementsByTagName("link");const p=document.querySelector("meta[property=csp-nonce]"),m=(p==null?void 0:p.nonce)||(p==null?void 0:p.getAttribute("nonce"));r=h(n.map(f=>{if(f=rE(f),f in xf)return;xf[f]=!0;const y=f.endsWith(".css"),v=y?'[rel="stylesheet"]':"";if(document.querySelector(`link[href="${f}"]${v}`))return;const b=document.createElement("link");if(b.rel=y?"stylesheet":sE,y||(b.as="script"),b.crossOrigin="",b.href=f,m&&b.setAttribute("nonce",m),document.head.appendChild(b),y)return new Promise((A,T)=>{b.addEventListener("load",A),b.addEventListener("error",()=>T(new Error(`Unable to preload CSS for ${f}`)))})}))}function c(h){const p=new Event("vite:preloadError",{cancelable:!0});if(p.payload=h,window.dispatchEvent(p),!p.defaultPrevented)throw h}return r.then(h=>{for(const p of h||[])p.status==="rejected"&&c(p.reason);return i().catch(c)})},lE="/assets/revanth-aIJoikZB.png",cE="/assets/main_banner_image-CYrGodu7.jpg",dE="/assets/mifos_mobile-CyfImdY2.png",hE="/assets/android_studio-DepsAEy5.png",uE="/assets/abhiyanth-BLZao8qe.png",pE="/assets/meme-CBUfHHT_.png",mE="/assets/swipe_assign-BDpvp73J.png",fE="/assets/kisan-Ccx7BOhw.png",gE="/assets/Finance-ClkzU8NH.png",yE="/assets/ipl-wOio7x8m.png",wE="/assets/department-k0NsSQqd.png",vE="/assets/insta-CxnzjlTm.png",bE="/assets/netflix-7RbTWAq9.png",kE="/assets/expense_tracker-DpUiUELN.png",xE="/assets/Revanth_final_version-yb7_pk0P.pdf",mt={name:"Revanth Kumar Jilakara",shortName:"Revanth",location:"Andhra Pradesh, India",email:"jrevanth101@gmail.com",resume:xE,photo:lE,heroImage:cE},Hy=[{name:"GitHub",url:"https://github.com/revanthkumarJ",icon:"github"},{name:"LinkedIn",url:"https://www.linkedin.com/in/jilakararevanthkumar/",icon:"linkedin"},{name:"Instagram",url:"https://www.instagram.com/revanth_kumar_j",icon:"instagram"},{name:"Email",url:"mailto:jrevanth101@gmail.com",icon:"mail"}],SE=[{value:120,suffix:"+",label:"Open-source PRs merged"},{value:98,suffix:"%",label:"PR merge rate"},{value:230,suffix:"+",label:"Contributor PRs reviewed"},{value:1e3,suffix:"+",label:"DSA problems solved"}],Sf={paragraphs:["I'm a Software Engineer from Andhra Pradesh, India, specializing in Android and Kotlin Multiplatform development. Currently SDE 1 – Android Developer at Swipe (YC S21), where I was promoted from intern after migrating 70+ screens to Jetpack Compose and restructuring a monolith into multi-module Clean Architecture.","I'm an active contributor and mentor at the Mifos Initiative — 120+ merged PRs at a 98% merge rate, 230+ contributor PRs reviewed, and 20+ modules migrated to Kotlin Multiplatform across production fintech apps used worldwide. I was a Mifos Summer of Code 2025 intern and now mentor contributors for GSoC and Code4GovTech.","I hold a B.Tech in CSE from RGUKT RK Valley and qualified GATE CS 2025. Off the keyboard: badminton, chess, and poking at new tools."],quote:"Consistency and curiosity are the keys to growth."},Ps={primary:{title:"Android & Kotlin Multiplatform",subtitle:"My core craft — what I ship to production every day",items:["Kotlin","Jetpack Compose","Kotlin Multiplatform","Compose Multiplatform","Coroutines & Flow","MVI","Clean Architecture","Multi-module Architecture","Room (KMP)","SQLDelight","DataStore","Koin","Dagger / Hilt","Ktor / Ktorfit","Retrofit","WorkManager","Compose Navigation","Material 3","kotlinx.serialization","Gradle Convention Plugins","R8 / Play Store Releases","Firebase Crashlytics & Analytics","Lottie","GitHub Actions (CI/CD)"]},secondary:[{title:"Web & Backend",items:["React","JavaScript","TypeScript","Node.js","Express","Tailwind CSS","Firebase","MongoDB","MySQL"]},{title:"Languages & Tools",items:["Java","Python","C","Git & GitHub","Postman","Figma-to-UI"]}]},AE=[{name:"LeetCode",detail:"Knight · 800+ solved",url:"https://leetcode.com/u/RevanthKumarJ/"},{name:"GeeksforGeeks",detail:"Institute Rank 1 · 1000+ solved",url:"https://www.geeksforgeeks.org/user/jrevanth/"},{name:"CodeChef",detail:"3★ Coder",url:"https://www.codechef.com/users/revanthkumarj1"},{name:"HackerRank",detail:"5★ Problem Solving",url:"https://www.hackerrank.com/jrevanth101"}],CE=[{company:"Swipe (YC S21)",role:"SDE 1 — Android Developer",period:"Jun 15, 2026 — Present",current:!0,summary:"Owning end-to-end feature flows in the Swipe billing app — settings, products, expenses, and indirect income — while improving architecture, build tooling, and reliability for thousands of businesses.",highlights:["Built all settings pages — document, general, thermal print, and product settings.","Own the entire products, expenses, and indirect-income flows (list / create / edit).","Introduced build-logic convention plugins, cutting app build.gradle complexity by ~80%.","Work on notifications, thermal prints, PDF generation; fix production crashes via Crashlytics."],tech:["Kotlin","Jetpack Compose","MVI","Koin","Room","Convention Plugins"],links:[{text:"Swipe on Play Store",url:"https://play.google.com/store/apps/details?id=com.swipe.bill"}]},{company:"Swipe (YC S21)",role:"Android Developer Intern",period:"Dec 2025 — Jun 14, 2026",summary:"Migrated 70+ screens from legacy XML to Jetpack Compose across 15 production flows — improving UX along the way — and restructured a monolithic codebase into multi-module Clean Architecture with MVI.",highlights:["Migrated the onboarding screen — the first thing users see — with a fresh Compose UI.","Migrated document settings, templates, create-expense, and create-product screens.","Restructured monolithic files into multi-module Clean Architecture built for scale.","Resolved production crashes and UI defects via Crashlytics and Intercom."],tech:["Kotlin","Jetpack Compose","Coroutines","Flow","Retrofit","Room"],links:[{text:"Swipe on Play Store",url:"https://play.google.com/store/apps/details?id=com.swipe.bill"}]},{company:"Mifos Initiative",role:"Mentor — GSoC & C4GT",period:"Mar 2026 — Present",current:!0,summary:"Mentoring open-source contributors: reviewing PRs, running standups, conducting GSoC and Code4GovTech interviews, and guiding Kotlin Multiplatform architecture discussions.",highlights:["Conduct interviews for Google Summer of Code and Code4GovTech.","Review contributor pull requests across community projects (230+ reviewed).","Run standups and guide mobile-architecture discussions.","Help new contributors onboard into the Mifos ecosystem."],tech:["Kotlin Multiplatform","Code Review","Mentorship"],links:[]},{company:"Mifos Initiative",role:"Open Source Mobile Developer",period:"Nov 2024 — Present",current:!0,summary:"120+ PRs at a 98% merge rate across 5 production fintech repositories. Migrated 20+ modules to Kotlin Multiplatform in apps focused on financial inclusion.",highlights:["android-client: 50+ PRs migrating UI to KMP/CMP, Figma redesigns, type-safe navigation.","mifos-mobile: 45+ PRs — module migrations, QR binding, localization, new screens.","mobile-wallet: migrated APIs to the Self API and refreshed the surrounding flows.","Contributed to kmp-project-template and mifos-x-group-banking setup."],tech:["Kotlin Multiplatform","Compose Multiplatform","Ktor","Koin","MVI"],links:[{text:"All merged PRs",url:"https://github.com/pulls?q=is%3Apr+is%3Amerged+author%3ArevanthKumarJ+org%3AopenMF+"},{text:"PRs reviewed",url:"https://github.com/pulls?q=is%3Apr+reviewed-by%3ArevanthKumarJ+org%3AopenMF+"},{text:"Mifos Mobile",url:"https://play.google.com/store/apps/details?id=org.mifos.mobile"},{text:"Android Client",url:"https://play.google.com/store/apps/details?id=com.mifos.mifosxdroid"}]},{company:"Mifos Initiative",role:"Mifos Summer of Code 2025 Intern",period:"Jun 2025 — Sep 2025",summary:"Selected for the competitive MSoC program ($2,500 stipend). Led the Kotlin Multiplatform migration of the Field Officer app and modernized UI across 100+ screens in three repositories.",highlights:["100+ PRs authored and 150+ reviewed during the program window.","Migrated core modules of android-client and mifos-mobile to KMP/CMP.","Implemented new Figma-based UIs: transactions, charges, beneficiaries, passcode, and more.","Grew from struggling to raise a single PR to redesigning two production apps."],tech:["Kotlin Multiplatform","Compose Multiplatform","GitHub Actions","Clean Architecture"],links:[{text:"Full MSoC writeup",url:"https://gist.github.com/revanthkumarJ/133c9e8ce0abb111fb19873ad902cb70"},{text:"Weekly progress",url:"https://github.com/revanthkumarJ/MSOC_progress"}]},{company:"Mobile Byte Sensei",role:"Mobile Development Intern (KMP & CMP)",period:"Apr 2025 — Nov 2025",summary:"Delivered 7 production-grade Kotlin Multiplatform apps — reusable UI components, file management, Firebase Analytics, and Crashlytics fixes in an Agile team.",highlights:["Shipped Reels Downloader, Stories Downloader, and Byte Wallpaper to the Play Store.","Built reusable Compose Multiplatform components and CI/CD pipelines.","Resolved critical Crashlytics-reported issues in production."],tech:["Kotlin Multiplatform","Compose Multiplatform","Firebase","CI/CD"],links:[{text:"Reels Downloader",url:"https://play.google.com/store/apps/details?id=com.sensei.social"},{text:"Stories Downloader",url:"https://play.google.com/store/apps/details?id=com.sensei.stories"},{text:"Byte Wallpaper",url:"https://play.google.com/store/apps/details?id=org.mobilebytesensei.wallpaper"}]},{company:"TLDE Technologies",role:"Software Development Intern",period:"Nov 2025 — Dec 2025",summary:"Built onboarding experiences for a Kotlin Multiplatform app using Compose Multiplatform and Lottie animations.",highlights:[],tech:["Kotlin Multiplatform","Compose Multiplatform","Lottie"],links:[]},{company:"Abhiyanth 2K25",role:"Frontend Team Lead & Developer",period:"Dec 2024 — Mar 2025",summary:"Led frontend development of the college fest platform in React — Firebase integration, admin panel, repo management, PR reviews, and UI/UX coordination.",highlights:[],tech:["React","Redux","Firebase","Material UI"],links:[{text:"GitHub repo",url:"https://github.com/revanthkumarJ/Abhiyanth-Client"}]},{company:"DevDisplay",role:"Open Source Contributor — React",period:"Jan 2025 — Feb 2025",summary:"10 merged PRs of UI enhancements and page development with React and Tailwind CSS — Sponsors, About Us, and Journey pages.",highlights:[],tech:["React","Tailwind CSS"],links:[{text:"Merged PRs",url:"https://github.com/codeaashu/DevDisplay/pulls?q=is%3Apr+is%3Amerged+author%3ArevanthkumarJ"}]},{company:"GeeksforGeeks",role:"Campus Ambassador",period:"Apr 2024 — Apr 2025",summary:"Promoted coding culture on campus, coordinated contests and events, and was recognized among the top-performing campus ambassadors.",highlights:[],tech:[],links:[]}],TE=[{role:"DSA Coordinator & Mentor",org:"SRC Student Club",period:"Apr 2024 — Mar 2025",note:"Weekly coding contests and mentoring sessions to uplift peers' DSA performance."},{role:"NSS Unit Coordinator",org:"National Service Scheme",period:"Jan 2024 — Jan 2026",note:"Organized social-outreach activities; maintain the NSS Unit-2 LinkedIn page."},{role:"Social Media Manager",org:"Dept. of CSE, RGUKT RK Valley",period:"Apr 2024 — Mar 2025",note:"Ran the department's social presence and analytics."},{role:"Class Representative",org:"Dept. of CSE, RGUKT RK Valley",period:"Jun 2024 — Nov 2024",note:"Bridge between faculty and students for E3 Sem 1."}],IE=[{title:"ExpenseTrackr",tagline:"Privacy-first offline finance tracker — one Kotlin codebase, three platforms",description:"A personal finance tracker where every expense, budget, and category lives on-device. Built as a single Kotlin Multiplatform codebase rendering native UI on Android, iOS, and Desktop via Compose Multiplatform — 16 Gradle modules, 18 screens, Clean MVI, 24 languages with an in-app switcher, PIN + biometric app-lock, and CSV backup that survives reinstalls.",metrics:[{value:"16",label:"Gradle modules"},{value:"24",label:"Languages"},{value:"3",label:"Platforms"}],tech:["Kotlin Multiplatform","Compose Multiplatform","Clean MVI","Room (KMP)","Koin","Material 3"],image:kE,accent:"emerald",links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/ExpenseTrackr",kind:"github"}],badge:"Play Store — closed testing"},{title:"Mifos Mobile — KMP Migration",tagline:"Production banking app, migrated to every platform",description:"Migrated 7 modules of Mifos Mobile — a self-service banking client used worldwide — to Kotlin Multiplatform and Compose Multiplatform, targeting Android, iOS, Web/WASM, and Desktop. 45+ merged PRs covering module migrations, new Figma-based screens, QR-code binding, and localization.",metrics:[{value:"45+",label:"PRs merged"},{value:"7",label:"Modules migrated"},{value:"5",label:"Platform targets"}],tech:["Kotlin Multiplatform","Compose Multiplatform","Ktor","Koin"],image:dE,accent:"violet",links:[{text:"GitHub",url:"https://github.com/openMF/mifos-mobile",kind:"github"},{text:"My contributions",url:"https://github.com/openMF/mifos-mobile/pulls?q=is%3Amerged+is%3Apr+author%3ArevanthkumarJ+",kind:"prs"},{text:"Play Store",url:"https://play.google.com/store/apps/details?id=org.mifos.mobile",kind:"playstore"}]},{title:"Android Client — KMP Migration",tagline:"Field-officer banking app for offline-first microfinance",description:"52 merged PRs migrating 10 modules of the Mifos Field Officer app to KMP/CMP — refactoring client screens to the latest Figma designs, adding type-safe navigation, fixing production bugs, and building new UI components for an app processing real financial transactions in remote areas.",metrics:[{value:"52",label:"PRs merged"},{value:"10",label:"Modules migrated"},{value:"100%",label:"Offline-capable"}],tech:["Kotlin Multiplatform","Compose Multiplatform","Room","MVI"],image:hE,accent:"cyan",links:[{text:"GitHub",url:"https://github.com/openMF/android-client",kind:"github"},{text:"My contributions",url:"https://github.com/openMF/android-client/pulls?q=is%3Amerged+is%3Apr+author%3ArevanthkumarJ+",kind:"prs"},{text:"Play Store",url:"https://play.google.com/store/apps/details?id=com.mifos.mifosxdroid",kind:"playstore"}]},{title:"Abhiyanth Fest Platform",tagline:"College fest platform — led the frontend team",description:"Led the frontend team building the official platform for Abhiyanth 2K25 — React, Redux, and Material UI with Firebase auth and hosting, CashFree payment integration, and a full admin panel. Managed the repo, reviewed PRs, and coordinated with UI/UX.",metrics:[{value:"Lead",label:"Frontend team"},{value:"Live",label:"Payments (CashFree)"}],tech:["React","Redux","Firebase","Material UI"],image:uE,accent:"amber",links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/abhiyanth-client",kind:"github"}]}],ME=[{title:"Meme Studio",description:"Kotlin Multiplatform meme editor with templates and custom editing, built on Compose Multiplatform.",tech:["KMP","CMP"],image:pE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/MemeStudio",kind:"github"}]},{title:"Swipe Assignment App",description:"Modern Android app — onboarding, product listing with search, offline-first Room sync, theming.",tech:["Kotlin","Room","Retrofit"],image:mE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/swipe-assignment",kind:"github"}]},{title:"KisanConnect",description:"Platform connecting farmers and customers — role-based UIs with React, Express, and TypeScript.",tech:["React","Express","TypeScript"],image:fE,links:[{text:"Client",url:"https://github.com/revanthkumarJ/kisan_connect_client",kind:"github"},{text:"API",url:"https://github.com/revanthkumarJ/Kisan_connect_API",kind:"github"}]},{title:"Finance Tracker (Web)",description:"Node.js + TypeScript API with a React/MUI frontend to manage and analyze bank statements.",tech:["Node.js","TypeScript","React"],image:gE,links:[{text:"Client",url:"https://github.com/revanthkumarJ/Finance-Client",kind:"github"},{text:"API",url:"https://github.com/revanthkumarJ/Finance-API",kind:"github"}]},{title:"Sports Auction",description:"Mock IPL auction platform — 250 players, live bidding for 150+ students across 30 teams.",tech:["React"],image:yE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/Sports-Auction",kind:"github"},{text:"Live",url:"https://sports-auction.vercel.app/",kind:"demo"}]},{title:"Dept. Resource Manager",description:"Kotlin app managing announcements, complaints, and timetables for students, faculty, and HOD.",tech:["Kotlin","Firebase"],image:wE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/Departmental-Resource-Management-App",kind:"github"}]},{title:"Instagram UI Clone",description:"Jetpack Compose recreation of the Instagram feed and profile screens.",tech:["Jetpack Compose"],image:vE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/InstaUI",kind:"github"}]},{title:"Netflix UI Clone",description:"Netflix-style UI built with Jetpack Compose — featured rows and layout aesthetics.",tech:["Jetpack Compose"],image:bE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/NetFlixUI",kind:"github"}]}],PE=[{title:"C4GT 2026 Mentor",detail:"Mentor for the Mifos Pay project under Code for GovTech 2026.",tag:"Mentorship"},{title:"Mifos Summer of Code 2025",detail:"Selected for the competitive program with a $2,500 stipend.",tag:"Open Source"},{title:"120+ Open-Source PRs",detail:"Merged across production repos at a 98% merge rate.",tag:"Open Source"},{title:"GATE CS 2025",detail:"Qualified — strong command of core CS fundamentals.",tag:"Academics"},{title:"LeetCode Knight",detail:"800+ problems solved with a consistent contest rating.",tag:"DSA"},{title:"GFG Institute Rank 1",detail:"1000+ problems — first among all institute peers.",tag:"DSA"},{title:"CodeChef 3★",detail:"Consistent contest performance.",tag:"DSA"},{title:"HackerRank 5★",detail:"Problem Solving — 500+ challenges.",tag:"DSA"},{title:"Qualified NMMS",detail:"National Means-cum-Merit Scholarship at school level.",tag:"Academics"}],Ds={quote:"Revanth brings strong expertise in Kotlin Multiplatform development, with a deep understanding of building efficient, cross-platform architectures. One of his standout strengths is his debugging ability — a sharp eye for identifying complex issues and resolving them with clarity and precision. Beyond his technical skills, he is reliable, proactive, and a great collaborator. I strongly recommend him for any role requiring expertise in Kotlin Multiplatform and robust software architecture.",author:"Rajan Maurya",title:"Engineering Manager · Kotlin Multiplatform Expert",proofUrl:"https://www.linkedin.com/in/jilakararevanthkumar/details/recommendations/"},Af={profileUrl:"https://medium.com/@jrevanth101",posts:[{title:"Ship Your Android App to Testers Automatically: A Complete GitHub Actions + Firebase App Distribution Guide",url:"https://medium.com/@jrevanth101/ship-your-android-app-to-testers-automatically-a-complete-github-actions-firebase-app-02def952a492",date:"Jul 2026",tags:["GitHub Actions","Firebase","CI/CD"]},{title:"How I Shrunk Every Feature Module's build.gradle from 60+ Lines to 12 Lines",url:"https://medium.com/@jrevanth101/how-i-shrunk-every-feature-modules-build-gradle-to-12-lines-c6341a73921f",date:"Jun 2026",tags:["Gradle","Convention Plugins","Kotlin"]},{title:"Reducing Boilerplate in Jetpack Compose MVI: The Template I Use for Every Screen",url:"https://medium.com/@jrevanth101/reducing-boilerplate-in-jetpack-compose-mvi-the-template-i-use-for-every-screen-396fa55cc17d",date:"May 2026",tags:["Jetpack Compose","MVI","Kotlin"]},{title:"Building a Comma-Separated Decimal Input in Jetpack Compose",url:"https://medium.com/@jrevanth101/building-a-comma-separated-decimal-input-in-jetpack-compose-bbcf82813e9c",date:"Mar 2026",tags:["Jetpack Compose","Kotlin"]}]},Ic={username:"revanthkumarJ",url:"https://github.com/revanthkumarJ"},Mc=[{label:"About",href:"#about"},{label:"Experience",href:"#experience"},{label:"Projects",href:"#projects"},{label:"Achievements",href:"#achievements"},{label:"Writing",href:"#writing"},{label:"Contact",href:"#contact"}];var Ky={color:void 0,size:void 0,className:void 0,style:void 0,attr:void 0},Cf=Dt.createContext&&Dt.createContext(Ky),DE=["attr","size","title"];function RE(t,i){if(t==null)return{};var n,s,r=EE(t,i);if(Object.getOwnPropertySymbols){var c=Object.getOwnPropertySymbols(t);for(s=0;s<c.length;s++)n=c[s],i.indexOf(n)===-1&&{}.propertyIsEnumerable.call(t,n)&&(r[n]=t[n])}return r}function EE(t,i){if(t==null)return{};var n={};for(var s in t)if({}.hasOwnProperty.call(t,s)){if(i.indexOf(s)!==-1)continue;n[s]=t[s]}return n}function Js(){return Js=Object.assign?Object.assign.bind():function(t){for(var i=1;i<arguments.length;i++){var n=arguments[i];for(var s in n)({}).hasOwnProperty.call(n,s)&&(t[s]=n[s])}return t},Js.apply(null,arguments)}function Tf(t,i){var n=Object.keys(t);if(Object.getOwnPropertySymbols){var s=Object.getOwnPropertySymbols(t);i&&(s=s.filter(function(r){return Object.getOwnPropertyDescriptor(t,r).enumerable})),n.push.apply(n,s)}return n}function Ys(t){for(var i=1;i<arguments.length;i++){var n=arguments[i]!=null?arguments[i]:{};i%2?Tf(Object(n),!0).forEach(function(s){UE(t,s,n[s])}):Object.getOwnPropertyDescriptors?Object.defineProperties(t,Object.getOwnPropertyDescriptors(n)):Tf(Object(n)).forEach(function(s){Object.defineProperty(t,s,Object.getOwnPropertyDescriptor(n,s))})}return t}function UE(t,i,n){return(i=qE(i))in t?Object.defineProperty(t,i,{value:n,enumerable:!0,configurable:!0,writable:!0}):t[i]=n,t}function qE(t){var i=OE(t,"string");return typeof i=="symbol"?i:i+""}function OE(t,i){if(typeof t!="object"||!t)return t;var n=t[Symbol.toPrimitive];if(n!==void 0){var s=n.call(t,i);if(typeof s!="object")return s;throw new TypeError("@@toPrimitive must return a primitive value.")}return(i==="string"?String:Number)(t)}function _y(t){return t&&t.map((i,n)=>Dt.createElement(i.tag,Ys({key:n},i.attr),_y(i.child)))}function ft(t){return i=>Dt.createElement(FE,Js({attr:Ys({},t.attr)},i),_y(t.child))}function FE(t){var i=n=>{var s=t.attr,r=t.size,c=t.title,h=RE(t,DE),p=r||n.size||"1em",m;return n.className&&(m=n.className),t.className&&(m=(m?m+" ":"")+t.className),Dt.createElement("svg",Js({stroke:"currentColor",fill:"currentColor",strokeWidth:"0"},n.attr,s,h,{className:m,style:Ys(Ys({color:t.color||n.color},n.style),t.style),height:p,width:p,xmlns:"http://www.w3.org/2000/svg"}),c&&Dt.createElement("title",null,c),t.children)};return Cf!==void 0?Dt.createElement(Cf.Consumer,null,n=>i(n)):i(Ky)}function LE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"line",attr:{x1:"18",y1:"6",x2:"6",y2:"18"},child:[]},{tag:"line",attr:{x1:"6",y1:"6",x2:"18",y2:"18"},child:[]}]})(t)}function Gy(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"},child:[]},{tag:"polyline",attr:{points:"22,6 12,13 2,6"},child:[]}]})(t)}function Jy(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"},child:[]},{tag:"rect",attr:{x:"2",y:"9",width:"4",height:"12"},child:[]},{tag:"circle",attr:{cx:"4",cy:"4",r:"2"},child:[]}]})(t)}function Yy(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"rect",attr:{x:"2",y:"2",width:"20",height:"20",rx:"5",ry:"5"},child:[]},{tag:"path",attr:{d:"M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"},child:[]},{tag:"line",attr:{x1:"17.5",y1:"6.5",x2:"17.51",y2:"6.5"},child:[]}]})(t)}function dr(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"},child:[]}]})(t)}function jE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"circle",attr:{cx:"18",cy:"18",r:"3"},child:[]},{tag:"circle",attr:{cx:"6",cy:"6",r:"3"},child:[]},{tag:"path",attr:{d:"M13 6h3a2 2 0 0 1 2 2v7"},child:[]},{tag:"line",attr:{x1:"6",y1:"9",x2:"6",y2:"21"},child:[]}]})(t)}function hr(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"},child:[]},{tag:"polyline",attr:{points:"15 3 21 3 21 9"},child:[]},{tag:"line",attr:{x1:"10",y1:"14",x2:"21",y2:"3"},child:[]}]})(t)}function VE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"},child:[]},{tag:"polyline",attr:{points:"7 10 12 15 17 10"},child:[]},{tag:"line",attr:{x1:"12",y1:"15",x2:"12",y2:"3"},child:[]}]})(t)}function NE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"polyline",attr:{points:"6 9 12 15 18 9"},child:[]}]})(t)}function WE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"circle",attr:{cx:"12",cy:"8",r:"7"},child:[]},{tag:"polyline",attr:{points:"8.21 13.89 7 23 12 20 17 23 15.79 13.88"},child:[]}]})(t)}function BE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"line",attr:{x1:"7",y1:"17",x2:"17",y2:"7"},child:[]},{tag:"polyline",attr:{points:"7 7 17 7 17 17"},child:[]}]})(t)}function zE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"line",attr:{x1:"12",y1:"5",x2:"12",y2:"19"},child:[]},{tag:"polyline",attr:{points:"19 12 12 19 5 12"},child:[]}]})(t)}const HE=Dt.lazy(()=>zy(()=>import("./PdfViewer-D1WFMJA6.js"),[])),Qy=x.createContext(()=>{});function $y(){return x.useContext(Qy)}function KE({children:t}){const[i,n]=x.useState(!1),s=x.useCallback(()=>n(!0),[]);return x.useEffect(()=>{if(!i)return;const r=c=>c.key==="Escape"&&n(!1);return window.addEventListener("keydown",r),document.body.style.overflow="hidden",()=>{window.removeEventListener("keydown",r),document.body.style.overflow=""}},[i]),g.jsxs(Qy.Provider,{value:s,children:[t,g.jsx(cd,{children:i&&g.jsx(He.div,{initial:{opacity:0},animate:{opacity:1},exit:{opacity:0},transition:{duration:.25},className:"fixed inset-0 z-[100] flex items-center justify-center bg-ink/85 p-2 backdrop-blur-sm sm:p-4 md:p-8",onClick:()=>n(!1),children:g.jsxs(He.div,{initial:{opacity:0,y:40,scale:.97},animate:{opacity:1,y:0,scale:1},exit:{opacity:0,y:40,scale:.97},transition:{duration:.35,ease:[.21,.65,.32,.99]},className:"flex h-[94vh] w-full max-w-4xl flex-col overflow-hidden rounded-2xl border border-line bg-ink-2 shadow-[0_30px_120px_-20px_rgba(167,139,250,0.35)] sm:h-[88vh]",onClick:r=>r.stopPropagation(),children:[g.jsxs("div",{className:"flex items-center justify-between border-b border-line px-5 py-3.5",children:[g.jsxs("span",{className:"font-display font-bold text-bright",children:["Resume — ",mt.name]}),g.jsxs("div",{className:"flex items-center gap-2",children:[g.jsxs("a",{href:mt.resume,download:"Revanth_Kumar_Jilakara_Resume.pdf",className:"inline-flex items-center gap-2 rounded-full border border-line px-4 py-1.5 text-sm font-medium text-bright transition-colors hover:border-violet/60 hover:bg-violet/10",children:[g.jsx(VE,{size:15})," Download"]}),g.jsx("button",{onClick:()=>n(!1),"aria-label":"Close resume",className:"rounded-full p-2 text-body transition-colors hover:bg-white/10 hover:text-bright",children:g.jsx(LE,{size:20})})]})]}),g.jsx(Dt.Suspense,{fallback:g.jsx("div",{className:"flex flex-1 items-center justify-center text-body",children:"Loading resume…"}),children:g.jsx(HE,{file:mt.resume})})]})})})]})}function _E(){const[t,i]=x.useState(!1),[n,s]=x.useState(""),[r,c]=x.useState(!1),h=$y();return x.useEffect(()=>{const p=()=>i(window.scrollY>24);return window.addEventListener("scroll",p,{passive:!0}),()=>window.removeEventListener("scroll",p)},[]),x.useEffect(()=>{const p=Mc.map(f=>document.querySelector(f.href)).filter(Boolean),m=new IntersectionObserver(f=>{f.forEach(y=>{y.isIntersecting&&s(`#${y.target.id}`)})},{rootMargin:"-40% 0px -55% 0px"});return p.forEach(f=>m.observe(f)),()=>m.disconnect()},[]),g.jsxs(He.header,{initial:{y:-80,opacity:0},animate:{y:0,opacity:1},transition:{duration:.7,ease:[.21,.65,.32,.99],delay:.1},className:`fixed inset-x-0 top-0 z-50 transition-all duration-300 ${t||r?"glass !bg-ink/85 shadow-lg shadow-black/20":"bg-transparent"}`,children:[g.jsxs("nav",{className:"flex h-[72px] w-full items-center justify-between px-5 md:px-10 xl:px-16",children:[g.jsxs("a",{href:"#top","aria-label":"Home",className:"flex items-center gap-3",children:[g.jsx(By,{size:38}),g.jsxs("span",{className:"font-display hidden text-sm font-semibold tracking-wide text-bright sm:block",children:[mt.shortName.toLowerCase(),".dev"]})]}),g.jsxs("ul",{className:"hidden items-center gap-1 md:flex",children:[Mc.map(p=>g.jsx("li",{children:g.jsxs("a",{href:p.href,className:`rounded-full px-4 py-2 text-sm font-medium transition-colors ${n===p.href?"text-bright":"text-body hover:text-bright"}`,children:[n===p.href&&g.jsx("span",{className:"mr-2 inline-block h-1.5 w-1.5 rounded-full bg-gradient-to-r from-violet to-fuchsia align-middle"}),p.label]})},p.href)),g.jsx("li",{children:g.jsx("button",{onClick:h,className:"ml-3 rounded-full border border-violet/50 px-5 py-2 text-sm font-semibold text-bright transition-all hover:border-violet hover:bg-violet/10 hover:shadow-[0_0_24px_-6px_rgba(167,139,250,0.6)]",children:"Resume"})})]}),g.jsxs("button",{className:"flex h-10 w-10 flex-col items-center justify-center gap-1.5 md:hidden",onClick:()=>c(!r),"aria-label":"Toggle menu",children:[g.jsx("span",{className:`h-0.5 w-6 bg-bright transition-transform ${r?"translate-y-2 rotate-45":""}`}),g.jsx("span",{className:`h-0.5 w-6 bg-bright transition-opacity ${r?"opacity-0":""}`}),g.jsx("span",{className:`h-0.5 w-6 bg-bright transition-transform ${r?"-translate-y-2 -rotate-45":""}`})]})]}),g.jsx(cd,{children:r&&g.jsxs(He.ul,{initial:{height:0,opacity:0},animate:{height:"auto",opacity:1},exit:{height:0,opacity:0},transition:{duration:.3},className:"overflow-hidden border-t border-line bg-ink-2/95 backdrop-blur-xl md:hidden",children:[Mc.map(p=>g.jsx("li",{children:g.jsx("a",{href:p.href,onClick:()=>c(!1),className:"block px-8 py-4 text-base font-medium text-bright/90 hover:bg-white/5",children:p.label})},p.href)),g.jsx("li",{children:g.jsx("button",{onClick:()=>{c(!1),h()},className:"block w-full px-8 py-4 text-left text-base font-semibold text-violet",children:"Resume ↗"})})]})})]})}const GE={github:dr,linkedin:Jy,instagram:Yy,mail:Gy},Nn={hidden:{opacity:0,y:40},show:t=>({opacity:1,y:0,transition:{duration:.8,delay:.25+t*.12,ease:[.21,.65,.32,.99]}})};function JE(){const t=$y();return g.jsxs("section",{id:"top",className:"relative flex min-h-screen flex-col justify-center overflow-hidden pt-[72px]",children:[g.jsx("div",{className:"blob left-[-10%] top-[-5%] h-[480px] w-[480px] bg-violet-600/25"}),g.jsx("div",{className:"blob right-[-8%] top-[30%] h-[420px] w-[420px] bg-fuchsia-600/15"}),g.jsx("div",{className:"blob bottom-[-10%] left-[30%] h-[380px] w-[380px] bg-cyan-500/10"}),g.jsxs("div",{className:"relative z-10 grid w-full items-center gap-10 px-5 md:px-10 lg:grid-cols-[1.15fr_0.85fr] xl:px-16",children:[g.jsxs("div",{children:[g.jsx(He.div,{custom:0,variants:Nn,initial:"hidden",animate:"show",className:"mb-8",children:g.jsxs("span",{className:"glass inline-flex items-center gap-2.5 rounded-full px-4 py-2 text-sm text-bright/90",children:[g.jsx("span",{className:"pulse-dot h-2 w-2 rounded-full bg-emerald"}),"Open to opportunities"]})}),g.jsxs(He.h1,{custom:1,variants:Nn,initial:"hidden",animate:"show",className:"font-display max-w-4xl text-3xl font-bold leading-[1.12] tracking-tight text-bright sm:text-4xl md:text-5xl 2xl:text-6xl",children:["I build ",g.jsx("span",{className:"text-gradient",children:"production Android"})," & Kotlin Multiplatform apps used by thousands of businesses."]}),g.jsxs(He.div,{custom:2,variants:Nn,initial:"hidden",animate:"show",className:"mt-8",children:[g.jsx("p",{className:"font-display text-xl font-bold text-bright md:text-2xl",children:mt.name}),g.jsxs("p",{className:"mt-1.5 text-base text-body md:text-lg",children:["SDE 1 · Android @ ",g.jsx("span",{className:"font-semibold text-violet",children:"Swipe (YC S21)"})]}),g.jsxs("p",{className:"mt-1 text-base text-body md:text-lg",children:["Mifos ",g.jsx("span",{className:"text-bright/90",children:"Open Source Mentor, Maintainer & Contributor"})]})]}),g.jsxs(He.div,{custom:3,variants:Nn,initial:"hidden",animate:"show",className:"mt-10 flex flex-wrap items-center gap-4",children:[g.jsx(Qc,{children:g.jsx("a",{href:"#projects",className:"font-display inline-block rounded-full bg-gradient-to-r from-violet-500 to-fuchsia-500 px-8 py-3.5 text-sm font-bold tracking-wide text-white shadow-[0_8px_40px_-8px_rgba(167,139,250,0.7)] transition-shadow hover:shadow-[0_8px_60px_-6px_rgba(232,121,249,0.8)]",children:"View my work"})}),g.jsx(Qc,{children:g.jsx("button",{onClick:t,className:"font-display inline-block cursor-pointer rounded-full border border-line px-8 py-3.5 text-sm font-bold tracking-wide text-bright transition-colors hover:border-violet/60 hover:bg-white/5",children:"Resume"})}),g.jsx("div",{className:"ml-1 flex items-center gap-1",children:Hy.map(i=>{const n=GE[i.icon];return g.jsx("a",{href:i.url,target:"_blank",rel:"noopener noreferrer","aria-label":i.name,className:"rounded-full p-3 text-body transition-all hover:text-bright hover:drop-shadow-[0_0_8px_rgba(167,139,250,0.8)]",children:g.jsx(n,{size:20})},i.name)})})]}),g.jsx(He.div,{custom:4,variants:Nn,initial:"hidden",animate:"show",className:"mt-16 grid max-w-3xl grid-cols-2 gap-x-8 gap-y-6 border-t border-line pt-8 sm:grid-cols-4 md:mt-20 xl:max-w-5xl",children:SE.map(i=>g.jsxs("div",{children:[g.jsx("div",{className:"font-display text-3xl font-bold text-bright md:text-4xl",children:g.jsx(nE,{value:i.value,suffix:i.suffix})}),g.jsx("div",{className:"mt-1 text-xs leading-snug text-body/80 md:text-sm",children:i.label})]},i.label))})]}),g.jsxs(He.div,{initial:{opacity:0,x:60},animate:{opacity:1,x:0},transition:{duration:1,delay:.6,ease:[.21,.65,.32,.99]},className:"relative flex justify-center pb-4 lg:pb-0",children:[g.jsx("div",{className:"absolute inset-0 -z-10 m-auto h-[70%] w-[70%] rounded-full bg-gradient-to-tr from-violet-600/30 via-fuchsia-600/20 to-cyan-500/20 blur-3xl"}),g.jsx(He.div,{animate:{y:[0,-12,0]},transition:{repeat:1/0,duration:6,ease:"easeInOut"},className:"glow-card glass w-full max-w-[420px] overflow-hidden rounded-3xl xl:max-w-[460px]",children:g.jsx("div",{className:"aspect-[4/5] w-full",children:g.jsx(cr,{src:mt.heroImage,alt:`${mt.name} at his dev workstation`,label:"Workspace portrait (1200×1500)"})})})]})]}),g.jsx(He.a,{href:"#about","aria-label":"Scroll to about",initial:{opacity:0},animate:{opacity:1},transition:{delay:1.6},className:"absolute bottom-8 left-1/2 z-10 -translate-x-1/2 text-body/60 hover:text-bright",children:g.jsx(He.div,{animate:{y:[0,8,0]},transition:{repeat:1/0,duration:1.8,ease:"easeInOut"},children:g.jsx(zE,{size:22})})})]})}const YE=864e5,Xy=6e4,Zy=36e5,If=Symbol.for("constructDateFrom");function mo(t,i){return typeof t=="function"?t(i):t&&typeof t=="object"&&If in t?t[If](i):t instanceof Date?new t.constructor(i):new Date(i)}function Zt(t,i){return mo(i||t,t)}function ew(t,i,n){const s=Zt(t,n==null?void 0:n.in);return isNaN(i)?mo(t,NaN):(i&&s.setDate(s.getDate()+i),s)}function Mf(t){const i=Zt(t),n=new Date(Date.UTC(i.getFullYear(),i.getMonth(),i.getDate(),i.getHours(),i.getMinutes(),i.getSeconds(),i.getMilliseconds()));return n.setUTCFullYear(i.getFullYear()),+t-+n}function tw(t,...i){const n=mo.bind(null,i.find(s=>typeof s=="object"));return i.map(n)}function Pf(t,i){const n=Zt(t,i==null?void 0:i.in);return n.setHours(0,0,0,0),n}function QE(t,i,n){const[s,r]=tw(n==null?void 0:n.in,t,i),c=Pf(s),h=Pf(r),p=+c-Mf(c),m=+h-Mf(h);return Math.round((p-m)/YE)}function $E(t,i,n){return ew(t,i*7,n)}function XE(t){return t instanceof Date||typeof t=="object"&&Object.prototype.toString.call(t)==="[object Date]"}function ZE(t){return!(!XE(t)&&typeof t!="number"||isNaN(+Zt(t)))}function eU(t,i){const[n,s]=tw(t,i.start,i.end);return{start:n,end:s}}function aw(t,i){const{start:n,end:s}=eU(i==null?void 0:i.in,t);let r=+n>+s;const c=r?+n:+s,h=r?s:n;h.setHours(0,0,0,0);let p=1;const m=[];for(;+h<=c;)m.push(mo(n,h)),h.setDate(h.getDate()+p),h.setHours(0,0,0,0);return r?m.reverse():m}function Ea(t,i){const n=t<0?"-":"",s=Math.abs(t).toString().padStart(i,"0");return n+s}function iw(t,i){const n=Zt(t,i==null?void 0:i.in);if(isNaN(+n))throw new RangeError("Invalid time value");const s=(i==null?void 0:i.format)??"extended",r=(i==null?void 0:i.representation)??"complete";let c="",h="";const p=s==="extended"?"-":"",m=s==="extended"?":":"";if(r!=="time"){const f=Ea(n.getDate(),2),y=Ea(n.getMonth()+1,2);c=`${Ea(n.getFullYear(),4)}${p}${y}${p}${f}`}if(r!=="date"){const f=n.getTimezoneOffset();if(f!==0){const I=Math.abs(f),M=Ea(Math.trunc(I/60),2),R=Ea(I%60,2);h=`${f<0?"+":"-"}${M}:${R}`}else h="Z";const y=Ea(n.getHours(),2),v=Ea(n.getMinutes(),2),b=Ea(n.getSeconds(),2),A=c===""?"":"T",T=[y,v,b].join(m);c=`${c}${A}${T}${h}`}return c}function nw(t,i){return Zt(t,i==null?void 0:i.in).getDay()}function tU(t,i){return Zt(t,i==null?void 0:i.in).getMonth()}function aU(t,i){return Zt(t,i==null?void 0:i.in).getFullYear()}function iU(t,i,n){let s=i-nw(t,n);return s<=0&&(s+=7),ew(t,s,n)}function Wi(t,i){const n=()=>mo(i==null?void 0:i.in,NaN),r=rU(t);let c;if(r.date){const f=lU(r.date,2);c=cU(f.restDateString,f.year)}if(!c||isNaN(+c))return n();const h=+c;let p=0,m;if(r.time&&(p=dU(r.time),isNaN(p)))return n();if(r.timezone){if(m=hU(r.timezone),isNaN(m))return n()}else{const f=new Date(h+p),y=Zt(0,i==null?void 0:i.in);return y.setFullYear(f.getUTCFullYear(),f.getUTCMonth(),f.getUTCDate()),y.setHours(f.getUTCHours(),f.getUTCMinutes(),f.getUTCSeconds(),f.getUTCMilliseconds()),y}return Zt(h+p+m,i==null?void 0:i.in)}const Rs={dateTimeDelimiter:/[T ]/,timeZoneDelimiter:/[Z ]/i,timezone:/([Z+-].*)$/},nU=/^-?(?:(\d{3})|(\d{2})(?:-?(\d{2}))?|W(\d{2})(?:-?(\d{1}))?|)$/,oU=/^(\d{2}(?:[.,]\d*)?)(?::?(\d{2}(?:[.,]\d*)?))?(?::?(\d{2}(?:[.,]\d*)?))?$/,sU=/^([+-])(\d{2})(?::?(\d{2}))?$/;function rU(t){const i={},n=t.split(Rs.dateTimeDelimiter);let s;if(n.length>2)return i;if(/:/.test(n[0])?s=n[0]:(i.date=n[0],s=n[1],Rs.timeZoneDelimiter.test(i.date)&&(i.date=t.split(Rs.timeZoneDelimiter)[0],s=t.substr(i.date.length,t.length))),s){const r=Rs.timezone.exec(s);r?(i.time=s.replace(r[1],""),i.timezone=r[1]):i.time=s}return i}function lU(t,i){const n=new RegExp("^(?:(\\d{4}|[+-]\\d{"+(4+i)+"})|(\\d{2}|[+-]\\d{"+(2+i)+"})$)"),s=t.match(n);if(!s)return{year:NaN,restDateString:""};const r=s[1]?parseInt(s[1]):null,c=s[2]?parseInt(s[2]):null;return{year:c===null?r:c*100,restDateString:t.slice((s[1]||s[2]).length)}}function cU(t,i){if(i===null)return new Date(NaN);const n=t.match(nU);if(!n)return new Date(NaN);const s=!!n[4],r=Wn(n[1]),c=Wn(n[2])-1,h=Wn(n[3]),p=Wn(n[4]),m=Wn(n[5])-1;if(s)return gU(i,p,m)?uU(i,p,m):new Date(NaN);{const f=new Date(0);return!mU(i,c,h)||!fU(i,r)?new Date(NaN):(f.setUTCFullYear(i,c,Math.max(r,h)),f)}}function Wn(t){return t?parseInt(t):1}function dU(t){const i=t.match(oU);if(!i)return NaN;const n=Pc(i[1]),s=Pc(i[2]),r=Pc(i[3]);return yU(n,s,r)?n*Zy+s*Xy+r*1e3:NaN}function Pc(t){return t&&parseFloat(t.replace(",","."))||0}function hU(t){if(t==="Z")return 0;const i=t.match(sU);if(!i)return 0;const n=i[1]==="+"?-1:1,s=parseInt(i[2]),r=i[3]&&parseInt(i[3])||0;return wU(s,r)?n*(s*Zy+r*Xy):NaN}function uU(t,i,n){const s=new Date(0);s.setUTCFullYear(t,0,4);const r=s.getUTCDay()||7,c=(i-1)*7+n+1-r;return s.setUTCDate(s.getUTCDate()+c),s}const pU=[31,null,31,30,31,30,31,31,30,31,30,31];function ow(t){return t%400===0||t%4===0&&t%100!==0}function mU(t,i,n){return i>=0&&i<=11&&n>=1&&n<=(pU[i]||(ow(t)?29:28))}function fU(t,i){return i>=1&&i<=(ow(t)?366:365)}function gU(t,i,n){return i>=1&&i<=53&&n>=0&&n<=6}function yU(t,i,n){return t===24?i===0&&n===0:n>=0&&n<60&&i>=0&&i<60&&t>=0&&t<25}function wU(t,i){return i>=0&&i<=59}function vU(t,i,n){return $E(t,-1,n)}const Bd="react-activity-calendar",Dc=8,sw=["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"],bU={months:sw,weekdays:["Sun","Mon","Tue","Wed","Thu","Fri","Sat"],totalCount:"{{count}} activities in {{year}}",legend:{less:"Less",more:"More"}},Df="(prefers-color-scheme: dark)";function kU(){const[t,i]=x.useState(()=>typeof window>"u"?"light":window.matchMedia(Df).matches?"dark":"light"),n=s=>{i(s.matches?"dark":"light")};return x.useEffect(()=>{const s=window.matchMedia(Df);return i(s.matches?"dark":"light"),s.addEventListener("change",n),()=>{s.removeEventListener("change",n)}},[]),t}const rw=`${Bd}--loading-animation`;function xU(t,i){const[n,s]=x.useState(!1);return x.useEffect(()=>{const r=`oklab(from ${t} l a b)`,c=i==="light"?`oklab(from ${t} calc(l * 0.96) a b)`:`oklab(from ${t} calc(l * 1.08) a b)`,h=document.createElement("style");h.innerHTML=`
      @keyframes ${rw} {
        0% {
          fill: ${r};
        }
        50% {
          fill: ${c};
        }
        100% {
          fill: ${r};
        }
      }
    `;const p=()=>{s(!0)};return document.head.appendChild(h),h.addEventListener("load",p),()=>{document.head.removeChild(h),h.removeEventListener("load",p),s(!1)}},[t,i]),n}const Rf="(prefers-reduced-motion: reduce)";function SU(){const[t,i]=x.useState(()=>typeof window>"u"?!0:window.matchMedia(Rf).matches);return x.useEffect(()=>{const n=window.matchMedia(Rf);i(n.matches);const s=r=>{i(r.matches)};return n.addEventListener("change",s),()=>{n.removeEventListener("change",s)}},[]),t}function AU(t,{minLevel:i,maxLevel:n}){if(t.length===0)throw new Error("Activity data must not be empty.");for(const{date:s,level:r}of t){if(!ZE(Wi(s)))throw new Error(`Activity date '${s}' is not a valid ISO 8601 date string.`);if(r<i||r>n)throw new RangeError(`Activity level ${r} for ${s} is out of range. It must be between ${i} and ${n}.`)}}function CU(t,i=0){const n=TU(t),s=n[0],r=Wi(s.date),c=nw(r)===i?r:vU(iU(r,i)),h=[...Array(QE(r,c)).fill(void 0),...n],p=Math.ceil(h.length/7);return Qs(p).map(m=>h.slice(m*7,m*7+7))}function TU(t){const i=new Map(t.map(r=>[r.date,r])),n=t[0],s=t[t.length-1];return aw({start:Wi(n.date),end:Wi(s.date)}).map(r=>{const c=iw(r,{representation:"date"});return i.has(c)?i.get(c):{date:c,count:0,level:0}})}function $a(t){return`${Bd}__${t}`}function IU(){const t=new Date().getFullYear();return aw({start:new Date(t,0,1),end:new Date(t,11,31)}).map(n=>({date:iw(n,{representation:"date"}),count:0,level:0}))}function Qs(t,i){const n=i===void 0?0:t,s=i??t;if(s<=n)throw new RangeError("Invalid range: to must be greater than from");return Array.from({length:s-n},(r,c)=>n+c)}function MU(t,i=sw){return t.reduce((n,s,r)=>{const c=s.find(m=>m!==void 0);if(!c)throw new Error(`Unexpected error: Week ${r+1} is empty.`);const h=i[tU(Wi(c.date))];if(!h){const m=new Date(c.date).toLocaleString("en-US",{month:"short"});throw new Error(`Unexpected error: undefined month label for ${m}.`)}const p=n[n.length-1];return r===0||(p==null?void 0:p.label)!==h?[...n,{weekIndex:r,label:h}]:n},[]).filter(({weekIndex:n},s,r)=>s===0?r[1]&&r[1].weekIndex-n>=3:s===r.length-1?t.slice(n).length>=3:!0)}function PU(t,i,n){if(t.length!==7)throw new Error("Exactly 7 labels, one for each weekday must be passed.");return t.reduce((s,r,c)=>i.byDayIndex(c)?Math.max(s,Math.ceil(DU(r,n).width)):s,0)}function DU(t,i){if(typeof document>"u"||typeof window>"u")return{width:0,height:0};if(i<1)throw new RangeError("fontSize must be positive");if(t.length===0)return{width:0,height:0};const n="http://www.w3.org/2000/svg",s=document.createElementNS(n,"svg");s.style.position="absolute",s.style.visibility="hidden",s.style.fontFamily=window.getComputedStyle(document.body).fontFamily,s.style.fontSize=`${i}px`;const r=document.createElementNS(n,"text");r.textContent=t,s.appendChild(r),document.body.appendChild(s);const c=r.getBBox();return document.body.removeChild(s),{width:c.width,height:c.height}}function RU(t,i){if(!t)return{byDayIndex:()=>!1,shouldShow:!1};if(t===!0)return{byDayIndex:s=>(7+s-i)%7%2!==0,shouldShow:!0};const n=[];for(const s of t){const r=EU[s.toLowerCase()];n[r]=!0}return{byDayIndex:s=>n[s]??!1,shouldShow:t.length>0}}const EU={sun:0,mon:1,tue:2,wed:3,thu:4,fri:5,sat:6};function UU(t,i={minLevel:0,maxLevel:4}){const n=qU(i);if(t){const s=i.maxLevel-i.minLevel+1;return OU(t,s),t.light=t.light??n.light,t.dark=t.dark??n.dark,{light:Ef(t.light)?Li([t.light[1],t.light[0],t.light[1]],i):Uf(t.light)?Li(t.light,i):t.light,dark:Ef(t.dark)?Li([t.dark[1],t.dark[0],t.dark[1]],i):Uf(t.dark)?Li(t.dark,i):t.dark}}return n}function qU(t){return{light:Li(["hsl(0, 0%, 26%)","hsl(0, 0%, 92%)","hsl(0, 0%, 26%)"],t),dark:Li(["hsl(0, 0%, 92%)","hsl(0, 0%, 22%)","hsl(0, 0%, 92%)"],t)}}function OU(t,i){const n="The number of colors must match the number of activity levels controlled by the `minLevel` and `maxLevel` props.";if(typeof t!="object"||t.light===void 0&&t.dark===void 0)throw new Error(`The theme object must contain at least one of the fields "light" and "dark" with exactly 2 or ${i} colors respectively. ${n}`);if(t.light){const{length:s}=t.light;if(s!==2&&s!==3&&s!==i)throw new Error(`theme.light must contain exactly 2 or 3 or ${i} colors, ${s} passed. ${n}`);for(const r of t.light)if(typeof window<"u"&&!CSS.supports("color",r))throw new Error(`Invalid color "${r}" passed. All CSS color formats are accepted.`)}if(t.dark){const{length:s}=t.dark;if(s!==2&&s!==3&&s!==i)throw new Error(`theme.dark must contain exactly 2 or 3 or ${i} colors, ${s} passed. ${n}`);for(const r of t.dark)if(typeof window<"u"&&!CSS.supports("color",r))throw new Error(`Invalid color "${r}" passed. All CSS color formats are accepted.`)}}function Li([t,i,n],{minLevel:s,maxLevel:r}){return Qs(s,r+1).map(c=>{if(c<0){if(c===s)return t;const p=(1-c/s)*100;return`color-mix(in oklab, ${i} ${parseFloat(p.toFixed(2))}%, ${t})`}if(c===0)return i;if(c===r)return n;const h=c/r*100;return`color-mix(in oklab, ${n} ${parseFloat(h.toFixed(2))}%, ${i})`})}function Ef(t){return t.length===2}function Uf(t){return t.length===3}const Xa={container:t=>({width:"max-content",maxWidth:"100%",display:"flex",flexDirection:"column",gap:"8px",fontSize:`${t}px`}),scrollContainer:t=>({maxWidth:"100%",overflowX:"auto",overflowY:"hidden",paddingTop:Math.ceil(.1*t)}),calendar:{display:"block",overflow:"visible"},rect:t=>({stroke:t==="light"?"rgba(0, 0, 0, 0.08)":"rgba(255, 255, 255, 0.04)"}),footer:{container:{display:"flex",flexWrap:"wrap",gap:"4px 16px",whiteSpace:"nowrap"},legend:{marginLeft:"auto",display:"flex",alignItems:"center",gap:"3px"}}},qf=x.lazy(()=>zy(()=>import("./Tooltip-D5idU7TC-C_YCbUBQ.js"),[]).then(t=>({default:t.Tooltip}))),$c=x.forwardRef(({data:t,blockMargin:i=4,blockRadius:n=2,blockSize:s=12,className:r,colorScheme:c,fontSize:h=14,labels:p,loading:m=!1,minLevel:f=0,maxLevel:y=4,renderBlock:v,renderColorLegend:b,showColorLegend:A=!0,showMonthLabels:T=!0,showTotalCount:I=!0,showWeekdayLabels:M=!1,style:R={},theme:O,tooltips:q={},weekStart:z=0},V)=>{const[K,ie]=x.useState(!1);if(x.useEffect(()=>{ie(!0)},[]),f>=y)throw new RangeError(`Minimum activity level must be less than maximum level. Got ${f} and ${y}.`);const J={minLevel:f,maxLevel:y},ce=UU(O,J),we=kU(),Te=c??we,Ke=ce[Te],je=xU(Ke[0],Te),Ve=!SU();if(m){if(Ve&&!je)return null;t=IU()}AU(t,J);const Ee=t[0],_e=aU(Wi(Ee.date)),oe=CU(t,z),W=Object.assign({},bU,p),Z=T?h+Dc:0,H=RU(M,z),P=K&&H.shouldShow?PU(W.weekdays,H,h)+Dc:void 0;function F(){return{width:oe.length*(s+i)-i,height:Z+(s+i)*7-i}}function se(){return oe.map((ve,Ie)=>ve.map((nt,Rt)=>{if(!nt)return null;const Ji=m&&Ve?{animation:`${rw} 1.75s ease-in-out infinite`,animationDelay:`${Ie*20+Rt*20}ms`}:void 0,Yi=g.jsx("rect",{x:0,y:Z+(s+i)*Rt,width:s,height:s,rx:n,ry:n,fill:ge(nt.level),"data-date":nt.date,"data-level":nt.level,style:{...Xa.rect(Te),...Ji}}),oi=v?v(Yi,nt):Yi;return g.jsx(x.Fragment,{children:q.activity?g.jsx(x.Suspense,{fallback:oi,children:g.jsx(qf,{text:q.activity.text(nt),colorScheme:Te,placement:q.activity.placement??"top",hoverRestMs:q.activity.hoverRestMs,offset:q.activity.offset,transitionStyles:q.activity.transitionStyles,withArrow:q.activity.withArrow,children:oi})}):oi},nt.date)})).map((ve,Ie)=>g.jsx("g",{transform:`translate(${(s+i)*Ie}, 0)`,children:ve},Ie))}function re(){if(!I&&!A)return null;const ve=t.reduce((Ie,nt)=>Ie+nt.count,0);return g.jsxs("footer",{className:$a("footer"),style:{...Xa.footer.container,marginLeft:P},children:[m&&g.jsx("div",{children:" "}),!m&&I&&g.jsx("div",{className:$a("count"),children:W.totalCount?W.totalCount.replace("{{count}}",String(ve)).replace("{{year}}",String(_e)):`${ve} activities in ${_e}`}),!m&&A&&g.jsxs("div",{className:$a("legend-colors"),style:Xa.footer.legend,children:[W.legend.less&&g.jsx("span",{style:{marginRight:"0.4em"},children:W.legend.less}),Qs(f,y+1).map(Ie=>{const nt=g.jsx("svg",{width:s,height:s,children:g.jsx("rect",{width:s,height:s,fill:ge(Ie),rx:n,ry:n,style:Xa.rect(Te)})},Ie),Rt=b?b(nt,Ie):nt;return g.jsx(x.Fragment,{children:q.colorLegend?g.jsx(x.Suspense,{fallback:Rt,children:g.jsx(qf,{text:q.colorLegend.text(Ie),colorScheme:Te,placement:q.colorLegend.placement??"bottom",hoverRestMs:q.colorLegend.hoverRestMs,offset:q.colorLegend.offset,transitionStyles:q.colorLegend.transitionStyles,withArrow:q.colorLegend.withArrow,children:Rt})}):Rt},Ie)}),W.legend.more&&g.jsx("span",{style:{marginLeft:"0.4em"},children:W.legend.more})]})]})}function de(){return H.shouldShow?g.jsx("g",{className:$a("legend-weekday"),children:Qs(7).map(ve=>{const Ie=(ve+z)%7;return H.byDayIndex(Ie)?g.jsx("text",{x:-Dc,y:Z+(s+i)*ve+s/2,dominantBaseline:"central",textAnchor:"end",fill:"currentColor",children:W.weekdays[Ie]},ve):null})}):null}function he(){return T?g.jsx("g",{className:$a("legend-month"),children:MU(oe,W.months).map(({label:ve,weekIndex:Ie})=>g.jsx("text",{x:(s+i)*Ie,y:0,dominantBaseline:"hanging",fill:"currentColor",children:ve},Ie))}):null}function ge(ve){return Ke[ve-f]}const{width:pe,height:be}=F();return g.jsxs("article",{ref:V,className:`${Bd} ${r??""}`.trim(),style:{...R,...Xa.container(h)},children:[g.jsx("div",{className:$a("scroll-container"),style:Xa.scrollContainer(h),children:g.jsxs("svg",{width:pe,height:be,viewBox:`0 0 ${pe} ${be}`,className:$a("calendar"),style:{...Xa.calendar,marginLeft:P},children:[!m&&de(),!m&&he(),se()]})}),re()]})});$c.displayName="ActivityCalendar";const FU={light:["#ebedf0","#9be9a8","#40c463","#30a14e","#216e39"],dark:["#161b22","#0e4429","#006d32","#26a641","#39d353"]},LU=(t,i)=>{if(typeof i!="function")return t;const n=i(t);if(!Array.isArray(n))throw Error("transformData() function must return a list of Activity objects.");for(const s of n){if(!jU(s))throw Error("transformData() must return a list of valid Activity objects.");if(typeof s.count!="number"||s.count<0)throw Error(`Required property "count: number" missing or invalid. Got: ${s.count}`);if(typeof s.date!="string"||!/\d{4}-\d{2}-\d{2}/.test(s.date))throw Error(`Required property "date: YYYY-MM-DD" missing or invalid. Got: ${s.date}`);if(typeof s.level!="number"||s.level<0||s.level>4)throw Error(`Required property "level: 0 | 1 | 2 | 3 | 4" missing or invalid: Got: ${s.level}.`)}return n},jU=t=>Object.prototype.toString.call(t)==="[object Object]";async function VU(t,i){const s=await fetch(`https://github-contributions-api.jogruber.de/v4/${t}?y=${String(i)}`),r=await s.json();if(!s.ok)throw Error(`Fetching GitHub contribution data for "${t}" failed: ${r.error}`);return r}const lw=x.forwardRef(({username:t,year:i="last",labels:n,transformData:s,throwOnError:r=!1,errorMessage:c=`Error – Fetching GitHub contribution data for "${t}" failed.`,...h},p)=>{const[m,f]=x.useState(null),[y,v]=x.useState(!1),[b,A]=x.useState(null),T=x.useCallback(()=>{v(!0),A(null),VU(t,i).then(f).catch(R=>{R instanceof Error&&A(R)}).finally(()=>{v(!1)})},[t,i]);if(x.useEffect(T,[T]),b){if(r)throw b;return g.jsx("div",{children:c})}if(y||!m)return g.jsx($c,{...h,data:[],loading:!0});const I=h.theme??FU,M={totalCount:`{{count}} contributions in ${i==="last"?"the last year":"{{year}}"}`};return g.jsx($c,{data:LU(m.contributions,s),labels:Object.assign({},M,n),ref:p,...h,theme:I,loading:!!h.loading||y,maxLevel:4})});lw.displayName="GitHubCalendar";function NU(){return g.jsxs("section",{id:"about",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx(Gi,{index:"01",title:"About",accent:"me"}),g.jsxs("div",{className:"grid gap-14 md:grid-cols-[1.4fr_1fr] md:gap-16",children:[g.jsxs("div",{children:[Sf.paragraphs.map((t,i)=>g.jsx(De,{delay:i*.08,children:g.jsx("p",{className:"mb-6 max-w-[68ch] text-base leading-relaxed text-body md:text-lg",children:t})},i)),g.jsx(De,{delay:.25,children:g.jsxs("blockquote",{className:"mt-8 border-l-2 border-violet/60 pl-5 font-display text-lg italic text-bright/80",children:["“",Sf.quote,"”"]})})]}),g.jsx(De,{delay:.15,children:g.jsxs("div",{className:"glow-card glass relative mx-auto aspect-square w-full max-w-sm overflow-hidden rounded-3xl",children:[g.jsx(cr,{src:mt.photo,alt:mt.name,className:"grayscale-[20%] transition-all duration-500 hover:grayscale-0"}),g.jsx("div",{className:"absolute inset-0 bg-gradient-to-t from-ink/70 via-transparent to-transparent"}),g.jsxs("div",{className:"absolute bottom-4 left-5",children:[g.jsx("div",{className:"font-display text-lg font-bold text-bright",children:mt.shortName}),g.jsx("div",{className:"text-sm text-body",children:mt.location})]})]})})]}),g.jsxs("div",{className:"mt-24",children:[g.jsx(De,{children:g.jsxs("h3",{className:"font-display mb-8 text-2xl font-bold text-bright",children:["Tech I ",g.jsx("span",{className:"text-gradient",children:"ship with"})]})}),g.jsx(De,{children:g.jsxs("div",{className:"glow-card glass rounded-3xl border-violet/25 p-7 md:p-9",children:[g.jsxs("div",{className:"flex flex-wrap items-baseline justify-between gap-2",children:[g.jsx("div",{className:"font-display text-lg font-bold uppercase tracking-[0.15em] text-gradient md:text-xl",children:Ps.primary.title}),g.jsx("span",{className:"text-sm text-body/70",children:Ps.primary.subtitle})]}),g.jsx("div",{className:"mt-5 flex flex-wrap gap-2.5",children:Ps.primary.items.map(t=>g.jsx(Ni,{className:"!border-violet/30 !bg-violet/[0.08] !px-4 !py-1.5 !text-sm !text-bright/90",children:t},t))})]})}),g.jsx("div",{className:"mt-6 grid gap-6 md:grid-cols-2",children:Ps.secondary.map((t,i)=>g.jsx(De,{delay:i*.1,children:g.jsxs("div",{className:"glow-card glass h-full rounded-2xl p-6 opacity-90",children:[g.jsx("div",{className:"font-display mb-4 text-sm font-semibold uppercase tracking-[0.18em] text-body",children:t.title}),g.jsx("div",{className:"flex flex-wrap gap-2",children:t.items.map(n=>g.jsx(Ni,{children:n},n))})]})},t.title))})]}),g.jsxs("div",{className:"mt-20",children:[g.jsx(De,{children:g.jsxs("h3",{className:"font-display mb-8 text-2xl font-bold text-bright",children:["Competitive ",g.jsx("span",{className:"text-gradient",children:"profiles"})]})}),g.jsx("div",{className:"grid grid-cols-2 gap-4 md:grid-cols-4",children:AE.map((t,i)=>g.jsx(De,{delay:i*.07,children:g.jsxs("a",{href:t.url,target:"_blank",rel:"noopener noreferrer",className:"glow-card glass group flex h-full flex-col justify-between rounded-2xl p-5",children:[g.jsxs("div",{className:"flex items-start justify-between",children:[g.jsx("span",{className:"font-display font-bold text-bright",children:t.name}),g.jsx(hr,{className:"text-body/50 transition-colors group-hover:text-violet",size:16})]}),g.jsx("span",{className:"mt-3 text-sm text-body",children:t.detail})]})},t.name))})]}),g.jsxs("div",{className:"mt-20",children:[g.jsx(De,{children:g.jsxs("div",{className:"mb-8 flex flex-wrap items-center justify-between gap-4",children:[g.jsxs("h3",{className:"font-display text-2xl font-bold text-bright",children:["Open-source ",g.jsx("span",{className:"text-gradient",children:"pulse"})]}),g.jsxs("a",{href:Ic.url,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-2 text-sm font-medium text-body transition-colors hover:text-violet",children:[g.jsx(dr,{size:16})," @",Ic.username]})]})}),g.jsx(De,{delay:.1,children:g.jsx("div",{className:"gh-cal glow-card glass overflow-hidden rounded-2xl p-6 text-body md:p-8",children:g.jsx(lw,{username:Ic.username,colorScheme:"dark",blockSize:11,blockMargin:4,fontSize:13,theme:{dark:["rgba(255,255,255,0.05)","#3b2b63","#6d4fae","#a78bfa","#e0d4ff"]}})})})]})]})}function WU({exp:t,index:i}){const[n,s]=x.useState(!1),r=t.highlights.length>0;return g.jsxs(De,{delay:.05,className:"relative pl-10 md:pl-16",children:[g.jsx("span",{className:`absolute left-[7px] top-7 h-3.5 w-3.5 -translate-x-1/2 rounded-full border-2 md:left-[15px] ${t.current?"border-emerald bg-emerald/30 shadow-[0_0_12px_rgba(110,231,183,0.8)]":"border-violet/70 bg-ink"}`}),g.jsxs("div",{className:"glow-card glass mb-8 rounded-2xl p-6 md:p-8",children:[g.jsxs("div",{className:"flex flex-wrap items-start justify-between gap-3",children:[g.jsxs("div",{children:[g.jsx("h3",{className:"font-display text-xl font-bold text-bright",children:t.role}),g.jsxs("div",{className:"mt-1 flex flex-wrap items-center gap-3",children:[g.jsx("span",{className:"font-medium text-violet",children:t.company}),t.current&&g.jsxs("span",{className:"inline-flex items-center gap-1.5 rounded-full bg-emerald/10 px-2.5 py-0.5 text-xs font-semibold text-emerald",children:[g.jsx("span",{className:"h-1.5 w-1.5 rounded-full bg-emerald"})," Now"]})]})]}),g.jsx("span",{className:"font-display text-sm text-body/70",children:t.period})]}),g.jsx("p",{className:"mt-4 leading-relaxed text-body",children:t.summary}),t.tech.length>0&&g.jsx("div",{className:"mt-4 flex flex-wrap gap-2",children:t.tech.map(c=>g.jsx(Ni,{children:c},c))}),g.jsx(cd,{initial:!1,children:n&&g.jsx(He.ul,{initial:{height:0,opacity:0},animate:{height:"auto",opacity:1},exit:{height:0,opacity:0},transition:{duration:.35,ease:"easeInOut"},className:"overflow-hidden",children:t.highlights.map((c,h)=>g.jsxs("li",{className:"mt-3 flex gap-3 text-sm leading-relaxed text-body first:mt-5",children:[g.jsx("span",{className:"mt-[7px] h-1.5 w-1.5 shrink-0 rounded-full bg-gradient-to-r from-violet to-fuchsia"}),c]},h))})}),(r||t.links.length>0)&&g.jsxs("div",{className:"mt-5 flex flex-wrap items-center gap-4",children:[r&&g.jsxs("button",{onClick:()=>s(!n),className:"inline-flex items-center gap-1.5 text-sm font-semibold text-violet transition-colors hover:text-fuchsia",children:[n?"Show less":"Deep dive",g.jsx(NE,{className:`transition-transform ${n?"rotate-180":""}`})]}),t.links.map(c=>g.jsxs("a",{href:c.url,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-1.5 text-sm text-body transition-colors hover:text-bright",children:[c.text," ",g.jsx(hr,{size:13})]},c.url))]})]})]})}function BU(){const t=x.useRef(null),{scrollYProgress:i}=eE({target:t,offset:["start 60%","end 75%"]}),n=no(i,{stiffness:90,damping:25});return g.jsxs("section",{id:"experience",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx(Gi,{index:"02",title:"Where I've",accent:"worked"}),g.jsxs("div",{ref:t,className:"relative",children:[g.jsx("div",{className:"absolute bottom-0 left-[7px] top-0 w-px bg-line md:left-[15px]"}),g.jsx(He.div,{style:{scaleY:n},className:"absolute bottom-0 left-[7px] top-0 w-px origin-top bg-gradient-to-b from-violet via-fuchsia to-cyan md:left-[15px]"}),CE.map((s,r)=>g.jsx(WU,{exp:s,index:r},`${s.company}-${s.role}`))]}),g.jsxs("div",{className:"mt-16",children:[g.jsx(De,{children:g.jsxs("h3",{className:"font-display mb-8 text-2xl font-bold text-bright",children:["Leadership & ",g.jsx("span",{className:"text-gradient",children:"community"})]})}),g.jsx("div",{className:"grid gap-4 sm:grid-cols-2",children:TE.map((s,r)=>g.jsx(De,{delay:r*.06,children:g.jsxs("div",{className:"glow-card glass h-full rounded-2xl p-5",children:[g.jsxs("div",{className:"flex items-start justify-between gap-3",children:[g.jsx("span",{className:"font-display font-semibold text-bright",children:s.role}),g.jsx("span",{className:"shrink-0 text-xs text-body/60",children:s.period})]}),g.jsx("div",{className:"mt-1 text-sm text-violet/90",children:s.org}),g.jsx("p",{className:"mt-2 text-sm leading-relaxed text-body",children:s.note})]})},s.role+s.org))})]})]})}function zU(t){return ft({attr:{viewBox:"0 0 512 512"},child:[{tag:"path",attr:{d:"M48 59.49v393a4.33 4.33 0 0 0 7.37 3.07L260 256 55.37 56.42A4.33 4.33 0 0 0 48 59.49M345.8 174 89.22 32.64l-.16-.09c-4.42-2.4-8.62 3.58-5 7.06l201.13 192.32zM84.08 472.39c-3.64 3.48.56 9.46 5 7.06l.16-.09L345.8 338l-60.61-57.95zM449.38 231l-71.65-39.46L310.36 256l67.37 64.43L449.38 281c19.49-10.77 19.49-39.23 0-50"},child:[]}]})(t)}const cw=t=>t==="github"?g.jsx(dr,{size:15}):t==="playstore"?g.jsx(zU,{size:15}):t==="prs"?g.jsx(jE,{size:15}):g.jsx(hr,{size:15}),HU={violet:"text-violet",cyan:"text-cyan",emerald:"text-emerald",amber:"text-amber"};function KU({p:t}){var i;return g.jsx(De,{children:g.jsx(oE,{max:3,className:"glow-card glass h-full overflow-hidden rounded-3xl",children:g.jsxs("div",{className:"flex h-full flex-col",children:[g.jsxs("div",{className:"relative aspect-video w-full",children:[g.jsx(cr,{src:t.image,alt:t.title,label:`${t.title} banner`,accent:t.accent}),g.jsx("div",{className:"absolute inset-0 bg-gradient-to-t from-ink/30 to-transparent"})]}),g.jsxs("div",{className:"flex flex-1 flex-col p-7 md:p-9",children:[g.jsxs("div",{className:"flex flex-wrap items-center gap-3",children:[g.jsx("h3",{className:"font-display text-2xl font-bold text-bright md:text-3xl",children:t.title}),t.badge&&g.jsx("span",{className:"rounded-full border border-emerald/40 bg-emerald/10 px-3 py-1 text-[11px] font-semibold uppercase tracking-wide text-emerald",children:t.badge})]}),g.jsx("p",{className:`font-display mt-2 text-sm font-semibold uppercase tracking-[0.14em] ${HU[t.accent]||"text-violet"}`,children:t.tagline}),g.jsx("p",{className:"mt-4 leading-relaxed text-body",children:t.description}),((i=t.metrics)==null?void 0:i.length)>0&&g.jsx("div",{className:"mt-6 flex flex-wrap gap-8",children:t.metrics.map(n=>g.jsxs("div",{children:[g.jsx("div",{className:"font-display text-2xl font-bold text-bright",children:n.value}),g.jsx("div",{className:"text-xs text-body/70",children:n.label})]},n.label))}),g.jsx("div",{className:"mt-6 flex flex-wrap gap-2",children:t.tech.map(n=>g.jsx(Ni,{children:n},n))}),g.jsx("div",{className:"mt-7 flex flex-wrap gap-3",children:t.links.map(n=>g.jsxs("a",{href:n.url,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-2 rounded-full border border-line px-4 py-2 text-sm font-medium text-bright transition-all hover:border-violet/60 hover:bg-violet/10",children:[cw(n.kind)," ",n.text]},n.url))})]})]})})})}function _U(){return g.jsxs("section",{id:"projects",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx("div",{className:"blob right-[-15%] top-[10%] h-[420px] w-[420px] bg-violet-600/15"}),g.jsx(Gi,{index:"03",title:"Selected",accent:"work"}),g.jsx("div",{className:"relative grid gap-6 md:-mx-4 md:gap-8 xl:-mx-10 xl:grid-cols-2",children:IE.map(t=>g.jsx(KU,{p:t},t.title))}),g.jsxs("div",{className:"mt-24",children:[g.jsx(De,{children:g.jsxs("h3",{className:"font-display mb-8 text-2xl font-bold text-bright",children:["More ",g.jsx("span",{className:"text-gradient",children:"builds"})]})}),g.jsx("div",{className:"grid gap-5 sm:grid-cols-2 lg:grid-cols-4",children:ME.map((t,i)=>g.jsx(De,{delay:i%4*.06,children:g.jsxs("div",{className:"glow-card glass group flex h-full flex-col overflow-hidden rounded-2xl",children:[g.jsxs("div",{className:"relative aspect-video overflow-hidden",children:[g.jsx(cr,{src:t.image,alt:t.title,label:t.title,className:"transition-transform duration-500 group-hover:scale-105"}),g.jsx("div",{className:"absolute inset-0 bg-gradient-to-t from-ink/60 to-transparent"})]}),g.jsxs("div",{className:"flex flex-1 flex-col p-5",children:[g.jsx("h4",{className:"font-display font-bold text-bright",children:t.title}),g.jsx("p",{className:"mt-2 flex-1 text-sm leading-relaxed text-body",children:t.description}),g.jsx("div",{className:"mt-3 flex flex-wrap gap-1.5",children:t.tech.map(n=>g.jsx(Ni,{className:"!px-2.5 !py-0.5 !text-[11px]",children:n},n))}),g.jsx("div",{className:"mt-4 flex flex-wrap gap-3 border-t border-line pt-3",children:t.links.map(n=>g.jsxs("a",{href:n.url,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-1.5 text-xs font-medium text-body transition-colors hover:text-violet",children:[cw(n.kind)," ",n.text]},n.url))})]})]})},t.title))})]})]})}const Of={"Open Source":"text-violet border-violet/40 bg-violet/10",Mentorship:"text-fuchsia border-fuchsia/40 bg-fuchsia/10",DSA:"text-cyan border-cyan/40 bg-cyan/10",Academics:"text-emerald border-emerald/40 bg-emerald/10"};function GU(){return g.jsxs("section",{id:"achievements",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx(Gi,{index:"04",title:"Trophy",accent:"wall"}),g.jsx("div",{className:"grid gap-4 sm:grid-cols-2 lg:grid-cols-3",children:PE.map((t,i)=>g.jsxs(He.div,{initial:{opacity:0,y:24,scale:.97},whileInView:{opacity:1,y:0,scale:1},viewport:{once:!0,margin:"-40px"},transition:{duration:.55,delay:i%3*.08,ease:[.21,.65,.32,.99]},className:"glow-card glass flex h-full flex-col rounded-2xl p-6",children:[g.jsxs("div",{className:"flex items-start justify-between",children:[g.jsx(WE,{className:"text-violet",size:22}),g.jsx("span",{className:`rounded-full border px-2.5 py-0.5 text-[11px] font-semibold ${Of[t.tag]||Of["Open Source"]}`,children:t.tag})]}),g.jsx("h3",{className:"font-display mt-4 text-lg font-bold text-bright",children:t.title}),g.jsx("p",{className:"mt-2 text-sm leading-relaxed text-body",children:t.detail})]},t.title))})]})}function JU(t){return ft({attr:{viewBox:"0 0 640 512"},child:[{tag:"path",attr:{d:"M180.5,74.262C80.813,74.262,0,155.633,0,256S80.819,437.738,180.5,437.738,361,356.373,361,256,280.191,74.262,180.5,74.262Zm288.25,10.646c-49.845,0-90.245,76.619-90.245,171.095s40.406,171.1,90.251,171.1,90.251-76.619,90.251-171.1H559C559,161.5,518.6,84.908,468.752,84.908Zm139.506,17.821c-17.526,0-31.735,68.628-31.735,153.274s14.2,153.274,31.735,153.274S640,340.631,640,256C640,171.351,625.785,102.729,608.258,102.729Z"},child:[]}]})(t)}function YU(){return g.jsxs("section",{id:"writing",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx(Gi,{index:"05",title:"Things I've",accent:"written"}),g.jsx("div",{className:"grid gap-5 md:grid-cols-2",children:Af.posts.map((t,i)=>g.jsx(De,{delay:i%2*.08,children:g.jsxs("a",{href:t.url,target:"_blank",rel:"noopener noreferrer",className:"glow-card glass group flex h-full flex-col rounded-2xl p-6 md:p-7",children:[g.jsxs("div",{className:"flex items-start justify-between gap-4",children:[g.jsx("span",{className:"font-display text-xs font-semibold uppercase tracking-[0.2em] text-body/60",children:t.date}),g.jsx(BE,{size:20,className:"shrink-0 text-body/40 transition-all group-hover:-translate-y-0.5 group-hover:translate-x-0.5 group-hover:text-violet"})]}),g.jsx("h3",{className:"font-display mt-3 flex-1 text-lg font-bold leading-snug text-bright transition-colors group-hover:text-gradient md:text-xl",children:t.title}),g.jsx("div",{className:"mt-4 flex flex-wrap gap-2",children:t.tags.map(n=>g.jsx(Ni,{children:n},n))})]})},t.url))}),g.jsx(De,{delay:.2,children:g.jsx("div",{className:"mt-10 flex justify-center",children:g.jsxs("a",{href:Af.profileUrl,target:"_blank",rel:"noopener noreferrer",className:"glass inline-flex items-center gap-2.5 rounded-full px-6 py-3 text-sm font-semibold text-bright transition-all hover:border-violet/50 hover:shadow-[0_0_24px_-8px_rgba(167,139,250,0.7)]",children:[g.jsx(JU,{size:18})," Read all on Medium"]})})})]})}function QU(){return g.jsxs("section",{className:"relative w-full px-5 py-20 md:px-10 md:py-28 xl:px-16",children:[g.jsx("div",{className:"blob left-[-20%] top-[0%] h-[360px] w-[360px] bg-fuchsia-600/12"}),g.jsx(De,{children:g.jsxs("figure",{className:"glow-card glass relative rounded-3xl p-8 md:p-12",children:[g.jsx("span",{className:"font-display absolute -top-7 left-8 text-[110px] leading-none text-gradient select-none",children:"“"}),g.jsx("blockquote",{className:"relative text-lg leading-relaxed text-bright/90 md:text-xl",children:Ds.quote}),g.jsxs("figcaption",{className:"mt-8 flex flex-wrap items-center justify-between gap-4 border-t border-line pt-6",children:[g.jsxs("div",{children:[g.jsx("div",{className:"font-display font-bold text-bright",children:Ds.author}),g.jsx("div",{className:"text-sm text-body",children:Ds.title})]}),g.jsxs("a",{href:Ds.proofUrl,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-2 text-sm font-medium text-violet transition-colors hover:text-fuchsia",children:["View on LinkedIn ",g.jsx(hr,{size:14})]})]})]})})]})}const $U={github:dr,linkedin:Jy,instagram:Yy,mail:Gy};function XU(){return g.jsxs("section",{id:"contact",className:"relative w-full px-5 py-28 md:px-10 md:py-40 xl:px-16",children:[g.jsx("div",{className:"blob bottom-[-10%] right-[10%] h-[400px] w-[400px] bg-violet-600/20"}),g.jsx(Gi,{index:"06",title:"Let's build",accent:"together"}),g.jsx(De,{children:g.jsxs("p",{className:"max-w-2xl text-lg leading-relaxed text-body md:text-xl",children:[g.jsxs("span",{className:"mb-4 flex items-center gap-2.5 text-emerald",children:[g.jsx("span",{className:"pulse-dot h-2 w-2 rounded-full bg-emerald"}),g.jsx("span",{className:"text-sm font-semibold uppercase tracking-[0.18em]",children:"Open to opportunities"})]}),"Whether it's a production Android app, a Kotlin Multiplatform migration, or an open-source collaboration — my inbox is always open."]})}),g.jsx(De,{delay:.15,children:g.jsx(Qc,{strength:.15,className:"mt-10 inline-block",children:g.jsx("a",{href:`mailto:${mt.email}`,className:"font-display block break-all text-3xl font-bold text-bright underline decoration-violet/50 decoration-2 underline-offset-8 transition-all hover:text-gradient hover:decoration-fuchsia sm:text-4xl md:text-6xl",children:mt.email})})}),g.jsx(De,{delay:.25,children:g.jsx("div",{className:"mt-12 flex flex-wrap gap-3",children:Hy.map(t=>{const i=$U[t.icon];return g.jsxs("a",{href:t.url,target:"_blank",rel:"noopener noreferrer",className:"glass inline-flex items-center gap-2.5 rounded-full px-5 py-3 text-sm font-medium text-bright transition-all hover:border-violet/50 hover:shadow-[0_0_24px_-8px_rgba(167,139,250,0.7)]",children:[g.jsx(i,{size:17})," ",t.name]},t.name)})})})]})}function ZU(){return g.jsx("footer",{className:"border-t border-line",children:g.jsxs("div",{className:"flex w-full flex-wrap items-center justify-between gap-4 px-5 py-8 md:px-10 xl:px-16",children:[g.jsxs("div",{className:"flex items-center gap-3",children:[g.jsx(By,{size:30}),g.jsxs("span",{className:"text-sm text-body",children:["Designed & built by ",g.jsx("span",{className:"font-medium text-bright",children:mt.name})]})]}),g.jsxs("span",{className:"text-xs text-body/60",children:["© ",new Date().getFullYear()," · Kotlin at heart, React when it counts."]})]})})}function eq(){return x.useEffect(()=>{const t=new sx({lerp:.12,smoothWheel:!0});let i;function n(r){t.raf(r),i=requestAnimationFrame(n)}i=requestAnimationFrame(n);function s(r){const c=r.target.closest('a[href^="#"]');if(!c)return;const h=document.querySelector(c.getAttribute("href"));h&&(r.preventDefault(),t.scrollTo(h,{offset:-72}))}return document.addEventListener("click",s),()=>{cancelAnimationFrame(i),document.removeEventListener("click",s),t.destroy()}},[]),g.jsx(KE,{children:g.jsxs("div",{className:"noise relative min-h-screen bg-ink text-body",children:[g.jsx(_E,{}),g.jsxs("main",{children:[g.jsx(JE,{}),g.jsx(NU,{}),g.jsx(BU,{}),g.jsx(_U,{}),g.jsx(GU,{}),g.jsx(YU,{}),g.jsx(QU,{}),g.jsx(XU,{})]}),g.jsx(ZU,{})]})})}function tq(){return g.jsx(Bk,{children:g.jsxs(wk,{children:[g.jsx(Bn,{path:"/",element:g.jsx(eq,{})}),g.jsx(Bn,{path:"/interview_preparation",element:g.jsx(GT,{})}),g.jsx(Bn,{path:"/interview_preparation/:categoryId/:topicId",element:g.jsx(aI,{})}),g.jsx(Bn,{path:"*",element:g.jsx(ag,{to:"/",replace:!0})})]})})}wb.createRoot(document.getElementById("root")).render(g.jsx(Dt.StrictMode,{children:g.jsx(tq,{})}));export{iq as R,fb as a,$a as b,Ff as g,g as j,x as r};
