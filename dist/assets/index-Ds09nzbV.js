var sb=Object.defineProperty;var rb=(t,a,i)=>a in t?sb(t,a,{enumerable:!0,configurable:!0,writable:!0,value:i}):t[a]=i;var X=(t,a,i)=>rb(t,typeof a!="symbol"?a+"":a,i);function lb(t,a){for(var i=0;i<a.length;i++){const s=a[i];if(typeof s!="string"&&!Array.isArray(s)){for(const r in s)if(r!=="default"&&!(r in t)){const c=Object.getOwnPropertyDescriptor(s,r);c&&Object.defineProperty(t,r,c.get?c:{enumerable:!0,get:()=>s[r]})}}}return Object.freeze(Object.defineProperty(t,Symbol.toStringTag,{value:"Module"}))}(function(){const a=document.createElement("link").relList;if(a&&a.supports&&a.supports("modulepreload"))return;for(const r of document.querySelectorAll('link[rel="modulepreload"]'))s(r);new MutationObserver(r=>{for(const c of r)if(c.type==="childList")for(const h of c.addedNodes)h.tagName==="LINK"&&h.rel==="modulepreload"&&s(h)}).observe(document,{childList:!0,subtree:!0});function i(r){const c={};return r.integrity&&(c.integrity=r.integrity),r.referrerPolicy&&(c.referrerPolicy=r.referrerPolicy),r.crossOrigin==="use-credentials"?c.credentials="include":r.crossOrigin==="anonymous"?c.credentials="omit":c.credentials="same-origin",c}function s(r){if(r.ep)return;r.ep=!0;const c=i(r);fetch(r.href,c)}})();function Of(t){return t&&t.__esModule&&Object.prototype.hasOwnProperty.call(t,"default")?t.default:t}var ic={exports:{}},Oi={},oc={exports:{}},le={};/**
 * @license React
 * react.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Vp;function cb(){if(Vp)return le;Vp=1;var t=Symbol.for("react.element"),a=Symbol.for("react.portal"),i=Symbol.for("react.fragment"),s=Symbol.for("react.strict_mode"),r=Symbol.for("react.profiler"),c=Symbol.for("react.provider"),h=Symbol.for("react.context"),p=Symbol.for("react.forward_ref"),m=Symbol.for("react.suspense"),f=Symbol.for("react.memo"),y=Symbol.for("react.lazy"),v=Symbol.iterator;function b(E){return E===null||typeof E!="object"?null:(E=v&&E[v]||E["@@iterator"],typeof E=="function"?E:null)}var T={isMounted:function(){return!1},enqueueForceUpdate:function(){},enqueueReplaceState:function(){},enqueueSetState:function(){}},M=Object.assign,I={};function A(E,O,se){this.props=E,this.context=O,this.refs=I,this.updater=se||T}A.prototype.isReactComponent={},A.prototype.setState=function(E,O){if(typeof E!="object"&&typeof E!="function"&&E!=null)throw Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");this.updater.enqueueSetState(this,E,O,"setState")},A.prototype.forceUpdate=function(E){this.updater.enqueueForceUpdate(this,E,"forceUpdate")};function R(){}R.prototype=A.prototype;function j(E,O,se){this.props=E,this.context=O,this.refs=I,this.updater=se||T}var V=j.prototype=new R;V.constructor=j,M(V,A.prototype),V.isPureReactComponent=!0;var B=Array.isArray,N=Object.prototype.hasOwnProperty,_={current:null},ae={key:!0,ref:!0,__self:!0,__source:!0};function G(E,O,se){var re,de={},he=null,ge=null;if(O!=null)for(re in O.ref!==void 0&&(ge=O.ref),O.key!==void 0&&(he=""+O.key),O)N.call(O,re)&&!ae.hasOwnProperty(re)&&(de[re]=O[re]);var pe=arguments.length-2;if(pe===1)de.children=se;else if(1<pe){for(var be=Array(pe),ve=0;ve<pe;ve++)be[ve]=arguments[ve+2];de.children=be}if(E&&E.defaultProps)for(re in pe=E.defaultProps,pe)de[re]===void 0&&(de[re]=pe[re]);return{$$typeof:t,type:E,key:he,ref:ge,props:de,_owner:_.current}}function ce(E,O){return{$$typeof:t,type:E.type,key:O,ref:E.ref,props:E.props,_owner:E._owner}}function we(E){return typeof E=="object"&&E!==null&&E.$$typeof===t}function Me(E){var O={"=":"=0",":":"=2"};return"$"+E.replace(/[=:]/g,function(se){return O[se]})}var _e=/\/+/g;function Fe(E,O){return typeof E=="object"&&E!==null&&E.key!=null?Me(""+E.key):O.toString(36)}function Ne(E,O,se,re,de){var he=typeof E;(he==="undefined"||he==="boolean")&&(E=null);var ge=!1;if(E===null)ge=!0;else switch(he){case"string":case"number":ge=!0;break;case"object":switch(E.$$typeof){case t:case a:ge=!0}}if(ge)return ge=E,de=de(ge),E=re===""?"."+Fe(ge,0):re,B(de)?(se="",E!=null&&(se=E.replace(_e,"$&/")+"/"),Ne(de,O,se,"",function(ve){return ve})):de!=null&&(we(de)&&(de=ce(de,se+(!de.key||ge&&ge.key===de.key?"":(""+de.key).replace(_e,"$&/")+"/")+E)),O.push(de)),1;if(ge=0,re=re===""?".":re+":",B(E))for(var pe=0;pe<E.length;pe++){he=E[pe];var be=re+Fe(he,pe);ge+=Ne(he,O,se,be,de)}else if(be=b(E),typeof be=="function")for(E=be.call(E),pe=0;!(he=E.next()).done;)he=he.value,be=re+Fe(he,pe++),ge+=Ne(he,O,se,be,de);else if(he==="object")throw O=String(E),Error("Objects are not valid as a React child (found: "+(O==="[object Object]"?"object with keys {"+Object.keys(E).join(", ")+"}":O)+"). If you meant to render a collection of children, use an array instead.");return ge}function Le(E,O,se){if(E==null)return E;var re=[],de=0;return Ne(E,re,"","",function(he){return O.call(se,he,de++)}),re}function Ke(E){if(E._status===-1){var O=E._result;O=O(),O.then(function(se){(E._status===0||E._status===-1)&&(E._status=1,E._result=se)},function(se){(E._status===0||E._status===-1)&&(E._status=2,E._result=se)}),E._status===-1&&(E._status=0,E._result=O)}if(E._status===1)return E._result.default;throw E._result}var oe={current:null},W={transition:null},Z={ReactCurrentDispatcher:oe,ReactCurrentBatchConfig:W,ReactCurrentOwner:_};function H(){throw Error("act(...) is not supported in production builds of React.")}return le.Children={map:Le,forEach:function(E,O,se){Le(E,function(){O.apply(this,arguments)},se)},count:function(E){var O=0;return Le(E,function(){O++}),O},toArray:function(E){return Le(E,function(O){return O})||[]},only:function(E){if(!we(E))throw Error("React.Children.only expected to receive a single React element child.");return E}},le.Component=A,le.Fragment=i,le.Profiler=r,le.PureComponent=j,le.StrictMode=s,le.Suspense=m,le.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=Z,le.act=H,le.cloneElement=function(E,O,se){if(E==null)throw Error("React.cloneElement(...): The argument must be a React element, but you passed "+E+".");var re=M({},E.props),de=E.key,he=E.ref,ge=E._owner;if(O!=null){if(O.ref!==void 0&&(he=O.ref,ge=_.current),O.key!==void 0&&(de=""+O.key),E.type&&E.type.defaultProps)var pe=E.type.defaultProps;for(be in O)N.call(O,be)&&!ae.hasOwnProperty(be)&&(re[be]=O[be]===void 0&&pe!==void 0?pe[be]:O[be])}var be=arguments.length-2;if(be===1)re.children=se;else if(1<be){pe=Array(be);for(var ve=0;ve<be;ve++)pe[ve]=arguments[ve+2];re.children=pe}return{$$typeof:t,type:E.type,key:de,ref:he,props:re,_owner:ge}},le.createContext=function(E){return E={$$typeof:h,_currentValue:E,_currentValue2:E,_threadCount:0,Provider:null,Consumer:null,_defaultValue:null,_globalName:null},E.Provider={$$typeof:c,_context:E},E.Consumer=E},le.createElement=G,le.createFactory=function(E){var O=G.bind(null,E);return O.type=E,O},le.createRef=function(){return{current:null}},le.forwardRef=function(E){return{$$typeof:p,render:E}},le.isValidElement=we,le.lazy=function(E){return{$$typeof:y,_payload:{_status:-1,_result:E},_init:Ke}},le.memo=function(E,O){return{$$typeof:f,type:E,compare:O===void 0?null:O}},le.startTransition=function(E){var O=W.transition;W.transition={};try{E()}finally{W.transition=O}},le.unstable_act=H,le.useCallback=function(E,O){return oe.current.useCallback(E,O)},le.useContext=function(E){return oe.current.useContext(E)},le.useDebugValue=function(){},le.useDeferredValue=function(E){return oe.current.useDeferredValue(E)},le.useEffect=function(E,O){return oe.current.useEffect(E,O)},le.useId=function(){return oe.current.useId()},le.useImperativeHandle=function(E,O,se){return oe.current.useImperativeHandle(E,O,se)},le.useInsertionEffect=function(E,O){return oe.current.useInsertionEffect(E,O)},le.useLayoutEffect=function(E,O){return oe.current.useLayoutEffect(E,O)},le.useMemo=function(E,O){return oe.current.useMemo(E,O)},le.useReducer=function(E,O,se){return oe.current.useReducer(E,O,se)},le.useRef=function(E){return oe.current.useRef(E)},le.useState=function(E){return oe.current.useState(E)},le.useSyncExternalStore=function(E,O,se){return oe.current.useSyncExternalStore(E,O,se)},le.useTransition=function(){return oe.current.useTransition()},le.version="18.3.1",le}var jp;function Xc(){return jp||(jp=1,oc.exports=cb()),oc.exports}/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Op;function db(){if(Op)return Oi;Op=1;var t=Xc(),a=Symbol.for("react.element"),i=Symbol.for("react.fragment"),s=Object.prototype.hasOwnProperty,r=t.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner,c={key:!0,ref:!0,__self:!0,__source:!0};function h(p,m,f){var y,v={},b=null,T=null;f!==void 0&&(b=""+f),m.key!==void 0&&(b=""+m.key),m.ref!==void 0&&(T=m.ref);for(y in m)s.call(m,y)&&!c.hasOwnProperty(y)&&(v[y]=m[y]);if(p&&p.defaultProps)for(y in m=p.defaultProps,m)v[y]===void 0&&(v[y]=m[y]);return{$$typeof:a,type:p,key:b,ref:T,props:v,_owner:r.current}}return Oi.Fragment=i,Oi.jsx=h,Oi.jsxs=h,Oi}var Up;function hb(){return Up||(Up=1,ic.exports=db()),ic.exports}var g=hb(),x=Xc();const Pt=Of(x),dR=lb({__proto__:null,default:Pt},[x]);var Ss={},sc={exports:{}},pt={},rc={exports:{}},lc={};/**
 * @license React
 * scheduler.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Fp;function ub(){return Fp||(Fp=1,(function(t){function a(W,Z){var H=W.length;W.push(Z);e:for(;0<H;){var E=H-1>>>1,O=W[E];if(0<r(O,Z))W[E]=Z,W[H]=O,H=E;else break e}}function i(W){return W.length===0?null:W[0]}function s(W){if(W.length===0)return null;var Z=W[0],H=W.pop();if(H!==Z){W[0]=H;e:for(var E=0,O=W.length,se=O>>>1;E<se;){var re=2*(E+1)-1,de=W[re],he=re+1,ge=W[he];if(0>r(de,H))he<O&&0>r(ge,de)?(W[E]=ge,W[he]=H,E=he):(W[E]=de,W[re]=H,E=re);else if(he<O&&0>r(ge,H))W[E]=ge,W[he]=H,E=he;else break e}}return Z}function r(W,Z){var H=W.sortIndex-Z.sortIndex;return H!==0?H:W.id-Z.id}if(typeof performance=="object"&&typeof performance.now=="function"){var c=performance;t.unstable_now=function(){return c.now()}}else{var h=Date,p=h.now();t.unstable_now=function(){return h.now()-p}}var m=[],f=[],y=1,v=null,b=3,T=!1,M=!1,I=!1,A=typeof setTimeout=="function"?setTimeout:null,R=typeof clearTimeout=="function"?clearTimeout:null,j=typeof setImmediate<"u"?setImmediate:null;typeof navigator<"u"&&navigator.scheduling!==void 0&&navigator.scheduling.isInputPending!==void 0&&navigator.scheduling.isInputPending.bind(navigator.scheduling);function V(W){for(var Z=i(f);Z!==null;){if(Z.callback===null)s(f);else if(Z.startTime<=W)s(f),Z.sortIndex=Z.expirationTime,a(m,Z);else break;Z=i(f)}}function B(W){if(I=!1,V(W),!M)if(i(m)!==null)M=!0,Ke(N);else{var Z=i(f);Z!==null&&oe(B,Z.startTime-W)}}function N(W,Z){M=!1,I&&(I=!1,R(G),G=-1),T=!0;var H=b;try{for(V(Z),v=i(m);v!==null&&(!(v.expirationTime>Z)||W&&!Me());){var E=v.callback;if(typeof E=="function"){v.callback=null,b=v.priorityLevel;var O=E(v.expirationTime<=Z);Z=t.unstable_now(),typeof O=="function"?v.callback=O:v===i(m)&&s(m),V(Z)}else s(m);v=i(m)}if(v!==null)var se=!0;else{var re=i(f);re!==null&&oe(B,re.startTime-Z),se=!1}return se}finally{v=null,b=H,T=!1}}var _=!1,ae=null,G=-1,ce=5,we=-1;function Me(){return!(t.unstable_now()-we<ce)}function _e(){if(ae!==null){var W=t.unstable_now();we=W;var Z=!0;try{Z=ae(!0,W)}finally{Z?Fe():(_=!1,ae=null)}}else _=!1}var Fe;if(typeof j=="function")Fe=function(){j(_e)};else if(typeof MessageChannel<"u"){var Ne=new MessageChannel,Le=Ne.port2;Ne.port1.onmessage=_e,Fe=function(){Le.postMessage(null)}}else Fe=function(){A(_e,0)};function Ke(W){ae=W,_||(_=!0,Fe())}function oe(W,Z){G=A(function(){W(t.unstable_now())},Z)}t.unstable_IdlePriority=5,t.unstable_ImmediatePriority=1,t.unstable_LowPriority=4,t.unstable_NormalPriority=3,t.unstable_Profiling=null,t.unstable_UserBlockingPriority=2,t.unstable_cancelCallback=function(W){W.callback=null},t.unstable_continueExecution=function(){M||T||(M=!0,Ke(N))},t.unstable_forceFrameRate=function(W){0>W||125<W?console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"):ce=0<W?Math.floor(1e3/W):5},t.unstable_getCurrentPriorityLevel=function(){return b},t.unstable_getFirstCallbackNode=function(){return i(m)},t.unstable_next=function(W){switch(b){case 1:case 2:case 3:var Z=3;break;default:Z=b}var H=b;b=Z;try{return W()}finally{b=H}},t.unstable_pauseExecution=function(){},t.unstable_requestPaint=function(){},t.unstable_runWithPriority=function(W,Z){switch(W){case 1:case 2:case 3:case 4:case 5:break;default:W=3}var H=b;b=W;try{return Z()}finally{b=H}},t.unstable_scheduleCallback=function(W,Z,H){var E=t.unstable_now();switch(typeof H=="object"&&H!==null?(H=H.delay,H=typeof H=="number"&&0<H?E+H:E):H=E,W){case 1:var O=-1;break;case 2:O=250;break;case 5:O=1073741823;break;case 4:O=1e4;break;default:O=5e3}return O=H+O,W={id:y++,callback:Z,priorityLevel:W,startTime:H,expirationTime:O,sortIndex:-1},H>E?(W.sortIndex=H,a(f,W),i(m)===null&&W===i(f)&&(I?(R(G),G=-1):I=!0,oe(B,H-E))):(W.sortIndex=O,a(m,W),M||T||(M=!0,Ke(N))),W},t.unstable_shouldYield=Me,t.unstable_wrapCallback=function(W){var Z=b;return function(){var H=b;b=Z;try{return W.apply(this,arguments)}finally{b=H}}}})(lc)),lc}var Np;function pb(){return Np||(Np=1,rc.exports=ub()),rc.exports}/**
 * @license React
 * react-dom.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var qp;function mb(){if(qp)return pt;qp=1;var t=Xc(),a=pb();function i(e){for(var n="https://reactjs.org/docs/error-decoder.html?invariant="+e,o=1;o<arguments.length;o++)n+="&args[]="+encodeURIComponent(arguments[o]);return"Minified React error #"+e+"; visit "+n+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings."}var s=new Set,r={};function c(e,n){h(e,n),h(e+"Capture",n)}function h(e,n){for(r[e]=n,e=0;e<n.length;e++)s.add(n[e])}var p=!(typeof window>"u"||typeof window.document>"u"||typeof window.document.createElement>"u"),m=Object.prototype.hasOwnProperty,f=/^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/,y={},v={};function b(e){return m.call(v,e)?!0:m.call(y,e)?!1:f.test(e)?v[e]=!0:(y[e]=!0,!1)}function T(e,n,o,l){if(o!==null&&o.type===0)return!1;switch(typeof n){case"function":case"symbol":return!0;case"boolean":return l?!1:o!==null?!o.acceptsBooleans:(e=e.toLowerCase().slice(0,5),e!=="data-"&&e!=="aria-");default:return!1}}function M(e,n,o,l){if(n===null||typeof n>"u"||T(e,n,o,l))return!0;if(l)return!1;if(o!==null)switch(o.type){case 3:return!n;case 4:return n===!1;case 5:return isNaN(n);case 6:return isNaN(n)||1>n}return!1}function I(e,n,o,l,d,u,w){this.acceptsBooleans=n===2||n===3||n===4,this.attributeName=l,this.attributeNamespace=d,this.mustUseProperty=o,this.propertyName=e,this.type=n,this.sanitizeURL=u,this.removeEmptyString=w}var A={};"children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(e){A[e]=new I(e,0,!1,e,null,!1,!1)}),[["acceptCharset","accept-charset"],["className","class"],["htmlFor","for"],["httpEquiv","http-equiv"]].forEach(function(e){var n=e[0];A[n]=new I(n,1,!1,e[1],null,!1,!1)}),["contentEditable","draggable","spellCheck","value"].forEach(function(e){A[e]=new I(e,2,!1,e.toLowerCase(),null,!1,!1)}),["autoReverse","externalResourcesRequired","focusable","preserveAlpha"].forEach(function(e){A[e]=new I(e,2,!1,e,null,!1,!1)}),"allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(e){A[e]=new I(e,3,!1,e.toLowerCase(),null,!1,!1)}),["checked","multiple","muted","selected"].forEach(function(e){A[e]=new I(e,3,!0,e,null,!1,!1)}),["capture","download"].forEach(function(e){A[e]=new I(e,4,!1,e,null,!1,!1)}),["cols","rows","size","span"].forEach(function(e){A[e]=new I(e,6,!1,e,null,!1,!1)}),["rowSpan","start"].forEach(function(e){A[e]=new I(e,5,!1,e.toLowerCase(),null,!1,!1)});var R=/[\-:]([a-z])/g;function j(e){return e[1].toUpperCase()}"accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(e){var n=e.replace(R,j);A[n]=new I(n,1,!1,e,null,!1,!1)}),"xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(e){var n=e.replace(R,j);A[n]=new I(n,1,!1,e,"http://www.w3.org/1999/xlink",!1,!1)}),["xml:base","xml:lang","xml:space"].forEach(function(e){var n=e.replace(R,j);A[n]=new I(n,1,!1,e,"http://www.w3.org/XML/1998/namespace",!1,!1)}),["tabIndex","crossOrigin"].forEach(function(e){A[e]=new I(e,1,!1,e.toLowerCase(),null,!1,!1)}),A.xlinkHref=new I("xlinkHref",1,!1,"xlink:href","http://www.w3.org/1999/xlink",!0,!1),["src","href","action","formAction"].forEach(function(e){A[e]=new I(e,1,!1,e.toLowerCase(),null,!0,!0)});function V(e,n,o,l){var d=A.hasOwnProperty(n)?A[n]:null;(d!==null?d.type!==0:l||!(2<n.length)||n[0]!=="o"&&n[0]!=="O"||n[1]!=="n"&&n[1]!=="N")&&(M(n,o,d,l)&&(o=null),l||d===null?b(n)&&(o===null?e.removeAttribute(n):e.setAttribute(n,""+o)):d.mustUseProperty?e[d.propertyName]=o===null?d.type===3?!1:"":o:(n=d.attributeName,l=d.attributeNamespace,o===null?e.removeAttribute(n):(d=d.type,o=d===3||d===4&&o===!0?"":""+o,l?e.setAttributeNS(l,n,o):e.setAttribute(n,o))))}var B=t.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED,N=Symbol.for("react.element"),_=Symbol.for("react.portal"),ae=Symbol.for("react.fragment"),G=Symbol.for("react.strict_mode"),ce=Symbol.for("react.profiler"),we=Symbol.for("react.provider"),Me=Symbol.for("react.context"),_e=Symbol.for("react.forward_ref"),Fe=Symbol.for("react.suspense"),Ne=Symbol.for("react.suspense_list"),Le=Symbol.for("react.memo"),Ke=Symbol.for("react.lazy"),oe=Symbol.for("react.offscreen"),W=Symbol.iterator;function Z(e){return e===null||typeof e!="object"?null:(e=W&&e[W]||e["@@iterator"],typeof e=="function"?e:null)}var H=Object.assign,E;function O(e){if(E===void 0)try{throw Error()}catch(o){var n=o.stack.trim().match(/\n( *(at )?)/);E=n&&n[1]||""}return`
`+E+e}var se=!1;function re(e,n){if(!e||se)return"";se=!0;var o=Error.prepareStackTrace;Error.prepareStackTrace=void 0;try{if(n)if(n=function(){throw Error()},Object.defineProperty(n.prototype,"props",{set:function(){throw Error()}}),typeof Reflect=="object"&&Reflect.construct){try{Reflect.construct(n,[])}catch(D){var l=D}Reflect.construct(e,[],n)}else{try{n.call()}catch(D){l=D}e.call(n.prototype)}else{try{throw Error()}catch(D){l=D}e()}}catch(D){if(D&&l&&typeof D.stack=="string"){for(var d=D.stack.split(`
`),u=l.stack.split(`
`),w=d.length-1,k=u.length-1;1<=w&&0<=k&&d[w]!==u[k];)k--;for(;1<=w&&0<=k;w--,k--)if(d[w]!==u[k]){if(w!==1||k!==1)do if(w--,k--,0>k||d[w]!==u[k]){var S=`
`+d[w].replace(" at new "," at ");return e.displayName&&S.includes("<anonymous>")&&(S=S.replace("<anonymous>",e.displayName)),S}while(1<=w&&0<=k);break}}}finally{se=!1,Error.prepareStackTrace=o}return(e=e?e.displayName||e.name:"")?O(e):""}function de(e){switch(e.tag){case 5:return O(e.type);case 16:return O("Lazy");case 13:return O("Suspense");case 19:return O("SuspenseList");case 0:case 2:case 15:return e=re(e.type,!1),e;case 11:return e=re(e.type.render,!1),e;case 1:return e=re(e.type,!0),e;default:return""}}function he(e){if(e==null)return null;if(typeof e=="function")return e.displayName||e.name||null;if(typeof e=="string")return e;switch(e){case ae:return"Fragment";case _:return"Portal";case ce:return"Profiler";case G:return"StrictMode";case Fe:return"Suspense";case Ne:return"SuspenseList"}if(typeof e=="object")switch(e.$$typeof){case Me:return(e.displayName||"Context")+".Consumer";case we:return(e._context.displayName||"Context")+".Provider";case _e:var n=e.render;return e=e.displayName,e||(e=n.displayName||n.name||"",e=e!==""?"ForwardRef("+e+")":"ForwardRef"),e;case Le:return n=e.displayName||null,n!==null?n:he(e.type)||"Memo";case Ke:n=e._payload,e=e._init;try{return he(e(n))}catch{}}return null}function ge(e){var n=e.type;switch(e.tag){case 24:return"Cache";case 9:return(n.displayName||"Context")+".Consumer";case 10:return(n._context.displayName||"Context")+".Provider";case 18:return"DehydratedFragment";case 11:return e=n.render,e=e.displayName||e.name||"",n.displayName||(e!==""?"ForwardRef("+e+")":"ForwardRef");case 7:return"Fragment";case 5:return n;case 4:return"Portal";case 3:return"Root";case 6:return"Text";case 16:return he(n);case 8:return n===G?"StrictMode":"Mode";case 22:return"Offscreen";case 12:return"Profiler";case 21:return"Scope";case 13:return"Suspense";case 19:return"SuspenseList";case 25:return"TracingMarker";case 1:case 0:case 17:case 2:case 14:case 15:if(typeof n=="function")return n.displayName||n.name||null;if(typeof n=="string")return n}return null}function pe(e){switch(typeof e){case"boolean":case"number":case"string":case"undefined":return e;case"object":return e;default:return""}}function be(e){var n=e.type;return(e=e.nodeName)&&e.toLowerCase()==="input"&&(n==="checkbox"||n==="radio")}function ve(e){var n=be(e)?"checked":"value",o=Object.getOwnPropertyDescriptor(e.constructor.prototype,n),l=""+e[n];if(!e.hasOwnProperty(n)&&typeof o<"u"&&typeof o.get=="function"&&typeof o.set=="function"){var d=o.get,u=o.set;return Object.defineProperty(e,n,{configurable:!0,get:function(){return d.call(this)},set:function(w){l=""+w,u.call(this,w)}}),Object.defineProperty(e,n,{enumerable:o.enumerable}),{getValue:function(){return l},setValue:function(w){l=""+w},stopTracking:function(){e._valueTracker=null,delete e[n]}}}}function Ie(e){e._valueTracker||(e._valueTracker=ve(e))}function it(e){if(!e)return!1;var n=e._valueTracker;if(!n)return!0;var o=n.getValue(),l="";return e&&(l=be(e)?e.checked?"true":"false":e.value),e=l,e!==o?(n.setValue(e),!0):!1}function Rt(e){if(e=e||(typeof document<"u"?document:void 0),typeof e>"u")return null;try{return e.activeElement||e.body}catch{return e.body}}function $a(e,n){var o=n.checked;return H({},n,{defaultChecked:void 0,defaultValue:void 0,value:void 0,checked:o??e._wrapperState.initialChecked})}function Ya(e,n){var o=n.defaultValue==null?"":n.defaultValue,l=n.checked!=null?n.checked:n.defaultChecked;o=pe(n.value!=null?n.value:o),e._wrapperState={initialChecked:l,initialValue:o,controlled:n.type==="checkbox"||n.type==="radio"?n.checked!=null:n.value!=null}}function sa(e,n){n=n.checked,n!=null&&V(e,"checked",n,!1)}function ur(e,n){sa(e,n);var o=pe(n.value),l=n.type;if(o!=null)l==="number"?(o===0&&e.value===""||e.value!=o)&&(e.value=""+o):e.value!==""+o&&(e.value=""+o);else if(l==="submit"||l==="reset"){e.removeAttribute("value");return}n.hasOwnProperty("value")?pr(e,n.type,o):n.hasOwnProperty("defaultValue")&&pr(e,n.type,pe(n.defaultValue)),n.checked==null&&n.defaultChecked!=null&&(e.defaultChecked=!!n.defaultChecked)}function Bd(e,n,o){if(n.hasOwnProperty("value")||n.hasOwnProperty("defaultValue")){var l=n.type;if(!(l!=="submit"&&l!=="reset"||n.value!==void 0&&n.value!==null))return;n=""+e._wrapperState.initialValue,o||n===e.value||(e.value=n),e.defaultValue=n}o=e.name,o!==""&&(e.name=""),e.defaultChecked=!!e._wrapperState.initialChecked,o!==""&&(e.name=o)}function pr(e,n,o){(n!=="number"||Rt(e.ownerDocument)!==e)&&(o==null?e.defaultValue=""+e._wrapperState.initialValue:e.defaultValue!==""+o&&(e.defaultValue=""+o))}var Qa=Array.isArray;function ra(e,n,o,l){if(e=e.options,n){n={};for(var d=0;d<o.length;d++)n["$"+o[d]]=!0;for(o=0;o<e.length;o++)d=n.hasOwnProperty("$"+e[o].value),e[o].selected!==d&&(e[o].selected=d),d&&l&&(e[o].defaultSelected=!0)}else{for(o=""+pe(o),n=null,d=0;d<e.length;d++){if(e[d].value===o){e[d].selected=!0,l&&(e[d].defaultSelected=!0);return}n!==null||e[d].disabled||(n=e[d])}n!==null&&(n.selected=!0)}}function mr(e,n){if(n.dangerouslySetInnerHTML!=null)throw Error(i(91));return H({},n,{value:void 0,defaultValue:void 0,children:""+e._wrapperState.initialValue})}function Hd(e,n){var o=n.value;if(o==null){if(o=n.children,n=n.defaultValue,o!=null){if(n!=null)throw Error(i(92));if(Qa(o)){if(1<o.length)throw Error(i(93));o=o[0]}n=o}n==null&&(n=""),o=n}e._wrapperState={initialValue:pe(o)}}function _d(e,n){var o=pe(n.value),l=pe(n.defaultValue);o!=null&&(o=""+o,o!==e.value&&(e.value=o),n.defaultValue==null&&e.defaultValue!==o&&(e.defaultValue=o)),l!=null&&(e.defaultValue=""+l)}function Kd(e){var n=e.textContent;n===e._wrapperState.initialValue&&n!==""&&n!==null&&(e.value=n)}function Jd(e){switch(e){case"svg":return"http://www.w3.org/2000/svg";case"math":return"http://www.w3.org/1998/Math/MathML";default:return"http://www.w3.org/1999/xhtml"}}function fr(e,n){return e==null||e==="http://www.w3.org/1999/xhtml"?Jd(n):e==="http://www.w3.org/2000/svg"&&n==="foreignObject"?"http://www.w3.org/1999/xhtml":e}var fo,Gd=(function(e){return typeof MSApp<"u"&&MSApp.execUnsafeLocalFunction?function(n,o,l,d){MSApp.execUnsafeLocalFunction(function(){return e(n,o,l,d)})}:e})(function(e,n){if(e.namespaceURI!=="http://www.w3.org/2000/svg"||"innerHTML"in e)e.innerHTML=n;else{for(fo=fo||document.createElement("div"),fo.innerHTML="<svg>"+n.valueOf().toString()+"</svg>",n=fo.firstChild;e.firstChild;)e.removeChild(e.firstChild);for(;n.firstChild;)e.appendChild(n.firstChild)}});function Xa(e,n){if(n){var o=e.firstChild;if(o&&o===e.lastChild&&o.nodeType===3){o.nodeValue=n;return}}e.textContent=n}var Za={animationIterationCount:!0,aspectRatio:!0,borderImageOutset:!0,borderImageSlice:!0,borderImageWidth:!0,boxFlex:!0,boxFlexGroup:!0,boxOrdinalGroup:!0,columnCount:!0,columns:!0,flex:!0,flexGrow:!0,flexPositive:!0,flexShrink:!0,flexNegative:!0,flexOrder:!0,gridArea:!0,gridRow:!0,gridRowEnd:!0,gridRowSpan:!0,gridRowStart:!0,gridColumn:!0,gridColumnEnd:!0,gridColumnSpan:!0,gridColumnStart:!0,fontWeight:!0,lineClamp:!0,lineHeight:!0,opacity:!0,order:!0,orphans:!0,tabSize:!0,widows:!0,zIndex:!0,zoom:!0,fillOpacity:!0,floodOpacity:!0,stopOpacity:!0,strokeDasharray:!0,strokeDashoffset:!0,strokeMiterlimit:!0,strokeOpacity:!0,strokeWidth:!0},dw=["Webkit","ms","Moz","O"];Object.keys(Za).forEach(function(e){dw.forEach(function(n){n=n+e.charAt(0).toUpperCase()+e.substring(1),Za[n]=Za[e]})});function $d(e,n,o){return n==null||typeof n=="boolean"||n===""?"":o||typeof n!="number"||n===0||Za.hasOwnProperty(e)&&Za[e]?(""+n).trim():n+"px"}function Yd(e,n){e=e.style;for(var o in n)if(n.hasOwnProperty(o)){var l=o.indexOf("--")===0,d=$d(o,n[o],l);o==="float"&&(o="cssFloat"),l?e.setProperty(o,d):e[o]=d}}var hw=H({menuitem:!0},{area:!0,base:!0,br:!0,col:!0,embed:!0,hr:!0,img:!0,input:!0,keygen:!0,link:!0,meta:!0,param:!0,source:!0,track:!0,wbr:!0});function gr(e,n){if(n){if(hw[e]&&(n.children!=null||n.dangerouslySetInnerHTML!=null))throw Error(i(137,e));if(n.dangerouslySetInnerHTML!=null){if(n.children!=null)throw Error(i(60));if(typeof n.dangerouslySetInnerHTML!="object"||!("__html"in n.dangerouslySetInnerHTML))throw Error(i(61))}if(n.style!=null&&typeof n.style!="object")throw Error(i(62))}}function yr(e,n){if(e.indexOf("-")===-1)return typeof n.is=="string";switch(e){case"annotation-xml":case"color-profile":case"font-face":case"font-face-src":case"font-face-uri":case"font-face-format":case"font-face-name":case"missing-glyph":return!1;default:return!0}}var wr=null;function vr(e){return e=e.target||e.srcElement||window,e.correspondingUseElement&&(e=e.correspondingUseElement),e.nodeType===3?e.parentNode:e}var br=null,la=null,ca=null;function Qd(e){if(e=ki(e)){if(typeof br!="function")throw Error(i(280));var n=e.stateNode;n&&(n=Fo(n),br(e.stateNode,e.type,n))}}function Xd(e){la?ca?ca.push(e):ca=[e]:la=e}function Zd(){if(la){var e=la,n=ca;if(ca=la=null,Qd(e),n)for(e=0;e<n.length;e++)Qd(n[e])}}function eh(e,n){return e(n)}function th(){}var kr=!1;function nh(e,n,o){if(kr)return e(n,o);kr=!0;try{return eh(e,n,o)}finally{kr=!1,(la!==null||ca!==null)&&(th(),Zd())}}function ei(e,n){var o=e.stateNode;if(o===null)return null;var l=Fo(o);if(l===null)return null;o=l[n];e:switch(n){case"onClick":case"onClickCapture":case"onDoubleClick":case"onDoubleClickCapture":case"onMouseDown":case"onMouseDownCapture":case"onMouseMove":case"onMouseMoveCapture":case"onMouseUp":case"onMouseUpCapture":case"onMouseEnter":(l=!l.disabled)||(e=e.type,l=!(e==="button"||e==="input"||e==="select"||e==="textarea")),e=!l;break e;default:e=!1}if(e)return null;if(o&&typeof o!="function")throw Error(i(231,n,typeof o));return o}var xr=!1;if(p)try{var ti={};Object.defineProperty(ti,"passive",{get:function(){xr=!0}}),window.addEventListener("test",ti,ti),window.removeEventListener("test",ti,ti)}catch{xr=!1}function uw(e,n,o,l,d,u,w,k,S){var D=Array.prototype.slice.call(arguments,3);try{n.apply(o,D)}catch(F){this.onError(F)}}var ni=!1,go=null,yo=!1,Sr=null,pw={onError:function(e){ni=!0,go=e}};function mw(e,n,o,l,d,u,w,k,S){ni=!1,go=null,uw.apply(pw,arguments)}function fw(e,n,o,l,d,u,w,k,S){if(mw.apply(this,arguments),ni){if(ni){var D=go;ni=!1,go=null}else throw Error(i(198));yo||(yo=!0,Sr=D)}}function Fn(e){var n=e,o=e;if(e.alternate)for(;n.return;)n=n.return;else{e=n;do n=e,(n.flags&4098)!==0&&(o=n.return),e=n.return;while(e)}return n.tag===3?o:null}function ah(e){if(e.tag===13){var n=e.memoizedState;if(n===null&&(e=e.alternate,e!==null&&(n=e.memoizedState)),n!==null)return n.dehydrated}return null}function ih(e){if(Fn(e)!==e)throw Error(i(188))}function gw(e){var n=e.alternate;if(!n){if(n=Fn(e),n===null)throw Error(i(188));return n!==e?null:e}for(var o=e,l=n;;){var d=o.return;if(d===null)break;var u=d.alternate;if(u===null){if(l=d.return,l!==null){o=l;continue}break}if(d.child===u.child){for(u=d.child;u;){if(u===o)return ih(d),e;if(u===l)return ih(d),n;u=u.sibling}throw Error(i(188))}if(o.return!==l.return)o=d,l=u;else{for(var w=!1,k=d.child;k;){if(k===o){w=!0,o=d,l=u;break}if(k===l){w=!0,l=d,o=u;break}k=k.sibling}if(!w){for(k=u.child;k;){if(k===o){w=!0,o=u,l=d;break}if(k===l){w=!0,l=u,o=d;break}k=k.sibling}if(!w)throw Error(i(189))}}if(o.alternate!==l)throw Error(i(190))}if(o.tag!==3)throw Error(i(188));return o.stateNode.current===o?e:n}function oh(e){return e=gw(e),e!==null?sh(e):null}function sh(e){if(e.tag===5||e.tag===6)return e;for(e=e.child;e!==null;){var n=sh(e);if(n!==null)return n;e=e.sibling}return null}var rh=a.unstable_scheduleCallback,lh=a.unstable_cancelCallback,yw=a.unstable_shouldYield,ww=a.unstable_requestPaint,je=a.unstable_now,vw=a.unstable_getCurrentPriorityLevel,Tr=a.unstable_ImmediatePriority,ch=a.unstable_UserBlockingPriority,wo=a.unstable_NormalPriority,bw=a.unstable_LowPriority,dh=a.unstable_IdlePriority,vo=null,Bt=null;function kw(e){if(Bt&&typeof Bt.onCommitFiberRoot=="function")try{Bt.onCommitFiberRoot(vo,e,void 0,(e.current.flags&128)===128)}catch{}}var Lt=Math.clz32?Math.clz32:Tw,xw=Math.log,Sw=Math.LN2;function Tw(e){return e>>>=0,e===0?32:31-(xw(e)/Sw|0)|0}var bo=64,ko=4194304;function ai(e){switch(e&-e){case 1:return 1;case 2:return 2;case 4:return 4;case 8:return 8;case 16:return 16;case 32:return 32;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return e&4194240;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return e&130023424;case 134217728:return 134217728;case 268435456:return 268435456;case 536870912:return 536870912;case 1073741824:return 1073741824;default:return e}}function xo(e,n){var o=e.pendingLanes;if(o===0)return 0;var l=0,d=e.suspendedLanes,u=e.pingedLanes,w=o&268435455;if(w!==0){var k=w&~d;k!==0?l=ai(k):(u&=w,u!==0&&(l=ai(u)))}else w=o&~d,w!==0?l=ai(w):u!==0&&(l=ai(u));if(l===0)return 0;if(n!==0&&n!==l&&(n&d)===0&&(d=l&-l,u=n&-n,d>=u||d===16&&(u&4194240)!==0))return n;if((l&4)!==0&&(l|=o&16),n=e.entangledLanes,n!==0)for(e=e.entanglements,n&=l;0<n;)o=31-Lt(n),d=1<<o,l|=e[o],n&=~d;return l}function Cw(e,n){switch(e){case 1:case 2:case 4:return n+250;case 8:case 16:case 32:case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return n+5e3;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return-1;case 134217728:case 268435456:case 536870912:case 1073741824:return-1;default:return-1}}function Mw(e,n){for(var o=e.suspendedLanes,l=e.pingedLanes,d=e.expirationTimes,u=e.pendingLanes;0<u;){var w=31-Lt(u),k=1<<w,S=d[w];S===-1?((k&o)===0||(k&l)!==0)&&(d[w]=Cw(k,n)):S<=n&&(e.expiredLanes|=k),u&=~k}}function Cr(e){return e=e.pendingLanes&-1073741825,e!==0?e:e&1073741824?1073741824:0}function hh(){var e=bo;return bo<<=1,(bo&4194240)===0&&(bo=64),e}function Mr(e){for(var n=[],o=0;31>o;o++)n.push(e);return n}function ii(e,n,o){e.pendingLanes|=n,n!==536870912&&(e.suspendedLanes=0,e.pingedLanes=0),e=e.eventTimes,n=31-Lt(n),e[n]=o}function Iw(e,n){var o=e.pendingLanes&~n;e.pendingLanes=n,e.suspendedLanes=0,e.pingedLanes=0,e.expiredLanes&=n,e.mutableReadLanes&=n,e.entangledLanes&=n,n=e.entanglements;var l=e.eventTimes;for(e=e.expirationTimes;0<o;){var d=31-Lt(o),u=1<<d;n[d]=0,l[d]=-1,e[d]=-1,o&=~u}}function Ir(e,n){var o=e.entangledLanes|=n;for(e=e.entanglements;o;){var l=31-Lt(o),d=1<<l;d&n|e[l]&n&&(e[l]|=n),o&=~d}}var ye=0;function uh(e){return e&=-e,1<e?4<e?(e&268435455)!==0?16:536870912:4:1}var ph,Ar,mh,fh,gh,Er=!1,So=[],pn=null,mn=null,fn=null,oi=new Map,si=new Map,gn=[],Aw="mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset submit".split(" ");function yh(e,n){switch(e){case"focusin":case"focusout":pn=null;break;case"dragenter":case"dragleave":mn=null;break;case"mouseover":case"mouseout":fn=null;break;case"pointerover":case"pointerout":oi.delete(n.pointerId);break;case"gotpointercapture":case"lostpointercapture":si.delete(n.pointerId)}}function ri(e,n,o,l,d,u){return e===null||e.nativeEvent!==u?(e={blockedOn:n,domEventName:o,eventSystemFlags:l,nativeEvent:u,targetContainers:[d]},n!==null&&(n=ki(n),n!==null&&Ar(n)),e):(e.eventSystemFlags|=l,n=e.targetContainers,d!==null&&n.indexOf(d)===-1&&n.push(d),e)}function Ew(e,n,o,l,d){switch(n){case"focusin":return pn=ri(pn,e,n,o,l,d),!0;case"dragenter":return mn=ri(mn,e,n,o,l,d),!0;case"mouseover":return fn=ri(fn,e,n,o,l,d),!0;case"pointerover":var u=d.pointerId;return oi.set(u,ri(oi.get(u)||null,e,n,o,l,d)),!0;case"gotpointercapture":return u=d.pointerId,si.set(u,ri(si.get(u)||null,e,n,o,l,d)),!0}return!1}function wh(e){var n=Nn(e.target);if(n!==null){var o=Fn(n);if(o!==null){if(n=o.tag,n===13){if(n=ah(o),n!==null){e.blockedOn=n,gh(e.priority,function(){mh(o)});return}}else if(n===3&&o.stateNode.current.memoizedState.isDehydrated){e.blockedOn=o.tag===3?o.stateNode.containerInfo:null;return}}}e.blockedOn=null}function To(e){if(e.blockedOn!==null)return!1;for(var n=e.targetContainers;0<n.length;){var o=Rr(e.domEventName,e.eventSystemFlags,n[0],e.nativeEvent);if(o===null){o=e.nativeEvent;var l=new o.constructor(o.type,o);wr=l,o.target.dispatchEvent(l),wr=null}else return n=ki(o),n!==null&&Ar(n),e.blockedOn=o,!1;n.shift()}return!0}function vh(e,n,o){To(e)&&o.delete(n)}function Pw(){Er=!1,pn!==null&&To(pn)&&(pn=null),mn!==null&&To(mn)&&(mn=null),fn!==null&&To(fn)&&(fn=null),oi.forEach(vh),si.forEach(vh)}function li(e,n){e.blockedOn===n&&(e.blockedOn=null,Er||(Er=!0,a.unstable_scheduleCallback(a.unstable_NormalPriority,Pw)))}function ci(e){function n(d){return li(d,e)}if(0<So.length){li(So[0],e);for(var o=1;o<So.length;o++){var l=So[o];l.blockedOn===e&&(l.blockedOn=null)}}for(pn!==null&&li(pn,e),mn!==null&&li(mn,e),fn!==null&&li(fn,e),oi.forEach(n),si.forEach(n),o=0;o<gn.length;o++)l=gn[o],l.blockedOn===e&&(l.blockedOn=null);for(;0<gn.length&&(o=gn[0],o.blockedOn===null);)wh(o),o.blockedOn===null&&gn.shift()}var da=B.ReactCurrentBatchConfig,Co=!0;function Rw(e,n,o,l){var d=ye,u=da.transition;da.transition=null;try{ye=1,Pr(e,n,o,l)}finally{ye=d,da.transition=u}}function Lw(e,n,o,l){var d=ye,u=da.transition;da.transition=null;try{ye=4,Pr(e,n,o,l)}finally{ye=d,da.transition=u}}function Pr(e,n,o,l){if(Co){var d=Rr(e,n,o,l);if(d===null)Gr(e,n,l,Mo,o),yh(e,l);else if(Ew(d,e,n,o,l))l.stopPropagation();else if(yh(e,l),n&4&&-1<Aw.indexOf(e)){for(;d!==null;){var u=ki(d);if(u!==null&&ph(u),u=Rr(e,n,o,l),u===null&&Gr(e,n,l,Mo,o),u===d)break;d=u}d!==null&&l.stopPropagation()}else Gr(e,n,l,null,o)}}var Mo=null;function Rr(e,n,o,l){if(Mo=null,e=vr(l),e=Nn(e),e!==null)if(n=Fn(e),n===null)e=null;else if(o=n.tag,o===13){if(e=ah(n),e!==null)return e;e=null}else if(o===3){if(n.stateNode.current.memoizedState.isDehydrated)return n.tag===3?n.stateNode.containerInfo:null;e=null}else n!==e&&(e=null);return Mo=e,null}function bh(e){switch(e){case"cancel":case"click":case"close":case"contextmenu":case"copy":case"cut":case"auxclick":case"dblclick":case"dragend":case"dragstart":case"drop":case"focusin":case"focusout":case"input":case"invalid":case"keydown":case"keypress":case"keyup":case"mousedown":case"mouseup":case"paste":case"pause":case"play":case"pointercancel":case"pointerdown":case"pointerup":case"ratechange":case"reset":case"resize":case"seeked":case"submit":case"touchcancel":case"touchend":case"touchstart":case"volumechange":case"change":case"selectionchange":case"textInput":case"compositionstart":case"compositionend":case"compositionupdate":case"beforeblur":case"afterblur":case"beforeinput":case"blur":case"fullscreenchange":case"focus":case"hashchange":case"popstate":case"select":case"selectstart":return 1;case"drag":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"mousemove":case"mouseout":case"mouseover":case"pointermove":case"pointerout":case"pointerover":case"scroll":case"toggle":case"touchmove":case"wheel":case"mouseenter":case"mouseleave":case"pointerenter":case"pointerleave":return 4;case"message":switch(vw()){case Tr:return 1;case ch:return 4;case wo:case bw:return 16;case dh:return 536870912;default:return 16}default:return 16}}var yn=null,Lr=null,Io=null;function kh(){if(Io)return Io;var e,n=Lr,o=n.length,l,d="value"in yn?yn.value:yn.textContent,u=d.length;for(e=0;e<o&&n[e]===d[e];e++);var w=o-e;for(l=1;l<=w&&n[o-l]===d[u-l];l++);return Io=d.slice(e,1<l?1-l:void 0)}function Ao(e){var n=e.keyCode;return"charCode"in e?(e=e.charCode,e===0&&n===13&&(e=13)):e=n,e===10&&(e=13),32<=e||e===13?e:0}function Eo(){return!0}function xh(){return!1}function gt(e){function n(o,l,d,u,w){this._reactName=o,this._targetInst=d,this.type=l,this.nativeEvent=u,this.target=w,this.currentTarget=null;for(var k in e)e.hasOwnProperty(k)&&(o=e[k],this[k]=o?o(u):u[k]);return this.isDefaultPrevented=(u.defaultPrevented!=null?u.defaultPrevented:u.returnValue===!1)?Eo:xh,this.isPropagationStopped=xh,this}return H(n.prototype,{preventDefault:function(){this.defaultPrevented=!0;var o=this.nativeEvent;o&&(o.preventDefault?o.preventDefault():typeof o.returnValue!="unknown"&&(o.returnValue=!1),this.isDefaultPrevented=Eo)},stopPropagation:function(){var o=this.nativeEvent;o&&(o.stopPropagation?o.stopPropagation():typeof o.cancelBubble!="unknown"&&(o.cancelBubble=!0),this.isPropagationStopped=Eo)},persist:function(){},isPersistent:Eo}),n}var ha={eventPhase:0,bubbles:0,cancelable:0,timeStamp:function(e){return e.timeStamp||Date.now()},defaultPrevented:0,isTrusted:0},Dr=gt(ha),di=H({},ha,{view:0,detail:0}),Dw=gt(di),Vr,jr,hi,Po=H({},di,{screenX:0,screenY:0,clientX:0,clientY:0,pageX:0,pageY:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,getModifierState:Ur,button:0,buttons:0,relatedTarget:function(e){return e.relatedTarget===void 0?e.fromElement===e.srcElement?e.toElement:e.fromElement:e.relatedTarget},movementX:function(e){return"movementX"in e?e.movementX:(e!==hi&&(hi&&e.type==="mousemove"?(Vr=e.screenX-hi.screenX,jr=e.screenY-hi.screenY):jr=Vr=0,hi=e),Vr)},movementY:function(e){return"movementY"in e?e.movementY:jr}}),Sh=gt(Po),Vw=H({},Po,{dataTransfer:0}),jw=gt(Vw),Ow=H({},di,{relatedTarget:0}),Or=gt(Ow),Uw=H({},ha,{animationName:0,elapsedTime:0,pseudoElement:0}),Fw=gt(Uw),Nw=H({},ha,{clipboardData:function(e){return"clipboardData"in e?e.clipboardData:window.clipboardData}}),qw=gt(Nw),Ww=H({},ha,{data:0}),Th=gt(Ww),zw={Esc:"Escape",Spacebar:" ",Left:"ArrowLeft",Up:"ArrowUp",Right:"ArrowRight",Down:"ArrowDown",Del:"Delete",Win:"OS",Menu:"ContextMenu",Apps:"ContextMenu",Scroll:"ScrollLock",MozPrintableKey:"Unidentified"},Bw={8:"Backspace",9:"Tab",12:"Clear",13:"Enter",16:"Shift",17:"Control",18:"Alt",19:"Pause",20:"CapsLock",27:"Escape",32:" ",33:"PageUp",34:"PageDown",35:"End",36:"Home",37:"ArrowLeft",38:"ArrowUp",39:"ArrowRight",40:"ArrowDown",45:"Insert",46:"Delete",112:"F1",113:"F2",114:"F3",115:"F4",116:"F5",117:"F6",118:"F7",119:"F8",120:"F9",121:"F10",122:"F11",123:"F12",144:"NumLock",145:"ScrollLock",224:"Meta"},Hw={Alt:"altKey",Control:"ctrlKey",Meta:"metaKey",Shift:"shiftKey"};function _w(e){var n=this.nativeEvent;return n.getModifierState?n.getModifierState(e):(e=Hw[e])?!!n[e]:!1}function Ur(){return _w}var Kw=H({},di,{key:function(e){if(e.key){var n=zw[e.key]||e.key;if(n!=="Unidentified")return n}return e.type==="keypress"?(e=Ao(e),e===13?"Enter":String.fromCharCode(e)):e.type==="keydown"||e.type==="keyup"?Bw[e.keyCode]||"Unidentified":""},code:0,location:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,repeat:0,locale:0,getModifierState:Ur,charCode:function(e){return e.type==="keypress"?Ao(e):0},keyCode:function(e){return e.type==="keydown"||e.type==="keyup"?e.keyCode:0},which:function(e){return e.type==="keypress"?Ao(e):e.type==="keydown"||e.type==="keyup"?e.keyCode:0}}),Jw=gt(Kw),Gw=H({},Po,{pointerId:0,width:0,height:0,pressure:0,tangentialPressure:0,tiltX:0,tiltY:0,twist:0,pointerType:0,isPrimary:0}),Ch=gt(Gw),$w=H({},di,{touches:0,targetTouches:0,changedTouches:0,altKey:0,metaKey:0,ctrlKey:0,shiftKey:0,getModifierState:Ur}),Yw=gt($w),Qw=H({},ha,{propertyName:0,elapsedTime:0,pseudoElement:0}),Xw=gt(Qw),Zw=H({},Po,{deltaX:function(e){return"deltaX"in e?e.deltaX:"wheelDeltaX"in e?-e.wheelDeltaX:0},deltaY:function(e){return"deltaY"in e?e.deltaY:"wheelDeltaY"in e?-e.wheelDeltaY:"wheelDelta"in e?-e.wheelDelta:0},deltaZ:0,deltaMode:0}),ev=gt(Zw),tv=[9,13,27,32],Fr=p&&"CompositionEvent"in window,ui=null;p&&"documentMode"in document&&(ui=document.documentMode);var nv=p&&"TextEvent"in window&&!ui,Mh=p&&(!Fr||ui&&8<ui&&11>=ui),Ih=" ",Ah=!1;function Eh(e,n){switch(e){case"keyup":return tv.indexOf(n.keyCode)!==-1;case"keydown":return n.keyCode!==229;case"keypress":case"mousedown":case"focusout":return!0;default:return!1}}function Ph(e){return e=e.detail,typeof e=="object"&&"data"in e?e.data:null}var ua=!1;function av(e,n){switch(e){case"compositionend":return Ph(n);case"keypress":return n.which!==32?null:(Ah=!0,Ih);case"textInput":return e=n.data,e===Ih&&Ah?null:e;default:return null}}function iv(e,n){if(ua)return e==="compositionend"||!Fr&&Eh(e,n)?(e=kh(),Io=Lr=yn=null,ua=!1,e):null;switch(e){case"paste":return null;case"keypress":if(!(n.ctrlKey||n.altKey||n.metaKey)||n.ctrlKey&&n.altKey){if(n.char&&1<n.char.length)return n.char;if(n.which)return String.fromCharCode(n.which)}return null;case"compositionend":return Mh&&n.locale!=="ko"?null:n.data;default:return null}}var ov={color:!0,date:!0,datetime:!0,"datetime-local":!0,email:!0,month:!0,number:!0,password:!0,range:!0,search:!0,tel:!0,text:!0,time:!0,url:!0,week:!0};function Rh(e){var n=e&&e.nodeName&&e.nodeName.toLowerCase();return n==="input"?!!ov[e.type]:n==="textarea"}function Lh(e,n,o,l){Xd(l),n=jo(n,"onChange"),0<n.length&&(o=new Dr("onChange","change",null,o,l),e.push({event:o,listeners:n}))}var pi=null,mi=null;function sv(e){Yh(e,0)}function Ro(e){var n=ya(e);if(it(n))return e}function rv(e,n){if(e==="change")return n}var Dh=!1;if(p){var Nr;if(p){var qr="oninput"in document;if(!qr){var Vh=document.createElement("div");Vh.setAttribute("oninput","return;"),qr=typeof Vh.oninput=="function"}Nr=qr}else Nr=!1;Dh=Nr&&(!document.documentMode||9<document.documentMode)}function jh(){pi&&(pi.detachEvent("onpropertychange",Oh),mi=pi=null)}function Oh(e){if(e.propertyName==="value"&&Ro(mi)){var n=[];Lh(n,mi,e,vr(e)),nh(sv,n)}}function lv(e,n,o){e==="focusin"?(jh(),pi=n,mi=o,pi.attachEvent("onpropertychange",Oh)):e==="focusout"&&jh()}function cv(e){if(e==="selectionchange"||e==="keyup"||e==="keydown")return Ro(mi)}function dv(e,n){if(e==="click")return Ro(n)}function hv(e,n){if(e==="input"||e==="change")return Ro(n)}function uv(e,n){return e===n&&(e!==0||1/e===1/n)||e!==e&&n!==n}var Dt=typeof Object.is=="function"?Object.is:uv;function fi(e,n){if(Dt(e,n))return!0;if(typeof e!="object"||e===null||typeof n!="object"||n===null)return!1;var o=Object.keys(e),l=Object.keys(n);if(o.length!==l.length)return!1;for(l=0;l<o.length;l++){var d=o[l];if(!m.call(n,d)||!Dt(e[d],n[d]))return!1}return!0}function Uh(e){for(;e&&e.firstChild;)e=e.firstChild;return e}function Fh(e,n){var o=Uh(e);e=0;for(var l;o;){if(o.nodeType===3){if(l=e+o.textContent.length,e<=n&&l>=n)return{node:o,offset:n-e};e=l}e:{for(;o;){if(o.nextSibling){o=o.nextSibling;break e}o=o.parentNode}o=void 0}o=Uh(o)}}function Nh(e,n){return e&&n?e===n?!0:e&&e.nodeType===3?!1:n&&n.nodeType===3?Nh(e,n.parentNode):"contains"in e?e.contains(n):e.compareDocumentPosition?!!(e.compareDocumentPosition(n)&16):!1:!1}function qh(){for(var e=window,n=Rt();n instanceof e.HTMLIFrameElement;){try{var o=typeof n.contentWindow.location.href=="string"}catch{o=!1}if(o)e=n.contentWindow;else break;n=Rt(e.document)}return n}function Wr(e){var n=e&&e.nodeName&&e.nodeName.toLowerCase();return n&&(n==="input"&&(e.type==="text"||e.type==="search"||e.type==="tel"||e.type==="url"||e.type==="password")||n==="textarea"||e.contentEditable==="true")}function pv(e){var n=qh(),o=e.focusedElem,l=e.selectionRange;if(n!==o&&o&&o.ownerDocument&&Nh(o.ownerDocument.documentElement,o)){if(l!==null&&Wr(o)){if(n=l.start,e=l.end,e===void 0&&(e=n),"selectionStart"in o)o.selectionStart=n,o.selectionEnd=Math.min(e,o.value.length);else if(e=(n=o.ownerDocument||document)&&n.defaultView||window,e.getSelection){e=e.getSelection();var d=o.textContent.length,u=Math.min(l.start,d);l=l.end===void 0?u:Math.min(l.end,d),!e.extend&&u>l&&(d=l,l=u,u=d),d=Fh(o,u);var w=Fh(o,l);d&&w&&(e.rangeCount!==1||e.anchorNode!==d.node||e.anchorOffset!==d.offset||e.focusNode!==w.node||e.focusOffset!==w.offset)&&(n=n.createRange(),n.setStart(d.node,d.offset),e.removeAllRanges(),u>l?(e.addRange(n),e.extend(w.node,w.offset)):(n.setEnd(w.node,w.offset),e.addRange(n)))}}for(n=[],e=o;e=e.parentNode;)e.nodeType===1&&n.push({element:e,left:e.scrollLeft,top:e.scrollTop});for(typeof o.focus=="function"&&o.focus(),o=0;o<n.length;o++)e=n[o],e.element.scrollLeft=e.left,e.element.scrollTop=e.top}}var mv=p&&"documentMode"in document&&11>=document.documentMode,pa=null,zr=null,gi=null,Br=!1;function Wh(e,n,o){var l=o.window===o?o.document:o.nodeType===9?o:o.ownerDocument;Br||pa==null||pa!==Rt(l)||(l=pa,"selectionStart"in l&&Wr(l)?l={start:l.selectionStart,end:l.selectionEnd}:(l=(l.ownerDocument&&l.ownerDocument.defaultView||window).getSelection(),l={anchorNode:l.anchorNode,anchorOffset:l.anchorOffset,focusNode:l.focusNode,focusOffset:l.focusOffset}),gi&&fi(gi,l)||(gi=l,l=jo(zr,"onSelect"),0<l.length&&(n=new Dr("onSelect","select",null,n,o),e.push({event:n,listeners:l}),n.target=pa)))}function Lo(e,n){var o={};return o[e.toLowerCase()]=n.toLowerCase(),o["Webkit"+e]="webkit"+n,o["Moz"+e]="moz"+n,o}var ma={animationend:Lo("Animation","AnimationEnd"),animationiteration:Lo("Animation","AnimationIteration"),animationstart:Lo("Animation","AnimationStart"),transitionend:Lo("Transition","TransitionEnd")},Hr={},zh={};p&&(zh=document.createElement("div").style,"AnimationEvent"in window||(delete ma.animationend.animation,delete ma.animationiteration.animation,delete ma.animationstart.animation),"TransitionEvent"in window||delete ma.transitionend.transition);function Do(e){if(Hr[e])return Hr[e];if(!ma[e])return e;var n=ma[e],o;for(o in n)if(n.hasOwnProperty(o)&&o in zh)return Hr[e]=n[o];return e}var Bh=Do("animationend"),Hh=Do("animationiteration"),_h=Do("animationstart"),Kh=Do("transitionend"),Jh=new Map,Gh="abort auxClick cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(" ");function wn(e,n){Jh.set(e,n),c(n,[e])}for(var _r=0;_r<Gh.length;_r++){var Kr=Gh[_r],fv=Kr.toLowerCase(),gv=Kr[0].toUpperCase()+Kr.slice(1);wn(fv,"on"+gv)}wn(Bh,"onAnimationEnd"),wn(Hh,"onAnimationIteration"),wn(_h,"onAnimationStart"),wn("dblclick","onDoubleClick"),wn("focusin","onFocus"),wn("focusout","onBlur"),wn(Kh,"onTransitionEnd"),h("onMouseEnter",["mouseout","mouseover"]),h("onMouseLeave",["mouseout","mouseover"]),h("onPointerEnter",["pointerout","pointerover"]),h("onPointerLeave",["pointerout","pointerover"]),c("onChange","change click focusin focusout input keydown keyup selectionchange".split(" ")),c("onSelect","focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(" ")),c("onBeforeInput",["compositionend","keypress","textInput","paste"]),c("onCompositionEnd","compositionend focusout keydown keypress keyup mousedown".split(" ")),c("onCompositionStart","compositionstart focusout keydown keypress keyup mousedown".split(" ")),c("onCompositionUpdate","compositionupdate focusout keydown keypress keyup mousedown".split(" "));var yi="abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(" "),yv=new Set("cancel close invalid load scroll toggle".split(" ").concat(yi));function $h(e,n,o){var l=e.type||"unknown-event";e.currentTarget=o,fw(l,n,void 0,e),e.currentTarget=null}function Yh(e,n){n=(n&4)!==0;for(var o=0;o<e.length;o++){var l=e[o],d=l.event;l=l.listeners;e:{var u=void 0;if(n)for(var w=l.length-1;0<=w;w--){var k=l[w],S=k.instance,D=k.currentTarget;if(k=k.listener,S!==u&&d.isPropagationStopped())break e;$h(d,k,D),u=S}else for(w=0;w<l.length;w++){if(k=l[w],S=k.instance,D=k.currentTarget,k=k.listener,S!==u&&d.isPropagationStopped())break e;$h(d,k,D),u=S}}}if(yo)throw e=Sr,yo=!1,Sr=null,e}function xe(e,n){var o=n[el];o===void 0&&(o=n[el]=new Set);var l=e+"__bubble";o.has(l)||(Qh(n,e,2,!1),o.add(l))}function Jr(e,n,o){var l=0;n&&(l|=4),Qh(o,e,l,n)}var Vo="_reactListening"+Math.random().toString(36).slice(2);function wi(e){if(!e[Vo]){e[Vo]=!0,s.forEach(function(o){o!=="selectionchange"&&(yv.has(o)||Jr(o,!1,e),Jr(o,!0,e))});var n=e.nodeType===9?e:e.ownerDocument;n===null||n[Vo]||(n[Vo]=!0,Jr("selectionchange",!1,n))}}function Qh(e,n,o,l){switch(bh(n)){case 1:var d=Rw;break;case 4:d=Lw;break;default:d=Pr}o=d.bind(null,n,o,e),d=void 0,!xr||n!=="touchstart"&&n!=="touchmove"&&n!=="wheel"||(d=!0),l?d!==void 0?e.addEventListener(n,o,{capture:!0,passive:d}):e.addEventListener(n,o,!0):d!==void 0?e.addEventListener(n,o,{passive:d}):e.addEventListener(n,o,!1)}function Gr(e,n,o,l,d){var u=l;if((n&1)===0&&(n&2)===0&&l!==null)e:for(;;){if(l===null)return;var w=l.tag;if(w===3||w===4){var k=l.stateNode.containerInfo;if(k===d||k.nodeType===8&&k.parentNode===d)break;if(w===4)for(w=l.return;w!==null;){var S=w.tag;if((S===3||S===4)&&(S=w.stateNode.containerInfo,S===d||S.nodeType===8&&S.parentNode===d))return;w=w.return}for(;k!==null;){if(w=Nn(k),w===null)return;if(S=w.tag,S===5||S===6){l=u=w;continue e}k=k.parentNode}}l=l.return}nh(function(){var D=u,F=vr(o),q=[];e:{var U=Jh.get(e);if(U!==void 0){var K=Dr,$=e;switch(e){case"keypress":if(Ao(o)===0)break e;case"keydown":case"keyup":K=Jw;break;case"focusin":$="focus",K=Or;break;case"focusout":$="blur",K=Or;break;case"beforeblur":case"afterblur":K=Or;break;case"click":if(o.button===2)break e;case"auxclick":case"dblclick":case"mousedown":case"mousemove":case"mouseup":case"mouseout":case"mouseover":case"contextmenu":K=Sh;break;case"drag":case"dragend":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"dragstart":case"drop":K=jw;break;case"touchcancel":case"touchend":case"touchmove":case"touchstart":K=Yw;break;case Bh:case Hh:case _h:K=Fw;break;case Kh:K=Xw;break;case"scroll":K=Dw;break;case"wheel":K=ev;break;case"copy":case"cut":case"paste":K=qw;break;case"gotpointercapture":case"lostpointercapture":case"pointercancel":case"pointerdown":case"pointermove":case"pointerout":case"pointerover":case"pointerup":K=Ch}var Y=(n&4)!==0,Oe=!Y&&e==="scroll",P=Y?U!==null?U+"Capture":null:U;Y=[];for(var C=D,L;C!==null;){L=C;var z=L.stateNode;if(L.tag===5&&z!==null&&(L=z,P!==null&&(z=ei(C,P),z!=null&&Y.push(vi(C,z,L)))),Oe)break;C=C.return}0<Y.length&&(U=new K(U,$,null,o,F),q.push({event:U,listeners:Y}))}}if((n&7)===0){e:{if(U=e==="mouseover"||e==="pointerover",K=e==="mouseout"||e==="pointerout",U&&o!==wr&&($=o.relatedTarget||o.fromElement)&&(Nn($)||$[tn]))break e;if((K||U)&&(U=F.window===F?F:(U=F.ownerDocument)?U.defaultView||U.parentWindow:window,K?($=o.relatedTarget||o.toElement,K=D,$=$?Nn($):null,$!==null&&(Oe=Fn($),$!==Oe||$.tag!==5&&$.tag!==6)&&($=null)):(K=null,$=D),K!==$)){if(Y=Sh,z="onMouseLeave",P="onMouseEnter",C="mouse",(e==="pointerout"||e==="pointerover")&&(Y=Ch,z="onPointerLeave",P="onPointerEnter",C="pointer"),Oe=K==null?U:ya(K),L=$==null?U:ya($),U=new Y(z,C+"leave",K,o,F),U.target=Oe,U.relatedTarget=L,z=null,Nn(F)===D&&(Y=new Y(P,C+"enter",$,o,F),Y.target=L,Y.relatedTarget=Oe,z=Y),Oe=z,K&&$)t:{for(Y=K,P=$,C=0,L=Y;L;L=fa(L))C++;for(L=0,z=P;z;z=fa(z))L++;for(;0<C-L;)Y=fa(Y),C--;for(;0<L-C;)P=fa(P),L--;for(;C--;){if(Y===P||P!==null&&Y===P.alternate)break t;Y=fa(Y),P=fa(P)}Y=null}else Y=null;K!==null&&Xh(q,U,K,Y,!1),$!==null&&Oe!==null&&Xh(q,Oe,$,Y,!0)}}e:{if(U=D?ya(D):window,K=U.nodeName&&U.nodeName.toLowerCase(),K==="select"||K==="input"&&U.type==="file")var Q=rv;else if(Rh(U))if(Dh)Q=hv;else{Q=cv;var ee=lv}else(K=U.nodeName)&&K.toLowerCase()==="input"&&(U.type==="checkbox"||U.type==="radio")&&(Q=dv);if(Q&&(Q=Q(e,D))){Lh(q,Q,o,F);break e}ee&&ee(e,U,D),e==="focusout"&&(ee=U._wrapperState)&&ee.controlled&&U.type==="number"&&pr(U,"number",U.value)}switch(ee=D?ya(D):window,e){case"focusin":(Rh(ee)||ee.contentEditable==="true")&&(pa=ee,zr=D,gi=null);break;case"focusout":gi=zr=pa=null;break;case"mousedown":Br=!0;break;case"contextmenu":case"mouseup":case"dragend":Br=!1,Wh(q,o,F);break;case"selectionchange":if(mv)break;case"keydown":case"keyup":Wh(q,o,F)}var te;if(Fr)e:{switch(e){case"compositionstart":var ie="onCompositionStart";break e;case"compositionend":ie="onCompositionEnd";break e;case"compositionupdate":ie="onCompositionUpdate";break e}ie=void 0}else ua?Eh(e,o)&&(ie="onCompositionEnd"):e==="keydown"&&o.keyCode===229&&(ie="onCompositionStart");ie&&(Mh&&o.locale!=="ko"&&(ua||ie!=="onCompositionStart"?ie==="onCompositionEnd"&&ua&&(te=kh()):(yn=F,Lr="value"in yn?yn.value:yn.textContent,ua=!0)),ee=jo(D,ie),0<ee.length&&(ie=new Th(ie,e,null,o,F),q.push({event:ie,listeners:ee}),te?ie.data=te:(te=Ph(o),te!==null&&(ie.data=te)))),(te=nv?av(e,o):iv(e,o))&&(D=jo(D,"onBeforeInput"),0<D.length&&(F=new Th("onBeforeInput","beforeinput",null,o,F),q.push({event:F,listeners:D}),F.data=te))}Yh(q,n)})}function vi(e,n,o){return{instance:e,listener:n,currentTarget:o}}function jo(e,n){for(var o=n+"Capture",l=[];e!==null;){var d=e,u=d.stateNode;d.tag===5&&u!==null&&(d=u,u=ei(e,o),u!=null&&l.unshift(vi(e,u,d)),u=ei(e,n),u!=null&&l.push(vi(e,u,d))),e=e.return}return l}function fa(e){if(e===null)return null;do e=e.return;while(e&&e.tag!==5);return e||null}function Xh(e,n,o,l,d){for(var u=n._reactName,w=[];o!==null&&o!==l;){var k=o,S=k.alternate,D=k.stateNode;if(S!==null&&S===l)break;k.tag===5&&D!==null&&(k=D,d?(S=ei(o,u),S!=null&&w.unshift(vi(o,S,k))):d||(S=ei(o,u),S!=null&&w.push(vi(o,S,k)))),o=o.return}w.length!==0&&e.push({event:n,listeners:w})}var wv=/\r\n?/g,vv=/\u0000|\uFFFD/g;function Zh(e){return(typeof e=="string"?e:""+e).replace(wv,`
`).replace(vv,"")}function Oo(e,n,o){if(n=Zh(n),Zh(e)!==n&&o)throw Error(i(425))}function Uo(){}var $r=null,Yr=null;function Qr(e,n){return e==="textarea"||e==="noscript"||typeof n.children=="string"||typeof n.children=="number"||typeof n.dangerouslySetInnerHTML=="object"&&n.dangerouslySetInnerHTML!==null&&n.dangerouslySetInnerHTML.__html!=null}var Xr=typeof setTimeout=="function"?setTimeout:void 0,bv=typeof clearTimeout=="function"?clearTimeout:void 0,eu=typeof Promise=="function"?Promise:void 0,kv=typeof queueMicrotask=="function"?queueMicrotask:typeof eu<"u"?function(e){return eu.resolve(null).then(e).catch(xv)}:Xr;function xv(e){setTimeout(function(){throw e})}function Zr(e,n){var o=n,l=0;do{var d=o.nextSibling;if(e.removeChild(o),d&&d.nodeType===8)if(o=d.data,o==="/$"){if(l===0){e.removeChild(d),ci(n);return}l--}else o!=="$"&&o!=="$?"&&o!=="$!"||l++;o=d}while(o);ci(n)}function vn(e){for(;e!=null;e=e.nextSibling){var n=e.nodeType;if(n===1||n===3)break;if(n===8){if(n=e.data,n==="$"||n==="$!"||n==="$?")break;if(n==="/$")return null}}return e}function tu(e){e=e.previousSibling;for(var n=0;e;){if(e.nodeType===8){var o=e.data;if(o==="$"||o==="$!"||o==="$?"){if(n===0)return e;n--}else o==="/$"&&n++}e=e.previousSibling}return null}var ga=Math.random().toString(36).slice(2),Ht="__reactFiber$"+ga,bi="__reactProps$"+ga,tn="__reactContainer$"+ga,el="__reactEvents$"+ga,Sv="__reactListeners$"+ga,Tv="__reactHandles$"+ga;function Nn(e){var n=e[Ht];if(n)return n;for(var o=e.parentNode;o;){if(n=o[tn]||o[Ht]){if(o=n.alternate,n.child!==null||o!==null&&o.child!==null)for(e=tu(e);e!==null;){if(o=e[Ht])return o;e=tu(e)}return n}e=o,o=e.parentNode}return null}function ki(e){return e=e[Ht]||e[tn],!e||e.tag!==5&&e.tag!==6&&e.tag!==13&&e.tag!==3?null:e}function ya(e){if(e.tag===5||e.tag===6)return e.stateNode;throw Error(i(33))}function Fo(e){return e[bi]||null}var tl=[],wa=-1;function bn(e){return{current:e}}function Se(e){0>wa||(e.current=tl[wa],tl[wa]=null,wa--)}function ke(e,n){wa++,tl[wa]=e.current,e.current=n}var kn={},Ze=bn(kn),lt=bn(!1),qn=kn;function va(e,n){var o=e.type.contextTypes;if(!o)return kn;var l=e.stateNode;if(l&&l.__reactInternalMemoizedUnmaskedChildContext===n)return l.__reactInternalMemoizedMaskedChildContext;var d={},u;for(u in o)d[u]=n[u];return l&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=n,e.__reactInternalMemoizedMaskedChildContext=d),d}function ct(e){return e=e.childContextTypes,e!=null}function No(){Se(lt),Se(Ze)}function nu(e,n,o){if(Ze.current!==kn)throw Error(i(168));ke(Ze,n),ke(lt,o)}function au(e,n,o){var l=e.stateNode;if(n=n.childContextTypes,typeof l.getChildContext!="function")return o;l=l.getChildContext();for(var d in l)if(!(d in n))throw Error(i(108,ge(e)||"Unknown",d));return H({},o,l)}function qo(e){return e=(e=e.stateNode)&&e.__reactInternalMemoizedMergedChildContext||kn,qn=Ze.current,ke(Ze,e),ke(lt,lt.current),!0}function iu(e,n,o){var l=e.stateNode;if(!l)throw Error(i(169));o?(e=au(e,n,qn),l.__reactInternalMemoizedMergedChildContext=e,Se(lt),Se(Ze),ke(Ze,e)):Se(lt),ke(lt,o)}var nn=null,Wo=!1,nl=!1;function ou(e){nn===null?nn=[e]:nn.push(e)}function Cv(e){Wo=!0,ou(e)}function xn(){if(!nl&&nn!==null){nl=!0;var e=0,n=ye;try{var o=nn;for(ye=1;e<o.length;e++){var l=o[e];do l=l(!0);while(l!==null)}nn=null,Wo=!1}catch(d){throw nn!==null&&(nn=nn.slice(e+1)),rh(Tr,xn),d}finally{ye=n,nl=!1}}return null}var ba=[],ka=0,zo=null,Bo=0,xt=[],St=0,Wn=null,an=1,on="";function zn(e,n){ba[ka++]=Bo,ba[ka++]=zo,zo=e,Bo=n}function su(e,n,o){xt[St++]=an,xt[St++]=on,xt[St++]=Wn,Wn=e;var l=an;e=on;var d=32-Lt(l)-1;l&=~(1<<d),o+=1;var u=32-Lt(n)+d;if(30<u){var w=d-d%5;u=(l&(1<<w)-1).toString(32),l>>=w,d-=w,an=1<<32-Lt(n)+d|o<<d|l,on=u+e}else an=1<<u|o<<d|l,on=e}function al(e){e.return!==null&&(zn(e,1),su(e,1,0))}function il(e){for(;e===zo;)zo=ba[--ka],ba[ka]=null,Bo=ba[--ka],ba[ka]=null;for(;e===Wn;)Wn=xt[--St],xt[St]=null,on=xt[--St],xt[St]=null,an=xt[--St],xt[St]=null}var yt=null,wt=null,Te=!1,Vt=null;function ru(e,n){var o=It(5,null,null,0);o.elementType="DELETED",o.stateNode=n,o.return=e,n=e.deletions,n===null?(e.deletions=[o],e.flags|=16):n.push(o)}function lu(e,n){switch(e.tag){case 5:var o=e.type;return n=n.nodeType!==1||o.toLowerCase()!==n.nodeName.toLowerCase()?null:n,n!==null?(e.stateNode=n,yt=e,wt=vn(n.firstChild),!0):!1;case 6:return n=e.pendingProps===""||n.nodeType!==3?null:n,n!==null?(e.stateNode=n,yt=e,wt=null,!0):!1;case 13:return n=n.nodeType!==8?null:n,n!==null?(o=Wn!==null?{id:an,overflow:on}:null,e.memoizedState={dehydrated:n,treeContext:o,retryLane:1073741824},o=It(18,null,null,0),o.stateNode=n,o.return=e,e.child=o,yt=e,wt=null,!0):!1;default:return!1}}function ol(e){return(e.mode&1)!==0&&(e.flags&128)===0}function sl(e){if(Te){var n=wt;if(n){var o=n;if(!lu(e,n)){if(ol(e))throw Error(i(418));n=vn(o.nextSibling);var l=yt;n&&lu(e,n)?ru(l,o):(e.flags=e.flags&-4097|2,Te=!1,yt=e)}}else{if(ol(e))throw Error(i(418));e.flags=e.flags&-4097|2,Te=!1,yt=e}}}function cu(e){for(e=e.return;e!==null&&e.tag!==5&&e.tag!==3&&e.tag!==13;)e=e.return;yt=e}function Ho(e){if(e!==yt)return!1;if(!Te)return cu(e),Te=!0,!1;var n;if((n=e.tag!==3)&&!(n=e.tag!==5)&&(n=e.type,n=n!=="head"&&n!=="body"&&!Qr(e.type,e.memoizedProps)),n&&(n=wt)){if(ol(e))throw du(),Error(i(418));for(;n;)ru(e,n),n=vn(n.nextSibling)}if(cu(e),e.tag===13){if(e=e.memoizedState,e=e!==null?e.dehydrated:null,!e)throw Error(i(317));e:{for(e=e.nextSibling,n=0;e;){if(e.nodeType===8){var o=e.data;if(o==="/$"){if(n===0){wt=vn(e.nextSibling);break e}n--}else o!=="$"&&o!=="$!"&&o!=="$?"||n++}e=e.nextSibling}wt=null}}else wt=yt?vn(e.stateNode.nextSibling):null;return!0}function du(){for(var e=wt;e;)e=vn(e.nextSibling)}function xa(){wt=yt=null,Te=!1}function rl(e){Vt===null?Vt=[e]:Vt.push(e)}var Mv=B.ReactCurrentBatchConfig;function xi(e,n,o){if(e=o.ref,e!==null&&typeof e!="function"&&typeof e!="object"){if(o._owner){if(o=o._owner,o){if(o.tag!==1)throw Error(i(309));var l=o.stateNode}if(!l)throw Error(i(147,e));var d=l,u=""+e;return n!==null&&n.ref!==null&&typeof n.ref=="function"&&n.ref._stringRef===u?n.ref:(n=function(w){var k=d.refs;w===null?delete k[u]:k[u]=w},n._stringRef=u,n)}if(typeof e!="string")throw Error(i(284));if(!o._owner)throw Error(i(290,e))}return e}function _o(e,n){throw e=Object.prototype.toString.call(n),Error(i(31,e==="[object Object]"?"object with keys {"+Object.keys(n).join(", ")+"}":e))}function hu(e){var n=e._init;return n(e._payload)}function uu(e){function n(P,C){if(e){var L=P.deletions;L===null?(P.deletions=[C],P.flags|=16):L.push(C)}}function o(P,C){if(!e)return null;for(;C!==null;)n(P,C),C=C.sibling;return null}function l(P,C){for(P=new Map;C!==null;)C.key!==null?P.set(C.key,C):P.set(C.index,C),C=C.sibling;return P}function d(P,C){return P=Pn(P,C),P.index=0,P.sibling=null,P}function u(P,C,L){return P.index=L,e?(L=P.alternate,L!==null?(L=L.index,L<C?(P.flags|=2,C):L):(P.flags|=2,C)):(P.flags|=1048576,C)}function w(P){return e&&P.alternate===null&&(P.flags|=2),P}function k(P,C,L,z){return C===null||C.tag!==6?(C=Xl(L,P.mode,z),C.return=P,C):(C=d(C,L),C.return=P,C)}function S(P,C,L,z){var Q=L.type;return Q===ae?F(P,C,L.props.children,z,L.key):C!==null&&(C.elementType===Q||typeof Q=="object"&&Q!==null&&Q.$$typeof===Ke&&hu(Q)===C.type)?(z=d(C,L.props),z.ref=xi(P,C,L),z.return=P,z):(z=fs(L.type,L.key,L.props,null,P.mode,z),z.ref=xi(P,C,L),z.return=P,z)}function D(P,C,L,z){return C===null||C.tag!==4||C.stateNode.containerInfo!==L.containerInfo||C.stateNode.implementation!==L.implementation?(C=Zl(L,P.mode,z),C.return=P,C):(C=d(C,L.children||[]),C.return=P,C)}function F(P,C,L,z,Q){return C===null||C.tag!==7?(C=Yn(L,P.mode,z,Q),C.return=P,C):(C=d(C,L),C.return=P,C)}function q(P,C,L){if(typeof C=="string"&&C!==""||typeof C=="number")return C=Xl(""+C,P.mode,L),C.return=P,C;if(typeof C=="object"&&C!==null){switch(C.$$typeof){case N:return L=fs(C.type,C.key,C.props,null,P.mode,L),L.ref=xi(P,null,C),L.return=P,L;case _:return C=Zl(C,P.mode,L),C.return=P,C;case Ke:var z=C._init;return q(P,z(C._payload),L)}if(Qa(C)||Z(C))return C=Yn(C,P.mode,L,null),C.return=P,C;_o(P,C)}return null}function U(P,C,L,z){var Q=C!==null?C.key:null;if(typeof L=="string"&&L!==""||typeof L=="number")return Q!==null?null:k(P,C,""+L,z);if(typeof L=="object"&&L!==null){switch(L.$$typeof){case N:return L.key===Q?S(P,C,L,z):null;case _:return L.key===Q?D(P,C,L,z):null;case Ke:return Q=L._init,U(P,C,Q(L._payload),z)}if(Qa(L)||Z(L))return Q!==null?null:F(P,C,L,z,null);_o(P,L)}return null}function K(P,C,L,z,Q){if(typeof z=="string"&&z!==""||typeof z=="number")return P=P.get(L)||null,k(C,P,""+z,Q);if(typeof z=="object"&&z!==null){switch(z.$$typeof){case N:return P=P.get(z.key===null?L:z.key)||null,S(C,P,z,Q);case _:return P=P.get(z.key===null?L:z.key)||null,D(C,P,z,Q);case Ke:var ee=z._init;return K(P,C,L,ee(z._payload),Q)}if(Qa(z)||Z(z))return P=P.get(L)||null,F(C,P,z,Q,null);_o(C,z)}return null}function $(P,C,L,z){for(var Q=null,ee=null,te=C,ie=C=0,$e=null;te!==null&&ie<L.length;ie++){te.index>ie?($e=te,te=null):$e=te.sibling;var me=U(P,te,L[ie],z);if(me===null){te===null&&(te=$e);break}e&&te&&me.alternate===null&&n(P,te),C=u(me,C,ie),ee===null?Q=me:ee.sibling=me,ee=me,te=$e}if(ie===L.length)return o(P,te),Te&&zn(P,ie),Q;if(te===null){for(;ie<L.length;ie++)te=q(P,L[ie],z),te!==null&&(C=u(te,C,ie),ee===null?Q=te:ee.sibling=te,ee=te);return Te&&zn(P,ie),Q}for(te=l(P,te);ie<L.length;ie++)$e=K(te,P,ie,L[ie],z),$e!==null&&(e&&$e.alternate!==null&&te.delete($e.key===null?ie:$e.key),C=u($e,C,ie),ee===null?Q=$e:ee.sibling=$e,ee=$e);return e&&te.forEach(function(Rn){return n(P,Rn)}),Te&&zn(P,ie),Q}function Y(P,C,L,z){var Q=Z(L);if(typeof Q!="function")throw Error(i(150));if(L=Q.call(L),L==null)throw Error(i(151));for(var ee=Q=null,te=C,ie=C=0,$e=null,me=L.next();te!==null&&!me.done;ie++,me=L.next()){te.index>ie?($e=te,te=null):$e=te.sibling;var Rn=U(P,te,me.value,z);if(Rn===null){te===null&&(te=$e);break}e&&te&&Rn.alternate===null&&n(P,te),C=u(Rn,C,ie),ee===null?Q=Rn:ee.sibling=Rn,ee=Rn,te=$e}if(me.done)return o(P,te),Te&&zn(P,ie),Q;if(te===null){for(;!me.done;ie++,me=L.next())me=q(P,me.value,z),me!==null&&(C=u(me,C,ie),ee===null?Q=me:ee.sibling=me,ee=me);return Te&&zn(P,ie),Q}for(te=l(P,te);!me.done;ie++,me=L.next())me=K(te,P,ie,me.value,z),me!==null&&(e&&me.alternate!==null&&te.delete(me.key===null?ie:me.key),C=u(me,C,ie),ee===null?Q=me:ee.sibling=me,ee=me);return e&&te.forEach(function(ob){return n(P,ob)}),Te&&zn(P,ie),Q}function Oe(P,C,L,z){if(typeof L=="object"&&L!==null&&L.type===ae&&L.key===null&&(L=L.props.children),typeof L=="object"&&L!==null){switch(L.$$typeof){case N:e:{for(var Q=L.key,ee=C;ee!==null;){if(ee.key===Q){if(Q=L.type,Q===ae){if(ee.tag===7){o(P,ee.sibling),C=d(ee,L.props.children),C.return=P,P=C;break e}}else if(ee.elementType===Q||typeof Q=="object"&&Q!==null&&Q.$$typeof===Ke&&hu(Q)===ee.type){o(P,ee.sibling),C=d(ee,L.props),C.ref=xi(P,ee,L),C.return=P,P=C;break e}o(P,ee);break}else n(P,ee);ee=ee.sibling}L.type===ae?(C=Yn(L.props.children,P.mode,z,L.key),C.return=P,P=C):(z=fs(L.type,L.key,L.props,null,P.mode,z),z.ref=xi(P,C,L),z.return=P,P=z)}return w(P);case _:e:{for(ee=L.key;C!==null;){if(C.key===ee)if(C.tag===4&&C.stateNode.containerInfo===L.containerInfo&&C.stateNode.implementation===L.implementation){o(P,C.sibling),C=d(C,L.children||[]),C.return=P,P=C;break e}else{o(P,C);break}else n(P,C);C=C.sibling}C=Zl(L,P.mode,z),C.return=P,P=C}return w(P);case Ke:return ee=L._init,Oe(P,C,ee(L._payload),z)}if(Qa(L))return $(P,C,L,z);if(Z(L))return Y(P,C,L,z);_o(P,L)}return typeof L=="string"&&L!==""||typeof L=="number"?(L=""+L,C!==null&&C.tag===6?(o(P,C.sibling),C=d(C,L),C.return=P,P=C):(o(P,C),C=Xl(L,P.mode,z),C.return=P,P=C),w(P)):o(P,C)}return Oe}var Sa=uu(!0),pu=uu(!1),Ko=bn(null),Jo=null,Ta=null,ll=null;function cl(){ll=Ta=Jo=null}function dl(e){var n=Ko.current;Se(Ko),e._currentValue=n}function hl(e,n,o){for(;e!==null;){var l=e.alternate;if((e.childLanes&n)!==n?(e.childLanes|=n,l!==null&&(l.childLanes|=n)):l!==null&&(l.childLanes&n)!==n&&(l.childLanes|=n),e===o)break;e=e.return}}function Ca(e,n){Jo=e,ll=Ta=null,e=e.dependencies,e!==null&&e.firstContext!==null&&((e.lanes&n)!==0&&(dt=!0),e.firstContext=null)}function Tt(e){var n=e._currentValue;if(ll!==e)if(e={context:e,memoizedValue:n,next:null},Ta===null){if(Jo===null)throw Error(i(308));Ta=e,Jo.dependencies={lanes:0,firstContext:e}}else Ta=Ta.next=e;return n}var Bn=null;function ul(e){Bn===null?Bn=[e]:Bn.push(e)}function mu(e,n,o,l){var d=n.interleaved;return d===null?(o.next=o,ul(n)):(o.next=d.next,d.next=o),n.interleaved=o,sn(e,l)}function sn(e,n){e.lanes|=n;var o=e.alternate;for(o!==null&&(o.lanes|=n),o=e,e=e.return;e!==null;)e.childLanes|=n,o=e.alternate,o!==null&&(o.childLanes|=n),o=e,e=e.return;return o.tag===3?o.stateNode:null}var Sn=!1;function pl(e){e.updateQueue={baseState:e.memoizedState,firstBaseUpdate:null,lastBaseUpdate:null,shared:{pending:null,interleaved:null,lanes:0},effects:null}}function fu(e,n){e=e.updateQueue,n.updateQueue===e&&(n.updateQueue={baseState:e.baseState,firstBaseUpdate:e.firstBaseUpdate,lastBaseUpdate:e.lastBaseUpdate,shared:e.shared,effects:e.effects})}function rn(e,n){return{eventTime:e,lane:n,tag:0,payload:null,callback:null,next:null}}function Tn(e,n,o){var l=e.updateQueue;if(l===null)return null;if(l=l.shared,(ue&2)!==0){var d=l.pending;return d===null?n.next=n:(n.next=d.next,d.next=n),l.pending=n,sn(e,o)}return d=l.interleaved,d===null?(n.next=n,ul(l)):(n.next=d.next,d.next=n),l.interleaved=n,sn(e,o)}function Go(e,n,o){if(n=n.updateQueue,n!==null&&(n=n.shared,(o&4194240)!==0)){var l=n.lanes;l&=e.pendingLanes,o|=l,n.lanes=o,Ir(e,o)}}function gu(e,n){var o=e.updateQueue,l=e.alternate;if(l!==null&&(l=l.updateQueue,o===l)){var d=null,u=null;if(o=o.firstBaseUpdate,o!==null){do{var w={eventTime:o.eventTime,lane:o.lane,tag:o.tag,payload:o.payload,callback:o.callback,next:null};u===null?d=u=w:u=u.next=w,o=o.next}while(o!==null);u===null?d=u=n:u=u.next=n}else d=u=n;o={baseState:l.baseState,firstBaseUpdate:d,lastBaseUpdate:u,shared:l.shared,effects:l.effects},e.updateQueue=o;return}e=o.lastBaseUpdate,e===null?o.firstBaseUpdate=n:e.next=n,o.lastBaseUpdate=n}function $o(e,n,o,l){var d=e.updateQueue;Sn=!1;var u=d.firstBaseUpdate,w=d.lastBaseUpdate,k=d.shared.pending;if(k!==null){d.shared.pending=null;var S=k,D=S.next;S.next=null,w===null?u=D:w.next=D,w=S;var F=e.alternate;F!==null&&(F=F.updateQueue,k=F.lastBaseUpdate,k!==w&&(k===null?F.firstBaseUpdate=D:k.next=D,F.lastBaseUpdate=S))}if(u!==null){var q=d.baseState;w=0,F=D=S=null,k=u;do{var U=k.lane,K=k.eventTime;if((l&U)===U){F!==null&&(F=F.next={eventTime:K,lane:0,tag:k.tag,payload:k.payload,callback:k.callback,next:null});e:{var $=e,Y=k;switch(U=n,K=o,Y.tag){case 1:if($=Y.payload,typeof $=="function"){q=$.call(K,q,U);break e}q=$;break e;case 3:$.flags=$.flags&-65537|128;case 0:if($=Y.payload,U=typeof $=="function"?$.call(K,q,U):$,U==null)break e;q=H({},q,U);break e;case 2:Sn=!0}}k.callback!==null&&k.lane!==0&&(e.flags|=64,U=d.effects,U===null?d.effects=[k]:U.push(k))}else K={eventTime:K,lane:U,tag:k.tag,payload:k.payload,callback:k.callback,next:null},F===null?(D=F=K,S=q):F=F.next=K,w|=U;if(k=k.next,k===null){if(k=d.shared.pending,k===null)break;U=k,k=U.next,U.next=null,d.lastBaseUpdate=U,d.shared.pending=null}}while(!0);if(F===null&&(S=q),d.baseState=S,d.firstBaseUpdate=D,d.lastBaseUpdate=F,n=d.shared.interleaved,n!==null){d=n;do w|=d.lane,d=d.next;while(d!==n)}else u===null&&(d.shared.lanes=0);Kn|=w,e.lanes=w,e.memoizedState=q}}function yu(e,n,o){if(e=n.effects,n.effects=null,e!==null)for(n=0;n<e.length;n++){var l=e[n],d=l.callback;if(d!==null){if(l.callback=null,l=o,typeof d!="function")throw Error(i(191,d));d.call(l)}}}var Si={},_t=bn(Si),Ti=bn(Si),Ci=bn(Si);function Hn(e){if(e===Si)throw Error(i(174));return e}function ml(e,n){switch(ke(Ci,n),ke(Ti,e),ke(_t,Si),e=n.nodeType,e){case 9:case 11:n=(n=n.documentElement)?n.namespaceURI:fr(null,"");break;default:e=e===8?n.parentNode:n,n=e.namespaceURI||null,e=e.tagName,n=fr(n,e)}Se(_t),ke(_t,n)}function Ma(){Se(_t),Se(Ti),Se(Ci)}function wu(e){Hn(Ci.current);var n=Hn(_t.current),o=fr(n,e.type);n!==o&&(ke(Ti,e),ke(_t,o))}function fl(e){Ti.current===e&&(Se(_t),Se(Ti))}var Ae=bn(0);function Yo(e){for(var n=e;n!==null;){if(n.tag===13){var o=n.memoizedState;if(o!==null&&(o=o.dehydrated,o===null||o.data==="$?"||o.data==="$!"))return n}else if(n.tag===19&&n.memoizedProps.revealOrder!==void 0){if((n.flags&128)!==0)return n}else if(n.child!==null){n.child.return=n,n=n.child;continue}if(n===e)break;for(;n.sibling===null;){if(n.return===null||n.return===e)return null;n=n.return}n.sibling.return=n.return,n=n.sibling}return null}var gl=[];function yl(){for(var e=0;e<gl.length;e++)gl[e]._workInProgressVersionPrimary=null;gl.length=0}var Qo=B.ReactCurrentDispatcher,wl=B.ReactCurrentBatchConfig,_n=0,Ee=null,We=null,Je=null,Xo=!1,Mi=!1,Ii=0,Iv=0;function et(){throw Error(i(321))}function vl(e,n){if(n===null)return!1;for(var o=0;o<n.length&&o<e.length;o++)if(!Dt(e[o],n[o]))return!1;return!0}function bl(e,n,o,l,d,u){if(_n=u,Ee=n,n.memoizedState=null,n.updateQueue=null,n.lanes=0,Qo.current=e===null||e.memoizedState===null?Rv:Lv,e=o(l,d),Mi){u=0;do{if(Mi=!1,Ii=0,25<=u)throw Error(i(301));u+=1,Je=We=null,n.updateQueue=null,Qo.current=Dv,e=o(l,d)}while(Mi)}if(Qo.current=ts,n=We!==null&&We.next!==null,_n=0,Je=We=Ee=null,Xo=!1,n)throw Error(i(300));return e}function kl(){var e=Ii!==0;return Ii=0,e}function Kt(){var e={memoizedState:null,baseState:null,baseQueue:null,queue:null,next:null};return Je===null?Ee.memoizedState=Je=e:Je=Je.next=e,Je}function Ct(){if(We===null){var e=Ee.alternate;e=e!==null?e.memoizedState:null}else e=We.next;var n=Je===null?Ee.memoizedState:Je.next;if(n!==null)Je=n,We=e;else{if(e===null)throw Error(i(310));We=e,e={memoizedState:We.memoizedState,baseState:We.baseState,baseQueue:We.baseQueue,queue:We.queue,next:null},Je===null?Ee.memoizedState=Je=e:Je=Je.next=e}return Je}function Ai(e,n){return typeof n=="function"?n(e):n}function xl(e){var n=Ct(),o=n.queue;if(o===null)throw Error(i(311));o.lastRenderedReducer=e;var l=We,d=l.baseQueue,u=o.pending;if(u!==null){if(d!==null){var w=d.next;d.next=u.next,u.next=w}l.baseQueue=d=u,o.pending=null}if(d!==null){u=d.next,l=l.baseState;var k=w=null,S=null,D=u;do{var F=D.lane;if((_n&F)===F)S!==null&&(S=S.next={lane:0,action:D.action,hasEagerState:D.hasEagerState,eagerState:D.eagerState,next:null}),l=D.hasEagerState?D.eagerState:e(l,D.action);else{var q={lane:F,action:D.action,hasEagerState:D.hasEagerState,eagerState:D.eagerState,next:null};S===null?(k=S=q,w=l):S=S.next=q,Ee.lanes|=F,Kn|=F}D=D.next}while(D!==null&&D!==u);S===null?w=l:S.next=k,Dt(l,n.memoizedState)||(dt=!0),n.memoizedState=l,n.baseState=w,n.baseQueue=S,o.lastRenderedState=l}if(e=o.interleaved,e!==null){d=e;do u=d.lane,Ee.lanes|=u,Kn|=u,d=d.next;while(d!==e)}else d===null&&(o.lanes=0);return[n.memoizedState,o.dispatch]}function Sl(e){var n=Ct(),o=n.queue;if(o===null)throw Error(i(311));o.lastRenderedReducer=e;var l=o.dispatch,d=o.pending,u=n.memoizedState;if(d!==null){o.pending=null;var w=d=d.next;do u=e(u,w.action),w=w.next;while(w!==d);Dt(u,n.memoizedState)||(dt=!0),n.memoizedState=u,n.baseQueue===null&&(n.baseState=u),o.lastRenderedState=u}return[u,l]}function vu(){}function bu(e,n){var o=Ee,l=Ct(),d=n(),u=!Dt(l.memoizedState,d);if(u&&(l.memoizedState=d,dt=!0),l=l.queue,Tl(Su.bind(null,o,l,e),[e]),l.getSnapshot!==n||u||Je!==null&&Je.memoizedState.tag&1){if(o.flags|=2048,Ei(9,xu.bind(null,o,l,d,n),void 0,null),Ge===null)throw Error(i(349));(_n&30)!==0||ku(o,n,d)}return d}function ku(e,n,o){e.flags|=16384,e={getSnapshot:n,value:o},n=Ee.updateQueue,n===null?(n={lastEffect:null,stores:null},Ee.updateQueue=n,n.stores=[e]):(o=n.stores,o===null?n.stores=[e]:o.push(e))}function xu(e,n,o,l){n.value=o,n.getSnapshot=l,Tu(n)&&Cu(e)}function Su(e,n,o){return o(function(){Tu(n)&&Cu(e)})}function Tu(e){var n=e.getSnapshot;e=e.value;try{var o=n();return!Dt(e,o)}catch{return!0}}function Cu(e){var n=sn(e,1);n!==null&&Ft(n,e,1,-1)}function Mu(e){var n=Kt();return typeof e=="function"&&(e=e()),n.memoizedState=n.baseState=e,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:Ai,lastRenderedState:e},n.queue=e,e=e.dispatch=Pv.bind(null,Ee,e),[n.memoizedState,e]}function Ei(e,n,o,l){return e={tag:e,create:n,destroy:o,deps:l,next:null},n=Ee.updateQueue,n===null?(n={lastEffect:null,stores:null},Ee.updateQueue=n,n.lastEffect=e.next=e):(o=n.lastEffect,o===null?n.lastEffect=e.next=e:(l=o.next,o.next=e,e.next=l,n.lastEffect=e)),e}function Iu(){return Ct().memoizedState}function Zo(e,n,o,l){var d=Kt();Ee.flags|=e,d.memoizedState=Ei(1|n,o,void 0,l===void 0?null:l)}function es(e,n,o,l){var d=Ct();l=l===void 0?null:l;var u=void 0;if(We!==null){var w=We.memoizedState;if(u=w.destroy,l!==null&&vl(l,w.deps)){d.memoizedState=Ei(n,o,u,l);return}}Ee.flags|=e,d.memoizedState=Ei(1|n,o,u,l)}function Au(e,n){return Zo(8390656,8,e,n)}function Tl(e,n){return es(2048,8,e,n)}function Eu(e,n){return es(4,2,e,n)}function Pu(e,n){return es(4,4,e,n)}function Ru(e,n){if(typeof n=="function")return e=e(),n(e),function(){n(null)};if(n!=null)return e=e(),n.current=e,function(){n.current=null}}function Lu(e,n,o){return o=o!=null?o.concat([e]):null,es(4,4,Ru.bind(null,n,e),o)}function Cl(){}function Du(e,n){var o=Ct();n=n===void 0?null:n;var l=o.memoizedState;return l!==null&&n!==null&&vl(n,l[1])?l[0]:(o.memoizedState=[e,n],e)}function Vu(e,n){var o=Ct();n=n===void 0?null:n;var l=o.memoizedState;return l!==null&&n!==null&&vl(n,l[1])?l[0]:(e=e(),o.memoizedState=[e,n],e)}function ju(e,n,o){return(_n&21)===0?(e.baseState&&(e.baseState=!1,dt=!0),e.memoizedState=o):(Dt(o,n)||(o=hh(),Ee.lanes|=o,Kn|=o,e.baseState=!0),n)}function Av(e,n){var o=ye;ye=o!==0&&4>o?o:4,e(!0);var l=wl.transition;wl.transition={};try{e(!1),n()}finally{ye=o,wl.transition=l}}function Ou(){return Ct().memoizedState}function Ev(e,n,o){var l=An(e);if(o={lane:l,action:o,hasEagerState:!1,eagerState:null,next:null},Uu(e))Fu(n,o);else if(o=mu(e,n,o,l),o!==null){var d=st();Ft(o,e,l,d),Nu(o,n,l)}}function Pv(e,n,o){var l=An(e),d={lane:l,action:o,hasEagerState:!1,eagerState:null,next:null};if(Uu(e))Fu(n,d);else{var u=e.alternate;if(e.lanes===0&&(u===null||u.lanes===0)&&(u=n.lastRenderedReducer,u!==null))try{var w=n.lastRenderedState,k=u(w,o);if(d.hasEagerState=!0,d.eagerState=k,Dt(k,w)){var S=n.interleaved;S===null?(d.next=d,ul(n)):(d.next=S.next,S.next=d),n.interleaved=d;return}}catch{}finally{}o=mu(e,n,d,l),o!==null&&(d=st(),Ft(o,e,l,d),Nu(o,n,l))}}function Uu(e){var n=e.alternate;return e===Ee||n!==null&&n===Ee}function Fu(e,n){Mi=Xo=!0;var o=e.pending;o===null?n.next=n:(n.next=o.next,o.next=n),e.pending=n}function Nu(e,n,o){if((o&4194240)!==0){var l=n.lanes;l&=e.pendingLanes,o|=l,n.lanes=o,Ir(e,o)}}var ts={readContext:Tt,useCallback:et,useContext:et,useEffect:et,useImperativeHandle:et,useInsertionEffect:et,useLayoutEffect:et,useMemo:et,useReducer:et,useRef:et,useState:et,useDebugValue:et,useDeferredValue:et,useTransition:et,useMutableSource:et,useSyncExternalStore:et,useId:et,unstable_isNewReconciler:!1},Rv={readContext:Tt,useCallback:function(e,n){return Kt().memoizedState=[e,n===void 0?null:n],e},useContext:Tt,useEffect:Au,useImperativeHandle:function(e,n,o){return o=o!=null?o.concat([e]):null,Zo(4194308,4,Ru.bind(null,n,e),o)},useLayoutEffect:function(e,n){return Zo(4194308,4,e,n)},useInsertionEffect:function(e,n){return Zo(4,2,e,n)},useMemo:function(e,n){var o=Kt();return n=n===void 0?null:n,e=e(),o.memoizedState=[e,n],e},useReducer:function(e,n,o){var l=Kt();return n=o!==void 0?o(n):n,l.memoizedState=l.baseState=n,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:e,lastRenderedState:n},l.queue=e,e=e.dispatch=Ev.bind(null,Ee,e),[l.memoizedState,e]},useRef:function(e){var n=Kt();return e={current:e},n.memoizedState=e},useState:Mu,useDebugValue:Cl,useDeferredValue:function(e){return Kt().memoizedState=e},useTransition:function(){var e=Mu(!1),n=e[0];return e=Av.bind(null,e[1]),Kt().memoizedState=e,[n,e]},useMutableSource:function(){},useSyncExternalStore:function(e,n,o){var l=Ee,d=Kt();if(Te){if(o===void 0)throw Error(i(407));o=o()}else{if(o=n(),Ge===null)throw Error(i(349));(_n&30)!==0||ku(l,n,o)}d.memoizedState=o;var u={value:o,getSnapshot:n};return d.queue=u,Au(Su.bind(null,l,u,e),[e]),l.flags|=2048,Ei(9,xu.bind(null,l,u,o,n),void 0,null),o},useId:function(){var e=Kt(),n=Ge.identifierPrefix;if(Te){var o=on,l=an;o=(l&~(1<<32-Lt(l)-1)).toString(32)+o,n=":"+n+"R"+o,o=Ii++,0<o&&(n+="H"+o.toString(32)),n+=":"}else o=Iv++,n=":"+n+"r"+o.toString(32)+":";return e.memoizedState=n},unstable_isNewReconciler:!1},Lv={readContext:Tt,useCallback:Du,useContext:Tt,useEffect:Tl,useImperativeHandle:Lu,useInsertionEffect:Eu,useLayoutEffect:Pu,useMemo:Vu,useReducer:xl,useRef:Iu,useState:function(){return xl(Ai)},useDebugValue:Cl,useDeferredValue:function(e){var n=Ct();return ju(n,We.memoizedState,e)},useTransition:function(){var e=xl(Ai)[0],n=Ct().memoizedState;return[e,n]},useMutableSource:vu,useSyncExternalStore:bu,useId:Ou,unstable_isNewReconciler:!1},Dv={readContext:Tt,useCallback:Du,useContext:Tt,useEffect:Tl,useImperativeHandle:Lu,useInsertionEffect:Eu,useLayoutEffect:Pu,useMemo:Vu,useReducer:Sl,useRef:Iu,useState:function(){return Sl(Ai)},useDebugValue:Cl,useDeferredValue:function(e){var n=Ct();return We===null?n.memoizedState=e:ju(n,We.memoizedState,e)},useTransition:function(){var e=Sl(Ai)[0],n=Ct().memoizedState;return[e,n]},useMutableSource:vu,useSyncExternalStore:bu,useId:Ou,unstable_isNewReconciler:!1};function jt(e,n){if(e&&e.defaultProps){n=H({},n),e=e.defaultProps;for(var o in e)n[o]===void 0&&(n[o]=e[o]);return n}return n}function Ml(e,n,o,l){n=e.memoizedState,o=o(l,n),o=o==null?n:H({},n,o),e.memoizedState=o,e.lanes===0&&(e.updateQueue.baseState=o)}var ns={isMounted:function(e){return(e=e._reactInternals)?Fn(e)===e:!1},enqueueSetState:function(e,n,o){e=e._reactInternals;var l=st(),d=An(e),u=rn(l,d);u.payload=n,o!=null&&(u.callback=o),n=Tn(e,u,d),n!==null&&(Ft(n,e,d,l),Go(n,e,d))},enqueueReplaceState:function(e,n,o){e=e._reactInternals;var l=st(),d=An(e),u=rn(l,d);u.tag=1,u.payload=n,o!=null&&(u.callback=o),n=Tn(e,u,d),n!==null&&(Ft(n,e,d,l),Go(n,e,d))},enqueueForceUpdate:function(e,n){e=e._reactInternals;var o=st(),l=An(e),d=rn(o,l);d.tag=2,n!=null&&(d.callback=n),n=Tn(e,d,l),n!==null&&(Ft(n,e,l,o),Go(n,e,l))}};function qu(e,n,o,l,d,u,w){return e=e.stateNode,typeof e.shouldComponentUpdate=="function"?e.shouldComponentUpdate(l,u,w):n.prototype&&n.prototype.isPureReactComponent?!fi(o,l)||!fi(d,u):!0}function Wu(e,n,o){var l=!1,d=kn,u=n.contextType;return typeof u=="object"&&u!==null?u=Tt(u):(d=ct(n)?qn:Ze.current,l=n.contextTypes,u=(l=l!=null)?va(e,d):kn),n=new n(o,u),e.memoizedState=n.state!==null&&n.state!==void 0?n.state:null,n.updater=ns,e.stateNode=n,n._reactInternals=e,l&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=d,e.__reactInternalMemoizedMaskedChildContext=u),n}function zu(e,n,o,l){e=n.state,typeof n.componentWillReceiveProps=="function"&&n.componentWillReceiveProps(o,l),typeof n.UNSAFE_componentWillReceiveProps=="function"&&n.UNSAFE_componentWillReceiveProps(o,l),n.state!==e&&ns.enqueueReplaceState(n,n.state,null)}function Il(e,n,o,l){var d=e.stateNode;d.props=o,d.state=e.memoizedState,d.refs={},pl(e);var u=n.contextType;typeof u=="object"&&u!==null?d.context=Tt(u):(u=ct(n)?qn:Ze.current,d.context=va(e,u)),d.state=e.memoizedState,u=n.getDerivedStateFromProps,typeof u=="function"&&(Ml(e,n,u,o),d.state=e.memoizedState),typeof n.getDerivedStateFromProps=="function"||typeof d.getSnapshotBeforeUpdate=="function"||typeof d.UNSAFE_componentWillMount!="function"&&typeof d.componentWillMount!="function"||(n=d.state,typeof d.componentWillMount=="function"&&d.componentWillMount(),typeof d.UNSAFE_componentWillMount=="function"&&d.UNSAFE_componentWillMount(),n!==d.state&&ns.enqueueReplaceState(d,d.state,null),$o(e,o,d,l),d.state=e.memoizedState),typeof d.componentDidMount=="function"&&(e.flags|=4194308)}function Ia(e,n){try{var o="",l=n;do o+=de(l),l=l.return;while(l);var d=o}catch(u){d=`
Error generating stack: `+u.message+`
`+u.stack}return{value:e,source:n,stack:d,digest:null}}function Al(e,n,o){return{value:e,source:null,stack:o??null,digest:n??null}}function El(e,n){try{console.error(n.value)}catch(o){setTimeout(function(){throw o})}}var Vv=typeof WeakMap=="function"?WeakMap:Map;function Bu(e,n,o){o=rn(-1,o),o.tag=3,o.payload={element:null};var l=n.value;return o.callback=function(){cs||(cs=!0,Hl=l),El(e,n)},o}function Hu(e,n,o){o=rn(-1,o),o.tag=3;var l=e.type.getDerivedStateFromError;if(typeof l=="function"){var d=n.value;o.payload=function(){return l(d)},o.callback=function(){El(e,n)}}var u=e.stateNode;return u!==null&&typeof u.componentDidCatch=="function"&&(o.callback=function(){El(e,n),typeof l!="function"&&(Mn===null?Mn=new Set([this]):Mn.add(this));var w=n.stack;this.componentDidCatch(n.value,{componentStack:w!==null?w:""})}),o}function _u(e,n,o){var l=e.pingCache;if(l===null){l=e.pingCache=new Vv;var d=new Set;l.set(n,d)}else d=l.get(n),d===void 0&&(d=new Set,l.set(n,d));d.has(o)||(d.add(o),e=Gv.bind(null,e,n,o),n.then(e,e))}function Ku(e){do{var n;if((n=e.tag===13)&&(n=e.memoizedState,n=n!==null?n.dehydrated!==null:!0),n)return e;e=e.return}while(e!==null);return null}function Ju(e,n,o,l,d){return(e.mode&1)===0?(e===n?e.flags|=65536:(e.flags|=128,o.flags|=131072,o.flags&=-52805,o.tag===1&&(o.alternate===null?o.tag=17:(n=rn(-1,1),n.tag=2,Tn(o,n,1))),o.lanes|=1),e):(e.flags|=65536,e.lanes=d,e)}var jv=B.ReactCurrentOwner,dt=!1;function ot(e,n,o,l){n.child=e===null?pu(n,null,o,l):Sa(n,e.child,o,l)}function Gu(e,n,o,l,d){o=o.render;var u=n.ref;return Ca(n,d),l=bl(e,n,o,l,u,d),o=kl(),e!==null&&!dt?(n.updateQueue=e.updateQueue,n.flags&=-2053,e.lanes&=~d,ln(e,n,d)):(Te&&o&&al(n),n.flags|=1,ot(e,n,l,d),n.child)}function $u(e,n,o,l,d){if(e===null){var u=o.type;return typeof u=="function"&&!Ql(u)&&u.defaultProps===void 0&&o.compare===null&&o.defaultProps===void 0?(n.tag=15,n.type=u,Yu(e,n,u,l,d)):(e=fs(o.type,null,l,n,n.mode,d),e.ref=n.ref,e.return=n,n.child=e)}if(u=e.child,(e.lanes&d)===0){var w=u.memoizedProps;if(o=o.compare,o=o!==null?o:fi,o(w,l)&&e.ref===n.ref)return ln(e,n,d)}return n.flags|=1,e=Pn(u,l),e.ref=n.ref,e.return=n,n.child=e}function Yu(e,n,o,l,d){if(e!==null){var u=e.memoizedProps;if(fi(u,l)&&e.ref===n.ref)if(dt=!1,n.pendingProps=l=u,(e.lanes&d)!==0)(e.flags&131072)!==0&&(dt=!0);else return n.lanes=e.lanes,ln(e,n,d)}return Pl(e,n,o,l,d)}function Qu(e,n,o){var l=n.pendingProps,d=l.children,u=e!==null?e.memoizedState:null;if(l.mode==="hidden")if((n.mode&1)===0)n.memoizedState={baseLanes:0,cachePool:null,transitions:null},ke(Ea,vt),vt|=o;else{if((o&1073741824)===0)return e=u!==null?u.baseLanes|o:o,n.lanes=n.childLanes=1073741824,n.memoizedState={baseLanes:e,cachePool:null,transitions:null},n.updateQueue=null,ke(Ea,vt),vt|=e,null;n.memoizedState={baseLanes:0,cachePool:null,transitions:null},l=u!==null?u.baseLanes:o,ke(Ea,vt),vt|=l}else u!==null?(l=u.baseLanes|o,n.memoizedState=null):l=o,ke(Ea,vt),vt|=l;return ot(e,n,d,o),n.child}function Xu(e,n){var o=n.ref;(e===null&&o!==null||e!==null&&e.ref!==o)&&(n.flags|=512,n.flags|=2097152)}function Pl(e,n,o,l,d){var u=ct(o)?qn:Ze.current;return u=va(n,u),Ca(n,d),o=bl(e,n,o,l,u,d),l=kl(),e!==null&&!dt?(n.updateQueue=e.updateQueue,n.flags&=-2053,e.lanes&=~d,ln(e,n,d)):(Te&&l&&al(n),n.flags|=1,ot(e,n,o,d),n.child)}function Zu(e,n,o,l,d){if(ct(o)){var u=!0;qo(n)}else u=!1;if(Ca(n,d),n.stateNode===null)is(e,n),Wu(n,o,l),Il(n,o,l,d),l=!0;else if(e===null){var w=n.stateNode,k=n.memoizedProps;w.props=k;var S=w.context,D=o.contextType;typeof D=="object"&&D!==null?D=Tt(D):(D=ct(o)?qn:Ze.current,D=va(n,D));var F=o.getDerivedStateFromProps,q=typeof F=="function"||typeof w.getSnapshotBeforeUpdate=="function";q||typeof w.UNSAFE_componentWillReceiveProps!="function"&&typeof w.componentWillReceiveProps!="function"||(k!==l||S!==D)&&zu(n,w,l,D),Sn=!1;var U=n.memoizedState;w.state=U,$o(n,l,w,d),S=n.memoizedState,k!==l||U!==S||lt.current||Sn?(typeof F=="function"&&(Ml(n,o,F,l),S=n.memoizedState),(k=Sn||qu(n,o,k,l,U,S,D))?(q||typeof w.UNSAFE_componentWillMount!="function"&&typeof w.componentWillMount!="function"||(typeof w.componentWillMount=="function"&&w.componentWillMount(),typeof w.UNSAFE_componentWillMount=="function"&&w.UNSAFE_componentWillMount()),typeof w.componentDidMount=="function"&&(n.flags|=4194308)):(typeof w.componentDidMount=="function"&&(n.flags|=4194308),n.memoizedProps=l,n.memoizedState=S),w.props=l,w.state=S,w.context=D,l=k):(typeof w.componentDidMount=="function"&&(n.flags|=4194308),l=!1)}else{w=n.stateNode,fu(e,n),k=n.memoizedProps,D=n.type===n.elementType?k:jt(n.type,k),w.props=D,q=n.pendingProps,U=w.context,S=o.contextType,typeof S=="object"&&S!==null?S=Tt(S):(S=ct(o)?qn:Ze.current,S=va(n,S));var K=o.getDerivedStateFromProps;(F=typeof K=="function"||typeof w.getSnapshotBeforeUpdate=="function")||typeof w.UNSAFE_componentWillReceiveProps!="function"&&typeof w.componentWillReceiveProps!="function"||(k!==q||U!==S)&&zu(n,w,l,S),Sn=!1,U=n.memoizedState,w.state=U,$o(n,l,w,d);var $=n.memoizedState;k!==q||U!==$||lt.current||Sn?(typeof K=="function"&&(Ml(n,o,K,l),$=n.memoizedState),(D=Sn||qu(n,o,D,l,U,$,S)||!1)?(F||typeof w.UNSAFE_componentWillUpdate!="function"&&typeof w.componentWillUpdate!="function"||(typeof w.componentWillUpdate=="function"&&w.componentWillUpdate(l,$,S),typeof w.UNSAFE_componentWillUpdate=="function"&&w.UNSAFE_componentWillUpdate(l,$,S)),typeof w.componentDidUpdate=="function"&&(n.flags|=4),typeof w.getSnapshotBeforeUpdate=="function"&&(n.flags|=1024)):(typeof w.componentDidUpdate!="function"||k===e.memoizedProps&&U===e.memoizedState||(n.flags|=4),typeof w.getSnapshotBeforeUpdate!="function"||k===e.memoizedProps&&U===e.memoizedState||(n.flags|=1024),n.memoizedProps=l,n.memoizedState=$),w.props=l,w.state=$,w.context=S,l=D):(typeof w.componentDidUpdate!="function"||k===e.memoizedProps&&U===e.memoizedState||(n.flags|=4),typeof w.getSnapshotBeforeUpdate!="function"||k===e.memoizedProps&&U===e.memoizedState||(n.flags|=1024),l=!1)}return Rl(e,n,o,l,u,d)}function Rl(e,n,o,l,d,u){Xu(e,n);var w=(n.flags&128)!==0;if(!l&&!w)return d&&iu(n,o,!1),ln(e,n,u);l=n.stateNode,jv.current=n;var k=w&&typeof o.getDerivedStateFromError!="function"?null:l.render();return n.flags|=1,e!==null&&w?(n.child=Sa(n,e.child,null,u),n.child=Sa(n,null,k,u)):ot(e,n,k,u),n.memoizedState=l.state,d&&iu(n,o,!0),n.child}function ep(e){var n=e.stateNode;n.pendingContext?nu(e,n.pendingContext,n.pendingContext!==n.context):n.context&&nu(e,n.context,!1),ml(e,n.containerInfo)}function tp(e,n,o,l,d){return xa(),rl(d),n.flags|=256,ot(e,n,o,l),n.child}var Ll={dehydrated:null,treeContext:null,retryLane:0};function Dl(e){return{baseLanes:e,cachePool:null,transitions:null}}function np(e,n,o){var l=n.pendingProps,d=Ae.current,u=!1,w=(n.flags&128)!==0,k;if((k=w)||(k=e!==null&&e.memoizedState===null?!1:(d&2)!==0),k?(u=!0,n.flags&=-129):(e===null||e.memoizedState!==null)&&(d|=1),ke(Ae,d&1),e===null)return sl(n),e=n.memoizedState,e!==null&&(e=e.dehydrated,e!==null)?((n.mode&1)===0?n.lanes=1:e.data==="$!"?n.lanes=8:n.lanes=1073741824,null):(w=l.children,e=l.fallback,u?(l=n.mode,u=n.child,w={mode:"hidden",children:w},(l&1)===0&&u!==null?(u.childLanes=0,u.pendingProps=w):u=gs(w,l,0,null),e=Yn(e,l,o,null),u.return=n,e.return=n,u.sibling=e,n.child=u,n.child.memoizedState=Dl(o),n.memoizedState=Ll,e):Vl(n,w));if(d=e.memoizedState,d!==null&&(k=d.dehydrated,k!==null))return Ov(e,n,w,l,k,d,o);if(u){u=l.fallback,w=n.mode,d=e.child,k=d.sibling;var S={mode:"hidden",children:l.children};return(w&1)===0&&n.child!==d?(l=n.child,l.childLanes=0,l.pendingProps=S,n.deletions=null):(l=Pn(d,S),l.subtreeFlags=d.subtreeFlags&14680064),k!==null?u=Pn(k,u):(u=Yn(u,w,o,null),u.flags|=2),u.return=n,l.return=n,l.sibling=u,n.child=l,l=u,u=n.child,w=e.child.memoizedState,w=w===null?Dl(o):{baseLanes:w.baseLanes|o,cachePool:null,transitions:w.transitions},u.memoizedState=w,u.childLanes=e.childLanes&~o,n.memoizedState=Ll,l}return u=e.child,e=u.sibling,l=Pn(u,{mode:"visible",children:l.children}),(n.mode&1)===0&&(l.lanes=o),l.return=n,l.sibling=null,e!==null&&(o=n.deletions,o===null?(n.deletions=[e],n.flags|=16):o.push(e)),n.child=l,n.memoizedState=null,l}function Vl(e,n){return n=gs({mode:"visible",children:n},e.mode,0,null),n.return=e,e.child=n}function as(e,n,o,l){return l!==null&&rl(l),Sa(n,e.child,null,o),e=Vl(n,n.pendingProps.children),e.flags|=2,n.memoizedState=null,e}function Ov(e,n,o,l,d,u,w){if(o)return n.flags&256?(n.flags&=-257,l=Al(Error(i(422))),as(e,n,w,l)):n.memoizedState!==null?(n.child=e.child,n.flags|=128,null):(u=l.fallback,d=n.mode,l=gs({mode:"visible",children:l.children},d,0,null),u=Yn(u,d,w,null),u.flags|=2,l.return=n,u.return=n,l.sibling=u,n.child=l,(n.mode&1)!==0&&Sa(n,e.child,null,w),n.child.memoizedState=Dl(w),n.memoizedState=Ll,u);if((n.mode&1)===0)return as(e,n,w,null);if(d.data==="$!"){if(l=d.nextSibling&&d.nextSibling.dataset,l)var k=l.dgst;return l=k,u=Error(i(419)),l=Al(u,l,void 0),as(e,n,w,l)}if(k=(w&e.childLanes)!==0,dt||k){if(l=Ge,l!==null){switch(w&-w){case 4:d=2;break;case 16:d=8;break;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:d=32;break;case 536870912:d=268435456;break;default:d=0}d=(d&(l.suspendedLanes|w))!==0?0:d,d!==0&&d!==u.retryLane&&(u.retryLane=d,sn(e,d),Ft(l,e,d,-1))}return Yl(),l=Al(Error(i(421))),as(e,n,w,l)}return d.data==="$?"?(n.flags|=128,n.child=e.child,n=$v.bind(null,e),d._reactRetry=n,null):(e=u.treeContext,wt=vn(d.nextSibling),yt=n,Te=!0,Vt=null,e!==null&&(xt[St++]=an,xt[St++]=on,xt[St++]=Wn,an=e.id,on=e.overflow,Wn=n),n=Vl(n,l.children),n.flags|=4096,n)}function ap(e,n,o){e.lanes|=n;var l=e.alternate;l!==null&&(l.lanes|=n),hl(e.return,n,o)}function jl(e,n,o,l,d){var u=e.memoizedState;u===null?e.memoizedState={isBackwards:n,rendering:null,renderingStartTime:0,last:l,tail:o,tailMode:d}:(u.isBackwards=n,u.rendering=null,u.renderingStartTime=0,u.last=l,u.tail=o,u.tailMode=d)}function ip(e,n,o){var l=n.pendingProps,d=l.revealOrder,u=l.tail;if(ot(e,n,l.children,o),l=Ae.current,(l&2)!==0)l=l&1|2,n.flags|=128;else{if(e!==null&&(e.flags&128)!==0)e:for(e=n.child;e!==null;){if(e.tag===13)e.memoizedState!==null&&ap(e,o,n);else if(e.tag===19)ap(e,o,n);else if(e.child!==null){e.child.return=e,e=e.child;continue}if(e===n)break e;for(;e.sibling===null;){if(e.return===null||e.return===n)break e;e=e.return}e.sibling.return=e.return,e=e.sibling}l&=1}if(ke(Ae,l),(n.mode&1)===0)n.memoizedState=null;else switch(d){case"forwards":for(o=n.child,d=null;o!==null;)e=o.alternate,e!==null&&Yo(e)===null&&(d=o),o=o.sibling;o=d,o===null?(d=n.child,n.child=null):(d=o.sibling,o.sibling=null),jl(n,!1,d,o,u);break;case"backwards":for(o=null,d=n.child,n.child=null;d!==null;){if(e=d.alternate,e!==null&&Yo(e)===null){n.child=d;break}e=d.sibling,d.sibling=o,o=d,d=e}jl(n,!0,o,null,u);break;case"together":jl(n,!1,null,null,void 0);break;default:n.memoizedState=null}return n.child}function is(e,n){(n.mode&1)===0&&e!==null&&(e.alternate=null,n.alternate=null,n.flags|=2)}function ln(e,n,o){if(e!==null&&(n.dependencies=e.dependencies),Kn|=n.lanes,(o&n.childLanes)===0)return null;if(e!==null&&n.child!==e.child)throw Error(i(153));if(n.child!==null){for(e=n.child,o=Pn(e,e.pendingProps),n.child=o,o.return=n;e.sibling!==null;)e=e.sibling,o=o.sibling=Pn(e,e.pendingProps),o.return=n;o.sibling=null}return n.child}function Uv(e,n,o){switch(n.tag){case 3:ep(n),xa();break;case 5:wu(n);break;case 1:ct(n.type)&&qo(n);break;case 4:ml(n,n.stateNode.containerInfo);break;case 10:var l=n.type._context,d=n.memoizedProps.value;ke(Ko,l._currentValue),l._currentValue=d;break;case 13:if(l=n.memoizedState,l!==null)return l.dehydrated!==null?(ke(Ae,Ae.current&1),n.flags|=128,null):(o&n.child.childLanes)!==0?np(e,n,o):(ke(Ae,Ae.current&1),e=ln(e,n,o),e!==null?e.sibling:null);ke(Ae,Ae.current&1);break;case 19:if(l=(o&n.childLanes)!==0,(e.flags&128)!==0){if(l)return ip(e,n,o);n.flags|=128}if(d=n.memoizedState,d!==null&&(d.rendering=null,d.tail=null,d.lastEffect=null),ke(Ae,Ae.current),l)break;return null;case 22:case 23:return n.lanes=0,Qu(e,n,o)}return ln(e,n,o)}var op,Ol,sp,rp;op=function(e,n){for(var o=n.child;o!==null;){if(o.tag===5||o.tag===6)e.appendChild(o.stateNode);else if(o.tag!==4&&o.child!==null){o.child.return=o,o=o.child;continue}if(o===n)break;for(;o.sibling===null;){if(o.return===null||o.return===n)return;o=o.return}o.sibling.return=o.return,o=o.sibling}},Ol=function(){},sp=function(e,n,o,l){var d=e.memoizedProps;if(d!==l){e=n.stateNode,Hn(_t.current);var u=null;switch(o){case"input":d=$a(e,d),l=$a(e,l),u=[];break;case"select":d=H({},d,{value:void 0}),l=H({},l,{value:void 0}),u=[];break;case"textarea":d=mr(e,d),l=mr(e,l),u=[];break;default:typeof d.onClick!="function"&&typeof l.onClick=="function"&&(e.onclick=Uo)}gr(o,l);var w;o=null;for(D in d)if(!l.hasOwnProperty(D)&&d.hasOwnProperty(D)&&d[D]!=null)if(D==="style"){var k=d[D];for(w in k)k.hasOwnProperty(w)&&(o||(o={}),o[w]="")}else D!=="dangerouslySetInnerHTML"&&D!=="children"&&D!=="suppressContentEditableWarning"&&D!=="suppressHydrationWarning"&&D!=="autoFocus"&&(r.hasOwnProperty(D)?u||(u=[]):(u=u||[]).push(D,null));for(D in l){var S=l[D];if(k=d!=null?d[D]:void 0,l.hasOwnProperty(D)&&S!==k&&(S!=null||k!=null))if(D==="style")if(k){for(w in k)!k.hasOwnProperty(w)||S&&S.hasOwnProperty(w)||(o||(o={}),o[w]="");for(w in S)S.hasOwnProperty(w)&&k[w]!==S[w]&&(o||(o={}),o[w]=S[w])}else o||(u||(u=[]),u.push(D,o)),o=S;else D==="dangerouslySetInnerHTML"?(S=S?S.__html:void 0,k=k?k.__html:void 0,S!=null&&k!==S&&(u=u||[]).push(D,S)):D==="children"?typeof S!="string"&&typeof S!="number"||(u=u||[]).push(D,""+S):D!=="suppressContentEditableWarning"&&D!=="suppressHydrationWarning"&&(r.hasOwnProperty(D)?(S!=null&&D==="onScroll"&&xe("scroll",e),u||k===S||(u=[])):(u=u||[]).push(D,S))}o&&(u=u||[]).push("style",o);var D=u;(n.updateQueue=D)&&(n.flags|=4)}},rp=function(e,n,o,l){o!==l&&(n.flags|=4)};function Pi(e,n){if(!Te)switch(e.tailMode){case"hidden":n=e.tail;for(var o=null;n!==null;)n.alternate!==null&&(o=n),n=n.sibling;o===null?e.tail=null:o.sibling=null;break;case"collapsed":o=e.tail;for(var l=null;o!==null;)o.alternate!==null&&(l=o),o=o.sibling;l===null?n||e.tail===null?e.tail=null:e.tail.sibling=null:l.sibling=null}}function tt(e){var n=e.alternate!==null&&e.alternate.child===e.child,o=0,l=0;if(n)for(var d=e.child;d!==null;)o|=d.lanes|d.childLanes,l|=d.subtreeFlags&14680064,l|=d.flags&14680064,d.return=e,d=d.sibling;else for(d=e.child;d!==null;)o|=d.lanes|d.childLanes,l|=d.subtreeFlags,l|=d.flags,d.return=e,d=d.sibling;return e.subtreeFlags|=l,e.childLanes=o,n}function Fv(e,n,o){var l=n.pendingProps;switch(il(n),n.tag){case 2:case 16:case 15:case 0:case 11:case 7:case 8:case 12:case 9:case 14:return tt(n),null;case 1:return ct(n.type)&&No(),tt(n),null;case 3:return l=n.stateNode,Ma(),Se(lt),Se(Ze),yl(),l.pendingContext&&(l.context=l.pendingContext,l.pendingContext=null),(e===null||e.child===null)&&(Ho(n)?n.flags|=4:e===null||e.memoizedState.isDehydrated&&(n.flags&256)===0||(n.flags|=1024,Vt!==null&&(Jl(Vt),Vt=null))),Ol(e,n),tt(n),null;case 5:fl(n);var d=Hn(Ci.current);if(o=n.type,e!==null&&n.stateNode!=null)sp(e,n,o,l,d),e.ref!==n.ref&&(n.flags|=512,n.flags|=2097152);else{if(!l){if(n.stateNode===null)throw Error(i(166));return tt(n),null}if(e=Hn(_t.current),Ho(n)){l=n.stateNode,o=n.type;var u=n.memoizedProps;switch(l[Ht]=n,l[bi]=u,e=(n.mode&1)!==0,o){case"dialog":xe("cancel",l),xe("close",l);break;case"iframe":case"object":case"embed":xe("load",l);break;case"video":case"audio":for(d=0;d<yi.length;d++)xe(yi[d],l);break;case"source":xe("error",l);break;case"img":case"image":case"link":xe("error",l),xe("load",l);break;case"details":xe("toggle",l);break;case"input":Ya(l,u),xe("invalid",l);break;case"select":l._wrapperState={wasMultiple:!!u.multiple},xe("invalid",l);break;case"textarea":Hd(l,u),xe("invalid",l)}gr(o,u),d=null;for(var w in u)if(u.hasOwnProperty(w)){var k=u[w];w==="children"?typeof k=="string"?l.textContent!==k&&(u.suppressHydrationWarning!==!0&&Oo(l.textContent,k,e),d=["children",k]):typeof k=="number"&&l.textContent!==""+k&&(u.suppressHydrationWarning!==!0&&Oo(l.textContent,k,e),d=["children",""+k]):r.hasOwnProperty(w)&&k!=null&&w==="onScroll"&&xe("scroll",l)}switch(o){case"input":Ie(l),Bd(l,u,!0);break;case"textarea":Ie(l),Kd(l);break;case"select":case"option":break;default:typeof u.onClick=="function"&&(l.onclick=Uo)}l=d,n.updateQueue=l,l!==null&&(n.flags|=4)}else{w=d.nodeType===9?d:d.ownerDocument,e==="http://www.w3.org/1999/xhtml"&&(e=Jd(o)),e==="http://www.w3.org/1999/xhtml"?o==="script"?(e=w.createElement("div"),e.innerHTML="<script><\/script>",e=e.removeChild(e.firstChild)):typeof l.is=="string"?e=w.createElement(o,{is:l.is}):(e=w.createElement(o),o==="select"&&(w=e,l.multiple?w.multiple=!0:l.size&&(w.size=l.size))):e=w.createElementNS(e,o),e[Ht]=n,e[bi]=l,op(e,n,!1,!1),n.stateNode=e;e:{switch(w=yr(o,l),o){case"dialog":xe("cancel",e),xe("close",e),d=l;break;case"iframe":case"object":case"embed":xe("load",e),d=l;break;case"video":case"audio":for(d=0;d<yi.length;d++)xe(yi[d],e);d=l;break;case"source":xe("error",e),d=l;break;case"img":case"image":case"link":xe("error",e),xe("load",e),d=l;break;case"details":xe("toggle",e),d=l;break;case"input":Ya(e,l),d=$a(e,l),xe("invalid",e);break;case"option":d=l;break;case"select":e._wrapperState={wasMultiple:!!l.multiple},d=H({},l,{value:void 0}),xe("invalid",e);break;case"textarea":Hd(e,l),d=mr(e,l),xe("invalid",e);break;default:d=l}gr(o,d),k=d;for(u in k)if(k.hasOwnProperty(u)){var S=k[u];u==="style"?Yd(e,S):u==="dangerouslySetInnerHTML"?(S=S?S.__html:void 0,S!=null&&Gd(e,S)):u==="children"?typeof S=="string"?(o!=="textarea"||S!=="")&&Xa(e,S):typeof S=="number"&&Xa(e,""+S):u!=="suppressContentEditableWarning"&&u!=="suppressHydrationWarning"&&u!=="autoFocus"&&(r.hasOwnProperty(u)?S!=null&&u==="onScroll"&&xe("scroll",e):S!=null&&V(e,u,S,w))}switch(o){case"input":Ie(e),Bd(e,l,!1);break;case"textarea":Ie(e),Kd(e);break;case"option":l.value!=null&&e.setAttribute("value",""+pe(l.value));break;case"select":e.multiple=!!l.multiple,u=l.value,u!=null?ra(e,!!l.multiple,u,!1):l.defaultValue!=null&&ra(e,!!l.multiple,l.defaultValue,!0);break;default:typeof d.onClick=="function"&&(e.onclick=Uo)}switch(o){case"button":case"input":case"select":case"textarea":l=!!l.autoFocus;break e;case"img":l=!0;break e;default:l=!1}}l&&(n.flags|=4)}n.ref!==null&&(n.flags|=512,n.flags|=2097152)}return tt(n),null;case 6:if(e&&n.stateNode!=null)rp(e,n,e.memoizedProps,l);else{if(typeof l!="string"&&n.stateNode===null)throw Error(i(166));if(o=Hn(Ci.current),Hn(_t.current),Ho(n)){if(l=n.stateNode,o=n.memoizedProps,l[Ht]=n,(u=l.nodeValue!==o)&&(e=yt,e!==null))switch(e.tag){case 3:Oo(l.nodeValue,o,(e.mode&1)!==0);break;case 5:e.memoizedProps.suppressHydrationWarning!==!0&&Oo(l.nodeValue,o,(e.mode&1)!==0)}u&&(n.flags|=4)}else l=(o.nodeType===9?o:o.ownerDocument).createTextNode(l),l[Ht]=n,n.stateNode=l}return tt(n),null;case 13:if(Se(Ae),l=n.memoizedState,e===null||e.memoizedState!==null&&e.memoizedState.dehydrated!==null){if(Te&&wt!==null&&(n.mode&1)!==0&&(n.flags&128)===0)du(),xa(),n.flags|=98560,u=!1;else if(u=Ho(n),l!==null&&l.dehydrated!==null){if(e===null){if(!u)throw Error(i(318));if(u=n.memoizedState,u=u!==null?u.dehydrated:null,!u)throw Error(i(317));u[Ht]=n}else xa(),(n.flags&128)===0&&(n.memoizedState=null),n.flags|=4;tt(n),u=!1}else Vt!==null&&(Jl(Vt),Vt=null),u=!0;if(!u)return n.flags&65536?n:null}return(n.flags&128)!==0?(n.lanes=o,n):(l=l!==null,l!==(e!==null&&e.memoizedState!==null)&&l&&(n.child.flags|=8192,(n.mode&1)!==0&&(e===null||(Ae.current&1)!==0?ze===0&&(ze=3):Yl())),n.updateQueue!==null&&(n.flags|=4),tt(n),null);case 4:return Ma(),Ol(e,n),e===null&&wi(n.stateNode.containerInfo),tt(n),null;case 10:return dl(n.type._context),tt(n),null;case 17:return ct(n.type)&&No(),tt(n),null;case 19:if(Se(Ae),u=n.memoizedState,u===null)return tt(n),null;if(l=(n.flags&128)!==0,w=u.rendering,w===null)if(l)Pi(u,!1);else{if(ze!==0||e!==null&&(e.flags&128)!==0)for(e=n.child;e!==null;){if(w=Yo(e),w!==null){for(n.flags|=128,Pi(u,!1),l=w.updateQueue,l!==null&&(n.updateQueue=l,n.flags|=4),n.subtreeFlags=0,l=o,o=n.child;o!==null;)u=o,e=l,u.flags&=14680066,w=u.alternate,w===null?(u.childLanes=0,u.lanes=e,u.child=null,u.subtreeFlags=0,u.memoizedProps=null,u.memoizedState=null,u.updateQueue=null,u.dependencies=null,u.stateNode=null):(u.childLanes=w.childLanes,u.lanes=w.lanes,u.child=w.child,u.subtreeFlags=0,u.deletions=null,u.memoizedProps=w.memoizedProps,u.memoizedState=w.memoizedState,u.updateQueue=w.updateQueue,u.type=w.type,e=w.dependencies,u.dependencies=e===null?null:{lanes:e.lanes,firstContext:e.firstContext}),o=o.sibling;return ke(Ae,Ae.current&1|2),n.child}e=e.sibling}u.tail!==null&&je()>Pa&&(n.flags|=128,l=!0,Pi(u,!1),n.lanes=4194304)}else{if(!l)if(e=Yo(w),e!==null){if(n.flags|=128,l=!0,o=e.updateQueue,o!==null&&(n.updateQueue=o,n.flags|=4),Pi(u,!0),u.tail===null&&u.tailMode==="hidden"&&!w.alternate&&!Te)return tt(n),null}else 2*je()-u.renderingStartTime>Pa&&o!==1073741824&&(n.flags|=128,l=!0,Pi(u,!1),n.lanes=4194304);u.isBackwards?(w.sibling=n.child,n.child=w):(o=u.last,o!==null?o.sibling=w:n.child=w,u.last=w)}return u.tail!==null?(n=u.tail,u.rendering=n,u.tail=n.sibling,u.renderingStartTime=je(),n.sibling=null,o=Ae.current,ke(Ae,l?o&1|2:o&1),n):(tt(n),null);case 22:case 23:return $l(),l=n.memoizedState!==null,e!==null&&e.memoizedState!==null!==l&&(n.flags|=8192),l&&(n.mode&1)!==0?(vt&1073741824)!==0&&(tt(n),n.subtreeFlags&6&&(n.flags|=8192)):tt(n),null;case 24:return null;case 25:return null}throw Error(i(156,n.tag))}function Nv(e,n){switch(il(n),n.tag){case 1:return ct(n.type)&&No(),e=n.flags,e&65536?(n.flags=e&-65537|128,n):null;case 3:return Ma(),Se(lt),Se(Ze),yl(),e=n.flags,(e&65536)!==0&&(e&128)===0?(n.flags=e&-65537|128,n):null;case 5:return fl(n),null;case 13:if(Se(Ae),e=n.memoizedState,e!==null&&e.dehydrated!==null){if(n.alternate===null)throw Error(i(340));xa()}return e=n.flags,e&65536?(n.flags=e&-65537|128,n):null;case 19:return Se(Ae),null;case 4:return Ma(),null;case 10:return dl(n.type._context),null;case 22:case 23:return $l(),null;case 24:return null;default:return null}}var os=!1,nt=!1,qv=typeof WeakSet=="function"?WeakSet:Set,J=null;function Aa(e,n){var o=e.ref;if(o!==null)if(typeof o=="function")try{o(null)}catch(l){De(e,n,l)}else o.current=null}function Ul(e,n,o){try{o()}catch(l){De(e,n,l)}}var lp=!1;function Wv(e,n){if($r=Co,e=qh(),Wr(e)){if("selectionStart"in e)var o={start:e.selectionStart,end:e.selectionEnd};else e:{o=(o=e.ownerDocument)&&o.defaultView||window;var l=o.getSelection&&o.getSelection();if(l&&l.rangeCount!==0){o=l.anchorNode;var d=l.anchorOffset,u=l.focusNode;l=l.focusOffset;try{o.nodeType,u.nodeType}catch{o=null;break e}var w=0,k=-1,S=-1,D=0,F=0,q=e,U=null;t:for(;;){for(var K;q!==o||d!==0&&q.nodeType!==3||(k=w+d),q!==u||l!==0&&q.nodeType!==3||(S=w+l),q.nodeType===3&&(w+=q.nodeValue.length),(K=q.firstChild)!==null;)U=q,q=K;for(;;){if(q===e)break t;if(U===o&&++D===d&&(k=w),U===u&&++F===l&&(S=w),(K=q.nextSibling)!==null)break;q=U,U=q.parentNode}q=K}o=k===-1||S===-1?null:{start:k,end:S}}else o=null}o=o||{start:0,end:0}}else o=null;for(Yr={focusedElem:e,selectionRange:o},Co=!1,J=n;J!==null;)if(n=J,e=n.child,(n.subtreeFlags&1028)!==0&&e!==null)e.return=n,J=e;else for(;J!==null;){n=J;try{var $=n.alternate;if((n.flags&1024)!==0)switch(n.tag){case 0:case 11:case 15:break;case 1:if($!==null){var Y=$.memoizedProps,Oe=$.memoizedState,P=n.stateNode,C=P.getSnapshotBeforeUpdate(n.elementType===n.type?Y:jt(n.type,Y),Oe);P.__reactInternalSnapshotBeforeUpdate=C}break;case 3:var L=n.stateNode.containerInfo;L.nodeType===1?L.textContent="":L.nodeType===9&&L.documentElement&&L.removeChild(L.documentElement);break;case 5:case 6:case 4:case 17:break;default:throw Error(i(163))}}catch(z){De(n,n.return,z)}if(e=n.sibling,e!==null){e.return=n.return,J=e;break}J=n.return}return $=lp,lp=!1,$}function Ri(e,n,o){var l=n.updateQueue;if(l=l!==null?l.lastEffect:null,l!==null){var d=l=l.next;do{if((d.tag&e)===e){var u=d.destroy;d.destroy=void 0,u!==void 0&&Ul(n,o,u)}d=d.next}while(d!==l)}}function ss(e,n){if(n=n.updateQueue,n=n!==null?n.lastEffect:null,n!==null){var o=n=n.next;do{if((o.tag&e)===e){var l=o.create;o.destroy=l()}o=o.next}while(o!==n)}}function Fl(e){var n=e.ref;if(n!==null){var o=e.stateNode;switch(e.tag){case 5:e=o;break;default:e=o}typeof n=="function"?n(e):n.current=e}}function cp(e){var n=e.alternate;n!==null&&(e.alternate=null,cp(n)),e.child=null,e.deletions=null,e.sibling=null,e.tag===5&&(n=e.stateNode,n!==null&&(delete n[Ht],delete n[bi],delete n[el],delete n[Sv],delete n[Tv])),e.stateNode=null,e.return=null,e.dependencies=null,e.memoizedProps=null,e.memoizedState=null,e.pendingProps=null,e.stateNode=null,e.updateQueue=null}function dp(e){return e.tag===5||e.tag===3||e.tag===4}function hp(e){e:for(;;){for(;e.sibling===null;){if(e.return===null||dp(e.return))return null;e=e.return}for(e.sibling.return=e.return,e=e.sibling;e.tag!==5&&e.tag!==6&&e.tag!==18;){if(e.flags&2||e.child===null||e.tag===4)continue e;e.child.return=e,e=e.child}if(!(e.flags&2))return e.stateNode}}function Nl(e,n,o){var l=e.tag;if(l===5||l===6)e=e.stateNode,n?o.nodeType===8?o.parentNode.insertBefore(e,n):o.insertBefore(e,n):(o.nodeType===8?(n=o.parentNode,n.insertBefore(e,o)):(n=o,n.appendChild(e)),o=o._reactRootContainer,o!=null||n.onclick!==null||(n.onclick=Uo));else if(l!==4&&(e=e.child,e!==null))for(Nl(e,n,o),e=e.sibling;e!==null;)Nl(e,n,o),e=e.sibling}function ql(e,n,o){var l=e.tag;if(l===5||l===6)e=e.stateNode,n?o.insertBefore(e,n):o.appendChild(e);else if(l!==4&&(e=e.child,e!==null))for(ql(e,n,o),e=e.sibling;e!==null;)ql(e,n,o),e=e.sibling}var Qe=null,Ot=!1;function Cn(e,n,o){for(o=o.child;o!==null;)up(e,n,o),o=o.sibling}function up(e,n,o){if(Bt&&typeof Bt.onCommitFiberUnmount=="function")try{Bt.onCommitFiberUnmount(vo,o)}catch{}switch(o.tag){case 5:nt||Aa(o,n);case 6:var l=Qe,d=Ot;Qe=null,Cn(e,n,o),Qe=l,Ot=d,Qe!==null&&(Ot?(e=Qe,o=o.stateNode,e.nodeType===8?e.parentNode.removeChild(o):e.removeChild(o)):Qe.removeChild(o.stateNode));break;case 18:Qe!==null&&(Ot?(e=Qe,o=o.stateNode,e.nodeType===8?Zr(e.parentNode,o):e.nodeType===1&&Zr(e,o),ci(e)):Zr(Qe,o.stateNode));break;case 4:l=Qe,d=Ot,Qe=o.stateNode.containerInfo,Ot=!0,Cn(e,n,o),Qe=l,Ot=d;break;case 0:case 11:case 14:case 15:if(!nt&&(l=o.updateQueue,l!==null&&(l=l.lastEffect,l!==null))){d=l=l.next;do{var u=d,w=u.destroy;u=u.tag,w!==void 0&&((u&2)!==0||(u&4)!==0)&&Ul(o,n,w),d=d.next}while(d!==l)}Cn(e,n,o);break;case 1:if(!nt&&(Aa(o,n),l=o.stateNode,typeof l.componentWillUnmount=="function"))try{l.props=o.memoizedProps,l.state=o.memoizedState,l.componentWillUnmount()}catch(k){De(o,n,k)}Cn(e,n,o);break;case 21:Cn(e,n,o);break;case 22:o.mode&1?(nt=(l=nt)||o.memoizedState!==null,Cn(e,n,o),nt=l):Cn(e,n,o);break;default:Cn(e,n,o)}}function pp(e){var n=e.updateQueue;if(n!==null){e.updateQueue=null;var o=e.stateNode;o===null&&(o=e.stateNode=new qv),n.forEach(function(l){var d=Yv.bind(null,e,l);o.has(l)||(o.add(l),l.then(d,d))})}}function Ut(e,n){var o=n.deletions;if(o!==null)for(var l=0;l<o.length;l++){var d=o[l];try{var u=e,w=n,k=w;e:for(;k!==null;){switch(k.tag){case 5:Qe=k.stateNode,Ot=!1;break e;case 3:Qe=k.stateNode.containerInfo,Ot=!0;break e;case 4:Qe=k.stateNode.containerInfo,Ot=!0;break e}k=k.return}if(Qe===null)throw Error(i(160));up(u,w,d),Qe=null,Ot=!1;var S=d.alternate;S!==null&&(S.return=null),d.return=null}catch(D){De(d,n,D)}}if(n.subtreeFlags&12854)for(n=n.child;n!==null;)mp(n,e),n=n.sibling}function mp(e,n){var o=e.alternate,l=e.flags;switch(e.tag){case 0:case 11:case 14:case 15:if(Ut(n,e),Jt(e),l&4){try{Ri(3,e,e.return),ss(3,e)}catch(Y){De(e,e.return,Y)}try{Ri(5,e,e.return)}catch(Y){De(e,e.return,Y)}}break;case 1:Ut(n,e),Jt(e),l&512&&o!==null&&Aa(o,o.return);break;case 5:if(Ut(n,e),Jt(e),l&512&&o!==null&&Aa(o,o.return),e.flags&32){var d=e.stateNode;try{Xa(d,"")}catch(Y){De(e,e.return,Y)}}if(l&4&&(d=e.stateNode,d!=null)){var u=e.memoizedProps,w=o!==null?o.memoizedProps:u,k=e.type,S=e.updateQueue;if(e.updateQueue=null,S!==null)try{k==="input"&&u.type==="radio"&&u.name!=null&&sa(d,u),yr(k,w);var D=yr(k,u);for(w=0;w<S.length;w+=2){var F=S[w],q=S[w+1];F==="style"?Yd(d,q):F==="dangerouslySetInnerHTML"?Gd(d,q):F==="children"?Xa(d,q):V(d,F,q,D)}switch(k){case"input":ur(d,u);break;case"textarea":_d(d,u);break;case"select":var U=d._wrapperState.wasMultiple;d._wrapperState.wasMultiple=!!u.multiple;var K=u.value;K!=null?ra(d,!!u.multiple,K,!1):U!==!!u.multiple&&(u.defaultValue!=null?ra(d,!!u.multiple,u.defaultValue,!0):ra(d,!!u.multiple,u.multiple?[]:"",!1))}d[bi]=u}catch(Y){De(e,e.return,Y)}}break;case 6:if(Ut(n,e),Jt(e),l&4){if(e.stateNode===null)throw Error(i(162));d=e.stateNode,u=e.memoizedProps;try{d.nodeValue=u}catch(Y){De(e,e.return,Y)}}break;case 3:if(Ut(n,e),Jt(e),l&4&&o!==null&&o.memoizedState.isDehydrated)try{ci(n.containerInfo)}catch(Y){De(e,e.return,Y)}break;case 4:Ut(n,e),Jt(e);break;case 13:Ut(n,e),Jt(e),d=e.child,d.flags&8192&&(u=d.memoizedState!==null,d.stateNode.isHidden=u,!u||d.alternate!==null&&d.alternate.memoizedState!==null||(Bl=je())),l&4&&pp(e);break;case 22:if(F=o!==null&&o.memoizedState!==null,e.mode&1?(nt=(D=nt)||F,Ut(n,e),nt=D):Ut(n,e),Jt(e),l&8192){if(D=e.memoizedState!==null,(e.stateNode.isHidden=D)&&!F&&(e.mode&1)!==0)for(J=e,F=e.child;F!==null;){for(q=J=F;J!==null;){switch(U=J,K=U.child,U.tag){case 0:case 11:case 14:case 15:Ri(4,U,U.return);break;case 1:Aa(U,U.return);var $=U.stateNode;if(typeof $.componentWillUnmount=="function"){l=U,o=U.return;try{n=l,$.props=n.memoizedProps,$.state=n.memoizedState,$.componentWillUnmount()}catch(Y){De(l,o,Y)}}break;case 5:Aa(U,U.return);break;case 22:if(U.memoizedState!==null){yp(q);continue}}K!==null?(K.return=U,J=K):yp(q)}F=F.sibling}e:for(F=null,q=e;;){if(q.tag===5){if(F===null){F=q;try{d=q.stateNode,D?(u=d.style,typeof u.setProperty=="function"?u.setProperty("display","none","important"):u.display="none"):(k=q.stateNode,S=q.memoizedProps.style,w=S!=null&&S.hasOwnProperty("display")?S.display:null,k.style.display=$d("display",w))}catch(Y){De(e,e.return,Y)}}}else if(q.tag===6){if(F===null)try{q.stateNode.nodeValue=D?"":q.memoizedProps}catch(Y){De(e,e.return,Y)}}else if((q.tag!==22&&q.tag!==23||q.memoizedState===null||q===e)&&q.child!==null){q.child.return=q,q=q.child;continue}if(q===e)break e;for(;q.sibling===null;){if(q.return===null||q.return===e)break e;F===q&&(F=null),q=q.return}F===q&&(F=null),q.sibling.return=q.return,q=q.sibling}}break;case 19:Ut(n,e),Jt(e),l&4&&pp(e);break;case 21:break;default:Ut(n,e),Jt(e)}}function Jt(e){var n=e.flags;if(n&2){try{e:{for(var o=e.return;o!==null;){if(dp(o)){var l=o;break e}o=o.return}throw Error(i(160))}switch(l.tag){case 5:var d=l.stateNode;l.flags&32&&(Xa(d,""),l.flags&=-33);var u=hp(e);ql(e,u,d);break;case 3:case 4:var w=l.stateNode.containerInfo,k=hp(e);Nl(e,k,w);break;default:throw Error(i(161))}}catch(S){De(e,e.return,S)}e.flags&=-3}n&4096&&(e.flags&=-4097)}function zv(e,n,o){J=e,fp(e)}function fp(e,n,o){for(var l=(e.mode&1)!==0;J!==null;){var d=J,u=d.child;if(d.tag===22&&l){var w=d.memoizedState!==null||os;if(!w){var k=d.alternate,S=k!==null&&k.memoizedState!==null||nt;k=os;var D=nt;if(os=w,(nt=S)&&!D)for(J=d;J!==null;)w=J,S=w.child,w.tag===22&&w.memoizedState!==null?wp(d):S!==null?(S.return=w,J=S):wp(d);for(;u!==null;)J=u,fp(u),u=u.sibling;J=d,os=k,nt=D}gp(e)}else(d.subtreeFlags&8772)!==0&&u!==null?(u.return=d,J=u):gp(e)}}function gp(e){for(;J!==null;){var n=J;if((n.flags&8772)!==0){var o=n.alternate;try{if((n.flags&8772)!==0)switch(n.tag){case 0:case 11:case 15:nt||ss(5,n);break;case 1:var l=n.stateNode;if(n.flags&4&&!nt)if(o===null)l.componentDidMount();else{var d=n.elementType===n.type?o.memoizedProps:jt(n.type,o.memoizedProps);l.componentDidUpdate(d,o.memoizedState,l.__reactInternalSnapshotBeforeUpdate)}var u=n.updateQueue;u!==null&&yu(n,u,l);break;case 3:var w=n.updateQueue;if(w!==null){if(o=null,n.child!==null)switch(n.child.tag){case 5:o=n.child.stateNode;break;case 1:o=n.child.stateNode}yu(n,w,o)}break;case 5:var k=n.stateNode;if(o===null&&n.flags&4){o=k;var S=n.memoizedProps;switch(n.type){case"button":case"input":case"select":case"textarea":S.autoFocus&&o.focus();break;case"img":S.src&&(o.src=S.src)}}break;case 6:break;case 4:break;case 12:break;case 13:if(n.memoizedState===null){var D=n.alternate;if(D!==null){var F=D.memoizedState;if(F!==null){var q=F.dehydrated;q!==null&&ci(q)}}}break;case 19:case 17:case 21:case 22:case 23:case 25:break;default:throw Error(i(163))}nt||n.flags&512&&Fl(n)}catch(U){De(n,n.return,U)}}if(n===e){J=null;break}if(o=n.sibling,o!==null){o.return=n.return,J=o;break}J=n.return}}function yp(e){for(;J!==null;){var n=J;if(n===e){J=null;break}var o=n.sibling;if(o!==null){o.return=n.return,J=o;break}J=n.return}}function wp(e){for(;J!==null;){var n=J;try{switch(n.tag){case 0:case 11:case 15:var o=n.return;try{ss(4,n)}catch(S){De(n,o,S)}break;case 1:var l=n.stateNode;if(typeof l.componentDidMount=="function"){var d=n.return;try{l.componentDidMount()}catch(S){De(n,d,S)}}var u=n.return;try{Fl(n)}catch(S){De(n,u,S)}break;case 5:var w=n.return;try{Fl(n)}catch(S){De(n,w,S)}}}catch(S){De(n,n.return,S)}if(n===e){J=null;break}var k=n.sibling;if(k!==null){k.return=n.return,J=k;break}J=n.return}}var Bv=Math.ceil,rs=B.ReactCurrentDispatcher,Wl=B.ReactCurrentOwner,Mt=B.ReactCurrentBatchConfig,ue=0,Ge=null,qe=null,Xe=0,vt=0,Ea=bn(0),ze=0,Li=null,Kn=0,ls=0,zl=0,Di=null,ht=null,Bl=0,Pa=1/0,cn=null,cs=!1,Hl=null,Mn=null,ds=!1,In=null,hs=0,Vi=0,_l=null,us=-1,ps=0;function st(){return(ue&6)!==0?je():us!==-1?us:us=je()}function An(e){return(e.mode&1)===0?1:(ue&2)!==0&&Xe!==0?Xe&-Xe:Mv.transition!==null?(ps===0&&(ps=hh()),ps):(e=ye,e!==0||(e=window.event,e=e===void 0?16:bh(e.type)),e)}function Ft(e,n,o,l){if(50<Vi)throw Vi=0,_l=null,Error(i(185));ii(e,o,l),((ue&2)===0||e!==Ge)&&(e===Ge&&((ue&2)===0&&(ls|=o),ze===4&&En(e,Xe)),ut(e,l),o===1&&ue===0&&(n.mode&1)===0&&(Pa=je()+500,Wo&&xn()))}function ut(e,n){var o=e.callbackNode;Mw(e,n);var l=xo(e,e===Ge?Xe:0);if(l===0)o!==null&&lh(o),e.callbackNode=null,e.callbackPriority=0;else if(n=l&-l,e.callbackPriority!==n){if(o!=null&&lh(o),n===1)e.tag===0?Cv(bp.bind(null,e)):ou(bp.bind(null,e)),kv(function(){(ue&6)===0&&xn()}),o=null;else{switch(uh(l)){case 1:o=Tr;break;case 4:o=ch;break;case 16:o=wo;break;case 536870912:o=dh;break;default:o=wo}o=Ap(o,vp.bind(null,e))}e.callbackPriority=n,e.callbackNode=o}}function vp(e,n){if(us=-1,ps=0,(ue&6)!==0)throw Error(i(327));var o=e.callbackNode;if(Ra()&&e.callbackNode!==o)return null;var l=xo(e,e===Ge?Xe:0);if(l===0)return null;if((l&30)!==0||(l&e.expiredLanes)!==0||n)n=ms(e,l);else{n=l;var d=ue;ue|=2;var u=xp();(Ge!==e||Xe!==n)&&(cn=null,Pa=je()+500,Gn(e,n));do try{Kv();break}catch(k){kp(e,k)}while(!0);cl(),rs.current=u,ue=d,qe!==null?n=0:(Ge=null,Xe=0,n=ze)}if(n!==0){if(n===2&&(d=Cr(e),d!==0&&(l=d,n=Kl(e,d))),n===1)throw o=Li,Gn(e,0),En(e,l),ut(e,je()),o;if(n===6)En(e,l);else{if(d=e.current.alternate,(l&30)===0&&!Hv(d)&&(n=ms(e,l),n===2&&(u=Cr(e),u!==0&&(l=u,n=Kl(e,u))),n===1))throw o=Li,Gn(e,0),En(e,l),ut(e,je()),o;switch(e.finishedWork=d,e.finishedLanes=l,n){case 0:case 1:throw Error(i(345));case 2:$n(e,ht,cn);break;case 3:if(En(e,l),(l&130023424)===l&&(n=Bl+500-je(),10<n)){if(xo(e,0)!==0)break;if(d=e.suspendedLanes,(d&l)!==l){st(),e.pingedLanes|=e.suspendedLanes&d;break}e.timeoutHandle=Xr($n.bind(null,e,ht,cn),n);break}$n(e,ht,cn);break;case 4:if(En(e,l),(l&4194240)===l)break;for(n=e.eventTimes,d=-1;0<l;){var w=31-Lt(l);u=1<<w,w=n[w],w>d&&(d=w),l&=~u}if(l=d,l=je()-l,l=(120>l?120:480>l?480:1080>l?1080:1920>l?1920:3e3>l?3e3:4320>l?4320:1960*Bv(l/1960))-l,10<l){e.timeoutHandle=Xr($n.bind(null,e,ht,cn),l);break}$n(e,ht,cn);break;case 5:$n(e,ht,cn);break;default:throw Error(i(329))}}}return ut(e,je()),e.callbackNode===o?vp.bind(null,e):null}function Kl(e,n){var o=Di;return e.current.memoizedState.isDehydrated&&(Gn(e,n).flags|=256),e=ms(e,n),e!==2&&(n=ht,ht=o,n!==null&&Jl(n)),e}function Jl(e){ht===null?ht=e:ht.push.apply(ht,e)}function Hv(e){for(var n=e;;){if(n.flags&16384){var o=n.updateQueue;if(o!==null&&(o=o.stores,o!==null))for(var l=0;l<o.length;l++){var d=o[l],u=d.getSnapshot;d=d.value;try{if(!Dt(u(),d))return!1}catch{return!1}}}if(o=n.child,n.subtreeFlags&16384&&o!==null)o.return=n,n=o;else{if(n===e)break;for(;n.sibling===null;){if(n.return===null||n.return===e)return!0;n=n.return}n.sibling.return=n.return,n=n.sibling}}return!0}function En(e,n){for(n&=~zl,n&=~ls,e.suspendedLanes|=n,e.pingedLanes&=~n,e=e.expirationTimes;0<n;){var o=31-Lt(n),l=1<<o;e[o]=-1,n&=~l}}function bp(e){if((ue&6)!==0)throw Error(i(327));Ra();var n=xo(e,0);if((n&1)===0)return ut(e,je()),null;var o=ms(e,n);if(e.tag!==0&&o===2){var l=Cr(e);l!==0&&(n=l,o=Kl(e,l))}if(o===1)throw o=Li,Gn(e,0),En(e,n),ut(e,je()),o;if(o===6)throw Error(i(345));return e.finishedWork=e.current.alternate,e.finishedLanes=n,$n(e,ht,cn),ut(e,je()),null}function Gl(e,n){var o=ue;ue|=1;try{return e(n)}finally{ue=o,ue===0&&(Pa=je()+500,Wo&&xn())}}function Jn(e){In!==null&&In.tag===0&&(ue&6)===0&&Ra();var n=ue;ue|=1;var o=Mt.transition,l=ye;try{if(Mt.transition=null,ye=1,e)return e()}finally{ye=l,Mt.transition=o,ue=n,(ue&6)===0&&xn()}}function $l(){vt=Ea.current,Se(Ea)}function Gn(e,n){e.finishedWork=null,e.finishedLanes=0;var o=e.timeoutHandle;if(o!==-1&&(e.timeoutHandle=-1,bv(o)),qe!==null)for(o=qe.return;o!==null;){var l=o;switch(il(l),l.tag){case 1:l=l.type.childContextTypes,l!=null&&No();break;case 3:Ma(),Se(lt),Se(Ze),yl();break;case 5:fl(l);break;case 4:Ma();break;case 13:Se(Ae);break;case 19:Se(Ae);break;case 10:dl(l.type._context);break;case 22:case 23:$l()}o=o.return}if(Ge=e,qe=e=Pn(e.current,null),Xe=vt=n,ze=0,Li=null,zl=ls=Kn=0,ht=Di=null,Bn!==null){for(n=0;n<Bn.length;n++)if(o=Bn[n],l=o.interleaved,l!==null){o.interleaved=null;var d=l.next,u=o.pending;if(u!==null){var w=u.next;u.next=d,l.next=w}o.pending=l}Bn=null}return e}function kp(e,n){do{var o=qe;try{if(cl(),Qo.current=ts,Xo){for(var l=Ee.memoizedState;l!==null;){var d=l.queue;d!==null&&(d.pending=null),l=l.next}Xo=!1}if(_n=0,Je=We=Ee=null,Mi=!1,Ii=0,Wl.current=null,o===null||o.return===null){ze=1,Li=n,qe=null;break}e:{var u=e,w=o.return,k=o,S=n;if(n=Xe,k.flags|=32768,S!==null&&typeof S=="object"&&typeof S.then=="function"){var D=S,F=k,q=F.tag;if((F.mode&1)===0&&(q===0||q===11||q===15)){var U=F.alternate;U?(F.updateQueue=U.updateQueue,F.memoizedState=U.memoizedState,F.lanes=U.lanes):(F.updateQueue=null,F.memoizedState=null)}var K=Ku(w);if(K!==null){K.flags&=-257,Ju(K,w,k,u,n),K.mode&1&&_u(u,D,n),n=K,S=D;var $=n.updateQueue;if($===null){var Y=new Set;Y.add(S),n.updateQueue=Y}else $.add(S);break e}else{if((n&1)===0){_u(u,D,n),Yl();break e}S=Error(i(426))}}else if(Te&&k.mode&1){var Oe=Ku(w);if(Oe!==null){(Oe.flags&65536)===0&&(Oe.flags|=256),Ju(Oe,w,k,u,n),rl(Ia(S,k));break e}}u=S=Ia(S,k),ze!==4&&(ze=2),Di===null?Di=[u]:Di.push(u),u=w;do{switch(u.tag){case 3:u.flags|=65536,n&=-n,u.lanes|=n;var P=Bu(u,S,n);gu(u,P);break e;case 1:k=S;var C=u.type,L=u.stateNode;if((u.flags&128)===0&&(typeof C.getDerivedStateFromError=="function"||L!==null&&typeof L.componentDidCatch=="function"&&(Mn===null||!Mn.has(L)))){u.flags|=65536,n&=-n,u.lanes|=n;var z=Hu(u,k,n);gu(u,z);break e}}u=u.return}while(u!==null)}Tp(o)}catch(Q){n=Q,qe===o&&o!==null&&(qe=o=o.return);continue}break}while(!0)}function xp(){var e=rs.current;return rs.current=ts,e===null?ts:e}function Yl(){(ze===0||ze===3||ze===2)&&(ze=4),Ge===null||(Kn&268435455)===0&&(ls&268435455)===0||En(Ge,Xe)}function ms(e,n){var o=ue;ue|=2;var l=xp();(Ge!==e||Xe!==n)&&(cn=null,Gn(e,n));do try{_v();break}catch(d){kp(e,d)}while(!0);if(cl(),ue=o,rs.current=l,qe!==null)throw Error(i(261));return Ge=null,Xe=0,ze}function _v(){for(;qe!==null;)Sp(qe)}function Kv(){for(;qe!==null&&!yw();)Sp(qe)}function Sp(e){var n=Ip(e.alternate,e,vt);e.memoizedProps=e.pendingProps,n===null?Tp(e):qe=n,Wl.current=null}function Tp(e){var n=e;do{var o=n.alternate;if(e=n.return,(n.flags&32768)===0){if(o=Fv(o,n,vt),o!==null){qe=o;return}}else{if(o=Nv(o,n),o!==null){o.flags&=32767,qe=o;return}if(e!==null)e.flags|=32768,e.subtreeFlags=0,e.deletions=null;else{ze=6,qe=null;return}}if(n=n.sibling,n!==null){qe=n;return}qe=n=e}while(n!==null);ze===0&&(ze=5)}function $n(e,n,o){var l=ye,d=Mt.transition;try{Mt.transition=null,ye=1,Jv(e,n,o,l)}finally{Mt.transition=d,ye=l}return null}function Jv(e,n,o,l){do Ra();while(In!==null);if((ue&6)!==0)throw Error(i(327));o=e.finishedWork;var d=e.finishedLanes;if(o===null)return null;if(e.finishedWork=null,e.finishedLanes=0,o===e.current)throw Error(i(177));e.callbackNode=null,e.callbackPriority=0;var u=o.lanes|o.childLanes;if(Iw(e,u),e===Ge&&(qe=Ge=null,Xe=0),(o.subtreeFlags&2064)===0&&(o.flags&2064)===0||ds||(ds=!0,Ap(wo,function(){return Ra(),null})),u=(o.flags&15990)!==0,(o.subtreeFlags&15990)!==0||u){u=Mt.transition,Mt.transition=null;var w=ye;ye=1;var k=ue;ue|=4,Wl.current=null,Wv(e,o),mp(o,e),pv(Yr),Co=!!$r,Yr=$r=null,e.current=o,zv(o),ww(),ue=k,ye=w,Mt.transition=u}else e.current=o;if(ds&&(ds=!1,In=e,hs=d),u=e.pendingLanes,u===0&&(Mn=null),kw(o.stateNode),ut(e,je()),n!==null)for(l=e.onRecoverableError,o=0;o<n.length;o++)d=n[o],l(d.value,{componentStack:d.stack,digest:d.digest});if(cs)throw cs=!1,e=Hl,Hl=null,e;return(hs&1)!==0&&e.tag!==0&&Ra(),u=e.pendingLanes,(u&1)!==0?e===_l?Vi++:(Vi=0,_l=e):Vi=0,xn(),null}function Ra(){if(In!==null){var e=uh(hs),n=Mt.transition,o=ye;try{if(Mt.transition=null,ye=16>e?16:e,In===null)var l=!1;else{if(e=In,In=null,hs=0,(ue&6)!==0)throw Error(i(331));var d=ue;for(ue|=4,J=e.current;J!==null;){var u=J,w=u.child;if((J.flags&16)!==0){var k=u.deletions;if(k!==null){for(var S=0;S<k.length;S++){var D=k[S];for(J=D;J!==null;){var F=J;switch(F.tag){case 0:case 11:case 15:Ri(8,F,u)}var q=F.child;if(q!==null)q.return=F,J=q;else for(;J!==null;){F=J;var U=F.sibling,K=F.return;if(cp(F),F===D){J=null;break}if(U!==null){U.return=K,J=U;break}J=K}}}var $=u.alternate;if($!==null){var Y=$.child;if(Y!==null){$.child=null;do{var Oe=Y.sibling;Y.sibling=null,Y=Oe}while(Y!==null)}}J=u}}if((u.subtreeFlags&2064)!==0&&w!==null)w.return=u,J=w;else e:for(;J!==null;){if(u=J,(u.flags&2048)!==0)switch(u.tag){case 0:case 11:case 15:Ri(9,u,u.return)}var P=u.sibling;if(P!==null){P.return=u.return,J=P;break e}J=u.return}}var C=e.current;for(J=C;J!==null;){w=J;var L=w.child;if((w.subtreeFlags&2064)!==0&&L!==null)L.return=w,J=L;else e:for(w=C;J!==null;){if(k=J,(k.flags&2048)!==0)try{switch(k.tag){case 0:case 11:case 15:ss(9,k)}}catch(Q){De(k,k.return,Q)}if(k===w){J=null;break e}var z=k.sibling;if(z!==null){z.return=k.return,J=z;break e}J=k.return}}if(ue=d,xn(),Bt&&typeof Bt.onPostCommitFiberRoot=="function")try{Bt.onPostCommitFiberRoot(vo,e)}catch{}l=!0}return l}finally{ye=o,Mt.transition=n}}return!1}function Cp(e,n,o){n=Ia(o,n),n=Bu(e,n,1),e=Tn(e,n,1),n=st(),e!==null&&(ii(e,1,n),ut(e,n))}function De(e,n,o){if(e.tag===3)Cp(e,e,o);else for(;n!==null;){if(n.tag===3){Cp(n,e,o);break}else if(n.tag===1){var l=n.stateNode;if(typeof n.type.getDerivedStateFromError=="function"||typeof l.componentDidCatch=="function"&&(Mn===null||!Mn.has(l))){e=Ia(o,e),e=Hu(n,e,1),n=Tn(n,e,1),e=st(),n!==null&&(ii(n,1,e),ut(n,e));break}}n=n.return}}function Gv(e,n,o){var l=e.pingCache;l!==null&&l.delete(n),n=st(),e.pingedLanes|=e.suspendedLanes&o,Ge===e&&(Xe&o)===o&&(ze===4||ze===3&&(Xe&130023424)===Xe&&500>je()-Bl?Gn(e,0):zl|=o),ut(e,n)}function Mp(e,n){n===0&&((e.mode&1)===0?n=1:(n=ko,ko<<=1,(ko&130023424)===0&&(ko=4194304)));var o=st();e=sn(e,n),e!==null&&(ii(e,n,o),ut(e,o))}function $v(e){var n=e.memoizedState,o=0;n!==null&&(o=n.retryLane),Mp(e,o)}function Yv(e,n){var o=0;switch(e.tag){case 13:var l=e.stateNode,d=e.memoizedState;d!==null&&(o=d.retryLane);break;case 19:l=e.stateNode;break;default:throw Error(i(314))}l!==null&&l.delete(n),Mp(e,o)}var Ip;Ip=function(e,n,o){if(e!==null)if(e.memoizedProps!==n.pendingProps||lt.current)dt=!0;else{if((e.lanes&o)===0&&(n.flags&128)===0)return dt=!1,Uv(e,n,o);dt=(e.flags&131072)!==0}else dt=!1,Te&&(n.flags&1048576)!==0&&su(n,Bo,n.index);switch(n.lanes=0,n.tag){case 2:var l=n.type;is(e,n),e=n.pendingProps;var d=va(n,Ze.current);Ca(n,o),d=bl(null,n,l,e,d,o);var u=kl();return n.flags|=1,typeof d=="object"&&d!==null&&typeof d.render=="function"&&d.$$typeof===void 0?(n.tag=1,n.memoizedState=null,n.updateQueue=null,ct(l)?(u=!0,qo(n)):u=!1,n.memoizedState=d.state!==null&&d.state!==void 0?d.state:null,pl(n),d.updater=ns,n.stateNode=d,d._reactInternals=n,Il(n,l,e,o),n=Rl(null,n,l,!0,u,o)):(n.tag=0,Te&&u&&al(n),ot(null,n,d,o),n=n.child),n;case 16:l=n.elementType;e:{switch(is(e,n),e=n.pendingProps,d=l._init,l=d(l._payload),n.type=l,d=n.tag=Xv(l),e=jt(l,e),d){case 0:n=Pl(null,n,l,e,o);break e;case 1:n=Zu(null,n,l,e,o);break e;case 11:n=Gu(null,n,l,e,o);break e;case 14:n=$u(null,n,l,jt(l.type,e),o);break e}throw Error(i(306,l,""))}return n;case 0:return l=n.type,d=n.pendingProps,d=n.elementType===l?d:jt(l,d),Pl(e,n,l,d,o);case 1:return l=n.type,d=n.pendingProps,d=n.elementType===l?d:jt(l,d),Zu(e,n,l,d,o);case 3:e:{if(ep(n),e===null)throw Error(i(387));l=n.pendingProps,u=n.memoizedState,d=u.element,fu(e,n),$o(n,l,null,o);var w=n.memoizedState;if(l=w.element,u.isDehydrated)if(u={element:l,isDehydrated:!1,cache:w.cache,pendingSuspenseBoundaries:w.pendingSuspenseBoundaries,transitions:w.transitions},n.updateQueue.baseState=u,n.memoizedState=u,n.flags&256){d=Ia(Error(i(423)),n),n=tp(e,n,l,o,d);break e}else if(l!==d){d=Ia(Error(i(424)),n),n=tp(e,n,l,o,d);break e}else for(wt=vn(n.stateNode.containerInfo.firstChild),yt=n,Te=!0,Vt=null,o=pu(n,null,l,o),n.child=o;o;)o.flags=o.flags&-3|4096,o=o.sibling;else{if(xa(),l===d){n=ln(e,n,o);break e}ot(e,n,l,o)}n=n.child}return n;case 5:return wu(n),e===null&&sl(n),l=n.type,d=n.pendingProps,u=e!==null?e.memoizedProps:null,w=d.children,Qr(l,d)?w=null:u!==null&&Qr(l,u)&&(n.flags|=32),Xu(e,n),ot(e,n,w,o),n.child;case 6:return e===null&&sl(n),null;case 13:return np(e,n,o);case 4:return ml(n,n.stateNode.containerInfo),l=n.pendingProps,e===null?n.child=Sa(n,null,l,o):ot(e,n,l,o),n.child;case 11:return l=n.type,d=n.pendingProps,d=n.elementType===l?d:jt(l,d),Gu(e,n,l,d,o);case 7:return ot(e,n,n.pendingProps,o),n.child;case 8:return ot(e,n,n.pendingProps.children,o),n.child;case 12:return ot(e,n,n.pendingProps.children,o),n.child;case 10:e:{if(l=n.type._context,d=n.pendingProps,u=n.memoizedProps,w=d.value,ke(Ko,l._currentValue),l._currentValue=w,u!==null)if(Dt(u.value,w)){if(u.children===d.children&&!lt.current){n=ln(e,n,o);break e}}else for(u=n.child,u!==null&&(u.return=n);u!==null;){var k=u.dependencies;if(k!==null){w=u.child;for(var S=k.firstContext;S!==null;){if(S.context===l){if(u.tag===1){S=rn(-1,o&-o),S.tag=2;var D=u.updateQueue;if(D!==null){D=D.shared;var F=D.pending;F===null?S.next=S:(S.next=F.next,F.next=S),D.pending=S}}u.lanes|=o,S=u.alternate,S!==null&&(S.lanes|=o),hl(u.return,o,n),k.lanes|=o;break}S=S.next}}else if(u.tag===10)w=u.type===n.type?null:u.child;else if(u.tag===18){if(w=u.return,w===null)throw Error(i(341));w.lanes|=o,k=w.alternate,k!==null&&(k.lanes|=o),hl(w,o,n),w=u.sibling}else w=u.child;if(w!==null)w.return=u;else for(w=u;w!==null;){if(w===n){w=null;break}if(u=w.sibling,u!==null){u.return=w.return,w=u;break}w=w.return}u=w}ot(e,n,d.children,o),n=n.child}return n;case 9:return d=n.type,l=n.pendingProps.children,Ca(n,o),d=Tt(d),l=l(d),n.flags|=1,ot(e,n,l,o),n.child;case 14:return l=n.type,d=jt(l,n.pendingProps),d=jt(l.type,d),$u(e,n,l,d,o);case 15:return Yu(e,n,n.type,n.pendingProps,o);case 17:return l=n.type,d=n.pendingProps,d=n.elementType===l?d:jt(l,d),is(e,n),n.tag=1,ct(l)?(e=!0,qo(n)):e=!1,Ca(n,o),Wu(n,l,d),Il(n,l,d,o),Rl(null,n,l,!0,e,o);case 19:return ip(e,n,o);case 22:return Qu(e,n,o)}throw Error(i(156,n.tag))};function Ap(e,n){return rh(e,n)}function Qv(e,n,o,l){this.tag=e,this.key=o,this.sibling=this.child=this.return=this.stateNode=this.type=this.elementType=null,this.index=0,this.ref=null,this.pendingProps=n,this.dependencies=this.memoizedState=this.updateQueue=this.memoizedProps=null,this.mode=l,this.subtreeFlags=this.flags=0,this.deletions=null,this.childLanes=this.lanes=0,this.alternate=null}function It(e,n,o,l){return new Qv(e,n,o,l)}function Ql(e){return e=e.prototype,!(!e||!e.isReactComponent)}function Xv(e){if(typeof e=="function")return Ql(e)?1:0;if(e!=null){if(e=e.$$typeof,e===_e)return 11;if(e===Le)return 14}return 2}function Pn(e,n){var o=e.alternate;return o===null?(o=It(e.tag,n,e.key,e.mode),o.elementType=e.elementType,o.type=e.type,o.stateNode=e.stateNode,o.alternate=e,e.alternate=o):(o.pendingProps=n,o.type=e.type,o.flags=0,o.subtreeFlags=0,o.deletions=null),o.flags=e.flags&14680064,o.childLanes=e.childLanes,o.lanes=e.lanes,o.child=e.child,o.memoizedProps=e.memoizedProps,o.memoizedState=e.memoizedState,o.updateQueue=e.updateQueue,n=e.dependencies,o.dependencies=n===null?null:{lanes:n.lanes,firstContext:n.firstContext},o.sibling=e.sibling,o.index=e.index,o.ref=e.ref,o}function fs(e,n,o,l,d,u){var w=2;if(l=e,typeof e=="function")Ql(e)&&(w=1);else if(typeof e=="string")w=5;else e:switch(e){case ae:return Yn(o.children,d,u,n);case G:w=8,d|=8;break;case ce:return e=It(12,o,n,d|2),e.elementType=ce,e.lanes=u,e;case Fe:return e=It(13,o,n,d),e.elementType=Fe,e.lanes=u,e;case Ne:return e=It(19,o,n,d),e.elementType=Ne,e.lanes=u,e;case oe:return gs(o,d,u,n);default:if(typeof e=="object"&&e!==null)switch(e.$$typeof){case we:w=10;break e;case Me:w=9;break e;case _e:w=11;break e;case Le:w=14;break e;case Ke:w=16,l=null;break e}throw Error(i(130,e==null?e:typeof e,""))}return n=It(w,o,n,d),n.elementType=e,n.type=l,n.lanes=u,n}function Yn(e,n,o,l){return e=It(7,e,l,n),e.lanes=o,e}function gs(e,n,o,l){return e=It(22,e,l,n),e.elementType=oe,e.lanes=o,e.stateNode={isHidden:!1},e}function Xl(e,n,o){return e=It(6,e,null,n),e.lanes=o,e}function Zl(e,n,o){return n=It(4,e.children!==null?e.children:[],e.key,n),n.lanes=o,n.stateNode={containerInfo:e.containerInfo,pendingChildren:null,implementation:e.implementation},n}function Zv(e,n,o,l,d){this.tag=n,this.containerInfo=e,this.finishedWork=this.pingCache=this.current=this.pendingChildren=null,this.timeoutHandle=-1,this.callbackNode=this.pendingContext=this.context=null,this.callbackPriority=0,this.eventTimes=Mr(0),this.expirationTimes=Mr(-1),this.entangledLanes=this.finishedLanes=this.mutableReadLanes=this.expiredLanes=this.pingedLanes=this.suspendedLanes=this.pendingLanes=0,this.entanglements=Mr(0),this.identifierPrefix=l,this.onRecoverableError=d,this.mutableSourceEagerHydrationData=null}function ec(e,n,o,l,d,u,w,k,S){return e=new Zv(e,n,o,k,S),n===1?(n=1,u===!0&&(n|=8)):n=0,u=It(3,null,null,n),e.current=u,u.stateNode=e,u.memoizedState={element:l,isDehydrated:o,cache:null,transitions:null,pendingSuspenseBoundaries:null},pl(u),e}function eb(e,n,o){var l=3<arguments.length&&arguments[3]!==void 0?arguments[3]:null;return{$$typeof:_,key:l==null?null:""+l,children:e,containerInfo:n,implementation:o}}function Ep(e){if(!e)return kn;e=e._reactInternals;e:{if(Fn(e)!==e||e.tag!==1)throw Error(i(170));var n=e;do{switch(n.tag){case 3:n=n.stateNode.context;break e;case 1:if(ct(n.type)){n=n.stateNode.__reactInternalMemoizedMergedChildContext;break e}}n=n.return}while(n!==null);throw Error(i(171))}if(e.tag===1){var o=e.type;if(ct(o))return au(e,o,n)}return n}function Pp(e,n,o,l,d,u,w,k,S){return e=ec(o,l,!0,e,d,u,w,k,S),e.context=Ep(null),o=e.current,l=st(),d=An(o),u=rn(l,d),u.callback=n??null,Tn(o,u,d),e.current.lanes=d,ii(e,d,l),ut(e,l),e}function ys(e,n,o,l){var d=n.current,u=st(),w=An(d);return o=Ep(o),n.context===null?n.context=o:n.pendingContext=o,n=rn(u,w),n.payload={element:e},l=l===void 0?null:l,l!==null&&(n.callback=l),e=Tn(d,n,w),e!==null&&(Ft(e,d,w,u),Go(e,d,w)),w}function ws(e){if(e=e.current,!e.child)return null;switch(e.child.tag){case 5:return e.child.stateNode;default:return e.child.stateNode}}function Rp(e,n){if(e=e.memoizedState,e!==null&&e.dehydrated!==null){var o=e.retryLane;e.retryLane=o!==0&&o<n?o:n}}function tc(e,n){Rp(e,n),(e=e.alternate)&&Rp(e,n)}function tb(){return null}var Lp=typeof reportError=="function"?reportError:function(e){console.error(e)};function nc(e){this._internalRoot=e}vs.prototype.render=nc.prototype.render=function(e){var n=this._internalRoot;if(n===null)throw Error(i(409));ys(e,n,null,null)},vs.prototype.unmount=nc.prototype.unmount=function(){var e=this._internalRoot;if(e!==null){this._internalRoot=null;var n=e.containerInfo;Jn(function(){ys(null,e,null,null)}),n[tn]=null}};function vs(e){this._internalRoot=e}vs.prototype.unstable_scheduleHydration=function(e){if(e){var n=fh();e={blockedOn:null,target:e,priority:n};for(var o=0;o<gn.length&&n!==0&&n<gn[o].priority;o++);gn.splice(o,0,e),o===0&&wh(e)}};function ac(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11)}function bs(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11&&(e.nodeType!==8||e.nodeValue!==" react-mount-point-unstable "))}function Dp(){}function nb(e,n,o,l,d){if(d){if(typeof l=="function"){var u=l;l=function(){var D=ws(w);u.call(D)}}var w=Pp(n,l,e,0,null,!1,!1,"",Dp);return e._reactRootContainer=w,e[tn]=w.current,wi(e.nodeType===8?e.parentNode:e),Jn(),w}for(;d=e.lastChild;)e.removeChild(d);if(typeof l=="function"){var k=l;l=function(){var D=ws(S);k.call(D)}}var S=ec(e,0,!1,null,null,!1,!1,"",Dp);return e._reactRootContainer=S,e[tn]=S.current,wi(e.nodeType===8?e.parentNode:e),Jn(function(){ys(n,S,o,l)}),S}function ks(e,n,o,l,d){var u=o._reactRootContainer;if(u){var w=u;if(typeof d=="function"){var k=d;d=function(){var S=ws(w);k.call(S)}}ys(n,w,e,d)}else w=nb(o,n,e,d,l);return ws(w)}ph=function(e){switch(e.tag){case 3:var n=e.stateNode;if(n.current.memoizedState.isDehydrated){var o=ai(n.pendingLanes);o!==0&&(Ir(n,o|1),ut(n,je()),(ue&6)===0&&(Pa=je()+500,xn()))}break;case 13:Jn(function(){var l=sn(e,1);if(l!==null){var d=st();Ft(l,e,1,d)}}),tc(e,1)}},Ar=function(e){if(e.tag===13){var n=sn(e,134217728);if(n!==null){var o=st();Ft(n,e,134217728,o)}tc(e,134217728)}},mh=function(e){if(e.tag===13){var n=An(e),o=sn(e,n);if(o!==null){var l=st();Ft(o,e,n,l)}tc(e,n)}},fh=function(){return ye},gh=function(e,n){var o=ye;try{return ye=e,n()}finally{ye=o}},br=function(e,n,o){switch(n){case"input":if(ur(e,o),n=o.name,o.type==="radio"&&n!=null){for(o=e;o.parentNode;)o=o.parentNode;for(o=o.querySelectorAll("input[name="+JSON.stringify(""+n)+'][type="radio"]'),n=0;n<o.length;n++){var l=o[n];if(l!==e&&l.form===e.form){var d=Fo(l);if(!d)throw Error(i(90));it(l),ur(l,d)}}}break;case"textarea":_d(e,o);break;case"select":n=o.value,n!=null&&ra(e,!!o.multiple,n,!1)}},eh=Gl,th=Jn;var ab={usingClientEntryPoint:!1,Events:[ki,ya,Fo,Xd,Zd,Gl]},ji={findFiberByHostInstance:Nn,bundleType:0,version:"18.3.1",rendererPackageName:"react-dom"},ib={bundleType:ji.bundleType,version:ji.version,rendererPackageName:ji.rendererPackageName,rendererConfig:ji.rendererConfig,overrideHookState:null,overrideHookStateDeletePath:null,overrideHookStateRenamePath:null,overrideProps:null,overridePropsDeletePath:null,overridePropsRenamePath:null,setErrorHandler:null,setSuspenseHandler:null,scheduleUpdate:null,currentDispatcherRef:B.ReactCurrentDispatcher,findHostInstanceByFiber:function(e){return e=oh(e),e===null?null:e.stateNode},findFiberByHostInstance:ji.findFiberByHostInstance||tb,findHostInstancesForRefresh:null,scheduleRefresh:null,scheduleRoot:null,setRefreshHandler:null,getCurrentFiber:null,reconcilerVersion:"18.3.1-next-f1338f8080-20240426"};if(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__<"u"){var xs=__REACT_DEVTOOLS_GLOBAL_HOOK__;if(!xs.isDisabled&&xs.supportsFiber)try{vo=xs.inject(ib),Bt=xs}catch{}}return pt.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=ab,pt.createPortal=function(e,n){var o=2<arguments.length&&arguments[2]!==void 0?arguments[2]:null;if(!ac(n))throw Error(i(200));return eb(e,n,null,o)},pt.createRoot=function(e,n){if(!ac(e))throw Error(i(299));var o=!1,l="",d=Lp;return n!=null&&(n.unstable_strictMode===!0&&(o=!0),n.identifierPrefix!==void 0&&(l=n.identifierPrefix),n.onRecoverableError!==void 0&&(d=n.onRecoverableError)),n=ec(e,1,!1,null,null,o,!1,l,d),e[tn]=n.current,wi(e.nodeType===8?e.parentNode:e),new nc(n)},pt.findDOMNode=function(e){if(e==null)return null;if(e.nodeType===1)return e;var n=e._reactInternals;if(n===void 0)throw typeof e.render=="function"?Error(i(188)):(e=Object.keys(e).join(","),Error(i(268,e)));return e=oh(n),e=e===null?null:e.stateNode,e},pt.flushSync=function(e){return Jn(e)},pt.hydrate=function(e,n,o){if(!bs(n))throw Error(i(200));return ks(null,e,n,!0,o)},pt.hydrateRoot=function(e,n,o){if(!ac(e))throw Error(i(405));var l=o!=null&&o.hydratedSources||null,d=!1,u="",w=Lp;if(o!=null&&(o.unstable_strictMode===!0&&(d=!0),o.identifierPrefix!==void 0&&(u=o.identifierPrefix),o.onRecoverableError!==void 0&&(w=o.onRecoverableError)),n=Pp(n,null,e,1,o??null,d,!1,u,w),e[tn]=n.current,wi(e),l)for(e=0;e<l.length;e++)o=l[e],d=o._getVersion,d=d(o._source),n.mutableSourceEagerHydrationData==null?n.mutableSourceEagerHydrationData=[o,d]:n.mutableSourceEagerHydrationData.push(o,d);return new vs(n)},pt.render=function(e,n,o){if(!bs(n))throw Error(i(200));return ks(null,e,n,!1,o)},pt.unmountComponentAtNode=function(e){if(!bs(e))throw Error(i(40));return e._reactRootContainer?(Jn(function(){ks(null,null,e,!1,function(){e._reactRootContainer=null,e[tn]=null})}),!0):!1},pt.unstable_batchedUpdates=Gl,pt.unstable_renderSubtreeIntoContainer=function(e,n,o,l){if(!bs(o))throw Error(i(200));if(e==null||e._reactInternals===void 0)throw Error(i(38));return ks(e,n,o,!1,l)},pt.version="18.3.1-next-f1338f8080-20240426",pt}var Wp;function fb(){if(Wp)return sc.exports;Wp=1;function t(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>"u"||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!="function"))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(t)}catch(a){console.error(a)}}return t(),sc.exports=mb(),sc.exports}var zp;function gb(){if(zp)return Ss;zp=1;var t=fb();return Ss.createRoot=t.createRoot,Ss.hydrateRoot=t.hydrateRoot,Ss}var yb=gb();const wb=Of(yb);/**
 * react-router v7.18.1
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */var Zc=/^(?:[a-z][a-z0-9+.-]*:|[\\/]{2})/i,Uf=/^[\\/]{2}/;function vb(t,a){return a+t.replace(/\\/g,"/")}var Bp="popstate";function Hp(t){return typeof t=="object"&&t!=null&&"pathname"in t&&"search"in t&&"hash"in t&&"state"in t&&"key"in t}function bb(t={}){function a(s,r){var f;let c=(f=r.state)==null?void 0:f.masked,{pathname:h,search:p,hash:m}=c||s.location;return Rc("",{pathname:h,search:p,hash:m},r.state&&r.state.usr||null,r.state&&r.state.key||"default",c?{pathname:s.location.pathname,search:s.location.search,hash:s.location.hash}:void 0)}function i(s,r){return typeof r=="string"?r:Qi(r)}return xb(a,i,null,t)}function Ce(t,a){if(t===!1||t===null||typeof t>"u")throw new Error(a)}function Wt(t,a){if(!t){typeof console<"u"&&console.warn(a);try{throw new Error(a)}catch{}}}function kb(){return Math.random().toString(36).substring(2,10)}function _p(t,a){return{usr:t.state,key:t.key,idx:a,masked:t.mask?{pathname:t.pathname,search:t.search,hash:t.hash}:void 0}}function Rc(t,a,i=null,s,r){return{pathname:typeof t=="string"?t:t.pathname,search:"",hash:"",...typeof a=="string"?Ba(a):a,state:i,key:a&&a.key||s||kb(),mask:r}}function Qi({pathname:t="/",search:a="",hash:i=""}){return a&&a!=="?"&&(t+=a.charAt(0)==="?"?a:"?"+a),i&&i!=="#"&&(t+=i.charAt(0)==="#"?i:"#"+i),t}function Ba(t){let a={};if(t){let i=t.indexOf("#");i>=0&&(a.hash=t.substring(i),t=t.substring(0,i));let s=t.indexOf("?");s>=0&&(a.search=t.substring(s),t=t.substring(0,s)),t&&(a.pathname=t)}return a}function xb(t,a,i,s={}){let{window:r=document.defaultView,v5Compat:c=!1}=s,h=r.history,p="POP",m=null,f=y();f==null&&(f=0,h.replaceState({...h.state,idx:f},""));function y(){return(h.state||{idx:null}).idx}function v(){p="POP";let A=y(),R=A==null?null:A-f;f=A,m&&m({action:p,location:I.location,delta:R})}function b(A,R){p="PUSH";let j=Hp(A)?A:Rc(I.location,A,R);f=y()+1;let V=_p(j,f),B=I.createHref(j.mask||j);try{h.pushState(V,"",B)}catch(N){if(N instanceof DOMException&&N.name==="DataCloneError")throw N;r.location.assign(B)}c&&m&&m({action:p,location:I.location,delta:1})}function T(A,R){p="REPLACE";let j=Hp(A)?A:Rc(I.location,A,R);f=y();let V=_p(j,f),B=I.createHref(j.mask||j);h.replaceState(V,"",B),c&&m&&m({action:p,location:I.location,delta:0})}function M(A){return Sb(r,A)}let I={get action(){return p},get location(){return t(r,h)},listen(A){if(m)throw new Error("A history only accepts one active listener");return r.addEventListener(Bp,v),m=A,()=>{r.removeEventListener(Bp,v),m=null}},createHref(A){return a(r,A)},createURL:M,encodeLocation(A){let R=M(A);return{pathname:R.pathname,search:R.search,hash:R.hash}},push:b,replace:T,go(A){return h.go(A)}};return I}function Sb(t,a,i=!1){let s="http://localhost";t&&(s=t.location.origin!=="null"?t.location.origin:t.location.href),Ce(s,"No window.location.(origin|href) available to create URL");let r=typeof a=="string"?a:Qi(a);return r=r.replace(/ $/,"%20"),!i&&Uf.test(r)&&(r=s+r),new URL(r,s)}function Ff(t,a,i="/"){return Tb(t,a,i,!1)}function Tb(t,a,i,s,r){let c=typeof a=="string"?Ba(a):a,h=un(c.pathname||"/",i);if(h==null)return null;let p=Cb(t),m=null,f=Ob(h);for(let y=0;m==null&&y<p.length;++y)m=jb(p[y],f,s);return m}function Cb(t){let a=Nf(t);return Mb(a),a}function Nf(t,a=[],i=[],s="",r=!1){let c=(h,p,m=r,f)=>{let y={relativePath:f===void 0?h.path||"":f,caseSensitive:h.caseSensitive===!0,childrenIndex:p,route:h};if(y.relativePath.startsWith("/")){if(!y.relativePath.startsWith(s)&&m)return;Ce(y.relativePath.startsWith(s),`Absolute route path "${y.relativePath}" nested under path "${s}" is not valid. An absolute child route path must start with the combined path of all its parent routes.`),y.relativePath=y.relativePath.slice(s.length)}let v=qt([s,y.relativePath]),b=i.concat(y);h.children&&h.children.length>0&&(Ce(h.index!==!0,`Index routes must not have child routes. Please remove all child routes from route path "${v}".`),Nf(h.children,a,b,v,m)),!(h.path==null&&!h.index)&&a.push({path:v,score:Db(v,h.index),routesMeta:b.map((T,M)=>{let[I,A]=zf(T.relativePath,T.caseSensitive,M===b.length-1);return{...T,matcher:I,compiledParams:A}})})};return t.forEach((h,p)=>{var m;if(h.path===""||!((m=h.path)!=null&&m.includes("?")))c(h,p);else for(let f of qf(h.path))c(h,p,!0,f)}),a}function qf(t){let a=t.split("/");if(a.length===0)return[];let[i,...s]=a,r=i.endsWith("?"),c=i.replace(/\?$/,"");if(s.length===0)return r?[c,""]:[c];let h=qf(s.join("/")),p=[];return p.push(...h.map(m=>m===""?c:[c,m].join("/"))),r&&p.push(...h),p.map(m=>t.startsWith("/")&&m===""?"/":m)}function Mb(t){t.sort((a,i)=>a.score!==i.score?i.score-a.score:Vb(a.routesMeta.map(s=>s.childrenIndex),i.routesMeta.map(s=>s.childrenIndex)))}var Ib=/^:[\w-]+$/,Ab=3,Eb=2,Pb=1,Rb=10,Lb=-2,Kp=t=>t==="*";function Db(t,a){let i=t.split("/"),s=i.length;return i.some(Kp)&&(s+=Lb),a&&(s+=Eb),i.filter(r=>!Kp(r)).reduce((r,c)=>r+(Ib.test(c)?Ab:c===""?Pb:Rb),s)}function Vb(t,a){return t.length===a.length&&t.slice(0,-1).every((s,r)=>s===a[r])?t[t.length-1]-a[a.length-1]:0}function jb(t,a,i=!1){let{routesMeta:s}=t,r={},c="/",h=[];for(let p=0;p<s.length;++p){let m=s[p],f=p===s.length-1,y=c==="/"?a:a.slice(c.length)||"/",v={path:m.relativePath,caseSensitive:m.caseSensitive,end:f},b=m.matcher&&m.compiledParams?Wf(v,y,m.matcher,m.compiledParams):Ns(v,y),T=m.route;if(!b&&f&&i&&!s[s.length-1].route.index&&(b=Ns({path:m.relativePath,caseSensitive:m.caseSensitive,end:!1},y)),!b)return null;Object.assign(r,b.params),h.push({params:r,pathname:qt([c,b.pathname]),pathnameBase:Nb(qt([c,b.pathnameBase])),route:T}),b.pathnameBase!=="/"&&(c=qt([c,b.pathnameBase]))}return h}function Ns(t,a){typeof t=="string"&&(t={path:t,caseSensitive:!1,end:!0});let[i,s]=zf(t.path,t.caseSensitive,t.end);return Wf(t,a,i,s)}function Wf(t,a,i,s){let r=a.match(i);if(!r)return null;let c=r[0],h=c.replace(/(.)\/+$/,"$1"),p=r.slice(1);return{params:s.reduce((f,{paramName:y,isOptional:v},b)=>{if(y==="*"){let M=p[b]||"";h=c.slice(0,c.length-M.length).replace(/(.)\/+$/,"$1")}const T=p[b];return v&&!T?f[y]=void 0:f[y]=(T||"").replace(/%2F/g,"/"),f},{}),pathname:c,pathnameBase:h,pattern:t}}function zf(t,a=!1,i=!0){Wt(t==="*"||!t.endsWith("*")||t.endsWith("/*"),`Route path "${t}" will be treated as if it were "${t.replace(/\*$/,"/*")}" because the \`*\` character must always follow a \`/\` in the pattern. To get rid of this warning, please change the route path to "${t.replace(/\*$/,"/*")}".`);let s=[],r="^"+t.replace(/\/*\*?$/,"").replace(/^\/*/,"/").replace(/[\\.*+^${}|()[\]]/g,"\\$&").replace(/\/:([\w-]+)(\?)?/g,(h,p,m,f,y)=>{if(s.push({paramName:p,isOptional:m!=null}),m){let v=y.charAt(f+h.length);return v&&v!=="/"?"/([^\\/]*)":"(?:/([^\\/]*))?"}return"/([^\\/]+)"}).replace(/\/([\w-]+)\?(\/|$)/g,"(/$1)?$2");return t.endsWith("*")?(s.push({paramName:"*"}),r+=t==="*"||t==="/*"?"(.*)$":"(?:\\/(.+)|\\/*)$"):i?r+="\\/*$":t!==""&&t!=="/"&&(r+="(?:(?=\\/|$))"),[new RegExp(r,a?void 0:"i"),s]}function Ob(t){try{return t.split("/").map(a=>decodeURIComponent(a).replace(/\//g,"%2F")).join("/")}catch(a){return Wt(!1,`The URL path "${t}" could not be decoded because it is a malformed URL segment. This is probably due to a bad percent encoding (${a}).`),t}}function un(t,a){if(a==="/")return t;if(!t.toLowerCase().startsWith(a.toLowerCase()))return null;let i=a.endsWith("/")?a.length-1:a.length,s=t.charAt(i);return s&&s!=="/"?null:t.slice(i)||"/"}function Ub(t,a="/"){let{pathname:i,search:s="",hash:r=""}=typeof t=="string"?Ba(t):t,c;return i?(i=Bf(i),i.startsWith("/")?c=Jp(i.substring(1),"/"):c=Jp(i,a)):c=a,{pathname:c,search:qb(s),hash:Wb(r)}}function Jp(t,a){let i=qs(a).split("/");return t.split("/").forEach(r=>{r===".."?i.length>1&&i.pop():r!=="."&&i.push(r)}),i.length>1?i.join("/"):"/"}function cc(t,a,i,s){return`Cannot include a '${t}' character in a manually specified \`to.${a}\` field [${JSON.stringify(s)}].  Please separate it out to the \`to.${i}\` field. Alternatively you may provide the full path as a string in <Link to="..."> and the router will parse it for you.`}function Fb(t){return t.filter((a,i)=>i===0||a.route.path&&a.route.path.length>0)}function ed(t){let a=Fb(t);return a.map((i,s)=>s===a.length-1?i.pathname:i.pathnameBase)}function Qs(t,a,i,s=!1){let r;typeof t=="string"?r=Ba(t):(r={...t},Ce(!r.pathname||!r.pathname.includes("?"),cc("?","pathname","search",r)),Ce(!r.pathname||!r.pathname.includes("#"),cc("#","pathname","hash",r)),Ce(!r.search||!r.search.includes("#"),cc("#","search","hash",r)));let c=t===""||r.pathname==="",h=c?"/":r.pathname,p;if(h==null)p=i;else{let v=a.length-1;if(!s&&h.startsWith("..")){let b=h.split("/");for(;b[0]==="..";)b.shift(),v-=1;r.pathname=b.join("/")}p=v>=0?a[v]:"/"}let m=Ub(r,p),f=h&&h!=="/"&&h.endsWith("/"),y=(c||h===".")&&i.endsWith("/");return!m.pathname.endsWith("/")&&(f||y)&&(m.pathname+="/"),m}var Bf=t=>t.replace(/[\\/]{2,}/g,"/"),qt=t=>Bf(t.join("/")),qs=t=>t.replace(/\/+$/,""),Nb=t=>qs(t).replace(/^\/*/,"/"),qb=t=>!t||t==="?"?"":t.startsWith("?")?t:"?"+t,Wb=t=>!t||t==="#"?"":t.startsWith("#")?t:"#"+t,zb=class{constructor(t,a,i,s=!1){this.status=t,this.statusText=a||"",this.internal=s,i instanceof Error?(this.data=i.toString(),this.error=i):this.data=i}};function Bb(t){return t!=null&&typeof t.status=="number"&&typeof t.statusText=="string"&&typeof t.internal=="boolean"&&"data"in t}function Hb(t){let a=t.map(i=>i.route.path).filter(Boolean);return qt(a)||"/"}var Hf=typeof window<"u"&&typeof window.document<"u"&&typeof window.document.createElement<"u";function _f(t,a){let i=t;if(typeof i!="string"||!Zc.test(i))return{absoluteURL:void 0,isExternal:!1,to:i};let s=i,r=!1;if(Hf)try{let c=new URL(window.location.href),h=Uf.test(i)?new URL(vb(i,c.protocol)):new URL(i),p=un(h.pathname,a);h.origin===c.origin&&p!=null?i=p+h.search+h.hash:r=!0}catch{Wt(!1,`<Link to="${i}"> contains an invalid URL which will probably break when clicked - please update to a valid URL path.`)}return{absoluteURL:s,isExternal:r,to:i}}Object.getOwnPropertyNames(Object.prototype).sort().join("\0");var Kf=["POST","PUT","PATCH","DELETE"];new Set(Kf);var _b=["GET",...Kf];new Set(_b);var Kb=["about:","blob:","chrome:","chrome-untrusted:","content:","data:","devtools:","file:","filesystem:","javascript:"];function Jb(t){try{return Kb.includes(new URL(t).protocol)}catch{return!1}}var Ha=x.createContext(null);Ha.displayName="DataRouter";var Xs=x.createContext(null);Xs.displayName="DataRouterState";var Jf=x.createContext(!1);function Gb(){return x.useContext(Jf)}var Gf=x.createContext({isTransitioning:!1});Gf.displayName="ViewTransition";var $b=x.createContext(new Map);$b.displayName="Fetchers";var Yb=x.createContext(null);Yb.displayName="Await";var kt=x.createContext(null);kt.displayName="Navigation";var oo=x.createContext(null);oo.displayName="Location";var zt=x.createContext({outlet:null,matches:[],isDataRoute:!1});zt.displayName="Route";var td=x.createContext(null);td.displayName="RouteError";var $f="REACT_ROUTER_ERROR",Qb="REDIRECT",Xb="ROUTE_ERROR_RESPONSE";function Zb(t){if(t.startsWith(`${$f}:${Qb}:{`))try{let a=JSON.parse(t.slice(28));if(typeof a=="object"&&a&&typeof a.status=="number"&&typeof a.statusText=="string"&&typeof a.location=="string"&&typeof a.reloadDocument=="boolean"&&typeof a.replace=="boolean")return a}catch{}}function ek(t){if(t.startsWith(`${$f}:${Xb}:{`))try{let a=JSON.parse(t.slice(40));if(typeof a=="object"&&a&&typeof a.status=="number"&&typeof a.statusText=="string")return new zb(a.status,a.statusText,a.data)}catch{}}function tk(t,{relative:a}={}){Ce(_a(),"useHref() may be used only in the context of a <Router> component.");let{basename:i,navigator:s}=x.useContext(kt),{hash:r,pathname:c,search:h}=so(t,{relative:a}),p=c;return i!=="/"&&(p=c==="/"?i:qt([i,c])),s.createHref({pathname:p,search:h,hash:r})}function _a(){return x.useContext(oo)!=null}function en(){return Ce(_a(),"useLocation() may be used only in the context of a <Router> component."),x.useContext(oo).location}var Yf="You should call navigate() in a React.useEffect(), not when your component is first rendered.";function Qf(t){x.useContext(kt).static||x.useLayoutEffect(t)}function Xf(){let{isDataRoute:t}=x.useContext(zt);return t?fk():nk()}function nk(){Ce(_a(),"useNavigate() may be used only in the context of a <Router> component.");let t=x.useContext(Ha),{basename:a,navigator:i}=x.useContext(kt),{matches:s}=x.useContext(zt),{pathname:r}=en(),c=JSON.stringify(ed(s)),h=x.useRef(!1);return Qf(()=>{h.current=!0}),x.useCallback((m,f={})=>{if(Wt(h.current,Yf),!h.current)return;if(typeof m=="number"){i.go(m);return}let y=Qs(m,JSON.parse(c),r,f.relative==="path");t==null&&a!=="/"&&(y.pathname=y.pathname==="/"?a:qt([a,y.pathname])),(f.replace?i.replace:i.push)(y,f.state,f)},[a,i,c,r,t])}x.createContext(null);function ak(){let{matches:t}=x.useContext(zt),a=t[t.length-1];return(a==null?void 0:a.params)??{}}function so(t,{relative:a}={}){let{matches:i}=x.useContext(zt),{pathname:s}=en(),r=JSON.stringify(ed(i));return x.useMemo(()=>Qs(t,JSON.parse(r),s,a==="path"),[t,r,s,a])}function ik(t,a){return Zf(t,a)}function Zf(t,a,i){var A;Ce(_a(),"useRoutes() may be used only in the context of a <Router> component.");let{navigator:s}=x.useContext(kt),{matches:r}=x.useContext(zt),c=r[r.length-1],h=c?c.params:{},p=c?c.pathname:"/",m=c?c.pathnameBase:"/",f=c&&c.route;{let R=f&&f.path||"";tg(p,!f||R.endsWith("*")||R.endsWith("*?"),`You rendered descendant <Routes> (or called \`useRoutes()\`) at "${p}" (under <Route path="${R}">) but the parent route path has no trailing "*". This means if you navigate deeper, the parent won't match anymore and therefore the child routes will never render.

Please change the parent <Route path="${R}"> to <Route path="${R==="/"?"*":`${R}/*`}">.`)}let y=en(),v;if(a){let R=typeof a=="string"?Ba(a):a;Ce(m==="/"||((A=R.pathname)==null?void 0:A.startsWith(m)),`When overriding the location using \`<Routes location>\` or \`useRoutes(routes, location)\`, the location pathname must begin with the portion of the URL pathname that was matched by all parent routes. The current pathname base is "${m}" but pathname "${R.pathname}" was given in the \`location\` prop.`),v=R}else v=y;let b=v.pathname||"/",T=b;if(m!=="/"){let R=m.replace(/^\//,"").split("/");T="/"+b.replace(/^\//,"").split("/").slice(R.length).join("/")}let M=i&&i.state.matches.length?i.state.matches.map(R=>Object.assign(R,{route:i.manifest[R.route.id]||R.route})):Ff(t,{pathname:T});Wt(f||M!=null,`No routes matched location "${v.pathname}${v.search}${v.hash}" `),Wt(M==null||M[M.length-1].route.element!==void 0||M[M.length-1].route.Component!==void 0||M[M.length-1].route.lazy!==void 0,`Matched leaf route at location "${v.pathname}${v.search}${v.hash}" does not have an element or Component. This means it will render an <Outlet /> with a null value by default resulting in an "empty" page.`);let I=ck(M&&M.map(R=>Object.assign({},R,{params:Object.assign({},h,R.params),pathname:qt([m,s.encodeLocation?s.encodeLocation(R.pathname.replace(/%/g,"%25").replace(/\?/g,"%3F").replace(/#/g,"%23")).pathname:R.pathname]),pathnameBase:R.pathnameBase==="/"?m:qt([m,s.encodeLocation?s.encodeLocation(R.pathnameBase.replace(/%/g,"%25").replace(/\?/g,"%3F").replace(/#/g,"%23")).pathname:R.pathnameBase])})),r,i);return a&&I?x.createElement(oo.Provider,{value:{location:{pathname:"/",search:"",hash:"",state:null,key:"default",mask:void 0,...v},navigationType:"POP"}},I):I}function ok(){let t=mk(),a=Bb(t)?`${t.status} ${t.statusText}`:t instanceof Error?t.message:JSON.stringify(t),i=t instanceof Error?t.stack:null,s="rgba(200,200,200, 0.5)",r={padding:"0.5rem",backgroundColor:s},c={padding:"2px 4px",backgroundColor:s},h=null;return console.error("Error handled by React Router default ErrorBoundary:",t),h=x.createElement(x.Fragment,null,x.createElement("p",null,"💿 Hey developer 👋"),x.createElement("p",null,"You can provide a way better UX than this when your app throws errors by providing your own ",x.createElement("code",{style:c},"ErrorBoundary")," or"," ",x.createElement("code",{style:c},"errorElement")," prop on your route.")),x.createElement(x.Fragment,null,x.createElement("h2",null,"Unexpected Application Error!"),x.createElement("h3",{style:{fontStyle:"italic"}},a),i?x.createElement("pre",{style:r},i):null,h)}var sk=x.createElement(ok,null),eg=class extends x.Component{constructor(t){super(t),this.state={location:t.location,revalidation:t.revalidation,error:t.error}}static getDerivedStateFromError(t){return{error:t}}static getDerivedStateFromProps(t,a){return a.location!==t.location||a.revalidation!=="idle"&&t.revalidation==="idle"?{error:t.error,location:t.location,revalidation:t.revalidation}:{error:t.error!==void 0?t.error:a.error,location:a.location,revalidation:t.revalidation||a.revalidation}}componentDidCatch(t,a){this.props.onError?this.props.onError(t,a):console.error("React Router caught the following error during render",t)}render(){let t=this.state.error;if(this.context&&typeof t=="object"&&t&&"digest"in t&&typeof t.digest=="string"){const i=ek(t.digest);i&&(t=i)}let a=t!==void 0?x.createElement(zt.Provider,{value:this.props.routeContext},x.createElement(td.Provider,{value:t,children:this.props.component})):this.props.children;return this.context?x.createElement(rk,{error:t},a):a}};eg.contextType=Jf;var dc=new WeakMap;function rk({children:t,error:a}){let{basename:i}=x.useContext(kt);if(typeof a=="object"&&a&&"digest"in a&&typeof a.digest=="string"){let s=Zb(a.digest);if(s){let r=dc.get(a);if(r)throw r;let c=_f(s.location,i),h=c.absoluteURL||c.to;if(Jb(h))throw new Error("Invalid redirect location");if(Hf&&!dc.get(a))if(c.isExternal||s.reloadDocument)window.location.href=h;else{const p=Promise.resolve().then(()=>window.__reactRouterDataRouter.navigate(c.to,{replace:s.replace}));throw dc.set(a,p),p}return x.createElement("meta",{httpEquiv:"refresh",content:`0;url=${h}`})}}return t}function lk({routeContext:t,match:a,children:i}){let s=x.useContext(Ha);return s&&s.static&&s.staticContext&&(a.route.errorElement||a.route.ErrorBoundary)&&(s.staticContext._deepestRenderedBoundaryId=a.route.id),x.createElement(zt.Provider,{value:t},i)}function ck(t,a=[],i){let s=i==null?void 0:i.state;if(t==null){if(!s)return null;if(s.errors)t=s.matches;else if(a.length===0&&!s.initialized&&s.matches.length>0)t=s.matches;else return null}let r=t,c=s==null?void 0:s.errors;if(c!=null){let y=r.findIndex(v=>v.route.id&&(c==null?void 0:c[v.route.id])!==void 0);Ce(y>=0,`Could not find a matching route for errors on route IDs: ${Object.keys(c).join(",")}`),r=r.slice(0,Math.min(r.length,y+1))}let h=!1,p=-1;if(i&&s){h=s.renderFallback;for(let y=0;y<r.length;y++){let v=r[y];if((v.route.HydrateFallback||v.route.hydrateFallbackElement)&&(p=y),v.route.id){let{loaderData:b,errors:T}=s,M=v.route.loader&&!b.hasOwnProperty(v.route.id)&&(!T||T[v.route.id]===void 0);if(v.route.lazy||M){i.isStatic&&(h=!0),p>=0?r=r.slice(0,p+1):r=[r[0]];break}}}}let m=i==null?void 0:i.onError,f=s&&m?(y,v)=>{var b,T;m(y,{location:s.location,params:((T=(b=s.matches)==null?void 0:b[0])==null?void 0:T.params)??{},pattern:Hb(s.matches),errorInfo:v})}:void 0;return r.reduceRight((y,v,b)=>{let T,M=!1,I=null,A=null;s&&(T=c&&v.route.id?c[v.route.id]:void 0,I=v.route.errorElement||sk,h&&(p<0&&b===0?(tg("route-fallback",!1,"No `HydrateFallback` element provided to render during initial hydration"),M=!0,A=null):p===b&&(M=!0,A=v.route.hydrateFallbackElement||null)));let R=a.concat(r.slice(0,b+1)),j=()=>{let V;return T?V=I:M?V=A:v.route.Component?V=x.createElement(v.route.Component,null):v.route.element?V=v.route.element:V=y,x.createElement(lk,{match:v,routeContext:{outlet:y,matches:R,isDataRoute:s!=null},children:V})};return s&&(v.route.ErrorBoundary||v.route.errorElement||b===0)?x.createElement(eg,{location:s.location,revalidation:s.revalidation,component:I,error:T,children:j(),routeContext:{outlet:null,matches:R,isDataRoute:!0},onError:f}):j()},null)}function nd(t){return`${t} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`}function dk(t){let a=x.useContext(Ha);return Ce(a,nd(t)),a}function hk(t){let a=x.useContext(Xs);return Ce(a,nd(t)),a}function uk(t){let a=x.useContext(zt);return Ce(a,nd(t)),a}function ad(t){let a=uk(t),i=a.matches[a.matches.length-1];return Ce(i.route.id,`${t} can only be used on routes that contain a unique "id"`),i.route.id}function pk(){return ad("useRouteId")}function mk(){var s;let t=x.useContext(td),a=hk("useRouteError"),i=ad("useRouteError");return t!==void 0?t:(s=a.errors)==null?void 0:s[i]}function fk(){let{router:t}=dk("useNavigate"),a=ad("useNavigate"),i=x.useRef(!1);return Qf(()=>{i.current=!0}),x.useCallback(async(r,c={})=>{Wt(i.current,Yf),i.current&&(typeof r=="number"?await t.navigate(r):await t.navigate(r,{fromRouteId:a,...c}))},[t,a])}var Gp={};function tg(t,a,i){!a&&!Gp[t]&&(Gp[t]=!0,Wt(!1,i))}x.memo(gk);function gk({routes:t,manifest:a,future:i,state:s,isStatic:r,onError:c}){return Zf(t,void 0,{manifest:a,state:s,isStatic:r,onError:c})}function ng({to:t,replace:a,state:i,relative:s}){Ce(_a(),"<Navigate> may be used only in the context of a <Router> component.");let{static:r}=x.useContext(kt);Wt(!r,"<Navigate> must not be used on the initial render in a <StaticRouter>. This is a no-op, but you should modify your code so the <Navigate> is only ever rendered in response to some user interaction or state change.");let{matches:c}=x.useContext(zt),{pathname:h}=en(),p=Xf(),m=Qs(t,ed(c),h,s==="path"),f=JSON.stringify(m);return x.useEffect(()=>{p(JSON.parse(f),{replace:a,state:i,relative:s})},[p,f,s,a,i]),null}function zi(t){Ce(!1,"A <Route> is only ever to be used as the child of <Routes> element, never rendered directly. Please wrap your <Route> in a <Routes>.")}function yk({basename:t="/",children:a=null,location:i,navigationType:s="POP",navigator:r,static:c=!1,useTransitions:h}){Ce(!_a(),"You cannot render a <Router> inside another <Router>. You should never have more than one in your app.");let p=t.replace(/^\/*/,"/"),m=x.useMemo(()=>({basename:p,navigator:r,static:c,useTransitions:h,future:{}}),[p,r,c,h]);typeof i=="string"&&(i=Ba(i));let{pathname:f="/",search:y="",hash:v="",state:b=null,key:T="default",mask:M}=i,I=x.useMemo(()=>{let A=un(f,p);return A==null?null:{location:{pathname:A,search:y,hash:v,state:b,key:T,mask:M},navigationType:s}},[p,f,y,v,b,T,s,M]);return Wt(I!=null,`<Router basename="${p}"> is not able to match the URL "${f}${y}${v}" because it does not start with the basename, so the <Router> won't render anything.`),I==null?null:x.createElement(kt.Provider,{value:m},x.createElement(oo.Provider,{children:a,value:I}))}function wk({children:t,location:a}){return ik(Lc(t),a)}function Lc(t,a=[]){let i=[];return x.Children.forEach(t,(s,r)=>{if(!x.isValidElement(s))return;let c=[...a,r];if(s.type===x.Fragment){i.push.apply(i,Lc(s.props.children,c));return}Ce(s.type===zi,`[${typeof s.type=="string"?s.type:s.type.name}] is not a <Route> component. All component children of <Routes> must be a <Route> or <React.Fragment>`),Ce(!s.props.index||!s.props.children,"An index route cannot have child routes.");let h={id:s.props.id||c.join("-"),caseSensitive:s.props.caseSensitive,element:s.props.element,Component:s.props.Component,index:s.props.index,path:s.props.path,middleware:s.props.middleware,loader:s.props.loader,action:s.props.action,hydrateFallbackElement:s.props.hydrateFallbackElement,HydrateFallback:s.props.HydrateFallback,errorElement:s.props.errorElement,ErrorBoundary:s.props.ErrorBoundary,hasErrorBoundary:s.props.hasErrorBoundary===!0||s.props.ErrorBoundary!=null||s.props.errorElement!=null,shouldRevalidate:s.props.shouldRevalidate,handle:s.props.handle,lazy:s.props.lazy};s.props.children&&(h.children=Lc(s.props.children,c)),i.push(h)}),i}var Ls="get",Ds="application/x-www-form-urlencoded";function Zs(t){return typeof HTMLElement<"u"&&t instanceof HTMLElement}function vk(t){return Zs(t)&&t.tagName.toLowerCase()==="button"}function bk(t){return Zs(t)&&t.tagName.toLowerCase()==="form"}function kk(t){return Zs(t)&&t.tagName.toLowerCase()==="input"}function xk(t){return!!(t.metaKey||t.altKey||t.ctrlKey||t.shiftKey)}function Sk(t,a){return t.button===0&&(!a||a==="_self")&&!xk(t)}var Ts=null;function Tk(){if(Ts===null)try{new FormData(document.createElement("form"),0),Ts=!1}catch{Ts=!0}return Ts}var Ck=new Set(["application/x-www-form-urlencoded","multipart/form-data","text/plain"]);function hc(t){return t!=null&&!Ck.has(t)?(Wt(!1,`"${t}" is not a valid \`encType\` for \`<Form>\`/\`<fetcher.Form>\` and will default to "${Ds}"`),null):t}function Mk(t,a){let i,s,r,c,h;if(bk(t)){let p=t.getAttribute("action");s=p?un(p,a):null,i=t.getAttribute("method")||Ls,r=hc(t.getAttribute("enctype"))||Ds,c=new FormData(t)}else if(vk(t)||kk(t)&&(t.type==="submit"||t.type==="image")){let p=t.form;if(p==null)throw new Error('Cannot submit a <button> or <input type="submit"> without a <form>');let m=t.getAttribute("formaction")||p.getAttribute("action");if(s=m?un(m,a):null,i=t.getAttribute("formmethod")||p.getAttribute("method")||Ls,r=hc(t.getAttribute("formenctype"))||hc(p.getAttribute("enctype"))||Ds,c=new FormData(p,t),!Tk()){let{name:f,type:y,value:v}=t;if(y==="image"){let b=f?`${f}.`:"";c.append(`${b}x`,"0"),c.append(`${b}y`,"0")}else f&&c.append(f,v)}}else{if(Zs(t))throw new Error('Cannot submit element that is not <form>, <button>, or <input type="submit|image">');i=Ls,s=null,r=Ds,h=t}return c&&r==="text/plain"&&(h=c,c=void 0),{action:s,method:i.toLowerCase(),encType:r,formData:c,body:h}}Object.getOwnPropertyNames(Object.prototype).sort().join("\0");function id(t,a){if(t===!1||t===null||typeof t>"u")throw new Error(a)}function ag(t,a,i,s){let r=typeof t=="string"?new URL(t,typeof window>"u"?"server://singlefetch/":window.location.origin):t;return i?r.pathname.endsWith("/")?r.pathname=`${r.pathname}_.${s}`:r.pathname=`${r.pathname}.${s}`:r.pathname==="/"?r.pathname=`_root.${s}`:a&&un(r.pathname,a)==="/"?r.pathname=`${qs(a)}/_root.${s}`:r.pathname=`${qs(r.pathname)}.${s}`,r}async function Ik(t,a){if(t.id in a)return a[t.id];try{let i=await import(t.module);return a[t.id]=i,i}catch(i){return console.error(`Error loading route module \`${t.module}\`, reloading page...`),console.error(i),window.__reactRouterContext&&window.__reactRouterContext.isSpaMode,window.location.reload(),new Promise(()=>{})}}function Ak(t){return t==null?!1:t.href==null?t.rel==="preload"&&typeof t.imageSrcSet=="string"&&typeof t.imageSizes=="string":typeof t.rel=="string"&&typeof t.href=="string"}async function Ek(t,a,i){let s=await Promise.all(t.map(async r=>{let c=a.routes[r.route.id];if(c){let h=await Ik(c,i);return h.links?h.links():[]}return[]}));return Dk(s.flat(1).filter(Ak).filter(r=>r.rel==="stylesheet"||r.rel==="preload").map(r=>r.rel==="stylesheet"?{...r,rel:"prefetch",as:"style"}:{...r,rel:"prefetch"}))}function $p(t,a,i,s,r,c){let h=(m,f)=>i[f]?m.route.id!==i[f].route.id:!0,p=(m,f)=>{var y;return i[f].pathname!==m.pathname||((y=i[f].route.path)==null?void 0:y.endsWith("*"))&&i[f].params["*"]!==m.params["*"]};return c==="assets"?a.filter((m,f)=>h(m,f)||p(m,f)):c==="data"?a.filter((m,f)=>{var v;let y=s.routes[m.route.id];if(!y||!y.hasLoader)return!1;if(h(m,f)||p(m,f))return!0;if(m.route.shouldRevalidate){let b=m.route.shouldRevalidate({currentUrl:new URL(r.pathname+r.search+r.hash,window.origin),currentParams:((v=i[0])==null?void 0:v.params)||{},nextUrl:new URL(t,window.origin),nextParams:m.params,defaultShouldRevalidate:!0});if(typeof b=="boolean")return b}return!0}):[]}function Pk(t,a,{includeHydrateFallback:i}={}){return Rk(t.map(s=>{let r=a.routes[s.route.id];if(!r)return[];let c=[r.module];return r.clientActionModule&&(c=c.concat(r.clientActionModule)),r.clientLoaderModule&&(c=c.concat(r.clientLoaderModule)),i&&r.hydrateFallbackModule&&(c=c.concat(r.hydrateFallbackModule)),r.imports&&(c=c.concat(r.imports)),c}).flat(1))}function Rk(t){return[...new Set(t)]}function Lk(t){let a={},i=Object.keys(t).sort();for(let s of i)a[s]=t[s];return a}function Dk(t,a){let i=new Set;return new Set(a),t.reduce((s,r)=>{let c=JSON.stringify(Lk(r));return i.has(c)||(i.add(c),s.push({key:c,link:r})),s},[])}function od(){let t=x.useContext(Ha);return id(t,"You must render this element inside a <DataRouterContext.Provider> element"),t}function Vk(){let t=x.useContext(Xs);return id(t,"You must render this element inside a <DataRouterStateContext.Provider> element"),t}var sd=x.createContext(void 0);sd.displayName="FrameworkContext";function er(){let t=x.useContext(sd);return id(t,"You must render this element inside a <HydratedRouter> element"),t}function jk(t,a){let i=x.useContext(sd),[s,r]=x.useState(!1),[c,h]=x.useState(!1),{onFocus:p,onBlur:m,onMouseEnter:f,onMouseLeave:y,onTouchStart:v}=a,b=x.useRef(null);x.useEffect(()=>{if(t==="render"&&h(!0),t==="viewport"){let I=R=>{R.forEach(j=>{h(j.isIntersecting)})},A=new IntersectionObserver(I,{threshold:.5});return b.current&&A.observe(b.current),()=>{A.disconnect()}}},[t]),x.useEffect(()=>{if(s){let I=setTimeout(()=>{h(!0)},100);return()=>{clearTimeout(I)}}},[s]);let T=()=>{r(!0)},M=()=>{r(!1),h(!1)};return i?t!=="intent"?[c,b,{}]:[c,b,{onFocus:Ui(p,T),onBlur:Ui(m,M),onMouseEnter:Ui(f,T),onMouseLeave:Ui(y,M),onTouchStart:Ui(v,T)}]:[!1,b,{}]}function Ui(t,a){return i=>{t&&t(i),i.defaultPrevented||a(i)}}function Ok({page:t,...a}){let i=Gb(),{nonce:s}=er(),{router:r}=od(),c=x.useMemo(()=>Ff(r.routes,t,r.basename),[r.routes,t,r.basename]);return c?(a.nonce==null&&s&&(a={...a,nonce:s}),i?x.createElement(Fk,{page:t,matches:c,...a}):x.createElement(Nk,{page:t,matches:c,...a})):null}function Uk(t){let{manifest:a,routeModules:i}=er(),[s,r]=x.useState([]);return x.useEffect(()=>{let c=!1;return Ek(t,a,i).then(h=>{c||r(h)}),()=>{c=!0}},[t,a,i]),s}function Fk({page:t,matches:a,...i}){let s=en(),{future:r}=er(),{basename:c}=od(),h=x.useMemo(()=>{if(t===s.pathname+s.search+s.hash)return[];let p=ag(t,c,r.v8_trailingSlashAwareDataRequests,"rsc"),m=!1,f=[];for(let y of a)typeof y.route.shouldRevalidate=="function"?m=!0:f.push(y.route.id);return m&&f.length>0&&p.searchParams.set("_routes",f.join(",")),[p.pathname+p.search]},[c,r.v8_trailingSlashAwareDataRequests,t,s,a]);return x.createElement(x.Fragment,null,h.map(p=>x.createElement("link",{key:p,rel:"prefetch",as:"fetch",href:p,...i})))}function Nk({page:t,matches:a,...i}){let s=en(),{future:r,manifest:c,routeModules:h}=er(),{basename:p}=od(),{loaderData:m,matches:f}=Vk(),y=x.useMemo(()=>$p(t,a,f,c,s,"data"),[t,a,f,c,s]),v=x.useMemo(()=>$p(t,a,f,c,s,"assets"),[t,a,f,c,s]),b=x.useMemo(()=>{if(t===s.pathname+s.search+s.hash)return[];let I=new Set,A=!1;if(a.forEach(j=>{var B;let V=c.routes[j.route.id];!V||!V.hasLoader||(!y.some(N=>N.route.id===j.route.id)&&j.route.id in m&&((B=h[j.route.id])!=null&&B.shouldRevalidate)||V.hasClientLoader?A=!0:I.add(j.route.id))}),I.size===0)return[];let R=ag(t,p,r.v8_trailingSlashAwareDataRequests,"data");return A&&I.size>0&&R.searchParams.set("_routes",a.filter(j=>I.has(j.route.id)).map(j=>j.route.id).join(",")),[R.pathname+R.search]},[p,r.v8_trailingSlashAwareDataRequests,m,s,c,y,a,t,h]),T=x.useMemo(()=>Pk(v,c),[v,c]),M=Uk(v);return x.createElement(x.Fragment,null,b.map(I=>x.createElement("link",{key:I,rel:"prefetch",as:"fetch",href:I,...i})),T.map(I=>x.createElement("link",{key:I,rel:"modulepreload",href:I,...i})),M.map(({key:I,link:A})=>x.createElement("link",{key:I,nonce:i.nonce,...A,crossOrigin:A.crossOrigin??i.crossOrigin})))}function qk(...t){return a=>{t.forEach(i=>{typeof i=="function"?i(a):i!=null&&(i.current=a)})}}var Wk=typeof window<"u"&&typeof window.document<"u"&&typeof window.document.createElement<"u";try{Wk&&(window.__reactRouterVersion="7.18.1")}catch{}function zk({basename:t,children:a,useTransitions:i,window:s}){let r=x.useRef();r.current==null&&(r.current=bb({window:s,v5Compat:!0}));let c=r.current,[h,p]=x.useState({action:c.action,location:c.location}),m=x.useCallback(f=>{i===!1?p(f):x.startTransition(()=>p(f))},[i]);return x.useLayoutEffect(()=>c.listen(m),[c,m]),x.createElement(yk,{basename:t,children:a,location:h.location,navigationType:h.action,navigator:c,useTransitions:i})}var Xi=x.forwardRef(function({onClick:a,discover:i="render",prefetch:s="none",relative:r,reloadDocument:c,replace:h,mask:p,state:m,target:f,to:y,preventScrollReset:v,viewTransition:b,defaultShouldRevalidate:T,...M},I){let{basename:A,navigator:R,useTransitions:j}=x.useContext(kt),V=typeof y=="string"&&Zc.test(y),B=_f(y,A);y=B.to;let N=tk(y,{relative:r}),_=en(),ae=null;if(p){let Le=Qs(p,[],_.mask?_.mask.pathname:"/",!0);A!=="/"&&(Le.pathname=Le.pathname==="/"?A:qt([A,Le.pathname])),ae=R.createHref(Le)}let[G,ce,we]=jk(s,M),Me=Kk(y,{replace:h,mask:p,state:m,target:f,preventScrollReset:v,relative:r,viewTransition:b,defaultShouldRevalidate:T,useTransitions:j});function _e(Le){a&&a(Le),Le.defaultPrevented||Me(Le)}let Fe=!(B.isExternal||c),Ne=x.createElement("a",{...M,...we,href:(Fe?ae:void 0)||B.absoluteURL||N,onClick:Fe?_e:a,ref:qk(I,ce),target:f,"data-discover":!V&&i==="render"?"true":void 0});return G&&!V?x.createElement(x.Fragment,null,Ne,x.createElement(Ok,{page:N})):Ne});Xi.displayName="Link";var Bk=x.forwardRef(function({"aria-current":a="page",caseSensitive:i=!1,className:s="",end:r=!1,style:c,to:h,viewTransition:p,children:m,...f},y){let v=so(h,{relative:f.relative}),b=en(),T=x.useContext(Xs),{navigator:M,basename:I}=x.useContext(kt),A=T!=null&&Qk(v)&&p===!0,R=M.encodeLocation?M.encodeLocation(v).pathname:v.pathname,j=b.pathname,V=T&&T.navigation&&T.navigation.location?T.navigation.location.pathname:null;i||(j=j.toLowerCase(),V=V?V.toLowerCase():null,R=R.toLowerCase()),V&&I&&(V=un(V,I)||V);const B=R!=="/"&&R.endsWith("/")?R.length-1:R.length;let N=j===R||!r&&j.startsWith(R)&&j.charAt(B)==="/",_=V!=null&&(V===R||!r&&V.startsWith(R)&&V.charAt(R.length)==="/"),ae={isActive:N,isPending:_,isTransitioning:A},G=N?a:void 0,ce;typeof s=="function"?ce=s(ae):ce=[s,N?"active":null,_?"pending":null,A?"transitioning":null].filter(Boolean).join(" ");let we=typeof c=="function"?c(ae):c;return x.createElement(Xi,{...f,"aria-current":G,className:ce,ref:y,style:we,to:h,viewTransition:p},typeof m=="function"?m(ae):m)});Bk.displayName="NavLink";var Hk=x.forwardRef(({discover:t="render",fetcherKey:a,navigate:i,reloadDocument:s,replace:r,state:c,method:h=Ls,action:p,onSubmit:m,relative:f,preventScrollReset:y,viewTransition:v,defaultShouldRevalidate:b,...T},M)=>{let{useTransitions:I}=x.useContext(kt),A=$k(),R=Yk(p,{relative:f}),j=h.toLowerCase()==="get"?"get":"post",V=typeof p=="string"&&Zc.test(p),B=N=>{if(m&&m(N),N.defaultPrevented)return;N.preventDefault();let _=N.nativeEvent.submitter,ae=(_==null?void 0:_.getAttribute("formmethod"))||h,G=()=>A(_||N.currentTarget,{fetcherKey:a,method:ae,navigate:i,replace:r,state:c,relative:f,preventScrollReset:y,viewTransition:v,defaultShouldRevalidate:b});I&&i!==!1?x.startTransition(()=>G()):G()};return x.createElement("form",{ref:M,method:j,action:R,onSubmit:s?m:B,...T,"data-discover":!V&&t==="render"?"true":void 0})});Hk.displayName="Form";function _k(t){return`${t} must be used within a data router.  See https://reactrouter.com/en/main/routers/picking-a-router.`}function ig(t){let a=x.useContext(Ha);return Ce(a,_k(t)),a}function Kk(t,{target:a,replace:i,mask:s,state:r,preventScrollReset:c,relative:h,viewTransition:p,defaultShouldRevalidate:m,useTransitions:f}={}){let y=Xf(),v=en(),b=so(t,{relative:h});return x.useCallback(T=>{if(Sk(T,a)){T.preventDefault();let M=i!==void 0?i:Qi(v)===Qi(b),I=()=>y(t,{replace:M,mask:s,state:r,preventScrollReset:c,relative:h,viewTransition:p,defaultShouldRevalidate:m});f?x.startTransition(()=>I()):I()}},[v,y,b,i,s,r,a,t,c,h,p,m,f])}var Jk=0,Gk=()=>`__${String(++Jk)}__`;function $k(){let{router:t}=ig("useSubmit"),{basename:a}=x.useContext(kt),i=pk(),s=t.fetch,r=t.navigate;return x.useCallback(async(c,h={})=>{let{action:p,method:m,encType:f,formData:y,body:v}=Mk(c,a);if(h.navigate===!1){let b=h.fetcherKey||Gk();await s(b,i,h.action||p,{defaultShouldRevalidate:h.defaultShouldRevalidate,preventScrollReset:h.preventScrollReset,formData:y,body:v,formMethod:h.method||m,formEncType:h.encType||f,flushSync:h.flushSync})}else await r(h.action||p,{defaultShouldRevalidate:h.defaultShouldRevalidate,preventScrollReset:h.preventScrollReset,formData:y,body:v,formMethod:h.method||m,formEncType:h.encType||f,replace:h.replace,state:h.state,fromRouteId:i,flushSync:h.flushSync,viewTransition:h.viewTransition})},[s,r,a,i])}function Yk(t,{relative:a}={}){let{basename:i}=x.useContext(kt),s=x.useContext(zt);Ce(s,"useFormAction must be used inside a RouteContext");let[r]=s.matches.slice(-1),c={...so(t||".",{relative:a})},h=en();if(t==null){c.search=h.search;let p=new URLSearchParams(c.search),m=p.getAll("index");if(m.some(y=>y==="")){p.delete("index"),m.filter(v=>v).forEach(v=>p.append("index",v));let y=p.toString();c.search=y?`?${y}`:""}}return(!t||t===".")&&r.route.index&&(c.search=c.search?c.search.replace(/^\?/,"?index&"):"?index"),i!=="/"&&(c.pathname=c.pathname==="/"?i:qt([i,c.pathname])),Qi(c)}function Qk(t,{relative:a}={}){let i=x.useContext(Gf);Ce(i!=null,"`useViewTransitionState` must be used within `react-router-dom`'s `RouterProvider`.  Did you accidentally import `RouterProvider` from `react-router`?");let{basename:s}=ig("useViewTransitionState"),r=so(t,{relative:a});if(!i.isTransitioning)return!1;let c=un(i.currentLocation.pathname,s)||i.currentLocation.pathname,h=un(i.nextLocation.pathname,s)||i.nextLocation.pathname;return Ns(r.pathname,h)!=null||Ns(r.pathname,c)!=null}var Yp="1.3.25";function og(t,a,i){return Math.max(t,Math.min(a,i))}function Xk(t,a,i){return(1-i)*t+i*a}function Zk(t,a,i,s){return Xk(t,a,1-Math.exp(-i*s))}function ex(t,a){return(t%a+a)%a}var tx=class{constructor(){X(this,"isRunning",!1);X(this,"value",0);X(this,"from",0);X(this,"to",0);X(this,"currentTime",0);X(this,"lerp");X(this,"duration");X(this,"easing");X(this,"onUpdate")}advance(t){var i;if(!this.isRunning)return;let a=!1;if(this.duration&&this.easing){this.currentTime+=t;const s=og(0,this.currentTime/this.duration,1);a=s>=1;const r=a?1:this.easing(s);this.value=this.from+(this.to-this.from)*r}else this.lerp?(this.value=Zk(this.value,this.to,this.lerp*60,t),Math.round(this.value)===Math.round(this.to)&&(this.value=this.to,a=!0)):(this.value=this.to,a=!0);a&&this.stop(),(i=this.onUpdate)==null||i.call(this,this.value,a)}stop(){this.isRunning=!1}fromTo(t,a,{lerp:i,duration:s,easing:r,onStart:c,onUpdate:h}){this.from=this.value=t,this.to=a,this.lerp=i,this.duration=s,this.easing=r,this.currentTime=0,this.isRunning=!0,c==null||c(),this.onUpdate=h}};function nx(t,a){let i;return function(...s){clearTimeout(i),i=setTimeout(()=>{i=void 0,t.apply(this,s)},a)}}var ax=class{constructor(t,a,{autoResize:i=!0,debounce:s=250}={}){X(this,"width",0);X(this,"height",0);X(this,"scrollHeight",0);X(this,"scrollWidth",0);X(this,"debouncedResize");X(this,"wrapperResizeObserver");X(this,"contentResizeObserver");X(this,"resize",()=>{this.onWrapperResize(),this.onContentResize()});X(this,"onWrapperResize",()=>{this.wrapper instanceof Window?(this.width=window.innerWidth,this.height=window.innerHeight):(this.width=this.wrapper.clientWidth,this.height=this.wrapper.clientHeight)});X(this,"onContentResize",()=>{this.wrapper instanceof Window?(this.scrollHeight=this.content.scrollHeight,this.scrollWidth=this.content.scrollWidth):(this.scrollHeight=this.wrapper.scrollHeight,this.scrollWidth=this.wrapper.scrollWidth)});this.wrapper=t,this.content=a,i&&(this.debouncedResize=nx(this.resize,s),this.wrapper instanceof Window?window.addEventListener("resize",this.debouncedResize):(this.wrapperResizeObserver=new ResizeObserver(this.debouncedResize),this.wrapperResizeObserver.observe(this.wrapper)),this.contentResizeObserver=new ResizeObserver(this.debouncedResize),this.contentResizeObserver.observe(this.content)),this.resize()}destroy(){var t,a;(t=this.wrapperResizeObserver)==null||t.disconnect(),(a=this.contentResizeObserver)==null||a.disconnect(),this.wrapper===window&&this.debouncedResize&&window.removeEventListener("resize",this.debouncedResize)}get limit(){return{x:this.scrollWidth-this.width,y:this.scrollHeight-this.height}}},sg=class{constructor(){X(this,"events",{})}emit(t,...a){var s;const i=this.events[t]||[];for(let r=0,c=i.length;r<c;r++)(s=i[r])==null||s.call(i,...a)}on(t,a){return this.events[t]?this.events[t].push(a):this.events[t]=[a],()=>{var i;this.events[t]=(i=this.events[t])==null?void 0:i.filter(s=>a!==s)}}off(t,a){var i;this.events[t]=(i=this.events[t])==null?void 0:i.filter(s=>a!==s)}destroy(){this.events={}}};const ix=100/6,Ln={passive:!1};function Qp(t,a){return t===1?ix:t===2?a:1}var ox=class{constructor(t,a={wheelMultiplier:1,touchMultiplier:1}){X(this,"touchStart",{x:0,y:0});X(this,"lastDelta",{x:0,y:0});X(this,"window",{width:0,height:0});X(this,"emitter",new sg);X(this,"onTouchStart",t=>{const{clientX:a,clientY:i}=t.targetTouches?t.targetTouches[0]:t;this.touchStart.x=a,this.touchStart.y=i,this.lastDelta={x:0,y:0},this.emitter.emit("scroll",{deltaX:0,deltaY:0,event:t})});X(this,"onTouchMove",t=>{const{clientX:a,clientY:i}=t.targetTouches?t.targetTouches[0]:t,s=-(a-this.touchStart.x)*this.options.touchMultiplier,r=-(i-this.touchStart.y)*this.options.touchMultiplier;this.touchStart.x=a,this.touchStart.y=i,this.lastDelta={x:s,y:r},this.emitter.emit("scroll",{deltaX:s,deltaY:r,event:t})});X(this,"onTouchEnd",t=>{this.emitter.emit("scroll",{deltaX:this.lastDelta.x,deltaY:this.lastDelta.y,event:t})});X(this,"onWheel",t=>{let{deltaX:a,deltaY:i,deltaMode:s}=t;const r=Qp(s,this.window.width),c=Qp(s,this.window.height);a*=r,i*=c,a*=this.options.wheelMultiplier,i*=this.options.wheelMultiplier,this.emitter.emit("scroll",{deltaX:a,deltaY:i,event:t})});X(this,"onWindowResize",()=>{this.window={width:window.innerWidth,height:window.innerHeight}});this.element=t,this.options=a,window.addEventListener("resize",this.onWindowResize),this.onWindowResize(),this.element.addEventListener("wheel",this.onWheel,Ln),this.element.addEventListener("touchstart",this.onTouchStart,Ln),this.element.addEventListener("touchmove",this.onTouchMove,Ln),this.element.addEventListener("touchend",this.onTouchEnd,Ln)}on(t,a){return this.emitter.on(t,a)}destroy(){this.emitter.destroy(),window.removeEventListener("resize",this.onWindowResize),this.element.removeEventListener("wheel",this.onWheel,Ln),this.element.removeEventListener("touchstart",this.onTouchStart,Ln),this.element.removeEventListener("touchmove",this.onTouchMove,Ln),this.element.removeEventListener("touchend",this.onTouchEnd,Ln)}};const Xp=t=>Math.min(1,1.001-2**(-10*t));var sx=class{constructor({wrapper:t=window,content:a=document.documentElement,eventsTarget:i=t,smoothWheel:s=!0,syncTouch:r=!1,syncTouchLerp:c=.075,touchInertiaExponent:h=1.7,duration:p,easing:m,lerp:f=.1,infinite:y=!1,orientation:v="vertical",gestureOrientation:b=v==="horizontal"?"both":"vertical",touchMultiplier:T=1,wheelMultiplier:M=1,autoResize:I=!0,prevent:A,virtualScroll:R,overscroll:j=!0,autoRaf:V=!1,anchors:B=!1,autoToggle:N=!1,allowNestedScroll:_=!1,__experimental__naiveDimensions:ae=!1,naiveDimensions:G=ae,stopInertiaOnNavigate:ce=!1}={}){X(this,"_isScrolling",!1);X(this,"_isStopped",!1);X(this,"_isLocked",!1);X(this,"_preventNextNativeScrollEvent",!1);X(this,"_resetVelocityTimeout",null);X(this,"_rafId",null);X(this,"_isDraggingSelection",!1);X(this,"isTouching");X(this,"isIos");X(this,"time",0);X(this,"userData",{});X(this,"lastVelocity",0);X(this,"velocity",0);X(this,"direction",0);X(this,"options");X(this,"targetScroll");X(this,"animatedScroll");X(this,"animate",new tx);X(this,"emitter",new sg);X(this,"dimensions");X(this,"virtualScroll");X(this,"onScrollEnd",t=>{t instanceof CustomEvent||(this.isScrolling==="smooth"||this.isScrolling===!1)&&t.stopPropagation()});X(this,"dispatchScrollendEvent",()=>{this.options.wrapper.dispatchEvent(new CustomEvent("scrollend",{bubbles:this.options.wrapper===window,detail:{lenisScrollEnd:!0}}))});X(this,"onTransitionEnd",t=>{var a;(a=t.propertyName)!=null&&a.includes("overflow")&&t.target===this.rootElement&&this.checkOverflow()});X(this,"onClick",t=>{const a=t.composedPath().filter(s=>s instanceof HTMLAnchorElement&&s.href).map(s=>new URL(s.href)),i=new URL(window.location.href);if(this.options.anchors){const s=a.find(r=>i.host===r.host&&i.pathname===r.pathname&&r.hash);if(s){const r=typeof this.options.anchors=="object"&&this.options.anchors?this.options.anchors:void 0,c=decodeURIComponent(s.hash);this.scrollTo(c,r);return}}if(this.options.stopInertiaOnNavigate&&a.some(s=>i.host===s.host&&i.pathname!==s.pathname)){this.reset();return}});X(this,"onPointerDown",t=>{t.button===1&&this.reset()});X(this,"onVirtualScroll",t=>{if(typeof this.options.virtualScroll=="function"&&this.options.virtualScroll(t)===!1)return;const{deltaX:a,deltaY:i,event:s}=t;if(this.emitter.emit("virtual-scroll",{deltaX:a,deltaY:i,event:s}),s.ctrlKey||s.lenisStopPropagation)return;const r=s.type.includes("touch"),c=s.type.includes("wheel");if(r&&this.isIos&&(s.type==="touchstart"&&(this._isDraggingSelection=this.isTouchOnSelectionHandle(s)),this._isDraggingSelection)){s.type==="touchend"&&(this._isDraggingSelection=!1);return}this.isTouching=s.type==="touchstart"||s.type==="touchmove";const h=a===0&&i===0;if(this.options.syncTouch&&r&&s.type==="touchstart"&&h&&!this.isStopped&&!this.isLocked){this.reset();return}const p=this.options.gestureOrientation==="vertical"&&i===0||this.options.gestureOrientation==="horizontal"&&a===0;if(h||p)return;let m=s.composedPath();m=m.slice(0,m.indexOf(this.rootElement));const f=this.options.prevent,y=Math.abs(a)>=Math.abs(i)?"horizontal":"vertical";if(m.find(M=>{var I,A,R,j,V;return M instanceof HTMLElement&&(typeof f=="function"&&(f==null?void 0:f(M))||((I=M.hasAttribute)==null?void 0:I.call(M,"data-lenis-prevent"))||y==="vertical"&&((A=M.hasAttribute)==null?void 0:A.call(M,"data-lenis-prevent-vertical"))||y==="horizontal"&&((R=M.hasAttribute)==null?void 0:R.call(M,"data-lenis-prevent-horizontal"))||r&&((j=M.hasAttribute)==null?void 0:j.call(M,"data-lenis-prevent-touch"))||c&&((V=M.hasAttribute)==null?void 0:V.call(M,"data-lenis-prevent-wheel"))||this.options.allowNestedScroll&&this.hasNestedScroll(M,{deltaX:a,deltaY:i}))}))return;if(this.isStopped||this.isLocked){s.cancelable&&s.preventDefault();return}if(!(this.options.syncTouch&&r||this.options.smoothWheel&&c)){this.isScrolling="native",this.animate.stop(),s.lenisStopPropagation=!0;return}let v=i;this.options.gestureOrientation==="both"?v=Math.abs(i)>Math.abs(a)?i:a:this.options.gestureOrientation==="horizontal"&&(v=a),(!this.options.overscroll||this.options.infinite||this.options.wrapper!==window&&this.limit>0&&(this.animatedScroll>0&&this.animatedScroll<this.limit||this.animatedScroll===0&&i>0||this.animatedScroll===this.limit&&i<0))&&(s.lenisStopPropagation=!0),s.cancelable&&s.preventDefault();const b=r&&this.options.syncTouch,T=r&&s.type==="touchend";T&&(v=Math.sign(v)*Math.abs(this.velocity)**this.options.touchInertiaExponent),this.scrollTo(this.targetScroll+v,{programmatic:!1,...b?{lerp:T?this.options.syncTouchLerp:1}:{lerp:this.options.lerp,duration:this.options.duration,easing:this.options.easing}})});X(this,"onNativeScroll",()=>{if(this._resetVelocityTimeout!==null&&(clearTimeout(this._resetVelocityTimeout),this._resetVelocityTimeout=null),this._preventNextNativeScrollEvent){this._preventNextNativeScrollEvent=!1;return}if(this.isScrolling===!1||this.isScrolling==="native"){const t=this.animatedScroll;this.animatedScroll=this.targetScroll=this.actualScroll,this.lastVelocity=this.velocity,this.velocity=this.animatedScroll-t,this.direction=Math.sign(this.animatedScroll-t),this.isStopped||(this.isScrolling="native"),this.emit(),this.velocity!==0&&(this._resetVelocityTimeout=setTimeout(()=>{this.lastVelocity=this.velocity,this.velocity=0,this.isScrolling=!1,this.emit()},400))}});X(this,"raf",t=>{const a=t-(this.time||t);this.time=t,this.animate.advance(a*.001),this.options.autoRaf&&(this._rafId=requestAnimationFrame(this.raf))});window.lenisVersion=Yp,window.lenis||(window.lenis={}),window.lenis.version=Yp,v==="horizontal"&&(window.lenis.horizontal=!0),r===!0&&(window.lenis.touch=!0),this.isIos=/(iPad|iPhone|iPod)/g.test(navigator.userAgent),(!t||t===document.documentElement)&&(t=window),typeof p=="number"&&typeof m!="function"?m=Xp:typeof m=="function"&&typeof p!="number"&&(p=1),this.options={wrapper:t,content:a,eventsTarget:i,smoothWheel:s,syncTouch:r,syncTouchLerp:c,touchInertiaExponent:h,duration:p,easing:m,lerp:f,infinite:y,gestureOrientation:b,orientation:v,touchMultiplier:T,wheelMultiplier:M,autoResize:I,prevent:A,virtualScroll:R,overscroll:j,autoRaf:V,anchors:B,autoToggle:N,allowNestedScroll:_,naiveDimensions:G,stopInertiaOnNavigate:ce},this.dimensions=new ax(t,a,{autoResize:I}),this.updateClassName(),this.targetScroll=this.animatedScroll=this.actualScroll,this.options.wrapper.addEventListener("scroll",this.onNativeScroll),this.options.wrapper.addEventListener("scrollend",this.onScrollEnd,{capture:!0}),(this.options.anchors||this.options.stopInertiaOnNavigate)&&this.options.wrapper.addEventListener("click",this.onClick),this.options.wrapper.addEventListener("pointerdown",this.onPointerDown),this.virtualScroll=new ox(i,{touchMultiplier:T,wheelMultiplier:M}),this.virtualScroll.on("scroll",this.onVirtualScroll),this.options.autoToggle&&(this.checkOverflow(),this.rootElement.addEventListener("transitionend",this.onTransitionEnd)),this.options.autoRaf&&(this._rafId=requestAnimationFrame(this.raf))}destroy(){this.emitter.destroy(),this.options.wrapper.removeEventListener("scroll",this.onNativeScroll),this.options.wrapper.removeEventListener("scrollend",this.onScrollEnd,{capture:!0}),this.options.wrapper.removeEventListener("pointerdown",this.onPointerDown),(this.options.anchors||this.options.stopInertiaOnNavigate)&&this.options.wrapper.removeEventListener("click",this.onClick),this.virtualScroll.destroy(),this.dimensions.destroy(),this.cleanUpClassName(),this._rafId&&cancelAnimationFrame(this._rafId)}on(t,a){return this.emitter.on(t,a)}off(t,a){return this.emitter.off(t,a)}get overflow(){const t=this.isHorizontal?"overflow-x":"overflow-y";return getComputedStyle(this.rootElement)[t]}checkOverflow(){["hidden","clip"].includes(this.overflow)?this.internalStop():this.internalStart()}setScroll(t){this.isHorizontal?this.options.wrapper.scrollTo({left:t,behavior:"instant"}):this.options.wrapper.scrollTo({top:t,behavior:"instant"})}isTouchOnSelectionHandle(t){const a=window.getSelection();if(!a||a.isCollapsed||a.rangeCount===0)return!1;const i=t.targetTouches[0]??t.changedTouches[0];if(!i)return!1;const s=a.getRangeAt(0).getClientRects();if(s.length===0)return!1;const r=s[0],c=s[s.length-1],h=40,p=Math.hypot(i.clientX-r.left,i.clientY-r.top)<=h,m=Math.hypot(i.clientX-c.right,i.clientY-c.bottom)<=h;return p||m}resize(){this.dimensions.resize(),this.animatedScroll=this.targetScroll=this.actualScroll,this.emit()}emit(){this.emitter.emit("scroll",this)}reset(){this.isLocked=!1,this.isScrolling=!1,this.animatedScroll=this.targetScroll=this.actualScroll,this.lastVelocity=this.velocity=0,this.animate.stop()}start(){if(this.isStopped){if(this.options.autoToggle){this.rootElement.style.removeProperty("overflow");return}this.internalStart()}}internalStart(){this.isStopped&&(this.reset(),this.isStopped=!1,this.emit())}stop(){if(!this.isStopped){if(this.options.autoToggle){this.rootElement.style.setProperty("overflow","clip");return}this.internalStop()}}internalStop(){this.isStopped||(this.reset(),this.isStopped=!0,this.emit())}scrollTo(t,{offset:a=0,immediate:i=!1,lock:s=!1,programmatic:r=!0,lerp:c=r?this.options.lerp:void 0,duration:h=r?this.options.duration:void 0,easing:p=r?this.options.easing:void 0,onStart:m,onComplete:f,force:y=!1,userData:v}={}){if((this.isStopped||this.isLocked)&&!y)return;let b=t,T=a;if(typeof b=="string"&&["top","left","start","#"].includes(b))b=0;else if(typeof b=="string"&&["bottom","right","end"].includes(b))b=this.limit;else{let M=null;if(typeof b=="string"?(M=b.startsWith("#")?document.getElementById(b.slice(1)):document.querySelector(b),M||(b==="#top"?b=0:console.warn("Lenis: Target not found",b))):b instanceof HTMLElement&&(b!=null&&b.nodeType)&&(M=b),M){if(this.options.wrapper!==window){const B=this.rootElement.getBoundingClientRect();T-=this.isHorizontal?B.left:B.top}const I=M.getBoundingClientRect(),A=getComputedStyle(M),R=this.isHorizontal?Number.parseFloat(A.scrollMarginLeft):Number.parseFloat(A.scrollMarginTop),j=getComputedStyle(this.rootElement),V=this.isHorizontal?Number.parseFloat(j.scrollPaddingLeft):Number.parseFloat(j.scrollPaddingTop);b=(this.isHorizontal?I.left:I.top)+this.animatedScroll-(Number.isNaN(R)?0:R)-(Number.isNaN(V)?0:V)}}if(typeof b=="number"){if(b+=T,this.options.infinite){if(r){this.targetScroll=this.animatedScroll=this.scroll;const M=b-this.animatedScroll;M>this.limit/2?b-=this.limit:M<-this.limit/2&&(b+=this.limit)}}else b=og(0,b,this.limit);if(b===this.targetScroll){m==null||m(this),f==null||f(this);return}if(this.userData=v??{},i){this.animatedScroll=this.targetScroll=b,this.setScroll(this.scroll),this.reset(),this.preventNextNativeScrollEvent(),this.emit(),f==null||f(this),this.userData={},requestAnimationFrame(()=>{this.dispatchScrollendEvent()});return}r||(this.targetScroll=b),typeof h=="number"&&typeof p!="function"?p=Xp:typeof p=="function"&&typeof h!="number"&&(h=1),this.animate.fromTo(this.animatedScroll,b,{duration:h,easing:p,lerp:c,onStart:()=>{s&&(this.isLocked=!0),this.isScrolling="smooth",m==null||m(this)},onUpdate:(M,I)=>{this.isScrolling="smooth",this.lastVelocity=this.velocity,this.velocity=M-this.animatedScroll,this.direction=Math.sign(this.velocity),this.animatedScroll=M,this.setScroll(this.scroll),r&&(this.targetScroll=M),I||this.emit(),I&&(this.reset(),this.emit(),f==null||f(this),this.userData={},requestAnimationFrame(()=>{this.dispatchScrollendEvent()}),this.preventNextNativeScrollEvent())}})}}preventNextNativeScrollEvent(){this._preventNextNativeScrollEvent=!0,requestAnimationFrame(()=>{this._preventNextNativeScrollEvent=!1})}hasNestedScroll(t,{deltaX:a,deltaY:i}){const s=Date.now();t._lenis||(t._lenis={});const r=t._lenis;let c,h,p,m,f,y,v,b,T,M;if(s-(r.time??0)>2e3){r.time=Date.now();const _=window.getComputedStyle(t);if(r.computedStyle=_,c=["auto","overlay","scroll"].includes(_.overflowX),h=["auto","overlay","scroll"].includes(_.overflowY),f=["auto"].includes(_.overscrollBehaviorX),y=["auto"].includes(_.overscrollBehaviorY),r.hasOverflowX=c,r.hasOverflowY=h,!(c||h))return!1;v=t.scrollWidth,b=t.scrollHeight,T=t.clientWidth,M=t.clientHeight,p=v>T,m=b>M,r.isScrollableX=p,r.isScrollableY=m,r.scrollWidth=v,r.scrollHeight=b,r.clientWidth=T,r.clientHeight=M,r.hasOverscrollBehaviorX=f,r.hasOverscrollBehaviorY=y}else p=r.isScrollableX,m=r.isScrollableY,c=r.hasOverflowX,h=r.hasOverflowY,v=r.scrollWidth,b=r.scrollHeight,T=r.clientWidth,M=r.clientHeight,f=r.hasOverscrollBehaviorX,y=r.hasOverscrollBehaviorY;if(!(c&&p||h&&m))return!1;const I=Math.abs(a)>=Math.abs(i)?"horizontal":"vertical";let A,R,j,V,B,N;if(I==="horizontal")A=Math.round(t.scrollLeft),R=v-T,j=a,V=c,B=p,N=f;else if(I==="vertical")A=Math.round(t.scrollTop),R=b-M,j=i,V=h,B=m,N=y;else return!1;return!N&&(A>=R||A<=0)?!0:(j>0?A<R:A>0)&&V&&B}get rootElement(){return this.options.wrapper===window?document.documentElement:this.options.wrapper}get limit(){return this.options.naiveDimensions?this.isHorizontal?this.rootElement.scrollWidth-this.rootElement.clientWidth:this.rootElement.scrollHeight-this.rootElement.clientHeight:this.dimensions.limit[this.isHorizontal?"x":"y"]}get isHorizontal(){return this.options.orientation==="horizontal"}get actualScroll(){const t=this.options.wrapper;return this.isHorizontal?t.scrollX??t.scrollLeft:t.scrollY??t.scrollTop}get scroll(){return this.options.infinite?ex(this.animatedScroll,this.limit):this.animatedScroll}get progress(){return this.limit===0?1:this.scroll/this.limit}get isScrolling(){return this._isScrolling}set isScrolling(t){this._isScrolling!==t&&(this._isScrolling=t,this.updateClassName())}get isStopped(){return this._isStopped}set isStopped(t){this._isStopped!==t&&(this._isStopped=t,this.updateClassName())}get isLocked(){return this._isLocked}set isLocked(t){this._isLocked!==t&&(this._isLocked=t,this.updateClassName())}get isSmooth(){return this.isScrolling==="smooth"}get className(){let t="lenis";return this.options.autoToggle&&(t+=" lenis-autoToggle"),this.isStopped&&(t+=" lenis-stopped"),this.isLocked&&(t+=" lenis-locked"),this.isScrolling&&(t+=" lenis-scrolling"),this.isScrolling==="smooth"&&(t+=" lenis-smooth"),t}updateClassName(){this.cleanUpClassName(),this.className.split(" ").forEach(t=>{this.rootElement.classList.add(t)})}cleanUpClassName(){for(const t of Array.from(this.rootElement.classList))(t==="lenis"||t.startsWith("lenis-"))&&this.rootElement.classList.remove(t)}};const rx=[{heading:"What is MVC and where it came from",blocks:[{t:"p",text:"**MVC (Model–View–Controller)** is the oldest UI architecture pattern — invented by Trygve Reenskaug at Xerox PARC in **1979** for Smalltalk. It splits an interactive application into three roles: the **Model** (data + business logic), the **View** (what the user sees), and the **Controller** (interprets user input and updates the Model)."},{t:"list",items:["**Model** — owns application data and rules; notifies observers when it changes. Knows nothing about View or Controller.","**View** — renders the Model. In *classic* MVC the View observes the Model directly and re-reads it on change notifications.","**Controller** — receives raw user input (clicks, keystrokes), translates it into Model operations. In classic MVC it does *not* update the View — the Model's change notification does."]},{t:"note",text:"Key structural fact interviewers probe: in classic MVC the **View talks to the Model directly** (reads it, observes it). That's the fundamental difference from MVP/MVVM, where the middle layer fully insulates the View from the Model."}]},{heading:"The variants that confuse everyone",blocks:[{t:"p",text:'"MVC" means different things in different ecosystems, and untangling them is a classic senior question:'},{t:"table",headers:["Variant","How it wires up","Where you see it"],rows:[["Classic (Smalltalk) MVC","View observes Model; Controller handles input only","original desktop GUIs; mostly historical"],["Web MVC (a.k.a. Model 2)","Controller receives the HTTP request, manipulates the Model, *selects and populates* the View (template); no live observation — one-shot request/response","Spring MVC, Rails, Django (MTV), ASP.NET MVC"],["Apple MVC (Cocoa)","Controller sits *between* Model and View, mediating both directions",'iOS UIKit — famously degrades into "Massive View Controller"'],['"Android MVC"',"Activity/Fragment plays View *and* Controller simultaneously; XML layout is the (dumb) View skeleton","pre-2017 Android codebases"]]},{t:"p",text:`So when an interviewer asks "is Android MVC?", the accurate answer: Android's framework never enforced any pattern; the *default* shape people wrote — everything in the Activity — is closest to Apple-style MVC where the controller absorbed everything, and that failure mode is exactly why the community moved on.`}]},{heading:"MVC on Android in practice — the God Activity",blocks:[{t:"p",text:`Here's what "MVC" Android code actually looked like — the Activity finds views, handles clicks, calls the network, parses responses, mutates widgets, and manages lifecycle, all in one class:`},{t:"code",title:"The classic God Activity (what NOT to do — but know how to critique it)",code:`class UserActivity : AppCompatActivity() {

    private lateinit var nameText: TextView
    private lateinit var progress: ProgressBar
    private var call: Call<UserDto>? = null

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_user)
        nameText = findViewById(R.id.name)
        progress = findViewById(R.id.progress)

        progress.visibility = View.VISIBLE
        call = api.getUser(intent.getStringExtra("id")!!)
        call!!.enqueue(object : Callback<UserDto> {
            override fun onResponse(c: Call<UserDto>, r: Response<UserDto>) {
                progress.visibility = View.GONE
                // business rule buried in the "view":
                val display = if (r.body()!!.isPremium) "⭐ " + r.body()!!.name
                              else r.body()!!.name
                nameText.text = display
            }
            override fun onFailure(c: Call<UserDto>, t: Throwable) {
                progress.visibility = View.GONE
                Toast.makeText(this@UserActivity, t.message, Toast.LENGTH_SHORT).show()
            }
        })
    }

    override fun onDestroy() {
        super.onDestroy()
        call?.cancel() // manual lifecycle management, easy to forget
    }
}`},{t:"list",items:["The Activity is simultaneously **View** (mutating widgets), **Controller** (handling input, orchestrating the call), and partly **Model** (the premium-star business rule).","Rotation destroys everything mid-flight: the request either dies or, worse, its callback touches a destroyed Activity — the historical source of countless `IllegalStateException`s and leaks.","Nothing is unit-testable: every line needs an Android device/emulator because it's welded to framework classes."]}]},{heading:"Why MVC breaks down on Android specifically",blocks:[{t:"list",items:["**No separate Controller slot in the framework**: Android hands you one entry-point class (Activity/Fragment) that already owns the view hierarchy and the lifecycle — the V and C roles collapse into it by gravity.","**Lifecycle hostility**: classic MVC assumes long-lived View/Controller objects; Android destroys and recreates them on rotation, so any in-controller state is lost and any Model→View observation must be re-wired constantly.",`**View–Model coupling**: even done "correctly", classic MVC lets the View read the Model — so Model refactors ripple into layouts/adapters, and you can't fake the Model easily for UI tests.`,"**Testability**: controllers welded to Activities need instrumented tests; JVM unit tests are impossible for the interesting logic.","**SRP violation at scale**: 2000-line Activities where a bug fix in networking risks breaking click handling — the maintenance cost that drove MVP adoption around 2014–2016."]},{t:"note",text:"Interview framing: don't just trash MVC. Say *why it made sense* (simple, no indirection, fine for tiny screens) and *what specifically about Android* (lifecycle + merged roles) made it fail — that shows architectural judgment rather than memorized dogma."}]},{heading:"MVC vs MVP vs MVVM — the exact deltas",blocks:[{t:"table",headers:["Question","MVC","MVP","MVVM"],rows:[["Does the View know the Model?","**Yes** — reads/observes it directly","No — Presenter mediates everything","No — ViewModel exposes prepared state"],["Does the middle layer know the View?","Controller: often yes (Android: they're the same object)","Yes — via a View **interface**","**No** — View observes the VM"],["Input handling","Controller","View forwards to Presenter","View calls VM functions"],["Testability of logic","poor (framework-welded)","good (mock View interface)","good (assert observable state)"],["Config-change story","none","manual (retain/detach)","built-in (Jetpack ViewModel)"]]},{t:"p",text:"Evolution in one sentence: MVC → MVP moved all View↔Model traffic through a testable Presenter with a View interface; MVP → MVVM replaced the Presenter→View interface calls with **observable state**, removing the attach/detach ceremony and gaining lifecycle survival."}]},{heading:"Migrating away from a God-Activity MVC codebase",blocks:[{t:"list",items:["**Extract the data layer first**: move API/database code behind repositories — highest value, lowest risk, benefits every later step.","**Then extract presentation logic per screen** into ViewModels (going straight to MVVM — migrating MVC→MVP in 2026 would be moving into another condemned building).","**Characterize before you cut**: God Activities hide implicit behavior (ordering, double-click guards, toast timing); pin it with UI tests (Espresso/Compose) before splitting.","**Kill silent couplings**: God Activities often communicate via static fields, singletons and sticky broadcasts — replace with explicit repository state as you go.","Strangler pattern, screen by screen; new features in the new architecture only."]}]}],lx=[{level:"junior",q:"What is MVC? Explain the three components.",a:[{t:"p",text:"MVC splits an app into **Model** (data + business logic, notifies observers on change), **View** (renders the model), and **Controller** (interprets user input and updates the model). It's the oldest UI pattern — Smalltalk, 1979. The defining wiring of classic MVC: the **View reads/observes the Model directly**, and the Controller handles input — it doesn't paint the screen."}]},{level:"junior",q:"In Android MVC, which component is the Activity?",a:[{t:"p",text:`Both View **and** Controller — and that's precisely the problem. Android gives you one class that owns the view hierarchy, receives input, and holds the lifecycle, so the roles merge into a "God Activity". The XML layout is only the static skeleton of the View, not the View role itself (it has no behavior). Because V and C are one object, you can't test controller logic without an emulator, and there's no seam to keep business rules out of UI code.`}]},{level:"junior",q:"What are the main problems with MVC on Android?",a:[{t:"list",items:["**God Activity** — UI mutation, input handling, networking and business rules in one class violating single-responsibility.","**Untestable** — logic welded to framework classes needs instrumented tests; no JVM unit tests.","**Lifecycle pain** — rotation destroys the Activity mid-operation: leaked callbacks, lost state, crashes touching dead views.","**View–Model coupling** — the View reading the Model directly means model refactors ripple into UI code."]}]},{level:"junior",q:"What is the difference between a Controller and a Presenter?",a:[{t:"p",text:"A **Controller** handles *input*: it translates user gestures into Model operations, and in classic MVC the View then updates itself by observing the Model — the Controller may never touch the View. A **Presenter** additionally owns *output*: all Model data reaches the View through it, and it actively drives the View via an interface (`view.showUser(...)`). So MVP fully insulates View from Model, while MVC does not. Mnemonic: Controller = input router; Presenter = input **and** presentation logic."}]},{level:"junior",q:"Is MVC ever an acceptable choice for an Android app?",a:[{t:"p",text:'For a genuinely trivial app — a one-screen utility, a prototype, a hackathon demo — putting logic straight into the Activity is honest pragmatism, and adding layers would be ceremony. The moment the app has real state, async work, or a second developer, the costs (untestability, lifecycle bugs, unbounded class growth) outweigh the simplicity. A good answer also notes that with modern tools the "simple" baseline has shifted: a minimal `ViewModel` + `StateFlow` costs almost nothing, so there\'s rarely a reason to start with God-Activity MVC even for small apps.'}]},{level:"junior",q:"In classic MVC, how does the View update when data changes?",a:[{t:"p",text:"Through the **observer pattern**: the View registers as an observer of the Model; when the Controller mutates the Model, the Model fires change notifications and the View re-reads the data it renders. This is worth knowing because it shows MVC already contained the seed of reactive UI — MVVM's `StateFlow` observation is the same idea with the crucial twist that the View observes *prepared UI state* rather than the raw domain model."}]},{level:"senior",q:"Compare classic MVC, Web MVC, and Apple's MVC. Why does 'MVC' mean such different things?",a:[{t:"list",items:["**Classic (Smalltalk)**: live objects; View observes Model directly; Controller strictly handles input devices. Designed for always-alive desktop GUIs.","**Web MVC (Model 2 — Rails/Spring/Django)**: request/response, not observation. The Controller receives an HTTP request, calls the Model, then *selects and fills a View template* and returns it. Nothing is long-lived; 'observing' is impossible across HTTP, so the Controller took over view population.","**Apple MVC**: the ViewController deliberately mediates both directions between Model and View — closer to MVP structurally, but because it also owns the view lifecycle it absorbs everything → 'Massive View Controller'."]},{t:"p",text:"The name survived while the wiring changed because each platform kept the *role names* and adapted the *communication paths* to its runtime model (live objects vs stateless requests vs framework-owned view controllers). That's the real lesson: patterns are shaped by platform constraints — the same reason Android 'MVC' degraded into the God Activity and the community eventually needed the Jetpack ViewModel to get a slot the framework didn't provide."}]},{level:"senior",q:"Why did MVC's failure on Android lead to MVP first rather than straight to MVVM?",a:[{t:"p",text:"Chronology and tooling. When teams hit the God-Activity wall (~2013–2015), Android had **no observability primitives** in the toolkit — no LiveData, no lifecycle-aware anything; RxJava existed but was a heavy adoption bet. MVP required nothing new: plain interfaces and classes, instantly unit-testable with JUnit + Mockito, which matched the era's testing push. MVVM only became the obvious default when Google shipped **Architecture Components (2017)** — a `ViewModel` that survives rotation plus `LiveData` for safe observation solved exactly the two things MVP handled awkwardly (config changes and lifecycle-safe view updates). So the sequence was driven by infrastructure availability, not pattern merit alone."}]},{level:"senior",q:"You inherit a 3000-line God Activity. Walk me through your migration plan and its risks.",a:[{t:"list",items:["**Stabilize first**: add characterization tests (Espresso) for the critical flows — God Activities are full of implicit behavior (ordering assumptions, double-tap guards) that naive extraction breaks.","**Extract the data layer**: all network/DB/prefs access behind repository classes. Mechanical, low-risk, immediately shrinks the Activity and creates the seam every later step needs.","**Extract state and presentation logic** into a ViewModel exposing a `UiState` — go directly to MVVM; MVP as a stepping stone doubles the work. Replace each 'mutate widget here' site with a state field + one render function.","**Untangle hidden channels**: statics, singletons-as-buses, `onActivityResult` chains, sticky intents — replace with repository state or explicit results; this is where the surprises live.","**Slice by responsibility, not by line count**: pull one feature (e.g. search) end-to-end at a time; ship after each slice."]},{t:"p",text:"Biggest risks: behavioral regressions from implicit ordering, team velocity stalling on a long-lived refactor branch (avoid — keep slices mergeable), and the half-migrated state becoming permanent — mitigate with an explicit definition of done per screen and architecture checks in review/CI."}]},{level:"senior",q:"Defend MVC: what did it get right that modern architectures still use?",a:[{t:"list",items:["**Separation of data from presentation** — the Model as an independent, observable source of truth is the direct ancestor of the repository + reactive stream stack.","**Observer-based UI updates** — 'the view re-renders when the model changes' *is* modern declarative UI; Compose recomposing on state change is classic-MVC observation industrialized.","**Role vocabulary** — Model/View/middle-thing framing is how every successor (MVP, MVVM, MVI) still explains itself; MVC defined the axis they all vary along: *who may talk to whom*.","**Minimal indirection** — for genuinely simple UIs, MVC's directness is a feature; part of why over-layered 'clean' codebases get criticized is that they forgot MVC's economy."]},{t:"p",text:"Sharp closing line: MVC didn't fail conceptually — it failed *operationally* on platforms that merged its roles into one framework class. The fix (MVP, then MVVM) was re-separating roles the platform had fused, not abandoning MVC's ideas."}]},{level:"senior",q:"Does Jetpack Compose bring Android back to MVC?",a:[{t:"p",text:"Interesting framing question — the honest answer is 'closer to classic MVC's *spirit*, but no'. Compose restores the classic-MVC loop: state changes → observing UI re-renders automatically; the framework View role (composables) is finally dumb and stateless like MVC intended. But two things differ fundamentally: the UI observes **prepared UI state from a ViewModel**, never the domain Model directly (that's the MVVM boundary MVC lacked), and input flows through **explicit event callbacks up to a state holder** (UDF), not a controller mutating shared objects. So the modern stack is best described as MVVM/UDF that *finally implements* MVC's original observe-and-render dream with the coupling problems fixed. Saying that shows you understand patterns as evolving wiring, not brand names."}]},{level:"senior",q:"Where does business logic actually live in Android 'MVC', and why is that the core architectural flaw?",a:[{t:"p",text:"In practice it lives **everywhere**: validation in click listeners, pricing rules in `onResponse` callbacks, formatting in adapters, caching in statics. Because the Activity is both V and C, there is no *structural* place that business logic belongs, so it accretes wherever the cursor happens to be. That's the core flaw — not that the Activity is 'too big', but that the architecture provides **no enforceable boundary** between decisions (business/presentation logic) and effects (widget mutation). Every successor pattern is essentially an answer to 'where do decisions live and how do we keep effects out of there': Presenter (MVP), ViewModel (MVVM), reducer (MVI). When asked this, land on the principle: an architecture is good exactly to the degree that logic has one obvious, testable home."}]}],cx={id:"mvc",title:"MVC",tagline:"Model – View – Controller: the original UI pattern, its variants, why it collapsed into the God Activity on Android, and how to talk about it in interviews.",tags:["Android","History","God Activity","Patterns"],content:rx,qa:lx},dx=[{heading:"What is MVP",blocks:[{t:"p",text:"**MVP (Model–View–Presenter)** evolved from MVC (Taligent, 1990s; popularized on Android ~2014–2016) to fix MVC's two coupling problems: the View talking to the Model, and logic having no testable home. In MVP, **all** communication between View and Model flows through the **Presenter**."},{t:"list",items:["**Model** — data layer: repositories, data sources, business logic. Same as in MVVM.","**View** — Activity/Fragment implementing a **View interface**. Completely passive: forwards user input to the Presenter, exposes methods like `showLoading()`, `showUser(user)`, `showError(msg)` that just mutate widgets.","**Presenter** — plain Kotlin/Java class holding a reference to the View *interface*. Receives events (`onRefreshClicked()`), talks to the Model, and drives the View by calling its methods. Contains all presentation logic; zero Android imports."]},{t:"note",text:"The defining wiring: **the Presenter holds a View reference and pushes to it imperatively**. Compare MVVM, where the ViewModel holds no View reference and the View pulls/observes. Every MVP strength and weakness follows from that one arrow."}]},{heading:"The contract — canonical MVP code",blocks:[{t:"code",title:"Contract interface (the signature MVP artifact)",code:`interface UserContract {
    interface View {
        fun showLoading()
        fun hideLoading()
        fun showUser(name: String, isPremium: Boolean)
        fun showError(message: String)
    }
    interface Presenter {
        fun attach(view: View)
        fun detach()
        fun onLoadUser(id: String)
    }
}`},{t:"code",title:"Presenter — pure JVM, fully unit-testable",code:`class UserPresenter(
    private val repository: UserRepository,
    private val mainScheduler: Scheduler, // era-typical: RxJava
) : UserContract.Presenter {

    private var view: UserContract.View? = null
    private val disposables = CompositeDisposable()

    override fun attach(view: UserContract.View) { this.view = view }

    override fun onLoadUser(id: String) {
        view?.showLoading()
        disposables += repository.getUser(id)
            .observeOn(mainScheduler)
            .subscribe(
                { user ->
                    view?.hideLoading()
                    view?.showUser(user.name, user.isPremium)
                },
                { error ->
                    view?.hideLoading()
                    view?.showError(error.message ?: "Unknown error")
                }
            )
    }

    override fun detach() {
        disposables.clear()
        view = null // critical — otherwise the destroyed Activity leaks
    }
}`},{t:"code",title:"View — passive Activity",code:`class UserActivity : AppCompatActivity(), UserContract.View {

    private lateinit var presenter: UserContract.Presenter

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_user)
        presenter = UserPresenter(repository, AndroidSchedulers.mainThread())
        presenter.attach(this)
        refreshButton.setOnClickListener {
            presenter.onLoadUser(intent.getStringExtra("id")!!)
        }
    }

    override fun onDestroy() {
        presenter.detach()
        super.onDestroy()
    }

    override fun showLoading() { progress.isVisible = true }
    override fun hideLoading() { progress.isVisible = false }
    override fun showUser(name: String, isPremium: Boolean) {
        nameText.text = if (isPremium) "⭐ " + name else name
    }
    override fun showError(message: String) {
        Snackbar.make(root, message, Snackbar.LENGTH_SHORT).show()
    }
}`}]},{heading:"Passive View vs Supervising Controller",blocks:[{t:"p",text:"Martin Fowler split MVP into two flavors — a favorite senior distinction:"},{t:"list",items:["**Passive View** — the View is *completely* dumb: every label, every visibility flag is set through an explicit Presenter call. Maximum testability (the whole UI decision surface is presenter logic), maximum boilerplate. The Android-common flavor.","**Supervising Controller** — the View handles simple declarative binding from the Model itself (e.g. data binding for trivial fields), and the Presenter only handles complex logic and input. Less boilerplate, but logic starts seeping back into the View."]},{t:"p",text:"Worth saying in interviews: Supervising Controller with data binding is already halfway to MVVM — the history is a smooth slide, not a jump."}]},{heading:"Lifecycle & configuration changes — MVP's hardest problem",blocks:[{t:"p",text:"The Presenter holds a View reference, and Android destroys Views on rotation. Everything painful about MVP on Android radiates from this:"},{t:"list",items:["**attach/detach ceremony**: the View must attach in `onCreate`/`onStart` and detach in `onDestroy`/`onStop`; forgetting `detach()` (or nulling the reference) leaks the destroyed Activity through the Presenter.","**The null-view dance**: every async callback must guard `view?.` because the result may arrive while detached — and then the update is silently *dropped*: rotate during load and the data never shows unless you re-request or cache.","**Presenter survival options** (none great): recreate it every time and re-fetch (wasteful, flicker); retain it via a retained Fragment / static cache / Loader (pre-Jetpack hacks with their own leaks); libraries like Mosby and Nucleus existed specifically to manage keep-alive + view re-attachment + delivering pending results.","**State restoration**: a re-attached View is blank; the Presenter needs a 'replay current state onto the new view' step — which MVP has no standard mechanism for. (MVVM gets this for free: the new collector immediately receives the current `StateFlow` value.)"]},{t:"note",text:'This section is *the* answer to "why did MVVM win?": Jetpack ViewModel + observable state solves attach/detach, the null-view dance, presenter survival AND state replay in one stroke. Being able to enumerate the four MVP pains and map each to its MVVM fix is a strong senior answer.'}]},{heading:"Testing Presenters",blocks:[{t:"p",text:`MVP's headline feature in its era: the Presenter is a plain class, and the View is an interface you mock — so tests read as "event in → view calls out":`},{t:"code",title:"Classic presenter test (JUnit + Mockito)",code:`@Test
fun loadUser_success_showsUser() {
    val view: UserContract.View = mock()
    val repository: UserRepository = mock()
    whenever(repository.getUser("42"))
        .thenReturn(Single.just(User(name = "Revanth", isPremium = true)))

    val presenter = UserPresenter(repository, Schedulers.trampoline())
    presenter.attach(view)

    presenter.onLoadUser("42")

    inOrder(view) {
        verify(view).showLoading()
        verify(view).hideLoading()
        verify(view).showUser("Revanth", true)
    }
    verify(view, never()).showError(any())
}`},{t:"list",items:["Style difference from MVVM testing: MVP tests **verify interactions** (mock method calls, ordering); MVVM tests **assert emitted state values**. State assertion is generally more robust — interaction tests break when you rename or reorder view calls even if behavior is identical.","Threading in tests: replace `AndroidSchedulers.mainThread()` with `Schedulers.trampoline()` (Rx) — the era's equivalent of today's `Dispatchers.setMain(TestDispatcher)`.","Edge cases worth testing then and now: events arriving while detached, double-clicks (re-entrancy), error-then-retry sequences."]}]},{heading:"Why MVVM replaced MVP — and where you'll still meet MVP",blocks:[{t:"table",headers:["Pain in MVP","MVVM's answer"],rows:[["Presenter → View interface + attach/detach ceremony","No view reference at all — observation"],["Presenter dies on rotation / retain hacks","Jetpack ViewModel survives config changes natively"],["Dropped updates while detached; manual state replay","StateFlow/LiveData replays current state to every new observer"],["One View interface + implementation per screen (boilerplate)","One state class; the View renders it"],["Lifecycle-safety hand-rolled","repeatOnLifecycle / collectAsStateWithLifecycle"]]},{t:"list",items:["You will still meet MVP in: long-lived codebases started 2014–2017 (banking, e-commerce), interviews for teams maintaining them, and some TV/embedded projects.","MVP remains *conceptually* fine where the platform lacks lifecycle churn or an observation toolkit — but on Android/KMP today there is no reason to start a new MVP screen.","Migration MVP→MVVM is mechanical: each `view.showX(args)` call becomes a field on `UiState`; the Presenter's methods become ViewModel functions; the contract dies; tests convert from interaction-verification to state-assertion (and usually get simpler)."]}]}],hx=[{level:"junior",q:"What is MVP and how does data flow through it?",a:[{t:"p",text:"Model–View–Presenter. The **View** (Activity/Fragment implementing a View interface) is passive: it forwards user events to the **Presenter** and exposes methods like `showUser(...)` that only mutate widgets. The **Presenter** — a plain class with no Android imports — receives events, calls the **Model** (repositories), and pushes results back by calling View interface methods. Flow: user input → View → Presenter → Model → Presenter → View. Nothing goes View↔Model directly — that's the fix over MVC."}]},{level:"junior",q:"What is a 'contract' in MVP?",a:[{t:"p",text:"A container interface (usually `SomethingContract`) declaring the paired `View` and `Presenter` interfaces for one screen — the explicit API between the two. Benefits: the whole screen's interaction surface is readable in one place, the Presenter depends only on the abstract View (mockable in tests), and either side can be swapped. Cost: one more file of ceremony per screen — part of why MVP felt heavy."}]},{level:"junior",q:"Why is the Presenter easy to unit test?",a:[{t:"p",text:"Because it's a plain JVM class whose only 'UI' dependency is the View *interface* — in a test you mock the View and the repository, call presenter methods, and **verify** the expected View calls happened (`verify(view).showUser(...)`), including their order. No emulator, no Robolectric. The one thing to handle is threading: swap the main-thread scheduler/dispatcher for a synchronous test one."}]},{level:"junior",q:"Why must you call detach() (or null the view) in MVP, and what happens if you forget?",a:[{t:"p",text:"The Presenter holds a strong reference to the View, which on Android is the Activity/Fragment. When the Activity is destroyed (rotation, back), the Presenter's reference keeps the entire destroyed Activity — and its view hierarchy — reachable, so the GC can't reclaim it: a **memory leak**. Worse, an async result arriving after destruction would call methods on a dead Activity (crashes touching views, `IllegalStateException`s). Hence the ceremony: `attach(view)` in onCreate/onStart, `detach()` (nulling the reference and cancelling work) in onDestroy/onStop — and `view?.` guards on every async callback."}]},{level:"junior",q:"Presenter vs ViewModel — what's the core difference?",a:[{t:"p",text:"The direction of the arrow to the View. A **Presenter** holds a View reference and *pushes* imperatively: `view.showLoading()`. A **ViewModel** holds no View reference; it exposes observable state and the View *pulls/observes*. Consequences: the ViewModel needs no attach/detach and can't leak the Activity; it survives configuration changes (Jetpack); and a new observer automatically receives current state, whereas a re-attached MVP View is blank until the Presenter replays state manually."}]},{level:"junior",q:"What happens in MVP when the screen rotates during a network call?",a:[{t:"p",text:"The Activity detaches and is destroyed; the new Activity attaches (usually to a *new* Presenter). The in-flight result then hits `view?.showUser(...)` with a null or stale view — so the update is **silently dropped** and the new screen either re-fetches (flicker, wasted request) or shows nothing. Solving this properly required retaining the Presenter (retained fragment, static cache, or libraries like Mosby/Nucleus that also buffer results and replay them to the re-attached view) — exactly the machinery Jetpack ViewModel + StateFlow made obsolete."}]},{level:"junior",q:"Is the Activity the View or something else in MVP?",a:[{t:"p",text:"The Activity/Fragment **is the View** — it implements the View interface and does nothing but widget mutation and event forwarding. That's the philosophical shift from Android MVC: instead of fighting to make the Activity a controller, MVP demotes it to a dumb rendering shell and moves the brain into a framework-free Presenter. Follow-up worth volunteering: 'passive view' is the ideal — any `if` beyond trivial rendering that appears in the Activity is logic escaping the Presenter."}]},{level:"senior",q:"Explain Passive View vs Supervising Controller and the trade-off between them.",a:[{t:"p",text:"Fowler's two MVP flavors. **Passive View**: the View is entirely driven by explicit Presenter calls — every text, every visibility. All UI decisions become presenter logic, so test coverage of presentation is total; the price is an interface method for every pixel-level change and verbose presenters. **Supervising Controller**: trivial, declarative bindings go straight View↔Model (e.g. data binding for a name field), and the Presenter handles only complex logic and input — less boilerplate, but simple-binding decisions escape testability and logic gradually leaks viewward."},{t:"p",text:"Senior observation: the industry's path Passive View → Supervising Controller → MVVM is one continuous slide — each step replaces imperative view calls with declarative binding, and MVVM is the endpoint where *all* of it is binding/observation."}]},{level:"senior",q:"How did production MVP apps keep Presenters alive across configuration changes before Jetpack?",a:[{t:"list",items:["**Retained fragment** (`setRetainInstance(true)`) holding the Presenter — the sanctioned pre-Jetpack survival hack (deprecated since).","**Static/application-scoped presenter caches** keyed by screen id, with manual eviction on real finish — leak-prone and easy to get wrong on multi-window/multi-instance.","**Loaders** (`LoaderManager`) — framework-retained async, awkward API, sometimes abused as presenter hosts.","**Libraries**: Mosby (presenter retention + `ViewState` replayed onto re-attached views), Nucleus (retention + delivering cached Rx results on re-attach)."]},{t:"p",text:"Two follow-up points that score: all of these still lose the Presenter on **process death** — input state needed `onSaveInstanceState` handling separately (same split as ViewModel vs SavedStateHandle today); and the very existence of this hack zoo is the evidence that the pattern fought the platform — the Jetpack ViewModel is institutionally 'the retained presenter done right'."}]},{level:"senior",q:"MVP interaction-based tests vs MVVM state-based tests — which are better and why?",a:[{t:"p",text:"MVP tests **verify calls**: `verify(view).showUser(\"Revanth\", true)` with `inOrder` for sequencing. MVVM tests **assert values**: collect the state flow and check emitted `UiState`s. State-based is generally more robust: interaction tests are coupled to the *names and granularity* of view methods — split `showUser` into two calls and every test breaks with identical behavior; mock-verification also can't easily express 'the final state is X regardless of path'. Interaction tests do have one edge: they naturally catch *unwanted* calls (`verify(view, never()).showError(...)`) and exact ordering/timing of imperative effects. Net: assert state where possible, verify interactions only for genuine one-shot effects — which is precisely the shape modern MVVM testing (state assertions + event-channel assertions) converged on."}]},{level:"senior",q:"Sketch the mechanical recipe for migrating one MVP screen to MVVM.",a:[{t:"list",items:["**Inventory the View interface** — it's a specification of the screen's states: each `showX(args)` becomes a field (or variant) of a new `UiState` data class; overlapping calls (`showLoading` + stale list visible) tell you whether you need flags or a sealed hierarchy.","**Presenter → ViewModel**: same constructor deps (repositories); each event method stays; every `view?.callX(...)` becomes `_uiState.update { ... }`. One-shot calls (`navigateTo`, `showToast`) move to an event channel or consumed-state fields.","**Delete attach/detach**; `CompositeDisposable`/manual cancellation becomes `viewModelScope`; Rx chains become coroutines/Flow (or keep Rx with `asFlow` bridges initially — smaller diff).","**View side**: contract implementation replaced by one render function collecting state via `repeatOnLifecycle` (or the screen goes Compose in the same step if the team allows).","**Tests**: each `verify(view).x(...)` assertion becomes an `awaitItem()` state assertion — usually a line-for-line translation."]},{t:"p",text:"Sequencing advice: migrate the *most-edited* screens first (highest interest earned), keep both patterns behind the same repositories, and forbid new MVP screens from day one."}]},{level:"senior",q:"In MVP, where do one-shot actions like navigation and toasts go, and how does that compare to MVVM's event problem?",a:[{t:"p",text:"MVP handles them *trivially*: the Presenter just calls `view.navigateToDetails(id)` — imperative push makes one-shot actions natural, no stickiness possible. The catch is the detached window: fire it while the view is detached and it's dropped (rotate right after a save completes → the navigation never happens), so retained-presenter setups had to queue pending view commands and flush on re-attach — Nucleus/Mosby did exactly this. MVVM inverted the trade: state observation made *continuous* UI trivial but made *one-shot* delivery the hard problem (sticky state, dropped SharedFlow emissions, Channel semantics). It's a genuinely symmetric trade-off, and framing it that way — each pattern is awkward exactly where the other is natural — is a strong senior answer."}]},{level:"senior",q:"Could you use MVP in a KMP project? Why does the ecosystem not do it?",a:[{t:"p",text:"Technically yes — a Presenter is a plain class, and contract interfaces cross platforms fine; early KMM experiments did exactly this. The ecosystem rejected it for three reasons. First, the **lifecycle ceremony multiplies per platform**: every host (Activity, Fragment, UIViewController, SwiftUI view) must implement attach/detach correctly — the leak-prone part is now duplicated. Second, **imperative push doesn't fit SwiftUI/Compose**, which are state-driven by construction: a `view.showUser(...)` call has nowhere natural to land in declarative UI, whereas an observable `StateFlow<UiState>` maps directly onto both. Third, shared observable state via coroutines/Flow was the piece MVP existed to avoid needing — and in KMP it's already there. So shared code converged on ViewModel-shaped state holders (androidx KMP ViewModel, moko-mvvm, Decompose components) exposing StateFlow."}]},{level:"senior",q:"What are the failure modes of MVP at scale that you'd warn a team about?",a:[{t:"list",items:["**Contract explosion** — hundreds of near-identical View/Presenter interface pairs; renaming a UI behavior touches interface + implementation + presenter + tests.","**God Presenters** — the God Activity relocated: without a data/domain layer discipline, presenters absorb caching, mapping and business rules. MVP doesn't prevent it; it just moves the pile.","**View methods becoming a remote control** — dozens of granular `setXVisible(Boolean)` methods whose valid *combinations* are undocumented; the screen's actual state space lives implicitly in call ordering (MVVM's single UiState fixes this by making the state space explicit).","**Lifecycle bug taxes** — every new async source re-introduces the detached-window/null-view/leak class of bugs; code review must police it forever.","**Interaction-test brittleness** — large mock-verification suites that break on refactors, training the team to rubber-stamp test changes."]}]}],ux={id:"mvp",title:"MVP",tagline:"Model – View – Presenter: contracts, passive views, the attach/detach lifecycle problem, presenter testing, and why MVVM replaced it.",tags:["Android","Contracts","Presenter","Legacy","Testing"],content:dx,qa:hx},px=[{heading:"What is MVVM and why it exists",blocks:[{t:"p",text:"**MVVM (Model–View–ViewModel)** is a UI architecture pattern that separates *what is shown* (View) from *the state and logic that decides what to show* (ViewModel) and from *where data comes from* (Model). It was introduced by Microsoft (WPF, 2005) and became the de-facto standard on Android after Google shipped Jetpack **Architecture Components** (2017) with a first-class `ViewModel` class."},{t:"list",items:["**Model** — the data layer: repositories, data sources (network, database, preferences), and domain/business logic. It knows nothing about UI.","**View** — Activity / Fragment / Composable. Renders state and forwards user input. It is as dumb as possible: no business logic, no data access.","**ViewModel** — holds UI state, exposes it as an observable stream, and contains presentation logic. It transforms Model data into something renderable and reacts to user events."]},{t:"p",text:"The defining rule is the **direction of references**: the View knows the ViewModel, the ViewModel knows the Model, but **never the other way around**. The ViewModel has *no reference to the View* — the View *observes* the ViewModel. This one-way dependency is what makes MVVM testable and lifecycle-safe, and it is the main difference from MVP (where the Presenter holds a View interface and calls it directly)."},{t:"note",text:'Interview one-liner: "MVVM separates UI from logic through **observable state** — the ViewModel exposes state, the View subscribes to it, and events flow the opposite way. The ViewModel never touches the View, which gives us testability, lifecycle safety and free survival of configuration changes."'}]},{heading:"The three layers on Android — minimal example",blocks:[{t:"p",text:"A canonical modern stack: Repository (Model) → ViewModel exposing `StateFlow<UiState>` → Compose UI collecting it. The same shape works with XML views observing via `repeatOnLifecycle`."},{t:"code",title:"Model — repository",code:`class UserRepository(
    private val api: UserApi,
    private val dao: UserDao,
) {
    fun observeUser(id: String): Flow<User> = dao.observeUser(id)

    suspend fun refreshUser(id: String) {
        val remote = api.fetchUser(id)
        dao.upsert(remote.toEntity())
    }
}`},{t:"code",title:"ViewModel — state + presentation logic",code:`data class UserUiState(
    val isLoading: Boolean = false,
    val user: User? = null,
    val error: String? = null,
)

class UserViewModel(
    private val repository: UserRepository,
    savedStateHandle: SavedStateHandle,
) : ViewModel() {

    private val userId: String = checkNotNull(savedStateHandle["userId"])

    private val _uiState = MutableStateFlow(UserUiState(isLoading = true))
    val uiState: StateFlow<UserUiState> = _uiState.asStateFlow()

    init {
        viewModelScope.launch {
            repository.observeUser(userId)
                .catch { e -> _uiState.update { it.copy(isLoading = false, error = e.message) } }
                .collect { user ->
                    _uiState.update { it.copy(isLoading = false, user = user, error = null) }
                }
        }
        refresh()
    }

    fun refresh() {
        viewModelScope.launch {
            _uiState.update { it.copy(isLoading = true) }
            runCatching { repository.refreshUser(userId) }
                .onFailure { e -> _uiState.update { it.copy(isLoading = false, error = e.message) } }
        }
    }
}`},{t:"code",title:"View — Compose",code:`@Composable
fun UserScreen(viewModel: UserViewModel = hiltViewModel()) {
    val state by viewModel.uiState.collectAsStateWithLifecycle()

    when {
        state.isLoading -> LoadingIndicator()
        state.error != null -> ErrorMessage(state.error, onRetry = viewModel::refresh)
        state.user != null -> UserContent(state.user)
    }
}`},{t:"list",items:["State flows **down** (ViewModel → View), events flow **up** (View → ViewModel) — this is **Unidirectional Data Flow (UDF)** and modern MVVM on Android is always described together with it.","The View never mutates state directly; it calls ViewModel functions (`viewModel.refresh()`), and the new state arrives through the observed stream.","The backing-property pattern (`_uiState` private mutable, `uiState` public read-only) guarantees only the ViewModel can change state."]}]},{heading:"Jetpack ViewModel deep dive — lifecycle & internals",blocks:[{t:"p",text:"Important distinction: **\"ViewModel\" the pattern role** vs **`androidx.lifecycle.ViewModel` the Jetpack class**. The class is Google's implementation of the role with two extra superpowers: it **survives configuration changes** and it gets a **cleanup callback** (`onCleared`). You could implement MVVM without it, but you'd lose both."},{t:"h3",text:"How it survives rotation (the internals interviewers love)"},{t:"list",items:["ViewModels are stored in a `ViewModelStore` — essentially a `Map<String, ViewModel>`.","Every `ViewModelStoreOwner` (Activity, Fragment, NavBackStackEntry) owns one store.","On a configuration change the Activity is destroyed and recreated, but the system retains its `NonConfigurationInstances` — the `ViewModelStore` rides along inside it. The recreated Activity gets the *same* store back, so `ViewModelProvider` returns the *same* ViewModel instance.","`onCleared()` is only called when the owner is destroyed **for good** — `Activity.isFinishing == true`, Fragment permanently removed, or back-stack entry popped. On rotation it is *not* called.","Consequence: a ViewModel **outlives** the Activity/Fragment instance — which is exactly why it must never reference one (that would leak the destroyed Activity until the ViewModel dies)."]},{t:"h3",text:"Creation: providers, factories, CreationExtras"},{t:"p",text:"You never call the constructor yourself in production code — you go through `ViewModelProvider` (or `by viewModels()`, `hiltViewModel()`, `koinViewModel()`), which checks the store first and only creates a new instance on a miss. Constructor arguments require a **Factory**; with Hilt, `@HiltViewModel` + `@Inject constructor` generates it for you, and `SavedStateHandle` can simply be added as a constructor parameter."},{t:"code",title:"Manual factory (what Hilt generates for you)",code:`class UserViewModelFactory(
    private val repository: UserRepository,
) : ViewModelProvider.Factory {
    override fun <T : ViewModel> create(modelClass: Class<T>, extras: CreationExtras): T {
        val handle = extras.createSavedStateHandle()
        @Suppress("UNCHECKED_CAST")
        return UserViewModel(repository, handle) as T
    }
}

// In the Fragment/Activity:
private val viewModel: UserViewModel by viewModels { UserViewModelFactory(repo) }`},{t:"h3",text:"viewModelScope"},{t:"list",items:["`viewModelScope` is a `CoroutineScope` bound to the ViewModel: `SupervisorJob() + Dispatchers.Main.immediate`.","All jobs launched in it are **cancelled automatically in `onCleared()`** — no manual cleanup, no leaked coroutines.","`SupervisorJob` means one failed child coroutine does not cancel the siblings.","`Main.immediate` executes without an extra dispatch if you're already on the main thread — avoids one-frame delays.","Never expose `viewModelScope` publicly or launch UI-lifetime work in it that must outlive the screen (e.g. an upload that should finish after the user leaves) — that belongs in an application-scoped scope or `WorkManager`."]},{t:"h3",text:"Scoping options (know all of them)"},{t:"table",headers:["Scope","How","Lives as long as"],rows:[["Activity","`by viewModels()` in Activity","the Activity (across rotations)"],["Fragment","`by viewModels()` in Fragment","the Fragment"],["Shared between fragments","`by activityViewModels()`","the host Activity — classic way to share state between sibling fragments"],["Navigation graph","`hiltViewModel(backStackEntry)` / `navGraphViewModels()`","the nav (sub)graph — great for multi-step flows like checkout or onboarding"],["Parent fragment","`by viewModels({ requireParentFragment() })`","the parent fragment"]]},{t:"note",text:"Edge case: `by viewModels()` is lazy — the ViewModel isn't created until first access. And two `by viewModels()` for the *same type with the same owner and key* return the same instance; pass a different `key` if you genuinely need two."}]},{heading:"Exposing state: LiveData vs StateFlow vs SharedFlow vs Compose State",blocks:[{t:"table",headers:["","LiveData","StateFlow","SharedFlow","Compose mutableStateOf"],rows:[["Initial value","optional","**required**","none","required"],["Lifecycle-aware out of the box","yes","no — use `repeatOnLifecycle` / `collectAsStateWithLifecycle`","no — same","n/a (Compose-managed)"],["Conflates equal values","no (re-emits same value)","**yes — skips `equals()` duplicates**","no (replay/buffer configurable)","skips duplicates (structural equality policy)"],["Operators","limited (`map`, `switchMap`)","full Flow API","full Flow API","n/a"],["KMP compatible","**no** (Android-only)","yes","yes","Compose Multiplatform only"],["Backing for one-shot events","bad (sticky)","bad (sticky, conflated)","possible (replay=0), with caveats","bad"]]},{t:"p",text:"Modern guidance: **`StateFlow` for state, and prefer state over events**. LiveData is in maintenance mode — fine in legacy code, but it's Android-only (a dealbreaker for KMP), has weak operators and no natural coroutine interop. Compose `mutableStateOf` inside a ViewModel is acceptable for Compose-only apps, but `StateFlow` keeps the ViewModel toolkit-agnostic and easier to test with Flow tooling."},{t:"code",title:"The canonical exposure pattern",code:`private val _uiState = MutableStateFlow(UiState())
val uiState: StateFlow<UiState> = _uiState.asStateFlow()

// Always mutate with update {} — it's atomic (compare-and-set loop),
// safe against concurrent updates from multiple coroutines:
fun onQueryChange(query: String) {
    _uiState.update { it.copy(query = query) }
}`},{t:"list",items:["**Why expose the read-only type?** Encapsulation: if the View could call `_uiState.value = ...`, state changes could originate anywhere and UDF collapses. The ViewModel must be the *single writer*.","**`update {}` vs `value =`**: `update` retries on CAS failure, so concurrent `copy()`-based mutations don't lose writes. `value = _uiState.value.copy(...)` from two threads can drop one update.","**Conflation edge case**: `StateFlow` never emits a value equal to the current one, and a slow collector only sees the *latest* value, not every intermediate one. If you must observe every emission (e.g. analytics of each step), StateFlow is the wrong tool — use `SharedFlow`.","**Deriving state**: use `map`/`combine` + `stateIn` instead of manually copying values between flows."]},{t:"code",title:"Derived state with combine + stateIn",code:`val uiState: StateFlow<SearchUiState> = combine(
    queryFlow,
    repository.observeResults(),
    filtersFlow,
) { query, results, filters ->
    SearchUiState(query = query, results = results.applyFilters(filters))
}.stateIn(
    scope = viewModelScope,
    started = SharingStarted.WhileSubscribed(5_000),
    initialValue = SearchUiState(),
)`}]},{heading:"Modeling UiState — one data class vs sealed hierarchy",blocks:[{t:"code",title:"Option A — single data class (flags)",code:`data class ScreenUiState(
    val isLoading: Boolean = false,
    val items: List<Item> = emptyList(),
    val error: String? = null,
)`},{t:"code",title:"Option B — sealed interface (mutually exclusive states)",code:`sealed interface ScreenUiState {
    data object Loading : ScreenUiState
    data class Success(val items: List<Item>) : ScreenUiState
    data class Error(val message: String) : ScreenUiState
}`},{t:"list",items:["**Sealed** shines when states are truly exclusive — the compiler forces exhaustive `when`, and impossible combinations (loading *and* error) can't be represented.","**Data class** shines when states overlap — e.g. showing cached items *while* refreshing *with* an error snackbar. With sealed classes that becomes state duplication.","Common hybrid: a data class whose fields include sealed sub-states (e.g. `val loadState: LoadState`).","Keep it **immutable** (val + immutable collections). Mutable state inside the object breaks Compose recomposition skipping and makes time-travel/debugging impossible.","Senior point: one `UiState` per screen is a guideline, not a law — very complex screens may expose a few cohesive streams instead of one giant object that recomposes everything."]},{t:"note",text:'Classic follow-up: "where do you transform domain models into UI models?" — in the ViewModel (or a dedicated mapper it calls). The View should receive data that is *ready to render* (formatted strings, resolved colors as semantic enums, stable list keys), so formatting logic is testable.'}]},{heading:"One-shot events — the classic hard problem",blocks:[{t:"p",text:"Navigation, snackbars, toasts — things that must happen **exactly once** — don't fit naturally into sticky state: put `showSnackbar = true` in a `StateFlow` and it re-fires after rotation. Every approach has trade-offs; interviewers want you to know all of them."},{t:"table",headers:["Approach","How","Problems"],rows:[["`SingleLiveEvent`","LiveData subclass that delivers once","legacy hack; only one observer; race-prone — avoid"],["`SharedFlow(replay = 0)`","emit events, collect in View","**events emitted while no collector is active are dropped** (e.g. during rotation or in background)"],["`Channel` + `receiveAsFlow()`","buffered; delivery is suspended until a collector exists","events survive gaps in collection, but delivery to *multiple* collectors is fan-out (each event to only one); still lost on process death"],["**State-based events (Google's recommendation)**","model the event as state; View performs it and calls back `eventConsumed()`","slightly more ceremony, but survives config change AND process death (if in SavedStateHandle); no delivery-guarantee ambiguity"]]},{t:"code",title:"Channel-based events (pragmatic, widely used)",code:`private val _events = Channel<UiEvent>(Channel.BUFFERED)
val events: Flow<UiEvent> = _events.receiveAsFlow()

fun onSaveClicked() {
    viewModelScope.launch {
        repository.save()
        _events.send(UiEvent.NavigateBack)
    }
}

// View:
LaunchedEffect(Unit) {
    viewModel.events.collect { event ->
        when (event) {
            UiEvent.NavigateBack -> navController.popBackStack()
            is UiEvent.ShowSnackbar -> snackbarHostState.showSnackbar(event.message)
        }
    }
}`},{t:"code",title:"State-based events (most robust)",code:`data class UiState(
    val savedMessage: String? = null, // non-null == "show it"
)

fun onMessageShown() {
    _uiState.update { it.copy(savedMessage = null) }
}

// View:
state.savedMessage?.let { message ->
    LaunchedEffect(message) {
        snackbarHostState.showSnackbar(message)
        viewModel.onMessageShown()
    }
}`},{t:"note",text:`Senior answer: "I default to modeling events as state per Google's guidance, because Channel/SharedFlow events can be dropped or double-handled around lifecycle gaps. Where true fire-and-forget is fine (analytics, haptics), a Channel is acceptable. And navigation-as-state also plays better with deep links and process death."`}]},{heading:"Configuration changes vs process death (know the difference cold)",blocks:[{t:"table",headers:["","Configuration change (rotation, locale, dark mode, resize)","System-initiated process death"],rows:[["Activity","destroyed & recreated","whole process killed"],["ViewModel","**survives** (same instance)","**destroyed** — recreated from scratch"],["In-memory state (StateFlow values)","survives","**lost**"],["SavedStateHandle / onSaveInstanceState","available (not needed)","**restored** — this is what it's for"],["How to test","rotate device",'background the app → "Terminate" in Android Studio, or `adb shell am kill <pkg>` — then relaunch from recents']]},{t:"p",text:"**`SavedStateHandle`** is a key-value map injected into the ViewModel that participates in the saved-instance-state mechanism. It also receives **navigation arguments** (Navigation component puts destination args into it). Constraint: values must be `Bundle`-compatible (primitives, `Parcelable`, `Serializable`) and **small** — the Binder transaction limit (~1 MB shared across the whole transaction) means you save *identifiers and input*, never lists of data. Restore heavy data from the repository/database using the saved id."},{t:"code",title:"SavedStateHandle + StateFlow integration",code:`class SearchViewModel(
    private val savedStateHandle: SavedStateHandle,
    repository: SearchRepository,
) : ViewModel() {

    // Survives config change AND process death; also writable:
    val query: StateFlow<String> = savedStateHandle.getStateFlow("query", "")

    fun onQueryChange(newQuery: String) {
        savedStateHandle["query"] = newQuery // updates the StateFlow too
    }

    @OptIn(ExperimentalCoroutinesApi::class)
    val results: StateFlow<List<Result>> = query
        .debounce(300)
        .flatMapLatest { q -> repository.search(q) }
        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), emptyList())
}`},{t:"note",text:'Interview trap: "does ViewModel survive process death?" — **No.** A surprising number of candidates say yes. ViewModel survives *configuration changes only*. Process death recovery = SavedStateHandle (for input/ids) + persistent layer (for data). A senior answer also mentions that over-relying on `savedInstanceState` for big data causes `TransactionTooLargeException` crashes.'}]},{heading:"Collecting safely: lifecycle-aware collection & WhileSubscribed(5000)",blocks:[{t:"list",items:["Flows are **not lifecycle-aware**; collecting in `lifecycleScope.launch { }` naively keeps collecting while the app is backgrounded — wasted work, possible crashes touching views after stop.","**XML views**: `viewLifecycleOwner.lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { flow.collect { ... } } }` — collection is **cancelled** at STOP and **restarted** at START.","**Why not `launchWhenStarted`?** It only *suspends* the collector while stopped — the upstream flow (e.g. a location listener) stays active, buffering. Deprecated for this reason. `repeatOnLifecycle` cancels the whole collection.","**Compose**: `collectAsStateWithLifecycle()` (lifecycle-runtime-compose) does the same; plain `collectAsState()` keeps collecting when the app is backgrounded.","Fragment gotcha: always use `viewLifecycleOwner`, not the Fragment's own lifecycle — the view can be destroyed and recreated (back stack) while the Fragment instance lives, leaking observers or touching a dead view."]},{t:"p",text:"**Why `SharingStarted.WhileSubscribed(5_000)`?** With `stateIn`, this stops the upstream flow 5 seconds after the last collector disappears. The magic number 5000 exists because a **configuration change** briefly drops all collectors — the timeout keeps the upstream (DB query, network observation) alive through rotation, but shuts it down for a real backgrounding (onStop → more than 5s). `Eagerly` never stops (fine for cheap, always-needed state); `Lazily` starts on first collector and never stops."}]},{heading:"MVVM with Clean Architecture — repositories, use cases, mapping",blocks:[{t:"p",text:"MVVM only prescribes the *presentation* boundary. In real apps it's combined with a layered data architecture. Google's recommended architecture: **UI layer (View + ViewModel) → optional Domain layer (use cases) → Data layer (repositories → data sources)**."},{t:"list",items:["**Repository**: single source of truth for one data type; decides between network/cache/DB; exposes `Flow` and suspend functions; hides data-source details from the ViewModel.","**Use case (interactor)**: a class encapsulating one business action (`GetUserProfileUseCase`), usually with a single `operator fun invoke()`. Add them when logic is *reused across ViewModels* or complex enough to isolate — a use case that only forwards to a repository is ceremony; be ready to defend either stance.","**Model mapping**: DTO (network) → entity (DB) → domain model → UI model. Each layer owns its model; mapping at boundaries prevents e.g. a server field rename from rippling into composables.","**Threading rule**: the ViewModel should be able to call the data layer from the main thread — repositories/use cases are *main-safe* and move themselves to the right dispatcher (`withContext(ioDispatcher)`) internally.","**Dependency rule**: dependencies point inward (UI → domain → data interfaces). The domain layer has no Android imports — which is exactly what makes it shareable in KMP."]}]},{heading:"MVVM in Kotlin Multiplatform",blocks:[{t:"p",text:"KMP is where MVVM choices really get stress-tested, because the ViewModel and Model can move into `commonMain` while each platform keeps its native (or Compose Multiplatform) UI."},{t:"list",items:["**`androidx.lifecycle:lifecycle-viewmodel` is multiplatform since 2.8** — `ViewModel`, `viewModelScope` and `onCleared` are usable in `commonMain`. `SavedStateHandle` support arrived for CMP in later releases. Before that, the community standard was **moko-mvvm** or hand-rolled `expect/actual` ViewModels.","**LiveData does not exist in KMP** — one of the strongest practical reasons the ecosystem standardized on `StateFlow`.","**Compose Multiplatform**: `viewModel()` / koin's `koinViewModel()` work in common code; ViewModel scoping ties into CMP navigation back-stack entries just like on Android.","**iOS with native SwiftUI**: Swift doesn't understand coroutines/Flow generics natively. Options: expose `StateFlow` and wrap it with a closure-based observer class in Kotlin; use **SKIE** (turns Flows into Swift `AsyncSequence` and adds proper enum bridging); or KMP-NativeCoroutines. In SwiftUI you typically wrap the shared ViewModel in an `ObservableObject`/`@Observable` adapter that subscribes and republishes state.",'**Lifecycle mismatch**: iOS has no Activity recreation, so "survives configuration change" is meaningless there — but `onCleared`-style cleanup still matters. The shared VM\'s lifetime is managed by whoever owns the adapter (e.g. deinit of the SwiftUI wrapper calls `clear()`).',"**Threading**: with the new K/N memory manager, freezing is gone; still, ensure state updates land on the main thread when driving SwiftUI (`Dispatchers.Main` in shared code)."]},{t:"code",title:"Shared ViewModel in commonMain (lifecycle 2.8+)",code:`// commonMain
class ProfileViewModel(
    private val repository: ProfileRepository,
) : ViewModel() {

    private val _uiState = MutableStateFlow<ProfileUiState>(ProfileUiState.Loading)
    val uiState: StateFlow<ProfileUiState> = _uiState.asStateFlow()

    init {
        viewModelScope.launch {
            repository.observeProfile()
                .catch { _uiState.value = ProfileUiState.Error(it.message ?: "Unknown") }
                .collect { _uiState.value = ProfileUiState.Content(it) }
        }
    }
}`},{t:"code",title:"iOS consumption (SwiftUI adapter, with SKIE)",code:`@MainActor
final class ProfileObservable: ObservableObject {
    @Published var state: ProfileUiState = ProfileUiState.Loading()
    private let viewModel: ProfileViewModel

    init(viewModel: ProfileViewModel) {
        self.viewModel = viewModel
        Task {
            // SKIE exposes StateFlow as an AsyncSequence
            for await state in viewModel.uiState {
                self.state = state
            }
        }
    }

    deinit { viewModel.clear() }
}`},{t:"note",text:'Strong senior talking point: "In KMP I keep the ViewModel in commonMain exposing StateFlow, which Android/CMP consume directly; for SwiftUI I bridge via SKIE into an ObservableObject adapter. The pattern stays MVVM everywhere — only the observation mechanism is platform-specific."'}]},{heading:"Testing ViewModels",blocks:[{t:"p",text:"Testability is MVVM's headline benefit — the ViewModel is a plain class with no Android UI dependencies, tested on the JVM with fake/mock repositories. Two pieces of infrastructure make coroutine ViewModels testable:"},{t:"code",title:"MainDispatcherRule — viewModelScope uses Dispatchers.Main",code:`class MainDispatcherRule(
    private val dispatcher: TestDispatcher = UnconfinedTestDispatcher(),
) : TestWatcher() {
    override fun starting(description: Description) = Dispatchers.setMain(dispatcher)
    override fun finished(description: Description) = Dispatchers.resetMain()
}`},{t:"code",title:"Testing state emissions with Turbine",code:`@get:Rule val mainDispatcherRule = MainDispatcherRule()

@Test
fun refresh_emitsLoadingThenContent() = runTest {
    val repository = FakeUserRepository(user = testUser)
    val viewModel = UserViewModel(repository, SavedStateHandle(mapOf("userId" to "42")))

    viewModel.uiState.test {
        assertEquals(true, awaitItem().isLoading)
        val loaded = awaitItem()
        assertEquals(testUser, loaded.user)
        assertEquals(false, loaded.isLoading)
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**`StandardTestDispatcher` vs `UnconfinedTestDispatcher`**: Standard queues coroutines until you `advanceUntilIdle()`/`runCurrent()` — precise control, good for testing intermediate states. Unconfined runs them eagerly — convenient, but you can miss transient states like `isLoading`.","**`stateIn(WhileSubscribed)` gotcha**: the flow does nothing until collected — a test asserting `viewModel.uiState.value` right after construction sees only `initialValue`. Collect it (Turbine, or `backgroundScope.launch { uiState.collect() }`) to start the upstream.","**Inject dispatchers** (`ioDispatcher: CoroutineDispatcher` constructor param) instead of hardcoding `Dispatchers.IO`, so tests can substitute the TestDispatcher and control virtual time (`advanceTimeBy` for `debounce`).","Prefer **fakes** (in-memory repository implementations) over mocks for the data layer — tests read better and don't break on refactors."]}]},{heading:"Anti-patterns & common mistakes (rapid-fire checklist)",blocks:[{t:"list",items:["**Holding a `Context`, View, Activity or Fragment reference in a ViewModel** — leaks the destroyed instance across rotation. If you truly need app context, use `AndroidViewModel`'s application (or better, inject what you need — a resource provider / use case).","**Exposing `MutableStateFlow`/`MutableLiveData` publicly** — breaks single-writer UDF.","**God ViewModel** — thousands of lines handling half the app. Fix: one ViewModel per screen, delegate logic to use cases, split independent widgets into their own state holders.","**Business logic in the View** — `if (user.age > 18)` in a composable/fragment means untestable logic. Views map state to pixels, nothing else.","**Doing data-layer work in the ViewModel** — Retrofit/Room calls directly in the VM couples presentation to data details; go through a repository.","**Observing with the wrong lifecycle owner** in fragments (`this` instead of `viewLifecycleOwner`).","**`GlobalScope` / hardcoded dispatchers** — untestable, leak-prone; use `viewModelScope` + injected dispatchers.","**Sticky-state events** — `showToast = true` in state without a consumed callback replays the toast on every rotation.","**Kicking off loads in `init` without thought** — it makes the VM eager and harder to test/paginate; `stateIn`-driven lazy starts or explicit triggers are often cleaner (know the trade-off; `init` is not *wrong*).","**Passing ViewModels down the composable tree** — pass state and lambdas instead; deep VM references kill previews and reusability.","**Storing big data in SavedStateHandle** — `TransactionTooLargeException`; save ids, reload data.","**Mutable collections in UiState** — breaks Compose stability/skipping and equality-based conflation."]}]},{heading:"MVVM vs MVC vs MVP vs MVI — the comparison question",blocks:[{t:"table",headers:["","MVC (classic Android)","MVP","MVVM","MVI"],rows:[["Who updates the UI","Activity does everything","Presenter calls View interface methods","View observes ViewModel state","View renders a single immutable state from a reducer loop"],["Middle layer → View reference","n/a (they're merged)","**yes** (interface)","**no** (observation)","no (observation)"],["Survives config change","no","no (needs retain hacks)","**yes** (Jetpack ViewModel)","yes (built on VM usually)"],["State shape","scattered","scattered across presenter calls","one or few observable streams","**single state object + explicit intents**"],["Testability","poor","good (mock the View interface)","good (assert emitted state)","excellent (pure reducer)"],["Boilerplate","low","high (View interfaces)","medium","medium-high"]]},{t:"list",items:["MVP died on Android largely because the Presenter↔View interface pair was boilerplate-heavy and lifecycle-blind; MVVM's observation model plus the Jetpack ViewModel's config-change survival won.",'MVI is best understood as **MVVM with stricter rules**: exactly one state object, events as explicit Intent objects, state changes only through a reducer. Modern "MVVM with UDF + single UiState" is already 80% of MVI.',`Good senior close: "I don't pick dogmatically — Compose + StateFlow + UDF naturally lands between MVVM and MVI, and that's what Google's architecture guidance recommends."`]}]}],mx=[{level:"junior",q:"What is MVVM? Explain each component.",a:[{t:"p",text:"MVVM is a UI architecture pattern with three roles. The **Model** is the data layer — repositories and data sources, plus business logic; it knows nothing about UI. The **View** (Activity, Fragment, or Composable) renders state and forwards user input. The **ViewModel** sits between them: it holds UI state, exposes it as an observable stream (`StateFlow`/`LiveData`), and contains presentation logic."},{t:"p",text:"The key rule is that references go one way: View → ViewModel → Model. The ViewModel **never references the View** — the View subscribes to the ViewModel. That makes the ViewModel testable on the JVM and safe across configuration changes."}]},{level:"junior",q:"How is MVVM different from MVP and MVC?",a:[{t:"p",text:"In classic Android **MVC**, the Activity ends up being controller *and* view — everything in one class, untestable. In **MVP**, a Presenter holds a reference to a View *interface* and calls methods on it (`view.showLoading()`); logic becomes testable by mocking that interface, but you write a lot of interface boilerplate and the Presenter must be manually attached/detached to avoid leaks."},{t:"p",text:"**MVVM inverts that last relationship**: the ViewModel exposes observable state and doesn't know who's watching. No View interface, no attach/detach, and with Jetpack's `ViewModel` you also get configuration-change survival for free — which Presenters never had. That's essentially why the Android community moved from MVP to MVVM after Architecture Components shipped."}]},{level:"junior",q:"Is the Jetpack ViewModel class the same thing as the ViewModel in MVVM?",a:[{t:"p",text:"Related but not identical. The MVVM ViewModel is a *pattern role* — any class holding UI state and presentation logic qualifies. `androidx.lifecycle.ViewModel` is Google's concrete implementation of that role with two extras: it **survives configuration changes** (stored in a `ViewModelStore` that's retained across recreation) and provides **`onCleared()`** for cleanup plus `viewModelScope` for coroutines. You could do MVVM without the Jetpack class, but you'd re-implement those features yourself."}]},{level:"junior",q:"How does a ViewModel survive screen rotation?",a:[{t:"p",text:"ViewModels live in a `ViewModelStore`, owned by a `ViewModelStoreOwner` (Activity, Fragment, or nav back-stack entry). On a configuration change the Activity instance is destroyed, but the system retains its non-configuration state — and the `ViewModelStore` is carried inside it. The recreated Activity receives the same store, so when it asks `ViewModelProvider` for the ViewModel, it gets the **same instance** back instead of a new one."},{t:"p",text:"`onCleared()` only fires when the owner goes away permanently — the Activity is finishing, the Fragment is removed for good, or the back-stack entry is popped. Important corollary: the ViewModel outlives the Activity instance, so it must never hold a reference to it."}]},{level:"junior",q:"What is viewModelScope and what happens to it in onCleared()?",a:[{t:"p",text:"`viewModelScope` is a `CoroutineScope` attached to the ViewModel, built from `SupervisorJob() + Dispatchers.Main.immediate`. Anything launched in it is **automatically cancelled when `onCleared()` runs**, so you never leak coroutines when the screen is gone. `SupervisorJob` means one failing child doesn't cancel its siblings, and `Main.immediate` avoids an extra dispatch when you're already on the main thread."},{t:"p",text:"Edge case worth adding: work that must **outlive the screen** (say, finishing an upload) doesn't belong in `viewModelScope` — use an application-scoped `CoroutineScope` injected into the repository, or `WorkManager` if it must survive process death."}]},{level:"junior",q:"Why must a ViewModel never hold a reference to an Activity, Fragment, View, or their Context?",a:[{t:"p",text:"Because the ViewModel outlives them. On rotation the Activity is destroyed and recreated, but the ViewModel instance stays — if it held the old Activity (or anything holding one, like a View or an adapter), the garbage collector can't reclaim it: a **memory leak** of the entire view hierarchy, until the ViewModel itself dies."},{t:"p",text:"If application-level context is genuinely needed (rare — usually a sign work belongs in the data layer), `AndroidViewModel` exposes the `Application`, which is a singleton and safe. The cleaner answer is to inject an abstraction — a resource provider or a use case — so the ViewModel stays free of Android classes and unit-testable."}]},{level:"junior",q:"LiveData vs StateFlow — what are the differences and which should you use?",a:[{t:"list",items:["`StateFlow` **requires an initial value**; LiveData can be empty.","LiveData is **lifecycle-aware by itself** (observers auto-pause/auto-remove); StateFlow needs `repeatOnLifecycle` or `collectAsStateWithLifecycle` to collect safely.","`StateFlow` **conflates equal values** — setting the same value twice emits once; LiveData re-notifies.","StateFlow has the **full Flow operator set** and clean coroutine interop; LiveData has only a handful of transformations.","LiveData is **Android-only**; StateFlow is pure Kotlin — the deciding factor for **KMP**."]},{t:"p",text:"Default today: **StateFlow**. LiveData is in maintenance mode and fine to keep in legacy code, but new code — especially anything that might share logic with KMP — should use StateFlow."}]},{level:"junior",q:"Why do we expose StateFlow instead of MutableStateFlow from the ViewModel?",a:[{t:"p",text:"Encapsulation and single-writer discipline. If the View got the mutable type, any code could set state, and you lose the unidirectional flow that makes the app predictable. The convention is a private `_uiState: MutableStateFlow` plus a public `uiState: StateFlow` via `asStateFlow()`, so the **ViewModel is the only writer** and the View can only read and observe."},{t:"p",text:"Detail: `asStateFlow()` is preferred over just upcasting, because upcast state can be cast back to mutable, while `asStateFlow()` returns a read-only wrapper."}]},{level:"junior",q:"How does the View observe the ViewModel in XML-based UIs vs Compose?",a:[{t:"code",title:"XML / Fragment",code:`viewLifecycleOwner.lifecycleScope.launch {
    viewLifecycleOwner.repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.uiState.collect { state -> render(state) }
    }
}`},{t:"code",title:"Compose",code:"val state by viewModel.uiState.collectAsStateWithLifecycle()"},{t:"p",text:"In both cases the point is **lifecycle-safe collection**: stop collecting when the UI isn't visible, resume when it is. In fragments, always use `viewLifecycleOwner` — the fragment instance can outlive its view on the back stack, and observing with `this` leaks or touches dead views."}]},{level:"junior",q:"How do you pass arguments (like an id) into a ViewModel?",a:[{t:"p",text:'Constructor injection through a **factory** — you never construct a ViewModel directly, so extra parameters must go through `ViewModelProvider.Factory`. With Hilt, `@HiltViewModel` with an `@Inject constructor` generates the factory, and runtime values like a user id are best passed via **`SavedStateHandle`**: the Navigation component automatically places destination arguments into it, so the ViewModel reads `savedStateHandle["userId"]` — and that value also survives process death.'},{t:"p",text:"For assisted injection of values not in navigation args, Hilt supports `@AssistedInject` ViewModel factories (or you write a small custom factory)."}]},{level:"junior",q:"Where should business logic live in MVVM? What belongs in the View?",a:[{t:"p",text:"**Business/domain logic** (rules, validation, calculations) belongs in the domain or data layer — use cases and repositories. **Presentation logic** (combining streams, mapping domain models to UI state, deciding what's visible) belongs in the ViewModel. The **View** only maps state to widgets and forwards events — if you see `if (user.balance < 0)` deciding colors in a Fragment, that condition should have produced a semantic field in UiState instead."},{t:"p",text:"Litmus test: could you unit-test the decision without instrumented tests? If not, it's in the wrong layer."}]},{level:"junior",q:"What is a Repository and why doesn't the ViewModel call Retrofit/Room directly?",a:[{t:"p",text:"A repository is the data layer's public API for one type of data — the **single source of truth**. It decides between cache, database, and network; exposes `Flow`s and suspend functions; and hides the data-source libraries entirely. The ViewModel calling Retrofit directly couples presentation to transport details, duplicates caching/merging logic across screens, and makes the ViewModel hard to test. With a repository interface, tests swap in an in-memory fake."},{t:"p",text:"Bonus rule: repositories should be **main-safe** — the ViewModel can call them from the main thread and they handle `withContext(ioDispatcher)` internally."}]},{level:"junior",q:"What is Unidirectional Data Flow (UDF)?",a:[{t:"p",text:"A cycle with one direction: **state flows down, events flow up**. The View sends user events to the ViewModel (`onQueryChange(...)`), the ViewModel updates state, and the new state flows back down through the observable stream to be rendered. The View never mutates state itself. Benefits: a single place where state changes (easy to debug), state that's a pure function of data (easy to reason about), and natural fit with Compose's declarative model. Modern MVVM on Android is always practiced together with UDF."}]},{level:"junior",q:"How do two Fragments share data using a ViewModel?",a:[{t:"p",text:"Scope one ViewModel to a **shared owner**. Sibling fragments in the same Activity use `by activityViewModels()` — both get the same instance because the Activity's `ViewModelStore` is shared. With the Navigation component, scoping to a **navigation graph** (`hiltViewModel(parentEntry)` / `navGraphViewModels(R.id.checkout_graph)`) is usually better: the ViewModel lives exactly as long as the flow (e.g. a checkout wizard) and is cleared when the user leaves it, instead of lingering for the whole Activity."}]},{level:"junior",q:"What happens to the ViewModel when the user presses back vs when they rotate the screen?",a:[{t:"p",text:"**Rotation**: configuration change — the Activity is recreated but `isFinishing` is false, the `ViewModelStore` is retained, the same ViewModel instance is reused, `onCleared()` is *not* called. **Back press**: the Activity is finishing — the store is cleared, **`onCleared()` runs**, `viewModelScope` is cancelled, and the ViewModel is gone. Same logic for fragments (permanent removal vs recreation) and nav back-stack entries (popped vs kept)."}]},{level:"junior",q:"How do you show a one-time message (Snackbar/Toast) from a ViewModel?",a:[{t:"p",text:"The ViewModel can't touch UI, so it signals the View. The naive way — a `showMessage: Boolean` in sticky state — replays after rotation. Standard options: a **`Channel` collected as a Flow** in the View (buffered, delivered once), or Google's recommended **state-based event**: put `message: String?` in UiState, the View shows it and calls `viewModel.onMessageShown()` which nulls it out. State-based is the most robust across config change and process death; Channel is pragmatic and common. What matters in the interview is naming the rotation-replay problem and one correct solution."}]},{level:"junior",q:"What is SavedStateHandle and when do you need it?",a:[{t:"p",text:"A key-value map handed to the ViewModel that plugs into the saved-instance-state system. Two jobs: it carries **navigation arguments**, and anything you write into it **survives system-initiated process death** (which the ViewModel itself does not). It exposes values as `getStateFlow(key, default)` / `getLiveData`. Constraints: values must be Bundle-compatible (primitives/Parcelable) and small — store *ids and user input*, then reload real data from the repository."}]},{level:"junior",q:"Is data binding required for MVVM on Android?",a:[{t:"p",text:'No. MVVM only requires that the View *observes* the ViewModel — the mechanism is an implementation detail. XML **DataBinding** (binding expressions in layouts) was one popular mechanism and is what people historically associated with MVVM, but it\'s effectively legacy now; most XML projects use ViewBinding plus manual `collect`, and Compose observes state natively. Saying "MVVM = data binding" is a red flag answer.'}]},{level:"junior",q:"How do you represent loading, success and error in the UI state?",a:[{t:"p",text:'Two idioms. A **single data class** with fields (`isLoading`, `data`, `error`) — flexible, allows overlapping states like "showing cached data while refreshing with an error banner". Or a **sealed interface** (`Loading / Success / Error`) — states are mutually exclusive and the compiler forces exhaustive handling. Choose sealed when states truly exclude each other; choose the data class when they can overlap. Either way the state object stays **immutable** and is replaced wholesale via `copy()`.'}]},{level:"junior",q:"Can you create a ViewModel with 'new'/direct constructor call? Why or why not?",a:[{t:"p",text:"You *can* (it's just a class — unit tests do exactly this), but in production you must go through `ViewModelProvider` / `by viewModels()` / `hiltViewModel()`. The provider first checks the owner's `ViewModelStore` and returns the existing instance if present — that lookup is the entire survival mechanism. A directly-constructed ViewModel is never stored, so you'd get a fresh object on every rotation and `onCleared` would never be managed for it."}]},{level:"senior",q:"Walk me through exactly how a ViewModel survives configuration change under the hood.",a:[{t:"p",text:"`ComponentActivity` implements `ViewModelStoreOwner` and owns a `ViewModelStore` — a map of key → ViewModel. When a config change begins, the framework calls `onRetainNonConfigurationInstance()`; ComponentActivity packs the `ViewModelStore` into its `NonConfigurationInstances` object, which the ActivityThread keeps in memory across the destroy/recreate (the process never dies). The new Activity instance retrieves it via `getLastNonConfigurationInstance()` and adopts the store. So `ViewModelProvider.get()` computes the same default key (`androidx.lifecycle.ViewModelProvider.DefaultKey:` + class name), finds the entry, and returns the same object."},{t:"p",text:"The cleanup path: ComponentActivity observes its own lifecycle; on `ON_DESTROY` it checks `isChangingConfigurations` — only when that's **false** does it call `viewModelStore.clear()`, which iterates ViewModels and invokes `onCleared()` (also closing `viewModelScope`). Fragments do the analogous dance through `FragmentManagerViewModel`, and Navigation scopes stores to `NavBackStackEntry`s cleared on pop."}]},{level:"senior",q:"Configuration change vs process death — what exactly survives each, and how do you build for both?",a:[{t:"list",items:["**Config change**: process lives. ViewModel instance and all in-memory state survive; the Activity/views are recreated. `SavedStateHandle` isn't needed but keeps working.","**Process death**: the OS kills the backgrounded process. ViewModel is gone; only the saved-instance-state Bundle survives (the system holds it) — restored into `SavedStateHandle` on relaunch. In-memory caches, singletons, StateFlows — all gone."]},{t:"p",text:"Strategy: treat `SavedStateHandle` as the place for **identity and transient input** (selected id, query text, scroll anchor), and the **persistent layer as the source of truth for data**. On recreation the ViewModel reads the id from the handle and re-subscribes to the repository — the same code path as first launch, so process-death recovery isn't a special case. Watch the Binder limit (~1 MB per transaction, shared): dumping lists into the handle causes `TransactionTooLargeException`. Test it honestly: background the app, kill via Android Studio's Terminate (or `adb shell am kill`), relaunch from Recents — rotation tests prove nothing about process death."}]},{level:"senior",q:"StateFlow conflation — when is it a problem and what do you do about it?",a:[{t:"p",text:"Two distinct behaviors. First, **equality skipping**: `StateFlow` won't emit a value `equals()` to the current one. That bites with mutable objects (mutate-and-reset looks unchanged) or when you *want* re-emission (retry the same 'scroll to top' command). Second, **conflation proper**: a slow collector only ever sees the latest value — intermediate states can be skipped entirely. For *state* this is exactly right (the screen only cares about now); for anything where **every emission matters** — analytics events, a queue of animations, commands — it's wrong."},{t:"p",text:"Fixes: keep state immutable data classes so equality is meaningful; for must-not-miss streams use `SharedFlow` with an explicit `replay`/`extraBufferCapacity`/`onBufferOverflow` policy or a `Channel`; and don't model events as state values (a `counter` field to force emission is a design smell — that's an event stream in disguise)."}]},{level:"senior",q:"Why exactly 5000 ms in SharingStarted.WhileSubscribed(5000)? Compare with Eagerly and Lazily.",a:[{t:"p",text:"`WhileSubscribed(5_000)` stops the upstream flow 5 s after the last collector unsubscribes and restarts it when someone re-subscribes. The 5 s exists for **configuration changes**: during rotation, collectors vanish for a few hundred milliseconds; without the timeout the upstream (a Room query, a socket subscription) would be torn down and cold-restarted — flicker, wasted IO, re-shown loading states. 5 s comfortably covers recreation while still stopping work shortly after a genuine backgrounding (ON_STOP with `repeatOnLifecycle(STARTED)`). It also pairs with `replay`/initial value so the returning collector gets the last state instantly."},{t:"list",items:["`Eagerly` — starts immediately with the scope, never stops until scope cancellation. Fine for cheap, always-relevant state; wasteful for expensive upstreams.","`Lazily` — starts on the first collector, then never stops. Rarely what you want for UI.","`WhileSubscribed(0)` — stops instantly on unsubscribe; restarts upstream on every rotation (the exact problem 5000 solves)."]}]},{level:"senior",q:"Why was launchWhenStarted deprecated in favor of repeatOnLifecycle?",a:[{t:"p",text:"`launchWhenStarted` only **suspends** the coroutine below STARTED — the upstream flow stays **active**. A location provider, a Room observer, a socket keeps producing while the app is backgrounded; emissions pile up (or hot hardware stays on), and work resumes with a burst on return. It also silently never cancels — the producer runs until the scope dies. `repeatOnLifecycle(STARTED)` instead **cancels the whole collection** at ON_STOP and **restarts the block** at ON_START — the upstream is genuinely stopped. That cancel/restart semantic is honest about lifecycle and is why the `launchWhenX` family was deprecated."},{t:"p",text:"Compose equivalent: `collectAsStateWithLifecycle()` (uses `repeatOnLifecycle` internally) vs plain `collectAsState()` which keeps collecting in background. Pairing detail: `repeatOnLifecycle` + `stateIn(WhileSubscribed(5000))` compose perfectly — background for >5 s and the upstream stops too."}]},{level:"senior",q:"Deep-dive the one-shot event problem: compare SingleLiveEvent, SharedFlow, Channel, and state-based events, with their failure modes.",a:[{t:"list",items:["**SingleLiveEvent** — LiveData hack with an 'handled' flag. Only one observer gets the event, racy with multiple, relies on LiveData; legacy — name it only to dismiss it.","**`MutableSharedFlow(replay = 0)`** — hot with no buffer for absent collectors: an event emitted while the View is between `repeatOnLifecycle` windows (rotation! backgrounded!) is **silently dropped**. `tryEmit` with no buffer simply fails. Setting `replay = 1` fixes dropping but re-introduces stickiness — replayed after rotation. Fundamentally awkward for events.","**`Channel(BUFFERED).receiveAsFlow()`** — `send` suspends/buffers until a collector receives, so events **wait through collection gaps** — the usual pragmatic winner. Caveats: fan-out (each event goes to exactly *one* collector — a bug if two collectors are accidentally active); buffered events are lost on process death; and an event can be received *just* before the UI stops, then the Snackbar never shows.","**State-based (Google's guidance)** — the 'event' becomes a state field; the View executes it and calls a `consumed()` callback to clear it. Survives config change, and process death too if mirrored in `SavedStateHandle`; no delivery ambiguity because state is always there to re-render. Cost: consumption ceremony and modeling multiple queued events takes a list."]},{t:"p",text:"My default: state-based for anything that changes what the user sees or where they are (navigation, dialogs); Channel for genuine fire-and-forget side effects. The senior signal is explaining the **dropped-event window** — most candidates don't know SharedFlow silently drops."}]},{level:"senior",q:"MVVM vs MVI — real differences, and when would you pick MVI?",a:[{t:"p",text:"MVI is MVVM with three extra constraints: **a single immutable state object** per screen (MVVM allows several streams), **reified user intents** (sealed `Intent`/`Action` objects through one entry point, vs plain method calls), and **state mutation only through a reducer** — a pure `(state, action) -> state` function. That gives you an auditable event log, trivially testable reducers, and time-travel debugging; it costs boilerplate, indirection for trivial screens, and a learning curve."},{t:"p",text:"When I'd pick MVI: screens with genuinely complex state machines (editors, players, multi-step flows), or a team that wants the discipline enforced rather than by convention. Otherwise, modern MVVM — single `UiState` + `update {}` + UDF — already captures most of the benefit; frameworks like Orbit or MVIKotlin (popular in KMP with Decompose) formalize it when needed. Honest close: the industry converged on a middle ground, and Google's guidance is exactly that hybrid."}]},{level:"senior",q:"Your ViewModel has grown to 1500 lines. How do you fix a God ViewModel?",a:[{t:"list",items:["**Push logic down**: business rules move into use cases; data orchestration into repositories. The VM should mostly wire streams and map models — extracting logic usually halves it.","**Split by feature, not screen**: independent widgets on one screen (search bar, feed, stories row) can have their own state holders or even ViewModels; a screen-level VM composes them.","**Extract plain state-holder classes** for reusable UI logic that doesn't need VM lifetime, hoisted in Compose (`remember { ... }`).","**Extract mapping** into dedicated mapper/formatter classes with their own tests.","**Split the UiState** if it has become a dumping ground — several cohesive streams beat one 40-field object recomposing the world."]},{t:"p",text:"Guardrail I apply in review: if a ViewModel imports a data-source library (Retrofit/Room types) or exceeds a few hundred lines, the layering has broken down somewhere below it."}]},{level:"senior",q:"How do you test a ViewModel that uses viewModelScope, debounce, and stateIn(WhileSubscribed)? Include the gotchas.",a:[{t:"p",text:"Infrastructure first: `viewModelScope` runs on `Dispatchers.Main`, which doesn't exist on the JVM — a `MainDispatcherRule` calls `Dispatchers.setMain(TestDispatcher)`/`resetMain()`. Tests run in `runTest`, which gives **virtual time**."},{t:"list",items:["**Dispatcher choice**: `StandardTestDispatcher` queues coroutines until `advanceUntilIdle()`/`runCurrent()` — lets you assert intermediate states (`isLoading`). `UnconfinedTestDispatcher` runs eagerly — simpler, but transient states may be skipped before you can observe them.","**`stateIn(WhileSubscribed)` gotcha**: cold until collected. Asserting `uiState.value` right after constructing the VM only ever shows `initialValue` — you must collect (Turbine's `uiState.test { }`, or `backgroundScope.launch { uiState.collect() }`) to trigger the upstream.","**`debounce` / delays**: virtual time — `advanceTimeBy(300)` then assert; no real sleeping. Requires the debounce to run on the test scheduler, which leads to:","**Inject dispatchers** — a hardcoded `Dispatchers.IO` inside the VM or repository escapes virtual time and makes tests flaky/slow. Constructor-inject `CoroutineDispatcher` (or a dispatcher provider) and pass the TestDispatcher in tests.","Use **fakes over mocks** for repositories — behavioral fidelity, refactor-friendly; and **Turbine** for asserting emission sequences (`awaitItem()`, `expectNoEvents()`)."]}]},{level:"senior",q:"How do you share a ViewModel (or its equivalent) between Android and iOS in KMP, and what are the pain points?",a:[{t:"p",text:"Since androidx lifecycle 2.8, `ViewModel` + `viewModelScope` are multiplatform — the VM moves to `commonMain` exposing `StateFlow<UiState>`. Android and Compose Multiplatform consume it natively (`collectAsStateWithLifecycle`, `koinViewModel`). The friction is **native iOS**: Objective-C interop erases Flow's generics and has no suspend-stream concept, so SwiftUI can't just subscribe."},{t:"list",items:["**Bridging**: SKIE (converts `Flow` to Swift `AsyncSequence`, sealed classes to Swift enums — nicest DX), KMP-NativeCoroutines, or a hand-written closure-based `watch { }` wrapper. On the Swift side, an `ObservableObject`/`@Observable` adapter subscribes and republishes into SwiftUI.","**Lifecycle**: iOS has no config changes, so 'VM survives rotation' is moot — but ownership is manual: the Swift adapter must call `clear()`/cancel on deinit or the VM's coroutines leak. moko-mvvm and Decompose formalize this; Decompose in particular replaces ViewModel with lifecycle-aware 'components' shared across platforms.","**Main-thread delivery**: state driving SwiftUI must land on the main thread — keep VM emissions on `Dispatchers.Main` in shared code or hop in the adapter.","**Errors**: Kotlin exceptions crossing to Swift need `@Throws` or Result-typed state — uncaught Kotlin exceptions crash the app on the Swift side."]}]},{level:"senior",q:"How does SavedStateHandle.getStateFlow work with the rest of your state, and what are its limits?",a:[{t:"p",text:"`getStateFlow(key, initial)` returns a `StateFlow` backed by the handle: writing `savedStateHandle[key] = value` emits to the flow *and* marks the value for saved-state persistence — so a field both drives UI reactively and survives process death with one line. Typical pattern: persist raw inputs (query, selected filters) via handle-backed flows, `combine` them with repository flows, and `stateIn` the result; the heavy derived state is rebuilt on restore instead of persisted."},{t:"list",items:["Values must be Bundle-able (primitives, Parcelable, Serializable) — arbitrary domain objects need mapping or `SavedStateHandle.setSavedStateProvider` for custom serialization.","Size: part of the Binder-limited saved state (~1 MB total) — ids and inputs only, `TransactionTooLargeException` otherwise.","Saving happens at `onSaveInstanceState` time — mutations after the Activity is stopped may not be captured if the process dies immediately.","It's per-owner: a nav-graph-scoped VM's handle restores with the graph entry; also note `SavedStateHandle` in Compose Multiplatform arrived later than the VM itself — check versions in KMP."]}]},{level:"senior",q:"Should the ViewModel know about navigation? Discuss the approaches.",a:[{t:"list",items:["**VM emits navigation events** (Channel/state), View executes with NavController — most common; keeps NavController (a UI-layer object) out of the VM; testable by asserting emitted events. Downside: event-delivery caveats.","**Navigation as state** — a `destination` field in UiState (or Compose Navigation 3-style back stack derived from state); most robust across process death and deep links, aligns with state-driven navigation direction the ecosystem is moving in.","**Injecting a Navigator abstraction** into the VM — an interface the VM calls (`navigator.goToDetails(id)`), implemented over NavController. Reads nicely and mocks easily, but the VM now triggers UI actions imperatively and the navigator implementation must handle lifecycle (not firing while stopped).","**NavController directly in the VM — never**: it's tied to views/lifecycle; holding it in a VM is the leak/lifecycle bug the pattern forbids."]},{t:"p",text:"My answer: decisions about *whether/where* to navigate are presentation logic and belong in the VM; the *mechanics* belong in the View. Events or navigation-state both satisfy that — I lean state-based for anything that must survive process death."}]},{level:"senior",q:"How do you implement debounced search properly in a ViewModel?",a:[{t:"code",title:"The canonical reactive pipeline",code:`private val query = savedStateHandle.getStateFlow("query", "")

@OptIn(ExperimentalCoroutinesApi::class, FlowPreview::class)
val results: StateFlow<SearchUiState> = query
    .debounce(300)
    .distinctUntilChanged()
    .flatMapLatest { q ->
        if (q.isBlank()) flowOf(SearchUiState.Idle)
        else repository.search(q)
            .map<List<Item>, SearchUiState> { SearchUiState.Results(it) }
            .onStart { emit(SearchUiState.Searching) }
            .catch { emit(SearchUiState.Error(it.message)) }
    }
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), SearchUiState.Idle)

fun onQueryChange(q: String) { savedStateHandle["query"] = q }`},{t:"list",items:["`debounce(300)` — wait for typing to pause; `distinctUntilChanged` — skip no-op changes (StateFlow gives this for the query itself, but it matters post-debounce).","**`flatMapLatest`** is the load-bearing operator: a new query **cancels** the in-flight search, killing the classic race where a slow old response overwrites fresh results. (`flatMapConcat` would queue, `flatMapMerge` would race.)","`catch` *inside* the inner flow: an error kills only that search, not the whole pipeline — a `catch` on the outer flow would end results forever.","SavedStateHandle-backed query → search state survives process death; test with virtual time (`advanceTimeBy(300)`)."]}]},{level:"senior",q:"Error handling strategy in a ViewModel: exceptions, CoroutineExceptionHandler, Result types?",a:[{t:"list",items:["**Prefer errors as data**: repositories return sealed results (`Result`/`Either`, or emit error states in flows) so the VM handles failure as an expected branch and maps it into UiState. Exceptions escaping into `viewModelScope` mean a crash (in a `launch`, an uncaught exception hits the thread's handler → app crash; `SupervisorJob` only protects *siblings*).","**`runCatching`/try-catch at the call site** for suspend calls; but never blanket-catch `CancellationException` — swallowing it breaks structured cancellation (rethrow it, or catch specific exceptions).","**Flows**: `catch` operator placed to scope the failure correctly (inner flow for per-operation errors); `retry`/`retryWhen` for transient network errors with backoff.","**CoroutineExceptionHandler** installed into the scope is a *last-resort* logger/crash-prevention net, not a control-flow mechanism — by the time it fires the coroutine is dead.","Map technical errors to **semantic UI errors** in the VM (offline vs server vs validation) — the View shouldn't parse exception types."]}]},{level:"senior",q:"What's wrong with starting data loads in a ViewModel's init block? Is there a better pattern?",a:[{t:"p",text:"It's common and works, but know the trade-offs. Problems: work starts at *construction* — in tests the coroutine races your setup (especially with `UnconfinedTestDispatcher`) before you can install fakes' behavior or attach Turbine; with lazy `by viewModels()` the start time is 'whenever first accessed', which is implicit; retry-on-failure needs a separate public function anyway; and `init` work runs even if the UI never actually collects."},{t:"p",text:"The alternative: build state as a **cold chain started by collection** — repository flow + `onStart`/`flatMapLatest` + `stateIn(WhileSubscribed(5000))`. Loading begins when the UI subscribes, stops when it leaves, restarts on return, and tests control it by choosing when to collect. Google's samples largely moved to this 'lazy observable state' shape. `init` remains fine for cheap wiring or fire-once triggers — the senior answer is knowing *why* you'd choose each."}]},{level:"senior",q:"Compose specifics: collectAsState vs collectAsStateWithLifecycle, and should you pass the ViewModel down the tree?",a:[{t:"p",text:"`collectAsState()` collects in the composition's scope — it keeps collecting while the app is backgrounded (composition isn't disposed on ON_STOP). `collectAsStateWithLifecycle()` wraps collection in `repeatOnLifecycle(STARTED)` — collection stops in background and, combined with `WhileSubscribed(5000)`, the upstream stops too. Default to the lifecycle variant on Android; plain `collectAsState` is acceptable in previews/desktop CMP where the lifecycle concept differs."},{t:"p",text:"ViewModel placement: inject it at the **screen-level composable only**; children receive **state and lambdas** (`state: UiState, onRetry: () -> Unit`). Passing VMs deep kills `@Preview`s (they can't build VMs), couples reusable components to one screen's VM, and makes recomposition reasoning harder. Also know the scoping subtlety: `viewModel()`/`hiltViewModel()` scope to the nearest `ViewModelStoreOwner` — the nav back-stack entry — so the same call in two destinations returns different instances."}]},{level:"senior",q:"Two screens need to observe and mutate the same data. Shared ViewModel or something else?",a:[{t:"p",text:"Reach for the **data layer first**: make the repository the single source of truth exposing a hot/observed `Flow` (Room queries, an in-memory `MutableStateFlow` cache); each screen's ViewModel observes it independently and mutations go through repository functions. This survives any navigation topology, process recreation per-screen, and keeps ViewModels screen-scoped. A **shared ViewModel** (activity- or nav-graph-scoped) is the right tool for *transient flow state* that has no business in the data layer — wizard steps, in-progress form input across a checkout graph — where the nav-graph scope also gives automatic cleanup on flow exit."},{t:"p",text:"Anti-pattern to call out: an activity-scoped God ViewModel used as a message bus between unrelated screens — outlives its usefulness, accumulates state, and hides data-flow direction."}]},{level:"senior",q:"Where do memory leaks still happen in MVVM apps, and how do you find them?",a:[{t:"list",items:["**VM → UI references**: holding Context/View/Fragment/listener in the VM (including via lambda captures — an `onClick` stored in the VM capturing the Fragment).","**Wrong lifecycle owner in fragments**: observing/collecting with the Fragment's lifecycle instead of `viewLifecycleOwner` — view recreated on back-stack return, old bindings/adapters leak; same for keeping a `_binding` not nulled in `onDestroyView`.","**Un-cancelled collections**: collecting in `GlobalScope` or an application scope from UI code; callbacks registered on singletons and never unregistered (VM registering a listener on a singleton repository without removing it in `onCleared`).","**Anonymous inner classes / handlers** capturing the Activity outliving it.","**Static/companion caches** of UI objects."]},{t:"p",text:"Detection: **LeakCanary** in debug builds (watches destroyed activities/fragments/view models automatically), Android Studio Memory Profiler heap dumps (search retained Activity instances, follow the shortest GC-root path), and StrictMode for some cases. Process discipline: `onCleared` must undo everything `init` registered."}]},{level:"senior",q:"Critique MVVM: what are its genuine weaknesses, and when would you not use it?",a:[{t:"list",items:["**Under-specified**: MVVM says nothing about state shape, events, or the data layer — two 'MVVM apps' can look wildly different; teams need added conventions (UDF, single UiState) to stay consistent — which is effectively drifting toward MVI.","**The event problem is unsolved by the pattern itself** — one-shot delivery needs bolt-on idioms, each with failure modes.","**ViewModel as god-object gravity**: the VM is the easiest place to dump code, and the pattern has no built-in pressure against it.","**Overkill for trivial UI**: a static screen or a tiny utility app doesn't need the ceremony; and heavily server-driven UIs may fit a different shape entirely.","**Debugging indirection**: with many combined streams, answering 'why did this render?' means tracing reactive graphs — worse without discipline around naming and derived state."]},{t:"p",text:"When not: throwaway prototypes, screens with no state (pure display), or when the team is committed to a stricter framework (MVI/Redux-style, or Decompose components in KMP) where adding classic MVVM alongside would fragment the codebase. The mature position: MVVM-with-UDF is the pragmatic default on Android/KMP, applied with judgment rather than dogma."}]},{level:"senior",q:"What is AndroidViewModel and why is it discouraged?",a:[{t:"p",text:"`AndroidViewModel` is a ViewModel subclass that receives the `Application` in its constructor — the sanctioned way to have a context in a VM, safe from leaks because Application is a process-lifetime singleton. It's discouraged because needing a context in the VM usually signals **misplaced responsibility**: resource strings belong in the View or come via a resource-provider abstraction; file/database/preferences access belongs in the data layer. It also drags an Android dependency into the class — killing plain-JVM unit tests (Robolectric or instrumentation needed) and making the VM unshareable in KMP `commonMain`. With DI you can inject `@ApplicationContext` anyway, so the base class adds little; prefer injecting narrow abstractions instead."}]},{level:"senior",q:"Explain cold vs hot flows in the context of a ViewModel, and where each appears.",a:[{t:"p",text:"**Cold** flows (`flow { }`, Room/Retrofit-adapter flows, repository chains) run their producer *per collector*, starting on collection — each collector gets its own execution. **Hot** flows (`StateFlow`, `SharedFlow`, channels) exist and emit independently of any particular collector, multicasting to all of them."},{t:"list",items:["The typical VM is a **cold-to-hot converter**: cold repository chain → operators → `stateIn`/`shareIn` → hot state the UI observes. Without `stateIn`, two collectors of the same chain (or recollection after rotation) would re-execute the query — duplicate work, duplicate network calls.","`stateIn` = hot + latest-value cache + initial value (state); `shareIn` = hot multicast with configurable replay (streams). Both take a `SharingStarted` policy.","Interview edge: `StateFlow.collect` never completes (hot flows don't end) — so code *after* `collect { }` in the same coroutine is unreachable; each hot collection needs its own `launch`. Also `Flow.asLiveData()` exists for legacy bridges."]}]},{level:"senior",q:"How would you migrate a legacy MVP (or God-Activity) codebase to MVVM incrementally?",a:[{t:"list",items:["**Strangler approach, screen by screen** — never a big-bang rewrite; both patterns coexist behind the same repository layer.","**Step 0: carve out the data layer** — if Presenters/Activities call APIs directly, extract repositories first; this benefits both old and new code and is where most risk hides.","**Mechanical MVP→MVVM per screen**: the Presenter's logic moves into a ViewModel; every `view.showX(...)` call becomes a field in a `UiState` emitted through StateFlow; the View interface is deleted and the Activity/Fragment becomes a renderer of state. Attach/detach lifecycle code disappears (viewModelScope + repeatOnLifecycle replace it).","**Characterization tests first** on the Presenter where coverage is thin — then port; the new VM tests assert emitted states instead of verified view-mock calls (usually *simpler*).","**Prioritize** screens that are actively changing or crash-prone; leave stable dead screens for last (or forever). New features: MVVM only.","**Team conventions before scale**: agree the UiState/event/DI idioms on the first migrated screen and document it — otherwise you get N dialects of MVVM."]}]}],fx={id:"mvvm",title:"MVVM",tagline:"Model – View – ViewModel: the default presentation architecture for Android & KMP — internals, state, events, testing, and every interview angle.",tags:["Android","KMP","Jetpack ViewModel","StateFlow","UDF","Testing"],content:px,qa:mx},gx=[{heading:"What is MVI",blocks:[{t:"p",text:"**MVI (Model–View–Intent)** is a reactive, unidirectional architecture inspired by Cycle.js and Redux, brought to Android by Hannes Dorfmann (~2016). Despite the name, it's less about three components and more about a **cycle**: the View emits **Intents** (user intentions, not Android `Intent`s), a processing loop reduces them into a new immutable **Model** (state), and the View **renders** that state. One loop, one direction, no side doors."},{t:"list",items:["**Intent** — a reified user action or trigger: `data class QueryChanged(val q: String)`, `object Refresh`, `object RetryClicked`. Everything that can happen is a value.","**Model (State)** — a single immutable object describing the *entire* screen at a moment. Not the domain model — the rendered state.","**View** — `render(state)`: a pure mapping from state to UI, plus a stream of intents. In Compose this is literally the composable signature.","**Reducer** — pure function `(currentState, change) -> newState`. The only place state is produced.","**Side effects** — async work (network, DB) triggered by intents, whose *results* are fed back into the reducer as changes."]},{t:"note",text:'One-line definition for interviews: "MVI is MVVM with the screws tightened: exactly one immutable state, all inputs reified as intent objects through a single entry point, and all state transitions through a pure reducer — giving you an auditable, replayable state machine per screen."'}]},{heading:"The cycle in code — a hand-rolled MVI ViewModel",blocks:[{t:"p",text:"MVI on Android is usually *hosted inside* a Jetpack ViewModel (for lifecycle survival) — the patterns are complementary, not competing:"},{t:"code",title:"Contract: state, intents, one-shot effects",code:`data class SearchState(
    val query: String = "",
    val results: List<Item> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null,
)

sealed interface SearchIntent {
    data class QueryChanged(val query: String) : SearchIntent
    data object Retry : SearchIntent
    data class ItemClicked(val id: String) : SearchIntent
}

sealed interface SearchEffect {
    data class NavigateToDetail(val id: String) : SearchEffect
    data class ShowSnackbar(val message: String) : SearchEffect
}`},{t:"code",title:"The loop: single entry point, reducer, effects",code:`class SearchViewModel(
    private val repository: SearchRepository,
) : ViewModel() {

    private val _state = MutableStateFlow(SearchState())
    val state: StateFlow<SearchState> = _state.asStateFlow()

    private val _effects = Channel<SearchEffect>(Channel.BUFFERED)
    val effects: Flow<SearchEffect> = _effects.receiveAsFlow()

    private var searchJob: Job? = null

    // Single entry point — every user action funnels through here
    fun onIntent(intent: SearchIntent) {
        when (intent) {
            is SearchIntent.QueryChanged -> {
                reduce { it.copy(query = intent.query, isLoading = true) }
                search(intent.query)
            }
            is SearchIntent.Retry -> search(state.value.query)
            is SearchIntent.ItemClicked ->
                viewModelScope.launch {
                    _effects.send(SearchEffect.NavigateToDetail(intent.id))
                }
        }
    }

    private fun search(query: String) {
        searchJob?.cancel()
        searchJob = viewModelScope.launch {
            runCatching { repository.search(query) }
                .onSuccess { items ->
                    reduce { it.copy(isLoading = false, results = items, error = null) }
                }
                .onFailure { e ->
                    reduce { it.copy(isLoading = false, error = e.message) }
                }
        }
    }

    // All mutations go through one reducer helper — greppable, loggable
    private fun reduce(reducer: (SearchState) -> SearchState) {
        _state.update(reducer)
    }
}`},{t:"code",title:"View — render + intent stream (Compose)",code:`@Composable
fun SearchScreen(viewModel: SearchViewModel) {
    val state by viewModel.state.collectAsStateWithLifecycle()

    LaunchedEffect(Unit) {
        viewModel.effects.collect { effect ->
            when (effect) {
                is SearchEffect.NavigateToDetail -> { /* nav */ }
                is SearchEffect.ShowSnackbar -> { /* snackbar */ }
            }
        }
    }

    SearchContent(
        state = state,
        onIntent = viewModel::onIntent, // one lambda, not N callbacks
    )
}`},{t:"list",items:["The **single `onIntent` entry point** is what enables MVI's superpowers: log every intent and every state and you have a complete, replayable history of the screen.","The reducer here is the `copy` lambdas — strict MVI extracts them into a standalone pure function `reduce(state, change): State` that's trivially unit-tested with no coroutines at all.","Note it still uses `StateFlow` + `viewModelScope` — MVI on Android is a *discipline layered on* the MVVM toolkit."]}]},{heading:"State machine thinking & modeling rules",blocks:[{t:"list",items:["**Make illegal states unrepresentable**: if loading and error can't coexist, encode that (sealed sub-states) rather than trusting flag discipline.","**State is render-ready**: formatted strings, sorted lists, resolved flags — the View must not compute.","**Reducers are pure and synchronous**: no IO, no clocks, no randomness inside — those enter as *inputs* (changes/results). Purity is what makes them exhaustively testable.","**Long-running work is cancelled by newer intents** where it matters (`searchJob?.cancel()` / `flatMapLatest`) — the reducer alone doesn't protect you from stale-result races; effect management does.","**Time-travel & debugging**: because every transition is (state, change) → state, you can log the full sequence, reproduce bugs by replaying intents, and snapshot state in bug reports — the operational argument for MVI in large teams."]},{t:"note",text:'Classic probing question: "where do side effects go if reducers must be pure?" Answer: an effect-handling layer *around* the reducer (the intent handler / middleware / `intent { }` block in Orbit, Executors in MVIKotlin) performs IO and feeds results back in as plain data. Redux calls the same slot middleware/thunks.'}]},{heading:"One-shot effects in MVI",blocks:[{t:"p",text:"Purist MVI says *everything* is state — but navigation and snackbars fit awkwardly (the sticky-state problem, same as MVVM). In practice every MVI codebase picks one of:"},{t:"list",items:["**A separate effect stream** (Channel/SharedFlow) beside the state — pragmatic standard, used by Orbit (`postSideEffect`) and MVIKotlin (Labels). Keeps state clean; inherits the usual event-delivery caveats.","**Effects-as-state with consumption** — `pendingNavigation: Destination?` cleared by a `NavigationConsumed` intent; purist, replay-safe, more ceremony.","**One-off state flags with IDs** — event objects carrying unique ids so re-renders can dedupe; seen in Redux-family ports."]}]},{heading:"The MVI library landscape",blocks:[{t:"table",headers:["Library","Shape","Notes"],rows:[["**Orbit MVI**","DSL inside a ViewModel: `intent { reduce { } postSideEffect() }`","Gentlest adoption path — 'MVVM+' feel, KMP support, minimal boilerplate"],["**MVIKotlin** (Arkadii Ivanov)","Store/Executor/Reducer, framework-agnostic","Strictest separation; pairs with **Decompose** for KMP navigation/lifecycle; popular in serious KMP apps"],["**Mavericks** (Airbnb)","ViewModel + `Async<T>` state wrappers + `setState/withState`","Battle-tested at scale in the RecyclerView/Epoxy era; less momentum in pure-Compose codebases"],["**Molecule** (Cash App)","Compose runtime to *produce* StateFlow from composable logic","Not an MVI framework per se — a way to write the state-production loop declaratively"],["**Circuit** (Slack)","Compose-driven Presenter+UI pairs, state classes with embedded event sinks","A modern rethink: UDF architecture built entirely on Compose, KMP-ready"],["Hand-rolled","StateFlow + sealed intents + reduce helper","Perfectly viable; most 'MVI' production code is exactly this"]]}]},{heading:"MVI vs MVVM — honest trade-offs",blocks:[{t:"table",headers:["","MVVM (typical)","MVI (strict)"],rows:[["State","one or several observable streams","exactly one immutable state object"],["User input","N public functions on the VM","one entry point taking sealed intents"],["Mutation","anywhere inside the VM via update{}","only the reducer"],["Auditability","log manually per mutation","free: intent log + state log = full history"],["Boilerplate","lower","higher (intent classes, changes, wiring)"],["Learning curve","low","moderate; reducer/effect discipline must be taught"],["Risk profile","logic scattering as VM grows","ceremony on trivial screens; giant state objects recomposing everything"]]},{t:"list",items:["**Choose MVI** when screens are genuinely stateful machines (editors, players, checkout flows), when the team is large enough that convention-by-discipline fails, or in KMP where MVIKotlin+Decompose is a mature stack.","**Stay MVVM** for CRUD-ish screens and small teams — modern MVVM with a single `UiState` + `update {}` already delivers ~80% of MVI's value at ~50% of the ceremony.","**Performance edge case**: a single giant state object means every change recomposes/diffs everything observing it — mitigate by splitting screen state into cohesive sub-objects, using `derivedStateOf`/field-level selectors, and keeping lists stable (immutable collections, stable keys)."]},{t:"note",text:"The mature closing take: MVVM and MVI have converged. Google's guidance (single UiState, UDF, events-as-state) is MVI in all but name; the remaining difference is whether intents are reified and reduction is centralized — a *team-scaling* decision, not a correctness one."}]},{heading:"MVI in KMP",blocks:[{t:"list",items:["MVI is arguably **more popular in KMP than on pure Android**: a framework-free state machine in `commonMain` is the perfect shared brain, with each platform just rendering state and sending intents.","**MVIKotlin + Decompose** is the flagship stack: Decompose provides lifecycle-aware, navigable 'components' (BLoC-style) shared across Android/iOS/desktop/web; MVIKotlin provides the Store loop inside them.","**Orbit** works in KMP too, hosted in the multiplatform androidx ViewModel.","iOS consumption is the same story as MVVM: bridge the state `StateFlow`/`Value` to SwiftUI via SKIE/adapters; intents are plain function calls into shared code, which Swift handles natively.","Sealed intent/state hierarchies crossing to Swift benefit hugely from **SKIE** (exhaustive Swift enums instead of class-check chains)."]}]}],yx=[{level:"junior",q:"What is MVI and how is the data flow different from MVVM?",a:[{t:"p",text:"MVI (Model–View–Intent) is a unidirectional cycle: the View emits **Intents** (reified user actions like `Refresh` or `QueryChanged`), a loop processes them and a pure **reducer** produces a new immutable **State**, and the View renders that state. Versus MVVM: instead of N public ViewModel functions there's **one entry point** taking sealed intent objects; instead of possibly several streams there's **exactly one state object**; and state may only change **through the reducer**. Same observation mechanics (StateFlow), stricter rules on top."}]},{level:"junior",q:"Is an MVI 'Intent' the same as an Android Intent?",a:[{t:"p",text:"No — unfortunate name collision. An MVI intent is a plain value expressing a *user's intention*: `data class ItemClicked(val id: String)`, `object Retry` — usually a sealed interface per screen. Android's `Intent` is the OS-level messaging object for launching activities/services. Zero relation; interviewers ask exactly to check you know the difference."}]},{level:"junior",q:"What is a reducer and why must it be pure?",a:[{t:"p",text:"A reducer is a function `(currentState, change) -> newState` — the only place a new state is produced. Pure means: no IO, no time, no randomness, no mutation — output depends only on inputs. Why it matters: pure reducers are **exhaustively unit-testable** (plain function calls, no coroutines or mocks), **deterministic** (same intent log always replays to the same state — the basis of time-travel debugging), and **safe to reason about** (all transition logic is in one greppable place). Async work happens *around* the reducer and feeds results back in as data."}]},{level:"junior",q:"What does 'single source of truth' mean in MVI?",a:[{t:"p",text:"The screen has exactly **one state object**, and everything the UI shows derives from it. There's no second place where the checkbox's value or the list's loading flag lives — no view-held state drifting out of sync, no 'the widget says X but the model says Y' bugs. Practical consequences: `render(state)` can redraw the whole screen from scratch at any time (which is literally what Compose does), state can be snapshotted for bug reports, and restoring the screen equals restoring one object."}]},{level:"junior",q:"Does MVI replace the Jetpack ViewModel?",a:[{t:"p",text:"No — they're complementary. On Android, the MVI loop (state, intents, reducer) almost always lives **inside** a Jetpack ViewModel, which contributes what MVI itself doesn't address: surviving configuration changes, `viewModelScope`, and `SavedStateHandle`. MVI is a *discipline about how state changes*; ViewModel is *infrastructure about where state lives*. (In KMP, alternatives like Decompose components can play the infrastructure role instead.)"}]},{level:"junior",q:"What are the practical benefits of funneling everything through one onIntent() entry point?",a:[{t:"list",items:["**Complete audit log**: log one function and you've captured every user action — attach the intent+state history to crash reports and bugs become replayable.","**One lambda down the Compose tree** (`onIntent = vm::onIntent`) instead of a growing bundle of callbacks.","**Cross-cutting policies in one place**: throttling double-clicks, analytics, permission gates — wrap the funnel once.","**Exhaustiveness**: a sealed intent + `when` forces handling every action; adding an intent without handling it fails compilation."]}]},{level:"junior",q:"How does the View send user actions in MVI, and what does render(state) mean?",a:[{t:"p",text:"The View converts raw UI events into intent objects and passes them to the single entry point — `onClick = { onIntent(SearchIntent.Retry) }`. `render(state)` is the other half of the contract: a function that maps the state object to the screen with **no decisions of its own** — every `if` in render should be reading a state field, not computing policy. In Compose, the composable *is* `render`: it takes `state` and `onIntent` as parameters, which also makes it trivially previewable with fake states."}]},{level:"senior",q:"If reducers must be pure, walk me through exactly how a network call flows through an MVI loop.",a:[{t:"list",items:["1. View emits `Intent.Refresh` → entry point.","2. The **effect/executor layer** (Orbit's `intent {}` block, MVIKotlin's Executor, hand-rolled handler) — *not* the reducer — receives it, immediately dispatches a synchronous change `Change.Loading` through the reducer (state now shows loading), and launches the coroutine.","3. The network call runs in the effect layer; its outcome is converted into plain data: `Change.Loaded(items)` or `Change.Failed(error)`.","4. That change is dispatched through the reducer → new state → View re-renders. The reducer only ever saw values."]},{t:"p",text:"Follow-ups to preempt: cancellation lives in the effect layer (`flatMapLatest` / job tracking per intent type — a new Refresh cancels the old one); stale-result races are prevented there too, or defensively in the reducer by ignoring results that don't match current input; and this layered shape is exactly Redux middleware / Elm commands — naming that shows you see the family resemblance."}]},{level:"senior",q:"How do you test an MVI screen? What becomes easier and what becomes harder than MVVM?",a:[{t:"list",items:["**Reducer tests** — the payoff: pure function, table-driven tests over (state, change) pairs, no coroutines/mocks/dispatchers. Edge transitions (error while loading, retry after error) become one-line cases.","**Loop/integration tests** — same machinery as MVVM: TestDispatcher + Turbine, send intents, assert the emitted state sequence; assert effects on the effect stream separately.","**Libraries ship test DSLs**: Orbit's test module asserts posted states/side effects; MVIKotlin stores are testable without any framework."]},{t:"p",text:"Easier: transition logic coverage, exhaustiveness (sealed classes force handling), reproducing bugs by replaying an intent sequence. Harder/more verbose: simple flows now need intent + change + reducer wiring for what MVVM did in one function; and asynchronous effect-ordering tests (two competing intents cancelling each other) remain exactly as tricky as in MVVM — MVI doesn't magically solve concurrency, it just localizes it."}]},{level:"senior",q:"A single giant state object recomposes the whole screen on every keystroke. How do you fix MVI's performance problem?",a:[{t:"list",items:["**Split the screen state into cohesive sub-objects** (`SearchBarState`, `ResultsState`) and pass only the slice each composable needs — Compose skips recomposition when an unchanged stable slice is passed.","**Keep the state stable in Compose's sense**: immutable collections (kotlinx.collections.immutable or `@Immutable` wrappers), no `List` that Compose must treat as unstable, stable data classes — otherwise skipping never happens regardless of splitting.","**Selectors / field-level flows**: expose `state.map { it.subState }.distinctUntilChanged()` (or `derivedStateOf` in the UI) so consumers observe only their slice.","**Don't put high-frequency transient values through the full loop**: text-field composition state or drag positions can stay local UI state, entering the loop debounced/committed — running an entire reduce cycle per keystroke is the actual smell.","**Measure first**: Layout Inspector recomposition counts / composition tracing — most 'MVI is slow' reports are unstable-collection bugs, not the architecture."]}]},{level:"senior",q:"Compare Orbit, MVIKotlin, and hand-rolled MVI — when would you pick each?",a:[{t:"list",items:["**Hand-rolled** (StateFlow + sealed intents + reduce helper): zero dependency, team learns the mechanics, fully sufficient for a handful of screens. Risk: N developers → N dialects; no test DSL; discipline decays without review pressure.","**Orbit**: thin DSL over exactly that (`intent { reduce { } postSideEffect() }`) hosted in a ViewModel — lowest-friction formalization, KMP-ready, good test support. Pick when you want MVVM-adjacent ergonomics with enforced structure.","**MVIKotlin**: full Store/Executor/Reducer separation, framework-independent, with time-travel tooling; pairs with Decompose for lifecycle/navigation. Pick for serious KMP (shared brain across Android/iOS/desktop) or when you want the strictest, most testable separation and accept the ceremony."]},{t:"p",text:"Also worth naming: **Mavericks** (mature, Epoxy-era, less Compose-native momentum) and **Circuit** (Slack's Compose-first rethink). Decision axis in one line: how much enforced structure does the team's size and platform spread justify?"}]},{level:"senior",q:"How does MVI handle process death? Does the intent log help?",a:[{t:"p",text:"The intent log is an in-memory debugging artifact — it dies with the process, and replaying a persisted log on restore is impractical (effects would re-fire network calls; the world has changed). Real answer: same discipline as MVVM, but MVI's single state object makes it cleaner to execute. Persist the **minimal seed** — ids, user input, scroll anchors — into `SavedStateHandle` (some teams make the state class itself contain a small `@Parcelize` 'persistable core'), and rebuild the rest by re-running the normal load path on restore. The reducer helps here: restoration is just an ordinary `Change.Restored(seed)` flowing through the same loop, not a special code path. What you must *not* do is parcel the whole state (TransactionTooLarge risk with lists) or treat replay-from-log as a persistence strategy."}]},{level:"senior",q:"Steelman the case AGAINST adopting MVI on a team currently happy with MVVM.",a:[{t:"list",items:["**The delta has shrunk**: MVVM done to Google's current guidance (single UiState, update{}, UDF, events-as-state) already yields most of MVI's benefits — the migration buys reified intents and a centralized reducer, which mainly pay off at large team scale.","**Ceremony tax on simple screens**: a settings toggle becomes intent class + change + reducer branch + wiring; developers respond by smuggling shortcuts, and inconsistent-MVI is worse than consistent-MVVM.","**Migration cost is real**: retraining, rewriting stable screens, a long bilingual period, and test-suite conversion — spent on screens that weren't causing bugs.","**It doesn't solve the hard problems**: one-shot events, process death, and async races persist in MVI (just relocated); anyone selling MVI as the fix for those is overpromising.","**Framework lock-in risk** if a library (with its DSL and test tooling) is adopted, versus discipline that's harder to enforce if hand-rolled — you pay one of those two costs."]},{t:"p",text:"Then the balanced close: I'd still reach for MVI selectively — the genuinely state-machine-like screens — and for KMP shared logic, where MVIKotlin/Decompose maturity changes the calculus. Showing you can argue both sides *is* the senior signal on this question."}]},{level:"senior",q:"Explain how MVI relates to Redux, Elm, and Flux — and why that lineage matters.",a:[{t:"p",text:"Direct family line: **Flux** (Facebook) introduced unidirectional dispatch; **Elm** formalized the pure `update : Msg -> Model -> Model` loop with effects as data (Commands); **Redux** popularized single-store + pure reducers + middleware in JS; **Cycle.js** framed UI as a stream cycle — and Hannes Dorfmann's MVI translated that stack of ideas to Android's Rx world. The mapping is one-to-one: Intent = Action/Msg, State = Store/Model, Reducer = reducer/update, effect layer = middleware/Commands, render = view function."},{t:"p",text:"Why it matters beyond trivia: the lineage tells you MVI's properties are *inherited theorems, not marketing* — determinism, replay, time-travel come from reducer purity (Elm proved the model); the middleware slot is where every family member handles IO, so you know where effects belong in any MVI library you meet; and the known failure modes (boilerplate fatigue, giant single stores) played out in the JS ecosystem first, so their mitigations (state slicing, selectors — reselect in Redux, field-flows in MVI) are already mapped. Recognizing a 'new' architecture as an instance of a family is exactly the judgment senior interviews probe for."}]}],wx={id:"mvi",title:"MVI",tagline:"Model – View – Intent: single immutable state, reified intents, pure reducers, side-effect handling, the library landscape, and when the discipline pays off.",tags:["Android","KMP","UDF","Reducer","Orbit","MVIKotlin"],content:gx,qa:yx},vx=[{heading:"The original Clean Architecture — circles and the Dependency Rule",blocks:[{t:"p",text:"**Clean Architecture** is Robert C. Martin's (Uncle Bob, 2012) synthesis of earlier layered ideas (Hexagonal/Ports & Adapters, Onion Architecture). Concentric circles: **Entities** (enterprise business rules) at the core, then **Use Cases** (application business rules), then **Interface Adapters** (presenters, controllers, gateways), then **Frameworks & Drivers** (UI, DB, network, devices) outermost."},{t:"p",text:"Everything hangs on one law — the **Dependency Rule**: *source-code dependencies may only point inward*. Inner circles know nothing about outer ones — not names, not types, not frameworks. The result: business rules that can be compiled, tested, and reasoned about with no UI, no database, no Android — and outer details (Room vs SQLDelight, Retrofit vs Ktor) that are swappable without touching the core."},{t:"list",items:["When the inner layer must *call outward* (a use case needs data), it defines an **interface (port)** that an outer layer implements (adapter) — that's **Dependency Inversion (the D in SOLID)** doing the load-bearing work.","Data crossing boundaries is converted at each boundary — inner layers never see outer-layer types (no Retrofit DTOs inside use cases).",'"Clean" is about **dependency direction**, not folder names — a project with domain/data/presentation folders where domain imports Retrofit is not Clean Architecture.']},{t:"note",text:'One-liner: "Clean Architecture = business logic at the center, frameworks at the edges, all dependencies pointing inward, with interfaces + dependency inversion wherever the flow of control must go outward."'}]},{heading:"The Android adaptation — three layers, and Google's pragmatic version",blocks:[{t:"p",text:"Android collapsed the circles into three layers. Know **both** the strict mapping and Google's official recommendation — and that they differ:"},{t:"list",items:["**Presentation (UI) layer** — Activities/Composables + ViewModels. Maps to Interface Adapters + Frameworks.","**Domain layer** — use cases + domain models + repository *interfaces*. Pure Kotlin, zero Android imports. Maps to Entities + Use Cases.","**Data layer** — repository *implementations* + data sources (Retrofit, Room, DataStore) + DTOs/entities + mappers. Outer circle."]},{t:"table",headers:["","Strict Clean (community)","Google's recommended architecture"],rows:[["Domain layer","mandatory core of the design","**optional** — 'add if complexity warrants'"],["Repository interface lives in","domain layer (dependency inversion: data implements domain's port)","data layer defines and exposes it"],["Use cases","every interaction goes through one","only when logic is complex or reused"],["Data flow","UI → UseCase → Repository","UI → Repository directly is fine"],["Philosophy","framework independence above all","pragmatism: SSOT, UDF, layered — without ceremony"]]},{t:"note",text:`High-signal interview move: explicitly name this divergence. "Strict Clean puts repository interfaces in domain and mandates use cases; Google's guide makes domain optional and lets ViewModels call repositories. I choose per project: shared-KMP or complex business rules → strict; CRUD app → Google-pragmatic."`}]},{heading:"Domain layer deep dive — entities, use cases, and when they're worth it",blocks:[{t:"h3",text:"Domain models (entities)"},{t:"list",items:["Plain immutable Kotlin data classes expressing business concepts — `Order`, `Money`, `Subscription` — with invariants enforced in construction (value classes for `UserId`, no raw strings for money).","No serialization annotations, no Room annotations, no framework anything — those belong to the layer-specific models (DTO/entity).","Business rules that belong to the concept live on it (`Order.canBeCancelled`), not in a ViewModel."]},{t:"h3",text:"Use cases (interactors)"},{t:"code",title:"Use case conventions",code:`// Naming: VerbNounUseCase; single public operator invoke
class GetPersonalizedFeedUseCase(
    private val feedRepository: FeedRepository,       // domain interfaces
    private val userRepository: UserRepository,
    private val defaultDispatcher: CoroutineDispatcher, // injected, main-safe
) {
    suspend operator fun invoke(): Result<Feed> = withContext(defaultDispatcher) {
        val user = userRepository.getUser()
        feedRepository.getFeed()
            .map { feed -> feed.rankFor(user.interests) } // the actual business value
    }
}

// Flow-returning variant for observation:
class ObserveCartTotalUseCase(
    private val cartRepository: CartRepository,
) {
    operator fun invoke(): Flow<Money> =
        cartRepository.observeItems()
            .map { items -> items.sumOf { it.price * it.quantity } }
}`},{t:"list",items:["**Earn their keep when**: logic combines multiple repositories, is reused by several ViewModels, or encodes real business policy (ranking, pricing, eligibility).","**Pure ceremony when**: `class GetUserUseCase(repo) { operator fun invoke(id) = repo.getUser(id) }` — a rename of the repository call. Teams mandating these for uniformity trade boilerplate for consistency; know the argument on both sides.","Use cases are **stateless** and reusable; they may call other use cases; they should be main-safe (own their `withContext`).","They do **not** know about UI state, navigation, or Android — a use case returning `UiState` is a layering bug."]}]},{heading:"Data layer deep dive — repositories, data sources, SSOT, offline-first",blocks:[{t:"list",items:["**Repository** = the public API of the data layer for one domain concept: hides *where* data comes from, exposes suspend functions + Flows of **domain models**, owns the merge strategy between sources.","**Data sources** = one class per origin (RemoteDataSource wrapping Retrofit/Ktor, LocalDataSource wrapping Room/SQLDelight DAO); repositories orchestrate them, never the reverse; data sources never know each other.","**Single Source of Truth**: pick one source (usually the DB) that the app *reads from*; network writes into it, UI observes it. Reads are then consistent, offline works, and the 'flash of stale then fresh' is handled uniformly.","**Offline-first**: reads always serve local; writes go local-first then sync (WorkManager, outbox pattern); conflict policy (last-write-wins vs merge) is a repository concern.","Repositories also own: in-memory caching, request de-duplication, pagination glue (Paging sources), and mapping DTO/entity → domain."]},{t:"code",title:"Repository implementing SSOT (network → DB → UI)",code:`class ArticleRepositoryImpl(
    private val remote: ArticleRemoteDataSource,
    private val local: ArticleLocalDataSource,
    private val ioDispatcher: CoroutineDispatcher,
) : ArticleRepository {

    // Reads: always from the local SSOT
    override fun observeArticles(): Flow<List<Article>> =
        local.observeAll().map { entities -> entities.map { it.toDomain() } }

    // Refresh: network writes INTO the SSOT; UI updates via the observation above
    override suspend fun refresh(): Result<Unit> = withContext(ioDispatcher) {
        runCatching {
            val dtos = remote.fetchArticles()
            local.replaceAll(dtos.map { it.toEntity() })
        }
    }
}`}]},{heading:"Model mapping — DTO vs entity vs domain vs UI model",blocks:[{t:"table",headers:["Model","Layer","Shaped by","Example concern"],rows:[["DTO","data (network)","the API contract",'`@SerialName("user_name")`, nullable everything'],["DB entity","data (local)","storage & queries","`@Entity`, indices, foreign keys"],["Domain model","domain","business meaning","non-null invariants, value classes, behavior"],["UI model / UiState","presentation","what the screen renders","formatted strings, resolved flags, stable keys"]]},{t:"list",items:["Mapping happens **at layer boundaries**: DTO→domain and entity→domain inside the data layer; domain→UI in the ViewModel (or a mapper it uses).","Why bother: a server field rename touches the DTO + one mapper — not forty composables. Nullability gets resolved *once*, at the boundary, so the domain stays honest.","Pragmatic seniority: for a tiny app, one model reused everywhere is a defensible YAGNI call — but say you'd split the moment API shape and UI needs diverge, or the DB schema needs to evolve independently.","Mappers are plain functions/extension functions (`fun ArticleDto.toDomain(): Article`) — trivially unit-tested, and the place where defaulting/validation of dirty server data lives."]}]},{heading:"A full vertical slice — every layer in one example",blocks:[{t:"code",title:"Domain layer (pure Kotlin — no Android, no libraries)",code:`// model
data class Article(val id: String, val title: String, val publishedAt: Instant)

// repository PORT — interface owned by domain (strict Clean)
interface ArticleRepository {
    fun observeArticles(): Flow<List<Article>>
    suspend fun refresh(): Result<Unit>
}

// use case
class ObserveSortedArticlesUseCase(
    private val repository: ArticleRepository,
) {
    operator fun invoke(): Flow<List<Article>> =
        repository.observeArticles()
            .map { articles -> articles.sortedByDescending { it.publishedAt } }
}`},{t:"code",title:"Data layer (implements the domain port)",code:`class ArticleRepositoryImpl(
    private val remote: ArticleRemoteDataSource,
    private val local: ArticleLocalDataSource,
    private val ioDispatcher: CoroutineDispatcher,
) : ArticleRepository { /* SSOT impl from the previous section */ }`},{t:"code",title:"Presentation layer",code:`class FeedViewModel(
    observeSortedArticles: ObserveSortedArticlesUseCase,
    private val refreshArticles: RefreshArticlesUseCase,
) : ViewModel() {

    val uiState: StateFlow<FeedUiState> = observeSortedArticles()
        .map { articles -> FeedUiState.Content(articles.map { it.toUiModel() }) }
        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), FeedUiState.Loading)

    fun onPullToRefresh() {
        viewModelScope.launch { refreshArticles() }
    }
}`},{t:"p",text:"Trace the Dependency Rule through it: presentation imports domain; data imports domain; **domain imports nothing** from either. Control flows UI→domain→data at runtime, but the *source dependency* from domain to data is inverted through the `ArticleRepository` interface — that inversion is the exam-question heart of Clean Architecture."}]},{heading:"Error handling across layers",blocks:[{t:"list",items:["**Data layer**: catch transport-level garbage (IOException, HTTP codes, serialization) at the boundary and translate to **typed domain errors** — `sealed interface DataError { NoConnection; NotFound; Unauthorized; Unknown }` — via `Result`/Either or error-emitting Flows. Never let Retrofit's `HttpException` leak upward.","**Domain layer**: business failures are *values*, not exceptions — `InsufficientFunds`, `CartExpired` as sealed results; exceptions are reserved for bugs.","**Presentation**: map typed errors to UX — retry affordance for connectivity, sign-in flow for auth, toast for the rest. The ViewModel `when`s over the sealed error, never string-matches messages.","**Flows**: remember `catch` only handles *upstream* exceptions, terminal failures kill the collection — repositories should catch inside and emit error values so long-lived observation streams survive individual failures.","**CancellationException must always be rethrown** — a blanket `runCatching` in every layer that swallows it silently breaks structured concurrency; use a helper that rethrows it."]}]},{heading:"Threading rules across layers",blocks:[{t:"list",items:["**Main-safety contract**: every suspend function in repositories/use cases must be safe to call from the main thread — the layer *doing* blocking work wraps itself (`withContext(ioDispatcher)`); callers never pre-emptively hop dispatchers.","**Inject dispatchers** everywhere (`ioDispatcher`, `defaultDispatcher` as constructor params or a DispatcherProvider) — testability + a single place to tune.","Room/Retrofit suspend calls are already main-safe (they manage their own threading) — wrapping them in `withContext(IO)` again is harmless but cargo-cult; know that nuance.","`Flow` work moves off-main with `flowOn` applied in the layer that owns the heavy operator, not by the collector.","CPU-heavy mapping of big lists → `defaultDispatcher`, IO → `ioDispatcher`; the UI layer stays on `Main.immediate` via `viewModelScope`."]}]},{heading:"SOLID mapped to Android Clean Architecture",blocks:[{t:"table",headers:["Principle","Where it shows up in this architecture"],rows:[["**S**ingle Responsibility","one use case = one business action; repository per concept; ViewModel per screen; the anti-God-class principle"],["**O**pen/Closed","sealed hierarchies for state/errors extend by adding variants; new data sources plug in behind the repository without touching consumers"],["**L**iskov Substitution","fake repositories in tests must honor the interface contract (same semantics for edge inputs) — if tests need to know which impl is behind the port, LSP is broken"],["**I**nterface Segregation","small per-concept repository interfaces instead of one `AppRepository` with 40 methods; ViewModels depend only on the slice they use"],["**D**ependency Inversion","the load-bearing one: domain defines repository ports, data implements them; high-level policy never depends on low-level detail"]]}]},{heading:"Clean Architecture in KMP",blocks:[{t:"list",items:["Clean's layering maps perfectly onto KMP: **domain + data in `commonMain`**, presentation shared (CMP ViewModels) or per-platform (SwiftUI adapters over shared state).","The domain layer's 'no framework imports' rule becomes *enforced by compilation*: `commonMain` literally cannot import Android classes — KMP is Clean Architecture's dependency rule with a compiler behind it.","Data layer in common code: **Ktor** (network), **SQLDelight/Room-KMP** (DB), **multiplatform DataStore** (prefs); platform specifics (file paths, drivers, engines) injected via `expect/actual` or DI.","The strict-Clean choice (repository interfaces in domain) pays off doubly here: platforms and tests substitute implementations without touching shared business logic.","Typical module shape: `:shared:domain`, `:shared:data`, `:shared:feature-x` consumed by `:androidApp` and the iOS framework — which is where Clean Architecture meets modularization."]}]},{heading:"Criticisms & pragmatism — the question that separates seniors",blocks:[{t:"list",items:["**Over-engineering small apps**: five modules, four model types and pass-through use cases for a two-screen app is architecture cosplay — the cost (navigation overhead reading code, mapper churn) is real and the payoff absent.","**Pass-through layers**: wrappers that only forward calls add nothing but stack frames; add a layer when it has *responsibility*, not for symmetry.","**Dogma vs delivery**: 'framework independence' is worth little if you'll never actually swap Room — the real, recurring payoff is **testability and team-scale boundaries**, so argue from those.","**Uncle Bob's version isn't gospel on mobile**: Google's guide deliberately deviates (optional domain, repo interfaces in data) — citing that shows you read primary sources, not blog cargo cult.","**When to invest**: multiple developers, KMP sharing, complex/regulated business rules, long lifespan → strict layering compounds. Hackathon/MVP → ViewModel + repository and ship."]},{t:"note",text:'Best closing sentence in a Clean Architecture interview: "Layers are a means; the ends are testable business logic, independent teams, and replaceable details. I add exactly as much architecture as buys those — and no more."'}]}],bx=[{level:"junior",q:"What is Clean Architecture and what is the Dependency Rule?",a:[{t:"p",text:"Clean Architecture (Uncle Bob, 2012) organizes code in concentric layers — business entities and use cases at the center, interface adapters around them, frameworks (UI, DB, network) at the edge. The **Dependency Rule** is its single law: source-code dependencies may only point **inward** — inner layers never know about outer ones. On Android that means the domain layer has zero Android/library imports, and things like Retrofit or Room are outer details the core never names. The payoff: business logic that's testable without an emulator and details you can swap without touching the core."}]},{level:"junior",q:"Describe the three layers of a typical Android Clean Architecture app.",a:[{t:"list",items:["**Presentation/UI**: Activities/Composables render state; ViewModels hold UI state and call the layer below.","**Domain**: pure Kotlin — domain models, use cases, and (in strict Clean) repository *interfaces*. No Android imports.","**Data**: repository *implementations*, remote/local data sources (Retrofit/Ktor, Room/SQLDelight), DTOs and DB entities, and mappers to domain models."]},{t:"p",text:"Dependencies: presentation → domain ← data. Both outer layers depend on domain; domain depends on nothing — the data layer's arrow points inward because it *implements* interfaces the domain defines."}]},{level:"junior",q:"What is a use case (interactor) and when should you create one?",a:[{t:"p",text:"A class encapsulating **one business action** — `GetPersonalizedFeedUseCase` — conventionally with a single `operator fun invoke()`, stateless, main-safe, depending on repository interfaces. Create one when logic **combines multiple repositories**, is **reused across ViewModels**, or encodes **real business policy** (ranking, eligibility, pricing). A use case that only forwards `repo.getUser(id)` adds nothing but a file — some teams still mandate them for uniformity and future-proofing; be able to argue both sides and note Google's guidance makes the whole domain layer optional."}]},{level:"junior",q:"What is the Repository pattern and what problems does it solve?",a:[{t:"p",text:"A repository is the single entry point for one kind of data — it hides *where* data lives (network, DB, cache), decides the merge/refresh strategy, and exposes clean domain types via suspend functions and Flows. Problems solved: ViewModels no longer know about Retrofit/Room (swap or add sources freely), caching and offline logic exist **once** instead of per-screen, tests substitute an in-memory fake behind the interface, and the app gets a **single source of truth** so two screens can't disagree about the same data."}]},{level:"junior",q:"Why does the domain layer have no Android dependencies?",a:[{t:"p",text:"Three concrete wins. **Testability**: pure Kotlin runs as plain JVM unit tests — fast, no Robolectric/emulator. **Portability**: an Android-free domain drops into KMP `commonMain` unchanged — the iOS app runs the same business rules. **Stability**: Android APIs, UI toolkits and libraries churn constantly; business rules shouldn't have to change when the framework does. The Dependency Rule is what protects all three — one `import android.*` in domain and you've lost them."}]},{level:"junior",q:"What is the difference between a DTO, a database entity, and a domain model? Why not one class for all?",a:[{t:"p",text:"A **DTO** mirrors the API contract (serialization names, everything nullable because servers lie); a **DB entity** mirrors storage (Room annotations, indices, foreign keys); a **domain model** mirrors business meaning (non-null invariants, value types, behavior). One class serving all three masters means a server field rename touches your database schema and your composables, Room annotations leak into business code, and nullability from the network infects every layer. Mapping at boundaries (`ArticleDto.toDomain()`) contains each concern. Honest caveat worth adding: in a tiny app, one model is a defensible YAGNI shortcut — split when shapes start diverging."}]},{level:"junior",q:"What does 'Single Source of Truth' mean in the data layer?",a:[{t:"p",text:"One designated place — usually the local database — is what the app *reads*; everything else feeds it. The network never returns data straight to the UI: a refresh **writes into the DB**, and the UI updates because it *observes* the DB (Room/SQLDelight Flows). Benefits: every screen shows consistent data, offline reads work automatically, and there's exactly one merge point for remote and local changes. This is the pattern behind 'observe local, refresh remote' repositories."}]},{level:"junior",q:"How does dependency injection relate to Clean Architecture?",a:[{t:"p",text:"Clean Architecture *requires* construction to happen somewhere outside the layers: the ViewModel needs a use case, the use case needs an `ArticleRepository`, and the actual `ArticleRepositoryImpl` lives in a layer the domain must not know. A DI container (Hilt/Koin) is that assembly point — the composition root binds interface → implementation (`@Binds ArticleRepositoryImpl into ArticleRepository`), so every layer receives its dependencies as abstractions through constructors. Side benefits: swapping fakes in tests is a binding change, and scoping (singleton repository, screen-scoped ViewModel) is declared in one place. DI isn't *part* of Clean Architecture — it's the mechanism that makes the Dependency Rule practical."}]},{level:"senior",q:"Where should repository interfaces live — domain or data layer? Argue it properly.",a:[{t:"p",text:"**Strict Clean: in domain.** The interface is the *port* the domain defines for what it needs; data implements it. This is dependency inversion doing its job — domain compiles standalone, use cases are testable against the port, and in KMP/multi-module setups the domain module genuinely doesn't depend on the data module. **Google's guide: in data.** Their reasoning: most apps don't swap implementations, the domain layer is optional in their architecture, and one interface + one impl in different modules is ceremony for typical apps — so the data layer just exposes its API."},{t:"p",text:"My resolution: it's decided by **who needs the seam**. If a domain layer exists and use cases must be testable/portable without data-layer code (KMP especially), the interface belongs to domain — that's the whole point of the port. If ViewModels call repositories directly and there's no domain module, an interface in data (or even a concrete class with a fake for tests) is honest. The failure mode to name: interface-in-domain but *shaped exactly like the impl* (leaking DTO-ish types, one method per endpoint) — that's inversion theater; the port should be expressed in domain vocabulary."}]},{level:"senior",q:"Your team writes a pass-through use case for every repository method 'for consistency'. Defend or attack this policy.",a:[{t:"p",text:"**Attack**: pass-throughs violate the reason use cases exist — they add a class, a file, DI wiring and a stack frame while encoding zero business knowledge. Cost is real: navigation overhead reading code, slower onboarding ('is there logic in here? no, never'), and diluted signal — when every call has a use case, the ones containing actual policy don't stand out. YAGNI says add the class when logic appears; refactoring a direct repository call into a use case later is a five-minute, tooling-assisted change."},{t:"p",text:"**Steelman before concluding**: uniformity has value at scale — 'ViewModels never touch repositories' is a rule a lint check can enforce without judgment calls, it pre-creates the seam so adding cross-cutting logic (analytics, authorization) later touches one layer, and juniors can't pick the wrong dependency. Verdict I'd give: for a large org with codegen/lint enforcement, mandatory use cases are a defensible *convention* tax; for a small team, rule-of-three pragmatism wins. What's non-negotiable either way is the direction: when a use case exists, the ViewModel must not *also* bypass it."}]},{level:"senior",q:"Design the error-handling strategy across all three layers. Where are exceptions caught, and what crosses each boundary?",a:[{t:"list",items:["**Data**: catch transport garbage at the source (IOException, HTTP codes, JSON errors) and translate into a **typed error vocabulary** — `sealed interface DataError` (NoConnection, Unauthorized, NotFound, Unknown(cause)). Nothing library-specific crosses upward: `HttpException` escaping the data layer is a layering bug.","**Domain**: business failures are **values in return types** (`Result<Order, OrderError>` / sealed results) — `InsufficientFunds` is an expected outcome, not an exception. Use cases may *translate* data errors into business terms (NotFound → `ProductDiscontinued`).","**Presentation**: exhaustive `when` over the sealed error → UX decision (retry UI for connectivity, auth flow for Unauthorized, generic toast + log for Unknown). Never string-match messages.","**Two global rules**: `CancellationException` is always rethrown, at every layer — a blanket `runCatching` that swallows it breaks structured concurrency (use a `runSuspendCatching` helper); and long-lived observation Flows must catch *inside* (emit error values) because a terminal exception kills the collection permanently."]},{t:"p",text:"The design principle to state: **each layer speaks errors in its own vocabulary**, translated at the same boundaries where models are mapped — errors *are* part of the model."}]},{level:"senior",q:"What does 'main-safe' mean and which layer owns dispatcher switching? What are the common mistakes?",a:[{t:"p",text:"Main-safe: any suspend function can be called from the main thread without blocking it — the function itself moves work where it belongs. **The layer doing the blocking work owns the switch**: a repository doing heavy mapping wraps in `withContext(ioDispatcher/defaultDispatcher)`; use cases wrap their own CPU work; the ViewModel then simply launches in `viewModelScope` with no dispatcher juggling."},{t:"list",items:["Mistake 1 — **caller-side hopping**: ViewModels wrapping every call in `withContext(IO)` 'just in case'; scatters threading policy everywhere and hides which layer actually blocks.","Mistake 2 — **hardcoding `Dispatchers.IO`**: kills virtual-time testing; inject dispatchers (constructor params / DispatcherProvider).","Mistake 3 — cargo-cult wrapping of **Room/Retrofit suspend calls** — they're already main-safe; the extra `withContext` is noise (harmless, but shows shallow understanding when probed).","Mistake 4 — collector-side `flowOn` confusion: `flowOn` affects *upstream* only; the layer owning the heavy operator applies it, not the UI."]}]},{level:"senior",q:"How do you structure Clean Architecture in a KMP project, and what changes versus Android-only?",a:[{t:"list",items:["**Domain + data move to `commonMain`**: models, use cases, repository ports and implementations shared; Ktor/SQLDelight (or Room KMP) as common data sources, with platform bits (DB driver, HTTP engine, file paths) injected via `expect/actual` or DI.","**The Dependency Rule becomes compiler-enforced**: `commonMain` physically cannot import Android — KMP turns Clean's discipline from convention into a build error, which is the strongest practical argument for strict layering there.","**Presentation splits by strategy**: Compose Multiplatform → shared ViewModels (androidx KMP ViewModel) all the way; native SwiftUI → shared state holders bridged via SKIE/adapters, with thin platform view layers.","**Strict-Clean choices start paying rent**: ports-in-domain, injected dispatchers, typed errors — all the things that felt ceremonial on Android-only become load-bearing when two platforms and their tests consume the same core.","What changes in judgment: the 'is a domain layer worth it?' question mostly disappears — sharing *is* the payoff; the new hard questions are where presentation logic lives (shared vs per-platform) and keeping the iOS bridge thin."]}]},{level:"senior",q:"How does Clean Architecture interact with Jetpack Paging, WorkManager, and other framework libraries that want to cross layers?",a:[{t:"p",text:"This is the honest pain point — several Jetpack libraries are architecturally invasive, and the senior answer is knowing where to bend. **Paging**: `PagingData`/`Flow<PagingData<T>>` is designed to flow from data layer to UI *as a type* — purists wrap it, but fighting it costs the library's main value (state handling, invalidation); the pragmatic standard is treating Paging types as an allowed cross-layer vocabulary while keeping the *items* as domain/UI models via `map`. **WorkManager**: workers are entry points like Activities — they should be thin, immediately delegating to injected use cases/repositories (HiltWorker), never containing business logic. **Room/DataStore observation**: expose as Flows of domain types at the repository boundary — that one's easy."},{t:"p",text:"The generalizable principle: framework types may act as **plumbing across layers** when a library's core value depends on it, but **decisions** stay in your layers, and the framework type never becomes a parameter of business logic. State it as a trade-off you make knowingly, not a rule you broke."}]},{level:"senior",q:"A pull-to-refresh must update data shown on three different screens simultaneously. Walk through how your architecture handles it.",a:[{t:"p",text:"This is the SSOT pattern's showcase. All three screens' ViewModels observe the same repository stream (`observeArticles(): Flow<List<Article>>`), which reads from the database. The refresh — triggered from any screen — calls `repository.refresh()`, which fetches from network and **writes into the database**. Room/SQLDelight invalidation re-emits the query, every observing Flow fires, and all three screens update in the same frame — no screen-to-screen communication, no event bus, no 'notify others' code at all."},{t:"list",items:["Loading/error state for the *gesture* is per-screen (the initiating ViewModel tracks its own `isRefreshing` around the suspend call) — shared data, local UI state: an important separation to name.","In-flight de-duplication (two screens refreshing at once) is a repository concern — a mutex or shared in-flight job, solved once.","Contrast the alternatives interviewers are fishing for you to reject: shared ViewModels stretched across unrelated screens, or broadcast/event buses — both reinvent SSOT badly."]}]},{level:"senior",q:"Critique Clean Architecture: when is it the wrong choice, and what do its critics get right?",a:[{t:"list",items:["**The ceremony critique is correct in the small**: for a 2-screen app, four model types, mandatory use cases and five modules are cost without benefit — you're maintaining translation machinery for translations that never vary.","**'Swap the framework' is mostly a myth**: nobody replaces Room with realm on a whim; arguing Clean from swappability is weak. The *real* recurring payoffs are unit-testable logic, parallel team workstreams, and KMP portability — argue from those.","**Indirection has a comprehension price**: following one button tap through VM → use case → port → impl → source is six files; without strong naming conventions this actively slows debugging.","**Dogma drift**: teams enforcing pattern compliance in review while shipping slowly have inverted means and ends; Google's own guide (optional domain layer) is the institutional acknowledgment.","**When I'd skip it**: prototypes/MVPs, single-developer utility apps, heavily server-driven UI where the client has little logic to protect."]},{t:"p",text:"Then land it: the judgment skill is *scaling architecture to stakes* — start with ViewModel + repository + SSOT (cheap, huge payoff), add domain/use cases when business logic or team size demands, add strict ports when KMP or module boundaries appear. Architecture is bought in increments, not installed as a religion."}]},{level:"senior",q:"Map each SOLID principle to a concrete decision in an Android Clean Architecture codebase.",a:[{t:"list",items:["**S** — one use case per business action; repository per domain concept; screen-scoped ViewModels. Violation smell: a 'UserManager' doing auth + profile + settings.","**O** — sealed error/state hierarchies grow by adding variants (compiler finds every `when` to update); new data source slots in behind the repository with zero consumer changes.","**L** — the test fake for `ArticleRepository` must honor the port's contract (same behavior for empty results, same error types) — if production code needs `is RoomArticleRepository` checks, substitution is broken.","**I** — per-concept repository interfaces, not a 40-method `AppRepository`; a ViewModel needing read-only data depends on an observe-only interface if you split reads/writes.","**D** — the architecture's spine: domain defines `ArticleRepository`, data implements it, DI binds them; high-level policy (use cases) never names low-level detail (Retrofit)."]},{t:"p",text:"Strong close: note that the Dependency Rule *is* DIP applied at architectural scale, and SRP at file level is what the layer boundaries are at system level — SOLID isn't a checklist beside the architecture, it's what the architecture is made of."}]}],kx={id:"clean-architecture",title:"Clean Architecture",tagline:"The Dependency Rule, layers, use cases, repositories, SSOT, error & threading strategy, SOLID, KMP layering — and when it's all overkill.",tags:["Android","KMP","Domain Layer","Repository","SOLID","SSOT"],content:vx,qa:bx},xx=[{heading:"Why modularize — and when not to",blocks:[{t:"p",text:"**Modularization** splits one monolithic Gradle module into many smaller ones with explicit dependencies. It is *orthogonal* to MVVM/Clean — those say how code is layered; modularization decides how it's **physically partitioned and compiled**. The reasons that actually hold up:"},{t:"list",items:["**Enforced boundaries**: within one module, any class can reach any other — architecture rules are convention. Across modules, an illegal dependency is a **compile error**. Modularization is architecture with teeth.","**Build speed**: Gradle skips compiling modules whose inputs didn't change and builds independent modules **in parallel** — in large apps this cuts incremental builds from minutes to seconds.","**Team ownership**: feature teams own modules with clear APIs; merge conflicts drop; CODEOWNERS maps cleanly.","**Reuse & delivery**: shared `core` modules across apps (or KMP targets); **Play Feature Delivery** (on-demand dynamic features) requires modules.","**Testability & tooling**: per-module test suites run in isolation; demo/sandbox apps can assemble a single feature."]},{t:"note",text:"When **not** to: small apps and small teams. Modularization has fixed costs — Gradle config sprawl, slower *clean* builds (module graph overhead), cross-module refactors touching many build files. Under ~50–100k LOC and a handful of developers, a well-layered monolith with package discipline is usually faster to work in. Say this unprompted; it's the judgment interviewers probe."}]},{heading:"Module taxonomy — app, feature, core, and the api/impl split",blocks:[{t:"list",items:["**`:app`** — thin shell: application class, DI aggregation, navigation host, final dependency wiring. Depends on everything; contains almost nothing.","**`:feature:*`** (e.g. `:feature:checkout`, `:feature:profile`) — one user-facing feature each: its screens, ViewModels, feature-scoped DI. Feature modules **never depend on each other**.","**`:core:*`** (a.k.a. library modules) — shared infrastructure: `:core:designsystem`, `:core:network`, `:core:database`, `:core:common`, `:core:testing`. Depend only on other core modules.","**`:data:*` / `:domain:*`** — when layering is also modularized: `:core:data` repositories, `:core:domain`/`:domain:orders` models+use cases (this is where Clean Architecture's layers become physical)."]},{t:"h3",text:"The api/impl split"},{t:"p",text:"Mature graphs split modules into a tiny **`-api`** module (interfaces + models only) and an **`-impl`** module (implementation): consumers depend on `:feature:payments:api`, while only `:app` depends on `:feature:payments:impl` to wire DI. Effects: consumers **can't** reach implementation details (enforced encapsulation), and an impl change doesn't recompile consumers — only api changes do. Cost: double the module count; adopt it where the compile-avoidance and decoupling pay, not everywhere by default."}]},{heading:"Slicing strategies: by layer, by feature, or hybrid",blocks:[{t:"table",headers:["Strategy","Shape","Verdict"],rows:[["**By layer**","`:presentation`, `:domain`, `:data` — three fat horizontal modules","enforces Clean's arrows but scales terribly: every feature touches all three modules, teams collide, parallelism is capped at 3"],["**By feature**","`:feature:search`, `:feature:cart`, … + shared `:core:*`","matches team boundaries and build parallelism; layering *inside* a feature is back to convention"],["**Hybrid (industry standard)**","features sliced vertically, each internally layered (or split into `:feature:x` + `:feature:x:api`), on top of `:core:*` layer modules","what Google's Now in Android sample and most large apps do"]]},{t:"p",text:"The dependency graph you're aiming for is a **DAG with three tiers**: `:app` → features → core. Arrows only point downward; anything sideways (feature→feature) or upward is a design smell the build should reject."}]},{heading:"Gradle mechanics — api vs implementation and what they do to builds",blocks:[{t:"code",title:"The dependency configurations that matter",code:`// :feature:checkout/build.gradle.kts
dependencies {
    implementation(project(":core:designsystem")) // internal use only
    api(project(":core:model"))                    // leaked in this module's public API

    implementation(libs.androidx.lifecycle.viewmodel)
}`},{t:"list",items:["**`implementation`**: the dependency is invisible to consumers' compile classpaths. When it changes (ABI), *consumers of this module don't recompile*. Default choice — always.","**`api`**: the dependency is re-exported — consumers see its types and **recompile when it changes**. Use only when the module's own public signatures expose those types (e.g. a models module).","The build-speed rule: `api` where unavoidable, `implementation` everywhere else — accidental `api` chains are the classic reason 'we modularized but builds didn't improve'.","**ABI vs non-ABI changes**: Gradle's compile avoidance recompiles consumers only when a dependency's *public signatures* change — a method-body edit in `:core:network` shouldn't recompile features (unless leaked via `api`).","Circular module dependencies are a **hard Gradle error** — usually a hidden shared concept that wants extraction into a lower module."]}]},{heading:"Feature-to-feature navigation without dependencies",blocks:[{t:"p",text:"Checkout must open Profile, but features can't depend on each other. Every large app solves this with **inversion through a shared contract** — know several concrete shapes:"},{t:"list",items:["**Route/deep-link contracts in a shared module**: `:core:navigation` holds route constants or type-safe route classes (Navigation-Compose serializable routes); features navigate by *route*, and `:app`'s NavHost maps routes → screens. Features never see each other's composables.","**Navigator interfaces**: `:feature:checkout` declares `interface CheckoutNavigation { fun openProfile(userId: String) }`; `:app` implements it with the real NavController and provides it via DI — full inversion, easiest to fake in tests.","**Deep links proper** (URIs) — loosest coupling, also covers external entry; stringly-typed unless wrapped.","**Screen-factory registries** for non-Navigation stacks (or multi-module Fragments): features register `Route -> Screen` factories into a map the host consumes."]},{t:"code",title:"Contract + app-level wiring (Navigator-interface flavor)",code:`// :feature:checkout (declares what it needs)
interface CheckoutNavigator {
    fun openProfile(userId: String)
    fun openOrderConfirmation(orderId: String)
}

// :app (the only module that knows everyone)
class AppNavigator(private val navController: NavHostController) :
    CheckoutNavigator, ProfileNavigator {
    override fun openProfile(userId: String) =
        navController.navigate(ProfileRoute(userId))
    override fun openOrderConfirmation(orderId: String) =
        navController.navigate(OrderConfirmationRoute(orderId))
}`}]},{heading:"Dependency injection across modules",blocks:[{t:"list",items:["**Hilt**: each module declares its own `@Module @InstallIn(...)` bindings; the annotation processor **aggregates them automatically** when `:app` depends on the module — no manual registry. `@HiltViewModel`s in feature modules just work.","**Hilt across api/impl splits**: consumers inject the interface from `-api`; the `@Binds` lives in `-impl`; only `:app` depends on `-impl`, so aggregation still sees it. **EntryPoints** cover non-injectable entry places (dynamic-feature code, content providers).","**Dynamic Feature caveat**: Hilt can't aggregate from on-demand modules (they depend on `:app`, inverting the graph) — you use `@EntryPoint` accessors there; a classic gotcha question.","**Koin**: each Gradle module exposes a `val featureModule = module { ... }`; `:app` collects the list into `startKoin` — explicit, no codegen, and multiplatform (the common choice in KMP).","**Scoping discipline**: singletons (network, DB) live in core modules' bindings; feature bindings stay feature/ViewModel-scoped so features don't bloat the app-lifetime graph."]}]},{heading:"Build infrastructure: version catalogs & convention plugins",blocks:[{t:"p",text:"Thirty modules means thirty `build.gradle.kts` files — without shared infrastructure they drift. Two standard tools:"},{t:"code",title:"Version catalog — gradle/libs.versions.toml",code:`[versions]
kotlin = "2.1.0"
compose-bom = "2025.01.00"

[libraries]
androidx-lifecycle-viewmodel = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version = "2.8.7" }

[plugins]
android-application = { id = "com.android.application", version = "8.7.0" }`},{t:"code",title:"Convention plugin — build-logic/ (one line per module afterwards)",code:`// build-logic/convention/src/main/kotlin/AndroidFeatureConventionPlugin.kt
class AndroidFeatureConventionPlugin : Plugin<Project> {
    override fun apply(target: Project) = with(target) {
        pluginManager.apply("com.android.library")
        pluginManager.apply("org.jetbrains.kotlin.android")
        extensions.configure<LibraryExtension> {
            compileSdk = 35
            defaultConfig.minSdk = 24
        }
        dependencies {
            "implementation"(project(":core:designsystem"))
            "implementation"(project(":core:common"))
        }
    }
}

// a feature module's ENTIRE build.gradle.kts:
plugins { id("myapp.android.feature") }
dependencies { implementation(project(":core:data")) }`},{t:"list",items:["**Version catalog** = single source of truth for versions/coordinates, type-safe accessors (`libs.androidx.lifecycle.viewmodel`), one place for Renovate/Dependabot updates.","**Convention plugins** (in an included `build-logic` build) capture per-module-type configuration — `myapp.android.feature`, `myapp.android.library`, `myapp.kmp.library` — so module build files shrink to declarations. This is the Now-in-Android pattern and a strong senior signal to name.","Anti-pattern they replace: `subprojects { }` / `allprojects { }` blocks in the root build file — cross-project configuration that breaks configuration caching and project isolation."]}]},{heading:"Build performance — what modularization actually buys, and the rest of the toolbox",blocks:[{t:"list",items:["**Compile avoidance**: unchanged modules aren't recompiled; ABI-only recompilation limits blast radius of a change to its dependents — *if* the graph is wide and `api` leakage is controlled. A deep chain of modules serializes and helps nothing.","**Parallel execution**: `org.gradle.parallel=true` builds independent modules concurrently — payoff scales with graph *width*.","**Build cache** (local/remote): task outputs keyed by inputs — CI reuses results across machines; a fat monolith module can never cache-hit partially.","**Configuration cache**: skips the configuration phase entirely on repeat builds — the biggest recent win; requires plugins (and your build logic) to be compatible.","**KSP over KAPT** (Hilt/Room/Moshi): removes the stub-generation tax, roughly 2× annotation-processing speed.","Measure, don't guess: **build scans** (`--scan`), `gradle-profiler`, and the module dependency graph (e.g. `graphviz` task plugins) to find the critical path and `api` leaks."]}]},{heading:"Modularization in KMP",blocks:[{t:"list",items:["Same DAG philosophy, plus the platform dimension: shared KMP modules (`:shared:domain`, `:shared:data`, `:shared:feature:x`) consumed by `:androidApp`, with iOS consuming through a framework.","**The umbrella module**: iOS wants *one* framework, so multi-module KMP projects add a `:shared:umbrella` module that `export`s the others into a single XCFramework — a KMP-specific pattern worth naming (direct multi-framework consumption breaks type identity across frameworks).","Per-module `commonMain/androidMain/iosMain` source sets keep `expect/actual` local to the module that owns the abstraction (DB driver in `:shared:database`, not globally).","DI: Koin modules per Gradle module aggregate in shared init code called from both platforms; Hilt stays Android-side only.","Watch iOS build times: each shared module compiles to Kotlin/Native — the umbrella + exported set should be curated, not 'export everything'."]}]},{heading:"Pitfalls, smells, and the migration path",blocks:[{t:"list",items:["**The `:core:common` dumping ground** — 'utils' modules that every module depends on become a new monolith: one change recompiles the world. Keep leaf modules small and purposeful; split `common` the moment it accretes.","**Over-modularization** — 200 micro-modules where configuration time exceeds compile time; module count should follow team/feature boundaries, not class count.","**`api` chains** — transitive leakage quietly restoring monolith-like recompilation; audit with build scans.","**Sideways dependencies** smuggled through 'shared feature' modules — `:feature:a` and `:feature:b` both depending on `:feature:shared-ab` that contains half of each; extract genuine common *domain* downward instead.","**Circulars resolved by merging** — merging two modules to kill a cycle usually papers over a missing abstraction; extract the shared concept downward instead.","**Migration order (monolith → modules)**: 1) extract leaf utilities (`:core:model`, `:core:common`); 2) extract infrastructure (`:core:network`, `:core:database`, `:core:designsystem`); 3) carve features out one at a time, newest/most-active first; 4) thin `:app` to a shell. Enforce with lint/Konsist/module-graph checks so the monolith doesn't regrow."]},{t:"note",text:'Best interview close: "Modularization is the physical enforcement of whatever architecture you chose — layers become compile errors instead of review comments. Its ROI curve starts negative and grows with team size, so the skill is timing the investment, not maximizing module count."'}]}],Sx=[{level:"junior",q:"What is modularization and why do teams do it?",a:[{t:"p",text:"Splitting one monolithic Gradle module into multiple smaller modules with explicit dependencies between them. The four real reasons: **enforced boundaries** (an illegal dependency becomes a compile error instead of a review comment), **build speed** (unchanged modules aren't recompiled, independent ones build in parallel), **team ownership** (features map to modules with clear APIs), and **reuse/delivery** (shared core modules, Play on-demand feature delivery). It's orthogonal to MVVM/Clean — those decide layering, modularization decides physical partitioning."}]},{level:"junior",q:"Describe a typical module structure for a modularized Android app.",a:[{t:"list",items:["**`:app`** — thin shell: Application class, DI aggregation, the NavHost. Depends on everything, contains almost nothing.","**`:feature:search`, `:feature:cart`, …** — one user-facing feature each (screens + ViewModels + feature DI). Never depend on each other.","**`:core:designsystem`, `:core:network`, `:core:database`, `:core:model`, `:core:common`** — shared infrastructure that features build on."]},{t:"p",text:"The shape is a three-tier **DAG**: app → features → core, arrows only pointing downward. This hybrid (vertical feature slices over horizontal core layers) is what Google's Now in Android sample demonstrates."}]},{level:"junior",q:"What's the difference between api and implementation in Gradle dependencies?",a:[{t:"p",text:"`implementation` keeps the dependency **private**: consumers of your module don't see its types and — crucially — **don't recompile when it changes**. `api` **re-exports** it: the types appear in your module's public surface and every consumer recompiles on its changes. Rule: `implementation` always, `api` only when your own public signatures genuinely expose the dependency's types (e.g. a `:core:model` module). Accidental `api` chains are the top reason modularized builds stay slow."}]},{level:"junior",q:"Why shouldn't feature modules depend on each other?",a:[{t:"p",text:"Because feature-to-feature edges rebuild the monolith incrementally: they serialize builds (B waits for A), create merge coupling between teams, invite cycles (checkout→profile→checkout is a hard Gradle error), and make features impossible to build/test/deliver in isolation. When feature A needs something of feature B's, the *thing* is either shared domain (extract downward into a core/domain module) or navigation (invert through a route contract or navigator interface that `:app` wires). The graph stays a DAG with arrows pointing down."}]},{level:"junior",q:"How does one feature open a screen that belongs to another feature module?",a:[{t:"p",text:"Through an inverted contract, since a direct dependency is forbidden. Common shapes: **shared route definitions** in `:core:navigation` (feature navigates by type-safe route; `:app`'s NavHost maps routes to screens), a **navigator interface** the feature declares (`fun openProfile(userId)`) and `:app` implements with the real NavController via DI, or **deep links** for the loosest coupling. In all three, only `:app` knows every screen — features know destinations as abstract contracts."}]},{level:"junior",q:"What is a version catalog and what problem does it solve?",a:[{t:"p",text:"`gradle/libs.versions.toml` — a single declarative file listing dependency versions, library coordinates and plugins, exposed as type-safe accessors (`libs.androidx.lifecycle.viewmodel`) in every module. Problems solved: thirty build files no longer each hardcode versions (no drift, no '3 versions of OkHttp' surprises), upgrades are a one-line change, and bots (Renovate/Dependabot) have one file to update. It replaced the older `ext {}` / `Dependencies.kt` buildSrc conventions."}]},{level:"junior",q:"Does modularization automatically make builds faster?",a:[{t:"p",text:"No — it *enables* faster builds, and can even slow them down if done badly. Wins come from **compile avoidance** (unchanged modules skipped, only dependents of a changed module rebuild) and **parallelism** (independent modules concurrently) — both require a *wide* graph with `implementation` boundaries. A deep chain of modules, `api` leakage everywhere, or a `:core:common` that everything depends on gives you monolith rebuild behavior plus extra Gradle configuration overhead. Clean builds actually get slightly slower (per-module fixed costs). So: measure with build scans, control `api`, keep the graph wide and shallow."}]},{level:"senior",q:"Compare modularizing by layer vs by feature. Which do you choose and why?",a:[{t:"p",text:"**By layer** (`:presentation`/`:domain`/`:data`) physically enforces Clean's arrows, but scales badly: every feature change touches all three modules (so teams collide in the same modules), parallelism caps at the layer count, and each layer module grows into its own monolith. **By feature** matches team ownership and maximizes graph width, but layering *inside* each feature falls back to convention, and shared logic gets duplicated or dumped into common modules."},{t:"p",text:"I choose the **hybrid** every serious codebase converges on: vertical feature slices (`:feature:x` — optionally split into `:api`/`:impl`) sitting on horizontal core modules (`:core:data`, `:core:designsystem`, `:core:model`). Feature boundaries follow *team and product* seams; layer boundaries live in core and, where the stakes justify it, inside features via the api/impl split. The honest observation to add: pure by-layer is the textbook answer, hybrid is the shipped answer — NowInAndroid, and most large apps, are hybrids."}]},{level:"senior",q:"Explain the api/impl module split — what does it buy, what does it cost, and when is it worth it?",a:[{t:"list",items:["**Mechanics**: `:feature:payments:api` holds only interfaces + models; `:feature:payments:impl` holds the implementation and its `@Binds`. Consumers depend on `:api`; only `:app` depends on `:impl` for wiring.","**Buys — encapsulation with teeth**: consumers *cannot* reach implementation classes (not convention — the classpath doesn't contain them).","**Buys — compile-avoidance**: impl changes recompile only `:app`'s wiring, not the consumers; api modules are small and change rarely, so the recompilation blast radius of most edits collapses.","**Buys — swappability**: fake impls for demo apps and tests slot in per-module.","**Costs**: 2× module count, DI indirection for every binding, and 'where does this type go' friction; api modules that accrete logic defeat the purpose."]},{t:"p",text:"Worth it when: many consumers per module (core modules first — `:core:data:api` pays off fastest), large teams needing hard encapsulation, or build times dominated by wide recompilation. Not worth it as a blanket rule on a 10-module app — apply it to the hubs of the dependency graph, not the leaves."}]},{level:"senior",q:"How does Hilt work across modules, and what breaks with dynamic feature modules?",a:[{t:"p",text:"Standard multi-module Hilt is pleasantly automatic: each module declares `@Module @InstallIn(SingletonComponent::class)` (etc.) bindings; because `:app` (the `@HiltAndroidApp` root) transitively depends on every module, annotation processing **aggregates all bindings** into the app's component graph at compile time — no manual registry, and `@HiltViewModel`s in feature modules just work. With api/impl splits, the `@Binds` sits in `:impl`, consumers inject the `:api` interface, and aggregation still finds it since `:app` depends on `:impl`."},{t:"p",text:"**Dynamic features invert the graph** — the on-demand module depends *on* `:app`, not vice versa — so Hilt's aggregation can't see it: no `@AndroidEntryPoint` in dynamic modules. The workaround is **`@EntryPoint`**: define an interface listing what the dynamic module needs, install it in the app's component, and fetch it reflectively via `EntryPointAccessors.fromApplication(...)` from the dynamic module's screens. It's clunky by design — worth also saying that this friction (plus dagger's per-module strictness) is why some heavily-dynamic apps choose Koin, which resolves at runtime and doesn't care about compile-time graph direction."}]},{level:"senior",q:"What are convention plugins and why are they preferred over buildSrc and allprojects blocks?",a:[{t:"p",text:"Convention plugins are small Gradle plugins in an **included build** (`build-logic/`) that encode each module *type*'s full configuration — `myapp.android.feature` applies AGP + Kotlin, sets SDK versions, adds standard deps — so a feature module's build file shrinks to `plugins { id(\"myapp.android.feature\") }` plus its specific dependencies. Thirty modules stay consistent by construction, and changing minSdk is a one-file edit."},{t:"list",items:["**vs `allprojects`/`subprojects`**: cross-project configuration reaches into other projects' models — it breaks **configuration caching** and **project isolation**, and hides configuration far from the module; convention plugins keep configuration declarative and local.","**vs `buildSrc`**: a `buildSrc` change invalidates the build classpath of *every* module (full rebuild); an included `build-logic` build is a normal dependency with normal invalidation — that single caching difference is why the ecosystem (and NowInAndroid) migrated.","Bonus points: convention plugins are themselves testable, versionable, and shareable across repositories — build logic promoted to first-class code."]}]},{level:"senior",q:"Your 60-module app still has slow incremental builds. Walk through your diagnosis.",a:[{t:"list",items:["**Get data first**: `--scan` / gradle-profiler on a representative edit (one-line body change in a hot module). Look at what actually recompiled and the task critical path — guesses about Gradle are usually wrong.","**Check the blast radius**: if a body-only (non-ABI) change recompiled half the graph, hunt `api` leakage — audit hub modules' dependency declarations, demote to `implementation`.","**Check the hubs**: a `:core:common` dumping ground everything depends on makes every change global — split it; check the graph's *width vs depth* (deep chains serialize; parallelism needs width).","**Check the perennial taxes**: KAPT still present? (migrate to KSP — Hilt/Room/Glide all support it); configuration time high? (enable configuration cache, kill `allprojects` blocks and eager task creation); no remote build cache on CI?","**Check the non-Gradle suspects**: single massive module dominating the critical path (carve it), annotation processors on hot paths, unnecessary resource processing in library modules, outdated AGP/Kotlin (compiler perf improves every release)."]},{t:"p",text:"The framing that lands: treat build performance as an engineering problem with measurements and a critical path, not folklore — and note the fix is often *dependency hygiene* (api→implementation, splitting hubs), not more modules."}]},{level:"senior",q:"How do you structure modules in a KMP project, and what is the umbrella framework problem?",a:[{t:"p",text:"Same DAG discipline with a platform dimension: shared modules (`:shared:model`, `:shared:database`, `:shared:feature:x`) with per-module `commonMain`/`androidMain`/`iosMain` source sets — keeping each `expect/actual` inside the module that owns the abstraction. Android consumes shared modules as ordinary Gradle deps; DI is typically Koin (modules aggregated in shared init) since Hilt is Android-only."},{t:"p",text:"**The umbrella problem**: iOS consumes Kotlin as an Apple framework, and if each shared module produced its own framework, shared types would lose identity across them (the same Kotlin class becomes two incompatible Obj-C classes in two frameworks). So multi-module KMP projects create one **umbrella module** whose framework `export()`s the modules iOS needs, producing a single XCFramework. Follow-ups worth volunteering: curate the export list (every exported module grows the binary and compile time — Kotlin/Native linking is the slow path), non-exported transitive types surface as opaque `KotlinBase` — so design the iOS-facing API to live in exported modules; and SKIE runs on the umbrella to improve the Swift surface."}]},{level:"senior",q:"How do you enforce architectural boundaries beyond what Gradle gives you — and prevent the monolith from regrowing?",a:[{t:"list",items:["**Gradle gives the coarse walls** (missing dependency = compile error) — but within a module, and for rules like 'features may not depend on features', you need active enforcement.","**Konsist / ArchUnit tests**: executable architecture rules — 'classes in ..presentation.. may not import ..data.internal..', 'ViewModels live in feature modules', run in CI like any test.","**Custom lint checks / detekt rules** for idiom-level rules (no `android.*` imports in domain packages, no repository types in composables).","**Module-graph assertions**: plugins that validate the dependency DAG against a declared spec (no feature→feature edges, max depth, no new edges without review) — turning graph erosion into a red build.","**Structural friction**: `internal` visibility + api/impl splits make violations *impossible* rather than detectable; CODEOWNERS on build files makes new edges reviewable by the platform team."]},{t:"p",text:"The principle to close on: every architecture decays at the rate reviewers get tired — durable boundaries are the ones a machine checks. Convention < detection (CI rules) < prevention (visibility + graph structure)."}]},{level:"senior",q:"Walk through migrating a 400k-line monolith module to a modular structure while the team keeps shipping.",a:[{t:"list",items:["**Prep**: measure builds (baseline to prove ROI later); map the internal dependency structure (package-level analysis — jdeps/Sonargraph/IDE); set up version catalog + convention plugins *first* so every extracted module is one line of config.","**Extract leaves first**: `:core:model`, then infrastructure with few inward deps — `:core:designsystem`, `:core:network`, `:core:database`. Each extraction is mechanical (move + fix imports) and immediately parallelizes builds.","**Break the hard knots**: the monolith's remaining tangle is usually cyclic feature code coupled through statics/utils — introduce interfaces at the seams inside the monolith *before* physically moving code (decouple in place, then extract cheaply).","**Carve features by activity**: highest-churn features first (that's where isolation pays daily); each becomes `:feature:x` with a navigation contract; the monolith shrinks to a legacy module that new code may not depend on (enforced by a graph rule).","**Keep shipping**: every step lands on main behind normal review — no long-lived migration branch; the graph rules + CI checks prevent regrowth; declare victory not at 'zero legacy' but when the legacy module is frozen and shrinking."]},{t:"p",text:"Risks to name: import-fixing churn creating merge storms (coordinate extraction windows), hidden reflection/resource references breaking at runtime (R classes, ProGuard), and morale — pick early extractions that visibly improve build times so the effort funds its own credibility."}]}],Tx={id:"modularization",title:"Modular Architecture",tagline:"Feature/core module graphs, api vs implementation, cross-module navigation & DI, convention plugins, build performance, and KMP module structure.",tags:["Android","KMP","Gradle","Build Speed","Hilt","Boundaries"],content:xx,qa:Sx},Cx=[{heading:"Null safety — nullability is part of the type",blocks:[{t:"p",text:"Kotlin's headline feature is **null safety**: the type system distinguishes a type that can hold null from one that can't. `String` is a non-null type — it can *never* be null, guaranteed by the compiler. `String?` is a nullable type — it might be null. This turns the billion-dollar `NullPointerException` from a runtime crash into a compile-time check: you literally cannot assign null to a non-null type, and you can't use a nullable value without handling the null case."},{t:"code",title:"The compiler enforces nullability",code:`var a: String = "hello"
a = null            // COMPILE ERROR — String is non-null

var b: String? = "hi"
b = null            // OK — String? is nullable

val len = b.length  // COMPILE ERROR — b might be null, must handle it first`},{t:"list",items:["The guarantee is *compile-time*: non-null types can't hold null, and nullable types force you to handle the null before use. NPEs become nearly impossible in pure Kotlin.","This shifts null handling from 'remember to check everywhere' (Java) to 'the compiler won't let you forget' — a systemic safety improvement."]}]},{heading:"The null-handling operators",blocks:[{t:"code",title:"Safe call, Elvis, not-null assertion",code:`val user: User? = getUser()

// ?. safe call — returns null if the receiver is null (short-circuits)
val name: String? = user?.name
val city: String? = user?.address?.city   // chains — null if any link is null

// ?: Elvis — provide a default when the left side is null
val displayName: String = user?.name ?: "Guest"

// ?.let — run a block only if non-null
user?.let { u -> println(u.name) }         // block skipped if user is null

// !! not-null assertion — assert non-null, THROW NPE if wrong (dangerous)
val forced: String = user!!.name           // crashes if user is null`},{t:"list",items:["**`?.` (safe call)** — access a member only if the receiver is non-null; the whole expression is null otherwise. Chainable (`a?.b?.c`), short-circuiting at the first null.",'**`?:` (Elvis operator)** — supply a fallback value (or throw / return) when the left side is null: `name ?: "default"`, `id ?: return`, `token ?: throw IllegalStateException()`.',"**`?.let { }`** — execute a block with the non-null value, skipped entirely if null — a common way to 'do something only if present'.","**`!!` (not-null assertion)** — force a nullable into non-null, throwing NPE if it's actually null. It reintroduces the exact crash null safety prevents, so it's a code smell — use only when you can *prove* non-null and the alternative is worse. Overusing `!!` is a red flag in review."]}]},{heading:"Smart casts — the compiler narrows types for you",blocks:[{t:"p",text:"After you *check* a value's type or nullability, Kotlin **smart-casts** it — automatically treating it as the narrower type within that scope, so you don't cast manually."},{t:"code",title:"Smart casts in action",code:`fun describe(x: Any) {
    if (x is String) {
        // x is smart-cast to String here — call String methods directly
        println(x.length)
    }
}

fun greet(name: String?) {
    if (name != null) {
        // name is smart-cast to non-null String inside this branch
        println(name.uppercase())
    }
}`},{t:"list",items:["After `if (x is Type)`, `x` is treated as `Type` in that branch. After `if (x != null)`, `x` is non-null there. Also works with `&&`, early returns, and `when` branches.","**Smart cast requires the value can't change between check and use** — it works on `val`s and local `var`s, but **not** on mutable properties that could be changed by another thread or a custom getter (a `var` property could return different values on each access). That's why you sometimes must copy a nullable property to a local val first: `val n = name; if (n != null) n.uppercase()`."]}]},{heading:"val vs var, and immutability",blocks:[{t:"list",items:["**`val`** — a read-only reference (assigned once, can't be reassigned). **`var`** — a mutable reference (can be reassigned). Prefer `val` by default; reach for `var` only when reassignment is genuinely needed. `val` makes code easier to reason about (the reference won't change under you).","**`val` is not deep immutability**: `val list = mutableListOf(1)` — you can't reassign `list`, but you *can* `list.add(2)` (the object it points to is mutable). Immutability of the *reference* is separate from immutability of the *object*. For an immutable collection, use `listOf()` (read-only) or a truly immutable type.","**`const val`** — a compile-time constant (only for top-level or `object` primitives/Strings); inlined at compile time. Different from a regular `val`, which is computed at runtime."]}]},{heading:"Kotlin type system essentials",blocks:[{t:"list",items:["**Everything is an object**: there are no Java-style primitives in the language — `Int`, `Boolean`, `Double` are classes. The compiler optimizes them to JVM primitives where possible (a non-null `Int` becomes `int`), but a nullable `Int?` becomes a boxed `Integer`.","**`Any`** — the root of all non-null types (like Java's `Object`). **`Any?`** — the root of *everything*, including nullables. **`Unit`** — the type of functions that return no meaningful value (like `void`, but an actual type/object). **`Nothing`** — the type that has no values; a function returning `Nothing` never returns normally (it always throws or loops forever) — used for `throw`, `TODO()`, and to make the compiler's flow analysis smarter.","**Type inference**: Kotlin infers types, so you rarely write them — `val x = 5` infers `Int`. You add explicit types for public API clarity or when inference is ambiguous."]}]},{heading:"Platform types — the Java interop gap",blocks:[{t:"p",text:"When Kotlin calls Java code, Java has *no* nullability information (any reference can be null in Java). Kotlin represents these as **platform types** (written `String!` in errors/docs), meaning 'nullability unknown'. The compiler *relaxes* null checks for them — it lets you treat a platform type as non-null, trusting you. The risk: if the Java method actually returns null and you treated it as non-null, you get an NPE at runtime — the one place Kotlin's null safety can be bypassed."},{t:"list",items:["**Why they exist**: forcing every Java call to be nullable would make interop unbearable (every result would need `?.`); treating every Java call as non-null would hide real nulls. Platform types are the pragmatic middle — you decide.","**Best practice**: when consuming Java APIs, *explicitly annotate* the type you assign to (`val name: String = javaObj.getName()` asserts non-null and will NPE early if wrong; `val name: String? = ...` treats it as nullable and safe). Don't let platform types flow deep into your code untyped.","**Nullability annotations help**: Java code annotated with `@Nullable`/`@NonNull` (JetBrains, AndroidX, JSpecify) *does* carry nullability into Kotlin, eliminating the platform-type gap — which is why AndroidX APIs interop cleanly."]},{t:"note",text:'Interview-ready: "Kotlin encodes nullability in the type system (`String` vs `String?`), enforced at compile time, handled with `?.`, `?:`, `?.let`, and (sparingly) `!!`. Smart casts narrow types after checks. The one hole is *platform types* from unannotated Java — nullability-unknown types the compiler trusts you on, which can still NPE; annotate them explicitly at the boundary."'}]}],Mx=[{level:"junior",q:"How does Kotlin's null safety work?",a:[{t:"p",text:"**The concept**: Kotlin makes nullability part of the type. `String` is a non-null type that can *never* hold null, and `String?` is a nullable type that might. The compiler enforces this: you can't assign null to a non-null type, and you can't call methods on a nullable value without first handling the possibility of null. This turns NullPointerExceptions from runtime crashes into compile-time errors."},{t:"code",title:"The compiler catches null mistakes",code:`var name: String = "Kotlin"
name = null           // compile error
var nick: String? = null   // fine — nullable
println(nick.length)       // compile error — must handle null first`},{t:"p",text:"You handle nullables with a small set of operators: `?.` (safe call — returns null instead of crashing), `?:` (Elvis — provide a default), `?.let { }` (run a block only if non-null), and `!!` (assert non-null, throwing if wrong — to be avoided). The big win is systemic: instead of *remembering* to null-check everywhere like in Java, the compiler *won't let you forget*. The only escape hatch is platform types from Java code, which the compiler can't verify."}]},{level:"junior",q:"What do the ?., ?:, and !! operators do?",a:[{t:"list",items:["**`?.` (safe call)**: accesses a member only if the receiver is non-null; otherwise the whole expression evaluates to null. `user?.name` is the name, or null if `user` is null. It chains and short-circuits: `user?.address?.city` is null if any link is null.","**`?:` (Elvis operator)**: provides a fallback when the left side is null. `user?.name ?: \"Guest\"` gives the name or 'Guest'. The right side can also be a `return` or `throw`: `val id = user?.id ?: return`.","**`!!` (not-null assertion)**: forces a nullable value to non-null, throwing a `NullPointerException` if it's actually null. `user!!.name` crashes if `user` is null."]},{t:"p",text:"The first two are the safe, idiomatic tools — `?.` to safely navigate and `?:` to supply defaults. `!!` is the dangerous one: it reintroduces the exact NPE crash that null safety exists to prevent, so it's considered a code smell. Use it only when you can genuinely *prove* the value is non-null and restructuring isn't practical. A method full of `!!` usually signals nullability that should have been handled properly upstream."}]},{level:"junior",q:"What is a smart cast?",a:[{t:"p",text:"**The concept**: after you check a value's type or nullability, Kotlin automatically treats it as the narrower type within that scope — you don't write an explicit cast. If you write `if (x is String)`, then inside that block `x` is already a `String`, so you can call `x.length` directly. Similarly, after `if (name != null)`, `name` is treated as non-null in that branch."},{t:"code",title:"No manual casting needed",code:`fun handle(value: Any?) {
    if (value is String) {
        println(value.uppercase())   // smart-cast to String
    }
    if (value != null) {
        println(value.toString())    // smart-cast to non-null
    }
}`},{t:"p",text:"It saves boilerplate and is safe because the compiler knows the check holds. The one limitation to know: smart casts require the value *can't change* between the check and the use. It works on `val`s and local `var`s, but **not** on mutable `var` properties (a custom getter or another thread could change the value between the check and the call). In those cases you copy to a local val first: `val n = name; if (n != null) n.uppercase()`. This 'why won't it smart-cast my property?' situation is a common Kotlin gotcha."}]},{level:"junior",q:"What is the difference between val and var?",a:[{t:"p",text:"**`val`** declares a read-only reference — it's assigned once and can't be reassigned. **`var`** declares a mutable reference — you can reassign it. The guidance is to prefer `val` by default and use `var` only when you genuinely need to reassign, because immutable references are easier to reason about (the value won't change out from under you)."},{t:"p",text:"The crucial subtlety: **`val` makes the *reference* immutable, not the *object* it points to.** `val list = mutableListOf(1, 2)` means you can't reassign `list` to a different list, but you *can* still mutate its contents with `list.add(3)`. If you want the contents immutable too, use a read-only type like `listOf()`. So `val` ≠ deep immutability — it's 'this variable always points to the same object', while that object may itself be mutable. There's also `const val` for compile-time constants (top-level or in an `object`, primitives/Strings only), which is inlined at compile time, unlike a regular `val` that's computed at runtime."}]},{level:"junior",q:"What are Unit and Nothing in Kotlin?",a:[{t:"p",text:"**`Unit`** is the type of a function that doesn't return a meaningful value — Kotlin's equivalent of `void`, except it's a real type with a single value (the `Unit` object). A function with no explicit return type returns `Unit`. It's a proper type, so you can use it as a generic argument (`Function<Unit>`), which `void` can't do."},{t:"p",text:"**`Nothing`** is the type that has *no values at all* — nothing can ever be an instance of it. A function declared to return `Nothing` *never returns normally*: it always throws an exception or loops forever. `throw`, `TODO()`, and `error()` all return `Nothing`. This is genuinely useful for the compiler's flow analysis: because a `Nothing`-returning expression can't complete, the compiler knows code after it is unreachable, and it lets `Nothing` fit anywhere in type inference — which is why `val x: String = someValue ?: throw Exception()` compiles (the `throw` branch has type `Nothing`, compatible with `String`). So `Unit` = 'returns, but nothing useful'; `Nothing` = 'never returns at all'."}]},{level:"senior",q:"What are platform types, and how do they create a hole in Kotlin's null safety?",a:[{t:"p",text:"**The concept**: when Kotlin calls into Java, Java carries no nullability information — any Java reference could be null, and the Java type system doesn't distinguish. Kotlin represents these Java-originated types as **platform types** (shown as `Type!` in errors, e.g. `String!`), meaning 'nullability is unknown'. For platform types, the compiler *relaxes* its null checks — it lets you treat the value as either nullable or non-null, trusting your choice, rather than forcing safe calls everywhere."},{t:"list",items:["**The hole**: if you treat a platform type as non-null (`val name: String = javaObj.getName()`) but the Java method actually returns null at runtime, you get a `NullPointerException` — the exact crash Kotlin's null safety normally prevents. It's the one place pure-Kotlin's guarantee can be silently bypassed.","**Why they exist rather than a stricter rule**: if every Java call returned a nullable type, interop would be miserable (safe calls on everything, even values that are never null); if every Java call returned non-null, real nulls would slip through as NPEs everywhere. Platform types are the pragmatic compromise: the compiler defers to you at the boundary.","**Where the NPE actually fires**: it can be immediate (if you dereference right away) or *delayed* — a null platform value assigned to a non-null Kotlin field can travel deep into your code and crash far from the Java call, making it hard to trace. This delayed failure is the dangerous case."]},{t:"list",items:["**Mitigation 1 — annotate at the boundary**: explicitly declare the type when consuming Java. `val name: String? = javaObj.getName()` treats it as nullable (safe); `val name: String = javaObj.getName()` asserts non-null and, if wrong, throws *immediately at the boundary* (an early, traceable crash) rather than deep later. Never let raw platform types flow untyped into your codebase.","**Mitigation 2 — nullability annotations**: Java code annotated with `@Nullable`/`@NonNull` (JetBrains, AndroidX) or JSpecify carries nullability *into* Kotlin, eliminating the platform type entirely — the type becomes a proper `String` or `String?`. This is why modern AndroidX APIs interop cleanly with full null safety, while old unannotated Java libraries are risky."]},{t:"p",text:"**The senior framing**: Kotlin's null safety is airtight *within* Kotlin, but Java interop is a trust boundary. Platform types make interop ergonomic at the cost of a null-safety gap; the discipline is to *pin down nullability explicitly at the Java boundary* so the uncertainty doesn't leak inward, and to prefer annotated Java libraries."}]},{level:"senior",q:"Why won't the compiler smart-cast a nullable var property, and what are the ways around it?",a:[{t:"p",text:"**The reason**: smart casting is only sound if the value provably *cannot change* between the null-check and the use. A mutable `var` property fails that condition for two reasons: (1) another thread could reassign it after your check but before your use (a data race), and (2) the property could be a *custom getter* that computes a new value on every access — so calling it twice might return non-null then null. The compiler can't prove stability, so it refuses to smart-cast, giving the error 'smart cast to X is impossible, because the property could have been changed'."},{t:"code",title:"The problem and the fixes",code:`class Foo {
    var name: String? = null
    fun p() {
        if (name != null) {
            // println(name.length)   // ERROR: name is a mutable property
        }
    }
}

// Fix 1: copy to a local val (most common)
fun p2(foo: Foo) {
    val n = foo.name
    if (n != null) println(n.length)   // n is a stable local val — smart-casts fine
}

// Fix 2: let / safe call
foo.name?.let { println(it.length) }

// Fix 3: Elvis to bail early
val n = foo.name ?: return`},{t:"list",items:["**Fix 1 — capture in a local `val`**: the most idiomatic. A local `val` is stable (can't be reassigned, no getter re-evaluation), so the compiler smart-casts it freely. This is the standard workaround.","**Fix 2 — `?.let { }`**: the safe call captures the value once and passes the non-null into the block — sidestepping the property re-read entirely.","**Fix 3 — Elvis with early return** (`val x = prop ?: return`): pull the value into a non-null local and bail if null.","**Why not just `!!`**: `foo.name!!` works but reintroduces NPE risk and doesn't fix the underlying race — if another thread nulls it between the `!!` evaluation... actually `!!` reads once so it's safe from re-read, but it throws instead of handling, so it's inferior to capturing a local val and handling the null case."]},{t:"p",text:"**The deeper point**: the restriction is protecting you from a real concurrency/getter hazard, not being pedantic. Capturing the value in a local val *snapshots* it, which is both what makes the smart cast sound and what makes the code correct under concurrency. Understanding *why* (races + custom getters) rather than just memorizing 'copy to a local' is the senior-level answer."}]}],Ix={id:"null-safety-types",title:"Null Safety & Types",tagline:"Nullable vs non-null types, ?./?:/!!/let, smart casts, val vs var & immutability, Unit/Nothing/Any, and platform types from Java interop.",tags:["Kotlin","Null Safety","Smart Cast","Platform Types","val/var"],content:Cx,qa:Mx},Ax=[{heading:"data class — value-holding classes with generated methods",blocks:[{t:"p",text:"A **`data class`** is for holding data. The compiler auto-generates the boilerplate you'd otherwise write by hand from the properties in the primary constructor: `equals()`/`hashCode()` (structural equality by content), `toString()` (readable), `copy()` (create a modified duplicate), and `componentN()` functions (for destructuring)."},{t:"code",title:"What a data class gives you",code:`data class User(val id: String, val name: String, val premium: Boolean = false)

val a = User("1", "Revanth")
val b = User("1", "Revanth")
a == b                       // true — structural equality (same content)

val updated = a.copy(premium = true)   // new instance, one field changed
val (id, name) = a                     // destructuring via componentN()
println(a)                             // User(id=1, name=Revanth, premium=false)`},{t:"list",items:["**`equals`/`hashCode` compare by content**, not reference — two data classes with equal fields are equal. This is why they're perfect for UI state (Compose skips recomposition when state is `equals`) and as map keys.","**`copy()`** is the idiomatic way to make a modified version of an immutable object — central to state management (`state.copy(loading = false)`).","**Rules/limits**: needs at least one primary-constructor parameter; only *primary-constructor properties* count toward the generated methods (a property in the body is excluded from equals/hashCode/toString); can't be abstract/open in the usual sense; components come from the constructor order.","**Immutability caveat**: `data class` doesn't force `val` — a `data class` with `var` or mutable collections is a mutable value holder, which breaks `equals`-based caching and is a common bug (prefer `val` + immutable collections)."]}]},{heading:"sealed class / sealed interface — restricted hierarchies",blocks:[{t:"p",text:"A **`sealed`** class or interface defines a *closed* set of subtypes — all direct subclasses must be declared in the same module/package, so the compiler knows *every* possible subtype. The payoff: a `when` over a sealed type can be **exhaustive** — the compiler verifies you've handled every case and doesn't require an `else`, and it *errors* if you add a new subtype without handling it."},{t:"code",title:"Sealed hierarchy + exhaustive when",code:`sealed interface UiState {
    data object Loading : UiState
    data class Success(val data: List<Item>) : UiState
    data class Error(val message: String) : UiState
}

fun render(state: UiState) = when (state) {   // no 'else' needed
    UiState.Loading -> showSpinner()
    is UiState.Success -> showList(state.data)   // smart-cast to Success
    is UiState.Error -> showError(state.message)
    // add a new subtype -> this 'when' fails to compile until handled
}`},{t:"list",items:["**Exhaustiveness is the killer feature**: model your states/results as a sealed type, and the compiler guarantees you handle every case — add a new state later and every `when` that isn't exhaustive breaks the build, pointing you to what to update. Huge for correctness.","**`sealed interface` vs `sealed class`**: interfaces are more flexible (a type can implement multiple sealed interfaces; no constructor). Prefer `sealed interface` unless you need shared state/constructor in the base.","**Ideal for**: UI states (Loading/Success/Error), navigation events, API results (Success/Failure), any 'one of a fixed set of shapes'. Combined with smart casts in `when` branches, you get typed access to each variant's data.","**vs enum**: an enum is a fixed set of *singleton instances* (no per-instance data beyond constructor values shared by all); a sealed type is a fixed set of *subtypes* that can each carry different data and have multiple instances. Use enum for simple constants, sealed for variants-with-data."]}]},{heading:"enum class — a fixed set of constants",blocks:[{t:"code",title:"Enums can hold data and methods",code:`enum class Status(val label: String) {
    ACTIVE("Active"),
    PAUSED("Paused"),
    ARCHIVED("Archived");

    fun isVisible() = this != ARCHIVED
}

Status.ACTIVE.label         // "Active"
Status.values()             // all constants (or entries in newer Kotlin)
Status.valueOf("PAUSED")    // lookup by name`},{t:"list",items:["An `enum` is a fixed set of named constant *instances*, each a singleton. They can have constructor parameters, properties, and methods (even per-constant overrides via anonymous bodies).","`when` over an enum is also exhaustive (no `else` needed if all constants are covered).","Use for a simple closed set of options where each option is just a labeled constant. When options need to carry *different* data shapes, use a sealed type instead."]}]},{heading:"value class (inline class) — zero-overhead wrappers",blocks:[{t:"p",text:"A **`value class`** (formerly `inline class`) wraps a single value to add type safety *without* the runtime cost of an object allocation. At runtime, the compiler **inlines** the wrapper away — a `value class UserId(val value: String)` is represented as just a `String` in most cases, so you get a distinct type at compile time with no boxing overhead."},{t:"code",title:"Type-safe wrappers with no allocation",code:`@JvmInline
value class UserId(val value: String)
@JvmInline
value class Email(val value: String)

fun sendEmail(to: Email, from: UserId) { }

// Compile-time safety — can't swap the arguments:
sendEmail(Email("a@b.com"), UserId("42"))   // OK
// sendEmail(UserId("42"), Email("a@b.com")) // COMPILE ERROR — types don't match
// At runtime, these are just Strings — no wrapper objects allocated`},{t:"list",items:["**The problem it solves**: passing raw `String`/`Int` everywhere loses meaning and lets you mix up arguments (a userId and an orderId are both Strings). Wrapping them in value classes makes the compiler enforce which is which — with *zero* runtime cost (no allocation).","**Requirements**: exactly one property (the wrapped value), `@JvmInline` annotation on the JVM. Can have methods and computed properties, but no `init` state beyond the single value.","**When it *does* box**: the inlining is dropped (a real object is allocated) in cases where a concrete type is needed — e.g. when used as a nullable (`UserId?`), in generics, or stored in a collection of `Any`. Mostly it's free, but not always."]}]},{heading:"object and companion object — singletons and statics",blocks:[{t:"code",title:"object declarations",code:`// object — a singleton (one instance, lazily created, thread-safe)
object Analytics {
    fun track(event: String) { }
}
Analytics.track("open")     // access directly, no instantiation

// companion object — 'static' members tied to a class
class User private constructor(val name: String) {
    companion object {
        fun create(name: String): User = User(name)   // factory
        const val MAX_NAME_LENGTH = 50
    }
}
User.create("Revanth")      // called on the class, like a static method`},{t:"list",items:["**`object`** — a **singleton**: declares a class *and* its single instance at once, created lazily on first access, thread-safely. Use for stateless helpers, singletons, and (Kotlin-idiomatically) things you'd make static utility classes for. `data object` adds a nice `toString`/`equals` (used for singleton sealed variants like `Loading`).","**`companion object`** — Kotlin has no `static`; instead, a class can have *one* companion object holding members accessible via the class name (`User.create()`). Used for factory methods, constants, and anything you'd make static in Java.","**Companion is a real object**: unlike Java statics, a companion object is an actual object instance — it can implement interfaces, be passed as a value, and have extension functions. This makes 'statics' first-class.","**Anonymous objects** (`object : Listener { }`) create one-off instances implementing an interface — Kotlin's version of anonymous classes."]},{t:"note",text:"Quick chooser: **data class** for value holders (auto equals/copy); **sealed** for a closed set of variants-with-data (exhaustive when); **enum** for a fixed set of simple constants; **value class** for zero-cost type-safe wrappers; **object** for singletons; **companion object** for statics/factories."}]}],Ex=[{level:"junior",q:"What is a data class and what does it generate?",a:[{t:"p",text:"**The concept**: a `data class` is designed to hold data, and the compiler auto-generates the boilerplate you'd otherwise write from the properties declared in the primary constructor."},{t:"list",items:["**`equals()` / `hashCode()`** — structural equality: two data classes are equal if their contents match (not their references). This makes them safe as map keys and lets Compose skip recomposition when state is unchanged.","**`toString()`** — a readable representation like `User(id=1, name=Revanth)`.","**`copy()`** — create a new instance with some fields changed: `user.copy(premium = true)`. The idiomatic way to 'modify' an immutable object.","**`componentN()`** — enables destructuring: `val (id, name) = user`."]},{t:"p",text:"Data classes are everywhere in Android — UI state, domain models, API responses — because `equals`-by-content and `copy()` are exactly what immutable state management needs. One caveat: only *primary-constructor* properties count toward the generated methods (a property declared in the class body is excluded), and you should keep them `val` with immutable collections, since a data class with `var` fields breaks the equality-based caching that makes them useful."}]},{level:"junior",q:"What is a sealed class and why is it useful?",a:[{t:"p",text:"**The concept**: a `sealed` class or interface defines a *closed* set of subtypes — all direct subclasses must be in the same module, so the compiler knows every possible subtype at compile time. The main benefit is **exhaustive `when`**: a `when` over a sealed type doesn't need an `else`, the compiler verifies you handled every case, and if you add a new subtype later, every non-exhaustive `when` fails to compile until you handle it."},{t:"code",title:"Modeling UI state",code:`sealed interface Result {
    data class Success(val data: List<Item>) : Result
    data class Error(val message: String) : Result
    data object Loading : Result
}

when (result) {                          // exhaustive — no else
    is Result.Success -> show(result.data)   // smart-cast to Success
    is Result.Error -> showError(result.message)
    Result.Loading -> showSpinner()
}`},{t:"p",text:"It's ideal for anything that's 'one of a fixed set of shapes, each possibly carrying different data' — UI states (Loading/Success/Error), API results, navigation events. The exhaustiveness is a real correctness tool: add a new state and the compiler *points you to every place* that needs updating, instead of you discovering a missed case at runtime. Combined with smart casts, each `when` branch gives you typed access to that variant's data."}]},{level:"junior",q:"What is the difference between a sealed class and an enum?",a:[{t:"p",text:"**Both represent a fixed, closed set of options and both give exhaustive `when` — the difference is what each 'option' can be.** An `enum` is a fixed set of *singleton constants*: each value is one instance, and while an enum can have properties, *all* instances share the same shape (the same constructor parameters). A `sealed` type is a fixed set of *subtypes*: each variant can be a different class carrying *different data*, and you can have multiple instances of each."},{t:"list",items:["**Use an enum** for a simple set of constants where each is just a named value — a `Status` (ACTIVE/PAUSED/ARCHIVED), a day of the week. All the same shape.","**Use a sealed type** when the variants carry *different* data — `Loading` (no data), `Success(val data)`, `Error(val message)`. An enum can't do this because each constant would need the same fields."]},{t:"code",title:"The distinguishing case",code:`// enum — all constants share the same shape
enum class Direction { NORTH, SOUTH, EAST, WEST }

// sealed — each variant has its OWN data shape
sealed interface Payment {
    data class Card(val number: String) : Payment
    data class Cash(val amount: Int) : Payment
    data object Free : Payment
}`},{t:"p",text:"Rule of thumb: if the options differ only by name/label, enum; if they differ by *structure* (different data per option), sealed. Sealed is essentially 'an enum where each case can be a different type'."}]},{level:"junior",q:"What is a companion object and how does it relate to Java's static?",a:[{t:"p",text:"**The concept**: Kotlin has no `static` keyword. Instead, to have members accessible on the class itself (rather than an instance), you put them in a `companion object` — a single special object declared inside the class. You then call them via the class name, just like Java statics: `User.create()`, `User.MAX_LENGTH`."},{t:"code",title:"Factory + constant via companion",code:`class User private constructor(val name: String) {
    companion object {
        const val MAX_LENGTH = 50
        fun create(name: String) = User(name.take(MAX_LENGTH))
    }
}
User.create("Revanth")`},{t:"p",text:"The key difference from Java statics: a companion object is a **real object instance**, not just a namespace. That means it can implement interfaces, be passed around as a value, hold state, and receive extension functions — none of which Java statics can do. So 'statics' in Kotlin are first-class objects. Common uses: factory methods (often with a `private` constructor so the only way to create instances is through the factory), constants (`const val` for compile-time constants), and anything you'd have made static in Java. Each class can have at most one companion object."}]},{level:"junior",q:"What is the difference between a class and an object declaration?",a:[{t:"p",text:"**A `class` is a blueprint you instantiate (possibly many times with `new`/constructor calls); an `object` declaration is a singleton — it declares the class *and* creates its single instance at the same time.** You never instantiate an `object`; you just use it directly by name."},{t:"code",title:"class vs object",code:`class Counter { var count = 0 }         // make instances
val c1 = Counter(); val c2 = Counter()  // two separate instances

object Config {                          // exactly ONE instance, ever
    val baseUrl = "https://api.example.com"
}
Config.baseUrl                           // used directly, no instantiation`},{t:"list",items:["**`object`** is Kotlin's built-in singleton: created lazily on first access, thread-safely, with no boilerplate. Use for stateless utilities, single shared instances, and things you'd make a static utility class for.","**`data object`** adds a sensible `toString`/`equals` — used for singleton sealed variants like `Loading` where you want it to print nicely.","**Anonymous objects** (`object : SomeInterface { }`) are one-off instances, Kotlin's version of Java anonymous classes — common for one-shot listener implementations.","The caution with `object`: since it's a single shared instance, don't put mutable state in it carelessly — that state is global and shared across the whole app (and can leak or cause concurrency issues)."]}]},{level:"senior",q:"What is a value class (inline class) and when would you use one?",a:[{t:"p",text:"**The concept**: a `value class` (annotated `@JvmInline`) wraps a single value to create a *distinct type* at compile time, but the compiler **inlines the wrapper away at runtime** — so there's no object allocation, no boxing overhead. You get type safety for free. `@JvmInline value class UserId(val value: String)` is, at runtime, just a `String` in most cases."},{t:"code",title:"Preventing argument mix-ups with zero cost",code:`@JvmInline value class UserId(val value: String)
@JvmInline value class OrderId(val value: String)

fun cancel(order: OrderId, requestedBy: UserId) { }

cancel(OrderId("o-1"), UserId("u-9"))    // OK
// cancel(UserId("u-9"), OrderId("o-1")) // COMPILE ERROR — can't swap them`},{t:"list",items:["**The problem it solves**: when domain identifiers are all raw `String`/`Int`, you lose meaning and can accidentally pass a userId where an orderId belongs (both are Strings, so the compiler can't catch it). Wrapping each in a value class makes them distinct types the compiler enforces — a whole class of bugs eliminated at *zero runtime cost*.","**vs a regular (data) class wrapper**: a normal wrapper class would allocate an object for every id, adding memory and GC pressure — often unacceptable for pervasive types like ids used in tight loops or large collections. Value classes give the same type safety without the allocation.","**Requirements & limits**: exactly one property (the wrapped value); `@JvmInline` on the JVM. It can have methods and computed properties. The inlining is *dropped* (a real object is boxed) in specific situations — when used as a nullable (`UserId?`), as a generic type argument, or when stored as `Any`/in a collection of interface type — so it's 'usually free', not 'always free'.","**Other uses**: units of measure (`value class Meters(val value: Double)`), validated wrappers (an `Email` value class with validation in `init`), and making primitive-obsessed APIs type-safe."]},{t:"p",text:"**The senior framing**: value classes are how you get the safety and expressiveness of strong domain types (no more 'stringly-typed' code) without paying the traditional wrapper-object tax — the compiler gives you the type at compile time and erases it at runtime. They're underused and a good thing to bring up when discussing modeling domain primitives."}]},{level:"senior",q:"When would you choose a sealed interface over a sealed class, and what are the trade-offs?",a:[{t:"p",text:"**Both create a closed, exhaustive hierarchy — the choice mirrors the general interface-vs-abstract-class decision, plus a few sealed-specific points.** Prefer a **`sealed interface`** by default; use a **`sealed class`** when you need shared *state* or a common constructor in the base type."},{t:"list",items:["**Multiple inheritance of type**: a class can implement *several* sealed interfaces but can only extend *one* sealed class. So sealed interfaces compose — a type can belong to more than one closed hierarchy, which is impossible with sealed classes. This flexibility is the main reason to prefer them.","**No state/constructor in interfaces**: a `sealed interface` can't hold properties with backing fields or have a constructor. If every variant genuinely shares stored state (say a common `timestamp` field with a backing field, or init logic), a `sealed class` with that state in the base is cleaner than repeating it in each variant.","**A variant can be a `data class`, `data object`, or regular class under either** — that's independent of whether the parent is a sealed class or interface. So you don't lose data-class benefits either way.","**Interoperability & design**: interfaces impose fewer constraints on implementers (no forced single-inheritance slot), which keeps the hierarchy open to future composition. This is the same 'prefer interfaces for flexibility' principle from general OO design."]},{t:"code",title:"When shared state pushes you to a sealed class",code:`// sealed interface — no shared state needed, most flexible
sealed interface NetworkResult {
    data class Success(val body: String) : NetworkResult
    data class Failure(val code: Int) : NetworkResult
}

// sealed class — genuine shared state/behavior in the base
sealed class Screen(val route: String) {   // every screen HAS a route
    data object Home : Screen("home")
    data class Detail(val id: String) : Screen("detail/$id")
}`},{t:"p",text:"**Rule of thumb**: default to `sealed interface` (flexible, composable, no downsides for most modeling); switch to `sealed class` only when the variants share stored state or constructor logic that's cleaner to centralize in the base. The exhaustiveness benefit is identical for both — the choice is purely about whether you need base-level state and single vs multiple inheritance."}]}],Px={id:"classes",title:"Classes & Objects",tagline:"data classes, sealed classes/interfaces & exhaustive when, enums, value/inline classes, object singletons, and companion objects.",tags:["Kotlin","data class","sealed","value class","companion object"],content:Ax,qa:Ex},Rx=[{heading:"Higher-order functions and lambdas",blocks:[{t:"p",text:"Kotlin treats functions as **first-class**: you can pass them as arguments, return them, and store them in variables. A **higher-order function** is one that takes or returns a function. A **lambda** is a function literal — an unnamed function you write inline, like `{ x -> x * 2 }`."},{t:"code",title:"Function types, lambdas, and passing them around",code:`// A function type: (Int) -> Int  means 'takes Int, returns Int'
fun transform(x: Int, op: (Int) -> Int): Int = op(x)

transform(5) { it * 2 }          // lambda; 'it' is the single implicit param -> 10
transform(5, ::square)            // pass a named function by reference

// Storing a lambda in a variable
val adder: (Int, Int) -> Int = { a, b -> a + b }
adder(2, 3)                       // 5`},{t:"list",items:["**Function type syntax**: `(A, B) -> R` — parameter types in parens, return type after the arrow. `() -> Unit` is a no-arg function returning nothing.","**`it`**: when a lambda has a single parameter, you can omit the declaration and refer to it as `it` (`list.map { it.name }`).","**Trailing lambda**: if the last parameter is a function, you can move the lambda *outside* the parentheses — `transform(5) { it * 2 }`. This is what makes DSLs and things like `launch { }` read naturally.","**Function references**: `::square` passes a named function; `String::length` a member reference; `user::save` a bound reference. Cleaner than wrapping in a lambda when you're just forwarding."]}]},{heading:"Extension functions — adding methods to existing types",blocks:[{t:"p",text:"An **extension function** lets you add a function to an existing type *without modifying it or subclassing it* — even to types you don't own (`String`, `View`, library classes). Inside the function, `this` refers to the receiver (the object it's called on)."},{t:"code",title:"Extending types you don't own",code:`fun String.isValidEmail(): Boolean =
    contains("@") && contains(".")

"a@b.com".isValidEmail()          // true — called like a member

fun View.hide() { visibility = View.GONE }   // extend Android's View
myButton.hide()`},{t:"list",items:["**How it works**: extensions are *statically resolved* — the compiler turns `\"x\".isValidEmail()` into a static call passing the string as the receiver. They don't actually modify the class or add anything to it at runtime; there's no dynamic dispatch.","**Consequence — no polymorphism**: because they're static, extensions are resolved by the *declared* type, not the runtime type. An extension on `Animal` won't be overridden by one on `Dog` based on the actual object — the *static* type at the call site decides which runs.","**Can't access private members** of the type they extend (they're outside the class), and **a member function wins** if there's a name clash (members take precedence over extensions).","**Uses**: utility functions that read naturally (`list.sumOf { }`), scoped helpers, making APIs fluent, and Kotlin's entire standard library (`map`, `filter`, `let` are all extension functions)."]}]},{heading:"inline functions — eliminating lambda overhead",blocks:[{t:"p",text:"Every lambda you pass is normally compiled into an **object** (a `Function` instance) — a small allocation per call. For higher-order functions called frequently, that adds up. The **`inline`** keyword tells the compiler to *copy the function's body (and the lambda's body) directly into the call site*, eliminating the function call and the lambda object entirely."},{t:"code",title:"inline removes the lambda allocation",code:`inline fun measure(block: () -> Unit) {
    val start = System.nanoTime()
    block()                        // body inlined at the call site — no lambda object
    println(System.nanoTime() - start)
}

// Standard library uses inline everywhere: let, run, with, apply, also,
// map, filter, forEach — so lambdas passed to them cost nothing.`},{t:"list",items:["**Benefit**: no lambda object allocated, no function-call overhead — the code is pasted inline. This is why `list.forEach { }` is as cheap as a `for` loop, and why scope functions (`let`, `apply`) are free.","**Enables non-local returns**: because the lambda body is inlined, a `return` inside a lambda passed to an inline function returns from the *enclosing* function — impossible with non-inline lambdas.","**Enables `reified` type parameters**: inline functions can use `reified T`, making the type available at runtime (see the generics topic) — e.g. `inline fun <reified T> Gson.fromJson(json: String): T`.","**Trade-offs / when NOT to inline**: inlining *duplicates* the body at every call site, so inlining a *large* function called in many places bloats the bytecode. Inline is for *small* higher-order functions where the lambda-overhead saving matters — not big functions. Use `noinline` (to exclude a specific lambda parameter) and `crossinline` (to forbid non-local returns from a lambda that's called from another context) for finer control."]}]},{heading:"The five scope functions: let, run, with, apply, also",blocks:[{t:"p",text:"Scope functions execute a block *in the context of an object*, differing in two dimensions: (1) how they refer to the object — as **`it`** (a lambda parameter) or **`this`** (the receiver), and (2) what they **return** — the object itself or the lambda's result. This table is worth memorizing:"},{t:"table",headers:["Function","Object referred to as","Returns","Typical use"],rows:[["`let`","`it`","lambda result","null-safe ops, transform, scoping a variable"],["`run`","`this`","lambda result","compute a result using the object; config + return"],["`with`","`this` (arg, not extension)","lambda result","call multiple methods on an object"],["`apply`","`this`","the object itself","configure an object then return it (builders)"],["`also`","`it`","the object itself","side effects (logging) in a chain, keep the object"]]},{t:"code",title:"Each scope function in its idiomatic use",code:`// let — null-safe transform; returns the result
val length = nullableStr?.let { it.trim().length }

// apply — configure and return the SAME object (builder-style)
val intent = Intent(this, MainActivity::class.java).apply {
    putExtra("id", 42)
    flags = Intent.FLAG_ACTIVITY_NEW_TASK
}   // returns the configured Intent

// also — side effect without breaking the chain; returns the object
val user = repository.load().also { Log.d("TAG", "loaded $it") }

// run — execute a block with 'this' and return the result
val area = rectangle.run { width * height }

// with — group calls on an object (not an extension; takes it as arg)
with(canvas) { drawRect(...); drawText(...) }`},{t:"list",items:["**Choose by return + reference**: need the *object back* → `apply` (this) or `also` (it). Need the *lambda's result* → `let`/`run` (this) or `with`. Doing null-safe work → `?.let`. Configuring a builder → `apply`. Logging in a chain → `also`.",'**`apply` vs `also`**: both return the object; `apply` uses `this` (good for setting properties: `apply { name = "x" }`), `also` uses `it` (good for side effects on the whole object: `also { log(it) }`).',"**`let` vs `run`**: both return the lambda result; `let` uses `it` (good with `?.` and for renaming/scoping), `run` uses `this` (good for calling the object's own methods).","**Don't overuse them**: nested scope functions (`apply` inside `let` inside `also`) become unreadable. Reach for a plain variable when clarity beats conciseness."]},{t:"note",text:"The memory hook: **`apply`/`also` return the object** (for chaining/config), **`let`/`run`/`with` return the result** (for computing). **`let`/`also` use `it`**, **`run`/`with`/`apply` use `this`**. All are `inline`, so they cost nothing at runtime."}]},{heading:"Default & named arguments, varargs, and more",blocks:[{t:"list",items:['**Default arguments**: parameters can have defaults (`fun greet(name: String = "Guest")`), removing the need for Java-style method overloads. Combined with **named arguments** (`greet(name = "Revanth")`), you get flexible, readable calls and can skip middle parameters.',"**`vararg`**: a variable number of arguments (`fun sum(vararg nums: Int)`); spread an existing array with `*` (`sum(*array)`).","**Infix functions**: `infix fun` allows calling without dot/parens (`1 to 2`, `x shl 3`) — for DSL-like readability.","**Local functions**: functions defined *inside* other functions, with access to the outer scope — for extracting repeated logic without polluting the class namespace.","**Single-expression functions**: `fun square(x: Int) = x * x` — omit braces and `return` for one-liners; type inferred.","**Operator overloading**: `operator fun plus(...)` lets `+`, `[]`, `in`, etc. work on your types (`operator fun get(i: Int)` enables `obj[i]`)."]}]}],Lx=[{level:"junior",q:"What is a higher-order function and a lambda?",a:[{t:"p",text:"**The concept**: Kotlin treats functions as first-class values — you can pass them as arguments, return them, and store them in variables. A **higher-order function** is one that takes a function as a parameter or returns one. A **lambda** is a function literal: an unnamed function written inline, like `{ x -> x * 2 }`."},{t:"code",title:"A higher-order function taking a lambda",code:`fun repeat(times: Int, action: (Int) -> Unit) {
    for (i in 0 until times) action(i)
}
repeat(3) { i -> println(i) }    // lambda passed as the action
repeat(3) { println(it) }         // single param can be 'it'`},{t:"p",text:"The function type `(Int) -> Unit` describes 'takes an Int, returns nothing'. When a lambda's last parameter is a function, you can write it *outside* the parentheses (a 'trailing lambda') — which is why `launch { }`, `forEach { }`, and DSLs read so naturally. Higher-order functions plus lambdas are the foundation of Kotlin's expressive standard library (`map`, `filter`, `let`) and of coroutine/Compose APIs."}]},{level:"junior",q:"What is an extension function and how does it work?",a:[{t:"p",text:"**The concept**: an extension function adds a function to an existing type *without modifying that type or subclassing it* — even for types you don't own like `String` or Android's `View`. You call it like a normal member, and inside it `this` refers to the object it was called on (the 'receiver')."},{t:"code",title:"Extending String",code:`fun String.toTitleCase(): String =
    split(" ").joinToString(" ") { it.replaceFirstChar(Char::uppercase) }

"hello world".toTitleCase()      // "Hello World"`},{t:"p",text:"**How it works under the hood**: extensions don't actually modify the class — they're *statically resolved*. The compiler turns `\"hi\".toTitleCase()` into a static function call passing the string as an argument. This has an important consequence: because it's static, an extension is dispatched by the *declared* type at the call site, not the runtime type — so extensions aren't polymorphic (an extension on `Animal` isn't 'overridden' by one on `Dog` based on the actual object). They also can't access private members of the type they extend, and if a real member function has the same name, the member wins. Despite these limits, they're everywhere in Kotlin — most of the standard library (`map`, `filter`, `let`) is extension functions."}]},{level:"junior",q:"What are the scope functions and how do you choose between them?",a:[{t:"p",text:"**The concept**: the five scope functions (`let`, `run`, `with`, `apply`, `also`) all execute a block *in the context of an object*. They differ in two ways: how they refer to the object (as `it` or as `this`), and what they return (the object itself, or the block's result)."},{t:"table",headers:["Function","Refers as","Returns"],rows:[["`let`","`it`","block result"],["`run`","`this`","block result"],["`apply`","`this`","the object"],["`also`","`it`","the object"],["`with`","`this`","block result"]]},{t:"list",items:["**`let`** — null-safe operations and transformations: `value?.let { transform(it) }`. Uses `it`, returns the result.","**`apply`** — configure an object and get it back (builder pattern): `Intent().apply { putExtra(...) }`. Uses `this`, returns the object.","**`also`** — a side effect (like logging) in a chain without breaking it: `load().also { log(it) }`. Uses `it`, returns the object.","**`run`** — compute a result using the object's members: `rect.run { width * height }`. Uses `this`, returns the result.","**`with`** — group multiple calls on an object: `with(canvas) { drawX(); drawY() }`. Takes the object as an argument, returns the result."]},{t:"p",text:"The decision: need the *object back* → `apply` (set properties via `this`) or `also` (side effect via `it`); need the *block's result* → `let`/`run`/`with`. They're all `inline`, so they compile away with zero runtime cost. The main pitfall is overusing them — deeply nested scope functions hurt readability, so a plain variable is sometimes clearer."}]},{level:"junior",q:"What is the difference between let and apply?",a:[{t:"p",text:"**Both run a block on an object, but they differ in what they return and how they reference the object.** `let` returns the *block's result* and refers to the object as `it`. `apply` returns *the object itself* and refers to it as `this`."},{t:"code",title:"The contrast",code:`// let — transform and return a NEW result; object is 'it'
val nameLength: Int? = user?.let { it.name.length }

// apply — configure the SAME object and return it; object is 'this'
val user = User().apply {
    name = "Revanth"        // 'this.name', can omit 'this'
    age = 25
}   // returns the configured User`},{t:"list",items:["**Use `let`** when you want to *do something with* an object and get a *different result* — especially null-safe transforms (`?.let { }`). The `it` reference makes it read like 'take this value and produce that'.","**Use `apply`** when you want to *configure* an object (set several properties) and get *the object back* — the builder pattern. The `this` reference lets you set properties directly (`name = ...` instead of `it.name = ...`)."]},{t:"p",text:"Mnemonic: `apply` = 'apply this configuration to the object and return the object'; `let` = 'let me compute something from this object and return that'. If you find yourself writing `.apply { }` but then using the block's result, you wanted `run`; if you write `.let { }` just to configure and return the object, you wanted `apply`."}]},{level:"senior",q:"What does the inline keyword do, and what are its benefits and costs?",a:[{t:"p",text:"**The concept**: normally, a lambda passed to a function is compiled into an *object* (a `Function` instance) — a small heap allocation per call, plus the overhead of the function call itself. Marking a higher-order function `inline` tells the compiler to *copy the function's body — and the bodies of its lambda parameters — directly into the call site*, eliminating both the function call and the lambda object."},{t:"list",items:["**Benefit 1 — no allocation/call overhead**: the lambda isn't turned into an object; the code is pasted inline. This is why `list.forEach { }` is as cheap as a `for` loop and why scope functions (`let`, `apply`) cost nothing. For higher-order functions called in hot paths, this is a real performance win.","**Benefit 2 — non-local returns**: because the lambda body is inlined into the caller, a `return` inside a lambda passed to an inline function returns from the *enclosing* function (not just the lambda). Non-inline lambdas can't do this.","**Benefit 3 — reified type parameters**: only inline functions can have `reified` type parameters, which makes the generic type available at runtime (e.g. `inline fun <reified T> parse(json: String): T` can call `T::class`). This is impossible without inlining because of type erasure."]},{t:"list",items:["**The cost — code bloat**: inlining *duplicates* the function body at every call site. Inline a *large* function called in *many* places and you multiply its bytecode everywhere, increasing method size and APK size. So `inline` is meant for *small* higher-order functions where the per-call lambda overhead actually matters — not big functions, and not functions without lambda parameters (the compiler even warns that inlining without a functional parameter is pointless).","**Fine-grained control**: `noinline` on a specific lambda parameter excludes it from inlining (e.g. when you need to store or pass that lambda elsewhere, which inlined lambdas can't be); `crossinline` marks a lambda that's inlined but must *not* do non-local returns (because it's invoked from a different execution context, like inside another lambda or object)."]},{t:"p",text:"**The senior framing**: `inline` trades bytecode size for eliminating lambda allocation and call overhead, and unlocks non-local returns and reified generics. Use it for small, frequently-called higher-order functions (which is exactly what the standard library does); avoid it for large functions where the duplication outweighs the savings. Blindly adding `inline` everywhere is an anti-pattern — it can *increase* size and even hurt performance via instruction-cache pressure."}]},{level:"senior",q:"Extension functions are statically resolved — what does that mean and what surprising behavior does it cause?",a:[{t:"p",text:"**Static resolution means the extension to call is decided at *compile time* based on the *declared* (static) type of the expression, not the *runtime* type of the object.** An extension function isn't really added to the class — the compiler rewrites `x.ext()` into a static call `ext(x)`, choosing which `ext` by the type it *thinks* `x` is at that line, not by what `x` actually is at runtime. This is the opposite of member functions, which are dispatched dynamically (virtually) by the runtime type."},{t:"code",title:"The surprising non-polymorphic behavior",code:`open class Animal
class Dog : Animal()

fun Animal.speak() = "generic sound"
fun Dog.speak() = "woof"

val pet: Animal = Dog()      // declared type Animal, runtime type Dog
println(pet.speak())         // prints "generic sound" — NOT "woof"!
// Resolved by the DECLARED type (Animal), ignoring the runtime Dog.`},{t:"list",items:["**The surprise**: even though `pet` is really a `Dog`, `pet.speak()` calls the `Animal` extension, because the compiler resolved it by `pet`'s declared type (`Animal`). If `speak()` were a *member* function overridden in `Dog`, it would print 'woof' (dynamic dispatch). Extensions have no polymorphism.","**Member wins over extension**: if the class already has a member function with the same signature, the member is always called — an extension can never override a member. So you can't use extensions to 'patch' existing behavior.","**No access to private members**: since extensions live outside the class, they can only use its public/internal API, not private fields.","**Practical implications**: don't rely on extensions for polymorphic behavior — if you need runtime dispatch, use a member function or an interface. Extensions are best for *stateless utility* on a type where the declared type is unambiguous (like extending `String` or `List`), not for behavior that should vary by subtype."]},{t:"p",text:"**Why it works this way**: extensions are a compile-time convenience (syntactic sugar over static helper functions), deliberately *not* modifying the class — so they can't participate in the virtual dispatch table. Knowing this prevents a genuinely confusing class of bugs where an extension 'should' be overridden but isn't, and it demonstrates you understand extensions are static helpers dressed up as members, not real methods."}]}],Dx={id:"functions-lambdas",title:"Functions, Lambdas & Scope Functions",tagline:"Higher-order functions & lambdas, extension functions (static resolution), inline/reified, the five scope functions, and default/named args.",tags:["Kotlin","Lambdas","Extensions","inline","Scope Functions"],content:Rx,qa:Lx},Vx=[{heading:"Read-only vs mutable collections",blocks:[{t:"p",text:"Kotlin splits collections into **read-only** and **mutable** interfaces. `List<T>`, `Set<T>`, `Map<K,V>` are read-only (no add/remove methods). `MutableList<T>`, `MutableSet<T>`, `MutableMap<K,V>` add modification methods. You choose the type by whether callers should be able to modify it."},{t:"code",title:"Creating collections",code:`val readOnly = listOf(1, 2, 3)          // List<Int> — no add/remove
val mutable = mutableListOf(1, 2, 3)   // MutableList<Int> — can modify
mutable.add(4)

val map = mapOf("a" to 1, "b" to 2)    // read-only Map
val mmap = mutableMapOf<String, Int>() // mutable

val set = setOf(1, 2, 2, 3)            // Set — duplicates removed -> {1,2,3}`},{t:"list",items:["**Read-only is not immutable**: `List` is an *interface* without mutators, but the underlying object might still be a mutable list held elsewhere. A `val list: List<Int>` prevents *you* from modifying it through that reference, but doesn't guarantee the data can't change via another reference. For true immutability use `kotlinx.collections.immutable` (`persistentListOf`).","**Expose read-only, use mutable internally**: the idiom is to keep a `private val _items = mutableListOf()` and expose `val items: List<T> = _items` — so only the owner mutates. (Same principle as the StateFlow backing-property pattern.)","**Default to read-only**: prefer `listOf`/`mapOf` unless you specifically need mutation — it communicates intent and prevents accidental modification."]}]},{heading:"The functional operators — map, filter, and friends",blocks:[{t:"code",title:"The everyday transformations",code:`val users = listOf(User("A", 25), User("B", 17), User("C", 30))

users.map { it.name }                    // [A, B, C] — transform each
users.filter { it.age >= 18 }            // adults only
users.first { it.age > 20 }              // first match (throws if none)
users.firstOrNull { it.age > 100 }       // null if none
users.any { it.age < 18 }                // true — at least one matches
users.all { it.age > 0 }                 // true — every element matches
users.count { it.age >= 18 }             // 2
users.sortedBy { it.age }                // sorted copy
users.groupBy { it.age >= 18 }           // Map<Boolean, List<User>>
users.associateBy { it.name }            // Map<String, User> keyed by name
users.sumOf { it.age }                   // 72
users.maxByOrNull { it.age }             // User C
users.partition { it.age >= 18 }         // Pair<adults, minors>
users.flatMap { it.tags }                // flatten nested lists`},{t:"list",items:["**These are extension functions on `Iterable`** returning *new* collections — the originals are never modified (functional style). `map`/`filter` are the core; the rest cover common needs (grouping, aggregation, searching).","**`map` vs `flatMap`**: `map` is 1:1 (each element → one result); `flatMap` is 1:many flattened (each element → a list, all concatenated).","**`associate`/`associateBy`/`groupBy`**: build maps from lists — `associateBy { it.id }` makes a lookup map, `groupBy { it.category }` buckets elements.","**`first`/`find`/`single`**: `first { }` throws if no match, `firstOrNull`/`find` returns null, `single { }` requires exactly one."]}]},{heading:"The eager evaluation problem",blocks:[{t:"p",text:"Standard collection operators are **eager**: each one processes the *entire* collection and creates a *new intermediate collection* before the next operator runs. Chaining several means allocating a full list at each step and iterating multiple times — fine for small collections, wasteful for large ones."},{t:"code",title:"Eager: multiple passes and intermediate lists",code:`val result = hugeList
    .map { it * 2 }        // creates a full new list (pass 1)
    .filter { it > 10 }    // creates ANOTHER full list (pass 2)
    .take(5)               // creates ANOTHER list, then takes 5 (pass 3)
// Even though we only wanted 5 items, map ran over the ENTIRE list first.`},{t:"p",text:"In that example, `map` transforms all million elements, then `filter` scans all of them, then `take(5)` keeps just 5 — we did a million transforms and a million filters to get 5 results, plus two throwaway intermediate lists. That's the eager evaluation cost."}]},{heading:"Sequences — lazy evaluation",blocks:[{t:"p",text:"A **`Sequence`** evaluates **lazily**: operators aren't applied until a *terminal* operation pulls values, and elements flow through the *whole chain one at a time* rather than the whole collection through each operator. No intermediate collections, and short-circuiting operators like `take`/`first` stop early. It's the collections analog of Kotlin Flow (Sequence is to collections what Flow is to async streams)."},{t:"code",title:"Lazy: one element at a time, short-circuits",code:`val result = hugeList.asSequence()
    .map { it * 2 }        // lazy — not run yet
    .filter { it > 10 }    // lazy
    .take(5)               // lazy
    .toList()              // TERMINAL — now it runs

// Now each element flows through map->filter one at a time, and the
// whole thing STOPS as soon as 5 pass the filter. No intermediate lists.`},{t:"list",items:["**How it differs**: eager processes *operator by operator* (all elements through map, then all through filter); lazy processes *element by element* (element 1 through the whole chain, then element 2…), stopping when a terminal like `take(5)`/`first` is satisfied.","**Terminal vs intermediate**: sequence operators are intermediate (lazy) until a terminal operation (`toList`, `first`, `sum`, `count`) triggers evaluation — exactly like Flow.","**When sequences win**: large collections, long operator chains, and short-circuiting (`take`, `first`, `any`) where you don't need to process everything. They avoid intermediate allocations and can stop early.","**When they DON'T help (or hurt)**: small collections — the lazy machinery has per-element overhead that outweighs the benefit, so eager is actually faster for small data. Don't reflexively `.asSequence()` everything."]}]},{heading:"Collections vs Sequences — the decision",blocks:[{t:"table",headers:["","Collection (eager)","Sequence (lazy)"],rows:[["Evaluation","each operator processes all elements immediately","elements flow through the whole chain lazily"],["Intermediate collections","one per operator","none"],["Passes over data","one per operator","single pass, element by element"],["Short-circuits (take/first)","no — earlier operators already ran fully","yes — stops as soon as satisfied"],["Best for","small collections, few operators","large data, long chains, early termination"]]},{t:"note",text:"Rule of thumb: use plain collection operators by default (simpler, faster for small data). Switch to `.asSequence()` when the collection is large *and* you have multiple chained operations *or* can short-circuit (take/first/any) — that's when avoiding intermediate lists and early termination pays off. For small lists, sequences can be slower due to overhead."}]},{heading:"Other collection essentials",blocks:[{t:"list",items:["**`Array` vs `List`**: `Array<T>` is a fixed-size, mutable, JVM-array-backed structure (use for performance-critical fixed data or interop); `List` is the general-purpose interface. Prefer `List` unless you need array semantics. Specialized `IntArray`/`FloatArray` avoid boxing.","**Destructuring**: `for ((key, value) in map) { }`, `val (a, b) = pair` — works with data classes, pairs, and map entries via `componentN()`.","**`zip`/`unzip`**: `listA zip listB` pairs elements; useful for combining parallel lists.","**`fold`/`reduce`**: accumulate to a single value — `fold(0) { acc, x -> acc + x }` (with initial value), `reduce { acc, x -> ... }` (uses first element as initial, throws on empty).","**`windowed`/`chunked`**: `chunked(3)` splits into groups of 3; `windowed(2)` creates sliding windows — handy for batching and pairwise processing.","**Null handling in collections**: `filterNotNull()`, `mapNotNull { }` (map and drop nulls in one step), `firstNotNullOfOrNull { }`."]}]}],jx=[{level:"junior",q:"What is the difference between List and MutableList?",a:[{t:"p",text:"**The concept**: Kotlin separates collections into read-only and mutable interfaces. `List<T>` is read-only — it has access methods (`get`, `size`, `contains`) but *no* methods to add or remove elements. `MutableList<T>` extends it with mutation methods (`add`, `remove`, `clear`). You pick the type based on whether the code holding it should be able to modify it."},{t:"code",title:"The two types",code:`val ro: List<Int> = listOf(1, 2, 3)          // no add/remove
val mut: MutableList<Int> = mutableListOf(1) // can modify
mut.add(2)`},{t:"p",text:"The important nuance: **read-only is not the same as immutable.** `List` is just an *interface* without mutators — but the actual object could be a `MutableList` referenced elsewhere and changed through that other reference. So `val list: List<Int>` prevents modification *through that variable*, not modification of the data entirely. The idiomatic pattern for safe encapsulation is to keep a private mutable collection and expose it as read-only (`private val _items = mutableListOf(); val items: List<T> = _items`), so only the owner can mutate. For genuine immutability you'd use `kotlinx.collections.immutable`. Default to read-only types unless you actually need mutation."}]},{level:"junior",q:"Explain map, filter, and a few other common collection operators.",a:[{t:"list",items:["**`map { }`** — transform each element into a new one, returning a new list: `users.map { it.name }` → list of names.","**`filter { }`** — keep only elements matching a predicate: `users.filter { it.age >= 18 }`.","**`first { }` / `firstOrNull { }` / `find { }`** — get the first matching element; `first` throws if none, `firstOrNull`/`find` return null.","**`any { }` / `all { }` / `none { }`** — boolean checks: at least one matches / all match / none match.","**`groupBy { }` / `associateBy { }`** — build maps: `groupBy` buckets elements by a key into lists; `associateBy` makes a lookup map keyed by a property.","**`sumOf { }` / `maxByOrNull { }` / `sortedBy { }`** — aggregate, find extremes, sort (returning a new sorted list)."]},{t:"p",text:"These are extension functions on `Iterable` that return *new* collections without modifying the original — functional style. They compose into readable pipelines: `users.filter { it.active }.sortedBy { it.name }.map { it.email }`. The one thing to keep in mind is that each operator on a regular collection is *eager* — it processes the whole collection and creates a new intermediate list — which matters for large data (where sequences help) but is fine for typical small lists."}]},{level:"junior",q:"What is the difference between map and flatMap?",a:[{t:"p",text:"**`map` is one-to-one; `flatMap` is one-to-many, flattened.** `map { }` transforms each element into exactly one result, giving a list of the same size. `flatMap { }` transforms each element into a *list* (or iterable), then concatenates all those lists into one flat result."},{t:"code",title:"The difference",code:`val users = listOf(
    User("A", tags = listOf("x", "y")),
    User("B", tags = listOf("z")),
)

users.map { it.tags }        // [[x, y], [z]]  — a list of lists
users.flatMap { it.tags }    // [x, y, z]       — flattened into one list`},{t:"p",text:"Use `map` when each element becomes one new element (names from users). Use `flatMap` when each element expands into *multiple* elements you want combined — like collecting all tags across all users into a single list, or all order-items across all orders. It's essentially `map` followed by `flatten`. This is the collections analog of `flatMapConcat` in Flow — the same 'each element produces a collection, merge them all' idea."}]},{level:"junior",q:"What is a Sequence and how is it different from a List?",a:[{t:"p",text:"**The concept**: a `List`'s operators are *eager* — each one (`map`, `filter`) processes the whole list immediately and produces a new intermediate list. A `Sequence`'s operators are *lazy* — nothing runs until a terminal operation (like `toList()` or `first()`), and then elements flow through the *entire chain one at a time* instead of the whole collection passing through each operator."},{t:"code",title:"Lazy vs eager",code:`// List (eager): map processes all, then filter processes all, then take
list.map { f(it) }.filter { g(it) }.take(5)

// Sequence (lazy): each element flows through map->filter->take;
// stops as soon as 5 elements pass
list.asSequence().map { f(it) }.filter { g(it) }.take(5).toList()`},{t:"p",text:"The practical difference: with a List, a chain of operators creates a new intermediate collection at each step and iterates multiple times. With a Sequence, there are no intermediate collections, it's a single pass, and short-circuiting operators like `take(5)`/`first` can *stop early* without processing the rest. So sequences shine for large collections with long operator chains or early termination. But for *small* collections, sequences are actually slower (the lazy machinery has per-element overhead), so you shouldn't convert everything to sequences reflexively. It's essentially the collections version of Kotlin Flow."}]},{level:"senior",q:"When should you use a Sequence over regular collection operators, and when would it hurt?",a:[{t:"p",text:"**The decision comes down to collection size, chain length, and whether you can short-circuit.** The costs sequences avoid — intermediate collection allocations and multiple passes — only matter when there's enough data and enough operators for those costs to be significant. Sequences also add a *fixed per-element overhead* (each element is pushed through the operator chain via iterator calls), which can dominate for small data."},{t:"list",items:["**Use a Sequence when**: (1) the collection is *large*, (2) you chain *multiple* operators (each intermediate list you avoid is a real saving), or (3) you can *short-circuit* — `take(n)`, `first { }`, `any { }` — so lazy evaluation stops early instead of processing everything. The classic win: `hugeList.asSequence().map { }.filter { }.first { }` processes only until the first match, whereas the eager version transforms and filters the *entire* list first.","**Sequences HURT when**: the collection is *small* (dozens of elements) — the lazy iterator overhead per element outweighs the avoided allocations, so plain operators are measurably faster. Also when you have a *single* operator (no intermediate list to avoid) or need the result materialized anyway.","**A subtle correctness point**: some operations (like `sorted`) are inherently *stateful* — they must see all elements, so within a sequence they materialize the whole thing internally, negating the laziness up to that point. Sequences help most with *stateless* operations (map/filter/take)."]},{t:"code",title:"The clearest win — early termination on large data",code:`// EAGER: transforms and filters ALL million items to find one
millionItems.map { expensive(it) }.filter { it.valid }.first()

// LAZY: stops at the first valid item — maybe processes only a handful
millionItems.asSequence().map { expensive(it) }.filter { it.valid }.first()`},{t:"p",text:"**The senior takeaway**: don't cargo-cult `.asSequence()`. The heuristic is 'large collection + multiple operators, or short-circuiting'. For a list of 20 UI items with two operators, plain collection functions are simpler and faster. For a million-element pipeline with a `first`/`take`, sequences can be dramatically faster. Measure if unsure — the crossover point depends on element count and operator cost."}]},{level:"senior",q:"'Read-only' collections aren't truly immutable. Explain the risk and how to get real immutability.",a:[{t:"p",text:"**The subtlety**: `List<T>` is a read-only *interface* — it lacks mutator methods, so you can't modify the collection *through a `List` reference*. But the underlying object at runtime might actually be a `MutableList`, and if another reference to it exists as `MutableList`, that reference can still change the data — and your `List` view reflects the change. Read-only means 'this view can't mutate', not 'this data can never change'."},{t:"code",title:"The read-only view can change under you",code:`val mutable = mutableListOf(1, 2, 3)
val readOnly: List<Int> = mutable          // a read-only VIEW of the same object

mutable.add(4)                              // mutate via the mutable reference
println(readOnly)                           // [1, 2, 3, 4] — the view changed!`},{t:"list",items:["**The risk in practice**: you expose a `List` from a class thinking it's safe, but you kept a `MutableList` internally and hand out the *same object* upcast to `List`. If the caller downcasts (`readOnly as MutableList`) or if the internal mutation happens while the caller iterates, you get surprises — including `ConcurrentModificationException` or, in Compose, stale UI because the 'immutable' state actually mutated in place (breaking equality-based skipping).","**Real immutability option 1 — defensive copy**: expose `_items.toList()`, which creates a genuinely separate list. Safe, but copies on every access (cost for large/frequent).","**Real immutability option 2 — `kotlinx.collections.immutable`**: `persistentListOf()` / `toImmutableList()` give truly immutable collections (`ImmutableList`) that *cannot* be mutated by anyone, with efficient structural sharing for 'modifications' (they return new versions). These are also what Compose recognizes as stable for recomposition skipping.","**Real immutability option 3 — don't share the mutable reference**: keep the `MutableList` strictly private and only ever expose copies or immutable types, never the same object upcast."]},{t:"p",text:"**Why it matters for Android specifically**: Compose stability relies on immutability — an `@Immutable`/`@Stable` data class holding a `List` is only safe if that list truly never mutates; a read-only view of a mutable list breaks that promise and causes stale UI. So for Compose state, prefer `ImmutableList` (compiler-recognized) or defensive copies over exposing read-only views of internally-mutable collections. The general principle: read-only is an *access restriction*, immutability is a *data guarantee* — know which one you actually have."}]}],Ox={id:"collections-sequences",title:"Collections & Sequences",tagline:"Read-only vs mutable collections, the functional operators, eager vs lazy evaluation, Sequences and when they help (or hurt), and real immutability.",tags:["Kotlin","Collections","Sequences","map/filter","Immutability"],content:Vx,qa:jx},Ux=[{heading:"Generics — writing type-safe reusable code",blocks:[{t:"p",text:"**Generics** let you write code that works with *any* type while keeping full type safety. Instead of a `Box` that holds `Any` (and needs casting), a `Box<T>` holds a specific type `T` chosen at the use site — `Box<String>` holds strings, `Box<Int>` holds ints, and the compiler enforces it. The `T` is a **type parameter** — a placeholder filled in when the type is used."},{t:"code",title:"Generic class and function",code:`class Box<T>(val value: T) {
    fun get(): T = value
}
val stringBox = Box("hello")     // Box<String> — T inferred as String
val intBox = Box(42)             // Box<Int>

// Generic function
fun <T> firstOrNull(list: List<T>): T? = if (list.isEmpty()) null else list[0]

// Type constraints (upper bounds): T must be Comparable
fun <T : Comparable<T>> max(a: T, b: T): T = if (a > b) a else b`},{t:"list",items:["**Type safety without casting**: `stringBox.get()` returns a `String`, not `Any` — no cast, no `ClassCastException` risk.","**Type constraints (`T : Bound`)**: restrict `T` to subtypes of a bound so you can use that bound's methods — `<T : Comparable<T>>` lets you use `>`. Multiple bounds use a `where` clause.",'**Type inference**: you rarely write the type argument explicitly (`Box("hi")` infers `Box<String>`); specify it when inference can\'t (`emptyList<String>()`).']}]},{heading:"Type erasure — generics are compile-time only",blocks:[{t:"p",text:"On the JVM, generics use **type erasure**: the type argument exists at *compile time* for type-checking but is *erased* at runtime. At runtime, a `List<String>` and a `List<Int>` are both just `List` — the JVM doesn't know the element type. This is inherited from Java's generics and has real consequences."},{t:"code",title:"What erasure prevents",code:`// CAN'T check a generic type at runtime — the type arg is erased:
if (list is List<String>) { }    // ERROR: cannot check erased type
if (list is List<*>) { }          // OK: can only check the raw type (star)

// CAN'T create an array of a generic type directly:
// val arr = Array<T>(10) { }     // ERROR without a reified type

// Two functions differing only by generic type CLASH after erasure:
fun process(list: List<String>) {}
fun process(list: List<Int>) {}   // ERROR: same signature after erasure`},{t:"list",items:["**You can't do `x is List<String>`** — at runtime there's no `String` info, only `List`. You can check `is List<*>` (the raw type) but not the type argument.","**You can't overload on generic type alone** — `f(List<String>)` and `f(List<Int>)` have the same erased signature and clash.","**Workaround — `reified`**: an `inline fun <reified T>` keeps the type at runtime (because the function is inlined, the concrete type is pasted in). This is why `inline fun <reified T> Gson.fromJson(json)` works and a non-inline one can't. Reified is the escape hatch from erasure."]}]},{heading:"Variance — the core hard topic",blocks:[{t:"p",text:"**Variance** answers: if `Dog` is a subtype of `Animal`, is `Box<Dog>` a subtype of `Box<Animal>`? By default in Kotlin, **no** — generics are *invariant*: `Box<Dog>` and `Box<Animal>` are unrelated types, even though `Dog` and `Animal` are related. This is safe but restrictive. **Variance annotations** (`out` and `in`) let you relax it safely."},{t:"code",title:"Why invariance is the safe default",code:`// If MutableList<Dog> WERE a subtype of MutableList<Animal>, this would break:
val dogs: MutableList<Dog> = mutableListOf(Dog())
val animals: MutableList<Animal> = dogs   // (hypothetically allowed)
animals.add(Cat())                         // adding a Cat to a list of Dogs!
val dog: Dog = dogs[0]                      // now a Cat is in here — corruption
// So Kotlin makes MutableList INVARIANT: the assignment above is a compile error.`}]},{heading:"out — covariance (producers)",blocks:[{t:"p",text:"**`out T`** makes a type parameter **covariant**: `Box<out Animal>` accepts `Box<Dog>`. It's allowed when the type is only ever *produced* (returned/read), never *consumed* (taken as input). Marking `out` is a promise: 'this class only outputs `T`, never accepts it as a parameter'. Because it only produces, it's safe to treat a producer of `Dog` as a producer of `Animal` (a Dog is an Animal)."},{t:"code",title:"out enables covariance",code:`// List<out E> is declared covariant — that's why this works:
val dogs: List<Dog> = listOf(Dog())
val animals: List<Animal> = dogs   // OK! List only PRODUCES elements (get), never adds

// Declaration-site: the class promises to only produce T
interface Producer<out T> {
    fun produce(): T          // OK — T is in output position
    // fun consume(t: T) { }  // ERROR — can't have T in input position with 'out'
}`},{t:"list",items:["**Read-only `List<out E>` is covariant** (that's why `List<Dog>` is a `List<Animal>`), while `MutableList<E>` is invariant (it consumes elements via `add`, so covariance would be unsafe).","**The restriction**: with `out T`, `T` can only appear in *output* positions (return types), never *input* positions (parameters) — the compiler enforces this so the covariance is sound.","**Mnemonic**: `out` = 'produces/outputs T' = covariant = you can *up*cast the generic (Box<Dog> → Box<Animal>)."]}]},{heading:"in — contravariance (consumers)",blocks:[{t:"p",text:"**`in T`** makes a type parameter **contravariant**: `Box<in Dog>` accepts `Box<Animal>`. It's allowed when the type is only ever *consumed* (taken as input), never *produced*. If something can consume any `Animal`, it can certainly consume a `Dog` — so a consumer of `Animal` is usable as a consumer of `Dog`. The subtyping *reverses*."},{t:"code",title:"in enables contravariance",code:`// A Comparator<in T> that compares Animals can compare Dogs:
val animalComparator: Comparator<Animal> = Comparator { a, b -> a.age - b.age }
val dogComparator: Comparator<Dog> = animalComparator   // OK with 'in'

interface Consumer<in T> {
    fun consume(t: T)          // OK — T is in input position
    // fun produce(): T        // ERROR — can't have T in output position with 'in'
}`},{t:"list",items:["**The restriction**: with `in T`, `T` can only appear in *input* positions (parameters), never output — enforced by the compiler.","**Mnemonic**: `in` = 'consumes/inputs T' = contravariant = subtyping reverses (Consumer<Animal> is a Consumer<Dog>).","**The unifying rule — PECS / 'producer out, consumer in'**: if a type parameter is *produced* (output), mark it `out`; if *consumed* (input), mark it `in`. This is Kotlin's version of Java's PECS ('Producer Extends, Consumer Super')."]}]},{heading:"Declaration-site vs use-site variance, and star projection",blocks:[{t:"list",items:["**Declaration-site variance** (Kotlin's advantage): you declare `out`/`in` *once*, at the class/interface definition (`interface List<out E>`). Every use of `List` is then covariant automatically. Java can't do this — it only has use-site variance (wildcards).","**Use-site variance (type projection)**: you apply variance at a specific *use* instead — `fun copy(from: Array<out Any>, to: Array<Any>)`. Kotlin supports this too (it's how you handle invariant classes like `Array` at a call site). It's Kotlin's equivalent of Java's `? extends`/`? super` wildcards.","**Star projection `<*>`**: when you don't know or care about the type argument — `List<*>` means 'a List of *something*'. You can read elements as the upper bound (`Any?`) but can't add anything (except null-related edge cases), because the actual type is unknown. Useful for 'I just need to know it's a List' situations."]},{t:"note",text:"The variance summary to state: generics are *invariant* by default (safe). `out` = covariant = for producers (T in output positions) = you can upcast (List<Dog> is List<Animal>). `in` = contravariant = for consumers (T in input positions) = subtyping reverses (Consumer<Animal> is Consumer<Dog>). Rule: 'producer out, consumer in'. Kotlin allows declaration-site variance (declare once) which Java can't; `<*>` is star projection for unknown type arguments."}]}],Fx=[{level:"junior",q:"What are generics and why are they useful?",a:[{t:"p",text:"**The concept**: generics let you write code that works with any type while keeping type safety. Instead of a container that holds `Any` (forcing casts and risking `ClassCastException`), you write `Box<T>` where `T` is a type parameter chosen at the use site — `Box<String>` holds strings, `Box<Int>` holds ints, and the compiler enforces the type throughout."},{t:"code",title:"Generic class and function",code:`class Box<T>(val value: T) { fun get(): T = value }
val b = Box("hi")            // Box<String>, get() returns String — no cast

fun <T> firstOrNull(list: List<T>): T? =
    if (list.isEmpty()) null else list[0]`},{t:"p",text:"The benefits: **type safety** (the compiler knows `box.get()` is a `String`, no cast needed), **reusability** (one `Box` class works for every type), and **clearer APIs** (a `List<User>` documents exactly what it holds). You can also constrain a type parameter with an upper bound — `fun <T : Comparable<T>> max(a: T, b: T)` requires `T` to be comparable so you can use `>` inside. The standard library is built on generics: `List<T>`, `Map<K,V>`, `Result<T>` all use them."}]},{level:"junior",q:"What is type erasure?",a:[{t:"p",text:"**The concept**: on the JVM, a generic type argument exists only at *compile time* — the compiler uses it to check your code, then *erases* it. At runtime, `List<String>` and `List<Int>` are both just `List`; the JVM has no idea what element type it holds. Kotlin inherits this from Java's generics."},{t:"list",items:["**You can't check a generic type at runtime**: `if (x is List<String>)` is a compile error because there's no `String` info at runtime. You can only check the raw type: `if (x is List<*>)`.","**You can't overload on generic type alone**: `fun f(x: List<String>)` and `fun f(x: List<Int>)` have the *same* signature after erasure, so they clash.","**You can't directly instantiate a generic type**: `Array<T>(n)` doesn't work without more info, because `T` isn't known at runtime."]},{t:"p",text:"The escape hatch is `reified` type parameters on `inline` functions: because an inline function's body (including its type argument) is pasted into each call site, the concrete type *is* available at runtime there. That's why `inline fun <reified T> fromJson(json: String): T` can do `T::class` while a non-inline version can't. So type erasure is the default limitation, and `reified` is how you work around it when you genuinely need the type at runtime."}]},{level:"junior",q:"What does the 'reified' keyword do?",a:[{t:"p",text:"**The concept**: normally a generic type parameter is erased at runtime, so you can't reference it (`T::class` doesn't compile, `x is T` doesn't work). Marking an `inline` function's type parameter `reified` keeps the type available at runtime. It works *because* the function is inlined — the compiler pastes the function body into each call site with the actual type substituted in, so at that pasted location the concrete type is known."},{t:"code",title:"reified makes the type usable at runtime",code:`inline fun <reified T> Gson.fromJson(json: String): T =
    fromJson(json, T::class.java)     // T::class works because reified

val user: User = gson.fromJson(jsonString)   // no need to pass User::class

// Also enables runtime type checks:
inline fun <reified T> List<*>.filterByType(): List<T> =
    filterIsInstance<T>()`},{t:"p",text:"The practical wins: you avoid passing `Class` objects around (`fromJson(json)` instead of `fromJson(json, User::class.java)`), you can do `x is T` and `T::class` inside the function, and it makes generic APIs much cleaner (Android's `viewModels<T>()`, `intent.getParcelableExtra` wrappers, DI's `get<T>()` all use it). The constraint: `reified` *requires* `inline`, so it only works on inline functions — and like all inlining, it duplicates code, so keep those functions small."}]},{level:"senior",q:"Explain variance, and why generics are invariant by default.",a:[{t:"p",text:"**Variance answers a subtyping question**: given that `Dog` is a subtype of `Animal`, what's the relationship between `Container<Dog>` and `Container<Animal>`? By default in Kotlin, generics are **invariant** — `Container<Dog>` and `Container<Animal>` are *unrelated* types, despite `Dog` and `Animal` being related. You can't assign one to the other."},{t:"p",text:"**Why invariance is the safe default** — because covariance-by-default would break type safety for mutable containers:"},{t:"code",title:"Why MutableList can't be covariant",code:`val dogs: MutableList<Dog> = mutableListOf(Dog())
// IF this were allowed (it's not — compile error):
val animals: MutableList<Animal> = dogs
animals.add(Cat())          // Cat added to a list that's really List<Dog>!
val d: Dog = dogs[0]         // ClassCastException waiting to happen`},{t:"list",items:["If `MutableList<Dog>` were a subtype of `MutableList<Animal>`, you could view a dog-list as an animal-list and `add(Cat())` to it — corrupting the dog-list with a cat. The next read of a `Dog` would blow up. So Kotlin makes `MutableList` invariant, making that assignment a compile error.","**The insight**: whether covariance is safe depends on whether the container *produces* or *consumes* the type. A container that only *produces* T (like read-only `List`, which you only read from) *can* safely be covariant. A container that *consumes* T (via `add`) cannot. Invariance is the default because it's safe for *both* cases; you opt into covariance (`out`) or contravariance (`in`) when you know the usage is one-directional."]},{t:"p",text:"This is exactly why `List<Dog>` *is* assignable to `List<Animal>` (List is declared covariant, `List<out E>`, because it only produces) while `MutableList<Dog>` is *not* assignable to `MutableList<Animal>` (invariant, because it consumes). Understanding that variance safety hinges on produce-vs-consume is the heart of the topic."}]},{level:"senior",q:"Explain the 'out' and 'in' variance modifiers with the producer/consumer rule.",a:[{t:"p",text:"**`out` and `in` relax invariance safely, based on whether the type parameter is produced or consumed.** The guiding rule is 'producer `out`, consumer `in`' (Kotlin's version of Java's PECS — Producer Extends, Consumer Super)."},{t:"list",items:["**`out T` — covariance, for producers**: mark a type parameter `out` when the class only ever *produces* (returns/outputs) `T`, never consumes it as a parameter. Then `Producer<Dog>` becomes a subtype of `Producer<Animal>` — subtyping goes the *same* direction. It's safe because if it only outputs Dogs, treating those outputs as Animals is fine (a Dog is an Animal). The compiler enforces that `T` appears only in *output* positions (return types).","**`in T` — contravariance, for consumers**: mark a type parameter `in` when the class only ever *consumes* (takes as input) `T`, never produces it. Then `Consumer<Animal>` becomes a subtype of `Consumer<Dog>` — subtyping *reverses*. It's safe because something that can consume any Animal can certainly consume a Dog. The compiler enforces that `T` appears only in *input* positions (parameters)."]},{t:"code",title:"Both in action",code:`interface Producer<out T> { fun next(): T }        // out -> covariant
val animalProducer: Producer<Animal> = dogProducer  // Producer<Dog> works

interface Consumer<in T> { fun accept(item: T) }   // in -> contravariant
val dogConsumer: Consumer<Dog> = animalConsumer     // Consumer<Animal> works

// Real examples: List<out E> (covariant), Comparator<in T> (contravariant)`},{t:"list",items:["**Mnemonic**: `out` = output = produce = covariant = upcast the generic (`List<Dog>` is a `List<Animal>`). `in` = input = consume = contravariant = reverse (`Comparator<Animal>` is a `Comparator<Dog>`).","**Kotlin's edge — declaration-site variance**: you write `out`/`in` *once* at the class declaration (`interface List<out E>`), and every use is variant automatically. Java can only do use-site variance (wildcards `? extends`/`? super` at each call). Kotlin supports use-site variance too (type projections like `Array<out Any>`), for cases like the inherently-invariant `Array`."]}]},{level:"senior",q:"What is star projection (<*>) and when do you use it?",a:[{t:"p",text:"**The concept**: `<*>` (star projection) is how you say 'a generic type with *some* type argument, but I don't know or care which'. `List<*>` means 'a List of something' — you know it's a List, but not its element type. It's a safe way to work with a generic type when the specific type parameter is unknown or irrelevant."},{t:"code",title:"Star projection",code:`fun printSize(list: List<*>) {          // any List, element type unknown
    println(list.size)                   // OK — size doesn't depend on T
    val item: Any? = list[0]             // reads come back as Any? (the upper bound)
    // list.add(...)                     // NOT allowed for MutableList<*> — can't
                                         // safely add when the real type is unknown
}`},{t:"list",items:["**What you can do with `<*>`**: read elements as the *upper bound* of the type parameter — for `List<*>` that's `Any?`, since any element is at least `Any?`. And use members that don't involve the type parameter (`size`, `isEmpty`).","**What you can't do**: *write* a value of the projected type into a consumer position, because the real type is unknown — adding to a `MutableList<*>` is disallowed (you might add the wrong type). The compiler treats the 'in' positions as unusable.","**How it relates to variance**: `<*>` is essentially projecting the type as `out (upperBound)` for reading and `in Nothing` for writing — 'I can get an `Any?` out, but I can't put anything in'. It's the Kotlin equivalent of Java's unbounded wildcard `<?>`.","**When to use it**: when you have a generic type but genuinely don't need its type argument — logging/inspecting a collection generically, storing heterogeneous generic instances (`List<Box<*>>`), or writing a function that only uses type-independent members. If you *do* need the type, use a generic function with a real type parameter instead."]},{t:"p",text:"**The distinction to make**: `<*>` (star projection) is 'unknown specific type' — safe, read-as-upper-bound. `<Any?>` is 'explicitly the Any? type'. And `<T>` (a real type parameter on a generic function) is 'I don't know it *here* but I want to *capture and use* it' — which is what you'd choose when you need to actually work with the element type rather than just inspect the container. Star projection is specifically for 'I don't care what T is'."}]}],Nx={id:"generics",title:"Generics & Variance",tagline:"Generic classes/functions, type constraints, type erasure & reified, invariance by default, out (covariance), in (contravariance), and star projection.",tags:["Kotlin","Generics","Variance","reified","out/in"],content:Ux,qa:Fx},qx=[{heading:"The 'by' keyword — delegation in one word",blocks:[{t:"p",text:"Kotlin's **`by`** keyword implements the **delegation pattern** — forwarding work to another object — at the language level. It comes in two flavors: **class delegation** (implement an interface by forwarding to another instance) and **property delegation** (let another object handle a property's get/set). Both replace boilerplate you'd otherwise write by hand."}]},{heading:"Class delegation — composition over inheritance",blocks:[{t:"p",text:"Class delegation lets a class implement an interface by **delegating all its methods to another object** that already implements it — without manually writing forwarding methods. `class MyList(list: List<T>) : List<T> by list` implements the entire `List` interface by forwarding to `list`. You can then override just the methods you want to customize."},{t:"code",title:"Implement an interface by delegation",code:`interface Repository { fun getAll(): List<Item>; fun save(item: Item) }

// Delegate everything to 'base', override only what you need:
class LoggingRepository(
    private val base: Repository,
) : Repository by base {          // forwards getAll() and save() to base
    override fun save(item: Item) {
        Log.d("Repo", "saving $item")
        base.save(item)           // add behavior, then delegate
    }
    // getAll() is auto-forwarded to base — no boilerplate
}`},{t:"list",items:["**Composition over inheritance**: instead of subclassing (tight coupling, fragile base class problem), you *compose* — wrap an object and forward to it, overriding selectively. This is the decorator pattern with zero boilerplate.","**The compiler generates the forwarding methods** for every interface member, delegating to the `by` object. You override only the ones you want to change.","**Uses**: decorators (logging, caching, validation wrappers around a repository), adapting one implementation, and avoiding deep inheritance hierarchies. It's why Kotlin favors 'prefer composition' — the language makes it easy."]}]},{heading:"Property delegation — by lazy, by Delegates, custom",blocks:[{t:"p",text:"Property delegation lets *another object* handle a property's get/set logic. `val x by SomeDelegate()` means reads and writes of `x` are routed to the delegate's `getValue`/`setValue`. The standard library provides ready-made delegates for common needs:"},{t:"code",title:"Standard property delegates",code:`// lazy — compute once, on first access, then cache; thread-safe by default
val config: Config by lazy {
    loadExpensiveConfig()          // runs only on first access to config
}

// Delegates.observable — run a callback whenever the property changes
var name: String by Delegates.observable("") { _, old, new ->
    println("name changed: $old -> $new")
}

// map-backed delegate — property reads from a map (common for JSON/config)
class User(map: Map<String, Any?>) {
    val id: String by map          // reads map["id"]
    val age: Int by map
}

// vetoable — validate/reject a change
var age: Int by Delegates.vetoable(0) { _, _, new -> new >= 0 }  // reject negatives`},{t:"list",items:["**`by lazy { }`** — the most common: computes the value *once*, lazily on first access, caches it, and is *thread-safe* by default (`LazyThreadSafetyMode.SYNCHRONIZED` — only one thread computes it). For single-threaded use, `lazy(LazyThreadSafetyMode.NONE)` is faster (no locking).","**`Delegates.observable`** — fires a callback on every change (old → new); great for reacting to property changes without manual setters.","**`Delegates.vetoable`** — like observable but can *reject* the change (return false).","**Map delegation** — a property backed by a map entry; handy for dynamic/config objects.","**Custom delegates** — implement `getValue`/`setValue` (or `ReadWriteProperty`) to encapsulate any get/set logic (e.g. a SharedPreferences-backed property)."]}]},{heading:"lateinit vs lazy — the classic comparison",blocks:[{t:"table",headers:["","lateinit var","by lazy { }"],rows:[["Keyword","`lateinit var`","`val ... by lazy { }`"],["Mutability","var (mutable, reassignable)","val (read-only)"],["Who initializes","you, explicitly, later","the lazy block, on first access"],["When initialized","whenever you assign it","automatically, on first read"],["If accessed before init","throws UninitializedPropertyAccessException","can't happen (initializes on access)"],["Nullable / primitives","non-null objects only (no Int/Boolean)","any type"],["Thread safety","none (you manage it)","thread-safe by default"]]},{t:"code",title:"When to use each",code:`// lateinit — value set by a framework/DI later, and it's mutable
class MyFragment : Fragment() {
    private lateinit var binding: FragmentBinding   // assigned in onCreateView
    // ::binding.isInitialized  can check if it's been set
}

// lazy — expensive read-only value computed on first use
class ViewModel {
    val repository: Repository by lazy { createRepository() }
}`},{t:"list",items:["**Use `lateinit`** for a non-null property that will be assigned *later by external code* (DI, a framework lifecycle callback like `onCreate`) and may be reassigned — you promise 'I'll set this before using it'. Accessing it before assignment throws (a clear error, better than a null).","**Use `lazy`** for a read-only property that's *expensive to compute* and should be created *once on first use* — the delegate handles initialization and caching. It's a `val`, so it can't be reassigned.","**`lateinit` can't be used with nullable types or primitives** (use a nullable `var` with null as the 'uninitialized' state instead); `lazy` works with anything.","**`::prop.isInitialized`** checks whether a lateinit has been assigned — useful to avoid the exception."]}]},{heading:"How property delegation works under the hood",blocks:[{t:"p",text:"`val x by delegate` compiles to calls on the delegate object: reading `x` calls `delegate.getValue(thisRef, property)`, and writing (for `var`) calls `delegate.setValue(thisRef, property, value)`. The delegate must provide these operator functions (or implement `ReadOnlyProperty`/`ReadWriteProperty`). This is why you can write your own delegates — it's just a convention of implementing two functions."},{t:"code",title:"A custom delegate — SharedPreferences-backed property",code:`class PrefDelegate(
    private val prefs: SharedPreferences,
    private val key: String,
    private val default: String,
) : ReadWriteProperty<Any?, String> {
    override fun getValue(thisRef: Any?, property: KProperty<*>): String =
        prefs.getString(key, default) ?: default
    override fun setValue(thisRef: Any?, property: KProperty<*>, value: String) {
        prefs.edit().putString(key, value).apply()
    }
}

// Usage — property reads/writes go straight to SharedPreferences:
var authToken: String by PrefDelegate(prefs, "token", "")`},{t:"note",text:"Delegation summary: **`by` (class delegation)** = implement an interface by forwarding to another object (composition over inheritance, zero boilerplate). **`by` (property delegation)** = another object handles a property's get/set. **`by lazy`** = compute-once, thread-safe, read-only. **`lateinit var`** = non-null, assigned-later, mutable, throws if accessed early. Custom delegates just implement `getValue`/`setValue`."}]}],Wx=[{level:"junior",q:"What is the difference between lateinit and lazy?",a:[{t:"p",text:"**Both let you delay initialization of a property, but they work differently and suit different situations.** `lateinit var` is a mutable property you promise to assign *yourself, later*, before using it — the initialization is your responsibility and can happen anytime (and be reassigned). `by lazy { }` is a read-only property whose value is computed *automatically on first access* by the lazy block, then cached."},{t:"list",items:["**`lateinit var`**: mutable (`var`), non-null object types only (no `Int`/`Boolean`), you assign it explicitly (often in a framework callback or via DI), and accessing it before assignment throws `UninitializedPropertyAccessException`. Use for values set later by external code — a Fragment's binding set in `onCreateView`, an injected dependency.","**`by lazy { }`**: read-only (`val`), works with any type, initializes itself on first read, caches the result, and is thread-safe by default. Use for expensive values that should be computed once, on demand — a lazily-created repository, a parsed config."]},{t:"code",title:"Each in its place",code:`private lateinit var binding: FragmentBinding    // assigned later in onCreateView
val db: Database by lazy { buildExpensiveDb() }  // computed once, on first use`},{t:"p",text:"Quick chooser: is it *mutable and set by someone else later*? → `lateinit`. Is it *read-only and expensive to compute once on demand*? → `lazy`. And remember `lateinit` can't hold primitives or nullables, while `lazy` can hold anything."}]},{level:"junior",q:"What does 'by lazy' do and is it thread-safe?",a:[{t:"p",text:"**The concept**: `val x by lazy { ... }` defers computing `x` until the *first time it's read*. The block runs once, on first access, and the result is cached — every subsequent read returns the cached value without re-running the block. If the property is never accessed, the block never runs."},{t:"p",text:"**Thread safety**: yes, by default. The default mode is `LazyThreadSafetyMode.SYNCHRONIZED`, which uses a lock to ensure that even if multiple threads access the property simultaneously on first use, the block runs *exactly once* and all threads see the same value. This makes it safe to use for shared lazy state. If you know the property is only accessed from a single thread, you can pass `LazyThreadSafetyMode.NONE` for a faster version that skips the locking (`by lazy(LazyThreadSafetyMode.NONE) { }`). There's also `PUBLICATION` mode (the block may run on multiple threads but only the first result is used). The default SYNCHRONIZED is the safe choice, and you only optimize to NONE when you're certain about single-threaded access and it's a hot path."}]},{level:"junior",q:"What is class delegation with the 'by' keyword?",a:[{t:"p",text:"**The concept**: class delegation lets a class implement an interface by *forwarding all its methods to another object* that already implements that interface — without you writing the forwarding boilerplate. `class Wrapper(base: Foo) : Foo by base` automatically implements every method of `Foo` by delegating to `base`. You then override only the methods you want to change."},{t:"code",title:"Decorator with almost no boilerplate",code:`interface Repository { fun getAll(): List<Item>; fun save(item: Item) }

class CachingRepository(private val base: Repository) : Repository by base {
    private val cache = mutableListOf<Item>()
    override fun getAll(): List<Item> = cache.ifEmpty { base.getAll().also { cache.addAll(it) } }
    // save() is auto-forwarded to base — no code needed
}`},{t:"p",text:"This is Kotlin's language-level support for **composition over inheritance** — the decorator pattern. Instead of subclassing (which couples you to the base class's implementation and risks the fragile-base-class problem), you *wrap* an object and forward to it, overriding selectively. The compiler generates all the forwarding methods, so adding logging, caching, or validation around an existing implementation takes almost no code. It's a big reason Kotlin can genuinely favor composition — the boilerplate that made composition tedious in Java is gone."}]},{level:"junior",q:"How does property delegation work?",a:[{t:"p",text:"**The concept**: `val x by delegate` routes the property's get (and set, for `var`) to another object — the delegate. Reading `x` doesn't read a field directly; it calls `delegate.getValue(...)`. Writing calls `delegate.setValue(...)`. The delegate encapsulates whatever logic backs the property."},{t:"code",title:"The compiler rewrites property access into delegate calls",code:`val name: String by SomeDelegate()
// reading 'name'  -> SomeDelegate.getValue(thisRef, property)
// (for var) writing -> SomeDelegate.setValue(thisRef, property, value)`},{t:"p",text:"This is how `by lazy`, `Delegates.observable`, and map-backed properties all work — they're just objects providing `getValue`/`setValue`. And because it's a simple convention, you can write *custom* delegates: implement `ReadOnlyProperty`/`ReadWriteProperty` (or the operator functions directly) to back a property with anything — SharedPreferences, a database, a network value, a computed cache. For example, a `PrefDelegate` can make `var token: String by PrefDelegate(prefs, \"token\")` read and write directly to SharedPreferences transparently. Property delegation is Kotlin's mechanism for factoring out reusable get/set behavior."}]},{level:"senior",q:"When would you write a custom property delegate, and what does it need to implement?",a:[{t:"p",text:"**You write a custom delegate when the same get/set *behavior* recurs across many properties and you want to factor it out cleanly.** The classic examples: properties backed by SharedPreferences/DataStore, properties that log or validate on change, properties computed from a resource, or properties with custom caching. Instead of writing a custom getter/setter on each one, you write the logic once in a delegate and apply it with `by`."},{t:"code",title:"A reusable SharedPreferences delegate",code:`class StringPref(
    private val prefs: SharedPreferences,
    private val key: String,
    private val default: String = "",
) : ReadWriteProperty<Any?, String> {
    override fun getValue(thisRef: Any?, property: KProperty<*>) =
        prefs.getString(key, default) ?: default
    override fun setValue(thisRef: Any?, property: KProperty<*>, value: String) =
        prefs.edit().putString(key, value).apply()
}

// Now every pref-backed property is one line:
class Settings(prefs: SharedPreferences) {
    var authToken: String by StringPref(prefs, "token")
    var username: String by StringPref(prefs, "username")
}`},{t:"list",items:["**What it must implement**: for a read-only (`val`) property, `getValue(thisRef, property)` — either directly as an `operator fun` or by implementing `ReadOnlyProperty<R, T>`. For a mutable (`var`) property, also `setValue(thisRef, property, value)` — via `ReadWriteProperty<R, T>`. The `property: KProperty<*>` parameter gives reflection info (like the property name), which is handy — e.g. you could default the preferences key to the property's own name.","**`provideDelegate`** (advanced): a delegate can also implement `provideDelegate` to run logic at the moment the delegate is *created and bound* to a property — useful for validation or registration at construction time (e.g. checking the property name, registering with a container).","**Why it's powerful**: it turns cross-cutting property behavior into a reusable, testable unit. The SharedPreferences example collapses dozens of boilerplate getter/setter pairs into one-liners, and you can unit-test the delegate once instead of every property. Libraries use this extensively — Koin/Hilt-style injection (`by inject()`), navigation args (`by navArgs()`), and `viewModels()` are all custom delegates."]}]},{level:"senior",q:"Why does Kotlin favor 'composition over inheritance', and how does class delegation support that?",a:[{t:"p",text:"**The principle 'composition over inheritance' says: to reuse and extend behavior, prefer *containing* another object and forwarding to it (composition) over *subclassing* it (inheritance).** Inheritance has well-known problems: it creates tight coupling to the base class's implementation details, suffers the 'fragile base class' problem (a base-class change silently breaks subclasses), forces a single-inheritance hierarchy, and exposes all of the base's protected members. Composition avoids all of these — you depend only on the *interface* of the object you wrap, not its internals."},{t:"list",items:["**The historical friction**: composition was always 'better' in theory, but in Java it meant writing tedious forwarding methods — to wrap a `List` and change one method, you had to manually implement all ~25 `List` methods to forward to the wrapped instance. That boilerplate pushed people toward inheritance out of laziness.","**How `by` removes the friction**: `class MyList(private val base: List<T>) : List<T> by base` generates *all* the forwarding methods automatically. Now wrapping-and-overriding-one-method is as little code as subclassing-and-overriding-one-method — so composition is finally as *convenient* as inheritance, not just as *correct*.","**Concrete benefits in practice**: you can decorate a repository with logging/caching/retry without subclassing it (and without the base needing to be `open`); you can compose multiple behaviors by wrapping wrappers; and you depend only on the interface, so the wrapped implementation can be swapped or mocked freely. It also plays well with Kotlin's default-`final` classes — you don't need to make classes `open` for inheritance, because you compose instead."]},{t:"code",title:"Stacking behaviors via composition",code:`val repo: Repository =
    LoggingRepository(
        CachingRepository(
            NetworkRepository()
        )
    )
// Each layer wraps the next via 'by', overriding only what it changes.
// Impossible to express this cleanly with single inheritance.`},{t:"p",text:"**The senior framing**: Kotlin's design nudges you toward composition in several ways — classes are `final` by default (inheritance requires explicit `open`), and `by` makes delegation nearly free. The language is deliberately making the *better* design (composition) also the *easier* one, correcting Java's accidental bias toward inheritance-via-boilerplate-avoidance. Class delegation is the concrete feature that makes 'prefer composition' practical rather than aspirational."}]}],zx={id:"delegation",title:"Delegation & Advanced",tagline:"The by keyword, class delegation (composition over inheritance), property delegation, by lazy vs lateinit, and writing custom delegates.",tags:["Kotlin","Delegation","by lazy","lateinit","Composition"],content:qx,qa:Wx},Bx={id:"kotlin",name:"Kotlin Language",description:"The language itself — null safety & types, classes (data/sealed/value/object), functions & scope functions, collections & sequences, generics & variance, and delegation.",topics:[Ix,Px,Dx,Ox,Nx,zx]},Hx=[{heading:"What Jetpack Compose is — and what 'declarative' actually means",blocks:[{t:"p",text:'**Jetpack Compose** is Android\'s modern UI toolkit where you build UI by writing **Kotlin functions** instead of XML layouts. Before Compose, Android UI was **imperative**: you inflated an XML layout into a tree of View objects, kept references to them (`findViewById`, ViewBinding), and *mutated* them step by step — `textView.text = "Hi"`, `progressBar.visibility = GONE`. Your code had to describe **how to change** the screen from its current state to the next one, for every possible transition.'},{t:"p",text:`Compose is **declarative**: you write a function that describes **what the UI looks like for a given state** — and that's all. You never mutate widgets. When state changes, Compose *re-runs* the relevant functions and figures out the difference itself. The mental model shifts from "a tree of objects I mutate" to "UI = f(state)": the screen is simply the result of calling your function with the current state.`},{t:"code",title:"Imperative (Views) vs declarative (Compose) — the same feature",code:`// IMPERATIVE: describe transitions, for every case, manually
fun render(user: User?) {
    if (user == null) {
        progressBar.visibility = View.VISIBLE
        nameText.visibility = View.GONE
    } else {
        progressBar.visibility = View.GONE
        nameText.visibility = View.VISIBLE
        nameText.text = user.name
    }
}

// DECLARATIVE: describe the result; Compose handles transitions
@Composable
fun UserScreen(user: User?) {
    if (user == null) {
        CircularProgressIndicator()
    } else {
        Text(text = user.name)
    }
}`},{t:"list",items:["Why this matters: imperative UI bugs are almost always **forgotten transitions** (you showed the spinner but forgot to hide the error from last time). Declarative UI can't have that class of bug — every run describes the *whole* result from scratch.",`"Re-runs the function" sounds wasteful — it isn't, because Compose is smart about re-running only what could have changed. That machinery (recomposition) is the heart of Compose and of most interview questions.`,"Compose is also **unbundled from the OS** (ships as a library, updates without OS updates) and is the foundation of **Compose Multiplatform** — the same model runs on iOS, desktop, and web."]}]},{heading:"@Composable functions — the rules of the game",blocks:[{t:"p",text:"A function annotated with `@Composable` is not a normal function. The annotation tells the **Compose compiler plugin** to rewrite it: behind the scenes, every composable secretly receives an extra parameter (a `Composer`) and gets calls injected that record what the function *emits* — text, layout nodes, images — into an in-memory structure. That's why the rules below exist:"},{t:"list",items:["**Composables can only be called from other composables** — because they need that hidden Composer parameter, which only the Compose runtime provides. Calling one from a normal function is a compile error.",'**They return Unit** (usually) — a composable doesn\'t *return* UI; it **emits** UI into the composition as a side effect the runtime tracks. `Text("Hi")` adds a text node; it hands you nothing back.',"**They can run in any order and in parallel** (in theory): Compose reserves the right to skip, reorder, or run composables optimistically. So they must be **fast and free of your own side effects** — no writing globals, no network calls, no mutating shared objects directly in the body. (Side effects have dedicated, controlled APIs — covered in the Side Effects topic.)","**They can re-run many times** (recomposition) — anything you do in the body must be safe to repeat. Creating an object per call is fine; incrementing a global counter is a bug.","**Naming convention**: composables that emit UI are `PascalCase` nouns (`ProfileCard`), because conceptually they *declare a thing*, not *do an action*."]},{t:"note",text:'Interview phrasing that shows understanding: "`@Composable` is a compiler contract, like `suspend`. Just as `suspend` functions can only be called from coroutines because the compiler threads a Continuation through them, composables can only be called from composables because the compiler threads a Composer through them." That parallel is exactly right and interviewers love it.'}]},{heading:"Composition and recomposition — the core concept",blocks:[{t:"p",text:"**Composition** is what Compose builds when it runs your composable functions the first time: a tree describing your UI, stored in an internal structure called the **slot table**. Think of it as Compose's memory of *what your functions emitted and with which inputs* — every composable call, its parameters, and its `remember`ed values get a slot."},{t:"p",text:'**Recomposition** is Compose re-running composable functions when the state they read has changed, and updating the composition to match. The critical word is **read**: during execution, Compose tracks exactly which state objects (`State<T>` instances) each composable *reads*. When one of those values changes, Compose knows precisely which functions depend on it — and re-runs **only those**, skipping everything else. This is why "the whole function re-runs" doesn\'t mean "the whole screen re-renders".'},{t:"code",title:"Recomposition scope in action",code:`@Composable
fun Counter() {
    var count by remember { mutableStateOf(0) }   // observable state

    Column {
        Header()                    // reads no state -> never recomposes
        Text("Count: " + count)     // reads count -> recomposes on change
        Button(onClick = { count++ }) {
            Text("Increment")       // reads no state -> skipped
        }
    }
}`},{t:"list",items:["When `count` changes: Compose invalidates the nearest **recomposition scope** that read it (here, the `Counter` body), re-runs it, and while re-running, **skips** child composables whose parameters are unchanged (`Header()`, the Button content). Only the `Text` showing count actually produces new output.","Recomposition is **triggered by state writes, scoped by state reads**. You don't subscribe manually to anything — reading `count` inside a composable *is* the subscription. This automatic tracking is done by the **snapshot state system** (detailed in the State topic).","Recomposition must be **restartable**: Compose may abandon a recomposition mid-way if the state changes again before it finishes (e.g. during an animation) and restart with fresh values. Another reason bodies must be side-effect-free.","Key vocab to use precisely: *composition* = the built tree; *initial composition* = first build; *recomposition* = incremental re-execution; *leaving the composition* = a composable's branch no longer being emitted (its `remember`ed state is forgotten)."]}]},{heading:"The three phases: Composition → Layout → Draw",blocks:[{t:"p",text:"Every frame that needs UI work goes through up to three phases — and understanding them separately is what unlocks both performance work and custom layouts:"},{t:"list",items:['**1. Composition — "what to show"**: runs your composable functions, producing/updating the tree of layout nodes. This is the only phase that executes *your* composable code.',`**2. Layout — "where to place it"**: walks the tree; each node measures its children and places them. Compose's layout is **single-pass**: a parent measures each child exactly once (children can't be re-measured in the same pass) — this is why deep nesting doesn't cause the exponential double-measurement View systems suffered from.`,'**3. Draw — "how to render it"**: nodes draw themselves into a Canvas, top-down.']},{t:"p",text:"The key performance insight: **the phases can run independently**. If only *where* something sits changes (a drag offset), Compose can skip composition entirely and just re-layout; if only *pixels* change (a color animation), it can skip straight to draw. You opt into this by **deferring state reads** to the latest possible phase — e.g. using the lambda version of `offset` or `graphicsLayer` so the animated value is read during layout/draw instead of composition:"},{t:"code",title:"Deferring a read to skip recomposition entirely",code:`// BAD: reads dragOffset during COMPOSITION -> recomposes every drag frame
Box(Modifier.offset(x = dragOffset, y = 0.dp))

// GOOD: lambda defers the read to the LAYOUT phase ->
// dragging re-runs layout only; composition never re-executes
Box(Modifier.offset { IntOffset(dragOffset.roundToPx(), 0) })

// Same idea for draw-phase-only changes:
Box(Modifier.graphicsLayer { alpha = animatedAlpha.value })`},{t:"note",text:'This is one of the highest-frequency Compose interview questions: "name the phases, and how do you use them to optimize?" Answer: composition/layout/draw; state read *location* decides which phase re-runs; defer reads (lambda modifiers, `graphicsLayer`) so high-frequency changes touch only layout or draw.'}]},{heading:"Under the hood: slot table, Composer, and positional memoization",blocks:[{t:"p",text:"You don't need internals to *use* Compose, but interviewers use them to separate depth from surface knowledge. The essentials, plainly:"},{t:"list",items:["**The slot table** is a gap-buffer data structure where the composition lives: every composable call gets a **group** identified by its *position in the code* (compiler-generated key), storing its inputs and `remember`ed slots. On recomposition, Compose walks the table, compares stored inputs to new ones, and re-executes only mismatching groups.","**Positional memoization**: identity comes from *call position*, not function name. The same `Text()` called at two different places in code is two different groups with independent state. Call it in a loop and each iteration's position is the same — which is why dynamic lists need `key()` to give items stable identities (detailed in Lazy Lists).","**The Composer** is the hidden parameter the compiler injects into every composable: it's the cursor into the slot table — `startGroup`/`endGroup` calls the compiler inserts around your code are it navigating and diffing the table.","**Donut-hole skipping**: because groups nest, Compose can re-run an outer function but skip inner groups whose inputs didn't change (the 'donut'), or re-run just an inner lambda while the outer stays cached (the 'hole') — this is what makes 'the whole function re-runs' cheap in practice.","**The Recomposer** is the scheduler: it collects invalidated scopes and drives recomposition each frame, synchronized with the display's frame clock (`withFrameNanos`)."]}]},{heading:"Where composition starts: setContent and the bridge to the OS",blocks:[{t:"code",title:"The entry point",code:`class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {                 // creates the composition
            MyAppTheme {
                AppNavHost()
            }
        }
    }
}`},{t:"list",items:["`setContent` installs a single **ComposeView** as the Activity's content — under Compose there is still exactly *one* Android View hosting the whole composition; inside it, Compose manages its own node tree (LayoutNodes), not Views.","That's why Compose interops cleanly both directions: a `ComposeView` can sit inside XML layouts, and `AndroidView { }` can host a classic View inside a composition (covered in the Interop topic).",'Frame flow end-to-end: state write → snapshot notifies → Recomposer schedules → next vsync: recompose invalid scopes → layout → draw → render thread. Knowing this pipeline lets you answer "what actually happens when I click a button?" precisely.']}]},{heading:"Compose vs the View system — the comparison question",blocks:[{t:"table",headers:["","View system (XML)","Compose"],rows:[["UI definition","XML inflated into View objects","Kotlin functions emitting nodes"],["Updates","imperative mutation of retained objects","re-execution + diffing (recomposition)"],["State handling","manual sync between data and widgets","UI derived from state; tracking automatic"],["Measurement","multiple measure passes possible (exponential in nesting)","single-pass; intrinsics for the exceptions"],["Reuse","custom Views, styles, fragments","plain functions — trivially composable and parameterizable"],["Tooling age","20 years of maturity, every edge case solved","modern preview/live-edit; some advanced cases still land first in Views"],["Interop","—","two-way (ComposeView / AndroidView)"]]},{t:"list",items:["Balanced take for interviews: Compose removes whole bug classes (state-sync, forgotten transitions) and makes custom UI dramatically cheaper; the costs are a learning curve (recomposition mental model), some interop seams, and needing performance discipline (stability) that XML never asked of you.","New development is Compose-first per Google; the View system remains supported and coexists via interop — 'rewrite everything' is never the right answer in an interview."]}]}],_x=[{level:"junior",q:"What is Jetpack Compose and how is it different from the XML/View system?",a:[{t:"p",text:'**The concept**: there are two ways to build UI. The old Android way is *imperative* — you create widget objects (from XML), keep references to them, and mutate them over time: `textView.text = "Hi"`, `spinner.visibility = GONE`. Your code manages every transition between screen states by hand. Compose is *declarative* — you write a Kotlin function that describes what the screen looks like **for a given state**, and never touch widgets afterwards. When state changes, Compose re-runs your function and updates only what differs.'},{t:"p",text:"**Why that's a big deal**: imperative UI bugs are usually forgotten transitions — you showed the loading spinner but forgot to hide last time's error message. In Compose that bug can't exist, because each run describes the *entire* result from scratch; there is no stale leftover to forget. It also collapses the state-synchronization problem: data and UI can't drift apart when UI is literally computed from data (`UI = f(state)`)."},{t:"p",text:"**Concrete differences to name**: no XML — layout, styling and logic are all Kotlin; no `findViewById`/ViewBinding — there are no retained widget objects to reference; updates happen by *recomposition* (smart re-execution) instead of mutation; and it ships as a library independent of the OS version, which is also what enables Compose Multiplatform (same code on iOS/desktop)."}]},{level:"junior",q:"What is a @Composable function and what special rules apply to it?",a:[{t:"p",text:"**The concept**: `@Composable` marks a function that *describes UI*. It's not just documentation — the Compose **compiler plugin** rewrites the function: it secretly adds a `Composer` parameter and injects bookkeeping calls, so that when the function runs, everything it emits (text nodes, layouts, images) is recorded into Compose's internal tree. The function doesn't *return* UI — calling `Text(\"Hi\")` returns `Unit`; its effect is to **emit** a text node into the composition."},{t:"list",items:["**Only callable from other composables** — a normal function has no Composer to pass along, so the compiler rejects the call. (Same mechanism as `suspend` functions only being callable from coroutines — a hidden compiler-threaded parameter.)","**Must be fast and side-effect-free**: Compose may re-run your function many times (recomposition), skip it, or even abandon a run halfway and restart. Anything in the body must be safe to repeat — no network calls, no mutating globals. Side effects go through dedicated APIs (`LaunchedEffect` etc.).","**No guaranteed order**: sibling composables may execute in any order — never make one sibling depend on another having run first.","**Convention**: UI-emitting composables are named like nouns in PascalCase (`ProfileCard`), since they declare a thing rather than perform an action."]}]},{level:"junior",q:"Explain composition and recomposition. What triggers a recomposition?",a:[{t:"p",text:"**The concept**: when Compose runs your composable functions the first time, it builds the **composition** — an internal tree recording what every function emitted and with which inputs. This is Compose's memory of your UI. **Recomposition** is Compose updating that memory: re-running the affected functions when something they depend on changes."},{t:"p",text:'**What triggers it**: writing to an observable **state object** that some composable previously **read**. While your functions execute, Compose records every `State<T>` read (reading `count` inside `Text("Count: " + count)` silently subscribes that spot to `count`). When `count` is written, Compose looks up exactly which *recomposition scopes* read it and re-runs only those. So the rule is: **writes trigger, reads scope** — no manual subscriptions anywhere.'},{t:"p",text:"**The crucial refinement**: re-running a function doesn't mean re-rendering everything inside it. During the re-run, child composables whose parameters haven't changed are **skipped** entirely — Compose compares new inputs against what's stored in the composition. So a screen-level recomposition typically executes only a handful of small functions. Edge cases worth knowing: recomposition can be abandoned mid-way and restarted if state changes again quickly (why bodies must be repeat-safe), and plain variables (`var x = 0`) trigger nothing — only snapshot state types (`mutableStateOf`, `StateFlow` via `collectAsState`) are observable."}]},{level:"junior",q:"Why does a composable function return Unit? How does the UI actually get built?",a:[{t:"p",text:"**The concept**: in an object-oriented UI toolkit, a builder returns a widget you insert into a tree. Compose inverts this: a composable *emits* nodes into an implicit tree while it runs. The hidden `Composer` (injected by the compiler) is a cursor into an internal structure called the **slot table**; every composable call registers a group there containing its inputs and emitted nodes. So the 'return value' of your UI code is a side effect the runtime captures — the function itself has nothing useful to return, hence `Unit`."},{t:"p",text:"**Why design it that way**: it's what makes recomposition possible. Because the tree is keyed by *call position in code* (positional memoization), Compose can re-run your function later, walk the same positions, diff new inputs against stored ones, and update only mismatches — impossible if you handed it opaque widget objects. It also makes composition trivially compositional: any function can call any other, and the emitted trees nest automatically. One exception to mention: `remember { }` *does* return a value — but it's not emitting UI; it's reading/writing a slot in that same table."}]},{level:"junior",q:"What are the three phases of a Compose frame?",a:[{t:"p",text:"**The concept**: turning your code into pixels happens in three separable steps each frame — and Compose keeps them separable on purpose."},{t:"list",items:["**Composition** — *what to show*: your composable functions run (or re-run), producing the tree of layout nodes. The only phase executing your composable code.","**Layout** — *where to put it*: each node measures its children and places them. Single-pass by design: a parent measures each child exactly once, so deeply nested UIs don't explode the way nested `RelativeLayout`s did with double measurement.","**Draw** — *how it looks*: nodes render themselves to the canvas."]},{t:"p",text:"**Why you should care** (the follow-up they're fishing for): the phases can run independently. If a state change only affects position (a drag), Compose can skip composition and just re-layout; if it only affects pixels (alpha, color), just re-draw. *Where you read state decides which phases re-run* — reading a drag offset in composition recomposes every frame, while the lambda form `Modifier.offset { ... }` reads it during layout, and `graphicsLayer { alpha = ... }` reads during draw. Deferring reads like this is the standard fix for animation-driven recomposition storms."}]},{level:"junior",q:"Can you call a composable function from a normal function or a coroutine? Why not?",a:[{t:"p",text:"No — it's a compile-time error, and the reason is mechanical, not stylistic. The Compose compiler rewrites every composable to take a hidden `Composer` parameter (the cursor into the composition), plus bookkeeping calls around the body. A normal function has no Composer to supply, so there's nothing to pass — the call cannot be compiled. Only a composition context (started by `setContent`, or a parent composable) provides it."},{t:"p",text:"**The analogy that shows understanding**: it's exactly how `suspend` works — suspend functions need a hidden `Continuation`, so only coroutines can call them; composables need a hidden `Composer`, so only composables can call them. **Practical corollaries**: to run non-UI code *from* UI you go the other direction (event lambdas calling ViewModel functions); to start UI from a coroutine or callback you don't call composables — you change *state* the composition already observes. If you genuinely need composable logic outside `setContent` (e.g. producing state from Compose code), that's what specialized runtimes like Molecule do — a niche but impressive thing to name."}]},{level:"senior",q:"Explain the slot table and positional memoization. Why does call position matter?",a:[{t:"p",text:"**The concept**: the slot table is the data structure where the composition physically lives — a gap buffer (an array optimized for localized inserts, like text editors use) holding **groups**. Each composable call site becomes a group storing: a compiler-generated key for that *position in the source code*, the call's inputs as recorded last time, and slots for `remember`ed values. Recomposition is the Composer walking this table in step with re-executing your code, comparing stored inputs with fresh ones — equal inputs mean 'skip, reuse everything in this group'; changed inputs mean 're-execute this group, update its slots'."},{t:"p",text:"**Positional memoization** is the identity rule making that walk possible: a composable's identity is *where it was called from*, not what function it is. Two `Text()` calls at different source positions are two independent groups; state `remember`ed in one is invisible to the other. The same call inside an `if/else` gets *different* positions per branch — so switching branches destroys one group (its remembered state is forgotten) and creates the other."},{t:"p",text:"**Where it breaks and why `key()` exists**: in a loop, every iteration shares one source position, so Compose falls back to index-based identity. Insert an item at the top of a list and every item's index shifts — Compose thinks *every* item changed, recomposing all of them and, worse, migrating remembered state to the wrong items (item 0's expanded-flag now belongs to the new item 0). Wrapping items in `key(item.id) { }` (or the `key` parameter in lazy lists) replaces positional identity with your stable identity, so moved items keep their state and only genuinely new items compose. That failure story is the expected answer to 'why do lists need keys'."}]},{level:"senior",q:"What is a recomposition scope, and what exactly decides how much code re-runs when state changes?",a:[{t:"p",text:"**The concept**: Compose doesn't track state reads per-line — it tracks them per **recomposition scope**, which is (approximately) the body of each non-inline composable function and certain lambda arguments. While a scope executes, every snapshot-state read inside it is recorded against that scope. When the state is written, the runtime invalidates *the innermost scopes that read it* and schedules them for the next frame; re-running a scope re-evaluates its body, and unchanged-child skipping limits the damage below it."},{t:"p",text:"**The consequences that answer real interview probes**: (1) Reading state high up (in the screen composable) invalidates the big screen scope; passing values down as parameters — or better, passing *lambdas* that read the state where needed — keeps invalidation small. (2) **Inline composables like `Column`, `Row`, `Box` don't create their own scopes** — a read inside a `Column` body actually registers against the nearest non-inline parent, which is why 'I read state in the Column but the whole function recomposed' surprises people. (3) A scope that reads *no* state never recomposes — it exists in the table but is never invalidated. (4) `derivedStateOf` inserts an extra node so downstream scopes only invalidate when the *derived* value changes, not on every underlying write."},{t:"code",title:"Scope size in practice",code:`@Composable
fun Screen(vm: ViewModel) {
    val state by vm.uiState.collectAsStateWithLifecycle()
    // Read here -> the WHOLE Screen scope invalidates on any state change.

    Header(title = state.title)   // skipped if title unchanged
    Feed(items = state.items)     // skipped if items unchanged (and stable)
}
// Finer-grained alternative: pass () -> String and read inside Header,
// so only Header's scope tracks the read.`}]},{level:"senior",q:"Compose measurement is 'single-pass'. What does that mean, why does it matter, and how do intrinsics fit in?",a:[{t:"p",text:"**The problem it solves**: in the View system, a parent could measure children multiple times (e.g. `RelativeLayout` measuring twice to resolve constraints). Nest such parents and measurement work multiplies per level — 2^depth in the worst case — which is why 'flatten your hierarchy' was gospel and `ConstraintLayout` was invented. Compose forbids this: **during layout, a parent may measure each child exactly once** (the runtime literally throws if you measure twice). Result: measurement cost is linear in tree size, and deep composable nesting is free — a genuine architectural improvement worth stating as such."},{t:"p",text:"**How layout actually flows**: constraints (min/max width/height) travel *down* — parent tells child what sizes are acceptable; sizes travel *up* — child picks its size within them; then the parent *places* children. One pass, top-down then bottom-up."},{t:"p",text:"**Intrinsics are the escape hatch**: some layouts genuinely need to 'ask before measuring' — e.g. a Row where all children should match the tallest child's height (`Modifier.height(IntrinsicSize.Max)`). Intrinsic measurements let a parent query 'what height would you want at this width?' *without* it counting as the real measurement — under the hood the child's intrinsic functions compute estimates, then the single real measure still happens. They're more expensive (extra tree walk), so the guidance is: use intrinsics when needed, don't build everything on them. This — plus SubcomposeLayout for 'measure one child to decide another' — covers every 'but what if I need two passes?' follow-up."}]},{level:"senior",q:"Walk through what happens end-to-end from tapping a button to pixels changing on screen.",a:[{t:"list",items:["**1. Input**: the OS delivers the touch to the Activity's window → Compose's pointer-input system routes it through the node tree via hit testing → your `onClick` lambda runs (on the main thread).","**2. State write**: the lambda executes `count++` on a `MutableState`. The write goes through the **snapshot system** — Compose's MVCC-style state layer — which records that this state object changed and notifies observers when the snapshot is applied.","**3. Invalidation**: the runtime looks up which **recomposition scopes** read `count` during their last execution and marks them invalid. Nothing re-runs yet.","**4. Scheduling**: the **Recomposer** — a coroutine-based scheduler synced to the display via the frame clock (`withFrameNanos`) — picks up pending invalidations for the next vsync. Multiple writes in one frame coalesce into one recomposition.","**5. Recomposition**: on the frame, invalid scopes re-execute; the Composer diffs against the slot table, skipping children with unchanged inputs; the LayoutNode tree gets updated where output actually differed.","**6. Layout**: nodes whose measurements were invalidated re-measure (constraints down, sizes up, single pass) and re-place.","**7. Draw**: invalidated draw regions re-record their drawing; the render thread rasterizes to the screen buffer for that frame."]},{t:"p",text:"Points that earn the 'deep understanding' checkmark: state writes are **decoupled** from re-execution (write now, recompose at next frame — that's why incrementing state twice in one click yields one recomposition); each phase only touches what was invalidated at *its* level; and if the frame budget (~16ms at 60Hz) is blown by any of steps 5–7 on the main thread, that's your jank — profiling means finding *which* phase overran."}]},{level:"senior",q:"How does Compose exist as 'just a library' without OS support, and what are the architectural consequences?",a:[{t:"p",text:"**The design**: Compose bypasses the OS widget stack almost entirely. `setContent` installs exactly one Android View (`ComposeView` hosting an `AndroidComposeView`) into the Activity; from there down, Compose owns everything — its own node tree (LayoutNodes, not Views), its own layout engine, its own drawing (to the same low-level Canvas the framework uses), its own input dispatch and accessibility bridge (translating its semantics tree into the framework's accessibility APIs). The OS sees one big custom View drawing itself."},{t:"list",items:["**Ships and evolves independently**: new Compose features reach every supported API level via Gradle, not OS updates — contrast with View-system features gated on Android versions for two decades.","**The runtime/UI split**: `compose.runtime` (slot table, snapshots, Recomposer) is UI-agnostic tree management — `compose.ui` is just one client emitting LayoutNodes. That's why Compose Multiplatform exists (same runtime, different renderers per platform) and why Molecule can use the runtime with *no* UI at all.","**Interop is structural, not hacked**: since ComposeView is a View, it drops into XML layouts; since Compose can host arbitrary nodes, `AndroidView { }` embeds real Views (maps, WebViews) inside compositions.","**Costs to name for balance**: Compose re-implements what the OS gave Views for free — text editing, magnifier, autofill, a11y — each had to reach parity over time; and one giant custom View means View-based tooling (hierarchy viewers, some test frameworks) sees an opaque blob, which is why Compose has its own semantics-based testing/tooling stack."]}]},{level:"senior",q:'"Composable functions may run in any order, in parallel, be skipped, or run again." Explain each claim and what it forbids you from doing.',a:[{t:"list",items:["**Any order**: Compose only guarantees parent-before-child, not sibling order — e.g. all tabs of a screen may compose in whatever order serves priority. Forbidden pattern: sibling A writing something sibling B's body reads (a shared `var`, a list you append to) — B may run first, or A may be skipped.","**In parallel** (reserved, not currently default): the contract allows Compose to run composables on multiple threads to fill frames faster — one more reason bodies must touch no unsynchronized shared mutable state. Writing code as if this is already true keeps you future-proof and is the honest way to state it.","**Skipped**: if a composable's inputs are unchanged (and stable), Compose reuses its previous output without executing it. Forbidden pattern: relying on a composable's body executing for anything besides describing UI — logging, analytics, counters in bodies silently stop firing when skipping kicks in.","**Run again (restartable)**: recomposition re-executes bodies arbitrarily often, and can even *abandon* an in-progress recomposition and restart when state changes mid-frame (fast animations do this). Forbidden pattern: any non-idempotent work in the body — the same allocation twice is fine, the same POST request twice is a bug."]},{t:"p",text:"**The unifying rule to close with**: a composable body must be a **pure description** — same inputs, same emitted UI, no observable side effects. Everything effectful gets a controlled home instead: `remember` for surviving re-runs, `LaunchedEffect`/`DisposableEffect` for actual side effects keyed to lifecycle, state hoisting for shared data. The four claims aren't trivia — they're *why* those APIs exist."}]}],Kx={id:"fundamentals",title:"Compose Fundamentals",tagline:"What declarative UI really means, @Composable rules, composition & recomposition, the three phases, and the slot-table internals underneath it all.",tags:["Compose","Recomposition","Slot Table","Phases","Declarative UI"],content:Hx,qa:_x},Jx=[{heading:"State in Compose — the starting point",blocks:[{t:"p",text:"In Compose, **state is any value that can change over time and that the UI should reflect**. Because Compose re-executes functions instead of mutating widgets, state must satisfy two needs at once: it must **survive re-execution** (a local `var` would reset every recomposition) and it must be **observable** (Compose must know when it changed so it can recompose). Two APIs split these jobs:"},{t:"list",items:["**`mutableStateOf(value)`** creates a `MutableState<T>` — an observable holder. Reading `.value` inside a composable silently subscribes that recomposition scope; writing `.value` triggers recomposition of subscribed scopes. This is the *observability* half.","**`remember { ... }`** stores a value in the composition (the slot table) so it survives recomposition — without it, `mutableStateOf` would create a *fresh* state object on every re-run and your UI would reset each frame. This is the *survival* half.","So the canonical local state line combines both: `var count by remember { mutableStateOf(0) }`."]},{t:"code",title:"Why both halves are needed",code:`@Composable
fun Counter() {
    // WRONG: no remember -> new state object every recomposition,
    // count visually stuck at 0 forever
    // var count by mutableStateOf(0)

    // WRONG: remember but not state -> survives recomposition,
    // but writing it notifies nobody; UI never updates
    // var count = remember { 0 }

    // RIGHT: survives + observable
    var count by remember { mutableStateOf(0) }

    Button(onClick = { count++ }) { Text("Clicked " + count) }
}`},{t:"list",items:["The `by` keyword is Kotlin **property delegation** — `by remember { mutableStateOf(0) }` unwraps `.value` reads/writes so `count` acts like a plain `Int`. Without `by`, you'd write `count.value` everywhere. Same object, nicer syntax.","`remember` keys: `remember(key1) { ... }` recalculates when the key changes — used for derived objects that must rebuild when an input changes.","`remember` is scoped to the **call site's position**: if the composable leaves the composition (its `if` branch stops being emitted), the remembered value is *forgotten*. State lifetime = presence in the tree."]}]},{heading:"rememberSaveable — surviving configuration change and process death",blocks:[{t:"p",text:"`remember` lives in the composition, and the composition lives inside the Activity — so **rotation destroys everything `remember`ed**. `rememberSaveable` fixes that: it additionally saves the value into the Activity's saved-instance-state `Bundle`, restoring it after configuration changes *and* system-initiated process death."},{t:"code",title:"rememberSaveable, with a custom Saver for non-Bundle types",code:`// primitives / Parcelable / Serializable: works out of the box
var query by rememberSaveable { mutableStateOf("") }

// custom types need a Saver — teach Compose how to (de)serialize:
data class Filter(val tag: String, val enabled: Boolean)

val FilterSaver = listSaver<Filter, Any>(
    save = { listOf(it.tag, it.enabled) },
    restore = { Filter(it[0] as String, it[1] as Boolean) }
)

var filter by rememberSaveable(stateSaver = FilterSaver) {
    mutableStateOf(Filter("all", true))
}`},{t:"list",items:["**Decision rule**: `remember` for transient visual state (animation scratch values, expanded flags you're fine losing); `rememberSaveable` for anything a user would be annoyed to lose on rotation (text input, selected tab, scroll — though scroll has its own `rememberLazyListState`, which is already saveable internally).","It uses the same `Bundle` machinery as `onSaveInstanceState` — so the same limits apply: small, Bundle-compatible data only. Screen *data* still belongs in the ViewModel/SavedStateHandle; `rememberSaveable` is for **UI-element state**.","Division of labor interviewers probe: **ViewModel** = screen state, survives config change in memory, needs SavedStateHandle for process death; **rememberSaveable** = widget-level state, survives both, but only Bundle-sized. They complement, not compete."]}]},{heading:"State hoisting — stateless vs stateful composables",blocks:[{t:"p",text:"**State hoisting** means moving state *up* out of a composable and passing it back down as parameters: instead of a composable owning `var text`, it receives `value: String` and `onValueChange: (String) -> Unit`. The composable becomes **stateless** — a pure function of its inputs — and the caller decides where the state actually lives."},{t:"code",title:"The hoisting pattern (this is exactly how TextField works)",code:`// STATEFUL: owns its state — convenient, but the parent can't
// read, control, or validate the text
@Composable
fun SearchBoxStateful() {
    var query by remember { mutableStateOf("") }
    TextField(value = query, onValueChange = { query = it })
}

// STATELESS (hoisted): state up, events up, value down
@Composable
fun SearchBox(
    query: String,
    onQueryChange: (String) -> Unit,
    modifier: Modifier = Modifier,
) {
    TextField(value = query, onValueChange = onQueryChange, modifier = modifier)
}

// Caller owns the state and can now share/validate/persist it:
@Composable
fun SearchScreen(viewModel: SearchViewModel) {
    val query by viewModel.query.collectAsStateWithLifecycle()
    SearchBox(query = query, onQueryChange = viewModel::onQueryChange)
}`},{t:"list",items:["**Why hoist**: single source of truth (no duplicated text living both in the widget and the ViewModel), reusability (a stateless composable works in any context), testability and previews (pass any state directly), and it's what makes UDF work end-to-end — state flows down, events flow up.","**How high to hoist**: to the **lowest common ancestor** of everyone who reads or writes it — no higher. UI-only state nobody else cares about (is this dropdown open?) can stay internal; state the screen logic needs goes to the ViewModel.","Compose's own components are all built hoisted (`TextField`, `Checkbox`, `Slider` — all take value + onChange). When you see `value`/`onValueChange` pairs, that's hoisting as an API convention.","**Plain state holder classes**: when a composable has several intertwined pieces of UI state + logic (e.g. scroll position + animation), bundle them in a `remember`ed class (`rememberScrollState()`, `rememberLazyListState()` are exactly this pattern) — a middle tier between loose `remember`s and the ViewModel."]}]},{heading:"derivedStateOf — computed state without extra recomposition",blocks:[{t:"p",text:`**The problem**: you have state that changes constantly (scroll position, every keystroke) but the UI only cares about a *derived* fact that changes rarely ("is the user past item 3?", "is the input valid?"). If you compute the fact inline, the scope recomposes on **every** underlying change even when the fact's answer is identical.`},{t:"code",title:"derivedStateOf: recompose only when the ANSWER changes",code:`val listState = rememberLazyListState()

// BAD: firstVisibleItemIndex changes every scroll frame ->
// this scope recomposes continuously while scrolling
val showButtonBad = listState.firstVisibleItemIndex > 3

// GOOD: derivedStateOf re-evaluates on every scroll, but only
// INVALIDATES readers when the Boolean result actually flips
val showButton by remember {
    derivedStateOf { listState.firstVisibleItemIndex > 3 }
}

AnimatedVisibility(visible = showButton) { ScrollToTopButton() }`},{t:"list",items:["Mechanics: `derivedStateOf` creates a state node that observes whatever snapshot state its lambda reads. The lambda re-runs on each underlying change, but downstream scopes are invalidated **only when the computed result differs** (by equality) from last time — it's a change-frequency filter.","**When to use**: input changes far more often than the output — scroll thresholds, validation flags, filtered snapshots of fast-changing sources. **When NOT to**: output changes as often as input (plain `remember(key)` is cheaper — derivedStateOf adds tracking overhead), or the inputs aren't snapshot state at all (it can't observe plain parameters — a classic misuse: `derivedStateOf` over a function parameter observes nothing).","Wrap it in `remember { }` — forgetting that recreates the derived node each recomposition, silently defeating it (and it's the most common derivedStateOf bug in review)."]}]},{heading:"Collecting external state: ViewModels, Flows, and collectAsStateWithLifecycle",blocks:[{t:"p",text:"Screen-level state usually lives in a ViewModel as `StateFlow`. Compose bridges reactive streams into snapshot state with collect-as-state adapters — each emission writes a `State<T>` that composables read like any other state:"},{t:"code",title:"The standard screen wiring",code:`@Composable
fun ProfileScreen(viewModel: ProfileViewModel = hiltViewModel()) {
    // Lifecycle-aware: collection stops at ON_STOP, restarts at ON_START
    val state by viewModel.uiState.collectAsStateWithLifecycle()

    ProfileContent(
        state = state,
        onRefresh = viewModel::refresh,
    )
}

@Composable // stateless, previewable, reusable
fun ProfileContent(state: ProfileUiState, onRefresh: () -> Unit) { /* ... */ }`},{t:"list",items:["**`collectAsStateWithLifecycle` vs `collectAsState`**: the lifecycle variant cancels collection when the app is backgrounded (via `repeatOnLifecycle(STARTED)`), pairing with `stateIn(WhileSubscribed(5000))` to stop upstream work too. Plain `collectAsState` keeps collecting in the background — use it only where lifecycle doesn't apply (previews, desktop).","Pattern: **route composable** (gets the ViewModel, collects state) + **content composable** (stateless, takes state + lambdas). Previews and tests target the content one.","You can also keep Compose state *in* the ViewModel (`var uiState by mutableStateOf(...)` with a private setter) — valid, Compose-runtime-dependent; `StateFlow` keeps the ViewModel toolkit-agnostic (and KMP/iOS-friendly). Know both, prefer StateFlow when sharing matters."]}]},{heading:"Under the hood: the snapshot system",blocks:[{t:"p",text:"How does reading `.value` *subscribe* a scope, and how are concurrent changes safe? The answer is Compose's **snapshot state system** — an MVCC (multi-version concurrency control) design, the same family of technique databases use:"},{t:"list",items:["Every snapshot-state object keeps a **linked list of value records**, each tagged with the snapshot id that wrote it. A **snapshot** is a consistent view of all state at a point in time — reading inside snapshot N sees the newest record valid for N, regardless of later writes elsewhere.","**Read tracking**: snapshots can carry read observers. During composition, Compose opens a snapshot whose observer records every state object read, per recomposition scope — that recording *is* the subscription. No listeners, no annotations: reading is subscribing.","**Write tracking & application**: writes inside a mutable snapshot create new records invisible to others until the snapshot is **applied** (atomically, like a commit). On apply, registered apply-observers receive the changed objects — the Recomposer intersects them with recorded reads and invalidates exactly the affected scopes.","**Why MVCC**: recomposition can run concurrently with new writes (even from other threads) without tearing — each computation sees an isolated, consistent world; conflicting parallel snapshots detect collision at apply time. This is also what makes `mutableStateOf` safe to write from any thread.","`snapshotFlow { }` bridges the reverse direction: it observes snapshot reads inside its lambda and emits into a cold Flow whenever they change — snapshot state → Flow, the mirror of `collectAsState`."]},{t:"note",text:`You rarely *say* 'MVCC' in an interview unprompted — but when asked "how does Compose know what to recompose?", the precise answer is: "state reads are recorded per scope by snapshot read-observers during composition; state writes notify apply-observers with the changed objects; the Recomposer intersects the two sets and invalidates only scopes that read what changed." That one sentence is the whole system.`}]},{heading:"State pitfalls checklist (the bugs interviewers describe and ask you to diagnose)",blocks:[{t:"list",items:["**`mutableStateOf` without `remember`** → state resets every recomposition; UI stuck at initial value. (Compiler lint usually flags it.)","**`remember` without `mutableStateOf`** → value survives but changes are invisible; UI never updates.","**Mutating a remembered mutable object** (`remember { mutableListOf() }`, then `list.add(x)`) → no recomposition: the *reference* never changed and the list isn't observable. Fix: `mutableStateListOf()` / `mutableStateMapOf()` (element-level observable) or immutable copies in a `MutableState`.","**Expecting recomposition from plain `var`s or normal Flows** — only snapshot state and collect-as-state adapters are observable.","**State captured in stale lambdas**: a long-lived lambda (callback registered once) capturing a state *value* sees the old value forever — capture the `State` object or use `rememberUpdatedState` (Side Effects topic).","**Remembered state keyed to nothing when it should follow an input**: `remember { mutableStateOf(item.isFavorite) }` keeps the *first* item's flag when `item` changes — needs `remember(item.id)`.","**Losing state on conditional structure**: moving a composable between `if` branches changes its slot-table position → remembered state is forgotten; use `key()` or restructure so identity is stable.","**Backwards writes** — writing state that the *same* composition pass already read (e.g. writing in the body based on a read above) → infinite recomposition loop; Compose logs a warning. Writes belong in event handlers/effects."]}]}],Gx=[{level:"junior",q:"What does remember do, and why do we need it if we already have mutableStateOf?",a:[{t:"p",text:"**The concept**: Compose re-executes your composable function on every recomposition — so any local variable created in the body is created *again*, from scratch, each time. `remember { ... }` opts a value out of that: it runs its lambda once, stores the result in the composition (the slot table), and on every later execution returns the stored value instead of recomputing."},{t:"p",text:"**Why both are needed**: `mutableStateOf` and `remember` solve two different problems. `mutableStateOf(0)` makes a value *observable* — Compose recomposes readers when it changes. But if you write `var count by mutableStateOf(0)` without `remember`, every recomposition creates a **brand-new state object reset to 0** — you increment it, recomposition happens, and the new execution builds a fresh zero. The UI appears frozen at the initial value. `remember` keeps the *same* state object alive across executions. Conversely, `remember { 0 }` alone survives but notifies nobody on change. So: **remember = survival, mutableStateOf = observability**, and UI state needs both: `remember { mutableStateOf(0) }`."},{t:"p",text:"**Edge cases worth adding**: `remember(key)` re-runs the lambda when the key changes (for state that must reset when an input changes); and remembered values die when the composable **leaves the composition** (its `if` branch stops being emitted, it's scrolled far away in a lazy list) — remember-lifetime equals presence in the tree, which is why rotation (which rebuilds the whole composition) also wipes it, leading to `rememberSaveable`."}]},{level:"junior",q:"What is the difference between remember and rememberSaveable?",a:[{t:"p",text:"**The concept first**: `remember` stores values in the composition — and the composition lives inside the Activity. A configuration change (rotation, dark mode, resize) destroys and recreates the Activity, taking the entire composition and everything remembered with it. So `remember` survives *recomposition*, but not *recreation*."},{t:"p",text:"`rememberSaveable` does everything `remember` does, **plus** writes the value into the saved-instance-state `Bundle` (the same mechanism as `onSaveInstanceState`). After rotation — and even after system-initiated **process death** — the value is restored. Requirements: the type must be Bundle-compatible (primitives, String, Parcelable, Serializable), or you supply a custom `Saver` telling Compose how to convert it to something saveable."},{t:"p",text:"**How to choose**: use `rememberSaveable` for anything a user would be annoyed to lose on rotating the phone — typed text, selected tab, checkbox choices. Use `remember` for transient/visual scratch state that's fine resetting. And know the boundary: screen *data* (the loaded list, the user object) belongs in the ViewModel, not in either — `rememberSaveable` is for small widget-level state, and stuffing big data into it risks `TransactionTooLargeException` like any Bundle abuse."}]},{level:"junior",q:"What is state hoisting and why is it recommended?",a:[{t:"p",text:"**The concept**: a composable can either *own* its state internally (`var text by remember {...}` inside it) or have the state **hoisted** — moved up to the caller, with the composable receiving the current `value` as a parameter and reporting changes through an `onValueChange` lambda. The composable becomes *stateless*: a pure function of its inputs that holds nothing itself. This is literally how Compose's own components work — `TextField(value, onValueChange)`, `Checkbox(checked, onCheckedChange)` — none of them store their own value."},{t:"list",items:["**Single source of truth**: without hoisting, the text lives both inside the widget and in your ViewModel, and they *will* drift apart. Hoisted, exactly one place owns it.","**The parent gains control**: it can validate input, transform it (force uppercase), share it between widgets, persist it — impossible when the state is locked inside a child.","**Reusability + previews + tests**: a stateless composable can be dropped into any screen, previewed with any state (`@Preview` can't provide ViewModels), and tested by passing values directly.","**It completes UDF**: state flows down as parameters, events flow up as lambdas — same pattern as ViewModel↔UI, applied recursively down the tree."]},{t:"p",text:`**The judgment part**: don't hoist dogmatically. Hoist to the **lowest common ancestor** of everything that reads or writes the state — and no higher. A dropdown's open/closed flag that nobody else cares about is fine staying internal; hoisting it to the ViewModel is noise. The interview-ready phrasing: "internal state for UI-only concerns, hoisted state the moment a second party needs it."`}]},{level:"junior",q:"Why doesn't the UI update when I add items to a remember { mutableListOf() }?",a:[{t:"p",text:"**The concept behind the bug**: Compose doesn't watch your objects' *contents* — it watches **snapshot state objects**. `remember { mutableListOf<String>() }` stores a plain `ArrayList` in the composition. When you call `list.add(item)`, the list mutates internally, but nothing observable happened from Compose's perspective: no `State` was written, so no scope is invalidated, so nothing recomposes. The data is there — the screen just never re-reads it."},{t:"code",title:"Three correct alternatives",code:`// 1) Observable collection — element operations trigger recomposition
val items = remember { mutableStateListOf<String>() }
items.add("new")            // recomposes readers

// 2) Immutable value in observable holder — replace, don't mutate
var items by remember { mutableStateOf(listOf<String>()) }
items = items + "new"       // new list assigned -> write -> recomposes

// 3) Real app: state lives in the ViewModel as StateFlow<List<T>>
//    and the UI just collects it`},{t:"p",text:"**The deeper lesson interviewers want**: mutation vs replacement. Compose's model favors **immutable values replaced through observable writes** — replacement is what the snapshot system can see, and equality of immutable values is what powers skipping. `mutableStateListOf` exists as the escape hatch for genuinely list-shaped local state, but if you find yourself mutating shared objects hoping the UI notices, the design is fighting the framework."}]},{level:"junior",q:"What does the 'by' keyword do in `var count by remember { mutableStateOf(0) }`?",a:[{t:"p",text:"**The concept**: `remember { mutableStateOf(0) }` gives you a `MutableState<Int>` — a *box* with a `.value` property. Without any sugar, you'd read `count.value` and write `count.value = 5` everywhere. Kotlin's **property delegation** (`by`) lets an object handle a property's get/set: `MutableState` provides `getValue`/`setValue` operator functions, so `var count by ...` makes plain-looking `count` reads and writes secretly go through `.value`."},{t:"p",text:"**What it changes — and doesn't**: purely syntax; the same state object is involved, the same read-tracking and recomposition happen (reading `count` still registers the read; assigning still triggers invalidation). Two practical notes: use `=` (not `by`) when you want to pass the `State` object itself around — `val countState = remember { mutableStateOf(0) }` — for example to hand a stable reference into a lambda; and the delegate imports (`androidx.compose.runtime.getValue/setValue`) are needed for `by` to compile, the source of a classic head-scratcher error for newcomers."}]},{level:"junior",q:"collectAsState vs collectAsStateWithLifecycle — which one and why?",a:[{t:"p",text:"**The concept**: a `StateFlow` in your ViewModel isn't something composables can read directly — Compose needs snapshot state. Both functions bridge this: they collect the flow inside the composition and mirror each emission into a `State<T>`, so your composable recomposes on new values. The difference is *when collection is active*."},{t:"p",text:"`collectAsState()` collects for as long as the composable is in the composition — **including while the app is in the background**, because backgrounding doesn't dispose the composition. `collectAsStateWithLifecycle()` additionally respects the lifecycle: it uses `repeatOnLifecycle(STARTED)` internally, so collection **stops at ON_STOP and restarts at ON_START**. Combined with the standard `stateIn(SharingStarted.WhileSubscribed(5000))` in the ViewModel, backgrounding the app stops not just the collection but the whole upstream chain (DB observation, location, sockets) — no wasted work while invisible."},{t:"p",text:'**Answer**: on Android, default to `collectAsStateWithLifecycle` (it\'s in `lifecycle-runtime-compose`). `collectAsState` remains for contexts without an Android lifecycle — previews, desktop/multiplatform targets. If asked "what actually goes wrong with plain collectAsState?": a flow of, say, GPS updates keeps the location pipeline alive with the app minimized — battery drain and pointless processing until the process is killed.'}]},{level:"junior",q:"Where should state live — the composable, a state holder, or the ViewModel?",a:[{t:"p",text:"**The concept**: Compose apps have a spectrum of state owners, and the skill is matching state to the cheapest owner that satisfies its consumers. The question to ask of every piece of state: *who needs to read or write this, and how long must it live?*"},{t:"list",items:["**Inside the composable** (`remember`/`rememberSaveable`): UI-element state nobody else cares about — is the dropdown expanded, current text-field selection, animation scratch. Dies with the composable, and that's correct.","**Plain state-holder class** (`remember`ed, like `rememberLazyListState()`): several intertwined pieces of *UI logic* — scroll position + 'show jump button', drag state + snap targets. Still UI-lifetime, but organized and reusable; this is Compose's own pattern for every `rememberXxxState` API.","**ViewModel**: *screen* state and anything involving business/data — loaded content, loading/error flags, user input that must survive configuration change or feed validation. Survives rotation, integrates SavedStateHandle for process death, testable on the JVM."]},{t:"p",text:"**The boundary rule**: business state never lives in `remember`; pure UI mechanics never need a ViewModel. And when one piece of state is needed at two levels (query text shown in the field *and* driving search), hoist it to the higher owner — the ViewModel — and pass it down; duplicating it at both levels is the drift bug hoisting exists to prevent."}]},{level:"senior",q:"Explain derivedStateOf: what problem it solves, how it works, and when it's the wrong tool.",a:[{t:"p",text:"**The problem**: recomposition frequency is driven by how often *read state changes* — not by how often the UI actually needs to look different. Scroll offset changes every frame while scrolling; if your composable reads `listState.firstVisibleItemIndex > 3`, the scope recomposes every frame even though the boolean answer flips maybe twice in the whole gesture. You're paying per-input-change for a per-output-change need."},{t:"p",text:"**How it works**: `derivedStateOf { ... }` creates an intermediate state node. Its lambda runs whenever any snapshot state it reads changes (cheap — just the lambda, not your UI), and the node compares the new result to the previous one by equality. **Only when the result differs** does it invalidate scopes that read the derived value. It's a change-frequency filter between fast inputs and slow outputs — conceptually the snapshot-world version of `distinctUntilChanged`."},{t:"list",items:["**Wrong tool #1 — output changes as often as input**: `derivedStateOf { a + b }` where the sum changes on every write adds tracking overhead and filters nothing; plain inline computation or `remember(a, b)` is cheaper.","**Wrong tool #2 — inputs aren't snapshot state**: it only observes snapshot reads. Deriving from a plain function parameter observes nothing and silently never updates — the parameter changing causes recomposition anyway, so use `remember(param) { compute(param) }` instead. This distinction (parameter changes → remember with keys; state-object changes at higher frequency → derivedStateOf) is the crispest way to show mastery.","**Bug to name**: forgetting to wrap it in `remember { derivedStateOf { ... } }` — recreated every recomposition, the comparison history is lost and the optimization silently vanishes."]}]},{level:"senior",q:"How does Compose's snapshot state system actually work? (How does reading state subscribe, and why is it thread-safe?)",a:[{t:"p",text:"**The design**: Compose state is built on **snapshots** — an MVCC (multi-version concurrency control) scheme, the same family databases use for isolation. Each `mutableStateOf` object internally keeps a *list of value records*, each stamped with the id of the snapshot that wrote it. Code always runs *inside* some snapshot, and reading a state object returns the newest record valid for that snapshot — so every computation sees one consistent version of the world, unaffected by writes happening elsewhere mid-flight."},{t:"p",text:"**How reading subscribes**: snapshots support **read observers**. When the Recomposer runs composition, it opens a snapshot whose read observer logs every state object read, attributed to the currently-executing recomposition scope. That log *is* the subscription table — no listeners registered by you, reading is subscribing. **How writing notifies**: writes in a mutable snapshot create new records invisible to others until the snapshot is atomically **applied** (a commit); apply-observers then receive the set of changed objects, and the Recomposer intersects 'objects changed' with 'scopes that read them' to invalidate exactly the right scopes for the next frame."},{t:"p",text:"**Why thread-safe**: because writes go into isolated snapshots and only become visible at atomic apply, a background thread writing `mutableStateOf` can't tear a half-finished composition — the composition's snapshot simply doesn't see the write until its next run. Concurrent conflicting snapshots detect collisions at apply time (optimistic concurrency). Practical corollaries worth naming: writing snapshot state from any thread is safe (unusual for UI frameworks); `Snapshot.withMutableSnapshot { }` batches multi-write consistency; and `snapshotFlow { }` is the same read-observer trick repackaged to emit into a Flow — the bridge *out* of the snapshot world, mirroring `collectAsState` bridging in."}]},{level:"senior",q:"A callback registered once keeps seeing stale state. Explain why lambdas capture stale values and the fixes.",a:[{t:"p",text:"**The concept**: every recomposition creates *new* lambda instances capturing the *current* values. That's fine for lambdas Compose re-reads each time (like `onClick` passed to Button — the newest lambda replaces the old in the composition). The trap is a lambda handed to something **long-lived that won't be replaced**: a broadcast receiver registered in a `DisposableEffect(Unit)`, a delayed coroutine in `LaunchedEffect(Unit)`, a listener on a singleton. That lambda was created during one particular composition and captured that moment's values — later recompositions create new lambdas, but the long-lived consumer still holds the original. It's not Compose magic misbehaving; it's ordinary closure capture meeting an unusual re-execution model."},{t:"code",title:"The bug and the idiomatic fix",code:`@Composable
fun Timer(onTimeout: () -> Unit) {
    // BUG: effect keyed to Unit runs once, capturing the FIRST onTimeout.
    // If the parent recomposes with a new handler, timeout calls the old one.
    LaunchedEffect(Unit) {
        delay(60_000)
        onTimeout()               // stale!
    }

    // FIX: rememberUpdatedState gives a stable State box whose
    // .value is refreshed every recomposition
    val currentOnTimeout by rememberUpdatedState(onTimeout)
    LaunchedEffect(Unit) {
        delay(60_000)
        currentOnTimeout()        // always the latest handler
    }
}`},{t:"list",items:["**Fix 1 — `rememberUpdatedState`**: capture a stable `State` wrapper instead of the value; the wrapper's `.value` is written on each recomposition, so the old closure dereferences fresh data. Use when the effect must *not* restart (a timer, a registered receiver).","**Fix 2 — key the effect on the value**: `LaunchedEffect(onTimeout)` cancels and relaunches with the new capture. Correct when restarting is acceptable/desired; wrong for the one-shot-timer case (it would reset the countdown).","**The decision rule to state**: 'should this ongoing work restart when the value changes?' Yes → put it in the keys. No → `rememberUpdatedState`. That sentence is the whole topic."]}]},{level:"senior",q:"What is a 'backwards write' and what happens if you write state during composition?",a:[{t:"p",text:"**The concept**: composition is supposed to be a *read-only derivation* — run functions, read state, emit UI. A **backwards write** breaks the direction: writing a state object during composition that composition (the same pass or an enclosing scope) has already read. The result is a self-invalidating loop: the pass reads X, then writes X → the write invalidates the scope that read it → next frame recomposes → reads and writes again → an **infinite recomposition loop**, burning a full extra frame of work forever. Compose detects simple cases and logs a 'backwards write' warning, but the loop still runs."},{t:"code",title:"Bug vs the correct homes for the logic",code:`@Composable
fun Bad(items: List<Item>) {
    var count by remember { mutableStateOf(0) }
    count = items.size          // WRITE during composition of state read below
    Text("Count: " + count)     // READ -> loop
}

// If it's derivable — don't store it at all:
@Composable
fun Good(items: List<Item>) {
    Text("Count: " + items.size)
}

// If it must react to a change — that's an effect, not composition:
LaunchedEffect(items) { analytics.log(items.size) }`},{t:"p",text:"**The rule and its exceptions**: writes belong in **event handlers** (clicks) and **effects** (`LaunchedEffect`, `SideEffect`) — never in the composable body. Two refinements that show depth: initializing state *inside* `remember { mutableStateOf(initial) }` is fine (it's construction, not a tracked write to observed state); and 'derive, don't sync' — most backwards writes are people caching something computable, and the real fix is deleting the stored state, not relocating the write. If genuine derived-state caching is needed, that's `remember(key)` or `derivedStateOf` doing it safely."}]},{level:"senior",q:"Compare holding screen state as StateFlow in the ViewModel vs mutableStateOf in the ViewModel. Trade-offs?",a:[{t:"p",text:"**Both are legitimate** — this is a real design choice, not a trick. `var uiState by mutableStateOf(UiState())` (private set) in a ViewModel works because snapshot state is observable from composables directly; Google's own samples have used it. `MutableStateFlow` + `asStateFlow` is the more traditional shape. What differs:"},{t:"list",items:["**Toolkit coupling**: `mutableStateOf` ties the ViewModel to the Compose *runtime* (a dependency, though not the UI toolkit). StateFlow is pure kotlinx — matters enormously in **KMP** (iOS consumes StateFlow via SKIE/adapters; snapshot state has no Swift story) and if any non-Compose consumer exists.","**Operators & derivation**: StateFlow plugs into the whole Flow world — `combine`, `debounce`, `flatMapLatest`, `stateIn` chains from repositories. Snapshot state has `snapshotFlow` as a bridge but no native operator algebra.","**Lifecycle-aware collection**: StateFlow + `collectAsStateWithLifecycle` + `WhileSubscribed` gives automatic stop-upstream-when-backgrounded. Compose state read directly has no such notion — the state just sits there (fine for cheap state, a non-feature for expensive upstreams).","**Ergonomics**: mutableStateOf wins on simplicity — no `.update {}`, no initial-value duplication, granular per-field states are trivial. Also no equality-conflation surprises (though StateFlow's skip-equal behavior is usually what you want for state).","**Testing**: StateFlow tests with Turbine/standard Flow tooling; Compose-state ViewModels test fine too but pull the Compose runtime into JVM tests."]},{t:"p",text:"**Recommendation to give**: StateFlow as the default — especially for KMP-bound code (which, for an Android/KMP engineer, is decisive) and screens with derived/combined streams; `mutableStateOf` in ViewModels is a pragmatic choice for Compose-only Android apps that value ergonomics. Either way, expose read-only, mutate only inside the ViewModel — the UDF contract is the invariant; the container is negotiable."}]}],$x={id:"state",title:"State & Recomposition",tagline:"remember, rememberSaveable, state hoisting, derivedStateOf, collecting Flows, the snapshot system internals, and every classic state bug.",tags:["Compose","remember","Hoisting","derivedStateOf","Snapshots"],content:Jx,qa:Gx},Yx=[{heading:"Modifiers — what they are and how they really work",blocks:[{t:"p",text:"A **Modifier** is Compose's way of decorating or configuring a composable: sizing, padding, backgrounds, clicks, scrolling, drawing — all attach through the `modifier` parameter instead of dozens of constructor parameters or XML attributes. `Modifier.padding(16.dp).background(Blue)` builds an **ordered chain** of modifier elements; each element wraps the element after it, forming a pipeline the layout/draw/input systems walk from left to right."},{t:"list",items:["Modifiers are **immutable**: each call like `.padding(...)` returns a *new* chain — you can safely store, share and reuse them (`val cardModifier = Modifier...` reused across items is a real optimization).","**Order is semantics, not style** — the single most-asked modifier fact. Each element applies to everything to its *right*: `padding` then `background` pads first, then paints inside the padded area; reversed, the background paints the full size and then content is inset.","Under the hood (Modifier.Node system): the chain materializes into persistent node objects attached to the LayoutNode, each participating in the phases it cares about — layout modifiers adjust measurement, draw modifiers wrap drawing, pointer-input modifiers join hit testing. On recomposition the chain is *diffed* and nodes updated in place, which is why modifiers are cheap."]},{t:"code",title:"Order changes meaning — the classic quartet",code:`// 1) Background BEHIND the padding (paints full area, then insets content)
Modifier.background(Color.Blue).padding(16.dp)

// 2) Background INSIDE the padding (pads first, paints smaller area)
Modifier.padding(16.dp).background(Color.Blue)

// 3) Whole padded area clickable (ripple covers padding)
Modifier.clickable { }.padding(16.dp)

// 4) Only the inner content clickable (padding is dead space)
Modifier.padding(16.dp).clickable { }

// size constraints resolve left-to-right too:
Modifier.size(100.dp).size(50.dp)   // stays 100.dp — first size fixes
                                    // constraints; later ones can't override`},{t:"note",text:"Convention interviewers check: every reusable composable should take a `modifier: Modifier = Modifier` as its **first optional parameter** and pass it to its **outermost** layout — so callers can size/pad/position your component without you predicting their needs. Accepting-but-ignoring the modifier, or applying it to an inner child, are the review bugs to name."}]},{heading:"The layout model: constraints down, sizes up, placement",blocks:[{t:"p",text:'Compose layout is a single-pass negotiation in three steps. Understanding it precisely unlocks every sizing question ("why is fillMaxSize ignored here?") and custom layouts:'},{t:"list",items:['**1. Constraints go down**: a parent hands each child a `Constraints` object — `minWidth..maxWidth`, `minHeight..maxHeight` in pixels. `fillMaxWidth()` means "take maxWidth"; `wrapContentSize()` means "prefer min"; a fixed `size(50.dp)` *tries* to be 50 but is **coerced into the incoming constraints** — this coercion is the answer to most "my size modifier is ignored" mysteries.',"**2. Sizes go up**: each child measures itself within the constraints (measuring *its* children first, recursively) and reports one final size. A parent may measure each child **exactly once**.","**3. Placement**: knowing all children's sizes, the parent positions each one (`placeable.place(x, y)`). Alignment, arrangement and offset all happen here."]},{t:"code",title:"Reading a constraints bug",code:`// Why is the Box not 50.dp? Because fillMaxSize already fixed
// constraints to exactly the parent size, and size() cannot escape
// the incoming min == max constraints:
Box(Modifier.fillMaxSize()) {
    Box(Modifier.fillMaxSize().size(50.dp)) { }  // still full size!
}

// requiredSize() is the escape hatch — it IGNORES incoming constraints
Box(Modifier.fillMaxSize().requiredSize(50.dp)) { } // truly 50.dp
// (parent still lays it out as if it were the constrained size;
// content visually overflows centered)`}]},{heading:"The standard layouts: Row, Column, Box — and their alignment vocabulary",blocks:[{t:"code",title:"The three workhorses",code:`Column(
    verticalArrangement = Arrangement.spacedBy(8.dp),   // main axis
    horizontalAlignment = Alignment.CenterHorizontally, // cross axis
) {
    Text("one")
    Text("two")
    Spacer(Modifier.weight(1f))   // weight: share leftover space
    Text("pinned to bottom")
}

Row(verticalAlignment = Alignment.CenterVertically) {
    Icon(Icons.Default.Star, contentDescription = null)
    Text("title", Modifier.weight(1f))   // takes all remaining width
    Text("meta")
}

Box {                                    // stacking (like FrameLayout)
    Image(painter, contentDescription = null)
    Text("badge", Modifier.align(Alignment.TopEnd))
}`},{t:"list",items:["**Arrangement** = distribution along the **main axis** (Column: vertical; Row: horizontal): `spacedBy`, `SpaceBetween`, `SpaceEvenly`, `Center`… **Alignment** = positioning on the **cross axis**. Mixing these up is the #1 layout-API confusion.","**`weight(n)`** (Row/Column-scoped): children *without* weight measure first; leftover space is split among weighted children proportionally. `weight(1f, fill = false)` lets a child take *up to* its share. This replaces LinearLayout's layout_weight — and is also the idiomatic 'push to the end' tool (a weighted Spacer).","Scoped modifiers are typed: `align()` exists only inside Box/Column/Row scopes, `weight()` only in Row/Column — the compiler stops you using them elsewhere (a nicety over XML worth mentioning).","**Slot APIs**: composables taking composable lambdas as parameters (`TopAppBar(title = { ... }, actions = { ... })`, Scaffold's slots) — Compose's pattern for customizable containers; you'll be asked to *design* one for a component library question."]}]},{heading:"Custom layouts with Layout — when Row/Column aren't enough",blocks:[{t:"p",text:"Any layout you can describe, you can implement with the `Layout` composable — the same primitive Row/Column are built on. You receive the children as `measurables` and the incoming `constraints`, and you implement the measure-place contract yourself:"},{t:"code",title:"A real custom layout: simple flow/chip layout that wraps lines",code:`@Composable
fun FlowRow(
    modifier: Modifier = Modifier,
    spacing: Dp = 8.dp,
    content: @Composable () -> Unit,
) {
    Layout(content = content, modifier = modifier) { measurables, constraints ->
        val gap = spacing.roundToPx()
        // 1) MEASURE each child once, with loose constraints
        val placeables = measurables.map { it.measure(constraints.copy(minWidth = 0, minHeight = 0)) }

        // 2) Break into rows against maxWidth
        var x = 0; var y = 0; var rowHeight = 0
        val positions = placeables.map { p ->
            if (x + p.width > constraints.maxWidth && x > 0) {
                x = 0; y += rowHeight + gap; rowHeight = 0
            }
            val pos = IntOffset(x, y)
            x += p.width + gap
            rowHeight = maxOf(rowHeight, p.height)
            pos
        }
        val totalHeight = (y + rowHeight).coerceIn(constraints.minHeight, constraints.maxHeight)

        // 3) Report size, then PLACE
        layout(constraints.maxWidth, totalHeight) {
            placeables.forEachIndexed { i, p -> p.place(positions[i]) }
        }
    }
}`},{t:"list",items:["The contract in words: measure every child **once**, decide your own size within `constraints`, call `layout(w, h) { ... }` and place children. That's the entire layout system — no onMeasure/onLayout pairs, no MeasureSpec bit-packing.","**`SubcomposeLayout`**: for the rare case where composing one child depends on another's *measured size* (e.g. Scaffold measuring bars before content) — it defers *composition* of some content until measure time. Heavier; use only when measurement genuinely drives what to compose (this is also how LazyColumn composes only visible items).","**Intrinsic measurements** (`Modifier.height(IntrinsicSize.Max)` etc.): pre-query 'what size would you want?' without consuming the single measure — for match-the-tallest-sibling cases. Costs an extra tree walk; fine when localized.","`Modifier.layout { measurable, constraints -> ... }` is the single-child version — for tweaking one child's measurement/placement without a full Layout (e.g. custom baseline padding)."]}]},{heading:"Common modifier families you must be fluent in",blocks:[{t:"table",headers:["Family","Members","Notes / gotchas"],rows:[["Size","`size`, `fillMaxWidth/Height/Size`, `wrapContentSize`, `requiredSize`, `sizeIn`, `aspectRatio`","`size` obeys incoming constraints; `requiredSize` overrides them; `fillMaxWidth(0.5f)` takes a fraction"],["Spacing","`padding`","there is no margin — outer spacing is just padding placed earlier in the chain, or parent `spacedBy`"],["Drawing","`background`, `border`, `clip`, `shadow`, `alpha`, `drawBehind`, `drawWithContent`, `graphicsLayer`","`clip(RoundedCornerShape(...))` must come *before* `background` to round it; `graphicsLayer` changes render without re-layout"],["Interaction","`clickable`, `combinedClickable`, `toggleable`, `selectable`, `pointerInput`","`clickable` adds ripple + semantics + focus; raw `pointerInput` gives gesture detectors but no accessibility for free"],["Scroll","`verticalScroll(rememberScrollState())`, `horizontalScroll`","composes ALL children (unlike lazy lists); gives content infinite max height — nesting a LazyColumn inside is the classic crash"],["Position","`offset` (and its lambda form), `zIndex`, `align` (scoped)","`offset` moves placement without affecting measurement of siblings; lambda form defers the state read to layout phase"]]},{t:"note",text:"Two chain-reading rules that resolve most puzzles: (1) each modifier wraps what's to its right — read left = outermost; (2) constraints only tighten as they travel right — once fixed, later size modifiers can't loosen them (except the `required*` family)."}]},{heading:"ConstraintLayout in Compose — and why you rarely need it",blocks:[{t:"p",text:"`ConstraintLayout` exists in Compose (separate artifact) with references and constraint DSL. But its *original reason to exist* — avoiding nested-layout double measurement in the View system — is gone: Compose measures in a single pass, so **nesting Rows/Columns is cheap and idiomatic**. Reach for ConstraintLayout only for genuinely relational designs (align this to that's baseline across branches, barrier/chain behavior, guideline percentages), not as the default screen scaffold. Saying exactly this — with the *why* — is a well-known interview checkpoint."}]},{heading:"BoxWithConstraints and adaptive layouts",blocks:[{t:"code",title:"Responding to available space",code:`BoxWithConstraints {
    if (maxWidth < 600.dp) {
        CompactList()
    } else {
        Row {
            ListPane(Modifier.weight(1f))
            DetailPane(Modifier.weight(2f))
        }
    }
}`},{t:"list",items:["`BoxWithConstraints` exposes the incoming constraints *during composition* — letting you compose different trees per available size. Cost: it's built on SubcomposeLayout (composition deferred to measure), so don't sprinkle it everywhere; use it at adaptive-layout decision points.","For app-level adaptivity prefer **WindowSizeClass** (compact/medium/expanded buckets from Material3-adaptive) — decisions follow the window, not any one box, and it standardizes breakpoints for phones/foldables/tablets.",'Related: `Spacer` for pure space, `Surface` for themed containers (color/elevation/shape + content color propagation), `Scaffold` for screen chrome (app bars, FAB, snackbar host) with correct insets via its `paddingValues` — forgetting to apply Scaffold\'s padding to content is a favorite "spot the bug".']}]}],Qx=[{level:"junior",q:"What is a Modifier and why does its order matter?",a:[{t:"p",text:"**The concept**: a Modifier is an ordered chain of decorations and behaviors attached to a composable — sizing, padding, background, clicks, scrolling. Instead of widgets exposing hundreds of properties, Compose factors all of that into one composable-agnostic chain: `Modifier.padding(16.dp).background(Blue).clickable { }`. Each call returns a new immutable chain with one more element."},{t:"p",text:"**Why order matters**: the chain isn't a bag of settings — it's a **pipeline** where each element wraps everything to its right. `background(Blue).padding(16.dp)` paints the background over the full area *then* insets the content — the padding is blue. `padding(16.dp).background(Blue)` insets *first*, so the background only covers the smaller inner area — the padding is transparent. Same two modifiers, visibly different UI. The same logic governs clickable areas (`clickable` before `padding` = padded area is tappable and rippled; after = only the content is) and size constraints (`size(100.dp).size(50.dp)` stays 100 — constraints fix left-to-right and later elements can't loosen them)."},{t:"p",text:"**How to never get confused**: read the chain left to right as \"outermost to innermost\". And there's no `margin` in Compose precisely because of this — outer spacing is just a `padding` placed *earlier* in the chain than the background/border, which is arguably cleaner than the View system's two separate concepts."}]},{level:"junior",q:"Explain how measurement works in Compose (constraints, sizes, placement).",a:[{t:"p",text:"**The model**: layout is a single-pass negotiation. First, **constraints travel down** — every parent gives each child a `Constraints` object: acceptable min/max width and height in pixels. The child doesn't get to demand a size; it gets a range. Second, **sizes travel up** — the child (after measuring its own children the same way) picks its final size *within* that range and reports it. Third, **placement**: the parent, now knowing every child's size, positions them with x/y coordinates. Each child is measured exactly once per pass — enforced by the runtime."},{t:"p",text:"**How modifiers plug in**: size modifiers just transform the constraints flowing through the chain. `fillMaxWidth()` = 'set min = max = the available maxWidth'; `wrapContentWidth()` = 'prefer the minimum'; `size(50.dp)` = 'try to fix constraints to 50, **coerced into what came in**'. That coercion answers the classic confusion: inside a parent that already fixed the size (`fillMaxSize` set min == max), a later `size(50.dp)` is silently overridden — the constraints leave it no room. The `requiredSize` family is the explicit escape hatch that ignores incoming constraints."},{t:"p",text:`**Why single-pass is a feature to name**: the View system allowed parents to measure children repeatedly, so nested weights/RelativeLayouts could go exponential with depth — the origin of "flatten your hierarchy". Compose's one-measure rule makes cost linear in tree size, so nesting Columns and Rows freely is idiomatic, not a smell.`}]},{level:"junior",q:"What's the difference between Arrangement and Alignment in Row/Column?",a:[{t:"p",text:"**The concept**: Row and Column each have a *main axis* (the direction they stack children — horizontal for Row, vertical for Column) and a *cross axis* (the perpendicular one). The two parameters control one axis each: **Arrangement** distributes children and leftover space **along the main axis**; **Alignment** positions each child **on the cross axis**."},{t:"p",text:"So in a `Column`: `verticalArrangement` decides vertical distribution (`Arrangement.spacedBy(8.dp)` for gaps, `SpaceBetween`, `Center`, `Bottom`…), while `horizontalAlignment` decides whether children sit left/center/right. In a `Row` it flips: `horizontalArrangement` + `verticalAlignment`. Two useful details: `Arrangement.spacedBy` is the idiomatic replacement for putting padding on every child; and individual children can override the cross-axis alignment with `Modifier.align(...)`, which only compiles inside the right scope — the type system prevents using `align` where it's meaningless. If you remember one sentence: **Arrangement = the stacking axis, Alignment = the other one.**"}]},{level:"junior",q:"How does Modifier.weight work, and what does it replace from the View system?",a:[{t:"p",text:"**The concept**: `weight` divides *leftover* space. Inside a Row or Column, measurement happens in two rounds: first, all children **without** weight measure normally and take what they need; then the remaining space is split among weighted children **proportionally to their weights** — `weight(1f)` and `weight(2f)` split leftovers 1:2. A weighted child's size is thus decided by the parent's arithmetic, not its content."},{t:"code",title:"The three everyday uses",code:`Row {
    Icon(Icons.Default.Star, null)
    Text("Title that truncates", Modifier.weight(1f), maxLines = 1)  // absorb space
    Text("3:45")
}

Column(Modifier.fillMaxHeight()) {
    Content()
    Spacer(Modifier.weight(1f))   // push the button to the bottom
    SubmitButton()
}

Row { // equal-width segmented buttons
    repeat(3) { OptionButton(Modifier.weight(1f)) }
}`},{t:"p",text:'It replaces LinearLayout\'s `layout_weight`, minus its famous pitfalls (no `0dp`-width trick needed, no double-measure cost). Details worth knowing: `weight(1f, fill = false)` means "take *up to* your share" (child can be smaller); weight only exists in Row/Column scope (compile-checked); and a weighted child receives its share as *fixed constraints* — so it will be exactly that size unless `fill = false`.'}]},{level:"junior",q:"Why should every reusable composable take a `modifier: Modifier = Modifier` parameter?",a:[{t:"p",text:"**The concept**: a component author cannot predict how callers will use the component — one screen wants it full-width, another wants it 100.dp with extra padding, a third wants it clickable and semi-transparent. Rather than adding parameters for each possibility, Compose's convention is: accept a single `modifier` parameter (first optional parameter, defaulting to `Modifier`) and apply it to your **outermost** layout. Callers then compose whatever sizing/spacing/behavior they need from the outside."},{t:"list",items:["**Apply it to the root, once**: applying the caller's modifier to an inner child means their `fillMaxWidth` sizes the wrong thing; applying it to multiple children duplicates paddings and clicks — both are real review bugs.","**Chain yours after theirs**: `modifier.then(internalStuff)` or simply `modifier.padding(...)` — caller's elements sit outermost, so their positioning wins, and your internal decorations stay inside.","**Don't accept-and-ignore**: silently dropping the parameter breaks every caller's layout expectations and is worse than not having it.","**Why a parameter and not hardcoding**: it keeps components context-free — the same `ProfileCard` works in a list, a grid, a dialog — which is the composability Compose is named after."]}]},{level:"junior",q:"There's no margin in Compose — how do you create spacing between elements?",a:[{t:"p",text:"**The concept**: the View system had two spacing ideas — padding (inside the border) and margin (outside it). Compose deliberately has only `padding`, because with ordered modifier chains one concept covers both: padding applied *before* the background/border in the chain acts like a margin (space outside the visible surface), padding applied *after* acts like classic padding (space inside it). `Modifier.padding(8.dp).background(Blue).padding(16.dp)` gives 8dp of 'margin' and 16dp of 'padding' with one API."},{t:"p",text:"**The idiomatic toolbox for spacing**: `Arrangement.spacedBy(8.dp)` on Row/Column/LazyColumn for uniform gaps between children (preferred over per-child padding — no doubled edges); `Spacer(Modifier.height(8.dp))` for one-off gaps; `contentPadding` on lazy lists for space at the edges *inside* the scrolling region (so content scrolls under the edge, unlike outer padding which clips the scroll area — a distinction interviewers like); and weighted Spacers to push things apart. If asked why this design is better: one concept + explicit order beats two concepts + implicit rules about how they interact."}]},{level:"senior",q:"Implement (or walk through) a custom layout with the Layout composable. What contract must you satisfy?",a:[{t:"p",text:"**The concept**: `Layout` is the primitive underneath Row, Column, Box — all of them are just measure policies. You provide the children (`content`) and a `MeasurePolicy` lambda receiving `measurables` (unmeasured children) and `constraints` (from your parent). The contract has three obligations: **measure each child at most once** (`measurable.measure(childConstraints)` → a `Placeable` with real width/height; measuring twice throws), **choose your own size within the incoming constraints** and report it via `layout(width, height) { ... }`, and inside that block **place every child** (`placeable.place(x, y)`) — unplaced children simply don't render."},{t:"code",title:"Skeleton every custom layout follows",code:`Layout(content = content, modifier = modifier) { measurables, constraints ->
    // 1. Measure children — you choose their constraints
    //    (often loosened: minWidth = 0 so children can be small)
    val placeables = measurables.map {
        it.measure(constraints.copy(minWidth = 0, minHeight = 0))
    }

    // 2. Compute your layout's geometry from the measured sizes
    val height = placeables.sumOf { it.height }.coerceIn(
        constraints.minHeight, constraints.maxHeight
    )

    // 3. Report size and place
    layout(constraints.maxWidth, height) {
        var y = 0
        placeables.forEach { p ->
            p.place(x = 0, y = y)
            y += p.height
        }
    }
}`},{t:"list",items:["**Decisions you own**: what constraints children get (pass yours through, loosen them, or fix them — this *is* your layout's behavior), and the placement math. Everything is plain Kotlin over `Placeable` sizes — no MeasureSpec bit-twiddling.","**Edge cases that show seniority**: respect min constraints when reporting size (`coerceIn`) or parents may reject it; use `placeRelative` for automatic RTL mirroring; density is in scope (`8.dp.roundToPx()`); and if your algorithm needs to know one child's size *before deciding what else to compose* — that's not Layout anymore, that's `SubcomposeLayout`.","**When asked 'why not just nest Rows/Columns?'**: custom Layout is for genuinely novel geometry (flow layouts, circular menus, staggered arrangements, text-baseline systems) — nesting stays the answer for anything expressible with the standard vocabulary, since single-pass measurement makes it free."]}]},{level:"senior",q:"What are intrinsic measurements, why do they exist if Compose is single-pass, and what do they cost?",a:[{t:"p",text:"**The problem they solve**: single-pass measurement means a parent can't measure a child, look at the result, and re-measure others accordingly. But some designs need exactly that shape of information — the classic: a Row of two texts separated by a divider, where the divider should match the *taller text's* height. The Row can't know that height until children measure, and can't re-measure the divider after. Deadlock with the one-measure rule."},{t:"p",text:"**The mechanism**: intrinsics are a separate, lightweight *query* pass that doesn't count as measurement. Every layout node can answer four questions — min/max intrinsic width for a given height, min/max intrinsic height for a given width — computed recursively from its children (`Text` answers from its text metrics, a Row sums children's widths, etc.). `Modifier.height(IntrinsicSize.Max)` on the Row asks children \"what's the largest height you'd want?\", takes the max, and then runs the *real* single measure pass with that height as a fixed constraint. One query walk + one measure walk — the invariant survives."},{t:"list",items:["**Cost**: an extra recursive tree walk under the intrinsic-modified node, potentially per layout pass. Fine localized (a card, a row); a smell if wrapping whole screens or appearing inside frequently re-laid-out ancestors.","**Accuracy caveat**: intrinsics are *estimates* computed outside real constraints — custom layouts that don't implement them sensibly (default falls back to measuring behavior approximations) can give wrong answers; if you write a serious custom Layout, consider overriding the intrinsic methods too.","**Alternatives to name**: restructure so the design doesn't need it (often possible); `SubcomposeLayout` when the *composition itself* should depend on a measured size (heavier); or fixed heights from the design system when honesty allows."]}]},{level:"senior",q:"SubcomposeLayout: what is it, where does Compose itself use it, and why should you be reluctant to use it?",a:[{t:"p",text:"**The concept**: normally, *composition* (running your composables) fully finishes before *layout* (measuring) begins — layout can only rearrange what composition already created. `SubcomposeLayout` breaks that ordering deliberately: it lets you delay composing part of your content until **measure time**, so you can measure something first and then decide *what to compose* — even feeding one child's measured size into another child's composition as a parameter."},{t:"list",items:["**Where the framework uses it**: `LazyColumn`/`LazyRow`/grids — they can't compose a million items, so at measure time (knowing the viewport size) they subcompose only the visible slice, item by item, until the viewport fills. `Scaffold` — measures top/bottom bars first, then subcomposes content with the bars' sizes available as `paddingValues`. `BoxWithConstraints` — your lambda literally runs at measure time, which is how it can hand you real constraints during composition.","**Why be reluctant**: cost and contract. Subcomposition runs composition machinery inside the measure phase — heavier than a normal measure, defeats some skipping optimizations, and content inside composes *later* than siblings (observable if you do effects at odd times). Most 'I need the size' impulses have cheaper answers: `BoxWithConstraints` gives available space; `onSizeChanged`/`onGloballyPositioned` report sizes after layout (fine for non-layout reactions); intrinsics handle match-the-sibling sizing.","**The interview litmus**: reach for SubcomposeLayout only when *what to compose* genuinely depends on a measurement — building a lazy container, a 'measure A, size B from it' widget pair. If the composition set is fixed and only geometry varies, it's a custom `Layout`."]}]},{level:"senior",q:"Explain the Modifier.Node system — what actually happens to a modifier chain at runtime, and why was the older composed {} approach replaced?",a:[{t:"p",text:"**The concept**: `Modifier.padding(8.dp).clickable{}.background(Blue)` is, at face value, an immutable linked description — cheap to build, but something must turn it into working behavior on the UI tree. The modern runtime does this with **Modifier.Node**: each modifier *element* in the chain materializes a persistent **node** object attached to the composable's LayoutNode. Nodes are typed by capability — `LayoutModifierNode` participates in measurement, `DrawModifierNode` wraps drawing, `PointerInputModifierNode` joins hit-testing, `SemanticsModifierNode` contributes accessibility — and the chain of nodes is what the layout/draw/input systems actually traverse."},{t:"p",text:"**The key property is persistence across recomposition**: when the chain 'changes', the runtime **diffs** the new element list against the existing nodes — unchanged elements keep their node (with all its internal state: gesture-in-progress, animation, focus), changed elements get an in-place `update()`, only added/removed elements create/destroy nodes. So recomposing with an equivalent chain costs near zero, and stateful modifiers don't lose state on recomposition."},{t:"p",text:"**Why `composed {}` was retired for framework use**: the old way to make a stateful modifier was `Modifier.composed { remember { ... } }` — a modifier factory invoking *composition* per use site. That meant every element of every chain ran its own little composition (slot table entries, remember lookups), materialized fresh on every recomposition, unskippable, and unresolvable at chain-construction time. Measurably slow at scale — clickable/padding on thousands of nodes added real frame cost. Modifier.Node moves the state *into the node object* (no composition involved), enabling the diff-and-reuse machinery above; framework modifiers were migrated and custom stateful modifiers should use the `ModifierNodeElement` API today. Knowing this migration story — and that `composed {}` still works but is the slow path — is a genuine depth signal."}]},{level:"senior",q:"A design needs text aligned to another element's baseline, and a divider matching the tallest sibling. Which tools solve relational layout in Compose, and when is ConstraintLayout actually justified?",a:[{t:"list",items:["**Baseline alignment**: Row's `verticalAlignment = Alignment.CenterVertically` is wrong for text pairs of different sizes — use `Modifier.alignByBaseline()` on the texts (Row scope), or `alignBy(FirstBaseline)`/`paddingFrom(FirstBaseline, ...)` for distance-from-baseline specs straight from redlines. Baselines propagate through the layout system as `AlignmentLine`s — custom layouts can expose their own.","**Match-the-tallest sibling**: `Modifier.height(IntrinsicSize.Max)` on the Row + `fillMaxHeight()` on the divider — the intrinsic query gets the max desired height, the real pass fixes everyone to it.","**Percentage/guideline positioning**: fractional sizes (`fillMaxWidth(0.4f)`), weights, or `BoxWithConstraints` arithmetic cover most 'guideline at 30%' cases without any library.","**Overlap and anchoring**: Box with `align` + `offset`; `zIndex` for stacking order."]},{t:"p",text:"**ConstraintLayout's honest niche**: in Views it existed to flatten hierarchies because nesting was expensive — that rationale is void in Compose (single-pass measurement). It remains justified when the design is *inherently relational in ways the above don't express*: barriers (position after the longest of several elements), chains with complex distribution, many elements constrained to each other where nesting would obscure the relationships, or porting an existing complex XML screen where the constraint graph is the clearest spec. Even then it's a normal library choice, not a performance one — and that reframing ('in Compose, ConstraintLayout is about expressiveness, never performance') is exactly the sentence interviewers wait for."}]}],Xx={id:"modifiers-layout",title:"Modifiers & Layout",tagline:"Modifier chains and ordering, the constraints model, Row/Column/Box fluency, custom Layouts, intrinsics, SubcomposeLayout, and Modifier.Node internals.",tags:["Compose","Modifiers","Constraints","Custom Layout","Intrinsics"],content:Yx,qa:Qx},Zx=[{heading:"Why lazy lists exist — the RecyclerView problem, solved differently",blocks:[{t:"p",text:"A list of 10,000 items can't create 10,000 UI elements — memory and time forbid it. The View system solved this with **RecyclerView**: a machine that *recycles* a small pool of ViewHolders, rebinding them as you scroll (with adapters, view types, DiffUtil — a lot of ceremony). Compose solves the same problem with **lazy composition**: `LazyColumn` composes **only the items currently visible** (plus a small buffer), and as you scroll, items leaving the viewport are *disposed* while entering ones are *composed*. No adapter, no ViewHolder, no DiffUtil — the item lambda is the whole API."},{t:"code",title:"The core API",code:`LazyColumn(
    state = rememberLazyListState(),
    contentPadding = PaddingValues(16.dp),
    verticalArrangement = Arrangement.spacedBy(8.dp),
) {
    item { Header() }                              // single item

    items(
        items = articles,
        key = { it.id },                           // STABLE identity — crucial
        contentType = { "article" },               // reuse hint
    ) { article ->
        ArticleCard(article)
    }

    item { Footer() }
}`},{t:"list",items:["**The DSL, not composables**: the `LazyColumn` block is a `LazyListScope` — `item { }` / `items(...)` are *descriptions* registered up front; the lambdas run only when their item scrolls into view. You cannot call arbitrary composables directly in the scope (compile error) — a common early confusion.","Under the hood it's built on **SubcomposeLayout**: at measure time, knowing the viewport, it subcomposes items one by one until the viewport (plus `beyondBoundsItemCount` buffer) is filled.","**Contrast with `Column + verticalScroll`**: that composes *every* child immediately — fine for a settings page of 15 rows, catastrophic for a feed. The decision rule: bounded, small, static content → scrollable Column; unbounded/large/dynamic → LazyColumn.","The family: `LazyRow`, `LazyVerticalGrid`/`LazyHorizontalGrid` (with `GridCells.Fixed(2)` / `Adaptive(120.dp)`), `LazyVerticalStaggeredGrid`, and `HorizontalPager`/`VerticalPager` (same lazy machinery, page-snapping behavior)."]}]},{heading:"Keys — the most important parameter you can pass",blocks:[{t:"p",text:"By default, an item's identity is its **position** (index). That's fine for a static list, and quietly wrong for anything that changes. Suppose item #0 is deleted: every remaining item shifts to a new index — to Compose, *every item changed*, so every visible item recomposes, and worse, **item state migrates to the wrong items**: the `remember`ed 'expanded' flag of old item 3 now belongs to whatever sits at index 3."},{t:"code",title:"What keys fix",code:`// WITHOUT key: delete one item ->
//  - every item after it recomposes (index identity shifted)
//  - remembered state (expanded flags, animations) attaches to WRONG items
//  - no sensible item animations possible

items(messages) { msg -> MessageRow(msg) }

// WITH key: identity follows the DATA ->
//  - unmoved items untouched, moved items keep their state
//  - animateItem() can animate reorders/insertions/deletions
items(messages, key = { it.id }) { msg ->
    MessageRow(msg, Modifier.animateItem())
}`},{t:"list",items:["**Key requirements**: stable (same item → same key across updates), unique within the list, and **saveable** (keys go into saved instance state so scroll position survives recreation — a data-class id is fine, an unparcelable object is not).","**Never use the index as the key** — that's just re-stating the default with extra steps.","Keys are also what `rememberLazyListState()` uses to keep **scroll position anchored to an item** when the list changes above the viewport: with keys, inserting items above doesn't visually jump the list.","`contentType` is the *other* hint: items of the same type can **reuse each other's node structure** when scrolled (Compose keeps a small reuse pool per type, RecyclerView-style but automatic) — meaningful for mixed feeds (ads vs posts); harmless to omit for uniform lists."]}]},{heading:"LazyListState — scroll position, programmatic control, derived facts",blocks:[{t:"code",title:"The state object and the standard patterns around it",code:`val listState = rememberLazyListState()
val scope = rememberCoroutineScope()

LazyColumn(state = listState) { /* ... */ }

// Programmatic scrolling (suspend — needs a coroutine)
Button(onClick = {
    scope.launch { listState.animateScrollToItem(0) }
}) { Text("Back to top") }

// Reacting to scroll WITHOUT recomposing every frame:
val showFab by remember {
    derivedStateOf { listState.firstVisibleItemIndex > 5 }
}

// Infinite scroll / load-more trigger:
val shouldLoadMore by remember {
    derivedStateOf {
        val last = listState.layoutInfo.visibleItemsInfo.lastOrNull()
        last != null && last.index >= listState.layoutInfo.totalItemsCount - 5
    }
}
LaunchedEffect(shouldLoadMore) {
    if (shouldLoadMore) viewModel.loadNextPage()
}`},{t:"list",items:["`rememberLazyListState()` survives recomposition *and* recreation (it's internally saveable — scroll position restores after rotation, keyed to item + offset).","`firstVisibleItemIndex` / `firstVisibleItemScrollOffset` change **every scroll frame** — reading them raw in composition recomposes continuously; wrap scroll-derived booleans in **`derivedStateOf`** (the canonical use case for it).","`layoutInfo` exposes the full visible-items snapshot (indices, offsets, sizes, viewport) — the basis for load-more triggers, scroll analytics, and 'current section' headers.","`scrollToItem` (instant) vs `animateScrollToItem` (animated) — both suspend functions, called from `rememberCoroutineScope()` in event handlers."]}]},{heading:"Performance rules for lazy lists",blocks:[{t:"list",items:["**Always provide keys** for changeable data — identity correctness *is* performance (prevents cascade recomposition on inserts/removals).","**Keep item composables cheap and skippable**: stable parameters (immutable item models), no heavy computation in item bodies — an item composes *during scroll*, on the frame deadline; expensive items = dropped frames at the scroll edge.","**Don't fetch/compute per item-composition**: item lambdas re-run whenever items scroll back into view — image loading must go through a caching loader (Coil handles this), formatting should live in the UI model, not the item body.",'**Never nest a lazy list inside a same-direction scrollable** (`LazyColumn` in `verticalScroll` Column) — infinite max-height constraints crash at runtime ("Vertically scrollable component was measured with an infinity maximum height"). Restructure: make everything items of ONE LazyColumn (`item { Header() }` + `items(feed)`), which is also the correct virtualization.',"**Cross-direction nesting is fine and idiomatic**: `LazyRow` inside `LazyColumn` items (Netflix-style carousels) — give the rows `contentType` and their own keys.","**Avoid 0-pixel initial layouts**: a LazyColumn inside a parent that measures it with zero height composes nothing, then everything when sized — a subtle jank source in dynamically-sized containers.","**Item animations**: `Modifier.animateItem()` (placement/appearance/disappearance) requires keys; without keys there is no 'same item moved' concept to animate.","In debug builds lazy scrolling is notably janky — **judge scroll performance only on release/R8 builds** (or at least with baseline profiles applied); this one sentence defuses a very common false alarm."]}]},{heading:"Sticky headers, multiple item types, and real feed structure",blocks:[{t:"code",title:"A realistic sectioned feed",code:`LazyColumn {
    contactsByLetter.forEach { (letter, contacts) ->
        stickyHeader(key = "header-" + letter) {   // pins until next header pushes it
            LetterHeader(letter)
        }
        items(contacts, key = { it.id }, contentType = { "contact" }) {
            ContactRow(it)
        }
    }

    item(contentType = "loader") {
        if (isLoadingMore) LoadingRow()
    }
}`},{t:"list",items:["**Heterogeneous lists need no view-type enums**: the item lambda is code — `when (item) { is Ad -> AdCard(item); is Post -> PostCard(item) }`; `contentType = { it::class }` keeps the reuse pool efficient per type.","**`stickyHeader`** pins the current section header to the top until the next one arrives — the RecyclerView ItemDecoration nightmare reduced to one DSL call.","Building sections by iterating a grouped map inside the DSL is normal — the DSL executes eagerly (it's cheap registration); only item *content* lambdas are lazy."]}]},{heading:"Paging 3 integration — infinite lists from the data layer",blocks:[{t:"p",text:"For genuinely unbounded data (network pages, DB + network), hand-rolling load-more triggers gets messy — **Paging 3** formalizes it: the data layer exposes `Flow<PagingData<T>>`, and Compose consumes it with `collectAsLazyPagingItems()`:"},{t:"code",title:"Paging 3 in Compose",code:`val pagingItems = viewModel.articles.collectAsLazyPagingItems()

LazyColumn {
    items(
        count = pagingItems.itemCount,
        key = pagingItems.itemKey { it.id },
        contentType = pagingItems.itemContentType { "article" },
    ) { index ->
        val article = pagingItems[index]           // triggers page loads
        if (article != null) ArticleCard(article)
        else ArticlePlaceholder()                  // placeholders enabled
    }

    when (pagingItems.loadState.append) {          // footer states
        is LoadState.Loading -> item { LoadingRow() }
        is LoadState.Error -> item { RetryRow { pagingItems.retry() } }
        else -> Unit
    }
}`},{t:"list",items:["Accessing `pagingItems[index]` near the loaded edge is what triggers fetching the next page — the 'load more' logic lives in the library, not your scroll math.","`loadState.refresh` / `append` / `prepend` give you first-load spinners, end-of-list errors and retry — model them as list items, not overlays, for correct UX.","Mention-worthy nuance: `PagingData` is a stream of *self-updating list snapshots* — it doesn't fit `stateIn`/normal Flow operators; it's cached with `cachedIn(viewModelScope)` (mandatory, or rotation re-fetches everything)."]}]}],eS=[{level:"junior",q:"What is the difference between LazyColumn and Column with verticalScroll?",a:[{t:"p",text:"**The concept**: both scroll vertically, but they compose their children completely differently. A `Column` with `Modifier.verticalScroll(...)` composes **every child immediately** — all of them exist in the composition at once, whether visible or not, and scrolling just moves the viewport over them. `LazyColumn` composes **only the visible items** (plus a small buffer); as you scroll, off-screen items are disposed and incoming ones composed on demand — the same virtualization idea as RecyclerView, but automatic."},{t:"p",text:"**How to choose**: use the scrollable Column for a *bounded, small* set of children — a settings screen with 15 rows, a form. Use LazyColumn for anything *large, unbounded, or dynamic* — a feed, search results, a chat. The failure mode that makes it a real bug, not just a preference: putting hundreds (or a network-paged list) of items in a scrollable Column composes them all up front — memory spikes and the first frame stalls. And you can't fix it by nesting — a LazyColumn inside a verticalScroll Column crashes, because the scroll container offers infinite height and the lazy list can't decide what's 'visible'."}]},{level:"junior",q:"Why should you provide a key in items()? What breaks without it?",a:[{t:"p",text:"**The concept**: Compose identifies each item so it knows, across list updates, which item is 'the same one'. By default that identity is the item's **index** (position). Index identity is fine while the list never changes — but the moment items are inserted, removed, or reordered, indices shift, and Compose can no longer tell 'item moved' from 'item changed'."},{t:"list",items:["**Cascade recomposition**: delete the first item and every following item now has a new index → Compose treats them all as changed → every visible item recomposes, even though their data is identical. With `key = { it.id }`, identity follows the data, so unmoved items are left completely alone.","**State attaches to the wrong item** — the worst bug: an item's `remember`ed state (an expanded/collapsed flag, a half-played animation, a text field's contents) is stored by identity. With index keys, after a deletion the state that belonged to old item #3 silently reattaches to whatever is now at index #3. Users see a checkbox 'jump' to a different row.","**No item animations**: `Modifier.animateItem()` needs stable keys to know an item *moved* rather than *appeared*; without keys, reorders can't be animated."]},{t:"p",text:"**Requirements for a good key**: stable, unique in the list, and saveable (a data-class `id`, not a random UUID generated in the item body — that changes every recomposition and defeats the purpose). And never pass the index as the key: that just reproduces the default."}]},{level:"junior",q:"How do you scroll a LazyColumn programmatically, e.g. a 'scroll to top' button?",a:[{t:"p",text:"**The concept**: scrolling is controlled through a `LazyListState` you create with `rememberLazyListState()` and pass to the list. The scroll functions are **suspend functions** (because scrolling can animate over time and must be cancellable), so you call them from a coroutine — in an event handler, that means a `rememberCoroutineScope()`."},{t:"code",title:"The standard pattern",code:`val listState = rememberLazyListState()
val scope = rememberCoroutineScope()

Scaffold(
    floatingActionButton = {
        FloatingActionButton(onClick = {
            scope.launch { listState.animateScrollToItem(index = 0) }
        }) { Icon(Icons.Default.KeyboardArrowUp, null) }
    }
) { padding ->
    LazyColumn(state = listState, contentPadding = padding) { /* items */ }
}`},{t:"p",text:"`animateScrollToItem(index)` animates smoothly; `scrollToItem(index)` jumps instantly (good for restoring a saved position). Both take an optional pixel offset. A subtle correctness point: launch from `rememberCoroutineScope`, **not** a `LaunchedEffect` triggered by a click flag, and never call the suspend function directly in the composable body — scrolling is an event-driven action, so it belongs to the event's coroutine."}]},{level:"junior",q:"How do you build a list with different item types (e.g. headers and rows, or ads and posts)?",a:[{t:"p",text:"**The concept**: RecyclerView made you enumerate 'view types', override `getItemViewType`, and switch in `onCreateViewHolder`. Compose needs none of that — the item content is *just code*, so you branch with an ordinary `when`."},{t:"code",title:"Heterogeneous list — plain Kotlin branching",code:`LazyColumn {
    items(
        items = feed,
        key = { it.id },
        contentType = { it::class },   // reuse hint: group by type
    ) { entry ->
        when (entry) {
            is FeedItem.Post -> PostCard(entry)
            is FeedItem.Ad   -> AdBanner(entry)
            is FeedItem.Divider -> Divider()
        }
    }

    // fixed-purpose items use item {} directly:
    item(contentType = "header") { FeedHeader() }
}`},{t:"p",text:"**The one thing to add for performance is `contentType`**: Compose keeps a small pool of disposed item node-structures to reuse as you scroll (like recycling), and it can only reuse a structure for an item of the *same* type. Passing `contentType` (e.g. `it::class`) tells it how to group the pool, so a post scrolling in reuses a post's structure instead of rebuilding from scratch. It's optional and harmless to omit on uniform lists, but for mixed feeds it's the difference between smooth and janky recycling."}]},{level:"junior",q:"What does rememberLazyListState() give you, and does scroll position survive rotation?",a:[{t:"p",text:"`rememberLazyListState()` creates the `LazyListState` that owns the list's scroll position and exposes scroll control and inspection. Because it's built with a saver internally, it survives **both** recomposition and configuration change / process recreation — so yes, scroll position is preserved across rotation automatically, you don't wire anything up. It restores by remembering the first visible item's *key* and pixel offset (which is another reason stable keys matter — with index keys, a list that changed above the viewport would restore to the wrong place)."},{t:"p",text:"What it exposes: `firstVisibleItemIndex` and `firstVisibleItemScrollOffset` (the current anchor), `layoutInfo` (a full snapshot of visible items — indices, sizes, offsets, viewport bounds), `isScrollInProgress`, and the suspend scroll functions. You only need to create and pass it when you want to *observe* or *control* scrolling; a plain `LazyColumn {}` without a state parameter gets an internal one and still preserves position."}]},{level:"senior",q:"How do you implement infinite scroll (load-more) efficiently in Compose without recomposing on every scroll frame?",a:[{t:"p",text:"**The trap first**: the naive approach reads `listState.firstVisibleItemIndex` (or `layoutInfo`) directly in the composable to decide when to load more. Those values change on *every scroll frame*, so any composable reading them recomposes continuously while scrolling — you've turned a scroll into a recomposition storm. The fix is to compute the *boolean decision* inside `derivedStateOf`, which only invalidates readers when the decision actually flips."},{t:"code",title:"Load-more with derivedStateOf + LaunchedEffect",code:`val shouldLoadMore by remember {
    derivedStateOf {
        val layoutInfo = listState.layoutInfo
        val lastVisible = layoutInfo.visibleItemsInfo.lastOrNull()?.index ?: 0
        // trigger when within 5 items of the end
        lastVisible >= layoutInfo.totalItemsCount - 5
    }
}

LaunchedEffect(shouldLoadMore) {
    if (shouldLoadMore) viewModel.loadNextPage()
}`},{t:"list",items:["`derivedStateOf` re-evaluates the threshold on every scroll (cheap — just arithmetic) but only *invalidates* the `LaunchedEffect` key when `shouldLoadMore` transitions false→true.","Keying the `LaunchedEffect` on the boolean means the load fires once per crossing, not repeatedly; the ViewModel should also guard against concurrent/duplicate page requests (an `isLoading` flag or a single in-flight job) since the flag can stay true across a few frames.","**The senior answer names the better alternative**: for real network pagination, hand-rolling this is fragile (dedup, error/retry, prepend, placeholders). **Paging 3** does it properly — accessing an item near the edge triggers the fetch inside the library, and `loadState` gives you loading/error/retry as list items. Roll your own only for simple bounded cases; reach for Paging for production infinite feeds."]}]},{level:"senior",q:"A LazyColumn scrolls smoothly in debug but users report jank. Walk through diagnosing lazy-list performance.",a:[{t:"list",items:["**First, rule out the false alarm**: debug builds are *expected* to be janky — no R8, extra Compose runtime checks, no baseline profile → JIT compiling on the fly during scroll. Always measure scroll performance on a **release build** (or at least with baseline profiles). Half of 'my list is janky' reports evaporate here; say this first.","**Check keys**: missing/unstable keys cause whole-list recomposition on any data change — data updating during scroll then compounds jank. Verify stable `key = { it.id }`.","**Check item skippability/stability**: if item parameters are unstable types (a plain `List`, a non-`@Immutable`/`@Stable` model), items recompose needlessly. Enable the Compose compiler metrics/strong-skipping and make item models stable — an unstable item model is the most common real cause.","**Check work done in the item body**: item lambdas run on the frame deadline as items scroll in. Anything heavy there — sorting, formatting, non-cached image decode, allocation — blows the budget. Move formatting into the UI model; ensure images go through a caching loader (Coil) with proper sizing.","**Use the tools**: Layout Inspector recomposition counts to spot items recomposing too often; the Android Studio profiler / `Macrobenchmark` with `FrameTimingMetric` to get real jank numbers; composition tracing to see time per item.","**Add a baseline profile**: for lists specifically, baseline profiles precompile the scroll/compose hot paths and are one of the biggest single wins — measurably fewer dropped frames on first scroll."]},{t:"p",text:"The framing that lands: treat it as a measurement problem — release build + profiler numbers first, then attack in order of likelihood (build type → keys → stability → item-body work → baseline profile). Guessing and sprinkling `remember` is the junior move."}]},{level:"senior",q:"Why does nesting a LazyColumn inside a vertically-scrolling Column crash, and how do you restructure it?",a:[{t:"p",text:"**The mechanism**: a `Modifier.verticalScroll` container measures its content with an **infinite maximum height** — that's how it lets content be taller than the screen and scroll. A `LazyColumn`'s entire job depends on knowing its viewport height so it can decide *which items are visible* and compose only those. Given infinite height, 'visible' would mean 'all of them', which defeats laziness and is undefined — so Compose throws: *\"Vertically scrollable component was measured with an infinity maximum height.\"* The two scrollables have incompatible contracts in the same axis."},{t:"p",text:"**The fix is to make everything one lazy list** rather than nesting. Whatever you were putting *around* the LazyColumn becomes items *of* it — headers, banners, and sections all move into the DSL:"},{t:"code",title:"Restructure: one LazyColumn owns the whole screen",code:`LazyColumn {
    item { ProfileHeader() }          // was above the list
    item { StatsRow() }               // was above the list
    items(posts, key = { it.id }) { PostCard(it) }
    item { Footer() }                 // was below the list
}`},{t:"list",items:["This is also *more* correct, not just a workaround: now the header/footer participate in virtualization too, and there's a single scroll container with one consistent scroll position.","**Cross-axis nesting is completely fine**: a `LazyRow` inside a `LazyColumn` item (horizontal carousels in a vertical feed) — different axes, no infinite-constraint conflict. Give the rows their own keys/contentType.","If you genuinely must nest same-axis (rare), you'd have to give the inner list a *fixed* height (`Modifier.height(300.dp)`) so it isn't measured with infinity — but that usually signals the one-LazyColumn restructure is what you actually want."]}]},{level:"senior",q:"Explain how LazyColumn is implemented under the hood, and how item reuse compares to RecyclerView.",a:[{t:"p",text:"**The foundation is SubcomposeLayout**: unlike normal composables (composed fully before layout), LazyColumn defers composing its items until *measure time*. At measure, it knows the viewport dimensions; starting from the current scroll anchor, it subcomposes items one at a time — measure, accumulate height — until the viewport plus a small buffer (`beyondBoundsItemCount`) is filled, then stops. Items scrolled fully out of that window are disposed (their composition removed from the slot table); items scrolling in are subcomposed fresh. So at any moment only ~viewport-worth of items exist in the composition, regardless of dataset size."},{t:"list",items:["**Reuse pool**: to avoid rebuilding node structures constantly, Compose keeps a small pool of *disposed but reusable* item layouts, grouped by `contentType`. When an item of a matching type scrolls in, it reuses a pooled structure and just updates the changed slots — conceptually RecyclerView's ViewHolder recycling, but the runtime does it for you and it operates on Compose nodes, not Views.","**vs RecyclerView — what's automatic**: no `Adapter`, no `ViewHolder` class, no `onCreateViewHolder`/`onBindViewHolder`, no `getItemViewType`, no `DiffUtil`/`notifyItemChanged`. The item lambda is the bind logic; keys replace DiffUtil (Compose diffs by key + parameter equality); `contentType` replaces view types.","**vs RecyclerView — the tradeoffs to be honest about**: RecyclerView's recycling is more aggressive/mature and its prefetch is very tuned, so extremely heavy lists can still favor it in edge cases; Compose leans on *stability + keys + baseline profiles* to hit smoothness instead. And RecyclerView had years of tooling; Compose lists need the stability discipline in exchange for far less boilerplate.","**Consequence for correctness**: because items are genuinely disposed and recomposed as you scroll, any `remember`ed item state is lost when the item leaves the pool unless it's hoisted or made saveable — a footgun that has no RecyclerView analogue (there, state was never in the item to begin with)."]}]},{level:"senior",q:"How does Paging 3 integrate with Compose, and what problems does it solve that a hand-rolled load-more doesn't?",a:[{t:"p",text:"**The integration**: the data layer exposes `Flow<PagingData<T>>` (from a `Pager` with a `PagingSource`, or `RemoteMediator` for network+DB). In the ViewModel it must be `cachedIn(viewModelScope)`. The UI calls `collectAsLazyPagingItems()` to get a `LazyPagingItems<T>`, and drives the list with `items(count = pagingItems.itemCount, key = pagingItems.itemKey { it.id }, contentType = ...)`, reading each element via `pagingItems[index]` — which is what triggers loading the next page as you approach the edge."},{t:"list",items:["**What it solves over hand-rolled**: automatic page-fetch triggering (no derivedStateOf edge math), request **de-duplication** and cancellation, a proper `loadState` machine (`refresh`/`append`/`prepend` each Loading/NotLoading/Error) so first-load spinners, end-of-list errors, retry, and empty states are modeled uniformly — as list items, not ad-hoc overlays. It also supports **placeholders** (show item count before data loads) and `RemoteMediator` for offline-first (DB is SSOT, network fills pages).","**cachedIn is mandatory, and interviewers check it**: without it, `PagingData` is re-fetched from scratch on every configuration change (rotation reloads page 1). `cachedIn` keeps the loaded pages alive in the ViewModel scope.","**Nuance about the type**: `PagingData` is a stream of self-updating list snapshots — it does *not* behave like a normal Flow of values, so you can't `stateIn`/`map` its contents with ordinary operators (there are dedicated `.map`/`.filter` on PagingData instead). That's why it has its own collection API rather than `collectAsStateWithLifecycle`.","**When NOT to use it**: bounded lists that fully fit in memory (a user's 20 settings, a fixed menu) — Paging's ceremony (PagingSource, load states, cachedIn) is overkill; a plain `items(list)` is correct there. Paging earns its complexity on genuinely unbounded, network-backed data."]}]}],tS={id:"lazy-lists",title:"Lazy Lists & Grids",tagline:"LazyColumn/Row/Grid, why keys matter, LazyListState & derivedStateOf, sticky headers, item types, Paging 3, and lazy-list performance.",tags:["Compose","LazyColumn","Keys","Paging 3","Performance"],content:Zx,qa:eS},nS=[{heading:"What a 'side effect' is and why Compose needs special APIs for them",blocks:[{t:"p",text:"A **side effect** is anything a composable does that reaches *outside* itself and isn't just describing UI: showing a snackbar, starting a coroutine, registering a listener, logging analytics, calling a callback, writing to a repository. The problem is that composable bodies are **not** a safe place for these — recall the rules: a composable can run many times per second (recomposition), be skipped, run in any order, or be abandoned mid-execution. If you fired a network call or logged an event directly in the body, it would fire an unpredictable number of times, or not at all."},{t:"p",text:'So Compose provides **effect APIs**: controlled entry points that run your side-effecting code at well-defined moments in the composition lifecycle — when a composable *enters* the composition, when it *leaves*, when specific *keys change* — instead of on every recomposition. Learning Compose side effects is really learning "which API runs my code at which moment, and how is it cleaned up".'},{t:"note",text:`Frame this in interviews as: "composition must stay a pure description of UI; anything that isn't pure UI goes through an effect handler that ties its execution and cleanup to the composition lifecycle." That one sentence explains why all these APIs exist.`}]},{heading:"LaunchedEffect — run suspend work tied to composition",blocks:[{t:"p",text:"`LaunchedEffect(keys)` launches a **coroutine** when the composable enters the composition, and **cancels it** when the composable leaves. If any key changes, it **cancels the old coroutine and starts a fresh one**. This is the primary way to run suspend work (load data on first show, animate, observe something) from the UI."},{t:"code",title:"The three canonical uses",code:`// 1) Run once when the screen appears (key = Unit / a constant)
LaunchedEffect(Unit) {
    viewModel.trackScreenView()
}

// 2) Re-run when an input changes (key = the input)
LaunchedEffect(userId) {
    viewModel.loadUser(userId)   // old load cancelled if userId changes
}

// 3) Consume a one-shot event stream (snackbars, navigation)
LaunchedEffect(Unit) {
    viewModel.events.collect { event ->
        when (event) {
            is UiEvent.ShowSnackbar -> snackbarHostState.showSnackbar(event.msg)
            is UiEvent.NavigateBack -> navController.popBackStack()
        }
    }
}`},{t:"list",items:["**The keys ARE the contract**: they answer 'when should this restart?'. `LaunchedEffect(userId)` = 'this work belongs to *this* userId; if it changes, abandon and redo'. `LaunchedEffect(Unit)` = 'run once for the composable's whole lifetime'.","The coroutine runs in a scope tied to the composition — leaving the composition cancels it automatically (no manual job management), which is why `LaunchedEffect` is the safe home for `collect`, `delay`, animations, and one-shot loads.","**Common bug — wrong key**: `LaunchedEffect(Unit) { load(userId) }` loads only the *first* userId and never reloads when it changes (Unit never changes). Put the real dependency in the key.","**Don't** use it for work that should outlive the screen (uploads that must finish) — that belongs in the ViewModel/repository with an app-scoped scope; `LaunchedEffect` is for UI-lifetime work."]}]},{heading:"rememberCoroutineScope — launch from event handlers, not composition",blocks:[{t:"p",text:"`LaunchedEffect` starts a coroutine *as part of composition*. But sometimes you need to start a coroutine in response to a **user event** (a click) — and you can't call a suspend function or `LaunchedEffect` from inside an `onClick` lambda. `rememberCoroutineScope()` gives you a `CoroutineScope` bound to the composition that you can `launch` from anywhere, including event handlers."},{t:"code",title:"The distinction that trips people up",code:`val scope = rememberCoroutineScope()
val listState = rememberLazyListState()

// Event-driven -> rememberCoroutineScope (NOT LaunchedEffect)
Button(onClick = {
    scope.launch { listState.animateScrollToItem(0) }
}) { Text("Top") }

// Composition-driven -> LaunchedEffect
LaunchedEffect(selectedTab) {
    pagerState.animateScrollToPage(selectedTab)
}`},{t:"list",items:["**Decision rule**: is the coroutine started *because the composition happened / a key changed* → `LaunchedEffect`. Started *because the user did something* → `rememberCoroutineScope().launch`.","Its scope is cancelled when the composable leaves the composition — same lifetime safety as LaunchedEffect.","Anti-pattern: creating a scope with `CoroutineScope(...)` inside a composable — it isn't lifecycle-aware and leaks; always use `rememberCoroutineScope()`."]}]},{heading:"DisposableEffect — effects that need cleanup",blocks:[{t:"p",text:"When a side effect **registers something that must be unregistered** — a listener, a callback, a broadcast receiver, a sensor — use `DisposableEffect`. It runs setup when the composable enters (or a key changes) and requires you to return an `onDispose { }` block that runs cleanup when it leaves (or before re-running on key change)."},{t:"code",title:"Registering and cleaning up a lifecycle observer",code:`DisposableEffect(lifecycleOwner) {
    val observer = LifecycleEventObserver { _, event ->
        if (event == Lifecycle.Event.ON_RESUME) viewModel.refresh()
    }
    lifecycleOwner.lifecycle.addObserver(observer)   // setup

    onDispose {
        lifecycleOwner.lifecycle.removeObserver(observer)  // cleanup — mandatory
    }
}`},{t:"list",items:["The mental model: `DisposableEffect` is 'setup + teardown' where the teardown is *required*; `LaunchedEffect` is 'a coroutine' where teardown is automatic cancellation. Use Disposable when there's no coroutine but there IS something to undo.","On key change, the *old* effect's `onDispose` runs **before** the new setup — so registrations always balance.","Forgetting `onDispose` (or registering in `LaunchedEffect` without cleanup) is the classic Compose leak — a receiver that outlives the screen."]}]},{heading:"SideEffect and rememberUpdatedState — the smaller, sharper tools",blocks:[{t:"p",text:"**`SideEffect { }`** runs *after every successful recomposition* — used to publish Compose state to a non-Compose object that isn't lifecycle-scoped. Rare, but the right tool when you must push the latest composed value out to imperative code (e.g. updating an analytics user property, configuring a third-party object) on each successful composition."},{t:"code",title:"SideEffect: publish composed state to external code",code:`SideEffect {
    analytics.setUserProperty("theme", if (isDark) "dark" else "light")
}`},{t:"p",text:"**`rememberUpdatedState(value)`** solves the *stale capture* problem: when a long-running effect (keyed to `Unit`, so it must NOT restart) needs to always see the *latest* value of something. It returns a `State` box refreshed every recomposition, so the effect's closure reads the current value instead of the one captured when it started."},{t:"code",title:"rememberUpdatedState: latest callback without restarting the effect",code:`@Composable
fun AutoLogout(onTimeout: () -> Unit) {
    val currentOnTimeout by rememberUpdatedState(onTimeout)
    LaunchedEffect(Unit) {          // must run exactly once — no restart
        delay(5.minutes)
        currentOnTimeout()          // always the latest handler, not the first
    }
}`},{t:"list",items:["The decision behind `rememberUpdatedState` vs keying the effect: 'should this ongoing work restart when the value changes?' No → `rememberUpdatedState`. Yes → put the value in the keys. Same one-liner from the State topic — it's the crux.","`SideEffect` vs `LaunchedEffect`: `SideEffect` is synchronous, runs after *every* recomposition, no coroutine — for pushing values out. `LaunchedEffect` is a coroutine keyed to restart conditions — for doing work."]}]},{heading:"produceState, snapshotFlow, and derivedStateOf — state-producing effects",blocks:[{t:"list",items:["**`produceState`** turns non-Compose sources (a Flow, a callback API, a suspend load) *into* Compose `State`. It launches a coroutine (like LaunchedEffect) and gives you a `value` to write; readers recompose on change. `val user by produceState<Result>(Loading, id) { value = repo.load(id) }` — handy for local, screen-scoped async state without a ViewModel.","**`snapshotFlow { }`** is the reverse bridge: it converts *Compose state reads* into a cold **Flow**, emitting when the read state changes. Use it to apply Flow operators (`debounce`, `distinctUntilChanged`, `filter`) to Compose state — e.g. react to scroll or text with debouncing.","**`derivedStateOf`** (covered fully in the State topic) is the 'compute state from other state, recompose only when the result changes' tool — technically a state object, not an effect, but it lives in the same toolbox."]},{t:"code",title:"snapshotFlow: debounce a Compose-state search query",code:`LaunchedEffect(Unit) {
    snapshotFlow { query }            // Compose state -> Flow
        .debounce(300)
        .distinctUntilChanged()
        .collectLatest { viewModel.search(it) }
}`}]},{heading:"The decision table — which effect API when",blocks:[{t:"table",headers:["Need","API"],rows:[["Run suspend work when composable appears / a key changes","`LaunchedEffect(keys)`"],["Launch a coroutine from a click / event handler","`rememberCoroutineScope().launch`"],["Register + unregister a listener/receiver/callback","`DisposableEffect(keys) { ...; onDispose { } }`"],["Publish composed state to non-Compose imperative code each recomposition","`SideEffect { }`"],["Keep a long-lived effect reading the latest value without restarting","`rememberUpdatedState(value)`"],["Turn a Flow/callback/suspend source into Compose State","`produceState(initial, keys) { }`"],["Turn Compose state into a Flow (to use Flow operators)","`snapshotFlow { }`"],["Compute state from state, recompose only when result changes","`derivedStateOf { }`"]]},{t:"note",text:"If you can articulate this table and the reasoning ('keys = restart contract', 'Disposable = has cleanup', 'rememberCoroutineScope = event-driven', 'rememberUpdatedState = don't restart but stay fresh'), you've covered the vast majority of Compose side-effect interview questions."}]}],aS=[{level:"junior",q:"What is a side effect in Compose and why can't you just write it in the composable body?",a:[{t:"p",text:"**The concept**: a side effect is anything a composable does beyond describing UI — starting a coroutine, showing a snackbar, registering a listener, logging analytics, calling a repository. A composable body is supposed to be a *pure description*: given the same inputs, it emits the same UI and does nothing else observable."},{t:"p",text:"**Why the body is unsafe for them**: composables don't run in a predictable, once-per-thing way. They recompose (possibly many times a second), can be **skipped** when inputs are unchanged, run in **any order**, and can even be **abandoned** mid-execution and restarted. So a network call written directly in the body might fire zero times (skipped), once, or dozens of times (every recomposition of an animation) — completely unpredictable. A snackbar would re-show on every recomposition."},{t:"p",text:"**The solution**: Compose provides *effect APIs* — `LaunchedEffect`, `DisposableEffect`, `SideEffect`, etc. — that run your effect at a *defined moment* in the composition lifecycle (enter, leave, key change) rather than 'whenever the body happens to run', and handle cleanup. Learning side effects is learning which API pins your code to which moment."}]},{level:"junior",q:"What does LaunchedEffect do, and what is the role of its keys?",a:[{t:"p",text:"**The concept**: `LaunchedEffect(keys) { }` launches a coroutine when the composable **enters the composition**, and automatically **cancels** it when the composable **leaves**. Because it's a coroutine, it's the home for suspend work driven by the UI: loading data when a screen appears, running an animation, or collecting a Flow of one-shot events."},{t:"p",text:"**The keys are the restart contract** — they answer the question 'when should this work be thrown away and redone?'. `LaunchedEffect(Unit)` (a constant that never changes) runs **once** for the composable's entire lifetime. `LaunchedEffect(userId)` runs now, and if `userId` changes it **cancels the running coroutine and starts a fresh one** — exactly what you want when the work belongs to a specific input."},{t:"code",title:"Right key vs wrong key",code:`// CORRECT: reloads whenever userId changes
LaunchedEffect(userId) { viewModel.loadUser(userId) }

// BUG: Unit never changes -> loads the FIRST userId, ignores later ones
LaunchedEffect(Unit) { viewModel.loadUser(userId) }`},{t:"p",text:"So the single most common LaunchedEffect bug is a **wrong key**: keying on `Unit` when the effect actually depends on a value means it never re-runs when that value changes. The rule: put every value the effect *depends on* into the keys."}]},{level:"junior",q:"When do you use rememberCoroutineScope instead of LaunchedEffect?",a:[{t:"p",text:"**The concept**: both launch coroutines tied to the composition's lifetime, but they answer different triggers. `LaunchedEffect` launches its coroutine *as part of composition* — it runs when the composable appears or a key changes. `rememberCoroutineScope()` gives you a `CoroutineScope` you can `launch` from *anywhere*, including places that aren't composition — most importantly **event handlers** like `onClick`."},{t:"p",text:"**Why you can't just use LaunchedEffect in a click**: an `onClick` lambda is ordinary (non-composable) code, so you can't call `LaunchedEffect` (a composable) or a suspend function inside it. `rememberCoroutineScope().launch { }` bridges that gap."},{t:"list",items:["**Decision rule**: coroutine started *because composition happened or a key changed* → `LaunchedEffect`. Started *because the user did something* → `rememberCoroutineScope().launch`.","Example: animating a scroll when a tab is *selected via state* → `LaunchedEffect(selectedTab)`. Animating a scroll when a *button is clicked* → `scope.launch { }` in the onClick.","Both scopes cancel when the composable leaves the composition. Never hand-roll `CoroutineScope(...)` in a composable — it's not lifecycle-aware and leaks."]}]},{level:"junior",q:"What is DisposableEffect and when do you need it instead of LaunchedEffect?",a:[{t:"p",text:"**The concept**: `DisposableEffect` is for side effects that **register something you must later unregister** — a listener, a callback, a `BroadcastReceiver`, a sensor subscription, a lifecycle observer. It runs a setup block when the composable enters (or a key changes) and *forces* you to return an `onDispose { }` block that runs when the composable leaves (or before the effect re-runs on a key change)."},{t:"p",text:"**Why not LaunchedEffect**: LaunchedEffect is for *coroutines*, and its cleanup is automatic cancellation of that coroutine. When there's no coroutine but there *is* a resource to release, LaunchedEffect gives you nowhere to put the cleanup — so you'd leak. DisposableEffect's whole reason to exist is the guaranteed teardown."},{t:"code",title:"The setup/teardown shape",code:`DisposableEffect(Unit) {
    val callback = SensorEventListener { /* ... */ }
    sensorManager.registerListener(callback, sensor, RATE)
    onDispose { sensorManager.unregisterListener(callback) }  // required
}`},{t:"p",text:"Mental model: **DisposableEffect = setup + mandatory teardown; LaunchedEffect = a coroutine with automatic teardown.** Forgetting `onDispose` (or worse, doing a registration inside LaunchedEffect with no cleanup) is the classic Compose memory leak — a receiver that outlives its screen."}]},{level:"junior",q:"How do you handle one-time events like navigation or showing a snackbar from a ViewModel in Compose?",a:[{t:"p",text:"**The concept and the trap**: navigation and snackbars must happen **exactly once**, but Compose state is *sticky* — if you put `showSnackbar = true` in state, it re-fires on every recomposition and after rotation. So one-shot events need a mechanism that delivers once and doesn't replay."},{t:"p",text:"**The common approach**: expose events from the ViewModel as a `Channel` (or `SharedFlow`) turned into a `Flow`, and collect it inside a `LaunchedEffect` in the UI. The LaunchedEffect runs the collection for the screen's lifetime and performs the action per event."},{t:"code",title:"Event channel + LaunchedEffect collection",code:`// ViewModel
private val _events = Channel<UiEvent>(Channel.BUFFERED)
val events = _events.receiveAsFlow()

// UI
LaunchedEffect(Unit) {
    viewModel.events.collect { event ->
        when (event) {
            is UiEvent.ShowSnackbar -> snackbarHostState.showSnackbar(event.message)
            is UiEvent.NavigateBack -> navController.popBackStack()
        }
    }
}`},{t:"p",text:"**The nuance to mention** (shows you know the debate): Google actually recommends modeling even these as *state* with a consumption callback, because Channel/SharedFlow events can be dropped or double-handled around lifecycle gaps — a snackbar message field cleared via `onMessageShown()` survives config change and process death. Both are acceptable; the interview signal is knowing *why* raw state is wrong for events and naming at least one correct solution."}]},{level:"senior",q:"Explain rememberUpdatedState. What exact problem does it solve, and how do you decide between it and keying the effect?",a:[{t:"p",text:"**The problem**: some effects must run **exactly once** and never restart — a 5-minute auto-logout timer, a one-time registration — so they're keyed on `Unit`. But such an effect may need to call a callback or read a value that the parent *updates over time*. Because the effect's coroutine closure captured the value at the moment it started (when `Unit` first ran), it will forever use the **stale** original — later recompositions create new callbacks, but the running effect never sees them."},{t:"p",text:"**How `rememberUpdatedState` fixes it**: instead of capturing the raw value, you capture a stable `State` box whose `.value` is **refreshed on every recomposition**. The effect's closure holds the box (which never changes identity, so no restart) but dereferences `.value` at call time, getting the latest. It's a controlled indirection: 'restart nothing, but always read fresh'."},{t:"code",title:"The pattern",code:`@Composable
fun LandingScreen(onTimeout: () -> Unit) {
    val currentOnTimeout by rememberUpdatedState(onTimeout)
    LaunchedEffect(Unit) {           // key = Unit: MUST NOT restart on new onTimeout
        delay(5_000)
        currentOnTimeout()           // latest handler, even if the parent recomposed
    }
}`},{t:"list",items:["**The decision rule** (the heart of the question): ask 'should this ongoing work *restart* when the value changes?' If **yes** → put the value in the effect's keys (`LaunchedEffect(value)`), which cancels and relaunches. If **no** → use `rememberUpdatedState`, which keeps the work running but feeds it the current value.","Concretely: the auto-logout timer must NOT reset just because the parent passed a new `onTimeout` lambda (that would delay logout forever) → `rememberUpdatedState`. But a data load that IS about `userId` *should* restart when `userId` changes → key it.","Same reasoning applies to any long-lived registration (DisposableEffect capturing a callback): if it shouldn't re-register, wrap the callback in `rememberUpdatedState`."]}]},{level:"senior",q:"Compare LaunchedEffect, SideEffect, and DisposableEffect. When is each correct?",a:[{t:"list",items:["**`LaunchedEffect(keys)`** — runs a **coroutine** on enter, cancels on leave, restarts on key change. For anything suspend/async or time-based: loading, collecting flows, animations, delays. Teardown is automatic (cancellation).","**`DisposableEffect(keys)`** — runs a **synchronous setup** on enter and a **required `onDispose`** on leave (and before each key-change re-run). For non-coroutine resources that must be released: listeners, receivers, observers, sensors, third-party subscriptions. Teardown is manual and mandatory.","**`SideEffect { }`** — runs **synchronously after every successful recomposition**, no keys, no cleanup. For pushing a freshly-composed value *out* to non-Compose imperative code that isn't lifecycle-scoped: updating an analytics user property, configuring an external object with the current state. It intentionally has no restart control because it runs every time."]},{t:"p",text:"**The distinguishing questions**: 'Is it a coroutine / async?' → LaunchedEffect. 'Does it register something needing cleanup?' → DisposableEffect. 'Do I just need to publish the latest composed value to imperative code each frame?' → SideEffect. A subtle SideEffect point that shows depth: it runs only on *successful* recomposition (not abandoned ones), which is precisely why it's the safe place to notify external systems — you won't publish a value from a composition that got rolled back."}]},{level:"senior",q:"What are snapshotFlow and produceState, and when would you reach for each?",a:[{t:"p",text:"**They bridge Compose state and the Flow world in opposite directions.** `produceState` goes *Flow/callback/suspend → Compose State*; `snapshotFlow` goes *Compose State → Flow*."},{t:"p",text:"**`produceState(initial, keys) { }`** launches a coroutine (LaunchedEffect under the hood) and gives you a mutable `value` to set; it returns a `State<T>` the UI reads. Use it to produce *local, screen-scoped* async state without spinning up a ViewModel — e.g. loading an image or a detail object for a small composable: `val result by produceState<Result>(Loading, id) { value = repository.load(id) }`. It also supports `awaitDispose` for cleanup if it wraps a callback source."},{t:"p",text:"**`snapshotFlow { }`** observes the Compose state read inside its lambda and emits a new value into a cold Flow whenever that state changes. You reach for it when you want to apply **Flow operators to Compose state** — the textbook case is debouncing: turn a `query` state or `listState.firstVisibleItemIndex` into a Flow and `.debounce().distinctUntilChanged().collect { }`. It only emits on actual changes (deduped), and being cold, it starts when collected (inside a LaunchedEffect)."},{t:"code",title:"snapshotFlow to react to scroll with operators",code:`LaunchedEffect(listState) {
    snapshotFlow { listState.firstVisibleItemIndex }
        .distinctUntilChanged()
        .filter { it > 0 }
        .collect { analytics.trackScrolled(it) }
}`},{t:"p",text:"**When NOT to use them**: `produceState` for anything that belongs to business logic or must survive rotation → that's a ViewModel/StateFlow, not a composable-scoped producer. `snapshotFlow` when a simple `derivedStateOf` suffices — snapshotFlow is for when you specifically need *Flow operators* (debounce, buffering, combine); for a plain 'compute boolean from scroll position', `derivedStateOf` is lighter."}]},{level:"senior",q:"A composable registers a BroadcastReceiver but it keeps leaking / firing after the screen is gone. Diagnose and fix.",a:[{t:"p",text:"**The likely cause**: the receiver was registered in a place with no cleanup — either directly in the composable body (so it re-registers on every recomposition, stacking up dozens of live receivers), or inside a `LaunchedEffect` (which has no teardown hook, so it's registered but never unregistered when the composable leaves). Both leave receivers alive past the screen's lifetime, firing into a dead UI and retaining it in memory."},{t:"code",title:"The correct pattern — DisposableEffect with balanced setup/teardown",code:`DisposableEffect(context) {
    val receiver = object : BroadcastReceiver() {
        override fun onReceive(c: Context?, intent: Intent?) {
            viewModel.onConnectivityChanged(/* ... */)
        }
    }
    val filter = IntentFilter(ConnectivityManager.CONNECTIVITY_ACTION)
    ContextCompat.registerReceiver(context, receiver, filter, ContextCompat.RECEIVER_NOT_EXPORTED)

    onDispose {
        context.unregisterReceiver(receiver)   // the fix: guaranteed cleanup
    }
}`},{t:"list",items:["**Why DisposableEffect**: it runs setup exactly on enter (and on key change), and its `onDispose` runs exactly on leave (and before re-running on key change) — so registrations and unregistrations always balance, one-to-one.","**Watch the keys**: keying on something that changes often (like a lambda) would re-register/unregister repeatedly — key on stable values (`context`), and if the receiver needs the latest callback without re-registering, wrap that callback in `rememberUpdatedState`.","**The deeper fix for many cases**: this kind of app-wide signal (connectivity, auth state) is usually better owned by a repository exposing a `Flow`, which the ViewModel observes — then the UI just collects state and no composable touches receivers at all. Mention this: the *most* robust answer moves the effect out of the UI layer entirely.","**Also consider lifecycle**: if the receiver should only be active while the screen is resumed, combine with a `LifecycleEventObserver` (also in a DisposableEffect) or use `Lifecycle.repeatOnLifecycle` semantics rather than composition presence."]}]},{level:"senior",q:"Why is it wrong to launch long-running work (like an upload that must complete) from LaunchedEffect, and where should it go?",a:[{t:"p",text:"**The concept**: `LaunchedEffect`'s coroutine is scoped to the *composable's presence in the composition*. The instant the composable leaves — the user navigates away, the screen is popped, a config change disposes and (for some hosts) recreates it — that coroutine is **cancelled**. That lifetime is correct for UI work (an animation, a screen-scoped load) but *wrong* for work that must finish regardless of what the UI does."},{t:"p",text:"**Concretely**: start a file upload in `LaunchedEffect` and the user backs out mid-upload → the upload is cancelled halfway. Even keeping the screen open, a rotation can interrupt it. The UI's lifetime is simply not the work's lifetime."},{t:"list",items:["**Screen-scoped-but-survives-config-change work** → the **ViewModel**, in `viewModelScope`. It survives rotation (the ViewModel outlives the composable) and is cancelled only when the ViewModel is cleared (screen truly gone). Most 'load data for this screen' work belongs here, not in LaunchedEffect — LaunchedEffect should usually just *trigger* a ViewModel function or collect its state.","**Work that must outlive even the screen** (uploads, syncs that should complete after the user leaves) → an **application-scoped `CoroutineScope`** injected into a repository, or an app-level component. It's not tied to any UI lifetime.","**Work that must survive process death / reboot / needs constraints** (guaranteed uploads, periodic sync) → **WorkManager**. It persists across process death and app restarts and enforces constraints (network, charging).","**So what IS LaunchedEffect for**: genuinely UI-lifetime, cancel-when-gone work — animations, collecting the ViewModel's event flow, one-shot 'on appear' triggers that call into the ViewModel. The rule: LaunchedEffect *initiates and observes*; it should rarely *own* important work."]}]}],iS={id:"side-effects",title:"Side Effects",tagline:"LaunchedEffect, rememberCoroutineScope, DisposableEffect, SideEffect, rememberUpdatedState, produceState & snapshotFlow — which one, when, and why.",tags:["Compose","LaunchedEffect","DisposableEffect","Effects","Lifecycle"],content:nS,qa:aS},oS=[{heading:"The mental model: recomposition is cheap, but only if skipping works",blocks:[{t:"p",text:"Compose's performance story rests on **skipping**: when a composable is about to recompose, Compose checks each of its inputs — if they're all *equal* to last time, it **skips** executing that composable entirely and reuses its previous output. A well-behaved Compose UI recomposes a handful of small functions per state change because everything else is skipped. Performance problems are almost always **skipping failing** — composables re-executing when their data didn't meaningfully change. To make skipping work, Compose must be able to (a) *compare* inputs reliably and (b) *trust* that equal inputs mean unchanged behavior. Those two requirements are what **stability** is about."},{t:"note",text:"Reframe every 'my Compose screen is slow' question as: 'which composables are recomposing that shouldn't, and why is skipping not kicking in?' That reframing is itself a strong signal."}]},{heading:"Stable vs unstable types — the core concept",blocks:[{t:"p",text:"A type is **stable** (in Compose's sense) if Compose can rely on two things: its `equals()` is consistent with whether a recomposition-relevant change happened, and if any public property changes, Compose is *notified* (either it's immutable, or it uses snapshot state). If a composable's parameters are all stable, Compose marks the composable **skippable** — it can compare old vs new and skip. If any parameter is **unstable**, Compose *can't trust the comparison*, so it conservatively re-executes the composable **every time its parent recomposes**, even if the data is identical."},{t:"list",items:["**Stable by default**: all primitives (`Int`, `Boolean`, `Float`…), `String`, function types (lambdas), and any class the compiler infers as stable (immutable with stable properties). `MutableState` is stable (changes are observed).","**Unstable by default**: `List`, `Map`, `Set`, and other collection *interfaces* — because the interface could be backed by a mutable implementation; Compose can't prove immutability from the type. Also: any class with a `var` property, or a `val` of an unstable type, or classes from modules the compiler can't analyze (e.g. a `Date` from the stdlib, a model from a module without the Compose compiler).","**The subtle one**: `val items: List<Item>` is unstable *even though you never mutate it*, because the static type `List` doesn't guarantee immutability. This single fact causes most real-world unnecessary recomposition."]},{t:"code",title:"Why an unstable parameter breaks skipping",code:`// UNSTABLE: List is an interface -> could be a MutableList ->
// Compose can't prove equality is meaningful -> ItemRow NOT skippable ->
// recomposes on every parent recomposition even if 'items' is identical
@Composable fun ItemList(items: List<Item>) { /* ... */ }

// FIX 1: immutable collection type the compiler trusts
@Composable fun ItemList(items: ImmutableList<Item>) { /* skippable */ }

// FIX 2: annotate the holder as stable/immutable
@Immutable data class ItemsUi(val items: List<Item>)`}]},{heading:"@Stable and @Immutable — promising stability to the compiler",blocks:[{t:"p",text:"When you *know* a type is stable but the compiler can't prove it (e.g. it holds a `List` you promise never to mutate, or comes from a non-Compose module), you can annotate it — a **promise** the compiler trusts without verifying:"},{t:"list",items:["**`@Immutable`** — a strong promise: *all* public properties are `val` and never change after construction (deeply). Compose treats instances as fully interchangeable by `equals`. Use for read-only UI models.","**`@Stable`** — a weaker promise: the type may have mutable properties, *but* any change is observable via snapshot state, and `equals` is consistent. Use for observable holders.","**These are promises, not checks**: if you annotate `@Immutable` and then mutate the underlying list, Compose will skip recompositions it shouldn't and you get **stale UI** — a genuinely nasty, silent bug. Only annotate what's actually true.","**The cleaner alternative**: use `kotlinx.collections.immutable` (`ImmutableList`, `persistentListOf()`) — the compiler *knows* these are immutable, so no promise/annotation needed and no risk of lying."]}]},{heading:"Strong skipping mode — what changed in recent Compose",blocks:[{t:"p",text:"Recent Compose (compiler 2.x, on by default now) introduced **strong skipping mode**, which softens the stability rules significantly — worth knowing because it changes the 'right answer' to older interview questions:"},{t:"list",items:["With strong skipping, composables with **unstable parameters become skippable too** — Compose compares unstable params by **instance equality** (`===`) instead of giving up. So if the parent passes the *same list instance*, the child skips.","**Lambdas are auto-remembered**: previously, an unstable capture in a lambda made it a new instance each recomposition (breaking skipping); strong skipping auto-remembers lambdas, removing a whole class of manual `remember` workarounds.","**What this means practically**: much of the old 'wrap every List in ImmutableList or nothing skips' pain is reduced. But it's not a free pass — passing a *new* list instance each time (e.g. `items.filter { }` inline in the body) still fails `===` and still recomposes. Stability discipline still matters; strong skipping just raises the floor.","Interview-ready phrasing: 'Strong skipping makes unstable-parameter composables skippable via instance equality and auto-remembers lambdas — so the classic advice shifted from *make everything stable or nothing skips* to *avoid creating new instances of your data each recomposition*.'"]}]},{heading:"Diagnosing recomposition — the tools",blocks:[{t:"list",items:["**Layout Inspector (Android Studio)** — shows live **recomposition counts** and **skip counts** per composable while you interact. The first tool: find the composable whose count climbs when it shouldn't. High recompositions + low skips on a static element = a stability problem.","**Compose Compiler Metrics/Reports** — a compiler flag emits reports listing every composable as `skippable`/`restartable` and every class as `stable`/`unstable`, with *why*. This is how you find *which* type is unstable and dragging a composable down. Run it, grep for your slow screen's params.","**Composition tracing** — shows composable execution on the system trace timeline (Perfetto) with names, so you see time spent composing during a janky frame.","**Macrobenchmark + `FrameTimingMetric`** — measures real jank (frame durations) on release builds for scroll/startup; the objective number to optimize against.","**Recomposition highlighting** (developer tooling) — flashes recomposing regions so you can *see* over-recomposition visually."]},{t:"note",text:"Process to state in an interview: reproduce on a release build → Layout Inspector to find the over-recomposing composable → Compiler Reports to find *why* (which unstable type) → fix (stability/keys/deferred reads) → Macrobenchmark to confirm. Naming this workflow beats reciting fixes."}]},{heading:"The optimization toolkit — fixes in order of impact",blocks:[{t:"list",items:["**Fix stability first**: make list-typed params `ImmutableList`/`persistentList`, annotate genuinely-immutable models `@Immutable`, ensure UI models are immutable data classes. Biggest win, addresses the root cause.","**Provide keys in lazy lists** — identity correctness prevents cascade recomposition (see Lazy Lists topic).","**Defer state reads to the latest phase**: for high-frequency values (scroll, drag, animation), read in *lambda* modifiers (`Modifier.offset { }`), `graphicsLayer { }`, or `drawBehind { }` so only layout/draw re-runs, skipping composition entirely. This is the single biggest win for animations.","**Use `derivedStateOf`** to convert high-frequency inputs into low-frequency outputs (recompose only when the *result* changes).","**Keep lambdas stable**: method references (`viewModel::onClick`) or remembered lambdas so children stay skippable (largely automatic under strong skipping, but still relevant when capturing unstable values).","**Don't hoist state higher than needed**: reading state in a big parent invalidates the big scope; read it lower (pass lambdas that read where used) to shrink the recomposition scope.","**Defer reads via lambdas at API boundaries**: pass `() -> Int` instead of `Int` for frequently-changing values so the reading scope, not the caller, tracks the change."]}]},{heading:"Baseline profiles and startup — the production performance lever",blocks:[{t:"p",text:"Compose code ships as bytecode that must be JIT-compiled at runtime — and on first run (and first scroll of a list), that compilation happens *during* the user's interaction, causing startup lag and initial jank. **Baseline Profiles** fix this: they're a list of hot methods/classes precompiled to native code at install time (AOT), so the critical paths are already fast on first launch."},{t:"list",items:["You generate one with a **Macrobenchmark test** that exercises the critical journeys (startup, scroll the main feed); it records the hot paths into `baseline-prof.txt` shipped with the app.","Real-world impact is large: commonly **20–40% faster startup** and dramatically smoother first-scroll — one of the highest-ROI things you can do, and often the *correct* answer to 'my list is janky' when the code is already clean.","Compose libraries ship their own baseline profiles, but your *app's* screens need your own profile to benefit.","Related startup levers: `App Startup` library for initializer ordering, avoiding heavy work in `Application.onCreate`, and lazy-initializing expensive dependencies. Startup is measured as cold/warm/hot; baseline profiles primarily help cold start and first-use jank."]},{t:"note",text:"The crucial testing caveat that resolves most false alarms: **never judge Compose performance on a debug build.** Debug has no R8, extra runtime assertions, and no AOT/baseline compilation — it's *expected* to be janky. Always measure on release (minified) builds, ideally with baseline profiles applied."}]}],sS=[{level:"junior",q:"What does it mean for a composable to be 'skippable', and why does it matter for performance?",a:[{t:"p",text:"**The concept**: when Compose is about to recompose a composable, it can sometimes avoid running it at all. If the composable is **skippable**, Compose compares its current parameters to the values from last time — and if they're all equal, it **skips** executing the function entirely and reuses the previous UI. A skippable composable that gets the same inputs is essentially free."},{t:"p",text:"**Why it matters**: Compose's whole performance model depends on skipping. A state change high in the tree triggers recomposition, but the intent is that *most* of the tree skips — only the few composables whose data actually changed re-execute. When skipping works, a screen recomposes a handful of small functions per change. When skipping *fails* (a composable is not skippable, or its inputs look changed when they aren't), composables re-run needlessly, and that wasted work is what shows up as jank. So 'is this composable skippable, and are its inputs stable?' is the central performance question."}]},{level:"junior",q:"What makes a type 'stable' or 'unstable' in Compose, and why is List unstable?",a:[{t:"p",text:"**The concept**: for Compose to skip a composable, it must trust two things about each parameter's type. One: `equals()` reliably tells whether anything recomposition-relevant changed. Two: if a property *does* change, Compose is notified — meaning the type is either immutable, or backed by observable snapshot state. A type satisfying both is **stable**; Compose can compare it and skip. A type that might change without notice, or whose equality can't be trusted, is **unstable** — and Compose responds by *never* skipping composables that take it, re-running them on every parent recomposition to be safe."},{t:"p",text:"**Why `List` is unstable** (the classic gotcha): `List` is an *interface*. A value typed as `List<Item>` could actually be a `MutableList` underneath — the type gives Compose no guarantee it won't change. Since Compose can't prove immutability from the declared type, it conservatively marks `List` unstable. The surprising consequence: `fun Row(items: List<Item>)` recomposes even when you *never* mutate the list, purely because the *type* doesn't promise immutability."},{t:"list",items:["**Stable by default**: primitives, `String`, lambdas, `MutableState`, and classes the compiler infers immutable.","**Unstable by default**: `List`/`Map`/`Set` (interfaces), any class with a `var`, any class holding an unstable property, and types from modules without the Compose compiler (a `Date`, a model from a pure-Kotlin module).","**Fixes**: use `ImmutableList`/`persistentListOf()` from kotlinx-collections-immutable (compiler-verified immutable), or wrap in an `@Immutable` data class."]}]},{level:"junior",q:"What are @Stable and @Immutable annotations and when do you use them?",a:[{t:"p",text:"**The concept**: sometimes *you* know a type is stable but the *compiler can't prove it* — for example it holds a `List` you promise never to mutate, or it comes from a module the Compose compiler doesn't process. These annotations let you **promise** stability, which the compiler then trusts without verifying."},{t:"list",items:["**`@Immutable`** — the strong promise: every public property is a `val` and nothing changes after construction (all the way down). Use it on read-only UI models (`@Immutable data class ProfileUi(...)`). Instances become fully comparable by `equals` for skipping.","**`@Stable`** — the weaker promise: the object *may* have changing state, but every change is exposed through snapshot state (so Compose gets notified) and `equals` is consistent. Use it on observable holders."]},{t:"p",text:"**The critical caveat**: these are promises, not checks. If you annotate a class `@Immutable` and then actually mutate its list, Compose will trust the promise and skip recompositions it *shouldn't* — leaving **stale UI** that silently shows old data. That's a genuinely hard bug to track down. So only annotate what's truly immutable. The safer path when possible: use `ImmutableList`/`persistentListOf` so the compiler *knows* the truth and you never have to promise anything."}]},{level:"junior",q:"How do you find out which composables are recomposing too much?",a:[{t:"p",text:"**The primary tool is the Layout Inspector in Android Studio.** With the app running, it shows a live tree of composables with two counts per node: how many times each **recomposed** and how many times it **skipped**. You interact with the app and watch the numbers — a composable whose recomposition count climbs while you do something unrelated to it (a static header incrementing during scroll, say) is your culprit. Healthy nodes show lots of skips and few recompositions; a problem node shows the opposite."},{t:"p",text:"**Then, to find *why*, use the Compose Compiler Reports**: a compiler flag makes the build emit files that classify every composable as skippable/restartable and every class as stable/unstable — *with reasons*. So once Layout Inspector tells you *which* composable, the reports tell you *which parameter's type* is unstable and defeating skipping. For real jank numbers on release builds, `Macrobenchmark` with `FrameTimingMetric` measures actual frame durations. The workflow is: Layout Inspector (where) → Compiler Reports (why) → fix → Macrobenchmark (confirm)."}]},{level:"junior",q:"Why should you never judge Compose performance on a debug build?",a:[{t:"p",text:"**The concept**: a debug build is deliberately un-optimized. It has no R8/minification, it includes extra Compose runtime assertions and debugging hooks, and — most importantly — its code runs through the JIT compiler *as you use it* rather than being precompiled. There's no baseline profile applied. So scrolling a list in debug means the device is literally compiling the scroll code on the fly, mid-gesture."},{t:"p",text:"**The consequence**: debug builds are *expected* to be janky, sometimes dramatically so, even when the code is perfectly optimized. A huge fraction of 'my Compose list stutters' reports simply disappear when tested on a release build. So the rule is: always measure performance on a **release (minified) build**, ideally with baseline profiles, before concluding anything is wrong. Saying this first in an interview shows you know the difference between a real problem and a measurement artifact."}]},{level:"senior",q:"Walk through diagnosing and fixing a screen that recomposes excessively.",a:[{t:"list",items:["**0. Confirm it's real**: reproduce on a *release* build. Rule out the debug-JIT artifact before spending time.","**1. Locate (where)**: Layout Inspector recomposition/skip counts while interacting. Identify the composable(s) whose recomposition count grows disproportionately — e.g. a whole list item recomposing on every keystroke in an unrelated search box.","**2. Explain (why)**: enable Compose Compiler Reports. Look up the offending composable — is it marked `skippable`? If not, which parameter is `unstable`? Trace the unstable type (usually a `List`, a model with a `var`, or a class from a non-Compose module).","**3. Fix at the root — stability**: convert list params to `ImmutableList`/`persistentListOf`; make UI models immutable data classes; `@Immutable`-annotate models from modules the compiler can't see; replace `var`s with `val` + copy.","**4. Fix scope size**: if a big parent scope invalidates on a value only a child needs, read the value lower — pass a lambda (`() -> T`) that reads it where used, shrinking the invalidated scope. Don't hoist state higher than its consumers.","**5. Fix high-frequency reads**: for scroll/drag/animation, defer the read to layout/draw via lambda modifiers (`Modifier.offset { }`, `graphicsLayer { }`) or funnel through `derivedStateOf` so composition doesn't re-run per frame.","**6. Keys**: in lazy lists, ensure stable `key = { it.id }` to stop cascade recomposition on data changes.","**7. Verify**: re-check Layout Inspector counts and Macrobenchmark frame timings to confirm the fix, not just assume it."]},{t:"p",text:"The senior signal is the *ordered, measurement-driven* process — locate with tools, explain with reports, fix the root cause (usually stability), verify with numbers — rather than reflexively sprinkling `remember` and `derivedStateOf` hoping something helps."}]},{level:"senior",q:"What is 'strong skipping mode' and how did it change stability best practices?",a:[{t:"p",text:"**The background**: originally, if a composable had *any* unstable parameter, it was not skippable at all — Compose re-ran it on every parent recomposition. That made the classic advice near-absolute: wrap every `List` in `ImmutableList`, make every model stable, or nothing skips. It was a lot of ceremony, and unstable models from other modules were a constant headache."},{t:"p",text:"**What strong skipping changed** (Compose compiler 2.x, now the default): composables with unstable parameters become **skippable anyway**, by comparing those unstable parameters using **instance equality (`===`)** instead of giving up. So if the parent passes the *same instance* of an unstable list, the child now skips. Additionally, **lambdas are automatically remembered**, which eliminated a whole category of manual `remember { }` workarounds that existed because a fresh lambda instance each recomposition used to break a child's skippability."},{t:"list",items:["**The best-practice shift**: from *'make everything stable or nothing skips'* to *'don't create new instances of your data on every recomposition.'* Instance equality only helps if you pass the same instance.","**What still bites**: computing a new collection inline in the body — `items.filter { it.active }` passed to a child — produces a *new* list instance each recomposition, fails `===`, and still recomposes. Fix by remembering the derived list (`remember(items) { items.filter { } }`) or computing it in the ViewModel.","**Stability still matters** for correctness of `equals`-based skipping of *stable* types and for readability of compiler reports — strong skipping raises the floor, it doesn't make stability irrelevant. The mature answer notes both: less manual ceremony now, but 'same instance' discipline replaced 'stable type' discipline rather than removing it."]}]},{level:"senior",q:"How does deferring state reads across the three phases optimize performance? Give a concrete example.",a:[{t:"p",text:"**The concept**: a Compose frame has three phases — composition (run composables), layout (measure/place), draw (paint). Crucially, *the phase in which you read a state value determines which phases must re-run when that value changes*. Read a value during composition and a change forces composition → layout → draw (the full pipeline). Read the *same* value only during layout, and a change skips composition and re-runs just layout → draw. Read it only during draw, and only draw re-runs. For a value that changes 60 times a second (a drag or animation), reading it in composition means recomposing 60 times a second — usually the actual cause of animation jank."},{t:"code",title:"The same animation, three cost levels",code:`// WORST: reads animated offset in COMPOSITION -> recomposes every frame
Box(Modifier.offset(x = animatedX.value.dp))

// BETTER: lambda form reads during LAYOUT -> composition skipped
Box(Modifier.offset { IntOffset(animatedX.value.roundToPx(), 0) })

// For pure visual props (alpha, scale, rotation, color): DRAW only
Box(Modifier.graphicsLayer { alpha = animatedAlpha.value })`},{t:"list",items:["**The mechanism**: the *lambda* forms of `offset`, and `graphicsLayer`/`drawBehind`/`drawWithContent`, take a lambda that Compose invokes during the layout or draw phase. By reading the state *inside* that lambda, the read is recorded against the layout/draw phase, not composition — so the state change invalidates only that phase.","**Concrete win**: animating position with `Modifier.offset(x = state.dp)` recomposes the composable (and re-skips its children, re-diffs, etc.) every frame; switching to `Modifier.offset { }` drops composition work to zero and the animation just re-lays-out — often the difference between 60fps and dropped frames on a complex element.","**Rule of thumb**: any value that changes at animation/gesture frequency should be read as *late* as possible — draw if it's purely visual (alpha, scale, color), layout if it's positional, and only composition if it genuinely changes *what* is composed.","This pairs with `derivedStateOf` (reduce read *frequency*) — deferring reduces *phase cost per read*, derivedStateOf reduces *number of invalidations*; together they handle most high-frequency-state performance issues."]}]},{level:"senior",q:"What are Baseline Profiles, why do they matter for Compose specifically, and how do you create one?",a:[{t:"p",text:"**The problem they solve**: Android ships apps as DEX bytecode that must be compiled to native code to run fast. Without help, this happens *just-in-time* — the device compiles hot methods as they first execute. For Compose that's especially painful: the composition/layout machinery and your screen code all compile *during* the user's first launch and first scroll, causing cold-start lag and initial-scroll jank even in a clean codebase."},{t:"p",text:"**What a Baseline Profile is**: a shipped list of the app's hot classes and methods (the critical startup and scroll paths) that the Android runtime **ahead-of-time compiles at install time**. So on the very first launch, those paths are already native — no mid-interaction JIT. The measured impact is significant: commonly **20–40% faster cold startup** and markedly smoother first scroll of the main list."},{t:"list",items:["**How you create it**: write a `Macrobenchmark` test using `BaselineProfileRule` that drives the critical user journeys — launch the app, scroll the main feed, open key screens. Running it records the exercised methods into a `baseline-prof.txt` bundled with the app; the Baseline Profile Gradle plugin automates generating and packaging it.","**Compose-specific note**: Compose *libraries* ship their own baseline profiles, so the framework's internals are covered — but *your app's* screens aren't, so you must generate a profile for your own critical journeys to get the app-level benefit.","**Where it fits in the performance story**: when a list is janky and the *code is already clean* (stable types, keys, deferred reads), a baseline profile is often the remaining big win — and it's the correct answer to 'first scroll always stutters but subsequent scrolls are fine' (that pattern is classic JIT-on-first-use).","**Related lever**: `startup profiles` (a variant applied at build/dexlayout time) and keeping `Application.onCreate` lean; baseline profiles primarily target cold start and first-use compilation."]}]},{level:"senior",q:"A data class parameter to a composable is unstable because it has one property from a non-Compose module. What are your options?",a:[{t:"p",text:"**Why this happens**: stability is *transitive* — a data class is only inferred stable if *all* its properties are stable. If one property is a type from a module the Compose compiler doesn't process (say a `java.util.Date`, a protobuf-generated class, or a model from a pure-Kotlin `:core:model` module that doesn't apply the Compose compiler), the compiler can't prove that property stable, so it marks the whole enclosing data class unstable — and every composable taking it becomes non-skippable."},{t:"list",items:["**Option 1 — annotate the holder `@Immutable`/`@Stable`**: if the class is genuinely immutable, `@Immutable data class Foo(val date: Date)` promises stability and restores skippability. Cheap, but a *promise* — only valid if it's truly immutable, and it doesn't help other composables using `Date` directly.","**Option 2 — map to a UI model** at the ViewModel boundary: convert the foreign property into a stable type (e.g. `Date` → a preformatted `String` or an epoch `Long`) so the UI model is inferred stable with no annotation and no promise. Often the *best* answer because the UI should receive render-ready data anyway (formatting belongs in the ViewModel), so this fixes stability *and* layering at once.","**Option 3 — the `stability configuration file`**: the Compose compiler lets you list external classes to *treat as stable* project-wide (e.g. add `java.time.LocalDate` to `stability_config.conf`). Ideal for widely-used third-party types you can't annotate and know are immutable — one config entry fixes them everywhere.","**Option 4 — apply the Compose compiler to your own modules**: if the unstable type is *your* model in a `:core:model` module, applying the Compose compiler plugin there lets it infer stability normally (an immutable data class becomes stable automatically). Best for first-party code.","**Option 5 — rely on strong skipping**: with strong skipping on, if the caller passes the *same instance*, the composable skips via `===` even with the unstable param — sometimes 'good enough' without changing types, as long as you're not creating new instances each recomposition."]},{t:"p",text:"The senior instinct: prefer the fix that also improves the design — mapping to a render-ready UI model (Option 2) or compiling your own modules (Option 4) — over sprinkling `@Immutable` promises you have to keep true forever."}]}],rS={id:"performance-stability",title:"Performance & Stability",tagline:"Skippability, stable vs unstable types, @Stable/@Immutable, strong skipping, diagnosing recomposition, deferring reads, and baseline profiles.",tags:["Compose","Stability","Recomposition","Baseline Profiles","Jank"],content:oS,qa:sS},lS=[{heading:"How theming works in Compose — CompositionLocal, not global styles",blocks:[{t:"p",text:"In the View system, themes were global XML attributes resolved through a `Context`. Compose does it differently: a theme is a set of values (colors, typography, shapes) **provided implicitly down the composition tree** via **CompositionLocal**, and composables read them where needed. `MaterialTheme` is just a composable that provides three CompositionLocals — `LocalColorScheme`, `LocalTypography`, `LocalShapes` — to everything inside it."},{t:"code",title:"A theme is a provider + accessor object",code:`@Composable
fun AppTheme(darkTheme: Boolean = isSystemInDarkTheme(), content: @Composable () -> Unit) {
    val colors = if (darkTheme) DarkColorScheme else LightColorScheme
    MaterialTheme(
        colorScheme = colors,
        typography = AppTypography,
        shapes = AppShapes,
        content = content,
    )
}

// Read anywhere below via the MaterialTheme accessor object:
Text(
    text = "Title",
    color = MaterialTheme.colorScheme.primary,     // reads LocalColorScheme
    style = MaterialTheme.typography.headlineMedium // reads LocalTypography
)`},{t:"list",items:["**Why CompositionLocal**: it lets a value be available to a whole subtree *without threading it through every function's parameters*. The theme is 'ambient' — any composable can read `MaterialTheme.colorScheme` without being passed it.","Because reading a CompositionLocal is a **state read**, switching the theme (light→dark) *recomposes exactly the composables that read the changed values* — theming is reactive for free.","Nesting themes is natural: wrap a subtree in another `MaterialTheme(...)` to override values just there (a red-accented section), because the inner provider shadows the outer for that subtree."]}]},{heading:"Material 3, color schemes, and dynamic color",blocks:[{t:"list",items:["**Material 3 (Material You)** is the current design system: `ColorScheme` has semantic roles — `primary`, `onPrimary`, `secondary`, `surface`, `onSurface`, `error`, `surfaceVariant`, etc. You style by *role* (`onSurface` = 'content on a surface'), never by raw hex, so light/dark and dynamic themes swap automatically.","**Light & dark** are two `ColorScheme` instances; you pick based on `isSystemInDarkTheme()`. Because components read roles, the same composables render correctly in both — no separate dark layouts.","**Dynamic color** (Android 12+): `dynamicDarkColorScheme(context)` / `dynamicLightColorScheme(context)` derive the scheme from the user's wallpaper — the 'Material You' personalization. Gate it on `Build.VERSION >= S` with a static fallback for older devices.","**Content color propagation**: `Surface` sets a `LocalContentColor` matching its background, so `Text`/`Icon` inside automatically use the correct 'on' color — a big reason to wrap regions in `Surface` rather than raw `Box` with a background."]},{t:"code",title:"Dynamic color with fallback",code:`val colorScheme = when {
    Build.VERSION.SDK_INT >= Build.VERSION_CODES.S ->
        if (darkTheme) dynamicDarkColorScheme(context) else dynamicLightColorScheme(context)
    darkTheme -> DarkColorScheme
    else -> LightColorScheme
}`}]},{heading:"CompositionLocal — the mechanism, and when to create your own",blocks:[{t:"p",text:"A **CompositionLocal** is a way to pass data implicitly through the composition tree. You *provide* a value at some level and *read* it anywhere below, without parameters in between. Compose ships many: `LocalContext`, `LocalDensity`, `LocalConfiguration`, `LocalLifecycleOwner`, `LocalContentColor`."},{t:"code",title:"Defining and providing a custom CompositionLocal",code:`// Two flavors:
val LocalSpacing = staticCompositionLocalOf { Spacing() }     // rarely changes
val LocalUser = compositionLocalOf<User?> { null }            // changes over time

// Provide:
CompositionLocalProvider(LocalSpacing provides Spacing(large = 24.dp)) {
    Screen()   // everything inside can read LocalSpacing.current
}

// Read:
val spacing = LocalSpacing.current`},{t:"list",items:["**`staticCompositionLocalOf` vs `compositionLocalOf`**: the *static* one doesn't track reads — changing it recomposes the **entire** provided subtree (cheap to read, expensive to change; use for values that essentially never change, like a design-system spacing object). The regular one tracks reads and recomposes only actual readers (use for values that change at runtime, like the current user).","**When to create one**: genuinely cross-cutting, ambient values many composables need — theme extensions (custom spacing/elevation tokens), the current logged-in user for deep trees, analytics loggers. ","**When NOT to** (the interview caveat): CompositionLocal is implicit — it hides dependencies, hurts testability and preview-ability, and makes data flow hard to follow. For normal data, **prefer explicit parameters**. Rule of thumb: use it only when passing the value as a parameter through many layers would be clearly worse, and the value is truly 'ambient'."]}]},{heading:"Animations — the high-level APIs (cover 90% of needs)",blocks:[{t:"p",text:"Compose animations are **state-driven**: you animate by changing state and letting an animation API interpolate toward the new value. The high-level APIs are named for what they animate:"},{t:"code",title:"The everyday animation toolkit",code:`// 1) animate*AsState — animate a single value toward a target
val color by animateColorAsState(if (selected) Blue else Gray, label = "color")
val size by animateDpAsState(if (expanded) 200.dp else 100.dp, label = "size")

// 2) AnimatedVisibility — animate appearance/disappearance
AnimatedVisibility(
    visible = showDetails,
    enter = fadeIn() + expandVertically(),
    exit = fadeOut() + shrinkVertically(),
) { DetailsPanel() }

// 3) AnimatedContent — animate BETWEEN different contents/states
AnimatedContent(targetState = count, label = "count") { value ->
    Text("$value")   // old value animates out, new animates in
}

// 4) Modifier.animateContentSize — animate size changes automatically
Column(Modifier.animateContentSize()) { /* grows/shrinks smoothly */ }

// 5) animateItem() — lazy-list item placement animations (needs keys)
items(list, key = { it.id }) { Row(Modifier.animateItem()) { /* ... */ } }`},{t:"list",items:["**`animate*AsState`** is the workhorse: give it a *target* value; it returns a `State` that smoothly moves there whenever the target changes. Variants for every type (`Float`, `Dp`, `Color`, `Offset`, `IntSize`…), plus `animateValueAsState` with a custom `TwoWayConverter` for your own types.","**`AnimatedVisibility`** animates a composable entering/leaving; enter/exit transitions compose with `+` (`fadeIn() + slideInHorizontally()`).","**`AnimatedContent`** cross-fades/transitions between *different content* for different states (a counter, a loading→loaded swap) with configurable `transitionSpec`.","**`Modifier.animateContentSize()`** smoothly animates a composable's own size changes — great for expandable cards."]}]},{heading:"AnimationSpec — controlling the motion",blocks:[{t:"p",text:"Every animation takes an **`AnimationSpec`** describing *how* the value moves over time. The two families:"},{t:"list",items:["**Duration-based — `tween`**: fixed duration + easing curve. `tween(300, easing = FastOutSlowInEasing)`. Predictable; use for deterministic transitions (a panel sliding in).","**Physics-based — `spring`**: no fixed duration; motion follows spring dynamics via `dampingRatio` (bounciness) and `stiffness` (speed). `spring(dampingRatio = Spring.DampingRatioMediumBouncy)`. Feels natural and, crucially, is **interruptible**: if the target changes mid-animation, a spring smoothly redirects from its current velocity rather than restarting — the right default for gesture-driven and frequently-changing values.","**`keyframes`**: specify values at specific timestamps for complex multi-stage motion.","**`repeatable`/`infiniteRepeatable`**: loop an animation (pulsing, loading shimmer) — used with `rememberInfiniteTransition`."]},{t:"code",title:"Specs and an infinite animation",code:`val offset by animateDpAsState(
    targetValue = target,
    animationSpec = spring(stiffness = Spring.StiffnessLow),
    label = "offset",
)

// Infinite pulse:
val infinite = rememberInfiniteTransition(label = "pulse")
val scale by infinite.animateFloat(
    initialValue = 1f, targetValue = 1.2f,
    animationSpec = infiniteRepeatable(tween(600), RepeatMode.Reverse),
    label = "scale",
)`}]},{heading:"Lower-level animation: Animatable, Transition, and performance",blocks:[{t:"list",items:["**`Animatable`** — the imperative, coroutine-based primitive underneath `animate*AsState`. You `animateTo()`/`snapTo()` from a coroutine and can read velocity, cancel, and chain — needed for **gesture-driven** animation (swipe-to-dismiss, draggable with fling) where you drive the value manually. `val offset = remember { Animatable(0f) }` then `offset.animateTo(target)`.","**`Transition` (`updateTransition`)** — coordinates **multiple animations off one state change**, all staying in sync (e.g. a selection toggling color + size + elevation together). Also gives named child animations for tooling.","**`rememberInfiniteTransition`** — for endless animations (shimmer, pulse) decoupled from state.","**Performance rule** (ties to the Performance topic): high-frequency animation values should be read in **layout/draw**, not composition — `Modifier.offset { }`, `Modifier.graphicsLayer { }` — so each frame re-lays-out or re-draws instead of recomposing. Reading `animatedValue.value` directly in a modifier's non-lambda argument recomposes every frame; the lambda form defers it. This single point is the intersection of the animation and performance topics interviewers love to probe.","Always pass a **`label`** to animations — it names them in the Animation Preview/inspector tooling (and is required-ish in newer APIs)."]}]}],cS=[{level:"junior",q:"How does theming work in Jetpack Compose?",a:[{t:"p",text:"**The concept**: instead of global XML style attributes resolved through a `Context`, Compose provides theme values *implicitly down the composition tree* using **CompositionLocal**. `MaterialTheme` is a composable that wraps your app and provides three things to everything inside it — a `ColorScheme`, a `Typography`, and `Shapes`. Any composable below can read them through the `MaterialTheme` accessor: `MaterialTheme.colorScheme.primary`, `MaterialTheme.typography.bodyLarge`."},{t:"p",text:"**Why this is nice**: the theme is 'ambient' — a `Text` deep in the tree reads the current color without anyone passing it down. And because reading a theme value is a *state read*, toggling light↔dark automatically recomposes exactly the composables that used the changed values — reactive theming with no manual work. You also style by *semantic role* (`onSurface` = content on a surface) rather than raw colors, so the same components render correctly across light, dark, and dynamic themes without separate layouts."}]},{level:"junior",q:"What is CompositionLocal and when should you use it?",a:[{t:"p",text:"**The concept**: a CompositionLocal is a mechanism to pass data implicitly through the composition — you *provide* a value at one level and *read* it (`.current`) anywhere below, without threading it through every function's parameters in between. Compose uses it for ambient things every composable might need: `LocalContext`, `LocalDensity`, `LocalConfiguration`, `LocalContentColor`, and the theme locals."},{t:"code",title:"Provide and read",code:`val LocalSpacing = staticCompositionLocalOf { Spacing() }

CompositionLocalProvider(LocalSpacing provides Spacing(large = 24.dp)) {
    MyScreen()   // anything inside:
}
// val spacing = LocalSpacing.current`},{t:"p",text:"**When to use it**: genuinely cross-cutting, ambient values that a wide subtree needs — theme extensions (custom spacing/elevation tokens beyond Material's), the current user for a deep screen, an analytics logger. **When NOT to** (the important half): it makes data flow *implicit*, which hurts readability, testability, and previews — you can't tell a composable's dependencies from its signature. So for ordinary data, **prefer explicit parameters**. The rule: reach for CompositionLocal only when passing the value as a parameter through many layers would clearly be worse, *and* the value is truly ambient."}]},{level:"junior",q:"How do you support light and dark themes, and what is dynamic color?",a:[{t:"p",text:"**Light/dark**: define two `ColorScheme` instances (a light one and a dark one), then pick between them with `isSystemInDarkTheme()` and pass the choice to `MaterialTheme`. That's it — because all your components read *semantic color roles* (`primary`, `surface`, `onSurface`) rather than hardcoded colors, the exact same composables render correctly in both modes. There's no separate dark layout; the role values differ, the UI code doesn't."},{t:"p",text:"**Dynamic color (Material You, Android 12+)**: instead of your hand-picked palette, the color scheme is *generated from the user's wallpaper* via `dynamicLightColorScheme(context)` / `dynamicDarkColorScheme(context)`. It personalizes the app to the device. You gate it on `Build.VERSION.SDK_INT >= S` and fall back to your static schemes on older devices. A related detail worth mentioning: wrapping regions in `Surface` propagates the correct content color automatically (`LocalContentColor`), so text/icons inside pick the right 'on' color for the surface — which is why `Surface` is preferred over a raw `Box` with a background."}]},{level:"junior",q:"How do you animate a simple value change, like a color or size, in Compose?",a:[{t:"p",text:"**The concept**: Compose animation is state-driven — you don't imperatively run an animation, you change a *target* value and an animation API smoothly interpolates toward it. The simplest tool is the `animate*AsState` family: you give it a target, it returns a `State` that moves there whenever the target changes."},{t:"code",title:"animate*AsState — declarative single-value animation",code:`val bgColor by animateColorAsState(
    targetValue = if (selected) Color.Blue else Color.Gray,
    animationSpec = tween(300),
    label = "bgColor",
)
val padding by animateDpAsState(if (expanded) 24.dp else 8.dp, label = "padding")

Box(Modifier.background(bgColor).padding(padding)) { /* ... */ }`},{t:"p",text:"There's a typed variant for each common type — `animateColorAsState`, `animateDpAsState`, `animateFloatAsState`, `animateOffsetAsState`, and so on. You just read the returned value like any state; when the target flips (e.g. `selected` changes), the value animates rather than jumping. For appearing/disappearing content there's `AnimatedVisibility`, and for animating *between different contents* there's `AnimatedContent` — but `animate*AsState` covers the everyday 'this value should change smoothly' case."}]},{level:"junior",q:"What is the difference between tween and spring animation specs?",a:[{t:"p",text:"**The concept**: an `AnimationSpec` describes *how* a value travels to its target over time, and there are two philosophies. **`tween`** is duration-based: you specify a fixed duration and an easing curve (`tween(300, easing = FastOutSlowInEasing)`), and the value follows that curve for exactly that long. **`spring`** is physics-based: there's no fixed duration — the value moves as if pulled by a spring, controlled by `dampingRatio` (how bouncy) and `stiffness` (how fast), and it settles naturally."},{t:"list",items:["**Predictability vs naturalness**: `tween` is deterministic — good when you need a transition to take a known time (a scripted onboarding step). `spring` feels more organic and matches how physical objects move.","**The decisive difference — interruptibility**: if the target changes *mid-animation*, a spring smoothly redirects from its current position *and velocity* toward the new target. A tween, by contrast, tends to restart. So for anything gesture-driven or frequently-changing (a value that can be re-targeted while animating), spring feels right and tween looks janky. That's why spring is the default for many Compose animations.","Other specs to know: `keyframes` (values at specific timestamps for multi-stage motion) and `infiniteRepeatable` (looping animations like a pulse or shimmer)."]}]},{level:"senior",q:"Explain staticCompositionLocalOf vs compositionLocalOf. What's the performance implication?",a:[{t:"p",text:"**The concept**: both create a CompositionLocal, but they differ in whether Compose *tracks reads* of the value — and that changes what happens when the value changes."},{t:"list",items:["**`compositionLocalOf`** — tracks reads. Compose records which composables read `.current`, so when the provided value changes, it recomposes **only those readers**. Slightly more bookkeeping per read, but changes are surgical. Use for values that **change at runtime** and where only some composables care — e.g. the current user, a runtime setting.","**`staticCompositionLocalOf`** — does *not* track reads. Reading `.current` is a plain lookup with zero tracking overhead. The catch: because Compose doesn't know who read it, when the value changes it must recompose the **entire subtree** under the provider. Cheap to read, expensive to change. Use for values that essentially **never change** after being provided — a design-system spacing object, a fixed logger, the theme's static tokens."]},{t:"p",text:"**The performance decision**: match the tool to the change frequency. A theme's spacing tokens that are set once at app start → `staticCompositionLocalOf` (fast reads, and the 'recompose everything' cost never triggers because it never changes). A value that flips during use → `compositionLocalOf` (you *want* read tracking so a change doesn't blow away the whole subtree). Getting this backwards is a real, subtle performance bug: a frequently-changing `staticCompositionLocalOf` recomposes enormous subtrees on every change."}]},{level:"senior",q:"When would you use Animatable or Transition instead of animate*AsState?",a:[{t:"p",text:"**The tiers**: `animate*AsState` is the declarative, fire-and-forget top layer — perfect when a value should smoothly follow a target and you don't need fine control. You drop to lower-level APIs when you need *control* or *coordination* that the declarative form can't express."},{t:"list",items:["**`Animatable`** — the imperative, coroutine-driven primitive underneath `animate*AsState`. You call `animateTo()` / `snapTo()` from a coroutine, and you can read current **velocity**, **cancel** mid-flight, and chain animations. This is what you need for **gesture-driven** animation: a swipe-to-dismiss or draggable card where the user's finger drives the value directly (`snapTo` during drag) and then you `animateTo` a settle target with the drag's velocity for a natural fling. The declarative API can't do velocity handoff from a gesture — Animatable can.","**`Transition` (`updateTransition`)** — for coordinating **multiple animations that must stay in sync off one state change**. If selecting an item should animate color *and* size *and* elevation together, driving three separate `animate*AsState` risks them drifting; a `Transition` runs them as one synchronized unit keyed to the state, and exposes named child animations that show up nicely in the Animation Preview tooling.","**`rememberInfiniteTransition`** — for endless, state-independent animations (shimmer loading, pulsing indicators)."]},{t:"p",text:"**Rule of thumb**: `animate*AsState` until you need (a) gesture/velocity control or manual cancellation → `Animatable`, or (b) several animations coordinated by one state → `Transition`. Reaching for the low-level API without one of those needs is over-engineering."}]},{level:"senior",q:"How do you make an animation performant — what's the connection between animation and the three phases?",a:[{t:"p",text:"**The core insight**: an animation changes a value ~60 times a second, and *where you read that value* decides how much work each of those 60 frames costs. If the animated value is read during **composition**, every frame recomposes the composable (re-executing it, re-diffing children) — the most expensive path, and the usual cause of janky Compose animations. If it's read during **layout**, composition is skipped and only measurement/placement re-runs. If read during **draw**, only the paint step re-runs. So performant animation means reading the animated value as *late* in the pipeline as possible."},{t:"code",title:"Same animation, escalating efficiency",code:`// COMPOSITION read -> recomposes every frame (avoid for animations)
Box(Modifier.offset(x = animatedX.value.dp))

// LAYOUT read (lambda defers it) -> composition skipped
Box(Modifier.offset { IntOffset(animatedX.value.roundToPx(), 0) })

// DRAW read -> only redraw, for purely visual properties
Box(Modifier.graphicsLayer { alpha = animatedAlpha.value; scaleX = scale.value })`},{t:"list",items:["**The mechanism**: the *lambda* forms of `offset`, plus `graphicsLayer`/`drawBehind`/`drawWithContent`, invoke their lambda during the layout or draw phase. Reading the animated state *inside* that lambda records the read against that phase, so the state change only invalidates layout/draw — not composition.","**Property-to-phase mapping**: positional changes (offset, size) → layout-phase reads; purely visual changes (alpha, scale, rotation, translation, clip) → `graphicsLayer` draw-phase reads. Only animate in composition if the animation genuinely changes *what is composed* (e.g. `AnimatedContent` swapping content).","**`graphicsLayer` bonus**: it renders into a separate layer, so transformations are GPU-cheap and don't even trigger redraw of the content itself for things like alpha/scale.","This is the exact intersection the interviewer is testing: you understand *both* the animation APIs *and* the phase model, and you connect them — 'animate with deferred reads so the 60fps updates hit layout/draw, never composition.'"]}]},{level:"senior",q:"How would you build a custom design system on top of (or beside) MaterialTheme in Compose?",a:[{t:"p",text:"**The concept**: `MaterialTheme` provides exactly three CompositionLocals (color scheme, typography, shapes). A real design system usually needs *more* — custom spacing tokens, elevation levels, brand-specific color roles, custom shapes — and sometimes wants to fully replace Material's vocabulary. Compose supports both extending and replacing because a theme is 'just' a set of CompositionLocals plus an accessor object; you can make your own."},{t:"list",items:["**Approach 1 — extend MaterialTheme**: keep `MaterialTheme` for standard tokens and *add* your own CompositionLocals for what it lacks. Define `val LocalSpacing = staticCompositionLocalOf { Spacing() }`, provide it inside your `AppTheme`, and expose a convenient accessor object (often an `object AppTheme { val spacing @Composable get() = LocalSpacing.current; val colors @Composable get() = MaterialTheme.colorScheme }`) so call sites read `AppTheme.spacing.large`. Low-friction, interoperates with Material components.","**Approach 2 — fully custom theme**: define your *own* `ColorScheme`/`Typography` data classes and CompositionLocals, provide them, and build your own accessor — used when the brand's system doesn't map to Material's roles at all. More work (you re-implement what Material gave you and lose automatic component theming), so justify it only for strong brand systems.","**Provider choice matters**: static tokens (spacing, shapes) that never change at runtime → `staticCompositionLocalOf` (fast reads); anything that can change during use → `compositionLocalOf`.","**Reactive theming falls out for free**: because tokens are read via CompositionLocal (a state read), a theme change recomposes only the readers.","**Testability note**: keep the accessor thin and the tokens as plain data so previews can wrap content in `AppTheme { }` easily — a design system nobody can preview is a failed one."]},{t:"p",text:"The senior framing: a Compose design system is a small set of CompositionLocals + immutable token data classes + a thin accessor, layered on or beside MaterialTheme — and the key decisions are *what to add vs replace*, and *static vs tracked* locals per token's change frequency."}]}],dS={id:"theming-animation",title:"Theming & Animation",tagline:"MaterialTheme & CompositionLocal, Material 3 color roles, dynamic color, animate*AsState, specs (tween/spring), Animatable/Transition, and animation performance.",tags:["Compose","Material 3","CompositionLocal","Animation","Theming"],content:lS,qa:cS},hS=[{heading:"Navigation Compose — the core model",blocks:[{t:"p",text:"**Navigation Compose** manages moving between screens (composables) as a **back stack** owned by a `NavController`. You declare a `NavHost` containing `composable(route) { }` destinations; the NavController swaps which destination is shown and maintains the stack for back handling. Each destination is a composable, not an Activity or Fragment — the whole app can be a single Activity with many composable destinations."},{t:"code",title:"The classic string-route setup",code:`val navController = rememberNavController()

NavHost(navController = navController, startDestination = "feed") {
    composable("feed") {
        FeedScreen(onOpenDetail = { id -> navController.navigate("detail/$id") })
    }
    composable(
        route = "detail/{itemId}",
        arguments = listOf(navArgument("itemId") { type = NavType.StringType }),
    ) { backStackEntry ->
        val itemId = backStackEntry.arguments?.getString("itemId")
        DetailScreen(itemId)
    }
}`},{t:"list",items:["`rememberNavController()` creates and remembers the controller; `navigate(route)` pushes; `popBackStack()` / `navigateUp()` pops.","**Arguments** travel in the route string (`detail/{itemId}`) and are read from `backStackEntry.arguments` — but importantly, they also land in the destination's `SavedStateHandle`, so a ViewModel scoped to that destination gets them automatically.",'**Navigation options**: `navigate(route) { popUpTo("feed") { inclusive = true }; launchSingleTop = true }` — control the stack (clear up to a destination, avoid duplicate top entries) — the Compose equivalent of Activity launch modes.','**Deep links**: `composable(route, deepLinks = listOf(navDeepLink { uriPattern = "app://detail/{itemId}" }))` maps external URIs to destinations.']}]},{heading:"Type-safe navigation (the modern approach)",blocks:[{t:"p",text:"String routes are stringly-typed — typos compile fine and crash at runtime, and argument types must be manually parsed. Recent Navigation Compose supports **type-safe routes** using `@Serializable` objects/classes as destinations, so arguments are real typed fields and the compiler catches mistakes:"},{t:"code",title:"Type-safe destinations",code:`@Serializable object Feed
@Serializable data class Detail(val itemId: String)

NavHost(navController, startDestination = Feed) {
    composable<Feed> {
        FeedScreen(onOpenDetail = { id -> navController.navigate(Detail(itemId = id)) })
    }
    composable<Detail> { backStackEntry ->
        val detail: Detail = backStackEntry.toRoute()   // typed, no manual parsing
        DetailScreen(detail.itemId)
    }
}`},{t:"list",items:["Arguments are constructor parameters of the route class — type-checked, no `navArgument` boilerplate, no string interpolation to get wrong.","`backStackEntry.toRoute<Detail>()` reconstructs the typed object; the ViewModel can also `savedStateHandle.toRoute<Detail>()`.","This is the recommended style for new code; string routes remain valid and common in existing apps — know both."]}]},{heading:"ViewModel scoping in navigation, and passing data between screens",blocks:[{t:"list",items:["**Each destination is a `ViewModelStoreOwner`**: `hiltViewModel()` inside a `composable(route) { }` scopes the ViewModel to *that back-stack entry* — created when you navigate in, cleared when the entry is popped. So a detail screen's ViewModel lives exactly as long as the detail screen.","**Nav-graph-scoped ViewModels** for multi-step flows: scope a ViewModel to a *nested graph* (a checkout flow's several screens) so they share one ViewModel that's cleared when the whole flow is popped — `hiltViewModel(navController.getBackStackEntry(\"checkout_graph\"))`.",'**Returning a result to the previous screen** (e.g. a picker): write into the *previous* entry\'s SavedStateHandle — `navController.previousBackStackEntry?.savedStateHandle?.set("picked", value)` then pop; the previous screen observes it. This replaces `startActivityForResult`/`setFragmentResult`.',"**Don't pass large objects as arguments**: routes/arguments go through saved state and URIs — pass *ids*, and load the data from a repository on the destination. Passing whole parcelable objects risks the same size limits as any saved state."]}]},{heading:"Interop: Compose in Views, and Views in Compose",blocks:[{t:"p",text:"Real apps migrate incrementally, so Compose interoperates with the View system **both directions** — a frequent interview topic because you'll rarely start greenfield."},{t:"code",title:"Both directions",code:`// VIEWS INSIDE COMPOSE: AndroidView hosts a classic View (MapView, WebView,
// AdView, a custom legacy View) inside a composition
AndroidView(
    factory = { context -> MapView(context).apply { onCreate(null) } },
    update = { mapView -> /* push new state INTO the view imperatively */ },
    modifier = Modifier.fillMaxSize(),
)

// COMPOSE INSIDE VIEWS: a ComposeView is a normal View you put in XML or
// add programmatically, then call setContent on
class LegacyFragment : Fragment() {
    override fun onCreateView(/* ... */): View =
        ComposeView(requireContext()).apply {
            setContent { MyComposableScreen() }
        }
}`},{t:"list",items:["**`AndroidView`** embeds a View in Compose: `factory` creates it once, `update` runs on recomposition to push new state into it imperatively (the View is mutated the old way). Use for things Compose lacks or that are expensive to reimplement — `MapView`, `WebView`, ad SDKs, camera preview, complex custom Views.","**`ComposeView`** embeds Compose in the View world: it's a regular View you place in XML (`<androidx.compose.ui.platform.ComposeView>`) or add in code, then call `setContent { }`. This is how you add Compose to an existing Fragment/Activity screen by screen.","**Lifecycle bridging gotchas**: a `MapView`/`WebView` inside `AndroidView` needs its own lifecycle callbacks (`onResume`/`onPause`/`onDestroy`) forwarded — a `DisposableEffect` + `LifecycleEventObserver` is the standard pattern, and forgetting it leaks or freezes the embedded view.","**`AndroidViewBinding`** embeds a whole XML layout (via ViewBinding) inside Compose — handy for reusing an existing complex layout during migration."]}]},{heading:"Semantics — how Compose UI is understood by accessibility and tests",blocks:[{t:"p",text:"Compose has no Views for accessibility services or test frameworks to inspect. Instead, every composable contributes to a parallel **semantics tree** — a description of *what the UI means* (this is a button, its text is 'Submit', it's disabled). Accessibility (TalkBack) and UI tests both read this tree, so getting semantics right serves both at once."},{t:"list",items:['Material components add semantics automatically (`Button` is announced as a button, `Text` exposes its text). You augment with `Modifier.semantics { }`, `contentDescription` (for images/icons), `Modifier.testTag("...")` (a test handle), and `mergeDescendants` (treat a composable + children as one node for a11y).',"`clickable` adds click semantics + focus + the a11y action; a raw `pointerInput` gesture does **not** — so custom gestures need explicit semantics or they're invisible to TalkBack and hard to test. This is a real accessibility bug pattern to name.","Because tests query the same tree, writing accessible UI *is* writing testable UI — a nice point to make."]}]},{heading:"Testing Compose UI",blocks:[{t:"p",text:"Compose UI tests use `createComposeRule()` (or `createAndroidComposeRule<Activity>()`), which sets content and drives it. You **find** nodes in the semantics tree, **act** on them, and **assert** — no Espresso view matchers, no ViewHolders:"},{t:"code",title:"A Compose UI test",code:`@get:Rule val composeRule = createComposeRule()

@Test
fun clickingIncrement_updatesCount() {
    composeRule.setContent { AppTheme { Counter() } }

    composeRule.onNodeWithText("Count: 0").assertIsDisplayed()   // FIND + ASSERT
    composeRule.onNodeWithText("Increment").performClick()        // ACT
    composeRule.onNodeWithText("Count: 1").assertExists()         // ASSERT
}`},{t:"list",items:["**Finders**: `onNodeWithText`, `onNodeWithContentDescription`, `onNodeWithTag` (paired with `Modifier.testTag`), `onNode(matcher)` with rich matchers (`hasText`, `isEnabled`, `hasClickAction`).","**Actions**: `performClick`, `performTextInput`, `performScrollTo`, `performTouchInput { swipeUp() }`.","**Assertions**: `assertIsDisplayed`, `assertExists`, `assertIsEnabled`, `assertTextEquals`, `assert(hasText(...))`.","**Synchronization**: the test **auto-waits for idle** — it synchronizes with recomposition and Compose-driven animations, so you rarely need manual waits. For indefinite animations or external async, control the clock (`composeRule.mainClock.autoAdvance = false`, `advanceTimeBy(...)`) or `waitUntil { }`.","**Prefer testing stateless content composables** (pass fake state + lambdas) for fast, focused UI tests; use the full route + ViewModel only for integration tests. `createComposeRule` runs as an instrumented test on device/emulator (or Robolectric for JVM-hosted Compose tests).","**Screenshot testing** (Paparazzi / Roborazzi / Compose Preview screenshot tests) catches visual regressions without a device — increasingly standard for design-system components."]}]}],uS=[{level:"junior",q:"How does navigation work in Jetpack Compose?",a:[{t:"p",text:"**The concept**: Navigation Compose treats screens as composables managed in a **back stack** by a `NavController`. You set up a `NavHost` that lists your destinations — each `composable(route) { ScreenContent() }` — and the NavController decides which one is currently shown and keeps the history for the back button. A whole app can be a single Activity hosting many composable destinations, rather than many Activities/Fragments."},{t:"code",title:"Minimal setup",code:`val navController = rememberNavController()
NavHost(navController, startDestination = "feed") {
    composable("feed") {
        FeedScreen(onOpen = { id -> navController.navigate("detail/$id") })
    }
    composable("detail/{id}") { entry ->
        DetailScreen(entry.arguments?.getString("id"))
    }
}`},{t:"p",text:"You navigate with `navController.navigate(route)` (push) and `popBackStack()`/`navigateUp()` (pop). Arguments ride in the route string (`detail/{id}`) and are read from the destination's `backStackEntry.arguments`. You can also control the stack with options like `popUpTo` and `launchSingleTop` — the Compose equivalent of Activity launch modes — and map external URIs with deep links. Modern Navigation also supports *type-safe* routes using `@Serializable` classes so arguments are real typed fields instead of strings."}]},{level:"junior",q:"How do you pass arguments to a destination, and why shouldn't you pass large objects?",a:[{t:"p",text:'**How**: with string routes, arguments are placeholders in the route (`"detail/{itemId}"`), supplied when navigating (`navigate("detail/42")`) and read from `backStackEntry.arguments`. With type-safe routes, they\'re constructor parameters of a `@Serializable` route class (`navigate(Detail(itemId = "42"))`), read back via `backStackEntry.toRoute<Detail>()`. Either way they also flow into the destination\'s `SavedStateHandle`, so a ViewModel scoped to that destination receives them automatically — which is the cleanest way to consume them.'},{t:"p",text:"**Why not large objects**: navigation arguments are serialized into the back stack's saved state (and, for deep links, into URIs). That mechanism is meant for small, identifier-like values — passing a whole parcelable object couples screens tightly, risks exceeding saved-state size limits (the same `TransactionTooLargeException` family as any Bundle abuse), and means the object can go stale. The idiomatic approach is to **pass an id** and have the destination load the actual data from a repository (which is the single source of truth anyway). If you truly need to hand a result *back* to the previous screen, use the previous back-stack entry's SavedStateHandle rather than forward arguments."}]},{level:"junior",q:"How do you embed a classic Android View (like a MapView or WebView) in Compose?",a:[{t:"p",text:"**The concept**: some UI isn't available in Compose or is expensive to reimplement — Google Maps' `MapView`, `WebView`, ad SDK views, camera preview, complex legacy custom Views. The `AndroidView` composable embeds any classic `View` inside a composition."},{t:"code",title:"AndroidView with factory + update",code:`AndroidView(
    factory = { context -> WebView(context) },   // created ONCE
    update = { webView -> webView.loadUrl(url) }, // runs on recomposition
    modifier = Modifier.fillMaxSize(),
)`},{t:"p",text:"`factory` builds the View a single time and Compose caches it; `update` runs whenever the composable recomposes, and it's where you *imperatively push new state into the View* (the old mutate-the-widget way) — bridging Compose's declarative world to the View's imperative one. The important gotcha to mention: Views like `MapView`/`WebView` have their own lifecycle (`onResume`/`onPause`/`onDestroy`) that Compose won't call — you forward those using a `DisposableEffect` with a `LifecycleEventObserver`, or the view leaks/freezes. The reverse direction (Compose inside Views) uses `ComposeView` — a normal View you place in XML or a Fragment and call `setContent { }` on, which is how apps migrate to Compose screen by screen."}]},{level:"junior",q:"How do you write a basic UI test for a composable?",a:[{t:"p",text:"**The concept**: Compose UI tests use a `ComposeTestRule` (from `createComposeRule()`), which lets you set content and then interact with it. The pattern is always **find → act → assert**: find a node in the semantics tree, perform an action, assert on the result. There are no Espresso view matchers — you query what the UI *means*, not View objects."},{t:"code",title:"Find, act, assert",code:`@get:Rule val rule = createComposeRule()

@Test fun increments() {
    rule.setContent { AppTheme { Counter() } }

    rule.onNodeWithText("Count: 0").assertIsDisplayed()  // find + assert
    rule.onNodeWithText("Increment").performClick()       // act
    rule.onNodeWithText("Count: 1").assertExists()        // assert
}`},{t:"list",items:['**Finders**: `onNodeWithText`, `onNodeWithContentDescription`, `onNodeWithTag` (with `Modifier.testTag("...")` in the UI).',"**Actions**: `performClick`, `performTextInput`, `performScrollTo`, `performTouchInput { swipeUp() }`.","**Assertions**: `assertIsDisplayed`, `assertExists`, `assertTextEquals`, `assertIsEnabled`.","**Auto-sync**: the test automatically waits for recomposition and Compose animations to settle before each assertion, so you rarely write manual waits. A big convenience over Espresso's idling resources."]}]},{level:"junior",q:"What is the semantics tree in Compose?",a:[{t:"p",text:"**The concept**: because Compose draws everything into essentially one custom View, there are no individual View objects for accessibility services or test frameworks to inspect. So Compose maintains a *parallel* tree — the **semantics tree** — that describes what the UI *means* rather than how it's drawn: 'this node is a button', 'its label is Submit', 'it's disabled', 'this text says Count: 3'. It's the machine-readable meaning of the screen."},{t:"p",text:"**Who reads it**: two audiences, and that's the key insight — **accessibility services** (TalkBack announces nodes from it) and **UI tests** (finders like `onNodeWithText` query it). Material components populate it automatically (a `Button` reports itself as a button), and you enrich it with `contentDescription` for images, `Modifier.semantics { }`, and `Modifier.testTag` for test handles. The practical consequence worth stating: because tests and accessibility read the *same* tree, writing accessible UI is simultaneously writing testable UI — and a custom gesture built on raw `pointerInput` (which adds no semantics) is invisible to *both* TalkBack and tests until you add semantics explicitly."}]},{level:"senior",q:"How are ViewModels scoped in Navigation Compose, and how do you share one across a multi-screen flow?",a:[{t:"p",text:"**The foundation**: each destination's `NavBackStackEntry` is a `ViewModelStoreOwner`. So calling `hiltViewModel()` inside a `composable(route) { }` scopes that ViewModel to *that back-stack entry* — it's created when you navigate to the destination and `onCleared()` when the entry is popped off the stack. This gives precise, automatic lifetimes: a detail screen's ViewModel lives exactly as long as the detail screen is on the stack, surviving that screen's own configuration changes but not outliving it."},{t:"list",items:["**Sharing across a flow** — the main technique: define a **nested navigation graph** for the flow (e.g. a checkout with address → payment → review screens) and scope the ViewModel to the *graph's* back-stack entry instead of an individual screen: `val parentEntry = remember(entry) { navController.getBackStackEntry(\"checkout_graph\") }; val vm = hiltViewModel(parentEntry)`. All three screens get the *same* ViewModel, and it's cleared only when the entire flow is popped. Perfect for wizard-style state that must persist across steps but not leak beyond the flow.","**Returning results to a previous screen**: write into `navController.previousBackStackEntry?.savedStateHandle` before popping; the previous screen observes that SavedStateHandle (as state/flow). This is the Compose replacement for `startActivityForResult` / `setFragmentResult` — used for pickers and selection screens.","**Avoid the anti-pattern**: scoping shared state to the *Activity* ViewModel to 'share it everywhere' — that outlives every flow, accumulates state, and obscures data flow. Graph scoping gives sharing *with* correct cleanup.",'**Arguments → SavedStateHandle**: because destination arguments land in the SavedStateHandle, a scoped ViewModel can read `savedStateHandle.toRoute<Detail>()` (or `savedStateHandle["id"]`) directly — no need to pass args through the composable into the ViewModel manually, and it survives process death.']}]},{level:"senior",q:"Compare string-based routes and type-safe navigation. What are the trade-offs?",a:[{t:"p",text:'**String routes** (`"detail/{itemId}"`) were the original model: destinations and arguments are encoded as strings, navigation is `navigate("detail/$id")`, and you declare argument types with `navArgument`. **Type-safe routes** (recent) use `@Serializable` objects/classes as destinations — `@Serializable data class Detail(val itemId: String)` — so `navigate(Detail(itemId = id))` and `backStackEntry.toRoute<Detail>()` are fully typed.'},{t:"list",items:["**Compile-time safety**: string routes fail at *runtime* on a typo or wrong argument type ('detial/42' compiles fine, crashes on navigate); type-safe routes are checked by the compiler — the biggest advantage.","**Argument handling**: strings require manual placeholders, `navArgument` type declarations, and parsing (`getString`, `getInt`); type-safe routes make arguments plain constructor properties with correct types, including nullable and default values, no parsing.","**Refactoring**: renaming a type-safe destination or changing an argument is a compiler-guided refactor; with strings you grep and pray.","**Readability & discoverability**: a route class documents exactly what arguments a screen needs; a route string hides it in a format convention.","**Costs of type-safe**: requires kotlinx.serialization setup; complex/custom argument types need custom `NavType`; and existing large codebases are full of string routes, so you must still read and maintain them."]},{t:"p",text:"**Recommendation**: type-safe routes for new code — they eliminate a whole class of runtime navigation crashes and make arguments first-class. String routes remain valid and ubiquitous, so know both; a migration can be incremental since they interoperate within the same NavHost."}]},{level:"senior",q:"You're embedding a MapView in Compose and it leaks / goes blank on backgrounding. What's wrong and how do you fix it?",a:[{t:"p",text:"**The root cause**: `MapView` (like `WebView`, camera preview, and other stateful legacy Views) is a lifecycle-aware component — it *requires* `onCreate`/`onStart`/`onResume`/`onPause`/`onStop`/`onDestroy` to be called at the right times to allocate/release its rendering resources. When you drop it into `AndroidView`, Compose creates the View but has **no idea** it needs these callbacks — nothing forwards them. So the map never gets `onResume` (goes blank) and never gets `onDestroy` (leaks native/graphics resources and often the Context)."},{t:"code",title:"The fix — forward the lifecycle via DisposableEffect + observer",code:`@Composable
fun MapViewContainer(modifier: Modifier = Modifier) {
    val context = LocalContext.current
    val lifecycle = LocalLifecycleOwner.current.lifecycle
    val mapView = remember { MapView(context) }

    // Forward host lifecycle events into the MapView
    DisposableEffect(lifecycle, mapView) {
        val observer = LifecycleEventObserver { _, event ->
            when (event) {
                Lifecycle.Event.ON_CREATE  -> mapView.onCreate(Bundle())
                Lifecycle.Event.ON_START   -> mapView.onStart()
                Lifecycle.Event.ON_RESUME  -> mapView.onResume()
                Lifecycle.Event.ON_PAUSE   -> mapView.onPause()
                Lifecycle.Event.ON_STOP    -> mapView.onStop()
                Lifecycle.Event.ON_DESTROY -> mapView.onDestroy()
                else -> {}
            }
        }
        lifecycle.addObserver(observer)
        onDispose {
            lifecycle.removeObserver(observer)
            mapView.onDestroy()   // release when the composable leaves
        }
    }

    AndroidView(factory = { mapView }, modifier = modifier)
}`},{t:"list",items:["**Why `remember` the MapView**: creating it in `AndroidView`'s factory is fine, but you need a stable reference to forward callbacks to — `remember { MapView(context) }` gives it, surviving recomposition.","**Why `DisposableEffect`**: it registers the lifecycle observer on enter and, crucially, its `onDispose` both removes the observer and calls `mapView.onDestroy()` when the composable leaves the composition — closing the leak.","**The general principle**: any stateful, lifecycle-owning View embedded via `AndroidView` needs its lifecycle bridged this way. In practice, use the official wrappers when they exist (Maps Compose's `GoogleMap`, Accompanist/AndroidX WebView wrappers) — they encapsulate exactly this. Hand-rolling it is the fallback and the thing to demonstrate you understand."]}]},{level:"senior",q:"How do you handle test synchronization and animations in Compose UI tests?",a:[{t:"p",text:"**What Compose does for you**: the test framework maintains an *idling* concept synchronized with the Compose runtime — before each assertion/action it waits until there are no pending recompositions, no pending layout/draw, and no running Compose-driven animations. This auto-synchronization is why Compose tests rarely need the manual `IdlingResource` dance Espresso required — you write `performClick()` then immediately `assertExists()` and the framework guarantees the recomposition settled in between."},{t:"list",items:["**Where auto-sync breaks — indefinite animations**: an infinite animation (loading shimmer, pulsing indicator) never goes idle, so the test would hang waiting. Fix: take control of the clock with `composeRule.mainClock.autoAdvance = false`, then manually `mainClock.advanceTimeBy(millis)` to step the animation to known points and assert frame by frame.","**External async (network, other threads)**: Compose can only sync with *its own* work, not your coroutines hitting a fake repository. Use fakes with controllable completion, or `composeRule.waitUntil(timeout) { condition }` to poll the semantics tree until the expected state appears.","**`waitForIdle()` / `awaitIdle()`**: explicit sync points when you've disabled auto-advance or need to force a flush.","**Controlling the clock is also a feature, not just a workaround**: pausing the clock lets you assert intermediate animation states deterministically (e.g. 'at 150ms the fade is half done'), which is impossible with a free-running clock.","**Testing strategy that avoids the pain**: prefer testing *stateless content composables* with injected fake state and lambdas — they have no async and settle instantly — and reserve full route+ViewModel+animation integration tests for the few flows that need them. Fast, stable unit-style UI tests plus a thin layer of integration tests."]}]}],pS={id:"navigation-interop-testing",title:"Navigation, Interop & Testing",tagline:"Navigation Compose & type-safe routes, ViewModel scoping across flows, AndroidView/ComposeView interop, the semantics tree, and Compose UI testing.",tags:["Compose","Navigation","Interop","Semantics","UI Testing"],content:hS,qa:uS},mS={id:"compose",name:"Jetpack Compose (UI)",description:"Android's declarative UI toolkit — from what recomposition really is, through state, layout, lists, effects and performance, to theming, navigation and testing.",topics:[Kx,$x,Xx,tS,iS,rS,dS,pS]},fS=[{heading:"What a coroutine is — a suspendable computation",blocks:[{t:"p",text:"A **coroutine** is a computation that can be **suspended and resumed** without blocking a thread. That's the whole idea. A normal blocking call (like a synchronous network request) *occupies* its thread while waiting — the thread sits idle, unusable, until the result arrives. A coroutine that hits a suspension point instead **releases the thread** while it waits, letting that thread do other work, and resumes later (possibly on a different thread) when the result is ready. You get asynchronous, non-blocking code that *reads* like sequential, blocking code."},{t:"code",title:"Sequential-looking async code",code:`suspend fun loadProfile(id: String): Profile {
    val user = api.getUser(id)          // suspends (doesn't block) until response
    val posts = api.getPosts(id)         // suspends until response
    return Profile(user, posts)          // reads top-to-bottom like blocking code
}`},{t:"list",items:["**Not threads**: a coroutine is much lighter than a thread. Threads are OS resources (each costs ~1MB of stack and kernel scheduling); you can run *millions* of coroutines on a handful of threads because a suspended coroutine consumes almost no resources — it's just an object holding its state.","**The payoff on Android**: the main thread must never block (or the UI freezes / ANR). Coroutines let you write `val data = repository.load()` on the main thread — it *suspends* while loading (freeing the main thread to render) and resumes with the result, no callbacks, no blocking.","**Replaces callback hell and RxJava** for most async work: nested callbacks and complex Rx chains become flat, sequential suspend functions."]}]},{heading:"Suspend functions — the building block",blocks:[{t:"p",text:"A function marked **`suspend`** is one that *can suspend*: it may pause its coroutine at a suspension point and resume later. The keyword is a **compiler contract** — a suspend function can only be called from another suspend function or from within a coroutine. That's because suspension requires machinery the compiler injects (below), which only exists inside a coroutine."},{t:"list",items:["**`suspend` doesn't mean 'runs on a background thread'** — this is the #1 misconception. A suspend function runs on whatever thread its caller's coroutine is on; it just has the *ability* to suspend. Making it main-safe (moving work off the main thread) is a separate concern handled by dispatchers/`withContext`.","**Suspension points** are calls to other suspend functions — `delay()`, `withContext()`, a Retrofit suspend call, `Mutex.lock()`. Between them, code runs normally; *at* them, the coroutine may suspend.","**Calling rule**: `suspend fun` → callable only from a coroutine or another `suspend fun`. To start one from normal code, you launch a coroutine (`scope.launch { }`). This mirrors `@Composable` (callable only from composables) — same 'hidden compiler parameter' idea."]},{t:"note",text:'Interview trap: "Does marking a function `suspend` move it off the main thread?" **No.** It only gives it the *ability to suspend*; it runs on the caller\'s thread. Thread-switching is done with dispatchers (`withContext(Dispatchers.IO)`). Getting this right immediately signals real understanding.'}]},{heading:"How suspension actually works — Continuation Passing Style",blocks:[{t:"p",text:"Under the hood, the compiler transforms every suspend function using **Continuation Passing Style (CPS)** and a **state machine**. This is what interviewers mean by 'how do coroutines work internally':"},{t:"list",items:["**Hidden `Continuation` parameter**: the compiler adds an extra parameter to every suspend function — a `Continuation<T>`, essentially a callback representing 'the rest of the computation after this point'. `suspend fun foo(): T` becomes `fun foo(cont: Continuation<T>): Any?`. Calling `cont.resumeWith(result)` continues where the function left off.","**State machine**: the compiler splits the function body at each suspension point into states (like a `switch`), and generates a class that tracks which state to resume into plus the local variables needed. Each resume enters the `switch` at the right label. This is why a suspend function can 'pause' and 'continue' — it's really a state machine driven by continuation callbacks.","**Suspend or return**: at a suspension point, the callee either returns a value immediately (no suspension needed) or returns a special marker `COROUTINE_SUSPENDED`, signaling the coroutine to suspend. When the awaited result is ready, the continuation is resumed, re-entering the state machine at the saved label.","**So 'suspension' = saving state + returning + resuming later via a callback** — no thread is blocked in between; the thread is free to run other coroutines."]},{t:"code",title:"Roughly what the compiler generates (simplified)",code:`// Your code:
suspend fun load(): Data {
    val a = fetchA()   // suspension point 1
    val b = fetchB(a)  // suspension point 2
    return combine(a, b)
}

// Compiler produces (conceptually) a state machine:
// state 0 -> call fetchA(continuation); may suspend, resume into state 1
// state 1 -> have 'a'; call fetchB(a, continuation); resume into state 2
// state 2 -> have 'b'; return combine(a, b) via continuation.resumeWith(...)`}]},{heading:"suspend vs blocking — the crucial difference",blocks:[{t:"table",headers:["","Blocking (e.g. Thread.sleep)","Suspending (e.g. delay)"],rows:[["Holds the thread while waiting","yes — thread unusable","no — thread freed for other work"],["Cost of many concurrent waits","one thread each (expensive)","cheap — thread shared across coroutines"],["On the main thread","freezes UI / ANR","safe — main thread keeps rendering"],["How it waits","OS blocks the thread","saves state, returns, resumes via callback"]]},{t:"p",text:"`delay(1000)` and `Thread.sleep(1000)` both 'wait a second', but `delay` **suspends** (frees the thread — a thousand coroutines can `delay` on one thread) while `Thread.sleep` **blocks** (occupies the thread — a thousand sleeping tasks need consideration of a thousand threads). This is why coroutines scale so well: waiting is nearly free."}]},{heading:"Sequential by default; concurrency is explicit",blocks:[{t:"p",text:"Inside a coroutine, suspend function calls run **sequentially** — one completes before the next starts, just like normal code. Concurrency is opt-in, which keeps code predictable."},{t:"code",title:"Sequential vs concurrent",code:`// SEQUENTIAL — total time = A + B
suspend fun sequential(): Result {
    val a = fetchA()   // wait for A
    val b = fetchB()   // then wait for B
    return Result(a, b)
}

// CONCURRENT — total time = max(A, B) — start both, then await
suspend fun concurrent(): Result = coroutineScope {
    val a = async { fetchA() }   // starts immediately
    val b = async { fetchB() }   // starts immediately
    Result(a.await(), b.await()) // await both
}`},{t:"list",items:["**Default = sequential**: `val a = fetchA(); val b = fetchB()` waits for A, then B. Simple and correct when B depends on A.","**Explicit concurrency with `async`**: when A and B are independent, `async { }` starts each immediately and `await()` collects results — total time becomes the *slower* of the two, not their sum.","The design philosophy: sequential is the safe default (no accidental races), and you *opt into* concurrency where it helps — the opposite of thread-based code where concurrency is the error-prone default."]}]}],gS=[{level:"junior",q:"What is a coroutine, and how is it different from a thread?",a:[{t:"p",text:"**The concept**: a coroutine is a computation that can be *suspended and resumed* without blocking a thread. When it hits a point where it must wait (a network response, a timer), it releases the thread it was running on — freeing that thread to do other work — and resumes later when the result is ready. This lets you write asynchronous code that reads like straightforward sequential code."},{t:"list",items:["**A thread is a heavy OS resource** — each has its own stack (~1MB) and is scheduled by the operating system. You can only have a limited number before memory and context-switching costs hurt.","**A coroutine is lightweight** — it's essentially an object holding its suspended state, running *on* threads but not owning one. You can run millions of coroutines on a small thread pool because a *suspended* coroutine uses almost no resources.","**Blocking vs suspending is the key**: a blocking task holds its thread while waiting (idle but occupied); a suspending coroutine gives the thread back while waiting. So 1000 coroutines waiting on network can share a few threads, whereas 1000 blocking tasks would need consideration of 1000 threads."]},{t:"p",text:"On Android this matters because the main thread must never block or the UI freezes. Coroutines let you write `val data = repository.load()` right on the main thread — it *suspends* (freeing the main thread to keep rendering) rather than blocking, and resumes with the data."}]},{level:"junior",q:"What does the suspend keyword actually do?",a:[{t:"p",text:"**The concept**: `suspend` marks a function that *can suspend* — pause its coroutine at a suspension point and resume later. It's a compiler contract: a suspend function can only be called from another suspend function or from inside a coroutine, because suspension needs machinery (a hidden `Continuation` parameter) that only exists in a coroutine context."},{t:"p",text:"**The critical thing it does NOT do**: `suspend` does *not* make the function run on a background thread. This is the most common misconception. A suspend function runs on whatever thread its calling coroutine is on — it merely gains the *ability* to suspend at suspension points (calls to other suspend functions like `delay`, `withContext`, or a Retrofit call). If you want the function's work to actually run off the main thread, that's a separate decision made with dispatchers — e.g. wrapping the heavy part in `withContext(Dispatchers.IO)`. So `suspend` = 'this can pause and resume without blocking', not 'this runs elsewhere'."}]},{level:"junior",q:"What is the difference between delay() and Thread.sleep()?",a:[{t:"p",text:"**Both pause for a duration, but in fundamentally different ways.** `Thread.sleep(1000)` **blocks** the thread — the thread is occupied and unusable for that whole second, doing nothing. `delay(1000)` **suspends** the coroutine — it schedules a resume after a second and *releases the thread*, so during that second the thread is free to run other coroutines."},{t:"list",items:["**On the main thread**: `Thread.sleep` on the main thread freezes the UI for that duration (and risks an ANR). `delay` on the main thread is fine — the main thread keeps rendering while the coroutine waits.","**Scalability**: a thousand coroutines all calling `delay` can share a single thread (they're all just suspended, waiting for their resume). A thousand tasks calling `Thread.sleep` would each hold a thread.","**`delay` is a suspend function** (only callable from a coroutine); `Thread.sleep` is a normal blocking call."]},{t:"p",text:"This contrast is the clearest illustration of what 'suspending' means: waiting without blocking. It's why coroutines make waiting nearly free — a suspended coroutine costs almost nothing, while a blocked thread is an expensive resource sitting idle."}]},{level:"junior",q:"How do you run two independent network calls concurrently with coroutines?",a:[{t:"p",text:"**The concept**: by default, suspend calls run *sequentially* — `val a = fetchA(); val b = fetchB()` waits for A to finish before starting B, so the total time is A + B. To run independent calls *concurrently*, you start each with `async` (which begins immediately and returns a `Deferred`) and then `await` both. Total time becomes the *slower* of the two, not the sum."},{t:"code",title:"async for concurrency",code:`suspend fun loadDashboard(): Dashboard = coroutineScope {
    val user = async { api.getUser() }      // starts now
    val feed = async { api.getFeed() }       // starts now, in parallel
    Dashboard(user.await(), feed.await())    // wait for both
}`},{t:"p",text:"The key points: `async` starts the work immediately (not when you call `await`), so both requests are in flight at once; `await()` suspends until that particular result is ready; and you wrap them in `coroutineScope { }` so the function only returns when both complete, and if one fails the other is cancelled (structured concurrency). Use plain sequential calls when B *depends* on A's result; use `async` when they're independent and you want them overlapped."}]},{level:"senior",q:"Explain how coroutine suspension works under the hood (continuations and the state machine).",a:[{t:"p",text:"**The mechanism is Continuation Passing Style (CPS) plus a compiler-generated state machine.** When you write a suspend function, the Kotlin compiler rewrites it substantially:"},{t:"list",items:["**Hidden Continuation parameter**: the compiler adds an extra parameter of type `Continuation<T>` to every suspend function. A `Continuation` is essentially a callback with a `resumeWith(Result<T>)` method representing 'the rest of the computation'. So `suspend fun load(): Data` compiles to something like `fun load(completion: Continuation<Data>): Any?`.","**State machine transformation**: the compiler splits the function body at each suspension point into discrete states and generates a class (a `Continuation` implementation) whose `invokeSuspend` is a big `switch` over a label. Each suspension point gets a label; the class stores which label to resume at, plus the local variables that must survive the suspension.","**Suspend-or-return protocol**: at a suspension point, the called suspend function either returns its result immediately (fast path, no actual suspension) or returns the sentinel `COROUTINE_SUSPENDED`. Returning that sentinel unwinds the call stack — the thread is released. When the awaited work completes, someone calls the continuation's `resumeWith`, which re-invokes the state machine, jumping (via the stored label) to right after the suspension point, with locals restored."]},{t:"p",text:"**Why this matters conceptually**: 'suspension' is not magic threading — it's *saving the computation's state, returning to free the thread, and resuming later via a callback*. There's no blocked thread in between. This also explains real behaviors: locals are stored in the continuation object (so a coroutine has a small heap footprint, not a thread stack); resumption can happen on a different thread (the dispatcher decides); and deeply nested suspend calls form a chain of continuations. When an interviewer asks 'how do coroutines work', this CPS-plus-state-machine explanation is the complete answer."}]},{level:"senior",q:"Why is 'suspend doesn't mean background thread' an important distinction? Give a concrete example.",a:[{t:"p",text:"**Because it's the difference between code that helps and code that still freezes your UI.** `suspend` gives a function the *ability to suspend* at suspension points, but it runs on the caller's thread. If a suspend function does *blocking* or *CPU-heavy* work without a suspension point that switches threads, and it's called from a main-thread coroutine, it runs that heavy work **on the main thread** — freezing the UI exactly as a normal blocking call would."},{t:"code",title:"A suspend function that still blocks the main thread",code:`// BUG: 'suspend' but does blocking IO on whatever thread called it
suspend fun readFileBad(path: String): String {
    return File(path).readText()   // blocking! runs on caller's thread (Main!)
}

// FIX: move the blocking work off-main with a dispatcher
suspend fun readFileGood(path: String): String = withContext(Dispatchers.IO) {
    File(path).readText()          // now runs on an IO thread
}`},{t:"list",items:["The `suspend` keyword on `readFileBad` compiles fine and looks async, but calling it from `viewModelScope.launch { }` (which uses `Dispatchers.Main`) runs `readText()` on the main thread → jank/ANR. The keyword bought nothing here.","**Main-safety is a separate contract**: a well-behaved suspend function is *main-safe* — safe to call from the main thread because it internally switches to the right dispatcher for any blocking/CPU work (`withContext(IO)` for IO, `withContext(Default)` for CPU). Retrofit and Room suspend functions are main-safe because they do this internally.","**The layering rule**: the layer *doing* the blocking work owns the `withContext`, not the caller. So repositories/use-cases wrap their heavy work; ViewModels can then call them from the main thread safely without sprinkling `withContext` everywhere."]},{t:"p",text:"The distinction matters because it's a real, shippable bug: developers assume `suspend` = 'off the main thread', write blocking code in a suspend function, and ship UI jank. The correct mental model — `suspend` = suspendable, dispatchers = threading — prevents it."}]},{level:"senior",q:"Coroutines are described as 'sequential by default'. Why is that a good design, and how do you opt into concurrency safely?",a:[{t:"p",text:"**The design**: inside a coroutine, suspend calls execute one after another — `a()` fully completes before `b()` starts — exactly like ordinary synchronous code. Concurrency is not the default; you explicitly request it. This is a deliberate inversion of thread-based programming, where everything runs concurrently and you must *add* synchronization to make it safe."},{t:"list",items:["**Why sequential-by-default is good**: most bugs in concurrent code come from *accidental* concurrency — two things touching shared state at once, races, ordering assumptions. When the default is sequential, you can't have those bugs unless you deliberately introduce concurrency, and where you do, it's visible and localized. Code is predictable and reads top-to-bottom.","**Opting in with `async`**: for independent work, `async { }` starts a child coroutine immediately and returns a `Deferred<T>`; `await()` gets its result. `val a = async{...}; val b = async{...}; use(a.await(), b.await())` runs both concurrently, taking `max` time instead of the `sum`.","**Doing it *safely* means using structured concurrency**: wrap the `async` calls in `coroutineScope { }` (or launch them in a proper scope). Then the enclosing function won't return until all children complete, and — critically — if one child fails, the others are cancelled and the exception propagates, rather than leaking orphaned coroutines. Naked `GlobalScope.async` opts out of that safety and is an anti-pattern."]},{t:"code",title:"Safe concurrency = async inside a structured scope",code:`suspend fun load(): Screen = coroutineScope {   // structured boundary
    val a = async { repo.getA() }
    val b = async { repo.getB() }
    Screen(a.await(), b.await())
    // if getA() throws, getB() is cancelled and coroutineScope rethrows
}`},{t:"p",text:"So the senior framing: sequential default eliminates whole classes of concurrency bugs and makes async code readable; when you need parallelism you add `async` *inside a structured scope*, which gives you concurrency *with* automatic cancellation and error propagation — parallel speed without orphaned work or swallowed failures."}]}],yS={id:"basics",title:"Coroutine Basics & Suspend",tagline:"What a coroutine is, suspend functions, how suspension works (continuations + state machine), suspend vs blocking, and sequential vs concurrent.",tags:["Coroutines","suspend","Continuation","delay","async"],content:fS,qa:gS},wS=[{heading:"Coroutine builders — how you start a coroutine",blocks:[{t:"p",text:"You can't just call a suspend function from normal code — you need a **coroutine builder** to launch a coroutine that provides the required context. The three builders, each for a different situation:"},{t:"table",headers:["Builder","Returns","Blocks caller?","Use for"],rows:[["`launch`","`Job`","no","fire-and-forget work (no result needed)"],["`async`","`Deferred<T>`","no","concurrent work that produces a result"],["`runBlocking`","`T`","**yes** (blocks the thread)","bridging blocking code / tests / main() only"]]},{t:"code",title:"The three builders",code:`// launch — start work, don't wait for a result
scope.launch {
    repository.syncData()          // fire and forget
}

// async — start concurrent work, get a result via await()
val deferred = scope.async { api.fetch() }
val result = deferred.await()

// runBlocking — BLOCKS the current thread until the block completes
fun main() = runBlocking {          // bridges main() into the coroutine world
    launch { doWork() }
}`},{t:"list",items:["**`launch`** returns a `Job` — a handle to cancel or join the coroutine. Its block returns nothing useful; exceptions in it propagate to the parent (and can crash).","**`async`** returns a `Deferred<T>` (a `Job` with a result). You get the value with `await()`, and — importantly — an exception is *held* in the Deferred and thrown when you `await()` it.","**`runBlocking`** actually **blocks** the calling thread until its coroutine finishes — the opposite of the whole point of coroutines. It's a bridge for `main()` functions, unit tests (though `runTest` is preferred), and calling suspend code from a truly blocking context. **Never use it on the main thread in an app** — it freezes the UI."]}]},{heading:"CoroutineScope — the lifetime of coroutines",blocks:[{t:"p",text:"Every coroutine runs inside a **`CoroutineScope`**, which defines its **lifetime** and holds its **context** (dispatcher, Job, etc.). Builders like `launch`/`async` are extension functions *on* `CoroutineScope` — that's why you write `scope.launch { }`. The scope's job: when the scope is cancelled, *all* coroutines started in it are cancelled. This is the foundation of not leaking coroutines."},{t:"list",items:["A scope is created from a `CoroutineContext` — typically a `Job` (or `SupervisorJob`) plus a `Dispatcher`: `CoroutineScope(SupervisorJob() + Dispatchers.Main)`.","Coroutines launched in a scope are its **children** — cancelling the scope cancels the children; the scope isn't considered complete until its children finish (structured concurrency).","**Never use `GlobalScope`**: it's a scope with no lifetime bound to anything — coroutines launched there live until the process dies and aren't cancelled when your screen/component goes away. It's the classic coroutine-leak source."]}]},{heading:"The Android lifecycle scopes you get for free",blocks:[{t:"list",items:["**`viewModelScope`** — a scope tied to a `ViewModel`; cancelled automatically in `onCleared()`. The home for ViewModel coroutines (data loading, driving state). Uses `Dispatchers.Main.immediate` + a `SupervisorJob`.","**`lifecycleScope`** — tied to a `Lifecycle` (Activity/Fragment); cancelled when the lifecycle is destroyed. For UI-layer coroutines. Often paired with `repeatOnLifecycle` for lifecycle-aware flow collection.","**`rememberCoroutineScope()`** — a Compose scope tied to the composable's presence in the composition; cancelled when it leaves. For launching from event handlers (covered in the Compose Side Effects topic)."]},{t:"code",title:"Using the framework scopes",code:`class MyViewModel : ViewModel() {
    fun refresh() {
        viewModelScope.launch {            // cancelled in onCleared()
            _state.value = repository.load()
        }
    }
}

// In a Fragment:
viewLifecycleOwner.lifecycleScope.launch {
    repeatOnLifecycle(Lifecycle.State.STARTED) {
        viewModel.uiState.collect { render(it) }
    }
}`},{t:"p",text:"The point of these scopes is **automatic cancellation**: when the ViewModel is cleared or the screen is destroyed, every coroutine you launched is cancelled — no leaks, no callbacks firing into dead UI. You almost never create your own scope in app code; you use the lifecycle-provided ones."}]},{heading:"coroutineScope vs supervisorScope — scoping functions",blocks:[{t:"p",text:"`coroutineScope { }` and `supervisorScope { }` are **suspend functions** that create a *new scope* as a child of the current one, run their block, and **don't return until all child coroutines complete**. They're how you do structured concurrency *within* a suspend function (e.g. run several things concurrently and wait for all)."},{t:"code",title:"coroutineScope waits for all children",code:`suspend fun loadAll(): Data = coroutineScope {
    val a = async { fetchA() }
    val b = async { fetchB() }
    Data(a.await(), b.await())
    // returns only after both children finish;
    // if one throws, the other is cancelled and the exception propagates
}`},{t:"list",items:["**`coroutineScope`** — if *any* child fails, it cancels *all* the other children and rethrows the exception. All-or-nothing: use when the results are interdependent (all needed, or the whole operation fails).","**`supervisorScope`** — children fail *independently*: one child throwing does *not* cancel its siblings. Use when children are independent and one failing shouldn't abort the rest (e.g. loading several independent widgets — one can fail while others succeed).","Both differ from `runBlocking` in that they **suspend** (don't block a thread) — they're the suspend-world way to establish a scope, whereas `runBlocking` blocks."]}]},{heading:"launch vs async — and the exception subtlety",blocks:[{t:"list",items:["**`launch`** is for side-effecting, resultless work. An uncaught exception in a `launch` coroutine **propagates immediately** to its parent/scope (and, without a handler, can crash or be routed to a `CoroutineExceptionHandler`).","**`async`** is for producing a value. An exception in an `async` coroutine is **captured in the `Deferred`** and only *rethrown when you call `await()`** — so if you never await, or the async is a top-level coroutine, the exception can be silently deferred. (When `async` is a *child* of a scope, the exception still propagates to the parent on failure, per structured concurrency — the 'await to see it' rule is about the direct throwing site.)","**Practical rule**: use `launch` for 'do this'; use `async` for 'compute this, I'll await it'. Don't use `async` for fire-and-forget (you lose the exception unless you await), and don't use `async` if you're just going to immediately `await` a single call (that's just sequential work — a plain suspend call is clearer)."]},{t:"note",text:'Common interview question: "launch vs async?" → "launch returns a Job for fire-and-forget work and throws exceptions to its parent; async returns a Deferred<T> for concurrent results and holds the exception until await(). Use async only when you actually need the result concurrently."'}]}],vS=[{level:"junior",q:"What is the difference between launch and async?",a:[{t:"p",text:"**Both start a coroutine, but for different purposes.** `launch` is for *fire-and-forget* work that doesn't return a result — it returns a `Job` (a handle to cancel or join). `async` is for *concurrent work that produces a result* — it returns a `Deferred<T>`, and you get the value by calling `await()`."},{t:"code",title:"The distinction",code:`// launch — do something, no result
viewModelScope.launch { repository.sync() }

// async — compute something concurrently, get the result
val a = async { api.getA() }
val b = async { api.getB() }
val combined = a.await() + b.await()   // both ran in parallel`},{t:"list",items:["**Use `launch`** when you just want to perform an action — update state, trigger a save, start a collection. No value comes back.","**Use `async`** when you need a *result* and want *concurrency* — typically several `async` calls started together, then `await`ed, so they run in parallel.","**Don't misuse `async`** for a single call you immediately await — that's just sequential work dressed up; a plain suspend call is clearer. And don't use `async` for fire-and-forget, because its exception is held until you `await` (so it can go unnoticed)."]}]},{level:"junior",q:"What is runBlocking and when should (and shouldn't) you use it?",a:[{t:"p",text:"**The concept**: `runBlocking` is a bridge between the blocking world and the coroutine world. It starts a coroutine and **blocks the current thread** until that coroutine (and its children) complete, returning the result. It's the opposite of normal coroutine behavior — it deliberately blocks a thread — which is exactly why its use is narrow."},{t:"list",items:["**Use it for**: a `main()` function (to call suspend code from a plain entry point), unit tests that need to run suspend code (though `runTest` is preferred for its virtual time), and bridging suspend code into a genuinely blocking API you can't change.","**Never use it on the Android main thread** — it blocks the UI thread until the coroutine finishes, freezing the app and risking an ANR. This is a serious bug, not a style issue.","**Never use it to 'call a suspend function from normal code' in app logic** — that defeats the point of coroutines (you've reintroduced blocking). Instead, launch a coroutine in a proper scope (`viewModelScope.launch`)."]},{t:"p",text:"The mental model: `launch`/`async` are *non-blocking* (the caller keeps going), while `runBlocking` is *blocking* (the caller's thread waits). In app code you almost always want the non-blocking builders in a lifecycle scope; `runBlocking` is a tool for tests and top-level entry points."}]},{level:"junior",q:"What is a CoroutineScope and why does it matter?",a:[{t:"p",text:"**The concept**: a `CoroutineScope` defines the *lifetime* of the coroutines started within it. Every coroutine belongs to a scope, and the builders (`launch`, `async`) are extension functions on `CoroutineScope`. The scope's crucial property: **when it's cancelled, all coroutines started in it are cancelled too.** That's how you avoid leaking coroutines that keep running after the thing that started them is gone."},{t:"p",text:"**Why it matters on Android**: consider a network call launched from a screen. If the user leaves the screen, that call should stop — otherwise it wastes resources and might try to update a destroyed UI (crash). By launching in a scope tied to the screen's lifecycle (`viewModelScope`, `lifecycleScope`), cancellation is automatic: the scope is cancelled when the ViewModel is cleared or the screen destroyed, and every coroutine in it stops. The rule that follows: never use `GlobalScope`, which has *no* lifetime bound to anything — its coroutines live until the process dies and are the classic leak. Always launch in a scope that matches how long the work should live."}]},{level:"junior",q:"What is viewModelScope and when is it cancelled?",a:[{t:"p",text:"**The concept**: `viewModelScope` is a `CoroutineScope` provided by the `androidx.lifecycle` library on every `ViewModel`. Coroutines launched in it are automatically **cancelled when the ViewModel is cleared** — i.e. in `onCleared()`, which happens when the ViewModel's owner (screen) is permanently gone (not on rotation — the ViewModel survives that)."},{t:"code",title:"The standard usage",code:`class FeedViewModel : ViewModel() {
    fun load() {
        viewModelScope.launch {          // auto-cancelled in onCleared()
            _state.value = repository.getFeed()
        }
    }
}`},{t:"p",text:"It's built with `Dispatchers.Main.immediate` (so it starts on the main thread without an extra dispatch) and a `SupervisorJob` (so one failed child coroutine doesn't cancel its siblings). The benefit is you never manually manage cancellation for ViewModel work — leave the screen, and all in-flight loads stop. The only caveat: work that must *outlive* the screen (an upload that should finish after the user navigates away) should *not* go in `viewModelScope` — it belongs in an application-scoped scope or WorkManager, because `viewModelScope` is meant to die with the screen."}]},{level:"senior",q:"Explain coroutineScope vs supervisorScope. When would you use each?",a:[{t:"p",text:"**Both are suspend functions that create a new child scope, run a block, and suspend until all child coroutines complete — the difference is how they handle a child's failure.** `coroutineScope` uses a regular `Job`, so failure propagates: if any child throws, *all* other children are cancelled and the exception is rethrown from the scope. `supervisorScope` uses a `SupervisorJob`, so children fail *independently*: one child throwing does **not** cancel its siblings."},{t:"list",items:["**Use `coroutineScope` for all-or-nothing work** — when the results are interdependent and a partial result is useless. Loading a screen that needs *both* the user profile *and* the settings: if either fails, the whole load fails, and you don't want the other call wastefully continuing. This is the common case.","**Use `supervisorScope` for independent work** — when children don't depend on each other and one failing shouldn't abort the others. A dashboard loading several independent widgets (weather, news, stocks): if the news call fails, you still want weather and stocks to load and show, with just the news widget showing an error."]},{t:"code",title:"The behavioral difference",code:`// coroutineScope: b failing cancels a, whole thing throws
coroutineScope {
    val a = async { fetchA() }
    val b = async { fetchB() }   // if this throws, a is cancelled too
    combine(a.await(), b.await())
}

// supervisorScope: widgets fail independently
supervisorScope {
    launch { loadWeather() }     // failure here doesn't stop the others
    launch { loadNews() }
    launch { loadStocks() }
}`},{t:"p",text:"**A subtlety worth naming**: in `supervisorScope`, because failures don't propagate to the parent automatically, each child must handle its *own* exceptions (try/catch or a `CoroutineExceptionHandler` on the launch) — otherwise the exception is unhandled. In `coroutineScope`, the exception naturally surfaces at the scope's call site where you can catch it. So `supervisorScope` gives independence at the cost of you owning each child's error handling."}]},{level:"senior",q:"How does exception handling differ between launch and async?",a:[{t:"p",text:"**The core difference is *when* and *where* the exception surfaces.** In `launch`, an uncaught exception **propagates immediately** to the coroutine's parent/scope as soon as it's thrown — it travels up the Job hierarchy and, if unhandled, reaches a `CoroutineExceptionHandler` or crashes. In `async`, the exception is **captured inside the `Deferred`** and only *thrown when you call `await()`** — the throwing is deferred to the point of consumption."},{t:"list",items:["**`launch` — exception is 'pushed'**: it fires up to the parent right away. You handle it with a try/catch *inside* the coroutine, or a `CoroutineExceptionHandler` installed in the context (which only works for `launch`, not `async`).","**`async` — exception is 'pulled'**: `await()` re-throws it. So `try { deferred.await() } catch (e) { }` is the natural handling. If you *never* await a standalone async, the exception can sit unnoticed in the Deferred.","**The structured-concurrency twist**: when `async` is a *child* of a scope (the normal case, e.g. inside `coroutineScope`), a failure *also* propagates to the parent scope and cancels siblings — the same as launch — *in addition to* being rethrown at `await()`. The 'exception waits for await()' behavior in isolation is most visible for a top-level `async` on a scope. So the precise statement is: async holds the exception for its `await()`, but as a child it still participates in structured cancellation.","**`CoroutineExceptionHandler` caveat**: it catches uncaught exceptions from `launch` (root coroutines) only. It does nothing for `async` (whose exceptions are meant to be surfaced via `await`) and nothing for child coroutines (whose exceptions propagate to the parent). Knowing where the handler does and doesn't apply is a common senior discriminator."]},{t:"p",text:"**Practical guidance**: for fire-and-forget, use `launch` and handle errors with try/catch inside or a `CoroutineExceptionHandler` at the scope. For concurrent results, use `async` and wrap the `await()` in try/catch. Mixing them up — e.g. expecting a `CoroutineExceptionHandler` to catch an `async` failure — is a frequent bug."}]},{level:"senior",q:"Why is GlobalScope considered an anti-pattern, and what should you use instead?",a:[{t:"p",text:"**The problem**: `GlobalScope` is a scope whose lifetime is the *entire application process* — coroutines launched in it are bound to nothing and are cancelled only when the process dies. This breaks structured concurrency in every way that matters:"},{t:"list",items:["**Leaks**: a `GlobalScope.launch` started from a screen keeps running after the screen is gone — wasting CPU/network/battery, and potentially trying to update a destroyed UI (crash or leak of the destroyed component it captured).","**No automatic cancellation**: nothing cancels it when the relevant lifecycle ends, so you must remember to cancel manually (and usually don't).","**Untestable**: tests can't control or wait for GlobalScope coroutines the way they control a provided scope; work escapes the test's timing.","**Orphaned failures**: exceptions in GlobalScope coroutines don't propagate to any meaningful parent."]},{t:"list",items:["**Instead, use a lifecycle-bound scope**: `viewModelScope` for ViewModel work, `lifecycleScope`/`viewLifecycleOwner.lifecycleScope` for UI work, `rememberCoroutineScope()` in Compose. These cancel automatically when their owner is gone.","**For work that legitimately must outlive a screen** (an upload that should complete after the user navigates away, app-wide sync): inject an **application-scoped `CoroutineScope`** (created once, e.g. `CoroutineScope(SupervisorJob() + Dispatchers.Default)` provided by DI and tied to the Application) into the repository — it's still a *managed, injectable, testable* scope, unlike GlobalScope. For work that must survive process death, use **WorkManager**."]},{t:"p",text:"**The principle**: every coroutine should have an owner whose lifecycle bounds it. `GlobalScope` deliberately has no owner, so it's almost never correct. The rare legitimate 'outlive the caller' need is met by an *injected application scope* (managed, testable) — not by GlobalScope. Kotlin even requires an opt-in annotation to use GlobalScope now, signaling it's a code smell."}]}],bS={id:"builders-scopes",title:"Builders & Scopes",tagline:"launch/async/runBlocking, CoroutineScope & lifetime, viewModelScope/lifecycleScope, coroutineScope vs supervisorScope, and why GlobalScope is an anti-pattern.",tags:["Coroutines","launch","async","CoroutineScope","viewModelScope"],content:wS,qa:vS},kS=[{heading:"What structured concurrency means",blocks:[{t:"p",text:"**Structured concurrency** is the principle that coroutines form a **parent-child tree**, and a parent doesn't complete until all its children complete. Every coroutine launched inside another (or inside a scope) becomes a *child* of that scope's Job. This tree gives three guarantees that make coroutines safe: **no leaks** (children are tracked and can't be forgotten), **cancellation propagates** (cancel a parent → all children cancelled), and **errors propagate** (a child failing can cancel the parent and siblings). It's the opposite of raw threads/callbacks, where started work can be silently orphaned."},{t:"list",items:["**No orphans**: because a scope waits for its children, you can't accidentally leave a coroutine running after the code that started it has 'finished' — the scope literally won't complete until they do.","**Cancellation flows down**: cancelling a scope or parent Job cancels the entire subtree beneath it.","**Errors flow up (by default)**: an unhandled exception in a child cancels its parent, which cancels the other children — failures don't get lost.","**Where it comes from**: builders (`launch`/`async`) are extensions on `CoroutineScope`, and each creates a child Job of the scope's Job — so the tree is built automatically as you launch coroutines."]}]},{heading:"The Job — a handle and a tree node",blocks:[{t:"p",text:"A **`Job`** is the handle to a coroutine's lifecycle. `launch` returns one; every coroutine *has* one in its context. A Job is both a *control handle* (cancel it, wait for it) and a *node in the parent-child tree* (it has a parent Job and child Jobs)."},{t:"code",title:"Using a Job",code:`val job = scope.launch {
    doLongWork()
}

job.cancel()                 // request cancellation
job.join()                   // suspend until it finishes (or is cancelled)
job.cancelAndJoin()          // cancel then wait for it to actually stop

if (job.isActive) { }        // still running
if (job.isCancelled) { }     // cancelled
if (job.isCompleted) { }     // finished (normally or cancelled)

job.invokeOnCompletion { cause -> /* cleanup */ }`},{t:"list",items:["**States**: a Job moves through *New → Active → Completing → Completed*, or *Cancelling → Cancelled*. `isActive`, `isCancelled`, `isCompleted` expose this.","**`join()`** suspends until the Job completes — used to wait for a coroutine. **`cancel()`** requests cancellation (cooperative). **`cancelAndJoin()`** does both.","**A parent Job doesn't complete until all children complete** — this is the mechanism behind 'the scope waits for its children'. Even after the parent's own code finishes, it's in *Completing* until children are done.","**`Deferred<T>`** (from `async`) is a `Job` that also carries a result via `await()`."]}]},{heading:"The Job hierarchy and how failure propagates",blocks:[{t:"p",text:"Jobs form a tree. When something goes wrong, the *type* of Job at each level decides what happens — and this is the crux of both this topic and the exception-handling topic:"},{t:"list",items:["**With a regular `Job`**: a child's uncaught exception *cancels its parent*, which *cancels all the parent's other children*, and the failure propagates up. One failure tears down the whole subtree. This is the default and is right when the work is interdependent.","**Cancellation is different from failure**: cancelling a *child* (via `child.cancel()`) does **not** cancel the parent or siblings — cancellation is a normal, expected outcome. Only an *unhandled exception* (a failure) propagates upward and takes down siblings.","**Parent cancellation cascades down**: cancel the parent and every descendant is cancelled — regardless of Job type."]},{t:"code",title:"One child's failure cancels its siblings (regular Job)",code:`coroutineScope {                 // regular Job
    launch { delay(100); error("boom") }   // fails
    launch { delay(1000); doWork() }        // CANCELLED because sibling failed
}   // coroutineScope rethrows the exception`}]},{heading:"SupervisorJob — isolating child failures",blocks:[{t:"p",text:"A **`SupervisorJob`** changes the failure rule: children fail **independently** — one child's exception does **not** cancel the parent or the siblings. It only changes *downward* propagation of failure (child → parent); parent cancellation still cascades to all children."},{t:"code",title:"SupervisorJob keeps siblings alive",code:`val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main)

scope.launch { error("boom") }    // fails alone
scope.launch { doOtherWork() }    // KEEPS RUNNING — not cancelled by the sibling

// Same idea in a suspend function:
supervisorScope {
    launch { mightFail() }        // failure isolated
    launch { alsoIndependent() }  // survives
}`},{t:"list",items:["**Where you meet it**: `viewModelScope` uses a `SupervisorJob` — so one failed coroutine in a ViewModel doesn't tear down the whole ViewModel's scope. Same for `lifecycleScope`.","**The catch**: with a SupervisorJob, a child's exception is *not* propagated to a parent, so it becomes 'uncaught' unless *that child* handles it (try/catch inside, or a `CoroutineExceptionHandler`). Independence means each child owns its error handling.","**`SupervisorJob()` (the scope) vs `supervisorScope { }` (the suspend function)**: the former builds a long-lived scope; the latter creates a temporary supervised scope within a suspend function. Same failure semantics, different lifetime.","**Placement subtlety**: a `SupervisorJob` only supervises the coroutines that are its *direct* children. If you `launch` a coroutine that itself `launch`es more, those grandchildren are under a *regular* Job (the child's) unless you nest another supervisor — so a supervisor at the top doesn't magically isolate failures deep in the tree."]}]},{heading:"Putting it together — the mental model",blocks:[{t:"table",headers:["Event","Regular Job","SupervisorJob"],rows:[["Child throws uncaught exception","cancels parent + all siblings","isolated — siblings unaffected"],["Child is cancelled (not failed)","siblings unaffected","siblings unaffected"],["Parent is cancelled","all children cancelled","all children cancelled"],["Who handles a child's error","propagates up to scope","the child itself must"]]},{t:"note",text:`Interview-ready summary: "Coroutines form a parent-child Job tree. A regular Job means a child failure cancels the parent and siblings (all-or-nothing); a SupervisorJob isolates child failures (independent). Parent cancellation always cascades to all children. Cancelling a child (vs it failing) never affects siblings. viewModelScope uses a SupervisorJob so one failure doesn't kill the whole ViewModel scope."`}]}],xS=[{level:"junior",q:"What is structured concurrency?",a:[{t:"p",text:"**The concept**: structured concurrency means coroutines form a **parent-child tree**, and a parent coroutine (or scope) does not complete until all of its children have completed. Every coroutine you launch inside a scope becomes a child of that scope. This structure is what makes coroutines safe compared to raw threads or callbacks."},{t:"list",items:["**No leaked work**: because a scope waits for its children, you can't accidentally start a coroutine and forget about it — the scope tracks it and won't finish until it does.","**Cancellation propagates down**: cancel a scope or parent, and all its children are cancelled automatically. Leave a screen → its scope cancels → all its coroutines stop.","**Errors propagate up**: by default, an unhandled exception in a child cancels its parent and siblings, so failures aren't silently lost."]},{t:"p",text:"The practical payoff: you get automatic cleanup and error handling for free. Contrast the old callback world, where a started network request could keep running and call back into a destroyed screen because nothing tied its lifetime to anything. Structured concurrency ties every coroutine's lifetime to a scope, so 'this work belongs to this screen' is enforced by the framework."}]},{level:"junior",q:"What is a Job in coroutines?",a:[{t:"p",text:"**The concept**: a `Job` is the handle to a coroutine's lifecycle. `launch` returns one, and every coroutine has one in its context. It serves two roles: a *control handle* you can use to cancel or wait for the coroutine, and a *node in the parent-child tree* that connects a coroutine to its parent and children."},{t:"code",title:"What you do with a Job",code:`val job = scope.launch { work() }
job.cancel()          // request cancellation
job.join()            // suspend until it completes
job.cancelAndJoin()   // cancel and wait for it to stop
job.isActive          // still running?`},{t:"list",items:["A Job has states: New → Active → Completing → Completed, or Cancelling → Cancelled, exposed via `isActive`/`isCancelled`/`isCompleted`.","`cancel()` requests cooperative cancellation; `join()` waits for completion; `invokeOnCompletion { }` runs a callback when it finishes.","A parent Job stays in a 'completing' state until all its child Jobs finish — this is the mechanism behind 'a scope waits for its children'.","`Deferred<T>` (returned by `async`) is a Job that also carries a result via `await()`."]}]},{level:"junior",q:"What is a SupervisorJob and where do you encounter it?",a:[{t:"p",text:"**The concept**: normally, if one child coroutine fails with an unhandled exception, it cancels its parent and all sibling coroutines. A `SupervisorJob` changes that rule so children fail **independently** — one child throwing does *not* cancel its siblings or the parent. It isolates failures."},{t:"code",title:"Independent children",code:`val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main)
scope.launch { error("fails") }   // this one fails
scope.launch { keepsWorking() }   // NOT cancelled by the sibling's failure`},{t:"p",text:"**Where you encounter it**: `viewModelScope` and `lifecycleScope` both use a `SupervisorJob` internally — that's deliberate, so that one failed coroutine (say a failed analytics call) doesn't tear down the entire ViewModel's scope and cancel all its other work. You also use it explicitly via `supervisorScope { }` when launching several independent operations where one failing shouldn't stop the others (e.g. loading independent dashboard widgets). The one caveat: because a SupervisorJob doesn't propagate a child's error to the parent, each child must handle its own exceptions — otherwise the error goes uncaught."}]},{level:"junior",q:"If you cancel a scope, what happens to the coroutines running in it?",a:[{t:"p",text:"**They're all cancelled.** Cancellation propagates *down* the Job tree: cancelling a scope cancels its Job, which cancels every child coroutine, and their children, all the way down. This is the mechanism that makes lifecycle scopes safe — when a ViewModel is cleared, `viewModelScope` is cancelled, and every coroutine launched in it stops."},{t:"p",text:"Two important details: (1) cancellation is *cooperative* — a coroutine only actually stops at a suspension point (or where it checks `isActive`/`ensureActive`), so a tight non-suspending loop must check cancellation manually to respond. (2) Once a scope is cancelled, it's dead — you can't launch new coroutines in it (they'll be cancelled immediately). This is why you don't cancel `viewModelScope` yourself; the framework does it at the right time. The takeaway: scope cancellation is the single lever that cleans up all associated work, which is exactly why binding coroutines to the right scope matters so much."}]},{level:"senior",q:"Walk through exactly how a failure propagates in a coroutine tree, and how SupervisorJob changes it.",a:[{t:"p",text:"**With a regular `Job`, failure propagates bidirectionally to tear down the subtree.** When a child throws an *unhandled* exception: (1) the child fails; (2) it propagates the exception *up* to its parent; (3) the parent, on receiving a child failure, cancels *itself*, which (4) cancels *all its other children* (siblings of the failed one); (5) the exception continues propagating up until it reaches the root, where it's handled by a `CoroutineExceptionHandler` or crashes. Net effect: one unhandled failure cancels the entire scope's subtree."},{t:"list",items:["**`SupervisorJob` breaks step 3→4**: a child failure is *not* propagated to the parent, so the parent isn't cancelled and siblings survive. Failure is contained to the failing child. But this means the exception has nowhere to auto-propagate — so it must be handled *at that child* (try/catch or a `CoroutineExceptionHandler` on that launch), or it's unhandled.","**Cancellation ≠ failure**: this whole propagation is for *failures* (unhandled exceptions). If you *cancel* a child yourself (`child.cancel()`), that's a normal `CancellationException` — it does **not** propagate up and does **not** cancel siblings. Cancellation is an expected outcome; failure is exceptional. Conflating them is a common mistake.","**Parent cancellation always cascades down** regardless of Job type: cancel the parent and all children die. SupervisorJob only affects the *upward* (child→parent) direction of *failures*, never downward cancellation.","**Depth matters**: a `SupervisorJob` only supervises its *direct* children. If a supervised child launches its own children with a regular Job (the default), a failure among *those* grandchildren cancels their parent (the supervised child) — the supervisor at the top doesn't reach down to isolate them. You'd need another `supervisorScope` at that level."]},{t:"code",title:"The two behaviors side by side",code:`// Regular Job: sibling cancelled by failure
coroutineScope {
    launch { throw IOException() }     // fails
    launch { delay(1000); log("A") }   // CANCELLED, never logs "A"
}

// SupervisorJob: sibling survives (but the failing child needs its own handler)
supervisorScope {
    launch(handler) { throw IOException() } // isolated, handled by 'handler'
    launch { delay(1000); log("A") }        // runs, logs "A"
}`}]},{level:"senior",q:"Why does viewModelScope use a SupervisorJob, and what would go wrong with a regular Job?",a:[{t:"p",text:"**Because a ViewModel typically runs multiple independent coroutines, and one failing shouldn't kill all the others — or the scope itself.** A ViewModel might have a coroutine observing the database, another handling a button-triggered refresh, another logging analytics. These are independent. With a `SupervisorJob`, if the analytics coroutine throws, it fails alone; the database observation and everything else keep working."},{t:"p",text:"**What would go wrong with a regular Job**: a regular Job propagates any child failure up to the scope, cancelling the scope and *all* its other coroutines. So a single unhandled exception in *any* coroutine — even a trivial fire-and-forget analytics call — would cancel the entire `viewModelScope`. After that, the scope is dead: the screen's data observation stops, and any new `viewModelScope.launch` would be cancelled immediately. The user's screen would silently stop updating because one unrelated coroutine failed. That's a terrible failure mode, so the framework uses a `SupervisorJob` to contain failures to the individual coroutine that caused them."},{t:"list",items:["**The tradeoff you inherit**: because `viewModelScope` supervises, a failing coroutine's exception won't propagate anywhere useful — it becomes uncaught (and can still crash via the default handler for `launch`). So *you* are responsible for handling errors inside each `viewModelScope.launch` (try/catch, or mapping failures into UI error state). The scope protects the *other* coroutines, not the failing one.","**Consistency**: `lifecycleScope` uses a `SupervisorJob` for the same reason — UI-layer coroutines are independent, and one failing shouldn't tear down the screen's whole coroutine scope."]}]},{level:"senior",q:"What's the difference between cancelling a coroutine and a coroutine failing with an exception, in terms of the tree?",a:[{t:"p",text:"**They're treated fundamentally differently by structured concurrency, and this trips people up constantly.** *Cancellation* is a normal, expected outcome — a coroutine being told 'stop, you're no longer needed'. *Failure* is an exceptional outcome — a coroutine throwing an unexpected exception. The Job tree responds to each differently:"},{t:"list",items:["**Cancelling a child** (`child.cancel()`): raises a `CancellationException` inside that child, which unwinds it. It does **not** propagate to the parent, and does **not** cancel siblings. The parent and other children carry on. This is why you can cancel one specific coroutine (e.g. an outdated search) without disturbing anything else.","**A child failing** (throwing a non-cancellation exception): with a regular Job, this *does* propagate up and cancel the parent and siblings (as covered above). Failure is 'something went wrong, tear down the related work'.","**Why `CancellationException` is special**: the coroutine framework treats it as the signal for normal cancellation, not an error. That's why you must **never swallow a `CancellationException`** in a catch block — catching `Exception` broadly and not rethrowing it makes the coroutine think it recovered from cancellation, breaking the whole cancellation mechanism (the coroutine keeps running when it should stop). Always rethrow `CancellationException` (or catch specific exceptions only)."]},{t:"code",title:"The catch that breaks cancellation",code:`// BUG: swallows CancellationException -> coroutine won't cancel properly
try { doWork() } catch (e: Exception) { log(e) }

// CORRECT: let CancellationException propagate
try { doWork() }
catch (e: CancellationException) { throw e }   // rethrow!
catch (e: Exception) { log(e) }`},{t:"p",text:"**The mental model**: cancellation flows *down* (parent cancels children) and is a normal outcome that doesn't disturb siblings; failure (with a regular Job) flows *up* and takes down the subtree. `CancellationException` is the plumbing for cancellation and must never be swallowed. Understanding this distinction is what separates 'I use coroutines' from 'I understand coroutines'."}]}],SS={id:"structured-concurrency",title:"Structured Concurrency & Jobs",tagline:"The parent-child Job tree, Job lifecycle & control, how failure propagates, SupervisorJob for isolation, and cancellation vs failure.",tags:["Coroutines","Job","SupervisorJob","Structured Concurrency"],content:kS,qa:xS},TS=[{heading:"CoroutineContext — the environment a coroutine runs in",blocks:[{t:"p",text:"Every coroutine carries a **`CoroutineContext`** — a set of elements describing *how and where* it runs. Think of it as a map of typed elements combined with `+`. The main elements: a **`Job`** (its lifecycle/tree position), a **`CoroutineDispatcher`** (which thread(s) it runs on), an optional **`CoroutineExceptionHandler`**, and an optional **`CoroutineName`** (for debugging)."},{t:"code",title:"Building and inspecting context",code:`val scope = CoroutineScope(
    SupervisorJob() + Dispatchers.Main + CoroutineName("ui")
)

scope.launch(Dispatchers.IO + CoroutineName("sync")) {
    // this coroutine's context = parent's context, with IO and name overridden
}`},{t:"list",items:["**Context is inherited and overridable**: a child coroutine inherits its parent's context, and any element you pass to the builder *overrides* that element for the child (e.g. `launch(Dispatchers.IO)` keeps the parent's Job-parentage but switches the dispatcher).","**The Job in a child's context is always a *new child Job*** — you don't inherit the parent's Job as your own; you get a fresh child of it (that's how the tree forms). Other elements (dispatcher, name) are inherited by value.","Elements are combined with `+` and retrieved by key (`coroutineContext[Job]`, `coroutineContext[CoroutineDispatcher]`)."]}]},{heading:"Dispatchers — which thread(s) the coroutine uses",blocks:[{t:"p",text:"A **`CoroutineDispatcher`** determines what thread or thread pool a coroutine runs on. There are four standard dispatchers, each tuned for a kind of work:"},{t:"table",headers:["Dispatcher","Backed by","Use for"],rows:[["`Dispatchers.Main`","the Android main/UI thread","UI updates, calling into ViewModel, light work"],["`Dispatchers.IO`","a large elastic thread pool (64+)","blocking I/O — network, disk, database"],["`Dispatchers.Default`","a pool sized to CPU cores","CPU-intensive work — parsing, sorting, image processing"],["`Dispatchers.Unconfined`","no confinement (runs in caller thread until first suspension)","rarely — advanced/testing; not for app logic"]]},{t:"list",items:["**IO vs Default — the key distinction**: `IO` is for *blocking* operations that spend most of their time *waiting* (a network call, a file read) — so its pool is large (many threads can be blocked waiting cheaply). `Default` is for *CPU-bound* work that *keeps a core busy* — so its pool matches core count (more threads than cores would just thrash). Using IO for CPU work wastes threads; using Default for blocking IO starves CPU work.","**`Dispatchers.Main.immediate`** — a variant that skips re-dispatching if you're *already* on the main thread (avoids an unnecessary post to the message queue / one-frame delay). `viewModelScope` uses it.","**`IO` and `Default` share threads**: switching between them (`withContext(Default)` from an IO coroutine) may not actually change threads — the dispatcher framework optimizes this. But the semantics (pool sizing/limits) still apply."]}]},{heading:"withContext — switching dispatchers within a coroutine",blocks:[{t:"p",text:"`withContext(dispatcher) { }` runs its block on the given dispatcher and **suspends until it returns the result**, then continues on the original context. It's the primary tool for moving a *piece* of work to the right thread without launching a new coroutine — and it's how you make suspend functions **main-safe**."},{t:"code",title:"withContext for main-safety",code:`suspend fun loadUser(id: String): User = withContext(Dispatchers.IO) {
    val response = api.getUser(id)   // blocking-ish network on IO
    response.toUser()                 // parse
}
// Caller (on Main) can call this safely; it hops to IO internally and back.

// Heavy CPU work -> Default:
suspend fun sortLarge(list: List<Item>): List<Item> = withContext(Dispatchers.Default) {
    list.sortedByDescending { it.score }
}`},{t:"list",items:["**`withContext` returns a value and doesn't create a new coroutine tree branch for concurrency** — it's sequential. Use it to *switch threads for a block*, not to run things in parallel (that's `async`).","**Main-safety rule**: the layer *doing* the blocking/CPU work owns the `withContext`. A repository's suspend function wraps its own IO in `withContext(IO)`, so a ViewModel can call it from the main thread without any dispatcher juggling. Callers should *not* pre-emptively wrap calls in `withContext(IO)`.","**Don't double-wrap main-safe calls**: Retrofit and Room suspend functions are already main-safe (they manage their own threading), so wrapping them in `withContext(IO)` is redundant (harmless but reveals shallow understanding).","**Inject dispatchers, don't hardcode**: take a `CoroutineDispatcher` as a constructor parameter (`ioDispatcher: CoroutineDispatcher = Dispatchers.IO`) so tests can substitute a `TestDispatcher` and control virtual time — hardcoding `Dispatchers.IO` makes code untestable."]}]},{heading:"withContext vs launch/async for threading",blocks:[{t:"list",items:["**`withContext(d) { }`** — switch context for a block, get the result back, *sequentially*. 'Run this part on IO and give me the answer.' The default choice for 'do this work on the right thread'.","**`launch(d) { }`** — start a *new concurrent coroutine* on dispatcher `d`. Use when you want fire-and-forget work on a specific thread, not when you just need a result.","**`async(d) { }`** — start a *concurrent* coroutine on `d` that returns a result via `await()`. Use for parallel work on specific threads.","The mistake to avoid: using `launch`/`async` + immediate `await`/`join` just to switch threads for a single result — that's what `withContext` is for, and it's clearer and cheaper."]}]},{heading:"Context preservation and dispatcher confinement",blocks:[{t:"list",items:["A coroutine **stays on its dispatcher across suspension points** — after a `delay` or a suspend call resumes, it comes back on the same dispatcher (not necessarily the same *thread* within a pool, but the same dispatcher). So `Dispatchers.Main` code that suspends resumes back on Main — safe to touch UI.","**`withContext` restores the outer context** when its block ends — so `withContext(IO){}` inside a Main coroutine runs the block on IO and then continues on Main automatically.","**Custom dispatchers**: you can create your own with `newSingleThreadContext` / `newFixedThreadPoolContext` (for confinement to specific threads, e.g. a single-threaded dispatcher to serialize access to a non-thread-safe resource) or wrap a Java `Executor` with `.asCoroutineDispatcher()`. Close them when done (they hold real threads).","**`Dispatchers.Main` requires a main-thread implementation** — on Android it's provided by the coroutines-android artifact; in pure JVM unit tests it doesn't exist, which is why you `Dispatchers.setMain(testDispatcher)` in tests."]},{t:"note",text:'The threading trio to state cleanly: "Main for UI, IO for blocking waits (network/disk — large pool), Default for CPU work (parsing/sorting — core-sized pool). Switch with withContext, owned by the layer doing the work, and inject dispatchers for testability."'}]}],CS=[{level:"junior",q:"What are the main coroutine dispatchers and what is each for?",a:[{t:"list",items:["**`Dispatchers.Main`** — the Android main/UI thread. For updating UI, calling ViewModel functions, and light work. UI can only be touched here.","**`Dispatchers.IO`** — a large elastic thread pool (64+ threads) for *blocking I/O*: network requests, disk reads/writes, database queries. These operations spend their time *waiting*, so a big pool lets many block cheaply at once.","**`Dispatchers.Default`** — a pool sized to the number of CPU cores, for *CPU-intensive* work: parsing large JSON, sorting big lists, image processing, computations. These keep a core busy, so more threads than cores wouldn't help.","**`Dispatchers.Unconfined`** — starts in the calling thread and isn't confined to any specific thread after suspension. Rarely used in app code; mostly advanced scenarios and some testing."]},{t:"p",text:"The everyday split is Main / IO / Default. The key judgment is IO vs Default: IO for things that *wait* (network, disk), Default for things that *compute* (parsing, sorting). You switch to the right one with `withContext(dispatcher) { }` around the relevant work."}]},{level:"junior",q:"What is the difference between Dispatchers.IO and Dispatchers.Default?",a:[{t:"p",text:"**The distinction is blocking-and-waiting vs CPU-bound work, and it's reflected in the pool sizes.** `Dispatchers.IO` is for operations that spend most of their time *waiting* on something external — a network response, a disk read, a database query. Because a waiting thread isn't using the CPU, you can afford *many* of them, so IO's pool is large (64+ threads by default). `Dispatchers.Default` is for work that *keeps the CPU busy* — parsing, sorting, computation. Here more threads than CPU cores would just cause context-switching overhead without speedup, so Default's pool is sized to the core count."},{t:"list",items:["**Using the wrong one has real costs**: run CPU-heavy work on `IO` and you can spawn dozens of CPU-bound threads competing for a few cores (thrashing). Run blocking IO on `Default` and a handful of blocked threads exhaust the small pool, starving other CPU work.","**Rule of thumb**: 'Does this operation mostly *wait* or mostly *compute*?' Wait → IO. Compute → Default.","**Implementation detail**: IO and Default actually share an underlying thread pool, and switching between them may not physically change threads — but the semantics (how many concurrent tasks each permits) still matter, so choose by the nature of the work."]}]},{level:"junior",q:"What does withContext do?",a:[{t:"p",text:"**The concept**: `withContext(dispatcher) { }` runs its block on the specified dispatcher, *suspends* until the block finishes, returns the block's result, and then continues on the original context. It's the standard way to move a piece of work to the appropriate thread within a suspend function — without launching a separate coroutine."},{t:"code",title:"Moving blocking work off the main thread",code:`suspend fun loadData(): Data = withContext(Dispatchers.IO) {
    val json = file.readText()   // blocking IO on an IO thread
    parse(json)                   // returns the result
}
// A caller on Main can call this; it hops to IO and back automatically.`},{t:"p",text:"It's how you make a suspend function *main-safe* — safe to call from the main thread because it internally switches to the right dispatcher for its heavy work. Unlike `launch`/`async`, `withContext` is *sequential* — it doesn't start concurrent work, it just switches threads for a block and gives you the result. So use `withContext` for 'run this part elsewhere and give me the answer', and `async` for 'run these things in parallel'."}]},{level:"junior",q:"Where should you call withContext(Dispatchers.IO) — in the ViewModel or the repository?",a:[{t:"p",text:"**In the repository (or wherever the blocking work actually happens), not the ViewModel.** The principle is *main-safety owned by the layer doing the work*: the function that performs blocking IO wraps *itself* in `withContext(IO)`, so that every caller — including a ViewModel on the main thread — can call it safely without thinking about threads."},{t:"list",items:["**Why not the ViewModel**: if ViewModels wrap every repository call in `withContext(IO)` 'to be safe', threading policy gets scattered across the whole app, it's easy to forget one, and it hides which layer actually blocks. Centralizing it in the data layer means one correct place per operation.","**The ViewModel just launches**: `viewModelScope.launch { val data = repository.load() }` — no dispatcher juggling, because `repository.load()` is main-safe.","**Don't double-wrap main-safe calls**: Retrofit and Room suspend functions already switch threads internally, so wrapping *those* in `withContext(IO)` is redundant. You add `withContext` for *your own* blocking code (a manual file read, a heavy parse), not for libraries that are already main-safe."]},{t:"p",text:"So the clean architecture: repositories/use-cases are main-safe (own their `withContext`), the ViewModel calls them from `viewModelScope` (Main) freely, and the UI stays responsive. This keeps threading decisions in one predictable place."}]},{level:"senior",q:"What is a CoroutineContext, and how is it inherited by child coroutines?",a:[{t:"p",text:"**The concept**: a `CoroutineContext` is the set of elements defining how a coroutine runs — it's an indexed collection (like a typed map) of elements combined with the `+` operator. The main elements are the **`Job`** (lifecycle and tree position), the **`CoroutineDispatcher`** (threading), an optional **`CoroutineExceptionHandler`**, and a **`CoroutineName`** (debugging). You retrieve elements by key: `coroutineContext[Job]`, `coroutineContext[CoroutineDispatcher]`."},{t:"list",items:["**Inheritance with override**: a child coroutine's context is computed as *parent context + whatever you pass to the builder*. So `launch(Dispatchers.IO)` takes the parent's context and overrides just the dispatcher — the child keeps the parent's other elements (name, handler) unless you also override them. This is why children run on the parent's dispatcher by default.","**The Job is special**: a child does NOT inherit the parent's Job as its own — it always gets a *new Job that is a child of* the parent's Job. That's the mechanism that builds the structured-concurrency tree. Every other element is inherited by value; the Job is always freshly parented.",'**Combining**: `SupervisorJob() + Dispatchers.Main + CoroutineName("x")` builds a context; later elements of the same key win (`Dispatchers.IO + Dispatchers.Main` yields Main).',"**`withContext(element)`** temporarily overrides context elements for its block (commonly the dispatcher) and restores them after — a scoped, sequential context change."]},{t:"p",text:"**Why this matters**: understanding context inheritance explains real behavior — why a coroutine launched in a Main scope runs on Main (inherited dispatcher), why a launched child's failure reaches the parent (new child Job under the parent Job), and how to override just one aspect (pass one element to the builder). It's the unifying model beneath dispatchers, Jobs, and exception handlers — they're all just context elements."}]},{level:"senior",q:"Why should you inject dispatchers rather than hardcode Dispatchers.IO, and how does it help testing?",a:[{t:"p",text:"**Because hardcoded dispatchers make coroutine code non-deterministic and slow to test.** If a repository hardcodes `withContext(Dispatchers.IO)`, that work runs on real background threads during tests — outside the test's control. The test can't make it deterministic, can't fast-forward `delay`s, and may get flaky timing. Injecting the dispatcher lets the test substitute a `TestDispatcher` that runs on the test's scheduler with *virtual time*."},{t:"code",title:"Injected dispatcher enables test control",code:`class UserRepository(
    private val ioDispatcher: CoroutineDispatcher = Dispatchers.IO,  // injected
) {
    suspend fun load() = withContext(ioDispatcher) { /* work */ }
}

// In tests:
val testDispatcher = StandardTestDispatcher()
val repo = UserRepository(ioDispatcher = testDispatcher)   // controllable
// runTest can now advance virtual time, run deterministically`},{t:"list",items:["**Virtual time**: a `TestDispatcher` runs inside `runTest`, where `delay(1000)` completes instantly by advancing a fake clock. Hardcoded `Dispatchers.IO` escapes that — a `delay` there really waits, making tests slow. Injecting lets tests of debounce/retry/timeout run in milliseconds.","**Determinism**: `StandardTestDispatcher` queues coroutines so you control execution order with `advanceUntilIdle()`/`runCurrent()` and can assert intermediate states (like a `Loading` before `Content`). Real dispatchers give you races.","**Common pattern**: inject a `CoroutineDispatcher` (or a small `DispatcherProvider` interface bundling Main/IO/Default) as a constructor default `= Dispatchers.IO`, so production uses the real one and tests inject the test one. Zero production cost, full test control.","**Also improves flexibility**: you can swap dispatchers per environment or tune them without touching call sites."]},{t:"p",text:"**The principle**: a hardcoded dispatcher is a hidden dependency on real threads and real time. Injecting it makes threading an explicit, substitutable dependency — the same reason you inject a repository instead of `new`-ing one. It's a small habit that makes the difference between testable and untestable coroutine code."}]},{level:"senior",q:"When would you create a custom dispatcher, and what must you be careful about?",a:[{t:"p",text:"**The standard dispatchers cover almost everything, so custom dispatchers are for specific needs — most commonly *confining work to a specific thread or a bounded set of threads*.** The classic case: serializing access to a resource that isn't thread-safe. A single-threaded dispatcher guarantees all operations on that resource happen on one thread, in order — a lock-free way to prevent concurrent access."},{t:"list",items:['**`newSingleThreadContext("name")`** — a dispatcher backed by one dedicated thread. Use to serialize access to a non-thread-safe library, a legacy single-threaded native component, or to guarantee ordering. It replaces manual synchronization with confinement.','**`newFixedThreadPoolContext(n, "name")`** — a fixed pool of `n` threads, for when you need a bounded, isolated pool separate from IO/Default (e.g. to cap concurrency to a specific external system).',"**`Executor.asCoroutineDispatcher()`** — wrap an existing Java `Executor` (a thread pool you already manage, or one with special properties) as a coroutine dispatcher. Common when integrating with existing threading infrastructure.","**`Dispatchers.IO.limitedParallelism(n)`** — a modern, lighter alternative: get a *view* of the IO pool limited to `n` concurrent coroutines, without creating new threads. Good for capping parallelism to a rate-limited API while reusing the shared pool."]},{t:"list",items:["**The big caution — they hold real threads**: `newSingleThreadContext`/`newFixedThreadPoolContext` create actual OS threads that are NOT automatically released. You must `close()` them when done (or scope them to something), or you leak threads. This is why `limitedParallelism` is often preferred — it doesn't create threads.","**Don't create dispatchers per-call**: creating a `newSingleThreadContext` inside a frequently-called function leaks a thread every time. Create once, reuse, close on teardown.","**Prefer the standard dispatchers unless you have a concrete confinement/isolation need** — custom dispatchers add resource management burden for benefits most code doesn't need."]}]}],MS={id:"dispatchers",title:"Dispatchers & Context",tagline:"CoroutineContext & inheritance, Main/IO/Default/Unconfined, withContext for main-safety, where threading belongs, injecting dispatchers, and custom dispatchers.",tags:["Coroutines","Dispatchers","withContext","Threading","Context"],content:TS,qa:CS},IS=[{heading:"Cancellation is cooperative",blocks:[{t:"p",text:"The single most important fact about coroutine cancellation: it is **cooperative**, not forceful. Calling `job.cancel()` doesn't kill the coroutine dead — it *requests* cancellation by marking the Job as cancelling. The coroutine only actually stops when it reaches a point that **checks for cancellation** and responds by throwing a `CancellationException`. A coroutine that never checks will keep running despite being 'cancelled'."},{t:"list",items:["**Where cancellation is checked automatically**: every suspension point in the kotlinx.coroutines library — `delay`, `withContext`, `yield`, a Retrofit/Room suspend call, `Mutex.lock`, channel operations. When a cancelled coroutine hits one of these, it throws `CancellationException` and unwinds.","**Where it is NOT checked**: your own tight loops and long computations that don't suspend. A `while (true) { computeStuff() }` with no suspension point ignores cancellation entirely.","**Why cooperative and not forceful**: forcefully killing a thread mid-operation (like the old `Thread.stop()`) leaves resources in inconsistent states — half-written files, held locks. Cooperative cancellation lets the coroutine unwind cleanly through normal exception handling (`finally` blocks run), which is safe."]}]},{heading:"Making CPU-bound work cancellable",blocks:[{t:"p",text:"If your coroutine does CPU work without natural suspension points, you must *cooperate* — periodically check whether you've been cancelled. Two idioms:"},{t:"code",title:"Cooperating with cancellation",code:`// Option A: ensureActive() — throws CancellationException if cancelled
suspend fun process(items: List<Item>) {
    for (item in items) {
        currentCoroutineContext().ensureActive()   // checkpoint
        heavyCompute(item)
    }
}

// Option B: isActive check — exit gracefully without throwing
suspend fun process2(items: List<Item>) {
    for (item in items) {
        if (!currentCoroutineContext().isActive) return   // bail out
        heavyCompute(item)
    }
}

// Option C: yield() — checks cancellation AND lets other coroutines run
suspend fun process3(items: List<Item>) {
    for (item in items) {
        yield()          // cooperative point
        heavyCompute(item)
    }
}`},{t:"list",items:["**`ensureActive()`** — throws `CancellationException` immediately if cancelled (the normal cancellation path). Preferred: it integrates with structured concurrency (the exception propagates correctly).","**`isActive`** — a boolean you check to exit gracefully (e.g. `return`). Use when you want to stop *without* throwing.","**`yield()`** — checks cancellation *and* gives other coroutines a chance to run (useful in long loops to avoid hogging a dispatcher thread).","**The `.cancellable()` flow operator** does the equivalent for flows that emit rapidly without other suspension points."]}]},{heading:"CancellationException — the special exception",blocks:[{t:"p",text:"Cancellation works by throwing a **`CancellationException`** inside the coroutine. This exception is *special*: the coroutine machinery treats it as a normal, expected signal (the coroutine was told to stop), **not** as a failure. That's why a cancelled coroutine doesn't crash the app or propagate a failure to its parent — it just quietly winds down."},{t:"list",items:["**Never swallow it**: the cardinal rule. A broad `try { } catch (e: Exception) { }` around a suspend call *catches* the `CancellationException` and, if you don't rethrow it, the coroutine thinks it recovered and keeps running — cancellation is broken. Always rethrow it, or catch specific exceptions only.","**`try/finally` still runs on cancellation**: because cancellation is an exception, `finally` blocks execute during unwinding — the correct place for cleanup (close resources, release locks). This is why cooperative cancellation is safe: cleanup happens.","**Suspending in a `finally`**: a coroutine is already cancelled when `finally` runs, so calling a *suspend* function there (e.g. to save state) will *immediately* throw `CancellationException` again. To run suspend cleanup during cancellation, wrap it in `withContext(NonCancellable) { }` (see below)."]},{t:"code",title:"Cleanup during cancellation",code:`val job = launch {
    try {
        work()
    } finally {
        // non-suspend cleanup runs fine here
        closeResources()
        // suspend cleanup needs NonCancellable:
        withContext(NonCancellable) {
            saveProgressToDb()   // still runs even though we're cancelled
        }
    }
}`}]},{heading:"withTimeout and withTimeoutOrNull",blocks:[{t:"p",text:"Cancellation powers **timeouts**. `withTimeout(ms) { }` runs a block and **cancels it** (throwing `TimeoutCancellationException`) if it doesn't finish in time. `withTimeoutOrNull(ms) { }` does the same but returns `null` instead of throwing — cleaner when a timeout is an expected outcome."},{t:"code",title:"Timeouts",code:`// Throws TimeoutCancellationException on timeout:
val result = withTimeout(5000) { api.fetch() }

// Returns null on timeout (no exception to catch):
val resultOrNull = withTimeoutOrNull(5000) { api.fetch() }
if (resultOrNull == null) showTimeoutMessage()`},{t:"list",items:["The block must be *cancellable* (cooperative) for the timeout to actually interrupt it — a tight non-suspending loop inside `withTimeout` won't be stopped at the deadline.","`TimeoutCancellationException` is a subclass of `CancellationException`, so it's treated as cancellation — it cancels the block but (as a normal cancellation) doesn't propagate as a failure to the parent. Catch it explicitly if you want to react (or use `withTimeoutOrNull`)."]}]},{heading:"How scope cancellation ties it together",blocks:[{t:"list",items:["**Cancelling a scope/parent cancels all descendants** — the tree cancellation from structured concurrency. `viewModelScope` cancelled in `onCleared()` → every ViewModel coroutine gets a `CancellationException` at its next suspension point.","**Cancellation is one-directional (down) for cancel, and a normal outcome** — cancelling a child doesn't affect siblings or parent (unlike a failure).","**A cancelled scope is dead** — you can't launch new coroutines in it (they cancel immediately). This is why you never cancel `viewModelScope` yourself.","**Cooperation is the developer's responsibility for custom loops** — the framework cancels at suspension points for free, but your CPU loops need `ensureActive`/`isActive`/`yield`."]},{t:"note",text:"The three cancellation facts to state: (1) it's cooperative — checked at suspension points, so tight loops need manual `ensureActive()`; (2) it works via `CancellationException`, which must never be swallowed (rethrow it) and lets `finally` run for cleanup; (3) suspend cleanup during cancellation needs `withContext(NonCancellable)`. Timeouts (`withTimeout`) are cancellation with a deadline."}]}],AS=[{level:"junior",q:"What does it mean that coroutine cancellation is 'cooperative'?",a:[{t:"p",text:"**The concept**: calling `cancel()` on a coroutine doesn't forcibly kill it — it *requests* cancellation by marking the coroutine's Job as cancelling. The coroutine actually stops only when it reaches a point that **checks for cancellation** and responds. So cancellation requires cooperation from the coroutine's code: it must pass through cancellation checkpoints for the request to take effect."},{t:"list",items:["**Suspension points check automatically**: `delay`, `withContext`, `yield`, network/database suspend calls all check for cancellation — so a coroutine that regularly suspends responds to cancellation promptly and for free.","**Tight non-suspending loops don't**: `while (true) { heavyCompute() }` with no suspension point will keep running even after cancellation, because it never hits a checkpoint. You'd need to add `ensureActive()` or `yield()` in the loop.","**Why cooperative rather than forceful**: forcibly killing a coroutine mid-operation could leave things half-done (partial file writes, held locks). Cooperative cancellation throws an exception that unwinds cleanly, running `finally` blocks — so resources are released properly."]}]},{level:"junior",q:"How do you make a long-running loop cancellable?",a:[{t:"p",text:"**The concept**: a CPU-bound loop with no suspension points won't respond to cancellation on its own, so you add a cancellation checkpoint inside it. There are three ways:"},{t:"code",title:"Three ways to cooperate",code:`for (item in items) {
    ensureActive()        // throws CancellationException if cancelled (preferred)
    // or: if (!isActive) return   // exit gracefully without throwing
    // or: yield()                 // check cancellation + let others run
    heavyCompute(item)
}`},{t:"list",items:["**`ensureActive()`** — throws `CancellationException` immediately if the coroutine was cancelled. This is the preferred option because it integrates correctly with structured concurrency (the exception propagates as a proper cancellation).","**`isActive`** — a boolean; check it and `return`/`break` to stop gracefully without throwing, if that's what you want.","**`yield()`** — checks for cancellation *and* yields the thread so other coroutines can run, which is polite in long loops that would otherwise monopolize a dispatcher thread."]},{t:"p",text:"The rule of thumb: if your coroutine does meaningful work without calling any suspend function for a while, sprinkle in one of these checkpoints — typically once per loop iteration. Without them, cancelling the coroutine (or its scope) has no effect until the whole computation finishes."}]},{level:"junior",q:"What is withTimeout and how is it different from withTimeoutOrNull?",a:[{t:"p",text:"**Both run a block with a time limit and cancel it if it exceeds the deadline — they differ in what happens on timeout.** `withTimeout(ms) { }` **throws** a `TimeoutCancellationException` if the block doesn't finish in time. `withTimeoutOrNull(ms) { }` instead **returns null** on timeout, with no exception to catch."},{t:"code",title:"Both forms",code:`// Throwing form — handle the exception
try {
    val data = withTimeout(5000) { api.fetch() }
} catch (e: TimeoutCancellationException) {
    showError("Timed out")
}

// Null form — cleaner when timeout is expected
val data = withTimeoutOrNull(5000) { api.fetch() }
if (data == null) showError("Timed out")`},{t:"p",text:"Use `withTimeoutOrNull` when a timeout is a normal, expected branch you handle with a null check (usually cleaner). Use `withTimeout` when a timeout is genuinely exceptional and you want it to throw. One caveat for both: the block must be *cancellable* (cooperative) for the timeout to interrupt it — if the block is a tight non-suspending computation, it won't be stopped at the deadline because there's no checkpoint to cancel at."}]},{level:"junior",q:"Why should you never catch and ignore a CancellationException?",a:[{t:"p",text:"**Because cancellation *is* a CancellationException — swallowing it breaks cancellation entirely.** When a coroutine is cancelled, the framework throws a `CancellationException` inside it to unwind it. If your code catches that exception broadly (`catch (e: Exception)`) and doesn't rethrow it, the coroutine 'recovers' from its own cancellation and keeps running — the exact opposite of what cancellation is supposed to do. The coroutine you thought you cancelled continues executing."},{t:"code",title:"The bug and the fix",code:`// BUG: swallows cancellation
try { doWork() } catch (e: Exception) { log(e) }

// FIX: rethrow CancellationException
try {
    doWork()
} catch (e: CancellationException) {
    throw e                    // let cancellation proceed
} catch (e: Exception) {
    log(e)                     // handle real errors only
}`},{t:"p",text:"This is a subtle, common bug because `CancellationException` is a subclass of `Exception`, so a broad catch grabs it unintentionally. The fix is to either catch `CancellationException` first and rethrow it, catch only the specific exceptions you actually expect, or use a helper like `runCatching`-with-rethrow. The principle: cancellation must be allowed to propagate; only *genuine errors* should be caught and handled."}]},{level:"senior",q:"You need to save progress to the database when a coroutine is cancelled, but the save is a suspend function. What's the problem and the fix?",a:[{t:"p",text:"**The problem**: cleanup goes in a `finally` block, but by the time `finally` runs during cancellation, the coroutine is *already in the cancelled state*. Any **suspend** function you call there — including your database save — will *immediately* throw `CancellationException` again, because a cancelled coroutine refuses to suspend. So your `saveProgress()` never actually runs; it's aborted the instant it suspends."},{t:"code",title:"The fix: NonCancellable",code:`val job = launch {
    try {
        doWork()
    } finally {
        // Non-suspend cleanup is fine:
        releaseInMemoryResources()

        // Suspend cleanup during cancellation MUST be shielded:
        withContext(NonCancellable) {
            database.saveProgress(currentState)   // now allowed to suspend & complete
        }
    }
}`},{t:"list",items:["**`NonCancellable`** is a special `Job` that can't be cancelled. Wrapping suspend cleanup in `withContext(NonCancellable) { }` creates a context where the coroutine is temporarily *not* cancellable, so suspend calls inside it are allowed to run to completion even though the outer coroutine is being cancelled.","**Use it sparingly and only for cleanup**: it deliberately ignores cancellation, so long or unbounded work inside `NonCancellable` defeats the point of cancellation (the coroutine can't be stopped until it finishes). Keep it to quick, essential cleanup (save state, flush a buffer, close a resource that needs a suspend call).","**Non-suspend cleanup doesn't need it**: closing a file handle or clearing a reference in `finally` works fine without `NonCancellable` — only *suspend* cleanup hits the 'already cancelled, can't suspend' problem."]},{t:"p",text:"**The senior insight**: this reveals a precise understanding of the cancellation state machine — that `finally` runs during unwinding, that a cancelled coroutine can't suspend, and that `NonCancellable` is the escape hatch for the narrow case of *suspending cleanup during cancellation*. It's a favorite question because the naive 'just save it in finally' fails silently."}]},{level:"senior",q:"A user leaves a screen but a network request keeps running. Walk through why, and how cancellation should prevent it.",a:[{t:"p",text:"**Why it happens**: the request was launched in a scope not tied to the screen's lifetime — the usual culprits are `GlobalScope.launch`, a manually-created `CoroutineScope` that's never cancelled, or a callback-based API (Retrofit `Call`, an old listener) that isn't coroutine-managed at all. Because nothing cancels that work when the screen is destroyed, it runs to completion, wasting network/battery and possibly trying to update a destroyed UI (leak or crash)."},{t:"list",items:["**The fix — launch in a lifecycle-bound scope**: run the request in `viewModelScope` (cancelled in `onCleared`) or `lifecycleScope` (cancelled on destroy). When the user leaves and the ViewModel is cleared / the lifecycle ends, the scope is cancelled, which — via structured concurrency — cancels the request coroutine. At its next suspension point (the network call is a suspension point), it throws `CancellationException` and stops.","**Retrofit/OkHttp cooperation**: modern Retrofit suspend functions are cancellation-aware — cancelling the coroutine actually cancels the underlying HTTP call. So the request isn't just abandoned in code; the socket work is cancelled too. (Old `enqueue`-callback style wasn't tied to any scope, which is why it leaked.)","**If the work must genuinely continue** (an upload that should finish regardless): that's a *different* requirement — it doesn't belong in the screen's scope at all. Put it in an application-scoped scope or WorkManager, so leaving the screen correctly does *not* cancel it."]},{t:"p",text:"**The principle to articulate**: 'a coroutine keeps running after the screen is gone' is almost always a *scope* bug — the work was bound to the wrong lifetime. Structured concurrency solves it by tying the coroutine to a lifecycle scope, so scope cancellation cascades down and cancels the request. The decision is simply: does this work belong to the screen (viewModelScope/lifecycleScope) or should it outlive the screen (app scope / WorkManager)? Bind it to the matching lifetime and cancellation takes care of itself."}]}],ES={id:"cancellation",title:"Cancellation",tagline:"Cooperative cancellation, making CPU loops cancellable, CancellationException (never swallow it), NonCancellable cleanup, timeouts, and scope cancellation.",tags:["Coroutines","Cancellation","ensureActive","withTimeout","NonCancellable"],content:IS,qa:AS},PS=[{heading:"Two exception models: launch propagates, async defers",blocks:[{t:"p",text:"Coroutine exception handling starts with one distinction: **`launch` and `async` handle exceptions differently.** In `launch`, an uncaught exception is *thrown immediately* and propagates up the Job hierarchy. In `async`, the exception is *stored in the `Deferred`* and only re-thrown when you call `await()`. Getting this right is the foundation for everything else."},{t:"code",title:"The two behaviors",code:`// launch: exception propagates right away (up to parent / handler / crash)
scope.launch {
    throw IOException()   // propagates immediately
}

// async: exception is held, re-thrown at await()
val deferred = scope.async {
    throw IOException()   // stored, not thrown here
}
deferred.await()          // NOW it throws`},{t:"list",items:["**`launch` — 'pushed'**: handle with a try/catch inside the coroutine, or a `CoroutineExceptionHandler` at the scope. It behaves like an uncaught exception on a thread.","**`async` — 'pulled'**: handle by wrapping `await()` in try/catch. If you never await, a standalone async's exception can sit unnoticed.","**The structured-concurrency overlay**: when either is a *child* of a scope, an unhandled failure *also* propagates to the parent (cancelling siblings, per the Jobs topic) — this is separate from the 'when is it thrown' question and always applies."]}]},{heading:"try/catch — the simplest tool, with a catch",blocks:[{t:"p",text:"You can wrap suspend calls in ordinary `try/catch`, and it works — but placement matters and there's a critical rule about `CancellationException`."},{t:"code",title:"try/catch around suspend calls",code:`viewModelScope.launch {
    try {
        val data = repository.load()          // suspend call
        _state.value = UiState.Content(data)
    } catch (e: CancellationException) {
        throw e                               // NEVER swallow cancellation — rethrow
    } catch (e: IOException) {
        _state.value = UiState.Error("Network error")
    }
}`},{t:"list",items:["**Rethrow `CancellationException`**: a broad `catch (e: Exception)` grabs the cancellation signal too. If you don't rethrow it, cancellation breaks. Catch it first and rethrow, or catch specific exception types only.","**Placement**: try/catch only catches exceptions from code *inside* it that runs *synchronously in this coroutine*. It does **not** catch exceptions from a *child* coroutine you `launch` inside the try — that child's failure propagates through the Job hierarchy, not through your try/catch. This surprises people.","**For a single operation**, try/catch is clean. For structuring error handling across a whole scope, a `CoroutineExceptionHandler` may fit better."]}]},{heading:"Why try/catch doesn't catch child coroutine failures",blocks:[{t:"code",title:"The gotcha",code:`// WRONG: the try does NOT catch the child launch's exception
scope.launch {
    try {
        launch {                 // a CHILD coroutine
            throw IOException()  // propagates via Job hierarchy, NOT to the try below
        }
    } catch (e: Exception) {
        // never reached!
    }
}

// RIGHT: put the try/catch INSIDE the coroutine that does the risky work
scope.launch {
    launch {
        try { risky() } catch (e: Exception) { handle(e) }   // caught here
    }
}`},{t:"p",text:"The rule: **a child coroutine's exception propagates up through the Job tree, not out through the parent's `try/catch`.** So to catch a child's failure, the try/catch must be *inside the child* (around its own work), or you use a `CoroutineExceptionHandler` in the context (which handles uncaught exceptions that reach the root). This is a direct consequence of structured concurrency — failures flow through Jobs, not call stacks."}]},{heading:"CoroutineExceptionHandler — the last-resort net",blocks:[{t:"p",text:"A **`CoroutineExceptionHandler`** is a context element that catches **uncaught** exceptions that reach the root of a coroutine tree — a global safety net for logging or graceful handling, not a control-flow mechanism."},{t:"code",title:"Installing a handler",code:`val handler = CoroutineExceptionHandler { _, throwable ->
    Log.e("App", "Uncaught coroutine exception", throwable)
    crashReporter.report(throwable)
}

val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main + handler)

scope.launch { riskyWork() }   // uncaught exception -> goes to handler`},{t:"list",items:["**Only works for `launch`, not `async`**: `async` exceptions are meant to surface via `await()`, so the handler never sees them.","**Only catches at the *root* coroutine**: it must be installed in the scope (or the root `launch`), not a nested child. An exception from a child propagates up to the root, and only there does the handler fire. Installing it on an inner `launch` does nothing.","**It's a net, not a strategy**: by the time it fires, the coroutine has already failed and (with a regular Job) taken down its subtree. Use it for *logging/reporting* and last-resort graceful degradation, not for handling expected errors — handle those with try/catch where they occur.","**With `SupervisorJob`**: since child failures don't propagate to the parent, each child that should be caught needs its own handling; a handler on the supervisor scope catches failures of its *direct* `launch` children."]}]},{heading:"Errors as values — the recommended architecture",blocks:[{t:"p",text:"The most robust approach in app code is to *not rely on exceptions crossing coroutine boundaries* at all — instead, **model expected failures as values**. The data layer catches technical exceptions at the boundary and returns a typed result; the ViewModel handles that result as an expected branch."},{t:"code",title:"Result types instead of thrown exceptions",code:`// Repository: catch at the boundary, return a typed result
suspend fun getUser(id: String): Result<User> = withContext(ioDispatcher) {
    runCatching { api.getUser(id).toUser() }
        // runCatching catches CancellationException too — rethrow it:
        .onFailure { if (it is CancellationException) throw it }
}

// ViewModel: no try/catch needed; handle the result
viewModelScope.launch {
    repository.getUser(id)
        .onSuccess { _state.value = UiState.Content(it) }
        .onFailure { _state.value = UiState.Error(it.message) }
}`},{t:"list",items:["**Why this is better**: expected failures (network down, 404) are *expected outcomes*, not exceptional control flow. Modeling them as `Result`/sealed types makes handling explicit and exhaustive, and keeps exceptions for genuine bugs.","**`runCatching` caveat**: it catches *everything*, including `CancellationException` — so in a coroutine you must rethrow cancellation (as shown), or use a custom `runSuspendCatching` helper that does it for you.","**Map to domain errors**: translate `HttpException`/`IOException` into a sealed `DataError` at the data-layer boundary, so upper layers never see library-specific exceptions (ties into the Clean Architecture error-handling topic)."]},{t:"note",text:"The layered answer to 'how do you handle coroutine exceptions': (1) `launch` propagates immediately, `async` defers to `await()`; (2) try/catch for local handling, but rethrow `CancellationException` and know it won't catch *child* failures; (3) `CoroutineExceptionHandler` as a root-level net for logging (launch only); (4) architecturally, prefer *errors as values* (Result/sealed types) so failures are explicit and exceptions are reserved for bugs."}]}],RS=[{level:"junior",q:"How do you handle an exception from a coroutine started with launch?",a:[{t:"p",text:"**The concept**: in a `launch` coroutine, an uncaught exception propagates immediately (up to the parent scope, and ultimately to a handler or a crash). The simplest way to handle it is a `try/catch` *inside* the coroutine around the risky suspend calls."},{t:"code",title:"try/catch inside launch",code:`viewModelScope.launch {
    try {
        val data = repository.load()
        _state.value = UiState.Content(data)
    } catch (e: CancellationException) {
        throw e                       // always rethrow cancellation
    } catch (e: Exception) {
        _state.value = UiState.Error(e.message)
    }
}`},{t:"p",text:"The one rule you must not forget: rethrow `CancellationException`. Since it's a subclass of `Exception`, a broad catch grabs it, and swallowing it breaks cancellation. So catch it first and rethrow, or catch only the specific exceptions you expect. For a single operation like this, try/catch is clean and clear. For app architecture, many teams prefer returning errors as values (a `Result` type) from the repository so the ViewModel handles failure as an explicit branch rather than catching exceptions."}]},{level:"junior",q:"How is exception handling different between launch and async?",a:[{t:"p",text:"**The difference is *when* the exception surfaces.** In `launch`, an exception is thrown *immediately* when it occurs and propagates up right away. In `async`, the exception is *captured inside the returned `Deferred`* and only re-thrown when you call `await()` on it."},{t:"code",title:"launch throws now, async throws at await",code:`launch { throw IOException() }          // propagates immediately

val d = async { throw IOException() }   // captured, not thrown yet
d.await()                                // throws here`},{t:"list",items:["**`launch`**: handle with try/catch inside, or a `CoroutineExceptionHandler` at the scope root.","**`async`**: handle by wrapping `await()` in try/catch. If you never call `await()` on a standalone async, its exception can go unnoticed.","**Both, as children of a scope**: an unhandled failure also propagates to the parent scope (cancelling siblings, with a regular Job) — that's structured concurrency and is separate from the 'when is it thrown' question."]},{t:"p",text:"So the practical guidance: for fire-and-forget work use `launch` and catch inside; for concurrent results use `async` and catch around `await()`. A common mistake is expecting a `CoroutineExceptionHandler` to catch an `async` exception — it won't, because async exceptions are meant to be surfaced through `await()`."}]},{level:"junior",q:"What is a CoroutineExceptionHandler and when does it work?",a:[{t:"p",text:"**The concept**: a `CoroutineExceptionHandler` is a coroutine context element that catches *uncaught* exceptions reaching the root of a coroutine tree. You install it in a scope's context, and it acts as a global net — typically for logging or crash reporting."},{t:"code",title:"Installing one",code:`val handler = CoroutineExceptionHandler { _, e ->
    Log.e("App", "Uncaught", e)
}
val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main + handler)
scope.launch { risky() }   // uncaught exception -> handler`},{t:"list",items:["**Works only for `launch`**, not `async` (async exceptions surface via `await()`).","**Works only at the *root* coroutine** — it must be in the scope or root launch; installing it on a nested child does nothing, because the exception propagates up to the root before the handler fires.","**It's a net, not a control-flow tool**: by the time it runs, the coroutine has already failed. Use it for logging/reporting and last-resort handling, not for handling expected errors (use try/catch or Result types for those)."]}]},{level:"junior",q:"Why doesn't a try/catch around a launch block catch exceptions from coroutines launched inside it?",a:[{t:"p",text:"**Because a child coroutine's exception propagates through the *Job hierarchy*, not up the call stack through your try/catch.** When you `launch` a child coroutine, it runs independently as a node in the coroutine tree. If it throws, the exception travels up the *parent-child Job relationship* — it does not 'return' to the point where you called `launch`, so the surrounding try/catch never sees it."},{t:"code",title:"The mistake and the fix",code:`// WRONG — try never catches the child's exception
launch {
    try {
        launch { throw IOException() }   // child; propagates via Job tree
    } catch (e: Exception) { /* never runs */ }
}

// RIGHT — catch inside the child
launch {
    launch {
        try { risky() } catch (e: Exception) { handle(e) }
    }
}`},{t:"p",text:"The fix is to put the try/catch *inside the coroutine that does the risky work* (so it's on the same call stack), or use a `CoroutineExceptionHandler` for uncaught exceptions at the root. This is a direct consequence of structured concurrency: exceptions flow along Job parent-child edges, not along synchronous call stacks. Understanding this prevents the common bug of 'my try/catch isn't catching anything'."}]},{level:"senior",q:"How does exception propagation interact with SupervisorJob and CoroutineExceptionHandler?",a:[{t:"p",text:"**The interaction is subtle and a favorite senior question.** With a *regular* Job, a child's uncaught exception propagates up to the parent, cancels the parent and siblings, and continues to the root where a `CoroutineExceptionHandler` (if present) handles it. With a `SupervisorJob`, the exception does **not** propagate to the parent — so the behavior changes:"},{t:"list",items:["**Under a `SupervisorJob`, each direct child is treated as a 'root' for exception purposes.** A child's uncaught exception doesn't cancel siblings or the parent — but it still needs handling, and a `CoroutineExceptionHandler` installed on the *supervisor scope* WILL catch the uncaught exceptions of its direct `launch` children (because for a supervised child, the scope is effectively its root).","**Placement of the handler matters**: with a regular Job, only a handler at the *root* matters (child handlers are ignored, since exceptions propagate to the root first). With a SupervisorJob, a handler on the supervisor catches direct children's failures. So `SupervisorJob() + handler` in the scope is a common pattern for 'isolate failures AND log them'.","**Still only `launch`, never `async`**: regardless of Job type, `CoroutineExceptionHandler` never handles `async` exceptions — those must be caught at `await()`.","**Depth caveat**: a SupervisorJob only supervises *direct* children. A grandchild (launched by a child with a regular Job) that fails will cancel *its* parent (the child), because that inner level uses a normal Job — the top supervisor doesn't reach down."]},{t:"code",title:"Isolate + log pattern",code:`val handler = CoroutineExceptionHandler { _, e -> log(e) }
val scope = CoroutineScope(SupervisorJob() + Dispatchers.Main + handler)

scope.launch { widgetA() }   // if it fails: isolated (siblings survive) AND logged
scope.launch { widgetB() }   // unaffected by A's failure`},{t:"p",text:"**The complete mental model**: `SupervisorJob` controls *whether* a failure propagates to siblings/parent (it doesn't); `CoroutineExceptionHandler` controls *what happens* to an exception once it becomes uncaught at a root. Together: supervisor isolates, handler logs. Knowing that the handler works on direct supervised children (but not on children under a regular Job, where only the root handler fires) is the depth signal."}]},{level:"senior",q:"Why is 'errors as values' often preferred over exceptions for coroutine error handling in app code?",a:[{t:"p",text:"**Because most failures in an app are *expected outcomes*, not exceptional bugs — and modeling them as values makes handling explicit, exhaustive, and decoupled from coroutine mechanics.** A network being down or a 404 isn't a programming error; it's a normal case the UI must handle. Throwing exceptions for these means every caller must remember to try/catch (easy to forget), the exception's type is untyped in the signature, and you're relying on exceptions crossing coroutine boundaries correctly (with all the launch/async/SupervisorJob subtleties)."},{t:"list",items:["**Explicit and type-safe**: a repository returning `Result<User>` or a sealed `Either<DataError, User>` puts the failure *in the type signature*. Callers can't ignore it, and a `when` over a sealed error is exhaustive (the compiler ensures every case is handled).","**Failures become normal control flow**: `repository.getUser().onSuccess { }.onFailure { }` handles the error as a branch, no try/catch, no reliance on exception propagation rules.","**Separation of concerns**: exceptions are reserved for genuine *bugs* (a null where there shouldn't be one) which *should* crash in debug, while expected failures (offline, not-found) are data. This makes crash reports meaningful — a crash is a real bug, not 'the network was down'.","**Boundary translation**: the data layer catches technical exceptions (`IOException`, `HttpException`) at its edge and maps them to a domain error vocabulary (`DataError.NoConnection`, `DataError.NotFound`), so upper layers never depend on library exception types."]},{t:"code",title:"The pattern (with the runCatching cancellation caveat)",code:`suspend fun getUser(id: String): Result<User> = withContext(io) {
    runCatching { api.getUser(id).toUser() }
        .onFailure { if (it is CancellationException) throw it }  // don't swallow cancellation
}`},{t:"p",text:"**The important caveat**: `runCatching` catches *everything*, including `CancellationException` — so you must rethrow cancellation (or use a `runSuspendCatching` helper), or you'll break structured cancellation while trying to model errors as values. With that handled, errors-as-values gives you the cleanest, most testable error handling — and it's why modern Android architecture guidance leans on Result/sealed types rather than exceptions crossing coroutine boundaries."}]}],LS={id:"exceptions",title:"Exception Handling",tagline:"launch vs async exception behavior, try/catch placement, why child failures escape try/catch, CoroutineExceptionHandler, and errors-as-values.",tags:["Coroutines","Exceptions","CoroutineExceptionHandler","Result"],content:PS,qa:RS},DS=[{heading:"Channels — communication between coroutines",blocks:[{t:"p",text:"A **`Channel`** is a coroutine primitive for passing values *between* coroutines — conceptually a coroutine-friendly `BlockingQueue`, but with *suspending* operations instead of blocking ones. One coroutine `send`s values, another `receive`s them. Unlike a Flow (a cold, per-collector stream), a Channel is **hot** and each value goes to exactly **one** receiver — it's a hand-off pipe, not a broadcast."},{t:"code",title:"A basic channel",code:`val channel = Channel<Int>()

launch {                       // producer
    for (x in 1..5) channel.send(x)   // suspends if channel is full
    channel.close()            // signal no more values
}

launch {                       // consumer
    for (x in channel) {       // receives until closed; suspends if empty
        println(x)
    }
}`},{t:"list",items:["**`send` and `receive` suspend**: `send` suspends if the channel is full (backpressure); `receive` suspends if it's empty (waits for a value). No blocking.","**One value → one receiver**: if multiple coroutines receive from a channel, each value goes to only one of them (fan-out). This is fundamentally different from a `SharedFlow`, which broadcasts each value to *all* collectors.","**`close()`** signals completion — receivers' `for` loops end. A closed channel throws on further `send`.","**Flow vs Channel**: use a **Flow** for a stream you observe (cold, declarative, operators); use a **Channel** for actual *communication/hand-off* between coroutines (hot, each value consumed once) — like a work queue or a pipe between a producer and a worker."]}]},{heading:"Channel capacity and buffering",blocks:[{t:"table",headers:["Capacity","Behavior"],rows:[["`RENDEZVOUS` (default, 0)","send suspends until a receiver is ready — direct hand-off"],["`BUFFERED` (default size)","send doesn't suspend until the buffer is full"],["`CONFLATED`","keeps only the latest value; send never suspends, old value dropped"],["`UNLIMITED`","unbounded buffer; send never suspends (watch memory)"],["a specific number","buffer of that size, with an onBufferOverflow policy"]]},{t:"list",items:["**Rendezvous (0)** is the default — sender and receiver must 'meet': `send` waits for a `receive`. Tightest synchronization, no buffering.","**Conflated** — like `StateFlow`'s conflation: only the latest value is kept, useful when the consumer only cares about the newest.","**Unlimited** — never suspends `send`, but an unbounded buffer can grow without limit if the consumer is slow (memory risk).","You can also set `onBufferOverflow` (SUSPEND / DROP_OLDEST / DROP_LATEST) — the same knobs as SharedFlow, because they share machinery."]}]},{heading:"produce and actor — structured channel patterns",blocks:[{t:"code",title:"produce — a channel-backed producer coroutine",code:`fun CoroutineScope.produceNumbers(): ReceiveChannel<Int> = produce {
    var n = 0
    while (true) { send(n++); delay(100) }
}   // channel is closed automatically when the coroutine ends/cancels

val numbers = produceNumbers()
launch { for (n in numbers) println(n) }`},{t:"list",items:["**`produce { }`** — a builder that creates a coroutine bound to a `ReceiveChannel`; the channel closes automatically when the coroutine completes or is cancelled (structured — no leaked channel). The idiomatic way to make a producer.","**`actor { }`** — the inverse: a coroutine with an incoming channel that processes messages one at a time. Because it processes messages sequentially, it's a way to *safely manage state without locks* — send it messages, it handles them in order (the actor model). (Now somewhat legacy in favor of other patterns, but conceptually important.)","**Channels are hot and stateful** — a `produce` channel starts producing as soon as it's created and a receiver pulls; unlike a cold Flow, it doesn't restart per consumer."]}]},{heading:"Shared mutable state — the problem and the tools",blocks:[{t:"p",text:"Coroutines run concurrently, so multiple coroutines touching the same mutable variable causes **race conditions** — the same data corruption threads have. `var counter = 0` incremented from 100 coroutines won't reach 100 without synchronization. Kotlin offers several tools, in rough order of preference:"},{t:"list",items:["**Avoid shared mutable state (best)**: prefer immutable data and confine state to one place — e.g. a `StateFlow` updated only via atomic `update { }`, or confining mutations to a single coroutine/dispatcher. No shared mutation, no races.","**`Mutex`** — a *suspending* lock (mutual exclusion). `mutex.withLock { ... }` ensures only one coroutine executes the critical section at a time, and it *suspends* rather than blocks while waiting (unlike a `synchronized` block, which blocks a thread).","**Atomics** — `AtomicInteger`, `atomic()` (kotlinx-atomicfu) for simple counters/flags: lock-free, fast, but only for single-variable operations.","**Single-threaded confinement** — run all access to the state on one dedicated dispatcher (`newSingleThreadContext` or `limitedParallelism(1)`), so operations are serialized by construction — no explicit locking needed."]},{t:"code",title:"Mutex — suspending mutual exclusion",code:`val mutex = Mutex()
var counter = 0

suspend fun increment() {
    mutex.withLock {          // suspends (not blocks) if another coroutine holds it
        counter++             // critical section — one coroutine at a time
    }
}`}]},{heading:"Mutex vs synchronized, and Semaphore",blocks:[{t:"list",items:["**`Mutex` vs `synchronized`/`ReentrantLock`**: a `synchronized` block *blocks the thread* while waiting for the lock — inside a coroutine that blocks a valuable dispatcher thread. `Mutex.withLock` *suspends* the coroutine while waiting, freeing the thread. In coroutine code, prefer `Mutex`. Also, `Mutex` is **not reentrant** (a coroutine can't re-acquire a lock it already holds — that deadlocks), unlike `ReentrantLock`.","**`Semaphore`** — like a Mutex but allows *N* concurrent holders instead of 1. `Semaphore(permits = 4).withPermit { }` limits concurrency — e.g. cap parallel network requests to 4. Suspending, coroutine-friendly.","**Never hold a lock across arbitrary suspension points carelessly** — if you suspend for a long operation while holding a `Mutex`, other coroutines wait the whole time; keep critical sections short."]}]},{heading:"select — awaiting the first of several",blocks:[{t:"p",text:"**`select { }`** lets a coroutine await *multiple* suspending sources simultaneously and proceed with whichever is ready *first* — like a coroutine `switch` over async events. Useful for racing operations, timeouts, or multiplexing channels."},{t:"code",title:"Racing two sources with select",code:`val result = select<String> {
    deferredA.onAwait { "A won: $it" }      // whichever completes first
    deferredB.onAwait { "B won: $it" }
    channel.onReceive { "channel: $it" }
}`},{t:"list",items:["`select` picks the *first* clause to become ready and runs it, ignoring the rest. Common uses: implement a timeout by racing work against a `delay`, pick the fastest of redundant requests, or merge several channels.","It's an advanced primitive — most app code uses higher-level tools (`withTimeout`, `combine`, `merge`), but `select` is the building block underneath and appears in deeper interview questions."]},{t:"note",text:"Summary of the synchronization toolbox: prefer *avoiding* shared mutable state (immutability + StateFlow.update or single-thread confinement); for real critical sections use `Mutex.withLock` (suspending, non-reentrant) over `synchronized` (blocking); use `Semaphore` to limit concurrency to N; use `select` to await the first of several sources. Channels are for coroutine-to-coroutine hand-off (hot, one-value-one-receiver), distinct from Flows (cold, observed streams)."}]}],VS=[{level:"junior",q:"What is a Channel and how is it different from a Flow?",a:[{t:"p",text:"**The concept**: a `Channel` passes values *between* coroutines — one coroutine `send`s, another `receive`s, with both operations suspending (send suspends if full, receive suspends if empty). It's like a coroutine-friendly queue with non-blocking operations. It's used for actual communication/hand-off between coroutines, like a work queue or a pipe."},{t:"list",items:["**Channel is hot; Flow is cold**: a Channel exists and holds/transfers values independent of consumers; a Flow is a recipe that runs per collector.","**Channel: one value → one receiver**: each value sent is received by exactly one receiver (fan-out). A `SharedFlow`, by contrast, broadcasts each value to *all* collectors. This is the key behavioral difference.","**Flow is for observing a stream** (declarative, operators, cold); **Channel is for communication between coroutines** (hot, each value consumed once)."]},{t:"p",text:"Rule of thumb: if you're modeling 'a stream of data I want to observe and transform', use a Flow. If you're modeling 'coroutine A hands work items to coroutine B', use a Channel. In app code you often use Channels indirectly — e.g. `callbackFlow` and one-shot event delivery are built on channels — while using Flows for the observable-stream cases."}]},{level:"junior",q:"What is a Mutex and how is it different from a synchronized block?",a:[{t:"p",text:"**The concept**: a `Mutex` (mutual exclusion) is a lock that ensures only one coroutine executes a critical section at a time — you wrap the section in `mutex.withLock { }`. It protects shared mutable state from concurrent access, just like a lock in thread-based code."},{t:"p",text:"**The key difference from `synchronized`**: a `synchronized` block (or `ReentrantLock`) *blocks the thread* while waiting to acquire the lock. Inside a coroutine, that blocks a valuable dispatcher thread — defeating the point of coroutines. A `Mutex.withLock` *suspends* the coroutine while waiting, freeing the thread to run other coroutines. So in coroutine code you should prefer `Mutex` over `synchronized`."},{t:"code",title:"Mutex protecting a counter",code:`val mutex = Mutex()
var count = 0
suspend fun inc() = mutex.withLock { count++ }`},{t:"p",text:"One important gotcha: `Mutex` is **not reentrant** — a coroutine that already holds the lock cannot acquire it again (doing so deadlocks), unlike `ReentrantLock`. And you should keep critical sections short and avoid long suspensions while holding the lock, since other coroutines wait the whole time. Better still, where possible, avoid shared mutable state entirely (immutable data + `StateFlow.update`) so you don't need a lock at all."}]},{level:"junior",q:"How do you safely update shared state from multiple coroutines?",a:[{t:"p",text:"**The concept**: coroutines run concurrently, so multiple coroutines mutating the same variable causes race conditions — data corruption, lost updates. You need a synchronization strategy. In rough order of preference:"},{t:"list",items:["**Avoid shared mutable state (best)**: use immutable data and a single owner. A `StateFlow` updated via `_state.update { it.copy(...) }` is atomic (compare-and-set), so concurrent updates don't lose writes — no explicit lock needed. This is the idiomatic Android answer.","**`Mutex.withLock { }`**: a suspending lock for a genuine critical section — one coroutine at a time, suspends (doesn't block) while waiting.","**Atomics** (`AtomicInteger`, atomicfu): lock-free and fast for a *single* variable (a counter, a flag), but only for single-variable operations.","**Single-threaded confinement**: run all access on one dedicated dispatcher (`limitedParallelism(1)`), serializing operations by construction — no explicit locking."]},{t:"p",text:"The key insight interviewers want: the *best* solution is usually to *not share mutable state* — confine it or make it immutable. `StateFlow.update { }` covers most Android cases (it's atomic), and you drop to `Mutex`/atomics/confinement only when you have genuinely shared mutable state that can't be restructured. Reaching straight for a lock when `StateFlow.update` would do is a mild red flag."}]},{level:"junior",q:"What is a Semaphore used for in coroutines?",a:[{t:"p",text:"**The concept**: a `Semaphore` is like a `Mutex` but allows up to *N* coroutines into the critical section at once, instead of just one. You create it with a number of permits and wrap work in `semaphore.withPermit { }`; a coroutine suspends until a permit is available. It's used to **limit concurrency** to a fixed number."},{t:"code",title:"Limiting parallel network requests to 4",code:`val semaphore = Semaphore(permits = 4)

suspend fun fetchAll(urls: List<String>) = coroutineScope {
    urls.map { url ->
        async {
            semaphore.withPermit {   // at most 4 run concurrently
                api.fetch(url)
            }
        }
    }.awaitAll()
}`},{t:"p",text:"The classic use is rate-limiting parallel work: you want to fetch 100 URLs but not open 100 simultaneous connections, so a `Semaphore(4)` caps it at 4 in flight. It's suspending (doesn't block threads while waiting for a permit), which makes it coroutine-friendly. A related modern alternative for the same goal is `Dispatchers.IO.limitedParallelism(4)`, which gives a view of the IO pool capped at 4 concurrent coroutines — often simpler when the limit is about dispatcher usage rather than a specific critical section."}]},{level:"senior",q:"When would you use a Channel vs a SharedFlow for passing events between coroutines?",a:[{t:"p",text:"**The decision hinges on delivery semantics: one-receiver hand-off vs broadcast, and guaranteed delivery vs best-effort.** A `Channel` delivers each value to *exactly one* receiver and `send` *suspends until the value is received* (with default capacity) — so values aren't lost even if the receiver is momentarily absent. A `SharedFlow` *broadcasts* each value to *all* current collectors and drops values emitted while no one is collecting (with replay 0)."},{t:"list",items:["**Use a `Channel` when**: each event should be handled *once* by a single consumer, and you can't afford to lose events across brief gaps in collection. This is exactly why `Channel(BUFFERED).receiveAsFlow()` is the common choice for one-shot UI events (navigation, snackbars) — `send` waits through the lifecycle gap during a config change, so the event isn't dropped, and it's consumed once.","**Use a `SharedFlow` when**: multiple independent collectors each need to see *every* event (a true broadcast) — e.g. several components reacting to the same app-wide signal. Configure `replay`/buffer as needed. But accept that events emitted with no active collector (replay 0) are lost.","**The fan-out trap**: if you use a `Channel` but accidentally have *two* collectors, each event goes to only *one* of them (whichever receives first) — a source of 'half my events disappear' bugs. If you need every listener to get every event, that's a SharedFlow, not a Channel.","**Guaranteed-once vs broadcast is the crux**: Channel = 'exactly one consumer, delivery waits'; SharedFlow = 'all consumers, best-effort'. For navigation/snackbar events (one handler, must not drop), Channel wins; for broadcasting state changes to many observers, SharedFlow (or StateFlow) wins."]},{t:"p",text:"**Neither fully solves process death** — a killed process loses buffered channel items and un-replayed SharedFlow emissions alike; truly durable one-shot outcomes should be modeled as persisted state. But for in-process event delivery, the Channel-vs-SharedFlow choice is precisely about one-receiver-guaranteed vs all-receivers-best-effort."}]},{level:"senior",q:"What is the select expression and when is it useful?",a:[{t:"p",text:"**The concept**: `select { }` lets a coroutine wait on *multiple* suspending sources at once and proceed with whichever becomes ready *first*, running that clause and abandoning the rest. It's like a `switch`/`when` over asynchronous events — the coroutine equivalent of `select()` in Go or a multiplexing wait."},{t:"code",title:"Racing sources",code:`val winner = select<String> {
    fastReplica.onAwait { "fast: $it" }     // whichever Deferred finishes first
    slowReplica.onAwait { "slow: $it" }
    timeoutChannel.onReceive { "timed out" }
}`},{t:"list",items:["**Uses**: racing redundant requests and taking the fastest (hedged requests); implementing a custom timeout by racing work against a `delay`; multiplexing several channels into one consumer (proceed with whichever has data); building higher-level combinators.","**Clauses**: `onAwait` (for `Deferred`), `onReceive`/`onReceiveCatching` (for channels), `onSend` (for sending when a channel is ready), `onTimeout` — you list the alternatives and `select` picks the first ready one.","**It's a low-level primitive**: most app code uses higher-level tools built on similar ideas — `withTimeout` (race against a deadline), `merge` (combine flows), `combine`. You reach for raw `select` when you need custom 'first of several' logic those don't express.","**Fairness and atomicity caveats**: `select` chooses among ready clauses and the non-selected clauses are not consumed; care is needed with channels so you don't accidentally drop values (use `onReceiveCatching` and understand which clause 'wins')."]},{t:"p",text:"**When to bring it up**: `select` appears in senior questions about implementing timeouts, hedged/racing requests, or channel multiplexing from scratch. Knowing it exists and what it does (await the first-ready of several sources) — even while noting you'd usually prefer `withTimeout`/`merge` — demonstrates depth beyond the everyday coroutine API."}]},{level:"senior",q:"Compare using produce/actor, Mutex, and single-threaded confinement for managing shared state.",a:[{t:"p",text:"**These are three different philosophies for safe concurrent state, and the 'best' depends on the shape of the problem.** They range from lock-based to message-based to confinement-based."},{t:"list",items:["**`Mutex` (lock-based)**: protect a critical section so one coroutine mutates at a time. Simple and direct for guarding a small piece of shared state. Downsides: you must remember to lock *every* access, it's non-reentrant (deadlock risk), and holding it across suspensions serializes coroutines. Good for a few well-defined critical sections; error-prone if state is accessed from many places.","**`actor` (message-based / actor model)**: a single coroutine owns the state and processes incoming messages from a channel *one at a time*. Other coroutines don't touch the state — they *send messages*. Because the owner processes sequentially, there's no concurrent access and thus no locks. Excellent for state with complex operations and many mutators (a cache, a connection manager); the tradeoff is more ceremony (define a message type) and it's somewhat legacy in Kotlin now.","**Single-threaded confinement**: run all access to the state on one dedicated dispatcher (`limitedParallelism(1)` or `newSingleThreadContext`). Operations are serialized by the dispatcher, so no locks and no message types — you just `withContext(stateDispatcher) { mutate() }`. Clean when the state is naturally 'owned' by one context; the cost is every access must hop to that dispatcher."]},{t:"list",items:["**How to choose**: for *simple* shared state, prefer *avoiding* the problem — immutable data + `StateFlow.update` (atomic) or a single atomic — over any of these. For a *critical section* guarding modest state accessed in a few places, `Mutex`. For state with *rich operations and many concurrent mutators*, the actor pattern (or confinement) reads better than scattering locks. For state that *conceptually belongs to one owner*, single-threaded confinement is the cleanest.","**The unifying principle**: the safest concurrent state is *no shared mutable state* — confine ownership to one place (one coroutine, one dispatcher, or one atomic holder) so concurrency can't corrupt it. Locks are the fallback when you genuinely have shared mutation you can't restructure away."]}]}],jS={id:"channels-sync",title:"Channels & Synchronization",tagline:"Channels vs Flows, capacity/buffering, produce/actor, shared mutable state, Mutex vs synchronized, Semaphore, and the select expression.",tags:["Coroutines","Channel","Mutex","Semaphore","select"],content:DS,qa:VS},OS={id:"coroutines",name:"Coroutines",description:"Kotlin's concurrency framework — suspension internals, builders & scopes, structured concurrency, dispatchers, cancellation, exceptions, and channels.",topics:[yS,bS,SS,MS,ES,LS,jS]},US=[{heading:"What a Flow is — a suspend-friendly stream",blocks:[{t:"p",text:"A **`Flow<T>`** is Kotlin's type for an **asynchronous stream of values** — think of it as the async, coroutine-native cousin of a `List`. A `List<T>` gives you all its values at once, already computed. A `suspend fun` gives you a *single* value, later. A `Flow<T>` gives you *multiple* values, over time, each possibly arriving after a suspend — and it does so cooperatively with coroutines (it can suspend between emissions instead of blocking)."},{t:"table",headers:["You need","Use"],rows:[["One value, now","a normal function"],["One value, later (async)","`suspend fun`"],["Many values, now","`List` / `Sequence`"],["Many values, over time (async)","`Flow`"]]},{t:"list",items:["Examples that are naturally flows: database rows that re-emit when the table changes (Room `Flow` queries), location updates, UI state over time, a stream of search results as the user types, WebSocket messages.","A Flow is built from three roles: a **producer** that emits values (`emit(value)`), zero or more **intermediate operators** that transform the stream (`map`, `filter`), and a **collector** (consumer) that receives the values (`collect { }`).","Flow is part of `kotlinx.coroutines` — it's pure Kotlin (works in KMP, no Android dependency), which is a major reason it replaced LiveData and RxJava in modern Android."]}]},{heading:"Cold flows — the defining property",blocks:[{t:"p",text:"Flows are **cold** by default. Cold means: **the producer code does not run until someone collects, and it runs again, from scratch, for every collector.** Building a flow (`flow { ... }`) creates only a *recipe*; nothing executes until a terminal operator like `collect` pulls values through it. Each new `collect` re-executes the whole recipe independently."},{t:"code",title:"Cold in action — nothing runs until collection",code:`fun numbers(): Flow<Int> = flow {
    println("Flow started")     // producer body
    for (i in 1..3) {
        delay(100)
        emit(i)                 // hand a value to the collector
    }
}

// Building the flow prints NOTHING:
val f = numbers()

// First collection: prints "Flow started", then 1,2,3
f.collect { println(it) }

// Second collection: prints "Flow started" AGAIN, then 1,2,3 again
f.collect { println(it) }`},{t:"list",items:["**Lazy**: no collector, no work. A flow that hits the network does nothing until collected — no wasted requests.","**Independent per collector**: two collectors each get their own private run of the producer — their own network call, their own sequence from the start. There is no sharing.","**Why this matters for Android**: a `Flow` from a repository is safe to create eagerly and pass around; the expensive work only happens where it's actually observed, and it stops when collection stops (structured concurrency). Contrast **hot** streams (StateFlow/SharedFlow), which emit regardless of collectors and share one stream among all — covered in their own topic."]},{t:"note",text:'The cold/hot distinction is the single most-asked Flow interview question. One-liner: "Cold flows are lazy recipes that re-run per collector and only while collected; hot flows exist and emit independently of collectors, multicasting one stream to all."'}]},{heading:"Building flows — the producer side",blocks:[{t:"code",title:"The common builders",code:`// flow { } — the general builder; can suspend, loop, call suspend fns
flow {
    val page1 = api.load(1); emit(page1)
    val page2 = api.load(2); emit(page2)
}

// flowOf(...) — a fixed set of values
flowOf(1, 2, 3)

// asFlow() — from a collection/sequence/range
listOf("a", "b").asFlow()
(1..100).asFlow()

// channelFlow { } — when emissions come from multiple coroutines
channelFlow {
    launch { send(fetchA()) }
    launch { send(fetchB()) }
}

// callbackFlow { } — bridge a callback-based API into a Flow
callbackFlow {
    val listener = LocationListener { loc -> trySend(loc) }
    manager.register(listener)
    awaitClose { manager.unregister(listener) }
}`},{t:"list",items:["**`flow { }`** is the workhorse: its lambda is a suspend block, so you can `delay`, call other suspend functions, loop, and `emit` as many times as you want.","**Emission must stay in the builder's coroutine**: you can't `emit` from a different coroutine you launched inside `flow { }` — that throws. When you genuinely need concurrent producers, use **`channelFlow`**/`callbackFlow`, which provide a thread-safe `send`/`trySend`.","**`callbackFlow`** is how you turn listener/callback APIs (location, sensors, Firebase listeners) into flows — it must call `awaitClose { }` to unregister when collection stops (covered fully in the callbackFlow topic)."]}]},{heading:"Terminal operators — the collector side",blocks:[{t:"p",text:"A **terminal operator** is what actually starts the flow and consumes it. Everything else (map, filter) is just describing the pipeline; a terminal operator turns the crank. Terminal operators are `suspend` functions — they suspend the collecting coroutine until the flow completes (or forever, for infinite flows)."},{t:"code",title:"Terminal operators",code:`// collect — the fundamental one; runs the block per emission
flow.collect { value -> println(value) }

// collectLatest — cancels the block if a NEW value arrives before it finishes
searchQueries.collectLatest { query ->
    val results = repository.search(query)  // cancelled if query changes
    show(results)
}

// Aggregating terminals (they collect internally, then return one value):
val total = flow.reduce { acc, v -> acc + v }
val count = flow.count()
val list  = flow.toList()
val first = flow.first()          // takes one value then cancels the flow
val single = flow.single()

// launchIn — collect in a given scope without a suspend call site
flow.onEach { render(it) }.launchIn(viewModelScope)`},{t:"list",items:["**`collect`** processes every emission in order, waiting for your block to finish before the next value is delivered (backpressure by suspension — the producer waits if the collector is slow).","**`collectLatest`** cancels the *previous* block when a new value arrives — essential for 'only the latest matters' cases (search-as-you-type: abandon the in-flight search when the query changes).","**`launchIn(scope)`** = `scope.launch { flow.collect() }` — a convenient way to start collection in a scope (paired with `onEach` for the per-value logic). Common in ViewModels.","**`first()`/`take(n)`** cancel the flow early once they have what they need — cancellation propagates back to the producer (which stops emitting), a nice property of structured flows."]}]},{heading:"Flows are sequential, and respect cancellation",blocks:[{t:"list",items:["**Sequential by default**: within a single collection, values are produced and processed one at a time, in order — `emit` suspends until the collector's block (and downstream operators) finish with that value. There's no concurrency unless you explicitly add it (`buffer`, `flatMapMerge`, `channelFlow`).","**Context preservation**: a flow's producer runs in the collector's coroutine context by default — you don't switch threads implicitly. Changing the producer's context requires `flowOn` (its own topic); emitting from a different context without it throws (a safety check).","**Cancellation-cooperative**: because collection happens in a coroutine, cancelling that coroutine cancels the collection, which cancels the producer. Flow builders check for cancellation at each `emit`, so a cancelled flow stops promptly. (Tight CPU loops that never emit/suspend still need manual `ensureActive()`/`isActive` checks — same as any coroutine.)","**Exceptions propagate downward**: an exception in the producer or an operator surfaces at the collector, and can be handled with the `catch` operator or a try/catch around `collect` (error handling has its own topic)."]}]}],FS=[{level:"junior",q:"What is a Flow and when would you use one?",a:[{t:"p",text:"**The concept**: a `Flow<T>` is an asynchronous stream of values — it produces *multiple* values *over time*, cooperatively with coroutines. The easiest way to place it: a normal function returns one value now; a `suspend fun` returns one value later; a `List` gives many values now; a `Flow` gives many values later, as they become available, able to suspend between them instead of blocking."},{t:"p",text:"**When to use it**: anything that's a *stream* rather than a one-shot. Room database queries that should re-emit whenever the data changes; location or sensor updates; a UI state that changes over time; search results updating as the user types; WebSocket messages. If the answer to 'how many values, and when?' is 'several, over time', it's a Flow. If it's 'exactly one, later', it's a plain suspend function. Flow is also pure Kotlin (no Android dependency), which is why modern Android and KMP use it in place of LiveData and RxJava."}]},{level:"junior",q:"What does it mean that a Flow is 'cold'?",a:[{t:"p",text:"**The concept**: cold means the flow's producer code **doesn't run until someone collects it**, and it **runs again from the start for every collector**. Creating a flow with `flow { ... }` just builds a recipe — nothing executes. Only a terminal operator like `collect` pulls values through, and each separate `collect` triggers its own independent run of the producer."},{t:"code",title:"Two collections = two independent runs",code:`val f = flow { println("start"); emit(1); emit(2) }
// nothing printed yet
f.collect { }   // prints "start", emits 1,2
f.collect { }   // prints "start" AGAIN, emits 1,2 again`},{t:"p",text:"**Why it's useful**: laziness means no wasted work — a flow that calls the network does nothing until observed. Independence means each collector gets its own fresh sequence, with no interference. And because collection lives in a coroutine, when the collector's scope is cancelled, the producer stops too. The contrast is a *hot* stream (StateFlow/SharedFlow), which emits whether or not anyone is collecting and shares one stream across all collectors — but that's a separate concept with its own trade-offs."}]},{level:"junior",q:"What is the difference between an intermediate and a terminal operator?",a:[{t:"p",text:"**The concept**: flow operators come in two kinds. **Intermediate operators** (`map`, `filter`, `onEach`, `take`…) transform or configure the stream and return a *new flow* — but they're lazy: they only *describe* work and run nothing by themselves. **Terminal operators** (`collect`, `toList`, `first`, `reduce`, `count`…) actually start the flow, pull values through all the intermediate operators, and consume them. Terminal operators are `suspend` functions because they run over time."},{t:"p",text:"The practical consequence: a chain like `flow.map { }.filter { }` does *absolutely nothing* — no map, no filter — until you attach a terminal operator. This trips people up: 'my map isn't running' almost always means 'nothing is collecting'. Think of intermediate operators as assembling a pipeline and the terminal operator as opening the tap. `launchIn(scope)` is a convenience that packages `scope.launch { collect() }`, so `flow.onEach { }.launchIn(scope)` is the common 'start collecting in this scope' idiom."}]},{level:"junior",q:"How do you create a Flow? Name a few builders.",a:[{t:"list",items:["**`flow { }`** — the general builder. Its lambda is a suspend block, so you can call suspend functions, loop, `delay`, and `emit(value)` as many times as needed. The workhorse for custom producers.","**`flowOf(a, b, c)`** — a flow of a fixed set of values, like `listOf` for flows.","**`.asFlow()`** — turn a collection, sequence, or range into a flow (`(1..10).asFlow()`, `myList.asFlow()`).","**`callbackFlow { }`** — bridge a *callback/listener*-based API (location updates, sensor, Firebase listener) into a Flow; it gives a thread-safe `trySend` and requires `awaitClose { }` to clean up when collection stops.","**`channelFlow { }`** — like `flow { }` but allows emitting from *multiple* coroutines concurrently (via `send`), for when producers are parallel."]},{t:"p",text:"The one rule to remember about `flow { }`: you must `emit` from the builder's own coroutine — you can't launch a child coroutine inside it and emit from there (it throws, to protect flow's sequential context guarantees). When emissions genuinely come from multiple coroutines or a callback, that's exactly what `channelFlow`/`callbackFlow` are for."}]},{level:"junior",q:"What is the difference between collect and collectLatest?",a:[{t:"p",text:"**The concept**: both are terminal operators that consume every emission, but they differ in what happens when a new value arrives *while you're still processing the previous one*. `collect` processes each value to completion before accepting the next — your block runs fully for value 1, then fully for value 2, and so on (the producer waits for you). `collectLatest` **cancels** your still-running block when a newer value arrives and restarts it with the new value."},{t:"code",title:"collectLatest for search-as-you-type",code:`queryFlow.collectLatest { query ->
    // If the user types another letter before this search finishes,
    // this block is cancelled and restarted with the new query —
    // no wasted request, no stale results overwriting fresh ones.
    val results = repository.search(query)
    show(results)
}`},{t:"p",text:"**When to use which**: `collect` when every value must be fully handled (processing a queue of events, writing each item to a DB). `collectLatest` when *only the latest value matters* and older in-flight work should be abandoned — the classic case is search-as-you-type or reacting to the newest UI state, where finishing an outdated computation is wasteful and can cause the stale-result race (an old slow response overwriting a newer one). The operator equivalents on the transform side are `mapLatest`/`flatMapLatest`, which apply the same 'cancel previous' idea inside a pipeline."}]},{level:"senior",q:"Explain cold vs hot flows in depth. Give concrete Android examples of when to use each.",a:[{t:"p",text:"**Cold**: the producer runs *per collector*, starting on collection and stopping when collection stops; each collector gets an independent execution. **Hot**: the stream exists and emits *independently* of collectors, multicasting the same emissions to all current collectors; late collectors miss earlier values (unless replay is configured)."},{t:"list",items:["**Use cold** for *pull-based, per-consumer work that should be lazy and cancellable*: a repository function returning `flow { emit(api.fetch()) }` or a Room `Flow` query. Nothing runs until a screen observes it; two screens each get their own execution; navigating away cancels the work. This is the default and the right choice for most data-layer streams.","**Use hot** for *shared state or events that exist regardless of observers*: a `StateFlow` holding UI state (there's one current value, shared by all observers, and it exists whether or not the UI is on screen), or a `SharedFlow` broadcasting events to multiple listeners. Sensor/location streams that should run once and fan out to many collectors are also hot (often a cold `callbackFlow` converted to hot via `shareIn`).","**The bridge**: `stateIn`/`shareIn` convert a cold flow into a hot one — you take an expensive cold upstream (a DB query), collect it *once*, and multicast the result, so two screens don't trigger two DB observations. That's the standard ViewModel pattern (`stateIn(scope, WhileSubscribed(5000), initial)`)."]},{t:"p",text:"**The subtle bug this prevents**: if a ViewModel exposes a *cold* flow directly and two things collect it (the UI plus, say, a logger), you get two independent upstream executions — two network calls, two DB observations. Converting to hot with `stateIn` collects once and shares. Conversely, exposing raw mutable *hot* state lets collectors miss events or see stale replays. Knowing which to use where — cold in the data layer, hot for shared UI state, bridged with stateIn/shareIn — is the core competency the cold/hot question tests."}]},{level:"senior",q:"Why must you emit only from the flow builder's coroutine, and what do you use when values come from multiple coroutines or a callback?",a:[{t:"p",text:"**The reason for the restriction**: `flow { }` guarantees **context preservation** and **sequential emission** — the collector can rely on emissions arriving one at a time, in the collector's context (unless `flowOn` changes the producer's). If you could launch a child coroutine inside `flow { }` and call `emit` from it, emissions could happen concurrently and from a different context, breaking those guarantees. So the flow builder actively detects cross-coroutine emission and throws `IllegalStateException` ('Flow invariant is violated'). `emit` is not thread-safe by design."},{t:"list",items:["**`channelFlow { }`** — use when emissions genuinely originate from *multiple coroutines you launch* (concurrent producers). It backs the flow with a `Channel` and exposes a thread-safe `send`/`trySend` that *is* safe to call from any coroutine, precisely because a channel synchronizes them. You can `launch { send(a) }` and `launch { send(b) }` inside it.","**`callbackFlow { }`** — a `channelFlow` specialized for *callback/listener* APIs (which invoke you on their own threads). Register the listener, `trySend` from its callback, and call `awaitClose { unregister() }` so the listener is removed when collection is cancelled. This is the canonical way to wrap `LocationManager`, sensors, `ValueEventListener`, etc.","**Cost/trade-off**: both use a channel internally, so they're slightly heavier and their emission isn't strictly sequential in the same way `flow { }` is — you use them specifically *because* you need concurrent/external emission, and accept the channel's buffering semantics (configurable capacity, `onBufferOverflow`)."]},{t:"p",text:"The mental rule: plain `flow { }` for a single sequential producer (the common case); `channelFlow`/`callbackFlow` the moment emission must cross coroutine or thread boundaries."}]},{level:"senior",q:"How does cancellation work with flows, and what's the gotcha with tight loops?",a:[{t:"p",text:"**The model**: flow collection happens inside a coroutine, so it's governed by structured concurrency. Cancel the collecting coroutine (its scope is cancelled, the screen leaves, a `take(1)` completes) and the collection is cancelled — which propagates back up: the producer stops, and terminal operators like `first`/`take` cancel the upstream once they have enough. Flow builders are **cancellation-cooperative at each `emit`**: `emit` checks for cancellation, so a flow that emits or calls suspend functions periodically will stop promptly when cancelled, throwing `CancellationException` that unwinds the coroutine normally."},{t:"list",items:["**The gotcha**: cancellation is cooperative — it's only *checked* at suspension points (`emit`, `delay`, other suspend calls). A flow whose producer runs a tight **CPU loop without emitting or suspending** won't notice cancellation and will run to completion even after the collector is gone. Example: `flow { var x = 0L; while (true) { x++ } }` ignores cancellation.","**The fix**: insert cooperative checks — call `currentCoroutineContext().ensureActive()` (throws if cancelled) or check `isActive` in the loop, or use the `.cancellable()` operator which inserts a cancellation check on every emission (useful for flows like `(1..1_000_000).asFlow()` that emit rapidly without other suspension points).","**Related correctness rule**: never `catch` a `CancellationException` and swallow it inside flow operators or `collect` blocks — that breaks cancellation (the coroutine thinks it recovered). Rethrow it, or catch specific exceptions only. The `catch` operator is already cancellation-aware and won't swallow it, but a hand-written `try/catch (e: Exception)` around a suspend call will — a classic bug."]}]}],NS={id:"basics",title:"Flow Basics & Cold Flows",tagline:"What a Flow is, cold vs hot, builders (flow/callbackFlow/channelFlow), terminal operators, sequential emission, and cancellation.",tags:["Flow","Cold Flow","collect","Coroutines","Cancellation"],content:US,qa:FS},qS=[{heading:"Operators are lazy transformations that return new flows",blocks:[{t:"p",text:"An **intermediate operator** takes a flow and returns a *new* flow with some transformation applied. They're **lazy** — calling `map { }` runs nothing; it just wraps the upstream flow in a new one that will apply the transformation *when collected*. You chain them into a pipeline, and only a terminal operator (`collect`) makes values flow through. Each operator runs in the collector's coroutine (unless `flowOn` intervenes), and processes values one at a time as they pass through."},{t:"code",title:"A pipeline reads top-to-bottom as values flow through",code:`repository.observeArticles()      // Flow<List<Article>>
    .map { it.filter(Article::isPublished) }   // transform each emission
    .filter { it.isNotEmpty() }                // drop empties
    .onEach { log("emitting \${it.size} articles") } // peek (side effect)
    .catch { emit(emptyList()) }               // handle upstream errors
    .collect { render(it) }                    // terminal: starts everything`}]},{heading:"Transforming operators: map, filter, transform, and friends",blocks:[{t:"list",items:["**`map { }`** — transform each value into exactly one new value (`Flow<A>` → `Flow<B>`).","**`filter { }`** / **`filterNot`** / **`filterNotNull`** / **`filterIsInstance<T>()`** — keep only values matching a predicate/type.","**`transform { }`** — the general operator that `map`/`filter` are built on: for each upstream value you can `emit` **zero, one, or many** downstream values. Use when the 1:1 shape of `map` doesn't fit.","**`onEach { }`** — perform a side effect per value without changing it (logging, triggering something); returns the same values. Great for debugging a pipeline.","**`withIndex()` / `runningReduce { }` / `scan(initial) { }`** — index values, or emit a running accumulation (scan emits the initial value then each intermediate fold — perfect for accumulating state)."]},{t:"code",title:"transform and scan",code:`// transform: expand each value into several
flowOf(1, 2, 3).transform { n ->
    emit("start $n"); emit("end $n")
}   // start 1, end 1, start 2, end 2, ...

// scan: emit a running total (initial, then each step)
flowOf(1, 2, 3).scan(0) { acc, n -> acc + n }   // 0, 1, 3, 6`}]},{heading:"Size/time-limiting operators",blocks:[{t:"list",items:["**`take(n)`** — take the first n values then cancel upstream. **`takeWhile { }`** — until the predicate fails. **`drop(n)` / `dropWhile { }`** — skip values.","**`debounce(ms)`** — emit a value only after `ms` of silence; each new value resets the timer. The search-box classic: wait until the user stops typing. Drops intermediate rapid values.","**`sample(ms)`** — emit the *latest* value every `ms` (a periodic snapshot). Debounce waits for a pause; sample takes regular snapshots regardless of pauses.","**`distinctUntilChanged()`** — suppress consecutive duplicates (only emit when the value differs from the previous). Prevents redundant downstream work when the same value repeats.","**`distinctUntilChangedBy { key }`** — same, comparing by a derived key."]},{t:"code",title:"The canonical search pipeline",code:`queryFlow
    .debounce(300)                 // wait for typing to pause
    .distinctUntilChanged()        // ignore no-op changes
    .filter { it.length >= 2 }     // don't search 1 char
    .flatMapLatest { q -> repository.search(q) }  // cancel old search
    .collect { render(it) }`}]},{heading:"Combining flows: zip, combine, merge",blocks:[{t:"p",text:"Three ways to bring multiple flows together — the differences are heavily tested because they behave subtly differently:"},{t:"table",headers:["Operator","Emits when","Pairing behavior"],rows:[["**`combine`**","ANY input emits (after all have emitted once)","latest value of each — recomputes on every change"],["**`zip`**","each input has a NEW value to pair","strict 1:1 pairing, waits for both; stops at the shorter"],["**`merge`**","any input emits","interleaves values from all into one stream (no pairing)"]]},{t:"code",title:"combine vs zip",code:`// combine: recompute UI state whenever ANY source changes
combine(userFlow, settingsFlow, cartFlow) { user, settings, cart ->
    HomeUiState(user, settings, cart.itemCount)
}.collect { render(it) }

// zip: pair values 1:1 (e.g. request with its matching response index)
flowOf(1, 2, 3).zip(flowOf("a", "b", "c")) { n, s -> "$n$s" }
// -> 1a, 2b, 3c`},{t:"list",items:["**`combine`** is the one you use most in Android — it's how you build a single `UiState` from several independent streams (user + settings + cart), recomputing whenever *any* changes. Note: it only starts emitting after *every* input has emitted at least once (give each an initial value if you need earlier output).","**`zip`** is for strict lockstep pairing — less common in UI, useful when two streams' values correspond positionally.","**`merge`** flattens several flows of the same type into one interleaved stream (e.g. merge events from several sources)."]}]},{heading:"Flattening operators: flatMapConcat, flatMapMerge, flatMapLatest",blocks:[{t:"p",text:"Sometimes each value in a flow *produces another flow* (a userId → a flow of that user's posts). You then have a `Flow<Flow<T>>` that must be flattened into `Flow<T>`. The three flatMap operators differ in **how they handle a new inner flow arriving while a previous one is still active** — the single most important distinction in this topic:"},{t:"table",headers:["Operator","When a new value arrives while inner flow is running","Use for"],rows:[["**`flatMapConcat`**","queues — waits for the current inner flow to finish first","order matters, process sequentially"],["**`flatMapMerge`**","runs concurrently — both inner flows active at once","parallel work, order doesn't matter"],["**`flatMapLatest`**","cancels the current inner flow, starts the new one","only the latest matters (search, latest-selection)"]]},{t:"code",title:"flatMapLatest — cancel stale work",code:`selectedUserId
    .flatMapLatest { id ->
        // if the user selects a different id, the in-flight
        // observation for the old id is CANCELLED
        repository.observeUserPosts(id)
    }
    .collect { render(it) }`},{t:"list",items:["**`flatMapLatest`** is the flow-pipeline equivalent of `collectLatest` — the go-to for search-as-you-type and 'react to the latest selection', because it cancels the previous inner flow (killing the stale-result race).","**`flatMapConcat`** preserves order and runs one inner flow at a time — use when sequencing matters (process each item's sub-stream fully before the next).","**`flatMapMerge`** runs inner flows concurrently (with a `concurrency` limit) — for parallel fan-out where order is irrelevant. Beware unbounded concurrency on large upstreams.","Mnemonic: **Concat = queue, Merge = parallel, Latest = cancel-and-replace.**"]}]},{heading:"Converting to and from other types",blocks:[{t:"list",items:["**`flow.asLiveData()`** — bridge a Flow to LiveData for legacy code (lifecycle-aware). Rarely needed in new code.","**`liveData.asFlow()`**, **`Observable.asFlow()`** (RxJava) — bring legacy reactive types into the Flow world during migration.","**`callbackFlow`/`channelFlow`** — from callbacks to flows (covered in the callbackFlow topic).","**`stateIn` / `shareIn`** — cold Flow → hot StateFlow/SharedFlow (covered in the hot flows topic).","**`flow.first()` / `flow.toList()`** — collapse a flow to a single value / a list (terminal)."]},{t:"note",text:"The three questions this topic must let you answer cold: (1) map vs transform (1:1 vs 0..n); (2) combine vs zip (latest-of-each on any change vs strict 1:1 pairing); (3) flatMapConcat vs Merge vs Latest (queue vs parallel vs cancel). Those three comparisons cover the majority of Flow-operator interview questions."}]}],WS=[{level:"junior",q:"What is the difference between map and transform?",a:[{t:"p",text:"**The concept**: both transform values flowing through a pipeline, but they differ in how many values they can produce per input. `map { }` is strictly **one-to-one** — each upstream value becomes exactly one downstream value. `transform { }` is the general form: for each upstream value you can call `emit` **zero, one, or many** times, producing any number of downstream values."},{t:"code",title:"map is 1:1, transform is 1:many (or 1:0)",code:`flowOf(1, 2).map { it * 10 }          // 10, 20  (one out per one in)

flowOf(1, 2).transform { n ->
    emit(n)                              // pass through
    emit(n * 10)                         // AND an extra value
}                                        // 1, 10, 2, 20`},{t:"p",text:"In fact `map` and `filter` are both implemented on top of `transform` — `map` just emits once, `filter` emits zero-or-one based on the predicate. You reach for `transform` directly only when the 1:1 shape doesn't fit: emitting a loading placeholder before a value, expanding one value into several, or conditionally emitting a variable number. For everyday 'change each value', `map` is clearer."}]},{level:"junior",q:"What does debounce do, and how is it different from sample?",a:[{t:"p",text:"**`debounce(ms)`** emits a value only after the flow has been *silent* for `ms` — every new value resets the timer. It's the 'wait until they stop' operator: in a search box, `debounce(300)` means you only search after the user pauses typing for 300ms, so you don't fire a request per keystroke."},{t:"p",text:"**`sample(ms)`** is different: it emits the *most recent* value on a fixed periodic tick, every `ms`, regardless of whether emissions are ongoing. Debounce reacts to *pauses*; sample takes *regular snapshots*. Example contrast: for a fast-moving sensor emitting continuously, `debounce` might emit *nothing* (there's never a pause) while `sample(1000)` gives you the latest reading once a second. Rule of thumb: use `debounce` to wait out bursts of activity (typing, rapid clicks); use `sample` to throttle a continuous stream to a manageable rate."}]},{level:"junior",q:"What does distinctUntilChanged do and why is it useful?",a:[{t:"p",text:"**The concept**: `distinctUntilChanged()` suppresses *consecutive* duplicate values — it only lets a value through if it's different from the one immediately before it (by `equals`). Note 'consecutive': `1,1,2,2,1` becomes `1,2,1` (the final 1 passes because the value right before it was 2). It's not global deduplication."},{t:"p",text:"**Why it's useful**: it prevents redundant downstream work when a source re-emits the same value. A common case: a flow derived with `map` might produce the same result for several different upstream values (e.g. mapping user objects to just their `isPremium` flag), and you don't want to re-render or re-query when nothing meaningful changed. `distinctUntilChangedBy { it.id }` compares by a key instead of the whole object. Worth knowing: `StateFlow` already has this behavior built in (it never emits a value equal to the current one), so you add `distinctUntilChanged` mainly on plain cold flows or after a `map` that collapses distinctions."}]},{level:"junior",q:"How do you combine two flows into one? When would you use combine?",a:[{t:"p",text:"**The concept**: `combine` merges multiple flows by taking the *latest value of each* and running your lambda whenever *any* of them emits. It's how you compose one derived value out of several independent streams. The most common Android use is building a single `UiState` from separate sources:"},{t:"code",title:"combine to build UI state from several streams",code:`combine(
    userRepository.observeUser(),
    settingsRepository.observeSettings(),
    cartRepository.observeCart(),
) { user, settings, cart ->
    HomeUiState(name = user.name, darkMode = settings.dark, cartCount = cart.size)
}.collect { render(it) }`},{t:"p",text:"So whenever the user, settings, *or* cart changes, `combine` recomputes `HomeUiState` with the latest of all three. Two things to remember: it only starts emitting once *every* input has emitted at least once (so give sources an initial value if you need output before all have fired), and it always uses the newest value of each — it doesn't pair them up. That last point is what distinguishes it from `zip`, which pairs values one-to-one and waits for a fresh value from *both* before emitting."}]},{level:"junior",q:"What does onEach do, and how is it different from map?",a:[{t:"p",text:"**`onEach { }`** runs a side effect for each value and passes the value through **unchanged** — the flow's type and values are identical before and after. **`map { }`** *transforms* each value into a (possibly different) new value, changing the stream. So `onEach` is for doing something *with* each value (logging, analytics, triggering an update) without altering the stream, while `map` is for *producing* new values."},{t:"p",text:"Two practical notes: `onEach` is excellent for debugging a pipeline — drop `.onEach { println(it) }` anywhere to peek at values at that stage without disturbing anything. And `onEach` combined with `launchIn(scope)` is a very common idiom: `flow.onEach { updateState(it) }.launchIn(viewModelScope)` starts collecting in a scope and handles each value, equivalent to `viewModelScope.launch { flow.collect { updateState(it) } }` but flatter."}]},{level:"senior",q:"Explain flatMapConcat, flatMapMerge, and flatMapLatest. When do you use each?",a:[{t:"p",text:"**The setup**: you use a flatMap operator when each value in a flow maps to *another flow* (a userId → a flow of that user's data), producing a `Flow<Flow<T>>` you need flattened to `Flow<T>`. The three variants differ entirely in **what happens when a new value arrives while the previous inner flow is still running** — that's the whole distinction."},{t:"list",items:["**`flatMapConcat`** — *sequential/queued*. It fully collects the current inner flow before starting the next. New upstream values wait their turn. Use when **order matters** and inner flows must not overlap — e.g. processing a queue of operations where each must complete before the next.","**`flatMapMerge`** — *concurrent*. It starts inner flows as values arrive and runs them **simultaneously**, interleaving their emissions (with an optional `concurrency` limit). Use for **parallel fan-out where order is irrelevant** — e.g. kicking off several independent fetches at once. Watch out: unbounded concurrency over a large upstream can launch too many parallel operations.","**`flatMapLatest`** — *cancel-and-replace*. When a new value arrives, it **cancels the current inner flow** and starts a fresh one for the new value. Use when **only the latest matters** — search-as-you-type, reacting to the latest selected item. It kills the stale-result race where an old, slow inner flow finishes after a newer one and overwrites correct data."]},{t:"code",title:"The definitive example — search",code:`queryFlow
    .debounce(300)
    .flatMapLatest { query ->            // old search cancelled on new query
        repository.search(query)         // returns Flow<Results>
    }
    .collect { showResults(it) }`},{t:"p",text:"**Mnemonic**: Concat = queue (one at a time, ordered), Merge = parallel (all at once, unordered), Latest = cancel-and-replace (only the newest). `flatMapLatest` is by far the most common in UI code; `flatMapConcat` when sequencing is required; `flatMapMerge` for deliberate parallelism."}]},{level:"senior",q:"Deep-dive combine vs zip vs merge — the exact semantics and a pitfall of each.",a:[{t:"list",items:["**`combine`** — emits whenever *any* input emits, using the *latest* value of *every* input. It waits until all inputs have emitted at least once before the first emission. **Pitfall**: because it uses latest-of-each, rapid emissions on one input can produce combinations you didn't 'intend' pairing-wise, and if one input never emits, `combine` never emits at all (a flow that's slow to produce its first value blocks all output — give it an initial/`onStart` value). It's the right tool for deriving UI state, wrong for strict pairing.","**`zip`** — emits only when it can form a *new pair*: it takes the next value from each input and pairs them 1:1, waiting for both. **Pitfall**: it advances in lockstep and **completes when the shortest flow completes**, so mismatched-length or mismatched-rate flows leave values unpaired and dropped. It's for positional correspondence (value #n of A with value #n of B), not for reacting to whichever changed.","**`merge`** — takes several flows of the *same type* and **interleaves** all their values into one stream, no pairing, no transformation, emitting each value as it arrives from any source. **Pitfall**: you lose track of which source a value came from (wrap values in a sealed type if you need to know), and a fast source can dominate the merged stream."]},{t:"p",text:"**The one-line differentiator**: `combine` = 'latest of each, recompute on any change' (for derived state); `zip` = 'strict 1:1 pairing, waits for both, stops at shorter' (for positional pairing); `merge` = 'flatten many into one interleaved stream' (for pooling same-typed events). The interview trap is using `combine` when you meant `zip` (getting extra recompositions) or `zip` when you meant `combine` (getting stuck waiting for a slow input)."}]},{level:"senior",q:"Build a debounced, cancel-stale search pipeline with flow operators and explain each operator's role.",a:[{t:"code",title:"The production-grade search pipeline",code:`val results: StateFlow<SearchUiState> = queryFlow
    .debounce(300)                          // 1
    .distinctUntilChanged()                 // 2
    .flatMapLatest { query ->               // 3
        if (query.length < 2) flowOf(SearchUiState.Empty)
        else repository.search(query)       // returns Flow<Results>
            .map { SearchUiState.Results(it) }
            .onStart { emit(SearchUiState.Loading) }   // 4
            .catch { emit(SearchUiState.Error(it.message)) } // 5
    }
    .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), SearchUiState.Empty) // 6`},{t:"list",items:["**1 — `debounce(300)`**: waits until the user pauses typing, so you issue at most one search per burst of keystrokes instead of one per letter. The single biggest reduction in wasted requests.","**2 — `distinctUntilChanged`**: if the debounced query equals the last one searched (e.g. they typed and deleted a character), skip re-searching — no redundant work.","**3 — `flatMapLatest`**: the crux. When a new query arrives, it **cancels the in-flight search** for the old query. This eliminates the stale-result race (a slow old response arriving after a newer one) *and* saves the abandoned request's remaining work.","**4 — `onStart { emit(Loading) }`**: emits a loading state as each new search begins, so the UI shows a spinner immediately.","**5 — `catch` inside the inner flow**: placing `catch` on the *inner* flow means an error in one search maps to an error state without tearing down the whole outer pipeline — the next keystroke can still search. A `catch` on the outer flow would end everything permanently after the first error.","**6 — `stateIn(WhileSubscribed(5000))`**: converts the cold pipeline into a hot `StateFlow` the UI observes, keeping it alive across configuration changes (the 5s window) but stopping the upstream when the screen is truly gone."]},{t:"p",text:"The senior signal is (a) reaching for `flatMapLatest` specifically for cancellation, (b) placing `catch` on the *inner* flow so errors are recoverable, and (c) knowing why `debounce` + `distinctUntilChanged` come *before* the flatMap (filter the query stream before spending a request). Each operator maps to a concrete failure it prevents — wasted requests, stale results, unrecoverable errors, lost state on rotation."}]}],zS={id:"operators",title:"Operators & Transformations",tagline:"map/transform, debounce/sample/distinctUntilChanged, combine vs zip vs merge, flatMapConcat/Merge/Latest, and building real pipelines.",tags:["Flow","Operators","combine","flatMapLatest","debounce"],content:qS,qa:WS},BS=[{heading:"Hot flows — the opposite of cold",blocks:[{t:"p",text:"`StateFlow` and `SharedFlow` are **hot** flows. Recall cold flows run per-collector and only while collected. Hot flows are the opposite: they **exist independently of collectors** and **multicast** the same emissions to all current collectors. Emitting into a hot flow happens whether or not anyone is listening; collectors 'tune in' to a live broadcast rather than triggering their own private run."},{t:"list",items:["**One shared stream, many collectors**: ten collectors of a `StateFlow` all receive the same emissions from the one underlying stream — not ten independent executions.","**Emission is decoupled from collection**: you push values in (`_state.value = x`, `emit(event)`) from anywhere; collectors receive them if they're active.","**They don't complete on their own**: a `StateFlow`/`SharedFlow` never finishes (there's always potentially another value), so collecting one suspends forever until the collecting coroutine is cancelled.","**Use case**: representing *shared state* (UI state everyone observes) or *broadcasting events* to multiple listeners — things that conceptually exist regardless of who's watching."]}]},{heading:"StateFlow — a hot flow that always has a current value",blocks:[{t:"p",text:"**`StateFlow<T>`** is a hot flow specialized for **state**: it always holds exactly one *current value*, and new collectors immediately receive that current value, then every subsequent update. It's essentially an observable value holder — the modern replacement for `LiveData`."},{t:"code",title:"The canonical ViewModel pattern",code:`class ProfileViewModel : ViewModel() {
    private val _uiState = MutableStateFlow(ProfileUiState.Loading) // requires initial value
    val uiState: StateFlow<ProfileUiState> = _uiState.asStateFlow()  // read-only exposure

    fun load() {
        viewModelScope.launch {
            val user = repository.getUser()
            _uiState.update { it.copy(user = user, isLoading = false) } // atomic update
        }
    }
}

// Reading the current value synchronously (no collection):
val current = uiState.value`},{t:"list",items:["**Requires an initial value** — there's always a 'current' state, so `MutableStateFlow(initial)` must be given one. (`SharedFlow` has no initial value.)","**`.value`** reads/writes the current value synchronously — you can read state without collecting.","**Conflated + distinct**: `StateFlow` only emits when the new value is *different* (`!=`) from the current, and a slow collector only sees the *latest* value, not every intermediate one. It's about *state*, not every event.","**Always update immutably**: replace with `copy()` via `update { }` (atomic compare-and-set, safe against concurrent writers) rather than mutating the held object — mutating in place won't emit (same reference).","**Backing-property pattern**: private `MutableStateFlow`, public `StateFlow` via `asStateFlow()` — so only the ViewModel writes (single source of truth, UDF)."]}]},{heading:"SharedFlow — a configurable hot broadcast",blocks:[{t:"p",text:"**`SharedFlow<T>`** is the general hot flow: a broadcaster with no notion of a single 'current value', configurable via **replay**, **buffer**, and **overflow** policy. `StateFlow` is actually a specialized `SharedFlow` (replay=1, conflated, with an initial value)."},{t:"code",title:"Configuring a SharedFlow",code:`private val _events = MutableSharedFlow<UiEvent>(
    replay = 0,                                  // how many past values new collectors get
    extraBufferCapacity = 64,                    // buffer beyond replay
    onBufferOverflow = BufferOverflow.DROP_OLDEST, // what to do when buffer full
)
val events: SharedFlow<UiEvent> = _events.asSharedFlow()

suspend fun sendEvent(e: UiEvent) = _events.emit(e)   // suspends if buffer full (SUSPEND)
fun tryEvent(e: UiEvent) = _events.tryEmit(e)         // non-suspending; may drop`},{t:"list",items:["**`replay`** — how many of the most recent values a *new* collector receives on subscribing. `replay = 0` means new collectors only get values emitted *after* they subscribe (good for events); `replay = 1` mimics StateFlow-ish behavior (last value re-delivered).","**`extraBufferCapacity`** — buffer slots beyond replay, so `emit` doesn't suspend as long as there's room.","**`onBufferOverflow`** — `SUSPEND` (default; producer waits), `DROP_OLDEST`, or `DROP_LATEST` when the buffer is full.","**`emit` vs `tryEmit`**: `emit` suspends when the buffer is full (with SUSPEND policy); `tryEmit` never suspends and returns `false` if it couldn't buffer the value — useful in non-suspend contexts, but can silently drop."]}]},{heading:"StateFlow vs SharedFlow — the comparison",blocks:[{t:"table",headers:["","StateFlow","SharedFlow"],rows:[["Initial value","required","none"],["Current value","yes (`.value`)","no"],["Replay to new collectors","always 1 (the current value)","configurable (0..n)"],["Conflation / distinct","yes — skips equal values, keeps latest","no (configurable buffering)"],["Best for","state (UI state, a value over time)","events (one-shot signals, broadcasts)"],["Analogy","an observable variable","an event bus / broadcast"]]},{t:"list",items:["**Rule of thumb**: **state → StateFlow, events → SharedFlow** (with `replay = 0`). State has a 'current value' that a new observer should immediately see; events are transient and shouldn't replay to late subscribers.","**Why not use StateFlow for events**: its conflation and distinct-until-changed drop values — two identical events collapse to one, and a slow collector misses intermediates. Fine for state (you only care about *now*), wrong for events (each must be delivered).","**Why not use SharedFlow for state**: it has no current value, so a new collector sees nothing until the next emission (unless you set `replay = 1`, at which point you've basically rebuilt a worse StateFlow). StateFlow is purpose-built for the state case."]}]},{heading:"The event-delivery problem SharedFlow doesn't fully solve",blocks:[{t:"p",text:"A common trap: using `SharedFlow(replay = 0)` for one-shot events (navigation, snackbars) *seems* right, but **values emitted while no collector is active are dropped**. During a configuration change the UI's collector is briefly gone — an event emitted in that window vanishes. `tryEmit` with a full/zero buffer also silently fails."},{t:"list",items:["**Mitigations**: give `extraBufferCapacity` so `emit` from a non-suspend context buffers instead of dropping; or use a **`Channel` + `receiveAsFlow()`**, where `send` *suspends until delivered* so events survive collection gaps (the common pragmatic choice).","**Google's recommendation**: model even one-shot events as *state* with a consumption callback (a nullable field cleared after handling) — survives config change and process death, at the cost of some ceremony.","This is the same 'one-shot event problem' covered in the Architecture (MVVM/MVI) topics — SharedFlow is one tool with real delivery caveats, not a complete solution."]},{t:"note",text:'Interview-ready summary: "StateFlow for state (always has a value, conflated, distinct); SharedFlow for events (configurable replay/buffer, no current value). But raw SharedFlow can drop events across lifecycle gaps, so for critical one-shot events I use a Channel or model them as consumable state."'}]},{heading:"StateFlow vs LiveData — the migration question",blocks:[{t:"table",headers:["","LiveData","StateFlow"],rows:[["Platform","Android-only","pure Kotlin (KMP-friendly)"],["Lifecycle awareness","built-in","needs `repeatOnLifecycle` / `collectAsStateWithLifecycle`"],["Initial value","optional (nullable)","required (non-null by default)"],["Operators","few (`map`, `switchMap`)","full Flow operator set"],["Threading / async","manual","coroutine-native"]]},{t:"list",items:["StateFlow is the modern default; LiveData is in maintenance mode. The decisive factor for an Android/KMP engineer is **KMP**: LiveData can't move to `commonMain`, StateFlow can.","The tradeoff StateFlow introduces: it's *not* lifecycle-aware by itself, so you must collect it safely (`collectAsStateWithLifecycle` in Compose, `repeatOnLifecycle` in Views) — otherwise you keep collecting in the background. LiveData handled that automatically."]}]}],HS=[{level:"junior",q:"What is the difference between a cold flow and a hot flow like StateFlow?",a:[{t:"p",text:"**The concept**: a *cold* flow (plain `Flow` from `flow { }`) runs its producer separately for each collector, starting only when collected and stopping when collection ends — every collector gets a private, independent run. A *hot* flow (`StateFlow`, `SharedFlow`) exists on its own and **broadcasts** the same emissions to all current collectors; it emits whether or not anyone is listening, and collectors share one underlying stream."},{t:"p",text:"**Concrete difference**: collect a cold repository flow twice → two network calls. Collect a `StateFlow` twice → both collectors read from the same single value, no duplication. Also, a hot flow like `StateFlow` never completes (there's always potentially a next value), so collecting it suspends until the collecting coroutine is cancelled, whereas a cold flow of finite values completes on its own. Hot flows are for *shared state and events*; cold flows are for *per-consumer, lazy work*."}]},{level:"junior",q:"What is StateFlow and how do you use it in a ViewModel?",a:[{t:"p",text:"**The concept**: `StateFlow<T>` is a hot flow that always holds one *current value* and emits it (plus every update) to collectors. It's an observable state holder — the modern replacement for `LiveData`. The standard ViewModel pattern uses the backing-property idiom: a private `MutableStateFlow` you write to, exposed publicly as a read-only `StateFlow`."},{t:"code",title:"The pattern",code:`private val _uiState = MutableStateFlow(UiState())     // initial value required
val uiState: StateFlow<UiState> = _uiState.asStateFlow()

fun onNameChange(name: String) {
    _uiState.update { it.copy(name = name) }          // immutable, atomic update
}`},{t:"list",items:["**Initial value required** — there's always a current state; `MutableStateFlow(initial)`.","**Read `.value` anytime** — synchronous access to the current state without collecting.","**Only the ViewModel writes** — exposing the read-only `StateFlow` enforces single-source-of-truth / unidirectional data flow; the UI can only observe.","**Update immutably with `update { }`** — replace via `copy()`; `update` is an atomic compare-and-set safe against concurrent writers (unlike `value = value.copy()` which can lose writes under concurrency).","**Collect safely in UI** — `collectAsStateWithLifecycle()` in Compose or `repeatOnLifecycle` in Views (StateFlow isn't lifecycle-aware itself)."]}]},{level:"junior",q:"When should you use StateFlow vs SharedFlow?",a:[{t:"p",text:"**The rule**: **StateFlow for state, SharedFlow for events.** StateFlow always has a *current value* that a new observer should immediately see — perfect for UI state, a selected item, a toggle, anything that has a 'value right now'. SharedFlow has *no* current value and is a configurable broadcast — perfect for *events* that happen and are gone: navigation commands, snackbar triggers, one-shot signals."},{t:"list",items:["**Why not StateFlow for events**: StateFlow conflates and drops duplicates — two identical events (`ShowError` twice) collapse into one emission, and a slow collector only sees the latest. For events, every occurrence must be delivered, so that behavior is wrong.","**Why not SharedFlow for state**: SharedFlow has no 'current value', so a fresh collector sees nothing until the next emission — a screen that subscribes late would show no state. StateFlow is purpose-built to give new collectors the current value immediately.","**Practical setup**: `MutableStateFlow(initialState)` for UI state; `MutableSharedFlow<Event>(replay = 0)` (often with `extraBufferCapacity`) for events."]},{t:"p",text:"Mental model: StateFlow is an *observable variable*; SharedFlow is an *event bus*. Match the tool to whether the thing has a persistent 'current value' or is a transient occurrence."}]},{level:"junior",q:"Why does StateFlow require an initial value but SharedFlow doesn't?",a:[{t:"p",text:"**Because of what each represents.** `StateFlow` models *state* — a value that always exists 'right now'. There's no such thing as state with no value, so it must be initialized, and `.value` can always return something. That's also why a new collector instantly receives the current value: there's always one to give."},{t:"p",text:"`SharedFlow` models a *stream of events/broadcasts*, which has no inherent 'current value' — before the first event, there simply is nothing, and that's valid. A new collector, by default (`replay = 0`), receives only events emitted *after* it subscribes. If you *want* SharedFlow to replay the last value to new collectors you set `replay = 1` — and at that point you've essentially reconstructed StateFlow-like behavior (which is literally how StateFlow is implemented: a specialized SharedFlow with replay 1, conflation, and an initial value). The initial-value requirement is the API encoding the semantic difference: state always has a value, events don't."}]},{level:"junior",q:"How is StateFlow different from LiveData?",a:[{t:"list",items:["**Platform**: LiveData is Android-only; StateFlow is pure Kotlin, so it works in KMP `commonMain`. For an Android/KMP engineer this is the decisive difference — shared ViewModels need StateFlow.","**Lifecycle awareness**: LiveData is lifecycle-aware out of the box (observers auto-pause/resume). StateFlow is *not* — you must collect it safely with `collectAsStateWithLifecycle` (Compose) or `repeatOnLifecycle` (Views), or it keeps collecting in the background.","**Initial value**: LiveData can start empty (null); StateFlow requires an initial value (and is non-null-friendly).","**Operators & async**: StateFlow has the full Flow operator set and native coroutine integration; LiveData has only a few transformations and awkward async."]},{t:"p",text:"**Bottom line**: StateFlow is the modern default and LiveData is in maintenance mode. The one thing StateFlow *costs* you is automatic lifecycle handling — you opt back into it with the lifecycle-aware collectors. Migrating LiveData→StateFlow is usually mechanical, and worth it especially if any code might be shared with KMP."}]},{level:"senior",q:"Explain replay, extraBufferCapacity, and onBufferOverflow in MutableSharedFlow.",a:[{t:"p",text:"These three parameters define a SharedFlow's buffering behavior — how it handles values relative to collectors that may be absent or slow."},{t:"list",items:["**`replay`** — how many of the most recent values a *newly subscribing* collector immediately receives. `replay = 0`: new collectors get only future emissions (correct for events — you don't want a late subscriber replaying an old navigation command). `replay = 1`: the last value is re-delivered to new collectors (StateFlow-like caching). Replayed values are also held in the buffer.","**`extraBufferCapacity`** — buffer slots *in addition to* replay. Their purpose: let the producer `emit` (or `tryEmit`) *without suspending* as long as buffer space remains. With `extraBufferCapacity > 0` and the default SUSPEND policy, a fast producer can get ahead of a slow collector up to the buffer size before it has to wait.","**`onBufferOverflow`** — what happens when the buffer (replay + extra) is full and a new value arrives: `SUSPEND` (default — the emitting coroutine suspends until space frees up, applying backpressure), `DROP_OLDEST` (evict the oldest buffered value, keep flowing — good for 'latest wins' like sensor data), or `DROP_LATEST` (discard the incoming value, keep what's buffered)."]},{t:"p",text:"**How they interact with emit/tryEmit**: `emit` respects SUSPEND (waits when full); `tryEmit` never suspends and returns `false` if it couldn't place the value (which, with a full buffer and SUSPEND policy, means the value is *dropped*). A subtle senior point: `tryEmit` can *always* succeed if `onBufferOverflow` is a DROP policy (it evicts to make room), so pairing `tryEmit` with `DROP_OLDEST` gives a non-suspending, never-failing emitter that keeps the newest values — a common configuration for high-frequency non-critical streams."}]},{level:"senior",q:"You used SharedFlow(replay=0) for navigation events but some events are getting lost. Why, and how do you fix it?",a:[{t:"p",text:"**Why events are lost**: a `SharedFlow` broadcasts to *currently active* collectors only. With `replay = 0` and no buffer, a value emitted while **no collector is active is dropped entirely** — nobody was subscribed to receive it, and there's no replay to catch up. This bites hardest around lifecycle transitions: during a configuration change (rotation) the UI's collector is torn down and re-created, and any event emitted in that gap vanishes. If you emit with `tryEmit` and there's no buffer space, it also silently fails (returns false)."},{t:"list",items:["**Fix 1 — buffer so emissions survive the gap**: `MutableSharedFlow(replay = 0, extraBufferCapacity = 1, onBufferOverflow = SUSPEND)`. Now an event emitted with `tryEmit` while the collector is briefly gone is *buffered* and delivered when collection resumes. Cheap and often sufficient.","**Fix 2 — use a `Channel` + `receiveAsFlow()`**: a `Channel(BUFFERED)` whose `send` *suspends until the value is actually received*. Events wait through collection gaps instead of being dropped — the common pragmatic choice for one-shot events. Caveat: a Channel fans out to a *single* collector (each event goes to exactly one), which is usually what you want for navigation but not for broadcasting to many listeners.","**Fix 3 — model events as consumable state (Google's recommendation)**: put the event in your `StateFlow` UI state as a nullable field, have the UI perform it and call back `onEventConsumed()` to clear it. This survives configuration change *and* process death (if in SavedStateHandle), with no delivery-gap ambiguity. More ceremony, most robust."]},{t:"p",text:"**The senior framing**: SharedFlow is a *hot broadcast*, and hot broadcasts inherently can't deliver to absent listeners — so it's the wrong primitive for 'must happen exactly once' events unless you add buffering. Name the lifecycle-gap mechanism as the root cause, then pick the fix by delivery guarantee needed: buffer (good enough), Channel (suspends-until-delivered), or consumable state (survives process death)."}]},{level:"senior",q:"How does StateFlow's conflation and distinct-until-changed behavior affect what you can use it for?",a:[{t:"p",text:"**Two behaviors, both intentional for state:** (1) **distinct** — StateFlow never emits a value that `equals` the current one, so setting the same value twice emits once. (2) **conflation** — a slow collector only ever sees the *latest* value; intermediate values it was too slow to process are skipped, never queued."},{t:"list",items:["**Why this is correct for state**: a UI only cares about the *current* state. If state changes A→B→C faster than the UI can render, rendering only C is exactly right — rendering A and B first would be wasted frames showing stale data. And re-emitting an equal state would cause needless recomposition. So conflation + distinct make StateFlow efficient for its purpose.","**Where it makes StateFlow the WRONG tool**: anything where *every emission matters*. Analytics that must log each step (conflation drops steps under load). Animations queued per value (skipped intermediates break them). Events (two identical events collapse to one; a missed intermediate is a missed event). For these, use `SharedFlow` (buffered, no conflation) or a `Channel`.","**A concrete gotcha**: mutating a mutable object held in StateFlow and re-assigning it won't emit if `equals`/reference is unchanged — and even a `copy()` producing an equal value won't emit. If you *need* a re-emission of an 'equal' value (rare — usually a design smell), StateFlow can't express it; that's a sign the thing is an *event*, not state."]},{t:"p",text:"**The principle**: StateFlow is optimized for 'what is the value now', trading away 'every individual change'. Choose it when you only care about the latest state; choose SharedFlow/Channel when you must observe each discrete emission."}]}],_S={id:"stateflow-sharedflow",title:"StateFlow & SharedFlow",tagline:"Hot flows, StateFlow as observable state, SharedFlow as configurable broadcast, replay/buffer/overflow, the event-delivery problem, and StateFlow vs LiveData.",tags:["StateFlow","SharedFlow","Hot Flow","Events","LiveData"],content:BS,qa:HS},KS=[{heading:"Backpressure — what happens when the producer is faster than the collector",blocks:[{t:"p",text:"**Backpressure** is the problem of a producer emitting values faster than the collector can process them. In RxJava this needed special types (`Flowable`) and strategies. Kotlin Flow solves it *by default and for free* through **suspension**: because `emit` is a suspend function and flows are sequential, when the collector is busy, `emit` simply **suspends the producer** until the collector is ready for the next value. No buffer overflows, no dropped values, no special API — the producer naturally slows to the collector's pace."},{t:"code",title:"Default: producer waits for the collector",code:`flow {
    for (i in 1..3) {
        println("emitting $i")
        emit(i)                 // SUSPENDS here until collector finishes with i
    }
}.collect { value ->
    delay(1000)                 // slow collector
    println("collected $value")
}
// emitting 1 -> (1s) -> collected 1 -> emitting 2 -> ...
// Producer and collector run in lockstep; total ~3s`},{t:"list",items:["Because a flow is **sequential**, producing value N+1 can't start until value N has been fully processed downstream — the producer is automatically throttled to the collector's speed.","This is the safest default: nothing is lost, memory stays bounded (no unbounded queue), and the two sides self-synchronize. The cost is *latency* — the producer idles while the collector works, so total time is roughly producer time + collector time added serially.","When that serial cost hurts, you opt into concurrency between producer and collector with **`buffer`** — trading memory for throughput."]}]},{heading:"buffer() — decouple producer and collector for throughput",blocks:[{t:"p",text:"`buffer(capacity)` inserts a channel between producer and collector so they run **concurrently**: the producer emits into the buffer without waiting for the collector, and the collector drains the buffer at its own pace. This overlaps their work, improving throughput when the producer has idle time (e.g. it does I/O between emissions)."},{t:"code",title:"buffer overlaps production and consumption",code:`flow {
    for (i in 1..3) { delay(100); emit(i) }   // producer: 100ms each
}
.buffer()                                     // run producer & collector concurrently
.collect { delay(300) }                       // collector: 300ms each

// WITHOUT buffer: (100+300)*3 = 1200ms (serial)
// WITH buffer: producer runs ahead while collector works -> ~1000ms`},{t:"list",items:["`buffer()` takes an optional capacity and an `onBufferOverflow` policy (SUSPEND/DROP_OLDEST/DROP_LATEST) — the same knobs as SharedFlow.","The tradeoff is **memory for latency**: you hold buffered values in exchange for the two sides not blocking each other. With an unbounded producer and a slow collector, an unbounded buffer grows without limit — so bound it, or use a drop policy.","`buffer` is also what several other operators are built on — `flowOn`, `conflate`, and `produceIn` all involve a channel between stages."]}]},{heading:"conflate() — keep only the latest, drop intermediates",blocks:[{t:"p",text:"`conflate()` is a buffering strategy that says: **if the collector is slow, skip intermediate values and give it the latest**. When the collector finishes and asks for the next value, it gets the *most recent* one the producer emitted, and everything in between is dropped. It's `buffer(onBufferOverflow = DROP_OLDEST)` with capacity that keeps only the newest."},{t:"code",title:"conflate drops values the collector was too slow to see",code:`flow {
    for (i in 1..100) { delay(10); emit(i) }   // fast producer
}
.conflate()
.collect { value ->
    delay(100)                                  // slow collector
    println(value)                              // prints latest: e.g. 1, 15, 31, ... 100
}`},{t:"list",items:["**When to use**: the collector only cares about the *latest* state, not every value — progress indicators, live sensor/price displays, UI that renders current state. Showing every intermediate would be pointless and wasteful.","**When NOT to use**: every value matters (each must be processed/logged/persisted) — conflation silently loses data. Use `buffer` (keeps all) instead.","This is exactly why `StateFlow` is conflated — state cares only about 'now'. `conflate()` gives any flow that same 'latest-wins' semantics."]}]},{heading:"collectLatest / mapLatest — cancel in-progress work on new values",blocks:[{t:"p",text:"`conflate` drops intermediate values *before* the collector starts processing them. `collectLatest` (and `mapLatest`/`flatMapLatest`) take a different approach: they **start** processing every value, but **cancel** the in-progress processing if a newer value arrives. The difference matters when the per-value work is cancellable and you want the latest value's work to actually run to completion."},{t:"code",title:"conflate vs collectLatest",code:`// conflate: collector processes some values fully, skips others entirely
flow.conflate().collect { process(it) }         // process() always completes

// collectLatest: collector starts every value, cancels if superseded
flow.collectLatest { value ->
    process(value)                              // CANCELLED mid-way if new value arrives
}`},{t:"list",items:["**`conflate`** — never cancels; it just doesn't deliver skipped values. Good when the processing itself isn't cancellable or you want each delivered value fully handled.","**`collectLatest`** — cancels the block for the previous value when a new one arrives; only the latest value's processing survives to completion. Good when processing is expensive/cancellable and stale work should be abandoned (search-as-you-type).","Subtle but real distinction interviewers probe: with `conflate`, an already-started `process()` finishes; with `collectLatest`, it gets cancelled. If `process()` writes to a DB, `conflate` completes the write, `collectLatest` might cancel it half-done — choose accordingly."]}]},{heading:"The decision guide",blocks:[{t:"table",headers:["Situation","Tool","Effect"],rows:[["Default; every value matters, don't overwhelm memory","(nothing — suspension)","producer waits for collector; nothing lost"],["Producer & collector both slow; overlap them","`buffer()`","concurrent; all values kept; uses memory"],["Collector only needs the latest value","`conflate()`","skip intermediates; keep newest"],["Per-value work is cancellable; only latest should complete","`collectLatest` / `flatMapLatest`","cancel in-progress work on new value"],["Bounded buffer with explicit drop policy","`buffer(n, onBufferOverflow = ...)`","control exactly what happens when full"]]},{t:"note",text:'Interview one-liner: "Flow handles backpressure by default through suspension — the producer waits for the collector, so nothing is dropped and memory is bounded. When I need throughput I add `buffer` (keep all, use memory); when I only want the latest I use `conflate` (drop intermediates) or `collectLatest` (cancel in-progress work)."'}]}],JS=[{level:"junior",q:"What is backpressure, and how does Kotlin Flow handle it?",a:[{t:"p",text:"**The concept**: backpressure is what happens when a producer emits values *faster* than the collector can consume them. Without a strategy, values pile up (unbounded memory) or get lost. In RxJava this required special reactive types and explicit strategies."},{t:"p",text:"**How Flow handles it — for free, by default:** through **suspension**. `emit` is a suspend function, and flows are sequential, so producing the next value can't begin until the current value has been fully processed downstream. If the collector is slow, `emit` simply *suspends the producer* until the collector is ready. The producer automatically runs at the collector's pace — nothing is dropped, memory stays bounded, and there's no special API to learn. The only cost is latency: the producer idles while the collector works (their times add up serially). When that serial cost matters, you opt into `buffer` to let them run concurrently."}]},{level:"junior",q:"What does the buffer() operator do?",a:[{t:"p",text:"**The concept**: by default a flow runs the producer and collector in lockstep — the producer waits for the collector to finish each value. `buffer()` inserts a channel between them so they run **concurrently**: the producer emits into the buffer and keeps going without waiting, while the collector drains the buffer at its own speed. This overlaps their work and improves throughput when the producer has idle time between emissions (e.g. it does I/O)."},{t:"code",title:"The speedup",code:`flow { repeat(3) { delay(100); emit(it) } }   // 100ms per item
  .buffer()
  .collect { delay(300) }                     // 300ms per item
// Without buffer: serial -> (100+300)*3 = 1200ms
// With buffer: overlapped -> ~1000ms (producer works ahead)`},{t:"p",text:"The tradeoff is **memory for latency** — you hold buffered values so neither side blocks the other. You can bound the buffer (`buffer(capacity)`) and set an overflow policy (`SUSPEND`, `DROP_OLDEST`, `DROP_LATEST`) for when it fills. Without a bound, a fast producer feeding a slow collector grows the buffer indefinitely, so bound it or use a drop policy in that scenario."}]},{level:"junior",q:"What is the difference between buffer() and conflate()?",a:[{t:"p",text:"**Both let the producer run ahead of the collector, but they handle the backlog differently.** `buffer()` **keeps all** the values — the collector eventually processes every one, just later. `conflate()` **keeps only the latest** — when the collector is slow, intermediate values are dropped, and when it's ready for the next value it gets the most recent one the producer emitted."},{t:"list",items:["Use **`buffer`** when every value matters and you just want throughput — e.g. processing a stream of items each of which must be handled.","Use **`conflate`** when the collector only cares about the *current/latest* value and intermediates are pointless — e.g. a live price ticker or progress indicator, where rendering every intermediate frame the collector was too slow to see would be wasted work.","Memory-wise, `conflate` is bounded (it holds at most the latest value); `buffer` holds as many as its capacity allows."]},{t:"p",text:"A neat way to remember it: `conflate()` is essentially `buffer` with a 'drop oldest, keep newest' overflow policy and minimal capacity. And it's exactly why `StateFlow` behaves the way it does — state only cares about 'now', so it's conflated by design."}]},{level:"junior",q:"If Flow handles backpressure automatically, when do you actually need buffer or conflate?",a:[{t:"p",text:"**The default (suspension) is correct most of the time** — it's safe (nothing lost) and memory-bounded. You reach for `buffer`/`conflate` only to address the default's one downside: because producer and collector run serially, their times add up, which can be too slow."},{t:"list",items:["**Add `buffer`** when the producer and collector each take real time and running them *concurrently* would meaningfully speed things up — and you need every value. Example: a flow that reads pages from disk (producer) and parses them (collector); buffering lets the next page load while the current one parses.","**Add `conflate` (or `collectLatest`)** when the collector can't keep up *and* only the latest value is relevant — a fast sensor stream feeding a UI that just shows the current reading. Here you *want* to skip the backlog.","**Leave it alone** when values arrive slowly relative to processing (the common case) — adding buffering does nothing useful and only adds memory/complexity."]},{t:"p",text:"So the honest answer is: usually you don't need them. They're targeted optimizations for 'producer and collector are both slow, and I either want to overlap them (buffer) or only care about the latest (conflate)'."}]},{level:"senior",q:"Explain the difference between conflate() and collectLatest(). When would you choose each?",a:[{t:"p",text:"**Both give you 'latest wins' behavior for a slow collector, but at different points.** `conflate()` acts *before* processing: it drops intermediate values so the collector's block only ever *starts* with values it can keep up with — and once a block starts, it **runs to completion**. `collectLatest()` acts *during* processing: it starts the block for *every* value, but **cancels** the in-progress block if a newer value arrives, so only the latest value's processing survives."},{t:"code",title:"The behavioral difference",code:`// conflate: some values skipped, but each STARTED block finishes
flow.conflate().collect { writeToDb(it) }        // every write that starts, completes

// collectLatest: every value starts, superseded ones are CANCELLED mid-way
flow.collectLatest { writeToDb(it) }             // an in-progress write may be cancelled`},{t:"list",items:["**Choose `conflate`** when the per-value work must not be interrupted once begun — a DB write, a file operation, anything where a half-done cancellation is bad. You accept that some values are never processed, but the ones that are, complete cleanly.","**Choose `collectLatest`/`flatMapLatest`** when the work is *cancellable and worth abandoning* when stale — search-as-you-type, recomputing an expensive result for the newest input. The whole point is that finishing outdated work is wasteful and possibly harmful (stale-result race), so cancelling it is desirable.","**The subtle senior point**: `conflate` never cancels — it's about *delivery* (which values reach the collector). `collectLatest` is about *cancellation* (which in-progress work survives). If your processing has side effects that shouldn't be interrupted, that distinction decides correctness, not just performance."]}]},{level:"senior",q:"How does buffer() interact with flowOn, and how do multiple buffer stages behave in a pipeline?",a:[{t:"p",text:"**They're deeply related — both introduce a channel between stages.** `flowOn(dispatcher)` changes the context of the *upstream* and, to do so, it must run upstream in a different coroutine, which requires a channel to hand values across — so `flowOn` *implicitly buffers*. That means adding `flowOn` already decouples the upstream from the downstream (they run concurrently on different dispatchers), giving you a throughput benefit similar to `buffer` as a side effect."},{t:"list",items:["**Each `buffer`/`flowOn` boundary is a concurrency stage**: values cross a channel, and the stages on either side run in parallel. So a pipeline `A.map{}.flowOn(io).buffer().collect{}` has the `io`-dispatched upstream, a buffer, and the collector all potentially running concurrently — a small pipeline of overlapping stages.","**Multiple buffers compound**: each `buffer()` adds another channel and another capacity, so `flow.buffer().map{}.buffer().collect{}` creates two decoupled stages. This can help (more overlap) or hurt (more memory, more latency between first emission and first collection) — measure rather than sprinkle.","**Adjacent buffers may be fused**: the Flow implementation fuses consecutive `buffer`/`flowOn`/`conflate` operators where possible (e.g. `buffer(); buffer()` becomes one channel with combined capacity; `conflate(); buffer()` resolves per rules), so you don't always pay for every operator literally — but relying on fusion is fragile; write the intent you mean.","**Practical rule**: you rarely need explicit `buffer` if you already have a `flowOn` boundary where the expensive work happens — the `flowOn` gives you the concurrency. Add explicit `buffer` only when producer and collector are on the *same* context but you still want them overlapped, or to set a specific capacity/overflow policy."]},{t:"p",text:"**The mental model to convey**: think of `buffer`, `flowOn`, and `conflate` as all placing a channel between pipeline stages — that's why they share capacity/overflow parameters and why they interact. Understanding that they're the *same underlying mechanism* (a channel handoff) is what separates surface knowledge from real understanding here."}]},{level:"senior",q:"A flow producing values rapidly is causing memory growth or UI lag. Walk through diagnosing and choosing a backpressure strategy.",a:[{t:"list",items:["**1. Identify the mismatch**: confirm it's producer-faster-than-collector. Profile where time goes — is the *collector* slow (heavy per-value work: parsing, DB writes, recomposition) or is the *producer* flooding (a high-frequency sensor/socket)? The fix differs.","**2. Decide: do you need every value?** This is the pivotal question. If yes → keep-all strategies (`buffer`, fix the slow collector). If no, only latest matters → drop strategies (`conflate`, `collectLatest`, `sample`, `debounce`).","**3. If every value matters and memory is growing**: an unbounded buffer somewhere (explicit `buffer()` with no cap, or a `channelFlow`/`callbackFlow` with unlimited capacity) is queuing faster than draining. Bound it (`buffer(capacity)`), and if the collector simply can't keep up long-term, that's a design problem — move work off the hot path, batch, or apply `sample` to reduce rate deliberately.","**4. If only the latest matters (typical UI case)**: `conflate()` (skip intermediates, keep newest) for a stream you render, or `collectLatest`/`flatMapLatest` if the per-value work is cancellable and stale computations should be abandoned. For time-based thinning, `sample(interval)` (regular snapshots) or `debounce(timeout)` (wait for quiet) cut the rate at the source.","**5. If the collector is slow due to threading**: ensure the collector isn't doing heavy work on the main thread — move it with `flowOn` on the upstream heavy stage, or process off-main, so the UI isn't the bottleneck.","**6. Verify with numbers**: re-profile memory and frame timing after the change — backpressure fixes are easy to cargo-cult; confirm the buffer is bounded and the UI keeps up."]},{t:"p",text:"**The framing**: the entire decision reduces to 'must I process every value, or only the latest?' plus 'where is the actual bottleneck?'. Rapid producer + UI that shows current state → `conflate`/`sample`. Rapid producer + must-process-all → bounded `buffer` and a faster or off-loaded collector. Naming that decision tree, and bounding any unbounded buffer, is the senior-level answer."}]}],GS={id:"backpressure",title:"Buffering, Conflation & Backpressure",tagline:"How Flow handles backpressure via suspension, buffer() for throughput, conflate() for latest-wins, collectLatest vs conflate, and choosing a strategy.",tags:["Flow","Backpressure","buffer","conflate","collectLatest"],content:KS,qa:JS},$S=[{heading:"Context preservation — flows run where they're collected",blocks:[{t:"p",text:"By default, a flow's producer and operators run in **the coroutine context of the collector**. If you collect on `Dispatchers.Main`, the `flow { }` body and every `map`/`filter` also run on Main. This is called **context preservation**: the flow doesn't secretly hop threads. It's a deliberate safety property — you always know where your flow code executes, and emissions arrive on the collector's context (perfect for updating UI)."},{t:"p",text:"This is also why Flow **forbids changing context inside the builder** — you can't wrap `emit` in `withContext(IO)` inside `flow { }` (it throws 'Flow invariant is violated'). The framework enforces that context changes happen through the dedicated operator, `flowOn`, so the guarantees hold."}]},{heading:"flowOn — change the context of the UPSTREAM",blocks:[{t:"p",text:"`flowOn(dispatcher)` changes the context (usually the dispatcher/thread) of **everything upstream of it** — the operators and producer that come *before* it in the chain. Crucially it affects only upstream, not downstream, so you move heavy work off the main thread while the collector still receives values on its own context."},{t:"code",title:"flowOn moves upstream work off the main thread",code:`flow {
    val data = expensiveBlockingRead()   // runs on IO (below is flowOn(IO))
    emit(data)
}
.map { heavyTransform(it) }              // also runs on IO (upstream of flowOn)
.flowOn(Dispatchers.IO)                  // <-- everything ABOVE runs on IO
.map { it.toUiModel() }                  // runs on the COLLECTOR's context (Main)
.collect { render(it) }                  // Main — safe to touch UI`},{t:"list",items:["**Upstream vs downstream**: `flowOn` changes the context for operators *above* it; operators *below* it stay on the collector's context. So you place `flowOn(IO)` right after the heavy work and before the UI-facing operators.","**Multiple `flowOn`s** are allowed — each governs the segment above it up to the next `flowOn`, letting different stages run on different dispatchers.","**It implicitly buffers**: to run upstream on a different thread, `flowOn` hands values across a channel — so it also decouples producer and collector (a throughput side effect, as covered in the backpressure topic).","**Best practice**: in clean architecture the repository/data layer should already be main-safe (it does its own `withContext`), so you often don't need `flowOn` in the ViewModel — but it's the right tool when a flow's *producer* does blocking/CPU work and you want it off Main."]}]},{heading:"stateIn — convert a cold flow into a hot StateFlow",blocks:[{t:"p",text:"`stateIn` turns a **cold** flow into a **hot `StateFlow`**. Why you'd want this: a cold flow re-runs per collector, so if two things observe it you get duplicate work (two DB queries, two network calls). `stateIn` collects the upstream *once* in a given scope and multicasts the latest value to all collectors as shared state."},{t:"code",title:"The standard ViewModel derivation",code:`val uiState: StateFlow<UiState> = repository.observeData()   // cold Flow
    .map { data -> UiState.Content(data) }
    .stateIn(
        scope = viewModelScope,                                 // where collection runs
        started = SharingStarted.WhileSubscribed(5_000),        // when to start/stop upstream
        initialValue = UiState.Loading,                         // value before first emission
    )`},{t:"list",items:["**`scope`** — the coroutine scope that owns the single upstream collection (usually `viewModelScope`).","**`initialValue`** — StateFlow needs a current value immediately; this is it, shown until the upstream emits.","**`started`** — the sharing policy that controls when the upstream is actually collected (below).","**Result**: one upstream execution shared by all collectors, exposed as a `StateFlow` with `.value` — the idiomatic way to expose repository data as UI state."]}]},{heading:"SharingStarted — Eagerly, Lazily, WhileSubscribed",blocks:[{t:"table",headers:["Policy","Starts upstream","Stops upstream"],rows:[["`Eagerly`","immediately when stateIn/shareIn is created","only when the scope is cancelled"],["`Lazily`","when the first collector appears","only when the scope is cancelled"],["`WhileSubscribed(stopTimeout)`","when the first collector appears","stopTimeout ms after the LAST collector leaves"]]},{t:"p",text:"**`WhileSubscribed(5000)` is the Android default, and the 5000ms is deliberate.** It stops the upstream flow 5 seconds after the last collector disappears. The magic number exists because a **configuration change** briefly drops all collectors (the UI is recreated) — the 5s timeout keeps the expensive upstream (DB observation, location) alive *through* a rotation, but shuts it down for a real backgrounding (where the collector is gone for longer than 5s). It's the sweet spot between 'restart the upstream on every rotation' (0ms) and 'never stop, waste resources' (Eagerly)."},{t:"list",items:["**`Eagerly`** — upstream runs from creation, never stops until scope death. Fine for cheap, always-needed state; wasteful for expensive upstreams the UI isn't watching.","**`Lazily`** — starts on the first collector, then never stops. Rarely the right choice for UI.","**`WhileSubscribed(5000)`** — the recommended default for UI state: starts on demand, survives config changes, stops when genuinely backgrounded. Pairs with `collectAsStateWithLifecycle` / `repeatOnLifecycle(STARTED)` on the UI side so backgrounding stops collection → stops the upstream after 5s."]}]},{heading:"shareIn — convert to a hot SharedFlow",blocks:[{t:"p",text:"`shareIn` is `stateIn`'s sibling for **events/streams** rather than state: it turns a cold flow into a hot **`SharedFlow`** with configurable `replay`. Use it when you want to share an upstream among many collectors but there's no single 'current value' (or you want more than one replayed value)."},{t:"code",title:"shareIn for a shared event/data stream",code:`val sharedLocationUpdates: SharedFlow<Location> = locationFlow   // cold callbackFlow
    .shareIn(
        scope = externalScope,
        started = SharingStarted.WhileSubscribed(5_000),
        replay = 1,   // new collectors get the last known location
    )`},{t:"list",items:["**`stateIn` vs `shareIn`**: `stateIn` → `StateFlow` (always a current value, conflated, `replay = 1` effectively, needs `initialValue`); `shareIn` → `SharedFlow` (configurable replay, no required initial value, no conflation). Choose by whether the thing is *state* or a *stream/events*.","**Common use**: sharing one expensive upstream (a socket, a location listener wrapped in `callbackFlow`) among multiple consumers so it runs once — the 'single upstream, many collectors' optimization."]}]},{heading:"callbackFlow — bridging callback APIs into flows",blocks:[{t:"p",text:"Many Android APIs are **callback-based**: you register a listener and get invoked on events (`LocationManager`, sensors, `addSnapshotListener`, `TextWatcher`). `callbackFlow` wraps such an API into a Flow — turning push-callbacks into a collectable stream."},{t:"code",title:"The callbackFlow template — register, send, awaitClose",code:`fun locationUpdates(client: FusedLocationProviderClient): Flow<Location> = callbackFlow {
    val callback = object : LocationCallback() {
        override fun onLocationResult(result: LocationResult) {
            result.lastLocation?.let { trySend(it) }   // push into the flow (thread-safe)
        }
    }
    client.requestLocationUpdates(request, callback, Looper.getMainLooper())

    awaitClose {                                        // REQUIRED
        client.removeLocationUpdates(callback)          // unregister when collection stops
    }
}`},{t:"list",items:["**`trySend(value)`** — the thread-safe way to emit from inside a callback (which may fire on any thread). It's non-suspending and returns a result you can inspect (it can fail if the buffer is full).","**`awaitClose { }` is mandatory** — it suspends `callbackFlow` open until the collector cancels, and its block runs the cleanup (unregister the listener). Forgetting it leaks the listener *and* throws at runtime (callbackFlow requires it). This is the #1 callbackFlow interview point.","**Buffer/overflow**: `callbackFlow` uses a channel; you can set capacity and `onBufferOverflow` (e.g. `BufferOverflow.DROP_OLDEST` for high-frequency sensors where only the latest matters).","**Cold by default**: like any flow, it registers the listener *per collector*. To share one registration among many collectors, convert to hot with `shareIn`."]},{t:"note",text:"The three things to always say about callbackFlow: use `trySend` (thread-safe emit), always `awaitClose` (cleanup + keeps it open), and remember it's cold (one registration per collector — `shareIn` to share). Those three points answer nearly every callbackFlow question."}]}],YS=[{level:"junior",q:"What does flowOn do, and why can't you just use withContext inside a flow builder?",a:[{t:"p",text:"**The concept**: by default a flow runs where it's collected (context preservation) — if you collect on Main, the producer and operators run on Main too. `flowOn(dispatcher)` changes the context of everything **upstream** of it (the operators and producer *above* it in the chain), while everything downstream stays on the collector's context. So you place `flowOn(Dispatchers.IO)` after heavy work to run that work off the main thread, and the collector still receives values on Main to safely update UI."},{t:"code",title:"flowOn affects only what's above it",code:`flow { emit(blockingRead()) }     // runs on IO
    .map { heavyParse(it) }        // runs on IO (above flowOn)
    .flowOn(Dispatchers.IO)
    .map { it.toUiModel() }        // runs on collector's context (Main)
    .collect { render(it) }        // Main`},{t:"p",text:"**Why not `withContext(IO)` inside `flow { }`**: it's forbidden — the flow builder throws 'Flow invariant is violated' if you try to `emit` from a different context than the one the flow was collected in. Flow *guarantees* context preservation and sequential emission, and wrapping `emit` in a `withContext` would break that guarantee (emissions could arrive on a different thread than promised). `flowOn` is the sanctioned mechanism that changes upstream context *while keeping the invariant intact* (it does the thread handoff safely via an internal channel). So the rule is: never change context manually inside the builder; use `flowOn`."}]},{level:"junior",q:"What is stateIn and why would you use it in a ViewModel?",a:[{t:"p",text:"**The concept**: `stateIn` converts a *cold* flow into a hot `StateFlow`. Remember cold flows re-run per collector — so if a ViewModel exposed a cold flow and two things observed it (the UI plus a logger, or after a config change), you'd trigger the upstream work (a DB query, network call) twice. `stateIn` collects the upstream **once** in a scope and shares the latest value with all collectors as state."},{t:"code",title:"The idiomatic derivation",code:`val uiState: StateFlow<UiState> = repository.observeItems()
    .map { UiState.Content(it) }
    .stateIn(
        viewModelScope,
        SharingStarted.WhileSubscribed(5_000),
        UiState.Loading,
    )`},{t:"list",items:["It gives you a `StateFlow` — with a synchronous `.value`, a current value for new collectors, and conflated state semantics — which is exactly what a UI wants to observe.","It collects the upstream once (in `viewModelScope`), so multiple UI collectors don't duplicate the expensive work.","`initialValue` provides the state to show before the upstream emits (e.g. `Loading`).","The `started` policy (`WhileSubscribed(5000)`) controls when the upstream actually runs — starting on demand and stopping when the UI is truly gone."]}]},{level:"junior",q:"What is the difference between stateIn and shareIn?",a:[{t:"p",text:"**Both convert a cold flow to a hot one and share a single upstream among collectors — the difference is the output type and semantics.** `stateIn` produces a **`StateFlow`**: it always has a current value, is conflated (only latest), and needs an `initialValue`. `shareIn` produces a **`SharedFlow`**: no required current value, configurable `replay` (0, 1, or more), and no conflation."},{t:"p",text:"**Choose by whether the thing is *state* or a *stream/events***: use `stateIn` for state the UI observes (screen state, a value over time) — it needs a current value and 'latest wins' is correct. Use `shareIn` for sharing an event stream or a data source where there isn't a single 'current value', or where you want to control replay explicitly — e.g. broadcasting location updates from one shared listener to several consumers with `replay = 1` so new consumers get the last known location. In short: `stateIn` → StateFlow (state), `shareIn` → SharedFlow (streams/events)."}]},{level:"junior",q:"What is callbackFlow and when do you use it?",a:[{t:"p",text:"**The concept**: many Android APIs are *callback-based* — you register a listener and they call you back on events: `LocationManager`, sensors, Firebase's `addSnapshotListener`, `TextWatcher`, connectivity callbacks. `callbackFlow` wraps such an API into a `Flow`, converting the push-callback model into a stream you can collect and use with operators."},{t:"code",title:"The template",code:`fun connectivity(cm: ConnectivityManager): Flow<Boolean> = callbackFlow {
    val callback = object : ConnectivityManager.NetworkCallback() {
        override fun onAvailable(n: Network) { trySend(true) }
        override fun onLost(n: Network) { trySend(false) }
    }
    cm.registerDefaultNetworkCallback(callback)
    awaitClose { cm.unregisterNetworkCallback(callback) }   // cleanup — required
}`},{t:"list",items:["**`trySend(value)`** pushes a value into the flow from inside the callback — it's thread-safe (callbacks may fire on any thread) and non-suspending.","**`awaitClose { }` is mandatory**: it keeps the flow open (waiting for events) and runs cleanup — unregistering the listener — when the collector cancels. Forgetting it both leaks the listener and causes a runtime error.","Use it whenever you need to consume a callback API reactively — it lets you apply Flow operators (`debounce`, `map`, `distinctUntilChanged`) to callback events and integrates them with structured concurrency (collection stops → listener unregistered)."]}]},{level:"senior",q:"Explain SharingStarted.WhileSubscribed(5000). Why 5000, and how does it compare to Eagerly and Lazily?",a:[{t:"p",text:"**What it does**: `WhileSubscribed(stopTimeoutMillis)` is a sharing policy for `stateIn`/`shareIn` that starts the upstream when the first collector subscribes and **stops it `stopTimeoutMillis` after the last collector unsubscribes**, restarting it if a collector returns. It ties the expensive upstream's lifetime to whether anyone is actually watching."},{t:"p",text:"**Why 5000ms specifically**: a **configuration change** (rotation) tears down the UI and recreates it, which briefly drops all collectors — for a few hundred milliseconds there are zero subscribers. Without a timeout (i.e. `WhileSubscribed(0)`), the upstream would be *cancelled and cold-restarted* on every rotation: the DB query re-runs, the network re-observes, the user sees a flash of loading state. The 5-second window comfortably outlasts a config change, so the upstream survives rotation seamlessly — but for a *real* backgrounding (the app goes to the background and the lifecycle-aware collector stops for more than 5s), the upstream does stop, freeing resources. 5000ms is the empirical sweet spot between those two."},{t:"list",items:["**`Eagerly`** — upstream starts immediately when `stateIn` is created and runs until the scope (e.g. `viewModelScope`) is cancelled, regardless of collectors. Use for cheap state that's always needed; wasteful for expensive upstreams the UI might not be observing.","**`Lazily`** — starts on the first collector, then runs until scope cancellation (never stops on unsubscribe). Rarely ideal for UI — it doesn't free resources when backgrounded.","**`WhileSubscribed(5000)`** — the recommended default for UI state, because it's the only policy that both survives config changes *and* releases resources on genuine backgrounding. It relies on the UI collecting with lifecycle awareness (`collectAsStateWithLifecycle` / `repeatOnLifecycle(STARTED)`), so that backgrounding actually removes the collector and, after 5s, stops the upstream."]},{t:"p",text:"**The complete picture to convey**: `WhileSubscribed(5000)` + `stateIn(viewModelScope)` + `collectAsStateWithLifecycle` is a coordinated system — the lifecycle-aware collector drops the subscription on background, the 5s timeout ignores the momentary drop of rotation, and the upstream (DB/network/location) runs exactly when the user can see the result and stops shortly after they can't."}]},{level:"senior",q:"Why is awaitClose required in callbackFlow, and what happens if you forget it?",a:[{t:"p",text:"**Two reasons, both essential.** First, `callbackFlow` (built on a channel) would otherwise **complete immediately** after the builder block returns — but a callback API keeps calling back *after* registration, over time. `awaitClose { }` **suspends the flow open**, keeping it alive to receive future callbacks until the collector cancels. Without it, the flow ends right after registering the listener and you'd receive nothing. Second, `awaitClose`'s block is where you **unregister the listener** — it runs precisely when collection is cancelled (scope death, `take`, the collector leaving)."},{t:"list",items:["**What happens if you forget it**: `callbackFlow` actively checks for it and throws `IllegalStateException` ('Flow invariant is violated... awaitClose { } should be used') — so it's not silent, it fails fast. That's the framework forcing you to handle cleanup.","**The bug it prevents**: without unregistering, the listener outlives the flow's collection — a leaked `LocationListener`/`NetworkCallback`/Firebase listener that keeps firing (draining battery, holding references, possibly crashing when it touches a dead component). `awaitClose` guarantees the registration and unregistration are balanced by the collection lifecycle.","**Correct usage detail**: `awaitClose` must be the *last* thing in the builder, and everything before it is the setup; the cleanup lambda should undo exactly the setup (unregister the exact callback you registered)."]},{t:"p",text:"**The senior framing**: `callbackFlow` bridges a *manually-managed resource* (a registered listener) into structured concurrency, and `awaitClose` is the seam that makes the resource's lifetime follow the flow's collection lifetime — register on collect, unregister on cancel. It's the flow-world equivalent of `DisposableEffect`'s `onDispose` in Compose: the mandatory cleanup hook that prevents leaks."}]},{level:"senior",q:"A cold flow from your repository is being collected by two parts of the UI and causing duplicate network calls. How do you fix it?",a:[{t:"p",text:"**The root cause**: cold flows execute their producer *independently per collector*. Two collectors of the same cold repository flow = two separate executions = two network calls (or two DB observations). This is correct cold-flow behavior, not a bug in the flow — the fix is to **share a single upstream execution** among collectors by making it hot."},{t:"list",items:["**Primary fix — `stateIn`/`shareIn`**: in the ViewModel, collect the cold flow once and expose a hot flow. `repository.observeData().stateIn(viewModelScope, WhileSubscribed(5000), initial)` collects the upstream a single time and multicasts to all UI collectors — one network call, shared results. Use `stateIn` if it's state, `shareIn` if it's a stream/events.","**Make sure both collectors observe the SAME hot instance**: the `StateFlow` must be a single property on the ViewModel that both UI pieces read — not two calls that each create a new `stateIn`. A common mistake is calling `.stateIn(...)` in a getter or per-collection, which recreates the sharing each time and defeats it. Store it in a `val`.","**If sharing must span ViewModels/screens** (two screens want the same live data): move the single-source-of-truth *down to the data layer*. Have the repository hold the shared hot flow (e.g. a cache `MutableStateFlow`, or the DB as SSOT that both observe) so any number of ViewModels observing it triggers just one network refresh. This is the more robust architectural fix.","**For request de-duplication specifically**: the repository can also guard with a shared in-flight job/mutex so even concurrent *refresh* calls collapse into one network request."]},{t:"p",text:"**The principle**: cold = per-collector execution; when you want 'observe the same thing in many places, compute once', convert to hot at the appropriate layer — `stateIn`/`shareIn` in the ViewModel for one screen, or a shared hot source / SSOT in the repository for cross-screen sharing. Naming *both* levels (and why storing the `stateIn` result in a `val` matters) is the complete answer."}]}],QS={id:"context-conversion",title:"Context, flowOn & Conversions",tagline:"Context preservation, flowOn for threading, stateIn/shareIn to go hot, SharingStarted policies (WhileSubscribed 5000), and callbackFlow with awaitClose.",tags:["flowOn","stateIn","shareIn","callbackFlow","SharingStarted"],content:$S,qa:YS},XS=[{heading:"How exceptions travel through a flow",blocks:[{t:"p",text:"An exception thrown anywhere in a flow — in the producer, in an operator like `map`, or in the collector's block — **propagates downstream** and, if unhandled, surfaces at the terminal operator (`collect`), where it can be caught with an ordinary try/catch or the `catch` operator. The important principle Flow enforces is **exception transparency**: operators should not hide exceptions from downstream, and you handle them declaratively rather than wrapping random pieces in try/catch."},{t:"code",title:"The naive way vs the Flow way",code:`// Works but discouraged — imperative, easy to get wrong:
try {
    flow.collect { value -> render(value) }
} catch (e: Exception) {
    showError(e)
}

// Idiomatic — declarative, composable:
flow
    .catch { e -> emit(fallbackValue) }   // handle UPSTREAM errors
    .collect { render(it) }`}]},{heading:"The catch operator — handle upstream errors only",blocks:[{t:"p",text:"`catch { }` catches exceptions that occur **upstream** of it (in the producer and any operators above it). It does *not* catch exceptions thrown *downstream* (in operators below it or in the `collect` block) — this is deliberate and follows the 'catch only what's above you' rule, so placement matters."},{t:"code",title:"Placement determines what's caught",code:`flow
    .map { riskyTransform(it) }   // errors here ARE caught below
    .catch { e ->
        // only sees exceptions from the producer and the map above
        emit(defaultValue)        // you can emit a fallback,
        // or rethrow, or log — but you're 'above' the collect
    }
    .map { it.toUiModel() }       // errors here are NOT caught by the catch above
    .collect { render(it) }       // errors here are NOT caught either`},{t:"list",items:["**Inside `catch` you can**: emit a fallback value (`emit(...)`), rethrow (to propagate), or just log/handle. It gives you the exception and an `emit` capability.","**Why not catch downstream too**: catching the collector's own errors would conflate 'the data source failed' with 'my rendering code has a bug' — Flow keeps them separate. To handle collector errors, use a try/catch around `collect` or move logic into an operator above the `catch`.","**Placement pattern**: put `catch` right after the risky upstream and before the UI-facing operators, so it handles data errors but not UI-code bugs."]}]},{heading:"The inner-flow catch pattern (critical for pipelines)",blocks:[{t:"p",text:"A subtle but vital pattern: in a `flatMapLatest`/`combine` pipeline (like search), where you place `catch` decides whether one error kills the *entire* pipeline or just one operation. A `catch` on the **outer** flow ends everything permanently after the first error — the user can never search again. A `catch` on the **inner** flow contains the error to that one operation, leaving the pipeline alive."},{t:"code",title:"Inner catch keeps the pipeline recoverable",code:`queryFlow
    .flatMapLatest { query ->
        repository.search(query)
            .map { SearchState.Results(it) }
            .onStart { emit(SearchState.Loading) }
            .catch { emit(SearchState.Error(it.message)) }  // INNER: this search fails,
    }                                                        // but the next keystroke works
    .collect { render(it) }
// A catch placed AFTER flatMapLatest (outer) would terminate the whole
// pipeline on the first failed search — no recovery.`}]},{heading:"retry and retryWhen — recovering from transient failures",blocks:[{t:"code",title:"Retry with backoff",code:`flow
    .retryWhen { cause, attempt ->
        // return true to retry, false to give up
        if (cause is IOException && attempt < 3) {
            delay(1000 * (attempt + 1))   // exponential-ish backoff
            true
        } else {
            false                          // propagate the error downstream
        }
    }
    .catch { emit(cachedFallback) }
    .collect { render(it) }`},{t:"list",items:["**`retry(count)`** — re-collect the flow up to `count` times on any exception (optionally filtered by a predicate). Simple case.","**`retryWhen { cause, attempt -> }`** — full control: inspect the exception and attempt number, `delay` for backoff, return `true` to retry or `false` to propagate. The standard way to retry network calls with exponential backoff, only on transient errors (`IOException`) and a bounded attempt count.","**Retry re-runs the upstream** (it's cold) — so a `retry` on a network flow re-issues the request. Combine with `catch` as the final fallback after retries are exhausted."]}]},{heading:"onStart, onEmpty, onCompletion — lifecycle hooks",blocks:[{t:"list",items:["**`onStart { }`** — runs *before* the first value; use to emit a loading state or trigger a side effect when collection begins (`.onStart { emit(Loading) }`).","**`onCompletion { cause -> }`** — runs when the flow finishes, whether normally (`cause == null`) or due to an exception/cancellation (`cause != null`). Use for cleanup or 'hide loading' logic. Note it *observes* the cause but doesn't handle it — the exception still propagates (put `catch` before it to handle).","**`onEmpty { }`** — runs if the flow completes without emitting anything; emit a default or empty state."]},{t:"code",title:"Composing lifecycle hooks with error handling",code:`repository.observeData()
    .onStart { emit(UiState.Loading) }
    .map { UiState.Content(it) }
    .catch { emit(UiState.Error(it.message)) }       // handle before onCompletion
    .onCompletion { cause -> if (cause != null) log("flow ended: $cause") }
    .collect { render(it) }`},{t:"note",text:"Order matters: `onStart`/`onCompletion` and `catch` are order-sensitive because each only sees what's upstream of it. The idiomatic order is `onStart` (loading) → transform → `catch` (errors → error state) → `onCompletion` (observe end). `CancellationException` must be rethrown, never swallowed — the `catch` operator already ignores it correctly, but a manual try/catch around a suspend call must rethrow it."}]},{heading:"Testing flows — Turbine and virtual time",blocks:[{t:"p",text:"Flows are tested inside `runTest` (which provides a `TestScope` with **virtual time** — `delay`s complete instantly by advancing a virtual clock). Collecting a flow and asserting its emissions is far cleaner with **Turbine**, a small library that turns collection into an await-based API."},{t:"code",title:"Testing a flow with Turbine",code:`@Test
fun searchEmitsLoadingThenResults() = runTest {
    val viewModel = SearchViewModel(FakeRepository(results = listOf(item)))

    viewModel.uiState.test {                     // Turbine: collect the flow
        assertEquals(SearchState.Loading, awaitItem())
        viewModel.onQueryChange("kotlin")
        advanceTimeBy(300)                        // skip debounce via virtual time
        assertEquals(SearchState.Results(listOf(item)), awaitItem())
        cancelAndIgnoreRemainingEvents()
    }
}`},{t:"list",items:["**Turbine basics**: `flow.test { }` collects the flow; inside, `awaitItem()` gets the next emission, `awaitComplete()` asserts completion, `awaitError()` asserts an exception, `expectNoEvents()` asserts nothing emitted. It fails the test if you leave unconsumed events (catching over-emission).","**Virtual time**: `runTest` runs `delay`s instantly by advancing a virtual clock; `advanceTimeBy(ms)` / `advanceUntilIdle()` let you test `debounce`, `sample`, and retry backoff *deterministically and fast* — no real waiting.","**The `stateIn(WhileSubscribed)` gotcha**: such a flow is cold until collected, so asserting `.value` right after construction sees only the `initialValue`. You must *collect* it (Turbine's `.test { }`, or `backgroundScope.launch { flow.collect() }`) to start the upstream and see real emissions.","**Inject a TestDispatcher**: with `viewModelScope` (which uses `Dispatchers.Main`), install `Dispatchers.setMain(StandardTestDispatcher())` via a JUnit rule so virtual time controls the ViewModel's coroutines; use `StandardTestDispatcher` when you need to assert intermediate states, `UnconfinedTestDispatcher` for eager execution."]}]}],ZS=[{level:"junior",q:"How do you handle errors in a Flow?",a:[{t:"p",text:"**The concept**: an exception anywhere in a flow — producer, an operator, or the collect block — propagates downstream to the terminal operator. You *can* wrap `collect` in a try/catch, but the idiomatic, composable way is the **`catch` operator**, which handles exceptions declaratively as part of the pipeline."},{t:"code",title:"The catch operator",code:`repository.observeData()
    .map { UiState.Content(it) }
    .catch { e -> emit(UiState.Error(e.message)) }   // handle upstream errors
    .collect { render(it) }`},{t:"p",text:"Inside `catch` you can emit a fallback value, rethrow to propagate, or just log. The key thing to know is that `catch` only catches exceptions from **upstream** (operators and the producer *above* it), not from operators below it or from the `collect` block — so you place it after the risky part and before the UI-facing code. This 'catch only what's above you' rule keeps data-source errors separate from bugs in your rendering code. For network flows you often combine it with `retry`/`retryWhen` (retry transient failures first) and then `catch` as the final fallback."}]},{level:"junior",q:"What does onStart and onCompletion do?",a:[{t:"list",items:["**`onStart { }`** runs *before* the first value is emitted — and it can itself `emit`. The classic use is emitting a loading state as collection begins: `.onStart { emit(UiState.Loading) }` so the UI shows a spinner immediately, before the real data arrives.","**`onCompletion { cause -> }`** runs when the flow ends — for any reason. If it completed normally, `cause` is null; if it ended from an exception or cancellation, `cause` is that throwable. Use it for cleanup or 'hide the loading indicator' logic that should run regardless of success or failure."]},{t:"p",text:"An important subtlety about `onCompletion`: it *observes* the completion cause but doesn't *handle* it — if the flow failed, the exception still propagates downstream after `onCompletion` runs. So `onCompletion` is not a substitute for `catch`; you put `catch` before it if you want to actually handle the error. The idiomatic order is `onStart` (loading) → transforms → `catch` (turn errors into an error state) → `onCompletion` (observe the end / cleanup)."}]},{level:"junior",q:"How do you retry a flow that fails, e.g. a flaky network call?",a:[{t:"p",text:"**The concept**: since a flow is cold, re-collecting it re-runs the upstream — so 'retry' means re-executing the producer. Flow gives you two operators for this. `retry(count)` simply re-collects up to `count` times on any exception (optionally filtered by a predicate). `retryWhen { cause, attempt -> }` gives full control: you inspect the exception and the attempt number, can `delay` between attempts (for backoff), and return `true` to retry or `false` to give up."},{t:"code",title:"Retry only transient errors, with backoff",code:`flow
    .retryWhen { cause, attempt ->
        if (cause is IOException && attempt < 3) {
            delay(1000 * (attempt + 1))   // wait longer each time
            true                          // retry
        } else false                      // give up -> error propagates
    }
    .catch { emit(cachedFallback) }       // final fallback after retries fail
    .collect { render(it) }`},{t:"p",text:"Best practice is to retry *only* transient failures (like `IOException` for network blips) and *not* things like a 404 or a parsing error that will fail identically every time — retrying those just wastes time. Bound the attempts, add increasing backoff so you don't hammer a struggling server, and pair it with a final `catch` that provides a fallback (cached data, an error state) once retries are exhausted."}]},{level:"junior",q:"How do you test a Flow?",a:[{t:"p",text:"**The concept**: flow tests run inside `runTest`, which provides virtual time (so `delay`s complete instantly by advancing a fake clock, making tests fast and deterministic). To assert emissions, the cleanest approach is the **Turbine** library, which collects the flow and gives you an await-based API."},{t:"code",title:"Turbine test",code:`@Test fun emitsValues() = runTest {
    flowOf(1, 2, 3).test {
        assertEquals(1, awaitItem())
        assertEquals(2, awaitItem())
        assertEquals(3, awaitItem())
        awaitComplete()
    }
}`},{t:"list",items:["Inside `flow.test { }`: `awaitItem()` gets the next emission, `awaitComplete()` asserts the flow finished, `awaitError()` asserts an exception, `expectNoEvents()` asserts nothing was emitted. Turbine also fails the test if emissions are left unconsumed, catching accidental over-emission.","For time-based operators (`debounce`, `sample`, retry backoff), use `advanceTimeBy(ms)` or `advanceUntilIdle()` to move virtual time forward without real waiting — so a test of a 300ms debounce runs instantly.","For ViewModels using `viewModelScope` (which uses `Dispatchers.Main`), install a `TestDispatcher` as Main via a JUnit rule so the test controls the coroutines."]}]},{level:"senior",q:"Explain exception transparency in Flow and why placement of the catch operator matters.",a:[{t:"p",text:"**Exception transparency** is a Flow design principle: a flow must not *catch* exceptions from downstream and *emit* in response — emissions and exception handling stay clearly separated so that downstream code can trust the contract. Concretely, this is why you can't put a try/catch *inside* a `flow { }` builder around an `emit` and then emit again from the catch (it violates transparency and throws). Handling belongs in the dedicated `catch` operator, which has well-defined scope."},{t:"p",text:"**Why placement matters**: `catch` only sees exceptions from **upstream** of it — the producer and operators *above* it. Anything *below* the `catch` (later operators, the `collect` block) is invisible to it. This is intentional: it separates 'the data source or a transformation failed' (which `catch` should handle) from 'my rendering code threw a bug' (which should *not* be silently swallowed by a data-error handler)."},{t:"code",title:"Placement changes behavior",code:`flow.map { parse(it) }      // A: caught
    .catch { emit(default) } //    <- catches producer + map A
    .map { render(it) }      // B: NOT caught (a bug here surfaces at collect)
    .collect { ui(it) }      // C: NOT caught`},{t:"list",items:["Put `catch` *right after the risky upstream* (the network/DB producer and its parsing) and *before* UI-facing operators, so data errors become error states but UI bugs still surface loudly.","**The pipeline corollary** (the high-value senior point): in a `flatMapLatest` search pipeline, a `catch` on the **inner** flow contains a failure to one search (the pipeline keeps working for the next query), while a `catch` on the **outer** flow terminates the entire pipeline permanently after the first error. Where you place `catch` literally decides whether the feature recovers.","`catch` correctly ignores `CancellationException` (it must, to not break cancellation) — but a hand-written `try/catch (e: Exception)` around a suspend call inside a flow will swallow it and break cancellation, a classic bug."]}]},{level:"senior",q:"In a flatMapLatest search pipeline, where do you put catch and why does it matter so much?",a:[{t:"p",text:"**Put `catch` on the INNER flow** — inside the `flatMapLatest` lambda, on the per-query flow — not on the outer pipeline. This is one of the most consequential placement decisions in real Flow code, because it determines whether a single failed search breaks the feature forever."},{t:"code",title:"Inner catch = recoverable; outer catch = broken forever",code:`// CORRECT — inner catch: one search fails, next keystroke still works
queryFlow.flatMapLatest { q ->
    repository.search(q)
        .map { SearchState.Results(it) }
        .onStart { emit(SearchState.Loading) }
        .catch { emit(SearchState.Error(it.message)) }   // contained here
}.collect { render(it) }

// WRONG — outer catch: first failed search terminates the WHOLE pipeline;
// queryFlow is now dead, no future query is ever processed
queryFlow.flatMapLatest { q -> repository.search(q).map { ... } }
    .catch { emit(SearchState.Error(it.message)) }
    .collect { render(it) }`},{t:"list",items:["**Why the outer catch is fatal**: when an exception reaches the outer `catch`, the outer flow *terminates* — and the outer flow is `queryFlow` itself. Once it's terminated, no further queries flow through; the user types and nothing happens. The `catch` 'handled' the error but killed the stream that feeds it.","**Why the inner catch works**: each search is its own short-lived inner flow. Catching *there* turns a failed search into an `Error` state emission that `flatMapLatest` passes along, and then the inner flow completes normally. The outer `queryFlow` never sees an exception, so it stays alive — the next keystroke starts a fresh inner search.","**General rule**: catch errors at the *narrowest* scope that should fail — the individual operation, not the long-lived stream that orchestrates operations. This applies to `combine`, `flatMapMerge`, and any long-lived flow driving short-lived sub-operations."]}]},{level:"senior",q:"What are the gotchas of testing a ViewModel that exposes stateIn(WhileSubscribed) StateFlow?",a:[{t:"p",text:"**The core gotcha**: a flow produced by `stateIn(scope, WhileSubscribed(...), initial)` is **cold until it has a collector** — `WhileSubscribed` specifically starts the upstream only when someone subscribes. So in a test, if you construct the ViewModel and immediately assert `viewModel.uiState.value`, you'll only ever see the **`initialValue`**, because nothing has subscribed to trigger the upstream. The real emissions never happen."},{t:"list",items:["**Fix — actually collect it**: use Turbine's `viewModel.uiState.test { }` (which subscribes, starting the upstream) and `awaitItem()` through the emissions, or launch a collector in the test's `backgroundScope` (`backgroundScope.launch { viewModel.uiState.collect() }`) so the sharing starts. Only then will `WhileSubscribed` run the upstream and produce real states.","**Install a TestDispatcher as Main**: `viewModelScope` uses `Dispatchers.Main`, which doesn't exist on the JVM. A JUnit rule calling `Dispatchers.setMain(testDispatcher)` / `resetMain()` is required, and it also routes the ViewModel's coroutines through virtual time.","**Dispatcher choice affects what you can observe**: `StandardTestDispatcher` queues coroutines until you `advanceUntilIdle()`/`runCurrent()`, letting you assert *intermediate* states like `Loading` before `Content`. `UnconfinedTestDispatcher` runs eagerly, which is simpler but can skip past transient states before you can assert them — pick based on whether you need to see the in-between states.","**Virtual time for operators**: if the pipeline has `debounce`/`stateIn` timeouts, use `advanceTimeBy` to move the clock; note `WhileSubscribed`'s own 5s stop-timeout can matter if you test unsubscribe behavior.","**Inject dispatchers into the ViewModel/repository**: hardcoded `Dispatchers.IO` inside escapes the test scheduler, making tests slow or flaky; constructor-inject dispatchers so the test substitutes the TestDispatcher and controls all timing."]},{t:"p",text:"**The one-sentence trap**: 'stateIn(WhileSubscribed) does nothing until collected, so a test must subscribe (Turbine or backgroundScope) before asserting — otherwise you only see initialValue.' Naming that, plus the Main-dispatcher rule and StandardvsUnconfined choice, is the complete senior answer."}]}],eT={id:"error-testing",title:"Error Handling & Testing",tagline:"Exception transparency, the catch operator & placement, inner vs outer catch, retry/retryWhen, onStart/onCompletion, and testing flows with Turbine + virtual time.",tags:["Flow","catch","retry","Turbine","Testing"],content:XS,qa:ZS},tT={id:"flows",name:"Kotlin Flows",description:"Asynchronous streams in Kotlin — cold vs hot, operators, StateFlow/SharedFlow, backpressure, threading & conversions, and error handling & testing.",topics:[NS,zS,_S,GS,QS,eT]},nT=[{heading:"What the Activity lifecycle is and why it exists",blocks:[{t:"p",text:"An **Activity** is a single screen in an Android app. Because the OS — not your app — controls when screens are shown, hidden, and destroyed (the user switches apps, gets a call, rotates the phone, the system needs memory), Android notifies your Activity of these transitions through **lifecycle callbacks**. Understanding them is fundamental because doing the *right work at the right callback* is what makes an app correct: acquire resources when visible, release them when not, and save state before you might be destroyed."},{t:"list",items:["The lifecycle exists because Android is a **resource-constrained, multitasking OS** that can destroy your Activity at any time to reclaim memory. Your app must cooperate: set up when needed, tear down when not, and be able to recreate itself.","Each callback is a signal about *visibility* and *interactivity*: created, visible, in-foreground-interactive, and the reverse on the way down."]}]},{heading:"The seven callbacks",blocks:[{t:"table",headers:["Callback","Meaning","Typical work"],rows:[["`onCreate()`","Activity created (once per instance)","inflate UI (setContent), init ViewModels, restore state"],["`onStart()`","becoming visible (not yet interactive)","start visible-only work (register some observers)"],["`onResume()`","now in foreground, interactive","start camera/sensors, resume animations, acquire exclusive resources"],["`onPause()`","losing focus (partially obscured)","pause animations, release camera, save critical data quickly (must be fast)"],["`onStop()`","no longer visible","release heavier resources, unregister observers, persist state"],["`onDestroy()`","being destroyed (finish or config change)","final cleanup"],["`onRestart()`","coming back to visible after onStop","re-prepare before onStart"]]},{t:"code",title:"The lifecycle flow",code:`onCreate -> onStart -> onResume   // launching, now interactive
                                  // ... user uses the screen ...
onPause -> onStop                 // user leaves (another app/screen)
onRestart -> onStart -> onResume  // user returns
onPause -> onStop -> onDestroy    // finishing / system destroys`},{t:"list",items:["**Visible lifetime** = `onStart` → `onStop` (the Activity is at least partially visible). **Foreground lifetime** = `onResume` → `onPause` (has focus, interactive). **Entire lifetime** = `onCreate` → `onDestroy`.","**Pairs**: acquire in one, release in its mirror — `onStart`/`onStop`, `onResume`/`onPause`, `onCreate`/`onDestroy`. Camera in `onResume`, release in `onPause`.","**`onPause` must be fast**: the next Activity can't resume until it returns, so no heavy work there — it briefly blocks the transition. Heavy persistence goes in `onStop`."]}]},{heading:"Configuration changes — the recreation trap",blocks:[{t:"p",text:"A **configuration change** (rotation, dark mode toggle, language change, window resize, font size change) by default **destroys and recreates the Activity** — full `onPause → onStop → onDestroy → onCreate → onStart → onResume`. This surprises newcomers: rotating the phone rebuilds your entire screen. Any state held in Activity fields is *lost* unless you preserve it."},{t:"list",items:["**Why recreate?** A different configuration may need different resources (a landscape layout, a different-language string, a larger font) — recreating lets Android load the right resources for the new configuration cleanly.","**What survives**: a `ViewModel` (retained across configuration changes), and small state saved via `onSaveInstanceState`/`rememberSaveable`. What's lost: everything in Activity fields, local variables, non-saved state.","**Common bug**: storing screen data in Activity fields → it vanishes on rotation → the screen resets. The fix is architectural: put state in a ViewModel (survives) and small UI state in saved instance state."]}]},{heading:"onSaveInstanceState / onRestoreInstanceState",blocks:[{t:"p",text:"`onSaveInstanceState(Bundle)` is called before an Activity *may* be destroyed (configuration change, or the system killing it in the background to reclaim memory). You write small key-value data into the `Bundle`; it's handed back in `onCreate` (and `onRestoreInstanceState`) when the Activity is recreated. This is how you preserve transient UI state across both configuration changes *and* process death."},{t:"code",title:"Saving and restoring state",code:`override fun onSaveInstanceState(outState: Bundle) {
    super.onSaveInstanceState(outState)
    outState.putString("query", currentQuery)   // small, Bundle-able data only
}

override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)
    val restored = savedInstanceState?.getString("query")
}`},{t:"list",items:["**For small transient UI state only**: scroll position, text input, selected tab. It uses the same `Bundle` mechanism as everything else, so it must be *small* and *Parcelable/Serializable* — the Binder transaction limit (~1MB, shared) means dumping large data causes `TransactionTooLargeException`.","**Not for large data**: screen data (a list of items, a user object) belongs in a ViewModel (config change) and a repository/database (process death). Save *ids* and reload, not the data itself.","**When it's called**: before a *possible* destruction — so it runs on config change and on background process kills, but NOT when the user explicitly finishes the Activity (back press / finish), because then there's nothing to restore.","**Modern equivalents**: `rememberSaveable` in Compose and `SavedStateHandle` in ViewModels both build on this same mechanism — they're the idiomatic ways to use saved instance state today."]}]},{heading:"Configuration change vs process death",blocks:[{t:"table",headers:["","Configuration change","Process death (background kill)"],rows:[["What happens","Activity destroyed & recreated; process lives","entire process killed by the system"],["ViewModel","survives (retained)","destroyed — recreated fresh"],["Activity fields / in-memory state","lost","lost"],["onSaveInstanceState Bundle","restored","restored (this is its main purpose)"],["How to test","rotate the device","background app, kill via Android Studio / adb, relaunch"]]},{t:"note",text:"Interview essentials to state: the seven callbacks and their pairs; `onPause` must be fast; configuration changes *recreate* the Activity by default (losing field state); `onSaveInstanceState` preserves *small* transient UI state across recreation AND process death; ViewModel survives config change but NOT process death — so the robust pattern is ViewModel (config change) + SavedStateHandle/persistent layer (process death)."}]}],aT=[{level:"junior",q:"Walk through the Activity lifecycle callbacks.",a:[{t:"list",items:["**`onCreate()`** — called once when the Activity is first created. Set up the UI (`setContentView`/`setContent`), initialize ViewModels, restore saved state. This is where one-time initialization goes.","**`onStart()`** — the Activity is becoming *visible* but not yet interactive. Start work that should run while visible.","**`onResume()`** — the Activity is now in the *foreground and interactive*. Acquire resources that need the screen active — camera, sensors, resume animations.","**`onPause()`** — the Activity is losing focus (a dialog appeared, or another Activity is starting). Release exclusive resources (camera) and pause animations. Must be *fast* — the next screen waits for it.","**`onStop()`** — the Activity is no longer visible. Release heavier resources, unregister observers, persist data.","**`onDestroy()`** — the Activity is being destroyed (finished or recreated for a config change). Final cleanup.","**`onRestart()`** — called when a stopped Activity is coming back, before `onStart()`."]},{t:"p",text:"The key mental model is *pairs*: you acquire in one callback and release in its mirror — `onStart`/`onStop`, `onResume`/`onPause`, `onCreate`/`onDestroy`. Launching goes create→start→resume; leaving goes pause→stop; returning goes restart→start→resume; finishing goes pause→stop→destroy. Putting the right work in the right callback (and cleaning up in the mirror) is what makes an Activity behave correctly."}]},{level:"junior",q:"What happens to an Activity when you rotate the screen?",a:[{t:"p",text:"**By default, the Activity is destroyed and completely recreated.** Rotation is a *configuration change*, and Android's default response is to run the full teardown-and-rebuild cycle: `onPause → onStop → onDestroy`, then a brand-new Activity instance goes `onCreate → onStart → onResume`. Your original Activity object is gone; a new one takes its place."},{t:"p",text:"**Why**: a new configuration (landscape orientation) may require different resources — a landscape layout, different dimensions — and recreating lets Android cleanly load the correct resources for the new configuration. **The consequence**: anything stored in Activity fields or local variables is *lost*. If you kept the screen's data in an Activity property, the screen resets on rotation — a classic bug. What survives is a `ViewModel` (which is retained across configuration changes) and small state written to `onSaveInstanceState`. So the correct approach is architectural: hold screen state in a ViewModel and small transient UI state (scroll position, text input) in saved instance state (`rememberSaveable`/`SavedStateHandle`), never in Activity fields."}]},{level:"junior",q:"What is onSaveInstanceState used for?",a:[{t:"p",text:"**The concept**: `onSaveInstanceState(Bundle)` is called before the Activity *might* be destroyed — on a configuration change, or when the system kills your app in the background to reclaim memory. You write small pieces of transient UI state into the `Bundle`, and it's returned to you in `onCreate` (and `onRestoreInstanceState`) when the Activity is recreated, so you can restore the screen to how the user left it."},{t:"list",items:["**Use it for small transient UI state**: scroll position, text field contents, which tab was selected, a search query. Things that would annoy the user to lose but aren't the actual screen data.","**Not for large data**: it uses the `Bundle`/Binder mechanism with a ~1MB shared transaction limit, so putting big lists or objects in it risks `TransactionTooLargeException`. Save *ids* and reload the data, not the data itself.","**Screen data belongs elsewhere**: a ViewModel holds it across config changes; a database/repository provides it after process death."]},{t:"p",text:"It's important because it's the *only* mechanism that survives **process death** — when the system kills your backgrounded app entirely, the ViewModel is gone, but the saved-instance-state Bundle is preserved by the system and handed back on relaunch. Modern APIs like `rememberSaveable` (Compose) and `SavedStateHandle` (ViewModel) are built on this exact mechanism and are the idiomatic ways to use it today."}]},{level:"junior",q:"Why should onPause be fast, and what should you not do in it?",a:[{t:"p",text:"**Because `onPause` runs during a screen transition, and the incoming Activity cannot become interactive (`onResume`) until the outgoing one's `onPause` returns.** So any slow work in `onPause` directly delays the next screen appearing — the user sees a lag or freeze during navigation. It's on the critical path of every transition away from the screen."},{t:"list",items:["**Don't do heavy work in `onPause`**: no database writes of large data, no network calls, no expensive computation. These block the transition.","**Do release exclusive/lightweight resources**: pause animations, release the camera (so another app can use it), unregister a sensor — quick operations that must happen the moment the screen loses focus.","**Put heavier persistence in `onStop`** instead: `onStop` isn't on the transition's critical path in the same way, so saving data or releasing heavier resources belongs there. Historically people over-used `onPause` for saving; modern guidance is to save in `onStop` (or continuously via a ViewModel/repository) and keep `onPause` minimal."]},{t:"p",text:"The rule of thumb: `onPause` is for *quick* 'I'm losing focus' cleanup; `onStop` is for 'I'm no longer visible' work that can afford to take a bit longer. Keeping `onPause` fast keeps navigation snappy."}]},{level:"senior",q:"Explain the difference between configuration change and process death, and how you build for both.",a:[{t:"p",text:"**They're two different ways your Activity can be torn down, and they preserve different things.** A *configuration change* (rotation, dark mode, locale, resize) destroys and recreates the Activity, but the *process stays alive* — so the ViewModel (which is retained) survives, and only Activity-instance state is lost. *Process death* is when the system kills your entire backgrounded process to reclaim memory — the ViewModel and all in-memory state are gone; only the saved-instance-state Bundle (held by the system) survives."},{t:"table",headers:["Survives?","Config change","Process death"],rows:[["ViewModel / in-memory state","yes / yes","no / no"],["onSaveInstanceState Bundle","restored","restored"],["Activity fields","lost","lost"]]},{t:"list",items:["**Build strategy — layered**: hold screen state in a **ViewModel** (covers configuration changes for free, in memory, no serialization). Hold the *minimal identity/input* needed to rebuild — selected id, query text, scroll anchor — in **`SavedStateHandle`** (which is backed by saved instance state, so it survives process death too). And treat the **persistent layer** (database/repository) as the source of truth for actual data, reloading it on recreation using the saved id.","**The recreation path is the same for both**: on recreate, the ViewModel reads its id from `SavedStateHandle` and re-subscribes to the repository — identical to first launch, so process-death recovery isn't a special code path. That's the elegance of the pattern.","**Don't over-save**: putting large data in `SavedStateHandle`/`onSaveInstanceState` risks `TransactionTooLargeException` (Binder limit). Save ids and reload; never parcel entire lists.","**Test both distinctly**: rotate to test config changes; to test process death, background the app and kill it via Android Studio's 'Terminate' or `adb shell am kill <pkg>`, then relaunch from recents. Rotation testing alone gives false confidence — it never exercises the ViewModel-is-gone path."]},{t:"p",text:"**The common interview trap**: 'does the ViewModel survive process death?' — *No*. It survives configuration changes only. Candidates frequently get this wrong. The complete answer is the layered strategy: ViewModel for config changes, SavedStateHandle (+ persistent layer) for process death, and reloading data from the source of truth rather than trying to serialize it all."}]},{level:"senior",q:"How can you handle configuration changes without the Activity being recreated, and why is that usually discouraged?",a:[{t:"p",text:'**You can opt out of automatic recreation by declaring `android:configChanges` in the manifest** — listing the configuration types you\'ll handle yourself (e.g. `android:configChanges="orientation|screenSize|keyboardHidden"`). When one of those changes, Android does *not* recreate the Activity; instead it calls `onConfigurationChanged(newConfig)`, and you manually update whatever needs updating.'},{t:"list",items:["**Why it's usually discouraged**: by handling config changes yourself, you take responsibility for reloading *all* configuration-dependent resources — layouts, dimensions, strings, drawables — for the new configuration. Miss one and you get subtle bugs (a landscape layout that never loads, a string that stays in the old language). The system's recreation does all of this correctly and automatically; opting out means reimplementing it by hand, error-prone.","**It doesn't solve the real problem**: people reach for `configChanges` to 'avoid losing state on rotation', but the *correct* solution to state loss is a ViewModel + saved instance state, which also handles process death (which `configChanges` does nothing for). So `configChanges` treats the symptom while leaving the actual robustness gap.","**Legitimate uses exist**: it's appropriate for specific cases like a full-screen video player or a game rendering with a custom `SurfaceView`/OpenGL context, where recreating would cause a jarring reload or lose a GPU context, and where there are few configuration-dependent resources to manage. Compose also handles many config changes gracefully within a single Activity, reducing the need."]},{t:"p",text:"**The senior framing**: `configChanges` trades the system's correct-but-recreating behavior for manual control, and it's the *wrong tool* for state preservation (use ViewModel + SavedStateHandle for that). Reserve it for the narrow cases — media/rendering surfaces — where recreation is genuinely undesirable and you can correctly handle the resource updates yourself. Reaching for it to 'fix rotation bugs' is an anti-pattern that hides the real architectural fix."}]}],iT={id:"activity-lifecycle",title:"Activity Lifecycle & State",tagline:"The seven callbacks and their pairs, configuration changes & recreation, onSaveInstanceState, and config change vs process death.",tags:["Android","Activity","Lifecycle","onSaveInstanceState","Process Death"],content:nT,qa:aT},oT=[{heading:"What a Fragment is",blocks:[{t:"p",text:"A **Fragment** is a reusable, modular portion of UI that lives *inside* an Activity (or another Fragment). Think of it as a 'sub-screen' with its own lifecycle, layout, and behavior, hosted by an Activity. Fragments were introduced to support **multiple screen sizes** (a tablet showing a list and detail side-by-side, a phone showing one at a time) and to enable **reusable UI components** and **navigation** within a single Activity."},{t:"list",items:["A Fragment can't exist on its own — it must be hosted by an Activity (or parent Fragment) and added to a container view via the `FragmentManager`.","**Single-Activity architecture**: the modern Android pattern is one Activity hosting many Fragments (or many Compose destinations), with the Navigation component managing transitions — rather than one Activity per screen.","Fragments have their own lifecycle that's *tied to but distinct from* the host Activity's — which is the source of most Fragment complexity and bugs."]}]},{heading:"The Fragment lifecycle — and the crucial view lifecycle",blocks:[{t:"p",text:"A Fragment has *two* lifecycles you must distinguish: the **Fragment instance lifecycle** and the **Fragment's view lifecycle**. This distinction is the #1 source of Fragment bugs and interview questions."},{t:"code",title:"The main callbacks",code:`onAttach()          // attached to its host (Activity)
onCreate()          // fragment instance created (no view yet)
onCreateView()      // create/inflate the fragment's VIEW hierarchy
onViewCreated()     // view is ready — set up UI, observe data HERE
onStart / onResume  // visible / interactive (mirrors Activity)
onPause / onStop    // losing focus / not visible
onDestroyView()     // the fragment's VIEW is destroyed (but the fragment lives!)
onDestroy()         // the fragment instance is destroyed
onDetach()          // detached from host`},{t:"list",items:["**The instance lifecycle** (`onCreate` → `onDestroy`) tracks the Fragment *object*. **The view lifecycle** (`onCreateView` → `onDestroyView`) tracks the Fragment's *UI*. The key insight: **a Fragment's view can be destroyed and recreated while the Fragment instance stays alive.**","This happens when a Fragment goes onto the **back stack**: navigate away, and `onDestroyView` runs (the view is torn down to save memory), but `onDestroy` does *not* — the Fragment instance is retained. Navigate back, and `onCreateView` runs again, building a *new* view for the *same* Fragment instance.","**Consequence**: the Fragment instance can outlive multiple views. Observing data or holding view references using the Fragment's own lifecycle (instead of the view's) leaks the old destroyed view or crashes when the callback fires on a dead view."]}]},{heading:"viewLifecycleOwner — the fix for the two-lifecycle problem",blocks:[{t:"p",text:"Because the view can be destroyed while the Fragment lives, you must tie view-related work (observing LiveData/Flow, view bindings) to the **view's** lifecycle, not the Fragment's. `viewLifecycleOwner` is a `LifecycleOwner` scoped to the current view — it's created in `onCreateView` and destroyed in `onDestroyView`."},{t:"code",title:"Always observe with viewLifecycleOwner",code:`override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
    // CORRECT — tied to the view's lifecycle
    viewLifecycleOwner.lifecycleScope.launch {
        viewLifecycleOwner.repeatOnLifecycle(Lifecycle.State.STARTED) {
            viewModel.uiState.collect { render(it) }
        }
    }

    // WRONG — tied to the FRAGMENT's lifecycle:
    // lifecycleScope.launch { viewModel.uiState.collect { } }
    // On back-stack return, the OLD collector is still active AND a new one
    // starts -> duplicate collectors, updating a destroyed view -> crash/leak.
}`},{t:"list",items:["**Use `viewLifecycleOwner`** (not `this`) for: observing LiveData, collecting flows, anything touching views. Its lifecycle matches the view, so observers are removed in `onDestroyView` — no leaks, no updates to dead views.","**The ViewBinding leak**: the same reason you null out a ViewBinding in `onDestroyView` (`_binding = null`) — the binding references views that are destroyed, so holding it past `onDestroyView` leaks the view hierarchy. The standard pattern uses a nullable backing property cleared in `onDestroyView`.","**Rule**: view-related → `viewLifecycleOwner`; fragment-instance-related (rare) → `this`. When in doubt for UI work, it's the view lifecycle."]}]},{heading:"Communication: between Fragments and with the Activity",blocks:[{t:"list",items:["**Shared ViewModel** (the modern way): scope a ViewModel to the host Activity (`by activityViewModels()`) or a navigation graph, so multiple Fragments share the same instance and communicate through its state. Clean and lifecycle-safe.","**Fragment Result API**: `setFragmentResult(key, bundle)` / `setFragmentResultListener(key) { }` — for one-off results between a Fragment and its parent (e.g. a picker Fragment returning a selection). Replaces the old interface-callback pattern.","**Navigation component**: for moving between Fragments with type-safe arguments and a managed back stack — the standard for Fragment navigation, analogous to Navigation Compose.","**Avoid**: direct references between Fragments (tight coupling, lifecycle hazards) and the old 'Fragment implements a callback interface the Activity calls' pattern (boilerplate, leak-prone)."]}]},{heading:"Fragments vs Activities, and the modern picture",blocks:[{t:"list",items:["**Activity** = an entry point / window managed by the OS, heavier, declared in the manifest. **Fragment** = a lighter UI module hosted within an Activity, managed by your `FragmentManager`, not in the manifest. Fragments enable multiple UI panels in one Activity and reusable UI.","**Single-Activity architecture** became the recommendation: one Activity, many Fragments (or Compose destinations) + Navigation component. Fewer Activities means simpler manifest, easier shared state (activity-scoped ViewModel), and smoother transitions.","**With Jetpack Compose**, Fragments become largely optional — you can have a single Activity hosting Compose destinations with Navigation-Compose and no Fragments at all. But Fragments remain ubiquitous in existing codebases, and interop (`ComposeView` in a Fragment, or a Fragment in a Compose-based app) is common — so understanding Fragment lifecycle is still essential."]},{t:"note",text:"The Fragment essentials to nail: the two lifecycles (instance vs view) and why they differ (back stack destroys the view but keeps the instance); always use `viewLifecycleOwner` for view-related observation and null ViewBinding in `onDestroyView`; communicate via shared ViewModels or the Fragment Result API, never direct references; and know that single-Activity + Navigation (or Compose) is the modern architecture."}]}],sT=[{level:"junior",q:"What is a Fragment and why do Fragments exist?",a:[{t:"p",text:"**The concept**: a Fragment is a reusable, modular piece of UI with its own lifecycle and layout that lives *inside* an Activity. It's like a sub-screen or UI component hosted by an Activity — it can't exist on its own. You add it to a container in the Activity via the `FragmentManager`."},{t:"p",text:"**Why they exist**: two main reasons. First, **adaptive layouts for different screen sizes** — on a tablet you might show a list Fragment and a detail Fragment side by side, while on a phone you show them one at a time; Fragments let you compose the same UI pieces differently per device. Second, **reusable UI and single-Activity navigation** — instead of one Activity per screen (heavy, manifest-declared, awkward to share state), you have one Activity hosting many Fragments that you navigate between, sharing an activity-scoped ViewModel. Modern Android favors this single-Activity architecture with the Navigation component. With Jetpack Compose, Fragments become optional (Compose destinations replace them), but they remain everywhere in existing codebases."}]},{level:"junior",q:"What is the difference between a Fragment and an Activity?",a:[{t:"list",items:["**Ownership/management**: an Activity is an app entry point managed by the *OS* and declared in the manifest; a Fragment is a UI module managed by *your* `FragmentManager` and not in the manifest. Fragments live inside Activities.","**Weight**: Activities are heavier (full window, OS-tracked); Fragments are lighter UI components you can add, remove, and swap within one Activity.","**Multiplicity**: one Activity can host many Fragments simultaneously (multi-pane layouts) or swap them (navigation), enabling single-Activity architecture.","**Lifecycle**: both have lifecycles, but a Fragment has *two* (instance and view), and its lifecycle is tied to — but distinct from — its host Activity's."]},{t:"p",text:"The practical takeaway: use Activities as coarse app entry points (often just one, or a few), and Fragments (or Compose destinations) as the actual screens within them. This gives simpler shared state (activity-scoped ViewModels), smoother transitions, and a cleaner manifest than one-Activity-per-screen. The main cost is Fragment lifecycle complexity — especially the two-lifecycle issue — which is why they have a reputation for being tricky."}]},{level:"junior",q:"Where should you set up UI and observe data in a Fragment — onCreateView, onViewCreated, or onCreate?",a:[{t:"p",text:"**Set up UI and observe data in `onViewCreated`.** The reasoning: `onCreate` runs before the view exists (so you can't touch views there); `onCreateView` is specifically for *creating/inflating* the view hierarchy and returning it (with ViewBinding, this is where you inflate); and `onViewCreated` runs right after, when the view is fully created and ready — the correct place to configure views, set click listeners, and start observing data to render into the view."},{t:"code",title:"The standard structure",code:`override fun onCreateView(inflater, container, saved): View {
    _binding = FragmentBinding.inflate(inflater, container, false)
    return binding.root                       // just create the view
}

override fun onViewCreated(view: View, saved: Bundle?) {
    super.onViewCreated(view, saved)
    binding.button.setOnClickListener { }     // configure views here
    viewLifecycleOwner.lifecycleScope.launch { // observe data here
        viewModel.uiState.collect { render(it) }
    }
}

override fun onDestroyView() {
    super.onDestroyView()
    _binding = null                            // avoid leaking the view
}`},{t:"p",text:"And crucially, when observing, use `viewLifecycleOwner` (not `this`), because the observation should be tied to the *view's* lifetime, not the Fragment instance's. Do fragment-instance-level setup (that doesn't touch views) in `onCreate`, and view-level setup in `onViewCreated`."}]},{level:"junior",q:"How do two Fragments communicate with each other?",a:[{t:"p",text:"**The modern, recommended way is a shared ViewModel** — scope a ViewModel to the host Activity (or a navigation graph) with `by activityViewModels()`, so both Fragments get the *same* ViewModel instance and communicate through its shared state. Fragment A updates the ViewModel's state; Fragment B observes it. This is clean, lifecycle-safe, and decoupled — neither Fragment references the other."},{t:"list",items:["**Shared ViewModel** (`activityViewModels()` or nav-graph-scoped) — for ongoing shared state between Fragments. The go-to approach.","**Fragment Result API** — `setFragmentResult(key, bundle)` and `setFragmentResultListener(key) { }` — for one-off results, like a picker Fragment returning a selection to the Fragment that launched it. Replaces the old callback-interface pattern.","**Navigation component with arguments** — for passing data when navigating from one Fragment to another (type-safe args)."]},{t:"p",text:"What to *avoid*: direct references from one Fragment to another (tight coupling, and lifecycle hazards since Fragments come and go), and the old pattern where a Fragment defines a callback interface that the Activity implements to relay messages (boilerplate and leak-prone). The principle is the same as elsewhere in Android — communicate through shared state or explicit result channels, not direct object references."}]},{level:"senior",q:"Explain the difference between a Fragment's lifecycle and its view lifecycle, and why it matters.",a:[{t:"p",text:"**A Fragment has two distinct lifecycles: the *instance* lifecycle (`onCreate` → `onDestroy`, tracking the Fragment object) and the *view* lifecycle (`onCreateView` → `onDestroyView`, tracking its UI). The critical fact is that the view can be destroyed and recreated while the Fragment instance stays alive.** This happens on the back stack: when you navigate away from a Fragment that's added to the back stack, Android calls `onDestroyView` to tear down its view (freeing memory) but does *not* call `onDestroy` — the Fragment instance is retained. Navigate back, and `onCreateView` runs again, creating a *brand-new view* for the *same* Fragment instance."},{t:"list",items:["**Why it matters — leaks and crashes**: if you observe data or hold references using the Fragment's *own* lifecycle (`this`), those observers survive across `onDestroyView`. So when you return to the Fragment, you have the old observer (still trying to update the *destroyed* view — crash, or a leak of that view) *plus* a new observer registered in the new `onViewCreated` — duplicate observers, one of them pointed at a dead view.","**The fix — `viewLifecycleOwner`**: for anything view-related (observing LiveData/Flow, updating views), use `viewLifecycleOwner`, whose lifecycle matches the *view* (created in `onCreateView`, destroyed in `onDestroyView`). Then observers are automatically removed in `onDestroyView`, so there's exactly one live observer tied to the current view, and none pointing at destroyed views.","**The ViewBinding corollary**: a ViewBinding holds references to the view hierarchy, so keeping it past `onDestroyView` leaks the whole destroyed view. The standard pattern is a nullable backing field set to null in `onDestroyView` (`_binding = null`), forcing you to only access the binding while the view exists."]},{t:"code",title:"The bug the two-lifecycle distinction causes",code:`// BUG: 'this' (fragment lifecycle) outlives the view
lifecycleScope.launch { viewModel.data.collect { binding.text.text = it } }
// After back-stack return: old collector alive (binding is stale/null) + new one

// CORRECT: viewLifecycleOwner matches the view
viewLifecycleOwner.lifecycleScope.launch {
    viewLifecycleOwner.repeatOnLifecycle(STARTED) {
        viewModel.data.collect { binding.text.text = it }
    }
}`},{t:"p",text:"**The senior framing**: the two lifecycles exist because Fragments optimize memory by destroying views of back-stacked Fragments while keeping the (cheaper) instance and its state. That optimization means 'the Fragment' and 'the Fragment's view' have different lifetimes, and *every* view-touching operation must be scoped to the view lifecycle (`viewLifecycleOwner`) or you get the classic leak/crash. This single distinction explains the majority of Fragment bugs, which is why it's such a common senior question."}]},{level:"senior",q:"Why is a ViewModel scoped to a Fragment vs its Activity vs a navigation graph, and how do you choose?",a:[{t:"p",text:"**A ViewModel's scope determines how long it lives and who shares it — you choose the scope to match the lifetime and sharing you need.** Each scope corresponds to a `ViewModelStoreOwner` whose lifecycle bounds the ViewModel."},{t:"list",items:["**Fragment-scoped** (`by viewModels()`) — the ViewModel lives as long as *this* Fragment, cleared when the Fragment is permanently removed. Use for state private to one screen. It survives that Fragment's configuration changes but is not shared with anyone.","**Activity-scoped** (`by activityViewModels()`) — one ViewModel shared by *all* Fragments in the host Activity, living as long as the Activity. Use to **share state between sibling Fragments** (the classic master-detail communication) or for state that should persist across several Fragments in the same Activity. The caution: it lives for the whole Activity, so it can accumulate state and be overkill if only a couple of Fragments need it briefly.","**Navigation-graph-scoped** (`by navGraphViewModels(R.id.checkout_graph)` / `hiltViewModel(backStackEntry)`) — one ViewModel shared by all Fragments *within a specific navigation (sub)graph*, cleared when you leave that graph. Use for **multi-step flows** — a checkout or onboarding wizard — where several screens share state that should be scoped to *just that flow* and cleaned up when it ends. This is often better than activity-scoping because the lifetime matches the flow, not the whole app session."]},{t:"p",text:"**How to choose**: match the ViewModel's lifetime to the *breadth and duration* of what needs the state. State for one screen → Fragment scope. State shared across sibling screens for the Activity's lifetime → Activity scope. State shared across a bounded multi-screen flow that should be discarded afterward → nav-graph scope (the sweet spot for wizards). The anti-pattern to avoid is activity-scoping *everything* as a lazy 'share it globally' habit — that outlives its usefulness, accumulates stale state, and blurs data ownership. The right scope gives you sharing *and* correct, automatic cleanup."}]}],rT={id:"fragments",title:"Fragments & Lifecycle",tagline:"What Fragments are, the instance vs view lifecycle, viewLifecycleOwner & ViewBinding leaks, communication patterns, and ViewModel scoping.",tags:["Android","Fragments","viewLifecycleOwner","Lifecycle"],content:oT,qa:sT},lT=[{heading:"Intents — the messaging system",blocks:[{t:"p",text:"An **Intent** is a messaging object used to request an action from a component — most commonly to *start an Activity*, but also to start services or deliver broadcasts. It's how components (even across different apps) communicate. There are two kinds: **explicit** (you name the exact component) and **implicit** (you describe *what you want done* and let the system find a component that can do it)."},{t:"code",title:"Explicit vs implicit intents",code:`// EXPLICIT — you name the target component (within your app)
val intent = Intent(this, DetailActivity::class.java).apply {
    putExtra("itemId", 42)
}
startActivity(intent)

// IMPLICIT — describe an action; the system finds a handler
val share = Intent(Intent.ACTION_SEND).apply {
    type = "text/plain"
    putExtra(Intent.EXTRA_TEXT, "Check this out")
}
startActivity(Intent.createChooser(share, "Share via"))

// Implicit: open a URL — any browser can handle it
startActivity(Intent(Intent.ACTION_VIEW, Uri.parse("https://example.com")))`},{t:"list",items:["**Explicit intent** — names the target class directly. Used for navigation *within your app* (you know exactly what to launch). Required for starting your own components securely.","**Implicit intent** — specifies an *action* (`ACTION_VIEW`, `ACTION_SEND`, `ACTION_DIAL`), optional *data* (a URI), and *category*; the system matches it against apps' declared `<intent-filter>`s and either launches the handler or shows a chooser. Used for cross-app actions: share, open URL, dial, pick a photo.","**Extras** — key/value data attached via `putExtra`; the receiver reads them from `intent`. Also `data` (a URI) and `flags` (task/launch behavior).","**`PendingIntent`** — a wrapper that lets *another* app or the system fire an intent *on your behalf later* (used by notifications, alarms, widgets). It grants the holder permission to execute your intent."]}]},{heading:"Intent filters — how implicit intents get resolved",blocks:[{t:"p",text:"An app declares what implicit intents it can handle via **`<intent-filter>`** elements in the manifest. When an implicit intent is sent, the system matches it against all installed apps' filters by **action**, **data** (scheme/host/mime type), and **category**. If exactly one matches it launches directly; if several match, the user sees a chooser."},{t:"code",title:"Declaring the app can handle a deep link",code:`<activity android:name=".ProductActivity">
    <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="shop.example.com"
              android:pathPrefix="/product" />
    </intent-filter>
</activity>`},{t:"list",items:["This is how **deep links** work: a URL or notification intent resolves to your Activity because its filter matches. `autoVerify` + a Digital Asset Links file makes it an **App Link** (opens your app directly without a chooser).","The `LAUNCHER` filter (`ACTION_MAIN` + `CATEGORY_LAUNCHER`) is what puts an Activity's icon in the app drawer as the entry point.",'Security note: an Activity with an intent filter is **exported** (callable by other apps) unless you set `android:exported="false"` — a common security consideration.']}]},{heading:"Tasks and the back stack",blocks:[{t:"p",text:"A **task** is a collection of Activities the user interacts with to accomplish something, arranged in a **back stack** (a stack of Activities). When you launch an Activity, it's normally *pushed* onto the current task's back stack; pressing Back *pops* it. The task is what you see in the Recents/Overview screen. Understanding tasks and the back stack is essential for controlling navigation behavior."},{t:"list",items:["**Default behavior**: `startActivity` pushes the new Activity onto the current task's stack. Back pops to the previous one. Simple LIFO.","**The back stack can contain multiple instances** of the same Activity by default — navigate A → B → A and you have two A instances stacked. Sometimes that's wrong (you want to return to the *existing* A, not create a new one), which is what launch modes and flags control.","**Recents**: each task appears as a card; the OS may destroy Activities within a backgrounded task to reclaim memory (process death), recreating them from saved state when the user returns."]}]},{heading:"Launch modes — controlling how Activities are added to tasks",blocks:[{t:"p",text:"**Launch modes** control whether launching an Activity creates a new instance or reuses an existing one, and in which task. Set via `android:launchMode` in the manifest (or Intent flags at runtime). The four modes:"},{t:"table",headers:["Mode","Behavior"],rows:[["`standard` (default)","always creates a new instance, pushed on the current task — duplicates allowed"],["`singleTop`","if an instance is already at the TOP of the stack, reuse it (onNewIntent) instead of creating a new one"],["`singleTask`","only one instance exists in the whole system; if it exists, bring its task forward and clear Activities above it (onNewIntent)"],["`singleInstance`","like singleTask, but the Activity is the ONLY one in its task — no other Activities can join it"]]},{t:"list",items:["**`standard`** — the default; every launch is a new instance. Fine for most screens.","**`singleTop`** — prevents *consecutive duplicates*. If A is on top and you launch A again, the existing one receives the new intent via `onNewIntent()` rather than stacking a second A. Common for a search or notification target you don't want duplicated when already showing.","**`singleTask`** — a *single* system-wide instance; launching it brings its task to the front and clears everything above it in that task. Used for a main/home Activity or a document root — an entry point that should be unique.","**`singleInstance`** — the strictest: sole instance in its own dedicated task, nothing else allowed in that task. Rare — for special standalone screens (e.g. a launcher, some telephony screens)."]}]},{heading:"Intent flags — runtime control of task behavior",blocks:[{t:"list",items:["**`FLAG_ACTIVITY_NEW_TASK`** — start the Activity in a new task (required when launching from a non-Activity context like a Service or notification).","**`FLAG_ACTIVITY_CLEAR_TOP`** — if the target already exists in the stack, clear all Activities above it and deliver the intent to it (rather than making a new one). Common for 'go back to home and clear the stack'.","**`FLAG_ACTIVITY_SINGLE_TOP`** — the flag equivalent of `singleTop` behavior for this launch.","**`CLEAR_TASK` + `NEW_TASK`** — clear the entire task and start fresh (e.g. logout → return to login with no back-stack history).","Flags let you apply launch behavior *per launch* at runtime, whereas `launchMode` sets it *per Activity* in the manifest. Flags override/augment the manifest mode for that specific launch."]},{t:"note",text:"The essentials: Intents (explicit = name the target, implicit = describe an action for the system to resolve via intent filters); PendingIntent lets others fire your intent later; tasks are back stacks of Activities; launch modes (standard/singleTop/singleTask/singleInstance) and Intent flags (NEW_TASK, CLEAR_TOP, SINGLE_TOP, CLEAR_TASK) control instance reuse and task placement. onNewIntent() delivers intents to a reused instance."}]}],cT=[{level:"junior",q:"What is the difference between an explicit and an implicit Intent?",a:[{t:"p",text:"**An Intent is a messaging object that requests an action — usually to start an Activity. The difference is whether you name the target or describe what you want done.** An *explicit* intent names the exact component (`Intent(this, DetailActivity::class.java)`) — you know precisely what to launch, used for navigation within your own app. An *implicit* intent describes an *action* (`ACTION_VIEW`, `ACTION_SEND`) and optional data, and lets the system find an app that can handle it."},{t:"code",title:"Both kinds",code:`// Explicit — launch a known component
startActivity(Intent(this, DetailActivity::class.java))

// Implicit — 'open this URL', system finds a browser
startActivity(Intent(Intent.ACTION_VIEW, Uri.parse("https://x.com")))`},{t:"p",text:"Use **explicit** for internal navigation (and it's required for security when starting your own components). Use **implicit** for cross-app actions where you don't care *which* app does it — sharing text, opening a URL, dialing a number, picking a photo. The system matches implicit intents against apps' declared `<intent-filter>`s; if multiple apps can handle it, the user gets a chooser. You attach data with `putExtra` (extras) and the receiver reads it from its `intent`."}]},{level:"junior",q:"What is an intent filter?",a:[{t:"p",text:"**The concept**: an `<intent-filter>` is a manifest declaration that tells the system *what implicit intents a component can handle*. It specifies an **action** (what to do, like `VIEW` or `SEND`), optional **data** (a URI scheme/host/path or MIME type), and a **category**. When an implicit intent is fired, Android matches it against every app's intent filters to find components that can respond."},{t:"list",items:["**It's how implicit intents get resolved**: your intent says 'ACTION_VIEW this https URL', and any Activity whose filter declares it handles `VIEW` + `https` data becomes a candidate.","**Deep links** use intent filters — declaring a filter for your app's URLs (`scheme=https, host=shop.example.com`) lets those links open your app. Adding `autoVerify` + a Digital Asset Links file makes it an **App Link** that opens directly without a chooser.","**The launcher icon** comes from a special filter: `ACTION_MAIN` + `CATEGORY_LAUNCHER` marks the Activity as the app's entry point in the drawer.",'**Security implication**: declaring an intent filter makes the component *exported* (other apps can invoke it) unless you explicitly set `android:exported="false"`.']}]},{level:"junior",q:"What is a task and the back stack in Android?",a:[{t:"p",text:"**The concept**: a *task* is a collection of Activities the user works through to accomplish something, and the *back stack* is how those Activities are arranged — a stack (last-in, first-out). When you launch an Activity, it's pushed onto the current task's back stack; pressing the Back button pops the top one, returning to the previous. A task is what appears as a card in the Recents/Overview screen."},{t:"p",text:"By default it's simple LIFO: navigate A → B → C and the stack is [A, B, C]; Back takes you C → B → A. One thing to know is that, by default, launching an Activity *always creates a new instance* — so A → B → A leaves two separate A instances on the stack. Sometimes that's not what you want (you'd rather return to the existing A), which is exactly what launch modes and Intent flags let you control. Also, when a task is in the background, the system may destroy its Activities to reclaim memory and recreate them from saved instance state when the user returns — which is why saving state matters."}]},{level:"junior",q:"What is a PendingIntent?",a:[{t:"p",text:"**The concept**: a `PendingIntent` is a wrapper around an Intent that you hand to *another* app or the system so *they* can execute it *later, on your behalf* — with your app's identity and permissions. A normal Intent you fire yourself, immediately; a PendingIntent is a 'token' that lets someone else fire your intent at a future time."},{t:"p",text:"**Where it's used**: notifications (when the user taps the notification, the system fires your PendingIntent to open your app), alarms (`AlarmManager` fires it at the scheduled time), app widgets, and any callback where an external component needs to trigger your Activity/Service later. You create it with `PendingIntent.getActivity(...)` / `getBroadcast(...)` / `getService(...)`. Two things to know: on Android 12+ you *must* specify mutability (`FLAG_IMMUTABLE` or `FLAG_MUTABLE`) — usually `FLAG_IMMUTABLE` for security unless something needs to fill in the intent; and flags like `FLAG_UPDATE_CURRENT` control what happens if a matching PendingIntent already exists. The key idea is that it delegates the *permission to execute* your intent to another process safely."}]},{level:"senior",q:"Explain the four launch modes and when you'd use each.",a:[{t:"p",text:"**Launch modes control whether launching an Activity creates a new instance or reuses an existing one, and in which task.** They're set via `android:launchMode` in the manifest."},{t:"list",items:["**`standard`** (default) — every launch creates a *new* instance pushed onto the current task, duplicates allowed. Correct for most screens (a detail screen you can stack multiple of). Use unless you have a reason not to.","**`singleTop`** — if an instance is *already at the top* of the stack, reuse it (its `onNewIntent()` is called) instead of creating a duplicate. Use to avoid consecutive duplicates — e.g. a search results screen or a notification target that shouldn't stack a second copy when it's already showing. Note: it only reuses if the instance is *on top*; if it's lower in the stack, a new one is still created.","**`singleTask`** — only *one* instance can exist system-wide. Launching it brings its task to the front and *clears any Activities above it* in that task (delivering the intent via `onNewIntent()`). Use for a unique entry point — a home/main screen or a document root that should have exactly one instance you return to, not multiple.","**`singleInstance`** — like `singleTask`, but the Activity is the *only* Activity allowed in its task; any Activity it launches goes into a *different* task. The strictest and rarest — for special standalone screens (a launcher, an incoming-call screen) that must be isolated."]},{t:"p",text:"**How to reason about it**: the question is always 'should launching this create a new instance or reuse an existing one, and should it be unique?' Most screens → `standard`. Don't-duplicate-when-on-top → `singleTop`. Unique-entry-point-that-clears-above-it → `singleTask`. Fully-isolated-single-instance → `singleInstance`. A subtle senior point: launch modes interact with Intent *flags* (which apply per-launch at runtime and can override the manifest mode), and `singleTask`/`singleInstance` affect *task affinity* and can produce surprising back-stack behavior, so they should be used deliberately, not casually."}]},{level:"senior",q:"How do you clear the back stack when a user logs out, returning to the login screen with no history?",a:[{t:"p",text:"**You launch the login (or home) Activity with Intent flags that clear the existing task, so the user can't press Back into the now-invalid authenticated screens.** The standard combination is `FLAG_ACTIVITY_NEW_TASK` + `FLAG_ACTIVITY_CLEAR_TASK`, which finishes all Activities in the current task and starts the target as the new root — an empty back stack behind it."},{t:"code",title:"Clearing the stack on logout",code:`fun logout() {
    authRepository.clearSession()
    val intent = Intent(this, LoginActivity::class.java).apply {
        flags = Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TASK
    }
    startActivity(intent)
    finish()   // finish the current Activity too
}`},{t:"list",items:["**`FLAG_ACTIVITY_CLEAR_TASK`** (requires `NEW_TASK`) — clears the *entire* existing task before starting the target, so nothing remains behind it. Pressing Back from login exits the app, not into the authenticated screens. This is the cleanest logout approach.","**Alternative — `FLAG_ACTIVITY_CLEAR_TOP` + `SINGLE_TOP`**: if login is already lower in the stack, this clears everything above it and reuses it. But `CLEAR_TASK` is more robust for logout because it doesn't depend on login being in the stack.","**In single-Activity / Navigation-component apps**: you'd instead use the Navigation API — `navController.navigate(loginRoute) { popUpTo(0) { inclusive = true } }` (or `popUpTo(nav_graph) { inclusive = true }`) to pop the entire back stack. Same concept (clear history), expressed through Navigation rather than task flags. In Navigation-Compose it's `popUpTo` with `inclusive`.","**Why it matters beyond UX**: leaving authenticated Activities on the back stack after logout is also a *security/privacy* issue — the user (or someone else) could Back-navigate into screens showing personal data from the previous session. Clearing the stack prevents that."]},{t:"p",text:"**The senior framing**: logout isn't just navigation — it's invalidating a security boundary, so you must ensure no path (Back button, Recents) leads back into authenticated state. `CLEAR_TASK` + `NEW_TASK` (or `popUpTo(inclusive)` in Navigation) enforces that by wiping the history, and you pair it with clearing the session/tokens so even a restored Activity can't show stale authenticated data."}]}],dT={id:"intents-tasks",title:"Intents, Tasks & Launch Modes",tagline:"Explicit vs implicit intents, intent filters & deep links, PendingIntent, tasks & the back stack, the four launch modes, and intent flags.",tags:["Android","Intents","Launch Modes","Back Stack","Deep Links"],content:lT,qa:cT},hT=[{heading:"The four app components — a quick map",blocks:[{t:"p",text:"Android apps are built from four types of **components**, each an entry point the system can start: **Activity** (a screen — covered separately), **Service** (long-running background work with no UI), **BroadcastReceiver** (responds to system/app-wide events), and **ContentProvider** (shares data between apps). This topic covers the latter three. All are declared in the manifest (except some runtime-registered receivers), and the system can instantiate any of them."}]},{heading:"Services — background work without UI",blocks:[{t:"p",text:"A **Service** is a component for work that should continue *without a UI*, potentially when the user isn't in the app. Important modern caveat: a Service does **not** automatically run on a background thread — it runs on the main thread by default, so you still move work to a background thread/coroutine yourself. There are three flavors:"},{t:"table",headers:["Type","What it is","Use for"],rows:[["Foreground service","runs with a persistent notification; user-visible ongoing work","music playback, navigation, active file upload, fitness tracking"],["Background service","runs without a notification (heavily restricted since Android 8)","mostly deprecated — use WorkManager instead"],["Bound service","other components bind to it and call its methods (client-server)","an in-app API surface, IPC via AIDL"]]},{t:"list",items:["**Foreground service** — for ongoing, *user-aware* work. It must show a persistent notification (so the user knows it's running) and, since Android 10+, declare a `foregroundServiceType`. This is the correct tool for music, navigation, an in-progress upload the user is watching.","**Background services are heavily restricted**: since Android 8 (Oreo), the system kills background services shortly after the app leaves the foreground. So 'just start a Service to do background work' no longer works — for deferrable/guaranteed background work you use **WorkManager**, and for immediate user-visible work you use a foreground service.","**Bound service** — components `bindService()` to it and get an interface to call; the service lives while bound. Used for a client-server relationship within an app, or cross-process IPC (via AIDL). `startService`/`stopService` (started services) vs `bindService`/`unbindService` (bound) are the two interaction models.","**Lifecycle**: started services have `onStartCommand`; bound services have `onBind`. The return value of `onStartCommand` (`START_STICKY` etc.) tells the system whether to recreate the service if killed."]}]},{heading:"Choosing the right background tool",blocks:[{t:"table",headers:["Need","Tool"],rows:[["Ongoing user-visible work (music, navigation)","Foreground Service"],["Deferrable, guaranteed work (sync, upload) surviving process death/reboot","WorkManager"],["Short async tied to a screen","coroutine in viewModelScope"],["Exact-time work (alarm, reminder)","AlarmManager"],["In-app client-server / IPC","Bound Service (AIDL for cross-process)"]]},{t:"note",text:"The single most common Services interview question is 'when do you use a Service vs WorkManager?' Answer: a foreground service for *ongoing, immediate, user-visible* work (must show a notification); WorkManager for *deferrable, guaranteed* work that must survive process death and reboots (it internally uses a foreground service when needed). Plain background services are effectively deprecated by the Android 8+ restrictions."}]},{heading:"BroadcastReceivers — responding to events",blocks:[{t:"p",text:"A **BroadcastReceiver** listens for **broadcast** messages — system-wide or app-wide events delivered as Intents. The system broadcasts events like connectivity changes, battery low, boot completed, airplane mode toggled; apps can send custom broadcasts too. A receiver's `onReceive` runs briefly to react."},{t:"code",title:"Two ways to register a receiver",code:`// 1) Runtime (context-registered) — tied to a component's lifecycle
val receiver = object : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        // react — keep it FAST (onReceive runs on main thread, ~10s limit)
    }
}
registerReceiver(receiver, IntentFilter(Intent.ACTION_BATTERY_LOW))
// must unregisterReceiver(receiver) later to avoid leaks

// 2) Manifest-declared (static) — for a few allowed system broadcasts
// <receiver android:name=".BootReceiver" android:exported="true">
//   <intent-filter><action android:name="android.intent.action.BOOT_COMPLETED"/></intent-filter>
// </receiver>`},{t:"list",items:["**Runtime-registered** — you register in code (usually in an Activity/Service) and *must* unregister (in the mirror lifecycle callback) or leak. Only active while registered. Preferred for most cases; in Compose, register/unregister in a `DisposableEffect`.","**Manifest-declared (static)** — declared in the manifest so it can be triggered even when the app isn't running. **Heavily restricted since Android 8**: most implicit broadcasts can no longer be received via manifest declaration (to stop apps waking up constantly). A few exceptions remain (like `BOOT_COMPLETED`).","**`onReceive` must be fast**: it runs on the main thread with a short time limit (~10 seconds); do no heavy work there. For real work, hand off to WorkManager or a foreground service. It's a *trigger*, not a place to do the work.","**Security**: broadcasts can be a vector for attacks — use `exported=false` for internal receivers, permission-protect sensitive broadcasts, and prefer `LocalBroadcastManager` alternatives (now deprecated — use a shared Flow/StateFlow in a repository for in-app events instead)."]}]},{heading:"ContentProviders — sharing data across apps",blocks:[{t:"p",text:"A **ContentProvider** exposes an app's data to *other apps* through a standard interface (query/insert/update/delete) identified by a `content://` URI. It's Android's mechanism for **structured data sharing across app boundaries** with permission control. You interact with providers via a `ContentResolver`."},{t:"code",title:"Reading from a system ContentProvider (contacts)",code:`val cursor = contentResolver.query(
    ContactsContract.Contacts.CONTENT_URI,   // content:// URI
    projection, selection, selectionArgs, sortOrder
)
cursor?.use { while (it.moveToNext()) { /* read columns */ } }`},{t:"list",items:["**When you consume one**: to read *system* data — contacts, calendar, media store, call log — you query the relevant system ContentProvider via `contentResolver` (with the right runtime permission).","**When you build one**: to *share your app's data with other apps* (e.g. a dictionary app exposing definitions), or historically to feed certain framework features. If your data is *only* used within your own app, you generally *don't* need a ContentProvider — use a database/repository directly. Building one just for internal use is over-engineering.","**`FileProvider`** — a special ContentProvider for securely sharing *files* with other apps via `content://` URIs (instead of exposing raw `file://` paths, which is forbidden since Android 7). Used for sharing images, PDFs, camera output.","**A hidden role**: ContentProviders are initialized very early in app startup (before `Application.onCreate` in some cases), which libraries exploit to auto-initialize themselves (the AndroidX `App Startup` library and Firebase use a ContentProvider to bootstrap without you writing init code) — a piece of trivia that explains 'why is there a provider I didn't add?'."]}]},{heading:"Manifest, exported, and component security",blocks:[{t:"list",items:["**All components are declared in the manifest** (except runtime-registered receivers) — `<activity>`, `<service>`, `<receiver>`, `<provider>`. The manifest is how the OS knows what your app contains and can launch.","**`android:exported`** — whether *other apps* can start/access the component. Since Android 12, you *must* explicitly set it for any component with an intent filter. Default to `exported=false` unless the component genuinely needs to be reachable by other apps (a shared provider, a public deep-link Activity) — an exported component is an attack surface.","**Permissions** — components can be protected by permissions (`android:permission`) so only callers holding that permission can access them. Providers can enforce read/write permissions separately."]}]}],uT=[{level:"junior",q:"What are the four main Android app components?",a:[{t:"list",items:["**Activity** — a single screen with a UI; the entry point for user interaction.","**Service** — a component for long-running work without a UI (like music playback), which can continue when the user isn't looking at the app.","**BroadcastReceiver** — responds to system-wide or app-wide broadcast events (connectivity change, boot completed, battery low).","**ContentProvider** — exposes structured data to other apps through a standard query interface, identified by a `content://` URI."]},{t:"p",text:"All four are *entry points* the Android system can instantiate and start — the OS, not just your app, decides when to create them. Three of the four (Activity, Service, BroadcastReceiver, and Provider) are declared in the manifest so the system knows they exist (runtime-registered receivers are the exception). Understanding these components — especially their lifecycles and the modern restrictions on Services and background receivers — is core Android knowledge."}]},{level:"junior",q:"What is a Service and does it run on a background thread?",a:[{t:"p",text:"**A Service is a component for work that runs without a UI, potentially continuing when the user leaves the app.** The critical thing to know — and a common interview trap — is that **a Service does NOT run on a background thread by default. It runs on the main thread.** So if you do heavy or blocking work directly in a Service, you'll freeze the UI (or ANR) just like anywhere else. You must still move the actual work to a background thread or coroutine yourself."},{t:"list",items:["**Foreground service** — for ongoing, *user-visible* work; shows a persistent notification so the user knows it's running (music, navigation, active upload).","**Background service** — heavily restricted since Android 8: the system kills it soon after the app leaves the foreground, so it's rarely usable now.","**Bound service** — other components bind to it to call its methods (a client-server relationship, or IPC)."]},{t:"p",text:"The practical modern guidance: for immediate, ongoing, user-aware work use a *foreground service* (with its required notification); for deferrable/guaranteed background work use *WorkManager*, not a plain Service. And always remember to do the actual work off the main thread — the Service is just the component that keeps your process running, not a background executor."}]},{level:"junior",q:"What is a BroadcastReceiver and when would you use one?",a:[{t:"p",text:"**A BroadcastReceiver listens for broadcast events — messages delivered as Intents, either from the system or from apps.** The system broadcasts things like connectivity changes, boot completed, battery low, airplane mode toggled; apps can send custom broadcasts too. When a matching broadcast arrives, the receiver's `onReceive` runs to react to it."},{t:"list",items:["**Runtime-registered**: you register in code (`registerReceiver`) tied to a component's lifecycle, and must unregister to avoid leaks. Only active while registered — preferred for most cases.","**Manifest-declared (static)**: declared in the manifest so it can trigger even when the app isn't running — but this is heavily restricted since Android 8 (most implicit broadcasts can't be received statically anymore, to stop apps constantly waking up). A few exceptions like `BOOT_COMPLETED` remain."]},{t:"p",text:"The most important rule: **`onReceive` runs on the main thread and must be fast** (there's about a 10-second limit). It's a *trigger*, not a place to do work — if you need to do real work in response, hand off to WorkManager or a foreground service. Use a BroadcastReceiver to *react* to system events; for *in-app* event communication, don't use broadcasts at all — expose a shared Flow/StateFlow from a repository instead (the old `LocalBroadcastManager` is deprecated)."}]},{level:"junior",q:"What is a ContentProvider and do you always need one?",a:[{t:"p",text:"**A ContentProvider exposes an app's structured data to *other apps* through a standard interface (query/insert/update/delete), addressed by a `content://` URI.** It's Android's mechanism for sharing data across app boundaries with permission control. You access providers through a `ContentResolver`."},{t:"p",text:"**No, you don't always need one — and that's the key insight.** You need to *consume* a ContentProvider to read *system* data (contacts, calendar, media store) via `contentResolver`. But you only need to *build* one if you're **sharing your app's data with other apps**. If your data is used only within your own app, a ContentProvider is unnecessary over-engineering — just use a database/repository directly. A related special case is `FileProvider`, a built-in ContentProvider for securely sharing *files* with other apps via `content://` URIs (required since Android 7 forbade sharing raw `file://` paths). One piece of trivia worth knowing: ContentProviders initialize very early in app startup, which libraries like Firebase and AndroidX App Startup exploit to auto-initialize themselves — so if you see a provider in the merged manifest you didn't add, that's usually why."}]},{level:"senior",q:"When would you use a foreground Service versus WorkManager?",a:[{t:"p",text:"**The distinction is immediate-and-user-visible versus deferrable-and-guaranteed.** A *foreground service* is for work that must run *right now* and that the *user is aware of* — it shows a persistent notification. WorkManager is for work that can be *deferred*, must be *guaranteed* to eventually run (surviving process death and reboots), and may have *constraints*."},{t:"list",items:["**Use a foreground service when**: the work is ongoing and user-facing and must run immediately and continuously — music playback, turn-by-turn navigation, an active workout tracker, a live location share, a file upload the user is actively watching. The notification is mandatory precisely because the work is user-visible and consumes resources. It runs until you stop it (or the user does).","**Use WorkManager when**: the work is deferrable and must be *reliable* — syncing data, uploading logs, periodic backups, processing that should happen 'sometime soon' under conditions (on Wi-Fi, while charging). WorkManager persists the work request in a database, so it survives the app being killed and even device reboots, and it retries on failure with backoff. It respects Doze/battery restrictions and enforces constraints.","**They're not mutually exclusive**: WorkManager can *itself* run as a long-running foreground worker (`setForeground`) when a task needs to run immediately and show progress — so 'guaranteed AND immediate AND user-visible' work (a large download) is often a WorkManager foreground worker, getting both reliability and immediacy."]},{t:"p",text:"**The senior framing**: since Android 8's background restrictions, you can't just 'start a background service' for deferred work — the system kills it. So the decision collapses to: is it *immediate + user-visible*? → foreground service. Is it *deferrable + must-not-be-lost*? → WorkManager. And for anything tied to a screen's lifetime, neither — a coroutine in `viewModelScope`. Naming that WorkManager persists across process death/reboot (which a service does not) and that it can escalate to a foreground worker is what demonstrates real understanding."}]},{level:"senior",q:"Why are background services and manifest-declared broadcast receivers restricted on modern Android, and what do you use instead?",a:[{t:"p",text:"**The restrictions (starting with Android 8 Oreo) exist to fix a systemic battery-and-performance problem: apps were abusing background services and static broadcast receivers to run constantly and wake up on every system event, draining battery and consuming resources even when the user wasn't using them.** A device with dozens of apps each listening for `CONNECTIVITY_CHANGE` and spinning up services on every network blip had terrible battery life. Android clamped down to protect the user."},{t:"list",items:["**Background service restriction**: an app in the background can no longer freely run background services — the system stops them shortly after the app leaves the foreground. This prevents apps from doing indefinite invisible work.","**Implicit broadcast restriction**: apps can no longer register for most *implicit* broadcasts (like `CONNECTIVITY_CHANGE`) in the *manifest* — that was the mechanism for waking up when not running. This stopped the 'every app wakes on every event' storm. Explicitly-targeted broadcasts and a small allowlist (e.g. `BOOT_COMPLETED`) still work statically; runtime-registered receivers still work while the app is active.","**Doze and App Standby** (from Android 6) added further deferral — batching background work into maintenance windows when the device is idle."]},{t:"list",items:["**What to use instead — deferrable guaranteed work → WorkManager**: it batches work respecting Doze/Standby, persists across process death and reboots, and enforces constraints — the sanctioned replacement for 'do work in the background reliably'.","**Immediate user-visible ongoing work → foreground service** (with its notification), which is exempt from the background limits because the user knows about it.","**Reacting to events while the app is active → runtime-registered receiver** (register in `onStart`/`DisposableEffect`, unregister when done). For *in-app* events, don't use broadcasts at all — expose a shared Flow/StateFlow from a repository.","**Waking on connectivity/state to do work → WorkManager constraints** (e.g. a work request that runs when the network is available) rather than a receiver that triggers a service."]},{t:"p",text:"**The senior framing**: modern Android deliberately made 'run freely in the background' hard because unbounded background execution was the primary cause of poor battery life. The design shift is from 'services + receivers that keep the app awake' to 'declare *what* work you need and *under what conditions*, and let WorkManager schedule it efficiently within the OS's battery-protection rules'. Understanding *why* (battery/UX at the OS level) and the replacement toolset (WorkManager + foreground services + runtime receivers) is the complete answer."}]}],pT={id:"services-components",title:"Services, Receivers & Providers",tagline:"Service types (foreground/bound), Service vs WorkManager, BroadcastReceivers & registration, ContentProviders & FileProvider, and modern background restrictions.",tags:["Android","Services","BroadcastReceiver","ContentProvider","WorkManager"],content:hT,qa:uT},mT=[{heading:"What Context is",blocks:[{t:"p",text:"**`Context`** is your handle to the Android application environment — it provides access to resources, system services, app-level operations, and information about the app. Almost everything in Android needs a Context: inflating layouts, starting Activities, accessing `getSystemService`, reading resources/strings, opening databases and files, showing toasts. It's 'the bridge between your code and the Android system'."},{t:"list",items:["**Things Context gives you**: resources (`getString`, `getDrawable`), system services (`getSystemService` for connectivity, notifications, etc.), starting components (`startActivity`, `startService`), file/database access, and app info (`packageName`).","It's an *abstract class*; the concrete instances you actually use are `Application`, `Activity`, and `Service` (they all *are* Contexts), plus a few wrappers."]}]},{heading:"Application Context vs Activity Context — the crucial distinction",blocks:[{t:"p",text:"The two Contexts you use most are the **Application Context** and the **Activity Context**, and choosing the wrong one causes memory leaks. The key difference is *lifetime*: the Application Context lives for the *entire app process*; an Activity Context lives only as long as *that Activity*."},{t:"table",headers:["","Application Context","Activity Context"],rows:[["Lifetime","the whole app process (singleton)","only while that Activity exists"],["Get it via","`applicationContext` / injected","`this` in an Activity"],["UI operations (dialogs, theming)","wrong — no theme/window","correct — themed, tied to a window"],["Long-lived references (singletons, WorkManager)","correct — won't leak","wrong — leaks the Activity"]]},{t:"list",items:["**Use the Activity Context for UI-related things**: showing a dialog, inflating a view into the Activity, anything that needs the Activity's theme or window. The Application Context lacks a theme and window, so UI operations with it misbehave or crash.","**Use the Application Context for long-lived things**: singletons, repositories, WorkManager, a database instance — anything that outlives an Activity. Storing an *Activity* Context in a long-lived object leaks the entire Activity (and its view hierarchy) because the object keeps the Activity alive after it should be destroyed.","**The classic leak**: a singleton holding an Activity Context (`object Manager { lateinit var ctx: Context }` set to an Activity) — the Activity can never be garbage-collected. Always pass `applicationContext` to anything that lives longer than the Activity.","**In practice with DI**: inject `@ApplicationContext` (Hilt) into repositories/managers, and only use the Activity Context locally within the Activity for UI."]}]},{heading:"The Application class",blocks:[{t:"p",text:"The **`Application`** class is a singleton created *before any other component* when your app's process starts, and it lives for the entire process lifetime. It *is* the Application Context. You can subclass it (declared in the manifest via `android:name`) to do app-wide initialization and hold app-global state."},{t:"code",title:"A custom Application class",code:`class MyApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // App-wide init: DI graph, crash reporting, logging
        // BUT keep this FAST — it delays every app start
    }
}`},{t:"list",items:["**`onCreate()` runs once per process**, before the first Activity. It's where DI frameworks initialize (Hilt generates an `Application` subclass), and where you'd set up crash reporting, logging, etc.","**Keep `Application.onCreate` lean**: it runs on the *main thread* on *every* cold start, so heavy work here directly increases startup time. Defer non-critical initialization (lazy init, App Startup library, background init) — a bloated `onCreate` is a common cause of slow app launches.","**Don't use it as a global variable dump**: it's tempting to store 'global state' here, but that's an anti-pattern (untestable, leak-prone). Use proper scoping (DI singletons) instead."]}]},{heading:"Processes — apps run in their own sandbox",blocks:[{t:"list",items:["**Each app runs in its own Linux process** with its own instance of the Android Runtime (ART) and its own user ID — a security *sandbox* isolating apps from each other. By default all of an app's components run in this single process on the **main thread** (also called the UI thread).","**The main thread** handles UI rendering and event dispatch. Blocking it (heavy work, network, disk) freezes the UI and triggers an **ANR** (Application Not Responding) if blocked ~5 seconds — hence all the emphasis on moving work to background threads/coroutines.","**The system manages process lifetime by priority**: foreground processes (visible Activity) are protected; background/cached processes are killed first when memory is needed. This is *process death* — your app's process is terminated, and it must restore from saved state when relaunched.","**Multi-process apps** (rare): you *can* run components in separate processes (`android:process` in the manifest) — e.g. isolating a crash-prone or memory-heavy component. But it complicates everything (separate memory, IPC needed to communicate, the Application class runs per-process), so it's used only for specific needs."]}]},{heading:"App startup and cold/warm/hot start",blocks:[{t:"table",headers:["Start type","What it involves","Speed"],rows:[["Cold start","process created from scratch: fork, Application.onCreate, first Activity","slowest — full setup"],["Warm start","process alive but Activity recreated (e.g. returned after being destroyed)","faster"],["Hot start","Activity already in memory, just brought to front","fastest"]]},{t:"list",items:["**Cold start** is the one to optimize — it does the most work (create the process, run `Application.onCreate`, inflate and draw the first frame). Users judge the app by it.","**What slows cold start**: heavy `Application.onCreate`, eager initialization of libraries, doing work before the first frame, large layouts, synchronous disk/network on the startup path.","**How to speed it up**: lean `Application.onCreate` (defer/lazy-init), the **App Startup** library (to order and consolidate initializers, avoiding multiple ContentProvider-based inits), **Baseline Profiles** (AOT-compile the startup path so it isn't JIT-compiled during launch — often 20-40% faster cold start), and avoiding heavy work before the first frame.","**The AndroidX App Startup library** replaces the old pattern where each library added its own ContentProvider to auto-initialize (each provider adding startup cost) — it consolidates them into a single, ordered initializer."]},{t:"note",text:"Context essentials: it's the bridge to the Android environment (resources, services, components). Use Activity Context for UI (needs theme/window), Application Context for long-lived objects (won't leak the Activity). The Application class is a per-process singleton for app-wide init — keep its onCreate fast. Each app runs in an isolated process on a main thread that must never block (ANR at ~5s). Optimize cold start with a lean Application.onCreate, App Startup, and Baseline Profiles."}]}],fT=[{level:"junior",q:"What is Context in Android?",a:[{t:"p",text:"**The concept**: `Context` is your handle to the Android app environment — the bridge between your code and the Android system. Almost everything needs it because it provides access to resources (strings, drawables), system services (`getSystemService`), the ability to start components (`startActivity`), file and database access, and app information."},{t:"list",items:["You need a Context to: inflate a layout, show a Toast or dialog, read a string resource, get a system service (connectivity, notifications), start an Activity or Service, open a database or file.","It's an abstract class; the concrete Contexts you actually use are `Application`, `Activity`, and `Service` — they *are* Contexts (they extend it).","The two you pick between most are the **Application Context** (lives for the whole app) and the **Activity Context** (lives only as long as that Activity) — and choosing correctly matters for avoiding leaks."]},{t:"p",text:"Think of Context as 'the environment handle' — whenever an Android API needs to know *which app* or *which screen* it's acting on behalf of, or needs access to system-provided things, it asks for a Context."}]},{level:"junior",q:"What is the difference between Application Context and Activity Context?",a:[{t:"p",text:"**The core difference is lifetime.** The Application Context lives for the *entire app process* (it's a singleton). An Activity Context lives only as long as *that specific Activity* — it dies when the Activity is destroyed. This lifetime difference dictates which you should use where."},{t:"list",items:["**Use the Activity Context for UI**: showing a dialog, inflating a view into the Activity, anything needing the Activity's theme or window. The Application Context has no theme or window, so UI operations with it look wrong or crash.","**Use the Application Context for long-lived objects**: singletons, repositories, a database, WorkManager — anything that outlives an Activity. Giving these an Activity Context would keep the Activity alive after it should be destroyed — a memory leak."]},{t:"code",title:"The classic leak",code:`// LEAK: singleton holding an Activity Context keeps the Activity alive forever
object Manager { lateinit var ctx: Context }
Manager.ctx = this   // 'this' is an Activity -> leaked

// CORRECT: give long-lived objects the application context
Manager.ctx = applicationContext`},{t:"p",text:"The rule of thumb: if the object using the Context lives *longer* than an Activity, use the Application Context; if the operation needs UI/theme/window, use the Activity Context. With dependency injection you inject `@ApplicationContext` into repositories and managers, and reserve the Activity Context for local UI work inside the Activity."}]},{level:"junior",q:"What is the Application class and what do you use it for?",a:[{t:"p",text:"**The concept**: the `Application` class is a singleton created before any other component when your app's process starts, living for the whole process lifetime. It *is* the Application Context. You can subclass it (declared in the manifest with `android:name`) to run app-wide initialization and hold process-global setup."},{t:"list",items:["**`onCreate()` runs once per process**, before the first Activity — the place for app-wide setup: initializing the DI graph (Hilt generates an Application subclass for this), crash reporting, logging frameworks.","**Keep it lean**: `Application.onCreate` runs on the *main thread* on *every* cold start, so heavy work here directly slows down every app launch. Defer non-critical initialization (lazy init, background threads, the App Startup library).","**Don't use it as a global-variable dump**: storing mutable 'global state' in the Application class is an anti-pattern — untestable and easy to leak. Use proper DI-scoped singletons instead."]},{t:"p",text:"So it's for genuine app-wide, process-lifetime concerns (initialization, providing the Application Context), not a convenient bag for globals. The main performance caution is that a bloated `onCreate` is a very common cause of slow cold starts, since it's squarely on the critical launch path."}]},{level:"junior",q:"What is an ANR and what causes it?",a:[{t:"p",text:"**ANR stands for 'Application Not Responding'** — a dialog the system shows (and a event it logs) when your app's *main thread* is blocked for too long and can't process user input or draw. The main thresholds: the main thread unresponsive to input for ~5 seconds, a BroadcastReceiver's `onReceive` taking more than ~10 seconds, or a Service not completing startup in time."},{t:"p",text:"**The cause is always the same fundamental mistake: doing slow work on the main thread.** The main thread (UI thread) is responsible for rendering the UI and handling input events; if you block it — with a network request, a large database query, heavy computation, or file I/O — it can't do its job, the UI freezes, and after ~5 seconds the system declares an ANR. The fix is to move all slow work *off* the main thread: use coroutines with `Dispatchers.IO`/`Default`, `withContext`, or background threads, keeping the main thread free to render and respond. This is *the* reason Android emphasizes coroutines and background threading so heavily — every 'don't block the main thread' rule exists to prevent ANRs and jank."}]},{level:"senior",q:"How do Android processes work, and what is process death?",a:[{t:"p",text:"**Each Android app runs in its own Linux process** with its own instance of the Android Runtime (ART) and a unique Linux user ID. This process is a *security sandbox* — apps can't directly access each other's memory or data; cross-app interaction goes through controlled channels (Intents, ContentProviders, permissions). By default, all of an app's components run in this single process, and specifically on its **main thread**."},{t:"list",items:["**The system manages processes by priority to reclaim memory**: it ranks processes — foreground (a visible Activity or foreground service) is highest priority and protected; visible, service, background (cached) processes descend in priority. When the system needs memory, it kills the *lowest-priority* processes first, starting with cached background ones.","**Process death** is when the system terminates your app's process (usually while it's in the background) to free resources. Everything in memory is gone — the ViewModel, singletons, static state, in-flight work. When the user returns, Android *recreates* the process and the Activity from scratch, restoring only the saved-instance-state Bundle.","**Why it matters for architecture**: because process death loses all in-memory state, robust apps must be able to rebuild from persisted state — the source of truth is the database/repository (for data) plus `SavedStateHandle`/`onSaveInstanceState` (for transient input/ids). Relying on in-memory state or singletons to 'always be there' is the bug process death exposes.","**Multi-process apps** exist (`android:process` in the manifest puts a component in a separate process — used to isolate a memory-heavy or crash-prone component, or for security isolation), but they add significant complexity: separate memory spaces, the Application class runs *per process*, and communication requires IPC (bound services/AIDL, ContentProviders). It's a deliberate choice for specific needs, not a default."]},{t:"p",text:"**The senior framing**: the process model is fundamentally about the OS treating memory as a shared, reclaimable resource and apps as disposable/recreatable. Understanding that your process can be killed at any time in the background — and that recovery means rebuilding from persisted state, not assuming memory survives — is what separates apps that lose the user's place on return from ones that restore seamlessly. It's the deeper 'why' behind the whole state-saving discipline."}]},{level:"senior",q:"What is the difference between cold, warm, and hot start, and how do you optimize cold start?",a:[{t:"p",text:"**These describe how much work Android must do to bring your app to the foreground, from most to least.** A *cold start* means the process doesn't exist yet — the system must create it (fork the process, initialize ART), run `Application.onCreate`, then create and draw the first Activity. A *warm start* means the process is alive but the Activity needs recreating (e.g. it was destroyed in the background) — less work. A *hot start* means the Activity is still in memory and just needs bringing to the front — the fastest."},{t:"list",items:["**Cold start is the one to optimize** — it does the most work and users judge the app's speed by it (especially first launch). It's measured as 'time to initial display' (first frame) and 'time to fully drawn'.","**What slows cold start**: a heavy `Application.onCreate` (runs synchronously on the main thread before anything), eager initialization of many libraries, synchronous disk/network on the startup path, large/complex initial layouts, and — a big one — JIT compilation of the startup code path happening *during* launch."]},{t:"list",items:["**Optimization 1 — lean Application.onCreate**: initialize only what's needed to draw the first screen; lazy-initialize or defer everything else (analytics, non-critical SDKs). Move eager work to background threads or trigger it after first frame.","**Optimization 2 — App Startup library**: consolidates library initializers into a single, ordered ContentProvider instead of each library adding its *own* ContentProvider (each provider adds measurable startup cost). It also lets you control initialization order and laziness.","**Optimization 3 — Baseline Profiles**: this is often the biggest single win. Without them, the startup code path is JIT-compiled *during* the user's launch. A Baseline Profile ships a list of the hot startup methods that get AOT-compiled at install time, so the launch path is already native — commonly 20-40% faster cold start and smoother first frames.","**Optimization 4 — avoid work before the first frame**: don't block the first draw on data loading (show a skeleton/placeholder and load asynchronously); keep the initial layout simple; use a proper splash screen (the SplashScreen API) rather than a heavy custom one.","**Measure it**: use `adb shell am start -W` for start time, Macrobenchmark with `StartupTimingMetric` for reliable numbers on release builds, and Perfetto/system traces to find what's on the critical path — optimize based on measurement, not guesses."]}]}],gT={id:"context-process",title:"Context, Processes & App Startup",tagline:"What Context is, Application vs Activity Context & leaks, the Application class, processes & process death, ANRs, and cold/warm/hot start optimization.",tags:["Android","Context","Application","Process Death","ANR","Startup"],content:mT,qa:fT},yT=[{heading:"The main thread and the single-threaded UI rule",blocks:[{t:"p",text:"Android UI is **single-threaded**: all UI updates must happen on the **main thread** (a.k.a. the UI thread). This is a hard rule — touching a View from a background thread throws `CalledFromWrongThreadException`. Conversely, the main thread must *never* be blocked (doing network, disk, or heavy work on it freezes the UI and causes an ANR after ~5 seconds). These two rules together — 'update UI only on main, do heavy work only off main' — are the source of all Android threading patterns."},{t:"list",items:["**Why single-threaded UI**: making the UI toolkit thread-safe (locks on every view operation) would be slow and deadlock-prone; a single UI thread avoids that entirely, at the cost of you managing the boundary.","**The boundary you constantly cross**: do work on a background thread → deliver the result to the main thread to update the UI. Coroutines make this clean (`withContext(IO)` for work, resume on Main), but it's built on lower-level machinery: the Handler/Looper/MessageQueue."]}]},{heading:"Handler, Looper, and MessageQueue — how the main thread works",blocks:[{t:"p",text:"The main thread isn't magic — it's a normal thread running an infinite **message loop**. Three pieces make it work: a **`Looper`** (runs the loop), a **`MessageQueue`** (a queue of work to do), and **`Handler`**s (used to post work onto the queue and process it). This is the foundation beneath everything — the coroutine `Dispatchers.Main`, `runOnUiThread`, `View.post`, all sit on top of it."},{t:"list",items:["**`Looper`** — an object that runs an infinite loop (`Looper.loop()`) pulling messages off a queue and dispatching them, one at a time. The main thread has a Looper set up for you (`Looper.getMainLooper()`); background threads don't by default.","**`MessageQueue`** — the queue of `Message`s/`Runnable`s waiting to be processed, ordered by time. The Looper takes the next one when its time arrives.","**`Handler`** — bound to a Looper; you use it to *post* work onto that thread's queue (`handler.post { }`, `handler.postDelayed({ }, 1000)`). A Handler on the main Looper is how a background thread delivers a result to the main thread: it posts a Runnable that the main Looper picks up and runs *on the main thread*.","**The whole model**: the main thread loops forever, processing queued messages (input events, UI updates, posted Runnables) one by one. That's why UI work is serialized and why blocking any single message (a long operation) stalls everything behind it — the loop can't advance."]},{t:"code",title:"Posting to the main thread (the classic pre-coroutine pattern)",code:`// Background thread doing work, delivering result to main:
val mainHandler = Handler(Looper.getMainLooper())
thread {
    val result = doHeavyWork()
    mainHandler.post {           // runs on the main thread
        textView.text = result   // safe to touch UI here
    }
}
// Modern coroutine equivalent:
lifecycleScope.launch {
    val result = withContext(Dispatchers.IO) { doHeavyWork() }
    textView.text = result       // back on Main automatically
}`},{t:"note",text:"Interview-ready: the main thread runs a Looper that pulls from a MessageQueue; Handlers post work onto a thread's queue. `Dispatchers.Main` (coroutines), `runOnUiThread`, `View.post`, LiveData delivery — all use this machinery to run work on the main thread. Blocking the loop (a long task) stalls all queued UI work → jank/ANR. Coroutines are the modern abstraction over this, but the Handler/Looper model is what's underneath."}]},{heading:"The evolution of Android threading",blocks:[{t:"table",headers:["Era","Tool","Status"],rows:[["Early","Thread + Handler, AsyncTask","AsyncTask deprecated (leak-prone, lifecycle-blind)"],["Middle","Loaders, IntentService, RxJava","Loaders/IntentService deprecated; RxJava still used"],["Modern","Kotlin Coroutines + Flow, WorkManager","current standard"]]},{t:"list",items:["**`AsyncTask`** (deprecated) — the old way to do background work with UI callbacks; deprecated because it leaked Activities, ignored lifecycle, and had error-prone threading. Know it exists (legacy code) but never use it in new code.","**Coroutines are the modern answer** — structured, lifecycle-aware (via scopes), cancellable, and readable. They replaced AsyncTask/Loaders/most manual Handler use.","You still occasionally touch `Handler`/`Looper` directly for things like a periodic UI update loop or `postDelayed`, but coroutines (`delay`, `withContext`) cover almost all cases more cleanly."]}]},{heading:"Permissions — the model",blocks:[{t:"p",text:"Android **permissions** gate access to sensitive data and capabilities (location, camera, contacts, microphone). There are two broad types by *protection level*:"},{t:"list",items:["**Normal permissions** — low-risk (internet, vibrate, set alarm). Declared in the manifest and granted *automatically* at install; no user prompt.","**Dangerous (runtime) permissions** — access sensitive data/capabilities (location, camera, contacts, storage, microphone). Declared in the manifest *and* must be requested *at runtime*, with the user explicitly granting or denying via a system dialog. Introduced in Android 6 (Marshmallow) — before that, all permissions were granted at install.","**Special permissions** — a few high-risk ones (display over other apps, modify system settings, exact alarms, all-files access) require the user to toggle them in a dedicated Settings screen, not a simple dialog."]}]},{heading:"Requesting runtime permissions",blocks:[{t:"code",title:"The modern Activity Result API",code:`// Register a launcher (in an Activity/Fragment)
val requestPermission = registerForActivityResult(
    ActivityResultContracts.RequestPermission()
) { granted ->
    if (granted) startLocationUpdates()
    else showRationaleOrDeniedUi()
}

// Request when needed:
when {
    checkSelfPermission(ACCESS_FINE_LOCATION) == PERMISSION_GRANTED ->
        startLocationUpdates()                       // already have it
    shouldShowRequestPermissionRationale(ACCESS_FINE_LOCATION) ->
        showRationaleDialog()                         // explain why, then request
    else ->
        requestPermission.launch(ACCESS_FINE_LOCATION) // ask
}`},{t:"list",items:["**The flow**: check if already granted → if not, show a rationale if appropriate (`shouldShowRequestPermissionRationale` returns true after a previous denial) → request via the launcher → handle the result.","**Use the Activity Result API** (`registerForActivityResult`) — the modern replacement for the old `onRequestPermissionsResult` callback (which was verbose and error-prone). It handles the plumbing and works with the lifecycle.","**Handle denial gracefully**: the user can deny, deny-with-'don't ask again' (after which the system won't show the dialog and you should guide them to Settings), or grant. Design for all outcomes — never assume you'll get the permission.","**Request in context, with rationale**: ask for a permission *when the feature needs it* (not all upfront at launch), and explain *why* — this dramatically improves grant rates and is required for sensitive permissions on modern Android.","**Newer nuances**: location has *coarse vs fine* and *background* location (background requires a separate, harder-to-get grant); Android 13+ added granular media permissions (photos vs video vs audio instead of broad storage) and a `POST_NOTIFICATIONS` runtime permission."]}]}],wT=[{level:"junior",q:"Why can't you update the UI from a background thread?",a:[{t:"p",text:"**Because Android's UI toolkit is single-threaded — all UI updates must happen on the main (UI) thread.** Touching a View from any other thread throws `CalledFromWrongThreadException`. The reason is a deliberate design choice: making the entire UI toolkit thread-safe would require locking on every view operation, which would be slow and prone to deadlocks. Instead, Android confines all UI work to one thread, which is simpler and faster — at the cost of you having to manage the boundary between background work and UI updates."},{t:"p",text:"So the pattern is always: do heavy work (network, disk, computation) on a *background* thread, then *deliver the result to the main thread* to update the UI. The flip side is equally important — you must *not block* the main thread either, because it's responsible for rendering and input; blocking it freezes the UI and causes an ANR after ~5 seconds. Coroutines make crossing this boundary clean: `withContext(Dispatchers.IO) { work() }` runs off-main and automatically resumes on Main to update the UI. Underneath, this uses the Handler/Looper machinery to post the result back to the main thread."}]},{level:"junior",q:"What are Handler, Looper, and MessageQueue?",a:[{t:"p",text:"**Together they're the machinery that makes a thread able to process a stream of work — and it's what the main thread is built on.** The main thread isn't special magic; it's a normal thread running an infinite message loop made of these three pieces:"},{t:"list",items:["**`Looper`** — runs an infinite loop that pulls messages off a queue one at a time and dispatches them. The main thread has one set up automatically (`Looper.getMainLooper()`); regular background threads don't.","**`MessageQueue`** — the queue of pending work (`Message`s / `Runnable`s), ordered by time, that the Looper pulls from.","**`Handler`** — bound to a specific Looper; you use it to *post* work onto that thread's queue (`handler.post { }`, `postDelayed`). A Handler tied to the main Looper is how a background thread hands a result to the main thread."]},{t:"code",title:"Delivering a result to the main thread",code:`val main = Handler(Looper.getMainLooper())
thread {
    val result = heavyWork()
    main.post { textView.text = result }   // runs on the main thread
}`},{t:"p",text:"The model: the main thread loops forever, processing queued messages (input events, UI updates, posted Runnables) sequentially. This is why UI work is serialized, and why a single long operation on the main thread stalls everything behind it (jank/ANR) — the loop can't advance until each message finishes. Higher-level tools all sit on this: `Dispatchers.Main`, `runOnUiThread`, `View.post`, and LiveData delivery all ultimately post to the main Looper via a Handler. Coroutines are the modern abstraction, but Handler/Looper is the foundation."}]},{level:"junior",q:"What is the difference between normal and dangerous (runtime) permissions?",a:[{t:"p",text:"**They differ by risk level and how they're granted.** *Normal* permissions are low-risk (internet access, vibrate, set an alarm) — you declare them in the manifest and the system grants them *automatically at install* with no user prompt. *Dangerous* (runtime) permissions guard sensitive user data or capabilities (location, camera, contacts, microphone, storage) — you declare them in the manifest *and* must request them *at runtime*, where the user sees a system dialog and explicitly grants or denies."},{t:"list",items:["**Normal** — internet, network state, vibrate, bluetooth (basic). Granted at install, no runtime code needed.","**Dangerous** — anything touching private data or hardware that could harm privacy: fine/coarse location, camera, microphone, contacts, calendar, SMS, media/storage. Must be requested at runtime (since Android 6 Marshmallow; before that all permissions were install-time).","**Special** — a few high-risk ones (draw over other apps, modify system settings, exact alarms, all-files access) require the user to enable them in a dedicated Settings screen rather than a simple dialog."]},{t:"p",text:"The runtime model exists so users control access to sensitive data *contextually* — they can grant camera access to a camera app but deny it to a game, and revoke it later. As an app developer this means you must *check* whether you have a dangerous permission before using the feature, *request* it if not (ideally with a rationale explaining why), and *handle denial gracefully* — never assume you have it."}]},{level:"junior",q:"How do you request a runtime permission in a modern Android app?",a:[{t:"p",text:"**You use the Activity Result API (`registerForActivityResult`) with a permission contract, following a check → rationale → request → handle-result flow.** This is the modern replacement for the old, verbose `onRequestPermissionsResult` callback."},{t:"code",title:"The pattern",code:`val launcher = registerForActivityResult(
    ActivityResultContracts.RequestPermission()
) { granted -> if (granted) useFeature() else showDenied() }

when {
    checkSelfPermission(CAMERA) == PERMISSION_GRANTED -> useFeature()
    shouldShowRequestPermissionRationale(CAMERA) -> showRationale()  // then launch
    else -> launcher.launch(CAMERA)
}`},{t:"list",items:["**Check first**: `checkSelfPermission` — if already granted, just use the feature; don't re-ask.","**Show rationale when appropriate**: `shouldShowRequestPermissionRationale` returns true if the user previously denied (but not permanently) — explain *why* you need it before re-requesting, which improves grant rates.","**Request via the launcher**: `launcher.launch(permission)` shows the system dialog; the result callback tells you granted or denied.","**Handle all outcomes**: granted, denied, or denied-permanently ('don't ask again'). After a permanent denial the dialog won't show again, so you guide the user to app Settings. Design the feature to degrade gracefully if denied — never assume you'll get it."]},{t:"p",text:"Best practice: request permissions *in context* — right when the feature needs it, not all at launch — and only ask for what you truly need. Modern Android also has finer-grained permissions (coarse vs fine location, background location as a separate grant, granular media permissions on Android 13+, and a runtime `POST_NOTIFICATIONS` permission), so request the *least* powerful permission that satisfies the feature."}]},{level:"senior",q:"Why was AsyncTask deprecated, and how do coroutines solve its problems?",a:[{t:"p",text:"**`AsyncTask` was the old API for running background work with UI callbacks (`doInBackground` on a background thread, `onPostExecute` on the main thread). It was deprecated because it had several serious, structural problems that made it a frequent source of bugs — problems coroutines solve by design.**"},{t:"list",items:["**Memory leaks**: AsyncTasks were often inner classes holding an implicit reference to the Activity. If the Activity was destroyed (rotation!) while the task was still running, the task kept the Activity alive — a leak — and then tried to update a destroyed UI. Coroutines tied to a lifecycle scope (`viewModelScope`, `lifecycleScope`) are *cancelled* when the scope dies, so they don't leak or update dead UI.","**Lifecycle-blindness**: AsyncTask had no awareness of the Activity/Fragment lifecycle — it just ran to completion regardless, causing crashes when `onPostExecute` touched a gone Activity. Coroutine scopes are lifecycle-aware, so work is cancelled at the right time automatically.","**No structured concurrency / poor cancellation**: cancelling an AsyncTask was cooperative and awkward, and there was no clean way to compose multiple tasks or propagate errors. Coroutines have structured concurrency — a parent-child tree with proper cancellation and error propagation.","**Threading pitfalls & serial executor confusion**: AsyncTasks shared a single serial executor by default (so parallel tasks unexpectedly queued), and configuring parallelism was error-prone. Coroutines make threading explicit and correct via dispatchers.","**Verbose, callback-based, hard to read**: nested AsyncTasks (task after task) became callback hell. Coroutines let you write sequential-looking async code (`val a = fetchA(); val b = fetchB(a)`), far more readable."]},{t:"p",text:"**The senior framing**: AsyncTask's failures all trace to the same root — it treated background work as *unstructured* and *lifecycle-unaware*, so the developer had to manually manage leaks, cancellation, and threading, and usually got at least one wrong. Coroutines invert this: work is *structured* (bound to a scope with automatic cancellation), *lifecycle-aware* (via `viewModelScope`/`lifecycleScope`), *cancellation-cooperative* by default, and *readable* (sequential style). That's why the entire ecosystem moved to coroutines, and why AsyncTask, Loaders, and IntentService were all deprecated in favor of coroutines + WorkManager."}]},{level:"senior",q:"What happens if you block the main thread's Looper, and how does this connect to jank and ANRs?",a:[{t:"p",text:"**The main thread runs a single `Looper` that processes messages from its `MessageQueue` one at a time, sequentially. If you block the processing of any one message with a long operation, the loop cannot advance to the next message — so every queued UI update, input event, and animation frame behind it is stalled.** This is the mechanical root of both jank and ANRs."},{t:"list",items:["**How rendering works via the Looper**: the system schedules a 'draw' message roughly every 16ms (at 60Hz) via the Choreographer, which posts frame callbacks to the main thread's queue. For smooth UI, each frame's work (measure, layout, draw, plus any of your main-thread code) must complete within that ~16ms budget so the next frame's draw message can be processed on time.","**Jank** happens when a message takes *longer than the frame budget* — say your `onBindViewHolder` or a recomposition does 30ms of work. The draw message for that frame is delayed, the frame is dropped, and the user sees a stutter. It's not a crash, just a missed frame, but repeated jank feels broken.","**ANR** happens when a message blocks the loop for *seconds* — a synchronous network call or a huge database query on the main thread holds the loop, so input events (taps) sit unprocessed in the queue. After ~5 seconds of unresponsiveness to input, the system shows the 'Application Not Responding' dialog. Same mechanism as jank, just a much longer block.","**Why it's all one problem**: jank and ANR are the *same* phenomenon — the Looper being unable to process pending messages promptly — at different severities. A 20ms block drops a frame; a 5s block triggers an ANR."]},{t:"p",text:"**The fix and the framing**: keep every unit of main-thread work *short*. Move anything slow (I/O, computation, large queries) off the main thread with coroutines/dispatchers, so the main thread's Looper only ever handles quick UI operations and can keep processing frames and input on time. This is *why* 'never block the main thread' is drilled so hard — it's not an abstract rule, it's about keeping the message loop flowing so frames render and taps respond. Understanding that jank and ANR are the same Looper-starvation problem at different timescales is the senior-level insight; it also tells you how to *diagnose* them (find the long-running main-thread message via Perfetto/systrace)."}]}],vT={id:"threading-permissions",title:"Threading, Handler/Looper & Permissions",tagline:"The single-threaded UI rule, Handler/Looper/MessageQueue, threading evolution (AsyncTask → coroutines), and the runtime permission model.",tags:["Android","Main Thread","Handler","Looper","Permissions","ANR"],content:yT,qa:wT},bT={id:"android-core",name:"Android Core",description:"The Android framework itself — Activity & Fragment lifecycles, Intents/tasks/launch modes, Services/Receivers/Providers, Context & processes, threading, and permissions.",topics:[iT,rT,dT,pT,gT,vT]},kT=[{heading:"SQLite and why Room exists",blocks:[{t:"p",text:"**SQLite** is a lightweight, embedded relational database built into Android — every app can use it for structured local storage, with no server. You *could* use it directly through the raw `SQLiteOpenHelper`/`SQLiteDatabase` APIs, but that means writing SQL as strings, manually mapping `Cursor` rows to objects, and getting no compile-time checking — error-prone and verbose. **Room** is a Jetpack library that sits on top of SQLite as an **ORM (Object-Relational Mapping)** layer: you define your schema with annotated Kotlin classes, write queries as annotated methods, and Room generates the boilerplate with *compile-time verification* of your SQL."},{t:"list",items:["**Room's headline benefit — compile-time SQL verification**: your `@Query` SQL is checked against your schema *when you build*, so a typo in a column name or a wrong type is a compile error, not a runtime crash. Raw SQLite would only fail at runtime.","**Less boilerplate**: no manual `Cursor` iteration, no manual object mapping — Room maps query results to your objects automatically.","**Reactive & coroutine support built in**: queries can return `Flow` (auto-updating), `suspend` functions (main-safe), LiveData — first-class integration with modern Android.","Room is the recommended way to use a database on Android; raw SQLite is legacy/niche."]}]},{heading:"The three Room components: Entity, DAO, Database",blocks:[{t:"code",title:"The complete minimal setup",code:`// 1) ENTITY — a table, defined by a data class
@Entity(tableName = "users")
data class UserEntity(
    @PrimaryKey val id: String,
    @ColumnInfo(name = "full_name") val name: String,
    val email: String,
    val createdAt: Long,
)

// 2) DAO — Data Access Object: your queries as annotated methods
@Dao
interface UserDao {
    @Query("SELECT * FROM users ORDER BY createdAt DESC")
    fun observeUsers(): Flow<List<UserEntity>>          // reactive, auto-updates

    @Query("SELECT * FROM users WHERE id = :id")
    suspend fun getUser(id: String): UserEntity?        // suspend = main-safe

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun upsert(user: UserEntity)

    @Delete
    suspend fun delete(user: UserEntity)
}

// 3) DATABASE — ties entities and DAOs together
@Database(entities = [UserEntity::class], version = 1)
abstract class AppDatabase : RoomDatabase() {
    abstract fun userDao(): UserDao
}`},{t:"list",items:["**`@Entity`** — a class mapped to a *table*; each property is a *column*. `@PrimaryKey` marks the unique key (can be `autoGenerate = true`); `@ColumnInfo` customizes column names; `@Ignore` excludes a property.","**`@Dao`** — an interface of database operations. `@Query` for SQL (verified at compile time), `@Insert`/`@Update`/`@Delete`/`@Upsert` for convenience operations. Methods can be `suspend` (one-shot, main-safe) or return `Flow`/`LiveData` (observable).","**`@Database`** — an abstract class listing the entities and exposing the DAOs, with a `version` number (critical for migrations). You build one instance (a singleton) with `Room.databaseBuilder(...)`."]}]},{heading:"Reactive queries with Flow — Room as the source of truth",blocks:[{t:"p",text:"The most powerful Room feature: a `@Query` returning **`Flow<T>`** *automatically re-emits whenever the underlying data changes*. Room tracks which tables a query touches and, when any write modifies those tables, it re-runs the query and emits the new result to all collectors. This is what makes Room the ideal **single source of truth** — the UI observes the database, writes go into the database, and the UI updates automatically."},{t:"code",title:"The observe-local, write-anywhere pattern",code:`// Repository: UI observes the DB; network writes INTO the DB
class UserRepository(private val dao: UserDao, private val api: Api) {
    // Reads always come from the DB (single source of truth)
    fun observeUsers(): Flow<List<User>> =
        dao.observeUsers().map { it.map(UserEntity::toDomain) }

    // Refresh writes into the DB; the Flow above re-emits automatically
    suspend fun refresh() {
        val remote = api.getUsers()
        dao.upsertAll(remote.map { it.toEntity() })   // triggers Flow re-emission
    }
}`},{t:"list",items:["**Why it's powerful**: multiple screens observing the same query all update in the same frame when a write happens — no manual notification, no event bus. Offline works because reads always come from the DB.","**suspend vs Flow**: use `suspend` for one-shot operations (insert, a single lookup); use `Flow` for data the UI should *observe* over time. A `suspend` query runs once and returns; a `Flow` query keeps emitting.","**Room handles threading**: `suspend` DAO functions and `Flow` queries are main-safe — Room runs the actual query on a background executor. You don't need to wrap them in `withContext(IO)` (doing so is redundant)."]}]},{heading:"Transactions and atomicity",blocks:[{t:"p",text:"A **transaction** groups multiple database operations so they happen **atomically** — all succeed together or all roll back together, with no partial state visible to other queries. Use `@Transaction` when a logical operation spans several writes/reads that must be consistent."},{t:"code",title:"@Transaction for multi-step consistency",code:`@Dao
interface OrderDao {
    @Transaction
    suspend fun replaceCart(items: List<CartItem>) {
        clearCart()              // delete old
        insertAll(items)         // insert new
        // If insertAll fails, clearCart is rolled back too — atomic
    }
}`},{t:"list",items:["**Atomicity**: without a transaction, a crash between `clearCart()` and `insertAll()` would leave an empty cart. With `@Transaction`, either both happen or neither does.","**Consistency for reads**: `@Transaction` on a query method ensures the reads within it see a consistent database snapshot — important for relation queries (`@Relation`) that do multiple reads, so they don't see the DB change mid-way.","**Performance**: batching many inserts in one transaction is *much* faster than many individual inserts (each insert is otherwise its own implicit transaction with fsync overhead). Inserting 1000 rows in a transaction can be orders of magnitude faster."]}]},{heading:"Building and configuring the database",blocks:[{t:"code",title:"Creating the singleton (usually via DI)",code:`val db = Room.databaseBuilder(
    context.applicationContext,       // application context — it's app-lived
    AppDatabase::class.java,
    "app.db",
)
    .fallbackToDestructiveMigration() // dev-only: wipe on schema change (DON'T ship)
    .build()`},{t:"list",items:["**Make it a singleton**: opening a database is expensive and multiple instances cause conflicts — build one instance for the whole app (provide it via Hilt/DI as `@Singleton`).","**Use the application context**: the database outlives any Activity, so it must not hold an Activity context.","**Never do DB work on the main thread**: Room *enforces* this — a non-suspend, non-Flow query on the main thread throws (unless you explicitly `allowMainThreadQueries()`, which you shouldn't). Use `suspend`/`Flow`.","**Type converters**: SQLite only stores primitives/String/blob. For custom types (a `Date`, an enum, a list), you write a `@TypeConverter` telling Room how to convert to/from a storable type (`Date` ↔ `Long`)."]},{t:"note",text:"Room essentials: it's a compile-time-verified ORM over SQLite. Three parts — @Entity (table), @Dao (queries), @Database (config + version). Flow queries auto-update, making Room the single source of truth (observe local, write into it). suspend/Flow queries are main-safe. @Transaction gives atomicity and speeds up batch writes. Build one singleton with the application context; never query on the main thread."}]}],xT=[{level:"junior",q:"What is Room and why use it instead of raw SQLite?",a:[{t:"p",text:"**Room is a Jetpack library that acts as an ORM (object-relational mapping) layer over SQLite.** SQLite is the embedded database built into Android, but using it directly means writing SQL as raw strings, manually iterating `Cursor` results and mapping them to objects, and getting no compile-time safety. Room lets you define your schema with annotated Kotlin classes and your queries as annotated methods, and it generates all that boilerplate for you."},{t:"list",items:["**Compile-time SQL verification** — the headline benefit. Room checks your `@Query` SQL against your schema *at build time*, so a misspelled column or wrong type is a compile error, not a runtime crash. Raw SQLite fails only at runtime.","**Less boilerplate** — no manual `Cursor` handling or object mapping; Room maps rows to your objects automatically.","**Reactive & coroutine support** — queries can return `Flow` (auto-updating when data changes), be `suspend` (main-safe), or return LiveData, integrating cleanly with modern Android."]},{t:"p",text:"Room is the recommended approach for local databases on Android; you'd only touch raw SQLite for unusual cases. The three building blocks are `@Entity` (a table), `@Dao` (your queries), and `@Database` (which ties them together and holds the version number)."}]},{level:"junior",q:"What are the three main components of Room?",a:[{t:"list",items:["**`@Entity`** — a Kotlin class mapped to a database *table*, where each property is a column. You mark the primary key with `@PrimaryKey`, and can customize column names with `@ColumnInfo`. This defines your schema.","**`@Dao`** (Data Access Object) — an interface declaring your database operations as annotated methods: `@Query` for SQL queries (verified at compile time), and `@Insert`/`@Update`/`@Delete`/`@Upsert` for common operations. Methods can be `suspend` (one-shot) or return `Flow`/`LiveData` (observable).","**`@Database`** — an abstract class that lists all your entities, exposes the DAOs, and carries a `version` number (used for migrations). You build a single instance of it with `Room.databaseBuilder`."]},{t:"code",title:"The three together",code:`@Entity data class User(@PrimaryKey val id: String, val name: String)

@Dao interface UserDao {
    @Query("SELECT * FROM User") fun observeAll(): Flow<List<User>>
    @Insert suspend fun insert(user: User)
}

@Database(entities = [User::class], version = 1)
abstract class AppDb : RoomDatabase() { abstract fun userDao(): UserDao }`},{t:"p",text:"Mental model: Entity = *what* you store (the table), DAO = *how* you access it (the queries), Database = the *container* that wires them together and versions the schema. You typically build one Database singleton and provide the DAOs through it via dependency injection."}]},{level:"junior",q:"What happens when a Room query returns a Flow?",a:[{t:"p",text:"**The Flow automatically re-emits a fresh result whenever the underlying data changes.** Room tracks which tables a query reads from; when *any* write modifies one of those tables (an insert, update, or delete), Room re-runs the query and emits the new result to every active collector. So a `Flow<List<User>>` from a query keeps your UI in sync with the database without any manual refresh."},{t:"p",text:"This is what makes Room the ideal **single source of truth**. The pattern is: the UI *observes* a Room Flow, and all writes (including network responses) go *into* the database. When you refresh from the network and write the results to Room, the Flow re-emits automatically and the UI updates — you never manually push data to the screen. Multiple screens observing the same query all update together. It also means offline works naturally, since reads always come from the local database. Contrast this with a `suspend` query, which runs *once* and returns a single result — you use `suspend` for one-shot operations (a single lookup, an insert) and `Flow` for data the UI should track over time."}]},{level:"junior",q:"Can you run Room queries on the main thread?",a:[{t:"p",text:"**No — and Room actively enforces this.** By default, if you try to run a synchronous (non-suspend, non-Flow) query on the main thread, Room throws an `IllegalStateException` telling you not to. This is because database operations involve disk I/O, which can be slow and would block the UI thread, causing jank or an ANR."},{t:"p",text:"The correct approach is to make your DAO methods either `suspend` functions (for one-shot operations) or return `Flow`/`LiveData` (for observable queries). Both are **main-safe** — Room automatically runs the actual database work on a background executor and delivers results appropriately, so you can call them from a main-thread coroutine (like `viewModelScope.launch`) without any manual threading. You do *not* need to wrap them in `withContext(Dispatchers.IO)` — Room already handles the threading, so that wrapping is redundant. There is an escape hatch, `allowMainThreadQueries()`, but you should essentially never use it; it exists for niche cases and defeats the safety Room provides."}]},{level:"senior",q:"What is a Room transaction and when do you need @Transaction?",a:[{t:"p",text:"**A transaction groups multiple database operations so they execute atomically — all of them succeed together, or all roll back together, with no partially-applied state ever visible.** You mark a DAO method `@Transaction`, and Room wraps its operations in a single SQLite transaction."},{t:"list",items:["**Atomicity — the primary reason**: when a logical operation spans several writes that must be consistent. Example: replacing a cart means deleting old items *and* inserting new ones. Without a transaction, a crash or error between the delete and insert leaves an empty cart. With `@Transaction`, if the insert fails, the delete is rolled back too — the cart is never left in a broken intermediate state.","**Read consistency**: `@Transaction` on a query method ensures all reads within it see a *consistent snapshot* of the database. This matters especially for `@Relation` queries, which internally do multiple reads (one for the parent, one for the children) — without a transaction, a concurrent write could change the data between those reads, giving an inconsistent combined result. Room actually requires/recommends `@Transaction` on relation queries for this reason.","**Performance — a big one**: batching many writes in a single transaction is dramatically faster than doing them individually. Each standalone insert is its own implicit transaction with its own disk-sync (fsync) overhead. Wrapping 1000 inserts in one transaction turns 1000 syncs into one, which can be orders of magnitude faster. So `@Transaction` is both a correctness *and* a performance tool for bulk operations."]},{t:"p",text:"**The senior framing**: reach for `@Transaction` whenever (a) multiple operations must be all-or-nothing, (b) a read must see a consistent snapshot across multiple internal queries (relations), or (c) you're doing bulk writes and want the performance of a single sync. It's the database-level guarantee that your multi-step operations don't leave corrupt or inconsistent state — the same 'atomic' guarantee databases are built to provide."}]},{level:"senior",q:"How does Room make the database a 'single source of truth', and why is that valuable?",a:[{t:"p",text:"**Room enables the single-source-of-truth (SSOT) pattern through its auto-updating Flow queries: you designate the database as the one place the app reads from, and everything — including network data — flows *into* it, so the UI is always a reflection of the local database.** The mechanism is Room's table-change tracking: a `Flow` query re-emits whenever its tables are written, so writing fresh network data to the DB automatically propagates to every observer."},{t:"code",title:"The SSOT repository pattern",code:`class ArticleRepository(private val dao: ArticleDao, private val api: Api) {
    // UI observes the DB — the single source of truth
    fun observeArticles(): Flow<List<Article>> =
        dao.observeArticles().map { it.map(ArticleEntity::toDomain) }

    // Network writes INTO the DB; observers update automatically
    suspend fun refresh() {
        val fresh = api.getArticles()
        dao.upsertAll(fresh.map { it.toEntity() })   // Flow above re-emits
    }
}`},{t:"list",items:["**Consistency across the app**: every screen observing the same query shows the *same* data and updates *together* when it changes — no screen can drift out of sync, because there's exactly one source. This eliminates a whole class of 'screen A shows the new value but screen B still shows the old' bugs.","**Offline-first for free**: since reads *always* come from the local DB, the app works offline by default — the network is just a mechanism to *update* the DB, not a dependency for *displaying* data. When offline, the user sees the last-cached data; when back online, a refresh updates the DB and the UI follows.","**Decoupled refresh**: writing data and displaying data are separate concerns. A pull-to-refresh, a push notification, or a background sync can all write to the DB from anywhere, and the UI updates automatically — no screen-to-screen communication, no event bus, no manual 'notify the UI' code.","**Simpler mental model**: the UI is a pure function of the database (`UI = f(DB)`), which is easy to reason about and test — you can verify behavior by writing to the DB and asserting emissions."]},{t:"p",text:"**Why it's valuable overall**: SSOT with Room turns 'keep the UI, the cache, and the network in sync' — a notoriously bug-prone coordination problem — into a one-directional flow where the database is authoritative and everything observes it. It's the foundation of Google's recommended offline-first architecture, and it's why Room's reactive Flow queries are considered its most important feature, not just a convenience."}]}],ST={id:"room-fundamentals",title:"Room & SQLite Fundamentals",tagline:"SQLite vs Room, @Entity/@Dao/@Database, Flow queries as single source of truth, suspend main-safety, transactions, and building the database.",tags:["Room","SQLite","DAO","Flow","Single Source of Truth"],content:kT,qa:xT},TT=[{heading:"Relationships between entities",blocks:[{t:"p",text:"Real data has relationships — a user *has many* posts, a playlist *contains many* songs. In a relational database these are modeled with **foreign keys** (a column in one table pointing to another table's key). Room lets you query related data together using `@Relation`, which fetches a parent and its children and assembles them into a combined object."},{t:"code",title:"One-to-many with @Relation",code:`@Entity data class User(@PrimaryKey val id: String, val name: String)

@Entity data class Post(
    @PrimaryKey val id: String,
    val userId: String,           // foreign key -> User.id
    val text: String,
)

// A combined object: a user with all their posts
data class UserWithPosts(
    @Embedded val user: User,
    @Relation(parentColumn = "id", entityColumn = "userId")
    val posts: List<Post>,
)

@Dao interface UserDao {
    @Transaction                  // relations do multiple reads — keep consistent
    @Query("SELECT * FROM User")
    fun getUsersWithPosts(): Flow<List<UserWithPosts>>
}`},{t:"list",items:["**`@Embedded`** — inlines another object's columns into the query result (here the `User`'s columns). **`@Relation`** — tells Room to fetch the related rows (the posts whose `userId` matches the user's `id`) and populate the `posts` list.","**Always annotate relation queries `@Transaction`** — Room does the parent query and the children query separately, so a transaction ensures they see a consistent snapshot (no write sneaking in between).","**Types of relations**: one-to-one (a single related object), one-to-many (a list, as above), and many-to-many (via an intermediate 'junction'/'associative' table with `@Junction`, e.g. students↔courses).","**`@ForeignKey`** — optionally declares an actual database foreign-key constraint (with `onDelete = CASCADE` etc.) for referential integrity — the DB enforces that a Post's `userId` refers to a real User. `@Relation` works without it, but foreign keys add DB-level integrity."]}]},{heading:"Why migrations exist",blocks:[{t:"p",text:"Your database schema evolves — you add a column, a table, an index. But users have the *old* schema on their devices with *their data* in it. A **migration** is code that transforms the database from one schema version to the next *while preserving the user's data*. Without a correct migration, changing the schema crashes the app (Room detects the schema mismatch and throws) or, worse, loses user data."},{t:"list",items:["**The version number** in `@Database(version = N)` tracks the schema version. When you change the schema, you bump the version and provide a migration from the old version to the new one.","**Room compares the schema at startup**: if the code's schema (version N) doesn't match the on-device database (version N-1) and there's no migration path, Room throws `IllegalStateException`. Migrations bridge the gap.","**The stakes**: this is *user data* — losing it (an account's saved content, offline data) is a serious bug. Migrations must be correct and tested."]}]},{heading:"Writing a migration",blocks:[{t:"code",title:"A migration adding a column",code:`val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(db: SupportSQLiteDatabase) {
        // raw SQL to transform the schema, preserving data
        db.execSQL("ALTER TABLE users ADD COLUMN phone TEXT")
    }
}

val MIGRATION_2_3 = object : Migration(2, 3) {
    override fun migrate(db: SupportSQLiteDatabase) {
        db.execSQL("CREATE TABLE IF NOT EXISTS orders (id TEXT PRIMARY KEY NOT NULL, userId TEXT)")
    }
}

Room.databaseBuilder(context, AppDatabase::class.java, "app.db")
    .addMigrations(MIGRATION_1_2, MIGRATION_2_3)   // register all migrations
    .build()`},{t:"list",items:["A `Migration(from, to)` runs raw SQL (`ALTER TABLE`, `CREATE TABLE`, etc.) to change the schema *without dropping existing data*. Room runs the chain of migrations needed to get from the device's version to the current one (e.g. 1→2→3).","**Complex migrations** (splitting a table, changing a column type, adding a NOT NULL column with a default) require careful SQL — often create a new table, copy transformed data, drop the old, rename. SQLite's limited `ALTER TABLE` sometimes forces this create-copy-drop-rename dance.","**Export the schema** (`room.schemaLocation`) — Room can export a JSON schema per version, which enables *automated migration testing* (`MigrationTestHelper`) that verifies a migration produces the expected schema and preserves data. Testing migrations is essential because they touch real user data."]}]},{heading:"Automated migrations and the destructive shortcut",blocks:[{t:"list",items:["**`@AutoMigration`** (Room 2.4+) — for *simple* schema changes (adding a column/table), Room can *generate* the migration for you: `@Database(autoMigrations = [AutoMigration(from = 1, to = 2)])`. You only hand-write migrations for changes Room can't infer (renames, deletes, type changes — where you use `@RenameColumn`/`@DeleteColumn` spec hints). Auto-migrations reduce boilerplate and error for the common cases.","**`fallbackToDestructiveMigration()`** — the shortcut that *drops and recreates* the database (losing all data) when a migration is missing. **Never ship this to production** — it wipes user data on every schema change. It's acceptable *only* during development (when your local data is disposable) to avoid writing migrations while iterating. Shipping it means users lose their data on app updates.","**The decision**: production apps must have real migrations (auto or manual) for every version bump. Destructive fallback is a dev convenience only."]}]},{heading:"Type converters and indices",blocks:[{t:"code",title:"TypeConverter for a non-primitive type",code:`class Converters {
    @TypeConverter fun fromDate(date: Date?): Long? = date?.time
    @TypeConverter fun toDate(millis: Long?): Date? = millis?.let { Date(it) }
}

@Database(entities = [Event::class], version = 1)
@TypeConverters(Converters::class)
abstract class AppDatabase : RoomDatabase() { /* ... */ }`},{t:"list",items:["**Type converters** — SQLite stores only primitives, String, and blobs. For a custom type (a `Date`, an enum, a `List` serialized to JSON), you write a `@TypeConverter` pair telling Room how to convert to/from a storable type. Register them with `@TypeConverters`.","**Indices** (`@Entity(indices = [Index(\"userId\")])`) — like a book's index, they speed up queries that filter/sort on a column, at the cost of extra storage and slightly slower writes. Add an index to columns you frequently query by (especially foreign keys). Room warns if a foreign key column isn't indexed.","**`@Fts4`** — full-text search support for fast text search over a table."]},{t:"note",text:"Relations & migrations essentials: model relationships with @Relation + @Embedded (annotate the query @Transaction); foreign keys add DB-level integrity. Migrations transform the schema across versions while preserving user data — bump the version, provide a Migration (or @AutoMigration for simple changes), and TEST them. Never ship fallbackToDestructiveMigration (it wipes data). Type converters handle non-primitive types; indices speed up filtered queries."}]}],CT=[{level:"junior",q:"How do you model a one-to-many relationship in Room?",a:[{t:"p",text:"**You use `@Relation` together with `@Embedded` in a combined data class that Room populates.** The child entity holds a foreign-key column pointing to the parent, and you define a result class that embeds the parent and declares the related children."},{t:"code",title:"A user with their posts",code:`data class UserWithPosts(
    @Embedded val user: User,                          // parent columns inlined
    @Relation(parentColumn = "id", entityColumn = "userId")
    val posts: List<Post>,                             // matching children
)

@Transaction @Query("SELECT * FROM User")
fun getUsersWithPosts(): Flow<List<UserWithPosts>>`},{t:"list",items:["**`@Embedded`** inlines the parent object's columns into the result. **`@Relation`** tells Room to fetch the child rows whose `entityColumn` (`userId`) matches the parent's `parentColumn` (`id`) and put them in the list.","**Annotate the query `@Transaction`** — Room runs the parent query and the children query separately, so a transaction ensures they see a consistent snapshot with no write in between.","The same pattern covers one-to-one (a single related object instead of a list) and many-to-many (adding a `@Junction` through an associative table)."]},{t:"p",text:"Room assembles the combined objects for you — you get a list of users each with their posts populated, in one observable query. Optionally add `@ForeignKey` on the child entity for database-level referential integrity (and cascade-delete behavior), though `@Relation` works without it."}]},{level:"junior",q:"What is a Room migration and why is it needed?",a:[{t:"p",text:"**A migration is code that transforms the database from one schema version to the next while preserving the user's existing data.** As your app evolves, you change the schema — add a column, add a table, add an index. But users already have the *old* schema on their devices *with their data in it*. A migration bridges the old schema to the new one without losing that data."},{t:"p",text:"**Why it's needed**: Room tracks a schema `version` number. At startup, it compares the schema your code expects against the database actually on the device. If they differ and there's no migration path, Room throws an exception — the app crashes. So whenever you bump the version (because you changed the schema), you must provide a migration from the previous version. The stakes are high because this is *user data* — an incorrect migration or a missing one can crash the app or lose data the user cares about (offline content, saved items). You write a `Migration(from, to)` containing SQL (`ALTER TABLE ADD COLUMN`, `CREATE TABLE`, etc.) that changes the schema while keeping existing rows, register it with `addMigrations(...)`, and — importantly — test it."}]},{level:"junior",q:"What does fallbackToDestructiveMigration do and when is it acceptable?",a:[{t:"p",text:"**`fallbackToDestructiveMigration()` tells Room that, if it can't find a migration path for a schema change, it should *drop the entire database and recreate it from scratch* — destroying all existing data.** It's a shortcut to avoid writing migrations: instead of transforming the data, Room just wipes it and starts fresh with the new schema."},{t:"p",text:"**When it's acceptable: only during development, never in production.** While you're actively iterating on the schema locally, your test data is disposable, and constantly writing migrations for every experimental change is tedious — so destructive fallback lets you change the schema freely without migrations. But shipping it to production means **every user loses all their local data whenever you change the schema** in an app update — a serious bug that would wipe accounts' offline content, saved items, drafts, etc. Production apps must have real migrations (hand-written or `@AutoMigration`) for every version bump. The rule of thumb: if you see `fallbackToDestructiveMigration()` in code destined for release, that's a red flag to fix before shipping."}]},{level:"junior",q:"What is a TypeConverter in Room?",a:[{t:"p",text:"**A TypeConverter tells Room how to store a type that SQLite can't store natively.** SQLite only understands a few storage types — integers, real numbers, text, and blobs. So a custom type like `Date`, an enum, or a `List` has no direct representation. A `@TypeConverter` is a pair of functions that convert your custom type *to* a storable type (for writing) and *back* (for reading)."},{t:"code",title:"Storing a Date as a Long",code:`class Converters {
    @TypeConverter fun fromDate(date: Date?): Long? = date?.time
    @TypeConverter fun toDate(millis: Long?): Date? = millis?.let { Date(it) }
}
// Register with @TypeConverters(Converters::class) on the @Database`},{t:"p",text:"Here, Room stores a `Date` as its millisecond `Long` value and reconstructs the `Date` when reading. You register the converter class on your `@Database` (or a specific entity/DAO) with `@TypeConverters`, and then you can use `Date` fields in entities transparently. Common uses: dates (↔ Long), enums (↔ String name or Int ordinal), and small lists/objects (↔ a JSON String via a serialization library). One caution: converting a big object to JSON and storing it as a String works but you lose the ability to query on its contents — for data you need to query, model it as proper columns/tables instead of a serialized blob."}]},{level:"senior",q:"How do you handle a complex migration, like changing a column type or splitting a table?",a:[{t:"p",text:"**Complex migrations usually require the 'create new table, copy transformed data, drop old, rename' pattern, because SQLite's `ALTER TABLE` is limited — it can add columns and rename, but can't change a column's type, drop a column (in older versions), or add constraints to an existing table.** So for anything beyond a simple add, you rebuild the table."},{t:"code",title:"The create-copy-drop-rename dance",code:`val MIGRATION_3_4 = object : Migration(3, 4) {
    override fun migrate(db: SupportSQLiteDatabase) {
        // 1. Create the new table with the desired schema
        db.execSQL("""
            CREATE TABLE users_new (
                id TEXT PRIMARY KEY NOT NULL,
                name TEXT NOT NULL,
                age INTEGER NOT NULL DEFAULT 0   -- new column / new type
            )
        """)
        // 2. Copy and transform existing data
        db.execSQL("""
            INSERT INTO users_new (id, name, age)
            SELECT id, name, CAST(age_text AS INTEGER) FROM users
        """)
        // 3. Drop the old table
        db.execSQL("DROP TABLE users")
        // 4. Rename the new table to the original name
        db.execSQL("ALTER TABLE users_new RENAME TO users")
    }
}`},{t:"list",items:["**Why the four steps**: since you can't alter the column in place, you create a fresh table with the correct schema, `INSERT ... SELECT` to copy data (applying any transformation like `CAST`, splitting a column, or providing defaults for NOT NULL additions), drop the old table, and rename the new one to take its place. Data is preserved because you explicitly copy it.","**Splitting a table** follows the same idea: create the two new tables, `INSERT ... SELECT` the appropriate columns into each from the old table, drop the old one. For moving data to a related table, you populate the child table with a foreign key derived from the old rows.","**Recreate indices and foreign keys** on the new table — dropping the old table drops its indices, so re-add them in the migration.","**Test it rigorously** with `MigrationTestHelper`: export the schema JSON per version (`room.schemaLocation`), create a DB at the old version with representative data, run the migration, and assert both the resulting schema *and* that the data was correctly transformed and preserved. Migration bugs corrupt or lose real user data, so this testing is not optional."]},{t:"p",text:"**The senior framing**: complex migrations are where the 'preserve user data' responsibility gets real — you're hand-transforming production data through SQL, and a mistake is unrecoverable for users who've already updated. So the discipline is: use the create-copy-drop-rename pattern for anything SQLite can't do in place, be meticulous about transforming and copying every column, re-establish indices/constraints, and *test with the schema-export + MigrationTestHelper* on realistic data before shipping. For simple additive changes, prefer `@AutoMigration` to eliminate the chance of hand-written SQL errors entirely."}]},{level:"senior",q:"When should you add an index to a Room entity, and what are the trade-offs?",a:[{t:"p",text:"**Add an index to a column when you frequently query, filter (`WHERE`), sort (`ORDER BY`), or join on it — an index is a separate sorted data structure that lets SQLite find matching rows quickly instead of scanning the whole table.** Without an index, a query filtering on a column does a full table scan (O(n)); with one, it's a fast lookup (roughly O(log n)). For large tables, this is the difference between an instant query and a slow one."},{t:"list",items:["**Index foreign-key columns**: columns you join on (a `userId` in a posts table) should almost always be indexed — Room even warns you if a `@ForeignKey` column isn't indexed, because joins on an unindexed column are slow.","**Index columns in frequent WHERE/ORDER BY clauses**: if you constantly query `WHERE status = ?` or `ORDER BY createdAt`, indexing those columns speeds those queries dramatically on large tables.","**Unique indices** (`Index(value = [...], unique = true)`) also enforce uniqueness at the DB level, useful for natural keys."]},{t:"list",items:["**Trade-off 1 — storage**: each index is an additional data structure stored on disk, so indices increase the database size. Many indices on a large table add up.","**Trade-off 2 — slower writes**: every insert/update/delete must *also* update every index on the affected columns. So a table with many indices has slower writes. If a table is write-heavy and rarely queried by a given column, indexing that column costs more than it saves.","**Trade-off 3 — over-indexing is a real anti-pattern**: indexing every column 'just in case' bloats storage and slows all writes while most indices go unused. Index based on *actual query patterns*, not speculatively."]},{t:"p",text:"**The senior framing**: indexing is a read-vs-write trade-off — you speed up reads on the indexed column at the cost of storage and write speed. The heuristic is 'index the columns you query by, especially foreign keys and frequent filter/sort columns, on tables large enough for scans to matter; don't index write-heavy columns you rarely filter on.' On a small table (a few dozen rows), indices barely matter since a full scan is already fast. The right approach is to profile actual slow queries (SQLite's `EXPLAIN QUERY PLAN` shows whether an index is used) and add indices where the query patterns and table size justify them."}]}],MT={id:"room-relations-migrations",title:"Room Relations & Migrations",tagline:"@Relation/@Embedded relationships, foreign keys, schema migrations & @AutoMigration, the destructive fallback, type converters, and indices.",tags:["Room","Relations","Migrations","TypeConverter","Indices"],content:TT,qa:CT},IT=[{heading:"The problem: storing small key-value data",blocks:[{t:"p",text:"Apps need to store small pieces of data — user settings, a theme choice, an auth token, a 'has the user seen onboarding' flag, the last-selected tab. This is too small and simple for a database. Android's tools for this are **SharedPreferences** (the old way) and **DataStore** (the modern replacement). Both store key-value data, but DataStore fixes serious flaws in SharedPreferences."}]},{heading:"SharedPreferences and its problems",blocks:[{t:"p",text:"**SharedPreferences** is the classic API: a simple key-value store backed by an XML file, accessed synchronously. It works, but it has well-known flaws that DataStore was created to solve:"},{t:"code",title:"SharedPreferences — simple but flawed",code:`val prefs = context.getSharedPreferences("settings", Context.MODE_PRIVATE)
prefs.edit().putString("theme", "dark").apply()   // async write
val theme = prefs.getString("theme", "light")     // SYNCHRONOUS read — on main thread!`},{t:"list",items:["**Synchronous API that can block the main thread**: reads (`getString`) are synchronous, and the first access to a SharedPreferences file loads and parses the entire XML *on the calling thread* — often the main thread — which can cause jank or ANRs (a documented source of ANRs).","**`apply()` vs `commit()` gotcha**: `commit()` writes synchronously (blocks); `apply()` writes asynchronously but can still cause issues (it blocks the main thread on `fsync` during certain lifecycle events like `onPause`).","**No error signaling**: if a write fails, you don't find out — errors are silently swallowed.","**No type safety**: keys are strings, values are untyped — easy to mismatch a key or type at runtime.","**Not reactive**: to observe changes you register an `OnSharedPreferenceChangeListener` (clunky, error-prone) — there's no Flow of updates.","**Runtime exceptions on parse errors / not transactional**: partial updates are possible; no strong consistency guarantees."]}]},{heading:"DataStore — the modern replacement",blocks:[{t:"p",text:"**DataStore** is Jetpack's modern key-value (and typed) storage, built on **Kotlin coroutines and Flow**. It stores data asynchronously, exposes it as a `Flow` (reactive), handles errors, and never blocks the main thread. It comes in two flavors: **Preferences DataStore** (key-value, like SharedPreferences but better) and **Proto DataStore** (typed, schema-defined with Protocol Buffers)."},{t:"code",title:"Preferences DataStore",code:`// Define the DataStore (once, as a top-level property)
val Context.dataStore by preferencesDataStore(name = "settings")
private val THEME_KEY = stringPreferencesKey("theme")

// READ — a Flow that emits on every change (async, main-safe)
val themeFlow: Flow<String> = context.dataStore.data
    .map { prefs -> prefs[THEME_KEY] ?: "light" }

// WRITE — a suspend function (async, transactional, error-aware)
suspend fun setTheme(theme: String) {
    context.dataStore.edit { prefs -> prefs[THEME_KEY] = theme }
}`},{t:"list",items:["**Fully asynchronous & main-safe**: reads are a `Flow` and writes are `suspend` functions — no blocking, no ANR risk. All I/O happens off the main thread via coroutines.","**Reactive by default**: `dataStore.data` is a `Flow` that emits the current value and re-emits on every change — perfect for observing settings in the UI (collect it like any Flow).","**Transactional & consistent**: writes are atomic (via `edit { }`), and DataStore guarantees consistency — no partial/corrupt states.","**Error handling**: reads can surface errors (e.g. `IOException` on a read failure) that you can `catch` in the Flow, rather than silently failing.","**Type-safe keys**: `stringPreferencesKey`, `intPreferencesKey`, etc. give typed keys, reducing runtime mistakes."]}]},{heading:"Preferences DataStore vs Proto DataStore",blocks:[{t:"table",headers:["","Preferences DataStore","Proto DataStore"],rows:[["Data shape","key-value pairs (untyped keys)","a defined schema (typed object) via Protocol Buffers"],["Type safety","typed keys, but no schema for the whole object","full type safety — the whole structure is a typed message"],["Setup","minimal (just define keys)","more (define a .proto schema + serializer)"],["Best for","simple, independent settings (theme, flags)","structured, related settings (a UserPreferences object)"]]},{t:"list",items:["**Preferences DataStore** — the direct SharedPreferences replacement: key-value, quick to set up, for loose independent settings. Its limitation: no schema, so you can still typo a key or store the wrong shape.","**Proto DataStore** — stores a *typed object* defined by a Protocol Buffers schema, giving full type safety across the whole structure and validation. More setup (define the `.proto`, a serializer), but ideal when your preferences form a cohesive structured object (all of a user's settings together).","**Choose Preferences DataStore** for simple cases (most apps); **Proto DataStore** when you want a strongly-typed, schema-validated settings object."]}]},{heading:"When to use what — the storage decision",blocks:[{t:"table",headers:["Data","Storage"],rows:[["Small key-value settings, flags, tokens","DataStore (Preferences or Proto)"],["Structured, queryable, relational data (lists, entities)","Room"],["Large files, images, media","the file system (internal/external storage)"],["Legacy code already using it","SharedPreferences (migrate to DataStore over time)"]]},{t:"list",items:["**DataStore for settings, Room for data**: DataStore is for a *handful* of key-value settings, not for structured or large data — you can't query it, and it loads everything into memory. Lists of entities, anything you filter/sort/relate → Room.","**Migration path**: DataStore provides a `SharedPreferencesMigration` to import existing SharedPreferences data into DataStore automatically, so you can migrate legacy prefs cleanly.","**Not for secrets in plaintext**: DataStore/SharedPreferences store data unencrypted by default. For sensitive data (tokens, keys), use encryption — `EncryptedSharedPreferences`, or encrypt values before storing, or the Android Keystore for keys."]},{t:"note",text:"The comparison to nail: SharedPreferences is synchronous (can block the main thread → ANRs), swallows errors, and isn't reactive. DataStore is coroutine/Flow-based — fully async and main-safe, reactive (data is a Flow), transactional, and error-aware. Preferences DataStore = key-value replacement; Proto DataStore = typed schema. Use DataStore for small settings, Room for structured data, files for large blobs. DataStore is the recommended modern choice."}]}],AT=[{level:"junior",q:"What is SharedPreferences used for and what are its main problems?",a:[{t:"p",text:"**SharedPreferences is a simple key-value store backed by an XML file, used for small pieces of data** — settings, a theme choice, feature flags, an auth token, a 'seen onboarding' flag. It's the classic Android API for lightweight persistent data that's too small for a database."},{t:"list",items:["**Synchronous, can block the main thread**: reads are synchronous, and the first access loads and parses the whole XML file on the calling thread — often the main thread — which is a documented cause of jank and ANRs.","**No error handling**: if a write fails, it's silently swallowed; you have no way to know.","**Not reactive**: observing changes requires a clunky `OnSharedPreferenceChangeListener`; there's no Flow of updates.","**No type safety**: string keys and untyped values make mismatches easy at runtime.","**`apply()` vs `commit()` confusion**: `commit()` blocks; `apply()` is async but can still block on `fsync` during lifecycle events."]},{t:"p",text:"These flaws — especially the main-thread blocking and lack of reactivity — are why Google introduced **DataStore** as the modern replacement. SharedPreferences still works and is fine in legacy code, but new code should use DataStore, which is fully async (coroutines/Flow), reactive, transactional, and error-aware."}]},{level:"junior",q:"What is DataStore and how is it better than SharedPreferences?",a:[{t:"p",text:"**DataStore is Jetpack's modern key-value (and typed) storage, built on Kotlin coroutines and Flow.** It solves SharedPreferences' problems by being fully asynchronous and reactive: reads are exposed as a `Flow`, and writes are `suspend` functions, so nothing blocks the main thread."},{t:"list",items:["**Fully async & main-safe** — reads (`Flow`) and writes (`suspend`) run off the main thread via coroutines, eliminating the ANR risk SharedPreferences had.","**Reactive** — `dataStore.data` is a `Flow` that emits the current value and re-emits on every change, so you observe settings in the UI like any other Flow (no manual change listeners).","**Transactional & consistent** — writes go through `edit { }` atomically; no partial or corrupt states.","**Error-aware** — read failures surface as catchable errors in the Flow, instead of being silently swallowed.","**Typed keys** — `stringPreferencesKey`, `intPreferencesKey`, etc. reduce runtime key/type mistakes."]},{t:"code",title:"The reactive read + suspend write",code:`val theme: Flow<String> = context.dataStore.data.map { it[THEME_KEY] ?: "light" }
suspend fun setTheme(v: String) = context.dataStore.edit { it[THEME_KEY] = v }`},{t:"p",text:"It comes in two forms: **Preferences DataStore** (key-value, the direct SharedPreferences replacement) and **Proto DataStore** (a typed object defined by a Protocol Buffers schema, for full type safety). DataStore is the recommended modern choice for small persistent data."}]},{level:"junior",q:"What is the difference between Preferences DataStore and Proto DataStore?",a:[{t:"p",text:"**Both are DataStore, but they differ in how strongly typed the stored data is.** *Preferences DataStore* stores loose key-value pairs (like SharedPreferences, but async and reactive) — you use typed keys, but there's no schema for the overall data. *Proto DataStore* stores a single *typed object* defined by a Protocol Buffers schema, giving full type safety and validation across the whole structure."},{t:"list",items:["**Preferences DataStore** — minimal setup (just define keys), good for a handful of simple, independent settings (theme, a flag, a token). Its limitation: no schema, so you can still typo a key or store an unexpected shape; the 'object' is just a bag of keys.","**Proto DataStore** — more setup (define a `.proto` schema and a serializer), but the entire stored value is a strongly-typed message, so the compiler enforces the structure and you get validation. Ideal when your preferences form a cohesive, structured object — e.g. a `UserSettings` with several related fields you want to treat as one typed unit."]},{t:"p",text:"**How to choose**: Preferences DataStore for most apps and simple independent settings; Proto DataStore when you want a strongly-typed, schema-validated settings object and are willing to define the schema. The trade-off is setup effort vs type safety — Proto gives more guarantees at the cost of more boilerplate."}]},{level:"junior",q:"When would you use DataStore vs Room vs the file system?",a:[{t:"list",items:["**DataStore** — for *small key-value settings*: theme, flags, an auth token, last-selected tab, onboarding-seen. A handful of simple values you read and write, not query.","**Room** — for *structured, queryable, relational data*: lists of entities, anything you filter, sort, join, or that has relationships. A cached feed, saved items, a local database of records.","**File system** — for *large files and binary blobs*: images, videos, downloaded documents, cached media. Internal storage (private to your app) or external/scoped storage."]},{t:"p",text:"The decision comes down to the data's shape and size. DataStore holds everything in memory and can't be queried, so it's wrong for lists or large data — a few settings, yes; a hundred cached articles, no (that's Room). Room is for structured data you need to query. Files are for large binary content that doesn't belong in a database or key-value store. A common mistake is stuffing a serialized list into SharedPreferences/DataStore as a JSON string — it works for tiny data but doesn't scale and can't be queried; use Room instead. One more consideration: none of these encrypt data by default, so for sensitive values (tokens) you'd add encryption (EncryptedSharedPreferences, or encrypt before storing, or the Keystore for keys)."}]},{level:"senior",q:"Why exactly does SharedPreferences cause ANRs, and how does DataStore's design prevent that?",a:[{t:"p",text:"**SharedPreferences causes ANRs because of *when and where* its I/O happens: the first read of a preferences file synchronously loads and parses the entire XML file on the calling thread, and certain writes force a synchronous disk sync on the main thread.** The two specific mechanisms:"},{t:"list",items:["**Synchronous first load**: `getSharedPreferences` returns immediately, but the XML file is loaded lazily on first *access* (`getString`, etc.), and that load/parse is *blocking*. If it happens on the main thread (common — reading a setting to configure UI), and the file is large or the disk is busy, the main thread blocks. Enough blocking → ANR.","**`apply()` and lifecycle fsync**: `apply()` writes to memory immediately and schedules the disk write asynchronously — but Android *waits* for all pending `apply()` disk writes to complete during certain lifecycle transitions (notably `Activity.onPause`/`onStop` and service commands), *on the main thread*. So a burst of `apply()` calls can turn into a blocking `fsync` at `onPause`, stalling the transition — a well-documented ANR source. `commit()` is even worse (synchronous by design)."]},{t:"list",items:["**DataStore's design eliminates both**: it's built on coroutines, so *all* I/O — reads and writes — happens on a background dispatcher (`Dispatchers.IO` internally), never on the main thread. Reads are a `Flow` you collect (suspending, off-main); writes are `suspend` functions (off-main). There is no synchronous main-thread I/O path at all, so the blocking-load and fsync-at-onPause problems simply can't occur.","**No lifecycle-coupled fsync**: because writes are already async coroutine operations that you `await` where appropriate, there's no hidden 'block the main thread at onPause to flush pending writes' behavior.","**Transactional consistency**: DataStore serializes writes through a single actor and guarantees atomic updates, so it also avoids the partial-write/corruption issues SharedPreferences could have — without any main-thread cost."]},{t:"p",text:"**The senior framing**: the SharedPreferences ANR problem isn't 'it's slow', it's that its *synchronous API forces disk I/O onto the main thread* at read time and at lifecycle-coupled flush time. DataStore fixes it at the API level — by making I/O exclusively asynchronous through coroutines, there's no way to accidentally do main-thread disk I/O. That architectural shift (from a synchronous API to a coroutine/Flow-based one) is the whole reason DataStore exists and why it's the recommended replacement, even though for tiny apps the practical ANR risk of SharedPreferences is small."}]},{level:"senior",q:"How would you migrate from SharedPreferences to DataStore in a shipping app without losing data?",a:[{t:"p",text:"**DataStore provides a built-in `SharedPreferencesMigration` that automatically imports existing SharedPreferences data into DataStore the first time the DataStore is accessed, so you migrate without manual copying or data loss.** You configure the DataStore with the migration, pointing at the old SharedPreferences file(s), and DataStore reads the old values in and (optionally) deletes the old file once migrated."},{t:"code",title:"Configuring the automatic migration",code:`val Context.dataStore by preferencesDataStore(
    name = "settings",
    produceMigrations = { context ->
        listOf(SharedPreferencesMigration(context, "old_prefs_name"))
    }
)
// On first access, values from 'old_prefs_name' are copied into DataStore.`},{t:"list",items:["**The migration runs once, lazily**: the first time DataStore's `data` Flow is collected or edited, the migration copies the SharedPreferences keys/values into DataStore, then marks the file as migrated (and can clean it up). Subsequent accesses skip it. So it's transparent and one-time.","**Do it incrementally, not all at once**: in a large app, you don't rewrite every SharedPreferences usage in one PR. Migrate one settings area at a time — set up the DataStore with the migration, switch the *reads and writes* for that area to DataStore, ship, verify, then move to the next. The migration handles the data; you handle the code call sites gradually.","**Watch for concurrent access during transition**: while migrating, make sure code isn't *still writing* to the old SharedPreferences after DataStore has taken over (or those writes would be lost/ignored) — the migration is a one-way import. Route all access for a migrated key through DataStore.","**Test the migration path**: write a test (or manual QA) that starts with a populated old SharedPreferences file, accesses DataStore, and verifies the values came through correctly — since this touches real user settings on update, you want to confirm nothing is dropped or type-mismatched.","**Proto DataStore migration**: if migrating to Proto DataStore, you map the old key-value pairs into the typed proto message in the migration's transform — a bit more work but the same one-time-import mechanism."]},{t:"p",text:"**The senior framing**: the migration itself is a solved problem (`SharedPreferencesMigration` does the data copy safely and once), so the real work is *rolling it out incrementally* — migrating settings area by area, ensuring each migrated key is accessed *only* through DataStore afterward, and testing that existing users' data survives the update. This lets you modernize a shipping app's storage without a risky big-bang rewrite or any user-facing data loss."}]}],ET={id:"datastore-prefs",title:"DataStore & SharedPreferences",tagline:"SharedPreferences and its ANR/reactivity flaws, DataStore's coroutine/Flow design, Preferences vs Proto DataStore, the storage decision, and migration.",tags:["DataStore","SharedPreferences","Preferences","Storage","ANR"],content:IT,qa:AT},PT=[{heading:"Why caching and offline-first matter",blocks:[{t:"p",text:"Mobile networks are unreliable — slow, intermittent, or absent. An app that shows a spinner and nothing else when offline is a poor experience. **Caching** means storing data locally so it's available instantly and without the network; **offline-first** is an architecture where the app is *designed* to work from local data by default, treating the network as a way to *update* that local data rather than a prerequisite for showing anything. Done well, the app feels fast (instant local reads), works offline, and uses less data/battery."}]},{heading:"Single source of truth — the foundation",blocks:[{t:"p",text:"Offline-first is built on the **single source of truth (SSOT)** pattern: one local store (usually a Room database) is the *authoritative* source the app reads from, and everything — including network responses — writes *into* it. The UI never reads directly from the network; it observes the local store, which the network updates. This is the same pattern covered in Room and Architecture, and it's what makes offline-first natural."},{t:"code",title:"The SSOT / observe-local, refresh-remote pattern",code:`class ArticleRepository(
    private val dao: ArticleDao,     // the single source of truth
    private val api: Api,
) {
    // READS always come from the local DB — works offline, instant
    fun observeArticles(): Flow<List<Article>> =
        dao.observeArticles().map { it.map(Entity::toDomain) }

    // NETWORK writes into the DB; the Flow above re-emits automatically
    suspend fun refresh(): Result<Unit> = runCatching {
        val fresh = api.getArticles()
        dao.upsertAll(fresh.map { it.toEntity() })
    }
}`},{t:"list",items:["**Reads never depend on the network**: the UI observes the DB Flow, so it always has *something* to show (the last-cached data), even offline. When a refresh succeeds, the DB updates and the UI follows automatically.","**Writes go into the DB**: a network fetch is just a mechanism to *update the local source of truth*. This decouples 'display data' from 'fetch data' — the two happen independently.","**Errors don't blank the screen**: if a refresh fails (offline), you still show the cached data and surface the error non-destructively (a snackbar), rather than an empty error screen."]}]},{heading:"Caching strategies",blocks:[{t:"table",headers:["Strategy","How it works","Trade-off"],rows:[["Cache-first (offline-first)","read cache immediately; refresh from network in background","instant + offline, but may briefly show stale data"],["Network-first","try network; fall back to cache on failure","freshest data, but slow/blocked when network is slow"],["Cache-then-network","show cache instantly, then update with network result","best UX — fast AND fresh (the common choice)"],["Network-only","always fetch, no cache","always fresh, but no offline and slow"]]},{t:"list",items:["**Cache-then-network** (a.k.a. stale-while-revalidate) is the most common for offline-first: show the cached data immediately (instant, works offline), and *simultaneously* kick off a network refresh that updates the cache (and thus the UI) when it returns. The user sees content instantly and it silently updates. The SSOT Flow pattern gives this naturally — observe the DB (instant cache), trigger a refresh (updates DB → UI).","**Network-first** suits data that must be current (a bank balance) where showing stale data is worse than waiting — but degrade to cache on failure.","**Choose per data type**: a feed → cache-then-network; a rarely-changing config → cache-first with occasional refresh; a payment confirmation → network-only or network-first."]}]},{heading:"Cache invalidation and freshness",blocks:[{t:"p",text:'"There are only two hard things in computer science: cache invalidation and naming things." The core challenge of caching is knowing *when* cached data is too stale to trust. Strategies:'},{t:"list",items:["**Time-based (TTL)**: store a timestamp with cached data; consider it stale after N minutes and refresh. Simple and common — 'refresh if the cache is older than 5 minutes'.","**Event-based**: invalidate on specific triggers — a pull-to-refresh, a push notification signaling new data, a user action that changes the data.","**Version/ETag-based**: the server provides a version tag (ETag); on refresh you send it, and the server responds 'not modified' (cheap) or with new data — efficient conditional fetching (HTTP caching).","**Manual/explicit**: the app decides when to refresh (on screen open, on app foreground, on a timer). Common combined with TTL.","**The trade-off**: refresh too often → wasted data/battery and unnecessary load; refresh too rarely → stale data. TTL + explicit refresh (pull-to-refresh) covers most cases."]}]},{heading:"Offline writes and syncing",blocks:[{t:"p",text:"Reading offline is straightforward (serve from cache). *Writing* offline is harder — the user creates or edits data with no network. Offline-first apps handle this with **local-first writes and background sync**:"},{t:"list",items:["**Write locally first**: apply the change to the local DB immediately (so the UI updates and the user isn't blocked), and mark the record as 'pending sync' (a `syncStatus` flag or a separate outbox table).","**Sync in the background**: a background worker (WorkManager, so it survives process death and runs when network returns) reads pending changes and pushes them to the server, then marks them synced. WorkManager's network constraint means it runs the sync automatically when connectivity is available.","**Conflict resolution**: when local and server versions diverge (the record changed on the server while you were offline), you need a policy — last-write-wins (simplest), server-wins, client-wins, or a merge. This is the genuinely hard part of offline sync and is a repository/domain concern.","**The outbox pattern**: queue outgoing operations durably (in the DB) so they survive app kills and are retried until they succeed — a reliable way to guarantee eventual delivery of offline changes."]}]},{heading:"HTTP caching and memory caching",blocks:[{t:"list",items:["**HTTP-level caching (OkHttp)**: OkHttp can cache HTTP *responses* automatically based on `Cache-Control`/`ETag` headers (`Cache` set on the client). This caches at the network layer — useful for images and responses that rarely change — and is complementary to app-level DB caching (which gives you queryable, structured, observable data).","**Memory cache**: an in-memory cache (a `Map`, an `LruCache`) for the fastest access to recently-used data, backed by the DB (disk) which is backed by the network — a three-tier hierarchy (memory → disk → network). Image loaders (Coil, Glide) do exactly this: memory cache → disk cache → network.","**Image caching specifically**: use a library (Coil/Glide) — they handle memory + disk caching, downsampling, and lifecycle automatically. Never hand-roll image caching.","**Layering**: a mature app has memory cache (fastest, volatile) → Room/disk (persistent, queryable, SSOT) → network (source of new data). Reads try the fastest available; writes populate the layers."]},{t:"note",text:"Offline-first essentials: build on SSOT (Room as authoritative; UI observes DB, network writes into it). Cache-then-network (show cache instantly, refresh in background) is the common strategy. Invalidate with TTL/events/ETags — balancing freshness vs waste. Offline writes go local-first + a WorkManager background sync (with a conflict-resolution policy and an outbox for durability). Layer caches memory → disk → network; use Coil/Glide for images."}]}],RT=[{level:"junior",q:"What does 'offline-first' mean and why is it good?",a:[{t:"p",text:"**Offline-first is an architecture where the app is designed to work from *local data* by default, treating the network as a way to *update* that local data rather than a prerequisite for showing anything.** Instead of 'fetch from network → show result (or spinner/error if offline)', the flow is 'show local data instantly → update it from the network in the background when possible'."},{t:"list",items:["**It feels fast**: reads come from a local database instantly, with no network round-trip — the UI appears immediately with cached content.","**It works offline**: because the app displays local data, it's usable with no connection — you see the last-synced content instead of an error screen.","**It's resilient**: a failed network refresh doesn't blank the screen; you keep showing cached data and surface the error gently.","**It saves data and battery**: you fetch only to update, not on every view, and can batch/schedule syncs efficiently."]},{t:"p",text:"The foundation is the *single source of truth* pattern: a local store (usually Room) is authoritative, the UI observes it, and the network writes into it. This is Google's recommended architecture because mobile networks are unreliable, and an app that gracefully handles being offline is far better than one that's helpless without a connection."}]},{level:"junior",q:"What is the single source of truth pattern and how does it enable offline support?",a:[{t:"p",text:"**Single source of truth (SSOT) means one local store — typically a Room database — is the *authoritative* source the app reads from, and everything (including network responses) writes *into* it.** The UI never reads directly from the network; it observes the local store, and the network's only job is to keep that store updated."},{t:"code",title:"Observe local, refresh remote",code:`fun observeData(): Flow<List<Item>> = dao.observeAll()  // UI reads the DB
suspend fun refresh() { dao.upsertAll(api.fetch()) }    // network writes to DB`},{t:"p",text:"**How it enables offline support**: since the UI always reads from the local DB, it *always has data to show* — the last-cached data — regardless of connectivity. The network being available or not only affects whether a *refresh* succeeds; it never affects whether the app can display something. When you're online and a refresh succeeds, the DB updates and (because Room's Flow queries auto-emit) the UI updates automatically. When you're offline, you simply see the cached data and can surface the connection error without wiping the screen. This cleanly separates 'displaying data' (always from the DB) from 'fetching data' (an independent update mechanism), which is exactly what makes offline behavior natural rather than a special case you bolt on."}]},{level:"junior",q:"What is 'cache-then-network' and why is it a popular strategy?",a:[{t:"p",text:"**Cache-then-network means: show the cached data immediately, and *simultaneously* start a network request that updates the cache (and thus the UI) when it returns.** The user gets instant content from the cache, and it silently refreshes to the latest data a moment later. It's also called 'stale-while-revalidate'."},{t:"p",text:"**Why it's popular**: it gives the best of both worlds — *fast* (no waiting for the network to see something) and *fresh* (the data updates to current). Compare the alternatives: network-first makes the user wait for the network before seeing anything (slow, and blocked when offline); cache-only never updates (stale); network-only is always slow and has no offline support. Cache-then-network avoids all those downsides. And it falls out *naturally* from the SSOT pattern: the UI observes the database (so it shows the cache instantly), and you trigger a refresh that writes to the database (so the UI updates when the network returns). The main trade-off is a brief moment of potentially-stale data before the refresh completes — acceptable for most content like feeds, though not for data that must always be current (a bank balance), where you'd use network-first instead."}]},{level:"junior",q:"How should you cache images in an Android app?",a:[{t:"p",text:"**Use an established image-loading library — Coil (Kotlin-first, coroutine-based) or Glide — and let it handle caching; never hand-roll image caching.** These libraries automatically manage a two-tier cache: a *memory cache* (an in-memory LRU cache of decoded bitmaps for the fastest repeat access) and a *disk cache* (persisted encoded images so they survive app restarts and don't need re-downloading), falling back to the network only when neither has the image."},{t:"list",items:["They also handle the hard parts you'd otherwise get wrong: downsampling images to the target view size (avoiding huge bitmaps and out-of-memory crashes), lifecycle awareness (cancelling loads when the view/Activity is gone), placeholder/error images, and request deduplication.","In Compose, Coil's `AsyncImage` / `rememberAsyncImagePainter` integrates directly; in Views, `imageView.load(url)` (Coil) or `Glide.with(...).load(url)`.","The reason not to hand-roll: correct image caching involves memory management, bitmap decoding/downsampling, disk eviction policies, and lifecycle handling — all easy to get subtly wrong (OOM crashes, leaks, jank). The libraries have solved this thoroughly."]},{t:"p",text:"This fits the general caching hierarchy — memory (fastest, volatile) → disk (persistent) → network (source) — applied specifically to images. It's complementary to your app-data caching (Room), which handles structured data; images are binary blobs best left to a dedicated loader."}]},{level:"senior",q:"How do you handle cache invalidation — deciding when cached data is too stale?",a:[{t:"p",text:"**Cache invalidation is deciding when cached data can no longer be trusted and must be refreshed — famously one of the hard problems in computing, because the goal is a balance: refresh too often and you waste data/battery and load; refresh too rarely and users see stale data.** There are several strategies, usually combined:"},{t:"list",items:["**Time-based (TTL)**: store a timestamp alongside cached data and treat it as stale after a defined interval ('refresh if older than 5 minutes'). Simple, predictable, and covers most cases. You check the age on access or on screen open and refresh if expired.","**Event-based**: invalidate on specific triggers — an explicit pull-to-refresh, a push notification signaling that server data changed, or a user action that you know altered the data. This keeps caches fresh precisely when it matters without polling.","**Version/ETag-based (conditional fetch)**: the server tags data with a version (ETag or Last-Modified). On refresh you send the tag; the server replies '304 Not Modified' (cheap, no body) if nothing changed, or sends new data. This makes refreshing *efficient* — you 'refresh' frequently but only pay for actual changes. HTTP caching in OkHttp supports this at the network layer.","**App-lifecycle triggers**: refresh on app foreground, on screen entry, or on a periodic background sync (WorkManager) — often combined with TTL so you don't refresh redundantly."]},{t:"list",items:["**Choose per data type**: a rarely-changing config can have a long TTL (hours); a live feed a short one (minutes) plus pull-to-refresh; a chat needs push-based invalidation (real-time). Match the strategy to how fast the data actually changes and how bad staleness is.","**Combine strategies**: a robust approach is TTL (baseline freshness) + explicit refresh (user control) + ETags (efficient checks) + push invalidation for critical real-time data.","**Don't over-engineer**: for many apps, 'refresh on screen open if the cache is older than N minutes, plus pull-to-refresh' is entirely sufficient. Reserve ETags/push invalidation for cases where efficiency or real-time freshness genuinely matter."]},{t:"p",text:"**The senior framing**: there's no universal answer — invalidation is a per-data-type trade-off between freshness and cost. The skill is picking the *cheapest* strategy that meets the data's freshness requirement: TTL for most, ETags when you refresh often and want efficiency, push/event-based for real-time. And it's why the SSOT pattern helps — invalidation just means 'trigger a refresh that updates the DB', and the observing UI follows automatically; you're never manually reconciling cache and UI."}]},{level:"senior",q:"How do you handle writes that happen while the user is offline?",a:[{t:"p",text:"**The pattern is local-first writes plus background sync: apply the change to the local database *immediately* (so the UI updates and the user isn't blocked), mark it as pending synchronization, and have a durable background worker push it to the server when connectivity returns.** Reading offline is easy (serve from cache); *writing* offline is the genuinely hard part, and this pattern makes it reliable."},{t:"list",items:["**1. Write locally, optimistically**: when the user creates or edits data offline, immediately write it to the local DB (the single source of truth) and flag it — e.g. a `syncStatus = PENDING` column, or an 'outbox' table of queued operations. The UI reflects the change instantly (optimistic update), so the app feels responsive even offline.","**2. Sync in the background with WorkManager**: a worker with a *network constraint* reads the pending changes and sends them to the server, then marks them `SYNCED`. WorkManager is the right tool because it *persists the work across process death and reboots* and *automatically runs when the network is available* — so the sync is guaranteed to eventually happen without the user doing anything. A plain coroutine wouldn't survive the app being killed.","**3. Handle conflicts**: the hard part. While offline, the same record may have changed on the server. When syncing, you need a conflict-resolution policy: *last-write-wins* (compare timestamps, newest wins — simplest), *server-wins*, *client-wins*, or a *field-level merge* (combine non-conflicting changes). This is a domain/repository decision, and there's no universal right answer — it depends on the data's semantics (a collaborative doc needs merging; a single-user note can use last-write-wins).","**4. Use the outbox pattern for durability**: queue outgoing operations as durable records in the DB (not just in-memory), so they survive app kills and are retried with backoff until they succeed. This guarantees *eventual delivery* — no offline change is silently lost."]},{t:"list",items:["**Optimistic UI + rollback**: since you apply the change locally before the server confirms, you need to handle the case where the server *rejects* it (validation failure, conflict lost) — roll back the local change or surface an error, keeping the user informed.","**Idempotency**: give each operation a client-generated id so that retries (WorkManager may retry) don't create duplicates on the server — the server dedupes by the id."]},{t:"p",text:"**The senior framing**: offline writes turn a simple 'send to server' into a distributed-systems problem — you're reconciling two copies of data that can diverge. The robust solution is: write local-first (responsive + durable), queue the operation in an outbox, sync via WorkManager (survives kills, runs on reconnect), resolve conflicts with a policy appropriate to the data, and use idempotency keys so retries are safe. Naming the conflict-resolution challenge and WorkManager's role in guaranteed delivery is what demonstrates you understand offline writes are fundamentally harder than offline reads."}]}],LT={id:"caching-offline",title:"Caching & Offline-First",tagline:"Offline-first architecture on SSOT, caching strategies (cache-then-network), invalidation (TTL/ETag/events), offline writes & sync, and cache layering.",tags:["Caching","Offline-First","SSOT","Sync","WorkManager"],content:PT,qa:RT},DT=[{heading:"The problem Paging solves",blocks:[{t:"p",text:"Some lists are effectively unbounded — a social feed, search results, a message history with thousands of entries. Loading *all* of it at once is impossible: it would exhaust memory, take forever, and waste bandwidth on data the user never scrolls to. The solution is **pagination** — loading the data in small chunks ('pages') as the user scrolls. **Paging 3** is Jetpack's library that handles this: it loads pages on demand, manages the in-memory list, triggers the next load as the user approaches the end, and integrates loading/error states."},{t:"list",items:["Doing pagination by hand is surprisingly hard: tracking the current page, detecting when to load more (scroll math), deduplicating requests, handling errors and retries, showing loading indicators at the right place, and keeping memory bounded. Paging 3 encapsulates all of it.","It's the recommended solution for large/unbounded lists, integrating with Room (as the source of truth), Compose/RecyclerView (UI), and coroutines/Flow."]}]},{heading:"The core pieces: PagingSource, Pager, PagingData",blocks:[{t:"code",title:"The three building blocks",code:`// 1) PagingSource — defines HOW to load a page (given a key, return items + next key)
class ArticlePagingSource(private val api: Api) : PagingSource<Int, Article>() {
    override suspend fun load(params: LoadParams<Int>): LoadResult<Int, Article> {
        return try {
            val page = params.key ?: 1
            val response = api.getArticles(page, params.loadSize)
            LoadResult.Page(
                data = response.items,
                prevKey = if (page == 1) null else page - 1,
                nextKey = if (response.items.isEmpty()) null else page + 1,
            )
        } catch (e: Exception) {
            LoadResult.Error(e)
        }
    }
    override fun getRefreshKey(state: PagingState<Int, Article>): Int? = null
}

// 2) Pager — configures paging and produces a stream of PagingData
val pager = Pager(PagingConfig(pageSize = 20)) { ArticlePagingSource(api) }

// 3) PagingData — a stream of paged data the ViewModel exposes
val articles: Flow<PagingData<Article>> = pager.flow.cachedIn(viewModelScope)`},{t:"list",items:["**`PagingSource`** — you implement `load()`: given a *key* (usually a page number or a cursor), return a `LoadResult.Page` with the items plus the *previous* and *next* keys (or `LoadResult.Error` on failure). This is where you define *how* to fetch a page. The keys chain the pages together.","**`Pager`** — configured with `PagingConfig` (page size, prefetch distance, etc.), it turns your `PagingSource` into a `Flow<PagingData<T>>`.","**`PagingData`** — a container for one snapshot of paged data. It's a *stream of self-updating list snapshots*, not a normal list — which is why it needs its own consumption API and doesn't work with regular Flow operators like `stateIn`.","**`cachedIn(scope)`** — *mandatory* in the ViewModel: it caches the loaded pages in the given scope so they survive configuration changes. Without it, rotating the device re-fetches everything from page 1."]}]},{heading:"Consuming PagingData in the UI",blocks:[{t:"code",title:"In Compose",code:`val items = viewModel.articles.collectAsLazyPagingItems()

LazyColumn {
    items(
        count = items.itemCount,
        key = items.itemKey { it.id },
        contentType = items.itemContentType { "article" },
    ) { index ->
        val article = items[index]          // accessing near the edge triggers next load
        if (article != null) ArticleCard(article) else PlaceholderCard()
    }

    // Load states as list items:
    when (items.loadState.append) {
        is LoadState.Loading -> item { LoadingRow() }
        is LoadState.Error -> item { RetryRow { items.retry() } }
        else -> Unit
    }
}`},{t:"list",items:["**`collectAsLazyPagingItems()`** (Compose) / `PagingDataAdapter` (RecyclerView) — the UI-side consumer. Accessing an item near the loaded edge is what *triggers* the next page load — no manual scroll listeners.","**`loadState`** — exposes the loading/error/success state for `refresh` (initial load), `append` (loading more at the end), and `prepend` (loading at the start). You render these as list items (a spinner row, a retry row) or full-screen states — giving proper first-load spinners, end-of-list loaders, and retry.","**Placeholders** — if enabled, Paging can show placeholder items (null entries) before their data loads, so the scrollbar reflects the full count and there's no visual jump."]}]},{heading:"RemoteMediator — network + database paging (offline-first)",blocks:[{t:"p",text:"The most powerful Paging 3 setup combines network *and* database: the **`RemoteMediator`** loads pages from the network *into Room*, and the `PagingSource` reads pages *from Room*. Room is the single source of truth; the network fills it as the user scrolls. This gives offline-first pagination — cached pages are available offline, and scrolling fetches new pages when online."},{t:"list",items:["**How it fits together**: the `Pager` uses Room's `PagingSource` (Room generates one for a paged query — `@Query ... : PagingSource<Int, Entity>`) as the source, and a `RemoteMediator` as the thing that *fills* Room when the source runs out. As you scroll past the cached data, the mediator fetches the next network page and writes it to Room, which the `PagingSource` then serves.","**Offline benefit**: because the UI reads from Room (the SSOT), previously-loaded pages are shown instantly and work offline; the network only fetches *new* pages. This is the pagination version of the offline-first / SSOT pattern.","**`RemoteMediator.load()`** handles `REFRESH`/`APPEND`/`PREPEND` load types, writing fetched pages to Room in a transaction (often clearing on refresh), and tracks remote keys (the next-page tokens) in a separate table so it knows what to fetch next."]}]},{heading:"Transformations and gotchas",blocks:[{t:"list",items:["**Transforming PagingData**: use the dedicated operators (`map`, `filter`, `insertSeparators` on `PagingData`) *before* `cachedIn`, not regular Flow operators — `PagingData` isn't a normal Flow of values. `insertSeparators` adds headers/separators (e.g. date dividers) between items.","**`cachedIn` is mandatory and interviewers check it**: forget it and every configuration change re-fetches from scratch (rotation reloads page 1, losing scroll position and wasting requests). Put it at the end of the pipeline in the ViewModel.","**Keys must be stable**: pass `itemKey`/`itemContentType` in the UI (as with any lazy list) so inserts/updates don't cause cascade recomposition and item state migrates correctly.","**When NOT to use Paging**: bounded lists that fit comfortably in memory (a user's 20 settings, a fixed menu) — Paging's ceremony (PagingSource, load states, cachedIn) is overkill. Use a plain `items(list)`. Paging earns its complexity only for genuinely large or unbounded, usually network-backed, data."]},{t:"note",text:"Paging 3 essentials: PagingSource (how to load a page — key → items + next/prev keys), Pager (config → Flow<PagingData>), PagingData (self-updating paged snapshots, needs cachedIn in the ViewModel — mandatory, or rotation re-fetches). Consume with collectAsLazyPagingItems / PagingDataAdapter; accessing near the edge triggers loads; loadState gives loading/error/retry as list items. RemoteMediator + Room = offline-first paging (network fills Room, UI reads Room). Only use Paging for genuinely large/unbounded lists."}]}],VT=[{level:"junior",q:"What problem does Paging 3 solve?",a:[{t:"p",text:"**Paging 3 handles loading large or unbounded lists in small chunks ('pages') as the user scrolls, instead of loading everything at once.** For a feed, search results, or a long message history, loading all the data would exhaust memory, be slow, and waste bandwidth on data the user never reaches. Pagination loads a page at a time, on demand."},{t:"p",text:"The reason to use the library rather than rolling your own is that manual pagination is deceptively hard: you'd have to track the current page, detect when the user is near the end (scroll math), avoid firing duplicate load requests, handle errors and retries, show loading indicators in the right place, and keep memory bounded by discarding far-away pages. Paging 3 encapsulates all of that. It integrates with Room (as the local source of truth), with Compose and RecyclerView on the UI side, and with coroutines/Flow, exposing paged data as a `Flow<PagingData<T>>`. It's the recommended solution whenever a list is genuinely large or unbounded."}]},{level:"junior",q:"What are the main components of Paging 3?",a:[{t:"list",items:["**`PagingSource`** — defines *how* to load a page. You implement `load()`: given a key (usually a page number or cursor), fetch that page and return the items plus the previous and next keys (which chain the pages together), or an error. This is where your actual data-fetching logic lives.","**`Pager`** — configured with a `PagingConfig` (page size, prefetch distance), it turns your `PagingSource` into a `Flow<PagingData<T>>`.","**`PagingData`** — a container for a snapshot of paged data; it's a self-updating stream of paged list snapshots that the ViewModel exposes and the UI consumes.","**UI consumer** — `collectAsLazyPagingItems()` in Compose or a `PagingDataAdapter` in RecyclerView, which displays the items and triggers loads as you scroll."]},{t:"p",text:"The flow is: `PagingSource` (how to load) → `Pager` (configuration) → `Flow<PagingData>` (exposed by the ViewModel, cached with `cachedIn`) → UI consumer (displays and triggers loading). Optionally a `RemoteMediator` sits alongside to fill a local Room database from the network for offline-first paging. Accessing an item near the end of the loaded data is what automatically triggers loading the next page — you never write scroll-position math yourself."}]},{level:"junior",q:"How does the UI load more data as the user scrolls in Paging 3?",a:[{t:"p",text:"**Automatically — accessing an item near the edge of the currently-loaded data triggers the next page load; you don't write any scroll-detection logic.** When you display paged items (via `collectAsLazyPagingItems` in Compose or `PagingDataAdapter` in RecyclerView) and the user scrolls so that they're accessing items close to the end of what's loaded, Paging detects this (based on the `prefetchDistance` in `PagingConfig`) and calls your `PagingSource.load()` (or `RemoteMediator`) to fetch the next page."},{t:"code",title:"Accessing items drives loading",code:`val items = viewModel.feed.collectAsLazyPagingItems()
LazyColumn {
    items(items.itemCount, key = items.itemKey { it.id }) { index ->
        val item = items[index]   // near the edge -> triggers next-page load
        if (item != null) Row(item) else Placeholder()
    }
}`},{t:"p",text:"This is the big convenience over hand-rolled pagination, where you'd add a scroll listener, compute whether the last visible item is within N of the end, guard against firing while a load is in progress, and manually append results. Paging does all of that internally. You also get `loadState` to render the loading spinner (for the append) and an error/retry row as list items, so the 'loading more…' UI at the bottom is handled cleanly too."}]},{level:"junior",q:"What does cachedIn do and why is it important?",a:[{t:"p",text:"**`cachedIn(scope)` caches the loaded pages in the given coroutine scope (usually `viewModelScope`) so they survive configuration changes.** You apply it at the end of the paging pipeline in the ViewModel: `pager.flow.cachedIn(viewModelScope)`."},{t:"p",text:"**Why it's important — and why interviewers check for it**: without `cachedIn`, the `PagingData` flow is cold and tied to the collector, so when a configuration change happens (rotation), the whole thing restarts and **re-fetches from page 1**, losing all the pages the user had scrolled through and their scroll position. With `cachedIn(viewModelScope)`, the loaded pages are kept alive in the ViewModel's scope (which survives configuration changes), so after rotation the UI re-attaches to the *same* cached data — no re-fetch, scroll position preserved. It also makes the paging flow shareable/multicast-safe. Forgetting `cachedIn` is one of the most common Paging 3 bugs: everything *works*, but every rotation silently reloads from the start, wasting requests and jarring the user. So the rule is: always end your ViewModel's paging pipeline with `cachedIn(viewModelScope)` (after any `PagingData` transformations)."}]},{level:"senior",q:"How does RemoteMediator enable offline-first pagination with Room?",a:[{t:"p",text:"**`RemoteMediator` implements the offline-first pattern for paging: the `PagingSource` reads pages from *Room* (the single source of truth), and the `RemoteMediator` *fills Room from the network* when the local data runs out. So the UI always reads from the local database — giving instant, offline-capable display of cached pages — while the network is only used to fetch *new* pages as the user scrolls past what's cached.**"},{t:"list",items:["**The architecture**: the `Pager` is built with Room's `PagingSource` (Room auto-generates one for a paged query — a DAO method returning `PagingSource<Int, Entity>`) as the *source of items*, and a `RemoteMediator` as the *boundary callback* that runs when the source is about to run out of loaded items. The mediator fetches the next network page, writes it into Room (in a transaction), and Room's `PagingSource` then serves those newly-inserted rows to the UI.","**`RemoteMediator.load()` handles three load types**: `REFRESH` (initial or pull-to-refresh — often clears Room and loads the first network page), `APPEND` (user scrolled to the end — fetch and insert the next page), and `PREPEND` (load earlier data, if applicable). It returns whether it reached the end of pagination.","**Remote keys tracking**: since the network uses page tokens/cursors that don't map cleanly to Room rows, you typically keep a separate `remote_keys` table storing the next/prev key per item or per page, so on `APPEND` the mediator knows what to fetch next. Managing these keys correctly is the fiddly part.","**The offline benefit**: because the UI reads from Room, previously-loaded pages display *instantly and offline* — no network needed to re-show them. The network only fetches genuinely new pages. When offline, the user sees all cached pages; scrolling past them shows an error/end state until connectivity returns. This is the pagination-specific realization of the SSOT/offline-first architecture."]},{t:"p",text:"**The senior framing**: `RemoteMediator` + Room is Paging's answer to 'how do I paginate a network list *and* have it work offline and survive process death?' The key insight is that Room remains the single source of truth — the UI never reads the network directly; the mediator's only job is to *populate* Room from the network at the right times. This gives you all the SSOT benefits (instant cached reads, offline support, consistency) applied to an unbounded, incrementally-loaded list. It's more setup than a pure-network `PagingSource` (you write the mediator, the remote-keys handling, and the Room queries), so you use it specifically when offline paging and caching matter — for a pure in-memory network list, a plain `PagingSource` without Room is simpler."}]},{level:"senior",q:"When should you NOT use Paging 3, and how does PagingData differ from a normal list/Flow?",a:[{t:"p",text:"**Don't use Paging 3 for bounded lists that fit comfortably in memory** — a user's settings, a fixed menu, a small fixed result set (a few dozen items). Paging's machinery — implementing a `PagingSource` (or `RemoteMediator`), handling load states, `cachedIn`, the special `PagingData` consumption API — is real complexity that only pays off for *genuinely large or unbounded* data, usually network-backed. For a small list, a plain `items(list)` over a `StateFlow<List<T>>` is far simpler and correct. Reaching for Paging on a 20-item list is over-engineering."},{t:"list",items:["**`PagingData` is NOT a normal list or a normal Flow of values** — it's a *stream of self-updating paged snapshots* with its own internal diffing and state. This has concrete consequences that trip people up:","**You can't use regular Flow operators on its contents**: `stateIn`, `combine`, a plain `map` over the *list* don't apply. Instead, `PagingData` has its *own* operators — `PagingData.map`, `PagingData.filter`, `PagingData.insertSeparators` — which transform individual items *within* the paged stream, and you apply them *before* `cachedIn`. Trying to treat `PagingData` like a `Flow<List<T>>` and operate on it with normal collection/Flow functions doesn't work.","**It doesn't hold a materialized list you can index freely**: you access items through the UI consumer (`collectAsLazyPagingItems`, which returns a `LazyPagingItems` you index by position, possibly getting null for not-yet-loaded placeholders). You don't get a `List<T>` you can call `.size` or `.filter` on directly.","**It manages its own loading/error state** (`loadState` for refresh/append/prepend) rather than you tracking a separate `isLoading` boolean — because loading is per-page and ongoing, not a single state."]},{t:"p",text:"**The senior framing**: the decision is about data size and boundedness — Paging earns its complexity only for large/unbounded lists where incremental loading and bounded memory genuinely matter; for small lists it's needless ceremony. And understanding that `PagingData` is a *specialized reactive paging primitive*, not a list or ordinary Flow (hence its own `map`/`filter`/`insertSeparators`, `cachedIn`, and `loadState`), is what prevents the common mistakes of trying to `stateIn` it or operate on it with standard Flow/collection operators."}]}],jT={id:"paging",title:"Paging 3",tagline:"Paginating large lists — PagingSource/Pager/PagingData, cachedIn, consuming in the UI, RemoteMediator for offline paging, and when not to use Paging.",tags:["Paging 3","PagingSource","RemoteMediator","cachedIn","Room"],content:DT,qa:VT},OT={id:"data-storage",name:"Data & Storage",description:"Local persistence — Room & SQLite, relations & migrations, DataStore vs SharedPreferences, caching & offline-first architecture, and Paging 3.",topics:[ST,MT,ET,LT,jT]},UT=[{heading:"HTTP — the request/response protocol",blocks:[{t:"p",text:"**HTTP (HyperText Transfer Protocol)** is the protocol apps use to talk to servers. It's a **request/response** model: the client (your app) sends a *request* to a URL, the server processes it and sends back a *response*. It's *stateless* — each request is independent, carrying everything the server needs (the server doesn't remember previous requests unless you send state like a token or cookie each time). Understanding the anatomy of requests and responses is the foundation of all networking."},{t:"code",title:"Anatomy of a request and response",code:`// REQUEST
GET /api/users/42 HTTP/1.1          // method + path + version
Host: api.example.com               // headers (metadata)
Authorization: Bearer eyJ...
Accept: application/json
// (body — for POST/PUT: the data being sent)

// RESPONSE
HTTP/1.1 200 OK                     // status line: version + status code
Content-Type: application/json      // headers
{ "id": "42", "name": "Revanth" }   // body — the returned data`},{t:"list",items:["**A request has**: a *method* (GET, POST…), a *URL/path*, *headers* (metadata: auth token, content type, accept), and optionally a *body* (data sent to the server, for POST/PUT).","**A response has**: a *status code* (200, 404…), *headers* (content type, caching info), and a *body* (the returned data, usually JSON).","**Stateless**: the server treats each request independently. To maintain a 'session', the client includes identifying state (an auth token in the `Authorization` header) on every request."]}]},{heading:"HTTP methods (verbs)",blocks:[{t:"table",headers:["Method","Purpose","Has body?","Idempotent?"],rows:[["GET","read/retrieve data","no","yes (safe — no side effects)"],["POST","create a new resource / submit data","yes","no (repeating creates duplicates)"],["PUT","replace/update a resource fully","yes","yes (same result if repeated)"],["PATCH","partially update a resource","yes","not necessarily"],["DELETE","remove a resource","usually no","yes (deleting twice = still deleted)"]]},{t:"list",items:["**GET** — retrieve data; should have *no side effects* (safe) and be cacheable. Never use GET to change server state.","**POST** — create a resource or submit data (a form, a new item). *Not idempotent*: sending it twice creates two resources — which is why retrying a POST needs care (idempotency keys).","**PUT vs PATCH** — PUT replaces the *entire* resource (send all fields); PATCH updates *part* of it (send only changed fields). PUT is idempotent (same result each time); PATCH may not be.","**Idempotent** means repeating the request produces the same result — important for *retries*: safe to retry GET/PUT/DELETE, risky to blindly retry POST."]}]},{heading:"Status codes",blocks:[{t:"table",headers:["Range","Meaning","Examples"],rows:[["2xx","success","200 OK, 201 Created, 204 No Content"],["3xx","redirection","301 Moved Permanently, 304 Not Modified"],["4xx","client error (your request is wrong)","400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 429 Too Many Requests"],["5xx","server error (the server failed)","500 Internal Server Error, 502 Bad Gateway, 503 Service Unavailable"]]},{t:"list",items:["**The first digit is the category** — 2xx success, 3xx redirect, 4xx client's fault, 5xx server's fault. Knowing the category tells you how to react.","**Key ones to handle**: 401 (unauthenticated — refresh the token or log in), 403 (forbidden — no permission), 404 (not found), 429 (rate-limited — back off and retry), 500/503 (server error — retry with backoff).","**4xx vs 5xx for retries**: 5xx (server errors) are often *transient* — retrying may succeed. 4xx (client errors) mean *your request is wrong* — retrying the same request will fail identically, so don't retry (except 401 after refreshing auth, or 429 after waiting).","**304 Not Modified** — the caching response: 'the data hasn't changed since your cached version', so no body is sent (efficient conditional requests via ETag)."]}]},{heading:"REST — the API design style",blocks:[{t:"p",text:"**REST (Representational State Transfer)** is an architectural style for designing web APIs around *resources* (nouns) manipulated with the standard HTTP methods (verbs). A resource is identified by a URL, and you act on it with GET/POST/PUT/DELETE. Most Android apps consume REST APIs, so recognizing RESTful design is important."},{t:"code",title:"RESTful resource design",code:`GET    /api/users          // list all users
POST   /api/users          // create a new user (body = user data)
GET    /api/users/42       // get user 42
PUT    /api/users/42       // replace user 42
PATCH  /api/users/42       // partially update user 42
DELETE /api/users/42       // delete user 42
GET    /api/users/42/posts // user 42's posts (nested resource)`},{t:"list",items:["**Resources as nouns, methods as verbs**: the URL names *what* (a user, a collection of users), the HTTP method says *what to do* (read, create, update, delete). Well-designed REST URLs don't have verbs (`/getUser` is not RESTful; `GET /users/42` is).","**Statelessness**: each request is self-contained (carries its own auth); the server keeps no client session state between requests.","**JSON is the common data format** — requests and responses carry JSON bodies (covered in the serialization topic).","**Alternatives to know**: **GraphQL** (client specifies exactly what data it wants in one flexible query — avoids over/under-fetching), **gRPC** (binary protocol over HTTP/2, efficient for service-to-service). REST is still the most common for mobile, but knowing the alternatives shows breadth."]}]},{heading:"Headers, auth, and content negotiation",blocks:[{t:"list",items:["**`Authorization` header** — carries credentials, most commonly `Bearer <token>` for token-based auth. Sent on every request (statelessness) to identify the user.","**`Content-Type`** — declares the format of the *request body* you're sending (`application/json`). **`Accept`** — tells the server what format you want *back*. Together they're 'content negotiation'.","**Caching headers** — `Cache-Control`, `ETag`, `Last-Modified` control HTTP-level caching. An `ETag` is a version fingerprint; sending `If-None-Match: <etag>` lets the server reply `304 Not Modified` (no body) if unchanged — efficient conditional fetching.","**HTTPS is mandatory** — all traffic should be encrypted (TLS). Android blocks cleartext (plain HTTP) by default since Android 9. For extra security, **certificate pinning** restricts which certificates your app trusts (preventing man-in-the-middle attacks) — important for sensitive apps."]},{t:"note",text:"HTTP/REST essentials: request (method + URL + headers + optional body) / response (status code + headers + body); stateless (send auth each time). Methods: GET (read, safe), POST (create, not idempotent), PUT (replace, idempotent), PATCH (partial), DELETE. Status codes by category: 2xx success, 4xx client error (don't blindly retry), 5xx server error (retry with backoff). REST = resources (URLs) + verbs (methods). Use HTTPS; know GraphQL/gRPC as alternatives."}]}],FT=[{level:"junior",q:"What are the main HTTP methods and what is each for?",a:[{t:"list",items:["**GET** — retrieve data. It should be *safe* (no side effects) and is cacheable. Use it for reads only; never change server state with a GET.","**POST** — create a new resource or submit data (a form, a new item). It carries a body and is *not idempotent* — sending it twice creates two resources.","**PUT** — replace/update a resource *fully* (you send all its fields). It's idempotent — the same request produces the same result each time.","**PATCH** — partially update a resource (send only the changed fields).","**DELETE** — remove a resource. Idempotent (deleting something twice leaves it deleted)."]},{t:"p",text:"The concept that ties these together is *idempotency* — whether repeating a request has the same effect as doing it once. GET, PUT, and DELETE are idempotent, which makes them safe to retry (important when a request times out and you're unsure if it went through). POST is *not* idempotent, so blindly retrying it can create duplicates — which is why retrying a POST needs care (like an idempotency key the server uses to dedupe). Knowing which methods are safe to retry directly informs how you build resilient networking."}]},{level:"junior",q:"What do HTTP status code categories mean?",a:[{t:"p",text:"**The first digit of a status code tells you the category of outcome**, which tells you how to react:"},{t:"list",items:["**2xx — Success**: the request worked. `200 OK` (here's the data), `201 Created` (resource created), `204 No Content` (success, nothing to return).","**3xx — Redirection**: further action needed. `301 Moved Permanently`, `304 Not Modified` (your cached version is still current — no body sent, used for efficient caching).","**4xx — Client error**: *your request* is wrong. `400 Bad Request`, `401 Unauthorized` (not authenticated), `403 Forbidden` (no permission), `404 Not Found`, `429 Too Many Requests` (rate-limited).","**5xx — Server error**: the *server* failed. `500 Internal Server Error`, `502 Bad Gateway`, `503 Service Unavailable`."]},{t:"p",text:"The practical distinction for handling errors: **5xx errors are often transient** (the server hiccupped), so retrying with backoff may succeed. **4xx errors mean your request is fundamentally wrong**, so retrying the *same* request will fail identically — don't retry (with two exceptions: retry after a `401` if you can refresh the auth token, and retry after a `429` once you've waited the required time). Mapping status codes to the right reaction — retry, re-authenticate, show an error, back off — is core to robust networking."}]},{level:"junior",q:"What is REST?",a:[{t:"p",text:"**REST (Representational State Transfer) is an architectural style for web APIs, organized around *resources* (nouns) that you act on with the standard HTTP *methods* (verbs).** A resource — a user, a post, a collection of orders — is identified by a URL, and you manipulate it with GET (read), POST (create), PUT/PATCH (update), and DELETE (remove)."},{t:"code",title:"RESTful design",code:`GET    /users        // list users
POST   /users        // create a user
GET    /users/42     // read user 42
PUT    /users/42     // replace user 42
DELETE /users/42     // delete user 42`},{t:"p",text:"The key ideas: **URLs name resources (nouns), not actions** — `/getUser` is not RESTful, but `GET /users/42` is (the verb is the HTTP method, not part of the URL). It's **stateless** — each request carries everything the server needs (like an auth token), and the server keeps no session between requests. Data is typically exchanged as **JSON**. Most Android apps consume REST APIs. Worth knowing as alternatives: **GraphQL** (the client specifies exactly what fields it wants in one query, avoiding over- and under-fetching) and **gRPC** (an efficient binary protocol over HTTP/2, common for backend-to-backend). REST remains the most common for mobile, but recognizing when an API is or isn't RESTful, and knowing the alternatives, shows solid fundamentals."}]},{level:"junior",q:"How does authentication typically work in a REST API?",a:[{t:"p",text:"**Because HTTP is stateless, the client must prove its identity on *every* request — most commonly by sending a token in the `Authorization` header.** The typical flow: the user logs in (POST credentials), the server returns a token (often a JWT), and the app then includes `Authorization: Bearer <token>` on every subsequent request. The server validates the token to identify the user, without keeping any session state itself."},{t:"list",items:["**Token-based (Bearer)** — the standard for mobile. The token is a credential the server issues and the client presents on each request. Tokens usually expire for security.","**Access + refresh tokens** — a common pattern: a short-lived *access token* (used on requests) plus a long-lived *refresh token* (used to get a new access token when it expires, without re-login). This limits the damage if an access token leaks.","**Where to store tokens** — securely: `EncryptedSharedPreferences`, or encrypted storage, or the Android Keystore — never in plaintext.","**How it's applied** — in Android you typically add the token to every request automatically with an OkHttp *interceptor* (rather than manually on each call), and handle token refresh in an `Authenticator` when a request returns 401."]},{t:"p",text:"The stateless nature of HTTP is *why* auth works this way — the server can't 'remember' you're logged in between requests, so you re-assert your identity every time via the token. This is different from older cookie/session server-side approaches; mobile APIs almost always use stateless token auth."}]},{level:"senior",q:"Which HTTP requests are safe to retry, and why does idempotency matter for a mobile app?",a:[{t:"p",text:"**Idempotent requests — those where repeating them has the same effect as doing them once — are safe to retry; non-idempotent ones are not, because a retry could duplicate an action.** GET, PUT, and DELETE are idempotent (reading twice, replacing twice, or deleting twice all leave the same end state). POST is *not* idempotent — each POST typically creates a new resource, so retrying it can create duplicates."},{t:"list",items:["**Why this matters acutely on mobile**: mobile networks are flaky. A request can *time out or lose the connection after the server processed it but before the response reached the app* — so the app doesn't know whether it succeeded. For an idempotent request (GET/PUT/DELETE), you can just retry safely. For a POST (say, 'place order' or 'send payment'), a blind retry might place *two* orders or charge *twice* — a serious bug.","**The solution for non-idempotent requests — idempotency keys**: the client generates a unique key (a UUID) per logical operation and sends it (e.g. in an `Idempotency-Key` header). The server records processed keys; if it sees the same key again (from a retry), it returns the *original* result instead of performing the action again. This makes even POST safe to retry. Payment APIs (Stripe, etc.) universally use this.","**Which errors to retry**: retry on network failures and 5xx (transient server errors), typically with *exponential backoff* (increasing delays) to avoid hammering a struggling server. Do *not* retry 4xx client errors (the request is wrong — it'll fail identically), except 401 after refreshing the token and 429 after honoring the rate-limit delay.","**Bound retries**: cap the number of attempts and add jitter (randomized backoff) so many clients don't retry in lockstep (the 'thundering herd' problem)."]},{t:"p",text:"**The senior framing**: retry logic isn't just 'try again on failure' — it's a correctness concern rooted in idempotency. On unreliable mobile networks you *will* face ambiguous failures (did it go through?), so you design for them: retry idempotent operations freely, make critical non-idempotent operations idempotent via keys, retry only transient (5xx/network) errors with bounded exponential backoff and jitter, and never blindly retry a POST that could double a side effect. This is what separates a robust networking layer from one that occasionally double-charges users."}]},{level:"senior",q:"What is an ETag and how does conditional caching with 304 work?",a:[{t:"p",text:"**An ETag (Entity Tag) is a version fingerprint the server assigns to a resource — a short identifier (like a hash) that changes whenever the resource's content changes. It enables *conditional requests*: the client can ask 'give me this resource, but only if it changed since the version I have', letting the server skip sending the body when nothing changed.**"},{t:"list",items:['**How it flows**: the first response includes an `ETag: "abc123"` header. The client caches the response *and* the ETag. On the next request for that resource, the client sends `If-None-Match: "abc123"`. The server compares: if the resource still has ETag `abc123` (unchanged), it responds `304 Not Modified` with *no body* — telling the client \'your cached copy is current\'. If it changed, the server responds `200 OK` with the new body and a new ETag.',"**Why it's efficient**: a `304` response is tiny (just headers, no body), so you 'refresh' the data — confirming it's current — without re-downloading it. For large responses or frequent polling, this saves enormous bandwidth and time. The client keeps showing its cached data on a 304, and only replaces it on a 200.","**`Last-Modified` / `If-Modified-Since`** is a timestamp-based alternative to ETags (same idea, using a modification date instead of a fingerprint). ETags are more precise (they detect any content change, even if the timestamp is the same).","**In Android**: OkHttp handles this automatically at the HTTP layer if you configure a `Cache` — it stores responses with their ETags and transparently sends conditional requests, serving the cached body on a 304. You get efficient caching for free without app-level code."]},{t:"p",text:"**The senior framing**: ETags implement 'stale-while-revalidate' efficiently at the protocol level — you can check freshness *cheaply* (a conditional request that usually returns a bodyless 304) rather than re-fetching. This is complementary to app-level caching (Room as SSOT): ETags optimize the *network* layer's freshness checks, while Room gives you queryable, offline, observable structured data. For frequently-polled or large resources that rarely change, ETag-based conditional caching is the right tool to minimize data usage — and it's why understanding HTTP caching headers matters even when you also cache in a database."}]}],NT={id:"http-rest",title:"HTTP & REST Fundamentals",tagline:"Request/response anatomy, HTTP methods & idempotency, status code categories, REST design, auth, and caching headers (ETag/304).",tags:["HTTP","REST","Status Codes","Idempotency","ETag"],content:UT,qa:FT},qT=[{heading:"The two-layer stack: Retrofit on top of OkHttp",blocks:[{t:"p",text:"Android's standard networking stack has two layers. **OkHttp** is the *HTTP client* — it does the actual network work: opening connections, sending requests, receiving responses, connection pooling, caching, retries, timeouts. **Retrofit** is a *type-safe wrapper on top of OkHttp* — you define your API as a Kotlin interface with annotations, and Retrofit generates the code that turns method calls into HTTP requests (using OkHttp underneath) and parses responses into your objects. You describe *what* the API looks like; Retrofit handles *how* to call it."},{t:"list",items:["**OkHttp** = the engine (raw HTTP: connections, interceptors, caching, pooling). You can use it directly, but it's low-level (manual request building, response parsing).","**Retrofit** = the ergonomic layer (declare an interface, get type-safe methods, automatic JSON parsing). It uses OkHttp as its HTTP client.","Together they're the de-facto standard for REST networking on Android. (Ktor is the KMP alternative — covered separately.)"]}]},{heading:"Defining an API with Retrofit",blocks:[{t:"code",title:"A Retrofit API interface",code:`interface UserApi {
    @GET("users")
    suspend fun getUsers(@Query("page") page: Int): List<UserDto>

    @GET("users/{id}")
    suspend fun getUser(@Path("id") id: String): UserDto

    @POST("users")
    suspend fun createUser(@Body user: CreateUserRequest): UserDto

    @PUT("users/{id}")
    suspend fun updateUser(@Path("id") id: String, @Body user: UserDto): UserDto

    @DELETE("users/{id}")
    suspend fun deleteUser(@Path("id") id: String)

    @GET("search")
    suspend fun search(@Query("q") query: String, @Header("X-Trace") trace: String): SearchDto
}`},{t:"list",items:["**HTTP method annotations**: `@GET`, `@POST`, `@PUT`, `@DELETE`, `@PATCH` — the path (relative to the base URL) goes in the annotation.","**`@Path`** — substitute a value into the URL path (`users/{id}`). **`@Query`** — add a query parameter (`?page=2`). **`@Body`** — send an object as the request body (serialized to JSON). **`@Header`/`@Headers`** — add headers. **`@Field`/`@FormUrlEncoded`** — form submissions.","**`suspend` functions** — Retrofit natively supports coroutines: a `suspend` API function is *main-safe* (Retrofit runs the network call off the main thread) and returns the parsed result directly. This is the modern way (older Retrofit used `Call<T>` with callbacks, or RxJava).","**Return types**: `suspend fun ...(): T` returns the parsed body directly (throwing on error); `suspend fun ...(): Response<T>` gives you the full response (status code, headers) for manual handling."]}]},{heading:"Building Retrofit and the converter",blocks:[{t:"code",title:"Wiring it up",code:`val okHttpClient = OkHttpClient.Builder()
    .connectTimeout(15, TimeUnit.SECONDS)
    .readTimeout(15, TimeUnit.SECONDS)
    .addInterceptor(loggingInterceptor)      // interceptors here
    .build()

val retrofit = Retrofit.Builder()
    .baseUrl("https://api.example.com/")     // must end with /
    .client(okHttpClient)
    .addConverterFactory(                      // how to parse bodies
        Json.asConverterFactory("application/json".toMediaType())  // kotlinx.serialization
    )
    .build()

val userApi: UserApi = retrofit.create(UserApi::class.java)  // Retrofit generates the impl`},{t:"list",items:["**`baseUrl`** — the API root (endpoint paths are relative to it). **`client`** — the configured OkHttpClient. **A converter factory** — tells Retrofit how to serialize request bodies and parse responses (kotlinx.serialization, Moshi, or Gson).","**`retrofit.create(...)`** generates the interface implementation via a dynamic proxy — you call methods, Retrofit builds and executes the OkHttp request and parses the response.","**Provide these as singletons via DI** (Hilt) — building Retrofit/OkHttp is expensive, and OkHttp's connection pool and cache should be shared across the app. Never create a new client per request."]}]},{heading:"OkHttp interceptors — the powerful middleware",blocks:[{t:"p",text:"**Interceptors** are OkHttp's middleware — they sit in the request/response pipeline and can observe, modify, or short-circuit requests and responses. Every request passes through the chain of interceptors on the way out, and every response on the way back. This is where you add cross-cutting networking concerns: auth, logging, headers, retries."},{t:"code",title:"An auth interceptor adds the token to every request",code:`class AuthInterceptor(private val tokenProvider: TokenProvider) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response {
        val request = chain.request().newBuilder()
            .addHeader("Authorization", "Bearer \${tokenProvider.token()}")
            .build()
        return chain.proceed(request)          // continue the chain
    }
}`},{t:"list",items:["**Common uses**: adding auth tokens/headers to every request (so you don't repeat it per call), logging (`HttpLoggingInterceptor`), adding analytics/trace headers, custom retry logic, response caching tweaks, and mocking responses in tests.","**Two types**: *application interceptors* (added with `addInterceptor` — called once per request, see the request as your app made it) and *network interceptors* (`addNetworkInterceptor` — closer to the wire, see redirects/retries, can observe the actual network request). Application interceptors are the common choice.","**`HttpLoggingInterceptor`** — logs requests/responses for debugging (set level to BODY in debug, NONE in release — never log bodies with sensitive data in production).","**Order matters**: interceptors run in the order added; the auth interceptor should run before logging so the logged request includes the header (or after, if you want to hide it)."]}]},{heading:"Timeouts, connection pooling, and caching",blocks:[{t:"list",items:["**Timeouts** — configure `connectTimeout` (time to establish a connection), `readTimeout` (time waiting for data), `writeTimeout` (time to send the body). Sensible defaults (10-30s) prevent requests from hanging forever on a bad network. Without timeouts, a stalled request blocks resources indefinitely.","**Connection pooling** — OkHttp *reuses* TCP connections across requests (keep-alive) instead of opening a new one each time. This is a major performance win (connection setup, especially TLS handshake, is expensive) and is why you share one OkHttpClient — each client has its own pool.","**HTTP caching** — configure a `Cache` on the client, and OkHttp automatically caches responses per HTTP headers (`Cache-Control`, ETag), serving cached responses and doing conditional requests (304). Complementary to app-level DB caching.","**HTTP/2 support** — OkHttp supports HTTP/2 (multiplexing multiple requests over one connection), automatically negotiated — more efficient than HTTP/1.1's one-request-per-connection.","**Retries** — OkHttp automatically retries some connection failures; for application-level retry (on specific status codes with backoff), you add a custom interceptor or handle it in the repository."]},{t:"note",text:"Retrofit + OkHttp essentials: OkHttp = the HTTP engine (connections, pooling, caching, interceptors, timeouts); Retrofit = type-safe interface layer on top (annotations → requests, auto JSON parsing, suspend support = main-safe). Define APIs as annotated interfaces; build Retrofit with a baseUrl, the OkHttpClient, and a converter factory — as DI singletons. Interceptors are middleware for auth/logging/headers/retry. Configure timeouts; rely on connection pooling and HTTP caching. Share one client for pooling."}]}],WT=[{level:"junior",q:"What is the relationship between Retrofit and OkHttp?",a:[{t:"p",text:"**They're two layers of the same networking stack. OkHttp is the actual HTTP client — the engine that does the network work; Retrofit is a type-safe wrapper on top of OkHttp that makes calling REST APIs ergonomic.**"},{t:"list",items:["**OkHttp** handles the low-level HTTP: opening and pooling connections, sending requests, receiving responses, TLS, caching, timeouts, retries, and interceptors. You *can* use it directly, but it's manual — you build requests and parse responses yourself.",'**Retrofit** sits on top: you declare your API as a Kotlin interface with annotations (`@GET("users/{id}")`), and Retrofit generates the code that turns a method call into an OkHttp request and parses the JSON response into your objects. It delegates the actual HTTP work to OkHttp.']},{t:"p",text:"So the division is: Retrofit gives you the *type-safe, declarative interface* (describe the API, get parsed results), and OkHttp is the *underlying HTTP machinery* it runs on. When you configure things like timeouts, interceptors (for auth/logging), and caching, you do that on the OkHttpClient and hand it to Retrofit. Together they're the standard Android REST networking stack."}]},{level:"junior",q:"How do you define an API endpoint in Retrofit?",a:[{t:"p",text:"**You declare a Kotlin interface where each method represents an endpoint, annotated with the HTTP method and path, and Retrofit generates the implementation.** The method's parameters are annotated to describe how they map into the request."},{t:"code",title:"An interface with common annotations",code:`interface UserApi {
    @GET("users/{id}")
    suspend fun getUser(@Path("id") id: String): UserDto      // path param

    @GET("users")
    suspend fun getUsers(@Query("page") page: Int): List<UserDto>  // query param

    @POST("users")
    suspend fun create(@Body body: CreateUserRequest): UserDto     // JSON body
}`},{t:"list",items:["**Method annotations** — `@GET`, `@POST`, `@PUT`, `@DELETE`, `@PATCH`, with the relative path in the annotation.","**Parameter annotations** — `@Path` (substitute into the URL), `@Query` (add a `?key=value`), `@Body` (serialize an object as the JSON request body), `@Header` (add a header).","**`suspend`** — make the function `suspend` and it's automatically main-safe (Retrofit runs the network off the main thread) and returns the parsed result directly."]},{t:"p",text:"You then get the working implementation with `retrofit.create(UserApi::class.java)` — Retrofit builds it via a dynamic proxy. Returning `T` directly gives you the parsed body (throwing on HTTP errors), or you can return `Response<T>` to access the status code and headers for manual handling. This declarative approach means you describe *what* the API looks like and Retrofit handles all the request-building and response-parsing boilerplate."}]},{level:"junior",q:"What does making a Retrofit function 'suspend' do?",a:[{t:"p",text:"**Making a Retrofit API function `suspend` gives you native coroutine support: the function is main-safe (Retrofit automatically runs the network call off the main thread) and returns the parsed result directly, so you can `await` it in sequential-looking code.** You call it from a coroutine and it suspends while the network request happens, then resumes with the result."},{t:"code",title:"Calling a suspend Retrofit function",code:`// In a repository (main-safe — no withContext needed)
suspend fun loadUser(id: String): User = api.getUser(id).toDomain()

// In a ViewModel
viewModelScope.launch {
    val user = repository.loadUser("42")   // suspends during network, resumes with result
    _state.value = UiState.Content(user)
}`},{t:"p",text:"The key points: it's **main-safe**, so you can call it from a main-thread coroutine (`viewModelScope`) without wrapping it in `withContext(Dispatchers.IO)` — Retrofit handles the threading internally (wrapping it is redundant). It returns the parsed body directly (throwing `HttpException` on non-2xx or `IOException` on network failure), which you handle with try/catch or by returning `Response<T>`. And it integrates with structured concurrency — if the calling coroutine is cancelled (the user leaves the screen), Retrofit cancels the underlying HTTP call. This is the modern approach; older Retrofit used `Call<T>` with `enqueue` callbacks (verbose, leak-prone) or RxJava. Suspend functions make networking read like simple sequential code."}]},{level:"junior",q:"What is an OkHttp interceptor and what would you use one for?",a:[{t:"p",text:"**An interceptor is OkHttp's middleware — it sits in the request/response pipeline and can observe, modify, or short-circuit requests and responses.** Every outgoing request passes through the chain of interceptors, and every incoming response comes back through them, so it's the place to add *cross-cutting* networking concerns that should apply to many or all requests."},{t:"code",title:"Adding an auth token to every request",code:`class AuthInterceptor(private val token: () -> String) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response {
        val request = chain.request().newBuilder()
            .addHeader("Authorization", "Bearer \${token()}")
            .build()
        return chain.proceed(request)
    }
}`},{t:"list",items:["**Adding auth headers** — attach the `Authorization` token to every request in one place, instead of repeating it on each API method.","**Logging** — `HttpLoggingInterceptor` logs requests/responses for debugging (BODY level in debug, NONE in release).","**Adding common headers** — API keys, trace/analytics headers, `Accept-Language`, app version.","**Retry / caching logic** — custom retry on specific failures, or tweaking cache behavior."]},{t:"p",text:"The value is *centralization* — a concern like auth applies to every request, and an interceptor lets you implement it once rather than in every call. You add them with `addInterceptor` on the OkHttpClient builder. They run in the order added, and calling `chain.proceed(request)` continues the chain (you can also short-circuit by returning a response without proceeding, which is how response mocking in tests works)."}]},{level:"senior",q:"Why should you share a single OkHttpClient/Retrofit instance across the app?",a:[{t:"p",text:"**Because OkHttp maintains shared, expensive resources — a connection pool, a thread pool, and optionally a response cache — that are meant to be reused across all requests. Creating a new client per request (or per screen) discards those benefits and wastes resources.**"},{t:"list",items:["**Connection pooling** — the big one. OkHttp *reuses* TCP/TLS connections across requests (keep-alive) instead of opening a new one each time. Establishing a connection, especially the TLS handshake, is *expensive* (multiple round trips). A shared client reuses warm connections, dramatically reducing latency for subsequent requests to the same host. A new client per request means a cold connection (full handshake) every time — much slower and more battery/data.","**Thread pool** — OkHttp has a dispatcher with a thread pool for async requests; each client creates its own. Many clients = many thread pools = wasted memory and threads.","**Response cache** — if you configure an HTTP cache, it's tied to the client; multiple clients would have separate (or conflicting) caches, defeating caching.","**Consistent configuration** — interceptors (auth, logging), timeouts, and TLS settings live on the client. One shared, correctly-configured client ensures every request behaves consistently; ad-hoc clients drift."]},{t:"list",items:["**How to share it**: provide the OkHttpClient and Retrofit as *singletons* via dependency injection (Hilt `@Singleton`). All API interfaces are created from the one Retrofit instance, which uses the one OkHttpClient.","**When you DO create variants**: sometimes you need a *specialized* client (different timeouts for a long-polling endpoint, a different interceptor set for an unauthenticated auth endpoint) — but you create it by *sharing the base client's pool* (`baseClient.newBuilder()...build()`), which reuses the underlying connection pool and thread pool while overriding specific settings. You never build a fully independent client just to change one setting."]},{t:"p",text:"**The senior framing**: OkHttp is *designed* around a shared client — the connection pool is the performance heart of the library, and it only helps if reused. Creating clients per request is a classic performance mistake that shows up as slow requests (repeated TLS handshakes), excess memory (duplicate thread pools), and broken caching. The correct pattern — one DI-provided singleton, with `newBuilder()` for the rare specialized variant that still shares the pool — is both the performance-correct and the maintainability-correct choice."}]},{level:"senior",q:"How do timeouts work in OkHttp, and why are they important?",a:[{t:"p",text:"**Timeouts bound how long different phases of a request can take before OkHttp gives up and fails the request. Without them, a request can hang indefinitely on a bad network, holding resources and leaving the user staring at a spinner forever.** OkHttp has several distinct timeouts for different phases:"},{t:"list",items:["**`connectTimeout`** — the maximum time to *establish* the connection to the server (TCP + TLS handshake). If the server is unreachable or very slow to connect, this fires.","**`readTimeout`** — the maximum time to wait for *data* once connected — specifically, the max gap between bytes arriving. If the server accepts the connection but then stalls (sends nothing), this fires. This is the most commonly hit one on flaky networks.","**`writeTimeout`** — the maximum time to *send* the request body to the server (relevant for large uploads).","**`callTimeout`** — an overall cap on the *entire* call (connect + write + read + redirects). A safety net that bounds total time regardless of the per-phase timeouts."]},{t:"list",items:["**Why they're important**: mobile networks fail in ways that *hang* rather than cleanly error — a connection that never completes, a server that stops responding mid-response. Without timeouts, the coroutine/thread waits forever, the UI shows an eternal spinner, resources (connections, threads) are tied up, and the user can't tell if it's working. Timeouts convert 'hung forever' into a *prompt, handleable failure* so you can show an error and offer retry.","**Choosing values**: too short and you fail requests that would have succeeded on a slow-but-working network (frustrating on poor connections); too long and the user waits ages before seeing an error. Typical values are 10-30 seconds, tuned per endpoint — a quick API call might use 15s, while a large file upload needs a longer write/call timeout. You can override per-request with `newBuilder()` for endpoints with different needs.","**Timeouts + retries together**: a timeout produces an `IOException`/`SocketTimeoutException`, which your retry logic can catch and retry (with backoff) — since a timeout is often transient. So timeouts and retry policy work together to make networking resilient."]},{t:"p",text:"**The senior framing**: timeouts are a *reliability* requirement, not an optional tuning knob — on unreliable mobile networks, requests hang, and unbounded waits are a real UX and resource-leak problem. The discipline is: always set sensible timeouts (never rely on 'no timeout'), pick per-phase values matched to the endpoint (short for quick APIs, longer for uploads), use `callTimeout` as an overall safety net, and pair timeouts with retry-on-transient-failure so a timed-out request gets a bounded retry rather than either hanging or failing permanently."}]}],zT={id:"retrofit-okhttp",title:"Retrofit & OkHttp",tagline:"The two-layer stack, defining APIs with annotations, suspend support, building Retrofit, interceptors as middleware, timeouts, and connection pooling.",tags:["Retrofit","OkHttp","Interceptors","Timeouts","Networking"],content:qT,qa:WT},BT=[{heading:"What serialization is and why it's needed",blocks:[{t:"p",text:"**Serialization** is converting an object into a format that can be stored or transmitted (like a JSON string), and **deserialization** (or parsing) is the reverse — turning that format back into objects. APIs communicate in JSON (text), but your app works with Kotlin objects, so you need to convert between them: your `User` object → JSON to send in a request body, and JSON response → your `User` object to use in code. A serialization library automates this mapping so you don't hand-write parsing."},{t:"code",title:"The conversion in both directions",code:`// Deserialization: JSON string -> Kotlin object
val json = """{"id":"42","name":"Revanth","is_premium":true}"""
val user: User = decode(json)     // -> User(id="42", name="Revanth", premium=true)

// Serialization: Kotlin object -> JSON string
val body: String = encode(user)   // -> {"id":"42",...}`}]},{heading:"The three main libraries",blocks:[{t:"table",headers:["Library","How it works","Notes"],rows:[["kotlinx.serialization","compile-time codegen via a compiler plugin","Kotlin-first, KMP-compatible, no reflection — the modern choice"],["Moshi","reflection or codegen (codegen recommended)","Square library, good Kotlin support, widely used"],["Gson","runtime reflection","older, Google's; works but has Kotlin pitfalls (ignores defaults/nullability) — legacy"]]},{t:"list",items:["**kotlinx.serialization** — the modern, Kotlin-first choice: it's a compiler plugin that generates serialization code *at compile time* (no runtime reflection), respects Kotlin's null safety and default values, and works in **KMP** (crucial for multiplatform). Use `@Serializable` on your classes.","**Moshi** — Square's library, a solid choice especially with its codegen (`moshi-kotlin-codegen`), which avoids reflection and handles Kotlin properly. Common in Retrofit apps.","**Gson** — Google's older library using runtime reflection. It works but has real Kotlin problems: it *ignores* default values and can put `null` into non-null Kotlin properties (breaking null safety) because it doesn't understand Kotlin's constructor semantics. Prefer kotlinx.serialization or Moshi for new code.","**For an Android/KMP engineer, kotlinx.serialization is the default recommendation** — it's Kotlin-native, KMP-ready, and avoids reflection."]}]},{heading:"kotlinx.serialization in practice",blocks:[{t:"code",title:"Annotating a data class",code:`@Serializable
data class UserDto(
    val id: String,
    val name: String,
    @SerialName("is_premium") val premium: Boolean = false,  // map JSON key -> property
    val email: String? = null,                                // nullable + default
    @Transient val localOnly: String = "",                    // excluded from JSON
)

// Configure the Json instance
val json = Json {
    ignoreUnknownKeys = true      // don't crash on extra JSON fields
    coerceInputValues = true      // use defaults for null/invalid where possible
    explicitNulls = false         // omit null fields when encoding
}`},{t:"list",items:["**`@Serializable`** — marks a class for serialization; the compiler plugin generates the serializer. **`@SerialName`** — maps a JSON key to a differently-named property (JSON `is_premium` → Kotlin `premium`), so your Kotlin code uses idiomatic names while matching the API's JSON.","**`@Transient`** — excludes a property from serialization (for computed or local-only fields). **`@Required`** — makes a property with a default mandatory in JSON.","**`ignoreUnknownKeys = true`** — *critical for robustness*: without it, an unexpected extra field in the JSON (which servers add over time) *crashes* parsing. With it, unknown fields are ignored. Almost always set this.","**Default values** — kotlinx.serialization respects Kotlin defaults: if a field is missing from the JSON, the default is used (not a crash). This makes your models resilient to optional/absent fields."]}]},{heading:"The DTO vs domain model distinction",blocks:[{t:"p",text:"A best practice: your **DTOs** (Data Transfer Objects — the classes that mirror the API's JSON exactly) should be *separate* from your **domain models** (the clean objects your app logic uses). The DTO is shaped by the *API contract* (nullable fields, snake_case names, exactly the server's structure); the domain model is shaped by *your app's needs* (non-null invariants, idiomatic names, only the fields you use). You map DTO → domain at the data-layer boundary."},{t:"code",title:"DTO mirrors JSON; domain model is clean",code:`// DTO — matches the API's JSON exactly (nullable, server names)
@Serializable
data class UserDto(
    @SerialName("user_id") val id: String?,
    @SerialName("full_name") val name: String?,
    @SerialName("is_premium") val premium: Boolean?,
)

// Domain model — clean, non-null, what your app uses
data class User(val id: String, val name: String, val premium: Boolean)

// Mapper at the boundary — resolves nullability, renames, validates
fun UserDto.toDomain() = User(
    id = id ?: error("missing id"),
    name = name ?: "Unknown",
    premium = premium ?: false,
)`},{t:"list",items:["**Why separate them**: the API's JSON changes independently of your app. Keeping DTOs separate means a server field rename or added field touches only the DTO and its mapper — not your whole codebase. The domain model stays stable and clean.","**Nullability resolved once**: servers often make everything nullable. The mapper resolves nullability at the boundary (providing defaults, validating), so the rest of your app works with clean non-null domain types instead of `?` everywhere.","**Pragmatic caveat**: for a tiny app, one model reused as both DTO and domain is a defensible shortcut — but split them the moment the API shape and your app's needs diverge (which they will). This ties into Clean Architecture's model-mapping principle."]}]},{heading:"Robustness and common pitfalls",blocks:[{t:"list",items:["**Always handle unknown/missing fields gracefully**: `ignoreUnknownKeys = true` (extra fields don't crash) and default values (missing fields use defaults). APIs evolve, and brittle parsing that crashes on any change is a common production bug.","**Polymorphic/sealed JSON**: for JSON that can be one of several shapes (a `type` discriminator field), kotlinx.serialization supports `@Serializable` sealed classes with a class discriminator — cleanly modeling 'this response is one of N types'.","**Dates and custom types**: JSON has no native date type — dates come as strings or numbers. Use a custom serializer (`@Serializable(with = ...)`) to convert to/from a proper type (`Instant`, `LocalDate`).","**Gson's Kotlin trap** (why to avoid it): Gson uses reflection and Java constructor semantics, so it *bypasses* Kotlin's default-value and null-check logic — it can leave a non-null property as `null` (a latent crash) or ignore your defaults. kotlinx.serialization and Moshi-codegen understand Kotlin properly.","**Retrofit integration**: add the converter factory (`Json.asConverterFactory(...)` for kotlinx.serialization, `MoshiConverterFactory`, or `GsonConverterFactory`) to your Retrofit builder, and Retrofit uses it to parse response bodies and serialize request bodies automatically."]},{t:"note",text:"Serialization essentials: converting objects ↔ JSON. kotlinx.serialization (compile-time, Kotlin-first, KMP, no reflection) is the modern choice; Moshi is solid; Gson is legacy with Kotlin pitfalls (ignores defaults/nullability). Use @Serializable/@SerialName; set ignoreUnknownKeys=true and use default values for robustness against API changes. Separate DTOs (mirror the API) from domain models (clean, non-null), mapping at the boundary. Integrate with Retrofit via a converter factory."}]}],HT=[{level:"junior",q:"What is JSON serialization and why do you need it?",a:[{t:"p",text:"**Serialization is converting a Kotlin object into a transmittable format (a JSON string), and deserialization is the reverse — parsing JSON back into objects.** You need it because APIs communicate in JSON (text) but your app works with Kotlin objects. So when you send data you serialize your object to JSON for the request body, and when you receive a response you deserialize the JSON into your objects to use in code."},{t:"code",title:"Both directions",code:`// JSON -> object (deserialize)
val user: UserDto = json.decodeFromString("""{"id":"1","name":"Rev"}""")
// object -> JSON (serialize)
val body: String = json.encodeToString(user)`},{t:"p",text:"A serialization library automates this mapping so you don't hand-write parsing code (which would be tedious and error-prone). On Android the main libraries are kotlinx.serialization (the modern Kotlin-first choice), Moshi, and Gson (legacy). You typically don't call serialize/deserialize manually — you configure a converter on Retrofit, and it automatically parses response bodies into your objects and serializes request bodies to JSON. The library reads your annotated data classes to know how to map between fields and JSON keys."}]},{level:"junior",q:"Which serialization library should you use and why?",a:[{t:"p",text:"**For new Android/KMP code, use kotlinx.serialization.** It's Kotlin-first: a compiler plugin generates serialization code *at compile time* (no runtime reflection), it fully respects Kotlin's null safety and default values, and — importantly for an Android/KMP engineer — it works in Kotlin Multiplatform, so the same models serialize on Android and iOS."},{t:"list",items:["**kotlinx.serialization** — the modern default. Compile-time, no reflection, KMP-compatible, understands Kotlin semantics. Mark classes `@Serializable`.","**Moshi** — Square's library, a solid choice too, especially with its codegen (avoiding reflection). Well-integrated with Retrofit. A reasonable alternative if you're not targeting KMP.","**Gson** — Google's older library, uses runtime reflection. It works but has real Kotlin pitfalls: it ignores Kotlin default values and can put `null` into non-null properties (because it bypasses Kotlin's constructor logic), silently breaking null safety. It's legacy — fine to maintain in existing code, but not for new code."]},{t:"p",text:"The short version: kotlinx.serialization for new code (Kotlin-native, KMP-ready, safe), Moshi as a solid alternative, and avoid Gson for new Kotlin code due to its null-safety and default-value problems. The KMP compatibility is often the deciding factor — Gson and Moshi are JVM-only, so a multiplatform project essentially must use kotlinx.serialization."}]},{level:"junior",q:"How do you handle a JSON field whose name doesn't match your Kotlin property?",a:[{t:"p",text:"**You use the `@SerialName` annotation (in kotlinx.serialization) to map the JSON key to your property, so your Kotlin code can use idiomatic names while still matching the API's JSON.** APIs often use snake_case (`is_premium`, `full_name`) while Kotlin convention is camelCase — `@SerialName` bridges that."},{t:"code",title:"Mapping JSON keys to Kotlin properties",code:`@Serializable
data class UserDto(
    @SerialName("user_id") val id: String,        // JSON "user_id" -> id
    @SerialName("full_name") val name: String,    // JSON "full_name" -> name
    @SerialName("is_premium") val premium: Boolean = false,
)`},{t:"p",text:'So the JSON key stays whatever the server sends (`user_id`), but in your code you use the clean name (`id`). Without this, your property name would have to exactly match the JSON key, forcing non-idiomatic Kotlin names. (Moshi uses `@Json(name = "...")` and Gson uses `@SerializedName("...")` for the same purpose.) This is also part of why keeping DTOs separate from domain models is useful — the DTO carries the API\'s naming via `@SerialName`, and the mapper translates to your clean domain model, so the server\'s naming conventions never leak into your app logic.'}]},{level:"junior",q:"How do you prevent parsing from crashing when the API adds a new field?",a:[{t:"p",text:"**You configure the parser to ignore unknown keys — in kotlinx.serialization, set `ignoreUnknownKeys = true` on the `Json` instance.** By default, strict parsing *crashes* when the JSON contains a field your data class doesn't declare. Since servers routinely add new fields over time (which they consider backward-compatible), strict parsing means a server-side addition can crash your released app. `ignoreUnknownKeys = true` makes the parser skip fields it doesn't recognize."},{t:"code",title:"Robust Json configuration",code:`val json = Json {
    ignoreUnknownKeys = true    // extra JSON fields don't crash parsing
    coerceInputValues = true    // use defaults for null/invalid values
    explicitNulls = false       // omit null fields when encoding
}`},{t:"p",text:"Combined with **default values** on your properties (so a *missing* field uses the default rather than failing), this makes your models resilient to API evolution in both directions — extra fields are ignored, and absent optional fields fall back to defaults. This is a genuinely important production concern: apps that crash whenever the backend adds a field are fragile, and the crash happens to *already-released* app versions you can't easily fix. Setting `ignoreUnknownKeys = true` and using sensible defaults is a standard robustness practice for any networked app."}]},{level:"senior",q:"Why separate DTOs from domain models, and where does mapping happen?",a:[{t:"p",text:"**A DTO (Data Transfer Object) mirrors the API's JSON *exactly* — its structure, nullability, and field names are dictated by the API contract. A domain model is shaped by *your app's* needs — clean names, non-null invariants, only the fields you use. Keeping them separate decouples your app from the API, and you map DTO → domain at the data-layer boundary (in the repository or a mapper it calls).**"},{t:"list",items:["**Decoupling from API changes** — the main reason. The server's JSON evolves independently of your app: fields get renamed, added, made nullable, restructured. If your whole app uses the DTO directly, every such change ripples through your entire codebase. With separate models, an API change touches only the DTO and its mapper — your domain model and all the code using it stay stable. It's a firewall between the external contract and your internal code.","**Nullability resolved once** — servers often mark everything nullable (defensive API design). If you use the DTO directly, `?` infects your whole app (`user?.name?.length`). The mapper resolves nullability *at the boundary* — providing defaults, validating, or failing fast for truly-required fields — so the rest of your app works with clean non-null domain types.","**Right shape for each layer** — the DTO can have API concerns (snake_case via `@SerialName`, serialization annotations, extra fields you ignore); the domain model has none of that noise and can add behavior/invariants. Serialization annotations don't pollute your business objects.","**Where mapping happens** — in the *data layer*, at the boundary where external data enters: the repository receives the DTO from Retrofit and maps it to the domain model (`dto.toDomain()`) before anything else in the app sees it. Mappers are plain functions, trivially unit-tested, and the place where dirty/optional server data gets validated and normalized."]},{t:"list",items:["**Often a three-way split**: DTO (network) → domain model (business) → UI model (presentation, formatted for display), mapping at each boundary. Plus a DB entity if you cache. Each is shaped by its layer's concerns.","**Pragmatic caveat**: for a tiny app or a throwaway prototype, reusing one model as both DTO and domain is a defensible YAGNI shortcut — but the moment the API shape and your app's needs diverge (which they inevitably do), split them. The cost of splitting later is higher than doing it up front for anything non-trivial."]},{t:"p",text:"**The senior framing**: this is the model-mapping principle from Clean Architecture applied to networking — each layer owns models shaped by *its* concerns, and you translate at boundaries. The payoff is a stable core that's insulated from an unstable, externally-controlled API, plus nullability and formatting resolved once at the edge instead of leaking everywhere. It's a small amount of mapping boilerplate that buys significant resilience and cleanliness."}]},{level:"senior",q:"How do you handle polymorphic JSON — a response that can be one of several types?",a:[{t:"p",text:"**You model it with a sealed class hierarchy plus a *class discriminator* — a field in the JSON (often `type`) that tells the parser which concrete type each object is. kotlinx.serialization supports this natively for `@Serializable` sealed classes.** The parser reads the discriminator field and deserializes into the matching subtype."},{t:"code",title:"Polymorphic deserialization with a discriminator",code:`@Serializable
sealed class Notification {
    @Serializable @SerialName("message")
    data class Message(val text: String, val from: String) : Notification()

    @Serializable @SerialName("friend_request")
    data class FriendRequest(val userId: String) : Notification()

    @Serializable @SerialName("system")
    data class System(val code: Int) : Notification()
}

val json = Json { classDiscriminator = "type" }  // JSON's "type" field selects the subtype
// {"type":"message","text":"hi","from":"a"} -> Notification.Message(...)`},{t:"list",items:['**How it works**: each subtype is annotated with a `@SerialName` that matches its discriminator *value* (`"message"`, `"friend_request"`). The `classDiscriminator` config names the JSON *field* that holds that value (`"type"`). When parsing, kotlinx.serialization reads the `type` field, finds the subtype whose `@SerialName` matches, and deserializes the rest into that type.',"**Why sealed + discriminator is the right model**: it turns 'this response could be one of N shapes' into a type-safe sealed hierarchy, so consuming code uses an *exhaustive `when`* over the subtypes — the compiler ensures you handle every notification type, and adding a new one flags every `when` that needs updating. This is far safer than parsing into a generic map and branching on a string.","**Custom discriminator handling**: if the API's discriminator doesn't fit the built-in mechanism (the type indicator is nested, or the shapes are distinguished by *presence* of fields rather than an explicit type field), you write a *custom serializer* that inspects the JSON and picks the type manually. kotlinx.serialization's `JsonContentPolymorphicSerializer` is designed for exactly the 'no explicit discriminator, distinguish by content' case.","**Open polymorphism / third-party types**: for types not in a sealed hierarchy (open polymorphism across modules), you register subtypes in a `SerializersModule`. Sealed hierarchies are preferred when possible because they're closed and exhaustive."]},{t:"p",text:"**The senior framing**: polymorphic JSON is common (notification feeds, event streams, content blocks, payment methods), and the robust approach is to model the closed set of shapes as a `@Serializable` sealed class with a discriminator, giving you type-safe, exhaustive handling. When the API's discrimination is irregular (no clean `type` field), you drop to a custom content-based serializer. Either way, the goal is to convert loosely-typed 'one of several JSON shapes' into a strongly-typed sealed hierarchy at the parsing boundary, so the rest of your code enjoys compile-time exhaustiveness rather than runtime string-matching."}]}],_T={id:"serialization",title:"JSON Serialization",tagline:"Serialize/deserialize, kotlinx.serialization vs Moshi vs Gson, @Serializable/@SerialName, robustness (ignoreUnknownKeys), DTO vs domain, and polymorphic JSON.",tags:["JSON","kotlinx.serialization","Moshi","DTO","Serialization"],content:BT,qa:HT},KT=[{heading:"The kinds of network failures",blocks:[{t:"p",text:"Networking fails in several distinct ways, and robust apps distinguish them because each needs a different response. Lumping all failures into 'something went wrong' gives poor UX and wrong retry behavior."},{t:"list",items:["**No connectivity / IO failure** — `IOException` (no network, connection dropped, DNS failure, timeout). Transient — the request never reached the server or got cut off. *Retry-able*; show 'check your connection'.","**HTTP error responses** — the server responded, but with a non-2xx status. Retrofit surfaces these as `HttpException` (with the status code). Sub-cases matter: 401 (re-authenticate), 403 (no permission), 404 (not found), 429 (rate-limited — back off), 5xx (server error — retry).","**Serialization/parsing errors** — the response arrived but couldn't be parsed (malformed JSON, unexpected shape). Usually a bug or API mismatch, not transient — don't retry; log it.","**Cancellation** — the coroutine was cancelled (user left the screen). This is *normal*, not an error — `CancellationException` must be rethrown, not treated as a failure."]}]},{heading:"Modeling errors as values (Result types)",blocks:[{t:"p",text:"The recommended approach: don't let raw exceptions propagate up through your layers. Instead, **catch technical exceptions at the data-layer boundary and map them into a typed result** — a sealed error type in domain terms. The ViewModel then handles that result as an expected branch (map to UI state), never dealing with `HttpException`/`IOException` directly."},{t:"code",title:"A typed error vocabulary + safe API call wrapper",code:`sealed interface DataError {
    data object NoConnection : DataError
    data object Unauthorized : DataError
    data object NotFound : DataError
    data class Server(val code: Int) : DataError
    data class Unknown(val cause: Throwable) : DataError
}

suspend fun <T> safeApiCall(call: suspend () -> T): Result<T, DataError> = try {
    Result.Success(call())
} catch (e: CancellationException) {
    throw e                                    // NEVER swallow cancellation
} catch (e: IOException) {
    Result.Failure(DataError.NoConnection)     // no network / timeout
} catch (e: HttpException) {
    Result.Failure(when (e.code()) {
        401 -> DataError.Unauthorized
        404 -> DataError.NotFound
        in 500..599 -> DataError.Server(e.code())
        else -> DataError.Unknown(e)
    })
} catch (e: Exception) {
    Result.Failure(DataError.Unknown(e))       // parse errors, etc.
}`},{t:"list",items:["**Map technical → domain errors at the boundary**: the data layer translates `IOException`/`HttpException` into a domain vocabulary (`NoConnection`, `Unauthorized`), so upper layers never depend on library-specific exception types.","**Always rethrow `CancellationException`** — a broad catch would swallow it and break coroutine cancellation. Catch it first and rethrow (or catch specific types only).","**The ViewModel handles the result exhaustively** — `when (error) { NoConnection -> showOffline(); Unauthorized -> goToLogin(); ... }` — mapping each error to the right UX, with the compiler ensuring every case is covered."]}]},{heading:"Mapping errors to good UX",blocks:[{t:"table",headers:["Error","UX response"],rows:[["No connection","'You're offline' + retry; keep showing cached data if available"],["Unauthorized (401)","attempt token refresh; if that fails, send to login"],["Forbidden (403)","'You don't have access' — not a retry situation"],["Not found (404)","'Not found' / empty state"],["Rate limited (429)","back off and retry after the delay; maybe a gentle 'try again shortly'"],["Server error (5xx)","'Something went wrong' + retry (with backoff)"]]},{t:"list",items:["**Don't blank the screen on error if you have cached data** — in an offline-first app, a failed refresh should keep showing the cached content and surface the error non-destructively (a snackbar), not replace everything with a full-screen error.","**Distinguish retryable from not** — offer retry for transient errors (network, 5xx); for a 403 or a 404, retry is pointless, so guide the user differently.","**Be specific but not technical** — 'Check your connection' is helpful; 'HttpException 503' is not. Map error types to human messages."]}]},{heading:"Token authentication and automatic refresh",blocks:[{t:"p",text:"Most APIs use token auth: a short-lived **access token** on each request, and a long-lived **refresh token** to get a new access token when it expires. The two mechanisms in OkHttp: an **Interceptor** to *attach* the token to every request, and an **Authenticator** to *refresh* it automatically on a 401."},{t:"code",title:"Interceptor (attach) + Authenticator (refresh on 401)",code:`// Attach the current access token to every request
class AuthInterceptor(private val tokens: TokenStore) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response =
        chain.proceed(
            chain.request().newBuilder()
                .header("Authorization", "Bearer \${tokens.access()}")
                .build()
        )
}

// When a request comes back 401, refresh the token and retry automatically
class TokenAuthenticator(private val tokens: TokenStore, private val authApi: AuthApi) : Authenticator {
    override fun authenticate(route: Route?, response: Response): Request? {
        synchronized(this) {                     // avoid many parallel refreshes
            val newToken = runBlocking { authApi.refresh(tokens.refresh()) }
            tokens.saveAccess(newToken.access)
            return response.request.newBuilder()
                .header("Authorization", "Bearer \${newToken.access}")
                .build()                         // OkHttp retries with the new token
        }
    }
}`},{t:"list",items:["**Interceptor attaches the token** to every outgoing request — so you never manually add it per call.","**Authenticator handles refresh**: when a response is 401 (token expired), OkHttp calls the `Authenticator`; you refresh the access token using the refresh token, and return the retried request with the new token. OkHttp automatically re-sends it. Returning `null` means 'give up' (refresh failed → log out).","**Concurrency care**: if many requests 401 at once, you don't want N simultaneous refresh calls — synchronize/deduplicate the refresh (a mutex, or check if the token was already refreshed) so only one refresh happens and the others use the new token.","**On refresh failure** (refresh token also expired/invalid) → clear the session and route to login. This is the 'you've been logged out' flow.","**Store tokens securely** — `EncryptedSharedPreferences` or encrypted storage, never plaintext."]}]},{heading:"Retry strategy",blocks:[{t:"list",items:["**Retry only transient errors** — network failures and 5xx (server errors). Don't retry 4xx client errors (they'll fail identically), except 401 (after refresh) and 429 (after the rate-limit delay).","**Exponential backoff with jitter** — increase the delay between attempts (1s, 2s, 4s…) and add randomness (jitter) so many clients don't retry in perfect sync (the 'thundering herd' that overwhelms a recovering server).","**Bound the attempts** — cap retries (e.g. 3) so a persistent failure doesn't loop forever draining battery.","**Idempotency for non-idempotent requests** — only blindly retry idempotent operations (GET/PUT/DELETE); for POST, use an idempotency key so a retry doesn't duplicate the action.","**Where to implement** — a Flow `retryWhen` operator, a custom OkHttp interceptor, or explicit logic in the repository. `retryWhen` in a Flow pipeline is clean for observable data."]},{t:"note",text:"Error/auth essentials: distinguish failure types (IOException = transient/retry; HttpException by code — 401 refresh, 403/404 don't retry, 429 back off, 5xx retry; parse errors = bug; cancellation = normal, rethrow). Model errors as typed values at the data-layer boundary; handle exhaustively in the ViewModel mapping to UX (keep cached data on refresh failure). Token auth: Interceptor attaches the token, Authenticator refreshes on 401 (deduplicated); refresh failure → logout; store tokens encrypted. Retry only transient errors with bounded exponential backoff + jitter."}]}],JT=[{level:"junior",q:"What are the different kinds of network errors you need to handle?",a:[{t:"list",items:["**Connectivity / IO failures** (`IOException`) — no network, connection dropped, DNS failure, timeout. The request never completed. These are *transient*, so retrying may work; show a 'check your connection' message.","**HTTP error responses** (`HttpException` in Retrofit) — the server responded with a non-2xx status. The specific code matters: 401 (re-authenticate), 403 (no permission), 404 (not found), 429 (rate-limited), 5xx (server error).","**Parsing/serialization errors** — the response arrived but couldn't be deserialized (malformed or unexpected JSON). Usually a bug or API mismatch, not transient — don't retry, log it.","**Cancellation** (`CancellationException`) — the coroutine was cancelled because the user left the screen. This is *normal*, not an error, and must be rethrown, not caught as a failure."]},{t:"p",text:"The reason to distinguish them is that each needs a *different* response. A network failure should offer retry and keep showing cached data; a 401 should trigger token refresh; a 404 should show an empty state; a parse error should be logged as a bug; and a cancellation should be ignored. Treating all failures as a generic 'error' gives poor UX (a retry button on a 403 that will never succeed) and wrong behavior (retrying a request that can't succeed, or breaking cancellation by swallowing `CancellationException`)."}]},{level:"junior",q:"Why should you map network exceptions to a typed result instead of letting them propagate?",a:[{t:"p",text:"**Because network failures are *expected outcomes* on mobile (the network is unreliable), not exceptional bugs — so modeling them as typed values makes handling explicit, exhaustive, and decoupled from library details.** Instead of letting `IOException`/`HttpException` propagate up through your layers, you catch them at the data-layer boundary and return a typed result — a sealed error type in your app's own vocabulary."},{t:"code",title:"Typed errors handled exhaustively",code:`sealed interface DataError { object NoConnection; object Unauthorized; data class Server(val code: Int) }

// ViewModel handles each case — compiler ensures all are covered
when (error) {
    DataError.NoConnection -> showOfflineMessage()
    DataError.Unauthorized -> navigateToLogin()
    is DataError.Server -> showRetry()
}`},{t:"list",items:["**Explicit and exhaustive** — a sealed error type forces the ViewModel to handle every case in a `when`, so you can't forget to handle 'offline' or 'unauthorized'. Exceptions are easy to forget to catch.","**Decoupled from libraries** — mapping `HttpException`/`IOException` to your own `DataError` at the boundary means the rest of your app never depends on Retrofit/OkHttp exception types. If you swap networking libraries, only the mapper changes.","**Better UX mapping** — each typed error maps cleanly to a UX response (offline message, login redirect, retry), rather than parsing exception messages.","**Reserves exceptions for real bugs** — expected failures become data; a thrown exception then genuinely means something is wrong (a programming error), making crash reports meaningful."]},{t:"p",text:"The one caution: when catching broadly to build the result, always rethrow `CancellationException` first (or catch specific types), or you'll break coroutine cancellation. This 'errors as values' approach is the modern recommended pattern and ties into Clean Architecture's layered error handling."}]},{level:"junior",q:"How does token-based authentication work in an Android app with Retrofit?",a:[{t:"p",text:"**The app obtains a token at login and then attaches it to every request via an OkHttp interceptor, so the server can identify the user on each stateless request.** The typical setup uses two OkHttp mechanisms: an *Interceptor* to attach the token, and an *Authenticator* to refresh it when it expires."},{t:"list",items:["**Login** — the user posts credentials; the server returns an access token (and usually a refresh token).","**Attach with an Interceptor** — an `AuthInterceptor` adds `Authorization: Bearer <accessToken>` to every outgoing request automatically, so you don't add it manually on each API call.","**Refresh with an Authenticator** — when a request returns 401 (token expired), OkHttp calls the `Authenticator`, which uses the refresh token to get a new access token and retries the request with it — transparently, without the user noticing.","**Store securely** — tokens go in `EncryptedSharedPreferences` or encrypted storage, never plaintext."]},{t:"p",text:"The access token is usually short-lived (for security — if it leaks, it expires soon), and the refresh token is longer-lived and used only to mint new access tokens. If refreshing *also* fails (the refresh token expired or was revoked), the app clears the session and sends the user to login — the 'you've been logged out' flow. This design works precisely because HTTP is stateless: the server doesn't remember you between requests, so you re-present the token every time, and the interceptor makes that automatic."}]},{level:"junior",q:"When should you retry a failed network request and when should you not?",a:[{t:"p",text:"**Retry *transient* failures — ones that might succeed on a second attempt — and don't retry failures that will fail identically no matter how many times you try.** The distinction comes from the error type and HTTP status code."},{t:"list",items:["**Retry**: network/IO failures (`IOException`, timeouts) — the request may have just hit a temporary connectivity blip. And 5xx server errors — the server had a transient problem that may resolve.","**Don't retry**: 4xx client errors — they mean *your request is wrong* (400 bad request, 403 forbidden, 404 not found), so retrying the same request fails the same way. Also don't retry parse errors (a bug/API mismatch).","**Special cases**: retry a 401 *after* refreshing the token (the retry uses the new token); retry a 429 (rate-limited) only *after* waiting the required delay."]},{t:"p",text:"And when you do retry, do it properly: use *exponential backoff* (increasing delays between attempts) so you don't hammer a struggling server, add *jitter* (randomness) so many clients don't retry in sync, and *bound* the attempts (e.g. 3) so a persistent failure doesn't loop forever draining battery. One more critical point: only blindly retry *idempotent* requests (GET/PUT/DELETE); for a POST that has side effects (placing an order), a blind retry could duplicate the action, so those need an idempotency key. The short rule: retry transient errors (network, 5xx) with bounded exponential backoff + jitter; never blindly retry client errors or non-idempotent operations."}]},{level:"senior",q:"How do you implement automatic token refresh, and what's the concurrency challenge?",a:[{t:"p",text:"**You use an OkHttp `Authenticator`, which OkHttp invokes automatically when a response comes back 401. In it, you refresh the access token using the refresh token and return the original request rebuilt with the new token — OkHttp then automatically retries it. Returning `null` signals 'give up' (refresh failed → log out).** The `Authenticator` is specifically designed for this reactive 'refresh on 401' flow, distinct from the `Interceptor` that proactively attaches the token."},{t:"code",title:"The Authenticator with refresh dedup",code:`class TokenAuthenticator(private val store: TokenStore, private val authApi: AuthApi) : Authenticator {
    override fun authenticate(route: Route?, response: Response): Request? {
        synchronized(this) {
            // If another thread already refreshed, use the new token instead of refreshing again
            val current = store.access()
            if (response.request.header("Authorization") != "Bearer $current") {
                return response.request.newBuilder()
                    .header("Authorization", "Bearer $current").build()
            }
            val refreshed = runBlocking { authApi.refresh(store.refresh()) } ?: return null
            store.saveAccess(refreshed.access)
            return response.request.newBuilder()
                .header("Authorization", "Bearer \${refreshed.access}").build()
        }
    }
}`},{t:"list",items:["**The concurrency challenge — the thundering herd of refreshes**: if the token expires while *several* requests are in flight, they *all* come back 401 around the same time, and each would independently trigger a refresh. That means N simultaneous refresh calls — wasteful, and worse, dangerous: many auth servers *rotate* the refresh token on use (return a new refresh token and invalidate the old), so parallel refreshes race, some using an already-invalidated refresh token, and you can end up logging the user out incorrectly.","**The fix — serialize and deduplicate the refresh**: use a lock (`synchronized`/`Mutex`) so only one refresh happens at a time. Critically, *before* refreshing inside the lock, check whether the token was *already refreshed* by a concurrent request (compare the failing request's token to the current stored token) — if so, just retry with the already-new token instead of refreshing again. This 'check-then-refresh under a lock' ensures exactly one refresh serves all the 401s.","**On refresh failure** — return `null` from the `Authenticator` so OkHttp stops retrying, and trigger the logout flow (clear session, navigate to login). Distinguish 'refresh token expired' (log out) from a transient network error during refresh (which you might retry).","**Avoid infinite loops** — if the retried request *also* 401s (e.g. the new token is immediately rejected), the Authenticator could loop; guard against retrying more than once (OkHttp limits this, but check `responseCount`).","**`runBlocking` caveat** — the `Authenticator` API is synchronous, so calling a suspend refresh requires `runBlocking`, which is acceptable here because it runs on OkHttp's background thread (not the main thread), but it's a known friction point."]},{t:"p",text:"**The senior framing**: automatic token refresh is straightforward in the happy path but the *concurrency* is where it goes wrong in production — concurrent 401s causing racing refreshes and (with rotating refresh tokens) spurious logouts. The robust implementation serializes refresh with a lock, deduplicates by checking if the token was already refreshed, handles refresh failure by logging out, and guards against retry loops. Getting this right is what makes 'the session silently keeps working' feel seamless instead of randomly logging users out."}]},{level:"senior",q:"In an offline-first app, how should a failed network refresh affect the UI?",a:[{t:"p",text:"**It should *not* blank the screen or replace content with a full-screen error — because the app has cached data to show. Instead, keep displaying the cached content and surface the refresh failure *non-destructively* (e.g. a snackbar or a subtle indicator), so the user still sees useful data and just knows the latest fetch didn't succeed.** This follows directly from the single-source-of-truth architecture: the UI reads from the local database, and a failed network refresh simply means the database wasn't updated — it doesn't affect the UI's ability to show what's already cached."},{t:"list",items:["**The mechanism**: because the UI observes the local DB (SSOT), it *always* has data to display. A refresh is a separate operation that *tries* to update the DB. If the refresh fails (offline, server error), the DB keeps its previous data, the observing UI keeps showing it, and you report the *refresh's* failure separately from the *data display*.","**Separate 'data' state from 'refresh' state**: the UI state should distinguish 'here's the data' (from the DB, always present if cached) from 'the last refresh failed' (a transient status). A common shape: `UiState(items = cached, isRefreshing = false, refreshError = \"Couldn't update\")`. The list shows `items`; the error shows as a snackbar; neither wipes the other.","**Contrast with the empty case**: if there's *no* cached data at all (first launch, offline), *then* a failure does warrant a full-screen error/empty state with retry — because there's genuinely nothing to show. So the rule is conditional: show cached data + gentle error if cache exists; show full-screen error + retry only if there's nothing cached.","**Offer retry appropriately**: for transient errors (network, 5xx), a retry affordance (pull-to-refresh, a retry button on the snackbar) makes sense. For a 403, retry is pointless — guide the user differently."]},{t:"p",text:"**The senior framing**: this is a defining benefit of offline-first / SSOT architecture — errors become *non-blocking* because the UI's data source (the DB) is decoupled from the network. The design principle is 'never punish the user for a failed background refresh when you already have data to show' — degrade gracefully by keeping cached content visible and reporting the refresh failure gently, reserving full-screen error states for the genuine 'no data at all' case. An app that shows a full-screen error and loses the user's content every time a refresh fails on a flaky network is exactly the fragile experience offline-first exists to prevent."}]}],GT={id:"error-auth",title:"Error Handling & Auth",tagline:"Failure types, modeling errors as typed values, mapping errors to UX, token auth with Interceptor + Authenticator (refresh dedup), and retry strategy.",tags:["Networking","Error Handling","Auth","Token Refresh","Retry"],content:KT,qa:JT},$T=[{heading:"Ktor Client — the KMP networking library",blocks:[{t:"p",text:"**Ktor** is a Kotlin networking library from JetBrains. The **Ktor Client** is the relevant part for apps — it's an HTTP client written in pure Kotlin with coroutines, and crucially it's **multiplatform**: the same networking code runs on Android, iOS, desktop, and web. This is why Ktor is the standard networking choice for **Kotlin Multiplatform (KMP)** projects, where Retrofit (JVM/Android-only) can't be used in shared code."},{t:"code",title:"A Ktor client and a request",code:`val client = HttpClient(CIO) {                 // CIO = the engine (platform-specific)
    install(ContentNegotiation) {
        json(Json { ignoreUnknownKeys = true })  // kotlinx.serialization
    }
    install(HttpTimeout) { requestTimeoutMillis = 15_000 }
    install(Logging) { level = LogLevel.HEADERS }
    defaultRequest { url("https://api.example.com/") }
}

// Requests are suspend functions returning typed results
suspend fun getUser(id: String): UserDto =
    client.get("users/$id").body()            // auto-parsed via ContentNegotiation

suspend fun createUser(user: CreateUserRequest): UserDto =
    client.post("users") {
        contentType(ContentType.Application.Json)
        setBody(user)                          // serialized to JSON
    }.body()`},{t:"list",items:["**Engine + config**: a Ktor client has an *engine* (the actual HTTP implementation — `CIO`, `OkHttp`, `Darwin` for iOS, `Android`) and a set of installed *plugins* (features). The engine is platform-specific (chosen per target via expect/actual); the client code is shared.","**Plugins (features)**: `ContentNegotiation` (serialization, integrates kotlinx.serialization), `HttpTimeout`, `Logging`, `Auth` (token auth), `HttpRequestRetry` (retries), `HttpCache`. You install what you need — Ktor is modular.","**Coroutine-native**: all requests are `suspend` functions; responses are parsed with `.body<T>()`. No interface-generation like Retrofit — you write request functions directly."]}]},{heading:"Ktor vs Retrofit",blocks:[{t:"table",headers:["","Retrofit (+OkHttp)","Ktor Client"],rows:[["Platform","JVM/Android only","Multiplatform (Android, iOS, desktop, web)"],["API style","declarative interface + annotations","imperative request functions (DSL)"],["Underlying engine","OkHttp","pluggable (CIO, OkHttp, Darwin, ...)"],["Maturity on Android","very mature, huge ecosystem","growing; standard for KMP"],["Best for","Android-only apps","KMP / shared networking code"]]},{t:"list",items:["**Choose Retrofit** for Android-only apps — it's extremely mature, has a huge ecosystem, and the declarative interface style is concise for REST.","**Choose Ktor** for KMP projects, where networking must live in `commonMain` and run on iOS too — Retrofit can't. On an all-Android project Ktor is also fine but has less momentum.","**They share concepts**: both are coroutine-based, both integrate kotlinx.serialization, both support interceptors/plugins for auth/logging/retry — so the mental model transfers. Ktor can even use OkHttp as its engine on Android (getting OkHttp's connection pooling under Ktor's API).","**For an Android/KMP engineer**: know both — Retrofit for Android-specific code, Ktor for the shared KMP layer."]}]},{heading:"WebSockets — real-time bidirectional communication",blocks:[{t:"p",text:"HTTP is request/response — the client asks, the server answers, then the connection is done. For **real-time** features (chat, live updates, presence, live scores), that's inefficient: you'd have to poll (repeatedly ask 'anything new?'). A **WebSocket** is a *persistent, bidirectional* connection: after an initial handshake (which upgrades an HTTP connection), both client and server can send messages to each other *at any time* over the open connection, until one side closes it."},{t:"list",items:["**Persistent** — the connection stays open, unlike HTTP's one-shot request/response. No repeated connection setup.","**Bidirectional (full-duplex)** — the *server* can push messages to the client without the client asking (impossible with plain HTTP request/response). And the client can send anytime too.","**Efficient for real-time** — no polling overhead, low latency, instant delivery. Ideal for chat, live feeds, collaborative editing, notifications, live dashboards.","**Starts as HTTP** — a WebSocket connection begins with an HTTP request with an `Upgrade: websocket` header; the server agrees, and the connection 'upgrades' to the WebSocket protocol (`ws://` or secure `wss://`)."]}]},{heading:"WebSockets in practice — polling vs WebSockets vs SSE",blocks:[{t:"code",title:"A WebSocket with Ktor, exposed as a Flow",code:`fun observeMessages(): Flow<ChatMessage> = callbackFlow {
    client.webSocket(urlString = "wss://chat.example.com/ws") {
        try {
            for (frame in incoming) {                 // receive messages as they arrive
                if (frame is Frame.Text) {
                    trySend(Json.decodeFromString(frame.readText()))
                }
            }
        } finally { /* connection closed */ }
    }
    awaitClose { /* cleanup */ }
}`},{t:"table",headers:["Approach","How","Use when"],rows:[["Polling","repeatedly GET on a timer","simple, infrequent updates; wasteful and laggy for real-time"],["Long polling","hold a request open until data is ready","near-real-time without WebSockets; a middle ground"],["WebSockets","persistent bidirectional connection","true real-time, bidirectional (chat, live collab)"],["Server-Sent Events (SSE)","server pushes over a one-way HTTP stream","server→client only updates (live feed, notifications) — simpler than WS"]]},{t:"list",items:["**WebSockets vs polling**: polling wastes battery/data (constant requests, most returning 'nothing new') and adds latency (updates only arrive on the next poll). WebSockets push instantly with one connection. But WebSockets need connection management (reconnection, heartbeats).","**SSE (Server-Sent Events)** — a simpler alternative when you only need *server → client* pushing (a live feed, notifications). It's a one-way HTTP stream, easier to implement and more firewall-friendly than WebSockets, but not bidirectional.","**Connection management is the hard part**: WebSockets drop (network changes, backgrounding, server restarts), so you need *reconnection with backoff*, *heartbeats/pings* to detect dead connections, and handling of message ordering/buffering across reconnects. Expose the stream as a Flow (via `callbackFlow`) so the UI observes it cleanly.","**Both OkHttp and Ktor support WebSockets** — OkHttp has a `WebSocket` API with a listener; Ktor has `client.webSocket { }` with coroutine-based `incoming`/`send`."]},{t:"note",text:"Ktor = pure-Kotlin, multiplatform HTTP client (engine + plugins, coroutine-native) — the KMP networking standard where Retrofit (Android-only) can't go; use Retrofit for Android-only, Ktor for shared KMP code. WebSockets = persistent, bidirectional connection for real-time (chat, live updates) — the server can push, no polling; starts as an HTTP upgrade. Alternatives: polling (simple, wasteful), long polling (middle ground), SSE (server→client only, simpler). Key challenge: connection management (reconnect + backoff, heartbeats); expose as a Flow via callbackFlow."}]}],YT=[{level:"junior",q:"What is Ktor and when would you use it over Retrofit?",a:[{t:"p",text:"**Ktor is a Kotlin networking library from JetBrains; its client is a pure-Kotlin, coroutine-based HTTP client that is *multiplatform* — the same networking code runs on Android, iOS, desktop, and web.** That multiplatform capability is the main reason to choose it: in a **Kotlin Multiplatform (KMP)** project, networking code lives in shared `commonMain`, and Retrofit — which is JVM/Android-only — can't be used there, but Ktor can."},{t:"list",items:["**Use Retrofit** for Android-only apps — it's very mature, has a large ecosystem, and its declarative interface-with-annotations style is concise for REST.","**Use Ktor** for KMP projects where the networking layer must be shared and run on iOS too. It's the standard KMP networking choice.","They share concepts — both are coroutine-based, both integrate kotlinx.serialization, both support plugins/interceptors for auth/logging/retry — so knowledge transfers. Ktor can even use OkHttp as its engine on Android."]},{t:"p",text:"The difference in style: Retrofit generates an implementation from an annotated interface, while Ktor uses imperative request functions (`client.get(\"users/$id\").body()`). For an Android/KMP engineer, the practical answer is 'Retrofit for Android-specific code, Ktor for the shared multiplatform layer' — and knowing both is expected."}]},{level:"junior",q:"What is a WebSocket and how is it different from HTTP?",a:[{t:"p",text:"**A WebSocket is a persistent, bidirectional connection between client and server, used for real-time communication. It's different from HTTP's request/response model: with HTTP, the client asks and the server answers, then it's done; with a WebSocket, the connection stays open and *both sides can send messages at any time*.**"},{t:"list",items:["**Persistent** — the connection stays open (unlike HTTP's one-shot exchange), so there's no repeated connection setup for each message.","**Bidirectional (full-duplex)** — crucially, the *server can push messages to the client* without the client asking. Plain HTTP can't do this; the client always has to initiate.","**Real-time & efficient** — messages arrive instantly with no polling, low latency and overhead. Ideal for chat, live feeds, presence, collaborative editing, live scores.","**Starts as HTTP** — a WebSocket begins with an HTTP request carrying an `Upgrade: websocket` header; the server agrees and the connection 'upgrades' to the WebSocket protocol (`ws://` or secure `wss://`)."]},{t:"p",text:"The core distinction to state: HTTP is *pull* (client requests, server responds, connection closes), while a WebSocket is a *persistent two-way channel* where the server can *push*. That's what makes real-time features practical — without WebSockets you'd have to constantly poll ('anything new?'), which is wasteful and laggy. The trade-off is that WebSockets require connection management (reconnection, heartbeats), which HTTP's stateless model doesn't."}]},{level:"junior",q:"When would you use WebSockets versus polling?",a:[{t:"p",text:"**Use WebSockets for genuine real-time, frequent, or bidirectional updates; use polling for simple, infrequent updates where near-real-time is fine.** The trade-off is real-time efficiency vs implementation simplicity."},{t:"list",items:["**Polling** (repeatedly GET on a timer) is simple — just a normal HTTP request on a schedule — and fine for data that changes infrequently and where a delay is acceptable (checking for a status update every 30 seconds). But it's *wasteful* (most requests return 'nothing new', burning battery and data) and *laggy* (updates only arrive on the next poll interval). For anything needing instant updates, it's a poor fit.","**WebSockets** give *instant* delivery with a single persistent connection and support the server *pushing* to the client — essential for chat, live collaboration, live feeds, or anything where the server needs to notify the client immediately. The cost is complexity: you must manage the connection (reconnect on drops, heartbeats to detect dead connections)."]},{t:"p",text:"There are middle grounds worth knowing: **long polling** (the server holds a request open until it has data, then responds — near-real-time without full WebSockets) and **Server-Sent Events (SSE)** (the server pushes over a one-way HTTP stream — simpler than WebSockets and firewall-friendly, but *server→client only*, so good for live feeds/notifications but not bidirectional chat). The decision: infrequent updates + simplicity → polling; server→client real-time only → SSE; full bidirectional real-time → WebSockets. Don't reach for WebSockets' complexity if simple polling or SSE meets the need."}]},{level:"junior",q:"How do you consume a WebSocket stream in a Compose/ViewModel architecture?",a:[{t:"p",text:"**You wrap the WebSocket in a `callbackFlow` (or `channelFlow`) to expose incoming messages as a Kotlin `Flow`, which the ViewModel collects and turns into UI state — just like any other reactive data source.** This bridges the WebSocket's callback/streaming nature into the coroutine world so it fits cleanly into the standard architecture."},{t:"code",title:"WebSocket as a Flow",code:`fun observeMessages(): Flow<ChatMessage> = callbackFlow {
    client.webSocket("wss://chat.example.com/ws") {
        for (frame in incoming) {                       // suspends, receives frames
            if (frame is Frame.Text) trySend(parse(frame.readText()))
        }
    }
    awaitClose { /* close connection / cleanup */ }
}
// ViewModel collects it into StateFlow like any other Flow`},{t:"p",text:"Exposing it as a Flow means the rest of your app treats real-time messages the same as any observable data — the repository provides `Flow<ChatMessage>`, the ViewModel collects it (often accumulating into a `StateFlow<List<ChatMessage>>`), and the UI observes that state. The `callbackFlow` + `awaitClose` pattern ensures the WebSocket is properly closed when collection stops (the user leaves the screen), tying the connection's lifetime to the observer via structured concurrency. For sending messages, the repository exposes a suspend function that writes to the socket's `send`. The key idea is that WebSockets don't need a *special* architecture — bridge them to a Flow and they slot into the same unidirectional-data-flow pattern as everything else."}]},{level:"senior",q:"What are the challenges of managing a WebSocket connection on mobile, and how do you handle them?",a:[{t:"p",text:"**The core challenge is that mobile connections are *unstable and lifecycle-constrained* — networks change (Wi-Fi ↔ cellular), the app gets backgrounded, the device sleeps, servers restart — so a WebSocket *will* drop repeatedly, and naive code that assumes a stable persistent connection breaks. Robust WebSocket handling is mostly about connection lifecycle management, not the messaging itself.**"},{t:"list",items:["**Reconnection with backoff**: the connection drops, and you must automatically reconnect — but with *exponential backoff* (increasing delays) and *jitter*, so you don't hammer the server (especially if it's down and thousands of clients reconnect at once — the thundering herd). Cap the backoff at a maximum and keep retrying. On reconnect, re-establish any subscriptions/state the server needs.","**Heartbeats / ping-pong**: a dropped connection often *doesn't* raise an error immediately — the socket looks open but is dead (a 'half-open' connection). You send periodic pings and expect pongs; if a pong doesn't arrive in time, you consider the connection dead and reconnect. Without heartbeats, you'd silently stop receiving messages while thinking you're connected.","**Lifecycle awareness**: keeping a WebSocket open while the app is backgrounded wastes battery and the OS may kill it anyway. Typically you *close* the connection when the app backgrounds (or the screen is not visible) and *reopen* on foreground — tying the connection to `lifecycleScope`/`repeatOnLifecycle(STARTED)` or the Compose lifecycle. For truly background real-time (a chat needing to notify when closed), you'd use push notifications (FCM) instead of holding a socket open.","**Message ordering & gap handling across reconnects**: while disconnected, you may have missed messages. On reconnect, you need a strategy — the server sends a 'since timestamp/id' catch-up, or you re-fetch missed data via REST — so the user doesn't have holes in their message history. Give messages sequence numbers/ids to detect gaps and dedupe.","**Send buffering**: messages the user sends while momentarily disconnected should be queued and sent on reconnect (or shown as 'sending…' and retried), not silently dropped.","**Authentication on (re)connect**: the connection needs auth (a token in the handshake or a first message); on reconnect with a possibly-expired token, you may need to refresh the token before reconnecting."]},{t:"p",text:"**The senior framing**: a WebSocket on mobile is not 'open it once and read forever' — it's a *managed, self-healing connection* that must survive constant network changes and lifecycle events. The robust implementation wraps the socket in a layer that handles reconnection (backoff + jitter), liveness (heartbeats), lifecycle (connect/disconnect on foreground/background), and continuity (catch-up on reconnect, send queue, dedup). You expose the *result* as a clean Flow of messages to the app, hiding all that management. Underestimating the connection-management complexity — treating a WebSocket like a reliable pipe — is the classic mistake; on mobile, the management *is* the hard part, and for background delivery you often complement WebSockets with push notifications rather than trying to keep a socket alive indefinitely."}]},{level:"senior",q:"How does Ktor's engine/plugin architecture work, and why is it well-suited to KMP?",a:[{t:"p",text:"**Ktor Client separates the *engine* (the actual platform HTTP implementation) from the *client API and plugins* (the shared, platform-agnostic features). This separation is exactly what makes it multiplatform: the API and your networking code are written once in shared code, while the engine is swapped per platform via the `expect/actual` mechanism.**"},{t:"list",items:["**The engine** is the low-level HTTP implementation — how bytes actually go over the network. Ktor provides several: `CIO` (pure Kotlin, cross-platform), `OkHttp` and `Android` (JVM/Android), `Darwin` (iOS, using Apple's URLSession), `Js` (browser). You pick the appropriate engine per target (often via `expect/actual`), and the *rest of your code doesn't change* — it talks to the same `HttpClient` API regardless of engine.","**Plugins (features)** are modular, installable capabilities that live in the shared layer: `ContentNegotiation` (serialization with kotlinx.serialization — itself multiplatform), `Auth` (token auth with refresh), `Logging`, `HttpTimeout`, `HttpRequestRetry`, `HttpCache`, `WebSockets`. You `install()` the ones you need. They're implemented against the engine-agnostic API, so they work on every platform.","**Why this suits KMP**: the value proposition of KMP is 'write business/data logic once, run on Android and iOS'. Networking is a big part of the data layer, and Ktor lets the *entire* networking layer — request definitions, serialization, auth, retry, error handling — live in `commonMain` and be shared. Only the engine (a single line choosing the platform implementation) differs per target. Retrofit can't do this because it's fundamentally tied to the JVM (dynamic proxies, OkHttp), so it can never run on iOS.","**Pairs with kotlinx.serialization**: since Ktor's ContentNegotiation uses kotlinx.serialization (also multiplatform, compile-time, reflection-free), the whole serialize→request→parse pipeline is shared and works on Kotlin/Native (iOS) where reflection-based libraries like Gson/Moshi don't."]},{t:"p",text:"**The senior framing**: Ktor's engine/plugin split is a deliberate design for portability — abstract the *what* (the HTTP semantics, features, and your API code) away from the *how* (the platform-specific byte transport), so the former is shared and only the latter is platform-specific. This mirrors KMP's whole philosophy (`expect/actual` for platform specifics, shared code for everything else) and is why Ktor + kotlinx.serialization is *the* KMP networking stack. On an Android-only project you'd still likely pick Retrofit for its maturity, but the moment iOS shares the networking code, Ktor's architecture is what makes that possible."}]}],QT={id:"ktor-websockets",title:"Ktor & WebSockets",tagline:"Ktor Client & KMP networking, Ktor vs Retrofit, engine/plugin architecture, WebSockets for real-time, polling vs WS vs SSE, and connection management.",tags:["Ktor","KMP","WebSockets","Real-time","SSE"],content:$T,qa:YT},XT={id:"networking",name:"Networking",description:"Talking to servers — HTTP & REST fundamentals, Retrofit & OkHttp, JSON serialization, error handling & auth, and Ktor & WebSockets.",topics:[NT,zT,_T,GT,QT]},rg=[Bx,{id:"architecture",name:"Architecture",description:"App architecture patterns and how they play out on Android & KMP — the layer every interview starts with.",topics:[cx,ux,fx,wx,kx,Tx]},bT,mS,OS,tT,OT,XT];function ZT(t,a){const i=rg.find(r=>r.id===t),s=i==null?void 0:i.topics.find(r=>r.id===a);return i&&s?{category:i,topic:s}:null}function eC(){return g.jsx("div",{className:"noise relative min-h-screen bg-ink text-body",children:g.jsxs("div",{className:"mx-auto max-w-4xl px-6 py-16 sm:py-24",children:[g.jsx(Xi,{to:"/",className:"text-sm text-body/70 transition-colors hover:text-bright",children:"← Back to portfolio"}),g.jsxs("header",{className:"mt-10",children:[g.jsx("p",{className:"font-mono text-xs uppercase tracking-[0.3em] text-violet",children:"Interview Preparation"}),g.jsxs("h1",{className:"font-display mt-4 text-3xl font-bold text-bright sm:text-5xl",children:["Android & KMP ",g.jsx("span",{className:"text-gradient",children:"Interview Prep"})]}),g.jsxs("p",{className:"mt-5 max-w-2xl leading-relaxed",children:["This page is dedicated to all information regarding preparing for Android and KMP developer interviews. Pick a category below, then a topic — each topic page has a ",g.jsx("strong",{className:"text-bright",children:"Content"})," ","tab to learn from and an"," ",g.jsx("strong",{className:"text-bright",children:"Interview Prep"})," tab with questions whose answers stay hidden until you reveal them."]})]}),g.jsx("div",{className:"mt-14 space-y-10",children:rg.map(t=>g.jsxs("section",{className:"glass rounded-2xl p-6 sm:p-8",children:[g.jsx("h2",{className:"font-display text-xl font-semibold text-bright",children:t.name}),g.jsx("p",{className:"mt-2 text-sm leading-relaxed",children:t.description}),g.jsx("div",{className:"mt-5 flex flex-wrap gap-3",children:t.topics.map(a=>g.jsxs(Xi,{to:`/interview_preparation/${t.id}/${a.id}`,className:"glow-card rounded-xl border border-line bg-white/[0.04] px-5 py-3 font-display text-sm font-semibold text-bright transition-colors hover:text-violet",children:[a.title," →"]},a.id))})]},t.id))}),g.jsx("p",{className:"mt-14 text-xs text-body/50",children:"More categories and topics are added over time."})]})})}function La({text:t}){return String(t).split(/(\*\*[^*]+\*\*|`[^`]+`)/g).map((i,s)=>i.startsWith("**")&&i.endsWith("**")?g.jsx("strong",{className:"font-semibold text-bright",children:i.slice(2,-2)},s):i.startsWith("`")&&i.endsWith("`")?g.jsx("code",{className:"rounded bg-white/[0.07] px-1.5 py-0.5 font-mono text-[0.85em] text-cyan",children:i.slice(1,-1)},s):g.jsx(Pt.Fragment,{children:i},s))}function tC({title:t,code:a}){return g.jsxs("figure",{className:"overflow-hidden rounded-xl border border-line bg-ink-2",children:[t&&g.jsx("figcaption",{className:"border-b border-line px-4 py-2 font-mono text-xs text-violet",children:t}),g.jsx("div",{className:"overflow-x-auto",children:g.jsx("pre",{className:"p-4 font-mono text-[13px] leading-relaxed text-bright/90",children:g.jsx("code",{children:a})})})]})}function nC({headers:t,rows:a}){return g.jsx("div",{className:"overflow-x-auto rounded-xl border border-line",children:g.jsxs("table",{className:"w-full min-w-[560px] text-left text-sm",children:[g.jsx("thead",{children:g.jsx("tr",{className:"border-b border-line bg-white/[0.03]",children:t.map((i,s)=>g.jsx("th",{className:"px-4 py-3 font-semibold text-bright",children:g.jsx(La,{text:i})},s))})}),g.jsx("tbody",{children:a.map((i,s)=>g.jsx("tr",{className:"border-b border-line/60 last:border-0 align-top",children:i.map((r,c)=>g.jsx("td",{className:"px-4 py-3 text-body",children:g.jsx(La,{text:r})},c))},s))})]})})}function lg({blocks:t}){return g.jsx("div",{className:"space-y-4",children:t.map((a,i)=>{switch(a.t){case"p":return g.jsx("p",{className:"leading-relaxed text-body",children:g.jsx(La,{text:a.text})},i);case"h3":return g.jsx("h4",{className:"font-display pt-2 text-base font-semibold text-bright",children:g.jsx(La,{text:a.text})},i);case"list":return g.jsx("ul",{className:"space-y-2 pl-5",children:a.items.map((s,r)=>g.jsx("li",{className:"list-disc leading-relaxed text-body marker:text-violet",children:g.jsx(La,{text:s})},r))},i);case"code":return g.jsx(tC,{title:a.title,code:a.code},i);case"table":return g.jsx(nC,{headers:a.headers,rows:a.rows},i);case"note":return g.jsxs("div",{className:"rounded-xl border border-violet/30 bg-violet/[0.07] px-4 py-3 text-sm leading-relaxed text-body",children:[g.jsx("span",{className:"mr-2 font-semibold text-violet",children:"Interview tip"}),g.jsx(La,{text:a.text})]},i);default:return null}})})}const aC=[{id:"content",label:"Content"},{id:"qa",label:"Interview Prep"}],iC=[{id:"all",label:"All"},{id:"junior",label:"Junior"},{id:"senior",label:"Senior"}];function oC({level:t}){const a=t==="senior";return g.jsx("span",{className:`rounded-full border px-2.5 py-0.5 font-mono text-[10px] uppercase tracking-wider ${a?"border-fuchsia/40 bg-fuchsia/10 text-fuchsia":"border-emerald/40 bg-emerald/10 text-emerald"}`,children:t})}function sC({item:t,isOpen:a,onToggle:i}){return g.jsxs("div",{className:"glass overflow-hidden rounded-xl",children:[g.jsxs("button",{type:"button",onClick:i,"aria-expanded":a,className:"flex w-full items-center gap-3 px-5 py-4 text-left transition-colors hover:bg-white/[0.03]",children:[g.jsx(oC,{level:t.level}),g.jsx("span",{className:"flex-1 font-display text-sm font-semibold text-bright sm:text-[15px]",children:t.q}),g.jsx("span",{className:`text-violet transition-transform duration-200 ${a?"rotate-180":""}`,children:"▾"})]}),a&&g.jsx("div",{className:"border-t border-line px-5 py-5",children:g.jsx(lg,{blocks:t.a})})]})}function rC({qa:t}){const[a,i]=x.useState("all"),[s,r]=x.useState(()=>new Set),c=x.useMemo(()=>t.map((p,m)=>({item:p,index:m})).filter(({item:p})=>a==="all"||p.level===a),[t,a]);function h(p){r(m=>{const f=new Set(m);return f.has(p)?f.delete(p):f.add(p),f})}return g.jsxs("div",{children:[g.jsxs("div",{className:"mb-6 flex flex-wrap items-center gap-2",children:[iC.map(p=>g.jsx("button",{type:"button",onClick:()=>i(p.id),className:`rounded-full border px-4 py-1.5 text-xs font-semibold transition-colors ${a===p.id?"border-violet/60 bg-violet/15 text-bright":"border-line text-body hover:text-bright"}`,children:p.label},p.id)),g.jsxs("span",{className:"ml-auto text-xs text-body/60",children:[c.length," questions — click one to reveal the answer"]})]}),g.jsx("div",{className:"space-y-3",children:c.map(({item:p,index:m})=>g.jsx(sC,{item:p,isOpen:s.has(m),onToggle:()=>h(m)},m))})]})}function lC({content:t}){return g.jsx("div",{className:"space-y-12",children:t.map((a,i)=>g.jsxs("section",{children:[g.jsxs("h2",{className:"font-display mb-5 flex items-baseline gap-3 text-lg font-bold text-bright sm:text-xl",children:[g.jsx("span",{className:"font-mono text-sm text-violet",children:String(i+1).padStart(2,"0")}),a.heading]}),g.jsx(lg,{blocks:a.blocks})]},i))})}function cC(){const{categoryId:t,topicId:a}=ak(),[i,s]=x.useState("content"),r=ZT(t,a);if(!r)return g.jsx(ng,{to:"/interview_preparation",replace:!0});const{category:c,topic:h}=r;return g.jsx("div",{className:"noise relative min-h-screen bg-ink text-body",children:g.jsxs("div",{className:"mx-auto max-w-4xl px-6 py-16 sm:py-20",children:[g.jsx(Xi,{to:"/interview_preparation",className:"text-sm text-body/70 transition-colors hover:text-bright",children:"← All topics"}),g.jsxs("header",{className:"mt-8",children:[g.jsx("p",{className:"font-mono text-xs uppercase tracking-[0.3em] text-violet",children:c.name}),g.jsx("h1",{className:"font-display mt-3 text-3xl font-bold text-bright sm:text-4xl",children:h.title}),g.jsx("p",{className:"mt-3 max-w-2xl text-sm leading-relaxed",children:h.tagline}),g.jsx("div",{className:"mt-4 flex flex-wrap gap-2",children:h.tags.map(p=>g.jsx("span",{className:"rounded-full border border-line px-3 py-1 font-mono text-[11px] text-body/80",children:p},p))})]}),g.jsx("nav",{className:"mt-10 flex gap-1 border-b border-line",children:aC.map(p=>g.jsx("button",{type:"button",onClick:()=>s(p.id),className:`-mb-px border-b-2 px-5 py-3 font-display text-sm font-semibold transition-colors ${i===p.id?"border-violet text-bright":"border-transparent text-body hover:text-bright"}`,children:p.label},p.id))}),g.jsx("div",{className:"mt-10",children:i==="content"?g.jsx(lC,{content:h.content}):g.jsx(rC,{qa:h.qa})})]})})}const rd=x.createContext({});function ro(t){const a=x.useRef(null);return a.current===null&&(a.current=t()),a.current}const tr=x.createContext(null),lo=x.createContext({transformPagePoint:t=>t,isStatic:!1,reducedMotion:"never"});class dC extends x.Component{getSnapshotBeforeUpdate(a){const i=this.props.childRef.current;if(i&&a.isPresent&&!this.props.isPresent){const s=this.props.sizeRef.current;s.height=i.offsetHeight||0,s.width=i.offsetWidth||0,s.top=i.offsetTop,s.left=i.offsetLeft}return null}componentDidUpdate(){}render(){return this.props.children}}function hC({children:t,isPresent:a}){const i=x.useId(),s=x.useRef(null),r=x.useRef({width:0,height:0,top:0,left:0}),{nonce:c}=x.useContext(lo);return x.useInsertionEffect(()=>{const{width:h,height:p,top:m,left:f}=r.current;if(a||!s.current||!h||!p)return;s.current.dataset.motionPopId=i;const y=document.createElement("style");return c&&(y.nonce=c),document.head.appendChild(y),y.sheet&&y.sheet.insertRule(`
          [data-motion-pop-id="${i}"] {
            position: absolute !important;
            width: ${h}px !important;
            height: ${p}px !important;
            top: ${m}px !important;
            left: ${f}px !important;
          }
        `),()=>{document.head.removeChild(y)}},[a]),g.jsx(dC,{isPresent:a,childRef:s,sizeRef:r,children:x.cloneElement(t,{ref:s})})}const uC=({children:t,initial:a,isPresent:i,onExitComplete:s,custom:r,presenceAffectsLayout:c,mode:h})=>{const p=ro(pC),m=x.useId(),f=x.useCallback(v=>{p.set(v,!0);for(const b of p.values())if(!b)return;s&&s()},[p,s]),y=x.useMemo(()=>({id:m,initial:a,isPresent:i,custom:r,onExitComplete:f,register:v=>(p.set(v,!1),()=>p.delete(v))}),c?[Math.random(),f]:[i,f]);return x.useMemo(()=>{p.forEach((v,b)=>p.set(b,!1))},[i]),x.useEffect(()=>{!i&&!p.size&&s&&s()},[i]),h==="popLayout"&&(t=g.jsx(hC,{isPresent:i,children:t})),g.jsx(tr.Provider,{value:y,children:t})};function pC(){return new Map}function cg(t=!0){const a=x.useContext(tr);if(a===null)return[!0,null];const{isPresent:i,onExitComplete:s,register:r}=a,c=x.useId();x.useEffect(()=>{t&&r(c)},[t]);const h=x.useCallback(()=>t&&s&&s(c),[c,s,t]);return!i&&s?[!1,h]:[!0]}const Cs=t=>t.key||"";function Zp(t){const a=[];return x.Children.forEach(t,i=>{x.isValidElement(i)&&a.push(i)}),a}const ld=typeof window<"u",nr=ld?x.useLayoutEffect:x.useEffect,cd=({children:t,custom:a,initial:i=!0,onExitComplete:s,presenceAffectsLayout:r=!0,mode:c="sync",propagate:h=!1})=>{const[p,m]=cg(h),f=x.useMemo(()=>Zp(t),[t]),y=h&&!p?[]:f.map(Cs),v=x.useRef(!0),b=x.useRef(f),T=ro(()=>new Map),[M,I]=x.useState(f),[A,R]=x.useState(f);nr(()=>{v.current=!1,b.current=f;for(let B=0;B<A.length;B++){const N=Cs(A[B]);y.includes(N)?T.delete(N):T.get(N)!==!0&&T.set(N,!1)}},[A,y.length,y.join("-")]);const j=[];if(f!==M){let B=[...f];for(let N=0;N<A.length;N++){const _=A[N],ae=Cs(_);y.includes(ae)||(B.splice(N,0,_),j.push(_))}c==="wait"&&j.length&&(B=j),R(Zp(B)),I(f);return}const{forceRender:V}=x.useContext(rd);return g.jsx(g.Fragment,{children:A.map(B=>{const N=Cs(B),_=h&&!p?!1:f===A||y.includes(N),ae=()=>{if(T.has(N))T.set(N,!0);else return;let G=!0;T.forEach(ce=>{ce||(G=!1)}),G&&(V==null||V(),R(b.current),h&&(m==null||m()),s&&s())};return g.jsx(uC,{isPresent:_,initial:!v.current||i?void 0:!1,custom:_?void 0:a,presenceAffectsLayout:r,mode:c,onExitComplete:_?void 0:ae,children:B},N)})})},rt=t=>t;let mC=rt,dg=rt;function dd(t){let a;return()=>(a===void 0&&(a=t()),a)}const ia=(t,a,i)=>{const s=a-t;return s===0?1:(i-t)/s},dn=t=>t*1e3,hn=t=>t/1e3,fC={useManualTiming:!1};function gC(t){let a=new Set,i=new Set,s=!1,r=!1;const c=new WeakSet;let h={delta:0,timestamp:0,isProcessing:!1};function p(f){c.has(f)&&(m.schedule(f),t()),f(h)}const m={schedule:(f,y=!1,v=!1)=>{const T=v&&s?a:i;return y&&c.add(f),T.has(f)||T.add(f),f},cancel:f=>{i.delete(f),c.delete(f)},process:f=>{if(h=f,s){r=!0;return}s=!0,[a,i]=[i,a],a.forEach(p),a.clear(),s=!1,r&&(r=!1,m.process(f))}};return m}const Ms=["read","resolveKeyframes","update","preRender","render","postRender"],yC=40;function hg(t,a){let i=!1,s=!0;const r={delta:0,timestamp:0,isProcessing:!1},c=()=>i=!0,h=Ms.reduce((R,j)=>(R[j]=gC(c),R),{}),{read:p,resolveKeyframes:m,update:f,preRender:y,render:v,postRender:b}=h,T=()=>{const R=performance.now();i=!1,r.delta=s?1e3/60:Math.max(Math.min(R-r.timestamp,yC),1),r.timestamp=R,r.isProcessing=!0,p.process(r),m.process(r),f.process(r),y.process(r),v.process(r),b.process(r),r.isProcessing=!1,i&&a&&(s=!1,t(T))},M=()=>{i=!0,s=!0,r.isProcessing||t(T)};return{schedule:Ms.reduce((R,j)=>{const V=h[j];return R[j]=(B,N=!1,_=!1)=>(i||M(),V.schedule(B,N,_)),R},{}),cancel:R=>{for(let j=0;j<Ms.length;j++)h[Ms[j]].cancel(R)},state:r,steps:h}}const{schedule:fe,cancel:Qt,state:Be,steps:uc}=hg(typeof requestAnimationFrame<"u"?requestAnimationFrame:rt,!0),ug=x.createContext({strict:!1}),em={animation:["animate","variants","whileHover","whileTap","exit","whileInView","whileFocus","whileDrag"],exit:["exit"],drag:["drag","dragControls"],focus:["whileFocus"],hover:["whileHover","onHoverStart","onHoverEnd"],tap:["whileTap","onTap","onTapStart","onTapCancel"],pan:["onPan","onPanStart","onPanSessionStart","onPanEnd"],inView:["whileInView","onViewportEnter","onViewportLeave"],layout:["layout","layoutId"]},Na={};for(const t in em)Na[t]={isEnabled:a=>em[t].some(i=>!!a[i])};function wC(t){for(const a in t)Na[a]={...Na[a],...t[a]}}const vC=new Set(["animate","exit","variants","initial","style","values","variants","transition","transformTemplate","custom","inherit","onBeforeLayoutMeasure","onAnimationStart","onAnimationComplete","onUpdate","onDragStart","onDrag","onDragEnd","onMeasureDragConstraints","onDirectionLock","onDragTransitionEnd","_dragX","_dragY","onHoverStart","onHoverEnd","onViewportEnter","onViewportLeave","globalTapTarget","ignoreStrict","viewport"]);function Ws(t){return t.startsWith("while")||t.startsWith("drag")&&t!=="draggable"||t.startsWith("layout")||t.startsWith("onTap")||t.startsWith("onPan")||t.startsWith("onLayout")||vC.has(t)}let pg=t=>!Ws(t);function bC(t){t&&(pg=a=>a.startsWith("on")?!Ws(a):t(a))}try{bC(require("@emotion/is-prop-valid").default)}catch{}function kC(t,a,i){const s={};for(const r in t)r==="values"&&typeof t.values=="object"||(pg(r)||i===!0&&Ws(r)||!a&&!Ws(r)||t.draggable&&r.startsWith("onDrag"))&&(s[r]=t[r]);return s}function xC(t){if(typeof Proxy>"u")return t;const a=new Map,i=(...s)=>t(...s);return new Proxy(i,{get:(s,r)=>r==="create"?t:(a.has(r)||a.set(r,t(r)),a.get(r))})}const ar=x.createContext({});function Zi(t){return typeof t=="string"||Array.isArray(t)}function ir(t){return t!==null&&typeof t=="object"&&typeof t.start=="function"}const hd=["animate","whileInView","whileFocus","whileHover","whileTap","whileDrag","exit"],ud=["initial",...hd];function or(t){return ir(t.animate)||ud.some(a=>Zi(t[a]))}function mg(t){return!!(or(t)||t.variants)}function SC(t,a){if(or(t)){const{initial:i,animate:s}=t;return{initial:i===!1||Zi(i)?i:void 0,animate:Zi(s)?s:void 0}}return t.inherit!==!1?a:{}}function TC(t){const{initial:a,animate:i}=SC(t,x.useContext(ar));return x.useMemo(()=>({initial:a,animate:i}),[tm(a),tm(i)])}function tm(t){return Array.isArray(t)?t.join(" "):t}const CC=Symbol.for("motionComponentSymbol");function Da(t){return t&&typeof t=="object"&&Object.prototype.hasOwnProperty.call(t,"current")}function MC(t,a,i){return x.useCallback(s=>{s&&t.onMount&&t.onMount(s),a&&(s?a.mount(s):a.unmount()),i&&(typeof i=="function"?i(s):Da(i)&&(i.current=s))},[a])}const pd=t=>t.replace(/([a-z])([A-Z])/gu,"$1-$2").toLowerCase(),IC="framerAppearId",fg="data-"+pd(IC),{schedule:md}=hg(queueMicrotask,!1),gg=x.createContext({});function AC(t,a,i,s,r){var c,h;const{visualElement:p}=x.useContext(ar),m=x.useContext(ug),f=x.useContext(tr),y=x.useContext(lo).reducedMotion,v=x.useRef(null);s=s||m.renderer,!v.current&&s&&(v.current=s(t,{visualState:a,parent:p,props:i,presenceContext:f,blockInitialAnimation:f?f.initial===!1:!1,reducedMotionConfig:y}));const b=v.current,T=x.useContext(gg);b&&!b.projection&&r&&(b.type==="html"||b.type==="svg")&&EC(v.current,i,r,T);const M=x.useRef(!1);x.useInsertionEffect(()=>{b&&M.current&&b.update(i,f)});const I=i[fg],A=x.useRef(!!I&&!(!((c=window.MotionHandoffIsComplete)===null||c===void 0)&&c.call(window,I))&&((h=window.MotionHasOptimisedAnimation)===null||h===void 0?void 0:h.call(window,I)));return nr(()=>{b&&(M.current=!0,window.MotionIsMounted=!0,b.updateFeatures(),md.render(b.render),A.current&&b.animationState&&b.animationState.animateChanges())}),x.useEffect(()=>{b&&(!A.current&&b.animationState&&b.animationState.animateChanges(),A.current&&(queueMicrotask(()=>{var R;(R=window.MotionHandoffMarkAsComplete)===null||R===void 0||R.call(window,I)}),A.current=!1))}),b}function EC(t,a,i,s){const{layoutId:r,layout:c,drag:h,dragConstraints:p,layoutScroll:m,layoutRoot:f}=a;t.projection=new i(t.latestValues,a["data-framer-portal-id"]?void 0:yg(t.parent)),t.projection.setOptions({layoutId:r,layout:c,alwaysMeasureLayout:!!h||p&&Da(p),visualElement:t,animationType:typeof c=="string"?c:"both",initialPromotionConfig:s,layoutScroll:m,layoutRoot:f})}function yg(t){if(t)return t.options.allowProjection!==!1?t.projection:yg(t.parent)}function PC({preloadedFeatures:t,createVisualElement:a,useRender:i,useVisualState:s,Component:r}){var c,h;t&&wC(t);function p(f,y){let v;const b={...x.useContext(lo),...f,layoutId:RC(f)},{isStatic:T}=b,M=TC(f),I=s(f,T);if(!T&&ld){LC();const A=DC(b);v=A.MeasureLayout,M.visualElement=AC(r,I,b,a,A.ProjectionNode)}return g.jsxs(ar.Provider,{value:M,children:[v&&M.visualElement?g.jsx(v,{visualElement:M.visualElement,...b}):null,i(r,f,MC(I,M.visualElement,y),I,T,M.visualElement)]})}p.displayName=`motion.${typeof r=="string"?r:`create(${(h=(c=r.displayName)!==null&&c!==void 0?c:r.name)!==null&&h!==void 0?h:""})`}`;const m=x.forwardRef(p);return m[CC]=r,m}function RC({layoutId:t}){const a=x.useContext(rd).id;return a&&t!==void 0?a+"-"+t:t}function LC(t,a){x.useContext(ug).strict}function DC(t){const{drag:a,layout:i}=Na;if(!a&&!i)return{};const s={...a,...i};return{MeasureLayout:a!=null&&a.isEnabled(t)||i!=null&&i.isEnabled(t)?s.MeasureLayout:void 0,ProjectionNode:s.ProjectionNode}}const VC=["animate","circle","defs","desc","ellipse","g","image","line","filter","marker","mask","metadata","path","pattern","polygon","polyline","rect","stop","switch","symbol","svg","text","tspan","use","view"];function fd(t){return typeof t!="string"||t.includes("-")?!1:!!(VC.indexOf(t)>-1||/[A-Z]/u.test(t))}function nm(t){const a=[{},{}];return t==null||t.values.forEach((i,s)=>{a[0][s]=i.get(),a[1][s]=i.getVelocity()}),a}function gd(t,a,i,s){if(typeof a=="function"){const[r,c]=nm(s);a=a(i!==void 0?i:t.custom,r,c)}if(typeof a=="string"&&(a=t.variants&&t.variants[a]),typeof a=="function"){const[r,c]=nm(s);a=a(i!==void 0?i:t.custom,r,c)}return a}const Dc=t=>Array.isArray(t),jC=t=>!!(t&&typeof t=="object"&&t.mix&&t.toValue),OC=t=>Dc(t)?t[t.length-1]||0:t,Ye=t=>!!(t&&t.getVelocity);function Vs(t){const a=Ye(t)?t.get():t;return jC(a)?a.toValue():a}function UC({scrapeMotionValuesFromProps:t,createRenderState:a,onUpdate:i},s,r,c){const h={latestValues:FC(s,r,c,t),renderState:a()};return i&&(h.onMount=p=>i({props:s,current:p,...h}),h.onUpdate=p=>i(p)),h}const wg=t=>(a,i)=>{const s=x.useContext(ar),r=x.useContext(tr),c=()=>UC(t,a,s,r);return i?c():ro(c)};function FC(t,a,i,s){const r={},c=s(t,{});for(const b in c)r[b]=Vs(c[b]);let{initial:h,animate:p}=t;const m=or(t),f=mg(t);a&&f&&!m&&t.inherit!==!1&&(h===void 0&&(h=a.initial),p===void 0&&(p=a.animate));let y=i?i.initial===!1:!1;y=y||h===!1;const v=y?p:h;if(v&&typeof v!="boolean"&&!ir(v)){const b=Array.isArray(v)?v:[v];for(let T=0;T<b.length;T++){const M=gd(t,b[T]);if(M){const{transitionEnd:I,transition:A,...R}=M;for(const j in R){let V=R[j];if(Array.isArray(V)){const B=y?V.length-1:0;V=V[B]}V!==null&&(r[j]=V)}for(const j in I)r[j]=I[j]}}}return r}const Ka=["transformPerspective","x","y","z","translateX","translateY","translateZ","scale","scaleX","scaleY","rotate","rotateX","rotateY","rotateZ","skew","skewX","skewY"],oa=new Set(Ka),vg=t=>a=>typeof a=="string"&&a.startsWith(t),bg=vg("--"),NC=vg("var(--"),yd=t=>NC(t)?qC.test(t.split("/*")[0].trim()):!1,qC=/var\(--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)$/iu,kg=(t,a)=>a&&typeof t=="number"?a.transform(t):t,Xt=(t,a,i)=>i>a?a:i<t?t:i,Ja={test:t=>typeof t=="number",parse:parseFloat,transform:t=>t},eo={...Ja,transform:t=>Xt(0,1,t)},Is={...Ja,default:1},co=t=>({test:a=>typeof a=="string"&&a.endsWith(t)&&a.split(" ").length===1,parse:parseFloat,transform:a=>`${a}${t}`}),Vn=co("deg"),$t=co("%"),ne=co("px"),WC=co("vh"),zC=co("vw"),am={...$t,parse:t=>$t.parse(t)/100,transform:t=>$t.transform(t*100)},BC={borderWidth:ne,borderTopWidth:ne,borderRightWidth:ne,borderBottomWidth:ne,borderLeftWidth:ne,borderRadius:ne,radius:ne,borderTopLeftRadius:ne,borderTopRightRadius:ne,borderBottomRightRadius:ne,borderBottomLeftRadius:ne,width:ne,maxWidth:ne,height:ne,maxHeight:ne,top:ne,right:ne,bottom:ne,left:ne,padding:ne,paddingTop:ne,paddingRight:ne,paddingBottom:ne,paddingLeft:ne,margin:ne,marginTop:ne,marginRight:ne,marginBottom:ne,marginLeft:ne,backgroundPositionX:ne,backgroundPositionY:ne},HC={rotate:Vn,rotateX:Vn,rotateY:Vn,rotateZ:Vn,scale:Is,scaleX:Is,scaleY:Is,scaleZ:Is,skew:Vn,skewX:Vn,skewY:Vn,distance:ne,translateX:ne,translateY:ne,translateZ:ne,x:ne,y:ne,z:ne,perspective:ne,transformPerspective:ne,opacity:eo,originX:am,originY:am,originZ:ne},im={...Ja,transform:Math.round},wd={...BC,...HC,zIndex:im,size:ne,fillOpacity:eo,strokeOpacity:eo,numOctaves:im},_C={x:"translateX",y:"translateY",z:"translateZ",transformPerspective:"perspective"},KC=Ka.length;function JC(t,a,i){let s="",r=!0;for(let c=0;c<KC;c++){const h=Ka[c],p=t[h];if(p===void 0)continue;let m=!0;if(typeof p=="number"?m=p===(h.startsWith("scale")?1:0):m=parseFloat(p)===0,!m||i){const f=kg(p,wd[h]);if(!m){r=!1;const y=_C[h]||h;s+=`${y}(${f}) `}i&&(a[h]=f)}}return s=s.trim(),i?s=i(a,r?"":s):r&&(s="none"),s}function vd(t,a,i){const{style:s,vars:r,transformOrigin:c}=t;let h=!1,p=!1;for(const m in a){const f=a[m];if(oa.has(m)){h=!0;continue}else if(bg(m)){r[m]=f;continue}else{const y=kg(f,wd[m]);m.startsWith("origin")?(p=!0,c[m]=y):s[m]=y}}if(a.transform||(h||i?s.transform=JC(a,t.transform,i):s.transform&&(s.transform="none")),p){const{originX:m="50%",originY:f="50%",originZ:y=0}=c;s.transformOrigin=`${m} ${f} ${y}`}}const GC={offset:"stroke-dashoffset",array:"stroke-dasharray"},$C={offset:"strokeDashoffset",array:"strokeDasharray"};function YC(t,a,i=1,s=0,r=!0){t.pathLength=1;const c=r?GC:$C;t[c.offset]=ne.transform(-s);const h=ne.transform(a),p=ne.transform(i);t[c.array]=`${h} ${p}`}function om(t,a,i){return typeof t=="string"?t:ne.transform(a+i*t)}function QC(t,a,i){const s=om(a,t.x,t.width),r=om(i,t.y,t.height);return`${s} ${r}`}function bd(t,{attrX:a,attrY:i,attrScale:s,originX:r,originY:c,pathLength:h,pathSpacing:p=1,pathOffset:m=0,...f},y,v){if(vd(t,f,v),y){t.style.viewBox&&(t.attrs.viewBox=t.style.viewBox);return}t.attrs=t.style,t.style={};const{attrs:b,style:T,dimensions:M}=t;b.transform&&(M&&(T.transform=b.transform),delete b.transform),M&&(r!==void 0||c!==void 0||T.transform)&&(T.transformOrigin=QC(M,r!==void 0?r:.5,c!==void 0?c:.5)),a!==void 0&&(b.x=a),i!==void 0&&(b.y=i),s!==void 0&&(b.scale=s),h!==void 0&&YC(b,h,p,m,!1)}const kd=()=>({style:{},transform:{},transformOrigin:{},vars:{}}),xg=()=>({...kd(),attrs:{}}),xd=t=>typeof t=="string"&&t.toLowerCase()==="svg";function Sg(t,{style:a,vars:i},s,r){Object.assign(t.style,a,r&&r.getProjectionStyles(s));for(const c in i)t.style.setProperty(c,i[c])}const Tg=new Set(["baseFrequency","diffuseConstant","kernelMatrix","kernelUnitLength","keySplines","keyTimes","limitingConeAngle","markerHeight","markerWidth","numOctaves","targetX","targetY","surfaceScale","specularConstant","specularExponent","stdDeviation","tableValues","viewBox","gradientTransform","pathLength","startOffset","textLength","lengthAdjust"]);function Cg(t,a,i,s){Sg(t,a,void 0,s);for(const r in a.attrs)t.setAttribute(Tg.has(r)?r:pd(r),a.attrs[r])}const zs={};function XC(t){Object.assign(zs,t)}function Mg(t,{layout:a,layoutId:i}){return oa.has(t)||t.startsWith("origin")||(a||i!==void 0)&&(!!zs[t]||t==="opacity")}function Sd(t,a,i){var s;const{style:r}=t,c={};for(const h in r)(Ye(r[h])||a.style&&Ye(a.style[h])||Mg(h,t)||((s=i==null?void 0:i.getValue(h))===null||s===void 0?void 0:s.liveStyle)!==void 0)&&(c[h]=r[h]);return c}function Ig(t,a,i){const s=Sd(t,a,i);for(const r in t)if(Ye(t[r])||Ye(a[r])){const c=Ka.indexOf(r)!==-1?"attr"+r.charAt(0).toUpperCase()+r.substring(1):r;s[c]=t[r]}return s}function ZC(t,a){try{a.dimensions=typeof t.getBBox=="function"?t.getBBox():t.getBoundingClientRect()}catch{a.dimensions={x:0,y:0,width:0,height:0}}}const sm=["x","y","width","height","cx","cy","r"],e0={useVisualState:wg({scrapeMotionValuesFromProps:Ig,createRenderState:xg,onUpdate:({props:t,prevProps:a,current:i,renderState:s,latestValues:r})=>{if(!i)return;let c=!!t.drag;if(!c){for(const p in r)if(oa.has(p)){c=!0;break}}if(!c)return;let h=!a;if(a)for(let p=0;p<sm.length;p++){const m=sm[p];t[m]!==a[m]&&(h=!0)}h&&fe.read(()=>{ZC(i,s),fe.render(()=>{bd(s,r,xd(i.tagName),t.transformTemplate),Cg(i,s)})})}})},t0={useVisualState:wg({scrapeMotionValuesFromProps:Sd,createRenderState:kd})};function Ag(t,a,i){for(const s in a)!Ye(a[s])&&!Mg(s,i)&&(t[s]=a[s])}function n0({transformTemplate:t},a){return x.useMemo(()=>{const i=kd();return vd(i,a,t),Object.assign({},i.vars,i.style)},[a])}function a0(t,a){const i=t.style||{},s={};return Ag(s,i,t),Object.assign(s,n0(t,a)),s}function i0(t,a){const i={},s=a0(t,a);return t.drag&&t.dragListener!==!1&&(i.draggable=!1,s.userSelect=s.WebkitUserSelect=s.WebkitTouchCallout="none",s.touchAction=t.drag===!0?"none":`pan-${t.drag==="x"?"y":"x"}`),t.tabIndex===void 0&&(t.onTap||t.onTapStart||t.whileTap)&&(i.tabIndex=0),i.style=s,i}function o0(t,a,i,s){const r=x.useMemo(()=>{const c=xg();return bd(c,a,xd(s),t.transformTemplate),{...c.attrs,style:{...c.style}}},[a]);if(t.style){const c={};Ag(c,t.style,t),r.style={...c,...r.style}}return r}function s0(t=!1){return(i,s,r,{latestValues:c},h)=>{const m=(fd(i)?o0:i0)(s,c,h,i),f=kC(s,typeof i=="string",t),y=i!==x.Fragment?{...f,...m,ref:r}:{},{children:v}=s,b=x.useMemo(()=>Ye(v)?v.get():v,[v]);return x.createElement(i,{...y,children:b})}}function r0(t,a){return function(s,{forwardMotionProps:r}={forwardMotionProps:!1}){const h={...fd(s)?e0:t0,preloadedFeatures:t,useRender:s0(r),createVisualElement:a,Component:s};return PC(h)}}function Eg(t,a){if(!Array.isArray(a))return!1;const i=a.length;if(i!==t.length)return!1;for(let s=0;s<i;s++)if(a[s]!==t[s])return!1;return!0}function sr(t,a,i){const s=t.getProps();return gd(s,a,i!==void 0?i:s.custom,t)}const Pg=dd(()=>window.ScrollTimeline!==void 0);class l0{constructor(a){this.stop=()=>this.runAll("stop"),this.animations=a.filter(Boolean)}get finished(){return Promise.all(this.animations.map(a=>"finished"in a?a.finished:a))}getAll(a){return this.animations[0][a]}setAll(a,i){for(let s=0;s<this.animations.length;s++)this.animations[s][a]=i}attachTimeline(a,i){const s=this.animations.map(r=>{if(Pg()&&r.attachTimeline)return r.attachTimeline(a);if(typeof i=="function")return i(r)});return()=>{s.forEach((r,c)=>{r&&r(),this.animations[c].stop()})}}get time(){return this.getAll("time")}set time(a){this.setAll("time",a)}get speed(){return this.getAll("speed")}set speed(a){this.setAll("speed",a)}get startTime(){return this.getAll("startTime")}get duration(){let a=0;for(let i=0;i<this.animations.length;i++)a=Math.max(a,this.animations[i].duration);return a}runAll(a){this.animations.forEach(i=>i[a]())}flatten(){this.runAll("flatten")}play(){this.runAll("play")}pause(){this.runAll("pause")}cancel(){this.runAll("cancel")}complete(){this.runAll("complete")}}class c0 extends l0{then(a,i){return Promise.all(this.animations).then(a).catch(i)}}function Td(t,a){return t?t[a]||t.default||t:void 0}const Vc=2e4;function Rg(t){let a=0;const i=50;let s=t.next(a);for(;!s.done&&a<Vc;)a+=i,s=t.next(a);return a>=Vc?1/0:a}function Cd(t){return typeof t=="function"}function rm(t,a){t.timeline=a,t.onfinish=null}const Md=t=>Array.isArray(t)&&typeof t[0]=="number",d0={linearEasing:void 0};function h0(t,a){const i=dd(t);return()=>{var s;return(s=d0[a])!==null&&s!==void 0?s:i()}}const Bs=h0(()=>{try{document.createElement("div").animate({opacity:0},{easing:"linear(0, 1)"})}catch{return!1}return!0},"linearEasing"),Lg=(t,a,i=10)=>{let s="";const r=Math.max(Math.round(a/i),2);for(let c=0;c<r;c++)s+=t(ia(0,r-1,c))+", ";return`linear(${s.substring(0,s.length-2)})`};function Dg(t){return!!(typeof t=="function"&&Bs()||!t||typeof t=="string"&&(t in jc||Bs())||Md(t)||Array.isArray(t)&&t.every(Dg))}const Bi=([t,a,i,s])=>`cubic-bezier(${t}, ${a}, ${i}, ${s})`,jc={linear:"linear",ease:"ease",easeIn:"ease-in",easeOut:"ease-out",easeInOut:"ease-in-out",circIn:Bi([0,.65,.55,1]),circOut:Bi([.55,0,1,.45]),backIn:Bi([.31,.01,.66,-.59]),backOut:Bi([.33,1.53,.69,.99])};function Vg(t,a){if(t)return typeof t=="function"&&Bs()?Lg(t,a):Md(t)?Bi(t):Array.isArray(t)?t.map(i=>Vg(i,a)||jc.easeOut):jc[t]}const Nt={x:!1,y:!1};function jg(){return Nt.x||Nt.y}function Id(t,a,i){var s;if(t instanceof Element)return[t];if(typeof t=="string"){let r=document;const c=(s=void 0)!==null&&s!==void 0?s:r.querySelectorAll(t);return c?Array.from(c):[]}return Array.from(t)}function Og(t,a){const i=Id(t),s=new AbortController,r={passive:!0,...a,signal:s.signal};return[i,r,()=>s.abort()]}function lm(t){return a=>{a.pointerType==="touch"||jg()||t(a)}}function u0(t,a,i={}){const[s,r,c]=Og(t,i),h=lm(p=>{const{target:m}=p,f=a(p);if(typeof f!="function"||!m)return;const y=lm(v=>{f(v),m.removeEventListener("pointerleave",y)});m.addEventListener("pointerleave",y,r)});return s.forEach(p=>{p.addEventListener("pointerenter",h,r)}),c}const Ug=(t,a)=>a?t===a?!0:Ug(t,a.parentElement):!1,Ad=t=>t.pointerType==="mouse"?typeof t.button!="number"||t.button<=0:t.isPrimary!==!1,p0=new Set(["BUTTON","INPUT","SELECT","TEXTAREA","A"]);function m0(t){return p0.has(t.tagName)||t.tabIndex!==-1}const Hi=new WeakSet;function cm(t){return a=>{a.key==="Enter"&&t(a)}}function pc(t,a){t.dispatchEvent(new PointerEvent("pointer"+a,{isPrimary:!0,bubbles:!0}))}const f0=(t,a)=>{const i=t.currentTarget;if(!i)return;const s=cm(()=>{if(Hi.has(i))return;pc(i,"down");const r=cm(()=>{pc(i,"up")}),c=()=>pc(i,"cancel");i.addEventListener("keyup",r,a),i.addEventListener("blur",c,a)});i.addEventListener("keydown",s,a),i.addEventListener("blur",()=>i.removeEventListener("keydown",s),a)};function dm(t){return Ad(t)&&!jg()}function g0(t,a,i={}){const[s,r,c]=Og(t,i),h=p=>{const m=p.currentTarget;if(!dm(p)||Hi.has(m))return;Hi.add(m);const f=a(p),y=(T,M)=>{window.removeEventListener("pointerup",v),window.removeEventListener("pointercancel",b),!(!dm(T)||!Hi.has(m))&&(Hi.delete(m),typeof f=="function"&&f(T,{success:M}))},v=T=>{y(T,i.useGlobalTarget||Ug(m,T.target))},b=T=>{y(T,!1)};window.addEventListener("pointerup",v,r),window.addEventListener("pointercancel",b,r)};return s.forEach(p=>{!m0(p)&&p.getAttribute("tabindex")===null&&(p.tabIndex=0),(i.useGlobalTarget?window:p).addEventListener("pointerdown",h,r),p.addEventListener("focus",f=>f0(f,r),r)}),c}function y0(t){return t==="x"||t==="y"?Nt[t]?null:(Nt[t]=!0,()=>{Nt[t]=!1}):Nt.x||Nt.y?null:(Nt.x=Nt.y=!0,()=>{Nt.x=Nt.y=!1})}const Fg=new Set(["width","height","top","left","right","bottom",...Ka]);let js;function w0(){js=void 0}const Yt={now:()=>(js===void 0&&Yt.set(Be.isProcessing||fC.useManualTiming?Be.timestamp:performance.now()),js),set:t=>{js=t,queueMicrotask(w0)}};function Ed(t,a){t.indexOf(a)===-1&&t.push(a)}function Pd(t,a){const i=t.indexOf(a);i>-1&&t.splice(i,1)}class Rd{constructor(){this.subscriptions=[]}add(a){return Ed(this.subscriptions,a),()=>Pd(this.subscriptions,a)}notify(a,i,s){const r=this.subscriptions.length;if(r)if(r===1)this.subscriptions[0](a,i,s);else for(let c=0;c<r;c++){const h=this.subscriptions[c];h&&h(a,i,s)}}getSize(){return this.subscriptions.length}clear(){this.subscriptions.length=0}}function Ld(t,a){return a?t*(1e3/a):0}const hm=30,v0=t=>!isNaN(parseFloat(t));class b0{constructor(a,i={}){this.version="11.18.2",this.canTrackVelocity=null,this.events={},this.updateAndNotify=(s,r=!0)=>{const c=Yt.now();this.updatedAt!==c&&this.setPrevFrameValue(),this.prev=this.current,this.setCurrent(s),this.current!==this.prev&&this.events.change&&this.events.change.notify(this.current),r&&this.events.renderRequest&&this.events.renderRequest.notify(this.current)},this.hasAnimated=!1,this.setCurrent(a),this.owner=i.owner}setCurrent(a){this.current=a,this.updatedAt=Yt.now(),this.canTrackVelocity===null&&a!==void 0&&(this.canTrackVelocity=v0(this.current))}setPrevFrameValue(a=this.current){this.prevFrameValue=a,this.prevUpdatedAt=this.updatedAt}onChange(a){return this.on("change",a)}on(a,i){this.events[a]||(this.events[a]=new Rd);const s=this.events[a].add(i);return a==="change"?()=>{s(),fe.read(()=>{this.events.change.getSize()||this.stop()})}:s}clearListeners(){for(const a in this.events)this.events[a].clear()}attach(a,i){this.passiveEffect=a,this.stopPassiveEffect=i}set(a,i=!0){!i||!this.passiveEffect?this.updateAndNotify(a,i):this.passiveEffect(a,this.updateAndNotify)}setWithVelocity(a,i,s){this.set(i),this.prev=void 0,this.prevFrameValue=a,this.prevUpdatedAt=this.updatedAt-s}jump(a,i=!0){this.updateAndNotify(a),this.prev=a,this.prevUpdatedAt=this.prevFrameValue=void 0,i&&this.stop(),this.stopPassiveEffect&&this.stopPassiveEffect()}get(){return this.current}getPrevious(){return this.prev}getVelocity(){const a=Yt.now();if(!this.canTrackVelocity||this.prevFrameValue===void 0||a-this.updatedAt>hm)return 0;const i=Math.min(this.updatedAt-this.prevUpdatedAt,hm);return Ld(parseFloat(this.current)-parseFloat(this.prevFrameValue),i)}start(a){return this.stop(),new Promise(i=>{this.hasAnimated=!0,this.animation=a(i),this.events.animationStart&&this.events.animationStart.notify()}).then(()=>{this.events.animationComplete&&this.events.animationComplete.notify(),this.clearAnimation()})}stop(){this.animation&&(this.animation.stop(),this.events.animationCancel&&this.events.animationCancel.notify()),this.clearAnimation()}isAnimating(){return!!this.animation}clearAnimation(){delete this.animation}destroy(){this.clearListeners(),this.stop(),this.stopPassiveEffect&&this.stopPassiveEffect()}}function Gt(t,a){return new b0(t,a)}function k0(t,a,i){t.hasValue(a)?t.getValue(a).set(i):t.addValue(a,Gt(i))}function x0(t,a){const i=sr(t,a);let{transitionEnd:s={},transition:r={},...c}=i||{};c={...c,...s};for(const h in c){const p=OC(c[h]);k0(t,h,p)}}function S0(t){return!!(Ye(t)&&t.add)}function Oc(t,a){const i=t.getValue("willChange");if(S0(i))return i.add(a)}function Ng(t){return t.props[fg]}const qg=(t,a,i)=>(((1-3*i+3*a)*t+(3*i-6*a))*t+3*a)*t,T0=1e-7,C0=12;function M0(t,a,i,s,r){let c,h,p=0;do h=a+(i-a)/2,c=qg(h,s,r)-t,c>0?i=h:a=h;while(Math.abs(c)>T0&&++p<C0);return h}function ho(t,a,i,s){if(t===a&&i===s)return rt;const r=c=>M0(c,0,1,t,i);return c=>c===0||c===1?c:qg(r(c),a,s)}const Wg=t=>a=>a<=.5?t(2*a)/2:(2-t(2*(1-a)))/2,zg=t=>a=>1-t(1-a),Bg=ho(.33,1.53,.69,.99),Dd=zg(Bg),Hg=Wg(Dd),_g=t=>(t*=2)<1?.5*Dd(t):.5*(2-Math.pow(2,-10*(t-1))),Vd=t=>1-Math.sin(Math.acos(t)),Kg=zg(Vd),Jg=Wg(Vd),Gg=t=>/^0[^.\s]+$/u.test(t);function I0(t){return typeof t=="number"?t===0:t!==null?t==="none"||t==="0"||Gg(t):!0}const Ki=t=>Math.round(t*1e5)/1e5,jd=/-?(?:\d+(?:\.\d+)?|\.\d+)/gu;function A0(t){return t==null}const E0=/^(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))$/iu,Od=(t,a)=>i=>!!(typeof i=="string"&&E0.test(i)&&i.startsWith(t)||a&&!A0(i)&&Object.prototype.hasOwnProperty.call(i,a)),$g=(t,a,i)=>s=>{if(typeof s!="string")return s;const[r,c,h,p]=s.match(jd);return{[t]:parseFloat(r),[a]:parseFloat(c),[i]:parseFloat(h),alpha:p!==void 0?parseFloat(p):1}},P0=t=>Xt(0,255,t),mc={...Ja,transform:t=>Math.round(P0(t))},na={test:Od("rgb","red"),parse:$g("red","green","blue"),transform:({red:t,green:a,blue:i,alpha:s=1})=>"rgba("+mc.transform(t)+", "+mc.transform(a)+", "+mc.transform(i)+", "+Ki(eo.transform(s))+")"};function R0(t){let a="",i="",s="",r="";return t.length>5?(a=t.substring(1,3),i=t.substring(3,5),s=t.substring(5,7),r=t.substring(7,9)):(a=t.substring(1,2),i=t.substring(2,3),s=t.substring(3,4),r=t.substring(4,5),a+=a,i+=i,s+=s,r+=r),{red:parseInt(a,16),green:parseInt(i,16),blue:parseInt(s,16),alpha:r?parseInt(r,16)/255:1}}const Uc={test:Od("#"),parse:R0,transform:na.transform},Va={test:Od("hsl","hue"),parse:$g("hue","saturation","lightness"),transform:({hue:t,saturation:a,lightness:i,alpha:s=1})=>"hsla("+Math.round(t)+", "+$t.transform(Ki(a))+", "+$t.transform(Ki(i))+", "+Ki(eo.transform(s))+")"},at={test:t=>na.test(t)||Uc.test(t)||Va.test(t),parse:t=>na.test(t)?na.parse(t):Va.test(t)?Va.parse(t):Uc.parse(t),transform:t=>typeof t=="string"?t:t.hasOwnProperty("red")?na.transform(t):Va.transform(t)},L0=/(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))/giu;function D0(t){var a,i;return isNaN(t)&&typeof t=="string"&&(((a=t.match(jd))===null||a===void 0?void 0:a.length)||0)+(((i=t.match(L0))===null||i===void 0?void 0:i.length)||0)>0}const Yg="number",Qg="color",V0="var",j0="var(",um="${}",O0=/var\s*\(\s*--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)|#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\)|-?(?:\d+(?:\.\d+)?|\.\d+)/giu;function to(t){const a=t.toString(),i=[],s={color:[],number:[],var:[]},r=[];let c=0;const p=a.replace(O0,m=>(at.test(m)?(s.color.push(c),r.push(Qg),i.push(at.parse(m))):m.startsWith(j0)?(s.var.push(c),r.push(V0),i.push(m)):(s.number.push(c),r.push(Yg),i.push(parseFloat(m))),++c,um)).split(um);return{values:i,split:p,indexes:s,types:r}}function Xg(t){return to(t).values}function Zg(t){const{split:a,types:i}=to(t),s=a.length;return r=>{let c="";for(let h=0;h<s;h++)if(c+=a[h],r[h]!==void 0){const p=i[h];p===Yg?c+=Ki(r[h]):p===Qg?c+=at.transform(r[h]):c+=r[h]}return c}}const U0=t=>typeof t=="number"?0:t;function F0(t){const a=Xg(t);return Zg(t)(a.map(U0))}const On={test:D0,parse:Xg,createTransformer:Zg,getAnimatableNone:F0},N0=new Set(["brightness","contrast","saturate","opacity"]);function q0(t){const[a,i]=t.slice(0,-1).split("(");if(a==="drop-shadow")return t;const[s]=i.match(jd)||[];if(!s)return t;const r=i.replace(s,"");let c=N0.has(a)?1:0;return s!==i&&(c*=100),a+"("+c+r+")"}const W0=/\b([a-z-]*)\(.*?\)/gu,Fc={...On,getAnimatableNone:t=>{const a=t.match(W0);return a?a.map(q0).join(" "):t}},z0={...wd,color:at,backgroundColor:at,outlineColor:at,fill:at,stroke:at,borderColor:at,borderTopColor:at,borderRightColor:at,borderBottomColor:at,borderLeftColor:at,filter:Fc,WebkitFilter:Fc},Ud=t=>z0[t];function ey(t,a){let i=Ud(t);return i!==Fc&&(i=On),i.getAnimatableNone?i.getAnimatableNone(a):void 0}const B0=new Set(["auto","none","0"]);function H0(t,a,i){let s=0,r;for(;s<t.length&&!r;){const c=t[s];typeof c=="string"&&!B0.has(c)&&to(c).values.length&&(r=t[s]),s++}if(r&&i)for(const c of a)t[c]=ey(i,r)}const pm=t=>t===Ja||t===ne,mm=(t,a)=>parseFloat(t.split(", ")[a]),fm=(t,a)=>(i,{transform:s})=>{if(s==="none"||!s)return 0;const r=s.match(/^matrix3d\((.+)\)$/u);if(r)return mm(r[1],a);{const c=s.match(/^matrix\((.+)\)$/u);return c?mm(c[1],t):0}},_0=new Set(["x","y","z"]),K0=Ka.filter(t=>!_0.has(t));function J0(t){const a=[];return K0.forEach(i=>{const s=t.getValue(i);s!==void 0&&(a.push([i,s.get()]),s.set(i.startsWith("scale")?1:0))}),a}const qa={width:({x:t},{paddingLeft:a="0",paddingRight:i="0"})=>t.max-t.min-parseFloat(a)-parseFloat(i),height:({y:t},{paddingTop:a="0",paddingBottom:i="0"})=>t.max-t.min-parseFloat(a)-parseFloat(i),top:(t,{top:a})=>parseFloat(a),left:(t,{left:a})=>parseFloat(a),bottom:({y:t},{top:a})=>parseFloat(a)+(t.max-t.min),right:({x:t},{left:a})=>parseFloat(a)+(t.max-t.min),x:fm(4,13),y:fm(5,14)};qa.translateX=qa.x;qa.translateY=qa.y;const aa=new Set;let Nc=!1,qc=!1;function ty(){if(qc){const t=Array.from(aa).filter(s=>s.needsMeasurement),a=new Set(t.map(s=>s.element)),i=new Map;a.forEach(s=>{const r=J0(s);r.length&&(i.set(s,r),s.render())}),t.forEach(s=>s.measureInitialState()),a.forEach(s=>{s.render();const r=i.get(s);r&&r.forEach(([c,h])=>{var p;(p=s.getValue(c))===null||p===void 0||p.set(h)})}),t.forEach(s=>s.measureEndState()),t.forEach(s=>{s.suspendedScrollY!==void 0&&window.scrollTo(0,s.suspendedScrollY)})}qc=!1,Nc=!1,aa.forEach(t=>t.complete()),aa.clear()}function ny(){aa.forEach(t=>{t.readKeyframes(),t.needsMeasurement&&(qc=!0)})}function G0(){ny(),ty()}class Fd{constructor(a,i,s,r,c,h=!1){this.isComplete=!1,this.isAsync=!1,this.needsMeasurement=!1,this.isScheduled=!1,this.unresolvedKeyframes=[...a],this.onComplete=i,this.name=s,this.motionValue=r,this.element=c,this.isAsync=h}scheduleResolve(){this.isScheduled=!0,this.isAsync?(aa.add(this),Nc||(Nc=!0,fe.read(ny),fe.resolveKeyframes(ty))):(this.readKeyframes(),this.complete())}readKeyframes(){const{unresolvedKeyframes:a,name:i,element:s,motionValue:r}=this;for(let c=0;c<a.length;c++)if(a[c]===null)if(c===0){const h=r==null?void 0:r.get(),p=a[a.length-1];if(h!==void 0)a[0]=h;else if(s&&i){const m=s.readValue(i,p);m!=null&&(a[0]=m)}a[0]===void 0&&(a[0]=p),r&&h===void 0&&r.set(a[0])}else a[c]=a[c-1]}setFinalKeyframe(){}measureInitialState(){}renderEndStyles(){}measureEndState(){}complete(){this.isComplete=!0,this.onComplete(this.unresolvedKeyframes,this.finalKeyframe),aa.delete(this)}cancel(){this.isComplete||(this.isScheduled=!1,aa.delete(this))}resume(){this.isComplete||this.scheduleResolve()}}const ay=t=>/^-?(?:\d+(?:\.\d+)?|\.\d+)$/u.test(t),$0=/^var\(--(?:([\w-]+)|([\w-]+), ?([a-zA-Z\d ()%#.,-]+))\)/u;function Y0(t){const a=$0.exec(t);if(!a)return[,];const[,i,s,r]=a;return[`--${i??s}`,r]}function iy(t,a,i=1){const[s,r]=Y0(t);if(!s)return;const c=window.getComputedStyle(a).getPropertyValue(s);if(c){const h=c.trim();return ay(h)?parseFloat(h):h}return yd(r)?iy(r,a,i+1):r}const oy=t=>a=>a.test(t),Q0={test:t=>t==="auto",parse:t=>t},sy=[Ja,ne,$t,Vn,zC,WC,Q0],gm=t=>sy.find(oy(t));class ry extends Fd{constructor(a,i,s,r,c){super(a,i,s,r,c,!0)}readKeyframes(){const{unresolvedKeyframes:a,element:i,name:s}=this;if(!i||!i.current)return;super.readKeyframes();for(let m=0;m<a.length;m++){let f=a[m];if(typeof f=="string"&&(f=f.trim(),yd(f))){const y=iy(f,i.current);y!==void 0&&(a[m]=y),m===a.length-1&&(this.finalKeyframe=f)}}if(this.resolveNoneKeyframes(),!Fg.has(s)||a.length!==2)return;const[r,c]=a,h=gm(r),p=gm(c);if(h!==p)if(pm(h)&&pm(p))for(let m=0;m<a.length;m++){const f=a[m];typeof f=="string"&&(a[m]=parseFloat(f))}else this.needsMeasurement=!0}resolveNoneKeyframes(){const{unresolvedKeyframes:a,name:i}=this,s=[];for(let r=0;r<a.length;r++)I0(a[r])&&s.push(r);s.length&&H0(a,s,i)}measureInitialState(){const{element:a,unresolvedKeyframes:i,name:s}=this;if(!a||!a.current)return;s==="height"&&(this.suspendedScrollY=window.pageYOffset),this.measuredOrigin=qa[s](a.measureViewportBox(),window.getComputedStyle(a.current)),i[0]=this.measuredOrigin;const r=i[i.length-1];r!==void 0&&a.getValue(s,r).jump(r,!1)}measureEndState(){var a;const{element:i,name:s,unresolvedKeyframes:r}=this;if(!i||!i.current)return;const c=i.getValue(s);c&&c.jump(this.measuredOrigin,!1);const h=r.length-1,p=r[h];r[h]=qa[s](i.measureViewportBox(),window.getComputedStyle(i.current)),p!==null&&this.finalKeyframe===void 0&&(this.finalKeyframe=p),!((a=this.removedTransforms)===null||a===void 0)&&a.length&&this.removedTransforms.forEach(([m,f])=>{i.getValue(m).set(f)}),this.resolveNoneKeyframes()}}const ym=(t,a)=>a==="zIndex"?!1:!!(typeof t=="number"||Array.isArray(t)||typeof t=="string"&&(On.test(t)||t==="0")&&!t.startsWith("url("));function X0(t){const a=t[0];if(t.length===1)return!0;for(let i=0;i<t.length;i++)if(t[i]!==a)return!0}function Z0(t,a,i,s){const r=t[0];if(r===null)return!1;if(a==="display"||a==="visibility")return!0;const c=t[t.length-1],h=ym(r,a),p=ym(c,a);return!h||!p?!1:X0(t)||(i==="spring"||Cd(i))&&s}const eM=t=>t!==null;function rr(t,{repeat:a,repeatType:i="loop"},s){const r=t.filter(eM),c=a&&i!=="loop"&&a%2===1?0:r.length-1;return!c||s===void 0?r[c]:s}const tM=40;class ly{constructor({autoplay:a=!0,delay:i=0,type:s="keyframes",repeat:r=0,repeatDelay:c=0,repeatType:h="loop",...p}){this.isStopped=!1,this.hasAttemptedResolve=!1,this.createdAt=Yt.now(),this.options={autoplay:a,delay:i,type:s,repeat:r,repeatDelay:c,repeatType:h,...p},this.updateFinishedPromise()}calcStartTime(){return this.resolvedAt?this.resolvedAt-this.createdAt>tM?this.resolvedAt:this.createdAt:this.createdAt}get resolved(){return!this._resolved&&!this.hasAttemptedResolve&&G0(),this._resolved}onKeyframesResolved(a,i){this.resolvedAt=Yt.now(),this.hasAttemptedResolve=!0;const{name:s,type:r,velocity:c,delay:h,onComplete:p,onUpdate:m,isGenerator:f}=this.options;if(!f&&!Z0(a,s,r,c))if(h)this.options.duration=0;else{m&&m(rr(a,this.options,i)),p&&p(),this.resolveFinishedPromise();return}const y=this.initPlayback(a,i);y!==!1&&(this._resolved={keyframes:a,finalKeyframe:i,...y},this.onPostResolved())}onPostResolved(){}then(a,i){return this.currentFinishedPromise.then(a,i)}flatten(){this.options.type="keyframes",this.options.ease="linear"}updateFinishedPromise(){this.currentFinishedPromise=new Promise(a=>{this.resolveFinishedPromise=a})}}const Re=(t,a,i)=>t+(a-t)*i;function fc(t,a,i){return i<0&&(i+=1),i>1&&(i-=1),i<1/6?t+(a-t)*6*i:i<1/2?a:i<2/3?t+(a-t)*(2/3-i)*6:t}function nM({hue:t,saturation:a,lightness:i,alpha:s}){t/=360,a/=100,i/=100;let r=0,c=0,h=0;if(!a)r=c=h=i;else{const p=i<.5?i*(1+a):i+a-i*a,m=2*i-p;r=fc(m,p,t+1/3),c=fc(m,p,t),h=fc(m,p,t-1/3)}return{red:Math.round(r*255),green:Math.round(c*255),blue:Math.round(h*255),alpha:s}}function Hs(t,a){return i=>i>0?a:t}const gc=(t,a,i)=>{const s=t*t,r=i*(a*a-s)+s;return r<0?0:Math.sqrt(r)},aM=[Uc,na,Va],iM=t=>aM.find(a=>a.test(t));function wm(t){const a=iM(t);if(!a)return!1;let i=a.parse(t);return a===Va&&(i=nM(i)),i}const vm=(t,a)=>{const i=wm(t),s=wm(a);if(!i||!s)return Hs(t,a);const r={...i};return c=>(r.red=gc(i.red,s.red,c),r.green=gc(i.green,s.green,c),r.blue=gc(i.blue,s.blue,c),r.alpha=Re(i.alpha,s.alpha,c),na.transform(r))},oM=(t,a)=>i=>a(t(i)),uo=(...t)=>t.reduce(oM),Wc=new Set(["none","hidden"]);function sM(t,a){return Wc.has(t)?i=>i<=0?t:a:i=>i>=1?a:t}function rM(t,a){return i=>Re(t,a,i)}function Nd(t){return typeof t=="number"?rM:typeof t=="string"?yd(t)?Hs:at.test(t)?vm:dM:Array.isArray(t)?cy:typeof t=="object"?at.test(t)?vm:lM:Hs}function cy(t,a){const i=[...t],s=i.length,r=t.map((c,h)=>Nd(c)(c,a[h]));return c=>{for(let h=0;h<s;h++)i[h]=r[h](c);return i}}function lM(t,a){const i={...t,...a},s={};for(const r in i)t[r]!==void 0&&a[r]!==void 0&&(s[r]=Nd(t[r])(t[r],a[r]));return r=>{for(const c in s)i[c]=s[c](r);return i}}function cM(t,a){var i;const s=[],r={color:0,var:0,number:0};for(let c=0;c<a.values.length;c++){const h=a.types[c],p=t.indexes[h][r[h]],m=(i=t.values[p])!==null&&i!==void 0?i:0;s[c]=m,r[h]++}return s}const dM=(t,a)=>{const i=On.createTransformer(a),s=to(t),r=to(a);return s.indexes.var.length===r.indexes.var.length&&s.indexes.color.length===r.indexes.color.length&&s.indexes.number.length>=r.indexes.number.length?Wc.has(t)&&!r.values.length||Wc.has(a)&&!s.values.length?sM(t,a):uo(cy(cM(s,r),r.values),i):Hs(t,a)};function dy(t,a,i){return typeof t=="number"&&typeof a=="number"&&typeof i=="number"?Re(t,a,i):Nd(t)(t,a)}const hM=5;function hy(t,a,i){const s=Math.max(a-hM,0);return Ld(i-t(s),a-s)}const Ve={stiffness:100,damping:10,mass:1,velocity:0,duration:800,bounce:.3,visualDuration:.3,restSpeed:{granular:.01,default:2},restDelta:{granular:.005,default:.5},minDuration:.01,maxDuration:10,minDamping:.05,maxDamping:1},yc=.001;function uM({duration:t=Ve.duration,bounce:a=Ve.bounce,velocity:i=Ve.velocity,mass:s=Ve.mass}){let r,c,h=1-a;h=Xt(Ve.minDamping,Ve.maxDamping,h),t=Xt(Ve.minDuration,Ve.maxDuration,hn(t)),h<1?(r=f=>{const y=f*h,v=y*t,b=y-i,T=zc(f,h),M=Math.exp(-v);return yc-b/T*M},c=f=>{const v=f*h*t,b=v*i+i,T=Math.pow(h,2)*Math.pow(f,2)*t,M=Math.exp(-v),I=zc(Math.pow(f,2),h);return(-r(f)+yc>0?-1:1)*((b-T)*M)/I}):(r=f=>{const y=Math.exp(-f*t),v=(f-i)*t+1;return-yc+y*v},c=f=>{const y=Math.exp(-f*t),v=(i-f)*(t*t);return y*v});const p=5/t,m=mM(r,c,p);if(t=dn(t),isNaN(m))return{stiffness:Ve.stiffness,damping:Ve.damping,duration:t};{const f=Math.pow(m,2)*s;return{stiffness:f,damping:h*2*Math.sqrt(s*f),duration:t}}}const pM=12;function mM(t,a,i){let s=i;for(let r=1;r<pM;r++)s=s-t(s)/a(s);return s}function zc(t,a){return t*Math.sqrt(1-a*a)}const fM=["duration","bounce"],gM=["stiffness","damping","mass"];function bm(t,a){return a.some(i=>t[i]!==void 0)}function yM(t){let a={velocity:Ve.velocity,stiffness:Ve.stiffness,damping:Ve.damping,mass:Ve.mass,isResolvedFromDuration:!1,...t};if(!bm(t,gM)&&bm(t,fM))if(t.visualDuration){const i=t.visualDuration,s=2*Math.PI/(i*1.2),r=s*s,c=2*Xt(.05,1,1-(t.bounce||0))*Math.sqrt(r);a={...a,mass:Ve.mass,stiffness:r,damping:c}}else{const i=uM(t);a={...a,...i,mass:Ve.mass},a.isResolvedFromDuration=!0}return a}function uy(t=Ve.visualDuration,a=Ve.bounce){const i=typeof t!="object"?{visualDuration:t,keyframes:[0,1],bounce:a}:t;let{restSpeed:s,restDelta:r}=i;const c=i.keyframes[0],h=i.keyframes[i.keyframes.length-1],p={done:!1,value:c},{stiffness:m,damping:f,mass:y,duration:v,velocity:b,isResolvedFromDuration:T}=yM({...i,velocity:-hn(i.velocity||0)}),M=b||0,I=f/(2*Math.sqrt(m*y)),A=h-c,R=hn(Math.sqrt(m/y)),j=Math.abs(A)<5;s||(s=j?Ve.restSpeed.granular:Ve.restSpeed.default),r||(r=j?Ve.restDelta.granular:Ve.restDelta.default);let V;if(I<1){const N=zc(R,I);V=_=>{const ae=Math.exp(-I*R*_);return h-ae*((M+I*R*A)/N*Math.sin(N*_)+A*Math.cos(N*_))}}else if(I===1)V=N=>h-Math.exp(-R*N)*(A+(M+R*A)*N);else{const N=R*Math.sqrt(I*I-1);V=_=>{const ae=Math.exp(-I*R*_),G=Math.min(N*_,300);return h-ae*((M+I*R*A)*Math.sinh(G)+N*A*Math.cosh(G))/N}}const B={calculatedDuration:T&&v||null,next:N=>{const _=V(N);if(T)p.done=N>=v;else{let ae=0;I<1&&(ae=N===0?dn(M):hy(V,N,_));const G=Math.abs(ae)<=s,ce=Math.abs(h-_)<=r;p.done=G&&ce}return p.value=p.done?h:_,p},toString:()=>{const N=Math.min(Rg(B),Vc),_=Lg(ae=>B.next(N*ae).value,N,30);return N+"ms "+_}};return B}function km({keyframes:t,velocity:a=0,power:i=.8,timeConstant:s=325,bounceDamping:r=10,bounceStiffness:c=500,modifyTarget:h,min:p,max:m,restDelta:f=.5,restSpeed:y}){const v=t[0],b={done:!1,value:v},T=G=>p!==void 0&&G<p||m!==void 0&&G>m,M=G=>p===void 0?m:m===void 0||Math.abs(p-G)<Math.abs(m-G)?p:m;let I=i*a;const A=v+I,R=h===void 0?A:h(A);R!==A&&(I=R-v);const j=G=>-I*Math.exp(-G/s),V=G=>R+j(G),B=G=>{const ce=j(G),we=V(G);b.done=Math.abs(ce)<=f,b.value=b.done?R:we};let N,_;const ae=G=>{T(b.value)&&(N=G,_=uy({keyframes:[b.value,M(b.value)],velocity:hy(V,G,b.value),damping:r,stiffness:c,restDelta:f,restSpeed:y}))};return ae(0),{calculatedDuration:null,next:G=>{let ce=!1;return!_&&N===void 0&&(ce=!0,B(G),ae(G)),N!==void 0&&G>=N?_.next(G-N):(!ce&&B(G),b)}}}const wM=ho(.42,0,1,1),vM=ho(0,0,.58,1),py=ho(.42,0,.58,1),bM=t=>Array.isArray(t)&&typeof t[0]!="number",kM={linear:rt,easeIn:wM,easeInOut:py,easeOut:vM,circIn:Vd,circInOut:Jg,circOut:Kg,backIn:Dd,backInOut:Hg,backOut:Bg,anticipate:_g},xm=t=>{if(Md(t)){dg(t.length===4);const[a,i,s,r]=t;return ho(a,i,s,r)}else if(typeof t=="string")return kM[t];return t};function xM(t,a,i){const s=[],r=i||dy,c=t.length-1;for(let h=0;h<c;h++){let p=r(t[h],t[h+1]);if(a){const m=Array.isArray(a)?a[h]||rt:a;p=uo(m,p)}s.push(p)}return s}function my(t,a,{clamp:i=!0,ease:s,mixer:r}={}){const c=t.length;if(dg(c===a.length),c===1)return()=>a[0];if(c===2&&a[0]===a[1])return()=>a[1];const h=t[0]===t[1];t[0]>t[c-1]&&(t=[...t].reverse(),a=[...a].reverse());const p=xM(a,s,r),m=p.length,f=y=>{if(h&&y<t[0])return a[0];let v=0;if(m>1)for(;v<t.length-2&&!(y<t[v+1]);v++);const b=ia(t[v],t[v+1],y);return p[v](b)};return i?y=>f(Xt(t[0],t[c-1],y)):f}function SM(t,a){const i=t[t.length-1];for(let s=1;s<=a;s++){const r=ia(0,a,s);t.push(Re(i,1,r))}}function fy(t){const a=[0];return SM(a,t.length-1),a}function TM(t,a){return t.map(i=>i*a)}function CM(t,a){return t.map(()=>a||py).splice(0,t.length-1)}function _s({duration:t=300,keyframes:a,times:i,ease:s="easeInOut"}){const r=bM(s)?s.map(xm):xm(s),c={done:!1,value:a[0]},h=TM(i&&i.length===a.length?i:fy(a),t),p=my(h,a,{ease:Array.isArray(r)?r:CM(a,r)});return{calculatedDuration:t,next:m=>(c.value=p(m),c.done=m>=t,c)}}const MM=t=>{const a=({timestamp:i})=>t(i);return{start:()=>fe.update(a,!0),stop:()=>Qt(a),now:()=>Be.isProcessing?Be.timestamp:Yt.now()}},IM={decay:km,inertia:km,tween:_s,keyframes:_s,spring:uy},AM=t=>t/100;class lr extends ly{constructor(a){super(a),this.holdTime=null,this.cancelTime=null,this.currentTime=0,this.playbackSpeed=1,this.pendingPlayState="running",this.startTime=null,this.state="idle",this.stop=()=>{if(this.resolver.cancel(),this.isStopped=!0,this.state==="idle")return;this.teardown();const{onStop:m}=this.options;m&&m()};const{name:i,motionValue:s,element:r,keyframes:c}=this.options,h=(r==null?void 0:r.KeyframeResolver)||Fd,p=(m,f)=>this.onKeyframesResolved(m,f);this.resolver=new h(c,p,i,s,r),this.resolver.scheduleResolve()}flatten(){super.flatten(),this._resolved&&Object.assign(this._resolved,this.initPlayback(this._resolved.keyframes))}initPlayback(a){const{type:i="keyframes",repeat:s=0,repeatDelay:r=0,repeatType:c,velocity:h=0}=this.options,p=Cd(i)?i:IM[i]||_s;let m,f;p!==_s&&typeof a[0]!="number"&&(m=uo(AM,dy(a[0],a[1])),a=[0,100]);const y=p({...this.options,keyframes:a});c==="mirror"&&(f=p({...this.options,keyframes:[...a].reverse(),velocity:-h})),y.calculatedDuration===null&&(y.calculatedDuration=Rg(y));const{calculatedDuration:v}=y,b=v+r,T=b*(s+1)-r;return{generator:y,mirroredGenerator:f,mapPercentToKeyframes:m,calculatedDuration:v,resolvedDuration:b,totalDuration:T}}onPostResolved(){const{autoplay:a=!0}=this.options;this.play(),this.pendingPlayState==="paused"||!a?this.pause():this.state=this.pendingPlayState}tick(a,i=!1){const{resolved:s}=this;if(!s){const{keyframes:G}=this.options;return{done:!0,value:G[G.length-1]}}const{finalKeyframe:r,generator:c,mirroredGenerator:h,mapPercentToKeyframes:p,keyframes:m,calculatedDuration:f,totalDuration:y,resolvedDuration:v}=s;if(this.startTime===null)return c.next(0);const{delay:b,repeat:T,repeatType:M,repeatDelay:I,onUpdate:A}=this.options;this.speed>0?this.startTime=Math.min(this.startTime,a):this.speed<0&&(this.startTime=Math.min(a-y/this.speed,this.startTime)),i?this.currentTime=a:this.holdTime!==null?this.currentTime=this.holdTime:this.currentTime=Math.round(a-this.startTime)*this.speed;const R=this.currentTime-b*(this.speed>=0?1:-1),j=this.speed>=0?R<0:R>y;this.currentTime=Math.max(R,0),this.state==="finished"&&this.holdTime===null&&(this.currentTime=y);let V=this.currentTime,B=c;if(T){const G=Math.min(this.currentTime,y)/v;let ce=Math.floor(G),we=G%1;!we&&G>=1&&(we=1),we===1&&ce--,ce=Math.min(ce,T+1),!!(ce%2)&&(M==="reverse"?(we=1-we,I&&(we-=I/v)):M==="mirror"&&(B=h)),V=Xt(0,1,we)*v}const N=j?{done:!1,value:m[0]}:B.next(V);p&&(N.value=p(N.value));let{done:_}=N;!j&&f!==null&&(_=this.speed>=0?this.currentTime>=y:this.currentTime<=0);const ae=this.holdTime===null&&(this.state==="finished"||this.state==="running"&&_);return ae&&r!==void 0&&(N.value=rr(m,this.options,r)),A&&A(N.value),ae&&this.finish(),N}get duration(){const{resolved:a}=this;return a?hn(a.calculatedDuration):0}get time(){return hn(this.currentTime)}set time(a){a=dn(a),this.currentTime=a,this.holdTime!==null||this.speed===0?this.holdTime=a:this.driver&&(this.startTime=this.driver.now()-a/this.speed)}get speed(){return this.playbackSpeed}set speed(a){const i=this.playbackSpeed!==a;this.playbackSpeed=a,i&&(this.time=hn(this.currentTime))}play(){if(this.resolver.isScheduled||this.resolver.resume(),!this._resolved){this.pendingPlayState="running";return}if(this.isStopped)return;const{driver:a=MM,onPlay:i,startTime:s}=this.options;this.driver||(this.driver=a(c=>this.tick(c))),i&&i();const r=this.driver.now();this.holdTime!==null?this.startTime=r-this.holdTime:this.startTime?this.state==="finished"&&(this.startTime=r):this.startTime=s??this.calcStartTime(),this.state==="finished"&&this.updateFinishedPromise(),this.cancelTime=this.startTime,this.holdTime=null,this.state="running",this.driver.start()}pause(){var a;if(!this._resolved){this.pendingPlayState="paused";return}this.state="paused",this.holdTime=(a=this.currentTime)!==null&&a!==void 0?a:0}complete(){this.state!=="running"&&this.play(),this.pendingPlayState=this.state="finished",this.holdTime=null}finish(){this.teardown(),this.state="finished";const{onComplete:a}=this.options;a&&a()}cancel(){this.cancelTime!==null&&this.tick(this.cancelTime),this.teardown(),this.updateFinishedPromise()}teardown(){this.state="idle",this.stopDriver(),this.resolveFinishedPromise(),this.updateFinishedPromise(),this.startTime=this.cancelTime=null,this.resolver.cancel()}stopDriver(){this.driver&&(this.driver.stop(),this.driver=void 0)}sample(a){return this.startTime=0,this.tick(a,!0)}}function EM(t){return new lr(t)}const PM=new Set(["opacity","clipPath","filter","transform"]);function RM(t,a,i,{delay:s=0,duration:r=300,repeat:c=0,repeatType:h="loop",ease:p="easeInOut",times:m}={}){const f={[a]:i};m&&(f.offset=m);const y=Vg(p,r);return Array.isArray(y)&&(f.easing=y),t.animate(f,{delay:s,duration:r,easing:Array.isArray(y)?"linear":y,fill:"both",iterations:c+1,direction:h==="reverse"?"alternate":"normal"})}const LM=dd(()=>Object.hasOwnProperty.call(Element.prototype,"animate")),Ks=10,DM=2e4;function VM(t){return Cd(t.type)||t.type==="spring"||!Dg(t.ease)}function jM(t,a){const i=new lr({...a,keyframes:t,repeat:0,delay:0,isGenerator:!0});let s={done:!1,value:t[0]};const r=[];let c=0;for(;!s.done&&c<DM;)s=i.sample(c),r.push(s.value),c+=Ks;return{times:void 0,keyframes:r,duration:c-Ks,ease:"linear"}}const gy={anticipate:_g,backInOut:Hg,circInOut:Jg};function OM(t){return t in gy}class Sm extends ly{constructor(a){super(a);const{name:i,motionValue:s,element:r,keyframes:c}=this.options;this.resolver=new ry(c,(h,p)=>this.onKeyframesResolved(h,p),i,s,r),this.resolver.scheduleResolve()}initPlayback(a,i){let{duration:s=300,times:r,ease:c,type:h,motionValue:p,name:m,startTime:f}=this.options;if(!p.owner||!p.owner.current)return!1;if(typeof c=="string"&&Bs()&&OM(c)&&(c=gy[c]),VM(this.options)){const{onComplete:v,onUpdate:b,motionValue:T,element:M,...I}=this.options,A=jM(a,I);a=A.keyframes,a.length===1&&(a[1]=a[0]),s=A.duration,r=A.times,c=A.ease,h="keyframes"}const y=RM(p.owner.current,m,a,{...this.options,duration:s,times:r,ease:c});return y.startTime=f??this.calcStartTime(),this.pendingTimeline?(rm(y,this.pendingTimeline),this.pendingTimeline=void 0):y.onfinish=()=>{const{onComplete:v}=this.options;p.set(rr(a,this.options,i)),v&&v(),this.cancel(),this.resolveFinishedPromise()},{animation:y,duration:s,times:r,type:h,ease:c,keyframes:a}}get duration(){const{resolved:a}=this;if(!a)return 0;const{duration:i}=a;return hn(i)}get time(){const{resolved:a}=this;if(!a)return 0;const{animation:i}=a;return hn(i.currentTime||0)}set time(a){const{resolved:i}=this;if(!i)return;const{animation:s}=i;s.currentTime=dn(a)}get speed(){const{resolved:a}=this;if(!a)return 1;const{animation:i}=a;return i.playbackRate}set speed(a){const{resolved:i}=this;if(!i)return;const{animation:s}=i;s.playbackRate=a}get state(){const{resolved:a}=this;if(!a)return"idle";const{animation:i}=a;return i.playState}get startTime(){const{resolved:a}=this;if(!a)return null;const{animation:i}=a;return i.startTime}attachTimeline(a){if(!this._resolved)this.pendingTimeline=a;else{const{resolved:i}=this;if(!i)return rt;const{animation:s}=i;rm(s,a)}return rt}play(){if(this.isStopped)return;const{resolved:a}=this;if(!a)return;const{animation:i}=a;i.playState==="finished"&&this.updateFinishedPromise(),i.play()}pause(){const{resolved:a}=this;if(!a)return;const{animation:i}=a;i.pause()}stop(){if(this.resolver.cancel(),this.isStopped=!0,this.state==="idle")return;this.resolveFinishedPromise(),this.updateFinishedPromise();const{resolved:a}=this;if(!a)return;const{animation:i,keyframes:s,duration:r,type:c,ease:h,times:p}=a;if(i.playState==="idle"||i.playState==="finished")return;if(this.time){const{motionValue:f,onUpdate:y,onComplete:v,element:b,...T}=this.options,M=new lr({...T,keyframes:s,duration:r,type:c,ease:h,times:p,isGenerator:!0}),I=dn(this.time);f.setWithVelocity(M.sample(I-Ks).value,M.sample(I).value,Ks)}const{onStop:m}=this.options;m&&m(),this.cancel()}complete(){const{resolved:a}=this;a&&a.animation.finish()}cancel(){const{resolved:a}=this;a&&a.animation.cancel()}static supports(a){const{motionValue:i,name:s,repeatDelay:r,repeatType:c,damping:h,type:p}=a;if(!i||!i.owner||!(i.owner.current instanceof HTMLElement))return!1;const{onUpdate:m,transformTemplate:f}=i.owner.getProps();return LM()&&s&&PM.has(s)&&!m&&!f&&!r&&c!=="mirror"&&h!==0&&p!=="inertia"}}const UM={type:"spring",stiffness:500,damping:25,restSpeed:10},FM=t=>({type:"spring",stiffness:550,damping:t===0?2*Math.sqrt(550):30,restSpeed:10}),NM={type:"keyframes",duration:.8},qM={type:"keyframes",ease:[.25,.1,.35,1],duration:.3},WM=(t,{keyframes:a})=>a.length>2?NM:oa.has(t)?t.startsWith("scale")?FM(a[1]):UM:qM;function zM({when:t,delay:a,delayChildren:i,staggerChildren:s,staggerDirection:r,repeat:c,repeatType:h,repeatDelay:p,from:m,elapsed:f,...y}){return!!Object.keys(y).length}const qd=(t,a,i,s={},r,c)=>h=>{const p=Td(s,t)||{},m=p.delay||s.delay||0;let{elapsed:f=0}=s;f=f-dn(m);let y={keyframes:Array.isArray(i)?i:[null,i],ease:"easeOut",velocity:a.getVelocity(),...p,delay:-f,onUpdate:b=>{a.set(b),p.onUpdate&&p.onUpdate(b)},onComplete:()=>{h(),p.onComplete&&p.onComplete()},name:t,motionValue:a,element:c?void 0:r};zM(p)||(y={...y,...WM(t,y)}),y.duration&&(y.duration=dn(y.duration)),y.repeatDelay&&(y.repeatDelay=dn(y.repeatDelay)),y.from!==void 0&&(y.keyframes[0]=y.from);let v=!1;if((y.type===!1||y.duration===0&&!y.repeatDelay)&&(y.duration=0,y.delay===0&&(v=!0)),v&&!c&&a.get()!==void 0){const b=rr(y.keyframes,p);if(b!==void 0)return fe.update(()=>{y.onUpdate(b),y.onComplete()}),new c0([])}return!c&&Sm.supports(y)?new Sm(y):new lr(y)};function BM({protectedKeys:t,needsAnimating:a},i){const s=t.hasOwnProperty(i)&&a[i]!==!0;return a[i]=!1,s}function yy(t,a,{delay:i=0,transitionOverride:s,type:r}={}){var c;let{transition:h=t.getDefaultTransition(),transitionEnd:p,...m}=a;s&&(h=s);const f=[],y=r&&t.animationState&&t.animationState.getState()[r];for(const v in m){const b=t.getValue(v,(c=t.latestValues[v])!==null&&c!==void 0?c:null),T=m[v];if(T===void 0||y&&BM(y,v))continue;const M={delay:i,...Td(h||{},v)};let I=!1;if(window.MotionHandoffAnimation){const R=Ng(t);if(R){const j=window.MotionHandoffAnimation(R,v,fe);j!==null&&(M.startTime=j,I=!0)}}Oc(t,v),b.start(qd(v,b,T,t.shouldReduceMotion&&Fg.has(v)?{type:!1}:M,t,I));const A=b.animation;A&&f.push(A)}return p&&Promise.all(f).then(()=>{fe.update(()=>{p&&x0(t,p)})}),f}function Bc(t,a,i={}){var s;const r=sr(t,a,i.type==="exit"?(s=t.presenceContext)===null||s===void 0?void 0:s.custom:void 0);let{transition:c=t.getDefaultTransition()||{}}=r||{};i.transitionOverride&&(c=i.transitionOverride);const h=r?()=>Promise.all(yy(t,r,i)):()=>Promise.resolve(),p=t.variantChildren&&t.variantChildren.size?(f=0)=>{const{delayChildren:y=0,staggerChildren:v,staggerDirection:b}=c;return HM(t,a,y+f,v,b,i)}:()=>Promise.resolve(),{when:m}=c;if(m){const[f,y]=m==="beforeChildren"?[h,p]:[p,h];return f().then(()=>y())}else return Promise.all([h(),p(i.delay)])}function HM(t,a,i=0,s=0,r=1,c){const h=[],p=(t.variantChildren.size-1)*s,m=r===1?(f=0)=>f*s:(f=0)=>p-f*s;return Array.from(t.variantChildren).sort(_M).forEach((f,y)=>{f.notify("AnimationStart",a),h.push(Bc(f,a,{...c,delay:i+m(y)}).then(()=>f.notify("AnimationComplete",a)))}),Promise.all(h)}function _M(t,a){return t.sortNodePosition(a)}function KM(t,a,i={}){t.notify("AnimationStart",a);let s;if(Array.isArray(a)){const r=a.map(c=>Bc(t,c,i));s=Promise.all(r)}else if(typeof a=="string")s=Bc(t,a,i);else{const r=typeof a=="function"?sr(t,a,i.custom):a;s=Promise.all(yy(t,r,i))}return s.then(()=>{t.notify("AnimationComplete",a)})}const JM=ud.length;function wy(t){if(!t)return;if(!t.isControllingVariants){const i=t.parent?wy(t.parent)||{}:{};return t.props.initial!==void 0&&(i.initial=t.props.initial),i}const a={};for(let i=0;i<JM;i++){const s=ud[i],r=t.props[s];(Zi(r)||r===!1)&&(a[s]=r)}return a}const GM=[...hd].reverse(),$M=hd.length;function YM(t){return a=>Promise.all(a.map(({animation:i,options:s})=>KM(t,i,s)))}function QM(t){let a=YM(t),i=Tm(),s=!0;const r=m=>(f,y)=>{var v;const b=sr(t,y,m==="exit"?(v=t.presenceContext)===null||v===void 0?void 0:v.custom:void 0);if(b){const{transition:T,transitionEnd:M,...I}=b;f={...f,...I,...M}}return f};function c(m){a=m(t)}function h(m){const{props:f}=t,y=wy(t.parent)||{},v=[],b=new Set;let T={},M=1/0;for(let A=0;A<$M;A++){const R=GM[A],j=i[R],V=f[R]!==void 0?f[R]:y[R],B=Zi(V),N=R===m?j.isActive:null;N===!1&&(M=A);let _=V===y[R]&&V!==f[R]&&B;if(_&&s&&t.manuallyAnimateOnMount&&(_=!1),j.protectedKeys={...T},!j.isActive&&N===null||!V&&!j.prevProp||ir(V)||typeof V=="boolean")continue;const ae=XM(j.prevProp,V);let G=ae||R===m&&j.isActive&&!_&&B||A>M&&B,ce=!1;const we=Array.isArray(V)?V:[V];let Me=we.reduce(r(R),{});N===!1&&(Me={});const{prevResolvedValues:_e={}}=j,Fe={..._e,...Me},Ne=oe=>{G=!0,b.has(oe)&&(ce=!0,b.delete(oe)),j.needsAnimating[oe]=!0;const W=t.getValue(oe);W&&(W.liveStyle=!1)};for(const oe in Fe){const W=Me[oe],Z=_e[oe];if(T.hasOwnProperty(oe))continue;let H=!1;Dc(W)&&Dc(Z)?H=!Eg(W,Z):H=W!==Z,H?W!=null?Ne(oe):b.add(oe):W!==void 0&&b.has(oe)?Ne(oe):j.protectedKeys[oe]=!0}j.prevProp=V,j.prevResolvedValues=Me,j.isActive&&(T={...T,...Me}),s&&t.blockInitialAnimation&&(G=!1),G&&(!(_&&ae)||ce)&&v.push(...we.map(oe=>({animation:oe,options:{type:R}})))}if(b.size){const A={};b.forEach(R=>{const j=t.getBaseTarget(R),V=t.getValue(R);V&&(V.liveStyle=!0),A[R]=j??null}),v.push({animation:A})}let I=!!v.length;return s&&(f.initial===!1||f.initial===f.animate)&&!t.manuallyAnimateOnMount&&(I=!1),s=!1,I?a(v):Promise.resolve()}function p(m,f){var y;if(i[m].isActive===f)return Promise.resolve();(y=t.variantChildren)===null||y===void 0||y.forEach(b=>{var T;return(T=b.animationState)===null||T===void 0?void 0:T.setActive(m,f)}),i[m].isActive=f;const v=h(m);for(const b in i)i[b].protectedKeys={};return v}return{animateChanges:h,setActive:p,setAnimateFunction:c,getState:()=>i,reset:()=>{i=Tm(),s=!0}}}function XM(t,a){return typeof a=="string"?a!==t:Array.isArray(a)?!Eg(a,t):!1}function Qn(t=!1){return{isActive:t,protectedKeys:{},needsAnimating:{},prevResolvedValues:{}}}function Tm(){return{animate:Qn(!0),whileInView:Qn(),whileHover:Qn(),whileTap:Qn(),whileDrag:Qn(),whileFocus:Qn(),exit:Qn()}}class Un{constructor(a){this.isMounted=!1,this.node=a}update(){}}class ZM extends Un{constructor(a){super(a),a.animationState||(a.animationState=QM(a))}updateAnimationControlsSubscription(){const{animate:a}=this.node.getProps();ir(a)&&(this.unmountControls=a.subscribe(this.node))}mount(){this.updateAnimationControlsSubscription()}update(){const{animate:a}=this.node.getProps(),{animate:i}=this.node.prevProps||{};a!==i&&this.updateAnimationControlsSubscription()}unmount(){var a;this.node.animationState.reset(),(a=this.unmountControls)===null||a===void 0||a.call(this)}}let eI=0;class tI extends Un{constructor(){super(...arguments),this.id=eI++}update(){if(!this.node.presenceContext)return;const{isPresent:a,onExitComplete:i}=this.node.presenceContext,{isPresent:s}=this.node.prevPresenceContext||{};if(!this.node.animationState||a===s)return;const r=this.node.animationState.setActive("exit",!a);i&&!a&&r.then(()=>i(this.id))}mount(){const{register:a}=this.node.presenceContext||{};a&&(this.unmount=a(this.id))}unmount(){}}const nI={animation:{Feature:ZM},exit:{Feature:tI}};function no(t,a,i,s={passive:!0}){return t.addEventListener(a,i,s),()=>t.removeEventListener(a,i)}function po(t){return{point:{x:t.pageX,y:t.pageY}}}const aI=t=>a=>Ad(a)&&t(a,po(a));function Ji(t,a,i,s){return no(t,a,aI(i),s)}const Cm=(t,a)=>Math.abs(t-a);function iI(t,a){const i=Cm(t.x,a.x),s=Cm(t.y,a.y);return Math.sqrt(i**2+s**2)}class vy{constructor(a,i,{transformPagePoint:s,contextWindow:r,dragSnapToOrigin:c=!1}={}){if(this.startEvent=null,this.lastMoveEvent=null,this.lastMoveEventInfo=null,this.handlers={},this.contextWindow=window,this.updatePoint=()=>{if(!(this.lastMoveEvent&&this.lastMoveEventInfo))return;const v=vc(this.lastMoveEventInfo,this.history),b=this.startEvent!==null,T=iI(v.offset,{x:0,y:0})>=3;if(!b&&!T)return;const{point:M}=v,{timestamp:I}=Be;this.history.push({...M,timestamp:I});const{onStart:A,onMove:R}=this.handlers;b||(A&&A(this.lastMoveEvent,v),this.startEvent=this.lastMoveEvent),R&&R(this.lastMoveEvent,v)},this.handlePointerMove=(v,b)=>{this.lastMoveEvent=v,this.lastMoveEventInfo=wc(b,this.transformPagePoint),fe.update(this.updatePoint,!0)},this.handlePointerUp=(v,b)=>{this.end();const{onEnd:T,onSessionEnd:M,resumeAnimation:I}=this.handlers;if(this.dragSnapToOrigin&&I&&I(),!(this.lastMoveEvent&&this.lastMoveEventInfo))return;const A=vc(v.type==="pointercancel"?this.lastMoveEventInfo:wc(b,this.transformPagePoint),this.history);this.startEvent&&T&&T(v,A),M&&M(v,A)},!Ad(a))return;this.dragSnapToOrigin=c,this.handlers=i,this.transformPagePoint=s,this.contextWindow=r||window;const h=po(a),p=wc(h,this.transformPagePoint),{point:m}=p,{timestamp:f}=Be;this.history=[{...m,timestamp:f}];const{onSessionStart:y}=i;y&&y(a,vc(p,this.history)),this.removeListeners=uo(Ji(this.contextWindow,"pointermove",this.handlePointerMove),Ji(this.contextWindow,"pointerup",this.handlePointerUp),Ji(this.contextWindow,"pointercancel",this.handlePointerUp))}updateHandlers(a){this.handlers=a}end(){this.removeListeners&&this.removeListeners(),Qt(this.updatePoint)}}function wc(t,a){return a?{point:a(t.point)}:t}function Mm(t,a){return{x:t.x-a.x,y:t.y-a.y}}function vc({point:t},a){return{point:t,delta:Mm(t,by(a)),offset:Mm(t,oI(a)),velocity:sI(a,.1)}}function oI(t){return t[0]}function by(t){return t[t.length-1]}function sI(t,a){if(t.length<2)return{x:0,y:0};let i=t.length-1,s=null;const r=by(t);for(;i>=0&&(s=t[i],!(r.timestamp-s.timestamp>dn(a)));)i--;if(!s)return{x:0,y:0};const c=hn(r.timestamp-s.timestamp);if(c===0)return{x:0,y:0};const h={x:(r.x-s.x)/c,y:(r.y-s.y)/c};return h.x===1/0&&(h.x=0),h.y===1/0&&(h.y=0),h}const ky=1e-4,rI=1-ky,lI=1+ky,xy=.01,cI=0-xy,dI=0+xy;function bt(t){return t.max-t.min}function hI(t,a,i){return Math.abs(t-a)<=i}function Im(t,a,i,s=.5){t.origin=s,t.originPoint=Re(a.min,a.max,t.origin),t.scale=bt(i)/bt(a),t.translate=Re(i.min,i.max,t.origin)-t.originPoint,(t.scale>=rI&&t.scale<=lI||isNaN(t.scale))&&(t.scale=1),(t.translate>=cI&&t.translate<=dI||isNaN(t.translate))&&(t.translate=0)}function Gi(t,a,i,s){Im(t.x,a.x,i.x,s?s.originX:void 0),Im(t.y,a.y,i.y,s?s.originY:void 0)}function Am(t,a,i){t.min=i.min+a.min,t.max=t.min+bt(a)}function uI(t,a,i){Am(t.x,a.x,i.x),Am(t.y,a.y,i.y)}function Em(t,a,i){t.min=a.min-i.min,t.max=t.min+bt(a)}function $i(t,a,i){Em(t.x,a.x,i.x),Em(t.y,a.y,i.y)}function pI(t,{min:a,max:i},s){return a!==void 0&&t<a?t=s?Re(a,t,s.min):Math.max(t,a):i!==void 0&&t>i&&(t=s?Re(i,t,s.max):Math.min(t,i)),t}function Pm(t,a,i){return{min:a!==void 0?t.min+a:void 0,max:i!==void 0?t.max+i-(t.max-t.min):void 0}}function mI(t,{top:a,left:i,bottom:s,right:r}){return{x:Pm(t.x,i,r),y:Pm(t.y,a,s)}}function Rm(t,a){let i=a.min-t.min,s=a.max-t.max;return a.max-a.min<t.max-t.min&&([i,s]=[s,i]),{min:i,max:s}}function fI(t,a){return{x:Rm(t.x,a.x),y:Rm(t.y,a.y)}}function gI(t,a){let i=.5;const s=bt(t),r=bt(a);return r>s?i=ia(a.min,a.max-s,t.min):s>r&&(i=ia(t.min,t.max-r,a.min)),Xt(0,1,i)}function yI(t,a){const i={};return a.min!==void 0&&(i.min=a.min-t.min),a.max!==void 0&&(i.max=a.max-t.min),i}const Hc=.35;function wI(t=Hc){return t===!1?t=0:t===!0&&(t=Hc),{x:Lm(t,"left","right"),y:Lm(t,"top","bottom")}}function Lm(t,a,i){return{min:Dm(t,a),max:Dm(t,i)}}function Dm(t,a){return typeof t=="number"?t:t[a]||0}const Vm=()=>({translate:0,scale:1,origin:0,originPoint:0}),ja=()=>({x:Vm(),y:Vm()}),jm=()=>({min:0,max:0}),Ue=()=>({x:jm(),y:jm()});function Et(t){return[t("x"),t("y")]}function Sy({top:t,left:a,right:i,bottom:s}){return{x:{min:a,max:i},y:{min:t,max:s}}}function vI({x:t,y:a}){return{top:a.min,right:t.max,bottom:a.max,left:t.min}}function bI(t,a){if(!a)return t;const i=a({x:t.left,y:t.top}),s=a({x:t.right,y:t.bottom});return{top:i.y,left:i.x,bottom:s.y,right:s.x}}function bc(t){return t===void 0||t===1}function _c({scale:t,scaleX:a,scaleY:i}){return!bc(t)||!bc(a)||!bc(i)}function ea(t){return _c(t)||Ty(t)||t.z||t.rotate||t.rotateX||t.rotateY||t.skewX||t.skewY}function Ty(t){return Om(t.x)||Om(t.y)}function Om(t){return t&&t!=="0%"}function Js(t,a,i){const s=t-i,r=a*s;return i+r}function Um(t,a,i,s,r){return r!==void 0&&(t=Js(t,r,s)),Js(t,i,s)+a}function Kc(t,a=0,i=1,s,r){t.min=Um(t.min,a,i,s,r),t.max=Um(t.max,a,i,s,r)}function Cy(t,{x:a,y:i}){Kc(t.x,a.translate,a.scale,a.originPoint),Kc(t.y,i.translate,i.scale,i.originPoint)}const Fm=.999999999999,Nm=1.0000000000001;function kI(t,a,i,s=!1){const r=i.length;if(!r)return;a.x=a.y=1;let c,h;for(let p=0;p<r;p++){c=i[p],h=c.projectionDelta;const{visualElement:m}=c.options;m&&m.props.style&&m.props.style.display==="contents"||(s&&c.options.layoutScroll&&c.scroll&&c!==c.root&&Ua(t,{x:-c.scroll.offset.x,y:-c.scroll.offset.y}),h&&(a.x*=h.x.scale,a.y*=h.y.scale,Cy(t,h)),s&&ea(c.latestValues)&&Ua(t,c.latestValues))}a.x<Nm&&a.x>Fm&&(a.x=1),a.y<Nm&&a.y>Fm&&(a.y=1)}function Oa(t,a){t.min=t.min+a,t.max=t.max+a}function qm(t,a,i,s,r=.5){const c=Re(t.min,t.max,r);Kc(t,a,i,c,s)}function Ua(t,a){qm(t.x,a.x,a.scaleX,a.scale,a.originX),qm(t.y,a.y,a.scaleY,a.scale,a.originY)}function My(t,a){return Sy(bI(t.getBoundingClientRect(),a))}function xI(t,a,i){const s=My(t,i),{scroll:r}=a;return r&&(Oa(s.x,r.offset.x),Oa(s.y,r.offset.y)),s}const Iy=({current:t})=>t?t.ownerDocument.defaultView:null,SI=new WeakMap;class TI{constructor(a){this.openDragLock=null,this.isDragging=!1,this.currentDirection=null,this.originPoint={x:0,y:0},this.constraints=!1,this.hasMutatedConstraints=!1,this.elastic=Ue(),this.visualElement=a}start(a,{snapToCursor:i=!1}={}){const{presenceContext:s}=this.visualElement;if(s&&s.isPresent===!1)return;const r=y=>{const{dragSnapToOrigin:v}=this.getProps();v?this.pauseAnimation():this.stopAnimation(),i&&this.snapToCursor(po(y).point)},c=(y,v)=>{const{drag:b,dragPropagation:T,onDragStart:M}=this.getProps();if(b&&!T&&(this.openDragLock&&this.openDragLock(),this.openDragLock=y0(b),!this.openDragLock))return;this.isDragging=!0,this.currentDirection=null,this.resolveConstraints(),this.visualElement.projection&&(this.visualElement.projection.isAnimationBlocked=!0,this.visualElement.projection.target=void 0),Et(A=>{let R=this.getAxisMotionValue(A).get()||0;if($t.test(R)){const{projection:j}=this.visualElement;if(j&&j.layout){const V=j.layout.layoutBox[A];V&&(R=bt(V)*(parseFloat(R)/100))}}this.originPoint[A]=R}),M&&fe.postRender(()=>M(y,v)),Oc(this.visualElement,"transform");const{animationState:I}=this.visualElement;I&&I.setActive("whileDrag",!0)},h=(y,v)=>{const{dragPropagation:b,dragDirectionLock:T,onDirectionLock:M,onDrag:I}=this.getProps();if(!b&&!this.openDragLock)return;const{offset:A}=v;if(T&&this.currentDirection===null){this.currentDirection=CI(A),this.currentDirection!==null&&M&&M(this.currentDirection);return}this.updateAxis("x",v.point,A),this.updateAxis("y",v.point,A),this.visualElement.render(),I&&I(y,v)},p=(y,v)=>this.stop(y,v),m=()=>Et(y=>{var v;return this.getAnimationState(y)==="paused"&&((v=this.getAxisMotionValue(y).animation)===null||v===void 0?void 0:v.play())}),{dragSnapToOrigin:f}=this.getProps();this.panSession=new vy(a,{onSessionStart:r,onStart:c,onMove:h,onSessionEnd:p,resumeAnimation:m},{transformPagePoint:this.visualElement.getTransformPagePoint(),dragSnapToOrigin:f,contextWindow:Iy(this.visualElement)})}stop(a,i){const s=this.isDragging;if(this.cancel(),!s)return;const{velocity:r}=i;this.startAnimation(r);const{onDragEnd:c}=this.getProps();c&&fe.postRender(()=>c(a,i))}cancel(){this.isDragging=!1;const{projection:a,animationState:i}=this.visualElement;a&&(a.isAnimationBlocked=!1),this.panSession&&this.panSession.end(),this.panSession=void 0;const{dragPropagation:s}=this.getProps();!s&&this.openDragLock&&(this.openDragLock(),this.openDragLock=null),i&&i.setActive("whileDrag",!1)}updateAxis(a,i,s){const{drag:r}=this.getProps();if(!s||!As(a,r,this.currentDirection))return;const c=this.getAxisMotionValue(a);let h=this.originPoint[a]+s[a];this.constraints&&this.constraints[a]&&(h=pI(h,this.constraints[a],this.elastic[a])),c.set(h)}resolveConstraints(){var a;const{dragConstraints:i,dragElastic:s}=this.getProps(),r=this.visualElement.projection&&!this.visualElement.projection.layout?this.visualElement.projection.measure(!1):(a=this.visualElement.projection)===null||a===void 0?void 0:a.layout,c=this.constraints;i&&Da(i)?this.constraints||(this.constraints=this.resolveRefConstraints()):i&&r?this.constraints=mI(r.layoutBox,i):this.constraints=!1,this.elastic=wI(s),c!==this.constraints&&r&&this.constraints&&!this.hasMutatedConstraints&&Et(h=>{this.constraints!==!1&&this.getAxisMotionValue(h)&&(this.constraints[h]=yI(r.layoutBox[h],this.constraints[h]))})}resolveRefConstraints(){const{dragConstraints:a,onMeasureDragConstraints:i}=this.getProps();if(!a||!Da(a))return!1;const s=a.current,{projection:r}=this.visualElement;if(!r||!r.layout)return!1;const c=xI(s,r.root,this.visualElement.getTransformPagePoint());let h=fI(r.layout.layoutBox,c);if(i){const p=i(vI(h));this.hasMutatedConstraints=!!p,p&&(h=Sy(p))}return h}startAnimation(a){const{drag:i,dragMomentum:s,dragElastic:r,dragTransition:c,dragSnapToOrigin:h,onDragTransitionEnd:p}=this.getProps(),m=this.constraints||{},f=Et(y=>{if(!As(y,i,this.currentDirection))return;let v=m&&m[y]||{};h&&(v={min:0,max:0});const b=r?200:1e6,T=r?40:1e7,M={type:"inertia",velocity:s?a[y]:0,bounceStiffness:b,bounceDamping:T,timeConstant:750,restDelta:1,restSpeed:10,...c,...v};return this.startAxisValueAnimation(y,M)});return Promise.all(f).then(p)}startAxisValueAnimation(a,i){const s=this.getAxisMotionValue(a);return Oc(this.visualElement,a),s.start(qd(a,s,0,i,this.visualElement,!1))}stopAnimation(){Et(a=>this.getAxisMotionValue(a).stop())}pauseAnimation(){Et(a=>{var i;return(i=this.getAxisMotionValue(a).animation)===null||i===void 0?void 0:i.pause()})}getAnimationState(a){var i;return(i=this.getAxisMotionValue(a).animation)===null||i===void 0?void 0:i.state}getAxisMotionValue(a){const i=`_drag${a.toUpperCase()}`,s=this.visualElement.getProps(),r=s[i];return r||this.visualElement.getValue(a,(s.initial?s.initial[a]:void 0)||0)}snapToCursor(a){Et(i=>{const{drag:s}=this.getProps();if(!As(i,s,this.currentDirection))return;const{projection:r}=this.visualElement,c=this.getAxisMotionValue(i);if(r&&r.layout){const{min:h,max:p}=r.layout.layoutBox[i];c.set(a[i]-Re(h,p,.5))}})}scalePositionWithinConstraints(){if(!this.visualElement.current)return;const{drag:a,dragConstraints:i}=this.getProps(),{projection:s}=this.visualElement;if(!Da(i)||!s||!this.constraints)return;this.stopAnimation();const r={x:0,y:0};Et(h=>{const p=this.getAxisMotionValue(h);if(p&&this.constraints!==!1){const m=p.get();r[h]=gI({min:m,max:m},this.constraints[h])}});const{transformTemplate:c}=this.visualElement.getProps();this.visualElement.current.style.transform=c?c({},""):"none",s.root&&s.root.updateScroll(),s.updateLayout(),this.resolveConstraints(),Et(h=>{if(!As(h,a,null))return;const p=this.getAxisMotionValue(h),{min:m,max:f}=this.constraints[h];p.set(Re(m,f,r[h]))})}addListeners(){if(!this.visualElement.current)return;SI.set(this.visualElement,this);const a=this.visualElement.current,i=Ji(a,"pointerdown",m=>{const{drag:f,dragListener:y=!0}=this.getProps();f&&y&&this.start(m)}),s=()=>{const{dragConstraints:m}=this.getProps();Da(m)&&m.current&&(this.constraints=this.resolveRefConstraints())},{projection:r}=this.visualElement,c=r.addEventListener("measure",s);r&&!r.layout&&(r.root&&r.root.updateScroll(),r.updateLayout()),fe.read(s);const h=no(window,"resize",()=>this.scalePositionWithinConstraints()),p=r.addEventListener("didUpdate",(({delta:m,hasLayoutChanged:f})=>{this.isDragging&&f&&(Et(y=>{const v=this.getAxisMotionValue(y);v&&(this.originPoint[y]+=m[y].translate,v.set(v.get()+m[y].translate))}),this.visualElement.render())}));return()=>{h(),i(),c(),p&&p()}}getProps(){const a=this.visualElement.getProps(),{drag:i=!1,dragDirectionLock:s=!1,dragPropagation:r=!1,dragConstraints:c=!1,dragElastic:h=Hc,dragMomentum:p=!0}=a;return{...a,drag:i,dragDirectionLock:s,dragPropagation:r,dragConstraints:c,dragElastic:h,dragMomentum:p}}}function As(t,a,i){return(a===!0||a===t)&&(i===null||i===t)}function CI(t,a=10){let i=null;return Math.abs(t.y)>a?i="y":Math.abs(t.x)>a&&(i="x"),i}class MI extends Un{constructor(a){super(a),this.removeGroupControls=rt,this.removeListeners=rt,this.controls=new TI(a)}mount(){const{dragControls:a}=this.node.getProps();a&&(this.removeGroupControls=a.subscribe(this.controls)),this.removeListeners=this.controls.addListeners()||rt}unmount(){this.removeGroupControls(),this.removeListeners()}}const Wm=t=>(a,i)=>{t&&fe.postRender(()=>t(a,i))};class II extends Un{constructor(){super(...arguments),this.removePointerDownListener=rt}onPointerDown(a){this.session=new vy(a,this.createPanHandlers(),{transformPagePoint:this.node.getTransformPagePoint(),contextWindow:Iy(this.node)})}createPanHandlers(){const{onPanSessionStart:a,onPanStart:i,onPan:s,onPanEnd:r}=this.node.getProps();return{onSessionStart:Wm(a),onStart:Wm(i),onMove:s,onEnd:(c,h)=>{delete this.session,r&&fe.postRender(()=>r(c,h))}}}mount(){this.removePointerDownListener=Ji(this.node.current,"pointerdown",a=>this.onPointerDown(a))}update(){this.session&&this.session.updateHandlers(this.createPanHandlers())}unmount(){this.removePointerDownListener(),this.session&&this.session.end()}}const Os={hasAnimatedSinceResize:!0,hasEverUpdated:!1};function zm(t,a){return a.max===a.min?0:t/(a.max-a.min)*100}const Fi={correct:(t,a)=>{if(!a.target)return t;if(typeof t=="string")if(ne.test(t))t=parseFloat(t);else return t;const i=zm(t,a.target.x),s=zm(t,a.target.y);return`${i}% ${s}%`}},AI={correct:(t,{treeScale:a,projectionDelta:i})=>{const s=t,r=On.parse(t);if(r.length>5)return s;const c=On.createTransformer(t),h=typeof r[0]!="number"?1:0,p=i.x.scale*a.x,m=i.y.scale*a.y;r[0+h]/=p,r[1+h]/=m;const f=Re(p,m,.5);return typeof r[2+h]=="number"&&(r[2+h]/=f),typeof r[3+h]=="number"&&(r[3+h]/=f),c(r)}};class EI extends x.Component{componentDidMount(){const{visualElement:a,layoutGroup:i,switchLayoutGroup:s,layoutId:r}=this.props,{projection:c}=a;XC(PI),c&&(i.group&&i.group.add(c),s&&s.register&&r&&s.register(c),c.root.didUpdate(),c.addEventListener("animationComplete",()=>{this.safeToRemove()}),c.setOptions({...c.options,onExitComplete:()=>this.safeToRemove()})),Os.hasEverUpdated=!0}getSnapshotBeforeUpdate(a){const{layoutDependency:i,visualElement:s,drag:r,isPresent:c}=this.props,h=s.projection;return h&&(h.isPresent=c,r||a.layoutDependency!==i||i===void 0?h.willUpdate():this.safeToRemove(),a.isPresent!==c&&(c?h.promote():h.relegate()||fe.postRender(()=>{const p=h.getStack();(!p||!p.members.length)&&this.safeToRemove()}))),null}componentDidUpdate(){const{projection:a}=this.props.visualElement;a&&(a.root.didUpdate(),md.postRender(()=>{!a.currentAnimation&&a.isLead()&&this.safeToRemove()}))}componentWillUnmount(){const{visualElement:a,layoutGroup:i,switchLayoutGroup:s}=this.props,{projection:r}=a;r&&(r.scheduleCheckAfterUnmount(),i&&i.group&&i.group.remove(r),s&&s.deregister&&s.deregister(r))}safeToRemove(){const{safeToRemove:a}=this.props;a&&a()}render(){return null}}function Ay(t){const[a,i]=cg(),s=x.useContext(rd);return g.jsx(EI,{...t,layoutGroup:s,switchLayoutGroup:x.useContext(gg),isPresent:a,safeToRemove:i})}const PI={borderRadius:{...Fi,applyTo:["borderTopLeftRadius","borderTopRightRadius","borderBottomLeftRadius","borderBottomRightRadius"]},borderTopLeftRadius:Fi,borderTopRightRadius:Fi,borderBottomLeftRadius:Fi,borderBottomRightRadius:Fi,boxShadow:AI};function RI(t,a,i){const s=Ye(t)?t:Gt(t);return s.start(qd("",s,a,i)),s.animation}function LI(t){return t instanceof SVGElement&&t.tagName!=="svg"}const DI=(t,a)=>t.depth-a.depth;class VI{constructor(){this.children=[],this.isDirty=!1}add(a){Ed(this.children,a),this.isDirty=!0}remove(a){Pd(this.children,a),this.isDirty=!0}forEach(a){this.isDirty&&this.children.sort(DI),this.isDirty=!1,this.children.forEach(a)}}function jI(t,a){const i=Yt.now(),s=({timestamp:r})=>{const c=r-i;c>=a&&(Qt(s),t(c-a))};return fe.read(s,!0),()=>Qt(s)}const Ey=["TopLeft","TopRight","BottomLeft","BottomRight"],OI=Ey.length,Bm=t=>typeof t=="string"?parseFloat(t):t,Hm=t=>typeof t=="number"||ne.test(t);function UI(t,a,i,s,r,c){r?(t.opacity=Re(0,i.opacity!==void 0?i.opacity:1,FI(s)),t.opacityExit=Re(a.opacity!==void 0?a.opacity:1,0,NI(s))):c&&(t.opacity=Re(a.opacity!==void 0?a.opacity:1,i.opacity!==void 0?i.opacity:1,s));for(let h=0;h<OI;h++){const p=`border${Ey[h]}Radius`;let m=_m(a,p),f=_m(i,p);if(m===void 0&&f===void 0)continue;m||(m=0),f||(f=0),m===0||f===0||Hm(m)===Hm(f)?(t[p]=Math.max(Re(Bm(m),Bm(f),s),0),($t.test(f)||$t.test(m))&&(t[p]+="%")):t[p]=f}(a.rotate||i.rotate)&&(t.rotate=Re(a.rotate||0,i.rotate||0,s))}function _m(t,a){return t[a]!==void 0?t[a]:t.borderRadius}const FI=Py(0,.5,Kg),NI=Py(.5,.95,rt);function Py(t,a,i){return s=>s<t?0:s>a?1:i(ia(t,a,s))}function Km(t,a){t.min=a.min,t.max=a.max}function At(t,a){Km(t.x,a.x),Km(t.y,a.y)}function Jm(t,a){t.translate=a.translate,t.scale=a.scale,t.originPoint=a.originPoint,t.origin=a.origin}function Gm(t,a,i,s,r){return t-=a,t=Js(t,1/i,s),r!==void 0&&(t=Js(t,1/r,s)),t}function qI(t,a=0,i=1,s=.5,r,c=t,h=t){if($t.test(a)&&(a=parseFloat(a),a=Re(h.min,h.max,a/100)-h.min),typeof a!="number")return;let p=Re(c.min,c.max,s);t===c&&(p-=a),t.min=Gm(t.min,a,i,p,r),t.max=Gm(t.max,a,i,p,r)}function $m(t,a,[i,s,r],c,h){qI(t,a[i],a[s],a[r],a.scale,c,h)}const WI=["x","scaleX","originX"],zI=["y","scaleY","originY"];function Ym(t,a,i,s){$m(t.x,a,WI,i?i.x:void 0,s?s.x:void 0),$m(t.y,a,zI,i?i.y:void 0,s?s.y:void 0)}function Qm(t){return t.translate===0&&t.scale===1}function Ry(t){return Qm(t.x)&&Qm(t.y)}function Xm(t,a){return t.min===a.min&&t.max===a.max}function BI(t,a){return Xm(t.x,a.x)&&Xm(t.y,a.y)}function Zm(t,a){return Math.round(t.min)===Math.round(a.min)&&Math.round(t.max)===Math.round(a.max)}function Ly(t,a){return Zm(t.x,a.x)&&Zm(t.y,a.y)}function ef(t){return bt(t.x)/bt(t.y)}function tf(t,a){return t.translate===a.translate&&t.scale===a.scale&&t.originPoint===a.originPoint}class HI{constructor(){this.members=[]}add(a){Ed(this.members,a),a.scheduleRender()}remove(a){if(Pd(this.members,a),a===this.prevLead&&(this.prevLead=void 0),a===this.lead){const i=this.members[this.members.length-1];i&&this.promote(i)}}relegate(a){const i=this.members.findIndex(r=>a===r);if(i===0)return!1;let s;for(let r=i;r>=0;r--){const c=this.members[r];if(c.isPresent!==!1){s=c;break}}return s?(this.promote(s),!0):!1}promote(a,i){const s=this.lead;if(a!==s&&(this.prevLead=s,this.lead=a,a.show(),s)){s.instance&&s.scheduleRender(),a.scheduleRender(),a.resumeFrom=s,i&&(a.resumeFrom.preserveOpacity=!0),s.snapshot&&(a.snapshot=s.snapshot,a.snapshot.latestValues=s.animationValues||s.latestValues),a.root&&a.root.isUpdating&&(a.isLayoutDirty=!0);const{crossfade:r}=a.options;r===!1&&s.hide()}}exitAnimationComplete(){this.members.forEach(a=>{const{options:i,resumingFrom:s}=a;i.onExitComplete&&i.onExitComplete(),s&&s.options.onExitComplete&&s.options.onExitComplete()})}scheduleRender(){this.members.forEach(a=>{a.instance&&a.scheduleRender(!1)})}removeLeadSnapshot(){this.lead&&this.lead.snapshot&&(this.lead.snapshot=void 0)}}function _I(t,a,i){let s="";const r=t.x.translate/a.x,c=t.y.translate/a.y,h=(i==null?void 0:i.z)||0;if((r||c||h)&&(s=`translate3d(${r}px, ${c}px, ${h}px) `),(a.x!==1||a.y!==1)&&(s+=`scale(${1/a.x}, ${1/a.y}) `),i){const{transformPerspective:f,rotate:y,rotateX:v,rotateY:b,skewX:T,skewY:M}=i;f&&(s=`perspective(${f}px) ${s}`),y&&(s+=`rotate(${y}deg) `),v&&(s+=`rotateX(${v}deg) `),b&&(s+=`rotateY(${b}deg) `),T&&(s+=`skewX(${T}deg) `),M&&(s+=`skewY(${M}deg) `)}const p=t.x.scale*a.x,m=t.y.scale*a.y;return(p!==1||m!==1)&&(s+=`scale(${p}, ${m})`),s||"none"}const ta={type:"projectionFrame",totalNodes:0,resolvedTargetDeltas:0,recalculatedProjection:0},_i=typeof window<"u"&&window.MotionDebug!==void 0,kc=["","X","Y","Z"],KI={visibility:"hidden"},nf=1e3;let JI=0;function xc(t,a,i,s){const{latestValues:r}=a;r[t]&&(i[t]=r[t],a.setStaticValue(t,0),s&&(s[t]=0))}function Dy(t){if(t.hasCheckedOptimisedAppear=!0,t.root===t)return;const{visualElement:a}=t.options;if(!a)return;const i=Ng(a);if(window.MotionHasOptimisedAnimation(i,"transform")){const{layout:r,layoutId:c}=t.options;window.MotionCancelOptimisedAnimation(i,"transform",fe,!(r||c))}const{parent:s}=t;s&&!s.hasCheckedOptimisedAppear&&Dy(s)}function Vy({attachResizeListener:t,defaultParent:a,measureScroll:i,checkIsScrollRoot:s,resetTransform:r}){return class{constructor(h={},p=a==null?void 0:a()){this.id=JI++,this.animationId=0,this.children=new Set,this.options={},this.isTreeAnimating=!1,this.isAnimationBlocked=!1,this.isLayoutDirty=!1,this.isProjectionDirty=!1,this.isSharedProjectionDirty=!1,this.isTransformDirty=!1,this.updateManuallyBlocked=!1,this.updateBlockedByResize=!1,this.isUpdating=!1,this.isSVG=!1,this.needsReset=!1,this.shouldResetTransform=!1,this.hasCheckedOptimisedAppear=!1,this.treeScale={x:1,y:1},this.eventHandlers=new Map,this.hasTreeAnimated=!1,this.updateScheduled=!1,this.scheduleUpdate=()=>this.update(),this.projectionUpdateScheduled=!1,this.checkUpdateFailed=()=>{this.isUpdating&&(this.isUpdating=!1,this.clearAllSnapshots())},this.updateProjection=()=>{this.projectionUpdateScheduled=!1,_i&&(ta.totalNodes=ta.resolvedTargetDeltas=ta.recalculatedProjection=0),this.nodes.forEach(YI),this.nodes.forEach(tA),this.nodes.forEach(nA),this.nodes.forEach(QI),_i&&window.MotionDebug.record(ta)},this.resolvedRelativeTargetAt=0,this.hasProjected=!1,this.isVisible=!0,this.animationProgress=0,this.sharedNodes=new Map,this.latestValues=h,this.root=p?p.root||p:this,this.path=p?[...p.path,p]:[],this.parent=p,this.depth=p?p.depth+1:0;for(let m=0;m<this.path.length;m++)this.path[m].shouldResetTransform=!0;this.root===this&&(this.nodes=new VI)}addEventListener(h,p){return this.eventHandlers.has(h)||this.eventHandlers.set(h,new Rd),this.eventHandlers.get(h).add(p)}notifyListeners(h,...p){const m=this.eventHandlers.get(h);m&&m.notify(...p)}hasListeners(h){return this.eventHandlers.has(h)}mount(h,p=this.root.hasTreeAnimated){if(this.instance)return;this.isSVG=LI(h),this.instance=h;const{layoutId:m,layout:f,visualElement:y}=this.options;if(y&&!y.current&&y.mount(h),this.root.nodes.add(this),this.parent&&this.parent.children.add(this),p&&(f||m)&&(this.isLayoutDirty=!0),t){let v;const b=()=>this.root.updateBlockedByResize=!1;t(h,()=>{this.root.updateBlockedByResize=!0,v&&v(),v=jI(b,250),Os.hasAnimatedSinceResize&&(Os.hasAnimatedSinceResize=!1,this.nodes.forEach(of))})}m&&this.root.registerSharedNode(m,this),this.options.animate!==!1&&y&&(m||f)&&this.addEventListener("didUpdate",({delta:v,hasLayoutChanged:b,hasRelativeTargetChanged:T,layout:M})=>{if(this.isTreeAnimationBlocked()){this.target=void 0,this.relativeTarget=void 0;return}const I=this.options.transition||y.getDefaultTransition()||rA,{onLayoutAnimationStart:A,onLayoutAnimationComplete:R}=y.getProps(),j=!this.targetLayout||!Ly(this.targetLayout,M)||T,V=!b&&T;if(this.options.layoutRoot||this.resumeFrom&&this.resumeFrom.instance||V||b&&(j||!this.currentAnimation)){this.resumeFrom&&(this.resumingFrom=this.resumeFrom,this.resumingFrom.resumingFrom=void 0),this.setAnimationOrigin(v,V);const B={...Td(I,"layout"),onPlay:A,onComplete:R};(y.shouldReduceMotion||this.options.layoutRoot)&&(B.delay=0,B.type=!1),this.startAnimation(B)}else b||of(this),this.isLead()&&this.options.onExitComplete&&this.options.onExitComplete();this.targetLayout=M})}unmount(){this.options.layoutId&&this.willUpdate(),this.root.nodes.remove(this);const h=this.getStack();h&&h.remove(this),this.parent&&this.parent.children.delete(this),this.instance=void 0,Qt(this.updateProjection)}blockUpdate(){this.updateManuallyBlocked=!0}unblockUpdate(){this.updateManuallyBlocked=!1}isUpdateBlocked(){return this.updateManuallyBlocked||this.updateBlockedByResize}isTreeAnimationBlocked(){return this.isAnimationBlocked||this.parent&&this.parent.isTreeAnimationBlocked()||!1}startUpdate(){this.isUpdateBlocked()||(this.isUpdating=!0,this.nodes&&this.nodes.forEach(aA),this.animationId++)}getTransformTemplate(){const{visualElement:h}=this.options;return h&&h.getProps().transformTemplate}willUpdate(h=!0){if(this.root.hasTreeAnimated=!0,this.root.isUpdateBlocked()){this.options.onExitComplete&&this.options.onExitComplete();return}if(window.MotionCancelOptimisedAnimation&&!this.hasCheckedOptimisedAppear&&Dy(this),!this.root.isUpdating&&this.root.startUpdate(),this.isLayoutDirty)return;this.isLayoutDirty=!0;for(let y=0;y<this.path.length;y++){const v=this.path[y];v.shouldResetTransform=!0,v.updateScroll("snapshot"),v.options.layoutRoot&&v.willUpdate(!1)}const{layoutId:p,layout:m}=this.options;if(p===void 0&&!m)return;const f=this.getTransformTemplate();this.prevTransformTemplateValue=f?f(this.latestValues,""):void 0,this.updateSnapshot(),h&&this.notifyListeners("willUpdate")}update(){if(this.updateScheduled=!1,this.isUpdateBlocked()){this.unblockUpdate(),this.clearAllSnapshots(),this.nodes.forEach(af);return}this.isUpdating||this.nodes.forEach(ZI),this.isUpdating=!1,this.nodes.forEach(eA),this.nodes.forEach(GI),this.nodes.forEach($I),this.clearAllSnapshots();const p=Yt.now();Be.delta=Xt(0,1e3/60,p-Be.timestamp),Be.timestamp=p,Be.isProcessing=!0,uc.update.process(Be),uc.preRender.process(Be),uc.render.process(Be),Be.isProcessing=!1}didUpdate(){this.updateScheduled||(this.updateScheduled=!0,md.read(this.scheduleUpdate))}clearAllSnapshots(){this.nodes.forEach(XI),this.sharedNodes.forEach(iA)}scheduleUpdateProjection(){this.projectionUpdateScheduled||(this.projectionUpdateScheduled=!0,fe.preRender(this.updateProjection,!1,!0))}scheduleCheckAfterUnmount(){fe.postRender(()=>{this.isLayoutDirty?this.root.didUpdate():this.root.checkUpdateFailed()})}updateSnapshot(){this.snapshot||!this.instance||(this.snapshot=this.measure())}updateLayout(){if(!this.instance||(this.updateScroll(),!(this.options.alwaysMeasureLayout&&this.isLead())&&!this.isLayoutDirty))return;if(this.resumeFrom&&!this.resumeFrom.instance)for(let m=0;m<this.path.length;m++)this.path[m].updateScroll();const h=this.layout;this.layout=this.measure(!1),this.layoutCorrected=Ue(),this.isLayoutDirty=!1,this.projectionDelta=void 0,this.notifyListeners("measure",this.layout.layoutBox);const{visualElement:p}=this.options;p&&p.notify("LayoutMeasure",this.layout.layoutBox,h?h.layoutBox:void 0)}updateScroll(h="measure"){let p=!!(this.options.layoutScroll&&this.instance);if(this.scroll&&this.scroll.animationId===this.root.animationId&&this.scroll.phase===h&&(p=!1),p){const m=s(this.instance);this.scroll={animationId:this.root.animationId,phase:h,isRoot:m,offset:i(this.instance),wasRoot:this.scroll?this.scroll.isRoot:m}}}resetTransform(){if(!r)return;const h=this.isLayoutDirty||this.shouldResetTransform||this.options.alwaysMeasureLayout,p=this.projectionDelta&&!Ry(this.projectionDelta),m=this.getTransformTemplate(),f=m?m(this.latestValues,""):void 0,y=f!==this.prevTransformTemplateValue;h&&(p||ea(this.latestValues)||y)&&(r(this.instance,f),this.shouldResetTransform=!1,this.scheduleRender())}measure(h=!0){const p=this.measurePageBox();let m=this.removeElementScroll(p);return h&&(m=this.removeTransform(m)),lA(m),{animationId:this.root.animationId,measuredBox:p,layoutBox:m,latestValues:{},source:this.id}}measurePageBox(){var h;const{visualElement:p}=this.options;if(!p)return Ue();const m=p.measureViewportBox();if(!(((h=this.scroll)===null||h===void 0?void 0:h.wasRoot)||this.path.some(cA))){const{scroll:y}=this.root;y&&(Oa(m.x,y.offset.x),Oa(m.y,y.offset.y))}return m}removeElementScroll(h){var p;const m=Ue();if(At(m,h),!((p=this.scroll)===null||p===void 0)&&p.wasRoot)return m;for(let f=0;f<this.path.length;f++){const y=this.path[f],{scroll:v,options:b}=y;y!==this.root&&v&&b.layoutScroll&&(v.wasRoot&&At(m,h),Oa(m.x,v.offset.x),Oa(m.y,v.offset.y))}return m}applyTransform(h,p=!1){const m=Ue();At(m,h);for(let f=0;f<this.path.length;f++){const y=this.path[f];!p&&y.options.layoutScroll&&y.scroll&&y!==y.root&&Ua(m,{x:-y.scroll.offset.x,y:-y.scroll.offset.y}),ea(y.latestValues)&&Ua(m,y.latestValues)}return ea(this.latestValues)&&Ua(m,this.latestValues),m}removeTransform(h){const p=Ue();At(p,h);for(let m=0;m<this.path.length;m++){const f=this.path[m];if(!f.instance||!ea(f.latestValues))continue;_c(f.latestValues)&&f.updateSnapshot();const y=Ue(),v=f.measurePageBox();At(y,v),Ym(p,f.latestValues,f.snapshot?f.snapshot.layoutBox:void 0,y)}return ea(this.latestValues)&&Ym(p,this.latestValues),p}setTargetDelta(h){this.targetDelta=h,this.root.scheduleUpdateProjection(),this.isProjectionDirty=!0}setOptions(h){this.options={...this.options,...h,crossfade:h.crossfade!==void 0?h.crossfade:!0}}clearMeasurements(){this.scroll=void 0,this.layout=void 0,this.snapshot=void 0,this.prevTransformTemplateValue=void 0,this.targetDelta=void 0,this.target=void 0,this.isLayoutDirty=!1}forceRelativeParentToResolveTarget(){this.relativeParent&&this.relativeParent.resolvedRelativeTargetAt!==Be.timestamp&&this.relativeParent.resolveTargetDelta(!0)}resolveTargetDelta(h=!1){var p;const m=this.getLead();this.isProjectionDirty||(this.isProjectionDirty=m.isProjectionDirty),this.isTransformDirty||(this.isTransformDirty=m.isTransformDirty),this.isSharedProjectionDirty||(this.isSharedProjectionDirty=m.isSharedProjectionDirty);const f=!!this.resumingFrom||this!==m;if(!(h||f&&this.isSharedProjectionDirty||this.isProjectionDirty||!((p=this.parent)===null||p===void 0)&&p.isProjectionDirty||this.attemptToResolveRelativeTarget||this.root.updateBlockedByResize))return;const{layout:v,layoutId:b}=this.options;if(!(!this.layout||!(v||b))){if(this.resolvedRelativeTargetAt=Be.timestamp,!this.targetDelta&&!this.relativeTarget){const T=this.getClosestProjectingParent();T&&T.layout&&this.animationProgress!==1?(this.relativeParent=T,this.forceRelativeParentToResolveTarget(),this.relativeTarget=Ue(),this.relativeTargetOrigin=Ue(),$i(this.relativeTargetOrigin,this.layout.layoutBox,T.layout.layoutBox),At(this.relativeTarget,this.relativeTargetOrigin)):this.relativeParent=this.relativeTarget=void 0}if(!(!this.relativeTarget&&!this.targetDelta)){if(this.target||(this.target=Ue(),this.targetWithTransforms=Ue()),this.relativeTarget&&this.relativeTargetOrigin&&this.relativeParent&&this.relativeParent.target?(this.forceRelativeParentToResolveTarget(),uI(this.target,this.relativeTarget,this.relativeParent.target)):this.targetDelta?(this.resumingFrom?this.target=this.applyTransform(this.layout.layoutBox):At(this.target,this.layout.layoutBox),Cy(this.target,this.targetDelta)):At(this.target,this.layout.layoutBox),this.attemptToResolveRelativeTarget){this.attemptToResolveRelativeTarget=!1;const T=this.getClosestProjectingParent();T&&!!T.resumingFrom==!!this.resumingFrom&&!T.options.layoutScroll&&T.target&&this.animationProgress!==1?(this.relativeParent=T,this.forceRelativeParentToResolveTarget(),this.relativeTarget=Ue(),this.relativeTargetOrigin=Ue(),$i(this.relativeTargetOrigin,this.target,T.target),At(this.relativeTarget,this.relativeTargetOrigin)):this.relativeParent=this.relativeTarget=void 0}_i&&ta.resolvedTargetDeltas++}}}getClosestProjectingParent(){if(!(!this.parent||_c(this.parent.latestValues)||Ty(this.parent.latestValues)))return this.parent.isProjecting()?this.parent:this.parent.getClosestProjectingParent()}isProjecting(){return!!((this.relativeTarget||this.targetDelta||this.options.layoutRoot)&&this.layout)}calcProjection(){var h;const p=this.getLead(),m=!!this.resumingFrom||this!==p;let f=!0;if((this.isProjectionDirty||!((h=this.parent)===null||h===void 0)&&h.isProjectionDirty)&&(f=!1),m&&(this.isSharedProjectionDirty||this.isTransformDirty)&&(f=!1),this.resolvedRelativeTargetAt===Be.timestamp&&(f=!1),f)return;const{layout:y,layoutId:v}=this.options;if(this.isTreeAnimating=!!(this.parent&&this.parent.isTreeAnimating||this.currentAnimation||this.pendingAnimation),this.isTreeAnimating||(this.targetDelta=this.relativeTarget=void 0),!this.layout||!(y||v))return;At(this.layoutCorrected,this.layout.layoutBox);const b=this.treeScale.x,T=this.treeScale.y;kI(this.layoutCorrected,this.treeScale,this.path,m),p.layout&&!p.target&&(this.treeScale.x!==1||this.treeScale.y!==1)&&(p.target=p.layout.layoutBox,p.targetWithTransforms=Ue());const{target:M}=p;if(!M){this.prevProjectionDelta&&(this.createProjectionDeltas(),this.scheduleRender());return}!this.projectionDelta||!this.prevProjectionDelta?this.createProjectionDeltas():(Jm(this.prevProjectionDelta.x,this.projectionDelta.x),Jm(this.prevProjectionDelta.y,this.projectionDelta.y)),Gi(this.projectionDelta,this.layoutCorrected,M,this.latestValues),(this.treeScale.x!==b||this.treeScale.y!==T||!tf(this.projectionDelta.x,this.prevProjectionDelta.x)||!tf(this.projectionDelta.y,this.prevProjectionDelta.y))&&(this.hasProjected=!0,this.scheduleRender(),this.notifyListeners("projectionUpdate",M)),_i&&ta.recalculatedProjection++}hide(){this.isVisible=!1}show(){this.isVisible=!0}scheduleRender(h=!0){var p;if((p=this.options.visualElement)===null||p===void 0||p.scheduleRender(),h){const m=this.getStack();m&&m.scheduleRender()}this.resumingFrom&&!this.resumingFrom.instance&&(this.resumingFrom=void 0)}createProjectionDeltas(){this.prevProjectionDelta=ja(),this.projectionDelta=ja(),this.projectionDeltaWithTransform=ja()}setAnimationOrigin(h,p=!1){const m=this.snapshot,f=m?m.latestValues:{},y={...this.latestValues},v=ja();(!this.relativeParent||!this.relativeParent.options.layoutRoot)&&(this.relativeTarget=this.relativeTargetOrigin=void 0),this.attemptToResolveRelativeTarget=!p;const b=Ue(),T=m?m.source:void 0,M=this.layout?this.layout.source:void 0,I=T!==M,A=this.getStack(),R=!A||A.members.length<=1,j=!!(I&&!R&&this.options.crossfade===!0&&!this.path.some(sA));this.animationProgress=0;let V;this.mixTargetDelta=B=>{const N=B/1e3;sf(v.x,h.x,N),sf(v.y,h.y,N),this.setTargetDelta(v),this.relativeTarget&&this.relativeTargetOrigin&&this.layout&&this.relativeParent&&this.relativeParent.layout&&($i(b,this.layout.layoutBox,this.relativeParent.layout.layoutBox),oA(this.relativeTarget,this.relativeTargetOrigin,b,N),V&&BI(this.relativeTarget,V)&&(this.isProjectionDirty=!1),V||(V=Ue()),At(V,this.relativeTarget)),I&&(this.animationValues=y,UI(y,f,this.latestValues,N,j,R)),this.root.scheduleUpdateProjection(),this.scheduleRender(),this.animationProgress=N},this.mixTargetDelta(this.options.layoutRoot?1e3:0)}startAnimation(h){this.notifyListeners("animationStart"),this.currentAnimation&&this.currentAnimation.stop(),this.resumingFrom&&this.resumingFrom.currentAnimation&&this.resumingFrom.currentAnimation.stop(),this.pendingAnimation&&(Qt(this.pendingAnimation),this.pendingAnimation=void 0),this.pendingAnimation=fe.update(()=>{Os.hasAnimatedSinceResize=!0,this.currentAnimation=RI(0,nf,{...h,onUpdate:p=>{this.mixTargetDelta(p),h.onUpdate&&h.onUpdate(p)},onComplete:()=>{h.onComplete&&h.onComplete(),this.completeAnimation()}}),this.resumingFrom&&(this.resumingFrom.currentAnimation=this.currentAnimation),this.pendingAnimation=void 0})}completeAnimation(){this.resumingFrom&&(this.resumingFrom.currentAnimation=void 0,this.resumingFrom.preserveOpacity=void 0);const h=this.getStack();h&&h.exitAnimationComplete(),this.resumingFrom=this.currentAnimation=this.animationValues=void 0,this.notifyListeners("animationComplete")}finishAnimation(){this.currentAnimation&&(this.mixTargetDelta&&this.mixTargetDelta(nf),this.currentAnimation.stop()),this.completeAnimation()}applyTransformsToTarget(){const h=this.getLead();let{targetWithTransforms:p,target:m,layout:f,latestValues:y}=h;if(!(!p||!m||!f)){if(this!==h&&this.layout&&f&&jy(this.options.animationType,this.layout.layoutBox,f.layoutBox)){m=this.target||Ue();const v=bt(this.layout.layoutBox.x);m.x.min=h.target.x.min,m.x.max=m.x.min+v;const b=bt(this.layout.layoutBox.y);m.y.min=h.target.y.min,m.y.max=m.y.min+b}At(p,m),Ua(p,y),Gi(this.projectionDeltaWithTransform,this.layoutCorrected,p,y)}}registerSharedNode(h,p){this.sharedNodes.has(h)||this.sharedNodes.set(h,new HI),this.sharedNodes.get(h).add(p);const f=p.options.initialPromotionConfig;p.promote({transition:f?f.transition:void 0,preserveFollowOpacity:f&&f.shouldPreserveFollowOpacity?f.shouldPreserveFollowOpacity(p):void 0})}isLead(){const h=this.getStack();return h?h.lead===this:!0}getLead(){var h;const{layoutId:p}=this.options;return p?((h=this.getStack())===null||h===void 0?void 0:h.lead)||this:this}getPrevLead(){var h;const{layoutId:p}=this.options;return p?(h=this.getStack())===null||h===void 0?void 0:h.prevLead:void 0}getStack(){const{layoutId:h}=this.options;if(h)return this.root.sharedNodes.get(h)}promote({needsReset:h,transition:p,preserveFollowOpacity:m}={}){const f=this.getStack();f&&f.promote(this,m),h&&(this.projectionDelta=void 0,this.needsReset=!0),p&&this.setOptions({transition:p})}relegate(){const h=this.getStack();return h?h.relegate(this):!1}resetSkewAndRotation(){const{visualElement:h}=this.options;if(!h)return;let p=!1;const{latestValues:m}=h;if((m.z||m.rotate||m.rotateX||m.rotateY||m.rotateZ||m.skewX||m.skewY)&&(p=!0),!p)return;const f={};m.z&&xc("z",h,f,this.animationValues);for(let y=0;y<kc.length;y++)xc(`rotate${kc[y]}`,h,f,this.animationValues),xc(`skew${kc[y]}`,h,f,this.animationValues);h.render();for(const y in f)h.setStaticValue(y,f[y]),this.animationValues&&(this.animationValues[y]=f[y]);h.scheduleRender()}getProjectionStyles(h){var p,m;if(!this.instance||this.isSVG)return;if(!this.isVisible)return KI;const f={visibility:""},y=this.getTransformTemplate();if(this.needsReset)return this.needsReset=!1,f.opacity="",f.pointerEvents=Vs(h==null?void 0:h.pointerEvents)||"",f.transform=y?y(this.latestValues,""):"none",f;const v=this.getLead();if(!this.projectionDelta||!this.layout||!v.target){const I={};return this.options.layoutId&&(I.opacity=this.latestValues.opacity!==void 0?this.latestValues.opacity:1,I.pointerEvents=Vs(h==null?void 0:h.pointerEvents)||""),this.hasProjected&&!ea(this.latestValues)&&(I.transform=y?y({},""):"none",this.hasProjected=!1),I}const b=v.animationValues||v.latestValues;this.applyTransformsToTarget(),f.transform=_I(this.projectionDeltaWithTransform,this.treeScale,b),y&&(f.transform=y(b,f.transform));const{x:T,y:M}=this.projectionDelta;f.transformOrigin=`${T.origin*100}% ${M.origin*100}% 0`,v.animationValues?f.opacity=v===this?(m=(p=b.opacity)!==null&&p!==void 0?p:this.latestValues.opacity)!==null&&m!==void 0?m:1:this.preserveOpacity?this.latestValues.opacity:b.opacityExit:f.opacity=v===this?b.opacity!==void 0?b.opacity:"":b.opacityExit!==void 0?b.opacityExit:0;for(const I in zs){if(b[I]===void 0)continue;const{correct:A,applyTo:R}=zs[I],j=f.transform==="none"?b[I]:A(b[I],v);if(R){const V=R.length;for(let B=0;B<V;B++)f[R[B]]=j}else f[I]=j}return this.options.layoutId&&(f.pointerEvents=v===this?Vs(h==null?void 0:h.pointerEvents)||"":"none"),f}clearSnapshot(){this.resumeFrom=this.snapshot=void 0}resetTree(){this.root.nodes.forEach(h=>{var p;return(p=h.currentAnimation)===null||p===void 0?void 0:p.stop()}),this.root.nodes.forEach(af),this.root.sharedNodes.clear()}}}function GI(t){t.updateLayout()}function $I(t){var a;const i=((a=t.resumeFrom)===null||a===void 0?void 0:a.snapshot)||t.snapshot;if(t.isLead()&&t.layout&&i&&t.hasListeners("didUpdate")){const{layoutBox:s,measuredBox:r}=t.layout,{animationType:c}=t.options,h=i.source!==t.layout.source;c==="size"?Et(v=>{const b=h?i.measuredBox[v]:i.layoutBox[v],T=bt(b);b.min=s[v].min,b.max=b.min+T}):jy(c,i.layoutBox,s)&&Et(v=>{const b=h?i.measuredBox[v]:i.layoutBox[v],T=bt(s[v]);b.max=b.min+T,t.relativeTarget&&!t.currentAnimation&&(t.isProjectionDirty=!0,t.relativeTarget[v].max=t.relativeTarget[v].min+T)});const p=ja();Gi(p,s,i.layoutBox);const m=ja();h?Gi(m,t.applyTransform(r,!0),i.measuredBox):Gi(m,s,i.layoutBox);const f=!Ry(p);let y=!1;if(!t.resumeFrom){const v=t.getClosestProjectingParent();if(v&&!v.resumeFrom){const{snapshot:b,layout:T}=v;if(b&&T){const M=Ue();$i(M,i.layoutBox,b.layoutBox);const I=Ue();$i(I,s,T.layoutBox),Ly(M,I)||(y=!0),v.options.layoutRoot&&(t.relativeTarget=I,t.relativeTargetOrigin=M,t.relativeParent=v)}}}t.notifyListeners("didUpdate",{layout:s,snapshot:i,delta:m,layoutDelta:p,hasLayoutChanged:f,hasRelativeTargetChanged:y})}else if(t.isLead()){const{onExitComplete:s}=t.options;s&&s()}t.options.transition=void 0}function YI(t){_i&&ta.totalNodes++,t.parent&&(t.isProjecting()||(t.isProjectionDirty=t.parent.isProjectionDirty),t.isSharedProjectionDirty||(t.isSharedProjectionDirty=!!(t.isProjectionDirty||t.parent.isProjectionDirty||t.parent.isSharedProjectionDirty)),t.isTransformDirty||(t.isTransformDirty=t.parent.isTransformDirty))}function QI(t){t.isProjectionDirty=t.isSharedProjectionDirty=t.isTransformDirty=!1}function XI(t){t.clearSnapshot()}function af(t){t.clearMeasurements()}function ZI(t){t.isLayoutDirty=!1}function eA(t){const{visualElement:a}=t.options;a&&a.getProps().onBeforeLayoutMeasure&&a.notify("BeforeLayoutMeasure"),t.resetTransform()}function of(t){t.finishAnimation(),t.targetDelta=t.relativeTarget=t.target=void 0,t.isProjectionDirty=!0}function tA(t){t.resolveTargetDelta()}function nA(t){t.calcProjection()}function aA(t){t.resetSkewAndRotation()}function iA(t){t.removeLeadSnapshot()}function sf(t,a,i){t.translate=Re(a.translate,0,i),t.scale=Re(a.scale,1,i),t.origin=a.origin,t.originPoint=a.originPoint}function rf(t,a,i,s){t.min=Re(a.min,i.min,s),t.max=Re(a.max,i.max,s)}function oA(t,a,i,s){rf(t.x,a.x,i.x,s),rf(t.y,a.y,i.y,s)}function sA(t){return t.animationValues&&t.animationValues.opacityExit!==void 0}const rA={duration:.45,ease:[.4,0,.1,1]},lf=t=>typeof navigator<"u"&&navigator.userAgent&&navigator.userAgent.toLowerCase().includes(t),cf=lf("applewebkit/")&&!lf("chrome/")?Math.round:rt;function df(t){t.min=cf(t.min),t.max=cf(t.max)}function lA(t){df(t.x),df(t.y)}function jy(t,a,i){return t==="position"||t==="preserve-aspect"&&!hI(ef(a),ef(i),.2)}function cA(t){var a;return t!==t.root&&((a=t.scroll)===null||a===void 0?void 0:a.wasRoot)}const dA=Vy({attachResizeListener:(t,a)=>no(t,"resize",a),measureScroll:()=>({x:document.documentElement.scrollLeft||document.body.scrollLeft,y:document.documentElement.scrollTop||document.body.scrollTop}),checkIsScrollRoot:()=>!0}),Sc={current:void 0},Oy=Vy({measureScroll:t=>({x:t.scrollLeft,y:t.scrollTop}),defaultParent:()=>{if(!Sc.current){const t=new dA({});t.mount(window),t.setOptions({layoutScroll:!0}),Sc.current=t}return Sc.current},resetTransform:(t,a)=>{t.style.transform=a!==void 0?a:"none"},checkIsScrollRoot:t=>window.getComputedStyle(t).position==="fixed"}),hA={pan:{Feature:II},drag:{Feature:MI,ProjectionNode:Oy,MeasureLayout:Ay}};function hf(t,a,i){const{props:s}=t;t.animationState&&s.whileHover&&t.animationState.setActive("whileHover",i==="Start");const r="onHover"+i,c=s[r];c&&fe.postRender(()=>c(a,po(a)))}class uA extends Un{mount(){const{current:a}=this.node;a&&(this.unmount=u0(a,i=>(hf(this.node,i,"Start"),s=>hf(this.node,s,"End"))))}unmount(){}}class pA extends Un{constructor(){super(...arguments),this.isActive=!1}onFocus(){let a=!1;try{a=this.node.current.matches(":focus-visible")}catch{a=!0}!a||!this.node.animationState||(this.node.animationState.setActive("whileFocus",!0),this.isActive=!0)}onBlur(){!this.isActive||!this.node.animationState||(this.node.animationState.setActive("whileFocus",!1),this.isActive=!1)}mount(){this.unmount=uo(no(this.node.current,"focus",()=>this.onFocus()),no(this.node.current,"blur",()=>this.onBlur()))}unmount(){}}function uf(t,a,i){const{props:s}=t;t.animationState&&s.whileTap&&t.animationState.setActive("whileTap",i==="Start");const r="onTap"+(i==="End"?"":i),c=s[r];c&&fe.postRender(()=>c(a,po(a)))}class mA extends Un{mount(){const{current:a}=this.node;a&&(this.unmount=g0(a,i=>(uf(this.node,i,"Start"),(s,{success:r})=>uf(this.node,s,r?"End":"Cancel")),{useGlobalTarget:this.node.props.globalTapTarget}))}unmount(){}}const Jc=new WeakMap,Tc=new WeakMap,fA=t=>{const a=Jc.get(t.target);a&&a(t)},gA=t=>{t.forEach(fA)};function yA({root:t,...a}){const i=t||document;Tc.has(i)||Tc.set(i,{});const s=Tc.get(i),r=JSON.stringify(a);return s[r]||(s[r]=new IntersectionObserver(gA,{root:t,...a})),s[r]}function wA(t,a,i){const s=yA(a);return Jc.set(t,i),s.observe(t),()=>{Jc.delete(t),s.unobserve(t)}}const vA={some:0,all:1};class bA extends Un{constructor(){super(...arguments),this.hasEnteredView=!1,this.isInView=!1}startObserver(){this.unmount();const{viewport:a={}}=this.node.getProps(),{root:i,margin:s,amount:r="some",once:c}=a,h={root:i?i.current:void 0,rootMargin:s,threshold:typeof r=="number"?r:vA[r]},p=m=>{const{isIntersecting:f}=m;if(this.isInView===f||(this.isInView=f,c&&!f&&this.hasEnteredView))return;f&&(this.hasEnteredView=!0),this.node.animationState&&this.node.animationState.setActive("whileInView",f);const{onViewportEnter:y,onViewportLeave:v}=this.node.getProps(),b=f?y:v;b&&b(m)};return wA(this.node.current,h,p)}mount(){this.startObserver()}update(){if(typeof IntersectionObserver>"u")return;const{props:a,prevProps:i}=this.node;["amount","margin","root"].some(kA(a,i))&&this.startObserver()}unmount(){}}function kA({viewport:t={}},{viewport:a={}}={}){return i=>t[i]!==a[i]}const xA={inView:{Feature:bA},tap:{Feature:mA},focus:{Feature:pA},hover:{Feature:uA}},SA={layout:{ProjectionNode:Oy,MeasureLayout:Ay}},Gc={current:null},Uy={current:!1};function TA(){if(Uy.current=!0,!!ld)if(window.matchMedia){const t=window.matchMedia("(prefers-reduced-motion)"),a=()=>Gc.current=t.matches;t.addListener(a),a()}else Gc.current=!1}const CA=[...sy,at,On],MA=t=>CA.find(oy(t)),pf=new WeakMap;function IA(t,a,i){for(const s in a){const r=a[s],c=i[s];if(Ye(r))t.addValue(s,r);else if(Ye(c))t.addValue(s,Gt(r,{owner:t}));else if(c!==r)if(t.hasValue(s)){const h=t.getValue(s);h.liveStyle===!0?h.jump(r):h.hasAnimated||h.set(r)}else{const h=t.getStaticValue(s);t.addValue(s,Gt(h!==void 0?h:r,{owner:t}))}}for(const s in i)a[s]===void 0&&t.removeValue(s);return a}const mf=["AnimationStart","AnimationComplete","Update","BeforeLayoutMeasure","LayoutMeasure","LayoutAnimationStart","LayoutAnimationComplete"];class AA{scrapeMotionValuesFromProps(a,i,s){return{}}constructor({parent:a,props:i,presenceContext:s,reducedMotionConfig:r,blockInitialAnimation:c,visualState:h},p={}){this.current=null,this.children=new Set,this.isVariantNode=!1,this.isControllingVariants=!1,this.shouldReduceMotion=null,this.values=new Map,this.KeyframeResolver=Fd,this.features={},this.valueSubscriptions=new Map,this.prevMotionValues={},this.events={},this.propEventSubscriptions={},this.notifyUpdate=()=>this.notify("Update",this.latestValues),this.render=()=>{this.current&&(this.triggerBuild(),this.renderInstance(this.current,this.renderState,this.props.style,this.projection))},this.renderScheduledAt=0,this.scheduleRender=()=>{const T=Yt.now();this.renderScheduledAt<T&&(this.renderScheduledAt=T,fe.render(this.render,!1,!0))};const{latestValues:m,renderState:f,onUpdate:y}=h;this.onUpdate=y,this.latestValues=m,this.baseTarget={...m},this.initialValues=i.initial?{...m}:{},this.renderState=f,this.parent=a,this.props=i,this.presenceContext=s,this.depth=a?a.depth+1:0,this.reducedMotionConfig=r,this.options=p,this.blockInitialAnimation=!!c,this.isControllingVariants=or(i),this.isVariantNode=mg(i),this.isVariantNode&&(this.variantChildren=new Set),this.manuallyAnimateOnMount=!!(a&&a.current);const{willChange:v,...b}=this.scrapeMotionValuesFromProps(i,{},this);for(const T in b){const M=b[T];m[T]!==void 0&&Ye(M)&&M.set(m[T],!1)}}mount(a){this.current=a,pf.set(a,this),this.projection&&!this.projection.instance&&this.projection.mount(a),this.parent&&this.isVariantNode&&!this.isControllingVariants&&(this.removeFromVariantTree=this.parent.addVariantChild(this)),this.values.forEach((i,s)=>this.bindToMotionValue(s,i)),Uy.current||TA(),this.shouldReduceMotion=this.reducedMotionConfig==="never"?!1:this.reducedMotionConfig==="always"?!0:Gc.current,this.parent&&this.parent.children.add(this),this.update(this.props,this.presenceContext)}unmount(){pf.delete(this.current),this.projection&&this.projection.unmount(),Qt(this.notifyUpdate),Qt(this.render),this.valueSubscriptions.forEach(a=>a()),this.valueSubscriptions.clear(),this.removeFromVariantTree&&this.removeFromVariantTree(),this.parent&&this.parent.children.delete(this);for(const a in this.events)this.events[a].clear();for(const a in this.features){const i=this.features[a];i&&(i.unmount(),i.isMounted=!1)}this.current=null}bindToMotionValue(a,i){this.valueSubscriptions.has(a)&&this.valueSubscriptions.get(a)();const s=oa.has(a),r=i.on("change",p=>{this.latestValues[a]=p,this.props.onUpdate&&fe.preRender(this.notifyUpdate),s&&this.projection&&(this.projection.isTransformDirty=!0)}),c=i.on("renderRequest",this.scheduleRender);let h;window.MotionCheckAppearSync&&(h=window.MotionCheckAppearSync(this,a,i)),this.valueSubscriptions.set(a,()=>{r(),c(),h&&h(),i.owner&&i.stop()})}sortNodePosition(a){return!this.current||!this.sortInstanceNodePosition||this.type!==a.type?0:this.sortInstanceNodePosition(this.current,a.current)}updateFeatures(){let a="animation";for(a in Na){const i=Na[a];if(!i)continue;const{isEnabled:s,Feature:r}=i;if(!this.features[a]&&r&&s(this.props)&&(this.features[a]=new r(this)),this.features[a]){const c=this.features[a];c.isMounted?c.update():(c.mount(),c.isMounted=!0)}}}triggerBuild(){this.build(this.renderState,this.latestValues,this.props)}measureViewportBox(){return this.current?this.measureInstanceViewportBox(this.current,this.props):Ue()}getStaticValue(a){return this.latestValues[a]}setStaticValue(a,i){this.latestValues[a]=i}update(a,i){(a.transformTemplate||this.props.transformTemplate)&&this.scheduleRender(),this.prevProps=this.props,this.props=a,this.prevPresenceContext=this.presenceContext,this.presenceContext=i;for(let s=0;s<mf.length;s++){const r=mf[s];this.propEventSubscriptions[r]&&(this.propEventSubscriptions[r](),delete this.propEventSubscriptions[r]);const c="on"+r,h=a[c];h&&(this.propEventSubscriptions[r]=this.on(r,h))}this.prevMotionValues=IA(this,this.scrapeMotionValuesFromProps(a,this.prevProps,this),this.prevMotionValues),this.handleChildMotionValue&&this.handleChildMotionValue(),this.onUpdate&&this.onUpdate(this)}getProps(){return this.props}getVariant(a){return this.props.variants?this.props.variants[a]:void 0}getDefaultTransition(){return this.props.transition}getTransformPagePoint(){return this.props.transformPagePoint}getClosestVariantNode(){return this.isVariantNode?this:this.parent?this.parent.getClosestVariantNode():void 0}addVariantChild(a){const i=this.getClosestVariantNode();if(i)return i.variantChildren&&i.variantChildren.add(a),()=>i.variantChildren.delete(a)}addValue(a,i){const s=this.values.get(a);i!==s&&(s&&this.removeValue(a),this.bindToMotionValue(a,i),this.values.set(a,i),this.latestValues[a]=i.get())}removeValue(a){this.values.delete(a);const i=this.valueSubscriptions.get(a);i&&(i(),this.valueSubscriptions.delete(a)),delete this.latestValues[a],this.removeValueFromRenderState(a,this.renderState)}hasValue(a){return this.values.has(a)}getValue(a,i){if(this.props.values&&this.props.values[a])return this.props.values[a];let s=this.values.get(a);return s===void 0&&i!==void 0&&(s=Gt(i===null?void 0:i,{owner:this}),this.addValue(a,s)),s}readValue(a,i){var s;let r=this.latestValues[a]!==void 0||!this.current?this.latestValues[a]:(s=this.getBaseTargetFromProps(this.props,a))!==null&&s!==void 0?s:this.readValueFromInstance(this.current,a,this.options);return r!=null&&(typeof r=="string"&&(ay(r)||Gg(r))?r=parseFloat(r):!MA(r)&&On.test(i)&&(r=ey(a,i)),this.setBaseTarget(a,Ye(r)?r.get():r)),Ye(r)?r.get():r}setBaseTarget(a,i){this.baseTarget[a]=i}getBaseTarget(a){var i;const{initial:s}=this.props;let r;if(typeof s=="string"||typeof s=="object"){const h=gd(this.props,s,(i=this.presenceContext)===null||i===void 0?void 0:i.custom);h&&(r=h[a])}if(s&&r!==void 0)return r;const c=this.getBaseTargetFromProps(this.props,a);return c!==void 0&&!Ye(c)?c:this.initialValues[a]!==void 0&&r===void 0?void 0:this.baseTarget[a]}on(a,i){return this.events[a]||(this.events[a]=new Rd),this.events[a].add(i)}notify(a,...i){this.events[a]&&this.events[a].notify(...i)}}class Fy extends AA{constructor(){super(...arguments),this.KeyframeResolver=ry}sortInstanceNodePosition(a,i){return a.compareDocumentPosition(i)&2?1:-1}getBaseTargetFromProps(a,i){return a.style?a.style[i]:void 0}removeValueFromRenderState(a,{vars:i,style:s}){delete i[a],delete s[a]}handleChildMotionValue(){this.childSubscription&&(this.childSubscription(),delete this.childSubscription);const{children:a}=this.props;Ye(a)&&(this.childSubscription=a.on("change",i=>{this.current&&(this.current.textContent=`${i}`)}))}}function EA(t){return window.getComputedStyle(t)}class PA extends Fy{constructor(){super(...arguments),this.type="html",this.renderInstance=Sg}readValueFromInstance(a,i){if(oa.has(i)){const s=Ud(i);return s&&s.default||0}else{const s=EA(a),r=(bg(i)?s.getPropertyValue(i):s[i])||0;return typeof r=="string"?r.trim():r}}measureInstanceViewportBox(a,{transformPagePoint:i}){return My(a,i)}build(a,i,s){vd(a,i,s.transformTemplate)}scrapeMotionValuesFromProps(a,i,s){return Sd(a,i,s)}}class RA extends Fy{constructor(){super(...arguments),this.type="svg",this.isSVGTag=!1,this.measureInstanceViewportBox=Ue}getBaseTargetFromProps(a,i){return a[i]}readValueFromInstance(a,i){if(oa.has(i)){const s=Ud(i);return s&&s.default||0}return i=Tg.has(i)?i:pd(i),a.getAttribute(i)}scrapeMotionValuesFromProps(a,i,s){return Ig(a,i,s)}build(a,i,s){bd(a,i,this.isSVGTag,s.transformTemplate)}renderInstance(a,i,s,r){Cg(a,i,s,r)}mount(a){this.isSVGTag=xd(a.tagName),super.mount(a)}}const LA=(t,a)=>fd(t)?new RA(a):new PA(a,{allowProjection:t!==x.Fragment}),DA=r0({...nI,...xA,...hA,...SA},LA),He=xC(DA);function Ny(t,a){let i;const s=()=>{const{currentTime:r}=a,h=(r===null?0:r.value)/100;i!==h&&t(h),i=h};return fe.update(s,!0),()=>Qt(s)}const Us=new WeakMap;let jn;function VA(t,a){if(a){const{inlineSize:i,blockSize:s}=a[0];return{width:i,height:s}}else return t instanceof SVGElement&&"getBBox"in t?t.getBBox():{width:t.offsetWidth,height:t.offsetHeight}}function jA({target:t,contentRect:a,borderBoxSize:i}){var s;(s=Us.get(t))===null||s===void 0||s.forEach(r=>{r({target:t,contentSize:a,get size(){return VA(t,i)}})})}function OA(t){t.forEach(jA)}function UA(){typeof ResizeObserver>"u"||(jn=new ResizeObserver(OA))}function FA(t,a){jn||UA();const i=Id(t);return i.forEach(s=>{let r=Us.get(s);r||(r=new Set,Us.set(s,r)),r.add(a),jn==null||jn.observe(s)}),()=>{i.forEach(s=>{const r=Us.get(s);r==null||r.delete(a),r!=null&&r.size||jn==null||jn.unobserve(s)})}}const Fs=new Set;let Yi;function NA(){Yi=()=>{const t={width:window.innerWidth,height:window.innerHeight},a={target:window,size:t,contentSize:t};Fs.forEach(i=>i(a))},window.addEventListener("resize",Yi)}function qA(t){return Fs.add(t),Yi||NA(),()=>{Fs.delete(t),!Fs.size&&Yi&&(Yi=void 0)}}function WA(t,a){return typeof t=="function"?qA(t):FA(t,a)}const zA=50,ff=()=>({current:0,offset:[],progress:0,scrollLength:0,targetOffset:0,targetLength:0,containerLength:0,velocity:0}),BA=()=>({time:0,x:ff(),y:ff()}),HA={x:{length:"Width",position:"Left"},y:{length:"Height",position:"Top"}};function gf(t,a,i,s){const r=i[a],{length:c,position:h}=HA[a],p=r.current,m=i.time;r.current=t[`scroll${h}`],r.scrollLength=t[`scroll${c}`]-t[`client${c}`],r.offset.length=0,r.offset[0]=0,r.offset[1]=r.scrollLength,r.progress=ia(0,r.scrollLength,r.current);const f=s-m;r.velocity=f>zA?0:Ld(r.current-p,f)}function _A(t,a,i){gf(t,"x",a,i),gf(t,"y",a,i),a.time=i}function KA(t,a){const i={x:0,y:0};let s=t;for(;s&&s!==a;)if(s instanceof HTMLElement)i.x+=s.offsetLeft,i.y+=s.offsetTop,s=s.offsetParent;else if(s.tagName==="svg"){const r=s.getBoundingClientRect();s=s.parentElement;const c=s.getBoundingClientRect();i.x+=r.left-c.left,i.y+=r.top-c.top}else if(s instanceof SVGGraphicsElement){const{x:r,y:c}=s.getBBox();i.x+=r,i.y+=c;let h=null,p=s.parentNode;for(;!h;)p.tagName==="svg"&&(h=p),p=s.parentNode;s=h}else break;return i}const $c={start:0,center:.5,end:1};function yf(t,a,i=0){let s=0;if(t in $c&&(t=$c[t]),typeof t=="string"){const r=parseFloat(t);t.endsWith("px")?s=r:t.endsWith("%")?t=r/100:t.endsWith("vw")?s=r/100*document.documentElement.clientWidth:t.endsWith("vh")?s=r/100*document.documentElement.clientHeight:t=r}return typeof t=="number"&&(s=a*t),i+s}const JA=[0,0];function GA(t,a,i,s){let r=Array.isArray(t)?t:JA,c=0,h=0;return typeof t=="number"?r=[t,t]:typeof t=="string"&&(t=t.trim(),t.includes(" ")?r=t.split(" "):r=[t,$c[t]?t:"0"]),c=yf(r[0],i,s),h=yf(r[1],a),c-h}const $A={All:[[0,0],[1,1]]},YA={x:0,y:0};function QA(t){return"getBBox"in t&&t.tagName!=="svg"?t.getBBox():{width:t.clientWidth,height:t.clientHeight}}function XA(t,a,i){const{offset:s=$A.All}=i,{target:r=t,axis:c="y"}=i,h=c==="y"?"height":"width",p=r!==t?KA(r,t):YA,m=r===t?{width:t.scrollWidth,height:t.scrollHeight}:QA(r),f={width:t.clientWidth,height:t.clientHeight};a[c].offset.length=0;let y=!a[c].interpolate;const v=s.length;for(let b=0;b<v;b++){const T=GA(s[b],f[h],m[h],p[c]);!y&&T!==a[c].interpolatorOffsets[b]&&(y=!0),a[c].offset[b]=T}y&&(a[c].interpolate=my(a[c].offset,fy(s),{clamp:!1}),a[c].interpolatorOffsets=[...a[c].offset]),a[c].progress=Xt(0,1,a[c].interpolate(a[c].current))}function ZA(t,a=t,i){if(i.x.targetOffset=0,i.y.targetOffset=0,a!==t){let s=a;for(;s&&s!==t;)i.x.targetOffset+=s.offsetLeft,i.y.targetOffset+=s.offsetTop,s=s.offsetParent}i.x.targetLength=a===t?a.scrollWidth:a.clientWidth,i.y.targetLength=a===t?a.scrollHeight:a.clientHeight,i.x.containerLength=t.clientWidth,i.y.containerLength=t.clientHeight}function eE(t,a,i,s={}){return{measure:()=>ZA(t,s.target,i),update:r=>{_A(t,i,r),(s.offset||s.target)&&XA(t,i,s)},notify:()=>a(i)}}const Ni=new WeakMap,wf=new WeakMap,Cc=new WeakMap,vf=t=>t===document.documentElement?window:t;function Wd(t,{container:a=document.documentElement,...i}={}){let s=Cc.get(a);s||(s=new Set,Cc.set(a,s));const r=BA(),c=eE(a,t,r,i);if(s.add(c),!Ni.has(a)){const p=()=>{for(const b of s)b.measure()},m=()=>{for(const b of s)b.update(Be.timestamp)},f=()=>{for(const b of s)b.notify()},y=()=>{fe.read(p,!1,!0),fe.read(m,!1,!0),fe.update(f,!1,!0)};Ni.set(a,y);const v=vf(a);window.addEventListener("resize",y,{passive:!0}),a!==document.documentElement&&wf.set(a,WA(a,y)),v.addEventListener("scroll",y,{passive:!0})}const h=Ni.get(a);return fe.read(h,!1,!0),()=>{var p;Qt(h);const m=Cc.get(a);if(!m||(m.delete(c),m.size))return;const f=Ni.get(a);Ni.delete(a),f&&(vf(a).removeEventListener("scroll",f),(p=wf.get(a))===null||p===void 0||p(),window.removeEventListener("resize",f))}}function tE({source:t,container:a,axis:i="y"}){t&&(a=t);const s={value:0},r=Wd(c=>{s.value=c[i].progress*100},{container:a,axis:i});return{currentTime:s,cancel:r}}const Mc=new Map;function qy({source:t,container:a=document.documentElement,axis:i="y"}={}){t&&(a=t),Mc.has(a)||Mc.set(a,{});const s=Mc.get(a);return s[i]||(s[i]=Pg()?new ScrollTimeline({source:a,axis:i}):tE({source:a,axis:i})),s[i]}function nE(t){return t.length===2}function Wy(t){return t&&(t.target||t.offset)}function aE(t,a){return nE(t)||Wy(a)?Wd(i=>{t(i[a.axis].progress,i)},a):Ny(t,qy(a))}function iE(t,a){if(t.flatten(),Wy(a))return t.pause(),Wd(i=>{t.time=t.duration*i[a.axis].progress},a);{const i=qy(a);return t.attachTimeline?t.attachTimeline(i,s=>(s.pause(),Ny(r=>{s.time=s.duration*r},i))):rt}}function oE(t,{axis:a="y",...i}={}){const s={axis:a,...i};return typeof t=="function"?aE(t,s):iE(t,s)}function bf(t,a){mC(!!(!a||a.current))}const sE=()=>({scrollX:Gt(0),scrollY:Gt(0),scrollXProgress:Gt(0),scrollYProgress:Gt(0)});function rE({container:t,target:a,layoutEffect:i=!0,...s}={}){const r=ro(sE);return(i?nr:x.useEffect)(()=>(bf("target",a),bf("container",t),oE((h,{x:p,y:m})=>{r.scrollX.set(p.current),r.scrollXProgress.set(p.progress),r.scrollY.set(m.current),r.scrollYProgress.set(m.progress)},{...s,container:(t==null?void 0:t.current)||void 0,target:(a==null?void 0:a.current)||void 0})),[t,a,JSON.stringify(s.offset)]),r}function ao(t){const a=ro(()=>Gt(t)),{isStatic:i}=x.useContext(lo);if(i){const[,s]=x.useState(t);x.useEffect(()=>a.on("change",s),[])}return a}function kf(t){return typeof t=="number"?t:parseFloat(t)}function io(t,a={}){const{isStatic:i}=x.useContext(lo),s=x.useRef(null),r=ao(Ye(t)?kf(t.get()):t),c=x.useRef(r.get()),h=x.useRef(()=>{}),p=()=>{const f=s.current;f&&f.time===0&&f.sample(Be.delta),m(),s.current=EM({keyframes:[r.get(),c.current],velocity:r.getVelocity(),type:"spring",restDelta:.001,restSpeed:.01,...a,onUpdate:h.current})},m=()=>{s.current&&s.current.stop()};return x.useInsertionEffect(()=>r.attach((f,y)=>i?y(f):(c.current=f,h.current=y,fe.update(p),r.get()),m),[JSON.stringify(a)]),nr(()=>{if(Ye(t))return t.on("change",f=>r.set(kf(f)))},[r]),r}const lE={some:0,all:1};function cE(t,a,{root:i,margin:s,amount:r="some"}={}){const c=Id(t),h=new WeakMap,p=f=>{f.forEach(y=>{const v=h.get(y.target);if(y.isIntersecting!==!!v)if(y.isIntersecting){const b=a(y);typeof b=="function"?h.set(y.target,b):m.unobserve(y.target)}else typeof v=="function"&&(v(y),h.delete(y.target))})},m=new IntersectionObserver(p,{root:i,rootMargin:s,threshold:typeof r=="number"?r:lE[r]});return c.forEach(f=>m.observe(f)),()=>m.disconnect()}function dE(t,{root:a,margin:i,amount:s,once:r=!1}={}){const[c,h]=x.useState(!1);return x.useEffect(()=>{if(!t.current||r&&c)return;const p=()=>(h(!0),r?void 0:()=>h(!1)),m={root:a&&a.current||void 0,margin:i,amount:s};return cE(t.current,p,m)},[a,t,i,r,s]),c}function Pe({children:t,delay:a=0,y:i=28,className:s="",once:r=!0}){return g.jsx(He.div,{className:s,initial:{opacity:0,y:i},whileInView:{opacity:1,y:0},viewport:{once:r,margin:"-60px"},transition:{duration:.7,delay:a,ease:[.21,.65,.32,.99]},children:t})}function hE({value:t,suffix:a="",className:i=""}){const s=x.useRef(null),r=dE(s,{once:!0,margin:"-40px"}),[c,h]=x.useState(0);return x.useEffect(()=>{if(!r)return;const p=1400,m=performance.now();let f;const y=v=>{const b=Math.min((v-m)/p,1),T=1-Math.pow(1-b,3);h(Math.round(T*t)),b<1&&(f=requestAnimationFrame(y))};return f=requestAnimationFrame(y),()=>cancelAnimationFrame(f)},[r,t]),g.jsxs("span",{ref:s,className:i,children:[c.toLocaleString(),a]})}function uE({children:t,className:a="",max:i=7}){const s=x.useRef(null),r=ao(0),c=ao(0),h=io(r,{stiffness:180,damping:20}),p=io(c,{stiffness:180,damping:20});function m(y){const v=s.current.getBoundingClientRect(),b=(y.clientX-v.left)/v.width-.5,T=(y.clientY-v.top)/v.height-.5;c.set(b*i),r.set(-T*i)}function f(){r.set(0),c.set(0)}return g.jsx(He.div,{ref:s,onMouseMove:m,onMouseLeave:f,style:{rotateX:h,rotateY:p,transformStyle:"preserve-3d",perspective:900},className:a,children:t})}function Yc({children:t,strength:a=.3,className:i=""}){const s=x.useRef(null),r=ao(0),c=ao(0),h=io(r,{stiffness:200,damping:15}),p=io(c,{stiffness:200,damping:15});function m(y){const v=s.current.getBoundingClientRect();r.set((y.clientX-v.left-v.width/2)*a),c.set((y.clientY-v.top-v.height/2)*a)}function f(){r.set(0),c.set(0)}return g.jsx(He.div,{ref:s,onMouseMove:m,onMouseLeave:f,style:{x:h,y:p},className:i,children:t})}function Ga({index:t,title:a,accent:i}){return g.jsxs(Pe,{className:"mb-12 md:mb-16",children:[g.jsxs("div",{className:"flex items-baseline gap-4",children:[g.jsx("span",{className:"font-display text-sm tracking-[0.3em] text-violet uppercase",children:t}),g.jsxs("h2",{className:"font-display text-4xl md:text-5xl font-bold text-bright",children:[a," ",i&&g.jsx("span",{className:"text-gradient",children:i})]})]}),g.jsx("div",{className:"mt-5 h-px w-full bg-gradient-to-r from-violet/60 via-line to-transparent"})]})}function zy({size:t=40}){return g.jsxs("svg",{width:t,height:t,viewBox:"0 0 48 48",fill:"none","aria-label":"RJ monogram",children:[g.jsx("defs",{children:g.jsxs("linearGradient",{id:"rj-grad",x1:"0",y1:"0",x2:"48",y2:"48",children:[g.jsx("stop",{offset:"0%",stopColor:"#a78bfa"}),g.jsx("stop",{offset:"55%",stopColor:"#e879f9"}),g.jsx("stop",{offset:"100%",stopColor:"#67e8f9"})]})}),g.jsx("rect",{x:"1.5",y:"1.5",width:"45",height:"45",rx:"12",stroke:"url(#rj-grad)",strokeWidth:"2.5"}),g.jsx("text",{x:"24",y:"31.5",textAnchor:"middle",fontFamily:"Space Grotesk, sans-serif",fontWeight:"700",fontSize:"19",fill:"url(#rj-grad)",children:"RJ"})]})}function cr({src:t,alt:a,label:i,className:s="",accent:r="violet"}){const c={violet:"from-violet-500/25 via-fuchsia-500/15",cyan:"from-cyan-400/25 via-sky-500/15",emerald:"from-emerald-400/25 via-teal-500/15",amber:"from-amber-400/25 via-orange-500/15"};return t?g.jsx("img",{src:t,alt:a,loading:"lazy",className:`h-full w-full object-cover ${s}`}):g.jsxs("div",{className:`flex h-full w-full flex-col items-center justify-center gap-3 bg-gradient-to-br ${c[r]||c.violet} to-transparent ${s}`,children:[g.jsxs("svg",{width:"44",height:"44",viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"1.3",className:"text-bright/40",children:[g.jsx("rect",{x:"3",y:"3",width:"18",height:"18",rx:"3"}),g.jsx("circle",{cx:"9",cy:"9",r:"2"}),g.jsx("path",{d:"m21 15-4.5-4.5L7 20"})]}),g.jsxs("span",{className:"px-4 text-center text-xs tracking-wide text-bright/40",children:[i||a," — screenshot coming soon"]})]})}function Wa({children:t,className:a=""}){return g.jsx("span",{className:`inline-block rounded-full border border-line bg-white/[0.03] px-3 py-1 text-xs font-medium tracking-wide text-body ${a}`,children:t})}const pE="modulepreload",mE=function(t){return"/"+t},xf={},By=function(a,i,s){let r=Promise.resolve();if(i&&i.length>0){let h=function(f){return Promise.all(f.map(y=>Promise.resolve(y).then(v=>({status:"fulfilled",value:v}),v=>({status:"rejected",reason:v}))))};document.getElementsByTagName("link");const p=document.querySelector("meta[property=csp-nonce]"),m=(p==null?void 0:p.nonce)||(p==null?void 0:p.getAttribute("nonce"));r=h(i.map(f=>{if(f=mE(f),f in xf)return;xf[f]=!0;const y=f.endsWith(".css"),v=y?'[rel="stylesheet"]':"";if(document.querySelector(`link[href="${f}"]${v}`))return;const b=document.createElement("link");if(b.rel=y?"stylesheet":pE,y||(b.as="script"),b.crossOrigin="",b.href=f,m&&b.setAttribute("nonce",m),document.head.appendChild(b),y)return new Promise((T,M)=>{b.addEventListener("load",T),b.addEventListener("error",()=>M(new Error(`Unable to preload CSS for ${f}`)))})}))}function c(h){const p=new Event("vite:preloadError",{cancelable:!0});if(p.payload=h,window.dispatchEvent(p),!p.defaultPrevented)throw h}return r.then(h=>{for(const p of h||[])p.status==="rejected"&&c(p.reason);return a().catch(c)})},fE="/assets/revanth-aIJoikZB.png",gE="/assets/main_banner_image-CYrGodu7.jpg",yE="/assets/mifos_mobile-CyfImdY2.png",wE="/assets/android_studio-DepsAEy5.png",vE="/assets/abhiyanth-BLZao8qe.png",bE="/assets/meme-CBUfHHT_.png",kE="/assets/swipe_assign-BDpvp73J.png",xE="/assets/kisan-Ccx7BOhw.png",SE="/assets/Finance-ClkzU8NH.png",TE="/assets/ipl-wOio7x8m.png",CE="/assets/department-k0NsSQqd.png",ME="/assets/insta-CxnzjlTm.png",IE="/assets/netflix-7RbTWAq9.png",AE="/assets/expense_tracker-DpUiUELN.png",EE="/assets/Revanth_final_version-yb7_pk0P.pdf",mt={name:"Revanth Kumar Jilakara",shortName:"Revanth",location:"Andhra Pradesh, India",email:"jrevanth101@gmail.com",resume:EE,photo:fE,heroImage:gE},Hy=[{name:"GitHub",url:"https://github.com/revanthkumarJ",icon:"github"},{name:"LinkedIn",url:"https://www.linkedin.com/in/jilakararevanthkumar/",icon:"linkedin"},{name:"Instagram",url:"https://www.instagram.com/revanth_kumar_j",icon:"instagram"},{name:"Email",url:"mailto:jrevanth101@gmail.com",icon:"mail"}],PE=[{value:120,suffix:"+",label:"Open-source PRs merged"},{value:98,suffix:"%",label:"PR merge rate"},{value:230,suffix:"+",label:"Contributor PRs reviewed"},{value:1e3,suffix:"+",label:"DSA problems solved"}],Sf={paragraphs:["I'm a Software Engineer from Andhra Pradesh, India, specializing in Android and Kotlin Multiplatform development. Currently SDE 1 – Android Developer at Swipe (YC S21), where I was promoted from intern after migrating 70+ screens to Jetpack Compose and restructuring a monolith into multi-module Clean Architecture.","I'm an active contributor and mentor at the Mifos Initiative — 120+ merged PRs at a 98% merge rate, 230+ contributor PRs reviewed, and 20+ modules migrated to Kotlin Multiplatform across production fintech apps used worldwide. I was a Mifos Summer of Code 2025 intern and now mentor contributors for GSoC and Code4GovTech.","I hold a B.Tech in CSE from RGUKT RK Valley and qualified GATE CS 2025. Off the keyboard: badminton, chess, and poking at new tools."],quote:"Consistency and curiosity are the keys to growth."},Es={primary:{title:"Android & Kotlin Multiplatform",subtitle:"My core craft — what I ship to production every day",items:["Kotlin","Jetpack Compose","Kotlin Multiplatform","Compose Multiplatform","Coroutines & Flow","MVI","Clean Architecture","Multi-module Architecture","Room (KMP)","SQLDelight","DataStore","Koin","Dagger / Hilt","Ktor / Ktorfit","Retrofit","WorkManager","Compose Navigation","Material 3","kotlinx.serialization","Gradle Convention Plugins","R8 / Play Store Releases","Firebase Crashlytics & Analytics","Lottie","GitHub Actions (CI/CD)"]},secondary:[{title:"Web & Backend",items:["React","JavaScript","TypeScript","Node.js","Express","Tailwind CSS","Firebase","MongoDB","MySQL"]},{title:"Languages & Tools",items:["Java","Python","C","Git & GitHub","Postman","Figma-to-UI"]}]},RE=[{name:"LeetCode",detail:"Knight · 800+ solved",url:"https://leetcode.com/u/RevanthKumarJ/"},{name:"GeeksforGeeks",detail:"Institute Rank 1 · 1000+ solved",url:"https://www.geeksforgeeks.org/user/jrevanth/"},{name:"CodeChef",detail:"3★ Coder",url:"https://www.codechef.com/users/revanthkumarj1"},{name:"HackerRank",detail:"5★ Problem Solving",url:"https://www.hackerrank.com/jrevanth101"}],LE=[{company:"Swipe (YC S21)",role:"SDE 1 — Android Developer",period:"Jun 15, 2026 — Present",current:!0,summary:"Owning end-to-end feature flows in the Swipe billing app — settings, products, expenses, and indirect income — while improving architecture, build tooling, and reliability for thousands of businesses.",highlights:["Built all settings pages — document, general, thermal print, and product settings.","Own the entire products, expenses, and indirect-income flows (list / create / edit).","Introduced build-logic convention plugins, cutting app build.gradle complexity by ~80%.","Work on notifications, thermal prints, PDF generation; fix production crashes via Crashlytics."],tech:["Kotlin","Jetpack Compose","MVI","Koin","Room","Convention Plugins"],links:[{text:"Swipe on Play Store",url:"https://play.google.com/store/apps/details?id=com.swipe.bill"}]},{company:"Swipe (YC S21)",role:"Android Developer Intern",period:"Dec 2025 — Jun 14, 2026",summary:"Migrated 70+ screens from legacy XML to Jetpack Compose across 15 production flows — improving UX along the way — and restructured a monolithic codebase into multi-module Clean Architecture with MVI.",highlights:["Migrated the onboarding screen — the first thing users see — with a fresh Compose UI.","Migrated document settings, templates, create-expense, and create-product screens.","Restructured monolithic files into multi-module Clean Architecture built for scale.","Resolved production crashes and UI defects via Crashlytics and Intercom."],tech:["Kotlin","Jetpack Compose","Coroutines","Flow","Retrofit","Room"],links:[{text:"Swipe on Play Store",url:"https://play.google.com/store/apps/details?id=com.swipe.bill"}]},{company:"Mifos Initiative",role:"Mentor — GSoC & C4GT",period:"Mar 2026 — Present",current:!0,summary:"Mentoring open-source contributors: reviewing PRs, running standups, conducting GSoC and Code4GovTech interviews, and guiding Kotlin Multiplatform architecture discussions.",highlights:["Conduct interviews for Google Summer of Code and Code4GovTech.","Review contributor pull requests across community projects (230+ reviewed).","Run standups and guide mobile-architecture discussions.","Help new contributors onboard into the Mifos ecosystem."],tech:["Kotlin Multiplatform","Code Review","Mentorship"],links:[]},{company:"Mifos Initiative",role:"Open Source Mobile Developer",period:"Nov 2024 — Present",current:!0,summary:"120+ PRs at a 98% merge rate across 5 production fintech repositories. Migrated 20+ modules to Kotlin Multiplatform in apps focused on financial inclusion.",highlights:["android-client: 50+ PRs migrating UI to KMP/CMP, Figma redesigns, type-safe navigation.","mifos-mobile: 45+ PRs — module migrations, QR binding, localization, new screens.","mobile-wallet: migrated APIs to the Self API and refreshed the surrounding flows.","Contributed to kmp-project-template and mifos-x-group-banking setup."],tech:["Kotlin Multiplatform","Compose Multiplatform","Ktor","Koin","MVI"],links:[{text:"All merged PRs",url:"https://github.com/pulls?q=is%3Apr+is%3Amerged+author%3ArevanthKumarJ+org%3AopenMF+"},{text:"PRs reviewed",url:"https://github.com/pulls?q=is%3Apr+reviewed-by%3ArevanthKumarJ+org%3AopenMF+"},{text:"Mifos Mobile",url:"https://play.google.com/store/apps/details?id=org.mifos.mobile"},{text:"Android Client",url:"https://play.google.com/store/apps/details?id=com.mifos.mifosxdroid"}]},{company:"Mifos Initiative",role:"Mifos Summer of Code 2025 Intern",period:"Jun 2025 — Sep 2025",summary:"Selected for the competitive MSoC program ($2,500 stipend). Led the Kotlin Multiplatform migration of the Field Officer app and modernized UI across 100+ screens in three repositories.",highlights:["100+ PRs authored and 150+ reviewed during the program window.","Migrated core modules of android-client and mifos-mobile to KMP/CMP.","Implemented new Figma-based UIs: transactions, charges, beneficiaries, passcode, and more.","Grew from struggling to raise a single PR to redesigning two production apps."],tech:["Kotlin Multiplatform","Compose Multiplatform","GitHub Actions","Clean Architecture"],links:[{text:"Full MSoC writeup",url:"https://gist.github.com/revanthkumarJ/133c9e8ce0abb111fb19873ad902cb70"},{text:"Weekly progress",url:"https://github.com/revanthkumarJ/MSOC_progress"}]},{company:"Mobile Byte Sensei",role:"Mobile Development Intern (KMP & CMP)",period:"Apr 2025 — Nov 2025",summary:"Delivered 7 production-grade Kotlin Multiplatform apps — reusable UI components, file management, Firebase Analytics, and Crashlytics fixes in an Agile team.",highlights:["Shipped Reels Downloader, Stories Downloader, and Byte Wallpaper to the Play Store.","Built reusable Compose Multiplatform components and CI/CD pipelines.","Resolved critical Crashlytics-reported issues in production."],tech:["Kotlin Multiplatform","Compose Multiplatform","Firebase","CI/CD"],links:[{text:"Reels Downloader",url:"https://play.google.com/store/apps/details?id=com.sensei.social"},{text:"Stories Downloader",url:"https://play.google.com/store/apps/details?id=com.sensei.stories"},{text:"Byte Wallpaper",url:"https://play.google.com/store/apps/details?id=org.mobilebytesensei.wallpaper"}]},{company:"TLDE Technologies",role:"Software Development Intern",period:"Nov 2025 — Dec 2025",summary:"Built onboarding experiences for a Kotlin Multiplatform app using Compose Multiplatform and Lottie animations.",highlights:[],tech:["Kotlin Multiplatform","Compose Multiplatform","Lottie"],links:[]},{company:"Abhiyanth 2K25",role:"Frontend Team Lead & Developer",period:"Dec 2024 — Mar 2025",summary:"Led frontend development of the college fest platform in React — Firebase integration, admin panel, repo management, PR reviews, and UI/UX coordination.",highlights:[],tech:["React","Redux","Firebase","Material UI"],links:[{text:"GitHub repo",url:"https://github.com/revanthkumarJ/Abhiyanth-Client"}]},{company:"DevDisplay",role:"Open Source Contributor — React",period:"Jan 2025 — Feb 2025",summary:"10 merged PRs of UI enhancements and page development with React and Tailwind CSS — Sponsors, About Us, and Journey pages.",highlights:[],tech:["React","Tailwind CSS"],links:[{text:"Merged PRs",url:"https://github.com/codeaashu/DevDisplay/pulls?q=is%3Apr+is%3Amerged+author%3ArevanthkumarJ"}]},{company:"GeeksforGeeks",role:"Campus Ambassador",period:"Apr 2024 — Apr 2025",summary:"Promoted coding culture on campus, coordinated contests and events, and was recognized among the top-performing campus ambassadors.",highlights:[],tech:[],links:[]}],DE=[{role:"DSA Coordinator & Mentor",org:"SRC Student Club",period:"Apr 2024 — Mar 2025",note:"Weekly coding contests and mentoring sessions to uplift peers' DSA performance."},{role:"NSS Unit Coordinator",org:"National Service Scheme",period:"Jan 2024 — Jan 2026",note:"Organized social-outreach activities; maintain the NSS Unit-2 LinkedIn page."},{role:"Social Media Manager",org:"Dept. of CSE, RGUKT RK Valley",period:"Apr 2024 — Mar 2025",note:"Ran the department's social presence and analytics."},{role:"Class Representative",org:"Dept. of CSE, RGUKT RK Valley",period:"Jun 2024 — Nov 2024",note:"Bridge between faculty and students for E3 Sem 1."}],VE=[{title:"ExpenseTrackr",tagline:"Privacy-first offline finance tracker — one Kotlin codebase, three platforms",description:"A personal finance tracker where every expense, budget, and category lives on-device. Built as a single Kotlin Multiplatform codebase rendering native UI on Android, iOS, and Desktop via Compose Multiplatform — 16 Gradle modules, 18 screens, Clean MVI, 24 languages with an in-app switcher, PIN + biometric app-lock, and CSV backup that survives reinstalls.",metrics:[{value:"16",label:"Gradle modules"},{value:"24",label:"Languages"},{value:"3",label:"Platforms"}],tech:["Kotlin Multiplatform","Compose Multiplatform","Clean MVI","Room (KMP)","Koin","Material 3"],image:AE,accent:"emerald",links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/ExpenseTrackr",kind:"github"}],badge:"Play Store — closed testing"},{title:"Mifos Mobile — KMP Migration",tagline:"Production banking app, migrated to every platform",description:"Migrated 7 modules of Mifos Mobile — a self-service banking client used worldwide — to Kotlin Multiplatform and Compose Multiplatform, targeting Android, iOS, Web/WASM, and Desktop. 45+ merged PRs covering module migrations, new Figma-based screens, QR-code binding, and localization.",metrics:[{value:"45+",label:"PRs merged"},{value:"7",label:"Modules migrated"},{value:"5",label:"Platform targets"}],tech:["Kotlin Multiplatform","Compose Multiplatform","Ktor","Koin"],image:yE,accent:"violet",links:[{text:"GitHub",url:"https://github.com/openMF/mifos-mobile",kind:"github"},{text:"My contributions",url:"https://github.com/openMF/mifos-mobile/pulls?q=is%3Amerged+is%3Apr+author%3ArevanthkumarJ+",kind:"prs"},{text:"Play Store",url:"https://play.google.com/store/apps/details?id=org.mifos.mobile",kind:"playstore"}]},{title:"Android Client — KMP Migration",tagline:"Field-officer banking app for offline-first microfinance",description:"52 merged PRs migrating 10 modules of the Mifos Field Officer app to KMP/CMP — refactoring client screens to the latest Figma designs, adding type-safe navigation, fixing production bugs, and building new UI components for an app processing real financial transactions in remote areas.",metrics:[{value:"52",label:"PRs merged"},{value:"10",label:"Modules migrated"},{value:"100%",label:"Offline-capable"}],tech:["Kotlin Multiplatform","Compose Multiplatform","Room","MVI"],image:wE,accent:"cyan",links:[{text:"GitHub",url:"https://github.com/openMF/android-client",kind:"github"},{text:"My contributions",url:"https://github.com/openMF/android-client/pulls?q=is%3Amerged+is%3Apr+author%3ArevanthkumarJ+",kind:"prs"},{text:"Play Store",url:"https://play.google.com/store/apps/details?id=com.mifos.mifosxdroid",kind:"playstore"}]},{title:"Abhiyanth Fest Platform",tagline:"College fest platform — led the frontend team",description:"Led the frontend team building the official platform for Abhiyanth 2K25 — React, Redux, and Material UI with Firebase auth and hosting, CashFree payment integration, and a full admin panel. Managed the repo, reviewed PRs, and coordinated with UI/UX.",metrics:[{value:"Lead",label:"Frontend team"},{value:"Live",label:"Payments (CashFree)"}],tech:["React","Redux","Firebase","Material UI"],image:vE,accent:"amber",links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/abhiyanth-client",kind:"github"}]}],jE=[{title:"Meme Studio",description:"Kotlin Multiplatform meme editor with templates and custom editing, built on Compose Multiplatform.",tech:["KMP","CMP"],image:bE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/MemeStudio",kind:"github"}]},{title:"Swipe Assignment App",description:"Modern Android app — onboarding, product listing with search, offline-first Room sync, theming.",tech:["Kotlin","Room","Retrofit"],image:kE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/swipe-assignment",kind:"github"}]},{title:"KisanConnect",description:"Platform connecting farmers and customers — role-based UIs with React, Express, and TypeScript.",tech:["React","Express","TypeScript"],image:xE,links:[{text:"Client",url:"https://github.com/revanthkumarJ/kisan_connect_client",kind:"github"},{text:"API",url:"https://github.com/revanthkumarJ/Kisan_connect_API",kind:"github"}]},{title:"Finance Tracker (Web)",description:"Node.js + TypeScript API with a React/MUI frontend to manage and analyze bank statements.",tech:["Node.js","TypeScript","React"],image:SE,links:[{text:"Client",url:"https://github.com/revanthkumarJ/Finance-Client",kind:"github"},{text:"API",url:"https://github.com/revanthkumarJ/Finance-API",kind:"github"}]},{title:"Sports Auction",description:"Mock IPL auction platform — 250 players, live bidding for 150+ students across 30 teams.",tech:["React"],image:TE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/Sports-Auction",kind:"github"},{text:"Live",url:"https://sports-auction.vercel.app/",kind:"demo"}]},{title:"Dept. Resource Manager",description:"Kotlin app managing announcements, complaints, and timetables for students, faculty, and HOD.",tech:["Kotlin","Firebase"],image:CE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/Departmental-Resource-Management-App",kind:"github"}]},{title:"Instagram UI Clone",description:"Jetpack Compose recreation of the Instagram feed and profile screens.",tech:["Jetpack Compose"],image:ME,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/InstaUI",kind:"github"}]},{title:"Netflix UI Clone",description:"Netflix-style UI built with Jetpack Compose — featured rows and layout aesthetics.",tech:["Jetpack Compose"],image:IE,links:[{text:"GitHub",url:"https://github.com/revanthkumarJ/NetFlixUI",kind:"github"}]}],OE=[{title:"C4GT 2026 Mentor",detail:"Mentor for the Mifos Pay project under Code for GovTech 2026.",tag:"Mentorship"},{title:"Mifos Summer of Code 2025",detail:"Selected for the competitive program with a $2,500 stipend.",tag:"Open Source"},{title:"120+ Open-Source PRs",detail:"Merged across production repos at a 98% merge rate.",tag:"Open Source"},{title:"GATE CS 2025",detail:"Qualified — strong command of core CS fundamentals.",tag:"Academics"},{title:"LeetCode Knight",detail:"800+ problems solved with a consistent contest rating.",tag:"DSA"},{title:"GFG Institute Rank 1",detail:"1000+ problems — first among all institute peers.",tag:"DSA"},{title:"CodeChef 3★",detail:"Consistent contest performance.",tag:"DSA"},{title:"HackerRank 5★",detail:"Problem Solving — 500+ challenges.",tag:"DSA"},{title:"Qualified NMMS",detail:"National Means-cum-Merit Scholarship at school level.",tag:"Academics"}],Ps={quote:"Revanth brings strong expertise in Kotlin Multiplatform development, with a deep understanding of building efficient, cross-platform architectures. One of his standout strengths is his debugging ability — a sharp eye for identifying complex issues and resolving them with clarity and precision. Beyond his technical skills, he is reliable, proactive, and a great collaborator. I strongly recommend him for any role requiring expertise in Kotlin Multiplatform and robust software architecture.",author:"Rajan Maurya",title:"Engineering Manager · Kotlin Multiplatform Expert",proofUrl:"https://www.linkedin.com/in/jilakararevanthkumar/details/recommendations/"},Tf={profileUrl:"https://medium.com/@jrevanth101",posts:[{title:"Ship Your Android App to Testers Automatically: A Complete GitHub Actions + Firebase App Distribution Guide",url:"https://medium.com/@jrevanth101/ship-your-android-app-to-testers-automatically-a-complete-github-actions-firebase-app-02def952a492",date:"Jul 2026",tags:["GitHub Actions","Firebase","CI/CD"]},{title:"How I Shrunk Every Feature Module's build.gradle from 60+ Lines to 12 Lines",url:"https://medium.com/@jrevanth101/how-i-shrunk-every-feature-modules-build-gradle-to-12-lines-c6341a73921f",date:"Jun 2026",tags:["Gradle","Convention Plugins","Kotlin"]},{title:"Reducing Boilerplate in Jetpack Compose MVI: The Template I Use for Every Screen",url:"https://medium.com/@jrevanth101/reducing-boilerplate-in-jetpack-compose-mvi-the-template-i-use-for-every-screen-396fa55cc17d",date:"May 2026",tags:["Jetpack Compose","MVI","Kotlin"]},{title:"Building a Comma-Separated Decimal Input in Jetpack Compose",url:"https://medium.com/@jrevanth101/building-a-comma-separated-decimal-input-in-jetpack-compose-bbcf82813e9c",date:"Mar 2026",tags:["Jetpack Compose","Kotlin"]}]},Ic={username:"revanthkumarJ",url:"https://github.com/revanthkumarJ"},Ac=[{label:"About",href:"#about"},{label:"Experience",href:"#experience"},{label:"Projects",href:"#projects"},{label:"Achievements",href:"#achievements"},{label:"Writing",href:"#writing"},{label:"Contact",href:"#contact"}];var _y={color:void 0,size:void 0,className:void 0,style:void 0,attr:void 0},Cf=Pt.createContext&&Pt.createContext(_y),UE=["attr","size","title"];function FE(t,a){if(t==null)return{};var i,s,r=NE(t,a);if(Object.getOwnPropertySymbols){var c=Object.getOwnPropertySymbols(t);for(s=0;s<c.length;s++)i=c[s],a.indexOf(i)===-1&&{}.propertyIsEnumerable.call(t,i)&&(r[i]=t[i])}return r}function NE(t,a){if(t==null)return{};var i={};for(var s in t)if({}.hasOwnProperty.call(t,s)){if(a.indexOf(s)!==-1)continue;i[s]=t[s]}return i}function Gs(){return Gs=Object.assign?Object.assign.bind():function(t){for(var a=1;a<arguments.length;a++){var i=arguments[a];for(var s in i)({}).hasOwnProperty.call(i,s)&&(t[s]=i[s])}return t},Gs.apply(null,arguments)}function Mf(t,a){var i=Object.keys(t);if(Object.getOwnPropertySymbols){var s=Object.getOwnPropertySymbols(t);a&&(s=s.filter(function(r){return Object.getOwnPropertyDescriptor(t,r).enumerable})),i.push.apply(i,s)}return i}function $s(t){for(var a=1;a<arguments.length;a++){var i=arguments[a]!=null?arguments[a]:{};a%2?Mf(Object(i),!0).forEach(function(s){qE(t,s,i[s])}):Object.getOwnPropertyDescriptors?Object.defineProperties(t,Object.getOwnPropertyDescriptors(i)):Mf(Object(i)).forEach(function(s){Object.defineProperty(t,s,Object.getOwnPropertyDescriptor(i,s))})}return t}function qE(t,a,i){return(a=WE(a))in t?Object.defineProperty(t,a,{value:i,enumerable:!0,configurable:!0,writable:!0}):t[a]=i,t}function WE(t){var a=zE(t,"string");return typeof a=="symbol"?a:a+""}function zE(t,a){if(typeof t!="object"||!t)return t;var i=t[Symbol.toPrimitive];if(i!==void 0){var s=i.call(t,a);if(typeof s!="object")return s;throw new TypeError("@@toPrimitive must return a primitive value.")}return(a==="string"?String:Number)(t)}function Ky(t){return t&&t.map((a,i)=>Pt.createElement(a.tag,$s({key:i},a.attr),Ky(a.child)))}function ft(t){return a=>Pt.createElement(BE,Gs({attr:$s({},t.attr)},a),Ky(t.child))}function BE(t){var a=i=>{var s=t.attr,r=t.size,c=t.title,h=FE(t,UE),p=r||i.size||"1em",m;return i.className&&(m=i.className),t.className&&(m=(m?m+" ":"")+t.className),Pt.createElement("svg",Gs({stroke:"currentColor",fill:"currentColor",strokeWidth:"0"},i.attr,s,h,{className:m,style:$s($s({color:t.color||i.color},i.style),t.style),height:p,width:p,xmlns:"http://www.w3.org/2000/svg"}),c&&Pt.createElement("title",null,c),t.children)};return Cf!==void 0?Pt.createElement(Cf.Consumer,null,i=>a(i)):a(_y)}function HE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"line",attr:{x1:"18",y1:"6",x2:"6",y2:"18"},child:[]},{tag:"line",attr:{x1:"6",y1:"6",x2:"18",y2:"18"},child:[]}]})(t)}function Jy(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"},child:[]},{tag:"polyline",attr:{points:"22,6 12,13 2,6"},child:[]}]})(t)}function Gy(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"},child:[]},{tag:"rect",attr:{x:"2",y:"9",width:"4",height:"12"},child:[]},{tag:"circle",attr:{cx:"4",cy:"4",r:"2"},child:[]}]})(t)}function $y(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"rect",attr:{x:"2",y:"2",width:"20",height:"20",rx:"5",ry:"5"},child:[]},{tag:"path",attr:{d:"M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"},child:[]},{tag:"line",attr:{x1:"17.5",y1:"6.5",x2:"17.51",y2:"6.5"},child:[]}]})(t)}function dr(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"},child:[]}]})(t)}function _E(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"circle",attr:{cx:"18",cy:"18",r:"3"},child:[]},{tag:"circle",attr:{cx:"6",cy:"6",r:"3"},child:[]},{tag:"path",attr:{d:"M13 6h3a2 2 0 0 1 2 2v7"},child:[]},{tag:"line",attr:{x1:"6",y1:"9",x2:"6",y2:"21"},child:[]}]})(t)}function hr(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"},child:[]},{tag:"polyline",attr:{points:"15 3 21 3 21 9"},child:[]},{tag:"line",attr:{x1:"10",y1:"14",x2:"21",y2:"3"},child:[]}]})(t)}function KE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"path",attr:{d:"M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"},child:[]},{tag:"polyline",attr:{points:"7 10 12 15 17 10"},child:[]},{tag:"line",attr:{x1:"12",y1:"15",x2:"12",y2:"3"},child:[]}]})(t)}function JE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"polyline",attr:{points:"6 9 12 15 18 9"},child:[]}]})(t)}function GE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"circle",attr:{cx:"12",cy:"8",r:"7"},child:[]},{tag:"polyline",attr:{points:"8.21 13.89 7 23 12 20 17 23 15.79 13.88"},child:[]}]})(t)}function $E(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"line",attr:{x1:"7",y1:"17",x2:"17",y2:"7"},child:[]},{tag:"polyline",attr:{points:"7 7 17 7 17 17"},child:[]}]})(t)}function YE(t){return ft({attr:{viewBox:"0 0 24 24",fill:"none",stroke:"currentColor",strokeWidth:"2",strokeLinecap:"round",strokeLinejoin:"round"},child:[{tag:"line",attr:{x1:"12",y1:"5",x2:"12",y2:"19"},child:[]},{tag:"polyline",attr:{points:"19 12 12 19 5 12"},child:[]}]})(t)}const QE=Pt.lazy(()=>By(()=>import("./PdfViewer-sd1cVmYH.js"),[])),Yy=x.createContext(()=>{});function Qy(){return x.useContext(Yy)}function XE({children:t}){const[a,i]=x.useState(!1),s=x.useCallback(()=>i(!0),[]);return x.useEffect(()=>{if(!a)return;const r=c=>c.key==="Escape"&&i(!1);return window.addEventListener("keydown",r),document.body.style.overflow="hidden",()=>{window.removeEventListener("keydown",r),document.body.style.overflow=""}},[a]),g.jsxs(Yy.Provider,{value:s,children:[t,g.jsx(cd,{children:a&&g.jsx(He.div,{initial:{opacity:0},animate:{opacity:1},exit:{opacity:0},transition:{duration:.25},className:"fixed inset-0 z-[100] flex items-center justify-center bg-ink/85 p-2 backdrop-blur-sm sm:p-4 md:p-8",onClick:()=>i(!1),children:g.jsxs(He.div,{initial:{opacity:0,y:40,scale:.97},animate:{opacity:1,y:0,scale:1},exit:{opacity:0,y:40,scale:.97},transition:{duration:.35,ease:[.21,.65,.32,.99]},className:"flex h-[94vh] w-full max-w-4xl flex-col overflow-hidden rounded-2xl border border-line bg-ink-2 shadow-[0_30px_120px_-20px_rgba(167,139,250,0.35)] sm:h-[88vh]",onClick:r=>r.stopPropagation(),children:[g.jsxs("div",{className:"flex items-center justify-between border-b border-line px-5 py-3.5",children:[g.jsxs("span",{className:"font-display font-bold text-bright",children:["Resume — ",mt.name]}),g.jsxs("div",{className:"flex items-center gap-2",children:[g.jsxs("a",{href:mt.resume,download:"Revanth_Kumar_Jilakara_Resume.pdf",className:"inline-flex items-center gap-2 rounded-full border border-line px-4 py-1.5 text-sm font-medium text-bright transition-colors hover:border-violet/60 hover:bg-violet/10",children:[g.jsx(KE,{size:15})," Download"]}),g.jsx("button",{onClick:()=>i(!1),"aria-label":"Close resume",className:"rounded-full p-2 text-body transition-colors hover:bg-white/10 hover:text-bright",children:g.jsx(HE,{size:20})})]})]}),g.jsx(Pt.Suspense,{fallback:g.jsx("div",{className:"flex flex-1 items-center justify-center text-body",children:"Loading resume…"}),children:g.jsx(QE,{file:mt.resume})})]})})})]})}function ZE(){const[t,a]=x.useState(!1),[i,s]=x.useState(""),[r,c]=x.useState(!1),h=Qy();return x.useEffect(()=>{const p=()=>a(window.scrollY>24);return window.addEventListener("scroll",p,{passive:!0}),()=>window.removeEventListener("scroll",p)},[]),x.useEffect(()=>{const p=Ac.map(f=>document.querySelector(f.href)).filter(Boolean),m=new IntersectionObserver(f=>{f.forEach(y=>{y.isIntersecting&&s(`#${y.target.id}`)})},{rootMargin:"-40% 0px -55% 0px"});return p.forEach(f=>m.observe(f)),()=>m.disconnect()},[]),g.jsxs(He.header,{initial:{y:-80,opacity:0},animate:{y:0,opacity:1},transition:{duration:.7,ease:[.21,.65,.32,.99],delay:.1},className:`fixed inset-x-0 top-0 z-50 transition-all duration-300 ${t||r?"glass !bg-ink/85 shadow-lg shadow-black/20":"bg-transparent"}`,children:[g.jsxs("nav",{className:"flex h-[72px] w-full items-center justify-between px-5 md:px-10 xl:px-16",children:[g.jsxs("a",{href:"#top","aria-label":"Home",className:"flex items-center gap-3",children:[g.jsx(zy,{size:38}),g.jsxs("span",{className:"font-display hidden text-sm font-semibold tracking-wide text-bright sm:block",children:[mt.shortName.toLowerCase(),".dev"]})]}),g.jsxs("ul",{className:"hidden items-center gap-1 md:flex",children:[Ac.map(p=>g.jsx("li",{children:g.jsxs("a",{href:p.href,className:`rounded-full px-4 py-2 text-sm font-medium transition-colors ${i===p.href?"text-bright":"text-body hover:text-bright"}`,children:[i===p.href&&g.jsx("span",{className:"mr-2 inline-block h-1.5 w-1.5 rounded-full bg-gradient-to-r from-violet to-fuchsia align-middle"}),p.label]})},p.href)),g.jsx("li",{children:g.jsx("button",{onClick:h,className:"ml-3 rounded-full border border-violet/50 px-5 py-2 text-sm font-semibold text-bright transition-all hover:border-violet hover:bg-violet/10 hover:shadow-[0_0_24px_-6px_rgba(167,139,250,0.6)]",children:"Resume"})})]}),g.jsxs("button",{className:"flex h-10 w-10 flex-col items-center justify-center gap-1.5 md:hidden",onClick:()=>c(!r),"aria-label":"Toggle menu",children:[g.jsx("span",{className:`h-0.5 w-6 bg-bright transition-transform ${r?"translate-y-2 rotate-45":""}`}),g.jsx("span",{className:`h-0.5 w-6 bg-bright transition-opacity ${r?"opacity-0":""}`}),g.jsx("span",{className:`h-0.5 w-6 bg-bright transition-transform ${r?"-translate-y-2 -rotate-45":""}`})]})]}),g.jsx(cd,{children:r&&g.jsxs(He.ul,{initial:{height:0,opacity:0},animate:{height:"auto",opacity:1},exit:{height:0,opacity:0},transition:{duration:.3},className:"overflow-hidden border-t border-line bg-ink-2/95 backdrop-blur-xl md:hidden",children:[Ac.map(p=>g.jsx("li",{children:g.jsx("a",{href:p.href,onClick:()=>c(!1),className:"block px-8 py-4 text-base font-medium text-bright/90 hover:bg-white/5",children:p.label})},p.href)),g.jsx("li",{children:g.jsx("button",{onClick:()=>{c(!1),h()},className:"block w-full px-8 py-4 text-left text-base font-semibold text-violet",children:"Resume ↗"})})]})})]})}const eP={github:dr,linkedin:Gy,instagram:$y,mail:Jy},qi={hidden:{opacity:0,y:40},show:t=>({opacity:1,y:0,transition:{duration:.8,delay:.25+t*.12,ease:[.21,.65,.32,.99]}})};function tP(){const t=Qy();return g.jsxs("section",{id:"top",className:"relative flex min-h-screen flex-col justify-center overflow-hidden pt-[72px]",children:[g.jsx("div",{className:"blob left-[-10%] top-[-5%] h-[480px] w-[480px] bg-violet-600/25"}),g.jsx("div",{className:"blob right-[-8%] top-[30%] h-[420px] w-[420px] bg-fuchsia-600/15"}),g.jsx("div",{className:"blob bottom-[-10%] left-[30%] h-[380px] w-[380px] bg-cyan-500/10"}),g.jsxs("div",{className:"relative z-10 grid w-full items-center gap-10 px-5 md:px-10 lg:grid-cols-[1.15fr_0.85fr] xl:px-16",children:[g.jsxs("div",{children:[g.jsx(He.div,{custom:0,variants:qi,initial:"hidden",animate:"show",className:"mb-8",children:g.jsxs("span",{className:"glass inline-flex items-center gap-2.5 rounded-full px-4 py-2 text-sm text-bright/90",children:[g.jsx("span",{className:"pulse-dot h-2 w-2 rounded-full bg-emerald"}),"Open to opportunities"]})}),g.jsxs(He.h1,{custom:1,variants:qi,initial:"hidden",animate:"show",className:"font-display max-w-4xl text-3xl font-bold leading-[1.12] tracking-tight text-bright sm:text-4xl md:text-5xl 2xl:text-6xl",children:["I build ",g.jsx("span",{className:"text-gradient",children:"production Android"})," & Kotlin Multiplatform apps used by thousands of businesses."]}),g.jsxs(He.div,{custom:2,variants:qi,initial:"hidden",animate:"show",className:"mt-8",children:[g.jsx("p",{className:"font-display text-xl font-bold text-bright md:text-2xl",children:mt.name}),g.jsxs("p",{className:"mt-1.5 text-base text-body md:text-lg",children:["SDE 1 · Android @ ",g.jsx("span",{className:"font-semibold text-violet",children:"Swipe (YC S21)"})]}),g.jsxs("p",{className:"mt-1 text-base text-body md:text-lg",children:["Mifos ",g.jsx("span",{className:"text-bright/90",children:"Open Source Mentor, Maintainer & Contributor"})]})]}),g.jsxs(He.div,{custom:3,variants:qi,initial:"hidden",animate:"show",className:"mt-10 flex flex-wrap items-center gap-4",children:[g.jsx(Yc,{children:g.jsx("a",{href:"#projects",className:"font-display inline-block rounded-full bg-gradient-to-r from-violet-500 to-fuchsia-500 px-8 py-3.5 text-sm font-bold tracking-wide text-white shadow-[0_8px_40px_-8px_rgba(167,139,250,0.7)] transition-shadow hover:shadow-[0_8px_60px_-6px_rgba(232,121,249,0.8)]",children:"View my work"})}),g.jsx(Yc,{children:g.jsx("button",{onClick:t,className:"font-display inline-block cursor-pointer rounded-full border border-line px-8 py-3.5 text-sm font-bold tracking-wide text-bright transition-colors hover:border-violet/60 hover:bg-white/5",children:"Resume"})}),g.jsx("div",{className:"ml-1 flex items-center gap-1",children:Hy.map(a=>{const i=eP[a.icon];return g.jsx("a",{href:a.url,target:"_blank",rel:"noopener noreferrer","aria-label":a.name,className:"rounded-full p-3 text-body transition-all hover:text-bright hover:drop-shadow-[0_0_8px_rgba(167,139,250,0.8)]",children:g.jsx(i,{size:20})},a.name)})})]}),g.jsx(He.div,{custom:4,variants:qi,initial:"hidden",animate:"show",className:"mt-16 grid max-w-3xl grid-cols-2 gap-x-8 gap-y-6 border-t border-line pt-8 sm:grid-cols-4 md:mt-20 xl:max-w-5xl",children:PE.map(a=>g.jsxs("div",{children:[g.jsx("div",{className:"font-display text-3xl font-bold text-bright md:text-4xl",children:g.jsx(hE,{value:a.value,suffix:a.suffix})}),g.jsx("div",{className:"mt-1 text-xs leading-snug text-body/80 md:text-sm",children:a.label})]},a.label))})]}),g.jsxs(He.div,{initial:{opacity:0,x:60},animate:{opacity:1,x:0},transition:{duration:1,delay:.6,ease:[.21,.65,.32,.99]},className:"relative flex justify-center pb-4 lg:pb-0",children:[g.jsx("div",{className:"absolute inset-0 -z-10 m-auto h-[70%] w-[70%] rounded-full bg-gradient-to-tr from-violet-600/30 via-fuchsia-600/20 to-cyan-500/20 blur-3xl"}),g.jsx(He.div,{animate:{y:[0,-12,0]},transition:{repeat:1/0,duration:6,ease:"easeInOut"},className:"glow-card glass w-full max-w-[420px] overflow-hidden rounded-3xl xl:max-w-[460px]",children:g.jsx("div",{className:"aspect-[4/5] w-full",children:g.jsx(cr,{src:mt.heroImage,alt:`${mt.name} at his dev workstation`,label:"Workspace portrait (1200×1500)"})})})]})]}),g.jsx(He.a,{href:"#about","aria-label":"Scroll to about",initial:{opacity:0},animate:{opacity:1},transition:{delay:1.6},className:"absolute bottom-8 left-1/2 z-10 -translate-x-1/2 text-body/60 hover:text-bright",children:g.jsx(He.div,{animate:{y:[0,8,0]},transition:{repeat:1/0,duration:1.8,ease:"easeInOut"},children:g.jsx(YE,{size:22})})})]})}const nP=864e5,Xy=6e4,Zy=36e5,If=Symbol.for("constructDateFrom");function mo(t,a){return typeof t=="function"?t(a):t&&typeof t=="object"&&If in t?t[If](a):t instanceof Date?new t.constructor(a):new Date(a)}function Zt(t,a){return mo(a||t,t)}function ew(t,a,i){const s=Zt(t,i==null?void 0:i.in);return isNaN(a)?mo(t,NaN):(a&&s.setDate(s.getDate()+a),s)}function Af(t){const a=Zt(t),i=new Date(Date.UTC(a.getFullYear(),a.getMonth(),a.getDate(),a.getHours(),a.getMinutes(),a.getSeconds(),a.getMilliseconds()));return i.setUTCFullYear(a.getFullYear()),+t-+i}function tw(t,...a){const i=mo.bind(null,a.find(s=>typeof s=="object"));return a.map(i)}function Ef(t,a){const i=Zt(t,a==null?void 0:a.in);return i.setHours(0,0,0,0),i}function aP(t,a,i){const[s,r]=tw(i==null?void 0:i.in,t,a),c=Ef(s),h=Ef(r),p=+c-Af(c),m=+h-Af(h);return Math.round((p-m)/nP)}function iP(t,a,i){return ew(t,a*7,i)}function oP(t){return t instanceof Date||typeof t=="object"&&Object.prototype.toString.call(t)==="[object Date]"}function sP(t){return!(!oP(t)&&typeof t!="number"||isNaN(+Zt(t)))}function rP(t,a){const[i,s]=tw(t,a.start,a.end);return{start:i,end:s}}function nw(t,a){const{start:i,end:s}=rP(a==null?void 0:a.in,t);let r=+i>+s;const c=r?+i:+s,h=r?s:i;h.setHours(0,0,0,0);let p=1;const m=[];for(;+h<=c;)m.push(mo(i,h)),h.setDate(h.getDate()+p),h.setHours(0,0,0,0);return r?m.reverse():m}function Dn(t,a){const i=t<0?"-":"",s=Math.abs(t).toString().padStart(a,"0");return i+s}function aw(t,a){const i=Zt(t,a==null?void 0:a.in);if(isNaN(+i))throw new RangeError("Invalid time value");const s=(a==null?void 0:a.format)??"extended",r=(a==null?void 0:a.representation)??"complete";let c="",h="";const p=s==="extended"?"-":"",m=s==="extended"?":":"";if(r!=="time"){const f=Dn(i.getDate(),2),y=Dn(i.getMonth()+1,2);c=`${Dn(i.getFullYear(),4)}${p}${y}${p}${f}`}if(r!=="date"){const f=i.getTimezoneOffset();if(f!==0){const I=Math.abs(f),A=Dn(Math.trunc(I/60),2),R=Dn(I%60,2);h=`${f<0?"+":"-"}${A}:${R}`}else h="Z";const y=Dn(i.getHours(),2),v=Dn(i.getMinutes(),2),b=Dn(i.getSeconds(),2),T=c===""?"":"T",M=[y,v,b].join(m);c=`${c}${T}${M}${h}`}return c}function iw(t,a){return Zt(t,a==null?void 0:a.in).getDay()}function lP(t,a){return Zt(t,a==null?void 0:a.in).getMonth()}function cP(t,a){return Zt(t,a==null?void 0:a.in).getFullYear()}function dP(t,a,i){let s=a-iw(t,i);return s<=0&&(s+=7),ew(t,s,i)}function za(t,a){const i=()=>mo(a==null?void 0:a.in,NaN),r=mP(t);let c;if(r.date){const f=fP(r.date,2);c=gP(f.restDateString,f.year)}if(!c||isNaN(+c))return i();const h=+c;let p=0,m;if(r.time&&(p=yP(r.time),isNaN(p)))return i();if(r.timezone){if(m=wP(r.timezone),isNaN(m))return i()}else{const f=new Date(h+p),y=Zt(0,a==null?void 0:a.in);return y.setFullYear(f.getUTCFullYear(),f.getUTCMonth(),f.getUTCDate()),y.setHours(f.getUTCHours(),f.getUTCMinutes(),f.getUTCSeconds(),f.getUTCMilliseconds()),y}return Zt(h+p+m,a==null?void 0:a.in)}const Rs={dateTimeDelimiter:/[T ]/,timeZoneDelimiter:/[Z ]/i,timezone:/([Z+-].*)$/},hP=/^-?(?:(\d{3})|(\d{2})(?:-?(\d{2}))?|W(\d{2})(?:-?(\d{1}))?|)$/,uP=/^(\d{2}(?:[.,]\d*)?)(?::?(\d{2}(?:[.,]\d*)?))?(?::?(\d{2}(?:[.,]\d*)?))?$/,pP=/^([+-])(\d{2})(?::?(\d{2}))?$/;function mP(t){const a={},i=t.split(Rs.dateTimeDelimiter);let s;if(i.length>2)return a;if(/:/.test(i[0])?s=i[0]:(a.date=i[0],s=i[1],Rs.timeZoneDelimiter.test(a.date)&&(a.date=t.split(Rs.timeZoneDelimiter)[0],s=t.substr(a.date.length,t.length))),s){const r=Rs.timezone.exec(s);r?(a.time=s.replace(r[1],""),a.timezone=r[1]):a.time=s}return a}function fP(t,a){const i=new RegExp("^(?:(\\d{4}|[+-]\\d{"+(4+a)+"})|(\\d{2}|[+-]\\d{"+(2+a)+"})$)"),s=t.match(i);if(!s)return{year:NaN,restDateString:""};const r=s[1]?parseInt(s[1]):null,c=s[2]?parseInt(s[2]):null;return{year:c===null?r:c*100,restDateString:t.slice((s[1]||s[2]).length)}}function gP(t,a){if(a===null)return new Date(NaN);const i=t.match(hP);if(!i)return new Date(NaN);const s=!!i[4],r=Wi(i[1]),c=Wi(i[2])-1,h=Wi(i[3]),p=Wi(i[4]),m=Wi(i[5])-1;if(s)return SP(a,p,m)?vP(a,p,m):new Date(NaN);{const f=new Date(0);return!kP(a,c,h)||!xP(a,r)?new Date(NaN):(f.setUTCFullYear(a,c,Math.max(r,h)),f)}}function Wi(t){return t?parseInt(t):1}function yP(t){const a=t.match(uP);if(!a)return NaN;const i=Ec(a[1]),s=Ec(a[2]),r=Ec(a[3]);return TP(i,s,r)?i*Zy+s*Xy+r*1e3:NaN}function Ec(t){return t&&parseFloat(t.replace(",","."))||0}function wP(t){if(t==="Z")return 0;const a=t.match(pP);if(!a)return 0;const i=a[1]==="+"?-1:1,s=parseInt(a[2]),r=a[3]&&parseInt(a[3])||0;return CP(s,r)?i*(s*Zy+r*Xy):NaN}function vP(t,a,i){const s=new Date(0);s.setUTCFullYear(t,0,4);const r=s.getUTCDay()||7,c=(a-1)*7+i+1-r;return s.setUTCDate(s.getUTCDate()+c),s}const bP=[31,null,31,30,31,30,31,31,30,31,30,31];function ow(t){return t%400===0||t%4===0&&t%100!==0}function kP(t,a,i){return a>=0&&a<=11&&i>=1&&i<=(bP[a]||(ow(t)?29:28))}function xP(t,a){return a>=1&&a<=(ow(t)?366:365)}function SP(t,a,i){return a>=1&&a<=53&&i>=0&&i<=6}function TP(t,a,i){return t===24?a===0&&i===0:i>=0&&i<60&&a>=0&&a<60&&t>=0&&t<25}function CP(t,a){return a>=0&&a<=59}function MP(t,a,i){return iP(t,-1,i)}const zd="react-activity-calendar",Pc=8,sw=["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"],IP={months:sw,weekdays:["Sun","Mon","Tue","Wed","Thu","Fri","Sat"],totalCount:"{{count}} activities in {{year}}",legend:{less:"Less",more:"More"}},Pf="(prefers-color-scheme: dark)";function AP(){const[t,a]=x.useState(()=>typeof window>"u"?"light":window.matchMedia(Pf).matches?"dark":"light"),i=s=>{a(s.matches?"dark":"light")};return x.useEffect(()=>{const s=window.matchMedia(Pf);return a(s.matches?"dark":"light"),s.addEventListener("change",i),()=>{s.removeEventListener("change",i)}},[]),t}const rw=`${zd}--loading-animation`;function EP(t,a){const[i,s]=x.useState(!1);return x.useEffect(()=>{const r=`oklab(from ${t} l a b)`,c=a==="light"?`oklab(from ${t} calc(l * 0.96) a b)`:`oklab(from ${t} calc(l * 1.08) a b)`,h=document.createElement("style");h.innerHTML=`
      @keyframes ${rw} {
        0% {
          fill: ${r};
        }
        50% {
          fill: ${c};
        }
        100% {
          fill: ${r};
        }
      }
    `;const p=()=>{s(!0)};return document.head.appendChild(h),h.addEventListener("load",p),()=>{document.head.removeChild(h),h.removeEventListener("load",p),s(!1)}},[t,a]),i}const Rf="(prefers-reduced-motion: reduce)";function PP(){const[t,a]=x.useState(()=>typeof window>"u"?!0:window.matchMedia(Rf).matches);return x.useEffect(()=>{const i=window.matchMedia(Rf);a(i.matches);const s=r=>{a(r.matches)};return i.addEventListener("change",s),()=>{i.removeEventListener("change",s)}},[]),t}function RP(t,{minLevel:a,maxLevel:i}){if(t.length===0)throw new Error("Activity data must not be empty.");for(const{date:s,level:r}of t){if(!sP(za(s)))throw new Error(`Activity date '${s}' is not a valid ISO 8601 date string.`);if(r<a||r>i)throw new RangeError(`Activity level ${r} for ${s} is out of range. It must be between ${a} and ${i}.`)}}function LP(t,a=0){const i=DP(t),s=i[0],r=za(s.date),c=iw(r)===a?r:MP(dP(r,a)),h=[...Array(aP(r,c)).fill(void 0),...i],p=Math.ceil(h.length/7);return Ys(p).map(m=>h.slice(m*7,m*7+7))}function DP(t){const a=new Map(t.map(r=>[r.date,r])),i=t[0],s=t[t.length-1];return nw({start:za(i.date),end:za(s.date)}).map(r=>{const c=aw(r,{representation:"date"});return a.has(c)?a.get(c):{date:c,count:0,level:0}})}function Xn(t){return`${zd}__${t}`}function VP(){const t=new Date().getFullYear();return nw({start:new Date(t,0,1),end:new Date(t,11,31)}).map(i=>({date:aw(i,{representation:"date"}),count:0,level:0}))}function Ys(t,a){const i=a===void 0?0:t,s=a??t;if(s<=i)throw new RangeError("Invalid range: to must be greater than from");return Array.from({length:s-i},(r,c)=>i+c)}function jP(t,a=sw){return t.reduce((i,s,r)=>{const c=s.find(m=>m!==void 0);if(!c)throw new Error(`Unexpected error: Week ${r+1} is empty.`);const h=a[lP(za(c.date))];if(!h){const m=new Date(c.date).toLocaleString("en-US",{month:"short"});throw new Error(`Unexpected error: undefined month label for ${m}.`)}const p=i[i.length-1];return r===0||(p==null?void 0:p.label)!==h?[...i,{weekIndex:r,label:h}]:i},[]).filter(({weekIndex:i},s,r)=>s===0?r[1]&&r[1].weekIndex-i>=3:s===r.length-1?t.slice(i).length>=3:!0)}function OP(t,a,i){if(t.length!==7)throw new Error("Exactly 7 labels, one for each weekday must be passed.");return t.reduce((s,r,c)=>a.byDayIndex(c)?Math.max(s,Math.ceil(UP(r,i).width)):s,0)}function UP(t,a){if(typeof document>"u"||typeof window>"u")return{width:0,height:0};if(a<1)throw new RangeError("fontSize must be positive");if(t.length===0)return{width:0,height:0};const i="http://www.w3.org/2000/svg",s=document.createElementNS(i,"svg");s.style.position="absolute",s.style.visibility="hidden",s.style.fontFamily=window.getComputedStyle(document.body).fontFamily,s.style.fontSize=`${a}px`;const r=document.createElementNS(i,"text");r.textContent=t,s.appendChild(r),document.body.appendChild(s);const c=r.getBBox();return document.body.removeChild(s),{width:c.width,height:c.height}}function FP(t,a){if(!t)return{byDayIndex:()=>!1,shouldShow:!1};if(t===!0)return{byDayIndex:s=>(7+s-a)%7%2!==0,shouldShow:!0};const i=[];for(const s of t){const r=NP[s.toLowerCase()];i[r]=!0}return{byDayIndex:s=>i[s]??!1,shouldShow:t.length>0}}const NP={sun:0,mon:1,tue:2,wed:3,thu:4,fri:5,sat:6};function qP(t,a={minLevel:0,maxLevel:4}){const i=WP(a);if(t){const s=a.maxLevel-a.minLevel+1;return zP(t,s),t.light=t.light??i.light,t.dark=t.dark??i.dark,{light:Lf(t.light)?Fa([t.light[1],t.light[0],t.light[1]],a):Df(t.light)?Fa(t.light,a):t.light,dark:Lf(t.dark)?Fa([t.dark[1],t.dark[0],t.dark[1]],a):Df(t.dark)?Fa(t.dark,a):t.dark}}return i}function WP(t){return{light:Fa(["hsl(0, 0%, 26%)","hsl(0, 0%, 92%)","hsl(0, 0%, 26%)"],t),dark:Fa(["hsl(0, 0%, 92%)","hsl(0, 0%, 22%)","hsl(0, 0%, 92%)"],t)}}function zP(t,a){const i="The number of colors must match the number of activity levels controlled by the `minLevel` and `maxLevel` props.";if(typeof t!="object"||t.light===void 0&&t.dark===void 0)throw new Error(`The theme object must contain at least one of the fields "light" and "dark" with exactly 2 or ${a} colors respectively. ${i}`);if(t.light){const{length:s}=t.light;if(s!==2&&s!==3&&s!==a)throw new Error(`theme.light must contain exactly 2 or 3 or ${a} colors, ${s} passed. ${i}`);for(const r of t.light)if(typeof window<"u"&&!CSS.supports("color",r))throw new Error(`Invalid color "${r}" passed. All CSS color formats are accepted.`)}if(t.dark){const{length:s}=t.dark;if(s!==2&&s!==3&&s!==a)throw new Error(`theme.dark must contain exactly 2 or 3 or ${a} colors, ${s} passed. ${i}`);for(const r of t.dark)if(typeof window<"u"&&!CSS.supports("color",r))throw new Error(`Invalid color "${r}" passed. All CSS color formats are accepted.`)}}function Fa([t,a,i],{minLevel:s,maxLevel:r}){return Ys(s,r+1).map(c=>{if(c<0){if(c===s)return t;const p=(1-c/s)*100;return`color-mix(in oklab, ${a} ${parseFloat(p.toFixed(2))}%, ${t})`}if(c===0)return a;if(c===r)return i;const h=c/r*100;return`color-mix(in oklab, ${i} ${parseFloat(h.toFixed(2))}%, ${a})`})}function Lf(t){return t.length===2}function Df(t){return t.length===3}const Zn={container:t=>({width:"max-content",maxWidth:"100%",display:"flex",flexDirection:"column",gap:"8px",fontSize:`${t}px`}),scrollContainer:t=>({maxWidth:"100%",overflowX:"auto",overflowY:"hidden",paddingTop:Math.ceil(.1*t)}),calendar:{display:"block",overflow:"visible"},rect:t=>({stroke:t==="light"?"rgba(0, 0, 0, 0.08)":"rgba(255, 255, 255, 0.04)"}),footer:{container:{display:"flex",flexWrap:"wrap",gap:"4px 16px",whiteSpace:"nowrap"},legend:{marginLeft:"auto",display:"flex",alignItems:"center",gap:"3px"}}},Vf=x.lazy(()=>By(()=>import("./Tooltip-D5idU7TC-DAWscDSM.js"),[]).then(t=>({default:t.Tooltip}))),Qc=x.forwardRef(({data:t,blockMargin:a=4,blockRadius:i=2,blockSize:s=12,className:r,colorScheme:c,fontSize:h=14,labels:p,loading:m=!1,minLevel:f=0,maxLevel:y=4,renderBlock:v,renderColorLegend:b,showColorLegend:T=!0,showMonthLabels:M=!0,showTotalCount:I=!0,showWeekdayLabels:A=!1,style:R={},theme:j,tooltips:V={},weekStart:B=0},N)=>{const[_,ae]=x.useState(!1);if(x.useEffect(()=>{ae(!0)},[]),f>=y)throw new RangeError(`Minimum activity level must be less than maximum level. Got ${f} and ${y}.`);const G={minLevel:f,maxLevel:y},ce=qP(j,G),we=AP(),Me=c??we,_e=ce[Me],Fe=EP(_e[0],Me),Ne=!PP();if(m){if(Ne&&!Fe)return null;t=VP()}RP(t,G);const Le=t[0],Ke=cP(za(Le.date)),oe=LP(t,B),W=Object.assign({},IP,p),Z=M?h+Pc:0,H=FP(A,B),E=_&&H.shouldShow?OP(W.weekdays,H,h)+Pc:void 0;function O(){return{width:oe.length*(s+a)-a,height:Z+(s+a)*7-a}}function se(){return oe.map((ve,Ie)=>ve.map((it,Rt)=>{if(!it)return null;const $a=m&&Ne?{animation:`${rw} 1.75s ease-in-out infinite`,animationDelay:`${Ie*20+Rt*20}ms`}:void 0,Ya=g.jsx("rect",{x:0,y:Z+(s+a)*Rt,width:s,height:s,rx:i,ry:i,fill:ge(it.level),"data-date":it.date,"data-level":it.level,style:{...Zn.rect(Me),...$a}}),sa=v?v(Ya,it):Ya;return g.jsx(x.Fragment,{children:V.activity?g.jsx(x.Suspense,{fallback:sa,children:g.jsx(Vf,{text:V.activity.text(it),colorScheme:Me,placement:V.activity.placement??"top",hoverRestMs:V.activity.hoverRestMs,offset:V.activity.offset,transitionStyles:V.activity.transitionStyles,withArrow:V.activity.withArrow,children:sa})}):sa},it.date)})).map((ve,Ie)=>g.jsx("g",{transform:`translate(${(s+a)*Ie}, 0)`,children:ve},Ie))}function re(){if(!I&&!T)return null;const ve=t.reduce((Ie,it)=>Ie+it.count,0);return g.jsxs("footer",{className:Xn("footer"),style:{...Zn.footer.container,marginLeft:E},children:[m&&g.jsx("div",{children:" "}),!m&&I&&g.jsx("div",{className:Xn("count"),children:W.totalCount?W.totalCount.replace("{{count}}",String(ve)).replace("{{year}}",String(Ke)):`${ve} activities in ${Ke}`}),!m&&T&&g.jsxs("div",{className:Xn("legend-colors"),style:Zn.footer.legend,children:[W.legend.less&&g.jsx("span",{style:{marginRight:"0.4em"},children:W.legend.less}),Ys(f,y+1).map(Ie=>{const it=g.jsx("svg",{width:s,height:s,children:g.jsx("rect",{width:s,height:s,fill:ge(Ie),rx:i,ry:i,style:Zn.rect(Me)})},Ie),Rt=b?b(it,Ie):it;return g.jsx(x.Fragment,{children:V.colorLegend?g.jsx(x.Suspense,{fallback:Rt,children:g.jsx(Vf,{text:V.colorLegend.text(Ie),colorScheme:Me,placement:V.colorLegend.placement??"bottom",hoverRestMs:V.colorLegend.hoverRestMs,offset:V.colorLegend.offset,transitionStyles:V.colorLegend.transitionStyles,withArrow:V.colorLegend.withArrow,children:Rt})}):Rt},Ie)}),W.legend.more&&g.jsx("span",{style:{marginLeft:"0.4em"},children:W.legend.more})]})]})}function de(){return H.shouldShow?g.jsx("g",{className:Xn("legend-weekday"),children:Ys(7).map(ve=>{const Ie=(ve+B)%7;return H.byDayIndex(Ie)?g.jsx("text",{x:-Pc,y:Z+(s+a)*ve+s/2,dominantBaseline:"central",textAnchor:"end",fill:"currentColor",children:W.weekdays[Ie]},ve):null})}):null}function he(){return M?g.jsx("g",{className:Xn("legend-month"),children:jP(oe,W.months).map(({label:ve,weekIndex:Ie})=>g.jsx("text",{x:(s+a)*Ie,y:0,dominantBaseline:"hanging",fill:"currentColor",children:ve},Ie))}):null}function ge(ve){return _e[ve-f]}const{width:pe,height:be}=O();return g.jsxs("article",{ref:N,className:`${zd} ${r??""}`.trim(),style:{...R,...Zn.container(h)},children:[g.jsx("div",{className:Xn("scroll-container"),style:Zn.scrollContainer(h),children:g.jsxs("svg",{width:pe,height:be,viewBox:`0 0 ${pe} ${be}`,className:Xn("calendar"),style:{...Zn.calendar,marginLeft:E},children:[!m&&de(),!m&&he(),se()]})}),re()]})});Qc.displayName="ActivityCalendar";const BP={light:["#ebedf0","#9be9a8","#40c463","#30a14e","#216e39"],dark:["#161b22","#0e4429","#006d32","#26a641","#39d353"]},HP=(t,a)=>{if(typeof a!="function")return t;const i=a(t);if(!Array.isArray(i))throw Error("transformData() function must return a list of Activity objects.");for(const s of i){if(!_P(s))throw Error("transformData() must return a list of valid Activity objects.");if(typeof s.count!="number"||s.count<0)throw Error(`Required property "count: number" missing or invalid. Got: ${s.count}`);if(typeof s.date!="string"||!/\d{4}-\d{2}-\d{2}/.test(s.date))throw Error(`Required property "date: YYYY-MM-DD" missing or invalid. Got: ${s.date}`);if(typeof s.level!="number"||s.level<0||s.level>4)throw Error(`Required property "level: 0 | 1 | 2 | 3 | 4" missing or invalid: Got: ${s.level}.`)}return i},_P=t=>Object.prototype.toString.call(t)==="[object Object]";async function KP(t,a){const s=await fetch(`https://github-contributions-api.jogruber.de/v4/${t}?y=${String(a)}`),r=await s.json();if(!s.ok)throw Error(`Fetching GitHub contribution data for "${t}" failed: ${r.error}`);return r}const lw=x.forwardRef(({username:t,year:a="last",labels:i,transformData:s,throwOnError:r=!1,errorMessage:c=`Error – Fetching GitHub contribution data for "${t}" failed.`,...h},p)=>{const[m,f]=x.useState(null),[y,v]=x.useState(!1),[b,T]=x.useState(null),M=x.useCallback(()=>{v(!0),T(null),KP(t,a).then(f).catch(R=>{R instanceof Error&&T(R)}).finally(()=>{v(!1)})},[t,a]);if(x.useEffect(M,[M]),b){if(r)throw b;return g.jsx("div",{children:c})}if(y||!m)return g.jsx(Qc,{...h,data:[],loading:!0});const I=h.theme??BP,A={totalCount:`{{count}} contributions in ${a==="last"?"the last year":"{{year}}"}`};return g.jsx(Qc,{data:HP(m.contributions,s),labels:Object.assign({},A,i),ref:p,...h,theme:I,loading:!!h.loading||y,maxLevel:4})});lw.displayName="GitHubCalendar";function JP(){return g.jsxs("section",{id:"about",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx(Ga,{index:"01",title:"About",accent:"me"}),g.jsxs("div",{className:"grid gap-14 md:grid-cols-[1.4fr_1fr] md:gap-16",children:[g.jsxs("div",{children:[Sf.paragraphs.map((t,a)=>g.jsx(Pe,{delay:a*.08,children:g.jsx("p",{className:"mb-6 max-w-[68ch] text-base leading-relaxed text-body md:text-lg",children:t})},a)),g.jsx(Pe,{delay:.25,children:g.jsxs("blockquote",{className:"mt-8 border-l-2 border-violet/60 pl-5 font-display text-lg italic text-bright/80",children:["“",Sf.quote,"”"]})})]}),g.jsx(Pe,{delay:.15,children:g.jsxs("div",{className:"glow-card glass relative mx-auto aspect-square w-full max-w-sm overflow-hidden rounded-3xl",children:[g.jsx(cr,{src:mt.photo,alt:mt.name,className:"grayscale-[20%] transition-all duration-500 hover:grayscale-0"}),g.jsx("div",{className:"absolute inset-0 bg-gradient-to-t from-ink/70 via-transparent to-transparent"}),g.jsxs("div",{className:"absolute bottom-4 left-5",children:[g.jsx("div",{className:"font-display text-lg font-bold text-bright",children:mt.shortName}),g.jsx("div",{className:"text-sm text-body",children:mt.location})]})]})})]}),g.jsxs("div",{className:"mt-24",children:[g.jsx(Pe,{children:g.jsxs("h3",{className:"font-display mb-8 text-2xl font-bold text-bright",children:["Tech I ",g.jsx("span",{className:"text-gradient",children:"ship with"})]})}),g.jsx(Pe,{children:g.jsxs("div",{className:"glow-card glass rounded-3xl border-violet/25 p-7 md:p-9",children:[g.jsxs("div",{className:"flex flex-wrap items-baseline justify-between gap-2",children:[g.jsx("div",{className:"font-display text-lg font-bold uppercase tracking-[0.15em] text-gradient md:text-xl",children:Es.primary.title}),g.jsx("span",{className:"text-sm text-body/70",children:Es.primary.subtitle})]}),g.jsx("div",{className:"mt-5 flex flex-wrap gap-2.5",children:Es.primary.items.map(t=>g.jsx(Wa,{className:"!border-violet/30 !bg-violet/[0.08] !px-4 !py-1.5 !text-sm !text-bright/90",children:t},t))})]})}),g.jsx("div",{className:"mt-6 grid gap-6 md:grid-cols-2",children:Es.secondary.map((t,a)=>g.jsx(Pe,{delay:a*.1,children:g.jsxs("div",{className:"glow-card glass h-full rounded-2xl p-6 opacity-90",children:[g.jsx("div",{className:"font-display mb-4 text-sm font-semibold uppercase tracking-[0.18em] text-body",children:t.title}),g.jsx("div",{className:"flex flex-wrap gap-2",children:t.items.map(i=>g.jsx(Wa,{children:i},i))})]})},t.title))})]}),g.jsxs("div",{className:"mt-20",children:[g.jsx(Pe,{children:g.jsxs("h3",{className:"font-display mb-8 text-2xl font-bold text-bright",children:["Competitive ",g.jsx("span",{className:"text-gradient",children:"profiles"})]})}),g.jsx("div",{className:"grid grid-cols-2 gap-4 md:grid-cols-4",children:RE.map((t,a)=>g.jsx(Pe,{delay:a*.07,children:g.jsxs("a",{href:t.url,target:"_blank",rel:"noopener noreferrer",className:"glow-card glass group flex h-full flex-col justify-between rounded-2xl p-5",children:[g.jsxs("div",{className:"flex items-start justify-between",children:[g.jsx("span",{className:"font-display font-bold text-bright",children:t.name}),g.jsx(hr,{className:"text-body/50 transition-colors group-hover:text-violet",size:16})]}),g.jsx("span",{className:"mt-3 text-sm text-body",children:t.detail})]})},t.name))})]}),g.jsxs("div",{className:"mt-20",children:[g.jsx(Pe,{children:g.jsxs("div",{className:"mb-8 flex flex-wrap items-center justify-between gap-4",children:[g.jsxs("h3",{className:"font-display text-2xl font-bold text-bright",children:["Open-source ",g.jsx("span",{className:"text-gradient",children:"pulse"})]}),g.jsxs("a",{href:Ic.url,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-2 text-sm font-medium text-body transition-colors hover:text-violet",children:[g.jsx(dr,{size:16})," @",Ic.username]})]})}),g.jsx(Pe,{delay:.1,children:g.jsx("div",{className:"gh-cal glow-card glass overflow-hidden rounded-2xl p-6 text-body md:p-8",children:g.jsx(lw,{username:Ic.username,colorScheme:"dark",blockSize:11,blockMargin:4,fontSize:13,theme:{dark:["rgba(255,255,255,0.05)","#3b2b63","#6d4fae","#a78bfa","#e0d4ff"]}})})})]})]})}function GP({exp:t,index:a}){const[i,s]=x.useState(!1),r=t.highlights.length>0;return g.jsxs(Pe,{delay:.05,className:"relative pl-10 md:pl-16",children:[g.jsx("span",{className:`absolute left-[7px] top-7 h-3.5 w-3.5 -translate-x-1/2 rounded-full border-2 md:left-[15px] ${t.current?"border-emerald bg-emerald/30 shadow-[0_0_12px_rgba(110,231,183,0.8)]":"border-violet/70 bg-ink"}`}),g.jsxs("div",{className:"glow-card glass mb-8 rounded-2xl p-6 md:p-8",children:[g.jsxs("div",{className:"flex flex-wrap items-start justify-between gap-3",children:[g.jsxs("div",{children:[g.jsx("h3",{className:"font-display text-xl font-bold text-bright",children:t.role}),g.jsxs("div",{className:"mt-1 flex flex-wrap items-center gap-3",children:[g.jsx("span",{className:"font-medium text-violet",children:t.company}),t.current&&g.jsxs("span",{className:"inline-flex items-center gap-1.5 rounded-full bg-emerald/10 px-2.5 py-0.5 text-xs font-semibold text-emerald",children:[g.jsx("span",{className:"h-1.5 w-1.5 rounded-full bg-emerald"})," Now"]})]})]}),g.jsx("span",{className:"font-display text-sm text-body/70",children:t.period})]}),g.jsx("p",{className:"mt-4 leading-relaxed text-body",children:t.summary}),t.tech.length>0&&g.jsx("div",{className:"mt-4 flex flex-wrap gap-2",children:t.tech.map(c=>g.jsx(Wa,{children:c},c))}),g.jsx(cd,{initial:!1,children:i&&g.jsx(He.ul,{initial:{height:0,opacity:0},animate:{height:"auto",opacity:1},exit:{height:0,opacity:0},transition:{duration:.35,ease:"easeInOut"},className:"overflow-hidden",children:t.highlights.map((c,h)=>g.jsxs("li",{className:"mt-3 flex gap-3 text-sm leading-relaxed text-body first:mt-5",children:[g.jsx("span",{className:"mt-[7px] h-1.5 w-1.5 shrink-0 rounded-full bg-gradient-to-r from-violet to-fuchsia"}),c]},h))})}),(r||t.links.length>0)&&g.jsxs("div",{className:"mt-5 flex flex-wrap items-center gap-4",children:[r&&g.jsxs("button",{onClick:()=>s(!i),className:"inline-flex items-center gap-1.5 text-sm font-semibold text-violet transition-colors hover:text-fuchsia",children:[i?"Show less":"Deep dive",g.jsx(JE,{className:`transition-transform ${i?"rotate-180":""}`})]}),t.links.map(c=>g.jsxs("a",{href:c.url,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-1.5 text-sm text-body transition-colors hover:text-bright",children:[c.text," ",g.jsx(hr,{size:13})]},c.url))]})]})]})}function $P(){const t=x.useRef(null),{scrollYProgress:a}=rE({target:t,offset:["start 60%","end 75%"]}),i=io(a,{stiffness:90,damping:25});return g.jsxs("section",{id:"experience",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx(Ga,{index:"02",title:"Where I've",accent:"worked"}),g.jsxs("div",{ref:t,className:"relative",children:[g.jsx("div",{className:"absolute bottom-0 left-[7px] top-0 w-px bg-line md:left-[15px]"}),g.jsx(He.div,{style:{scaleY:i},className:"absolute bottom-0 left-[7px] top-0 w-px origin-top bg-gradient-to-b from-violet via-fuchsia to-cyan md:left-[15px]"}),LE.map((s,r)=>g.jsx(GP,{exp:s,index:r},`${s.company}-${s.role}`))]}),g.jsxs("div",{className:"mt-16",children:[g.jsx(Pe,{children:g.jsxs("h3",{className:"font-display mb-8 text-2xl font-bold text-bright",children:["Leadership & ",g.jsx("span",{className:"text-gradient",children:"community"})]})}),g.jsx("div",{className:"grid gap-4 sm:grid-cols-2",children:DE.map((s,r)=>g.jsx(Pe,{delay:r*.06,children:g.jsxs("div",{className:"glow-card glass h-full rounded-2xl p-5",children:[g.jsxs("div",{className:"flex items-start justify-between gap-3",children:[g.jsx("span",{className:"font-display font-semibold text-bright",children:s.role}),g.jsx("span",{className:"shrink-0 text-xs text-body/60",children:s.period})]}),g.jsx("div",{className:"mt-1 text-sm text-violet/90",children:s.org}),g.jsx("p",{className:"mt-2 text-sm leading-relaxed text-body",children:s.note})]})},s.role+s.org))})]})]})}function YP(t){return ft({attr:{viewBox:"0 0 512 512"},child:[{tag:"path",attr:{d:"M48 59.49v393a4.33 4.33 0 0 0 7.37 3.07L260 256 55.37 56.42A4.33 4.33 0 0 0 48 59.49M345.8 174 89.22 32.64l-.16-.09c-4.42-2.4-8.62 3.58-5 7.06l201.13 192.32zM84.08 472.39c-3.64 3.48.56 9.46 5 7.06l.16-.09L345.8 338l-60.61-57.95zM449.38 231l-71.65-39.46L310.36 256l67.37 64.43L449.38 281c19.49-10.77 19.49-39.23 0-50"},child:[]}]})(t)}const cw=t=>t==="github"?g.jsx(dr,{size:15}):t==="playstore"?g.jsx(YP,{size:15}):t==="prs"?g.jsx(_E,{size:15}):g.jsx(hr,{size:15}),QP={violet:"text-violet",cyan:"text-cyan",emerald:"text-emerald",amber:"text-amber"};function XP({p:t}){var a;return g.jsx(Pe,{children:g.jsx(uE,{max:3,className:"glow-card glass h-full overflow-hidden rounded-3xl",children:g.jsxs("div",{className:"flex h-full flex-col",children:[g.jsxs("div",{className:"relative aspect-video w-full",children:[g.jsx(cr,{src:t.image,alt:t.title,label:`${t.title} banner`,accent:t.accent}),g.jsx("div",{className:"absolute inset-0 bg-gradient-to-t from-ink/30 to-transparent"})]}),g.jsxs("div",{className:"flex flex-1 flex-col p-7 md:p-9",children:[g.jsxs("div",{className:"flex flex-wrap items-center gap-3",children:[g.jsx("h3",{className:"font-display text-2xl font-bold text-bright md:text-3xl",children:t.title}),t.badge&&g.jsx("span",{className:"rounded-full border border-emerald/40 bg-emerald/10 px-3 py-1 text-[11px] font-semibold uppercase tracking-wide text-emerald",children:t.badge})]}),g.jsx("p",{className:`font-display mt-2 text-sm font-semibold uppercase tracking-[0.14em] ${QP[t.accent]||"text-violet"}`,children:t.tagline}),g.jsx("p",{className:"mt-4 leading-relaxed text-body",children:t.description}),((a=t.metrics)==null?void 0:a.length)>0&&g.jsx("div",{className:"mt-6 flex flex-wrap gap-8",children:t.metrics.map(i=>g.jsxs("div",{children:[g.jsx("div",{className:"font-display text-2xl font-bold text-bright",children:i.value}),g.jsx("div",{className:"text-xs text-body/70",children:i.label})]},i.label))}),g.jsx("div",{className:"mt-6 flex flex-wrap gap-2",children:t.tech.map(i=>g.jsx(Wa,{children:i},i))}),g.jsx("div",{className:"mt-7 flex flex-wrap gap-3",children:t.links.map(i=>g.jsxs("a",{href:i.url,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-2 rounded-full border border-line px-4 py-2 text-sm font-medium text-bright transition-all hover:border-violet/60 hover:bg-violet/10",children:[cw(i.kind)," ",i.text]},i.url))})]})]})})})}function ZP(){return g.jsxs("section",{id:"projects",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx("div",{className:"blob right-[-15%] top-[10%] h-[420px] w-[420px] bg-violet-600/15"}),g.jsx(Ga,{index:"03",title:"Selected",accent:"work"}),g.jsx("div",{className:"relative grid gap-6 md:-mx-4 md:gap-8 xl:-mx-10 xl:grid-cols-2",children:VE.map(t=>g.jsx(XP,{p:t},t.title))}),g.jsxs("div",{className:"mt-24",children:[g.jsx(Pe,{children:g.jsxs("h3",{className:"font-display mb-8 text-2xl font-bold text-bright",children:["More ",g.jsx("span",{className:"text-gradient",children:"builds"})]})}),g.jsx("div",{className:"grid gap-5 sm:grid-cols-2 lg:grid-cols-4",children:jE.map((t,a)=>g.jsx(Pe,{delay:a%4*.06,children:g.jsxs("div",{className:"glow-card glass group flex h-full flex-col overflow-hidden rounded-2xl",children:[g.jsxs("div",{className:"relative aspect-video overflow-hidden",children:[g.jsx(cr,{src:t.image,alt:t.title,label:t.title,className:"transition-transform duration-500 group-hover:scale-105"}),g.jsx("div",{className:"absolute inset-0 bg-gradient-to-t from-ink/60 to-transparent"})]}),g.jsxs("div",{className:"flex flex-1 flex-col p-5",children:[g.jsx("h4",{className:"font-display font-bold text-bright",children:t.title}),g.jsx("p",{className:"mt-2 flex-1 text-sm leading-relaxed text-body",children:t.description}),g.jsx("div",{className:"mt-3 flex flex-wrap gap-1.5",children:t.tech.map(i=>g.jsx(Wa,{className:"!px-2.5 !py-0.5 !text-[11px]",children:i},i))}),g.jsx("div",{className:"mt-4 flex flex-wrap gap-3 border-t border-line pt-3",children:t.links.map(i=>g.jsxs("a",{href:i.url,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-1.5 text-xs font-medium text-body transition-colors hover:text-violet",children:[cw(i.kind)," ",i.text]},i.url))})]})]})},t.title))})]})]})}const jf={"Open Source":"text-violet border-violet/40 bg-violet/10",Mentorship:"text-fuchsia border-fuchsia/40 bg-fuchsia/10",DSA:"text-cyan border-cyan/40 bg-cyan/10",Academics:"text-emerald border-emerald/40 bg-emerald/10"};function eR(){return g.jsxs("section",{id:"achievements",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx(Ga,{index:"04",title:"Trophy",accent:"wall"}),g.jsx("div",{className:"grid gap-4 sm:grid-cols-2 lg:grid-cols-3",children:OE.map((t,a)=>g.jsxs(He.div,{initial:{opacity:0,y:24,scale:.97},whileInView:{opacity:1,y:0,scale:1},viewport:{once:!0,margin:"-40px"},transition:{duration:.55,delay:a%3*.08,ease:[.21,.65,.32,.99]},className:"glow-card glass flex h-full flex-col rounded-2xl p-6",children:[g.jsxs("div",{className:"flex items-start justify-between",children:[g.jsx(GE,{className:"text-violet",size:22}),g.jsx("span",{className:`rounded-full border px-2.5 py-0.5 text-[11px] font-semibold ${jf[t.tag]||jf["Open Source"]}`,children:t.tag})]}),g.jsx("h3",{className:"font-display mt-4 text-lg font-bold text-bright",children:t.title}),g.jsx("p",{className:"mt-2 text-sm leading-relaxed text-body",children:t.detail})]},t.title))})]})}function tR(t){return ft({attr:{viewBox:"0 0 640 512"},child:[{tag:"path",attr:{d:"M180.5,74.262C80.813,74.262,0,155.633,0,256S80.819,437.738,180.5,437.738,361,356.373,361,256,280.191,74.262,180.5,74.262Zm288.25,10.646c-49.845,0-90.245,76.619-90.245,171.095s40.406,171.1,90.251,171.1,90.251-76.619,90.251-171.1H559C559,161.5,518.6,84.908,468.752,84.908Zm139.506,17.821c-17.526,0-31.735,68.628-31.735,153.274s14.2,153.274,31.735,153.274S640,340.631,640,256C640,171.351,625.785,102.729,608.258,102.729Z"},child:[]}]})(t)}function nR(){return g.jsxs("section",{id:"writing",className:"relative w-full px-5 py-28 md:px-10 md:py-36 xl:px-16",children:[g.jsx(Ga,{index:"05",title:"Things I've",accent:"written"}),g.jsx("div",{className:"grid gap-5 md:grid-cols-2",children:Tf.posts.map((t,a)=>g.jsx(Pe,{delay:a%2*.08,children:g.jsxs("a",{href:t.url,target:"_blank",rel:"noopener noreferrer",className:"glow-card glass group flex h-full flex-col rounded-2xl p-6 md:p-7",children:[g.jsxs("div",{className:"flex items-start justify-between gap-4",children:[g.jsx("span",{className:"font-display text-xs font-semibold uppercase tracking-[0.2em] text-body/60",children:t.date}),g.jsx($E,{size:20,className:"shrink-0 text-body/40 transition-all group-hover:-translate-y-0.5 group-hover:translate-x-0.5 group-hover:text-violet"})]}),g.jsx("h3",{className:"font-display mt-3 flex-1 text-lg font-bold leading-snug text-bright transition-colors group-hover:text-gradient md:text-xl",children:t.title}),g.jsx("div",{className:"mt-4 flex flex-wrap gap-2",children:t.tags.map(i=>g.jsx(Wa,{children:i},i))})]})},t.url))}),g.jsx(Pe,{delay:.2,children:g.jsx("div",{className:"mt-10 flex justify-center",children:g.jsxs("a",{href:Tf.profileUrl,target:"_blank",rel:"noopener noreferrer",className:"glass inline-flex items-center gap-2.5 rounded-full px-6 py-3 text-sm font-semibold text-bright transition-all hover:border-violet/50 hover:shadow-[0_0_24px_-8px_rgba(167,139,250,0.7)]",children:[g.jsx(tR,{size:18})," Read all on Medium"]})})})]})}function aR(){return g.jsxs("section",{className:"relative w-full px-5 py-20 md:px-10 md:py-28 xl:px-16",children:[g.jsx("div",{className:"blob left-[-20%] top-[0%] h-[360px] w-[360px] bg-fuchsia-600/12"}),g.jsx(Pe,{children:g.jsxs("figure",{className:"glow-card glass relative rounded-3xl p-8 md:p-12",children:[g.jsx("span",{className:"font-display absolute -top-7 left-8 text-[110px] leading-none text-gradient select-none",children:"“"}),g.jsx("blockquote",{className:"relative text-lg leading-relaxed text-bright/90 md:text-xl",children:Ps.quote}),g.jsxs("figcaption",{className:"mt-8 flex flex-wrap items-center justify-between gap-4 border-t border-line pt-6",children:[g.jsxs("div",{children:[g.jsx("div",{className:"font-display font-bold text-bright",children:Ps.author}),g.jsx("div",{className:"text-sm text-body",children:Ps.title})]}),g.jsxs("a",{href:Ps.proofUrl,target:"_blank",rel:"noopener noreferrer",className:"inline-flex items-center gap-2 text-sm font-medium text-violet transition-colors hover:text-fuchsia",children:["View on LinkedIn ",g.jsx(hr,{size:14})]})]})]})})]})}const iR={github:dr,linkedin:Gy,instagram:$y,mail:Jy};function oR(){return g.jsxs("section",{id:"contact",className:"relative w-full px-5 py-28 md:px-10 md:py-40 xl:px-16",children:[g.jsx("div",{className:"blob bottom-[-10%] right-[10%] h-[400px] w-[400px] bg-violet-600/20"}),g.jsx(Ga,{index:"06",title:"Let's build",accent:"together"}),g.jsx(Pe,{children:g.jsxs("p",{className:"max-w-2xl text-lg leading-relaxed text-body md:text-xl",children:[g.jsxs("span",{className:"mb-4 flex items-center gap-2.5 text-emerald",children:[g.jsx("span",{className:"pulse-dot h-2 w-2 rounded-full bg-emerald"}),g.jsx("span",{className:"text-sm font-semibold uppercase tracking-[0.18em]",children:"Open to opportunities"})]}),"Whether it's a production Android app, a Kotlin Multiplatform migration, or an open-source collaboration — my inbox is always open."]})}),g.jsx(Pe,{delay:.15,children:g.jsx(Yc,{strength:.15,className:"mt-10 inline-block",children:g.jsx("a",{href:`mailto:${mt.email}`,className:"font-display block break-all text-3xl font-bold text-bright underline decoration-violet/50 decoration-2 underline-offset-8 transition-all hover:text-gradient hover:decoration-fuchsia sm:text-4xl md:text-6xl",children:mt.email})})}),g.jsx(Pe,{delay:.25,children:g.jsx("div",{className:"mt-12 flex flex-wrap gap-3",children:Hy.map(t=>{const a=iR[t.icon];return g.jsxs("a",{href:t.url,target:"_blank",rel:"noopener noreferrer",className:"glass inline-flex items-center gap-2.5 rounded-full px-5 py-3 text-sm font-medium text-bright transition-all hover:border-violet/50 hover:shadow-[0_0_24px_-8px_rgba(167,139,250,0.7)]",children:[g.jsx(a,{size:17})," ",t.name]},t.name)})})})]})}function sR(){return g.jsx("footer",{className:"border-t border-line",children:g.jsxs("div",{className:"flex w-full flex-wrap items-center justify-between gap-4 px-5 py-8 md:px-10 xl:px-16",children:[g.jsxs("div",{className:"flex items-center gap-3",children:[g.jsx(zy,{size:30}),g.jsxs("span",{className:"text-sm text-body",children:["Designed & built by ",g.jsx("span",{className:"font-medium text-bright",children:mt.name})]})]}),g.jsxs("span",{className:"text-xs text-body/60",children:["© ",new Date().getFullYear()," · Kotlin at heart, React when it counts."]})]})})}function rR(){return x.useEffect(()=>{const t=new sx({lerp:.12,smoothWheel:!0});let a;function i(r){t.raf(r),a=requestAnimationFrame(i)}a=requestAnimationFrame(i);function s(r){const c=r.target.closest('a[href^="#"]');if(!c)return;const h=document.querySelector(c.getAttribute("href"));h&&(r.preventDefault(),t.scrollTo(h,{offset:-72}))}return document.addEventListener("click",s),()=>{cancelAnimationFrame(a),document.removeEventListener("click",s),t.destroy()}},[]),g.jsx(XE,{children:g.jsxs("div",{className:"noise relative min-h-screen bg-ink text-body",children:[g.jsx(ZE,{}),g.jsxs("main",{children:[g.jsx(tP,{}),g.jsx(JP,{}),g.jsx($P,{}),g.jsx(ZP,{}),g.jsx(eR,{}),g.jsx(nR,{}),g.jsx(aR,{}),g.jsx(oR,{})]}),g.jsx(sR,{})]})})}function lR(){return g.jsx(zk,{children:g.jsxs(wk,{children:[g.jsx(zi,{path:"/",element:g.jsx(rR,{})}),g.jsx(zi,{path:"/interview_preparation",element:g.jsx(eC,{})}),g.jsx(zi,{path:"/interview_preparation/:categoryId/:topicId",element:g.jsx(cC,{})}),g.jsx(zi,{path:"*",element:g.jsx(ng,{to:"/",replace:!0})})]})})}wb.createRoot(document.getElementById("root")).render(g.jsx(Pt.StrictMode,{children:g.jsx(lR,{})}));export{dR as R,fb as a,Xn as b,Of as g,g as j,x as r};
